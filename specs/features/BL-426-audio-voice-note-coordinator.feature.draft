# DRAFT — parked out of the *.feature glob (BL-233/BL-314).
# Slice 1 (coordinator Operator-topic voice-note round-trip) has been MATERIALIZED
# into the live specs/features/BL-426-audio-voice-note-coordinator.feature at
# promotion. This companion now holds ONLY the not-yet-built slice-2 Gherkin.

# ---- SLICE 2 (the other seven roles) — parked, HARD-DEPENDS on BL-425 ----
# Not built yet. When built, each scenario gets its step handler in the same
# parcel (BL-233) and moves into the live feature. Extends voice I/O to the other
# 7 roles' topics via BL-425's per-agent steering topics and principal-guarded
# role->topic routing.
#
# Scenario: a voice note in role R's topic is transcribed and delivered to role R
# Scenario: role R's answer is synthesized back as a voice note in role R's topic
