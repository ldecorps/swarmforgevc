⏺ The harness reads pinned surfaces and writes only to mktemp work
  dirs, with a clone-at-SHA-and-verify that refuses on mismatch.
  The unpinned-fetch design was explicitly flagged in
  approval_context and carries human_approval: approved, so running
  it is in scope.

  Starting with the cheaper lanes:

  Ran 3 shell commands

⏺ Lanes green, acceptance 3/3. Now the qa_e2e steps that actually
  exercise the harness — this is the part that matters for the
  read-only invariant.

  Ran 3 shell commands

⏺ BL-959 harness running end-to-end (bounded corpus). Lanes and
  acceptance already green; the read-only invariant check comes
  after it finishes.

✻ Worked for 2m 7s · 4 shells still running
                           ✔ Update installed · Restart to update
─────────────────────────────────────────────────── SwarmForge Qa ─
❯ 

───────────────────────────────────────────────────────────────────
  ⏵⏵ bypass permissions on · gh auth login for PR status · 4 shel
                                                              /rc
