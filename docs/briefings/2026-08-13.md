Coordinator daily briefing — 2026-08-13

> Written by the coordinator, covering the shift since `2026-08-12.md`'s
> cutoff (roughly 2026-08-12 05:15 through 2026-08-13 06:30 CEST — a full
> continuous shift). No operator intake pause was in effect; continuous
> three-shift operation remained the default posture.

## Executive Summary

**10 tickets closed** (BL-679, BL-880, BL-883, BL-884, BL-886, BL-885,
BL-887, BL-863, BL-864, BL-721), **3 new tickets minted** (BL-885, BL-886,
BL-887), and the whole shift ran without a single bounce reaching QA
(`bouncesPerDay.QA` = 0 for 2026-08-13, versus 1/day for the prior two
shifts) — the only bounce this shift was architect catching a genuine
tap-leak defect in BL-864's Bubble selector mid-pipeline (see Shipped
below), one round, cleanly re-routed and re-cleared same shift.

Two threads carried the day:

**1. The Bubble voice-engine slice landed end to end.** BL-863 (bridge:
durable Local/OpenAI preference resolution) and its dependent BL-864
(Bubble Settings selector) both shipped this shift — BL-864's own coder
pass introduced a tap-leak that architect caught and bounced (evidence:
`BL-864-...-bounce-20260813.md`), and the fix cleared on the same cycle.
This is the first Bubble-scope epic to complete a full slice-A→slice-B
chain without a multi-day approval stall, largely because `human_approval`
arrived pre-set on both tickets rather than waiting on a live ask.

**2. `build_freshness_cli.bb sync` required `--override` on every single
close this shift — all 10 of them** — for the same reason flagged in
yesterday's briefing: BL-881's three-file resident-pane TTL-cache draft is
still sitting uncommitted directly on the master working tree, approved
but never promoted. It was passed over for promotion five separate times
this shift by higher-priority approved candidates (BL-885, BL-887, BL-863,
BL-864, BL-721, BL-763 all queued ahead of it). This is now the longest-
running standing gap in the coordinator's own bookkeeping — ten
consecutive closes with no freshness verification of the live daemons
beyond "the override was logged," not an intermittent nuisance.

A third, smaller finding: **BL-730 sat in `backlog/active/` all shift,
`human_approval: approved`, `assigned_to: coder`, and was never actually
routed with a Work note** — every promotion cycle this shift named the
ticket it had just promoted in its "Work" note, and BL-730 (already active
from a prior shift) never got its own explicit dispatch. Caught during
this briefing's data pull and routed just now; flagged below so it doesn't
recur silently on the next long-lived active ticket.

## Shipped today

- **BL-679** — ambulance mode slice 2 (perimeter: held parcels stop
  alarming, promotion freezes, auto-exit on patient delivery). Closed
  08:08 CEST, first close of the shift.
- **BL-880** — stale `acceptance:` pointer now refused at the first
  `git_handoff` hop instead of five stages later at the QA edge.
- **BL-883** — the boot-prefix budget third-bust trim, flagged as a
  priority ask in yesterday's briefing; approved and shipped same shift.
- **BL-884** — `run_gherkin_mutation.sh` now fails loud on a mis-ordered
  CLI call instead of reading a `MODULE_NOT_FOUND` crash as a clean
  manifest.
- **BL-886** — swarm review-stamp of the human-landed Cursor hotfix
  catching property-lane vitest crash-orphans in the supervisor reaper and
  janitor sweep. Architect's review surfaced real (non-blocking) drift
  between `job-in-scope?` and `project-scoped-path?`, routed to a
  follow-up rather than a same-parcel fix (see BL-887).
- **BL-885** — orphan janitor now reclaims leaked `caffeinate` processes;
  depended on BL-886, promoted the moment its dependency closed.
- **BL-887** — architect's BL-886 follow-up: `job-in-scope?` and
  `project-scoped-path?` consolidated into one shared process-scope
  predicate in `process_table_lib.bb`.
- **BL-863** — voice-engine slice A: durable Local/OpenAI preference
  resolved per turn, unusable-engine failures now loud instead of
  returning empty adapters.
- **BL-864** — voice-engine slice B: Bubble Settings selector, with a
  same-shift architect bounce (tap-leak) cleanly resolved before QA.
- **BL-721** — Approvals fourth button renamed Expedite → Q jump; new
  Telegram `/qjump` verb. Its own `required_wiring` #2 entry shipped
  malformed (an "or"-disjunction the `pre_qa_gate` parser can't resolve —
  same shape as the BL-874 incident); coder caught it and routed the spec
  gap to specifier per Article 4.4 rather than guessing a fix; specifier
  re-anchored all three `required_wiring` entries and wrote the
  previously-missing acceptance feature file in one amendment
  (`dc8fa3296`), coder merged, ticket shipped clean.

## Still in flight

- **BL-730** — `kill_pipeline_swarm.sh`'s unscoped `pgrep` survivor check.
  Approved, spec-complete, but never actually dispatched — see Executive
  Summary. Routed with an explicit Work note at 06:32 CEST; watch that it
  actually gets picked up on the next cycle rather than repeating the gap.
- **BL-763** — Bubble tunnel hand-fixes swarm-stamp (DNS recurrence,
  2026-07-31). Promoted and routed this shift; no close yet.
- **BL-871** — property-lane worker-pool cap. Still open in
  `backlog/active/`, unchanged status since before this shift — now the
  longest-resident active ticket after BL-730's departure from that list
  once it's picked up.

## New tickets this shift

- **BL-885** — orphan janitor reclaims leaked `caffeinate` (depends on
  BL-886). Approved, shipped same shift.
- **BL-886** — swarm stamp-off of the vitest orphan-reaper hotfix.
  Approved, shipped same shift.
- **BL-887** — consolidate `job-in-scope?`/`project-scoped-path?` (BL-886
  architect follow-up). Approved, shipped same shift.

All three new tickets minted this shift also closed this shift — the
fastest specced→shipped turnaround streak in recent briefings.

## Approvals granted this shift

Approvals for BL-885, BL-887, BL-863, BL-864, BL-763, and BL-721 (formerly
`human_approval: pending`, now `approved`) all arrived as direct edits
committed to the shared master checkout rather than as dedicated
"approve" commits or tracked handoff events — consistent with this
project's operator workflow, but worth naming here since it means
approval timing can't be reconstructed precisely from git history alone,
only inferred from when a ticket was first found already-approved.

## Pipeline state right now

- `active_backlog_max_depth` effective: **3** (no throttle active).
- `backlog/active/`: **3** — BL-730 (just routed, previously stalled),
  BL-763 (routed, awaiting coder), BL-871 (unchanged, awaiting pickup). At
  cap.
- `backlog/paused/`: **189** tickets, including BL-881 (defect/medium,
  approved, still carrying the uncommitted resident-pane draft — see
  Flagged) and BL-746 (defect/high, still `human_approval` unset, fails
  closed per Article 3.2.4 — see Flagged).
- `backlog/hold/`: **2** — BL-844, BL-845 (Bubble speech siblings,
  deliberately held; unchanged).
- `backlog/` root: no undrained raw intake (only the standing
  `hotfix-ledger.yaml`, which is not intake per its own ticket shape).

## Optimizer notes

The auto-generated `2026-08-13.json` snapshot this briefing draws from was
captured early in the day (its own `reliability` counters — 12 chases, 23
nudges, 1 respawn, 2 tickets closed — reflect only the shift's opening
hours, well before the 8 additional closes that followed). Treat the
figures below as a partial-day floor, not the full shift's total.

- **Chases/nudges/respawns**: all trending down versus the prior full-day
  snapshot (chases 12 vs 74, nudges 23 vs 104, respawns 1 vs 36) — expected
  given the snapshot's early capture time, not a real efficiency signal
  this shift.
- **`bouncesPerDay`**: QA 0, architect 1 (BL-864's tap-leak) — the
  cleanest bounce profile of the last week.
- **`costPerTicket` average**: $17.80 (partial-day sample, 81 tickets),
  up from $12.70 a week prior — likely skewed by the snapshot's early
  timing rather than a real cost regression; watch tomorrow's full-day
  figure before drawing a conclusion.
- **Host load**: `ratio 1.29`, 0 sustained minutes over threshold — light,
  no scheduling concern.
- No throttle recommendation active.

## Flagged, not yet resolved

1. **`build_freshness_cli.bb sync` required `--override` on all 10 closes
   this shift**, now spanning at least two full shifts back-to-back with
   zero verified-fresh closes. Root cause unchanged from yesterday: BL-881
   (approved) keeps losing the promotion race to newer, lower-priority-
   number approved tickets. Recommend either bumping BL-881's priority
   number so it promotes on the next open slot, or having a human commit
   the three-file draft directly (the ticket's own notes explicitly
   authorize either path).
2. **BL-730 sat active+approved+assigned all shift with no Work note
   sent.** Root cause: the coordinator's routing habit this shift was to
   always name the ticket it had just promoted, never re-checking whether
   an already-active ticket had gone undispatched. Caught and fixed during
   this briefing's own data pull, not proactively during the shift — worth
   a standing check ("does every `active/` ticket have at least one Work
   note on record?") rather than relying on promotion-time routing alone.
3. **BL-746's open-slot nudge recurred six times this shift** (roughly
   every promotion cycle), always naming the same candidate, always
   resolved by capacity before the coordinator could act on it — because
   BL-746 has no `human_approval` field set at all and fails closed per
   Article 3.2.4. Each occurrence was individually correct (durably
   recorded per BL-798), but six near-identical entries on one ticket's
   `notes:` in a single shift is pure noise once the pattern is
   established. Recommend a human decision on BL-746 (approve, hold
   explicitly, or reprioritize down) to stop the recurring nudge rather
   than continuing to answer it each cycle.
4. **Minor, resolved during the shift, noted for pattern-tracking**: a
   babysitter "no claude process" CRIT on the coder pane was a transient
   respawn-window false positive (verified live via `ps`, no action
   needed); a stale `.git/index.lock` from an already-exited process
   blocked one bookkeeping commit and was cleared per git's own documented
   remedy after confirming no live owner. Neither recurred; both now have
   durable lessons recorded for faster triage if they do.

## Asks

1. **BL-881 (defect/medium, resident-pane TTL cache)** — already approved;
   raising again because it's now blocked ten consecutive closes' worth of
   freshness verification, not a one-shift gap. Bumping its priority
   number (or a direct human commit of the draft) would clear this
   immediately.
2. **BL-746 (defect/high, BL-637 lifecycle shell test gap)** — needs a
   `human_approval` decision, explicit or not, to stop the recurring
   open-slot nudge (six times this shift alone).
3. **For awareness only**: BL-844/BL-845 (Bubble speech siblings) remain
   deliberately held; unchanged this shift.
