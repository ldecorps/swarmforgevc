Coordinator daily briefing — 2026-08-28

> Written by the coordinator, covering the overnight shift since
> `2026-08-27.md`'s ~12:55 BST cutoff through ~04:30 BST 2026-08-28 (night
> closing ceremony, BL-658). Cost & health sidecar:
> `docs/briefings/2026-08-28.json`.

## Business features delivered

**BL-1208** — record-bounce.js's revert remedy now requires established
authorship, not liveness alone; closed to `done/M8/` after QA approval
(`261dfe967`).

**BL-1211** — quarantine-lift check fails closed on an unresolvable bounce
commit; architect passed it clean, then the specifier grew scope mid-flight
(`7e40f0567`) to wire in the CLI call site the architect flagged. Routed back
to coder for the rebuild — not yet re-closed.

**BL-1216 (respec)** — DUPLICATE-ID findings will name the live copy and flag
content divergence. Re-specced clean by the specifier after the original
ticket text tripped the deprecator freshness gate on unrelated historical
narrative; promoted and routed to coder.

## The night's dominant story: BL-1214, 9th recurrence

The `main`-reset-to-origin bug fired again, live, mid-shift — the first time
its own author caught it inside a single turn rather than reconstructing it
after the fact. A `git reset` silently discarded 19 local-only, unpushed
commits (6 ticket closes, 7 promotions, 4 topic records, 1 accepted
rule_proposal — no product code). All 19 were still reachable as dangling
objects and were recovered via a conflict-free merge of the destroyed tip
(`51ec22622`); the specifier independently recovered BL-1226's spec and the
rule_proposal accept (`b6a0c44b4`, `c17d2bf36`). Everything destroyed this
occurrence has been verified byte-identical and restored — no net data loss,
but the underlying defect is still open and still firing.

**New and sharper this time:** the loss briefly reached `swarmforge/roles/
hardender.prompt`, a role prompt every hardener respawn reads — an accepted
constitutional rule was silently absent from it for several minutes with no
gate able to detect that. Prior occurrences stayed confined to backlog/spec
bookkeeping; this one showed the blast radius can reach live role behavior.

**Likely mechanism found, unticketed by the swarm but minted as BL-1230:** a
leaked test-fixture git repository at `backlog/.git` (own `master` branch, no
remote, created 2026-08-27) sitting inside the live backlog directory,
flagged as actively redirecting git commands. Needs a human to remove it —
not touched here per the never-delete-what-you-didn't-create rule.

A second, separate spec-gap surfaced from the same reset: BL-1196's own
in-flight scope amendment (adding `GIT_INDEX_FILE` handling) was destroyed
before QA could land on it; QA correctly held the parcel rather than ship
stale scope. The specifier has since restored the amendment; it's back in
`paused/` awaiting human approval.

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$9.02 (up from ~$8.13 prior briefing).
- **Closed (flow sidecar):** 3; **specced:** 0 (overnight window, not a
  full day).
- **Chases:** 17 (reliability sidecar).
- **Suite duration:** ~20.5k ms latest, down sharply from ~57.6k ms prior
  reading — still noisy, watch before trusting the drop.
- **Not done:** ~1,141 tickets total (7 active — at cap, 103 paused).
- **Top spenders (cumulative):** GH-22 (~$533), BL-657 (~$388), BL-523
  (~$221).

## Agent / pipeline business

- **`main` sync:** `proceed`, ahead of `origin/main` by unpushed
  coordinator bookkeeping (expected — QA pushes origin, not the
  coordinator).
- **Effective active depth:** 7, currently full (BL-1192, BL-1201, BL-1207,
  BL-1211, BL-1215, BL-1216, BL-1217).
- **Queued for the next open slot (priority order):** BL-1206 (confirmed
  promote — an earlier hold was a BL-1193 mint-tooling bug, not a real
  freshness issue).
- **Held for human approval:** BL-1227 (boot-prefix budget gate FAILING
  47648/44000 — 4th such overrun, previously undetected because BL-883
  pinned the one live check to its own fix commit), BL-1228, BL-1229
  (9 pilot-gate tests crash on a missing `checkOrphanedAuthoredDocs`),
  BL-1230 (the `backlog/.git` leak above), BL-1196 (restored amendment).
- **Mis-pooled ticket recovered:** BL-1196 was sitting in `done/` with
  `status: todo` and no pipeline history — a leftover from an unrelated
  2026-08-27 duplicate-id incident (`f8a41c1e2`); restored to `paused/`.
  Three siblings from the same incident (BL-644, BL-751, BL-1200) remain
  in `hold/`, which is human-held under Article 3.1 and untouched here.
- **Router false-positive (BL-1223 pattern) hit twice:** BL-1216's route to
  coder was refused on a stale dispatch-trail read (a topic-record commit,
  not real work); forced past it after confirming via `git log --all
  --grep` that no coder/cleaner/architect/hardener/documenter/QA commit
  existed for it yet.

## Optimizer recommendations

1. **Human:** remove the leaked `backlog/.git` (BL-1230) — it is flagged as
   actively redirecting git and is a plausible contributor to the BL-1214
   reset pattern.
2. **Human:** approve BL-1227 (high-severity, boot-prefix budget already
   failing) and BL-1196/1228/1229 when ready.
3. BL-1214 itself needs to stop being a recurring incident, not a recurring
   recovery: 9 occurrences now, several eating redos of earlier eaten
   commits. If the fix (owned, blocked on BL-1198 per the specifier's
   evidence) is stalled, it deserves expedite-lane attention once
   `human_approval` clears.
4. Watch the BL-1216 / BL-1206 style freshness- and dispatch-trail false
   positives — both were real tooling bugs (BL-1193, BL-1223), not policy
   holds; don't let the pattern normalize into reflexive `--force` without
   checking branch history first (BL-1204 already hid a real bounce behind
   this exact shape once).

## Process hiccups (and recovery)

1. **BL-1214, 9th occurrence** — see above; fully recovered, root defect
   still open.
2. **BL-1196 mis-pooled + separate spec-gap** — both now correctly parked;
   no data lost, verified byte-identical.
3. **Two dispatch-trail false positives** (BL-1216, and the earlier BL-1223
   pattern generally) — forced past with verification, not blind override.
4. **Night-closing ceremony (BL-658) rotation step no-oped** — this pack is
   a standing pack (every role has its own pane), so
   `rotate_to_role.sh documenter` correctly refused
   (`not-a-rotation-router`) and the daemon fell back to notifying the
   coordinator, per the code's own designed fallback. The documenter
   correctly declined a follow-up ask to author this briefing — that's the
   coordinator's job on this pack, not the documenter's; this file is that
   correction being applied.

## Asks

1. Remove the leaked `backlog/.git` fixture (BL-1230).
2. Approve BL-1227, BL-1228, BL-1229, BL-1196 when ready.
3. Confirm the BL-1214 fix (blocked on BL-1198) doesn't need expedite
   priority given the 9th recurrence and the near-miss on a live role
   prompt.
4. No PRs queued for merge from this session.
