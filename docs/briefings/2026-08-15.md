Coordinator daily briefing — 2026-08-15

> Written by the coordinator, covering the shift since `2026-08-14.md`'s
> cutoff (2026-08-14 07:53 through 2026-08-15 06:31 CEST — a full
> continuous shift). No operator intake pause was in effect; continuous
> three-shift operation remained the default posture.

## Executive Summary

**7 tickets closed** (BL-810, BL-871, BL-746, BL-890 was bounced-not-closed
— see below, BL-891, BL-892, BL-894), **4 new tickets minted** (BL-892,
BL-893, BL-894, BL-895 — two of the four closed same shift), and **one
significant process incident**: the architect design-review gate got
skipped entirely on two tickets in the same batch chain (BL-746, BL-891),
survived an earlier flag, and was only caught by QA at the very last gate.

**The architect-gate-skip incident is today's main finding.** Coder's
BL-746/BL-891 batch merged straight cleaner → hardener with no
architect-authored commit anywhere in the chain, and neither ticket
carried a `stage_skip_reasons.architect` entry. The documenter caught this
mid-shift and flagged it as a routing gap (`6be43385b`); the coordinator
relayed a routing note to send it back to architect. That routing did not
close the gap before the parcel reached QA — QA independently caught and
bounced **both** tickets for the same missing gate (`fd2e742bd`,
`ee14a3ce6`). Architect's redo then found a **real defect** on BL-746
while filling the gate (Article 4.4 in action, not stopping at "gate
filled"): a fixture-root leak in `bl746StopSwarmFixture.js` — +9 leaked
tmpdirs per property-suite run, uncovered by `fixture_reaper_lib.bb`'s
allowlist. A collateral casualty surfaced too: the bounce-lineage revert
that cleared BL-746's bad merge also deleted BL-891's own evidence files
(hardener pass, how-to doc, diagram annotations) as a side effect;
architect caught this and restored them verbatim (`d1970aa10`) before it
reached QA. Separately, **BL-890 bounced for a different procedural gap**
— documenter pass missing entirely, no changelog anywhere, no
`stage_skip_reasons.documenter` to justify it.

Net: three procedural bounces, zero code defects found by any of them
except the fixture leak surfaced incidentally — all three tickets did
eventually land clean. But two gate-skips reaching QA on the same chain,
after an earlier flag failed to close them, is worth a structural look:
does the mono-router's batch-commit chain (coder straight to hardener)
make it too easy for an unrun stage to go unnoticed until the last gate?

**A second open item, discovered near the end of the shift and still
unresolved: a stranded, uncommitted working-tree change** (not from any
swarm role — a direct human/Cursor edit) adds a "not-done ticket burndown"
chart to the morning briefing email (`notDoneBurndown.ts` +
`handoffd.bb`/`briefing_email_lib.bb` wiring, complete-looking). This
appears to be the exact chart type an operator ruling three weeks ago
(`d6db1ef56`, 2026-07-26) banned as "permanently out of scope," reasoning
that burndown misleads on a backlog with growing scope. Raised via
`role_ask` rather than committed or discarded; the working tree is
untouched pending the answer. Side effect: local `main` is currently 31
commits behind `origin/main` (11 ahead) because the conflicting file
blocks a clean merge — this is the master-main-reconcile sweep (BL-891,
shipped this same shift) correctly refusing to auto-merge over dirty
state rather than a new defect.

## Shipped today

- **BL-810** — retro-closed. Never physically bookkept from a human
  ruling weeks ago (satisfied by an operator hotfix, verified via BL-811);
  found and fixed during a routine coordinator sweep.
- **BL-871** — property-lane worker-pool cap. Its documenter-stage fix
  had already reached `main` on 2026-08-11 with zero QA record (3 days
  idle in `active/`); coordinator routed it to QA for genuine
  certification per BL-340 rather than closing on code presence alone.
  QA-certified and closed this shift.
- **BL-746** — BL-637 lifecycle shell test now drives the real
  `stop-swarm.sh` end to end. Bounced twice (architect gate missing, then
  the fixture-leak defect architect's redo found); closed clean.
- **BL-891** — master-main-reconcile sweep, so the coordinator's own local
  `main` ref advances after QA lands. Bounced once (architect gate
  missing, same shape as BL-746); closed clean. (Ironic footnote: this is
  the exact mechanism currently blocked by the stranded-burndown conflict
  above — working as designed, refusing to merge over dirty state.)
- **BL-892** — approval flips (Approve/Reject/Amend) now durably commit
  through `commit_integrity_cli` instead of risking staying disk-only.
  Human-requested directly from yesterday's briefing Ask #2.
- **BL-894** — swarm stamp-off: verified the `/queue` selection-poll
  hotfix's one-live-poll guard behaves as intended.
- **BL-890** — bounced for a missing documenter pass; not yet re-closed
  as of this briefing's cutoff (still mid-redo).

## Still in flight

- **BL-514** — retro-ticket RC health/respawn tooling + `./swarm ensure`
  wiring. Its original prose acceptance failed the QA gate outright (no
  feature file); specifier replaced it with a runnable 6-scenario feature
  file (`53652f006`) and bounced the gap back with the ticket, per Article
  4.4 spec-gap routing. Coder now owns adding the 6 scenarios' step
  handlers in the same parcel.
- **BL-765** — Bubble bridge reads capability flags + hold-music catalog
  without an APK ship. Promoted specifically to break BL-824's
  repeatedly-recurring open-slot nudge (it's the real dependency
  bottleneck for the whole Bubble thin-shell epic chain — BL-825/BL-829
  wait on it).
- **BL-806** — review-role forward names the bare received commit instead
  of its own; a clean pass currently leaves no trace in lineage.

## New tickets this shift

- **BL-892** — durable approval-flip commits (human-requested). Closed
  same shift.
- **BL-893** — Approvals: add Ambulance as a fifth choice
  (human-requested, "Est-ce qu'ambulance fait partie des choix
  d'approvals?"). Still `.feature.draft` — not yet build-ready, skipped
  twice by the promotion tool for that reason.
- **BL-894** — swarm stamp-off for the `/queue` selection-poll hotfix.
  Closed same shift.
- **BL-895** — `Specification.MD` carries 286 duplicated copies of a
  BL-607 paragraph (~1.5MB bloat), found by documenter during doc work
  and routed to specifier per Article 4.4. Approved, but deliberately held
  from promotion this shift — it targets the same file currently mid-
  conflict with the stranded burndown edit, and promoting it now risked
  compounding that conflict before the human's answer lands.

## Pipeline state right now

- `active_backlog_max_depth` effective: **3** (no throttle active).
- `backlog/active/`: **3** — BL-514, BL-765, BL-806. At cap.
- `backlog/paused/`: **181** tickets, including BL-895 (approved, held —
  see above) and BL-893 (approved, not yet build-ready).
- `backlog/hold/`: **2** — BL-844, BL-845 (Bubble speech siblings,
  deliberately held; unchanged).
- `backlog/` root: no undrained raw intake.
- Local `main`: **31 behind / 11 ahead of `origin/main`**, blocked by the
  stranded burndown-chart conflict — see Flagged #1.

## Optimizer notes

- **Severe, sustained host-load event this shift**: load ratio peaked
  around 21.5–27x normal (4-core host, 1-min load average briefly hit
  ~110), sustained roughly 55 minutes. QA's process stayed alive and
  progressing throughout (confirmed via `ps`/CPU-time deltas across
  several checks) but its `stop-swarm.sh` end-to-end suite ran
  dramatically slower than normal as a result — one QA pass took nearly
  90 minutes largely on account of this contention window, not a hang.
  This is outside the Article 3.5 circuit breaker's scope (that watches
  swarm health signals — bounce rate, chase/nudge telemetry — not raw
  host CPU), but worth a look at what else was competing for this host's
  4 cores during that window.
- **`costPerTicket` average**: $13.56, up $0.85 from the prior snapshot —
  a mild reversal of the two-day improving trend noted in yesterday's
  briefing. Plausibly explained by the BL-746/BL-891 double-bounce cycle
  (extra architect/hardener/QA passes on the same tickets); not yet a
  concerning trend on one data point.
- **Chases/nudges**: 9 chases, 76 nudges this snapshot window — nudges
  dominated by the recurring BL-824 open-slot signal (epic, never
  directly promotable) until BL-765 was promoted late in the shift to
  break the cycle.
- No throttle recommendation active on `active_backlog_max_depth` itself.

## Flagged, not yet resolved

1. **Stranded burndown-chart work, pending human decision.** Uncommitted
   working-tree changes (not from any swarm role) implement a "not-done
   ticket burndown" chart for the morning briefing email — complete-
   looking, coherent, spans `notDoneBurndown.ts`,
   `render-briefing-burndown.ts`, `handoffd.bb`, `briefing_email_lib.bb`,
   and a `Specification.MD` doc addition. This looks like the exact chart
   type an operator ruling (`d6db1ef56`, 2026-07-26) explicitly banned as
   "permanently out of scope" for the PWA dashboard, with reasoning
   (burndown misleads on a growing-scope backlog) that reads as
   surface-agnostic rather than PWA-specific. Raised via `role_ask`
   rather than committed or discarded unilaterally; left untouched.
   Blocks local `main` from reconciling with `origin/main` in the
   meantime (31 commits behind as of this briefing).
2. **Architect-gate-skip recurrence, two tickets, one batch, past an
   earlier flag.** See Executive Summary. Worth asking whether the
   mono-router's straight coder→hardener batch-commit pattern needs a
   structural checkpoint (an automated pre-QA check for a missing
   architect commit when the ticket has no documented skip reason) rather
   than relying on a downstream role noticing and a routing note landing
   correctly.
3. **BL-895 deliberately held from promotion** despite being approved and
   otherwise eligible, because its target file (`Specification.MD`) is
   mid-conflict with Flagged #1. Revisit once that's resolved.

## Asks

1. **Burndown-chart hotfix (Flagged #1)** — needs the human's decision:
   land as a hotfix (if the 2026-07-26 ban was PWA-dashboard-specific) or
   hold/discard (if it applies here too). Currently blocking local `main`
   reconciliation.
2. **Architect-gate-skip structural fix (Flagged #2)** — for awareness;
   no immediate ask, but worth a specifier ticket if it recurs a third
   time.
3. **For awareness only**: BL-844/BL-845 (Bubble speech siblings) remain
   deliberately held; unchanged this shift.
