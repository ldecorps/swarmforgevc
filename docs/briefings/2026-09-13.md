Coordinator daily briefing — 2026-09-13

> Composed by the documenter per human ruling A (2026-09-07): the morning
> briefing is documenter-owned agentic work, not coordinator-owned.
> Covers the period since `2026-09-11.md`'s cutoff (commit `af7fb19c55`).

## The story of this window: the standing-red discharge sweep keeps finding and closing its own tail

The past two days kept working the standing-red register the same way the
last several briefings describe: each closed row exposes another hidden red
in the same neighborhood (mostly `bb`-lane load-closure/chokepoint assertions
and property-lane fixtures), which gets minted and worked in turn. Twenty
defects closed this window, the register discharged and refilled in step
(34 rows / 21 tickets both before and after — see below), and the
BL-1518-a root-guard fallout that dominated 2026-09-11 (seven senders
refused, a QA bounce dropped) is now fully landed: BL-1537 (all seven
senders draft under their own root), BL-1536 (a QA bounce is never stamped
merge-only), BL-1535 (the chase never displaces a working resident), and
BL-1544/BL-1546/BL-1547 (an ambiguous or closed-ticket commit subject is
refused by name at land time, never silently excluded) all closed.

## Defects closed this window

Twenty tickets closed since the previous briefing:

- **BL-1455** — a ticket re-pended for a second ruling now posts a fresh
  approval ask instead of reading its earlier, closed ask as still live
  (BL-1348 and BL-1408 had been dark since 2026-09-06).
- **BL-1460** — the events bridge seeds `lastSnapshot` on connect so an idle
  connection gets exactly one snapshot instead of a duplicate broadcast race
  with the keepalive tick.
- **BL-1464** — the BL-1297 merge-only fixture now authors the merged side
  branch under the parcel's own ticket id instead of a foreign one, so a
  merge of the parcel's own content is no longer refused as a sibling's.
- **BL-1482** — the BL-582 wiring assertion now names the imported symbol
  and its module instead of pinning the whole import line, surviving
  BL-1368's second imported name.
- **BL-1484** — the four hook-installing shell fixtures (ticket-deletion,
  commit-size, merge-deletion, retirement-readdition guards) now derive
  their copy set from the real hooks they install instead of a hand-typed
  list that drifted from BL-1252's `run_commit_guards.sh` wiring.
- **BL-1518-a, BL-1535, BL-1536, BL-1537, BL-1544, BL-1546/BL-1547** — the
  full BL-1518-a aftermath: the handoff-CLI root guard itself, the chase
  no longer displacing a mid-parcel resident, all seven senders now
  drafting under their own root, a QA bounce never stamped merge-only, and
  an ambiguous or closed-ticket commit subject refused by name rather than
  silently dropped at land.
- **BL-1524, BL-1525, BL-1526** — the daemon load-closure and spawn-target
  standing reds: four stray direct-process/spawn sites now route through
  the bounded chokepoint, and the human ruled the second wall-clock runner
  (`bounded_run_lib.bb`) stands beside the chokepoint rather than folding
  into it.
- **BL-1525** *(see above)*, **BL-1529** — a script-originated `git_handoff`
  (the salvage `/redo`/`/reroute` verbs and the daemon's dispatch-gap
  auto-route) now completes the two-call self-audit itself instead of
  reporting success on a swallowed first call that queued nothing.
- **BL-1530** — the three standing shell tests driving `swarm_handoff.sh`
  now speak the two-call self-audit protocol instead of asserting a queue
  on the first call.
- **BL-1534** — the BL-1373 path-set-cache property test bounds its real
  `babysitter_check.bb` sweeps per draw and requires at least one decisive
  draw, instead of timing out under host load and passing vacuously.
- **BL-1537** *(see above)*, **BL-1543** — the master-checkout drift wiring
  test now asserts BL-1139's live auto-repair contract (a `RESTORED` note,
  drift left silently repaired) instead of the superseded detect-only
  contract, with no fixture daemon left behind.
- **BL-1545** — the BL-1374 sync-merge passenger test reads the `:paths` map
  key instead of grepping the whole own-paths report (which now also
  carries `:excluded`), and pins both ends of its origin/main walk so the
  range is never vacuously empty.
- **BL-1550** — a draft cleanup's `removeDraftIfPresent` no longer throws
  `EISDIR` when a property draw produces `.` as the draft name; the helper
  leaves a directory in place instead of unlinking it, and the property now
  quantifies only over strict children of the fixture directory.

## Standing-red register: 34 rows / 21 tickets (unchanged from last briefing)

The register's raw numbers look flat, but the composition turned over
almost completely: nearly every row that stood at the last briefing's
cutoff has since closed (BL-1524, BL-1525, BL-1526, BL-1529, BL-1530,
BL-1534, BL-1537, BL-1543, BL-1545, BL-1550 among them), and the same
sweep surfaced a fresh set naming BL-1457, BL-1462, BL-1483, BL-1485,
BL-1486, BL-1489, BL-1496, BL-1498 (active), BL-1499, BL-1500, BL-1502,
BL-1503, BL-1527, BL-1533, BL-1538, BL-1539, BL-1540, and BL-1541. No row
in the register currently names a closed or absent ticket.

## New this window

Thirteen tickets minted since the previous briefing, all from the
standing-red/root-guard-fallout sweep or its direct consequences:

- **BL-1533, BL-1534** — two unowned property-lane reds (delta-sign
  reach-floor sampled not constructed; BL-1373's real-sweep timeout).
- **BL-1535, BL-1536, BL-1537** — the chase-displacement, merge-only
  bounce-stamp, and seven-sender root-guard fallout from BL-1518-a.
- **BL-1538, BL-1539, BL-1540, BL-1541** — four more standing-red owners
  from the coder's `bl1028` unowned-red note (a promotion-control fixture
  rotted by three later `promotion_gates_lib.bb` load edges, a fail-open
  `grep -q` under `pipefail` in the BL-998 guard's derivation, two shell
  tests dispatching real helpers through a relative `SCRIPT_DIR/../`, and
  five `bb` property runners still speaking the single-call handoff
  protocol).
- **BL-1542** — a Bubble live-screen ticket-strip collapse control,
  draining the root intake queue.
- **BL-1543** — retired above; also produced its own split-off,
  **BL-1548** (a case the coder's spec-gap note flagged as dropped).
- **BL-1544, BL-1545** — the ambiguous-subject land-attribution fix and the
  BL-1374 sync-merge passenger test's oracle-rot fix.
- **BL-1546, BL-1547** — the closed-ticket-owned-path land adjudication
  (BL-1537's own hold), splitting the guard fix from the send-time
  scope-gate change.
- **BL-1549** — a stamp-off ticket for hotfix `5bdf93beed` (an ephemeral
  consult session behind the departing-mid-parcel refusal).
- **BL-1550** — retired above.

## Backlog snapshot

- `backlog/active/`: **1** (BL-1498 — the supervisor test's zsh-`PATH` and
  startup-grace pid-age fixture fix)
- `backlog/paused/`: 113
- `backlog/hold/`: 0
- `backlog/done/`: 1,355 total (+20 since 2026-09-11)

## Operator watch items

- **`cron` has been dead on this WSL host since 2026-08-30** — nothing
  scheduled fires until a human runs `sudo service cron start`
  interactively. Still open as of this writing.
- **Launch can boot the resident as a second coordinator** when the
  mono-router active-role marker reads `coordinator` — verify any launch by
  process tree, never `./swarm status`; recovery is `rotate_to_role.sh
  coder`. Reported open through 2026-09-11; re-verify before trusting the
  marker on the next launch.
- **A QA bounce stamped `non-forwarding: true` can silently merge-only
  complete, dropping the fix** (BL-1536, closed this window) — a rotation
  hop can also strand an unsent `tmp/handoff.txt`; this recurred twice in
  one shift before BL-1536 landed. Watch for a recurrence on any bounce
  handled between BL-1536's landing and its own QA pass.
- **The active pack may still have no `specifier` row in `roles.tsv`**
  depending on which pack is live — confirm via `.swarmforge/roles.tsv`
  before routing a note to `specifier`.
- **Two Telegram/icon-shaped surfaces exist and are not interchangeable**
  (static backlog PWA vs. the live holistic UI) — confirm which one a new
  request means before speccing.
- Claude-seat capacity has been confirmed restored and producing since
  2026-09-10 06:09:58Z; a `no claude process` escalation is a real signal,
  not fallback noise.
- **Approval asks on Telegram must carry pros/cons per option** (human
  directive, 2026-09-11) — a ruling ask answered without weighed
  alternatives is out of policy; don't approve unasked.
