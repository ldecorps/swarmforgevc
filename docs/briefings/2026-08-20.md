Coordinator daily briefing — 2026-08-20

> Written by the coordinator, covering the shift since `2026-08-19.md`'s cutoff
> (2026-08-19 ~05:30 BST through 2026-08-20 ~06:00 BST). The pack was
> `full-forge` throughout; no pack switch this shift. The dominant story is not
> throughput — it is that the swarm spent the night diagnosing its own
> infrastructure faster than those diagnoses could be authorised to be fixed.

## Executive Summary

**35 tickets closed** across the full shift, **8 of them after 22:00 BST**
(BL-954, BL-827, BL-956, BL-955, BL-935, BL-957, BL-958, BL-960, BL-910,
BL-959, BL-571 — QA landed the last seven in three batches).

**13 tickets minted** (BL-961 … BL-973). Every one is a defect or chore in the
swarm's *own* machinery: launcher, promotion gate, chase/wake, acceptance gate,
handoff daemon, test fixtures. Not one is product work.

**Three live infrastructure failures ran concurrently for most of the night**,
all confirmed with direct evidence, none resolved:

1. **handoffd is silently stalled and flapping.** Confirmed state `U`
   (uninterruptible wait), one `heartbeat cycle` line in the whole log, and
   each fresh daemon re-stalls **~20 seconds** after restart. The restart
   remedy is already running continuously — via the freshness cron — and
   failing. Cascade preconditions from the 2026-08-05 outage (duplicate
   supervisors, hung `swarm-cleanup`) were checked repeatedly and remained
   **absent**, so this stayed degraded-but-stable rather than fatal.
   Fix = BL-967, in flight at hardener.
2. **Roles are unwakeable once they leave a background shell behind.** QA sat
   idle at a prompt for **~70 minutes** holding a *completed* BL-910 gate while
   every wake was refused — `deliver-notify-skip-busy QA` — because hours-old
   background shells read as "busy". handoffd logged that same skip three times
   in one second immediately before it died. Fix = BL-970, **unapproved**.
3. **The acceptance-contract gate fails OPEN on every QA-bound send.** Verbatim
   warning captured on 3/3 sends; root-caused by the specifier to three step
   files calling `resolveMainCheckout(__dirname)` at module load inside a
   materialised temp tree that is not a git repo. Acceptance contracts have not
   been enforced. Fix = BL-968, **unapproved**.

## Shipped today (post-22:00 BST portion)

| Ticket | What it was |
|---|---|
| BL-954, BL-827, BL-956, BL-955 | landed and closed at shift start |
| BL-935 | vitest fork-pool cap under full-forge on macOS |
| BL-957 | promotion gate refuses unsatisfied `depends_on` |
| BL-958 | full-forge tmux control-plane crash root cause |
| BL-960 | heal-wrapper parse-safe round-trip |
| BL-910 | briefing burndown projected ETA |
| BL-959 | APS candidate toolchain equivalence |
| BL-571 | sequential rotation dormant parity |

## Still in flight (7 of 7 — at cap all shift)

BL-946 (coder), BL-961 / BL-962 / BL-966 (cleaner), BL-967 (hardener),
BL-948, BL-964.

## New tickets this shift

**Approved and already active:** BL-961, BL-962, BL-964, BL-966, BL-967.
**Approved, queued:** BL-963 (blocked on BL-957 — now closed, so unblocked),
BL-965 (blocked on BL-960 — now closed, so unblocked).
**Awaiting human approval (6):**

| Ticket | Sev | Pri | What it fixes |
|---|---|---|---|
| BL-968 | high | 20 | acceptance gate silently skipping on every send |
| BL-970 | high | 20 | roles unwakeable (froze QA for 70 min tonight) |
| BL-971 | high | 20 | property lane can never come back green |
| BL-969 | high | 30 | flaky unit suite |
| BL-972 | med | 30 | pre-QA gate blocks on evidence, not subject mentions |
| BL-973 | med | 30 | 4 bb copy-lists red since BL-911, suite ungated |

## Pipeline state right now

Active 7/7. Paused: 6 pending approval, 108 approved. handoffd log silent since
06:00 BST, single supervisor, no cleanup processes. QA recovered and cleared
three landings after its stall.

## Optimizer notes

- **The approval gate, not capacity, is now choosing what gets built.** Five
  consecutive slots this shift went to `medium`/`chore` work (BL-946, BL-948,
  BL-964, BL-966, BL-961) because they were the only *approved* buildable
  candidates, while **four `defect`/high tickets sat unpromotable**. No
  expedited ticket has been promotable since BL-967 took the 01:43 slot. This
  is a measurable inversion of Article 3.2.4's intent, produced entirely
  outside the promotion machinery.
- **QA's gate is running a suite that cannot pass.** The property lane shows
  `2 failed | 115 passed`, both failures pure wall-clock exhaustions — bl868 at
  **126374ms against a 60000ms** budget, bl632 at **154346ms against 90000ms**.
  That is ~36 minutes per parcel spent on a known-red verdict at the *final*
  gate. Fixing BL-971 is the single highest-leverage throughput change
  available, and it costs no capacity.
- **Alert noise scales with delivery.** The on-main sweep produced **6 false
  CRITs**, one per QA landing, each re-firing once per sweep indefinitely (it
  keeps no adjudication memory). Every landing permanently adds one alert
  source. Recurrence 6 was authored by *me* — the reference-freshness guard
  compels the coordinator to `git merge origin/main` to work at all, so our own
  tooling generates the condition another tool misreports as a breach.
- **Boot prefix headroom is effectively gone: 43997/44000.** Every future
  inline constitution rule now requires evicting an existing one; this shift
  already spent a BL-859 budget swap to land one.

## Flagged, not yet resolved

1. **`swarmforge/scripts/daemon_log_freshness.conf` is uncommitted** and has
   now blocked **three** BL-328 build-freshness syncs. It raises handoffd's
   log-freshness threshold 120→300s and is currently the only thing throttling
   the daemon flap. One `git checkout` from vanishing.
2. **BL-960/BL-965 sequencing.** BL-960 has *landed*, so the heal wrapper's
   `mktemp` leak (no `trap`; leaks one file per interrupted command, wrapping
   every Bash call) is live on main while BL-965, its fix, sits unpromoted
   behind it by dependency.
3. **Coordinator bookkeeping error, mine.** Three close commits read
   `Close BL: move to done` instead of naming BL-957 / BL-958 / BL-960 — a
   shell expansion (`${t%%-*}`) truncated the id. **The closes themselves are
   sound**: all three files are in `done/M8`, absent from `active/`, and the
   integrity CLI recorded the correct `closed-ticket-ids`. Only the commit
   subject is wrong, so `git log --grep 'Close BL-957'` finds nothing. Not
   rewritten (shared history); recorded here instead. Pointedly relevant to
   BL-972: subject lines are not evidence.
4. **BL-893 is parked on an unanswered question** — *"Confirm hold-only. If you
   want Approve+Ambulance as a single tap, say so."* The promotion gate refuses
   it; it needs a human answer, not a slot.

## Asks

1. **Approve BL-970 first** — it is the only defect that actually froze a role
   this shift, and it is live now.
2. **Approve BL-971** — highest throughput leverage per the numbers above.
3. **Approve BL-968** — best-evidenced ticket in the backlog; three roles
   confirmed it independently. Acceptance contracts are unenforced until it lands.
4. **Rule on BL-960/BL-965 sequencing** — fold the one-line `trap` into a
   follow-up, or accept the leak window knowingly.
5. **Commit or revert `daemon_log_freshness.conf`** — deliberately, either way.
6. **Answer BL-893's hold-only question.**

---

## Day shift addendum — 2026-08-20 ~06:00 BST through ~21:15 BST

> Written by the coordinator, covering the day shift since the section above's
> ~06:00 BST cutoff. Pack stayed `full-forge` throughout, with one staffing
> change (below). The story flips from last night's: infrastructure diagnosis
> gave way to infrastructure *fixing* — every "unapproved" defect from the
> Asks list above landed and closed today, and the shift's own defects were
> caught and re-fixed within the same shift rather than carrying overnight.

### Executive summary

**21 tickets closed** since 06:00: BL-946, BL-948, BL-591, BL-961, BL-962,
BL-963, BL-964, BL-965 (×2 — a duplicate close commit, ticket only closed
once), BL-966, BL-967, BL-968, BL-969, BL-970, BL-971, BL-977, BL-978,
BL-982, BL-983, BL-985, BL-995. This clears the *entire* "still in flight"
list and "Asks" list from the section above — BL-967, BL-970, BL-971, BL-968,
BL-969 (all four Asks-list approvals landed and shipped), plus BL-591, an
older stalled-rebase-incident ticket that had sat open since the 2026-08-19
outage.

**~22 tickets minted** (BL-977 through BL-1003, non-contiguous — several are
epic/child splits). Unlike last night's batch, several are downstream
*regressions introduced by today's own work*, caught same-shift:

- **BL-982/BL-983** (multi-seat coder stages: seat identity split + idle-first
  stage-mailbox delivery) shipped, closed, and stood up the `coder@sonnet2`
  seat for real — but their own step files reintroduced the exact
  `os.tmpdir()` socket-fixture-root violation BL-948 was built to catch
  (independently confirmed by QA and hardener; main's test lane went RED).
  Fix = **BL-1002** (high, no approval gate — the gate itself is not broken,
  the fix is dictated by an already-shipped helper), queued behind BL-1003.
- **BL-997** (busy-marker TS/Babashka agreement) surfaced a real drift while
  in flight; before it reached hardening, the specifier found the drift is
  *worse* than first diagnosed — it runs both directions (false-busy **and**
  false-idle, the latter letting a forced respawn through mid-turn) — and
  minted **BL-1003** (high, priority 10) as the real fix, parking BL-997's
  parcel. Architect and hardener both correctly redirected mid-flight rather
  than hardening the superseded fix.
- **BL-994** (Live Screen role-tile grid — human-directed hotfix, patch
  captured as evidence then formally ticketed and promoted) is in flight at
  `coder@sonnet2`; a late finding says its patch breaks an existing BL-929
  acceptance scenario — routed to the specifier for a spec amendment, not yet
  resolved.

### Staffing change

**Main coder seat (`coder`, "coder1") moved back from `claude-fable-5` to
`claude-opus-5`** (operator directive, this shift). The fable-5 bounce
experiment this file's own history was tracking is now doubly moot: it was
already recorded ABANDONED in `full-forge.conf` as of the second coder seat's
addition (seat identity never escapes the bounce-blame metric, BL-983
invariant 3), and now the seat itself is off fable-5. Do not read forward
coder-blamed bounce rate as fable-5 evidence.

**Second coder seat (`coder@sonnet2`, claude-sonnet-5) is live and working
real tickets** — the "fire up 2nd seat when BL-982+983 land" project trigger
fired today; both are closed. A **new, human-directed difficulty-aware
routing rule (BL-1001, approved)** now governs seat choice going forward:
hard work (`mutation_cost: high`/`medium`) is reserved for `coder`
(claude-opus-5, "coder1"); `coder@sonnet2` gets `low` only, with asymmetric
spill (easy work may spill up to coder1 when coder2 is busy; hard work never
spills down to coder2, however idle). Not yet implemented — BL-1001 is
approved and paused, queued behind BL-1003 and BL-1002.

### Infrastructure incident: shared master checkout divergence (BL-891, recurring)

The stalled-`pull --rebase`-detaches-master-checkout pattern (documented after
recurring three times as of this morning) **recurred at least four more times
today**, each self-resolving with `git rebase --abort` (safe — `orig-head`
always equalled `main`, zero commits lost). One recurrence was a **genuine
merge conflict**, not just a stalled autostash: local `main` diverged to 9
ahead / 14 behind `origin/main`, with independent bookkeeping landed on both
sides (e.g. BL-970 closed locally while also landing through a different
merge chain on origin). Per my own recorded lesson, reconciling `main` with
`origin/main` is QA's integration job, not something to half-do solo in a
shared checkout — I escalated rather than guess. Human ruled: **QA
reconciles**. QA did, cleanly (confirmed additive on both parents, 0 behind
afterward). Separately, QA also found and safely `git stash`-ed 6 uncommitted
domain-code edits sitting live in the master checkout (real BL-981 follow-on
work — a board seat-identity leak fix — landed in the wrong worktree, not
discarded, not yet rescued to `coder`'s worktree).

**This is now the single most recurrent operational failure mode of the
day** — worth a fix to whatever triggers these auto-sync attempts on the
shared checkout, not just another documented safe-recovery.

### Pipeline state right now

Active 7/7 (cap 7, no ambulance, onboarding gate: allow). Queue order by
priority for the next open slot: **BL-1002** (high, pri 15, no approval
gate) then **BL-1001** (approved, pri 25) then the rest of the ~180-item
paused backlog. `main` build-freshness confirmed synced (5/6 long-lived
daemons fresh; `operator_runtime` is a short-lived on-demand process, not a
persistent daemon, so it naturally trails by a commit between runs).

### Still open / not yet resolved

1. **BL-1002 and BL-1001 both queued, no open slot.** BL-1002 is the direct
   fix for main's currently-passing-again-but-recently-RED test lane;
   promotes next regardless of BL-1001.
2. **BL-994's BL-929 acceptance conflict** — spec amendment needed, with
   the coder mid-turn on other verification work in the meantime.
3. **stash@{0} (real BL-981 board-seat-leak fix) has no worktree** — flagged
   twice by the specifier, not yet rescued. Real, reviewed-sound work
   (`stageOfSeat()` helper preventing `coder@sonnet2` from leaking onto the
   pipeline board) sitting outside any branch.
4. **BL-979/BL-980 (pipeline-board rows + epic separators) still paused** —
   the project's depth-uncap trigger (`active_backlog_max_depth: -1` once
   BL-979 reaches `done/`) has not fired yet.
5. **Recurring babysitter false-positives on long verification turns.**
   Investigated 4 separate ">30m in_process" / "batch claim stale" alerts
   this shift; all four were healthy, actively-progressing work (property
   runners, full compiles, multi-stage acceptance verification) confirmed via
   live process/pane evidence, not stalls. Worth tuning the threshold or
   widening claim-progress heartbeat coverage to Monitor-based work (one
   flagged parcel had no `claim-progress.json` at all because the architect
   was tracking it via background `Monitor` tasks instead).

### Asks

1. **Approve BL-1002 and BL-1001** if not already — both are already
   `human_approval: approved` or gate-exempt, so no action needed there;
   listed for visibility since they're first in the promotion queue.
2. **Decide on stash@{0} rescue** — who applies it to `coder`'s worktree and
   under what ticket (it is real, reviewed work with no ticket of its own
   right now).
3. **Consider a fix for the recurring master-checkout rebase-stall** — fourth+
   recurrence today alone, always safely recovered but always consuming a
   turn to diagnose.
4. **Confirm coder1/coder2 tier assignment (BL-1001)** matches intent before
   it builds — the ticket itself flags that its framing reverses BL-983's own
   description of which seat is "the more capable coder."
