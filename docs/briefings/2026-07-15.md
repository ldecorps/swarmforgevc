A quieter-volume day than yesterday's record (**21 tickets closed** vs 34), but a heavier one operationally: the front desk had **two separate live production incidents** today, both diagnosed and shipped within the session — a critical Telegram redelivery loop that had already replayed into 209 junk commits, and a second, distinct spam mechanism this coordinator caught firsthand from the human's own screenshot, root-caused, and fixed within the hour. Alongside that, a four-ticket contract-negotiation-over-Telegram build, a three-ticket briefing-email presentation cleanup (also from direct human feedback), and the test-suite "three poles" speed epic all shipped in full.

## Business features delivered

- **Front-desk Telegram reliability, round three — two live incidents, both closed.** BL-389 was the critical one: a deliberately-dropped Telegram update (a photo, a sticker, a stranger's message) parked the getUpdates offset forever, so Telegram replayed it and everything after it on every poll — 209 messages and ~8 commits/minute pushed to `origin/main` until a human killed the bot. Fixed by making the drop terminal rather than feeding it into the anti-loss retry gate. BL-390 closed the matching blast-radius gap so a future no-op replay can never mint a commit even if a similar bug recurs. Then, hours after BL-389/390 shipped, a **second, distinct** spam mechanism surfaced live: an epic-progress announcement ("N of M ticketed slice(s) complete") had no change-gate at all and re-flooded a topic on every concierge tick and every restart (BL-394) — the human sent a screenshot, this coordinator killed the front desk (supervisor first), traced it to a missing `emittedKeys` entry for epic-progress events specifically (every other event type was already deduped), filed the ticket, got it expedited and approved directly in-session, and verified the fix live (a real progress change, 3/6 → 4/6, announced exactly once) before restarting the channel. Separately, BL-391 stopped sending the human raw ANSI escape codes scraped from an agent's terminal pane, and BL-379 made the front desk filter inbound messages by chat, not just by sender, so it can no longer ingest a chat it merely happens to see.
- **The onboarding contract now negotiates over Telegram, not just files.** A target repo now provisions its own Telegram bot and group during onboarding (BL-380), the contract's back-and-forth proposal/objection/revision loop runs live in that group across as many rounds as the human needs (BL-381), verbosity became a first-class negotiable contract term alongside scope (BL-382), and the front desk's own replies now honor whatever verbosity level was agreed — re-negotiable without a swarm restart (BL-383).
- **Briefing email presentation, fixed from direct feedback.** The human reported the subject line swallowing an entire lede paragraph and the body reading as an unstyled wall of markdown; both are now fixed (bounded, markdown-stripped subject headline — BL-392; the full body rendered to HTML alongside the plain-text fallback, diagrams still inline — BL-393), plus the diagrams themselves now render at double the previous pixel width for crispness on high-DPI screens (BL-402), all while staying on the Gmail-safe cid-PNG pipeline rather than regressing to SVG (which BL-286 had moved away from for exactly that reason).
- **The "three poles" test-suite speed epic is fully shipped.** All three previously-identified single-file wall-clock bottlenecks are split (BL-375's dependency-gate engine test, BL-376's real-timer backoff wait, BL-377's fake-tmux-as-real-process-spawn), and BL-378 adds a standing per-file duration budget guard so a fourth pole would fail the build automatically instead of silently regressing the suite again. Confirmed working: a fresh profile this session found the suite still healthy (273 files, 3897 tests, ~11-12s wall) with every file inside the 7s budget.
- **Role-benchmark credibility, slice 1 of 4.** The leaderboard no longer silently breaks a genuine tie by array order and reports it honestly instead (BL-385), and the battery gained tasks with a real difficulty ceiling so models can actually be told apart rather than all scoring a saturated 6/6 (BL-386). The epic (BL-384) continues: BL-387 (score what survives the pipeline, not the first diff) is active; BL-388, the mandatory wiring slice that makes the leaderboard actually consume that signal, stays paused until BL-387 lands.
- **Topic/governance cleanup.** Epics are now first-class Telegram topics with their own slice-progress posts (BL-341 — the same feature whose progress-announcer gained the change-gate above in BL-394), a topic's icon now tracks its ticket's state automatically (BL-342), a ticket sitting on `human_approval: pending` now gets actively asked about in its own topic instead of only waiting to be found on a pull surface (BL-357), and a topic can be reconstructed from its durable serialised record as a real, tested round trip (BL-332).

## Delivery metrics

- **Velocity: 21 tickets closed** since yesterday's briefing (2026-07-14 09:02 BST) — well below yesterday's 34-ticket record, in line with two incident-response detours eating build time. 213 closed in the trailing 7 days (essentially flat vs the prior week). Backlog now **219 done · 4 active · 3 paused**.
- **Cycle time:** median **7h 26m**, p85 **18h 10m** (most recent 20 closed tickets) — up from yesterday's 6h3m/19h6m; consistent with today's incident work interleaving with normal build throughput rather than a new structural slowdown.
- **Suite duration:** 10s latest, -2s vs prior — healthy, and now protected by BL-378's per-file budget guard against a silent regression.
- **Cost (partial day, digest generated 08:00 local — does not capture the afternoon/evening incident-response and bookkeeping work):** ~**1.53M tokens / ~$300** across the roster — coordinator+specifier $57.74 (still one combined figure, see optimizer note below), coder $63.76, cleaner $69.12 (today's highest single line), hardender $35.07, QA $25.54, architect $29.49, documenter $18.92.
- **Reliability (today so far):** 72 chases, 84 nudges, 0 respawns, 0 dead-letters — both chase and nudge counts down sharply from yesterday's very high build-volume day.

## Agent business

- **Active: 4** (depth cap `-1`, uncapped) — BL-101 (still blocked on a human hardware step), BL-361, BL-368 (both `human_approval: approved`, part of the human's earlier "promote all 9" authorization), and BL-387 (the benchmark oracle, `approved`).
- **Paused: 3** — BL-384 (the benchmark epic's own tracking ticket, not directly promotable), BL-388 (`approved`, but blocked on BL-387 still being active), and BL-395 (`human_approval: pending-review` — strips the remaining *visible* terminal chrome, box-rule lines and the permission-mode footer, from NeedsApproval messages; the human's own follow-up ask after BL-391's invisible-byte fix).
- **Swarm profile:** `full-forge` (all 7 pipeline roles + coordinator), `active_backlog_max_depth -1`, matching `swarmforge.conf`.
- **Governance:** onboarding-contract gate returns `allow`. Only one item sits on `human_approval` short of a decision (BL-395) — a marked improvement over recent days' longer pending lists, helped along by BL-357 shipping today (a pending approval now gets asked about directly instead of sitting silently).

## Optimizer recommendations

- **The coordinator+specifier combined cost figure is still unresolved** — now carried over four briefings running. Today's figure ($57.74) is no longer the top line item (cleaner's $69.12 is), but the coordinator's true individual cost is still unknown and this has gone unaddressed for four days.
- **No capacity change recommended.** The pipeline absorbed two live production incidents today (BL-389's critical redelivery loop and BL-394's epic-progress spam) without a build-throughput stall elsewhere — chase/nudge counts are down, not up, and the suite-speed epic closing plus its new budget guard reduces future risk of exactly the kind of silent regression that caused this week's earlier "three poles" problem.
- **Front-desk incident response worked as designed, twice, and is worth naming as a pattern.** Both BL-389 and BL-394 followed the same shape: kill the supervisor first, root-cause from durable state (not guesswork), file the defect with concrete evidence, get expedited human approval, verify the fix against the exact reproduction before declaring it safe. Consider whether this incident-response shape (documented ad hoc in `.swarmforge/operator/archive/` this week) is worth formalizing as a named runbook rather than being re-derived per incident.

## Process hiccups

- **A stale local checkout nearly caused a false "gap" bookkeeping incident.** A QA note claimed BL-392 had "landed on main," but the commit wasn't reachable from this coordinator's local `main` — turned out to be a simple fast-forward lag against `origin/main` (already pushed by QA), not a real gap. Caught by verifying with `git merge-base --is-ancestor` before acting, per the BL-340 lesson to never trust a claim without checking it against the ref directly.
- **A split-branch parcel got stranded mid-pipeline on BL-381's second QA round, and QA's own bounce-evidence discipline caught it cleanly.** The architect-bounce fix for BL-381 was correctly written on `swarmforge-cleaner` but never forwarded past cleaner, while a separate, earlier line of the same ticket kept moving forward without it all the way to QA. QA diagnosed the exact divergence point with `git merge-base --is-ancestor` on both lines, wrote detailed bounce evidence, and routed it straight back to the coder with clear instructions not to redo the already-correct fix — no coordinator intervention needed; the pipeline's own self-correction worked.
- **Two live incidents in one session, zero swarm downtime beyond the deliberate, scoped front-desk parks.** Both BL-389 and BL-394 required taking the front-desk trio down; in both cases only that group was stopped and restarted (via its own scoped launcher, not a swarm-wide bounce), and the rest of the pipeline kept moving throughout.
