SwarmForge shipped its own reliability fix today: the swarm can now detect and recover from handoff breakage automatically

## Business features delivered

- **Self-healing handoffs (BL-121, BL-122).** The swarm now detects delivery-level breakage (dead-lettered parcels, stalled queues, a failing synthetic canary) and automatically re-delivers or escalates to a human — instead of showing a green dashboard while parcels silently rot, the failure mode that caused a real multi-hour halt earlier this week.
- **Needs-human alerts actually reach a human now (BL-148).** A confirmed stuck role reliably sends one email alert, closing a gap where the notification path existed but never fired.
- **Multi-provider pane detection (BL-142).** Recognizing whether an agent (Claude, Aider, Codex, Copilot, Grok) is busy, running, or waiting to start now reads from a descriptor registry instead of hardcoded brand names — adding a new provider is now a data change, not a code change.
- **Delivery metrics (BL-096).** Velocity, per-milestone burndown, cycle time, and delivery forecasts are now computed directly from git history — the numbers below are the first real output of that.
- **Cleaner coordinator inbox (BL-143)** and **durable chase/nudge telemetry (BL-098)** — operational visibility improvements, both live.
- Plus: swarm-name branch namespacing (BL-106), living Gherkin feature files replacing embedded YAML acceptance blocks (BL-111), GitHub issue auto-close on ticket completion (BL-114), launch-path diagnostics (BL-116), and two dead-branch/path bugs fixed (BL-133, BL-204).

Internal-only work today: real-timer removal across the test suite (BL-131, should meaningfully cut suite runtime) and the mutation-cooldown gate wired into the hardener (BL-149).

## Delivery metrics

- **Velocity:** 15 tickets closed today (2026-07-09) — a notably high-throughput day, driven by filling previously-idle active-backlog slots rather than running solo.
- **Cycle time:** single-ticket pipeline passes (promote → merged) ran 18–46 minutes clean; one bounce (BL-131) took ~89 minutes including a QA-rejected-and-refixed round trip.
- **Burndown / forecasts:** not yet available as computed p50/p85 dates — BL-096 shipped the computation layer today; this is the first briefing that could theoretically carry them, but I'm not running the compiled metrics CLI from this channel yet. Next briefing should carry real numbers here.
- **Suite duration:** expected to drop after today's real-timer removal (BL-131); no measured before/after figure yet.

## Agent business

- **In flight:** BL-094 (holistic web UI, phone-app foundation) and BL-100 (cost/resource telemetry) are both active at the coder, BL-094 queued directly behind BL-100.
- **Paused, gated:** BL-097/BL-117/BL-211 (phone dashboard + charts) wait on BL-094; BL-213 (cost figures in the briefing + on the phone) waits on both BL-097 and BL-100; BL-206/207/208 (remaining multi-provider slices) are explicitly held — operator is running Claude-only for now, no urgency there.
- **No stalls, no dead-letters, no open needs-human gates** as of this briefing.

## Optimizer recommendations

- `active_backlog_max_depth` was lowered 5 → 3 today (operator directive, cost-driven). Active is currently 2/3.
- Documenter effort tier (`medium`) is the safest remaining cost lever not yet pulled — lowest-stakes role, hasn't been touched.
- Real per-role cost/token data (BL-100, in flight) is the responsible next step before cutting any other role's model or effort tier further — guessing at tiers risks paying more in rework than it saves.

— sent as a manual test of the BL-099 send path, not the scheduled daily trigger.
