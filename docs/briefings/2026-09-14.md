Coordinator daily briefing — 2026-09-14

> Composed by the documenter per human ruling A (2026-09-07): the morning
> briefing is documenter-owned agentic work, not coordinator-owned.
> Covers the period since `2026-09-13.md`'s cutoff (commit `9c86c22bdc`).

## The story of this window: the standing-red register drains from 34 rows to 14

The sweep that dominated the last several briefings reached its floor this
window: the register fell from 34 rows / 21 tickets to **14 rows / 7 owning
tickets** (BL-1457, BL-1462, BL-1483, BL-1486, BL-1555, BL-1556, BL-1559) —
the sharpest single-window discharge yet. Sixteen tickets closed, ten new
ones minted (nearly all direct QA unowned-red or hotfix-ledger fallout,
none freshly discovered scope), and `backlog/active/` emptied out entirely
for the first time in recent memory. The overnight shift also produced two
live operational incidents worth flagging below: a second specifier seat
briefly raced the dedicated one over the same mint, and a seat-side
`ready_for_next.sh` hunting bug got its hotfix landed and stamped.

## Defects closed this window

Sixteen tickets closed since the previous briefing:

- **BL-1485** — the BL-1318 acceptance handler now decides the pane-hatch
  override itself instead of relying on the launcher's inherited
  environment, so the fixture window line no longer flips red/green
  depending on whether `PACK_STAFFING_SKIP_GATE=1` happens to be exported.
- **BL-1487** — every deleted swarm profile reference is now either
  restored or retired everywhere it's named, closing the drift the BL-1479
  ruling left open.
- **BL-1489** — the BL-982 single-seat handler now compares only the
  fields it actually names instead of the whole record, surviving later
  additions to the seat shape.
- **BL-1496, BL-1538** — the BL-1028 property-runner fixture (two separate
  reds, integrity-refusal and runner-fixture flavors) now carries its own
  `bb` closure instead of loading a stale one.
- **BL-1499** — the front-desk notify fixture's fake tmux pane now keys its
  state on what was actually typed via `send-keys`, not on how many times
  the pane was read back.
- **BL-1500** — the BL-977 wedge-check fixture now ages its pid file past
  the daemon startup grace window instead of presenting an artificially
  young daemon.
- **BL-1502** — the BL-1089 fixture's source contract now follows the
  liveness fixture instead of a copy that had drifted from it.
- **BL-1503** — the BL-1313 property test now copies the closure it
  actually computes rather than a stand-in.
- **BL-1527** — a whitespace-only ruling is now treated as blank by the
  BL-1367 property oracle instead of being read as a real answer.
- **BL-1528** — an undeliverable closing-ceremony note is now recorded as a
  failed run, not a silently-pending one.
- **BL-1533** — the BL-604 property test now constructs each delta sign
  directly instead of sampling for one, closing the reach-floor flake.
- **BL-1539** — the dispatch-isolation guard now derives every
  self-rooting dispatcher instead of a hand-maintained list.
- **BL-1540** — the two shell tests that dispatch a self-rooting helper now
  go through the real `SCRIPT_DIR/../` path instead of a relative stand-in.
- **BL-1541** — the five `bb` property runners now answer the two-call
  self-audit challenge before asserting the queue, closing the last of the
  BL-1530-shaped standing reds.
- **BL-1553** — the BL-1364 property test now constructs each damage kind
  directly instead of sampling for one (same shape as BL-1533).

## Standing-red register: 14 rows / 7 owning tickets (down from 34/21)

The specifier's stale-row retirement passes (09-13 and again 09-14) removed
every row naming a ticket that had since closed — most recently four `bb`
rows left behind by BL-1541's land, re-pointed or dropped in
`906e915dfb`/`28db2441b0`. What remains is a small, current set: BL-1457
(two property fixtures needing the pane-hatch override decided in-file),
BL-1462 (an acceptance feature reading a stale fixture path), BL-1483 (a
superseded icon-choice scenario row), BL-1486 (four acceptance rows failing
only without the pane hatch), BL-1555 and BL-1556 (three property files
flaking on generator-coverage floors, from the same BL-1485 QA unowned-red
note), and BL-1559 (the freshly-discovered BL-983 two-seat coverage floor,
a fair-coin miss about 10.5% of runs). No row currently names a closed or
absent ticket.

## New this window

Ten tickets minted since the previous briefing, all direct consequences of
QA unowned-red notes or hotfix-ledger stamp sweeps rather than newly found
scope:

- **BL-1553** — retired above.
- **BL-1554** — a QA CLI that gathers the mechanical review checklist in
  one call (gather/report only, never the pass/fail verdict), from the
  operator's own intake question.
- **BL-1555, BL-1556** — the reach-floor owners for the BL-1253/BL-956
  flapping-arbiter and grid-overflow property flakes, plus the BL-1272
  one-`bb`-process-per-draw timeout — all three from the same BL-1485 QA
  unowned-red note (evidence `dfc2a60b23`).
- **BL-1557, BL-1558** — a stamp-off ticket for hotfix `92ecf4aea3`
  (resident rotates to the forward recipient) and a defect for the
  resident-rotation-reason label `respawn-as!` drops, so telemetry never
  reads rotate-home vs. rotate-forward.
- **BL-1559** — the BL-983 two-seat coverage-floor flake, unobservable
  until BL-1541's land let the run reach the check at all; owner
  constructs the draw schedule instead of leaving it to chance.
- **BL-1560, BL-1561, BL-1562** — the stamp-off of hotfix `1fc9065605`
  (seats stop hunting `ready_for_next.sh` at the worktree root), plus its
  two follow-on defects: the script-path heal must never rewrite a data
  token, and every generated wake nudge should name the helper's real path.

## Backlog snapshot

- `backlog/active/`: **0** (BL-1498, the last occupant, closed this window)
- `backlog/paused/`: 109
- `backlog/hold/`: 0
- `backlog/done/`: 1,372 total (+16 since 2026-09-13)

## Operator watch items

- **A second specifier seat briefly raced the dedicated one over the same
  mint (2026-09-14 06:25–06:31Z).** A dedicated `swarmforge-specifier`
  tmux session (pid 539) and the coder pane, rotated to specifier two
  minutes later, both picked up the same in_process architect note and
  both started minting BL-1559 in the shared checkout within six minutes;
  the coder-pane seat noticed only because its register edit reported
  "replaced 0" and stood down. Nothing currently refuses a second seat for
  a role whose dedicated session is already live — on any specifier turn,
  check `pgrep -af specifier.claude-settings` and `tmux list-sessions`
  before minting.
- **A seat "stuck on `ready_for_next.sh`" can be a hunting bug, not a
  hang.** Seen 2026-09-14 06:45Z on the architect seat: `./ready_for_next.sh`
  at the worktree root fails instantly (the helper lives in
  `swarmforge/scripts/`), then the model ran a `find /` sweep that pinned
  the pane for the full 120s Bash cap — 258 bad attempts and 121 `find /`
  hunts across role sessions in the week to 09-14. Hotfix `1fc9065605`
  landed this window (every boot nudge now names the real path, plus a
  `:missing-script-path` heal class); its stamp tickets (BL-1560/1561/1562)
  are minted but still paused. Read the role's own transcript before
  assuming the script itself is hung.
- **`cron` is restored** (human ran `sudo service cron start` 2026-09-13
  07:23:30Z after being dead since 2026-08-30) but the WSL boot-persistence
  fix (`/etc/wsl.conf [boot]`) is still unproven — re-check `pgrep -x cron`
  after any host reboot.
- **Launch can still boot the resident as a second coordinator** when the
  mono-router active-role marker reads `coordinator` — verify any launch by
  process tree, never `./swarm status`; recovery is `rotate_to_role.sh
  coder`. Last confirmed recurrence 2026-09-13.
- **Two Telegram/icon-shaped surfaces exist and are not interchangeable**
  (static backlog PWA vs. the live holistic UI) — confirm which one a new
  request means before speccing.
- Claude-seat capacity has been confirmed restored and producing since
  2026-09-10 06:09:58Z; a `no claude process` escalation is a real signal,
  not fallback noise.
- **Approval asks on Telegram must carry pros/cons per option** (human
  directive, 2026-09-11) — a ruling ask answered without weighed
  alternatives is out of policy; don't approve unasked.
