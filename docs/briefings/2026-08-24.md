Coordinator daily briefing — 2026-08-24

> Written by the coordinator, covering the ~24h since `2026-08-23.md`'s
> cutoff (~05:52 BST 2026-08-23) through ~05:30 BST 2026-08-24. Pack
> moved from `full-forge` into **`cursor-mono-router`** (no specifier seat;
> coordinator + rotating Cursor resident). The overnight stretch finished
> the Cursor/local-model chain's last QA approvals, then spent most of the
> early morning **blocked on a diverged dirty `main`** that prevented
> post-QA bookkeeping — not a pipeline stall.

## Executive summary

**Several Cursor-chain tickets closed cleanly** earlier in the window
(BL-1078, BL-1077+1082 together, BL-1087, BL-1098, BL-1099, plus
BL-1030 clearing from hold). **BL-1108** (Cursor seat readiness stamp-off)
and **BL-1052** (local-model seat path) then ran the full review chain,
took a QA bounce/re-fix on BL-1108 RC-6, and **QA-approved together** on
`origin/main` at `69dd1fdf3c`.

**The shift's dominant open problem is bookkeeping, not coding.** QA's
approval note has sat in the coordinator `in_process` inbox since
~22:21 UTC 2026-08-23 (~6h at briefing time). Constitution step 0
(`git fetch && git merge --ff-only origin/main`) **refuses**: local
`main` is **3 ahead / 30 behind** `origin/main`. The master-main
reconcile daemon is stuck `surfaced: dirty` / `escalated: true`
(~1400+ ticks) over ~45 uncommitted paths (extension/concierge + bridge
WIP, many `D backlog/paused/BL-*`, `?? backlog/debt/`, etc.). Coordinator
policy forbids silent `--no-ff` / rebase and forbids closing over a
diverged tip — so both tickets remain in `active/` with stale
`assigned_to: coder` while stage map correctly shows **coordinator**.

**Babysitter repeatedly misreads this as STARVED / “no parcel” / aged
`in_process`.** Each investigation confirmed the same intentional hold:
QA close authority kept; do **not** re-route to coder; human must
reconcile `main` (or clear the dirty tree so reconcile can). OPERATOR was
escalated on that block throughout the night.

Promotion freeze from yesterday (`control-pause.json`) is **now off**
(`active: false`). Effective depth is still stuck at the two
QA-approved actives until they can close.

## Shipped this window (selected)

| Ticket | What it was |
|---|---|
| BL-1030 | forbidden-stop-flag guard (cleared from hold → done) |
| BL-1078 | Cursor agent token accepted by the launcher |
| BL-1077 + BL-1082 | documented qwen credential name + pull/serve named model on host (closed together on QA `2dd9d6cf24`) |
| BL-1087 | supersede/cleanup slice on the local-model chain |
| BL-1098 | silent-revert defect (QA `6c04a7d3bd`) |
| BL-1099 | retire superseded give-up-cooldown scenario |

**Approved on origin, not yet bookkept (blocked):**

| Ticket | Status |
|---|---|
| BL-1108 | QA pass `69dd1fdf3c` — Cursor seat readiness stamp-off (RC-6 re-fix) |
| BL-1052 | QA pass `69dd1fdf3c` — staff a seat with a downloaded local model |

## What blocked bookkeeping (human action required)

1. **Local tip** includes coordinator-only commits not on origin
   (deferral note, Q-jump, plus today's cost/health sidecar
   `5c5403921`).
2. **Origin tip** is the QA pass `69dd1fdf3` and ~29 other commits the
   local checkout never absorbed.
3. **Dirty worktree (~45 paths)** keeps the reconcile daemon from
   auto-healing the divergence.
4. Coordinator correctly **stops loudly** on refused `--ff-only` rather
   than merging parcels or closing over a lagging tip.

Until `ff-only` succeeds (or an explicit human reconcile lands), the
coordinator cannot: close BL-1108/1052, run a trustworthy
`build_freshness_cli sync` for that tip, or promote into the freed
slots.

## Pipeline state right now

- **Active 2:** BL-1108, BL-1052 — both QA-approved; stage = coordinator
  bookkeeping; `assigned_to: coder` is stale and must not be re-routed.
- **Coordinator `in_process`:** QA note
  `00_20260823T222143Z_000975` — “BL-1108+BL-1052 approved 69dd1fdf3c;
  bookkeep both done” (held on purpose).
- **Hold (selected):** BL-1031, BL-1071, BL-1079, BL-1081, BL-1097,
  BL-1100 still under `backlog/hold/` from the Cursor-chain focus /
  structured-promotion work.
- **Pack:** `cursor-mono-router` (no specifier — cold intake undrained
  until a specifier-capable pack returns).
- **`main` vs `origin/main`:** 3 ahead / 30 behind; reconcile
  dirty-escalated.
- **Cost sidecar** for today already on local tip
  (`docs/briefings/2026-08-24.json`); average delivered cost/ticket
  continues down (~$6.31 on the recent horizon vs ~$10 prior).

## Flagged, not yet resolved

1. **Diverged dirty `main` blocking QA closes** — the shift's single
   blocking human ask (see Asks).
2. **Babysitter STARVED / drop false positives** while coordinator holds
   a legitimate blocked close — noisy but correctly ignored; a structural
   fix would recognize “stage=coordinator + QA approval in_process +
   ff-only refused” as healthy wait, not lost work.
3. **BL-1100** (structured promotion candidacy) still in hold — yesterday's
   “do not promote” prose-grep false positives remain a landmine once
   promotion resumes at volume.
4. **Foreign dirty surface** (extension concierge/bridge/front-desk and
   related tests) still uncommitted — overlaps BL-1090's earlier
   dirty-surface story and keeps reconcile/freshness unhappy.
5. **Stale `role_ask`** on coordinator (old specifier-pane question) still
   pending — blocks a fresh `role_ask` (`already-pending`).

## Asks

1. **Reconcile local `main` ↔ `origin/main`** (and/or clear/commit the
   dirty tree so the reconcile daemon can). Then wake the coordinator so
   BL-1108 + BL-1052 can close, freshness can sync to the approved tip,
   and promotion can refill under the normal depth cap.
2. **Do not** tell the coordinator to re-route those two tickets to
   coder, abandon the QA handoff, or `merge --no-ff` silently — that
   would rewind or launder approved work.
3. Once unblocked: retire or answer the stale coordinator `role_ask`, and
   decide whether the foreign extension WIP rides with BL-1090 or gets a
   deliberate commit of its own.
4. When ready for cold intake again, switch off `cursor-mono-router` (or
   staff a specifier-capable pack) — this pack correctly leaves intakes
   undrained.
