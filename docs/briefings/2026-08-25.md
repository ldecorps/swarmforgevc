Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~19:03 BST 2026-08-25.
> Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**You can staff a swarm seat with a downloaded local model**, and the
intelligence layer can route work to that seat instead of only cloud
providers (BL-1052 + BL-1053).

**Cursor seat readiness is certified** for the current hotfix line
(BL-1108; later BL-1113 for main-sync deadlock, cursor-forge, board UX,
CreatePlan confirm).

**An ACP-host-in-a-pane can drive one seat from structured stop reasons**
(BL-1081). **Claim-without-progress auto-heal is wired** (BL-612).
**Mistral Vibe routes through the intelligence layer**, and Model Steward
Slice 2 `evaluate` ingests bakeoff artifacts (BL-682 + BL-556).

**Approvals gain Ambulance as a fifth choice** (BL-893).
**needsHumanDetection pane-title chrome** matches multi-word / @-seat
names (BL-732). **Live absorb rematch clears refuse-rematch to
behind=0** for the BL-1138 residual (BL-1141).

Also closed earlier: pause visible from every checkout (BL-1106);
auto-route coherence (BL-1094); stranded QA-landed close path (BL-1104);
BL-370 liveness fixture honesty (BL-1089); active-cap raise on host
headroom (BL-1128); stamp/board closes BL-1123, BL-1120, BL-1115.

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (flat/tiny up vs ~$8.13).
- **Unit suite duration:** ~285 min today vs ~245 yesterday — up; watch.
- **Reliability:** chases spiked hard through the day (main-sync stalls +
  post-rematch drop cascade); nudges down. Host load not severe.

## Agent / pipeline business

- **Pack:** `cursor-forge` since ~09:28 BST (Token Plan quota exhausted
  on qwen until 2026-08-31 23:03 UTC).
- **Evening shift tonight only** (2026-08-25 marker).
- **`main` sync:** `proceed` / behind=0 at briefing time.
- **Active flood cleared:** human pause commit `23d001e4b` (“Pause
  non-ollama / lower-urgency actives — keep depth at 5”) brought
  `backlog/active/` from ~77 down to **5**. Promote gate can open again.
- **Active now (5):** BL-1126, BL-1127, BL-1131, BL-1138, BL-1140.
- **Just closed:** BL-1139 (master-checkout drift auto-repair) — human
  approved; moved to done (`467b9d6bf`).
- **BL-1140** still `human_approval: pending` — coder blocked until
  human approve/reject.
- **Hold:** BL-1079.
- **Coordinator inbox:** idle (`NO_TASK`); drop notes drained without
  mass `--force` re-route.

## Optimizer recommendations

1. **Approve or reject BL-1140** so the steward bake-off seat can build.
2. **Keep the depth-5 pause discipline** — do not re-flood active from
   paused without triage.
3. **BL-631 absorb/stamp-off** under QA for non-ancestor SHAs on `main`.
4. **Answer standing `role_ask`s:** BL-658 edge-6; specifier legacy-topic
   intake probe.
5. Within powers today: closed when `proceed`; held on wait-reconcile;
   drained drops; did not mass-demote (human did the pause).

## Process hiccups (and recovery)

1. **Diverged/dirty `main` bookkeeping** — rematch cleared repeatedly;
   closes resumed between stalls.
2. **Token Plan quota / pack switch** to cursor-forge.
3. **Active≈77 flood after rematch** — coordinator refused mass-demote;
   human pause restored depth 5 (this briefing’s main recovery).
4. **Promote re-opened closed tickets** from paused dups — scrubbed when
   seen.
5. **Briefing tip churn** — multiple re-files after rematch/dirt-clear.
6. **False wakes / drop-note cascade** — drained, no mass re-route.
7. **Standing BL-631 Article 4.2** — no rewrite/stamp from coordinator.

## Asks

1. **Approve or reject BL-1140** (`human_approval: pending`).
2. **BL-631 absorb or stamp-off**.
3. **Answer BL-658 + specifier intake `role_ask`s**.
4. Confirm whether **evening shift** extends past tonight.
