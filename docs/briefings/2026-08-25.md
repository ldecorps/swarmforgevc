Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~19:46 BST 2026-08-25.
> Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**You can staff a swarm seat with a downloaded local model**, and the
intelligence layer can route work to that seat instead of only cloud
providers (BL-1052 + BL-1053). Steward bake-off path closed as
QA-passed (BL-1140 — later re-closed when stranded in active).

**Cursor seat readiness certified** (BL-1108; BL-1113). **ACP structured
stop control** (BL-1081). **Claim-without-progress auto-heal** (BL-612).
**Mistral routing** + Model Steward `evaluate` ingest (BL-682 + BL-556).

**Approvals: Ambulance fifth choice** (BL-893). **needsHumanDetection
multi-word chrome** (BL-732). **Live absorb rematch** (BL-1141).
**Master-checkout drift auto-repair** (BL-1139, human-approved).

Also: pause visibility (BL-1106); auto-route coherence (BL-1094);
stranded QA-land close path (BL-1104); BL-370 liveness fixtures
(BL-1089); active-cap raise (BL-1128).

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (flat/tiny up).
- **Suite duration:** elevated vs yesterday — watch.
- **Host load:** not severe (~0.20). Chase spike earlier from sync
  stalls + drop cascade.

## Agent / pipeline business

- **Pack:** `cursor-forge` (qwen Token Plan exhausted until 2026-08-31
  23:03 UTC). Evening shift tonight only.
- **`main` sync:** `proceed` (tip matched origin).
- **Starvation cleared (evening):** QA-passed tickets stranded in
  `active/` were closed (BL-1126/1127/1131/1138/1140/1070/1091/1107/
  1130/1132/1097, …). Do **not** re-open those IDs.
- **JumpQ Ollama remaining — rematch regression:** Specifier had minted
  **BL-1142** + **BL-1143** and coordinator had promoted both into
  active (by-name after a wrong auto-promote). A later rematch onto
  origin **rewound tip-ahead commits**: JumpQ actives are gone;
  **BL-1142** is back in `paused/`; **BL-1143** yaml/features are **not
  on tip** (mint commit not ancestor of current HEAD). Prioritize
  intake / STEERING preference still stands.
- **New intake (on tip, not minted):** Host queue "enqueue next" pin +
  hold-when-host-asks (`INTAKE-host-queue-enqueue-next-hold-on-host-question.md`).
  Specifier drain when free — behind JumpQ restore.
- **Active now (5):** BL-828, BL-987, BL-988, BL-989, BL-999
  (pipeline-note / Bubble refill — not JumpQ).
- **Coordinator inbox:** idle (`NO_TASK`) between due/wakes.

## Optimizer recommendations

1. **Restore JumpQ ASAP:** re-land BL-1143 mint onto tip (rematch/
   cherry or specifier re-mint), then demote two pipeline-note actives
   and promote **BL-1142 + BL-1143** by-name. Do not leave Bubble/
   pipeline-note work occupying the cap ahead of Ollama remaining.
2. **Do not re-promote the evening starvation-closed set.**
3. **BL-631** absorb/stamp-off still outstanding.
4. Answer standing **role_ask**s when ready (BL-658; specifier probe).

## Process hiccups (and recovery)

1. Main-sync wait-reconcile through afternoon — rematch; closes between.
2. Token Plan / pack switch to cursor-forge.
3. Active flood → pause → QA-stranded starvation → human closes + refill.
4. False-trail force-routes on refill (BL-999).
5. JumpQ promote then **rematch dropped tip-ahead JumpQ commits** —
   active reverted to non-JumpQ set; BL-1143 off tip.
6. Briefing due cascade; tip now also carries Host enqueue-next intake
   while JumpQ restore remains the top restore ask.

## Asks

1. Confirm absorb of BL-1143 mint (or re-mint) so cold-swap slice exists
   on tip; topics BL-1142/1143 JSON still `??` here.
2. Authorize coordinator to re-demote BL-987/988 (or similar) and
   re-promote JumpQ by-name once BL-1143 is on tip.
3. Specifier: drain Host enqueue-next intake after JumpQ restore.
4. Evening-shift marker tonight only — extend?
5. BL-631 + role_asks when ready.
6. After BL-1143 greens: cold-swap off cursor-forge onto
   `./start-swarm-ollama-qwen.sh` when ready.
