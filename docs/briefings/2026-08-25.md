Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~18:46 BST 2026-08-25 (third
> archive pass today — rematch/dirt clears keep dropping or rewriting the
> tip copy). Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**You can staff a swarm seat with a downloaded local model**, and the
intelligence layer can route work to that seat instead of only cloud
providers (BL-1052 + BL-1053). That closes the hole where a model served
on this machine had no agent shape / pack and would still launch through
a cloud binary.

**Cursor seat readiness is certified for the current hotfix line**
(BL-1108 stamp-off; later BL-1113 for main-sync deadlock, cursor-forge,
board UX, CreatePlan confirm).

**An ACP-host-in-a-pane can drive one seat from structured stop reasons**
(BL-1081). **Claim-without-progress auto-heal is wired** (BL-612).
**Mistral Vibe routes through the intelligence layer**, and Model Steward
Slice 2 `evaluate` ingests bakeoff artifacts (BL-682 + BL-556).

**Approvals gain Ambulance as a fifth choice** (BL-893) — engage hold for
that ticket from the approvals UI.

**needsHumanDetection pane-title chrome** now matches multi-word and
@-seat role names (BL-732).

**Live absorb rematch clears refuse-rematch to behind=0** for the BL-1138
residual path (BL-1141).

Also closed earlier today (product-adjacent): pause visible from every
checkout (BL-1106); auto-route coherence (BL-1094); stranded QA-landed
close path (BL-1104); BL-370 liveness fixture honesty (BL-1089);
active-cap raise on host headroom (BL-1128); plus stamp/board closes
BL-1123, BL-1120, BL-1115 (and a larger afternoon drain that rematch
later re-opened into `active/` — see Process hiccups).

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (flat/tiny up vs ~$8.13).
- **Unit suite duration:** ~285 min today vs ~245 yesterday / ~267 prior
  — up; watch.
- **Reliability:** chases ~1737 (up hard, Δ≈+859); nudges ~155 (down).
  Chase load is main-sync stalls + post-rematch drop-note cascade, not
  coding throughput.
- **Host load:** not severe (ratio ~0.20).

## Agent / pipeline business

- **Pack:** `cursor-forge` since ~09:28 BST (Token Plan quota exhausted
  on qwen until 2026-08-31 23:03 UTC). Standing rotation, depth 3.
- **Evening shift tonight only** (2026-08-25 marker) — bedtime skipped;
  tomorrow returns to day-only unless extended.
- **`main` sync now:** `proceed` / behind=0 at briefing time (after a
  day of intermittent `wait-reconcile` / `wait-dirty-clear`).
- **Coordinator inbox:** drained to empty (`ready_for_next` → `NO_TASK`)
  after closing the evening QA pile and acknowledging drop notes without
  mass `--force` re-route.
- **Active flood still ~77** (cap ~5). Promote refused. Rematch restored
  a historical active set; human triage still required before any
  depth-cap change is meaningful.
- **BL-1140** (steward local-model bake-off): back in `active/` with
  `human_approval: pending` — coder blocked; needs human approve/reject.
  Earlier QA close did not survive rematch.
- **Hold:** BL-1079 also present under `backlog/hold/` while an active
  copy exists — inventory mess from rematch (do not mass-delete).

## Optimizer recommendations

1. **Triage `backlog/active/` (~77)** — which stay active vs done/paused
   — before treating BL-1128's cap raise as license to grow further.
2. **Approve or reject BL-1140** so the coder seat is not stuck on a
   pending gate with a live active YAML.
3. **BL-631 absorb/stamp-off** under QA for non-ancestor SHAs on `main`
   (standing renudges all day).
4. **Answer standing `role_ask`s:** BL-658 edge-6 (stop-time source);
   specifier intake probe on legacy topic adoption.
5. Within powers today: closed when `proceed`; held on wait-reconcile;
   drained drop notes; scrubbed false re-promotes of already-done ids
   when promote pulled paused dups back into active.

## Process hiccups (and recovery)

1. **Diverged/dirty `main` bookkeeping** (from yesterday) — human rematch
   cleared mid-afternoon; closes resumed; divergence returned repeatedly
   through the evening.
2. **Token Plan quota / pack switch** to cursor-forge.
3. **Active flood + “no parcel” cascade** — dozens of drop notes drained
   without mass re-route.
4. **Promote re-opened closed tickets** from paused duplicates (BL-759,
   BL-1080, …) — scrubbed when detected; root cause is rematch leaving
   active+paused copies.
5. **Briefing tip drops** — this is the third re-file of today's archive
   after rematch/dirt-clear wiped prior commits.
6. **False mail wakes / false resume** — specifier false-wake notes
   acknowledged; no invented parcels.
7. **BL-579 false re-open** after force-route on a false trail —
   specifier corrected; left in paused/done path.
8. **Standing BL-631 Article 4.2** — coordinator will not rewrite `main`
   or mint stamps.

## Asks

1. **Triage active≈77** (and hold/active dups like BL-1079).
2. **Human-approve or reject BL-1140**.
3. **BL-631 absorb or stamp-off**.
4. **Answer BL-658 + specifier intake `role_ask`s**.
5. Confirm whether **evening shift** extends past tonight.
