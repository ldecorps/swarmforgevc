Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~21:18 BST 2026-08-25 (due
> re-trigger). Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**Local-model swarm seats** (BL-1052 + BL-1053); steward bake-off
(BL-1140). **Cursor seat readiness** (BL-1108; BL-1113). **ACP stop
control** (BL-1081). **Claim auto-heal** (BL-612). **Mistral + Steward
evaluate** (BL-682 + BL-556).

**Approvals Ambulance** (BL-893). **needsHumanDetection chrome**
(BL-732). **Live absorb rematch** (BL-1141). **Master-checkout drift
repair** (BL-1139). **Collapsed-bubble gesture** (BL-828).

**Tonight bookkept closes:** BL-989, BL-999, BL-1124, BL-1100, and
**BL-988** (QA `86ded02c52` — BL-578 contract binding).

**QA-landed on origin, bookkeep blocked:** **BL-1142** (`25c4c1655e`)
and **BL-1143** (`95b63b9a14`) — JumpQ mono-router decision + cold-swap
verify. Active→done moves waiting on master absorb.

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (slightly up vs prior).
- **Suite duration:** ~285 min today (up vs ~245 yesterday) — watch.
- **Friction:** rematch vs tip-ahead bookkeep; BL-988 bounce cascade;
  JumpQ documenter→QA drop (salvaged); **BL-891 wait-dirty-clear**
  holding closes (ticks≥34, escalated).

## Agent / pipeline business

- **Pack:** `cursor-forge` (qwen Token Plan exhausted until 2026-08-31
  23:03 UTC). Evening shift tonight only.
- **`main` sync:** **`wait-dirty-clear`** — ahead=2 (briefings),
  behind=4 (both JumpQ QA lands + rematch docs). Dirty tracked:
  JumpQ active YAMLs + `hotfix-ledger.yaml`. Untracked intakes/topics
  also present. **No self-merge.**
- **Starvation-closed set:** do **not** re-open.
- **Active (2):** BL-1142 + BL-1143 still in `active/` only because
  close is gated; four QA close parcels held in coordinator
  `in_process`. Live day-shift **`--execute` still human/ops**.
- **Intake (unminted):** Host enqueue-next;
  `INTAKE-frequent-qa-push-races-on-main-land.md`;
  `INTAKE-open-slot-nudge-escalates-on-epic-trackers.md`.
- **Coordinator:** not idle — closes parked on absorb gate.

## Optimizer recommendations

1. **Human/ops:** clear dirty tracked files (or stash), then let
   handoffd absorb so `behind=0`; coordinator will close+promote.
2. Push or drop tip-ahead briefing before rematch eats it.
3. **Human:** live BL-1143 `--execute` when ready (code landed).
4. Specifier: drain Host enqueue-next + QA push-race intakes.
5. Do not promote BL-545 / starvation-closed IDs; BL-631 + role_asks
   when ready.

## Process hiccups (and recovery)

1. JumpQ rematch drops until origin push.
2. Close commits rematched off until retries / absorb.
3. Wrong-SHA BL-999 close note refused; real tip `b55a10823a`.
4. BL-988 bounce7–11 vs concurrent tip races — finally QA-passed.
5. **JumpQ drop:** documenter completed hardener tips with no forward
   to QA; `redo_from documenter` ~20:02Z — both then QA-passed.
6. **BL-891:** tip-ahead briefings + dirty JumpQ YAML/`hotfix-ledger`
   block master absorb; closes held ~30+ escalated ticks — needs human
   dirty clear.
7. Token Plan → cursor-forge earlier today.

## Asks

1. **Clear dirty + absorb** so BL-1142/1143 can close (highest).
2. **Live BL-1143 `--execute`** for day-shift cold-swap.
3. Specifier: mint/drain intakes above.
4. Evening-shift marker tonight only — extend?
5. BL-631 + role_asks when ready.
