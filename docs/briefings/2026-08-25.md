Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~19:29 BST 2026-08-25.
> Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**You can staff a swarm seat with a downloaded local model**, and the
intelligence layer can route work to that seat instead of only cloud
providers (BL-1052 + BL-1053).

**Cursor seat readiness is certified** (BL-1108; BL-1113 hotfix stamp-off).

**ACP-host-in-a-pane structured stop control** (BL-1081).
**Claim-without-progress auto-heal** (BL-612). **Mistral Vibe routing** +
Model Steward Slice 2 `evaluate` ingest (BL-682 + BL-556).

**Approvals: Ambulance as a fifth choice** (BL-893).
**needsHumanDetection multi-word / @-seat chrome** (BL-732).
**Live absorb rematch clears refuse-rematch** (BL-1141).
**Master-checkout drift auto-repair** closed after human approval
(BL-1139).

Also earlier: pause visible everywhere (BL-1106); auto-route coherence
(BL-1094); stranded QA-landed close path (BL-1104); BL-370 liveness
fixture honesty (BL-1089); active-cap raise on host headroom (BL-1128).

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (flat/tiny up).
- **Unit suite:** elevated vs yesterday (~285 vs ~245 min) — watch.
- **Reliability:** chase spike earlier from main-sync stalls + drop
  cascade; host load not severe (~0.20).

## Agent / pipeline business

- **Pack:** `cursor-forge` (Token Plan quota exhausted on qwen until
  2026-08-31 23:03 UTC). Evening shift tonight only.
- **`main` sync:** `proceed` / behind=0.
- **Starvation root cause fixed (evening):** five+ QA-passed tickets had
  been stranded in `active/`, filling the depth cap so nothing new could
  promote. Human closed them (incl. BL-1126, BL-1127, BL-1131, BL-1138,
  BL-1140, BL-1070, BL-1091, BL-1107, BL-1130, BL-1132, BL-1097, …).
  Coordinator will **not** re-open those IDs.
- **Earlier flood:** rematch had ballooned active≈77; human pause
  `23d001e4b` restored depth 5, then stranded QA-passed set re-starved
  the cap until the evening closes.
- **Active now (5, refilled):** BL-828, BL-987, BL-988, BL-989, BL-999 —
  routed (BL-999 force-routed on false trail).
- **Coordinator:** normal loop resumed (`ready_for_next` / promote);
  inbox idle at briefing time.

## Optimizer recommendations

1. **Keep closing QA-passed actives promptly** — stranding them at the
   depth cap is the starvation mode we just exited.
2. **Do not re-promote the evening closed starvation set.**
3. **BL-631 absorb/stamp-off** still outstanding for non-ancestor SHAs.
4. **Answer standing `role_ask`s** (BL-658 edge-6; specifier intake
   probe) when convenient.
5. Within powers: promote+route on open slots; force-route only on false
   trails; skip awaiting-approval (BL-545).

## Process hiccups (and recovery)

1. **Main-sync wait-reconcile / dirty** through the afternoon — rematch
   cleared; closes batched between stalls.
2. **Token Plan / pack switch** to cursor-forge.
3. **Active flood then pause then QA-stranded starvation** — recovered
   by human closes + promote refill.
4. **False-trail refuse on promote** (BL-1097/BL-999) — `--force` for
   deliberate re-route of unfinished work.
5. **Drop-note cascade** — drained without mass re-route.
6. **Briefing due spam** — archive refreshed multiple times; substance
   only when state moved.

## Asks

1. Confirm evening-shift marker for tonight only (or extend).
2. **BL-631** absorb or stamp-off when ready.
3. Answer pending **role_ask**s when ready.
