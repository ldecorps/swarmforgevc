Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~20:21 BST 2026-08-25.
> Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**You can staff a swarm seat with a downloaded local model**, and the
intelligence layer can route work to that seat instead of only cloud
providers (BL-1052 + BL-1053). Steward bake-off path closed as
QA-passed (BL-1140 — later re-closed when stranded in active).

**Cursor seat readiness certified** (BL-1108; BL-1113). **ACP structured
stop control** (BL-1081). **Claim-without-progress auto-heal** (BL-612).
**Mistral routing** + Model Steward `evaluate` ingest (BL-682 + BL-556).

**Approvals: Ambulance fifth choice** (BL-893). **needsHumanDetection
multi-word chrome** (BL-732). **Live absorb rematch** (BL-1141).
**Master-checkout drift auto-repair** (BL-1139, human-approved).
**Collapsed-bubble gesture model** (BL-828, QA `1bbc8bba8a` — bookkept
tonight).

Also: pause visibility (BL-1106); auto-route coherence (BL-1094);
stranded QA-land close path (BL-1104); BL-370 liveness fixtures
(BL-1089); active-cap raise (BL-1128).

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (flat/tiny up).
- **Suite duration:** elevated vs yesterday — watch.
- **Host load:** not severe (~0.20). Flow-stall WARN **2× slack** on tip.

## Agent / pipeline business

- **Pack:** `cursor-forge` (qwen Token Plan exhausted until 2026-08-31
  23:03 UTC). Evening shift tonight only.
- **`main` sync:** `proceed`.
- **Starvation-closed set:** do **not** re-open
  (BL-1126/1127/1131/1138/1140/1070/1091/1107/1130/1132/1097, …).
- **JumpQ Ollama remaining — on origin + flowing:** **BL-1142** +
  **BL-1143** (hardender) active after specifier origin push of rematch
  restores. Coder verify done; **live `--execute` still human/ops**.
  Tip-ahead JumpQ commits must land on origin before rematch.
- **BL-988:** QA bounce3 (tip purity) → documenter; coder rematch4 tip
  `d8652b5111` claimed tip-pure — forwarded to documenter; stays active
  until QA on `main`.
- **Also bouncing:** BL-1100 / BL-999 / BL-1124 QA bounce traffic on tip.
- **Tonight close:** BL-828 → `done/M8`.
- **Active now (5):** BL-1142, BL-1143, BL-988, BL-1100, BL-999.
- **Coordinator inbox:** idle (`NO_TASK`) between due/wakes.

## Optimizer recommendations

1. Keep JumpQ flowing; push tip-ahead commits before rematch.
2. **Human:** authorized live cold-swap `--execute` when ready.
3. Do not re-promote the evening starvation-closed set.
4. BL-631 + standing role_asks when ready.

## Process hiccups (and recovery)

1. Rematch ate tip-ahead JumpQ until origin push.
2. Token Plan / pack switch to cursor-forge.
3. Active flood → starvation → human clears stranded QA-passed.
4. BL-988 bounce3/rematch4: tip ancestry races vs JumpQ restore on origin.
5. Pack `route_backlog_to_coder --force` targets coder — bounce used
   mailbox + `assigned_to: documenter`.

## Asks

1. **Live BL-1143 `--execute`** when you want day-shift cut over.
2. Land JumpQ/bookkeep commits on origin promptly.
3. Topic JSON BL-1142/1143 if still `??`.
4. Specifier: Host enqueue-next intake after JumpQ moves.
5. Evening-shift marker tonight only — extend?
6. BL-631 + role_asks when ready.
