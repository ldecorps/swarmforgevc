Coordinator daily briefing — 2026-08-25

> Written by the coordinator, covering the ~24h since `2026-08-24.md`'s
> cutoff (~05:30 BST 2026-08-24) through ~18:08 BST 2026-08-25 (refresh /
> re-file — prior local briefing commit was dropped from tip by rematch /
> dirt clear). Cost & health sidecar: `docs/briefings/2026-08-25.json`.

## Business features delivered

**You can staff a swarm seat with a downloaded local model**, and the
intelligence layer can route work to that seat instead of only cloud
providers (BL-1052 + BL-1053). That closes the hole where a model served
on this machine had no agent shape / pack and would still launch through
a cloud binary.

**Cursor seat readiness is certified for the current hotfix line**
(BL-1108 stamp-off; later BL-1113 for main-sync deadlock, cursor-forge,
board UX, CreatePlan confirm). The Cursor/local-model chain that was
stuck in yesterday's bookkeeping hold is closed on `done/`.

**An ACP-host-in-a-pane can drive one seat from structured stop reasons**
(BL-1081) — idle and permission blocks are no longer inferred only from
lossy pane text for Cursor/ACP-native seats.

**Claim-without-progress auto-heal is wired** (BL-612): BL-528's
acceptance step handlers land so a stuck claim can heal without a human
nudge.

**Mistral Vibe can be routed by the intelligence layer**, and Model
Steward Slice 2 `evaluate` ingests recruiter/bakeoff artifacts into the
capability registry (BL-682 + BL-556).

Also closed in product-adjacent plumbing: pause state is visible from
every checkout (BL-1106); auto-route no longer cites HEAD in a way that
trips the coherence gate (BL-1094); stranded QA-landed tickets get a
close path (BL-1104); BL-370 liveness fixtures stop lying about
predecessor heartbeats (BL-1089); **active-cap raise on host headroom**
landed and bookkept (BL-1128); plus afternoon QA closes BL-1123, BL-1120,
BL-1115.

Internal-only closes (fixtures, stamp-offs, rematch bookkeeping) are
covered above where they touch product; remainder is pipeline hygiene.

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.20 (horizon n=31), essentially flat
  vs prior ~$8.13 (tiny up).
- **Unit suite duration:** ~285 min today vs ~245 yesterday / ~267 the
  day before — up; worth watching.
- **Reliability:** chases ~1729 (up hard, Δ≈+851); nudges ~155 (down).
  Chase spike tracks main-sync stalls, false wakes, and post-rematch
  “no parcel” cascade — not healthy coding throughput.
- **Host load:** not severe (ratio ~0.25).

## Agent / pipeline business

- **Pack:** switched mid-day from `qwen-mono-router` → **`cursor-forge`**
  after Token Plan 1-week quota exhaustion (reset 2026-08-31 23:03 UTC).
  Standing rotation, depth 3, cursor/auto.
- **Evening shift tonight only** (2026-08-25): day-only bedtime skipped;
  night-start keeps the swarm up. Tomorrow returns to 17:00 bedtime
  unless the marker is extended.
- **Bookkept closes this window (selected):** BL-1108+1052, BL-1053,
  BL-1089, BL-556, BL-682, BL-1113, BL-1094+1106, BL-1104, BL-1081,
  BL-612, BL-1128, BL-1123, BL-1120, BL-1115.
- **Coordinator `in_process`:** BL-1079 approved `b907912c0b` — close
  blocked on `wait-reconcile` / `refuse-rematch` (currently
  ahead≈11 / behind≈3). YAML also still present under `backlog/hold/`.
- **~60 coordinator `new/` notes** still queued (many QA approvals plus
  absorb-refuse / drop noise) — same gate.
- **Active flood:** rematch restored a large historical active set;
  count still **~78** (cap ~5). Promote refused. No mass-demote without
  human triage.
- **Hold:** BL-1079. **Paused:** ~146.

## Optimizer recommendations

1. **Unblock main reconcile** so BL-1079 and the queued QA approvals can
   close (do not ask for `--ff-only` / self-merge while `ahead>0`).
2. **Triage `backlog/active/` (~78)** before any depth-cap change —
   raising the cap on a polluted folder only hides inventory debt
   (BL-1128 already closed the raise feature; do not treat that as
   license to ignore the flood).
3. **BL-631 absorb or stamp-off** under QA for non-ancestor SHAs on
   `main`.
4. Within powers today: closed when `proceed`; held on `wait-reconcile`;
   drained false “no parcel” notes without mass `--force` re-route.

## Process hiccups (and recovery)

1. **Diverged / dirty `main` bookkeeping block (from yesterday)** —
   cleared mid-afternoon by human rematch; coordinator closed the held
   Cursor/local-model pile.
2. **Token Plan quota exhausted on qwen** — recovered by switching to
   `cursor-forge`.
3. **`core.bare=true` briefly broke worktree/git** — cleared; temporary
   `GIT_DIR`/`GIT_WORK_TREE` workaround while bare.
4. **False mail wakes / false in-process resume** — specifier asked to
   stop false resume nudges; notes filed.
5. **BL-631 Article 4.2** — many SHAs still fail `is_qa_ancestor.sh`;
   coordinator will not rewrite/revert `main` or mint stamps.
6. **Active flood + drop-parcel cascade** after rematch — drained notes
   without mass re-route.
7. **Premature complete then restore on BL-1128** earlier; later closed
   cleanly when `proceed` returned.
8. **Briefing file dropped from tip** after rematch/dirt-clear of a
   generic placeholder briefing — this file re-establishes the real
   business-first archive for the day.
9. **Standing `role_ask`s:** coordinator BL-658 edge-6 (swarm stop-time
   source); specifier intake probe “legacy topic adoption”.

## Asks

1. **Clear `wait-reconcile` / rematch** so BL-1079 and queued QA
   approvals can close.
2. **Triage `backlog/active/` (~78)** — which stay active vs done/paused.
3. **BL-631 absorb or stamp-off** under QA.
4. **Answer BL-658 `role_ask`** and the specifier intake probe ask.
5. Confirm whether **evening shift** extends past tonight.
