Coordinator daily briefing — 2026-08-01

> Written by the coordinator, covering the shift since `2026-07-31.md`
> (roughly 2026-07-31 16:00 through 2026-08-01 03:00 Europe/London).

## Executive Summary

Two tickets shipped (**BL-727**, **BL-716**), both hitting the *same* fork-and-
race pattern the pipeline is currently exposed to: a mid-review amendment lands
while an earlier commit is still in flight, producing two live parcel chains
under the same ticket. BL-727's fork reached QA before anyone noticed and had
to be cleared by hand; BL-716's fork was caught and self-deduped by the cleaner
before it went anywhere. The fix for the class (**BL-760**) is specced,
approved, and paused, waiting only on a promotion slot.

Separately, a large (83-file) uncommitted diff on `main` — real out-of-band
Cursor IDE work, not slop — was reviewed end-to-end by the specifier per your
direct instruction, safety-snapshotted, and split into routine bookkeeping
(committed) plus three retro-tickets (**BL-764/765/766**) now flowing through
the ordinary pipeline under an adopt-not-revert posture. One genuinely
dangerous find came out of that review for free: BL-722 had been approved and
queue-jumped to priority 0 while its own spec file sat untracked — invisible
to every worktree role. Fixed as part of the same commit.

A second systemic gap surfaced twice today: **BL-716 landed with 0/5**
acceptance scenarios actually executing (same defect class as **BL-761**,
which now documents 5 shipped tickets with unrunnable acceptance contracts).
BL-761 itself is the fix, but it's a live landmine — landing it today would
immediately freeze the whole Bubble/Android track, because there is no Kotlin
test seam for the acceptance runner to reach. That seam is now specced as
**BL-769**, and should land first.

## Shipped today

| Ticket | What | Notes |
|---|---|---|
| BL-727 | Pilot land runs the acceptance-contract gate | Forked into 2 task-name chains; stale QA copy carrying the fixed defect manually cleared from QA's inbox |
| BL-716 | Bubble/Let's Talk tunnel-hostname DNS failure UX | Also forked (self-deduped by cleaner this time); landed with 0/5 acceptance — BL-761's 5th known instance |

## New tickets this shift

| Ticket | Type/Severity | State | What |
|---|---|---|---|
| BL-760 | defect / high | paused, approved | Guard against duplicate parcel chains per ticket (the BL-727/BL-716 fork class) |
| BL-761 | defect / high | paused, approved | Pre-QA gate: refuse a `git_handoff` to QA whose acceptance contract can't actually run. **Sequencing hazard** — see below |
| BL-762 | feature | paused, approved | Finish-shift bedtime should leave Bubble reachable |
| BL-764 | defect / high | **active**, at hardener | Retro-ticket: front-desk shared-token dual poller drops Host/Bubble bridge updates |
| BL-765 | feature | paused, approved | Retro-ticket: Bubble remote-capability config + hold-music catalog |
| BL-766 | defect / high | paused, approved | Retro-ticket: Mini App Let's Talk half-retired without its server half (real defect, no gate caught it) |
| BL-767 | defect / medium | paused, pending approval | Queued question answered in wrong Telegram topic (found reviewing BL-764) |
| BL-768 | defect / medium | paused, pending approval | Bounce natural-key property test flaky at 2.6% — generator luck, not the property |
| BL-769 | (Android JVM seam) | paused | Policy/infra ticket: give Android a testable unit seam so BL-761 has something to gate against |
| BL-770 | epic | paused | Workflow canary from tracer bullet |

## Pipeline state right now

- `active_backlog_max_depth` effective: **1** (mono-router, one resident)
- `backlog/active/`: **BL-764** only, currently at hardener
- `backlog/paused/`: **167** tickets
- `backlog/` root: **16** raw intakes — structurally blocked, not stalled:
  - 11 behind BL-769 (Android seam policy needs to land or be explicitly
    deferred before they can be scoped)
  - 5 behind a **role_ask pending since 2026-07-30** (still unanswered as of
    this writing — three of those five carry 5-8 open design questions each)

## Flagged, not yet resolved

1. **`android/app/.../BridgeClient.kt` sits with an unresolved merge conflict
   on the shared `main` checkout.** Raised three times today, still
   unresolved on disk. Not currently blocking pipeline work (coder's own
   worktree doesn't have it), but it's live risk on the shared checkout and
   needs a human hand.
2. **The 07-30 specifier role_ask is now 24+ hours stale** and is the direct
   cause of 5 stuck root intakes. Options on the table: mint epic + slice A
   for each of the three design-heavy intakes with specifier defaults (you
   amend after), hold all three as capture-only, or mint just the
   voice-barge-in one and hold the other two.
3. **BL-761's sequencing hazard**: land it now and the Bubble/Android track
   stalls immediately (no way to write a passing contract yet); land BL-769
   first and this resolves itself. Recommend promoting BL-769 ahead of
   BL-761 once a slot opens, per the ticket's own stated preference.

## Optimizer notes

- Depth cap of 1 is correct for the current mono-router pack (single
  resident) — no change recommended.
- Self-correction is visibly improving: BL-716's fork was caught and deduped
  by the cleaner before reaching QA, versus BL-727's fork which needed manual
  clearing. Worth watching whether this holds once BL-760's mechanical guard
  lands, or whether it was one attentive pass.
- `build_freshness_cli.bb sync` will keep refusing until BL-764/765/766 land
  through the pipeline — this is expected and intentional (adopt posture),
  not a fault.

## Asks

1. Human: answer the 2026-07-30 role_ask (see item 2 above) — it's the
   single blocker on 5 of the 16 root intakes.
2. Human: resolve or hand off the `BridgeClient.kt` conflict on `main`
   whenever convenient — not urgent, but growing stale.
3. Coordinator (self): promote BL-769 ahead of BL-761 when a slot next opens,
   per the sequencing hazard above.
