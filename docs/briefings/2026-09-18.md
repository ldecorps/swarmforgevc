Coordinator daily briefing — 2026-09-18

> Composed by the documenter per human ruling A (2026-09-07): the morning
> briefing is documenter-owned agentic work, not coordinator-owned.
> Covers the period since `2026-09-17.md`'s composition (commit
> `b1e276fe9b`) through this writing.

## The story of this window: three Bubble screens ship, one register handoff race caught and cleared

Three of the four Bubble pager screens the epic has been building toward
landed today: **BL-775** (Live, one shared renderer with the Mini App),
**BL-831** (Pipeline board, reusing the board's own read model with a
bounded blurb and a detail sheet), and **BL-1542** (the live-screen
ticket-strip collapse control). Alongside them, **BL-693** closed a real
gap — a standing repeated-paragraph guard over the docs tree, so the kind
of 286-line duplication BL-692 shipped undetected three days running
cannot recur unnoticed — and **BL-1633** fixed the unit suite's per-file
budget gate to confirm a suspected new pole with a solo measurement before
refusing it, ending the false refusals fork contention alone was causing
on `telegramFrontDeskBotCli.test.js`.

Along the way, forwarding BL-831 hit a live instance of the merge-drop
guard's known false-positive shape (see the 2026-09-17 briefing's watch
item): a hardener branch still carrying BL-1637's mint commit in
`backlog/paused/` merged against a branch where BL-1637 had already been
promoted to `backlog/active/` with identical content, and the guard read
the relocation as a dropped hunk. Cleared with the documented BL-1576
remedy — an explicit, empty `git revert` of the commit that authored the
flagged hunk — recorded here as a second confirmed occurrence of that
shape in two days.

## Tickets closed this window

34 tickets closed since the previous briefing's cutoff:

- **BL-677** — the epic backfill apply lands: batched, commit-integrity
  writes of a human-approved `epic:` mapping into `backlog/done/`, refusing
  loud on an unapproved, stale, or unknown-epic mapping.
- **BL-693** — the docs duplicate-paragraph guard, riding the suite every
  parcel already runs.
- **BL-775, BL-831, BL-1542** — three Bubble pager screens, retired above.
- **BL-799** — a bounce record can no longer misattribute a producer role
  to the specifier.
- **BL-1185** — the claim path attributes a Work note's ticket from its
  message again (a silent land-time revert from 2026-08-27, restored).
- **BL-1513, BL-1582** — carried over from the merge-drop gate cascade's
  own tail.
- **BL-1599** — the work ratchet for the unit-suite pole register.
- **BL-1601, BL-1604 .. BL-1621, BL-1623, BL-1624, BL-1626 .. BL-1628,
  BL-1631, BL-1632, BL-1633** — the full tail of yesterday's merge-drop
  gate cascade and the unit-lane-budget/sampled-reach-floor epics: register
  rows restored at land time, reverse-hop send-gate fixes, property-lane
  budget and duration-recording fixes, a full-suite runner for the 1,294
  landed acceptance features that no lane had been exercising, a
  host-wide-process-count standing-red shape identified and owned, and
  the per-file gate's confirm-alone fix (BL-1633) that ends the cascade's
  root symptom.

## Standing-red register: 6 rows (up from 3), all owned

`check_standing_red_register.sh list` shows 6 rows, all under BL-1634
(a stale scenario 02 assertion against the bridge's built-in pages,
red three weeks, unseen because no lane ran landed features until
BL-1628's runner) and BL-1635 (the Android JVM test suite's 4-of-275
`BridgeClientTest` failures from a locale-dependent compile charset —
test source compiled under a no-locale daemon while `main` compiled
UTF-8, so an em-dash literal split into three replacement characters).
Both tickets are active with owners; unowned count is 0.

## New this window

- **BL-1634, BL-1635** — retired above (owners for the standing-red rows).
- **BL-1636** — a step handler never leaks its temp root: the cleaner's
  fifth by-hand mkdtemp-leak fix this session surfaced 583 of 1,191
  handlers `mkdtemp`-ing with 455 never registering the root, ~48k leaked
  entries a day; a unit-lane guard, a shrink-only committed census, and an
  age-floored reap that keeps live-pid roots.
- **BL-1637** — a seat's forward is filed where its completion gate looks:
  `handoffd` files a delivered outbox file under the sender's *stage*
  sent folder while the forward-evidence gate (BL-1609) reads only the
  seat's own folders, so every `coder@2` forward completes only through
  `--no-op`. Surfaced by BL-831's own coder@2 forward; adjudicated and
  minted the same day BL-1615 (landed) fixed the inbound half of the same
  class.

## Backlog snapshot

- `backlog/active/`: **6** (BL-775, BL-831, BL-1634, BL-1635, BL-1636,
  BL-1637)
- `backlog/paused/`: 96
- `backlog/hold/`: 0
- `backlog/done/`: 1,449 total (+34 since 2026-09-17's cutoff, `main`;
  more landed after this briefing's cutoff are not yet counted here)

## Operator watch items

- **The merge-drop guard's false-positive shape (BL-1576) recurred a
  second time today**, on a promoted-vs-still-paused ticket file caught in
  a merge, not a bounce. The documented remedy (an explicit empty
  `git revert` naming the flagged commit) cleared it, but a guard that
  needs this recipe twice in two days on unrelated shapes is worth a
  hardening pass of its own before a third occurrence costs more time.
- **`coder@2`'s stage-queue-dir mismatch, flagged 2026-09-17, is now
  understood on both sides** (BL-1615 inbound, BL-1637 outbound) but
  BL-1637 itself is still active, not yet landed — a `coder@2` forward
  that completes only through `--no-op` between now and its land is this
  known defect, not a new incident.
- **The unit suite's per-file budget gate no longer refuses on fork
  contention alone (BL-1633)** — a suspected new pole is now confirmed
  with a solo measurement before the gate refuses it. Treat this as the
  closing fix for the cascade the 2026-09-17 briefing flagged as
  "provisional… scans more history than the sender is responsible for" —
  that was the merge-drop guard's own cascade, a separate shape from this
  one, and both are now believed closed pending a quiet day.
- **Approval asks on Telegram must carry pros/cons per option** (human
  directive, 2026-09-11) — a ruling ask answered without weighed
  alternatives is out of policy; don't approve unasked.
- **An expedite/"jump it" request counts as pre-approval for a plain
  (no-choice) gate on that ticket** (human directive, 2026-09-15) — a real
  ruling with pros/cons still comes back for anything with a choice.
