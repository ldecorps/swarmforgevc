Coordinator daily briefing — 2026-08-23

> Written by the coordinator, covering the 24h since `2026-08-22.md`'s
> cutoff (~05:35 BST 2026-08-22) through ~05:30 BST 2026-08-23. Pack stayed
> `full-forge` throughout. The dominant story is a very large human
> approval sweep (~85 tickets touched) landing on top of the Boy Scout
> debt-scan's first two slices, which together produced this shift's
> unusually high new-ticket count. Cost and token spend fell sharply across
> every role compared with yesterday. **Right at the tail of the window
> (~05:26 BST) the human engaged a promotion freeze** to focus the swarm on
> the Cursor-as-swarm-agent chain — this is the single most important thing
> in this briefing and is covered in full below.

## Executive summary

**40 tickets closed** across the day (list below), including the Bubble
hands-free voice epic (BL-777/844/845), the Boy Scout debt-scanner itself
(BL-1014/1015), and a cluster of daemon/supervisor correctness fixes
(front-desk respawn storms, freshness-watchdog thrash, tmux-reaper
false-fires).

**~85 new tickets minted**, the large majority produced by the Boy Scout
scan's first live run ranking real recurring debt (BL-1031 through the
BL-1070s) plus the Cursor/local-model seat epic (BL-1078–1082) and its
qwen-code supersede cleanup (BL-1052/1053/1084/1087).

**A very large human approval sweep landed today**: roughly 70 `Approve
BL-*` commits, clearing most of yesterday's 68-ticket approval backlog
(BL-1028 through BL-1090, plus the three pilot defects BL-732/738/748/750/
752/754). Despite that, paused-but-unapproved count barely moved (66 today
vs 68 yesterday) — the Boy Scout scan is minting new paused work about as
fast as approvals clear it.

**Three process-friction patterns recurred and are worth naming together:**

1. **BL-1100 (promotion prose-grep)** — `promote_and_route_next.sh`'s
   do-not-promote check greps ticket prose rather than a structured field,
   and false-positived on unrelated "do not promote" text in notes for
   BL-1030, BL-1031, and BL-1071 — all three needed manual promotion by me
   today. BL-1100 (specced + approved) fixes this at the root; not yet
   built.
2. **BL-1082 re-park loop** — the local-model-seat ticket kept getting
   re-picked by `promote_and_route_next.sh` despite being explicitly
   re-parked by the human (reserved for human/Cursor build, not yet
   resumable pending BL-1079/1080). Re-parked twice by hand today; no
   machine-checkable hold field exists yet for this case.
3. **BL-1090, 3rd occurrence of the recurring "dirty master WIP blocks
   freshness sync" gap** — uncommitted edits in
   `extension/src/concierge/{approvalAskReconcile,conciergeTick}.ts`
   repeatedly refused `build_freshness_cli.bb sync` today. Handled
   correctly per precedent (patch captured to
   `backlog/evidence/BL-1090-uncommitted-draft-20260823.patch`, ticketed,
   approved) but still sitting in `paused/` unpromoted — the standing gap
   (nobody owns clearing a `:dirty-surface` refusal) remains unticketed,
   raised in BL-1090's notes for the BL-820 closing ceremony.

**Other notable items**: BL-891 reconcile resolved a ticket-id collision on
BL-1096 (two unrelated tickets minted under the same ID); BL-1098 specced a
silent-revert defect QA reported (BL-973 content reverted 3× on `main`,
restored by hand twice); BL-1099 retired BL-303's superseded scenario;
BL-782 logged its 3rd starvation report (cap was full, not expedited, ranks
behind by priority — correct disposition, no promotion).

## Shipped this shift (40)

| Ticket | What it was |
|---|---|
| BL-1083 | promotion into `active/` now goes only through the gates (Telegram Expedite verb + paused-pager endpoint were bypassing depends_on/hold/depth cap) |
| BL-1086 | babysitterd no longer re-walks every SHA ahead of QA every tick |
| BL-1088 | given-up-child cooldown now actually holds for its full window |
| BL-1063 | nvm-PATH property test's two wrong invariants fixed |
| BL-973 | 5 hand-maintained bb fixture copy-lists now derive/gate from their own closures |
| BL-1076 | batch-claim staleness observer no longer flags a mid-Stryker hardener as a stale claim |
| BL-1043 | `poll-heartbeat-stale?` 3-arity call sites get a real startup grace |
| BL-1045 | pipeline board surfaces `hold`-stage tickets (previously invisible) |
| BL-777 | Bubble barge-in detector + playback abort (<300ms) |
| BL-844 | Bubble hands-free session state machine |
| BL-845 | Bubble on-device "hey bubble" wake word |
| BL-713 | Cursor-seat driver spike — one role seat over Cursor SDK/ACP completes a real parcel |
| BL-1069 | tmux 3.4 WSL segfault: version warn + soft harden + installer |
| BL-1015 | Boy Scout slice 2 — one-item-or-refuse cleanup pass |
| BL-1075 | `window-size largest` mitigation documented against tiling-panel override |
| BL-1029 | remaining 7 bare-single-quote respawn sites fixed (BL-1018 family) |
| BL-991 | `route-required-stages` no longer silently records a 4-stage jump as addressed |
| BL-1057 | host-switchover doctor names what a host move left behind |
| BL-1066 | panel's mean-ticket-time computation no longer runs a ~102s git walk on a 2s poll tick |
| BL-975 | BL-967's owed 30-min live soak run + recorded |
| BL-1058 | shell test lane's BSD-only `mktemp` fixed — 83 suites unblocked |
| BL-1061 | tunnel-ownership property fixture no longer binds the operator's real tunnel |
| BL-1064 | Front Desk BL-582 diagnostics log grounded against what the launcher actually writes |
| BL-1033 | BL-1025 property runner's temp-root leak on throw fixed |
| BL-1060 | Bubble re-pair button URI scheme Telegram accepts |
| BL-1028 | `promote_and_route_next.sh` no longer bypasses a refused commit-integrity lock |
| BL-1049 | role panes no longer inherit all 15 provider secrets |
| BL-1038, BL-1039 | unit-lane live-repo and per-scenario git-fixture cost cut from the profile |
| BL-1032 | tmux-reaper guard no longer fires on a file that only asserts about tmux argv |
| BL-1048 | board no longer renders an at-the-door parcel as not-started |
| BL-1037 | freshness watchdog no longer restarts a healthy front desk once per commit |
| BL-1041 | rescue path no longer makes orphaned work less durable (stash-pop-into-another-worktree fixed) |
| BL-1036 | front-desk 409 conflicts on respawn logged with recovery, not just degradation |
| BL-1035 | front-desk supervisor startup grace nil-guard fixed (was a 6s stall/respawn loop) |
| BL-1014 | Boy Scout slice 1 — deterministic debt scan across 5 live evidence sources |
| BL-1011 | freshness alarm names its swarm and its actual reason (was a raw sentinel) |
| BL-1010 | secondary swarm now publishes fleet status under its own identity, not the primary's |
| BL-1022 | daemon subprocess-API gate now follows spawn edges, not just the load-file closure |
| BL-1026 | expeditor's per-stage budget raised 45→90min (was uncommitted WIP, now landed) |
| BL-1024 | expedited runs now name what they left behind (parked tickets, staged-but-uncommitted moves) |

## Promotion freeze in force (human-directed, ~05:26 BST)

The human engaged `.swarmforge/operator/control-pause.json` (`active:
true`, no timer) to steer the swarm's focus onto the Cursor-as-swarm-agent
chain specifically. Effect: effective `active_backlog_max_depth` is now
**0** — auto-pick, by-name `promote_and_route_next`, and open-slot nudges
all refuse. Documented in `backlog/STEERING.md` (still uncommitted as of
this writing — see Flagged, below).

As part of the same directive, four in-flight tickets not on the Cursor
chain were moved **out of `active/` into `hold/`** at 05:25
(`3feb55b92`, co-authored by Cursor): **BL-1030, BL-1031, BL-1071,
BL-1077**. They stay parked until the human resumes.

**Only two tickets remain active, both on the Cursor chain, both
continuing normally:**

- **BL-1078** — a Cursor agent token is accepted by the launcher (with
  coder)
- **BL-1081** — an ACP host in a pane can drive one seat (with coder)

The freeze is self-expiring by its own text: "spent the moment all four
[held] tickets are in `backlog/done/` and the watcher has fired" — but an
expired directive is not a policy and should be retired explicitly, not
left silently vetoing work. **No promotion action is needed from me while
this is engaged**; I will resume normal promotion the moment the human
clears the pause marker or the watcher fires.

## New tickets today (~85, selected highlights)

| Ticket | What it addresses |
|---|---|
| BL-1100 | promotion candidacy must be decided by structured fields, never a prose grep — root-causes today's BL-1030/1031/1071 false-blocks |
| BL-1098 | silent revert of already-landed content (BL-973 reverted 3× on `main`) |
| BL-1096 | router re-routes a ticket that has already been worked (id collided with an unrelated ticket, reconciled) |
| BL-1090 | dirty-master WIP (concierge tick/ask-reconcile) — approved, still paused, no open slot |
| BL-1084 | consolidates the BL-1052/1053 qwen-code→local-model supersede gap |
| BL-1085–1089 | repo-creation guard, push-sweep re-proof loop, front-desk liveness gate gap, and related infra defects from the Boy Scout scan |
| BL-1042–1076 | bulk of the Boy Scout scan's ranked debt items — host-load intake throttling, terminal-title/board seat-naming, mailbox retention, ticket-duration measurement inflation, generator-reach lottery, etc. |
| BL-1099 | retires BL-303's superseded give-up-cooldown scenario |
| BL-1034 | expeditor should commit the backlog moves it makes itself |

Full list is the `backlog/paused/` and `backlog/active/` diff since
yesterday's cutoff — not reproduced in full here for length.

## Pipeline state right now

Active **2** (BL-1078, BL-1081) against an effective cap of **0** —
promotion frozen by human directive (see above), not a stall. Held: 4
(BL-1030/1031/1071/1077). Paused: 212 total, 146 already `human_approval:
approved` (66 pending). No ambulance active. `main` is 56 commits ahead of
`origin/main`, 0 behind — pushing is `push_sweep_lib.bb`'s job, not the
coordinator's, so this is reported not acted on. Token/cost spend fell
sharply across every role vs yesterday (coordinator+specifier $258→$121,
coder $376→$195, hardener $359→$148, architect $168→$58, QA $105→$41,
cleaner $86→$37, documenter $60→$23) — all down 45–60%.

## Flagged, not yet resolved

1. **Promotion is frozen** (see above) — expected, human-directed, no
   action needed from me until resumed.
2. **Three files are uncommitted on `main` right now, beyond BL-1090's
   already-ticketed WIP**: `backlog/STEERING.md` (the freeze directive
   itself), `swarmforge/roles/hardender.prompt` (BL-1101 rule-proposal text
   appended), and BL-1101's own new ticket + feature file
   (`backlog/paused/BL-1101-*.yaml`, `specs/features/BL-1101-*.feature`,
   both untracked). None of these block `build_freshness_cli.bb sync`
   today only because BL-1090's `extension/src/concierge/*.ts` edits
   already do — but they should be committed in their own right rather
   than riding along uncommitted indefinitely. I have not touched them:
   STEERING.md is the human's own directive text, and BL-1101 is a
   hardener rule-proposal awaiting its own disposition, not mine to
   commit unilaterally.
3. **BL-1090 is approved but unpromoted** — the underlying dirty-tree WIP
   is captured and safe, but the ticket itself is still waiting for an
   active slot (doubly so now, with the cap at 0); the standing gap (a
   `:dirty-surface` refusal escalates to nobody) is still unticketed,
   raised only in BL-1090's `notes:` for the BL-820 closing ceremony to
   pick up.
4. **BL-1100 needs to land** — until it does, any ticket whose notes happen
   to contain unrelated "do not promote"-shaped prose will keep
   false-blocking automatic promotion, forcing manual coordinator
   intervention (3 instances today: BL-1030, BL-1031, BL-1071).
5. **BL-1082 has no machine-checkable hold** — it will keep getting
   re-picked by `promote_and_route_next.sh` (once promotion resumes) until
   either BL-1079/1080 land (making it resumable) or a real hold field
   exists.
6. **Approval queue is still large**: 66 of 212 paused tickets await
   `human_approval`, and the Boy Scout scan is generating new paused
   tickets faster than today's large approval sweep could keep pace with.
7. **Two rescued WIP stashes from prior days** — not re-checked this
   shift; worth a follow-up look if they're still sitting unclaimed.

## Asks

1. **No blocking decision needed right now** — today's session was mostly
   the human clearing the approval backlog directly and then engaging the
   Cursor-chain focus freeze; both read as intentional and correct.
2. **When ready to resume promotion**, clear
   `.swarmforge/operator/control-pause.json` (or let the watcher fire once
   BL-1078/1081/held-four are all in `done/`) — I'll pick back up
   immediately, no reminder needed.
3. **BL-1100 and BL-1082's hold-field gap** are both process-machinery
   defects worth prioritizing once promotion resumes — they cost manual
   coordinator intervention every time they recur.
4. **Approval queue (66 pending)** — no urgency, but the Boy Scout scan
   will keep feeding it; worth periodic attention so it doesn't grow
   unbounded.
5. **STEERING.md / hardener.prompt / BL-1101 uncommitted** — worth a
   deliberate commit when convenient; flagged above, not acted on by me.
