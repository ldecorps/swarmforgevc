# Daily Briefing — 2026-07-24

## Executive Summary

**Four tickets closed since yesterday.** BL-531 (pre-QA durability/wiring gate),
BL-532 (sibling-bounce isolation), BL-606 (specifier-declared `required_stages`
routing), and BL-528 (auto-heal claim-without-progress) are all in `backlog/done/`.
BL-606's kill-switch (`required_stages_routing_enabled`) has been flipped **ON**
(go-live commit `a30247640`) — dynamic stage-skipping is now live, not just built.

**A recurring "false-completed" mailbox defect surfaced three times today.** A
resident processing a `merge_and_process` git_handoff dequeued and marked it
`completed` in its own inbox **without actually running the `git merge`** —
leaving the ticket's real lineage stuck one hop behind while the mailbox record
made it look done. This hit **QA** for BL-532, **QA** again for BL-606 (both
discovered together — QA's branch had merged both parcels but never sent an
approval note for either), and then **cleaner** for BL-607 (coder's architect-
bounce fix, `16e7c461dd`, sat "completed" in cleaner's inbox with cleaner's branch
never actually containing it). Same shape all three times: mailbox bookkeeping
happened, the substantive work did not, and nothing about the mailbox state would
have revealed that without checking `git merge-base --is-ancestor` by hand. This
is flagged below as a swarm-reliability ticket candidate, not something to keep
babysitting manually.

**A related, separate defect: notes asking the resident to `rotate_to_role.sh`
were completed twice without the resident ever running the command.** Two
consecutive rotate-to-QA notes (`000740`, `000741`) got dequeued/marked-done with
`mono-router-active-role` never changing from `coder`. Direct invocation of
`rotate_to_role.sh` from the coordinator shell (bypassing the note entirely)
worked both times it was tried. This and the false-completed pattern above may
share a root cause worth a single investigation.

**BL-528's own evidence trail produced two real follow-up tickets.** The
specifier filed **BL-612** (claim-progress acceptance step handlers, now
`human_approval: approved`) and **BL-613** (stuck-escalation email never fires,
priority 2 defect, approved) directly from BL-528's QA pass. BL-613 is next in
line for promotion once the active slot frees.

**Backlog root intake is growing, not draining.** Three raw human/operator
intakes are sitting in `backlog/` root right now, all filed within the last few
hours: a Telegram-bot-token cloning incident (rival poller silently stole inbound
messages for ~9 hours), a front-desk defect where photo/media captions are
silently dropped (which is *how* the token-cloning report almost went unheard —
the human's own screenshot-with-caption got no reply), and a new human directive
asking the morning briefing to warn when projected token burn will outrun the
weekly reset. The specifier has been rotated to drain these repeatedly today but
new intake keeps arriving faster than it's being drained.

**BL-607 (role clarifying-poll-in-own-topic) is the sole active ticket**, and has
already reached QA today — it went active → coder → (architect bounce, fixed) →
cleaner (stalled on the false-completed defect above, manually unstuck) →
architect → hardener → documenter → QA within the day.

---

## Pipeline State

| Slot | Ticket | Stage | Notes |
|------|--------|-------|-------|
| 1/1  | **BL-607** — role clarifying-poll-in-own-topic | **QA** | Documenter's commit (`7fa17fecd1`) merged into QA's branch; QA review in progress. |

**Cap:** effective 1 (`effective_backlog_depth_cli.bb` — configured `-1`/unlimited
via the base conf, but this pack's own conf pins it to `1`).

**Onboarding contract gate:** `allow`.

**Backlog root:** **NOT drained** — 3 raw intakes outstanding (see Executive
Summary and Blockers). This is a live intake-order violation risk: per
constitution, root must drain before paused work is considered, though it does
not block active/paused promotion mechanically.

**Paused (67 items),** notable for the next promotion decision:
- **BL-613** — stuck-escalation email never fires (priority 2, defect, approved,
  no deps) — top of the queue once BL-607 closes or another slot opens.
- **BL-612** — claim-progress acceptance step handlers (priority 30, approved) —
  BL-528 follow-up, eligible but lower priority.
- **BL-536** (priority 2, defect) — blocked, depends on `BL-535` which sits in
  `backlog/hold/`, not done.
- **BL-563** (priority 03) — gated on a human ruling per the standing BL-545
  promotion order; do not promote out of turn.
- Priority-`00` items are epics (`BL-517`, `BL-539`–`BL-543`, `BL-545`, `BL-558`,
  `BL-594`) — explicitly "do not promote directly; tracks children" per their own
  titles.

---

## Token Metrics (per-role activity today, 2026-07-24 so far — partial day)

| Role | Tokens today | Δ vs yesterday | Cost (USD) |
|------|-------------|-----------------|------------|
| Coordinator+Specifier | 306,860 | −1,950,298 | $126.86 |
| Coder | 373,061 | −979,540 | $47.53 |
| Architect | 190,334 | −595,805 | $65.03 |
| Cleaner | 95,895 | −1,661,794 | $3.77 |
| QA | 86,148 | −199,135 | $12.90 |
| Hardener | 83,174 | −375,317 | $11.81 |
| Documenter | 58,630 | −1,028,383 | $2.27 |

All roles down sharply vs. yesterday — expected, this snapshot covers only part
of the day and the swarm has been running a single ticket end-to-end plus
bookkeeping rather than a multi-ticket batch.

---

## Blockers & Risks

**False-completed `merge_and_process` handoffs (3x today, cross-role).** QA/BL-532,
QA/BL-606, and cleaner/BL-607 all showed the same failure: mailbox marks a
`git_handoff` "completed" without the underlying `git merge` ever happening. No
mechanical check flags this — it only surfaces via manual
`git merge-base --is-ancestor` comparison against the receiving role's own branch
tip. Recommend a ticket to add this check as an automated close-guard-style gate
(analogous to `ticket_close_guard_lib.bb`'s QA-approval check) rather than relying
on the coordinator noticing by hand each time.

**`rotate_to_role.sh` notes silently no-op'd twice.** Two rotate-to-QA notes
completed without the resident ever invoking the script; direct coordinator-side
invocation worked both times. Possibly the same root cause as the item above
(resident treating an instruction note as informational rather than actionable).
Worth a combined investigation ticket.

**Backlog root growing faster than it drains.** Three raw intakes are outstanding,
including a **security-relevant one** (rival poller stealing the Telegram bot's
inbound channel for ~9 hours) that itself almost went unreported due to the
*second* outstanding intake (photo-caption defect). Recommend the specifier be
given a dedicated drain pass before any further promotion work, given the
security angle on the token-cloning incident.

**BL-536 and BL-563 both correctly held** on unmet dependencies — no action
needed, just noting per the standing "recheck stale holds" duty.

---

## Swarm Optimizer Recommendations

**File a ticket for the false-completed-handoff defect.** Same shape hit QA twice
and cleaner once in one day — this is systemic, not a one-off. A mechanical
lineage check at handoff-completion time (or at minimum a periodic sweep
comparing each role's "completed" mailbox entries against actual branch ancestry)
would catch this without coordinator babysitting.

**File a ticket for the rotate-note no-op defect**, possibly bundled with the
above if root-caused the same way (resident LLM behavior treating a note as
descriptive rather than a literal command to execute).

**Prioritize a specifier drain pass over further promotion** this cycle — three
root intakes outstanding, one with security implications, is a bigger risk than
an empty second active slot.

**BL-613 is ready the moment a slot opens** — no optimizer action needed beyond
normal promotion order once BL-607 closes.

---

## Coordinator Action Log

1. Verified QA-approved commits for BL-532/BL-606 (batch-shipped with BL-531,
   `81d06697`) and BL-528 (`07031bca31`); closed all three tickets to `done/`
   after confirming ancestry and running `build_freshness_cli.bb sync`.
2. Resolved a local/`origin` main divergence (uncommitted approval-flip edits to
   BL-607/609/610 plus a stale duplicate doc edit) via commit + `merge --no-ff` +
   push, rather than a forced fast-forward.
3. Promoted and routed **BL-528** then, after its close, **BL-607** — cap stayed
   fed at 1/1 throughout.
4. Diagnosed and manually recovered from three false-completed `merge_and_process`
   handoffs (QA×2, cleaner×1) by direct `rotate_to_role.sh` invocation plus an
   explicit corrective note, each time verifying via `git merge-base
   --is-ancestor` rather than trusting mailbox state.
5. Rotated the resident to specifier multiple times today to drain backlog-root
   intake; root remains undrained (3 items) as new intake keeps arriving.
6. Corrected BL-606's stale `assigned_to: specifier` field to `coder` after
   confirming (via git log) that its architect bounce #3 had already been
   resolved and the ticket had shipped — avoiding a duplicate/wrong re-dispatch.
7. This briefing.

---

*Coordinator — orchestration altitude only.*
