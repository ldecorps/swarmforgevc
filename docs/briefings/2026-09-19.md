Coordinator daily briefing — 2026-09-19

> Composed by the documenter per human ruling A (2026-09-07): the morning
> briefing is documenter-owned agentic work, not coordinator-owned.
> Covers the period since `2026-09-18.md`'s composition (commit
> `1af06d965b`) through this writing.

## The story of this window: the forward-completion gate learns two more shapes, and a standing-red hygiene sweep clears the calendar-dependent and host-load-dependent flakes it surfaced

BL-1609's forward-completion gate (landed 2026-09-17) kept refusing real
work it should have accepted, three times running: **BL-1637** found that
a seat's delivered forward is filed under its STAGE's sent folder while
the gate reads only the seat's own, so every `coder@2` forward completed
through `--no-op`; **BL-1642** found that QA's own forward is always a
`note` (it lands the commit itself and broadcasts merge-up/bookkeeping),
never a `git_handoff`, so every QA approval since the gate landed —
27 of 27 — refused and completed `--no-op`; **BL-1645** found the
gate's evidence window opened at an inbound's `dequeued_at` rather than
its `created_at`, so a re-dequeued Work note whose queue held older items
first could read real, already-sent work as too old to count, blocking a
coder seat both ways (`WORK_NOT_EVIDENCED` and `WORK_ACTIVE_ON_MAIN`).
All three landed today, sharing one fix surface
(`forward_evidence_lib.bb`) without drifting apart.

Alongside that thread, a standing-red hygiene sweep closed four flakes the
register had been carrying: **BL-1647** (a zombie pidfile reading as
alive under `kill -0` alone, flaking `test_finish_shift_lib.sh` case 08
under host load), **BL-1648** (`BL-1428`'s own acceptance scenario 01
asserting a wall-clock age against a fixture row seeded on a fixed date —
true on 2026-09-05, red by arithmetic every day since 2026-09-06, unowned
for two weeks because nothing ran the feature until BL-1646's changed-path
lane did), **BL-1646** (the commit guard's ledger-row check ignoring the
register's own ownership join and treating a branch merge's *inherited*
lines as newly authored, refusing `coder@2`'s routine merge of `main`),
and **BL-1639** (bedtime's babysitterd stop signalling one pidfile while
its verify matched every `babysitterd.sh` process on the host, so a second,
operator-local daemon copy was neither stopped nor excused, and
finish-shift refused with `REFUSE: ... still running: babysitterd`,
leaving a 283-finding orphan sweep running until a human killed it by
hand). **BL-1651** (in flight) replaces the property lane's V8-heap crash
— five parcels' coder passes hit `FATAL ERROR: JavaScript heap out of
memory` on unrelated files across two days — with a named, deterministic
per-file ceiling and a host-derived cap, and repairs the two BL-871
acceptance scenarios its own derivation change made a standing red.

A recurring merge-time hazard surfaced three separate times this window:
an auto-merge silently duplicating a freshly-added function definition
(`inbound-window-start` in `forward_evidence_lib.bb`) when the same
content landed on two lineages through different paths — caught each time
by running the affected file's own test runner before forwarding, never
by the merge tool itself.

## Tickets closed this window

12 tickets closed since the previous briefing's cutoff:

- **BL-775, BL-831** — the two Bubble screens still active at the 2026-09-18
  cutoff (their deferred Stryker mutation gates deferred separately as
  BL-1638 and BL-1643, both paused, cooldown-gated to 2026-09-21 and
  2026-09-20 respectively).
- **BL-1634, BL-1635** — the two standing-red rows the 2026-09-18 briefing
  reported as active-with-owners: a stale scenario against the bridge's
  built-in pages, and the Android JVM locale/charset compile mismatch.
- **BL-875** — the stray root `package-lock.json`, tracked with no
  `package.json` beside it, removed and root-anchored-ignored so an
  accidental root `npm install` can no longer produce a git-visible diff.
- **BL-1636** — the step-handler temp-root leak: a standing unit-lane
  guard, a shrink-only committed census (532 offenders), a
  `trackedTmpRoot` one-line fix, and a reap script for the 687k roots
  already leaked on this host.
- **BL-1637, BL-1642, BL-1645** — the forward-completion gate's three
  fixes, retired above.
- **BL-1639, BL-1646, BL-1647, BL-1648** — the standing-red hygiene sweep,
  retired above.
- **BL-1649** — the claim-idle escalation ladder gains a conf-driven
  per-role timeout (`QA 90`, shipped) and stops counting a role whose
  agent process is absent or was just respawned as idle, after a
  legitimate 80-minute QA land bounced at reclaims=6 on 2026-09-19.

## Standing-red register: 6 rows, all owned

`check_standing_red_register.sh list` on `main` shows 6 rows, all owned:
`BL-1638` and `BL-1643` (the two deferred Stryker mutation gates above,
cooldown-gated), `BL-1647` and `BL-1648` (retired above — their rows
discharge when their lands reach `main`), `BL-1651` (the property-lane
heap fix, in flight, whose own row discharges with it), and `BL-1654`
(new this window — `BL-1308` scenario 03 asserts a shape `BL-1389`
retired on 2026-09-04; unseen until the cleaner's changed-path lane ran it
2026-09-19). Unowned count is 0.

## New this window

- **BL-1639 .. BL-1649, BL-1651** — retired above.
- **BL-1650** — the land step's landed-sibling verdict walks first-parent
  ancestry only, so a closed sibling whose commit reached the branch
  through a non-first-parent merge (the everyday pipeline shape) reads
  `ENTANGLED_SIBLING` instead of `LANDED_SIBLING` — surfaced 2026-09-19
  when BL-1636's replay kept printing it for BL-831 and BL-1634 after a
  ruled cherry-pick; also lands a closed-owner stray whose every path is
  pure evidence, standalone.
- **BL-1652** — the chase sweep never respawns a busy role, and respawns
  at most once per sweep (a duplicate-respawn shape observed alongside the
  BL-1639 incident's own crash-and-recover).
- **BL-1653** — a commit abandoned at the daemon's 60-second subprocess
  bound never leaves its paths staged in the shared checkout: twice today
  (a briefing send-marker commit and a specifier mint commit, both killed
  mid-hook), the reconcile sweep stalled naming staged paths as a
  "conflict" instead of releasing ownership, until a human unstaged them
  by hand. Amended same window to add: a failed abort that finds no
  `MERGE_HEAD` releases merge ownership instead of looping
  `merge-abort-failed` every tick.
- **BL-1654** — retired above (owner for the new standing-red row).

## Backlog snapshot

- `backlog/active/`: **6** (BL-1647, BL-1648, BL-1650, BL-1651, BL-1652,
  BL-1653)
- `backlog/paused/`: 101
- `backlog/hold/`: 0
- `backlog/done/`: 1,461 total (+12 since 2026-09-18's cutoff, `main`;
  more landed after this briefing's cutoff are not yet counted here)

## Operator watch items

- **An auto-merge can silently duplicate a just-added function definition
  when identical content reaches two branch lineages by different
  paths** — seen three times this window on
  `forward_evidence_lib.bb`'s `inbound-window-start` (BL-1645's own
  addition, each time re-introduced whole by a later merge that carried
  it on the other side too). Git reports these merges as clean (no
  conflict marker), so the only catch is running the affected file's own
  test runner before forwarding — a role that skips that step on a
  same-day merge could forward silently-duplicated code. Worth a standing
  guard if a fourth occurrence appears.
- **Two deferred Stryker mutation-gate rows are cooldown-gated**
  (`BL-1638` not before 2026-09-21, `BL-1643` not before 2026-09-20) —
  both throttle `active_backlog_max_depth` to 1 until their owners
  discharge them; expect reduced concurrent capacity until then.
- **The property lane (`npm run test:properties`) has not completed a
  full run on a parcel tree since at least 2026-09-18** (five crashes
  across five parcels' coder passes) — BL-1651 (in flight) is the fix;
  until it lands, every role continues the run-once directive from
  2026-09-17 rather than retrying.
- **Approval asks on Telegram must carry pros/cons per option** (human
  directive, 2026-09-11) — a ruling ask answered without weighed
  alternatives is out of policy; don't approve unasked.
- **An expedite/"jump it" request counts as pre-approval for a plain
  (no-choice) gate on that ticket** (human directive, 2026-09-15) — a real
  ruling with pros/cons still comes back for anything with a choice.
