Coordinator daily briefing — 2026-08-27

> Written by the coordinator, covering the ~24h since `2026-08-26.md`'s
> cutoff (~09:55 BST 2026-08-26) through ~12:55 BST 2026-08-27 (due
> refresh). Cost & health sidecar: `docs/briefings/2026-08-27.json`.

## Business features delivered

**handoffd parse restore** (BL-1163) — BL-668 post-QA sweep left unmatched
parens; daemon load and deliveries recovered.

**Half-launch / swarm-starved auto-repair** (BL-1169) — bounded repair
instead of escalation-only for half-launch and STARVED CRIT classes.

**Portable agent-memory payload** (BL-1177) — capture/inject API for
same-role model swap (wired into follow-on BL-1178 active work).

**Property-suite standing reds** (BL-1175) — unrelated green commits no
longer blocked by standing fixture reds.

**Work-note task header** (BL-1185) — hard seats defer-better-fit when
work notes omit the task header (strand ambulance patients fixed).

**Deprecator operator scan** (BL-1174) — ranked stale-rule scan, one
retirement, `docs/deprecated/` output.

**Yesterday's approval holds cleared:** **BL-753**, **BL-755**, **BL-758**
all bookkept to `done/M8/` on current `origin/main`.

**QA bookkeep wave (today):** BL-602, BL-780, BL-781, BL-600, BL-1164,
BL-601, BL-597, BL-605, BL-980 plus closes BL-779, BL-754, BL-1084,
BL-784, BL-1023, BL-1173, BL-695, BL-534, BL-1020, BL-668, BL-1085,
BL-748, BL-649, BL-664, BL-743, BL-741, BL-738, BL-596, BL-1174,
BL-1175, BL-1177, BL-1185, BL-1169, BL-1163, BL-1007/BL-888
(mistaken re-promote undone).

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.13 (flat vs ~$8.31 prior briefing).
- **Closed today:** 18 (flow sidecar); **specced today:** 0.
- **Suite duration:** ~42s latest (+20s vs prior) — low but trending up.
- **Not done:** 127 tickets (35 active, 93 paused on `origin/main`).
- **Friction:** property-suite standing-red churn on `main`; BL-631
  pipeline-on-main CRITs; phantom coordinator wakes; specifier approval
  queue (BL-1187/1188/1189/1190, BL-1396/1397) spec-ready but not
  promoted.

## Agent / pipeline business

- **Pack:** `cursor-forge` (qwen Token Plan exhausted until 2026-08-31
  23:03 UTC). Day shift per STEERING.
- **`main` sync:** `proceed` on bookkeep path; local checkouts often
  `ahead` of `origin/main` from property-suite reconcile spam — treat
  `origin/main` as shipped truth until QA integration (BL-631).
- **Effective depth:** unlimited (`-1`).
- **Active highlights:** BL-1167 (tier routing bypass), BL-1181 (bob
  cherry-pick), BL-1184 (briefing shift velocity), BL-1000/1001
  (freshness + coder routing), BL-1178 (wire agent-memory), BL-592
  (spec-tree console — spec gap flagged to specifier).
- **Paused (not promoted):** BL-1112 (standing-unit reds sandbox),
  BL-472 (babashka hardening — deferred tracker), BL-755/758/753 now
  done.
- **Coordinator inbox:** idle after routing BL-1167/1175 drops; repeated
  `NO_TASK` wakes with empty inbox (likely delivery bug).

## Optimizer recommendations

1. **Human:** approve BL-1187, BL-1188, BL-1189, BL-1190, BL-1396,
   BL-1397 when ready — specifier has spec-ready parcels held.
2. **QA / ops:** land BL-631 integration so pipeline paths on `main` are
   QA-approved; stop property-suite init spam diverging local `main`.
3. Keep tip-ahead bookkeep tight (today's wave was healthy).
4. Promote BL-1112 only after human clears standing-unit sandbox scope.
5. Watch suite-duration uptick (+20s) and top spenders GH-22 / BL-657 /
   BL-523.

## Process hiccups (and recovery)

1. **handoffd "down" CRIT** — false alarm; process alive with fresh
   heartbeats; BL-1163 was the real parse outage earlier.
2. **Phantom coordinator wakes** — `new handoff mail` with empty inbox /
   `NO_TASK`; benign but noisy.
3. **Local `main` ahead spam** — ~143 property-suite / BL-9999 init
   commits ahead of remote on dirty checkouts; `build_freshness sync`
   refused when unapproved pipeline commits present.
4. **BL-631 babysitter CRITs** — pipeline code on `main` outside QA
   ancestry; routed priority-00 note to QA.
5. **Orphaned specifier mint** (`117dd0f9b` BL-1187/1188) — not on
   current `main` tips; re-land after approval flip.
6. **Mistaken re-promotes** BL-1007 / BL-888 — coordinator closed back
   to done without re-opening QA work.

## Asks

1. Approve held specifier parcels (BL-1187–1190, BL-1396/1397).
2. QA integration for BL-631 + reconcile property-suite standing reds.
3. Confirm BL-592 spec-tree feature lands or stays paused until spec gap
   closed.
4. Human decision on BL-472 deferred tracker before any promote.
5. Merge-ready GitHub PRs #2 and #3 (extension autostart / WSL bootstrap)
   when operator wants them on `main`.
