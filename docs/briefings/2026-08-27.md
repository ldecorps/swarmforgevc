Coordinator daily briefing — 2026-08-27

> Written by the coordinator, covering the ~24h since `2026-08-26.md`'s
> cutoff (~09:55 BST 2026-08-26) through ~09:45 BST 2026-08-27.
> **Missed ceremony** (handoffd parse-broken ~05:25–06:00 BST) — this
> file is the catch-up send. Cost & health sidecar:
> `docs/briefings/2026-08-27.json`.

## Business features delivered

**Deprecator soft verbs** (ambulance patient **BL-1174**, QA
`c4df35beed`): `/deprecate` ranked scan + one retirement +
`docs/deprecated/` stubs; hard-tier / multi-document reasoner only.
**Deprecator freshness-gate CLI** (**BL-1173**, QA `7348186dfd`) —
promote-time Article 3.6 check path the soft verbs reuse.

**Babysitter half-launch / swarm-starved auto-repair** (**BL-1169**, QA
`ee839adb7d`) — CRITs queue bounded repair, not escalation-only.

**Trend: mono-router rotation dynamics** (**BL-596**, QA `dfc74ee245`).
**BL-718 chunking property reaches the real split boundary** (**BL-738**,
QA `401048f0f6`).

**Stranded-QA bookkeep wave:** many already-approved actives closed to
`done/M8/` (among them BL-748, BL-1085, BL-1020, BL-534, BL-695, BL-668,
BL-784, BL-1023, BL-1084, BL-754, BL-779, plus mistaken re-promote
undoes BL-888 / BL-1000 / BL-1001 / BL-1027 / BL-1007).

## Delivery metrics (trends)

- **Avg delivered cost/ticket:** ~$8.13 (slightly down vs ~$8.31 prior).
- **Suite duration:** ~402 min on 2026-08-27 (up vs ~366 on 08-26) —
  still climbing; watch.
- **Chases:** 565 (down vs 749 prior) — less thrash after handoffd
  recovery, still elevated overnight.

## Agent / pipeline business

- **Pack:** `cursor-forge` (hard `coder` + easy `coder@cursor2`).
- **`main` sync:** `proceed` (`ahead>0` local bookkeep not pushed;
  `behind=0`).
- **Active (~27):** heavy pilot / BL-718 follow-ons (**BL-726**,
  **BL-739**, **BL-744**, **BL-745**, **BL-751**, **BL-756**,
  **BL-757**), disaster-recovery (**BL-1171**), agent-memory
  (**BL-1177**), briefing velocity (**BL-1184**), console tree
  (**BL-592**), CRAP tracker (**BL-428**), plus trend tickets still in
  flight.
- **Paused awaiting human approval (3):** **BL-554**, **BL-584**,
  **BL-974** — do not promote.
- **Ambulance:** engaged on BL-1174 through the morning; auto-released
  on close. Early-release asks from coder seats refused (policy:
  human or `done/` only).

## Optimizer recommendations

1. **Process / code:** re-mint or restore **Work-note `task:` attribution
   for BL-1001** (the BL-1185 hitchhiker died in a tip rematch). Until
   then, hard seats `:defer-better-fit` whenever easy is idle — ops pin
   of easy `in_process` is a brittle workaround, not a fix.
2. **Human:** `approve BL-554` `BL-584` `BL-974` when ready.
3. **Watch suite duration** (~402 min) — investigate standing reds /
   property-suite cost (BL-1175 still active).
4. Keep handoffd starts **fully detached** (`setsid` / outside agent
   shells) — Cursor tool sessions were reaping `nohup` children.
5. Prefer **by-id promote of UNDISPATCHED** tickets; auto-pick keeps
   hitting already-done paused YAML (BL-1097 refuse storms).

## Process hiccups (and recovery)

1. **Missed briefing ceremony ~05:25–06:00 BST:** handoffd parse-broken;
   no auto compose/send. Catch-up briefing is this file; email watcher
   reconfigured to send on commit.
2. **handoffd flap:** NULs in `handoffd-supervisor.log` → freshness
   `no-heartbeat-line` restart loop; rotated logs. Separate deaths when
   started from the agent shell — restarted via `setsid` +
   `start_handoff_daemon.sh`.
3. **Ambulance BL-1174:** held non-patient mail (incl. BL-726) until
   close; QA bounce once for BL-1185 hitchhiker, then rematch pass.
4. **Hard-seat starvation:** Work notes cannot carry `task:` → nil
   mutation_cost → hard defers to idle easy — pinned easy busy twice
   (BL-1174 morning; again ~09:34).
5. **`core.bare=true` mid-turn** on shared master — reset before git
   ops; several promote/close retries.
6. **Open-slot / promote noise:** freshness HOLD on BL-568 (stale
   Expedite premise); depends_on blocks on memory/BoB children until
   BL-1177 lands.

## Asks

1. Restore a durable fix for Work-note → mutation_cost attribution
   (reopen/re-mint the BL-1185 class).
2. `approve BL-554` `BL-584` `BL-974` when ready.
3. Confirm suite-duration climb is acceptable vs standing property
   reds.
4. Prefer not to ask for early ambulance release after coder stage —
   wait for `done/` (or human).
