Coordinator daily briefing — 2026-09-07

> Written by documenter at the coordinator's explicit `note` instruction
> (2026-09-07), covering the period since `2026-09-06.md`'s cutoff
> (2026-09-06 05:28 local). 14 tickets closed to `backlog/done/` in that
> window; grouped by theme below. Ticket titles and closure timestamps are
> read directly from `backlog/done/` and `git log`, not recalled.

## Business features delivered

**Art Director epic (BL-1417) reaches a shipped landing path.** BL-1444
gives the Art Director's `docs/design/` tip a real route onto `main`
through QA (human ruling B, 2026-09-06): a pre-merge-commit guard now
refuses the land unless the tip's own content stays inside `docs/design/`
plus its own evidence, content carried in from an already-landed `main` is
exempt by provenance, and no other worktree's main-sync merge is judged by
the guard. The mechanism shipped in three parts this window — the guard
itself, its wiring into the two hand-listed merge-chain test fixtures
(`test_pre_merge_commit_hook.sh`, `test_pipeline_code_on_main_guard.sh`),
and a hardener pass that covered `resolve_landed_main`'s previously-untested
`origin/main` preference — plus a documenter pass recording the seat's
main-landing path in the Art Director how-to. BL-1437 certifies the model
the seat runs under (Model Steward scorecard, seed row, report artifact),
closing the gap that had forced the full-forge pack to launch only under
`PACK_STAFFING_SKIP_GATE=1`.

**Coordinator activity feed (GH-24) landed, then had to be defused the same
day.** The feed itself shipped as planned — coordinator activity surfaced
as a compact live feed on its Telegram topic — but its handoffd sweep
turned out to kill the whole swarm roughly every 5 minutes: with no cursor
file yet written it selected all 6463 sent handoffs plus 500 commits and
posted one `bb` subprocess per trace with no bound, overrunning the
supervisor's 225s sweep budget and triggering BL-144's halt-swarm path.
Four full swarm deaths were measured overnight (21:52:19Z, 21:59:42Z,
22:04:22Z, 22:10:32Z) before the sweep was made config-gated and defaulted
OFF as an immediate hotfix. **BL-1454 (critical, still open)** is the real
fix — seed at the newest trace on first run, post a capped batch inside
its own deadline, and persist the cursor after every successful post — see
Operator watch below.

**Smaller fixes:** BL-1409 (a wiring assertion still checked the pre-BL-1252
direct hook name, so BL-570's feature had read 0/7 and the drift-guard
suite red at case 07 since 2026-08-30 with the guard actually wired
correctly the whole time), BL-1265 (the `operator_runtime.bb` fixture
dependency list drifted a seventh time — four undeclared transitive
load-file deps, guard red since BL-944), BL-1424 (a commit that adds a
test file under `swarmforge/scripts/test/` with no `suite-manifest.tsv`
row is now refused at commit time, not just at a parcel's `git_handoff` —
closes the gap a 2026-09-02 hand-commit slipped through, leaving the
standing suite refusing every run for three days unnoticed), BL-1426
(`post_qa_branch_sweep.bb`, which had never parsed since its birth commit
and duplicated what BL-1421's fact supplier now does, is retired along
with its shell shim), BL-1438 (BL-1432's QA-branch re-point after a land
shipped with no live caller — `land_main_publish.sh` now invokes it on
every land and prints the outcome), BL-1442 (a briefing-email list item
opening with a ticket id now renders that id in bold), BL-1443 (a property
test's tree walk now tolerates a file vanishing between listing and read
instead of failing on a sibling run's transient fixture), BL-725 (the
standing host-agent Telegram topic is named "Host", not "Cursor Remote").
BL-1226 and BL-1412 and BL-676 closed as scoping/reporting work (mkTmpDir
convention-gate coverage note, live spec-tree text filter, and the
done-epic backfill proposal report) rather than live defects.

## Pipeline / backlog state (as of this writing)

- **Backlog counts** (this worktree's tree, synced to `main`): 1 active, 79
  paused, 708 done, 9 held.
- **Active** (1): BL-1454 — the GH-24 activity-feed sweep fix (see above),
  `epic: swarm-reliability`, `priority: 5`.
- **Held** (9): BL-1348 and BL-1349 (fork-pool sizing to the real host —
  contention ruling pending, see Operator watch), BL-1441 (four remaining
  2026-08-19 hardening gates, deferred on host load/cooldown), BL-1446/1447
  (land-walk landed-history and replay-parcel-path guards), BL-1448 (drift
  guard suite owns its allowlist), BL-1450 (BL-968 sensitivity property
  fits a budget), BL-1452 (bounce recorders accept GitHub-seeded ticket
  ids), BL-940 (board per-ticket legend links and agent age).
- New tickets minted since 2026-09-06 and still open: BL-1455 (a re-pended
  ticket never gets a fresh Telegram approval ask — the concierge counts
  its closed, keyboard-stripped earlier ask as live and never reconciles
  one; BL-1348's contention ruling and BL-1408's re-approval have both sat
  dark since 2026-09-06 as a result).
- Human approvals recorded this window: BL-1442/1443/1444 (expedited),
  BL-1445/1446/1447/1448/1449/1450/1451/1452/1453.

## Standing-red register

32 rows, owned by 5 open tickets (BL-1408, BL-1441, BL-1445, BL-1448,
BL-1450) — every row names an open ticket, so none is throttling intake
under BL-1429, but the row count sits well above the >10 baseline that
elevates the circuit-breaker's health signal; worth a look if intake feels
slow.

## Operator watch

- **BL-1454 (critical, unresolved cause).** The config-gate hotfix stops
  the swarm-killing sweep from running by default, but the cursor
  mechanism itself is still unbuilt — re-enabling the feed today would
  reproduce the four-death night. Do not flip the feed's config flag on
  until BL-1454 lands.
- **BL-1455 / dark asks.** BL-1348's fork-pool contention ruling (A-D
  options) and BL-1408's re-approval have had no fresh Telegram ask since
  2026-09-06 because the concierge treats their earlier, now-closed asks
  as still live. Until BL-1455 ships, either needs a manual nudge to
  surface for the human.
- Full incident detail and recovery recipes for the above are already
  tracked in the working memory index (GH-24 sweep-halt entry, BL-1454
  critical-defect entry, BL-1455 re-pended-ticket entry) — this section is
  a pointer, not a duplicate.
