Coordinator daily briefing — 2026-08-08

> Written by the coordinator, covering the shift since `2026-08-07.md`
> (roughly 2026-08-07 06:30 through 2026-08-08 06:30 CEST). Continuous
> three-shift operation remained in effect (`cooldown_window_enabled: false`,
> operator directive 2026-08-04).

## Executive Summary

A heavy shift: **five tickets closed** (BL-535, BL-822, BL-839, BL-846,
BL-848), six new tickets minted, and — late in the shift — the automated
promotion path and the review-queue chase mechanism both broke under real
load at the same time. Both breaks were root-caused and worked around by
hand rather than left to fester; both now have their own tracked fix.

**BL-848** ("hotfix certification is declared, not inferred") queue-jumped in
on a repeated human directive and shipped after two QA bounce rounds
(cleaner/documenter re-passes that initially left no committed trace, then a
Gherkin-mutation gap on re-verify) — closed to `backlog/done/` by the human
directly (`7a468b4a`) rather than through the normal coordinator bookkeeping
step. While speccing it, the specifier found **two more uncommitted,
unticketed changes** sitting live in the master checkout — exactly the class
of risk BL-848 exists to catch — and minted them same-shift: **BL-850**
(Bubble reply voice was silently gated by the hold-music slider; human fix
already in tree) and **BL-851** (the bridge now serves sideload APKs
pre-auth on the public tunnel — a real security decision, not a bug, but one
nobody had ruled on). Both are now `human_approval: approved`; **BL-851's
underlying code is still uncommitted**, so it continues to block
`build_freshness_cli.bb sync` (see *Flagged*).

Late in the shift, with BL-848 mid-verification, the coordinator sent a
priority-00 note flagging that the chase sweep was ignoring an engaged
**ambulance hold** — a mechanism that deliberately parks a ticket's parcel in
`inbox/new/` while a human decision is pending. The specifier confirmed and
specced it same-hour as **BL-852** (`severity: high`, "chase sweep never
read the ambulance marker"): the sweep had already chased the ambulance-held
BL-722 dispatch handoff **past its `maxChases` threshold**, one sweep away
from a forced respawn or dead-letter of stuck work. The coordinator promoted
BL-852 by hand, overriding an **orthogonality gate false positive** (it
refused on a shared `epic: swarm-reliability` tag with active BL-848, but the
two tickets' file scopes don't overlap at all — verified before overriding)
and documented the override in the promotion commit. The specifier has since
split that gate bug out as its own ticket, **BL-854**.

The coordinator also found the promotion path **itself refusing every
candidate**: `promote_and_route_next.sh` resolves the effective depth cap
(`-1`, meaning unlimited) down to a hardcoded library default of `5` because
it hands the wrong argument shape to `backlog_depth_cli.bb`. With active
count already at 5, every promotion — expedited or not — was refused with
exit code 2. The specifier root-caused and specced this as **BL-853**
(`severity: high`, human-approved, expedited), which the coordinator also
promoted by hand per the specifier's own "promote by hand" instruction, since
the sanctioned path can't currently promote its own fix.

Separately, the coordinator caught and reverted its own mistake: an
automated promotion pick landed **BL-826** (Bubble hands-free echo-loop) in
`backlog/active/` despite the standing 2026-08-07 Bubble-speech
deprioritization hold — the hold lived only in `backlog/paused/` and
depended on the coordinator remembering not to promote it, which failed on
the very first automated pick after the hold was set. Reverted within the
same check, and BL-826 plus its siblings BL-844/BL-845 were moved into
`backlog/hold/`, which is never auto-promoted — making the hold mechanical
rather than a memory obligation.

**BL-773** and **BL-819** are still active, both past a QA bounce-and-refix
round (lost documenter/hardener passes, a shared `mkdtemp` helper fix) and
re-verified clean through cleaner → architect → hardener this shift;
BL-819 (with sibling BL-822/BL-839, both now closed) has a fresh documenter
pass and is expected at QA imminently. BL-773 needed no re-documentation
(already covered by an earlier pass) and should be at or near QA as well.

## Shipped today

- **BL-535** — closed by the human directly (batch commit `12d16228`),
  clearing one of the four long-stale `backlog/hold/` items.
- **BL-822** — resource-anomaly detection now reads host load, not only
  per-role RSS/CPU trends. Closed to `backlog/done/`.
- **BL-839** — master-checkout-vs-`main` drift detector for daemon-executed
  scripts. Closed to `backlog/done/`.
- **BL-846** — root-caused and fixed the stuck-QA-pane deadlock
  (`resolveRolePaneTarget` reading a `roles.tsv` session mono-router never
  creates). Closed to `backlog/done/`.
- **BL-848** — hotfix-certification recurring check: an operator/hand hotfix
  now can't count as an official swarm deal until stamp-off review + human
  ask. Two QA bounce rounds, then approved (`f28a84ad`), closed by the human
  (`7a468b4a`).

## Still in flight

- **BL-773** — coordinator asks through the role-ask path. Post-bounce
  fix (shared `mkdtemp` helper) re-verified clean through hardener; already
  documented from an earlier pass. Expected at QA next.
- **BL-819** — ticket-lifecycle ledger. Same bounce-fix round as BL-773;
  fresh documenter pass landed this shift (alongside BL-822/BL-839); expected
  at QA next.
- **BL-722** — pilot-safe defects. Promoted and routed to coder this shift;
  its dispatch handoff is the one the chase-sweep-ambulance bug (BL-852,
  below) chased past `maxChases`.
- **BL-847** — resource sampler measures the pane's root shell instead of
  the agent process (explains why `resourceAnomalies` reads empty even
  during today's respawn spike — see *Optimizer notes*). Promoted, routed to
  coder, and picked up already (`f776bbc6`).
- **BL-852** — chase sweep now must respect the ambulance hold. Promoted by
  hand past an orthogonality false positive; routed to coder.
- **BL-853** — promotion path must honor the unlimited-depth sentinel.
  Promoted by hand (sanctioned path can't currently promote anything);
  routed to coder.

## New tickets this shift

- **BL-847** — resource sampler measures the pane shell, not the agent
  process (already shipped, see above).
- **BL-848 → BL-851** — hotfix-certification epic and its first three
  ledger entries: BL-849 (Darwin orphan-janitor stamp-off, approved), BL-850
  (Bubble reply-volume stamp-off, approved), BL-851 (bridge sideload-APK
  pre-auth route — a real security decision, approved, code still
  uncommitted).
- **BL-852** — chase sweep ignores the ambulance hold (shipped this shift,
  see above).
- **BL-853** — promotion path mishandles the unlimited-depth sentinel
  (shipped this shift, see above).
- **BL-854** — split from BL-853's report: the orthogonality gate should
  advise, not hard-block, on a shared epic tag alone. `human_approval:
  approved`, not yet promoted (behind the broken promotion path it exists to
  soften — no urgency now that BL-853 is in flight).

Backlog root drained to empty again this shift; two intakes surfaced and were
minted directly into the BL-848 hotfix-certification ledger rather than sitting
at root (see Executive Summary).

## Approvals granted this shift

- **BL-849, BL-850, BL-851, BL-854** — all `human_approval: approved`.
- **BL-535** — resolved by direct human close, not a promotion+approval cycle.
- Epic priority rewrites: **BL-818** and **BL-824** both made top-priority
  within their epics this shift (human/epic-reorder commits); no ticket-level
  action needed from the coordinator beyond noting the reorder.

## Pipeline state right now

- `active_backlog_max_depth` effective: **-1 (unlimited)** per
  `swarmforge/packs/mono-router.conf` — but see *Flagged* item 1: the
  promotion path currently can't act on that correctly without BL-853.
- `backlog/active/`: **6** — BL-722, BL-773, BL-819, BL-847, BL-852, BL-853.
- `backlog/paused/`: **205** tickets.
- `backlog/hold/`: **6** — BL-548, BL-557, `INTAKE-phone-wire-format-and-
  offline.md` (all three now untouched for a **sixth shift** running, one
  shift after BL-535 finally cleared the fourth long-stale item), plus
  BL-826/BL-844/BL-845 (moved here this shift from `paused/` to make the
  Bubble-speech deprioritization hold mechanical rather than memory-based).
- `backlog/` root: **empty**.
- `build_freshness_cli.bb sync` **REFUSED** as of this writing: uncommitted
  change under the deployed surface (`extension/src/bridge/bridgeServer.ts`,
  BL-851's pre-auth route). All six tracked daemons (`bridge`, `bot`,
  `front_desk_supervisor`, `handoffd`, `handoffd_supervisor`,
  `operator_runtime`) remain **stale** against `main`. Not overridden —
  see *Asks*.

## Flagged, not yet resolved

1. **The automated promotion path refuses every candidate right now.**
   `promote_and_route_next.sh` resolves an unlimited (`-1`) depth cap down to
   a hardcoded default of `5` (wrong argument shape passed to
   `backlog_depth_cli.bb`), and active count is already at that number. Every
   promotion since — BL-852, BL-853 itself — was done by hand, by-name,
   bypassing the auto-pick path. **BL-853** is the tracked fix, in flight
   now. Until it ships, treat `promote_and_route_next.sh`'s auto-pick mode as
   unusable; by-name promotion still works but every gate refusal needs a
   human-legible reason check before overriding (see item 2).
2. **The orthogonality gate false-positives on shared epic tags.** Refused
   BL-852 against active BL-848 despite zero real file overlap. Overridden
   once, with the reasoning recorded in the promotion commit; **BL-854**
   (approved, unpromoted) is the tracked fix — advise instead of hard-block
   on epic-tag-only matches.
3. **BL-851's pre-auth route is approved but still uncommitted**, so
   `build_freshness_cli.bb sync` stays refused and every long-lived daemon
   stays stale. BL-848's own hotfix-certification-sweep is therefore **not
   confirmed live in production yet** — the exact "green ticket over inert
   code" risk BL-328/BL-848 exist to prevent. Needs either a proper commit
   through the pipeline or an explicit human call to override the sync gate.
4. **`backlog/hold/`'s pre-existing items (BL-548, BL-557, phone-wire-format
   intake) are untouched for a sixth shift running.** BL-535 (the fourth
   long-stale item) finally closed this shift, so at least one is moving;
   the remaining three have had no signal either way.
5. **A stale ambulance hold blocked mailbox delivery mid-shift.** The
   BL-848 ambulance marker stayed engaged after the ticket closed (closed by
   the human directly, not through the release path), silently holding a
   fresh priority-00 specifier note. Released by the coordinator once
   confirmed stale — third occurrence of this exact pattern (after BL-835
   and an earlier incident); worth a ticket if it recurs a fourth time.

## Optimizer notes

- **Respawns spiked to 120 today** (down from a prior-period 166, but this
  reading is almost certainly the BL-852 ambulance-chase bug: the
  BL-848 ambulance was engaged roughly 20:59Z–03:10Z, and every chase-sweep
  cycle in that window against the held BL-722 handoff would have counted as
  a forced respawn once past `maxChases`. Expect this number to drop sharply
  once BL-852 ships).
- **Chases (21) and nudges (2)** both read low — consistent with the
  post-BL-848 wind-down, not masking anything.
- `failedDeliveries` and `daemonRestarts` both **0** — the stale-daemon
  problem (item 3 above) is a "never restarted," not a "restarted and
  crashed," situation.
- `resourceAnomalies` reads **empty** even across today's respawn spike and
  the two QA bounce rounds — this is very likely **BL-847**'s own bug
  (sampler measures the pane's root shell, not the agent process), now
  in flight. Treat an empty anomalies list as uninformative until BL-847
  ships.
- `topExpensiveTickets`: **BL-826 ($76.67, carried from yesterday, now on
  `backlog/hold/`)**, **BL-848 ($43.58, new — two bounce rounds explain the
  cost)**, BL-761 ($33.30, carried), **BL-773 ($28.14, new — its own bounce
  round)**, BL-805 ($27.56, carried). Nothing here reads anomalous once
  matched to bounce activity.
- No throttle recommendation active. The effective depth cap reads `-1`
  (unlimited) but is currently unenforceable correctly by the promotion path
  (item 1 above) — this is a mechanism bug, not a health-based throttle
  decision, and should not be read as a circuit-breaker signal.

## Asks

1. **Human: commit or rule on BL-851's code.** The ticket is approved, but
   the pre-auth sideload-APK route itself is still sitting uncommitted in
   `extension/src/bridge/bridgeServer.ts`, blocking every daemon restart.
   Either land it through the normal pipeline (stamp-off review, per the
   ticket) or explicitly authorize a `build_freshness_cli.bb sync --override`
   so BL-848's certification sweep actually goes live.
2. **Human: sanity-check `backlog/hold/`'s three long-stale items** —
   BL-548, BL-557, phone-wire-format intake — untouched for a sixth shift.
   BL-535 (the fourth) finally cleared this shift; worth confirming whether
   the same is now true for the rest.
3. **Human/specifier: confirm the two coordinator gate overrides were the
   right call** — promoting BL-852 past the orthogonality false positive,
   and hand-promoting both BL-852 and BL-853 past the broken depth-cap
   fallback. Both are documented in their promotion commits with the
   reasoning; flagging for review since overriding a gate is not something
   the coordinator does routinely.
4. Coordinator (self): once BL-853 ships, re-run `promote_and_route_next.sh`
   without a by-name argument to confirm auto-pick mode is healthy again
   before relying on it for the next promotion.
