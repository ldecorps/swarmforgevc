Coordinator daily briefing — 2026-09-11

> Composed by the documenter per human ruling A (2026-09-07): the morning
> briefing is documenter-owned agentic work, not coordinator-owned.
> Covers the period since `2026-09-09.md`'s cutoff (commit `891e6d0a4d`).

## The story of this window: a provider outage and recovery, standing-red discharge continues

On 2026-09-09 the swarm hit a genuine weekly Claude usage limit. The human
mitigated it directly on `main`: a provider-exhaustion switch moved the bob
pack to all-GLM/b.ai (`7715954e26`), a brief Anthropic-compat trial for the
coder was wired and then reverted back to the proven aider path
(`f8c202688e`, `09fb7b59de`), and the coordinator was moved back onto a real
Anthropic seat once the b.ai gateway proved it blocked the claude agent
entirely (`f233d30829`, `f86be42f7a`). Four hotfixes landed during the
fallback window (wake-dedup fresh-target, b.ai base-follows-key, landed
auto-close, respawn bootstrap) and were later stamped as their own tickets
(BL-1504, BL-1506, BL-1507, BL-1508).

Claude seats were confirmed restored and producing again at 06:09:58Z on
2026-09-10 (the pack relaunched onto `anthropic-mono-router`, and new
commits/parcel fan-out from claude seats were observed within the hour) —
the previously-recorded 2026-09-15 reset date is dead. The rest of the
window returned to the standing-red register discharge sweep that has
driven most of the past several days' throughput.

## Other defects closed this window

Thirteen tickets closed since the previous briefing:

- **BL-1410** — the fixture-root migration (last briefing's single active
  ticket) landed: 17 acceptance step handlers now take their fixture root
  from the steps-lane helper instead of leaking scratch directories.
- **BL-1278** — the Telegram approval card shows the ticket description, not
  the mint notes.
- **BL-1441** — the four remaining 2026-08-19 hardening gates completed
  their Stryker runs once their cooldowns cleared.
- **BL-1449** — `OPERATOR_RUNTIME_BB_FILES` is now derived from the BL-944
  closure walk at load time instead of hand-retyped (drifted eight times
  before this fix).
- **BL-1468, BL-1488** — two more hardening-debt ledger rows discharged once
  their mutation cooldowns cleared (`backlogTicketId.ts`/`bounceArgsCore.ts`/
  `qa-sibling-check.ts`, and the BL-1476 file set).
- **BL-1478** — handoffd's four compiled-tool sweeps now log a sweep-failure
  line (tool, exit code, first stderr line) instead of going silent on
  non-zero exit — this is what let the BL-1477 context-telemetry outage run
  eight days unlogged.
- **BL-1494** — a note sent as deferred (`wake: defer`) no longer gets woken
  anyway by the delivery hop; this had been costing cleaner a merge-up turn
  every three-to-five minutes in bursts.
- **BL-1497** — the commit-integrity checkout lock now reaps a lock whose
  owner is dead or whose record is past an age bound, never a live one —
  fixes the 2026-09-08 incident where a mid-commit respawn left an orphaned
  lock blocking every shared-checkout writer.
- **BL-1501** — `tempDirTrapGuard.test.js`'s real-tree scan is green again;
  the two 09-09 hotfix runners now adopt the sibling shutdown-hook sweep
  instead of leaking scratch directories.
- **BL-1505** — a chase attempt now counts whether or not its wake text was
  actually injected, fixing an unreachable respawn/dead-letter path for a
  pane that ignores an unchanged-mailbox wake.
- **BL-1514** — three acceptance features asserting the per-tick
  `AGENT_EXITED` producer that BL-653 retired on 2026-08-26 are retired,
  not reworded (red for 15 days, hidden by the per-feature runner).
- **BL-1515** — the coder's worktree had silently drifted onto branch `side`
  since 2026-09-03; `ready_for_next.bb` now runs a pre-turn branch-identity
  guard that repairs the one provably-safe shape and refuses fail-closed on
  every other mismatch. (See the operator-watch note below — this class of
  drift recurred once more after this ticket landed.)

## Standing-red register: 34 rows / 21 tickets (was 29 / 18)

The register grew by five rows and three tickets. This is continued
detection, not new regression — the same systematic sweep (fixtures
inheriting `PACK_STAFFING_SKIP_GATE=1`, hand-copied hooks drifting from the
real guard chain, and now a `BL-1367` property-oracle whitespace flake and
the daemon's spawn-target closure work) keeps surfacing hidden reds as the
2026-09-08 specifier sweep's tickets (BL-1482 through BL-1500) work through
the pipeline. No row in the register currently names a closed or absent
ticket.

## New this window

Twenty-seven tickets are currently paused and one (BL-1518) is active,
minted mostly from three sources:

- **BL-1502, BL-1503, BL-1526, BL-1527, BL-1529, BL-1530** — new
  standing-red register rows, each naming its own owning ticket.
- **BL-1504, BL-1506, BL-1507, BL-1508** — stamp tickets for the four
  hotfixes landed during the 09-09 provider-exhaustion window.
- **BL-1509, BL-1510, BL-1511, BL-1512, BL-1513** — front-desk/pack-recovery
  follow-ups from the outage (Telegram document posting, model-scoring
  table as an attachment, restoring a specifier seat to the bob pack, a
  diversified fallback pack, and cutting the router note).
- **BL-1515, BL-1516, BL-1517** — the branch-identity drift cluster: the
  guard itself (closed this window, see above), a fixture-root proof, and a
  project-root argument check.
- **BL-1518** (active) — a unit test under mutation was shelling the real
  `swarm_handoff.bb` with the coder's live cwd/role, delivering fake
  priority-00 traffic (a retired human directive's phrasing) into the
  specifier's live inbox on every BL-1441 mutation run; the CLI now refuses
  a draft outside its resolved project root.
- **BL-1519, BL-1520, BL-1521, BL-1523** — first-run mutation-debt epic and
  its slices (Telegram/negotiation survivors-to-zero, transcript-walker
  coverage).
- **BL-1524, BL-1525** — daemon spawn-target closure and banned-API-debt
  sweeps.
- **BL-1528** — an undeliverable closing-ceremony note is recorded as a
  failed run, not a pending one.
- **BL-1531, BL-1532** — the Telegram approval ask now explains each ruling
  option (lettered options with `ruling_tradeoffs`, context trimmed to
  Telegram's limit, lettered buttons) and a typed `approve` names its
  ruling by letter. These drain the 2026-09-11 pros/cons operator directive;
  both are human-approved and awaiting promotion.

## Backlog snapshot

- `backlog/active/`: **1** (BL-1518 — the mutation-injects-live-handoff fix)
- `backlog/paused/`: 116
- `backlog/hold/`: 0
- `backlog/done/`: 1,329 total (+13 since 2026-09-09)

## Operator watch items

- **Launch can still boot the resident as a second coordinator** when the
  mono-router active-role marker reads `coordinator` (recurred 2026-09-11
  05:05Z, after BL-1515 landed) — verify any launch by process tree, not
  `./swarm status`; fix with `rotate_to_role.sh coder`. Still open.
- **`cron` has been dead on this WSL host since 2026-08-30** — nothing
  scheduled fires (including the 09:00 day-shift start) until a human runs
  `sudo service cron start` with an interactive password.
- **The active pack (bob-multi-provider-mono-router.conf) may still have no
  `specifier` row in `roles.tsv`** depending on which pack is live —
  confirm before routing any note to `specifier`; a hotfix-stamp or
  spec-gap note is structurally undeliverable otherwise.
- **Approval asks pane-typed rather than tapped can sit unsubmitted** and
  masquerade as a starved swarm — probe with `send-keys -l` before
  escalating.
- **Two Telegram/icon-shaped surfaces exist and are not interchangeable**
  (static backlog PWA vs. the live holistic UI); confirm which one a new
  request means before speccing (BL-1509/1510 territory this window).
- Claude-seat capacity is confirmed restored and producing since
  2026-09-10 06:09:58Z; a `no claude process` escalation is a real signal
  again, not fallback noise.
