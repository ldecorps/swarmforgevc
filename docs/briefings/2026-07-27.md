# Daily Briefing — 2026-07-27

> Written by the COORDINATOR session directly (not the documenter role) —
> this was a long, human-driven operator session spanning the evening of
> 2026-07-26 into the early hours of 2026-07-27, covering schedule changes,
> multiple shipped tickets, several live incidents, and a backlog reshape.
> Numbers below are cited from `2026-07-27.json` (the real sidecar,
> generated ~04:30Z) where available; everything else is drawn directly from
> this session's own actions (git history, live pane checks, handoff logs).

## Executive Summary

Four tickets shipped tonight: **BL-648** (relaunch orphan-claim fix),
**BL-672/673/674** (epic reorder "make top priority" button + topic
drill-down), and **BL-655** (ambulance mode slice 1 — the hold). **BL-684**
(rename Onboarding Facilitator → **Onboarder** across 49 files) is in flight
now, deep in hardening.

The schedule changed: the swarm now runs continuously **17:00→09:00** (an
operator-requested "evening shift" merged with the existing night window,
since nothing differs between them), daytime pause **09:00→17:00**.

A significant governance moment: **BL-680/681** grant the specifier
explicit consolidation authority (merge/split tickets) with a ratified
constitutional guarantee (Article 5 — "a consolidation never drops a human
sentence"). The specifier used that authority for real tonight: it split
**BL-684** out of BL-643 to fix an unsatisfiable acceptance criterion,
corrected BL-590's stale `assigned_to: coder` to `cleaner` (its slice 1 is
already on `main` — this resumes review, not a rebuild), and refused two
other proposed consolidations with recorded reasoning.

Two real live incidents were caught and fixed, not just false alarms: a
`vibe config` subprocess hung for 74+ minutes at 99% CPU, freezing the
entire mono-router resident (killed, recovered in seconds); and the
resident twice finished a role's work and went idle without rotating
onward, stranding real backlogged work for up to an hour until manually
rotated (`rotate_to_role.sh`). Both are now filed as intakes for a real
fix, not just patched by hand each time.

## Pipeline State

- `active/`: **BL-684** only (rename), in hardener as of this writing —
  mutation-testing the renamed modules, real progress each check (new
  commits, new uncommitted diffs, live CPU) across four consecutive
  babysitter "stale in_process" alerts tonight, all confirmed false.
- `done/` today: BL-648, BL-672, BL-673, BL-674, BL-655.
- `paused/`, queued in order behind BL-684: **BL-590** (Onboarder slice 1,
  resumes at cleaner — review only, not rebuild) → **BL-624** (survey→
  contract) → **BL-625** (prompts→launch handoff), with **BL-643** (class
  docs) any time after BL-684. All five re-certified for `required_stages`
  (BL-606) this session — none had been touched by the earlier retrofit
  sweep (BL-590 was excluded because it sat in `hold/`, not `paused/`, at
  sweep time; BL-624/625/643 simply weren't caught).
- **BL-682** (Mistral Vibe → Intelligence Layer wiring) and the **qwen-code**
  onboarding intake both explicitly de-prioritized behind the Onboarder
  work per operator direction (BL-682: `priority 01→20`; qwen note
  resequenced to `p21`).
- **BL-590 unparked** this session: both conditions from its 2026-07-25 park
  (BL-633 shipped, BL-635 shipped, live-checked via
  `grep -ic invariant specifier.prompt` = 6, `grep -c record-bounce
  architect.prompt` = 2) were satisfied and verified live before moving it
  `hold/` → `paused/`.

## Flow Metrics (sidecar 2026-07-27.json, generated ~04:30Z)

- closed/day: **1** (BL-655 — the sidecar's day boundary is UTC and most of
  tonight's other closes/specs landed before UTC midnight, so they count
  toward 2026-07-26's figures instead).
- chases: **7** (down from 125 the prior day) · nudges: **10** · respawns:
  **0** · failed deliveries: **0** · daemon restarts: **0**. A materially
  calmer night than the trend line — consistent with the two rotation-gap
  fixes landing manually as they occurred rather than needing daemon
  recovery.

## Blockers & Risks

- **Babysitter false-positive on "swarm STARVED"**: two confirmed root
  causes, both filed (`INTAKE-babysitter-starved-false-positive-narrow-pane.md`).
  (1) The busy-detection grep (`esc to interrupt`) gets truncated off the
  swarm's 80-column tmux panes whenever the footer is crowded with other
  hints — confirmed live, `capture-pane -S` controls scrollback depth, not
  width, so it cannot fix this. (2) The `-mmin -120` staleness window on
  `in_process` handoffs excludes a genuinely-still-active claim once it
  passes 2 hours old (happened tonight: a hung `vibe config` process
  delayed real hardening work well past that window). Fired twice tonight
  as a genuine positive (the vibe hang) and at least six times as a
  confirmed false positive after.
- **Resident can finish a role's work and never rotate onward**
  (`INTAKE-resident-turn-ends-without-self-rotation.md`): observed twice —
  specifier finished all queued work and sat idle at a prompt for ~30+ min
  while hardener's real batch waited; same pattern recurred with coder→QA.
  Manually fixed both times via `rotate_to_role.sh`; not yet a real fix.
- **Local/origin `main` divergence** caught twice when QA landed its own
  approved commits directly to `origin/main` while this session's local
  `main` pointer lagged behind. Both times verified safe
  (`merge-base --is-ancestor`, confirming no commits were at risk) and
  fast-forwarded cleanly — never a force-push, never lost work — but worth
  normalizing a `git fetch && ff-only` step before every bookkeeping pass
  rather than discovering the lag reactively.
- **`.gitkeep` miscounted as an active ticket** in `swarm_handoff.sh`'s
  depth warning (already filed, BL-683, low severity) — cosmetic, but noisy
  in every handoff log tonight ("active=2, max=1" when the real count was 1).

## Governance / Backlog Process Changes

- **BL-680 (specifier consolidation authority)** and **BL-681 (Article 5
  amendment, ratified)** both landed tonight: the specifier may now merge N
  intakes into one ticket, split one into N, or consolidate an epic's orbit
  when it goes top-priority — bound by a constitutional guarantee that no
  human-quoted directive is ever dropped in the process.
- **BL-643 naming ruling**: "Onboarder", not "Facilitator", not a split
  vocabulary. Full rename (BL-684) ordered to land before any further
  Onboarder documentation or slices, to stop the word from spreading
  further (36 tracked files on 2026-07-25 → 49 by the time BL-684 was
  specced today).
- **BL-545 (Swarm Intelligence Layer epic) bookkeeping fixed**: the
  Onboarder pillar (BL-624/625/643, now also BL-590/684) had been tagged
  `epic: swarm-intelligence-layer` but never backfilled into the epic's own
  `decomposes_into` or PILLARS list — under-reporting its own orbit. Fixed
  in two passes (coordinator, then the specifier's own reshape).

## Swarm Optimizer Recommendations

1. Fix the babysitter false-positive class before it erodes trust in the
   alert — a real future starvation risks being waved off if this keeps
   firing on healthy work. Both root causes are diagnosed and filed; this
   is a small, well-scoped fix.
2. Once BL-684 lands, promote **BL-590** next — it's a review-only resume
   (slice 1's 1929 insertions are already on `main`), not new build work,
   so it should move fast relative to its ticket size.
3. Consider normalizing `git fetch origin main && git merge --ff-only`
   as a standing first step of every QA-approval bookkeeping pass, given
   it's now happened twice that local `main` silently lagged origin.

## Coordinator Action Log (evening/night)

- ~17:00: swarm start (new continuous 17:00→09:00 window), cooldown pause
  flipped to 09:00→17:00 in `swarmforge.conf`.
- ~20:00–21:00: BL-648/672/673/674 progressed coder→cleaner→architect
  (one real bounce on BL-672, "make top disabled at epics-index-0", fixed)
  →hardener→documenter→QA, approved and merged (`90a34930d`), closed.
- ~21:00–23:00: BL-655 promoted, specced, built (ambulance mode slice 1 —
  Telegram Control topic verbs, `ambulance_lib.bb`), reviewed, hardened,
  documented, QA-approved (`23ca831b6`), closed. BL-684 promoted and
  routed to coder immediately after.
- Live incident (~21:15–21:46): `vibe config` hung at 99% CPU for 74+
  minutes during the specifier's Mistral research, freezing the resident;
  killed, specifier resumed within seconds.
- Live incident (~21:57, ~22:29): resident finished specifier work twice
  without self-rotating, stranding hardener's claimed batch; both times
  fixed via `rotate_to_role.sh hardender`.
- ~22:00–23:00: backlog reshape session — BL-590 unparked, BL-643 split
  (BL-684 carved out), BL-624/625 requeued, BL-545 epic bookkeeping fixed,
  all five re-certified for `required_stages`. BL-681 constitutional
  ratification recorded. Mistral (BL-682) and qwen-code intake
  de-prioritized behind this work per operator direction.
- Throughout: multiple babysitter "swarm STARVED" / "stale in_process"
  alerts investigated; one genuine (the vibe hang), the rest confirmed
  healthy and now traced to two filed root causes.
