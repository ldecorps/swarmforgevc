id: BL-1193
title: "Deprecator freshness gate: retired-token extractor must name the item the RETIRED marker actually retires, not a co-occurring word"
milestone: M8
type: defect
severity: high
status: todo
epic: deprecator
priority: 40
direction: swarm-reported
depends_on: [BL-1173]
assigned_to: coder
mutation_cost: high
slice_size_envelope: low
size_envelope_decision: "justify not split - one token extractor narrowed in one CLI, 2 scenarios, no new surface. Band declared because mutation_cost: high on this ticket is Article 3.6 seat-tier (deprecator judgment needs a hard-tier seat, BL-1001), not a size estimate. Same cause and same decision as BL-1267 and BL-1268."
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-1193-deprecator-retired-token-must-name-the-actual-retired-item.feature
human_approval: approved
approval_context: >
  Discovered adjudicating BL-1190's Article 3.6 freshness HOLD (2026-08-27).
  `node extension/out/tools/deprecate-check.js . BL-1190` returned
  `{"decision":"hold","reason":"stale premise: depends_on done but
  description still names retired surface(s): Mint"}`. Traced to
  docs/how-to/BL-1095-retire-the-expedite-lanes-legacy-bug-type.md:21 —
  `| Mint hygiene (\`backlog_hygiene_lib.bb\`) | \`type: bug\` -> \`RETIRED-TICKET-TYPE
  ... use type: defect\` |` — whose RETIRED marker actually names `type: bug`,
  not "Mint". deprecate-check.js's loadRetiredTokens verb regex
  (`\b([a-z][a-z0-9_-]{2,})\b.*\bRETIRED\b`) captures the first a-z word on
  the line, not the word adjacent to/naming the marker, so any unrelated
  ticket whose description merely contains the common word "Mint" (this
  project's own vocabulary for creating a ticket — "mint a ticket",
  "mint-time gate") gets a false HOLD. BL-1190 confirmed-promote past this
  false hold (see its notes:); this ticket fixes the extractor itself so the
  Article 3.6 gate — mandatory before EVERY promotion — stops false-blocking
  any ticket that happens to use common vocabulary. Needs human sign-off per
  the standard human_approval gate before promotion; not an emergency
  queue-jump.

description: |
  ## What

  `deprecate-check.js`'s `loadRetiredTokens` scans `docs/` for lines matching
  `RETIRED`, then extracts a "retired token" via
  `/\b([a-z][a-z0-9_-]{2,})\b.*\bRETIRED\b/i` — the first word-like token
  anywhere earlier on the line, regardless of whether that word is what the
  RETIRED marker actually names. `collectRetiredSurfaceHits` then treats a
  bare substring match of that token anywhere in a candidate ticket's YAML
  text as proof the ticket references retired behaviour.

  This conflates "co-occurs on the same doc line as a RETIRED marker" with
  "is the thing the marker retires." A table row documenting that `type: bug`
  is `RETIRED-TICKET-TYPE` also happens to start with "Mint hygiene" (the
  ticket-minting hygiene checker's name) — an unrelated concept two columns
  away — and the regex extracts "Mint" instead of `type: bug`.

  Because "mint"/"Mint" is core vocabulary in this project (the specifier
  "mints" tickets; multiple existing tickets and gates are literally named
  "Mint-time ..."), this false-positive token can HOLD any paused ticket
  whose description happens to use the word — and Article 3.6 runs this
  check before every single promotion, fail-closed. A high false-positive
  rate on common vocabulary erodes the gate's credibility and burns
  specifier/coordinator cycles on manual adjudication for holds that are not
  real.

  ## How (direction, not mandate)

  Anchor the extracted token to the RETIRED marker's actual referent rather
  than "first word on the line" — for example, prefer a token immediately
  adjacent to (or delimited/backtick-quoted directly before) the RETIRED
  marker over one merely appearing earlier in the same line. The existing
  path-like extraction (`pathLike` regex) already anchors correctly by
  requiring a file-extension match immediately before the marker context;
  the verb-token branch needs the equivalent discipline. The fix lives in
  `extension/src/tools/deprecate-check.ts` (`loadRetiredTokens`); compile to
  `extension/out/tools/deprecate-check.js` per normal build.

  ## Constraints

  - Must not regress BL-1173's existing true-positive coverage (feature
    scenario `freshness-hold-retired-surface-02`): a ticket genuinely naming
    a retired module/verb/conf-key must still hold.
  - Fail-closed posture (Article 3.6) is unchanged — this fixes a
    false-positive extractor, not the fail-closed default.

qa_e2e_procedure: >
  Run `node extension/out/tools/deprecate-check.js . BL-1190` (or any live
  paused ticket whose description contains "Mint" but names no genuinely
  retired surface) against the current docs/ tree and confirm the decision
  is `allow`, not a false hold naming "Mint". Then run it against a fixture
  ticket whose description genuinely names `type: bug` and confirm it still
  holds with a reason naming `type: bug`.

out_of_scope: |
  - Rebuilding the freshness gate's other signals (supersede marker,
    depends_on-not-done, spec-gap bounce count) — untouched, working as
    designed.
  - Re-adjudicating BL-1190 itself — handled separately (see its notes:).

invariants:
  - "A retired-token hold's reason always names a token that is itself the marked-RETIRED referent on its source doc line, never a merely co-occurring word."

source: |
  Discovered by the specifier while adjudicating the Article 3.6 freshness
  HOLD on BL-1190, 2026-08-27 (coordinator note, priority 00).

notes: |
  - [2026-08-28] specifier, Article 3.6: CONFIRM PROMOTE. Hold reason was
    "stale premise: depends_on done but description still names retired
    surface(s): backlog_hygiene_lib.bb, Mint". Verified the opposite:
    swarmforge/scripts/backlog_hygiene_lib.bb is 18KB, live, and load-file'd by
    both backlog_epic_milestone_audit.bb and specifier_backlog_hygiene_gate.bb
    - the latter run by this role today. Nothing there is retired. The hold is
    the very loadRetiredTokens false positive THIS ticket fixes: the extractor
    grabbed "Mint" as a retired token from a doc table row whose RETIRED marker
    actually names `type: bug`. A checker holding the ticket that repairs it is
    the strongest possible argument for promoting it.
  - [2026-08-27] specifier: minted while confirm-promoting BL-1190 past a
    false hold from this exact extractor bug. depends_on BL-1173 (done) —
    Independent per INVEST once BL-1173 shipped, which it already has.
  - "[2026-08-28] specifier: a SIBLING false-positive branch, found
    adjudicating BL-1216's hold. `deprecate-check.js` also holds on `ticket
    claims superseded/retired/obsolete without a backlog/done/ closure`, and
    that branch has the same co-occurrence-is-not-subject defect this ticket
    fixes in `loadRetiredTokens`: it fires when a ticket NARRATES someone
    else's retirement rather than claiming its own. BL-1216's description
    recounts three tickets being wrongly retired by a bad resolution and was
    held for it; BL-1228 quotes the trigger words while describing this very
    branch and is held too. Both were confirm-promoted by hand. Same
    function, same shape — fold this branch into this slice if it stays
    small, or split it out, but do not let it be re-found a third time."
  Size envelope, 2026-08-29. Discharged alongside BL-1267 and BL-1268, which
  tripped BL-634's gate for the identical reason: Article 3.6 requires every
  deprecator build/run ticket to carry mutation_cost: high so BL-1001 keeps it
  off an easy seat, and the size gate reads mutation_cost as a size proxy when
  no explicit band is declared. Seat tier and slice size are different axes.
  The true band is declared in slice_size_envelope: low.

  Why not split: the change is one extractor narrowed so a RETIRED marker
  names the item it actually retires rather than a co-occurring word. Two
  scenarios, one predicate, no new surface. There is nothing to cut that
  would leave two independently valuable slices.

  - [2026-08-29] specifier, Article 3.6: CONFIRM PROMOTE, second time, same
    hold. Re-verified from scratch rather than trusting the note above.
    swarmforge/scripts/backlog_hygiene_lib.bb is 23KB and live, load-file'd by
    backlog_epic_milestone_audit.bb, specifier_backlog_hygiene_gate.bb and
    five test runners - not retired by any reading. The named integration
    point is still the live path and still carries the defect verbatim:
    extension/src/tools/deprecate-check.ts:116 exports loadRetiredTokens and
    line 133 is still the first-word-on-the-line regex this ticket narrows.
    depends_on BL-1173 is in done/. This ticket has not shipped - no step
    handler answers to it and no QA pass commit names it.
  - [2026-08-29] specifier: the sibling branch flagged in the 2026-08-28 note
    was re-found a third time today - BL-1220 was held on it this morning -
    but it must NOT be folded in here. It was split out as BL-1268, which is
    active and routed to coder. Folding now would collide with in-flight work
    and break this slice's declared size envelope. Keep the two separate.
  - [2026-08-29] specifier: note for whoever promotes this. The ticket that
    repairs the false positive is guaranteed to keep tripping it, and every
    adjudication note added here makes that worse, because explaining the
    defect requires writing the trigger tokens the extractor scans for. It
    will hold on every promotion attempt no matter how many times it is
    adjudicated. Promote it by hand. Article 3.2.4 already places it ahead of
    non-expedited work without any priority change - it is type: defect,
    severity: high - so the priority: 40 above is not what is holding it back.
