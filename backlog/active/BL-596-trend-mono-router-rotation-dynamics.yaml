id: BL-596
title: "Trend: mono-router rotation dynamics — per-persona resident dwell, rotations/day, thrash, time stranded off-home"
milestone: M8
type: feature
status: todo
epic: swarm-behaviour-trends
human_approval: approved
severity: medium
priority: 160
direction: human-requested
depends_on: []
assigned_to: coder
mutation_cost: medium
required_stages: [coder, qa]
stage_skip_reasons:
  cleaner: "telemetry append and pure aggregator; no batch dedup surface"
  architect: "event shape and trend.ts series settled in-ticket"
  hardender: "extension TS outside mutate scope; fixture aggregator tests"
  documenter: "epic BL-594 trend framework docs; series self-describing in briefing"
acceptance: specs/features/BL-596-trend-mono-router-rotation-dynamics.feature

approval_context: >
  Human 2026-07-23 epic BL-594: cannot see how the mono-router single resident
  spends time across personas. stageDwell is per classic pipeline stage, not
  rotating resident personas.

invariants:
  - "Each rotation emits exactly one append-only event — no duplicate or folded records per rotate."
  - "Aggregation is a pure function over the event stream — unit-testable without fs."
  - "Non-mono-router packs yield empty/NA series never an error (mono_router_lib posture)."

required_wiring:
  - 'swarmforge/scripts/mono_router_lib.bb or rotate_to_role hook::rotation event emit::append {from,to,reason,ts} to rotation telemetry jsonl'
  - '.swarmforge/telemetry/rotation-<YYYY-MM>.jsonl::append-only gitignored log::machine-local rotation event stream'
  - 'extension/src/metrics/rotationDynamics.ts::pure aggregator::per-persona dwell rotations/day thrash stranded-off-home'
  - 'extension/src/metrics/trend.ts::rotation dynamics series::plotted via shared TrendedNumber framework'
  - 'specs/pipeline/steps/index.js::bl596MonoRouterRotationDynamicsSteps::acceptance handler registered so the runner executes every scenario'

description: |
  Add a rotation-behaviour series specific to the single-resident model, so
  the human can see whether the resident is healthily cycling or pathological
  (stuck as one persona, thrashing between two, or stranded off its home role -
  the exact BL-550 failure mode).

  Metrics, all derived from rotation events already implied by the runtime
  (rotate_to_role / respawn-as!, mono_router_lib decisions):

    - per-persona DWELL: cumulative + share of wall-clock the resident spent
      as each role (coder/cleaner/architect/…). "80% coder, 3% cleaner" is the
      health readout.
    - rotations per day, and rotation THRASH: rotations that flip back within a
      short window (a proxy for the burst-rotate the should-rotate-resident?
      cooldown guards against).
    - time STRANDED off-home: cumulative time the resident sat in a non-home
      role with an empty mailbox before rotating back — BL-550's stranding,
      made visible as a trend rather than a one-off incident.

  Emit a rotation event (from-role, to-role, reason, ts) to
  `.swarmforge/telemetry/rotation-<YYYY-MM>.jsonl`; the dwell/thrash/stranding
  aggregation is a pure function over that event stream, plotted via trend.ts.

  Classic multi-pack packs have no single rotating resident — the series is
  simply empty/NA there (guard, do not crash), the same
  fires-only-under-mono-router posture mono_router_lib already uses.

qa_e2e_procedure: >
  (1) Trigger rotations on mono-router fixture — one jsonl event per rotate with
  from/to/reason/ts. (2) Unit-test aggregator: dwell shares, rotations/day,
  stranded time. (3) Thrash vs ordinary rotation counts. (4) Classic pack —
  empty/NA no error. Live: trend surface shows rotation dynamics on mono-router host.

source: |
  Human, 2026-07-23, epic BL-594. The mono-router runs ONE resident rotating
  through personas; today you cannot see how it spends its time. stageDwell
  measures the classic per-stage pipeline and cannot represent a single pane
  that is coder now and cleaner in ten minutes.

specifier_decisions: |
  Minted 2026-08-26 per coordinator route (ticket in backlog/paused). Four scenarios
  map inline acceptance 1–4. Thrash window aligns with should-rotate-resident cooldown
  semantics in implementation. Mini-app/briefing consumers are BL-603/BL-604 not this slice.

notes: |
  Child of epic BL-594. Pairs with BL-602 handoff latency (rotation-gated stranding).

out_of_scope: |
  - Changing rotation behaviour (mono_router_lib owns it). Observability only.
  - Per-stage classic-pipeline dwell (stageDwell already owns that).
  - Mini-app publish and morning briefing analysis (BL-603/BL-604).
