id: BL-835
title: "DEFECT — flow-watchdog floors sub-minute p67 to 60s and WARNs healthy in-flight hops (QA→coordinator aged 1m)"
milestone: M8
type: defect
status: todo
epic: swarm-reliability
human_approval: approved
severity: high
priority: 0
direction: human-requested
depends_on: []
assigned_to: specifier
mutation_cost: low
required_stages: [specifier, coder, cleaner, architect, hardender, documenter, qa]

approval_context: >
  Human asked (Cursor 2026-08-06 ~20:16 CEST) to intake the floored-percentile
  false-alarm process bug and get the swarm implementing ASAP. Live evidence
  is in the intake and below — not a design debate. The one process call this
  ticket locks: min-warn-ms is a REJECT GATE (discard the key, fall through),
  not a FLOOR that invents a calibrated threshold. If the human later wants a
  higher gate than 60s, that is a one-line constant change on top of the gate
  semantics; do not ship floor-clamp again.

severity_rationale: >
  high, not critical. The swarm still moves and no data is lost, so not
  critical. Above medium because (a) the hot-synced adopt is already live in
  handoffd, (b) OPERATOR is taking real Telegram WARNs on healthy in_process
  work at ~1 minute, and (c) trusting a fake percentile teaches operators to
  ignore flow-stall — the same alarm surface BL-577 made unsuppressable.

source: |
  Drained from
  `backlog/INTAKE-flow-watchdog-floored-percentile-false-alarms.md`
  (human via Cursor 2026-08-06). Live log + calibrated table verified the
  same evening.

description: |
  ## Symptom (live)

  After the spec-dependent percentile table came up
  (`.swarmforge/daemon/flow-watchdog-thresholds.json`, 544 samples):

  - `architect→hardender|git_handoff` WARN at 24m — matches calibrated ~24.6m (good)
  - `hardender→documenter|git_handoff` WARN at 12m — matches calibrated ~11.6m (good)
  - `QA→coordinator|git_handoff` WARN at **1m** in `coordinator in_process`
    — investigate — while that route's table row is `warn-ms: 60000` (bad)

  Same floor hits `coder→coordinator|note`, `specifier→coordinator|note`, and
  the `*->coordinator|git_handoff` fallback.

  ## Cause

  `thresholds-from-samples` clamps:

      warn-ms (max min-warn-ms (long warn-raw))  ;; 60000

  When historical p67 is a few seconds, the emitted "calibrated" warn is the
  floor, not the percentile. Resolution then trusts that row. Every parcel
  that sits ~1 minute on those hops crosses WARN — including healthy
  in-flight work.

  Process rule that failed: **floor ≠ calibrate**. A sample set whose raw
  warn percentile is below the gate has not earned a calibrated threshold.

  Secondary: the percentile-section comment in `flow_watchdog_lib.bb` still
  says sparse specs fall through `*->*|type`; `resolve-thresholds` correctly
  does not consult that row (docs/Specification already match the code).

  ## Fix

  1. When raw warn percentile `< min-warn-ms`, do **not** emit a specs entry
     for that key. Fall through exact → `*->to|type` → global as today.
     Escalate-strictly-above-warn stays. Global fallback stays.
  2. Fix the stale fallthrough comment.
  3. Unit regression in `flow_watchdog_test_runner.bb`: sub-floor samples →
     no exact entry → ~90s parcel does not WARN under global 15m.
  4. Specifier note on BL-827: scenario 01 / feature intro currently require
     early fire below global on fast routes. After this lands, that contract
     must say stricter-than-global is allowed only when the raw percentile
     itself clears the gate — not when a floor invented the number. Amend
     BL-827 in the same parcel if it has not started; otherwise file the
     amend as a follow-up note the specifier owns.

  ## Relationship to BL-827

  BL-827 adopts the percentile working tree (priority 52, paused). This
  defect patches the live process bug in that tree so OPERATOR stops
  spamming. Do not wait for the full BL-827 adopt parcel. Once this lands,
  BL-827's starting patch / working tree must include (or re-apply) this
  fix; do not reintroduce floor-clamp.

acceptance: specs/features/BL-835-flow-watchdog-floored-percentile-false-alarms.feature

invariants:
  - "A calibrated specs entry is emitted only when the raw warn percentile is >= min-warn-ms; the floor never invents a warn threshold."
  - "decide-tier still never sees from/to/type/dormancy; resolution stays outside it."
  - "Absent or rejected calibration still resolves to a usable warn/escalate pair (global fallback); the watchdog never disables itself."

required_wiring:
  - "swarmforge/scripts/flow_watchdog_lib.bb::thresholds-from-samples::reject-below-gate not floor-clamp"
  - "swarmforge/scripts/test/flow_watchdog_test_runner.bb::sub-floor samples do not WARN a 90s parcel under global 15m"

verification: |
  ## Gate

  Extend `bb swarmforge/scripts/test/flow_watchdog_test_runner.bb` (already
  green). No mutation tooling for Babashka.

  ## e2e QA procedure

  1. Seed >=8 completed hops on a route with residence well under 60s. Run
     calibrate. Confirm no exact (and no to-type, if all samples are that
     route) entry is written for that key.
  2. Place a ~90s live parcel on that route. Sweep with global 15m/1h.
     Confirm zero alarms.
  3. Seed a route whose p67 is ~12 minutes. Confirm it still calibrates and
     still WARNs a parcel past that warn (regression of the good path).
  4. Confirm the percentile-section comment no longer claims `*->*|type`
     fallthrough.

out_of_scope: |
  - BL-650 active-time clock.
  - Re-adding `*->*|type` into resolve-thresholds.
  - BL-827's adaptation-hazard ceiling options (a)/(b)/(c).
  - Changing the unsuppressable posture or snooze semantics.
  - Raising the global 15m/1h defaults.

notes: |
  Queue-jumped by human request 2026-08-06. Ambulance engaged at mint so
  BL-823 (active interval ledger) parcels hold until this clears.

  The hot-synced working tree already carries the percentile adopt; the
  coder patches `thresholds-from-samples` (and the stale comment) in place.
  Do not revert the adopt to "fix" this — reject-gate is the fix.
