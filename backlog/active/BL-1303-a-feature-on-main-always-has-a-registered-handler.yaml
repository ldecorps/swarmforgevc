id: BL-1303
title: "A feature file can reach main with no registered step handler, so the runner throws on every one of its scenarios and the next role to run the suite inherits a red it did not cause"
milestone: M8
type: defect
severity: high
# high, matching BL-1294's reasoning for the same shape: this breaks a safety
# SIGNAL rather than degrading it. main's acceptance verdict stops reflecting
# the code under test, and it fails against whichever parcel runs the suite
# next rather than the one that caused it. Not critical: the swarm is up, no
# production path is touched, and in both instances found so far the complete
# parcel still existed elsewhere in git - nothing lost.
# Live instance UPDATED 2026-08-31: the BL-1253 one cited at mint was repaired
# by QA on 2026-08-30 (8674998ad6). The live instance is now BL-709 - handler
# file present, registration absent, 8 scenarios 0 pass, red on main since
# 2026-08-27. Two independent instances in five days is the evidence for the
# grade, not either one alone.
status: todo
epic: code-quality-gates
priority: 28
direction: swarm-reported
depends_on: []
mutation_cost: low
slice_size_envelope: low
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-1303-a-feature-on-main-always-has-a-registered-handler.feature

required_wiring:
  - "swarmforge/scripts/run_commit_guards.sh::run_guard check_feature_handler_registration.sh::the guard must be added to the commit-guard chain. run_commit_guards.sh is a pre-existing live consumer - swarmforge/git-hooks/pre-commit execs it - so this anchor proves the guard is REACHED on the plain-commit path rather than merely written. CORRECTION 2026-08-31: this entry originally asserted that pre-merge-commit execs run_commit_guards.sh too. It does not - pre-merge-commit execs check_pipeline_code_on_main.sh directly - so this anchor alone leaves the merge path uncovered, and the pre-merge-commit entry below is what closes it. Per BL-1235 the two entries together carry the wiring proof. The guard name is fixed here at mint so the pattern is a literal that will exist; keep the run_guard prefix so it joins the chain the same way check_pipeline_code_on_main.sh does."
  - "swarmforge/git-hooks/pre-merge-commit::check_feature_handler_registration.sh::the guard must ALSO be reached on the --no-ff merge path. Both incidents this ticket cites put main into the bad state by merge (BL-1253 by a resurrecting merge, BL-709 by merge 45625ef9cb), and QA lands every approved commit with git merge --no-ff, so a guard wired only into run_commit_guards.sh would have caught NEITHER of them. git itself invokes this hook, so it is a pre-existing live consumer and this anchor is real reachability proof rather than a self-satisfied declaration (BL-1235). Verified absent at amend time: pre-merge-commit today execs only check_pipeline_code_on_main.sh. If you reach the guard from this hook by some other route than naming it here, say so in a note and the anchor will be repointed - do not work around it."
  - "specs/pipeline/steps/index.js::bl1303::the new step handler must be registered - specs/pipeline/runtime.js THROWS on any scenario with no handler, which is this ticket's own subject, so an unregistered file would fail every scenario here. Verified absent from index.js at spec time (grep bl1303 -> 0). Honest note per BL-1235: this anchor is satisfied by the parcel's own diff and so proves nothing about reachability - the run_commit_guards.sh entry above is the real wiring proof. It is kept only because registering a handler is literally the behaviour under test."

invariants:
  - "The guard fails closed: an artifact it cannot resolve or read is a refusal naming it, never a silent pass."
  - "One pass reports every offending artifact, not only the first."

human_approval: approved
approval_context: >
  A new feature file is authored here, so it needs a tap. One scope judgment is
  genuinely yours.

  Scenario 03 widens the guard from REGISTRATION to RUNNABILITY: it also refuses
  when a registered handler executes a sibling script under
  specs/pipeline/steps/lib/ that is absent from the tree. I included it because
  it is the half the coder actually reported, and because the live incident lost
  BOTH the registration line and the lib script in the same revert - fixing only
  registration would turn 8 no-handler failures into 8 execution failures and
  look like a regression. If you would rather keep this slice to registration
  alone and let the sibling-script check be its own ticket, say so and I will
  split it; the remaining five scenarios stand on their own.

  What is NOT in question: the guard refuses rather than warns. Every other
  member of the commit-guard chain refuses, and a warning here would reproduce
  exactly the accumulate-unseen failure BL-1240 measured at six files a day.

description: |
  ## What is wrong

  `specs/pipeline/runtime.js` throws on any scenario whose steps no registered
  handler matches. Nothing refuses a commit or merge that puts `main` into that
  state, so a feature file can land with its handler unregistered - or with the
  handler registered but a script it executes missing - and the failure surfaces
  later, to whichever role next runs the suite, against a parcel that did not
  cause it.

  This is the step-registry sibling of BL-1240, which measured the same
  accumulate-unseen shape for `suite-manifest.tsv`. Different registry,
  different gate, same failure mode; neither ticket subsumes the other.

  ## The live incident (2026-08-30)

  BL-1253's QA bounce-revert `403b75e44e` correctly removed four coupled
  artifacts together: the handler
  `specs/pipeline/steps/bl1253DeadFeederOwnsGetUpdatesStampSteps.js`, its lib
  `specs/pipeline/steps/lib/bl1253StartCursorBridgeFeederCli.sh`, the
  `specs/pipeline/steps/index.js` registration line, and four evidence files.

  A later merge then resurrected the handler file and the feature file, but
  neither the registration nor the lib. Measured on `main` at spec time:

      steps/bl1253DeadFeederOwnsGetUpdatesStampSteps.js   PRESENT
      steps/lib/bl1253StartCursorBridgeFeederCli.sh       ABSENT
      specs/features/BL-1253-...feature                   PRESENT
      index.js registration (grep bl1253)                 ABSENT

      node specs/pipeline/cli.js specs/features/BL-1253-...feature
        -> 8 tests, 0 pass, 8 fail
        -> 'no step handler matched "Given the landed sources at commit 2ec06b6ef1"'

  BL-1253 itself is not at fault and needs no respec: it was parked to
  `backlog/hold/` at 17:17 by bookkeeping (`66be60ea40`, orphaned by a dead
  expedite run for BL-1295), and its complete reworked parcel still exists at
  `2e519c4a8f` on all six `primary/*` branches. Restoring `main` is a separate,
  immediate QA action - `specs/pipeline/steps/` is a QA-exclusive path under
  `check_pipeline_code_on_main.sh`, so no other role may commit there. This
  ticket is only about the missing detection.

  ## How (direction, not mandate)

  A guard in the commit-guard chain beside `check_pipeline_code_on_main.sh`,
  which already refuses a bad `main` tip before it exists rather than reacting
  to one that already does. Resolving each feature's steps against the registry
  is the obvious route; the implementer chooses how.

  Two constraints worth naming because both have bitten before:

  - `run_commit_guards.sh` runs `set -uo pipefail`, NOT `set -e`, and captures
    each guard's status so one failure cannot mask the rest (BL-1242/BL-1252).
    A new guard joins that shape and keeps its own `set -euo pipefail` inside.
  - The refusal must list every offender in one pass (Article 4.4's shape,
    applied in a gate) - a guard that stops at the first offender reproduces
    the one-defect-at-a-time bounce loop that rule exists to prevent.

  ## The guard must be reached on the MERGE path (added 2026-08-31)

  `swarmforge/git-hooks/pre-commit` is a thin wrapper over
  `run_commit_guards.sh`, but `swarmforge/git-hooks/pre-merge-commit` is NOT -
  it execs `check_pipeline_code_on_main.sh` directly and never reaches the
  chain. A guard wired only into `run_commit_guards.sh` therefore does not
  fire for `git merge --no-ff`, which is how QA lands every approved commit
  and how BOTH incidents above put `main` into the bad state. It would have
  caught neither of them. `pre-merge-commit`'s own BL-632 comment names this
  exact coverage-in-disguise hole; closing it for this guard is in scope here.

  Two things NOT to do while closing it:

  - Do NOT repoint `pre-merge-commit` at `run_commit_guards.sh` wholesale.
    That newly subjects every merge to `check_commit_size.sh`,
    `check_ticket_deletion.sh` and the expensive `check_property_suite_drift.sh`
    - whose allowlist matcher is currently broken and running under an
    operator override (BL-1234). Widening the whole chain to the merge path is
    a separate ticket, not this one.
  - Do NOT let `pre-merge-commit` become a sequential `set -e` chain of two
    guards. That is the first-failure-stop shape BL-1242/BL-1252 removed from
    `pre-commit`: capture each guard's status, exit the combined status, and
    let each guard keep its own `set -euo pipefail`.

qa_e2e_procedure: |
  1. On a scratch branch off main, delete the `bl1253` line from
     `specs/pipeline/steps/index.js` (or pick any registered handler) and stage
     the change. Run `swarmforge/scripts/run_commit_guards.sh <repo-root>`.
     Expect a refusal naming that feature file and the unregistered handler,
     alongside any other guard's output, with the chain still running every
     guard rather than aborting at the first failure.
  2. Restore the line, then delete a lib script a registered handler executes.
     Re-run: expect a refusal naming the missing sibling script.
  3. Restore both. Re-run: expect the guard silent and the chain green.
  4. Repeat step 1 on a non-main branch: expect the guard silent.
  5. Confirm the live case is closed: `node specs/pipeline/cli.js
     specs/features/BL-1253-swarm-stamp-dead-feeder-owns-getupdates-2ec06b6ef1.feature`
     runs 8 scenarios with no "no step handler matched" error.
  6. Exercise the MERGE path, which step 1 does not reach: in a scratch clone,
     branch off main, remove a registration line there, then
     `git checkout main && git merge --no-ff <branch>`. Expect the merge to be
     refused, naming the offending feature. Step 1 fires `pre-commit`; a clean
     `--no-ff` merge fires only `pre-merge-commit`, a different hook, so a
     guard that passes step 1 can still be absent from the path that matters.

source: >
  Coder note 2026-08-30T19:25Z (priority 00): "BL-1253 main: steps ref missing
  lib/bl1253StartCursorBridgeFeederCli.sh". Investigated by the specifier the
  same turn; the missing lib is real but is the SECOND defect - the primary
  cause of the red is the absent index.js registration. Both were lost in the
  same revert and both were left behind by the same partial resurrection.

notes: >
  Sibling of BL-1240 (same accumulate-unseen shape, different registry:
  suite-manifest.tsv vs specs/pipeline/steps/index.js). Neither subsumes the
  other and they can land in either order. Checked at spec time and NOT
  duplicated by BL-1294 (pinnedRepoFixture closure resolver) or BL-1279
  (front-desk fixture bb closure).

  Second live offender, found 2026-08-31 by the coder while working this
  ticket and verified by the specifier the same turn: BL-709. Its handler
  specs/pipeline/steps/bl709BubbleItsOwnTelegramTopicSteps.js is present and
  intact, its registration line is absent from index.js, and
  `node specs/pipeline/cli.js specs/features/BL-709-bubble-its-own-telegram-topic.feature`
  is 8 tests / 0 pass on main today. This is a BETTER fixture for the guard
  than the BL-1253 instance was, because the mechanism differs: BL-1253 lost
  the line to a bounce-revert plus a partial resurrection, whereas BL-709 lost
  it to a MERGE. Merge 45625ef9cb ("merge: process documenter 83b00aa1dd for
  BL-941 QA rematch verification", 2026-08-27 13:58 +0100) had the line in
  first parent 6e78c39a88 and not in second parent 83b00aa1dd, and resolved to
  the side without it - the silent-revert-by-merge shape of BL-571/BL-958/
  BL-954. The line had been landed deliberately by b3ddc2e692 ("fix(BL-709):
  register bl709 steps in index.js. By documenter.") and survived a conflict
  cleanup at cab163c9cf before the merge dropped it. So a guard keyed only to
  revert/resurrection would miss this one; it must refuse on the STATE of main
  regardless of how that state was reached. Evidence:
  backlog/evidence/BL-1303-second-live-offender-bl709-20260831.md

  The BL-709 repair itself is NOT in this ticket's scope and must not be folded
  into this parcel: specs/pipeline/steps/ is QA-exclusive on main under
  check_pipeline_code_on_main.sh, so only QA may commit it. Routed to QA by
  note 2026-08-31. It is also a live regression test for this guard - if QA
  repairs it before this lands, use it as a historical fixture rather than
  re-breaking main to demonstrate the guard.

  LANDING PRECONDITION (specifier, 2026-08-31). Once the guard is reached on
  the merge path it refuses on the STATE of main, not on what a commit
  changes. While BL-709's registration line is absent from
  specs/pipeline/steps/index.js it therefore refuses EVERY plain commit on
  main - specifier spec commits and coordinator bookkeeping alike - and every
  --no-ff land, including this parcel's own. Verified still absent on main on
  2026-08-31. So BL-709's repair must land BEFORE this parcel does. That
  repair is QA-exclusive under check_pipeline_code_on_main.sh and was routed
  to QA by note on 2026-08-31; it stays out of this parcel. If QA has already
  repaired it by the time this arrives, nothing here changes - the
  precondition is simply already met.

  KNOWN RESIDUAL, stated so it is not later mistaken for coverage: a
  FAST-FORWARD merge fires neither pre-commit nor pre-merge-commit, so no
  commit-hook guard can cover that path at all. A CONFLICTED merge finished
  with `git commit` does fire pre-commit and so is covered. Both are
  properties of githooks(5) rather than of this guard, and closing the
  fast-forward hole needs a different instrument - not in scope here.

  This amendment does not reopen the human approval above. The tap ruled on
  one question - whether scenario 03's sibling-script check belongs in this
  slice - and that question is untouched. What changed is a correction to a
  wiring premise that was factually wrong, without which the ticket does not
  deliver its own title.

  Constraint the implementer should not have to rediscover (specifier sweep
  2026-08-31, full result in the evidence file above): twelve step-handler
  files are absent from index.js on main, and ELEVEN of them are correct. The
  bl<NNN>Only.js files are deliberate focused entry points - each a three-line
  re-export of the real handler, each carrying the comment "avoids loading the
  full steps/index.js, which requires a compiled extension/out tree" - and all
  eleven of those targets ARE registered (verified individually). A guard keyed
  to "every steps/*.js file must appear in index.js" therefore emits twelve
  refusals today, one true and eleven false, which is the shape that gets a
  guard normalized away within a week. Key it on the FEATURE side instead -
  every specs/features/*.feature must be matched by a REGISTERED handler, which
  is this ticket's own title and asks the question runtime.js actually throws
  on, and has no false-positive class. Do not special-case the *Only.js
  filename: that encodes a naming convention as a boundary and silently exempts
  the next real offender that happens to be named that way.

  Confirmed by the coder 2026-08-31 (note, priority 00): the implementation
  was already feature-keyed before that constraint landed, and the guard
  reports 1 offender with 0 of the 11 *Only.js files flagged. Recorded here
  as verification - the constraint cost no rework and demands none.

assigned_to: coder
bounce_count: 1
bounce_history:
  - { at: 2026-08-31, by: architect, blamed: coder, class: behavior, commit: 7d782800ad, evidence: backlog/evidence/BL-1303-a-feature-on-main-always-has-a-registered-handler-bounce-20260831.md }
