id: BL-739
title: "Pilot process: a declared-invariant property test landed without checking its generator actually reaches the behavior under test"
milestone: M8
type: defect
severity: low
status: todo
epic: swarmforge-console
assigned_to: coder
priority: 18
direction: queue-jump
depends_on: []
mutation_cost: low
required_stages: [coder, hardender, qa]
stage_skip_reasons:
  cleaner: "pilot land gate and prompt edits; no batch dedup surface"
  architect: "Invariants Review contract settled in BL-654; this extends pilot land gate only"
  documenter: "architect.prompt / pilot gate prose is the doc surface"
acceptance: specs/features/BL-739-bl718-pilot-missed-vacuous-property-generator.feature

invariants:
  - "A property test whose generator provably cannot reach the non-trivial branch it names refuses pilot land — green assertions alone are insufficient."
  - "The reach check diffs generator bounds against literal boundary constants of the targeted function, not pane text or prose claims."
  - "A refused vacuous-property land writes nothing durable (no backlog/done move, no acceptance receipt)."

required_wiring:
  - "extension/src/tools/pilotAcceptanceGate.ts::checkPropertyGeneratorReach::vacuous *.property.test.js refuses land"
  - "swarmforge/roles/architect.prompt::Invariants Review::vacuous generator send-back before hand-verification"
  - "specs/pipeline/steps/index.js::bl739PilotVacuousPropertyGateSteps::acceptance handler registered"

source: |
  Found during BL-723 (pilot-tonight quality review), architect viewpoint
  pass, 2026-07-30. Companion remaining-work ticket: BL-738.

notes: |
  Human 2026-07-31 Let's Talk: queue-jump for today's live shift — pilot-process hardening before more /pilot safe volume.

  Pilot-process half of the BL-738 architect-lens shortfall pair on BL-718.

  [2026-08-27] specifier: feature file minted from coordinator Work note; extends
  BL-654 Invariants Review to /pilot land gate with generator-vs-boundary check.

description: |
  ## What

  BL-718's landing added a fast-check property test for
  `splitTelegramChunks` capped at `maxLength: 200` against a real 4096-char
  split boundary — the property never triggers the multi-chunk branch it
  is nominally there to protect (see BL-738 for the full vacuousness
  proof). The offline pilot's architect-equivalent pass either had no
  step that reviews a new `*.property.test.js`'s generator range against
  the boundary constants of the code under test, or that step did not
  catch this one.

  This is the same class of near-miss as this project's own recorded
  lesson ("property-test generator must reach deep state — weight ops,
  assert coverage"): a property test that reads as real coverage but,
  because of a generator range mismatch, only ever exercises the trivial
  branch.

  ## Question to answer

  Does `/pilot`'s architect-equivalent step (BL-654's Invariants Review)
  actually diff a new or touched `*.property.test.js`'s generator bounds
  against the literal constants (max lengths, thresholds, boundary values)
  of the function it targets? Harden the pilot (or the live architect gate
  contract) so a property test whose generator provably cannot reach a
  function's non-trivial branch is flagged before landing — e.g. a cheap
  check: run the property once with logging/coverage and confirm the
  branch under test was actually hit, not just that assertions passed.

qa_e2e_procedure: >
  (1) Pilot fixture with vacuous splitTelegramChunks property (maxLength 200 vs 4096)
  — land refused, names bounds. (2) Fixture with generator crossing boundary — reach
  gate passes. (3) Architect invariants review records vacuity finding. (4) Refused
  land unmoved yaml, no receipt. Live: /pilot on ticket adding *.property.test.js
  runs generator-reach check before backlog/done.

specifier_decisions: |
  Minted 2026-08-27 per coordinator route. Five scenarios: touched property triggers
  reach check; vacuous generator refuses land (BL-718 worked example); architect-equivalent
  review flags vacuity; reaching generator passes; refused land writes nothing durable.
  Answer: /pilot land gate diffs generator bounds to function boundary constants; BL-654
  live architect posture extended to offline pilot. Code fix for BL-718 property remains BL-738.

out_of_scope: |
  - Fixing cursorBridgeLive.property.test.js generator (BL-738).
  - Retroactively scanning every historical property test on main.
  - Replacing fast-check or the property-test runner itself.
