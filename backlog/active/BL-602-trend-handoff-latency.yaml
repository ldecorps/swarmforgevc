id: BL-602
title: "Trend: handoff latency — time from a git_handoff being written to the recipient dequeuing it"
milestone: M8
type: feature
status: todo
epic: swarm-behaviour-trends
human_approval: approved
severity: low
priority: 183
direction: human-requested
depends_on: []
assigned_to: coder
mutation_cost: low
abandoned_commits:
  - ce2ee20358
  - 986ae0f534
  - b2368e7f0a
  - 19779e6886
  - d317c2ec49
  - 0f6b7a845a
  - 618e99fd3b
  - 7ee4ad73fc
  - d912a443c7
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-602-trend-handoff-latency.feature

required_wiring:
  - "swarmforge/scripts/swarm_handoff.bb::send path::enqueued_at stamped on each outbound handoff"
  - "swarmforge/scripts/ready_for_next_task.bb::claim path::dequeued_at stamped when parcel moves to in_process"
  - "extension/src/metrics/handoffLatency.ts::gatherRoleHandoffLatencyRecords::reads master and worktree role mailboxes"
  - "extension/src/metrics/stageDwell.ts::splitOutliers::median + outlier honesty reused per recipient role"
  - "extension/src/metrics/handoffLatency.ts::computeTrend inward::plotted via shared TrendedNumber framework (no trend.ts re-export — acyclic)"
  - "extension/src/metrics/trend.ts::BL-602 handoff-latency::callers import from ./handoffLatency (acyclic — no re-export)"
  - "specs/pipeline/steps/index.js::bl602TrendHandoffLatencySteps::acceptance handler registered"

invariants:
  - "Still-queued handoffs contribute OPEN wait only — never fabricated as a fast completed pickup."
  - "Gather covers master-resident and worktree mailboxes (new, in_process, completed) — master-only read is a false negative."
  - "Aggregation is a pure function over enqueued/dequeued pairs — unit-testable without live handoffd."
  - "Measuring never changes dispatch, rotation, or ready_for_next claim behaviour."

source: |
  Human, 2026-07-23, epic BL-594. The "handoff vanished" fear (a parcel sitting
  in a dormant role's inbox/new looking identical to one never sent) has no
  metric. On mono-router this wait is rotation-gated and genuinely variable.
description: |
  Measure how long each handoff waits between being SENT and being PICKED UP:
  from the enqueued_at the handoff file already carries, to the dequeued_at
  ready_for_next stamps when it moves new/ -> in_process/. Trend the
  distribution per recipient role via trend.ts.

  Both timestamps already exist on the handoff itself:
    - enqueued_at — written by swarm_handoff.bb on send
    - dequeued_at — stamped by ready_for_next_task.bb on claim
  So the raw data is on disk already; this reads the pair per handoff (from the
  completed/ handoffs, worktree mailboxes included per the search-must-cover-
  worktree-mailboxes rule) and aggregates latency per recipient.

  Value: on mono-router, a handoff to a dormant role waits until the resident
  next rotates INTO that role — so a spike in handoff latency for one role is a
  direct signal that role is being starved (pairs with BL-596's stranding
  metric). A parcel stuck for hours shows as a latency outlier instead of a
  silent "did it vanish?".
qa_e2e_procedure: >
  (1) Fixture handoff headers → latency = dequeued_at - enqueued_at per role.
  (2) Pure aggregator unit tests: median + outlier split per role without fs.
  (3) Queued parcel → open wait excluded from processed stats. (4) Worktree
  mailbox fixtures included. (5) trend.ts current/prior/delta on median series.

out_of_scope: |
  - Changing dispatch/rotation. Measurement only.

notes: |
  - [2026-08-27] specifier: feature file minted from coordinator Work note;
    pairs enqueued_at/dequeued_at already on handoff headers; worktree mailbox
    coverage per BL-577/BL-1109 gather posture; pairs with BL-596 stranding.
bounce_count: 1
bounce_history:
  - { at: 2026-08-27, by: QA, blamed: coder, class: behavior, commit: 7ee4ad73fc, evidence: backlog/evidence/BL-602-qa-bounce-20260827.md }
