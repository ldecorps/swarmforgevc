id: BL-601
title: "Trend: context-compaction cadence — per-role auto-compaction frequency and the token level it triggers at"
milestone: M8
type: feature
status: todo
epic: swarm-behaviour-trends
human_approval: approved
severity: low
priority: 190
direction: human-requested
depends_on: [BL-665]
assigned_to: coder
mutation_cost: medium
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-601-trend-compaction-cadence.feature
# Rematch 2026-08-27: first-lineage / bounce / cleaner tips superseded by
# tip-pure acyclic rematch on hardener→documenter tip (content folded in).
abandoned_commits:
  - 8cea3c3e59
  - d483f10527
  - 809f0dab14
  - 1228838bc3
  - 2f777fc113
  - a2e4e88a34
  - 2faa3e5210

required_wiring:
  - "specs/pipeline/steps/index.js::bl601TrendCompactionCadenceSteps::acceptance handler registered"
  - "swarmforge/scripts/context_telemetry_lib.bb::compaction::primary structured detection signal (requires BL-665 producer)"
  - "extension/src/metrics/compactionTelemetryStore.ts::compaction-YYYY-MM.jsonl::append-only derived compaction records"
  - "extension/src/metrics/compactionCadence.ts::aggregateCompactionCadence::compactions/hr per role + token-at-compaction distribution"
  - "extension/src/metrics/compactionCadence.ts::trendForCompactionCadencePerHour::plotted via shared computeTrend framework"
  - "extension/src/metrics/trend.ts::computeTrend::shared TrendResult framework (compactionCadence imports inward; no re-export)"

invariants:
  - "Pane spinner strings alone never emit compaction records — only structured context-events with compaction:true count."
  - "Roles without a reliable compaction signal read NA, never a fabricated zero."
  - "Aggregation is a pure function over compaction records — unit-testable without live panes or transcripts."
  - "Measuring never tunes compaction thresholds or alters agent compaction behaviour."

source: |
  Human, 2026-07-23, epic BL-594. The "13 min, no compaction yet" moment showed
  compaction is watched by eye but never recorded. Compaction frequency is a
  clean proxy for which roles are under context pressure.
description: |
  Record each auto-compaction the resident/roles perform, and trend the
  frequency and the token level it fires at, per role. A role compacting every
  few minutes is under heavy context load; the token-at-compaction level shows
  whether the trigger threshold is well-tuned for that role's model.

  Detection source: the pane already shows compaction ("auto-compact NN%",
  "Compacting"), and transcript/token telemetry (transcriptUsage) already
  tracks per-role token totals over time. Pair a compaction event with the
  token level at that moment. The specifier confirms the most reliable signal -
  a pane-text marker, a transcript boundary, or a token-drop discontinuity -
  and captures {role, model, tokens-at-compaction, ts} to
  `.swarmforge/telemetry/compaction-<YYYY-MM>.jsonl`.

  Trend via trend.ts: compactions-per-hour per role, and the distribution of
  token-at-compaction. Cross-reads naturally with BL-596 (a persona that
  compacts constantly is a rotation-cost signal too).

  Note honestly: this is the one child whose detection signal is not already a
  clean structured event - the specifier must pin a RELIABLE trigger rather
  than scrape a spinner string that changes between CLI versions. If no robust
  signal exists, say so and scope to what is detectable rather than emit noisy
  data.
qa_e2e_procedure: >
  (1) Fixture context-events with compaction:true → derived compaction record with
  role/model/tokens/ts. (2) Pure aggregator: compactions/hr + token distribution
  without fs. (3) Undetectable role → NA. (4) Spinner-only → no record. (5)
  trend.ts current/prior/delta on compactions/hr series.

out_of_scope: |
  - Tuning the compaction threshold. Measurement only.

notes: |
  - [2026-08-27] specifier: feature file minted from coordinator Work note.
    Reliable signal pinned to GH-22 context-events.jsonl compaction:true (BL-665
    producer required); pane spinner text explicitly rejected as primary trigger;
    tokens-at-compaction from input_tokens + context_utilization_pct on event.
