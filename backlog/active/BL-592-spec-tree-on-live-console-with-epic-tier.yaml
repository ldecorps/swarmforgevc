id: BL-592
title: "Spec navigation tree on the live Mini App console — Milestone -> Epic -> BL item -> Gherkin (ports BL-117's tree, adds the Epic tier)"
milestone: M8
type: feature
status: todo
epic: swarmforge-console
human_approval: approved
severity: medium
priority: 116
direction: human-requested
depends_on: []
assigned_to: coder
mutation_cost: medium
acceptance: specs/features/BL-592-spec-tree-on-live-console-with-epic-tier.feature

invariants:
  - "Spec tree navigation is read-only — no path from any level edits backlog YAML, documentation sources, or files a ticket."
  - "Every non-epic-tracker backlog ticket appears under exactly one epic bucket within its milestone (including the visible '(no epic)' bucket when epic: is absent)."
  - "A cross-milestone epic duplicates its header under every milestone that has at least one member ticket, listing only tickets whose milestone: matches that milestone under each appearance."

required_wiring:
  - "extension/src/docs/docsTree.ts::DOCS_TREE_SCHEMA_VERSION::schema bumped for epic tier; MilestoneNode carries epic grouping"
  - "extension/src/bridge/bridgeServer.ts::spec-tree::live JSON route serves computeDocsTree output over the bridge"
  - "extension/src/bridge/consoleMenuUiHtml.ts::spec-tree::console menu links the live spec tree screen"
  - "specs/pipeline/steps/index.js::bl592SpecTreeOnLiveConsoleWithEpicTierSteps::acceptance handler registered"

source: |
  Human, 2026-07-23: "I want a tree structure to navigate through the spec.
  From Milestones -> Epic -> BL Item -> gherkins. This subject has been debated
  at length, there must be a trace of that in the spec, there might even be
  some ticket already written somewhere."

  There is. BL-117 (done) shipped exactly this drill-down, with
  operator-confirmed decisions dated 2026-07-05, as
  `vision -> milestone -> ticket -> Gherkin`. This ticket is NOT a fresh idea;
  it is two deltas against BL-117, both stated below. Read BL-117 first.

description: |
  ## What already exists - reuse it, do not rebuild

  BL-117 built a read-only documentation drill-down:
  `vision -> milestone -> ticket -> Gherkin`. The derivation lives in
  `extension/src/docs/docsTree.ts` (`computeDocsTree` pure over already-read
  backlog + gherkin data; `translateDocsTree` for the BL-118 French siblings),
  published as `docs-tree.json` by
  `extension/src/tools/generate-docs-tree.ts`. Gherkin extraction is
  `docs/gherkinScenarios.ts`. All of that is the engine and is REUSED whole.

  The human's request differs from BL-117 in exactly two ways. Everything else
  - read-only, repo-is-the-API derivation, Gherkin at the leaf - is unchanged.

  ## Delta 1: the LIVE console surface, not the static PWA

  BL-117's tree renders on the static backlog-dashboard PWA (`pwa/app.js`
  fetches `docs-tree.json`, a git-SHA-reproducible offline artifact). The human
  asked for "the mini app" - the LIVE holistic console
  (`extension/src/bridge/holisticUiHtml.ts`, served token-authed by
  `bridge/bridgeServer.ts`), the same surface BL-572's epic reorder screen
  targets. Per the project's two-phone-surfaces rule, the static PWA is a
  read-only reproducible projection and the live console is the connected
  surface; this tree belongs on the live console alongside the other console
  screens.

  **Specifier decision (live serving):** mirror BL-572 — add a console-menu
  screen plus `GET /spec-tree-state?token=...` that returns `computeDocsTree`
  output from the live target checkout on each poll (no committed sidecar).
  Being on the live surface, it reflects the working tree now rather than the
  last published SHA; stay READ-ONLY (BL-117 non-behavioral gate).

  ## Delta 2: insert the EPIC tier

  BL-117 predates first-class epics (BL-341 / BL-492 / BL-493). Its
  `MilestoneNode` groups tickets DIRECTLY under a milestone
  (docsTree.ts:45-55). The human wants a level between them:

      Milestone -> Epic -> BL item -> Gherkin

  **Specifier decisions (edge cases):**

  - **No `epic:` field:** visible `(no epic)` bucket under the ticket's
    milestone; ticket never dropped.
  - **`type: epic` tracker:** supplies the Epic node's title/description;
    NOT duplicated as a navigable ticket leaf under its own epic bucket.
  - **Cross-milestone epic (the non-obvious choice):** the epic header appears
    under **every milestone that has at least one member ticket** (by the
    member's own `milestone:` field). Under each appearance, only tickets whose
    `milestone:` equals that milestone are listed. Rationale: milestone-first
    navigation must not hide tickets or force a single "dominant" milestone
    guess.

  ## Schema-version discipline

  `DOCS_TREE_SCHEMA_VERSION` bumps (structural change, not additive field).
  **Specifier decision:** bump to `2` with `MilestoneNode.epics[]` between
  milestone and tickets. The static PWA MUST be migrated in the same parcel so
  `pwa/app.js` drills milestone -> epic -> ticket when `schemaVersion >= 2`,
  while still reading the flat root `tickets[]` roll-up for ticket lookup and
  search. A silently broken PWA docs explorer is not acceptable.

qa_e2e_procedure: >
  (1) Open live console spec tree via bridge token; drill M8 -> an epic ->
  BL-592 -> Gherkin scenarios render. (2) Fixture ticket with no epic: appears
  under "(no epic)". (3) Cross-milestone epic fixture: epic header under both
  milestones, filtered children each side. (4) No edit/create affordance at any
  level. (5) Publish docs-tree.json after schema bump; static PWA still drills
  to Gherkin. (6) `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-592-spec-tree-on-live-console-with-epic-tier.feature`.

out_of_scope: |
  - Editing specs or filing tickets from the tree. Read-only, full stop.
  - Removing or changing the static PWA docs explorer beyond the schema
    migration needed to keep it working.
  - The vision/top level from BL-117 (Specification.MD, roadmap, diagrams).
    The human's hierarchy starts at Milestone; carrying BL-117's vision level
    on the live console is optional, not required.
  - Per-epic ETA (BL-591) or reordering (BL-572) - this is navigation only,
    though it shares the epic surface with both.

notes: |
  - [2026-08-27] specifier: wrote acceptance feature; pinned cross-milestone
    epic modelling (duplicate header per touched milestone, filtered children);
    live serving via bridge poll of computeDocsTree; schema v2 + PWA migration
    in same parcel. Replaced inline acceptance block.
bounce_count: 1
bounce_history:
  - { at: 2026-08-27, by: architect, blamed: coder, class: behavior, commit: 3cd8e6c173, evidence: backlog/evidence/BL-592-architect-bounce-20260827.md }
