id: BL-1142
title: "Local Ollama: mono-router vs full-forge under CPU (depth / rotation)"
milestone: M8
type: feature
status: active
epic: local-llm-swarm
assigned_to: coder
priority: 0
direction: queue-jump
depends_on: [BL-1140]
mutation_cost: medium
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
human_approval: approved
approval_context: >
  Human (Cursor) 2026-08-25 evening prioritize intake
  INTAKE-prioritize-local-ollama-remaining-20260825.md — JumpQ remaining
  BL-1125 slices. Accept slow; evidence from BL-1127 battery + live host
  headroom over cloud forge defaults. Do not launch qwen-forge / Token Plan
  full forge as a substitute for the local path.
acceptance: specs/features/BL-1142-local-ollama-mono-vs-forge-cpu.feature
required_wiring:
  - "specs/pipeline/steps/index.js::bl1142LocalOllamaMonoVsForgeCpuSteps::acceptance handler registered"
invariants:
  - "The durable local pack shape on this WSL/CPU host is an explicit documented decision (mono-router depth OR fuller forge with a stated depth cap / rotation discipline) — never an accidental copy of cloud forge defaults."
  - "Choosing fuller forge never wedges Ollama by unbounded concurrent local seats; depth/rotation limits are named in pack or how-to and enforceable at launch."
  - "qwen-forge / Token Plan full forge is not the substitute path for this local decision."
description: |
  ## What / why

  Epic BL-1125 remaining slice A. Once the coder bar has passed (BL-1126/
  BL-1127/BL-1140 landed), decide and ship the durable local pack shape for
  this WSL/CPU host: stay on mono-router depth, or graduate to a fuller
  forge with an explicit depth cap / rotation discipline that does not
  wedge Ollama.

  ## What to do

  1. Gather evidence: BL-1127 battery + live host headroom (CPU/RAM/Ollama
     concurrency), not cloud forge defaults.
  2. Record the decision (mono stay vs capped fuller forge) in pack and/or
     how-to under the local Ollama launch path.
  3. If fuller forge: encode depth cap / rotation so concurrent local seats
     cannot unbounded-wedge Ollama; refuse or degrade clearly when over cap.
  4. Keep `./start-swarm-ollama-qwen.sh` / `ollama-qwen3-mono-router` (or
     successor local pack) honest to that decision.

  ## Out of scope

  - Cold-swap day-shift off cursor-forge (BL-1143).
  - Reminting BL-1126 / BL-1127 / BL-1140; promoting epic BL-1125.
  - Instant local replies; OpenRouter as entirely-local substitute.
  - Launching qwen-forge / Token Plan full forge as the local path.
source: |
  backlog/INTAKE-prioritize-local-ollama-remaining-20260825.md under epic
  BL-1125. Human prioritize remaining slices 2026-08-25 evening.
qa_e2e_procedure: >
  (1) Decision artifact or how-to names mono vs capped-forge with evidence
  cite. (2) Pack/launch path matches decision; over-cap cannot silently
  staff unbounded local seats. (3) qwen-forge not used as substitute.
  (4) cursor-forge day-shift untouched by this ticket alone.
notes: |
  - [2026-08-25T19:05Z] coder: decision mono-router stay (depth 1). Evidence:
    BL-1127 pass + ~4.8Gi avail RAM / load~5. Gate
    local_ollama_pack_shape_gate.sh wired into start-swarm-ollama-qwen.sh;
    how-to + APS + property + shell unit. Acceptance 4/4.
  - [2026-08-25T18:35Z] specifier: minted JumpQ under BL-1125 from
    INTAKE-prioritize-local-ollama-remaining-20260825.md; feature armed;
    depends_on BL-1140. Epic remaining_slices updated.

assigned_to: coder
