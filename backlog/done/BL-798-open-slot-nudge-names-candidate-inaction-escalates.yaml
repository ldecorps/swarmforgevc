id: BL-798
title: "DEFECT — ticketless open-slot nudges are cleared by the coordinator without promoting: approved queue-jump work starved ~2.5h on an idle swarm (SUP-1)"
milestone: M8
type: defect
severity: high
status: todo
epic: swarm-reliability
human_approval: approved
assigned_to: coder
priority: 97
direction: human-requested
depends_on: []
mutation_cost: medium
required_stages: [coder, cleaner, architect, hardender, documenter, qa]

invariants:
  - "An open-slot nudge to the coordinator names the top eligible paused candidate(s) by Article 3.2.4 ordering (expedited defects first, then ticket priority) with their human_approval state — never a ticketless generic poke."
  - "Repeated open-slot nudges while the slot stays open and an approved eligible candidate exists never repeat silently: past a bounded count the condition escalates to the operator as promotion inaction."
  - "The coordinator never clears an open-slot nudge without either promoting a candidate or durably recording the specific blocking reason (cap reached, no eligible candidate, orthogonality conflict, throttle engaged)."

approval_context: >
  Defaults I picked, all amendable. (1) severity high — this is the
  promotion-side sibling of BL-795's rotation-side starvation: the swarm sat
  idle ~2.5h with three approved defects unpromoted until the human pushed,
  and the human explicitly ordered "Fix the process". Flip to medium if you
  consider the BL-795 fixes sufficient mitigation. (2) The nudge NAMES
  candidates but the coordinator keeps promotion judgment (orthogonality,
  throttle) — the daemon never promotes on its own. (3) Escalation threshold
  defaults to 3 unacted nudges; amend if too chatty.

description: |
  ## What

  On 2026-08-03 morning three defects sat `human_approval: approved` in
  `backlog/paused/` for ~2.5h (BL-787 priority-2 queue-jump, BL-788,
  BL-789) while the swarm was fully idle (active:0, coder ready). handoffd's
  open-slot sweep kept nudging the coordinator, but the nudge draft
  (`chase_sweep_lib.bb::open-slot-nudge-draft-lines`) is GENERIC and
  ticketless — so the coordinator treated the Nth identical nudge as noise
  and cleared it without ever picking a concrete candidate. Auto-recovery
  never took; the human had to intervene over Telegram.

  Three coupled gaps:

  1. **The nudge carries no candidate.** A ticketless "open slot + paused
     work" poke gives the coordinator nothing concrete to act on and makes
     every nudge look identical — inviting dedup-as-noise.
  2. **Clearing is unaccountable.** The coordinator can clear the nudge with
     no promoted ticket and no recorded blocking reason, and nothing
     distinguishes a deliberate hold from inaction.
  3. **Inaction never escalates.** The sweep re-nudges on cooldown forever;
     N unacted nudges with an open slot and approved eligible work is a
     starvation signal that should reach the operator, not loop silently.

  ## Live integration points (verified 2026-08-03)

  - `swarmforge/scripts/chase_sweep_lib.bb` — `open-slot-nudge-draft-lines`
    (nudge content), `decide-open-slot-nudge?` (dedup/cooldown decision).
  - `swarmforge/scripts/handoffd.bb` — `open-slot-nudge-sweep!` /
    `nudge-coordinator-open-slot!` (the production sweep wiring).
  - `swarmforge/roles/coordinator.prompt` — the never-clear-without-cause
    duty (invariant 3) lands as a prompt amendment in the same parcel; the
    specifier owns that file and will apply it when this ticket activates.

  Candidate selection in the nudge is ADVISORY: Article 3.2.4 ordering
  (expedited defects first, fail-closed on missing severity, then ticket
  priority) plus human_approval state. Orthogonality and the circuit
  breaker stay coordinator judgment — the daemon never promotes.

  ## Out of scope

  - Rotation-side starvation (BL-795, active) and stale holders (BL-794).
  - Stranded-resident detection (BL-685).
  - The depth-warning miscount (BL-683).

acceptance: specs/features/BL-798-open-slot-nudge-names-candidate.feature.draft

source: |
  Intake SUP-1 (.swarmforge/operator/INTAKE-sup-1.md, archived
  2026-08-03 with pointer to this ticket). Operator directives preserved
  verbatim per BL-680/BL-681:

  [telegram 2026-08-02T19:15:24.660Z] "Non je veux qu'elle tourne tout le
  temps. Est ce que l'operator et le babysitter arrivent à régler les
  problèmes de famine, assez courant depuis peu?"

  [telegram 2026-08-03T08:59:02.913Z] "Fix the process . If it were an
  intended pause I would have said so"

  [telegram 2026-08-03T09:00:09.063Z] "I said multiple times since
  yesterday evening I want the swarm to get back to work"

  Operator diagnostic [2026-08-03T08:36:29.160873Z]: "The coordinator keeps
  clearing handoffd's generic ticketless open-slot sweeps ('Nth identical
  nudge — cleared') without ever picking a concrete candidate. Auto-recovery
  (coder nudge loop + 5-min sweeps) has not taken. I can't promote (operator
  hard limit)."

notes: |
  ## Severity rationale

  `high`: a live fault degrading behaviour — the promotion path starved
  approved queue-jump work on an idle swarm until a human pushed, and the
  human states the famine is recurrent ("assez courant depuis peu").

  ## Disposition of the rest of SUP-1 (recorded here so the archive walks)

  - "Send me the base URL and the token bearer" — dispositioned live by the
    operator (no credentials over Telegram); the underlying need (phone
    access) shipped via BL-787 (named tunnel fixed URL) + BL-788 (pairing).
  - Operator's A-or-B question (full 7-role pack vs mono-router): answered
    by "Fix the process" — mono-router stays (BL-518 by design); the
    starvation defects are fixed instead (BL-795, BL-794, this ticket).
  - "Ok go" (full relaunch) — consumed live 2026-08-02; operator corrected
    itself and relaunched mono-router properly.

  ## e2e procedure for QA

  1. Fixture a repo with an open slot and approved paused candidates
     (one expedited defect, one higher-priority feature); run the sweep
     once (daemon poll-once); assert the delivered nudge names the
     expedited defect id and its approval state.
  2. Repeat the sweep past the escalation threshold without promoting;
     assert a promotion-inaction escalation reaches the operator channel
     and silent identical re-nudges stop.
  3. On a live mono-router: leave one approved ticket in paused/ with an
     open slot; observe the coordinator either promotes+routes or records
     a concrete blocking reason — never an uncaused clear.

  ## Invariant 3's prose half landed OUT OF BAND (specifier, 2026-08-10)

  The architect's review (evidence
  `backlog/evidence/BL-798-architect-bounce-20260810.md`, item 5) accepted
  invariant 3's non-encodability carve-out but flagged that its actual
  deliverable — the `swarmforge/roles/coordinator.prompt` never-clear-
  without-cause duty named in this ticket's own "Live integration points" —
  had not landed, and that `required_stages` structurally cannot deliver it
  (`specifier` is not a valid stage: schema allows coder/cleaner/architect/
  hardender/documenter/qa only, and the specifier is pre-pipeline by design).
  Routed to the specifier by `note` per Article 4.4.

  Delivered directly on `main` as section "## Never Clear An Open-Slot Nudge
  Without A Cause (BL-798 invariant 3, SUP-1)" in `coordinator.prompt`. It is
  written against the mechanism THIS parcel builds (verified at coder commit
  `6c97e2eeee`): the nudge message shape `open slot + paused work -
  promote+route: <BL-id>[ awaiting approval]`, and the
  `decide-open-slot-escalation` state machine's post-escalation quiet.

  Two points for QA, both derived from reading the built code rather than
  restating the invariant:
  - The sweep fires only when `active-count < cap`, so "cap reached" cannot
    honestly be the blocking reason for a nudge that was delivered.
  - After `:escalate`, `handoffd.bb`'s `open-slot-nudge-sweep!` sends the
    coordinator NOTHING further for that candidate (action `:none`) until the
    top candidate changes — so nudges going silent is not evidence the
    condition cleared.

  Durable-record location (specifier's call, amendable): the named
  candidate's own ticket YAML `notes:`, committed. Chosen because it needs no
  new machinery, is git-walkable into `done/`, and travels with the ticket;
  explicitly NOT the BL-819 lifecycle ledger, which is instrument-written
  only and has no instrument for a promotion non-decision.

  This changes NO scenario and adds NO step-handler work — the acceptance
  contract is untouched.
bounce_count: 1
bounce_history:
  - { at: 2026-08-10, by: architect, blamed: coder, class: invariant-unencoded, commit: 6c97e2eeee, evidence: backlog/evidence/BL-798-architect-bounce-20260810.md }
