id: BL-1170
title: "/postmortem operator verb — qualify outage, teach babysitterd and operator playbook"
milestone: M8
type: feature
status: todo
epic: disaster-recovery-loop
priority: 4
direction: human-requested
depends_on: [BL-698, BL-958]
mutation_cost: medium
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-1170-postmortem-operator-verb-failure-class-learn.feature

required_wiring:
  - "extension/src/tools/telegramCursorOperatorExec.ts::postmortem::register /postmortem in shared operator verb backend"
  - "specs/pipeline/steps/index.js::bl1170PostmortemOperatorVerbFailureClassLearnSteps::acceptance handler registered"

invariants:
  - "/postmortem never auto-mints BL tickets — only backlog INTAKE-disaster-* stubs (BL-311)."
  - "One postmortem pass per failure_class incident window — idempotent, no duplicate learn artefacts."

human_approval: approved
approval_context: >
  INTAKE-postmortem-operator-verb-failure-class-learn.md (human Laurent,
  2026-08-27). Learn slice of disaster-recovery loop; depends on BL-698 verb
  backend (done) and BL-958 incident record.

description: |
  ## What

  Shared operator verb `/postmortem` (soft confirm tier) that after recovery:
  qualifies cause (`failure_class`, hypotheses, evidence), updates babysitter
  failure-class registry (gitignored JSON), updates operator playbook JSON,
  and mints `backlog/INTAKE-disaster-<class>-<timestamp>.md` stub.

  Reuses `collect_daemon_postmortem.sh`, BL-958 incident record, BL-653
  escalation shape. Refuses when no recent incident in lookback.

qa_e2e_procedure: >
  (1) Starvation-cascade fixture → /postmortem writes qualified record + registries
  + intake stub. (2) Second occurrence → babysitter uses registry (with BL-1171).
  (3) No recent incident → refuse. (4) Parse-error class → human hotfix playbook.

out_of_scope: |
  - Fully automated LLM root-cause without human confirm on learn artefacts.
  - Replacing collect_daemon_postmortem.sh.

source: |
  backlog/INTAKE-postmortem-operator-verb-failure-class-learn.md (human Laurent,
  2026-08-27 ~06:44 BST). Sibling to BL-1169/BL-1171 under epic BL-1168.

notes: |
  - [2026-08-27] specifier: minted from root intake drain.

  - [2026-08-28] specifier adjudication of the coordinator's lost-QA-close note
    (.swarmforge/operator/NOTE-BL-1170-lost-close-commit-20260828.md):
    CONFIRM-CLOSE. The work SHIPPED and is live on main; only the bookkeeping
    close commit 0f3a58b71 was lost from main's line. Do NOT force-route to
    the coder - that would duplicate-rebuild shipped work, exactly as the
    coordinator suspected. Coordinator to move active/ -> done/ (Article
    1.1/3.3; not mine to do).
  - [2026-08-28] specifier: evidence for the close, checked by CONTENT and
    deliberately not by ancestry, because 0f3a58b71 is genuinely not an
    ancestor of main. (1) Both declared required_wiring anchors resolve on
    main: /postmortem dispatches at telegramCursorOperatorExec.ts:762-763 over
    an import of formatPostmortemReply/runOperatorPostmortem, and
    bl1170PostmortemOperatorVerbFailureClassLearnSteps is registered in
    specs/pipeline/steps/index.js. (2) The feature file and step handler are
    present on main and byte-identical to the QA-approved tree (24caed6d3c).
    (3) git diff 24caed6d3c main over this ticket's paths is +34/-0 - main is
    strictly AHEAD and nothing was deleted; none of the 34 added lines
    mentions postmortem, so they are unrelated later work. (4) The
    implementation is substantive, not a stub: operatorPostmortem.ts is 333
    lines on main and the dispatch is a thin wrapper over it, which is this
    project's own stated convention. (5) The step handler drives the REAL
    compiled module (out/tools/operatorPostmortem), not fakes, so the
    acceptance is not green-over-fakes. This is the OPPOSITE shape to the
    BL-1196 false positive the coordinator rightly guarded against: there a
    ticket was pooled to done/ but never built; here the build is
    demonstrably on main and only the paperwork is missing.
  - [2026-08-28] specifier: incidental finding for BL-1220, recorded here so it
    is not lost. This ticket's QA evidence records its unit lane as
    `node --test test/operatorPostmortem.test.js` (5/5 PASS), and
    extension/test/operatorPostmortem.test.js is confirmed to declare its
    tests to node:test - so those 5 assertions passed under node --test at QA
    time and are NOT collected by the Vitest unit lane today. This does not
    weaken the close (the 4/4 acceptance run is independent and drives the
    real module), but it is a concrete instance of BL-1220's defect landing on
    shipped, QA-approved work.

assigned_to: coder
