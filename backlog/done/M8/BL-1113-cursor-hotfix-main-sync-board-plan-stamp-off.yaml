id: BL-1113
title: "Swarm stamp-off: certify Cursor hotfix 27273f2b0a (main-sync deadlock, cursor-forge, board UX, CreatePlan confirm)"
milestone: M8
type: defect
severity: high
# high: live uncertified hotfix on main that never passed the normal gates;
# same bar as BL-1108. Not critical: swarm is up; the risk is unreviewed
# behaviour staying permanent without a human certification decision.
status: todo
epic: swarm-reliability
priority: 15
direction: human-requested
depends_on: []
mutation_cost: low
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-1113-cursor-hotfix-main-sync-board-plan-stamp-off.feature
human_approval: approved
approval_context: >
  This is a BL-848 hotfix stamp-off. The human decision requested is whether
  the already-landed commit 27273f2b0a is certified or waived after the normal
  review chain. Approval does not reimplement or un-land the hotfix. Pushing
  local main to origin is a separate human/QA decision — the commit is
  currently local-ahead only.
required_wiring:
  - "specs/pipeline/steps/index.js::bl1113CursorHotfixStampOffSteps::the acceptance handler must be registered so the runner executes every review scenario"
invariants:
  - "This stamp-off never reimplements the hotfix — review confirms or refutes the landed commit 27273f2b0a only."
  - "Green tests alone never write certified or waived into the hotfix ledger; only a recorded human decision does."
description: >
  WHAT. Operator/Cursor hotfix 27273f2b0a is live on local main with trailer
  Hotfix-Certification: pending. Subject: Land Cursor hotfix: main-sync
  deadlock breaker, cursor-forge, board UX, plan confirm.

  Landed behaviours under review (do not redesign):
  1. Coordinator step 0 gates on behind=0 via main_sync_status_cli.bb;
     master_main_reconcile_lib trip-once deadlock suppresses dropped-parcel
     nudges and alerts once until behind=0 (BL-891 still owns non-ff joins).
  2. Standing pack swarmforge/packs/cursor-forge.conf — depth 3, remote_control
     off, all pipeline windows on cursor (cooldown still off for the night it
     landed).
  3. Pipeline Board: escapeHtml emits &#160; for U+00A0 (not named &nbsp;,
     which Telegram shows literally); deriveKebabSlug default maxWords=3.
  4. Telegram Cursor Remote: CreatePlan progress surfaces Confirm plan /
     Reject plan buttons via telegramCursorBridgeLive + OperatorCore helpers.

  WHY. Unticketed hotfixes never become official swarm deals on green tests
  alone (BL-848). Stamp-off confirms or refutes; human certifies/waives in
  the ledger.

  HOW (direction, not mandate). Walk the acceptance feature against the
  landed paths; open narrow follow-ups for defects found; do not rewrite the
  hotfix in this parcel.

qa_e2e_procedure: >
  (1) Run master_main_reconcile_lib / main_sync_status unit runners and the
  acceptance feature. (2) With a fixture ahead/behind matrix, confirm CLI
  actions proceed|ff-only|wait-reconcile|deadlock-tripped. (3) With deadlock
  active and behind>0, confirm dropped-parcel path logs suppressed. (4) Parse
  cursor-forge.conf: standing, depth 3, remote_control off, cursor windows.
  (5) extension vitest pipelineBoard tests for &#160; and 3-word slugs.
  (6) Drive or unit-test CreatePlan progress → Confirm/Reject buttons +
  pending plan-confirm file. (7) Record degraded gate where shell/Babashka
  has no mutation/CRAP/DRY. Leave Hotfix-Certification pending until human
  ledger decision.

out_of_scope: |
  - Reverting or rewriting the hotfix.
  - Pushing main to origin (human/QA).
  - Certifying the ledger row without a human decision.
  - Unrelated provider/seat readiness work (BL-1108 lineage).

notes: >
  Source intake: .swarmforge/operator/INTAKE-stamp-off-hotfix-27273f2b0a.md
  (2026-08-24). Coordinator note: Mint stamp-off for hotfix 27273f2b0a.
  Ledger key 27273f2b0a linked via hotfix_ledger_update.bb --link after mint.
  Commit is not yet an ancestor of origin/main; babysitter Article 4.2 /
  BL-247 CRIT expected until stamped and absorbed.

source: |
  Operator intake INTAKE-stamp-off-hotfix-27273f2b0a.md; babysitter health
  sweep (BL-631 / pipeline-code-on-main) + coordinator triage.

notes: |
  2026-08-24 coordinator: babysitter CRIT resurfaced for 27273f2b0a — expected until this stamp-off lands under swarmforge-QA. human_approval now approved; promotion blocked by active_backlog_max_depth (3/3: BL-556, BL-682, BL-1089). Will promote+route on next open slot (expedited high).
  2026-08-24 coordinator: stamp-off acknowledged; ledger linked 27273f2b0a. Holding in paused — human_approval pending; active cap full (BL-556/BL-682/BL-1089). Promote after approval (expedited high defect).

assigned_to: coder
