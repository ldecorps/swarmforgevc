id: BL-9001
title: "TEST PROBE - do not implement - approval tap live QA check for BL-582"
milestone: M8
type: defect
status: blocked
epic: swarm-reliability
human_approval: pending
severity: low
priority: 99
direction: swarm-surfaced
depends_on: []
assigned_to: QA
mutation_cost: low
promotion_blockers: >
  NEVER PROMOTE. Disposable diagnostic probe created 2026-08-22 by QA to
  surface one approval ask in the Telegram Approvals topic, reproducing
  BL-582's own qa_e2e_procedure ("QA: reproduce by hand against the live
  bot"). It describes no real work. QA deletes it as soon as the probe is
  done. If you are a coordinator or specifier reading this: take no action,
  do not promote, do not spec, do not route. Mirrors BL-583's own
  now-deleted disposable probe for the same ticket.
source: |
  QA, 2026-08-22, per BL-582's own qa_e2e_procedure note: "reproduce by
  hand against the live bot - post an ask, tap Approve, and confirm both
  the yaml write and the repaint. A fixture-only pass is not sufficient
  here." Operator agreed to perform the live tap.

description: |
  Not a real ticket. Exists only to make the concierge tick post an
  approval ask into the Approvals topic (any active/paused item carrying
  `human_approval: pending` is surfaced by `pendingApprovalFor` on the next
  tick). The operator taps Approve on THIS ask while QA watches this file
  and the bot's own state/log surfaces, confirming BL-582's fix live:
  the yaml flips to `approved`, the ask's buttons strip and show the
  verdict, and (if anything goes wrong) a diagnostic is now observable
  instead of silence.

  Delete this file once the probe is done.

acceptance: |
  None. Never implemented, specced, or promoted. Its only acceptance is
  that it gets deleted after the probe.

out_of_scope: |
  Everything. See promotion_blockers above.
