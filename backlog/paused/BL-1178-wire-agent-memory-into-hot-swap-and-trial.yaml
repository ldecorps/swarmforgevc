id: BL-1178
title: "Wire agent-memory transfer into seat relaunch, hot-swap, and trial start/end"
milestone: M8
type: feature
status: todo
epic: agent-memory-transfer
priority: 4
direction: human-requested
depends_on: [BL-1177]
mutation_cost: medium
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-1178-wire-agent-memory-into-hot-swap-and-trial.feature

human_approval: approved
approval_context: >
  Second slice of agent-memory-transfer epic. Reuse BL-235 / ModelFactory /
  steward failover paths — do not invent a third assignment path.

invariants:
  - "Every same-role model switch on a wired path captures then injects before the incoming agent takes live work."
  - "A failed transfer aborts the switch with a clear signal — never leaves an amnesiac seat as success."

required_wiring:
  - "seat relaunch / hot-swap / trial lifecycle::capture then inject::BL-1177 API"
  - "specs/pipeline/steps/index.js::bl1178WireAgentMemoryHotSwapSteps::acceptance handler registered"

description: |
  Call BL-1177 capture on the outgoing agent and inject on the incoming agent
  for same-role model changes on relaunch, hot-swap, and BoB trial start/end
  (and steward cast apply where it changes the role's model).

qa_e2e_procedure: >
  (1) Fixture hot-swap → capture then inject before live work. (2) Failed
  inject aborts switch. (3) Trial start/end paths call the same API.

out_of_scope: |
  - Defining the payload schema (BL-1177).
  - Cross-vendor unsupported matrix (BL-1179).

source: |
  .swarmforge/operator/INTAKE-agent-memory-transfer.md (2026-08-27). Epic BL-1176 child.

notes: |
  - [2026-08-27] specifier: minted from operator intake drain.
