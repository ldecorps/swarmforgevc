id: BL-543
title: "EPIC — Fleet topology: secondary/headless swarms and remote-control health/ensure wiring; do not promote directly; tracks children"
milestone: M8
status: todo
type: epic
epic: fleet-topology
priority: 1
depends_on: []
decomposes_into: [BL-101, BL-514, BL-622, BL-628]
source: "Human hygiene 2026-07-19: every ticket must have an epic; every epic must have a milestone. Tracker created during open-backlog cleanup."

notes: |
  UMBRELLA / tracking item (type: epic), NOT directly promotable — the coordinator
  promotes CHILD slices, never this. Children carry `epic: fleet-topology` and are
  listed in `decomposes_into`.

  VISION: multi-host / secondary swarm topology and remote-control health stay
  ticketed and ensure-able, not tribal knowledge.

  2026-08-18 21:33 coordinator: handoffd's open-slot nudge (000496) named this
  as the top candidate by priority, but it is an epic tracker — never directly
  promotable per this ticket's own title/notes. No action taken on it. The
  open slot that triggered the nudge was independently filled by BL-930
  (promoted same evening, ~21:33) before this note was processed.

  2026-08-18 22:10 coordinator: same nudge repeated (000501) — same non-reason
  (epic, not directly promotable). Slot again independently filled, this time
  by BL-929 (~22:13), before this note was processed. The nudge-ranking logic
  does not appear to exclude type:epic from "top candidate by priority" — a
  ranking gap, not a stuck-work signal; BL-543 itself is not starved since it
  was never eligible to promote in the first place.

  2026-08-19 00:49 coordinator: same nudge repeated a third time (000511).
  Same non-reason, same outcome — slot independently filled by BL-927 (~00:49)
  before this note was processed. Not re-explaining further on recurrence;
  see the two entries above for the standing root cause (epic type excluded
  from promotion, nudge-ranking logic doesn't filter it out).

  2026-08-19 00:55 coordinator: 4th repeat (000514), same non-reason, slot
  filled by BL-909 first.

  2026-08-19 02:38 coordinator: 5th repeat (000517), same non-reason, slot
  filled by BL-920 first. This is recurring reliably enough that the
  underlying nudge-ranking gap (exclude type:epic from "top candidate")
  is worth the specifier minting a real fix rather than more hand-recorded
  entries here.

  2026-08-19 05:11 coordinator: 6th repeat (000529) — this time a genuine
  open slot (cap raised to 4), not already filled. Same non-reason (epic,
  not directly promotable) applies regardless; promoted BL-915 (top
  eligible non-epic defect) into the slot instead. Flagged in today's
  briefing (docs/briefings/2026-08-19.md) for a real fix.

  2026-08-19 06:05 coordinator: 7th repeat (000535), same non-reason, slot
  filled by BL-938 first.

  2026-08-19 21:28 coordinator: 8th repeat (nudge "BL-543 awaiting
  approval"). THREE specific blockers, none of them "not now":
  (1) `human_approval` is ABSENT from this ticket entirely — not pending,
  not rejected, simply never recorded — so the approval gate holds it
  closed. This is the one a human can clear.
  (2) `type: epic` with `acceptance: none` ("tracker only — see
  decomposes_into children"). Even approved it is NOT routable as a parcel:
  routing a tracker to the coder hands them no acceptance contract. Its
  CHILDREN are the promotable units, and they carry their own approval.
  (3) The slot that triggered this sweep was filled in the SAME turn by
  BL-910 (23f70be5f), leaving active 7 = cap 7.
  Blocked on a human for (1) and on decomposition for (2) — not asleep.
  Recorded per SUP-1 rather than cleared as a duplicate.

acceptance:
  none: "tracker only — see decomposes_into children for acceptance"
mutation_cost: high
