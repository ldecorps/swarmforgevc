id: BL-1117
title: "Swarm stamp-off: certify tip 646ffe85d (Pipeline Board numeric &#160; entity)"
milestone: M8
type: defect
severity: medium
# medium: QA-exclusive pipelineBoard path landed outside swarmforge-QA;
# babysitter Article 4.2 / BL-631 CRIT. Not high — board still posts; the
# defect is literal "&nbsp;" on some clients until numeric entity is stamped.
status: paused
epic: swarm-reliability
priority: 18
direction: human-requested
depends_on: []
mutation_cost: low
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
acceptance: specs/features/BL-1117-swarm-stamp-pipeline-board-numeric-nbsp.feature
human_approval: approved
approval_context: >
  BL-848-style stamp-off for tip 646ffe85d (undeclared on ledger at mint —
  register then link). Human certifies or waives. Follow-on to certified
  35c36037ff / BL-1113: named &nbsp; is not in Telegram's HTML entity set;
  numeric &#160; is. Do not reimplement.
required_wiring:
  - "specs/pipeline/steps/index.js::bl1117PipelineBoardNumericNbspStampOffSteps::acceptance handler registered so the runner executes every review scenario"
abandoned_commits:
  - 0688be054
  - 464b09288

invariants:
  - "This stamp-off never reimplements the tip — review confirms or refutes landed commit 646ffe85d only."
  - "Green tests alone never write certified or waived into the hotfix ledger; only a recorded human decision does."
description: >
  WHAT. Commit 646ffe85d is on local main and touches QA-exclusive paths
  (pipelineBoard.ts / test / bl1113CursorHotfixStampOffSteps.js) without a
  Hotfix-Certification trailer. Subject: Fix Pipeline Board: emit &#160; so
  Telegram renders NBSP as a space. Body notes human already certified
  27273f2b0a and 35c36037ff; this tip replaces named &nbsp; with numeric
  &#160; because Telegram's allowed named entities are only &lt; &gt; &amp;
  &quot;.

  WHY. Babysitter check 13 (BL-631) flagged the tip as pipeline code on main
  outside swarmforge-QA. Stamp confirms or refutes; ledger must gain a row
  (register via --new) then link this ticket.

  HOW (direction, not mandate). Walk the acceptance feature against
  escapeHtml; do not widen into board redesign.

qa_e2e_procedure: >
  (1) extension vitest pipelineBoard: escapeHtml maps U+00A0 to the
  characters &#160; (ampersand hash 160 semicolon), never the named entity
  string &nbsp;. (2) Stage header between DC and QA uses that numeric
  entity. (3) Register ledger row if missing, leave decision pending until
  human. (4) Do not re-open certified 35c36037ff / BL-1113 except as
  provenance notes.

out_of_scope: |
  - Reverting or rewriting the tip.
  - Re-certifying 35c36037ff / 27273f2b0a.
  - Pushing main to origin.
  - Operator freshness tofu glyphs (separate intake).

notes: >
  - [2026-08-25T09:50Z] coordinator: specifier ready notice ack; hold — human_approval pending (no promote).
  Source intakes: backlog/INTAKE-stamp-off-hotfix-646ffe85d-nbsp.md,
  .swarmforge/operator/INTAKE-stamp-off-hotfix-646ffe85d-nbsp.md,
  referenced from .swarmforge/operator/INTAKE-stamp-off-recent-hotfixes-20260825.md.
  Related certified: 35c36037ff via BL-1113 (named-entity era).

source: |
  Babysitter CRIT + operator stamp-off intake 2026-08-24 for tip 646ffe85d.

assigned_to: coder
