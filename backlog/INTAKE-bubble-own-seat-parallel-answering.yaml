# Operator intake — raw human request. Specifier: assign a BL id and formalize
# into paused/. Do NOT mint the id here (live specifier + pilot BL-1251 in flight
# → collision risk). Filed by the operator on 2026-08-28 on the human's request.
id: INTAKE-bubble-own-seat
title: "Bubble gets its own answering seat so it can respond in parallel while cursor is busy"
milestone: M3            # remote-control surface; sibling of BL-1235. Specifier to confirm.
status: todo
priority: 50             # enhancement, not a defect. Specifier/human to re-rank.
assigned_to: specifier

source: >
  Operator conversation, 2026-08-28. Human observed Bubble (the operator phone app)
  going quiet while the single Cursor answering seat was busy running the BL-1248
  pilot's long gherkin-mutation/property sweep. The tunnel/bridge were healthy the
  whole time (fresh heartbeat, offset advancing) — the responder was simply occupied.
  Human asked whether Bubble could have its own seat to run in parallel.

description: >
  Today Bubble is a MIRROR topic (extension/src/bridge/bubbleMirrorTopic.ts). There is
  exactly ONE answering seat — the live Cursor agent session
  (extension/src/bridge/cursorBridgeAgentSession) — and both the cursor host topic
  (8435) and the Bubble topic (11810) route to it. So whenever the Cursor seat is busy
  (e.g. a piloted expedite), Bubble cannot answer: same responder, one turn at a time.

  The request is to give Bubble a dedicated answering seat so it can respond in parallel,
  leaving cursor's host topic and the front desk untouched — the SAME shape as BL-1235
  ("A local qwen seat answers in its own dedicated messaging topic"). Ride on BL-1235's
  seat/lifecycle machinery rather than inventing a parallel mechanism.

open_question_for_human: >
  DECISION NEEDED before speccing — this changes the goal:
  (A) Bubble becomes an INDEPENDENT responder — answers on its own, in parallel, and may
      diverge from what the human sees at the front desk (a genuine second brain); or
  (B) Bubble STAYS a mirror of the front desk but gets a DEDICATED WORKER so the mirror
      stays live/answerable even while cursor is busy (lighter; keeps "Bubble = phone
      view of the front desk").

acceptance:
  - "Bubble answers in its own topic even while the Cursor host seat is busy with other work (parallelism demonstrated)."
  - "Cursor's host topic and the front desk are never served by the Bubble seat, and vice versa (no cross-answering)."
  - "A Bubble seat that cannot answer says why in its own topic; it never fails silently and never hands the turn to another seat."
  - "No regression of the Telegram 409 / double-poller or dead-feeder classes: only one getUpdates owner; the second seat does not open a competing poller."
  - "The new seat is supervised/kept-alive like the existing bridge (watchdog covers both seats)."

related:
  - "BL-1235 (paused) — local qwen dedicated-topic seat; shares the seat/lifecycle machinery this should reuse."
  - "Dead-feeder / 409 bridge stability work (commits 2ec06b6ef1, 5045fae371) — the concurrency hazards a second seat must not reopen."

notes: >
  Operator did NOT implement or pre-spec this (out of bounds without a spec). Feasibility
  read given to the human: conceptually straightforward and already patterned on BL-1235,
  but a real ticket, not a quick edit — a second agent session bound to the Bubble topic,
  a model/seat choice, lifecycle/supervision, and care not to reopen the 409/dead-feeder
  bugs the bridge is still stabilizing (3 restarts in the hour before this was filed).

# ── specifier status (appended 2026-08-29 ~02:00Z) ────────────────────────
# NOT YET SPECCED. Blocked on the human, deliberately.
#
# The open_question_for_human above is a real fork, not a detail: (A) makes
# Bubble an independent responder that may diverge from the front desk, (B)
# keeps it a mirror and only gives it its own worker. The two produce
# different acceptance scenarios, so guessing one would mint the wrong ticket.
#
# Raised via role_ask into the specifier topic on 2026-08-29 (slot was free;
# not a re-ask). Awaiting the tap.
#
# Premise verified before asking, so it does not need re-checking on resume:
#   - extension/src/bridge/bubbleMirrorTopic.ts exists and routes through
#     telegramCursorBridgeCore's topic map
#   - .swarmforge/operator/cursor-bridge-topic-map.json reads
#     {"8435": "CURSOR_REMOTE", "11810": "BUBBLE"} — two topics, and
#     BUBBLE_SUBJECT_ID / CURSOR_BRIDGE_SUBJECT_ID both resolve to the one
#     Cursor seat, exactly as the intake describes
#   - no existing paused/active/held ticket covers a Bubble answering seat
#   - BL-1235 (paused, approved) is the sibling to ride on: it adds a THIRD
#     subject id (QWEN_LOCAL, topic 41004) to that same map, with its own
#     seat and turn loop. Its notes now carry the human's model-tag answer
#     (qwen3:14b) and a measured ~2.8 tok/s on this host — a slow seat is a
#     live design input for any second seat's turn loop.
#
# ON RESUME: mint as BL-1255+1 or later (BL-1255 is taken as of this note —
# check for the next free id, do not reuse), epic likely M3 per the intake's
# own milestone line, and carry the acceptance list above forward. Article 5.3:
# the human's sentences in source: and the operator's directive text survive
# verbatim into the resulting ticket.
#
# ── premise refresh (appended 2026-08-30 ~06:00Z) ─────────────────────────
# STILL BLOCKED ON THE HUMAN. `deliver-role-answer.js --role specifier`
# returns {"kind":"already-consumed"}, which means the store holds an OLDER
# already-paired answer and the A/B question above is genuinely unanswered.
# Marker deliberately LEFT in place — archiving it would discard a live
# question and free the slot for a duplicate re-ask. Do not re-ask.
#
# One premise moved, and it does NOT change the question: BL-1235 has SHIPPED
# (backlog/done/BL-1235-local-qwen-seat-behind-its-own-topic.yaml, QA-passed
# 2026-08-30). The intake and the note above both describe it as "paused,
# approved". The seat/lifecycle machinery this intake was told to ride on is
# therefore REAL now rather than pending, which strengthens options A and B
# equally — the fork is untouched, so the question stands exactly as posed.
# Re-read BL-1235 in done/ (not paused/) when speccing.
