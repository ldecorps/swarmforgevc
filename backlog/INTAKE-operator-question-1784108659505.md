# Intake: a question the Operator could not answer

Filed by the Operator (2026-07-15T09:44:19.506215729Z) - a question came in via Telegram
that the Operator judged it could not answer itself. This is a RAW
ask, not a spec: the specifier drains this like any other backlog-root
item and decides what (if anything) becomes a real ticket.

## The question

Human request (implement ASAP): build an efficient way to approve tickets that are awaiting approval (human_approval: pending-review) directly from Telegram — e.g. an approve/amend/reject action from the front desk on tickets sitting in the backlog, without needing the CLI. Context thread SUP-6 (also asked re BL-405 onboarding approval).
