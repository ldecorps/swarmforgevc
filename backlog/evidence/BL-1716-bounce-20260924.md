# BL-1716 — architect review pass, 2026-09-24 (send-back)

Reviewed commit: 418bd45cb8 (coder, "a land replay recovers the scratch a
killed land left behind"), merged via cleaner's 6cda827b0b (NONE) into
architect.

## D1 — the amended Scope's shell-test edits were never delivered

The ticket was amended (specifier, d29810cd56, ~13:40Z) on the coder's own
spec-gap note 002121: Scope widened to include two stale assertions in
`swarmforge/scripts/test/test_bl1366_land_is_one_command.sh` (case 1 -
assert the land's PUBLISHED commit reaches origin/main, not the cited SHA
verbatim; case 5 - the second land lands a FRESH approved commit, not a
repeat of the same SHA). qa_e2e_procedure step 2 requires this file to run
with no FAIL line.

The coder's own commit (418bd45cb8) predates that amendment and correctly
raised the spec-gap in the first place; cleaner's pass (6cda827b0b) merged
main (which carries the amendment text) into its own branch but did not
notice the amended Scope was still unmet, and recorded NONE.

Verified by running the file at the parcel commit:

    bash swarmforge/scripts/test/test_bl1366_land_is_one_command.sh

Two FAIL lines remain, exactly as the coder's own spec-gap evidence
(`backlog/evidence/BL-1716-spec-gap-shell-test-residual-20260924.md`)
predicted and analyzed correctly:
1. `FAIL: origin/main is <published-sha>, expected <cited-sha>` (case 1's
   stale SHA-equality assertion).
2. `FAIL: the next land could not proceed: ... entangled tip ... specifier
   adjudication needed.` (case 5's second call, now correctly finding
   "nothing to commit" for a repeat land of an already-landed commit -
   land-plan's own pre-existing BL-1474 answer, wrapped by
   `land_main_publish.sh` as an escalation).

The `replay!` fix itself (the ticket's core deliverable - scratch/branch
leftover recovery keyed on an owner {pid, start-ms} record) is correct and
complete: all three of the ticket's own Gherkin scenarios pass (3/3), the
declared invariant holds by inspection (a live or unestablished owner is
never touched; a dead leftover is cleared; every create-failure reason now
carries git's own stderr), and BL-1678/BL-1241/BL-1713's own regression
features are unaffected (4/4, 4/4, 4/4). Only the amended Scope's
test-file edits are missing.

Remediation: apply the two assertion rewrites the amendment's own Scope
text and the coder's spec-gap evidence already specify - case 1 checks the
published commit's ancestry/tree against the approved commit rather than
raw SHA equality; case 5's second `run_land` assertion checks for a benign
"nothing to commit" outcome (not a FAIL) for a repeat land of an
already-landed ticket+commit. Both are test-only, scoped exactly as the
amendment states.

By architect.
