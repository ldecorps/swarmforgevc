# BL-1308 — architect pass, 2026-08-31

Task: `BL-1308-an-unlanded-sibling-on-a-second-parent-is-invisible`
Received: cleaner's `51b9a74b78` (verdict NONE, forwarding coder's `f3166a3fd5` unmodified).

## Verdict: CLEAN — forward to hardener

### Architecture rules
- No `extension/src` or webview code touched at all — the change is confined
  to `swarmforge/scripts/land_step_lib.bb` (a Babashka script, no
  mutation/CRAP/DRY tooling wired, gated by its own unit suite per Article
  Startup Tools), plus its `.bb` test runner, an acceptance feature/handler
  pair, and a coder-owned property test.
- Two-layer boundary, extension-host-owns-I/O, no-webview-storage,
  integrate-not-fork: all N/A — no VS Code extension code in this diff.
- The fix itself is a single flag removal (`ancestry-commits` drops
  `--first-parent`) plus a docstring explaining why; `own-commit-changed-paths`
  / `task-tagged-changed-paths` are deliberately untouched, matching the
  ticket's own constraint (BL-1297's fix must stay intact).

### Hard gate — dependency-rule checker (BL-259)
`cd extension && node out/tools/dependency-gate.js test/bl1308SiblingDetectorCoversReplay.property.test.js`
-> **PASSED: no forbidden edges.** (Only in-scope file under `extension/`;
the rest of the diff — `.bb`, `specs/pipeline/steps/*.js`,  fixture `.sh` —
is outside the cruiser's TS/webview scope.)

### Co-change tool (BL-255)
`co-change-report.js` against the property test file: every other changed
file (`land_step_lib.bb`, its test runner, the step handler, `index.js`,
the fixture CLI, the evidence file) shows exactly 1 co-change — below the
default frequency-3 threshold. No suspected logical coupling to flag.

### Invariants review (BL-633/654)
Both declared invariants carry a coder-authored `*.property.test.js`
(`test:properties` lane), non-vacuous by the break-then-fix discipline: the
coder temporarily restored `--first-parent` and both properties + 4/5
acceptance scenarios went red naming the real defect, then were restored
green. Independently re-ran here:
- `npx vitest run --config vitest.properties.config.mjs test/bl1308SiblingDetectorCoversReplay.property.test.js` -> 2/2 pass.
No hand-verification of the property needed beyond confirming the test
exists and bites — both confirmed.

### Acceptance
`specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1308-….feature`
-> 5/5 pass. Scenario Outline `<position>` validated against explicit
`KNOWN_VALUES` (`POSITIONS` in the step handler) — house rule satisfied.
Handler registered in `specs/pipeline/steps/index.js:905` (`bl1308…`) —
verified directly, not just cited.

### Unit suite
`bb swarmforge/scripts/test/land_step_lib_test_runner.bb` -> ALL PASS.

### Pre-existing unowned red (coder's claim, verified not mine to ticket)
Coder's evidence names 26 unrelated `npm test` failures, root-caused to
`checkOrphanedAuthoredDocs` missing from test fakes (BL-757 follow-on).
`grep -rl checkOrphanedAuthoredDocs backlog/` finds it already tracked at
`backlog/paused/BL-1221-pilot-gate-deps-stubs-missing-required-orphan-docs-check.yaml`
— not unticketed, no note needed.

### Correctness read
No defect spotted beyond architecture/invariants. The chosen shape (widen
detection, shape 1) is squarely the one the ticket's own acceptance file
already commits to (3 of 4 original scenarios end in "the sibling ticket is
named in the report") — consistent, not a unilateral pick.

No spec gaps, no send-back. Forwarding to hardener with the same task name,
commit `51b9a74b78` (the merge I received).

By architect.
