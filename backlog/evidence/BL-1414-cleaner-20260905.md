# BL-1414 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed `freshness_announce_lib.sh`'s `announce_transition_only`: the
  first-tick-in-violation branch (`[ ! -f "$_at_state" ]`) always writes
  fresh state and prints `announce` unconditionally before any window
  arithmetic runs (invariant 2); the `fresh` action branch clears state and
  reports `recovered <secs>` only when a violation was on record, `none`
  otherwise; state is one plain file per (daemon, reason) under
  `.swarmforge/daemon/freshness-announce/`, read fresh on every call — no
  process memory (invariant 3).
- Reviewed the three `daemon_log_freshness_check.sh` call sites (fresh,
  escalate, restart): each keeps its existing unconditional
  `append_incident` call BEFORE the transition decision — the durable
  incident log is never thinner than before (invariant 1), matching the
  ticket's own explicit "append BEFORE any announce" direction.
- Confirmed the same 4 test failures reported both with and without this
  ticket's changes (temporarily restored the pre-BL-1414 test file in
  place and re-ran it): `BL-796-01/02/03` (nvm-fake-HOME resolution) and
  `fleet-telegram: default announce invoked curl` — genuinely pre-existing,
  unrelated to this parcel.
- `jscpd` (which, unlike the `.bb` files reviewed in sibling tickets this
  session, actually parses these `.sh` files) reports 0 clones across both
  changed files — the escalate/restart branches' shared `case` shape is a
  few lines below its duplication threshold and is itself the correct,
  minimal repetition for two structurally distinct call sites.
- Shell scripts have no mutation/CRAP/DRY tooling wired (BL-472 deferred,
  same posture as Babashka) — this pass is gated by the unit/property/
  acceptance suites only, per constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bash swarmforge/scripts/test/test_daemon_log_freshness.sh` | new BL-1414-01..05b: 13/13 ok; same 4 pre-existing unrelated failures confirmed present before this ticket too |
| `bash swarmforge/scripts/test/bl1414_freshness_announce_digest_property_runner.sh` | ALL PROPERTIES HOLD, 200 runs (slow — ~2 min, spawns a genuine subprocess per P2 case; not a hang) |
| `node specs/pipeline/cli.js specs/features/BL-1414-...feature` | 6/6 scenario runs |
| `bb swarmforge/scripts/test/daemon_log_freshness_pulse_lib_test_runner.bb` (regression) | ALL PASS |
| `bash swarmforge/scripts/test/test_stop_swarm_freshness_cron.sh` (regression) | ALL CHECKS PASSED |
| `bash swarmforge/scripts/test/test_start_ancillary_services_freshness_cron.sh` (regression) | ALL CHECKS PASSED |
| `grep -n announce_transition_only daemon_log_freshness_check.sh` | 3 call sites (fresh/escalate/restart), required_wiring |
| `bl1414FreshnessAnnounceDigestSteps.js::registerSteps` exists | yes (required_wiring) |
| `jscpd` on both changed files | 0 clones |

No fix required. Forwarding unchanged to architect.
