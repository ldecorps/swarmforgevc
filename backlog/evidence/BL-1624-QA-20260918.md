# BL-1624 — QA review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Sibling check: `qa-sibling-check.js status --ticket BL-1624` -> `VERIFY
BL-1624`. Register: `test_bl1388_land_step_guard_fixture.sh` row present,
owned by BL-1624 (as expected before landing).

- Wiring gate: `pre_qa_gate.sh` -> OK.
- qa_e2e step 1: `bash swarmforge/scripts/test/test_bl1388_land_step_guard_fixture.sh`
  once -> `ALL PASS`, exit 0.
- qa_e2e step 2: fresh `git clone` of the parcel with no `extension/out`
  -> exactly one `FAIL: extension/out missing ... run 'npm run compile'`
  line, exit 1, no runner assertion printed (the loud precondition, not
  a skip).
- Acceptance: BL-1624 3/3. Regression BL-1388 4/4, unchanged.
- Census guard: `test_standing_shell_tests_never_diff_main.sh` ->
  `PASS: no standing test asserts a diff against main/origin-main
  (population: 520)`.
- Register: `test_bl1388_land_step_guard_fixture.sh` row present before
  land (expected — BL-1631's automation retires it on THIS land, not
  before); left for the land step per the now-standard automation.
- Unit suite: `npm test` run twice.
  - First run: one genuine test FAILURE —
    `bl968StepRegistryMaterializedTreeGuard.test.js` invariant 2 timed
    out at 60000ms in-suite (96.7s total for the file, its highest
    in-suite reading yet). This file is an ALREADY-ACCEPTED pole
    (BL-1629 disposition, 69900ms baseline, by design: two full
    step-registry loads) with no relation to BL-1624's diff
    (`git diff origin/main..HEAD` empty for both this file and its
    setup helper). Solo re-run: 3/3 green, invariant 2 at 28.2s — well
    under its own 60s timeout. Host load at the time: 10.24 average,
    17 concurrent vitest/stryker processes from other worktrees.
  - Second run (same host, contention easing): 626/626 files,
    10690/10690 tests, "suite file budget OK: 626 files, all within
    7.0s" — fully clean, including `telegramFrontDeskBotCli.test.js`
    correctly reading as "watch" tier (9.7s) rather than the
    contention-driven "new-pole" misread the first run's heavier load
    produced (BL-1633's own confirm-alone fail-safe: a timed-out
    confirmation counts as over-budget by design).
  - Treated as host-load noise, not a regression: two different files
    flagged across two runs, neither touched by this parcel, both clean
    when re-checked.

By QA.
