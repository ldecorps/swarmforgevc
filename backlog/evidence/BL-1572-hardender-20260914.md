# BL-1572 — hardender review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- `mutation_cost: low` per the ticket: no `src` touched (`git diff main
  --name-only -- extension/src` empty), so no Stryker/CRAP/DRY target —
  `npm run dry`/`crap` both scope to `src` only. This is a test+step-handler
  parcel; hardened by hand per the BL-638 fallback.
- Load checked before running (`uptime`: 1.48/2.59/3.80 on 20 cores) — well
  under the 2x-cores bypass threshold, no deferral needed.
- Property file alone, 15/15 green
  (`for i in $(seq 1 15); do npx vitest run --config
  vitest.properties.config.mjs test/bl1429StandingRedThrottleFoldInvariants.property.test.js;
  done`), no FAIL, every run printing
  `BL-1572 reach map (invariant 1): {"combinations":12,"minDrawsPerCombination":5}`.
- Break-then-restore, run myself (not just re-reading the coder's evidence):
  narrowed the outer loop's `reworkCat` iteration to `['none','degraded']`
  only (leaving `ALL_COMBINATIONS`/`REWORK_CATEGORIES` untouched, unlike a
  naive edit that shrinks both and would pass vacuously) — first run failed
  `expected all 12 combinations reached, got degraded:age,degraded:count,
  degraded:none,degraded:unowned,none:age,none:count,none:none,none:unowned`
  (the `seen.size` assertion, ahead of `assertReachFloor`, matching the
  ticket's QA e2e step 3). Restored from a pre-edit copy; `diff` confirmed
  byte-identical restore.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1572-*.feature` — 5/5 scenarios pass (01; 02's two
  Examples rows; 03's two Examples rows).
- `swarmforge/scripts/check_feature_handler_registration.sh`: exit 0.
- Whole-tree standing guards (Article 4.4-adjacent rule for any parcel
  touching `extension/test/`): all 18 non-property `*Guard*.test.js` files,
  183/183 tests green (`bl968StepRegistryMaterializedTreeGuard`,
  `bl1277UnscopedStepCollisionGuard`, `tmuxReaperGuard`, `tmpDirMigrationGuard`,
  etc.).
- Confirmed via `git diff main` hunk boundaries that invariants 2 and 3 are
  byte-identical to `main` — the diff's second hunk closes before the
  "invariant 2" comment block.
- Register row `backlog/standing-reds.tsv:47` for this file is unchanged
  (left for QA to remove at land, per the ticket's own notes) — confirmed
  present, not touched by this pass.
- No orphaned test/mutation processes before or after
  (`pgrep -fl 'node --test|stryker'` clean both times).

By hardender.
