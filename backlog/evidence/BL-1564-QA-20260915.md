## Detail

Commands run, in order, from `.worktrees/QA`:
1. `git merge documenter 7ae9da54ce` — merged clean, no conflicts.
2. `pgrep -fl 'node --test|stryker'` before/after — none.
3. `npm run compile` (extension) — clean, tsc + postcompile OK.
4. `npx vitest run --config vitest.properties.config.mjs test/bl1297MergeOwnPathsInvariants.property.test.js` — 3/3 passed, 6.36s total (was 18.8-20.6s / 98 bb processes before the fix); reach maps logged per invariant (21/18/18 cases).
5. `npm test` (full unit suite, extension) — 616 files, 10479 tests: 10478 passed, 1 failed (`test/telegramCursorOperatorExec.test.js` BL-1204, `ENOTEMPTY` tmpdir race in `sweepPendingTmpDirs`). Re-ran that file alone: 24/24 passed. Not touched by this parcel's diff (only property test + step handler + feature + evidence files changed); no standing-red register row and no open ticket found via `grep -rl telegramCursorOperatorExec backlog/`. Transient cleanup race under parallel run, not attributable to BL-1564 — not bounced, not re-ticketed (passes deterministically alone).
6. `npm run test:properties` (full property lane, 404 files) — 1193 tests, all passed. 2 unhandled `[vitest-worker]: Timeout calling "onTaskUpdate"` errors — the allowlisted BL-871 benign signature.
7. `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1564-the-bl1297-property-costs-one-bb-process-per-invariant.feature` — 5/5 scenarios pass (green-on-tree, bb-process bound, and 3 per-invariant judged-every-case checks).
8. `bash swarmforge/scripts/pre_qa_gate.sh . HEAD` — OK (`required_wiring` confirmed: the feature's handler is in the same parcel).

Invariant honored: `extension/test/bl1297MergeOwnPathsInvariants.property.test.js` now starts one bb process per invariant (3 total, confirmed by acceptance scenario 2) instead of 98; every constructed case and random draw is still judged (scenario 3-5); no bb library touched; no timeout raised; not added to `property_suite_standing_allowlist.tsv`.

Ancestry: `git merge-base --is-ancestor 7ae9da54ce9 <this commit>` holds (documenter's hardener/cleaner/architect/coder chain is the merge parent).

Housekeeping: removed the now-green `BL-1564` row from `backlog/standing-reds.tsv` in this commit (register comment: "A row is removed in the same land that turns the test green").
# BL-1564 — QA review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.
