# BL-1610 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second hardener pass, after the coder's rebuild (`c9cf7ba477`, via cleaner
and architect) for the D1 spec-gap this same pass bounced earlier today
(`BL-1610-bounce-20260917.md`).

- Re-reproduced D1 directly on the amended build:
  `bb merge_drop_guard_lib.bb .worktrees/documenter 4356ab57c1 6ad1d3f616
  073a34b5e1` now returns nothing (the finding on the cleaner's merge
  `6e5087cd43` is gone entirely, not merely excused); the stampless
  fallback (no head) is unchanged (one excused finding on `c96761faa1`,
  0 blocking) — confirmed fixed, not merely re-asserted from the coder's
  own evidence.
- `merge_drop_guard_lib_test_runner.bb`: green (carries this hardener's
  own commit `648e7bc93e` additions from the first pass, undisturbed by
  the rebuild).
- `bl1576_merge_drop_guard_property_runner.bb`: 2000 runs, ALL PROPERTIES
  HOLD, unaffected (3-arity callers unchanged).
- `bl1610_merge_drop_gate_scan_bound_property_runner.bb` (new this
  rebuild): 3 real-git-fixture cases, ALL PROPERTIES HOLD.
- Acceptance: `BL-1610-*.feature` 6/6 (including the new row 5);
  `BL-1576-*.feature` 11/11 unchanged.
- Hand-mutation, independently re-verified (not trusting the coder's own
  claims): dropping `^head` alone (keeping only `^received`) is killed by
  BOTH this hardener's own unit test (case from the first pass) AND the
  new property runner's invariant 1(b); dropping `^received` alone
  (keeping only `^head`) survives this hardener's own unit test — real
  gap in the FIRST pass's fixture (received was already dominated by
  head there) — but is killed by the new property runner's invariant
  1(a). Together the two test files cover both negative refs
  independently; neither alone would have.
- `excused-by-blob-identity?` unchanged (`and`, not `or`) — re-confirmed
  present.
- Standing whole-tree guards: 17/18 pass; the one failure
  (`socketFixtureShortRootGuard`) is the same pre-existing, already-owned
  BL-1605 defect (register row present, QA bouncing it to coder) — unrelated
  to this parcel's own new step-handler additions, which are clean.
- CRAP/DRY: not applicable — Babashka, no tool wired (BL-472).
- No orphaned test/mutation processes before or after (other roles' live
  vitest processes at load 5.28/20 cores were untouched).
