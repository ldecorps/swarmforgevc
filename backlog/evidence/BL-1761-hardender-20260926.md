# BL-1761 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

### The ticket's own required gate (What is wanted, item 2)

`node specs/pipeline/cli.js specs/features/BL-1761-...feature`: 2/2 pass.
`npx vitest run test/confirmPoleAloneOutcomeShapes.test.js`: 3/3 pass.
`npx vitest run --config vitest.properties.config.mjs test/bl1761ConfirmPoleAloneSandboxInvariant.property.test.js`: 1/1 pass.
All 22 `test/*Guard*.test.js` whole-tree guard files: 211/211 pass.

Ran the ticket's own required real `npx stryker run --mutate out/<file>.js`
dry-run gate, target `out/util/envFlag.js` (11-line file, chosen small to
minimize instrumentation cost — coverageAnalysis:perTest still runs the
FULL suite once regardless of mutate target).

**Before the fix reached this file's own bug** (first live sandbox run,
02:31:14): the dry run failed exactly as the ticket predicts —
`confirmPoleAloneOutcomeShapes.test.js`'s third case failed:
`expected both forms to succeed, got: {"viaRel":{"ms":...},"viaAbs":{"failed":"no entry for .../.stryker-tmp/extension/test/bl1007ContentionBudgetSmoke.test.js in the confirmation report"}}`.
Root cause traced to the TEST FILE ITSELF (not `recordTestDuration.js`,
already fixed by the coder): its own `repoRoot = path.join(__dirname, '..', '..')`
is the same BL-1066 walk-up hazard, landing one level too shallow in the
sandbox. Fixed in commit `8bd7b1f566` (this pass) by building the absolute
path directly from `__dirname` (the target is this file's own sibling) —
no assertion weakened, no skip/exclude, per the ticket's FIRM constraint.

**After the fix** (three further live sandbox runs, `--dryRunOnly` and one
full `--mutate` attempt, 02:57–03:14): `confirmPoleAloneOutcomeShapes.test.js`
is NEVER again named among the failures. The dry run does still fail on
each of these runs, but on OTHER, unrelated, pre-existing files — proof the
fix holds, not that the sandbox is now clean end-to-end (it is not, see
census below).

### Census of other sandbox-fragile sites (out of scope per the ticket)

The ticket's Scope explicitly excludes "other `path.join(ROOT_DIR, '..')`
sites (the hardener lists any it meets in evidence, as a census)". Met:

1. `extension/test/activePoolFreshnessAudit.test.js`'s `checkFreshnessViaCli`
   — ALREADY a documented, accepted standing Stryker-only red (see the
   file's own comment, and hardender.prompt's BL-1066 §"`git rev-parse
   --show-toplevel` is the WRONG repair for it"). Its comment says to
   "exclude this file when running Stryker" but no such exclusion is
   actually wired into `vitest.config.mjs` or `stryker.config.json` —
   this is a real, separate gap (the dry run has never been able to
   complete a full run since this comment was written) but fixing the
   wiring is a scope decision for the specifier, not mine to make here.
2. **NEW**: `extension/test/bl1418RoleEnumerationClassification.test.js`'s
   "BL-1418: roleTopicMapStore.ALL_SWARM_ROLES names art-director" case
   failed with `ENOENT: ... open '.../.stryker-tmp/extension/src/concierge/roleTopicMapStore.ts'`
   — the same BL-1066 class, a different site, not previously documented.
3. A repo-wide grep (`grep -rln "path.join(__dirname, '..', '..')" test/ src/`)
   found ~180 further files using the same two-parent walk-up shape. Most
   are `*.property.test.js` (excluded from this vitest config's default
   test run, so invisible to Stryker's dry run) or reach a repo-root
   sibling Stryker never mutates (the documented-safe case) — not
   individually verified for sandbox-safety here; flagging the SCALE of
   the pattern is itself the finding. Recommend a specifier-owned
   follow-up ticket to audit/fix these systematically rather than
   whack-a-mole one per Stryker run (~9 min per full-suite dry-run
   iteration makes exhaustive per-file verification impractical for one
   hardening pass).

### Verification method note

Isolated the fixed file's behavior from the two unrelated standing reds by
TEMPORARILY adding them to `extension/vitest.config.mjs`'s `test.exclude`
for verification only, confirmed the fix holds, then restored the file to
its exact original committed content (`git status --short` clean on it
before committing only the test-file fix). No config change is included
in this parcel's commit.

CRAP/DRY: N/A — no new `src/*.ts` production logic; the fix is a one-line
input-construction correction in a test file.
No orphaned test/mutation processes at handoff (`pgrep` clean).
