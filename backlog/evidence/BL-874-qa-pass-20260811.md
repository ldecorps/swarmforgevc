# BL-874-portable-relative-mtime-in-shell-tests — QA pass, 2026-08-11

Commit verified: `40214b4d1` (merge of documenter's forward `8918d2112f` into
QA; coder merge `30c8fe251`, cleaner merges `9112dd521`/`328211e3e`, architect
merge `c905f8b20`, hardener merge `669111570` all confirmed ancestors).

## Gates run — complete inventory, NONE outstanding

| Gate | Result |
|---|---|
| `npm run compile` | clean |
| `required_wiring` (7 entries) | all 7 confirmed as literal, present, actually called — `portable_touch_relative` sourced and invoked at all six named shell-test sites; the 7th (`extension/test/portableTimeGuard.test.js`) confirmed sitting in the standing `npm test` suite |
| `which gtouch gdate` | empty — no GNU coreutils on this host, matches ticket's stated constraint |
| Six named shell scripts run directly (`bash <path>`) | 3/6 exit 0 clean: `test_operator_runtime_sandbox_sweep_bounded_progress.sh`, `test_operator_runtime_tick.sh` (incl. `swarm-seed-race-02` reporting `ok` — the BL-310 site, confirms backdating genuinely bites, not a no-op), and — independently reproduced matching coder/architect/hardener evidence — 3/6 fail on unrelated, pre-existing, already-ticketed macOS gaps: `test_operator_runtime_sandbox_sweep.sh` and `test_operator_runtime_fixture_reaper_sweep*.sh` fail on `/proc`-based process-liveness checks (BL-877, specced 2026-08-11, explicitly documents these as "surfaced, not caused, by BL-874"); `test_handoffd_stuck_escalation_email_wiring.sh` fails on `setsid: No such file or directory` (BL-878, same date, same relationship). None of the three failures occur at a `touch -d`/`date -d` line — all three now run PAST the point that used to crash before this fix, into unrelated downstream checks. Ticket's own "Out of scope" clause pre-authorizes exactly this: "If a test fails on macOS for an unrelated reason, that is its own ticket." |
| Scratch-file break-then-fix (ticket's own e2e step 4) | independently performed, not just re-read from evidence: planted `touch -d "2 hours ago"` in a new scratch file under `swarmforge/scripts/`, ran `portableTimeGuard.test.js` — failed, naming the exact scratch file; removed the scratch file, reran — 7/7 green. |
| `bl874PortableTimeInvariants.property.test.js` (isolated) | 6/6 pass |
| `portableTimeGuard.test.js` (isolated, the standing guard) | 7/7 pass |
| Acceptance pipeline (`run_acceptance.sh`) | not run — ticket's own `acceptance:` field points at `specs/features/BL-874-portable-relative-mtime-in-shell-tests.feature.draft` (never activated: zero step handlers exist under `specs/pipeline/steps/`). Ticket carries no top-level `qa_e2e_procedure:` either; its manual e2e steps live as prose inside `description:`, which is what was executed above instead. Recorded as an observation, not a defect — the draft Gherkin's own scenario 02 would assert all six scripts exit 0, which does not hold for the reasons above, so activating it as-is would misrepresent the ticket's actual, deliberately-scoped state. |
| Orphaned test processes before/after | none from this ticket's own runs; incidentally reaped one unrelated stray (`bl684-launcher-prop` fixture leak) and a set of leftover vitest fork workers from an earlier BL-871 acceptance-run timeout kill, both while checking hygiene during the BL-871 pass immediately prior in this same session |

## Verdict

NONE outstanding. Scope discipline independently re-verified, not merely
trusted from upstream evidence: read `operator_runtime.bb`'s
`live-process-paths!` directly, confirmed the `/proc` dependency predates
this ticket (BL-413, dated a month earlier) and is unrelated to GNU
relative-time syntax. The six sites' own backdated-mtime assertions are
unchanged from before this ticket — only the mtime-setting mechanism moved,
exactly as scoped. Approved.

By QA.
