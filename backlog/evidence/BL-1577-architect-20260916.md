# BL-1577 — architect review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- Received commit aae10046c8 (cleaner, merged clean into architect worktree).
- Diff scope confirmed via `git diff main --name-only`: only
  `extension/test/telegramClient.test.js`,
  `extension/test/bl568MenuAnswerPollMapping.test.js`, and
  `specs/pipeline/steps/bl1577TelegramClientFirstRunSurvivorsSteps.js`.
  `extension/src/**` untouched — two-layer boundary, host-I/O-ownership,
  webview-storage, and secrets-location checks are all vacuously satisfied
  (no production code, no webview, no I/O, no secrets touched).
- `node extension/out/tools/dependency-gate.js` on the three changed files:
  PASSED, no forbidden edges.
- `node extension/out/tools/co-change-report.js` on the three changed files:
  reported only expected coupling (the test file to its own module under
  test, the step file to `specs/pipeline/steps/index.js`); nothing outside
  the parcel's own subject flagged as a new structural coupling.
- Invariants Review: the ticket's one declared invariant ("every mutant ...
  is killed / accepted equivalent / grandfathered / owned; none is silent")
  quantifies over the hardener's mutation-disposition table, not a pure
  input domain — coder's evidence states this non-encodability reason
  correctly; no property test is missing here. Discharge is the hardener's
  stage per the ticket's own "How" section.
- Property Testing pass: no pure production module was touched by this
  parcel (tests and a step handler only) — no-op, nothing to add.
- Correctness spot-check: read the source for every function the new tests
  target (`resolveForumTopicName`/`extractForumTopicCreatedName`,
  `setChatMenuButton`, `inlineKeyboardButtonToWire`, `formatNetworkError`,
  `extractUpdates`, `extractIconStickers`, `defaultPost`/`defaultPostVoice`,
  `defaultWaitMs` via `createForumTopicWithRateLimitRetry`,
  `bl568PlanMenuAnswerDrive`, `bl568TextFallbackMessage`) in
  `extension/src/notify/telegramClient.ts` — each new assertion matches the
  real branch it claims to reach; none is vacuous.
- Acceptance wiring: grepped `specs/pipeline/steps/*.js` for the feature's
  step text — only the new `bl1577TelegramClientFirstRunSurvivorsSteps.js`
  matches; no duplicate handler. Confirmed
  `backlog/evidence/BL-1577-telegramClient-mutation.md` does not exist yet
  (expected — hardener's stage) and the three scenarios fail for that
  reason only, not a coder-stage defect.
- Ran `npx vitest run test/telegramClient.test.js
  test/bl568MenuAnswerPollMapping.test.js`: 108/108 pass.
- Ran the full suite (`npx vitest run`): 619/620 files, 10524/10525 tests
  green. The one failure
  (`test/telegramCursorOperatorExec.test.js` > BL-1204 redeploy-all,
  `ENOTEMPTY: directory not empty, rmdir '/tmp/bl1204-...'`) is unrelated to
  this parcel (no file this ticket touches), reproduces as an isolated
  concurrent-tmp-dir-sweep race — reran the file alone and it passed
  24/24 — and is not a new/unticketed class (ENOTEMPTY tmp-sweep races
  under concurrent worktree runs are a recognized recurring flake pattern
  referenced across many prior evidence files, e.g.
  `backlog/evidence/BL-952-architect-pass-2-20260819.md`). Not a send-back.
