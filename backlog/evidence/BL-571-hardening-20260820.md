# BL-571 — hardening pass, 2026-08-20

Parcel received from architect (`1c5a8b28d0`, review pass 3, PASS) as a batch
of one. Task name `BL-571-sequential-rotation-dormant-parity`. This is the
re-fix after QA's bounce (`affac4f828`, D1 `integration`).

## Degraded toolchain — recorded, not implied

BL-571's production change is **Babashka** (`mono_router_lib.bb`,
`swarm_ensure.bb`) plus a bash launcher mirror. Per engineering.prompt Startup
Tools there is **no mutation, CRAP, or DRY tooling wired for `.bb`** — the
wired gate is the unit/property runners under `swarmforge/scripts/test/`.
Stryker, `crapReport.js` and `jscpd` did NOT run against this ticket's code
and no figure from them is claimed. A hand-authored surgical sweep stands in
(BL-567 pattern). The one JS file in the parcel went through the standing
whole-tree guards.

## The merge: four conflicts, and one silent drop the conflicts did not surface

My branch carried QA's bounce revert of BL-571 (`fa34044d99`) while this
parcel restores it, so the merge conflicted in four places. Three were
routine; one was a trap and one drop was silent.

- **`bl571SequentialRotationDormantParitySteps.js`** — modify/delete. Git
  surfaced it rather than silently re-applying the deletion (the BL-954
  hazard behaving correctly); took the sender's restored file.
- **`specs/pipeline/steps/index.js`** — **the trap.** Both branches append
  domains and had ordered `bl956`/`bl955`/`bl827` differently, so the merge
  had ALREADY taken `bl956PipelineBoardCaptionCapSteps` above the conflict
  and HEAD's side of the hunk was a SECOND copy of it. "Keep both sides",
  the reflex resolution for an append-only registry, would have registered
  that domain twice. Took the sender's `bl571` line only; verified every
  domain appears exactly once and the registry loads.
- **`test_swarm_ensure.sh`** — additive on both sides (HEAD's two BL-958
  cases, the sender's BL-571 case). Kept both, one separator between them;
  `bash -n` clean.
- **`docs/reference/Specification.MD`** — both sides prepend to the
  `Last Updated` chain. BL-571 is this parcel's entry so it became newest and
  HEAD's chain demoted to `Previously`: BL-571 → BL-960 → BL-958 → BL-957.

### The silent drop: BL-958's landed recovery code, deleted with no conflict

The merge also re-applied this branch's earlier stale-rider revert and
deleted **110 lines** of BL-958's control-plane recovery code from
`swarm_ensure.bb` — the `control_plane_lib` load and the entire
`control-plane-state` function — **with no conflict marker**. BL-958 is
approved and landed; this parcel would have reverted it.

**Why the routine check missed it.** The BL-954 discipline is to diff every
file against the sender's tip. I did, and it came back clean: the sender's
branch does not carry BL-958's code either. The dropped content lives on
**QA's** tip (`fa34044d99`), which is what my branch had merged one parcel
earlier. A silent drop of ALREADY-LANDED work is invisible to a
sender-tip diff by construction — the sender never had it.

Found by diffing `swarm_ensure.bb` against `fa34044d99`, and confirmed
independently by the suite, which is the evidence that matters:

```
FAIL: BL-958: control-plane row not reported FIXED with the lib's decision,
      got: extension: HEALTHY / agent:coder: FAILED (respawned pane ...)
ENSURE_EXIT=1
```

**Fixed inside this parcel**: `swarm_ensure.bb` taken from `fa34044d99`, then
only BL-571's docstring + two-predicate hunk re-applied. The result is
byte-identical to the coder's own independent fix (`diff` reports zero
lines), and differs from QA's landed tip by exactly the 3 lines BL-571
legitimately replaces.

## The stranded commit: declared, not merged

QA's D1 named `17ae8a4822`; that is now correctly declared. But the gate
still failed on a NEW commit:

```
PRE_QA_GATE_FAIL ancestry BL-571 91ce11e180 stranded on swarm/coder
```

`91ce11e180` is the coder's merge of QA's merge-up into the coder branch,
made 04:20 — three minutes BEFORE the architect's review commit at 04:23,
which does not contain it. The coder had hit and fixed the same BL-958 drop
there; that fix never reached the review chain.

**Not merged, deliberately.** It carries BL-967's unapproved in-flight work
on the same lineage — `bl967HandoffdCycleStallSteps.js`,
`daemon_cycle_guard_lib.bb`, ~267 lines of `handoffd.bb`. An approval on
BL-571 does not authorise that (BL-506), and it is the same rider problem the
ticket already records for `17ae8a4822`.

Declared abandoned instead, on the content test QA itself applied: every
BL-571 file in this parcel is byte-identical to that commit's version — the
six others matched already, and `swarm_ensure.bb` matches after the
restoration above. Rationale in `notes:`, id appended to the FLOW-style
`abandoned_commits` list (a block list with inline comments reads to the gate
as ABSENT — BL-935's two-commit lesson, which QA restated in the bounce).

## Gates run

| Gate | Result |
|---|---|
| `test_swarm_ensure.sh` (pre-restoration) | **ENSURE_EXIT=1** — caught the BL-958 drop |
| `test_swarm_ensure.sh` (post-restoration) | see re-run below |
| `mono_router_lib_test_runner.bb` (incl. the parity gate) | exit 0 |
| `bl571_single_resident_rotation_property_runner.bb` | exit 0, 500 runs |
| Acceptance — BL-571 feature | 0 failures, exit 0 |
| Standing whole-tree guards (7 files) | 51 passed, exit 0 |
| `pre_qa_gate.sh` (pre-declaration) | **exit 1** — `91ce11e180` stranded |
| `pre_qa_gate.sh` (post-declaration) | see re-run below |
| Hand-authored `.bb` mutation sweep | see below |

BL-571's own behaviour case passed throughout — `PASS: BL-571: rotation
sequential dormant roles report DORMANT without respawn`. The only failure at
any point was the BL-958 regression my own merge introduced, plus the
ancestry declaration. **The delivered BL-571 work is sound.**

## A claim I checked rather than trusted

The Specification entry claims "a parity gate now derives the launcher's
accepted set from `swarmforge.sh` itself and asserts set equality". In
`bl571_single_resident_rotation_property_runner.bb` the set is HARDCODED
(`["router" "sequential"]`), which reads like a false claim — the class the
architect bounced this same coder's BL-959 for.

It is not false. The derivation lives in the sibling unit runner: it slurps
`swarmforge.sh`, regex-extracts `is_sequential_dormant`'s own
`"$ROTATION_MODE" == "<value>"` literals, asserts SET EQUALITY in both
directions, then functionally probes every derived value against the REAL
sourced bash function plus a rejected control (`classic`), cleaning up in a
`finally`. Its own non-vacuity is recorded for both drift directions. This
satisfies the engineering Guardrail on a constant mirrored across a language
boundary no import can bridge (BL-897). Nothing to add.

## The architect branch advanced mid-pass — merged, not declared

After the first restoration was committed, the pre-QA gate reported two NEW
stranded commits, this time on `swarmforge-architect`:

```
PRE_QA_GATE_FAIL ancestry BL-571 2f3fd69c89 stranded on swarmforge-architect
PRE_QA_GATE_FAIL ancestry BL-571 2e43c4b94b stranded on swarmforge-architect
```

The architect had kept working after handing off `1c5a8b28d0`:

- `2e43c4b94b` — the architect's **independent** fix of the same silent
  `swarm_ensure.bb` revert. Its own commit message records the identical
  diagnosis: *"diffing the merge against my own pre-merge HEAD could not see
  this — content the OTHER parent had never appears as a deletion in that
  direction. Caught by diffing against fa34044d99."*
- `2f3fd69c89` — an evidence file recording the hazard and a both-parents
  detection rule for QA at landing. This parcel did not otherwise carry it.

**Merged, not declared abandoned — and the distinction is the point.**
Declaring a commit abandoned is for when merging it would drag in unapproved
riders (BL-506), which is exactly why `91ce11e180` could not be merged: it
carries BL-967's in-flight work. The architect's two commits carry no other
ticket's work — only the QA merge-up this branch already had — and the
architect is this parcel's SENDER, so merging is the ordinary lineage
operation. An ancestry failure is not a licence to declare; the content test
decides which remedy applies.

`swarm_ensure.bb` auto-merged clean and still carries BL-958's code (3 lines
differ from `fa34044d99`, exactly BL-571's replacements). The drop check was
re-run against QA's tip, not the sender's — the only direction that can see
it.

### The conflict resolution that only goes wrong once both sides land

`docs/reference/Specification.MD` conflicted on the `Last Updated` chain
lead. Taking my side (BL-571 newest, chaining into the shared BL-960 body)
is correct in isolation — and left **BL-571 present TWICE**, because the
architect had independently added a BL-571 entry demoted under BL-960.
Neither side's resolution was wrong on its own; the duplication exists only
in the merged result. Verified the two entries were textually identical, then
dropped the lower. Chain now reads BL-571 → BL-960 → BL-958 → BL-957 →
BL-935, each ticket exactly once.

`test_swarm_ensure.sh` conflicted on whitespace only: the sender's side
re-added a separator that already follows the conflict, so HEAD's empty side
was taken to avoid duplicating it. `bash -n` clean; 1 BL-571 and 2 BL-958
cases intact.

## Hand-authored `.bb` mutation sweep — 6 killed, 0 survived

Baseline verified green BEFORE any mutation, so a "kill" cannot be the
harness. The tracked lib was mutated under an EXIT trap (the sandbox copy
could not host the unit runner's functional half, which sources the real
`swarmforge.sh`); restoration verified clean afterwards.

```
BASELINE unit=0 props=0
killed  M1 drop 'sequential' from the accepted set (THE original defect)
killed  M2 widen the accepted set with an extra value (launcher drift)
killed  M3 widen the ROUTER-ONLY conf predicate     (ROTATE_HOME fence)
killed  M4 widen the ROUTER-ONLY identity predicate (ROTATE_HOME fence)
killed  M5 drop the trailing word boundary (so 'sequentially' matches)
killed  M6 drop the multiline start anchor (so a commented mention matches)
```

M3/M4 matter most for this ticket specifically: widening the router-only pair
is the thing BL-571 deliberately does NOT do, and both the unit runner and
the property runner's fence rows reject it.
