# BL-1410 — architect review pass, 2026-09-09

D1. **behavior** — acceptance test scenario 02 fails: 10 step handlers still define local mkTmpDir

The acceptance test `bl1410StepHandlersTakeTheStepsLaneRootSteps.js` scenario 02 ("no acceptance step handler obtains a fixture root through the Vitest-swept helper") checks EVERY step handler under `specs/pipeline/steps/` for mkTmpDir usage. The test fails because 10 handlers still define local mkTmpDir:
- bl1415LostDispatchSteps.js
- bl1420FreshnessFixturesPassTheGuardSteps.js
- bl1432LandWalkRangesOverTheParcelSteps.js
- bl1440ConstitutionCitationsResolveSteps.js
- bl1465PassengerPropertyAssertsInPlanSteps.js
- bl1466BouncedSiblingNeverRidesSteps.js
- bl1470BounceCheckReadsSharedStoreSteps.js
- bl1479ParkedActiveSweepSteps.js
- bl1481SharedPathContentCheckSteps.js
- bl1495BaiGatewaySeatSteps.js

These handlers were added AFTER the audit on 2026-09-05 (the ticket description's audit date), so they were not included in the migration. However, the acceptance test checks ALL handlers, not just the audited ones.

**Commit reviewed:** c2311cf20b (coder), d272a70823 (cleaner)

**Remediation:** Either migrate the 10 newer handlers to mkSocketFixtureRoot, or scope the acceptance test to only check the handlers that were part of the original audit (the 11 handlers listed in the ticket description). The invariant requires that no handler uses the Vitest-swept helper, so option 1 is preferred if the 10 handlers are meant to be in scope. If they are out of scope for this ticket, the acceptance test must be narrowed to avoid a false failure.

**Blamed role:** coder

---

D2. **behavior** — BL-743 feature fails (pre-existing, not introduced by parcel)

The BL-743 feature fails with `deps.checkOrphanedAuthoredDocs is not a function`. This is a pre-existing defect (verified by running BL-743 on the parent commit ce587d038b — same failure). The parcel did not introduce this defect.

However, scenario 03 of the BL-1410 acceptance test runs BL-743 as part of verifying that convention-gate handlers' features still pass. This causes scenario 03 to fail as well.

**Commit reviewed:** c2311cf20b (coder)

**Remediation:** This is not a defect in the parcel, but it blocks the acceptance test. Either fix the BL-743 defect (separate ticket), or exclude BL-743 from scenario 03's convention-gate verification until it is fixed.

**Blamed role:** specifier (spec gap — the parcel cannot satisfy the acceptance criteria while BL-743 is broken)

---

## Architectural compliance

The dependency-gate checker PASSED for `specs/pipeline/steps/lib/socketFixtureRoot.js` when run from the extension directory. The helper's dependencies are clean (node:fs, node:path, ./fixtureReaper).

The migration respects the two-layer boundary (step handlers are test infrastructure, not extension runtime). No browser storage, no secrets leakage, no direct tmux spawning from TypeScript.

Invariant 1 (fixture roots are removed on process exit) is satisfied for the migrated handlers — the helper uses `onAbnormalExit(removeStragglers)` to track and remove roots.

Invariant 2 (string literals are preserved) is satisfied — bl743 still contains the mkTmpDir string literal as test data.

However, the acceptance test fails because the migration is incomplete (10 newer handlers not migrated) and because BL-743 is broken (pre-existing defect).

By architect.
