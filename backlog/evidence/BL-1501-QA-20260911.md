# BL-1501 — QA review pass, 2026-09-11

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Commit verified: `d62423d019` (Merge documenter d62848f6d0 into QA). Lineage
confirmed ancestor-of-this-commit for every stage: `a8b241edb5` (coder) →
`67cfb6c116` (cleaner) → `27994772ab` (architect) → `80ac0b69c3` (hardener)
→ `d62848f6d0` (documenter) — never merely ancestor-of-HEAD.

Gates run:
1. `cd extension && npx vitest run test/tempDirTrapGuard.test.js` — 4
   passed, 0 failed.
2. Each hotfix runner under `bb -Djava.io.tmpdir=<mktemp -d>`:
   - `landed_ticket_autoclose_test_runner.bb` — exit 0, 24 assertions
     passed, 0 entries left in the isolated tmpdir, `rmdir` succeeded.
   - `wake_dedup_lib_test_runner.bb` — exit 0, all assertions passed, 0
     entries left, `rmdir` succeeded.
3. `git diff main...HEAD --stat` touches only the two runners, the new
   step handler, the feature file evidence set, and the standing-reds.tsv
   row removal — `tempDirTrapGuard.js` and its standing unit test
   unchanged.
4. `bash specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-1501-*.feature` — 4/4 scenarios passed (real-tree
   scan, both runner sweeps, exempt-list break-then-fix).
5. `backlog/standing-reds.tsv`'s `unit` row for
   `extension/test/tempDirTrapGuard.test.js` (BL-1501) is gone.
6. Full unit suite (`npm test`): first run showed 1 failure in
   `test/telegramCursorOperatorExec.test.js` (ENOTEMPTY on
   `sweepPendingTmpDirs`, ticket's diff never touches this file or its
   fixtures); isolated re-run of that file alone passed 3/3 times, and a
   full-suite re-run passed 615/615 files, 10439/10439 tests clean —
   confirmed flaky under full-suite concurrency, not a regression this
   parcel introduced.
7. `npm run test:properties`: 2 failed files / 3 failed tests, both
   pre-existing and already ticketed in `backlog/standing-reds.tsv` with
   open tickets in `backlog/active/`: BL-1502
   (`bl1089FrontDeskLivenessFixture.property.test.js`) and BL-1503
   (`bl1313BatchGuardVisibilityInvariants.property.test.js`). No new
   standing red introduced by this parcel.
8. `qa-sibling-check.js status --ticket BL-1501` → `VERIFY BL-1501`, no
   open deferral.
9. No orphaned test/mutation processes before or after the run
   (`pgrep -fl 'node --test|stryker'` matched no live processes both
   times).

Wiring: the new step handler
(`specs/pipeline/steps/bl1501HotfixRunnersSweepTempRootsSteps.js`) needs
no manual registration — `specs/pipeline/steps/index.js` discovers any
top-level `*Steps.js` file by filename (BL-1371); confirmed present and
picked up by the acceptance run above.
