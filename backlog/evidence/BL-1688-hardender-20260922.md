# BL-1688 — hardender review pass, 2026-09-22

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No extension/src or extension/out changes (bash + Babashka only, no
mutation/CRAP/DRY wired, BL-472). Verified:
- `bash swarmforge/scripts/test/test_bl1688_status_ledger_survives_start.sh`:
  3/3 PASS.
- `bash swarmforge/scripts/test/bl1688_start_owner_acceptance_runner.sh`
  (default, `BL1688_MARKER=1 BL1688_CALLER=build_freshness_cli`, and
  `BL1688_MARKER=1 BL1688_CALLER=swarmforge.sh`): refusal, deliberate-launch
  clear, and default-success shapes all confirmed by hand.
- `node specs/pipeline/cli.js` on the ticket's own feature: 4/4 ok.
- `node specs/pipeline/cli.js` on BL-1492's own feature (must stay
  unchanged): 5/5 ok, unchanged.
- `bash swarmforge/scripts/test/test_bl785_freshness_deliberate_stop.sh`
  (touched by this parcel's caller fix): all PASS, including scenario 04's
  real re-arm check.
- `bash swarmforge/scripts/test/test_start_handoff_daemon.sh`: 2/2 PASS,
  unaffected (no marker present in that fixture).
- `node specs/pipeline/cli.js` on BL-403's own feature (drives
  `bl403_supervisor_kill_acceptance_runner.bb`, named in this ticket's
  qa_e2e_procedure): 3/3 ok, unaffected (front_desk_supervisor_lib.bb
  untouched by this parcel).
- `bb -e '(load-file ...)'` on both `handoffd.bb` and
  `handoffd_supervisor.bb`: load cleanly (expected guard/usage messages
  only, no parse errors).

Hand-authored mutation sweep (Babashka/bash fallback, BL-567 pattern),
each applied, run, restored:
1. `start_handoff_daemon.sh`'s caller-gate `!=` -> `==`
   (`"$DAEMON_START_CALLER" != "swarmforge.sh"` inverted): killed - 2/4
   acceptance scenarios fail (deliberate launch now refused, heal-path
   caller now clears the marker - the two invariants swapped).
2. `handoffd_supervisor.bb`'s `check!` tmux-socket skip condition
   inverted (`(not (fs/exists? tmux-socket-file))` ->
   `(fs/exists? tmux-socket-file)`): killed - scenario 4 fails,
   `restart-in-place dead` fires instead of the `skip` line.

Two pre-existing, already-ticketed reds sighted by the coder during this
build (both independently confirmed unrelated to BL-1688 via git-stash
isolation) and correctly filed as notes rather than blocking BL-1688's
own forward: `test_build_freshness_cli.sh` case 02/03(compiled) is now
BL-1692 (paused); `test_handoffd_role_context_clear_wiring.sh`'s setup
timeout is now BL-1694 (paused). Neither touches a file this ticket
owns or changes.

No orphaned processes or dirty tree after the mutation sweep.

By hardender.
