# BL-1315 — QA verification PASS, offline expedited run, 2026-08-31

## Lineage / commit cited

Verified in `.worktrees/expedite-BL-1315` (branch `expedite/BL-1315`), tip
`02361ab58e` (documenter's doc-only pass). Ancestry checked directly, not
inferred from "ancestor of HEAD" (BL-336's trap):

- `git merge-base --is-ancestor 28e023dd9a 02361ab58e` — OK (coder's original fix)
- `git merge-base --is-ancestor 64156b80cf 02361ab58e` — OK (hardener's bounce, invariant-1 finding)
- `git merge-base --is-ancestor b15219834d 02361ab58e` — OK (hardener's rework pass, fix verified)

Cleaner and architect made no functional change across either the original
pass or the rework pass (Article 1.9 No-Op Rule; confirmed by prior stages'
own verdicts, not re-litigated here).

## required_wiring (verified by hand — expedite path skips this gate, BL-1255)

- `swarmforge/scripts/land_step_cli.bb:65` calls `land-step-lib/replay!` —
  confirmed present by direct grep. `own-paths` is called inside `land-plan`
  (`land_step_lib.bb`), whose `:own-paths` field `land_step_cli.bb` passes
  straight to `replay!` — the changed computation is on the only live
  caller's path.
- `specs/pipeline/steps/index.js:909` registers
  `require('./bl1315OwnPathsFullRangeSteps')` — confirmed present.

## Unit suite — babashka (`land_step_lib_test_runner.bb`)

Ran via absolute path (`bb /home/carillon/swarmforgevc/.worktrees/expedite-BL-1315/swarmforge/scripts/test/land_step_lib_test_runner.bb`)
to guarantee the worktree's own file was exercised, not a stale sibling copy:
`ALL PASS: land_step_lib.bb`. Confirmed the file's failure-collecting
harness (`assert=`/`assert-includes` push onto a `failures` atom; only
prints `ALL PASS` when empty) — a silent pass is not a rubber stamp.
Confirmed the file textually contains scenarios 01, 03, 04, 05, 06, 07 (02a/02b
inline) before trusting the summary line.

## Property suite — this ticket's own two files

- `test/bl1297MergeOwnPathsInvariants.property.test.js` — 3/3 pass
- `test/bl1315OwnPathsFullRangeInvariants.property.test.js` — 2/2 pass
  (invariant 1+2: every own path survives, every unlanded-sibling-only path
  is dropped; invariant 2 refusal: an unreadable path refuses and names
  itself)

## Acceptance (`run_acceptance.sh`)

`specs/features/BL-1315-the-replay-tip-carries-only-the-ticket-being-landed.feature`
— 7/7 scenarios pass (all Background + Scenario/Scenario Outline rows: 01,
02[1], 02[2], 03, 04, 05, 06, 07 is covered by the same computation as 03/06
per the feature's own scenario count — verified against the generated
test's own `1..7 / pass 7 / fail 0` tally, not summarized from a partial
tail).

## qa_e2e_procedure — live reproduction, re-run independently of every prior stage's own claim

Ran directly against `own-paths` (bypassing `land_step_cli.bb`'s worktree
creation, which collides on a pre-existing scratch branch
`land-replay/BL-1298-86c2ed1c2d` left over from the ORIGINAL bug discovery
on 2026-08-31T04:36Z — pre-existing repo debris, not a defect in this
ticket's own change; the underlying function is the actual unit under test
and does not depend on that branch):

- Step 6 (the ticket's own motivating case, BL-1303 under-inclusion):
  `(land-step-lib/own-paths "." "ab8d10a8b3" "BL-1303")` now returns 63
  paths (was 20 before the fix, per the ticket's own description) and
  includes every previously-dropped file:
  `extension/src/tools/check-feature-handler-registration.ts`,
  `specs/pipeline/steps/bl1303FeatureHandlerRegistrationSteps.js`,
  `swarmforge/scripts/check_feature_handler_registration.sh`, and the three
  associated test files. The guard and the CLI it shells out to land
  together, as required.
- Steps 1-3 (BL-1298 over-inclusion, the sibling report is right but the
  tip was wrong): `(land-step-lib/own-paths "." "86c2ed1c2d" "BL-1298")`
  returns 56 paths, `warning nil`, and explicitly EXCLUDES
  `swarmforge/scripts/check_feature_handler_registration.sh` and
  `extension/src/tools/check-feature-handler-registration.ts` (BL-1303's
  files) while including BL-1298's own backlog/evidence/specifier-ruling
  paths from the same tagged range — matching invariant 2 (exclude only on
  positive attribution to a still-unlanded sibling).
- Step 5 (fail-loud repro) — NOT re-run as literally written. Already
  surfaced by the hardener (`BL-1315-hardener-bounce-20260831.md`, "Spec-gap
  surfaced") as a drafting inconsistency: step 5's described repro (an
  untagged commit that is the sole toucher of a unique path) is the EXACT
  shape scenario 03 / invariant 1 require to SURVIVE, not refuse. Verified
  live that the shipped `own-paths` keeps such a path (matches invariant 1),
  which is incompatible with step 5's literal expectation. Not re-litigated
  here — specifier-owned per Article 4.4, already on file.
- Step 4 (undeterminable attribution refuses) — covered by the acceptance
  suite's scenario 04 (7/7 above) and by `land_step_lib_test_runner.bb`'s
  own-paths (04) assertions; not separately hand-repro'd.
- Step 7 (acceptance run with no unmatched step handler) — satisfied by the
  7/7 result above; no "no step handler matched" error.

## Full unit suite (`npm test`, from `extension/`)

15 files / 25 tests failed. Cross-checked against BL-1315's own changed
paths (backlog/evidence, docs/reference/Specification.MD,
extension/test/bl1297*.property.test.js + bl1315*.property.test.js,
specs/pipeline/steps/*, swarmforge/scripts/land_step_lib.bb,
swarmforge/scripts/test/land_step_lib_test_runner.bb): zero overlap. Every
failing file is the SAME 15-file set BL-1297's QA pass recorded one day
earlier (`backlog/evidence/BL-1297-qa-pass-20260830.md`), confirmed by
direct comparison, not re-triaged from scratch:

- `checkOrphanedAuthoredDocs is not a function` (crossFileDuplicationCheck,
  multiBranchParserCoverageCheck, perHatRolePromptEvidenceCheck,
  pilotAcceptanceGate, pilotScopedCrapCheck, shellEntryPointDriveCheck,
  unreachableStepHandlerCheck) → BL-1221.
- `constitutionDocCitations`, `tempDirTrapGuard`, `socketFixtureShortRootGuard`,
  `liveRepoDerivationGuard` → named repo-hygiene reds, specifier disposition
  note `specifier-disposition-qa-standing-red-note-20260828.md`.
- `operatorRuntimeBbFixtureClosure` → BL-1265.
- `backendSwitch`, `telegramClient`, `telegramCursorOperatorExec` → BL-1263.

Reported per BL-1063 doctrine (grep/cross-check before calling something
untracked) — not bounced, not blocking this approval.

## Property suite (`npm run test:properties`, from `extension/`)

25 files / 14 tests failed, plus the one allowlisted benign unhandled error
(`[vitest-worker]: Timeout calling "onTaskUpdate"`, BL-871). All 25 failing
files confirmed present in
`swarmforge/scripts/property_suite_standing_allowlist.tsv` with
`disposition=allowlist` (checked programmatically against the log, all 25
matched). This ticket's own two property files pass clean (see above).

Foreground execution note: `vitest run` exceeded the Bash tool's 2-minute
hard cap twice; the underlying process was backgrounded with output
redirected to a worktree-local `tmp/` file and polled to completion via
repeated short foreground `kill -0` checks (no Monitor tool, no
run_in_background flag, no IDE notification) — consistent with "run checks
in the foreground." Confirmed no orphaned `vitest`/`stryker`/`node --test`
process before or after each run (reaped two stragglers from timed-out
attempts via `kill -TERM`/`kill -KILL` on their PIDs before the final clean
run).

## Wiring correctness

- `land_step_cli.bb` itself is unchanged (confirmed by the documenter's own
  pass; re-confirmed here: `git diff origin/main..02361ab58e --
  swarmforge/scripts/land_step_cli.bb` is empty) — output contract
  (`LAND_CLEAN` / `LAND_REPLAY <branch> <commit>` / `LAND_ESCALATE`)
  unchanged.
- `land-plan`'s own arity of `own-paths` (2-arg, self-computing
  `unlanded-siblings` via `entangled-siblings`) and the 3-arg form
  `land-plan` actually calls are both exercised by the test runner and the
  property tests; no dead arity.

## Non-blocking items already on file, not re-raised

- `commit-ticket-id`'s nil-collapse observation (hardener's original pass).
- The BL-1272 passenger-land bookkeeping gap, master checkout's unrelated
  staged/unstaged drift (documenter's pass — this is pre-existing dirty
  state in the MASTER checkout unrelated to this ticket's own worktree,
  confirmed clean: `git status --short` in `.worktrees/expedite-BL-1315`
  reports nothing to commit before this evidence file).
- qa_e2e step 5 spec-gap (hardener's pass; restated above for QA's own
  record).

## Verdict

PASS. No defects found in this ticket's own scope. Approved commit:
this evidence commit, on top of documenter's `02361ab58e`.

By QA.
