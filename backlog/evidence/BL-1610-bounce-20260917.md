# BL-1610 — bounce to coder, 2026-09-17

D1 (class: spec-gap): confirmed live, per the specifier's amendment
`a5e59a0f5b` and its adjudication evidence
`BL-1610-specifier-adjudication-of-documenter-blocked-sends-20260917.md`.

## Reproduction (re-run here, from the hardener's worktree, before bouncing)

```
bb swarmforge/scripts/merge_drop_guard_lib.bb /home/carillon/swarmforgevc/.worktrees/documenter 4356ab57c1 6ad1d3f616 073a34b5e1
{"merge":"6e5087cd4340dacf47a1a9809d3bb7ecf3dac567","path":"extension/vitest.bl1599.stryker.config.mjs","side":"sender","lines":12,"excused":true}
```

With the `received_at_head` bound in place, the build's `merge-commits`
still names `6e5087cd43` — "Merge hardener 46c409305c into cleaner",
21:47:13Z, a merge the CLEANER made. It reaches the documenter's tree
through the RECEIVED commit's own ancestry
(`4356ab57c1 → hardender → architect → cleaner`), not through anything the
documenter did after receipt. `merge-commits` scans `head..forwarded`
only; that range re-admits the received commit's whole ancestry whenever
the sender's own head lacks it — true for every fresh parcel, not just
this one. Here the finding happens to be excused (the documenter never
touched that path), but a role that DOES edit the same path an upstream
merge one-sidedly resolved would be refused for a merge it never made and
cannot redo.

Amended invariant 1 (ticket YAML, this commit's ancestor `a5e59a0f5b`):
"reachable from forwarded and from NEITHER the received commit NOR the
head ... never a finding on an upstream merge, excused or not." The fix
is `git rev-list --merges --reverse <forwarded> ^<head> ^<received>` in
`merge-commits`/`findings-between`'s 4-arity form (stampless 3-arity
fallback unchanged: `forwarded ^received`), plus scenario 01 row 5 added
INSIDE this parcel (feature file, `specs/features/BL-1610-*.feature`) with
its step-handler Given (the sibling's own one-sided dropping merge on a
SECOND path).

Blamed: spec-gap, not the coder's own commit — invariant 1's prose
promised this, but its checkable predicate (`head..forwarded`) admitted
the build; the specifier's adjudication attributes it to the ticket's own
predicate, tightened in the amendment now on `main` (merged into this
worktree, `a5e59a0f5b`).

## What travels with the bounce

- The amended ticket YAML (already on `main`, carried by this parcel's
  merge).
- This hardener pass's own unit-test additions to
  `merge_drop_guard_lib_test_runner.bb`,
  `review_forward_evidence_gate_lib_test_runner.bb` and
  `ready_for_next_claim_task_name_runner.bb` (commit `648e7bc93e`) — still
  valid against the amended scan; the coder's rebuild should extend the
  head-bound-scan test to also exercise the `^received` exclusion (a merge
  reachable from `head` but arriving through `received`'s ancestry, per
  the reproduction above), the same isolating-fixture discipline the
  existing test already applies to the `^head` exclusion alone.
- This hardener's own evidence (`BL-1610-hardender-20260917.md`, NONE +
  detail) predates this amendment and is superseded by this bounce record
  for verdict purposes.

## Not blocked

No check was blocked; the reproduction above ran clean. `--blocked 0`.

## `record-bounce.js` note

First call passed this evidence commit itself as `--commit` (wrong -
`revertCheck` then reported `verdict: violation` against
`backlog/evidence/BL-1610-bounce-20260917.md`, a file this same call had
just added, which is nonsensical to revert). Per the BL-994 precedent
("bounce_history records commit: <tip received from the architect> ...
naming the reviewed tip, not this pass's own additions"), re-ran with
`--commit c80be224c0` (the architect's tip this hardener pass actually
reviewed for BL-1610). **No revert applies to this bounce** either way:
class `spec-gap` is an OMISSION (the invariant's checkable predicate
admitted the build; nothing was wrongly added to revert) - the same
posture BL-994's own bounce recorded for its OMISSION class.
