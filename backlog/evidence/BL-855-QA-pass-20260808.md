# BL-855 QA final pass — 2026-08-08

Reviewed commit `fc1ad6fd` (merge of documenter's `14f5d207b5` into this
worktree; documenter's chain: `762873f6` docs, `40c09da7`/`14f5d207`
`abandoned_commits` bookkeeping, on top of hardener's `8b4a68c2`, architect's
`e2d5f3ca`, cleaner's `cfe6a906`, coder's `d207aa1c`).

## Disposition

`node extension/out/tools/qa-sibling-check.js status --ticket BL-855` ->
`VERIFY BL-855` (exit 0). No open deferral.

## Checks run

- Orphan sweep before starting: `pgrep -fl 'node --test|stryker'` -> none.
- `bb swarmforge/scripts/test/push_sweep_lib_test_runner.bb` -> ALL TESTS
  PASSED.
- `bb swarmforge/scripts/test/push_sweep_lib_property_runner.bb` -> 500 runs,
  ALL PROPERTIES HOLD, including both BL-855 non-vacuity checks
  (unconditional-merge-exemption mutant, dirty-working-tree-suppresses-verdict
  mutant).
- `bb swarmforge/scripts/test/pre_qa_gate_lib_test_runner.bb` -> ALL PASS.
- `bash swarmforge/scripts/pre_qa_gate.sh BL-855-noop-landing-merge-drops-approved-work <HEAD>`
  -> `OK` (required_wiring + abandoned_commits block-list parse clean after
  documenter's `14f5d207` formatting fix).
- `bb swarmforge/scripts/test/briefing_email_test_runner.bb` (regression check,
  shares `handoffd.bb`) -> ALL PASS.
- `bash swarmforge/scripts/test/test_handoffd_push_sweep_wiring.sh` -> FAILS
  with `env: setsid: No such file or directory`. Independently reconfirmed
  pre-existing/environmental (macOS has no util-linux `setsid`; spot-checked
  `test_handoffd_cooldown_sweep_wiring.sh` fails identically), matching
  cleaner's/architect's/hardener's own findings on this same ticket. Read the
  script: `setsid` only detaches the daemon process for the test harness, not
  part of BL-855's own logic. Not a regression.
- `npm run compile` (extension/, Node 22) -> clean.
- `npm test` (extension/, Node 22, full suite, `recordTestDuration.js`) ->
  **411/411 test files, 7292/7292 tests passed.** (A first attempt under the
  shell's default Node 20.20.2 showed 7 failures, all in
  `dependencyGateCliStorageGlobals.test.js`, with `dependency-cruiser`
  refusing to run on Node < 22 — re-ran under `nvm use 22` per the architect's
  own noted workaround on this same ticket and got a clean 7292/7292. Node
  version artifact, not a regression.)
- `npm run test:properties` (extension/, Node 22) -> two runs, both under
  severe host load (`uptime` load averages 22.89/39.22/37.92 against 2
  physical cores at check time; confirmed via `sysctl -n hw.physicalcpu`).
  Failures both runs were confined to `test/bl760DuplicateChainGuard.property.test.js`
  and/or `test/bl787NamedTunnelInvariants.property.test.js` — neither file
  touches push-sweep, no-op-merge, or token-burn code, and both fail with
  test-timeout signatures (17s/6.7s/96.6s/116s/75.1s against a 20s budget) or
  `[vitest-worker]: Timeout calling "onTaskUpdate"` worker-communication
  errors, the same shape documented for Stryker dry-runs under severe load.
  Treated as environmental flakiness unrelated to this ticket's own code, not
  a defect — no property test for `noop-landing-merge?`/`noop-merge-decision`
  or the BL-619 metrics modules appeared in either failure list.
- Acceptance: `specs/features/BL-855-noop-landing-merge-drops-approved-work.feature.draft`
  stays `.draft` (no step handlers registered in `specs/pipeline/steps/index.js`)
  — consistent with the project convention that an unbuilt/unwired feature
  file stays `.draft` rather than going live with nothing to bind its
  scenarios (a live `.feature` with no handlers throws in `runtime.js`,
  BL-233). Nothing in this ticket's `required_wiring` names a step-handler
  deliverable; the closest thing to an e2e acceptance check is the real-git
  reproduction in `test_handoffd_push_sweep_wiring.sh` (see above).
- Wiring read: confirmed `noop-gate` is consulted in `push_sweep_lib.bb`'s
  `:should-push` branch BEFORE `qa-gate-decision`, exactly per
  `required_wiring`; confirmed the real git adapter
  (`push-sweep-noop-merge-gate-facts!` / `noop-merge-commit-facts` in
  `handoffd.bb`) diffs only explicit refs (`sha^1`, `sha^2`, `sha`) and fails
  `:facts-complete? false` (closed) on any git error, never fabricating a
  clean answer.
- `pgrep -fl 'node --test|stryker'` / `pgrep -fl vitest` after all runs ->
  none; no leaked processes.

## Verdict

NONE — no defects found against this ticket's own scope. The two property-
suite failures are pre-existing, unrelated tests degraded by measured severe
host load, not a BL-855 regression. APPROVE.
