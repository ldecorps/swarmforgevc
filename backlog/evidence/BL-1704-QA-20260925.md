# BL-1704 — QA review pass, 2026-09-25

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bash tmp/bl1704-which.sh  (sources swarmforge/scripts/ollama_ancillary_lib.sh, writes a swarm-owned record for a dead stand-in pid, then for a live non-ollama stand-in (sleep 300), and calls ollama_ancillary_stop_swarm_owned each time; script in ## Detail)
- **Commit hash**: d458fb9f57
- **First error excerpt**: dead pid 6785:
ollama-ancillary: recorded swarm-owned ollama server (pid 6785) is no longer running as ollama serve - clearing the stale record, nothing signalled
reused pid 6802 (sleep 300):
ollama-ancillary: recorded swarm-owned ollama server (pid 6802) is no longer running as ollama serve - clearing the stale record, nothing signalled
  (unrelated process untouched)
ollama_ancillary_pid_is_ollama_serve folds 'pid not alive' and 'alive but not ollama serve' into one false, and the one caller logs one message for both.
- **Failure class**: behavior
- **Expected vs observed**: requirement 3: 'Record swarm-owned but the pid is gone, or now belongs to a process that is not ollama serve: signal nothing; remove the record; the stop log says which' / observed: nothing is signalled and the record is removed (correct), but both cases log the same line, so the stop log cannot tell a dead server from a recycled pid
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/ollama_ancillary_lib.sh ollama_ancillary_stop_swarm_owned: split the stale branch on ollama_ancillary_pid_alive. Log '... (pid N) is gone ...' when the pid is dead and '... (pid N) now belongs to <cmdline> ...' (or 'a process that is not ollama serve') when it was recycled. Have scenario 03's 'the stop log says why' in bl1704OllamaStopPathsSteps.js assert the per-example wording, so the two examples are told apart

## D2

- **Failing command**: git diff origin/main d458fb9f57 -- docs/how-to/BL-1052-local-model-seat-launch.md; sed -n 42,67p swarmforge/scripts/ollama_ancillary_lib.sh; git grep -n BL-1705 origin/main -- docs/reference/Specification.MD
- **Commit hash**: d458fb9f57
- **First error excerpt**: The new 'Ollama is stopped by the swarm (BL-1704)' section says: '- **`external`.** Signals nothing; the stop log names the server (pid, endpoint) as left running.' But BL-1703 writes an external record with an empty pid ('ollama_ancillary_write_record "$record_path" "external" "" "" "$url"', lib line 273), and the stop logs only 'leaving an external ollama server running at $endpoint'. It also says the stale-record log 'says which' (true only once D1 is fixed). The rewritten closing sentence still calls BL-1705 'a separate, not-yet-documented ticket', but BL-1705 is in backlog/done and is documented in docs/reference/Specification.MD (lines 57-84, 'BL-1705: the orphan janitor gains ...').
- **Failure class**: behavior
- **Expected vs observed**: the how-to describes what the stop actually logs and the current state of BL-1705 / observed: it claims a pid is logged for an external server (none is recorded to log) and calls documented, done BL-1705 not yet documented
- **Blamed role**: documenter
- **Remediation pointer**: docs/how-to/BL-1052-local-model-seat-launch.md, 'Ollama is stopped by the swarm (BL-1704)': external -> 'names the server by its endpoint as left running (an external record carries no pid)'. Keep 'says which' in step with D1's final wording. The BL-1705 sentence should point at its Specification.MD section instead of 'not-yet-documented'

By QA.

## Detail

Parcel: documenter fcd287e071, merged into QA as d458fb9f57 (the commit tested).
The only prior bounce is the architect's (d274c8ff02, set -e guard), fixed in
90507597fa: both call sites now use `|| true`. `qa-sibling-check.js status`:
VERIFY BL-1704. No BL-1704 rows in standing-reds.tsv,
property_suite_standing_allowlist.tsv or suite-poles.tsv.
Merge note: the inbound branch still carries BL-1698's rework (QA-bounced,
reverted in bcfcd35eef). The merge kept those paths deleted (named in the
merge body for the deletion guard). No conflicts.

Gates (every one RUN, none BLOCKED; one run each):
- qa-gather.js: stragglers none before/after (pgrep exit 1 = none found);
  sibling VERIFY; register 6 rows, all owned, unowned []; pre_qa_gate.sh
  required_wiring OK; npm test exit 0; npm run test:properties exit 0;
  BL-1704 feature 5/5.
- qa_e2e 2: every stop-path/ollama suite in suite-manifest.tsv, one run, all
  exit 0: test_babysitterd_census_lib.sh (PASS=8), test_kill_all_swarm_ollama_failure_not_fatal.sh,
  test_kill_all_swarm_vitest_reap.sh, test_lifecycle_script_scope.sh (PASS=15),
  test_ollama_ancillary_launch_gate.sh, test_ollama_ancillary_stop_pid.sh,
  test_stop_ancillary_services_ollama_failure_not_fatal.sh,
  test_stop_ancillary_services_onboarder_dual_clear.sh,
  test_stop_ancillary_services_tunnel_orphan_reap.sh, test_swarm_socket_not_in_tmp.sh.
- qa_e2e 3 (diff read): the server pid's live command line is checked
  (ollama_ancillary_pid_is_ollama_serve: alive AND `ps -o args=` matches
  *ollama*serve*) before any signal. Runner children are collected with
  `pgrep -P <server>` only after that check, and each child's own command line
  must read `ollama runner` or `llama-server`. There is no host-wide pattern
  sweep. The invariant holds by reading and by the probe (the unrelated process
  was untouched).
- qa_e2e 4: BL-1704's own paths are the Scope files (ollama_ancillary_lib.sh,
  stop_ancillary_services.sh, kill_all_swarm.sh, the step handler), docs, tests
  (property test, fixture helper, two failure-not-fatal shell tests,
  suite-manifest lines) and evidence.
- Requirements 1 and 4 are met. Requirement 2 is met in code (nothing
  signalled, endpoint named); its "(pid, endpoint)" wording cannot apply because
  BL-1703 records no pid for an external server, so only the how-to claim is an
  item (D2). Requirement 3 is met except "says which" (D1).

Routing: D1 is the coder's and D2 the documenter's (later), so one bounce goes
to the coder and the inventory travels with it.

### Probe script: tmp/bl1704-which.sh (run from the worktree root)

```bash
#!/usr/bin/env bash
# Scratch probe: the stale-record branch of ollama_ancillary_stop_swarm_owned for a
# dead pid and for a pid now owned by a non-ollama process. Stand-ins only.
set -uo pipefail
source swarmforge/scripts/ollama_ancillary_lib.sh
S="$(mktemp -d "$PWD/tmp/bl1704-which-XXXXXX")"
R="$(ollama_ancillary_record_path "$S")"
sleep 300 & dead=$!; kill $dead; wait $dead 2>/dev/null
ollama_ancillary_write_record "$R" swarm-owned "$dead" 2026-09-25T00:00:00Z http://127.0.0.1:11434
echo "dead pid $dead:"; ollama_ancillary_stop_swarm_owned "$S"
sleep 300 & other=$!
ollama_ancillary_write_record "$R" swarm-owned "$other" 2026-09-25T00:00:00Z http://127.0.0.1:11434
echo "reused pid $other (sleep 300):"; ollama_ancillary_stop_swarm_owned "$S"
kill -0 $other && echo "  (unrelated process untouched)"; kill $other
rm -rf "$S"
```
