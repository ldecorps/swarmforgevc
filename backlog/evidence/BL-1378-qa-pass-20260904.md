# BL-1378 — QA pass, 2026-09-04

## Received

Documenter's commit `eb9beed6d2` (task
`BL-1378-an-expedite-closed-ticket-can-never-satisfy-the-close-guard`), merged
into this worktree (`3647e63486`; merge-deletion guard fired on stale
`backlog/hold/BL-1309`/`BL-1356`/`BL-1359` copies un-parked on the incoming
side — same class the hardener's own merge hit, resolved the same way: named
`BL-1296` in the merge commit message).

## Ruling legitimacy — independently re-verified a third time

Coder flagged, architect independently re-verified: `human_ruling:` is the
`(recommended)` option's text verbatim. Checked myself before trusting either
write-up (per the BL-1296/BL-1367/BL-1368 false-positive traps):
`.swarmforge/operator/telegram-approval-ask-messages.json`'s `BL-1378` entry
carries `-- Ruled: ... 2026-09-03 11:19 UTC`, matching the approval commit
`cde3528921` (`Approve BL-1378: record human_approval`, 2026-09-03 12:19:54
BST = 11:19:54 UTC) to the minute. A genuine recorded reply, not an echoed
label — `human_ruling:` is present (not the BL-1367-class pager-approval gap,
which never writes one).

## Verification

- `npm run compile` — clean.
- `bb swarmforge/scripts/test/ticket_close_guard_lib_test_runner.bb` — ALL
  PASS.
- `bash swarmforge/scripts/test/test_bl1378_expedite_close_guard.sh` — ALL
  PASS (31 checks).
- `bb swarmforge/scripts/test/bl1378_expedite_close_guard_property_runner.bb`
  — ALL PROPERTIES HOLD (500 runs), all three declared invariants (BL-654).
- `bash swarmforge/scripts/test/test_ticket_close_guard.sh` — ALL PASS, the
  pre-existing guard unchanged.
- `bb swarmforge/scripts/test/bl869_multi_ticket_close_guard_property_runner.bb`
  — ALL PROPERTIES HOLD.
- `bash swarmforge/scripts/test/test_commit_integrity_cli.sh` — ALL PASS.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1378-an-expedite-closed-ticket-can-never-satisfy-the-close-guard.feature`
  — 12/12 pass, matching coder/cleaner/architect/hardener evidence.
- Process hygiene: no orphaned `node --test`/`stryker`/`vitest` before or
  after (`ps -ef` confirms, past `pgrep`'s own self-match noise).
- Wiring: `commit_integrity_cli.bb:130` calls the PRE-EXISTING
  `ticket-close-guard-lib/validate-close-allowed`, which now internally
  calls the new `close-verdict` (line 249) — read both call sites directly;
  the coordinator's one live caller reaches the new decision without any
  code change of its own, matching the ticket's `required_wiring` anchor
  and the coder/architect's own claim.
- Hardener batch evidence (`BL-1360-...BL-1378-hardener-batch-20260903.md`)
  independently confirms: acceptance 12/12, BL-113 soft Gherkin mutation
  11/11 killed (detached run, ~246s), CRAP unaffected (no TS touched by this
  ticket specifically — the batch's CRAP work was all BL-1367's), DRY
  unchanged, standing whole-tree guard reds pre-existing and unrelated.
- `human_approval: approved`, `human_ruling:` present and cross-verified
  above. `bounce_history`: none.

## Verdict

PASS. Approving BL-1378's own work.

By QA.
