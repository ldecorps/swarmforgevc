# BL-1361 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked

- `post_qa_branch_sweep_lib_test_runner.bb` — re-ran: ALL PASS.
- `test_bl1361_sweep_tells_roles.sh` — re-ran TWICE (this is a real daemon
  spawn under `timeout 300`, worth checking for the flakiness pattern
  that hit BL-1391): 9/9 PASS both times, no interference.
- Confirmed the acceptance fixture drives the REAL daemon path, not a
  fake-adapter seam: `test_bl1361_sweep_tells_roles.sh` calls `bb
  handoffd.bb "$ROOT" --post-qa-sweep-once`, a new one-shot CLI flag
  coder added specifically because `post_qa_branch_sweep_cli.bb` (BL-668's
  existing acceptance seam) uses fake adapters and cannot reach the send
  at all — the same fixture-vs-production gap this session's BL-1379/
  1386/1387 tickets were bounced for, avoided here by construction rather
  than found after the fact.
- Human ruling correctly implemented: `wake-for-reason?` wakes ONLY for
  `:dirty-worktree` (the one reason that doesn't resolve itself); every
  other reason is told via a deferred send riding
  `SWARMFORGE_SKIP_SYNC_INJECT` (an existing switch, not a new mechanism)
  so the note lands in the role's mailbox for its next natural
  `ready_for_next` at no extra turn cost.
- Invariant 2 (told once per landed commit per reason): `tell!` is only
  invoked when `sweep-one-role` returns a genuinely NEW `:surfaced`
  action — the existing surfaced record in state stays authoritative, so
  a repeat sweep of unchanged state sends nothing. Confirmed by the
  "a repeat sweep of the same state tells nobody a second time" row.
- Invariant 3 (one unreachable mailbox never withholds the rest): the
  `tell!` call is wrapped in try/catch inside the `reduce`, logging
  `post-qa-branch-sweep-tell-failed` and continuing to the next role
  rather than aborting the sweep.
- Invariant 1 (unchanged behavior on the branch itself): read directly —
  no new git-mutating call was added; `:fast-forward!` is untouched, and
  the send happens only after `sweep-one-role`'s existing decision, never
  altering it.
- The 80-char note cap: `surface-notice` truncates defensively
  (`(subs text 0 80)`) if construction ever exceeds it, avoiding the
  known "oversized note quarantines silently as .dead" failure mode this
  ticket's own commit message cites.
- `jscpd` over the new step handler and the two touched `.bb` files: 0
  clones.
- `mutation-site-count.js` on the step handler: 117 sites (`over` 100).
  Reviewed and declined to split, same reasoning as every other ticket
  this session: one cohesive single-feature handler.
- TypeScript compiles clean; the handler discovers via BL-1371's
  registry.

No bounce history on BL-1361 (first pass). Forwarding unchanged to
architect.

By cleaner.
