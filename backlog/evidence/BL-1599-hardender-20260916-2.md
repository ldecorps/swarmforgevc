# BL-1599 — hardender review pass, 2026-09-16

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: merge review: cleaner commit removed extension/vitest.bl1599.stryker.config.mjs
- **Commit hash**: e5523a0519
- **First error excerpt**: cleaner evidence (BL-1599-cleaner-20260916-2.md) called it hardener's transient scoped Stryker config left committed instead of reverted; ~20 identical vitest.bl<ticket>.stryker.config.mjs files persist committed on origin/main (bl1081, bl1348, bl1365, ...), and the accepted rule text itself cites vitest.bl1081.stryker.config.mjs as the shape to follow - only the central stryker.config.json pointer is meant to be reverted (already clean, 0 diff vs origin/main), not the scoped file itself
- **Failure class**: coverage-gap
- **Expected vs observed**: the scoped per-ticket Stryker config persists as a committed artifact, matching repo-wide precedent
- **Blamed role**: cleaner
- **Remediation pointer**: restored the file from this hardener's own prior commit (46c409305c) during merge conflict resolution; content unchanged, tests and acceptance re-verified green

By hardender.

## Detail

- Coder's required_wiring re-fix (`8270aec4b0`) is a comment-only change
  (names `SUITE_WORK_BUDGET_MS` in a comment beside the
  `buildSuiteWorkVerdict` call in `extension/scripts/recordTestDuration.js`,
  no logic change) - no new mutation testing needed; this hardener's
  prior pass on this ticket (`46c409305c`) already achieved 100%
  (77/77) Stryker on the scoped file and 27/27 Gherkin kills, and no
  production logic changed since.
- Merge review finding (not a bounce - fixed forward in this same pass):
  the cleaner's re-pass (`358d24099d`, evidence
  `BL-1599-cleaner-20260916-2.md`) removed
  `extension/vitest.bl1599.stryker.config.mjs`, characterizing it as "a
  transient scoped Stryker config left committed instead of reverted".
  Checked against repo-wide precedent: ~20 identical
  `extension/vitest.bl<ticket>.stryker.config.mjs` files persist
  committed on `origin/main` today (bl1081, bl1348, bl1365, bl1369,
  bl1383, bl1402, bl1441 and its sub-runs, bl1455, bl1468, bl1475,
  bl1476, bl1477, bl1509, bl1510, bl1528, bl1537, bl1577), and the
  accepted rule this cites (`local-engineering.prompt`'s "A standing
  Stryker-sandbox red never licenses editing vitest.config.mjs excludes")
  itself names `vitest.bl1081.stryker.config.mjs` as "the shape ... to
  follow" - the part the rule requires reverted before the forward is
  the central `extension/stryker.config.json`'s POINTER to the scoped
  file, which was already byte-identical to `origin/main` before the
  cleaner's edit (no lingering pointer). Restored the file from this
  hardener's own prior commit (`46c409305c`, content unchanged) during
  this pass's merge conflict resolution; re-verified 47/47 unit tests,
  5/5 acceptance, `SUITE_WORK_BUDGET_MS` literal present, standing-red
  register clean (`"unowned":[]`).
- Acceptance pre-check: `BL-1599-the-unit-suite-work-ratchet-holds-the-line.feature`
  5/5 green.
- No orphaned test/mutation processes; no unstaged changes remain.
