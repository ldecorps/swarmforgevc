# BL-1291 — coder pass, 2026-09-05

`extension/test/liveRepoDerivationGuard.test.js` (BL-1038) fails on any
`extension/test` file that derives from the live repository with no pinned
fixture and no recorded exemption. The ticket named three violators at mint
time; a live run at pickup showed only two still red.

## What landed

| file | fix |
|---|---|
| `bl1243PaneActivitySignal.test.js` | converted from `fs.readdirSync(FIXTURES)` (a live directory enumeration, cost grows with however many BL-970 captures accumulate) to a pinned `FIXTURE_FILES` array naming the same seven files the file's own header comment already asserted ("the same seven pane texts"). All four call sites (two filtered by prefix, two unfiltered) now iterate the pinned array. |
| `deprecateRetiredReferents.test.js` | recorded a `// BL-1038-EXEMPT: ...` exemption on the one test that hands the live repo root to `loadRetiredTokens` (production code). This test's whole purpose is catching the extractor mis-parsing a REAL retired marker into a false positive — the same "a test whose whole point is the real tree" class the ticket's own approval_context names for `docsStructureRealTree.test.js`, applied here on the merits rather than pre-assigned. |
| `docsStructureRealTree.test.js` | **untouched, no fix needed.** The ticket's description was accurate at mint time (2026-08-30); since then this file's `REPO_ROOT` derivation switched to `git rev-parse --show-toplevel` (a 2026-09-02 rule_proposal fix for a Stryker-sandbox path bug, unrelated to this ticket), which the guard's pattern matcher does not recognize as a live-root binding at all (`LIVE_ROOT_BINDING_RE` only matches `path.join(__dirname, '..', '..')`). Confirmed by a live run: this file is not among the guard's current violations. Recorded here rather than silently doing nothing, since the ticket's own description named it. |

## A self-inflicted false negative caught mid-fix

My first draft's own explanatory comment above the pinned array literally
contained the text `fs.readdirSync(FIXTURES)` (describing what was
removed) — and the guard's own scan is a plain text-level regex over the
WHOLE file, comments included, so my comment alone kept the file flagged
after the real code was already fixed. Reworded to describe the same fact
without the literal call-shape substring; re-ran the guard clean
immediately after.

## Verification

| check | result |
|---|---|
| `npx vitest run test/liveRepoDerivationGuard.test.js` | **19/19 pass** (was 1 failing, naming 2 files) |
| `npx vitest run test/bl1243PaneActivitySignal.test.js test/deprecateRetiredReferents.test.js test/docsStructureRealTree.test.js` | **22/22 pass** — both edited files' own test suites unaffected by the fix |
| acceptance `BL-1291-...feature` | **5/5 scenario runs** |
| `required_wiring`: `bl1291LiveRepoReadPinnedSteps.js::registerSteps` | handler file exists; discovery registers all 3 scenarios |

### qa_e2e_procedure

1. `npx vitest run test/liveRepoDerivationGuard.test.js` — zero violations,
   confirmed above.
2. Full unit lane (`npx vitest run`): `liveRepoDerivationGuard.test.js` is
   **not** among the 25 failing tests / 15 failing files. The remaining
   failures are the same pre-existing, unrelated cluster a prior coder pass
   this session already documented in full
   (`backlog/evidence/BL-1402-coder-20260905.md`: "26 failures across 16
   files... confirmed pre-existing, not a regression", traced back to
   before BL-1383's own commit) — `pilotAcceptanceGate.ts`'s
   `checkOrphanedAuthoredDocs is not a function` family
   (`pilotAcceptanceGate.test.js`, `crossFileDuplicationCheck.test.js`,
   `multiBranchParserCoverageCheck.test.js`,
   `perHatRolePromptEvidenceCheck.test.js`, `pilotScopedCrapCheck.test.js`,
   `shellEntryPointDriveCheck.test.js`, `unreachableStepHandlerCheck.test.js`)
   plus repo-tree self-checks
   (`constitutionDocCitations.test.js`, `tempDirTrapGuard.test.js`,
   `socketFixtureShortRootGuard.test.js`,
   `operatorRuntimeBbFixtureClosure.test.js`, `pricingTable.test.js`) and
   two others (`telegramClient.test.js`, `telegramCursorOperatorExec.test.js`,
   `backendSwitch.test.js`) — the SAME list that evidence names, minus
   `liveRepoDerivationGuard.test.js` itself, which this ticket removes from
   it. `npm run compile` before the full run ruled out a stale build as the
   cause of the remainder; several are also formally registered in
   `backlog/standing-reds.tsv` (BL-1289, BL-1221, BL-1212, BL-1229, BL-1290,
   BL-1263 x2). Out of this ticket's scope (a different guard's own
   pre-existing family).
3. Every scenario in `BL-1291-...feature` resolves to a registered handler
   — confirmed by `run_acceptance.sh` passing 5/5 (a missing handler would
   THROW per BL-1371 discovery, not silently skip).

## Declared invariant (BL-654)

- **"A test's cost and verdict never depend on live repository content it
  did not establish; where a test must read live state, the reason it
  cannot use a pinned fixture is recorded rather than left implicit."**
  This invariant is a claim about TEST FILE SHAPE (a documentation/process
  convention enforced by an existing, unmodified guard), not a pure
  function this parcel introduces to property-test. Per BL-654's own
  allowance for a declared invariant with no fresh executable encoding to
  record a stated reason instead: `liveRepoDerivationGuard.test.js` (BL-1038,
  pre-existing, untouched by this ticket) is itself the executable
  encoding of exactly this invariant, run against the real tree in
  qa_e2e_procedure step 1/2 above and in this ticket's own acceptance
  scenario 02. Writing a second, parallel property test asserting the same
  relation this guard already asserts would encode nothing new — the
  ticket's own `stage_skip_reasons` for `hardender` says as much explicitly
  ("no production module changes... Scenario 03 pins the exemption path so
  it cannot become a silent loophole, which is the non-vacuity check the
  hardener would otherwise be asked for, and it is in the acceptance
  contract").

## required_wiring

- `bl1291LiveRepoReadPinnedSteps.js::registerSteps` — the handler file
  exists; discovery registers all 3 scenarios (5 runs), confirmed by
  `run_acceptance.sh` passing 5/5.

## Stage routing note

This ticket declares `required_stages: [coder, architect, qa]` (cleaner,
hardender, documenter explicitly skipped with stated reasons in the
ticket's own `stage_skip_reasons`). Forwarding to `cleaner` per the
role's own default instruction, per `swarm_handoff.bb`'s BL-606
`required_stages_routing_enabled` mechanism (confirmed `true` in
`swarmforge.conf`) — the send path rewrites the recipient to `architect`
automatically since cleaner is not in this ticket's declared set.
