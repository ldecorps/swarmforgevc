# BL-1753 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Correction: `BL-1753-hardender-20260926.md` (committed `0baa8d429a`) mis-used
the Article 4.4 bounce-inventory format (`D1`, `Blamed role: hardender`) for
what was actually normal in-pass hardening, not a send-back — no
`record-bounce.js` call was made and nothing was routed to another role.
This file is the correct verdict for the pass: NONE outstanding.

What that first file's D1 actually described, fixed in commit `81b1f7a04c`
before this evidence was written: a scoped Stryker mutation run
(`vitest.bl1753.stryker.config.mjs`, avoiding the standing
Stryker-sandbox-only red in `activePoolFreshnessAudit.test.js`) against
`out/tools/night-closing-ceremony-run.js` found 4 non-Killed mutants inside
`rotateDocumenter`'s own changed range (lines 146-168):

- NoCoverage/Survived on the `!fs.existsSync(rotate)` missing-script guard
  — closed with a new dedicated file,
  `nightClosingCeremonyRotateDocumenterMissingScript.test.js` (kept OUT of
  the frozen `nightClosingCeremonyRotateDocumenterFallback.test.js`:
  BL-1593's own acceptance gate pins that file's `npx vitest run` output at
  literally "3 passed"). A bare "does not throw, no marker written"
  assertion could not discriminate the guard from its removal, so the new
  test intercepts `bash` via `PATH` with a logging stub and asserts it is
  never invoked. Confirmed non-vacuous by hand-emptying the guard's block
  and watching the test go red, then restored.
- Survived StringLiteral on the `'coordinator'` `SWARMFORGE_ROLE` value —
  closed by extending the fallback test's three existing stub scripts to
  log `SWARMFORGE_ROLE` too and asserting `actor=coordinator` on every
  call line (no new test added, so BL-1593's frozen "exactly once"/"3
  passed" gates stayed green).
- Survived StringLiteral on `stdio: 'pipe'` and Survived BlockStatement on
  the catch body (`return false` emptied) — both confirmed EQUIVALENT by
  patch-and-diff against the real compiled file and the full local test
  suite: `stdio: ''` vs `'pipe'` produced byte-identical `execFileSync`
  capture behaviour (verified directly with `child_process` too), and
  `run()`'s return value is only ever read in a boolean context
  (`if (run(false))`), where implicit `undefined` and explicit `false` are
  indistinguishable.

Also found and fixed the stale-`out/` trap (BL-497) before any of the
above: the merged TS source had not been recompiled, so the acceptance
feature's scenario 02 first failed against the OLD `spawnConsultDocumenter`
fallback that should have been deleted. Recompiled, reran, 4/4 green —
confirmed this was the known compile-lag trap, not a real regression.

Gates run: BL-1753 feature (4/4), BL-658 (10/10), BL-1593 (5/5), BL-1641
(4/4), `extension/test` `nightClosingCeremony*` (61/61 with the new file),
all 22 whole-tree guard files (211/211, required by touching
`extension/test/`). No orphaned processes, clean tree.
