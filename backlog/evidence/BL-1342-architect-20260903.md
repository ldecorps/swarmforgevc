# BL-1342 — architect pass (2026-09-03)

## Merge

Merged cleaner `02aecc319b` (coder `18933a4dba` + self-audit `4759eef785`)
into the architect worktree. Clean fast-forward, no conflicts.

## Nature of this ticket

BL-848 review-only stamp-off of already-landed commit `27d6ab8630`. Same
shape as BL-1333, reviewed earlier this session. Confirmed the parcel
touches ONLY test/fixture/step-handler files — no production code, and
`backlog/hotfix-ledger.yaml` is untouched (`state: stamp-open`,
`human_decision`/`decided_at` implicitly null per the row).

## required_wiring confirmed live (all four anchors)

- `handoff_lib.bb::read-envelope-if-present` — present (1 occurrence).
- `handoffd.bb::outbox-parcel-unreadable` — present (1 occurrence).
- `handoffd_supervisor.bb::daemon-age-ms (file-age-ms pid-file)` — present
  at line 582 exactly as named. This is the anchor the coder's own
  self-audit caught missing an assertion for in the first commit and fixed
  before forwarding — good catch, since without it the grace scenarios would
  all still pass keyed on an age this daemon never actually produced.
- `specs/pipeline/steps/index.js::bl1342SwarmStampHandoffdCrashloopHotfixSteps`
  — registered.

## Review of the harness

- The vanished-parcel fixture reproduces the exact 2026-09-02 race
  deterministically: a fake `tmux` shim renames (not deletes) the trailing
  parcel while the daemon is still delivering the parcel ahead of it in the
  same poll — a real listing-then-gone race, not a permissions trick.
- Invariant 1 / scenario 03 drives the real `read-envelope-if-present` with
  `with-redefs` over `slurp` to inject both I/O and non-I/O exception
  classes, confirming the catch is `java.io.IOException` specifically (by
  execution AND by reading the function body for the absence of a
  widened `Exception`/`Throwable` catch and any bare `slurp` left in the
  poll loop).
- Invariant 3 / the grace scenarios confirm the `cond` ordering places
  `(not alive?) :dead` BEFORE `within-startup-grace? :healthy` — the
  structural guarantee that the grace can never override a dead verdict —
  and scenario "the grace never rescues a daemon that is not running"
  exercises this directly (`alive: false`, young age → `:dead`, confirmed
  green).
- Scenario/property reach floors are enumerated (io/non-io groups called
  unconditionally; grace CASES iterated via `for`), so none share the
  BL-1345/BL-1352 luck-drawn-reach defect shape.

## Independently re-run

- `run_acceptance.sh specs/features/BL-1342-…feature` — 9/9 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1342CrashloopStampInvariants` — 3 consecutive runs, 3/3 tests each.
- `test_handoffd_outbox_vanished_parcel_wiring.sh` — 4/4 PASS.
- `handoffd_supervisor_startup_grace_test_runner.bb` — ALL TESTS PASS.
- `bl977_supervisor_progress_property_runner.bb` — 200 draws, ALL
  PROPERTIES HOLD.
- Dependency-rule gate (BL-259), scoped and full-repo: PASSED, no forbidden
  edges.
- Co-change report (BL-255): all reported co-changes at threshold —
  expected self-coupling within this ticket's own three files.

## Item 7 of qa_e2e_procedure (pre-existing, not this parcel's)

The ticket itself flags `test_handoffd_supervisor.sh` scenario 01 as failing
identically on unmodified main (tmux-shim shadowing). Not re-litigated —
consistent with the ticket's own instruction and the cleaner's evidence.

## Correctness read

No defect. The review harness genuinely drives the landed code (not a
reimplementation), all four wiring anchors are live, and the constraints
(narrow catch only, bounded grace only, ledger untouched) are verified both
by execution and by static inspection.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
