# BL-1299 — QA pass, 2026-08-30

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1299` → `VERIFY BL-1299` (exit 0).

## Merge

Merged documenter's cited commit `1137549a2e` into QA at `5486e839db`
(`git merge --no-ff`). Ancestry re-confirmed: both `866fee1228` (my own prior
QA tip) and `1137549a2e` (documenter's tip) are ancestors of the merge.
`push-rejection?` (BL-1288) survives the merge unchanged.

## Architect + hardener corroboration

Both re-read (`BL-1299-architect-review-20260830.md`,
`BL-1299-hardener-pass-20260830.md`): clean architect pass; hardener found
and closed one real hand-mutation survivor (`back-one` branch coincided with
`back-all`'s output at the only index every existing fixture used —
`cleaner`, pipeline index 1 — so mutating `back-one` to reuse `back-all`'s
`(take idx roles)` was invisible; fixed with a deeper-index fixture at
`hardender`/index 3). Both invariants (no master-resident ever addressed;
terminal stamp unmoved) independently confirmed encoded and non-vacuous.

## Wiring

- `pack-role-names` fully retired from `swarm_handoff.bb`; both call sites
  (`last-pack-role?`, `reverse-roles`) now delegate to
  `reverse-hop-lib/last-pipeline-role` / `reverse-hop-lib/reverse-recipients`.
  `grep -rn pack-role-names` across the tree: one hit, in the feature file's
  own background prose (naming the pre-fix defect), zero remaining call
  sites.
- `specs/pipeline/steps/index.js:902` registers
  `bl1299ReverseHopMasterResidentSteps` — confirmed by grep.
- Step handlers drive the REAL `reverse_hop_lib.bb` via `bb`/`execFileSync`
  (confirmed by reading the file's own header comment and the `REVERSE_HOP_LIB`
  path constant) — not a second implementation of the selection math, which
  is exactly the class of bug (a coincidental-fixture re-derivation) the
  ticket itself was minted from.

## Acceptance (BL-112)

`specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1299-reverse-hop-skips-master-resident-roles.feature` →
**9/9 green** (5-example Scenario Outline for recipient exclusion including
the hardener's added `hardender|back-one|architect` deep-index row, the
specifier-never-addressed scenario, the terminal-role-unmoved scenario, and
the 2-example roles-table-derivation scenario that fails a hardcoded
name-list implementation by design).

## `.bb` test runners (own gate — no Stryker/CRAP/DRY wired for Babashka)

- `reverse_audit_handoff_test_runner.bb` → **ALL PASS** (23 checks).
- `bl1299_reverse_hop_property_runner.bb` → **ALL PASS** (500 runs); reach
  floors met for all five properties (`any-master-resident`,
  `non-obvious-master-resident`, `deep-pipeline`, `non-empty-reverse`,
  `terminal-metamorphic`).

## Full unit suite (`npm test`) and property suite (`npm run test:properties`)

Identical failure counts to my own prior BL-1288 pass on this same session
(16 files / 26 tests unit; 25 files / 14 tests property) — re-grepped every
failing file/message for `bl1299`, `reverse.hop`, `reverse_hop`: **zero
matches, both lanes**. BL-1299's diff touches no `extension/src`/`extension/test`
file at all (all `.bb`/`specs/pipeline/steps`/backlog), so this is expected,
not merely tolerated. Same pre-existing standing debt as
`BL-1288-qa-pass-20260830.md` (BL-1221/BL-1289/BL-1290/BL-1291 + prior
QA-corroborated repo-hygiene reds). Orphan check clean before/after both
lanes (`pgrep -fl 'node --test|stryker'`).

## Compile + boot-prefix gate

- `npm run compile` (extension/): clean.
- `bash swarmforge/scripts/boot_prefix_budget_gate.sh`: `ok — 41952/44000
  chars` (hardener's H10 re-confirmed at my own tip; the ticket's own notes
  track the separate BL-1300 headroom question, not reopened here).

## Documenter pass — verified deliberately empty, not missing

Documenter's commit is a bare merge of hardener's tip with no documenter-
authored commit and no `BL-1299-documenter-*` evidence file. Checked this is
NOT the BL-575 "missing pass" shape before accepting it:

- The one prose fix this ticket's own notes name
  (`swarmforge/constitution/articles/02_handoffs.md`'s reverse-hop paragraph)
  is explicitly specifier-owned (Article 5.1) and explicitly pre-declared
  blocked at spec time, pending BL-1300 settling the boot-prefix character
  budget — not a documenter deliverable.
- `grep -n "pack-role-names\|back-all\|reverse" swarmforge/handoff-protocol.md`
  → zero matches: no living doc describes this mechanism, so nothing there
  is stale.
- `grep -i "reverse\|back-all\|back-one" docs/diagrams/handoff-flow.mmd
  docs/diagrams/swarm-flow.mmd` → zero matches in both: neither registered
  diagram depicts reverse-hop recipient selection (handoff-flow.mmd is
  explicitly scoped to file-lifecycle HOW, not recipient WHO, per its own
  header comment), so no diagram-currency trigger fires (local-engineering
  .prompt "Diagrams").
- No `swarmforge/roles/*.prompt` file names `pack-role-names` or a hardcoded
  reverse-hop exclusion either.

No documenter-domain gap. Not bouncing.

## Entangled tip — BL-1241 remedy, again

`bb land_step_cli.bb BL-1299-... 5486e839db <repo-root>` →
`LAND_REPLAY land-replay/BL-1299-5486e839db dbcdd00722`,
`ENTANGLED_SIBLING BL-1288`, `ENTANGLED_SIBLING BL-1295`. Both names are
artifacts of MY OWN prior bookkeeping this same session, not real unlanded
functional work — verified before accepting the replay:

- `git diff origin/main HEAD -- swarmforge/scripts/master_main_reconcile_lib.bb
  docs/how-to/BL-1288-*.md` → empty. BL-1288's actual production/doc content
  is already byte-identical on `origin/main` (landed as `16570bc52b` earlier
  this pass) — the tool names it "entangled" only because MY OWN QA-evidence
  commit (`866fee1228`, adding `BL-1288-qa-pass-20260830.md` and the
  `abandoned_commits:` yaml edit) is itself unlanded, ticket-tagged content
  that was never meant to ride onto `main`.
- `git diff --name-only 5486e839db dbcdd00722` → exactly three files:
  the same BL-1288 evidence/yaml pair plus the BL-1295 confirmation note
  (same file already excluded from BL-1288's own land, for the same reason:
  its substantive work already landed as a passenger of BL-1297).
- BL-1299's own files (`reverse_hop_lib.bb`, `swarm_handoff.bb`, the feature
  file, the step handler, `index.js`, both `.bb` test runners,
  `suite-manifest.tsv`) diff empty between `5486e839db` and `dbcdd00722`.
- `git show dbcdd00722:specs/pipeline/steps/index.js | grep bl1299` →
  present. Wiring survives the rebuild.
- Replay parent is `16570bc52b`, current `origin/main` tip exactly (no
  drift since BL-1288's own landing minutes earlier).

## Landed

BL-1144 discipline from a scratch worktree at `land-replay/BL-1299-5486e839db`
(`HEAD` = `dbcdd00722`, matching the commit under evaluation): acquired
`.swarmforge/land-main.publish.lock`, `land_main_publish.sh --decide-only` →
`:tip-contains-origin true, :purity-action :push, :origin-advanced-since-gate
false`. `git push origin dbcdd00722:main` → `16570bc52b..dbcdd00722 ->
main`, fast-forward. Released the lock, removed the scratch worktree, deleted
the fulfilled `land-replay/BL-1299-5486e839db` branch.

**Landed commit: `dbcdd00722`, not the originally-cited `5486e839db`.**
`abandoned_commits: [5486e839db]` recorded on the ticket YAML.

BL-1299 carries no `GH-` id — no `issue_done.sh` step required.

## Merge-up broadcast

Sent to coder, cleaner, architect, hardender, documenter: merge up to
`5486e839db` (the normal pipeline-chain QA-approved commit; `dbcdd00722` is
a `main`-only landing artifact, not part of any worktree role's own branch
lineage). Coordinator notified for bookkeeping, naming both commits.

## Verdict: APPROVE

By QA.
