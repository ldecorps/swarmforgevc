# BL-1052 / BL-1053 — hardener pass

Merged architect's clean review of BL-1052 (`99db10c752`) plus the coder's
same-day amendment (`1696fb41fe`, "new qwen_code ancillary arm") — the
amendment's diff carries BL-1053's own step handlers, property runner and
shell suite alongside BL-1052's, so this pass hardens both tickets
together and forwards both (Article 2.6).

Both `.bb`/`.sh` (BL-1052) and `.bb`/`.sh` (BL-1053) — no TypeScript
touched; engineering.prompt's no-mutation/CRAP/DRY lane, gated by unit +
property + acceptance suites.

Checked both new property runners' generator seeding given the day's
recurring defect class (BL-991, BL-1057, BL-1069 all hit a per-run
`make-rng` reseed): both `bl1052_qwen_code_seat_property_runner.bb` and
`bl1053_provider_routing_property_runner.bb` already use a single
top-level `(def rng ...)` threaded across their runs — the correct,
established convention. No fix needed here.

## Suite verification, re-run live

- `test_qwen_code_seat.sh` (BL-1052): **ALL PASS**.
- `bl1052_qwen_code_seat_property_runner.bb`: **ALL PROPERTIES HELD** (200
  map runs, 40 launch runs).
- `test_qwen_code_ancillary_family.sh` (BL-1053): **ALL PASS**.
- `bl1053_qwen_provider_routing_test_runner.bb`: **ALL PASS**.
- `bl1053_provider_routing_property_runner.bb`: **ALL PROPERTIES HELD**
  (300 runs).
- Acceptance: BL-1052 **8/8**, BL-1053 **7/7**.
- BL-113 Gherkin mutation (one Scenario Outline each): BL-1052 **2/2
  killed**, BL-1053 **6/6 killed**, 0 survived, 0 errors on both.
- `required_wiring`: both step handlers confirmed registered in
  `specs/pipeline/steps/index.js` (acceptance ran without a
  missing-handler failure on either feature).

## A stray backlog duplicate found, not mine to fix

`backlog/paused/BL-1053-...yaml` and `backlog/active/BL-1053-...yaml` both
exist simultaneously in the tree (confirmed via `git log`: the promotion/
expedite commit for BL-1053 left the paused copy behind instead of moving
it). Not this parcel's scope — sent a `note` to the coordinator rather than
touching backlog bookkeeping myself.

## Orphaned processes / leaked fixtures

Checked before and after — clean. Gherkin mutation work dirs removed after
each run.

## Verdict

Both hardened, all gates green. Forwarding BL-1052 and BL-1053 as two
separate `git_handoff`s from this one commit (Article 2.6).

— By hardender.
