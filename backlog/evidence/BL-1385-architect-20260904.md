# BL-1385 — architect review, 2026-09-04

Reviewed coder commit merged in via cleaner `aa44d9cc83`.

## Checks run

- Dependency gate on `specs/pipeline/steps/bl1385HandlerModuleGraphGuardSteps.js`:
  PASSED, no forbidden edges.
- Co-change: `check_handler_module_graph.sh` is a new file with no
  co-changers (expected); `land_step_lib.bb`'s hits are the pre-existing
  hub coupling of the land-replay guard list, nothing new.
- `required_wiring`: both anchors present and correct —
  `run_commit_guards.sh:73` (`run_guard check_handler_module_graph.sh`, in
  the cheap tier, correctly using the same `|| st=$?`-via-`run_guard`
  capture discipline as its siblings, no `set -e` bypass) and
  `land_step_lib.bb:993` (`replayed-tree-guards` list, alongside the
  registration check). Step handler registers (`check_feature_handler_
  registration.sh` exit 0).

## Re-verified independently, not trusted from evidence

- `bash swarmforge/scripts/check_handler_module_graph.sh` (no args, this
  worktree) — exit 0, 947 handlers.
- `bash swarmforge/scripts/check_handler_module_graph.sh a93aa4a18f .` —
  exit 1, `HANDLER_LOAD_BLOCK`, names `bl1296BubbleSeatSteps.js` and
  `.../extension/out/tools/bubbleSeat` exactly as the evidence claims —
  this is the real 2026-09-04 incident commit, reproduced and caught.
- `run_acceptance.sh` on the BL-1385 feature — 9/9.
- `check_feature_handler_registration.sh` — exit 0, unaffected.

## Invariants

- Invariant 1 (no commit reaches main with an unloadable discovered
  handler, via either path): satisfied by the dual wiring above.
- Invariant 2 (verdict is a function of the tree alone, never the
  checking worktree): read the resolver directly
  (`check_handler_module_graph.sh`'s embedded `Module._resolveFilename`
  hook) — the child process is rooted at a `git archive`-materialised
  tree, and the `extension/out/<p>.js -> extension/src/<p>.ts` mapping
  reads paths under that materialised root exclusively, never the
  checker's own worktree. Structural, not a discipline, matching the
  ticket's own framing. The a93aa4a18f re-run above is itself proof of
  one direction (my worktree HAS `bubbleSeat.ts` today; the checked tree
  does not, and it still refused).
- The `node_modules` exclusion and the relative-require-to-absolute fix
  (both self-caught false-positive classes in the coder's evidence) read
  correctly in the code: dependencies are excluded from tree-content
  checking by design, matching the ticket's own scope note.

## Correctness read

No defect found. The design choice (git-archive + child-process require
over static parsing) is well-justified given the nine computed require
forms measured across 947 files; the `.ts`-stub-not-load choice avoids
needing `tsc` in a commit-time guard, correctly reasoned since
`extension/out/` is gitignored and never archived. The single-process
collapse (27s -> 2s) is a legitimate performance fix that preserves the
guard's actual question (resolution, not coexistence) — re-verified by
the coder after the change per their own evidence, and independently
timed as fast in my own runs above.

## Verdict

COMPLIANT. Forwarding to hardener.
