# BL-1422 documenter review pass — 2026-09-05

Reviewed the hardened commit (`d31a220eb1`) and the coder/cleaner/architect/
hardener trail for BL-1422 (a Work note is not completed without work).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1422-a-work-note-is-not-completed-without-work.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1422WorkNoteNotCompletedWithoutWorkSteps.js`; the
  `done_with_current_task.bb::dispatch-trail-ticket-id` recognition
  required by the first wiring line is present (`work-note-ticket-id`
  delegates to `work-note-evidence-lib/work-note-ticket-id-from-message`,
  which delegates to `chase-sweep-lib/dispatch-trail-ticket-id`).
- Operator/agent-visible behavior change (task-mode `done_with_current.sh`
  now refuses to complete a Work dispatch note with no commit/git_handoff
  evidence since dequeue, unless given `--no-work "<reason>"`) is
  documented in `docs/how-to/BL-652-done-with-current-arg-rejection.md` —
  the existing how-to for this helper's argv contract (BL-652) — with a
  new "`--no-work` and the Work-note evidence gate" section, an updated
  contract table, and an updated operator/agent check block.
- `docs/index.md`'s one-line summary for that how-to updated to mention
  the new gate.
- `swarmforge/handoff-protocol.md` (the constitution-cited exhaustive
  reference for the handoff helpers, Article 2.2) had a stale
  `done_with_current_task.sh` responsibilities list — updated with the
  Work-note evidence gate and the `--no-work` argv exception so the
  reference matches current behavior.
- No "Last Updated" freshness field in any of the three edited files — no
  date bump owed.
- Checked `swarmforge/constitution/articles/02_handoffs.md` and
  `swarmforge/PIPELINE.md` for other `done_with_current` mentions: both
  reference it only for unrelated concerns (non-forwarding merge-only
  inbounds, the merge-up sequence) that this ticket doesn't change — no
  edit needed there.
- No registered diagram depicts `done_with_current`'s internal completion
  logic (this is a guard added inside an existing helper's decision path,
  not a new component, boundary, or pipeline-topology change) — no
  diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
