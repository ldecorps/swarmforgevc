# BL-599 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Delivered

Wired acceptance for intake balance trend: `deriveIntakeBalanceEvents` and
`computeIntakeBalance` in `deliveryMetrics.ts` (already on main), plus
`bl599TrendIntakeBalanceSteps` and property tests for declared invariants.

## Invariants (BL-654)

| invariant | encoding |
|---|---|
| Same git-history adapter as deliveryMetrics | derive from `GitLogEntry[]` only |
| Epic trackers excluded | property P1 |
| Pure aggregation | `computeIntakeBalance` over in-memory events; acceptance scenario 02 |

## required_wiring

All three anchors registered and exercised.

## Verification

| check | result |
|---|---|
| `deliveryMetrics.test.js` (intake section) | pass |
| `deliveryMetricsIntakeBalance.property.test.js` | 3/3 |
| `run_acceptance.sh BL-599…feature` | **7/7** |

By coder.
