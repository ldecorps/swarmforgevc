# BL-1353 architect re-review — supersede merge, still clean

## What changed since the first pass (f28ebd7c06)
A cleaner supersede note (`ff002a9cbc` supersedes `7229395b44`): the coder
independently produced its own fix for the same specifier amendment
(`6ce8257a7e`), and the cleaner reconciled a duplicate step registration —
kept the coder's version (adds an `s.hibernated` assertion in the relaunch
step), dropped the cleaner's own now-redundant `backlogDrained` field.

Merged `ff002a9cbc` clean, no conflicts.

## Review
Diff is confined to `specs/pipeline/steps/bl1353TaskArrivedIsNotAnEscalationSteps.js`
(state shape gains `hibernated`, an extra assertion that the relaunch
up-trigger only evaluates from a hibernated state) and the coder evidence
file's wording. No `.bb` production logic touched — everything in my prior
review (both invariants, required_wiring, dependency/co-change gates)
stands unchanged.

## Re-verification
`node specs/pipeline/cli.js specs/features/BL-1353-...feature` — **4/4
pass** on the merged tree.

## Verdict
No architecture violation, no invariant violation, no correctness defect.
Forwarding to hardener (supersedes the prior BL-1353 forward at
f28ebd7c06).
