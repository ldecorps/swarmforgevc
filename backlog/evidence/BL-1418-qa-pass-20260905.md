# QA pass — BL-1418 — 2026-09-05

Commit under review: `8bfcd5f357` (merge of documenter's `432944e056` into
`swarmforge-QA`), carrying the full chain (`d8690e2995` BL-1418 coder,
confirmed ancestor via `git merge-base --is-ancestor`). Includes a
mid-flight amendment (`184089b44b`, "amend qa_e2e step 5 to the honest
factory answer") I already merged earlier this session per the
specifier's own note, plus mint BL-1437 (Model Steward follow-up).

## Verification

- Acceptance: `run_acceptance.sh
  specs/features/BL-1418-the-art-director-seat-is-addressable.feature`
  — 7/7 scenarios pass (initial run of the standalone unit-test files
  showed 2 stale-`out/`-caused failures, resolved by `npm run compile` —
  same benign cause hit repeatedly this session; re-ran clean).
- `npx vitest run test/pipelineBoard.test.js test/epicIcon.test.js
  test/roleTopicMapStore.test.js test/topicIcon.test.js
  test/telegramFrontDeskBotCli.test.js
  test/bl1418RoleEnumerationClassification.test.js`: 468/468 pass.
- **Live-verified qa_e2e step 5 directly** (the amendment's own honest
  answer): `bb model_factory_cli.bb assign --mode quality` ran clean with
  no `art-director` entry and no error (grep for either returned nothing);
  `bb model_steward_cli.bb role-matrix art-director --include-uncertified`
  exited 0 and listed nothing — exactly the "absent rather than erroring"
  degradation the amendment specifies.
- Read the role-enumeration split directly: `ALL_SWARM_ROLES = [
  ...PIPELINE_CHAIN, 'coordinator', 'art-director']` — `PIPELINE_CHAIN`
  itself confirmed untouched (`grep -rn PIPELINE_CHAIN ... | grep
  art-director` empty), while the swarm-roles lists
  (`roleTopicMapStore`, `topicIcon.ts`, `model_factory_lib.bb`) each gain
  the seat — matches invariant 1's chain-vs-swarm-roles split exactly.
- Icon collision: 🔮 chosen over the obvious 🎨 because 🎨 collides with
  `epicIcon.ts`'s own fixed epic-pool glyph (a DIFFERENT icon pool per
  local-engineering rule 6 — epic icons vs role-topic icons are separate
  pools with separate collision rules) — confirmed by reading both files;
  🔮 absent from every role/epic/standing-topic icon table and present in
  the live 112-sticker set.
- Pack config: `window art-director ...` line present, task receive mode
  (default, omitted per its own comment), forward-only, no reverse-hop
  window — matches "outside the forward pipeline chain" (Article 1.10).
- `required_wiring`: `full-forge.conf` declares the window;
  `topicIcon.ts` carries the seat; `specs/pipeline/steps/bl1418ArtDirectorSeatSteps.js`
  exports `registerSteps`.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- `bl1040SeatIdentityObservationPath.property.test.js` (the touched
  property regression): 4/4 pass. Full property suite: exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
