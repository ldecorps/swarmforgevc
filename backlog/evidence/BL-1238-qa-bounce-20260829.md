# BL-1238 — QA bounce, 20260829

## D1 — entangled tip: cited commit carries repeated `main`/expedite merges across four other tickets' state

1. **Failing command:**
   ```
   git diff --name-only origin/main...2d68050646 | wc -l
   ```
   → **66** changed paths (multiple-merge-base warning: `using bf302a266e73e271b305a08fa601a5f4e18e439f`).

2. **Commit hash:** `2d68050646` — "BL-1238: document agent-side idle-clear fullness parity",
   received via `git_handoff` payload `merge_and_process documenter 2d68050646`.
   Its own merge history (`git log --oneline --merges bcd78251f8..2d68050646`)
   surfaces `c6f64f84c Merge main: pick up BL-1247 retirement`, `e00c0323c
   Merge QA bf302a266e merge-up for BL-1211`, `2c1044c57 Merge main into
   hardender for BL-1247-reconcile-sweep-kill-switch`, `fb72a0698 Merge
   origin/main into expedite/BL-1248`, `2b3c796d1 Merge BL-1236`, and
   several more — none of which have any business in a documenter parcel
   for BL-1238.

3. **First error excerpt** — the 66 paths span, beyond BL-1238's own scope:
   fresh `backlog/paused/BL-1252` .. `BL-1255` ticket YAMLs and topic
   records (unbuilt specifier mints), `swarmforge/constitution/articles/
   engineering.prompt` and `reference/engineering-detailed.prompt`
   (a constitution amendment still local-only, unpushed to origin/main),
   `backlog/done/M8/BL-1192` and `BL-1248`, `backlog/done/BL-1236`,
   `backlog/evidence/BL-1191-restart-gate-twelfth-reading-discharged-*`,
   `BL-1236-thirteenth-reset-*`, `BL-1247-*` adjudication evidence, and
   restored `backlog/active/`+`backlog/hold/` copies of BL-1179/1207/1211/
   1222/1228/1230 (this session's own BL-1211 landing content, re-merged
   via a criss-cross rather than fast-forwarded). This is not a scoped
   multi-ticket batch (Article 2.6) — it is repeated whole-branch merges
   of `main` into a role worktree, same class BL-1211's own third bounce
   was filed for (`backlog/evidence/BL-1211-qa-bounce-20260828.md`), and
   the exact anti-pattern `check_merge_deletion.sh` (BL-1242, landed this
   session at `b6cb7a951`) exists to make visible on the deletion side —
   this is its addition-side twin.

   Isolating BL-1238's own new contribution over this parcel's immediate
   predecessor (`bcd78251f8`, already landed as part of BL-1242): `git diff
   --name-only 6e30f1650 2d68050646` shows only `docs/reference/
   Specification.MD` — meaning ALL of the other 65 paths were already
   present by `6e30f1650` ("Merge hardender 2878210cd0 for BL-1238"),
   i.e. the entanglement happened earlier in this same documenter pass,
   between `bcd78251f8` and `6e30f1650`, not at the final doc commit.

4. **Failure class:** `behavior` (BL-506 entangled tip, same class as
   BL-1211's own third bounce).

5. **Expected vs observed:** Expected — a `git_handoff` naming `BL-1238`
   cites a commit touching only BL-1238's own paths (plus ordinary
   ancestry from already-landed sibling tickets). Observed — a chain of
   `main`/expedite merges into the documenter worktree, carrying four
   unbuilt specifier mints, a local-only constitution amendment, and
   duplicated hold/active state for six already-landed or already-parked
   tickets.

## Other checks this pass

- BL-1238's own functional correctness not independently re-verified —
  **BLOCKED BY D1**: no productive way to isolate BL-1238's own diff from
  a repeatedly-entangled branch without rebuilding it tip-pure first.
  (Its own shell/bb test runner, acceptance feature, and required_wiring
  anchors were spot-checked informally while investigating the
  entanglement and looked sound — `idle_clear_fullness_lib_test_runner.bb`
  ALL PASS, acceptance feature 9/9 — but that is not a substitute for a
  clean QA pass against a tip-pure commit.)

## Remediation

Documenter (and coder/cleaner/architect/hardener upstream of it, per
BL-1211's own remediation note): do not merge `main` into a role worktree
as a way to pick up unrelated backlog state or sync with sibling tickets —
rebuild a tip-pure `BL-1238`-only commit on the current `origin/main`
(`b6cb7a951`) and re-send. BL-1238's own work — coder, cleaner, architect
(post-bounce), hardener, and the Specification.MD doc paragraph — should
carry over cleanly into the rebuild; nothing here suggests any of it is
unsound, only that this specific commit cannot be landed as BL-1238's
approval.

By QA.
