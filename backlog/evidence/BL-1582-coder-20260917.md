# BL-1582 coder pass

New page `docs/how-to/BL-1582-onboarder-run-an-onboarding-end-to-end.md`,
written from the six source files named in the ticket's `How` direction
(`launch_onboarder.sh`, `onboarder_supervisor.bb`,
`stop_ancillary_services.sh`, `onboarder-reconcile.ts`, `onboarderState.ts`,
`contractPhaseRelay.ts`) plus `start_ancillary_services.sh` for the
ancillary-start env gate. Every runtime file name and repo path in the page
was grepped out of the source it names, not recalled:

- `onboarder-supervisor.pid` / `.log` — `launch_onboarder.sh`
- `onboarder-supervisor.stop` — `stop_ancillary_services.sh`
- `onboarder-heartbeat.json` — `extension/src/tools/onboarder-reconcile.ts`
- The 9 `OnboardingPhase` values and the 5 `PREREQUISITE_STEP_IDS`, in
  order — `extension/src/onboarding/onboarderState.ts`
- The launch-handoff message text ("You run this - I cannot launch or
  observe the target host myself...") and the retry instruction ("Fix the
  issue and post \"proceed\" to retry.") — `extension/src/onboarding/contractPhaseRelay.ts`
- `.swarmforge/onboarding/<slug>.json` per-target state path —
  `extension/src/onboarding/onboarderStateStore.ts`
- `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` / `SWARMFORGE_SKIP_ONBOARDER`
  gate and the WARN line — `swarmforge/scripts/start_ancillary_services.sh`
  lines 98-106
- `ONBOARDER_MAX_ATTEMPTS` (default 5) / `ONBOARDER_GIVEUP_COOLDOWN_MS`
  (default 900000) — `swarmforge/scripts/onboarder_supervisor.bb`

## Step handler and acceptance

`specs/pipeline/steps/bl1582OnboarderHowToSteps.js` lands in this same
parcel (BL-233). `specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1582-*.feature`: 26 of 26 scenarios pass EXCEPT scenario
01 ("the how-to guides section links the page"), which is EXPECTED red at
this stage per the ticket's own `notes:`/`stage_skip_reasons` (the BL-643
precedent, e1c39eef31): "the coder writes the page and its step handler in
one commit, the documenter owns the final wording and the links." The
documenter's pass adds the `docs/index.md` link (and the two cross-links
into the class document and the tutorial) and scenario 01 turns green
then — not before.

## Manual verification

`ONBOARDER_LAUNCH_DRYRUN=1 swarmforge/scripts/launch_onboarder.sh .`:

```
DRYRUN launch_onboarder swarm-repo=.
DRYRUN supervisor cmd: bb /home/carillon/swarmforgevc/.worktrees/coder/swarmforge/scripts/onboarder_supervisor.bb .
DRYRUN reconcile cmd: node ./extension/out/tools/onboarder-reconcile.js . poll-loop
```

matches exactly what the page's section 2 describes.
