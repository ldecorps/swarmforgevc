# BL-1738 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

A large but well-scoped mechanical fix: one shared fixture-root helper,
fourteen handlers converted to use it, plus BL-325's own reference bug
fixed (named in-scope). No feature file's Scenario/Examples/narrative text
touched anywhere (`git diff main...HEAD --name-only -- specs/features`:
empty — BL-1738's own feature was already on `main` from the mint).

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1738-every-operator-runtime-acceptance-fixture-roots-itself-in-a-git-checkout.feature`:
  15/15 ok.
- Spot-ran a representative sample of the fourteen (not all fifteen, to
  keep this pass's own lane small per the human directive on changed-path
  test scope — BL-877 alone runs ~300s):
  - `BL-325-human-in-the-loop-closed.feature`: 5/7 — the exact 2
    pre-existing failures the coder's evidence predicted (dead
    `require('./lib/socketFixtureRoot')` inside `OPERATOR_DECIDE_STUB`'s
    own string content, unrelated to git-checkout-ness), confirmed by
    reading the failure output myself: `expected the gated role's approval
    relay invoked, got []`.
  - `BL-307-swarm-auto-hibernate-on-drain.feature`: 7/7 (the "aps-operator-"
    trio's own duplicated-builder shape).
  - `BL-413-stale-sandbox-sweep.feature`: 6/6 (a surgical single-field
    edit in a multi-root file).
  - `BL-877-portable-process-liveness-without-proc.feature`: 6/7 — the one
    pre-existing, unrelated failure (scenario 7, a liveness-facility gap)
    the coder's evidence predicted, confirmed directly.
  - `BL-1224-watch-adopts-a-deliberately-restarted-operator-runtime.feature`
    (one of the eight listed green/untouched): still 7/7.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4 (the standing
  BL-1636 census guard, which required the coder's census-file edit).
- Scope check (qa_e2e step 4): `git diff main...HEAD --name-only`, filtered
  to non-backlog/spec-sync noise from this session's continuous main
  merges, resolves to exactly: the 14 named handlers + `humanInTheLoop
  ClosedSteps.js` (BL-325's named exception) + the new helper + the new
  pinned handler + the census file + evidence. Nothing else.

Correctness read: `humanInTheLoopClosedSteps.js`'s three fixture builders
were checked individually — `mkRuntimeFixture` (the one actually spawning
`operator_runtime.bb`) is the only one converted; `mkOperatorDecideCliFixture`
and `mkMultiRoleOperatorDecideCliFixture` already `git init` by hand and
were correctly left alone (a different CLI, already compliant). Confirms
the coder's "touch only the root(s) actually passed to operator_runtime.bb"
discipline held on this file too, not just the ones its own evidence
walked through in detail.

Dependency-gate note: `node out/tools/dependency-gate.js` targets only
`extension/`'s own module graph (webview/host/policy layering); it has no
rule surface for `specs/pipeline/steps/*.js` (plain Node acceptance step
handlers, outside the extension's architecture boundary) — confirmed by
reading `.dependency-cruiser.cjs` (no `specs`/`pipeline` mention at all).
Not a gate that applies to this parcel's changed files; not skipped, out
of scope by design.

Invariants: the one declared invariant (fixture root's git-common-dir
resolves inside it) is covered by scenario 01, driving the real helper and
the real `operator_runtime.bb` CLI — a live accept/refuse check, not a
restatement. `proveFixtureIsolated`'s `path.resolve(root,
commonDir).startsWith(root)` check (same shape reviewed for BL-1728's
bl906 handler earlier today) is safe here for the same reason: `git init`
always targets the mkdtemp'd root directly in every converted call site,
so the "nested inside a live enclosing checkout" hazard the stricter
shell-side proof (`--path-format=absolute` + separator boundary) guards
against cannot arise on this call path.

By architect.
