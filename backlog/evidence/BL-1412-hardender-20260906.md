# BL-1412 — hardender review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Ran the full checklist: BL-1412's acceptance feature (11/11 scenario
runs), BL-592's feature (8/8, no regression), the full unit suite
(610/610 files, 10296/10296 tests, with coverage), the coder's property
test (4/4), and all 18 whole-tree standing guards (183 tests).

CRAP: `filterSpecTree` shipped at complexity 8 (CRAP 8.00), over the <= 6
threshold - real code this ticket adds, not grandfathered debt. Fixed by
extracting the per-milestone epic-filtering loop into its own function,
`filterMilestoneEpics` (the same split-for-independent-CRAP pattern
already used elsewhere in this file), dropping `filterSpecTree` to
complexity 4 (CRAP 4.00) and the extraction itself to complexity 4 (CRAP
4.00). Confirmed load-bearing, not merely permitted, by hand-mutating the
`||` in the label-match condition to `&&`: 2/11 acceptance runs fail
(epic-title-only and ticket-id-only label matches), proving the
milestone/epic OR is independently exercised. Mutant reverted before
committing (b4861f1c91).

Mutation (Stryker has no wired coverage for this file at all until this
pass - a scoped run via `vitest.bl1412.stryker.config.mjs`, reverted
before this commit per the standing-red rule against editing the shared
`vitest.config.mjs`, avoiding the known Stryker-sandbox-only red in
`activePoolFreshnessAudit.test.js`): found 3 real survivors inside
`filterSpecTree` itself - `query.trim()` and the whole `if (!trimmed)
return tree` early-return guard, both deletable with every existing test
green. Root cause: `filterDocsTree` (reused verbatim) already trims
internally and passes a blank query straight through, and every
milestone/epic `buildDocsTree` can produce has >= 1 member by
construction, so the rebuild happens to reconstruct the identical tree
regardless - true only for trees the normal builder produces. Closed
with a test on a HAND-BUILT tree carrying an anomalous milestone (zero
epics), the shape that distinguishes "return input unchanged" from "run
the rebuild anyway" (which drops an empty-epics milestone). Confirmed
the new test fails against the exact Stryker mutant and passes against
the real code; the mutant was reverted before committing (89eeb39676).
Re-ran Stryker after: the three survivors killed (222->225), remaining
survivors confirmed in unrelated pre-existing code (VISION_DOCS,
DOCS_TREE_LOCALE, readVisionDocs - none touched by this parcel).

By hardender.
