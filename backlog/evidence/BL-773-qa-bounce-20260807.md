# BL-773-coordinator-asks-through-the-role-ask-path — QA bounce evidence (2026-08-07)

## Commit checked

`9a8b7578` (QA worktree merge of documenter's `ec25074cf6`, which merges
hardener lineage `00760bdb` -> `518f73c1` -> cleaner `58c42419` -> coder
`f3613d20` -> `d30ee726`).

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`): clean.
- Full unit suite (`npm test`): 400/406 files green. 5 unrelated files
  (`bounceWatcher.test.js`, `dependencyGateCliReportsAndScope.test.js`,
  `dependencyGateCliStorageGlobals.test.js`, `renderBriefingDiagramsCli.test.js`,
  `startBridgeHeadlessCli.test.js`) failed only with generic 20s subprocess
  timeouts, none of them touched by this commit (confirmed via
  `git show ec25074cf6 --stat` — none appear); re-run in isolation, still
  intermittently timed out. Host `uptime` showed load averages of 114-166 on
  a 4-core machine throughout (30-40x oversubscription) — the same
  severe-load timeout signature the hardener's own pass on this same ticket
  independently recorded ("Host load was extreme throughout ... 99-176 on 4
  cores"). Treated as environmental, not a functional regression; not part of
  this inventory.
- Property tests (`npm run test:properties`), scoped to this ticket's own
  file: `test/bl773RoleAskPerRoleGuard.property.test.js` — 2/2 PASS.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-773-coordinator-asks-through-the-role-ask-path.feature`):
  6/6 scenarios PASS.
- `required_wiring` (`swarmforge/roles/coordinator.prompt::role_ask.bb`):
  confirmed present (`grep -n role_ask swarmforge/roles/coordinator.prompt`
  -> lines 171, 187).
- Invariants: both declared invariants have real coverage — invariant 2 (the
  per-role guard) via the property test above; invariant 1 (single answer
  channel) via acceptance scenario `BL-773-coordinator-role-ask-03`, per the
  property file's own header note explaining why it isn't separately
  property-tested.

## D1 — new test file uses a raw `mkdtemp` call site, breaking the shared migration guard

- **Class:** unit.
- **Blamed role:** coder (`extension/test/bl773RoleAskPerRoleGuard.property.test.js`
  authored "By coder." in `d30ee726`).
- **Failing command:** `npm test` (unit suite) ->
  `test/tmpDirMigrationGuard.test.js` > "the real extension/test/ tree has
  zero raw mkdtemp call sites outside the shared helper" — FAILS.
- **First error excerpt:**
  ```
  AssertionError [ERR_ASSERTION]: expected zero raw mkdtemp call sites, found:
  [ { file: '.../test/bl773RoleAskPerRoleGuard.property.test.js', line: 39 } ]
  ```
- **Site:** `extension/test/bl773RoleAskPerRoleGuard.property.test.js:38-40`:
  ```js
  function mkTmp() {
    return fs.mkdtempSync(path.join(os.tmpdir(), 'sfvc-bl773-guard-'));
  }
  ```
- **Expected vs observed:** every test file's temp-dir creation must go
  through the shared `mkTmpDir` helper (`extension/test/helpers/tmpDir.js`,
  BL-420/BL-627 migration convention, enforced by
  `test/tmpDirMigrationGuard.test.js`); this file calls `fs.mkdtempSync`
  directly instead.
- **Remediation pointer:** replace the local `mkTmp()` helper in
  `bl773RoleAskPerRoleGuard.property.test.js` with
  `const { mkTmpDir } = require('./helpers/tmpDir');` and call
  `mkTmpDir('sfvc-bl773-guard-')` — matches the pattern already used by
  every other migrated test file (e.g. `leanLedgerInvariants.property.test.js`
  in this same batch, which has no raw mkdtemp call site). This also gets the
  file BL-420's automatic per-file tmp-dir sweep in `afterEach`, which the
  raw call site currently opts out of.
- **Note:** this is a genuinely reproducible, deterministic failure — verified
  independently in isolation (`npx vitest run test/tmpDirMigrationGuard.test.js`),
  not a host-load flake.

## Routing

Single item, blamed on coder (the earliest and only role in this ticket's
inventory). Bounced alongside BL-819's separate inventory (same batch
commit, same target commit); see `backlog/evidence/BL-819-qa-bounce-20260807.md`.
