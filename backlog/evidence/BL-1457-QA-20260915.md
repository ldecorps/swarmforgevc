# BL-1457 — QA review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Merged documenter's efc203138f into QA (merge commit, this evidence
committed on top). Diff vs prior QA-approved tip (cce3f1802a) touches only
test files, docs, the step handler, and evidence — no production line of
`swarmforge.sh` or `pack_staffing_gate_lib.bb` changed.

Ran the ticket's own `qa_e2e_procedure` in full:
1. `env -u PACK_STAFFING_SKIP_GATE npx vitest run --config
   vitest.properties.config.mjs test/bl1218RemoteControlConfigInvariants.property.test.js
   test/bl1320DocumentedStepsAreExecutedInvariants.property.test.js` — 6/6 green.
2. Same with `PACK_STAFFING_SKIP_GATE=1` exported — 6/6 green.
3. `env -i PATH="$PATH" HOME="$(mktemp -d)" npx vitest run ...` (offline-expedite
   shape) — 6/6 green.
4. `grep -n PACK_STAFFING extension/vitest*.config.mjs` — no output (no
   lane-wide export).
5. The other five parse_config-driving files
   (bl1010SwarmNameResolution.test.js, bl1078CursorSeatUsesSharedChannels,
   bl1324ClaudeSeatQwenCloudContextWindowInvariants,
   bl1328QwenModelTokenFormsInvariants, swarmforgeShErrorChannelGuard) all
   green with the variable unset.
6. `backlog/standing-reds.tsv` has no row for either file;
   `standing_red_register_cli.bb` reports them absent.
7. `env -u PACK_STAFFING_SKIP_GATE bash swarmforge/scripts/test/test_pack_staffing_gate.sh`
   — ALL CHECKS PASSED, unchanged behavior.

Also ran, straggler-checked before and after (none found):
- Full unit suite (`npm test`, extension/): 616 files, 10479 tests, all
  green. Pre-existing suite/file budget warnings (bl1277, bl968,
  briefingDigestLineCli, renderBriefingBurndownCli,
  renderBriefingDiagramsCli, telegramFrontDeskBotCli) unrelated to this
  ticket and not a QA-relevant regression.
- Full property suite (`npm run test:properties`): 405 files, 1194 tests,
  all green; two `[vitest-worker]: Timeout calling "onTaskUpdate"` errors
  are the BL-871 allowlisted benign noise, not a failure.
- Acceptance run:
  `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1457-every-test-that-drives-parse-config-decides-the-staffing-gate-itself.feature`
  — 11/11 scenarios pass.
- `pre_qa_gate.sh` (required_wiring: the step handler landed in the same
  parcel as the feature) — OK.

qa-sibling-check.js status for BL-1457: `VERIFY BL-1457` (no open
deferral). No standing-red register hit for anything this parcel's
evidence names.

Verdict: APPROVE. Landing on `main`.
