# BL-428 — QA pass (acceptance retired, re-verify) — 20260827

## Inbound

Specifier `note` `000787` (priority 00): "BL-428 acceptance parked as
.feature.draft 0e203a662 - merge main, re-run", resolving the spec-gap held
in `backlog/evidence/BL-428-qa-spec-gap-20260827.md` (2026-08-27, commit
`f58507d39`).

Merged `0e203a662` (`spec(BL-428): park the paneHistory acceptance as a
permanently non-executable draft`) into this worktree at `ab12d5088`.

## Disposition confirmed

- `acceptance:` field in `backlog/active/BL-428-decrap-preexisting-high-crap-on-touch.yaml`
  now points at `specs/features/BL-428-decrap-paneHistory-slice.feature.draft`
  — no `*.feature` glob in `specs/features/` matches BL-428 any more, so
  `run_acceptance.sh`/the pointer gate no longer tries to execute it.
- Specifier's rationale (recorded in the ticket YAML and the draft's own
  header): a decrap slice is behavior-preserving by definition
  ("pure refactor — no acceptance scenario changed" per the ticket's own
  `qa_e2e`), so no honest step handler could ever exist for it — retired,
  not rewritten, per Article 4.4's spec-gap disposition table.
- `qa_e2e:` remains the operative contract, already satisfied in the prior
  pass (CRAP regenerated, targeted functions ≤6, existing tests green) — no
  coder rebuild needed, confirmed unchanged below.

## Re-verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling | `qa-sibling-check.js status --ticket BL-428` | `VERIFY BL-428` |
| Ancestry | coder `742f2d290`, cleaner `279bf406e`, architect `a2759319f`, hardener `28a973dfa` all ancestors of `ab12d5088` | CONFIRMED |
| Mapped unit (BL-1164) | `vitest run test/paneHistory.test.js test/footerDetectionParity.test.js test/footerAwareScroll.test.js` | 43/43 PASS (re-run; unchanged from prior pass — nothing the coder built changed) |
| CRAP (scoped) | carried over from prior pass — all targeted functions ≤6 (`detectFooterLineCount`=3.00); no source changed since | CONFIRMED still applicable |
| Acceptance | retired — draft is not reachable by any `*.feature` glob; `qa_e2e` is the operative contract per specifier's disposition | N/A by design, not a gap |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.** Landing `ab12d5088` on `main`; merge-up broadcast to pipeline
worktree roles; coordinator notified for bookkeeping (BL-428 →
`backlog/done/`).

By QA.
