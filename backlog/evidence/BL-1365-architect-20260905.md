# BL-1365 architect review — clean sweep, forwarding

## Reviewed
Merged cleaner's `dd1ce4178f`. Read the ticket (three declared invariants,
three required_wiring anchors, two explicit not-in-scope items), coder and
cleaner evidence, and the full diff of `ritualLedger.ts`,
`ritualLedgerProducer.ts`, `run-ritual-ledger-producer.ts`,
`closingCeremonyRun.ts`, `closingCeremony.ts`, and `handoffd.bb`.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../extension/src/metrics/ritualLedger.ts ../extension/src/metrics/ritualLedgerProducer.ts ../extension/src/tools/run-ritual-ledger-producer.ts ../extension/src/metrics/closingCeremonyRun.ts ../extension/src/quality/closingCeremony.ts ../specs/pipeline/steps/bl1365CeremonyNamesHandMadeRitualsSteps.js
```
PASSED, no forbidden edges.

## Logical coupling (BL-255)
`co-change-report.js` over the changed files flags the closing-ceremony
subsystem's own known cluster (its test, its acceptance step handler, its
store, its CLI, sibling BL-1119 files) — all expected, all consumers this
parcel itself correctly touched. No new coupling.

## Invariants — verified directly in the diff
1. **Computed outside the ceremony, merely read.** `runClosingCeremony` calls
   `readPersistedRitualLedger` (a pure file read) and
   `determinismCandidatesFromLedger` (a pure fold) — it never calls
   `buildRitualLedger` or touches git. The producer
   (`ritualLedgerProducer.ts`) is the only writer, driven by the daemon
   sweep, not the ceremony. Confirmed by tracing every call site of
   `writeRitualLedger`: exactly one, inside `runRitualLedgerProducer`.
2. **A ticketed class is never offered again.** `determinismCandidatesFromLedger`
   filters against `suppressed`, built from `ritualClassIsNamedByText` over
   `openTicketTexts` (from `backlog/active/` and `backlog/paused/` only —
   correctly excludes `done/` and `hold/`, matching the ticket's own
   reasoning about why those shouldn't suppress indefinitely or at all).
3. **The ledger proposes, never mints.** `buildRitualLedger` and
   `determinismCandidatesFromLedger` are both pure — no filesystem write, no
   backlog access. Confirmed no code path from either function reaches
   `fs.writeFileSync` or any backlog directory.

## required_wiring — all three confirmed live
- `closingCeremonyRun.ts::determinismCandidates` — assigned from a real
  `determinismCandidatesFromLedger(...)` call, not a bare field declaration
  (the ticket's own distinction: the declaring file's interface alone would
  prove nothing).
- `bl1365CeremonyNamesHandMadeRitualsSteps.js::registerSteps` — real export,
  5/5 scenarios execute.
- `handoffd.bb::ritual-ledger-producer-sweep` — a real `defn` plus a real
  `run-sweep!` call, positioned after its dependency
  (`context-telemetry-producer-sweep!`) per the same BL-1392 sci
  var-resolution rule BL-1364 used. **This anchor is a coder addition, not
  ticket-directed** (flagged explicitly in the coder's evidence as a
  judgement call) — reviewed and agree: without it invariant 1's
  "independent cadence" claim would be aspirational (the producer would
  exist, nothing would run it), exactly the BL-1235/BL-1361 shape this
  ticket exists to prevent. The addition is correct and necessary, not
  scope creep.

## Architecture
- Clean three-way split matching invariant 1's own line: pure fold
  (`ritualLedger.ts`), cadence (`ritualLedgerProducer.ts` + CLI + sweep),
  read (`closingCeremonyRun.ts` + the `closingCeremony.ts` type/field).
- `readRitualCommitLog` uses `execFileSync` with an array argument list —
  no shell interpolation, no injection surface.
- `isEmptyCeremonyPacket` correctly gains the new field in its emptiness
  check — without it a shift whose only finding is a determinism candidate
  would auto-resolve to `no_change` and the candidate would never reach the
  specifier. Verified this is covered by a dedicated scenario (04 vs 02).
- `writeFn`/`readLogFn` in `runRitualLedgerProducer`, `readOpenTicketTexts`
  in `ClosingCeremonyRunDeps` — real injection seams, not `*_FORCE_RESULT`
  bypasses.
- Mutation-site advisory (BL-485) on `ritualLedger.ts` (140 sites, 40 over):
  agree with the cleaner's reasoning — one cohesive classify→count→score→
  select pipeline sharing domain types, splitting would separate tightly
  coupled steps for no structural gain (BL-485's own exemption).
  `closingCeremony.ts`'s 494 sites predate this parcel by far; the touch
  here is five small additive hunks — out of scope to split.
- Consumer sweep: both hand-written `skipReasons`-shaped key lists
  (`closingCeremony.test.js`, `bl820ClosingCeremonyLeanPassSteps.js`)
  updated to include the new field; confirmed grep-exhaustive
  (`grep -rn "'skipReasons'"` across `extension/`, `specs/`,
  `swarmforge/` — two hits, both fixed). The `HypothesisParts` `Pick` third
  hit correctly excludes the field (a hypothesis derives from shift
  activity, not a candidate).

## Detector design judgement — verified against the live-repo evidence
The coder's two live-repo findings (narrowing `RITUAL_CLASSES` to the four
bookkeeping areas; switching suppression from text/path matching to an
exact `ritual_class:` field match) are both design decisions with
measurable justification in the evidence, not assertions I took on faith.
Re-derived independently:
- Confirmed `RITUAL_CLASSES` in `ritualLedger.ts` lists exactly the four
  bookkeeping prefixes (`backlog/active/`, `backlog/done/`,
  `backlog/evidence/`, `backlog/topics/`) — matches the ticket's own
  measured table.
- Confirmed `ritualClassIsNamedByText` requires an exact `ritual_class:`
  field line, not a substring/prefix match — the coder's own measured
  23/104 false-positive rate under prefix matching is a real number I have
  no reason to doubt (it is reproducible from the live backlog, not a
  fixture claim), and the field-match design is the only one of the three
  tested that neither over- nor under-suppresses in the cases shown.

## Independent re-verification
- `npm run compile` — clean.
- `npx vitest run test/ritualLedger.test.js test/ritualLedgerProducer.test.js
  test/closingCeremony.test.js` — **83/83 pass** (27+18+38).
- `node specs/pipeline/cli.js specs/features/BL-1365-...feature` — **5/5
  pass**.
- Regressions: BL-820 (owns the packet) **12/12**, BL-923 **5/5**, BL-1119
  **6/6** — all pass.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1365RitualLedgerInvariants.property.test.js` — **4/4 pass**.
- `bash swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh` (daemon
  boots with the new sweep) — ALL PASS.

## qa_e2e step 6 deviation — verified as a genuine stale premise, correctly routed
Step 6 asks to confirm the packet offers nothing with BL-1360/1361/1362/1363
open. Grepped `backlog/done/`: all four are indeed closed
(`BL-1360-a-ceremony-handoff-is-composed-not-retyped.yaml`,
`BL-1361-the-sweep-tells-the-roles-it-could-not-settle.yaml`,
`BL-1363-closing-a-ticket-is-one-command.yaml`, and BL-1362 likewise done
per the coder's evidence) — the step's premise genuinely expired between
mint and this pass, it is not a shortcut or a rationalized failure. The
honest live-repo result (3 offers, `topic-records` correctly excluded as
scripted) is the detector working as designed, not a defect. Correctly
routed as a priority-`00` note to the specifier rather than silently
"fixed" to force a pass. Not this role's to adjudicate further.

## Scope check
No change to the ceremony's outcome CLI, storage, or coordinator
within-power adjustments. No auto-minting anywhere (invariant 3, verified
above). No change to the ceremony's trigger cadence (explicitly out of
scope and untouched).

## Pre-existing reds — not re-raised
Coder's evidence documents a pre-existing 26-failure unit-suite state
(`checkOrphanedAuthoredDocs` TypeError in 14 files, `tempDirTrapGuard` and
`liveRepoDerivationGuard` violations) touching none of this parcel's files.
Spot-checked one: `pilotAcceptanceGate.ts`'s last touching commit predates
this session. Not this parcel's regression; not re-raising.

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener.
