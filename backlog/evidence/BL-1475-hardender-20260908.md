# BL-1475 — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Verified no orphaned test/mutation processes before starting
  (`pgrep -fl 'node --test|stryker'`); host load quiet (11-14 on 20 cores).
- Compiled clean; full unit suite green (612 files, 10373 tests).
- `mutation_cooldown_gate.bb` for `extension/src/util/commitIntegrityRunner.ts`:
  DECISION run (file age 3.02d past 3d cooldown, load quiet).
- Extracted `commitIntegrityFailureFromError` out of `runCommitIntegrityDetailed`
  (CRAP isolation) and added 6 new unit tests (4 in
  `gitCommitScopedFile.test.js`, 2 in `blTopicStore.test.js`) closing gaps in
  the exact backoff sequence, the first-try-success no-reverify path, real
  stderr surfacing through both writers' custom reporters, real retry-on-
  transient-lock behavior, and whitespace-only porcelain output.
- Scoped Stryker run (`vitest.bl1475.stryker.config.mjs`, temporary
  `stryker.config.json` pointer, reverted after) on `out/util/commitIntegrityRunner.js`:
  87.69% / 91.94% covered, 57 killed, 5 survived + 3 no-coverage, run twice
  with identical results. All 8 are the same guard
  (`err && typeof err === 'object' && 'stdout' in err` and its `?? ''`
  fallback) - documented equivalent in-source (BL-1081 !x/typeof-guard
  class): empirically verified execFileAsync's only caller here always
  rejects with an object carrying a defined string `stdout` across ENOENT,
  non-zero exit, timeout, and signal-kill cases, so the guards' false
  branches are unreachable through this call site. Same reasoning as the
  file's pre-existing `?? '{}'` fallback in `parseCommitIntegrityResult`.
- CRAP (`crapReport.js`, `src/*.ts` paths per the scoping rule) on every
  touched function: all <= 6 (`commitIntegrityFailureFromError`=6.00,
  `defaultAttemptCommit`=6.00, `commitScopedFile`=6.00, rest lower).
- jscpd on the three touched source files: 0 clones.
- All 18 whole-tree guard files (`test/*Guard*.test.js`, non-property):
  183 tests green.
- BL-1475 acceptance: `run_acceptance.sh` on the feature file, 6/6 scenarios
  green.
- BL-1475 property test (`bl1475SharedCheckoutLockRetryInvariants.property.test.js`,
  via `vitest.properties.config.mjs`): both invariants green.
- Pre-commit hook's full `test:properties` run: 2 reds
  (`bl1253TokenOwnershipInvariants`, `bl1309LandDecideEntanglementInvariants`),
  both cleared by the property-suite-guard's own re-run-alone check
  (BL-1407) as load flakes, unrelated to this parcel's files.
- No lessons rule-proposal this pass (no non-obvious tooling trap or
  misjudged survivor class beyond the already-documented BL-1081 class,
  applied here rather than newly discovered).

By hardener.
