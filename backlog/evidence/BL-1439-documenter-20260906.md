# BL-1439 documenter review pass — 2026-09-06

Reviewed the hardened commit (`e235878dcc`) and the coder/cleaner/architect/
hardener trail for BL-1439 (the hardening-debt ledger gains `--discharge`
and `--attempt`; one of five 2026-08-19 deferrals discharged, four
recorded as attempts and re-pointed to BL-1441).

Checklist:
- `required_wiring` anchors present, confirmed by grep:
  `hardening_debt_ledger_update.bb::--discharge`,
  `hardening_debt_ledger_lib.bb::discharged`,
  `hardening_debt_ledger_update.bb::--attempt`,
  `specs/pipeline/steps/bl1439DeferredHardeningGatesDischargedSteps.js::registerSteps`.
- The ticket's own description explicitly directs: "The documenter
  updates BL-942's how-to with the discharge verb and the rule that a run
  discharges what a deferral recorded." Updated
  `docs/how-to/BL-942-hardening-debt-ledger.md` with a new "Discharging a
  deferral (BL-1439)" section covering both verbs — documented the
  ACTUAL landed CLI shape (`--discharge <parcel> <gate> --evidence <path>
  [<date>]`, `--attempt <parcel> <gate> <blocker> [<date>]`), confirmed
  by reading `hardening_debt_ledger_update.bb` directly, not the ticket's
  own earlier draft usage line for `--attempt` (which included an
  `--evidence`/`--blocker` flag pair the coder's amendment pass replaced
  with positional args — the amendment note in
  `backlog/evidence/BL-1439-coder-20260906-amendment.md` confirms this
  was a deliberate landed shape, not a coder shortcut). Added a "See
  also" entry naming BL-1441 (the follow-up owning the four still-blocked
  runs) and cross-linking BL-1428's register doc. This how-to has no
  "Last Updated" field, so none was added.
- Added a dated top entry to `docs/reference/Specification.MD`,
  cross-linking to the BL-942 how-to.
- No `docs/index.md` change needed — the BL-942 how-to is already linked
  there; no new page was authored.
- No registered diagram trigger fires — this extends a ledger-file verb
  set and a standing-red register row assignment; it changes neither the
  extension host/webview boundary, the tmux substrate, the `.swarmforge/`
  state layout, the pipeline topology/backlog flow, the handoff file
  lifecycle, nor front-desk routing.
- No production code, test, or requirements change made by this pass —
  documentation only.
- `docs/how-to/BL-942-hardening-debt-ledger.md` is named for a different
  (already-shipped) ticket, so its edit lands in its own UNTAGGED commit
  per the cross-ticket-doc-edit convention (BL-1192 task-scope gate); the
  Specification.MD entry and this evidence file, both this ticket's own
  paths, land in a separate tagged commit.

Result: NONE — no defect found; the BL-942 how-to gained a section for
this ticket's two new verbs, one changelog entry added.
