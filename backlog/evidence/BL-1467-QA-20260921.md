# BL-1467 — QA review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Second land-tooling ticket in a row (after BL-1678); fixes
`post-land-repoint!` so a re-point re-applies bookkeeping commits for
other tickets instead of silently dropping them - exactly the gap that
left my own earlier `abandoned_commits:` annotations for BL-1640/BL-1641/
BL-1658 stranded on `swarmforge-QA` and never separately landed this
session.

- `node specs/pipeline/cli.js specs/features/BL-1467-the-re-point-keeps-qa-bookkeeping-and-names-what-it-drops.feature`:
  6/6 ok.
- `npx vitest run --config vitest.properties.config.mjs test/bl1467RepointKeepsBookkeepingInvariants.property.test.js`:
  2/2 pass.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- Regression check: `specs/features/BL-1438-the-publish-re-points-the-qa-branch-after-a-land.feature`
  (the re-point mechanism's own prior contract): 4/4 ok, unchanged.
- Wiring: `bl1467RepointKeepsBookkeepingSteps.js` exports `registerSteps`,
  confirmed present.
- `npm test` (extension/, after `npm run compile`): 634/635 files,
  10847/10848 tests passed - the one failure is the already-registered
  BL-1658 cascade (`backlog/standing-reds.tsv`, `bl1146...` over budget),
  not this parcel's defect.
- `npm run test:properties` (extension/): 439/440 files, 1284/1285 tests
  passed; one unrelated sighting
  (`test/bl956PipelineBoardCaptionCapInvariants.property.test.js`, not in
  this parcel's diff, clean on a standalone re-run) reported separately
  as an unowned-red note to the specifier
  (`backlog/evidence/unowned-red-bl956-caption-cap-flake-QA-20260921.md`),
  per the 2026-09-20 closing-ceremony rule (one lane run, no re-run loop).
- `pre_qa_gate.sh BL-1467 d7e53d1d1a`: OK.
- No standing-red register/allowlist row names BL-1467.
- No straggler test/mutation processes before or after.
