# QA pass — BL-1289 — 2026-09-05

Commit under review: `0f3bda581d` (architect's NONE pass, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`2d84266eaa` BL-1289
coder, confirmed ancestor via `git merge-base --is-ancestor`). Routed
coder→cleaner→architect→QA per `stage_skip_reasons` (hardener/documenter
skipped: no TS production change, no mutation/CRAP/DRY tooling for
Babashka/shell (BL-472), no living doc affected).

## Verification

- `npx vitest run test/tempDirTrapGuard.test.js`: 4/4 pass (was the
  standing red naming 21+ violations — coder's own pickup count was 27,
  drifted since the ticket's 2026-08-30 spec-time snapshot; correctly
  treated the guard's zero-violation bar as the acceptance criterion, not
  the stale list).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1289-a-temp-root-is-always-cleaned-up.feature` — 4/4
  scenarios pass, including scenario 4 ("a leftover temp root from a
  killed run does not survive the next run") — the SIGKILL-can't-be-trapped
  half of invariant 1.
- Spot-checked 3 of the ~19 modified bb/shell runners directly:
  `bl1141_refuse_rematch_test_runner.bb`, `test_shell_test_discovery.sh`,
  `post_qa_branch_sweep_lib_test_runner.bb` — all pass.
- **Safety check** (this class of fix has a known incident, BL-1385/1390:
  a naive prefix sweep on a production guard can delete a live
  concurrently-running sibling's tree): read the
  `fixture_isolation.sh` diff directly. It adds `register_tmp_dir "$WORK"`
  (an EXIT-trap cleanup, BL-1289's own contribution) **on top of, never
  replacing**, the pre-existing owner-liveness sweep
  (`fixture_isolation_reap`, BL-1385/1390/1392) that remains the only
  defense against a killed run. Confirmed no prefix-sweep-before-assert
  logic was newly introduced anywhere in this diff — the coder's own
  commit message states this explicitly and the code matches.
- Re-ran BL-1421's own regression suite (one of the two "design call"
  files singled out for a hand-off-aware hook, since its fixture is
  deliberately reused across separate per-Gherkin-step process
  invocations): `bl1421_one_standing_surfacing_property_runner.bb` (500
  runs/property, ALL HOLD) and its acceptance feature (6/6) both still
  green — the reuse-aware hook did not break the reuse.
- `required_wiring`: `specs/pipeline/steps/bl1289TempRootAlwaysCleanedSteps.js`
  exports `registerSteps`.
- `backlog/standing-reds.tsv` row for the UNIT-lane guard removed, per the
  ticket's own scope (unit lane only). Noted for a future ticket, not a
  BL-1289 defect: the PROPERTY sibling
  (`test/tempDirTrapGuard.property.test.js`) is now also green (verified:
  9/9 pass) since the same underlying files were fixed, but its
  `property_suite_standing_allowlist.tsv`/`standing-reds.tsv` rows are
  still present — out of this ticket's stated scope, and exactly the kind
  of staleness BL-1428's new register (landed this session) exists to
  surface.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — merging up and landing.
