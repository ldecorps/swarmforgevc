# BL-1339 — architect pass (2026-09-03)

## Merge

Merged cleaner `93378587f0` (coder `26f93b3f61`) into the architect
worktree. One trivial `require(...)`-list conflict in
`specs/pipeline/steps/index.js`: the incoming branch (forked before my
BL-1345 bounce/revert on this branch) still carried
`bl1345StaleRouterMarkerStaffingSteps` — correctly dropped, since that file
no longer exists on this branch (reverted at `1193b48d22` as part of
bouncing BL-1345 back to the coder). Kept only the new
`bl1339LandApprovalSharedRootSteps` require. The merge commit also tripped
the merge-deletion guard on the BL-1345 paths my earlier revert removed;
named BL-1345 explicitly in the merge commit message per the guard's
requirement, since the deletion is deliberate and unrelated to this ticket.

## Review

- Scope: commit `26f93b3f61` touches exactly `land_step_lib.bb` (writer),
  `is_qa_ancestor.sh` (reader), and its own step/property test files. No
  scope creep.
- Human ruling (option 2) followed precisely: writer resolves
  `shared-target-root` via the pre-existing `git-common-dir` resolver
  (already used by `replay!` twelve lines below — no new resolver invented);
  reader (`is_qa_ancestor.sh`) resolves the land-approval store the same way,
  with a fallback to the old relative path only when git cannot answer (the
  pre-existing non-repo-caller behavior, preserved). The bounce stores
  deliberately stay on the caller's directory — option 3 correctly left out
  of scope.
- No second approval predicate (BL-925 invariant 2): one definition, moved
  read location, not duplicated — confirmed by reading both diffs side by
  side.
- Fail-closed on an unresolvable root (invariant 3, BL-925 invariant 3):
  `shared-target-root` returns nil rather than falling back to the caller's
  directory; `record-land-approval!` then reports `{:ok? false :reason ...}`
  and the land still succeeds — confirmed in the diff and independently
  re-verified via the property test and acceptance scenario 7.
- Dependency-rule gate (BL-259), scoped and full-repo: **PASSED, no
  forbidden edges** both times.
- Co-change report (BL-255): all reported co-changes at frequency 1 — no
  suspected coupling.

## Invariants review (BL-633/654)

All three declared invariants have property tests in
`bl1339LandApprovalRootInvariants.property.test.js`. Reach floors are
enumerated by the enclosing `for` loops over the checkout axis (main vs.
worktree) — the actual axis of the defect — not left to a random draw, the
same lesson BL-1345's D1 bounce (this session, same coder) exists to teach.
Re-ran independently **5 consecutive times, 3/3 tests each, no flake** —
unlike BL-1345, this reach design does not share that risk.

## Independently re-run

- `npx vitest run --config vitest.properties.config.mjs
  bl1339LandApprovalRootInvariants` — 5/5 runs green.
- `run_acceptance.sh specs/features/BL-1339-…feature` — 7/7 pass.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — ALL PASS.
- `test_land_step_records_approval.sh`,
  `test_is_qa_ancestor_land_replay_store.sh`,
  `test_is_qa_ancestor_expedite_store.sh`, `test_is_qa_ancestor_yaml_store.sh`,
  `test_build_freshness_land_replay_approved.sh` — all ALL PASS /
  ALL CHECKS PASSED, confirming the three daemon consumers named in the
  ticket (handoffd's push sweep proxy via is_qa_ancestor, the babysitter's
  sweep, the deploy freshness gate) and the BL-952 bounce veto all still
  hold with the store moved.

## Correctness read

No defect. The fix directly addresses the diagnosed root cause (writer used
`--show-toplevel`, readers used `project-root`/target-root — now both
resolve through the same `git-common-dir`-based shared root), matches the
human's ruling exactly, and the constraint against inventing a second
resolver or a second approval predicate is honored.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
