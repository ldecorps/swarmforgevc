# BL-1651 — QA review pass, 2026-09-19

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

### Gates run

- `npm run compile`: clean (needed after the merge; out/ was stale, per hardener's own note).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1651-*.feature`: 5/5 scenarios green.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-871-*.feature`: 6/6 scenarios green
  (scenario 04 retired per the ticket's item 5 amendment).
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1651 <commit>`: OK (required_wiring anchor present).
- `npx vitest run test/stepHandlerTmpRootGuard.test.js test/tempDirTrapGuard.test.js`: both green (the two
  reds the coder's own note flagged as arriving via the BL-1639/BL-1645 merge-up chain, already fixed
  upstream on main and merged into this branch).
- `npm test` (extension unit lane): 632/632 files, 10802/10802 tests green.
- `npm run test:properties`: 423/426 files green on the full run (no FATAL ERROR / heap crash — this
  parcel's own fix holding); 3 files timed out at 20000ms under full-suite fork contention
  (`bl1108CursorSeatReadiness`, `bl1474ReplayCommitRefusalReasonInvariants`,
  `bl1565CoordinatorNeverReceivesGitHandoffInvariants`). None relates to this parcel's diff (heap gate +
  BL-871 feature repair); all three pass cleanly and quickly (9-13s) run together in isolation
  (`npx vitest run --config vitest.properties.config.mjs <the 3 files>` — 6/6 tests pass). Judged as
  host-load timeout, not a defect or standing red.

### Lineage / attribution

- `git merge-base --is-ancestor 25b5188bc6 d861e25886` (documenter merge) → ancestor: OK.
- `git merge-base --is-ancestor 5dd2f631bd d861e25886` (cleaner's own-pass fix) → ancestor: OK.
- Approving commit `d861e25886` ("Merge documenter 25b5188bc6 into QA."), merged this session; the
  same duplicate `inbound-window-start` conflict (forward_evidence_lib.bb) recurred a third time this
  session and was resolved the same way (kept one copy, verified the file loads).

### Prior-stage evidence reviewed

architect NONE, documenter NONE, hardener NONE (detail: recompiled stale out/, ran mutation via hand-
fallback after Stryker's dry run timed out twice under host contention, verified BL-871 scenario 04's
retirement is clean); cleaner self-bounced one test-coverage-gap defect (D1, the two new pure functions
had only property-test coverage) and fixed it in the same pass (`5dd2f631bd`) — no re-bounce needed.

### Standing-red register

`backlog/standing-reds.tsv` line 38 names BL-1651 as owner of the `acceptance` lane row for
`specs/features/BL-871-property-lane-worker-pool-cap.feature` — this parcel's own land retires it.
