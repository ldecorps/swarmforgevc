# BL-1567 — hardender review pass, 2026-09-15

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1567-the-inbound-non-forwarding-test-speaks-the-two-call-audit.feature
- **Commit hash**: 12e387e5c8
- **First error excerpt**: no step handler matched And the list names test_swarm_handoff_refuses_coordinator_git_handoff.sh (before fix); 5/5 pass after
- **Failure class**: acceptance-step-mismatch
- **Expected vs observed**: scenario 05 passes matching the specifier amended census text (thirteen entries, names the BL-1565 newcomer)
- **Blamed role**: coder (handler predates specifier amendment b18ee80df9)
- **Remediation pointer**: added the two missing step handlers to bl1567InboundNonForwardingTwoCallSteps.js

By hardender.

## Detail

- D1 was found and fixed IN THIS PASS, not bounced: the specifier's
  2026-09-15 amendment (`b18ee80df9`, landed after the coder's commit
  `d9f1cc00c2`) reworded scenario 05 to "thirteen entries" and added a
  step naming `test_swarm_handoff_refuses_coordinator_git_handoff.sh`
  (BL-1565), but the coder's step handler still only matched "the list
  has twelve entries" — a stale handler left behind by the amendment,
  the BL-233 unreachable/mismatched-step class. Fixed by adding the two
  missing step handlers in `bl1567InboundNonForwardingTwoCallSteps.js`
  (commit `12e387e5c8`).
- Re-ran full acceptance after the fix: 5/5 pass
  (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1567-the-inbound-non-forwarding-test-speaks-the-two-call-audit.feature`).
- Ran `swarmforge/scripts/test/test_swarm_handoff_inbound_non_forwarding.sh`
  3x standalone: `ALL PASS`, exit 0 every time.
- Break-then-restore (qa_e2e_procedure step 4): removed case 03's second
  `run_send_queue` call and its immediately-following refusal check —
  the mutated file failed (`rc3b: unbound variable`, exit 0 from the
  subshell but a real script error) rather than reporting `ALL PASS`;
  restored from a backup copy, re-ran clean, `git status` empty before
  continuing.
- Confirmed `git diff main -- swarmforge/scripts/swarm_handoff.bb
  swarmforge/scripts/handoff_lib.bb` is empty (FIRM constraint: no
  audit-bypass edit).
- Confirmed the queueing send's env-var shape: `SWARMFORGE_MAILBOX_ONLY=1`
  exported and `SWARMFORGE_SKIP_DAEMON` unset only in `run_send_queue`;
  the refusal cases (01/02) keep `SWARMFORGE_SKIP_DAEMON=1`, single call,
  no second invocation (FIRM constraint honored).
- Mutation: `mutation_cost: low` (ticket) — no production/Stryker-scoped
  code touched (shell test + Gherkin step handler only, BL-472's
  no-mutation-tooling class for `.bb`/`.sh`, and the step handler is a
  thin file-read/subprocess harness with no logic of its own to mutate
  beyond what break-then-restore already exercised). Degraded fallback:
  the break-then-restore hand-mutation above stands in place of a tool
  run, per this prompt's BL-567/BL-638 fallback.
- BL-113 Gherkin mutation: the feature has no `Scenario Outline:` /
  `Examples:` (five plain `Scenario:` blocks) — `inapplicable` by
  BL-638, not run.
- CRAP/DRY: N/A — no `src/*.ts` file touched.
- `backlog/standing-reds.tsv` row for this file left in place, per the
  ticket's own notes ("QA removes it in the land that turns the file
  green") — not mine to remove.
- No orphaned test/mutation processes: `pgrep -fl 'node --test|stryker'`
  clean before handoff.
