# BL-1594 — hardender review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Babashka only, no wired mutation tool (BL-472 fallback) - hand-authored
sweep over `land_step_lib.bb`/`land_step_cli.bb`:

- Reverted the fix entirely (`(not (contains? #{:landed :vacuous}
  verdict))` -> `(not= :landed verdict)`, the pre-BL-1594 shape): killed
  by `land_step_lib_test_runner.bb` (7 failures across the new BL-1594
  cases and the sibling BL-1481/02 case).
- CLI report-line mutant (always print "landed", never "reverted"):
  survived the bb unit runner (it drives the pure functions, not the CLI
  process) but killed by acceptance scenario 01 (6/7, the exact scenario
  asserting the reverted wording) - both lanes exercised, not just one.
- Confirmed `content-blocked`'s `:verdicts` metadata survives to the CLI
  report-line call site unmutated (the binding is read directly, never
  passed through a set-transforming op that would drop metadata first).
- `bl1481SharedPathContentCheckInvariants.property.test.js` (invariant 3,
  fail-closed on an unreadable read, in every shape) still green -
  confirms the ticket's own invariant 1 (never narrows a refusal on nil)
  holds through this change without needing a fresh hand mutant.
- Acceptance 7/7, bb test runner ALL PASS (clean, post-restore), standing-
  red register unaffected (5 rows, all owned, unowned:[]).
- No `extension/src` change; no CRAP/DRY surface.
