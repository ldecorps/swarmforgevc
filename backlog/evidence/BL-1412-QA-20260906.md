# BL-1412 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Land incident (third occurrence this shift, not re-escalated — BL-1446/1447 already own it)

First `--land` attempt produced `LAND_REPLAY`; the mandated hand
cross-check found the pushed tip missing 11 of this ticket's own 15
files. Fixed by hand: merged the broken push under the correct branch,
verified every file present by name AND content, pushed the merge
(`origin/main` `4bbcddbdc1` → `57b59a9555`), and recorded
`abandoned_commits: [138fda0543]` on the ticket. No new specifier note —
BL-1446 (root cause) and BL-1447 (the automated net) already exist from
BL-1424's report. Full narrative:
`backlog/evidence/BL-1424-land-replay-dropped-own-paths-incident-20260906.md`.

## Detail

- `qa-sibling-check.js status --ticket BL-1412`: `VERIFY BL-1412` (no open
  deferral).
- Orphan process check before/after: empty both times.
- Acceptance: first run of `run_acceptance.sh
  specs/features/BL-1412-a-text-filter-on-the-live-spec-tree.feature`
  failed all 11 subtests — traced to my own stale compiled `extension/out/`
  (the acceptance handler `require`s the compiled bridge, and I had not
  recompiled since the coder's `.ts` changes); `npm run compile` then
  11/11 pass. Not a code defect — confirmed by the failure signature
  itself (`querySelector` for the filter input returning null, and the
  bridge route returning the unfiltered tree) matching exactly what a
  stale pre-BL-1412 compiled bridge would produce.
- Acceptance regression: `specs/features/BL-592-spec-tree-on-live-console-with-epic-tier.feature`
  — 8/8, including "navigating the spec tree is read-only" (the
  read-only-gate-06 scenario).
- Full unit lane (`npm test`, extension/): 610/610 files, 10297/10297
  tests green (+14 over the prior parcel, matching the new
  docsTree/specTreeBridge/specTreeUiHtml test cases this ticket adds).
  Same chronic suite-duration/per-file budget overage as other recent
  parcels, unrelated.
- BL-1412's own property invariants file
  (`bl1412SpecTreeFilterInvariants.property.test.js`) run standalone: 4/4,
  covering invariant 1 (structural: inline script never matches text;
  ticket-match always agrees with `filterDocsTree`'s real output),
  invariant 2 (prune-never-regroup), and invariant 3 (GET-only wiring).
- Wiring anchor 1: `bridgeServer.ts:2086` — the `/spec-tree-state` route's
  `compute` calls `filterSpecTree(computeDocsTree(...), queryParams(url).get('q') ?? '')`.
- Wiring anchor 2: `bl1412SpecTreeTextFilterSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Invariant 1 (direct code read + qa_e2e item 4's own greps):
  `grep -c toLowerCase extension/src/bridge/specTreeUiHtml.ts` → 0 (the
  inline script never matches text itself); `git diff <merge-base> --
  extension/src/docs/docsTree.ts` shows `filterDocsTree`'s existing body
  untouched — the diff is purely additive (`labelMatches`,
  `filterMilestoneEpics`, `filterSpecTree` appended after it);
  `filterSpecTree` itself calls `filterDocsTree(tree, query)` for the
  ticket-text match, never reimplementing it.
- Invariant 2 confirmed by code read: `filterMilestoneEpics` passes
  `epic.tickets` through unfiltered (whole subtree) on a label match, or
  filters by membership in `ticketMatchedIds` otherwise — no ticket object
  is ever mutated or moved to a different milestone/epic.
- Invariant 3 confirmed: `bridgeServer.ts`'s diff touches only the
  existing `/spec-tree-state` route's `compute` fn, no new route; `grep -n
  'fetch(' extension/src/bridge/specTreeUiHtml.ts` shows exactly one call,
  no explicit method (defaults GET).
- **Live check (qa_e2e items 2-3)**: started the real `startBridge()`
  against this actual repo (not a synthetic fixture) and queried
  `/spec-tree-state` directly: full tree `schemaVersion: 2`, 1362 tickets;
  `?q=spec-tree` narrowed to 3 real matching tickets (`BL-1412`, `BL-1213`,
  `BL-592`); a deliberately-unmatchable term returned 0 tickets and 0
  milestones (the no-results shape). Confirms the real end-to-end behavior
  against live backlog data, not just the acceptance fixture.
- Hardener evidence reviewed: real Stryker mutation run found and closed
  (3 survivors on `filterSpecTree`'s early-return guard, via a hand-built
  anomalous-tree test case) plus a CRAP-driven extraction
  (`filterMilestoneEpics`), both with before/after mutant-kill
  confirmation and clean reverts.
- Docs: `docs/how-to/BL-592-spec-tree-on-live-console-with-epic-tier.md`
  gained the "Text filter (BL-1412)" section. No diagram trigger fires
  (not claimed by the ticket; confirmed no diagram touched).
- Out of scope respected: `pwa/app.js` (BL-254's hand-kept mirror)
  untouched; no write-path route added; only ticket title/description/
  scenario text and the three visible labels match — confirmed by code
  read, no status/priority/other YAML field is compared anywhere in
  `filterSpecTree`/`filterMilestoneEpics`.

## Not bounced (observed, not this ticket's defect, not reproducible — host load)

Full property run failed 4 files under elevated host load (uptime ~9-16
during the run; other worktrees' concurrent acceptance/property runs
visible in `ps aux`): `bl1297MergeOwnPathsInvariants`,
`bl1343ReplayNeverDropsOwnPathInvariants`,
`bl1389UnlandedSiblingPathNeverRidesInvariants` (all three on the fixed
20s per-test timeout), and `bl968MaterializedGuardSensitivity` (its own
300s timeout, after printing valid coverage stats). None touch BL-1412's
domain (spec-tree filter vs. land-step entanglement/replay and a
load-time-binding guard). No ticket found (`grep -rl <name>
backlog/paused/ backlog/active/` empty for all four; none in
`backlog/standing-reds.tsv`). Re-ran all four in isolation once load
eased (~9): all four passed cleanly, `bl968` in its normal ~193s with
only the BL-871 allowlisted benign error. Same disposition as this
shift's two prior "Not bounced" sections (BL-1442, BL-1424).
