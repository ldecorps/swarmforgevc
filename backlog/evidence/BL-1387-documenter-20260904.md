# BL-1387 — documenter pass (2026-09-04)

## Received
`96cb0d0a3d` (hardener, degraded Babashka fallback recorded — no
mutation/CRAP/DRY wired for `.bb`, gated on the lib's own runners per
`backlog/evidence/BL-1387-hardener-pass-20260904.md`).

## Merge
Two conflicts resolving hardener's send against my own tree (which still
carried an earlier duplicate scenario block from a prior BL-1385 merge):
`specs/features/BL-1385-....feature` (adopted hardener's scenario
ordering, dropped my duplicate) and
`specs/pipeline/steps/bl1387OrphanedMergeSurfacedSteps.js`
(whitespace-only).

## What changed, operator-visible
Companion detector to BL-1386 (which I documented earlier this session):
an open `MERGE_HEAD` the daemon does not own used to be surfaced as
`human-merge-in-progress` on the strength of absence alone (no BL-1386
ownership record). This ticket adds a positive-evidence classifier — live
git process or fresh index lock required for the human reading — and a
new `orphaned-merge` reason for the remaining case, escalating
immediately rather than holding silently. The coordinator's step-0 action
table for this reason is the specifier's own prompt-file edit (BL-798,
already landed on `main`) — not a documenter doc, but the operator-facing
CONCEPT (both classification outcomes and what to do about `orphaned-merge`)
belongs in the same two how-to pages BL-1386 already extended.

## Doc-domain review
- `docs/how-to/BL-891-master-main-reconcile-sweep.md`: step 5's bullet and
  the verdict table both updated — the "human merge in progress" row now
  states the positive-evidence requirement, and a new "orphaned merge"
  row describes the escalate-at-once behavior and the manual remedy.
- `docs/how-to/BL-1120-handoffd-must-not-abort-foreign-merge.md`: the
  BL-1386 section's closing sentence ("a MERGE_HEAD with no record... is
  still surfaced as human-merge-in-progress exactly as before") was
  correct when written, now stale — corrected, and a new "Foreign but not
  always human (BL-1387)" section added.
- `docs/reference/Specification.MD`: new top-of-stack entry, including
  the specifier's in-flight amendment (scenario 02's owned-row retirement)
  since it's part of what actually shipped.
- `docs/index.md`: refreshed both BL-891 and BL-1120 summary lines.
- No new dedicated how-to page — same reasoning as BL-1386: this ticket's
  natural home is the two pages it directly extends.
- Diagram registry checked — no diagram depicts reconcile-sweep internals
  at this granularity (same finding as BL-1386).

## Verdict
Doc content corrected in both owning how-to pages (not NONE), plus the
standard Specification.MD/index.md entries. Forwarding to QA.
