# BL-1337 — documenter pass (2026-09-03)

## Context

Merged hardener `23b489d554` (mutation sweep: 2 real gaps closed with tests,
no production code touched; property-suite flake from architect's D1 fix
confirmed clean over 5 consecutive runs). Reviewed the coder/cleaner/
architect/hardener evidence chain plus the ticket YAML, the feature file, and
the shipped `bob_starting_cast_lib.bb` / `bob_starting_cast_cli.bb` to
identify what user-visible/operator-visible surface changed.

## Review checklist (complete pass, Article 4.4)

- **Existing how-to currency** (`docs/how-to/BL-1181-bob-starting-cast-cherry-pick-apply.md`):
  the doc described only the un-profiled `export`/`apply` verbs. The ticket
  adds a second, profile-driven mode to the SAME CLI/lib — extended the doc
  in place with the profile JSON shape, the `--profile` verb usage, the
  handshake/verdict semantics, and the propose-never-install /
  no-secret-material guarantees, rather than forking a new page (one
  CLI/lib pair, one doc).
- **`docs/index.md` orphan/exhaustiveness check**: the how-to was already
  linked; updated its one-line hook to mention the BL-1337 addition so the
  index entry stays accurate.
- **`Specification.MD` "Last Updated" freshness**: added a new dated entry
  at the top of the changelog describing the profile/handshake mechanism,
  cross-referencing the same how-to. Bumped in the same commit as the
  content, per the standing rule.
- **Diagram currency** (constitution Article 1.7 / local-engineering
  "Diagrams"): checked the four registered diagrams
  (`architecture.mmd`, `swarm-flow.mmd`, `handoff-flow.mmd`,
  `front-desk-flow.mmd`) against this parcel's change. None of their
  change-triggers fired — this ticket adds a CLI mode and library functions
  inside the existing Model Steward store/CLI shape; it does not change the
  extension host/webview boundary, the tmux substrate relationship, the
  `.swarmforge/` state layout, pipeline topology, backlog flow, the handoff
  file lifecycle, or front-desk routing. No diagram update required.
- **`docs/deprecated/` check** (Article 3.6): nothing was retired by this
  parcel — the un-profiled `export`/`apply` verbs remain live and unchanged
  (BL-1337's `required_wiring` explicitly reuses rather than replaces them).
  No page moved.
- **Production-code scope**: none touched by this pass; doc/spec files only.

## Findings

NONE.

## Verdict

Forward to QA.
