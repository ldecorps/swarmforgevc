# BL-1337 — QA approval (20260903)

Received: documenter commit `f85e5d3c0a`, forwarding hardener `23b489d554`,
architect (D1 re-review `48c8e22a67`), cleaner rework `87437f3323`, coder's
profile-driven cast generator + pre-apply handshake, per the human's ruling
("Cast plus ModelFactory overlay only, reusing the apply path BL-1181
already ships"). Merged into QA clean, no conflicts (merge `d14ffe827e`).

## Ancestry check (BL-336 discipline)

`git merge-base --is-ancestor 23b489d554 HEAD` and
`git merge-base --is-ancestor f85e5d3c0a HEAD` both confirmed true against
the commit this approval cites — this ticket's own hardener/documenter work
is actually an ancestor of the commit being approved, not merely something
that happens to be on `main` by another path.

## Bounce history check (BL-340)

One prior bounce: architect → coder, 2026-09-03, D1 (invariant 1's property
reach-floor drawn by luck, ~17.5% flaky over 40 runs), class
`invariant-unencoded`, commit `922078d70f`, evidence
`backlog/evidence/BL-1337-architect-bounce-20260903.md`. Independently
re-verified below as fixed — not merely trusted from the architect's
re-review.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/bl1337_profile_cast_test_runner.bb`: ALL PASS.
  Assertions map to every changed production path (Article 4.5): profile
  parse/refusal (no name, no roles, unknown handshake bar), handshake
  fallthrough on registry-ineligibility and on host-unreachability,
  unstaffable-seat cast-entry absence (key-absent, not nil-valued),
  at-floor boundary (`>=`, not `>`), `registry-only` weaker-bar path and its
  evidence-note recording, both `apply-via-modelfactory-overlay` arities
  (gated 2-arg refuses a not-runnable cast; the pre-existing 1-arg BL-1181
  call site untouched), evidence-note content (no credential material),
  and `host-reachable?`'s fail-closed default for an unrecognised provider.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1337-a-profile-generates-a-handshaken-cast.feature`):
  7/7 pass — matches the ticket's own `qa_e2e_procedure` scenario-for-
  scenario (eligible+reachable, not-certified fallthrough, certified-but-
  unreachable fallthrough, floor failure names the seat, live-pack
  untouched, no-secret-material).
- `required_wiring`
  (`bob_starting_cast_lib.bb::apply-via-modelfactory-overlay`): confirmed
  the gate sits in front of the REAL door, not a parallel one — read
  `-main`'s dispatch in `bob_starting_cast_cli.bb`: `apply --profile <n>`
  routes to `run-profile-apply`, which calls the gated 2-arg arity; the
  ungated `apply` (no `--profile`) still calls the original 1-arg arity,
  byte-identical to BL-1181. Both call sites grepped and read directly,
  not assumed from evidence.
- D1 flake, independently re-verified (not trusted from the architect's
  re-review): `npx vitest run --config vitest.properties.config.mjs
  bl1337ProfileCastInvariants` — 5 consecutive isolated runs, 3/3 green
  every time, no flake. Present and green in the full concurrent
  `test:properties` run too (see below).
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1337ProfileCastSteps` — confirmed, not dead code.
- Suite-manifest registration: `swarmforge/scripts/test/suite-manifest.tsv`
  lists `bl1337_profile_cast_test_runner.bb` as `standing` — confirmed.
- Scope check: diffed this ticket's full pipeline lineage
  (`git diff --name-only 68664038e4 f85e5d3c0a`) against its declared
  files — exactly the ticket YAML, its own evidence files, the how-to +
  index.md + Specification.MD entries, the property test, feature file,
  step/fixture files, the two `.bb` production files, its own test runner
  and the suite manifest. No drift outside declared scope.
- No regression in what this extends:
  `bb swarmforge/scripts/test/bob_starting_cast_test_runner.bb` ALL PASS,
  `bb swarmforge/scripts/test/model_steward_test_runner.bb` ALL PASS.
- Full unit suite (`npm test`): 15 files / 25 tests red. Every one grepped
  against the backlog and confirmed pre-existing, already-ticketed standing
  debt with zero file overlap with this parcel's diff: the
  `deps.checkOrphanedAuthoredDocs is not a function` cluster (10 files) is
  BL-1221; `backendSwitch`/`telegramClient`/`telegramCursorOperatorExec` is
  BL-1263; `operatorRuntimeBbFixtureClosure` is BL-1265;
  `tempDirTrapGuard`/`liveRepoDerivationGuard`/`socketFixtureShortRootGuard`
  are the standing whole-tree guard reds (BL-1290 owns the last; the other
  two flag files last touched 2026-09-02/2026-08-25, unrelated to this
  parcel).
- Property suite (`npm run test:properties`, captured via a detached
  background run per the known 120s tool-timeout trap): 25 files / 14 tests
  red. Used the guard's own helpers
  (`property_suite_standing_allowlist_lib.sh`) rather than reading the log
  by eye: of the 25 failing files, 24 are allowlisted standing debt and
  exactly ONE is not — `bl1218RemoteControlConfigInvariants` invariant 2.
  This parcel touches no launch-script/remote-control-config file, and the
  same file+invariant was independently diagnosed as a load flake by
  today's earlier BL-1336 QA pass (`backlog/evidence/BL-1336-qa-approval-
  20260903.md`: 4/4 clean in isolation). Re-ran it myself, 4 consecutive
  isolated runs, 4/4 clean every time — confirms the same load flake, not a
  regression from this parcel. BL-1337's own property test
  (`bl1337ProfileCastInvariants`) is green in this same full run.
- Documentation currency: how-to `BL-1181-bob-starting-cast-cherry-pick-
  apply.md` extended in place with the profile JSON shape, `--profile`
  usage, and the handshake/verdict semantics (not forked — one CLI/lib
  pair, one doc). `docs/index.md` hook updated. `Specification.MD` gained a
  dated top entry. Diagram-currency claim checked against all four
  registered diagrams' change-triggers (extension host/webview boundary,
  tmux substrate, `.swarmforge/` state layout, pipeline topology, backlog
  flow, handoff lifecycle, front-desk routing) — none fire for a CLI mode +
  library functions added inside the existing Model Steward store/CLI
  shape. No diagram update required, no page retired.
- Orphaned test processes: none found in this worktree before, during, or
  after (checked `pgrep -fal 'node --test|stryker|vitest'` scoped away from
  other worktrees' own legitimate concurrent runs each time).

## Findings

NONE.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
