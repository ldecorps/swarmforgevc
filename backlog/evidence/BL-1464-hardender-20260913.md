# BL-1464 — hardender review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No production line changes (constraint); no `src/*.ts` touched (CRAP/DRY
n/a); no `Scenario Outline` in the feature (BL-113/BL-638 inapplicable).
Hardened via hand-authored mutation over the new fixture logic (BL-567
fallback pattern), plus the standing-guard and suite re-runs the touched
path requires.

Suites re-run (all green, matching coder's evidence):
- `run_acceptance.sh` BL-1464's own feature: 3/4 pass, scenario 04
  ("register row leaves") fails as documented — that removal is the
  documenter's job in this same land, not a hardener defect.
- `run_acceptance.sh` BL-1297's feature: 6/6 (was 5/6 before the fix).
- `run_acceptance.sh` BL-1343's and BL-1389's features: 6/6 and 5/5,
  unchanged verdicts.
- `land_step_lib_test_runner.bb`, `task_scope_gate_lib_test_runner.bb`:
  ALL PASS.
- `bl1389UnlandedSiblingPathNeverRidesInvariants.property.test.js`,
  `bl1343ReplayNeverDropsOwnPathInvariants.property.test.js`: 5/5 pass.
- Standing whole-tree guards (`extension/test/*Guard*.test.js`, non-property,
  required because this parcel adds a file under `specs/pipeline/steps/`):
  18 files / 183 tests, ALL PASS, including the new
  `bl1464MergeOnlyFixtureOwnContentSteps.js`.

Hand-authored mutation sweep over `mergeOwnParcelIn` (the ticket's only new
production-adjacent logic — a test fixture builder, since the ticket makes
no `.bb`/`src` changes):

1. Branch commit subject `${TASK}` → `${OTHER}` (reintroduces the
   foreign-id authorship the fix removes) — **KILLED**: BL-1464 scenario 02
   fails (attribution shows the path owned solely by the sibling), and
   scenario 01 fails transitively (BL-1297 scenario 03 reverts to red).
2. `TRUNK_PATH` commit moved back to AFTER the `origin/main` ref update (the
   coder's own documented pre-fix ordering, §1 of the coder's evidence,
   verified empirically here rather than taken on their word) — **KILLED**:
   BL-1464 scenario 02 fails specifically (`TRUNK_PATH` now shows as a
   delivered path owned solely by `BL-9999`), scenario 01 and 03 unaffected
   — isolates the ordering fix as the discriminating change, distinct from
   mutant 1's authorship fix.

Both mutants restored; working tree diffed clean against the received
commit (`50864bb172`) before and after the sweep. No orphaned mutation
fixtures or test processes (`pgrep -fl 'node --test|stryker'` clean before
and after; the fixture's own `cleanup(ctx)` runs at each scenario's
terminal step, same shape as BL-1297's own handler).

No functional gap found. Forwarding to documenter.

By hardender.
