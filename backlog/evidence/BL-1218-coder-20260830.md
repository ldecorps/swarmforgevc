# BL-1218 — coder pass, 2026-08-30

`config remote_control off` decides the launched flag, not just auto-inject.

## 1. The change

`swarmforge/scripts/swarmforge.sh` line 723 used to read:

```zsh
if [[ "$agent" == "claude" && "$REMOTE_CONTROL_DEFAULT" == 1 && "$extra_cli" != *"--remote-control"* ]]; then
  extra_cli+=" --remote-control $(remote_control_session_name_for_role "$role")"
fi
```

— an INJECT-only rule. A window line that named the flag itself was never
consulted about the config, and both `swarmforge.conf` and
`packs/full-forge.conf` name it on every Claude window line, so on exactly
the packs a human is most likely to run, `remote_control off` switched
nothing off.

Replaced with one call to a new pure `resolve_remote_control_cli`
(`swarmforge/scripts/remote_control_launch_lib.sh`), sourced at file scope
next to `project_socket_id_lib.sh`:

- agent is not `claude` → unchanged.
- default `1` → today's rule exactly: a named flag stays where the window
  line put it, an absent one is appended the way `extra_cli+=" ..."` always
  appended it.
- default `0` → no remote-control flag survives, and a following session
  name goes with it. A following token starting with `-` is another flag and
  is kept.

The decision is a pure string transform so it is asserted directly, not only
observed through a written script.

**Sourced by zsh, tested under bash.** The lib avoids word-splitting an
unquoted parameter — zsh does not split and bash does, so a token loop would
behave differently in the two and the difference would be invisible in
whichever one was not exercised (BL-801's shape). It uses `sed` instead, and
`test_remote_control_launch_lib.sh` passes under **both** shells.

## 2. Invariant 3 verified byte-for-byte, not argued

Composed a real `coder.sh` from the same fixture before and after the change
for the two on/absent paths, with the temp root normalised out, and diffed:

| path | diff |
|---|---|
| window line NAMES the flag, config absent | **identical** |
| window line OMITS the flag, config absent (auto-inject) | **identical** |

That is why the config-on branch returns `$rc_cli` untouched rather than
stripping and re-appending: re-appending would move the flag to the end of
the line and byte-for-byte would fail. Break 2 in the table below is exactly
that mistake, and the invariant-3 property catches it.

The empty-`extra_cli` case keeps today's leading space — `extra_cli+=" --remote-control X"`
has always produced one — pinned by lib case 08.

## 3. Tests

`swarmforge/scripts/test/test_remote_control_launch_lib.sh` (new, registered
in the suite manifest; inventory gate `ok - 441 test file(s)`): 11 cases,
green under bash AND zsh. Written before the lib existed; the run failed on
the missing file first.

`swarmforge/scripts/test/test_remote_control_launch.sh` extended from 2 to 5
cases. Cases 01/02 are unchanged and still pass — they omit the flag, so
they compose identically before and after and prove nothing about this
ticket. **Case 03 is the row that gates it**, and it FAILS against the
pre-change `swarmforge.sh` (verified by checking that file out at HEAD,
re-running, and restoring).

Case 03's second assertion had to be corrected during the pass: `--model`
and `--effort` are parsed out of `extra_cli` into the settings JSON (BL-319)
and never appear in the script text, so "stripping did not eat the rest of
the line" is checked in both places — `--dangerously-skip-permissions` in
the script, the model id in `coder.claude-settings.json`.

## 4. Declared invariants (BL-654) — all three executable

`extension/test/bl1218RemoteControlConfigInvariants.property.test.js`,
`npm run test:properties` lane. 4 properties, all pass.

Invariants 1 and 3 drive the pure lib; invariant 2 drives the REAL
`swarmforge.sh write_role_launch_script` under zsh and asserts against the
file that lands on disk.

Invariant 3 does not assume "unchanged" — it models the pre-BL-1218 rule in
`legacyCompose` and asserts the new composition equals it for every
on/absent case.

Generator reach, asserted every run: the flag's PLACEMENT is drawn as
absent / trailing / leading / bare, because a line that omits the flag
composes identically before and after and would pass against the defect; and
both `claude` and `codex` agents, since invariant 3 covers the untouched
seat too.

Non-vacuity, run and restored:

| deliberate break | properties that FAIL |
|---|---|
| config-off branch returns the cli unstripped (the defect) | invariant 1, invariant 2 (2 of 4) |
| config-on branch strips and re-appends (order changes) | invariant 3 (1 of 4) |
| none | 0 of 4 |

## 5. Acceptance (BL-112)

`specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1218-config-off-is-honored-over-an-explicit-window-flag.feature`
— 7 scenarios, 7 pass, 0 fail. Handlers in
`specs/pipeline/steps/bl1218RemoteControlConfigAtLaunchSteps.js`, registered
in `steps/index.js`. Both Examples columns resolve through explicit
`KNOWN_WINDOW_LINE` / `KNOWN_CONFIG` / `KNOWN_OUTCOME` lookups.

Scenario 03 asserts TWO things separately and on purpose. `expected-rc-name`
returning nil is BL-1217's config gate, which already landed and would report
`off` even with a stale script still naming a flag — so on its own that
assertion is vacuous for THIS ticket. `extract-rc-name` on the persisted
script returning nil is BL-1218's half: the script itself no longer names a
session, so script and config agree rather than merely being overridden.

One handler regex covers both the outline's substituted
`carries no a remote-control flag` and scenario 04's
`carries no remote-control flag`; the article belongs to the outline
template, not the assertion.

## 6. Scope held

Non-Claude seats are untouched in both directions (lib cases 10/11, feature
scenario 04). Absent config is indistinguishable from on. Nothing here
rewrites launch scripts already on disk — that is BL-1217, explicitly out of
scope. The coordinator's own path already composed from scratch under the
config and is unchanged.

By coder.

---

## Re-dispatch verification, 2026-08-31 (coder)

BL-1218 was re-dispatched to the coder by a coordinator `note` after
`d326333e06` released it `hold -> active`. It was NOT rework: no bounce
reached this role, and the coder stage was already complete. This is the
same shape as BL-1264, and for the same reason.

What I verified before declining to re-implement:

- **The fix is present and landed.** `5b534f8ea9` ("config remote_control
  decides the launched flag, not just inject") is on `main`, together with
  `swarmforge/scripts/remote_control_launch_lib.sh`, its test file, the
  property test and the acceptance step handlers. Diffing this branch
  against `main` over `extension/ specs/ swarmforge/ docs/` is EMPTY —
  there is no code of mine left to land.
- **The parcel had already cleared three downstream gates.** `58842aa55e`
  (cleaner), `4c4595baf5` (architect, COMPLIANT), `171095907a` (hardener —
  verified, no code changes needed). All three evidence files are in
  `backlog/evidence/`.
- **Why it stalled**: `66be60ea40` parked BL-1210, BL-1218, BL-1225,
  BL-1240, BL-1252, BL-1253 and BL-1264 `active -> hold`, "orphaned by a
  dead expedite run for BL-1295". The parcel died between the hardener's
  forward and the documenter's receipt. The release commit returned the
  ticket to `active/` but re-entered the chain at the coder rather than at
  the stage that never ran.
- **Still green after merging `main`** (merge `9a0ae9e37b`; diffed against
  BOTH parents — no code reverted, and no code divergence in either
  direction):
  - `test_remote_control_launch_lib.sh` → 11/11 pass.
  - `test_remote_control_launch.sh` → ALL PASS (5 cases).
  - `npm run test:properties -- bl1218` → 4/4 pass.
  - `run_acceptance.sh specs/features/BL-1218-config-off-is-honored-over-an-explicit-window-flag.feature`
    → 7 scenarios, 7 pass, 0 fail.

Surfaced, not swept: `test_remote_control_launch.sh` prints
`write_role_launch_script:281: command not found:
terminal_backend_can_open_sessions` while still passing every case. The
harness does not source whatever defines that helper, so the branch guarded
by it is never exercised under test. It is not BL-1218's doing — this
ticket's diff does not touch that line — and per the ticket's own scope
line it is a note, not scope.

No functional change is available for me to forward (Article 1.9 / 2.3.2
No-Op Rule), so no `git_handoff` was sent. Resumption point is the
**documenter**, the first `required_stages` entry with no recorded pass.

By coder.
