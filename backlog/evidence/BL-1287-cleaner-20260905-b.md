# BL-1287 — cleaner pass (redo after bounce), 2026-09-05

DRY fix applied (no behavior change) — real duplication found and
eliminated; the coder's bounce fix (zombie-creator via `ps -o stat=`) is
correct and complete.

## Defect found and fixed

`jscpd` found a clone this ticket's own two new files share:
`spawnFakeCloudflared`/`nameWithCreator`/`deadPid`/`killPid` were written
independently, near-identically, in both
`bl1287FixtureSweepScopingInvariants.property.test.js` and
`specs/pipeline/steps/bl1287FixtureSweepSparesLiveRunSteps.js` — a gap in
my own EARLIER cleaner pass (`97c297608d`, reverted by the architect for
an unrelated defect): that pass only ran `jscpd` on the three files it
called "touched/new" and did not include the acceptance step handler in
that run, so this duplication existed since the original coder commit
(`caf26a4c07`) and went unflagged. Extracted into
`extension/test/helpers/bl1287FixtureSweepFixture.js`
(`killPid`/`spawnFakeCloudflared`/`nameWithCreator`/`deadPid`); both files
now delegate to it. Confirmed no behavior change: same fixture shape, same
binDir/name construction, only a cosmetic tunnel-name label suffix
unified (`bl1287prop` → `bl1287`, asserted nowhere, grepped to confirm).

Left BL-857's own pre-existing, separate copies of the same shape
(`bl857TunnelOwnershipInvariants.property.test.js`,
`bl857TunnelOwnershipOneOwnerSteps.js`) untouched — out of this ticket's
scope (a different, unrelated ticket's fixtures; a bounce-fix pass sweeps
its own ticket's duplication, not an unrelated one's).

**Self-caught regression during the extraction**: the first cut of the new
helper copied the step handler's `spawnFakeCloudflared` body verbatim,
which used a raw `fs.mkdtempSync(path.join(os.tmpdir(), ...))` rather than
the shared `mkTmpDir()` wrapper the property test's original (pre-fix)
copy used — this tripped `check_property_suite_drift.sh`'s BL-1280
mkdtemp-migration guard at commit time (`bl1280MkdtempMigrationInvariants
.property.test.js`'s "no raw call site under the three-path list"
assertion), refusing the commit outright rather than landing silently.
Fixed by routing the shared helper through `mkTmpDir('bl1287-fake-cf-')`
instead; re-ran the guard's own property test (6/6) plus the full BL-1287
suite again after the fix, all green.

`jscpd` re-run on the touched/new files (now including the step handler)
after the fix: 0 clones (was 1 clone, 17 duplicated lines / 94 duplicated
tokens).

## What else was checked

- Direct diff review of `isProcessAlive`'s fix: confirms the zombie check
  (`ps -o stat=`, treating a leading `Z` as gone) runs only after the
  pre-existing `kill(pid, 0)` probe succeeds, and the pid<=0/non-integer
  guard runs first — matches the bounce's own remedy exactly, no
  broader behavior change to the pre-BL-1287 contract (still "never
  throws" for pid 0/negative, now additionally correct for pid 0/negative
  by refusing them outright rather than relying on `kill`'s incidental
  non-throw).
- Confirmed neither BL-1287's nor BL-1429's reverted files were
  resurrected by this merge (coder's branch predates both bounce reverts;
  merge diff confirmed to add only BL-1287's re-added files, no BL-1429
  path reappeared).
- Confirmed the new zombie unit test uses BL-1292's own `spawnZombie`
  helper (`fixtureLiveness.js`) rather than a fabricated zombie — same
  real-process discipline the rest of this suite already established.
- Confirmed the property test's invariant-2 non-vacuity claim directly:
  the `ZOMBIE` death mode is a real member of `DEATH_MODES`, asserted
  drawn via the `seen` set pattern this file already used pre-bounce.
- Mutation-site count on the touched helper: `fixtureTunnelName.js` = 101,
  one over the 100 advisory threshold (BL-485). **No split**: this is a
  single cohesive module (tunnel-name shape + leak-detection selection,
  one declared concern), and a split here would only relocate code across
  an arbitrary line count, not improve structure or separation of
  concerns — the correct call per BL-485's own "weigh, don't mechanically
  chop" guidance, not an oversight.
- `npm run compile` clean.

## Verification (all re-run in this worktree, all green, no regressions, after the DRY fix)

| check | result |
|---|---|
| `npx vitest run test/helpers/fixtureTunnelName.test.js` | 7/7 pass |
| `npx vitest run --config vitest.properties.config.mjs test/bl1287FixtureSweepScopingInvariants.property.test.js test/bl1061TunnelFixtureIsolation.property.test.js` | 8/8 pass |
| `npx vitest run --config vitest.properties.config.mjs test/bl857TunnelOwnershipInvariants.property.test.js test/bl787NamedTunnelInvariants.property.test.js` (concurrent overlap regression, unaffected) | 7/7 pass |
| `node specs/pipeline/cli.js specs/features/BL-1287-...feature` | 4/4 |
| `npm run compile` | clean |
| `node extension/out/tools/mutation-site-count.js extension/test/helpers/fixtureTunnelName.js` | 101 — one over threshold, no split (cohesive module) |
| `npx jscpd` on the touched/new files (step handler included this time) | 0 clones (was 1, now fixed) |
| `npx vitest run --config vitest.properties.config.mjs test/bl1280MkdtempMigrationInvariants.property.test.js` (guard, after self-caught fix) | 6/6 pass |

Forwarding the DRY-fixed candidate to architect.
