# BL-1332 — documenter pass (2026-09-03)

## D1 (found, fixed): behaviour change undocumented

The landed fix (`swarmforge/scripts/land_step_lib.bb::own-paths`, commit
`fd94ef4a63`) adds a whole new refusal shape to the land step — a path
shared between the landing ticket and an unlanded sibling now refuses the
land by name instead of replaying the whole blob (carrying the sibling's
lines in). This is swarm-delivery-machinery behaviour with existing,
actively-maintained documentation coverage:
`docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`
already documents the neighboring BL-1315 and BL-1343 own-paths fixes in
their own sections, and its BL-1343 section explicitly forward-referenced
BL-1332 as landing separately — so leaving it undocumented here would be a
stale reference, not merely an omission. `docs/reference/Specification.MD`
is also actively dated per landed ticket (most recent prior entry: BL-1056).

**Remediation**: added a new "A path shared with an unlanded sibling
refuses instead of carrying its lines (BL-1332)" section to the BL-1241
how-to (commit `a1dadfb760`, untagged subject per
`task_scope_gate_lib.bb` — the file's basename names BL-1241, a different,
already-shipped ticket, so a BL-1332-tagged commit touching it is flagged
as foreign scope; established precedent for this repo per prior documenter
turns), and a new dated entry at the top of Specification.MD (commit
`22dc239190`, tagged — Specification.MD's own basename names no ticket, so
the scope gate does not apply to it).

## Full inventory checked, no other defect found

- Diagram currency (local-engineering.prompt "Diagrams"): none of the four
  registered diagrams (`architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`) depict land-step internals or
  own-paths attribution — `grep` for `land.step`/`land_step` across
  `docs/diagrams/*.mmd` returns nothing. No trigger fired.
- README/command-list currency: this ticket adds no new extension command,
  setting, or CLI flag — `land_step_cli.bb`'s invocation shape and output
  (`LAND_CLEAN`/`LAND_REPLAY`/`LAND_ESCALATE`) are unchanged; only the
  refusal condition inside `own-paths` is new, already covered by the
  how-to update above.
- Retired behaviour (Article 3.6 / `docs/deprecated/`): not applicable —
  this ticket adds a new refusal, retires nothing.

By documenter.
