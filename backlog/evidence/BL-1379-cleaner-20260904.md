# BL-1379 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked

- **Correctness / manual verification** — ran the acceptance fixture's
  `landed` and `not-landed` shapes directly (not only via the step
  handler):
  - `landed`: `{"restored":["BL-9002"],"restoredAndMarked":["BL-9002"],
    "marked":true,"restoredStatus":"blocked","promotionBlocked":true,
    "markNamesRun":true,"activeAfter":["BL-9002"],"holdAfter":["BL-9003"],
    "left":[]}` — matches every scenario 08/09 assertion exactly, including
    the human-held BL-9003 staying untouched.
  - `not-landed`: nothing restored, BL-9002 stays in `hold/` reported as
    `hold-not-landed`, `note` matches invariant 2's own wording.
  - Confirmed `promotionBlocked` is read from the REAL
    `promotion_gates_lib.bb` (`load-file`d directly in the fixture CLI), not
    simulated — matches the step handler's own comment claim.
- `expedite_lib_test_runner.bb` — re-ran: ALL PASS.
- **Retirement discipline** — `specs/features/BL-567-...feature` scenario 18
  is REMOVED, not reworded, matching the ticket's own `retires:` field and
  Article 3.6's retirement convention (dead logic never left as commented-out
  or stale-reworded text).
- **Coordinator wiring** — `swarmforge/roles/coordinator.prompt` gained a
  clear, self-contained instruction for clearing the `freshness_check`
  mark, naming the exact CLI (`deprecate-check.js`) and both outcomes
  (allow/hold), consistent with Article 3.6's existing gate.
- `mutation-site-count.js` on `bl1379ParkReversalSteps.js`: 232 sites
  (`over` 100 — the highest of this batch). Reviewed and declined to split:
  still one Gherkin feature, one handler (the established convention
  throughout this codebase), ~18 tightly-scoped Given/When/Then defs
  organized into clear background/given/when/then/amendment sections; the
  count reflects assertion density and scenario breadth (base scenarios
  plus the specifier's 08/09 amendment), not mixed responsibilities. A
  split along the amendment boundary would be arbitrary (following commit
  history, not a domain seam) rather than structural.
- `jscpd` over the new JS handler and the touched `.bb` files: 0 clones.
- TypeScript compiles clean; the handler discovers via BL-1371's registry.
- Babashka has no mutation/CRAP/DRY wired (Startup Tools, BL-472 deferred);
  gated by `expedite_lib_test_runner.bb` and the fixture-driven acceptance
  instead, per the documented degraded fallback.

No bounce history on BL-1379 (first pass, `main`/`origin/main` agree).
Forwarding unchanged to architect.

By cleaner.
