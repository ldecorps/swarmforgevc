# BL-1623 — hardender review pass, 2026-09-17

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: npx vitest run --config vitest.properties.config.mjs test/bl1623ScopedTmpDirSweepInvariants.property.test.js (hand-mutated sweepStaleTmpDirs: dropped ownerPid===process.pid clause)
- **Commit hash**: d293731ebd
- **First error excerpt**: 3/3 tests still passed - the own-pid short-circuit is unobservable through the fixture as originally written
- **Failure class**: test-coverage-gap
- **Expected vs observed**: the own-pid case must prove removal happens via the OR short-circuit, not merely because the fake isPidAlive happens to also report it dead
- **Blamed role**: hardener
- **Remediation pointer**: added process.pid to aliveSet for own-kind entries so isPidAlive(process.pid) genuinely returns true; confirmed red on the mutant, green on real code

By hardender.

## Detail

This is a self-remediated finding in the hardener's own domain (mutation
hardening/test coverage), fixed in this same pass — not a bounce to
another role.

- `npm run compile` (extension/) → clean.
- `npm test` (extension/) → 626/626 files, 10677/10677 tests, exit 0.
  One earlier run under heavy concurrent host load (multiple worktrees
  running their own heavy suites simultaneously — verified via `ps`)
  reported transient unhandled-timer noise ("148 errors"); a clean
  re-run on a quieter host reproduced zero errors, confirming it was
  host-contention noise, not a regression.
- `npm run test:properties` (extension/) → 416/416 files, 1222/1222
  tests, exit 0. 4 unhandled errors, all the documented allowlisted
  `[vitest-worker]: Timeout calling "onTaskUpdate"` class (BL-871) — no
  other errors.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1623-*.feature`
  → 12/12 pass.
- `npx vitest run test/helpers/tmpDir.test.js test/blindTmpDirSweepGuard.test.js`
  → 19/19 pass; guard census line confirms 7 migrated files, 0 still-blind.
- Each of the seven migrated property files run individually, all green
  (bl1309, bl1343, bl1030, bl1300, bl1356, bl1358, bl1359).
- Hand-mutation (BL-567 fallback where applicable — `tmpDir.js` is a
  test helper, outside Stryker's `src`/`out` scope per the ticket's own
  `mutation_cost` note):
  - Dropped `ownerPid === process.pid ||` from `sweepStaleTmpDirs`'s
    removal condition (leaving only `!isPidAlive(ownerPid)`): all 3
    tests in `bl1623ScopedTmpDirSweepInvariants.property.test.js` stayed
    green — the property's own 'own'-kind case never actually drove
    `isPidAlive(process.pid)` to return true (the fake `aliveSet` never
    contained the real `process.pid`), so it could not distinguish "the
    own-pid short-circuit fired" from "the fake liveness check happened
    to say dead anyway". Direct probe against the real helper
    (`node -e` against the committed code) confirmed the real behavior:
    it removes an own-pid-named entry even when `isPidAlive` explicitly
    returns `true` for that pid.
  - Fixed by adding `process.pid` to `aliveSet` for `'own'`-kind
    entries, so `isPidAlive(process.pid)` genuinely reports alive and
    removal can only be explained by the OR short-circuit. Confirmed red
    on the mutant, green on the real code; reverted the mutant before
    committing (`git status --short` clean after restore).
- Verified `tmpDir.js`'s guard/finder/blind-sweep helper interacts
  correctly with my own earlier BL-1601 EBUSY hardening — both survived
  the merge intact and re-verified green together.
- An `MaxListenersExceededWarning` (11 exit listeners) observed on
  `bl1343ReplayNeverDropsOwnPathInvariants.property.test.js`, alone and
  in a batch, was confirmed pre-existing and unrelated: reproduces
  identically on a clean checkout of `origin/main` with none of this
  session's commits present. Not this ticket's defect, not investigated
  further (out of scope).
