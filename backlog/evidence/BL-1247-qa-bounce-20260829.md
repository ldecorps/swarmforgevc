# BL-1247 (bl593-property-generator-emits-values-its-own-contract-refuses) — QA bounce, 20260829

## D1 — entangled tip: same class as BL-1238's bounce this same pass (BL-506)

1. **Failing command:**
   ```
   git diff --name-only origin/main b5f2489906 | wc -l
   ```
   → **46** changed paths.

2. **Commit hash:** `b5f2489906` — "Merge hardender 9b70d5398d for BL-1247
   (bl593-property-generator-emits-values-its-own-contract-refuses). By
   documenter." — received via `git_handoff` payload `merge_and_process
   documenter b5f2489906`. Its own merge history
   (`git log --oneline --merges HEAD..b5f2489906`, HEAD = `b6cb7a951`, my
   last landed commit) is dense with `Merge main into <role>` commits
   spanning coder, cleaner, architect and hardener, not only documenter —
   this is not a single role's mistake, it is systemic across this pass.

3. **First error excerpt** — the 46 paths overlap heavily with BL-1238's
   own bounce this same pass (`BL-1238-qa-bounce-20260829.md`): unbuilt
   specifier mints BL-1252–1255, the local-only constitution reference
   amendment (`engineering.prompt` / `reference/engineering-detailed.prompt`),
   BL-1238's own hold/ ticket riding back in even though this QA session
   just bounced it, and — the part specific to this ticket —
   `git diff --name-only 9b70d5398d b5f2489906` (documenter's own tip
   minus the hardener merge it started from) shows the RETIRED
   `BL-1247-reconcile-sweep-kill-switch` ticket's own files
   (`backlog/active/BL-1247-reconcile-sweep-kill-switch.yaml`,
   `specs/features/BL-1247-reconcile-sweep-kill-switch.feature`,
   `specs/pipeline/steps/bl1247ReconcileSweepKillSwitchSteps.js`,
   `swarmforge/scripts/master_main_reconcile_lib.bb`,
   `swarmforge/swarmforge.conf`) — the SAME id this ticket's own YAML
   notes say was already adjudicated as a collision and retired in favour
   of the bl593 ticket (`Adjudicate BL-1247 id collision: retire the
   kill-switch copy, keep the id with BL-593`). Whether that retirement
   survived this branch's repeated main-merges, or whether it is the
   surviving bl593 ticket that got overwritten, cannot be determined from
   this entangled tip alone.

4. **Failure class:** `behavior` (BL-506 entangled tip).

5. **Expected vs observed:** Expected — a `git_handoff` naming
   `BL-1247-bl593-property-generator-emits-values-its-own-contract-refuses`
   cites a commit touching only that ticket's own paths. Observed — a
   branch carrying unbuilt specifier mints, a local-only constitution
   amendment, another already-bounced ticket's content, and what looks
   like a resurrection of a ticket its own notes say was retired for an
   id collision.

## Other checks this pass

- BL-1247's own functional correctness not independently re-verified —
  **BLOCKED BY D1**, same reasoning as the BL-1238 bounce this pass.

## Systemic flag

This is the SECOND parcel this pass carrying entangled foreign content
(BL-1238 was the first, same class, `BL-1238-qa-bounce-20260829.md`), and
the merge history shows every pipeline role — not only documenter —
merging `main` into its own worktree repeatedly this session. Flagging as
a branch-wide note to specifier + coordinator alongside this bounce,
rather than treating each occurrence as an isolated defect.

## Remediation

Same as BL-1238's: rebuild a tip-pure `BL-1247`-only commit on the
current `origin/main` (`b6cb7a951`), and separately confirm whether the
`BL-1247-reconcile-sweep-kill-switch` retirement adjudication needs to be
re-applied or was never actually lost (a specifier-level check, not a
coder one — flagging in the branch-wide note).

By QA.
