# BL-1186 — QA pass — 20260828

**Received:** `merge_and_process documenter 3eb3de6f91` (handoff
`00_20260828T012854Z_000877_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `3eb3de6f91b2ceb765dd68125690d632a77dc150`
**QA merge commit:** `4422614c6`

## Merge conflict resolved

`specs/pipeline/steps/index.js` conflicted (documenter's branch re-added
`bl1195WorktreeTrackedContentDriftSteps`, already present later in my own
tip). Took the empty HEAD side, dropping the duplicate. `swarmforge/scripts/ready_for_next.bb`
auto-merged clean (added one line, no conflict). Verified post-resolve: no
new duplicate `require()` targets introduced (`bl718`/`bl726` duplicate
present is pre-existing on both merge parents, unrelated, out of scope —
same as noted in the BL-1189 pass immediately before this one). `node -c`
clean.

## BL-336 ancestry guard

- `git merge-base --is-ancestor 5fd89f866f HEAD` (coder) — CONFIRMED
- `git merge-base --is-ancestor 38f100a4c1 HEAD` (architect) — CONFIRMED
- `git merge-base --is-ancestor 3eb3de6f91 HEAD` (documenter, received tip) — CONFIRMED

## Correction-content integrity (BL-1213/main-reset fallout check, carried from prior pass)

`extension/src/bridge/specTreeUiHtml.ts` present; BL-1189 `bounce_history`
2 entries; BL-1188 `bounce_history` 4 entries — unchanged, intact.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1186` | `VERIFY BL-1186` — no open deferral |
| Dependency | `backlog/done/BL-1173-*.yaml` exists | `depends_on: [BL-1173]` satisfied |
| Compile | `npm run compile` | PASS, clean |
| Targeted unit | `npx vitest run test/deprecateIdentifyUnused.test.js` | 16/16 PASS |
| Targeted property | `npx vitest run --config vitest.properties.config.mjs test/deprecateIdentifyUnused.property.test.js` | 5/5 PASS |
| Acceptance | `run_acceptance.sh specs/features/BL-1186-*.feature` | 4/4 PASS (unused, seldom, notify-without-retire, above-threshold omitted) |
| required_wiring (both rows) | grep + confirm real caller | `runDeprecatorIdentifyUnusedScan` is the CLI's own thin-wrapper entry point (`makeArgsGuardedMain`, confirmed by architect) — the ticket's own required_wiring text is "CLI ... emits ranked report", satisfied by the tool running end-to-end via the acceptance run above, not a call site elsewhere in the extension (none is asked for by the ticket's "How" section, which explicitly leaves notification-surface choice open as a later concern). Step registration present in `index.js`, single occurrence, no duplicate. |
| Invariants (1, 2 encoded; 3 reasoned) | architect/hardener evidence + my own targeted re-runs above | 1 (notify-only, never mutates outside `pending-notifications/`) and 2 (locked 90-day/3-hit thresholds, disjoint) both non-vacuously property-tested; 3 (hard-tier-only judgment) correctly left unencoded — enforced by `mutation_cost: high` (confirmed set) routing via BL-1001/Article 3.6, not by this pure module |
| Docs | grep | `docs/how-to/BL-1186-deprecator-identify-unused-notify.md` present, linked from `docs/index.md:102`; `docs/reference/Specification.MD` updated |
| Mutation | hardener's hand-authored sweep (Stryker blocked by pre-existing `CURSOR_API_KEY` env gap, same as this session's other passes) | 5/5 probes killed, no gaps |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker\|[v]itest'` | none, before and after |

Full unit/property suite not re-run a third time this session for this
parcel: same standing-red reasoning recorded in `BL-1189-qa-pass-20260828.md`
immediately before this one (206/9193 unit, 18/690 property, all confirmed
pre-existing and unrelated — none touch `deprecate-identify-unused.ts` or
its test siblings). Targeted + acceptance + wiring above fully cover this
ticket's own changed paths per Article 4.5.

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1186's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1186 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1186 active → done).

By QA.
