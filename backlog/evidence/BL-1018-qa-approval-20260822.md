# BL-1018 QA approval, 2026-08-22

**Reviewer**: QA. **Reviewed at**: documenter tip `b11a8560cc`, merged into
QA. Ancestry confirmed: `git diff` against both merge parents shows only
additive content on each side, no overlap, nothing reverted.

## Scope note: incidental BL-1025 content riding along

This parcel's diff also carries BL-1025 (expedite/QA-verdict-store) content
(`expedite_lib.bb`, `expedite_cli.bb`, `is_qa_ancestor.sh`, its own property
runner/step handlers). BL-1025 is still mid-pipeline with an OPEN architect
bounce (`backlog/evidence/BL-1025-architect-bounce-20260822.md` - "hand-
mirrored verdict vocabulary across the Babashka/bash boundary has no test
asserting agreement") and its own ticket stays in `backlog/active/` with
that defect unresolved. The `git_handoff` that reached me named ONLY
BL-1018 (`TASK_NAME: BL-1018-single-role-respawn-never-kills-the-server`).
I am approving ONLY BL-1018 here (BL-506: an approval authorizes only its
own ticket's work) - BL-1025's content is present in this commit's tree
incidentally (shared batch-role branch lineage) but is NOT being closed,
approved, or represented as done by this review. Its own ticket file is
untouched by my approval note.

## Verification order (Article 4.4, qa_e2e_procedure)

1. **Read `single_role_repair_lib.bb`.** `resolve-single-role-repair` is
   pure and total: refuses (empty commands) on any blank required input,
   otherwise returns EXACTLY ONE command via the single `tmux-cmd`
   constructor that always prefixes `-S <socket>` - "explicit socket" is a
   property of the one constructor, not a rule each call site must
   remember. Missing session -> one `new-session` carrying the launch
   command inline (never create-then-respawn, the incident's own shape);
   present session -> one `respawn-pane -k` in place (never a duplicate
   create). Neither branch can ever produce `kill-server` or
   `kill-session` - those verbs do not appear anywhere in the file.
   Shell-quoting handles launch-script paths containing an apostrophe
   (e.g. `/Users/O'Brien/...`), a real macOS shape.

2. **Wiring verified by grep at the parcel commit**: `swarm_ensure.bb`'s
   `ensure-standing-role!` (the operator's own single-role repair path -
   the exact function implicated in the 2026-08-21 incident) and
   `babysitter_check.bb`'s `ensure-role-session!` (BL-1017's own consumer)
   both route through `run-single-role-repair!` -> the shared resolver,
   executing exactly the commands it returns via `process/sh`, refusing
   (no commands run) on any non-`:ok` status. `kill-session!` is a
   separate, pre-existing function unrelated to the repair path.

3. **Independent test runs from my own worktree**, after merging the
   documenter's commit (clean merge, `git status` clean):
   - `bb swarmforge/scripts/test/single_role_repair_lib_test_runner.bb`:
     **ALL PASS** (27 assertions).
   - `bb swarmforge/scripts/test/bl1018_single_role_repair_property_runner.bb`:
     **ALL PROPERTIES HOLD** (400 runs; generator-reach floor confirms
     coverage of missing/present/no-env/with-env/refusal/quoted-launch
     branches). Directly asserts the negative (qa_e2e_procedure step 2: zero
     `kill-server`/`kill-session` hits across every resolved command) and
     the positive (step 3: every non-refusal command's first 3 elements are
     `["tmux" "-S" socket]`). The runner's own comment records the
     break-then-fix non-vacuity check: dropping `-S` from `tmux-cmd`
     produced 234 property failures.
   - `specs/pipeline/scripts/run_acceptance.sh
     specs/features/BL-1018-single-role-respawn-never-kills-the-server.feature`:
     **4/4 pass** (3 scenarios, one a 2-row Outline), step handler
     registered (`bl1018SingleRoleRepairNeverKillsServerSteps`,
     `specs/pipeline/steps/index.js:562`).
   - `bash swarmforge/scripts/test/test_swarm_ensure.sh`: **ALL PASS**
     (every RC-*/05*/07* scenario, including the pre-existing suite this
     ticket's diff touches).
   - `bash swarmforge/scripts/test/test_babysitter_check.sh`: **ALL PASS**
     (11/11, including J/K - BL-1017's own live-wiring scenarios, now
     routed through this shared resolver too, still green).

4. **No JS/TS production files touched** by BL-1018's own diff (confirmed
   by `git diff` against `extension/src` and `extension/test/*.test.js` -
   empty) - the npm unit/property suites do not apply here, matching the
   established posture for Babashka-only surfaces this session (BL-1012,
   BL-1017).

5. **Live confirmation (qa_e2e_procedure step 4)**: explicitly framed by
   the ticket itself as "outside the unit boundary" and "recorded as a
   manual procedure" - not the exceptional BL-582 "QA must reproduce by
   hand" instruction. Deferred to the operator, same posture as BL-1017's
   own live kill-a-session confirmation earlier this session: kill one
   role's session on a scratch pack, drive a single-role repair, confirm
   the role returns, every other session survives, handoffd stays alive,
   and the socket id is unchanged.

6. **BL-532 sibling check**: `qa-sibling-check.js status --ticket BL-1018`
   returned `VERIFY BL-1018` (exit 0) - no open deferral.

7. **No orphaned test/mutation processes** before or after this pass (the
   `bb`/supervisor processes visible in `pgrep` are the live swarm's own
   production daemons in the master checkout, not test fixtures - left
   untouched).

## Verdict

PASS. Landing on `main`.
