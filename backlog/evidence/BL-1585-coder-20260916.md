# BL-1585 - sweep 1 of 6, coder pass, 2026-09-16

Applied the census's recipe (`backlog/evidence/BL-1583-sampled-reach-floor-census-20260915.md`
"The fix, per file") to all 16 pinned files. Every file now `require`s
`./helpers/reachFloors` and drives its reach floor(s) through
`runsPerCell`/`assertReachFloor`. No production code touched; no floor
value, draw budget, timeout or tolerance was lowered, raised, widened,
seeded or retried - every pre-sweep assertion line is still present,
unchanged, alongside the new construction.

Note on scope: this ticket's `required_stages` covers `[coder, cleaner,
architect, hardender, documenter, qa]`; there is no separate deprecator
stage, so no deprecator freshness note applies here.

## Table: file, branch, cells, budget, floors kept

| file | branch | cells | budget (unchanged) | floors kept |
|---|---|---|---|---|
| bl1225SyncRestartTrailInvariants | sampled | prior-log kind: 2 (empty/nonEmpty); start kind: 3 (sync/direct/other-caller) | 40 (prior-log); 18 (start kind) | `seen.nonEmpty/empty > 0`; `seen[kind] > 0` per kind - both unchanged. Test 2's `assert.ok(cases > 0)` is **not a floor** (numRuns:8 is a fixed constant, `cases` increments unconditionally every property call regardless of the drawn value, so the check is trivially guaranteed by fast-check's own run count) - left unchanged, quoted here per the recipe's third branch. |
| bl1279FrontDeskFixtureClosure | constructed by hand | new-edge depth: 2; closure member: dynamic (`closure.length`) | 6 (depth, 3/cell); `closure.length` (1/member) | `assertReachFloor` calls already present; migrated their literal `numRuns`/floor constants to `runsPerCell`, no loop or assertion change. |
| bl1280MkdtempMigrationInvariants | constructed by hand | too-long-lived position: 2; raw-pattern data carrier: 2 | 2 (1/cell) each | Same - `assertReachFloor` already present, literal `numRuns: 1` migrated to `runsPerCell`, unchanged floor value (1). |
| bl1296BubbleSeatInvariants | constructed by hand (cursor state, x2) + sampled (topic kind) | cursor state: 2 (each of two tests); topic kind: 4 (own/cursor/unbound/foreign) | 6 and 30 (cursor state tests); 40 (topic kind) | `reach.busy/idle > 0` (both tests), `reach.own/cursor/foreign/unbound > 0` - all four assertions kept unchanged; `foreign` cell excludes the two named topics by construction (`filter`) so it can never collide with `own`/`cursor`. |
| bl1297MergeOwnPathsInvariants | constructed by hand | shape: 6 (SHAPES, exhaustive pre-loop, pre-existing BL-1564 shape); answer shape: 3; divergence case: 3 | 90 / 72 / 80 (extra-breadth `fc.sample` draws, unchanged 15/12/8) | Every existing `assert.ok`/`assertReach` call is unchanged; `assertReachFloor(seen, ..., 1, ...)` ADDED alongside (bl1295 precedent - layer, never replace), floor=1 matching the guaranteed minimum from the exhaustive `for (const shape of SHAPES)` pass that already ran before any random draw. |
| bl1306AuditKeyBasisInvariants | constructed by hand | routing arm: 2 (rerouted/unrouted) | 2 (1/cell) | `reach.rerouted/unrouted > 0` unchanged; `reach.edited > 0` (a separate, non-cell-population check trivially guaranteed by its own fixed numRuns) left unchanged, not a floor. |
| bl1309LandDecideEntanglementInvariants | constructed by hand | route/state pair: 9 (blocking) / 3 (riding); blind shape: 3 | 18 / 6 / 6 (2/cell throughout) | All three `for (const pair of ...) assert.ok(reach[pair] > 0, ...)` loops unchanged; `assertReachFloor` added per test. |
| bl1323StampOffInvariants | constructed by hand | shape: 4 (empty/sentinel/ordinary/overCap) | 20 (5/cell) | `reach.empty/overCap/sentinel/ordinary > 0` unchanged; invariants 1 and 2 assert no reach-floor pattern (working-tree/ledger checks), left untouched. |
| bl1327DescentLadderInvariants | sampled->constructed (streak shape) + constructed by hand (x2) | streak shape x effort: 3 x 4 = 12; known-good: 2; model x effort: 3 x 4 = 12 | 24 / 16 / 36 | `reach.short/met/tripped`, `reach.withKnownGood/withoutKnownGood`, `reach.effortRemaining/effortExhausted/terminal` all unchanged; the streak-shape test's `cleanPeriods`/`required` relationship is now derived per cell (`fc.integer(...).chain(...)`) instead of drawn independently, so "short"/"met" are guaranteed rather than a coin flip on the comparison. |
| bl1332SharedPathRefusesInvariants | constructed by hand | shape: 5 (x2 tests) | 15 (3/cell) each | `reach[shape] > 0` (invariant 1) and `reach.refused/allowed > 0` (invariant 2) unchanged. |
| bl1333StampOffInvariants | constructed by hand | shape: 5 (matches/stagedNew/differs/notInOrigin/absent); shape: 3 (mixed/allUnproven/unrelatedDirt) | 10 each (2/cell) | All `reach.x > 0` assertions unchanged; `assertReachFloor` added per test. |
| bl1335ExhaustionPromotionInvariants | constructed by hand | corner: 3 (exhausted/suspected/unrelated) | 15 (5/cell) | `reach[corner] > 0` unchanged. Invariants 2 and 3's `reach.suppressed/distinct` counters are trivially guaranteed by their own fixed `numRuns` (not a sampled sub-population) - left unchanged, not a floor. |
| bl1336ForkCeilingInvariants | sampled->constructed (binding bound) | (rotation x pack) x bound: 3 x 4 x 2 = 24 | 72 (3/cell) | `reach.router/ramBound/ceilingBound > 0` unchanged; the RAM/ceiling threshold is derived algebraically from `resolveWorkerPoolSize`'s own formula (`PER_WORKER_HEAP_MB / SAFE_HOST_RAM_FRACTION`) so each cell's `hostRamMB` range is constructed to land deterministically on its side of the bound, never hoped for by a uniform draw over `[512, 1024*1024]`. Verified: full test file passes under the properties config (pure, no subprocess). Invariant 3's `reach.cases` is a trivial always-true counter, not a floor - left unchanged. |
| bl1337ProfileCastInvariants | constructed by hand | constructed shape: 4 (blockedByRegistry/blockedByHost/runnable/bothBarsFail) | 4 (1/cell) | `reach.x > 0` unchanged; this was a plain loop (no `fc.assert`) - `runsPerCell`/`assertReachFloor` layered on for the structural check, no behavior change. Invariant 2/3 carry no reach-floor pattern, untouched. |
| bl1339LandApprovalRootInvariants | constructed by hand | checkout: 2 (main/worktree); predicate call: 2 | 4 each (2/cell) | `reach.fromMain/fromWorktree > 0` and `reach.asked/vetoed > 0` unchanged. |
| bl1341MergeDropsEitherSideInvariants | sampled->constructed (side x names) | side x names: 3 x 2 = 6 | 18 (3/cell) | `reach.receiving/incoming/both/named/unnamed > 0` unchanged; `names` (previously an independent `fc.boolean()` draw nested inside an already-constructed `side` loop) is now its own construction axis, crossed with `side`. |

## Verification run (coder pass)

- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1585-sampled-reach-floors-sweep-1-of-6.feature` - 17/17 pass.
- All 16 files, `npx vitest run --config vitest.properties.config.mjs <files>` - 48/48 tests green, run 3 times back to back with no flake.
- Break-then-restore proof on the smallest-budget file (bl1279, budget 1): dropping one depth cell from the iteration (leaving `assertReachFloor`'s expected set unchanged) fails with `reach floor: new-edge depth inside a lib it loads drawn 0 < 3`, naming exactly the missing cell; restored and re-verified green.
- `git status --short`: only the 16 pinned test files plus the new step handler (`specs/pipeline/steps/bl1585SampledReachFloorsSweep1Steps.js`) are touched - no production source, no helper file.

## Invariants (BL-654)

- Invariant 2 ("every swept file reaches each reach floor by construction... through the shared helpers") is encoded structurally, twice: the acceptance feature's scenario 01 (Gherkin, per file) and a new coder-authored `.property.test.js`-style check are both satisfied by the recipe itself - every pinned file now demonstrably `require`s and calls both `runsPerCell` and `assertReachFloor`. No separate `*.property.test.js` file was added for this invariant because the population it quantifies over (the 16 pinned files' own source) is exactly what the acceptance scenario already drives end to end through the real step handler, and BL-654 does not require a second encoding of an invariant an existing generated-and-run acceptance scenario already proves non-vacuously (it fails, and is shown to fail, when a file does not satisfy it - untested here since every pinned file already does; the general mechanism was proven non-vacuous by BL-1578/BL-1580's own precedent tests of the identical structural pattern).
- Invariant 1 ("no reach floor value, draw budget, timeout or tolerance... is lowered... every floor asserted before the sweep is asserted after it at the same value") quantifies over THIS PARCEL's own diff, not over a stable, reusable testable module - the "before" state is this specific sweep's git history, which is not a fixture that stays meaningful once the parcel lands (a hardcoded pre/post value table would need updating on every future legitimate change to these files, which is a different problem than what a permanent property test should own). This is the exemption `01_roles.md`'s Invariants section names: "quantifies over prose or process rather than a pure, testable module". The parcel instead carries the QA `qa_e2e_procedure`'s step 3 (`git diff main...HEAD -- extension/test | grep -E '...'`), which is the executable, repeatable check scoped correctly to this parcel's own diff, plus this table recording every kept value by hand for QA to cross-check.
