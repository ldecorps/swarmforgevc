# BL-1376 — cleaner pass, 2026-09-03

One fix made and verified; otherwise clean.

## Fixed

- `swarmforge/scripts/expedite_cli.bb::run-branch-facts` — the `let` binding
  `counted` and the trailing `if` were under-indented by two spaces relative
  to their own `let`, and the `rev-list` args line was misaligned with its
  `sh` map. Functionally correct (Babashka does not care about whitespace)
  but the visual nesting no longer matched the actual form, which is exactly
  the kind of drift that makes the next reader misjudge what is inside the
  `if-not` vs. the outer `let`. Reindented to match the surrounding style;
  no logic changed.

## Checks run (all pass, before and after the reindent)

| # | Check | Result |
|---|-------|--------|
| C1 | `bb swarmforge/scripts/test/expedite_lib_test_runner.bb` | ALL PASS |
| C2 | `bash swarmforge/scripts/test/test_bl1376_expedite_branch_handover.sh` | ALL PASS (32 checks) |
| C3 | `bb swarmforge/scripts/test/bl1376_expedite_branch_handover_property_runner.bb` | ALL PROPERTIES HOLD (500 runs) |
| C4 | `bash swarmforge/scripts/test/test_expedite_cli.sh` | ALL PASS — no regression in the existing handover checks |
| C5 | `bb swarmforge/scripts/test/expedite_lib_property_runner.bb` | ALL PROPERTIES HOLD |
| C6 | `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1376-*.feature` | 7/7 pass |
| C7 | `node extension/out/tools/mutation-site-count.js specs/pipeline/steps/bl1376ExpediteBranchHandoverSteps.js` | 151 sites, advisory `over` |
| C8 | `jscpd` over the changed `.bb`/`.js` sources, min-lines 15 | 0 clones |
| C9 | `suite-manifest.tsv` / `index.js` registration | new suite files and acceptance handler registered |

## Mutation-site advisory (BL-485) — no split

`bl1376ExpediteBranchHandoverSteps.js` is 168 lines / 151 sites, over the
100-site threshold. Same reasoning as BL-1360's sibling file: one feature's
step handlers, well under the 500-1000+ line norm of the other files in
`specs/pipeline/steps/`, with no natural seam a split would improve rather
than fragment. Kept whole.

## Architecture review

`run-branch-name`/`run-branch-owner`/`branch-outstanding` in `expedite_lib.bb`
stay pure (invariant 3's single-definition intent for the branch name is
honoured — `ensure-worktree!` now calls the same function rather than
re-literalling `"expedite/" ticket`); `run-branch-facts` in `expedite_cli.bb`
is the one place that shells out (`git rev-parse`/`git rev-list`), matching
the pure-core/thin-IO-adapter split already used elsewhere in this file. No
merge, push, or checkout was added anywhere in the diff (grepped, matching
qa_e2e_procedure step 6).

## Not addressed here — flagged for the coordinator, not this ticket

The coder's own evidence (`BL-1376-coder-20260903.md`, "Surfaced, not swept")
already reports that `backlog/active/`'s BL-1360/BL-1296/BL-1309/BL-1356/
BL-1359 sweep to `backlog/hold/` (the BL-1375 expeditor park) needs a human
or the coordinator to say what it means for parcels already in flight. My
own prior merge-up pass (`c967be2d6b`) independently confirmed the same
park via `origin/main`'s `2b69c4a47e`. Nothing new to add; not this
ticket's fix to make.

## No functional change beyond the reindent

Forwarding.

By cleaner.
