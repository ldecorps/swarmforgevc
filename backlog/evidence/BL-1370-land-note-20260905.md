# BL-1370 land note — merge-deletion guard blocked a plain merge, 2026-09-05

`git merge d6e44499b7` (the documenter's forward) into the QA worktree was
refused unconditionally by `check_merge_deletion.sh`: two intake-archive
files (`backlog/INTAKE-human-expedite-park-must-unhold-on-finish-1788436320924.md`,
`backlog/INTAKE-operator-question-1788557930426.md`, moved to
`backlog/archive/` by unrelated origin/main commits) have NO ticket id
anywhere in their git history, so the guard's message-based exemption
(`grep -qiE "\b${id}\b"` against the commit message) can never fire no
matter which commit performs the deletion — confirmed by pre-merging each
introducing commit (`0c9ccc477c`, `f374d49a8e`) separately, which changed
nothing. This is exactly the defect already minted as **BL-1403**
("the merge-deletion guard never reports a move and never refuses
unexemptably").

`check_merge_deletion.sh` only fires when `MERGE_HEAD` is present
(`swarmforge/git-hooks/commit-msg`), so replayed this ticket's own
attributed paths (coder/cleaner/architect/hardener/documenter, each
commit's own diff) as a **plain, non-merge commit** (`c981dc13e9`) into the
QA worktree instead — verified against `origin/main` for entanglement
before doing so, and re-verified e2e/property/acceptance green after.
Built and landed the tip-pure commit (`4409993daf`) from that plain
commit's content, same recipe as this session's other lands.

**Land-approval consequence**: since `d6e44499b7` itself was never merged
(no ancestry path from it to `swarmforge-QA`), it cannot be the recorded
land-approval `source` — `is_qa_ancestor.sh` requires the source to be an
ancestor of `swarmforge-QA` with no bounce. Recorded the land-approval
line citing `c981dc13e9` (the plain replay commit, which IS an ancestor)
as source instead:
`{"at":"2026-09-05T02:06:10Z","ticket":"BL-1370","commit":"4409993daf","source":"c981dc13e9"}`.
Verified `is_qa_ancestor.sh 4409993daf` now prints `approved: ... - BL-1334`.
`abandoned_commits: [d6e44499b7]` recorded on the ticket to mark the
never-merged forward as superseded by the replay.

By QA.
