# BL-1380 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## A stale-build false alarm, not a defect

`pausedPagerBridge.test.js`'s primary scenario ("Expedite refuses (409) a
ticket that poses an unanswered choice") failed on first run (200 instead
of 409). Investigated rather than assumed broken: `readRulingOptions`'s
underlying `parseBacklogYaml` parses the fixture's `ruling_options:` block
correctly in isolation (checked directly against `out/panel/backlogReader.js`),
and `classifyApprovalRulingRequirement`'s logic is straightforwardly
correct for the fixture (empty chosen ruling + 2 declared options ->
`ruling-required`). Re-ran `npm run compile` (the merge picked up
`bridgeServer.ts` changes I had not rebuilt since a prior ticket's pass)
and the full suite passed 25/25 including this scenario. Confirmed as a
stale-`out/` artifact of my own review sequencing, not a coder defect.

## What was checked

- `pausedPagerBridge.test.js` — re-ran after a fresh compile: 25/25 PASS,
  including the 409-refusal scenario (gate `human_ruling`, options
  present, nothing written) and the three "unchanged today" scenarios
  (already-answered, no-options, never-pending).
- `bl1380ExpediteNeverAnswersUnshownQuestion.property.test.js` — re-ran:
  2/2 properties hold (P1+P2: unanswered choice refused with nothing
  written; P3: a ticket with nothing to choose expedites unchanged).
- Architecture: `classifyExpediteRulingRefusal` reuses BL-1367's own
  `classifyApprovalRulingRequirement`/`readRulingOptions`/
  `readRecordedRuling` rather than growing a second copy of the ruling
  rules — confirmed by reading the diff; the fix's own commit message
  states this explicitly and the diff bears it out. `jscpd` over
  `bridgeServer.ts` (2500 lines) and the new step handler: 0 clones.
- The decision is computed and checked BEFORE `recordApprovalReply` runs
  (read the diff directly) — the exact ordering the defect was about.
- `mutation-site-count.js` on the new step handler: 145 sites (`over`
  100). Reviewed and declined to split, same reasoning as every other
  ticket in this batch: one cohesive single-feature handler.
- TypeScript compiles clean; the handler discovers via BL-1371's registry.

## Noted, not acted on

Three unrelated shell-script mode changes rode in on this merge
(`bl1379ParkReversalCli.sh`, `bl1381ShiftScheduleCli.sh`,
`test_handoffd_expedite_park_reversal_wiring.sh`: 755→644). Traced to
QA's own tip-pure hand-built replay commit for BL-1379
(`108517e9f7`), predating any of my own merges — not something I or this
ticket introduced. Functionally inert: every call site invokes these via
`bash <path>` explicitly (checked `run_bb_suite.sh` and the two step
handlers that shell out to the CLI fixtures), never `./<path>` directly.
Not fixed here — restoring the bit would create a diff fighting what QA's
land already converged on, for zero functional gain.

No bounce history on BL-1380 (first pass, `main`/`origin/main` agree).
Forwarding unchanged to architect.

By cleaner.
