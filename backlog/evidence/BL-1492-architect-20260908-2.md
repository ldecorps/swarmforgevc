# BL-1492 — architect review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

Re-review after coder's D1 fix (`a21f9b2233`, remediation for
`backlog/evidence/BL-1492-architect-20260908.md`) and cleaner's pass-through
(`09516e67f8`, evidence `BL-1492-cleaner-20260908-2.md`, NONE).

- **D1 fix verified**: `decide-response`'s filter is now
  `(and (number? at) (<= budget-window-ms (- now-ms at)))` — an entry is
  excluded from the recent count only when `:at` is a real number AND
  genuinely older than the window; a malformed/non-numeric or future `:at`
  now counts AS recent (fails toward `:halt`), matching the docstring.
  Hand repro confirmed: budget-count=2 exhausted history with in-window
  `:at` → `:halt`; same count with future `:at` (clock-skew shape) → `:halt`
  (previously `:restart` — the D1 bug). `handoffd_supervisor_restart_budget_test_runner.bb`
  ALL TESTS PASS, including the new mixed-entry case and the corrected
  malformed/future assertions.
- **P4 added** to `bl1492_restart_in_place_property_runner.bb` — a
  budget-exhausting history with a random mix of valid/malformed/future
  `:at` always reads `:halt`; this closes exactly the generator-reach gap
  named in the D1 bounce (P1/P2/P3 never put a malformed/future entry
  inside a budget-exhausting history). Ran green: `ALL PROPERTIES HOLD`
  (150 runs each, `p4-kinds=#{:valid :future :malformed}` confirming the
  generator actually reaches all three cases).
- **dependency-gate.js**: ran against the one changed JS file
  (`specs/pipeline/steps/bl1492RestartInPlaceSteps.js`) — PASSED, no
  forbidden edges.
- **co-change-report.js**: ran against all changed files — the only
  SUSPECTED COUPLING flagged is within-parcel (the BL-1492 files
  co-changing with each other across the coder-fix and cleaner-evidence
  commits); nothing new or cross-ticket.
- **Acceptance**: `specs/features/BL-1492-*.feature` 5/5 green via
  `node specs/pipeline/cli.js`.
- **test_handoffd_supervisor.sh**: re-ran under `ZDOTDIR=<empty dir>`
  isolation — cases 01/03/04 PASS (budget pinned to 0 for this suite, per
  coder's added comment); case 02 fails identically to the prior pass and
  to unmodified `main` — pre-existing, already registered as BL-1498, not
  this parcel's defect.
- **Everything else** (two-layer boundary, secrets, required_wiring,
  invariants existence/non-vacuity) unchanged from the prior pass — the
  coder's commit message states it restored that content unchanged and
  layered only the D1 fix on top; confirmed by diff.
- **Correctness**: no defect. D1 is resolved and the fix's own generator
  gap is closed with a new property.
