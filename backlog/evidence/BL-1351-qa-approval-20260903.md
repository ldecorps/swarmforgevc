# BL-1351 — QA approval (20260903)

Received: documenter commit `06d2189eec` (review-pass evidence, no doc
defect found), forwarding architect (clean sweep, plus the required
architectural ruling on the JSON `/state` routes), hardener, and cleaner
passes. Real feature work implementing the human's ruled option 1: keep
every backlog folder on the `/events` stream, narrow each item to the
fields consumers actually read.

## Review inventory (complete pass, Article 4.4)

- Architectural ruling (required by the ticket's own `description`, not a
  side effect): the JSON `/state` routes keep full fidelity;
  `stateForRoute`/`isStateRoute` still call `buildBridgeState` directly.
  The projection applies only at the two `/events` stream producers.
  Confirmed by reading `bridgeServer.ts` — `stateForRoute` at line ~1893
  still computes off raw `buildBridgeState`, unchanged.
- Read `streamSnapshot.ts` by hand: `projectBacklogItemForStream` returns
  exactly `{id, title}` (`STREAM_BACKLOG_ITEM_FIELDS`), matching the
  documented consumer sweep (holisticUiHtml.ts reads only id/title off
  the stream; bubbleHostUiHtml.ts and the front-desk relay read no
  backlog item fields off the stream at all — the relay reads from disk).
  Every folder the reader returns is kept (option 1 narrows items, never
  folders).
- Invariant 2 (single producer): both `resolveEventsSnapshot` (connect)
  and `broadcastSnapshotIfChanged` (poll-loop rebroadcast) in
  `bridgeServer.ts` call `buildStreamSnapshot` — the one function that
  produces the snapshot string — so the two cannot drift apart by
  construction. Confirmed by grep and by reading both call sites.
- **First acceptance run showed a stale build** (`extension/out/` predated
  the source — `streamSnapshot.js` did not exist in `out/` at all, and
  `bridgeServer.js`'s mtime was older than `bridgeServer.ts`'s), producing
  a false 10.9 MB / item-bodies-present failure on all 4 scenarios. Ran
  `npm run compile` and re-ran: 4/4 clean. Not a code defect — a leftover
  compiled tree in this worktree from before this ticket's own changes
  landed; noted here so the false-negative isn't mistaken for evidence
  later.
- Acceptance
  (`specs/features/BL-1351-event-snapshot-carries-only-what-consumers-read.feature`),
  after compiling: 4/4, including the 512000-byte budget scenario (both
  connect and rebroadcast triggers) and the same-shape-across-producers
  scenario.
- Unit test (`extension/test/streamSnapshot.test.js`): 6/6.
- Property test (`bl1351StreamSnapshotInvariants.property.test.js`): 2/2,
  covering both declared invariants (invariant 1: nothing a consumer
  reads was dropped; invariant 2: both producers emit the same shape).
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated `projectBacklogItemForStream` to return the item
  unprojected (`return item as StreamBacklogItem`, restoring the full
  body) — all 4 acceptance scenarios failed exactly as expected (budget
  blown, item bodies back). Restored, recompiled, re-ran 4/4 clean.
- Constraint check: the 512000-byte budget in the feature is unchanged
  from the ticket's own text (not tuned to the implementation); the
  fixture builds its 1223 items in a temp dir removed in a `finally`,
  swept by prefix before the run, and does not read the real `backlog/`
  (confirmed by reading `bl1351StreamSnapshotFixture.js`).
- Compile (`extension`, `npm run compile`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population (includes the three known standing guards:
  `tempDirTrapGuard`, `socketFixtureShortRootGuard`, `liveRepoDerivationGuard`).
  None touch this parcel's files.
- Property suite (`npm run test:properties`), run sequentially after the
  unit suite: 25 files / 14 tests red. 24/25 match the standing-debt
  population exactly; one new
  (`bl1074PostCloseRefileDuration.property.test.js`) — investigated: ran
  in isolation, 3/3 clean, confirming a load flake under the full
  concurrent suite (this host had a second worktree's own
  `test:properties` run active concurrently), not a regression from this
  parcel. No BL-1351 file present in the failing set.
- `qa_e2e_procedure` step 4 (JSON `/state` routes unaffected): confirmed
  by the architect's ruling + code read above; the routes were not
  independently curled against a live bridge in this pass (no running
  bridge instance on this host at review time), relying instead on the
  acceptance feature's own route-fidelity scenario and the static read —
  acceptable since the acceptance suite drives a REAL bridge server per
  the fixture's own design.
- Orphaned test processes: none of mine after the runs. `vitest`
  processes were alive mid-check but confirmed (via `/proc/<pid>/cwd`) to
  belong to the coder worktree's own concurrent `test:properties` run —
  left untouched.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
