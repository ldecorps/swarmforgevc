# BL-1414 documenter review pass — 2026-09-05

Reviewed the hardened commit (`2ce29711f7`) and the coder/cleaner/architect/
hardener trail for BL-1414 (a repeating freshness violation is announced
once then digested).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1414-a-repeating-freshness-violation-is-announced-once-then-digested.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1414FreshnessAnnounceDigestSteps.js`; the
  `daemon_log_freshness_check.sh::announce_transition_only` call site
  required by the first wiring line is present (three call sites in
  `daemon_log_freshness_check.sh`: the fresh-tick recovery check and both
  the cool-off/escalate and fresh-restart violation paths).
- Read `swarmforge/scripts/freshness_announce_lib.sh`: confirmed invariant
  1 (suppression is announce-only — the function only ever decides
  announce/suppress/digest/recovered/none, never touches the incident
  log, which the caller appends unconditionally before calling it per the
  ticket's own "append BEFORE any announce" direction), invariant 2 (the
  `fresh` branch's `recovered`/`none` decision and the violation branch's
  `announce` decision for a first-ever state file are both unconditional,
  before any window arithmetic runs), and invariant 3 (state lives in a
  `.swarmforge/daemon/freshness-announce/<daemon>__<reason>.state` file,
  never process memory).
- Operator-visible behavior change (a persisting `FRESHNESS_VIOLATION` no
  longer announces on every 2-minute tick — the first tick of a
  transition announces, further ticks inside the digest window (default
  1800s, `FRESHNESS_ANNOUNCE_DIGEST_SECS`) are recorded but not
  announced, the window's elapse announces one digest naming the
  suppressed-tick count, and recovery announces once) is documented in
  `docs/how-to/BL-675-daemon-log-freshness-watchdog.md` — the existing
  freshness-watchdog how-to — as a new "Announce is a transition, not a
  tick (BL-1414)" section (matching the doc's existing per-ticket section
  pattern), a new env-seam table row, and an added
  feature/property-runner reference in the Verify section's acceptance
  list.
- `docs/index.md`'s one-line summary for that how-to extended with a
  BL-1414 clause in the same running-list style the entry already uses.
- No "Last Updated" freshness field in the edited doc — no date bump
  owed.
- No registered diagram depicts this mechanism's internal decision logic
  (an announce-suppression decision added to an existing checker, not a
  new component, boundary, or pipeline-topology change) — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
