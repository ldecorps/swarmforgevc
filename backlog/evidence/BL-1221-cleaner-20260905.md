# BL-1221 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Confirmed the coder's recount (15 real callers, not the ticket's stale
  16): `grep -rl "checkOrphanedAuthoredDocs" extension/test/` returns
  exactly 15 files, matching the diff exactly. The ticket's own count was
  measured by grepping the bare string `landPilotedTicket`, which also
  matches two files that only mention the name in passing — correctly
  excluded, same "stale-measurement drift" class this session's other
  tickets (BL-1289, BL-1428) each hit.
- Confirmed production is untouched: `git diff --stat` on both
  `pilotAcceptanceGate.ts` and `pilot-acceptance-gate.ts` is empty; the
  deps member is still required (no `?`), the call site still
  unconditional (no `?.()`, no truthiness guard).
- Each of the 15 edits is a single added line
  (`checkOrphanedAuthoredDocs: () => ({ checked: true, docsTouched: false }),`)
  at an existing stub construction site — no shared factory introduced,
  correctly matching the ticket's own "only prefer a shared factory when
  it does not distort what individual tests assert" guidance, since none
  of the 15 files had a pre-existing shared helper to extend.
- Ran the full unit lane: 6 files failed / 598 passed (604) — none of the
  9 `.test.js` files this ticket touched are among the 6 remaining
  failures (backendSwitch, constitutionDocCitations,
  operatorRuntimeBbFixtureClosure, pricingTable→BL-1212,
  telegramClient/telegramCursorOperatorExec→BL-1263), matching the
  coder's claim exactly.
- Ran the 6 `.property.test.js` files this ticket touched: 3 pass fully
  (15/15), 3 fail to COLLECT with "No test suite found" — confirmed this
  is BL-1220's own unrelated node:test defect (the ticket's own qa_e2e
  item 3 names this as expected), and none of the 6 shows the
  `deps.checkOrphanedAuthoredDocs is not a function` message.
- Ran the acceptance feature: 3/3 pass.
- No duplication concern: each stub gets one additive line following the
  same pre-existing pattern every other `check*` member in that stub
  already uses.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `grep -rl checkOrphanedAuthoredDocs extension/test/` | exactly 15 files |
| `git diff --stat pilotAcceptanceGate.ts pilot-acceptance-gate.ts` | empty (production untouched) |
| Full unit lane (`npx vitest run --config vitest.config.mjs`) | 6 failed / 598 passed — none of the 9 touched `.test.js` files remain failing |
| The 6 touched `.property.test.js` files | 3 pass (15/15), 3 fail-to-collect on BL-1220's unrelated defect, none shows the missing-function message |
| `node specs/pipeline/cli.js specs/features/BL-1221-...feature` | 3/3 |
| `bl1221PilotGateDepsStubsSteps.js::registerSteps` exists | yes (required_wiring) |

No fix required. Forwarding unchanged to architect.
