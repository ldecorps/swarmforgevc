# BL-1541 — cleaner review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Merged coder 0bc64cc1e3 (BL-1541 rebuild: scenario 01 retired, bl982
  invariant 2 projection fix, scenario 03 shared send-through-audit! helper).
- Ran all five runners individually: bl983 (16 draws), bl992 (100 draws),
  bl951 (12 sampled hops) green on the merged commit as-is. bl982
  (multi-seat identity) and bl991 (binding stages) were re-checked because
  an earlier combined run (with `tail`-piped exit codes, a harness bug on
  my side, not the code) looked ambiguous.
- Found bl982's `check-compose!` (invariant 1, untouched by this parcel)
  red in THIS worktree: `generate_dormant_role_launch_artifacts`, run with
  no MODEL_FACTORY_STATE_DIR override, resolved each seat's model through
  this worktree's real `.swarmforge/model-factory/assignment.json` (a live
  overlay pinning coder/cleaner/architect/... to claude-sonnet-5,
  `agent: claude`), which unconditionally overrides the fixture pack's own
  drawn model whenever it differs (3 of 4 pack models do). Environment-
  dependent: the coder's own worktree carries no such overlay file, so
  their verification run never hit it — not a regression from this parcel,
  not a defect in the properties themselves (they kept their meaning; the
  assertion is what the ticket already states).
- Fixed by pinning `MODEL_FACTORY_STATE_DIR`/`MODEL_STEWARD_STATE_DIR` in
  `check-compose!`'s `sh` call to nonexistent fixture-root paths, so
  `resolve_claude_model_for_index` always falls through to the pack's own
  `--model` (commit 74fc5cff18, same file this ticket already owns for the
  invariant-2 change — cleanup-domain fix, not a bounce).
- Reran bl982 twice post-fix (100 draws each): ALL PROPERTIES HOLD both
  times. Reran bl991: ALL 80 SENDS PASSED.
- Acceptance: `run_acceptance.sh` on the BL-1541 feature — 7/7 pass.
- `git diff origin/main -- swarmforge/scripts/swarm_handoff.bb
  swarmforge/scripts/handoff_lib.bb` empty — no production script touched,
  constraint held.
- Mutation-site-count: not applicable — the only non-bb changed file
  (`bl1541GitHandoffRunnersAnswerAuditSteps.js`) is a `specs/pipeline/steps/`
  acceptance step handler, outside Stryker's `out/**/*.js` mutate scope; the
  five bb runners have no mutation/CRAP/DRY tooling wired (BL-472).
