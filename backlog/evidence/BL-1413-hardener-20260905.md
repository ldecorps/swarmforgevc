# BL-1413 — hardener pass, 2026-09-05

Clean sweep. No code or test changes needed; forwarding the received commit
unchanged.

## Verification re-run

| check | result |
|---|---|
| `bash swarmforge/scripts/test/test_daemon_log_freshness.sh` | all 15 BL-1413 assertions pass; the same 4 pre-existing unrelated reds the coder/architect documented (BL-796-01/02/03 nvm/node PATH, `fleet-telegram: default announce invoked curl`) reproduced here too, none touching `heartbeat_age_secs` |
| `bb swarmforge/scripts/test/bl1413_freshness_nul_byte_property_runner.bb` | 40/40 runs, ALL PROPERTIES HOLD |
| acceptance `BL-1413-...feature` | 4/4 |

## No mutation tool for `.sh`/`.bb` — hand-authored surgical sweep (engineering.prompt fallback)

`daemon_log_freshness_check.sh` has no Stryker/CRAP/DRY wiring (shell, not
TypeScript; `mutation_cost: low`). Hand-mutated `heartbeat_age_secs` one edit
at a time, re-ran the BL-1413-scoped shell scenarios, then restored the
original file from a backup copy before moving to the next mutant (never two
mutants live at once, file restored to a verified-clean diff between each).

| mutant | change | result |
|---|---|---|
| A | `grep -aE` → `grep -E` (the ticket's own root-cause revert) | **KILLED** — BL-1413-01/02/03/04 all fail (wrong age, false restart/announce) |
| B | drop `LC_ALL=C` (keep `-a`) | **SURVIVED** — see below |
| C | reverse the newest-to-oldest walk to oldest-to-newest (`i=$line_count` descending → `i=1` ascending) | **KILLED** — BL-1413-01 fails (wrong age: picks the oldest parseable line instead of the newest) |
| D | drop the `epoch -ne 0` guard (accept a `0` epoch as parsed) | **KILLED** — BL-1413-03b fails (torn-newest fallback picks the wrong line) |

**Mutant B is not a gap — it is untestable in this environment, not
equivalent-by-code.** `LC_ALL=C`'s stated purpose (ticket "How" section) is
byte-wise matching "regardless of the invoking locale" - i.e. it guards
against a DIFFERENT ambient locale than this test harness runs under
mis-happing an invalid multi-byte sequence around the NUL/crash byte, on top
of `-a`'s own binary-file bypass. `locale -a` on this host lists only `C`,
`C.utf8`, `POSIX` — there is no second, genuinely different UTF-8 locale
available to construct a fixture that would make grep behave differently
with vs. without the override, and the ambient `LANG=C.UTF-8` here is already
close enough to `LC_ALL=C` that removing it produces byte-identical grep
output for every existing NUL-only fixture. This is the same class as the
`LC_ALL=en_US.UTF-8`-requiring cases the engineering rules elsewhere warn are
host-dependent, just discovered in the opposite direction (defensive code
this host's locale roster cannot exercise, not a false survivor to explain
away). Recorded here rather than forced with a synthetic locale nobody would
trust; a host that ships `en_US.UTF-8` (or any locale with real multi-byte
collation) could reproduce the discriminating fixture directly.

Mutants A, C and D landing on exactly the scenarios their own doc comments
name (BL-1413-01/04 for the text-mode read, BL-1413-01 for walk direction,
BL-1413-03b for the fallback-parse guard) confirms the coder's own
non-vacuity claims in `BL-1413-coder-20260905.md` rather than merely
repeating them.

## CRAP / DRY

Not applicable — no TypeScript touched (confirmed via `git diff --stat`
against `285fbf64f3..HEAD` for this ticket's own commits: only `.sh`, `.bb`,
step-handler `.js`, and fixture files).

## No violations found

By hardener.
