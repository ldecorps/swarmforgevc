# BL-1700 — documenter review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

Second documenter pass on the rebuilt parcel: the first pass (evidence
`BL-1700-documenter-20260926.md`, commit 1338adac75) reviewed the parcel
merge a547104675, which QA later bounced (D1-D3, `BL-1700-QA-20260926.md`)
back to coder for the cap-flag NPE, the mis-scoped turn cap, and the
missing resolved-model-id/turns/llmHistoryPath scorecard fields. The
coder's fix (commit 2f35451608) rewrote `model_steward_coder_probe_lib.bb`
AND `docs/how-to/BL-1700-model-steward-coder-probe.md` together, so the
prior evidence's "doc already accurate" verdict does not carry over
unread - re-verified from scratch against the fixed code, not trusted
from the earlier pass:

- `default-max-ticks` 400 / `default-wall-clock-seconds` 900 are coalesced
  in the single place `run-fixture!` names (line 68-69, 367-369) - the D1
  NPE (an `:or` never firing because callers pass the keys present-but-nil)
  cannot recur; the how-to's "Omitting a cap flag falls back to its
  documented default, never an error" matches.
- `safety-ticks` is sized against `wall-clock-seconds` only (line 411,
  comment names D2 explicitly) - the how-to's "`--max-ticks` ... counts
  real MODEL TURNS ... never the harness's own internal polling ticks" and
  "the ONE cap that bounds real elapsed time" both match.
- `resolve-model-id` GETs `<endpoint>/models` and the scorecard/summary
  carry the resolved id (line 71, 454-475); every scorecard also carries
  `:turns` and `:llmHistoryPath` (an absolute path outside the throwaway
  root, line 377-378, 425) - the how-to's "What a scorecard means" section
  matches D3's remediation.
- Every outcome string in the how-to's table (`handed off`, `turn cap`,
  `wall-clock cap` in the probe lib; `spec changed`, `acceptance still
  failing`, `no model commit`, `edited outside its files`, `merge
  conflict` in `local_parcel_driver_lib.bb`'s `green-gate-decision`)
  greps verbatim against both files.
- `docs/index.md`'s BL-1700 entry (line 86) still describes the shipped
  shape; no diagram trigger, matching the ticket's own direction.

Checks run this pass:
- `node specs/pipeline/cli.js` on the BL-1700 feature: 7/7.
- Harness `selftest!`: 5/5 fixtures `:red-ok true :green-ok true`.
- `bb swarmforge/scripts/test/model_steward_coder_probe_lib_test_runner.bb`:
  ALL PASS (covers D1's no-cap-flag and wall-clock-seconds-only cases,
  D2's turn-cap-counts-model-turns case).
- `npm run compile` (extension/): clean.

By documenter.
