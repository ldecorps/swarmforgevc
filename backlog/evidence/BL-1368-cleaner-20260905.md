# BL-1368 — CLEANER PASS, 2026-09-05

NONE. No defect found; no cleanup change made.

## Merge conflict resolution

Same recurring merge-deletion guard limitation as earlier in this session
(BL-1370's and BL-1401's merge-ups, BL-1403's own scope): the guard's
attribution check reads HEAD's own most-recent touch of
`backlog/INTAKE-human-expedite-park-must-unhold-on-finish-1788436320924.md`
and `backlog/INTAKE-operator-question-1788557930426.md`, which is still
the pre-ticketing original commit for both — no commit message can
satisfy it. Restored both files at their original paths once more
(non-destructive; content byte-identical to what was already at HEAD).

## What was checked

- `extension/src/util/commitIntegrityRunner.ts`: new exported
  `HUMAN_DECISION_BYLINE` constant (`'By the human, recorded by the front
  desk.'`) and `humanDecisionCommitMessage(subject)` helper — ONE
  composition point, so the two required call sites cannot drift apart.
- Both `required_wiring` anchors confirmed live at their call sites:
  `bridgeServer.ts:953` and `telegramFrontDeskBotCore.ts:1323` both now
  call `humanDecisionCommitMessage(...)` instead of the hardcoded `By
  coder.` literal.
- Beyond the two named anchors, the coder also fixed a THIRD genuine
  human-decision site not named in `required_wiring`:
  `telegram-front-desk-bot.ts:2376` (the Expedite path, which the shared
  `commitApprovalWrites`'s own comment already documented as "shared by
  every automated human_approval writer (Expedite, paused-pager Approve,
  Telegram Approve/Reject/Amend)"). Correct and complete, not scope creep
  — leaving it on the old literal would have left exactly the "surviving
  half" the ticket's own `description` warns against.
- Invariant 2 (pipeline role commits keep their byline) verified NOT
  regressed: `bridgeServer.ts:1199,1385,1498` (epic/topic reorder) still
  read the literal `By coder.` unchanged — these are role commits, not
  human-decision commits, and were correctly left alone.
- Grepped `swarmforge/scripts/*.sh` and `*.bb` for anything parsing `By
  coder.` out of a commit body: 0 hits — no gate goes red (qa_e2e step 5).
- `bl1368ApprovalCommitByline.test.js`: 7/7 PASS.
  `bl1368ApprovalCommitByline.property.test.js`: 3/3 PASS, including the
  property that a pipeline role's own commit still passes the role-byline
  check while a human-decision commit correctly is not one.
  `telegramFrontDeskBotCore.test.js`: 449/449 PASS (unaffected; the three
  stderr lines during the run are expected fixture output for negative
  cases, not failures). `commitIntegrityRunner.test.js`: 10/10 PASS
  (pre-existing suite, unaffected).
- Real acceptance run:
  `specs/features/BL-1368-an-approval-commit-names-the-decider.feature` —
  4/4 pass, including "every decision verb records the same way" (all
  three: Approve/Reject/Amend) and "an agent's own commit still carries
  its role byline."
- `specs/pipeline/steps/bl1368ApprovalCommitNamesDeciderSteps.js::registerSteps`
  present, matches BL-1371's discovery suffix (anchor 3).
- `tsc -p ./` compiles clean.
- `jscpd` over the new/changed non-generated files: 0 clones.
- `mutation-site-count.js`: `commitIntegrityRunner.ts` 41 sites (within);
  `bridgeServer.ts` (1885), `telegramFrontDeskBotCore.ts` (1968) and
  `telegram-front-desk-bot.ts` (1837) are all `over` — but this ticket's
  own diff to each is a one-line call-site swap in a large pre-existing
  file, not new structure this parcel introduces; splitting any of the
  three is a pre-existing, unrelated concern out of this ticket's scope.
- No prior bounce history for BL-1368. `main`/`origin/main` in sync
  (`0 0`).

No architecture concerns: a single exported constant and a two-line helper
replace three hardcoded literals, called identically from all three sites
— the minimal shape for "cannot drift apart" the ticket asks for.

By cleaner.
