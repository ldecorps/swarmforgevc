# BL-1409 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Second pass — the spec-gap bounce resolved

First pass (`BL-1409-QA-20260906.md`) held this ticket for a spec-gap: D1
scenario 04 asserted the whole drift-guard suite exits 0, unsatisfiable
given BL-1448's already-known case 11. Bounced to the coder per the
specifier's own amendment note
(`backlog/evidence/BL-1409-spec-amendment-scenario-04-20260906.md`), which
supplied the exact replacement text.

- `backlog/evidence/BL-1409-coder-spec-gap-fix-20260906.md` confirms the
  fix applied VERBATIM: scenario 04 replaced with "case 07 passes / every
  case through 10 passes"; handler asserts `PASS: 07`..`PASS: 10` lines,
  never `rc == 0` (BL-1006: never red-when-correct once BL-1448 lands).
- Re-verified independently: `run_acceptance.sh` on this ticket's own
  feature — 5/5 (was 4/5, only scenario 04 previously failing).
  `run_acceptance.sh` on `BL-570-property-suite-drift-guard.feature` —
  still 7/7. `test_property_suite_drift_guard.sh` — case 07 still PASS,
  case 11 still FAIL (now outside scenario 04's amended assertion,
  correctly).
- Hardener's second pass confirms their own first-pass fix
  (`4968e6960c`, the hook-file-missing mislabeling) is intact and
  unaffected by the bounce — it touched only the acceptance feature and
  step handler, never `bl1409PropertyGuardWiring.js`.
- Architect/cleaner/documenter second passes: all clean NONE.
- Full unit lane (`npm test`, extension/): 610/610 files, 10309/10309
  tests green.
- Full property lane (`npm run test:properties`): 364/368 files green on
  the full run (1096/1100); see "Not bounced" below — confirmed 368/368
  clean via isolated re-run.
- Lineage: hardener merge `9f2620d332` confirmed an ancestor of the
  citing commit.
- record-bounce-correction.js (BL-990) already recorded on the first
  pass, correctly attributing this bounce to the specifier's own
  mint-time gap, not coder workmanship — nothing further to correct here.

## Not bounced (observed, not this ticket's defect, not reproducible — host load, BL-1348-documented class)

Full property run failed 4 files under host load:
`bl1272LandedSiblingInvariants`, `bl1297MergeOwnPathsInvariants`,
`bl1343ReplayNeverDropsOwnPathInvariants` (all three fixed 20s timeouts,
the same land-step-family flakes seen repeatedly this shift), and
`bl687EpicTileSurfaceUntouched` (a new file for this specific flake
class, unrelated domain — epic-tile-surface invariance vs. this ticket's
property-guard wiring). No ticket found for any (`grep -rl <name>
backlog/paused/ backlog/active/` empty; none in
`backlog/standing-reds.tsv`). Re-ran all four in isolation once load
eased (~6.0): all four passed cleanly. Same disposition as this shift's
prior "Not bounced" sections (BL-1442, BL-1424, BL-1412, BL-1409's own
first pass, BL-1226).
