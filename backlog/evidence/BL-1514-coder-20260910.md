# BL-1514 — coder evidence (2026-09-10)

## What changed

- `specs/features/BL-368-control-loss-is-not-agent-death.feature`: retired
  scenario 03 ("A genuinely dead agent is still detected and recovered"),
  `RETIRE-WITH: BL-1514` left in its place. Scenarios 01, 02, 04 unchanged.
- `specs/features/BL-359-always-on-operator-presence.feature`: retired
  scenario 05 ("An always-on Operator never suspends the swarm's own
  recovery"), `RETIRE-WITH: BL-1514` left in its place. Re-tensed the
  Background's "the swarm's health sweeps, dead-pane respawns and stall
  nudges need" to name the babysitter as the current owner (BL-1006).
  Scenarios 01-04, 06 unchanged.
- `specs/features/BL-647-rotation-router-liveness.feature`: deleted outright.
- `specs/pipeline/steps/controlLossIsNotAgentDeathSteps.js`: removed the
  scenario-03-only step handlers (`a role's agent process has really died`,
  `the swarm checks the health of its roles`, `it reports that role as
  exited`, `it recovers it`) and the now-unused `track` import. Grepped
  every `.feature` file first for each removed pattern — no other scenario
  used them.
- `specs/pipeline/steps/alwaysOnOperatorPresenceSteps.js`: removed the
  scenario-05-only step handlers (`the Operator presence is live`, `a
  role's pane dies and a handoff is left unattended`, `the swarm still
  detects and recovers them`) and the now-dead `mkRuntimeFixture`/
  `eventsText` helpers and `track` import (both helpers had no remaining
  caller once the scenario-05 handlers were gone). Grepped first — no other
  scenario used the removed patterns. The retired step's static source grep
  ("handoffd.bb and chase_sweep_lib.bb name no operator-running?/
  operator.pid") was re-verified true at removal:
  `grep -n "operator[-_]running?\|operator\.pid" swarmforge/scripts/handoffd.bb
  swarmforge/scripts/chase_sweep_lib.bb` — no matches.
- `specs/pipeline/steps/bl647RotationRouterLivenessSteps.js`: deleted
  outright (registry auto-discovers step files; no index edit needed, no
  other `.feature` referenced its patterns).
- `swarmforge/scripts/operator_lib.bb`: deleted the now-unwired
  `dead-agent-events` function. Kept `control-lost-event` and
  `parse-roles-tsv` (both still called). Re-tensed
  `tick-observed-events`'s docstring, which had referred to
  `dead-agent-events` as if it still existed.
- `swarmforge/scripts/operator_runtime.bb`: dropped the dead `roles`,
  `rotation-mode`, `rotation-opts` let-bindings from `tick!` — verified
  nothing else in `tick!` reads them (`agents-running`, which IS still
  read at lines ~2454/2495, is untouched). `prev-conf`'s own
  `conf-rotation-mode` call at line 1381 is a different site and is
  untouched.
- `swarmforge/scripts/test/operator_lib_test_runner.bb`: removed the
  `dead-agent-events`/BL-647 assertions (the `router-roles` fixture and
  its 6 scenarios) — kept the `parse-roles-tsv` test and its `sample-roles`
  fixture (unrelated, still needed).
- `swarmforge/scripts/test/operator_lib_bl647_property_runner.bb`: deleted
  outright per the ticket's explicit instruction.
- `swarmforge/scripts/test/bl648_relaunch_resume_property_runner.bb`:
  updated a comment that named the now-deleted property runner as a
  discipline precedent.
- `swarmforge/scripts/test/test_operator_runtime_bl647_rotation_liveness.sh`
  and `test_operator_runtime_control_lost.sh`: kept (both already assert
  BL-653's zero-event behaviour and stayed green); updated comments in the
  bl647 file that still described `dead-agent-events` as a live,
  currently-wired function.

No `required_wiring`: no mechanism was built, none needed.

## Invariants (BL-654)

- **INV2** ("operator_lib.bb exports no function that no production caller
  reaches: dead-agent-events is gone and nothing under swarmforge/scripts
  (outside test/) is left referencing it") — coder-authored executable
  property test:
  `extension/test/bl1514DeadAgentEventsRetiredInvariants.property.test.js`.
  Exhaustive walk of the real `swarmforge/scripts/` tree (not sampled —
  the property quantifies over a fixed, enumerable file set). Non-vacuity
  proven against two synthetic broken fixtures (a re-introduced `defn`, a
  re-introduced non-test caller) plus a synthetic clean fixture (a
  test/-confined reference is correctly NOT flagged). All 4 cases pass:
  `cd extension && npx vitest run --config vitest.properties.config.mjs
  test/bl1514DeadAgentEventsRetiredInvariants.property.test.js` → 4/4.

- **INV1** ("no scenario under specs/features/ and no step handler under
  specs/pipeline/steps/ asserts that operator_runtime.bb's tick emits an
  AGENT_EXITED event; the only AGENT_EXITED assertions left in the
  acceptance lane are BL-653's and BL-368's negative ones") — **no
  executable property-test encoding written; stated reason recorded
  instead**, per the Invariants contract ("a declared invariant that
  admits no executable encoding ... quantifies over prose or process
  rather than a pure, testable module"). This invariant quantifies over
  Gherkin feature content and step-handler assertion semantics — the
  coder role does not hand-author or judge Gherkin ("Does Not Own": Gherkin/
  feature files and their mutation stay with specifier/hardener), and a
  generic detector for "this step handler asserts AGENT_EXITED presence
  vs. absence" has no principled static encoding short of re-implementing
  the acceptance runner's own semantics. It is instead verified directly:
  running the acceptance lane itself (below) and an exhaustive repo grep
  showing every remaining `AGENT_EXITED` occurrence in the acceptance lane
  is either a pre-existing negative assertion or unrelated (BL-598's alert
  trending, which handles the string as inert data, not a tick-emission
  claim):

  ```
  $ grep -rn "AGENT_EXITED" specs/features specs/pipeline/steps
  ```
  → only BL-653's "no fabricated AGENT_EXITED events" negative assertion,
  BL-368's "it does not report any agent as having exited" negative
  assertion, and BL-598's unrelated alert-type constant/fixtures. No
  positive tick-emission assertion remains.

## Verification run

```
$ bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-368-control-loss-is-not-agent-death.feature
ok 1..3, # fail 0

$ bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-359-always-on-operator-presence.feature
ok 1..6, # fail 0

$ ls specs/features/BL-647-rotation-router-liveness.feature specs/pipeline/steps/bl647RotationRouterLivenessSteps.js
(both: No such file or directory)

$ grep -rln "dead-agent liveness sweep runs" specs/features
(no output)

$ grep -rn dead-agent-events swarmforge/scripts specs/pipeline
(only dated comments in test_operator_runtime_control_lost.sh and
test_operator_runtime_bl647_rotation_liveness.sh)

$ bash swarmforge/scripts/test/test_operator_runtime_control_lost.sh
ALL CHECKS PASSED

$ bash swarmforge/scripts/test/test_operator_runtime_bl647_rotation_liveness.sh
ALL CHECKS PASSED

$ bb swarmforge/scripts/test/operator_lib_test_runner.bb
operator_lib: ALL TESTS PASSED

$ bb swarmforge/scripts/test/bl648_relaunch_resume_property_runner.bb
ALL PROPERTIES HOLD

$ cd extension && npm run compile
(clean)

$ cd extension && npx vitest run --config vitest.properties.config.mjs test/bl1514DeadAgentEventsRetiredInvariants.property.test.js
4 passed (4)
```

qa_e2e_procedure items 7 (mutation-stamp re-stamp) and 8 (register-row
removal) are hardener/documenter/coordinator downstream steps per the
ticket's own "How" section and are not part of this coder parcel.

By coder.
