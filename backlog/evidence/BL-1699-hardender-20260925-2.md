# BL-1699 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second same-day hardener pass, after QA bounced 4 defects (D1-D4: the
coordinator's aider bootstrap text leaked "swarm"/"seat"/"backlog"/
"scripts"/"backlog/paused"/"backlog/active"; the forbiddenHit
predicate in both the property test and step handler had a 6-char
length floor and a files-only path list that made it blind to exactly
those leaks; invariant 2's disposition pointed at a golden-fixture
comparison that does not exist; the seat-command-vocabulary reference
never documented requirement 4's driver-record scope fallback) — all
four fixed and re-passed clean by cleaner, architect and documenter
(`BL-1699-{cleaner,architect,documenter}-20260925{,-2}.md`).

D2 is directly in this stage's own domain (the property test's own
predicate correctness) — QA's own find is a real embarrassment for
the ORIGINAL hardening pass on this ticket: I ran the existing
non-vacuity assertion, saw it pass, and did not independently probe
the predicate's own boundary (a short tracked path, a short
basename, a directory rather than a file) before trusting it. Re-read
the fix this pass: whole-word matching (split on whitespace, strip
only leading/trailing non-word/dot/slash/hyphen punctuation), no
length floor, `FORBIDDEN_WORDS` unions repo paths, every directory
PREFIX of one, and scripts basenames. Confirmed non-vacuous myself
(not merely re-trusting QA/architect's empirical claims) by reading
the five new assertions: three positive (a long embedded path, the
short file "swarm", the short basename "seat", a directory prefix)
and one negative ("a seating chart" must NOT match — proves whole-word,
not substring, so the fix does not overcorrect into false positives).

BL-113 Gherkin mutation: same stale-manifest trap identified on
BL-1698 this session (recorded as a `rule_proposal` there,
scope `role:hardender`) — the feature's Scenario text is unchanged by
the D1-D4 fix, so a soft OR hard re-run would have silently reused the
pre-bounce manifest. Forced `level=full`: 8/11 killed, 3 survivors
(the Scenario Outline 01 role-column case-flips, m1-m3) — the SAME
three mutants I accepted as equivalent in my ORIGINAL pass, re-verified
still equivalent after the coordinator branch's rewrite: mutating
"coordinator" still only diverts execution into the generic branch
(since `(= role "coordinator")` no longer matches "Coordinator"), and
that branch's text is now ALSO clean of every forbidden word (the D1
fix), so this scenario's own Then step still cannot observe the
mutation. No new gap. The tool correctly withheld the mutation-stamp
header this run (survivors present), so a future soft/hard run will
not silently trust this verdict either — confirmed by grepping the
feature file for `# mutation-stamp:`, absent.

Checks run (all on commit `9c3afa1cb5`, this pass's own tip):
- `node specs/pipeline/cli.js specs/features/BL-1699-….feature`:
  10/10.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=full: 8/11
  killed, 3 accepted-equivalent survivors (see above).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1699AiderBootstrapNoRepoPaths.property.test.js`: 2/2
  (targeted, not the full properties lane — coder-authored, not
  changed by this pass, per the 2026-09-17 rule).
- `bash swarmforge/scripts/test/test_aider_seat_launch_config.sh`
  (13 cases, including this session's own case 13): ALL PASS.
- `npm test` (extension/): 638 files / 10874 tests, all green. Same
  pre-existing per-file budget warnings on a handful of unrelated
  files (the exact set fluctuates run to run by host contention, not
  by this ticket's own diff).
- No TS/JS production file in this ticket's scope — CRAP/DRY not run
  (Babashka/shell only, per engineering.prompt).
- `pgrep -f '[t]mux -S' | wc -l`: 9 after this pass's runs, all
  identified individually by name (`ps aux`) as OTHER live swarm
  sessions/roles' own servers (agent, reaper-test-session,
  swarmforge-spy-router, specifier launch, swarm, coder, coordinator)
  — none a stray fixture from this pass's own runs (none of this
  ticket's own tests spawn a real tmux server). `git status --short`
  clean before each commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load 3-19/20 cores throughout (briefly ~19 during the
  gherkin mutation run, still under the 2x-cores ceiling), no fallback
  needed.

By hardender.
