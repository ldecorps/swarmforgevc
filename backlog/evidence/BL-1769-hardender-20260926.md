# BL-1769 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Full findings and fixes in commit `bb644ae3fe` (CRAP regression fix +
isolating tests + equivalence confirmation) - see its own commit message
for the complete write-up. Summary:

- Fixed a real CRAP regression: `buildRegisterJoin` grew from baseline
  complexity 5 (origin/main) to 7 at 100% coverage after this ticket's own
  unidentified-entry tracking was inlined; extracted
  `collectFailingAndUnidentified` to bring it back to complexity 3.
- Closed 3 genuine mutation gaps on `isRedParseableRow`'s own AND-chain
  (every existing fixture masked at least one clause behind another) with
  3 new isolating unit tests, and 1 gap on the `unidentifiedEntries.sort()`
  call with 1 new ordering test.
- Confirmed the remaining 4 survivors (composeQaGatherReport's raw-output
  capture guard) EQUIVALENT by patch-and-diff against the real compiled
  file (45/45 tests, byte-identical with the guard forced to `if (true)`) -
  grep-confirmed `rawOutputByCheckId` is never read for any id but
  unit/properties.
- Found and fixed the stale-`out/` compile-lag trap (BL-497) before any of
  the above.
- Also noted: the coder's own evidence miscounted
  `qaGather.property.test.js` as "3 tests (2 pre-existing + 1 new)" - it
  is actually 2 (1 pre-existing + 1 new), confirmed against
  origin/main's copy of the file. Prose-only inaccuracy, no functional
  defect - not itself a bounce item.

Gates run: BL-1554 feature (14/14), qaGather.test.js (37/37),
qaGatherCli.test.js (8/8), qaGather.property.test.js (2/2), all 22
whole-tree guard files (211/211, required by touching `extension/test/`),
`npx jscpd` (no duplication involving qaGather.ts). Full unit lane
(`npm test`) has one pre-existing, already-ticketed failure
(`briefingDigestLineCli.test.js`, BL-1770) confirmed via grep before
excluding it as unrelated to this parcel.

Commit note: the FIRST `git commit` attempt was refused by the
property-suite-guard pre-commit hook over
`test/bl1373PathSetCacheInvariants.property.test.js` "still fails when run
alone" - reproduced it standalone myself immediately after and it passed
3/3; the hook's own run showed `fatal: ambiguous argument 'origin/main'`
errors mid-run (this host runs a live, concurrently-active swarm with
frequent git ref churn from other sessions). Re-ran `git fetch origin
main` (resolved cleanly) and retried the commit unchanged; the guard
passed on retry. Recorded here rather than treated as a silent retry,
per the transient-vs-real discipline for reds outside the parcel.
