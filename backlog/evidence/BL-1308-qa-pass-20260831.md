# BL-1308 — QA pass, 2026-08-31

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1308` → `VERIFY BL-1308` (exit 0),
checked both before and after the full verification pass.

## Merge

Merged documenter's cited commit `2c7a90266d` into QA at `a9000b9bd5`
(`git merge --no-ff`). The merge deleted `backlog/active/BL-1300-...yaml`
with no other staged path recognized by `check_ticket_deletion.sh` (the
guard's `is_ticket_path` list does not include `backlog/hold/`) — verified
content-identical against `backlog/hold/BL-1300-...yaml` (diff empty, 167
lines both sides) before naming BL-1300 in the merge commit message per the
established precedent (`82b0a1a0db`, coder's own merge of this same
content). No retirement; carries the coordinator's active→hold move
(human ruling pending on BL-1300), unchanged since BL-1307.

Diffed the merge against BOTH parents: nothing dropped. Diff vs my own prior
QA tip (`6d9e987a07`) shows only additions plus the expected BL-1300 path
move; diff vs documenter's tip (`2c7a90266d`) shows only QA's own
pre-existing evidence files that documenter's branch didn't have — no
content lost either direction.

Ancestry re-confirmed for the full chain: coder `f3166a3fd5`, cleaner
`51b9a74b78`, architect `1e319901c7`, hardener `3345500c22`, documenter
`2c7a90266d` are all ancestors of the commit I am approving (`a9000b9bd5`),
not merely ancestors of HEAD on a busy branch (BL-336 discipline).

## Stage corroboration

Read all five stage evidence files
(`BL-1308-{coder,cleaner,architect,hardener,documenter}-*.md`). Cleaner:
NONE, no defect. Architect: CLEAN, dependency-gate/co-change/invariants/
acceptance/unit all independently re-run and green, correctness read clean.
Hardener: mutation cooldown skipped (file 0.22 days old, quiet load) per
BL-149 — Babashka has no Stryker/CRAP/DRY wired regardless
(engineering.prompt); BL-113 soft Gherkin mutation on the one Scenario
Outline — 2/2 killed. Documenter: doc-only pass, Specification.MD + the
BL-1241 how-to updated accurately (verified by reading the diff directly),
no registered diagram's change-trigger fired.

## Change under verification

`ancestry-commits` in `swarmforge/scripts/land_step_lib.bb` drops
`--first-parent` from `git rev-list`, so the land step's sibling detector
now walks a commit's full ancestry instead of first-parent only — closing
the gap where a sibling ticket's untagged commits, riding into a
forward-merge on the second parent, entered the replay's `:delivered`
path diff while never being named in the `entangled-siblings` report.
`own-commit-changed-paths` / `task-tagged-changed-paths` untouched, as the
ticket requires (BL-1297's fix stays intact). Shape 1 of the two directions
the ticket offered; the acceptance file's own scenarios ("the sibling
ticket is named in the report") commit to this shape, so it's the only one
that satisfies the contract already written.

Verified the widened walk actually reaches BL-1300 by construction: BL-1300
was the real live case (BL-1307's documenter tip `bd27e884cb`), and this
same fix is what now makes BL-1300 show up in `entangled-siblings` on a
tip carrying it as a second-parent ancestor.

## Wiring

`land_step_cli.bb:59` still calls `land-plan`, which calls
`entangled-siblings`, which calls the modified `ancestry-commits` — the
call chain is unchanged, only the internals of the leaf function. Confirmed
by grep, not assumed. Step handler `bl1308SiblingDetectorCoversReplaySteps.js`
registered in `specs/pipeline/steps/index.js:905` — confirmed directly.

## Own independent runs (BL-112, separate from coder's/hardener's)

| Command | Result |
|---|---|
| `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` | ALL PASS |
| `run_acceptance.sh` BL-1308 feature | 5/5 pass |
| `run_acceptance.sh` BL-1241 feature (regression) | 4/4 pass |
| `run_acceptance.sh` BL-1272 feature (regression) | 6/6 pass |
| `npx vitest --config vitest.properties.config.mjs` BL-1308 property file (isolated) | 2/2 pass |
| `npm run compile` | clean |
| `npm test` (full unit suite) | 26 failed / 9780 passed |
| `npm run test:properties` (full property lane) | 25 files / 14 tests failed, 821 passed (835); 1 allowlisted `[vitest-worker] onTaskUpdate` timeout (BL-871) |

## Unowned red — verified pre-existing, not this parcel's (BL-1063)

All unit-suite failures trace to the same root already named in the coder's
and architect's own evidence: `deps.checkOrphanedAuthoredDocs` missing from
`landPilotedTicket` test fakes (`pilotAcceptanceGate.ts:624`, BL-757
follow-on) → tracked at `backlog/paused/BL-1221-...yaml`. The three
repo-hygiene guard failures (`tempDirTrapGuard`, `socketFixtureShortRootGuard`,
`liveRepoDerivationGuard`) are tracked at `BL-1289`, `BL-1290`, `BL-1291`
respectively (all `backlog/paused/`, confirmed by grep, all named already in
the hardener's own evidence file).

Property-lane failures are the same standing pattern independently
corroborated on 2026-08-28 (`QA-standing-red-corroboration-20260828.md`):
`node:test`/TAP-style collection breaks Vitest's "No test suite found"
class (BL-1220 lineage — spot-checked 4 of the newly-seen file names,
3 literally `require('node:test')`, the 4th emits TAP output the same way),
plus already-evidenced `BL-654` stamp-off invariant reds (`bl1113`, `bl1115`,
`bl1136` — `bl1136`'s ENOENT is because the ticket moved to
`backlog/done/M8/` and the test fixture path was never updated, a stale-path
issue on an already-closed ticket, unrelated to BL-1308).

None of the 25 failing property files or 16 failing unit-suite files
reference `land_step_lib.bb`, `task_scope_gate_lib.bb`, `entangled-siblings`,
`ancestry-commits`, or any BL-1308 file
(`bl1308SiblingDetectorCoversReplay.property.test.js`,
`bl1308SiblingDetectorCoversReplaySteps.js`,
`bl1308SiblingDetectorFixtureCli.sh`) — checked by reading every failure
name and, for the ambiguous ones, the actual error text. Surfacing, not
sweeping — no fix attempted here.

## Orphan check

`pgrep -fl 'node --test|stryker|vitest'` clean before this pass and after
both the full unit run and the full property-lane run.

## Verdict: APPROVE

By QA.
