# BL-1444 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1444`: `VERIFY BL-1444` (no open
  deferral).
- Orphan process check before/after: `ps aux | awk '/[n]ode --test|[s]tryker/'`
  empty both times (`pgrep -fl` self-matches its own argv via `-f` and is not
  a reliable signal here).
- Full unit lane (`npm test`, extension/): 610/610 files, 10283/10283 tests
  green. One transient failure on the first run
  (`telegramCursorOperatorExec.test.js` BL-1204 cases, `ENOTEMPTY` on
  `/tmp/bl1204-*`) traced to leftover fixture marker dirs from this same run
  never swept by `sweepPendingTmpDirs` — identical signature to
  `BL-1263-qa-pass-20260905.md`'s "session debris, not a defect" finding;
  removed the two dirs and reran clean. The suite-duration/per-file budget
  overage (`recordTestDuration.js`'s guard, exit 1) is chronic and
  pre-existing — `.test-durations.jsonl` shows 30-50s runs going back well
  before this parcel, none of the four flagged slow files touched by
  BL-1444's diff — not a regression from this ticket.
- Full property lane (`npm run test:properties`): 363/363 files, 1084/1084
  tests green, exit 0. One `[vitest-worker]: Timeout calling "onTaskUpdate"`
  unhandled error — the BL-871 allowlisted benign case. Includes
  `bl1444ArtDirectorTipGuardInvariants.property.test.js` (3/3).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1444-the-art-directors-tip-lands-on-main-by-qa.feature`
  — 13/13 scenario runs pass. Neighboring merge-chain features stay green:
  BL-1385 (13/13), BL-1303 (7/7), BL-632 (11/11).
- `test_bl1444_art_director_tip_guard.sh`: ALL PASS (11 cases, direct +
  hook mode, both wiring assertions).
- `test_pre_merge_commit_hook.sh`: ALL PASS (9/9) — hook stays green one
  guard longer.
- `test_pipeline_code_on_main_guard.sh`: red at case 02 exactly as on main
  (BL-1408's registered standing red, `backlog/standing-reds.tsv`, open
  ticket `backlog/paused/BL-1408-...yaml`); its `cp` list now names
  `check_art_director_tip.sh` per the ticket's "How". Not made green,
  per the ticket's explicit instruction not to (that's BL-1408's own
  deliverable).
- `grep test_bl1444 swarmforge/scripts/test/suite-manifest.tsv`: one row.
- Wiring anchor 1: `grep -n 'run_guard check_art_director_tip.sh'
  swarmforge/git-hooks/pre-merge-commit` hits once;
  `grep -c check_art_director_tip swarmforge/scripts/run_commit_guards.sh`
  is 0 (correctly excluded from the pre-commit chain).
- Wiring anchor 2: `specs/pipeline/steps/bl1444ArtDirectorTipLandsSteps.js`
  exports `registerSteps`; `specs/pipeline/steps/index.js` auto-discovers
  every file in the directory via `readdirSync` (BL-1371 posture) — no
  hand-maintained require list to update.
- Live direct mode: `check_art_director_tip.sh --tip
  $(git rev-parse primary/art-director)` → `ART_DIRECTOR_TIP_OK`.
  `--tip $(git rev-parse origin/main)` also printed OK rather than the
  ticket's literal expected REFUSED — traced to time elapsed since the
  ticket was authored: `primary/art-director` has since re-merged
  `origin/main` in its own regular sweep, so that exact tip commit is now
  genuinely reachable from `primary/art-director` (confirmed:
  `git merge-base --is-ancestor origin/main primary/art-director`). This is
  the guard behaving correctly given current history, not a defect — the
  negative case was instead proven against this QA worktree's own HEAD
  (`1a7875e27d`, provably not on `primary/art-director`):
  `ART_DIRECTOR_TIP_REFUSED` / "... is not on primary/art-director." as
  expected.
- Live land: this ticket's own guard was exercised live before this parcel
  even arrived — the actual `land art-director tip eb7b5cafe5` note earlier
  this session was landed under this exact procedure (merge `--no-ff`
  subject naming no ticket, diffed clean against both parents, pushed
  under the BL-1144 lock). `git log --oneline origin/main..primary/art-director`
  no longer names that tip.
- Live negative (out-of-lane refusal on the real branch): not exercised —
  doing so would require committing an out-of-lane file to
  `primary/art-director` for the sole purpose of the test. Per the ticket's
  own item 5, the fixture scenario (`test_bl1444_art_director_tip_guard.sh`
  hook-mode out-of-lane case, and the feature's own out-of-lane scenarios)
  is the evidence instead.
- Live scenario-03 merge: not separately reproduced — this worktree's HEAD
  was already caught up to `origin/main` by fast-forward at every sync
  point in this pass (no distinguishable merge-commit event to observe:
  `pre-merge-commit` does not fire on a fast-forward). Covered instead by
  `test_pre_merge_commit_hook.sh` case 01 and the feature's own "a merge of
  main's own tip is never judged" scenario, both green.
- Invariants: spot-checked `check_art_director_tip.sh` for `SWARMFORGE_ROLE`
  / `git push` / `git fetch` / `git commit` / `update-ref` — none found
  outside a comment; read-only by inspection, consistent with the green
  invariants property test.
- Lineage: this ticket's hardener merge (`ad368c7b81`, per
  `BL-1444-hardender-20260906-2.md`) is an ancestor of the citing commit
  (`1a7875e27d`) — `git merge-base --is-ancestor` confirmed.
- Docs: `docs/how-to/BL-1418-the-art-director-seat-is-addressable.md` gained
  "How the seat's work reaches main"; `docs/diagrams/swarm-flow.mmd` gained
  the ARTDIR node and its three edges; `docs/reference/Specification.MD`
  lists the guard in the hook chain.

By QA.
