# BL-1554 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- **Dogfooded the tool itself** (this ticket's own deliverable): `node extension/out/tools/qa-gather.js --ticket BL-1554 --task BL-1554 --root <worktree>` — ran to completion, exit 0, printed one JSON report with the fixed 8-check order (`stragglers_before`, `sibling`, `register`, `wiring`, `unit`, `properties`, `acceptance`, `stragglers_after`) and a `register_join` section. No `verdict`/`pass`/`bounce`/`approve` key anywhere in the report (`d.keys()` checked directly).
- **QA bounce D1 (path-prefix mismatch) verified fixed live, not just by unit test**: the dogfooded run's `unit` check genuinely failed on `bl1277UnscopedStepCollisionGuard.test.js` (the real pre-existing standing red), and `register_join` correctly reported it `{"file":"extension/test/bl1277UnscopedStepCollisionGuard.test.js","join":"owned","ticket":"BL-1607"}` — the exact real-world case the QA bounce named as broken (previously reported `absent`).
- `--ticket` omitted: exits 2 with a usage message (confirmed directly).
- `required_wiring`: `bl1554QaGatherReportSteps.js::registerSteps` present and exported.
- Acceptance: `BL-1554-*.feature` 10/10 scenarios pass (dogfooded run's own `acceptance` check, plus a direct re-run).
- `qa-sibling-check.js status --ticket BL-1554`: `VERIFY BL-1554` (no open deferral).
- Straggler check before and after: no orphaned processes belonging to this worktree (live `stryker`/`vitest` processes are the `hardender` worktree's own, confirmed by full command line each time).
- Unit tests for the tool itself: `npx vitest run test/qaGather.test.js test/qaGatherCli.test.js` — 37/37 pass, including the exact blocked-checklist and never-a-verdict scenarios (`runChecklist runs every check and never skips one on a blocked prerequisite`, `a check the runner cannot start is reported blocked...`).
- Full unit suite (dogfooded run's own `unit` check): 1 failing file, `bl1277UnscopedStepCollisionGuard.test.js`, the pre-registered, owned standing red (BL-1607) — not new, not caused by this parcel.
- Full property suite (dogfooded run's own `properties` check): exit 0, all green; 4 `Unhandled Error` entries, all the allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871).
- Standing-red register: unchanged by this ticket (3 rows, `"unowned":[]`).
- Diff scope: `git diff main...HEAD --name-only` (excluding backlog/docs) lists `qaGatherAdapter.ts`/`qaGather.ts`/`qa-gather.ts`, their tests, the step handler and feature, plus files already landed by sibling ticket BL-1606 (bl1304/bl968/propertyLaneContentionBudget.js/the allowlist) via the ordinary merge chain — nothing under `swarmforge/scripts/` touched by this ticket itself, confirming no check was reimplemented.
- Both bounces (architect's excerpt-truncation D1, QA's path-prefix D1) were fixed in-stage and correctly re-forwarded; no unfixed defect remains in the ticket's bounce history.
- Hardener's own self-caught D1 (coverage/CRAP gap) was fixed in the same stage before forwarding.
