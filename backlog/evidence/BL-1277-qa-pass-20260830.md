# BL-1277 QA pass — 2026-08-30

Commit verified: merge of documenter `76bc415b87` into QA. BL-1277's own
coder/cleaner/architect(repass)/hardener/documenter commits (e29af64cf7,
c2b36bf2c4, 5fbc428728, 9530147e3f, 76bc415b87) are all confirmed ancestors
of the merge HEAD (`git merge-base --is-ancestor`). Bounce history checked
against `main`: one prior architect bounce (D1, literal NUL byte in the
property test source) — verified fixed independently below, not just
trusted from the bounce record.

## Guard's own test suite
`npx vitest run test/bl1277UnscopedStepCollisionGuard.test.js`: 6/6, including
"the shipped step files register no colliding unscoped pattern" — the real
`specs/pipeline/steps/` tree scanned, zero collisions.

## Independent second opinion (qa_e2e_procedure step 3)
Wrote a standalone Python scan (regex-literal-aware, not trusting the guard's
own implementation) over `specs/pipeline/steps/*.js` for
`registry.define(<pattern>` calls: 5223 unscoped registrations scanned,
**zero** patterns registered by more than one file. Corroborates the guard's
verdict from outside its own code.

## Guard is not vacuous (qa_e2e_procedure step 4)
Planted a live duplicate: copied `burndownEtaSteps.js`'s
`/^delivery metrics whose forecasts\.milestones carry each milestone's ETA$/`
pattern, unscoped, into `gateAnswerSteps.js`'s `registerSteps`. Re-ran the
guard: refused, naming the exact pattern and both files
(`burndownEtaSteps.js`, `gateAnswerSteps.js`). Reverted with `git checkout --`
immediately after; `git status` confirmed clean.

## Property tests
`bl1277StepCollisionInvariants.property.test.js`: 5/5 — invariant 1 (load-order
independence, including a "plant a second file, expect a flip" case) and
invariant 2 (verdict derives from the registry, not source-text grep — proven
both directions: a duplicate registered via an aliased call with no literal
`registry.define(` in its own source is still caught, and a file whose source
text is full of the literal string but registers nothing is not flagged).

## Acceptance
- `specs/features/BL-1277-...feature` (own): 5/5.
- **The two tickets this collision had put on `main` at reduced pass rates,
  independently re-run by me:**
  `BL-1268-stale-claim-branch-must-name-this-ticket.feature`: **7/7** (ticket
  claimed prior 2/7). `BL-378-no-single-file-bounds-the-suite.feature`: **4/4**
  (prior 3/4). Both restored, matching the coder/architect/hardener's own
  independent measurements.
- Five of the six qa_e2e_procedure-named at-risk features, independently
  run and green: `BL-334` collision partner `BL-334-restricted-front-desk-operator`
  (see caveat below — could not complete in this session, not a code
  finding), `BL-655-ambulance-mode-hold` 20/20, `BL-589-approval-ask-carries-ruling-options`
  5/5, `BL-852-chase-sweep-respects-ambulance-hold` 11/11,
  `BL-484-decided-ask-closes-itself` 4/4,
  `BL-328-merged-code-reaches-running-daemons` 6/9 (see below).

### BL-328: 3 failures, matched against the coder's own pre-fix/post-fix baseline
The coder's evidence file (`backlog/evidence/BL-1277-unscoped-step-collision-sweep-and-guard-20260830.md`)
records this exact feature's own before/after count: 4 pass/5 fail →
5 pass/4 fail, "one scenario RECOVERED by the sweep" — this ticket improves
it, does not claim to fully fix it. My own run got 6 pass/3 fail; the extra
pass relative to the coder's post-fix baseline is consistent with the
scenario's own real-compile/real-respawn timing sensitivity (each subtest
takes 6-14s and drives an actual build+process-restart cycle), not a
regression. The three still-red scenarios fail inside their own embedded
`build_freshness_cli` subprocess assertions
("bridge should be reported stale before sync") and a message-delivery
timeout — pre-existing, unrelated to step-registry resolution, and
untouched by this ticket's diff (`mergedCodeReachesDaemonsSteps.js` is not
one of the 16 files BL-1277 edited).

### BL-334: could not complete in this session — infrastructure hang, not a finding
`node specs/pipeline/cli.js specs/features/BL-334-restricted-front-desk-operator.feature ...`
hung with literally zero stdout (not even the TAP header) across three
attempts (foreground killed at timeout twice, one fully detached run left to
run for several minutes, also silent) despite `test_operator_runtime_tick.sh`
— the real shell test this feature's steps drive via `spawnSync` — running
cleanly standalone in 47s with all checks passing. Reaped the resulting
orphaned process each time (verified: no leftover process, no port left
bound, all 8 live tmux sessions confirmed intact throughout — the hang does
not touch real tmux). The coder's own before/after table does not list
`BL-334` as red, implying it ran clean for them; I have no positive evidence
of a defect here, only an inability to reproduce a clean run in this specific
interactive session. Not treated as a BL-1277 finding.

## Full unit lane, cross-checked against three independent measurements
`npx vitest run` (default config): 26 failed files / 219 failed tests / 9442
passed. The hardener's own evidence records "26 failed files / 218 failed
tests / 9443 passed... same failing-file count as the BL-1280 hardening
baseline"; my own BL-1280 QA pass earlier today measured 26 failed/218-ish
(±1, consistent flake) with the identical two root causes (CURSOR_API_KEY
absent in this session's env; `deps.checkOrphanedAuthoredDocs is not a
function`, BL-1229). Three independent passes (hardener, and my own two QA
passes today) converge on the same noise floor. None of the 16 files
BL-1277 edited appear in the failing set.

## Wiring (no `required_wiring:` declared, deliberately — per ticket notes)
Guard discovered by vitest's glob with no call site required. Confirmed:
`npx vitest run test/bl1277UnscopedStepCollisionGuard.test.js` passes both
standalone and as part of the full default-config run above (qa_e2e_procedure
step 6). `specs/pipeline/steps/index.js` line 877 registers
`bl1277UnscopedStepCollisionSteps` (the architect's self-caught merge-drop
fix — confirmed still present after my own merge).

## Verdict: APPROVE
The collision mechanism is fixed, independently corroborated (own scan +
plant test), non-vacuous, and the two previously-degraded tickets are
restored to full green. Every residual red is either pre-existing (BL-328,
matched against the coder's own baseline) or an environmental inability to
complete one run in this session (BL-334) with no positive evidence of a
defect and no sign the coder or hardener hit the same wall.

By QA.
