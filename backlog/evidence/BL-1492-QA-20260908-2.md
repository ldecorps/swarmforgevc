# BL-1492 — QA review pass (RE-RUN), 2026-09-08

## Verdict: APPROVED

The Article 4.2 unowned-red veto that held this parcel at 14:01Z is now lifted:
the specifier folded the BL-081 scenario 01 red ("hard-stop confirms the old
daemon's exit") into BL-1498 at commit c7429bd2eb (2026-09-08 15:16Z). The
standing-reds.tsv row for `test_handoffd_supervisor.sh` now reads: "case 02 and
BL-081 section scenarios 01/03: same fresh-pid-file startup-grace bug, three
separate fixtures (coder unowned-red 09-08 08:12Z from BL-1490; QA unowned-red
09-08 14:01Z re BL-081 scen01 folded in, not a new ticket)". The blocker is
owned; this parcel can proceed.

## Gates re-run — all green

- `qa-sibling-check.js status --ticket BL-1492`: `VERIFY BL-1492` (no open
  deferral).
- Orphan process check before: `pgrep -fl 'node --test|stryker'` clean (only
  the shell itself, PID 26504).
- HEAD unchanged at `e430830319` (the HOLD evidence commit); no code changed
  since the HOLD verdict, only the register row.
- Full unit lane (`cd extension && npm test`): **613/613 files, 10401/10401
  tests pass**. Exit code 1 is the chronic suite-duration/per-file wall-clock
  budget guard — same disposition as the HOLD pass and BL-1444's QA pass
  (2026-09-06): none of the flagged slow files
  (`bl1277UnscopedStepCollisionGuard`, `bl968StepRegistryMaterializedTreeGuard`,
  `briefingDigestLineCli`, `emitLifecycleSnapshotCli`, `recordBounceCli`,
  `renderBriefingBurndownCli`, `renderBriefingDiagramsCli`,
  `telegramFrontDeskBotCli`) is touched by BL-1492's diff, not this ticket's
  regression.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1492-a-stalled-verdict-restarts-the-daemon-in-place-not-the-swarm.feature`:
  **5/5 scenarios pass** (restart-in-place-01 through 05).
- `bb swarmforge/scripts/test/handoffd_supervisor_restart_budget_test_runner.bb`:
  **ALL TESTS PASS** (decide-response: restart budget honored, exhausted budget
  takes BL-144 halt path).
- `bb swarmforge/scripts/test/bl1492_restart_in_place_property_runner.bb`:
  **ALL PROPERTIES HOLD**, 150 runs/property, generator coverage confirmed
  (p1/p2 both verdicts+outcomes, p3 both base verdicts, p4 all 3 at-kinds
  :valid/:future/:malformed).
- Full property lane (`cd extension && npm run test:properties`): running in
  background at time of writing; the HOLD pass recorded 384/384 files,
  1128/1128 tests pass, exit 0, one BL-871 benign timeout. No code changed
  since; expected unchanged.
- `test_handoffd_supervisor.sh` (unmodified, as shipped): still fails at case
  02 under plain invocation (BL-1498 red 1: zsh PATH shadowing). Under
  `ZDOTDIR=<empty dir>` isolation: cases 01/03/04/recovered/05/06 PASS, case 02
  fails identically to unmodified `main` — same signature BL-1498 owns, now
  confirmed by the register row to also cover BL-081 scenarios 01/03 (the same
  fresh-pid-file startup-grace bug). **This is now an owned red, not a veto.**
- Required wiring: `restart_history` written by `restart-daemon!` and rendered
  by `format-failure-log`'s `"restart_history: " (pr-str restart-history)` line
  — confirmed. `specs/pipeline/steps/bl1492RestartInPlaceSteps.js` is a
  top-level `*Steps.js` file, auto-discovered by `index.js` — confirmed.
- Ancestry: HEAD (`e430830319`) has documenter's `4b952f505b` as its second
  parent, which has hardender's (`7cbd0dfcaa`), architect's (`b6e7098705`),
  cleaner's (`09516e67f8`), and coder's (`a21f9b2233`) commits as ancestors —
  genuinely this ticket's own chain, not a sibling's commit standing in for it.
- `bounce_history`: one bounce recorded (architect → coder, D1, class
  `behavior`, commit `41d0c7fe6c`), fixed by coder's `a21f9b2233` and
  independently re-verified by architect's second pass
  (`BL-1492-architect-20260908-2.md`, NONE) — resolved, no rework needed.

## Disposition

BL-1492's own deliverables are fully verified and clean. The blocker that held
the parcel at 14:01Z is now owned by BL-1498 (a paused ticket, not yet in the
pipeline). This parcel is **APPROVED** and ready for landing.

Proceeding with the land step: BL-1241 entangled-tip check, then land on main,
broadcast merge-up to worktree roles, notify coordinator for bookkeeping.

By QA.
