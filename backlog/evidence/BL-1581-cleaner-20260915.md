# BL-1581 — cleaner review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

Checks run:
- `npx vitest run --config vitest.properties.config.mjs test/bl1358MutantTimeCeilingInvariants.property.test.js` — 15/15-equivalent green, printed `BL-1581 reach map (bl1358): {"positions":3,"minDrawsPerPosition":1}` matching the ticket exactly.
- `./specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1581-*.feature` — 5/5 scenarios green.
- Ran the ticket's own `qa_e2e_procedure` step 3 break-then-restore by hand: trimming `POSITION_CELLS` to `['first','last']` did NOT fail the property test as the ticket requires (`assertReachFloor(counts, POSITION_CELLS, ...)` checked the same array the loop iterates, so a dropped cell vanished from the check along with the loop).
- Fixed in commit 184d2a3a34: introduced `REQUIRED_POSITIONS`, a list independent of the loop-driving `POSITION_CELLS`, and pointed `assertReachFloor` at it. Re-ran the same break: now throws `reach floor: position middle drawn 0 < 1` exactly as the ticket specifies. Restored and re-verified green (property test + all 5 acceptance scenarios).
- `extension/src` and `specs/pipeline/mutationWorker.js` untouched, matching the ticket's constraints.
- No production code, no Stryker mutation target for this ticket (mutation_cost: low, ticket's own note); mutation-site count check not applicable.

No other defect found. Forwarding to architect on commit 184d2a3a34 (test-file-only change, `extension/test/bl1358MutantTimeCeilingInvariants.property.test.js`).
