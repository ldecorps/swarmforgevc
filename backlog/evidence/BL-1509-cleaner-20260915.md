# BL-1509 — cleaner review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Merged coder 6597ef3509 into cleaner (already-ancestor QA merge-up for
  BL-1558 completed separately, merge-only, no forward).
- `npm run compile`: clean.
- `vitest run` (full suite): 620 files / 10507 tests passed.
- `vitest run --config vitest.properties.config.mjs` on
  `bl1509SendDocumentTokenRedactionInvariant.property.test.js`: 4/4 passed.
- `node scripts/crapReport.js src/notify/telegramClient.ts
  src/tools/send-telegram-document.ts`: `parseArgs` (new in this parcel)
  flagged at complexity 8 / CRAP 8.00 despite 100% coverage. Split the
  `--caption` extraction into a new `extractCaptionFlag` helper
  (commit 41719c18d2) — no behavior change, same tests — bringing
  `parseArgs` to complexity 5 / CRAP 5.00 and the new helper to
  complexity 3 / CRAP 3.00. Three pre-existing functions in
  telegramClient.ts (`setChatMenuButton`, `bl568PlanMenuAnswerDrive`,
  `getBotUsername`) also exceed CRAP 6 but are untouched by this parcel's
  diff (verified via `git show 6597ef3509` — the only hunk touching that
  file is the new `sendDocument` function appended at EOF) — out of scope.
- `node extension/out/tools/mutation-site-count.js
  extension/src/notify/telegramClient.ts
  extension/src/tools/send-telegram-document.ts`: telegramClient.ts
  reports 653 sites (over 100), but per the ticket's own FIRM directive
  the upload must extend that existing file rather than a new module —
  a mechanical split would violate the spec and harm a legitimately
  cohesive one-concern (Bot API wrapper) module; send-telegram-document.ts
  is within threshold (65 sites after the split).
- `npx jscpd src --min-lines 8`: no clone pairs involving either changed
  file (48 pre-existing clones elsewhere, unrelated).
- Architecture: `sendDocument` correctly mirrors `sendVoiceNote`'s
  multipart/injectable-postFn/redacted-error shape per spec; the CLI
  mirrors `notify-dead-letters.ts`'s topic-resolution and env-credential
  pattern. No boundary or encapsulation issues found.
