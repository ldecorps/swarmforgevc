# BL-1390 — QA bounce, 2026-09-04

Merged documenter `2d952cd90c` into QA worktree (`9910f75082`). Full
review before sending back (Article 4.4 — complete inventory, one
bounce).

## D1 — scenario 6 (first in-flight amendment) has no step handler; acceptance FAILS

**Failing command:** `specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1390-a-commit-on-the-shared-main-checkout-is-pushed-while-it-still-fast-forwards.feature`
**Commit tested:** `9910f75082` (this QA worktree's merge tip).
**Failure class:** behavior (the feature's own acceptance step, not a
compile/unit-suite issue).

**First error excerpt:**
```
not ok 6 - the hook's own test suite leaves the live repository's remotes and worktrees untouched
  error: Scenario "the hook's own test suite leaves the live repository's
  remotes and worktrees untouched": no step handler matched "Given the live
  repository's origin URL and worktree list are recorded"
  code: 'ERR_TEST_FAILURE'
1..6
# pass 5
# fail 1
```

**What happened:** the specifier's in-flight amendment (`68212c0731`,
18:05:36) added Scenario 6 ("the hook's own test suite leaves the live
repository's remotes and worktrees untouched") to this ticket's feature
file, directly answering QA's first live incident
(`backlog/evidence/QA-bl1390-shared-git-config-origin-clobbered-20260904.md`).
`68212c0731` IS an ancestor of the commit I hold (verified via
`git merge-base --is-ancestor`) — the amended feature TEXT reached me —
but no corresponding step handler was ever added to
`specs/pipeline/steps/bl1390PushWhileFastForwardSteps.js`, so the
acceptance runner throws `no step handler matched` for every one of
Scenario 6's four steps and the suite reads 5/6, not 6/6.

Confirmed the underlying FIX (the guarded `in_fixture()`/`git_q()` helper
in the coder's rework, `46c2a47e24`) is real and does work — ran
`swarmforge/scripts/test/test_bl1390_post_commit_push.sh` directly and
diffed this repository's own `remote.origin.url` before and after: byte-
identical. The CODE is sound; only the acceptance-layer proof of it (the
scenario the specifier explicitly minted for this) was never wired.

**Expected vs observed:** Expected — BL-233 requires a scenario's handler
to land in the SAME commit as the feature file naming it (this ticket's
own `required_wiring` cites BL-233 language for its OTHER anchor).
Observed — the feature text exists, the handler does not.

**Owning role:** coder. This is not a spec-gap (the scenario is concrete,
unambiguous, and independently proven testable — I ran its intent by
hand successfully); it is an ordinary implementation gap in the same file
the coder already owns.

## D2 — the second in-flight amendment (the concurrency/reap fix for QA's
own second incident) is entirely absent from this parcel — no code exists
for it yet

**Not a test failure** — a stale-parcel finding, checked by ancestry.

While root-causing D1, found the specifier had ALSO adjudicated QA's
SECOND live incident today
(`backlog/evidence/QA-bl1390-runaway-test-process-explosion-20260904.md`,
1156 concurrent copies of this same e2e test exhausting the host):
`13f5834285` ("second incident - ... the suite must lock, reap only
dead-owner roots, bound its clock and log its invoker"), committed
18:35:10 — extends invariant 2, adds scenario 07 and e2e steps 8-9 to the
feature file.

```
git merge-base --is-ancestor 13f5834285 HEAD
```
returns false — **not an ancestor of the commit I hold.** Documenter's
forward to QA (`2d952cd90c`, 18:18:05) predates this amendment (18:35:10)
by seventeen minutes; the amendment simply hadn't reached the documenter's
worktree yet when they sent the parcel on. Like `68212c0731`, this
amendment's own commit touches only the ticket YAML, the feature file,
and the engineering prompt articles — no shell/bb code. The actual fix
(a lock, dead-owner-only reaping, a bounded clock, invoker logging) does
not exist anywhere in what reached QA, and per the commit's own account
does not yet exist in the coder's own worktree either (only a `Merge main
... for BL-1390's second amendment` receipt, not an implementation).

**Owning role:** coder (same file family: `push_sweep_lib.bb`/
`post_commit_push.bb`/`test_bl1390_post_commit_push.sh`, plus a new
step-handler addition for scenario 07 to avoid repeating D1).

## Everything else checked, clean (Article 4.4 full inventory)

- `npm run compile` — clean.
- `bash swarmforge/scripts/test/bl1390_post_commit_push_mutation_sweep.sh`
  — 6/6 killed, 0 survived, 0 skipped (matches hardener's own claim,
  independently re-run).
- `bb swarmforge/scripts/test/bl1390_post_commit_push_property_runner.bb`
  — ALL PROPERTIES HOLD (123 constructed states).
- `bb swarmforge/scripts/test/push_sweep_lib_test_runner.bb` — ALL PASS.
- `bash swarmforge/scripts/test/test_handoffd_push_sweep_wiring.sh` — ALL
  PASS (3/3 real-daemon scenarios).
- `bash swarmforge/scripts/check_feature_handler_registration.sh` — rc 0
  (this guard checks registration/reachability, not "every scenario has a
  handler" — it did not and could not catch D1).
- No `extension/src` touched — CRAP/DRY N/A, matches hardener's own note.
- No orphaned test/mutation processes in this worktree before or after.
- `required_wiring`'s two anchors both confirmed present (the hook →
  `push_sweep_lib.bb` consumer link, and the handler file's own
  `registerSteps` export) — neither anchor's literal presence proves
  Scenario 6/7's steps are wired, which is exactly D1/D2's gap.

## Disposition

Bounce to **coder** with both items. The specifier is independently
adjudicating both incidents QA raised (confirmed live — a coder worktree
merge already picked up the second amendment,
`4e9d3300cf`), so this bounce likely formalizes work already in motion
rather than surprising anyone; sending it anyway per protocol, since the
PARCEL I hold does not yet reflect either fix and must not be approved as
though it did. Full remaining chain (cleaner → architect → hardener →
documenter → QA) re-runs once both items land, so acceptance and the
mutation sweep get proper coverage of the new scenarios too.

By QA.
