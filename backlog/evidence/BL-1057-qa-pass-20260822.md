# BL-1057 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-1057 — the host switchover doctor
- **Parcel**: `00_20260822T221544Z_000476_from_documenter_to_QA_for_QA` (from
  documenter, commit `cfc251d35e`)
- **Verified at**: `f6a0cea378` (my QA worktree tip after this pass) — this
  evidence file is committed on top; the forward names that commit.
- **Reviewed by**: QA, 2026-08-22
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-1057`
  → exit 0, `VERIFY BL-1057`. No open deferral.

## Verdict

**PASS — inventory items: NONE.**

Every gate below was RUN, none blocked, none omitted.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — coder `6f4c92ef8` / cleaner / architect `30d42093e1` / hardener `f0eb88878` + fix `452a6f4a7` / documenter `18857fc67` all confirmed ancestors of the cited commit | PASS |
| G2 | Prior-bounce check against `main` and `origin/main` (BL-891) | PASS — no prior QA bounce for BL-1057; local `main` is 1 commit behind `origin/main` on an unrelated BL-1077 commit, neither touches BL-1057 |
| G3 | Compile (no TS in this parcel — pure Babashka/JS) | N/A, confirmed by diff: only `.bb`, `.js` (step handler), `.md`, `.feature` files touched |
| G4 | `bb swarmforge/scripts/test/host_switchover_doctor_lib_test_runner.bb` | PASS — ALL TESTS PASSED |
| G5 | `bb swarmforge/scripts/test/bl1057_host_switchover_doctor_property_runner.bb` | PASS — ALL 60 RUNS PASSED, all 4 row-1 floors (`:row1-healthy 21 :row1-absent 12 :row1-stale 15 :row1-unreadable 12`) and both oracle checks cleared — independently confirms the specifier-reported degenerate-generator defect (row 1 stuck, never reaching STALE/BLOCKED) is genuinely fixed, not merely re-measured |
| G6 | Acceptance (`run_acceptance.sh specs/features/BL-1057-the-doctor-names-what-a-host-move-left-behind.feature`) | PASS — 11/11 |
| G7 | `required_wiring` (`docs/index.md` links `how-to/BL-1057-host-switchover-doctor.md`) | PASS — confirmed by direct grep, `docs/index.md:34` |
| G8 | Wiring is real, not just the lib (BL-419 shape) | PASS — `host_switchover_doctor.bb`'s `-main` is a thin CLI wrapper; the acceptance steps drive the real compiled lib; live-host run below drives the real CLI end to end |
| G9 | Step-handler registration present | PASS — `specs/pipeline/steps/index.js` registers `bl1057HostSwitchoverDoctorSteps` |
| G10 | Docs currency (documenter's own pass) | PASS — `docs/how-to/BL-1057-host-switchover-doctor.md` (coder-authored, matches shipped behavior exactly) and `docs/reference/Specification.MD`'s changelog entry (`18857fc67`, the documenter's own commit) both read accurately against the live run below |
| G11 | Functional change present (not manifest/format churn) | PASS — new CLI, new lib, new tests, new docs |
| G12 | Orphaned test/mutation processes before and after | PASS — `pgrep -fl 'node --test\|stryker'` clean; the only `bb` processes running are long-lived daemons (`handoffd`, `front_desk_supervisor`, `operator_runtime`, etc.), not orphaned test runs |
| G13 | Babashka tooling-fallback correctly recorded, no fabricated CRAP/mutation claim (Startup Tools) | PASS — hardener's evidence explicitly states no mutation/CRAP/DRY is wired for this lane and none is claimed; matches this ticket's own note |

## `qa_e2e` — every step run, live against this real host

1. `bb swarmforge/scripts/host_switchover_doctor.bb /home/carillon/swarmforgevc`
   → all 7 host-pinned locations **OK**, exit 0. **This differs from the
   ticket's own `qa_e2e` steps 1–3**, which expected the three tunnel
   locations MISSING and a non-zero exit — the architect's own pass already
   recorded this exact drift (`backlog/evidence/BL-1057-architect-pass-20260822.md`):
   the live Error 1033 casualty this ticket was filed against was hand-repaired
   between ticket-authoring and this parcel, so the tunnel registry now
   exists on this host. Independently re-confirmed the same drift myself, not
   taken on the architect's report.
2. `git status --short` in `/home/carillon/swarmforgevc` before and after the
   run: unchanged by the run itself. The main checkout does carry pre-existing
   unrelated WIP (other in-flight tickets' backlog edits, an unrelated
   `.vscode/settings.json` re-indent from an earlier hand-edit) — none of it
   touches the doctor's own declared inventory locations, and the doctor's own
   source contains no `spit`/write call anywhere (grepped directly), confirming
   invariant 1 independent of the property suite's fixture-only proof.
3. Re-ran with an injected non-existent root
   (`bb ... /some/other/fake/root`): `~/.swarmforge/tunnels/operator-root`
   correctly flips to **STALE**, quoting the real root it found
   (`/home/carillon/swarmforgevc`); `.swarmforge/operator/named-tunnel.env`
   correctly reports **MISSING**; both name
   `docs/how-to/named-tunnel-bubble-musicalsifu.md` as the remediation; exit
   code **1**. Matches invariant 3 (every non-OK finding names a concrete
   location and a remediation step) against a live filesystem, not only the
   property suite's fixtures.

## Sibling content riding in this commit (surfaced, not swept)

Both cleaner and hardener are batch roles, and documenter processes tasks
sequentially on one persistent branch, so the cited commit's ancestry is an
entangled multi-ticket lineage. Besides BL-1057 it carries:

- **BL-1015** (boy-scout-run) — its own coder→documenter chain, forwarded to
  QA in the SAME parcel batch as this ticket (documenter processed BL-1015
  then BL-1057 in sequence, one task at a time). **I bounced BL-1015 in this
  same session** (`backlog/evidence/BL-1015-...-bounce2-20260822.md`, commit
  `91e45bcdbd`, two items: CRAP<=6 unmet on 6 functions in new code, and
  unresolved/unproven mutation survivors on `lineDiff.ts`) — recorded in both
  the ticket's own `bounce_history` and the durable JSONL store. Per the
  `is_qa_ancestor.sh`/BL-952 design (`swarmforge/handoff-protocol.md` §"QA
  ancestor is bounce-aware"), a commit naming a recorded bounce verdict reads
  as refused regardless of plain ancestry, so `91e45bcdbd` being reachable
  from this landing does not read as QA-approved anywhere downstream.
  **BL-1015 is NOT approved by this verdict** and is not this parcel's own
  task; it re-enters the pipeline at hardener per my bounce handoff.
- **BL-1029, BL-1069, BL-1075, BL-991** — each has a coder pass and an
  architect pass riding in this history (`backlog/evidence/BL-{1029,1069,1075,991}-{coder,architect-pass}-20260822.md`)
  plus a shared hardener commit (`f242fe621`, "harden — fix a third
  generator-seeding defect, run BL-113 across all four"). None has reached
  documenter or QA yet — their own parcels are still in flight and **are not
  approved by this verdict**.
- Assorted specifier/coordinator bookkeeping on `backlog/{active,paused,hold,topics}/`
  (promotions, pauses, an epic consolidation touching BL-1052/1053/1077-1082)
  — ticket-state bookkeeping, not code this review scope covers.

Per "An Approval Authorizes Only Its Ticket's Work" (BL-506) and Article 2.6,
**this approval names BL-1057 only.**

By QA.
