# BL-1317 — documenter pass (20260902)

Received: hardener commit `533da24a41` (BL-1317: hardener pass - kill 10
real mutants, fix CRAP regression, fix two BL-1066-class sandbox path bugs),
forwarded unchanged from architect's clean sweep `09c6ce0ac8` on top of
cleaner's clean sweep `df349fbc73` / coder's self-audit `cfdb532b64`.

## Review inventory

- Read `backlog/active/BL-1317-adapt-tier-effort-from-outcome-signals.yaml`,
  `specs/features/BL-1317-adapt-tier-effort-from-outcome-signals.feature`,
  and the architect/cleaner/hardener evidence files.
- Read the shipped production code
  (`extension/src/tools/effortDialAdapt.ts`,
  `swarmforge/scripts/seat_difficulty_lib.bb`,
  `swarmforge/scripts/handoff_lib.bb::record-effort-adapt!`,
  `swarmforge/scripts/done_with_current_task.bb::record-effort-adapt-for!`)
  to confirm the user-visible/operator-visible behavior actually shipped
  before documenting it, per the standing "doc already accurate ≠ doc pass
  done" / "check git log first" discipline.
- No diagram in the registry (`architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`) depicts per-seat effort
  tuning, pipeline topology, or the handoff file lifecycle in a way this
  ticket changes — none of the four change-triggers fired. No diagram
  update required.
- No production code or test changes needed — doc-only ticket.

## Doc changes made (D-none: no defects found, all as new authored content)

1. New how-to `docs/how-to/BL-1317-adapt-tier-effort-from-outcome-signals.md`
   (Diátaxis how-to mode), mirroring the sibling BL-1316 how-to's shape,
   linked from `docs/index.md` in the same commit.
2. `docs/reference/Specification.MD`: updated the existing "Effort dial"
   passage (its claim that "The Adapt/Auto tiers below remain unbuilt" was
   stale — Adapt's reasoning-effort half is now built), added a new
   "Adapt-tier effort follows outcome signals (BL-1317)" paragraph next to
   the existing BL-1316 paragraph, and updated the "Tiers of autonomy"
   table's Adapt row to describe what actually shipped (automatic
   reasoning-effort retuning, not a surfaced prompt; model escalation still
   unbuilt) rather than leave the original forward-looking vision text
   uncorrected. Prepended a new top-of-file "Last Updated" changelog entry
   dated 2026-09-02 (today) ahead of the existing BL-1334 entry, per this
   file's running-changelog convention (`Prior entry —` chain) and the
   "Last Updated" role rule (bump only with a content change, same
   commit).
3. No retirement: nothing this ticket does deprecates prior behavior, so no
   `docs/deprecated/` move applies.

## Verdict

NONE — no documenter-domain defect found in the inbound parcel. This pass's
own output is the doc changes above, committed as `c19fecbe68`.

By documenter.
