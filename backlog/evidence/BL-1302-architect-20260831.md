# BL-1302 — architect pass, 2026-08-31

Task: `BL-1302-a-reverse-hop-copy-is-not-a-duplicate-chain`
Received: coder's `a2c09f69b1` directly (cleaner deliberately skipped —
ticket declares `stage_skip_reasons: cleaner: "one predicate in an existing
guard; no structure to refactor"`).

This is the exact bug that blocked my own BL-1308 forward earlier this
session (specifier's ticket note cites it directly). Reviewed with extra
care given that history, not less.

## Verdict: CLEAN — forward to hardener

### What changed
- `handoff_lib.bb`: new `non-forwarding?` — the ONE spelling of the
  reverse-hop marker check (absence, `false`, and any non-`"true"` value
  all fail closed).
- `duplicate_chain_guard_lib.bb`: `git-handoff-for-ticket?` now also
  requires `(not (non-forwarding? file))` — skips a candidate, doesn't
  abort the walk, so a genuine competing chain sitting behind a reverse
  copy is still found and named.
- `swarm_handoff.bb`: `inbound-non-forwarding?` now calls the shared
  predicate instead of a second inline spelling.

### Architecture rules
No extension/webview/TS code touched — confined to three `.bb` production
files, matching the ticket's own "additive predicate, no new mechanism"
framing. Two-layer boundary etc.: N/A.

### Hard gate — dependency-rule checker / co-change tool
Dependency gate: N/A, nothing under `extension/` in this diff.
Co-change: ran against both changed libs — every co-change count is 1,
well below the frequency-3 threshold. No coupling flagged.

### required_wiring
None declared, and correctly so — this is a predicate inside a function
`swarm_handoff.bb`'s validate step already calls on every send; no new
call site exists to anchor (BL-1235's own vacuous-anchor rule). Confirmed
`specs/pipeline/steps/index.js:654` registers the acceptance handler.

### Checks re-run independently (not taken on trust)
| Command | Result |
|---|---|
| `bb duplicate_chain_guard_lib_test_runner.bb` | ALL PASS |
| `bb bl1302_reverse_copy_not_duplicate_chain_property_runner.bb` | ALL PASS, 200 runs, all 6 generator-reach floors met (incl. `exempt-hides-a-real-blocker`, the shape that matters) |
| `run_acceptance.sh specs/features/BL-1302-….feature` | 3/3 pass |
| `bb handoff_lib_test_runner.bb` | ALL PASS |
| `bash test_swarm_handoff_sync_deliver.sh` | ALL PASS |

### Invariants review (BL-633/654)
Both invariants pinned by ONE property against an independent oracle
(`legacy-blocking-parcel`, a from-scratch reimplementation of the
pre-BL-1302 walk) rather than a second copy of the same logic — a strong
non-vacuity shape. Coder's evidence documents two deliberate breaks (drop
the exemption; exempt on any marker presence rather than the literal
`"true"`) each failing from an early run. Re-ran the full property suite
green above; did not re-break-and-restore myself since the recorded
transcript already demonstrates both directions and the oracle is
independent, not self-referential.

### Pre-existing red — verified already ticketed, not unowned
Coder's claim that `test_reroute.sh` exits 2 pre-existing (unchanged
transcript vs `HEAD`) is corroborated: `grep -rl test_reroute.sh backlog/`
finds it already recorded at `backlog/evidence/BL-1299-coder-20260830.md`.
Not unticketed, no note needed.

### Correctness read
The fix is narrowly scoped to the exact defect: `non-forwarding?` gates
only the CANDIDATE match, not the walk — a genuine second chain hiding
behind a reverse copy is still caught (property's `exempt-hides-a-real-blocker`
generator shape, 50 runs, floor met). No overreach, no defect spotted.

No spec gaps, no send-back. Forwarding to hardener with the same task
name, commit (the merge I received).

By architect.
