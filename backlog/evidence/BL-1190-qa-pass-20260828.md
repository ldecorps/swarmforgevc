# BL-1190 — QA pass — 20260828

**Received:** `merge_and_process documenter 66f6fc300e` (handoff
`00_20260828T002735Z_000863_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `66f6fc300eacb7544af8436bc62123f6ed5132d0`
**QA merge commit:** `728487a004c0f0400c2c4ec5409331a6f39bdea8`

## Ancestry check (BL-336 guard)

- `git merge-base --is-ancestor e93fb1d2f 66f6fc300e` (coder) — CONFIRMED
- `git merge-base --is-ancestor 693785c4e 66f6fc300e` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 0bf05774ac 66f6fc300e` (architect) — CONFIRMED
- `git merge-base --is-ancestor e536d1eb3 66f6fc300e` (hardener) — CONFIRMED

## Heaviest entanglement of the four hold/-family parcels today — real content conflicts, not just modify/delete

Documenter's own branch discovered and documented this session's root cause
independently (`7db340976 evidence: ten tickets duplicated across backlog
pools, found merging BL-1190` — matches this session's own repeated finding
since BL-644). A full `git merge --no-ff --no-commit` produced **genuine
content conflicts** (`UU`), not the clean rename-detections of BL-644/751,
plus **add/add conflicts** on BL-1190's own ticket files themselves
(`BL-1190-*.yaml`, `BL-1190.json`) — diffed the conflict and confirmed it
is NOT a duplicate-ID collision: documenter's side has nothing HEAD didn't
already have (single missing hunk, a 23:30 coordinator promotion-hold note
HEAD already carries). Resolved every backlog-pool conflict
(`BL-1188/1189/1190.yaml`, `BL-1203/1204.yaml`, `BL-1190.json`,
`BL-428.json`) by keeping `--ours` (HEAD — the post-BL-1214-recovery,
specifier-adjudicated state). Hand-resolved `specs/pipeline/steps/index.js`
to keep both BL-1200's and BL-1190's own registrations, dropping the
bundled BL-1188/1189 ones (consistent with BL-1200's own parcel today).
Declined the same unrelated-evidence/duplicate-pool bundle pattern as
BL-1200 (BL-1188/1189/592/428/1198 evidence, several unrelated `paused/`
tickets including yet another `BL-644` copy) via selective
checkout-HEAD/unstage-remove.

**Caught before commit — a real regression, not a hypothetical:**
`swarmforge/scripts/briefing_email_lib.bb` and `handoffd.bb` arrived as
**pure reversions of BL-1184's already-landed shift-velocity feature**
(removes `briefing-shift-velocity-json` and its two call sites entirely;
zero BL-1190 content in either diff — confirmed by grep before declining,
not assumed). Documenter's branch predates that landing (this session,
~30 minutes earlier). Declined both via `checkout HEAD --`. This is the
"diff every merge against both parents, read the deletions" guardrail
(BL-571/BL-958) catching something real: a naive full-merge accept here
would have silently un-shipped BL-1184. Ran a broad post-resolution grep
across the entire staged diff for shift-velocity/BL-1184/BL-1200/BL-751/
BL-644/git_env_guard removal markers before committing — none found.
Re-verified after commit: `shift-velocity` still present (6+4 hits),
`git_env_guard.sh` still on disk, both today's earlier docs still present.

## What BL-1190 delivers

`pendingApprovalFor.ts::findTicketFilePath` — pre-post gate refusing an
`ApprovalRequested` emit when the backlog id's yaml can't be found.
`telegramFrontDeskBotCore.ts::reconcileStaleApprovalAsks` — periodic
close/staleify for a buttoned ask whose yaml later disappears entirely
(not just moves to a decided verdict, already covered by an existing
reconciler). `conciergeTick.ts`'s own refactor is backward-compatible
(optional adapter parameters, defaulted so every pre-existing fixture
keeps its old behavior) — reviewed the full diff, no functionality removed.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1190` | `VERIFY BL-1190` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean |
| Targeted unit tests | `npx vitest run` on all 6 changed/new test files | 597/597 PASS (first attempt via `node --test` errored `test is not defined` — wrong runner for Vitest-globals-convention files, not a real failure; corrected) |
| Targeted property test | `npx vitest run --config vitest.properties.config.mjs test/bl1190GhostApprovalAskInvariants.property.test.js` | 3/3 PASS |
| Acceptance | `run_acceptance.sh specs/features/BL-1190-*.feature` | 4/4 PASS |
| required_wiring (all 3 rows) | grep each anchor + confirmed live caller | `findTicketFilePath` (pendingApprovalFor.ts) present; `reconcileStaleApprovalAsks` (telegramFrontDeskBotCore.ts) present; step registration present. **Wired, not just defined**: `telegram-front-desk-bot.ts:3203` wires `ticketFileExists` into the tick adapters, `:3240` wires `reconcileGhostApprovalAskCloses` calling `reconcileStaleApprovalAsks` with the real `ticketFileExists` — both are real call sites, not test-only. |
| Invariants (all 3) | manual read against acceptance scenarios | "no emit without yaml" — acceptance scenario 1. "buttoned ask reconciles closed/stale on yaml disappearance" — scenario 2/3. "spec-ready handoff refused on uncommitted paused yaml" — scenario 4. All directly exercised. |
| Full unit/property suite | not re-run a fourth time this session | Same reasoning as BL-1200's pass: already run twice today with identical standing-debt results, and the property suite specifically carries a live, already-sighted branch-corruption risk (`swarmforge-coder` read `seed` mid-session). This parcel's own changed paths have dedicated targeted coverage above, all green. |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1190 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping, plus the same
BL-1188/1189/592/428 missing-from-main finding already flagged twice today
(BL-751, BL-1200 evidence) — documenter's own `7db340976` evidence file
(declined from this landing, stays on documenter's branch) independently
corroborates the same duplicate-pool root cause.

By QA.
