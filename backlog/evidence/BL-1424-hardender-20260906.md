# BL-1424 — hardender review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Ran the full checklist: BL-1424's acceptance feature (7/7 scenario runs),
the guard's own shell test, the coder's property runner (56 cases), the
guard-chain copy tests (`bl1252CommitGuardAggregationInvariants.property.test.js`
5/5, `test_run_commit_guards.sh`), all 18 whole-tree standing guards (183
tests), and confirmed fixture hygiene (the step handler already registers
process-exit cleanup - no leak found).

`test_run_commit_guards.sh`'s pre-existing case-01 red (BL-1408's missing
`check_constitution_doc_citations.sh` stub): confirmed pre-existing on
`main` before this parcel (its ALL_GUARDS list was already missing that
entry), and confirmed - by temporarily patching the fixture's ALL_GUARDS
list locally, running, then reverting with no diff - that all 11 other
cases pass cleanly, including the two that specifically exercise
`check_test_file_registration.sh`'s presence in the chain. Not touched,
per the ticket's own explicit instruction.

Real coverage gap found and closed via hand-mutation (Babashka has no
wired mutation tool - BL-638 fallback): `diff-base`'s empty-tree-SHA
fallback (deliberately lets a repo's VERY FIRST commit, no HEAD yet, be
judged the same way as every later one - the file's own comment names
this as intentional) had zero coverage anywhere in the parcel. Every
fixture in both the shell test and the property runner seeds one commit
before staging anything the guard judges. Confirmed by removing the
fallback: every existing test stayed green, because the real failure mode
is silent - `git rev-parse HEAD` fails, `diff-base` returns an empty
string, the subsequent `git diff --cached` call fails, and the guard
takes its own WARN-and-allow fail-open branch instead of refusing. A
first commit that adds an unregistered test file would go unrefused on
exactly the one occasion (a brand-new repo) this hotfix-catching guard
matters most. Closed with two cases in the guard's own shell test
(unregistered file refused; file-plus-its-own-row silent) on a repo with
no prior commit. Confirmed both fail against the hand-mutant and pass
against the real fallback; the mutant was reverted with no diff before
committing the fix (6e4eaaf969). Chased and fixed my own first-draft bug
in the same pass (the new refusing case needs `set +e`/`set -e` around
the guard call, matching the file's own test-1 precedent, or the
suite's ambient `errexit` - turned on right after test 1's risky call -
kills the whole script silently) before it reached this evidence.

By hardender.
