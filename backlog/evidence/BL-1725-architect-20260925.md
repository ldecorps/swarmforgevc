# BL-1725 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Checks run:
- `npx vitest run --config vitest.properties.config.mjs test/bl1650LandStepPureEvidenceStrayInvariants.property.test.js`:
  2/2 passed, invariant 2 ~3.9s (was 10.8s alone at load 12.4) — well under
  half, matching the ticket's expectation.
- Floor audit (qa_e2e step 2): `git diff main...HEAD -- extension/test |
  grep -E '(numRuns|assertReachFloor|runsPerCell)'` — `runsPerCell(3 *
  shapes.length, shapes.length)` and `shapes = [1, 2, 3]` unchanged for
  invariant 2; the removed `{ numRuns: RUNS }` line is structural (the
  per-draw `fc.assert(fc.property(...))` wrapper was replaced by
  `fc.sample(..., RUNS)` + a plain loop, since invariant 2 no longer
  decides one draw per `fc.property` call — same RUNS constant, same
  shape count, no value lowered).
- `node out/tools/dependency-gate.js` / `co-change-report.js` on the
  touched file: PASSED, no forbidden edges; coupling only with BL-1650's
  own historical evidence and the lib/CLI it drives.
- `node specs/pipeline/cli.js specs/features/BL-1650-the-land-step-lands-a-pure-evidence-closed-owner-stray-itself.feature`:
  8/8 ok.

Correctness read: `landPlans` (the shared-bb-process path for every draw
but the wiring case) calls `land-step-lib/land-plan` with the exact same
argument shape (`{:root :commit :task-ticket-id :origin-main}`) that
`land_step_cli.bb`'s own `main-land` passes it — read both call sites side
by side to confirm this is the same decision function, never a JS
restatement or a divergent code path. The wiring draw still runs the real
CLI end to end (argv/stdout), matching the ticket's FIRM constraint. Every
draw's assertions read real git state afterward (`git log -1 --format=%B`
on the landed sha, checked for the `-x` cherry-pick trailer) against the
actual fixture repository on disk, not a JS-side simulation — this is
strong non-vacuity evidence: a broken land-plan producing a wrong action,
a missing stray entry, or a landed commit without the trailer would fail
these assertions against real git output, not a mock.

The per-draw nonce fix (`28cac5192f`, folding a draw id into the incident
content to keep commits distinct across draws sharing one fixture root)
is correct: `PURE_PATH_POOL` has 4 entries and 3 draws/shape, so two draws
picking the same path subset would otherwise build an identical tree off
the same seed — an identical commit sha colliding with land-plan's replay
scratch (keyed by task-ticket-id + commit-sha prefix). The nonce is folded
into the incident file's own content, so it changes the resulting sha
without touching the path set the invariant is exercising.

By architect.
