# BL-1650 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail (fourth pass, final)

### Gates run

- `npm run compile`: clean.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1650-the-land-step-lands-a-pure-evidence-closed-owner-stray-itself.feature`:
  6/6 scenarios green, including new scenario 06 (the tip-content fix).
- Re-verified `specs/features/BL-1546-a-path-owned-only-by-a-closed-ticket-is-never-silently-excluded.feature`:
  4/4 green (SHARED_PATH fixture regression fixed for the third and hopefully final time this shift).
- Re-verified `specs/features/BL-1544-an-ambiguous-commit-subject-never-silently-excludes-a-path.feature`:
  4/4 green.
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1650 <commit>`: OK.
- `npm test`: 635/635 files, 10819/10819 tests green.
- `npm run test:properties`: 432/432 files green, 1262/1262 tests green (two BL-871 allowlisted benign
  timeouts, not defects).

### Landing — confirms the fix works

Landed directly from this branch's own `land_step_cli.bb` (this pass's own delivered content IS the
fix, safe to self-host). `LAND_REPLAY` correctly resolved ~70 historical closed-owner strays as
`LAND_STRAY_EVIDENCE_ALREADY_LANDED` via tip-content comparison — none required a cherry-pick attempt,
none escalated on stale commit-content diffs. This directly confirms the D1×2 fix (tip content, not
commit content) works as intended in a real, large-scale scenario. `BL-1630`, `BL-1656`, `BL-1660`
correctly named `ENTANGLED_SIBLING` (genuinely unlanded). Diff against `origin/main` inspected directly:
only BL-1650's own paths (all four passes' evidence) plus one harmless untagged-touch passenger
(BL-1315). Landed `90c7b53cca` in place of `0cd79ce4f5`; `abandoned_commits` recorded on the ticket.

This closes out BL-1650 after three bounces this shift (D1 already-applied-cherry-pick fix; a
regressed fixture path; a spec-gap on commit-vs-tip content) — full history in
`backlog/active/BL-1650-...yaml`'s `bounce_history` and the four `BL-1650-bounce-20260920*.md` files.
