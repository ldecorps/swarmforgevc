# BL-1457 — hardender review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Test-only parcel (mutation_cost: low, two spawnSync env-object call sites
in two property files; no production line changes). No `src/*.ts`
touched, so CRAP/DRY do not apply (both tools scope to production source).
Host quiet: load avg 2.6 on 20 cores; no orphaned test/mutation processes
before or after.

Hand-authored mutation (per BL-638/no-wired-tool posture for these two
call sites — Stryker's `--mutate` scope is `out/**/*.js` production code,
never `extension/test/**`): reverted each fix to its pre-fix shape one at
a time, ran the file under `env -u PACK_STAFFING_SKIP_GATE`, confirmed
red, restored, confirmed green, confirmed `git status --short` clean
after each cycle.

- `bl1218RemoteControlConfigInvariants.property.test.js`: reverted
  `writeLaunchScript`'s `env: { ...process.env, PACK_STAFFING_SKIP_GATE: '1' }`
  back to bare `{ encoding: 'utf8' }`. Result: `AssertionError: no launch
  script was written for config off` (1 failed, 3 passed). Restored → 4/4
  green.
- `bl1320DocumentedStepsAreExecutedInvariants.property.test.js`: reverted
  the added `PACK_STAFFING_SKIP_GATE: '1'` key out of `parsePack`'s `env`
  object. Result: 1 failed, 1 passed. Restored → 2/2 green.

Both mutants killed by the fix itself, confirming each added key is
load-bearing under the exact env-unset shell the ticket exists for (QA's
offline expedite shape).

Re-verified on this tree: `bash specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1457-every-test-that-drives-parse-config-decides-the-staffing-gate-itself.feature`
→ 11/11 pass (all four scenarios, including the real per-file vitest
spawns in scenarios 01/03).

Not run: Stryker/CRAP/DRY — no production file in the diff (`git diff
main..HEAD --stat` shows only test files, the new step-handler file, one
evidence file per stage, and the `standing-reds.tsv` row removal).
