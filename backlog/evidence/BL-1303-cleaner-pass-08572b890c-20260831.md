# BL-1303 cleaner pass — 2026-08-31

Reviewed commit `ab46787808` (coder), merged into `swarmforge-cleaner` as
`08572b890c` ("Merge coder BL-1303 ab46787808 into cleaner. By cleaner.").

Scope: `swarmforge/git-hooks/pre-merge-commit`,
`swarmforge/scripts/run_commit_guards.sh`,
`swarmforge/scripts/commit_guard_chain_lib.sh` (new),
`swarmforge/scripts/check_feature_handler_registration.sh` (header only),
`specs/pipeline/steps/bl632CommitTimeGuardSteps.js`,
`extension/test/bl632CommitTimeGuardInvariants.property.test.js`,
`swarmforge/scripts/test/test_pre_merge_commit_hook.sh` (new),
`swarmforge/scripts/test/test_pipeline_code_on_main_guard.sh`,
`swarmforge/scripts/test/test_run_commit_guards.sh`,
`swarmforge/scripts/test/suite-manifest.tsv`.

This is coder's remediation of the architect's D1 bounce (fbcc7b7712):
`pre-merge-commit` now runs `check_feature_handler_registration.sh` beside
`check_pipeline_code_on_main.sh` on the `--no-ff` merge path, with the
guard-chain aggregation (`run_guard`/status-capture/report) extracted to a
shared, sourced `commit_guard_chain_lib.sh` rather than duplicated between
`pre-commit` and `pre-merge-commit` — closing exactly the DRY gap the
specifier's ruling (`BL-1303-specifier-ruling-cleaner-forward-blocked-20260831.md`)
named for cleaner/architect to weigh on the way back up. Also closes a
fail-closed gap of its own: an unloadable `commit_guard_chain_lib.sh` now
refuses the commit/merge instead of leaving `run_guard` undefined and
falling through to `exit 0`.

## Checks

- `bash swarmforge/scripts/test/test_pre_merge_commit_hook.sh`: 9/9 PASS
  (new regression suite covering the real `--no-ff` merge path).
- `bash swarmforge/scripts/test/test_run_commit_guards.sh`: 12/12 PASS.
- `bash swarmforge/scripts/test/test_check_feature_handler_registration.sh`: 7/7 PASS.
- `bash swarmforge/scripts/test/test_pipeline_code_on_main_guard.sh`: all
  cases PASS except the pre-existing, out-of-scope
  "BL-925 invariant 2: handoffd.bb still runs its own inline ancestry git
  call" — `swarmforge/scripts/handoffd.bb` is untouched by this parcel
  (byte-identical to `main`), already reported to the specifier by coder's
  commit message on `652603514d`, not this parcel's to fix.
- `npx vitest run --config vitest.properties.config.mjs bl632CommitTimeGuardInvariants`:
  1/1 PASS.
- `npx vitest run test/checkFeatureHandlerRegistrationCli.test.js
  test/featureHandlerRegistrationCheck.test.js`: 29/29 PASS, unchanged by
  this parcel.

## DRY / structure / mutation-site review

- The only duplication concern raised against this parcel (`run_guard`/
  refusal-reporting hand-rolled twice) is already resolved by coder's own
  extraction to `commit_guard_chain_lib.sh`; nothing further to DRY.
- Bash hooks and scripts carry no Stryker/CRAP/DRY tooling (TypeScript-only
  per the Startup Tools article); `check_feature_handler_registration.sh`'s
  diff is a header-comment correction only, no logic change.
- `extension/src/tools/featureHandlerRegistrationCheck.ts` is untouched by
  this parcel — no new mutation sites to count.

## Verdict

NONE — no cleanup defect found. Forwarding `08572b890c` plus this evidence
commit to architect unchanged.
