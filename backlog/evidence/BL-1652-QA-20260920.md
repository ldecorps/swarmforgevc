# BL-1652 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

### Gates run

- `npm run compile`: clean.
- `bash swarmforge/scripts/test/test_chase_sweep.sh`: ALL PASS (17 cases).
- `bash swarmforge/scripts/test/test_handoffd_chase_sweep_wiring.sh`: ALL PASS (3 cases).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1652-the-chase-sweep-never-respawns-a-busy-role-and-respawns-at-most-once-per-sweep.feature`:
  4/4 scenarios green.
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1652 <commit>`: OK (required_wiring anchor present).
- `npm test` (extension unit lane): 634/634 files, 10811/10811 tests green.
- `npm run test:properties`: 428/428 files green, 1251/1251 tests green (three `[vitest-worker]:
  Timeout calling "onTaskUpdate"` unhandled errors, the BL-871 allowlisted benign error, not a defect).

### Lineage / attribution

- Reviewed lineage confirmed per the specifier's ruling (89198196ee): coder 43213a6bc2 → cleaner
  0ace704af1 → architect 1c9677b6b2 → hardener 5bee934afc/5f30ea5596 → documenter e67b7f6b9b, all
  ancestors of this HEAD. The duplicate coder@2 build (4eacde9068) is recorded under
  `abandoned_commits:` on the ticket per the specifier's ruling — not part of this lineage, never
  forwarded, nothing to reconcile (diverges in implementation shape from the reviewed lineage).
- No standing-red register row owned by BL-1652.
- The historical unowned-red sighting (`unowned-red-bl1647-stepHandlerTmpRootGuard-20260919.md`)
  self-resolved via merge before this parcel reached QA (confirmed by the coder's own update note in
  that file, and independently: `npx vitest run test/stepHandlerTmpRootGuard.test.js` — 4/4 green on
  this HEAD).

### Cross-ticket entanglement with BL-1653 — land BL-1652 first

Both BL-1652 and BL-1653 modify `swarmforge/scripts/handoffd.bb`. BL-1653's tip-pure replay attempt
failed `check_bb_scripts_load` (`handoffd.bb` does not boot standalone) because BL-1653's own-paths
carry the CURRENT merged content of `handoffd.bb`, which already calls BL-1652's new
`swarmforge/scripts/lane_process_lib.bb` — a path BL-1653 does not itself own, so it is excluded from
BL-1653's replay. This is genuine file-level entanglement, not a bug: BL-1652 lands first (this pass),
then BL-1653's replay retry will find `lane_process_lib.bb` already on `origin/main`.
