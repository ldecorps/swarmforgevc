# BL-1584 — hardender review pass, 2026-09-16

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: cd extension && npx vitest run test/tempDirTrapGuard.test.js
- **Commit hash**: 797746ff79
- **First error excerpt**: swarmforge/scripts/test/bl1584_sampled_reach_floor_property_runner.bb: creates a temp root (fs/create-temp-dir) but has no shutdown hook and no try/finally delete-tree
- **Failure class**: test-infra-leak
- **Expected vs observed**: zero temp-dir-trap violations under swarmforge/scripts
- **Blamed role**: hardender
- **Remediation pointer**: add a JVM shutdown hook beside the existing unconditional delete-tree, guarded with fs/exists? on both cleanup sites, so an exception before the final line cannot leak the fixture root

By hardender.
