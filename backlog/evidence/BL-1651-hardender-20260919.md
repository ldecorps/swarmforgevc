# BL-1651 — hardender review pass, 2026-09-19

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Merge conflict resolution (commit 4be128db22): removed an add/add
  duplicate `inbound-window-start` (BL-1645) in
  `swarmforge/scripts/forward_evidence_lib.bb` - the third occurrence of
  this exact duplicate this session; verified the file loads and its bb
  runner passes.
- `extension/out/` was stale after the merge brought in TS changes
  (`resolvePropertyLaneHeapMB is not a function`), false-failing 4/5
  acceptance scenarios. Recompiled (`npm run compile`); all 5 BL-1651
  scenarios and all 5 BL-871 scenarios green afterward. `out/` is
  gitignored, no commit needed - noted here so the next role does not
  chase a phantom regression.
- Unit tests (`vitestWorkerMemoryBudget.test.js` 42/42,
  `propertyLaneHeapGate.test.js` 6/6) and the property test
  (`bl1651PropertyLaneHeapCeilingInvariants.property.test.js`) reviewed:
  already thorough (explicit non-vacuity checks, cell-coverage discipline,
  boundary cases at the ceiling).
- Stryker mutation on the two changed extension/src files
  (`vitest-worker-memory-budget.ts`, `property-lane-heap-gate.ts`) could
  not complete: the dry run timed out twice under sustained host
  contention this session (load 12-24 on 20 cores, hundreds of concurrent
  fixture processes from other live swarm sessions observed earlier this
  session; not this ticket's code). Fell back to hand-mutation against
  the compiled `out/` files directly (BL-472-style degraded fallback,
  applied here because the tool itself could not run, not because the
  language lacks one): (1) dropped the `Math.max(PER_WORKER_HEAP_MB, ...)`
  floor in `resolvePropertyLaneHeapMB` - caught by the existing "tight
  host never regresses below the floor" test; (2) changed
  `heapCeilingVerdict`'s `<=` boundary to `<` - caught by the existing
  "exactly at the ceiling: not exceeded" test. Both mutants restored
  (out/ is gitignored, no diff to check).
- BL-871's scenario 04 retirement (item 5) verified clean: both the
  Gherkin scenario and its step handler were removed together, no
  dangling reference.
- Whole-tree guards (21 files, 210 tests) and the BL-1636 fixture-leak
  guard specifically: all green, no leak from the new step handler this
  pass.
- No hardening commit produced this pass beyond the merge conflict
  resolution above; forwarding that commit.
