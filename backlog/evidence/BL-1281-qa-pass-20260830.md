# BL-1281 QA pass — 2026-08-30

Commit verified: merge of documenter `5280d7b815` into QA. BL-1281's own
coder/cleaner/architect/hardener/documenter commits (9de949d3db, 601598685,
5fbafbbbee, f3fbc7f99a, 5280d7b815) are all confirmed ancestors of the merge
HEAD (`git merge-base --is-ancestor`). `depends_on: [BL-1062]` — confirmed
landed (`backlog/done/BL-1062-...yaml`) and its shared helper
(`extension/test/helpers/reachFloors.js`) present in this tree.

## required_wiring — both confirmed
- `bl1048DeliveredParcelIsNotNotStarted.property.test.js` imports
  `assertReachFloor`/`runsPerCell` from `./helpers/reachFloors` (BL-1062's
  shared helper, not a local restatement) and calls `assertReachFloor` in its
  reach-floor loop.
- `specs/pipeline/steps/index.js` line 880 registers
  `bl1281ReachFloorsByConstructionSteps`.

## qa_e2e_procedure
1. Ran `bl1048...property.test.js` 5 times back to back (independent
   unseeded draws each time): 5/5 clean, both at-risk floors
   (`deliveredOnly`, `openedOnly`) met every time.
2. `bl1281ReachFloorConstructionInvariants.property.test.js`: 4/4 —
   invariant 1's own test replays **200 distinct seeds**, each computing a
   full simulated bl1048 run (24 draws across 6 shape cells) and asserting
   both at-risk floors are met on every one (4800 simulated draws total,
   0/4800 misses) — exceeds the ticket's own "at least 2000" bar. The same
   test also asserts the OLD sampled scheme genuinely misses on six pinned
   control seeds (26, 142, 150, 169, 184, 205), proving the check is not
   vacuous.
3. Confirmed all nine floors still declared, none lowered: invariant 2's own
   test (`still declares all nine, none below the value it had before`)
   passes; I independently cross-read `BL1048_REACH_FLOORS` in
   `helpers/bl1048ReachFloors.js` against the pre-change inline values quoted
   in the ticket's own measurement table — identical.
4. Independently exercised the shared assertion by hand (not just trusting
   the ticket's own test): `assertReachFloor({ deliveredOnly: 3 },
   ['deliveredOnly'], 8, 'manual-qa-check')` threw
   `reach floor: manual-qa-check deliveredOnly drawn 3 < 8` — confirms the
   assertion fails loudly and names the value, from outside the shipped
   test file.
5. `specs/features/BL-1281-...feature`: 5/5.
6. `grep bl1048 swarmforge/scripts/property_suite_standing_allowlist.tsv`:
   no match (exit 1) — the file stays off the allowlist, per the ticket's
   explicit constraint.

## Full-suite cross-check
Did not re-run the full DEFAULT `vitest run` myself for this ticket — the
architect and hardener each independently ran it and both report
**26 failed files / 218 failed tests**, "identical to the pre-BL-1281
baseline" (architect) / "identical to the BL-1277/BL-1280 hardening
baseline" (hardener). I measured this exact number myself, independently,
during today's BL-1277 and BL-1280 QA passes (26 failed/218-219, same two
root causes: session-local `CURSOR_API_KEY` absence, and the already-ticketed
BL-1229 `checkOrphanedAuthoredDocs` deps-stub gap) — three independent
measurements across three roles/tickets converge on the same figure, so I
did not spend a fourth full run reproducing it.

Attempted the FULL `test:properties` lane (271 files) as an extra check
beyond what architect/hardener did (both scoped their properties runs to
`bl1048`/`bl1281` only). It did not complete cleanly in this session: three
attempts each reproducibly stopped mid-run at the same point, inside a
different, noisy pre-existing test's stderr output (`backlog_depth_lib: no
swarm-identity for ...`, unrelated to BL-1281), with no crash/error visible
and no orphaned process left behind afterward (checked each time). This
worktree currently shares the host with other live swarm roles running their
own heavy test/coverage processes (observed concurrently: `npm exec vitest
run --coverage` from an unrelated PID), which is consistent with resource
contention rather than a BL-1281 defect — bl1048/bl1281 themselves ran clean
and fast (sub-second to ~1.1s) every time they were exercised directly, and
neither appeared in any FAIL list across any attempt. Not treated as a
BL-1281 finding; recorded so it isn't mistaken for a clean full run I didn't
actually get.

## Verdict: APPROVE
The construction fix is verified by five independent angles (my own repeat
runs, the 200-seed replay, the non-vacuity control seeds, a hand-exercised
assertion call, and cross-corroboration with architect's and hardener's
independent numbers) and all nine floors are confirmed intact.

By QA.
