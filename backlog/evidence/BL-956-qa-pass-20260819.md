# BL-956 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-956 — pipeline board caption and cap hotfix (`type: defect`, `severity: high`)
- **Parcel**: `00_20260819T184405Z_000210_from_documenter_to_QA_for_QA` (from documenter, commit `f916f86675`)
- **Verified at**: the QA merge of `f916f86675`; this evidence file is committed on top and the forward names that commit.
- **Reviewed by**: QA, 2026-08-19
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-956` → exit 0, `VERIFY BL-956`. No open deferral.
- **Prior bounces**: one — hardener → coder, class `behavior`, commit `b08d0a817e`, evidence `backlog/evidence/BL-956-hardener-bounce-20260819.md`. **Re-verified below; the fix is real.**

## Verdict

**PASS — inventory items: NONE.**

Every gate below was RUN, none blocked, none omitted.

## The prior bounce (D1) — independently re-verified, not taken on trust

The hardener's D1 was: invariant 3 holds in `renderPipelineBoardBody` but **not** in the actually-shipped `composePipelineBoardHtml`, and all three test layers asserted against the wrong function. Its live repro was 5 epic-tracker paused items against `PIPELINE_BOARD_COLLAPSED_EPICS_MAX = 3`, giving `collapsedEpicsOmittedCount: 2`.

I re-ran that exact repro at this commit:

```
composePipelineBoardHtml contains "more epics"?  true   (was FALSE at bounce time)
renderPipelineBoardBody  contains "more epics"?  true
```

Both paths now agree, and the coverage moved with the fix — `composePipelineBoardHtml` is now referenced 15× in `test/pipelineBoard.test.js`, 5× in the property file and 3× in the step handler, so all three layers exercise the **live** composer rather than the internal body renderer. This is the specific defect the bounce was for, and it is closed.

The bounced content was properly removed from the reviewing branch at the time (`96882d743`, scoped revert per BL-490/BL-495) and the re-fix restored what that revert had dropped (`ab6eaa6f9`, step registration) — verified present at G7 below rather than assumed, since a re-fix merge silently re-applying an earlier revert's deletion is exactly the BL-954 failure mode.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — coder `294d46406a`, cleaner `4c75a67155`, architect `bb8cad57d0`, documenter `f916f86675` all ancestors of the cited commit | PASS |
| G2 | Prior-bounce check against `main` **and** `origin/main` (BL-891) | PASS — one hardener bounce, re-verified fixed above; no unfixed bounce outstanding |
| G3 | `npm run compile` | PASS |
| G4 | Unit — `test/pipelineBoard.test.js` (the changed module's own suite) | PASS — 126/126 in 4.3s |
| G5 | Full unit suite | PASS — 7885 passed / 444 files; 6 failures, all load timeouts, all unrelated to `pipelineBoard` (see "Load caveat") |
| G6 | Property lane — `test/bl956PipelineBoardCaptionCapInvariants.property.test.js` | PASS — 3/3 in 2.9s, one property per declared invariant (message limit, caption context, every cap names its dropped count) |
| G7 | `required_wiring` — `specs/pipeline/steps/index.js::bl956` | PASS — registered at `index.js:519`. This is the item the earlier scoped revert dropped, so it was checked directly, not inferred from a green run |
| G8 | Pre-QA gate | PASS — `pre_qa_gate_cli.bb BL-956-pipeline-board-caption-and-cap-hotfix … .` → `OK` |
| G9 | Acceptance — `run_acceptance.sh specs/features/BL-956-pipeline-board-caption-and-cap-hotfix.feature` | PASS — 6 pass / 0 fail, exit 0 |
| G10 | Acceptance pointer restored (the feature was parked `.draft` during the bounce) | PASS — renamed back to a live `.feature` in this parcel; the ticket's `acceptance:` is a single-line pointer that resolves |
| G11 | Docs currency (documenter's own pass) | PASS — `docs/reference/Specification.MD` (+43) and `docs/how-to/BL-513-pipeline-board-current-folder-links.md` |
| G12 | Functional change present (not manifest/format churn) | PASS — `pipelineBoard.ts` +170/-49, new property file, new step handler, extended unit suite |
| G13 | Orphaned test/mutation processes before and after | PASS — none in this worktree |

## Acceptance scenarios

1. A grid caption identifies its ticket by title, not by epic — **ok**
2. A grid row with no backlog entry still identifies itself (the `NO_EPIC_LABEL` regression invariant 2 names) — **ok**
3. Caption lines are separated into groups where the epic changes — **ok**
4/5. A below-grid list shows at most three entries and names the rest — **ok** (both rows)
6. A board of long-titled tickets still fits one Telegram send (invariant 1, the 2026-07-17 live-outage path) — **ok**

## Unblocks BL-955

BL-955 was deferred behind this ticket (BL-532). Its recorded blocker check — `composePipelineBoardHtml` rendering `more epics` at `collapsedEpicsOmittedCount: 2` — **now exits 0** with this parcel merged, having exited 1 immediately before it. The deferral is cleared on the strength of this pass and BL-955 goes on to its own verification.

## Load caveat (recorded, not swept)

Host load averaged **190–293 on 4 cores** during this pass. The full unit suite showed 6 failures: `bounceDrain`, `briefingDigestLineCli` (×2), `dependencyGateCliStorageGlobals`, `renderBriefingBurndownCli`, `startBridgeHeadlessCli`. Re-run isolated, `bounceDrain` (21/21), `dependencyGateCliStorageGlobals` (6/6), `startBridgeHeadlessCli` (13/13) and `briefingDigestLineCli` (12/12) all pass. The residue is one `renderBriefingBurndownCli` case — "the compiled CLI runs with no flags at all against the real repo" — which renders burndown diagrams over the whole repo and cannot meet its 20 s budget at this load; it passed isolated earlier in the same session at load ~145. Every failure is a `Test timed out` and none touches `pipelineBoard`. Environment/capacity signal, surfaced rather than attributed to this ticket.

Minor, not a defect and not bounced: the documenter's commit message for `f916f86675` has a stray trailing `MSG 2>&1` line, an artifact of shell heredoc mangling. Content is correct.

By QA.
