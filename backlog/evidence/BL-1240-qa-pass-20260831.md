# BL-1240 — QA pass, 2026-08-31

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1240` → `VERIFY BL-1240` (exit 0),
checked before and after the full verification pass.

## Merge

Merged documenter's cited commit `35e7e2c47b` into QA at `55b2905dd9`
(`git merge --no-ff`). `check_merge_deletion.sh` refused the first attempt:
the merge removes `backlog/hold/BL-1240-...yaml` and
`backlog/hold/BL-1253-...yaml` (both promoted hold→active on the incoming
side), and the guard's attribution of the HEAD-side introducing commit
(`d0083cfa54`, "Merge main (specifier BL-1295 adjudication) into QA") names
`BL-1295`, not BL-1240/BL-1253 — the guard extracts the id from the commit
SUBJECT, not the deleted file's own name. Verified both paths
content-identical to their `active/` counterparts at the documenter tip
(empty diff both), so this is a re-promotion, not a retirement; named
`BL-1295` in the merge commit message per the guard's own attribution to
satisfy it. Diffed the merge against both parents (`82347884f3` and
`35e7e2c47b`): nothing dropped either side — only additions plus the
expected hold→active path move.

Ancestry re-confirmed for the full chain: coder `31ffc314a0`, cleaner
`c7bfa6f186`, architect `bf686b27a1`, hardener `6b1e09a6b6`, documenter
`35e7e2c47b` are all ancestors of the commit under evaluation (`55b2905dd9`),
not merely ancestors of a busy HEAD (BL-336 discipline).

## Stage corroboration

Read all evidence for this final round (`BL-1240-{coder,cleaner,architect,
hardener,documenter}-20260831.md`) plus the two earlier bounce evidence
files this round answers. Two prior bounces on this ticket: QA's own
(`unit`, a `pinnedRepoFixture.js` closure-walker edge dropping a `test/`-
subdirectory dependency, regressing `telegramFrontDeskBotCli.test.js`/
`.property.test.js`) and architect's (`unit`/`correctness`, a second
closure-walker idiom — `repo-root "swarmforge" "scripts" "X.bb"` — silently
dropping 3 more files). Both fixes verified landed and held in this tip.

This round's real defect (found by the coder on arrival, acceptance red 3/4):
a prior BL-490 revert of QA's bounce had removed BL-1240's entire parcel,
and the rework that answered both bounces re-landed everything **except**
21 lines of production wiring in `swarm_handoff.bb` — a partial
resurrection (matches the class already known from BL-1303, a different
ticket, same failure shape). Every artifact (lib, unit runner, property
runner, step handler, driver, manifest row, docs) survived; the call site
that actually wires the gate into the send path did not. Nothing gated it
because the ticket's own 2026-08-30 amendment deliberately declared no
`required_wiring` entry for that call site (verified by hand at spec time
instead). Coder restored the 21 lines verbatim from the revert's own
reverse diff, and separately fixed the acceptance fixture driver
(`bl1240UnregisteredTestGateCli.sh`) to answer the send-time self-audit
challenge (Article 2.3) on retry instead of asserting against the
pre-audit response — without that fix the acceptance driver could not
reach the gate chain at all, which is why it did not catch the missing
wiring on its own.

Cleaner: NONE. Architect: CLEAN, both `required_wiring` entries
independently re-verified live, correctness read confirms the root-cause
trace against the revert's own reverse diff. Hardener: clean, went beyond
the two named regressions with a repo-wide sweep of `resolveScriptClosure`
vs `copyScriptClosure` across all 376 real `.bb` test files (zero silent
drops anywhere, not just the four named files) and a hand-mutation
non-vacuity probe (reverted the `SCRIPTS_ROOT_ANCHOR` fix in place, 4 tests
failed as expected, restored, confirmed byte-identical). Documenter: verified
(not assumed) `handoff-protocol.md`, `Specification.MD`, and the BL-1240
how-to page against the RESTORED wiring per the architect's forwarded note —
all three already described the intended behavior accurately (the revert
took the code, not the docs), so nothing needed editing.

## Wiring — verified directly, not cited

- `grep -n unregistered swarmforge/scripts/swarm_handoff.bb` → load-file at
  line 28, gate block at 444-459, refusal arm at 492-495 — present.
- `specs/pipeline/steps/index.js:897` registers
  `bl1240UnregisteredTestFailsAuthorSteps`.
- Both `depends_on` tickets (BL-973, BL-1239) confirmed in `backlog/done/`.
- `bb swarmforge/scripts/test/suite_inventory_cli.bb swarmforge/scripts/test`
  → 443 files, 439 standing, 4 excluded, **no inherited drift** — re-arming
  this gate refuses nobody for BL-1239's backlog.

## Own independent runs (BL-112, separate from every prior stage's)

| Command | Result |
|---|---|
| `bb .../unregistered_test_gate_lib_test_runner.bb` | ALL PASS |
| `bb .../bl1240_unregistered_test_gate_property_runner.bb` | ALL PASS, 400 runs/invariant, all 7 shapes covered |
| `bash .../test_swarm_handoff_sync_deliver.sh` | ALL PASS |
| `bash .../test_swarm_handoff_daemon_backup.sh` | ALL PASS |
| `run_acceptance.sh` BL-1240 feature | 4/4 pass (scenario 01 is the one that proves the restored wiring) |
| `npm run compile` | clean |
| `npx vitest run telegramFrontDeskBotCli.test.js pinnedRepoFixture.test.js` | 287/287 pass (QA's own bounced regression) |
| `npx vitest --config vitest.properties.config.mjs telegramFrontDeskBotCli.property.test.js` | 3/3 pass |
| Direct repro of architect's D1 (`copyScriptClosure` on `cursor_seat_guard_lib_test_runner.bb`) | dependency present, confirmed independently |
| `npm test` (full unit suite) | 25 failed / 9781 passed |
| `npm run test:properties` (full property lane) | 25 files / 14 tests failed, 821 passed (835) |

## Regression check against the pre-merge (BL-1308) baseline

Both full suites were already run once this session on the tip immediately
before this merge (`82347884f3`). Direct comparison:

- Unit suite: 26→25 failed files, 9780→9781 passed. The one file that
  flipped from failing to passing is `test/pinnedRepoFixture.test.js`'s own
  `"BL-1240: the real live swarm_handoff.bb closure carries its test/
  dependency"` case — this parcel's fix resolving its own previously-red
  regression test, live proof the fix holds. Every other failing file/test
  name is byte-identical between the two runs — zero new failures.
- Property lane: **identical** file-level and test-level failure counts
  before and after (25 files / 14 tests both runs) — diffed the two failing-
  file lists directly, empty diff. Zero regression.

## Unowned red — verified pre-existing, not this parcel's (BL-1063)

Same catalogue already corroborated for BL-1308 this session and by
multiple prior QA passes: `checkOrphanedAuthoredDocs` class → BL-1221;
`tempDirTrapGuard`/`socketFixtureShortRootGuard`/`liveRepoDerivationGuard`
→ BL-1289/1290/1291; property-lane `node:test`/TAP collection-break class
→ BL-1220 lineage; `BL-654` stamp-off reds (`bl1113`, `bl1115`, `bl1136`)
already evidenced. None reference `swarm_handoff.bb`,
`unregistered_test_gate_lib.bb`, `pinnedRepoFixture.js`, or any BL-1240
file. Surfacing, not sweeping.

## Orphan check

`pgrep -fl 'node --test|stryker|vitest'` clean before this pass and after
both the full unit run and the full property-lane run.

## Verdict: APPROVE

By QA.
