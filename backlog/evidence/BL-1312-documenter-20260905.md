# BL-1312 documenter review pass — 2026-09-05

Reviewed the hardened commit (`cc1f5ae4f9`) and the coder/cleaner/architect/
hardener trail for BL-1312 (fixture root cleanup does not survive SIGTERM).

Checklist:
- The ticket's own `.feature.draft`→`.feature` conversion, step handler
  registration, and `acceptance:` repoint were all completed in-parcel per
  the ticket's explicit notes: confirmed
  `specs/features/BL-1312-fixture-root-signal-cleanup.feature` exists
  (renamed from the draft) and
  `specs/pipeline/steps/bl1312FixtureRootSignalCleanupSteps.js` is present.
- Confirmed the fix took human-ruled option 1 (fix the shared helper):
  `specs/pipeline/steps/lib/socketFixtureRoot.js`'s `installExitHook` now
  calls `onAbnormalExit(removeStragglers)` (imported from the shared
  `fixtureReaper`) instead of only `process.on('exit', ...)` — the same
  SIGINT/SIGTERM/exit coverage `track()`/`reap()` already have, never a
  second, independent signal listener (invariant 2: one process-wide
  install regardless of caller count). Confirmed the secondary
  caller-side fix in `roleLifecycleParkUnneededSteps.js` (registering
  `liveFakeBinDirs` earlier) also landed, per the ticket's "worth closing
  while in the file" direction.
- This is an internal acceptance-suite test-infrastructure fix (a shared
  fixture-cleanup helper's signal coverage) with no operator-visible
  behavior, no CLI/extension command/setting change, and no production
  `.ts`/`src` file touched. No existing doc describes
  `mkSocketFixtureRoot`/`socketFixtureRoot.js`/`onAbnormalExit`/
  `fixtureReaper` (grepped `docs/how-to/`, no hits) — no documentation
  update is in scope for this ticket.
- No "Last Updated" freshness field anywhere touched — no date bump owed.
- No registered diagram depicts acceptance-suite fixture-cleanup
  internals — no diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond what the hardened commit already carries.

Result: NONE — no defect found, no documentation change in scope.
