# BL-1381 — documenter pass (2026-09-04)

## Received
`bf1915f300` ("BL-1381: tip-pure replay onto origin/main (BL-1241 land-step
remedy)", the hand-built replay that reached documenter via hardener's
forward — a fast, small-scope fix per its own mint-time INVEST note).

## Doc-domain review
This is an operationally significant fix — every schedule cron install had
silently failed since 2026-08-27, with the only trace one WARN line in
launch output nobody reads. `docs/how-to/BL-1162-start-stop-swarm-cron-lifecycle-symmetry.md`
already documents the start path's cron install ("Logs name what was
installed or skipped" — a claim this incident falsified) and is the
natural, single home for this fix: added a note under "Start path"
describing the silent-failure window and the fail-loud rule that closes
it.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: refreshed the BL-1162 summary line.
- Checked `docs/how-to/BL-660-three-shift-packs-conf-selectable.md` and
  `docs/how-to/BL-666-budget-aware-shift-governor.md` (both reference the
  same lib/test runner) — neither makes a claim this bug falsified, so
  left unchanged; BL-1162 is the doc whose own text ("logs name what was
  installed or skipped") was actually wrong during the incident window.
- Diagram registry checked — no diagram depicts shift-schedule-applier
  internals.

## Verdict
Doc content corrected (BL-1162 extended, not NONE) plus the standard
Specification.MD/index.md entries. Forwarding to QA.
