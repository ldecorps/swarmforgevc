# BL-1120 — QA complete review inventory (Article 4.4) — 20260825

- **Ticket**: BL-1120 — handoffd must not abort a foreign master-main merge
  (`type: defect`, `severity: high`)
- **Parcel**: `00_20260825T101023Z_000585_from_documenter_to_QA_for_QA`
  (documenter tip `bfe3e7a53a`)
- **Verified at**: tip `bfe3e7a53a` vs `origin/main` `be3a93e47e` (no merge
  completed — merge aborted; index reset to `origin/main`)
- **Reviewed by**: QA, 2026-08-25

## Verdict

**BOUNCE — inventory items: D1 (one item).**

Product gates for BL-1120 were **not** run: the tip cannot be merged onto
current `origin/main` without landing a large out-of-scope delta. Per BL-506
an approval authorizes only this ticket's work.

## Inventory

### D1 — tip hitchhikes unfinished / foreign work onto `origin/main`

- **Failure class**: `behavior`
- **Blamed role**: `coder`
- **Failing command**:
  `git diff --name-only origin/main...bfe3e7a53a | wc -l`
  (and attempted `git merge --no-ff bfe3e7a53a` from `origin/main` —
  rejected: would delete `backlog/paused/BL-626-…` among many other
  non-BL-1120 mutations)
- **Commit tested**: `bfe3e7a53a`
- **First error excerpt**:
  ```
  path count tip vs main: 221
  … includes e.g.:
  extension/src/quality/thinMainGate.ts          (BL-534 — already QA-bounced)
  backlog/done/M8/BL-* (mass active→done moves)
  backlog/active/BL-695-…
  D backlog/paused/BL-626-… (lands as delete vs main)
  ```
  Hardener tip `74a637648` alone is already **212** paths vs `origin/main`.
- **Expected vs observed**: expected a hitchhike-free rematch tip whose
  `origin/main...tip` delta is only BL-1120 (reconcile lib / handoffd /
  APS / docs / evidence); observed ~221-path contamination including the
  in-flight BL-534 product we just bounced.

**Remediation pointer**: recreate `swarmforge-coder` on current
`origin/main` (`be3a93e47e`+), cherry-pick / re-apply **only** BL-1120
commits (coder `7743cfd0b` lineage through hardener `74a637648` and
documenter docs `bfe3e7a53a` content), verify
`git diff --name-only origin/main...HEAD` is BL-1120-only, then forward
down the remaining chain. Do **not** fold BL-534 or coordinator backlog
bookkeeping into this land.

## Gates not run (blocked by D1)

Compile, unit, properties, acceptance, `pre_qa_gate`, wiring deep-check —
all **BLOCKED BY D1**. Not assumed clean.

## Note

BL-534 was bounced earlier this turn to documenter for incomplete
`abandoned_commits` (`2418f52f4e`). This tip must not re-land that product
via hitchhike.

By QA.
