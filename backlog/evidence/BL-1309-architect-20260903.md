# BL-1309 — architect pass (2026-09-03)

## Scope

Merge (coder fcb06cb32d) had one conflict in `specs/pipeline/steps/index.js`
(re-introducing `bl1296BubbleSeatSteps` registration, which is legitimate
passenger ancestor content from the coder's own branch — BL-1296's D1 half
one is now fixed per `backlog/evidence/BL-1296-coder-rework-20260903.md`,
half two correctly raised as a spec-gap note rather than decided
unilaterally, matching my own prior bounce's remediation pointer 2 exactly).
Resolved by taking the incoming registration; not part of this review.

BL-1309's own diff: `swarmforge/scripts/land_main_publish.sh` (the mandatory
land-decide step, now consults `entangled-siblings` before advising a push),
its unit test rows, mutation sweep fix, step handler, fixture CLI, property
test, and the draft→live acceptance promotion. Matches the ticket's declared
scope exactly (required_stages excludes cleaner — `stage_skip_reasons`
recorded at mint: one guard added beside an existing decision, nothing to
extract).

## Ruling followed correctly

`ruling_options` (option 1: refuse every entangled tip, no
withheld-vs-ordinary predicate) was left unanswered at mint; the coder
evidence records it was answered 2026-09-03 before implementation started —
not guessed. The shipped code implements exactly option 1: no predicate,
unconditional refusal on any unlanded sibling.

## Read, not just evidence-trusted

- `land_main_publish.sh`: `DECISION` (the existing BL-1144 purity/lock
  decision) is computed and held, THEN the entanglement check runs and can
  still refuse before anything is printed — a refusal never has stale push
  advice already on stdout ahead of it. Confirmed by reading the script's
  control flow, not just the evidence claim.
- Fail-open mechanics, traced by hand: `ENTANGLED_OUT="$(entangled_sibling_
  report || true)"` — the `|| true` is INSIDE the substitution, so a `bb -e`
  failure (an unrunnable detector) cannot trip `set -e` and abort the script
  mid-flight with the lock held. `entangled_sibling_report` itself: `task`
  (the tip's own ticket id) nil short-circuits to no output (a tip naming no
  ticket has no sibling notion to check against); a `:warning` from
  `entangled-siblings` (unreadable range) also produces no output. Only a
  POSITIVE `ENTANGLED_SIBLING_BLOCK` match on `$ENTANGLED_OUT` triggers
  `exit 3` — matches invariant 2.
- Exit status contract (3 on refusal, 2 usage error unchanged, 0 otherwise)
  documented in the script's own header and matches the `case` dispatch.

## Invariants (both declared, both non-vacuous)

- **Invariant 1** (no unlanded content advised for push, whatever route):
  drives all three named routes (plain commit, second-parent merge — the
  route BL-1308 had to widen the detector for, rematch) against a REAL bare
  origin remote and the REAL shell script, each route built by construction
  and reach-asserted.
- **Invariant 2** (an unreadable input never becomes a refusal): drives
  no-detector / unreadable-range / unknown-ticket, each asserting the
  fixture actually refuses BEFORE the input is broken (so no row passes
  vacuously by never having been entangled to begin with).
- Ran `npx vitest run --config vitest.properties.config.mjs
  bl1309LandDecideEntanglementInvariants` 3 consecutive times — 2/2 each, no
  flake.

## Verification (run, not assumed)

- `bash swarmforge/scripts/test/land_main_publish_test_runner.sh` — ALL PASS
  (rows 04-07 are the new BL-1309 coverage: refusal names the sibling, exits
  3, no advice, no lock held; ordinary decision when nothing unlanded;
  missing-detector and no-ticket-subject both fail open).
- `bash swarmforge/scripts/test/bl1144_main_land_publish_mutation_sweep.sh`
  — 9 killed, 0 survived.
- `run_acceptance.sh specs/features/BL-1309-….feature` — 6/6.
- `run_acceptance.sh specs/features/BL-1144-….feature` (regression) — 3/3.
- `required_wiring` confirmed, both entries: `ENTANGLED_SIBLING_BLOCK`
  literal live in `land_main_publish.sh`; `bl1309LandDecideStepEntanglementSteps`
  registered in `specs/pipeline/steps/index.js:953`.
- Dependency-rule gate (BL-259), scoped to the touched JS files AND
  full-repo: PASSED, no forbidden edges.
- Co-change report (BL-255): all reported co-changes at frequency 1 — no
  suspected coupling.

## A1 (coder self-audit) — noted, not a blocker

The mandatory step measured ~12x slower (1.1s → 13.7s) with the guard,
inherent to `entangled-siblings`' per-candidate-commit `git log` calls. The
ticket's own `depends_on` note explicitly scopes detector optimization OUT
of this slice ("this slice does not touch the detector"). A few-times-an-hour
step at 13.7s is a real but modest cost for the ruling that was chosen, and
optimizing `land_step_lib.bb` is legitimately a separate slice. Not a defect
in this parcel; flagged for hardener/documenter awareness per the coder's
own note.

## Nothing else found

Production code change is scoped, fails open correctly, both invariants are
independently verified non-vacuous. No architecture violation, no invariant
violation, no correctness defect. Forwarding to hardener.
