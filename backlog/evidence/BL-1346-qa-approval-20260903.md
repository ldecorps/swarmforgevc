# BL-1346 — QA approval (20260903)

Received: documenter commit `f4c4e015b8` (review-pass evidence, no doc
defect found), forwarding architect (clean sweep) and hardener/cleaner/
coder review passes. This is a BL-848 review-only stamp-off for
already-landed commit `195de28861` — confirms or refutes, never
reimplements or reverts.

## Review inventory (complete pass, Article 4.4)

- Wiring anchors on the merged tree, all present at declared counts:
  - `swarmforge/scripts/swarm_ensure.bb::resolve-resident-role`: 2
    occurrences.
  - `swarmforge/scripts/swarm_ensure.bb::rc-launch-role`: 3 occurrences.
  - `specs/pipeline/steps/index.js` requires
    `bl1346SwarmStampRcLaunchRoleSteps`: present.
- Read `rc-launch-role` by hand: it now routes through
  `mono-router-lib/resolve-resident-role` (the shared BL-1020 decision)
  with no local resident rule left beside it — matches
  `qa_e2e_procedure` step 1.
- Read `resolve-resident-role` by hand (`mono_router_lib.bb:335`): on a
  non-router pack the marker is unconditionally ignored (`:role
  home-role`, `:honour-marker? false`) and flagged `:stale?` for callers
  to surface — matches invariant 2 (marker authority removed off-router
  only). On a router pack the marker is honoured when present — matches
  invariant "retains its full authority on a rotation-router pack."
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated `resolve-resident-role`'s `(if rotation-router? ...)` to
  `(if true ...)` (always honour the marker, reproducing the pre-hotfix
  bug) — the property test's invariant 2 failed exactly as expected
  (assertion on the returned role). Restored (`git diff --stat` empty
  after restore), re-ran 3/3 clean.
- `bash swarmforge/scripts/test/test_swarm_ensure.sh`: 51 PASS / 0 FAIL,
  matching the operator's own recorded baseline exactly. RC-7 (rotated
  resident still follows its own launch script) and RC-7b (the hotfix's
  own new fixture: stale marker not topology on a standing pack) both
  PASS.
- Acceptance
  (`specs/features/BL-1346-swarm-stamp-rc-launch-role-stale-marker-195de28861.feature`):
  5/5.
- Property test (`bl1346RcRepairStampInvariants.property.test.js`): 3/3
  (invariant 1: a degraded pane is still repaired, with its own pack
  role — confirms the repair path itself is untouched, only the launch-
  role lookup changed; invariant 2 above).
- Constraint check: `backlog/hotfix-ledger.yaml`'s BL-1346 row still
  reads `state: stamp-open`, `human_decision: null` — no
  `certified`/`waived` written.
- No hand-port into worker worktrees: `swarm ensure` runs only from the
  master checkout — confirmed no commit in this ticket's history touches
  a worker worktree copy.
- Compile (`extension`, `npm run compile`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population (includes the three known standing guards:
  `tempDirTrapGuard`, `socketFixtureShortRootGuard`, `liveRepoDerivationGuard`).
  None touch this parcel's files.
- Property suite (`npm run test:properties`), run sequentially after the
  unit suite (never concurrently): 24 files / 13 tests red, matching the
  exact standing-debt population. No BL-1346 file present in the failing
  set.
- Side findings from the ticket's own notes: neither is this parcel's
  (fixture-argument-forwarding chore, not yet minted; the post-repair
  HEALTHY-report gap is BL-1345's, already landed separately this
  session) — not taken on here, per the ticket's own constraints.
- Orphaned test processes: none of mine after the runs. A `vitest`
  process was alive mid-check but confirmed (via `ps -o pid,ppid,args`)
  to belong to the cleaner worktree's own concurrent BL-1344 flake-check
  run — left untouched.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`. Ledger row
stays `state: stamp-open` — the certify/waive decision remains
human-only.

By QA.
