# BL-1635 — hardender review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Received: architect 219a382538 (BL-1635), merged 13d84803bf (clean, no
  conflicts).
- Scope confirmed: `git diff 45206d20db..HEAD` (net change over the prior
  merge-up) touches only the two build files, the one Kotlin test (byte
  level: NUL escaped), the lib, the feature handler, the new property
  test, and evidence — matches the ticket's constraint exactly (no test
  assertion, no other production string, no other Kotlin source, no other
  feature).
- mutation_cost: low (specifier's own assessment). No `Scenario Outline`
  in the feature (three plain `Scenario:`), so BL-113 Gherkin mutation is
  inapplicable (BL-638). No `src/*.ts` touched, so CRAP/DRY don't apply to
  this parcel.
- Live re-verification in this worktree (compiled fresh):
  `bl1635JvmBuildEnvInvariant.property.test.js` green alone;
  `run_acceptance.sh specs/features/BL-1635-*.feature` 3/3 green.
- Hand-mutation-style probe on scenario 03's scan logic (NUL-byte /
  invalid-UTF-8 detection in `bl1635JvmBuildReadsSourcesAsUtf8Steps.js`):
  reproduced the exact `hasNul`/`TextDecoder(fatal)` logic against three
  cases in a standalone script (a NUL byte, an invalid UTF-8 continuation
  byte, and a real em-dash) — confirms the check is non-vacuous (flags
  real defects) and has no false positive on the exact character class
  this ticket is about (em-dash). Not run against the live repo files;
  no repo file touched by the probe.
- Confirmed by direct byte read (`python3`, not `grep -c` which
  miscounts on this binary-flagged file): `BridgeClientTest.kt` has 0 NUL
  bytes post-fix, and the adjacent SOH (0x01) byte is untouched, matching
  the ticket's own scope note (only the NUL byte was in scope).
- `buildGradleEnv`'s own coverage (property test with non-vacuity proof,
  breaking the function and watching it fail) was already thorough from
  the coder's pass — not re-derived here, reviewed and confirmed sound.
- Did not re-run the real `:app:testDebugUnitTest` JVM suite in this
  pass — the coder's evidence already reproduces the failure pre-fix and
  proves 275/275 post-fix (FIRM ticket requirement), and the ticket's own
  `qa_e2e_procedure` step 1 assigns the live suite run to QA. Re-running
  it here would duplicate that under host contention with no additional
  information (the scenario checks are config/byte-level and don't launch
  a JVM).
- pgrep scoped to this worktree: no orphaned test/mutation processes
  before or after this pass.
- Forward commit: 95ff01b673.
