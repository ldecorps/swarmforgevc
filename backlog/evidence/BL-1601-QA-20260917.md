# BL-1601 — QA review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Citing commit: `a8154c0a4d` in this QA worktree (documenter merge
  `e54a5dec1a` chain, plus this pass's own untagged land_step_cli.bb
  repair and standing-red row removal).
- `qa-sibling-check.js status --ticket BL-1601` → `VERIFY BL-1601`.
- qa_e2e step 1: 20 standalone real runs of
  `npx vitest run test/telegramCursorOperatorExec.test.js` (extension/) →
  20/20 green, 24 tests each.
- qa_e2e step 2: full unit lane (`npm test`) → 625 files / 10675 tests,
  all pass (one full run captured; budget-warning telemetry is
  pre-existing noise unrelated to this ticket). Full property lane
  (`npm run test:properties`) → 414 files / 1218 tests, all pass; the
  3 `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors
  are the BL-871 known-benign allowlisted noise (documenter's own note
  on this same ticket already names this class).
  `npx vitest run --config vitest.properties.config.mjs
  test/tmpDirRemoveRetry.property.test.js` → 1/1 pass on its own too.
- qa_e2e step 3: `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1601-*.feature` → 5/5 pass.
- qa_e2e step 4: `git diff <parent>..<parcel> --
  extension/test/telegramCursorOperatorExec.test.js` adds only the
  marker-wait loop and assertion in the two BL-1204 spawn tests, removes
  nothing (confirmed via the coder/hardener/cleaner evidence chain; the
  cleaner's DRY attempt was reverted back to byte-identical, per its own
  evidence).
- qa_e2e step 5: `backlog/standing-reds.tsv`'s BL-1601 row removed in
  this pass (this commit) — the file is green 20/20 standalone and in
  the full unit lane. `standing_red_register_cli.bb` now reports 8 rows,
  none for this file, `"unowned":[]`.
- `check_feature_handler_registration.sh` → exit 0, clean
  (`bl1601RedeployTestsAwaitTheMarkerSteps.js::registerSteps` wired).
- **Recurrence of the known BL-1604 closed-owner `land_step_cli.bb` hunk**
  (same class as BL-1618's LAND_ESCALATE, already adjudicated by the
  specifier that pass — see `backlog/evidence/BL-1618-qa-land-escalate-20260917.md`
  and its resume note): the cleaner's evidence
  (`unowned-red-bl1388-land-step-guard-fixture-cleaner-20260917.md`)
  itself documents re-planting this same 8-line hunk as a "silent-revert
  repair" without checking `abandoned_commits` — exactly the BL-1624/1625
  pattern. Applied the SAME adjudicated remedy directly (restore
  `land_step_cli.bb` to `origin/main`'s content, untagged subject) rather
  than re-escalating a structural cause already adjudicated (Article 4.4
  rule 4). Confirmed byte-identical to `origin/main`'s copy after the
  restore — no future land will see this path as a diff.
- `test_bl1388_land_step_guard_fixture.sh`'s own red (documented by the
  cleaner, confirmed pre-existing on a fresh `origin/main` clone, now
  owned by BL-1624) is untouched by this parcel — its register row (row
  4 in the current file) stays, unrelated to BL-1601's own fix.
- No prior bounce history for BL-1601 beyond the hardener's own
  self-remediated D1 (fixed in the same hardening pass, not forwarded as
  a defect - EBUSY branch coverage gap, evidence already on file).
- Straggler check before/after: clean in this worktree's own process
  tree; one long-running `vitest.properties.config.mjs` process observed
  belongs to the `hardender` worktree (cwd
  `.worktrees/hardender/extension`), not reaped (not mine).
