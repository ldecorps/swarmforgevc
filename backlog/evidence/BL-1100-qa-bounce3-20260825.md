# BL-1100 QA bounce 3 — 20260825

**Reviewed tip:** documenter `feedbbf096` (not merged — tip impure after gate race)
**Task:** `BL-1100-promotion-candidacy-is-decided-by-structured-fields-never-prose`
**Sibling:** `VERIFY BL-1100`
**Prior:** bounce2 `7af96d97e8`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` (at claim) | PASS — paths=9, dels=0 |
| Tip purity vs `origin/main` (post-gate fetch) | **FAIL → D1** |
| APS BL-1100 feature | 8/8 |
| Property bl1100 (vitest.properties) | 2/2 |
| compile | PASS |
| Sibling | VERIFY |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main HEAD  # fails after fetch
git rev-list --count HEAD..origin/main  # → 1
# dels_on_origin=0
```

**Commit hash checked:** `feedbbf096`

**First error excerpt:**
```
main_NOT_ancestor after APS
behind=1: 89afdb3bd BL-988: assigned_to hardender for QA bounce4 D1
paths=10 dels=0
```

**Failure class:** `behavior`

**Expected vs observed:** Tip must keep current `origin/main` as ancestor
through QA gate window. Observed rematch3 raced by BL-988 assignment land.

**Remediation:** Immediately before QA handoff: `git fetch && git reset --hard
origin/main` + restore BL-1100 only; keep bounce*.md; re-verify ancestry in
the **same session** as `swarm_handoff.sh`.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
