# BL-955 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-955 — annotate caption-derived text on every forwarding surface (`type: defect`, `severity: medium`)
- **Parcel**: `00_20260819T163133Z_000208_from_documenter_to_QA_for_QA` (from documenter, commit `70860df6f2`)
- **Verified at**: the QA branch tip carrying `70860df6f2` and BL-956's approved fix; this evidence file is committed on top and the forward names that commit.
- **Reviewed by**: QA, 2026-08-19
- **Prior bounces**: none.

## Verdict

**PASS — inventory items: NONE.**

## Deferral history (BL-532) — why this pass happened out of parcel order

BL-955 was **deferred behind BL-956**, not bounced: it had no failing check of its own, and the recorded blocker was BL-956's `composePipelineBoardHtml` / `more epics` cap check. On first handling I re-ran only that printed blocker command, it still exited 1, so the deferral stood and BL-955 got no verification pass, no evidence file and no bounce — correct per BL-532.

BL-956 was then approved and landed (`8728e8c1ec`), its blocker check went to exit 0, and I cleared the deferral (`qa-sibling-check.js clear --ticket BL-955 --blocked-by BL-956 --commit 8728e8c1ec` → `{"recorded": true}`; `status` → `VERIFY BL-955`).

**BL-955's own parcel had already been consumed** by the deferral handling, so no further parcel would have brought it back. Its implementation was meanwhile already on `main`, having ridden there in BL-954's entangled batch lineage (recorded in `BL-954-qa-pass-20260819.md`). Leaving it here would have left approved-nowhere code on `main` permanently — the exact leak class BL-952/BL-954 exist to close. So this pass was run directly on the unblocked ticket rather than waiting for a parcel that was never coming. Flagged for the coordinator: BL-955's bookkeeping is owed without a matching inbound parcel.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — coder `517123c243`, cleaner `36c1757b35`, architect `5673cf319a`, hardener `814812883a`, documenter `70860df6f2` all ancestors of the cited commit | PASS |
| G2 | Prior-bounce check against `main` **and** `origin/main` (BL-891) | PASS — none |
| G3 | `npm run compile` | PASS |
| G4 | Unit — the changed modules' own suites (`telegramFrontDeskBotCore`, `negotiationTelegramRelay`, `negotiationTelegramRouting`) | PASS — 431/431 |
| G5 | Full unit suite | PASS — 7885 passed / 444 files at the BL-956 pass, which included every line of BL-955's code; the 6 failures there were load timeouts in unrelated CLI files (see `BL-956-qa-pass-20260819.md`) |
| G6 | Property lane — `test/bl955ForwardingAnnotationInvariants.property.test.js` | PASS — 3/3; invariant 1 (every surface annotates, plain text byte-identical) and invariant 2 (a photo caption classifies exactly as the identical plain text) |
| G7 | `required_wiring` — `negotiationTelegramRouting.ts::annotateRoutedMediaText` | PASS — imported at `negotiationTelegramRouting.ts:9` and **called** at `:107` (`return annotateRoutedMediaText(text, update)`). This is the surface the ticket says a `telegramFrontDeskBotCore.ts`-only fix would leave dark, so it was checked at its call site, not just its import |
| G8 | Step-handler registration | PASS — `specs/pipeline/steps/index.js:517` registers `bl955ForwardingAnnotationSteps` |
| G9 | Pre-QA gate | PASS — `pre_qa_gate_cli.bb BL-955-annotate-caption-derived-text-on-every-forwarding-surface … .` → `OK` |
| G10 | Acceptance — `run_acceptance.sh specs/features/BL-955-annotate-caption-derived-text-on-every-forwarding-surface.feature` | PASS — 8 pass / 0 fail, exit 0 |
| G11 | Docs currency (documenter's own pass) | PASS — `94a3481eb` documents the annotation across all forwarding surfaces |
| G12 | Functional change present (not manifest/format churn) | PASS — three source modules changed, new property file, new step handler, extended unit suites |
| G13 | Orphaned test/mutation processes before and after | PASS — none in this worktree |

## Intent check

The defect is that caption-derived text reached readers and durable records without the image-not-read note, and that a fix confined to `telegramFrontDeskBotCore.ts` would miss the negotiation relay. Both invariants are directly exercised:

- **Invariant 1** — all **six** forwarding surfaces annotate (acceptance rows 1–6, one per surface; the hardener independently re-derived the same six from source in `BL-955-hardener-pass-20260819.md`), and a message with no attachment is forwarded **byte-identical** (row 7).
- **Invariant 2** — the note is appended at forwarding/record time only, never to text a parser reads: a control command sent as a photo caption still parses as that command (row 8), and the property asserts a caption classifies exactly as the identical plain text.

By QA.
