# BL-1533 — hardender review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Mutation fallback (BL-638, `mutation_cost: low`, no `extension/src` touched
  per ticket): hand-authored break-then-restore over
  `extension/test/helpers/signedTrendSeries.js` — the `down` arm was
  mutated to build an ascending pair (an `up` delta); the property failed
  deterministically on invariant 1's constructed-sign assertion
  (`'up' !== 'down'`), not the sampled loop; restored, re-ran green. No
  Stryker target exists for this parcel.
- Flake verification: `npx vitest run --config vitest.properties.config.mjs
  test/bl604TrendAnalysisInvariants.property.test.js` run 15x standalone —
  0 FAIL. Every run's printed `BL-1533 invariant 1 reach: {...}` line
  showed `up` and `down` both >= 20 (well above `SIGN_FLOOR = 10`), e.g.
  `{"none":12,"one":12,"many":12,"down":29,"up":41}`.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1533-*.feature` — 5/5 scenarios pass.
- Whole-tree standing guards (parcel touches `extension/test/` and
  `specs/pipeline/steps/`): all 18 `test/*Guard*.test.js` files (183
  tests) pass, including `tmuxReaperGuard`, `tmpDirMigrationGuard`,
  `bl968StepRegistryMaterializedTreeGuard`.
- DRY: `npx jscpd` over the three new/changed files — 0 clones.
- CRAP: not applicable — no `extension/src/*.ts` file in this parcel's
  diff (`git diff main...HEAD --stat` confirms only test/helper/step/
  evidence files).
- Scope confirmed: `extension/src/`, `property_suite_standing_allowlist.tsv`,
  and BL-604's own feature/handler are unchanged in this parcel.
- No orphaned test/mutation processes left running
  (`pgrep -fl 'node --test|stryker|vitest'` clean after all runs).
- Register row (`backlog/standing-reds.tsv` BL-1533 line) left in place
  per the ticket's own notes — QA removes it in the land that turns the
  file green, not the hardener.
