# BL-1336 — cleaner pass (2026-09-03)

## Scope

Merge `49fca1c741` touches only `vitest-worker-memory-budget.ts`, the two
vitest lane configs, `swarmforge.sh`'s rotation export, the ticket's own
step/unit/property test files, and one documented incidental fix to
`bl1323StampOffInvariants` (the row's lifecycle state changed from
`stamp-open` to also legitimately pass through `awaiting-human`; the
invariant now asserts the actual DECISION fields — `certified`/`waived`
absent, `human_decision`/`decided_at` null — rather than one specific
pre-decision state name). No other scope creep.

## Architecture / constraints (per ticket)

- Single composition point preserved: `resolveVitestForkCeiling` gained one
  `rotation` param and one `if (rotation === 'router')` branch; both
  `vitest.config.mjs` and `vitest.properties.config.mjs` read
  `process.env.SWARMFORGE_ROTATION` into the SAME `resolveVitestWorkerPool`
  call — no second route.
- Keyed on rotation value, never pack name (`rotation_signal()` in
  `swarmforge.sh` is the single normalizer both `swarm-identity` and the
  role-env export read, so the two can't disagree about what "router"
  means).
- Full-forge-on-darwin ceiling of 1 checked first and unchanged; the two
  conditions (full-forge+darwin vs router) can't both fire since a router
  pack is never full-forge.
- RAM floor stays binding: `resolveVitestWorkerPool` still takes
  `min(ramAllows, ceiling)` — raising the ceiling only raises the ceiling.

## Verification (run, not assumed)

- **False alarm caught and cleared, not a coder defect**: first acceptance
  run showed 2/6 failing (`ceiling` read `6` instead of the raised value,
  and the RAM-cap scenario tripped `raised ceiling widened the pool past
  what memory allows: 1`). Root cause was my own stale `extension/out/`
  build predating this merge's `.ts` edit — `npm run compile` and both
  failures cleared. Re-run: 6/6 pass. Recorded so it isn't mistaken for a
  coder defect on a later reviewer's re-run.
- `npx vitest run test/bl1336RouterForkCeiling.test.js` — 6/6 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1336ForkCeilingInvariants` — 15 consecutive green runs (no flake).
- Re-ran `bl1323StampOffInvariants`, `bl1352EscalationVisibilityInvariants`,
  `bl1335ExhaustionPromotionInvariants` together — all green, nothing this
  ticket touched broke a sibling property file.
- Mutation-site advisory (BL-485): production file
  `vitest-worker-memory-budget.ts` is 42 sites (well within the 100
  threshold). The two "over" files are the step-handler and property-test
  files (124, 134 sites) — consistent with sibling `bl*Steps.js`/
  `*.property.test.js` files elsewhere in this session (124–241 range); no
  split warranted.

## Verdict

No defect. Forwarding.
