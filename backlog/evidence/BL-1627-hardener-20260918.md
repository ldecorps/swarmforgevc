# BL-1627 — hardener review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

Reviewed both `mapfile` → while-read rewrites for the specific known
bash pitfall this class of change usually hits: a `while IFS= read -r
line; do ...; done < <(cmd)` loop silently drops the LAST line when
`cmd`'s output has no trailing newline (unlike `mapfile`, which does not
have this gap). Checked `changed_bb_files()` (both branches: `find
... -print` and a `printf '%s\n'` loop) and `check_constitution_doc_citations.sh`'s
two commands (`git diff --cached --name-only`, `git ls-files --cached`) -
every one of them terminates its LAST line with `\n` same as every other
line, so this pitfall does not apply here. Confirmed `set -uo pipefail`
is active in both scripts (making the `${arr[@]+"${arr[@]}"}` guard load-
bearing, not defensive noise) and that `${#NAME[@]}` (length only) sites
needed no change, matching the coder's own claim.

Ran: `bash -n` on both scripts (no syntax errors);
`bash swarmforge/scripts/check_bb_scripts_load.sh --all` (311 scripts,
all clean); `bash swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh`
(ALL PASS); `check_constitution_doc_citations.sh` on a staged no-op
change under `swarmforge/constitution/articles/reference/` (exit 0);
BL-937's feature (2/2, green for the first time on this host) and
BL-1627's own feature (4/4). No fixture/process-spawning code in the new
handler, so none of this pass's earlier leaked-mkdtemp class applies
here. Register row for BL-937's feature left in place per the BL-1631
pattern (QA's land step retires it).
