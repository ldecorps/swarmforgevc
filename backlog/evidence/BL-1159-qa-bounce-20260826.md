# BL-1159 QA bounce — 20260826

**Commit checked:** `f15623b2d` (Merge documenter `0e16721ec7`)
**Task:** `BL-1159-bridge-child-survives-without-crash-giveup-loop`
**Routing:** `cleaner`

## Gates PASS (BL-1159 surface)

| Gate | Result |
|------|--------|
| Compile | PASS |
| Acceptance `BL-1159-bridge-child-survives-without-crash-giveup-loop.feature` | 4/4 PASS |
| `test_bl1159_bridge_child_survives_without_crash_giveup_loop.sh` | ALL PASS |
| `test_recover_miniapp_bridge.sh` | ALL PASS |
| `test_start_stop_bridge_headless.sh` | ALL PASS |
| `test_operator_runtime_tick.sh` | ALL PASS |
| `bl1159_bridge_child_mutation_sweep.sh` | 4/4 killed |
| `required_wiring` bl1159 steps in `index.js` | present |
| `recover_miniapp_bridge.sh` wired in `operator_runtime.bb` | present |

## Defects

**D1 — behavior (blame: cleaner):** `specs/pipeline/steps/index.js` **replaced** landed
`bl1153StickyWebFontSizeChoiceSteps` with `bl1159BridgeChildSurvivesWithoutCrashGiveupLoopSteps`
instead of adding BL-1159 alongside BL-1153. Coder `43da3844ec` had both; cleaner merge
`75a6ec9ab` dropped BL-1153.

- **Failing command:** `git show HEAD:specs/pipeline/steps/index.js | grep bl1153` (empty) vs
  `git show origin/main:specs/pipeline/steps/index.js | grep bl1153` (present)
- **Expected:** both `bl1153StickyWebFontSizeChoiceSteps` and `bl1159BridgeChildSurvivesWithoutCrashGiveupLoopSteps` registered
- **Observed:** BL-1153 handler removed from DOMAINS

**Remediation:** `specs/pipeline/steps/index.js` — restore `require('./bl1153StickyWebFontSizeChoiceSteps')`
immediately before or after the BL-1159 require; do not replace.

**D2 — behavior (blame: cleaner):** BL-1153 regression — `extension/test/residentSpyUiHtml.test.js`
missing reload test (landed on `origin/main`).

- **Failing command:** `git show HEAD:extension/test/residentSpyUiHtml.test.js | grep -c BL-1153` → 0;
  `origin/main` → 1
- **Expected:** BL-1153 sticky font-size reload test present
- **Observed:** test absent (polluted QA lineage)

**Remediation:** restore `residentSpyUiHtml.test.js` from `origin/main` on the forward tip.

**D3 — behavior (blame: cleaner):** tip not BL-1159-only — merged on BL-588 base (`75a6ec9ab`:
"merge coder 43da3844ec on BL-588 tip"). Land diff vs `origin/main` includes BL-588 batchRecovery
src, BL-653 operator escalation, BL-660 shift packs, INTAKE files, and BL-653 mixed into
`operator_runtime.bb` (`tick-observed-events` refactor alongside BL-1159 recover path).

- **Failing command:** `git diff origin/main...HEAD --name-only | grep -E '588|653|660|INTAKE|batchRecovery|swarmShift'`
- **Expected:** BL-1159 deliverables only on a queue-jump defect parcel
- **Observed:** 80+ paths including sibling tickets

**Remediation:** re-cut BL-1159-only tip from `origin/main` (cherry-pick coder `43da3844ec` slice +
downstream docs/hardening), forward clean tree through architect → hardender → documenter → QA.
Do not merge BL-1159 onto BL-588 polluted lineage.

## Inventory

D1, D2, D3 (all cleaner merge hygiene). Bounce to **cleaner**.

By QA.
