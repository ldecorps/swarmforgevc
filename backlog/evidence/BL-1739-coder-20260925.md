# BL-1739 — coder pass, 2026-09-25

26 of 27 scenarios fixed. Item 3 (scenario 26) turned out to need far more
than the ticket's own census found; see "Item 3" below for what was fixed,
what was deliberately not chased, and why. A `note` (priority 00) to the
specifier accompanies this parcel's forward.

## Item 1 (scenario 15, "rotate-not-honored-check-9-07") — fixed

`check-rotate-not-honored` (`swarmforge/scripts/babysitterd_sweep_lib.bb`)
gates on `:rotation-router?` (BL-1129: standing packs never rotate, so the
check is suppressed there) - a required field the fixture never set, so the
`and` chain short-circuited and the check always returned nil regardless of
the rest of the scenario's setup. Fixed: the Given step
("the newest completed parcel carries a rotate instruction older than the
grace period") now declares `'rotation-router?': true` - the scenario's own
premise (a rotate note) only ever exists on that topology in the first
place. No scenario text changed.

## Item 2 (scenario 19, "aged-active-claim-counts-as-motion-6d-10") — fixed

`motion-in-process?` (same lib) was changed by BL-1109 to key on
`:abandoned?`, not `:owner-busy?` - its own comment: "a non-abandoned
in_process claim is motion even when the owning pane is idle this sweep...
owner-busy? remains on the claim for stuck-in-process (BL-807); starved
must not require it." The fixture's second Given step only ever set
`'owner-busy?': false`, never `'abandoned?'`, so under BL-1109's real rule
the claim still read as live motion (not starvation) regardless. Fixed: the
`^no other motion is present$` step (its own text is exactly the semantic
BL-1109's rule cares about) now sets `'abandoned?': true` on the aged
claim. No scenario text changed; this is a fixture catching up to a later
ticket's deliberate rule change, not a code regression.

## Item 3 (scenario 26, "the-agent-based-babysitter-is-gone...") — partial, escalated

The ticket's own census (run at mint time) found exactly one offender
(`extension/test/bl1071SweepSurvivesAnyProbeFailure.property.test.js`).
Running the SAME repo-wide scan now (`git ls-files --cached --others
--exclude-standard`, so it also sees this parcel's own new files) found:

- **1 environmental contamination file, not fixed, not this ticket's**:
  `swarmforge/scripts/bl1652-non-vacuity-scratch2-11946-1790318102522.bb` -
  BL-1743's own already-tracked issue (a killed non-vacuity probe leaves
  its scratch copy inside the checkout; a since-morning launch sync spread
  it to seven worktrees, this one included). Untracked, unignored, not
  created by this parcel. Per coder.prompt ("NEVER delete, move, or `git
  clean` a file you did not create") this parcel does not touch it. It
  will keep this ONE line failing in this worktree until BL-1743 lands or
  the worktree is otherwise cleaned - not a defect in this commit.
- **~140 additional legitimate matches**, all individually checked against
  zero hits on `FORBIDDEN_RETIRED_PATTERNS` (a separate, path/content-
  independent safety net scenario 15/26 also runs - see below) before
  being allowed:
  - The bare `extension/test/` prefix rule already existed for
    `swarmforge/scripts/test/` and its own comment already said "every
    test file, regardless of filename" - the code just never matched that
    promise for `extension/test/`. Fixed the actual gap (one prefix line)
    rather than adding individual entries file by file
    (`acpHostRuntime.test.js`, `bl1012FreshnessSelfInflictedIncidents.property.test.js`,
    and whatever comes next).
  - 8 ACP-surface product files (`extension/src/swarm/acp*`,
    `extension/src/tools/acp-host-pane.ts`,
    `extension/src/tools/multiworktreeAcceptanceFixture.ts`,
    `extension/src/tools/operatorPostmortem.ts`,
    `extension/src/metrics/swarmMetrics.ts`,
    `extension/src/bridge/catchUpLive.ts`, individually named + explained)
    reference the current daemon by name/constant/log-line-shape - built
    after this allowlist was last comprehensively touched.
  - `swarmforge/packs/*.prompt` (13 of 14 files) and `swarmforge/roles/*`
    (5 of 13, including the ALREADY-individually-listed coordinator.prompt,
    now subsumed) - each pack/role legitimately documents its own real
    interaction with babysitterd; a directory rule for each, not per-file,
    since the pattern is systematic across the whole directory.
  - `swarmforge/runtime/*` (expedite run logs, handoff drafts, specifier
    updates - durable audit-trail records) - the SAME "history/records"
    posture the pre-existing `backlog/`/`docs/` rule already grants
    wholesale, one directory over.
  - `finish-shift` (repo root) and `swarmforge/handoff-protocol.md` -
    individually named, same shape as already-allowlisted neighbours
    (start-swarm.sh/stop-swarm.sh/swarm-kill; docs/).
- **~59 files still unaddressed**, almost entirely
  `swarmforge/scripts/*.bb`/`*.sh` - the daemon's own deeply-woven core
  surface (chase_sweep_lib.bb, control_plane_lib.bb, expedite_lib.bb,
  handoffd.bb, land_step_lib.bb, operator_runtime.bb, swarm_ensure.bb, and
  dozens more). **Not chased individually or via a directory rule** -
  `swarmforge/scripts/` is the one directory where a blanket rule would
  meaningfully widen what this scan accepts (it is where the actually-
  retired files live), and continuing file-by-file here is no longer
  "fixture and allowlist changes... low effort" (this ticket's own sizing)
  - it is a structural question about whether a curated content-allowlist
  is still the right design at all, now that "babysitterd" is ordinary
  vocabulary across most of that directory.

  **Why this is safe to leave red rather than force through**: scenario
  26's own second check (`forbidden`, `RETIRED_FILE_PATHS.filter(rel =>
  tracked.has(rel))`) is a COMPLETELY SEPARATE, path-based existence check,
  independent of `isAllowedBabysitterMatch` entirely - it is what actually
  guards against a resurrected retired file, and every edit in this parcel
  leaves it untouched and still armed. No change here weakens the real
  regression-detection this scenario exists for; only the (now clearly
  unmaintainable) auxiliary content-scan stays partially red.

  Recommend to the specifier: either (a) mint a follow-up to replace the
  curated content-scan with something that does not need per-file curation
  now that "babysitterd" is this widespread (or drop it, given the
  independent RETIRED_FILE_PATHS/FORBIDDEN_RETIRED_PATTERNS checks already
  cover the real risk), or (b) rule directly that `swarmforge/scripts/`
  becomes a directory-level allow like the four added here, since the
  safety this scenario provides does not depend on the content-scan at
  all. Either way, not a call for the coder to make unilaterally at this
  ticket's stated size.

## Verification run before forwarding

- `node specs/pipeline/cli.js specs/features/BL-611-...feature`: 26 of 27
  (scenario 26 red for the documented, out-of-scope reason above).
- `node specs/pipeline/cli.js specs/features/BL-1129-...feature`: 2 of 2
  (qa_e2e step 2's own bar).
- `npm test` (extension/, compile + unit lane): 637 files / 10868 tests,
  exit 0.
- `git diff main...HEAD --name-only`: exactly
  `specs/pipeline/steps/bl611BabysitterdLifecycleSteps.js` - the one Scope
  file, nothing else (qa_e2e step 3).

## Scope

Touched: `specs/pipeline/steps/bl611BabysitterdLifecycleSteps.js`
(fixtures for items 1/2; allowlist for item 3), this evidence file.
`backlog/standing-reds.tsv` NOT touched (coder.prompt, BL-1663). No BL-611
scenario/Examples/narrative edited anywhere. Nothing else in this worktree
staged - the unrelated untracked leftovers (BL-1666/BL-1652, including the
exact contamination file scenario 26 names) are not this ticket's and are
left as found.

By coder.
