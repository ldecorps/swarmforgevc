# BL-1323 — hardener pass (2026-09-03)

Review-only stamp-off parcel (BL-848). No product code (`swarmforge/scripts/*.bb`)
changed in this parcel's own diff — only a new acceptance step handler, its
registration, a new property test, and evidence files. There is nothing new
for this stage to mutation-test: the reviewed *hotfix* sources
(`babysitter_check.bb`, `handoffd.bb`, `master_main_reconcile_lib.bb`) are
pre-existing and out of scope for this parcel per its own firm line ("Do NOT
reimplement, rewrite or revert the hotfix"), and the parcel's own new files
are `specs/pipeline/steps/` and `extension/test/` — neither is `src/*.ts`, so
CRAP (scoped to `src/*.ts`) does not apply here.

## Re-verification (independent, this pass)

- `bb swarmforge/scripts/test/master_main_reconcile_lib_test_runner.bb` →
  ALL TESTS PASS.
- `bb swarmforge/scripts/test/babysitterd_sweep_lib_test_runner.bb` → ok.
- `node specs/pipeline/cli.js specs/features/BL-1323-…feature` → 7/7 scenarios
  pass.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1323StampOffInvariants.property.test.js` — run **6 consecutive
  times**, 3/3 tests clean every time (on top of the coder's 14 and the
  architect's 12+7 independent clean runs already on record for the D1
  non-flakiness fix). No flake observed.
- Required wiring: `specs/pipeline/steps/index.js:20` registers
  `bl1323MainSyncDeadlockOverlapHintsStampSteps` — confirmed present.
- `backlog/hotfix-ledger.yaml` row for `9c94735f03`: still `state:
  stamp-open`, `human_decision: null` — untouched by this parcel, as required.
- `grep -rn 'wait for BL-891 reconcile' swarmforge/scripts/*.bb` — no hits;
  the generic pre-hotfix alert text stays gone.

## Fixture hygiene (own-domain check)

`bl1323MainSyncDeadlockOverlapHintsStampSteps.js`'s `newRoot()` sweeps stale
`bl1323-acceptance-*` dirs from `os.tmpdir()` by prefix before creating a new
one (BL-971 pattern), so a killed run cannot accumulate fixture litter across
runs. No tmux/bridge usage in this step file (grepped for `startBridge`/
`tmux` — none), so the cross-step bridge-leak hazard from prior lessons does
not apply. No `bl1323-acceptance-*` directories left in `/tmp` after this
pass's runs.

## Conclusion

Nothing to harden — the hardening pass itself added no tests or fixes.
Forwarding the merged commit unchanged to the documenter per the Handoff
section's no-op-hardening rule (a docs/review-only parcel is still a real
deliverable and must not stall the pipeline).

By hardender.
