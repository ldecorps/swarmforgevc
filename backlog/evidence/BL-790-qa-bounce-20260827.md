# BL-790 — QA bounce — 20260827

## Failing command
`specs/pipeline/scripts/run_acceptance.sh specs/features/BL-790-bridge-queues-a-note-for-a-role.feature`

## Commit tested
`e289212459` (tip before bounce evidence)

## First error excerpt
```
✖ a note for a declared role is queued into that role's mailbox
Error: failed at step "Then a note parcel is queued for that role at priority 00":
expected a new note parcel in the role mailbox
```

Also fail: `a dormant role is a valid recipient` (same — no parcel queued);
`the queued note is attributable to the operator` (no parcel to inspect).

## Failure class
acceptance

## Expected vs observed
Expected: POST /agent-notes queues priority-00 note parcels for declared/dormant roles. Observed: 3/8 scenarios fail on missing mailbox parcel; 5 refusal/auth scenarios pass. Unit `agentNotesCore.test.js` 17/17 green.

## Remediation (coder)
Fix bridge send path so acceptance happy-path scenarios queue real note parcels via swarm_handoff.bb. Check route dispatch, handoff target paths, and roles.tsv fixture alignment in bl790BridgeQueuesNoteForRoleSteps.

By QA.
