# BL-663 QA bounce — 2026-08-01

## Inventory (Article 4.4 — complete pass, single item found)

### D1 — architect stage pass missing entirely

- **Failing command**:
  `git log --graph --oneline ffa509a8a..422ae07db5` (the full BL-663 commit
  range QA received), cross-checked with
  `git log --oneline --all --since="2026-08-01 15:00" --grep=architect -i`
  (zero hits) and `git show --stat <each merge commit>` for the `By <role>.`
  byline on every merge in the chain.
- **Commit hash**: 422ae07db50198fcff52ec31a2e94bc0565d672f (documenter's
  handoff to QA). The gap is specifically between `e70dc132547215d71411782a79ba9f2c5e4dc74b`
  ("Merge coder 3f66c55230 (BL-663)", `By cleaner.`) and
  `6316d9c09aee8e6aab55332cf7a461194f105b55` ("BL-663: harden
  promotion_gates_cli.bb dispatch/output contract...") — `6316d9c09`'s sole
  parent is `e70dc1325`, i.e. hardener branched directly off cleaner's
  forward.
- **First error excerpt** (the graph across the full received range):
  ```
  * 422ae07db BL-663: document the promotion_gates chokepoint         (By documenter.)
  * deb0e5bd6 Merge hardender 6316d9c09a (BL-663)                     (By documenter.)
  * 6316d9c09 BL-663: harden promotion_gates_cli.bb ...               (hardener's own commit)
  *   e70dc1325 Merge coder 3f66c55230 (BL-663)                       (By cleaner.)
  |\
  | * 3f66c5523 BL-663: one chokepoint for every promotion gate       (coder's own commit)
  | *   9e97544dc Merge specifier 67d2741ef9 (BL-663)                 (specifier)
  ```
  No commit or merge anywhere in the range carries `By architect.`, and no
  commit message or grep hit references "architect" at all.
- **Failure class**: `behavior` (the pipeline's own required behavior —
  every parcel gets an architect design/security/pattern review before
  hardening, per constitution Article 1.5 and Article 4.1 gate 2 — did not
  happen; same class BL-575's missing-documenter-pass precedent used).
- **Expected vs observed**: Expected an architect merge/review commit for
  BL-663 between cleaner's forward (`e70dc1325`) and hardener's pickup,
  reviewing the new `promotion_gates_lib.bb` / `promotion_gates_cli.bb`
  chokepoint and the rewritten `promote_and_route_next.sh` /
  `route_backlog_to_coder.sh` for scalability, security, and design
  patterns (Article 1.5) — this ticket is itself an architecture-relevant
  change: a new single enforcement chokepoint for backlog promotion, exactly
  the kind of design decision Article 4.1 gate 2 exists to review. Observed:
  zero architect involvement anywhere in the ticket's commit ancestry;
  hardener began hardening a commit the architect never saw.

## Remediation pointer

Owning role: **architect** (Article 1.5 — design review of a new
chokepoint/enforcement layer is squarely architect's domain; this is not a
coder/cleaner/hardener/documenter defect, and the absence of the stage's own
output is that stage's defect per Article 4.3's "stage pass MISSING
ENTIRELY" clause).

Route: bounce `git_handoff` to **architect**, holding commit
`e70dc132547215d71411782a79ba9f2c5e4dc74b` (cleaner's forward — the point
the architect should have picked up from) merged forward through hardener's
and documenter's work (`6316d9c09`, `deb0e5bd6`, `422ae07db5`) so no
hardening/doc work is lost; architect reviews the whole BL-663 diff
(`promotion_gates_lib.bb`, `promotion_gates_cli.bb`,
`promote_and_route_next.sh`, `route_backlog_to_coder.sh`) for scalability,
security, and design-pattern soundness, then forwards to hardener as usual.

## Checks run and their results (informational — not the bounce cause)

- Unit suite (`npm test`, extension/): 399 files / 7052 tests, all green.
- Property suite (`npm run test:properties`): 38 files / 124 tests green on
  retry; `test/bl760DuplicateChainGuard.property.test.js` timed out once
  under full-suite load (20s budget) and passed standalone in 8s and again
  in a full-suite retry (23.5s) — pre-existing flake on an unrelated
  (BL-760) ticket's test, not a BL-663 regression; not bounced.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-663-promote-and-route-enforces-every-promotion-gate.feature`):
  9/9 scenarios pass.
- `bb swarmforge/scripts/test/promotion_gates_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/promotion_gates_lib_property_runner.bb`: ALL PROPERTIES HOLD (500 runs each).
- `bb swarmforge/scripts/test/promotion_gates_cli_test_runner.bb`: ALL PASS.
- `env -u SWARMFORGE_CONFIG bash swarmforge/scripts/test/test_promote_and_route_next_priority.sh`: ALL PASS.
- `required_wiring` grep: `promotion_gates` present in both
  `promote_and_route_next.sh` (11 hits) and `route_backlog_to_coder.sh`
  (3 hits).
- No dangling consumers of the removed shell helpers
  (`ticket_priority`/`ticket_id`/`candidate_sort_line`/`pick_lowest_sort_line`)
  found elsewhere in `swarmforge/`.
- Documentation (`docs/diagrams/swarm-flow.mmd`,
  `docs/reference/Specification.MD`) updated and consistent with the new
  chokepoint.
