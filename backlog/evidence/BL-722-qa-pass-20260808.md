# BL-722-pilot-safe-defects — QA final pass (2026-08-08)

## Commit checked

`a3b5a01e58` (documenter, tip of the shared BL-846/BL-722 chain) — the same
commit already merged into QA as `60a2ab27` while processing BL-846
(a separate parcel, per Article 2.6 the documenter forwarded each ticket as
its own `git_handoff` against the same commit). No new merge needed here.

## Prior bounce check (required per the constitution's "prior QA bounce" rule)

`git log --oneline main -- 'backlog/evidence/BL-722*'` shows an earlier
documenter bounce, `70ed2222` (`backlog/evidence/BL-722-bounce-20260808.md`):
the ticket's own `required_wiring` literal ("pilot safe list/start handlers")
was missing from `telegramCursorBridgeLive.ts`, tripping
`swarm_handoff.sh`'s `PRE_QA_GATE` before ever reaching QA. Confirmed fixed:
coder's `5042cc22` ("BL-722: land the required_wiring literal for pilot-safe
handlers") is an ancestor of `a3b5a01e58`
(`git merge-base --is-ancestor 5042cc22 a3b5a01e58` holds), and the literal
comment is present at `telegramCursorBridgeLive.ts:855`.

## required_wiring re-verified by reading the code (all four items)

1. `extension/src/tools/pilotSafeDefects.ts::listSafePilotDefects` — exported
   pure filter+rank function, line 151.
2. `extension/src/tools/telegramCursorBridgePilot.ts::parsePilotSafeCommand`
   — exported, line 34.
3. `extension/src/tools/telegramCursorBridgeCore.ts::decideInboundAction` —
   `/pilot safe` and `/pilot safe --list` both dispatch through the
   `pilot-safe-start`/`pilot-safe-list` actions (line 118-119, 324, help
   text at 768).
4. `extension/src/tools/telegramCursorBridgeLive.ts::pilot safe list/start
   handlers` — literal marker comment present at line 855, handlers wired at
   856/858.

## Checks run

- Compile (`npm run compile`, node v20.20.2): clean (already verified while
  processing BL-846 against the same merged tree).
- Scoped suite (`npx vitest run pilotSafeDefects telegramCursorBridgePilot
  telegramCursorBridgeCore telegramCursorBridgeLive`): 271/271 passed.
- Acceptance (`node specs/pipeline/cli.js
  specs/features/BL-722-pilot-safe-defects.feature`): 4/4 scenarios PASS —
  safe list filters correctly, safe start picks the top-ranked ticket, an
  empty safe pool refuses to start (never falls back to a wider filter), and
  explicit `/pilot BL-xxx` remains an escape hatch. Covers all three of the
  ticket's declared invariants directly.
- No orphaned test/mutation/vitest processes before or after
  (`pgrep -fl 'node --test|stryker|vitest'` clean).
- Full-suite flakiness analysis (dependencyGateCli node-version mismatch,
  renderBriefingDiagramsCli/startBridgeHeadlessCli load-induced timeouts)
  already covered in `backlog/evidence/BL-846-qa-pass-20260808.md` against
  this same merged tree — none of those failures touch
  `pilotSafeDefects.ts`, `telegramCursorBridgePilot.ts`,
  `telegramCursorBridgeCore.ts`, or `telegramCursorBridgeLive.ts`.

## Verdict

PASS. Approving BL-722-pilot-safe-defects.
