# BL-1306 — hardener pass (2026-09-03)

Real fix in `swarmforge/scripts/swarm_handoff.bb` (delivery machinery this
very pipeline runs on), already reviewed by cleaner and architect with no
defect found. No `src/*.ts` files touched — CRAP not applicable.

## Re-verification (independent, this pass)

- `bb swarmforge/scripts/test/bl1306_audit_reroute_test_runner.bb` — ALL PASS
  (7 assertions).
- `node specs/pipeline/cli.js specs/features/BL-1306-handoff-audit-reroute.feature`
  — 4/4, including scenario 03 (edited draft still re-challenges).
- `npx vitest run --config vitest.properties.config.mjs
  bl1306AuditKeyBasisInvariants` — 6 consecutive clean runs (2/2 tests
  each), on top of coder's and architect's own clean runs.
- Sibling regression check (the coder's concurrency-safe sweep fix touched
  the three OTHER acceptance handlers from this same session): re-ran all
  three independently — BL-1323 (7/7), BL-1332 (6/6), BL-1343 (6/6), all
  still green.
- Workaround retirement confirmed: `grep -rn BL-1306
  swarmforge/handoff-protocol.md swarmforge/roles/hardender.prompt` — no
  hits. The stale-premise text this ticket existed to retire is gone.

## Hand-authored mutation sweep on `swarm_handoff.bb` (BL-638 fallback, Babashka has no wired mutation tool)

**Mutant 1 — reintroduce the pre-fix key basis** (pass the raw drafted
`to:` header instead of `(:recipients routed)` at the call site): **killed**
— exactly the guarded scenario failed
("an identical second invocation QUEUES rather than re-challenging"), all
others stayed green. Restored; `git diff` on the file was empty afterward.

**Mutant 2 — drop the `:version` dissoc** (`invocation-fingerprint` returns
the full `audit-candidate` map instead of dissoc'ing `:version`):
**survived** — both the bb runner and the acceptance feature stayed green.
Investigated rather than accepted on sight: `:version` is a hardcoded
literal (`{:version 1 ...}` in `audit-candidate`, the only place it is set)
identical on both the stored side and every freshly-computed lookup side
within this running binary, so `select-keys candidate (keys invocation)`
can never disagree on it regardless of whether it is present. This is a
BL-234-class equivalent mutant, demonstrable from the code, not a test
gap — and the function's own docstring says exactly why the field is
dropped anyway: forward-compatibility if a future schema bump changes
`:version` while an old on-disk audit file still carries the prior value,
a case this session's fixtures cannot construct (they always write the
current binary's `:version 1`) and which no test in THIS ticket's scope
needs to reach. Restored; `git diff` on the file was empty afterward.

## Fixture hygiene (own-domain check)

`bl1306HandoffAuditRerouteSteps.js`'s fixture root uses the shared
`mkTmpDir` helper (BL-1280), not raw `mkdtempSync`. A stray
`/tmp/bl1306-dbg-*` directory (not matching this file's own
`bl1306-acceptance-` prefix, so outside its sweep) is present from what
looks like a manual live-check debug session (the ticket's own
qa_e2e_procedure step 2 calls for one) — not created by this hardening
pass, so left in place per "never delete what you did not create."
Several `bl1343-acceptance-*` directories under 5 minutes old were also
present at check time, inside the 10-minute age-guard window and
consistent with another role's concurrent acceptance run in this live
swarm — not touched, for the same reason the age-guard exists (a sibling
scenario's live root).

## Conclusion

No test gap found in the fix itself. One investigated survivor recorded as
an accepted BL-234-class equivalent with its code-level reason. Forwarding
to documenter.

By hardender.
