# BL-1391 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked

- `master_main_reconcile_lib_test_runner.bb` — re-ran: ALL TESTS PASS.
- `test_bl1391_bookkeeping_conflict.sh` — re-ran: 14/14 PASS, including
  invariant 1's all-or-nothing case (a conflict spanning one code path
  and one bookkeeping path refuses and resolves nothing), invariant 2's
  loss cases (a rewritten scalar or a deleted evidence paragraph refuses
  rather than silently dropping content), and the guard-chain case (a
  resolved absorb that the commit guards would refuse makes no merge
  commit and is logged as a refusal, not a resolution — the resolved
  bookkeeping path still passes the same guards any merge on `main`
  does, per invariant 3).
- `bl1391_bookkeeping_conflict_property_runner.bb` — re-ran: ALL
  PROPERTIES HOLD over 515 constructed cases.
- Algorithm read directly: `append-only-merge`'s `insertion-blocks`
  matches each side against the base by consuming base lines in order;
  a side that deletes or rewrites a base line leaves it unconsumed and
  the whole file refuses (`nil`), never resolving partially. A line
  both sides added is de-duplicated (`remove (set ...))`), matching
  invariant 2's "survives once" requirement rather than doubling it.
- `bookkeeping-path?`'s set is exactly the four narrow prefixes plus
  ticket YAML the ticket's own invariant 1 names — confirmed no drift
  from the yaml file's stated set (`backlog/topics/`,
  `backlog/evidence/`, `backlog/answers-archive/`, `docs/briefings/`,
  and any `backlog/*.yaml`).
- `required_wiring` — both anchors confirmed: `bookkeeping-conflict` is
  logged by name at the daemon dispatch site (`handoffd.bb`, four call
  sites for refused/resolved/refused-by-guards), and the step handler
  registers via BL-1371's discovery.
- `swarmforge/scripts/test/suite-manifest.tsv` — both new test files
  correctly registered in the standing lane.
- `mutation-site-count.js` on the new step handler: 156 sites (`over`
  100). Reviewed and declined to split, same reasoning as every other
  ticket this session: one cohesive single-feature handler.
- `jscpd` over the new handler and the two touched `.bb` files: 0
  clones.
- TypeScript compiles clean; the handler discovers via BL-1371's
  registry.
- Babashka/shell have no mutation/CRAP/DRY wired (Startup Tools,
  BL-472 deferred); gated by the two runners and the shell suite
  instead, per the documented degraded fallback.

No bounce history on BL-1391 (first pass). Forwarding unchanged to
architect.

By cleaner.
