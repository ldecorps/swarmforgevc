# BL-1202 — QA pass — 20260828

**Received:** `merge_and_process documenter 3245f5a82d` (handoff
`00_20260828T032935Z_000886_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `3245f5a82d...`
**QA merge commit:** `167aa662183ccc75be5cfd22bcf3676446feed7b`

Single pass, no bounce. Fixes the detector half of the 2026-08-27
shared-repo-canary incident (BL-1188's branch-corrupting property-suite
guard escape) — the reporting half never blocks other in-flight tickets
because it touches a single bash script only.

## Merge

Clean, no conflicts (`git merge --no-ff 3245f5a82d`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor c6705b444 HEAD` (coder) — CONFIRMED
- `git merge-base --is-ancestor f8524fb3c HEAD` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 7858fce2e HEAD` (architect) — CONFIRMED
- `git merge-base --is-ancestor 2af11a059 HEAD` (hardener) — CONFIRMED
- `git merge-base --is-ancestor 3245f5a82 HEAD` (documenter, received tip) — CONFIRMED

## required_wiring

`swarmforge/scripts/check_property_suite_drift.sh` — confirmed both traps
installed in the guard that actually runs (not only the sourced lib):
`trap on_interrupt INT TERM` and `trap 'report_canary_once || true' EXIT`
(lines 157-158).

## Verification inventory (independently re-run, not just re-read)

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1202` | `VERIFY BL-1202` — no open deferral |
| Shell unit suite | `bash swarmforge/scripts/test/test_property_suite_drift_guard.sh` | 16/16 PASS (isolated `mktemp -d` fixtures, no real-repo risk — confirmed by reading the fixture setup before running) |
| Acceptance | `run_acceptance.sh` on the BL-1202 feature | 4/4 PASS |
| Invariant 1 (canary reported on every exit path) | Scenarios 14 (SIGTERM) and 16 (SIGHUP) | Architect independently re-verified scenario 14 non-vacuous against the pre-fix baseline commit in a throwaway worktree; hardener independently re-verified scenario 16 non-vacuous the same way (removed the EXIT trap, confirmed ONLY scenario 16 fails) |
| Invariant 2 (no process outlives the guard) | Scenario 15 | Checks both direct child and grandchild by PID/process-group liveness, not by name |
| Orphaned processes (mine, post-run) | `ps aux \| grep 'sleep 30'`, `pgrep -fl` | none, before and after my own run |
| Short-circuits unchanged | Scenarios 05 (toolchain-missing), 07 (docs-only), 08 (BL-1121 reconcile-import), 10 (SKIP env var) | All still green, traced by architect through the control flow (`[[ -n "$BEFORE" ]] \|\| return 0` makes the EXIT trap a no-op for every short-circuit) |
| A real SIGHUP gap found and closed (hardener) | Hand-mutation: deleted the EXIT trap line, confirmed only the new SIGHUP scenario fails, all others including the SIGTERM scenario still pass (SIGTERM is separately caught by `on_interrupt`) | Genuine gap, not equivalence — verified live per the project's own "verify before calling it a gap" rule |
| Constraint: BL-1124's own `bl1124_snapshot`/`bl1124_assert_unchanged` unchanged | Out of scope, not touched | Confirmed — this ticket's diff is scoped to `check_property_suite_drift.sh` and its own test/acceptance files only |
| Docs | `docs/how-to/BL-570-property-suite-drift-guard.md` (+6 lines, cross-linking) and `docs/how-to/BL-1124-property-suite-fixtures-must-not-mutate-shared-main.md` (+section, anchor `#canary-fires-on-every-exit-path-including-a-kill-bl-1202` confirmed to exist) both updated in place; both pages already linked from `docs/index.md:114,118` so no new index entry was needed; `Specification.MD` top entry accurate | Present and accurate |

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1202's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1202 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1202 active → done).

By QA.
