# BL-1615 — QA review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Sibling check: `qa-sibling-check.js status --ticket BL-1615` -> `VERIFY
BL-1615` (no open deferral). Register grep (standing-reds.tsv, allowlist,
suite-poles.tsv): no row for BL-1615.

- Prior bounce (BL-340): `bounce_count: 1`, architect bounce on baf4eea434
  (D1: a refused claim requeued a seat-addressed candidate into the shared
  stage queue, violating invariant 2; D2 secondary: qa_e2e_procedure step 2
  runner not extended, non-blocking). Verified D1's fix
  (`c035cc871d`): `origin-new-dir-for` resolves the requeue target from the
  handoff file's own `recipient:` header (seat's own mailbox vs. shared
  stage queue), both call sites (resume + fresh-dequeue) updated, dead
  single `new-dir` binding removed. New scenario 04 exercises exactly the
  architect's remediation direction (seat-addressed candidate, dirty
  worktree, guard refusal, requeue lands back in the seat's own new/,
  never the stage queue) — 8/8 BL-1615 scenarios green, including it.
- D2 (runner not extended): confirmed still not done
  (`ready_for_next_claim_task_name_runner.bb` untouched by any BL-1615
  commit — it tests unrelated `claim-task-name` attribution, BL-1608).
  Judgment: not a blocking gap — the new acceptance scenario 04 already
  gives concrete, end-to-end behavioral coverage of the origin-aware
  requeue logic the runner would have unit-tested narrowly; the ticket's
  own "How" names the runner as direction ("or a sibling"), not mandate,
  and `required_wiring` does not name it. Architect independently reached
  the same non-blocking judgment on D2.
- Ancestry/wiring gate: `pre_qa_gate.sh` -> OK.
- Acceptance: BL-1615 8/8 (including scenario 04 above). Regression named
  in qa_e2e_procedure, all unchanged: BL-983 5/5, BL-1004 5/5, BL-1001
  6/6, BL-1167 3/3.
- Docs: the ticket's "How" named `handoff-protocol.md`'s "BL-983 seat
  paragraph" as direction, but no such paragraph exists there currently
  (`grep -i seat swarmforge/handoff-protocol.md` — no stage-queue-dir
  content at all). The documenter instead updated
  `docs/how-to/BL-1004-cross-seat-rework-claim-deferral.md`, the actual
  location this seat/stage-queue behaviour is documented, correctly
  explaining the union claim and that it's orthogonal to BL-1004's own
  deferral logic. Reasonable substitution of a stale direction, not a
  defect. Diagram: `docs/diagrams/handoff-flow.mmd` never drew a
  seat-vs-stage-queue distinction (only a generic `new/` lifecycle) —
  its change-trigger did not fire, correctly left untouched.
- Negative check (qa_e2e step 4, no sibling-seat leak): covered
  programmatically by acceptance scenario 01 row 4 ("a file in a seat's
  own new/ is never listed by a sibling seat"); not reproduced by hand
  against a live coder@2 mailbox to avoid touching another worktree's
  live state.
- Unit suite: `npm test` — 626/626 files, 10677/10677 tests passed.
  Exit 1 from "Vitest caught 100 unhandled errors" — all 100 share the
  identical `Object.onTimeoutError ... onTaskUpdate` signature (the BL-871
  known-benign vitest-worker RPC-timeout class), coincident with sustained
  heavy concurrent host load this session (11–14 concurrent
  vitest/stryker processes from other worktrees, load average 12–22
  throughout). Same host-load-noise class already diagnosed twice today
  on this host (BL-1621, BL-1582 evidence) — here manifesting as RPC
  timeouts under volume rather than a per-file duration budget. Zero
  actual test or file failures. Not a regression: BL-1615 touches only
  Babashka scripts, the feature, and the handler — nothing in
  `extension/src` or the vitest harness itself.

By QA.
