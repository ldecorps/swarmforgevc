# BL-1332 — hardener pass (2026-09-03)

## Fixed: the architect's flagged assertion-strength gap in scenario 04

Both the cleaner and the architect flagged (but neither owns, per their role
prompts) that scenario 04's Then-step
("the feature-handler registration guard passes against every tip produced")
only asserted `r.status !== null` — proving the guard *ran*, never that it
*passed*. The architect independently reproduced the fixture by hand and
found the real guard exits 1 against it (correctly refuses the dangerous
shape), and corrected the naive remedy direction: `assert.equal(r.status, 0)`
would break the scenario, since `st.root`'s committed HEAD legitimately
carries the dangerous shape by construction (this scenario's own fixture,
not a hypothetical).

Applied the architect's verified correction in
`specs/pipeline/steps/bl1332SharedPathLineLeakSteps.js`: added
`assert.notEqual(r.status, 0, ...)` alongside the existing "guard ran at
all" check. This affirmatively proves the fixture reproduces the historically
dangerous shape and that the guard would reject it if it ever shipped raw —
strengthening what was previously a vacuous-on-status assertion.

**Reproduced independently before trusting the architect's finding**: built
the identical fixture by hand (landing ticket's commit + a shared
`index.js` carrying an unlanded sibling's `require()` with no backing
handler file) and ran `check_feature_handler_registration.sh` against it
directly — confirmed `status=1`, "missing or unreadable registry module".

**Verified the new assertion is not vacuous**: `run_acceptance.sh` on the
feature — 6/6 pass with the strengthened assertion in place.

## Hand-authored mutation sweep on `land_step_lib.bb::own-paths` (BL-638 fallback)

Babashka has no wired mutation/CRAP/DRY tool (engineering.prompt, Startup
Tools) — surgical hand-mutation per the BL-638 fallback pattern, on the
ticket's own new `cond` branch:

- `and` → `or` (both operands of the shared-path refusal condition): **33
  test failures** — killed loudly, restored, empty diff, suite ALL PASS.
- `some unlanded-siblings owners` → `every? unlanded-siblings owners`
  (the fold-over-owners predicate — this is the "some vs every?" fold class
  from prior hardening lessons): **6 test failures** — killed cleanly,
  restored, empty diff, suite ALL PASS.

Both hand-authored mutants of the new logic are killed by the coder's
existing 7 new test cases; no test gap found in the library change itself.
Coverage already spans every ownership shape the new branch's boolean logic
can take (landing-only, sibling-only, shared-with-unlanded, shared-with-
landed-not-unlanded, unattributed, unreadable-attribution), so no additional
test was needed on the `.bb` side.

## Re-verification (independent, this pass)

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` → ALL PASS
  (both before and after the mutation probes, with an empty diff after
  restore).
- `node specs/pipeline/cli.js specs/features/BL-1332-…feature` → 6/6, with
  the strengthened scenario 04 assertion in place.
- `npx vitest run --config vitest.properties.config.mjs
  bl1332SharedPathRefusesInvariants` — 5 consecutive clean runs (2/2 tests
  each), on top of the coder's 5 and architect's 8.
- CRAP: not applicable — no `src/*.ts` files touched by this parcel.
- Required wiring: `land_step_cli.bb:65` still calls `land-step-lib/replay!`;
  `specs/pipeline/steps/index.js` registers `bl1332SharedPathLineLeakSteps`
  — both confirmed present.
- `backlog/hotfix-ledger.yaml`: not touched by this parcel (not applicable —
  BL-1332 is not a stamp-off ticket).
- No orphaned `node --test`/`stryker` processes; no leaked
  `bl1332-acceptance-*` fixture directories in `/tmp` after this pass.

## Not touched

The cleaner's `SWARMFORGE_ROLE: 'QA'` observation (unused env var passed to
the guard spawn, harmless) — cosmetic, no behavior at stake, left as noted.

By hardender.
