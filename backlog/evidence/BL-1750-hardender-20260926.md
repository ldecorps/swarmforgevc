# BL-1750 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Verified: BL-458 acceptance 6/6; the real host's own live front-desk bridge
(pid 15667, port 8765) confirmed alive/unchanged (same pid, `GET /pipeline`
-> 401) before and after the run; no leaked fixture process (`pgrep -af
aps-fixture-reaper-abnormal-exit-` matches only the check's own argv);
fix diff verified against `front_desk_supervisor.bb` line 172/185 and
`fleet_telegram_creds_lib.bb`'s `resolve-telegram-creds` (creds-file
`bridgePort` wins wholesale over the passed `BRIDGE_PORT` default) —
matches the coder's own diagnosis exactly; pattern matches
`bl789MacHostSwitchFreshnessBridgeAdoptSteps.js`'s established
`SWARMFORGE_FLEET_HOME` convention. All 22 whole-tree guard files
(211/211, required by touching `specs/pipeline/steps/lib/`). Did NOT
repeat the coder's own manual pre-fix repro (killing the real bridge) —
already documented thoroughly with a quoted log line; redoing it against
this same live host would risk the same real kill for no new information.

Out-of-scope discovery, reported separately (priority-00 note to
specifier, not a bounce — BL-1750's own diff is correct and complete for
its stated scope): five OTHER step-handler files spawn the real
`launch_front_desk.sh` with the real `$HOME` and no `SWARMFORGE_FLEET_HOME`
override, so any of them can hit the exact same live-bridge-kill hazard
this ticket just fixed for one harness — confirmed via
`~/.swarmforge/fleet/primary/telegram.json` on this very host recording
`"bridgePort": 8765`, which `resolve-telegram-creds` returns
UNCONDITIONALLY over any `BRIDGE_PORT` a fixture passes, once a creds file
exists for the resolved swarm-name (a fixture project-root with no
`.swarmforge` swarm-identity marker defaults to `"primary"` — the same
name this host's own real swarm is registered under). Confirmed files:
`bl404LaunchFrontDeskHonorsParkedFlagSteps.js`,
`bl629SyncQaApprovalGateSteps.js` (its own comment already names the
credential-leak half of this hazard and cites
`mergedCodeReachesDaemonsSteps.js`, but its "explicit allowlist" fix only
solves the TELEGRAM_* leak, not the fleet-creds-file bridgePort override —
the real `HOME` alone is enough to trigger it), `frontDeskSurvivesRebootSteps.js`,
`mergedCodeReachesDaemonsSteps.js`, `frontDeskHeadlessLauncherSteps.js`.
Ruled out (read-only references to the launcher's path/content, never
execute it): `bl1064LogGroundingSourceSteps.js`, `bl461StartSwarmEnsureFullStackSteps.js`,
`bl643NonPipelineAgentsSteps.js`, `systemdUnitsCanStartSteps.js`.
