# BL-1577 coder evidence — 2026-09-16

## What changed

Drove the 19 no-coverage mutants the census
(`backlog/evidence/BL-1509-survivor-census-specifier-20260915.md`) named to
zero by adding behaviour tests that reach each unreached branch through its
real caller, asserting what the caller observes rather than a literal for
its own sake. No production code changed — every gap was a missing test,
not a missing seam.

- `extension/test/telegramClient.test.js` (12 new tests, `setChatMenuButton`
  added to the import list):
  - `getForumTopicIconStickers` returns `[]` when `result` is not an array
    (malformed shape) — kills `extractIconStickers`'s BlockStatement/
    ArrayDeclaration no-coverage pair.
  - `getForumTopicIconStickers` defaults `customEmojiId` to `''` when a
    sticker carries no `custom_emoji_id` — kills its StringLiteral
    no-coverage mutant.
  - `resolveForumTopicName` returns `undefined` when the probe reply carries
    a `reply_to_message` that is NOT itself a `forum_topic_created` service
    message — kills `extractForumTopicCreatedName`'s BlockStatement
    no-coverage mutant (the `!created` guard, not the already-survived
    `!reply` guard).
  - `setChatMenuButton` posts `{ type: 'default' }` — the ternary's other
    arm was never called anywhere in the suite; kills all three no-coverage
    mutants on that branch.
  - `getTelegramUpdates` returns an empty batch when `result` is not an
    array — kills `extractUpdates`'s ArrayDeclaration no-coverage mutant.
  - `sendTelegramMessage` defaults `callback_data` to `''` for a button with
    neither `callbackData`, `webAppUrl` nor `url` — kills
    `inlineKeyboardButtonToWire`'s StringLiteral no-coverage mutant.
  - `sendTelegramMessage` reports `"unknown error"` when the thrown failure
    is not an `Error` instance (`throw 'connection reset'`) — kills
    `formatNetworkError`'s StringLiteral no-coverage mutant; every prior
    network-error test always threw a real `Error`.
  - `answerCallbackQuery` called with NO `postFn` (uses `defaultPost`),
    `global.fetch` stubbed — every existing test supplied its own mock
    `postFn`, so `defaultPost`'s real-fetch body was never executed at all.
    Kills its BlockStatement no-coverage mutant.
  - `sendVoiceNote` called with NO `postVoiceFn` (uses `defaultPostVoice`),
    same stubbed-`global.fetch` pattern as
    `sendTelegramDocumentCli.test.js`'s existing precedent. Kills
    `defaultPostVoice`'s BlockStatement no-coverage mutant.
  - `createForumTopicWithRateLimitRetry` called with NO `wait` (uses
    `defaultWaitMs`), driven with `vi.useFakeTimers()` +
    `vi.advanceTimersByTimeAsync(1000)` through a real 429-then-success
    sequence — every existing `*WithRateLimitRetry` test supplied its own
    fake `wait`. Kills both `defaultWaitMs` no-coverage mutants (the
    `setTimeout` call and its arrow-function body).

- `extension/test/bl568MenuAnswerPollMapping.test.js` (2 new tests):
  - `bl568PlanMenuAnswerDrive` with `optionIds: []` returns
    `{ action: 'drop', reason: 'empty-vote' }` — the existing file only
    exercised `stale-fingerprint` and `inject`; the empty-vote branch had no
    test at all. Kills the BlockStatement/ObjectLiteral/StringLiteral×2
    no-coverage cluster.
  - `bl568TextFallbackMessage` called with no `rcHint` — every existing call
    passed one. Kills its StringLiteral no-coverage mutant (the `''`
    fallback).

`extension/src/**` is unchanged (confirmed via `git diff main --name-only`
— only the two test files above are touched).

## Verification

- `npx vitest run test/telegramClient.test.js test/bl568MenuAnswerPollMapping.test.js`:
  108/108 pass (102 + 6).
- `npx vitest run --coverage --coverage.include='out/notify/telegramClient.js' --coverage.reporter=text test/telegramClient.test.js test/bl568MenuAnswerPollMapping.test.js test/notifyResidentSpyTunnelCli.test.js test/sendTelegramDocumentCli.test.js`
  (the four files that exercise this module without a full-suite run):
  99.05% stmts / 93.45% branch / 98.21% funcs, remaining uncovered lines
  39-40 (`defaultPost`'s `res.json()` catch — never named by the census),
  185 (`extractResultObject`'s fail-branch `return undefined` — survived-
  only, s8/n0, out of this ticket's scope), 302 (`extractPollId`'s
  fail-branch `return undefined` — survived-only, s7/n0, out of scope), and
  1052-1057 (`defaultDownload`, exercised elsewhere in the full suite via
  `telegram-front-desk-bot.ts`'s no-`download`-arg call, not in this
  4-file slice). None of these four gaps is a census-named no-coverage
  mutant; every function the census named with a no-coverage mutant now
  shows 0 uncovered lines in this scoped report.
- Full suite: `npx vitest run` — 620/620 test files, 10525/10525 tests
  green (no standing-red observed this run).

## Not done here (per the ticket's recommended ruling option, tapped)

The full re-run of Stryker over `out/notify/telegramClient.js` with a
`json` reporter, the confirmation of zero no-coverage mutants at the
mutation level, and the per-survivor disposition table (killed / accepted
equivalent / grandfathered under BL-1519 / owned elsewhere) at
`backlog/evidence/BL-1577-telegramClient-mutation.md` is the hardener's
stage per this ticket's own "How" section (mirrors BL-1509's hardener
precedent) — not run here to avoid a duplicate 9+ minute mutation pass
before cleaner/architect have touched the diff.

## Acceptance wiring (BL-112, required_wiring)

Added `specs/pipeline/steps/bl1577TelegramClientFirstRunSurvivorsSteps.js`
(no prior handler resolved the feature's Given/When/Then text; verified by
grep — no other step file matches `the parcel's discharge evidence for
out/notify/telegramClient.js is read`). It reads the fixed evidence path
`backlog/evidence/BL-1577-telegramClient-mutation.md` the ticket names and
defines the markdown contract that file must satisfy (documented in the
step file's header comment): `Include set:` / `Instrumented:` /
`No-coverage:` / `Survived:` summary lines, a `## No-coverage regions
reached` section (one row per function the BL-1509 census named — hardcoded
as `KNOWN_NO_COVERAGE_FUNCTIONS`, all eleven load-bearing per
engineering.prompt), and a `## Survivor disposition` section whose row
count must equal `Survived:`.

- `run_acceptance.sh` against the feature: all three scenarios currently
  **fail** — the evidence file does not exist yet, because the full
  Stryker run and disposition table is the hardener's stage, not the
  coder's (Article 1.6; this ticket's own "How" section). This is the gate
  correctly reporting undischarged work, not a coder-stage defect.
- Sanity-checked the parsing logic itself against a synthetic fixture
  evidence file matching the documented schema (not committed — written
  to a scratch path, run, then removed): all three scenarios passed,
  confirming the handler will go green once the hardener commits the real
  evidence in that shape.

## Declared invariant (BL-654)

The ticket's one declared invariant ("every mutant Stryker reports... is
killed, recorded as an accepted equivalent, grandfathered, or owned
elsewhere; none is silent") quantifies over a mutation-testing disposition
process and the hardener's per-survivor table, not over states of a pure,
testable module a generator can traverse — there is no input domain to draw
from. It admits no executable property-test encoding; per the Invariants
section of the coder's own role, this is the stated reason recorded in lieu
of a property test. The invariant's actual discharge is the hardener's
per-survivor disposition table at
`backlog/evidence/BL-1577-telegramClient-mutation.md` (this ticket's own
"How" section and QA e2e steps 3-4), not a test this stage can author.

By coder.
