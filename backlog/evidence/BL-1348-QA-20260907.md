# BL-1348 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Merged documenter tip `08a012536e` (2nd pass, scenario 03 added, Specification.MD
conflict resolved by keeping the documenter's newer text). Checklist:

- `pgrep -fl 'node --test|stryker'`: none before/after.
- `npm run compile`: clean.
- `node -e resolveFreeCoresCeiling(20,12/17/19.5)` → `8 3 1`, matches ticket's ruled values.
- `vitestWorkerMemoryBudget.test.js`: 37/37.
- `bl1348VitestWorkerPoolHostSizingInvariants.property.test.js`: 4/4;
  `bl871PropertyLaneWorkerPoolCapInvariants.property.test.js`: 2/2.
- Acceptance `specs/features/BL-1348-fork-pool-sizes-to-the-real-host.feature`: 6/6 (was 3/3
  pre-addendum — confirms scenario 03 landed).
- Full compiled unit suite: 611/611 files, 10328/10328 tests, green.
- `npm run test:properties` full run: 5 files failed on first pass, all
  `bl1358MutantTimeCeilingInvariants`, `bl1272LandedSiblingInvariants`,
  `bl1343ReplayNeverDropsOwnPathInvariants` (20000ms timeouts under full
  parallel contention), `bl687EpicTileSurfaceUntouched` and
  `bl1358MutantTimeCeilingInvariants` (reachability-floor assertion misses).
  None touch BL-1348's own paths (`vitest-worker-memory-budget.ts`, the two
  vitest configs) or any land-step/replay file this parcel changed. Re-ran
  all 5 alone: 5/5 files, 11/11 tests pass — confirmed flaky-under-contention
  (the exact class BL-1348's own ticket text describes: "each victim passes
  alone; the set of victims differs every run"), not a regression this
  parcel introduced. Grepped `backlog/` for each file name: no existing
  ticket found; not minting one myself — flagging here as an
  observation rather than blocking, since re-run-alone proves it clean and
  this parcel touches none of the failing files' production code.
- Wiring: both `vitest.config.mjs` and `vitest.properties.config.mjs` call
  `resolveFreeCoresCeiling(os.cpus().length, os.loadavg()[1])` as
  `defaultCeiling` — confirmed by grep, invariant 1 (one composition point) holds.
- Upstream evidence (cleaner/architect/hardener/documenter, both passes)
  reviewed: all explicit NONE, hardener's mutation figures (Stryker 45/46 +
  9/9 new; gherkin 16/19 + prior 3 confirmed equivalents) reasoned through
  and accepted.

Approving. Landing per Article 1.8 / BL-247.
