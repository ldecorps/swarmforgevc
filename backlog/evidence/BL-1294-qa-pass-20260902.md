# BL-1294 — QA pass — 2026-09-02

## Commit verified
`f96b11f2de` (merge of documenter's `d6527bb783` into QA's worktree, after
fast-forwarding the worktree branch to `origin/main` c5a7409f73 first — the
worktree was 23 commits behind and a merge attempted before the
fast-forward tripped `check_merge_deletion.sh` on BL-1311/BL-545 paths
already reconciled further downstream; the fast-forward resolved that
cleanly with no deletions).

Ancestry check: `git merge-base --is-ancestor <commit> f96b11f2de` — OK for
both the coder merge (`bfcc3b1e6f`) and the hardener merge (`ebfbb42bf4`) —
not merely ancestors of HEAD, ancestors of the commit being approved.

No prior QA bounce found for BL-1294 on `main`/`origin/main`/`backlog/evidence/`
(one architect bounce D1 — leaked scratch dirs — already fixed by coder and
re-verified by architect before this parcel reached QA).

## Sibling check (BL-532)
`qa-sibling-check.js status --ticket BL-1294` → `VERIFY BL-1294`, exit 0. No
open deferral.

## Verified against `qa_e2e_procedure`

1. **Original incident closed.** With `swarmforge/scripts/unregistered_test_gate_lib.bb`
   (BL-1240) present in this tree (its closure crosses into `test/`):
   - `npx vitest run --config vitest.properties.config.mjs test/telegramFrontDeskBotCli.property.test.js` → 3/3 green, including BL-1203 invariant 1.
   - `npx vitest run test/telegramFrontDeskBotCli.test.js` → 271/271 green.
2. **Fixture completeness / loud failure proven directly** by the new
   `extension/test/bl1294FixtureClosurePathAndFailureInvariants.property.test.js`
   (2/2 green, 30 runs each, generator reaches nested paths ≥8/30 both
   properties, non-vacuity documented in-file against the parent commit's
   `continue` and against a basename-only resolver). P1 = loud failure
   naming the unresolved dependency; P2 = nested path survives the copy,
   never flattened to the root.
3. Same property file covers scenario 3 of the qa_e2e_procedure (loud
   failure on a genuinely-absent dependency) — `assert.throws` against a
   regex naming the missing path.
4. **No regression, full baseline diff performed** (not merely spot-checked):
   spun up a disposable worktree at the pre-merge tip (`origin/main`
   `c5a7409f73`), ran both `npm test` and `npm run test:properties` there,
   and diffed the exact failing-test-name sets against the same runs on
   this parcel's merged tip.
   - `npm test`: 15 failed files / 25-26 failed tests both sides (one test
     in `telegramCursorOperatorExec.test.js` swaps between two flaky
     assertions run-to-run under worker-parallel scheduling — reproduced on
     baseline too across two consecutive full-suite runs; confirmed
     non-deterministic, not a regression, and already corroborated as a
     known `/tmp` fixture-race flake for this exact file in
     `backlog/evidence/QA-standing-red-corroboration-20260828.md`, BL-1039
     lineage). Every other failing file/test name is byte-identical between
     baseline and this parcel's tip.
   - `npm run test:properties`: 24 failed files / 13 failed tests, **identical
     failing-test-name set** on both sides (`diff` empty). All failures are
     the known `deps.checkOrphanedAuthoredDocs is not a function` collection
     class, tracked open by BL-1221/BL-1229 (grepped, not re-reported per
     BL-1063). `telegramFrontDeskBotCli.property.test.js` is NOT in either
     failing set. This parcel's own new property file adds 2 passing tests
     and 1 passing file, otherwise the two runs match exactly.
5. `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1294-fixture-script-closure-preserves-dependency-paths.feature` → 4/4 green.

## required_wiring
`grep bl1294 specs/pipeline/steps/index.js` →
`require('./bl1294FixtureScriptClosurePreservesDependencyPathsSteps')`
present (line 902). Registered, not orphaned.

## Wiring into real callers
`copyLiveScriptClosureInto` / `pinnedRepoFixture` helpers are consumed by
11 live test files (`bl1038PinnedFixture.property.test.js`,
`telegramFrontDeskBotCli.{test,property.test}.js`,
`commitIntegrityRunner.test.js`, `liveRepoDerivationGuard.test.js`, and
others) — this is test infrastructure, so "real caller" means the fixture
consumers it already serves, confirmed via grep, not merely unit-tested in
isolation.

## Documenter's diagram-currency / doc-currency check
Confirmed via documenter's own evidence
(`backlog/evidence/BL-1294-documenter-pass-20260902.md`): reference doc
`docs/reference/BL-1038-pinned-repo-fixture-and-live-derivation-guard.md`
updated to describe the path-preserving, fail-loud behaviour; no registered
diagram's change-trigger fires for a test-fixture-only mechanism.

## Process hygiene
`pgrep -fl 'node --test|stryker|vitest'` checked clean before and after
every run in this pass; the disposable comparison worktree and its
`node_modules` were removed (`git worktree remove --force`) after use.

## Verdict: PASS

## Handoff
- `note` broadcast (priority `00`) to coder, cleaner, architect, hardender,
  documenter: merge worktree branch up to QA's approved commit `f96b11f2de`.
- Land `f96b11f2de` on `origin/main` (BL-1144 discipline).
- `git_handoff`/`note` to coordinator: BL-1294 approved, commit `f96b11f2de`,
  bookkeep (move to `backlog/done/`, promote next paused item, route).

By QA.
