# BL-1361 — architect review, 2026-09-04

Reviewed coder commit merged in via cleaner `d99182426f`.

## Checks run

- Dependency gate on `bl1361SweepTellsSurfacedRolesSteps.js`: PASSED.
- Co-change: expected hub coupling within the post-QA-branch-sweep
  family; no new smell.
- `required_wiring`: `post-qa-branch-sweep-tell!` wired into `handoffd.bb`
  and the `--post-qa-sweep-once` CLI flag both present, confirmed by
  reading the code (not just grep) — the sweep's `:tell!` adapter is the
  real send path (`swarm_handoff.bb`), not a second mailbox writer.
- `check_feature_handler_registration.sh`: exit 0.

## Re-verified independently, not trusted from evidence

- `post_qa_branch_sweep_lib_test_runner.bb` — ALL PASS.
- `test_bl1361_sweep_tells_roles.sh` (real daemon, `--post-qa-sweep-once`,
  real role worktrees) — 4 clean runs this pass, on top of the coder's
  own reported 6/6 — 10/10 combined.
- `run_acceptance.sh` on the BL-1361 feature — 6/6.

## Correctness read

- `wake-for-reason?` correctly implements the human ruling exactly
  (`= :dirty-worktree`, nothing else wakes) — an unknown/future reason
  fails closed to "tell, don't wake", matching the ticket's own framing
  of waking as the exception.
- `surface-notice` stays within the 80-char note cap with a truncation
  fallback, correctly avoiding the exact silent-`.dead`-quarantine failure
  mode this ticket exists to end.
- `sweep!`'s `tell!` call is wrapped in its own `try/catch` inside the
  `reduce`, so a throwing adapter cannot break the loop — confirmed by
  reading the code directly: the catch produces a `{:success false}`
  result that only logs `post-qa-branch-sweep-tell-failed`, and the
  `reduce` continues to the next role regardless (invariant 3).
  Dedupe (invariant 2) is structural: `sweep-one-role` only returns an
  `action` for a genuinely NEW surfacing, so a repeat tick with the same
  state produces no action and therefore no send.

## One dormant note, consistent with today's session-wide pattern

This script's `fixture_isolation_begin` call does not thread `"$@"`
through (unlike the retrofitted argv-fix I just verified in
`fixture_isolation.sh` moments ago, in BL-1392's merge-up). Dormant and
harmless here too — this script takes no CLI arguments of its own — but
worth a documenter/specifier note so the fix lands uniformly across all
fixture_isolation_begin callers rather than piecemeal per ticket.

## Verdict

COMPLIANT. Forwarding to hardener.
