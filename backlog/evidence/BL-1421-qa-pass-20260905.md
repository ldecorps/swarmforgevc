# QA pass — BL-1421 — 2026-09-05

Redriven per coordinator note: the original documenter→QA parcel (commit
`64d3b3fbe8`, task `BL-1421-one-standing-surfacing-per-role`) was
blind-completed (dequeued 11:05:15.769Z, completed 11:05:15.954Z — 0.19s,
BL-1422's own failure mode) without review. `64d3b3fbe8` was already an
ancestor of my HEAD (picked up incidentally through an origin/main rematch
merge) but the ticket stayed `status: todo` in `backlog/active/` — never
actually reviewed or closed. This pass does the real review.

## Verification

- `bb swarmforge/scripts/test/post_qa_branch_sweep_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/bl1421_one_standing_surfacing_property_runner.bb`:
  ALL PROPERTIES HOLD (500 runs/property).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1421-one-standing-surfacing-per-role.feature` — 6/6
  scenarios pass, including "replaying 103 landed commits against a role
  that never merges tells it exactly once" (the exact 2026-09-05 103-commit
  storm this ticket fixes).
- Regression: `bash swarmforge/scripts/test/test_bl1361_sweep_tells_roles.sh`
  — ALL PASS (9/9); BL-1361's contract (invariant 3) holds unchanged, its
  feature untouched.
- `required_wiring` confirmed: `handoffd.bb` defines `caught-up-to-told?`
  (line 4079) and wires it into `post-qa-branch-sweep-role-facts!`'s
  adapter (line 4139, `:caught-up-to-told? caught-up-to-told?`).
  `specs/pipeline/steps/bl1421OneStandingSurfacingSteps.js` exports
  `registerSteps`.
- Ancestry: `git merge-base --is-ancestor ae8734974f HEAD` (coder's commit)
  confirms OK; `64d3b3fbe8` already an ancestor of `origin/main`.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED. The ticket's code is already on `origin/main` (picked up via an
earlier rematch merge before this review ran) — no further push needed for
the code itself. This evidence file lands normally; coordinator bookkeeps
BL-1421 to `backlog/done/`.
