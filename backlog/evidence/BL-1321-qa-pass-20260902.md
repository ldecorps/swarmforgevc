# BL-1321 QA pass — 2026-09-02

Merged documenter `ab281e7199` into QA worktree. Review-only BL-848
stamp-off for already-landed hotfix `3d70c0f4ec` (chase rotate yields
when the preferred role is already seated). No production source
touched by this parcel — only the acceptance step handler, its Babashka
CLI shim, docs, and evidence files.

## Process hygiene

`pgrep -fl 'node --test|stryker'`: no strays in my own worktree.

## Independent re-run

- `bb swarmforge/scripts/test/mono_router_lib_test_runner.bb` — ok.
- `bash swarmforge/scripts/test/test_handoffd_rule_proposal_rotate_wiring.sh`
  — ALL PASS (A/B/C), the BL-795 redirect regression stays green.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1321-swarm-stamp-seated-preferred-yield-3d70c0f4ec.feature`
  — 9/9 scenarios pass, matching qa_e2e_procedure items 1-6 plus invariant 2
  and the marker-vs-live-identity direction (item's own scenario 03/07).

## Full unit suite (`npm test`, extension/)

Same 15 pre-existing standing-red test files as this session's prior QA
passes, zero overlap — BL-1321 adds no `extension/` file at all. Count
unchanged at 9844/9869.

## required_wiring — re-verified live

- `swarmforge/scripts/handoffd.bb:1518` calls
  `mono-router-lib/chase-rotate-decision` from the live `chase-rotate-to!`
  daemon path (comment at :1504 confirms "unit-tested; IO stays here").
- `bl1321SeatedPreferredYieldStampSteps` registered at
  `specs/pipeline/steps/index.js:909`.

## Ledger — untouched, invariant 2 held

`backlog/hotfix-ledger.yaml` row for `3d70c0f4ec`: `state: stamp-open`,
`human_decision: null` — this parcel wrote nothing to it.

## Item 5 — line-ending normalisation, confirmed and reported not undone

`file swarmforge/scripts/mono_router_lib.bb
swarmforge/scripts/test/mono_router_lib_test_runner.bb`: both report
plain UTF-8 text with no CRLF marker — matches the commit's own claimed
CRLF→LF normalisation, confirmed still in place (not reverted). Per
constraints, not acted on further here (no `.gitattributes` added) —
that choice is the human's call per `ruling_options`.

## Item 3 — duplicated sixth assert, confirmed present and unremoved

Scenario 08 in the feature file matches the ticket's own finding that two
of the six new asserts in `mono_router_lib_test_runner.bb` are
byte-identical; left as landed, per constraints (not this parcel's to
remove a landed assert).

## Constraints honoured

Hotfix `3d70c0f4ec` not reimplemented, rewritten, or redesigned. No file
re-line-ended back to CRLF; no other file's line endings touched by this
parcel. The duplicated assert not removed.

## Verdict

PASS. Landing on `main`; `Hotfix-Certification` stays `pending` until the
human records the ledger decision separately.
