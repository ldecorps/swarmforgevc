# BL-829 — QA pass — 2026-08-17

## Scope reviewed

Parcel received from documenter at `d8c6cd4969` (fast-forward merge into QA,
no new merge commit — HEAD is `d8c6cd496`). Full chain, this ticket's own
commits:

- coder `31ab50538` — pager renders the bundle's remote pages beside native Talk
- cleaner `fa5e71941` — dropped unused `getLetsTalkUiBundleManifestJsonBody`, covered JSON.parse failure path
- architect `fef5077e6` — clean review, added bridge-side pages property test
- hardener `a1af55b80` — split CRAP-flagged `hasValidManifestFields`, covered `RemotePageHost.resolveUrl`
- documenter `d8c6cd496` — how-to doc, docs/index.md link, Specification.MD changelog

`git merge-base --is-ancestor a1af55b80 d8c6cd496` confirmed — this ticket's
own hardener commit is an ancestor of the commit being approved, not merely
"HEAD contains other tickets' work."

## Fresh verification run this pass

- `npm run compile`: clean.
- `npm test` (full extension unit suite): 433/437 files, 7728/7733 tests
  passed on first run. 5 failures — 4 are the well-known real-`fs.watch`
  timeouts (`activateBounceWatcher.test.js`, `bounceDrain.test.js`,
  `bounceWatcher.test.js`), each documented in their own header comment as
  genuinely OS-async and unfakeable; 1 is `renderBriefingDiagramsCli.test.js`
  (load-induced). Confirmed zero overlap with this parcel's diff via
  `git diff --stat 7120d9524 HEAD` (BL-829 touches only
  `android/app/src/main/java/com/swarmforge/floatcompanion/*`,
  `extension/src/bridge/letsTalkRoutes.ts`,
  `extension/src/bridge/letsTalkUiBundle.ts`, their tests, and docs — no
  overlap with any of the 4 failing files). Reran all 4 in isolation:
  `renderBriefingDiagramsCli.test.js` passed cleanly (1/1 file, all tests
  green); the 3 `fs.watch` tests still timed out on isolated rerun,
  consistent with the same host-load-independent constraint recorded on
  BL-825's own QA pass this session.
- `npm run test:properties`: 97/97 files, 304/304 tests passed. 3
  "unhandled error" RPC timeouts (`[vitest-worker]: Timeout calling
  onTaskUpdate`) are worker-IPC noise from host load (`uptime` showed
  ~8.8 on a 4-core box), not test failures — every test file still reported
  passed.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-829-bubble-remote-page-pager.feature`: 8/8 scenarios
  pass. Scenarios 1-2 drive the real bridge server; scenarios 3-8 each
  drive a real `./gradlew :app:testDebugUnitTest` invocation (48.6s + 4.5s +
  3.1s + 2.5s + 2.7s + 2.3s — real execution, not a passthrough). The
  step-handler's `KNOWN_VALUES` map is an identity map from Gherkin decision
  text to Kotlin test name, verified genuine (not a passthrough, BL-233): the
  literal strings appear as `PagerListResolverTest.kt` backtick-quoted test
  names (`grep -n 'fun \`' PagerListResolverTest.kt` — all 6 present
  verbatim), and the check requires the matching JUnit result to exist AND
  have passed.
- `required_wiring` — re-verified fresh by direct source read:
  - `MainActivity.kt::RemotePageHost` — not instantiated in MainActivity
    itself (pairing-only); confirmed the real chain:
    `MainActivity.startBubbleService()` → `OverlayService` → (bubble
    expand) → `TalkPanelActivity` → `TalkPagerAdapter` — `grep -n
    RemotePageHost TalkPagerAdapter.kt` shows `val host =
    RemotePageHost(binding.remoteWebView, binding.remoteFailureReason)` at
    line 34, a real instantiation site, not merely referenced in a comment.
  - `letsTalkRoutes.ts::pages` — no separate pages route (documented
    sibling-module pattern, same as BL-825's own manifest route); confirmed
    served via `bridgeServer.ts:74` (`import { getLetsTalkUiBundleManifest,
    isLetsTalkUiBundlePath } from './letsTalkUiBundle'`) and wired into the
    JSON route table at `bridgeServer.ts:1800-1801`
    (`matches: isLetsTalkUiBundlePath, compute: () =>
    getLetsTalkUiBundleManifest(...)`); `letsTalkUiBundle.ts:25` confirms the
    manifest type carries `pages: LetsTalkUiBundlePage[]`.
- Invariant coverage — all three of the ticket's stated invariants have
  dedicated Kotlin/TS property tests, not merely example-based unit tests:
  - Talk always reachable → `PagerListResolverTalkAlwaysFirstPropertyTest.kt`
  - Only manifest-named pages load → `PagerListResolverPageAllowlistPropertyTest.kt`
    + `letsTalkUiBundlePagesWholeOrNothing.property.test.js` (bridge-side
    whole-or-nothing parse, fuzzed)
  - A failing page states why → `PagerListResolverBareReasonPropertyTest.kt`
    + `RemotePageHostResolveUrlTest.kt`
- Process hygiene: `pgrep -fl 'node --test|stryker|vitest'` clean before and
  after this pass; no orphaned processes left running. (Pre-existing Gradle/
  Kotlin daemons observed are Gradle's own long-lived daemon management, not
  a leaked test-runner process, and are outside this check's scope per the
  same posture as BL-825's own QA pass.)

## Device-surface manual procedure — NOT independently completed by QA

The ticket's `verification:` block specifies a 5-step manual procedure on a
paired Android phone (fresh-bundle render + swipe reachability, remote page
render with Talk still reachable, malformed-page-path stated reason,
bridge-down cached/stale pages, no-cache bare state). This QA session has no
physical device or ADB access (`which adb` → not found), consistent with the
Testability Boundary — Bubble article's device-surface carve-out and the
same constraint recorded on BL-825's own QA pass earlier this session
(`BL-825-QA-pass-20260817.md`) and BL-826's
(`BL-826-QA-pass-20260809.md`).

Asked the human operator directly this session (the pending async
`role_ask.bb` channel for QA was already occupied by a stale, unresolved
2026-08-07 BL-826 ping, so this was asked directly in-session rather than
queued): approved disposition is **"Approve, device check as fast-follow"**
— land now, human runs the 5-step procedure on their own schedule and
reports back. Same disposition as BL-825's own pass this session.

Weighing the available evidence supporting this:
- All three of the ticket's stated invariants are each covered by a
  dedicated, passing property test (Kotlin and/or TS), not merely
  example-based unit tests.
- `required_wiring` re-verification confirms `RemotePageHost` is not only
  correct in isolation but is genuinely reachable from the real
  bubble-expand → pager chain, and the `pages` list is genuinely served by
  the live bridge route — no bypass found in either direction.
- The architect's independent pass recorded "clean review", adding a
  bridge-side property test of its own; the hardener's pass closed the
  remaining CRAP/coverage gaps it found.
- BL-825's whole-or-nothing manifest-rejection posture is reused for the
  `pages` field rather than reinvented, reducing the chance of a novel
  failure mode this slice's own logic doesn't already cover.

This is code-level and property-level evidence that the pager's page
resolution and allowlisting behave as specified, but it is **not** the same
as a device confirmation of the actual WebView render, pager swipe gesture,
and stale/bare degraded-state rendering on a real paired phone. Recording
this gap explicitly rather than silently treating it as satisfied.

## Verdict

APPROVE. All executable gates are green and independently re-verified this
pass; both `required_wiring` items are real, not merely unit-tested; all
three invariants have dedicated passing property coverage. The
device-surface manual procedure is recorded as an **outstanding
fast-follow**, per explicit human direction this session — not silently
passed.

By QA.
