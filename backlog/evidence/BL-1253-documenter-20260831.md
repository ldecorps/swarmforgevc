# BL-1253 — documenter pass, 2026-08-31

Reviewed hardender commit `46135c8b38` (architect's `638f627e8d`, cleaner's,
coder's `94301a6c72` re-stamp confirming round 2's remedy was already on
`main`). Third pipeline round for this ticket; no production hotfix source
touched by any of this ticket's own commits — review-only stamp-off.

## What changed since the last doc pass

The existing `docs/reference/Specification.MD` entry for BL-1253 (dated
August 30) predates this ticket's absorption of retired BL-1260 (Article
5.3) and did not mention: invariant 3 (token exclusivity + return-on-
recovery, carried from BL-1260), Scenario 06 ("A recovered feeder gets the
token back"), or BL-1260's own outstanding 90-second stall-window question
— carried into this ticket's `approval_context` and still unanswered.

Confirmed live before writing anything: `grep -n
bl1253TokenOwnershipInvariants` finds the property test file present and
referenced in hardener's evidence (4/4 pass, invariant 3 asserted);
`grep -n "stamp-06" specs/features/BL-1253-...feature` finds Scenario 06
registered and green (8/8 acceptance per hardener); `grep -n
DEFAULT_FRONT_DESK_FEEDER_STALL_MS extension/src/tools/telegramCursorBridgeCore.ts`
confirms the constant at line 371, unchanged.

## Docs updated

- New `docs/how-to/BL-1253-swarm-stamp-dead-feeder-owns-getupdates.md`,
  following the established stamp-off how-to convention (BL-1136, BL-1254):
  landed behaviour, stamp-off posture, invariant 3, and the outstanding
  90-second question quoted verbatim (Article 5.3 — a consolidation must
  not drop a human sentence, and this question was never individually
  answered even in its own retired ticket).
- `docs/index.md`: linked the new how-to in the how-to list, same commit.
- `docs/reference/Specification.MD`: new top changelog entry superseding
  the stale August 30 BL-1253 entry (kept below as history, not deleted)
  — states the round-3 final content (invariant 3, scenario 06, the
  outstanding question) and points to the new how-to.

No other doc references BL-1253, BL-1260, or `2ec06b6ef1`'s mechanics
outside these two files (checked: `grep -rl "2ec06b6ef1\|BL-1260"
docs/` before this pass returned only `Specification.MD`).

No registered diagram depicts this — a review-only stamp-off of already-
landed extension-host bridge code touches none of the four diagrams'
change-triggers (no boundary, pipeline-topology, handoff-mechanism, or
front-desk-routing change; the front-desk *bot* itself is explicitly out
of scope per the ticket's own constraints).

## Verdict

Clean. Docs brought current with the round-3 state. Forwarding to QA.

By documenter.
