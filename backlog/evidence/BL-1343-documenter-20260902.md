# BL-1343 — documenter pass (20260902)

Received: hardener commit `c949064bac` (merged into
`swarmforge-documenter` at `2df0f9766a`).

## Scope reviewed

- Ticket description, invariants, `qa_e2e_procedure`, and every evidence
  file in the chain (coder, cleaner, architect repass, hardener pass) to
  confirm what user/operator-visible behavior changed: `own-paths` in
  `swarmforge/scripts/land_step_lib.bb` now refuses (`{:paths nil
  :warning "..."}`) instead of silently reporting a completed empty
  replay when every one of a landing ticket's own delivered paths gets
  excluded as attributed to an unlanded sibling, and the refusal warning
  now names every excluded path (not just the last one — the hardener's
  own gap closed this same pass).
- Existing documentation of this same mechanism: `docs/reference/
  Specification.MD`'s BL-1272/BL-1308/BL-1315 entries and
  `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-
  remedy.md`, which already document `own-paths` and `land_step_cli.bb`'s
  output shapes end to end — this is the doc surface BL-1343 changes.
- `docs/index.md` — no new doc file created, so no new index link
  required; the BL-1241 how-to is already linked there.
- Diagram registry (`docs/diagrams/*.mmd`, engineering-detailed's
  change-trigger list): none of the four registered diagrams depict the
  land step's internal attribution walk (architecture.mmd is host/webview/
  tmux/`.swarmforge/` layout; swarm-flow.mmd is pipeline topology;
  handoff-flow.mmd is the handoff file lifecycle; front-desk-flow.mmd is
  Telegram routing) — no diagram change-trigger fired.

## Findings

NONE. No documentation gap, no stale doc, no missing index link, no
diagram change-trigger. Doc work: one `Specification.MD` top entry
(commit `10c44a2619`) plus a matching addendum section in the BL-1241
how-to, committed separately with an untagged subject per
`task_scope_gate_lib.bb`'s cross-ticket-filename rule (the how-to's own
basename names BL-1241, not BL-1343, even though the edit is BL-1343's
own doc work) — commit `7743fe113d`.

By documenter.
