# BL-1683 — hardener review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

Probed the one asymmetry the coder's own evidence flags as untested by
construction: the property test and every bb fixture build the rename on
the SENDER side only (the real incident's own shape). Code inspection
shows `findings-for-merge` treats both sides symmetrically (the same
`findings-for-side` call with received/sender swapped, the same
rename-map lookup for each), so built a hand fixture with RECEIVED as the
renaming side instead (`git mv paused -> active` on `received`, an
independent disjoint edit on `sender`) and ran the real guard directly
against it, both clean (merge preserves both sides: no finding) and
dropped (hand-built merge strips sender's own uncontested line at the new
path: one finding, `path: backlog/active/...`, `side: "sender"`, not
excused) - confirmed correct in the untested direction too, exactly as
code symmetry predicted. Not a defect; recorded as verification, not a
gap closed.

Also reviewed (not owned by this parcel): architect's separate note-filed
finding on `mk_fixture`'s `prove_root` call running one line too late in
`test_bl1378_expedite_close_guard.sh` (BL-1516's own first-pass commit,
already landed on main) -
`backlog/evidence/defect-bl1378-mk_fixture-prove_root-ordering-architect-20260921.md`.
Confirmed it names no ticket yet and is outside this parcel's own diff
(this parcel does not touch that file); left for the specifier's
adjudication per architect's own note, not re-filed here.

- `bb merge_drop_guard_lib_test_runner.bb`: ALL PASS.
- `bb bl1576_merge_drop_guard_property_runner.bb`: 2000 runs, ALL HOLD.
- `bb bl1610_merge_drop_gate_scan_bound_property_runner.bb`: ALL HOLD.
- `node specs/pipeline/cli.js` on BL-1683's own feature: 2/2.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1683MergeDropGuardMapsRenamesInvariants.property.test.js`: 1/1.
- All 21 standing whole-tree guard files (210 tests) green.
- `npm run compile`: clean.
- No leftover processes or fixtures.
