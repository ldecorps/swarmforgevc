# BL-1547 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1547`: `VERIFY BL-1547` (no open deferral).
- Straggler check before and after: no orphaned `node --test`/`stryker`/`vitest` processes belonging to this worktree (a live `stryker` run belongs to the `hardender` worktree, confirmed by full command line).
- `required_wiring`: `bl1547ClosedTicketFileIsNotForeignSteps.js::registerSteps` present and exported.
- Wired verification: `task_scope_gate_lib.bb`'s `foreign-scope-findings` takes the `closed-ticket-ids` 4th argument and its one impure caller, `findings-for-git-handoff`, computes it via `landed_ticket_lib.bb`'s `ticket-closed?` (confirmed by grep at both call sites, not merely covered by isolated tests).
- `bb swarmforge/scripts/test/task_scope_gate_lib_test_runner.bb`: `ALL PASS`.
- `bb swarmforge/scripts/test/bl1547_closed_ticket_not_foreign_property_runner.bb`: `ALL PASS`.
- `bb swarmforge/scripts/test/task_scope_gate_acceptance_exemption_property_runner.bb` (BL-1276's own, unmodified): `ALL PASS`.
- Acceptance: `BL-1547-*.feature` 3/3 scenarios pass. Sibling features re-verified green on this commit: BL-1192 (9/9), BL-1276 (8/8), BL-1295 (3/3).
- Interim retirement: `grep -rn "Until BL-1547 lands"` in `swarmforge/roles/documenter.prompt` finds nothing — the documenter's own commit `80f9ae95f3` retired the interim sentence in this same parcel, as the ticket's out_of_scope note required.
- Full unit suite (`npm test`): 623/624 test files green; the one failure (`bl1277UnscopedStepCollisionGuard.test.js`) is the pre-registered, owned standing red (BL-1607, `backlog/standing-reds.tsv`), unrelated to this ticket's diff.
- Property suite (`npm run test:properties`): 412/412 test files, 1214/1214 tests green; 4 `Unhandled Error` entries, all the allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871).
- Standing-red register: unchanged by this ticket (5 rows, `"unowned":[]`) — BL-1547 fixes a foreign-scope gap, not a registered red.
- The architect's earlier bounce (misfiled cleaner evidence path) was fixed and correctly re-forwarded; no unfixed defect remains in the ticket's bounce history.
