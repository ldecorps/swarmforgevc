# BL-1614 — QA unowned-red hold, 2026-09-17

Parcel commit: d5f41ac50d (QA branch, merges main 8d4e2273f0 + documenter
4580248b93). Full `npm run test:properties` (from `extension/`) failed one
file on an assertion, not a timeout:

```
FAIL  test/bl1071RecoveryBoundedInTime.property.test.js > BL-1071/BL-654 invariant 2: a recovery that never returns is bounded, reported unfinished, and leaves nothing behind
AssertionError: a grandchild survived the kill (plain): 2 stray hangs before, 0 after

'0' !== '2'


Expected: "2"
Received: "0"

 ❯ test/bl1071RecoveryBoundedInTime.property.test.js:123:12
```

Grep of `backlog/` for `bl1071`/`BL-1071`/`grandchild survived` finds no
open ticket naming this file as a current standing red;
`register_join` from `qa-gather.js` reports it `absent` from
`backlog/standing-reds.tsv`.

Solo re-run, same commit, same host:
`npx vitest run --config vitest.properties.config.mjs
test/bl1071RecoveryBoundedInTime.property.test.js` → 2/2 green in 8.47s.

At the time of the full-lane run, `.worktrees/coder2` was running many
concurrent vitest workers on the same host (confirmed via `pgrep -fl
vitest` + `/proc/<pid>/cwd`). The test counts live "stray hang" child
processes system-wide after a kill; under heavy concurrent host load from
an unrelated lane this counter can read stale/wrong, matching the known
class in BL-1107 ("property lane verdict turns on host load, not code")
though for a different file/assertion than BL-1107 fixed.

This is presumed a genuinely new observation (grep is empty) rather than a
duplicate of BL-1107's file/scenario, so it is reported as an unowned red
per Article 4.2/BL-1509, not silently treated as flaky.

Per Article 4.2 (standing-red-register-amendment-2026-09-05), BL-1614 is
held pending the specifier minting an owner for this red. QA holds the
BL-1614 parcel via `qa_hold_cli.bb` and completes the inbound task without
approving/landing.

By QA.
