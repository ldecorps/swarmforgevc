# BL-1287 documenter review pass — 2026-09-05

Reviewed the hardened commit (`dad7b07626`) and the coder/cleaner/architect/
hardener trail for BL-1287 (a fixture sweep must not signal a live run's
fixtures).

Checklist:
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1287FixtureSweepSparesLiveRunSteps.js`.
- Read `extension/test/helpers/fixtureTunnelName.js`: confirmed invariant
  1 (`leakedFixtureTunnelPids` now checks `creatingPidFor` against
  `isProcessAlive`, skipping any fixture whose creator is still alive —
  never a start-time cutoff), invariant 2 (`isProcessAlive` distinguishes
  a genuine live process from an unreaped zombie via `ps -o stat=`, not a
  bare `process.kill(pid, 0)`, per the architect's own bounce finding —
  so a killed creator's fixtures are still swept regardless of the
  zombie window), and invariant 3 (the temp-path check is unchanged —
  selection is still never by tunnel name).
- Per the ticket's own explicit "Documenter note" (the change is in
  scope for THIS parcel, not a follow-up): fixed the stale sentence in
  `docs/reference/Specification.MD`'s BL-1061 entry ("selecting by the
  fixture's own throwaway temp path rather than by tunnel name") which
  stopped being the whole truth once selection gained the creator-liveness
  question, and added a new dated top-of-file entry summarizing BL-1287's
  fix in the same changelog convention the rest of the document already
  uses (newest "Last Updated" entry, older ones pushed to "Prior entry —").
- Checked `docs/how-to/named-tunnel-bubble-musicalsifu.md` (cross-linked
  from the BL-1061 Specification.MD entry): confirmed it describes only
  the PRODUCTION shell-side reap (`tunnel_ownership_lib.sh`), never the
  JS test-lane's own `leakedFixtureTunnelPids` sweep BL-1287 touches — no
  mention of it anywhere in that doc — so it does not go stale and needs
  no edit.
- No dedicated how-to exists for the JS fixture-sweep helper itself
  (grepped `docs/how-to/` for `leakedFixtureTunnelPids`/`fixtureTunnelName`,
  no hits) — the Specification.MD changelog entry is this mechanism's
  only documented home, consistent with how BL-1061 itself was recorded
  there.
- Bumped the top-of-file "Last Updated" date/entry in the same commit as
  the content change, per the Documentation Order's requirement.
- No registered diagram depicts test-lane fixture-sweep internals — no
  diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond the Specification.MD update.

Result: NONE — no defect found; documented per the ticket's own explicit
documenter-note direction.
