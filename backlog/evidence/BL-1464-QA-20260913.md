
## Detail

- Ancestry check: this ticket's own coder (7f989110b5), cleaner (1168f7ca4e),
  architect (50864bb172), hardener (badab4dd60) merges are all ancestors of
  the approved commit (verified via `git merge-base --is-ancestor`).
- `run_acceptance.sh` on this ticket's feature: 4/4 pass (scenario 04, the
  register-row removal, now green — documenter's row-removal commit
  ea5a263ee6 is in range).
- `run_acceptance.sh` on BL-1297's feature: 6/6 pass (was 5/6 before this fix).
- `run_acceptance.sh` on BL-1343's and BL-1389's features: 6/6 and 5/5,
  unchanged verdicts, as required.
- `backlog/standing-reds.tsv`: no row for BL-1297's feature file (grep
  confirms absence).
- `land_step_lib_test_runner.bb`, `task_scope_gate_lib_test_runner.bb`:
  ALL PASS.
- Property suites `bl1389UnlandedSiblingPathNeverRidesInvariants` (3) and
  `bl1343ReplayNeverDropsOwnPathInvariants` (2): 5/5 pass, run via
  `vitest run --config vitest.properties.config.mjs` (BL-124: this suite no
  longer runs under bare `node --test`).
- Full unit suite (`npm test` under extension/): 616 test files / 10469
  tests, all pass. The run's own suite-duration/per-file budget guard
  (BL-378/BL-445) separately exits 1 on six pre-existing slow files
  (bl968StepRegistryMaterializedTreeGuard, bl1277UnscopedStepCollisionGuard,
  briefingDigestLineCli, renderBriefingBurndownCli,
  renderBriefingDiagramsCli, telegramFrontDeskBotCli) — a wall-clock budget
  check, not a test assertion failure; none of these files are touched by
  this ticket (fixture-only change per constraint, no production lines),
  and no standing-reds.tsv row or open ticket exists for this budget
  overage. Out of scope for BL-1464; noted here for visibility, not gating.
- No orphaned `node --test`/`stryker` processes before or after (`pgrep -fl`
  clean).

By QA.
