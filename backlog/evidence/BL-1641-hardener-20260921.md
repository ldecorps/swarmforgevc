# BL-1641 — hardener review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

Ran a scoped Stryker dry run on `night-closing-ceremony-run.ts` (via a
temporary `vitest.bl1641.stryker.config.mjs` pointer, reverted after — same
pattern as BL-1640's earlier pass, routing around the unrelated full-suite
red in `bl1652HandoffdRespawnReadingsWiring.test.js`, now owned by BL-1673).

### `mainHasBriefing`'s own lines — all survivors are equivalent

Six survived/no-coverage mutants land inside `mainHasBriefing` itself
(`out/tools/night-closing-ceremony-run.js:357,359,364`):

- Line 357: the `'-e'` flag in `['cat-file', '-e', ...]` mutated to `''`.
- Line 359: `stdio: 'pipe'` mutated to `stdio: ''`.
- Line 364 (x4): every clause of `err && typeof err === 'object' &&
  'stderr' in err` mutated (ConditionalExpression/LogicalOperator variants),
  plus a StringLiteral mutant on the `''` fallback.

Verified equivalent by DIRECT PROBE, not by shape or resemblance to an
already-accepted case (BL-1081's own rule):

- **Line 357/359 (`-e` and `stdio`)**: ran the real `git cat-file` command
  by hand with the mutated arguments against a real fixture repo, for both
  the genuine-absence and genuine-presence case. Absence: `git cat-file -e
  main:<path>` and `git cat-file "" main:<path>` print the BYTE-IDENTICAL
  stderr (`fatal: path '<path>' does not exist in 'main'`) - git's absence
  message does not depend on the `-e`/`''` distinction here. Presence:
  correct code exits 0 (returns `true` directly, no catch); the mutant
  exits 128 with `fatal: invalid object type ''` (a DIFFERENT stderr, but
  one the absence-regex still does not match, so the catch branch's
  fail-closed default ALSO returns `true`) - same boolean outcome via a
  different code path. Every one of `mainHasBriefing`'s four test cases
  (no-ref, no-repo, absent, present) produces the identical boolean under
  both the real code and this mutant - not reachable through the
  function's own black-box contract.
- **Line 364 (`err && typeof err === 'object' && 'stderr' in err`)**:
  probed three real execFileSync failure shapes (a genuine git error with
  stderr text, an ENOENT from a missing `git` binary, an ENOENT from a
  nonexistent `cwd`) - all three satisfy `'stderr' in err` as `true`
  (Node's execFileSync always attaches a `stderr` property to its thrown
  error, even when its VALUE is `undefined` for an ENOENT). No reachable
  failure inside `mainHasBriefing`'s own try block (`briefingRelPath`
  cannot throw for any string-coercible `dayKey`; every thrown error
  originates from execFileSync) can make this guard's else-branch fire.
  Equivalent on the same grounds BL-1081 documents for a safe-primitive
  guard: no test could ever differentiate the mutant from the original
  through this function's own inputs.

Not chased further per the BL-234/BL-1081 exception - each equivalence is
demonstrated from the code and confirmed by direct execution, not assumed
from resemblance to a prior accepted case.

### Pre-existing survivors elsewhere in the file — not this parcel's

The scoped run also reported the SAME line-664 `advanced` field survivors
BL-1640's own hardening pass already found and handed to the specifier as
first-run debt (owned by BL-1676) - unrelated to `mainHasBriefing`, not
reproduced here.
