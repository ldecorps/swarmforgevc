# BL-1550 — hardender review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- BL-149 cooldown gate on `extension/src/swarm/draftPathUnder.ts`:
  `skip-cooldown` (age 0.62d < 1d) — Stryker mutation deferred this pass per
  the gate; not a discharge, the file is too fresh to mutate yet.
- Property lane flakiness verification: `for i in 1..10` runs of
  `test/draftPathUnder.property.test.js` under
  `vitest.properties.config.mjs` — 10/10 green, no FAIL line (qa_e2e_procedure
  step 1).
- Acceptance: `run_acceptance.sh` on
  `specs/features/BL-1550-a-draft-cleanup-never-unlinks-a-directory.feature`
  — 5/5 pass.
- Break-then-restore (qa_e2e_procedure step 4): restored the pre-fix bare
  `existsSync`/`unlinkSync` body, recompiled, re-ran acceptance — scenario
  02's 3 directory/idempotency rows failed with the expected `EISDIR`
  message, confirming the tests genuinely discriminate; restored the fix,
  recompiled, re-ran acceptance — 5/5 green again, `git diff --stat` clean.
- CRAP gate: `removeDraftIfPresent` was 6.84 (71% statement coverage) —
  the two non-ENOENT rethrow branches (lstat, unlink) and the race-window
  ENOENT-from-unlink swallow had no unit coverage. Added 3 unit tests to
  `extension/test/draftPathUnder.test.js` using `vi.spyOn(fs, ...)` to
  drive each branch directly (rethrow-on-lstat-EACCES,
  swallow-ENOENT-on-unlink-race, rethrow-on-unlink-EACCES). Re-measured:
  100% coverage, CRAP 6.00 (at threshold, not flagged). 11/11 unit tests
  pass.
- DRY: `npm run dry` — 76 pre-existing clones repo-wide, none touching
  `draftPathUnder.ts` or its tests.
- Whole-tree standing guards (touched `extension/test/`): all 18
  `test/*Guard*.test.js` files (183 tests) green.
- mkTmpDir convention: confirmed no raw `fs.mkdtempSync` in the touched
  test/step files — all use `mkTmpDir`/`mkProcessTmpDir`.
- No orphaned `node --test`/`stryker` processes before or after this pass.
- Lessons: none novel — no new standing rule proposed this pass.

By hardender.
