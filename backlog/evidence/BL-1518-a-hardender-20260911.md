# BL-1518-a — hardender review pass, 2026-09-11

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: hand-authored mutation sweep over handoff_draft_root_guard_lib.bb (dropped path-separator boundary, inverted negation, flipped equality branch, swapped starts-with? args, swapped draft/root labels in refusal-message)
- **Commit hash**: 9c163bdcb9
- **First error excerpt**: label-swap mutant survived unit+property runners (both paths still substring-matched); other mutants killed
- **Failure class**: mutation-gap
- **Expected vs observed**: every hand-authored mutant of the new pure lib is killed by its unit or property runner
- **Blamed role**: hardender
- **Remediation pointer**: anchor draft/root label assertions to their own "draft: "/"root:  " prefix in handoff_draft_root_guard_lib_test_runner.bb; verified kill + restore-green

By hardender.

## Detail

- **Scope**: `swarmforge/scripts/handoff_draft_root_guard_lib.bb` (new pure
  lib, coder-authored). `swarm_handoff.bb` was skip-cooldown'd
  (`mutation_cooldown_gate.bb`: file age 0.87d < 1d cooldown, quiet host) —
  not mutation-tested this pass. No `extension/src` file changed by this
  ticket (coder left `enqueueRoleAnswerNote` unchanged per the ticket's
  "How" direction), so no Stryker/CRAP run applies.
- **Host**: load average 6.5–7.5 on 20 cores throughout (quiet); no leftover
  test/mutation processes before or after (`pgrep -fl 'node --test|stryker|vitest'`
  clean both times).
- **Babashka has no wired mutation tool** (Engineering Rules, Startup
  Tools) — hand-authored sweep per BL-638 fallback, on top of the coder's
  own non-vacuity proof (backlog/evidence/BL-1518-a-coder-20260911.md):
  dropped path-separator boundary (M1, killed both), inverted `(not (or …))`
  negation (M2, killed both), flipped the exact-match `=`→`not=` branch
  (M4, killed both), swapped `str/starts-with?` argument order (M5, killed
  both), and the D1 label-swap in `refusal-message` (survived until fixed).
- **Re-ran clean** after the fix: `handoff_draft_root_guard_lib_test_runner.bb`
  ALL PASS; `bl1518_handoff_draft_root_guard_property_runner.bb` ALL PASS
  (400 runs each, coverage {:inside-exact 63 :inside-flat 65 :inside-nested 77
  :outside-parent 73 :outside-prefix 62 :outside-sibling 60}); `suite_inventory_cli.bb`
  ok - 512 files, 508 standing; acceptance feature 3/3 pass; `extension`
  compiled clean, `telegramFrontDeskBotCli.test.js` 272/272 pass; all 18
  whole-tree `*Guard*.test.js` files (BL-631/BL-945 rule, this parcel
  touches `specs/pipeline/steps/`) 183/183 pass.
- **CRAP/DRY**: not applicable — no `src/*.ts` file was added or changed.
- **Property lane**: `test:properties` not touched by this parcel (no
  `.property.test.js` file added or edited); the coder's `.bb` property
  runner above is this ticket's property-style coverage for the new lib
  and lives in its own runner, not folded into the vitest property lane.
