# BL-1767 — coder pass, 2026-09-26

## What was wrong (confirmed by running each surface before touching anything)

`swarmforge/scripts/model_steward_lib.bb`'s `safety-critical-competencies`
gained a third member (`coder-stop_banner_compliance`, hotfix 7ba57573ff,
2026-09-23). Four fixture sites had hand-copied the prior two-member set
into planted "passing" compliance-battery scorecards and never picked up
the addition:

- `swarmforge/scripts/test/test_model_steward_cli.sh` case 05: FAIL
  (`certify refused: safety competencies absent from scorecard:
  coder-stop_banner_compliance`), 05d never reached.
- `specs/features/BL-547-...feature`: 13/15 (scenarios 14, 15 failed the
  same way).
- `specs/features/BL-556-...feature`: 5/6 (scenario 5 left status at
  `candidate`, no error surfaced to the test — see below).
- `specs/features/BL-1079-...feature`: 3/5 (scenario 4 [2] same "absent
  from scorecard" refusal; scenario 1 failed for the older, separate
  reason: BL-669 legitimately added a second anthropic seed identity,
  claude-opus-4-8, and the step asserted the seed carries "exactly one").

## What was built

- **`specs/pipeline/steps/lib/modelStewardSafetyCard.js`** (new, shared by
  all three JS step-handler files): `readSafetyCompetencies()` shells to
  `bb -e` and reads `model_steward_lib.bb`'s real, sorted
  `safety-critical-competencies` set (memoized per process — one subprocess
  spawn per acceptance run, not per scenario); `passingSafetyEntries(names)`
  is a pure function building `{competency, status: "pass"}` for each name.
  This is the mechanism that makes "adding a competency to that set leaves
  all four files green" durably true: nothing hand-copies the set again.
- `bl547ModelStewardSteps.js`: both planted "passed all certification
  gates" cards (scenarios 14/15's shared setup) now spread
  `passingSafetyEntries(readSafetyCompetencies())` instead of two literal
  entries.
- `bl1079CursorStewardCertifySteps.js`:
  - `plantScorecard` (scenario 03/04's card) same swap.
  - "no anthropic identity was added or altered by the seed change"
    (scenario 01): the seed now legitimately carries two anthropic
    identities (BL-669). Removed the frozen `length === 1` count; the
    step's other two checks already carry the real intent directly —
    `claude-sonnet-5` unchanged (deepEqual, pre-existing) and no anthropic
    identity name matches `/cursor/i` (pre-existing, kept). Replaced
    `seedAnthropic[0]` (an order assumption) with a `.find()` lookup by
    model name for the status assertion.
- `bl556EvaluateIngestSteps.js`: two independent fixes, both required —
  discovered the second by testing, not by re-reading the ticket text
  alone:
  1. Added the safety entries to the "captured recruiter scorecard"
     fixture (the ticket's own named card, ~line 70) per direction. This
     alone did **not** fix scenario 5: re-ran after this change alone,
     still `candidate` not `certified`.
  2. Traced `model_steward_cli.bb`'s `registry-after-evaluate`: `evaluate`'s
     clean-gates auto-certify does **not** read the ingested
     `--scorecard` artifact for its safety check — its own comment says
     so explicitly ("the recruiter scorecard it just ingested carries role
     gates, not the compliance-battery safety probe, so read the
     well-known battery scorecard here") and reads
     `scorecards/<provider>__<model>.json` via
     `model-steward-store/read-scorecard!`, completely separately. Nothing
     in this file had ever planted that file. Added it in the
     `"winner-model" is currently certified with a prior passing gate"`
     step (the one Given that needs the auto-certify path to actually
     fire), scoped to that one scenario, not globally — no other scenario
     checks certified status.
  - Checked the "with safety entries... the scenarios that read capability
    dimensions still hold" concern the ticket raised: scenarios 1, 2, 3,
    4, 6 (which read `caps.coding_quality`/`protocol_compliance` etc. off
    the SAME recruiter card) were already green with the safety entries
    added and stayed green — the capability-dimension mapping ignores
    competency names it doesn't recognize.
- `swarmforge/scripts/test/test_model_steward_cli.sh`: added
  `SAFETY_COMPETENCIES` (bash-3.2-safe `while read` array build, no
  `mapfile`), `safety_entries_json` (builds the full pass set, or one
  named competency overridden to a given status/reason — used by 05c to
  keep testing "one probe failed" cleanly rather than colliding with
  "one probe absent"), and `safety_competencies_joined` (05d's expected
  refusal text, built the same live way). Cases 05, 05c, 05d now read the
  set instead of hand-copying it.

## Scope discipline
Touched only the four Scope files plus the one new shared helper the
ticket explicitly permitted ("A shared test helper if the coder wants
one") and the evidence file. Never touched `model_steward_lib.bb`,
`model_steward_cli.bb`, `model_steward_store.bb`, the seed file, or any
`.feature` file (verified: `git diff main...HEAD --name-only` on this
parcel's own commit names only the five files above).

## Invariants (1, this ticket)
"Every planted passing-certification scorecard in the four files carries
every member of model_steward_lib's safety-critical-competencies as pass,
read from the lib when the test runs..." — split disposition, same shape
BL-1698 used for its own two invariants:

- **Encoded** (coder-authored property test,
  `extension/test/bl1767ModelStewardSafetyCardEntries.property.test.js`):
  the durable, generically-testable half is the pure transform every JS
  site now shares, `passingSafetyEntries`. Property: for any generated set
  of distinct competency names (0-20, fast-check, 200 runs), the built
  entries carry exactly those names, none dropped or added, every status
  `"pass"` — including after growing the set by one, directly modeling
  "adding a competency... leaves it green". Non-vacuous, verified this
  session with three independent breaks, each restored byte-for-byte
  after confirming RED: (a) function returns `[]` unconditionally, (b)
  function drops the last name (`.slice(0, -1)`), (c) function hardcodes
  `status: "fail"`. All three failed as expected; restored; ALL PROPERTIES
  HOLD (200/200).
- **Not encoded as a property, stated reason**: the bash script's mirror
  of the same algorithm, and the "wiring" half (that all four sites
  actually call/mirror the live read rather than a stale literal) — this
  quantifies over shell-script text and real `bb -e`/CLI subprocess
  dispatch across a language boundary, not a pure module a generator can
  drive (same disposition class BL-1698's invariant 2 used). Covered
  instead by the real, live runs below, which exercise the actual lib,
  actual CLI, and actual fixtures together — exactly what "read from the
  lib when the test runs" asks for, and drift here fails those runs
  directly and loudly.

## Checks run
- `bash swarmforge/scripts/test/test_model_steward_cli.sh`: 05, 05c, 05d
  now PASS; ALL PASS (13 cases).
- `node specs/pipeline/cli.js specs/features/BL-547-model-steward-infrastructure-agent.feature`:
  15/15 (was 13/15).
- `node specs/pipeline/cli.js specs/features/BL-556-model-steward-slice2-evaluate-ingestion.feature`:
  6/6 (was 5/6).
- `node specs/pipeline/cli.js specs/features/BL-1079-a-cursor-identity-can-be-steward-certified.feature`:
  5/5 (was 3/5).
- `bb swarmforge/scripts/test/model_steward_test_runner.bb`: ALL PASS.
- `npx vitest run --config vitest.properties.config.mjs test/bl1767ModelStewardSafetyCardEntries.property.test.js`:
  1/1 (200/200 property runs), plus the three non-vacuity breaks above.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js` (new/edited step
  handlers touched, none add a raw `fs.mkdtempSync`): 4/4.
- `npm test` (from `extension/`): 638 files / 10875 tests, all green. One
  run hit an unrelated `briefingDigestLineCli.test.js` 20s timeout under
  full-suite contention (a file this parcel never touches, already flagged
  by the suite's own budget monitor as one of several "registered pole"
  files that run slower in-suite than alone); re-ran that file alone
  (12/12 in 18.7s) and the whole suite again (638/638, 10875/10875) to
  confirm it was transient contention, not a regression.

No architectural change, no new dependency, no secrets touched, no
`.feature` scenario text changed.

## Continuation note (coder, after coder@2 seat retirement)

coder@2 was retired mid-task (operator directive, throughput); the
coordinator committed its staged work as a WIP snapshot (915f7617a7) and
routed a note to pick it up here. Merged clean, no conflicts. Re-ran
every check above after the merge (main had advanced further in the
meantime): `test_model_steward_cli.sh` ALL PASS (13 cases, 05/05c/05d
green); BL-547 15/15; BL-556 6/6; BL-1079 5/5; `model_steward_test_runner.bb`
ALL PASS; the property test 1/1 (200/200 runs); `stepHandlerTmpRootGuard.test.js`
4/4. Confirmed `model_steward_lib.bb`/`model_steward_cli.bb`/
`model_steward_store.bb`/the seed file/every `.feature` file remain
untouched by this ticket's own commits (the CLI's one diff against main
is BL-1700's unrelated `probe` verb dispatch, already on this branch).
Nothing further needed; forwarding as-is.

By coder.
