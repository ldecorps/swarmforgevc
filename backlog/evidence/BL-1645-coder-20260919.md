# BL-1645 coder evidence

## What was built

- `swarmforge/scripts/forward_evidence_lib.bb`: adds `inbound-window-start`
  — the shared reader returning an inbound's `created_at`, else
  `enqueued_at`, else `dequeued_at`, else the epoch. One function, three
  callers.
- `swarmforge/scripts/done_with_current_task.bb`: both `work-note-gate!`'s
  `since` (the Work-note gate, BL-1422/BL-1614) and `forward-gate!`'s
  `since` (the forward gate, BL-1609) now read
  `(forward-evidence-lib/inbound-window-start source-file)` instead of a
  bare `dequeued_at` lookup.
- `swarmforge/scripts/done_with_current_batch.bb`: `forward-verdict`'s
  `since` reads the same shared function.
- `specs/pipeline/steps/bl1645EvidenceWindowOpensAtCreationSteps.js` (new):
  the feature's 5 scenarios (6 subtests — outline scenario 03 has 2
  examples), driving the real `done_with_current.sh` against a two-role
  (coder, architect) fixture git repo with explicit `created_at`/
  `enqueued_at`/`dequeued_at` headers and controlled commit dates
  (`GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE`).
- `swarmforge/scripts/test/forward_evidence_lib_test_runner.bb`: gains 4
  header-precedence cases for `inbound-window-start` (created_at wins;
  falls back to enqueued_at; falls back to dequeued_at; falls back to the
  epoch) — a small, deliberate IO exception in an otherwise-pure runner,
  matching the file's own existing exception for
  `sent-handoff-names-ticket-since?`.
- Narratives re-tensed per the ticket's own direction (scenario rows
  unchanged): `specs/features/BL-1422-a-work-note-is-not-completed-without-work.feature`
  and `specs/features/BL-1609-a-forwarding-parcel-is-not-completed-with-nothing-sent.feature`
  now say "since the note/inbound was queued" (created_at), not "since its
  dequeue".

## FIRM constraints held

- BL-1422's own contract stands: a Work note is never completed on
  evidence older than the dispatch itself (scenario 03's outline row 1) —
  `git-log-names-ticket-since?`/`sent-handoff-names-ticket-since?` are
  UNCHANGED, only the `since` bound moved earlier (to created_at); a
  re-dispatch note is itself created after the first pass's own work, so
  the same guarantee holds under the new window.
- BL-1614's clause stands: a stated reason on a ticket active on main with
  no evidence is still refused (scenario 04) — `work-note-completion-decision`
  is untouched (out of scope, confirmed unedited).
- BL-1609's contract stands: a forward that predates the inbound's own
  existence never completes it (scenario 03's outline row 2).
- None of the four decision tables' clauses changed — only their `since`
  input's source.

## Declared invariants — no property-test framework for this shape

Both declared invariants (one shared reader; the window opens at
created_at) are covered by construction (one function, three call sites -
scenario 05's static wiring check proves exactly two call sites in
`done_with_current_task.bb` and one in `done_with_current_batch.bb`, and
proves the reader's own precedence order in source) and by the acceptance
feature's 6 real, end-to-end scenarios. `inbound-window-start` is
Babashka, which per `engineering.prompt` has no mutation/CRAP/DRY lane
wired; its 4 header-precedence cases in `forward_evidence_lib_test_runner.bb`
are the deterministic-input-space equivalent this project's own precedent
(`test_finish_shift_lib.sh`'s scenario 02, `test_babysitterd_census_lib.sh`
this session) already establishes for untooled languages.

## Property lane (npm run test:properties) — not this parcel's

This parcel adds no property-test file (Babashka, no lane per the
section above). Ran it once anyway per the forward checklist: crashed
with `FATAL ERROR: ... JavaScript heap out of memory` after
`test/bl1297MergeOwnPathsInvariants.property.test.js` — the same host-side
heap-ceiling crash class documented in `backlog/evidence/BL-1636-coder-20260918.md`
and `backlog/evidence/BL-1639-coder-20260919.md`, unrelated to this
parcel's diff. Not re-attempted per the 2026-09-17 "run each lane once"
directive.

## Testability boundary

Nothing here touches VS Code, a webview, or a live tmux/PTY — every
verification is a real git fixture under `mkProcessTmpDir`
(BL-1385/BL-1390: never the live checkout).

## Verification

- `bash swarmforge/scripts/test/forward_evidence_lib_test_runner.bb`:
  ALL PASS (8 pure decision cases unchanged + 4 new header-precedence
  cases).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1645-*.feature`:
  6/6 scenarios green.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1422-*.feature`:
  6/6 green (no regression from the narrative reword).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1609-*.feature`:
  8/8 green (no regression from the narrative reword).
- `bb swarmforge/scripts/test/bl1614_work_note_active_on_main_property_runner.bb`:
  ALL PROPERTIES HOLD (untouched decision table, unaffected).
- `bb swarmforge/scripts/test/bl1609_forward_gate_property_runner.bb`:
  ALL PROPERTIES HOLD (untouched decision table, unaffected).
- `bash swarmforge/scripts/test/test_done_with_current_arg_rejection.sh`:
  ALL PASS (no regression).
- `bash swarmforge/scripts/test/test_done_with_current_work_note_evidence.sh`:
  ALL PASS (no regression — this suite's own fixture always evidences or
  fails well after its far-past DEQUEUED_AT constant, so moving the
  window earlier to created_at changes nothing observable for it).

## Files touched

- `swarmforge/scripts/forward_evidence_lib.bb`
- `swarmforge/scripts/done_with_current_task.bb`
- `swarmforge/scripts/done_with_current_batch.bb`
- `swarmforge/scripts/test/forward_evidence_lib_test_runner.bb`
- `specs/pipeline/steps/bl1645EvidenceWindowOpensAtCreationSteps.js` (new)
- `specs/features/BL-1422-a-work-note-is-not-completed-without-work.feature`
  (narrative only)
- `specs/features/BL-1609-a-forwarding-parcel-is-not-completed-with-nothing-sent.feature`
  (narrative only)
- this evidence file

## Out of scope (per ticket)

`swarmforge/handoff-protocol.md` and `docs/diagrams/handoff-flow.mmd` are
the documenter's own stage of this same ticket, not touched here. No
change to any of the four decision tables' clauses.
