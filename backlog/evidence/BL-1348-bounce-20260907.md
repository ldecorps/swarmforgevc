# BL-1348 — QA bounce, 2026-09-07

## D1: the ticket's own spec-gap instruction (verbatim scenario + handler
direction, already fully resolved) was never implemented — no pipeline
stage caught it

**Failing command**: N/A — this is a missing-deliverable finding, not a
failing test. Checked via `grep -n "Scenario\|Examples" specs/features/BL-1348-fork-pool-sizes-to-the-real-host.feature`
and reading the full file.

**Commit tested**: `9dd64ab8ba` (documenter's tip, merged into this QA
worktree).

**First error excerpt** (the ticket's own notes, verbatim, dated
2026-09-07 by the specifier):
```
2026-09-07 (specifier), spec-gap on the documenter's note: ruling B was
recorded on 2026-09-07 (123ae42f9c) without the feature amendment this
ticket's approval_context promised [...] The parcel is past the coder
(documenter, 16:01Z), so per BL-1385 the text lives HERE and enters the
feature INSIDE the parcel: documenter bounces to coder (class spec-gap),
the coder adds the scenario and its step handler in the same commit, and
the chain re-walks.
```
followed by the complete, ready-to-use Scenario Outline text and Examples
table (cores=20/load=12→pool=8, load=17→pool=3, load=19.5→pool=1), plus
explicit handler direction: "a step that injects cores and the 5-minute
load into the composition point (or the exported free-cores ceiling
function) — never os.cpus()/os.loadavg() read live in a test."

**Failure class**: `spec-gap` (the ticket's own prescribed classification
for this exact instance — a late-arriving, but fully resolved, spec
amendment a stage failed to incorporate; not an Article 4.4 ambiguous-spec
case, since nothing about it is untestable or unclear).

**Expected vs observed**: Expected — `specs/features/BL-1348-fork-pool-sizes-to-the-real-host.feature`
contains the third Scenario Outline ("with no override the default
ceiling is the cores the host has free") with its three Examples rows,
backed by a step handler in `bl1348ForkPoolSizesToTheRealHostSteps.js`.
Observed — the feature file still has only its original two scenarios
(pool-follows-host-and-override-01, no-override-still-bounded-02); no
step handler for the third scenario exists anywhere in this parcel's
diff.

## Why this is not waived by the coder's own stated rationale

The coder's evidence (`backlog/evidence/BL-1348-coder-pass-20260907.md`,
"Why the acceptance feature needed no change") argues the scenario
"cannot be pinned into a deterministic Gherkin Example without either
faking `os.loadavg()` ... or measuring a real, unstable host signal." This
is the exact concern the specifier's own note already resolved: the
handler direction explicitly says to inject `cores`/`load` values
directly into `resolveFreeCoresCeiling` (the same pure function the unit
and property tests already call with fixed inputs), never to read real
`os.loadavg()` inside the acceptance run. The coder's evidence does not
engage with this specific direction at all — it reasons only from the
OLDER, superseded 2026-09-06 note ("the feature gains one Examples row...
once you rule", before the specifier's 2026-09-07 note replaced "a row"
with "its own scenario" and supplied the handler seam).

Independently confirmed the underlying function is correct against the
specifier's exact example values (so this is purely a missing acceptance
scenario, not a functional defect):
```
$ node -e 'const {resolveFreeCoresCeiling}=require("./out/tools/vitest-worker-memory-budget");
console.log(resolveFreeCoresCeiling(20,12), resolveFreeCoresCeiling(20,17), resolveFreeCoresCeiling(20,19.5))'
8 3 1
```

## Everything else on this ticket's checklist is clean (Article 4.4 complete inventory)

- `npm run compile`: clean.
- `vitestWorkerMemoryBudget.test.js` 37/37; `bl1348VitestWorkerPoolHostSizingInvariants.property.test.js`
  4/4; `bl871PropertyLaneWorkerPoolCapInvariants.property.test.js` 2/2.
- Full compiled unit suite: 611/611 files, 10328/10328 tests, fully green.
- Acceptance (existing 2 scenarios): 3/3 pass.
- Live-verified the fix directly: with `SWARMFORGE_VITEST_MAX_FORKS=12`,
  the resolved pool is 12 (was clamped to 7 at the parent commit — the
  original defect); with no override, the pool follows free cores as
  ruling B specifies. Both `vitest.config.mjs` and
  `vitest.properties.config.mjs` correctly call
  `resolveFreeCoresCeiling(os.cpus().length, os.loadavg()[1])` as their
  `defaultCeiling` — invariant 1 (one composition point) holds.
  Full-forge+darwin ceiling of 1 unchanged (invariant 3) — architect's own
  evidence traced this structurally and I did not need to re-derive it.
- Hardener's mutation pass (Stryker 45/46, 1 confirmed equivalent; BL-113
  gherkin 7/10, 3 confirmed equivalent) reviewed and the equivalence
  arguments hold up on inspection — no issue there.
- The hardener independently noticed a RELATED but distinct absence (the
  full-forge/linux Examples ROW the OLDER 2026-09-06 note promised) and
  correctly reasoned it is not a functional gap, flagging it for
  documenter/specifier rather than blocking. That observation and this
  bounce's D1 are not the same finding: the hardener was looking at the
  superseded ask; this bounce is about the NEWER, more specific,
  already-fully-resolved 2026-09-07 spec-gap instruction, which is a
  discrete new Scenario Outline with its own step handler, not a row on
  the existing outline.

## Remediation (direction already fully specified by the ticket's own notes)

Add the verbatim Scenario Outline and Examples table from the ticket's
2026-09-07 note to `specs/features/BL-1348-fork-pool-sizes-to-the-real-host.feature`,
plus a step handler that injects `cores`/`loadAvg5min` directly into
`resolveFreeCoresCeiling` (never a live `os.loadavg()`/`os.cpus()` read in
the test), following the exact handler direction already given. No
production code change is needed — `resolveFreeCoresCeiling` is already
correct and already covered at the unit/property level; this is
acceptance-level traceability for the human-ruled behavior only.

Blamed role: **coder** (the specifier's instruction named the coder as
the one to add the scenario and handler in the same commit; this was the
coder's own current pass, and no stage after it can retroactively supply
the missing step handler without becoming the coder in all but name).

By QA.
