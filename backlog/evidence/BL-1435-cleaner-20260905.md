# BL-1435 — cleaner pass, 2026-09-05

NONE — no defect found. The guard-widening is correct, complete, and
verified independently against the real code paths (not merely the
coder's claimed output). One evidence-accuracy discrepancy noted
(non-blocking).

## What was checked

- Read the `REV_PARSE_TOPLEVEL_SRC` regex diff directly and traced it by
  hand against the two real call sites (`docsStructureRealTree.test.js`,
  `bl1300HeadroomProofIsPinned.test.js`): both use the identical shape
  `execFileSync('git', ['-C', __dirname, 'rev-parse',
  '--show-toplevel'], { encoding: 'utf8' })`, which the pattern's three
  lookaheads (`__dirname`, `rev-parse`, `--show-toplevel`, each scanning
  up to the nearest `)`) correctly find within the array before matching
  the array/options-object literal. Folded into both
  `LIVE_ROOT_BINDING_RE` (named form) and `LIVE_ROOT_INLINE_SRC` (inline
  form) as one alternation each, so `growthPatternsFor`/`EXEMPTION_RE`
  apply identically regardless of which idiom bound the root — matches
  invariant 1 by construction, not merely by claim.
- Verified the core non-vacuity claim independently, not by re-running
  the coder's own script: called `violationFor('test/docsStructureRealTree
  .test.js', text)` directly from a throwaway script — with the real
  `BL-1038-EXEMPT:` comment present, returns `null`; with it stripped,
  returns a real violation object naming the `computeDocsStructure`
  production-escape reason. This is the exact red/green pair
  `qa_e2e_procedure` step 2 asks for, reproduced from scratch.
- Verified the widened binding regex genuinely matches the rev-parse
  idiom (not merely that the exemption machinery works) by feeding a
  synthetic string pairing the real binding line with a real growth
  surface (`readdirSync(REPO_ROOT)`) directly into `liveRepoDerivation`:
  correctly flags it. Confirms the widening is load-bearing, independent
  of the coder's own mutation-restore proof.
- Verified all five named files' dispositions directly (not merely
  reading the coder's table): `bl1300HeadroomProofIsPinned.test.js` and
  `gitEnvGuard.test.js` both correctly resolve to no violation (the
  former genuinely binds the rev-parse root but hands it to no growth
  pattern; the latter never binds `__dirname` at all — its rev-parse
  calls target `mkTmpDir` fixtures);
  `activePoolFreshnessAudit.test.js` resolves via the PRE-EXISTING
  `path.join` idiom (already exempted, unrelated to this widening) rather
  than a rev-parse binding at all — matches the coder's own table exactly.
- Confirmed `findLiveRepoDerivations('extension/test')` returns `[]` at
  this commit, called directly (invariant 2).
- `npm run compile` clean (no TypeScript touched, matches the coder's own
  N/A note).
- `npx jscpd` on the touched/new files: 0 clones.
- Mutation-site-count tooling does not apply: `liveRepoDerivationGuard.js`
  is a plain JS test helper (252 lines), not TypeScript compiled to
  `extension/out/**/*.js`.

## Evidence-accuracy discrepancy (non-blocking)

The coder's evidence claims `extension/test/{docsStructureRealTree,
bl1300HeadroomProofIsPinned,gitEnvGuard,activePoolFreshnessAudit,
swarmMetricsCli}.test.js | 103/103 (unaffected)`. Re-running these five
files directly: 79/79 pass (5 + 4 + 5 + 25 + 40 = 79), all green, no
failure — the NUMBER in the evidence file is wrong, but every test
genuinely passes and nothing is broken. Noted here rather than silently
carried forward; not a functional defect and not grounds for a bounce
(Article 4.4 is about defects in the WORK, and the work itself is
correct and fully verified above by independent means, not merely by
trusting this one miscounted line).

## Verification (all re-run in this worktree, all green)

| check | result |
|---|---|
| `npx vitest run test/liveRepoDerivationGuard.test.js` | 24/24 pass (19 original + 5 new) |
| `npx vitest run test/{docsStructureRealTree,bl1300HeadroomProofIsPinned,gitEnvGuard,activePoolFreshnessAudit,swarmMetricsCli}.test.js` | 79/79 pass (evidence file claimed 103 — see discrepancy note above) |
| `findLiveRepoDerivations('extension/test')` (called directly) | `[]` |
| `violationFor` red/green pair on `docsStructureRealTree.test.js` (reproduced independently) | `null` with reason; real violation without |
| Synthetic rev-parse-binding + growth-surface probe (reproduced independently) | correctly flagged |
| acceptance `BL-1435-...feature` | 6/6 |
| acceptance `BL-1038-...feature` (regression) | 8/8, unaffected |
| acceptance `BL-1212-...feature` (regression) | 2/2, unaffected |
| `npm run compile` | clean |
| `npx jscpd` on touched/new files | 0 clones |

No fix required. Forwarding unchanged to architect.
