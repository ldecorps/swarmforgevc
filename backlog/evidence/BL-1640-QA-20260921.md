# BL-1640 — QA review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Batch parcel with BL-1641 (Article 2.6): BL-1641 has its own failing
ancestry-gate defect (see `backlog/evidence/BL-1641-qa-bounce-20260921.md`)
and is bounced separately; BL-1640 has no failing check of its own and is
forwarded on its own clean git_handoff.

- `bash swarmforge/scripts/test/test_finish_shift_lib.sh`: 18/18 PASS,
  including new case 15 (deadlines read the fixture's configured budgets
  even when the caller's CWD differs from `--target` - the coder's
  bounce-fix for QA's earlier BL-1640 bounce
  `backlog/evidence/BL-1640-bounce-20260921.md`) - fast (no ~3min
  real-wall-clock wait, per hardener's own D1 fix).
- `bash swarmforge/scripts/test/test_bl1640_sleep_runs_ceremony_to_done.sh`:
  ALL PASS (12/12), fast (~2s).
- `node specs/pipeline/cli.js specs/features/BL-1640-a-finish-shift-sleep-runs-the-closing-ceremony-to-done.feature`:
  6/6 ok.
- `npx vitest run --config vitest.properties.config.mjs test/bl1640SleepDeadlinesAndCeilingInvariants.property.test.js`:
  2/2 pass.
- Wiring: `bl1640SleepPathCeremonyRunsToDoneSteps.js` exports
  `registerSteps`, confirmed present.
- `npm test` (extension/, after `npm run compile`): 635/635 files,
  10836+/10836+ tests passed clean (one earlier run under heavy local
  contention from concurrent verification work in this same session
  showed spurious `[vitest-worker]` timeouts and 3 failed files; a clean
  re-run immediately after showed 0 failures - confirmed contention, not
  a real defect, and not reproducible standalone).
- `npm run test:properties` (extension/): 437/437 files, 1278/1278 tests
  passed; one known-benign BL-871 unhandled error, no failures.
- `pre_qa_gate.sh BL-1640 bca21ee5d3`: OK.
- No standing-red register/allowlist row names BL-1640.
- No straggler test/mutation processes before or after.
