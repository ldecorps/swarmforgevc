# BL-735 coder pass — 2026-08-25

## What shipped

- `extension/src/tools/pilotAcceptanceExecution.ts` — pure helpers for
  acceptance-execution tracking and revert-reland note assessment.
- `extension/src/tools/pilotAcceptanceGate.ts` — new refusal reasons
  `acceptance-not-executed` and `reland-notes-required`; records execution
  per landing attempt before yaml move.
- `extension/src/tools/pilot-acceptance-gate.ts` — wires `readTicketNotes`,
  `acceptanceReceiptExists`, and execution tracking into `buildDeps`.
- `specs/pipeline/steps/bl735PilotAcceptanceExecutionSteps.js` — acceptance
  handlers for all 5 BL-735 scenarios; registered in `index.js`.
- Updated BL-727/729/731/733 step `baseDeps` with execution-record hooks so
  existing pilot-gate acceptance features stay green.

## Verification

- `node --test extension/test/pilotAcceptanceGate.test.js` — 37/37 pass
- `npm run test:properties` slice:
  `bl735PilotAcceptanceExecution.property.test.js` — 5/5 pass
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-735-*.feature` — 5/5 pass
- `npm run compile` — green

## Invariants encoded

1. Declaration alone insufficient — `requireAcceptanceExecuted` after
   `runContract` records execution via `recordAcceptanceExecution`.
2. Revert-reland notes — `requireRelandNotes` when `isRevertRelandTicket`.
3. Refused land inert — unchanged BL-727 behavior; covered by existing
   property tests plus BL-735 scenario 05.
