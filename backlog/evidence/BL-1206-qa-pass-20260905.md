# QA pass — BL-1206 — 2026-09-05

Commit under review: `faae38fe91` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`5ed229309e` BL-1206
coder, confirmed ancestor via `git merge-base --is-ancestor`).

This is the root cause behind the spurious "No test suite found in file"
artifacts I hit twice myself this session running individual property
files ad hoc (BL-1221, BL-1229 evidence) — confirmed now genuinely fixed,
not merely worked around.

## Verification

- `grep -rl "require('node:test')" test/*.property.test.js`: 2 hits, both
  confirmed by direct read to be string literals inside guard-invariant
  fixture lists (`bl1206PropertyLaneAllowlistInvariants.property.test.js`,
  `nodeTestImportGuard.property.test.js`), never a real import — the 13
  original + 1 added (`bl1200FixtureGitWritesStayInOwnRepo`) genuine
  imports are gone.
- Ran all 14 named files under the correct full-suite invocation
  (`npx vitest run --config vitest.properties.config.mjs`, no per-file
  filter — a per-file filter on these produces the same spurious "No test
  suite found" harness artifact I hit and diagnosed as such in my BL-1221
  evidence): all 14 collected and green, zero "No test suite found"
  errors.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1206-drain-the-node-test-import-entries-from-the-property-allowlist.feature`
  — 7/7 scenarios pass, including "a converted file that still fails on
  its own merits stays allowlisted, with the reason it actually failed"
  and "an allowlist entry red for an unrelated reason is left alone".
- `test/bl1206PropertyLaneNodeTestImportGuard.test.js` (new standing
  guard, BL-1220's twin for the property lane): 4/4 pass.
- **Verified the required_wiring canary directly**: `git show faae38fe91
  -- backlog/standing-reds.tsv` has no hit for `hostActivityFeed` — that
  row (a genuinely different, pre-existing collection failure with no
  test()/describe() calls at all, unrelated to the node:test-import
  class) is confirmed untouched, proving the ticket did not drain the
  allowlist wholesale.
- Allowlist row count: `property_suite_standing_allowlist.tsv` now holds
  exactly 1 data row (`hostActivityFeed.property.test.js`) — 13 of the 14
  targeted files passed and were dropped; `bl1200FixtureGitWritesStayInOwnRepo`
  (the file "added 2026-09-05: same node:test import, line 26; it had no
  owner" per the ticket) also passed and was dropped.
- Full property suite (`npm run test:properties`): exit 0. (Two
  "fatal: not a git repository" stderr lines are known, unrelated
  fixture-teardown noise from a different property test's own probe on
  an already-cleaned-up mkdtemp root — not a failure signal, suite still
  exits 0.)
- Full unit lane (`npm test`): same 6 pre-existing unrelated failures as
  my BL-1229 evidence (`backendSwitch`, `constitutionDocCitations`,
  `operatorRuntimeBbFixtureClosure`, `pricingTable`, `telegramClient`,
  `telegramCursorOperatorExec`) — no regression.
- `required_wiring`: `specs/pipeline/steps/bl1206DrainTheNodeTestImportEntriesFromThePropertyAllowlistSteps.js`
  exports `registerSteps`. Docs: `docs/index.md` links the new how-to.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — merging up and landing.
