# BL-1624 — hardener review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

### Leaked mkdtemp fixture root found and fixed in-pass (not a bounce)

Scenario 02's fixture builder (`a copy of the repository's shell test and
runner in a fixture root with no extension/out`,
`bl1624StandingShellTestNeverDiffsAgainstMainSteps.js`) creates
`ctx.bl1624Root` via `fs.mkdtempSync` and had NO cleanup anywhere in the
file - not a partial leak on a failure path, a permanent one on every
run. Confirmed: `ls -d /tmp/bl1624-no-build-*` found 4 directories
already leaked from the coder/cleaner/architect's own prior verification
runs, before I touched anything.

This is the third instance of this exact class this pass (after
`bl1632Bl1071ProbeCountsOnlyItsOwnFixturesHangsSteps.js` and
`bl1626PromotionFixturesCarryTheClosureSteps.js`, both earlier today) -
the acceptance runtime has no After-hook, so any assertion between
fixture creation and the file's own inline teardown (here: none existed
at all) leaks the resource.

Fixed in commit `24df270c83`: tracked via
`specs/pipeline/steps/lib/fixtureReaper.js`'s `onAbnormalExit` the moment
the directory is created, with an inline cleanup in the scenario's
terminal assertion step (`no runner assertion line is printed`) for the
happy path. Confirmed with a standalone probe script: the tracked
directory survives a throw that happens before any inline cleanup runs -
reaped via the process `'exit'` handler regardless.

All 3 acceptance scenarios re-run green after the fix; 0 leaked
`/tmp/bl1624-no-build-*` directories before or after (cleared the 4
pre-existing ones before re-testing). The real shell test
(`test_bl1388_land_step_guard_fixture.sh`, ALL PASS, 5 assertions), the
new census script (`test_standing_shell_tests_never_diff_main.sh`, ALL
PASS, population 520, no offender), and BL-1388's own feature (4/4,
confirming the handler's `'only-block'` claim removal did not regress
anything) all still green. Register row for the shell test left in
place per the BL-1631/BL-1626 pattern (QA's land step retires it).
