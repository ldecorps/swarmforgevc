# BL-1436 — coder pass, 2026-09-05

## What was wanted

`swarmforge/packs/full-forge.conf` runs the specifier seat on
`claude-fable-5-1` since 2026-09-04, but `PRICING_TABLE` had no entry for
it (only the distinct `claude-fable-5` row) - BL-627's coverage check has
been red since, `estimateCostUsd` returns null for every turn of that
seat, and the standing-red register's row for this test named the
now-closed BL-1212 (a mis-attribution: BL-1212 only cites this file as an
example of a well-phrased exemption reason).

## What landed

- **`extension/src/metrics/pricingTable.ts`**: a `'claude-fable-5-1'`
  entry - `inputPerMTok: 10`, `outputPerMTok: 50`, `cacheReadPerMTok: 0.25`
  (a Fable-5.1-specific cache-read rate, not the sibling row's 0.1x-input
  convention) - with a comment naming the source (the project's Claude API
  reference) and the date it was cached (2026-06-24). Cache-creation is
  NOT published in that reference and is deliberately left unset rather
  than derived from the `claude-fable-5` row (invariant 1; the ticket's
  own explicit direction). `Rates.cacheCreatePerMTok` is now optional
  (the ticket's own suggested shape, left for architect review) and
  `costFrom` was widened to the honest-null discipline this file already
  applies at the whole-model level: a category with zero tokens costs 0
  regardless of whether its rate is known (unchanged for every existing
  row - every field they carry is defined, so their own behavior is
  numerically identical); a category with NONZERO tokens and an unknown
  rate (only reachable via the new optional field) returns `null` for the
  whole estimate rather than silently costing it at zero or guessing.
- **`extension/src/swarm/modelDisplayName.ts`**: `'claude-fable-5-1':
  'Fable 5.1'`.
- **`backlog/standing-reds.tsv`**: the `pricingTable.test.js` / BL-1212
  row removed (invariant 3) - plain `grep -v` (not `-n -v`, which
  previously corrupted a TSV this session by prepending line numbers).
- **`specs/pipeline/steps/bl1436PricingTableCoversFable51Steps.js`**
  (new): drives the real compiled `pricingTable.js`, the real compiled
  `modelDisplayName.js`, the parcel's own `pricingTable.ts` source text
  (for the comment scenario), and `backlog/standing-reds.tsv`.
- **`extension/test/pricingTable.test.js`**: two new unit tests added
  after an architect bounce (see `BL-1436-coder-20260905-rework.md`)
  giving the honest-null cache-creation branch real, gated coverage -
  the original pass's own non-vacuity proof for this branch was only a
  manual probe.

## Declared invariants

1. **Every referenced model traces to a published source named beside
   it; no guess, no sibling copy.** The entry's own comment names the
   source and date (scenario 03); cache-creation, absent from that
   source, is left unset rather than copied from `claude-fable-5`'s
   `12.5` - demonstrated directly: `estimateCostUsd` with cache-creation
   tokens on `claude-fable-5-1` returns `null` (verified live against the
   compiled table, and now by a gated unit test after the bounce).
2. **The coverage check is the only notion of the roster - untouched.**
   `collectReferencedClaudeModels` and `checkPricingCoverage` are
   unmodified; this parcel only adds a table entry. Scenario 01 confirms
   the check's own roster read genuinely includes `claude-fable-5-1`
   (non-vacuity: the assertion would be trivially satisfiable over an
   empty or unrelated roster otherwise).
3. **A land that turns the test green removes its standing-red row in
   the same commit.** `backlog/standing-reds.tsv` no longer names
   `pricingTable.test.js` anywhere; scenario 04 re-added the row via
   mutation and confirmed the acceptance scenario catches it (see
   Non-vacuity).

## Non-vacuity (byte-identical restore confirmed for every file)

- **`extension/src/metrics/pricingTable.ts`** (rate mutation): changed
  the new entry's rates to `999`/`999`/`999` - scenario 02's three rows
  (input/output/cache-read) all failed with the wrong expected value.
  Restored; `diff /tmp/pricingTable.ts.bak
  extension/src/metrics/pricingTable.ts` empty; `npx vitest run
  test/pricingTable.test.js` back to green immediately after.
- **`extension/src/metrics/pricingTable.ts`** (honest-null mutation):
  changed `costFrom`'s `return null` (unknown rate, nonzero tokens) to
  `continue` (silently cost at 0) - after the architect bounce this is
  now caught by a GATED unit test (`test/pricingTable.test.js` fails,
  1/32), not only a manual probe. Restored; diff empty.
- **`backlog/standing-reds.tsv`**: re-appended the removed row -
  scenario 04 failed ("no register row names pricingTable.test.js").
  Restored; diff empty.

## Verification

| check | result |
|---|---|
| `npm run compile` (extension) | clean |
| `npx vitest run test/pricingTable.test.js` | 32/32 (was 29/30 before this ticket) |
| `npx vitest run --config vitest.properties.config.mjs test/pricingWindows.property.test.js` | 3/3, unaffected |
| acceptance `BL-1436-the-pricing-table-prices-every-model-the-swarm-runs.feature` | 6/6, stable across two runs |
| acceptance `BL-627-pricing-table-correctness-and-coverage-invariant.feature` | 6/6, unaffected |
| live: `estimateCostUsd(..., 'claude-fable-5-1')` per category | input 10, output 50, cache-read 0.25, cache-create null (qa_e2e_procedure step 3, exact) |
| `grep -c pricingTable backlog/standing-reds.tsv` | 0 |
| `bb swarmforge/scripts/standing_red_register_cli.bb .` | no row names pricingTable.test.js (the register's other unowned rows are a separate, pre-existing hardening-lane class, untouched by this parcel) |

## required_wiring

- `extension/src/metrics/pricingTable.ts::claude-fable-5-1` - present.
- `extension/src/swarm/modelDisplayName.ts::claude-fable-5-1` - present.
- `specs/pipeline/steps/bl1436PricingTableCoversFable51Steps.js::registerSteps` -
  present, discovered by directory scan (BL-1371), confirmed by the
  acceptance run passing 6/6.

## Out of scope (per the ticket's own text, not acted on here)

- Re-verifying the other rows' rates - untouched.
- Any change to the ledger, the briefing's cost maths, or the roster
  sources - `collectReferencedClaudeModels`/`checkPricingCoverage`
  untouched; the briefing/docs pricing-section refresh is explicitly the
  documenter's own pass per the ticket's "What changes".
- Bumping `PRICING_TABLE_VERSION` - no scenario, invariant, or
  required_wiring entry names it; left at 3.
