# BL-1425 documenter review pass — 2026-09-05

Reviewed the hardened commit (`24884df7f2`) and the coder/cleaner/architect/
hardener trail for BL-1425 (a queue-jump places the ticket past the depth
cap).

Checklist:
- `required_wiring` anchors present: `backlogWriter.ts::--queue-jump` (the
  mover passes the declared mode to the chokepoint CLI),
  `telegram-front-desk-bot.ts::queueJump` (the Q-jump tap and `/qjump`
  verb request the mode via `recordExpediteDecisionAndClose`), and
  `bridgeServer.ts::queueJump` (the paused-pager Expedite requests it
  too). Step handler exists:
  `specs/pipeline/steps/bl1425QueueJumpPastCapSteps.js`.
- Read `promotion_gates_lib.bb`/`promotion_gates_cli.bb`: confirmed
  invariant 1 (`--queue-jump` skips `depth-refusal` ONLY; `depends_on`,
  `hold`, `blocked`, `human_approval`, `acceptance`, and
  `slice_size_envelope` all still refuse exactly as for any promotion),
  invariant 2 (the mode is a parameter of the one `evaluate` chokepoint —
  `select` and every other CLI verb untouched by construction, no second
  TypeScript copy of the depth-cap rule), and invariant 3 (a crossing is
  reported as a structured advisory naming the active count and cap, and
  the ticket fills a real active slot — the coordinator's own next
  ordinary promotion still waits for the count to fall below the cap).
  Confirmed `promote_and_route_next.sh`/`route_backlog_to_coder.sh`/
  `handoffd.bb`/`chase_sweep_lib.bb` name no `--queue-jump`/`queue-jump?`
  (grepped, per the ticket's own scenario 03 structural check).
- Confirmed the BL-1083 retirement (BL-1006, retire-never-reword): the
  depth-cap Examples row is gone from
  `specs/features/BL-1083-every-promotion-path-goes-through-the-gate.feature`
  and the matching branches in `bl1083PromotionGateSteps.js` and
  `bl1083PromotionGateInvariants.property.test.js`; `depends_on`/hold
  coverage in that feature is unchanged, per `retires:` naming exactly
  those three paths.
- Per the ticket's own explicit direction ("Documenter:
  docs/reference/Specification.MD (the BL-721 and BL-1083 entries),
  docs/explanation/BL-900-epic-priority-promotion-ranking.md ..., the
  how-to for the Approvals buttons, and docs/diagrams/swarm-flow.mmd"):
  - Extended `docs/reference/Specification.MD`'s BL-1083 entry with the
    queue-jump depth-cap override (the doc's own home for the mechanism
    this amends) and added a forward pointer from the BL-721 entry to it.
  - Corrected `docs/explanation/BL-900-epic-priority-promotion-ranking.md`'s
    "queue-jump is a coordinator action" mischaracterization — it is the
    human's action; the coordinator's own promotion path stays capped and
    never declares the mode.
  - Updated `docs/how-to/BL-538-console-paused-ticket-pager.md`'s
    "Expedite a Ticket" section (the Approvals-buttons/pager how-to) —
    the stale claim "it does not bypass `depends_on`, hold, or the depth
    cap" is now split: `depends_on`/hold still refuse, the depth cap is
    the one exception, reported never silently.
  - Added the queue-jump edge to `docs/diagrams/swarm-flow.mmd`
    (`PAUSED -.-> ACTIVE`, dashed, labeled with the crossed-only-depth-cap
    scope) plus a changelog comment at the top, since Article 1.7's
    backlog-flow-changes trigger fires for this ticket (a human queue-jump
    now reaches `backlog/active/` by a path the diagram didn't show).
- No "Last Updated" freshness field in the how-to/explanation docs; the
  Specification.MD top-of-file date was already current (no separate
  bump needed beyond the content added under the existing entries, since
  this parcel's edits are additions to already-dated entries, not a new
  top-of-file changelog item on their own).
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found; documented per the ticket's own explicit
documenter-note direction across four files.
