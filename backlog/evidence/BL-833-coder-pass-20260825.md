# BL-833 coder pass — 2026-08-25

## Ticket
Host-agent activity feed — tee progress lines into a bounded per-session
feed the bridge serves (catch-up JSON + live SSE).

## Tip intent
- `hostActivityFeed.ts`: bounded buffer, quiet/active, subscribe, best-effort write
- `telegramCursorBridgeLive.recordHostActivity`: tees every progress line before Telegram throttle
- `bridgeServer`: `GET /host-activity` + `event: host-activity` on `/events`
- APS 8/8; unit + property green

## Verification
- `npm run compile` (extension)
- `node --test test/hostActivityFeed.test.js test/hostActivityFeed.property.test.js`
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-833-host-agent-activity-feed.feature`
