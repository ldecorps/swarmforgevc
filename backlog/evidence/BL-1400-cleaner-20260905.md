# BL-1400 — CLEANER PASS, 2026-09-05

NONE. No defect found; no cleanup change made.

## What was checked

- `extension/src/tools/check-feature-handler-registration.ts`: `readTree`
  now lists the steps directory with `readdirSync(..., { withFileTypes:
  true, recursive: true })` — confirmed at line 56, comment at line 82
  explicitly states the invariant ("a handler in a subdirectory is IN the
  tree"). The pre-existing `isDiscovered` predicate needed no change, per
  the ticket's own direction.
- Invariant 2 (no false offenders) verified both by test and by reading
  `collectUnregisteredHandlers`: still keyed on the feature's ticket, so a
  `lib/` helper reached by a discovered handler's `require` stays
  reachable, and one required by nothing is simply not a handler — never
  reported.
- `test_check_feature_handler_registration.sh`: 11/11 PASS, including new
  scenario 08 "a nested handler is refused by name, and a lib helper is
  not an offender" — both halves of invariant 1 and invariant 2 in one
  case.
- Real acceptance run:
  `specs/features/BL-1400-the-registration-guard-never-sees-a-nested-handler.feature`
  — 5/5 pass, including "the guard against the live tree reports no new
  offender" (qa_e2e step 4 — every nested file on `main` today really is a
  lib helper, confirmed live rather than assumed).
- `checkFeatureHandlerRegistrationCli.test.js`: 12/12 PASS (pre-existing
  suite, unaffected — confirms the recursive change didn't regress the
  suffix-based or missing-registry cases).
- `bl1400NestedHandlerIsSeen.property.test.js`: 2/2 PASS.
- `specs/pipeline/steps/bl1400NestedHandlerIsSeenSteps.js::registerSteps`
  present, matches BL-1371's `*Steps.js` discovery suffix (anchor 2).
  Anchor 1 (the `recursive: true` literal) confirmed live above.
- `tsc -p ./` compiles clean.
- `jscpd` over the two changed/new files: 0 clones.
- No prior bounce history for BL-1400 (mint → IR-DRY review → approve →
  promote → topic → coder, no adjudicate-again entries).
  `main`/`origin/main` in sync (`0 0`).
- New/changed files here are a small TS tree-walker change plus
  hand-authored JS test/step-handler files — `mutation-site-count.js`
  applies to the one compiled TS file (`check-feature-handler-registration.ts`);
  the diff itself is a one-arm change to an existing tree-listing branch,
  not new structure, so no split consideration arises.

No architecture concerns: a single-option change to an existing
`readdirSync` call, isolated to the tree-building step; the pure
classification logic downstream needed no change, exactly as the ticket's
own direction anticipated.

By cleaner.
