# BL-1715 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 9b9f0bc229 (task BL-1715), merged into QA as
0de249f8a1. The BL-1700 and BL-1711 ticket YAMLs conflicted only in
bounce_history; QA's side, which records both bounces, was kept.
a7c145648d restores the bounced BL-1700 and BL-1711 code paths to
origin/main a second time (the BL-1772 re-point had dropped the first
restore). `docs/index.md` drops only BL-1700's added line and keeps
BL-1715's edit to the BL-1697 line. QA then synced main (`Merge main
6c7b428b70 into QA.`, 8c583a369b); every gate below ran once on
8c583a369b. The diff against origin/main is exactly BL-1715's two commits
(bd0ef3a434, f0cb9e5cea), compared hunk-for-hunk per file across 9 files.
`qa-sibling-check status`: VERIFY. No earlier bounce is recorded.

The gates ran by hand, in qa-gather's own order and invocations, with each
lane's full log kept under tmp/ (the BL-1769 tail-excerpt gap):
- stragglers before and after: none
- register: `unowned: []`
- `pre_qa_gate.sh BL-1715 8c583a369b <QA root>` (required_wiring, the
  handler's registerSteps pin): OK
- `npm test`: 638 files, 10879 tests passed (46s)
- `npm run test:properties`: 469 files, 1355 tests passed (392s). The one
  unhandled error is BL-871's allowlisted `onTaskUpdate` timeout. The
  coder's `bl1715DriverSeatGiveUp.property.test.js` is inside the include.
- `run_acceptance.sh` on the BL-1715 feature: 5/5 (7s)

The ticket's qa_e2e_procedure, one run each:
1. The BL-1715 feature: 5 of 5 (the acceptance run above).
2. The BL-1697 feature 12/12 (50s) and the BL-1698 feature 9/9 (11s);
   `seat_affinity_lib_test_runner.bb`: all assertions passed;
   `seat_difficulty_lib_test_runner.bb`: ALL PASS.
3. Scope: the driver lib (the give-up branch), the claim path
   (`ready_for_next_task.bb` and `handoff_lib.bb`'s
   worked/given-up readers), the pinned handler, the coder's invariant
   property test and its fixture helper, and docs (the BL-1697 how-to,
   handoff-flow.mmd, index). The feature was already on main.

Invariants: invariant 1 and the tree-revert half of invariant 2 are
property-tested across generated rosters and commit chains. The
outcome-row half has a stated reason and is encoded by scenarios 01, 03
and 04 against the real driver.

Observation, not a defect of this parcel (sent to the specifier as a
note): `give-up!` reverts every commit since `pre-claim-head`, and
`start-ticket-parcel!` reads that head BEFORE its optional `Merge main`
and the `merge sender commit`. So the give-up reverts those merges with
`git revert -m 1`, which is what the FIRM line asks for ("reverted
(never reset), so its next parcel starts on its pre-claim tree"). Git
then treats that content as merged. A later merge of main or of the next
received commit will not bring it back, so the seat's next parcel works
on a tree missing it. That parcel's forward also carries the reverts
downstream, where a merge would delete the content (the BL-490/BL-1348
revert-a-merge shape). It fires only after a real give-up, which needs
the BL-1702 mixed pack (not live), so this is a follow-up for the
specifier before BL-1702 promotes, not a hold on this parcel.

Hook-run observation: the pre-commit hook's property run for a7c145648d
(same tree plus the restore) failed
`test/bl1678LandNeverShipsUnapprovedForwardInvariants.property.test.js >
BL-1678/BL-654 invariant 2: origin/main never gains a merge commit through
the publish step - a merge tip and a foreign-path single-parent tip both
refuse, a genuinely clean one lands` with `Error: Test timed out in
20000ms.`. The same file passed in this gate run and in the BL-1767 and
BL-1772 gate runs today. It is not a red in this measurement, and neither
flakiness nor its cause is established. The invariant drives the real
`--push`, which since hotfix db4da5c573 also runs the re-point. Reported
to the specifier as information only; the hook log is kept at
tmp/restore-commit.log.

By QA.
