# BL-1353 architect review — clean sweep, forwarding

## Reviewed
Merged cleaner's `7229395b44`. Read the ticket (human ruling: RETIRE
`TASK_ARRIVED`), coder evidence, cleaner evidence, the full diff of
`operator_lib.bb`/`operator_runtime.bb`, the amended feature file, and the
doc update.

## Dependency gate (hard gate, BL-259)
No TypeScript source changed (diff is `.bb`, `.md`, and a plain JS
acceptance step file). Ran anyway for completeness:
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1353TaskArrivedIsNotAnEscalationSteps.js ../extension/test/bl1353TaskArrivedIsNotAnEscalation.property.test.js
```
PASSED, no forbidden edges.

## Logical coupling (BL-255)
`co-change-report.js` over the changed files: nothing at or above the
default threshold (frequency 3) — every match is a single co-change.
No suspected coupling.

## Invariants — verified directly in the diff
1. **No wake source removed before a live producer covers it.** The diff
   removes `TASK_ARRIVED` from both `tick-observed-events` (drops the
   `coordinator-inbox-fresh?` destructure and its `conj`) and
   `manufactured-tick-event-types` in the same hunk — exactly BL-654's
   "editing the emitter without the set leaves a false green" concern,
   avoided. `babysitter-escalation-event` and the `SWARM_CONTROL_LOST`
   path are untouched; both still fire via the queue, not the tick.
2. **The freshness probe keeps its second consumer.** `operator_runtime.bb`
   drops the probe from the tick call site only (`coordinator-inbox-fresh?
   (coordinator-inbox-has-fresh?)` line removed from the tick's `observed`
   map) — `coordinator-inbox-has-fresh?` itself is untouched, still called
   at line 1459 for `closing-pass-sweep!`'s `fresh-mail?`, feeding
   `should-relaunch?` (BL-310 up-trigger). `should-hibernate?` is not
   touched anywhere in the diff, confirming the down-trigger stays
   independent of mail freshness as the ticket requires.

## Amendment travel (BL-233)
The specifier's priority-00 amendment on `main` (`98454ae8c4`, merged into
this worktree ahead of the coder's own commit) changed scenario 03's
wording from hibernate-direction to relaunch-direction. The coder's first
handler predated that wording (flagged correctly as a note rather than
silently patched over); the cleaner's pass is exactly the required
same-parcel handler fix — renamed the `When`/`Then` regexes to match the
amended step text, added the new `Given` step, and left every real
assertion (`s.relaunches === 'true'`) unchanged, since the underlying
`should-relaunch?` behavior was never wrong, only the step wording that
described it. Confirmed the current feature file text matches the handler
regexes and that the "trap to avoid" framing in the ticket description
lines up with the code (`should-hibernate?` never reads mail freshness).

## required_wiring
Both anchors confirmed live:
- `docs/how-to/BL-653-operator-escalation-driven-wake-model.md` names
  `TASK_ARRIVED` twice — in the "What retired" list and the quiet-night
  regression checklist.
- `specs/pipeline/steps/bl1353TaskArrivedIsNotAnEscalationSteps.js`
  exports `registerSteps`, confirmed the runner picks it up (4/4 scenarios
  execute, none unmatched).

## Independent re-verification
- `npm run compile` — clean.
- `node specs/pipeline/cli.js specs/features/BL-1353-...feature` — **4/4
  pass**.
- `bb swarmforge/scripts/test/operator_lib_test_runner.bb` — ALL TESTS
  PASSED.
- `bb swarmforge/scripts/test/operator_lib_bl653_property_runner.bb` — ALL
  PASSED (forbidden set now also catches `TASK_ARRIVED` re-entering).
- `test_operator_runtime_tick.sh` — ALL CHECKS PASSED.
- `test_operator_runtime_bl653_escalation_driven.sh` — ALL CHECKS PASSED
  (escalation and `SWARM_CONTROL_LOST` still launch).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1353TaskArrivedIsNotAnEscalation.property.test.js` — **3/3
  pass**.

## Scope check
No architecture-boundary change: pure Babashka logic edit plus a doc
update plus one acceptance step-handler file kept in sync with its
feature. No new module, no I/O/UI boundary crossed, no secrets, no
process spawned outside tmux.

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener.
