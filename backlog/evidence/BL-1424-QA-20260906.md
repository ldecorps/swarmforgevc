# BL-1424 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1424`: `VERIFY BL-1424` (no open
  deferral).
- Orphan process check before/after: empty both times.
- Merge lineage: the inbound documenter merge (`a72f26cb42`) was a real
  3-way merge, not a fast-forward. Diffed against BOTH parents: parent1
  (my prior HEAD) gained exactly BL-1424's own files; parent2
  (documenter's branch) gained only `swarmforge/roles/hardender.prompt`'s
  25-line rule_proposal-accept content my branch already had — no path
  dropped on either side.
- `bash swarmforge/scripts/test/test_check_test_file_registration.sh`:
  ALL PASS (9/9, including the two wiring assertions and the hardener's
  added first-commit-no-HEAD cases).
- `bb swarmforge/scripts/test/bl1424_test_file_registration_guard_property_runner.bb`:
  ALL PASS (56 cases).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1424-a-commit-that-adds-a-test-file-registers-it.feature`
  — 7/7 scenario runs.
- Wiring anchor 1: `grep -n check_test_file_registration.sh
  swarmforge/scripts/run_commit_guards.sh` — placed immediately before
  `if guard_chain_has_refusal`, i.e. in Tier 1. Confirmed NOT in
  `land_step_lib.bb`'s tree-guard list (`git diff --name-only <merge-base>
  <tip> -- swarmforge/scripts/land_step_lib.bb` empty — untouched, as the
  ticket requires).
- Wiring anchor 2: `bl1424TestFileRegistrationGuardSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Standing manifest row for the guard's own dogfooded test file confirmed:
  `swarmforge/scripts/test/suite-manifest.tsv` has one `standing` row for
  `test_check_test_file_registration.sh`.
- Invariants confirmed by direct code read of `check_test_file_registration.sh`,
  `check_test_file_registration_cli.bb`, and `unregistered_test_gate_lib.bb`'s
  `findings-for-staged-commit`/`staged-added-test-paths`/`staged-manifest-rows`:
  commit-scoped via `git diff --cached --diff-filter=A` against
  `diff-base` (invariant 1); the staged-commit path calls the SAME
  `unregistered-findings`/`suite-inventory-lib` logic `findings-for-git-handoff`
  uses, no second notion of registered (invariant 2); an unreadable staged
  diff or staged manifest (`git show :<path>`) returns `{:warning ...}`,
  printed then exit 0, never a refusal, and the index is read exclusively
  — never unstaged working-tree state (invariant 3).
- **Live probe (qa_e2e_procedure item 3)**: `git clone` (never a linked
  worktree — shares `.git/config`, BL-1390) of the live repo into the
  scratchpad, `git checkout` this parcel's exact merge commit,
  `core.hooksPath` set to the clone's own `swarmforge/git-hooks`. Staged
  an unregistered `test_probe_bl1424.sh` under `swarmforge/scripts/test/`
  and committed: refused, naming the file and quoting exactly
  `test_probe_bl1424.sh<TAB>standing` as the ticket specifies. Added the
  row and re-committed: silent, the rest of the chain ran normally. Clone
  deleted afterward; live repo's own `origin` remote confirmed unchanged
  (`git@github.com:ldecorps/swarmforgevc.git`).
- `bl1252CommitGuardAggregationInvariants.property.test.js`: 5/5 (run
  standalone and again as part of the full property suite below) with
  `check_test_file_registration.sh` in `INDEX_GUARDS`.
- `test_run_commit_guards.sh`: fails at case 01 on
  `check_constitution_doc_citations.sh` (exit 127) — confirmed this is
  BL-1408's pre-existing, already-registered standing red
  (`backlog/standing-reds.tsv`, open ticket
  `backlog/paused/BL-1408-...yaml`), unrelated to and unmoved by this
  ticket's own `check_test_file_registration.sh` addition to the same
  `ALL_GUARDS` list (confirmed present in the diff, deliberately not
  touching the constitution-citations entry per the ticket's own
  instruction not to paper over BL-1408's red here).
- Full unit lane (`npm test`, extension/): 610/610 files, 10283/10283
  tests green. Same chronic suite-duration/per-file budget overage as
  other recent parcels — none of the four flagged slow files touched by
  this diff.
- Full property lane (`npm run test:properties`): 363/364 files green on
  this run (1086/1087); see "Not bounced" below for the one failure —
  confirmed 364/364 (1087/1087) via the isolated re-run.
- Docs: `docs/reference/Specification.MD`, `docs/index.md` (guard count
  bumped six → seven), and
  `docs/how-to/BL-1252-commit-guard-chain-reports-every-violation.md` all
  describe the new guard. No diagram trigger fires (not claimed by the
  ticket; confirmed no diagram touched).

## Not bounced (observed, not this ticket's defect, not reproducible — recurring flake)

`test/bl1218RemoteControlConfigInvariants.property.test.js` — "property
(invariant 2): a persisted launch script never disagrees with the config
that wrote it" failed with the SAME `edamame`/EDN parse error
("EOF while reading, expected \" to match \"" inside a `bb`-generated
source) already documented on `backlog/evidence/BL-1442-QA-20260906.md`'s
own "Not bounced" section two parcels ago this same shift. Passed cleanly
on immediate isolated re-run (`npx vitest run --config
vitest.properties.config.mjs test/bl1218RemoteControlConfigInvariants.property.test.js`,
4/4, host load normal at 5.24). Still no ticket found (`grep -rl
bl1218RemoteControlConfigInvariants backlog/paused/ backlog/active/`
empty; not in `backlog/standing-reds.tsv`). This is the SECOND occurrence
I've personally observed this shift, both one-off and both clean on
rerun — flaky by direct evidence, unrelated domain (remote-control
launch-script/config invariant vs. this ticket's commit-guard-chain
wiring). Not re-litigated at length here; see BL-1442's own evidence for
the first occurrence's full detail.
