# BL-732 — cleaner pass — 20260825

- Tip-pure rebuild from `origin/main` + coder `7075718109` only
  (`dels_on_origin=0`).
- Extract `titleCaseAlnumChunk` from `displayNameForRole` (zsh `(C)` mirror).
- `vitest run test/needsHumanDetection.test.js` — 66/66 pass (after `tsc`).

By cleaner.
