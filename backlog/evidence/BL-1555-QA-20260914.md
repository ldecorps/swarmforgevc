# BL-1555 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Resume of a QA hold opened earlier today (evidence
`BL-1555-qa-unowned-red-bl1297-20260914.md`, commit `c859adced9`): the
parcel was fully verified except for one unowned red
(`bl1297MergeOwnPathsInvariants.property.test.js`, timeout in the full
404-file property lane). The coordinator's resume note (2026-09-14
13:36Z) reported BL-1564 now owns that row in `backlog/standing-reds.tsv`
(confirmed by direct read). Re-ran the full gate on the held parcel
commit `006c8a3eed` (`Merge documenter ad46cc95bd into QA.`) rather than
trusting the note alone:

1. `qa-sibling-check.js status --ticket BL-1555`: `VERIFY BL-1555`, no
   open deferral.
2. Orphaned `node --test|stryker|vitest` process check: none, before and
   after (two loosely-matching `pgrep -f` hits were this session's own
   shell/monitor scaffolding, confirmed via `/proc/<pid>/cmdline`).
3. `npm run compile` (extension/): clean.
4. Full unit suite (`npm test`, extension/): 616/616 files, 10479/10479
   tests green. Suite-budget-over-time warnings only (perf, non-blocking).
5. `npm run test:properties`: 401/404 files green on the full-lane run;
   3 failed — `bl1272LandedSiblingInvariants` (already owned, BL-1556,
   registered), `bl1429StandingRedThrottleFoldInvariants` (unregistered;
   confirmed concurrency-flake disposition twice before on this exact
   file, `BL-1494-QA-20260910.md` and `BL-1498-QA-20260913.md` — "not a
   red at all once isolated"), `meanTicketTimeCost` (unregistered; same
   symptom class — a corpus-reach coverage assertion sensitive to
   full-lane host load). All three re-run individually
   (`npx vitest run --config vitest.properties.config.mjs test/<file>`):
   9/9 tests green in 12.4s total. `bl1297MergeOwnPathsInvariants` itself
   did NOT fail this run. Two `[vitest-worker]: Timeout calling
   "onTaskUpdate"` unhandled errors — the allowlisted BL-871 signature,
   not a new failure.
6. `pre_qa_gate.sh BL-1555-the-bl1253-and-bl956-properties-construct-their-reach-floors 006c8a3eed`:
   `OK` (one subject-only-ancestry warning naming this session's own
   `c859adced9`, no path overlap with the parcel — not a defect).
7. Acceptance: `run_acceptance.sh specs/features/BL-1555-*.feature` —
   10/10 scenarios (TAP) green, including the reach-floor and
   floor-still-asserted subtests the ticket's own `qa_e2e_procedure`
   items 1-3 describe.
8. Ancestry: this ticket's own hardener merges (`5b480129cd`,
   `a3bcf9fe28`) confirmed ancestors of `006c8a3eed` via
   `git merge-base --is-ancestor`.
9. `git diff main...006c8a3eed --name-only`: exactly the two property
   test files, the step handler, and this ticket's own evidence files —
   matches `qa_e2e_procedure` item 4 (`extension/src/`,
   `property_suite_standing_allowlist.tsv`, BL-1253/BL-956 features
   untouched).
10. Bounce history: `main`==`origin/main` (0/0 rev-list), no
    `bounce_history` on the ticket YAML — clean prior record
    (BL-340/BL-891).

Forwarding `006c8a3eed` (the same commit the original hold cited, now
cleared) as the approved commit, per BL-536.

By QA.
