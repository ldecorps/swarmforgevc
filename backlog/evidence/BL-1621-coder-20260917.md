# BL-1621 coder pass

## The full-lane failure, verbatim (first sighting)

QA's BL-1605 pass, 2026-09-17T08:41Z, parcel 9583f2177c, full property lane
via `npm run test:properties` / qa-gather.js
(`backlog/evidence/BL-1605-qa-unowned-red-bl1297-20260917.md` on the QA
branch, commit 574d99483c):

```
FAIL  test/bl1297MergeOwnPathsInvariants.property.test.js > property
(invariant 3): the land step reads delivered, the two send-time gates
read authored
Error: Test timed out in 20000ms.
```

The specifier re-measured the file alone on main (738952fbc0, 08:53Z,
1-minute load 2.67 on 20 cores): green, 3 tests, 15.13 s wall - invariant 1
3.6 s, invariant 2 2.8 s, invariant 3 8.3 s - reach maps 21, 18 and 18
cases, unchanged from BL-1564
(`backlog/evidence/BL-1621-specifier-adjudication-of-qa-unowned-red-bl1297-20260917.md`).

## The remedy that removed it

`20000ms` was `vitest.properties.config.mjs`'s flat suite-wide
`testTimeout`: the file's three `test(` calls passed no third argument, so
the lane's raw ceiling was their budget and did not move with contention
(the bl1304 shape BL-1606 fixed on 2026-09-16, on a file BL-1606's census
did not include because it was green that day).

Fixed by adding `const { propertyLaneTimeoutMs } =
require('./helpers/propertyLaneContentionBudget');` and closing each of
the file's three `test(...)` calls with `}, propertyLaneTimeoutMs(20000));`
instead of a bare `});` - byte-for-byte the shape
`extension/test/bl1304DryRunSpawnsNothing.property.test.js` has carried
since BL-1606. Nothing else in the file moved: BL-1564's one-bb-process
batches, the fc.sample seeds, and the reach maps are untouched.

## Solo run after the fix

`npx vitest run --config vitest.properties.config.mjs
test/bl1297MergeOwnPathsInvariants.property.test.js`, once, from
`extension/`:

```
 ✓ test/bl1297MergeOwnPathsInvariants.property.test.js (3 tests) 8142ms
   ✓ property (invariant 1): delivered is the first-parent delta, authored is what differs from every parent  1557ms
   ✓ property (invariant 2): an empty answer is the truth, never an artefact of the invocation  1434ms
   ✓ property (invariant 3): the land step reads delivered, the two send-time gates read authored  5149ms

BL-1564 reach map (invariant 1): {"cases":21,"seed":1502749020}
BL-1564 reach map (invariant 2): {"cases":18,"seed":3858746275}
BL-1564 reach map (invariant 3): {"cases":18,"seed":1127922307}
```

3 passed, 8.43 s total - reach maps 21, 18, 18, exactly BL-1564's.

## Register rows

Left in place at mint, per the ticket's `notes:` - QA removes both in the
land that turns the file green (`backlog/standing-reds.tsv`'s BL-1621 row,
`swarmforge/scripts/property_suite_standing_allowlist.tsv`'s mirror row).
