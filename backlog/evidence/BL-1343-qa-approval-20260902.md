# BL-1343 — QA approval (20260902)

Received: documenter commit `e9992c4e51` (doc-only, Specification.MD entry +
BL-1241 how-to addendum, both committed separately per the cross-ticket-doc
convention), forwarding hardener `c949064bac` (architect repass `36dd69e341`,
cleaner rework `e81e1bdbfc`, coder's D1 rework `f89d6eadff` fixing the
architect's own property-test-generator bounce). Merged into QA.

This is the fix for the exact attribution-walk defect I personally hit twice
today hand-landing BL-1317 and BL-1340: `land_step_cli.bb`'s `LAND_REPLAY`
reporting success on a replay that either dropped the ticket's own path
entirely or carried unrelated siblings' content. My own land-success evidence
for both those tickets independently documents the same silence this ticket
closes.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1343-replay-drops-the-tickets-own-path.feature`): 6/6.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1343ReplayDropsTheTicketsOwnPathSteps` (line 19, the
  `required_wiring` pin) — confirmed by grep.
- Declared invariants
  (`extension/test/bl1343ReplayNeverDropsOwnPathInvariants.property.test.js`,
  `npx vitest run --config vitest.properties.config.mjs`): 2/2.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-patched `own-paths`' refusal guard (`land_step_lib.bb:322`,
  `(if (and (empty? acc) (seq delivered)) ...)` → `(if false ...)`) and
  re-ran the property test — both invariants failed exactly as the
  hardener's evidence claimed. Restored (`git diff --stat` empty), re-ran
  clean.
- **Hardener's own mutant-kill re-verified independently**: hand-patched the
  refusal-warning fold (`land_step_lib.bb:342`,
  `(conj excluded {...})` → `[{...}]`, the mutant that drops all but the
  last excluded path from a multi-path refusal) and re-ran the bb test
  suite — scenario 03b failed exactly as claimed (both assertions, naming
  only the first path/sibling). Restored, re-ran ALL PASS.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical to the
  standing-debt population from my own prior BL-1317/BL-1340 passes today
  (landPilotedTicket/`checkOrphanedAuthoredDocs` family,
  `constitutionDocCitations`, real-tree scanners). None touch
  `land_step_lib`, `own-paths`, or `entangled-siblings`.
- Property suite (`npm run test:properties`): 24 files / 13 tests red, same
  standing-debt population. BL-1343's own property test — 2/2 green.
- Documentation currency: `docs/reference/Specification.MD`'s top entry and
  `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-
  remedy.md`'s new addendum section confirmed present and accurate; no new
  doc file so no `docs/index.md` link required; no diagram change-trigger
  fired (confirmed — none of the four registered diagrams depict the land
  step's internal attribution walk).
- Orphaned test processes: none after the property-suite run.

## Constraints honoured (verified, not just claimed)

- BL-1272's fail-closed sibling rule untouched: `sibling-landed?`,
  `entangled-siblings`, `landed-siblings` unchanged in this diff.
- BL-1332 (the mirror case, a shared path taken whole) not folded in.
- BL-1338 not landed by this ticket.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
