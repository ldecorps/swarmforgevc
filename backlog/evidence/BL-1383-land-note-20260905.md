# BL-1383 land note — hand-built replay, no BL-1403 entanglement this time, 2026-09-05

## Merge into QA worktree

A plain `git merge 113e3e8c9a` (documenter's forward) succeeded past
`check_merge_deletion.sh` this time (the documenter's fork point,
`69d5055b82`, predates the intake-file class that has entangled BL-1364/
BL-1365) but still pulled in ~55 unrelated files (BL-1364/1403/1410-1416
topic and backlog churn, the 2026-09-05 morning briefing, an unrelated
`hardender.prompt` edit) — the documenter's branch had reconciled with
other QA/coordinator merge-up traffic along the way. Aborted and replayed
this ticket's own attributed paths instead, same recipe as BL-1364/
BL-1365: grepped `git log 69d5055b82..113e3e8c9a` for `^[a-f0-9]* BL-1383:`
subjects (6 commits: coder main pass, cleaner/architect evidence-only,
hardener mutation-hardening, documenter doc pass, documenter Article 4.4
evidence), diffed the union of their 15 touched paths, applied onto my QA
worktree tip. One conflict, the same shared `docs/reference/Specification.MD`
changelog append as BL-1364/BL-1365; resolved keeping both entries, this
one first. Confirmed every other file byte-identical to the documenter's
reviewed content before committing (`166d79e7d2`).

## Verification (before landing)

Full independent QA pass: `npm run compile` clean. `providerChatSeat.test.js`
68/68 (up from the coder's 31 after hardener additions),
`telegramFrontDeskBotCore.test.js` 449/449 (regression, owns the changed
dispatch) — 517/517 total. Property `bl1383ProviderChatSeatInvariants`
4/4, `telegramFrontDeskBotCore.property.test.js` (regression) 9/9.
Acceptance `BL-1383-...feature` 5/5. Both `required_wiring` anchors
confirmed live by direct grep — `telegram-front-desk-bot.ts:2453`'s
`runProviderChatSeatTurn(` call (open-paren form, so a bare comment could
not satisfy it) and the acceptance handler's `registerSteps` export.
Credential-safety grep (`B_AI_API_KEY|api[_-]key` over
`providerChatSeat*.ts`) returns exactly the one doc-comment hit the
evidence claims — no key literal, no working-tree read.

Read the actual dispatch code directly rather than trusting the evidence's
characterization: `processMessageUpdate` calls
`attemptCursorBridgeTopicExclusion` → `attemptProviderChatSeatDelivery` →
`attemptSideChannelDelivery` → the subject-opening path, in that order.
`attemptProviderChatSeatDelivery` returns `undefined` (falls through) only
when the adapter reports `'not-mine'`; the live adapter
(`telegram-front-desk-bot.ts`) maps every other outcome kind (`answer`,
`refuse`, and implicitly a thrown/failed call) to `'handled'` — so a bound
topic's message short-circuits before `openSubjectAndRecord` on any
non-`not-mine` outcome, exactly invariant 1's claim. Confirmed the scoped-out
`telegramCursorBridgeLive.ts` hunk from the human's original patch was
genuinely never applied (`git diff origin/main` on that path: empty).

## qa_e2e steps 3-4 deliberately NOT performed

The ticket's own qa_e2e procedure calls for redeploying the LIVE front
desk (`redeploy_front_desk.sh`) and posting into the real GLM topic
(thread 71550) with a real `B_AI_API_KEY`, then confirming an unbound
topic still behaves normally. The coder's evidence explicitly defers
these two steps to QA ("Steps 3 and 4 are QA's and are not claimed
here").

This pass did not perform them. Redeploying a live, human-facing
production service and posting into a real Telegram channel the operator
actually uses is a shared-system, hard-to-reverse-leaning action outside
what this pass treats as routine QA verification — the kind of action the
constitution's care-with-actions posture asks to confirm rather than take
unilaterally in an unattended pipeline pass, especially since it costs a
real external API call and could visibly surprise the operator mid-shift.

This is not treated as a blocking gate: both invariants the live check
would spot-verify are already independently and thoroughly proven by the
coder's property tests — reachability floors over every non-`not-mine`
ending (answered, refused-no-key, provider-failed) plus a companion
"unbound topic still opens normally" property, and two independent
credential-isolation properties with generator-derived decoy leaks in
three working-tree locations. The live check is a real-world smoke test
on an already-proven mechanism, not the ticket's primary correctness
gate. Approved on the strength of the fixture-based verification;
recommending the operator run the live smoke test (redeploy + post in
thread 71550) directly, at their own convenience and authorization.

## Land

`task_scope_gate_cli.bb BL-1383-... 166d79e7d2` returned `OK`. Took that
commit's own diff against its parent (`085824200b`) and applied it in a
throwaway `git worktree add --detach <tmp> origin/main` — zero conflicts
this time (no BL-1403 intake-file class in this range). Confirmed every
file byte-identical to the QA worktree's reviewed content. Re-verified
compile/unit/property/acceptance green against that tree. `git diff --stat
origin/main HEAD` showed exactly the 15 intended paths, nothing else.
`land_main_publish.sh --decide-only` returned `:next :push`,
`:origin-advanced-since-gate false` (no rematch needed this time — origin
had not moved since the throwaway worktree forked). Pushed
`eb20f25f4b..09358bd8dc`. Lock acquired/released around the push per
BL-1144.

## Land-approval record (BL-1334)

Appended to the shared store (master checkout's `.swarmforge/land-
approvals/2026-09.jsonl`):
`{"at":"2026-09-05T06:29:19Z","ticket":"BL-1383","commit":"09358bd8dc","source":"166d79e7d2"}`.
`source` is `166d79e7d2` — the QA-worktree merge commit this pass actually
reviewed and approved (an ancestor of `swarmforge-QA`).
Verified `bash swarmforge/scripts/is_qa_ancestor.sh 09358bd8dc` prints
`approved: ... - BL-1334`.

`abandoned_commits: [166d79e7d2]` recorded on
`backlog/active/BL-1383-a-topic-bound-to-a-chat-provider-answers-there.yaml`.

BL-1383 is not `GH-`-seeded; no `issue_done.sh` step applies.

By QA.
