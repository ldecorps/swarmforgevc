# BL-725 — coder pass, 2026-09-06

## What was implemented

Renamed the standing host-agent Telegram topic's DISPLAY NAME from
"Cursor Remote" to "Host", matching the human's hand-rename already live
in Telegram. The binding stays keyed by subject id (`CURSOR_REMOTE`,
unchanged), so the already-bound thread is never orphaned or duplicated.

- `extension/src/tools/telegramCursorBridgeCore.ts`:
  `CURSOR_BRIDGE_TOPIC_NAME = 'Host'` (was `'Cursor Remote'`) — its one
  production consumer (`ensureCursorTopic`'s create path, in
  `telegramCursorBridgeLive.ts` — renamed from the ticket's own cited
  `createCursorRemoteTopic` since mint) reads this, so only a NEWLY
  created topic's title changes.
- Three verified operator-facing text sites (strings a human actually
  reads in Telegram), all still present at mint-verified call sites,
  just at drifted line numbers:
  - `telegramCursorBridgePilot.ts` (4 status-prompt strings, one
    corrected from an awkward "host Host" duplication after the raw
    substitution).
  - `telegramCursorOperatorExec.ts`'s unknown-verb reply.
  - `telegram-front-desk-bot.ts`'s topic-ownership error.
  - Their own synced test assertions (`telegramCursorBridgeCore.test.js`,
    `telegramCursorBridgePilot.test.js`).
- `docs/diagrams/cursor-remote-flow.mmd`: the topic-check node relabeled
  Host (file itself not renamed — a content relabel, per the ticket's own
  "diagram label" instruction).
- `docs/how-to/BL-698-telegram-cursor-operator-commands.md`: initially
  relabeled its own operator-instructional sentence, then REVERTED
  (separate commit) — the task-scope gate correctly refused the send,
  since that file's own name carries a different ticket id (BL-698) and
  BL-725's ticket YAML declares no `retires:`/`acceptance:` exemption for
  it. Not required by any scenario in this ticket's own acceptance
  feature (only the diagram and the three verified operator-text sites
  are tested), so reverted rather than widening the ticket's own
  declared scope for a one-sentence, untested doc edit. Deliberately NOT
  a sweep of every "Cursor Remote" mention across the repo's ~40 other
  reference/historical docs either way — the ticket's own carve-out
  ("internal code comments... name the incarnation correctly") extends by
  the same reasoning to historical spec/reference prose that documents
  what a past ticket did, not live operator-facing instruction; BL-711 is
  explicitly the (out-of-scope) vocabulary-glossary ticket for that wider
  sweep.
- New acceptance step handler
  `specs/pipeline/steps/bl725RenameCursorRemoteTopicToHostSteps.js`
  driving the real compiled code (`ensureCursorTopic` with a stubbed
  `createTopic`/real topic-map fixture files for the create/reuse
  scenarios; the three operator-text sites and the diagram read directly
  off their live source files, scoped to each site's own specific string
  so a pass can't happen on an unrelated "Host" substring elsewhere in a
  large file). Feature file (pre-minted, not modified).

## BL-654 invariant — non-vacuous property test

`extension/test/bl725HostTopicBindingInvariants.property.test.js` (new):
for any already-bound thread id (0 through the realistic Telegram int32
range, an explicit boundary draw since a naive truthiness check could
mishandle 0), `ensureCursorTopic` always resolves to that exact id and
never calls `createTopic`. Non-vacuous: broke the reuse branch
(`if (false && action.kind === 'reuse')`), confirmed the property caught
it (counterexample thread 0, create called), restored, confirmed
byte-identical via diff, reconfirmed green.

## A second, unrelated defect found and fixed in the same pass

Running the FULL unit suite (not just the files this ticket touches)
caught `test/tempDirTrapGuard.test.js` flagging
`coordinator_activity_feed_lib_test_runner.bb` (my own GH-24 authorship,
already forwarded past me in the pipeline): its temp root had only manual
cleanup calls, no shutdown hook. Fixed and committed separately
(`GH-24: add a shutdown hook...`, not bundled into this ticket's own
commit) — GH-24's parcel has already moved on, so this rides in my next
handoff for it rather than a separate note; not this ticket's scope.

## Checks run

- `npx tsc -p ./`: clean.
- `npx vitest run test/telegramCursorBridgeCore.test.js test/telegramCursorBridgePilot.test.js test/telegramCursorOperatorExec.test.js test/telegramFrontDeskBotCli.test.js test/telegramFrontDeskBotCore.test.js`:
  all green (152 + 24 + 735 tests across the touched/adjacent files).
- Full `npm test`: 10309/10309 (after the tempDirTrapGuard fix above).
- Acceptance (pre-minted feature): 9/9.
- Property test: non-vacuous, one deliberate break caught and restored.

## Out of scope, confirmed untouched

- The subject id `CURSOR_BRIDGE_SUBJECT_ID`/topic-map value
  `CURSOR_REMOTE` — unchanged, per the ticket's own "Explicitly NOT
  renamed" section (verified: `grep CURSOR_BRIDGE_SUBJECT_ID` still
  returns `'CURSOR_REMOTE'`).
- Filenames, module names, symbols (`telegramCursorBridge*`,
  `telegramCursorOperatorExec`, etc.) — untouched.
- `docs/done/**`, historical evidence, the BL-711 glossary — untouched.
- Internal code comments naming the module/incarnation (`// the Cursor
  Remote bridge`) — deliberately left as-is throughout every touched file.

By coder.
