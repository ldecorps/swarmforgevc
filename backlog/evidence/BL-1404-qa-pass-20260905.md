# QA pass — BL-1404 — 2026-09-05

Redriven per coordinator note ("merged into your worktree, never
QA-passed — queue is empty, run it"). Commit under review: `15e667aa50`
(merge of documenter's `71583741ed` into `swarmforge-QA`), already an
ancestor of my current HEAD (it landed earlier in this shift as part of
the BL-1422 land chain, but the QA review step itself was skipped). Full
chain confirmed: `fd08126767` (BL-1404 coder) is an ancestor of HEAD via
`git merge-base --is-ancestor`.

## Verification

- `bb swarmforge/scripts/test/bl1344_waive_lib_test_runner.bb`: ALL PASS
  (BL-1344's own waive-store contract, unchanged).
- `bb swarmforge/scripts/test/bl1404_waive_silences_escalation_property_runner.bb`:
  ALL PROPERTIES HOLD (500 runs/property).
- `bash swarmforge/scripts/test/test_babysitter_check.sh`: ALL PASS,
  including this ticket's own scenarios N/O/P/Q (an un-waived CRIT still
  escalates; a recorded waive silences BOTH the nudge and the escalation;
  two findings, one waived, only the other escalates; a corrupt/unreadable
  waive store escalates everything and logs WAIVE-STORE-UNUSABLE).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1404-a-recorded-waive-silences-the-operator-escalation-too.feature`
  — 4/4 scenarios pass.
- Wiring confirmed directly in `swarmforge/scripts/babysitter_check.bb`
  line 1339: `decide-escalations` is now called with `nudgeable` (the
  post-waive/post-partition set), not the raw `findings` list — the exact
  fix the ticket names. `specs/pipeline/steps/bl1404WaiveSilencesEscalationSteps.js`
  exports `registerSteps`.
- Docs: `docs/how-to/BL-611-babysitterd-runbook.md` already documents the
  waive mechanism and its BEFORE-cooldown application; consistent with the
  fix (no separate escalation-specific doc gap found).
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED. The code (`15e667aa50`) is already on `origin/main` (landed
earlier in this shift as part of the BL-1422 chain) — no further push
needed. This evidence file lands normally; coordinator bookkeeps BL-1404
to `backlog/done/`.
