# BL-1565 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Reviewed against documenter's commit af6c197938, merged into this worktree
(this pass's own commit is the merge). `git merge-base --is-ancestor
af6c197938 HEAD` and `...60975202ab HEAD` (hardener's fix commit) both OK —
this ticket's own stage commits are ancestors of the reviewed tip, not
merely something else's commit that happens to be an ancestor of HEAD
(BL-336 trap).

- `qa-sibling-check.js status --ticket BL-1565` -> `VERIFY BL-1565` (no open
  deferral).
- No orphaned test/mutation/vitest processes before or after
  (`pgrep -fl 'node --test|stryker|vitest'` clean each time).
- `bash swarmforge/scripts/pre_qa_gate.sh . <HEAD>` -> `OK` (both
  `required_wiring` entries satisfied).
- Full unit suite (`npm test`, extension/): 616 files / 10479 tests, all
  green.
- `swarmforge/scripts/gherkin_lint_gate.sh` on each of
  `specs/features/BL-1565-*.feature`, `BL-1536-*.feature`, `BL-950-*.feature`
  individually (the gate's 2nd positional arg is ROOT, not a second file —
  passing >1 file at once silently mis-resolves it): all three "parses
  cleanly".
- `grep -n 'coordinator | carries' specs/features/BL-1536-*.feature` — empty
  (exit 1): the retired QA-to-coordinator example rows are gone, nothing
  reworded (BL-1006). `specs/features/BL-950-*.feature`'s narrative already
  re-tensed, confirmed by re-read.
- Acceptance (`run_acceptance.sh specs/features/BL-1565-*.feature`): first
  run 8/9 (scenario 03 "a note from QA to the coordinator still queues"
  failed — `HANDOFF SYNC INJECT FAILED: tmux send-literal failed`, fixture
  socket is a placeholder, real send fell back to async daemon-backup
  delivery and the assertion ran before it landed); re-ran 3x consecutively
  in isolation, 9/9 green each time — a delivery-timing flake in the
  fixture's fallback path, not a defect in the parcel's own code (confirmed
  by hand: a manual `swarm_handoff.bb` run of the identical note draft
  against a throwaway fixture queued synchronously and correctly).
- Property suite (`npm run test:properties`): the full 405-file run is
  self-contending under its own worker pool — 3 different files failed
  across two separate full runs (`bl1272LandedSiblingInvariants` class of
  finding not seen this pass; actually saw `bl1297MergeOwnPathsInvariants`,
  `bl1343ReplayNeverDropsOwnPathInvariants`, and
  `bl1474ReplayCommitRefusalReasonInvariants` — a different file set each
  run, all 20000ms timeouts), matching the pattern already on record from
  BL-1502/1527/1500/1489/1543/1560's QA passes. Re-ran each flagged file
  alone, nothing else running: all green (bl1297 3/3, bl1343 2/2, bl1474
  1/1). None of the three touch this ticket's own diff (`swarm_handoff.bb`,
  `git_handoff_recipient_guard_lib.bb`, the BL-1565 step handler and its own
  property file, which passed 3/3 every run) — grepped
  `backlog/standing-reds.tsv` and `backlog/paused|active`: no ticket, no row
  for any of the three; not a blocker for this parcel.
- `bl1565CoordinatorNeverReceivesGitHandoffInvariants.property.test.js`
  (this ticket's own): 3/3 green, every run.
- Wired-in check: `swarm_handoff.bb -main` calls
  `git-handoff-recipient-guard-lib/decide` right after `validate` and again
  on the routed recipient set (grepped both call sites directly in the
  merged file) — not merely unit-tested in isolation.
- `main` and `origin/main` in sync (`git rev-list --left-right --count
  main...origin/main` -> `0  0`); no prior bounce for BL-1565 in
  `.swarmforge/bounces/2026-09.jsonl` (master checkout).
- Coder/cleaner/architect/hardender/documenter passes all on record as NONE
  or (hardender) one defect found-and-fixed in the same pass, self-verified
  above; no unresolved bounce history for this ticket.

By QA.
