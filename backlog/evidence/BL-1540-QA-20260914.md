# BL-1540 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1540` → `VERIFY BL-1540` (no open deferral).
- `pgrep -fl 'node --test|stryker'` clean before and after.
- `npm test` (extension): 616 test files / 10479 tests, all green.
- `npm run test:properties` (extension): 403 passed / 404 files; 1 failure
  in `test/bl1218RemoteControlConfigInvariants.property.test.js` (invariant
  2). Checked against the standing-red register
  (`bb swarmforge/scripts/standing_red_register_cli.bb .`): this file is a
  pre-existing owned row (`ticket: BL-1457`, `first_seen: 2026-09-07`),
  unrelated to this parcel's shell-test changes — not a new regression, not
  unowned.
- `bash swarmforge/scripts/test/test_shell_fixture_dispatch_isolation.sh`
  (the BL-998 guard this ticket exists to green): PASS, exit 0.
- `bash swarmforge/scripts/test/test_ceremony_handoff_cli.sh`: 6/6 PASS.
- `bash swarmforge/scripts/test/test_bl1097_router_refuses_dispatched_ticket.sh`:
  10/10 PASS.
- `grep -n 'SCRIPT_DIR/\.\./' ` on both repaired tests: no remaining binding
  to `ceremony_handoff.sh` or `route_backlog_to_coder.sh` through the real
  scripts dir.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1540-two-shell-tests-dispatch-a-self-rooting-helper-through-the-real-scripts-dir.feature`:
  5/5 scenarios PASS.
- `bash swarmforge/scripts/pre_qa_gate.sh . HEAD` (required_wiring for
  `bl1540DispatchIsolationOffendersSteps.js::registerSteps`): OK.
- `git status --short` clean after the run.
