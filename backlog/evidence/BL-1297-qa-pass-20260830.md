# BL-1297 — QA verification PASS, landed 2026-08-30

## Lineage / commit cited

Merged documenter's tip `9f80beedfd` into QA (pre-merge QA HEAD `13f867acd1`),
producing merge commit `5b969670f8e46593f9626f2573cf366f6c74fa7c`. Verified
this ticket's own hardener merge `c05f99a9f3` is an ancestor of the cited
commit (`git merge-base --is-ancestor c05f99a9f3 5b969670f8` — OK), not merely
that the cited commit is an ancestor of HEAD (BL-336's trap).

Landed via BL-1144 discipline: acquired `.swarmforge/land-main.publish.lock`,
`land_main_publish.sh --decide-only` reported `:tip-contains-origin true`,
`:origin-advanced-since-gate false`, `:purity-action :push` (the composite
`:next :rematch-once-at-edge` is a lock-bookkeeping artifact of running
decide-only while self-holding the lock, not real peer contention — no other
session was landing concurrently). `git push origin HEAD:main` —
`fe99aacb52..5b969670f8`, fast-forward. Lock released.

## Unit suite (`npm test`)

16 files / 26 tests failed — all cross-checked against BL-1297's own changed
paths (backlog/evidence + docs/reference/Specification.MD +
extension/test/bl129{5,7}*.property.test.js + specs/pipeline/steps/* +
swarmforge/scripts/{task_scope_gate_lib,land_step_lib,swarm_handoff}.bb +
swarmforge/scripts/test/task_scope_gate_lib_test_runner.bb): zero overlap.
Every failing file traces to already-open/approved tickets, confirmed by
grep, not inferred:

- `checkOrphanedAuthoredDocs is not a function` (crossFileDuplicationCheck,
  multiBranchParserCoverageCheck, perHatRolePromptEvidenceCheck,
  pilotAcceptanceGate, pilotScopedCrapCheck, shellEntryPointDriveCheck,
  unreachableStepHandlerCheck) → BL-1221.
- `constitutionDocCitations`, `tempDirTrapGuard`, `socketFixtureShortRootGuard`,
  `liveRepoDerivationGuard` → named repo-hygiene reds, specifier disposition
  note `specifier-disposition-qa-standing-red-note-20260828.md`.
- `operatorRuntimeBbFixtureClosure` → BL-1265.
- `pinnedRepoFixture` → BL-1294 / BL-1240 lineage.
- `backendSwitch`, `telegramClient`, `telegramCursorOperatorExec` → BL-1263
  (three standing stale assertions, confirmed by reading the ticket: it names
  these exact three files and line numbers).

## Property suite (`npm run test:properties`)

25 files / 14 tests failed. Every one of the 25 failing files is present in
`swarmforge/scripts/property_suite_standing_allowlist.tsv` with
`disposition=allowlist` (checked programmatically, all 25 confirmed
allowlisted). BL-1297's own two property files both pass clean:
`test/bl1297MergeOwnPathsInvariants.property.test.js` (3/3, invariants 1-3)
and `test/bl1295RevertAttributionInvariants.property.test.js` (3/3 — this is
the neighbouring-fixture repair, still fails against BL-1295's own defect per
the coder's evidence, and passes now because BL-1295's own fix is already on
this QA branch from an earlier, separately-verified pass, not delivered by
this parcel).

No orphaned `node --test`/`stryker`/`vitest` processes before or after either
run (one property-suite run hit the 2-minute foreground Bash timeout;
verified the backgrounded process had already exited on its own — no reap
needed).

## Acceptance (`run_acceptance.sh`)

`specs/features/BL-1297-a-merge-commits-own-paths-are-not-empty.feature` —
6/6 scenarios pass (delivered-not-empty, merge-authors-foreign-path-refused,
merge-only-parcel-has-content-to-replay, non-merge-unchanged,
clean-receive-merge-not-refused, two-questions-agree-on-single-parent).

## required_wiring

`specs/pipeline/steps/index.js:656` registers
`require('./bl1297MergeCommitOwnPathsSteps')` — confirmed by direct grep, not
inferred from the acceptance run alone (though the 6/6 result also proves it,
per BL-233: an unregistered handler throws rather than silently skipping).

## Wiring correctness (invariant 3 — each caller reads its own question)

Confirmed by reading the call sites directly:
- `task_scope_gate_lib.bb`'s `parcel-own-changed-paths` (the send-time scope
  gate) calls `task-tagged-changed-paths ... :authored` explicitly.
- `unregistered_test_gate_lib.bb` calls `parcel-own-changed-paths` (inherits
  `:authored`).
- `land_step_lib.bb` passes `:delivered` explicitly at every call site
  (own-paths, task-tagged-changed-paths, diff-readable?) — commented in the
  source as a deliberate choice, not inherited from the helper's default.
- Root-commit handling read directly in `own-commit-changed-paths`: no first
  parent falls through to `--root`-flagged diff-tree for both semantics,
  never nil/empty for a root commit — matches the invariant "a root commit
  must not fail the gate open."

## qa_e2e — the real-world regression

Re-ran BL-1272's own landing exactly as the ticket's `qa_e2e` directs:

    bb swarmforge/scripts/land_step_cli.bb \
      BL-1272-a-landed-sibling-is-not-reported-as-entangled 22f267b6e9 \
      /home/carillon/swarmforgevc

Before this fix (original evidence,
`backlog/evidence/BL-1272-land-escalate-20260830.md`): `LAND_ESCALATE`,
"nothing to commit - own-paths identical to origin/main" (the merge blind
spot misreading `dde87ca41`'s DELIVERED paths as empty).

With the fix live: `LAND_REPLAY land-replay/BL-1272-22f267b6e9
5af4d27fd0a0b49b06ff212d14f4d26a25675753`, correctly naming 6
`ENTANGLED_SIBLING` tickets (BL-1183, BL-1235, BL-1240, BL-1250, BL-1253,
BL-1254) and 1 `LANDED_SIBLING` (BL-1224). This is the exact incident BL-1297
was minted to fix, confirmed resolved. Cleaned up the scratch
`land-replay/BL-1272-22f267b6e9` branch afterward (verification-only; landing
BL-1272 itself is a separate ticket's own QA pass, not this one's).

Also verified via the .bb files' own unit-test runners (Article 4.5 — no
mutation/CRAP/DRY wired for Babashka per engineering.prompt, gated by their
own suite): `task_scope_gate_lib_test_runner.bb` → `ALL PASS`;
`land_step_lib_test_runner.bb` → `ALL PASS`.

## Merge-up broadcast

Sent to coder, cleaner, architect, hardender, documenter: merge up to
`5b969670f8`. Coordinator notified for bookkeeping.

By QA.
