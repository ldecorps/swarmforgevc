# BL-1390 — documenter pass (2026-09-04)

## Received
`7251aa847f` (hardener; per `backlog/evidence/BL-1390-hardener-pass-20260904.md`).
Also merged `main` first (constitution amendment — the fixture-isolation
guardrail this same ticket's own incident produced, flagged by the
STALE_REFERENCE_ELABORATION check before `ready_for_next.sh` would
proceed).

## What changed, operator-visible
A new `post-commit` git hook pushes a commit on the shared master
checkout immediately, while a fast-forward is still possible, instead of
waiting for the periodic push sweep's own cadence — closing the daily
main-sync deadlock (32 local-only commits, 115 deadlock-latch trips, one
close held 48 ticks, four operator hand-merges, measured 2026-09-04).

## Doc-domain review
No existing page covers the shared checkout's PUSH path as a whole —
`docs/how-to/BL-1085-...md` is narrowly about the periodic sweep's own
caching efficiency, and `docs/how-to/BL-891-...md` is the RECONCILE
(pull) side, not push. This is a genuinely new, distinct mechanism (a git
hook, not an extension of the periodic sweep) with real operator-facing
surface (env controls, a log to read, what to check if a landing still
needs a real merge) — wrote a new how-to page rather than stretching an
existing one to fit.
- `docs/how-to/BL-1390-post-commit-push-while-it-still-fast-forwards.md`
  (new): the mechanism, the decision/push shape, operator env controls,
  what to check if a landing still needs a merge, and the fixture-isolation
  incident that happened during this ticket's own build (now a standing
  engineering guardrail, cross-referenced rather than restated).
- `docs/index.md`: linked the new page in the same commit (orphan-free).
- `docs/reference/Specification.MD`: new top-of-stack entry.
- Diagram registry checked — `architecture.mmd`'s daemon-sweep edge label
  already only lists the PERIODIC sweeps; this hook runs synchronously
  outside that poll loop entirely (handoffd.bb's own change here is an
  internal refactor — delegating to the shared push adapter, not a new
  sweep). No diagram depicts git-hook internals.

## Verdict
Doc content added (new how-to page, indexed) plus the standard
Specification.MD entry. Forwarding to QA.
