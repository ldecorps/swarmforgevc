# BL-1199 — QA pass — 20260828

**Received:** `merge_and_process documenter 294f77ef7e` (handoff
`00_20260828T004428Z_000869_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `294f77ef7eb37d671b20f6808604911a2ed9cd33`
**QA merge commit:** `57c3920ecaaf6dd3119fb199dec91174a018ea4e`

## Ancestry check (BL-336 guard)

- `git merge-base --is-ancestor c378125d1 294f77ef7e` (coder) — CONFIRMED
- `git merge-base --is-ancestor eaa72408c 294f77ef7e` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 571b1fe6d 294f77ef7e` (architect) — CONFIRMED
- `git merge-base --is-ancestor f2e516474 294f77ef7e` (hardener) — CONFIRMED

## Declined BL-1195's bundled content — a second in-flight sibling this session

Same shape as BL-592/BL-1189 earlier this session: this tip carries
BL-1195's `worktree_drift_lib.bb` (+ its tests/steps, evidence trail, and
a `ready_for_next.bb` pre-turn guard hunk) via ordinary branch ancestry.
**BL-1195's own evidence shows a `hardener-bounce`** — that ticket is
still in-flight, not ready for QA. Declined all of it via selective
checkout-HEAD/unstage-remove. Two files needed surgical (not whole-file)
resolution because they genuinely mixed both tickets' content:
- `specs/pipeline/steps/index.js` — kept the `bl1199PackSwitchBubbleTunnelSteps`
  require, dropped `bl1195WorktreeTrackedContentDriftSteps`.
- `swarmforge/scripts/test/suite-manifest.tsv` — the entire diff turned
  out to be BL-1195-only (`worktree_drift_lib_test_runner.bb`,
  `test_worktree_drift_guard.sh`); declined the whole file back to HEAD.

Ran the systematic comm-based gap check (this session's established
technique) against 1199/tunnel-tagged paths after resolution — empty,
fully covered.

## Finding, not fixed (QA does not author): BL-1199's own 3 new test files are unregistered

`suite-manifest.tsv` registers neither `named_tunnel_liveness_lib_test_runner.bb`,
`test_named_tunnel_liveness_ancillary_start.sh`, nor
`test_swarm_status_bubble_tunnel_row.sh` — checked documenter's tip
directly, not just this worktree's resolution; the omission is native to
what was delivered, not something QA's BL-1195 decline caused. Not a
`required_wiring` violation (the ticket declares only one row, for the
`swarm_status.bb::bubble-cloudflared` surface itself, confirmed present and
wired below) and the tests themselves run and pass cleanly when invoked
directly — this only means the standing sweep (`run_bb_suite.sh`) will not
pick them up automatically. Below QA's bounce threshold (no declared
contract violated, no functional defect) but real enough to record rather
than silently complete around; per role prompt QA reports quality findings
rather than fixing them.

## What BL-1199 delivers

Two of the operator intake's three wanted fixes: (1) after ancillary
start, `named_tunnel_liveness_lib.bb` asserts real `cloudflared` liveness
(pidfile + `kill -0`) for the Bubble named tunnel rather than trusting the
launcher's exit code, with one bounded relaunch attempt; (2)
`swarm_status.bb` gives the Bubble named tunnel its own
`bubble-cloudflared` row, distinct from `vscode-tunnel` (previously
ambiguous `cloudflare-tunnel` naming removed entirely), and an absent
named tunnel renders as its own status word rather than `DOWN` (not
configured is not a fault).

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1199` | `VERIFY BL-1199` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean (this ticket is pure babashka + acceptance, no TS changes) |
| Unit | `bb .../named_tunnel_liveness_lib_test_runner.bb` | ALL PASS |
| Property | `bb .../named_tunnel_liveness_lib_property_runner.bb` | ALL PROPERTIES HOLD, 200 runs |
| Unit (status surface) | `bb .../swarm_status_lib_test_runner.bb` | ok |
| Shell wiring (ancillary start) | `test_named_tunnel_liveness_ancillary_start.sh` | 3/3 PASS, including the exit-code-lied regression case |
| Shell wiring (status row) | `test_swarm_status_bubble_tunnel_row.sh` | 4/4 PASS, including "old ambiguous name gone" and "unconfigured ≠ DOWN" |
| Acceptance | `run_acceptance.sh specs/features/BL-1199-*.feature` | 3/3 PASS — confirms the step handler resolves (unlike BL-1198's bounced parcel earlier this session) |
| required_wiring (the one row) | grep `bubble-cloudflared` in `swarm_status.bb` | present, wired into the status-gathering row list (line 309) |
| Invariants (both) | shell wiring tests above | "no row derived from a different tunnel's pidfile" — bubble-tunnel-row test's two-diverging-rows cases. "liveness re-read after launch, never inferred from exit code" — ancillary-start test's exit-code-lied case. |
| Regression: previously-landed/held features intact | grep spot-check | `shift-velocity` (6 hits), `git_env_guard.sh` (on disk), `dedupePrimaryWorkingTicket` (0 hits — BL-1189 correctly still held) |
| Full unit/property/vitest suite | not re-run again this session | This ticket touches zero TS/JS production paths; Article 4.5 scopes to changed paths, all covered above at the targeted level. |

## Inventory (Article 4.4)

NONE (the manifest-registration item above is a recorded finding, not a
gate failure — no declared contract violated).

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1199 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping — ticket already
correctly pooled in `active/`, no pool flag needed. BL-1195 remains
in-flight (hardener-bounce), unaffected by this landing.

By QA.
