# BL-1747 — coder pass, 2026-09-25

## The fix

`extension/test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`:
the reachability floor's 7-kind x 2-side coverage now holds by
construction, not by chance over 40 unseeded `fc.sample` draws.

- `STDERR_KINDS` names the 7 declared stderr shapes.
- `constructedStderrFor(kind)` returns one fixed stderr string per kind
  (mirrors the shapes `stderrCaseArb` already generates: empty,
  whitespace-only, short, short-with-newline, at-limit, one-over-limit,
  well-over-limit).
- `constructedDraws()` builds the 14 (kind x index-empty) cells, `BL-9001`
  as the ticket id for all of them (ticket id plays no role in the pure
  function beyond string interpolation, already covered by the sampled
  draws' `fc.constantFrom`).
- `draws = constructedDraws().concat(fc.sample(inputArb, 40))`: the 14
  constructed cells first, the sampled draws after, still one `bb`
  process for the whole batch (BL-1663 unchanged).
- The floor now asserts BOTH `seen.has('true:'+kind)` and
  `seen.has('false:'+kind)` for every kind — stricter than the ticket's
  "either side" wording, and it holds unconditionally now that both sides
  are constructed.

Every per-draw assertion (invariant 1) is unchanged.

## Non-vacuity (BL-654), checked directly

Temporarily reverted `draws` to the bare `fc.sample(inputArb, 40)` (no
constructed cells): 3/3 runs failed the reachability floor. Restored the
fix: 5/5 runs green (the floor is no longer probabilistic — every run
passes, not "usually passes").

## Verification run before forwarding

- `npx vitest run --config vitest.properties.config.mjs test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`:
  1/1, run 5 times back to back, all green (~200-500ms each — no longer
  the old 40-draw cost, still one `bb` process).
- `node specs/pipeline/cli.js specs/features/BL-1474-the-replay-reports-why-its-commit-was-refused.feature`:
  5 of 5.

## Scope

Touched: `extension/test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
only, plus this evidence file. No production code. `backlog/standing-reds.tsv`
NOT touched — retiring BL-1747's register row is the land step's job
(qa_e2e_procedure step 4).

By coder.

## Commit blocked by the same unrelated owned red (2026-09-25, ~15:34Z)

Same as BL-1744's evidence: `git commit` refused by
`property-suite-guard` on `test/bl781LiveGrepOffender.property.test.js`
("isAllowedBabysitterMatch must exist"), pre-existing on `main`,
unrelated to this parcel. Owner: BL-1739. Already flagged to
specifier/coordinator via a priority-00 note from this worktree (sent
~15:24Z, covers both tickets — repo-wide, blocks every commit here
until resolved). This parcel's own work (test fix, non-vacuity check,
acceptance run) is complete and verified above; only the commit is
blocked.

By coder.
