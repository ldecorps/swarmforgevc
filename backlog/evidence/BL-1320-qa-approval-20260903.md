# BL-1320 — QA approval (20260903)

Received: documenter commit `992b114dc7` (D3: added `docs/index.md` link),
forwarding hardener, architect, cleaner, coder's docs-only how-to page per
human ruling ("docs now, mint the optimizer-emits-recommendation slice as
its own child"). Merged into QA.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- Acceptance (`specs/features/BL-1320-operator-step-for-adding-a-seat-to-a-bottleneck-stage.feature`):
  4/4.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1320SeatOperatorStepSteps` (line 23) — confirmed.
- Declared invariant
  (`extension/test/bl1320DocumentedStepsAreExecutedInvariants.property.test.js`):
  2/2.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated the how-to's own quoted parser refusal text (line 61,
  "must exist because parcels address the stage" → "must exist for some
  other reason") — 1 of 4 acceptance scenarios failed exactly as claimed
  ("the page quotes a refusal the parser does not actually print").
  Restored (`git diff --stat` empty), re-ran 4/4 clean.
- **Quoted refusal independently confirmed against live source**:
  `grep -n "must exist because parcels address the stage"
  swarmforge/scripts/swarmforge.sh` — matches the how-to's quoted text
  exactly (line 934).
- Doc index link confirmed:
  `docs/index.md` links `docs/how-to/BL-1320-add-or-remove-a-seat-of-a-
  bottleneck-stage.md` in the how-to list.
- `test/docsStructureRealTree.test.js`: 5/5 — no orphaned doc.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical to the
  standing-debt population from every prior pass this session. None touch
  this parcel's files.
- Property suite (`npm run test:properties`): 25 files / 14 tests red,
  same standing-debt population as the BL-1306 pass immediately prior.
  BL-1320's own property test — 2/2 green.
- `specs/pipeline/steps/bl1332SharedPathLineLeakSteps.js`'s one-line diff
  (a duplicate comment line removed) confirmed harmless — trivial cleanup
  from an earlier session commit, not this parcel's own scope, not a
  regression.
- Orphaned test processes: none after the property-suite run.

## Constraint honoured

Per the human's ruling, this parcel stops at documentation — the optimizer
(`stageDwell.ts`) still emits no recommendation text, confirmed unchanged.
The premise correction (no pre-existing "add/remove a role instance"
recommendation exists) independently re-confirmed: grepped `extension/src`
for "role instance" — zero hits.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
