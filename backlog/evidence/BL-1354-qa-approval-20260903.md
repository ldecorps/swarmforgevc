# BL-1354 — QA approval (20260903)

Received: documenter commit `1068f4095c`, forwarding hardener `12aebe9650`,
architect `fde2410c33`, cleaner (self-audit coverage-gap fix), coder's
per-sibling line-scoped `land_step_lib.bb` fix. Merged into QA clean, no
conflicts.

## Ancestry check (BL-336 discipline)

`git merge-base --is-ancestor 12aebe9650 HEAD` and
`git merge-base --is-ancestor 1068f4095c HEAD` both confirmed true against
the commit this approval cites.

## Bounce history check (BL-340)

None. Checked `main` and `origin/main` (in sync): no `bounce_history` on
this ticket's YAML on either ref, matching the evidence chain — every stage
forwarded clean, no send-backs.

## An unplanned live regression test of this exact fix

Landing BL-1337 minutes earlier hit precisely the defect this ticket fixes:
`land_step_cli.bb` `LAND_ESCALATE`d, naming `backlog/active/BL-1337-...yaml`
as shared with already-done sibling BL-1344. Diagnosed and hand-built a
tip-pure replay per the pre-BL-1354 workaround
(`backlog/evidence/BL-1337-land-success-20260903.md`). This ticket is
independently reviewed on its own merits below, not approved on the
strength of that anecdote — but it is worth recording that the failure
mode this fixes is not hypothetical or historical, it fired in this same
session.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS
  (includes the cleaner's own merge-commit-exclusion regression test).
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1354-a-shared-path-does-not-hide-a-landed-sibling.feature`):
  5/5 pass — matches the ticket's own `qa_e2e_procedure` (landed sibling
  reads landed despite a co-owner's unlanded lines; a sibling with nothing
  present never reads landed; walk-failed/empty-path/unreadable-diff each
  still fail closed).
- `required_wiring` (`specs/pipeline/steps/index.js::
  bl1354SharedPathLandedSiblingSteps`): confirmed registered at line 951 —
  the ticket's only wiring entry, deliberately so per its own notes (an
  anchor on the fixed function itself would be satisfied by this parcel's
  own diff and prove nothing, BL-1235).
- Declared invariants, independently re-verified (not trusted from
  evidence): `npx vitest run --config vitest.properties.config.mjs
  bl1354SharedPathLandedSiblingInvariants` — 3 consecutive isolated runs,
  2/2 green every time, no flake. Both invariants present and green in the
  full concurrent `test:properties` run too (see below).
- **Read the actual fix, not just the evidence chain's claims about it**
  (`swarmforge/scripts/land_step_lib.bb`): `diff-line-changes` parses a
  unified diff into per-path added/removed line sets;
  `sibling-own-line-changes` merges a sibling's OWN commits' changes,
  explicitly excluding merge commits (a merge's first-parent diff carries
  its second parent's content regardless of which ticket the merge subject
  names — exactly the misattribution mechanism this evidence file's own
  BL-1337 land hit minutes earlier); `nil` propagates on any unread commit
  rather than being scored as empty. `sibling-path-verdict` is three-valued
  (`:landed`/`:unlanded`/`:vacuous`) and fails closed whenever any input is
  falsy. `landed-siblings` drops `:vacuous` paths before scoring, so a
  sibling left with nothing still fails closed through the pre-existing
  empty-paths row (`sibling-landed?`, untouched). Traced by hand for
  nil-changes, all-vacuous, and mixed landed/vacuous cases — matches the
  docstrings' claims exactly, not merely plausible-sounding prose.
- Scope check: diffed BL-1354's full pipeline lineage
  (`git diff --name-only d1513ffa0e 1068f4095c`) — every file is either
  this ticket's own contribution (ticket evidence, how-to, index.md,
  Specification.MD, property test, feature file, step handler, fixture
  CLI, the two `.bb` production/test files) or other tickets' bookkeeping
  already independent of this parcel (BL-1337/1328/1342/1344/1345/1346/
  1351/1359, all pre-existing on origin/main by other paths). No drift.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population to every pass today (BL-1221/BL-1263/BL-1265/
  BL-1290 clusters), confirmed zero overlap with this parcel's files.
- Property suite (`npm run test:properties`, captured via a detached
  background run per the known 120s tool-timeout trap): 25 files / 14
  tests red. Used the guard's own helpers
  (`property_suite_standing_allowlist_lib.sh`): 24 of 25 allowlisted, one
  new — `bl604TrendAnalysisInvariants` invariant 1, a property
  reach-floor miss ("delta sign down drawn 8 < 10"). This parcel touches
  no trend-analysis file; re-ran in isolation, 5 consecutive runs, 4/4
  clean every time — a load-induced generator reach-floor flake under the
  full concurrent suite, not a regression (same class as yesterday's
  `bl1218RemoteControlConfigInvariants` flake on the BL-1337 pass, which is
  itself green in THIS run — confirms the "one random file" pattern is
  load-driven, not a fixed defect). This ticket's own property test
  (`bl1354SharedPathLandedSiblingInvariants`) is green in this same full
  run.
- Documentation currency: how-to `BL-1241-entangled-tip-at-the-land-step-
  has-a-reachable-remedy.md` extended in place with the whole-blob → per-
  sibling-line-scoped fix, the merge-commit attribution-inflation cause,
  the three-valued verdict, and the real-repo regression numbers (matches
  the coder's own measured numbers exactly: BL-1056/1271/1340/1341/1343
  landed, BL-1317/1323 correctly still unlanded for unrelated reasons).
  `docs/index.md` hook updated. `Specification.MD` gained a dated top
  entry. Diagram-currency checked against all four registered diagrams'
  change-triggers — none fire for an internal land-step scoring change.
  No page retired (Article 3.6: nothing this ticket touches is deprecated
  behaviour).
- Orphaned test processes: none found in this worktree before, during, or
  after (checked `pgrep -fal 'node --test|stryker|vitest'` scoped away
  from other worktrees' own legitimate concurrent runs).

## Findings

NONE.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
