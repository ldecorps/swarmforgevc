# BL-571 — QA complete review inventory, re-fix pass (Article 4.4)

- **Ticket**: BL-571 — `rotation sequential` packs are invisible to ensure's declared signal (`type: defect`, `severity: medium`, milestone M8)
- **Parcel**: `00_20260820T0?????Z_from_documenter_to_QA_for_QA` (from documenter, commit `2202626fb6`) — the re-fix for QA's own bounce
- **Verified at**: the QA merge of `2202626fb6`
- **Reviewed by**: QA, 2026-08-20
- **Prior bounces**: 2 — architect→coder `bbb14382d8` (`behavior`, closed at pass 2), and **QA→coder `affac4f828`** (`integration`, D1: stranded commit `17ae8a4822` undeclared).

## Verdict

**PASS — inventory items: NONE.**

## D1 is closed

The bounce asked for `abandoned_commits: ["17ae8a4822"]` in FLOW style (a block list with trailing comments does not parse and reads to the gate as absent — BL-935's own two-commit lesson). Delivered at `backlog/active/BL-571-…yaml:275`:

```yaml
abandoned_commits: ["17ae8a4822", "b7a6e580d7", "91ce11e180"]
```

Flow style, parses, and **`pre_qa_gate.sh` now returns `OK`** — the gate that was red at the bounce is green.

Two commits beyond the one QA named were added, and both are legitimate: `b7a6e580d7` (QA's own "Merge documenter BL-957 … BL-571 content kept reverted") and `91ce11e180` (coder's merge of QA's merge-up). Both are MERGE commits carrying no BL-571 deliverable content of their own; each only *mentions* BL-571 in its subject, which is the same subject-line matching defect that produced this bounce. Declaring them is a sound workaround, not scope creep.

## The architect's silent-drop warning — checked at merge time, and it held

An architect `note` (priority 00) warned: *"BL-571 merge silently drops BL-958 control-plane from `swarm_ensure.bb`"*. **The hazard was real**: the coder's re-fix commits `97d1d0e92d` and `b7ebb6b4d` both carried **0** `control-plane-lib/` references in `swarm_ensure.bb`, i.e. BL-958's landed work was absent there.

It was repaired downstream before reaching QA (`2202626fb6`, `02454d39c5`), and was re-measured across this merge rather than trusted:

| Point | `swarm_ensure.bb` | `swarm_status.bb` | `handoffd.bb` |
|---|---|---|---|
| QA HEAD before the merge | 9 | 2 | 2 |
| QA HEAD after the merge | **9** | **2** | **2** |

`origin/main` also reads 9/2/2, so nothing BL-958 landed was lost at any point. The ensure suite additionally proves co-existence: BL-571's case and BOTH BL-958 cases pass in the same run.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | **`pre_qa_gate.sh`** (the bounced gate) | **PASS — `OK`** |
| G2 | BL-958 control-plane not silently dropped (BL-954 class) | PASS — 9/2/2 before and after, measured |
| G3 | BL-571 content fully restored | PASS — `single-resident-rotation` ×5 in `mono_router_lib.bb`, 6 BL-571 refs in the ensure suite, step handler present and registered, property runner present |
| G4 | `qa_e2e` step 1 — `test_swarm_ensure.sh` | PASS — `ALL PASS`, exit 0, with BL-571 and both BL-958 cases green together |
| G5 | `qa_e2e` step 2 — `mono_router_lib_test_runner.bb` (carries the BL-897 bash↔Babashka parity gate) | PASS — `ok` |
| G6 | `qa_e2e` step 3 — `bl571_single_resident_rotation_property_runner.bb` | PASS — `ok (500 runs, 246 positive, 120 sequential, 254 negative)`, same non-vacuous distribution as the first pass |
| G7 | `qa_e2e` step 6 — ROTATE_HOME fence | PASS — `ready_for_next_{task,batch}.bb` byte-identical to `origin/main`, each still calling the router-ONLY `conf-rotation-router?` |
| G8 | Acceptance — `BL-571-sequential-rotation-dormant-parity.feature` | PASS — 4/4, 0 fail, exit 0 |
| G9 | Step-registry consistency after re-registration | PASS — `bl800StepRegistryScopingConsistency.property.test.js` 2/2 |
| G10 | Full unit suite | PASS (inherited) — 440/444 at `aa980e00b6`, all failures isolated-clean; no `extension/` change since, only `specs/pipeline/steps/`, covered by G9 |
| G11 | Non-vacuity (steps 4–5) | PASS — established in `BL-571-qa-bounce-20260820.md` G8/G9 against this same predicate, which is unchanged by the re-fix; not re-run |
| G12 | Docs currency | PASS — `2202626fb` documents that ensure now respects mono-rotate's dormant roles |
| G13 | Functional change present | PASS |

By QA.
