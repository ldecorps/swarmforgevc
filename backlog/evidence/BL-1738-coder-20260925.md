# BL-1738 — coder pass, 2026-09-25

## What was built

- `specs/pipeline/steps/lib/operatorRuntimeFixtureGitRoot.js` (new): the
  shared helper. `mkFixtureGitRoot(prefix, base?)` for the common case (a
  fresh mkdtemp under os.tmpdir()); `gitifyFixtureRoot(root)` to turn an
  ALREADY-built directory into the same isolated git checkout, so a handler
  that also needs `mkSocketFixtureRoot`'s short-base/socket-length
  guarantee (BL-948) composes both rather than losing either. Smoke-tested
  directly against the real `operator_runtime.bb` before wiring any handler
  (accepted the root, tick ran).
- `specs/pipeline/steps/bl1738OperatorRuntimeFixtureGitRootSteps.js` (new,
  the pinned handler, same parcel as the feature per BL-233/BL-1371):
  scenario 01 drives the real helper + real `operator_runtime.bb`;
  scenario 02 is a static read of each of the fourteen named handlers,
  asserting its source contains `require('./lib/operatorRuntimeFixtureGitRoot')`
  (the KNOWN_HANDLERS set is the same fourteen filenames the ticket's own
  census table names, per the Scenario Outline KNOWN_VALUES rule).
- All fourteen handlers converted, each edited to touch ONLY the root(s)
  actually passed to `operator_runtime.bb` as its positional project-root
  arg — verified per file by grepping every call site before editing, never
  a blanket swap of every `mkdtempSync` in a file (several files build
  OTHER roots for sweep targets, fake-`/proc` simulation, etc. that are
  never passed to that CLI and were left as bare mkdtemp, unchanged):
  - Plain single-root swap (`mkTmp()` -> `mkFixtureGitRoot(prefix)`):
    operatorLongtermMemorySteps.js, answerPairingAcrossThreadsSteps.js,
    bl466AgentQuestionsAsTelegramPollsSteps.js,
    gh26RoleQuestionUndeliverableClearsMarkerSteps.js.
  - The "aps-operator-" trio (each carries its own deliberately-duplicated
    `mkRosterFixture`, per that file's own documented "no shared lifecycle
    worth coupling" posture — same edit applied to each copy):
    operatorAutoHibernateSteps.js, operatorSeedRaceLaunchGraceSteps.js,
    operatorSelfGenProvenanceSteps.js.
  - Socket-composition (`gitifyFixtureRoot(mkSocketFixtureRoot(prefix))`,
    preserving BL-948's short-base/socket-length guarantee):
    alwaysOnOperatorPresenceSteps.js (only `mkAttendFixture`'s root, which
    becomes `ctx.runtimeTarget`; `mkThreadRoot`'s root goes to
    `operator_reply.bb`, which check-root does not gate, left alone),
    controlLossIsNotAgentDeathSteps.js (only `mkRuntimeFixture`'s root;
    `mkRoleLifecycleFixture`'s root goes to the bash `role_lifecycle.sh`,
    not gated, left alone).
  - Single-function reroute inside a file that keeps a separate bare `mkTmp`
    for a non-gated root: noInboundMessageIsEverLostSteps.js
    (`mkRuntimeFixture` only; `ctx.bridgeTarget`, built by the same generic
    `mkTmp`, goes only to an in-process `bridgeServer()` call, never a
    spawned CLI, left alone).
  - Surgical single-field edits in multi-root files (verified via grep which
    field reaches `OPERATOR_RUNTIME`/`execFileSync` before touching
    anything): bl413StaleSandboxSweepSteps.js (`ctx.projectRoot`, not
    `ctx.sweptRoot`/`ctx.outsideRoot`), bl458AcceptanceFixtureProcessLeakSteps.js
    (`ctx.reapProjectRoot`, not `ctx.root`/`ctx.reapRoot`),
    bl460TmpSweepsBoundDeletesSteps.js (`mkFixture`'s `projectRoot`, not
    `sweptRoot`), bl877PortableProcessLivenessSteps.js (`ctx.projectRoot`,
    not `ctx.sandboxRoot`/`ctx.fixtureReapRoot`/the fake-proc/holder dirs).
  - humanInTheLoopClosedSteps.js (BL-325, out_of_scope's "and BL-325's
    reference" item): fixed the actual scope bug (a real top-level
    `require('./lib/socketFixtureRoot')` — `mkRuntimeFixture` called
    `mkSocketFixtureRoot` but that identifier was only ever bound inside
    OPERATOR_DECIDE_STUB's own STRING content, a different script's source
    text, never this module's real scope) **and** gitified the root
    (`gitifyFixtureRoot(mkSocketFixtureRoot(...))`) — the ticket's own text
    said "its roots already `git init`"; checked directly against
    `lib/socketFixtureRoot.js`'s real source and that is false, it never
    did. Left uncorrected, the feature stayed red with `REFUSED
    project-root ...: not inside a git checkout` after the scope fix alone
    (confirmed: ran the feature between the two edits). Both fixes are
    "how BL-325's handler builds its fixture root", squarely the ticket's
    own named exception, and touch no scenario/Examples/narrative text.
- `extension/test/step-handler-tmp-root-census.txt`: removed the 7 entries
  (answerPairingAcrossThreadsSteps.js, bl466AgentQuestionsAsTelegramPollsSteps.js,
  gh26RoleQuestionUndeliverableClearsMarkerSteps.js, operatorAutoHibernateSteps.js,
  operatorLongtermMemorySteps.js, operatorSeedRaceLaunchGraceSteps.js,
  operatorSelfGenProvenanceSteps.js) that no longer call a raw
  `fs.mkdtempSync` anywhere in the file (the standing BL-1636 guard,
  `extension/test/stepHandlerTmpRootGuard.test.js`, failed on exactly these
  7 as stale census entries until this edit — required, not optional, to
  get `npm test` green). Every other converted file still calls a bare
  `fs.mkdtempSync` for a DIFFERENT root and correctly stays in the census.

## Residual failures found, NOT fixed here (ticket's own established policy)

The ticket's own text already anticipated exactly this shape of finding for
BL-458: "BL-458's scenario 05 fails one layer down with `fixture bridge
never came up`... If it stays red after the roots are fixed, note the
specifier rather than widening this parcel." Applying that same policy to
what full feature runs surfaced (below) once the git-checkout refusal
stopped masking each feature's later logic:

- **BL-458 scenario 05** ("an interrupted step file still tears down its
  detached process tree"): still red, `Error: fixture bridge never came up`
  — exactly the ticket's own prediction. Uses `ctx.root`
  (aps-fixture-reaper-abnormal-exit-), deliberately NOT converted (never
  passed to operator_runtime.bb). Not fixed here, per the ticket's own
  instruction.
- **BL-325 scenarios 2 and 6** ("A reply typed into the item's topic
  reaches a consumer that acts on it", "The whole loop closes without the
  human being bypassed"): both now fail with `expected operator-decide.js
  invoked with [...], got []` — a DIFFERENT, pre-existing defect than the
  scope bug this ticket named. `OPERATOR_DECIDE_STUB` (the fixture's own
  written-out script) contains a DEAD `require('./lib/socketFixtureRoot')`
  line inside the string it writes to `<root>/extension/out/tools/operator-decide.js`;
  when that stub is genuinely spawned (`execFileSync('node', [OPERATOR_DECIDE_JS, ...])`),
  the require throws `MODULE_NOT_FOUND` (no `./lib/` exists relative to
  that written location) before ever reaching the `appendFileSync` that
  would populate `consumed.log` — confirmed directly (isolated repro:
  writing the exact stub content to a scratch dir and running it with
  `node` reproduces the identical `MODULE_NOT_FOUND` stack). This was
  invisible before today: EVERY one of BL-325's 4 originally-failing
  scenarios threw the earlier `mkSocketFixtureRoot is not defined`
  ReferenceError first, in `mkRuntimeFixture`, before ever reaching this
  code path. Not this ticket's scope (a dead require inside a fixture's own
  stub script, unrelated to fixture-root git-checkout-ness); the specifier
  should mint separately.
- **BL-877 scenario 7** ("a host with no working liveness facility says so
  instead of reporting nothing live"): still red, `expected runtime.log to
  record that liveness could not be determined` — the sweep's log shows
  ordinary scan/reap lines instead. Same masking shape (all 7 of BL-877's
  scenarios originally failed on the git-checkout refusal, so this gap was
  never previously observable). Not investigated further — outside a
  git-checkout-ness fix, the specifier should mint separately.

A `note` (priority 00) to the specifier and coordinator naming these three
findings accompanies this parcel's forward, so they are not lost.

## Verification run before forwarding

- Shared helper smoke test (standalone, against the real `operator_runtime.bb`):
  accepted the fixture root, tick ran.
- `node specs/pipeline/cli.js <feature>` for ALL FIFTEEN features (the
  fourteen + BL-1738's own): BL-1738 15/15; BL-282 5/5; BL-307 7/7; BL-310
  4/4; BL-318 4/4; BL-325 5/7 (2 pre-existing, unrelated - above); BL-354
  5/5; BL-359 6/6; BL-368 3/3; BL-369 6/6; BL-413 6/6; BL-458 5/6 (1
  pre-existing, unrelated, the ticket's own prediction - above); BL-460
  18/18; BL-466 2/2; BL-877 6/7 (1 pre-existing, unrelated - above); GH-26
  5/5. Zero `not inside a git checkout` lines anywhere across all fifteen
  runs (qa_e2e step 2's own bar, met in full).
- `npm test` (extension/, compile + unit lane), run TWICE (contention):
  both runs 637 test files / 10868 tests passed. Both runs exited 1 purely
  from the suite's own duration-budget guard tripping on
  `emitLifecycleSnapshotCli.test.js`/`telegramFrontDeskBotCli.test.js` under
  measured host load average 19.19 on 20 cores — files this parcel never
  touches, the same "the pool, not the code" contention shape
  BL-1717-QA-20260925.md already recorded for these exact two files earlier
  today. Not a correctness regression.
- `extension/test/stepHandlerTmpRootGuard.test.js` alone: 4/4 (required the
  census edit above; failed before it, green after).

## Scope

Touched: `specs/pipeline/steps/lib/operatorRuntimeFixtureGitRoot.js` (new),
`specs/pipeline/steps/bl1738OperatorRuntimeFixtureGitRootSteps.js` (new,
auto-discovered per BL-1371, no index.js edit needed), the fourteen named
handlers + humanInTheLoopClosedSteps.js (BL-325's own reference fix, named
in-scope by the ticket), `extension/test/step-handler-tmp-root-census.txt`
(required by the standing BL-1636 guard, a direct consequence of these
exact edits), this evidence file. `backlog/standing-reds.tsv` NOT touched
(coder.prompt, BL-1663 - the land retires those rows). No feature file's
Scenario/Examples/narrative text changed anywhere. Nothing else in this
worktree was staged for this commit - the unrelated untracked leftovers
present in this worktree (BL-1666/BL-1652) are not this ticket's and are
left as found.

By coder.
