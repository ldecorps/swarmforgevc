# BL-1379 — documenter pass (2026-09-04)

## Received
`b4aa6d9115` (hardener, degraded Babashka fallback recorded, gated on the
lib test runner + property runner (500 runs) + real-daemon wiring test +
`test_expedite_cli.sh` + acceptance 9/9; confirmed BL-567's superseded
scenario 18 correctly retired — deleted, not reworded).

## Merge fix
The organic merge auto-combined a duplicate `abandoned_commits:` YAML key
on `backlog/active/BL-1381-...yaml` (hardener's own bookkeeping entry near
the top of the file, my own comprehensive list further down, in different
positions so git's line-based merge saw no conflict) — removed the
superseded duplicate, kept the comprehensive list (already a superset,
including hardener's single sha).

## What changed, operator-visible
A daemon sweep now reverses an expedition's park automatically once the
expedition's own commit lands, restoring each parked ticket to
`backlog/active/` (never `paused/` — the human's literal ruling, "restore
to the prior folder") marked `status: blocked` / `freshness_check:
required` pending an Article 3.6 deprecator check. This directly
contradicts and supersedes text in both existing expeditor docs claiming
parked tickets are never restored — real doc corrections, not just
additions.

## Doc-domain review
- `docs/how-to/BL-567-expedite-one-ticket-with-the-swarm-stopped.md`:
  corrected the "reported, never re-promoted" claim, the "does not
  restore what it parked" claim, and added context to the OUTSTANDING
  block explaining the parked-tickets item is now temporary.
- `docs/reference/BL-567-expeditor-manual.md`: corrected the "no
  re-promotion of what it parked" bullet, extended `park-record.json`'s
  documented fields (three new keys), and added a full "Park reversal"
  reference section (decision table, freshness-mark field table,
  `unpark` subcommand, `unpark-done.json`).
- Caught and fixed my own first-draft error: initially wrote the
  restoration destination as `backlog/paused/` (the ruling's
  *recommended* option), re-read the ticket's actual `human_ruling:`
  ("restore to the prior folder") and the code (`:origin-folder
  "active"`, `backlog-schema.md`'s own field doc) and corrected both docs
  to `backlog/active/` before committing.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: refreshed both BL-567 summary lines.
- Diagram registry checked — no diagram depicts expeditor internals.

## Verdict
Doc content corrected in both owning pages (not NONE — real inaccuracies,
not just additions) plus the standard Specification.MD/index.md entries.
Forwarding to QA.
