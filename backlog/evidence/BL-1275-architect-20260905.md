# BL-1275 architect review — clean sweep, forwarding

## Reviewed
Merged cleaner's `4fd6b5ad30`. Read the ticket (two declared invariants,
required_wiring, explicit not-in-scope list), coder and cleaner evidence,
and the full diff of `check_property_suite_drift.sh`.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1275PropertyGuardKeepsRefusalEvidenceSteps.js ../extension/test/bl1275RefusalEvidenceInvariants.property.test.js
```
PASSED, no forbidden edges. (Bash tooling script itself is outside the
TS dependency-cruiser's scope; no host/webview/tmux boundary applies to a
git-hook helper script.)

## Logical coupling (BL-255)
`co-change-report.js` over the changed script flags the neighbouring
commit-guard chain files (pre-commit, pre-merge-commit,
check_commit_size.sh, check_ticket_deletion.sh,
test_property_suite_drift_guard.sh, etc.) at high co-change counts — all
expected, legitimate coupling: these files co-evolve as part of the same
guard chain and none of them changed in this diff. No new coupling
introduced.

## Invariants — verified directly in the diff
1. **A refusal retains this run's full output and names where.**
   `retain_refusal_log` copies `$SUITE_OUT_FILE` (still populated at the
   point of the refusal branch — its `rm -f` moved from the inline read
   site onto the EXIT trap) into a per-refusal, monotonically-indexed file
   under `.swarmforge/property-guard-refusals/`, and the refusal path
   echoes `property-suite-guard: refusal output retained at $RETAINED`.
   `next_refusal_index` derives the index from existing filenames rather
   than a clock — correct given `date +%N` isn't available on the bash 3.2
   target, and same-second refusals still order correctly.
2. **Nothing retained is ever commitable.** `retain_refusal_log` writes a
   self-ignoring `.gitignore` (`*`) into the retention directory on first
   use, so invariant 2 holds by construction regardless of whether the
   outer checkout's own `.swarmforge/` ignore rule exists. Verified this is
   the ONLY write path into that directory — no other code touches
   `REFUSAL_LOG_DIR_REL`.
   Confirmed the temp-file lifecycle change is safe: `retain_refusal_log`
   does a `cp` (never a move), so the original `$SUITE_OUT_FILE` the caller
   still owns is untouched, and `cleanup_suite_out` (now on the EXIT trap,
   covering `on_interrupt`'s explicit exit too) still removes it exactly
   once, on every path.
3. **Retention is bounded.** `prune_refusal_logs` counts via one glob pass,
   deletes the oldest excess via a second, no array — the coder's own
   self-audit fix (`0ce8a2e7e0`) correctly avoids the BL-801
   empty-array-under-`set -u` shape (an array that can legitimately be
   empty on the common, sub-bound path). Verified the fix directly: ran
   `prune_refusal_logs` under `bash -uo pipefail` against an empty
   directory myself — no error, returns 0.

## required_wiring
`specs/pipeline/steps/bl1275PropertyGuardKeepsRefusalEvidenceSteps.js`
exports `registerSteps`; confirmed 4/4 acceptance scenarios execute with no
"no step handler matched" errors.

## Scope check
Only `check_property_suite_drift.sh` changed production-side. Confirmed
against the ticket's explicit not-in-scope list: no change to which reds
refuse a commit (BL-1175's allowlist untouched), no change to the reds
themselves, no change to exit codes (`STATUS`, canary path, `exit 0`/`exit
1` sites all unmoved).

## Independent re-verification
- `bash swarmforge/scripts/test/test_bl1275_refusal_evidence_retained.sh`
  — **13/13 PASS**.
- `node specs/pipeline/cli.js specs/features/BL-1275-...feature` — **4/4
  pass**.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1275RefusalEvidenceInvariants.property.test.js` — **2/2 pass**.
- `node specs/pipeline/cli.js specs/features/BL-1202*.feature` (the
  feature owning this script's exit paths, the riskiest edit here) —
  **4/4 pass**, no regression.

## Pre-existing reds noted by coder/cleaner (not this parcel's)
BL-570 (stale `pre-commit` assertion since BL-1252) and
`test_run_commit_guards.sh` case 01 (third hand-written guard-list copy,
same class as BL-1398/BL-1401) — grepped `backlog/active/`,
`backlog/paused/`, and root intakes: no ticket exists yet for either. Both
were already routed correctly as priority-`00` notes to the specifier by
the coder (spec-gap path, not a parcel) rather than fixed here or ticketed
by a pipeline role. Not re-raising; already surfaced.

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener.
