# BL-1745 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

Reviewed coder's minimal fix (985a262a87): each of the three affected
`test(...)` calls (bl1670's sole test, bl1108 invariant 1, bl1565's
reverse-hop invariant) gains the shared `SUBPROCESS_HEAVY_TIMEOUT_MS`
(240000ms, BL-932's one constant, already used by bl1713/bl760/bl787/
bl797/onboarderLauncherPidGuard) as its third argument. No assertion,
shape, generator, or reach-floor changed — exactly the "direction, not
mandate" the ticket offered as an alternative to BL-1725's fixture-rewrite
shape, chosen because it satisfies the ticket's FIRM constraint (every
assertion/shape/reach-floor unchanged) with the smallest diff.

Checks run:
- `node out/tools/dependency-gate.js` (from `extension/`) on the three
  changed files: PASSED, no forbidden edges.
- `node out/tools/co-change-report.js` on the same three files: every
  reported co-change is frequency <= 2, below the default threshold (3)
  — nothing flagged as coupling.
- `npx vitest run --config vitest.properties.config.mjs` on all three
  files together: 6/6, 5.1s/6.5s/6.4s for the three previously-timing-out
  invariants — comfortably under both the old 20s and new 240s ceilings.
- `node specs/pipeline/cli.js
  specs/features/BL-1650-the-land-step-lands-a-pure-evidence-closed-owner-stray-itself.feature`:
  8/8.
- `npm run compile` (extension): clean.
- Scope check: `git diff main...HEAD --name-only` for `extension/test`
  and `specs/pipeline` names exactly the three Scope files (plus two
  unrelated files still on this branch tip from BL-1739, a separate
  in-flight ticket not yet landed to main — no scope leakage from this
  parcel).

No production code touched. No defect found.
