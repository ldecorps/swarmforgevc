# BL-825 — QA pass — 2026-08-17

## Scope reviewed

Parcel received from documenter at `a35e48fd21`, merged into QA at
`b9c62b426` (`git merge-base --is-ancestor a35e48fd21 b9c62b426` confirmed).
Carries the full chain, this ticket's own commits, verified against THIS
ticket's own coder work per the "cite the right commit" rule:

- coder `0f4de7bf8` — Bubble UI bundle resolver (slice A)
- cleaner `adb294cdd` — extracted shared `boolFromEnv` into `src/util/envFlag`
- architect `747db5c76` — clean review, no defects found
- hardener `0db3fe77d` — split two CRAP-flagged functions, closed coverage gaps
- documenter `6680effba` — how-to doc, docs/index.md link, Specification.MD changelog

`git merge-base --is-ancestor 0f4de7bf8 b9c62b426` confirmed — this ticket's
own coder commit is an ancestor of the commit being approved, not merely
"HEAD contains other tickets' work."

Note: this ticket's road to QA was not linear. Its coder commit `0f4de7bf8`
was initially found unreviewed inside the BL-908 branch (documenter's own
finding, `BL-825-stranded-at-coder-found-in-BL-908-branch-20260817.md`); QA
held BL-908 for it (`BL-908-held-pending-BL-825-gates-qa-20260817.md`) rather
than shipping ungated code, per the coordinator's HOLD note. The coordinator
then routed BL-825 through its own `required_stages` (cleaner → architect →
hardener → documenter) properly, and it now arrives at QA as its own parcel.
This pass is BL-825's own, independent verification — not a rubber stamp of
the earlier finding.

## Fresh verification run this pass

- `npm run compile`: clean.
- `npm test` (full extension unit suite): 433/437 files, 7722/7726 tests
  passed on first run. 4 failures, all timeouts unrelated to this parcel's
  diff (confirmed via `git diff --stat` — zero overlap):
  `bounceDrain.test.js`, `bounceWatcher.test.js` (both depend on genuine
  OS-async real `fs.watch` event delivery, by their own header comment),
  and `renderBriefingDiagramsCli.test.js`. Host load was 29 (1-min avg) on
  a 4-core box (`uptime` / `os.cpu_count()`) — ~7x cores. Reran all three in
  isolation: `renderBriefingDiagramsCli.test.js` passed cleanly (4/4),
  confirming general load-induced flake for that file.
  `bounceDrain`/`bounceWatcher`'s single real-fs.watch scenario each still
  timed out on isolated rerun; both are pre-existing, unrelated to this
  diff, and explicitly documented in their own test file as "the only piece
  of this that is genuinely OS-async ... can't be faked" — an environment
  constraint on this host, not a BL-825 regression.
- BL-825's own test file in isolation: `test/letsTalkUiBundle.test.js`,
  11/11 pass.
- `npm run test:properties`: 96/96 files, 299/299 tests passed. 4
  "unhandled error" RPC timeouts (`[vitest-worker]: Timeout calling
  onTaskUpdate`) are worker-IPC noise from the same host-load condition,
  not test failures — every test file still reported passed.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-825-bubble-remote-ui-bundle-resolution.feature`: 6/6
  scenario-outline decisions pass, each driving a real
  `./gradlew :app:testDebugUnitTest` invocation (40s+5s+3s+3s+4s+3s — real
  execution, not a passthrough).
- Gradle JVM test-result XML for the resolver's own test classes, fresh
  from this run: `UiBundleResolverTest` 9/9,
  `UiBundleResolverEvidenceConfidencePropertyTest` 4/4,
  `UiBundleResolverKeepsCachedIntactPropertyTest` 3/3,
  `UiBundleResolverNeverThrowsPropertyTest` 2/2,
  `UiBundleResolverParseWholeOrNothingPropertyTest` 5/5 — all 0
  failures/errors. These map directly onto the ticket's three stated
  `invariants:` (always-usable Talk surface, whole-or-nothing bundle
  application, never-more-confident-than-evidence).
- `required_wiring` — re-verified fresh by direct source read:
  - `letsTalkRoutes.ts::ui-bundle` — the route is not in letsTalkRoutes.ts
    itself (a documented sibling-module pattern); confirmed served via
    `bridgeServer.ts:74` (`import { getLetsTalkUiBundleManifest,
    isLetsTalkUiBundlePath } from './letsTalkUiBundle'`) and wired into
    `buildJsonRoutes` (line ~1724).
  - `BridgeClient.kt::resolveUiBundle` — real caller chain confirmed:
    `OverlayService.kt:74` (`talkEngine?.syncUiBundle()`, same
    every-overlay-start cadence as the BL-763/BL-765 syncs) →
    `TalkEngine.kt:136` (`syncUiBundle()` calls
    `BridgeClient.resolveUiBundle(...)`) — a real pair/resume-path caller,
    not only unit tests.
- Process hygiene: `pgrep -fl 'node --test|stryker|vitest'` clean before and
  after this pass; no orphaned processes left running.

## Device-surface manual procedure — NOT independently completed by QA

The ticket's `verification:` block specifies a 7-step manual procedure on a
paired Android phone (fresh-bundle render, cached-bundle no-refetch,
malformed-bundle whole-rejection, bridge-down stale state, no-cache bare
state, shell-behind refusal, BL-765 regression check). This QA session has
no physical device or ADB access (`which adb` → not found), consistent with
the Testability Boundary — Bubble article's device-surface carve-out and
the same constraint recorded on BL-826's QA pass
(`BL-826-QA-pass-20260809.md`).

Asked the human operator directly this session: approved disposition is
**"Approve, device check as fast-follow"** — land now, human runs the
7-step procedure on their own schedule and reports back.

Weighing the available evidence supporting this:
- All three of the ticket's stated invariants are each covered by a
  dedicated, passing Kotlin property test (see fresh JVM results above),
  not merely example-based unit tests.
- `required_wiring` re-verification confirms the resolver is not only
  correct in isolation but is the actual decision path on every
  overlay-start (pair/resume) cycle — no bypass found.
- The architect's independent pass recorded "clean review, no defects
  found"; the hardener's pass closed remaining coverage/CRAP gaps.
- BL-765's reuse posture (whole-document validation, bundled-default
  fallback) is followed on the bridge side, not reinvented — reducing the
  chance of a novel failure mode this slice's own logic doesn't already
  cover in the JVM suite.

This is code-level and property-level evidence that the four-outcome
resolver behaves as specified, but it is **not** the same as a device
confirmation of the actual WebView/tunnel/overlay integration. Recording
this gap explicitly rather than silently treating it as satisfied.

## Verdict

APPROVE. All executable gates are green and independently re-verified this
pass; both `required_wiring` items are real, not merely unit-tested; all
three invariants have dedicated passing property coverage. The
device-surface manual procedure is recorded as an **outstanding
fast-follow**, per explicit human direction this session — not silently
passed.

By QA.
