# BL-1739 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Fourth bounce cycle on this ticket (two architect bounces, four commits in
`bounce_history`), now resolved. Scope:
`specs/pipeline/steps/bl611BabysitterdLifecycleSteps.js` only — plain Node
acceptance step handler, no production `src`/`out`/`.bb` code touched
(CRAP/Stryker not applicable). The `specs/features/BL-611-...feature`
change visible against an earlier session base was confirmed already on
`main` (the specifier's own mid-flight ruling, `git diff --stat
main...HEAD -- specs/features/BL-611*` empty) — this parcel edits no
scenario/Examples/narrative, matching the FIRM constraint.

Checks run (host load ~15-24 on 20 cores):
- `node specs/pipeline/cli.js specs/features/BL-611-deterministic-babysitterd-managed-by-swarm-lifecycle.feature`:
  27/27 ok.
- `node specs/pipeline/cli.js specs/features/BL-1129-babysitter-rotate-not-honored-skips-standing.feature`:
  2/2 ok.
- `git diff --stat main...HEAD -- specs/pipeline/steps/bl611BabysitterdLifecycleSteps.js`:
  the one Scope file only.
- `backlog/standing-reds.tsv`: row present, owner BL-1739, not yet
  retired (expected pre-land).

Independent non-vacuity checks (three separate hand-mutations, none
repeating the architect's own two prior bounce repros exactly):
1. Resurrected `swarmforge/scripts/babysitter_lib.bb` (a `RETIRED_FILE_PATHS`
   entry) with dummy content — scenario 15 correctly failed
   ("the agent-based babysitter is gone..."). Removed.
2. Appended a retired-name mention (`launch_babysitter.sh`) to a genuine
   live-code file (`swarmforge/scripts/swarm_status.bb`, not a test/docs/
   backlog path) — scenario 15 correctly failed. Reverted via `git
   checkout --` (the file was clean beforehand), confirmed clean after.
3. Reverted BOTH of the two fixture-topology fixes in place (scenario 7's
   `'rotation-router?': true` and scenario 14's `agedClaim['abandoned?'] =
   true`) — each independently reproduced the EXACT original bug reports
   from the ticket's own description (`expected a CRIT rotate-unhonored
   finding, got nil`; `expected the idle-owner aged claim to contribute to
   a starved finding`). Restored the file byte-for-byte between each
   mutation (`git status --short` clean after each); re-ran the full
   feature green (27/27) after the final restore.

Fixture hygiene: `pgrep -fl babysitterd` after scenario 27's real
start/stop/ensure script runs shows only three pre-existing live-swarm
daemons (12483s-21748s elapsed, well before this pass) under real repo
roots — no leaked fixture daemon. `git status --short` clean of anything
this pass's runs could leak.

Not run: `npm run test:properties` (2026-09-17 rule — this parcel touches
no `*.property.test.js`) and Stryker/CRAP (no `src`/`out` file in Scope).

By hardender.
