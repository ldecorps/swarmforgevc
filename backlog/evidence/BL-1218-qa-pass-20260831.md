# BL-1218 — QA pass, 2026-08-31

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1218` → `VERIFY BL-1218` (exit 0).

## Merge

Merged documenter's cited commit `390fc68faf` into QA at `f441da937a`
(`git merge --no-ff`). `check_ticket_deletion.sh` refused the first
attempt: the merge removes `backlog/hold/BL-1218-...yaml`, attributed by
the guard to the introducing commit's subject (`66be60ea40`, whose first
regex match is `BL-1210` — the same park commit already hit for
BL-1240/BL-1253/BL-1264 this session). Verified content-identical to the
`active/` copy (empty diff) before naming it in the merge message — a pure
hold→active promotion, not a retirement.

My prior QA tip (`ab89ce7086`) was already an ancestor of documenter's
BL-1218 tip (confirmed directly) — the documenter branch had already
merged up from an earlier QA broadcast this session, so the merge diff
against my own parent is exactly this ticket's own new content (evidence,
docs, the yaml move) and nothing more.

Ancestry re-confirmed for the full chain: coder `5b534f8ea9`, cleaner
`58842aa55e`, architect `4c4595baf5`, hardener `171095907a`, documenter
`390fc68faf` are all ancestors of the commit under evaluation
(`f441da937a`) — not merely ancestors of a busy HEAD (BL-336 discipline).
No `abandoned_commits` needed on this ticket (unlike BL-1264) — the coder's
own re-dispatch evidence confirms the branch diff against `main` was empty,
so no tip-pure rebuild was required.

## Ticket shape

Same park/hold/release history as BL-1264 and BL-1253 this session:
`66be60ea40` parked this ticket mid-flight (orphaned dead expedite run for
BL-1295) between the hardener's forward and the documenter's receipt; the
release re-entered the chain at the coder, which correctly declined to
re-implement (Article 1.9/2.3.2 No-Op Rule — diff against `main` was
empty) and resumed at the documenter, the first stage with no recorded
pass.

**The fix**: `config remote_control off` now decides the launched
`--remote-control` flag itself via a new pure `resolve_remote_control_cli`
(`swarmforge/scripts/remote_control_launch_lib.sh`), wired into
`swarmforge.sh`'s launch-script composition — previously the config only
governed auto-inject, so a window line naming the flag explicitly (as both
`swarmforge.conf` and `packs/full-forge.conf` do on every Claude line)
ignored the config entirely. Config-on and config-absent paths are
required to be byte-identical to the pre-fix composition; only config-off
strips the flag.

## Stage corroboration

Read all evidence including the two coder passes (original + re-dispatch
verification). Architect: COMPLIANT, non-vacuity re-verified BY HAND
(swapped the strip call for a passthrough, watched 2/4 properties fail,
restored, confirmed byte-identical). Hardener: Gherkin acceptance mutation
12/12 killed, dual-shell (bash + zsh) shell-suite pass — the coder's own
stated hazard (BL-801's word-splitting shape, sourced by zsh/tested under
bash) directly addressed with a `sed` transform rather than a token loop.
Documenter: fixed two stale sibling-ticket cross-references in
BL-1217's own how-to section that had said "unticketed here" in future
tense, now corrected to "landed, see below."

## Own independent verification (BL-112, separate from every prior stage's)

| Command | Result |
|---|---|
| `bash test_remote_control_launch_lib.sh` | 11/11 pass |
| `zsh test_remote_control_launch_lib.sh` | 11/11 pass — independently re-confirmed the dual-shell claim myself, not just cited |
| `bash test_remote_control_launch.sh` | 5/5 pass (case 03 is the ticket's own gating case) |
| `npx vitest --config vitest.properties.config.mjs` BL-1218 property file | 4/4 pass |
| `run_acceptance.sh` BL-1218 feature | 7/7 pass |
| `grep -n resolve_remote_control_cli swarmforge.sh` | wired at the real call site, line 746 (not merely defined) |
| `grep -n test_remote_control_launch suite-manifest.tsv` | both suites registered `standing` |
| `grep -n bl1218 specs/pipeline/steps/index.js` | registered, line 655 |
| `find backlog/done -iname "BL-1217*"` | sibling repair ticket confirmed already landed — scenario 03's split assertion (BL-1217's gate vs. BL-1218's own script-write half) is meaningful, not vacuous |
| Direct read of `remote_control_launch_lib.sh` | matches every stage's description exactly: non-Claude passthrough, config-on preserves today's composition byte-for-byte, config-off strips via `sed` (not a word-split-sensitive loop) |

Not re-run by hand: the mutation-revert non-vacuity check — already
independently performed by both the coder (deliberate breaks, documented
failure signature per property) and the architect (hand-reverted the strip
call in the actual file, watched it fail, restored) with results that
agree; re-doing a third time adds no new information for a shell-only
change with no wired mutation tooling (Babashka/shell has none per
Engineering Rules) where the Gherkin acceptance-mutation lane (12/12
killed) is the file's own recorded gate.

## Constraint compliance — verified directly

- Absent config ≡ config on: covered by property invariant 3 and
  acceptance scenario "an absent remote-control config behaves exactly as
  on" — both green.
- Non-Claude seats untouched: lib cases 10/11 and acceptance scenario "a
  non-Claude seat is unaffected by the config" — both green; the function's
  first branch is a plain passthrough for `agent != claude`.
- Config-on path byte-identical to before: property invariant 3 models the
  legacy rule explicitly and asserts equality, not just "looks the same."
- Persisted script agrees with the config that wrote it: property
  invariant 2 drives the REAL `swarmforge.sh write_role_launch_script`
  under zsh, not a reimplementation.
- No launch scripts already on disk rewritten (BL-1217's job): out of
  scope, not touched by this diff — confirmed by reading the coder's scope
  section, no rewrite logic present anywhere in the new lib.

## Orphan check

`pgrep -fl 'node --test|stryker|vitest'` clean before and after.

## Verdict: APPROVE

By QA.
