# BL-1409 — hardender review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Ran the full checklist: BL-1409's acceptance feature (4/5 - see below),
BL-570's feature (0/7 -> 7/7, confirmed the ticket's own headline claim),
the shell suite (case 07 pass; case 11 fail - see below), the coder's
property test (3/3 after my addition), and all 18 whole-tree standing
guards (183 tests).

BL-1409's own scenario 04 ("the drift-guard shell suite runs / every case
passes") fails at case 11, NOT case 07 (this ticket's own target, which
passes cleanly). Confirmed this is a separate, already-owned, deliberately
sequenced red: `backlog/standing-reds.tsv` carries BL-1448's own row for
case 11, whose text says verbatim "hidden behind case 07 until BL-1409
lands" - BL-1448 `depends_on: [BL-1409]`. Not a defect in this parcel;
the ticket's own notes pre-acknowledge it ("escalated via a priority-00
unowned-red note, not fixed here"). Verified directly: `bash
test_property_suite_drift_guard.sh` shows case 07 PASS, case 11 FAIL, no
other case affected.

Real coverage gap + a real mislabeling bug found via hand-mutation
(propertyGuardIsWired's `!exists(hookPath)` branch): the property test's
`seamArb` 'absent' hookShape always WRITES a hook file (one that just
doesn't invoke the runner) - the hook FILE literally missing from disk is
a separate code path nothing exercised anywhere (acceptance scenario 03
only names two broken-hop examples, neither this one). Confirmed by
deleting the whole `!exists` branch: every existing test stayed green.

While writing the closing test, found the branch itself was also wrong:
it reported `missing: runnerRel` (the RUNNER's name) for a HOOK FILE that
does not exist - the `reason` string correctly named the hook, `missing`
did not, breaking the parallel structure the other two failure branches
follow (each names the hop that is actually missing) and would mislead a
reader debugging "why is this failing" into the wrong file. Fixed to
`path.basename(hookRel)`. Closed with a test building an empty fixture
root (no hook file at all) and asserting `missing === 'pre-commit'`;
confirmed it fails against the pre-fix code (reporting the runner's path)
and passes against the fix. Mutant/bug both confirmed via hand-mutation
before committing the fix (4968e6960c).

Process note: killed two redundant concurrent `git commit` invocations of
my own making (each running the full `npm run test:properties` pre-commit
guard, ~4+ minutes elapsed each, doubling host load) after wrongly
concluding the first had died with no output; confirmed via `ps` before
killing, per the standing "confirm with ps before assuming a background
command died" discipline. Retried once, cleanly, under an explicit
`timeout 300` wrapper - landed on the next attempt.

By hardender.
