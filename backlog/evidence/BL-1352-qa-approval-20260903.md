# BL-1352 — QA approval (20260903)

Received: documenter commit `127e412876` (doc-only, Specification.MD entry
+ how-to section), forwarding hardener, architect, cleaner, coder's fix
(bounced once by cleaner for a flaky property-test generator, reworked and
re-verified 15 consecutive clean runs). Merged into QA.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/bl1352_escalation_transport_test_runner.bb`:
  ALL PASS.
- Acceptance (`specs/features/BL-1352-escalation-transport-fault-is-visible.feature`):
  7/7.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1352EscalationTransportFaultSteps` (line 26) — confirmed.
- `required_wiring`: `swarm_status.bb`'s `ask_escalation` confirmed reading
  the `:ask_escalation` key off `status.json` and folded into the rendered
  status map (line 338).
- Declared invariants
  (`extension/test/bl1352EscalationVisibilityInvariants.property.test.js`):
  2/2.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-disabled the `:fault` classification branch in
  `role_ask_escalation_lib.bb` (`(seq waiting)` → `(and false (seq
  waiting))`) — 4 bb-suite assertions failed exactly as the fault-branch
  test class predicts. Restored (`git diff --stat` empty), re-ran ALL
  PASS.
- **Two incidental property-test fixes riding on this branch, independently
  re-verified**: `bl1323StampOffInvariants` (invariant 1's scope fix — now
  scoped to BL-1323's own commits via `git log --grep`, not the whole
  branch diff) and `bl1306AuditKeyBasisInvariants` (explicit 180s timeout
  + reduced `numRuns` for a genuinely slow real-process property test).
  Re-ran both: invariant 1 now passes (confirmed the fix); invariant 2
  still fails on the frozen `state: stamp-open` assertion — expected,
  already tracked as `BL-1356`, not touched by this parcel and not
  re-reported. `bl1306`'s test passed clean.
- Standing whole-tree guards re-run
  (`tempDirTrapGuard`/`socketFixtureShortRootGuard`/`liveRepoDerivationGuard`):
  3 failures, matching the same pre-existing/already-ticketed set as
  every prior pass today.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population. None touch this parcel's files.
- Property suite (`npm run test:properties`): 25 files / 14 tests red —
  the same standing-debt population plus `bl1323StampOffInvariants`
  invariant 2 (BL-1356, expected and already accounted for above).
  BL-1352's own property test — 2/2 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry and the "Transport visibility (BL-1352)" how-to section confirmed
  present and accurate. No diagram change-trigger fired.
- Orphaned test processes: reaped several leftover vitest process groups
  after the property-suite run before proceeding.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
