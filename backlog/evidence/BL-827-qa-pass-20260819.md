# BL-827 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-827 — adopt the spec-dependent flow-watchdog thresholds
- **Parcel**: `00_20260819T163840Z_000209_from_documenter_to_QA_for_QA` (from documenter, commit `738b30a393`)
- **Verified at**: `650b4d6a3` (Merge documenter BL-827 into QA); this evidence file is committed on top and the forward names that commit.
- **Reviewed by**: QA, 2026-08-19
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-827` → exit 0, `VERIFY BL-827`. No open deferral.
- **Prior bounces**: none — no bounce evidence file, no `bounce_count` on the ticket, nothing on `main` or `origin/main`.

## Verdict

**PASS — inventory items: NONE.**

Every gate below was RUN, none blocked, none omitted.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — coder `d7065a260c`, cleaner `05cb456717`, architect `68056982e0`, hardener `9eab1f8360`, documenter `738b30a393` all ancestors of the cited commit | PASS |
| G2 | Prior-bounce check against `main` **and** `origin/main` (BL-891) | PASS — none |
| G3 | Babashka unit gate — `bb swarmforge/scripts/test/flow_watchdog_test_runner.bb` | PASS — `ALL PASS: flow_watchdog_lib.bb`, exit 0 |
| G4 | Property runner — `bb swarmforge/scripts/test/bl827_flow_watchdog_thresholds_property_runner.bb` | PASS — exit 0, 400 runs/invariant, 88 malformed tables, 61 firing tiers, 141 flat sets, 156 emitted pairs |
| G5 | Acceptance — `run_acceptance.sh specs/features/BL-827-flow-watchdog-spec-dependent-thresholds-adopt.feature` | PASS — 8 pass / 0 fail, exit 0 |
| G6 | TypeScript unit suite | PASS (carried) — `git diff --name-only 19be8ca32 HEAD -- extension/src extension/test` is **empty**, so this parcel changes no TypeScript and the 7881-passing run recorded in `BL-954-qa-pass-20260819.md` still applies unchanged |
| G7 | `required_wiring` — pre-QA gate | PASS — `pre_qa_gate_cli.bb BL-827-flow-watchdog-spec-dependent-thresholds-adopt 650b4d6a37 .` → `OK` |
| G8 | `required_wiring` item 1 — `handoffd.bb::completed-dir` | PASS — `role-inboxes-for-flow-watchdog` (handoffd.bb:1979) passes `:completed-dir` **and** `:abandoned-dir` into the sweep, so calibration genuinely has samples |
| G9 | `required_wiring` item 2 — `flow_watchdog_lib.bb::resolved-via` | PASS — resolved at `resolve-thresholds` (:284/:288), threaded through the sweep (:865/:879), and **reaches `format-alarm-text`** (:730-731, `" Threshold … via <source>."`) — computed *and* surfaced |
| G10 | Step-handler registration (the BL-954/BL-956 drop class) | PASS — `specs/pipeline/steps/index.js:518` registers `bl827FlowWatchdogSpecThresholdsSteps` |
| G11 | Docs currency (documenter's own pass) | PASS — `docs/reference/Specification.MD` (+33) documents the ceiling, the config key, and the reject-not-clamp posture |
| G12 | Functional change present (not manifest/format churn) | PASS — `flow_watchdog_lib.bb` +220/-93, new property runner, extended unit runner, new conf key, new step handler |
| G13 | Orphaned test/mutation processes before and after | PASS — none in this worktree; live `node --test` PIDs belong to the coder's and hardener's worktrees |

## Intent check — does it do what the ticket asked?

The ticket's `approval_context` says the load-bearing open item was **not** whether to adopt (already ruled) but the **adaptation hazard**: history-calibrated thresholds drift toward whatever a route has been doing, so a chronically slow hop teaches the table that slow is normal and the alarm goes quiet. Three options were put to the human, (a) hard ceiling, (b) ceiling multiple, (c) freshness-weighted samples.

- **The decision was made and is recorded, not assumed.** `docs/reference/Specification.MD:19` — "the human chose option (b), a ceiling multiple (`flow_watchdog_calibration_ceiling_factor`, default 4)".
- **It is implemented as specified, and as reject-not-clamp.** `default-calibration-ceiling-factor` (flow_watchdog_lib.bb:129), parsed at :140, carried as `:calibration-ceiling-warn-ms` = warn × factor (:164), enforced in `thresholds-from-samples` (:240). A raw warn above the ceiling is **REJECTED** — the key falls through to the global pair, which then fires — never clamped down. Same posture as the 60 s floor, and the two compose.
- **Config surfaced** at `swarmforge/swarmforge.conf:153` next to the global pair, which is where the ticket asked for it.

## `qa_e2e_procedure` — every step covered

| Step | Covered by | Result |
|---|---|---|
| 1 — seeded route below global warn but above `min-warn-ms` alarms early; alarm names threshold + source | acceptance scenarios 1 and 5 | PASS |
| 1b — route entirely below `min-warn-ms` writes no entry and stays silent (BL-835 regression) | `flow_watchdog_test_runner.bb:718-722` (explicit reject-gate assertion) and `:780` (sub-`min-warn-ms` history → **no entry at all**) | PASS |
| 2 — delete the thresholds file, sweep again, table rebuilt, no alarm lost | acceptance scenario 7 ("a calibration failure never disables the watchdog") | PASS |
| 3 — corrupt the file to invalid JSON, sweep completes, everything still evaluated, nothing throws | acceptance scenario 7 + property runner's **88 malformed tables** | PASS |
| 4 — sweep twice inside the recalibration interval, audits read once not twice | acceptance scenario 6 | PASS |
| 5 — never-before-seen route resolves to the global pair and says so | acceptance scenarios 2, 3 and 5 | PASS |

Invariants 1–3 are additionally exercised as properties (400 runs each), including invariant 2's "no route identity reaches `decide-tier`" (acceptance scenario 4) and invariant 3's "escalate strictly greater than warn" across flat and adversarial sample sets (141 flat sets, 156 emitted pairs).

## Degraded-tooling record (constitution, Startup Tools)

`.bb` swarm scripts have **no mutation, CRAP or DRY tooling pinned** for Babashka (BL-472 tracks wiring it). This parcel is gated by its own unit suite, its property runner, and its acceptance feature only. The hardener recorded the same degraded fallback in `backlog/evidence/BL-827-hardener-pass-20260819.md`; this pass does not imply mutation/CRAP/DRY ran, because they cannot.

## Sibling content riding in this commit (surfaced, not swept)

The QA branch still carries BL-955's chain (deferred behind BL-956, re-checked and still blocked today) and BL-956's scoped revert. Both were already landed with BL-954 earlier this session and are recorded in `BL-954-qa-pass-20260819.md`. Per BL-506 / Article 2.6, **this approval names BL-827 only.**

By QA.
