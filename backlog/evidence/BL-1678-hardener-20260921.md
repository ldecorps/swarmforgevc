# BL-1678 — hardener review pass, 2026-09-21

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: hand-mutated (> parent-count 1) to (> parent-count 2) in verify-push-safe (land_step_lib.bb) and ran bb/shell/property test suites
- **Commit hash**: 5fd754927d
- **First error excerpt**: every existing merge-commit fixture also trips the ticket-tagged-subject or path-attribution guard, masking the parent-count checks own correctness; a fixture isolating only that guard showed the mutant wrongly returns {:safe? true}
- **Failure class**: mutation-coverage-gap
- **Expected vs observed**: verify-push-safe rejects any 2+-parent merge candidate regardless of subject/path attribution, with a dedicated isolating test proving it
- **Blamed role**: hardener
- **Remediation pointer**: added isolating unit test to land_step_lib_test_runner.bb; verified fails against the mutant, passes against real code

By hardener.
