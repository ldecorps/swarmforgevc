# BL-1240 — cleaner pass, 2026-08-31

Task: `BL-1240-unregistered-test-fails-the-ticket-that-adds-it`
Received: coder's `31ffc314a0`.

## Verdict: NONE — no defect found, no cleanup needed

This restores a production call site lost in an earlier BL-490 revert, in
the exact position and shape of its own reverse diff:

- `swarmforge/scripts/swarm_handoff.bb`: 21 lines wiring
  `unregistered-test-gate-lib/findings-for-git-handoff` into the send path,
  matching the existing pattern used by every sibling gate in this file
  (`task-scope-block`, `tree-collapse-block`, etc.) — same shape, same
  fail-open posture, no new duplication or structural concern.
- `specs/pipeline/steps/lib/bl1240UnregisteredTestGateCli.sh`: the fixture
  driver now answers the self-audit challenge (retries once on
  `AUDIT_REQUIRED`) the way a real agent does, rather than asserting
  against the pre-audit response. Small, well-commented, scoped fix.

Both files were already through architect/hardener review earlier in this
ticket's history (see `backlog/evidence/BL-1240-architect-pass-2-20260830.md`,
`BL-1240-hardener-pass-2-20260830.md`); this parcel restores lost hunks
verbatim rather than introducing new design.

## Checks run

| Command | Result |
|---|---|
| `run_acceptance.sh specs/features/BL-1240-…feature` | 4/4 pass (was 1/4) |
| `bb swarmforge/scripts/test/unregistered_test_gate_lib_test_runner.bb` | ALL PASS |
| `bash swarmforge/scripts/test/test_swarm_handoff_sync_deliver.sh` | ALL PASS |
| `grep -n unregistered swarmforge/scripts/swarm_handoff.bb` | 5 hits — call site confirmed present |

No functional change made by cleaner; forwarding coder's work unmodified
to architect.

By cleaner.
