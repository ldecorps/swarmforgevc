# BL-954 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-954 — a bounce verifies its own revert
- **Parcel**: `00_20260819T162858Z_000207_from_documenter_to_QA_for_QA` (from documenter, commit `1ac2a979fa`)
- **Verified at**: `19be8ca32` (Merge documenter BL-954 into QA) — this evidence file is committed on top; the forward names that commit.
- **Reviewed by**: QA, 2026-08-19
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-954` → exit 0, `VERIFY BL-954`. No open deferral.

## Verdict

**PASS — inventory items: NONE.**

Every gate below was RUN, none blocked, none omitted.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — this ticket's own coder/cleaner/architect/hardener/documenter commits are ancestors of the cited commit | PASS |
| G2 | Prior-bounce check against `main` **and** `origin/main` (BL-891) | PASS — no prior QA bounce for BL-954; the one recorded bounce is the architect's, already fixed and reverted |
| G3 | `npm run compile` (in `.worktrees/QA/extension`) | PASS |
| G4 | Full unit suite (`npm test`) | PASS — 7881 passed / 444 files; see "Load caveat" |
| G5 | Property lane (`npm run test:properties`) | PASS for BL-954 — `bl954BounceRevertCheckInvariants.property.test.js` 3/3 in 15.0s; see "Load caveat" |
| G6 | Acceptance (`run_acceptance.sh specs/features/BL-954-a-bounce-verifies-its-own-revert.feature`) | PASS — 6 pass / 0 fail, exit 0 |
| G7 | `required_wiring` — pre-QA gate | PASS — `pre_qa_gate_cli.bb BL-954-a-bounce-verifies-its-own-revert 19be8ca32e .` → `OK` |
| G8 | Wiring is real, not just the lib (BL-419 shape) | PASS — `bounceRevertCheck` imported and called in `extension/src/tools/record-bounce.ts`; acceptance steps drive the **real** `record-bounce.js` as a subprocess over a fixture repo |
| G9 | Step-handler registration present (the BL-954/BL-956 drop class) | PASS — `specs/pipeline/steps/index.js:516` registers `bl954BounceRevertVerifiedSteps` |
| G10 | Docs currency (documenter's own pass) | PASS — `swarmforge/handoff-protocol.md` §"Bounce Revert Verification (BL-954)" and `docs/reference/Specification.MD`; prose matches the implementation, including the report-only stance and the policy/IO split |
| G11 | Functional change present (not manifest/format churn) | PASS — new modules, new CLI output field, new tests, new docs |
| G12 | Orphaned test/mutation processes before and after | PASS — none in this worktree; the only live `vitest` PIDs belong to the cleaner's worktree with a live parent, not orphans |

## `qa_e2e_procedure` — every step run

1. **Reproduce the defect from durable git data.** Of the 7 paths in `e4b327e031`, exactly 5 are byte-identical at `swarmforge-architect`'s tip and differ from the parent: `vitest-worker-memory-budget.ts`, `vitestWorkerMemoryBudget.test.js`, `vitest.config.mjs`, `vitest.properties.config.mjs`, `bl935VitestForkPoolSteps.js`. `git log --all --grep=e4b327e0` shows no revert commit. **Defect confirmed exactly as specced.**
2. **Drive the check over a fixture repo.** Acceptance scenario 01 — violation / clean / breach-report rows all correct.
3. **Invariant 1 is content-based, not ancestry-based.** Scenario 02 passes: the bounced commit remains an ancestor of the bouncing branch and the verdict is still `clean`.
4. **Invariant 2 — already-on-main emits no revert instruction.** Scenario 01 row 3: `breach-report`, remedy `none`.
5. **Invariant 3 — recording is never contingent on the check.** Scenario 03 rows: unresolvable commit and missing bouncing branch both → `undeterminable`, cause named, record still durable.
6. **Non-vacuity — PROVEN, not asserted.** Replaced the content comparison in the compiled adapter with an ancestry check (`tipMatchesBounced: isAncestorOf(runGit, opts.commit, branch)`) and re-ran acceptance: **`not ok 2`** (scenario 01's `reverted` row) and **`not ok 4`** (scenario 02) — exactly the two rows the ticket names, and only those. Restored and re-confirmed 6 pass / 0 fail. Mutation was applied to gitignored `extension/out/` only; no tracked file was touched.
7. **Wiring is real.** Covered by G8 — the acceptance handlers execute `extension/out/tools/record-bounce.js` itself and assert on its `revertCheck` output plus the durable JSONL record.
8. **No regression.** `recordBounceCli.test.js` 58/58 and `recordQaBounceCli.test.js` 34/34 green; no test weakened.

## Load caveat (recorded, not swept)

This host ran at load average **88 → 255 on 4 cores** throughout verification (a full 7-role pack plus concurrent suites). Under that load:

- Unit suite: 3 files failed, all `Test timed out in 20000ms` — `briefingDigestLineCli`, `dependencyGateCliStorageGlobals`, `renderBriefingBurndownCli`. **All 3 pass when re-run isolated** (23/23). Load artifacts.
- Property lane: 12 files / 15 tests failed. 14 were explicit `Test timed out` at 20s–240s budgets; 1 (`bl796NvmNodePathFollowUpAdoptInvariants` invariant 1) failed as a property counterexample and **passes clean when re-run alone** (3/3). Re-running the failures isolated recovered `bl622`, `bl628`, `bl687`, `bl773`, `bl796`. The residue (`bl632`, `bl760`, `bl797`, `bl868`, `bl892`, `onboarderContractPhaseRedelivery`) still times out even run alone at load 185–255 — `bl868PropertyLaneIsolationGuards` spawns a nested vitest and cannot meet a 60s budget on this box right now.
- No non-benign `Unhandled Error` (BL-871): the only ones present are the allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` artifact.

**None of these touch BL-954's code paths**, and every BL-954-owned test is green and fast (property 15.0s, acceptance 29.7s). The residue is an environment/capacity signal, not a defect in this parcel, and is surfaced here rather than swept or attributed to the ticket.

## Sibling content riding in this commit (surfaced, not swept)

The cited commit is an entangled batch lineage (cleaner and hardener are batch roles). Besides BL-954 it carries:

- **BL-955** — its full coder→documenter chain (`517123c24` … `94a3481eb`). Its own parcel `000208` (commit `70860df6f2`) is queued to QA and **has not been verified by me**; it gets its own pass next. It is **not** approved by this verdict.
- **BL-956** — bounced by the hardener, with the scoped revert (`96882d743`) and the feature parked as `.draft` (`959cd340d`). This landing therefore *removes* bounced content from `main` rather than adding any; BL-956 remains unapproved and its parcel `000210` is queued.

Per "An Approval Authorizes Only Its Ticket's Work" (BL-506) and Article 2.6, **this approval names BL-954 only.**

By QA.
