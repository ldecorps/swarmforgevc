# BL-1192 — QA bounce, 20260828

## D1 — entangled tip: cited commit carries BL-1211's still-bounced, incomplete work (BL-506 tip-purity violation)

1. **Failing command, exactly as run:**
   ```
   env -u GIT_DIR -u GIT_WORK_TREE git diff --name-only origin/main...4478c3bb6a
   ```

2. **Commit hash checked out and tested:** `4478c3bb6a` (`4478c3bb6a4aa...`,
   documenter's `docs(BL-1192): document pre-handoff task-scope gate`),
   received via `git_handoff` payload `merge_and_process documenter
   4478c3bb6a`.

3. **First error excerpt** — the diff vs `origin/main` includes, beyond
   BL-1192's own scope (`task_scope_gate_lib.bb` + test runner,
   `swarm_handoff.bb` wiring, `specs/features/BL-1192-*.feature` +
   `bl1192TaskScopeGateSteps.js`, `handoff-protocol.md` +
   `Specification.MD`):

   ```
   extension/src/metrics/bounceResurrectionGitAdapter.ts
   extension/src/tools/quarantineLiftCliArgs.ts
   extension/src/tools/recovery-filter-check.ts
   extension/src/tools/recoveryFilterCliArgs.ts
   extension/test/bl1211OperatorCli.test.js
   extension/test/bounceResurrection.test.js
   specs/features/BL-1211-quarantine-lift-cannot-restore-reverted-bounce-content.feature
   specs/pipeline/steps/bl1211QuarantineLiftAuthorshipSteps.js
   backlog/evidence/BL-1211-architect-pass-recovery-cli-20260828.md
   backlog/evidence/BL-1211-cleaner-recovery-cli-pass-20260828.md
   ```

   All of these belong to **BL-1211**
   (`backlog/active/BL-1211-recovery-resurrects-reverted-bounce-content-and-the-lift-check-is-blind.yaml`),
   whose own ticket record shows `required_stages: [coder, cleaner,
   architect, hardender, documenter, qa]`, `status: todo`, `bounce_count: 2`,
   with the LAST recorded event an **architect bounce**
   (`c914c40443`, `backlog/evidence/BL-1211-architect-bounce-20260828.md`,
   2026-08-28) — i.e. BL-1211 has not yet cleared architect, let alone
   reached hardener/documenter/QA. The `-recovery-cli`-suffixed evidence
   files found in this commit's tree are a later, uncommitted-to-the-ticket
   round of work not reflected in BL-1211's own YAML.

   **Independent confirmation, not merely inferred:** attempting to merge
   this commit into the QA worktree (already carrying the BL-1227 bounce's
   revert, see `backlog/evidence/BL-1227-qa-bounce-20260828.md`) produced a
   real `CONFLICT (modify/delete): extension/src/tools/
   recoveryFilterCliArgs.ts deleted in HEAD and modified in 4478c3bb6a` —
   i.e. this commit and BL-1227's cited commit carry two DIFFERENT,
   conflicting in-flight versions of the same BL-1211 file. The merge was
   aborted cleanly (`git merge --abort`); no content was written or landed.

4. **Failure class:** `behavior` — the same "entangled tip" class as
   `backlog/evidence/BL-1227-qa-bounce-20260828.md`; both parcels trace to
   the same underlying branch-hygiene problem BL-1192 itself was minted to
   catch mechanically (not yet landed, so no automated gate caught either).

5. **Expected vs observed:** Expected — a `git_handoff` naming `BL-1192`
   cites a commit whose diff vs `origin/main` touches only BL-1192's own
   paths. Observed — the cited commit also carries BL-1211's own
   still-disputed (architect-bounced, not yet re-passed per its own ticket
   record) work, in a version that actively conflicts with another
   in-flight copy of the same files seen on the same branch this session.

## Other checks this pass

- BL-1192's own scope (task-scope gate lib, wiring, docs) was not
  functionally re-verified this pass: merging to inspect it in isolation
  hit the D1 conflict immediately, and resolving that conflict is not QA's
  call to make on BL-1211's behalf. **BLOCKED BY D1.**

## Remediation

Coder: rebuild a tip-pure `BL-1192`-only commit based on the current
`origin/main` (or current QA-approved base), containing only BL-1192's own
paths, and re-send. Separately: BL-1211 needs its own re-fix, its own
cleaner/architect/hardener/documenter passes, and its own `git_handoff` to
QA under its own name once complete — it must not ride in under another
ticket's task name (Article 2.6).
