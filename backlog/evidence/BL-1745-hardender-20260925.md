# BL-1745 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: the three test files
(`bl1670LandStrayConflictSupersededInvariant.property.test.js`,
`bl1108CursorSeatReadiness.property.test.js`,
`bl1565CoordinatorNeverReceivesGitHandoffInvariants.property.test.js`)
each gain `SUBPROCESS_HEAVY_TIMEOUT_MS` (240000ms, BL-932's shared
constant, already used by bl1713 and others) as their `test()` timeout —
no assertion, shape, generator, or reach-floor changed. mutation_cost:
low; no production code touched. Nothing to mutation-test: the diff adds
a timeout parameter only, and mutating a timeout constant proves nothing
about correctness (a wrong timeout still lets a fast test pass and only
changes how long a hung one is tolerated before failing).

Checks run:
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1670LandStrayConflictSupersededInvariant.property.test.js
  test/bl1108CursorSeatReadiness.property.test.js
  test/bl1565CoordinatorNeverReceivesGitHandoffInvariants.property.test.js`:
  6/6, all three previously-timing-out invariants comfortably under
  both the old 20s and new 240s ceilings (5.9s/7.5s/7.3s).
- `node specs/pipeline/cli.js
  specs/features/BL-1650-the-land-step-lands-a-pure-evidence-closed-owner-stray-itself.feature`:
  8/8.
- `git diff main...HEAD --name-only -- extension/test specs/pipeline`
  names exactly the three Scope files (BL-1739's three files still on
  this branch tip are a separate in-flight ticket, not this parcel's
  diff — confirmed by excluding them explicitly).
- `grep -n SUBPROCESS_HEAVY_TIMEOUT_MS
  extension/test/helpers/subprocessHeavyTimeout.js`: confirms the
  shared constant is 240000, the same value all three files now import.
- Every standing whole-tree guard (this parcel touches
  `extension/test/`): `npx vitest run $(ls test/*Guard*.test.js | grep
  -v '\.property\.')` — 22 files, 211 tests, all green.

Not run: `npm run test:properties` in full (2026-09-17 rule — ran the
three touched property files directly instead, per the same rule's own
guidance), CRAP/DRY (no `src`/`out` file in Scope), Stryker (no
production code changed).

Fixture hygiene: `git status --short` clean of anything this pass's
runs could leak (pre-existing untracked BL-1652 scratch files predate
this session, not mine to remove — surfaced, not swept).

By hardender.
