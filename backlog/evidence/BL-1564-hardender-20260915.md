# BL-1564 — hardender review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Merged architect e11ae7505b (`Fast-forward`, `git log` confirms not-an-ancestor
  before merge). Diff vs main: property test file, new step handler, three
  upstream evidence files, plus this pass's manifest commit — matches ticket
  scope exactly; `extension/src/`, the three bb libraries,
  `vitest.properties.config.mjs`, `property_suite_standing_allowlist.tsv`
  untouched throughout.
- Load checked first (`uptime`): 1.8-2.3 on 20 cores, quiet — ran everything
  in the foreground, no office-hours bypass needed. No orphaned
  `node --test|stryker` processes before starting.
- `mkdir tmp/shim` counting-bb-shim recipe from the ticket's own
  `qa_e2e_procedure`, run 3× standalone: 3/3 pass, exactly 3 bb processes
  each run (was 98 pre-fix), wall time 6.19-6.46s (was 18.8-21.3s). Reach
  maps every run: invariant 1 cases 21, invariant 2 cases 18, invariant 3
  cases 18 — matches the declared floors exactly.
- `./specs/pipeline/scripts/run_acceptance.sh` on the ticket's feature —
  5/5 scenarios green.
- Whole-tree standing guards (parcel touches both `specs/pipeline/steps/`
  and `extension/test/`): all 18 `test/*Guard*.test.js` files run, 183/183
  pass (grew from the "six files as of 2026-08-19" note — enumerated by
  glob, not recalled).
- **BL-113 Gherkin acceptance mutation** (scenario 03 is a Scenario
  Outline, `soft`): 6 mutants — m1 `examples[0].cases 21->28` killed, m2
  `examples[0].invariant 1->-3` killed, m3 `examples[1].cases 18->17`
  **survived**, m4 `examples[1].invariant 2->-7` killed, m5
  `examples[2].cases 18->19` killed, m6 `examples[2].invariant 3->-4`
  killed.
- m3 accepted as **equivalent (BL-234)**, not chased: the step handler's
  assertion (`bl1564OneBbProcessPerInvariantSteps.js`, `that reach map
  counts at least (\d+) cases`) is literally `map.cases >= Number(floor)`,
  matching the scenario's own wording ("at least") and the ticket's
  `qa_e2e_procedure` ("cases at least 21, 18 and 18"). The actual reach
  count is deterministic (constructed cases + fixed `numRuns`) and equals
  its declared floor EXACTLY for all three rows (21/18/18) — so for
  every row, decreasing the floor can never fail the assertion, since the
  actual value already satisfies the ORIGINAL floor and loosening only
  helps. This is a general, code-level equivalence (provable from the `>=`
  operator and the observed exact-equality invariant), not specific to m3's
  particular delta — m1's and m5's mutants were increases and were
  correctly killed, which is the asymmetry this equivalence predicts.
  Tightening the assertion to exact equality was considered and rejected:
  it would contradict the scenario's own literal "at least" wording and the
  ticket's own `qa_e2e_procedure`, i.e. it is a spec-semantics change, not a
  hardening fix, and BL-234 forbids forcing an artificial assertion to kill
  a survivor that tests implementation trivia (here, an intentional design
  choice) rather than behavior.
- BL-502: the embedded manifest correctly writes `scenarios: []` for this
  non-clean run (one survivor) — expected, not a regression; recorded here
  per BL-234's requirement that an accepted equivalent be recorded WITH its
  code-level reason, separately from `record-bounce.js` (no bounce: this is
  a pass, not a defect).
- No production code touched (test file + step handler only) — no Stryker
  target per ticket's `mutation_cost: low`; `jscpd` scopes to `src` only,
  not applicable.
- No orphaned `bb`/tmux/vitest processes after any run; `git status --short`
  clean before this commit.
- No bounce; forwarding to documenter.
