# BL-1488 — hardender review pass, 2026-09-11

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Merged architect `252eea0d19` (NONE — the architect forwarded the
coder's own run with the specifier's ruling on top; nothing for the
architect to redo). Ran the deferred BL-1476 mutation gate itself in
this pass, then hardened the hardener's own in-pass slice: full work is
in `5fd5244aa3` ("BL-1488: run the deferred BL-1476 mutation gate,
discharge it") and its evidence at
`backlog/evidence/BL-1488-BL-1476-mutation.md`.

- Reproduced the coder's run byte-for-byte first (400 killed/120
  survived/54 no-coverage), then chased the specifier's assigned 43-mutant
  slice: 21 newly killed with test-only changes to
  `extension/test/turnProfileProducer.test.js` (a mid-tick "gone"
  transcript, a torn-tail transcript through the `runTick` path, and the
  turnProfileProducer barrel's two previously-dark re-exports), 22
  accepted equivalent with code-level reasoning recorded per-mutant in the
  discharge evidence (10 reusing BL-1364's own already-confirmed
  equivalences verbatim, on lines confirmed unchanged since that pass via
  `git diff`; the rest newly derived and confirmed against the actual
  compiled code: a union-tag fallthrough never checked by any caller, a
  `nowFn() >= NaN`/`>= undefined` always-false equivalence, and an outer
  hardcoded field masking an inner boolean).
- transcriptWalker.ts's remaining 131 mutants are owned by BL-1523 per the
  specifier's ruling — untouched, each named in the discharge evidence.
- No production code changed (`git status --short` before commit showed
  only the test file, the ledger, the register TSV and the new evidence
  file).
- Discharged the BL-1476/mutation ledger row
  (`hardening_debt_ledger_update.bb --discharge`) and removed its
  `backlog/standing-reds.tsv` register row in the same commit (BL-1439
  invariant 2, same pattern as BL-1468's `d458b242bc`).
- Both feature scenarios pass:
  `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1488-the-bl1476-deferred-mutation-gate-is-run.feature`
  — 2/2.
- Re-ran the scoped unit suite green (64/64 across
  `turnProfileProducer.test.js`, `transcriptWalker.test.js`,
  `runTurnProfileProducer.test.js`, `transcriptSummaryStore.test.js`)
  before and after hardening.
- CRAP/DRY: not run fresh — no production code touched, so neither metric
  is affected (ticket constraint: "No production code changes").
- No orphaned test/mutation processes: `pgrep -fl 'node --test|stryker'`
  clean before commit.

By hardender.
