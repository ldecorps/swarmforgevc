# BL-1218 — documenter pass, 2026-08-31

Reviewed hardener's forwarded commit (via coordinator re-dispatch
`ec2683d517`, resuming at documenter — architect/hardener passes were
already complete before this ticket was parked to `backlog/hold/` by an
orphaned dead expedite run, then released back to `active/` unchanged; no
functional change since the hardener's clean verdict). Fix:
`swarmforge/scripts/remote_control_launch_lib.sh`'s new
`resolve_remote_control_cli`, wired into `swarmforge.sh`'s `parse_config`,
now makes `config remote_control off` strip `--remote-control` from a
Claude window line's composed launch flags even when the line names the
flag explicitly — previously only auto-inject (an omitted flag) honored
the config.

## Doc-impact check

Grepped `docs/` and `swarmforge/handoff-protocol.md` for `remote_control`:
BL-1217's own how-to section (`docs/how-to/BL-514-...md`) and
`docs/reference/Specification.MD`'s BL-1217 changelog entry both explicitly
named BL-1218 as its sibling and, at the time they were written, said
"unticketed here" / "covers the launch-script-write half" in future tense
— stale now that BL-1218 has landed. `docs/index.md`'s BL-514 line also
listed only the BL-1108/BL-1217 highlights.

## Docs updated

- `docs/how-to/BL-514-remote-control-health-and-ensure-wiring.md`: new
  "config remote_control off decides the launched flag itself (BL-1218)"
  section, following the established per-ticket section pattern already
  used for BL-1108/BL-1217 in the same doc; updated the BL-1217 section's
  forward pointer from future tense to "landed, see below".
- `docs/index.md`: added the BL-1218 highlight to the existing BL-514
  how-to line.
- `docs/reference/Specification.MD`: new top changelog entry. Left the
  historical BL-1217 entry (dated August 28) untouched, per this doc's
  established convention of stacking new entries rather than rewriting
  frozen history — its "unticketed here" phrasing is now superseded by
  today's new top entry, not silently wrong.

No diagram depicts launch-script composition internals; none of the four
registered diagrams' change-triggers fired (this is a bash launch-flag
decision, not an extension-host/webview boundary, pipeline-topology,
handoff-mechanism, or front-desk-routing change).

## Verdict

Clean. Docs brought current, sibling-ticket cross-references fixed.
Forwarding to QA.

By documenter.
