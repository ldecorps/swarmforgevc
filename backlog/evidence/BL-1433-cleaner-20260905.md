# BL-1433 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean —
this is the fix for the exact recurring stale merge-up-note flood I
observed and flagged repeatedly this session (a note to the coordinator
was already sent earlier; this ticket is the mechanical root cause).

## What was checked

- Reviewed `decide-role`'s new branch: `contains-landed?` is checked right
  after `already-settled` and before `in-process?`/`dirty?`/`can-ff?` —
  `true` → `:holds-landed` (never surfaced, whatever the worktree holds,
  invariant 1), `nil` → `:skip :unknown-containment` (logged, never a
  tell, invariant 3, correctly distinct from `:missing-ref`), `false`
  falls through unchanged to the pre-existing chain (invariant 2).
- Confirmed the daemon's new `:contains-landed?` fact correctly reverses
  the argument order vs the existing `:can-ff?` (`git-is-ancestor? wt
  landed head` vs `wt head landed`) — verified directly against a real git
  fixture (`bl1433_supplier_git_facts_runner.bb`): `can-ff?: false,
  contains-landed?: true`, exactly the shape a branch merely ahead of
  origin should report. The coder's own evidence notes this argument order
  was initially backwards and caught by this same fixture — confirmed the
  final, committed version is correct.
- Ran all suites: unit runner (ALL PASS), the new property runner (500/500
  ALL PROPERTIES HOLD), the supplier git-fixture runner (stable across the
  run), and all four features — BL-1433 itself (6/6), plus the three
  named regressions BL-668 (5/5), BL-1361 (6/6), BL-1421 (6/6) — all
  green.
- Confirmed every existing caller of `decide-role`/`sweep!` was updated to
  supply an explicit `:contains-landed? false` rather than leaving it
  absent (absence reads as `nil` → unknown-containment, which the ticket's
  own direction says must never silently read as containment) — spot-
  checked the test runner and step-handler diffs directly.
- `check_bb_scripts_load.sh --all` and `check_standing_red_register.sh`
  both clean except the one already-known, unrelated, out-of-scope
  `post_qa_branch_sweep.bb` parse failure (BL-1426's own scope, confirmed
  multiple times earlier this session).
- Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred) — this
  pass is gated by the unit/property/acceptance suites only, per
  constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bb post_qa_branch_sweep_lib_test_runner.bb` | ALL PASS |
| `bb bl1433_branch_holds_landed_commit_property_runner.bb` | ALL PROPERTIES HOLD, 500/500 |
| `bb bl1433_supplier_git_facts_runner.bb` | `can-ff?: false, contains-landed?: true` — correct |
| `bb bl1421_one_standing_surfacing_property_runner.bb` (regression) | ALL PROPERTIES HOLD, 500/500 |
| `node specs/pipeline/cli.js specs/features/BL-1433-...feature` | 6/6 |
| BL-668/BL-1361/BL-1421 features (regression) | 5/5, 6/6, 6/6 |
| `grep -n contains-landed? handoffd.bb` | present, correct arg order (required_wiring) |
| `bl1433BranchHoldsLandedCommitSteps.js::registerSteps` exists | yes (required_wiring) |
| `check_bb_scripts_load.sh --all` | 1 pre-existing unrelated failure (BL-1426's scope), confirmed identical to before this parcel |
| `check_standing_red_register.sh` | exit 0 |

No fix required. Forwarding unchanged to architect.
