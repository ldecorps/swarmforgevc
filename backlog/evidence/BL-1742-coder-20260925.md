# BL-1742 — coder pass, 2026-09-25

## What made the file slow, and what replaced it

The "BL-1518: swarm_handoff.bb refuses a draft..." test spawned two real
`bb swarm_handoff.bb` processes (~9.7 s of the file's own ~15.2 s alone on
main). It never called `enqueueRoleAnswerNote` - it shelled to the CLI
directly with a hand-built wrong `cwd`/`env`, so no mutant of
`enqueueRoleAnswerNote`'s own options object was ever exercised by it,
despite the test's own comment naming that mutant as its purpose.
Everything it actually proved (a draft outside the resolved root is
refused, naming both; nothing queued under either root; a normal send
once cwd/env point at the draft's own root) is independently, completely
covered by:
- `specs/features/BL-1518-a-handoff-cli-never-writes-outside-the-root-its-draft-lives-in.feature`
  scenarios 01/02 (a third scenario, 03, covers a boundary this test never
  did - a sibling directory sharing the root's name as a text prefix).
- `swarmforge/scripts/test/bl1518_handoff_draft_root_guard_property_runner.bb`
  (400 runs, six boundary categories: inside-exact/flat/nested,
  outside-parent/prefix/sibling).

Both run independently of this file and both still pass (confirmed
below) - nothing this test asserted is dropped, only de-duplicated.

**The established pattern that replaced it**: this file's own BL-1620
convention, `fakeRunHandoff()` - already the seam every OTHER test in the
file drives `enqueueRoleAnswerNote` through. The real mutant the old
comment named (the `{ cwd, env }` object `enqueueRoleAnswerNote` hands its
`runHandoff` seam, dropped or emptied) needed exactly this: a test that
calls `enqueueRoleAnswerNote` itself and asserts the recorded `opts.cwd`/
`opts.env.SWARMFORGE_ROLE` - one `test()` in, one `test()` out, the file's
own test count unchanged (276, matching main exactly).

Also removed: `readQueuedOutboxNote`/`listOutboxFilesIfPresent`, the two
helpers only the removed test used - dead code otherwise.

## Non-vacuity, checked directly (not left in the commit)

Hand-mutated the compiled `out/tools/telegram-front-desk-bot.js` (backed
up first, restored after): `runHandoff(cli, draftPath, { cwd: targetPath,
env: {...} })` -> `runHandoff(cli, draftPath, {})` - the exact mutant the
new test's own comment names. The new seam test failed
(`AssertionError: runHandoff must be called with cwd pointed at the
draft's own root`, `undefined !== '/tmp/sfvc-role-ticket-...'`); every
other test in the file stayed green (only this one test ran, 275 skipped
via `-t`). Restored the compiled file, re-ran: green again.

## Verification run before forwarding

- `npx vitest run --config vitest.config.mjs test/telegramFrontDeskBotCli.test.js`:
  276 of 276, 1.4 s alone (was 15.2 s on main / 8.1-19.2 s in-suite).
- Same file inside the full `npm test` run: 6.1 s in-suite, under the
  7000 ms budget even under contention.
- `node specs/pipeline/cli.js specs/features/BL-1620-...feature`: 2 of 2
  (the count step: 276 at the parcel >= 276 received; the budget step:
  under 7000 ms in every alone run).
- `node specs/pipeline/cli.js specs/features/BL-1518-...feature`: 3 of 3.
- `bb swarmforge/scripts/test/bl1518_handoff_draft_root_guard_property_runner.bb`:
  ALL PASS (400 runs each).
- `npm test` (extension/, compile + unit lane): 637 files / 10868 tests,
  exit 0.

## Scope

Touched: `extension/test/telegramFrontDeskBotCli.test.js` only - no bb-lane
runner needed (BL-1518's own feature/property runner already cover
everything the removed test asserted, confirmed above). This evidence
file. `backlog/standing-reds.tsv` NOT touched. No production code touched.
Nothing else in this worktree staged.

By coder.
