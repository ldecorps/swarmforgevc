# BL-1491 — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Reviewed architect's `248400bfdd` (cleaner `7bf81e6818`, coder `9b1c4c393c`).

- **Babashka/no-Stryker fallback (BL-472/BL-638)**: no mutation tool wired
  for `.bb`. Best-effort hand-authored mutation sweep, each mutant applied
  live to the worktree file, run, then restored — confirmed byte-identical
  via `git diff --stat` (empty) after each restore:
  1. Dropped the verdict text from `write-kill-all-audit-row!`'s appended
     line — killed by `bl1491_halt_records_itself_property_runner.bb`'s P1
     (fails on all 6 distinct verdicts generated).
  2. Swapped `stop` → `start` in `write-availability-stop-record!`'s
     `availability_record` call — killed by the same P1 (0 stop records
     found).
  3. Removed the `try/catch` around `record-halt!` in
     `daemon-alarm-lib/alarm-and-halt!` — killed by P2 (uncaught throw
     propagates, stack trace) AND by `test_daemon_alarm_lib.sh`'s
     `BL-1491 record-halt-02` (FAIL).
  No detached suite was outstanding during any mutation/restore cycle
  (single-writer discipline, this prompt's rule).
- **Tests run, all green**: `test_daemon_alarm_lib.sh` ALL PASS (incl.
  record-halt-01/02/03); `test_availability_ledger_lib.sh` ALL PASS;
  `bl1491_halt_records_itself_property_runner.bb` ALL PROPERTIES HOLD (150
  runs/property, 6 distinct verdicts, 2 distinct exception types — coverage
  floors met); acceptance `specs/features/BL-1491-*.feature` 4/4 green via
  `node specs/pipeline/cli.js`.
- **`test_handoffd_supervisor.sh` cases 01/02**: fail in this sandbox
  (tmux/hung-pid mechanics), confirmed by the architect as pre-existing on
  `9b1c4c393c^` and ticketed BL-1498 — not this parcel's defect. Re-verified
  the BL-1491-specific block (lines 397-422, no tmux socket) in isolation by
  extracting the shared fixture helpers (lines 1-110) plus that block into a
  temp copy under `swarmforge/scripts/test/` (same directory, so
  `SCRIPT_DIR`-relative paths resolve), running it, then deleting the temp
  copy: both BL-1491 assertions PASS.
- **Timestamp format**: `seconds-iso`'s `ISO_INSTANT` truncated to seconds
  produces byte-identical output to `kill_pipeline_swarm.sh`'s
  `date -u +%Y-%m-%dT%H:%M:%SZ` (spot-checked live, same second).
- **Whole-tree guards (this prompt's rule — parcel adds files under
  `specs/pipeline/steps/`)**: ran all 18 non-property `*Guard*.test.js`
  files under `extension/test/` — 183/183 PASS, including
  `tmuxReaperGuard` and `bl968StepRegistryMaterializedTreeGuard`.
- **Load/orphans**: host load 2.22 on 20 cores throughout (no cooldown/busy
  gate needed); no orphaned `node --test`/`stryker` processes before or
  after this pass (`pgrep -fl`).
- **CRAP/DRY**: n/a — CRAP tooling scopes `extension/src/*.ts` only; this
  parcel touches `.bb`/`.js` pipeline-step files exclusively.
- **Correctness**: no defect found. Both declared invariants hold under
  hand-mutation; write-ahead ordering, best-effort failure isolation, and
  the BL-897 shared-writer-reuse posture all verified against the real
  writers, not a reimplementation.

Forwarding to documenter.
