# BL-1717 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Rebuild pass, after QA's bounce (`backlog/evidence/BL-1717-QA-20260925.md`,
commit `e81a640797`, 3 defects: D1/D2 coder, D3 documenter — travels).
Re-hardened the coder's rebuild (`6ebe8176d3`, D1 own-paths fix + D2
property test) rather than trusting the prior (pre-bounce) hardener pass
(`backlog/evidence/BL-1717-hardender-20260925.md`), which hardened the
buggy version QA caught.

Scope: `swarmforge/scripts/land_step_lib.bb` (own-paths' BL-1717 disjunct
only) — Babashka-only (BL-472: no mutation/CRAP/DRY wired); no `src`/`out`
production code touched.

Checks run (host load 15-24 on 20 cores — office-hours bypass not needed,
under the 2x-cores threshold, just slow):
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS
  (auto-backgrounded past the 120s foreground cap under load; collected
  via the task notification, exit 0 — no detach_job needed, the harness's
  own background handling sufficed).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1717LandedCoOwnerNeverShieldsInvariants.property.test.js` (from
  `extension/`): 1/1 passed, 14.3s (load-driven, well under any per-file
  ceiling for a property file).
- `node specs/pipeline/cli.js specs/features/BL-1717-a-landed-co-owner-never-shields-an-unlanded-siblings-lines.feature`:
  2/2 ok.
- Neighbouring exclusion/passenger features (QA's qa_e2e_procedure step 2),
  one run each: BL-1389 5/5, BL-1375 7/7, BL-1481 4/4, BL-1678 4/4 —
  matches QA's pre-bounce counts, unchanged.
- `git diff --stat f93ebdf4af..HEAD -- <Scope files>`: only
  `land_step_lib.bb`, the property test, the handler + its CLI lib
  fixture, the test runner, and the doc (D3, still travelling, not
  touched by this pass) — matches Scope.
- `backlog/standing-reds.tsv`: no row named BL-1717 itself; two rows
  (BL-1741, BL-1742) are unowned reds QA *found* during this ticket's
  first pass but that are now owned by their own minted tickets —
  orthogonal, not this ticket's to close.

Independent re-verification of QA's D1 probe (re-typed from
`backlog/evidence/BL-1717-QA-20260925.md`, byte-identical logic, run fresh
rather than trusted from the architect's re-run): scenario B ("landed L +
U") now reads `{:paths ["BL-9003-own.txt"]}`, matching control A and
scenario C — confirmed independently.

Extended the probe past QA's own three cases with three more shapes this
pass constructed, to press on exactly what D3's doc-wording bug implied
might still be wrong (a THREE-or-more-owner path, and the two controls a
narrower fix could plausibly still miss):
- D: two landed co-owners + one not-yet-landed, none in the ticket-level
  `unlanded-siblings` set (the "three or more owners" shape D3's stale doc
  wording named as unhandled) → correctly excludes. This is the strongest
  confirmation available that the fix is NOT narrowly two-owner-shaped —
  it generalizes to any owner count, which is what the corrected D3
  wording (documenter's next pass) will need to say accurately.
- E: every owner path-landed, no unlanded owner at all → correctly rides
  (clause 2's pre-existing territory, not regressed).
- F: the landing ticket itself also owns the path, alongside a landed and
  an unlanded co-owner → correctly rides regardless (BL-1375's
  `task-ticket-id` guard, unaffected by the BL-1717 clause).

Hand-mutation (BL-638 fallback — no wired mutation tool for `.bb`,
`mutation_cost: medium`): reintroduced the EXACT pre-fix D1 bug shape in
place — replaced the fixed clause `(seq (remove #(path-landed? % path)
(:owners attribution)))` with the literal pre-fix form `(every?
unlanded-siblings (remove #(path-landed? % path) (:owners attribution)))`
(QA's own remediation pointer's description of the bug, reconstructed).
- The probe reproduced the exact D1 symptom (scenario B and the new
  scenario D wrongly kept `shared.md`) and, as a bonus finding, also broke
  scenario E (all-owners-landed wrongly EXCLUDED — a vacuous-`every?`-on-
  empty-collection regression, the same trap class as mutant 3 in the
  prior pre-bounce hardener pass), confirming the old bug shape is a
  strictly worse regression than a narrow reading suggested.
- `land_step_lib_test_runner.bb` against the mutant: 2 failures, both the
  pre-existing BL-1389 unit case ("a sibling path whose own lines are
  already on origin/main is kept") — killed.
- The new property test against the mutant: failed on its first
  (constructed, non-random) cell, `excluded-not-in-set` — killed.
- Restored the file byte-for-byte (`git status --short` clean); re-ran
  both suites green before finalizing (unit ALL PASS; property 1/1
  passed).

Fixture hygiene: my own hand-built probe script (git fixtures under
`fs/create-temp-dir`, wrapped in `try/finally` with `fs/delete-tree`) and
the shipped `bl1717LandedCoOwnerCli.bb` fixture both left nothing behind
(`ls /tmp` swept clean of `bl1717-hardener-edge-*`/`bl1717-qa-edge-*`
after all runs). `git status --short` clean of anything this pass's runs
could leak (the three untracked files present predate this session — see
prior turns' evidence).

Not run: `npm test` / `npm run test:properties` in full (2026-09-17
rule — this parcel touches no other `*.property.test.js`, and the coder's
own rebuild evidence already ran the full unit lane green at 637/637) and
Stryker/CRAP (no `src`/`out` file in Scope).

D3 (documenter, `docs/how-to/BL-1241-...md`'s stale "three or more owners"
wording) confirmed still unfixed in this rebuild by reading the live doc
file — unchanged from the architect's own note. Not mine to fix (Article
4.4 multi-stage-blame, travels to the documenter). This pass's extended
probe (scenario D above) independently confirms the corrected wording the
documenter needs: the fix excludes on ANY not-yet-landed co-owner
remaining after dropping path-landed owners, whatever the total owner
count — not "two owners suffice" vs "three or more", but owner-count-
agnostic.

By hardender.
