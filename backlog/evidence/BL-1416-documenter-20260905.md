# BL-1416 documenter review pass — 2026-09-05

Reviewed the hardened commit (`efa4ed04a0`) and the coder/cleaner/architect/
hardener trail for BL-1416 (a busy pane is never auth-dead).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1416-a-busy-pane-is-never-auth-dead.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1416BusyPaneNeverAuthDeadSteps.js`; the
  `handoffd.bb::observe-pane-auth!` busy-predicate consult required by the
  second wiring line is present (`chase-sweep-lib/actively-processing?`
  gating classification, `swarmforge/scripts/handoffd.bb` lines ~1321-1327).
- Operator-visible behavior change (a busy pane's own scrollback text is
  never classified auth-dead; a skipped respawn attempt no longer counts
  toward the attempt cap; the persist alert now names the matched pane
  line and the performed-respawn count) is documented in
  `docs/how-to/BL-536-provider-auth-error-auto-respawn.md` — the existing
  how-to for this mechanism (BL-536) — with a new "How it works" item 0,
  updated log-tag descriptions, and a new "A credential alert arrived for
  a healthy role" troubleshooting entry naming the 2026-09-05 false-alarm
  this ticket fixes.
- `docs/index.md`'s existing one-line summary for that how-to remains
  accurate at its level of detail — no edit needed there.
- No "Last Updated" freshness field in the edited doc — no date bump owed.
- Checked `docs/reference/Specification.MD` for stale auth-observer
  description: existing BL-536 references there are about unrelated
  concerns (evidence-gate history, BL-840 outage evidence, BL-849) — none
  describe the busy-pane classification behavior this ticket changes, so
  none go stale.
- No registered diagram depicts this mechanism's internal decision logic
  (this is a bugfix inside an existing observer's classification, not a
  new component, boundary, or pipeline-topology change) — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
