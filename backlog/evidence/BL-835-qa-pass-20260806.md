# BL-835 — QA verification pass — 2026-08-06

**Verdict: PASS.** No defects found. Approved for merge-up and integration to `main`.

**Commit reviewed:** `515ac6cb` (documenter tip; merged into this branch as this
evidence commit's parent). Ancestry confirmed: coder `17ba35e7`, cleaner
`ca569e09`, architect `92298b22`, hardener `8cca02bc` are all real ancestors of
`515ac6cb` (`git merge-base --is-ancestor` checked individually — not merely
"ancestor of HEAD" on a busy `main`).

## Inventory

1. **Orphaned processes** — `pgrep -fl 'node --test|stryker|vitest'` checked
   clean before and after this pass (only standing swarm daemons present:
   cursor_bridge_supervisor, onboarder_supervisor, handoffd, handoffd_supervisor,
   front_desk_supervisor, operator_runtime — none are test/mutation runs).

2. **Ticket-scoped unit suite** — `bb swarmforge/scripts/test/flow_watchdog_test_runner.bb`
   → `ALL PASS: flow_watchdog_lib.bb`.

3. **Ticket-scoped property runner** — `bb swarmforge/scripts/test/bl835_flow_watchdog_threshold_gate_property_runner.bb`
   → `ok`.

4. **Wiring test** — `bash swarmforge/scripts/test/test_handoffd_flow_watchdog_wiring.sh`
   → both PASS lines (real daemon sweep records warn-tier state; real Telegram
   OPERATOR alarm emitted naming role + rotate verb).

5. **Acceptance — ticket's own feature file** —
   `node specs/pipeline/cli.js specs/features/BL-835-flow-watchdog-floored-percentile-false-alarms.feature`
   → 4/4 scenarios pass. Step handlers confirmed registered:
   `specs/pipeline/steps/index.js` diff adds
   `require('./bl835FlowWatchdogFlooredPercentileFalseAlarmsSteps')` to `DOMAINS`.

6. **Acceptance — regression** —
   `node specs/pipeline/cli.js specs/features/BL-577-flow-watchdog-parcel-age-invariant.feature`
   → 22/22 scenarios still pass (unsuppressable-alarm surface untouched by this fix).

7. **required_wiring literal match** — both declared phrases confirmed present
   verbatim: `"reject-below-gate not floor-clamp"` (code comment / fix intent,
   `thresholds-from-samples`) and `"sub-floor samples do not WARN a 90s parcel
   under global 15m"` (exact assertion message at
   `flow_watchdog_test_runner.bb:702`, post `09b521c5` rewording).

8. **Correctness read of the actual fix** — `thresholds-from-samples` in
   `flow_watchdog_lib.bb` now reads
   `(when (>= warn-ms min-warn-ms) ...)` — returns `nil` (no specs entry, falls
   through) when the raw warn percentile sits below the gate, replacing the old
   `(max min-warn-ms (long warn-raw))` floor-clamp. Matches invariant 1 exactly.
   Escalation still forced strictly above warn (`(max (inc warn-ms) ...)`) —
   invariant unaffected.

9. **Full project unit suite (vitest, `extension/`)** — attempted; **pre-existing,
   unrelated environmental failures found, not a BL-835 regression**. Host load
   at time of run: `uptime` → load averages ~109-111 on 4 cores (~27x). This
   worktree's own `.test-durations.jsonl` shows the full suite has recorded
   `"result":"fail"` on every run for the last 2+ days (15 consecutive records,
   2026-08-04 through 2026-08-06), independent of any single ticket. The
   `.vitest-report.json` from the most recent full run names 6 failing files
   (`bounceDrain`, `briefingDigestLineCli`, `dependencyGateCliReportsAndScope`,
   `dependencyGateCliStorageGlobals`, `renderBriefingDiagramsCli`,
   `startBridgeHeadlessCli`) — **none touch `flow_watchdog` or anything in this
   parcel's diff** (`git diff --stat -- extension/src extension/media` against
   the merge-base is empty, confirmed by architect and hardener passes above).
   Re-ran the failing files standalone: `bounceDrain.test.js` passes cleanly in
   isolation (21/21, 370ms). The other three re-run standalone
   (`dependencyGateCliReportsAndScope`, `startBridgeHeadlessCli`,
   `renderBriefingDiagramsCli`) still failed under the same host load, with
   failure signatures that are subprocess-timing races, not assertion
   mismatches: `Test timed out in 20000ms` waiting on a spawned CLI subprocess,
   and a CLI subprocess producing empty stdout before its own startup line was
   written (`BRIDGE_LISTENING port=...` never appeared) — consistent with CPU
   starvation under 27x load, not code defects. This exact class of failure is
   already tracked: `backlog/paused/BL-815-classify-five-unit-suite-timeout-failures.yaml`
   and `backlog/paused/BL-822-resource-anomalies-miss-host-load-spikes.yaml`.
   Not blocking this ticket: pre-existing, unrelated to the parcel's diff,
   already ticketed for separate resolution.

10. **Scope guardrails** — diffed against the ticket's `out_of_scope` list
    (BL-650, `*->*|type` reintroduction, BL-827's ceiling options, snooze/
    unsuppressable posture, global defaults): none touched. Pre-existing
    untracked `swarmforge/scripts/operator_path_lib.sh` (matches paused BL-796)
    left untouched, not staged.

## Blocked checks

None. Every check above ran to completion or is explicitly recorded as a known,
pre-existing, unrelated environmental condition (item 9).
