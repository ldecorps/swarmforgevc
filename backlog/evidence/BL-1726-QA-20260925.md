# BL-1726 — QA review pass, 2026-09-25

3 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: git diff --name-only origin/main 785ebef0fc -- extension/test ; grep -rln 1726 extension/test
- **Commit hash**: 785ebef0fc
- **First error excerpt**: (empty) - no *.property.test.js in the parcel encodes BL-1726 declared invariant, and no stated non-encodability reason in any BL-1726 commit (c44c604e42, a60fc0e4cf) or evidence file
- **Failure class**: invariant-unencoded
- **Expected vs observed**: a coder-authored, non-vacuous property test for invariant 1 (never signal a llama-server lacking both marks: lib/ollama executable AND --model blob), per coder.prompt BL-654
- **Blamed role**: coder
- **Remediation pointer**: extension/test/bl1726*.property.test.js driving orphan_janitor_lib.bb reapable-ollama-ghost? over a CONSTRUCTED cmdline space (lib-path x blob-model x llama.cpp/PATH-resolved/ollama-runner shapes) x parent/age/window flags, reach floor asserted, shown red against a broken predicate (BL-1705 property test is the pattern). Routed as a note to specifier+coordinator per QA.prompt spec-failure table

## D2

- **Failing command**: git diff --name-only origin/main 785ebef0fc (qa_e2e step 4)
- **Commit hash**: 785ebef0fc
- **First error excerpt**: specs/features/BL-1705-the-orphan-janitor-reaps-ghost-ollama-runners-and-detached-run-clients.feature (Examples row 2 rewritten), specs/pipeline/steps/bl1705OllamaGhostJanitorSteps.js (audit assertion cmd=llama-server -> llama-server), swarmforge/scripts/test/bl1705_ollama_ghost_janitor_acceptance_runner.bb - none in the ticket Scope list
- **Failure class**: spec-gap
- **Expected vs observed**: the spec names every file the fix must touch; qa_e2e step 2 requires the BL-1705 feature green, but its row 2 (bare llama-server, parent init, reaped) contradicts BL-1726 FIRM rule, so the coder hand-authored Gherkin (coder.prompt Does Not Own)
- **Blamed role**: specifier
- **Remediation pointer**: specifier ratifies (or re-authors) the BL-1705 Examples row 2 amendment and the loosened step assertion, and adds the three BL-1705 paths to the BL-1726 Scope; the edit itself is behaviourally correct (BL-1705 feature 8/8 green) and is NOT reverted

## D3

- **Failing command**: grep -n audit!\|log! swarmforge/scripts/orphan_janitor_sweep_lib.bb
- **Commit hash**: 785ebef0fc
- **First error excerpt**: the ollama-ghost branch (orphan_janitor_sweep_lib.bb ~267-281) audits only on reap; a kept llama-server is counted in swept N candidate(s) but no line names it as not ollama own
- **Failure class**: spec-gap
- **Expected vs observed**: ticket What-is-wanted bullet 2: a kept llama-server is still classified (seen, logged as not ollama own) - no scenario and no qa_e2e step encode the logged half, and the target (stdout log! vs orphan-janitor-audit.log) is unstated
- **Blamed role**: specifier
- **Remediation pointer**: specifier states whether a per-process kept line is required (then a scenario + target log) or strikes logged (classification + candidate count already satisfy seen, never silently unscanned)

By QA.

## Detail

Parcel: documenter 9c10e2985f, merged into QA as ffc91dbac2, synced with
origin/main e1fefff4cd -> tested tip 785ebef0fc. Disposition: every item is
spec-side (D1 per QA.prompt's spec-failure table; D2, D3 spec-gap), so no
git_handoff bounce, no revert, no record-bounce.js call; one note to the
specifier and coordinator. Not approved, not landed.

Gates (run or blocked, Article 4.4):
- qa-sibling-check status: VERIFY BL-1726 (exit 0).
- Standing-red register: 4 rows, all owned (BL-1724 unit bl1652, BL-1725
  property bl1650, BL-1727 property bl1703, BL-1728 shell); unowned [].
  No row names BL-1726.
- pre_qa_gate.sh BL-1726 785ebef0fc (required_wiring): OK.
- Unit (npm test): the qa-gather run exited 1. Its failing file is not
  recoverable, because the gather builds register_join from the tail-bounded
  excerpt, which held only stderr noise, so register_join came back [].
  A second run with the full log (tmp/BL-1726-unit.log) was green:
  Test Files 637 passed (637), Tests 10867 passed (10867). Not called flaky
  or deterministic; the first run's failing file is unknown.
- Properties (npm run test:properties): ONE lane run (via qa-gather), exit 1.
  The excerpt shows only `[vitest-worker]: Timeout calling "onTaskUpdate"`
  (BL-871 allowlisted), and its failing-file list was lost the same way. The
  parcel touches no *.property.test.js. The one property file that exercises
  the touched lib, test/bl1705OllamaGhostJanitor.property.test.js, ran alone:
  3/3 green. The lane was NOT re-run. The returning parcel gets its one run
  with a full log.
- Acceptance: run_acceptance.sh BL-1726 feature 5/5 ok; node specs/pipeline/cli.js
  BL-1726 feature pass 5 fail 0; run_acceptance.sh BL-1705 feature 8/8 ok.
- bb: orphan_janitor_lib_test_runner.bb ALL CHECKS PASSED;
  bl1705 bridge `sweep-two-ollama`: ghost reaped, owned kept, audit names the
  marked cmdline.
- qa_e2e step 3 (live read, no signal sent): loaded minicpm5-2b (keep_alive
  120s) under ollama serve pid 730, whose runner is pid 5222
  `/mnt/d/dev/ollama/lib/ollama/llama-server --model /home/carillon/.ollama/models/blobs/sha256-ec2d58...`.
  reapable-ollama-ghost? with :parent-live-ollama-serve? false -> true
  (reaped); the same cmdline with the executable swapped to
  /home/u/llama.cpp/build/bin/llama-server -> false (kept); the real cmdline
  with a live serve parent -> false.
- qa_e2e step 4: fails as D2 (three BL-1705 paths outside Scope).
- Wiring: orphan_janitor_sweep_lib.bb:267-281 calls
  ollama-ghost-candidate-cmdline? and reapable-ollama-ghost?; the sweep runs
  in operator_runtime.bb and onboarder_supervisor.bb (BL-1705).
- Article 4.5: changed production path orphan_janitor_lib.bb is covered by
  orphan_janitor_lib_test_runner.bb (8 new BL-1726 assertions).
- Docs: Specification.MD Last-Updated entry is accurate (the only janitor
  reference carrying BL-1705's ollama text). No diagram trigger.
- Stragglers: none of mine before or after. Live vitest pids 8107/9470
  (.worktrees/coder) and 18412/19631 (.worktrees/coder2) belong to other
  roles' runs and were left alone.

Upstream note: the architect NONE (180506630d) passed D1, which
architect.prompt's Invariants Review makes a send-back.

Observed, unticketed (grep of backlog/ for failingFilesFromRow/register_join
finds only evidence files): qaGather.ts failingFilesFromRow parses
row.excerpt (tailExcerpt-bounded) for unit/properties. So a red lane whose
tail is stderr noise or BL-871 timeouts reports register_join [], which reads
as "no failing file". BL-1554's D1 fix moved only the register parse to raw
stdout.
