# BL-1605 — coder, socket-fixture-root bounce, 2026-09-17

## QA bounce (D1, backlog/evidence/BL-1605-QA-20260917.md)

`extension/test/socketFixtureShortRootGuard.test.js` fails on this
ticket's own tree: `specs/pipeline/steps/bl1605NoReverseCopyToForwardRecipientSteps.js`
rooted its fixture at `os.tmpdir()` (line 44) — long on macOS (resolves
under `/var/folders/<hash>/<hash>/T/`), overrunning
`swarm_socket_lib.bb`'s 100-char socket-path guard once
`.swarmforge/tmux/<n>.sock` is appended. Green on `main` (16/16, the
handler isn't landed there); red on every tree carrying this parcel.
Confirmed the specifier's adjudication
(backlog/evidence/BL-1605-specifier-adjudication-of-hardender-socket-fixture-root-note-20260917.md):
this ticket's own coder-domain defect, not an unowned red.

## The fix

`specs/pipeline/steps/bl1605NoReverseCopyToForwardRecipientSteps.js`:
`makeFixtureRoot`'s `fs.mkdtempSync(path.join(os.tmpdir(), 'bl1605-'))`
swapped for `mkSocketFixtureRoot('bl1605-')`
(`specs/pipeline/steps/lib/socketFixtureRoot.js`, the same short-base
helper BL-1290 already landed for the two older offenders and BL-1604/
BL-1610/BL-1481's own fixtures use) — the `node:os` import, now unused,
removed with it. Line 73's `.swarmforge/tmux-socket` write is unchanged,
per the adjudication ("the socket write at line 73 stays").

`backlog/standing-reds.tsv`: removed the row the specifier's amendment
added (`unit  extension/test/socketFixtureShortRootGuard.test.js  BL-1605`)
in the same commit — the row's own text says it "leaves in BL-1605's land"
and a row surviving this ticket's close would read unowned and throttle
intake to 1 (BL-1429).

## Merge note: a silently-dropped file caught by the guardrail

Merging QA's forwarded commit (4e4e60a6a5) hit the
"merge deletes a path... not named in the commit message" pre-commit
guard: `extension/vitest.bl1599.stryker.config.mjs`, a pure one-sided add
on QA's side (absent at the merge-base and on my side both, no rename, no
conflicting change), was missing from the staged merge result. Verified
no other MERGE_HEAD path was similarly dropped
(`git diff --cached --diff-filter=D --name-only MERGE_HEAD` empty after
restoring), then restored it directly (`git checkout MERGE_HEAD --
extension/vitest.bl1599.stryker.config.mjs`) before completing the merge
commit. Root cause not chased further (outside this ticket's scope); the
guard did its job.

## Verification

- `cd extension && npx vitest run test/socketFixtureShortRootGuard.test.js`
  → 16/16 green (was: 1 failure on `bl1605NoReverseCopyToForwardRecipientSteps.js`).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1605-*.feature`
  → 5/5 pass, unchanged.
- `bb swarmforge/scripts/test/reverse_hop_lib_remove_forward_recipients_runner.bb`,
  `bb swarmforge/scripts/test/reverse_audit_handoff_test_runner.bb`,
  `bb swarmforge/scripts/test/bl1299_reverse_hop_property_runner.bb` (500
  runs, all reach floors) → all green, unaffected (this fix touches only
  the JS fixture's temp-root call, no production `reverse_hop_lib.bb`
  change).

By coder.
