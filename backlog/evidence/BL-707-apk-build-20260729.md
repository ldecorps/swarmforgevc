# BL-707 — debug APK build (2026-07-29)

Portable toolchain under `.swarmforge/` (gitignored):

- Temurin JDK 17 → `.swarmforge/tooling/jdk-17`
- Android cmdline-tools + platform 34 / build-tools 34.0.0 → `.swarmforge/android-sdk`
- Gradle wrapper 8.7 in `android/`

Command: `cd android && ./gradlew :app:assembleDebug` →
`android/app/build/outputs/apk/debug/app-debug.apk` (~5.6MB).

Sideload link posted to Cursor Remote; pair with bridge tunnel base URL +
`bridge-token` bearer; grant overlay; Start bubble.
