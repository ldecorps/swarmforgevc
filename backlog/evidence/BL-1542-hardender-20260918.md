# BL-1542 hardener — own-diff hardening pass, 2026-09-18

Scope: BL-1542's own diff only — `extension/src/bridge/webUiTicketStripCollapseRoutes.ts`
(new file), and the new/modified surface of `extension/src/bridge/webUiFontSizePreference.ts`
(`readTicketStripCollapsedMap`, `readFontSizeMap`, `writeWebUiFontSizePreference`'s
collapsed-map branch, `readWebUiTicketStripCollapsed`,
`resolveWebUiTicketStripCollapsed`, `writeWebUiTicketStripCollapsed`,
`isWebUiTicketStripCollapsedWriteRequestShape`). Pre-existing functions in
the same file the diff does not touch (`isWebUiFontSizeWriteRequestShape`,
`readPreferenceMap`, `clampForSurface`) are handed to the specifier as
unowned debt — see `BL-1542-hardener-preexisting-debt-census-20260918.md`.

## Mutation (Stryker, scoped detached run, `--ignoreStatic --concurrency 6`)

Ran `--mutate out/bridge/webUiTicketStripCollapseRoutes.js,out/bridge/webUiFontSizePreference.js`
via a registered `detach_job.sh` run against a ticket-scoped Vitest config
(`extension/vitest.bl1542.stryker.config.mjs`, excluding two unrelated
known-red property files) — `extension/stryker.config.json` was pointed at
it only for the run and reverted before this commit (its own standing
rule). 49 survived/no-coverage mutants surfaced, all in the pre-existing
functions listed above and in the census file; zero survivors in BL-1542's
own diff at the end of this pass. 16 unit tests were added directly against
the survivor list to close every mutant reachable from the new code
(see the diffs in `webUiFontSizePreference.test.js` and
`webUiTicketStripCollapseRoutes.test.js`, each carrying an inline comment
naming the mutant it discriminates — argument-order/`&&`-vs-`||`,
`null`-vs-`typeof`-object guards, StringLiteral path segments, the
`Object.keys(...).length > 0` write-key-omission guard, route-matcher
`startsWith` vs substring, and the `surfaceFromUrl` `?`-detection branch).

## CRAP

`readTicketStripCollapsedMap` (new function) measured complexity=7,
100% coverage, CRAP=7.00 — over threshold on a function this ticket's own
diff introduced. Fixed by replacing a ternary (`existing ? existing.x :
undefined`) with optional chaining (`existing?.x`), which removes one
counted decision point (ConditionalExpression) with no behavior change;
re-measured at complexity=6, CRAP=6.00. Confirmed no other function in the
diff regresses versus `main` (differential complexity gate) — the two
flagged pre-existing functions (`isWebUiFontSizeWriteRequestShape`,
`readPreferenceMap`) have unchanged bodies in this diff (only new call
sites), so this is not a regression, it is the same grandfathered debt the
census file already names.

## DRY

`npx jscpd --config .jscpd.json` over both changed source files: 0 clones,
0% duplication.

## Suite / acceptance

- Full unit suite: 628 files, 10757 tests, all green (the 354
  `[vitest-worker]: Timeout calling "onTaskUpdate"` errors are the
  allowlisted BL-871 noise, not failures).
- `run_acceptance.sh` on `specs/features/BL-1542-live-screen-ticket-strip-collapse-control.feature`:
  6/6 scenarios pass.
- No property test files were touched by this ticket; `test:properties`
  not required per the coder/QA lane split (BL-1618).

By hardender.
