# BL-880 — QA pass, 2026-08-12

Commit verified: 3c6b2952ab (documenter). Confirmed ancestors: coder
4a740ba96, hardener 69830cfc5, documenter 3c6b2952a — all
`git merge-base --is-ancestor <commit> 468acfff9` (QA merge) hold. Merged
(no-ff) into QA at 468acfff9.

## Diff scope confirmed

`git show 3c6b2952ab --stat` (documenter, full chain from `paused/`
promotion): 17 files — `swarmforge/scripts/swarm_handoff.bb`,
`swarmforge/scripts/pre_qa_gate_gather_lib.bb`,
`swarmforge/scripts/acceptance_pointer_gate_lib.bb` (new), three new
`swarmforge/scripts/test/` runners, the promoted `.feature` file (from
`.feature.draft`) + its new step-handler file + `steps/index.js`
registration, `swarmforge/handoff-protocol.md`,
`docs/how-to/BL-531-handoff-refusal-remedies.md`, `Specification.MD`
changelog, and the ticket's own YAML/topic files. No `extension/src`
TypeScript touched — pure Babashka + spec/docs, consistent with the
ticket's declared "no required_wiring" and "degraded gate" notes.

## In-scope checks (all green, run directly by this role)

1. **required_wiring** — none declared for this ticket (confirmed in its
   own `notes:`); the mechanism lands inside `swarm_handoff.bb`'s existing
   send path, which every handoff already exercises.
2. **Wiring confirmed live, not just unit-tested in isolation** —
   `pointer-gate-errors` (swarm_handoff.bb:223) is genuinely called from
   `validate` (swarm_handoff.bb:317-318), alongside the existing
   `pre-qa-gate-errors`. Its `pointer-findings-for-git-handoff`
   (pre_qa_gate_gather_lib.bb:418) correctly short-circuits via
   `gate-armed?` to skip the QA edge, leaving Check D (BL-761) as the sole
   evaluator there — matches the ticket's invariant 3 exactly.
3. **Invariants read directly against the code**: (1) pre-QA refusal keys
   only on existence, never draft-ness/step-resolution — confirmed, the
   probe is a single `git show <commit>:<path>` class check with no Gherkin
   parsing; (2) only a readable-tree-with-path-absent fails closed,
   infrastructure trouble fails open with a warning — confirmed in
   `pointer-gate-errors`' warning/finding split; (3) the QA edge's BL-761
   evaluation is unchanged — confirmed, `pre-qa-gate-errors` (the QA-edge
   path) is untouched by this diff and still called unconditionally.
4. **Unit suite** (`npm test`) — full run: 7435/7440, 5 failures across 3
   files (`dependencyGateCliReportsAndScope`, `mermaidRender`,
   `renderBriefingDiagramsCli`) — none touch this ticket's diff. Host was
   under extreme, sustained contention throughout this pass (load average
   ranged 21–210 on a 4-core machine across repeated `uptime` checks). All 3
   flagged files re-run together in isolation: 15/15 pass cleanly (51.6s).
   Compile (`npm run compile`) clean.
5. **Property suite** (`npm run test:properties`) — two full runs: first
   232/237 (5 failures, all `[vitest-worker]: Timeout calling
   "onTaskUpdate"` artifacts per host load), second (JSON reporter)
   223/237 (14 failures) under even heavier contention (load peaked 210).
   Of the 14, 12 were the same generic timeout artifact; 2 carried genuine
   fast-check counterexamples
   (`bl796NvmNodePathFollowUpAdoptInvariants`,
   `bl797MutationGateProbeCrashFallback`) — neither touches this ticket's
   diff. Both re-run in isolation: `bl797` passed clean (1/1, 156s, a
   real-subprocess-heavy test). `bl796` failed once more paired with
   `bl797` (a different counterexample than the full-run one — consistent
   with flakiness, not a stable bug), then passed clean fully isolated with
   a single worker (3/3, 40.7s). Matches the SAME flake this role's prior
   BL-679 pass (backlog/evidence/BL-679-qa-pass-20260812.md) independently
   hit and cleared the same way. No orphaned `node --test`/`stryker`/vitest
   processes before or after (`pgrep` confirmed clean).
6. **BL-880-specific bb runners, independently re-run, all green:**
   - `acceptance_pointer_gate_lib_test_runner.bb` — ALL PASS
   - `pre_qa_gate_gather_lib_acceptance_pointer_test_runner.bb` — ALL PASS
   - `bl880_acceptance_pointer_gate_property_runner.bb` — 300 runs/property,
     ALL PROPERTIES HOLD (all 3 declared invariants non-vacuous, including
     the QA-edge-arming short-circuit)
7. **Gherkin lint gate** — `gherkin_lint_gate.sh` on
   `specs/features/BL-880-stale-acceptance-pointer-refused-at-first-hop.feature`:
   clean parse.
8. **Formal acceptance gate** —
   `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-880-stale-acceptance-pointer-refused-at-first-hop.feature`
   — 5/5 scenarios pass, driving the real `swarm_handoff.bb` send path
   against real fixture tickets/commits, never a reimplementation.
9. **Docs currency** — `swarmforge/handoff-protocol.md` gained a full
   "First-Hop Acceptance-Pointer Gate (BL-880)" section matching the code's
   arm/skip/fail-open/fail-closed behavior exactly;
   `docs/how-to/BL-531-handoff-refusal-remedies.md` gained the new
   acceptance-pointer refusal class and remedy; `Specification.MD`
   changelog entry accurately describes the gate.

## Verdict

PASS. The early, existence-only acceptance-pointer gate arms at every
pre-QA `git_handoff` hop exactly as specced, correctly excludes the QA edge
(Check D unchanged), fails open on infrastructure trouble and closed only
on a readable-tree-with-path-absent, and is genuinely wired into
`swarm_handoff.bb`'s live send path. All 3 invariants hold under 300-run
property testing. Unit + property + acceptance + Gherkin-lint suites green
modulo confirmed host-load flakes in files this ticket never touched, each
independently re-verified clean in isolation.

By QA.
