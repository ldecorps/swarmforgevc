# BL-1273 QA Pass — 20260829

## BL-1241 entangled-tip rebuild (see also the ticket's own notes field)

The documenter's handoff named commit `67d0c18c1f`. Its ancestry carried two
OTHER tickets' unlanded, unapproved work: BL-1246 (coder commit `3c07857a5`,
still blocked at its own handoff by the task-scope gate) and a cleaner batch
"BL-1267-BL-1268-BL-1220-BL-603-step-scoping-fix" (commit `205fdd36f`) whose
symptom looks like the one I already landed separately as BL-1277
(`e3980cba2`) — neither sibling is approved/landable, so per my own role
prompt's BL-1241 procedure I rebuilt tip-pure rather than bouncing: cherry-
picked the three plain, cleanly-scoped BL-1273 commits (coder `9747e6c1d`,
hardener `a5b065e0ca`, documenter `67d0c18c1f` — verified individually via
`git show --stat` that none touches a path outside BL-1273's own nine files)
onto current `origin/main`. `abandoned_commits` on the ticket records the
superseded chain. Sent the specifier a `note` (priority 00) naming both
entangled siblings.

While auditing that lineage for other casualties (the same abandoned branch
also carried commits from the master checkout, pulled in by the coder's own
routine "merge main into my worktree" sync), I found six further commits
present ONLY in the abandoned lineage and absent from `origin/main` —
matching the actively-recurring `master_main_reconcile` reset-sweep casualty
pattern origin/main's own tip already documents
(`backlog/evidence/BL-1251-rearmed-sweep-destroyed-a-mint-20260829.md`).
Each is a small, self-contained, backlog-bookkeeping-only change with no
overlap with BL-1246 or the step-scoping batch — rescued by `cherry-pick -x`
in chronological order:

- `670473039` Record third BL-1251 open-slot nudge clearance (coordinator)
- `795d79a2f` Remove per-provider-usage intake from backlog root (empty pick
  — already satisfied on `origin/main`'s own history, skipped)
- `9296711a7` BL topic record for BL-603
- `b06b75cde` Approve BL-1275: record human_approval
- `f13403676` Approve BL-1274: record human_approval
- `ae503cbe6` BL-1246 handoff-blocked-by-task-scope-gate evidence record
- `8521d22d5` BL-1062: remove stale debt/ copy (conflicted — `origin/main`
  already has the equivalent re-adjudication via `cf7241ca0` on a different
  path; skipped as redundant, confirmed no debt/ copy survives either way)

Net: two human_approval records (BL-1274, BL-1275) and three other
bookkeeping commits that were invisible from `origin/main` are now landed
alongside BL-1273. Nothing outside these accounted-for commits remains
unexplained in `origin/main..67d0c18c1f` (checked every non-merge commit in
that range; merge commits carry no unique content beyond their non-merge
parents, verified for the QA-approved BL-603/BL-1220/BL-1268 merges
specifically by confirming their real second-parent payload commits are
already `origin/main` ancestors).

## Commit under review

`2701ad794` (tip of my rebuilt+rescued chain on `swarmforge-QA`). BL-1273's
own three commits (`8feaa2ad2`/coder, `6165889ff`/hardener, `09f0dae07`
/documenter — content-identical cherry-picks of `9747e6c1d`/`a5b065e0ca`
/`67d0c18c1f`) are ancestors of it. `git diff --stat origin/main
2701ad794` touches exactly BL-1273's own nine files plus the six rescued
bookkeeping files — confirmed no BL-1246 or step-scoping paths present.

## required_wiring — all four anchors confirmed present and correct

- `front_desk_supervisor.bb`: `append-self-heal-event!` at lines 293
  (`:type "stale-build-recompile"`) and 480 (`:type "supervisor-respawn"`).
- `handoffd.bb`: `append-self-heal-event!` at line 407
  (`:type "claim-heal"`).
- `handoff_lib.bb`: load-file (line 48) AND a call (line 1000,
  `:type "rotation-respawn"`) — no longer a dead dependency edge
  (`grep -c self_heal_telemetry` = 2, was 1).
- `kill_pipeline_swarm.sh`: shells `self_heal_telemetry_cli.bb` at line 330
  on the SUCCESS path.

All four :type literals match the Outline's own `<event_type>` column
exactly (the hardener's own mutation finding — a blank/wrong :type is now
caught, verified by re-reading the actual site content, not just the
hardener's evidence).

## Invariants held

- No host load-files `self_heal_telemetry_lib.bb` without calling it
  (handoff_lib.bb confirmed above).
- Every emit is a pure observer beside its existing prose-log anchor, never
  gating control flow (confirmed by reading each site: `do` keeps prose-log
  first at the :started arm, and each comment states the append swallows
  its own failures).

## Constraints held

- `extension/test/selfHealTelemetry.property.test.js`: untouched
  (`git diff origin/main HEAD` empty for this path; `git log
  origin/main..HEAD -- <path>` empty).
- `docs/how-to/BL-597-trend-self-heal-events.md`: still claims the emits
  happen at the existing prose-log sites — now true again, not reworded
  away.
- No change to `self_heal_telemetry_lib.bb`, the aggregator, or the trend
  computation (not in the diff).
- No new detector: every site's anchor was one that already logged a prose
  line pre-BL-1273 (verified by reading each site).

## This pass's verification (qa_e2e_procedure)

1. **Compile**: `cd extension && npm run compile` → clean, exit 0.
2. **Unit suite**: `npx vitest run` → 20 failed files / 33 failed tests /
   9584 passed (571 files total). All 20 failing files cross-checked
   against BL-1273's changed-file list — zero overlap. Matches the exact
   count the coder's own BL-1246 evidence recorded the same day ("20
   failing files, 33 failing tests") — standing debt, not a regression.
3. **Property suite**: `npm run test:properties` → 26 failed files / 15
   failed tests / 729 passed (268 files), plus the one allowlisted
   `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871) unhandled
   error. `selfHealTelemetry.property.test.js` NOT in the failing list —
   confirmed separately in isolation: `npx vitest run --config
   vitest.properties.config.mjs test/selfHealTelemetry.property.test.js` →
   7/7 pass, including `invariant1` and `invariant1 property` (the reds
   this ticket exists to fix). None of the 26 failing files touch
   self-heal telemetry or any of BL-1273's four changed scripts.
4. **Acceptance**: `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-1273-restore-self-heal-telemetry-emit-sites.feature`
   → 7/7 pass (all five host emit checks, the no-dead-load-file check, and
   the property-suite-passes check).
5. **Article 4.5 changed-path unit/wiring tests**: ran
   `self_heal_telemetry_lib_test_runner.bb` (ALL PASS),
   `front_desk_supervisor_lib_test_runner.bb` (ALL PASS),
   `handoff_lib_test_runner.bb` (ALL TESTS PASSED), the four direct
   `test_front_desk_supervisor_*.sh` wiring tests, four `kill_pipeline_swarm`
   -adjacent wiring tests, and all 37 `test_handoffd_*.sh` files that
   source `handoffd.bb`. Two pre-existing reds surfaced and were each
   confirmed against an `origin/main` baseline via a detached worktree
   before ruling them unrelated:
   - `test_front_desk_supervisor_bl622_refusal.sh`: 3 sub-checks fail
     because `front_desk_supervisor.bb`'s load-file prologue requires
     `daemon_log_freshness_pulse_lib.bb` (added by BL-784, already on
     `origin/main`, unrelated to BL-1273), which this test's own
     `make_fixture` never copies — reproduced byte-for-byte identically
     against a materialized `origin/main` copy of the same seven files
     this test copies (same crash, same rc, same "exits non-zero"
     coincidentally satisfying the first check while every log-content
     check fails because nothing was ever logged). BL-784 is already
     `done/`; this is an untracked fixture-drift regression from that
     ticket, not from this one. Not blocking BL-1273 — reported separately
     below rather than bounced here.
     Full inventory: of 45 changed-path test files run, 7 failed
     (`test_handoffd_chase_sweep_wiring.sh`, `test_handoffd_supervisor.sh`,
     `test_handoffd_closing_context_clear_wiring.sh`,
     `test_handoffd_master_checkout_drift_wiring.sh`,
     `test_handoffd_notify_verified.sh`,
     `test_handoffd_wake_attribution_wiring.sh`,
     `test_handoffd_push_sweep_wiring.sh`). Every one of the 6 handoffd
     daemon-sweep/timing tests reproduced byte-for-byte identically (same
     FAIL/AssertionError line, same rc) against a detached `origin/main`
     worktree — pre-existing, unrelated to BL-1273's single additive
     claim-heal emit call (none test the resume/claim-heal site itself).
     `test_handoffd_push_sweep_wiring.sh` PASSED cleanly on the same
     `origin/main` baseline check, so it was re-run once more in this
     worktree in isolation (git now quiescent, no concurrent cherry-picks)
     and passed clean too — its one failure was self-inflicted transient
     git-lock contention from running my own rescue cherry-picks
     concurrently with this test batch's real git fixtures, not a code
     defect.
   - `test_handoffd_one_shot_flags_parse.sh` (Article 4.5's own worked
     example) passes clean.
6. **Live end-to-end (writer proof, without tearing down the live swarm)**:
   the running swarm (8 live tmux role sessions) made running
   `kill_pipeline_swarm.sh` for real destructive and out of proportion for
   this check — the feature file's own Background note already flags this
   step as needing "a running swarm" with "no runnable binding here."
   Substituted the equivalent, non-destructive proof the ticket's own CLI
   exists for: `bb swarmforge/scripts/self_heal_telemetry_cli.bb <scratch-
   root> "a kill_all_swarm invocation"` against an isolated scratch
   project-root (never the live swarm's real `.swarmforge/`) → exit 0,
   `.swarmforge/telemetry/self-heal-2026-08.jsonl` created with exactly one
   line, `{"type":"kill-all-swarm","subject":"lifecycle","reason":"clean
   slate","at":...}`. Confirms the vocabulary the hardener flagged as
   pre-existing and out-of-scope (`kill-all-swarm`, not the ticket
   qa_e2e_procedure's literal "kill_all" wording) — same divergence the
   hardener's own evidence already named, not a new defect. Scratch
   directory removed after.
7. **Regression**: unit suite failure count (20 files / 33 tests) matches
   the coder's own same-day baseline exactly; property suite failure set
   has no new members relative to the standing classes already
   corroborated elsewhere in `backlog/evidence/`.

## Untracked defect found, reported separately (not blocking this ticket)

`test_front_desk_supervisor_bl622_refusal.sh` is red on `origin/main` today
(BL-784's `daemon_log_freshness_pulse_lib.bb` dependency vs this test's own
stale `make_fixture` copy list) — grepped `backlog/` for both the test
filename and `daemon_log_freshness_pulse_lib.bb`; only hit was BL-784
itself (the cause, already `done/`), no open ticket names this specific
fixture-drift symptom. Reported to the specifier by note for triage; not
BL-1273's to fix.
