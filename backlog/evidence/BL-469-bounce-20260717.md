# BL-469 QA bounce — 2026-07-17

1. **Failing command exactly as run:**
   ```
   node check_role_icons.js
   ```
   (a QA-authored one-off script calling the compiled, unmodified production
   functions directly: `getForumTopicIconStickers(process.env.TELEGRAM_BOT_TOKEN)`
   from `extension/out/notify/telegramClient.js`, then
   `resolveIconStickerId(stickers, emoji)` from `extension/out/concierge/topicIcon.js`
   for each of the 8 `ROLE_TOPIC_ICON` entries — the exact real-surface check the
   ticket's own "E2E QA PROCEDURE" and `human_approval` note both call for: "QA
   verifies all 8 actually resolve on the real surface.")

2. **Commit hash checked out and tested:** `1d4d0c6bf2` (documenter's handoff,
   merged into QA's worktree as `c3dcbeca68`).

3. **First error excerpt** (full output — 10 lines):
   ```
   Live sticker set size: 112
   coordinator  🧭  -> UNRESOLVED
   specifier    📝  -> RESOLVED (5373251851074415873)
   architect    🏗  -> UNRESOLVED
   coder        ⌨️  -> UNRESOLVED
   cleaner      🧹  -> UNRESOLVED
   hardender    🛡  -> UNRESOLVED
   QA           🔍  -> UNRESOLVED
   documenter   📚  -> RESOLVED (5350481781306958339)
   ```
   Telegram's real `getForumTopicIconStickers` result (112 stickers) does not
   contain 🧭, 🏗, ⌨️, 🧹, 🛡, or 🔍 as an exact `emoji` field match — only 📝 and
   📚 resolve. (Confirmed by dumping the full 112-emoji live list; the closest
   available stand-ins are 🔎 for QA's 🔍 and 💻 for coder's ⌨️ — neither an exact
   match — and no crane/broom/shield/compass equivalent exists in the set at all.)

4. **Failure class:** `behavior` (intent/behavior mismatch — not compile, not
   unit, not integration/acceptance: the fixture-based acceptance run is green
   because its scenarios assume the mapped icon IS in the live set; the gap only
   surfaces against the real Telegram API, which is exactly why the ticket's own
   E2E procedure calls for this specific check).

5. **Expected vs observed:** Expected — per the ticket's stated intent ("give
   each of the 8 role topics its own icon so they're tellable apart at a
   glance") — all 8 (or a documented few) role topics to receive a distinct,
   visible icon on the real Telegram surface. Observed — only 2 of 8
   (specifier, documenter) can ever resolve against Telegram's live
   `getForumTopicIconStickers` set as currently mapped; the other 6 topics will
   permanently sit at `skipped-unresolved-icon` (logged, never applied),
   indistinguishable from each other and from any other icon-less topic — the
   feature delivers its intent for 2 of 8 roles, not 8 of 8.

## Disposition note (not part of the 5-field contract, for the receiving role)

The CODE is not at fault: `syncPerAgentTopicIcons`/`syncTopicIcon` behave
exactly per Scenario 2's own contract (surface `skipped-unresolved-icon`,
never crash, never block the other roles) — that machinery is correct and
already proven by both the unit suite and the fixture-based acceptance run.
The defect is that the ticket's FIRM, human-approved icon literals
(compass/crane/keyboard/broom/shield/magnifier) were validated against
Telegram's live sticker set for only one of the eight originally (🎼 →
🧭 during the specifier's 2026-07-16 clarifying question) — the other seven
were never checked against the real `getForumTopicIconStickers` set, and six
of them are not in it. This is not something the coder can fix by writing
different code: it requires new icon choices (from the 112 the live set
actually offers) and fresh human approval, i.e. a spec correction. Flagging
to the specifier via a separate `note` alongside this bounce so the mapping
can be re-picked against the real set and re-approved, rather than leaving
this to stall in the coder's queue for a decision outside its authority.
