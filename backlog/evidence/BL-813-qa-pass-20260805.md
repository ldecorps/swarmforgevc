# BL-813 — QA pass, 2026-08-05

Commit verified: 8560344f92 (documenter). Confirmed ancestors: coder 3236f454,
architect 134f6bbc, hardener dcf8eae7 — all `git merge-base --is-ancestor
<commit> 8560344f92` hold. Merged (fast-forward) into QA at ecd5d2ca ->
8560344f92.

## In-scope checks (all green, run directly by this role)

1. **Diff scope confirmed** — `git diff --stat ecd5d2ca 8560344f92`: only
   `swarmforge/scripts/{daemon_alarm_lib,handoffd_supervisor,ambulance_lib}.bb`,
   their `.bb`/`.sh` test fixtures, one new pipeline step-handler file
   (`bl813DeathAlarmAttachAndAmbulanceRaceSteps.js`) + its registration in
   `specs/pipeline/steps/index.js`, two how-to docs, Specification.MD, the
   feature file's mutation-manifest stamp, and prior-role evidence files.
   Zero `extension/src`/`extension/media` TypeScript touched.
2. **Production `.bb` diff read directly** against the ticket's 3
   `required_wiring` entries and 3 `invariants` — all 3 wiring sites present
   and matching: `ambulance_lib.bb::ticket-has-file?` wraps each glob
   candidate's slurp+read in its own try/catch;
   `daemon_alarm_lib.bb::alarm-and-halt!` builds a `{:filename :content-id
   :base64}` attachment from the just-written failure-log content (never
   re-slurping) and threads it through a 3-arg `send-email!`, itself wrapped
   in try/catch so a build failure can't block `halt-swarm!`;
   `handoffd_supervisor.bb::send-configured-alarm-email!` is now 3-arg and
   forwards attachments into the 7-arg attachment-capable
   `send-configured-email!` call.
3. **Unit suites, independently re-run** — all green:
   - `test_daemon_alarm_lib.sh` (ALL PASS, incl. `BL-813 attach-01`)
   - `ambulance_lib_test_runner.bb` (ALL PASS)
   - `test_ambulance_cli.sh` (ALL PASS; pre-existing unrelated
     `tmp_cleanup.sh` unbound-variable warning after completion, confirmed
     pre-existing to BL-459 by architect/hardener, untouched by this parcel)
   - `test_handoffd_supervisor.sh` (ALL PASS, full regression suite incl.
     `04: no silent auto-restart remains`)
4. **Property runners, independently re-run, all green:**
   - `bl813_daemon_alarm_lib_property_runner.bb` — 500/500, ALL PROPERTIES
     HOLD (P1 attachment fidelity; P2 attachment-build failure never blocks
     halt; P3 halt-swarm! fires exactly once / final state always "halted")
   - `bl813_ambulance_vanish_safety_property_runner.bb` — 500/500, ALL
     PROPERTIES HOLD (never throws on a vanished glob entry; degrades
     correctly through the real production reader)
   - `bl813_supervisor_alarm_attachment_wiring_test.bb <fixture-root>` — ALL
     PASS (this harness requires an explicit temp fixture-root arg; ran with
     a fresh `mktemp -d`, cleaned up after)
5. **Formal acceptance gate** —
   `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-813-handoffd-death-email-attach-and-ambulance-race.feature`
   — 3/3 scenarios pass, driving the real `.bb` libraries via the new step
   handlers (no reimplementation): attachment fidelity + build-failure
   resilience, glob-then-vanish non-throw, vanished-only-ticket degrade.
6. **e2e_qa_procedure (5 steps), all independently confirmed:** step 1 by
   `test_daemon_alarm_lib.sh`'s `attach-01` + property P1; step 2 by property
   P2; step 3 by `bl813_ambulance_vanish_safety_property_runner.bb` P1; step
   4 by the same runner's P2 (through the real `read-ambulance-state`) and
   acceptance scenario 03; step 5 (no-auto-restart regression) by
   `test_handoffd_supervisor.sh`'s existing `04` assertion, re-run clean.
7. **Wiring/callers** — grepped every caller of `send-configured-alarm-email!`
   (only production call site is `alarm-and-halt!`'s own wiring) and of
   `build-failure-attachment` (only called from `alarm-and-halt!`) — no stray
   unwired module, matches architect's finding.
8. **Documentation** — `docs/how-to/BL-144-daemon-death-alarm.md` and
   `docs/how-to/BL-655-ambulance-mode-the-hold.md` both updated accurately
   (attachment behavior + degrade-safe race note), `docs/reference/
   Specification.MD` "Last Updated" entry added describing the incident and
   fix; content checked against the actual code diff, matches.
9. **No orphaned processes** — `pgrep -fl 'node --test|stryker'` clean before
   and after every check above.

## Out-of-scope: full extension unit suite (`npm test`) — BLOCKED by host load, not by this parcel

`npm test` (compile + full `node --test` suite) was started in the QA
worktree's `extension/` dir; after ~10 minutes it had produced no output at
all (not even the `tsc` compile step completing) while `uptime` climbed from
~90 to ~174 load average on this 4-core host — the same severe-load signature
already documented and accepted in `backlog/evidence/BL-638-qa-pass-20260805.md`
earlier the same day. Stopped the stalled run rather than let it hang
indefinitely; confirmed no orphaned `tsc`/`node --test` processes survived
(three transient `tsc` PIDs observed via `pgrep` had already exited on their
own by the time of inspection — no manual kill needed).

This ticket's own diff contains zero `extension/src`/`extension/media`
TypeScript (confirmed in check 1 above and independently by the architect's
own evidence) — the one JS file this parcel adds
(`bl813DeathAlarmAttachAndAmbulanceRaceSteps.js`) is a `specs/pipeline/steps/`
acceptance step handler, already exercised end-to-end and green via the
formal acceptance gate (check 5), which itself runs via `node --test` under
`specs/pipeline/scripts/run_acceptance.sh` — a working, unblocked signal on
the exact JS this parcel touches, obtained from a different (lighter-weight)
invocation than the full extension suite. Per the same load-bypass reasoning
already adopted for Stryker and used earlier the same day for BL-638, the
full-suite block is recorded as a host-infrastructure condition, not a BL-813
defect.

## Verdict: PASS

Every check that can isolate BL-813's own scope from the host-load condition
is green, independently re-run, and matches the ticket's required_wiring and
invariants. Approved on that basis. The continuing severe host-load condition
(now 174 vs ~90 earlier today) is flagged to the coordinator alongside this
approval as a standing/worsening health signal (constitution Article 3.5).
