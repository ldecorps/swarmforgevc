# BL-1253 — QA pass, 2026-08-31

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1253` → `VERIFY BL-1253` (exit 0).

## Merge

Merged documenter's cited commit `437999fb38` into QA at `f7a815a081`
(`git merge --no-ff`, clean this round — BL-1253's own hold→active
promotion was already absorbed into the branch via BL-1240's merge earlier
this session). Diffed against both parents: additions only, nothing
dropped either direction.

Ancestry re-confirmed for the full chain: coder `94301a6c72`, cleaner
`a007baf9e5`, architect `638f627e8d`, hardener `46135c8b38`, documenter
`437999fb38` are all ancestors of the commit under evaluation (`f7a815a081`)
— not merely ancestors of a busy HEAD (BL-336 discipline).

## Ticket shape — review-only stamp-off (BL-848), not a normal build

BL-1253 confirms or refutes already-landed hotfix `2ec06b6ef1` ("Own
getUpdates when the front-desk inbound feeder is dead"). Constraints:
never reimplement/rewrite/revert the hotfix; never write `certified`/
`waived` into `backlog/hotfix-ledger.yaml` (only a recorded human decision
does); leave `Hotfix-Certification` pending. This is the third pipeline
round: round 1 hardened, QA bounced on scenario 06 (D1: no step handler —
`backlog/evidence/BL-1253-...-bounce-20260830.md`); the coder found the fix
already existed stranded on the coder branch (`026ae2aa3`, never forwarded)
and carried it forward (`BL-1253-coder-rework-scenario06-strand-20260830.md`);
separately a QA-exclusive `main`-red repair
(`BL-1253-qa-main-red-repair-20260830.md`) restored an unregistered handler
left by a partial-resurrection incident on `main` itself — unrelated to
this ticket's own gate, already landed directly, not re-verified here.
This final round (`94301a6c72` onward) re-verifies the already-complete
fix rather than re-fixing it — confirmed by every stage re-running the
suite rather than trusting the prior evidence.

## Stage corroboration

Read all round-3 evidence (`BL-1253-{coder-restamp,cleaner,architect,
hardener,documenter}-20260831.md`). Cleaner: NONE — no code to act on.
Architect: CLEAN, diff of the received commit touches exactly one file
(the coder's own evidence), independently spot-checked the stamp against
`2ec06b6ef1` itself. Hardener: clean, confirmed the ticket's own files
have zero diff since the second hardener pass, so the existing Gherkin
mutation stamp (6/6 killed) is still valid, not stale. Documenter: added
a new how-to page and Specification.MD entry carrying invariant 3,
scenario 06, and the outstanding 90s question verbatim (Article 5.3).

## Own independent verification (BL-112, separate from every prior stage's)

| Command | Result |
|---|---|
| `run_acceptance.sh` BL-1253 feature | 8/8 pass (scenario 5, the previously-bounced one, and scenario 8, ledger-pending, both green) |
| `npx vitest --config vitest.properties.config.mjs` BL-1253 property file | 4/4 pass |
| `npx vitest run` cursorBridgeInboundQueue/telegramCursorBridgeCore/telegramCursorBridgeLive `.test.js` | 258/258 pass |
| `git diff 82347884f3 f7a815a081` scoped to the three hotfix source files + `start_cursor_bridge.sh` | **empty** — invariant 1 (never reimplements) confirmed independently, not taken on trust |
| `grep -A5 2ec06b6ef1 backlog/hotfix-ledger.yaml` | `state: stamp-open`, `human_decision: null` — untouched, invariant 2 holds |
| `grep -n bl1253DeadFeederOwnsGetUpdatesStampSteps specs/pipeline/steps/index.js` | registered, line 899 |
| Direct read of `cursorBridgeInboundQueue.ts` / `start_cursor_bridge.sh` **at commit `2ec06b6ef1` itself** | `frontDeskPollHeartbeatPath`/`readFrontDeskPollHeartbeatMs` present and exported; `start_cursor_bridge.sh` defaults `CURSOR_BRIDGE_INBOUND_QUEUE=0` against a dead feeder unless the operator set it explicitly — matches every stage's citation exactly |

Not re-run: the full `npm test`/`npm run test:properties` lanes — this
parcel's diff against production is empty (confirmed above) and both full
suites were already run twice this session (BL-1308, BL-1240 tips) with
zero drift; a third full run would be redundant given no production
surface changed. Targeted checks above are the correct scope for a
review-only ticket with a verified-empty diff.

## Outstanding — carried forward, not this pass's to close

The **90-second stall-window** question, carried verbatim from retired
BL-1260 into `approval_context` (Article 5.3), remains unanswered:
`DEFAULT_FRONT_DESK_FEEDER_STALL_MS = 90_000`
(`telegramCursorBridgeCore.ts:371`, confirmed live). Whoever takes the
`2ec06b6ef1` ledger decision (`certified`/`waived`) must answer it as part
of that same decision — a green suite is not an answer, and this QA pass
does not settle it either. Also per the ticket's own `out_of_scope`: the
`SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1` override the hotfix landed under,
and whether the skipped reds were genuinely unrelated, stays unverified —
not this ticket's gate.

## Orphan check

`pgrep -fl 'node --test|stryker|vitest'` clean before and after.

## Verdict: APPROVE

BL-1253's own review deliverables (evidence, docs, tests) are complete and
correct. This approval is for the STAMP-OFF TICKET only — it does not
certify or waive the `2ec06b6ef1` ledger row, which stays `stamp-open`
pending a human decision that must include the 90s question above.

By QA.
