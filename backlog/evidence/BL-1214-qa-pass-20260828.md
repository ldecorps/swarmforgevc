# BL-1214 — QA pass — 20260828

**Received:** `merge_and_process documenter 7f1592a118` (handoff
`00_20260828T033553Z_000887_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `7f1592a118...`
**QA merge commit:** `26780d997bab930678b4dd99a764cc37eae0d42f`

Severity `critical` — this defect fired IN PRODUCTION on `main` on
2026-08-28 and destroyed four committed commits, including the human's own
approval of this very ticket. One bounce in flight (architect D1+D2:
ff-success exit-code regression and a stale divergence test), re-fixed and
re-verified before reaching QA. depends_on BL-1198 (already landed this
session, `11db4d42e`).

## Merge

Clean, no conflicts (`git merge --no-ff 7f1592a118`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor 6b08f29d2 HEAD` (coder, original) — CONFIRMED
- `git merge-base --is-ancestor 1f931ec0d HEAD` (coder, D1+D2 re-fix) — CONFIRMED
- `git merge-base --is-ancestor 740c6e895 HEAD` (cleaner, round 2) — CONFIRMED
- `git merge-base --is-ancestor f6ca57dc3 HEAD` (architect, round 2) — CONFIRMED
- `git merge-base --is-ancestor 19810dd6e HEAD` (hardener) — CONFIRMED
- `git merge-base --is-ancestor 7f1592a11 HEAD` (documenter, received tip) — CONFIRMED

## Design review (independent)

`absorb-with-merge!` (`master_main_reconcile_lib.bb:711`) implements
exactly the ladder the ticket asks for: `ff!` → (on failure) `merge!` →
(on failure) `abort!` then `fallback!`, with `fallback!`'s return value
passed through **verbatim** (constraint: conflicting path unchanged). All
three named call sites route through this ONE shared function — confirmed
by grep: `handoffd.bb:3206` calls it directly; `swarm_heal.bb` and
`post_hotfix_merge_origin.bb` both call it indirectly via
`post_hotfix_merge_origin_lib.bb:169`'s `run-post-hotfix-merge!`. No
independent reimplementation anywhere.

## D1 fix re-verified (ff-success exit-code regression)

`post_hotfix_merge_origin_lib.bb:187`:
`(if (:success result) (finish-ok daemon-dir rev-counts! (:outcome result)) result)`
— treats any `:success true` outcome uniformly (both `:ff` and `:merged`),
exactly matching architect's remediation. Confirmed by re-running
`post_hotfix_merge_origin_lib_test_runner.bb`: `ALL TESTS PASSED`.

## D2 fix re-verified (stale swarm_heal divergence test)

`test_swarm_heal_push_before_reset.sh` §2 now asserts merge-and-preserve
(both the origin-landed and local-only commits reachable, tip is a real
2-parent merge commit) instead of the pre-BL-1214 discard-by-reset
expectation. Re-ran: 5/5 PASS, including the corrected §2 assertions.

## Verification inventory (independently re-run)

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1214` | `VERIFY BL-1214` — no open deferral |
| Standing shell test (previously red, item 1) | `test_handoffd_master_main_reconcile_wiring.sh` | ALL SCENARIOS PASS — specifically "a genuine two-way divergence reconciles with BOTH the landed commit and the local-only bookkeeping commit reachable" and "produces a real merge commit, never a rewrite of local history" |
| Consumer shell test (D2) | `test_swarm_heal_push_before_reset.sh` | ALL PASS (5/5), corrected §2 assertions |
| D1 unit test | `bb post_hotfix_merge_origin_lib_test_runner.bb` | ALL TESTS PASSED |
| Shared-lib unit + property | `bb master_main_reconcile_lib_test_runner.bb` / `_property_runner.bb` | ALL PASS; 500 runs, ALL PROPERTIES HOLD, including BL-1198's own push-first non-vacuity confirmation (same file) |
| Post-hotfix property | `bb bl1118_post_hotfix_merge_property_runner.bb` | ALL TESTS PASSED |
| Acceptance (3 independent runs) | `run_acceptance.sh` on the BL-1214 feature | 3/3 PASS every run — covers all 3 qa_e2e_procedure items (non-conflicting divergence absorbed losslessly; conflicting divergence aborts cleanly and falls back unchanged; a pre-existing `MERGE_HEAD` is left entirely alone, BL-1120 unchanged) |
| Constraint: conflicting path unchanged | `fallback!`'s return passed through verbatim (code read) + acceptance scenario 2 + shell test's own conflict scenarios | Confirmed |
| Constraint: never abort a foreign merge | `abort!` called only inside the `(if (:success merge-result) ... (do (abort!) (fallback!)))` branch — only reached after this function's OWN `merge!` attempt failed | Confirmed by reading the function body directly |
| Constraint: no conflicted MERGE_HEAD left behind | Acceptance scenario "leaving no conflicted merge behind" + shell test's aborted-merge scenario | Confirmed |
| Orphaned processes / leaked fixtures | `pgrep -fl '[n]ode --test\|[s]tryker'`, `git status --short` | none, before and after |
| Docs | `docs/how-to/BL-1214-ff-absorb-attempts-real-merge-before-reset.md` present, accurate, includes the bounce-round history; all 3 internal cross-references resolve (`BL-1198`, `BL-891`, `BL-1120` how-to pages all exist); linked from `docs/index.md:201`; `Specification.MD` top entry accurate | Present and accurate |

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1214's own work on this pass.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1214 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1214 active → done).

By QA.
