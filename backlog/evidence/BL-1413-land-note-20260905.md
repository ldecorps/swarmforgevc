# BL-1413 land note — BL-1402 entanglement, cross-ticket doc-file scope trap, live-host confirmation, 2026-09-05

## Merge into QA worktree

A plain `git merge 8c1e839eae` succeeded cleanly (no BL-1403 intake-file
class this time — the documenter's fork point postdated it) but pulled in
~28 unrelated files: **BL-1402**'s own unlanded/bounced work (still
`active`, an architect bounce recorded against it — not `done`), entangled
via a shared ancestor on the documenter's branch. Reset (`git reset --hard`
back to my prior tip) and replayed this ticket's own attributed paths
instead, same recipe as BL-1364/BL-1365/BL-1383: grepped
`git log f7d399ed2f..8c1e839eae` for `^[a-f0-9]* BL-1413:` subjects (4
content commits — coder main pass, cleaner/architect evidence-only,
hardener evidence-only — plus the documenter's own final doc commit),
diffed the union of 17 touched paths.

**Five of those 17 are the trimmed supervisor-log fixtures, which contain
the ticket's own NUL byte and so are BINARY to git** — `git apply` refused
them ("cannot apply binary patch ... without full index line"). Excluded
them from the text patch and copied each directly via `git show
<commit>:<path> > <path>` instead; confirmed byte-identical to the
documenter's committed blobs afterward.

## A NEW trap: collapsing a cross-ticket doc edit into a TAGGED replay commit trips the scope gate

The documenter's own pass extended `docs/how-to/BL-675-daemon-log-
freshness-watchdog.md` — an EXISTING page named for the ticket that first
shipped it (BL-675), correctly done with an untagged original commit
subject (no "BL-1413" in it) per the established convention
(`documenter-cross-ticket-doc-edit-needs-untagged-commit-subject` memory).
My FIRST replay attempt collapsed all 17 files into ONE commit whose
subject named "BL-1413" — `task_scope_gate_cli.bb` then correctly refused:
"this task's own commits ... carry a path (docs/how-to/BL-675-...) belonging
to BL-675 ... tip is entangled with another ticket's work (BL-1192/BL-506)."
The ORIGINAL per-role commits were fine; my collapse broke the convention
by re-tagging content that needed to stay untagged.

**Fix, applied to both the QA-worktree merge and the main-tip replay**:
split into two commits — one tagged `BL-1413: ...` carrying everything
else, one UNTAGGED (`docs: ...`, `By documenter.`) carrying only the
`BL-675-*.md` file. `task_scope_gate_cli.bb` returned `OK` on the tagged
commit afterward. Recorded a pointer to this trap in the existing memory
file for future replays.

## Verification (before landing)

Full independent QA pass (against the correctly-split QA-worktree commit,
`8153ca7b2c` + `6fac24e87e`): `bash test_daemon_log_freshness.sh` — all 15
BL-1413 assertions pass (4 pre-existing unrelated reds — BL-796-01/02/03,
`fleet-telegram` — reproduced and confirmed identical to prior stages'
reports, none touching `heartbeat_age_secs`). Property runner 40/40 runs,
ALL PROPERTIES HOLD, coverage across all four noise placements and both
threshold sides. Acceptance 4/4. Both `required_wiring` anchors confirmed
live by direct grep (`grep -aE` at line 191 inside `heartbeat_age_secs`;
`registerSteps` export). Read the actual walk logic directly rather than
trusting the evidence: confirmed newest-to-oldest iteration
(`i=$line_count` descending), the `epoch -ne 0` parse guard, and the
sentinel only after exhausting every line — matches both invariants and
all four of the hardener's hand-mutation results (mutants A/C/D killed on
exactly the scenarios their own doc comments name; mutant B — dropping
`LC_ALL=C` — genuinely untestable on this host, which has no second
locale to construct a discriminating fixture with, not a hidden gap).

## qa_e2e steps 2-3: live-host confirmation, fully stubbed, no side effects

Ran the fixed checker against the MASTER checkout's real `.swarmforge/`
(the actual production supervisor logs, which still carry the real
2026-08-30 NUL line) with every side-effect command replaced: `FRESHNESS_
KILL_CMD='echo KILL'`, `FRESHNESS_START_CMD='echo START'`, `FRESHNESS_
ANNOUNCE_CMD='echo ANNOUNCE'`, and `FRESHNESS_INCIDENT_FILE` redirected to
a scratch path (never the real `freshness-incidents.log`) — nothing was
actually killed, restarted, announced, or recorded to the live incident
log. No `binary file matches` warning appeared (the one stderr line,
`swarm.env: [[: not found`, is a pre-existing, unrelated `sh`-vs-`bash`
sourcing issue in a different script, reproduced with or without this
fix — not investigated further, out of scope).

The fix correctly reported REAL, non-sentinel heartbeat ages for every
supervisor. Two runs (a few minutes apart) each found a genuinely stale
supervisor — first `front_desk_supervisor` (age 357s, then 410s on a
second run: persistently stalled, last real heartbeat frozen at
06:36:01Z), then also `onboarder_supervisor` (age 51s, transient — its
log showed a fresh heartbeat again moments later). Neither is a BL-1413
defect: this is the fix WORKING — measuring real ages accurately instead
of being blind to them via a NUL byte, correctly leaving the other three
supervisors (handoffd, cursor-bridge, operator-runtime) unflagged as
fresh. Flagged the persistent `front_desk_supervisor` finding to the
coordinator by `note` (priority 50) as a separate, real, currently-open
operational concern — not this ticket's to fix or block on.

**Incidental, unrelated to BL-1413: an inadvertent credential exposure.**
While investigating the `swarm.env` sourcing warning, `head -5`'d that
file directly and displayed a real `CURSOR_API_KEY` value in this
session's own transcript — a mistake (should have grepped for the
specific failing construct instead of dumping file content that could
hold secrets). Flagged transparently in-session; recommending that key be
rotated out of caution, consistent with this project's existing practice
for similar leaks (`zshenv-reexports-real-keys-over-fixture-values`
memory). Not touched further, not repeated.

## Land

Skipped `land_step_cli.bb`'s full entangled-sibling walk (established
precedent this session: it took ~25 minutes on BL-1364 before hitting a
`LAND_ESCALATE` on BL-1403's own broken attribution, and BL-1403 is still
unfixed). Went straight to the hand-built replay: diffed the QA-worktree
commit pair (`8153ca7b2c` tagged + `6fac24e87e` untagged) against their
base, applied cleanly in a throwaway `git worktree add --detach <tmp>
origin/main` (zero conflicts — origin/main had already absorbed BL-1364/
1365/1383's own Specification.MD entries by this point), copied the five
binary fixtures directly again, confirmed byte-identical, symlinked
`extension/node_modules` AND `extension/out` (BL-1413 touches no
TypeScript, but the acceptance runner loads every OTHER registered step
handler too, several of which need compiled JS — without the symlink the
run fails on an unrelated missing-module error, not a BL-1413 defect;
removed both symlinks before committing). Re-verified shell suite,
property runner, and acceptance green against that exact tree. One
rematch needed (origin/main advanced by one commit between forking the
worktree and committing); `git rebase origin/main` — clean. `git diff
--stat origin/main HEAD` showed exactly the 17 intended paths.
`land_main_publish.sh --decide-only` returned `:next :push`,
`:origin-advanced-since-gate false`. Pushed `97b7dc7cbe..aa05c9616c`.
Lock acquired/released around the push per BL-1144.

## Land-approval record (BL-1334)

Appended to the shared store (master checkout's `.swarmforge/land-
approvals/2026-09.jsonl`):
`{"at":"2026-09-05T06:50:13Z","ticket":"BL-1413","commit":"aa05c9616c","source":"6fac24e87e"}`.
`source` is `6fac24e87e` — the QA-worktree's own final commit this pass
actually reviewed and approved (an ancestor of `swarmforge-QA`).
Verified `bash swarmforge/scripts/is_qa_ancestor.sh aa05c9616c` prints
`approved: ... - BL-1334`.

`abandoned_commits: [8c1e839eae, a02d1374f5, 8e29b01fae]` recorded on
`backlog/active/BL-1413-the-freshness-check-reads-past-a-nul-byte.yaml` —
unlike BL-1364/1365/1383 (where a literal `git merge` into the QA
worktree succeeded and only the LAND step needed a replay), this ticket
never merged cleanly at all: `8c1e839eae` is the originally-cited
documenter commit (never an ancestor of `swarmforge-QA` in this
lineage — the merge was reset away, not kept); `a02d1374f5` was the first
BL-1402-entangled merge attempt (discarded via `git reset --hard`);
`8e29b01fae` was the second attempt, scoped but with the cross-ticket
doc file still wrongly collapsed into its tagged subject (discarded via
`git reset --soft` and re-split). All three are recorded for a complete
trail even though only the first is likely to ever be independently
cited elsewhere.

BL-1413 is not `GH-`-seeded; no `issue_done.sh` step applies.

## Correction, 2026-09-05 07:05Z — a two-commit land split needs TWO land-approval records

Coordinator's Article 4.2 sweep flagged `24c3272344` (the TAGGED half of
this land's two-commit push — `aa05c9616c`, the untagged doc commit, is
its child) as failing the QA-approval predicate. The land-approval JSONL
only had a row for `aa05c9616c`; `is_qa_ancestor.sh`'s BL-1334 match is
exact-commit, not ancestry, so the tagged parent had no record of its
own even though it is genuinely part of the same approved land. Appended
the missing row:
`{"at":"2026-09-05T07:05:10Z","ticket":"BL-1413","commit":"24c3272344","source":"8153ca7b2c"}`
(source = the QA-worktree's own tagged commit, an ancestor of
`swarmforge-QA`). Verified `is_qa_ancestor.sh 24c3272344` now prints
`approved`. **Lesson for any future cross-ticket-doc-file split**: when a
land pushes N commits instead of one, record N land-approval rows, one
per pushed commit — not just one for the final tip.

By QA.
