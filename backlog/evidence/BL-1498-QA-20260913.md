# BL-1498 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Merged documenter's tip `7df2001af9` into QA (`main` and `origin/main` in
sync, 0/0 ahead-behind). Diff checked against both merge parents — no path
reverted or dropped either way. Ticket lineage confirmed: only two
substantive commits (`dc1c1da112` coder fixture/handler, `6d54f1708a` coder
afterEach reap fix, `bb7183c15b` documenter's one-sentence doc addition) plus
per-stage NONE/detail evidence commits from cleaner/architect/hardener — no
production code touched, matching the ticket's constraint (shell fixture +
one step handler + two doc lines only).

1. `bash swarmforge/scripts/test/test_handoffd_supervisor.sh` from this
   worktree, operator's real `~/.zshenv` in place (confirmed line 7 prepends
   `~/.local/bin`), `RESEND_API_KEY` unset: exits 0, PASS on every case
   including 01, 02, 05 and both BL-081 scenarios (01 SLOW_PID, 03
   STUBBORN_PID) — was FAIL 01 on unfixed main.
2. Hostile ZDOTDIR (own `.zshenv` prepending a directory with a no-op
   `tmux`): still exits 0 — the fixture's own `ZDOTDIR` wins.
3. `grep -c ZDOTDIR` = 11 (>=1); `grep -c 'touch -t 202601010000
   "$DAEMON_DIR/handoffd.pid"'` = 3 (case 02 + both BL-081 scenarios),
   matching qa_e2e_procedure step 5 exactly.
4. `handoffd_supervisor_startup_grace_test_runner.bb`: all 7 assertions
   pass; `git diff main -- swarmforge/scripts/handoffd_supervisor.bb` is
   empty — grace contract (BL-1342) untouched, invariant 2 holds.
5. Acceptance: BL-1498's own feature (4/4 scenarios), BL-326 (5/5), BL-977
   (9/10 — see below) all run via `run_acceptance.sh`, sequentially.
6. Step handler wiring: `bl1498SupervisorTestIsolatesOperatorShellSteps.js`
   is not referenced anywhere by name (no hand-maintained registry since
   BL-1371 — handlers are directory-discovered by `specs/pipeline/steps/index.js`);
   its scenarios actually running in the acceptance pass above is the wiring
   proof.
7. Orphaned-process check before/after: only long-lived swarm daemons
   (handoffd, handoffd_supervisor, operator_runtime, front_desk_supervisor,
   etc.) present both times, no leftover test/mutation process.
8. Full unit suite (`npm test`, extension/): 615 passed / 1 failed
   (`test/telegramCursorOperatorExec.test.js`, `ENOTEMPTY` on
   `sweepPendingTmpDirs`/`/tmp/bl1204-fd-*`). BL-1498 touches no
   `extension/` file. Isolated re-run: 24/24 clean. Same signature already
   recorded as a full-suite-concurrency flake in
   `BL-1501-QA-20260911.md`, `BL-1526-QA-20260911.md`, `BL-1444-QA-20260906.md`.
9. `npm run test:properties`: two separate full runs surfaced 5 distinct
   failures across 6 files total, no two runs agreeing on the same set
   (classic full-suite-concurrency flakiness for `fc.assert`-based
   property tests). Isolated re-run of each:
   - `bl1089FrontDeskLivenessFixture.property.test.js` — fails in isolation
     too; already owned, `BL-1502` (standing-red register: owned=true).
   - `bl1313BatchGuardVisibilityInvariants.property.test.js` (both
     invariants) — fails in isolation too; already owned, `BL-1503`
     (owned=true).
   - `bl1367ApprovalCarriesItsRuling.property.test.js` — passes 2/2 in
     isolation; concurrency flake (also separately owned `BL-1527` per the
     register, unaffected either way).
   - `bl1429StandingRedThrottleFoldInvariants.property.test.js` — passes
     3/3 in isolation; concurrency flake (coverage-style `fc.assert`
     asserting all 12 category combinations were drawn — inherently
     seed-sensitive under a bounded run budget). Not in the standing-red
     register; not reproducible outside full-suite concurrency; no ticket
     needed (BL-1063: not a red at all once isolated).
   - `bl1272LandedSiblingInvariants.property.test.js` — passes 3/3 in
     isolation; same disposition as bl1429.
   None of these five are reachable from BL-1498's diff (zero `extension/`
   files touched by this ticket).
10. `test_handoffd_supervisor.sh` (BL-1498's own row) and
    `specs/features/BL-977-supervisor-never-halts-a-progressing-daemon.feature`
    (BL-1500's row, scenario 05 "a wedged poll loop is still caught within
    the in-sweep budget") both confirmed `owned: true` via
    `standing_red_register_cli.bb` — the BL-977 red is the explicitly
    out-of-scope sibling case named in BL-1498's own ticket text, not a
    new finding.
11. Invariant 1 (no zsh script reads the operator's own startup files) —
    proven by item 2. Invariant 2 (grace contract unwidened/unnarrowed) —
    proven by item 4 and the acceptance scenario outline's two rows
    (younger/healthy/still-alive, older/halted/terminated), both passing.

No `abandoned_commits` needed — the commit forwarded is a plain merge, not a
replay. No `GH-` ticket close applicable (BL- id). No standing-red register
row to remove pre-land (removed by the land itself, per the ticket's own
"Register" bullet — done at merge-up time below).

By QA.
