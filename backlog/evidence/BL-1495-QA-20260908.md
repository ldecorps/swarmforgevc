# BL-1495 — QA review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Lineage: hardener merge `2587697310` and documenter commit `b62362216d`
  both confirmed ancestors of the reviewed commit `44e669e0c3`
  (`git merge-base --is-ancestor`). No prior bounce found for BL-1495 in
  the master checkout's `.swarmforge/bounces/`. Branch is in sync with
  `origin/main` (0/0 ahead-behind), including the specifier's 01b9a83f9f
  amendment retiring bl1328 invariant 3's net-diff tail — confirmed
  applied in `extension/test/bl1328QwenModelTokenFormsInvariants.property.test.js:125`.
- `qa-sibling-check.js status --ticket BL-1495` → `VERIFY BL-1495` (no
  open deferral).
- Unit suite (`npm test`): 613 files / 10401 tests, all green. Suite/file
  duration-budget nags are pre-existing, unrelated CLI test files, not a
  failure.
- Property suite (`npm run test:properties`): 384 files / 1128 tests, all
  green; the one `[vitest-worker]: Timeout calling "onTaskUpdate"`
  unhandled error is the BL-871 allowlisted case.
- Acceptance (`run_acceptance.sh specs/features/BL-1495-...feature`): all
  7 scenarios pass (launch-gate resolution x2, compat resolver x2,
  respawn parity, key-never-sticks-to-server, shipped pack staffing).
- All 8 `required_wiring` anchors grepped against real call sites (not
  merely unit-tested in isolation):
  `pack_staffing_gate_lib.bb` (api.b.ai→tencentcloud2, claude-fable-5-1→anthropic),
  `swarmforge.sh` (api.b.ai host-sniff + B_AI_API_KEY forwarded/re-applied),
  `harness_env_scrub_lib.bb` + `harness_env_scrub.sh` (B_AI_API_KEY scrub
  map + aider keep set), `glm-mono-router.conf` (api.b.ai pinned on every
  window), `bl1495BaiGatewaySeatSteps.js` (auto-discovered by
  `specs/pipeline/steps/index.js`'s `*Steps.js` top-level scan — no
  explicit registration line needed).
- Both invariants checked directly: `provider_compat_lib.bb`,
  `provider_respawn_env_lib.bb` and `handoff_lib.bb` all treat api.b.ai
  the same way as api.cerebras.ai (launch-cli-wins posture, real
  OPENAI_API_KEY excluded); `B_AI_API_KEY` reaches a pane only via
  explicit `-e` passthrough (swarmforge.sh:2272,
  provider_respawn_env_lib.bb:87, handoff_lib.bb:850) — `git grep` finds
  no committed value, only names and one clearly-fake test fixture value
  (`test_alternate_runtime_launch.sh:307`, `bai-secret-do-not-leak`).
  `ancillary_provider_lib.sh`'s pack-prefix case block is unrelated to
  this launch path (used only by front-desk/ancillary-operator launches,
  not the mono-router pane launch, which uses swarmforge.sh's own
  host-sniff) — its absence from `required_wiring` is correct, not a gap.
- Documenter deliverables present: `docs/how-to/glm-mono-router-launch.md`
  in the qwen-precedent shape, linked from `docs/index.md:201`;
  `model-compatibility.md` already carries both models. No registered
  diagram depicts provider wiring (confirmed by reading
  `docs/diagrams/architecture.mmd`) so no diagram update was due.
- No orphaned `node --test`/`stryker` processes before or after
  verification.
