# BL-1649 — hardender review pass, 2026-09-19

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb swarmforge/scripts/test/batch_claim_progress_sweep_test_runner.bb (baseline, then hand-mutated parse-claim-idle-timeout-role-minutes-ms dropping the (pos? n) guard, then restored)
- **Commit hash**: 7b8152cf07
- **First error excerpt**: mutant (zero/negative minutes accepted as a valid, TIGHTER timeout) survived both this file and the full test_claim_progress_sweep.sh shell suite - the ticket own How section calls for these parser cases (valid, zero, negative, non-numeric, repeated role, unknown role), the sibling BL-1076 parser has 8, this new one had none
- **Failure class**: test-coverage-gap
- **Expected vs observed**: invariant 2 (never a tighter window) holds at the parser level for zero/negative/missing conf values, not only through indirect shell exercises
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added nine direct pure-function cases mirroring the sibling parsers shape; commit 7b8152cf07; confirmed three of them now kill the hand-applied mutant

By hardender.
