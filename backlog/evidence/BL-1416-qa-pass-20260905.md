# BL-1416 QA pass — 2026-09-05

BL-1416 PASSES.

## Pipeline lineage verified

Documenter's commit `393412996b` merged into the QA worktree at
`e74c901d96`. Confirmed via `git merge-base --is-ancestor` that this
ticket's own coder (`a95f87849d`), cleaner (`456d089a59`), architect
(`45c4fe6eb4`), and hardener (`efa4ed04a0`) commits are all ancestors of
the documenter commit being cited. No prior bounce history for BL-1416 on
`main`/`origin/main` (in sync, 0/0 divergence).

## Ticket-specific verification (independent re-run)

- `bb swarmforge/scripts/test/provider_auth_observe_lib_test_runner.bb`:
  PASS (all assertions, old + new).
- `bb swarmforge/scripts/test/bl1416_busy_pane_never_auth_dead_property_runner.bb`:
  ALL PROPERTIES HOLD, 500/500 each of P1/P2/P3, coverage
  `{:p1-min-attempts-nonzero 401, :p2-some-skipped 425, :p2-all-performed 75,
  :p3-nil-line 116}` — matches coder/architect/hardener's own runs.
- `bb swarmforge/scripts/test/provider_auth_observe_lib_property_runner.bb`
  (BL-536 regression): ALL PROPERTIES HOLD, 500/500, unaffected.
- `bash swarmforge/scripts/test/test_handoffd_auth_observe_wiring.sh`
  (real daemon + fake tmux, regression): PASS 01-03, unaffected.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1416-a-busy-pane-is-never-auth-dead.feature`: 7/7
  scenario runs pass.
- `required_wiring` both anchors confirmed live:
  `swarmforge/scripts/handoffd.bb:1321` —
  `busy-signal (if (chase-sweep-lib/actively-processing? pane) :healthy :observe)`
  (functional gate, not a comment); `bl1416BusyPaneNeverAuthDeadSteps.js::registerSteps`
  present.
- `docs/how-to/BL-536-provider-auth-error-auto-respawn.md` documents the
  BL-1416 busy-gate fix, the `resolve-committed-state` semantics, and the
  new alert-reason shape.
- qa_e2e_procedure items 1, 2, 4 covered by the above. Item 3 (live,
  read-only: `grep -a 'auth-respawn-skip-busy\|auth-persist'
  .swarmforge/daemon/handoffd.log` across three observer ticks) checked
  against the actual running daemon's log: no such lines present
  currently (no live false-alarm episode in progress) — consistent with
  the expected outcome; the live daemon has not yet picked up this fix
  (unlanded at check time), so this is an absence-of-evidence observation,
  not a claim the fix is live-verified end-to-end. The fixture-driven
  acceptance scenarios (esp. #7, the hardener's real 2026-09-05 pane
  capture) already exercise the exact real-world trigger directly.
- No orphaned test/mutation/tmux processes before or after this pass.

## Full unit suite (BL-1063 diligence)

`npm test` in `extension/`: 588/604 files passed, 10207/10233 tests
passed (run twice; 26-27 failures, same 16 files both times, one
count difference from `telegramCursorOperatorExec.test.js`'s "BL-1204"
assertion which passes cleanly in isolation — full-suite load flakiness,
not a regression). BL-1416's diff never touches `extension/src/**`; it is
confined to `swarmforge/scripts/handoffd.bb`,
`swarmforge/scripts/provider_auth_observe_lib.bb`, their test/property
runners, and one pipeline step handler.

All 16 failing files are the same already-tracked, already-approved,
`status: todo` defects catalogued in `BL-1407-qa-pass-20260905.md`
(BL-1221/BL-1229, BL-1263, BL-1265, BL-1289/1290/1291) plus the same
untracked `pricingTable.test.js` gap (`claude-fable-5-1` missing from
`PRICING_TABLE`) noted there. No new failing file appeared. None
introduced by BL-1416.

## Verdict

BL-1416 APPROVED.

By QA.
