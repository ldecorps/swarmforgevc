# QA pass — BL-1423 — 2026-09-05

Commit under review: `617db7094f` (merge of documenter's `66f8ea73f0` into
`swarmforge-QA`), carrying the full chain (`82145fa26e` BL-1423 coder,
confirmed ancestor via `git merge-base --is-ancestor`).

## Verification

- `bb swarmforge/scripts/test/suite_inventory_cli.bb swarmforge/scripts/test`:
  `suite inventory: ok - 495 test file(s), 491 standing, 4 excluded with a
  dated reason` — the three-day-standing refusal (BL-1239's gate) is clear.
- Invariant 1 spot-checked directly: `git diff` of `suite-manifest.tsv`
  between the coder's parent and its own commit shows exactly two added
  lines (`handoffd_supervisor_startup_grace_test_runner.bb`,
  `test_handoffd_outbox_vanished_parcel_wiring.sh`), both `standing`, both
  in the file's existing alphabetical position; no other row touched.
- Both named files run and pass independently:
  `bb swarmforge/scripts/test/handoffd_supervisor_startup_grace_test_runner.bb`
  → ALL TESTS PASS; `bash
  swarmforge/scripts/test/test_handoffd_outbox_vanished_parcel_wiring.sh` →
  ALL PASS (4/4).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1423-the-standing-bb-suite-runs-again.feature` — 3/3
  scenarios pass (inventory gate passes on the parcel's tree; each hotfix
  file has exactly one standing row and is listed by the standing run).
- `required_wiring` confirmed: both manifest rows present (grep), handler
  file `specs/pipeline/steps/bl1423StandingSuiteRunsAgainSteps.js` exports
  `registerSteps`.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.
- Out of scope per ticket: BL-1424 (the commit-time guard preventing
  recurrence) is a separate, not-yet-seen ticket; not required here.

## Verdict

APPROVED — merging up and landing.
