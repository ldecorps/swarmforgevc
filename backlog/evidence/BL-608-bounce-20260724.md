# BL-608 QA bounce — 2026-07-24

1. **Failing command**: `npm test` (extension/, vitest)

2. **Commit hash tested**: 54500ed3f (QA merge of documenter commit a7ef26bc5c for BL-608)

3. **First error excerpt**:

```
 FAIL  test/recordQaBounceCli.test.js > BL-608: QA.prompt documents the live record-qa-bounce.js invocation with --by/--evidence
AssertionError: expected QA.prompt to document the record-qa-bounce.js invocation with --by QA and --evidence flags, so updateTicketBounceHistory() is reachable from the live caller

- Expected
+ Received

- true
+ false

 ❯ test/recordQaBounceCli.test.js:491:10
    489|   const qaPromptPath = path.join(__dirname, '..', '..', 'swarmforge', …
    490|   const text = fs.readFileSync(qaPromptPath, 'utf8').replace(/\s+/g, '…
    491|   assert.ok(
       |          ^
    492|     text.includes(
    493|       '--commit <10-hex bounce commit> --by QA --evidence <path to the…

Test Files  1 failed | 345 passed (346)
     Tests  1 failed | 5869 passed (5870)
```

4. **Failure class**: `unit`

5. **Expected vs observed**: the coder's architect-send-back-#1 fix (commit
   `5e69f7710`) pinned the literal invocation text in
   `swarmforge/roles/QA.prompt` as
   `--commit <10-hex bounce commit> --by QA --evidence <path to the evidence
   file you just committed>`, and added a test
   (`extension/test/recordQaBounceCli.test.js`, "BL-608: QA.prompt documents
   the live record-qa-bounce.js invocation with --by/--evidence") that pins
   that exact string specifically so a future doc edit cannot silently
   regress the live caller's wiring. The documenter's pass (commit
   `a7ef26bc5c`) rewrote that line for readability to
   `--commit <10-hex bounce commit> --by <bouncing role> --evidence <path>`
   — a paraphrase that drops the literal `--by QA --evidence <path to the
   evidence file you just committed>` the test pins, so the test now fails.
   `npm run compile` is clean; this is a pure documentation regression, not a
   code or logic defect.

Producing role: documenter (its own edit to `swarmforge/roles/QA.prompt`
broke a test explicitly written to guard that exact text — the docs
deliverable named in the ticket's own scope).

Fix: restore the literal `--by QA --evidence <path to the evidence file you
just committed>` wording in that one invocation line (the elaboration
paragraphs below it, which explain `--by`/`--evidence` in prose, are fine as
rewritten — only the pinned command-line literal must not drift), then
re-run `npm test` to confirm the pinned test passes again.
