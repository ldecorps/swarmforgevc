# BL-1164 QA bounce — 20260827

**Routing:** coder (acceptance wiring missing)

## Failing command

```
specs/pipeline/scripts/run_acceptance.sh \
  specs/features/BL-1164-qa-changed-path-unit-test-gate.feature
# no step handler matched "Given the constitution article on quality gates…"
```

## Commit tested

`14d5a669f2` (documenter handoff — how-to + Spec/index). Merge onto
`origin/main` had Spec Last Updated conflict only (resolvable); product docs
tip-pure. Acceptance cannot run.

## First error excerpt

```
Error: Scenario "Article 4.5 declares the changed-path unit test gate for QA":
no step handler matched "Given the constitution article on quality gates at the
parcel commit"
```

Same for QA.prompt and documenter-corpus scenarios. No
`bl1164*Steps.js` / index require on tip or `origin/main`.

## Failure class

acceptance

## Expected vs observed

**Expected:** `required_stages` skips coder for prose mint, but acceptance
feature still needs registered step handlers so QA can execute the 3 scenarios.

**Observed:** Feature + Article 4.5/how-to exist; acceptance steps/wiring
absent → acceptance red.

## Defects

**D1 — missing acceptance wiring (blame: coder):** Add
`bl1164QaChangedPathUnitTestGateSteps.js` (or equivalent) + `index.js`
require covering all three scenarios; rematch tip-pure atop current
`origin/main` (include documenter how-to if not already landed).

By QA.
