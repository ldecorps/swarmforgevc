# BL-1503 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- Merged documenter commit `1f8e7c5cb4` into QA at `042b512131`.
- `qa-sibling-check.js status --ticket BL-1503` → `VERIFY BL-1503` (no open deferral).
- `pre_qa_gate.sh BL-1503-the-bl1313-property-test-copies-the-closure-it-computes 042b512131` → `OK`
  (required_wiring anchors present: `operatorRuntimeBbClosure` require in the property test,
  `registerSteps` in the new step-handler file).
- `npx vitest run --config vitest.properties.config.mjs test/bl1313BatchGuardVisibilityInvariants.property.test.js`
  → 3/3 passed (both invariants that were red on main are now green).
- `npm test` (full unit suite) → 616 files, 10479 tests, all passed.
- `npm run test:properties` (full property suite) → 401/402 files passed, 1186/1187 tests passed.
  One unrelated failure: `test/bl1343ReplayNeverDropsOwnPathInvariants.property.test.js` invariant 1
  timed out at 20000ms under full-suite contention. Re-ran that file alone: passed in 8.5s. Not
  caused by this ticket's diff (BL-1503 touches only the bl1313 property test and its own new step
  handler), not in `backlog/standing-reds.tsv`, no matching active/paused ticket — a transient
  resource-contention timeout, not a deterministic red; not blocking.
- `run_acceptance.sh specs/features/BL-1503-the-bl1313-property-test-copies-the-closure-it-computes.feature`
  → both scenarios passed.
- Wiring check: the property test seeds from `computeClosure(SCRIPTS_DIR, 'handoff_lib.bb')` ∪
  `computeClosure(SCRIPTS_DIR, 'duplicate_chain_guard_lib.bb')` (no hand-typed list, no
  `property_suite_standing_allowlist.tsv` entry added), matching `human_approval`'s FIRM constraints.
- Standing-red register row for this file will be retired at land (per the ticket's own intent:
  "the register row this ticket adds is removed in the same land").
- No orphaned `node --test`/`stryker` processes before or after.

By QA.
