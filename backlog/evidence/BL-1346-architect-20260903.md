# BL-1346 — architect pass (2026-09-03)

## Merge

Merged cleaner `2873274c7c` (coder `8adb56d4a2` + `ac110de8dd`) into the
architect worktree. One trivial `require(...)`-list conflict in
`specs/pipeline/steps/index.js`; resolved by keeping both branches' entries.

## Nature of this ticket

BL-848 review-only stamp-off of already-landed commit `195de28861`, same
shape as BL-1333/BL-1342 reviewed earlier this session. Confirmed no
production code touched (`swarm_ensure.bb`, `mono_router_lib.bb`,
`test_swarm_ensure.sh` all byte-identical across this span) and
`backlog/hotfix-ledger.yaml` untouched.

## required_wiring confirmed live (all three anchors, exact counts match)

- `swarm_ensure.bb::resolve-resident-role` — 2 occurrences.
- `swarm_ensure.bb::rc-launch-role` — 3 occurrences.
- `specs/pipeline/steps/index.js::bl1346SwarmStampRcLaunchRoleSteps` —
  registered.

## Review of the harness — weight where the ticket asks for it

The ticket is explicit that scenarios 02/03 (the repair still repairs; the
marker keeps authority on a router pack) matter more than scenario 01, since
a hotfix that bought silence by disabling repair would be a worse bug than
the one it replaced. Confirmed:
- Scenario 02 / invariant 1 drives a REAL `swarm_ensure.bb` run with a
  genuinely degraded pane (every pack role, enumerated by a `for` loop, not
  drawn) and the stale marker present, and asserts the pane IS respawned,
  with its own role's script, never the marker's.
- Scenario 03 / invariant 2 confirms the marker still governs on a
  rotation-router pack — the authority removed is exactly the non-router
  case — checked both via the shared decision directly and end-to-end
  through a real ensure run on both pack shapes from one derived marker
  value.
- Scenario 04 asserts `rc-launch-role` reads the marker EXACTLY ONCE — as
  the shared decision's input — so no local rule survived beside it.
- Non-vacuity demonstrated: reverting the landed line reproduces the
  original incident exactly and reds both invariants.

## Side finding disclosure honored

The ticket's notes ask the reviewer to record whether the vacuous-fixture
pattern in older RC-* fixtures (bare script path receiving no arguments)
widened while this parcel was in the file. Coder's evidence states it did
not; independently confirmed — `test_swarm_ensure.sh` diff across this span
is empty.

## Incidental fix reviewed (sibling stamp-off timeouts)

The coder's first commit also adds an explicit 120s `test()` timeout to two
already-reviewed sibling property files (`bl1333StampOffInvariants`,
`bl1342CrashloopStampInvariants`) — both drive real git/daemon fixtures that
exceed Vitest's 20s default under full-suite load. No assertion logic
changed. Re-ran both independently: 6/6 pass, still green.

## Invariants review (BL-633/654)

All three declared invariants have property tests, reach floors enumerated
(invariant 1's `for (const degraded of ROLES)`, invariant 2's unconditional
counters) rather than drawn — no BL-1345/BL-1352-style flakiness risk.
Invariant 3 uses the same `git log --grep <ticket> origin/main..HEAD` scoping
established earlier this session. Re-ran independently **3 consecutive
times, 3/3 tests each, no flake** (real-fixture properties, ~20s/run).

## Independently re-run

- `run_acceptance.sh specs/features/BL-1346-…feature` — 5/5 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1346RcRepairStampInvariants` — 3 runs, 3/3 each.
- `npx vitest run --config vitest.properties.config.mjs
  bl1333StampOffInvariants bl1342CrashloopStampInvariants` — 6/6 pass
  (confirms the incidental timeout fix didn't break either sibling).
- Dependency-rule gate (BL-259), scoped and full-repo: PASSED, no forbidden
  edges.
- Co-change report (BL-255): all reported co-changes at threshold — expected
  self-coupling within this ticket's own files plus the two sibling property
  files it touched for timeouts only.

## Correctness read

No defect. The harness drives the landed code faithfully, weights its
scenarios exactly where the ticket asks, and the side-finding disclosure
obligation is honored and independently confirmed.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
