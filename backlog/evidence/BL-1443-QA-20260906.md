# BL-1443 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1443`: `VERIFY BL-1443` (no open
  deferral).
- Orphan process sweep before verifying: found 28 fully-orphaned
  (reparented to init, PPID chain -> `/init`) `node /tmp/bl1358-ceiling-*/
  {hang,hang2,spawner,b}.test.js` processes running since 2026-09-04
  (~1d21h CPU time each). Traced to BL-1358 ("a mutant that will not
  finish is killed and reported"), which landed 2026-09-04
  (`backlog/evidence/BL-1358-land-success-20260904.md`) — pure leftover
  debris from that ticket's own test run, no ongoing evidentiary value.
  Reaped each by its own process group (`kill -- -<pgid>`); confirmed
  gone. Unrelated to BL-1443; noted since they were silently consuming
  cores for two days and could confound suite-timing measurements.
  `ps aux | awk '/[n]ode --test|[s]tryker/'` clean before and after
  (the earlier `pgrep -f` self-match on its own argv is not a reliable
  signal here — used `ps`/`awk` bracket-trick instead).
- Full unit lane (`npm test`, extension/): 610/610 files, 10283/10283
  tests green. Same chronic suite-duration/per-file budget overage as
  other recent parcels (`recordTestDuration.js`'s guard, exit 1) —
  `.test-durations.jsonl` shows 30-50s runs long before this parcel;
  none of the four flagged slow files touched by BL-1443's diff.
- Full property lane (`npm run test:properties`): 364/364 files,
  1087/1087 tests green, exit 0. Two `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled errors — both the BL-871 allowlisted benign
  case. Includes `bl1443TolerantTreeWalkInvariants.property.test.js`
  (3/3, both declared invariants plus the hardener's excludeDirs
  coverage-gap addition) and the three converted files
  (`bl874PortableTimeInvariants`, `tempDirTrapGuard`,
  `bl1060TelegramButtonUrlScheme`).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1443-property-walks-tolerate-vanishing-fixtures.feature`
  — 4/4 scenario runs pass.
- Race reproduction (qa_e2e_procedure item 2): `bl868PropertyLaneIsolationGuards`
  + `bl874PortableTimeInvariants` run together 5 consecutive times — no
  ENOENT in any run, 9/9 tests every time.
- Structural greps (item 3): `grep -lE 'function walk\('
  extension/test/*.property.test.js` empty; `grep -l walkFilesTolerant
  extension/test/*.property.test.js` lists exactly the three converted
  files plus the new invariants test. Independently re-ran the coder's
  original identification grep (`function walk|walk\(`) and spot-checked
  the "out of scope" classifications: `nestedGitRepoGuard`'s hit is
  inside a comment only (confirmed by line read); `bl1147ProbeLegacyTopicAdoption`
  walks its own `mkTmpDir` root, not a live tree (confirmed). `bl1060`'s
  reappearance under the broader grep is `function walkWithContent(` — a
  local wrapper calling the shared helper, not a reintroduced inline walk;
  does not match the ticket's own stricter `function walk\(` check.
- Wiring anchor 1: `bl874PortableTimeInvariants.property.test.js` requires
  and calls `walkFilesTolerant` (line 9, 87).
- Wiring anchor 2: `bl1443PropertyWalksTolerateVanishingFixturesSteps.js`
  exports `registerSteps`; discovered automatically by
  `specs/pipeline/steps/index.js`'s `readdirSync`-based scan (BL-1371
  posture, confirmed on BL-1444's pass) — no hand-maintained require list.
- Read `extension/test/helpers/tolerantTreeWalk.js` directly: ENOENT
  tolerance scoped strictly to `readFileSync` (content read), never
  `readdirSync` (listing) — matches invariant 1 exactly; no write/move/
  delete call anywhere in the file — matches invariant 2.
- Lineage: this ticket's hardener merge (`5a8250bfcd`) is an ancestor of
  the citing commit (`1fc2338582`) — `git merge-base --is-ancestor`
  confirmed.
- Docs: `docs/how-to/BL-1124-property-suite-fixtures-must-not-mutate-shared-main.md`
  names `walkFilesTolerant`. No diagram trigger fires (ticket says so;
  confirmed no diagram touched by the diff).
- Out of scope respected: no fixture files moved out of `extension/test/`;
  `bl1352EscalationVisibilityInvariants` (separate, closed load-flake
  class per BL-1407) untouched.

By QA.
