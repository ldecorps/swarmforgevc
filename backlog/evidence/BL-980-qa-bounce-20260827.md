# BL-980 QA bounce — 2026-08-27

**Routing:** coder (tip-pure handoff / branch pollution — BL-506)

## Failing command

```
git diff --stat HEAD...e86cd84963
# 177 files changed, 4687 insertions(+), 2476 deletions(-)
```

`merge_and_process documenter e86cd84963` then hit modify/delete conflicts on
`extension/test/bl980RecentlyClosedElapsed.test.js` and
`specs/pipeline/steps/bl980RecentlyClosedElapsedTimeSteps.js` (deleted on QA
when prior hitchhiked merges were reverted; present on documenter tip). Merge
aborted — tip not integrated.

## Commit tested

`e86cd84963` (documenter handoff). Documenter commit itself is docs-only
(4 files); ancestry tip is polluted.

## First error excerpt

Out-of-scope paths in `HEAD...e86cd84963` include (non-exhaustive):

- Retire **BL-545** (`backlog/done/M8/BL-545-…`, catch-up paths)
- **BL-780** shell test + mono_router ordering work
- **BL-754** / **BL-1084** / **BL-1020** / **BL-1023** evidence hitchhikers
- Intakes, pack conf, bridge/catchUp churn, BL-656/736 reactivation, etc.

## Failure class

behavior

## Expected vs observed

**Expected:** a BL-980 handoff carries a tip whose tree delta vs QA/`origin/main`
is limited to recently-closed elapsed-time work (pipelineBoard / conciergeTick
field plumbing, format ladder, acceptance, docs).

**Observed:** documenter tip is 29 commits / 177 files ahead of QA HEAD with
many unrelated tickets. Landing would integrate them under a BL-980-only
approval (BL-506).

## Defects

**D1 — entangled handoff (blame: coder):** Re-forward BL-980 from a tip-pure
commit (cherry-pick / rematch the BL-980 coder→documenter line without
BL-545/BL-780/BL-754 hitchhikers). Do not forward the full documenter tip.

## Notes

Documenter pass content (`docs/how-to/BL-980-…md`, Specification, index,
evidence) looks in-scope when viewed alone (`e86cd84963` is 4 files). The
bounce is tip purity of the *handoff commit ancestry*, not docs quality.

By QA.
