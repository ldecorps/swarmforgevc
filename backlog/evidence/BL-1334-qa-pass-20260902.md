# BL-1334 — QA pass, 2026-09-02

Reviewed commit `abe9e97d2b` (documenter, clean sweep NONE), merged as
`7f5790d318`. Verified lineage: coder `...` -> cleaner -> architect ->
hardener `e11822c359` -> documenter `abe9e97d2b`, all present as ancestors
of the commit landed below.

## Process hygiene
- `pgrep -fl 'node --test|stryker|vitest'`: clean before start. A prior
  `test:properties` run left stray vitest workers after a 2-minute CLI
  timeout mid-run; reaped by process group (`kill -- -<pgid>`) before
  re-running with a longer timeout. Clean after this pass.

## Independent re-run, all green
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/build_freshness_lib_test_runner.bb` — ALL PASS.
- `bash swarmforge/scripts/test/test_is_qa_ancestor_land_replay_store.sh` — 11/11.
- `bash swarmforge/scripts/test/test_land_step_records_approval.sh` — 9/9.
- `bash swarmforge/scripts/test/test_build_freshness_land_replay_approved.sh` — 8/8.
- `bb swarmforge/scripts/test/bl1334_land_replay_approval_property_runner.bb` — 48/48 exhaustive, ALL PROPERTIES HOLD.
- Regression: `test_is_qa_ancestor_expedite_store.sh`, `test_is_qa_ancestor_yaml_store.sh`,
  `test_pipeline_code_on_main_guard.sh` — all pass, sibling approval paths undisturbed.
- Acceptance: `node specs/pipeline/cli.js specs/features/BL-1334-a-landed-replay-is-qa-approved-when-it-lands.feature` — 5/5.
- `npm test` (extension unit lane): 570/585 files, 9844/9869 tests pass.
  25 failures across 15 files (pilotAcceptanceGate.test.js `checkOrphanedAuthoredDocs
  is not a function`, tempDirTrapGuard, socketFixtureShortRootGuard,
  liveRepoDerivationGuard, operatorRuntimeBbFixtureClosure, telegramClient,
  telegramCursorOperatorExec, constitutionDocCitations, etc.) — none touch
  `is_qa_ancestor.sh`, `land_step_lib.bb`, `land_step_cli.bb`,
  `build_freshness_cli.bb`/`build_freshness_lib.bb`, or any file this
  parcel's diff names. Cross-checked against `git diff --name-only
  eaee1afdeb...7f5790d318` — empty intersection. Matches the standing-red
  lineage already on file (`QA-standing-red-corroboration-20260828.md`,
  BL-1220/BL-1221/BL-1206/BL-1265/BL-1289/BL-1290/BL-1291), corroborating
  rather than reporting new.
- `npm run test:properties`: 279/305 files, 852/868 tests pass. 26 failed
  files, same standing-red lineage (BL-654 stamp-off invariants, `bl1012`,
  pilotAcceptanceGate/checkOrphanedAuthoredDocs collection gaps,
  tempDirTrapGuard, hostActivityFeed, etc.) — none intersect this parcel's
  diff. One `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled
  error, the sole BL-871-allowlisted exception.

## Wiring
- `land_step_cli.bb:81` calls `land-step-lib/record-land-approval!` — real caller.
- `build_freshness_cli.bb:182` shells to `is_qa_ancestor.sh` — real caller.
- `is_qa_ancestor.sh` gains the land-replay store consult; BL-925 invariant 2
  (ONE shared predicate, three consumers: `check_pipeline_code_on_main.sh`,
  `handoffd.bb`, `babysitter_check.bb`) unchanged — none of the three call
  sites needed to change, confirmed by grep.

## Intent check
Ticket invariants verified directly by the acceptance feature and the
dedicated test suites above: a landed replay reads QA-approved with no
later unrelated merge required; approval never spreads to an unrelated
commit on the same branch; a bounce verdict (JSONL or YAML-only) still
vetoes, even against a replay record naming an otherwise-approved source.

## Verdict
APPROVED. Landing on `main`.
