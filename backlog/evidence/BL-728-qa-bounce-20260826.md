# BL-728 QA bounce — 20260826

**Commit checked:** `2977813a3` (Merge documenter `bd69c22eb7` for BL-728)
**Task:** `BL-728-bl636-commit-message-claims-unlanded-parenfix`
**Sibling check:** `VERIFY BL-728` (exit 0)
**Routing:** `cleaner` (earliest blamed — conflict resolution kept out-of-scope BL-1153 test)

## Gates run (Article 4.4 — complete inventory)

| Gate | Result |
|------|--------|
| `npm run compile` | PASS |
| Evidence file content vs ticket `qa_e2e_procedure` | PASS |
| `required_wiring` (`bl728HandoffdDeliverParenVerificationSteps` in `index.js`) | PASS |
| `bash swarmforge/scripts/test/test_handoffd_one_shot_flags_parse.sh` | ALL PASS |
| APS `BL-728-handoffd-deliver-paren-verification.feature` | 7/7 PASS |
| `node --test test/residentSpyUiHtml.test.js` (parcel-touched file) | **FAIL** — see D1 |
| Full `npm test` unit lane | BLOCKED BY standing host debt — see noted |
| `npm run test:properties` | BLOCKED BY standing host debt — see noted |

## Defects

### D1 — unit: out-of-scope BL-1153 test fails in parcel-touched file (class: `unit`) — **blame: cleaner**

1. **Failing command:**
   ```bash
   cd extension && npx vitest run test/residentSpyUiHtml.test.js
   ```
2. **Commit hash:** `2977813a3`
3. **First error excerpt:**
   ```
   FAIL  test/residentSpyUiHtml.test.js > BL-1153: Live Screen pane font size survives a full Mini App reload
   AssertionError: Expected values to be strictly equal:
   '13px' !== '16px'
   Expected: "16px"
   Received: "13px"
   ```
4. **Failure class:** `unit`
5. **Expected vs observed:** Expected BL-728 parcel to carry only handoffd verification deliverables (BL-506). Observed `extension/test/residentSpyUiHtml.test.js` adds a BL-1153 font-persistence case (`fontStore: { 'live-screen': 16 }`) that is absent from `main` and fails because BL-1153 host persistence is not in this parcel. Cleaner merge `a1176ff14` explicitly kept this test during conflict resolution (`Keep BL-1153 font-reload test alongside BL-1046 tile assertions`) after BL-1154 QA land had dropped the stale BL-1153 test on `main`.

**Remediation:** Remove the out-of-scope BL-1153 block from `extension/test/residentSpyUiHtml.test.js` in the BL-728 parcel (restore `main`'s version for that file, keeping only BL-728-relevant diffs if any). BL-1153 font persistence belongs on ticket BL-1153, not BL-728.

## Not bounced (noted — BL-1063 standing debt)

| Lane | Observation | Ticket |
|------|-------------|--------|
| Full unit suite | 198 failures — `CURSOR_API_KEY is not set for the headless bridge` across bridge suites (`topicMakeTopBridge.test.js`, etc.) | **BL-720** family (done; host env gap) |
| Property lane | 20 files / 5 tests failed; 2× benign `[vitest-worker]: Timeout calling "onTaskUpdate"` allowlisted | **BL-1063** / **BL-1112** |

Parcel diff vs `main` does not touch bridge sources; these reds are outside BL-728 tip surface.

## Inventory

D1 (cleaner). Bounce to **cleaner**.

By QA.
