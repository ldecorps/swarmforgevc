# BL-859 bounce — documenter pass, 2026-08-12

Complete review inventory for this pass (Article 4.4). Documentation
deliverable (docs/how-to/BL-859-boot-prefix-budget-gate.md + docs/index.md
entry, commit 861038c901) is done and not in question — this bounce is
solely the pre-QA wiring gate failure hit while forwarding.

## D1 — wiring: required_wiring literal not present verbatim

- **Class**: wiring
- **Blamed role**: coder
- **Detail**: The ticket's `required_wiring` entry is
  `swarmforge/roles/specifier.prompt::boot prefix budget gate::a gate the
  specifier is never instructed to run is never run — this is the BL-419
  shape the ticket exists to avoid`. The pre-QA gate does a literal,
  case-sensitive substring match on the pattern `boot prefix budget gate`.
  Commit 915634671 (coder, "boot-prefix budget gate, wired into the
  specifier's amendment-commit procedure") added a section to
  `swarmforge/roles/specifier.prompt` headed `## Boot-prefix budget gate
  before a boot-inlined article change (BL-859)` — hyphenated and
  capitalized. The exact literal `boot prefix budget gate` (lowercase, space
  not hyphen) does not appear anywhere in the file. `swarm_handoff.sh`
  rejects the forward with:
  `PRE_QA_GATE_FAIL wiring BL-859 swarmforge/roles/specifier.prompt does not
  contain "boot prefix budget gate" (...)`.
- **Remediation**: Either adjust the wording in
  `swarmforge/roles/specifier.prompt` to contain the literal substring `boot
  prefix budget gate` (e.g. rephrase the heading/body so it appears verbatim,
  matching the ticket's own required_wiring pattern), or — if the pattern
  itself was miswritten — that is a ticket-YAML fix, but the pattern as
  written is a reasonable, unambiguous description of the section that
  exists; the simpler and correct fix is wording the prose to match it
  literally, per the BL-874 lesson that required_wiring patterns are
  substring-matched literals, not paraphrases.
- **Blocked by**: nothing — this is the only finding from this pass.

## Inventory

- D1: wiring, blamed on coder, remediation above.
- No other items found: doc deliverable is complete, docs/index.md updated
  in the same commit, no other defects observed in this pass.
