# BL-828 QA bounce 4 — 20260825

**Reviewed tip:** documenter `93e539fae6` (not merged — tip impure)
**Task:** `BL-828-bubble-collapsed-gesture-model`
**Sibling:** `VERIFY BL-828`
**Prior:** bounce3 `8e12913de4`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 93e539fae6  # fails
git rev-list --count 93e539fae6..origin/main  # → 1
```

**Commit hash checked:** `93e539fae6`

**First error excerpt:**
```
main_NOT_ancestor
behind=1: cdf61ad59 Coordinator daily briefing — 2026-08-25 (due refresh)
paths=22 dels=0
(rematch4 claimed "verify ancestry immediately before handoff" — still raced)
```

**Failure class:** `behavior`

**Expected vs observed:** `origin/main` must be ancestor at QA claim.
Observed tip one briefing commit stale.

**Remediation:** In the **same shell session** as `swarm_handoff.sh`, after
product restore: `git fetch origin main && git merge origin/main` (or
reset --hard + restore) then re-check
`git merge-base --is-ancestor origin/main HEAD` **and** re-fetch once more
immediately before writing the handoff draft. Briefing lands often; one
fetch is not enough if handoff is delayed.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
