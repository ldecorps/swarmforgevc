# BL-1309 — documenter pass on the option-2 rebuild (2026-09-04)

## Context
Received `0654b8af97` (hardener, batch: unit 12/12, acceptance 10/10,
property 3/3, mutation 9 killed/0 survived/2 equivalent, Gherkin mutation
14/14). This is a REBUILD, not an extension: BL-1375's ruling revised the
ticket's own `human_ruling:` from option 1 (refuse every entangled tip,
unconditionally) to option 2 (refuse only a withheld/awaiting-approval/
unreadable sibling; an approved sibling rides), because option 1 was
approved but never landed — it deadlocked the land queue within hours of
real use.

## Doc-domain review
Both doc entries this ticket owns described the SUPERSEDED option-1
behavior (written by an earlier documenter pass whose commit never landed
on `main` — confirmed: `git show origin/main:docs/reference/Specification.MD`
carries no BL-1309 mechanism entry, only passing mentions). Rewrote both
in place rather than stacking a second entry, since the old text was never
shipped and is now actively wrong about the current predicate:

- `docs/how-to/BL-1144-frequent-qa-push-races-on-main-land.md`'s
  "`--decide-only` now also refuses an entangled tip (BL-1309)" section:
  replaced the "option 1, unconditional refusal" paragraph with the
  option-2 narrowing, the `blocking-siblings` predicate name, and the
  three blocker states (`withheld`/`awaiting-approval`/`unreadable`) each
  sibling line now carries, verified against `land_main_publish.sh`'s
  `entangled_sibling_report` and `land_step_lib.bb`'s `ticket-approval-state`.
- `docs/reference/Specification.MD`: replaced the stale September 3
  BL-1309 entry (option 1) with a September 4 entry (option 2) describing
  the same mechanism accurately, and moved it to the TOP of the
  chronological stack (today's newest entry) rather than leaving it in its
  old position dated September 3.
- `docs/index.md`'s BL-1144 how-to summary line named "unconditionally" —
  corrected to name the three blocking states.

Diagram registry checked — no diagram depicts land-step internals (same
finding as every other land-step-remedy-chain ticket this batch).

## Verdict
Doc content corrected (not NONE) — two stale sections and one index line
rewritten to match the shipped predicate. Forwarding to QA.
