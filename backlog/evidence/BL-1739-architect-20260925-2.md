# BL-1739 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

Reviewing coder's QA-bounce fix (3c485ff1ca): exports `isLiveCodePath`
from `bl611BabysitterdLifecycleSteps.js`, rewrites BL-781's property
test invariant 3 to assert each `DELETED_WAKE` path is live code via
`isLiveCodePath` (rather than regex-reading the deleted
`isAllowedBabysitterMatch`), and fixes `loadBl611Scan`'s stale slice
marker (`function isAllowedBabysitterMatch` → `const RETIRED_FILE_PATHS`)
and stale `scan.offenders` field name (→ `scan.liveCodeMatches`) in the
BL-781 acceptance step handler — same rename this ticket made earlier,
QA caught two readers it missed.

Checks run:
- `node out/tools/dependency-gate.js` (from `extension/`) on the three
  changed files: PASSED, no forbidden edges.
- `node out/tools/co-change-report.js` on the same three files: every
  reported co-change is frequency 1, below the default threshold (3) —
  nothing flagged as coupling.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl781LiveGrepOffender.property.test.js`: 5/5.
- Independently re-verified non-vacuity myself: mutated `isLiveCodePath`
  to except `babysitter_lib.bb`, reran the property test — invariant 3
  failed exactly as expected (`false` vs expected `true`). Restored the
  file (confirmed `git diff` clean), reran green.
- `node specs/pipeline/cli.js
  specs/features/BL-611-deterministic-babysitterd-managed-by-swarm-lifecycle.feature`:
  27/27.
- `node specs/pipeline/cli.js
  specs/features/BL-781-retire-dead-babysitter-files-keep-list-preserved.feature`:
  12/12.
- `npm run compile` (extension): clean.
- `grep -rn 'isAllowedBabysitterMatch\|scan\.offenders' specs/
  extension/test/`: only history comments naming the retired symbol, no
  live reference — confirms QA's two readers are both fully rebuilt, no
  dead code left behind.
- Scope check: the coder's fix commit (3c485ff1ca) touches exactly the
  ticket's Scope files plus evidence — no unrelated diff.

No production code touched beyond the pure `isLiveCodePath` export (no
behavior change to it). No defect found.
