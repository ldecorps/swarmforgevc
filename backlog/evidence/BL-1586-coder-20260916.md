# BL-1586 - sweep 2 of 6, coder pass, 2026-09-16

Applied the census's recipe (`backlog/evidence/BL-1583-sampled-reach-floor-census-20260915.md`
"The fix, per file") to the 16 pinned files. 15 of the 16 now `require`
`./helpers/reachFloors` and drive their reach floor(s) through
`runsPerCell`/`assertReachFloor`. The 16th (bl1362) carries no genuine
reach-floor pattern at all and is left unchanged per the recipe's third
branch (below). No production code touched; no floor value, draw budget,
timeout or tolerance was lowered, raised, widened, seeded or retried -
every pre-sweep assertion line is still present, unchanged, alongside the
new construction.

Note on scope: this ticket's `required_stages` covers `[coder, cleaner,
architect, hardender, documenter, qa]`; there is no separate deprecator
stage, so no deprecator freshness note applies here.

## Table: file, branch, cells, budget, floors kept

| file | branch | cells | budget (unchanged) | floors kept |
|---|---|---|---|---|
| bl1342CrashloopStampInvariants | sampled (inv1, 2 cells via explicit calls) + constructed by hand (inv3, 6 cells) | inv1: io/nonIo group, 2; inv3: young/boundary/old/unknown/deadYoung/deadOld, 6 | inv1: 8 (4/cell); inv3: 18 (3/cell) | `reach.io`/`reach.nonIo` floors (2 and 4, the `LENGTH-1` literals, unchanged) via `assertReachFloor` per key; the 6 grace-case floors unchanged. Invariant 2's `runs > 0` is **not a floor** (a single scalar tally, unconditional every call - the bl1225/BL-1585 shape) - left unchanged, quoted here. |
| bl1344WaiveInvariants | sampled (inv1, other-class axis now a cell) + constructed by hand (inv3, 4 cells) | inv1: pipeline-code-on-main/stuck-parcel/daemon-down, 3; inv3: unreadable/unparseable/incomplete/empty, 4 | inv1: 6 (2/cell); inv3: 12 (3/cell) | `reach.sameClass`/`reach.differentClass` (floor 1 each) via `assertReachFloor`; the 4 unreadable-store-shape floors unchanged. Invariant 2's `noStore`/`existingWaive`/`repeatedSweeps` are **not floors** (three unconditional checkpoints in a fixed sequence, not a coverage measure over any drawn population) - left unchanged, quoted here. |
| bl1345StaleMarkerInvariants | constructed by hand (both invariants) | inv1: 4 marker states; inv2: 4 rotationRouter x sameRole cases | inv1: 12 (3/cell); inv2: 24 (6/cell) | `reach.usable`/`reach.unusable` (floor 1) and `reach.mismatch`/`reach.match`/`reach.router` (floor 1) via `assertReachFloor`. Invariant 3 carries no reach-floor pattern at all (direct correctness assertions, no counter) - untouched, not swept. |
| bl1346RcRepairStampInvariants | constructed by hand (inv1) | inv1: 5 roles | 5 (1/cell) | `reach[role]` floors (1 each) via `assertReachFloor`. Invariant 2's `reach.router`/`reach.standing` are **not a floor** (both incremented unconditionally together every run, no branching on the draw) - left unchanged, quoted here. Invariant 3 (git-diff/ledger check) carries no reach-floor pattern. |
| bl1350KeepaliveInvariants | constructed by hand (inv2's hold-open-frame test) | 3 frame shapes (keepalive/snapshot/comment) | 12 (4/cell) | `reach[name]` floors (1 each) via `assertReachFloor`. Invariant 1's `reach.windows` (test 1, single scalar tally) is **not a floor** - left unchanged, quoted here. Invariant 1's "other side" test has no reach-floor pattern. |
| bl1352EscalationVisibilityInvariants | constructed by hand (inv1) + sampled->constructed (inv2) | inv1: 4 transport x waiting cases; inv2: held/changed, 2 (changeAt now derived per cell via `fc.chain` rather than drawn independently) | inv1: 16 (4/cell); inv2: 10 (5/cell) | `reach[key]` (inv1, floor 1 each) and `reach.held`/`reach.changed` (inv2, floor 1 each) via `assertReachFloor`. |
| bl1354SharedPathLandedSiblingInvariants | constructed by hand (both invariants) | inv2 (title says inv2 first in file): 3 mixes; inv1: 3 unanswerable-attribution shapes | inv2: 9 (3/cell); inv1: 9 (3/cell) | `reach[mix]` and `reach[shape]` floors (1 each) via `assertReachFloor`. |
| bl1362ReviewEvidenceByToolInvariants | **not a floor after all** (whole file) | n/a | unchanged (4 and 3) | Both tests' reach counters (`none`/`items`/`refused`/`collision` in P2+P3; `reached` in P1) are unconditional checkpoints/tallies in a fixed execution sequence - none is a coverage measure over any drawn value. File left completely unchanged; excluded from the acceptance's structural check (the step handler names it explicitly) per the recipe's "never add a runsPerCell call to a file that draws nothing." Architect: please confirm this exclusion so the epic's census (BL-1583) can be corrected. |
| bl1375ApprovedSiblingsCanLandInvariants | constructed by hand (both invariants) | inv1: 6 approval shapes; inv2: 2 consistency cases | inv1: 18 (3/cell); inv2: 4 (2/cell) | `reach[shape.name]` and `reach.dangling`/`reach.resolved` floors (1 each) via `assertReachFloor`. Invariant 3's `checked` is a single scalar tally across all shape iterations, not per-shape - **not a floor** - left unchanged. |
| bl1380ExpediteNeverAnswersUnshownQuestion | constructed by hand (P3) | 3 shapes (options-with-ruling/no-options-pending/no-options-never-pending) | 9 (3/cell) | `reach[shape]` floors (1 each) via `assertReachFloor`. P1+P2's `reached` is a single scalar tally - **not a floor** - left unchanged. |
| bl1389UnlandedSiblingPathNeverRidesInvariants | constructed by hand (both invariants) | inv1: 3 approval shapes; inv2: 2 sibling-landed cases | inv1: 9 (3/cell); inv2: 6 (3/cell) | `reach[s.name]` and `reach['some-landed']`/`reach['all-landed']` floors (1 each) via `assertReachFloor`. Invariant 3 carries no reach-floor pattern. |
| bl1460IdleEventsOneSnapshotInvariants | already constructed by hand, direct (not fc.assert-loop) | 2 shapes (allIdle/backToBackChanges), guaranteed via direct deterministic calls outside any fc.assert | main exploratory pass: 8 (unchanged, now `runsPerCell(8, 1)` - its own single cell); the 2 forced calls carry no numRuns (deterministic, not drawn) | `reach.allIdle`/`reach.backToBackChanges` converted from booleans to counters and asserted via `assertReachFloor` (floor 1 each); no other change - the file already reached both shapes by construction, only the helper functions were not yet used. |
| bl1481SharedPathContentCheckInvariants | constructed by hand (inv1, inv3) | inv1: 2 still-blocking shapes; inv3: 3 shapes | inv1: 6 (3/cell); inv3: 9 (3/cell) | `reach[shape]` floors (1 each) via `assertReachFloor` in both. Invariant 2's `reach` is a single scalar tally - **not a floor** - left unchanged. |
| bl1538Bl1028RunnerFixtureClosureInvariants | constructed by hand (both invariants; already imported `assertReachFloor`, only `runsPerCell` was missing) | inv1: 2 depths; inv2: closure.length members (dynamic) | inv1: 6 (3/cell, unchanged); inv2: closure.length (1/cell, unchanged) | `DEPTH_FLOOR` and `CLOSURE_CELL_RUNS` now derived via `runsPerCell` instead of a bare literal; `assertReachFloor` calls (already present) unchanged in value. Verified with a break-then-restore: dropping one closure member from the iteration fails naming it (`reach floor: closure member dropped acceptance_pointer_gate_lib.bb drawn 0 < 1`), then restored. |
| bl1546ClosedOwnerNeverSilentlyExcludesInvariants | constructed by hand (both invariants) | inv1: 2 shapes (own-touch/no-own-touch); inv2: 3 shapes | inv1: 6 (3/cell); inv2: 9 (3/cell) | `reach[shape]` floors (1 each) via `assertReachFloor` in both. |
| bl687EpicTileSurfaceUntouched | sampled->constructed (epic-slug axis now a cell) | 2 cells (sameEpicSlug/crossEpicSlug); the first drawn active-extra's `sameEpicSlug` field is forced per cell, the rest of the draw (ids, priorities, doneExtras) stays random | 6 (3/cell, unchanged) | `reach.sameEpicSlug`/`reach.crossEpicSlug` floors (1 each) via `assertReachFloor`, replacing two booleans set inside the single prior fc.assert. |

## Verification run (coder pass)

- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1586-sampled-reach-floors-sweep-2-of-6.feature` - 17/17 pass (bl1362's row passes via the step handler's documented exclusion, not by a fabricated migration).
- All 16 files, `npx vitest run --config vitest.properties.config.mjs <files>` - 39/39 tests green, run in two batches back to back with no flake (bl1352's chain-based construction re-run 5x standalone with no flake).
- Break-then-restore proof on the smallest-budget file (bl1538, budget 1/cell): dropping one closure member from the iteration (`closure.slice(1)`) fails with `reach floor: closure member dropped acceptance_pointer_gate_lib.bb drawn 0 < 1`; restored and re-verified green.
- `git status --short`: only the 16 pinned test files plus the new step handler (`specs/pipeline/steps/bl1586SampledReachFloorsSweep2Steps.js`) are touched - no production source, no helper file.

## Invariants (BL-654)

- Invariant 2 ("every swept file reaches each reach floor by construction... through the shared helpers") is encoded structurally, twice: the acceptance feature's scenario 01 (Gherkin, per file, with the bl1362 exclusion named and enforced by the step handler rather than papered over) and the migration itself - every pinned file that carries a genuine floor now demonstrably `require`s and calls both `runsPerCell` and `assertReachFloor`. No separate `*.property.test.js` file was added for this invariant for the same reason BL-1585 gave: the population it quantifies over (the 16 pinned files' own source) is exactly what the acceptance scenario already drives end to end through the real step handler, proven non-vacuous by BL-1578/BL-1580/BL-1585's own precedent of the identical structural pattern, and reproven here by the bl1538 break-then-restore.
- Invariant 1 ("no reach floor value, draw budget, timeout or tolerance... is lowered... every floor asserted before the sweep is asserted after it at the same value") quantifies over THIS PARCEL's own diff, not over a stable, reusable testable module - the same exemption BL-1585 recorded (`01_roles.md`'s Invariants section: "quantifies over prose or process rather than a pure, testable module"). The parcel carries the QA `qa_e2e_procedure`'s step 3 (the `git diff main...HEAD -- extension/test | grep ...` floor audit) plus this table recording every kept value by hand for QA to cross-check.
