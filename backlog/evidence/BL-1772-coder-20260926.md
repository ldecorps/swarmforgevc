# BL-1772 — coder pass, 2026-09-26 (stamp-off review of hotfix db4da5c573)

## What was reviewed

`land_main_publish.sh`'s `run_push` now invokes `land_step_cli.bb repoint`
after `LAND_PUBLISHED`, the same verb `run_land` already ran (BL-1438),
passing the published commit's own subject as the BL-1467 landed-task when
one can be read. `QA.prompt`'s standing recipe (`--decide-only` then a
hand-built tip-pure commit then `--push`, never `--land`) is what made the
re-point never fire before this hotfix — confirmed by reading both files
(neither edited; read-only per the ticket's FIRM constraint).

## What was built (BL-233 draft promotion, same commit as the handler)

- Promoted `specs/features/BL-1772-...feature.draft` to a live `.feature`
  file (`git mv`, content unchanged) and flipped the ticket YAML's
  `acceptance:` pointer to match, per the coder role's own two-edits-one-commit
  rule.
- New `specs/pipeline/steps/bl1772PushPublishRepointsQaBranchSteps.js`:
  drives the REAL `land_main_publish.sh --push` against a real fixture
  repository (a bare origin, the real `swarmforge/scripts` symlinked in) —
  never a reimplementation of the publish or re-point decision logic.
  Fixture construction mirrors `test_bl1772_push_repoints.sh` (the shell
  suite the hotfix already landed with) exactly: `growQaBranch` (a
  leftover local-only commit) then `makeTipPure` (a detached commit off
  `origin/main`, checked back out — the tip-pure landing `--push`
  publishes, never reachable from the QA branch's own history). Scenario
  03's refusal case builds a synthetic 2-parent commit via `commit-tree`
  directly (never checked out), matching the shell suite's own construction
  for triggering `verify-push-safe`'s merge-commit refusal. Both the
  worktree and its bare origin live under one `trackedTmpRoot` root
  (BL-1636's migration-complete gate — never a raw `fs.mkdtempSync`).

## Review goals (from the ticket)

1. **Scenarios 01–03 green against the real `--push`**: 4/4 (01, both
   rows of the 02 outline, 03) — see Checks run.
2. **`test_bl1772_push_repoints.sh` ALL PASS on the parcel commit**: 14/14,
   unchanged (already landed with the hotfix; re-run only, not rewritten —
   no finding said the suite was wrong).
3. **PROBE, do not fix — a dirty QA worktree still skips, leaving the
   branch long on one unclean land**: confirmed exactly this by scenario
   02's own two rows (an uncommitted change; a parcel in its in_process) —
   both publish successfully but skip the re-point, leaving the leftover
   branch history in place. This is the documented, accepted shape
   (`post-land-repoint!`'s own fail-closed guards); the ticket's own text
   already states the follow-up path is a note, never an edit here, and
   no NEW finding beyond what the ticket already names surfaced during
   this review — no note sent.

## Invariants (3, this ticket)

All three quantify over the ALREADY-LANDED hotfix's own script-level
control flow and this parcel's own diff shape, not a pure module a
generator can drive — the same disposition class BL-1698's invariant 2 and
BL-1767's non-encoded half both used. **Not encoded as properties, stated
reasons** (never silently unencoded):

1. "The stamp never rewrites the hotfix" — a property of which files
   THIS PARCEL's own commit touches, not runtime behavior; verified
   instead by the `git diff main...HEAD --name-only` check above (Checks
   run) and by having read, never edited, `land_main_publish.sh`/
   `QA.prompt` throughout this review.
2. "A skipped re-point never fails a --push that already printed
   LAND_PUBLISHED" — a real bash script's exit-code/output contract under
   conditions only a real git worktree + real subprocess (`bb`, `git`)
   can produce, not a pure JS function; a fast-check generator over this
   domain would just re-shape the acceptance Scenario Outline's own two
   real cases (an uncommitted change; a parcel in its in_process) without
   adding genuine reach — verified instead by scenario 02's two rows
   (both: LAND_PUBLISHED then LAND_REPOINT_SKIPPED, exit 0, nothing
   moved) against the real script.
3. "The re-point runs only after LAND_PUBLISHED... never more than once
   per --push" — same class; verified by scenario 03 (LAND_STOPPED, no
   re-point line at all) and scenarios 01/02 (exactly one
   LAND_REPOINTED/LAND_REPOINT_SKIPPED line each) against the real
   script's real output.

Re-implementing any of the three as a parallel JS decision function
purely to property-test it would itself be the in-parcel rewrite the
ticket's FIRM constraint forbids.

## Disposition

**Confirmed.** The hotfix does what its own commit message and the
ticket's "What landed" section claim: `--push` invokes the same re-point
verb `--land` already ran, a skip never fails the publish, and the
re-point runs only after `LAND_PUBLISHED` (never on `LAND_STOPPED`, never
on a refused push, never twice per `--push` — scenario 03 and the shared
`When` step each invoke the script exactly once). No finding requiring a
follow-up note to the specifier.

## Checks run

- `node specs/pipeline/cli.js specs/features/BL-1772-the-push-publish-re-points-the-qa-branch-after-a-land.feature`:
  4/4 pass (~5.6s).
- `bash swarmforge/scripts/test/test_bl1772_push_repoints.sh`: ALL PASS
  (14/14), unchanged.
- `cd extension && npx vitest run test/stepHandlerTmpRootGuard.test.js
  test/bl1277UnscopedStepCollisionGuard.test.js
  test/bl968StepRegistryMaterializedTreeGuard.test.js`: all green — the
  new handler trips none of the standing acceptance guards.
- `git diff main...HEAD --name-only` (parcel commit only): touches only
  the promoted feature, its handler, the ticket YAML's `acceptance:`
  pointer, and evidence — `land_main_publish.sh`, `QA.prompt`,
  `land_step_lib.bb`, `land_step_cli.bb`, and
  `test_bl1772_push_repoints.sh` are untouched (read-only, per the
  ticket's FIRM constraint and invariant 1).

By coder.
