# BL-1312 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and matches
the human's ruled option 1 exactly.

## What was checked

- Direct diff review of `socketFixtureRoot.js`: `installExitHook` now
  routes `removeStragglers` through `fixtureReaper`'s `onAbnormalExit`
  instead of a bare `process.on('exit', ...)`. Read `fixtureReaper.js`'s
  `installGlobalHandlersOnce`/`onAbnormalExit` directly to confirm the
  claim: the process-wide `exit`/`SIGINT`/`SIGTERM` listeners install
  exactly once (`globalHandlersInstalled` guard) regardless of how many
  callers register a callback — satisfies invariant 2 (one listener set,
  not one per root) by construction, not just by test.
- Confirmed no private SIGTERM/SIGINT listener was added anywhere — the
  ticket's own direction explicitly warns against that (two independent
  listeners reintroducing the ordering question the shared helper exists
  to settle). Only the shared `onAbnormalExit` primitive is used.
- Confirmed the `roleLifecycleParkUnneededSteps.js` secondary fix
  (`liveFakeBinDirs.add(dir)` moved before the stub/`.zshenv` writes) is
  correctly scoped as the ticket's own text describes: this file's OWN
  redundant BL-1305 cleanup path, not the ticket's main defect
  (`mkSocketFixtureRoot`'s own `trackedRoots` already covers `dir` from
  birth regardless).
- Confirmed the acceptance-conversion housekeeping the ticket's own
  `notes:` mandated in-parcel: `.feature.draft` renamed to `.feature`,
  the new step handler present and discovered by directory scan
  (BL-1371, no `index.js` edit needed), and the ticket's own `acceptance:`
  field repointed at the `.feature` path — all in this same commit, as
  required (a split across commits would have turned the suite red for
  every other in-flight parcel per the ticket's own BL-233 warning).
- `jscpd` found one clone, but it is entirely PRE-EXISTING and unrelated
  to this ticket's 2-line diff in `roleLifecycleParkUnneededSteps.js`
  (confirmed by running jscpd against the pre-commit version of that
  file alone — same clone, same line count, already present) — out of
  this ticket's scope, left untouched.
- Confirmed no `backlog/standing-reds.tsv` or
  `property_suite_standing_allowlist.tsv` row is owned by this ticket —
  it is a fresh defect fix, not a red-register cleanup.
- Mutation-site-count tooling does not apply to these files: they are
  plain JS under `specs/pipeline/steps/`, not TypeScript compiled to
  `extension/out/**/*.js` (Stryker's own mutate scope, per BL-485) — no
  applicable check to run here.
- `npm run compile` clean.

## Verification (all re-run in this worktree, all green, matching every claimed number)

| check | result |
|---|---|
| acceptance `BL-1312-fixture-root-signal-cleanup.feature` | 4/4 pass |
| acceptance `BL-324-per-role-lifecycle-park-unneeded-roles.feature` (regression) | 11/11 pass, unaffected |
| acceptance `BL-1305-fixture-agent-binary-is-the-stub.feature` (regression) | 3/3 pass, unaffected |
| `npx vitest run test/{socketFixtureShortRootGuard,bl948SocketGuardLimitParity,tmuxReaperGuard}.test.js` | 34/34 pass |
| `npx vitest run --config vitest.properties.config.mjs test/{bl948SocketFixtureInvariants,fixtureReaperLiveSocketGuard,bl1032TmuxReaperScope,bl789MacHostSwitchFreshnessBridgeAdoptInvariants}.property.test.js` | 11/11 pass |
| `grep -c onAbnormalExit specs/pipeline/steps/lib/socketFixtureRoot.js` | 4 |
| `npm run compile` | clean |
| `npx jscpd` on touched/new files | 1 clone found, confirmed pre-existing/out-of-scope (unrelated to this ticket's diff) |
| standing-red register / allowlist rows owned by this ticket | none |

No fix required. Forwarding unchanged to architect.
