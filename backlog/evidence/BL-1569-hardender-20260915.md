# BL-1569 — hardender review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Gates run, all green:
- `bb swarmforge/scripts/test/bb_load_closure_agreement_test_runner.bb` — ok, 5 entry points agree.
- `bash swarmforge/scripts/test/test_operator_runtime_hotfix_certification_sweep.sh` — previously red on main since 2026-08-31, now ALL CHECKS PASSED.
- `npx vitest run --config vitest.properties.config.mjs test/operatorRuntimeBbClosure.property.test.js` — 3/3.
- `specs/pipeline/scripts/run_acceptance.sh` on the ticket's feature — 7/7 scenarios.
- All 18 whole-tree guard files under `extension/test/*Guard*.test.js` (excluding `.property.`) — 183/183.
- No Stryker/CRAP target: no `extension/src/*.ts` touched; the JS twin lives under `specs/pipeline/steps/lib`, outside Stryker's `out/**/*.js` scope; `bb_load_closure_lib.bb` has no wired mutation tool (BL-472).

Hand-authored mutation sweep (BL-567 fallback for `.bb`/untargeted JS), each mutant restored after confirming a kill:
1. Drop `strip-comment-lines` — killed by the agreement runner (`handoff_lib.bb` disagreement via a doc-comment example).
2. Revert `(str/join "/" segs)` to `(last segs)` (the original last-segment-only bug) — killed by the agreement runner (`swarm_handoff.bb`/`unregistered_test_gate_lib.bb` disagreements, bare `suite_inventory_lib.bb` reappears).
3. Remove `copy_bb_closure`'s explicit `[[ ! -f "$src/$dep" ]]` fail-loud check — SURVIVED the as-received scenario 04 (it ran the copy under `set -euo pipefail` with a single closure member, so `set -e` masked the missing check by aborting on cp's own failure regardless of position). Verified by hand that a real production caller (`test_bl1028_promotion_obeys_integrity_refusal.sh`) has no `set -e`, and there a missing dependency that is not the loop's LAST member silently returns exit 0 without the explicit check — the exact silent-skip defect BL-1569 exists to close. Strengthened scenario 04 in this same commit (second present dependency sorting after the missing one, copy run under `set -uo pipefail` only) so the mutant is now killed; re-confirmed correct implementation passes and mutant fails.

No defect found outside the parcel's own scope; the one gap found (mutant 3) was in this same ticket's own new acceptance scenario and was fixed in this pass rather than bounced.
