# BL-754 QA bounce — 2026-08-27

**Routing:** coder (tip-pure handoff / branch pollution — same class as
`BL-754-cleaner-bounce-20260827.md`)

## Failing command

`git diff 0f7c8b2ae..b2c66927c0 --stat` (entanglement inventory after
`merge_and_process documenter b2c66927c0`)

## Commit tested

`b2c66927c0` (documenter handoff) merged to QA as `b9f115c20d`.

## First error excerpt

```
146 files changed, 2832 insertions(+), 2320 deletions(-)
```

Includes out-of-scope functional deltas beyond BL-754 rematch:

- Retire **BL-545** (`catchUpLive.ts`, feature, step handler deleted)
- **BL-1160** live-screen activity dot (extension + docs + feature)
- **BL-1161** miniapp watchdog feature minted
- Reactivate **BL-656** / **BL-736** from `done/` → `active/`
- Repause **BL-1019**, **BL-1103**, **BL-1133**, **BL-752**, **BL-780**, **BL-980**
- `bridgeServer.ts`, `pendingApprovalReply.ts`, `telegramFrontDeskBotCore.ts`, etc.

BL-754-specific delta vs `origin/main` is only invariant encoding in
`required_stages_test_runner.bb` (+ evidence, mutation-manifest timestamp).
`required_stages_lib.bb` is byte-identical to main.

## Failure class

behavior

## Expected vs observed

**Expected:** a `git_handoff` naming BL-754 carries a commit whose tree diff
vs `origin/main` is limited to BL-754 rematch deliverables (invariant tests,
evidence, docs touch). QA approval authorizes only that ticket (BL-506).

**Observed:** documenter tip `b2c66927c0` is 218 commits / 146 files ahead of
`origin/main`, bundling many other tickets' functional changes. Landing would
integrate all of them under a BL-754-only approval.

## Defects

**D1 — entangled handoff (blame: coder):** Branch pollution on the documenter
lineage — multiple re-promotion tickets (BL-779, BL-1020, BL-1023, BL-784,
BL-1084, BL-1160, BL-545 retirement, etc.) merged onto the same tip forwarded
as BL-754.

- **Remediation:** Re-handoff BL-754 from a tip-pure commit (evidence-only
  re-promotion like `b6f92083b`, or cherry-pick line isolated from hitchhikers).
  Do not forward the full documenter branch tip.

## BL-754 functional checks (PASS — not bounce items)

| check | result |
|---|---|
| Ancestry: `ed424eb7d`, `b6f92083b` ancestor of tip | OK |
| `bb required_stages_test_runner.bb` | ALL PASS |
| `run_acceptance.sh BL-754-…feature` | 5/5 |
| Invariants 1–3 encoded in test runner | OK |
| Wiring: `swarm_handoff.bb` → `read-stage-skip-reasons` | OK |
| Docs: `docs/how-to/BL-754-…md` | present |

## Standing debt (already ticketed — report only)

| check | result | ticket |
|---|---|---|
| Full unit suite | 201 failures (`CURSOR_API_KEY` bridge suites) | **BL-720** |
| `npm run test:properties` | 16 failures + vitest-worker timeout | **BL-720** / known artifact |

By QA.
