# BL-1440 — coder pass, 2026-09-06

## What was wanted

BL-945's dangling-constitution-citation guard has been red on main since
2026-08-27 (`docs/deprecated/`, cited by Article 3.6 and its reference
pages, was never created) and got a second wound on 2026-09-05 (Article
1.10's two Art Director design-document citations). The guard is a
vitest test only, so no commit guard caught either citation landing, and
Stryker's whole-suite dry run refuses on this one red - the exact thing
that blocked BL-1439's BL-954 mutation gate.

## What landed

- **`docs/deprecated/README.md`** (new): states what arrives here (a
  page Article 1.7's documenter moves in the same commit that retires
  its behaviour, per Article 3.6's `/deprecate confirm`), that nothing
  is retired yet, and a table for future retirements.
- **`docs/index.md`**: links `deprecated/README.md` under Reference
  (required_wiring).
- **`docs/design/artifact-inventory.md`** / **`docs/design/system.md`**
  (new, human ruling option 1): seeded from the BL-1417 epic's own
  artifact list (briefing email, Telegram messages, PWA, console
  screens, rendered docs) and the one already-ruled asset
  (`docs/branding/icon-system.md`, BL-945's icon system), each row
  naming its producer module and how to view it on the real surface;
  both documents say the Art Director keeps them current from its first
  pass, per the ruling.
- **`swarmforge/scripts/check_constitution_doc_citations.sh`** (new
  commit guard): when a staged change touches
  `swarmforge/constitution/articles/**`, mirrors the STAGED articles
  corpus (via `git show :<path>`, reference/ subdirectory included) into
  a temp snapshot and calls the SAME resolver the suite test uses
  (`specs/pipeline/steps/lib/constitutionDocCitations.js`'s
  `findUnresolvedCitations`) - never a second implementation (invariant
  2). Skips cleanly when nothing under the prefix is staged. Refuses
  naming the citing article (translated back from the snapshot path to
  its real repo-relative name) and the unresolved citation.
- **`swarmforge/scripts/run_commit_guards.sh`**: wired the new guard
  into the cheap tier (BL-1252/BL-1242 shape - runs alongside every
  other cheap guard, one combined refusal report).
- **`backlog/standing-reds.tsv`**: the `unit` row for
  `constitutionDocCitations.test.js` removed (standing-red rule - a land
  that turns the test green removes its own register row in the same
  commit).
- **`specs/pipeline/steps/bl1440ConstitutionCitationsResolveSteps.js`**
  (new): scenarios 01-02 are a read-only live-tree read (the resolver
  called directly, and the two doc files/index link checked); scenario
  03 drives the REAL guard shell script against a fixture git repo
  (mkdtemp + `git init`, the resolver lib copied in at the same
  repo-relative path the guard expects, per BL-1390's "prove the fixture
  root" posture).

## Declared invariants

1. **A cited path resolves, or the citation is removed - never reworded
   to hide a path it still relies on, never a loosened test.** All six
   previously-dangling citations now resolve on disk; no article text
   was reworded to stop citing them.
2. **The commit guard and the suite test share one resolver.** The
   guard's own node one-liner requires the identical
   `constitutionDocCitations.js` module the vitest test imports -
   demonstrated directly: the guard's fixture scenario (03) and the
   suite test's own fixture tests exercise the same function, and
   nothing in this parcel adds a second citation-extraction regex or a
   second resolution rule.

## Non-vacuity (byte-identical restore confirmed)

Mutated the guard's own refusal branch (`if (( NODE_STATUS != 0 ))` →
`if (( 0 ))`, i.e. never refuses) - acceptance scenario 03's "refuses"
row failed immediately (exit 0 where nonzero was expected). Restored;
`diff /tmp/check_constitution_doc_citations.sh.bak
swarmforge/scripts/check_constitution_doc_citations.sh` empty;
acceptance back to 4/4 immediately after, and the executable bit
re-confirmed present.

## Verification

| check | result |
|---|---|
| `npx vitest run test/constitutionDocCitations.test.js` | 6/6 (was 5/6) |
| `npx vitest run` (whole extension suite) | 610/610 files, 10283/10283 tests (one run hit the known, pre-existing `telegramCursorOperatorExec.test.js` tmp-dir-sweep race; confirmed a pure flake by an immediate clean re-run) |
| acceptance `BL-1440-every-constitution-doc-citation-resolves.feature` | 4/4, stable across two runs, no leaked fixture dirs |
| acceptance `BL-1439-....feature` (the ticket this red blocked) | 4/4, unaffected |
| live: staged a bogus `docs/nope.md` citation, ran `bash swarmforge/scripts/run_commit_guards.sh` | refused, naming the article and the path; removed the line, worktree restored clean |
| `grep -c constitutionDocCitations backlog/standing-reds.tsv` | 0 |

## required_wiring

- `swarmforge/scripts/run_commit_guards.sh::check_constitution_doc_citations.sh` -
  present.
- `docs/index.md::deprecated/README.md` - present.
- `specs/pipeline/steps/bl1440ConstitutionCitationsResolveSteps.js::registerSteps` -
  present, discovered by directory scan (BL-1371), confirmed by the
  acceptance run passing 4/4.

## Out of scope (per the ticket's own text, not acted on here)

- Writing design briefs or ruling on look and feel - Article 1.10's own
  job, not this parcel's.
- Retiring any page into `docs/deprecated/` - the directory is seeded
  with its own index only; nothing is retired by this parcel.
