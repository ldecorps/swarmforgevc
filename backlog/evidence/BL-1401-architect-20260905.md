# BL-1401 architect review — clean sweep, forwarding

## Reviewed
Fast-forward merged cleaner's `451314e2d6`. As with the prior fast-forward
this session, the merge resurrected two stale duplicate intake files at
`backlog/` root (`INTAKE-human-expedite-park-must-unhold-on-finish-...md`,
`INTAKE-operator-question-...md`) since cleaner's branch forked before this
session's BL-1379/BL-1402 archiving. Confirmed via diff (again) that both
root copies are strict prefixes of their already-correct `backlog/archive/`
counterparts; removed the stale root duplicates in a follow-up commit,
keeping the archived record. Read the ticket's three invariants, coder and
cleaner evidence, and the live handler diff.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl632CommitTimeGuardSteps.js ../specs/pipeline/steps/bl1401AcceptanceFixtureDerivesItsSetSteps.js
```
No forbidden edges.

## Logical coupling (BL-255)
`co-change-report.js` against the three changed files: only frequency-1
hits, below the default threshold (3). Nothing flagged.

## Invariants — verified directly in the live handler
1/2. **Derived, never hand-listed; a missing guard fails loud.** Both are
properties of `deriveCommitGuardFixtureSet()` itself (already reviewed and
approved on BL-1398) — this ticket's job is consuming it, not re-deriving
it, which the code does.
3. **Exactly one parser of `run_guard` lines.** Grepped
`bl632CommitTimeGuardSteps.js` directly: the only occurrence of `run_guard`
is the explanatory comment at line 40 ("no second parser of `run_guard`
lines exists in the repo"); the actual guard set comes from one call,
`deriveCommitGuardFixtureSet({ repoRoot: REPO_ROOT }).files` at line 81. No
regex, no hand-written list.

## Independent re-verification
- **The red this ticket exists to close**: `run_acceptance.sh
  specs/features/BL-632-commit-time-guard-refuses-pipeline-code-on-main.feature`
  → **11/11 pass** (confirmed previously red at 4 pass/7 fail on `main`,
  matching my own earlier BL-1398 review finding).
- `test_bl1401_acceptance_fixture_derives_set.sh`: **6/6 PASS** — the real
  BL-632 feature passing, the handler consuming the helper, no second
  parse, a seam-added guard appearing in the derived set and running, and a
  seam-named-but-absent guard failing the build by name.
- Acceptance (`BL-1401-...feature`): **3/3 pass**.
- Property test (`bl1401AcceptanceFixtureDerivedSet.property.test.js`,
  after `npm run compile`): **2/2 pass**.
- `jscpd` over the three changed/new JS files: **0 clones**.

## required_wiring
Both anchors confirmed live: `bl632CommitTimeGuardSteps.js` calls
`deriveCommitGuardFixtureSet` (anchor 1), and the new step handler matches
BL-1371's discovery-suffix convention (anchor 2).

## Scope check
The BL-632 feature file itself is untouched (per the ticket's explicit
instruction that its scenarios were already correct) — confirmed no diff
to that file in this commit. `depends_on: [BL-1398]` correctly sequenced
(BL-1398 is done, so this ticket was promotable). No second parser was
written to route around the dependency, matching invariant 3 and the
ticket's own INVEST reasoning.

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener.
