# BL-1377 — cleaner pass, 2026-09-03

One simplification made and verified; otherwise clean.

## Fixed

- `swarmforge/scripts/suite_baseline_cli.bb` `-main`: the two-run branch
  built `final` with an `:ran-base? true` key that no code path, evidence
  sentence, or test ever reads. It is set only inside the branch where
  `:second-run?` is already `true` (they are always identically true
  together in this build site), so it carried zero information beyond what
  `:second-run?` already states, while still leaking into the `--json`
  output as an unexercised, undocumented field. Removed the dead key; the
  `--json` shape now only ever emits fields the tests and `evidence-line`
  actually use.

## Checks run (all pass, before and after)

| # | Check | Result |
|---|-------|--------|
| C1 | `bb swarmforge/scripts/test/suite_baseline_lib_test_runner.bb` | ALL PASS |
| C2 | `bash swarmforge/scripts/test/test_suite_baseline_cli.sh` | ALL PASS (37 checks) |
| C3 | `bb swarmforge/scripts/test/bl1377_suite_baseline_property_runner.bb` | ALL PROPERTIES HOLD (500 runs) |
| C4 | `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1377-*.feature` | 10/10 pass |
| C5 | `node extension/out/tools/mutation-site-count.js` on both changed `.js` files | `bl1377SuiteBaselineSteps.js` 192 sites (advisory `over`); `index.js` 4 sites (`within`) |
| C6 | `jscpd` over the changed `.bb`/`.js` sources, min-lines 15 | 0 clones |
| C7 | `suite-manifest.tsv` / `index.js` registration | new suite files and acceptance handler registered |

## Mutation-site advisory (BL-485) — no split

`bl1377SuiteBaselineSteps.js` is 182 lines / 192 sites, over the 100-site
threshold. Same reasoning as BL-1360's and BL-1376's sibling files: one
feature's step handlers, well under the 500-1000+ line norm of the rest of
`specs/pipeline/steps/`, with the ticket's own six-scenario Given/When/Then
shape as the only natural seam and no structural gain from splitting it.
Kept whole.

## Architecture review

- `suite_baseline_lib.bb` stays pure — no suite runs, no git, no file reads —
  matching invariant 3's intent that the whole decision (`decide`,
  `parse-failures`, `record-matches?`, `evidence-line`) lives in one testable
  place; `suite_baseline_cli.bb` is the only file that shells out or touches
  the record file, and both suite runs go through the one `run-suite` seam so
  the base and parcel measurements can never be taken two different ways.
- Confirmed invariant 1 (a record excuses only what it names) and invariant 2
  (unusable record → both runs, never a manufactured green) hold in `decide`
  by inspection, matching the property runner's P1/P2 and their stated
  non-vacuous mutants.
- `parse-failures` returning `nil` (distinct from `[]`) for unreadable output,
  and the CLI's refusal on `nil`, is the right shape for "never mistake a
  broken suite run for a clean one" — checked against `default-run` and CLI
  test `01b`.

## Not addressed here

Same standing items already surfaced by BL-1376's and BL-1360's coder/cleaner
passes (`handoffd_supervisor_startup_grace_test_runner.bb` /
`test_handoffd_outbox_vanished_parcel_wiring.sh` unregistered from an
unrelated 2026-09-02 hotfix; the `backlog/active/` → `backlog/hold/` park
already flagged for the coordinator). Nothing new from this pass.

By cleaner.
