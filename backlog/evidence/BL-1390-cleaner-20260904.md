# BL-1390 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked

- `push_sweep_lib_test_runner.bb` — re-ran: ALL TESTS PASSED.
- `test_bl1390_post_commit_push.sh` — re-ran: ALL PASS, including the two
  load-bearing rows: "no push used force" and "the hook contains no git
  push of its own (BL-1198)" — the one-push-path invariant is asserted
  against the real hook file's content, not merely against behavior.
- `bl1390_post_commit_push_property_runner.bb` — re-ran: ALL PROPERTIES
  HOLD over 123 constructed states.
- One push path (invariant 3), verified structurally: `push_sweep_lib.bb`
  gained `post-commit-decision` (delegates to the pre-existing
  `push-decision`, never restates the ahead/behind question) and
  `push-main!` (the one `git push origin main`, never `--force`).
  `handoffd.bb`'s daemon-side `push-sweep-push!` was rewritten to call
  `push-sweep-lib/push-main!` instead of its own inline `git push` —
  confirmed the daemon's OWN push path was consolidated, not just the new
  hook pointed at the shared function while the old path stayed inline.
- The hook (`swarmforge/git-hooks/post-commit`) is picked up automatically:
  `swarmforge.sh` already sets `core.hooksPath swarmforge/git-hooks`
  repo-wide, confirmed by reading the install call site — no separate
  install step this ticket needed to add.
- Fail-closed and non-blocking, read directly: `exit 0` unconditionally at
  every path, the whole runner wrapped in a bounded `timeout`
  (`SWARMFORGE_POST_COMMIT_PUSH_TIMEOUT`, default 20s), and
  `post-commit-decision` treats unknown ahead/behind counts as
  `:counts-unknown` rather than defaulting to permission to push.
- Worktree discrimination read correctly: the hook resolves the acted-on
  repo from `git rev-parse --show-toplevel` (the checkout the commit
  actually happened in) rather than the hook file's own install location
  — necessary because `core.hooksPath` is shared across every worktree,
  and using the file's location would misjudge every role-branch commit
  as if it were on the shared `main` checkout.
- `required_wiring` — both anchors confirmed: `push_sweep_lib.bb` named in
  the hook's own header/body, and the step handler registers via
  BL-1371's discovery.
- `swarmforge/scripts/test/suite-manifest.tsv` — both new test files
  correctly registered in the standing lane.
- `mutation-site-count.js` on the new step handler: 139 sites (`over`
  100). Reviewed and declined to split, same reasoning as every other
  ticket this session: one cohesive single-feature handler.
- `jscpd` over the new step handler, hook, and touched `.bb` files: 0
  clones.
- TypeScript compiles clean; the handler discovers via BL-1371's registry.
- Babashka/shell have no mutation/CRAP/DRY wired (Startup Tools, BL-472
  deferred); gated by the two runners and the shell suite instead, per
  the documented degraded fallback.

No bounce history on BL-1390 (first pass). Forwarding unchanged to
architect.

By cleaner.
