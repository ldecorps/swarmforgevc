# BL-853-promotion-path-honours-no-limit-depth-sentinel — QA final pass (2026-08-08)

## Commit checked

`53ce8bfa62` (documenter, tip of the BL-852/BL-847/BL-853 chain), merged into
QA as `68b8188d`. Ancestry confirmed: coder, cleaner `3ee40328`, architect
`e68631f0`, hardener `f9503a50` are all ancestors. `git merge-base
--is-ancestor` holds for each stage's own commit against `68b8188d`.

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`, node v22.23.2): clean.
- Full unit suite (`npm test`, node v22.23.2): 401/406 files, 7228/7237
  tests green — see `backlog/evidence/BL-852-qa-pass-20260808.md` for full
  analysis; none of the 5 remaining failures touch
  `promotion_gates_lib.bb`, `promote_and_route_next.sh`, or
  `backlog_depth_conf_path_cli.bb`.
- `bb swarmforge/scripts/test/promotion_gates_lib_property_runner.bb`: 500
  runs, ALL PROPERTIES HOLD.
- Existing suites named in the ticket's notes, all re-run directly and
  green: `test_backlog_depth_cli.sh` (3/3), `test_backlog_depth_conf.sh`
  (10/10), `test_effective_backlog_depth_cli.sh` (9/9),
  `test_promote_and_route_next_priority.sh` (1/1),
  `test_promote_and_route_next_no_limit_depth.sh` (2/2),
  `test_backlog_depth_conf_path_cli.sh` (2/2).
  Two suites from that list failed for reasons independently confirmed
  pre-existing on `main` HEAD (`6c7a9d9d`), reproduced identically there,
  and untouched by this ticket's diff:
  - `test_backlog_depth_pack_override.sh`: fails on this host with a
    unix-socket path-length error from `resolve_swarm_socket.bb` (macOS temp
    dir path exceeds 100 chars) plus an unrelated unbound-variable bug in
    `test/lib/tmp_cleanup.sh` — an environment/shared-fixture issue, not a
    depth-cap regression.
  - `test_ready_for_next_no_promotion.sh`: `dispatch-unchanged-01 (task)`
    fails with `ROTATE_HOME`/`HOME_ROLE: coder` instead of the expected
    `TASK:` output. `ready_for_next.bb`, `dispatch_lib.bb`, and
    `ready_for_next_task.sh`/`.bb` are byte-identical to `main` (confirmed
    via `git diff main HEAD`) and this ticket's diff never touches them —
    reproduces identically on `main` itself. Pre-existing defect, out of
    scope for this ticket; not re-ticketed here as it is orthogonal to
    BL-853's own domain.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-853-promotion-path-honours-no-limit-depth-sentinel.feature`):
  11/11 scenarios PASS — unlimited cap never blocks (3 active-depth
  variants), resolved cap matches launched config, finite ceiling still
  refuses (6 variants, named-reason string unchanged per BL-663), unreadable
  config degrades to the shared library default rather than a shell
  literal. Noisy stderr from a fixture-local
  `effective_backlog_depth_cli.bb` throttle-recommendation refresh
  (`Cannot find module .../emit-throttle-recommendation.js` — the fixture's
  temp project root has no compiled `out/`) is a benign best-effort refresh
  failure; every subtest still reports `ok`.
- `required_wiring` independently re-verified by reading the code:
  1. `swarmforge/scripts/promotion_gates_lib.bb::depth-refusal` — guards with
     `(not (backlog-depth-lib/no-limit? max-depth))` before comparing
     `active-count` to `max-depth`; a negative cap never refuses.
  2. `swarmforge/scripts/promote_and_route_next.sh` — the unsigned
     `^[0-9]+$` check is now `^-?[0-9]+$` (signed) at both validation points;
     the fallback resolves `CONF_PATH` via
     `backlog_depth_conf_path_cli.bb "$ROOT"` first and passes that path
     (not `$ROOT`) to `backlog_depth_cli.bb`; the gate itself reads
     `(( CAP >= 0 )) && (( ACTIVE_COUNT >= CAP ))`, so a negative CAP never
     trips the refusal branch.
- Invariants: all three declared invariants covered — invariant 1 (no
  promotion path treats a negative cap as a ceiling) by acceptance scenario
  1 and the property runner; invariant 2 (no second depth/default source) by
  the wiring re-check above (`no-limit?` reused, no re-derivation);
  invariant 3 (finite-cap path byte-identical) by acceptance scenarios 5-10
  and `test_backlog_depth_conf.sh`'s pre-existing rows, all still green.
- No orphaned test/mutation processes before or after this pass.

## Verdict

Clean pass. No defects.

## Merge-up and landing (shared for the BL-852/BL-847/BL-853 batch)

- QA broadcasts a `note` (priority `00`) to `coder,cleaner,architect,
  hardender,documenter` naming this commit for merge-up.
- QA lands this commit on `main` (fast-forward/merge, push origin) — none of
  the three tickets are `GH-`-seeded, so `issue_done.sh` is not invoked.
- QA notifies the coordinator with the approved commit hash and all three
  ticket ids (BL-852, BL-847, BL-853) for backlog bookkeeping — all three
  move `active/` -> `done/` together, per Article 2.6 (a batch commit
  satisfying multiple tickets must have every satisfied ticket id reach the
  coordinator, not only the first task name forwarded).
- BL-722 is explicitly excluded: it shares this lineage but is still under
  rework upstream (bounce evidence
  `backlog/evidence/BL-722-bounce-20260808.md`, `required_wiring` marker
  still missing) and has not been forwarded to QA as its own `git_handoff`.

By QA.
