# BL-1401 — documenter pass (2026-09-05)

## Received
`69824221f0` (hardener; per `backlog/evidence/BL-1401-hardener-pass-20260905.md`).

## What changed, operator-visible
`specs/pipeline/steps/bl632CommitTimeGuardSteps.js`'s `mkFixtureRepo` — the
acceptance-side twin of BL-1398's already-fixed property fixture — carried
its own separate hand-written twelve-entry copy list and went red the same
way when `check_handler_module_graph.sh` (BL-1385) joined
`run_commit_guards.sh` (4 pass / 7 fail, reproduced by the architect
2026-09-04). Rather than write a second parser of `run_guard` lines, the
handler now calls the SAME `deriveCommitGuardFixtureSet()` helper BL-1398
shipped — one parser, two callers, so a guard added to or removed from the
runner is reflected in both without editing either.

## Doc-domain review
`docs/how-to/BL-973-bb-fixture-closure-guards-and-suite-inventory.md`'s
existing BL-1398 section is the established home.
- Added "The acceptance-side twin (BL-1401)" as a subsection right after
  the BL-1398 content it directly continues.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-973/BL-1398 summary line.
- Diagram registry checked — no diagram depicts acceptance-fixture
  internals at this granularity.
- Ran `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-632-commit-time-guard-refuses-pipeline-code-on-main.feature`
  — 11/11 PASS (was 4 pass / 7 fail on `main` per the ticket's own
  reproduction).
- Ran `bash swarmforge/scripts/test/test_bl1401_acceptance_fixture_derives_set.sh`
  — ALL PASS (6/6).
- Ran `bl1401AcceptanceFixtureDerivedSet.property.test.js` (from
  `extension/`) — 2/2 PASS.

## Verdict
Doc content extended in the owning how-to page plus the standard
Specification.MD/index.md entries, not NONE. Forwarding to QA.
