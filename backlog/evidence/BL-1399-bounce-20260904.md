# BL-1399 — QA bounce, 2026-09-04

Merged hardener `2ecdd6341e` into QA worktree (`447896612b`, itself already
carrying the specifier's amendment `9b5442f553` via routine bookkeeping
merges earlier this session). Full review before sending back (Article 4.4
— complete inventory, one bounce).

## D1 — the amended Background step text has no matching step handler; acceptance FAILS 0/3

**Failing command:** `specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1399-the-freshness-property-fixture-supplies-its-own-required-registry.feature`
**Commit tested:** `447896612b` (this QA worktree's merge tip).
**Failure class:** behavior (the feature's own acceptance step, not a
compile/unit-suite issue).

**First error excerpt:**
```
not ok 1 - the checker runs green against the fixture's own registry
  error: Scenario "the checker runs green against the fixture's own
  registry": no step handler matched "Given a freshness fixture root whose
  conf pins handoffd and carries a derived row per supervisor the guard
  walks"
```
All 3 scenarios fail identically (0/3) — every scenario shares this
Background line.

**What happened:** the specifier's in-flight amendment (`9b5442f553`,
2026-09-04T21:17:08+01:00, "record the guard's seamless supervisor arm and
adopt the derived-rows fix; re-tense the feature Background (spec-gap
raised by the coder)") re-worded the feature's `Background:` line from
`Given a freshness fixture root with a one-row conf for handoffd` to
`Given a freshness fixture root whose conf pins handoffd and carries a
derived row per supervisor the guard walks`, reflecting a real behavior
extension (the guard's second, seam-less supervisor arm, which walks its
own `$SCRIPT_DIR/*_supervisor.bb` and needs derived per-supervisor fixture
rows, not just a registry-seam fix). `9b5442f553` is NOT an ancestor of the
hardener's tip (`2ecdd6341e`, verified via `git merge-base --is-ancestor`)
— the amendment landed on `main` about a minute before hardener's forward,
on a completely separate branch (the master checkout), and never reached
the hardener's own worktree. Hardener's "acceptance 3/3" claim in their own
evidence was true and honest AT THE TIME, against the pre-amendment feature
text, which matched the pre-amendment handler wording. I only see the
mismatch because I independently merged the specifier's amendment into
this worktree earlier this session via routine coordinator-bookkeeping
merges, before this parcel arrived.

The specifier's own amendment note is explicit about ownership: "the
feature's Background said 'a one-row conf'; that is now a false sentence
in a durable contract and is re-tensed (a setup line, not an assertion;
**the coder updates the matching handler pattern in the same parcel**).
Nothing built changes. No bounce: nothing had been reviewed." — i.e. the
specifier's own intent was for the coder to update the step handler's regex
as a mechanical follow-up, not for this to be treated as an open spec
question.

Confirmed the underlying FIX is real and already implemented: ran
`swarmforge/scripts/test/test_bl1399_freshness_fixture_own_registry.sh`
directly — 6/6 PASS, including "the guard read the fixture's registry, not
the live one (live-only daemons: babysitterd)", and independently re-ran
`extension/test/bl1012FreshnessSelfInflictedIncidents.property.test.js` in
isolation — 4/4 PASS (the previously-standing-red BL-1012 suite is
genuinely fixed). The step handler's own Background body
(`bl1399FreshnessFixtureOwnRegistrySteps.js`) is trivial — it only
initializes `ctx.bl1399`, so no fixture-building logic needs to change,
only the regex string itself:
```js
scoped(/^a freshness fixture root with a one-row conf for handoffd$/, (ctx) => {
```
needs to match the amended text (e.g.
`/^a freshness fixture root whose conf pins handoffd and carries a derived row per supervisor the guard walks$/`).

**Expected vs observed:** Expected — BL-233 requires a scenario's handler
to land in the SAME commit as the feature file naming it (this ticket's own
`required_wiring` cites the same BL-1371/registration-discovery language
elsewhere). Observed — the feature text was amended on `main` directly,
the handler regex was not, and the CODE (not just the string) is otherwise
already correct and independently verified working.

**Owning role:** coder. This is not a spec-gap — the scenario is concrete,
unambiguous, and already independently proven testable end-to-end via the
shell e2e suite; the specifier's own amendment note names the coder as the
owner of this exact follow-up. It is an ordinary implementation/wiring gap
in the same file the coder already owns, precisely the class my own
BL-1390 D1 bounce this session classified the same way for the same
reason.

## Everything else checked, clean (Article 4.4 full inventory)

- `npm run compile` — clean.
- `bash swarmforge/scripts/test/test_bl1399_freshness_fixture_own_registry.sh`
  — 6/6 PASS.
- `extension/test/bl1012FreshnessSelfInflictedIncidents.property.test.js`
  (run in isolation via `npx vitest run --config
  vitest.properties.config.mjs`) — 4/4 PASS, confirming the previously-
  standing-red suite is genuinely fixed.
- `bash swarmforge/scripts/check_feature_handler_registration.sh` — rc 0
  (this guard checks registration/reachability, not "every scenario has a
  matching handler" — it did not and could not catch D1, same lesson as
  BL-1390's D1).
- Both `required_wiring` anchors confirmed present by grep:
  `FRESHNESS_REQUIRED` (×3 in the property test) and `registerSteps` (×2 in
  the step handler) — neither anchor's presence proves the Background
  step's own wiring, exactly D1's gap.
- No `extension/src` touched — CRAP/DRY N/A, matches hardener's own note
  ("no production code in scope so no mutation target").
- No orphaned test/mutation processes in this worktree before or after.
- Ancestry confirmed: coder (`01c2590744`) and hardener (`2ecdd6341e`) both
  ancestors of the merge tip.
- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1399` —
  `VERIFY BL-1399`, no open deferral.

## Disposition

Bounce to **coder** with D1: update
`specs/pipeline/steps/bl1399FreshnessFixtureOwnRegistrySteps.js`'s
Background step regex to match the amended feature text (already merged
onto `main` via `9b5442f553`, so the coder's own worktree already has the
new text once it merges `main`). Given this ticket's `required_stages:
[coder, hardender, qa]` deliberately skips cleaner/architect/documenter,
the rework need only pass back through coder → hardener → QA.

By QA.
