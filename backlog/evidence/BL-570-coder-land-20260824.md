# BL-570 land note — 2026-08-24

## Parcel

Implements `specs/features/BL-570-property-suite-drift-guard.feature`:
shared pre-commit calls `swarmforge/scripts/check_property_suite_drift.sh`.

## First commit attempt (recovered)

Running the new guard for real against this worktree's staged
`*.property.test.js` change invoked `npm run test:properties` on the live
tree. Fixture property tests then:

1. Renamed `refs/heads/swarmforge-coder` → `refs/heads/main` (expedite
   fixture author in the worktree reflog).
2. Advanced the shared `main` tip through dozens of `BL-9999` promote/close
   commits.
3. Left nested `tmp/bl*-*` worktrees that made other properties red
   (`tempDirTrapGuard` saw duplicate definitions under `tmp/`).

## Recovery (done before this tip)

- Restored `main` to `origin/main` (`f97dc466f1`).
- Recreated `swarmforge-coder` at `6acae8b9f1` (pre-BL-570 tip).
- Removed nested coder `tmp/` worktrees.
- Relanded the BL-570 tree with
  `SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1` (ticket's designed override for
  recovery commits).

## Verification (without live property lane)

- `bash swarmforge/scripts/test/test_property_suite_drift_guard.sh` — ALL PASS
- `run_acceptance.sh specs/features/BL-570-property-suite-drift-guard.feature`
  — 7/7 pass
