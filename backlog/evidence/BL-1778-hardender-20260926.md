# BL-1778 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Verified: BL-1778 feature 5/5; BL-1715 feature 4/4 (scenario 03 correctly
retired); BL-1697/BL-1698 features unaffected (12/12, 9/9, matching the
coder's own reported counts exactly - no regression on the driver lib's
own shared surface, code this hardener originally reviewed for BL-1715);
`bl1715DriverSeatGiveUp.property.test.js` 8/8 (one run hit the allowlisted
BL-871 `[vitest-worker]: onTaskUpdate` timeout under a load spike to 29 on
this host, confirmed by immediate retry going clean - not a regression).
All 22 whole-tree guard files (211/211, required by touching
`extension/test/` and `specs/pipeline/steps/`).

Independently confirmed non-vacuity beyond the coder's own git-stash check:
removed `--no-merges` from `attempt-commit-shas`'s `git rev-list` call
(kept `--first-parent`). The main property test ("invariants 1 and 2")
caught it - not as a clean assertion failure but as a 20s timeout, itself
a real behavioral difference (the mutated code now includes the
mid-attempt merge commit in the revert list, and `git revert` on a merge
with no `-m` fails per-call; `git!` never checks exit status, so the
`doseq` silently continues past each failed call). Restored, reconfirmed
green.

Observation for the record (not a functional gap, not fixed here): the
DEDICATED unit test "a merge the attempt itself made mid-attempt keeps its
content - excluded from revert by --no-merges" passed UNCHANGED under
the same mutation, despite its own name and comment claiming to verify
the `--no-merges` exclusion specifically. Traced why: because `git!`
never checks exit status, an attempted-but-failed revert of the merge
commit (no `-m`, silently a no-op) produces a BYTE-IDENTICAL final tree
and commit log to a clean exclusion from the list for THIS test's
specific shape (a `--no-ff` merge with no conflicting content) - the test
verifies the correct OUTCOME (which both code paths happen to share here),
not the exclusion MECHANISM its own name claims. The broader property test
still catches this exact mutation (via the timeout), so no coverage gap
exists overall; only this one unit test's docstring overclaims what it
discriminates. Left as-is: correcting the comment is a documenter-scope
nit, not something worth a code or test change at hardener stage, and
BL-654's invariant is still soundly encoded by the property test that
actually catches the regression.
