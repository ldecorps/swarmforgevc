# BL-807 — QA pass, 2026-08-06

Commit verified: ed26be065d (documenter). Confirmed ancestors, each checked
directly against ed26be065d (not merely "ancestor of HEAD" — BL-336 trap):
coder 66267392, cleaner e8bec1e1, architect b96d3942, hardener 42640a46 — all
`git merge-base --is-ancestor <commit> ed26be065d` hold. Merged (fast-forward,
no merge commit) into QA at 97878832 -> ed26be065d.

No prior bounce evidence for BL-807 exists on `main`
(`git log --oneline main -- 'backlog/evidence/BL-807*'` empty) — first QA
pass, no re-bounce check needed.

## In-scope checks (all green, run directly by this role)

1. **Diff scope confirmed** — `git diff --stat 97878832 ed26be065d`: only
   `swarmforge/scripts/babysitter_check.bb`,
   `swarmforge/scripts/babysitterd_sweep_lib.bb`, two `.bb` test/property
   runners, the feature file (+ mutation-manifest stamp), its step handler
   `specs/pipeline/steps/bl807BabysitterStuckInProcessOwnerLivenessSteps.js`
   and its registration in `specs/pipeline/steps/index.js`, two backlog
   evidence files, and the runbook doc. Zero `extension/src`/`extension/media`
   TypeScript touched.
2. **Production `.bb` diff read directly** against R1-R5 and the declared
   invariant. `stuck-parcels` (babysitter_check.bb:259) now takes
   `busy-by-role` and attaches `:owner-busy?` per parcel;
   `check-stuck-in-process` (babysitterd_sweep_lib.bb:95-102) suppresses via
   `:when (not owner-busy?)`, unchanged otherwise (30m threshold literal
   untouched, R2). Confirmed the call site
   (babysitter_check.bb:396,418,431) builds `busy-by-role` ONCE and threads
   the SAME map into both `stuck-parcels` (check 5) and `in-process-claims`
   (check 10) — R1's "consume the existing signal, don't invent one" is real
   at the wiring level, not only in the isolated unit/property tests
   (required_wiring item satisfied).
3. **Glob widening (R4) read directly**:
   `"{,**/}inbox/in_process/*.handoff"` plus the two-pattern
   `owning-role-for-path` (`worktree-owner-pattern` requiring
   `.worktrees/<role>/.swarmforge/handoffs`, `master-owner-pattern` requiring
   `.swarmforge/handoffs/<role>/inbox/`) — structurally disjoint, matching the
   architect's empirical no-double-count verification.
4. **Unit suite, independently re-run** —
   `swarmforge/scripts/test/babysitterd_sweep_lib_test_runner.bb`: exit 0,
   `ok`. Covers the `:owner-busy?` gate directly (busy suppresses, idle still
   warns, mixed busy/idle suppresses only the busy one).
5. **Property suite, independently re-run** —
   `babysitterd_sweep_lib_property_runner.bb`: exit 0, `ok`. P5
   (`stuck-warning-never-fires-while-owner-busy`) encodes the ticket's
   declared invariant over randomized parcel ages/names, both
   owner-busy/owner-idle branches demonstrably reached (asserted in the
   runner itself), matching architect's separate mutation-based
   non-vacuousness check (guard reverted -> property fails; restored -> ok).
6. **Formal acceptance gate, independently re-run** —
   `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-807-babysitter-stuck-in-process-warn-ignores-owner-liveness.feature`
   — 11/11 scenarios pass (TAP: `# pass 11`, `# fail 0`), driving the real CLI
   against real tmux panes: busy-owner-suppresses (01), idle-owner-still-warns
   (02), no-self-contradiction with check 10's motion signal (03),
   below-threshold-never-warns x2 pane states (04), every mailbox shape x5
   (role-nested master idle/busy for specifier+coordinator, flat worktree
   idle/busy for coder) (05), no-double-count (06). No orphaned tmux fixtures
   left behind (`tmux list-sessions` clean after the run; only the two live
   swarm sessions `swarmforge-coder`/`swarmforge-coordinator` present) and no
   orphaned test processes (`pgrep -fl 'node --test|stryker'` clean before and
   after).
7. **e2e_qa_procedure (5 steps), all independently confirmed** via the
   acceptance run above: step 1 (false positive gone) = scenarios 01+03;
   step 2 (true positive survives) = scenario 02; step 3 (master mailbox
   visible, busy suppresses / idle warns) = scenario 05's `role-nested master`
   rows; step 4 (worktree mailbox unregressed) = scenario 05's
   `flat worktree` rows; step 5 (no double-count) = scenario 06.
8. **Wiring/callers** — grepped every call site of `stuck-parcels` and
   `check-stuck-in-process`: single production caller each, both reached from
   the live sweep entry points named in the ticket (R5); no stray unwired
   duplicate.
9. **Documentation** — `docs/how-to/BL-611-babysitterd-runbook.md`'s updated
   check-5 table row and new "Stuck-in-process now gates on owner liveness"
   section accurately describe both defects (false-positive escalation to
   nudge; blind master-mailbox glob) and the fix (shared `busy-by-role`
   consumption, widened glob), name the unchanged 30m threshold and the
   accepted busy-on-something-else residual gap (R3), and cross-reference the
   feature file per the established BL-802/BL-804/BL-809 convention.
10. **Human approval / severity** — `backlog/active/BL-807-...yaml`:
    `human_approval: approved`, `severity: high` — both specifier
    approval_context questions were resolved before promotion; nothing
    outstanding for QA to escalate.
11. **Untracked file surfaced, not swept** — `swarmforge/scripts/operator_path_lib.sh`
    (untracked, pre-existing, unrelated to this ticket) is present in this
    worktree; the hardener's own evidence already names and leaves it
    unstaged (BL-506 discipline). Confirmed again here: not part of this
    parcel's diff, left untouched, not committed.
12. **No orphaned mutation/test processes** — `pgrep -fl 'node --test|stryker'`
    clean before this pass and reconfirmed clean after every check above.

## Out-of-scope: full extension unit suite (`npm test`) — BLOCKED by host load, not by this parcel

Host load was severe and worsening throughout this pass: `uptime` climbed from
~73 to 117 (4-core host, ~29x cores at peak) across the verification window —
squarely inside, and by the end above, the 90-190 range already documented and
accepted the same week in `backlog/evidence/BL-809-qa-pass-20260805.md`,
`backlog/evidence/BL-813-qa-pass-20260805.md`, and
`backlog/evidence/BL-638-qa-pass-20260805.md`. `tsc` compiled cleanly
(confirmed independently, twice). The follow-on `vitest` run under `npm test`
did progress this time and was observed directly: 5 files completed before it
was stopped, one apparent failure per file
(`test/dependencyGateCliReportsAndScope.test.js`: hard 20s test timeout;
`test/readLiveRoleHeldTicketsCli.test.js`: BL-487 empty-map-instead-of-
populated-map shape; `test/startBridgeHeadlessCli.test.js`: a spawned
subprocess never printed its expected startup line in time) — the same
load-starved-subprocess signature already characterized and dismissed in
BL-809's evidence, none in a file this parcel touches or related to the
babysitter subsystem. At that rate, with load still climbing rather than
easing, full completion over 447 files would run well past the hour mark —
stopped rather than let run indefinitely; the `npm test` process group was
killed and reconfirmed clean via `pgrep`/`ps` (no `vitest`/`node --test`
workers remaining).

This ticket's own diff contains zero `extension/src`/`extension/media`
TypeScript (confirmed in check 1) — the one JS file this parcel adds
(`bl807BabysitterStuckInProcessOwnerLivenessSteps.js`) is a
`specs/pipeline/steps/` acceptance step handler, already exercised end-to-end
and green via the formal acceptance gate (check 6), itself a
`node --test`-driven run — a working, unblocked signal on the exact JS this
parcel touches, obtained from a lighter-weight invocation than the full
extension suite, per the same load-bypass reasoning already adopted three
times this same week (BL-809, BL-813, BL-638).

## Verdict: PASS

Every check that can isolate BL-807's own scope from the host-load condition
is green, independently re-run, and matches the ticket's required_wiring, its
declared invariant, and every one of R1-R5's specifier rulings. Approved on
that basis. The continuing severe host-load condition is flagged to the
coordinator alongside this approval as a standing health signal (constitution
Article 3.5) — now observed across four consecutive QA passes this week.

By QA.
