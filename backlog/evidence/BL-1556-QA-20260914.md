# BL-1556 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Merged `Merge documenter a1dac80841 into QA.` (fast-forward, `fda9b7d429`
-> `a1dac80841`).

1. `qa-sibling-check.js status --ticket BL-1556`: `VERIFY BL-1556`, no open
   deferral.
2. Orphaned `node --test|stryker|vitest` process check before/after: two
   loosely-matching `pgrep -f` hits each time were this session's own
   zsh/monitor scaffolding (PIDs did not resolve via `ps -p`/`ps aux`) —
   no real orphans.
3. `pre_qa_gate.sh BL-1556-the-bl1272-property-costs-one-bb-process-per-query a1dac80841`:
   `OK`.
4. Full unit suite (`npm test`, extension/): 616/616 files, 10479/10479
   tests green. `npm test`'s own exit code was 1 from BL-378's per-file
   wall-clock budget gate (`check-suite-file-budget.js`), NOT from any
   failing test — 7-9 files (varying between two consecutive runs, host
   load 1.9-8.5) exceeded the 7s per-file budget. None of BL-1556's own
   changed files are among the offenders. This is a known, already-ticketed
   condition: `backlog/paused/BL-791-epic-unit-suite-speed.yaml` (open
   epic) explicitly documents "BL-378's guard has 19 live offenders and
   therefore fails every `npm test` ... it is already a permanently-red
   gate" (2026-08-22 profile note), with slice C (the gate fix itself)
   deliberately not yet minted pending an operator answer on lane/host
   budget policy. Same disposition already recorded today on the sibling
   ticket's QA pass (`backlog/evidence/BL-1555-QA-20260914.md` item 4:
   "Suite-budget-over-time warnings only (perf, non-blocking)"). Treated as
   non-blocking per that precedent — not this parcel's defect, not newly
   unticketed.
5. `npm run test:properties` (full 404-file lane): 403/404 files green,
   1189/1190 tests. `bl1272LandedSiblingInvariants` (BL-1556's own file) is
   NOT in the failure list — full-lane confirmation it is fixed. The one
   failure, `bl1529ScriptSenderAuditOutcomesInvariant`, timed out at 60s
   under full-lane contention; re-ran alone:
   `npx vitest run --config vitest.properties.config.mjs test/bl1529ScriptSenderAuditOutcomesInvariant.property.test.js`
   -> 1/1 pass in 18.6s. No register row for this file; grepped backlog
   first (BL-1063) — same file was already dispositioned as transient
   host-load noise under full-lane contention in
   `backlog/evidence/BL-1489-QA-20260913.md` (2026-09-13, "no ticket
   minted"). Consistent re-disposition, not new.
6. Acceptance: `run_acceptance.sh specs/features/BL-1556-*.feature` — 4/4
   scenarios pass (TAP): green-on-tree-as-it-stands, bb-process-count
   bound, invariant-1-judges-every-case, fixture-git-step-names-itself.
7. `qa_e2e_procedure` reproduced by hand:
   - Counting `bb` shim, 3 runs: exit 0 each time, shim count exactly 3
     each run (unfixed baseline 62), reach map printed each run with
     `cases:60`, wall time 1.6-1.9s per run (vs 20s budget).
   - `git` shim failing `push`, direct CLI call: exit 1, stderr
     `fixture step failed: git push -q origin main: bl1556 shim: refusing
     push`.
   - `git diff main...HEAD --name-only`: exactly the property test, the
     CLI, the step handler, and this ticket's evidence files;
     `extension/src/`, `land_step_lib.bb`, `vitest.properties.config.mjs`,
     `property_suite_standing_allowlist.tsv` untouched; `numRuns: 60`
     still present (grep confirmed).
   - Break-then-restore not independently re-run here (hardener's own
     evidence, `BL-1556-hardender-20260914.md`, already performed and
     recorded it: reverting the batch call into the per-case loop
     reproduces the 62-process baseline and fails acceptance scenario 02;
     restored and diffed byte-identical). Code read directly: the single
     `batch('landed-batch', drawn.map(...))` call at line 55 sits after
     `fc.sample` draws all 60 cases up front (line 52) and outside any
     per-case loop — structurally matches the fix as described.
8. Ancestry: this ticket's own coder (`015500fd02`), cleaner
   (`d5329e7ebb`), architect (`d88e628ff2`), and hardener (`011d187e7a`)
   merges all confirmed ancestors of `a1dac80841` via
   `git merge-base --is-ancestor`.
9. Bounce history: `main`==`origin/main` (0/0 rev-list), no
   `bounce_history` on the ticket YAML — clean prior record (BL-340/891).
10. Own evidence-path mistake caught and corrected in this same pass: first
    ran `record-review-evidence.js` from `extension/`, which wrote
    `extension/backlog/evidence/...` instead of the repo-root path (the
    exact footgun the coder's own commit `c7096d8985` fixed for its own
    evidence). Reverted (`785daafe2e`) and re-ran from the repo root
    (`f620349fd6`).

Forwarding THIS pass's own commit (the detail-section commit that follows
this file) as the approved commit, per BL-536 — not the received
`a1dac80841` hash.

By QA.
