# BL-1704 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: the one gap found (below) was in this stage's own domain
(mutation hardening / regression-suite integrity) and was fixed in
this pass, not bounced.

BL-113 Gherkin acceptance mutation (soft, first run on this feature —
ran to completion, took ~150s under load 13-19/20 cores, no fallback
needed): 4/4 mutants killed across the two Scenario Outlines (01's
stop-path column, 03's pid-state column), 0 survivors. Manifest
embedded in the feature file.

The architect's own bounce (D1, `BL-1704-architect-20260925.md`) found
`stop_ancillary_services.sh`'s new ollama-stop call had no `|| true`,
so a real documented failure return (a server/runner surviving
TERM+KILL) would abort the whole full-stack stop under its own `set
-euo pipefail`. Coder fixed it and added
`test_stop_ancillary_services_ollama_failure_not_fatal.sh`, which
stubs the failure and proves the "done" log line is still reached.
The architect's own evidence separately noted the SIBLING call site in
`kill_all_swarm.sh` already had `|| true` from the start — established
by READING the file, never by a test. Grepped for any test exercising
that call site's failure resilience: none existed anywhere (the
acceptance feature's kill_all_swarm Example only covers the SUCCESS
path). Closed the gap: added
`test_kill_all_swarm_ollama_failure_not_fatal.sh`, running the real
`kill_all_swarm.sh` from a throwaway directory carrying stub siblings
at its fixed `$SCRIPT_DIR`-relative paths (stub
`ollama_ancillary_lib.sh` forcing the failure, no-op
`babysitterd_census_lib.sh`, and a stub `kill_pipeline_swarm.sh` that
only marks itself reached — the real `kill_pipeline_swarm.sh` never
runs, so nothing destructive happens, unlike the excluded live-only
`test_kill_all_swarm_vitest_reap.sh` beside it in the manifest).
Verified non-vacuity by hand: removed the real file's `|| true`,
confirmed the new test fails (exit 1), restored the file (`git status`
clean), reran green.

Independently re-verified (not merely trusted) the property test's own
non-vacuity claim for `ollama_ancillary_pid_is_ollama_serve`'s guard:
replaced the `if` condition in `ollama_ancillary_stop_swarm_owned`
with `if false`, ran the property test, confirmed the "reused-pid"
category fails exactly as the header claims (the unrelated process
gets killed instead of left untouched), restored the file (`git
status` clean), reran green.

No TS/JS production file is in this ticket's scope (`git diff
main...HEAD --name-only` scoped to BL-1704's own files: two `.sh`
files, one step handler, one fixture, one property test, two `.sh`
test files) — CRAP/DRY are not wired for shell (engineering.prompt)
and were not run.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1704-….feature`: 5/5.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=soft (first
  run, no stale manifest possible): 4/4 killed, 0 survivors.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1704OllamaStopSwarmOwnedInvariant.property.test.js`: 1/1.
- `bash swarmforge/scripts/test/test_ollama_ancillary_stop_pid.sh`:
  ALL PASS (pre-existing, unaffected sibling — confirmed still green).
- `bash swarmforge/scripts/test/test_stop_ancillary_services_ollama_failure_not_fatal.sh`:
  ALL PASS.
- `bash swarmforge/scripts/test/test_kill_all_swarm_ollama_failure_not_fatal.sh`
  (new this pass): ALL PASS.
- `npm test` (extension/): 688 suites / 10874 tests, all green. Same
  pre-existing per-file budget warnings on a handful of unrelated
  files (fluctuates by host contention, not this ticket's diff).
- `pgrep -f '[t]mux -S' | wc -l`: no real tmux server spawned by this
  ticket's own tests (the fixture uses real Node stand-in processes
  for the ollama server/runner, never tmux). `git status --short`
  clean before each commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load 3-19/20 cores throughout, no fallback needed.

By hardender.
