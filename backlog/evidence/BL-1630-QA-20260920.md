# BL-1630 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect. This is the fix for the property-lane flake
this session's other QA passes repeatedly documented as BL-1630's own pre-existing, unrelated red
(`bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`, "non-vacuity" test's `listsDir`
listing the real OS temp root instead of its own fixture directory) — confirmed fixed below.

By QA.

## Detail

- `npm run compile` (extension/out was stale after the merge).
- `npx vitest run test/stepHandlerModuleLoadBudget.test.js`: 8/8 green (matches qa_e2e_procedure step 1).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1630-no-step-handler-does-work-at-module-load.feature`:
  14/14 ok (step 2), including scenario 01 pinning all twelve moved handlers by name under budget.
- `npx vitest run test/bl968StepRegistryMaterializedTreeGuard.test.js`: 3/3 green, both spawn
  durations well under the 15 s / 24 s ceilings (step 3).
- Scope check (step 4): inspected the parcel's own commits directly (`212ed26e4e` rework,
  `a3841f4aab` bounce fix, `b74da1152` hardening, `fbaa2c7c3e` docs) — touches only
  `extension/test/helpers/stepHandlerRequireCensus.js`, `extension/test/stepHandlerModuleLoadBudget.test.js`,
  `extension/test/bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`,
  `specs/pipeline/steps/bl1630NoStepHandlerWorksAtModuleLoadSteps.js`,
  `docs/how-to/BL-1371-step-handlers-register-by-discovery.md`,
  `swarmforge/constitution/articles/reference/engineering-detailed.prompt`, and evidence files.
  No production step-handler file touched outside what the ticket names.
- `npm run test:properties` (extension/): **432/432 files, 1264/1264 tests passed** — the
  `bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js` flake this session's other QA passes
  (BL-1661, BL-1664, BL-1667) documented as pre-existing and BL-1630-owned is now green, confirming
  the fix (`listsDir` now reads `readdirSync(__dirname)`, the fixture's own small directory, instead
  of `os.tmpdir()`; the non-vacuity assertion checks for the specific `listedDir` reason, not an empty
  violations array).
- `npm test` (extension/): 635/635 files, 10821/10821 tests passed.
- `pre_qa_gate.sh BL-1630 499fa1b430`: OK.
- No straggler test/mutation processes before or after.
- `qa-sibling-check.js status --ticket BL-1630`: VERIFY (no open deferral).
- All three prior bounces (two cleaner, one QA) are addressed: the acceptance-feature/allowlist rework
  (cleaner bounces) and the `os.tmpdir()` fixture defect (QA bounce) are all present in this commit.
