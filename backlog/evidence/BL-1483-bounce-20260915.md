# BL-1483 QA bounce — 2026-09-15

## D1 — standing-red register row not removed (documenter)

1. **Failing command**: `grep -n 'BL-1483' backlog/standing-reds.tsv`
2. **Commit hash**: 660b915946 (documenter's forwarded commit, merged into QA at this parcel)
3. **First error excerpt**:
   ```
   acceptance	specs/features/BL-418-standing-topic-icons.feature	BL-1483	2026-09-08	scenario 01 Examples row 2 asserts the operator standing topic icon is the opera house; BL-453 (aa6785d847, 2026-07-16) superseded it with the human-chosen bell (red since, hidden by the per-feature runner; found by the coder batch-verifying BL-1475 09-08); owner BL-1483 retires the superseded row, never rewords it
   ```
4. **Failure class**: `behavior` (a missing deliverable — the ticket's own description names this the documenter's pass: "Remove the register row in the same land", and `qa_e2e_procedure` step 7 explicitly requires `backlog/standing-reds.tsv` to carry no row for BL-418's feature — this is not a code/compile/unit/acceptance-suite failure, the feature itself is green)
5. **Expected vs observed**: Expected `backlog/standing-reds.tsv` to carry no row naming BL-418's feature / BL-1483 once the feature is fixed and green; observed the row is still present, unremoved, in the documenter's forwarded commit.

**Blamed role**: documenter (ticket description, "How" section: "Documenter: ... Remove the register row in the same land."). The documenter's own evidence file (`backlog/evidence/BL-1483-documenter-20260915.md`) recorded NONE and forwarded with no changes to `backlog/standing-reds.tsv`.

**Remediation pointer**: `backlog/standing-reds.tsv` — remove the row whose fourth column names `BL-1483` (the BL-418 acceptance-lane row), following the pattern of prior removals (e.g. `6f8c0efa4f`, `19e744c142`, `1ca811ac47`), then re-verify with `standing_red_register_cli.bb` that `unowned: []` and the count is down by one.

## Everything else checked this pass — no other defects found

- `run_acceptance.sh specs/features/BL-418-standing-topic-icons.feature`: `ok 1..3`, `# fail 0` (run twice, matches qa_e2e_procedure steps 1-2).
- `grep -n '🏛' specs/features/BL-418-standing-topic-icons.feature`: only the `RETIRE-WITH: BL-1483` narrative comment, no Examples row, no step line — matches step 3.
- `run_acceptance.sh specs/features/BL-453-concierge-front-desk-icon-label.feature`: `ok 1..3`, `# fail 0` — matches step 4.
- `grep -n '🏛' docs/branding/icon-system.md`: line 312 is an unrelated sticker-set-availability list (not a claim about the operator/concierge topic's current icon); the standing front-desk topic line (317-320) names the bell and cites BL-453 — matches step 5.
- Mutation-stamp header `tested_at: 2026-09-15T10:40:24...` is later than 2026-09-08 (hardener re-stamped in this parcel) — matches step 6.
- Full unit suite (`npm test` from `extension/`): 619 files / 10495 tests, all passed.
- Property suite (`npm run test:properties`, run solo/sequentially): 406 files / 1195 tests, all passed (2 benign allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors, BL-871).
- No `required_wiring` declared on the ticket (explicitly noted: "No mechanism is built and no call site is needed").
- No orphaned test/mutation processes before or after this pass.
