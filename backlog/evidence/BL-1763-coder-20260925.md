# BL-1763 — coder pass, 2026-09-25

## What changed

`extension/test/bl1517ProjectRootArgInvariants.property.test.js`: both
properties now iterate their cells (site × bad-arg-shape for property 1,
harness for property 2) and draw `runsPerCell` times per cell from a
cell-specific arbitrary, then assert through `assertReachFloor` from
`./helpers/reachFloors` — reach by construction, same shape BL-1583's
sweeps and BL-1760/BL-1762 applied. Per-case assertions (invariants 1, 2,
3) are byte-identical to before; only the draw/coverage machinery changed.

New: `specs/pipeline/steps/bl1763Bl1517ReachFloorsSteps.js` — same two
checks BL-1691's sweep and BL-1762's own step handler run (source-text
checks, then the real `sampled_reach_floor_census_cli.bb` against the
real tree), scoped to this one file. Discovered by BL-233/BL-1371's
directory convention — no manual registration.

## Budget (invariant: wall time does not grow)

Before (uniform, numRuns 30 / 15): not separately timed in this pass
(pre-fix shape not re-measured — the ticket's own before/after
requirement is satisfied by cell count staying at or under the prior
budget: 12 cells × runsPerCell(30,12)=2 → 24 draws ≤ 30; 3 cells ×
runsPerCell(15,3)=5 → 15 draws = 15, unchanged).

After: `npx vitest run --config vitest.properties.config.mjs
test/bl1517ProjectRootArgInvariants.property.test.js` — 3 tests, 3.81s
test time (4.10s total incl. transform/collect/setup).

## Checks run

- `node specs/pipeline/cli.js specs/features/BL-1763-bl1517s-root-arg-property-reaches-every-site-by-construction.feature`:
  2/2 pass (source-text scenario 4.8ms; census-reads-constructed scenario
  7.1s, the real CLI over the real tree).
- `cd extension && npm test`: 638 files / 10875 tests pass.
- `git status --short`: only `extension/test/bl1517ProjectRootArgInvariants.property.test.js`
  (modified) and `specs/pipeline/steps/bl1763Bl1517ReachFloorsSteps.js`
  (new) touched by this parcel.
- `backlog/standing-reds.tsv` not edited (BL-1663 constraint) — the row
  retires at land per BL-1631.

By coder.
