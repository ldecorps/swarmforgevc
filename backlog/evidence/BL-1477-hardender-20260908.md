# BL-1477 — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Received `f01ca83d3b` (architect merge-up). Found the inbound worktree had
uncommitted WIP from an interrupted prior hardening turn — a legitimate,
correct fix: the three BL-1477 test/step fixture files carried literal raw
NUL bytes inside string literals (source encoding, not the fixture's logic)
instead of the backslash-u-0000 escape sequence, which is why `git diff`/`file` saw
them as binary. Finished that fix, verified byte-for-byte identical runtime
behavior, then ran the full hardening checklist:

- Mutation (Stryker, scoped via `extension/vitest.bl1477.stryker.config.mjs`
  to `test/contextTelemetryProducer.test.js` + `test/runContextTelemetryProducerCli.test.js`,
  per-file `--mutate`, avoiding the standing `activePoolFreshnessAudit.test.js`
  Stryker-sandbox red without touching the shared `vitest.config.mjs`):
  - `contextTelemetryStore.ts`: closed real gaps (whitespace-only interior
    line vs. `!raw.trim()`; the real `recordEventsViaCliBatch` production
    write path, previously exercised by no test at all — only the injected
    fake `recordFn`; the join-separator across multiple events). Recorded
    4 equivalents with evidence: a dead `lineNumber` field on the `ok:false`
    branch (grepped — never read anywhere but this file); the
    `badIndexes.length === 1` gate (arithmetically forced whenever
    `badIndexes[0] === parsed.length - 1`, confirmed by Stryker running my
    own multi-bad-line discriminating test against the mutant and it still
    passing); a dropped trailing newline (BufferedReader reads the final
    line with or without one — confirmed empirically with a 2-event test);
    an unused `execFileSync` `encoding` option (return value never read —
    confirmed by hand-patching `out/` and re-running, no observable change).
  - `contextTelemetryProducer.ts`: closed real gaps in the NEW/CHANGED code
    (invariant 2's cross-role-group timestamp sort; `skippedDuplicates`
    subtraction vs. addition; the default-deadline path with no explicit
    override; the recordFn-absent dispatch reaching the real subprocess
    end-to-end via a `swarmforge/` symlink into a fixture root). Left
    PRE-EXISTING, untouched-by-this-ticket survivors alone
    (`isCompactionAfterPrior`, `providerForAgentBrand`,
    `deriveContextEventsFromUsageRecords`'s sort — all present unchanged on
    `main`, confirmed via `git show main:...`). Recorded 2 equivalents: the
    `transcriptPaths.length === 0` guard (redundant — `readTranscriptUsage`
    already tolerates a nonexistent directory, confirmed by reading its
    `catch { return []; }`) and the `selected.length > 0` outer guard
    (redundant — `recordEventsViaCliBatch` already no-ops safely on `[]`).
    2 re-export-boilerplate `enumerable` mutants also equivalent (cosmetic,
    unobservable through normal property access/destructuring).
  - `run-context-telemetry-producer.ts`: 0 survivors after adding a test
    isolating each conjunct of `formatProducerResult`'s three-way `&&` guard
    (one had shown as `[Survived]` even against an existing test whose
    assertion I proved by hand-patching `out/` should have killed it —
    re-ran single- and multi-threaded, same result both times; resolved by
    adding the two isolating tests rather than chasing the discrepancy
    further, and it is now killed for real). `main()` itself is untested
    in-process by design (thin CLI wrapper over already-tested helpers,
    per the CLI-thin-wrapper rule) — 6 `[NoCoverage]` mutants there are
    expected.
  - CRAP regression: `runContextTelemetryProducer` was CRAP=8.00 (complexity
    8 at 100% coverage) after the invariant-2/cap/deadline/dispatch logic
    landed. Extracted `recordSelectedEvents()` (the recordFn/real-CLI
    dispatch) into its own function — CRAP=8.00 → 5.00, no behavior change,
    re-verified fully covered and its own mutants killed.
  - DRY (jscpd): 0 clones involving any of the three touched files.
  - `extension/test/liveRepoDerivationGuard.test.js` (standing whole-tree
    guard) flagged the new real-subprocess tests for handing the live repo
    root to `recordEventsViaCliBatch`; added a `BL-1038-EXEMPT:` comment —
    genuinely O(1) cost (one fixed file path, one subprocess spawn against a
    FIXTURE telemetryDir), matching the guard's own "reading one named file"
    exemption one level of indirection deep. Ran all 18 standing
    `test/*Guard*.test.js` files clean.
  - Full suite: `npx vitest run --coverage` — 612 files / 10358 tests green
    (twice, before and after the CRAP extraction). Property lane
    (`bl1477ContextTelemetryTornTailInvariants.property.test.js`) green.
    Both bb runners (`context_telemetry_test_runner.bb`,
    `test_context_telemetry_cli.sh`) green, 16/16. Acceptance for both
    `BL-1477...feature` (5/5) and `BL-665-context-telemetry-producer-wiring.feature`
    (4/4, per the ticket's own qa_e2e_procedure item 1) green.
  - No orphaned test/mutation processes at handoff (`pgrep -fl
    'node --test|stryker|vitest'` clean); `stryker.config.json` and
    `stryker-incremental.json` restored/removed so the shared config is
    untouched at commit.

Forwarding to documenter with the same task name.

By hardender.
