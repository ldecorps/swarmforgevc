# BL-1568 — QA bounce evidence, 2026-09-15

## D1

1. **Failing command**: `bash swarmforge/scripts/test/test_dispatch_gap_autoroute.sh`
2. **Commit hash**: 1403b913b6 (QA worktree tip, merge of documenter cf98429535)
3. **First error excerpt**:
   ```
   PASS: 01: dispatch-gap-items detects the undispatched active item
   PASS: 02: auto-route!'s exact mechanism (dispatch-gap-draft-lines + vector-form process/sh) sends successfully
   PASS: 03: the queued note is correctly attributed to the coordinator and addressed to the assignee
   ----- Error --------------------------------------------------------------------
   Type:     java.lang.AssertionError
   Message:  Assert failed: expected no gaps once the auto-route note is queued, got: [{:id "BL-217", :assigned-to "coder", :status "todo"}]
   (empty? gaps)
   FAIL: 04: expected the second sweep not to re-detect a gap while the route is in flight
   ```
   Reproduced 3/3 runs, deterministic.
4. **Failure class**: `acceptance` — the ticket's own acceptance criterion
   ("the file prints `ALL PASS` on `main`") and `qa_e2e_procedure` item 1
   are both unmet; scenario 01 and scenario 05 of the ticket's feature file
   fail for the same reason.
5. **Expected vs observed**: expected `bash
   swarmforge/scripts/test/test_dispatch_gap_autoroute.sh` to print
   `ALL PASS` and exit 0 (ticket's "What is wanted" + qa_e2e_procedure #1);
   observed it stops at case 04 with the pre-existing idempotency-guard
   failure the coder itself documented (case 02 still constructs its draft
   via the 1-arg/legacy-soft-note form instead of the amended form —
   `dispatch-gap-draft-lines` called WITH the fixture's 10-hex HEAD, queued
   through `handoff-lib/queue-git-handoff!`'s two-call audit — that the
   specifier's 2026-09-15 amendment (commit c1c675af80) specifies for case
   02, with case 03 updated to grep `type:`/`task:`/`commit:` and a new
   scenario 05).

**Blamed role**: coder. The coder's own evidence
(`backlog/evidence/BL-1568-coder-20260915.md`) documents this exact gap,
correctly root-causes it, and sent a priority-00 note to the specifier
flagging it — but the specifier's amendment (c1c675af80, same day) already
answers that note: it revises the ticket's own "How" section to give the
exact case 02/03 rewrite and adds scenario 05, and says explicitly "Both
FIRM points ... hold and no choice is posed, so the ticket is not
re-pended." The amendment landed on `main` before this parcel reached
cleaner/architect/hardener/documenter, all of whom recorded NONE without
re-checking the file against the amended spec or re-running the shell
test to `ALL PASS`. There is no longer a spec ambiguity to route to the
specifier: the amended ticket gives the exact fix. The gap is that the
coder's case 02/03/05 rewrite was never done.

**Remediation pointer**: `swarmforge/scripts/test/test_dispatch_gap_autoroute.sh`,
case 02 (construct `dispatch-gap-draft-lines {:id "BL-217" :assigned-to
"coder"} head-commit-10` where `head-commit-10` is
`git -C "$ROOT" rev-parse --short=10 HEAD`, then queue via
`handoff-lib/queue-git-handoff!`'s two-call protocol — see ticket's amended
"How" section for full recipe) and case 03 (grep `type:`/`task:`/`commit:`
instead of `message:`); add scenario 05's step handler in
`specs/pipeline/steps/bl1568DispatchGapAssertsByIdSteps.js` per the
feature file's scenario 05 (already present, unimplemented against).

Sweep otherwise complete: no other defects found. Article 4.4 full
inventory — this is the only item.

By QA.
