# BL-1487 — coder pass (2026-09-13)

## Ruling applied

`human_ruling`: restore, not retire. Restored the eight files
`swarmforge/profiles/{stabilize-two-pack,cheap-copilot-seven-pack,mono-router-gpt}.*`
via `git checkout 4695d31402^ -- swarmforge/profiles/` (the state
immediately before the QA-lane chore deletion) — byte-identical to that
commit, no hand edits.

## What changed

- `swarmforge/profiles/` (8 files) restored from `4695d31402^`.
- `swarmforge/scripts/test/test_smoke_check_stabilize_two_pack.sh`:
  `make_good_fixture()`'s profile fixture dropped its
  `window coordinator ...` line. That line was BL-203-era wording; BL-243
  made the coordinator reserved infrastructure and BL-939 corrected the
  real shipped `stabilize-two-pack.conf` (and the smoke check's own
  `expected=(coder cleaner)` assertion) to match, but this test's fixture
  was never updated to follow, so case 01 failed against a fixture the
  real profile no longer resembles — independent of, and predating, the
  2026-08-23 deletion.
- `backlog/standing-reds.tsv`: removed the three BL-1487 rows (acceptance
  BL-939 feature, both shell tests) now that all three reds are green.

## Why no other file needed a change (restore branch)

Every other reference the ticket names
(`swarmforge/scripts/swarmforge.sh`'s `-d "$SWARM_FORGE_DIR/profiles"`
sync guard, `reset_worktrees.sh`'s `rm -rf swarmforge/profiles`,
`sync_worktree_scripts_lib.bb`, `.vscode/launch.json`'s two configs,
`docs/how-to/BL-203-stabilize-two-pack-smoke-check.md`, the BL-373
feature/step file) already worked correctly once the directory exists
again — none needed edits under restore; the retire-branch actions
(deprecating the how-to, repointing launch.json, retiring BL-939
scenarios 01/03) do not apply.

## Verification

- `bash swarmforge/scripts/test/test_smoke_check_stabilize_two_pack.sh` —
  `ALL PASS` (all 9 cases; was failing on case 01 before the fixture fix).
- `bash swarmforge/scripts/test/test_shipped_confs_no_coordinator_window.sh`
  — `ALL PASS` (12 shipped confs, including the three restored profiles).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-939-two-pack-smoke-check-stops-demanding-a-coordinator-window.feature`
  — `# pass 4`, `# fail 0` (all four rows: scenario 01, both scenario 02
  examples, scenario 03).
- `bash swarmforge/scripts/test/test_sync_worktree_scripts_never_clobbers.sh`
  — `ALL PASS` (unaffected; profiles sync path exercised, no regression).
- Grep from the ticket's own "How" —
  `grep -rln 'swarmforge/profiles' . --include='*.sh' --include='*.bb' --include='*.ts' --include='*.js' --include='*.json' --include='*.md' --include='*.feature' | grep -v 'backlog/\|docs/deprecated/\|node_modules\|.worktrees'`
  — lists 14 files; every one names a path that now exists (restore's
  passing condition, per `qa_e2e_procedure` step 3).
- `bb swarmforge/scripts/standing_red_register_cli.bb .` — no row names
  BL-1487.

## Invariants (BL-654) — stated reason, no property test

The one declared invariant — *"no tracked file outside backlog/ history
and docs/deprecated/ names swarmforge/profiles/ unless that directory
exists on main with the file named"* — quantifies over the repo's own
tracked-file tree and grep matches against it, not over a pure module's
input domain. This repo's property-test harness
(`extension/test/*.property.test.js`, `npm run test:properties`) drives
TypeScript modules under `extension/` via `fast-check` generators; there
is no testable module here whose behavior this invariant is a property
of — it is a structural fact about which files exist and what they name,
already the exact check the ticket's own "How" grep and
`qa_e2e_procedure` step 3 perform at verification time (reproduced above).
A generator over "the set of all repo files" has no meaningful arbitrary
to shrink toward and would only reimplement the same grep with extra
machinery.

By coder.
