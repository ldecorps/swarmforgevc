# BL-1228 — QA bounce, 20260828

## D1 — entangled tip: documenter's branch merged main before the BL-1227 entangled-commit revert, so forwarding it now resurrects reverted BL-1211 content and conflicts on shared files

1. **Failing command, exactly as run:**
   ```
   env -u GIT_DIR -u GIT_WORK_TREE git merge 9dcf1c3beb --no-ff
   ```
   run from the QA worktree with HEAD at a fresh merge of current `main`
   (`114ea5c869`, which already contains the QA-approved, tip-pure BL-1227
   landing and its predecessor revert).

2. **Commit hash checked out and tested:** `9dcf1c3beb` (documenter's
   `docs(BL-1228): document the active-pool freshness/hold divergence
   audit`), received via `git_handoff` payload `merge_and_process documenter
   9dcf1c3beb`.

3. **First error excerpt:**
   ```
   CONFLICT (modify/delete): extension/src/tools/recoveryFilterCliArgs.ts deleted in HEAD and modified in 9dcf1c3beb.  Version 9dcf1c3beb of extension/src/tools/recoveryFilterCliArgs.ts left in tree.
   Auto-merging specs/pipeline/steps/index.js
   CONFLICT (content): Merge conflict in specs/pipeline/steps/index.js
   Auto-merging swarmforge/scripts/test/backlog_hygiene_lib_test_runner.bb
   CONFLICT (content): Merge conflict in swarmforge/scripts/test/backlog_hygiene_lib_test_runner.bb
   Automatic merge failed; fix conflicts and then commit the result.
   ```
   Root cause, confirmed by direct ancestry check (not merely "reachable
   from HEAD"): `9dcf1c3beb`'s history has two merge-bases with `main`
   (`5b2db1c7b` and `b283239757`) because the **hardener** stage merged
   main at commit `4924e4077` ("Merge branch 'main' into
   swarmforge-hardender") while `b283239757` — the entangled, since-bounced
   BL-1227 attempt (see `backlog/evidence/BL-1227-qa-bounce-20260828.md`)
   — was still on `main`, unreverted:
   ```
   git merge-base --is-ancestor b283239757 4924e4077   # → true
   git merge-base --is-ancestor 778991d21 114ea5c869   # revert IS on current main → true
   git merge-base --is-ancestor 778991d21 9dcf1c3beb   # revert is NOT in documenter's line → false
   ```
   No stage between hardener and documenter re-synced with main after my
   revert landed, so documenter forwarded a tip whose merge-base predates
   the cleanup. Concretely, a straight two-tree diff
   (`git diff --name-only 114ea5c869 9dcf1c3beb`) shows 61 changed paths,
   of which BL-1228's own scope is only 6 (`extension/src/tools/
   active-pool-freshness-audit.ts`, `extension/test/
   activePoolFreshnessAudit.test.js`, `swarmforge/scripts/
   active_pool_freshness_audit.sh`, `specs/pipeline/steps/
   bl1228ActivePoolFreshnessHoldAuditSteps.js`, `docs/how-to/
   BL-1228-active-pool-freshness-hold-divergence-audit.md`, plus a one-line
   addition to `specs/pipeline/steps/index.js` and `docs/index.md`). The
   rest belongs to BL-1192, BL-1201, BL-1207, BL-1211, and BL-1216 — most of
   it (`bl1192TaskScopeGateSteps.js`, `bl1207AbandonedLockLivenessSteps.js`,
   `bl1216DuplicateIdLiveCopyContentVerdictSteps.js`) is harmless because its
   introducing commit is already an ancestor of current `main` (so a real
   merge would no-op there), but two files are **not** already on `main`
   and would be freshly reintroduced by this merge:
   - `extension/src/tools/recoveryFilterCliArgs.ts` — introduced by
     `1535f0b0a` (hardener's own BL-1211 CLI-wrapper commit), **not** an
     ancestor of current `main`; deleted from `main`'s line by my BL-1227
     revert precisely because it was BL-1211's own unlanded work swept in
     by the entangled commit. A plain merge reports this as a
     modify/delete CONFLICT (caught above).
   - `extension/src/tools/recovery-filter-check.ts` — introduced by a
     commit that **is** an ancestor of `main`, but the file is currently
     **missing** from `main` (deleted by the same revert). This pairing
     (introducing commit present, file absent) means a merge would
     **silently resurrect a deleted file with no conflict marker** — the
     exact BL-954 hazard, here hitting a sibling ticket's reverted content
     rather than a re-fix of the same ticket.
   `extension/src/tools/quarantineLiftCliArgs.ts` is a third risk: its
   documenter-line version comes from the same stale `1535f0b0a`, but
   `main` already carries an independent, newer version of the same path —
   a content clash requiring hand resolution either way, not a clean
   fast-forward of anyone's work.

4. **Failure class:** `behavior` — same "entangled tip" class as
   `BL-1227-qa-bounce-20260828.md`'s D1, and a live instance of the
   resurrection hazard BL-1211's own ticket
   (`BL-1211-recovery-resurrects-reverted-bounce-content-and-the-lift-check-is-blind.yaml`)
   is about, arriving here as a cross-ticket side effect rather than
   BL-1211's own lift path.

5. **Expected vs observed:** Expected — per "Forwarded Commits Carry Their
   Lineage" (workflow.prompt), the sender re-runs the ancestry/freshness
   check against current `main` before forwarding, so the forwarded commit
   contains only its own ticket's reviewed substance plus main's current
   state. Observed — documenter forwarded a tip whose merge-base predates a
   revert that landed on `main` hours earlier (2026-08-28, before
   10:54Z), so accepting it as BL-1228's approval would resurrect BL-1211's
   already-reverted, still-`active/`-and-unreviewed CLI-wrapper content
   onto `main`.

## Other checks this pass (run-or-blocked)

- BL-1228's own substance, checked in isolation against the received
  commit (`git worktree add … 9dcf1c3beb --detach`, `npm install && npm run
  compile`): **PASS** — `tsc` compiles clean.
- `npx vitest run test/activePoolFreshnessAudit.test.js` on the isolated
  commit — **PASS**, 25/25.
- `specs/pipeline/scripts/run_acceptance.sh specs/features/
  BL-1228-active-pool-freshness-hold-divergence-audit.feature` on the
  isolated commit — **PASS**, 9/9.
- `required_wiring` (`promote_and_route_next.sh::
  active_pool_freshness_audit.sh`) — **PASS**, confirmed by grep: the
  script calls `"$SCRIPT_DIR/active_pool_freshness_audit.sh" "$ROOT" ||
  true` at its promotion-success call site.
- Full unit/property suite and the standing `run_bb_suite.sh` entry point
  against the merged tree — **BLOCKED BY D1**: cannot produce a clean
  merged tree to run them against without pre-judging which side of each
  conflict is correct, which is exactly the fix being bounced.

## Remediation

Documenter: merge current `main` (post BL-1227 landing, tip `df9899feb` or
later) into your branch — a plain `git merge`, not a rebuild — and resolve
the three conflicts by keeping `main`'s side everywhere except BL-1228's own
one-line addition to `specs/pipeline/steps/index.js` (add back
`require('./bl1228ActivePoolFreshnessHoldAuditSteps')`). Do not reintroduce
`extension/src/tools/recoveryFilterCliArgs.ts` or `recovery-filter-check.ts`
— both are BL-1211's own unlanded work and were deliberately removed from
`main` by BL-1227's QA bounce-revert; BL-1211 will bring them back itself
through its own pipeline pass if and when it lands. The substance of
BL-1228 does not need rework — cleaner/architect/hardener's reviews of the
actual audit logic stand; only the merge base needs to advance. Re-send
directly to QA (documenter is the last stage before QA; no need to cycle
back through cleaner/architect/hardener for a lineage-only fix).
