# BL-851-swarm-stamp-bridge-serves-sideload-apks-pre-auth — QA final pass (2026-08-09)

## Commit checked

`728c0b0cd3` (documenter, "fix stale acceptance pointer"), merged into QA's
branch as this pass's merge commit. Ancestry confirmed: coder `492a920166`,
cleaner `1a011a2f3c`, architect `1ad7a4657a`, hardener `c0f41e0619` are all
ancestors of `728c0b0cd3` (`git merge-base --is-ancestor` holds for each).
Single-ticket parcel — no sibling ticket bundled in this diff.

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`, extension/): clean.
- Full unit suite (`npx vitest run`, two independent full runs): `79/79`
  `test/bridgeServer.test.js` tests pass in both runs. Run 1: 417/418 files,
  7366/7368 tests green (2 failed). Run 2 (re-run to check reproducibility):
  417/418 files, 7367/7368 tests green (1 failed). The only reproducible
  failure across both runs is `test/renderBriefingDiagramsCli.test.js` >
  "the compiled CLI runs standalone as a subprocess and produces the same
  result" — a `child_process.execFileSync` subprocess-spawn test that hit
  its 20s timeout. This file/test has zero overlap with this ticket's diff
  (`git diff bb9f04e6^..728c0b0cd3 --stat` touches no `render`/`diagram`
  file) and the host was under severe, worsening load throughout this pass
  (`uptime` load averages observed: 27-52 on a 4-core host, i.e. 7-13x
  oversubscribed — consistent with the project's own documented lesson that
  subprocess-heavy tests and mutation dry-runs time out under severe host
  load, unrelated to code correctness). Also observed 123-131 benign
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors both
  runs — worker-IPC timeouts from the same CPU contention, not test
  assertion failures.
- Property tests, ticket-scoped (`npx vitest run --config
  vitest.properties.config.mjs test/sideloadApk.property.test.js`): 3/3
  pass (both declared invariants + the concrete non-vacuousness lock-down).
- Property tests, full suite (`npm run test:properties`, 59 files): 56/59
  files green, 182/188 tests green.
  `test/sideloadApk.property.test.js` (BL-851's own) is green within this
  run too, both invariants. The 3 failing files —
  `bl760DuplicateChainGuard.property.test.js` (3 tests, one ran 315s before
  failing), `bl787NamedTunnelInvariants.property.test.js` (2 tests),
  `bl797MutationGateProbeCrashFallback.property.test.js` (1 test, hit its
  120s timeout) — touch handoff-mailbox duplicate-chain guarding, named-
  tunnel liveness detection, and mutation-gate probe fallback respectively;
  none reference `bridgeServer.ts`, `sideloadApk.*`, or any file in this
  ticket's diff. This reproduces the cleaner's own independently-recorded
  finding almost exactly (`backlog/evidence/BL-851-cleaner-pass-20260809.md`
  names `bl787NamedTunnelInvariants` and `bl797MutationGateProbeCrashFallback`
  plus "one more not captured in the tail-truncated log" — that third file
  is `bl760DuplicateChainGuard`, confirmed here). Two independent
  observations (cleaner's and this one) under two different load conditions
  agreeing on the same unrelated-file set is strong evidence this is a
  pre-existing, host-load-sensitive flake set, not a regression from this
  parcel.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-851-swarm-stamp-bridge-serves-sideload-apks-pre-auth.feature`):
  8/8 scenarios PASS — served-with-no-credentials, 404-when-absent, the
  4-example outline (plain/doubly-encoded traversal, backslash climb,
  escape symlink), directory-is-not-a-download, and every-other-route-still-
  401s. Covers all four steps of the specifier's own E2E QA procedure
  (works at all / encoded-traversal fails for the right reason / symlink
  containment / everything else still needs the token).
- `required_wiring` / ordering independently re-verified by reading the
  code (not taken on the pipeline's word): `tryServeSideloadApk` is called
  in `startBridge`'s handler at line 1682, explicitly *before*
  `isAuthorizedForRead` at line 1692 (comment: "must stay ahead of the 401
  gate") — invariant 2 (pre-auth route never widens what auth'd routes
  below can reach) holds structurally: the function returns `false`
  immediately for any non-matching method/path, before any fs access.
  `resolveSideloadApkFile` (extracted by hardener) checks containment via
  `isWithinPublicRoot` (prefix check on the *resolved* path) AND
  `statRegularNonSymlinkFile` (`fs.lstatSync` — does not follow symlinks) —
  both required, matching invariant 1. Confirmed export surface
  (`SIDELOAD_APK_PATH`, `resolveSideloadApkFile`) used by the property test
  file is the actual production code path, not a test-only duplicate.
- Invariants: both declared invariants carry non-vacuous property coverage
  (architect independently confirmed non-vacuousness by reverting the fix
  and re-running — see `backlog/evidence/BL-851-architect-pass-20260809.md`);
  this pass re-ran that same property file standalone and within the full
  suite, green both times.
- CRAP gate: hardener's table (all 8 sideload-route functions ≤ CRAP 6,
  down from 8.01/7.00) independently spot-checked by reading the split
  functions directly — decomposition is behavior-preserving, no branch
  added/removed/reordered.
- No orphaned test/mutation processes before or after this pass
  (`pgrep -fl 'node --test|stryker'` clean both times); reaped 3 leftover
  vitest worker processes by process group after an interrupted exploratory
  run (`kill -- -<pgid>`) before the real verification runs.

## Notes, not blockers

- The underlying sideload route landed on `main` earlier via an unrelated
  bundled commit (`2e65b769`, no `Hotfix-Certification` trailer, no
  hotfix-ledger entry) — flagged by both coder and documenter as a known
  gap, explicitly out of this ticket's scope (`out_of_scope`: "Broader
  bridge auth rework"). This ticket's own acceptance criteria are about the
  security review/fix, not ledger bookkeeping; not a QA gate for BL-851.
  Surfacing for the coordinator/specifier's awareness, not a bounce.
- Goal 5 (content-length vs. mid-request file replacement, a narrower
  TOCTOU concern) is explicitly descoped by the coder as a candidate
  follow-up; it bears on neither declared invariant. Accepting the descope,
  consistent with architect's own read.

## Verdict

Clean pass. No defects in this ticket's own domain.

## Landing

- QA broadcasts a `note` (priority `00`) to `coder,cleaner,architect,
  hardender,documenter` naming this commit for merge-up.
- QA lands this commit on `main` (merge, push origin). BL-851 is not
  `GH-`-seeded (no `source:` GitHub issue URL), so `issue_done.sh` is not
  invoked.
- QA notifies the coordinator with the approved commit hash and BL-851 for
  backlog bookkeeping: move `backlog/active/BL-851-...yaml` to
  `backlog/done/`.
  Correction to this evidence file's earlier draft (written before merging
  `main` into this pass): this QA branch had not yet picked up `main`'s own
  `424e8c8f` ("Promote BL-851: paused → active for coder"), so the ticket
  briefly *looked* paused-only from this branch's stale view. Merging
  `main` (commit `40916b94`) picked up the real promotion — `git ls-tree -r
  --name-only main | grep 851` confirms
  `backlog/active/BL-851-...yaml` is the ticket's actual current location.
  Standard `active/` -> `done/` bookkeeping applies; no special case.

By QA.
