# BL-1747 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

Reviewed coder's fix (3ce5b01ae0): `constructedDraws()` builds all 14
(kind x index-empty) cells with a fixed stderr string per kind
(`constructedStderrFor`), prepended to the 40 sampled draws; the
reachability floor now asserts both `true:<kind>` and `false:<kind>` for
every kind, drawing on `seen`, which is populated directly from each
draw's own declared `kind`/`indexEmpty` tags (not from output
classification) — so with all 14 cells always present, the floor holds
unconditionally, independent of what the sampler draws.

Declared invariant review (this ticket's one `invariants:` entry — reach
floor holds for every seed, by construction): checked each constructed
kind's stderr content against its own per-draw assertions (not just the
floor): `empty`/`whitespace-only` both trim to length 0 (assert "no
text"); `short`/`short-with-newline` are short and unter the
`TRUNCATE_LIMIT` (verbatim, untruncated); `at-limit` is exactly
`TRUNCATE_LIMIT` chars (untruncated, boundary not exceeded);
`one-over-limit` is `TRUNCATE_LIMIT+1` (truncated); `well-over-limit` is
`TRUNCATE_LIMIT+500` (truncated) — each matches its kind's own shape from
`stderrCaseArb`, so the constructed cells satisfy invariant 1's per-draw
assertions too, not only the floor.

Checks run:
- `node out/tools/dependency-gate.js` (from `extension/`): PASSED, no
  forbidden edges.
- `node out/tools/co-change-report.js`: nothing above the default
  frequency-3 threshold — no flagged coupling.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`: run 5
  times back to back, all green (previously ~1.5% chance of a floor
  miss per run) — consistent with the fix's claim that the floor is no
  longer probabilistic.
- Independently re-verified non-vacuity myself: reverted `draws` to the
  bare `fc.sample(inputArb, 40)` (no constructed cells) and ran the
  property 8 times back to back — 4 of 8 runs failed the reachability
  floor, reproducing the ticket's described ~1.5%-per-run miss rate at
  this scale. Restored the fix (confirmed `git diff` clean), reran green.
- `node specs/pipeline/cli.js
  specs/features/BL-1474-the-replay-reports-why-its-commit-was-refused.feature`:
  5/5.
- Scope check: the coder's commit touches exactly the one Scope file
  plus evidence; `backlog/standing-reds.tsv` untouched.

No production code touched. No defect found.
