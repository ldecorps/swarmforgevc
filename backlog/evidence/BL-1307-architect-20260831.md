# BL-1307 — architect pass, 2026-08-31

Reviewed coder commit `6f8227894e` (parent `46c2d2a6b2`).

## Checklist run

- **Dependency-rule gate (BL-259)**: `node extension/out/tools/dependency-gate.js`
  (full-repo, no extension/ files touched by this parcel) — PASSED, no
  forbidden edges.
- **Co-change report (BL-255)** against the four changed core files
  (`review_forward_evidence_gate_lib.bb`, `swarm_handoff.bb`,
  `bl1307ReviewForwardOwnEvidenceSteps.js`, `specs/pipeline/steps/index.js`):
  flags the sibling gate family (`bl806`/`bl950` steps,
  `pre_qa_gate_gather_lib.bb`, the two `.bb` test runners) — all already
  edited together in this same commit or genuinely part of the same gate
  family; the rest of the report is repo-wide noise from unrelated merge
  commits (android/, .vscode/, .github/ at frequency 4). No unticketed
  coupling found.
- **Module boundaries / policy purity**: `blocked?` stays a pure decision
  over booleans (`carries-own-evidence?` is threaded in as a plain fact,
  same shape as the two existing facts); the new fs-touching read
  (`forward-carries-own-evidence?`) is a third, separate function, matching
  the file's existing `received-commit-for-task` /
  `forward-introduces-nothing-own?` pattern. `swarm_handoff.bb` computes
  `review-forward-received-commit` once and reuses it for both the gate call
  and the new fact — an improvement over the prior duplicate computation, not
  a regression.
- **Invariants (BL-654)**: both declared invariants have coder-authored
  property tests in `review_forward_evidence_gate_lib_property_runner.bb`
  with a by-construction generator and asserted reachability of both verdicts
  (`evidence-refused-reached` / `evidence-allowed-reached`). Confirmed
  non-vacuous by independently re-running the suites (not re-deriving the
  break-then-fix by hand — coder's documented deletion of the
  `(false? carries-own-evidence?)` disjunct turning the suite red is taken as
  reported, since the current suite is green and the disjunct is present in
  the reviewed diff).
- **Property coverage (undeclared)**: no other pure module was touched by
  this parcel — the two production `.bb` files are already covered by the
  invariant-driven property tests above; no gap to add.
- **Required wiring anchor**: `forward-carries-own-evidence?` is called from
  `swarm_handoff.bb`'s live send path (lines ~381-395), not merely declared —
  confirmed by grep, matches the ticket's `required_wiring` entry.
- **Step registration**: `bl1307ReviewForwardOwnEvidenceSteps` is required
  in `specs/pipeline/steps/index.js:551`.
- **Tests re-run directly** (not trusted from the coder's report alone):
  - `bb swarmforge/scripts/test/review_forward_evidence_gate_lib_test_runner.bb` — ALL PASS
  - `bb swarmforge/scripts/test/review_forward_evidence_gate_lib_property_runner.bb` — ALL PASS
  - `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1307-a-review-forward-adds-evidence-the-role-authored.feature` — 6/6
  - sibling `BL-1293` acceptance — 6/6 (unmoved)
  - sibling `BL-806` acceptance — 9/9 (unmoved)
- **Sibling fixture edit** (`bl1293ReviewForwardOwnWorkSteps.js`, "carries the
  role's own fix" row): now commits an evidence file alongside the fix,
  matching what a real review pass does under this gate. Row's contract
  (BL-1293 judges commit CONTRIBUTION) is unchanged — a fixture correction,
  not a contract change. Agreed with the ticket's own framing.

## Verdict

**D: NONE.** No architecture violation, no invariant violation, no
correctness defect found. This IS the parcel whose gate now requires this
very evidence file on my own forward — filed per Article 4.4.

Forwarding to `hardender`.
