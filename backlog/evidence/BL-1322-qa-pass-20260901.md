# BL-1322 — QA pass, 2026-09-01

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1322` → `VERIFY BL-1322` (exit 0).

## Merge

Merged `swarmforge-documenter` (documenter's cited commit `092a2b4392`) into
QA — fast-forward from `2734f132b7`, no conflicts.

Ancestry confirmed against the commit under evaluation (`092a2b4392`), not
merely ancestor-of-HEAD (BL-336 discipline):
- coder `4c88141782` — ancestor
- cleaner `3c5476fdaf` — ancestor
- architect `c6414820f2` — ancestor
- hardener `5e150904f1` — ancestor
- documenter `e6549cae56` — ancestor

## Code review

`extension/src/bridge/cursorBridgeAgentSession.ts`: `resolveCursorApiKey`
moved from `createLiveCursorBridgeAgentSession`'s top level into `ensureAgent`
(called only from `promptAgent`). Matches the ticket's declared invariant
exactly: constructing the session, reading the stored agent id, and resetting
session state no longer require `CURSOR_API_KEY`; only an actual prompt
attempt resolves the key. `shouldUseFrontDeskRunner` branch untouched.

## Wiring

`createLiveCursorBridgeAgentSession`'s only production call site is
`bridgeServer.ts:2026` (`startBridge`'s `letsTalkAgentSession` construction) —
unchanged by this diff, confirmed by grep. No new call site to wire (per the
ticket's own `notes:` — pure code motion inside an existing single-call-site
function).

## Acceptance

`node specs/pipeline/cli.js specs/features/BL-1322-bridge-startup-does-not-require-cursor-api-key.feature`
— 4/4 scenarios pass.

## Full unit suite (`npm test`, extension/)

15 failed / 570 passed (585) test files; 25 failed / 9821 passed (9846)
tests. Zero overlap with this parcel's changed paths. Grepped every failing
file against `backlog/` and against BL-1318's own QA-pass evidence (one
commit prior on this same branch, `2734f132b7`):

- The 8 files BL-1322 was minted to fix (bridgeServer, epicMakeTopBridge,
  epicReorderBridge, pausedPagerBridge, specTreeBridge,
  startBridgeHeadlessCli, telegramCursorBridgeCli, topicMakeTopBridge) are
  now **green** — confirmed absent from this run's failing list.
- The remaining 15 failing files match BL-1318's evidence exactly:
  - liveRepoDerivationGuard, socketFixtureShortRootGuard, tempDirTrapGuard —
    pre-existing standing reds.
  - backendSwitch, telegramClient, telegramCursorOperatorExec — **BL-1263**.
  - constitutionDocCitations, pilotAcceptanceGate, unreachableStepHandlerCheck
    — **BL-1221**.
  - crossFileDuplicationCheck, multiBranchParserCoverageCheck,
    perHatRolePromptEvidenceCheck, pilotScopedCrapCheck,
    shellEntryPointDriveCheck — **BL-1229**.
  - operatorRuntimeBbFixtureClosure — **BL-1265**.

No new/untracked red found. Per BL-1063, none re-reported.

## Property suite (`npm run test:properties`, extension/)

14 failed / 835 passed (849) tests, 25 failed test files. Cross-checked every
failing file against `swarmforge/scripts/property_suite_standing_allowlist.tsv`:
24/25 present with `disposition=allowlist`. The 25th,
`bl1218RemoteControlConfigInvariants.property.test.js`, is not yet in the tsv
but independently confirmed pre-existing and unrelated: last touched only by
BL-1218's own coder commit (`5b534f8ea9`, remote-control launch-config
decision logic), which already shipped through QA on 2026-08-31
(`backlog/evidence/BL-1218-qa-pass-20260831.md`) — untouched by this diff.
The new invariant test for this ticket
(`cursorBridgeAgentSessionLazyApiKey.property.test.js`) passes.

No new/untracked red found.

## Documentation

`docs/reference/Specification.MD` carries a changelog entry (Last Updated
Sept 1, 2026) summarizing the fix and the 8 previously-failing files now
passing. Documenter's own evidence (`BL-1322-documenter-20260901.md`)
confirms no diagram trigger fired and no how-to page is warranted (internal
call-timing fix, no new user-visible command/setting/flow).

## Verdict

PASS. Ticket's invariant satisfied, acceptance green, wiring confirmed, no
untracked regression, docs current. Landing.
