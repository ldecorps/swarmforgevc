# QA pass — BL-1428 — 2026-09-05 (completing the full chain)

## Correction to my earlier BL-1290/BL-1428 batch approval

My earlier evidence file `BL-1290-BL-1428-qa-pass-20260905.md` (this same
session, commit `2d8427420f`, landed to `origin/main`) treated BL-1428 as
fully verified based on `git log --all --oneline | grep "BL-1428:"`, which
lists commits on ANY branch — not necessarily ancestors of my tree. That
approval's actual commit did NOT contain the hardener pass
(`e957ec9e23`, mutation testing) or documenter pass (`699a44fd13`, the
how-to doc) as ancestors — confirmed now via `git merge-base
--is-ancestor e957ec9e23 2d8427420f`, which returns false. Only
coder/cleaner/architect had landed for BL-1428 at that point (BL-1290's
own `stage_skip_reasons` legitimately skipped cleaner/hardener/documenter
for BL-1290 itself, but BL-1428's `required_stages` is the full chain —
I conflated the two tickets' skip states).

Net effect: BL-1428's production code (`standing_red_register_lib.bb`,
`check_standing_red_register.sh`, wired into `run_commit_guards.sh`'s live
Tier 1) was live on `main` for a period without the hardener's mutation
pass having run against it. The hardener pass in this parcel (`e957ec9e23`)
retroactively confirms it clean (BL-113: 8/8 mutants killed, no defect),
so no regression resulted, but the gate order was violated. Recording this
here per the constitution's evidence discipline rather than quietly
completing the parcel.

## This parcel's actual verification (hardener + documenter passes)

Commit under review: `6c48494356` (merge of documenter's `699a44fd13` into
`swarmforge-QA`). `git merge-base --is-ancestor e957ec9e23 HEAD` and
`...699a44fd13 HEAD` both confirm OK.

- `bb swarmforge/scripts/test/standing_red_register_lib_test_runner.bb`: ALL PASS.
- `bash swarmforge/scripts/test/test_run_commit_guards.sh`: ALL PASS (12/12).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1428-every-standing-red-names-an-open-owner.feature`
  — 7/7 scenarios pass (re-run, independent of the earlier batch run).
- Hardener's BL-113 soft Gherkin mutation: 8/8 mutants killed, 0 survived
  (verified by reading the hardener's evidence in full — re-running the
  mutation tool myself is out of QA's domain per constitution).
- Documenter's new how-to (`docs/how-to/BL-1428-standing-red-register.md`)
  confirmed linked from `docs/index.md` (line 139).
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — the full required_stages chain is now genuinely complete and
verified. Merging up and landing.
