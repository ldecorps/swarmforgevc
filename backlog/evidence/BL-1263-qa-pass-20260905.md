# QA pass — BL-1263 — 2026-09-05

Commit under review: `2461fbd23f` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`e34b387d6e` BL-1263 redo
coder commit, confirmed ancestor via `git merge-base --is-ancestor`). I
myself surfaced the original register-cleanup gap the architect bounced
on (visible earlier via `BL-1263-bounce-20260905.md`, noted in my BL-1433
evidence); this parcel is the redo that closes it.

## Verification

- Read the actual diff (`git diff` between this ticket's parent and its
  own commit) for all three sites: each is a minimal, targeted realignment
  matching the ticket's own description exactly — `'auto'` → `'cursor/auto'`,
  `allows_multiple_answers: false` added to a still-full `deepEqual` object,
  fixture moved from `paused/` to `active/`. No weakening, no deletion.
- `git diff -- extension/src/`: empty — confirmed zero production source
  changed, per the ticket's firm constraint.
- `npx vitest run test/backendSwitch.test.js test/telegramClient.test.js
  test/telegramCursorOperatorExec.test.js`: 123/123 pass.
  - Note: an initial combined run showed a 4th, unrelated test
    (`BL-1204: executeOperatorVerb(/redeploy, "all")`) failing with
    `ENOTEMPTY` on `/tmp/bl1204-all-*` — traced to a leftover fixture
    directory from my OWN much-earlier testing this session (13:16Z),
    never cleaned up by that run's own sweep. Removed it and re-ran: all
    123 pass, confirming this was session debris, not a BL-1263 defect.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1263-stale-assertions-are-retired-to-the-behaviour-that-shipped.feature`
  — 5/5 scenarios pass, including "the whole-body comparison stays whole,
  so the next unannounced field is still caught" (deepEqual not weakened)
  and "no assertion is removed and no source behaviour is changed."
- Full unit lane (`npm test`): 3 files failing
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — all three of BL-1263's own target files confirmed
  gone from the failing set; the 3 remaining are pre-existing, separately
  ticketed (BL-1212, BL-1265).
- Register cleanup: `grep -c BL-1263 backlog/standing-reds.tsv` = 0.
- `required_wiring`: `specs/pipeline/steps/bl1263StaleAssertionsRetiredToShippedBehaviourSteps.js`
  exports `registerSteps`.
- Hardener's own BL-113 mutation pass found and fixed a genuine gap (3
  survived mutants on first run, re-verified 0 survived after the fix) —
  read in full, re-verification table confirms independently.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after (once my own session debris was cleared).

## Verdict

APPROVED — merging up and landing.
