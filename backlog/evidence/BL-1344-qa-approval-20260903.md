# BL-1344 — QA approval (20260903)

Received: documenter commit `082b70699c` (review-pass evidence, new
BL-611 runbook section), forwarding architect (clean sweep, escalation
suppression explicitly deferred as out of scope), hardener, and cleaner
passes. Real feature work: a durable, explicitly-recorded waive for a
babysitter finding over permanent history, beside (never instead of) the
existing 30-minute nudge cooldown.

## Review inventory (complete pass, Article 4.4)

- Wiring anchors on the merged tree:
  - `swarmforge/scripts/babysitter_check.bb` requires
    `babysitter-waive-lib` and calls `read-waive-store` /
    `partition-findings` at the nudge decision (line ~1288), before
    `decide-nudges` — confirmed live, not merely defined in the lib.
  - `specs/pipeline/steps/index.js` requires
    `bl1344BabysitterFindingWaiveSteps` — present.
- Read `babysitter_waive_lib.bb` by hand: `partition-findings`'s `:else`
  branch (unreadable/malformed store) returns EVERY finding in
  `:to-nudge` and none in `:suppressed` — matches invariant 3 (fail-open,
  never fail-quiet). `record-waive` throws unless key/waived-by/reason
  are all non-blank — matches invariant 2 (only a recorded, authored,
  reasoned decision waives). Suppression keys off `(contains? (:waives
  read-result) key)` for the exact finding key only — matches invariant 1
  (one key, one waive; no class/pattern matching anywhere in the lib).
- Consumer wiring read by hand
  (`swarmforge/scripts/babysitter_check.bb:1279-1301`): the waive is
  applied to `nudgeable` (passed to `decide-nudges`) but NOT to
  `to-escalate` (still computed from the full `findings`) — confirmed
  deliberate and in-scope per the ticket's own text ("consulted at the
  nudge decision") and the architect's evidence, which records this as a
  reviewed, not-taken-on generalization (constraints: "do NOT extend to
  the other detectors... in this parcel").
- `bb swarmforge/scripts/test/bl1344_waive_lib_test_runner.bb`: ALL PASS.
- Acceptance
  (`specs/features/BL-1344-an-investigated-finding-can-be-waived.feature`):
  7/7, including the corrupt-store negative case and the second-finding
  non-suppression case (qa_e2e_procedure steps 3 and 5).
- Property test (`bl1344WaiveInvariants.property.test.js`): 3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated `partition-findings`'s error branch to suppress every
  finding on an unreadable/malformed store instead of nudging them (the
  exact fail-quiet bug invariant 3 forbids) — 4 of the bb test runner's
  assertions failed exactly as expected. Restored (`git diff --stat`
  empty), re-ran ALL PASS.
- Constraint check: no change to `nudge-cooldown-ms` or the dedup
  mechanism; no widening to other detectors' findings; the waive store is
  a new tracked YAML (`backlog/babysitter-waives.yaml`), never rewriting
  dedup or finding history.
- Compile (`extension`, `npm run compile`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population (includes the three known standing guards:
  `tempDirTrapGuard`, `socketFixtureShortRootGuard`, `liveRepoDerivationGuard`).
  None touch this parcel's files.
- Property suite (`npm run test:properties`), run sequentially after the
  unit suite: 24 files / 13 tests red, exactly matching the standing-debt
  population. No BL-1344 file present in the failing set.
- Documentation currency: `docs/how-to/BL-611-babysitterd-runbook.md`
  gains a waive section; `docs/reference/Specification.MD`'s new top
  entry confirmed present.
- Orphaned test processes: none of mine after the runs. A `vitest`
  process was alive mid-check but confirmed (via `ps -o pid,ppid,args`)
  to belong to a different concurrent worktree's own BL-1351 run — left
  untouched.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
