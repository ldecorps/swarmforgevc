# BL-1431 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed the full threading chain: `land_step_cli.bb` resolves
  `origin-main-sha` exactly once at entry and passes it into both
  `land-plan` and (on replay) `replay!`; `land-plan` uses the
  `contains? opts :origin-main` guard correctly (distinguishing "caller
  supplied a value, even nil, because entry resolution already failed"
  from "caller doesn't know this key exists, resolve here") — the same
  guard shape is reused identically in `replay!`.
  `entangled-siblings`/`own-paths`/`main-ticket-sources`/
  `ticket-approval-state` each gained a new explicit-`origin-main` arity
  with the old convenience arity resolving once and delegating — no reader
  function re-resolves the ref by name.
- `grep -c "(origin-main-sha root)" land_step_lib.bb` → 7, all 7 confirmed
  by line number to be entry-point convenience arities (413, 511, 581,
  823, 835, 993, 1256), none inside a function that already receives an
  already-resolved value from a caller — matches the coder's own
  enumeration exactly. `grep -c origin-main-sha land_step_cli.bb` → 1
  (required_wiring, was 0 before this ticket).
- Confirmed `land_main_publish.sh` is genuinely untouched (`git diff
  <base> <head> --stat` names no changes to it) — invariant 2 rests on the
  unmodified publish/rematch mechanism, proven by its own unchanged test
  suite plus this ticket's live scenario 04.
- Confirmed the one reported test failure
  (`test_bl1388_land_step_guard_fixture.sh` item 4, "the diff touches
  assertions outside the fixture block") is genuinely pre-existing and
  unrelated: `git diff main <base-before-this-ticket> --
  land_step_lib_test_runner.bb` is empty — the file was already
  byte-identical to `main` before this parcel touched it, so the check's
  own premise (comparing against `main` to prove "only the fixture block
  changed") is permanently broken for any later parcel, not something this
  ticket introduced or could fix within its own scope.
- Ran all five sibling land-step features named in the ticket's own
  qa_e2e_procedure (BL-1389, BL-1375, BL-1354, BL-1343, BL-1315) — all
  pass unchanged, proving the entanglement/landed/own decision logic
  itself was not altered, only WHEN origin/main is read.
- No duplication: `jscpd` doesn't parse `.bb` (0 files analyzed,
  informational only), but a manual read confirms the "convenience arity
  resolves once and delegates" pattern is applied consistently and matches
  the codebase's existing style (the same shape `land-plan`/`replay!`
  already used for other optional keys).
- Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred) — this
  pass is gated by the unit/property/acceptance suites only, per
  constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/bl1431_one_land_plan_one_tip_property_runner.bb` | ALL PROPERTIES HOLD, 40/40 each of P1/P2 |
| `node specs/pipeline/cli.js specs/features/BL-1431-...feature` | 4/4 scenario runs |
| BL-1389/BL-1375/BL-1354/BL-1343/BL-1315 features (regression) | 5/5, 7/7, 5/5, 6/6, 7/7 — all pass unchanged |
| `bash swarmforge/scripts/test/test_land_step_records_approval.sh` (regression) | ALL CHECKS PASSED |
| `bash swarmforge/scripts/test/land_main_publish_test_runner.sh` (regression, untouched file) | ALL PASS |
| `bash swarmforge/scripts/test/test_is_qa_ancestor_land_replay_store.sh` (regression) | ALL CHECKS PASSED |
| `bash swarmforge/scripts/test/test_bl1366_land_is_one_command.sh` (regression) | ALL PASS |
| `grep -c "(origin-main-sha root)" land_step_lib.bb` | 7, all confirmed entry points |
| `grep -c origin-main-sha land_step_cli.bb` | 1 (required_wiring) |
| `bl1431OneLandPlanOneTipSteps.js::registerSteps` exists | yes (required_wiring) |
| pre-existing `test_bl1388_...` item 4 failure | confirmed pre-existing/unrelated (empty diff against main before this ticket) |

No fix required. Forwarding unchanged to architect.
