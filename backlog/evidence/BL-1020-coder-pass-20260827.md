# BL-1020 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Context

Re-promoted coordinator route (`d8378dd3a`). Implementation already landed
through QA (`13b2cb53a`). This pass verifies standing-pack marker inertness on
the re-promotion path.

## Invariant (BL-654)

Topology from pack configuration, never from a leftover marker on empty-rotation
(standing) packs — encoded in `mono_router_lib.bb` + acceptance scenarios.

## Verification

| check | result |
|---|---|
| `mono_router_lib_test_runner.bb` | ALL PASS |
| `run_acceptance.sh BL-1020…feature` | **3/3** |

Router packs still honour the marker (scenario 02 regression pinned).

By coder.
