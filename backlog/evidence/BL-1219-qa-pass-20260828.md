# BL-1219 — QA pass — 20260828

**Received:** `merge_and_process documenter eb71a186f6` (handoff
`00_20260828T032330Z_000884_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `eb71a186f6...`
**QA merge commit:** `290f6d456f482e03b68e0ed0312b522d0ddddc6b`

Single pass, no bounce. Fixes a real production blind spot: dead-lettered
mail to the two master-resident roles (specifier, coordinator) was
silently unscanned by the TypeScript-side notify sweep.

## Merge

Clean, no conflicts (`git merge --no-ff eb71a186f6`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor c9210fe88 HEAD` (coder) — CONFIRMED
- `git merge-base --is-ancestor 35dade9a9 HEAD` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 3840f9b3c HEAD` (architect) — CONFIRMED
- `git merge-base --is-ancestor bd1e33b8c HEAD` (hardener) — CONFIRMED
- `git merge-base --is-ancestor eb71a186f HEAD` (documenter, received tip) — CONFIRMED

## Fix review (independent)

`buildRoleInboxes` (`extension/src/watchdog/chaserMonitor.ts`) now calls
the pre-existing shared `mailboxDir`/`mailboxBaseDir` resolver
(`extension/src/swarm/swarmState.ts`, BL-128) instead of hand-joining
`worktreePath + '.swarmforge/handoffs/inbox'`. That resolver already
special-cases `worktreeName === 'master'` to return
`<root>/.swarmforge/handoffs/<role>/` — exactly the real mailbox path — so
the fix is "use the already-correct shared resolver" rather than new logic,
matching the ticket's own "How" direction. Worktree-role resolution is
provably unchanged: `mailboxBaseDir` returns the identical
`<worktreePath>/.swarmforge/handoffs` join for any non-master
`worktreeName`, which is what it always returned before.

**Cross-language parity (the ticket's explicit ask — "pinned by a test
asserting they agree").** `specs/pipeline/steps/lib/bl1219MailboxDirCli.bb`
is a thin driver calling the REAL `handoff-lib/mailbox-dir` (the Babashka
daemon's own resolver); acceptance scenario 06 ("the two language
implementations agree on every seated role") asserts the TS and Babashka
resolvers produce identical paths for every seated role, both shapes. This
is the durable gate the ticket asked for, not a comment.

## Verification inventory

| Gate | Result |
|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1219` | `VERIFY BL-1219` — no open deferral |
| Compile | `npm run compile` | PASS, clean |
| Targeted unit (`chaserMonitor`/`notifyDeadLettersCli`/`stuckInProcessChase`) | 33/33 PASS |
| Acceptance (`run_acceptance.sh`, 7 scenarios incl. the outline's 2 examples) | 7/7 PASS — covers qa_e2e_procedure items 1 (dead letter seen, scenario 02), 2 (both role shapes seeded, scenarios 01/03), 3 (fossil dir not scanned, scenario 04), 4 (no duplicate announcement, scenario 05), plus the explicit cross-language parity ask (scenario 06) |
| BL-113 Gherkin mutation | 2/2 killed on the one Scenario Outline (hardener's evidence) — non-vacuous, keyed on `rolesList.includes(entry.role)` against real `roles.tsv` |
| Fixture-leak review | Hardener deliberately checked this file for the exact BL-1204-class hazard (early-step throw before terminal `try/finally`) and confirmed it's NOT a live leak here — `mkSocketFixtureRoot`'s own `process.on('exit')` backstop covers it, unlike BL-1204's raw `mkdtempSync`. Independently plausible: this file uses the shared fixture helper throughout (grepped). |
| Constraint: worktree-role resolution unchanged | Confirmed by reading `mailboxBaseDir`'s branch structure directly — the non-master path is byte-identical to the pre-fix join |
| Constraint: fossil directory not migrated/deleted | Confirmed — `.swarmforge/` is untracked runtime state, fix is a pure code change to path resolution, no filesystem operation on the fossil dir |
| Docs | `docs/reference/Specification.MD` top entry present and accurate; no dedicated how-to page — a defensible scope call for an internal resolver-parity fix (a real, substantive changelog entry, not a bare forward — unlike this session's BL-1203/BL-1204 first-pass bounces, which had zero documentation of any kind) |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker'` | none, before and after |

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1219's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1219 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1219 active → done).

By QA.
