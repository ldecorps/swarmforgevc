# BL-1324 — documenter pass, 2026-09-02

Reviewed commit `911a787f37` (hardener clean sweep), merged into documenter as
`f8bb180828`. Review-only BL-848 stamp-off parcel: no production wiring, no
new user-visible behavior introduced by this parcel itself — the reviewed
hotfix `4ed88430b2` already landed on `main` independently, outside this
parcel.

## Doc sweep

- Checked `docs/` for any existing description of the qwen-cloud
  context-window mechanism (`CLAUDE_CODE_MAX_CONTEXT_TOKENS`,
  `bob-multi-provider-mono-router.conf`, `qwen3.8-max`): none existed before
  this pass. The hotfix landed directly to `main` under the BL-848
  certification doctrine, which documents each stamp-off as a "Related"
  entry in `docs/how-to/BL-848-certify-an-operator-hotfix.md` rather than by
  authoring a new standalone doc for every hotfix's mechanism — confirmed
  this is the established pattern by checking the doc's git history for the
  three prior stamp-offs (BL-1113, BL-1117, BL-1116), each of which added
  itself to that file's "Related" list in its own documenter pass.
- Followed the same pattern: added a BL-1324 entry to that "Related" list
  (commit `9b21a478b9`), naming the reviewed commit, the human ruling
  (certify + follow-up), and that the ledger decision remains pending until
  the stamp ticket reaches `done`.
- No diagram registered under `docs/diagrams/` depicts pack staffing,
  billing-guard routing, or context-window sizing — none of the four
  registered diagrams' change-triggers (architecture boundary, pipeline
  topology, handoff state machine, front-desk routing) fired for this
  parcel. No diagram update owed.
- `docs/reference/Specification.MD`: not touched — this parcel adds no new
  user-visible command, setting, or flow of its own; it reviews a hotfix
  that already shipped and is out of scope to redescribe here (ticket
  constraints: do not reimplement/rewrite/redesign the hotfix).
- No RETIRED/deprecated behavior involved — Article 3.6 deprecator gate is
  not implicated by this parcel.

## Verdict

NONE — no documentation defect found. Forwarding to QA.
