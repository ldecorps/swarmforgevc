# BL-1341 — QA approval (20260902)

Received: documenter commit `af7dd8a336` (doc-only), forwarding hardener
`de7e7edf01` (architect `fd46a98f52`, cleaner `479373f6a9`, coder's
second-direction fix `c90be7587e..963dec3774`), unchanged. Merged into QA.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bash swarmforge/scripts/test/test_merge_deletion_guard.sh`: ALL PASS,
  14 cases (13 + 13b).
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1242-merge-never-silently-drops-branch-work.feature`):
  12/12 (was 7 pre-BL-1341, five new scenarios for the incoming-side
  direction).
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1242MergeBranchWorkDeletionSteps` (line 875) — confirmed.
- Declared invariant
  (`extension/test/bl1341MergeDropsEitherSideInvariants.property.test.js`):
  1/1.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-disabled the second `collect_deletions "$MERGE_HEAD_SHA" "the
  incoming branch"` call in `check_merge_deletion.sh` — both the shell
  suite (case 10) and the property test failed exactly as claimed.
  Restored (`git diff --stat` empty), re-ran clean.
- **Hardener's own mutant-kill re-verified independently**: hand-patched
  the both-sides dedup (`side_of["$path"]="${side_of[$path]} and $side"` →
  `side_of["$path"]="$side"`, dropping the first-seen side's name) — case
  13b failed exactly as claimed (the finding named only one side).
  Restored, re-ran ALL PASS.
- Confirmed the guard is actually wired: `swarmforge/git-hooks/commit-msg`
  line 30 calls `check_merge_deletion.sh` on every commit-msg hook run —
  not merely declared, a real live caller.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical to the
  standing-debt population from every prior pass today. None touch
  `check_merge_deletion.sh` or the merge-guard tests.
- Property suite (`npm run test:properties`): 24 files / 13 tests red, same
  standing-debt population. BL-1341's own property test — 1/1 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry, `docs/how-to/BL-1242-merge-deletion-guard.md`'s rewrite (both
  directions, dedup, hardener's own fix), and `docs/index.md`'s updated
  line for the same how-to — all confirmed present and accurate. No new
  doc file, no diagram change-trigger fired.
- Orphaned test processes: none after the property-suite run.

## Live-history reproduction re-verified

`git diff --name-status 0132715d1e b71c941a19` → 0 deletions;
`git diff --name-status 0a5bffe057 b71c941a19` → 9 deletions. Confirms the
guard's blind spot was real on the actual incident commit and that the
fix's second-direction diff is the one that would have caught it.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
