# BL-795 spec-gap send-back — 2026-08-03

## Defect inventory (Article 4.4 — complete pass)

- **D1** — class: `spec-gap`, blamed role: **specifier**. BL-795 was promoted
  to coder with `acceptance: specs/features/BL-795-mono-router-starvation-hand-fix.feature`,
  but no such file existed (the specifier never wrote it at spec time; the
  ticket even lists it under `required_wiring`). The coder correctly raised a
  spec-gap `note` to specifier + coordinator per Article 4.4 rather than
  bouncing — every item was a spec gap, so the parcel stayed with the coder.
- No other defects found in this pass. **NONE** beyond D1.

## Remediation (specifier, this commit)

Wrote `specs/features/BL-795-mono-router-starvation-hand-fix.feature`:
five scenarios pinning the ticket's three invariants plus the BL-576
fresh-note broadcast-thrash guard —

1. `mono-router-starvation-hand-fix-01` — a directed rule_proposal alone is
   immediately actionable (preferred rotate target), mirroring
   `test_handoffd_rule_proposal_rotate_wiring.sh` case A.
2. `-02` — a fresh note alone stays non-actionable (BL-576 guard; case B).
3. `-03` — an in_process priority-00 held claim outranks a priority-50
   rule_proposal (case C).
4. `-04` — a chase poke at a non-preferred role REDIRECTS onto the preferred
   role instead of dropping as not-preferred (`chase-rotate-to!` redirect;
   drivable offline via `--poll-once` over a temp fixture, asserting the
   `chase-rotate-redirect` path).
5. `-05` — exhausted chase escalation still applies a wake and advances the
   nudge count (`test_chase_sweep.sh` scenario 06).

Gates run: `gherkin_lint_gate.sh` clean parse; APS IR-DRY checker run, one
accidental near-duplicate normalized ("stale rule_proposal" → identical step
reuse), remaining findings reviewed as deliberate (priority-explicit variant
is load-bearing in -03; role-parameterized Then steps).

## Routing

Holder (coder) notified by `note` (priority 00) naming this amendment commit:
merge `main`, re-read the ticket + feature file, and land the five scenarios'
step handlers in the same parcel (BL-233 — the acceptance runner throws on
unhandled scenarios). Handler shape precedent:
`specs/pipeline/steps/bl636RotatePreferenceParcelPrioritySteps.js` (drive the
real bb libs / wiring shell tests, no hand-built score rows).
