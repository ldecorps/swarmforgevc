# BL-1752 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter c4fe8341e8, merged into QA as dad52e07bf. Three
bookkeeping conflicts were resolved: BL-1700's and BL-1711's YAML kept QA's
fuller abandoned_commits, and BL-1711-QA-20260926.md kept the documenter
side's correction paragraph. No BL-1752 path conflicted.

Gates (qa-gather, one run each on dad52e07bf):
- straggler check before/after: none. Sibling status: `VERIFY BL-1752`.
- register: `unowned: []`. `pre_qa_gate` (required_wiring): OK.
- `npm test`: exit 1 on one file only,
  `test/briefingDigestLineCli.test.js > the compiled CLI falls back to
  deriving its own history when --snapshot points at a nonexistent file`:
  `Error: Test timed out in 20000ms.` The parcel did not touch this file,
  and the register row names BL-1770 as its owner (register_join: owned).
- `npm run test:properties`: exit 0.
- acceptance (BL-1752 feature): 5 of 5.

qa_e2e:
1. Feature: 01 x3, 02 and 03 all ok.
2. `mono_router_lib_test_runner.bb` ok, and
   `test_chase_departing_mid_parcel_gate.sh` ALL CHECKS PASSED (08a-10 new;
   01a-07 are unchanged BL-1535 cases).
   `test_consult_spawn_cli.sh` ALL CHECKS PASSED.
3. git grep for spawn-consult-session / consult-eligible /
   single_inference_slot / single-inference-slot in swarmforge/scripts,
   swarmforge/packs and extension/src, excluding test/: no hits.
4. BL-1752 commits touch the Scope files, tests, the step handler and
   evidence, plus comment-only edits to consult_spawn_cli.bb and
   night-closing-ceremony-run.ts. Those edits drop references to the
   removed functions; step 3's grep needs that, and no code line changed.

Invariants: there is no property test. The coder's stated reason is
accepted: Babashka carries no property-test wiring (engineering "Startup
Tools"). The real shell fixture covers both invariants, and the coder
showed it is not vacuous (08a-c fail on the pre-fix tree).
