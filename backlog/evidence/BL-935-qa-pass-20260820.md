# BL-935 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-935 — cap the vitest fork pool under a live full-forge pack on macOS (`type: feature`, milestone M8)
- **Parcel**: `00_20260819T231010Z_000213_from_documenter_to_QA_for_QA` (from documenter, commit `1df7cb1a35`)
- **Verified at**: `fca6fd430a` (QA merge of the documenter tip). This evidence file is committed on top; the forward names that commit.
- **Reviewed by**: QA, 2026-08-20
- **Prior bounces**: 2, both architect→coder (`e4b327e031` invariant-unencoded, `dfc82c1fdf` behavior). Both predate the hardener pass; neither defect survives (see G14).

## Verdict

**PASS — inventory items: NONE.**

## BL-532 disposition

`qa-sibling-check.js status --ticket BL-935` → exit 0, `VERIFY BL-935`. No open deferral; full verification pass run.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Merge integrity — `git diff 1df7cb1a3 HEAD` after the merge | PASS — empty; the merge dropped nothing of the sender's tip (BL-954 class) |
| G2 | Stage ancestry — coder `428ba6a14`, cleaner, architect `2c1aef0bcd`, hardener merge `aa8965a89` + `0eaefd519`, documenter `f3097b3d7`/`01e0461c7`/`1df7cb1a3` | PASS — every one an ancestor of the cited commit, not merely of HEAD |
| G3 | Prior-bounce check against `main` **and** `origin/main` (BL-891) | PASS — `main` 7 ahead / 0 behind `origin/main`; both read; both recorded bounces are architect-side and pre-hardener |
| G4 | `npm run compile` | PASS — exit 0 |
| G5 | `pre_qa_gate.sh BL-935-… fca6fd430a` | PASS — `OK` |
| G6 | Full unit suite (`npm test`) | RED, fully attributed — see "Suite attribution" below. 7 failed files / 437 passed (444); 14 failed / 7881 passed (7895); 1462s |
| G7 | Unit failures re-run in isolation | PASS — all 7 files, **107/107 tests, zero failures** |
| G8 | Property lane (`npm run test:properties`) | RED, fully attributed — 4 failed files / 112 passed (116); 6 failed / 357 passed (363); 3372s |
| G9 | Property failures re-run in isolation | 3 files still time out (4 failed / 8 passed) — **all timeouts, zero assertion failures** |
| G10 | Property A/B at 3 forks (`SWARMFORGE_PACK=`, i.e. pre-BL-935 behaviour) | PASS as evidence — the same files still time out **without** the cap; `bl868` fails **2** at 3 forks vs **1** at 1 fork. The cap is not the cause |
| G11 | Unhandled Errors in the property lane | PASS — all 9 are exactly `[vitest-worker]: Timeout calling "onTaskUpdate"`, the engineering.prompt BL-871 allowlisted artifact. No other class present |
| G12 | Acceptance — `run_acceptance.sh specs/features/BL-935-vitest-fork-pool-is-capped-under-a-live-full-forge-pack.feature` | PASS — 11/11, 0 fail, exit 0 |
| G13 | `required_wiring` — both named call sites | PASS — `vitest.config.mjs` and `vitest.properties.config.mjs` each call `resolveVitestWorkerPool`; the step handler additionally **spawns both real config files**, so a resolver exported-but-unread (the BL-419 shape) fails acceptance, not merely unit tests |
| G14 | Invariants 1–3 encoded as live properties | PASS — `vitestForkCeiling.property.test.js` carries 8 properties incl. the architect's pass-3 additions over the real composed route (closes both prior bounces) |
| G15 | `qa_e2e_procedure` steps 1–6 | PASS — see table below |
| G16 | Docs currency | PASS — `docs/reference/Specification.MD` documents the ceiling, the precedence rule, the ignored-override half, the single shared route, and states the inherited-vs-wired distinction accurately with BL-961 named as the durability slice |
| G17 | Functional change present (not manifest/format churn) | PASS — resolver module, two configs, property file, step handler, feature file, docs |
| G18 | Orphaned test/mutation processes before and after | PASS in this worktree — none of mine left. See "Foreign processes" below |

## qa_e2e_procedure — measured

| Step | Expected | Observed |
|---|---|---|
| 1 — pack live in a role shell | `full-forge` | **`SWARMFORGE_PACK=full-forge`** in this QA role shell → the cap engages live today. Not BLOCKED BY BL-961 |
| 2 — baseline, unit lane | 3 | 3 |
| 3 — full-forge macOS, unit lane | 1 | 1 |
| 4 — full-forge macOS, property lane | 1 | 1 (and 3 at baseline, so both lanes agree, invariant 3) |
| 5 — `SWARMFORGE_VITEST_MAX_FORKS=99`, pack cleared | 3 (memory clamp binds) | 3 |
| 6 — solo human unaffected | full budget | 3 with the variable cleared |
| extra — override `2` on full-forge | 2 | 2 |
| extra — override `-1` / `abc` on full-forge | 1 (ignored, never floored) | 1, 1 |
| extra — full-forge on `linux` | 3 (platform gate) | 3 |

## Suite attribution — why RED gates G6/G8 are not BL-935 defects

Every one of the 20 failures across both lanes is `Test timed out`. **Zero assertion failures anywhere.** Three independent lines of evidence rule BL-935 out:

1. **Isolation** — all 7 failing unit files pass 107/107 when re-run alone.
2. **Pre-existing** — `extension/.test-durations.jsonl` records `"result":"fail"` for **every** whole-suite run on this host since 2026-08-19T10:28, i.e. before BL-935 reached any branch. This red predates the ticket.
3. **A/B against the cap itself** — re-running the failing property files with `SWARMFORGE_PACK=` (3 forks, the pre-BL-935 count) still times them out, and `bl868` fails *more* tests at 3 forks than at 1. Removing the change does not fix the failures.

Host conditions during verification: load average 90–198 on 2 physical cores, and a **concurrently running foreign vitest run** from another role's worktree overlapped both of my lanes (observed repeatedly: `npm exec vitest run test/notDoneBurndown.test.js test/renderBriefingBurndownCli.test.js`, and later runs). That overlap is itself the exact condition BL-935 exists to reduce.

**Not a BL-935 defect, but worth the coordinator's attention**: the whole-suite red is host-wide and standing. It is not attributable to any ticket currently in flight and no ticket presently owns it.

## Foreign processes (G18 detail)

`pgrep` found no orphaned mutation processes. A live foreign vitest run belonging to another role was present during verification; it was **not** reaped — it is not mine to kill, and it was a live run rather than an orphan. My own stale poll-loop shells were reaped.

## Intent check

The ticket asks for a second, CPU-axis ceiling composed with — never replacing — the BL-422/BL-792 memory budget, engaging only on a full-forge pack on macOS, with an explicit override that can lower but never raise the count, and both vitest lanes resolving through one route.

All four hold, and the last is true *by construction* rather than by parallel maintenance: the cleaner collapsed the two lanes' duplicated composition onto a single `resolveVitestWorkerPool`, and the hardener then pinned the capped VALUE in acceptance scenario 02 (lane agreement alone had gone vacuous once the route was shared) and added the `0`/`-1` Examples rows that close the ignored-override half of the precedence rule.

The human's own words — "ok for an intake to limit vtest to 1 core on full forge on mac" — are satisfied exactly: 1 fork, on that pack, on that platform, and nowhere else.

By QA.
