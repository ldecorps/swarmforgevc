# BL-828 QA bounce 3 — 20260825

**Reviewed tip:** documenter `a81af77cfe` (not merged — tip impure)
**Task:** `BL-828-bubble-collapsed-gesture-model`
**Sibling:** `VERIFY BL-828`
**Prior:** bounce `67b737263b`, bounce2 `b1737f54f5`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main a81af77cfe  # fails
# dels_on_origin=0; tip still behind current main
```

**Commit hash checked:** `a81af77cfe`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = 4abf0e04e briefing due refresh)
paths=20 dels=0
tip rematched onto bounce2 then main advanced again before QA claim
```

**Failure class:** `behavior`

**Expected vs observed:** Tip must have **current** `origin/main` as ancestor
at QA claim time. Observed rematch2 already stale vs newer briefing tip.

**Remediation:** Immediately before `git_handoff` to QA: `git fetch origin
main && git merge-base --is-ancestor origin/main HEAD` must succeed; if not,
`git reset --hard origin/main` + restore BL-828 paths only. Do not hand off
a tip that can lose a briefing race.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
