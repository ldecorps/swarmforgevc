# BL-1589 — coder evidence, 2026-09-16

## The fix

`extension/test/bl1030StopFlagTokenBoundary.property.test.js`'s first test
(`BL-1030/BL-654 invariant 1: a forbidden flag is refused exactly when the
SHELL makes it a token of its own`) drew its kind (real flag / unquoted
look-alike / quoted look-alike) uniformly via the file's own time-seeded
LCG — a Binomial(60, 1/3) that misses the `refusedCount >= 12` floor in
0.77% of runs. `buildAdmissibleDraw` now takes `kind` as a parameter
instead of drawing it, and the test loop derives it by construction:
`kind = i % KINDS.length` (`KINDS = [0, 1, 2]`), the same `i % length`
idiom the file's own compound-command and unreadable-command sweeps
already use below it. Over `DRAWS = 60` this guarantees exactly 20 draws
per kind every run — never sampled, never a lottery.

The existing `refusedCount >= 12` and `lookalikeCount >= 12` assertions
are unchanged at their values. A new `assertReachFloor(kindCoverage,
KINDS, KIND_CELL_RUNS, 'bl1030 kind')` call
(`extension/test/helpers/reachFloors.js`, `KIND_CELL_RUNS =
runsPerCell(DRAWS, KINDS.length)` = 20) additionally names the specific
kind if construction ever stops reaching it, and the test prints one
reach-map line: `BL-1589 reach map (bl1030):
{"kinds":3,"minDrawsPerKind":20}`.

Nothing in `extension/src` or `swarmforge/scripts/expedite_lib.bb`
changed; the draw budget stays 60, the real bash oracle and the real
Babashka guard call are unchanged.

## Green alone, 15 of 15 (`extension/tmp/bl1589-alone.log`)

`for i in $(seq 1 15); do npx vitest run --config
vitest.properties.config.mjs test/bl1030StopFlagTokenBoundary.property.test.js
|| echo FAIL; done` — 15 of 15 green, 0 FAIL lines, every run printing
`BL-1589 reach map (bl1030): {"kinds":3,"minDrawsPerKind":20}`.

## Non-vacuity (break-then-restore, this session)

- **Break 1** (`kind = i % 2`, kind 2 never built): RED, naming the
  reach floor exactly —
  `Error: reach floor: bl1030 kind 2 drawn 0 < 20`.
- **Break 2** (`kind = 1` constant): RED on the pre-existing
  `refusedCount >= 12` assertion (`generator coverage: only 0 of 60
  draws were real flags (floor 12)`) — the earlier-positioned assertion
  fires first, per the ticket's "whichever runs first."
- Restored byte-for-byte: all three tests in the file green again.

## Acceptance

`swarmforge/scripts/run_acceptance.sh
specs/features/BL-1589-bl1030-draw-kind-is-constructed.feature` — 6/6
scenarios pass (scenario 01 x1, scenario 02 x2, scenario 03 x3).

## Register row

The row for `extension/test/bl1030StopFlagTokenBoundary.property.test.js`
(owner BL-1589) is removed from `backlog/standing-reds.tsv` in this same
commit. `bb swarmforge/scripts/standing_red_register_cli.bb .` reports no
row for the file and `"unowned":[]`.
