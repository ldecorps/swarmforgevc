# BL-1227 — QA bounce, 20260828

## D1 — entangled tip: cited commit carries functional/backlog paths for five OTHER, unfinished tickets (BL-506 tip-purity violation)

1. **Failing command, exactly as run:**
   ```
   env -u GIT_DIR -u GIT_WORK_TREE git diff --name-only origin/main...b283239757
   ```
   followed by, for each unexpected path's introducing commit:
   ```
   env -u GIT_DIR -u GIT_WORK_TREE git merge-base --is-ancestor <introducing-commit> origin/main
   ```

2. **Commit hash checked out and tested:** `b283239757` (`b28323975...`, coder's
   `BL-1227: trim boot prefix back under budget and add a live, unpinned
   budget check`), received via `git_handoff` payload `merge_and_process coder
   b283239757`, parent `94808e295` (already on `origin/main`).

3. **First error excerpt** — `git diff --name-only origin/main...b283239757`
   lists 35 changed paths. Of these, the following are **not** BL-1227's own
   scope and belong to five other tickets, none of which is `done`:

   | Path(s) | Ticket | Ticket status at this commit |
   |---|---|---|
   | `backlog/{active,paused}/BL-1192-*.yaml`, `specs/features/BL-1192-*.feature`, `specs/pipeline/steps/bl1192TaskScopeGateSteps.js`, `specs/pipeline/steps/lib/bl1192TaskScopeGateCli.sh`, `swarmforge/scripts/task_scope_gate_lib.bb` (+ its test runner), `swarmforge/scripts/backlog_hygiene_lib.bb` (+ its test runner), `swarmforge/scripts/swarm_handoff.bb` | BL-1192 | `backlog/paused/`, `bounce_count: 1`, architect-bounced 2026-08-28 (`backlog/evidence/BL-1192-architect-bounce-20260828.md`). Introducing commit `1a016cdb0` for the steps file IS already on `origin/main`, but `swarm_handoff.bb`/`task_scope_gate_lib.bb` are the ticket's own not-yet-landed fix content. |
   | `extension/src/tools/recovery-filter-check.ts`, `extension/src/tools/recoveryFilterCliArgs.ts`, `specs/features/BL-1211-quarantine-lift-cannot-restore-reverted-bounce-content.feature`, `specs/pipeline/steps/bl1211QuarantineLiftAuthorshipSteps.js` | BL-1211 | `backlog/active/`, multiple bounce/refix rounds in evidence (`BL-1211-cleaner-bounce`, `-cleaner-refix-verified`, `-architect-bounce`, `-architect-pass`, all 2026-08-28) — not through hardener/documenter/QA yet. |
   | `extension/src/tools/deliver-role-answer.ts` (further modified beyond its already-landed base), `extension/src/tools/telegram-front-desk-bot.ts`, `extension/test/cursorBridgeAgentSession.test.js`, `extension/test/telegramFrontDeskBotCli.test.js` | BL-1201 | `backlog/active/`, `status: todo`. |
   | `specs/pipeline/steps/bl1207AbandonedLockLivenessSteps.js` | BL-1207 | `backlog/active/`, introducing commit `11b8b9af1a0` is **not** an ancestor of `origin/main` — no downstream review evidence found at all. |
   | `specs/pipeline/steps/bl1216DuplicateIdLiveCopyContentVerdictSteps.js` | BL-1216 | `backlog/active/`, introducing commit `54c08de4ada` is **not** an ancestor of `origin/main`; architect raised a duplicate-id finding still awaiting specifier disposition (`backlog/evidence/BL-1216-specifier-disposition-of-architect-duplicate-id-note-20260828.md`). |

   Confirmed by direct ancestry check (not merely "reachable from HEAD"):
   `git merge-base --is-ancestor 11b8b9af1a05e5adef003f413815014fe83bc952
   b283239757` and the equivalent for `54c08de4adaf67f45a7dbb50ae6daa1e3e674d11`
   both return true — these are direct ancestors of the cited BL-1227 commit
   itself, not sibling history pulled in only by my own merge.

   BL-1227's *own* scope — verified correct and complete in isolation — is
   exactly: the eight boot-inlined constitution files + `PIPELINE.md`, their
   four paired `articles/reference/*` files (all pre-trim text confirmed
   verbatim present, see below), `swarmforge/scripts/test/
   boot_prefix_budget_gate_lib_test_runner.bb`'s new live assertion,
   `specs/pipeline/steps/bl1227BootPrefixLiveBudgetCheckSteps.js`, and one new
   line in `specs/pipeline/steps/index.js` registering it. None of that is in
   question.

4. **Failure class:** `behavior` — this is precisely the "entangled tip"
   class BL-1192 itself was minted to catch mechanically (its own
   description: "a handoff naming ticket A cites a commit whose diff vs
   `origin/main` includes functional paths for ticket B... QA's manual BL-506
   inventory rejects them"). BL-1192's pre-handoff gate is not yet landed
   (paused, bounced), so this send hit no automated gate and reached QA's
   manual check, which is the documented current backstop.

5. **Expected vs observed:** Expected — a `git_handoff` naming `BL-1227`
   cites a commit whose diff vs `origin/main` touches only BL-1227's own
   paths. Observed — the cited commit also carries substantial, unreviewed or
   still-disputed work for five other in-flight tickets (one of them,
   BL-1216, has an open, unresolved duplicate-id concern from the architect).
   Landing this commit as BL-1227's approval would ship all five past
   cleaner/architect/hardener/documenter without their own review completing.

## Other checks this pass (run-or-blocked)

- BL-1227's own boot-prefix trim, live budget-check assertion, non-vacuity
  (hand-appended ~2980 chars to `03_backlog.md`, confirmed FAIL at
  44943/44000, restored, confirmed PASS at 41930/44000), the `44000`/`51200`
  literals (unchanged — confirmed via `git diff` over the commit, no touched
  line contains either literal except new test/comment references to the
  existing `44000`), and verbatim preservation of every trimmed passage in
  its paired `articles/reference/*` file (spot-checked across all 8 touched
  articles + `complete-review-inventory-amendment-2026-07-27.md`) — all
  **PASS**, run directly against the isolated coder commit.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1227-boot-prefix-live-budget-check.feature` — **PASS**,
  7/7.
- `npm run compile` (extension) on the entangled tree — **PASS** (sanity
  check only; not meaningful evidence about any of the five foreign
  tickets' own correctness).
- Full unit/property suite and the standing `run_bb_suite.sh` entry point —
  **BLOCKED BY D1**: the tree must be rebuilt tip-pure before a full-suite
  run against it would mean anything for BL-1227 specifically. (Separately,
  and not part of this bounce: `run_bb_suite.sh` currently refuses to run
  *any* standing test — pre-existing, unticketed `suite-manifest.tsv`
  drift unrelated to this commit, reported by `note` to specifier +
  coordinator in the same pass, per Article 4.4's spec-gap-adjacent
  handling.)

## Remediation

Coder: rebuild a tip-pure `BL-1227`-only commit on top of the current
`origin/main` (or the current QA-approved base), containing only the paths
enumerated above as BL-1227's own scope, and re-send. The substance does not
need rework — only the branch needs to stop carrying BL-1192/1201/1207/1211/
1216's unfinished work.
