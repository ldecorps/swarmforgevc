# BL-1279 QA pass — 2026-08-30

Commit verified: `4c427e63a` (Merge documenter BL-1279 9a24a32093 into QA).
BL-1279's own coder/cleaner/hardener/documenter merges (501dedf09, 27c73385a,
5552a5e92, 9a24a3209) are all confirmed ancestors of this commit
(`git merge-base --is-ancestor`).

## required_wiring — all 6 confirmed present
- `copy_bb_closure` call present in all four fixtures (bl622_refusal, tick,
  liveness, fleet_creds).
- All four enrolled in `bbFixtureClosureGate.js`'s `FIXTURES` map.
- `specs/pipeline/steps/index.js` registers `bl1279FrontDeskFixtureClosureSteps`.

## Direct fixture runs
All four `test_front_desk_supervisor_*.sh` fixtures run standalone: exit 0,
ALL CHECKS PASSED (first pass, clean). See "Known pre-existing flake" below
for a caveat on `..._liveness.sh` under repeated runs.

## Property suite
- `test/bl1279FrontDeskFixtureClosure.property.test.js`: 2/2 pass
  (invariant 1: derives-not-hand-lists; invariant 2: no vacuous pass on a
  dead load). Re-run twice, deterministic.
- Full `npm run test:properties`: 245/271 files pass. The 26 failures are
  ALL attributable to the already-ticketed `checkOrphanedAuthoredDocs`
  missing-deps-stub defect (BL-1229, paused; BL-1221 related) — confirmed by
  matching the `deps.checkOrphanedAuthoredDocs is not a function` signature
  and the affected file list against BL-1229's own table. Not caused by, or
  related to, BL-1279's diff (`pilotAcceptanceGate.ts` untouched by this
  ticket). Not re-reported.

## Acceptance
- `specs/features/BL-1279-front-desk-fixtures-derive-their-bb-closure.feature`:
  9/10 pass. The one red (subtest 3, `test_front_desk_supervisor_liveness.sh`,
  "the bot transitions to stalled") reproduced intermittently on a bare rerun
  too (1 fail in 3 standalone reruns, a DIFFERENT check failing each time) —
  see "Known pre-existing flake" below.
- `specs/features/BL-973-copy-lists-closure-derived-and-suite-completeness.feature`
  (re-run per qa_e2e_procedure step 5, unowned by this ticket): 12/13 pass.
  The one red is BL-973's own suite-inventory scenario, and matches exactly
  what the coder's commit message (501dedf09) already flagged: it fails on
  the manifest row BL-1276 added, not on anything BL-1279 touched.

## Two pre-existing defects found, neither introduced by this ticket
Both confirmed absent from `backlog/active`, `backlog/paused`, `backlog/hold`
by grep before reporting (per the BL-1063 "grep before calling it untracked"
rule). Reported to specifier by separate `note`, not folded into this
approval or bounced against BL-1279:

1. **`run_bb_suite.sh` inventory gate is down for the whole standing suite.**
   `suite-manifest.tsv` row `task_scope_gate_acceptance_exemption_property_runner.bb`
   (added by BL-1276, commit `d6d65df53`/`9706a8b9f`, already landed and
   done) ends in `_property_runner.bb`, not the `_test_runner.bb`/`test_*.sh`
   pattern `suite_inventory_lib.bb`'s naming check requires — the only
   manifest row with this shape. `run_bb_suite.sh` runs the inventory gate
   unconditionally first, so this blocks the ENTIRE standing suite runner
   for every future ticket, not just this one. The coder's own commit
   message (501dedf09) already surfaced this to the specifier; it had not
   yet reached a durable ticket as of this QA pass.
2. **`test_front_desk_supervisor_liveness.sh` has a real-clock timing flake
   in its stall-detection checks**, unrelated to BL-1279's diff (confirmed:
   `git show 501dedf09` touches only the `make_fixture` copy/load-guard
   lines; the check bodies and their `sleep`-cushion timing, e.g. line 65's
   `stall_ms + 300ms` fixed cushion, are byte-identical to before). Because
   this fixture died at load 100% of the time before BL-1279's fix, this
   flake was structurally unobservable until now — it is newly EXPOSED, not
   newly introduced. Reproduced twice: once inside the acceptance run
   (different check failed — "the bot transitions to stalled") and once on
   a bare rerun (yet another set of checks failed — liveness-01, the bridge
   check, and the stall-logged check). Different checks failing each time is
   consistent with a scheduling race against the fixed sleep cushion, not a
   logic defect.

## Verdict: APPROVE
BL-1279's own declared scope (closure derivation, non-vacuous load guard,
guard enrolment) is correct, deterministic, and fully wired. Neither found
defect lives in code this ticket wrote, and BL-973's own suite is otherwise
unaffected.

By QA.
