# BL-1411 documenter review pass — 2026-09-05

Reviewed the hardened commit (`a827d28272`) and the coder/cleaner/architect/
hardener trail for BL-1411 (a forward built on an amended contract is
refused).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1411-a-forward-built-on-an-amended-contract-is-refused.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1411ContractFreshnessGateSteps.js`; the
  `swarm_handoff.bb::contract-freshness-gate-lib/refusal-message` wiring
  required by the first line is present (`swarmforge/scripts/swarm_handoff.bb`
  lines ~452-468 call `findings-for-git-handoff` and gate on `blocked?`,
  and line ~532 calls `refusal-message` in the same `cond->` shape as
  `task-scope-gate-lib/refusal-message`).
- Read `swarmforge/scripts/contract_freshness_gate_lib.bb`: confirmed
  invariant 1 (comparison is always `base..R` on the declared acceptance
  path, never the parcel tip — `path-differs?`/`amending-commits` both
  take `base` and `ref`, never the sender's own commit as anything but
  the merge-base input), invariant 2 (the path comes from
  `task-scope-gate-lib/declared-acceptance-path`, `grep -c
  'read-yaml-field\|acceptance:'` style self-check the ticket names — no
  second reader), and invariant 3 (`decide-for-ref`'s `cond` returns
  `:not-evaluated` for every unreadable case and only `:refuse` for a
  confirmed diff).
- Agent-visible behavior change (a `git_handoff` whose ticket's acceptance
  feature file was amended on `main`/`origin/main` since the sender's own
  merge-base is now refused at send time, unqueued, naming the amending
  commit(s) and the remedy) is documented in
  `swarmforge/handoff-protocol.md` — the exhaustive send-time-gates
  reference (Article 2.2's own citation) — as a new "Contract Freshness
  Gate (BL-1411)" section immediately after the sibling "Pre-Handoff
  Task-Scope Gate (BL-1192)" section, matching that section's own
  mechanics/refusal-message documentation shape (same treatment as
  BL-953/BL-760, no dedicated how-to doc).
- Checked `swarmforge/constitution/articles/02_handoffs.md` (the inlined
  constitution article): it does not enumerate individual send-time gates
  by name anywhere (BL-1192, BL-953, BL-760 are absent too) — the
  exhaustive-reference split is deliberate per Article 2.2's own text, so
  no edit needed there.
- No "Last Updated" freshness field in `handoff-protocol.md` — no date
  bump owed.
- `docs/index.md` does not index `swarmforge/handoff-protocol.md` (a
  swarmforge/-tree reference file, not a docs/ Divio page) — no edit
  needed there.
- No registered diagram depicts individual send-time gate mechanics (this
  adds one more gate to an existing validation chain, not a new
  component, boundary, or pipeline-topology change) — no diagram change
  needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
