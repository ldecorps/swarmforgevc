# BL-1739 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Reviewing the coder's QA-unit-bounce fix (3c485ff1ca): exports
`isLiveCodePath` from `bl611BabysitterdLifecycleSteps.js`, rewrites
BL-781's property test invariant 3 to assert each `DELETED_WAKE` path
via `isLiveCodePath` directly, and fixes `loadBl611Scan`'s stale slice
marker and stale `scan.offenders` field name (→ `scan.liveCodeMatches`)
in the BL-781 acceptance step handler — same rename BL-1739 made
earlier; QA caught two readers the earlier rebuild missed. No production
code touched (`isLiveCodePath`'s own body is unchanged, only its
export). mutation_cost: low.

Checks run:
- `npx vitest run --config vitest.properties.config.mjs
  test/bl781LiveGrepOffender.property.test.js`: 5/5.
- `node specs/pipeline/cli.js
  specs/features/BL-781-retire-dead-babysitter-files-keep-list-preserved.feature`:
  12/12.
- `node specs/pipeline/cli.js
  specs/features/BL-611-deterministic-babysitterd-managed-by-swarm-lifecycle.feature`:
  27/27.
- `npm test` (full unit suite, `extension/`): 637/637 files, 10868/10868
  tests. Seven registered poles and six watch files reported over their
  per-file budget — all pre-existing, none touched by this parcel's
  diff, guard exits 0 ("suite file budget OK").
- Every standing whole-tree guard (this parcel touches
  `extension/test/`): `npx vitest run $(ls test/*Guard*.test.js | grep
  -v '\.property\.')` — 22 files, 211 tests, all green.

Independent non-vacuity, checked directly, on the two hand-authored
fixes in `bl781RetireDeadBabysitterFilesKeepListPreservedSteps.js`
(distinct from the coder's and architect's own `isLiveCodePath` mutant —
both already independently confirmed that one):
- Reverted `loadBl611Scan`'s slice-marker fix (`const RETIRED_FILE_PATHS`
  → back to the deleted `function isAllowedBabysitterMatch`): the BL-781
  feature failed as expected (11 pass, 1 fail — `scanRepoForBabysitter is
  not defined`, the exact QA-bounce symptom). Restored (`git status
  --short` clean on the file), re-ran: 12/12.
- Reverted the field-rename fix (`scan.liveCodeMatches` → back to
  `scan.offenders`): the same feature failed the same way (11 pass, 1
  fail). Restored, re-ran: 12/12.

Not run: CRAP/DRY (no `src`/`out` file in Scope — `isLiveCodePath` lives
in `specs/pipeline/steps/`, outside CRAP's `extension/src/*.ts` scope,
and Stryker's `--mutate out/**/*.js` scope likewise doesn't reach it),
`npm run test:properties` (2026-09-17 rule — only ran the one touched
property file directly, per the same rule's own guidance).

Fixture hygiene: `git status --short` clean of anything this pass's
runs could leak (pre-existing untracked BL-1652 scratch files predate
this session, not mine to remove — surfaced, not swept).

By hardender.
