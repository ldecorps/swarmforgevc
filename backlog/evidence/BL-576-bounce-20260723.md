# BL-576 — QA BOUNCE (2026-07-23)

**Verdict:** BOUNCED at the QA gate. Bounced to **coder**, priority `00`.
**Class:** behavior (docs deliverable does not match the ticket's own `docs:`
requirement, nor the architect's F4 finding that was explicitly assigned to
the documenter stage).

All code-level gates pass cleanly — unit (`mono_router_lib_test_runner.bb`),
the new wiring test (`test_handoffd_aged_note_rotate_wiring.sh`), the 24/24
acceptance suite, and the wider unit/property suites (see below). This bounce
is scoped ONLY to two missed documentation items; nothing else in the parcel
is in question.

## 1. Failing command / observed vs expected

**Command:**
```
grep -rn "Broadcast notes never qualify" swarmforge/
```
**Commit tested:** `c216b05f15` (documenter's forward), merged into QA's
worktree at `c7f78e903`.

**Observed:**
```
swarmforge/scripts/handoffd.bb:1013:   with the newest actionable mail. Broadcast notes never qualify."
```

**Expected:** the architect's `BL-576-architect-review-20260723.md` (F4)
named this exact docstring by name:

> **F4 — for the documenter.** `preferred-mono-rotate-role`'s docstring still
> reads "Broadcast notes never qualify." — now false, aged notes do qualify.

The hardener's own evidence file confirms this was handed to the documenter,
not closed by an earlier stage:

> **F4/F5 — not this role's** F4 (docstring + runbook) is the documenter's

The documenter's commit (`c216b05f15`) touched only
`docs/how-to/BL-576-aged-note-actionability-mono-router.md`, `docs/index.md`,
and `swarmforge/PIPELINE.md` — it never touched `handoffd.bb`, so the
docstring the architect flagged is unchanged and still asserts the opposite
of what this ticket ships: aged broadcast notes now DO qualify for rotation
(that is the entire point of BL-576), but `preferred-mono-rotate-role`'s own
docstring — a function inside the very file this ticket modified — tells the
next reader they never do.

(For contrast, the docstring on the *new* function `actionable-mail?` in
`mono_router_lib.bb`, written by the coder as part of the original
implementation, is correct: "a note that has aged past the BL-576 threshold"
/ "Fresh notes never qualify — that broadcast-thrash protection is
unchanged." Only the pre-existing `preferred-mono-rotate-role` docstring in
`handoffd.bb` was missed.)

## 2. Second gap — same class, same root cause

**Command:**
```
grep -n "note_actionable_after_ms\|aged-note\|note-aged" swarmforge/handoff-protocol.md
```
**Observed:** no output — zero matches.

**Expected:** the ticket's own `docs:` field states plainly:

> documenter: ... the mono-router rotation description wherever
> `rotation_home` is already documented
> (swarmforge/handoff-protocol.md, swarmforge/PIPELINE.md) gains the
> aged-note rotate trigger.

`swarmforge/PIPELINE.md` was updated (confirmed: mentions
`note_actionable_after_ms` and the aged-note rotate trigger).
`swarmforge/handoff-protocol.md` — named in the same sentence, same
requirement — was not touched at all.

## Why this is a QA-level bounce and not a note

Article 4.2 (Merge Criteria) requires "Documentation updated" as a gate
condition, and this project's `docs:` ticket field is exactly how that
requirement is scoped per-ticket. Both misses are on items the review chain
already surfaced by name (architect F4) or the ticket spec already named
explicitly (`handoff-protocol.md`) — this is not a new requirement QA is
inventing, only confirming it was not actually done before forwarding past
the last gate. A docstring that asserts the opposite of the shipped behavior,
left in the file this very ticket edits, is exactly the kind of stale-doc
trap this project's own history flags as a recurring source of chase/rotate
confusion.

## Remediation (coder, or forward to documenter as appropriate)

1. Fix `preferred-mono-rotate-role`'s docstring in
   `swarmforge/scripts/handoffd.bb` (~line 1013) to state that a note aged
   past `note_actionable_after_ms` now qualifies, matching
   `actionable-mail?`'s already-correct docstring.
2. Add the aged-note rotate trigger description to
   `swarmforge/handoff-protocol.md`, alongside the existing `rotation_home`
   documentation, per the ticket's own `docs:` field.
3. Verify lineage before forwarding:
   `git merge-base --is-ancestor c216b05f15 <your-new-commit>` must succeed.
4. Forward to `architect` (or wherever this pack's convention re-enters the
   chain) under task name `BL-576`.

## Bounce hygiene

This evidence commit is being added directly to QA's branch and merged
nowhere else; the bounced commit `c216b05f15` remains merged into
`swarmforge-QA` only as a merge ancestor of QA's own evidence commit, which
is expected (QA always merges before reviewing, per
`merge_and_process`) and is not itself being forwarded anywhere.

By QA.
