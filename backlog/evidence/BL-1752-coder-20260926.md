# BL-1752 — coder pass — 20260926

## Invariants (stated reason, no `*.property.test.js`)

Both declared invariants quantify over `swarmforge/scripts/handoffd.bb`'s
`attempt-resident-rotate!` (babashka) driving real tmux commands - a
stateful daemon function integrating with an external process, not a pure
module a JS property test can fuzz in-process. Babashka/bash carry no
mutation/CRAP/DRY or property-test wiring in this repo (engineering.prompt
"Startup Tools"); the equivalent gate here is the real, non-mocked shell
fixture this parcel's own commit extends.

- Invariant 1 ("the chase sweep creates no tmux session for any role,
  whatever mail is waiting and whatever state the resident is in"):
  `swarmforge/scripts/test/test_chase_departing_mid_parcel_gate.sh` cases
  08a/08b/08c (departing-mid-parcel refusal, three mail shapes), 09
  (ordinary rotate - respawn-pane, never new-session) and 10 (retired
  knob present) all assert no `new-session` line ever appears in the fake
  tmux's own call log.
- Invariant 2 ("every chase gate outcome other than the removed spawn ...
  decides and logs exactly as before"): cases 01a-07 are the PRE-EXISTING
  BL-1535 cases, unmodified, still green against the parcel's own commit -
  the removed spawn call was reached only from the `:departing-mid-parcel`
  branch and touched no other gate's decision or logging.

## Non-vacuity

Ran the extended shell script against the pre-fix tree (spawn call still
present): cases 08a-c failed on a real `new-session ... -s
swarmforge-specifier ...` line in the tmux log - the fixture's `has-session`
stub needed a `SESSION_MISSING` seam first (an unconditional `exit 0`
made `session-exists?` always true, so the create path was never actually
reached until fixed). Confirmed red, then green after the removal.

## QA e2e procedure (all four checks run)

1. `node specs/pipeline/cli.js` on the feature: 5/5.
2. `bb .../mono_router_lib_test_runner.bb`, `bash
   .../test_chase_departing_mid_parcel_gate.sh`,
   `bash .../test_consult_spawn_cli.sh`: all green.
3. `git grep -e spawn-consult-session -e consult-eligible -e
   single_inference_slot -e single-inference-slot -- swarmforge/scripts
   swarmforge/packs extension/src`: no hit outside `swarmforge/scripts/test/`
   (two stale comment references fixed: handoffd.bb, and
   extension/src/tools/night-closing-ceremony-run.ts - the latter's
   `spawnConsultDocumenter` itself is untouched, on-demand only via
   consult_spawn_cli.bb, never the removed automatic path).
4. `git diff main...HEAD --name-only`: Scope files, tests, docs, evidence
   only (see commit).

By coder.
