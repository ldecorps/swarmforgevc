# BL-975 — the owed BL-967 soak window is unrecoverable; verdict BLOCKED

QA, 2026-08-22. Attempted the qa_e2e_procedure as written. Two independent
findings block a qa_e2e-conformant verdict; neither is a code defect and
neither has an owning pipeline role to bounce to (`required_stages: [qa]`).

## Finding 1 — both required logs have rotated past the target window

Target window per the ticket's corrected facts: first post-fix handoffd
start after local main took `a5ecfc281b` (~2026-08-20T05:56Z), 30 minutes
active from there.

Checked at `/home/carillon/swarmforgevc/.swarmforge/daemon/` (the master
checkout's live state — confirmed this is the right path, not the
unrelated `/home/carillon/.swarmforge/` which belongs to a different tool):

- `daemon-start-audit.log` (1906 lines): date coverage is 2026-07-09 through
  2026-08-01, then a **gap**, then 2026-08-21 onward. **Zero lines for
  2026-08-20.**
- `freshness-incidents.log` (103 lines, `epoch=` format): earliest entry is
  epoch 1787299321 = **2026-08-21T08:02:01Z** — a full day after the target
  window (epoch range needed: 1787205360–1787207400+, 2026-08-20T05:56–06:26Z).

No rotated/backup copy exists anywhere on the filesystem (searched). Both
paths are gitignored (`.swarmforge/` line 4 of `.gitignore`), so there is no
git-recoverable copy either. This is very plausibly downstream of the
macOS→WSL2/Linux host move BL-1058 documents happening around 2026-08-22,
compounded by ordinary rotation — the mechanism doesn't change the
conclusion: **the raw window is gone.**

## Surviving secondary evidence

`backlog/evidence/BL-967-soak-observation-20260820.md` (coordinator,
2026-08-20 ~06:45Z, git-committed, explicitly captured "so a planned
restart does not erase the evidence") is the only surviving record:

- handoffd pid 57343, uptime 43m54s at ~06:45Z → start ≈ 06:01Z. This is a
  **derived approximation from a `ps` uptime sample**, not a value read from
  `daemon-start-audit.log` itself — it cannot satisfy "by start-audit
  timestamp" (scenario 01) as a primary source.
- completed heartbeat cycles 341 → 481 across ~8 min of direct observation:
  healthy, growing, log actively writing.
- `freshness-incidents.log`: 661 (baseline, 05:57Z, PRE-fix daemon 96980
  still running) → 662 at ~06:45Z. **One new entry**, unattributed. The
  observation file's own author flagged this as needing to be "read and
  dated" to determine whether it predates 57343's start (pre-fix tail,
  excluded per the ticket's own rule) or postdates it (a genuine step-3
  FAIL). **That raw line no longer exists anywhere I can find** — I cannot
  resolve the ambiguity its own author left open.

Reading the surrounding shape (57343 ran 44+ uninterrupted minutes with
growing cycles and an actively-writing log — behaviour inconsistent with a
mid-window stall/escalate event) leans toward the one entry being the
pre-fix tail, not a post-fix incident. That is a read of the shape, not a
dated fact, and does not meet the bar the acceptance scenarios set.

## Finding 2 — the feature file has no step handler; scenarios 01-02 cannot run

`required_stages: [qa]` means no coder stage ever touched this ticket, so
nobody was tasked with writing a Gherkin step handler for
`specs/features/BL-975-bl967-owed-live-soak-recorded.feature`. Confirmed:

- No `specs/pipeline/steps/bl975*.js` file exists.
- `specs/pipeline/steps/index.js` has no `bl975` registration.

Per `specs/pipeline/runtime.js`'s own behaviour (throws on any scenario with
no handler — see BL-1063's `required_wiring` note for the same mechanism),
running `run_acceptance.sh` against this feature file would throw rather
than validate any evidence file's content, regardless of what that content
says. This is independent of Finding 1: even perfect evidence cannot be
mechanically gated as the ticket's own `qa_e2e_procedure` step 6 assumes.

## Verdict: BLOCKED (not PASS, not FAIL)

I am not recording PASS: the acceptance scenarios' own evidentiary bar
("first daemon start... by start-audit timestamp", a dated resolution of
the one ambiguous incident) is not met by data that no longer exists, and I
will not fabricate or round an approximation into a primary-source claim it
isn't ("a green suite is not evidence" applies equally to a green-looking
secondary-evidence read). I am not recording FAIL either: nothing found
indicates a genuine post-fix stall, and the shape of the surviving evidence
(44+ min continuous healthy uptime) argues against one.

## Requested from the specifier (both are spec gaps, routed by `note`, not a parcel)

1. Disposition of Finding 1: either accept the secondary evidence
   (57343 continuous run + cycle growth + the pre-fix-tail read of the one
   incident) as sufficient for a PASS despite not meeting scenario 01's
   literal "by start-audit timestamp" sourcing, or close this ticket with an
   explicit "undeterminable — log retention window passed before QA could
   run this" resolution. A fresh soak run today would observe a real
   post-fix daemon (the fix has been on `main` since 2026-08-20), but that
   is not "BL-967's owed 2026-08-20 window" — it is a different, later
   observation; specifier's call whether that satisfies this chore's intent.
2. Finding 2 is `required_wiring` nobody was ever assigned to build under
   `required_stages: [qa]`. If this ticket (or its replacement) is to be
   acceptance-gated at all, a step handler needs an owner — which
   structurally means either adding a coder stage or the specifier
   authoring the (trivial, log-reading) handler directly.

Not bounced as a parcel: `required_stages: [qa]` leaves no owning role to
route a `git_handoff` to, and both findings are spec gaps per Article 4.4's
routing table, not implementation defects.
