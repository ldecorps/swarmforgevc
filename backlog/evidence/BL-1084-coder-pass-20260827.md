# BL-1084 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Context

Re-promoted after coordinator route `6670eb484`. Implementation landed in
`a8a794e40` (supersede pre-turn guard in `ready_for_next.bb` beside BL-640;
`supersede_lib.bb`; shell/acceptance/property coverage). This pass verifies
the parcel on the re-promotion path — no behavior change required.

## Wiring (required_wiring)

`enforce-supersede-guard!` runs in `ready_for_next.bb` before dispatch chooses
task vs batch — same posture as BL-640 freshness guard (Article 2.4).

## Declared invariants (BL-654)

| invariant | encoding |
|---|---|
| Durable, stage-independent supersede | `bl1084_supersede_property_runner.bb` — 500 runs ALL HOLD |
| Absent passes; unreadable refuses | same runner + scenario 05 in acceptance |

Non-vacuity: property runner derives store states and candidate task sets per
run; unreadable store and recorded supersede both refuse with distinct kinds.

## Verification

| check | result |
|---|---|
| `supersede_lib_test_runner.bb` | ALL PASS |
| `test_supersede_guard.sh` | ALL PASS (6 markers) |
| `bl1084_supersede_property_runner.bb` | ALL PROPERTIES HOLD (500 runs) |
| `run_acceptance.sh BL-1084…feature` | **9/9** |

Babashka degraded fallback (BL-472): no mutation, CRAP or DRY for `.bb`/shell —
unit, property, and acceptance coverage only.

By coder.
