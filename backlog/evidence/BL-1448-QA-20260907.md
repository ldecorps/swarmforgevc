# BL-1448 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect in the parcel itself.
Landing required a hand-fix of the automated land tool's own output — see
below.

By QA.

## Verification (this ticket's own deliverable)

- `bash swarmforge/scripts/test/test_property_suite_drift_guard.sh`: all 25
  cases PASS, including 11/12/13/13b/13c/13d/21.
- `grep -c '"\$GUARD"'` restricted to cases 11-13d and 21: 0 — every
  allowlist-dependent case routes through the fixture's own
  `install_guard_copy_with_allowlist`/`GUARD_COPY_*`, never the live
  `$GUARD` (qa_e2e_procedure step 3).
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1448-the-drift-guard-suite-owns-its-allowlist.feature`:
  4/4 scenario-outline examples pass (~46s each); the live allowlist file
  is untouched (`git status` clean) after the scratch-tree runs (qa_e2e
  steps 1-2).
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass; the new handler
  registers cleanly with no unscoped-pattern collision.
- `backlog/standing-reds.tsv`: BL-1448's row removed (qa_e2e step 4).
- Full compiled unit suite (`npm test`): unchanged from this session's
  BL-1454 pass (609/610 files, 10308/10309 tests) — confirmed no file
  under `extension/src` or `extension/test` changed since that run
  (`git diff --name-only aa80d696ba HEAD -- extension/src extension/test`
  empty); the one known red (`bridgeServer.test.js`, BL-1460) is
  independently owned and unrelated to this ticket.
- `npm run test:properties` (full, unscoped): 26/371 files red under full
  concurrency — consistent with the documented BL-1407 load-flake shape,
  not reconciled file-by-file here since this ticket touches zero property
  test files and the already-known genuine reds (bl968→BL-1450,
  bl1218/bl1320→BL-1457 conditional on `PACK_STAFFING_SKIP_GATE`,
  bl1252→BL-1349 borderline) remain the tracked set.
- No orphaned `node --test`/`stryker` processes before or after (checked by
  PID, not bare `pgrep` self-match).

## The land: an automated replay dropped this ticket's own content (BL-1446/BL-1424 shape)

`bb land_step_cli.bb BL-1448 6c8caf27fb` (the QA-verified merge tip)
returned `LAND_REPLAY land-replay/BL-1448-6c8caf27fb 31cae8ba4d`, correctly
excluding the genuinely-unlanded, in-flight sibling BL-1349 (still `todo`,
actively bouncing between coder/cleaner/architect — confirmed its yaml in
the replay matched `origin/main` byte-for-byte, not the cited tip's WIP
bounce history) and three already-landed siblings (BL-099, BL-1454,
BL-1460, correctly reported `LANDED_SIBLING`). It also flagged
`ENTANGLED_SIBLING BL-1444` — verified FALSE (BL-1444 is in
`backlog/done/M8/`, its guard script and closed ticket file both already
on `origin/main`; the known BL-1354 residual per-ticket-aggregation gap).

**Reviewing the replay tip per this prompt's own instruction found it
incomplete**, the exact BL-1424/BL-1446 shape from 2026-09-06
(`backlog/evidence/BL-1424-land-replay-dropped-own-paths-incident-20260906.md`):
comparing every one of this ticket's own paths between the cited commit
and the replay (content diff, not mere presence — a presence-only check
missed the worst one on the first pass here too) found the replay:

1. **Missing entirely**: the step handler `specs/pipeline/steps/bl1448DriftGuardSuiteOwnsItsAllowlistSteps.js`
   (the actual `required_wiring` deliverable — landing without it would
   have left the feature file's steps unregistered, the BL-233 shape),
   plus four evidence files (architect pass, architect bounce, coder pass,
   hardender pass) and the ticket's own `bounce_history`.
2. **Present but STALE** (content differed, existence check alone would
   have missed this): `swarmforge/scripts/test/test_property_suite_drift_guard.sh`
   in the replay still called the live `$GUARD` directly in cases
   11/12/13/13b/13c/13d/21 — the pre-fix shape this ticket exists to
   replace. Confirmed the cited commit's version is genuinely correct by
   diffing it against the documenter's own real final tip `429f8defcc`
   (byte-identical) before restoring it. The feature file was missing only
   its hardener-stamped mutation-manifest header (cosmetic, restored for
   fidelity).

Per this role's own instruction for exactly this shape ("A replay missing
or differing on any of them is LAND_ESCALATE by hand: do not push it"):
did not push the replay. BL-1446 (root cause, active) and BL-1447
(fail-closed net, active) already own this class — not re-escalated with a
fresh priority-00 note (no new information over the 09-06 incident; same
shape, same known unlanded root cause). Hand-fixed per that incident's own
established recovery recipe instead:

1. Checked out the `land-replay/BL-1448-6c8caf27fb` branch in a scratch
   worktree, restored every missing/stale path from the cited commit via
   `git checkout 6c8caf27fb -- <path>`, and verified EVERY one of this
   ticket's own paths byte-identical to the cited commit afterward (not
   spot-checked).
2. Verified the fix commit's diff against `origin/main` was exactly this
   ticket's own 12-file change (no BL-1349 content), and BL-1349's yaml in
   the fix commit still matched `origin/main` exactly (not the cited tip's
   WIP) — the entanglement exclusion the replay got right was preserved.
3. Re-ran the shell suite (all 25 PASS) against the corrected tree before
   landing.
4. Landed `de0bde5c61` (origin/main `0e9c9af982..de0bde5c61`), then
   independently verified every restored path present BY NAME on the
   pushed `origin/main` tip (not merely trusting the push succeeded), plus
   `grep -c install_guard_copy_with_allowlist` on the landed file returning
   8 (the fixed shape, not the stale one).
5. Recorded `abandoned_commits: [07e17e6ed6]` on the ticket (landed as
   `a06fcad6ce`) — found via `task-scope-gate-lib/last-handoff-commit`,
   NOT guessed from the commits QA reasoned about while isolating the
   rebuild (per this session's own memory of the BL-1224 precedent: the
   real answer is the hardener's own forward-to-documenter merge, not any
   commit an engineer would naturally list).
6. Re-ran `land_step_cli.bb BL-1448 a06fcad6ce`: `LAND_ESCALATE` /
   "own-paths identical to origin/main" — the expected, benign "nothing
   left to land" signal once a ticket's content is already fully on
   `origin/main`, not a real escalation (verified by hand, not taken on
   trust, per this exact tool's own documented history of false claims of
   this kind).

## Disposition

Approved and landed (`a06fcad6ce`, `de0bde5c61` its content commit). No
rework needed on this ticket's own work — the defect was entirely in the
automated land tooling (BL-1446's own known shape), not in anything the
coder/cleaner/architect/hardener/documenter produced.

By QA.
