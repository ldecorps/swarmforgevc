# BL-1427 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed `bb_load_analyse_driver.bb` closely: it reads the target's forms
  one at a time with a `LineNumberingPushbackReader` and evals every form
  EXCEPT one whose head is `-main` (bare or via `apply`) — read, never run
  (a reader error in it still refuses); `*file*` is bound to the target's
  own path for the duration (many scripts compute their own location via
  `*file*` outside `-main`); the target path travels through an env var
  (`BB_LOAD_ANALYSE_TARGET`), never a positional `*command-line-args*`
  entry, which the driver's own comment correctly explains was necessary
  to avoid leaking into `post_commit_push.bb`'s own top-level
  `*command-line-args*` read (the coder's own found-and-fixed
  StackOverflowError). The driver dogfoods its own convention (wrapping
  its real work in `-main`, skipped on self-analysis) rather than working
  around it.
- Confirmed both ticket-named files now pass through the real driver
  directly: `check_swarm_detached.bb` and
  `clear_identical_untracked_and_merge.bb` — previously refused with
  `ArityException`, now exit 0.
- Ran `check_bb_scripts_load.sh --all` against the real 293-file tree:
  exactly ONE remaining failure, `post_qa_branch_sweep.bb` — confirmed
  this is BL-1426's own scope (an unreadable parse error), explicitly out
  of this ticket's stated scope, and not something this parcel should fix.
- Verified `</dev/null` required_wiring anchor present on both the probe
  invocation and the `boot_handoffd` boot step (`grep -n "</dev/null"`).
- Ran the real regression suites for every one of the four
  newly-discovered, previously-hidden defects this parcel also fixed
  (`model_factory_lib.bb`, `model_steward_evaluate_lib.bb`,
  `pack_staffing_gate_lib.bb`, `post_commit_push.bb`) — all pass, confirms
  the defensive self-loads and the CLI-arg fallback are harmless for every
  real caller.
- Ran `test_run_commit_guards.sh` (12/12) and `test_bl1395_bb_scripts_load.sh`
  (BL-1395's own regression suite, ALL PASS) — confirms the guard's name,
  position in the commit-guard chain, and pre-existing behavior for
  reader errors/undefined symbols are all unchanged.
- No duplication: `jscpd` (which parses the `.sh` file) reports 0 clones.
- Babashka/shell have no mutation/CRAP/DRY tooling wired (BL-472 deferred)
  — this pass is gated by the unit/property/acceptance suites only, per
  constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `BB_LOAD_ANALYSE_TARGET=check_swarm_detached.bb bb bb_load_analyse_driver.bb </dev/null` | exit 0 (was ArityException) |
| `BB_LOAD_ANALYSE_TARGET=clear_identical_untracked_and_merge.bb bb bb_load_analyse_driver.bb </dev/null` | exit 0 (was ArityException) |
| `bash check_bb_scripts_load.sh --all` (real 293-file tree) | 1 failure: `post_qa_branch_sweep.bb` only (BL-1426's scope) |
| `node specs/pipeline/cli.js specs/features/BL-1427-...feature` | 6/6 scenario runs |
| `npx vitest run --config vitest.properties.config.mjs test/bl1427...Invariants.property.test.js test/bl1395BbScriptsLoadGuard.property.test.js` | 3/3, 3/3 |
| `bash swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh` (regression) | ALL PASS |
| `bash swarmforge/scripts/test/test_run_commit_guards.sh` (regression) | 12/12 PASS |
| `test_model_steward_cli.sh`, `test_model_factory_cli.sh`, `test_bl1390_post_commit_push.sh`, `pack_staffing_gate_lib_test_runner.bb` (regression for the 4 newly-fixed files) | ALL PASS |
| `grep -n "</dev/null" check_bb_scripts_load.sh` | present on probe + boot step (required_wiring) |
| `bl1427LoadGuardCoversEveryScriptSteps.js::registerSteps` exists | yes (required_wiring) |

No fix required. Forwarding unchanged to architect.
