# BL-1654 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

### Gates run

- `npm run compile`: clean.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1308-an-unlanded-sibling-on-a-second-parent-is-invisible.feature`:
  5/5 scenarios green (scenario 03 retired, replacement scenario green, scenarios 01/02/04 untouched
  and still green).
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1654 <commit>`: OK (required_wiring anchor present).
- `npm test` (extension unit lane): 635/635 files, 10815/10815 tests green.
- `npm run test:properties`: 429/429 files green, 1253/1253 tests green (one `[vitest-worker]:
  Timeout calling "onTaskUpdate"` unhandled error, the BL-871 allowlisted benign error, not a defect).

### Lineage / attribution

- `git merge-base --is-ancestor 6398518814 <QA HEAD>` (documenter merge) → ancestor: OK.
- Full BL-1654 commit chain (mint → freshness-hold adjudication → coder → self-audit fix → evidence
  → cleaner → architect → hardener (BL-113 stamp refresh, self-caught) → documenter) verified present
  by `git log --grep`.
- Standing-red register: `backlog/standing-reds.tsv` named BL-1654 as owner of the `acceptance` lane
  row for `specs/features/BL-1308-an-unlanded-sibling-on-a-second-parent-is-invisible.feature` —
  confirmed fixed (5/5 green above, no scenario titled the retired one) before approving.

### Landing — BLOCKED, not a defect in this ticket

`land_step_cli.bb BL-1654 <commit>` escalates: `swarmforge/scripts/test/suite-manifest.tsv`'s only
owner is closed BL-1652, no BL-1654 commit touches it. Root cause: BL-1652's manifest registration for
a test file coder@2 kept from its own abandoned build, against the specifier's explicit ruling to drop
it — already flagged as a quality observation (note 002998), now confirmed as an active land blocker.
Full writeup: `backlog/evidence/BL-1654-land-escalate-bl1652-manifest-stray-20260920.md`. Specifier
noted (priority 00). BL-1654 stays approved, verified, and unlanded in `backlog/active/` pending that
resolution.

### Prior-stage evidence reviewed

architect NONE, cleaner NONE, documenter NONE, hardener (1 defect, self-caught: stale BL-113 Gherkin
mutation stamp after the scenario edit, refreshed same pass). Coder's own non-vacuity check (temporarily
broke the new scenario's assertion, confirmed it fails as expected, reverted) reviewed and sufficient.
