# Cleaner pass — BL-722, BL-852, BL-847, BL-853 (2026-08-08)

## Context
Batch-salvaged after an operator error: `done_with_current.sh` was run a
second time before this batch's actual work was done, mis-marking these four
coder handoffs as completed with no cleaner action taken. Recovered via
`redo_from.sh <id> cleaner` per ticket, then hand-corrected the requeued
handoffs' `commit`/`task` fields — `salvage_lib.bb`'s `latest-item-handoffs`
scan does not look inside `inbox/completed/batch_*/` subdirectories, so
`redo_from.sh` silently fell back to the current HEAD commit and the bare
item-id as task name for all four. Flagging this scanning gap for a ticket
separately; it is not fixed here (out of scope for this cleanup pass).

Merged: 84ded08177 (BL-722), 932a4a1f/b2911ade5d (BL-852),
f776bbc669 (BL-847), 8871cfa446 (BL-853) — a linear coder chain, fast-forward
merged as one commit into cleaner.

## Review scope
Diffed d66fc073 (pre-BL-722 promote) .. 8871cfa4 (BL-853 tip) to isolate
these four tickets' own changes from inherited ancestor history (an
unrelated leanLedger-machinery removal predates this range and is not part
of these tickets' work).

## Findings
- `extension/src/swarm/resourceSamplerActivation.ts` (BL-847): clean
  composition of `resolvePanePid` -> `selectAgentDescendant` ->
  `resolveAgentPid`; pure BFS helper, well-commented invariants (never
  falls back to the pane's shell pid). No changes needed.
- `extension/src/tools/pilotSafeDefects.ts` (BL-722): pure filter+rank
  module, small and well-typed. No changes needed.
- `swarmforge/scripts/chase_sweep_lib.bb` (BL-852): minimal, well-commented
  `held?` branch ahead of the age ladder, reuses the existing shared
  ambulance predicate (no new adapter). No changes needed.
- `swarmforge/scripts/promote_and_route_next.sh` /
  `backlog_depth_conf_path_cli.bb` (BL-853): signed-integer cap parsing +
  a proper conf-path CLI instead of re-deriving the swarm-identity lookup
  in bash. No changes needed.
- No CRAP/DRY/structural issues found; no duplication introduced.

## Verification run
- `npm run compile` — clean.
- `npm test` (extension/, full vitest suite): 7128/7134 passed. The 6
  failures are all in `dependencyGateCliCleanAndViolations.test.js` /
  `dependencyGateCliReportsAndScope.test.js` /
  `dependencyGateCliStorageGlobals.test.js`, all the same pre-existing cause
  (`dependency-cruiser` requires Node ^22||^24||>=26; this host runs Node
  20.20.2) — unrelated to these four tickets, no dependency-cruiser files
  touched in this diff.
- `swarmforge/scripts/test/test_backlog_depth_conf_path_cli.sh` — PASS
- `swarmforge/scripts/test/test_promote_and_route_next_no_limit_depth.sh` — PASS
- `swarmforge/scripts/test/test_promote_and_route_next_priority.sh` — PASS
- `swarmforge/scripts/test/bl852_chase_sweep_ambulance_hold_property_runner.bb` — PASS (300 runs/invariant)
- `swarmforge/scripts/test/chase_sweep_test_runner.bb` via `test_chase_sweep.sh` — PASS (all 20 cases)
- `swarmforge/scripts/test/promotion_gates_lib_property_runner.bb` — PASS (500 runs)

## Verdict
NONE — no defects found. Forwarding to architect.
