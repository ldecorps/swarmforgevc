# BL-1387 — CLEANER PASS (D2 fix), 2026-09-04

NONE. No defect found in BL-1387 itself; no cleanup change made beyond the
merge resolution (recorded on the merge commit, `14dc8e1671`).

## What changed

Coder's D2 fix (`6a6a052d05`, a tip-pure rebuild carrying only BL-1387's own
paths after a send-time task-scope gate refusal on the previous branch tip)
adds the two step handlers scenario 06 needed: "the open merge is classified
as the daemon's own" and "the surfaced reason is neither
human-merge-in-progress nor orphaned-merge". Resolved the add/add conflict
on `bl1387OrphanedMergeSurfacedSteps.js` by taking coder's version wholesale
— confirmed byte-identical to `6a6a052d05`'s own content via `git diff`.

## What was checked

- Ran the fixture's `owned` shape directly (not only via the step handler):
  `{"class":"own","reason":"aborted-owned-merge",...}` — matches both of
  scenario 06's assertions exactly (positively pinned to
  `aborted-owned-merge`, not merely "neither of the other two").
- `master_main_reconcile_lib_test_runner.bb` — re-ran: ALL TESTS PASS.
- TypeScript compiles clean; the handler discovers via BL-1371's registry.
- Ticket state: `bounce_count` unaffected here — this is the D2 rework,
  not a new bounce.

## Separately reported: a concurrency defect found in BL-1385's guard, not BL-1387's

While committing this merge, `check_handler_module_graph.sh` (BL-1385,
already forwarded past cleaner) intermittently refused my commit with a
large, VARYING number of "cannot load" handlers (519, then 530) — and
passed cleanly seconds later run standalone. Reproduced directly: two
concurrent invocations of the unmodified script in this same worktree
raced on the shared `/tmp/bl1385-handler-graph.*` prefix (its sweep-before-
mkdtemp pattern deletes ANY matching directory, including one another
concurrent invocation is actively using) — one run exits 0, the other
refuses with "could not determine a tree to examine". Since this guard now
runs on every commit (`run_commit_guards.sh`) and every land
(`land_step_lib.bb`) across every worktree sharing `/tmp`, this can
intermittently block ANY commit anywhere in the swarm whenever two guard
invocations overlap. This is a BL-1385 defect, not a BL-1387 one — BL-1385
already passed cleaner and architect review before this was found, so it is
reported via a priority-00 note (see next handoff) rather than reworked
here; not mine to fix mid-flight on someone else's already-forwarded ticket.

Forwarding BL-1387 unchanged to architect.

By cleaner.
