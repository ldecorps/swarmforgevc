# BL-1413 — architect pass, 2026-09-05

Clean sweep. No changes needed; forwarding to hardener.

## Checks run

| check | result |
|---|---|
| `sh -n daemon_log_freshness_check.sh` | clean |
| `bash swarmforge/scripts/test/test_daemon_log_freshness.sh` | all BL-1413 scenarios pass; the same 4 pre-existing unrelated reds the coder/cleaner documented (BL-796-01/02/03 nvm/node PATH resolution, `fleet-telegram: default announce invoked curl`) reproduced here too, none touching `heartbeat_age_secs` |
| `bb bl1413_freshness_nul_byte_property_runner.bb` | 40/40 runs, ALL PROPERTIES HOLD |
| acceptance `BL-1413-...feature` | 4/4 |
| `required_wiring` anchor `grep -aE` | resolves at `heartbeat_age_secs`, line 191, the live cron function |
| `required_wiring` anchor `registerSteps` | resolves, handler file registers |

## Architecture review

Pure POSIX-`sh` daemon-watchdog fix; no TypeScript, no webview, no extension
host/webview boundary implications. Dependency-gate and co-change tooling
target `extension/src/**/*.ts` and don't apply to this parcel's shell/babashka
surface — confirmed no TS file touched (`git diff --stat` shows only the
`.sh` checker, its shell test suite, a probe helper, fixture logs, the
acceptance step handler, and a `.bb` property runner).

- `heartbeat_age_secs`'s fix is minimal and exactly what the ticket's "How"
  section named: `grep -E` → `LC_ALL=C grep -aE` (text-mode, byte-wise
  match) plus a newest-to-oldest walk that only falls back to the
  `unparseable-timestamp` sentinel when NO matching line parses. Portable:
  `-a` and `LC_ALL=C` are both accepted by BSD grep (macOS) per the
  engineering rule targeting stock macOS `/bin/bash`/grep, and the loop uses
  only POSIX `sh` constructs (`sed -n "${i}p"`, `wc -l | tr -d ' '`,
  `$(( ))`).
- `bl1413_heartbeat_age_probe.sh` extracts the real function body via `awk`
  range rather than a hand-duplicated copy — avoids a stale-fixture trap by
  construction, consistent with this file's own convention for BL-1011/
  BL-1012's tests.
- The recommended one-flag hotfix is explicitly NOT applied by this parcel
  (ticket reserves it for human authorization) — confirmed: this parcel
  lands the full fix (flag + fallback + tests), nothing partial.
- Registry-guard trap (BL-784) correctly avoided: the parcel's own tests use
  the shipped conf rather than a scoped one, sidestepping the unconditional
  on-disk `*_supervisor.bb` scan the guard's second check performs — read
  and confirmed against `daemon_log_freshness_registry_guard.sh`'s actual
  two checks, not just the coder's claim.

## Invariants Review (BL-654)

Both declared invariants have a coder-authored, non-vacuous property test
(`bl1413_freshness_nul_byte_property_runner.bb`), each independently
confirmed to break on the two described reverts (dropping `-a`; disabling
the newest-to-oldest fallback walk) and to hold once restored — re-ran here,
40/40, coverage across all four noise placements and both threshold sides.
No violation found on code review of the fix itself: the walk correctly
starts at the newest match (`i=$line_count`, descending) and returns the
sentinel only after exhausting every line, matching invariant 1's "a byte
can only ever make one line unparseable, never hide the lines after it."

## No violations found

No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener with the same task name and the received
commit.

By architect.
