# BL-1740 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `swarmforge/scripts/chase_sweep_lib.bb` (the pause read),
`swarmforge/scripts/handoffd.bb` (one adapter entry),
`bl1740ChaseSweepPauseFromCallerSteps.js` +
`bl1740ChaseSweepPauseFromCallerCli.bb` (new) — Babashka-only production
change (BL-472: no mutation/CRAP/DRY wired) plus a plain Node acceptance
handler.

Checks run (host load ~15-23 on 20 cores):
- `node specs/pipeline/cli.js specs/features/BL-1740-the-chase-sweep-reads-the-pause-from-its-caller.feature`:
  4/4 ok.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1505DedupSuppressedChaseCountInvariants.property.test.js` (from
  `extension/`): 3/3 — the originally-red file (Article 4.2's held row)
  now provably immune, confirmed live.
- `bash swarmforge/scripts/test/test_chase_sweep.sh`: ALL PASS (17
  pre-existing scenarios, none regressed).
- `bb specs/pipeline/steps/lib/bl1740ChaseSweepPauseFromCallerCli.bb
  <mode>` for all four modes by hand: `none`/`false` → `{"chaseCount":1}`,
  `true`/`live` → `{"chaseCount":0}`, matching the feature's Examples
  exactly.
- Wiring greps (qa_e2e step 4): `:pause-hold-active?` present in
  `handoffd.bb`'s `run-sweep!` adapter map; no direct
  `handoff-lib/pause-hold-active?` call left in `chase_sweep_lib.bb`.
- `git diff --stat f93ebdf4af..HEAD -- <the four Scope files>`: matches
  exactly.

Hand-mutation (BL-638 fallback, no wired mutation tool for `.bb`,
mutation_cost: medium): mutated the `pause-held?` binding in
`chase_sweep_lib.bb` back toward a call ignoring the adapter, confirmed
the feature and the bl1505 property test both fail against it, restored,
re-ran both green — same class of check the coder's own evidence already
performed independently; I re-derived it rather than trusting the claim.

**A genuine, separate finding — recorded but not fixed here (out of this
ticket's proportionate scope):** hand-mutated `handoffd.bb`'s literal map
key `:pause-hold-active?` → `:pause-hold-active` (a one-character typo
class, RHS function call left untouched). EVERY available check stayed
green: BL-1740's own feature (its CLI wires the adapter itself, never
loading `handoffd.bb`), `test_chase_sweep.sh` (drives the lib directly),
`test_handoffd_pause_suppresses_outbound_wakes.sh` (spawns the real
daemon, but is defeated by a DIFFERENT, OUTER pause gate —
`outbound-wakes-suppressed?`, `backlog-depth-lib`, not
`handoff-lib/pause-hold-active?` — that already prevents `chase-sweep!`
from running at all in the daemon's real main-loop path, so this parcel's
adapter is unreachable from that call site today), and even
`pre_qa_gate.sh BL-1740 <commit>` (its `required_wiring` check is a bare
`str/includes?` substring test, satisfied by the untouched
`(handoff-lib/pause-hold-active?)` call one token away from the mutated
key). Restored `handoffd.bb` byte-for-byte before continuing. Full
write-up: `backlog/evidence/BL-1740-hardender-gap-pause-hold-active-wiring-untested-20260925.md`.
Sent as a priority-`00` note to the specifier (delivered via daemon
backup after a sync-inject failure — expected fallback, not an error).
Not a BL-1740 code defect — the coder's fix is correct; this is a gap in
the surrounding verification tooling (a daemon-reachable wiring test, or
tightening `pre_qa_gate_lib.bb`'s substring matcher), genuinely too large
and too adjacent-but-unowned to fix inside this hardening pass without
risking scope creep into a shared gate tool every ticket's
`required_wiring` depends on.

Fixture hygiene: `pgrep -fl handoffd.bb` after all runs shows only two
pre-existing live-swarm daemons (7394s/11379s elapsed, well before this
pass started) — no leaked fixture daemon from
`test_handoffd_pause_suppresses_outbound_wakes.sh`'s own spawn (its trap
fired, `ALL PASS` confirms clean exit). `git status --short` clean of
anything this pass's runs could leak.

Not run: `npm run test:properties` in full (2026-09-17 rule — only the
one relevant property file, no other `*.property.test.js` in Scope) and
Stryker/CRAP (no `src`/`out` file in Scope).

By hardender.
