# BL-601 QA bounce — 20260827

**Routing:** coder (tip-pure handoff / branch pollution — BL-506)

## Failing command

```
git fetch origin main
git reset --hard origin/main
git merge --no-ff bc6bdf0497
git diff --name-only origin/main...HEAD
# 56 paths — BL-599 / BL-780 / BL-781 / BL-980 / … hitchhikers
```

## Commit tested

`bc6bdf0497` (documenter handoff; tip chore YAML + evidence). Merge onto
`origin/main` (`76128c853`) succeeded; post-merge tree is not tip-pure.

## First error excerpt

Post-merge delta vs `origin/main` includes unrelated product:

- **BL-599** intake-balance how-to, steps, property, mutation sweep
- **BL-780** note-actionability steps + ordering script + conf
- **BL-781** retire-dead-babysitter steps
- **BL-980** recently-closed elapsed tests/steps/how-to/sweep
- Plus assorted evidence/merge-up noise (BL-754, BL-1173, …)

## Failure class

behavior

## Expected vs observed

**Expected:** BL-601-only tip (compactionCadence + store + trend export +
acceptance + docs + evidence).

**Observed:** Documenter tip folds BL-599/780/781/980 into a BL-601 handoff
(BL-506). Task header said `BL-601-acyclic-cycle-bounce` but commit is the
compaction-cadence feature rematch — still not tip-pure.

## Defects

**D1 — entangled tip (blame: coder/documenter rematch):** Re-forward tip-pure
BL-601 atop current `origin/main` without BL-599/780/781/980 hitchhikers.

By QA.
