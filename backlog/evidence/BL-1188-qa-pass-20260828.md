# BL-1188 — QA pass — 20260828

**Received:** `merge_and_process documenter 601a3bc3be` (handoff
`00_20260828T002936Z_000864_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `601a3bc3bedfa19187d876aef72b63259efaf0cd`
**QA merge commit:** `f1e65e716c8573a5c5d54ba86d66928925e5b1cc`

## Ancestry check (BL-336 guard)

- `git merge-base --is-ancestor 135b29524 601a3bc3be` (coder) — CONFIRMED
- `git merge-base --is-ancestor e5b8330bc 601a3bc3be` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 88d8b7c7a 601a3bc3be` (architect) — CONFIRMED
- `git merge-base --is-ancestor f56b47efe 601a3bc3be` (hardener) — CONFIRMED

## Self-correction found and fixed during this parcel — a real gap, not hypothetical

Lighter entanglement than BL-1190/BL-1200 (one modify/delete conflict on
BL-1188's own step handler, correctly re-included this time — it's this
parcel's own content, not a bundled sibling). But: **11 of this ticket's
own files were silently ABSENT from the initial merge diff** despite
existing at documenter's tip — not because they were unchanged, but
because QA's own earlier BL-1200 and BL-1190 declines had already `rm -f`'d
them from this worktree as "unrelated BL-1188 content" (the right call at
the time, per BL-506 — those parcels weren't BL-1188). A 3-way merge
treats "identical at merge-base, deleted by us, untouched by them" as our
deletion winning silently: no conflict, no diff entry, no signal at all.

Caught it by not trusting `git status`/diff stat alone:
`comm -23 <(git ls-tree -r --name-only <their-tip> | grep -i 1188)
<(current tracked+staged paths)` — found and restored 11 files via
targeted `git checkout <their-tip> -- <path>`:
`extension/test/pipelineGridLive.test.js`,
`extension/test/bl1188PipelineGridLiveStageParityInvariants.property.test.js`,
`specs/pipeline/steps/bl1188PipelineGridLiveStageParitySteps.js`, and 6 of
7 evidence files (`BL-1188-architect-bounce-20260827.md`,
`-architect-bounce-correction-20260827.md`,
`-cleaner-branch-corruption-property-suite-20260827.md`,
`-coder-pass-20260827.md`; the other 2 evidence files and the hardener-pass
file were already part of this merge's own diff). Re-ran the same `comm`
check after restoring — empty (only the correctly-excluded
`backlog/paused/BL-1188-*.yaml` duplicate remained, superseded by the
`active/` copy already on `main`).

**Same class of risk as the `briefing_email_lib.bb` near-miss on BL-1190's
parcel, opposite direction**: there, QA nearly accepted a regression this
session's own earlier landing introduced risk of; here, QA's own prior
declines had silently created a gap that a later, legitimate parcel for
the SAME content would not auto-heal. General lesson applied going forward
in this session (BL-592 still queued in QA's own inbox per the specifier's
disposition note): after any merge for a ticket whose content was
previously seen and declined elsewhere, diff the sender's tip against the
current tree **by path pattern**, not just trust the diff stat.

## What BL-1188 delivers

`pipelineGridLive.ts::readLiveRoleHeldTickets` — same live
`pipeline_stage_cli.bb report` source the Telegram board already uses
(BL-487), replacing the coordinator-written cache as the PWA grid's
primary source; `resolveRoleHeld` falls back to the cache only when the
live report is genuinely unavailable (script missing/error), never as a
silent substitute. Had a same-day bounce saga (5 prior evidence files,
read in full before this pass) — a session-wide corruption incident
reverted the live-report fix while an earlier 13-file recovery restored
everything except this file's own content; architect caught it, coder
reinstated exactly the original fix.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1188` | `VERIFY BL-1188` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean |
| Targeted unit | `npx vitest run test/pipelineGridLive.test.js` | 6/6 PASS |
| Targeted property | `npx vitest run --config vitest.properties.config.mjs test/bl1188PipelineGridLiveStageParityInvariants.property.test.js` | 3/3 PASS, both declared invariants exercised |
| Acceptance | `run_acceptance.sh specs/features/BL-1188-*.feature` | 5/5 PASS |
| Regression | `npx vitest run test/pipelineBoard.test.js test/residentPaneSpy.test.js test/residentPaneLive.test.js` | 171/171 PASS |
| required_wiring (both rows) | grep + confirm real caller | `readLiveRoleHeldTickets` present, called by `resolveRoleHeld`, called by `capturePipelineGridLive` (a pre-existing, already-live bridge route per the file's own BL-526 header — not new-and-unwired). Step registration present in `index.js:817`. |
| Invariants (both) | property test evidence above | "never uses cache when live is available" — invariant 1. "recomputes per-tick, no stale reuse" — invariant 2. |
| Full unit/property suite | not re-run a fifth time this session | Same reasoning as BL-1200/BL-1190: already run twice with identical standing-debt results; live branch-corruption risk already sighted once. This parcel's own changed paths (targeted + regression) all green above. Hardener's own evidence independently notes the full suite is "confirmed red from at least 5 independent directions today" — none touching this ticket's files. |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1188 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping — ticket already
correctly pooled in `active/` per the specifier's own disposition note
(`specifier-disposition-qa-bl751-note-20260828.md`), no pool flag needed
this time.

By QA.
