# BL-1370 — documenter pass (2026-09-05)

## Received
`72d359e397` (hardener; per `backlog/evidence/BL-1370-hardener-pass-20260905.md`).

## What changed, operator-visible
`check_worktree_strays.bb <worktree-root> [--reap]` is the entry point a
role now calls for the per-pass orphan gate QA's prompt already stated in
prose — replacing 326 hand-worded variants with one stable
`WORKTREE_STRAYS: ...` result line. A stray found is a refusal (non-zero
exit). Scope delegates entirely to `process_table_lib.bb`'s
`project-scoped-process?` (BL-887), the same classifier the orphan
janitor and handoffd supervisor already share. Folded into this same
parcel per specifier amendment: that shared classifier's bare
prefix-substring matching (`str/includes?`/`str/starts-with?` with no
boundary) let `.worktrees/coder` claim `.worktrees/coder-cursor2`'s
processes and `/repo` claim `/repo-2` — fixed to require a
path-component boundary, which the janitor and supervisor inherit too
(narrows what they may reap, never widens it). Reaping signals the
process GROUP, never a bare pid, with two reported-not-killed exceptions:
an unreadable pgid, and a stray sharing this process's own group (would
take the role's own shell session down with it).

## Doc-domain review
No existing how-to page covered BL-887's scope classifier or the orphan
janitor family at this level — created a new page.
- `docs/how-to/BL-1370-worktree-stray-check.md` (new): usage, the
  clean/stray/other-worktree/prefix-sibling distinctions, the
  path-component-boundary amendment (with before/after matching rules),
  the reap exceptions, and a verify block.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: new link entry (orphan-free, per index discipline).
- Confirmed via grep that the classifier's docstring (the note's flagged
  concern: "it still says starts-with") was ALREADY corrected by the
  coder/hardener pass — `process_table_lib.bb:171-181` reads "PATH-
  COMPONENT BOUNDARY" and no stale "starts-with" description remains
  anywhere referencing `project-scoped-process?`. No doc-domain action
  needed there beyond what the new how-to page already states correctly.
- Diagram registry checked — no diagram depicts scope-classifier or
  orphan-gate internals at this granularity.
- Ran `bash swarmforge/scripts/test/test_bl1370_worktree_strays.sh` — ALL
  PASS (16/16 assertions; one benign environment-dependent SKIP: "this
  host did not put the stray in the caller's own group").
- Ran `bash swarmforge/scripts/test/process_table_lib_test_runner.bb` —
  ALL CHECKS PASSED.
- Ran `bl1370WorktreeStrayCheck.property.test.js` (from `extension/`) —
  4/4 PASS.

## Verdict
New doc content (how-to page) plus the standard Specification.MD/index.md
entries, not NONE. Forwarding to QA.
