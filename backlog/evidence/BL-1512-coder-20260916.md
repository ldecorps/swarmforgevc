# BL-1512 — coder, 2026-09-16

Completes the ticket after the specifier's eligibility adjudication
(`backlog/evidence/BL-1512-specifier-eligibility-adjudication-20260916.md`),
which confirmed the coder's ineligibility finding
(`backlog/evidence/BL-1512-coder-verdict-20260916.md`) came from this
worktree's stale gitignored steward registry (three known models) and,
for qwen, from citing the aider CLI prefix (`openai/qwen3.7-plus`)
instead of the steward id the gate actually resolves (`qwen/qwen3.7-plus`).
On the launch root all seven FIRM picks are eligible.

## New pack: `swarmforge/packs/candidate-diversified-fallback-mono-router.conf`

The seven pipeline seats staffed per the ticket's FIRM picks (coder/
architect: codex, gpt-5.3-codex; specifier/cleaner/hardender/QA: cursor,
auto; documenter: aider, qwen/qwen3.7-plus on the Alibaba Token Plan),
plan-diversified per the intake's own discipline (never repeat the
previous seat's plan). Coordinator: claude-sonnet-5 (Anthropic) per
human_ruling, with a header caveat that this one seat is NOT
diversified (only Anthropic and b.ai are proven live on coordinator).

## Verified against the real master checkout (qa_e2e_procedure steps 1-2)

Step 1 (`env -u PACK_STAFFING_SKIP_GATE bb pack_staffing_gate_cli.bb
/home/carillon/swarmforgevc <derived-windows-file>`): the four cursor
lines `pass`; both codex lines `seat-model-unresolved` (no `codex` row
in `agent-model-providers` - BL-1597); the documenter line
`role-gate-not-pass` (no recorded `documenter-gate`) - exactly the three
verdicts the PREREQ now names, and exactly what the ticket predicted.

Step 2 (`model_steward_cli.bb eligible <provider>/<model> --role
<role>`, no override): all seven picks (`cursor/auto` on specifier/
cleaner/hardender/QA, `openai/gpt-5.3-codex` on coder/architect,
`qwen/qwen3.7-plus` on documenter) read **eligible**.

Step 3 (a scratch-root dry launch reaching the staffing gate) is QA's
own e2e check per the qa_e2e_procedure; not reproduced here.

## Invariant

`invariants:` declares "No pipeline seat of the diversified pack
resolves to the anthropic or the tencentcloud2 provider" - encoded as
acceptance scenario 01, which drives the REAL
`pack_staffing_gate_lib.bb/resolve-seat` over all 7 derived window lines
(never a reimplementation of its `agent-model-providers`/
`api-base-host-providers` tables). This is a structural check over a
fixed, finite set of exactly 7 lines, not a sampled domain, so a
generative property test would have nothing to vary; the acceptance
scenario is the invariant's executable encoding for this pack-conf
ticket (no TypeScript, no property-test lane - same posture as BL-1511's
own structural scenarios).

## Acceptance

`swarmforge/scripts/run_acceptance.sh
specs/features/BL-1512-a-diversified-fallback-pack-survives-two-exhausted-plans.feature`
- 4/4 scenarios pass (scenario 02 x2 for its two Examples rows).
