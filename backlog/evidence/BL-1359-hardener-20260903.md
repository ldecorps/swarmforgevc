# BL-1359 — hardener pass (2026-09-03)

## Context

Merged architect `e8d060b5b8` (all three invariants verified non-vacuous,
no defect found). This pass hardens the coder's fix in
`swarmforge/scripts/babysitter_check.bb` (the two-tree first-parent diff
replacing the `-m --first-parent` no-op) and the matching comment
correction in `babysitterd_sweep_lib.bb`.

## BL-149 file-change cooldown gate

- `mutation_cooldown_gate.bb . swarmforge/scripts/babysitter_check.bb` →
  **`skip-cooldown`** (file age 0.23 days — coder → cleaner → architect all
  committed to it today). Not hand-mutated this pass per the gate.
- `mutation_cooldown_gate.bb . swarmforge/scripts/babysitterd_sweep_lib.bb`
  → `run` (age 6.79 days, quiet host). Confirmed by diff
  (`3cc596a523~1..dc529f5233`) this parcel's change to that file is
  COMMENT-ONLY — no logic changed, so there is nothing in it to mutate.
- As with BL-1354 earlier today, this pass leans on the non-vacuous
  verification already independently repeated by coder, cleaner and
  architect (each reverted the fix and confirmed the old command's charge
  set reappears, and each re-ran the real-repo regression table
  `15dc336877`/`e3ce579112`/`c2cd0ca698`/`f8dc07963`) rather than
  duplicating a hand-mutation sweep on a file the gate says not to touch
  this pass.

## BL-113 Gherkin acceptance mutation (Scenario Outline present)

`run_gherkin_mutation.sh specs/features/BL-1359-….feature <mktemp-dir>
specs/pipeline/steps/index.js soft` — **3/3 killed, 0 survived, 0 errors**
(manifest embedded in the feature file).

## Verification

- `bb swarmforge/scripts/test/bl1359_merge_charged_test_runner.bb` — ALL
  PASS.
- `run_acceptance.sh specs/features/BL-1359-….feature` — 7/7 pass.
- Property `bl1359MergeChargedInvariants` — ran 3 consecutive times, 3/3
  green every run, no flake.
- Regression: `test_babysitter_check.sh` — ALL PASS, unaffected.
- Whole-tree guard sweep (parcel touches `specs/pipeline/steps/` and
  `extension/test/`): all 17 `extension/test/*Guard*.test.js` (excluding
  `.property.` siblings) run. 3 failed — `tempDirTrapGuard`,
  `liveRepoDerivationGuard`, `socketFixtureShortRootGuard` — none flags a
  BL-1359 file (grepped for `bl1359`/`BL-1359`, zero hits). Same standing
  reds observed on every pass today.
- CRAP/DRY: not applicable — no `extension/src/*.ts` touched, and Babashka
  has no wired CRAP/DRY (documented gap).
- No orphaned test/mutation processes at handoff; no stray `bl1359-*`
  fixture directories under `/tmp`.

## Two pre-existing reds, re-confirmed not mine

Both already named and grepped-as-untracked by the architect
(`bl962_merge_adjudication_test_runner.bb`'s "no second ancestry predicate"
structural invariant, and `BL-631` acceptance 16/17). Re-ran both here:
byte-identical to the architect's report, still no open ticket for either
per a fresh grep. Not actioned — the architect already sent the note.

## Verdict

No mutation gap surfaced within this pass's scope (the one changed
production file is under cooldown, and the gate correctly skipped it; the
other touched file's change is comment-only). BL-113 clean, all suites
green, no regression, no new guard violation. Forwarding to documenter.
