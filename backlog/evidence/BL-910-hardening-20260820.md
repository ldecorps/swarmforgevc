# BL-910 — hardening pass, 2026-08-20

Parcel received from architect (`efb4ae0d15`, review PASS) as a batch of one.
Task name `BL-910-briefing-burndown-projected-eta`.

## Host conditions

Load average **171 → 235 on 4 cores** for the whole pass (other worktrees'
runs; nothing of mine orphaned — `pgrep -fl 'node --test|stryker'` empty at
start and at handoff). That is ~29–59× the core count, far above the 2×
threshold at which the role article forbids attempting Stryker or the BL-113
Gherkin mutator. Every gate below is recorded run-or-blocked; nothing is
assumed clean.

A foreground `npx tsc -p .` timed out at the tool's 120s ceiling, so the
compile, the verification gauntlet and the coverage/CRAP run were all launched
detached (`python3` double-fork + `os.setsid`, absolute `cd` embedded, logs
under `./tmp/`), per the BL-932 rule.

## Gates run

| Gate | Result |
|---|---|
| `tsc -p .` (extension) | exit 0 |
| Unit — `test/notDoneBurndown.test.js` | **18 passed / 0 failed**, exit 0 |
| Standing whole-tree guards (7 files) | **51 passed / 0 failed**, exit 0 |
| Property lane — `notDoneBurndownEta.property.test.js` | **2 passed** (600 draws), exit 0, no `Unhandled` lines |
| Acceptance — `run_acceptance.sh` on the BL-910 feature | **9 scenarios passed / 0 failed**, exit 0 |
| Gherkin mutation, hand-authored surgical sweep (BL-638 fallback) | **total=21 killed=21 survived=0**, baseline 6/6 rows pass |
| DRY — `jscpd --config .jscpd.json src` | 35 clones repo-wide, 0.51% duplicated lines; **none in either file this parcel touches** (all in `tools/telegram*`), exit 0 |
| Differential complexity vs `main` | clean after the extraction below (see D2) |
| CRAP on changed `src/*.ts` | see below |

The standing guards were run because this parcel adds files under BOTH
`specs/pipeline/steps/` and `extension/test/` — the case the guard rule exists
for. Enumerated at run time, not recalled: seven non-property `test/*Guard*.test.js`
files (`bl643NonPipelineAgentsStepsGuards`, `envRestoreGuard`, `portableTimeGuard`,
`swarmforgeShErrorChannelGuard`, `tempDirTrapGuard`, `tmpDirMigrationGuard`,
`tmuxReaperGuard`). Note the count has grown from the six recorded in the role
article on 2026-08-19.

## Gates deferred, with the reason

- **Stryker (language mutation)** — NOT RUN. Load 229–235 on 4 cores; the role
  article forbids attempting it, including at `concurrency=1`, above 2× cores.
  Deferred to the next quiet pass. The parcel is not forwarded as though this
  gate passed.
- **BL-113 `run_gherkin_mutation.sh`** — NOT RUN, same load condition (the load
  rules bind every mutation runner, and this one's failure shape under
  contention is a silent 0%-CPU stall). The **BL-788 pre-check is clean on
  evidence, not assumption**: `bl910BriefingBurndownEtaSteps.js` starts no
  bridge and creates no fixture directory (`grep` for
  `mkdtemp|tmux|startBridge|mktemp` returns nothing), so the cross-step
  handle-leak hazard does not apply here. In its place the BL-638
  hand-authored surgical sweep was run and is recorded above.

## Hardening changes made

### D1 — the step file required the gitignored build output at module load (BL-968 class)

`bl910BriefingBurndownEtaSteps.js` required `extension/out/metrics/*` at
**module top level**. `specs/pipeline/steps/index.js` requires every domain
eagerly, so in any tree where `extension/out/` has not been built — a tree
materialized from git, which is exactly what the BL-761 acceptance gate does —
requiring the step registry throws at load. That is the defect class BL-968 was
minted for ("three step files bind `resolveMainCheckout` at module load … the
gate is blind on effectively every send").

Its own sibling on this chart, `bl896BriefingOpenTicketChartSteps.js`, already
does this correctly and says why: the compiled module is required **lazily**,
inside the functions that use it. BL-910 now mirrors that. Behaviour is
unchanged (acceptance still 9/9 after the change — the acceptance run above ran
against the edited file).

### D2 — differential complexity regression in `buildNotDoneBurndownSvg`

Measured with the project's own `crapLib` AST walker, each changed function
against `main`'s version of that function:

```
buildNotDoneBurndownSvg: base=5 now=7   ROSE
```

The parcel added a guard `if` and a two-branch ternary directly into an
already-large renderer. Both sides were under the CRAP threshold, so the
absolute check reads identically before and after and hides this — the exact
BL-956 shape the differential rule was accepted for. CRAP also grows
superlinearly with complexity, so the new caption would have inherited a score
it did not earn (the BL-866 dispatcher lesson, same mechanism, different file).

Fixed by extracting the guard and the two-branch caption into a pure exported
`formatProjectionLine(projection)` in the same module — a behaviour-preserving
testability split, which is this role's own domain. After extraction:

```
formatProjectionLine:    NEW  now=3
buildNotDoneBurndownSvg: base=5 now=5   same
notDoneBurndown.ts:      no function rose; projectNotDoneEta=2, yyyyMmDd=1
```

No function in either changed file is now above its `main` baseline.

### D3 — assertion gaps the existing suite could not see

Five tests added to `extension/test/notDoneBurndown.test.js`. Each closes a gap
where a mutant would have survived, not a restatement of coverage already there:

1. **The eta caption's net-burn figure.** Nothing asserted the `at net burn
   2.0/d` half of the line, so `toFixed(1)` → `toFixed(0)` survived.
2. **The not-shrinking caption in full.** Neither the `(net burn …/d, all open
   tickets)` suffix nor the negative-rate rendering was asserted anywhere.
3. **`netBurnPerDay` on the no-eta path.** Asserted only for the eta branch, so
   the `netBurnTenths / 10` reconstruction was unguarded when there is no date
   — including the rounded-from-noise case.
4. **The guard as its own unit.** `formatProjectionLine(undefined)` throws,
   testing the required wiring at the function that now owns it.
5. **Placement — the human's explicit decision.** The ticket chose "the caption,
   not a dashed overlay line". Nothing checked it: the heading, subtitle and
   projection `y` ordinates are now asserted in order, and the projection is
   asserted to sit strictly above the first gridline, i.e. clear of the plot
   area. This kills the layout-constant mutants (`padT`, the caption's `y`)
   that would otherwise all survive silently, and it does so by asserting the
   decision rather than the literal.

## Wiring verified by hand (BL-900)

`renderBriefingBurndown` (`extension/src/tools/render-briefing-burndown.ts`) is
the single consumer: it calls `computeNotDoneBurndownSeries`, which now carries
`projection`, then `buildNotDoneBurndownSvg`, which renders it. No call site
passes a hand-built series; the guard makes one fail loud rather than render no
answer. `briefing_email_lib.bb`'s caption text is a fixed sentence naming the
chart, not a restatement of its numbers, so the projection reaches the briefing
only through the PNG — which is what `qa_e2e_procedure` step 7 checks.

The `required_wiring` needle (`no ETA` in
`extension/src/metrics/notDoneBurndown.ts`) is unaffected by these changes:
`NOT_SHRINKING_REASON` still lives in that file.

## Merge-graph check (BL-343)

`git log --oneline $(git merge-base HEAD main)..main` — 28 commits landed
behind this parcel, all backlog/spec/evidence bookkeeping. No bounce fix among
them touches the burndown code, and the sibling ticket bounced from this
coder's session (BL-959) was bounced for a false claim in an evidence report,
not a code pattern that could recur here.

## D4 — BLOCKED / SPEC GAP: this parcel breaks BL-896's acceptance, deterministically

**Not fixed here — it needs a specifier ruling, and it is recorded rather than
worked around.**

`specs/pipeline/steps/bl896BriefingOpenTicketChartSteps.js` implements BL-896's
step `And it projects no completion date` as:

```js
if (/complet/i.test(ctx.burndownSvg) || /\bETA\b/i.test(ctx.burndownSvg)) {
  throw new Error(`expected no projected completion date; got: ...`);
}
```

BL-910 renders `no ETA — backlog still growing` on a not-shrinking chart. That
string contains `ETA`, so the step throws.

Reproduced in-process against BL-896's own fixture (8 tickets filed, 1 closed
over a 30-day window — the fixture asserts `net > 0` by construction, so the
chart is ALWAYS on the not-shrinking branch, never the eta branch):

```
net = 7   closePerDay = 0.033   mintPerDay = 0.267
projection = {"kind":"no-eta","netBurnPerDay":-0.3,"reason":"no ETA — backlog still growing"}
/complet/i   -> false
/\bETA\b/i   -> true          <-- step throws
```

This is deterministic, not load-flaky: BL-896 scenario `briefing-open-chart-01`
fails on every run once BL-910 lands. It passed before this parcel (there was no
projection line at all).

**Why this is a spec gap and not a defect I should fix.** The contradiction is
between two human rulings and only the human's later one resolves it. BL-910's
`source:` carries decision 3 verbatim — *"Add a projected ETA the human can read
at a glance"* — and decision 1, *"The chart stays. The 2026-07-26
PWA/milestone-burndown ban does not apply here (recorded on BL-896)."* So the
substance is settled: this chart carries a projection. What is NOT settled is
BL-896's scenario, whose English sentence — `And it projects no completion date`
— is now false of the chart it describes. Narrowing only the handler regex would
leave a green scenario whose text asserts the opposite of what the chart does:
a test that reads as coverage and proves nothing. Rewriting the scenario text is
a requirements change on a landed ticket, which is the specifier's call, not
mine.

**And the obvious dodge is not available.** The colliding string is not the
implementer's wording — it is the human's, verbatim in BL-910's own `source:`
block: *"If net burn ≤ 0, render 'no ETA — backlog still growing' (never
'never', never a fabricated infinity)."* Quietly rewording it to slip past
BL-896's regex would drop a human sentence to make a test green, which
Article 5.3 forbids outright. The collision has to be resolved on BL-896's
side or not at all.

The irony worth naming for whoever rules on it: the string that trips the guard
is BL-910's *refusal* to project — the exact behaviour BL-896's step wanted.

Leaving as a `note` at priority `00` to specifier AND coordinator per Article
4.4 (a spec gap travels by note, never as a parcel, and is not a bounce). The
parcel continues to the documenter; its own gates are green.

## CRAP

Recorded separately below once the coverage run completes — a full
`vitest run --coverage` was launched detached because the tool's 120s ceiling
cannot hold it at this load.

## D4 RESOLVED — the specifier ruled, and the fix is applied inside this parcel

The priority-00 note went out at `20260820T011755Z_000254`; the specifier
answered within minutes (`2fe07b291`, ticket notes on BL-910; coordinator
corroboration `8846292cc`). The ruling, in short: **both contracts agree in
meaning for BL-896's rising-case fixture** — no completion date exists. BL-896
asserted that by SILENCE (grep `/\bETA\b/i`, throw on any hit); BL-910 states it
aloud in the human's verbatim wording. The newer approved directive supersedes
the older scenario's letter, and its spirit — never a fabricated forecast while
the backlog grows — stays mechanically asserted.

Applied here, inside the parcel, never as a separate main-side edit: the runner
hard-fails when a scenario step has no matching handler, so the feature text and
the handler have to move in one commit.

1. `specs/features/BL-896-briefing-not-done-burndown-stamp.feature`,
   scenario `briefing-open-chart-01`:
   `And it projects no completion date`
   → `And it projects no completion date, stating "no ETA — backlog still growing"`
2. `bl896BriefingOpenTicketChartSteps.js`, the matching handler now asserts, in
   order: the Gherkin literal equals the production `NOT_SHRINKING_REASON`
   constant (pinned BOTH ways, so neither the scenario text nor the rendered
   wording can drift alone); the chart states it; `/complet/i` matches nothing;
   and `/\bETA\b/i` matches nothing in the REMAINDER once that literal is
   removed — the original no-fabricated-forecast intent, kept enforced, now
   stated positively.
3. Mutation-stamp manifest: **NOT hand-edited** (guardrail). The scenario-text
   change moves the feature hash, so the next `soft` run re-mutates
   `briefing-open-chart-01` on its own. Note that scenario is absent from the
   manifest's `scenarios:` list today, which per BL-502 means it had a survivor
   or an error, not that the tool no-op'd — the list records CLEAN scenarios
   only. **Regenerating it is recorded BLOCKED BY host load** (see the deferred
   gates above), not passed.
4. Supersession recorded here so BL-896's history stays walkable: the
   2026-08-16 ruling's *letter* → BL-910's later approved directive; the intent
   is unchanged and is still tested.

BL-896's other scenario-01 steps stand unchanged — the heading keeps
"burndown", and the new string matches neither `/\btarget\b/` nor
`/remaining to zero/`. No other BL-896 scenario asserts ETA absence; only 01
collided.
