# BL-773-coordinator-asks-through-the-role-ask-path — QA final pass (2026-08-08)

## Commit checked

`a7e8536c645` (QA worktree merge of documenter's `8bb7f48af529`, batched
with BL-839/BL-819/BL-822 — one coder→cleaner→architect→hardener→documenter
lineage covering all four tickets). Ancestry confirmed via
`git merge-base --is-ancestor <commit> a7e8536c`, exit 0 for every commit
below:

- coder/cleaner batch: `8a65d548`, `25c24fd1`
- architect (round 2, post-QA-bounce recheck): `de0d8701`
- hardener (round 2): `cd67e036`
- documenter: `8bb7f48a`
- BL-773's own D1 remediation (raw `mkdtemp` → shared `mkTmpDir` helper),
  confirmed by the architect's recheck at
  `backlog/evidence/BL-773-architect-recheck-pass-20260807.md`, is part of
  this same lineage — no separate hash to check, it is the diff between my
  prior bounce point and this commit.
- BL-819's D1 remediation (`6a6e69b4`, the lean-ledger `.bb` wiring test
  that QA's prior bounce found dropped from the lineage): confirmed an
  ancestor of `a7e8536c` this time — the exact failure mode from the prior
  bounce (a fix existing in git history but not in the reviewed lineage) is
  ruled out.

This is my **third** touch on this batch: original bounce
(`backlog/evidence/BL-773-qa-bounce-20260807.md`,
`backlog/evidence/BL-819-qa-bounce-20260807.md`), architect/hardener/
documenter re-passes, and now this final approval pass.

## Checks run (Article 4.4 complete inventory) — this evidence file covers
## the environment note shared by all four tickets in this batch

- **Node-version environment issue, diagnosed and resolved (not a code
  defect):** initial `npm test` run under the QA shell's active node
  (v20.20.2, an `nvm` misconfiguration in this session — the repo pins
  nothing older) failed 6 tests across 3 files
  (`dependencyGateCliCleanAndViolations.test.js`,
  `dependencyGateCliReportsAndScope.test.js`,
  `dependencyGateCliStorageGlobals.test.js`), all with the identical error
  `dependency-cruiser ... runs on these node versions: ^22||^24||>=26`.
  None of the 4 tickets in this batch touch `dependency-gate.ts` or its
  tests (`git diff 25c24fd1 HEAD --stat` — no `dependencyGate*` files).
  Switched to node v22.23.2 (`nvm use 22`), re-ran: all 10 tests in those 3
  files pass. Re-ran the **entire** suite under node 22 for a clean
  baseline: **406/406 files, 7215/7215 tests green.** No orphaned
  test/mutation processes before or after
  (`pgrep -fl 'node --test|stryker|vitest'`).
- **Unrelated pre-existing flake, confirmed out of scope:**
  `npm run test:properties` (full run) failed 5 tests in
  `test/bl787NamedTunnelInvariants.property.test.js` (2 files total
  failing including 3 `[vitest-worker]: Timeout calling "onTaskUpdate"`
  unhandled errors — the same host-load-driven vitest-worker timeout
  signature recorded elsewhere in this project's own evidence, e.g.
  BL-773's prior QA bounce and this batch's hardener passes). BL-787 is
  unrelated to this batch (`git log -3 --oneline -- extension/test/
  bl787NamedTunnelInvariants.property.test.js` shows no BL-839/819/822/773
  commit touching it; `git diff 25c24fd1 HEAD --stat` confirms). Re-ran
  this batch's own four property files in isolation, clean:
  `test/bl773RoleAskPerRoleGuard.property.test.js` (2/2),
  `test/leanLedgerInvariants.property.test.js` (4/4),
  `test/bl822HostLoadAnomaly.property.test.js` (6/6), plus BL-839's
  babashka property runner (`bl839_master_checkout_drift_property_runner.bb`
  → `ok`).

## BL-773's own checks (this ticket specifically)

- D1 remediation (raw `mkdtemp` in
  `bl773RoleAskPerRoleGuard.property.test.js` → shared `mkTmpDir` helper):
  confirmed fixed, `test/tmpDirMigrationGuard.test.js` passes as part of the
  406/406 green run above.
- Property test: `test/bl773RoleAskPerRoleGuard.property.test.js` — 2/2
  PASS, independently re-run.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-773-coordinator-asks-through-the-role-ask-path.feature`):
  6/6 scenarios PASS.
- `required_wiring` (`swarmforge/roles/coordinator.prompt::role_ask.bb`):
  confirmed present — `grep -n role_ask swarmforge/roles/coordinator.prompt`
  → lines 171, 187.
- Invariants: both declared invariants have real coverage (per-role guard
  via the property test; single-answer-channel via acceptance scenario 3).
- Scope hygiene (BL-506): `git status --short` in the QA worktree shows only
  the pre-existing, already-ticketed untracked `operator_path_lib.sh`
  (BL-796, left unstaged per standing guidance) — no ticket-less functional
  changes folded into this commit.

## BL-819's D2/D3 — confirmed cleared/appropriately scoped

- **D2 (hardener):** confirmed cleared — hardener's pass in this lineage
  (`cd67e036`) genuinely ran against BL-819's production TypeScript this
  time (CRAP fixes, 6 real coverage gaps closed, 10/10 hand-authored
  mutation sweep). See `backlog/evidence/BL-819-hardener-pass-20260807.md`.
- **D3 (documenter), partially cleared — remainder is a spec-gap, not a
  documenter defect:** the documenter's commit (`8bb7f48a`) delivers
  `docs/reference/BL-819-ticket-lifecycle-ledger.md` (the "docs/how-to/
  BL-819-*.md or equivalent" half of D3's remediation pointer), cross-linked
  from `docs/index.md`, `docs/reference/Specification.MD`, and the
  architecture diagram. **Not delivered:** a section in
  `swarmforge/roles/coordinator.prompt` describing the lean-ledger duty.
  Checked: `grep -rn "BL-819\|lean-aware\|lifecycle ledger"
  swarmforge/roles/coordinator.prompt` — no output.

  This is **not** a documenter defect to bounce again. The original D3
  remediation pointer (in `backlog/evidence/BL-819-qa-bounce-20260807.md`,
  written by a prior QA pass) asked the documenter to edit
  `swarmforge/roles/coordinator.prompt` directly — but Article 1.2 reserves
  writing prompt/constitution files to the **specifier** exclusively
  ("Writes specifications and prompt/constitution files only"), and
  BL-819's own ticket `notes:` say so explicitly: *"The specifier writes
  the coordinator.prompt half itself, per Article 1.2."* The documenter
  correctly did not touch a specifier-owned file. Bouncing this back to
  documenter would repeat the BL-425/BL-575 mistake this project's own
  constitution exists to prevent — sending a fix to a role structurally
  barred from making it.

  Also confirmed this gap is **not gating**: no `required_wiring` entry, no
  acceptance scenario, and neither declared invariant references
  `coordinator.prompt` (`grep -n Scenario
  specs/features/BL-819-ticket-lifecycle-ledger.feature` — 10 scenarios,
  none about the prompt file). It is a real, worth-fixing documentation
  gap, but not a defect blocking this approval. **Routed via `note` to
  specifier + coordinator** (spec-gap channel, Article 4.4), not a bounce —
  see the handoff sent alongside this approval.

## Verdict

All four tickets (BL-773, BL-819, BL-822, BL-839) verified clean:
compile, full unit suite (406/406), all four property files, all four
acceptance suites (6+12+9+9 = 36/36 scenarios), all `required_wiring`
items, all declared invariants. Both environment-flake findings
(node-version, BL-787 host-load flake) confirmed unrelated to this batch
and non-blocking. **APPROVED.**

By QA.
