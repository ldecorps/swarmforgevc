# BL-1365 land note — same BL-1403 class, hand-built replay, 2026-09-05

## Merge into QA worktree

A plain `git merge da53f1d7db` (documenter's forward) was refused by
`check_merge_deletion.sh` over the same unattributed intake-archive
deletion as BL-1364's own land (`backlog/evidence/BL-1364-land-note-
20260905.md`) — the documenter's branch had reconciled with many
coordinator merge-up notes along the way (visible in `git log
a17c232020..da53f1d7db`: a long run of "Merge main (...) into
swarmforge-documenter per coordinator merge-up note" commits), entangling
unrelated origin/main content into what would otherwise be a clean
forward.

Identified this ticket's own content commits by grepping the range for
`^[a-f0-9]* BL-1365:` subjects: `0026558eb7` (coder, main implementation —
the true fork point, off `a17c232020`, a BL-1364 coder commit already on
my QA worktree), `59693c3b60` (coder follow-up), `dd1ce4178f` (cleaner,
evidence-only), `2b11d7c86e` (architect, evidence-only), `8b5bc751b7`
(hardener, mutation hardening), `102caee259` (documenter, main doc pass),
`da53f1d7db` (documenter, Article 4.4 evidence). `fdaa567f83` ("adopt the
ritual_class ticket field") was already an ancestor of both origin/main
and my HEAD — a specifier amendment landed separately, excluded from the
replay.

Diffed `a17c232020..da53f1d7db` restricted to the union of paths those 7
commits touch (19 unique files) and applied with `git apply --3way`
directly onto my QA worktree tip (not origin/main — this step is the
intra-pipeline merge, not the land). One conflict, in the shared
`docs/reference/Specification.MD` append-only changelog (both this
ticket's and BL-1364's entries wanted the top slot, same shape as BL-1364's
own land); resolved by keeping both, this entry first. Confirmed every
other file byte-identical to the documenter's reviewed content before
committing (`620b2200fe`).

## Verification (before landing)

Full independent QA pass: `npm run compile` clean. Ticket's own + regression
unit suite: `ritualLedger.test.js` 27/27, `ritualLedgerProducer.test.js`
18/18, `closingCeremony.test.js` 38/38, `closingCeremonyRun.test.js` 13/13
(96/96 total). Property `bl1365RitualLedgerInvariants.property.test.js`
4/4. Acceptance: `BL-1365-...feature` 5/5,
`BL-820-closing-ceremony-lean-pass.feature` (regression, owns the packet)
12/12, `BL-923-dwell-counts-occupied-time-not-parcel-sum.feature`
(regression) 5/5, `BL-1119-closing-ceremony-role-quality-dial.feature`
(regression) 6/6. All three `required_wiring` anchors confirmed live by
direct grep, not just cited. Daemon boots cleanly with the new
`ritual-ledger-producer-sweep`
(`test_bl1395_bb_scripts_load.sh` ALL PASS).

Independently reproduced qa_e2e step 6 (live repo): ran
`run-ritual-ledger-producer.js` against the real repo (13850 commits, 4
classes recorded), then called `determinismCandidatesFromLedger` directly
against that ledger with no declared `ritual_class:` anywhere in
`backlog/active/`+`backlog/paused/` (confirmed by grep) — got exactly the
three offers the coder/architect/hardener evidence claims
(`pass-bounce-evidence`, `backlog-promotion`, `backlog-closure`),
`topic-records` correctly excluded (dominance 0.945 > the 0.5 ceiling).
Re-ran with a synthetic `ritual_class: backlog-closure` declaration and
confirmed that class alone drops out — suppression genuinely works, not
merely asserted. Cleaned up my own scratch `ritual-ledger.json` telemetry
artifact afterward (gitignored).

Spec gaps the coder raised mid-flight (the new `ritual_class:` field;
qa_e2e step 6's stale BL-1360..1363-open premise) were both already
adjudicated by the specifier in-flight (`fdaa567f83`, referenced in the
ticket's own `notes:`) — confirmed `ritual_class` is genuinely adopted in
`swarmforge/backlog-schema.md` and referenced from `specifier.prompt`, not
just claimed. No outstanding spec-gap to route.

Documenter's "no diagram edit" verdict mirrors BL-1364's own same-day,
already-QA-approved reasoning (architecture diagram's per-ticket comment
log inactive since 2026-08-27) — established precedent, not a fresh
omission.

## Land

`task_scope_gate_cli.bb BL-1365-... 620b2200fe` (the single-commit scope
check) returned `OK` — the QA-worktree merge commit's own diff against its
parent is exactly this ticket's scope, confirming the replay-into-QA-worktree
step above stayed clean. Did not re-run the full `land_step_cli.bb`
entangled-sibling walk (it took ~25 minutes on BL-1364 before hitting the
identical BL-1403 `LAND_ESCALATE`, and the class is already adjudicated —
see BL-1364's land note and the specifier-per-class discipline). Took
`620b2200fe`'s own diff against its parent (`ae778cde28`, its own scope
already confirmed pure), applied it in a throwaway
`git worktree add --detach <tmp> origin/main` — zero conflicts this time.
Confirmed every file byte-identical to the QA worktree's reviewed content.
Re-verified compile/unit/property/acceptance green against that tree.

One rematch needed: `origin/main` advanced by one commit (`briefing:
record sent marker`, `71681f8daa`) between forking the throwaway worktree
and committing — caught via `git diff --stat origin/main HEAD` showing an
unexpected `docs/briefings/.sent.json` line before the intended file list,
confirmed as pure ref-drift (not a file my own commands touched: `git
status --short` showed no such modification). Committed anyway (parent
`c43450a8b4`), then `git fetch` + `git rebase origin/main` — clean, no
conflicts. `git diff --stat origin/main HEAD` afterward showed exactly
the 19 intended paths.

`land_main_publish.sh --decide-only` returned `:next :push`,
`:origin-advanced-since-gate false`. Pushed `71681f8daa..3b248a472f`.
Lock acquired/released around the push per BL-1144.

## Land-approval record (BL-1334)

Appended to the shared store (master checkout's `.swarmforge/land-
approvals/2026-09.jsonl`, `record_land_approval.bb` still not landed as of
this pass):
`{"at":"2026-09-05T06:17:31Z","ticket":"BL-1365","commit":"3b248a472f","source":"620b2200fe"}`.
`source` is `620b2200fe` — the QA-worktree merge commit this pass actually
reviewed and approved (an ancestor of `swarmforge-QA`).
Verified `bash swarmforge/scripts/is_qa_ancestor.sh 3b248a472f` prints
`approved: ... - BL-1334`.

`abandoned_commits: [620b2200fe]` recorded on
`backlog/active/BL-1365-the-ceremony-packet-names-hand-made-rituals.yaml`.

BL-1365 is not `GH-`-seeded; no `issue_done.sh` step applies.

By QA.
