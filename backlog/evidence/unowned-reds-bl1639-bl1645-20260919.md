# Two unowned reds found in npm test while verifying BL-1651, 2026-09-19

Neither is BL-1651's own defect - both trace to content that arrived via
today's merge-up chain (BL-1639/BL-1645's own already-landed work), not
anything this parcel authored.

## test/stepHandlerTmpRootGuard.test.js > "the real handler tree names no
offender outside the committed census"

`bl1639BabysitterdCensusSteps.js` mkdtemps without registering its root
(BL-1636's own standing guard) and is not yet in
`extension/test/step-handler-tmp-root-census.txt`. Traces to BL-1639's
own landed work (merged into this branch today via an architect
merge-only pass), not authored here.

## test/tempDirTrapGuard.test.js > "the real swarmforge/scripts tree has
zero temp-dir-trap violations"

`swarmforge/scripts/test/forward_evidence_lib_test_runner.bb`'s
`write-headers` helper calls `(fs/create-temp-dir {:prefix
"bl1645-window-"})` with no shutdown hook and no try/finally delete-tree.
The `bl1645-window-` prefix and content confirm this is BL-1645's own
landed work (merged into this branch today), not authored here - BL-1642's
own edits to this same file (four `forward-completion-decision`
decision-table rows) touch no filesystem at all.

By coder.
