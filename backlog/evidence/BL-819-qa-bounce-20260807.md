# BL-819-ticket-lifecycle-ledger — QA bounce evidence (2026-08-07)

## Commit checked

`9a8b7578` (QA worktree merge of documenter's `ec25074cf6`). BL-819's own
lineage inside that commit: `dcc9d8df` (coder `3bfb4347` merged for cleaning)
-> `29c9e1e2` (cleaner split) -> `a6f61c28` (merged for architect review) ->
architect bounce `95be440a` -> `ef2ff7a4` (bounce metadata recorded) ->
`58c42419` (merged onward as part of the BL-773 cleaner-stage commit).

## Checks run (Article 4.4 complete inventory)

- Full unit suite, this ticket's own files (`leanLedger.test.js`,
  `leanLedgerCompose.test.js`, `leanLedgerStore.test.js`,
  `leanLedgerRecordCli.test.js`): all green (part of the 400/406-file green
  `npm test` run; none of these four is in the 5-file timeout list logged in
  `BL-773-qa-bounce-20260807.md`).
- Property tests, this ticket's own file
  (`test/leanLedgerInvariants.property.test.js`): 4/4 PASS.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-819-ticket-lifecycle-ledger.feature`): 12/12 scenarios
  PASS.
- Wiring: `extension/out/tools/lean-ledger-record.js` is compiled and is a
  real call site of two live scripts —
  `grep -rln "commit_integrity_cli.bb\|done_with_current_task.bb"
  swarmforge/` shows both are referenced from `swarmforge/roles/coordinator.prompt`,
  `swarmforge/scripts/swarmforge.sh`, `promote_and_route_next.sh`, and
  `done_with_current_task.sh` — not orphaned.

Everything above is genuinely clean. The inventory below is not about the
ledger's own logic — it is about the PIPELINE's handling of this ticket
after its architect bounce.

## D1 — the architect's bounce fix was never merged into the lineage that reached QA; the originally-bounced defect is still present

- **Class:** behavior (same class the architect used for the original D1 —
  see `backlog/evidence/BL-819-architect-bounce-20260807.md`).
- **Blamed role:** coder — the fix exists in git history but was dropped
  from the forwarded lineage, the exact failure mode
  "Forwarded Commits Carry Their Lineage" exists to prevent (every
  downstream role that merged this commit should have refused it on a
  failed ancestry check, but none did).
- **Evidence:**
  - The architect bounced BL-819 at `95be440a` (13:34) for missing wiring
    test coverage on the two `.bb` lean-ledger CLI call sites. Coder fixed
    it at `6a6e69b4` ("BL-819: wiring tests for the two .bb lean-ledger CLI
    call sites"), adding `swarmforge/scripts/test/test_lean_ledger_bb_wiring.sh`.
  - That fix commit is **not an ancestor** of the commit reaching QA:
    ```
    git merge-base --is-ancestor 6a6e69b4 9a8b7578   # exit 1, not an ancestor
    ```
  - The file the fix added does not exist anywhere in the reviewed tree:
    ```
    find swarmforge/scripts/test -iname '*lean_ledger*'   # no output
    git show ec25074cf6:swarmforge/scripts/test/test_lean_ledger_bb_wiring.sh
    # fatal: path does not exist in 'ec25074cf6'
    ```
  - Instead, what merged forward at `58c42419` (13:36:17) was `ef2ff7a4`
    ("BL-819: record bounce metadata on the ticket", 13:35:11) — the
    bounce-history YAML update — merged onward through hardener and
    documenter without ever picking up `6a6e69b4` (authored later, 14:05:07,
    on what the log shows as a separate `swarm/coder` line
    `d5790b79`/`c82b6254` that never rejoins the reviewed lineage). The
    ticket's own `bounce_history` entry now reads as if the bounce were
    being normally processed, but the artifact it names never arrived.
  - Net effect: `done_with_current_task.bb`'s and `commit_integrity_cli.bb`'s
    shell-outs to `lean-ledger-record.js` are, in the commit under review,
    in exactly the same zero-coverage state the architect originally flagged.
- **Remediation pointer:** coder re-applies (or re-authors) the wiring test
  from `6a6e69b4` — `swarmforge/scripts/test/test_lean_ledger_bb_wiring.sh`,
  covering the three branches `backlog/evidence/BL-819-architect-bounce-20260807.md`
  specifies (happy path, non-zero exit, missing CLI) for both call sites —
  and merges it into the branch actually being forwarded this time. Every
  downstream role (cleaner, architect, hardener, documenter) must run the
  ancestry check from "Forwarded Commits Carry Their Lineage" before
  forwarding again, so this specific loss cannot repeat silently.

## D2 — hardener's pass never ran against this ticket's own production code

- **Class:** behavior (absence-of-stage-output; Article 4.4/BL-575
  precedent — never `compile`).
- **Blamed role:** hardener.
- **Evidence:** the only hardener commit in this lineage, `518f73c1`, is
  titled "BL-773: hardener pass" and states in its own body: "Coder's
  parcel for this ticket touches only test/step-handler files ... no
  extension/src/*.ts changed, so Stryker mutation ... and CRAP/DRY ... have
  nothing new to check this pass" — describing BL-773 only. But BL-819's
  substantial new production TypeScript is present in that exact commit's
  own tree:
  ```
  git ls-tree -r 518f73c1 --name-only | grep -i leanledger
  extension/src/metrics/leanLedgerCompose.ts
  extension/src/metrics/leanLedgerComposeBounce.ts
  extension/src/metrics/leanLedgerComposeClose.ts
  extension/src/metrics/leanLedgerComposeShared.ts
  extension/src/metrics/leanLedgerComposeStageDwell.ts
  extension/src/metrics/leanLedgerComposeStageSkip.ts
  extension/src/metrics/leanLedgerComposeStall.ts
  extension/src/metrics/leanLedgerStore.ts
  extension/src/quality/leanLedger.ts
  extension/src/tools/leanLedgerRecordArgs.ts
  ```
  No Stryker report, mutation manifest, or CRAP figure for any of these
  files exists anywhere in the repo (`find . -iname '*stryker*report*'`,
  `find . -iname 'mutation.json'` both empty outside `node_modules`/the
  unrelated `mutationProgressReporter.js` tool source). Compare
  `specs/features/BL-773-...feature`, which carries a real Gherkin
  acceptance-mutation manifest header stamped by this same hardener pass —
  `specs/features/BL-819-ticket-lifecycle-ledger.feature` carries **no such
  header at all**, i.e. Gherkin acceptance mutation was never run against
  it either.
  ```
  git log --oneline --all -- extension/src/metrics/leanLedger*.ts extension/src/quality/leanLedger.ts
  29c9e1e2 BL-819: split leanLedgerCompose.ts into per-instrument modules   (By cleaner)
  3bfb4347 BL-819: coordinator-owned ticket lifecycle ledger                (By coder)
  ```
  No hardener-authored commit touches these files at all.
- **Mitigating context (not a waiver):** the architect's own pre-bounce
  review (`backlog/evidence/BL-819-architect-bounce-20260807.md`) already
  ran the full unit suite for these files (54/54) and both property tests
  (4/4) and found them sound at the design/behavior level — this item is
  specifically about the hardener's own required gate (mutation, CRAP <= 6,
  DRY, Gherkin acceptance mutation) never having been exercised, which is a
  role action, not an incidental outcome (Article 4.1 gate 3).
- **Remediation pointer:** hardener runs its normal pass — Stryker mutation
  scoped to the compiled `leanLedger*` outputs, CRAP/DRY, and the Gherkin
  acceptance mutation run for `specs/features/BL-819-ticket-lifecycle-ledger.feature`
  — once D1's wiring-test fix is back in the tree, and stamps the feature
  file's manifest header the same way it did for BL-773's.

## D3 — documenter's pass never produced this ticket's required documentation

- **Class:** behavior (absence-of-stage-output).
- **Blamed role:** documenter.
- **Evidence:** the ticket's own `notes:` field is explicit: "The intake
  asked for prompt documentation of the lean-aware duty and its boundary vs
  Swarm Optimizer / the human briefing. That is folded into this slice's own
  documenter pass rather than split into a third orphan docs ticket." The
  only documenter commit in this lineage, `ec25074cf6`, is titled "BL-773:
  document the coordinator's new role_ask clarifying-question path" and its
  diff (`git show ec25074cf6 --stat`) touches exactly
  `docs/how-to/BL-773-coordinator-role-ask-clarifying-question.md` and
  `docs/index.md` — nothing naming BL-819, the lean ledger, or the
  coordinator's new lifecycle-recording duty.
  ```
  grep -n "BL-819\|BL-818\|lean-aware\|lifecycle ledger\|\.swarmforge/lean" \
    swarmforge/roles/*.prompt docs/how-to/*.md
  # (no output)
  ```
  `swarmforge/roles/coordinator.prompt` — the exact file the ticket says
  should carry this duty's documentation — has no section describing it.
- **Remediation pointer:** documenter adds a section to
  `swarmforge/roles/coordinator.prompt` describing the lean-ledger duty (what
  it records, that it composes existing instruments rather than inventing
  new telemetry, and its boundary vs Swarm Optimizer / the human briefing,
  per the ticket's own framing), and a `docs/how-to/BL-819-*.md` or
  equivalent covering the JSONL shift-ledger + per-ticket-snapshot shape for
  anyone reading `.swarmforge/lean/` later.

## Routing

One `git_handoff` to **coder** (earliest blamed role, D1). Cleaner, architect,
hardener, and documenter must each clear the item(s) blamed on them (D2 ->
hardener, D3 -> documenter) before forwarding again, per Article 4.4 "one
bounce, many owners." Bounced alongside BL-773's separate one-item inventory
(same batch commit); see `backlog/evidence/BL-773-qa-bounce-20260807.md`.
