# BL-1356 — architect pass (2026-09-03)

## Scope

Fast-forward merge, no conflicts. Diff matches the ticket's declared scope:
the shared helper `extension/test/helpers/stampOff.js`, the five stamp-off
files it now backs (bl1113/1115/1116/1117/1136 — bl1323 already fixed
out-of-band per the ticket's own notes), the helper's own unit/property
coverage, the step handler, and removal of the five now-unneeded allowlist
rows. No production `extension/src/**` file touched.

## Ruling followed

`human_ruling`: option 1 (keep invariants on the live ledger, assert
non-mutation across the run rather than a state literal). Confirmed
`ledgerPath` is injected only in the helper's own test file
(`bl1356StampOffHelper.test.js`) to prove the gate still bites without
writing to the live ledger; every real stamp-off caller uses the default
`LIVE_LEDGER`.

## Read, not just evidence-trusted

- `assertRunWritesNoDecision`: captures the watched row before `work()`,
  runs it, then asserts (a) no decision field transitioned false→true
  (named specifically for diagnostics), (b) the row is byte-identical
  before/after, AND (c) the WHOLE ledger file is byte-identical before/after.
  The whole-file check alone is already the strongest possible non-mutation
  assertion — nothing weaker slipped in under it.
- `hotfixRow`: a missing row is `assert.notEqual(start, -1, ...)` — fails
  loud rather than silently watching an empty string forever.
- `assertParcelDoesNotEditReviewedSources`: scopes to `origin/main..HEAD`
  commits naming the stamp ticket (BL-1323's reference shape, `49fca1c741`)
  rather than a working-tree-vs-hotfix-blob comparison — matches the
  ticket's own diagnosis of why the old encoding drifted red.
- `findTicketYaml`: walks `backlog/` for `<id>-*.yaml` rather than assuming
  `active/` — fixes the ENOENT class when a ticket completes and moves to
  `done/`.

## Invariants (both declared, both non-vacuous)

- **Invariant 1** (fails iff the run changed the row): `bl1356StampOffInvariants`
  crosses all 5 states (including the two DECIDED ones — `certified`,
  `waived` — the case a state-literal pin got wrong in both directions) with
  all 6 write kinds, BY CONSTRUCTION (a full nested loop, not left to `fc`'s
  draw), plus an `fc.assert` pass. Ran 3 consecutive times — 2/2 both tests,
  no flake.
- **Invariant 2** (non-weakening: a real write still fails from any start):
  same cross-product, asserted from every starting state including decided
  ones.
- `bl1356StampOffHelper.test.js` (17/17) drives the helper directly, pinning
  both an undecided AND an already-decided starting row all still fail on
  any write.

## Verification (run, not assumed)

- `npx vitest run test/bl1356StampOffHelper.test.js` — 17/17.
- `npx vitest run --config vitest.properties.config.mjs bl1356StampOffInvariants`
  — 3 consecutive runs, 2/2 each, no flake.
- All 6 stamp-off family files (bl1113/1115/1116/1117/1136/1323) run
  together — 6 files, 12 tests, all green.
- `run_acceptance.sh specs/features/BL-1356-….feature` — 6/6.
- `property_suite_standing_allowlist.tsv` — confirmed bl1113/1115/1116/1117/
  1136 rows are gone (grep returns nothing).
- `git status --short backlog/hotfix-ledger.yaml` — clean; the runs left it
  byte-identical.
- Dependency-rule gate (BL-259), scoped to the touched files AND full-repo:
  PASSED, no forbidden edges.
- Co-change report (BL-255): all reported co-changes at frequency 1 — no
  suspected coupling.
- `required_wiring` confirmed: `bl1356StampOffWatchesTheRunSteps` registered
  in `specs/pipeline/steps/index.js`.

## Nothing else found

The design is genuinely non-weakening (byte-identical whole-file check is
the strongest assertion available) and both invariants are exercised
across the full state×write cross-product by construction, not by draw.
No architecture violation, no invariant violation, no correctness defect.
Forwarding to hardener.
