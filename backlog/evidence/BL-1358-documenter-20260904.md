# BL-1358 — documenter pass (2026-09-04)

## Received
`5931d585e5` (hardener, degraded Babashka fallback N/A — this ticket
touches `extension/src`/`specs/pipeline` JS, so Stryker applies; per
`backlog/evidence/BL-1358-hardener-pass-20260904.md`).

## Doc-domain review
Searched for an existing doc home before deciding: no how-to or reference
page documents the Gherkin mutation harness's own worker/timeout
mechanics (`docs/reference/BL-427-mutation-worker-rss-measurement.md` is
a one-off measurement report about RSS/concurrency, not the harness's
general behavior, and makes no claim this ticket touches). This is a
narrow internal fix to a CI/quality-gate worker's timeout behavior with
no operator-facing workflow — no new how-to page manufactured just to
have coverage (per the documentation-standard's own "classify, never
fill" rule).
- `docs/reference/Specification.MD`: new top-of-stack entry, covering the
  incident, the fix (`DEFAULT_MUTANT_TIMEOUT_MS`, `GHERKIN_MUTATION_TIMEOUT_MS`
  override), and both invariants.
- `docs/index.md`: no line to add or update — no how-to page.
- Diagram registry checked — no diagram depicts mutation-harness
  internals.

## Verdict
Doc content added (Specification.MD entry only — no existing page to
extend, none manufactured). Forwarding to QA.
