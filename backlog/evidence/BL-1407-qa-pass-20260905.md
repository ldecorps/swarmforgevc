# BL-1407 QA pass — 2026-09-05

BL-1407 PASSES.

## Pipeline lineage verified

Documenter's commit `c1af12e0ab` merged into the QA worktree at `ec33d197d3`.
Confirmed via `git merge-base --is-ancestor` that this ticket's own
coder (`2e6f1a3202`, `8fb4da1736`), cleaner (`b9c6e077b0`), architect
(`580f12bf17`), and hardener (`e398437712`) commits are all ancestors of
the documenter commit being cited — not merely ancestors of HEAD.

No prior bounce history for BL-1407 on `main`/`origin/main` (in sync,
0/0 divergence).

## Ticket-specific verification (independent re-run, not trusting prior stages' record alone)

- `bash swarmforge/scripts/test/test_property_suite_drift_guard.sh`:
  scenarios 01-06, 08-25 all PASS (25 total). Scenario 07 is a
  **pre-existing, already-ticketed** failure (BL-1409, confirmed by grep)
  caused by BL-1252's pre-commit delegation to `run_commit_guards.sh` —
  unrelated to BL-1407. Verified independently by temporarily replacing
  scenario 07's two `fail()` calls with a log line (never committed;
  restored via `git checkout --`, confirmed clean via `git diff --quiet`
  before continuing) — same technique the coder and architect used and
  recorded.
- `bb swarmforge/scripts/test/bl1407_property_gate_rerun_isolation_property_runner.bb`:
  45/45 runs, ALL PROPERTIES HOLD, coverage
  `{:det-red 11, :flaky-touched 11, :flaky-untouched 7, :hang 16}` —
  matches the coder's and architect's own runs.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1407-the-property-gate-reruns-a-red-in-isolation-before-it-refuses.feature`:
  4/4 scenarios PASS.
- Wiring into real caller confirmed: `run_commit_guards.sh:90` has a live
  (non-comment) `run_guard check_property_suite_drift.sh` call; the
  pre-commit hook `exec`s `run_commit_guards.sh`. Not merely unit-tested
  in isolation.
- `required_wiring` anchor present:
  `bl1407PropertyGateRerunsARedInIsolationSteps.js::registerSteps` exists.
- No bare prefix-glob temp-dir sweep in the new code; `record_flake`/
  `run_bounded` use `mktemp` + trap, consistent with BL-1385/BL-1390
  guardrails.
- `docs/how-to/BL-570-property-suite-drift-guard.md` documents the new
  flake semantics (JSONL store path, allowed-vs-refused table entry).
- qa_e2e_procedure items 1-4, 6 covered by the above. Item 5 ("live: on
  the next real load flake in any worktree...") is explicitly left to QA
  as a forward-looking live observation per the coder's own evidence —
  not fixture-reproducible in one sitting; no `.swarmforge/property-flakes/`
  records exist yet in this worktree, which is expected (no live flake has
  occurred here since this guard shipped).
- No orphaned test/mutation processes before or after this pass
  (`ps aux | grep -E 'node --test|stryker|vitest'` empty both times).

## Full unit suite (BL-1063 diligence — pre-existing reds outside BL-1407's own diff)

`npm test` in `extension/`: 588/604 files passed, 10206/10233 tests
passed. BL-1407's own commits never touch any file underlying these
failures (confirmed: `git log 2906109502..c1af12e0ab -- extension/src/tools/pilotAcceptanceGate.ts`
returns nothing; BL-1407's diff is confined to
`swarmforge/scripts/check_property_suite_drift.sh`, its shell test, a
`.bb` property runner, and one pipeline step handler).

Grepped `backlog/{paused,active,hold}` for every failing file before
reporting, per BL-1063:

**Already tracked, confirmed by grep (status: todo, human_approval: approved on each):**
- `pilotAcceptanceGate.test.js`, `crossFileDuplicationCheck.test.js`,
  `multiBranchParserCoverageCheck.test.js`,
  `perHatRolePromptEvidenceCheck.test.js`, `pilotScopedCrapCheck.test.js`,
  `shellEntryPointDriveCheck.test.js`, `unreachableStepHandlerCheck.test.js`,
  `constitutionDocCitations.test.js` (16 tests, `deps.checkOrphanedAuthoredDocs
  is not a function`) → **BL-1221** / **BL-1229**.
- `backendSwitch.test.js`, `telegramClient.test.js`,
  `telegramCursorOperatorExec.test.js` (3 tests, named site-by-site in the
  ticket) → **BL-1263**.
- `tempDirTrapGuard.test.js` → **BL-1289**.
- `socketFixtureShortRootGuard.test.js` → **BL-1290**.
- `liveRepoDerivationGuard.test.js` → **BL-1291**.
- `operatorRuntimeBbFixtureClosure.test.js` (2 tests) → **BL-1265**.

**Grepped, found no clear ticket — reporting as untracked, not investigated
further (outside BL-1407 scope), same posture as the BL-1244 precedent:**
- `test/pricingTable.test.js` — `PRICING_TABLE missing entries for
  swarm-referenced model(s): claude-fable-5-1`. Worth a specifier look;
  not raised as a new ticket here since it is unrelated to BL-1407 and a
  full triage is outside this pass's scope.

None of the above were introduced by BL-1407: its own diff never touches
`extension/src/**` or any TS test file.

## Verdict

BL-1407 APPROVED. Landing on `main`; merge-up broadcast to worktree roles;
coordinator notified for bookkeeping.

By QA.
