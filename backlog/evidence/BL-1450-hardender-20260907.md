# BL-1450 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second hardening pass, after QA's bounce (D1: `testTimeout` 120000ms
measured only against solo runs, hit twice under the real full-lane
pooled run — 126046ms, 149518ms — `backlog/evidence/BL-1450-bounce-20260907.md`)
and the coder's rework (120000 → 240000ms).

- Solo run: 55.5s, comfortably under the 60s budget and the new 240s
  timeout.
- **Reproduced QA's exact bounce scenario directly**, not just trusted the
  fix: ran the REAL `npm run test:properties` full-lane pooled run (the
  qa_e2e_procedure step 3 command QA's bounce cites). bl968 now passes at
  127465ms — comfortably under the new 240000ms ceiling, where the old
  120000ms one failed at 126046ms and 149518ms. This is the actual
  contended-pool timing regime the fix needed to hold under; solo timing
  alone (which already passed before the bounce) would not have caught
  the original defect and does not by itself prove this fix.
- The full-lane run also showed 12 other files fail — none touch BL-1450.
  Spot-checked two I had not seen in this exact shape before
  (`bl1252IndexGuardsAllRunInvariant`, `bl1324ClaudeSeatQwenCloudContextWindowInvariants`):
  both pass alone, confirming the same full-lane host-contention flake
  class already documented in this session's BL-1349 hardening pass
  (`backlog/evidence/BL-1349-hardender-20260907.md`). `bl1375ApprovedSiblingsCanLandInvariants`
  also failed, alone too this time — but that is the ALREADY-KNOWN,
  registered defect BL-1465 (`backlog/standing-reds.tsv`: invariant 2's
  assertion is stale against BL-1447's landed behaviour), confirmed by
  matching the exact failure text against the register's own description.
  No new unowned red.
- Acceptance: 5/5 pass (re-run fresh since the bounce-revert reset the
  feature file to its pre-hardening state, dropping my prior mutation
  manifest along with the rest of the reverted chain — expected, not a
  defect).
- `backlog/standing-reds.tsv`: no BL-1450 row reintroduced; confirmed
  directly (architect's own re-check flagged a merge-deletion-guard near
  miss on this exact point, independently verified here too).
- BL-113 soft gherkin mutation (re-run fresh, manifest having been reset
  by the bounce-revert): 6/6 mutants killed, 0 survived, 0 errors,
  `"outcome": "pass"`. Manifest written and committed (`ce24164a46`).
- No orphaned `node --test`/stryker/gherkin-mutator processes of mine
  remained; `git status` clean before each commit. No TS/JS production
  source touched (test-only file + acceptance step handler) —
  Stryker/CRAP/DRY N/A per architect's own pass.
- Wiring: `bl1450Bl968SensitivityFitsBudgetSteps.js` loads clean, exports
  `registerSteps` (BL-1371 suffix-discovery) — unchanged by the bounce.
