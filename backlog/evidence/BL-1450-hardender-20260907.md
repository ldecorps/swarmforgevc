# BL-1450 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- qa_e2e_procedure step 1 (3 runs alone, under 60s each): ran the file
  three times via `npx vitest run --config vitest.properties.config.mjs`.
  Run 2/3: 50s and 49s, both printing the coverage line with every
  class/depth present. Run 1: 67s wall, test `Duration` 65.54s, with the
  BL-871-allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"`
  benign error (no test failure) — a momentary host-scheduling spike, not
  a regression: re-ran the AUTHORITATIVE gate (scenario 01 via
  `run_acceptance.sh`) independently and it measured 45.3s, consistent
  with architect's own 50.9s. The coder's own comment on the timeout
  change (300000→120000ms) already discloses "one transient spike to
  110s" observed across 8 runs on this host — my 67s outlier is well
  inside that documented margin and nowhere near the 120s test timeout or
  the property guard's 180s rerun ceiling that actually matters.
- Full acceptance suite: 5/5 pass (scenario 01 45.3s, scenario 02 49.2s
  confirming the coverage line and re-derived floors, scenario 03's three
  Outline examples 7.7-8.2s each).
- No-deletion / no-weakening (invariants 1 and 2): diffed the test file
  against its pre-BL-1450 parent (`f476e976a3`, BL-1062) — only
  `RUNS_PER_CELL` (4→1), `CLASS_FLOOR`/`DEPTH_FLOOR` (now derived from
  `DEPTHS.length * RUNS_PER_CELL` / `CLASSES.length * RUNS_PER_CELL`
  instead of hand-typed 5/6), the test timeout (300000→120000), and
  explanatory comments changed. The property body, its assertions, and
  the fresh-process-per-draw shape are byte-identical.
- `backlog/standing-reds.tsv`: BL-1450's own row already removed by the
  architect's merged commit; confirmed no row remains. The BL-1462 row
  (a different, already-owned defect the coder's own e2e surfaced) is
  correctly untouched — not this ticket's to fix.
- BL-113 soft gherkin mutation (the one `Scenario Outline:`, `class`/
  `depth` columns): 6/6 mutants killed, 0 survived, 0 errors,
  `"outcome": "pass"`. Manifest written and committed (`c37dc920fc`).
- No orphaned `node --test`/stryker/gherkin-mutator processes of mine
  remained; `git status` clean before commit. No TS/JS production source
  touched (test-only file + acceptance step handler) — Stryker/CRAP/DRY
  N/A per architect's own pass.
- Wiring: `bl1450Bl968SensitivityFitsBudgetSteps.js` loads clean, exports
  `registerSteps` (BL-1371 suffix-discovery).
