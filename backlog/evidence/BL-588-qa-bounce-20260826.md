# BL-588 QA bounce — 20260826

**Commit checked:** `e2f870928` (Merge documenter `871c21b194` for BL-588)
**Task:** `BL-588-isolate-batch-recovery-trees`
**Sibling check:** `VERIFY BL-588` (exit 0); BL-653/BL-660 also VERIFY (entangled, not deferred)
**Routing:** `cleaner` (earliest blamed — rematch merge reintroduced hitchhikers and BL-1153 regression)

## Gates run (Article 4.4 — complete inventory)

| Gate | Result |
|------|--------|
| `npm run compile` | PASS |
| `required_wiring` (`bl588BatchRecoverySteps` in `index.js`) | PASS |
| `npx vitest run test/batchRecovery.test.js test/batchRecoveryCli.test.js` | 16/16 PASS |
| `npm run test:properties -- test/batchRecovery.property.test.js` | 3/3 PASS |
| APS `BL-588-isolate-batch-recovery-trees.feature` | 7/7 PASS |
| Hardener merge `0d24d73ff9` ancestor of documenter commit | PASS |
| `git diff origin/main...HEAD` tip purity (BL-506) | **FAIL** — see D1, D2 |
| BL-653 APS / operator shell (entangled, not this parcel's task name) | BLOCKED BY D1 — not verified as BL-588 scope |

## Defects

### D1 — behavior: land tip carries tickets and paths outside BL-588 (class: `behavior`) — **blame: cleaner**

1. **Failing command:**
   ```bash
   git diff origin/main...HEAD --name-only
   ```
2. **Commit hash:** `e2f870928`
3. **First error excerpt:** Parcel handoff names **BL-588 only**, but tip vs `origin/main` also includes:
   - `backlog/active/BL-653-operator-wakes-only-on-real-events-escalation-driven.yaml`
   - `specs/features/BL-653-operator-wakes-only-on-real-events-escalation-driven.feature`
   - `specs/pipeline/steps/bl653OperatorEscalationDrivenSteps.js`
   - `swarmforge/scripts/operator_{enqueue_event,lib,runtime}.bb`, `babysitter_check.bb`, BL-653 shell tests
   - `specs/features/BL-660-three-shift-packs-conf-selectable.feature` (spec-only hitchhiker)
   - `backlog/INTAKE-start-swarm-stop-swarm-cron-lifecycle.md` (raw intake in backlog root)
   - `backlog/done/M8/BL-728-*.yaml` stripped of landed QA metadata (see D2)
4. **Failure class:** `behavior`
5. **Expected vs observed:** Expected BL-588 approval to land only batch-recovery deliverables (BL-506). Observed multi-ticket batch residue from rematch merge `563124fd45` / hardener batch pass — would land BL-653 operator topology and backlog churn with a BL-588-only coordinator notify.

**Remediation:** Re-cut BL-588 tip on current `origin/main` with only BL-588 paths; forward BL-653 (and any other satisfied ticket) as its own `git_handoff` per Article 2.6. Drop INTAKE from tracked backlog; restore `backlog/done/M8/BL-728-*.yaml` to `origin/main` content.

### D2 — behavior: residentSpyUiHtml reverts landed BL-1153 (class: `behavior`) — **blame: cleaner**

1. **Failing command:**
   ```bash
   git diff origin/main...HEAD -- extension/test/residentSpyUiHtml.test.js
   ```
2. **Commit hash:** `e2f870928`
3. **First error excerpt:** `origin/main` has `BL-1153: Live Screen pane font size survives a full Mini App reload` (landed `000a8c8706`). Tip **deletes** that test and reverts BL-1046/BL-929 tile assertions (`assert.doesNotMatch` → `assert.match` for held ticket on tile head).
4. **Failure class:** `behavior`
5. **Expected vs observed:** Expected no regression of landed BL-1153/BL-1046 contracts. Observed conflict resolution during BL-588 rematch kept stale pre-BL-1153 `residentSpyUiHtml.test.js`.

**Remediation:** Restore `extension/test/residentSpyUiHtml.test.js` from `origin/main` (or merge BL-1153 host-persisted font wiring without dropping BL-1153/BL-1046 tests).

## Inventory

D1 (cleaner), D2 (cleaner). Bounce to **cleaner** (earliest).

By QA.
