# BL-1480 — cleaner review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Merged coder commit `63c9d39179` into `cleaner` (`20c8cc40be`, no conflicts).
- Ran both standing fixtures directly: `test_promote_and_route_next_priority.sh`
  and `test_promote_and_route_next_no_limit_depth.sh` — exit 0/0.
- `run_acceptance.sh` on BL-1480's own feature — 7/7 pass.
- Regression: `run_acceptance.sh` on BL-973's and BL-1279's features —
  13/13 and 10/10 pass, no regression from the multi-entry-point
  `FIXTURES.entry` change in `bbFixtureClosureGate.js`.
- Reviewed the diff on both fixtures, the gate lib, the new step handler,
  and the new `deprecate_check_allow_stub.sh`: `promote_and_route_next.sh`,
  `promotion_gates_cli.bb` and the libs are untouched, matching the FIRM
  constraint in `human_approval`. No hand-typed `.bb` copy list remains
  (`grep -c 'cp "\$SCRIPTS/.*\.bb"'` is 0 on both fixed fixtures).
  `bl1480PromoteRouteFixtureClosureSteps.js` validates Examples against
  explicit `KNOWN_FIXTURES`/`KNOWN_ENTRIES`, discards its scratch roots in
  a `finally`-equivalent path on every scenario branch, and never mutates
  the live repo (scratch copies only).
- Mutation-Site Size (BL-485): `node extension/out/tools/mutation-site-count.js`
  on the two changed/added JS files reports `bl1480PromoteRouteFixtureClosureSteps.js`
  at 165 sites and `bbFixtureClosureGate.js` at 149 (both `over` the 100
  advisory threshold; the gate lib was already 135/`over` before this
  change). Checked against precedent: the directly analogous
  `bl1279FrontDeskFixtureClosureSteps.js` is 154/`over` and was never
  split. Both files are single-responsibility (one step handler per
  feature per BL-233; one shared closure-gate module) — a mechanical split
  here would only fragment a cohesive unit to duck the count, not improve
  structure or separation of concerns, so left whole per this ticket's
  advisory-not-block guidance.
- No jscpd/Stryker gate applies: `extension/.jscpd.json`'s pattern is
  `**/*.ts` only; `specs/pipeline/steps/**` is JS test infrastructure
  covered by its own acceptance suite, same posture as BL-973/BL-1279.
- No architectural concerns: adapters (bash fixtures) depend inward on the
  shared `bb_closure_copy.sh`/`bbFixtureClosureGate.js` policy, no new
  coupling introduced.
