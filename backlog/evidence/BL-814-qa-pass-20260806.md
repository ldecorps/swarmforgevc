# BL-814 — QA pass

Reviewed commit (this parcel): `2cd01798` (merge of documenter's `8497c39354`
into QA — documenter received unchanged from hardener's `7575c9c3`, which
received unchanged from architect's `a03b863d18`, which received unchanged
from cleaner's `5a95b3d0` (a pure merge, no diff of its own), which received
coder's `d147e5c3c9`).
Ancestry check:
```
git merge-base --is-ancestor d147e5c3c9 HEAD   # coder    -> OK
git merge-base --is-ancestor a03b863d18 HEAD   # architect -> OK
git merge-base --is-ancestor 7575c9c3   HEAD   # hardener  -> OK
git merge-base --is-ancestor 8497c39354 HEAD   # documenter -> OK
```
Coder/architect/hardener/documenter work is confirmed inside the commit this
evidence approves.

## Verification Order — sibling check

`node extension/out/tools/qa-sibling-check.js status --ticket BL-814` ->
`VERIFY BL-814` (exit 0). No open deferral; no prior QA bounce for BL-814
exists on `main` (`git log --oneline main -- 'backlog/evidence/BL-814*'` is
empty) — this is the first QA pass.

## Process hygiene

`pgrep -fl 'node --test|stryker'` clean before this pass and reconfirmed
clean after every check below. No orphaned test/mutation processes.

## Compile

`npm run compile` (extension/) — clean.

## Host load — severe, and it shaped how this pass was run

`uptime` ranged 57-144 on a 4-core host across this pass (14x-36x cores),
well past the documented "do not even attempt mutation" threshold and high
enough to make the full `npx vitest run` unreliable as a single command: two
independent full-suite runs each timed out a small, DIFFERENT set of
subprocess-heavy tests (`dependencyGateCliReportsAndScope.test.js`,
`dependencyGateCliStorageGlobals.test.js`, `renderBriefingDiagramsCli.test.js`
first run; a different pair plus one more on the second run), none of them
touching this parcel's changed files
(`conciergeTick.ts`/`telegram-front-desk-bot.ts`). Re-run in isolation with
more headroom, all of them passed cleanly:

```
$ npx vitest run test/dependencyGateCliReportsAndScope.test.js \
    test/dependencyGateCliStorageGlobals.test.js --testTimeout=120000
 Test Files  2 passed (2)
      Tests  8 passed (8)
```

Confirmed load-induced timeout flakiness in unrelated pre-existing
subprocess tests (each individually takes 17-19s against this repo's real
`dependency-gate.js` checker, close to the default 20s test timeout even
without contention) — not a regression introduced by this parcel. This
exact class of problem (parcels meeting a red full-suite run under host
overload) is its own tracked, out-of-scope ticket per BL-814's own `notes`
(BL-816, "the process question of parcels proceeding past a red npm test").
Given that, this pass verifies BL-814's own changed surface directly and
targetedly rather than leaning on one flaky full-suite invocation.

## Unit tests — BL-814's own changed/added test files, run directly

```
$ npx vitest run test/readLiveRoleHeldTicketsCli.test.js \
    test/conciergeTick.test.js test/telegramFrontDeskBotCli.test.js
 Test Files  3 passed (3)
      Tests  365 passed (365)
```

Matches hardener's own reported counts (119/119 on the first two files,
607/607 on the broader `telegramFrontDeskBotCli.test.js` — this run's total
differs only because it's the intersection of exactly these three files
rather than hardener's two separate invocations).

## Property tests

```
$ npx vitest run --config vitest.properties.config.mjs \
    test/telegramFrontDeskBotCli.property.test.js \
    test/onboarderRedeliveryIdempotent.property.test.js
 Test Files  2 passed (2)
      Tests  2 passed (2)
```

Matches hardener's reported 2/2. The full property config run (`npm run
test:properties`, no file filter) hit the same load-induced timeout pattern
as the full unit run (3 failed files, all unrelated to this parcel's diff);
not re-litigated here since architect's evidence already confirmed no new or
touched pure/property-shaped module in this parcel's diff, and the two files
that are actually relevant pass cleanly above.

## Acceptance (BL-112) — the ticket's own `acceptance:` field

```
$ specs/pipeline/scripts/run_acceptance.sh \
    specs/features/BL-814-live-role-held-fixture-loud-degrade.feature
# tests 6
# pass 6
# fail 0
```

6/6 (Scenario Outline 03 expands to 3 examples, one per non-leaf load-file
dependency deleted). My own independent run.

Also re-ran the sibling feature whose fixture list this parcel touched:

```
$ specs/pipeline/scripts/run_acceptance.sh \
    specs/features/BL-487-board-freshness-without-coordinator-sync.feature
# tests 2
# pass 2
# fail 0
```

## required_wiring — both re-checked directly against source

1. `extension/test/readLiveRoleHeldTicketsCli.test.js::mono_router_lib.bb` —
   `REQUIRED_SCRIPT_FILES` includes `mono_router_lib.bb`; both original
   BL-487 tests (`reports a role-held ticket computed LIVE...`, `a
   stale/absent ticket-stage-map.json cache is irrelevant`) pass in the run
   above.
2. `specs/pipeline/steps/index.js::bl814LiveRoleHeldLoudDegradeSteps` —
   `require('./bl814LiveRoleHeldLoudDegradeSteps')` present in the `DOMAINS`
   array; confirmed executing, not merely present, by the 6/6 acceptance run
   above (an unregistered module would throw on scenario 01 per the ticket's
   own wording).

## Production wiring — confirmed myself, not taken on trust

`grep -rn "readLiveRoleHeldTickets\b" extension/src extension/test` shows
exactly one production call site: `buildConciergeTickAdapters` wires
`readRoleHeldTickets: () => readLiveRoleHeldTickets(targetPath)`, consumed by
`syncBoardIfWired` in `conciergeTick.ts`, which now wraps the call in
try/catch and returns `{ state: prevBoard }` on failure. No other caller in
the tree assumes the old `{}`-on-failure contract.

## Invariants (BL-633/BL-654) — reviewed against the actual diff

1. "A copy-real-scripts fixture whose dependency set is incomplete fails
   loudly; it never reports a passing-shaped empty result." — Scenario
   Outline 03 (3 examples) plus the mirrored unit-test loop in
   `readLiveRoleHeldTicketsCli.test.js`, all green above. Confirmed
   non-vacuous: the diff shows the pre-existing "CLI missing" / "garbage
   stdout" tests flipped from `assert.deepEqual(result, {})` to
   `assert.rejects(..., RoleHeldTicketsComputationFailedError)` — a real
   behavior change, not a newly-added tautology.
2. "readLiveRoleHeldTickets distinguishes 'no role holds a ticket' from
   'the computation did not run'." — Scenario 04 (genuinely-empty case)
   still asserts a bare empty map, no exception; Scenario 03 asserts the
   opposite shape. Both directions exercised.

## What this ticket did (intent match)

Both required parts of the two-part fix are present and independently
verified: (1) the fixture copy list carries `mono_router_lib.bb`, restoring
the two originally-red BL-487 tests; (2) `readLiveRoleHeldTickets` now
throws `RoleHeldTicketsComputationFailedError` on subprocess or parse
failure instead of returning `{}`, and the sole production caller catches
it, logs it, and preserves the prior tick's board state rather than
blanking it. The `out_of_scope` note (do not widen the copy list to
"everything") is honored — `REQUIRED_SCRIPT_FILES` lists exactly five named
files, and the new Scenario Outline/unit-test loop proves each of the three
non-leaf ones is load-bearing (deleting any one breaks the fixture).

## Approval scope — diff confined to this ticket

`git diff --stat e3a894da HEAD` (my last QA-approved commit before this
parcel, through this merge) touches exactly: the ticket's own yaml
(acceptance path fixed to the non-draft name), the two production/test
source files, the two new evidence files (architect, hardener), the
renamed feature file, the new/touched step-handler files, the step registry,
and the documenter's changelog entry. No ticket-less functional file is
included.

## CRAP / DRY — hardener's findings reviewed, not re-run (QA does not run mutation)

`syncBoardIfWired` CRAP 6.00 (at, not over, threshold); `readLiveRoleHeldTickets`
CRAP 3.06. Hardener deferred full Stryker under the same severe host-load
conditions observed independently in this pass (see Host load section above)
and substituted a manual mutation review plus a scoped coverage run — read
and found sound: each new try/catch branch has a dedicated
`instanceof`-asserting test, and the `syncBoardIfWired` catch-and-return path
is a structural (non-compiling-if-removed) kill, not merely a test-dependent
one. No new duplication (`npm run dry` reviewed by hardener, nothing in the
changed region).

## Untracked file surfaced, not swept

`swarmforge/scripts/operator_path_lib.sh` (untracked, pre-existing, unrelated
to this ticket) is present in this worktree. Confirmed not part of this
parcel's diff (`git diff --stat e3a894da HEAD` above); left unstaged and
unswept per BL-506 discipline (same file already surfaced in BL-807's and
BL-811's QA evidence).

## Verdict: PASS

Every automated gate this pipeline can run is green for this parcel's own
changed surface, independently re-run at this stage. Full-suite/full-property
invocations were unreliable only because of severe, unrelated host overload
(14x-36x cores) — confirmed by isolating and re-running exactly the files
that timed out, all of which passed with headroom and none of which touch
this parcel's diff. Both `required_wiring` entries, the production call site,
and both ticket-declared invariants were confirmed by reading the actual
source and re-running the actual tests, not merely trusted from upstream
evidence.

By QA.
