# BL-746 — QA bounce — 2026-08-14

## D1 — architect design-review gate never ran; no documented skip

1. **Failing command**: not a test command — an ancestry check for an
   architect-authored contribution between the cleaner and hardender
   stages:
   `git log --oneline --ancestry-path 7f30ede4d..53d75a8d3`
   (`7f30ede4d` = coder's BL-746 commit; `53d75a8d3` = the hardener's own
   merge commit that starts the batch's hardening pass, shared with
   BL-891 — see `BL-891-qa-bounce-20260814.md`, same root cause).

2. **Commit hash checked out and tested**: `d3d03757a` (documenter's
   forward to QA, `merge_and_process documenter d3d03757a6`), merged into
   this worktree as `f7f880cb5`.

3. **First error excerpt** — full ancestry-path output:

   ```
   53d75a8d3 Merge commit 'f912278236' into swarmforge-hardender
   f91227823 Merge commit 'e0408b8628' into swarmforge-cleaner
   b43ceb8fb Merge commit '7f30ede4d9' into swarmforge-cleaner
   e0408b862 BL-892: durably commit every automated human_approval verdict
   ```

   `7f30ede4d` (coder's BL-746 commit) is merged straight into
   `swarmforge-cleaner` (`b43ceb8fb`), and the cleaner's branch is merged
   straight into `swarmforge-hardender` (`f91227823` → `53d75a8d3`) — no
   commit or merge names `swarmforge-architect` anywhere in the range, and
   no architect-authored commit exists. This is the identical gap already
   found and bounced for BL-891 in the same batch: the documenter's own
   `6be43385b` ("BL-746: flag missing architect evidence, routing gap for
   coordinator", committed 12:12) surfaced exactly this absence and asked
   the coordinator to route BL-746 through architect or record a
   legitimate skip — but BL-746's next commit in this chain
   (`d3d03757a`, the documenter's doc pass, forwarded to QA) sits directly
   on top of the flag commit with no architect commit inserted in between.
   The gap the documenter raised was never closed before reaching QA.

4. **Failure class**: `behavior` — absence of required stage output, not a
   code defect; same routing rule as BL-891 (Article 4.3, BL-575
   precedent): ownership drives routing, not failure class.

5. **Expected vs observed**: Expected — an architect-authored
   commit/merge between the cleaner and hardener stages (Article 4.1 gate
   2), or a `stage_skip_reasons.architect` entry in
   `backlog/active/BL-746-bl637-lifecycle-shell-test-reimplements-instead-of-driving-real-script.yaml`
   justifying a skip. Observed — no `required_stages` override and no
   `stage_skip_reasons` field exist in that YAML at all (defaults to the
   full pipeline, architect included), no architect evidence file exists
   for BL-746 anywhere in `backlog/evidence/`, and the documenter's own
   `6be43385b` already flagged this exact gap upstream of the commit QA
   received.

## Everything else checked — no other defects found

- `bash swarmforge/scripts/test/test_lifecycle_script_scope.sh` — 15/15
  PASS, including the new `06: stop path refuses a clean report when the
  pipeline kill exited non-zero` scenario (re-run independently by QA).
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-746-stop-swarm-real-refuse-gates.feature` — 5/5 PASS
  (re-run independently by QA).
- Both `required_wiring` literals confirmed present verbatim in
  `swarmforge/scripts/test/test_lifecycle_script_scope.sh`:
  `"full stack SUCCESS — no known survivors"` (line 198) and
  `"REFUSE: pipeline stop exited 7"` for the `kill_rc` scenario (line 208).
- Full extension unit suite (428 files / 7585 tests) green — not scoped to
  this ticket's shell/Gherkin files, but confirms no collateral TS
  regression from the shared batch commit.
- No orphaned `node --test`/`stryker` processes before or after QA's runs.

The implementation itself is sound and independently re-verified by QA —
this mirrors BL-891 exactly. The sole blocker is procedural: the mandatory
architect gate was never run and never documented as skipped, despite the
documenter already surfacing the gap one commit upstream of what QA
received.

## Remediation pointer

Architect: review coder's `7f30ede4d` (BL-746: rewrite stop-path tests to
drive the real `stop-swarm.sh`) per Article 1.5 — the fixture technique
(byte-identical runtime copy of `stop-swarm.sh` resolving helpers via its
own `SCRIPT_DIR` seam), the new `kill_rc` refuse scenario's exact
exit-code propagation, and the scope split between the rewritten stop-path
blocks and the untouched scanner-unit cases (04a/05a/04b). Either record a
normal architect pass (evidence file, forward to hardener) or, if design
review is genuinely unnecessary, record `stage_skip_reasons.architect` in
BL-746's own YAML with a stated reason and forward to hardener — no code
changes are requested by this bounce.

By QA.
