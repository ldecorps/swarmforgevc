# BL-1382 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made beyond the merge-conflict
resolution below.

## Merge conflict resolution

`swarmforge/scripts/test/suite-manifest.tsv` conflicted purely additively
(two new rows: `test_bl1382_unmarked_cron_lines_survive.sh`,
`test_bl1382_cron_ownership_agreement.sh`). Confirmed both files exist on
disk, kept both rows, restored the file's trailing-double-tab convention.

## What was checked (extra care given this ticket is a data-safety fix —
the human's own hand-installed schedule was erased overnight)

- Read `swarmforge_cron_lib.sh::swarmforge_cron_line_belongs_to_root` end
  to end: the three path clauses that caused the incident
  (`$root/.swarmforge/operator/`, `$root/start-swarm.sh`,
  `$root/stop-swarm.sh`) are gone — ownership is now markers only
  (freshness marker, operator-schedule marker, shift-schedule begin/end,
  `FRESHNESS_ROOT=$root `), matching the human's ruled option 1 verbatim
  (`human_ruling:` in the ticket YAML).
- The shift-schedule begin/end checks are bounded on `$root` ending the
  marker or being followed by whitespace/`]` — a bare substring match
  would let root `/a` claim a sibling `/a-sibling`'s block; the comment
  attributes this fix to the coder's own invariant-1 property test finding
  it, and `test_bl1382_cron_ownership_agreement.sh`'s "no sibling root's
  line is claimed" case exercises exactly that boundary.
- `reconcile_shift_schedule_crontab.bb::strip-schedule-lines` read
  alongside the shell predicate: same marker set, explicitly commented as
  mirroring `swarmforge_cron_line_belongs_to_root` (invariant 2, BL-897).
- `swarmforge_cron_report_unmarked` (built on the new
  `swarmforge_cron_line_names_root`) is wired into BOTH
  `uninstall_swarmforge_crons.sh` and `install_shift_schedule_cron.sh` —
  an unmarked line naming the root is reported "left in place" on both
  paths per the ruling, not swept silently.
- `swarmforge/scripts/test/test_bl1382_unmarked_cron_lines_survive.sh`:
  11/11 PASS — the ticket's own qa_e2e steps 1-4 almost verbatim: an
  unmarked operator-script line and an unmarked scripts-dir line both
  survive a full-stack uninstall AND a recognized-mode install
  byte-identical; a sibling root's marked line is untouched; a line
  carrying the operator-schedule marker IS still removed; both paths
  report the unmarked line as left in place; every check runs against a
  fixture crontab, never the live one (grepped the file for any bare
  `crontab -l`/`crontab -` call outside the fixture shim — none).
- `swarmforge/scripts/test/test_bl1382_cron_ownership_agreement.sh`: 6/6
  PASS — both readers (shell predicate, bb strip) agree on a shared
  corpus for two different roots, an unmarked line is not owned however it
  names the root, every swarm-marked line for the root IS owned, no
  sibling root's line is claimed, and the suite's own non-vacuity case
  (editing one reader's marker list without the other) fails as expected
  — proving the agreement check isn't tautological.
- `bl1382CronOwnershipMarkerOnly.property.test.js`: 3/3 PASS, including
  invariant 2 (shell predicate and bb strip agree on every generated
  line).
- `bl1162_swarmforge_cron_property_runner.sh` (BL-1162's own pre-existing
  regression suite, since this ticket changes a function BL-1162 built):
  14/14 PASS — multi-root isolation, `root_has_lines`, and the filter
  behavior BL-1162 established are all still correct, plus the new
  "orphan operator path does NOT belong" / "is reported as left in place"
  cases.
- No `required_wiring` section on this ticket (deliberately absent per the
  ticket's own `notes:` — both predicates are already on live call paths,
  verified above; an anchor on the parcel's own edit would be satisfied by
  construction).
- `jscpd` over the four touched/new shell, Babashka and JS files: 0
  clones.
- No prior bounce history for BL-1382 (mint → ruling recorded → approve →
  promote → topic → coder, no adjudicate-again entries). `main`/`origin/main`
  in sync (`0 0`).
- All touched/new files here (shell, Babashka, one hand-authored JS step
  handler) are not compiled from TypeScript — `mutation-site-count.js`
  does not apply. The one new TS-adjacent artifact,
  `bl1382CronOwnershipMarkerOnly.property.test.js`, is itself a hand-authored
  test file (not production code) and compiles/runs clean via `tsc`/vitest.

No architecture concerns: the fix narrows an existing predicate to what the
swarm itself writes rather than introducing new structure, and the
report-unmarked helper is a small, single-purpose addition reused
identically at both call sites.

By cleaner.
