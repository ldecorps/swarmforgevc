# BL-1460 — hardender review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Merged architect 35422e7586 (fast-forward). Reviewed the diff: coder
  commit 732504cf64 seeds `lastSnapshot` on `/events` connect; cleaner
  a442773434 (evidence-only, no code); architect commit 9d0dbec423
  recorded a clean NONE pass.
- BL-149 cooldown gate on the changed production file
  (`extension/src/bridge/bridgeServer.ts`): `mutation_cooldown_gate.bb`
  returned `DECISION: run` (file age 4.98d > 1d cooldown, load 1.06 on 20
  cores, quiet). `uptime` load average stayed well under 2x cores
  throughout the pass. No orphaned `node --test`/stryker processes before
  or after (`pgrep -fl`).
- **Caught and self-corrected a stale-`out/` false read (BL-497).** First
  run of `npx vitest run test/bridgeServer.test.js` (before recompiling
  after the merge) showed the new idle-snapshot scenario FAILING
  deterministically (5/5 reproductions, 2 data frames vs expected 1) —
  contradicting the architect's evidence of 102/102 green. Traced to a
  stale `out/bridge/bridgeServer.js` predating the merged TS change
  (acceptance/unit tests require the compiled artifact, `out/` is
  gitignored and not carried by a merge). `npm run compile`, re-ran: clean
  102/102, and the scenario passed 20/20 direct + 20/20 via
  `-t "periodic keepalive comment frame"`. Recorded here because it is
  exactly the trap the engineering rules name — a defect-shaped signal
  that was actually an environment gap, not a regression in the parcel.
- **Confirmed the hand-mutation kill claimed by the architect**, independently:
  removed the `lastSnapshot = snapshot;` seed line, recompiled — both the
  property test (`bl1460IdleEventsOneSnapshotInvariants.property.test.js`,
  fast-check counterexample `[[true,true]]`, 3 vs 2 assertion) and the unit
  scenario (`exactly one data frame across many poll ticks`) went red.
  Restored the source, recompiled, reconfirmed clean (102/102, working
  tree clean via `git status --short`).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1460IdleEventsOneSnapshotInvariants.property.test.js`: 2/2 green
  (property lane kept separate from the unit run, per the shared
  engineering rule).
- Acceptance: `node specs/pipeline/cli.js
  specs/features/BL-1460-an-idle-events-connection-gets-one-snapshot.feature`:
  4/4 scenarios pass, matching the architect's evidence.
- CRAP on `src/bridge/bridgeServer.ts` (coverage scoped to
  `bridgeServer.test.js`, the file's own exercising suite): the touched
  functions score `resolveEventsSnapshot` complexity=2/CRAP=2.00,
  `broadcastSnapshotIfChanged` complexity=3/CRAP=3.00, `startBridge`
  complexity=4/CRAP=4.00 — all well under the <=6 threshold, and none of
  their complexity numbers changed by this diff (a single assignment
  added to an existing branch-free connect handler, no new branch).
  31 pre-existing functions elsewhere in this hub file exceed CRAP 6 —
  none touched by this parcel's diff (differential complexity gate: no
  regression on any function this ticket changed).
- DRY: not run standalone — the diff is one assignment plus a four-line
  comment inside an existing function; no new duplication is plausible at
  this size, and `dependency-gate`/`co-change-report` already ran clean in
  the architect pass with no files this parcel would need to touch again.
- Standing whole-tree guards (this parcel adds files under
  `specs/pipeline/steps/` and `extension/test/`, so Article-4.4-adjacent
  BL-631/BL-945 class applies): ran all 18 current
  `test/*Guard*.test.js` files (grew from 6 to 18 since that rule was
  written) — 183/183 green, including `tmuxReaperGuard`,
  `tmpDirMigrationGuard`, `bl968StepRegistryMaterializedTreeGuard`,
  `bl1277UnscopedStepCollisionGuard`.
- No functional behavior was added by this hardening pass itself (the
  parcel arrived already hardened by the coder/architect); no new tests
  were added here beyond the independent mutant re-verification above.
- Forwarding this commit unchanged in substance from what architect
  handed off — only this evidence file was added.

By hardender.
