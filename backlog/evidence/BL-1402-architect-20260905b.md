# BL-1402 — architect re-review after bounce 1, 2026-09-05

Bounce 1 defect fixed correctly. Clean sweep; forwarding to hardener.

## Bounce 1 defect verified fixed

`persistPhotoIfRouted`'s gate is now an allowlist
(`decision.action === 'post-existing' || decision.action === 'operator-context'
|| isOpenDecision(decision)`) instead of the leaky `!== 'drop'` exclusion —
confirmed at the exact line the bounce named. This matches the allowlist
shape my bounce's remediation asked for, and fails safe against any future
decision kind (unrecognized = don't persist, never persist-by-default).

## Checks run

| check | result |
|---|---|
| `dependency-gate.js` on the changed file | PASSED: no forbidden edges |
| `co-change-report.js` on the changed file | no pair at or above threshold |
| `test/telegramFrontDeskBotCore.test.js` (2 new regression tests) | 459/459 |
| `test/bl1402FrontDeskPhotoPassthrough.test.js` | 9/9 unchanged |
| `test/atomicWrite.test.js` | 7/7 unchanged |
| property `bl1402FrontDeskPhotoPassthroughInvariants.property.test.js` | 3/3 unchanged |
| acceptance `BL-1402-...feature` | 6/6 unchanged |
| `npm run compile` | clean |

## New regression tests reviewed

Read both new tests in `telegramFrontDeskBotCore.test.js`
(`BL-1402 architect bounce 1: persistRoutedPhoto is never called for an
approvals-topic reply carrying a photo` / `...a recert-topic reply...`):
each drives the real dispatch with a spy `persistRoutedPhoto`, asserts zero
calls, and separately asserts the reject/validate itself still records
normally — non-vacuous (the coder's evidence shows both fail against the
pre-fix gate) and reachable through the real `subjectForTopic`/decision
path, not a hand-rolled shortcut.

## No new violations found

No architecture violation, no invariant violation, no correctness defect
spotted on this pass. Forwarding to hardener with the same task name and
the received commit.

By architect.
