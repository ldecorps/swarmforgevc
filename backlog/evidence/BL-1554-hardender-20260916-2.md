# BL-1554 — hardender review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Third hardening pass on this ticket (QA bounced a real defect D1: the
register join compared vitest's bare `test/...` output path against the
register's repo-root-relative `extension/test/...` rows, never matching,
silently reporting every real unit/properties failure `absent`).

- Independent hand-mutation of the coder's fix (`toRepoRootRelative`):
  reverting to a no-op (pre-fix shape) - killed hard, 5 failures,
  reproducing the exact QA-found symptom. A double-prefix mutant
  (dropping the `startsWith('extension/')` idempotency guard) - also
  killed by an existing test (1 failure).
- 37/37 unit tests green; CRAP clean (no function over 6,
  `toRepoRootRelative` itself 100% covered at CRAP 2.00); jscpd 0
  duplicates.
- Acceptance: `specs/features/BL-1554-*.feature` 10/10 scenarios pass,
  including the new scenario reproducing the bare-path/register-row
  mismatch directly (scenario "a bare unit/properties vitest path is
  still joined to the register's extension/-relative row").
- No orphaned processes; git status clean.
