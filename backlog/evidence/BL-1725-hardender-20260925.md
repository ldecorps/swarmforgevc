# BL-1725 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `extension/test/bl1650LandStepPureEvidenceStrayInvariants.property.test.js`
only — test-speed chore, no production code touched; CRAP/Stryker not
applicable.

Checks run (host load ~15-24 on 20 cores):
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1650LandStepPureEvidenceStrayInvariants.property.test.js` (from
  `extension/`): 2/2 passed, 4.38s total (invariant 2 ~2.92s) — well under
  half of the 10.8s measured alone at load 12.4, matching the ticket's
  expectation.
- Floor audit (qa_e2e step 2): `git diff main...HEAD --
  extension/test/bl1650LandStepPureEvidenceStrayInvariants.property.test.js
  | grep -E '(numRuns|assertReachFloor|runsPerCell)'` — `runsPerCell` and
  `assertReachFloor` lines unchanged; only the `{ numRuns: RUNS }` line is
  removed (structural — the per-draw `fc.property` wrapper was replaced
  by `fc.sample` + a shared-process loop, same `RUNS` constant carried
  through). No value lowered.
- `node specs/pipeline/cli.js specs/features/BL-1650-the-land-step-lands-a-pure-evidence-closed-owner-stray-itself.feature`:
  8/8 ok.
- `git diff --stat f93ebdf4af..HEAD -- <the one Scope file>`: only this
  file touched.

Independent re-verification of the CLI-wiring FIRM constraint and the
non-mock-assertion claim: read `runCli`/`landPlans` and confirmed the
wiring draw (`draws[0]`) still runs the real `bb land_step_cli.bb`
end-to-end (argv, stdout parsed for `LAND_REPLAY`/
`LAND_STRAY_EVIDENCE_LANDED`), and every draw's post-condition reads real
`git log` output on the actual fixture repo (`-x` trailer check) — never
a JS-side simulation of the decision.

Hand-verified the per-draw nonce fix's non-vacuity myself, independent of
the architect's read (they reasoned about it; I reproduced it): stripped
the `drawId` interpolation from the incident content/commit messages in
place and ran the file three times. 2 of 3 runs FAILED —
`plan.action` came back other than `'replay'`/no matching stray entry —
exactly the collision race the architect's evidence describes (two draws
of the same shape picking the same path subset off the same seed produce
an identical tree/sha, colliding with land-plan's replay-scratch key).
Restored the original file (`git status --short` clean); re-ran three
times green to confirm the restore, not just once.

Fixture hygiene: `fixtures.map(...).forEach(fs.rmSync(...))` in the
test's own `finally` swept every shape's shared root after each of my six
extra runs (mutant x3, restored x3) — `git status --short` clean
throughout, no leaked `bl1650-property-*` directories.

Not run: `npm run test:properties` in full (2026-09-17 rule — only this
one file, no other `*.property.test.js` in Scope) and Stryker/CRAP (no
`src`/`out` file touched).

By hardender.
