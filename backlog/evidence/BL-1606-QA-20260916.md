# BL-1606 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1606`: `VERIFY BL-1606`. `qa_hold_cli.bb . status`: no open hold.
- Straggler check before and after: no orphaned `node --test`/`stryker`/`vitest` processes belonging to this worktree (a live `stryker` run belongs to the `hardender` worktree, confirmed by full command line).
- `required_wiring`: `bl1606TwoMorePropertyFilesFitAFullLaneSteps.js::registerSteps` present and exported. Both files wired to the helper: `bl1304DryRunSpawnsNothing.property.test.js` and `bl968MaterializedGuardSensitivity.property.test.js` both `require`/call `propertyLaneTimeoutMs`.
- Acceptance: `BL-1606-*.feature` 10/10 scenarios pass.
- Invariant 2 (diff scope): `git diff b71cedfcf3..f5907751cc -- extension/test/bl1304DryRunSpawnsNothing.property.test.js` shows exactly one added `require` line and three `});` → `}, propertyLaneTimeoutMs(20000));` changes, nothing else — confirms no draw count, reach floor or assertion changed.
- Worked example: `node -e "...propertyLaneTimeoutMs(240000,{forksFn:()=>9,loadavg1mFn:()=>1.8})..."` → `540000 40000`, matching the ticket's own worked example exactly.
- bl1304 solo run 3/3 green (~11s each). bl968 solo run green within the property suite's own full-lane run (202687ms, comfortably under its new headroom).
- Register/allowlist: both BL-1606-owned rows already removed by the coder's own commit (confirmed in the merge diff); `standing_red_register_cli.bb` reports 3 rows, none unowned; `property_suite_standing_allowlist.tsv` carries no row for either file.
- Full unit suite (`npm test`): 623/624 test files green; the one failure (`bl1277UnscopedStepCollisionGuard.test.js`) is the pre-registered, owned standing red (BL-1607), unrelated to this ticket.
- Property suite (`npm run test:properties`): 412/412 test files, 1214/1214 tests green, including both previously-timing-out files now solidly green; 5 `Unhandled Error` entries, all the allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871).
- Hardener's self-caught mutation gap (D1, test-coverage-gap) was fixed in the same stage before forwarding; no unfixed defect remains.
