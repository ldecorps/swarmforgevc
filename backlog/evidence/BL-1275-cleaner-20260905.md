# BL-1275 — cleaner pass, 2026-09-05

## Review

`swarmforge/scripts/check_property_suite_drift.sh` gains four small, single-
purpose functions (`refusal_log_keep`, `next_refusal_index`,
`prune_refusal_logs`, `retain_refusal_log`) plus one EXIT-trap cleanup
(`cleanup_suite_out`). Each does one thing; the retention/pruning/naming
concerns are cleanly separated rather than inlined into the refusal branch.

The two `for name in "$dir"/refusal-*.log; do [[ -e "$name" ]] || continue`
loops in `prune_refusal_logs` (count pass, then delete pass) and the third
copy in `next_refusal_index` are the same three-line glob-guard idiom
repeated three times. Left alone: bash has no clean way to factor a "for each
matching file" callback without losing readability, and the loops do
genuinely different things with the match (index-extract vs count vs
delete-with-cutoff) — three similar lines, not a premature abstraction
candidate.

No production code beyond this script changed. `specs/pipeline/steps/
bl1275PropertyGuardKeepsRefusalEvidenceSteps.js` and `extension/test/
bl1275RefusalEvidenceInvariants.property.test.js` are both single-responsibility,
drive the real script/functions (no reimplementation of the rule under
test), and match the shape of neighbouring BL-570-style handlers. No
duplication introduced. Bash is outside the CRAP/mutation/DRY tool wiring
(TypeScript-only per Startup Tools); no compiled `out/**/*.js` changed, so
`mutation-site-count.js` and Stryker have nothing to score here.

Merged in this pass: coder's own self-audit fix (`0ce8a2e7e0`, the BL-801
empty-array-under-`set -u` shape in `prune_refusal_logs`) — already the tip
I received, nothing further needed.

## Verification (re-run, all green)

- `swarmforge/scripts/test/test_bl1275_refusal_evidence_retained.sh` — 13/13
  PASS.
- `specs/features/BL-1275-property-guard-deletes-the-evidence-for-its-own-refusal.feature`
  via `node specs/pipeline/cli.js` — 4/4.
- `extension/test/bl1275RefusalEvidenceInvariants.property.test.js`
  (`npm run test:properties` config) — 2/2.
- `specs/features/BL-1202*.feature` (the feature owning this script's exit
  paths, the riskiest edit here) — 4/4, confirmed no regression.

## Pre-existing reds, unrelated (not this parcel's to fix)

Reconfirmed the coder's finding: BL-570's feature (7/7 red, stale assertion
against `pre-commit` post-BL-1252) and `test_run_commit_guards.sh` case 01
(exit 127, third hand-written guard-list copy per BL-1398/BL-1401's class)
both fail before `check_property_suite_drift.sh` is ever reached and are
untouched by this diff. Already raised to the specifier by the coder's
priority-00 note; not re-raised here.

## Forwarding

No code or test changes made. Forwarding to architect.
