# BL-1537 — documenter review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

Checklist run:
- Re-read backlog/active/BL-1537-*.yaml (task 4, "Documenter: correct the
  two narratives that state the false premise").
- Reviewed the merged hardender commit's diff: draftPathUnder.ts helper,
  the four shell senders' mktemp under `$ROOT/tmp/`, the three TS tools'
  callers, and the property/unit tests.
- Corrected the false "every production draft is under the root by
  construction" premise in `handoff_draft_root_guard_lib.bb`'s header
  comment and in the `Feature:` comment block of
  `specs/features/BL-1518-a-handoff-cli-never-writes-outside-the-root-its-draft-lives-in.feature`
  (comment lines only, no scenario changes — BL-1518-a is landed).
- Also found and corrected the same false premise in two other living
  docs affected by this change: `swarmforge/handoff-protocol.md`'s
  Draft-Root Guard section and `docs/how-to/BL-1518-handoff-draft-root-guard.md`
  — both asserted the same now-false claim to a reader troubleshooting a
  refusal.
- Added a new dated entry at the top of `docs/reference/Specification.MD`
  (Last Updated: September 12, 2026) recording the BL-1537 fix; left the
  prior BL-1518 entry (September 11, 2026) untouched as historical record
  of what was true at that landing.
- Checked README.md, HANDOFF.md, docs/index.md for stale references to the
  seven senders' old TMPDIR/os.tmpdir() behavior — none found.
- Confirmed no diagram change-trigger: this fix is below
  `docs/diagrams/handoff-flow.mmd`'s depicted granularity (guard mechanics
  only), consistent with BL-1518-a's own "no diagram edit" precedent.
- Split the doc fix into two commits per the task-scope gate (BL-1192):
  `5dbd34f27f` (untagged subject, the two BL-1518-named files) and
  `1a2152006e` (`BL-1537:`-tagged, Specification.MD + handoff-protocol.md
  + the guard lib's own header).

Blamed role: none. Class: none. Remediation: none — clean sweep.
