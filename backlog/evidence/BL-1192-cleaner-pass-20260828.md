# BL-1192 cleaner pass — 2026-08-28

Merged coder handoff `1a016cdb08` for BL-1192 (pre-handoff task-scope gate
refuses an entangled `git_handoff` — a cited commit whose diff carries
another ticket's backlog/features/how-to path — at every hop, not only
the QA edge BL-531 already covers). Resolved a trivial `index.js`
conflict (kept the `bl1211` require, deduped `bl1217`, added the new
`bl1192` require).

## Review
`task_scope_gate_lib.bb` (5th gate in the chokepoint chain) matches the
established pattern exactly: reuses `pipeline-stage-lib/extract-ticket-id`
(same extractor as `task_commit_coherence_gate_lib.bb`, never a second
parser), the `delay`-based single-warning-string pattern, and the
absolute fail-open posture on any unreadable fact. Scoping to the cited
commit's own first-parent diff (never a deeper `origin/main...commit`
range) correctly avoids flagging normal pipeline lag as entanglement. No
duplication or structural issues.

## Verification
- `task_scope_gate_lib_test_runner.bb`: ALL PASS.
- Acceptance (`BL-1192-pre-handoff-task-scope-gate.feature` via
  `run_acceptance.sh`) run 3 times: 7/7 pass every time. Two stale
  `/tmp/bl1192-{dry,ir}.json` files found were pre-existing dev debris
  from 2026-08-27 (unchanged timestamp across my 3 runs, confirming they
  weren't generated by the gate's own runtime) — cleaned up as courtesy,
  not a defect.

By cleaner.
