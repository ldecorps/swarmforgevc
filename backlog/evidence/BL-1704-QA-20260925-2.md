# BL-1704 — QA review pass, 2026-09-25

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: sed -n 84,116p docs/how-to/BL-1052-local-model-seat-launch.md
- **Commit hash**: d4fd242514
- **First error excerpt**: L89-90: '- **external.** Signals nothing; the stop log names the server (pid, endpoint) as left running.' L114-116: 'Stopping a swarm-owned server (BL-1704) and reaping ghost runners / detached run clients (BL-1705) are separate, not-yet-documented tickets from the same intake.'
- **Failure class**: behavior
- **Expected vs observed**: how-to says an external server is named by endpoint only (external record carries no pid; lib logs 'leaving an external ollama server running at $endpoint'), and no longer calls BL-1704 (documented in this very section) / BL-1705 (done, Specification.MD) not-yet-documented / observed: prior-bounce D2 unfixed, both claims unchanged
- **Blamed role**: documenter
- **Remediation pointer**: docs/how-to/BL-1052-local-model-seat-launch.md 'Ollama is stopped by the swarm (BL-1704)' bullet external -> endpoint only; rewrite the closing sentence of the launch section to point at the BL-1704 section and BL-1705's Specification.MD section

By QA.

## Detail

Commit tested: d4fd242514 (documenter d3935280e3 merged). Sibling: VERIFY.
Prior bounce D1 (coder, stale-record log "says which"): FIXED -
ollama_ancillary_lib.sh now logs "is gone" vs "now belongs to <cmdline>".
Prior bounce D2 (documenter): NOT fixed - carried as D1 above.

Gates (qa-gather, one run each): stragglers before/after clean (the one hit
was the gather's own node); register ran; pre_qa_gate wiring exit 0;
properties exit 0; BL-1704 acceptance exit 0; unit exit 1 -
extension/test/nightClosingCeremonyRun.test.js (3 BL-1641 tests), a file this
parcel does not touch, register join "absent". Verbatim:
- "AssertionError: landDocumenterBriefing was not called: [[\"freeze\",...],[\"lean\",\"2026-09-25\"],[\"rotate\"],[\"instruct\",\"2026-09-25\"],[\"freeze\",...]]" (line 272)
- "AssertionError: The expression evaluated to a falsy value: assert.ok(actions.some((a) => a[0] === 'land'))" (line 296)
- "AssertionError: Expected values to be strictly deep-equal: + 'freeze-promotion' - 'briefing-missing', - 'swarm-stopped'" (line 310)
Not a parcel defect; sent as an unowned-red note to specifier + coordinator.
Routing: the only open item is the documenter's.
