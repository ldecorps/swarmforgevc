# BL-1322 — documenter pass — 20260901

## Inbound

Hardener tip `5e150904f1`. Merge on `swarmforge-documenter` (clean, fast merge
of the coder→cleaner→architect→hardener chain, no conflicts).

## Review inventory (Article 4.4 — complete pass, one bounce would carry all items)

- **D1**: `docs/reference/Specification.MD` had no changelog entry for
  BL-1322's user-visible fix (bridge startup no longer requires
  `CURSOR_API_KEY` for non-Cursor routes). REMEDIATED this pass: new
  top-of-file entry (Last Updated Sept 1, 2026) summarizing the fix, root
  cause, and the 8 previously-failing test files now passing.
- No other doc surface affected: no README/how-to claims the old (eager-key)
  behavior as intentional (checked all 4 files referencing `CURSOR_API_KEY`
  under `docs/` — `BL-713`, `BL-1220`, `BL-723`, `BL-1079` — all incidental
  historical mentions, none describing bridge-startup key requirements as a
  contract).
- No diagram affected: this is an internal call-timing fix inside
  `cursorBridgeAgentSession.ts`, not a change to extension-host/webview
  boundary, tmux substrate relationship, `.swarmforge/` state layout,
  pipeline topology, handoff lifecycle, or front-desk routing — none of the
  four registered diagrams' change-triggers fire.
- No new user-visible command/setting/flow introduced (bug fix restores
  intended behavior per the ticket's own invariant) — no how-to page
  warranted.

Sweep result: **D1 only, remediated in this pass.** No BLOCKED items.

## Pre-QA verification

`node specs/pipeline/cli.js specs/features/BL-1322-bridge-startup-does-not-require-cursor-api-key.feature`
— 4/4 scenarios pass (required `npx tsc -p .` first; `extension/out/` is
gitignored and was stale from before the fix).

By documenter.
