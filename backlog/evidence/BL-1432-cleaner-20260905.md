# BL-1432 — cleaner pass, 2026-09-05

NONE — no defect found in the delivered work; every declared invariant
and acceptance scenario is verified independently. One flag for QA's own
attention recorded below (not a bounce — reasoning inside).

## What was checked

- Read `parcel-own-base` (`task_scope_gate_lib.bb`) directly: reuses the
  existing `last-handoff-commit`/`effective-base` machinery rather than a
  second implementation, fails open to `nil` (caller widens to
  `origin-main`) on a first hop or an unreadable base — correct per
  invariant 2 ("a narrower range must not hide" — a wrong/unreadable base
  never narrows past the pre-existing behavior).
- Read `land_step_lib.bb`'s bounded-walk diff directly: `entangled-siblings`
  and `own-paths` both gained a trailing `walk-base` (default `origin-main`,
  every pre-existing call site unaffected), and `land-plan` auto-computes
  it via `(or (parcel-own-base ...) origin-main)` when the caller omits
  `:base` — confirmed `land_step_cli.bb`'s own live call to `land-plan`
  passes no `:base` key, so the bounded walk (option 2) is live in
  production by construction, no further wiring needed. `origin-main`
  itself is threaded unchanged everywhere a landed/unlanded verdict is
  read, confirmed by direct read of every call site touched — invariant 2
  holds by construction, not merely by test.
- Read `post-land-repoint!` directly: the dirty-check ordering (in_process
  checked BEFORE the generic uncommitted-change check, mirroring BL-1421's
  own reasoning about an in_process mailbox file's own untracked status),
  the `git reset --hard` gated behind BOTH clean checks, and logging on
  every branch (repointed or skipped, naming the reason) — correctly
  implements invariant 3.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` and
  `task_scope_gate_lib_test_runner.bb`: ALL PASS, both.
- acceptance `BL-1432-...feature`: 5/5.
- acceptance `BL-668-...feature` (regression): 5/5, unaffected.
- All nine land-step sibling features
  (BL-1241/1315/1332/1339/1343/1354/1375/1389/1431): 100% pass on every
  one, matching "same rate as an unmodified checkout."
- The three named pre-existing-failure features, run directly (not merely
  trusting the coder's stash-verified baseline): BL-1374 0/4, BL-1297 5/1,
  BL-1272 5/1 — exact match to the coder's own claimed counts, confirming
  this parcel introduces no new failure and fixes none of these
  (unrelated, out of scope, correctly not touched).
- `npx jscpd` on the new step handler (alone, and against the sibling
  pattern file it was modeled on, `bl1026StageBudgetStatedOnceSteps.js`):
  0 clones either way.
- No `backlog/standing-reds.tsv` or allowlist row is owned by this ticket.
- `npm run compile`: N/A, no TypeScript touched (matches the coder's own
  note) — no JS/TS compile step applies to `.bb` files.

## Flagged for QA's own attention (not a coder defect, not a bounce)

`qa_e2e_procedure` step 2 describes a LIVE, end-to-end check: timing a
real `land_step_cli.bb` invocation and then asserting `git rev-list
--count main..swarmforge-QA` equals the parcel's own commit count "after
the re-point." I confirmed directly that **nothing in this parcel calls
`post-land-repoint!` from any production entry point** — not
`land_step_cli.bb`, not `post_qa_branch_sweep_lib.bb` (confirmed
untouched by this diff, and its own `:divergent-branch` exclusion of QA's
branch is exactly why the sweep does not reach it either), not
`handoffd.bb`. The function is built, safely, and is exercised directly
by the acceptance step handler and the unit tests — but it will never run
during a real land unless something is later wired to call it.

This is NOT a defect in the coder's own delivered work: the acceptance
FEATURE (the actual automated gate) tests `post-land-repoint!` as a
standalone mechanism — scenario 03/04's own Given/When text ("When the
post-land re-point runs") invokes the function directly, exactly matching
what was built, and `required_wiring` names only the step-handler file,
never a call site for `post-land-repoint!` itself. The coder's own
evidence explicitly and transparently flags this exact scope call under
"Out of scope," citing the ticket's own required_wiring and the "How"
section's "may be the place" (direction, not mandate) framing.

The tension is between two DIFFERENT parts of the SAME ticket's own text
(the feature file vs. `qa_e2e_procedure`), not a gap the coder introduced
by cutting a corner — a spec-authoring inconsistency, the kind Article
4.4 routes as a note rather than a bounce. Recording it here so it
travels forward with the parcel: **QA's own qa_e2e_procedure step 2, as
written, cannot be satisfied by this parcel alone** — the option-1
re-point half of the human's ruled "option 3, both" has no live effect
until something (BL-668's sweep, `land_step_cli.bb` itself, or a new
call site) invokes `post-land-repoint!` after a real land. QA should
either treat step 2 as a live-wiring item for a follow-up ticket, or
bounce for the wiring itself if they judge this ticket's own scope
requires it — that judgment is QA's own to make at the gate this
procedure belongs to, not mine to preempt by rewriting the coder's
scope call unilaterally.

## Verification (all re-run in this worktree, all green, matching every claimed number)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/task_scope_gate_lib_test_runner.bb` | ALL PASS |
| acceptance `BL-1432-...feature` | 5/5 |
| acceptance `BL-668-...feature` (regression) | 5/5, unaffected |
| 9 land-step sibling features (BL-1241/1315/1332/1339/1343/1354/1375/1389/1431) | 100% pass on every one |
| 3 pre-existing-failure features (BL-1374/1297/1272) | 0/4, 5/1, 5/1 — exact match to the coder's own claimed baseline |
| `grep post-land-repoint! swarmforge/scripts/*.bb` outside `land_step_lib.bb`/its own tests | no live call site found |
| `npx jscpd` on the new step handler (alone and vs. its sibling pattern) | 0 clones |

No fix required. Forwarding unchanged to architect, with the QA-facing
flag above carried in this evidence file.
