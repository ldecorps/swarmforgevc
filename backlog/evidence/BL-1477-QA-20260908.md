# BL-1477 — QA review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Received `ed587b61eb` (documenter merge-up) from the documenter, merged into
`swarmforge-QA` as `11e25b1339`, then re-synced to a moved `origin/main` and
re-merged as `ccd92e1751` before landing (origin/main advanced twice during
this pass from concurrent role activity sharing this repo's remote-tracking
refs).

Gates run, all green:
- `qa-sibling-check.js status --ticket BL-1477`: `VERIFY BL-1477` (no open
  deferral).
- Compile (`npm run compile`): clean.
- Full unit suite (`npx vitest run`): 612 files / 10358 tests green.
- Property suite (`npx vitest run --config vitest.properties.config.mjs`),
  run twice: BL-1477's own file
  (`bl1477ContextTelemetryTornTailInvariants.property.test.js`, 4 tests)
  green both times. Each run also flaked on ONE unrelated pre-existing file
  under host load (`bl1295RevertAttributionInvariants`+`bl1297MergeOwnPathsInvariants`
  on the first run, `bl1344WaiveInvariants` on the second) — a different file
  each time, both re-run alone and green in isolation. Per BL-1063: grepped
  and found the standing cause already ticketed and active —
  `backlog/active/BL-1348-fork-pool-sizes-to-the-real-host.yaml` (fork/test
  pool sizing under real host contention). Not BL-1477's own files; not
  blamed on this ticket.
- bb unit runner (`context_telemetry_test_runner.bb`): ALL PASS.
- CLI shell test (`test_context_telemetry_cli.sh`): 16/16 PASS.
- Acceptance, `BL-1477-the-context-telemetry-producer-records-again.feature`:
  5/5 scenarios pass (`run_acceptance.sh`).
- Acceptance regression check, `BL-665-context-telemetry-producer-wiring.feature`
  (per the ticket's own qa_e2e_procedure item 1): 4/4 pass.
- Live read-only check against the REAL master-checkout store
  (`/home/carillon/swarmforgevc/.swarmforge/telemetry/context-events.jsonl`,
  61112320 bytes, untouched since 2026-08-30): `summary` and `agents` via
  `CONTEXT_TELEMETRY_STATE_DIR` override both exit 0, correctly name
  "torn tail dropped at line 234114", correct event_count 234113, correct
  7-role agent list. Read-only — no write performed against this file
  (deliberately did not stop the live `handoffd` daemon to run the full
  live-cycle procedure in qa_e2e_procedure step 3: disruptive to every
  other actively-working role sharing this swarm; the read-only checks plus
  the full test/acceptance suite are the substitute for this pass).
  Ran `run-context-telemetry-producer.js` for real against a fresh local
  store (this worktree's own `.swarmforge/telemetry/`, git-ignored, deleted
  after): first run recorded exactly the 500-event cap; second run recorded
  another 500 with zero duplicate `agent:session_id:timestamp` keys across
  all 1000 records (checked programmatically) — invariant 2 holds.
- Wiring: `handoffd.bb` still shells to `run-context-telemetry-producer.js`
  unchanged (required_wiring anchor, BL-1235); `record-batch` is the sole
  write path now (`recordEventViaCli` removed); dead `walkTranscriptFiles`
  call removed per the ticket's own direction, confirmed BL-665's feature
  still resolves without it.
- Docs: both named how-to files updated with the torn-tail rule, cap,
  deadline and batch verb. No diagram change needed — `architecture.mmd`'s
  one context-telemetry mention is a high-level wiring note, not depicting
  the internal mechanism BL-1477 changed (diagram change-trigger not hit).
- No orphaned test/mutation processes at any point (`pgrep -fl 'node
  --test|stryker'` checked before and after; one unrelated timeout-bounded
  hardener process in a different worktree observed and correctly left
  alone).

Ancestry: confirmed the full coder→cleaner→architect→hardener→documenter
merge chain (`b62cbd47fa`→`73f47474d7`→`011a2cb55f`→`68939ac4d1`→`b77586a210`→
`f01ca83d3b`→`0bec49028a`→`27f37c272b`→`ed587b61eb`) is entirely ancestor of
the landed commit, not merely of `HEAD`.

Land (BL-1241 remedy — entangled tip, not a bounce): `land_step_cli.bb
BL-1477-the-context-telemetry-producer-records-again ccd92e1751` (re-run
fresh against the re-synced HEAD after origin/main moved) returned
`LAND_REPLAY land-replay/BL-1477-ccd92e1751 86e42d21ef4a1829afd82d1b1e61bfe8d062b387`
with entangled siblings BL-1348, BL-1349, BL-1408, BL-1444, BL-1446,
BL-1447, BL-1448, BL-1450, BL-1451, BL-1452, BL-1461, BL-1463, BL-1465,
BL-1466, BL-1468, BL-1469, BL-1470, BL-1472, BL-1473, BL-1479, BL-940,
BL-968 — every one either already `done` (branch-residue evidence/commits
never separately landed) or, for BL-1348/BL-1408 specifically, genuinely
still-active tickets QA is deliberately holding un-landed
(BL-1474/BL-1481 lineage); their paths were correctly excluded from the
replay (`EXCLUDED_SIBLING_PATH` lines, verified by name). Reviewed the
replay tip's diff vs `origin/main`: exactly BL-1477's own 17 files, 1852(+)
83(-). Landed via the BL-1144 discipline from a detached temp worktree at
the replay tip (memory: land_main_publish.sh's `--decide-only`/push must
run from the worktree HOLDING the candidate commit, not the master
checkout) — lock acquired, `--decide-only` returned `:next :push`
(`:tip-contains-origin true`, `:origin-advanced-since-gate false`), pushed
`86e42d21ef` to `origin/main` (`f257b24c96..86e42d21ef`), lock released,
temp worktree removed. `is_qa_ancestor.sh 86e42d21ef` confirms `approved`
(auto-recorded land-approval mapping to source `ccd92e1751`, BL-1334) — no
manual approval-store append needed.

`abandoned_commits: [ccd92e1751]` recorded on the ticket (the pre-replay
QA merge commit cited to `land_step_cli.bb`; the actual landed commit is
the replay `86e42d21ef`).

By QA.
