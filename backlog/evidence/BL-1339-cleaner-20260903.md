# BL-1339 — cleaner pass (2026-09-03)

## Scope

Merge `26f93b3f61` touches only files it declares: `land_step_lib.bb`
(writer), `is_qa_ancestor.sh` (reader), and the ticket's own step/property
test files. No unrelated files touched.

## Ruling compliance (human ruled option 2)

- **Writer**: `record-land-approval!` now resolves `shared-target-root`
  (`git-common-dir`'s parent, the same resolver `replay!` already used
  twelve lines below) instead of the caller's own worktree root. Fails
  closed on an unresolvable root — writes nothing, returns
  `{:ok? false :reason ...}`, land still succeeds (invariant 3).
- **Reader**: `is_qa_ancestor.sh`'s `LAND_DIR` now resolves from
  `git rev-parse --git-common-dir`'s parent, falling back to the old
  relative path only when git can't answer (pre-existing behavior for a
  non-repo caller preserved). The bounce stores are deliberately left
  reading from the caller's directory — ruling option 3 was not picked and
  out_of_scope names it explicitly.
- No second approval predicate: one definition, store location moved, not
  duplicated (BL-925 invariant 2 honored).
- Bounce veto (BL-952) unweakened — scenario 6 and invariant 2 in the
  property test both cover a bounce-after-land flipping the predicate back.

## Verification (run, not assumed)

- `run_acceptance.sh specs/features/BL-1339-…feature` — 7/7 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1339LandApprovalRootInvariants` — 15 consecutive green runs, no flake.
- Re-ran every existing suite the ticket's own evidence names as a
  regression check: `test_is_qa_ancestor_land_replay_store.sh`,
  `test_is_qa_ancestor_expedite_store.sh`, `test_is_qa_ancestor_yaml_store.sh`,
  `test_land_step_records_approval.sh`,
  `test_build_freshness_land_replay_approved.sh`, and
  `land_step_lib_test_runner.bb` — all ALL PASS / ALL CHECKS PASSED.

## Mutation-site advisory (BL-485)

Step-handler file is 265 sites, property test 222 — both "over" the 100
threshold and somewhat above the range seen elsewhere this session
(124–241), driven by the fixture shape the ticket itself demands (every
scenario builds a full main-checkout-plus-linked-worktree git fixture, the
only way to make writer-root and reader-root actually differ, which is the
whole point per the ticket's own "how this got past six stages green"
section). All scenarios serve one feature; a split would fragment a single
cohesive fixture-building concern rather than improve structure. No split
performed.

## Verdict

No defect. Forwarding.
