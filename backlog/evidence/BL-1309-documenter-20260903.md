# BL-1309 — documenter pass (2026-09-03)

## Context

Merged hardener `977fe447de`. Reviewed the coder/architect/hardener evidence
chain, the ticket YAML (including the human ruling — option 1, unconditional
refusal, answered 2026-09-03 per the coder's evidence), the feature file, and
the shipped `land_main_publish.sh` diff to identify the operator/QA-visible
surface this change adds.

The merged tree also carries BL-1296's in-progress work (`bubbleSeat.ts`,
`telegramCursorBridgeLive.ts` and their tests) as ancestor commits on the
shared hardener branch — checked `backlog/active/BL-1296-*.yaml` and its
evidence: only architect-bounce/cleaner/coder-rework exist, no hardener pass,
so BL-1296 has not itself reached documenter under its own handoff. This is
the ordinary long-lived-branch entanglement BL-1241/BL-1309 themselves
describe, not a second finished ticket riding this handoff (BL-250) — no
separate doc entry or QA handoff for BL-1296 is due from this pass.

## Review checklist (complete pass, Article 4.4)

- **Existing how-to currency**
  (`docs/how-to/BL-1144-frequent-qa-push-races-on-main-land.md`): this doc
  already documents the operator/QA `--decide-only` call this ticket changes.
  Added a section describing the new `ENTANGLED_SIBLING_BLOCK` refusal, exit
  status 3, the chosen ruling (option 1, unconditional, no
  withheld-vs-ordinary predicate), the fail-open posture, and the measured
  ~12x cost — extending the existing doc in place (same script, same
  operator-facing call) rather than forking a new page. Cross-linked to
  BL-1241's tip-pure-replay remedy in both the new section and the Related
  list.
- **`docs/index.md` orphan/exhaustiveness check**: the how-to was already
  linked; extended its one-line hook to name the BL-1309 addition.
- **`Specification.MD` "Last Updated" freshness**: added a new dated entry
  at the top of the changelog, cross-referencing the same how-to. Bumped in
  the same commit as the content.
- **Diagram currency** (constitution Article 1.7 / local-engineering
  "Diagrams"): checked the four registered diagrams. None of their
  change-triggers fired — this is a guard added inside an existing shell
  script at an existing mandatory call site; it does not change the
  extension host/webview boundary, the tmux substrate relationship, the
  `.swarmforge/` state layout, pipeline topology, backlog flow, the handoff
  file lifecycle, or front-desk routing. No diagram update required.
- **`docs/deprecated/` check** (Article 3.6): nothing retired — the
  pre-existing tip-purity decision and exit codes 0/2 are unchanged; only a
  new refusal path (exit 3) was added. No page moved.
- **Production-code scope**: none touched by this pass; doc/spec files only.

## Findings

NONE.

## Verdict

Forward to QA.
