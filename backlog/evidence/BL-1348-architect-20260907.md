# BL-1348 — architect review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- `dependency-gate.js` / `co-change-report.js`: PASSED / expected coupling
  only (own config files, own test suite, and a historical bulk-commit
  cluster unrelated to this diff).
- Verified `resolveFreeCoresCeiling(cores, loadAvg5min)` matches the
  operator's ruling B text exactly: `max(1, min(cores, floor(cores -
  loadAvg5min)))`. Both `vitest.config.mjs` and
  `vitest.properties.config.mjs` call it identically
  (`resolveFreeCoresCeiling(os.cpus().length, os.loadavg()[1])`, index 1 =
  5-minute average) — invariant 1 (one composition point) confirmed by
  direct diff read, not just trusted.
- Traced `resolveVitestForkCeiling`'s precedence: override → full-forge+
  darwin (returns 1) → router rotation → `defaultCeiling` fallback. The
  darwin-ceiling check runs BEFORE `defaultCeiling` is ever consulted, so
  ruling B's change (what `defaultCeiling` computes to) cannot affect it —
  invariant 3 confirmed structurally.
- `resolveWorkerPoolSize` still takes `min(ceiling, safeCount)`, floored
  at 1 independent of `resolveFreeCoresCeiling`'s own floor — invariant 2
  confirmed at both layers.
- `PER_WORKER_HEAP_MB` 1280→640: matches the ticket's own suggested
  figure ("298 MB observed argues for something near 640"), real margin
  over the measured peak, not a round number.
- Ran `npm run compile` then the full check set directly:
  `vitestWorkerMemoryBudget.test.js` 37/37,
  `bl1348VitestWorkerPoolHostSizingInvariants.property.test.js` 4/4,
  `bl871PropertyLaneWorkerPoolCapInvariants.property.test.js` 2/2 (ran
  three additional times to check cleaner's flake note: all three clean —
  the one flaky run cleaner saw is consistent with inherent host-load-
  timing sensitivity, which is the ruled design's own accepted tradeoff,
  not a logic defect). BL-1348 acceptance: 3/3.
- Confirmed the acceptance feature correctly needed no change (the
  handler never passes `defaultCeiling`, only exercises the pack/
  platform/override/RAM rules, which ruling B leaves untouched) — matches
  the coder's own stated reasoning, verified by reading the handler.

No defect found. Forwarding to hardender.
