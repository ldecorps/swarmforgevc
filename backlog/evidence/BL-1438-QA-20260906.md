# BL-1438 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1438`: `VERIFY BL-1438` (no open
  deferral).
- Orphan process check before/after: `ps aux | awk '/[n]ode --test|[s]tryker/'`
  empty both times.
- `bb swarmforge/scripts/test/bl1438_publish_repoints_qa_branch_property_runner.bb`:
  ALL PASS (32 generated cases + 1 hardener addition).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1438-the-publish-re-points-the-qa-branch-after-a-land.feature`
  — 4/4 scenario runs. Sibling features stay green: BL-1431 (4/4), BL-1432
  (5/5).
- Regression (this ticket's own dependency surface — I run `land_main_publish.sh`
  for every land, so verified these myself rather than trusting the
  coder's evidence alone): `land_main_publish_test_runner.sh` — ALL PASS
  (12/12, decide-only/entanglement paths); `test_bl1366_land_is_one_command.sh`
  — ALL PASS (21/21), including "the live repository's origin URL is
  byte-identical after the suite" and "no live remote points into the
  fixture directory".
- Wiring anchor 1: `land_main_publish.sh:205` calls `bb
  ".../land_step_cli.bb" repoint "$ROOT"` immediately after `echo
  "LAND_PUBLISHED $land_sha"`.
- Wiring anchor 2: `land_step_cli.bb`'s `-main` dispatches `"repoint"` to
  `repoint-verb`, which is a fixed literal string (grep confirms both
  sides).
- Wiring anchor 3: `bl1438PublishRepointsQaBranchSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Invariants confirmed by reading `run_land`'s full body: the repoint call
  sits after every early `return` (LAND_ESCALATE, no-verdict, no-land-sha,
  lock-timeout, rematch-conflict, second-push-rejection) — structurally
  unreachable on any LAND_STOPPED/rejected-push path (invariant 1); wrapped
  in `repoint_out="$(...)" || true` with its exit code never read again,
  so it cannot affect `run_land`'s own return (invariant 2); confirmed
  `land_step_lib.bb` itself (the re-point's own guard logic, BL-1432) is
  untouched by this diff — this ticket adds no second notion of clean
  (invariant 3).
- `land_step_lib.bb` confirmed unmodified (out of scope, correctly): `git
  diff --name-only <merge-base> <tip> -- swarmforge/scripts/land_step_lib.bb`
  empty.
- Docs: `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`
  updated to describe the live call chain instead of "no production call
  site yet"; `swarmforge/roles/QA.prompt` gained the two new output-line
  bullet (already re-read as part of this pass).
- Full unit lane (`npm test`, extension/): 610/610 files, 10283/10283
  tests green (after removing the same chronic `bl1204-*` fixture debris
  documented on BL-1444's/earlier passes — this run's own leftover, not a
  regression). Host was under unusually heavy load partway through this
  pass (see below); one run took 74s instead of the usual ~31s, still all
  green.
- qa_e2e_procedure item 2 ("the next QA land after this ships") will be
  exercised live by this very parcel's own upcoming land — not yet
  performed as of this evidence commit; its outcome will be visible in the
  land step's own printed output and `.swarmforge/daemon/land-repoint.log`,
  not fabricated here in advance.

## Not bounced (observed, not this ticket's defect, not reproducible — host load)

`npm run test:properties`'s first full run coincided with visibly heavy
host contention: `uptime` read load average 27.6-28.3 (vs. a normal ~1-3)
while `ps aux` showed a concurrent coder-worktree stryker/vitest run and
several independent `bb`/`land_step_lib.bb` subprocess trees active at
once. Individual property tests that normally complete in under a second
took 10-30s; the whole run took 431s against a normal ~235s. Seven files
failed, six on the fixed 20s per-test timeout and one (`bl968Materialized
GuardSensitivity`) on its own 300s timeout after printing valid coverage
stats first; one (`bl1225SyncRestartTrailInvariants`) failed on generator
coverage ("generator never reached a other-caller start") rather than a
timeout, plausibly the same contention affecting fast-check's own timing-
sensitive exploration:

- `test/bl1225SyncRestartTrailInvariants.property.test.js`
- `test/bl1272LandedSiblingInvariants.property.test.js`
- `test/bl1297MergeOwnPathsInvariants.property.test.js`
- `test/bl1308SiblingDetectorCoversReplay.property.test.js`
- `test/bl1309LandDecideEntanglementInvariants.property.test.js`
- `test/bl1343ReplayNeverDropsOwnPathInvariants.property.test.js`
- `test/bl968MaterializedGuardSensitivity.property.test.js`

None of these seven files are touched by BL-1438's diff (confirmed: this
ticket's own diff is `land_main_publish.sh`, `land_step_cli.bb`, and its
own new test/doc files only — none of the seven). No ticket found for any
of them (`grep -rl <name> backlog/paused/ backlog/active/` empty for all
seven; none in `backlog/standing-reds.tsv`). Re-ran all seven in isolation
after `uptime` confirmed load had dropped to ~9: all seven passed cleanly
on the first re-run, `bl968...` in its normal ~203s with only the BL-871
allowlisted benign unhandled-error. Flaky-under-load by direct evidence,
unrelated domain (land-decide entanglement/replay, sync-restart audit
trail, and a load-time-binding guard sensitivity check vs. this ticket's
publish-script re-point wiring) — same disposition as BL-1440's and
BL-1442's own "Not bounced" sections.
