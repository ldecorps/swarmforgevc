# BL-1323 — cleaner pass (2026-09-02)

NONE — no defect found. Forwarded to architect unchanged.

## Reviewed

Diff scope (the coder's actual BL-1323 work, a review-only stamp-off parcel):

- `specs/pipeline/steps/bl1323MainSyncDeadlockOverlapHintsStampSteps.js` (280
  lines) — drives the real `babysitter_check.bb` gather, `handoffd.bb`'s trip
  path (via source-text assertion, since it lives inside a poll loop that
  cannot be called directly without standing a daemon up — the file says why),
  and `master_main_reconcile_lib.bb`'s formatters. BL-971 stale-fixture sweep
  present. Well sectioned, one `state(ctx)` helper, no duplication. No
  changes needed.
- `specs/pipeline/steps/index.js` — one `require` line added, registered
  (confirmed via grep, satisfies required_wiring entry 3).
- `extension/test/bl1323StampOffInvariants.property.test.js` — three declared
  invariants. Invariants 1/2 are properties of the working tree itself
  (never edits the reviewed sources; the ledger row stays untouched even
  after actually running the reviewing suite). Invariant 3's generator uses
  a weighted `fc.oneof` over whole-array shapes (not independent per-element
  draws), avoiding the flakiness class BL-1343's first property test hit.
  Ran 5x, no flakes.

Confirmed the coder's own non-vacuity checks are real, not just claimed: read
the `babysitter_check.bb` diff referenced in the evidence and the acceptance
scenario that fixtures a non-git-repo root specifically to prove marker
preference over recompute by behavior.

## Verification run (not assumed)

- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1323-main-sync-deadlock-hints-name-overlaps-and-teach-swarm-heal.feature` → 7/7 pass.
- `npx vitest run --config vitest.properties.config.mjs bl1323StampOffInvariants`, 5 consecutive runs → 5/5 clean (3/3 tests each run).
- `grep bl1323 specs/pipeline/steps/index.js` → handler registered.

## Confirmed, not re-litigated

- This is a review-only parcel: no ledger edit (`state: stamp-open`,
  `human_decision: null` unchanged), no reimplementation of the hotfix's
  own sources (`babysitter_check.bb`, `handoffd.bb`,
  `master_main_reconcile_lib.bb` and their test runners all show zero diff
  against `origin/main...HEAD`) — matches invariant 1's own assertion.
- The coder's commit-time `SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1` override
  is recorded and justified in their evidence file (two unrelated files
  flaking under full-suite concurrent load, neither touched by this parcel,
  same flake observed earlier this session on an unrelated commit) — this
  is the documented recovery-only path, correctly used and disclosed rather
  than left in shell history.
- The follow-up ticket the human's ruling asks for (recompute-or-label the
  snapshot-not-live hint) is correctly left for the specifier to mint, not
  acted on here — the ticket's own firm line against silently "fixing" it.
