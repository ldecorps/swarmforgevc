# BL-1225 QA pass inventory — 20260831

**Documenter tip:** `bdcf4fa1dc` (BL-1225: record self-audit finding and fix in evidence)
**Task:** `BL-1225-sync-initiated-restart-leaves-a-readable-trail`
**Sibling check:** VERIFY BL-1225
**QA merge commit:** `71d2138e63` (Merge documenter BL-1225 bdcf4fa1dc into QA)

## Lineage

| Commit | Role | Note |
|--------|------|------|
| `0dfdaf91ed` | coder | `operator-log-spawn-opts` (append, not truncate), `SWARMFORGE_DAEMON_START_CALLER` extra-env |
| `7d8b5d7da8` | architect | confirmed compliant |
| `12f45c14ef` | hardener | verified, no code changes needed |
| `dc14f070dd` | coordinator | released BL-1225 hold -> active (last approved held ticket) |
| `28bf5c57f0` | coder (re-pass) | no-op — chain already landed on `main` before release |
| `c681bcd05b` / `47d08bb08d` | documenter | how-to doc, docs/index.md, Specification.MD, evidence |
| `4256c4479c` | documenter (self-audit) | corrected the caller-attribution value in docs (was citing the qualified var name instead of its literal string value) |
| `bdcf4fa1dc` | documenter | recorded the self-audit finding+fix in evidence |
| `71d2138e63` | QA | merge, deletion-guard satisfied (BL-1225 hold->active and BL-1252 active->done/M8 both verified content-identical relocations; guard attributes the BL-1225 path to "BL-1210" — the first id in multi-ticket park commit 66be60ea40's subject — named in the merge message accordingly) |

All of the above are ancestors of `71d2138e63` (`git merge-base --is-ancestor` checked individually).

## Ticket gates (parcel-owned)

| Gate | Result |
|------|--------|
| `restart-operator-group!` uses `operator-log-spawn-opts` (`:append`, not a path-string `:out` that truncates) | PASS — read directly |
| `restart-handoffd-group!` sets `SWARMFORGE_DAEMON_START_CALLER` via `:extra-env` (required_wiring) | PASS |
| `swarmforge/scripts/test/build_freshness_lib_test_runner.bb` | ALL TESTS PASSED |
| `suite-manifest.tsv` registration | PASS (`build_freshness_lib_test_runner.bb	standing`) |
| Acceptance `specs/features/BL-1225-sync-initiated-restart-leaves-a-readable-trail.feature` | 3/3 PASS, against the real `start_handoff_daemon.sh` and real audit log (per coder commit) |
| `specs/pipeline/steps/index.js` registers `bl1225SyncRestartTrailSteps` | PASS |
| Property `test/bl1225SyncRestartTrailInvariants.property.test.js` | 3/3 PASS — invariant 1 (append, never discards, both single and repeated restarts) and invariant 2 (iff: sync-initiated names build-freshness, otherwise unknown) |
| Constraint: restart groups/order untouched, BL-433 stop-file/status.json/settle-wait untouched | PASS — diff is two surgical edits inside existing functions, no call-site/order change |
| Docs match implementation | PASS — self-audited and corrected by documenter (caller value is the literal `"build_freshness_cli"`, not the qualified var name) before reaching QA |
| Diagram currency | N/A — sync-restart forensic trail isn't depicted by any registered diagram |

qa_e2e_procedure's manual steps are covered by the acceptance scenarios above (coder commit: "driving real artefacts - the production opts map, and the real start_handoff_daemon.sh with its real audit log"); did not additionally run a live `sync` against this session's own operator runtime/handoff daemon — that would restart shared live swarm infrastructure for coverage the automated acceptance already provides against fixture roots.

## Full unit suite (`npm run test`, extension/)

567/582 files green, identical failing-test set to this session's BL-1252 pass minutes earlier (diffed the two `FAIL` line lists — empty diff): 15 files / 25 tests, all pre-existing, all outside this ticket's diff (this ticket touches no test file already on the earlier list), tracked to BL-1229/BL-1221/BL-1172/BL-1212/BL-1291/BL-1263/BL-1265/BL-1290/BL-1289 as recorded in `backlog/evidence/BL-1252-qa-pass-20260831.md`.

## Property suite

Not re-run in full (no production TS/JS changed beyond this ticket's own new property test file, already run directly and green; BL-1225's actual code is Babashka, gated by its own bb test runner per the constitution's Babashka/Clojure carve-out). Baseline from this session's BL-1252 pass minutes earlier stands unaffected.

## Inventory

**NONE** — no parcel-owned defect.

## Verdict

PASS — approve; land on `origin/main`.

By QA.
