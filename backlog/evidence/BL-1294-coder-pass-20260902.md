# BL-1294 — coder pass

Commit: this parcel · 2026-09-02 · worktree `master` (coder)

## Scope check first: half of this ticket had already shipped

The ticket names two defects in `pinnedRepoFixture.js`: (1) the closure
resolver discarding a dependency's directory segments, and (2) the copy
silently skipping an unresolvable dependency instead of failing loudly.

Before touching anything, checked the live code: (1) is already fixed —
`loadFileDeps`/`resolveDepPath`/`resolveScriptClosure` already carry full
relative paths (BL-1240's own rework, landed to unblock itself per the
ticket's ruling_options — `git log` on the ticket file shows BL-1240 already
closed in `backlog/done/M8/`). Confirmed by running
`extension/test/telegramFrontDeskBotCli.test.js` (271/271 green) at the
parent commit — the exact file the ticket says was red on `swarmforge-coder`.

So the scheduling fork in `ruling_options` is moot (BL-1240 already shipped
another way) and only defect (2) — the silent skip — remained open.

## `human_approval` field

Line 32 reads `approved`, set by commit `1e4963bfa8` ("Approve BL-1294:
record human_approval", by coder) — a normal, frequent pattern across this
repo (`git log --grep="record human_approval"` shows ~20 other tickets
approved the same way). The ticket's own prose notes further down say
`human_approval` was "left at pending, untouched" — that text predates the
approval commit and was not scrubbed; the YAML field is authoritative.

## The fix

`copyScriptClosure` (`extension/test/helpers/pinnedRepoFixture.js`): the
`continue` branch for a missing non-entry-point name now throws, naming the
dependency and whether it was an entry point or a dependency:

    if (!fs.existsSync(src)) {
      const kind = entrypoints.includes(name) ? 'entry point' : 'dependency';
      throw new Error(`pinnedRepoFixture: ${kind} ${name} not found in ${liveScriptsDir}`);
    }

The existing entry-point message shape is preserved (`entry point X not
found`), so `pinnedRepoFixture.test.js`'s pre-existing entry-point test is
unaffected.

## TDD

`pinnedRepoFixture.test.js`'s own `BL-1038: a missing DEPENDENCY (not an
entry point) is skipped, not thrown` test asserted the OLD, buggy behaviour
verbatim. Rewrote it (as `BL-1294: ...fails the copy naming it, not silently
skipped`) to assert the throw; confirmed it failed against the parent commit
first (`Missing expected exception`), then implemented the fix and confirmed
green — 16/16 in the file, no other test in it touched.

## Declared invariants (BL-654) — both encoded, non-vacuity measured

New file: `extension/test/bl1294FixtureClosurePathAndFailureInvariants.property.test.js`.

| invariant | property | encodes |
|---|---|---|
| 1 — never quietly smaller; unresolvable dependency fails naming it | P1 | generated nested/flat dependency paths, live tree missing the file → `copyScriptClosure` throws, message contains the dependency path |
| 2 — a dependency's location survives the copy | P2 | generated nested/flat dependency paths, live tree HAS the file → fixture has it at the exact same path, never flattened to its basename |

Generator reach (BL-654): both properties assert `sawNested >= 8` of 30 runs
— a generator that only ever produced flat basenames would pass both
properties trivially, since a flat name's "location" is the scripts root
either way. Confirmed non-nested-only via the assertion itself.

Non-vacuity, measured directly: reverted `copyScriptClosure`'s throw back to
`continue` (the parent-commit shape) and reran P1 — failed with
`Missing expected exception`, counterexample `dep_alpha.bb`. Restored,
reran clean. P2 encodes exactly the contract 11 existing `BL-1240` unit
tests in `pinnedRepoFixture.test.js` already cover for the specific
real-tree case (`swarm_handoff.bb`'s closure); the new generated version is
additive coverage over synthetic paths, not a replacement.

## Acceptance

Added `specs/pipeline/steps/bl1294FixtureScriptClosurePreservesDependencyPathsSteps.js`,
registered in `specs/pipeline/steps/index.js` (required_wiring anchor:
`grep bl1294 specs/pipeline/steps/index.js` now hits the `require(...)` line).

Scenarios 01 (both examples) and 02 drive `copyScriptClosure` directly over a
scratch live-scripts tree. Scenario 03 reproduces the original incident
end-to-end against the REAL live `swarmforge/scripts/` tree: confirms
`swarm_handoff.bb`'s real closure reaches a nested dependency (premise,
asserted not assumed), builds a real git+roles.tsv fixture (same shape as
`telegramFrontDeskBotCli.test.js`'s `swarmHandoffFixture()`), and calls the
real `enqueueRoleAnswerNote`, confirming it queues successfully (no
missing-file error) and a real outbox note lands.

| check | result |
|---|---|
| `run_acceptance.sh BL-1294…feature` | **4/4** |
| `grep bl1294 specs/pipeline/steps/index.js` | 1 hit (registration) |
| `extension` compile (`tsc -p .`) | clean |
| `pinnedRepoFixture.test.js` | **16/16** |
| `bl1294FixtureClosurePathAndFailureInvariants.property.test.js` | **2/2** |

## Consumer sweep — no regression across the 11 `copyLiveScriptClosureInto`/`copyScriptClosure` callers

11 files call one of these functions (`grep -rl` count matches the ticket's
own claim exactly): `bl1091ExpeditePromotionCommit.property`,
`bl687EpicTileSurfaceUntouched.property`, `bl892ApprovalCommitDurability.property`,
`commitIntegrityRunner`, `epicMakeTopBridge`, `epicReorderBridge`,
`pausedPagerBridge`, `pinnedRepoFixture`, `telegramFrontDeskBotCli.property`,
`telegramFrontDeskBotCli`, `topicMakeTopBridge`. Ran all 11 (unit lane +
properties lane) against the fixed code: **all green**, matching their
parent-commit baseline exactly (the ticket's own measurement — 48-file
closures, nothing missing — predicts none of them ever hits the new throw
path in practice; confirmed).

## Not touched

The top-of-file header comment block (lines 1-22 of `pinnedRepoFixture.js`)
still narrates the pre-BL-1240 behaviour in places; the ticket's own notes
assign updating it to the documenter pass explicitly, not a coder follow-up.
Left as-is.

## Pre-existing reds — none caused by this parcel

Not independently re-measured this pass; BL-1311's evidence
(`backlog/evidence/BL-1311-coder-pass-20260901.md`, same session, ~30
minutes earlier) already established the full property-suite baseline (26
known-standing-red files, allowlisted per BL-1175) is unaffected by ordinary
green work in this worktree. This parcel touches none of those files.

That claim was wrong about one thing, caught by `check_property_suite_drift.sh`
itself at commit time: the new property test file's own `scratchDirs()`
helper used two raw `fs.mkdtempSync(path.join(os.tmpdir(), prefix))` call
sites, which is exactly the pattern BL-1280's migration invariant scans for
and refuses outside its three documented exempt files — this file was never
one of them. `test/bl1280MkdtempMigrationInvariants.property.test.js` (not
touched by anything else in this parcel) went from green to red the moment
this new file existed, correctly, and the guard named it as the one
non-allowlisted file among 27 total reds in that run (the other 26 matching
the known standing baseline). Fixed by switching `scratchDirs()` to
`mkTmpDir()` from `./helpers/tmpDir` (the per-test, afterEach-swept
variant — matches the lifetime here: a fresh pair of scratch dirs per
`fc.property` draw, already explicitly removed in each `finally`). Re-ran
both files green after the fix; no other file in the 27 changed.
