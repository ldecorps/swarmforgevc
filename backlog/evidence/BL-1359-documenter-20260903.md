# BL-1359 — documenter pass (2026-09-03)

## Context

Merged hardener `6c9df5bbc8`. Reviewed the coder/cleaner/architect/hardener
evidence chain, the ticket YAML (no ruling_options — the one alternative
shape was checked and rejected at mint), the feature file, and the shipped
`babysitter_check.bb` diff (including the corrected in-code comment at
lines 480-507) to identify the operator-visible documentation this defect
fix touches.

## Review checklist (complete pass, Article 4.4)

- **Existing how-to currency** (`docs/how-to/BL-611-babysitterd-runbook.md`):
  found this doc's Check 13 ("pipeline code on main") section stated, as
  fact, the exact false claim this ticket's own description says the code
  comment made and the code did not implement — "Merge commits are diffed
  with `git diff-tree -m --first-parent` ... which is exactly the shape
  that let the BL-590 incident's own merge commit go unnoticed", with no
  caveat that `--first-parent` was inert. Corrected the paragraph in place
  to describe the two-tree first-parent diff `commit-touched-paths` now
  runs, added a dated note explaining what the flag form actually did (a
  no-op) and why the BL-962 exemption could not rescue the over-charge,
  and confirmed BL-590's own catch (`f8dc07963`) is unaffected — this was a
  factual correction of a doc that had encoded the same bug the code
  comment did, not a new-feature addition.
- **`docs/index.md` orphan/exhaustiveness check**: the how-to was already
  linked; its existing one-line hook is generic ("the detector that tells
  someone when pipeline code lands on `main` outside QA") and does not
  name the retracted claim, so it needed no edit.
- **`Specification.MD` "Last Updated" freshness**: added a new dated entry
  at the top of the changelog, cross-referencing the same how-to. Bumped in
  the same commit as the content.
- **Diagram currency** (constitution Article 1.7 / local-engineering
  "Diagrams"): checked the four registered diagrams. None of their
  change-triggers fired — this is an internal git-diff mechanism inside an
  existing operator health-sweep script; it does not change the extension
  host/webview boundary, the tmux substrate relationship, the
  `.swarmforge/` state layout, pipeline topology, backlog flow, the
  handoff file lifecycle, or front-desk routing. No diagram update
  required.
- **`docs/deprecated/` check** (Article 3.6): nothing retired — Check 13
  itself, the BL-962 exemption, the waive mechanism (BL-1344), and the
  fail-closed posture are all unchanged in kind; only the merge-diff
  comparison and the doc paragraph describing it were corrected. No page
  moved.
- **`babysitterd_sweep_lib.bb:449-456`'s restated comment**: the ticket
  names this file's own paragraph as in-scope alongside
  `babysitter_check.bb`'s; confirmed the coder's diff corrects both
  (per coder evidence) — no separate documentation-facing artifact beyond
  the runbook section already reviewed.
- **Production-code scope**: none touched by this pass; doc/spec files
  only.

## Findings

NONE.

## Verdict

Forward to QA.
