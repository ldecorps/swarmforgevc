# BL-1728 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Checks run:
- `bash swarmforge/scripts/test/test_operator_runtime_babysitterd_watchdog.sh`:
  ALL CHECKS PASSED, no REFUSED line (the original defect).
- `node specs/pipeline/cli.js specs/features/BL-906-operator-babysitterd-freshness-watchdog.feature`:
  10/10 ok (was 4/10 on main per the ticket's amendment).
- `npx vitest run --config vitest.properties.config.mjs test/bl1728GitFixtureRootIsOwnCheckout.property.test.js`:
  1/1 passed.
- `node out/tools/dependency-gate.js test/bl1728GitFixtureRootIsOwnCheckout.property.test.js`
  → PASSED, no forbidden edges.
- `node out/tools/co-change-report.js` on the same file → coupling only with
  this parcel's own sibling files (the CLI shim, the shell test, its own
  evidence) — no cross-ticket suspects.

Guardrail read (engineering Guardrails, BL-1390 — a fixture root must never
mutate the live repository, proven before any mutating git command): both
fixture-root builders (`init_git_fixture_root` in the shell test,
`initGitFixtureRoot`/`proveGitFixtureIsolated` in the bl906 step handler)
`git init` the mkdtemp'd root directly — never a pre-existing directory
trusted before proof — so every later git command resolves to that fresh
`.git`, never a live ancestor's, by construction of the init-then-mutate
ordering; the shell version additionally proves with
`--path-format=absolute` and a `case`-based path-separator boundary check
before any commit. The bl906 JS version's `proveGitFixtureIsolated` uses
`path.resolve(root, commonDir).startsWith(root)` without an explicit
separator boundary — weaker in form than the shell version, but not a live
defect: `path.resolve` already normalizes a relative `git-common-dir`
output (e.g. a `../.git` climb) to an absolute path before the prefix
check, so it is not fooled by the same relative-path false-pass class the
shell version's own commit message names; and since `git init` always
targets the mkdtemp'd root itself (never a subdirectory reached via a
parent `.git`), the "nested inside a live enclosing checkout" hazard the
guardrail exists to catch cannot arise on this call path regardless.
Noted, not bounced — no concrete failure scenario reaches it.

Invariants review: the declared invariant ("the fixture root is its own
git checkout, and no git command in the test resolves to the live
repository") has a non-vacuous property test covering all three shapes the
invariant quantifies over (good / not-a-repo / outside-a-live-checkout),
with a before/after snapshot proving the enclosing repository is left
untouched by a refused proof — exactly the hazard class BL-1390 names.
Verified by hand per the test's own docstring: swapping
`--path-format=absolute` back to plain `--git-common-dir` makes the
"outside" shape wrongly pass.

Scope: `2fa4d85946` and `b0cb088daa` (the coder's two commits, direct)
touch only the shell test, the bl906 step handler, the property test + its
CLI shim, and evidence — matches the ticket's amended Scope. Two unowned
reds surfaced during this parcel (BL-906's feature, since fixed and owned
by this ticket; a pre-existing, orthogonal `bl1505` property red) were
each handled per Article 4.2/recovery-path discipline — notes sent,
standing-reds.tsv rows present for the two this ticket owns, the `bl1505`
one left as a documented, orthogonal unowned red for the specifier.

By architect.
