# BL-1206 documenter review pass — 2026-09-05

Reviewed the hardened commit (`e10cfa4a14`) and the coder/cleaner/architect/
hardener trail for BL-1206 (drain the node:test import entries from the
property allowlist).

Checklist:
- Step handler exists:
  `specs/pipeline/steps/bl1206DrainTheNodeTestImportEntriesFromThePropertyAllowlistSteps.js`.
- Confirmed the fix: `node:test` import deleted from all fourteen named
  property files (including `bl1200FixtureGitWritesStayInOwnRepo.property.test.js`,
  added mid-flight per the ticket's own amendment); the allowlist
  (`property_suite_standing_allowlist.tsv`) is now down to exactly one row
  (`test/hostActivityFeed.property.test.js`), confirmed out of scope and
  correctly left alone. Confirmed the 14 unrelated allowlist rows named in
  scenario 04 were not the ones this run needed to preserve — by the time
  this ticket ran, sibling tickets processed earlier this session
  (BL-1428, BL-1430, BL-1229 among others) had already re-pointed or
  cleared most of the allowlist's other rows, so the final state is a
  near-empty allowlist rather than a 14-row residual; verified this
  against invariant 2 (`findSilentRemovals` in
  `extension/test/helpers/allowlistRemovalGuard.js`) rather than assuming
  the ticket's own row count was stale.
- Confirmed the new standing guard,
  `extension/test/bl1206PropertyLaneNodeTestImportGuard.test.js`, reuses
  BL-1220's own `findNodeTestImportLines` (never a reimplementation) and
  adds `isPropertyLaneTestFile`/`findPropertyLaneNodeTestImports` scoped
  to `*.property.test.js`, with no allowlist parameter or skip path per
  BL-1220's own established shape.
- Operator/reviewer-visible behavior change (fourteen property files now
  actually collect and run under Vitest instead of silently registering
  with node:test and reporting "No test suite found"; a new no-allowlist
  standing guard prevents the property lane's copy of this defect from
  recurring; the allowlist stops citing a closed ticket's "pending fix")
  is documented in a new how-to,
  `docs/how-to/BL-1206-drain-the-node-test-import-entries-from-the-property-allowlist.md`,
  mirroring BL-1220's existing doc shape (What/Why, the guard, what to do,
  Verify, Related), linked from `docs/index.md` in the same commit
  (Documentation Standard requirement) and cross-linked from BL-1220's own
  how-to, which previously forward-referenced this ticket as
  "lands independently."
- No "Last Updated" freshness field in either touched doc — no date bump
  owed.
- No registered diagram depicts test-lane guard membership — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond the new how-to and the BL-1220 cross-link update.

Result: NONE — no defect found; new how-to authored and a sibling doc's
forward-reference completed.
