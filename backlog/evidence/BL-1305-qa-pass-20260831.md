# BL-1305 — QA pass, 2026-08-31

## Disposition check (BL-532)

`node extension/out/tools/qa-sibling-check.js status --ticket BL-1305` →
`VERIFY BL-1305` (exit 0). No open deferral.

## Inbound

`merge_and_process documenter 4272b1476f` — fast-forward merge into
`swarmforge-QA` (no conflicts). `human_approval: approved` — confirmed
genuine (BL-1294/BL-1305 id-collision reset-both was already discharged
40 min before the tap, on BL-1305's own Telegram topic; see the ticket's
own `notes:`). Spec-gap adjudication (invariant 2 retired, scenario 01/02
amended) is recorded in the ticket and in
`backlog/evidence/BL-1305-bounce-20260831.md`, correctly attributed to the
specifier, not the coder — not counted as a coder bounce.

## Ancestry (BL-336 discipline)

`git merge-base --is-ancestor <commit> HEAD` for every stage merge on this
ticket, against the cited commit `4272b1476f` (documenter's own tip, now
`HEAD`): coder `56ef140e82`, cleaner-merge `62f9f5c420`, architect
`745cf0f01d`, hardener `0738994b12` — all **true**. This is BL-1305's own
work, not a sibling's.

## required_wiring

`specs/pipeline/steps/index.js:169` registers
`require('./bl1305FixtureAgentBinarySteps')`. Confirmed by direct grep on
the merged tree.

## BL-1305's own tests

- Unit (`npx vitest run test/bl1305FixtureAgentBinary.test.js`): 3/3 pass.
- Property (`--config vitest.properties.config.mjs
  bl1305FixtureAgentBinary.property.test.js`): 2/2 pass, including the
  reach-floor non-vacuity assertion (stub loses with isolation removed,
  wins with it restored).
- `tmuxReaperGuard.test.js` (the hardener's own fix target — new step file
  adopting `fixtureReaper.track()`): 15/15 pass.

## Acceptance (own gate, BL-112) — run twice

`specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1305-fixture-agent-binary-is-the-stub.feature` → **3/3
green**, twice in a row. Before/after both runs: zero real agent processes
matching an `aps-role-lifecycle` fixture root, zero leaked `tmux -S`
fixture sockets, zero leftover `/tmp/aps-role-lifecycle-*` or
`-fakebin-*` dirs.

## qa_e2e — SIGTERM mid-scenario (own gate, this ticket's own procedure)

Started the acceptance run, sent `SIGTERM` to it ~0.3s in (mid-scenario,
confirmed a `node --test` child was live at the moment of signal). After
the signal: **zero real agent processes** survived from any fixture root
— invariant 1 holds exactly as the ticket's own `qa_e2e` requires ("no
real binary should ever have been launched to survive"). The fixture's
own tmux server was also killed cleanly (global `SIGTERM` handler via
`fixtureReaper`).

**New finding, not blocking BL-1305**: 4 inert `/tmp` directories (1
`aps-bl1305-rival-*`, 3 `aps-role-lifecycle-fakebin-*`) survived the
SIGTERM — a create-then-track race in the shared `fixtureReaper`: a
directory created moments before the process-wide signal handler fires,
but not yet registered via `track()`/`liveFakeBinDirs`, is never swept.
No process, no real agent risk — just clutter (confirmed no live
processes in any of the 4 dirs). This is explicitly **not** BL-1305's own
invariant (which is about the real binary never running, not about
reaping — the ticket's own description names reaping BL-458/BL-817's
job, "a second line of defence" to this ticket's prevention). Grepped
`backlog/` for an existing ticket on this exact race (create-then-track,
SIGTERM, fixtureReaper) — none found; distinct from BL-1289 (that ticket's
21 named violators are all `swarmforge/scripts` bb/shell runners, none
under `specs/pipeline/steps/`). Removed the 4 dirs I created during this
manual repro. Reporting via `note` to the specifier rather than gating
this parcel on it.

## Full unit suite (`npm test` in `extension/`)

15 files / 25 tests failed, 568 files / 9784 tests passed. Every failing
file grepped against `backlog/` (BL-1063) before writing this down — zero
overlap with any BL-1305 file:

- `deps.checkOrphanedAuthoredDocs is not a function` cascade
  (`pilotAcceptanceGate`, `crossFileDuplicationCheck`,
  `multiBranchParserCoverageCheck`, `perHatRolePromptEvidenceCheck`,
  `pilotScopedCrapCheck`, `shellEntryPointDriveCheck`,
  `unreachableStepHandlerCheck`, `constitutionDocCitations`) → BL-1221
  (paused).
- `backendSwitch`, `telegramClient` (`sendTelegramPoll`),
  `telegramCursorOperatorExec` (BL-698 ambulance) → BL-1263 ("three
  standing assertions contradict deliberate source behaviour", paused) —
  exactly three files, matching the ticket title.
- `operatorRuntimeBbFixtureClosure` (×2) → BL-1265 (paused).
- `tempDirTrapGuard` → BL-1289 (paused). `socketFixtureShortRootGuard` →
  BL-1290 (paused). `liveRepoDerivationGuard` → BL-1291 (paused) — all
  three already named by the hardener's own evidence as pre-existing.

## Property suite (`npm run test:properties`)

269 files / 823 tests passed, 25 files / 14 tests failed, 1 collection
error. Compared the full failing-file list directly against
`swarmforge/scripts/property_suite_standing_allowlist.tsv` (BL-1175):
**all 25 failing files are on the allowlist**, disposition `allowlist`,
zero files outside it. BL-1305's own property file
(`bl1305FixtureAgentBinary.property.test.js`) is not among the failures
(verified 2/2 separately above). Not blocking.

Orphan check: `pgrep -fl 'node --test|stryker'` clean before and after
every lane. Note for the record, not a BL-1305 defect: this session's own
`test:properties` invocations twice hit the harness's background-task
2-minute cap and were killed mid-run before completing (once genuinely,
once after I mistakenly targeted the wrong process tree while clearing a
duplicate concurrent invocation of my own making) — reaped all leaked
vitest/esbuild processes by PID before each retry; final run completed
clean, fully detached (`setsid nohup … & disown`) so the harness's
background-task killer could not reach it.

## Diagram currency (documenter's D3, re-checked independently)

`extension/src/tools/render-briefing-diagrams.ts`'s `DIAGRAM_FILES`
allowlist (architecture, swarm-flow, handoff-mechanism, front-desk): none
of their change-triggers fired for this parcel (fixture/test-only change,
no extension-host/webview/tmux-substrate boundary, pipeline topology, or
handoff/front-desk mechanism touched). Agrees with the documenter's own
finding.

## Entangled-tip check (BL-1241)

The documenter's tip carries `BL-1303`'s promotion (`paused` → `active`,
`swarmforge-QA` diff at `577ef1b7d0`) as an ancestor. Checked: BL-1303 is
`status: todo`, `assigned_to: coder` — no coder/cleaner/architect/
hardener/documenter work has happened on it yet, so there is no unlanded
*implementation* riding along, only routine backlog bookkeeping (spec +
approval + promotion). Not a BL-1241 entanglement — no sibling
implementation content to replay around. Proceeding with a normal land.

## Verdict: APPROVE
