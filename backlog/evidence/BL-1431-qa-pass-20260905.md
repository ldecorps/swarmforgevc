# QA pass — BL-1431 — 2026-09-05

Commit under review: `61aa8e9d9c` (merge of documenter's `42b8e649c8` into
`swarmforge-QA`), carrying the full chain (`a9d25a38ca` BL-1431 coder,
confirmed ancestor via `git merge-base --is-ancestor`).

This is the fix for the exact race I hit twice earlier this shift landing
BL-1422 (decide-only walk genuinely slow, ~7-8 min; origin/main advanced
mid-plan on the BL-1411/BL-1405 and BL-1423 lands, each requiring a rematch)
and the coordinator's own BL-1416/BL-1407 escalations referenced in the
ticket's source.

## Verification

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/bl1431_one_land_plan_one_tip_property_runner.bb`:
  ALL PROPERTIES HOLD (40 runs/property; land-fixture properties are
  necessarily heavier than pure-function ones, matching the ticket's own
  fixture-per-run cost).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1431-one-land-plan-reads-one-tip.feature` — 4/4
  scenarios pass, including "origin/main advancing during the attribution
  walk does not change the verdict" and "origin/main is resolved by name
  once per invocation and threaded to every read".
- Regression, all 5 named sibling land-step features still green:
  BL-1315 (7/7), BL-1343 (6/6), BL-1354 (5/5), BL-1375 (7/7), BL-1389 (5/5).
- `required_wiring` / qa_e2e_procedure checks:
  - `grep -c origin-main-sha swarmforge/scripts/land_step_cli.bb` = 1, at
    the CLI's single entry-point resolution (line 80), threaded into
    `land-plan` as `:origin-main`.
  - `grep -c "(origin-main-sha root)" swarmforge/scripts/land_step_lib.bb`
    = 7: all 7 are either a direct-caller default arity that resolves once
    at ITS OWN entry (`entangled-siblings`, `main-ticket-sources`,
    `ticket-approval-state`, `own-paths` 2-arity) or an `opts`-checked
    fallback (`(if (contains? opts :origin-main) (:origin-main opts)
    (origin-main-sha root))`, lines 993/1256) that only resolves when no
    sha was threaded in — matching invariant 1 and the ticket's own
    "finish the pattern" direction; none is a second read inside a single
    threaded plan. Confirmed by scenario 2 passing directly.
  - `specs/pipeline/steps/bl1431OneLandPlanOneTipSteps.js` exports
    `registerSteps`.
- Item 3 of qa_e2e_procedure (live land during a concurrent specifier mint)
  is exercised implicitly: every land I ran this shift for BL-1411/BL-1405,
  BL-1414 and BL-1423 hit exactly this race (origin/main advancing between
  decide-only and push) and each required a rematch — the documented,
  unchanged single-rematch publish path (invariant 2) is what resolved them
  every time, consistent with this fix leaving the publish step untouched.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
