# BL-1633 - send-back, 2026-09-18 (spec-gap, charged to the specifier)

Inbound: hardender note 00_20260918T103907Z_001399 (priority 00, to
specifier and coordinator): "BL-1633 spec-gap: own land breaks BL-1598's
unscoped 9-row scenario". Hardender evidence
`backlog/evidence/BL-1633-hardener-spec-gap-20260918.md` (hardender
4bdf3ba12e). The parcel is in the hardender's in_process.

## D1 - a second census pin the mint did not scope for retirement (spec-gap, specifier)

BL-1598's landed scenario `unit-suite-pole-register-03` asserts "exactly
9 rows" in `backlog/suite-poles.tsv` and that telegramFrontDeskBotCli's
row is among them - the 2026-09-16 census, frozen into a standing
scenario. BL-1633's intended fix retires that very row (9 -> 8, the file
gone); the hardender ran BL-1598's feature on the parcel: 11 pass, 1
fail at "Then it holds exactly 9 rows: 8 !== 9". This is the BL-1006
shape the 09:2x amendment scoped for BL-1620's scenario 02 and missed for
BL-1598's scenario 03 - symmetric, an oversight. The coder built exactly
to the spec as it stood.

## Ruling: retire, never reword

- "exactly 9 rows" and "the named files among them" are a census pin the
  register's own designed mechanism falsifies one row at a time; no
  standing meaning survives.
- "every row names a ticket present under paused or active" is the gate's
  own `unowned-row` verdict, already covered by BL-1598's scenario 01
  table; and BL-1629's accepted row (a rationale ticket that closes) would
  break this clause later anyway.
- So: delete the scenario block and its four feature-scoped handler steps
  in the same commit; re-tense BL-1598's narrative sentence about the nine
  poles to the past. BL-1598's scenarios 01 and 02 stay. Scoped into
  BL-1633's ticket (this commit); the parcel is at the hardender, so the
  feature is not edited on main (BL-1385) - the bounce to the coder
  carries it, and BL-1633's own scenario 04 gains a last step reading
  BL-1598's feature at the parcel.

## Record

Bounce recorded against 4bdf3ba12e as spec-gap with a correction naming
the specifier's mint as the cause (BL-990). Notes sent to the hardender
(holder: merge main, bounce to coder) and to both coder seats (bounce
coming, merge main first).

By specifier.
