# BL-1748 — coder pass, 2026-09-25

## What changed

`specs/pipeline/steps/humanInTheLoopClosedSteps.js`: removed the stray
`const { mkSocketFixtureRoot } = require('./lib/socketFixtureRoot');`
line from the `OPERATOR_DECIDE_STUB` source-text template (a copy of the
real BL-948 module-scope require that should never have landed inside
the string the fixture writes to
`<root>/extension/out/tools/operator-decide.js`). No `./lib/` directory
exists beside the written file, so the spawned stub threw
`MODULE_NOT_FOUND` before reaching its `appendFileSync` to
`consumed.log`. The stub now loads only `fs` and `path`, which is what
it uses.

## Checks run

- `node specs/pipeline/cli.js specs/features/BL-325-human-in-the-loop-closed.feature`:
  7/7 pass, including scenario 2 ("A reply typed into the item's topic
  reaches a consumer that acts on it") and scenario 6 ("The whole loop
  closes without the human being bypassed"), both previously failing
  with "expected operator-decide.js invoked with [...], got []".
- `cd extension && npm test`: 638 files / 10875 tests pass.
- `git status --short`: only `specs/pipeline/steps/humanInTheLoopClosedSteps.js`
  touched by this parcel.

By coder.
