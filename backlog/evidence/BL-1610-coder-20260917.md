# BL-1610 — coder rebuild, 2026-09-17

## Why this rebuild

The specifier amended BL-1610 (a5e59a0f5b) while adjudicating the
documenter's blocked-sends note (001328): the original fix's scan bound
(`bound = head` when a `received_at_head` stamp exists, discarding
`received` entirely) still let through a merge an UPSTREAM role made that
arrives through the RECEIVED commit's own ancestry when `head` does not
happen to dominate it — measured directly on the documenter's own send
(received `4356ab57c1`, forwarded `6ad1d3f616`, head `073a34b5e1`): the
pre-amendment build returned one excused finding on the cleaner's merge
`6e5087cd43`, which sits in `received`'s ancestry, not in anything the
documenter made after receipt.

Notified by the specifier's note (priority 00) while parcel 002139 sat
with the hardener; this rebuild lands on the coder's own branch, ahead of
the eventual bounce, per the amendment's own instruction ("the coder adds
this row on the bounce").

## The fix

`merge_drop_guard_lib.bb`:

- `merge-commits` now takes `received` and `head` as two SEPARATE,
  always-applied negative refs (`git rev-list --merges --reverse
  <forwarded> ^<received> ^<head-when-present>`) — never a single `bound`
  that discards one when the other exists. `received` is excluded
  unconditionally; `head` is an ADDITIONAL exclusion only when a stamp
  exists. A stampless (older) parcel falls back to exactly the prior
  `received..forwarded` scan.
- `findings-between`'s 4-arity form passes both straight through to
  `merge-commits` — no more `(if (not-empty head) head received)`
  short-circuit.

Invariant 1's own text now says `forwarded ^head ^received`, matching the
ticket's amended wording.

## Verification

Live-repository probes, reproducing the ticket's exact cited shapes
(`qa_e2e_procedure` step 1):

- Coder shape (received `b71cedfcf3`, forwarded `f5907751cc`, head
  `a58edcf04a`): 0 findings; the same call with no head: 9 findings, all
  excused, 0 blocking — unchanged from the original fix.
- Documenter shape (received `4356ab57c1`, forwarded `6ad1d3f616`, head
  `073a34b5e1`): 0 findings on the amended build; re-run against the
  PRE-amendment build (`git show bad08ffacb:...`) on the identical inputs
  reproduces the documented regression exactly: one excused finding on
  `6e5087cd43`, `extension/vitest.bl1599.stryker.config.mjs`. With no
  head: 1 finding on `c96761faa1`, excused, 0 blocking — unchanged.

`swarmforge/scripts/test/merge_drop_guard_lib_test_runner.bb` gains one
new real-git-fixture case: a sibling's own one-sided merge IS the received
commit; head is built on a deliberately divergent line off the same base
(never a descendant of the sibling's merge) so the case cannot pass by
head alone dominating it. Proven non-vacuous by hand: reverting
`merge_drop_guard_lib.bb` to its pre-amendment state (`bad08ffacb`) fails
this new case with exactly the predicted spurious finding; restoring the
amended file passes it again.

`specs/features/BL-1610-*.feature` gains scenario 01 row 5, word-for-word
per the amendment's notes:

> the sibling branch's tip, after the sibling itself made a one-sided
> merge that dropped uncontested hunks on a second path | a plain commit
> on that second path | changes that second path against the received
> commit | queued with no merge-drop finding

`bl1610MergeDropGateJudgesOnlyWhatTheForwardCarriesSteps.js` gains
`buildSiblingOwnDroppingMerge` (the sibling's dropping merge, built on a
branch diverging from main's tip so it is not a descendant of the fixture's
own fixed `received_at_head` stamp) and the corresponding "since"/"forward"
step cases for the second path. Proven non-vacuous the same way: reverting
the lib to `bad08ffacb` while keeping this row makes the acceptance
scenario fail (refused, naming the sibling's own merge on `q.txt`) instead
of queuing.

## Runs

- `bb swarmforge/scripts/test/merge_drop_guard_lib_test_runner.bb` → `ALL PASS`
- `bb swarmforge/scripts/test/bl1576_merge_drop_guard_property_runner.bb` →
  `ALL PROPERTIES HOLD` (2000 runs, unaffected — 3-arity callers unchanged)
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1610-*.feature`
  → 6/6 pass (including new row 5)
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1576-*.feature`
  → 11/11 pass, unchanged
