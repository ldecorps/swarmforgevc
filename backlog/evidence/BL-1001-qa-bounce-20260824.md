# BL-1001 — QA bounce — 20260824

## Review inventory (Article 4.4)

Received documenter tip `acbfb931a7` for BL-1001. Sibling: `VERIFY BL-1001`.
HOTFIX pack/board MATCH `27273f2b0a`; BL-1113 acceptance **9/9**.
`depends_on` BL-983 is in `backlog/done/`.

BL-1001's own surface (difficulty-aware seat claim) verifies green after the
architect bounce-refix (undeclared seats skip on tier-active stages). The tip
also promotes and ships unfinished **BL-1007** (contention-relative unit
budgets) that architect already bounced — D1 below is still live. Cannot land.

| Gate | Result |
|---|---|
| Sibling BL-1001 | PASS — `VERIFY` |
| Scope BL-1001 | PASS — `mutation_cost` vs declared `--seat-tier`; asymmetric spill |
| Unit BL-1001 | PASS — `seat_difficulty_lib_test_runner.bb` ALL PASS |
| Properties BL-1001 | PASS — vitest `bl1001DifficultyAwareSeatRouting.property.test.js` **4/4** |
| Acceptance BL-1001 | PASS — **6/6** |
| Hotfix stamp-off | PASS — MATCH `27273f2b0a`; BL-1113 **9/9** |
| Hitchhiker BL-1007 | **FAIL** — D1 below |
| BL-1007 acceptance (on tip) | **11/11** — does not close architect D1 (softened scenario 03) |
| Orphans | NONE |

### D1 — behavior (hitchhiker / unfinished bounce) — blamed: coder

- **Failing contract:** tip tree includes `extension/test/helpers/contentionBudgetSetup.js`
  still pushing `loadNormalizedDurationMs: null` as a permanent placeholder
  (architect bounce
  `backlog/evidence/BL-1007-architect-bounce-20260824.md` inventory item 1).
  Invariant 1 requires attributable load-normalized duration after a completed
  run; tip has no hardener/documenter close for BL-1007 and no refill of that
  field.
- **Commit hash checked:** `acbfb931a7` (merged at `692c6f49a`)
- **First excerpt (still true on tip):**
  ```
  loadNormalizedDurationMs: null,
  ```
  with comment "Placeholder until a reporter records wall time / factor."
- **Failure class:** `behavior`
- **Close by:** strip unfinished BL-1007 from this tip **or** finish the
  architect bounce (record real wall÷factor before forward) on a dedicated
  BL-1007 chain; then return BL-1001 alone (or Art. 2.6 batch when both ready).

## Verdict: BOUNCE — do not land.

By QA.
