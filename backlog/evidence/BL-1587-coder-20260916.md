# BL-1587 - sweep 3 of 6, coder pass, 2026-09-16

Applied the census's recipe (`backlog/evidence/BL-1583-sampled-reach-floor-census-20260915.md`
"The fix, per file") to the 11 pinned files. 10 of the 11 now `require`
`./helpers/reachFloors` and drive their reach floor(s) through
`runsPerCell`/`assertReachFloor`. The 11th (bl1305) carries no genuine
reach-floor pattern at all and is left unchanged per the recipe's third
branch (below). No production code touched; no floor value, draw budget,
timeout or tolerance was lowered, raised, widened, seeded or retried -
every pre-sweep assertion line is still present, unchanged, alongside the
new construction.

Note on scope: this ticket's `required_stages` covers `[coder, cleaner,
architect, hardender, documenter, qa]`; there is no separate deprecator
stage, so no deprecator freshness note applies here.

## Table: file, branch, cells, budget, floors kept

| file | branch | cells | budget (unchanged) | floors kept |
|---|---|---|---|---|
| bl1218RemoteControlConfigInvariants | sampled (inv1, inv2) | inv1: 4 placements; inv2 (config-on): agent x placement product, 8; inv2 (on-disk): setting x nameFlag product, 6 | inv1: 100 (25/cell); inv2 config-on: 100 (12/cell); inv2 on-disk: 12 (2/cell) | `assertReachFloor(seen, PLACEMENTS, ...)`, `assertReachFloor(seen, [...agent/placement keys], ...)`, `assertReachFloor(seen, [...setting/nameFlag keys], ...)` - all unchanged values. The third test's `assert.ok(cases > 0)` is **not a floor** (a single scalar tally, unconditionally true for any numRuns >= 1) - left unchanged, quoted here; its own generator (`WINDOW_CLI()`) is untouched. |
| bl1239SuiteManifestAccountsForEveryTestFile | sampled | 5 breakages | 60 (12/cell) | `assertReachFloor(reached, BREAKAGES, 3, 'breakage')` - floor value 3 unchanged (not tied to the 12/cell budget). Test 2 (numRuns:15, the file's census-pinned "min runs") has no reach-floor pattern at all (no counter, no threshold assertion) - **not a floor** - left unchanged. |
| bl1254LedgerCertificationNeedsAHuman | sampled -> constructed (the `hit` link's commit is now derived from the drawn `created` set via `fc.chain`, rather than an independent draw hoping to land on one of it - the file's own header comment already documented the "went red on an unlucky seed" incident this fixes) | 1 (the whole generator space; reach is now guaranteed by the chained derivation, not by cell splitting) | 12 (`runsPerCell(12, 1)`, identity) | `assertReachFloor(reached, ['created', 'linkedExisting', 'linkedMissing'], 1, ...)` - floor 1 (`>0`) unchanged. `miss`'s commit already drew from a pool (`ABSENT`) never created, so it was already a guaranteed miss by construction; only `hit` needed the fix. |
| bl1275RefusalEvidenceInvariants | sampled (both invariants) | inv1: 2 (large/multiLine); inv2: 3 (overBound/underBound/withoutIgnoreRule) | inv1: 12 (6/cell); inv2: 10 (3/cell, `10/3` floors to 3, so 9 of the 10 draws are spent - `runsPerCell`'s own floor-division semantics, not a budget cut) | `assertReachFloor(seen, ['large','multiLine'], ...)` and `assertReachFloor(seen, ['overBound','underBound','withoutIgnoreRule'], ...)`. Each cell's generator (or fixed cell values, for inv2) guarantees its own predicate on every run of that cell - e.g. `padKb` drawn from `[16,60]` always exceeds the 16KB floor. |
| bl1305FixtureAgentBinary | **not a floor after all** (whole file) | n/a | unchanged (12, and a non-generative hand-written second test) | Invariant 1's property (numRuns:12) has no reach counter or threshold assertion at all - only per-run correctness checks. The file's own second test ("reach floor: ...") is a fixed, non-generative sequence with no `fc.property`/`numRuns` and no per-category count - a hand-written non-vacuity check, not a sampled population floor. File left completely unchanged; excluded from the acceptance's structural check (the step handler names it explicitly) per the recipe's "never add a runsPerCell call to a file that draws nothing." Architect: please confirm this exclusion so the epic's census (BL-1583) can be corrected. |
| bl1308SiblingDetectorCoversReplay | already constructed by hand, direct (not fc.assert-loop) | 4 PINNED ops sequences run directly before the random pass; the random `OPS()` pass is 1 cell (exploratory only) | PINNED: n/a (direct calls, no numRuns); random pass: 12 (`runsPerCell(12, 1)`, identity), unchanged | `assertReachFloor(seen, values, 1, 'PINNED-construction')` for both invariants - verified empirically that the PINNED sequence ALONE (before any random draw) already hits `merge=3, siblingOnSide=3, replayed=3`; the manual `assert.ok` checks were migrated to the shared helper for consistency, no loop or budget change. |
| bl1315OwnPathsFullRangeInvariants | sampled | 3 (withSiblings/noSiblings/withUntaggedOwn) | 25 (8/cell) | `assertReachFloor(seen, ['withSiblings','withUntaggedOwn','noSiblings'], ...)`. Test 2 (numRuns:12) has no reach-floor pattern (no counter/threshold) - **not a floor** - left unchanged. |
| bl1317AdaptEffortInvariants | already constructed by hand, direct (PINNED sequences added, run before each random pass) | inv2: 1 PINNED sequence (climbs to the top rung, then a full clean streak back down) covers all four states in one deterministic run; inv1: 1 PINNED case (`['bounce']` against a fixture that always starts at 'medium', paired with `mutationCost:'low'`) | PINNED: n/a (direct calls); random passes: 40 and 12, both `runsPerCell(_, 1)` identity, unchanged | `assertReachFloor(reach, ['top','floor','climbs','drops'], 1, ...)` (inv2) and the pre-existing `reach.climbed + reach.dropped > 0` OR-style check (inv1, left as `assert.ok` since it is a disjunction across two keys, not a per-key floor - `assertReachFloor` does not fit that shape). The pure decision (`seat_difficulty_lib.bb::adapt-effort-decision`) is fully deterministic (bounce always climbs one notch until the top rung; clean only drops after a full streak), so the PINNED sequences reach every state with zero chance dependence - read from source, not guessed. |
| bl1343ReplayNeverDropsOwnPathInvariants | already constructed by hand (both invariants; this file's own header comment documents its own prior architect bounce, D1 2026-09-02, that already fixed the SHAPES-outer-loop pattern) - only the `numRuns:9` literal and the raw `assert.ok` floor checks needed migrating | 3 (SHAPES: all-sibling/mixed/none-sibling) | 27 total (`runsPerCell(27, 3) = 9`, byte-identical to the pre-existing literal) | `assertReachFloor(reach, ['fullySubtracted','partiallySubtracted','nothingSubtracted'], 1, ...)` (inv1) and `assertReachFloor(reach, ['refusals','kept'], 1, ...)` (inv2) - floor 1 (`>0`) unchanged in both. |
| bl1445StaffingGateWiringTestDecidesOverrideInvariants | already constructed by hand, deterministic exhaustive sweep (no `fc.property` at all - nested `for` loops over the full 4x2 Cartesian product, this file's own BL-1445/BL-654 hardening pass having already fixed the exact sampling-flake shape this epic targets elsewhere, per its own header comment) | 8 (4 PANE_GATE_VALUES x 2 STALE_STATE_DIR_VALUES) | n/a (no fc.assert numRuns); `RUNS_PER_COMBINATION = runsPerCell(8, 8) = 1` added so "once per combination" is derived through the shared helper rather than a bare loop | `assertReachFloor(reach, [...6 keys...], 1, ...)` - migrated from six `assert.ok` calls, same floor. Invariant 2 (static source-text check) carries no reach-floor pattern - untouched. |
| bl1495BaiGatewayInvariants | sampled (3 of 4 property tests) | inv1 test1: 2 providers; inv1 test2: 2 providers; inv2: 2 (aider-present/aider-absent) | inv1 test1: 30 (15/cell); inv1 test2: 30 (15/cell); inv2: 40 (20/cell) | `assertReachFloor(reach, PROVIDER_KEYS, ...)` (x2) and `assertReachFloor(reach, ['aider-present','aider-absent'], ...)`. inv1's third test (numRuns:10, launch-cli host-sniff) has `assert.ok(reach > 0)` - a single scalar tally, **not a floor** - left unchanged. Invariant 2's `backendSetArb`/`fc.pre` filter was replaced by two constructive generators (`fc.subarray` over the non-aider backends, one always appending `'aider'`, one drawing only from the non-aider pool) so both shapes are guaranteed by construction with no `fc.pre` discard. |

## Verification run (coder pass)

- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1587-sampled-reach-floors-sweep-3-of-6.feature` - 12/12 pass (bl1305's row passes via the step handler's documented exclusion, not by a fabricated migration).
- All 11 files, `npx vitest run --config vitest.properties.config.mjs <file>` - every file green individually; the 10 migrated files each re-run 5x standalone back to back with no flake.
- Break-then-restore proof on the smallest-cell-budget file (bl1343, 9/cell): dropping `'none-sibling'` from `SHAPES` fails with `reach floor: subtraction-shape nothingSubtracted drawn 0 < 1`; restored and re-verified green (2/2 tests).
- `git status --short`: only the 10 migrated test files plus the new step handler (`specs/pipeline/steps/bl1587SampledReachFloorsSweep3Steps.js`) are touched - bl1305 (the "not a floor" file), no production source, no helper file.
- `git diff HEAD -- extension/test | grep -E '^[-+].*(numRuns|assertReachFloor|>= *[0-9]|> *0|runsPerCell)'`, read line by line: every removed `numRuns` literal reappears as the budget handed to `runsPerCell`; every removed floor value (`reach.get(b) >= 3`, `reached.created > 0`, etc.) reappears as the floor argument to `assertReachFloor`; no numeric value went down.

## Invariants (BL-654)

- Invariant 2 ("every swept file reaches each reach floor by construction... through the shared helpers") is encoded structurally, twice: the acceptance feature's scenario 01 (Gherkin, per file, with the bl1305 exclusion named and enforced by the step handler rather than papered over) and the migration itself - every pinned file that carries a genuine floor now demonstrably `require`s and calls both `runsPerCell` and `assertReachFloor`. No separate `*.property.test.js` file was added for this invariant for the same reason BL-1585/BL-1586 gave: the population it quantifies over (the 11 pinned files' own source) is exactly what the acceptance scenario already drives end to end through the real step handler, proven non-vacuous by BL-1578/BL-1580/BL-1585/BL-1586's own precedent of the identical structural pattern.
- Invariant 1 ("no reach floor value, draw budget, timeout or tolerance... is lowered... every floor asserted before the sweep is asserted after it at the same value") quantifies over THIS PARCEL's own diff, not over a stable, reusable testable module - the same exemption BL-1585/BL-1586 recorded (`01_roles.md`'s Invariants section: "quantifies over prose or process rather than a pure, testable module"). The parcel carries the QA `qa_e2e_procedure`'s step 3 (the `git diff main...HEAD -- extension/test | grep ...` floor audit) plus this table recording every kept value by hand for QA to cross-check.
