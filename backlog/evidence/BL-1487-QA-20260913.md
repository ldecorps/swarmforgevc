# BL-1487 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Reviewed commit 46173a67f4 (documenter's no-op forward, on top of
cb36b40b20 hardender's evidence commit) for BL-1487 — human-ruled
"restore": eight `swarmforge/profiles/*.{conf,prompt,yaml}` files restored
byte-identical to pre-`4695d31402` state, plus a 3-line stale-fixture fix
in `test_smoke_check_stabilize_two_pack.sh`.

- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1487` —
  `VERIFY BL-1487`, no open deferral.
- No orphaned `node --test`/`stryker` processes before or after this pass.
- `bash swarmforge/scripts/test/test_smoke_check_stabilize_two_pack.sh` —
  ALL PASS (9/9).
- `bash swarmforge/scripts/test/test_shipped_confs_no_coordinator_window.sh`
  — ALL PASS (12/12 shipped confs).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-939-two-pack-smoke-check-stops-demanding-a-coordinator-window.feature`
  — `# pass 4`, `# fail 0` (all four rows).
- Ticket's own reference grep (`grep -rln 'swarmforge/profiles' . --include=…
  | grep -v 'backlog/\|docs/deprecated/\|node_modules\|.worktrees'`) — 15
  hits, every one names a path that now exists: the 8 restored profile
  files, `.vscode/launch.json`'s two configs (both point at existing
  files), the two shell tests, `swarmforge.sh`'s sync guard,
  `sync_worktree_scripts_lib.bb` + its test runner, the BL-203 how-to
  (correctly NOT moved to deprecated under restore), the BL-939 and BL-373
  feature files, `daemonWorkflowSteps.js`, and a historical briefing —
  matches the restore invariant.
- `bb swarmforge/scripts/standing_red_register_cli.bb .` — no row names
  BL-1487; confirmed the three rows (acceptance BL-939 feature, both shell
  tests) are gone from `backlog/standing-reds.tsv`.
- Changed production paths are conf/prompt/yaml + one shell fixture only —
  no TypeScript/JS/Babashka logic touched, so Article 4.5's changed-path
  unit-test gate and mutation/CRAP/DRY tooling have no surface here,
  matching the ticket's own `mutation_cost: low` note.
- Ancestry: `git merge-base --is-ancestor cb36b40b20 HEAD` (this ticket's
  own hardener merge) — OK. Not a multi-ticket batch; no sibling
  entanglement.
- Reviewed documenter's evidence independently: `docs/reference/Specification.MD`
  already documents `swarmforge/profiles/` as the live location (no edit
  needed under restore), no diagram depicts the profile/pack relationship,
  and no `docs/deprecated/` move applies since nothing is retired.

Verdict: satisfies the ticket's `qa_e2e_procedure` in full under the
"restore" ruling. Landing on `main`.
