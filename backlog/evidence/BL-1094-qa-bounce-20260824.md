# BL-1094 — QA bounce — 20260824

## Review inventory (Article 4.4)

BL-1094's own gates are green. The tip that would land on `main` is **not**
landable: hitchhiking commits after stamped hotfix `27273f2b0a` regress the
BL-1113 stamp-off and diverge two of its HOTFIX_PATHS.

### D1 — acceptance / behavior — blamed: coder

- **Failing command:**
  `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1113-cursor-hotfix-main-sync-board-plan-stamp-off.feature`
- **Commit hash checked:** `89e153355e` (QA merge of documenter `34140ea8bd`)
- **First error excerpt:**
  ```
  ✖ cursor-forge is a standing full-forge Cursor pack at depth 3
    Error: … "And every pipeline window uses the cursor agent token":
    expected pipeline windows, got 6
  ```
- **Failure class:** `acceptance`
- **Expected vs observed:** Stamp-off expects the seven cursor windows from
  `27273f2b0a` (incl. specifier); tip's `cursor-forge.conf` (via `2e4a17854`
  "drop specifier seat") has six windows and no specifier line.
- **Remediation:** `swarmforge/packs/cursor-forge.conf` — do not land this tip
  until the pack change is stamp-off certified or reverted off the land path.
  BL-1094 code does not own this file.

### D2 — acceptance / behavior — blamed: coder

- **Failing command:** same BL-1113 `run_acceptance.sh` as D1 (board Outline)
- **Commit hash checked:** `89e153355e` (after `npm run compile` in `extension/`)
- **First error excerpt:**
  ```
  ✖ Pipeline Board HTML keeps stage spacing and uses three-word slugs
    Error: … /DC&nbsp;QA/ did not match
    Input: '<pre>DC&#160;QA\nBL-1113 fix-the-widget</pre>'
  ```
- **Failure class:** `acceptance`
- **Expected vs observed:** Stamp-off step asserts named `&nbsp;`; tip emits
  numeric `&#160;` from hitchhiker `35c36037f` (`Hotfix-Certification: pending`).
- **Remediation:** `extension/src/concierge/pipelineBoard.ts` /
  `specs/pipeline/steps/bl1113CursorHotfixStampOffSteps.js` — keep stamped
  blob identity or mint a BL-848 stamp-off for `35c36037f`; do not fold the
  uncertified hotfix into a BL-1094 land.

### D3 — behavior (invariant / HOTFIX_PATHS) — blamed: coder

- **Failing command:**
  `npx vitest run --config vitest.properties.config.mjs test/bl1113CursorHotfixStampOff.property.test.js`
- **Commit hash checked:** `89e153355e`
- **First error excerpt:** assertion at
  `extension/test/bl1113CursorHotfixStampOff.property.test.js:64` — HEAD blob
  for a HOTFIX_PATH diverges from `27273f2b0a` (board and/or pack paths).
- **Failure class:** `behavior`
- **Expected vs observed:** Stamp-off invariant 1 — tip blobs match
  `27273f2b0a` for all HOTFIX_PATHS; tip diverges
  `pipelineBoard.ts` and `cursor-forge.conf`.
- **Remediation:** Same as D1/D2 — strip hitchhikers from the land tip or
  complete separate stamp-offs before re-forwarding BL-1094.

### BL-1094 own gates (for completeness — not defects)

| Gate | Result |
|---|---|
| `task_commit_coherence_gate_lib_test_runner.bb` | ALL PASS |
| `dispatchGapSteps.test.js` | 18/18 |
| BL-1094 acceptance | 5/5 |
| `bl1094DispatchGapAutoroute.property.test.js` | 2/2 |
| Wiring (`DISPATCH_GAP` / steps index / handoff-protocol) | present |
| Documenter docs | present |

### BLOCKED BY hitchhikers (not omitted)

- Landing this tip would push uncertified `35c36037f` and pack change
  `2e4a17854` onto `origin/main` under a BL-1094 approval (BL-506 / BL-848).
- Extension unit shared-lane standing reds remain **BL-1112** (not this tip).

## Routing

Earliest owning role: **coder** (parcel tip includes merge-main hitchhikers;
BL-1094 implementation itself is green). Rebuild / re-forward a tip that
contains only BL-1094's exemption work (plus already-landed ancestors), or
wait for stamp-offs of the hitchhikers — do not ask QA to approve a tip that
reds the just-landed BL-1113 stamp-off.

By QA.
