# BL-1345 — QA approval (20260903)

Received: documenter commit `3c39bd3e76` (Specification.MD entry + two
how-to sections), forwarding hardener (D1 fix re-verified 15/15),
architect (D1 rework re-verified 40/40), cleaner rework, coder's fix for
the two halves hotfix `195de28861` did not touch. Bounced once by
architect for a flaky property-test generator (invariant 2's reach floor
drawn independently rather than enumerated — the same class fixed
elsewhere this session, e.g. BL-1352), reworked and thoroughly
re-verified before reaching QA.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/bl1345_stale_marker_test_runner.bb`: ALL
  PASS.
- `bash swarmforge/scripts/test/test_swarm_ensure.sh`: ALL PASS (51
  tests, the hotfix baseline — confirms the parcel's changes don't
  regress the RC-repair machinery `195de28861` fixed).
- Acceptance (`specs/features/BL-1345-a-stale-router-marker-does-not-staff-a-standing-pack.feature`):
  7/7.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1345StaleRouterMarkerStaffingSteps` — confirmed.
- `required_wiring`: confirmed present.
- Declared invariants
  (`extension/test/bl1345StaleMarkerInvariants.property.test.js`): 3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-disabled `assigned-role-mismatch`'s core comparison in
  `remote_control_health_lib.bb` (`(not= assigned-rc-name
  observed-rc-name)` → `false`) — invariant 2 failed exactly as expected
  (a wrong-role pane would read healthy again, reproducing the 2026-09-02
  outage shape). Restored (`git diff --stat` empty), re-ran ALL PASS.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population. None touch this parcel's files.
- Property suite (`npm run test:properties`): 25 files / 14 tests red.
  13 of the 14 match the exact standing-debt population from every prior
  pass this session. **One new**: my own just-landed `bl1333StampOffInvariants`
  invariant 2 — investigated: ran in isolation, 3/3 clean, confirming a
  load flake under the full concurrent suite (that test drives real git
  fixtures, ~22s, the same class as this session's other documented load
  flakes on slow real-process property tests), not a regression from
  this parcel. BL-1345's own property test — 3/3 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry, and the two how-to sections
  (`docs/how-to/BL-1020-stale-mono-router-marker-is-not-topology.md`,
  `docs/how-to/BL-514-remote-control-health-and-ensure-wiring.md`)
  confirmed present. No diagram change-trigger fired.
- Orphaned test processes: none after the property-suite run (checked
  full process args, no lingering vitest workers of my own).

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
