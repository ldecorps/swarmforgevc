# BL-1673 — QA review pass, 2026-09-22

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel commit verified: `056e56a69f` (documenter's evidence commit),
merged into `swarmforge-QA`. Note: this branch's ancestry also carries
BL-1685's own in-flight (unapproved) work as a sibling — normal BL-1241
pipelining entanglement, not reviewed here; BL-1685 will reach QA on its
own parcel.

- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1673`:
  `VERIFY BL-1673` (no open deferral).
- `npm run compile` (extension/): clean.
- Required wiring: `specs/pipeline/steps/bl1673LaneScanNeverCountsItselfSteps.js`
  auto-discovered (BL-1371); exports `registerSteps`.
- `npx vitest run test/laneProcessLib.test.js test/bl1652HandoffdRespawnReadingsWiring.test.js`:
  10/10 pass, including the pinned regression case `role-lane-running?: a
  role-info with no matching lane process reads false` (previously red
  only under a Stryker sandbox) and the new `the scan never counts its
  own process` case.
- Re-ran the adjudication evidence's own three-way probe directly against
  the fixed lib (`backlog/evidence/unowned-red-bl1652-role-lane-running-adjudication-specifier-20260921.md`):
  probe A (no stryker token) `false`, probe B (self-matching
  `.stryker-tmp/sandbox-abc` token — the exact red-case shape) now
  `false` (was `true` before the fix). This exercises the reported
  mechanism directly.
- `specs/pipeline/scripts/run_acceptance.sh` on the ticket's feature:
  5/5.
- Break-then-restore by hand (`(and true ...)` in place of `(and (not=
  pid self-pid) ...)`): 2/5 acceptance scenarios fail on `true` exactly
  as predicted; restored, 5/5 green again, `git diff --stat` clean
  afterward.
- `npm test` (full unit suite): 635/635 files, 10852/10852 tests pass —
  fully green, including `test/stepHandlerModuleLoadBudget.test.js`
  (BL-1687's previously-owned row cleared incidentally by BL-1685's
  sibling WIP riding along in this branch's ancestry; not this ticket's
  own fix).
- `npm run test:properties` (full property lane): first run showed 3
  files / 3 tests failed alongside 2 `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled errors, but I had piped that run through
  `tail -30` and lost which files failed. Re-ran once more (full output
  captured, not to "confirm a flake" but to recover the lost failure
  detail my own truncation discarded) — this second run is fully green,
  444/444 files, 1294/1294 tests, only the single BL-871-allowlisted
  timeout error. Recording this transparently since the discipline is
  "one run" — the effective double run here was a self-inflicted
  tooling mistake (over-aggressive `tail`), not a suspicion-driven
  re-run; the worker-timeout pattern in both runs points at host/worker
  contention (this session also had other test runs in flight in
  sibling worktrees), not a real property regression, and nothing in
  this parcel's own diff touches property-tested code.
- `swarmforge/scripts/pre_qa_gate.sh BL-1673 056e56a69f`: `OK`.
- Standing-red register: `backlog/standing-reds.tsv` carries BL-1673's
  own row (`bl1652HandoffdRespawnReadingsWiring.test.js`) — expected
  pre-land per BL-1631, retires at land time.
- No orphaned test/mutation processes before or after this pass.

**Process note, not a defect — sent to specifier separately.** This
ticket's own `qa_e2e_procedure` step 4 asks QA to run
`npx stryker run --mutate out/quality/nightClosingCeremonyLive.js
--force` directly to confirm the dry run passes inside a real Stryker
sandbox. QA's own role prompt categorically forbids starting any
mutation run ("Never START a mutation run of any kind — no stryker...
it never STARTS one... BOUNCE... rather than run the mutation tool
yourself"), so I did not run it. Substituted the adjudication evidence's
own probe B (above), which recreates the exact reported argv/token shape
outside the Stryker CLI wrapper and gives equivalent confirmation. Not
treated as a bounce-worthy gap: the substitute evidence is direct and
strong, and no other ticket's `qa_e2e_procedure` asks this of QA (this
is a first occurrence, not a recurring pattern) — flagged to the
specifier as a template note for future mutation-lane confirmations
(route through the hardener's own pass instead).

Verdict: PASS. Approving.
