# BL-1763 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: the one gap found (below) was in this stage's own domain
(mutation hardening) and was fixed in this pass, not bounced.

BL-113 Gherkin mutation: both scenarios are plain `Scenario:` (no
`Scenario Outline:`), so the tool correctly reports `inapplicable`
(BL-638), recorded in the feature file's own manifest.

Read `project_root_arg_lib.bb`'s `check-root` in full: it has a
DISTINCT `cond` branch per bad-arg shape (`:flag-shaped` on a leading
`-`, `:not-a-directory` otherwise), each rendered as different reason
text by `reason-text` — the `:flag-shaped` branch is precisely the
hazard BL-1517 exists for. The property test's own refusal assertion
only ever checked `REFUSED project-root <arg>:`, never which reason
followed. Verified concretely: hand-mutated `BADARG_ARBS.notADirectory`
to draw `'--help'` (a flag-shaped string) instead of a random non-flag
one — every existing assertion (both invariants, the reach floor)
stayed green, since the mislabeled draw still triggered SOME refusal
and its coverage counter still incremented under the `'notADirectory'`
key (the shape label is hardcoded in the same `.map()`, independent of
what the generator actually produced). Fixed by pinning
`EXPECTED_REASON_TEXT` per shape and folding it into the refusal-line
regex; reran the identical mutation, now caught immediately (named
both the expected and actual reason text). Restored the mutated
arbitrary (confirmed clean via `git status`), reran green (3/3).

No TS/JS production file is in this ticket's scope beyond the property
test itself and its new step handler — CRAP/DRY not run
(engineering.prompt's property-test separation rule).

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1763-….feature`: 2/2.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=soft:
  `outcome: "inapplicable"` (no Scenario Outline) — expected.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1517ProjectRootArgInvariants.property.test.js`: 3/3 (targeted;
  the pre-commit `check_property_suite_drift.sh` guard's own full
  `npm run test:properties` run at commit time also passed).
- `npm test` (extension/): 638 files / 10875 tests — 3 failures, all in
  the already-ticketed, unrelated `nightClosingCeremonyRun.test.js`
  (BL-1764, reported by me on the prior BL-1711 pass, independently
  ticketed by QA the same evening); every other file green.
- `git status --short` clean before each commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load moderate throughout, no fallback needed.

By hardender.
