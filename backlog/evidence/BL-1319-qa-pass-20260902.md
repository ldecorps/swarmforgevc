# BL-1319 QA pass — 2026-09-02

Merged documenter `4139f0418b` into QA worktree. Real production change:
stage-dwell fold on `extension/src/metrics/stageDwell.ts` (plus the new
ops seat-detail view per the human's ruling) and
`extension/src/tools/stage-dwell-report.ts`.

## Process hygiene

`pgrep -fl 'node --test|stryker'`: clean before/after in my own worktree
(a hardener mutation run from earlier in this session had already
finished).

## Independent re-run

- `npm run compile` — clean.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1319StageDwellNamesTheStage.property.test.js` — 4/4 pass.
- `npx vitest run test/stageDwell.test.js test/stageDwellReportCli.test.js`
  — 53/53 pass (35 + 18).
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1319-stage-dwell-names-the-stage-not-a-seat.feature`
  — 5/5 scenarios pass, matching qa_e2e_procedure's corrected shape (two
  seats fold to one row, bottleneck names a stage never a seat, no
  ranking-by-omission, per-seat detail preserved, single-seat output
  byte-identical to pre-fold).

## Full unit suite (`npm test`, extension/)

Same 15 pre-existing standing-red test files as this session's prior QA
passes (BL-1221/1229/1263/1265/1289/1290/1291), zero overlap with this
parcel's changed paths. 9844 passed of 9869 (up from 9831/9856 before
this parcel — the 13 new BL-1319 tests, consistent).

## Fix shape and invariants — verified by reading the diff

- The fold happens BEFORE the `PIPELINE_ORDER` membership filter
  (`computeStageDwellReportForRoles`), not after — matches the ticket's own
  corrected finding that the pre-fix bug was a DROP, not a split: a
  non-bare seat's entry used to fail the bare-name membership test and
  never have its mailbox read at all.
- `nameBottleneck` folds via the same shared `stageOfSeat` helper BL-1040
  introduced in `swarmState.ts` — one chokepoint, not a second hand-rolled
  `@` parse (invariant 1).
- `computeSeatDwellDetail` (the ops seat-detail addition, human ruling
  "Fold plus ops seat-detail") reads the per-seat `DwellRecords` the fold
  deliberately preserves, keyed by `entry.role` (the seat id) with `stage`
  as a separate field — per-seat attribution is not discarded (invariant
  3), and it is NOT folded into `StageDwellReportResult`, so the
  `/stage-dwell` optimizer payload still carries no seat id.
- Both live consumers (`bridge/bridgeState.ts`,
  `tools/stage-dwell-report.ts`) still call
  `computeStageDwellReportForRoles` directly — confirmed by grep; no new
  wiring needed, matching the ticket's own explicit "No required_wiring,
  deliberately" note (BL-1235: an anchor inside the module the parcel
  itself adds proves nothing).

## Constraints / scope honoured

Diff confined to `extension/src/metrics/stageDwell.ts` and
`extension/src/tools/stage-dwell-report.ts` (plus tests/docs) — no touch
to supervisor/chase/ensure/babysitter (explicitly out of scope per the
ticket's approval_context) or to BL-1040's board/stage-map path (same
family, different files, no overlap).

## Verdict

PASS. Landing on `main`.
