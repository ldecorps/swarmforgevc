# BL-732 QA bounce — 20260825

**Reviewed tip:** documenter `ed983add0a` (not merged — tip impure)
**Task:** `BL-732-bl642-chrome-regex-misses-multiword-role-names`
**Sibling:** `VERIFY BL-732`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** — tip not landable; not merged |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main ed983add0a  # fails
comm -12 <(git ls-tree -r --name-only origin/main | sort) \
  <(git diff --diff-filter=D --name-only origin/main ed983add0a | sort)
# → 2 paths
```

**Commit hash checked:** `ed983add0a`

**First error excerpt:**
```
main_NOT_ancestor (origin/main at e15ca0825 after BL-1081 close / BL-595 land)
paths=18 dels=2
deleted on tip vs present on origin/main:
backlog/evidence/BL-595-qa-pass-20260825.md
docs/briefings/2026-08-25.json
(+ tip also resurrects BL-1081 into active vs current done bookkeeping)
```

**Failure class:** `behavior`

**Expected vs observed:** Expected tip-pure rematch from current `origin/main`
authorizing **BL-732 paths only**. Observed stale tip that would delete
landed BL-595 QA evidence and today's briefing JSON.

**Remediation:** Rematch onto current `origin/main`; keep BL-595 evidence and
`docs/briefings/2026-08-25.json`. Authorize BL-732 only; `dels_on_origin=0`;
`origin/main` must be ancestor.

## Verdict

BOUNCE → documenter. Do not land.

By QA.

## Recovery note

First bounce tip `e9668fb28f` was handoffed before push; push lost a race with
main bookkeeping (`e15ca0825`/`70529af30`/`7b6086e6b`). Re-landed bounce
evidence on current `origin/main` with this commit; documenter should use
**this** bounce tip, not `e9668fb28f`.
