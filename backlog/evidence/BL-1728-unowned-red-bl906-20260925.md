# BL-1728 - unowned red found via its own acceptance pointer, 2026-09-25

BL-1728's `acceptance:` pointer names
`specs/features/BL-906-operator-babysitterd-freshness-watchdog.feature`
("the pointer names the feature this test serves, which must still resolve;
it is not a claim of ownership over BL-906's contract"). Running it
(`node specs/pipeline/cli.js specs/features/BL-906-operator-babysitterd-freshness-watchdog.feature`)
on this parcel: 4 of 10 pass, 6 fail, all the same shape:

    operator_runtime.bb --tick-once failed (exit 2):
    Usage: operator_runtime.bb <project-root> [--tick-once|--poll-once]
    REFUSED project-root /tmp/bl906-op-<rand>: not inside a git checkout
    (git -C <arg> rev-parse --git-common-dir failed)

Cause: `specs/pipeline/steps/bl906OperatorBabysitterdFreshnessWatchdogSteps.js`
builds its fixture roots with a bare `fs.mkdtempSync` (lines 108 and 207,
prefixes `bl906-op-` and `bl906-lc-`) - never git-init'd. Same BL-1517
(2026-09-22, 3252212a66) fallout BL-1728 itself fixes, but in this feature's
OWN step handler, a different file from the one BL-1728 touches
(`swarmforge/scripts/test/test_operator_runtime_babysitterd_watchdog.sh`).

This parcel does not touch `bl906OperatorBabysitterdFreshnessWatchdogSteps.js`
and did not cause the break. `backlog/standing-reds.tsv` has no row for it;
a grep of `backlog/active` and `backlog/paused` for the handler's filename
finds no ticket. Continuing BL-1728's own scope (its target shell test, the
declared invariant's property test) unblocked by this; flagging so it gets
a ticket and a register row before it holds a QA pass the way BL-1727's own
red held BL-1720.

By coder.
