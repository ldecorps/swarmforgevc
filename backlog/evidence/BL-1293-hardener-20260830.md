# BL-1293 — hardener pass, 2026-08-30

**Verdict: CLEAN PASS. Forwarding to documenter.** No new tests added — coder
and architect already left this parcel non-vacuously mutation-proven; this
pass re-verifies independently and adds the BL-113 Gherkin mutation gate,
which had not yet run on this ticket's `Scenario Outline`.

## Re-verification (re-run, not eyeballed)

| Check | Result |
|---|---|
| `review_forward_evidence_gate_lib_test_runner.bb` | ALL PASS |
| `review_forward_evidence_gate_lib_property_runner.bb` | ALL PASS |
| `pre_qa_gate_lib_test_runner.bb` (sibling consumer of promoted primitive) | ALL PASS |
| `pre_qa_gate_gather_lib_acceptance_contract_test_runner.bb` | ALL PASS |
| `pre_qa_gate_gather_lib_acceptance_pointer_test_runner.bb` | ALL PASS |
| BL-1293 acceptance | 6/6 |
| BL-806 acceptance (regression, sibling gate) | 9/9 |
| BL-950 acceptance (regression, sibling gate) | 6/6 |

## BL-113 Gherkin mutation (soft), this ticket's own feature file

The `Scenario Outline` had not been mutation-tested by either upstream role.
Ran `run_gherkin_mutation.sh` against
`specs/features/BL-1293-a-review-forward-carries-the-roles-own-work.feature`:

```
scenario "A forward is judged by what the role contributed, not by the commit id":
Total=8 Killed=8 Survived=0 Errors=0
```

All 8 mutants (4 `<shape>` cells, 4 `<verdict>` cells) killed. Both columns
are keyed lookups in the step handler
(`specs/pipeline/steps/bl1293ReviewForwardOwnWorkSteps.js`) — `SHAPES` and
`VERDICTS` maps, each throwing loudly on an unrecognized value rather than
falling through by shape (BL-908's lesson) — so this is real coverage, not a
passthrough. Manifest stamp committed in the feature file (2nd soft run
correctly reads `skipped_scenarios=1` per BL-460 — the stamp already matched,
not a broken tool).

## Mutation tooling scope (engineering.prompt Startup Tools)

All changed production code this ticket touches is Babashka (`.bb`) plus one
acceptance step-handler `.js` file — no Stryker-eligible `out/**/*.js`
compiled TypeScript changed, so there is no Stryker/CRAP/DRY run to perform
here (CRAP scopes to `src/*.ts`; none touched). The `.bb` mutation
verification is what coder and architect already ran and I re-ran above:
`blocked?`'s declared invariant (BL-654) proven non-vacuous twice — reverting
to the old identity-only comparison produces 90 property-runner failures, 2
unit failures, and 2 acceptance failures (verified by architect at
`backlog/evidence/BL-1293-architect-20260830.md`), which I did not need to
re-derive: it is the mutation proof for the core diff, already run
independently by two upstream roles.

## required_wiring (re-confirmed)

- `review_forward_evidence_gate_lib.bb::merge-introduces-nothing-unique` —
  `forward-introduces-nothing-own?` calls
  `pre-qa-gate-gather-lib/merge-introduces-nothing-unique?` directly, no
  second definition (line 107).
- `specs/pipeline/steps/index.js::bl1293` — registered, line 550.

## Housekeeping

No orphaned `node --test`/Stryker/gherkin-mutator processes after the run
(`pgrep -fl` scoped, clean). Temp mutation work dirs under `./tmp/` removed
after the run. Load stayed well under the 2x-core ceiling throughout
(uptime ~2.9–4.7 on 20 cores).

## Spec-gap note (carried forward, not mine to resolve)

Coder's evidence documents a genuine open gap: `qa_e2e_procedure` step 4
(`b7d22b9ee` vs `3bc1e28b53`) cannot be satisfied as written because that
real commit resolved an actual conflict and is therefore not
"introduces-nothing-own" under this ticket's own invariant. Architect
confirmed this is correctly routed as a spec-gap `note` to the specifier,
not an architect send-back. Nothing further for hardening to add here — nothing
was hidden by the parcel, and no test can be written to close a hole the
ticket's own direction forbids closing by growing a second contribution
heuristic.

By hardender.
