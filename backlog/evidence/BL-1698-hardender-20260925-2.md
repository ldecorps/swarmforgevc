# BL-1698 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second same-day hardener pass, after QA bounced 7 defects (D1-D7) and
the architect bounced 2 more (D1-D2, non-vacuous-test gaps on the
D2/D3 fixes) — both fully fixed and both re-passed clean by cleaner
and architect (`BL-1698-cleaner-20260925-{2,3}.md`,
`BL-1698-architect-20260925-2.md`). Re-ran the full Article 4.4
checklist against the reworked commit rather than trusting the prior
(pre-bounce) hardener pass.

BL-113 Gherkin mutation: the reused pre-bounce manifest (tested_at
16:44, before the rework) would have been silently REUSED by a
default `soft` OR `hard` re-run — verified empirically that `hard`
reports the identical `total=0 skipped_mutations=10` stale-reuse
result as `soft`, because this repo's `resolve-implementation-hash`
always falls back to the literal string `"unknown"` (no
`generated-dir` metadata is wired), making the `hard` level's
implementation_hash check a permanent no-op. Forced a genuine re-run
with `level=full` (bypasses the manifest unconditionally): 10/10
mutants killed across all three Scenario Outlines, 0 survivors — the
rework (chat-set-up!/ensure-chat-set-up!, resume-from-hold!'s
off-by-one and mail-hold disposition, run-gate!'s new
ensure-chat-set-up! call) did not regress Gherkin mutation coverage.
Sent a `rule_proposal` (scope `role:hardender`) recording this as a
standing rule for future post-bounce passes; the manifest's
`tested_at` timestamps now read the fresh run.

Documentation accuracy (flagged for the documenter, next in this
forward chain — not fixed here, doc content is out of hardener's
domain): `docs/how-to/BL-1697-local-parcel-driver.md`'s "By answer"
section (under "Releasing an escalated hold") still reads "re-arms
the gate for exactly one more try (so a failing gate after that
escalates again rather than looping)" — this is the PRE-D4-fix
semantic. D4's actual fix sets `fixTurnsUsed = fixTurnsLimit` (not
`fixTurnsLimit - 1`), so the answer's own fix request IS the one
extra try and the VERY NEXT failed gate check escalates immediately
with NO further fix request ever typed — there is no "one more try"
after that. The same section also doesn't state that this path is a
no-op for a mail merge-conflict hold (D5: `resume-from-hold!` only
re-arms when `:acceptancePath` is present; a mail hold's answer, if
any, is left unconsumed — operator `release` is the only way out).
This text was written by the ORIGINAL pre-bounce documenter pass
(`8238de1a89`) and was never re-verified against the D4/D5 fixes that
landed afterward — the documenter's own "doc already accurate ≠ pass
done" duty applies here regardless of what I flag, but naming it
explicitly avoids it being missed on a second pass through an
already-"NONE"-dispositioned area.

Independently re-verified (not merely re-trusting architect's
empirical claims) that the two QA/architect-bounce fixes are
non-vacuous:
- `test_babysitter_nudge_driver_seat_skip.sh` (D1/D2 chain, the
  driver-capable non-aider stub): read the fixture, confirmed it
  redefs `prompt-engine-lib/capabilities` to return `{:wake-style
  :chat-message :parcel-driver true}` for a throwaway agent name, so
  it genuinely bypasses the pre-existing `aider-agent?` short-circuit.
  6/6 pass.
- `bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js`'s D4 test:
  read the assertions — pins `fixTurnsUsed === fixTurnsLimit` and
  asserts exactly ONE `send-keys` call after the answer (not two),
  genuinely distinguishing the fixed off-by-one from the pre-fix
  `fixTurnsLimit - 1` shape. 6/6 pass.

Reviewed the new `seats-chat-ready` module-level `defonce` atom
(`local_parcel_driver_lib.bb`) for cross-invocation state leakage: a
fresh OS process (every CLI invocation, every real daemon
crash/restart) gets a fresh empty atom, matching the "since this
process's boot" contract the docstring states. A NEW parcel's own
`continue-after-merge!` always calls `chat-set-up!` UNCONDITIONALLY
(never `ensure-chat-set-up!`), so a stale atom entry from an earlier
ticket on the same long-lived seat can never suppress a new ticket's
own chat set-up — confirmed by reading, no gap found.

Checks run (all on commit `6ab4347c8c`, this pass's own tip):
- `node specs/pipeline/cli.js specs/features/BL-1698-….feature`: 9/9.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=full: 10/10
  killed, 0 survivors (see above).
- `npx vitest run test/bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js`:
  6/6.
- `bash swarmforge/scripts/test/test_babysitter_nudge_driver_seat_skip.sh`:
  ALL PASS (6 cases).
- `bash swarmforge/scripts/test/test_handoffd_startup_restores_driver_spec_writable.sh`
  (D7's own fix, from the ORIGINAL bounce — re-confirmed still green
  through this rework): ALL PASS.
- All 4 babysitter `.bb` suite runners and 6 babysitter `.sh` suites
  from `suite-manifest.tsv` (both new entries — the two files above —
  and the pre-existing ones): ALL PASS, one run each.
- `npm test` (extension/): 638 files / 10874 tests, all green
  (2 more tests than the prior pass — the D2/D3-non-vacuity fix).
  Same pre-existing per-file budget warnings on 6 unrelated files.
- No TS/JS production file in this ticket's scope — CRAP/DRY not run
  (Babashka/shell only, per engineering.prompt).
- `pgrep -f '[t]mux -S' | wc -l`: fluctuated 6-8 across this pass's
  runs (this ticket's own fixtures use fake `tmux` binaries and a
  throwaway git checkout, never a real server) — no leak pattern
  attributable to this pass. `git status --short` clean before each
  commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load 3-8/20 cores throughout, no fallback needed.

By hardender.
