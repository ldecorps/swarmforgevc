# BL-1646 — hardender review pass, 2026-09-19

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bash swarmforge/scripts/test/test_check_standing_red_register.sh (baseline, then hand-mutated added_or_changed_lines to skip validation whenever MERGE_HEAD is present, then restored)
- **Commit hash**: a3cb7db44d
- **First error excerpt**: mutant survived all 5 existing shell-lane cases - none has a merge whose own resolution adds genuinely new content; the acceptance features own scenario 3 covers this shape but the separate fast unit-test lane did not
- **Failure class**: test-coverage-gap
- **Expected vs observed**: invariant 2s second clause (a merge whose own resolution adds a new row naming a closed ticket is still refused) holds at the fast unit-test lane, not only at acceptance
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added case 06 (merge inherits cleanly, then a genuinely new unowned row is appended during resolution, still refused); commit a3cb7db44d; confirmed it now kills the hand-applied mutant

By hardender.
