# BL-1700 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Read the ticket YAML, coder's evidence, the real-model probe record, the
new library `model_steward_coder_probe_lib.bb`, `model_steward_cli.bb`'s
`run-probe`, the step handler, the doc, and all five fixture directories
in full.

Investigated a concurrency hypothesis before ruling it out: `run-fixture!`
derefs its driving `future` with a timeout and, on a wall-clock-cap
timeout, does NOT cancel that future before `finally` kills the tmux
session and `fs/delete-tree`s the throwaway root — the future keeps
polling (via `Thread/sleep` in `drive-to-end!`'s loop) until its own
`effective-max-ticks` bound, concurrently with the root being deleted.
Found empirical grounds to suspect this while auditing `/tmp`: 9 leftover
`bl1700-probe-01-one-line-fix-*` directories already present from the
coder's own real-model evidence session (`.aider.chat.history.md`,
`.aider.tags.cache.v4` present — real aider runs, not stand-in). Verified
the actual cause before treating it as a defect: reproduced the identical
wall-clock-cap path with the acceptance suite's own `--stand-in hang`
mode (`--wall-clock-seconds 2 --fix-turns-limit 1`) and confirmed no new
leftover directory appeared, immediately or 5s later — cleanup is
correct on this path. The 9 pre-existing directories are artifacts of a
REAL aider run killed externally (almost certainly a Bash-tool timeout
delivering SIGKILL/SIGTERM to the whole process tree while a 14B model
was still generating on CPU), which bypasses every `finally` in the JVM
process regardless of what the library does — not a code defect in this
ticket, matching the standing `detach_job.sh` discipline for any
real-model run exceeding the sandbox's own command timeout. No fix
needed; left the pre-existing directories untouched (not mine to
delete — harmless stray `/tmp` scratch from a completed session, not
repo or in-progress work).

Confirmed both required-wiring anchors: `model_steward_cli.bb`'s `case`
dispatches `"probe" (run-probe rest-args)`, and
`bl1700StewardCoderProbeSteps.js` exports `registerSteps`. Confirmed
`docs/index.md`'s BL-1700 entry is present and matches the how-to.

BL-113 Gherkin mutation (`run_gherkin_mutation.sh ... soft`): the one
`Scenario Outline` (scenario 01) mutated clean — 6/6 killed, 0 survived,
0 errors (embedded manifest, `tested_at` fresh). Scenarios 02-05 are
plain `Scenario:` with no Examples to mutate (BL-638) — correctly absent
from the manifest, not treated as a pass or a gap.

No TS/JS production file in this ticket's own scope beyond the step
handler (`specs/pipeline/steps/`, not `extension/src`) — CRAP/DRY not
run (out of that tool's scope, matching prior step-handler-only tickets
this session, e.g. BL-1748, BL-1763).

Observation, not a defect: a `--scenario`-scoped real invocation (fewer
than 5 fixtures) can never reach the hardcoded `pass-bar` of 4, so its
`summary.verdict` always reads `"fail"` even when every scored fixture
in that scoped run handed off — the per-scorecard `outcome`/`handedOff`
fields are correct regardless. The doc's own "Scoping to fewer fixtures"
example doesn't mention this, and none of the acceptance scenarios
exercise a scoped multi-fixture run's summary field. Low risk (the
five-fixture unscoped run is the documented pass-bar path; `--scenario`
is for triage) — flagging for the documenter/specifier's awareness
rather than bouncing, since fixing it would mean redesigning what
`summarize` means for a partial fixture set, beyond this ticket's scope.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1700-....feature`: 7/7.
- `bb -e '(load-file ".../model_steward_coder_probe_lib.bb")
  (println (model-steward-coder-probe-lib/selftest!))'`: all 5 fixtures
  red on `files/`, green on `solution/`.
- `specs/pipeline/scripts/run_gherkin_mutation.sh ... soft`: 6/6 killed,
  0 survived, 0 errors on the one Scenario Outline; manifest committed.
- `cd extension && npm test`: 638 files / 10875 tests, all green (the
  previously-observed unrelated `nightClosingCeremonyRun.test.js` red
  did not reproduce this run).
- `git status --short` clean before commit (beyond the mutation-stamp
  manifest, which is committed as part of this pass).
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner. `ps aux | grep -E 'probe.tmux.sock|stand-in.sh'`: none, after
  both my own acceptance run and mutation run.

By hardender.
