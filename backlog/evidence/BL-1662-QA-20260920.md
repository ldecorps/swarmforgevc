# BL-1662 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Reviewed the parcel's own commits (fedb1ea215..a5c0bc78b4) scoped to
  `swarmforge/scripts/check_merge_deletion.sh`,
  `specs/features/BL-1242-merge-never-silently-drops-branch-work.feature`,
  `specs/pipeline/steps/bl1242MergeBranchWorkDeletionSteps.js`,
  `specs/pipeline/steps/lib/bl1242MergeBranchWorkDeletionCli.sh`,
  `docs/how-to/BL-1242-merge-deletion-guard.md`. `attribution_for_path`
  now walks each side's whole history (newest first) for the first
  ticket-tagged commit, matching the invariant.
- `node specs/pipeline/cli.js specs/features/BL-1242-merge-never-silently-drops-branch-work.feature`:
  14/14 ok, including new scenarios 09 and 10 (HEAD-side and
  MERGE_HEAD-side untagged-removal walks).
- `bash swarmforge/scripts/test/test_merge_deletion_guard.sh`: PASS 01-08,
  then exits on the standing red owned by BL-1664 (register row,
  `backlog/standing-reds.tsv:40`) — pre-existing, unrelated to this
  ticket's diff, not this ticket's to fix.
- `npm test` (extension/): 635 files / 10819 tests passed.
- `npm run test:properties` (extension/): 432 files / 1262 tests passed
  (one allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"`
  benign error, BL-871).
- `pre_qa_gate.sh BL-1662 a5c0bc78b4`: OK (required_wiring satisfied).
- No straggler test/mutation processes before or after.
- `qa-sibling-check.js status --ticket BL-1662`: VERIFY (no open
  deferral).
- Land step: `land_step_cli.bb BL-1662 HEAD` first run hit a mechanical
  worktree-creation failure (stale worktree path); rematch (bounded, one
  retry) succeeded: `LAND_REPLAY land-replay/BL-1662-773a2ebd8d
  8c418509c2` naming several LANDED_SIBLING tickets (already on
  origin/main) and excluding BL-1630/BL-1656/BL-1663 paths (unlanded,
  correctly excluded). Manual inspection of the replay's two-tree diff
  against origin/main found it additionally reverted content origin/main
  already carries but the replay's own base snapshot predates (BL-1630's
  bounce_history, a specifier ruling note in BL-1663's ticket YAML, and a
  coder.prompt section — none this ticket's own, all already-landed other
  tickets' work per BL-1408's two-tree-diff hazard). Did not land that
  replay commit; hand-built a tip-pure commit off `origin/main` carrying
  only this ticket's own paths (the parcel's own diff plus its own
  evidence files) instead.
