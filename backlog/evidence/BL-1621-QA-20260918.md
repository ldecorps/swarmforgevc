# BL-1621 — QA review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel merge commit: `55e92177d0` ("Merge documenter 6d719d942a into QA.").
Sibling check: `qa-sibling-check.js status --ticket BL-1621` ->
`VERIFY BL-1621` (no open deferral).

- Straggler check: hardener's own `stryker` run was active in
  `.worktrees/hardender` before verification (a different worktree, not an
  orphan of this run) and gone by the time verification finished. No
  leftover test/mutation processes from this run itself.
- Standing-red register (`standing_red_register_cli.bb`): all rows owned,
  `unowned: []`, before the retirement below.
- Wiring gate: `pre_qa_gate.sh BL-1621 55e92177d0` -> OK.
- Unit suite: first `npm test` failed the "suite file budget" gate on
  `extension/test/emitLifecycleSnapshotCli.test.js` (11.2s) while the
  hardener's concurrent `stryker --concurrency 4` mutation run was consuming
  the host. Same file, same class of host-load noise already diagnosed in
  `backlog/evidence/BL-1623-QA-20260917.md` (12.8s there, resolved by a
  clean re-run). Solo re-run here: 5.32s, well under the 7.0s budget.
  Full `npm test` re-run once the stryker run finished: 626 files passed,
  10677 tests passed, exit 0 (the file read as a "watch" tier, 9.6s,
  not refused). Not a regression from this parcel — BL-1621 touches only
  the property test file, its feature, handler, evidence and the two
  register files.
- Property suite: `npm run test:properties` exit 0. Target file green in
  the full lane (the ticket's core claim). Unrelated noise (bl603-prop
  tmp-git-log errors, front-desk-bot BL-582 edit-refused lines, the
  allowlisted BL-871 `[vitest-worker]: Timeout calling "onTaskUpdate"`)
  are pre-existing fixture/test artifacts, not from this parcel.
- Target file solo run (`vitest.properties.config.mjs`,
  `test/bl1297MergeOwnPathsInvariants.property.test.js`): 3/3 green in
  7.38s (invariant 2: 18 cases, invariant 3: 18 cases — reach maps
  unchanged, confirmed programmatically by the ticket's own acceptance
  scenario 02).
- Acceptance: `run_acceptance.sh specs/features/BL-1621-*.feature` -> 3/3
  scenarios pass.
- Register retirement (QA's own step, per the ticket's qa_e2e_procedure
  step 3 and the row's own note "row leaves with BL-1621 land" — BL-1631's
  land-step automation for this is minted but not yet built): removed the
  `bl1297MergeOwnPathsInvariants` row from `backlog/standing-reds.tsv` and
  its mirror from `swarmforge/scripts/property_suite_standing_allowlist.tsv`
  after confirming the file green in the full lane above.
  `standing_red_register_cli.bb .` now names no row for the file; 5 rows
  remain, all owned, `unowned: []`.
- `git diff main...HEAD --name-only`: property test, feature, handler,
  evidence (BL-1621 and BL-1614 sibling batch), the two register files —
  matches the ticket's constraints.

By QA.
