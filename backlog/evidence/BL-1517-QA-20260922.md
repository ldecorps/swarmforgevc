# BL-1517 — QA review pass, 2026-09-22

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel commit verified: `fee1f4a817` (documenter's tip `aa02597fcf` merged
into `swarmforge-QA`; ticket's own hardener lineage `b14cd073de` confirmed
an ancestor).

- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1517`:
  `VERIFY BL-1517` (no open deferral).
- No orphaned test/mutation processes before or after this pass
  (`pgrep -fl 'node --test|stryker|vitest'` clean of any suite/mutation
  runner both times).
- `npm run compile` (extension/): clean.
- Wiring (`required_wiring`): grepped all six named sites -
  `main_sync_status_cli.bb`, `operator_runtime.bb`, `expedite_cli.bb`,
  `dispatch_gap_sweep_harness.bb`, `dropped_parcel_sweep_harness.bb`,
  `commit_integrity_856_scenarios_cli.bb` - each sources
  `project_root_arg_lib.bb`. `bl1517ProjectRootArgSteps.js` exports
  `registerSteps` per BL-1371's discovery pattern (no manual pipeline
  wiring needed, confirmed against `docs/how-to/BL-1371-step-handlers-
  register-by-discovery.md`).
- `node specs/pipeline/cli.js specs/features/BL-1517-a-project-root-
  argument-is-a-repository-or-refused.feature`: 11/11 pass.
- Exercised the fix directly: `bb swarmforge/scripts/main_sync_status_cli.bb
  --help` now exits 2 with `REFUSED project-root --help: looks like a
  flag, not a path` and creates no `--help/` directory (the original
  defect's exact repro from the ticket).
- `npm test` (full unit suite, extension/): 635/636 files, 10852/10853
  tests pass. The one failure, `test/stepHandlerModuleLoadBudget.test.js`
  (bl1634 over budget), is standing-red register row 35, owned by
  **BL-1687**, unrelated to and untouched by BL-1517's diff.
- `npm run test:properties`: first run showed 3 failed files with
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled-error noise
  (the BL-871-allowlisted exception) but the earlier capture was
  truncated (`tail -30`) before the failing file names were recorded;
  re-ran once to capture full output (not a re-confirmation of a flake -
  the first run's evidence was lost to my own truncation, never
  reviewed) and got 445/445 files, 1297/1297 tests pass, with the same
  allowlisted noise (2 occurrences). Treating the second, fully-captured
  run as authoritative; nothing in BL-1517's diff touches property-tested
  code.

Verdict: PASS. Approving.
