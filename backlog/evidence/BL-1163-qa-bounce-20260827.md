# BL-1163 QA bounce — 20260827

**Routing:** coder (acceptance wiring missing on tip — rematch)

## Failing command

```
specs/pipeline/scripts/run_acceptance.sh \
  specs/features/BL-1163-handoffd-parse-error-bl668-paren-hotfix.feature
# Scenario "…": no step handler matched
# "Given a throwaway project root with a fake tmux socket and roles.tsv"
```

## Commit tested

`e7842d33f5` (coder handoff). Already ancestor of `origin/main` / QA HEAD, but
acceptance steps from that commit are **not** present on current tip.

## First error excerpt

```
Error: Scenario "one-shot handoffd flags still reach their done log lines after
the paren repair": no step handler matched "Given a throwaway project root with
a fake tmux socket and roles.tsv"
```

`specs/pipeline/steps/bl1163HandoffdParseErrorBl668ParenHotfixSteps.js` is absent
from tip; `index.js` has no `bl1163…` require. File was present at `e7842d33f5`
and dropped by `219ccfb4f` (BL-781 QA gate ancestry **ours** merge).

## Failure class

behavior

## Expected vs observed

**Expected:** required_wiring registered; acceptance scenarios green; handoffd
loads without parse error.

**Observed:**
- Production paren repair is already on `origin/main` —
  `test_handoffd_one_shot_flags_parse.sh` → ALL PASS; `bb` load of handoffd.bb
  reaches usage (no EOF/unmatched delimiter).
- BL-1163 acceptance feature exists, but step module + index registration are
  gone from tip → acceptance red / required_wiring unmet.

## Defects

**D1 — acceptance wiring lost after land (blame: coder rematch):** Re-land tip-pure
BL-1163 that restores `bl1163HandoffdParseErrorBl668ParenHotfixSteps.js` and the
`index.js` require atop current `origin/main`, without hitchhikers. Paren fix need
not be re-done if already present.

By QA.
