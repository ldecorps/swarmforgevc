# BL-1587 — architect review pass, 2026-09-16

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: git ls-tree -r HEAD --name-only -- extension/backlog/
- **Commit hash**: 0a6d9b6053
- **First error excerpt**: extension/backlog/evidence/BL-1587-cleaner-20260916.md added by 0a6d9b6053 (cleaner evidence commit)
- **Failure class**: behavior
- **Expected vs observed**: backlog/evidence/BL-1587-cleaner-20260916.md at the repo root, not nested under extension/
- **Blamed role**: cleaner
- **Remediation pointer**: git mv to backlog/evidence/; 0a6d9b6053 reverted (15566ca40d) so the stray never reaches main; recurring class already tracked by backlog/paused/BL-1552

By architect.

## Detail

Checks run, all clean except D1:

- `dependency-gate.js` on every changed file: PASSED, no forbidden edges.
- `co-change-report.js` on every changed file: informational only, no
  suspected coupling above threshold.
- Floor audit (`git diff main...HEAD -- extension/test | grep -E
  '(numRuns|assertReachFloor|>= *[0-9]|> *0|runsPerCell)'`, 132 lines) read
  line by line against `backlog/evidence/BL-1587-coder-20260916.md`'s table:
  every removed `numRuns` literal reappears as the budget handed to
  `runsPerCell` at the same value; every removed floor value reappears as
  the floor argument to `assertReachFloor`; no numeric value went down.
  Invariant 1 (no floor/budget/tolerance lowered) holds.
- Invariant 2 (reached by construction) hand-verified on `bl1254` (chained
  `hitCommit` draw from the non-empty `created` set) and `bl1495` (outer
  loop over `PROVIDER_KEYS`, `fc.subarray` construction for the
  aider-present/aider-absent cells replacing the removed `fc.pre` filter) —
  both genuinely reach by construction.
- `bl1305FixtureAgentBinary.property.test.js`: confirmed unchanged in the
  diff and confirmed by reading source that it carries no genuine
  reach-floor pattern — architect confirms the coder's "not a floor after
  all" exclusion; the BL-1583 census should drop this file from its count.
- `extension/src` and `extension/test/helpers/reachFloors.js`: confirmed
  unchanged.
- `git diff main...HEAD --name-only`: only the 10 migrated test files, the
  step handler, and evidence — no epic scope-creep.
- Acceptance (`specs/features/BL-1587-sampled-reach-floors-sweep-3-of-6.feature`):
  12/12 pass.
- Standalone green spot-checks: `bl1343ReplayNeverDropsOwnPathInvariants`,
  `bl1495BaiGatewayInvariants`, `bl1445StaffingGateWiringTestDecidesOverrideInvariants`.
- Commit bylines present throughout the chain.

D1: `0a6d9b6053` ("BL-1587: cleaner review pass evidence (NONE)") added its
own evidence file at `extension/backlog/evidence/BL-1587-cleaner-20260916.md`
instead of `backlog/evidence/BL-1587-cleaner-20260916.md` — almost certainly
because the cleaner's shell was in `extension/` (npm runs from there) and
the relative path resolved against that cwd. `git log --all --
extension/backlog/` shows 17 commits by four roles since 2026-09-07, and
`git ls-tree -r HEAD -- extension/backlog/` showed 7 tracked strays still on
main before this pass (BL-1278, BL-1464, BL-1478, BL-1503, BL-1558,
BL-1569, plus this parcel's BL-1587). The class is already ticketed —
`backlog/paused/BL-1552-a-nested-backlog-tree-never-enters-a-commit.yaml`
(a commit-time guard + a 3-file move) — but that ticket is pinned to only 3
of the now-7 strays (minted 2026-09-13 against an earlier main) and its own
scope explicitly defers prevention to the future guard rather than to
editing role prompts. This bounce is scoped to THIS parcel's own instance
only; BL-1552 remains the tracking ticket for the guard and the
pre-existing backlog of strays — not proposing a new ticket.

Per "A Bounce Must Be Reverted Out Of The Bouncing Branch" (the file was
wrongly ADDED, not omitted), `0a6d9b6053` was reverted in this worktree at
`15566ca40d` so the stray does not ride forward; the ticket's
`bounce_history` was updated via `record-bounce.js` (commit `f29fe6e920`).

By architect.
