# BL-1359 — QA APPROVAL, 20260903

Merged documenter `75f689c781` (forwarding hardener `6c9df5bbc8`,
architect `e8d060b5b8`). Reviewed the full coder/cleaner/architect/
hardener/documenter evidence chain; independently re-ran rather than
trusted.

## Independent re-verification

- `bb swarmforge/scripts/test/bl1359_merge_charged_test_runner.bb` —
  ALL PASS.
- `run_acceptance.sh specs/features/BL-1359-a-merge-is-charged-only-with-
  what-it-introduced.feature` — 7/7.
- `npx vitest run --config vitest.properties.config.mjs
  bl1359MergeChargedInvariants` — 3/3 (all three declared invariants).
- **Real-repo regression, reproduced independently** (own Babashka
  script driving the actual `git diff-tree` commands, not the coder's
  claimed numbers): QA-exclusive paths charged —
  `15dc336877` 7→**0**, `e3ce579112` 4→**0**, `f8dc07963` (BL-590's own
  catch) unchanged at 13. Matches the ticket's and coder's table exactly.
- `bash swarmforge/scripts/test/test_babysitter_check.sh` — ALL PASS
  (regression, unaffected).
- `required_wiring` confirmed: `bl1359MergeChargedOnlyWithIntroducedSteps`
  registered in `specs/pipeline/steps/index.js:955`.
- Full unit suite (`npm test`, extension): 576/591 files, 9924/9949
  tests — identical 15-file/25-test standing-red set to every other pass
  today (BL-1356's own QA evidence names the same set); none intersect
  this parcel's diff.
- No orphaned test/mutation processes before or after any run.
- `backlog/hotfix-ledger.yaml` untouched.
- Ancestry: `git merge-base --is-ancestor 6c9df5bbc8 HEAD` — OK (hardener
  merge is an ancestor of the commit being approved).

## Two pre-existing untracked reds

Architect/hardener already grepped and confirmed genuinely untracked
(`bl962_merge_adjudication_test_runner.bb`'s "no second ancestry
predicate" breach from BL-1187, and `BL-631` acceptance 16/17). Re-grepped
myself: still no open ticket for either. Already minted by the specifier
(`c4a396d5c2`, BL-1372/BL-1373) — nothing further needed here.

## Verdict

APPROVE. Merge-up broadcast sent; landing next.

By QA.
