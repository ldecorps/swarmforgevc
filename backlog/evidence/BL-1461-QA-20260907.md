# BL-1461 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Why this ticket matters to this session specifically

This is the direct fix for my own finding earlier today
(`backlog/evidence/BL-1446-QA-followup-landclean-can-miss-preexisting-entanglement-20260907.md`):
`land-plan`'s candidate walk was bounded to `parcel-own-base` (the
parcel's LAST hop), so a sibling's unlanded work absorbed into a shared
batch-role branch BEFORE that hop was invisible, and `LAND_CLEAN` was
reported for a tip that was not, in fact, clean.

## Verification

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- `specs/features/BL-1461-...feature`: 5/5 pass, including the
  three-position scenario (pre-first-hop, between-hops, post-last-hop) and
  the landed-history-exclusion and replay-completeness scenarios.
- **qa_e2e step 2 — re-ran the EXACT real incident inputs from this
  session's own original discovery**, not just the fixture:
  `bb land_step_cli.bb BL-1448 6c8caf27fb` and `... d854af2126`. Both
  commits are still reachable in this worktree. Before this fix, both
  answered bare `LAND_CLEAN` naming no sibling (the defect). Now: both
  correctly answer `LAND_ESCALATE` naming `BL-1349,BL-1444,BL-940` as
  entangled/unlanded (via the tip-pure replay path — `own-paths identical
  to origin/main`, since BL-1448 is itself fully landed by now, so there
  is genuinely nothing left to replay, but the entanglement is correctly
  SEEN and NAMED, which is the invariant that matters — never a bare
  `LAND_CLEAN` again). One transient `could not create worktree` on the
  first attempt for `d854af2126` cleared on retry (stale git-state
  artifact under this session's own heavy same-day git object churn, not
  reproducible) — confirmed on a clean retry.
- qa_e2e step 3: directly compared the wide, unbounded 3-arg
  `entangled-siblings` call against `land-plan`'s own verdict for
  `6c8caf27fb` — both agree exactly: `{:entangled #{BL-1349 BL-940
  BL-1444} :unlanded #{BL-1349 BL-940 BL-1444}}`.
- qa_e2e step 4: `grep "Until BL-1461 lands" swarmforge/roles/QA.prompt`
  — empty, correctly retired by the documenter.
- Sibling features, all independently re-run (not trusting
  architect/hardener's evidence alone): BL-1432 5/5, BL-1431 4/4, BL-1389
  5/5, BL-1241 4/4, BL-1375 7/7, BL-1446 5/5, BL-1447 3/3 — all green, no
  regression.
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass.
- No file under `extension/src` changed by this ticket's own diff (bb +
  test-only steps handler) — the full compiled unit suite's last
  established green state (611/611) stands unaffected.
- The architect's own evidence notes the coder deferred qa_e2e step 2 to
  QA (BL-1448 long since closed by the time the coder reached this check;
  correctly did not force it against the live repo) — confirmed this is
  exactly the check I ran above, now with a positive result.
- The `BL-1468-premature-promotion-coder-20260907.md` evidence riding
  along (an unrelated coordinator-promotion-timing finding on a different
  ticket) is already correctly routed per architect's own note (matches
  BL-1469) — not this ticket's content, not actioned here.

## Disposition

Approved. No rework needed. Landing next.

By QA.
