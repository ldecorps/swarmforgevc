# BL-1439 — coder pass, 2026-09-05

## What was wanted

Discharge the five hardening gates the hardener deferred on 2026-08-19
(recorded in `backlog/hardening-debt-ledger.yaml`, BL-942), whose only
verb was `--defer`; give the ledger a discharge verb; remove the
register rows once all five are discharged.

## What landed (the ledger mechanics - fully done)

- **`swarmforge/scripts/hardening_debt_ledger_lib.bb`**: `discharge-debt`
  (matches a row by `(parcel, gate)` - the real ledger's own identity
  today, since no two rows share that pair - and sets
  `:discharged-at`/`:discharged-evidence`; refuses, changing nothing,
  with no evidence path or no matching row) and `field->key` gains the
  two new scalar fields (parse picks them up with no other parser
  change). `outstanding-debt` now excludes discharged rows (invariant 2)
  - the ONE filter both `hardening_debt_ledger_read.bb`'s `--parcel`
    lookup path (unaffected, still reads raw rows) and
    `standing_red_register_cli.bb` (via `outstanding-debt`) read.
- **`swarmforge/scripts/hardening_debt_ledger_update.bb`**: `--discharge
  <parcel> <gate> --evidence <path> [<discharged-at>]` - the row is
  NEVER deleted (invariant 1); refuses (exit 1, nothing written) on a
  missing `--evidence` flag or no matching outstanding row.
- **`swarmforge/scripts/hardening_debt_ledger_read.bb`**: now prints
  EVERY row (discharged or not) with its discharge fields (null when
  outstanding) - a discharged row's evidence pointer stays visible
  (qa_e2e_procedure step 2), distinct from `outstanding-debt` (which
  DOES drop it, for the register/throttle).
- Unit coverage: 8 new assertions in `hardening_debt_ledger_lib_test_runner.bb`
  (discharge matching, the untouched-row guarantee, both refusal paths,
  the outstanding-debt exclusion, round-trip through render/parse); 10
  new CLI wiring checks in `test_hardening_debt_ledger_cli.sh`.

## The five gates themselves - ONE discharged for real, four blocked

1. **BL-956 / gherkin-mutation** — **DISCHARGED.** Ran clean:
   `bash specs/pipeline/scripts/run_gherkin_mutation.sh
   specs/features/BL-956-....feature "" specs/pipeline/steps/index.js full`
   completed in 19.1s (no BL-687 stall this time), 6/6 mutants killed, 0
   survivors, 0 errors. Discharged via the new verb; evidence
   `backlog/evidence/BL-1439-bl956-gherkin-mutation.md`.

2. **BL-620 / mutation** (telegram-front-desk-bot.ts,
   telegramFrontDeskBotCore.ts, telegramTopicDecisions.ts) — **BLOCKED,
   stays outstanding (invariant 3).** `mutation_cooldown_gate.bb` reports
   `DECISION: skip-cooldown` for the first two files: `file_age_days
   0.12` against a 3-day window. Not a false reading — `git log` shows
   `telegramFrontDeskBotCore.ts` was touched by BL-1425 (landed on this
   branch a few hours before this pass, `2cd4072055`). The gate is
   working as designed: a file this fresh has no stable mutation
   surface to test yet. Recorded here as the attempt; the ledger row is
   untouched.

3. **BL-955 / mutation** (negotiationTelegramRelay.ts,
   negotiationTelegramRouting.ts, telegramFrontDeskBotCore.ts) —
   **BLOCKED, same reason.** Shares `telegramFrontDeskBotCore.ts` with
   BL-620's set; same fresh-cooldown block.

4. **BL-956 / mutation** (pipelineBoard.ts) — **BLOCKED, same class.**
   `file_age_days 0.10` — also touched by `2cd4072055` a few hours ago.
   (Its SIBLING row, gherkin-mutation on the FEATURE file rather than
   this TS file, has a different cooldown target and was clear — hence
   discharged above while this one is not.)

5. **BL-954 / mutation** (bounceRevertGitAdapter.ts,
   bounceRevertVerdict.ts, record-bounce.ts) — **BLOCKED, but by a
   DIFFERENT, pre-existing, unrelated defect, not cooldown.** All three
   files clear cooldown (8.81-17.29 days). Stryker's dry run
   (`stryker.bl1439.config.json`, mirroring BL-941's own precedent
   exactly) runs the WHOLE suite before instrumenting any mutant
   (`vitest.related: false`), and the whole suite currently has TWO
   standing reds unrelated to this file set:
   - `extension/test/operatorRuntimeBbFixtureClosure.test.js` -
     **fixed in this parcel** (see below): `OPERATOR_RUNTIME_BB_FILES`
     had drifted 5 more entries behind the real `operator_runtime.bb`
     load-file closure since it was last caught up. This is the exact
     "hand-mirrored production closure" class this session already
     fixed three times (BL-1398/1399/1420) - a mechanical, safe,
     in-scope-adjacent list addition, so fixed directly rather than
     ticketed.
   - `extension/test/constitutionDocCitations.test.js` - **NOT fixed
     here.** `swarmforge/constitution/articles/01_roles.md` (Art
     Director, BL-1417/1418) and `03_backlog.md` (deprecator, BL-1379-era)
     cite `docs/design/artifact-inventory.md`, `docs/design/system.md`
     and `docs/deprecated/`, none of which exist on disk yet. This is
     substantive documentation content (Art Director's own inventory/
     system docs, or a deprecator-retirement target directory) - out of
     the coder's scope to author, and a new, currently-UNOWNED standing
     red in its own right (not yet in `backlog/standing-reds.tsv`).
     Reported via a priority-00 note to the specifier alongside this
     handoff; BL-954's mutation pass is blocked until it (or a targeted
     scope narrowing of the dry run) lands.

## Non-vacuity (byte-identical restore confirmed)

Mutated `discharge-debt`'s match condition to always find no row
(`(and false ...)`) - the CLI wiring test's 4 new `--discharge` checks
all failed (no row ever gained `discharged_at`, both refusal-path checks
also failed since nothing could ever succeed to contrast against).
Restored; `diff` empty; `bash test_hardening_debt_ledger_cli.sh` back to
ALL CHECKS PASSED.

## Verification

| check | result |
|---|---|
| `bb swarmforge/scripts/test/hardening_debt_ledger_lib_test_runner.bb` (+8) | ok |
| `bb swarmforge/scripts/test/bl942_hardening_debt_ledger_property_runner.bb` | ok, unaffected |
| `bash swarmforge/scripts/test/test_hardening_debt_ledger_cli.sh` (+10) | ALL CHECKS PASSED |
| `bb swarmforge/scripts/hardening_debt_ledger_read.bb .` (live) | 5 rows, BL-956/gherkin-mutation shows `discharged_at`/`discharged_evidence`, the other 4 show both `null` |
| `bb swarmforge/scripts/standing_red_register_cli.bb .` (live) | still reports all 5 hardening rows (owned, BL-1439) - EXPECTED: the register rows themselves are independent of ledger discharge (they were added directly to `backlog/standing-reds.tsv` at mint) and leave only "in the same commit that discharges the last of the five" per the ticket's own text; 4 of 5 gates remain genuinely undischarged, so they correctly stay |
| gherkin-mutation run for BL-956 | 6/6 killed, 0 survivors, 0 errors, real completed run |
| `extension/test/operatorRuntimeBbFixtureClosure.test.js` | 6/6, was 4/6 |
| full `npx vitest run` (extension) | 1 known unrelated flake (tmp-dir sweep race, `telegramCursorOperatorExec.test.js`, passes alone - not this ticket's) + 1 known unfixed pre-existing red (`constitutionDocCitations.test.js`, reported, not fixed here) |

## required_wiring

- `swarmforge/scripts/hardening_debt_ledger_update.bb::--discharge` -
  present.
- `swarmforge/scripts/hardening_debt_ledger_lib.bb::discharged` -
  present (`:discharged-at`/`:discharged-evidence`, `outstanding-debt`'s
  filter).
- `specs/pipeline/steps/bl1439DeferredHardeningGatesDischargedSteps.js::registerSteps` -
  present. **Acceptance run at this commit: 3/4, not 4/4** (`node
  specs/pipeline/cli.js specs/features/BL-1439-....feature`). Scenarios
  01 (both Examples rows) and 02 pass against a fixture ledger - the
  discharge mechanics themselves are fully proven. Scenario 03, which
  reads the LIVE `backlog/hardening-debt-ledger.yaml`, correctly FAILS:
  it finds the same 4 genuinely-outstanding 2026-08-19 rows named above,
  because they genuinely are still outstanding. This is not a defect in
  the scenario or the handler - forcing it green would mean either
  discharging a gate that did not actually run (never done here) or
  writing the assertion to lie about the ledger's real state (also never
  done here). The remaining rows discharge once BL-620/BL-955/BL-956-mutation
  clear cooldown again and once the `constitutionDocCitations` red is
  resolved for BL-954; a follow-up pass (this ticket, re-run, or a
  successor) closes scenario 03 then.

## Invariants

1. **The row is never deleted, only marked.** `discharge-debt` never
   removes a row; `render-ledger`/`parse-ledger` round-trip the new
   fields exactly (unit-tested).
2. **One filter for the register and the throttle.** `outstanding-debt`
   is the only place a discharged row stops counting; the register CLI
   was NOT changed (it already calls `outstanding-debt`, per the
   ticket's own design).
3. **A gate that cannot complete stays outstanding, never discharged by
   assertion.** Four of five gates above are genuinely blocked (three by
   a real, working cooldown gate correctly protecting freshly-touched
   files; one by an unrelated pre-existing suite-wide defect) and none
   of the four was force-discharged.

## Out of scope (per the ticket's own text, and per role scope)

- Changing which gates the hardener may defer, or the cooldown gate -
  untouched; both blocks observed above are the gate working correctly.
- The register's rule for how deferred-gate debt is aged/counted -
  untouched (human ruling already declined to change it this morning).
- Authoring `docs/design/artifact-inventory.md`, `docs/design/system.md`,
  or a `docs/deprecated/` directory - Art Director's/documenter's own
  content, not a coder's mechanical fix; reported instead.
