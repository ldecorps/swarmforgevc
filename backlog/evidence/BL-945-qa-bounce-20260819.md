# BL-945 QA bounce — 2026-08-19

## Reviewed commit
`57bf1d6f28` (merge of documenter's `ff36ddeb47` into QA). Lineage
verified: `bfeeef67e` (BL-942, unrelated batch sibling), `c1c6163f0`
(BL-943, unrelated batch sibling), `c6c7a443e`/`e0f7ddb4c`/`46c0903fb`/
`c9162b588`/`ff36ddeb47` (BL-945's own coder/architect/hardener/documenter
chain) are all ancestors of this commit via `git merge-base
--is-ancestor`.

## Batch note (Article 2.6 check, not a defect)
This commit's diff also carries BL-942 and BL-943 files (shared cleaner/
architect/hardener branch history). Checked before starting: BL-942 is
still architect-bounced and unfixed (`backlog/evidence/BL-942-architect-
bounce-20260819.md`, no later fix/re-verify commit in range); BL-943 has
a coder commit but no architect/hardener/documenter pass yet (no evidence
file, `status: todo`) — confirmed still in-flight (architect's worktree
was observed live-running `BL-943-fixture-cleanup-never-decides-the-
verdict.feature` acceptance during this QA pass, PID 45378/65809,
`.worktrees/architect`, not orphaned). Neither is satisfied, so
documenter forwarding only BL-945 is correct — not a batch-forward gap.

## Complete inventory (Article 4.4 — full pass, not first-failure-stop)

1. **Doc landed on `main`** (this QA worktree): `docs/branding/
   icon-system.md` exists with §4d and §5a present; only the document
   landed — `find docs/branding -type f` returns exactly that one file,
   no `gen7.py`/`gen8.py`/assets (out-of-scope constraint honored).
2. **Citations resolve**: `local-engineering.prompt` Rule 6, `epicIcon.ts`,
   `topicIcon.ts` all cite the doc; all three now resolve on `main`.
3. **Required wiring** — independently grepped: `ARTICLES_DIR` in
   `extension/test/constitutionDocCitations.test.js` carries a comment
   with the literal substring `constitution/articles` (the D2 bounce fix).
4. **Guard test** (`extension/test/constitutionDocCitations.test.js`) — 6/6
   PASS in isolation.
5. **Property test**
   (`extension/test/constitutionDocCitationsInvariant.property.test.js`,
   `--config vitest.properties.config.mjs`) — 4/4 PASS in isolation.
6. **Acceptance**
   (`specs/features/BL-945-constitution-doc-citations-resolve-on-main.feature`)
   — 4/4 PASS (scenarios 01, 02, 03's two outline rows).
7. **Live prove-then-revert** (qa_e2e_procedure step 5, exercised
   directly, not just read from prior evidence): appended a citation to
   `docs/does-not-exist.md` in `swarmforge/constitution/articles/
   05_amendments.md`, re-ran the guard — exactly 1 test failed, naming
   the article and the dangling path; restored the article via the
   pre-edit backup, re-ran — 6/6 green again, `git status --short`
   confirmed byte-identical restore.
8. **Full extension unit suite** (`npm test`, detached tmux, host load
   30.79 at start, climbing) — 10 test files failed. Triaged:
   - **D1 — REAL, BL-945-caused**: `test/tmpDirMigrationGuard.test.js >
     the real extension/test/ tree has zero raw mkdtemp call sites
     outside the shared helper` FAILS, reproduced identically in an
     isolated solo re-run (6.15s). Both of BL-945's own new test files
     call `fs.mkdtempSync(path.join(os.tmpdir(), ...))` directly instead
     of the shared `extension/test/helpers/tmpDir.js`'s `mkTmpDir()`
     (BL-420 — the single sweep point every other extension test's
     fixture root goes through):
     - `extension/test/constitutionDocCitations.test.js:41,62`
     - `extension/test/constitutionDocCitationsInvariant.property.test.js:69,90`
     Introduced by coder in `0bfd52db4` (the main landing commit) and
     `c6c7a443e` (the property test). Not a style nit: the two sites do
     have their own hand-rolled `try/finally`, but that is exactly the
     ~147-hand-rolled-variants pattern BL-420 replaced with one shared
     sweep point precisely so cleanup coverage doesn't depend on each
     file remembering its own `finally`.
   - `test/tmuxReaperGuard.test.js` also fails, but this is the **same
     standing BL-631 defect already bounced** this session
     (`backlog/evidence/BL-631-qa-bounce-20260819.md`,
     `git_handoff` to coder sent 2026-08-19T10:51Z) —
     `bl631BabysitterDetectsPipelineCodeOnMainSteps.js`'s missing
     `fixtureReaper.track()` wiring, untouched by BL-945. Not re-bounced
     here; already tracked under BL-631.
   - `test/conciergeTick.test.js` (×2) — reproduced deterministically in
     isolation, but zero diff touches this file or its `extension/src/`
     dependencies in this merge (`git diff --stat c1b3602bf 51dca4abe --
     extension/src/` empty). Pre-existing, unrelated to BL-945 — already
     surfaced to the coordinator via `note` during the BL-631 pass; not
     re-surfaced here.
   - 6 further failures (`dependencyGateCliReportsAndScope`,
     `dependencyGateCliStorageGlobals`, `emitLifecycleSnapshotCli`,
     `renderBriefingBurndownCli`, `renderBriefingDiagramsCli`,
     `startBridgeHeadlessCli`) — all either a plain `Test timed out in
     20000/45000ms` or (`startBridgeHeadlessCli`) an empty-subprocess-
     output symptom consistent with CPU starvation, zero diff touching
     any of them or `extension/src/`. Re-run in isolation: 5 of 6 passed
     outright; `dependencyGateCliStorageGlobals` still timed out
     alongside 6 concurrent files, then passed solo (16.39s against a
     20000ms budget — a tight margin, not a defect). All 6 ruled
     load-induced (host load measured 30.79–67+ during this session's
     runs), consistent with the identical pattern independently confirmed
     during the BL-631 pass earlier today.
9. **Leak/process check**: `pgrep -fl 'node --test|stryker'` clean before
   and after (one live `node --test` seen mid-pass belonged to the
   architect worktree's own active BL-943 run, PPID traced to a live
   `specs/pipeline/cli.js` process, not orphaned — left untouched);
   private test tmux sockets killed after use; `git status --short`
   clean pre-bounce.

## Failing command (D1)
```
cd extension && npx vitest run test/tmpDirMigrationGuard.test.js
```

## Commit hash
`57bf1d6f28e1dc942325a1343ab566cdde251dc9`

## First error excerpt
```
FAIL  test/tmpDirMigrationGuard.test.js > the real extension/test/ tree has zero raw mkdtemp call sites outside the shared helper
AssertionError: expected zero raw mkdtemp call sites, found:
/Users/ldecorps/projects/swarmforgevc/.worktrees/QA/extension/test/constitutionDocCitations.test.js:41
/Users/ldecorps/projects/swarmforgevc/.worktrees/QA/extension/test/constitutionDocCitations.test.js:62
/Users/ldecorps/projects/swarmforgevc/.worktrees/QA/extension/test/constitutionDocCitationsInvariant.property.test.js:69
/Users/ldecorps/projects/swarmforgevc/.worktrees/QA/extension/test/constitutionDocCitationsInvariant.property.test.js:90
```

## Failure class
`unit` (a standing regression guard fails against BL-945's own two new
test files).

## Expected vs observed
Expected: zero raw `mkdtemp` call sites under `extension/test/` outside
the shared helper (`tmpDirMigrationGuard.test.js`'s standing assertion).
Observed: 4 raw `fs.mkdtempSync` call sites across BL-945's two new test
files, bypassing the shared `mkTmpDir()` helper's single sweep point.

## Remediation pointer
- `extension/test/constitutionDocCitations.test.js:41,62` — replace
  `fs.mkdtempSync(path.join(os.tmpdir(), 'sfvc-bl945-'))` with
  `mkTmpDir('sfvc-bl945-')` from `./helpers/tmpDir`; the hand-rolled
  `try { ... } finally { fs.rmSync(dir, ...) }` blocks can be dropped
  once the shared helper's `afterEach` sweep (wired via
  `tmpDirSetup.js`/`vitest.config.mjs`) owns cleanup, matching every
  other file's usage.
- `extension/test/constitutionDocCitationsInvariant.property.test.js:69,90`
  — same fix.
Owning role: **coder** (introduced in `0bfd52db4` and `c6c7a443e`).
