# BL-612 QA bounce — 20260825

**Reviewed tip:** documenter `8634f6eb1f` (not merged — tip impure)
**Task:** `BL-612-claim-progress-acceptance-step-handlers`
**Sibling:** `VERIFY BL-612`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** — tip not landable; not merged |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 8634f6eb1f  # fails
comm -12 <(git ls-tree -r --name-only origin/main | sort) \
  <(git diff --diff-filter=D --name-only origin/main 8634f6eb1f | sort)
# → backlog/evidence/BL-615-qa-bounce-20260825.md
```

**Commit hash checked:** `8634f6eb1f`

**First error excerpt:**
```
main_NOT_ancestor (merge-base = 2177e4c7e, before BL-615 bounce land ead05c233)
paths=14 dels=1
deleted on tip vs present on origin/main:
backlog/evidence/BL-615-qa-bounce-20260825.md
```

**Failure class:** `behavior`

**Expected vs observed:** Expected tip-pure documenter tip with `origin/main`
(`ead05c233a`) as ancestor and **0** deletes of files on main. Observed tip
that drops the just-landed BL-615 QA bounce evidence (despite commit message
claiming pre-QA ancestry cleared).

**Remediation:** Rematch onto current `origin/main`; keep
`backlog/evidence/BL-615-qa-bounce-20260825.md`. Authorize BL-612 paths only;
`dels_on_origin` must be 0.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
