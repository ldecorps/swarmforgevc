# BL-1366 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked (extra care given this ticket is the one that pushes)

- `test_bl1366_land_is_one_command.sh` — re-ran: 21/21 PASS, including
  every safety-critical row:
  - "the push used no force"
  - "a rejected push triggers exactly one rematch onto the current tip"
    / "and only one - never a second rematch"
  - "an escalation leaves origin/main byte-identical" / "and the lock is
    not left held by an escalation"
  - "a held lock is waited on to a deadline, then given up" / "and the
    wait is bounded (4s), never an unbounded spin"
  - "a land killed mid-sequence leaves no lock behind (trap on every exit
    path)"
  - "the live repository's origin URL is byte-identical after the suite"
    / "no live remote points into the fixture directory" — the exact
    class of check this session's BL-1390 incident established as
    mandatory for any fixture that shells real git.
- Read `run_land` end to end against all three declared invariants:
  - Invariant 1 (never force, at most one rematch): `land_push_ff_only`
    is a plain `git push origin "$sha:refs/heads/main"` — no `-f`/`--force`
    anywhere in the file (grepped). The rematch path runs exactly once;
    a second rejection returns `LAND_STOPPED` rather than looping.
  - Invariant 2 (lock released on every exit path): `land_release_trap`
    is registered on `EXIT INT TERM` before the lock is ever acquired,
    and only releases if `LAND_LOCK_HELD=1` — so a failed
    `land_acquire_with_deadline` (which returns before setting the flag)
    correctly does not double-release someone else's lock.
  - Invariant 3 (LAND_ESCALATE never auto-resolved): the entanglement
    verdict (`land_step_cli.bb`) runs BEFORE the lock is ever taken, and
    an escalation returns immediately with `LAND_STOPPED` — the lock is
    never acquired for an escalating tip at all, not merely released
    after.
- Coder's own self-caught false positive (evidence file): an earlier
  version of the fixture compared this repository's own main SHA before
  and after the suite, which false-positives whenever another role lands
  concurrently (a live, active swarm). Fixed to assert the origin URL and
  that no live remote points into the fixture instead — read the diff
  directly, confirmed the fix is what's in the merged commit.
- `jscpd` over the new step handler and `land_main_publish.sh`: 0 clones.
- `mutation-site-count.js` on the step handler: 161 sites (`over` 100).
  Reviewed and declined to split, same reasoning as every other ticket
  this session: one cohesive single-feature handler.
- TypeScript compiles clean; the handler discovers via BL-1371's
  registry.

## Observation for documenter (not a defect, not blocking)

`--land` is a fully functional, directly-invocable new mode on
`land_main_publish.sh` — unlike the dead-code-with-zero-callers class of
defect this session bounced BL-1379/1386/1387 for, a human (QA) can run
it right now by name. What's still true: `swarmforge/roles/QA.prompt`
line 121 still only documents `--decide-only` and the old hand-run
sequence; nothing yet tells QA to prefer `--land` over hand-orchestrating.
Same shape as BL-1362's evidence-writer tool (also not yet referenced
from any role prompt, also not bounced for it) — recording this for
documenter's normal pass in this same parcel's pipeline, not raising it
as a defect myself.

No bounce history on BL-1366 (first pass). Forwarding unchanged to
architect.

By cleaner.
