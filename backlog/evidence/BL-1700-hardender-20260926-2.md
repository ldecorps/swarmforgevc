# BL-1700 — hardender review pass, 2026-09-26 (post-bounce rework)

NONE. The full checklist was run and found no defect. A test-coverage gap
found during the pass was closed in this same commit, not bounced — the
hardener's own job, never a send-back for something the pass can fix
itself.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

This is the post-QA-bounce rework of BL-1700 (cleaner 20260926-2, architect
20260926-2, both NONE). Reviewed the coder's bounce fix (2f35451608) and
its new dedicated test file `model_steward_coder_probe_lib_test_runner.bb`
against the QA bounce evidence (D1/D2/D3, `BL-1700-QA-20260926.md`).

**Gap found and closed (BL-755, a multi-branch parser needs one distinct
test per branch)**: `resolve-model-id` has four branches (nil ids / exact
match / `:latest` match / else fallback), but the bounce's own new D3 test
exercised only the nil-ids fallback (an unreachable endpoint). Added a
real local `/v1/models` HTTP fixture (Python `http.server`, the same curl
call the function makes, never a mock of it) exercising the exact-match
and `:latest`-tag branches, plus the else-fallback when neither form is
listed. Verified each new assertion actually discriminates: hand-mutated
the exact-match arm (`requested-model` → `"MUTATED"`) and the `:latest`
arm (`(str requested-model ":latest")` → `"MUTATED-LATEST"`), confirmed
both go red, then restored the file (`git diff` clean before commit).
Committed as d11ca45f14.

**Re-mutated with `full`, not `hard`**: this project's `implementation_hash`
is always `"unknown"` (no `--implementation-hash`/`--generated-dir` passed
by `run_gherkin_mutation.sh`), so `hard` silently reuses the pre-bounce
stamp regardless of the coder's rewrite. Ran
`specs/pipeline/scripts/run_gherkin_mutation.sh <feature> <workdir>
specs/pipeline/steps/index.js full`: 6/6 killed, 0 survived, 0 errors on
the current implementation; manifest `tested_at` refreshed, committed with
the test change.

**Investigated and ruled out a concurrency false lead**: mid-pass, found a
leaked tmux + stand-in.sh process outside any test run. Traced it, in
order: (1) a first "leaked process" acceptance-suite failure was a pure
self-match artifact — my own diagnostic `ps aux | grep -E
'tmux -S /tmp/bl1700|stand-in.sh'` commands were themselves visible in
`ps aux` at the moment the suite's system-wide leak check ran concurrently,
and their own command text contains those literal substrings (mirrors the
documented pgrep-self-match hazard). A clean, fully isolated re-run of the
acceptance suite (no concurrent diagnostics) passed 7/7 with zero leaked
processes. (2) The one genuine leaked process found afterward traced to my
own two overlapping ad hoc Gherkin-mutation invocations: the second's
`rm -rf` of the shared `tmp/bl1700-mutation-work` directory raced the
first invocation's still-running background mutation workers, denying one
in-flight stand-in run its own working directory mid-cleanup. Self-
inflicted test-methodology noise, not a defect in `run-fixture!`'s cleanup
- confirmed by 5 sequential solo runs (solve and hang modes) and 5
concurrent solo runs all cleaning up correctly with no interference from
my own overlapping mutation-harness invocations. Reaped the stray process
and its throwaway root; not committed (scratch, `/tmp`).

Checks run:
- `bb swarmforge/scripts/test/model_steward_coder_probe_lib_test_runner.bb`:
  ALL PASS (11 assertions, including the 3 new D3 branch tests).
- `bb swarmforge/scripts/test/model_steward_test_runner.bb`: ALL PASS.
- `node specs/pipeline/cli.js <feature>`: 7/7, isolated, no leaked process.
- `specs/pipeline/scripts/run_gherkin_mutation.sh ... full`: 6/6 killed, 0
  survived, 0 errors; manifest committed.
- `cd extension && npm test`: 638 files / 10875 tests, exit 0. Pre-existing
  registered-pole duration warnings (`renderBriefingBurndownCli.test.js`,
  `briefingDigestLineCli.test.js`, etc.) are unrelated to this ticket's
  scope (no TS/JS production file in BL-1700 beyond the untouched step
  handler) and were not introduced by this pass.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): none after all
  runs. Manual `ps aux` sweep for `tmux -S /tmp/bl1700-probe` /
  `stand-in.sh`: clean after cleanup.
- `git status --short`: clean before commit, beyond the two files this
  pass changed (test runner, mutation-stamp manifest).

CRAP/DRY not run - no TS/JS production file in this ticket's own scope
beyond the step handler (unchanged by the bounce fix), matching the
pre-bounce hardener pass's own disposition.

By hardender.
