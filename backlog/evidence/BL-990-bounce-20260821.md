# BL-990 QA bounce, 2026-08-21

**Reviewer**: QA.
**Reviewed at**: documenter tip `411ffb2f1f` (Document BL-990: bounce
attribution is now correctable, append-only), merged into QA at `16c0ed9ca`.

## BL-990's own work: fully verified, clean (Article 4.4 complete inventory)

- **Ancestry**: this ticket's own coder (`687a57ca9`), cleaner (`b983acaa6b`),
  architect (`1c4093bd3`), hardener (`4589673a0`) and documenter (`411ffb2f1f`)
  merges are all confirmed ancestors of the QA tip `16c0ed9ca` via
  `git merge-base --is-ancestor`.
- **npm run compile**: clean.
- **BL-990 acceptance** (`node specs/pipeline/cli.js
  specs/features/BL-990-bounce-attribution-correctable.feature`): **8/8 PASS**,
  exit 0.
- **qa_e2e_procedure step 4** (replay the real BL-971 record, left for QA
  because it needs the live `.swarmforge/bounces/` store): executed for real.
  `node extension/out/tools/record-bounce-correction.js --ticket BL-971
  --commit 8956d30eee --reason "..." --by QA --evidence
  backlog/evidence/BL-971-amendment-supersession-20260820.md` — recorded
  `true`. Verified: the original line
  (`{"ticket":"BL-971",...,"commit":"8956d30eee",...}`) is still present
  byte-for-byte in `.swarmforge/bounces/2026-08.jsonl`; the correction line
  is appended after it. Re-running the identical correction returns
  `recorded: false` (idempotent, line count unchanged at 2). Live
  `qa-bounce-line.js` output confirms the merged store reads through the
  corrected view.
- **Design/wiring review**: `applyBounceCorrections` is the single chokepoint
  (`bounceStore.readBounceRecords`); all three independent consumer paths
  named in the ticket (`bounceStore.readBounceRecords` —
  `qa-bounce-line.js`/`costHealthSidecar.ts`/`reworkRounds`,
  `failureModeInventory.recordsFromQaBounceJsonl`,
  `leanLedgerComposeBounce.composeBounceEvents`) resolve corrections —
  confirmed by direct grep of each module, not assumed. Append dedup
  (`appendBounceRecordIfNew`) correctly reads the RAW (uncorrected) view so a
  corrected bounce cannot be silently resurrected by a re-run. `--role` was
  not widened; the excluded-from-attribution design sidesteps the enum per
  the ticket's own `approval_context`. Real live caller confirmed by grep
  (`qa-bounce-line.ts` — the CLI feeding the daily briefing's bounce-rate
  figure the ticket's `description` names as the live-contaminated
  consumer) — not merely unit-tested in isolation.
- **Property lane** (`bl990BounceCorrectionInvariants.property.test.js`):
  pass, both declared invariants, reachability floors cleared
  (realTarget >= 60, absentTarget >= 60, noCorrections >= 20 all met per the
  test's own asserted floors).
- **Unit** (`bl990BounceCorrection.test.js`, `bl990BounceCorrectionStore.test.js`,
  `recordBounceCorrectionCli.test.js`): all pass as part of the full suite
  run below.

**BL-990 itself has NO defect.** Every gate it owns is green.

## D1 (behavior, blamed role: architect) — parcel's tree fails `conciergeTick.test.js` (2/111), same BL-979 contamination already bounced for BL-986 today, already fixed upstream

`npm test` (full unit suite, `456 passed | 2 failed` files, `8076 passed | 3
failed` tests) at this parcel's commit (`16c0ed9ca`):

```
FAIL  test/conciergeTick.test.js > BL-455: role-held tickets are joined
  to their backlog item epic/title...
FAIL  test/conciergeTick.test.js > BL-473: a ticket physically in
  backlog/active/ that no role holds still renders, in the not-started
  state
```

These are the **exact same two failures**, byte-identical assertion text,
already found by the architect for BL-979 and re-diagnosed by QA today in
`backlog/evidence/BL-986-bounce-20260821.md`: BL-979 pivoted
`pipelineBoard.ts` (tickets become rows, header carries stage glyphs) but
`conciergeTick.test.js`'s pre-pivot header-carries-ticket-id assertions were
never re-expressed, and the architect's bounce of BL-979
(`c183c0b7a`, `backlog/evidence/BL-979-architect-bounce-20260821.md`) rode
forward on the architect worktree branch without the constitutionally-owed
revert (BL-490/BL-495), contaminating every ticket that passed through
architect afterward — BL-986 first, and independently, BL-990.

**Scope check**: `git diff --stat 30998e3df..16c0ed9ca` (BL-990's own commit
range) touches exactly the 10 files listed in the ticket's own domain
(`bounceStore.ts`, `failureModeInventory.ts`, `leanLedgerComposeBounce.ts`,
`qaBounce.ts`, `record-bounce-correction.ts`, `recordBounceCorrectionArgs.ts`,
and their 4 test files). Zero overlap with `pipelineBoard.ts` or
`conciergeTick.test.js`. **Verified not a BL-990 defect.**

**The fix already exists**: unlike when BL-986 was bounced, the architect has
since cleared this exact contamination on their own worktree branch —
`bfc398249` (revert BL-979's un-reverted pivot) →
`3d3506d6a` ("BL-986: branch contamination cleared, ready for QA re-review")
→ `7ee9b4d40` (merges cleaner's BL-979 D1 refix, `e8ad302017`) →
`9498e524c` (re-applies the pivot now that the refix makes it safe;
current architect HEAD). Confirmed:
`git merge-base --is-ancestor 1c4093bd3 9498e524c` → yes (BL-990's own
architect-stage commit is an ancestor of the fixed tip) — the fix branch
already contains BL-990's own architect work, so bringing it forward should
be a straightforward merge, not new review work.

**Isolation re-check (host-load discipline)**: `renderBriefingDiagramsCli.test.js`
also failed in the full-suite run (`Test timed out in 45000ms`) alongside 458
benign `[vitest-worker]: Timeout calling "onTaskUpdate"` artifacts (the known
BL-871 pattern, exact message match, allowlisted). Re-ran that one file in
isolation: `npx vitest run test/renderBriefingDiagramsCli.test.js` → **4/4
PASS in 28s** (vs the 45s timeout that tripped under whole-suite contention).
Confirmed load-flake, not a regression — not part of this inventory's D-items.

**Suite/per-file duration budget**: the run also reports "suite duration over
budget" and 33 per-file budget offenders. None of BL-990's own new test files
(`bl990Bounce*.test.js`, `recordBounceCorrectionCli.test.js`) are among the
offenders; all 33 are pre-existing files outside this parcel's diff
(`swarmLauncher.test.js`, `telegramFrontDeskBotCli.test.js`, etc.) and match
the sustained host load (20-46) the hardener independently recorded
throughout this same shift. Pre-existing infrastructure debt, not a BL-990
regression — not part of this inventory's D-items, noted for traceability
only.

**Remediation**: merge architect's current tip (`9498e524c`, which already
contains BL-990's own architect-stage work as an ancestor) forward through
this ticket's own hardener → documenter → QA chain, or equivalently revert
BL-979's still-outstanding contamination on whatever branch state the fix for
BL-990 continues from. Either way, BL-990's own ticket needs no code change —
do not route fix work to BL-990's coder.

## Outcome

Bounced to **architect** (earliest role able to clear the branch
contamination on this lineage; also the role the constitutional revert
obligation binds, and the role that already fixed the identical defect for
BL-986 today). Class: `behavior`. Task name carries BL-990's stable name plus
this defect summary.
