# QA pass — BL-1287 — 2026-09-05

Commit under review: `6f46ed5c9a` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`6db5211bf4` BL-1287
coder, confirmed ancestor via `git merge-base --is-ancestor`).

Safety-critical fix: a module-load fixture sweep in the property lane
could SIGKILL a concurrently-running sibling suite's LIVE fixture process.
Reviewed with corresponding care, including a bounce-and-redo cycle
(zombie-creator blind spot) already caught and fixed before reaching me.

## Verification

- Read `extension/test/helpers/fixtureTunnelName.js` in full:
  `leakedFixtureTunnelPids` keeps the temp-path/cloudflared/`run <name>`
  gate (invariant 3, unchanged) and adds a second filter — a fixture whose
  encoded creator pid (`sfvc-test-<pid>-...`) is still alive is never
  selected. The discriminator is genuinely creator-liveness, not a
  start-time cutoff, matching the ticket's explicit rejection of the
  cheaper age-based approach.
- `isProcessAlive` confirmed checking `ps -o stat=` for a leading `Z`
  (zombie) state, not a bare `process.kill(pid, 0)` — this is the
  architect-bounced fix (a SIGKILLed-but-unreaped creator was previously
  misread as alive, per the bounce evidence I read in
  `BL-1287-bounce-20260905.md`), same discriminator class as BL-1292's
  `isAlive` fix earlier this session.
- `npx vitest run test/helpers/fixtureTunnelName.test.js`: 7/7 pass.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1287FixtureSweepScopingInvariants.property.test.js`: 3/3 pass.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1287-a-fixture-sweep-must-not-signal-a-live-runs-fixtures.feature`
  — 4/4 scenarios pass, including "two suites sweeping in one lane each
  keep their own fixtures" (the exact concurrent-fork scenario this ticket
  exists for) and "an installed cloudflared outside the temp directory is
  never selected" (invariant 3 preserved).
- Regression: `bl857TunnelOwnershipInvariants` (4/4),
  `bl1061TunnelFixtureIsolation` (5/5), and `bl787NamedTunnelInvariants`
  (3/3, the exact sibling the ticket's own measurement named as the
  would-be victim) all still green.
- **Live production check**: the real `cloudflared ... run
  swarmforge-bubble` process (pid 29147, this host's actual operator
  tunnel) confirmed running with the SAME pid before and after the full
  property suite run — untouched by any sweep in this parcel's tests.
- `required_wiring`: `specs/pipeline/steps/bl1287FixtureSweepSparesLiveRunSteps.js`
  exports `registerSteps`.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Docs: `docs/reference/Specification.MD` updated, dated 2026-09-05.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Out-of-scope note (not this ticket's defect)

The merge also carries a `bounce_history` addition to the still-active,
separately-in-flight `BL-1429-standing-reds-throttle-intake.yaml` — ordinary
shared-branch ancestry (BL-1241's shape), not part of this `git_handoff`'s
named task. No action taken on it here.

## Verdict

APPROVED — merging up and landing.
