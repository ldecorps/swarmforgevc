# BL-1407 documenter review pass — 2026-09-05

Reviewed the hardened commit (`e398437712`) and the coder/architect trail for
BL-1407 (property-suite gate re-runs a non-allowlisted red once, alone,
before refusing).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1407-the-property-gate-reruns-a-red-in-isolation-before-it-refuses.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1407PropertyGateRerunsARedInIsolationSteps.js`.
- User-visible/operator-visible behavior change (`check_property_suite_drift.sh`
  now re-runs a non-allowlisted red alone before refusing, and records a
  cleared red as a load flake) is documented in the guard's own how-to,
  `docs/how-to/BL-570-property-suite-drift-guard.md`, per the ticket's own
  direction ("the guard's own how-to gains the flake semantics").
- `docs/index.md`'s one-line summary for that how-to updated to mention the
  new behavior.
- No "Last Updated" freshness field present in the edited doc — no date bump
  owed.
- No registered diagram (`docs/diagrams/{architecture,swarm-flow,handoff-flow,front-desk-flow}.mmd`)
  depicts this mechanism: the new `.swarmforge/property-flakes/` directory is
  a per-worktree, guard-local artifact store (like BL-1275's
  `property-guard-refusals/`, which is likewise absent from the diagram's
  shared `.swarmforge/` State subgraph) — not part of the shared/master
  `.swarmforge/` state layout that subgraph enumerates. No diagram change
  needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
