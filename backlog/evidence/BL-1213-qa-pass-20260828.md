# BL-1213 — QA pass — 20260828

**Received:** `merge_and_process documenter f2e6968a22` (handoff
`00_20260828T020107Z_000879_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `f2e6968a2222c4a4d1c9dce0f5eb2b6a3fdd6a58`
**QA merge commit:** `daef724b0b16a626cd08e0645c79c275d4a850aa`

Single pass, no prior bounce. This is the systemic gate ticket named as
owner in operator memory for the recurring BL-592/1188/1189 silent-revert
class (five prior occurrences this session, all hand-caught).

## Merge

Clean, no conflicts (`git merge --no-ff f2e6968a22`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor eb7ac64e8 HEAD` (coder) — CONFIRMED
- `git merge-base --is-ancestor 85e61c012 HEAD` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 61493f9df HEAD` (architect) — CONFIRMED
- `git merge-base --is-ancestor f2e6968a2 HEAD` (documenter, received tip) — CONFIRMED

## Wiring verification (required_wiring, both rows)

1. `swarm_handoff.bb` loads `parcel_rollback_guard_lib.bb` (line 20) and
   calls `findings-for-git-handoff`/`blocked?`/`refusal-message` at the
   send chokepoint (lines 373-410), gated on `(= "git_handoff" type)`,
   matching the exact precedent shape of the four existing send-time gates
   in the same file (duplicate-chain, task/commit coherence, ticket-close,
   pre-QA/pointer) and their fail-open-on-unreadable-facts posture (a
   `PARCEL_ROLLBACK WARNING` is printed to stderr, the send is never
   blocked on an unreadable parcel commit).
2. `specs/pipeline/steps/index.js:822` registers
   `bl1213ParcelRollbackGuardSteps` — confirmed present.

## Design review (independent, since architect's own pass was a bare
forward with no evidence file — noted, not bounced; see Disposition)

- Ticket-scoped: `changed-paths` uses `git diff-tree` on the parcel commit
  itself, bounding the check to only the paths that commit touched — not a
  full-tree walk (constraint honored).
- Never uses ancestry as the discriminator: `path-rolled-back?` compares
  blob shas only (`tip-blob`, `parent-blob`), matching the ticket's explicit
  "do NOT make ancestry a criterion" constraint and the BL-1211 lesson it
  cites.
- Does not touch or weaken BL-1098: `parcel_rollback_guard_lib.bb` is a
  wholly separate file from `push_sweep_lib.bb`; grepped for any edit to
  `silent-revert-path?` — none.
- BL-490/495 bounce-revert legality preserved:
  `revert-of-commit-on-branch?` greps for the standard `This reverts
  commit <sha>` trailer both `git revert` forms produce, `-F` fixed-string
  (never a regex compile of arbitrary commit text).
- 5th-occurrence requirement (compare EVERY parcel path, not only
  conflicted ones) is satisfied by construction: `changed-paths` is
  `diff-tree` over the whole parcel commit, not scoped to a merge's
  conflict list.

## Verification inventory

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1213` | `VERIFY BL-1213` — no open deferral |
| Compile | `npm run compile` | PASS, clean (re-run after this merge too) |
| `parcel_rollback_guard_lib_test_runner.bb` | `bb ...` | ALL PASS |
| `bl1213_parcel_rollback_guard_property_runner.bb` | `bb ...` | 2000 runs, ALL PROPERTIES HOLD; explicit reachability check (the refusing shape is generated at least once) + a real-git fixture proving a dirty uncommitted working-tree edit cannot flip the verdict (invariant 1) |
| Acceptance | `run_acceptance.sh specs/features/BL-1213-...feature` | 8/8 PASS — covers all 7 qa_e2e_procedure items 2-7 (Scenario Outline rows = items 2/3/4; "commit authoring the rollback" scenario = item 5, the exact BL-1098-reimplementation trap; "warns and sends" = item 6; "note is untouched" = item 7) plus a refusal-message-content scenario beyond the minimum |
| qa_e2e_procedure item 1 (pre-fix live reproduction) | ticket's own `notes:` section | Specifier's mint-time verification recorded in the ticket YAML itself: all seven paths confirmed byte-identical to `e5cf2a3af^` on all three branches by blob-sha comparison (braced refs), independent of the architect's method — already satisfies this item, branches were live-damaged at mint time and this is the record of that state |
| Full unit suite (before AND after this merge) | `npm run test` (extension/) | 206 failed / 8987 passed (9193), identical count before and after the BL-1213 merge — standing red (see BL-1198-qa-pass-20260828.md, same session), zero new failures introduced. BL-1213 touches no `extension/src` file. |
| BL-113 Gherkin mutation | manifest embedded in feature file | 12/12 mutants killed on the one Scenario Outline, 0 survived (hardener) |
| Docs | grep | `docs/how-to/BL-1213-parcel-rollback-guard.md` present, linked from `docs/index.md`; `swarmforge/handoff-protocol.md` gains a section alongside the other three send-time gates; `docs/reference/Specification.MD` top entry updated |
| Fixture hygiene | hardener's evidence + spot-check | `mkTmp` switched to `mkSocketFixtureRoot` after hardener found a 180+-straggler leak; confirmed `0` stragglers after a fresh acceptance run |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker'` | none, before and after |

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1213's own work.

**Observation, not a defect:** architect's own hop (`61493f9df2`) is a bare
merge-forward with no dedicated evidence file, unlike every other stage on
this ticket. Cleaner's evidence (`BL-1213-cleaner-pass-20260828.md`)
explicitly recorded "No architecture, DRY, or module-boundary issues found"
one hop earlier, and the ticket's own `approval_context` shows the two
genuinely open design questions (fail-open posture; BL-1211 vs. separate
ticket) were already settled by explicit human ruling before
implementation started, narrowing what remained for architect to
independently adjudicate. My own design review above (ticket-scoping,
non-ancestry discriminator, BL-1098 non-interference, bounce-revert
legality, 5th-occurrence full-path coverage) found the delivered design
sound. Not bounced — consistent with this session's identical call on
BL-1198's own architect round-2 pass.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1213 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1213 active → done).

By QA.
