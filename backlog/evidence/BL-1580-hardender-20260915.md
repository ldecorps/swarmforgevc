# BL-1580 — hardender review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- No production anchor (BL-1235); `extension/src` unchanged
  (`git diff main --name-only -- extension/src` empty) — no Stryker/CRAP/DRY
  gate applies, per ticket's `mutation_cost: low` and constraints.
- `bl1295RevertAttributionInvariants.property.test.js` alone, 5 consecutive
  runs (`npx vitest run --config vitest.properties.config.mjs
  test/bl1295RevertAttributionInvariants.property.test.js`): 5/5 green,
  `BL-1580 reach map (bl1295): {"arms":2,"minDrawsPerArm":3}` printed every
  run. The fix reaches both arms BY CONSTRUCTION (`fc.constant(foreignCommit)`
  inside an outer loop over `[false, true]`), so this is not a sampled floor
  any more — no simulation needed to trust repeat-green.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1580-*.feature`): 5/5 scenarios pass.
- Standing whole-tree guards (touched `specs/pipeline/steps/` and
  `extension/test/`): all 18 `test/*Guard*.test.js` files, 183/183 tests
  pass.
- `swarmforge/scripts/check_feature_handler_registration.sh`: exit 0.
- Break-then-restore, done independently of the coder's own record: edited
  `REVERT_ARMS` to `[false]` (dropped the `true` arm) — first run failed
  `reach floor: arm genuinelyForeign drawn 0 < 6` via `assertReachFloor`;
  restored from an untouched backup, re-ran green, `git status --short`
  clean.
- Register row for the file (`backlog/standing-reds.tsv` line 29) confirmed
  still present — correctly left for QA to remove per the ticket's and
  coder's note, not the hardener's or documenter's to touch.
- No orphaned test/mutation processes left behind (`pgrep -fl 'node
  --test|stryker|vitest'` clean pre- and post-pass).

By hardender.
