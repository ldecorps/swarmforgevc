# BL-1490-a — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Mutation tooling: `mutation_cost: low` per ticket (Babashka only, no
  Stryker/CRAP/DRY wired — BL-472). The coder's declared-invariant property
  runner (`bl1490_supervisor_tick_progress_property_runner.bb`) already
  proves non-vacuity at authoring time (its own header documents three
  breaks restored: `effective-in-sweep-budget-ms`'s tick-phase branch
  dropped, the over-budget heartbeat-void dropped, `mark-tick-phase!`
  no-op'd — each caught by the corresponding invariant). Re-ran green:
  `bb swarmforge/scripts/test/bl1490_supervisor_tick_progress_property_runner.bb`
  → `ALL PROPERTIES HOLD` (200 draws, coverage floors all met).
- Hand-probed the between-tick-and-sweep-budget equivalence directly
  (obs with `in-flight-sweep-name "delivery"`, age 100000ms — between
  `tick-budget-ms` 60000 and `in-sweep-budget-ms` 225000, stale outbox):
  confirmed `:stalled`, matching the tight tick-budget being enforced
  rather than the looser sweep budget the pre-fix code would have used.
- Acceptance: `node specs/pipeline/cli.js specs/features/BL-1490-a-poll-cycle-phase-is-never-invisible-to-the-supervisor.feature`
  → 5/5 green (fresh run, ~28s).
- Whole-tree standing guards (parcel touches `specs/pipeline/steps/`):
  `cd extension && npx vitest run $(ls test/*Guard*.test.js | grep -v '\.property\.')`
  → 18 files, 183 tests, all green (includes `tmuxReaperGuard`,
  `tmpDirMigrationGuard`, `bl968StepRegistryMaterializedTreeGuard`).
- Registration: `bl1490PollPhaseNeverInvisibleSteps.js` is picked up by
  the discovery-based `specs/pipeline/steps/index.js` (BL-1371, suffix
  `Steps.js`, non-recursive) — no explicit registration line needed;
  confirmed the file matches the discovery predicate.
- Fixture hygiene: both the property runner and
  `bl1490TickPhaseFixtureCli.bb` create their temp fixture root with a
  JVM shutdown hook (`fs/delete-tree` on any exit path) plus an explicit
  `fs/delete-tree` at the end of the happy path — no tmux server is
  started by either, so the tmux-reaper hazard class does not apply here.
  No leftover `bb`/fixture processes after the runs above
  (`pgrep -fl 'node --test|stryker'` clean, scoped to this worktree).
- CRAP/DRY: N/A per `mutation_cost: low` — Babashka has neither wired
  (BL-472); no `extension/src/**` or TS file touched.
- Load: `uptime` 1.64/1.77/1.88 on 20 cores throughout — no cooldown/busy
  gate applicable, no office-hours bypass needed.
- No orphaned test/mutation processes left running; nothing further to
  harden beyond confirming and re-running architect's and coder's own
  evidence.
