# BL-1437 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1437`: `VERIFY BL-1437` (no open
  deferral).
- Orphan process check before/after: empty both times.
- Merge lineage: the inbound documenter merge's base (`002def27c6`,
  BL-1424's now-fixed replay tip) is my prior HEAD; diffed the merge
  against both parents — parent1 gained exactly BL-1437's own files,
  parent2 gained the BL-1424 incident evidence commit my branch already
  had. No drop on either side.
- `bb swarmforge/scripts/test/bl1437_art_director_seat_certification_property_runner.bb`:
  ALL PASS (24 generated cases + 1 hardener addition).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1437-the-art-director-seat-has-a-certified-model.feature`
  — 5/5 scenario runs. Sibling feature BL-1418 stays green: 7/7.
- Wiring anchor 1: `swarmforge/model-steward/seed/models.seed.json` has an
  `"art-director"` role_matrix key with one entry
  (`anthropic/claude-sonnet-5`, score 1.0, evidence
  `art-director-scorecard:bl1419-briefing-email-claude-sonnet-5-20260906`).
- Wiring anchor 2: `bl1437ArtDirectorSeatIsCertifiedSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Evidence pointer resolves: `swarmforge/model-steward/scorecards/art-director-claude-sonnet-5-bl1419-20260906.json`
  carries exactly that `scorecard_id`; the certification report
  (`certification-reports/anthropic__claude-sonnet-5__art-director__20260906.json`)
  carries the same `scorecard_id`, `"role":"art-director"` (the coder's
  own real gap fix — a report with no role field, confirmed present here),
  and `"result":"certified"` with all six look-and-feel gates `pass`,
  faithfully transcribing the six findings in the real BL-1419 art-director
  review (`backlog/evidence/BL-1419-art-director-20260906.md`).
- **Live qa_e2e probes (items 2-4), against the real committed seed via an
  isolated `MODEL_STEWARD_STATE_DIR`/`MODEL_FACTORY_STATE_DIR` (never the
  swarm's own already-bootstrapped `.swarmforge/model-steward/` — that is
  stale runtime state from before this ticket and never auto-resyncs from
  a seed change, a deployment concern this ticket does not own, not a
  defect; confirmed by reading `model_steward_cli.bb`'s own state-dir
  resolution)**:
  - `role-matrix art-director` → `anthropic/claude-sonnet-5 1.0
    art-director-scorecard:bl1419-briefing-email-claude-sonnet-5-20260906`.
  - `show anthropic/claude-sonnet-5` → `"status":"certified"`.
  - `assign --mode quality` → JSON includes
    `"art-director":{...,"model":"claude-sonnet-5",...,"reason":"quality:
    top-ranked eligible candidate (score=1.0)"}`.
  - Pack staffing gate: first attempt used a malformed windows-file (raw
    `window ...` lines copied from `full-forge.conf` instead of the CLI's
    actual `role<TAB>seat_stage<TAB>agent<TAB>extra-cli` shape) and
    produced a misleading "both pass" — traced to `swarmforge.sh`'s own
    `pack_staffing_gate()` field construction (line ~563) and corrected.
    Re-run with the properly-built two-line file (art-director and
    specifier's real `full-forge.conf` window lines, no `--override`):
    `specifier` → `refuse ... seat-model-unresolved` (its own separate,
    still-open gap, untouched by this ticket); `art-director` refused too
    on first correct run, but for `role-gate-not-pass` — BL-1079's
    SEPARATE compliance-battery gate, out of scope here per the ticket's
    own text — seeding a synthetic `anthropic__claude-sonnet-5.json`
    scorecard with an `art-director-gate: pass` entry (matching the
    coder's own stated test setup) then reproduced the ticket's exact
    claim: `art-director` → `pass`, `specifier` → `refuse
    seat-model-unresolved` only.
- Standing red the coder flagged (`test_pack_staffing_gate_wiring.sh` case
  01, unrelated to this parcel): confirmed already registered as `BL-1445`
  in `backlog/standing-reds.tsv`, an open ticket
  (`backlog/paused/BL-1445-...yaml`) — same red BL-1424's own coder
  independently found and deferred to earlier this session.
- Invariants: confirmed via the property runner's declared non-vacuity
  (evidence-pointer tamper failed 24/24; a cross-role leak into every
  role_matrix key failed 168/168; a steward write under
  `MODEL_FACTORY_STATE_DIR` failed 24/24) plus direct read of
  `seat-staffing-decision`/`ranked-on-role-matrix?` — nothing in the
  factory or gate's eligibility logic changed, only a new row was added.
- Full unit lane (`npm test`, extension/): 610/610 files, 10283/10283
  tests green (49.3s, mild host load, no new failures).
- Full property lane (`npm run test:properties`): 364/364 files,
  1087/1087 tests green, exit 0. Two `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled errors — both the BL-871 allowlisted benign
  case. No repeat of the bl1218 flake seen on the two prior parcels this
  shift.
- Docs: `docs/how-to/BL-1318-pack-staffing-gate.md` gained the "Art-director
  seat certified" section; `docs/reference/model-compatibility.md`
  (steward-generated `compat-docs`) lists `art-director` in the role
  recommendation matrix. No diagram trigger fires (not claimed; confirmed
  no diagram touched).
- Out of scope respected: specifier's claude-fable-5-1 gap untouched
  (confirmed still refuses, live); the other seven roles' role_matrix
  rows, capability entries and adapters byte-identical in the diff; the
  live evaluate run's capability-registry overwrite deliberately NOT
  folded into the seed (would have corrupted claude-sonnet-5's unrelated
  coding-competency capability profile) — confirmed the seed's
  `capabilities."anthropic/claude-sonnet-5"` section is untouched by this
  parcel's diff.
