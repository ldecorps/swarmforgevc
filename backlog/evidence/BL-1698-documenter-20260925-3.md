# BL-1698 — documenter review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

Third documenter pass. The received merge (c6bcbf915c, shared lineage
with BL-1704 this batch) had reverted my own pass-1 doc fix (bfddb4aad7)
as collateral of the QA-bounce-revert restoration sweep — flagged by
hardender's own pass-3 evidence (`BL-1698-hardender-20260925-3.md`)
rather than missed silently. Re-applied all three corrections, updated
further for pass 3's own code change (the once-per-process
`chat-set-up!` marker was removed entirely; it now runs unconditionally
before every fix request, in `run-gate!`'s retry branch and
`resume-from-hold!` alike):
1. "Releasing an escalated hold" (`BL-1697-local-parcel-driver.md`):
   ticket-hold-only answer resolution, chat set up unconditionally,
   `fixTurnsUsed = fixTurnsLimit`; a mail hold's answer stays unconsumed.
2. "Mail that needs no model turn": a mail hold carries no gate context,
   so an answer never resolves it — `release-hold!` only.
3. The babysitterd bullet: `driver-seat?` now reads
   `parcel-driver-capable?` directly, not "unchanged by this ticket".
Same three fixes mirrored in `handoff-flow.mmd` (top comment, DHOLDCHECK/
DRESUME/DRELEASE node text, DMERGECONFLICT/DMAILCONFLICT → DRELEASE-only
edges), plus DFIXTYPE updated for the unconditional chat re-set-up.

Checks: BL-1698 feature 9/9 (`specs/pipeline/cli.js`);
`bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js` 7/7 (`npx
vitest`); `npm run compile` clean.

By documenter.
