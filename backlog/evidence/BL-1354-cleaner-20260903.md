# BL-1354 — cleaner pass (2026-09-03)

## Scope

Diff touches only what the ticket declares: `swarmforge/scripts/land_step_lib.bb`
(new `blob-lines`, `diff-line-changes`, `merge-commit?`, `commit-line-changes`,
`sibling-own-line-changes`, `sibling-path-verdict`; `landed-siblings` reworked
to score per-sibling own lines instead of a shared path's whole blob), its
acceptance driver `specs/pipeline/steps/lib/bl1354SharedPathClassifyCli.bb`,
step handler `specs/pipeline/steps/bl1354SharedPathLandedSiblingSteps.js`,
property test `extension/test/bl1354SharedPathLandedSiblingInvariants.property.test.js`,
and the `.bb` unit-test runner. Includes the coder's own self-audit follow-up
(`8e2cdda6ea`): fixture prefix-sweep before the run (not only in `finally`,
BL-971) and a stale comment rename (`sibling-lines-landed?` → the actual
function name `sibling-path-verdict`).

Also merged in this pass (non-forwarding reverse hops, no cleanup action
needed — recorded, not reviewed as cleaner work): the BL-1296 revert
(architect bounce — seat never wired to a real topic id/turn function) and
the BL-1337 re-review (D1 verified fixed). And a `main` sync merge to pick up
BL-1354's own paused→active promotion, which the coder's branch had not yet
merged.

## Invariants checked against the diff

- **Invariant 1** (landed stays a positive finding, never inferred from
  silence): `sibling-path-verdict` returns `:unlanded` whenever `changes`,
  `main-lines`, or `tip-lines` is falsy — an unread diff, an unread blob, and
  a walk-failed/empty-path/unreadable-diff row all still fail closed.
  `sibling-own-line-changes` returns `nil` (never `{}`) when any candidate
  commit's diff could not be read, and that `nil` is scored `:unlanded` in
  `landed-siblings`, not silently skipped.
- **Invariant 2** (a sibling is judged on its own attributed content only):
  `sibling-own-line-changes` filters candidates to the sibling's own commits
  before diffing; `sibling-path-verdict` compares only that sibling's
  surviving added/removed lines against the tip, never the path's whole blob.
  Merge commits are explicitly excluded from a sibling's own line changes
  (their first-parent diff carries the second parent's content, not the
  merging ticket's), which is the mechanism that stopped BL-1341-class
  misattribution.
- `:vacuous` (a path with nothing left for this sibling to land) is dropped
  from the attributed set before scoring, so it can't be misread as either a
  landed or unlanded obstacle — matches the "silence is never scored as
  evidence" contract in the docstring.

## Verification (run, not assumed)

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — `ALL PASS`.
- `npx vitest run --config vitest.properties.config.mjs
  bl1354SharedPathLandedSiblingInvariants` — 2/2 pass (invariant 1 and 2).
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1354-a-shared-path-does-not-hide-a-landed-sibling.feature`
  — 5/5 pass.
- `required_wiring` confirmed:
  `bl1354SharedPathLandedSiblingSteps` registered in
  `specs/pipeline/steps/index.js:951`.

## Tooling note (Babashka/Clojure)

Per Startup Tools, Babashka has no mutation/CRAP/DRY tooling wired (BL-472
deferred) — this pass is gated by the `.bb` unit-test suite only, which is
green. No compiled production JS (`out/**/*.js`) was touched by this ticket,
so the mutation-site-count gate (BL-485) does not apply to this parcel; the
only JS files added are a step handler and a property test, neither of which
is a Stryker mutation target.

## Structure

`sibling-path-verdict` is pure over injected facts (`changes`/`main-lines`/
`tip-lines`), consistent with the file's existing style
(`sibling-landed?`). `landed-siblings` gained a `lines-fn` seam the same way
its existing `paths-fn` seam works, so the walk-failed/empty-path rows stay
drivable without a repository. No structural concerns — no unrelated
responsibilities mixed in, no leaked implementation details.

## Verdict

No defect found. Forwarding.
