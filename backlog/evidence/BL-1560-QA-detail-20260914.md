# BL-1560 — QA review pass detail, 2026-09-14

Reviewed against commit 2808e039ec (documenter's evidence commit),
merged into this worktree (85eb2826bf).

- `qa-sibling-check.js status --ticket BL-1560` -> `VERIFY BL-1560` (no
  open deferral).
- No orphaned test/mutation processes before or after (`pgrep -fl
  'node --test|stryker|vitest'` clean each time).
- Full unit suite (`npm test`, extension/): 616 files / 10479 tests, all
  green.
- Property suite (`npm run test:properties`):
  - First full run (contended — ran concurrently with the acceptance/bb
    checks below, my own mistake): 3 files red
    (`bl1272LandedSiblingInvariants`, `bl1297MergeOwnPathsInvariants`,
    `bl1343ReplayNeverDropsOwnPathInvariants`), all 20000ms timeouts.
  - Re-ran in true isolation (nothing else running): only
    `bl1297MergeOwnPathsInvariants` red (still a 20000ms timeout);
    `bl1272LandedSiblingInvariants` (registered `BL-1556`) and
    `bl1343ReplayNeverDropsOwnPathInvariants` both green this time —
    confirms those two were host-contention artefacts of my own
    concurrent-run mistake, not real.
  - `bl1297MergeOwnPathsInvariants.property.test.js` grepped against
    `backlog/standing-reds.tsv` and `backlog/paused|active`: no ticket,
    no row. Re-ran the file alone (nothing else running): 3/3 green,
    18.9s. Matches the documented pattern from BL-1502/1527/1500/1489/1543
    QA passes (2026-09-12/13): this file times out only under host
    contention and is clean in isolation, with no open ticket across
    repeated specifier/QA checks. Unrelated to this parcel's diff (BL-1560
    touches only `specs/pipeline/steps/*` — nothing under
    `land_step_lib.bb`/`own-paths`). Not a blocker.
- Acceptance: `run_acceptance.sh` on
  `specs/features/BL-1560-swarm-stamp-missing-script-path-1fc9065605.feature`
  -> 16/16 green (scenario 01 = 6 rows, scenario 02 = 5 rows, scenarios
  03-07 = one each).
- `bb swarmforge/scripts/test/tool_miss_heal_lib_test_runner.bb` -> ALL
  TESTS PASS. `bash swarmforge/scripts/test/test_tool_miss_heal_hook_wiring.sh`
  -> ALL SCENARIOS PASS.
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1560 HEAD .` -> OK
  (`required_wiring` satisfied: `registerSteps` is present in
  `bl1560SwarmStampMissingScriptPathSteps.js` and `steps/index.js`
  discovers `*Steps.js` by directory scan — no separate registration
  edit needed, confirmed by reading the scan code directly).
- Invariant 1 (`git diff main...HEAD -- swarmforge/scripts/swarmforge.sh
  swarmforge/scripts/tool_miss_heal_lib.bb
  swarmforge/scripts/tool_miss_heal_hook.bb`): empty. `git diff
  main...HEAD --name-only` touches only evidence, the feature, the step
  handler + its lib/ CLI, and the ticket yaml.
- `backlog/hotfix-ledger.yaml` row `1fc9065605`: `state: stamp-open`,
  `human_decision: null`, `decided_at: null` — matches scenario 07 and
  the qa_e2e_procedure's expectation; the parcel writes no certify/waive.
- Live check (shared root, `.swarmforge/launch/*.sh`): all 9 standard
  role launch scripts (QA, architect, art-director, cleaner, coder,
  coordinator, documenter, hardender, specifier) carry `NOT at the
  worktree root`. Three non-standard, hand-crafted one-off utility
  scripts (`specifier-drain-once.sh`, `coder-once-41mini.sh`,
  `specifier-then-coder.sh`) do not — these are outside the standard
  `write_role_launch_script` five-flavor render this ticket's scope
  covers (scenario 06's own census pin is "exactly five bodies plus one
  resume note", matching only the standard render path); not a defect
  of this parcel.
- Goals 7/8/9 (probes/notes, owned by BL-1561/BL-1562): correctly left
  unfixed by every stage, per the ticket's FIRM constraint. Coder's
  evidence records goal 7 reproduced (general defect confirmed, exact
  symptom diverges from the mint-time narration — recorded, no new note
  needed since BL-1561 already owns the class), goal 8's count (7
  occurrences / 4 files, one more file than mint-time named), and goal 9's
  worktree census (all six role branches + main now carry the hotfix,
  closing the cleaner gap the mint-time note flagged).
- Commit lineage: this ticket's hardener merge (450cb383f1) is an
  ancestor of the commit approved here (85eb2826bf); confirmed via
  `git merge-base --is-ancestor`.
- Article 1.10 / art-director sign-off: not applicable — this parcel
  produces no human-facing artifact (review-only, no UI/doc-surface
  change beyond evidence and a step handler).

Approving 85eb2826bf.

By QA.
