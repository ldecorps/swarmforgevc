# BL-1226 — hardender review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Ran the full checklist: BL-1226's acceptance feature (14/14 scenario
runs, after `npm run compile` - the merge brought TS source changes and
the stale `out/` initially false-failed 12/14, BL-497's known trap), the
coder's unit tests (21/21) and property tests (3/3), the extension/test
lane's own regression guard (`tmpDirMigrationGuard.test.js`, 12/12
unchanged), all 18 whole-tree standing guards (183 tests), and the full
unit suite with coverage (610 files, 10309 tests - one pre-existing
`bridgeServer.test.js` SSE-keepalive flake, reproduced clean alone,
confirmed unrelated to this parcel and not in `backlog/standing-reds.tsv`,
matching the coder's own documented finding exactly).

Confirmed via `task_scope_gate_cli.bb`: BL-1348's WIP checkpoint riding
in this branch's ancestry (its own evidence/feature/prompt files present
in the merge, `human_approval: pending`) does not entangle BL-1226's own
scope - the coder's own commit (a172b28cfa) touches only BL-1226's files,
explicitly says so in its own message, and the gate returns OK.

CRAP: `assessPilotMkdtempConvention` shipped at complexity 7 (CRAP
7.00), over the <= 6 threshold - and a real regression against main's
own baseline (complexity 5, itself pre-existing debt this parcel is not
on the hook for, but the RISE to 7 is this parcel's own doing per the
differential-complexity rule). Fixed by extracting two pieces into their
own functions (`detectorForLane`, the per-lane lazy-load resolution, and
`violationsInFile`, scanning one in-scope file) - the same
split-for-independent-CRAP pattern this file's own `inScopePath` already
uses. `assessPilotMkdtempConvention` dropped to complexity 3 (CRAP 3.00);
`detectorForLane` sits at complexity 4 (CRAP 4.00) on its own. Confirmed
load-bearing, not merely permitted, by hand-mutating `detectorForLane` to
always resolve the test-lane detector (dropping the steps-lane branch
entirely): 6/21 unit tests and 6/14 acceptance scenario runs fail.
Mutant reverted before committing the fix (7df2334c16).

By hardender.
