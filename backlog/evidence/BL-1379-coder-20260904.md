# BL-1379 — CODER PASS, 2026-09-04

## What was wrong

`park-plan` moved every ticket in `backlog/active/` except the run ticket into
`backlog/hold/` at initiation, and nothing ever moved them back. Article 3.1
makes `hold/` **human-held** — "never auto-promote from here; they sit until a
human moves them back" — so a mechanical, temporary park landed in the one
folder whose entire contract is that only a human empties it, and became
indistinguishable from a deliberate hold the moment the run ended. Five tickets
sat there from 12:02 on 2026-09-04 with nothing to notice.

## Built to the RULING, not to the acceptance — and why

`human_ruling` is **option 3**: restore to the prior folder, and mark each
restored ticket as needing a freshness check before it may be worked. The
feature file was written for option 1 (`restore to paused/`) and its scenario
02 still says so. The ticket anticipates this — "options 2 and 3 amend that
scenario alone" — and the amendment has not happened.

So scenario 02 FAILS on this branch, deliberately. Building to the stale
scenario would repeat BL-1309 exactly, where a parcel was built against a
ruling that had already been superseded. Raised to the specifier by priority-00
note; not resolved here, because rewriting the acceptance to match my code is
the wrong direction of fit.

**Acceptance: 7/8, the one failure being that mismatch.**

## Invariant 1 is structural, not checked

`unpark-plan` iterates the run's OWN park record and nothing else, so a ticket
a human put in `hold/` is not skipped — it is never considered. A reversal
driven by "everything in hold/" is the one implementation that breaks Article
3.1, and a unit row asserts the plan only ever ASKS about tickets it parked
(the `seen` atom), which is the row that would catch that rewrite.

The fixture carries a human-held `BL-9003` in EVERY shape, not only in the
scenario named for it. Measured across all six: it stays in `hold/` every time
and is named in no report.

## The record is recorded, not assumed

A `park-record.json` already existed but carried only ticket IDs — the origin
folder was implicit in "park-plan only ever parks out of active/". It now
carries each ticket's origin explicitly, alongside the existing keys so
anything reading `:tickets` is unchanged. That implicit uniformity is precisely
what the ticket names as the thing that would rot.

## Invariant 2, against a real ancestry check

Nothing is restored until the expedition's commit is an ancestor of `main`. The
fixture builds that difference REALLY — the unlanded shape puts the commit on a
side branch `main` cannot see — because a fixture that fakes the check cannot
exhibit the thing the invariant guards. Measured: nothing restored, both
tickets still in `hold/`, each reported `hold-not-landed`.

## Invariant 3: refuses rather than guesses

`skip-moved`, `skip-closed`, `skip-absent` are distinct answers, each reported
with its reason — "left alone" and "left alone because it was closed" are
different facts to whoever reads the handover. Idempotence is a consequence
rather than a special case: the second run finds the ticket already in
`active/` and answers `skip-moved`.

## BL-567 scenario 18 — retired, and its manifest deliberately untouched

Removed, not reworded (BL-1006), because this ticket's scenarios are the
replacement coverage. Its stated worry — staleness — is answered by the
ruling's freshness mark rather than dismissed; the live proof it is not
hypothetical is that BL-1309 was parked by the very expedition whose ruling
superseded BL-1309's own.

The feature's mutation manifest is **not** touched: `git diff` shows six
deletions, all scenario text, and zero manifest lines. Regenerating it is the
hardener's pass, and hand-editing a mutation manifest is forbidden outright
(Guardrails). The stamp is therefore stale BY DESIGN until that pass runs —
recorded here because a stale stamp and a forgotten one look identical to a
later reader.

## REWORK after the specifier's amendment (13ab0b1f82)

Holding the parcel was right: the amendment asks for MORE than I had built, so
forwarding would have sent a reviewer both a red scenario and an incomplete
mark.

Scenario 02 is retired (never reworded) and replaced by 08 and 09. My
implementation already satisfied 08 - restored to the folder it was parked
from. **09 exposed a real hole in my version.**

I had marked the restored ticket with a bare `freshness_check_required: true`
and left it otherwise workable. That is nearly useless: the ticket goes back to
`active/`, the coordinator's depth check picks it straight up, and Article
3.6's freshness gate only runs on promotion out of `paused/` - so a restored
ticket would have bypassed the very check the mark exists to demand. The
annotation would have been true and inert.

The amended schema is better and I have built to it: `status: blocked` is the
load-bearing half, because that is what `promote_and_route_next.sh` and the
dropped-parcel sweep actually read (BL-1100/BL-1301). `freshness_check:
required` says why, and `freshness_check_reason:` names the expedition so a
coordinator knows what to check the ticket against. Clearing is the
coordinator's after `deprecate-check.js` allows - never this code's, since
machinery that can clear its own mark makes the mark meaningless.

Scenario 09's "the promotion helper skips it as blocked" is answered by the
REAL `promotion_gates_lib.bb`, loaded in the fixture, rather than by restating
its rule: the mark is only worth something if production's own gate refuses the
ticket. Measured: status blocked, mark present, reason naming BL-9001,
promotion gate refuses.

One of my own earlier unit rows asserted the pre-amendment field name and went
red - corrected rather than deleted, with the reason recorded.

**Acceptance: 9/9 on the amended feature.**

## Verification

- `expedite_lib_test_runner.bb` — ALL PASS, with rows for the record, every
  per-ticket decision, both invariant-2 directions, idempotence, and invariant
  1's structural form.
- `expedite_lib_property_runner.bb` — ALL PROPERTIES HOLD, 500 runs each.
- `expedite_announce_lib_test_runner.bb` — ok.
- Acceptance — 7/8, the failure being the ruling/scenario mismatch above.
- Babashka has no mutation/CRAP/DRY wired (BL-472): gated by the runners above;
  degraded fallback recorded rather than implied.

## One correction to my own first attempt

The handler registered under a feature name I had written from the ticket title
rather than read from the file, so every scenario reported "no step handler
matched" — a failure that looks like broken steps and is actually a scoping
typo. Read the `Feature:` line, do not reconstruct it.

By coder.

---

# Rework after the architect bounce (D1), same day

D1: "the reversal is never wired to its own trigger" — correct, and the
ticket's own `notes:` had asserted the opposite ("the reversal's call site is
the land step, which is itself being changed by this ticket"). Neither half of
that was true on my diff. I inherited the claim without checking it, which is
the actual mistake; the missing wiring is downstream of it.

## The trigger, and why it is not the land step

The architect suggested QA's land step as the most plausible site. It cannot be
the expedition's own process — that exits long before the commit lands — and a
hook inside `land_main_publish.sh` would only fire for lands that go through
that path, missing the hand-lands the expedite lane routinely uses (which is
how BL-1375, the incident this ticket was minted from, actually landed).

So the trigger is a `handoffd` sweep on the reconcile cadence, asking per park
record whether that expedition is on main yet. The queue comes back within a
tick of the land, by whatever route the land took, and it is self-healing for
parks left by runs that already ended.

## What was built

- `handoffd.bb` — `expedite-park-reversal-sweep!`, registered in the poll loop.
- `expedite_cli.bb` — an `unpark <project-root> <run-dir>` subcommand, the
  production entry point the sweep calls. Both its exits route through `exit!`
  (BL-1024e's one-termination-point invariant, which my first draft broke and
  `test_expedite_cli.sh` caught).
- An absent `expedite/<id>` branch now leaves the park in place and says so,
  rather than resolving to something that fails the ancestor check for the
  wrong reason.
- A shadowed binding fix: the freshness mark named the parked ticket instead of
  the expedition that parked it — the one field that tells a coordinator WHICH
  run to check against.
- `unpark-done.json`, so a settled run stops being re-asked on every tick (11
  historical run dirs on this checkout would have been re-shelled forever).

## Verification, both directions

- Acceptance 9/9, now driving `expedite_cli.bb unpark` — the production entry
  point — rather than the pure `unpark-plan` beneath it. With the subcommand
  dispatch removed the fixture FAILS.
- `test_handoffd_expedite_park_reversal_wiring.sh` (new, registered in
  `suite-manifest.tsv` as standing): spawns the REAL daemon against a real
  fixture root and waits for a real ticket to leave a real `backlog/hold/`,
  then asserts it came back `status: blocked` with a mark naming its
  expedition, that the settled run is marked done, and that a ticket a human
  placed in hold/ was untouched. With the sweep's registration removed it
  FAILS.
- `test_expedite_cli.sh`, `expedite_lib_test_runner.bb`, `test_bl1023` — pass.

## The lesson, stated plainly

This is the third parcel this session bounced for the same shape: logic built,
fixture driving the logic directly, no production caller, nothing able to
notice. A fixture that reimplements the wiring cannot see the wiring missing.
The rule I am holding myself to from here: the acceptance fixture calls the
entry point production calls, or it proves nothing about reachability.

By coder.
