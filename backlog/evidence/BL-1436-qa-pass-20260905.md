# QA pass — BL-1436 — 2026-09-05

Commit under review: `5363269449` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`3d698f676f` BL-1436 redo
coder commit, confirmed ancestor via `git merge-base --is-ancestor`).
Went through one architect bounce (real, mutation-verified missing test
coverage for the `costFrom` honest-null rewrite) before reaching me.

This closes `pricingTable.test.js`, one of the small handful of files I'd
been repeatedly listing as "pre-existing unrelated" all session.

## Verification

- `npx vitest run test/pricingTable.test.js`: 32/32 pass (was red for a
  full day, `PRICING_TABLE missing entries for swarm-referenced model(s):
  claude-fable-5-1`).
- Read `PRICING_TABLE['claude-fable-5-1']` directly: `{ inputPerMTok: 10,
  outputPerMTok: 50, cacheReadPerMTok: 0.25 }` — matches the ticket's own
  cited published rates exactly; `cacheCreatePerMTok` deliberately absent
  (not copied from the `claude-fable-5` sibling row, per invariant 1 and
  this ticket's own root-cause callback to the original BL-627 defect —
  an Opus row copied from Opus 4.1).
- Confirmed the bounce fix landed: `pricingTable.test.js` now asserts
  `estimateCostUsd(..., 'claude-fable-5-1')` returns `null` (not a dollar
  figure or a silently-zero cost) for nonzero `cacheCreationTokens` — the
  exact gap the architect found by mutating `return null` to `continue`
  and confirming 30/30 tests and 6/6 acceptance scenarios stayed green
  regardless (a genuine, measured non-vacuity finding, not a nitpick).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1436-the-pricing-table-prices-every-model-the-swarm-runs.feature`
  — 6/6 scenarios pass, including "the entry records where its rates came
  from" and "the display-name map names the model and the register row is
  gone".
- `extension/src/swarm/modelDisplayName.ts` confirmed carrying
  `'claude-fable-5-1': 'Fable 5.1'`.
- Register cleanup: `grep -c pricingTable backlog/standing-reds.tsv` = 0.
- Regression: `pricingWindows.property.test.js` — 3/3 pass, unaffected by
  the `costFrom` rewrite (its own generator never produces an undefined
  rate, so it never exercised the honest-null path either way).
- `required_wiring`: all three anchors confirmed
  (`pricingTable.ts::claude-fable-5-1`, `modelDisplayName.ts::claude-fable-5-1`,
  `bl1436PricingTableCoversFable51Steps.js::registerSteps`).
- Full unit lane (`npm test`): 2 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`) —
  `pricingTable.test.js` confirmed absent; no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
