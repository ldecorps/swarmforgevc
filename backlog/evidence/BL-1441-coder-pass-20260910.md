# Evidence — BL-1441 — coder pass (2026-09-10)

Role: coder. Ticket: BL-1441-the-four-remaining-0819-hardening-gates-are-run.
Promoted per the specifier's 2026-09-10 deprecator freshness amendment
(Article 3.6): `not_before` moved 2026-09-11 → 2026-09-10 once
`mutation_cooldown_days` dropped 3 → 1 (`df25be597b`).

## Eligibility re-checked fresh, all three still-outstanding rows

All three (BL-620, BL-955, BL-956-pipeline-board-caption-and-cap-hotfix)
read `mutation_cooldown_gate.bb` **DECISION: run** on every file, host load
quiet throughout (`1.35`–`2.46` on 20 cores). Detail in each row's own
evidence file.

## The three runs — all completed, all discharged

| parcel | mutants | killed | survived | no coverage | timeout | duration | evidence |
|---|---|---|---|---|---|---|---|
| BL-620 | 4307 | 2911 | 573 | 732 | 11 | 56m44s | `backlog/evidence/BL-1441-BL-620-mutation.md` |
| BL-955 | 2195 | 1795 | 303 | 91 | 6 | 9m47s | `backlog/evidence/BL-1441-BL-955-mutation.md` |
| BL-956-pipeline-board-caption-and-cap-hotfix | 1028 | 811 | 177 | 39 | 1 | 3m17s | `backlog/evidence/BL-1441-BL-956-pipeline-board-caption-and-cap-hotfix-mutation.md` |

0 errors on every run. All three ledger rows discharged
(`hardening_debt_ledger_update.bb --discharge <parcel> mutation --evidence
<path>`); every survivor and no-coverage mutant is named in its own
evidence file, one bullet line each (`Survivors: N` line + `- ...` per
mutant, matching the acceptance handler's own parse contract in
`specs/pipeline/steps/bl1441RemainingHardeningGatesRunSteps.js`).

Survivor/no-coverage volume (573+732, 303+91, 177+39) is far past BL-954's
2026-09-06 precedent (25, 0 no-coverage) — these three files
(`telegram-front-desk-bot.ts` especially, 51.72%/77.92% mutation score) have
never had a scoped hardening pass before. **Not force-discharged, not
force-killed**: no mutant suppressed, no assertion loosened, no test
authored to chase survivors — that is the hardener's domain (Article 1.6),
next in this ticket's own `required_stages`, working from these three
evidence files as its worklist.

## Register cleanup (invariant 2)

`backlog/standing-reds.tsv`'s three `hardening`/`BL-1441` rows (BL-620,
BL-955, BL-956) removed in this same commit, per invariant 2 ("The register
row of a gate leaves in the same commit that discharges it, never
earlier"). Verified: `standing_red_register_cli.bb .`'s hardening lane now
holds only `BL-1468` and `BL-1488` — two **unrelated** tickets' own
hardening debt (see next section), never BL-1441's own rows.

## Ledger check: all five 2026-08-19 rows discharged

`hardening_debt_ledger_read.bb .` — every row with `detected_at:
"2026-08-19"` now carries a `discharged_at` and evidence pointer:

| parcel | gate | discharged_at | evidence |
|---|---|---|---|
| BL-620 | mutation | 2026-09-10 | `BL-1441-BL-620-mutation.md` |
| BL-955 | mutation | 2026-09-10 | `BL-1441-BL-955-mutation.md` |
| BL-954-a-bounce-verifies-its-own-revert | mutation | 2026-09-06 | `BL-1441-BL-954-a-bounce-verifies-its-own-revert-mutation.md` |
| BL-956-pipeline-board-caption-and-cap-hotfix | mutation | 2026-09-10 | `BL-1441-BL-956-pipeline-board-caption-and-cap-hotfix-mutation.md` |
| BL-956-pipeline-board-caption-and-cap-hotfix | gherkin-mutation | 2026-09-05 | `BL-1439-bl956-gherkin-mutation.md` |

`no outstanding row is dated 2026-08-19` — the first half of scenario 01 —
passes.

## Acceptance run: 4/5, one spec gap found (not fixed here)

`specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1441-the-four-remaining-0819-hardening-gates-are-run.feature`:

- Scenario 01, step "no outstanding row is dated 2026-08-19": **passes**.
- Scenario 01, step "the register report holds no hardening lane row":
  **fails**. The register's hardening lane is NOT empty — it holds two
  rows for **BL-1468** (`first_seen: 2026-09-07`) and **BL-1488**
  (`first_seen: 2026-09-08`), both minted and deferred AFTER BL-1441 itself
  was minted (2026-09-06) and BOTH ENTIRELY UNRELATED to BL-1441's own four
  file sets. Neither ticket, nor its file set, nor its ledger row is named
  anywhere in BL-1441.
- Scenario 02, all four Examples: **pass** (BL-620, BL-955,
  BL-954-a-bounce-verifies-its-own-revert,
  BL-956-pipeline-board-caption-and-cap-hotfix — each evidence file's
  `Survivors: N` line and its N-or-more `- ` reason lines validate clean).

**This is a spec gap, not an unmet invariant of this ticket.** BL-1441's
own two invariants both hold: invariant 1 (every one of BL-1441's own five
rows is either discharged with a committed result or, had it stayed
blocked, an attempt — none left "discharged by assertion") and invariant 2
(BL-1441's own three register rows left in this commit). The feature's
scenario 01 assertion — "the register report holds no hardening lane row"
— was written 2026-09-06 when BL-1441's four rows were the ONLY hardening
debt in existence; it does not scope to BL-1441's own rows, so it now
requires the ENTIRE hardening lane to be empty, which BL-1441 was never
positioned to guarantee once other tickets (BL-1468, BL-1488) started
accruing their own hardening debt during the days this ticket sat parked
on cooldown. Per Article 1.3/1.2 and the coder role's "Does Not Own" (no
hand-authored Gherkin), this is not fixed here — routed to the specifier
by note instead (Article 4.4: spec gaps leave by note, priority 00, never
a parcel).

**Not forwarded this pass.** The ticket's own `qa_e2e_procedure` step 1
requires "The BL-1441 feature passes (two scenarios, five runs)" — it
currently does not, for the reason above. Forwarding a parcel whose own
QA procedure cannot pass, for a reason outside this ticket's scope to fix,
would just push the same failure downstream. Staying with coder /
specifier until the scenario 01 assertion is scoped to BL-1441's own rows
(or the specifier rules otherwise).

## Verification

| check | result |
|---|---|
| `hardening_debt_ledger_read.bb .` | all five 2026-08-19 rows `discharged_at` set |
| `standing_red_register_cli.bb .` hardening lane | `BL-1468`, `BL-1488` only — no `BL-1441` rows, `unowned: []` |
| Acceptance feature | 4/5 (scenario 01 step 2 fails on unrelated rows; scenario 02 all 4 examples pass) |

## Scope

Nothing in `out_of_scope` touched: cooldown gate/window untouched, BL-1440
untouched (only relied upon). `backlog/standing-reds.tsv` edited only to
remove BL-1441's own three discharged rows — BL-1468's and BL-1488's rows
untouched. No `.feature` file edited (Gherkin authorship is out of coder's
domain).

## Addendum — spec amendment re-match (2026-09-10, commit 810ddf4d5b)

Specifier amended the spec in `7c53f4882f` (note, priority 00): scenario
01's second `Then` rescoped from "the register report holds no hardening
lane row" to "...holds no hardening lane row naming BL-1441", matching the
shape BL-1468/BL-1488 already use. Merged main, re-matched the handler
regex in `bl1441RemainingHardeningGatesRunSteps.js` and scoped the
assertion to `r.ticket === 'BL-1441'` (BL-233, same parcel). Re-ran
`specs/pipeline/scripts/run_acceptance.sh` on this feature: 5/5 pass. The
ticket's `qa_e2e_procedure` step 1 ("BL-1441 feature passes") now holds;
forwarding this pass to cleaner.
