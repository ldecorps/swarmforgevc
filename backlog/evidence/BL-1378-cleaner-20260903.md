# BL-1378 — cleaner pass, 2026-09-03

Clean sweep. No changes made.

## Checks run

| # | Check | Result |
|---|-------|--------|
| C1 | `bb swarmforge/scripts/test/ticket_close_guard_lib_test_runner.bb` | ALL PASS |
| C2 | `bash swarmforge/scripts/test/test_bl1378_expedite_close_guard.sh` | ALL PASS (31 checks) |
| C3 | `bb swarmforge/scripts/test/bl1378_expedite_close_guard_property_runner.bb` | ALL PROPERTIES HOLD (500 runs) |
| C4 | `bash swarmforge/scripts/test/test_ticket_close_guard.sh` | ALL PASS — pre-existing guard unchanged |
| C5 | `bb swarmforge/scripts/test/bl869_multi_ticket_close_guard_property_runner.bb` | ALL PROPERTIES HOLD |
| C6 | `bash swarmforge/scripts/test/test_commit_integrity_cli.sh` | ALL PASS |
| C7 | `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1378-*.feature` | 12/12 pass |
| C8 | `node extension/out/tools/mutation-site-count.js` on both changed `.js` files | `bl1378ExpediteCloseGuardSteps.js` 180 sites (advisory `over`); `index.js` 4 sites (`within`) |
| C9 | `jscpd` over the changed `.bb`/`.js` sources, min-lines 15 | 0 clones |
| C10 | `suite-manifest.tsv` / `index.js` registration | new suite files and acceptance handler registered |

## Mutation-site advisory (BL-485) — no split

`bl1378ExpediteCloseGuardSteps.js` is 201 lines / 180 sites, over the
100-site threshold. Same reasoning as this session's three prior tickets
(BL-1360/BL-1376/BL-1377): one feature's step handlers, well under the
500-1000+ line norm of the rest of `specs/pipeline/steps/`, no seam a split
would improve. Kept whole.

## Careful read of the gate logic (BL-1378 changes an approval gate)

Traced `expedite-approval`, `close-verdict`, and `ancestor-of-main?` by hand
against all three invariants before trusting the green suites:

- **Invariant 1 (approval path, not a second definition)**: `close-verdict`'s
  `qa-mailbox?` branch returns `:allowed? true` unconditionally and first —
  a corrupt or missing store can never veto a close the mailbox already
  approved. Confirmed against shell check `02c` (corrupt store + QA handoff
  present → still allowed).
- **Invariant 2 (fail closed, never absent-as-approval)**: `:absent` and
  `:problem` are distinct `:kind`s throughout — an absent store falls through
  to `:missing-qa-approval` (today's behavior), a `:problem` store always
  refuses via `:expedite-store-problem`. `expedite-approval`'s file loop
  keeps scanning every remaining file for a `record-line-problem` even after
  finding a match, so a corrupt file sorted after a matching one still
  poisons the whole store rather than the match winning — verified by
  reading the `loop`/`recur` directly, not just by the green suite.
- **Invariant 3 (matches ticket + stage + approval, all three)**:
  `expedite-record-approves?` is a single `and` over all three fields, and
  property P3's near-misses (each breaking exactly one field) all refuse —
  confirmed the property runner's own generator asserts all seven near-miss
  shapes and five other-ticket-id draws were actually produced, not just
  hoped for.
- `ancestor-of-main?` consults both `main` and `origin/main` (BL-891) and
  only returns `false` when BOTH resolve and BOTH say "not an ancestor";
  any unresolvable ref or ambiguous exit code falls to `nil`
  (undeterminable), which `close-verdict` refuses on rather than treating as
  landed. No path collapses "could not tell" into "yes".

No defect found. Nothing to fix.

## Not addressed here

`test_handoffd_outbox_vanished_parcel_wiring.sh` remains unregistered in
`suite-manifest.tsv` (2026-09-02 handoffd hotfix, not this ticket's work —
already surfaced by the coder and by this session's prior three tickets).
BL-1375's own staged/uncommitted close move is left exactly as the human's
ruling requires; QA owns `qa_e2e_procedure` step 7's live regression, per
the coder's own evidence.

## No functional change

Forwarding as-is.

By cleaner.
