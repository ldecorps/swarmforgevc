# BL-1302 — documenter review pass, 2026-08-31

## Scope checked

- Re-read the ticket description, invariants, and the coder/architect/hardener
  evidence files already in `backlog/evidence/` for this ticket.
- Diffed the received commit (hardener `bbdbc09a3d`, carrying the full
  coder+architect+hardener chain) for user-visible behaviour: the
  duplicate-chain guard (`duplicate_chain_guard_lib.bb`) now skips a
  `non-forwarding: true` parcel instead of treating it as a competing chain,
  via the shared `handoff-lib/non-forwarding?` predicate.
- Grepped `docs/` and `swarmforge/*.md` for existing coverage of the
  duplicate-chain guard and the reverse-hop feature it interacts with:
  `swarmforge/handoff-protocol.md` (Duplicate-Chain Guard, BL-760 section)
  is the only doc describing the guard's mechanics; `docs/reference/Specification.MD`
  and `docs/diagrams/*.mmd` do not mention the guard or reverse hops at all
  (a pre-existing gap from when reverse-hop itself landed, out of scope for
  this ticket — it did not change reverse-hop behaviour, only the guard's
  interaction with it).

## Findings

**D1 — resolved in this pass.** `swarmforge/handoff-protocol.md`'s
Duplicate-Chain Guard mechanics list described only exact-id matching; it did
not mention the non-forwarding exemption this ticket adds, so a reader would
still expect a reverse-hop copy to block a forward. Fixed by adding a bullet
documenting the `non-forwarding?` skip, its fail-closed default, and the
shared-spelling rationale (commit `8369e33099`).

No other defect found. Diagrams under `docs/diagrams/` do not depict
guard-level predicates (checked `handoff-flow.mmd` — no mention of
duplicate-chain or non-forwarding), so no diagram change is triggered by this
parcel.

## Forwarding

Documentation updated and committed (`8369e33099`, ancestor of hardener's
`bbdbc09a3d`). Forwarding to QA.
