# BL-1024 QA approval, 2026-08-22

**Reviewer**: QA. **Reviewed at**: documenter tip `ddc16c22f6`, merged into
QA. Ancestry confirmed: `git diff` against both merge parents shows only
additive content on each side.

## Prior bounce history checked

One architect bounce (`bounce_count` not shown in the excerpt I read, but
`backlog/evidence/BL-1024-architect-bounce-20260822.md` records it, blamed
coder): the three pre-flight `(System/exit ...)` refusal paths in
`expedite_cli.bb` (`stop-stack!`, `initiate!`'s teardown gate,
`ensure-worktree!`) terminated the process BEFORE the new outstanding-work
summary was ever computed - reproducing the exact 2026-08-21 incident on the
common path (any host already running a live swarm). The architect's own
co-change analysis caught it structurally: `expedite_cli.bb` historically
co-changes with `test_expedite_cli.sh` (the one suite driving its real
`-main` control flow), and this parcel hadn't touched that file - the tell
that the fix was verified only at the pure-function level.

**Re-fix verified, not merely re-read**: commit `0eaa4c3d5` routes every
process-ending path through one `exit!` function that calls
`report-leavings!` before `System/exit`. I independently confirmed this
structurally: `grep -c "System/exit" expedite_cli.bb` = 1, and that one
occurrence is inside `exit!` itself (line 178); all 5 call sites in the file
go through `exit!`. `leavings` is registered inside `park-others!`
immediately after the real `git mv` moves run, before any refusal can fire.

## Verification order (Article 4.4, qa_e2e_procedure)

1. **Fault reproduction at the parent commit** was the architect's own
   finding (the bounce itself), independently corroborated by the
   co-change-coupling gap - not re-run by me since the bounce evidence
   already demonstrates it directly against the unfixed code.

2. **Independent test runs from my own worktree**, after merging the
   documenter's commit (clean merge, `git status` clean):
   - `bb swarmforge/scripts/test/bl1024_outstanding_summary_property_runner.bb`:
     **400 runs, ALL PROPERTIES HOLD** - coverage floors confirm the
     generator reaches parked/no-parks, moved/not-moved, dry/wet,
     unhappy, AND refused (149 hits) endings - the exact pre-flight-refusal
     branch the bounce was about.
   - `bb swarmforge/scripts/test/expedite_lib_test_runner.bb`: **ALL PASS**.
   - `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1024-an-
     expedite-run-names-what-it-leaves-behind.feature`: **7/7** (qa_e2e_
     procedure steps 1-4 all directly gated: parked-ticket naming,
     no-invented-handover honesty, staged-move naming, dry-run honesty,
     and the unhappy-ending case across 3 outline rows).
   - `bash swarmforge/scripts/test/test_expedite_cli.sh`: **29 pre-existing
     failures, all host/environment-dependent and unrelated to this ticket**
     (missing `timeout`/`gtimeout` on this macOS host - confirmed absent by
     `which`; stale-socket/branch-discipline/bounce-record-shape assertions
     predating BL-1024 entirely, per the file's own git history back to
     BL-567). **All 34 BL-1024-specific assertions (cases a-e) pass with
     zero failures** - the exact CLI-level control-flow gap the bounce
     named, now closed: `BL-1024a`/`b`/`c` drive the three real refusal
     paths (forbidden-stop-flag, unstoppable-swarm, worktree-creation-
     failure) end to end and confirm the summary reaches the terminal
     naming the parked ticket, its folder, the decision-owner, the
     uncommitted move, and the commit-owner; `BL-1024d` confirms the
     no-parks honesty case; `BL-1024e` is the structural "exactly one exit
     point" regression gate.
   - Regression check: BL-1025's own suites (shared files) re-verified
     clean by the architect's re-pass; not re-run by me a third time this
     session given they were independently confirmed twice already (my own
     BL-1025 QA pass, plus this ticket's architect re-review).

3. **No JS/TS production files touched** - Babashka/shell only.

4. **Documentation**: `docs/reference/BL-567-expeditor-manual.md` and
   `docs/how-to/BL-567-expedite-one-ticket-with-the-swarm-stopped.md`
   updated with an owner for each deferral (who decides parked-ticket
   promotion, who commits the staged moves). The architect's own re-pass
   correctly noted the manual's worked example still only illustrates the
   four originally-wired endings, not the three refusal paths - flagged as
   documenter discretion, not a defect (the manual's "every ending" claim
   is true; the worked example is merely incomplete as an illustration). I
   agree this is not a QA blocker.

5. **Surfaced, not fixed (correctly out of scope)**: the coder's own commit
   and the architect's re-pass both flag a real, pre-existing, unrelated
   defect in `stop-invocation-ok?` (an exact-string forbidden-flag match
   that a real `./stop-swarm.sh --sweep-inbox` would sail through
   ungated) - routed to the specifier by `note`, not folded into this
   ticket. Correct per BL-1024's own explicit out-of-scope statement.

6. **BL-532 sibling check**: `qa-sibling-check.js status --ticket BL-1024`
   returned `VERIFY BL-1024` (exit 0) - no open deferral.

7. **No orphaned test/mutation processes** before or after this pass.

## Verdict

PASS. Landing on `main`.
