# BL-1538 — hardender review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Test-fixture/test-gate parcel; no `extension/src/*.ts` touched, so CRAP and
DRY (both scoped to `extension/src`) are N/A. Babashka has no wired
mutation/CRAP/DRY (BL-472); `mutation_cost: low` per the ticket.

Gates run:
- `mutation_cooldown_gate.bb`: DECISION run (file age 22.43d, load 1.28/20
  cores, quiet).
- `bb swarmforge/scripts/test/bl1028_promotion_refusal_property_runner.bb`
  run twice: both `ALL PROPERTIES HOLD`, coverage includes
  `:control-commit 1` both times.
- Negative control (invariant 1, manual, mirrors the coder's own property
  test): scratch copy of `swarmforge/scripts`, added a new `load-file` edge
  to `promotion_gates_lib.bb` pointing at a sibling `.bb`, ran the runner's
  own `--copy-into <dir>` with no runner edit — the new file landed in the
  copy set.
- Acceptance: `run_acceptance.sh` on the ticket's feature — 9/9 scenarios
  green (includes the Outline's 5 examples).
- BL-113 Gherkin soft mutation on the ticket's feature (it has one Scenario
  Outline, scenario 02): 5/5 mutants killed, 0 survived, 0 errors,
  ~76s. Manifest embedded in the feature file by the tool.
- Whole-tree standing guards (parcel touched `specs/pipeline/steps/` and
  `extension/test/`): all 18 non-property `*Guard*.test.js` files, 183/183
  tests green.
- Coder-authored property test
  (`bl1538Bl1028RunnerFixtureClosureInvariants.property.test.js`) via
  `vitest.properties.config.mjs`: 2/2 green. Read the file: both invariants
  are driven behaviourally through the real `--copy-into` flag against a
  scratch scripts tree (never a source parse), invariant 1 covers both a
  direct and a transitive new load-file edge with a reach-floor assertion,
  invariant 2 sweeps every closure member in turn and includes a decoy
  "nothing missing" source comment to prove the guard's read is behavioural
  and not fooled by a textual claim. No vacuity found.
- Orphan check: no stray tmp dirs or fixture processes after the run;
  live `bb` processes present are the standing swarm daemons
  (`operator_runtime_supervisor.bb`, `handoffd.bb`, etc.), not fixtures.

No production line changed (constraint honored): `promote_and_route_next.sh`,
`promotion_gates_lib.bb`, `promotion_gates_cli.bb` untouched.
