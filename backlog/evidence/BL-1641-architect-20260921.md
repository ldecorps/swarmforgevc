# BL-1641 — architect review pass, 2026-09-21

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: code review of extension/src/tools/night-closing-ceremony-run.ts mainHasBriefing, confirmed via git cat-file -e repro
- **Commit hash**: bf75516785
- **First error excerpt**: catch { return false; } in mainHasBriefing() returns false (briefing absent, safe to write) on ANY git error, including transient/unrelated failures - contradicting its own comment ("must fail closed... a broken read must never read as safe to write") and invariant 1 (never touch a briefing main already has)
- **Failure class**: behavior
- **Expected vs observed**: a git read error in mainHasBriefing must return true (fail closed: treat as "might already have it", refuse to write) per the function's own documented contract and the ticket's FIRM invariant
- **Blamed role**: coder
- **Remediation pointer**: swap the catch block's return to true; add a test that forces git cat-file -e to fail for a reason other than path-absence and asserts landDocumenterBriefing/composeHeadlessBriefing both refuse to write

By architect.
