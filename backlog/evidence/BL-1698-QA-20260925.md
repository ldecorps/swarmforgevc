# BL-1698 — QA review pass, 2026-09-25

7 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: sed -n 312,332p specs/pipeline/steps/bl1698LocalParcelDriverLifecycleSteps.js; grep -c -i babysit swarmforge/scripts/test/test_handoffd_driver_seat_injection_skip.sh
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: Scenario 06 'Given babysitterd finds both ... idle with work' runs spawnSync('bash', [DRIVER_INJECTION_SKIP_TEST]) = test_handoffd_driver_seat_injection_skip.sh, which starts `bb handoffd.bb` (line 60); 'When babysitterd runs its nudge pass' body is `/* the fixture test above already ran the real daemon's own pass */`; the Then steps match that script's '01:'/'02:' PASS lines, which are about handoffd's wakes. grep -c babysit on the reused script = 0: babysitterd's nudge pass never runs.
- **Failure class**: acceptance
- **Expected vs observed**: scenario 06 drives babysitterd's real nudge pass (the ticket's own required_wiring comment: 'scenario 06 drives babysitterd's real nudge pass, so the babysitter skip is exercised through its live consumer') / observed: it drives handoffd's wake path, so it stays green whatever babysitter_nudge_lib.bb does
- **Blamed role**: coder
- **Remediation pointer**: specs/pipeline/steps/bl1698LocalParcelDriverLifecycleSteps.js scenario 06 handlers: drive babysitter_nudge_lib.bb nudge-resident! (babysitterd's nudge pass) over fake tmux panes for an aider driver seat and a Claude seat; assert the driver seat's skip status and the Claude seat's injected nudge

## D2

- **Failing command**: grep -n 'aider-agent?\|parcel-driver\|driver-seat' swarmforge/scripts/babysitter_nudge_lib.bb; git diff origin/main 537f4f8cf0 --stat -- swarmforge/scripts/babysitter_nudge_lib.bb
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: 67:(defn aider-agent? [agent]  (= :shell-run-script (:wake-style (agent-runtime-lib/capabilities agent))))  93:(aider-agent? agent) -> :skip-aider-agent. No :parcel-driver / parcel-driver-capable? / driver-seat? reference; the diff against origin/main is empty. The coder's evidence item 6: 'No production change ... already covers the driver's coder seat - confirmed, not re-implemented.'
- **Failure class**: behavior
- **Expected vs observed**: requirement 6: 'babysitterd skips a driver seat in its nudge pass, reading the same capability flag BL-1697 added' (How: 'the skip belongs next to it and must read the new capability') / observed: the only skip is the pre-existing :wake-style check, so a driver-capable provider with a :chat-message wake style would still be nudged
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/babysitter_nudge_lib.bb nudge-resident! cond: add a driver-seat skip reading prompt-engine-lib/parcel-driver-capable? (or local-parcel-driver-lib/driver-seat?) beside aider-agent?; D1's rebuilt scenario 06 then exercises it

## D3

- **Failing command**: grep -n 'chat-clear!\|chat-read-only!\|set-writable! p false' swarmforge/scripts/local_parcel_driver_lib.bb; sed -n 618,620p swarmforge/scripts/local_parcel_driver_lib.bb
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: Chat set-up (chat-clear!/chat-read-only!/chat-add!/set-writable! false) occurs only at lines 518-521 inside continue-after-merge! (a fresh parcel). run-gate! 618-620: `(if (< fixTurnsUsed fixTurnsLimit) (do (type-raw! socket session agent (fix-request-text ticket (:reason decision))) ...` - no set-up. After handoffd's startup resume-writable-sweep! an 'awaiting-model' record's spec files are writable, and a failed gate types the fix request into the relaunched chat with no files added, no read-only spec, and the spec still writable for every remaining turn. resume-from-hold! (hold release by answer) also types straight in. The how-to 'Surviving a restart' says the chat 'is set up again before any such fix request, exactly as a fresh parcel's would be' - untrue today.
- **Failure class**: behavior
- **Expected vs observed**: requirement 1: 'A relaunched seat's chat is set up again (clear, read-only spec, editable files) before any fix request' / observed: no resume or hold-release path runs the chat set-up before a fix request
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/local_parcel_driver_lib.bb: before the first fix request after a process start and in resume-from-hold!, run the chat set-up (chat-clear!, chat-read-only! per spec file, chat-add! per editable path, set-writable! false), plus a test for a failing gate after resume; then re-check the how-to text

## D4

- **Failing command**: bb tmp/bl1698-offbyone.bb  (scratch probe: with-redefs stubs of answer-available?/type-raw!/state/seat-test!; held record fixTurnsUsed 3 of 3, an answer waiting, gate failing, drive-tick! x3)
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: TYPED: BL-9: use the other approach - fix it; never edit a read-only file.
TYPED: BL-9 did not pass the gate (no model commit) - fix it; never edit a read-only file.
TYPED: ESCALATE BL-9 no model commit
- **Failure class**: behavior
- **Expected vs observed**: after the answer's ONE fix request, a failing gate escalates again (resume-from-hold! docstring 'so the very next failed gate escalates again'; spec '(one more escalation if it still fails)'; how-to 'escalates again rather than looping'; diagram DRESUME 'one more try only') / observed: a second, generic fix request is typed first, because fixTurnsUsed = limit-1 still passes run-gate!'s (< fixTurnsUsed fixTurnsLimit)
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/local_parcel_driver_lib.bb resume-from-hold!: set :fixTurnsUsed to the limit (or equivalent), and add a test for the answer-then-still-failing path (scenario 02 covers only the passing case)

## D5

- **Failing command**: bb tmp/bl1698-sparse.bb  (scratch probe: seat!/type-raw!/escalate! stubbed; a hold as mechanical-mail! and start-ticket-parcel! write it on a merge conflict, {:escalated true :ticket <t> :reason "merge conflict"}; an answer waiting; drive-tick! x3)
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: HELD mail
  TYPED: mail: resolve the conflict by taking theirs - fix it; never edit a read-only file.
  TYPED: mail did not pass the gate (no model commit) - fix it; never edit a read-only file.
  TYPED: ESCALATE mail no model commit
  SEAT CALLS: [[[test] {SEAT_TICKET mail, SEAT_ACCEPTANCE nil}] [[test] {SEAT_TICKET mail, SEAT_ACCEPTANCE nil}]]
HELD BL-9: the same two typed fix requests and two gate runs with SEAT_ACCEPTANCE nil. (The real daemon-cycle-guard-lib/sh! accepts a nil env value without throwing, so the live daemon behaves the same.)
- **Failure class**: behavior
- **Expected vs observed**: a merge-conflict hold (the pseudo-ticket 'mail' from requirement 4, or a ticket whose merge conflicted) never gets a model fix request, since requirement 4 says 'no model turn', and no gate runs without an acceptance path, post-merge head or spec files / observed: an answer types two model fix requests into the pane and runs the gate with SEAT_ACCEPTANCE unset and postMergeHead nil, then re-escalates
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/local_parcel_driver_lib.bb resume-from-hold!: re-arm only records that carry gate context (:acceptancePath/:postMergeHead); give a merge-conflict hold's answer its own disposition (e.g. retry the merge, or leave it to `release`); if the right disposition is unclear, note the specifier

## D6

- **Failing command**: git grep -n 'resume!' -- swarmforge specs extension/test docs | grep -v 'chase_sweep\|non-vacuity\|resume-writable\|resume-from-hold\|in-process-resume'
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: Only hits: local_parcel_driver_lib.bb:650 (defn resume! ...), docs/how-to/BL-1697-local-parcel-driver.md:100, docs/diagrams/handoff-flow.mmd:20 and :118. handoffd.bb:5559 calls (local-parcel-driver-lib/resume-writable-sweep! project-root) directly. resume!'s docstring says 'The live daemon calls this exactly once per driver seat, at its own startup'; the how-to says '`resume!` ... the live daemon calls it exactly once per driver seat'; diagram DBOOT says 'daemon start: resume!'.
- **Failure class**: behavior
- **Expected vs observed**: the documented 'driver starts' entry point is the one the daemon actually calls / observed: resume! has no caller anywhere, and its docstring, the how-to and the diagram all describe a call path that does not exist
- **Blamed role**: coder
- **Remediation pointer**: either call resume! from handoffd's startup, or delete it and make the lib docstring, the how-to 'Surviving a restart' section and handoff-flow.mmd DBOOT name resume-writable-sweep! at daemon start (both code and docs are wrong, so coder, the earlier stage)

## D7

- **Failing command**: git grep -ln 'resume-writable-sweep' -- extension/test swarmforge/scripts/test specs/pipeline/steps; bash swarmforge/scripts/test/test_handoffd_one_shot_flags_parse.sh; bash swarmforge/scripts/test/test_handoffd_driver_seat_injection_skip.sh
- **Commit hash**: 537f4f8cf0
- **First error excerpt**: Hits: extension/test/bl1698LocalParcelDriverResumeAndRelease.property.test.js (calls the fn directly via bb -e) and specs/pipeline/steps/bl1698LocalParcelDriverLifecycleSteps.js (via the CLI's own trailing 'resume' flag). No handoffd test. Both handoffd changed-path tests RUN and PASS (ALL PASS), but they load handoffd and assert nothing about spec permissions. handoffd.bb:5559 (the new line) is the only live caller that restores a crashed driver's spec files.
- **Failure class**: unit
- **Expected vs observed**: the changed production path handoffd.bb:5559 (invariant 1's live consumer) has a test that fails when the line is removed / observed: deleting it leaves every suite green; scenario 01's 'the driver starts again' drives the CLI flag, not the daemon
- **Blamed role**: hardender
- **Remediation pointer**: a handoffd startup wiring test: seed .swarmforge/local-driver/<seat>.json naming a 0444 spec file, run handoffd once (e.g. --poll-once), assert the file is writable

By QA.

## Detail

Parcel: documenter 2969b0eda8, merged into QA as 537f4f8cf0 (the commit tested).
No prior BL-1698 bounce on `main`, on `origin/main` (0/0 apart) or in the master bounce store.
`qa-sibling-check.js status`: VERIFY BL-1698 (no deferral).

Gates (every one RUN, none BLOCKED; qa-gather.js report, one run each):
- stragglers before/after: none from this repo. The live Stryker workers belong
  to /home/carillon/gpu-bargain-hunter, a different swarm, and were not touched.
- standing-red register: 6 rows, all owned (BL-1748/1749/1750/1760/1762/1763), unowned [].
  No BL-1698 row in standing-reds.tsv, property_suite_standing_allowlist.tsv or suite-poles.tsv.
- pre_qa_gate.sh BL-1698 537f4f8cf0 (required_wiring): OK.
- npm test: exit 0.
- npm run test:properties: exit 0. Only the allowlisted BL-871
  `[vitest-worker]: Timeout calling "onTaskUpdate"` appeared.
- qa_e2e 1: BL-1698 feature: 9/9 pass. It is green but not proof of scenario 06 (D1).
- qa_e2e 2: BL-1697 feature on the same commit: 12/12 pass.
- qa_e2e 3: babysitter suites in suite-manifest.tsv, one run each, all exit 0:
  babysitter_assess_lib_test_runner.bb, babysitter_nudge_lib_test_runner.bb,
  babysitterd_freshness_lib_test_runner.bb, babysitterd_sweep_lib_test_runner.bb,
  test_babysitter_check.sh, test_babysitter_nudge_resident.sh,
  test_babysitterd_census_lib.sh, test_babysitterd_heartbeat_pulses.sh,
  test_babysitterd_lifecycle.sh, test_operator_runtime_babysitterd_watchdog.sh.
- qa_e2e 4: `bb swarmforge/scripts/local_parcel_driver_cli.bb` with no args
  prints both usage lines and exits 1; `git status` is unchanged (no mailbox touched).
- qa_e2e 5: the parcel's own paths are the Scope files, tests, docs and evidence, plus
  `swarmforge/scripts/handoffd.bb` (6 lines: the startup sweep call). That call site is
  the lib's existing live consumer (BL-1697's anchor), so it is accepted and not an item;
  its missing test is D7.
- Article 4.5 changed paths: local_parcel_driver_lib.bb and local_parcel_driver_cli.bb
  are exercised by bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js and
  bl1698LocalParcelDriverResumeAndRelease.property.test.js (in npm test and
  test:properties) and by both acceptance features. handoffd.bb:
  test_handoffd_one_shot_flags_parse.sh and test_handoffd_driver_seat_injection_skip.sh
  RUN, ALL PASS (see D7).

Invariant 2: disposed the same way as BL-1697's own invariant 2 (encoded by the
acceptance scenarios, reason recorded in the property test's header). Accepted on
that precedent, so it is not an item. D5 is a path the scenarios do not reach.

Routing: every item except D7 is the coder's, and D7 (hardender) is later in the chain,
so the single bounce goes to the coder and the inventory travels with it (Article 4.4).
The scratch probes for D4/D5 are reproduced verbatim in their items and were deleted
after the run.

## Detail (probe scripts for D4 and D5, verbatim, run from the worktree root as `bb <file>`)

### D4: tmp/bl1698-offbyone.bb

```clojure
(load-file "swarmforge/scripts/local_parcel_driver_lib.bb")
(ns user (:require [local-parcel-driver-lib :as d]))
(def typed (atom []))
(def st (atom nil))
(with-redefs [d/answer-available? (fn [_ _] "use the other approach")
              d/type-raw! (fn [_ _ _ t] (swap! typed conj t))
              d/write-driver-state! (fn [_ _ s] (reset! st s))
              d/read-driver-state (fn [_ _] @st)
              d/seat-test! (fn [& _] {:exit 1 :out "" :err ""})
              d/commit-count-since (fn [& _] 0)
              d/touched-paths-since (fn [& _] [])
              d/file-sha256 (fn [_] "x")
              d/restore-spec-writable! (fn [_] nil)
              d/escalate! (fn [_ t r] (swap! typed conj (str "ESCALATE " t " " r)))
              d/turn-idle? (fn [_] true)
              agent-runtime-inject/capture-pane-text (fn [& _] "")]
  (let [ctx {:project-root "/nonexistent" :checkout "/nonexistent" :role "coder" :seat-id "coder" :agent "aider" :socket "s" :session "x"}
        held {:escalated true :reason "acceptance still failing" :ticket "BL-9" :phase "awaiting-model"
              :specFiles [] :specHashesBefore {} :editablePaths [] :fixTurnsUsed 3 :fixTurnsLimit 3 :acceptancePath "a" :postMergeHead "h"}]
    (reset! st held)
    (dotimes [i 4] (d/drive-tick! ctx))
    (doseq [t @typed] (println "TYPED:" t))
    (println "FINAL:" (select-keys @st [:escalated :fixTurnsUsed :reason]))))
```

### D5: tmp/bl1698-sparse.bb

```clojure
(load-file "swarmforge/scripts/local_parcel_driver_lib.bb")
(ns user (:require [local-parcel-driver-lib :as d]))
(def typed (atom []))
(def st (atom nil))
(def calls (atom []))
(with-redefs [d/answer-available? (fn [_ _] "resolve the conflict by taking theirs")
              d/type-raw! (fn [_ _ _ t] (swap! typed conj t))
              d/write-driver-state! (fn [_ _ s] (reset! st s))
              d/read-driver-state (fn [_ _] @st)
              d/seat! (fn [& args] (swap! calls conj (vec (drop 2 args))) {:exit 1 :out "" :err ""})
              d/escalate! (fn [_ t r] (swap! typed conj (str "ESCALATE " t " " r)))
              d/turn-idle? (fn [_] true)
              agent-runtime-inject/capture-pane-text (fn [& _] "")]
  (let [dir (str (babashka.fs/create-temp-dir))
        _ (babashka.process/shell {:dir dir :out :string} "git init -q")
        ctx {:project-root dir :checkout dir :role "coder" :seat-id "coder" :agent "aider" :socket "s" :session "x"}]
    (doseq [held [{:escalated true :ticket "mail" :reason "merge conflict"}
                  {:escalated true :ticket "BL-9" :reason "merge conflict"}]]
      (reset! typed []) (reset! calls [])
      (reset! st held)
      (dotimes [i 3] (try (d/drive-tick! ctx) (catch Exception e (swap! typed conj (str "EXCEPTION " (.getMessage e))))))
      (println "HELD" (:ticket held))
      (doseq [t @typed] (println "  TYPED:" t))
      (println "  SEAT CALLS:" @calls)
      (println "  FINAL:" @st))
    (babashka.fs/delete-tree dir)))
```

Both were run against 537f4f8cf0 (the lib as the parcel delivered it). They load
`swarmforge/scripts/local_parcel_driver_lib.bb` from the checkout, so run them on a
tree that has the parcel's lib (e.g. after reverting 01c5595b84 in your rebuild).
