# BL-1317 — cleaner pass, 2026-09-02

Reviewed coder commit `00cc6872eb` (final: "rework on the required_wiring
amendment — Adapt is Babashka-only") merged into this worktree.

## Checklist run
- Coverage: reviewed `seat_difficulty_lib.bb::adapt-effort-decision` (pure
  decision) and `handoff_lib.bb::record-effort-adapt!` (IO edge) —
  documented, single-responsibility, fails closed on unknown prior effort /
  unknown signal / lever-less backend.
- DRY: the adapt ladder and clean-streak default are defined once
  (`seat_difficulty_lib.bb`) and read out via `bb -e` in
  `bl1317AdaptEffortInvariants.property.test.js` rather than mirrored in JS
  (BL-897 constant-mirroring rule already applied by the coder). No TS copy
  remains — `effortDialAdapt.ts` was deleted as unwired dead code, verified
  gone from `extension/src/tools/`.
- Structure/CRAP: `adapt-effort-decision` is a single pure function with one
  `case` dispatch on signal, no nested conditionals beyond the necessary
  climb/drop branches. `active-ticket-mutation-cost` is correctly factored
  into `handoff_lib.bb` (shared by BL-1316's claim-time apply and BL-1317's
  Adapt baseline) rather than duplicated per caller — exactly the "one
  policy, one place" the docstring calls out.
- Mutation-site-count: only TS production file touched is the deletion of
  `effortDialAdapt.ts` (net negative); no new/changed compiled `out/**/*.js`
  file to count. `.bb` files are unit-test-gated only per BL-472 (no
  mutation/CRAP/DRY wired for Babashka).
- Wiring: `required_wiring` in `backlog/active/BL-1317-...yaml` names three
  anchors, all present —
  `seat_difficulty_lib.bb::adapt-effort-decision`,
  `handoff_lib.bb::record-effort-adapt!`,
  `done_with_current_task.bb::record-effort-adapt!` (pre-existing live
  caller, confirmed at line 123).

## Tests run
- `bb swarmforge/scripts/test/seat_difficulty_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/handoff_lib_test_runner.bb` — ALL TESTS PASSED.
- `bash swarmforge/scripts/test/test_bl1317_effort_ladder_parity.sh` — ALL PASS.
- `npx vitest run test/bl1317AdaptSingleApplierPerLanguage.test.js
  test/effortDial.test.js` — 14/14 passed.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1317AdaptEffortInvariants.property.test.js` — 4/4 passed
  (invariant 1 and invariant 2, both non-vacuity checks included).

## Verdict
NONE. No defect found; no cleanup change needed. The coder's rework already
meets the cleaner's own bar (DRY constants, single IO edge, documented
invariants, non-vacuous property + unit coverage). Forwarding to architect
unchanged.
