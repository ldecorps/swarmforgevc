# BL-1704 — documenter review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

Second documenter pass. The "Ollama is stopped by the swarm" section
(pass 1, `642de5dca1`) had been reverted from the tree as collateral of
the QA-bounce-revert restoration sweep (same class as the BL-1698 D6
reversion this batch's hardender pass flagged); re-added it, updated for
D1's own fix (`ff00492f27`, "stale-record stop log doesn't say which
case"): the stop-log bullet now says the log names which of the two
stale-record cases (pid gone, or pid now belongs to something else)
applied, matching `ollama_ancillary_lib.sh`'s current two distinct log
lines (`ollama_ancillary_stop_swarm_owned`).

Also rebuilt the merge commit that brought this parcel in
(`f9d9b30e11` → `dd5737a4d1`, same tree, real parents) after the BL-1576
merge-drop guard refused the original resolution for dropping 1 line of
my own prior uncontested content in `docs/how-to/BL-1052-local-model-seat-launch.md`
— a reordering artifact of re-adding the reverted BL-1704 section, not a
substance loss; fixed by matching the accepted documenter tip's section
order exactly.

Checks: BL-1704 feature 5/5 (`specs/pipeline/cli.js`); `npm run compile`
clean.

By documenter.
