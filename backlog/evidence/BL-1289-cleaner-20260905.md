# BL-1289 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Confirmed the guard reports zero violations directly:
  `npx vitest run test/tempDirTrapGuard.test.js` → 4/4 pass. The ticket's
  spec-time count of 21 had drifted to 27 by pickup (six more call sites
  landed in between); the coder correctly treated the guard itself as the
  acceptance contract and fixed all 27, not just the 21 named nine days
  earlier.
- Spot-checked the trickiest fix by hand: `bl1421_one_standing_surfacing_acceptance_runner.bb`,
  which is invoked once per Gherkin step and deliberately reuses the same
  `dir` across invocations. The `created-fresh?`/`handed-off?` guard is
  correct — the shutdown hook only exists on the invocation that created
  the dir, and is a no-op once the JSON handoff has printed; re-ran the
  BL-1421 acceptance feature twice (6/6 both times) to confirm the reuse
  behavior is unaffected.
- Ran the full unit lane: 13 files failed / 591 passed (604) —
  `tempDirTrapGuard.test.js` is NOT among the 13, matching the coder's
  claim exactly; every failure is either an already-registered owned
  ticket (BL-1212/BL-1229/BL-1263/BL-1221) or the pre-existing
  `deps.checkOrphanedAuthoredDocs` upstream regression family — none touch
  temp roots or fixture cleanup.
- Ran the acceptance feature (4/4) and the regression suite for a real
  `fixture_isolation.sh` consumer (`test_bl1370_worktree_strays.sh`, ALL
  PASS) — confirms the new `register_tmp_dir` call added on top of the
  file's own lock/reap/bound machinery does not disturb it.
- `jscpd` found 3 clones (2.01% duplicated lines) confined entirely to
  `test_shell_test_discovery.sh`'s own pre-existing parallel test-scenario
  boilerplate — confirmed by diff that this ticket's edit to that file is
  exactly two lines (a `source` and a `register_tmp_dir` call inside
  `mk_repo()`), touching none of the duplicated blocks. Not introduced by
  this parcel.
- Shell/Babashka have no mutation/CRAP/DRY tooling wired (BL-472 deferred)
  — this pass is gated by the unit/property/acceptance suites only, per
  constitution; the ticket's own `stage_skip_reasons` correctly skips
  hardener/documenter for the same reason.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `cd extension && npx vitest run test/tempDirTrapGuard.test.js` | 4/4, zero violations |
| Full unit lane | 13 failed / 591 passed (604) — same 13 files as coder's claim, `tempDirTrapGuard` not among them |
| `node specs/pipeline/cli.js specs/features/BL-1289-...feature` | 4/4 scenario runs |
| `node specs/pipeline/cli.js specs/features/BL-1421-...feature` (regression, run twice) | 6/6, 6/6 |
| `bash swarmforge/scripts/test/test_bl1370_worktree_strays.sh` (regression) | ALL PASS |
| `bb swarmforge/scripts/test/post_qa_branch_sweep_lib_test_runner.bb` (regression) | ALL PASS |
| `bb swarmforge/scripts/test/swarm_shift_lib_test_runner.bb` (regression) | ALL TESTS PASSED |
| `bb swarmforge/scripts/test/master_checkout_integrity_lib_test_runner.bb` (regression) | ALL TESTS PASSED |
| `bl1289TempRootAlwaysCleanedSteps.js::registerSteps` exists | yes (required_wiring) |

No fix required. Forwarding unchanged to architect.
