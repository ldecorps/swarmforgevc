# BL-1682 - send-back for a spec defect: which briefing text lands (2026-09-24, specifier)

**Inbound.** QA spec-gap note 003144 (13:18Z) and QA evidence a7de7946c6
(`backlog/evidence/BL-1682-spec-gap-briefing-version-QA-20260924.md`), on
parcel f70e54d39d. Every code gate passed; the parcel was built correctly
from the 2026-09-21 draft the ticket named.

**Defect: the spec's.** The ticket said the briefing "lands with the parcel
as the operator wrote it" and pointed at the 09-21 patch. The operator has
since replaced the live file on the master checkout (2026-09-24 07:44 local,
1049 bytes, sha256 8ff4ad6a7e615b71344547fd80a4f958c4edac3e7e97cb0fc93d07d0437fe55c)
with a compressed rewrite for the CPU-bound IQ3 27B seat.

**Ruling.** "As the operator wrote it" means the operator's latest text: the
09-24 trim lands. Its exact bytes are in
`backlog/evidence/BL-1682-operator-briefing-20260924.md`. The ticket is
amended (description, scope, qa_e2e step 3 now a sha256 check); the feature
file does not pin the text and is unchanged.

**Rebuild route.** QA sends the parcel back to the coder, who replaces the
parcel's `docs/reference/local-model-briefing.md` with the evidence copy.
The documenter re-checks facts: the trim spells the role "hardener" where the
repo's id is `hardender`; report it, the wording is the operator's. QA then
re-runs the changed-file checks. The master precondition stands.

Recorded against the specifier (producing role of the defect), class
spec-gap.

By specifier.
