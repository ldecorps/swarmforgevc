# BL-1400 — documenter pass (2026-09-05)

## Scope reviewed

Re-read the ticket, the acceptance feature
(`specs/features/BL-1400-the-registration-guard-never-sees-a-nested-handler.feature`),
and the full chain of prior evidence (coder `BL-1400-coder-20260905.md`,
cleaner `BL-1400-cleaner-20260905.md`, architect `BL-1400-architect-20260905.md`,
hardener `BL-1400-hardener-pass-20260905.md`).

Change under review: `check-feature-handler-registration.ts`'s `readTree`
now lists `specs/pipeline/steps/` recursively, so a nested handler enters
`stepFiles` and the existing `isDiscovered` predicate can classify and
refuse it; `lib/` helpers reached by a discovered handler's `require` stay
reachable and are not reported as offenders.

## Documentation surfaces checked

- `docs/how-to/BL-1371-step-handlers-register-by-discovery.md` (lines 37-45):
  this how-to already ASSERTS that
  `check_feature_handler_registration.sh` "still refuses a handler file
  present but unreachable from the registry (a shape discovery cannot
  produce for a conforming file, but still possible for a misnamed/nested
  one)." That sentence was true of the *intent*, not the code, before this
  ticket landed (the subdirectory case was dead code per BL-1400's
  description). Now that BL-1400 has fixed `readTree`, the sentence is true
  of the code too — no wording change needed, the doc was written for the
  behavior this ticket delivers.
- `docs/reference/Specification.MD`: no prior entry described the guard's
  tree-walk being non-recursive as current behavior, so nothing there was
  stale. Added a new dated entry (commit `0c84ae8f54`, prior pass) recording
  the fix, per the standing convention of one entry per landed ticket.
- Registered diagrams (`architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`): none depict
  guard-internal tree-walk behavior at this granularity; none of their
  change-triggers fired.
- No README, guide, or command list references `readTree`'s recursion
  behavior.

## Verdict

NONE (beyond the Specification.MD entry already committed) — the one
surface that discusses this exact guarantee (BL-1371's how-to) already
described the fixed behavior correctly; it needed no edit. No other
documentation defect found.

By documenter.
