# BL-1685: every eager bridgeServer requirer loads the bridge graph inside its step — coder

## What changed

Fourteen step handlers (the ticket's own named census) moved their
`require('../../../extension/out/bridge/bridgeServer')` from module scope
to inside the function/step that actually starts a bridge — the same
`getStartBridge()` / `getCombineWithinEpicLiveItems()` lazy-accessor shape
BL-1658 used for the fifteen cursor-bridge handlers and BL-1630 for jsdom:

`bl1412SpecTreeTextFilterSteps.js`, `bl538ConsolePausedTicketPagerSteps.js`,
`bl572EpicReorderConsoleSteps.js`, `bl591EpicEtaSteps.js`,
`bl592SpecTreeOnLiveConsoleWithEpicTierSteps.js`,
`bl665ContextTelemetryProducerWiringSteps.js`,
`bl672EpicMakeTopPrioritySteps.js`, `bl673TopicMakeTopPrioritySteps.js`,
`bl674EpicDrilldownUiSteps.js`, `bl686EpicDrilldownSlugMatchSteps.js`,
`bl687EpicReorderIncludesActiveChildrenSteps.js`,
`bl766MiniAppLetsTalkRetiredSteps.js`, `bl905HideChildlessEpicsReorderSteps.js`,
`gh23ContextBudgetDashboardSteps.js`. Nothing else in any of the fourteen
changed — each still starts the same real bridge inside its own steps.

New `specs/pipeline/steps/bl1685BridgeServerRequirersLoadLazilySteps.js`
drives the pre-minted `specs/features/BL-1685-...feature` (BL-233): scenario
01 requires each of the fourteen alone in a fresh child process with the
loader intercept (reusing BL-1658's own `censusOneHandler` helper) and
checks both that `bridge/bridgeServer` never appears among the requests
observed and that the confirmed-alone cost is under the 400ms budget;
scenario 02 drives the ticket's own byte-safe grep census and asserts the
eager (column-0) require count is zero across every file it names.

## Two notes filed while building, both adjudicated by the specifier before this commit

1. **Spec-gap** (note 000053): scenario 02's fixed "exactly thirty-two"
   drifted to 34 as sibling tickets landed handler files between mint and
   build. Specifier amendment `2a1a890a06` turned it into a floor ("at
   least thirty-two"); this parcel's step handler is written to the
   amended text (`ctx.bl1685CensusFiles.length >= 32`), not the original.
2. **Unowned-red** (note 000054): a second, invisible-to-both-censuses
   population of eager `bridgeServer` requirers (`path.join(...)` shape and
   a module-scope object-literal require) found while verifying —
   `bl1634RejectedManifestOffersNoPageFromItSteps.js` and others, none of
   them in this ticket's fourteen. Specifier minted BL-1687 off this note
   and **re-pointed the `stepHandlerModuleLoadBudget.test.js` standing-red
   register row BL-1685 → BL-1687** (`unowned-red-second-bridgeserver-population-adjudication-specifier-20260921.md`).
   Consequence for THIS parcel: the guard test can still name a violation
   after this lands (`bl1634...`, borderline 259-768ms depending on host
   load) — that violation belongs to BL-1687's row, not this ticket's; none
   of the fourteen this ticket owns is ever named.

Merged `main` before this commit to pick up both amendments
(`2a1a890a06`, `b338d79861`) — my local step handler and the specifier's
amended feature text now agree.

## Invariant (BL-654)

Declared: "No step handler under specs/pipeline/steps requires
extension/out/bridge/bridgeServer at module scope... requiring any handler
alone in a fresh process never loads it." Same disposition BL-1658 used for
its own materially identical invariant: this is a finite, named 14-handler
enumeration pinned by the ticket itself (BL-1445 census), not a space to
fuzz — a fast-check property adds nothing over the acceptance feature's own
exhaustive real-census Scenario Outline (scenario 01, all fourteen named,
each required alone in a fresh child process against the real tree), which
is already this invariant's coder-authored executable test. No separate
`*.property.test.js` file added.

## Verified

- `cd extension && npx vitest run test/stepHandlerModuleLoadBudget.test.js`:
  green (8/8), none of the fourteen named, allowlist unchanged.
- `node specs/pipeline/cli.js specs/features/BL-1685-...feature`: 15/15 ok.
- Byte-safe grep: `for f in $(git grep -l 'bridge/bridgeServer' -- 'specs/pipeline/steps/*Steps.js'); do /usr/bin/grep -c '^const .*require(.*bridge/bridgeServer' "$f"; done | grep -vc '^0$'` → 0.
- `npm test` (extension, full unit lane): 10847/10848 passed; the one
  failure is `bl1634RejectedManifestOffersNoPageFromItSteps.js` over the
  stepHandlerModuleLoadBudget guard — owned by BL-1687 (register row
  re-pointed above), unrelated to this diff (confirmed: file untouched by
  this parcel; borderline/flaky, green on a second run same session).
- `npm run test:properties` (extension, full property lane, once): 444/444
  files, 1292/1292 tests green; the only unhandled errors are the two
  allowlisted BL-871 `[vitest-worker]: Timeout calling "onTaskUpdate"`.

By coder.
