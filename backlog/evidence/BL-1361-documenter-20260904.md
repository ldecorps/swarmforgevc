# BL-1361 — documenter pass (2026-09-04)

## Received
`903d26717e` (hardener; per `backlog/evidence/BL-1361-hardener-pass-20260904.md`).
Re-verified `swarmforge/scripts/handoffd.bb` still loads clean after this
merge (`bb -e '(load-file ...)'` → usage message, one definition of
`cron-heartbeat-sweep!`, zero `(read-json ` matches) — this ticket also
touches `handoffd.bb`; no regression of the BL-1392 crash bug.

## What changed, operator-visible
BL-668's "surfaced to its role" contract was a log line only —
`record-surface!` wrote the sweep's state file and `log!`, nothing ever
reached the role. 125 `post-qa-branch-sweep-surfaced` events against 3
`post-qa-branch-sweep-settled` on 2026-09-03; on 2026-08-27T16:41:56Z all
six roles surfaced in one pass and nobody heard about it. `handoffd.bb`
now sends a note through the daemon's existing `swarm_handoff.bb` path
(`post-qa-branch-sweep-tell!`) — every reason is told, only
`:dirty-worktree` wakes immediately (human ruling 2026-09-04), the other
two reasons ride `SWARMFORGE_SKIP_SYNC_INJECT` deferred. A `tell!` failure
is logged (`post-qa-branch-sweep-tell-failed`) and the sweep continues for
every other role.

## Doc-domain review
`docs/how-to/BL-668-post-qa-deterministic-branch-sweep.md` is the
established, directly-relevant home (BL-1361 is a regression against
BL-668's own invariant 1).
- Added a new section ("A surfaced role is told, not just logged
  (BL-1361)") covering the send trigger, message format/cap, wake-vs-defer
  rule, and the one-unreachable-mailbox-never-withholds-the-rest guarantee.
- Extended Verify and Siblings sections with BL-1361's test script,
  feature file, and epic siblings (BL-1360, BL-667).
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-668 summary line to name
  BL-1361 and the tell/wake behavior.
- Diagram registry: `docs/diagrams/swarm-flow.mmd` DOES depict this — the
  sweep's post-land fast-forward/surface edges are already drawn, and the
  new send is a genuine new handoff-direction edge (daemon sweep → role,
  distinct from QA's hand-composed merge-up note). Updated the caption
  comment and added `sweep tell` edges from MAIN to each pipeline role.
  Verified the diagram file stays in `render-briefing-diagrams.ts`'s
  `DIAGRAM_FILES` allowlist (unchanged, still 4 entries, no orphan).
- Ran `bash swarmforge/scripts/test/test_bl1361_sweep_tells_roles.sh` —
  ALL PASS (9/9), local verification per role prompt.

## Verdict
Doc content extended in the owning how-to page plus the diagram it
depicts, not NONE. Forwarding to QA.
