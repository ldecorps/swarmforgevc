# BL-1491 — QA review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Reviewed documenter's `8836d3b121` (hardener merge `10ba34e407`, architect
merge `248400bfdd`, cleaner merge `7bf81e6818` — all confirmed ancestors of
the reviewed commit). Before verifying, synced this worktree to
`origin/main` (`45c5c20b25`): BL-1490's tip-pure replay `607e7fc56d` had
already landed and the coordinator had already closed it to `done/`, so the
merge picked up that state cleanly (one content conflict, resolved by
keeping origin's closed/done shape and re-adding the local
`abandoned_commits` annotation that had never made it to `main`; diffed the
merge against both parents — only that 12-line re-add, nothing else moved).

- `pgrep -fl 'node --test|stryker'` clean before and after (matched PIDs
  already dead).
- `qa-sibling-check.js status --ticket BL-1491` → `VERIFY BL-1491` (no open
  deferral). No prior bounce for BL-1491 in the master checkout's
  `.swarmforge/bounces/`.
- Acceptance: `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1491-a-supervisor-halt-records-itself-on-the-ledgers-a-stop-already-uses.feature`
  → 4/4 scenarios pass.
- `bb swarmforge/scripts/test/bl1491_halt_records_itself_property_runner.bb`
  → `ALL PROPERTIES HOLD` (150 runs each).
- `bash swarmforge/scripts/test/test_daemon_alarm_lib.sh` → `ALL PASS`
  (includes BL-1491 record-halt-01/02/03: write-ahead ordering, a throwing
  adapter never blocking the halt, and an absent adapter as a silent no-op).
- `bash swarmforge/scripts/test/test_availability_ledger_lib.sh` → `ALL
  PASS` (qa_e2e_procedure item 1's second named runner).
- `bash swarmforge/scripts/test/test_handoffd_supervisor.sh` → case 01
  ("hard-stop never killed any tmux session") fails. This is the standing
  red `BL-1498` (`backlog/standing-reds.tsv`: zshenv shadows the fixture
  tmux fake ahead of the real one, so `kill-session` is never logged to the
  fixture's tmux log), owned and registered — confirmed via
  `bb swarmforge/scripts/standing_red_register_cli.bb .` (`"unowned":[]`,
  count 29, row names `test_handoffd_supervisor.sh` → `BL-1498`). Not this
  parcel's defect: the parcel's own new block (lines 397-422 of that file)
  deliberately runs with no tmux socket file at all specifically to isolate
  itself from this red (its own comment says so), and both its new
  assertions passed. The architect's own pass already bisected this
  identically on the pre-parcel commit and recorded the same finding.
- `required_wiring`: `bl1491HaltRecordsItselfSteps.js` sits top-level in
  `specs/pipeline/steps/` and matches `index.js`'s `*Steps.js` discovery
  predicate (confirmed by reading the discovery code) — no explicit
  registration line needed. `handoffd_supervisor.bb`'s `write-kill-all-audit-row!`
  and `write-availability-stop-record!` are both wired into the injected
  `:record-halt!` adapter map (grepped both symbols to their call sites).
- Diff read against `constraints`/`invariants`: `record-halt!` is called
  before `halt-swarm!` in `alarm-and-halt!` (write-ahead, invariant 1),
  wrapped in `try/catch` so a write failure never blocks the halt
  (invariant 2, exercised by scenario 04). No third record format
  introduced — reuses `kill_pipeline_swarm.sh`'s exact `<iso> <text>` row
  shape and `availability_ledger_lib.sh`'s own `availability_record` via a
  bounded `sh!` subprocess, never `kill_pipeline_swarm.sh` itself (which
  would sweep inboxes and reset worktrees) and never a bb reimplementation
  of the record shape (avoids a BL-897 mirrored-literal need by
  construction). The failure log, alarm email and halt's kill behaviour are
  unchanged — no diff near `halt-swarm!`'s own body.
- Diagram currency: no registered diagram in `docs/diagrams/*.mmd` depicts
  supervisor-halt internals at this granularity — no change-trigger fired;
  documenter's own evidence agrees.
- Working tree clean after the pass (`git status --short` empty).

Approving and proceeding to merge-up/land/coordinator notification.
