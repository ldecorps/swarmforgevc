# BL-1198 — QA bounce — 20260828

## D1: acceptance step handler missing entirely

1. **Failing command:**
   `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1198-main-rematch-reset-must-attempt-push-before-discarding-local-ahead-commits.feature`
2. **Commit hash:** `19514695d75a0538b0dce00d038c1c449c6bf5fc` (QA's own merge
   of documenter's received tip `8241cfd158`, this session's
   `merge_and_process` commit for this parcel).
3. **First error excerpt:**
   ```
   Error: Scenario "A local-ahead commit that origin has not diverged from is
   pushed, not discarded": no step handler matched "Given local main holds
   one or more commits not yet present on origin/main"
   ...
   Error: Scenario "A local-ahead commit that collides with origin still
   falls back to today's reset recovery": no step handler matched "Given
   local main holds one or more commits not yet present on origin/main"
   ```
4. **Failure class:** `acceptance`.
5. **Expected vs observed:** Expected both Gherkin scenarios in the
   ticket's own declared `acceptance:` feature file to execute against a
   registered step handler and pass. Observed: `no step handler matched`
   for both scenarios — **no step-handler file for this ticket exists
   anywhere in the delivered work** (`find specs/pipeline/steps -iname
   "*1198*"` — empty; `git ls-tree -r documenter's tip` — empty;
   `specs/pipeline/steps/index.js` — no `1198` reference).

## Why this is a real gap, not a QA merge-resolution artifact

Confirmed the missing file is not something QA's own selective-merge
declined by mistake: searched documenter's full received tip
(`8241cfd158`) for any `bl1198*Steps.js`-shaped path — none exists on that
branch at all. Every other hold/-family ticket processed this session
(BL-644, BL-751, BL-1188, BL-1189, BL-592, BL-1200) had its step handler
appear at or near the coder stage and get carried through the pipeline;
this one never did. Hardener's own evidence
(`backlog/evidence/BL-1198-hardener-pass-20260827.md`) confirms this: its
gate table lists unit/property/real-git-wiring results but **never
mentions acceptance, `run_acceptance.sh`, or a step handler at all** —
the acceptance gate was never run by any stage, not silently broken by one.

## Everything else checked — genuinely clean, not just unexamined

Completing the full inventory before this one-item bounce (Article 4.4 —
never first-failure-stop):

| Gate | Result |
|---|---|
| Ancestry (coder → architect → hardener) | CONFIRMED all three ancestors of received tip |
| `master_main_reconcile_lib_test_runner.bb` | ALL TESTS PASS (3 new `rematch-with-push-first!` cases, re-run independently) |
| `master_main_reconcile_lib_property_runner.bb` | ALL PROPERTIES HOLD, 500 runs — non-vacuity explicitly confirmed: "a mutant that always resets regardless of push success is flagged" |
| `test_swarm_heal_push_before_reset.sh` (new, real-git) | 4/4 PASS |
| Invariant ("never discarded without a push attempt first") | Directly exercised and mutation-confirmed by the property test above |
| Three call sites (handoffd.bb, swarm_heal.bb, post_hotfix_merge_origin.bb) | All confirmed wired to the shared `rematch-with-push-first!`, not three independent copies |
| Backlog-pool conflicts (BL-751/1200 done/, BL-1196 accidental-delete-then-restore) | Resolved correctly per this session's established pattern; verified post-resolution |

**The underlying behavior change itself is solid and well-verified** by
unit, property (with explicit mutation/non-vacuity proof), and real-git
wiring tests — this bounce is narrowly about the missing formal acceptance
layer, not a doubt about correctness.

## Routing

Owning stage is not clear-cut from any single stage's own admission (no
stage's evidence claims responsibility, none confesses skipping it), so
per Article 4.3's routing table this defaults to **coder** — the stage
that, for every sibling ticket this session, is where the step handler
first appeared. The fix is mechanical: a step handler wiring the
Background/Given/When/Then text above to the already-correct, already-
tested `rematch-with-push-first!` (fake `:push!`/`:reset!` adapters,
mirroring the property test's own pattern in
`master_main_reconcile_lib_property_runner.bb`) — no new production logic
needed, no known-good code is in question.

By QA.
