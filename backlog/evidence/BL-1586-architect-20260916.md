# BL-1586 — architect review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- `node extension/out/tools/dependency-gate.js` over all 15 migrated
  property test files + the new step handler: PASSED, no forbidden edges.
- `node extension/out/tools/co-change-report.js` over the same 15 files:
  no pair at or above the default threshold (min-frequency 3) - nothing
  flagged.
- Invariants review (BL-654): both declared invariants held. Confirmed
  invariant 2 (construction via `runsPerCell`/`assertReachFloor`)
  structurally through the acceptance feature - ran
  `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1586-sampled-reach-floors-sweep-2-of-6.feature`
  myself: 17/17 pass. Confirmed invariant 1 (no floor/budget/tolerance
  changed) by hand-auditing `git diff 0caa761d7f..HEAD -- extension/test |
  grep -E '(numRuns|assertReachFloor|>= *[0-9]|> *0|runsPerCell)'`: every
  removed `{ numRuns: N }` literal reappears as `runsPerCell(N * cells,
  cells)` (same total budget), every removed `assert.ok(count > 0, ...)`
  reappears as `assertReachFloor(..., 1, ...)` (same floor of 1) - no
  value moved.
  - Ran all 15 migrated files directly (`npx vitest run --config
    vitest.properties.config.mjs <files>`, two batches): 39/39 green,
    matching the coder's evidence table.
- bl1362 exclusion (the recipe's third branch): read the file myself -
  its four reach counters (`none`/`items`/`refused`/`collision`,
  `reached`) are unconditional checkpoints hit every `fc.assert` case,
  never a coverage measure over a drawn value that could miss a cell.
  Confirmed via the step handler
  (`specs/pipeline/steps/bl1586SampledReachFloorsSweep2Steps.js`):
  `NOT_A_FLOOR_FILES` makes the two structural steps a no-op for exactly
  this file rather than a blanket passthrough, and scenario 01 still
  requires it to exist and be named (no bare skip). Exclusion confirmed.
- No new property-shaped pure module was touched by this parcel (only
  the property test files themselves and a Gherkin step handler
  changed) - no property-testing pass action needed beyond the above.
- `git diff 0caa761d7f..HEAD --name-only`: only the 16 pinned test files
  minus bl1362 (unchanged, confirmed above), the new step handler, the
  feature, and backlog/evidence bookkeeping - matches the ticket's
  constraints (no production source, no helper file changed).
