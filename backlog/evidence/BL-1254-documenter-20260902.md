# BL-1254 — documenter pass, 2026-09-02

Reviewed commit `920c2bf0ad` (hardener clean sweep), merged into documenter
as (this branch's merge commit). Review-only BL-848 stamp-off parcel — no
hotfix source touched; this is the re-walk after the QA stand-down over
scenario 06's missing step handler (see ticket `notes:` 2026-08-30
correction). Scenarios 01-05 were already documented by an earlier
documenter pass (2026-08-30) that produced
`docs/how-to/BL-1254-swarm-stamp-expedite-no-verdict-chain.md`; that pass's
work stands unchanged except for the gap below.

## Doc sweep

- Re-read `docs/how-to/BL-1254-swarm-stamp-expedite-no-verdict-chain.md`
  against the current feature file
  (`specs/features/BL-1254-swarm-stamp-expedite-no-verdict-chain.feature`),
  per the standing rule that "doc already accurate" must be checked against
  git log/current state, not assumed. Found a real gap: the doc's
  "Resulting behaviour" section described only the no-verdict recovery path
  (scenarios 01-05) and said nothing about scenario 06 — the
  timeout-beats-missing-verdict ordering in `finalize-stage-result`, absorbed
  from retired BL-1259 and built by the coder in this re-walk
  (`36d8fd08b4`). Without it, a reader would not know a stage killed for
  overrun is classified `stage-timeout` and never re-invoked, as distinct
  from the no-verdict case which IS re-invoked.
- Fixed: added a "Timeout beats missing-verdict" section to the how-to
  (commit `a927be6018`, subject tagged BL-1254 — the file's own basename
  already names this ticket, no cross-ticket scope concern).
- `docs/index.md` already links this how-to (pre-existing entry, line ~237)
  — left as-is; its summary line still accurately covers the doc's scope
  (does not need scenario 06 named explicitly since the summary was already
  written at a coarser grain covering "one resulting state").
- Added a BL-1254 entry to `docs/how-to/BL-848-certify-an-operator-hotfix.md`'s
  "Related" list, following the established pattern (BL-1113, BL-1117,
  BL-1116, BL-1324 each self-list there) — commit `c08e74160a`, subject
  deliberately untagged (no ticket id in the subject) per the cross-ticket
  doc-edit rule: that file's own basename names BL-848, a different,
  already-shipped ticket, so a BL-1254-tagged subject touching it would trip
  `task_scope_gate_lib.bb`'s foreign-scope check.
- No diagram registered under `docs/diagrams/` depicts expedite's internal
  recovery/bounce decision logic — none of the four registered diagrams'
  change-triggers (architecture boundary, pipeline topology, handoff state
  machine, front-desk routing) fired. No diagram update owed.
- `docs/reference/Specification.MD`: not touched — this parcel reviews
  already-landed hotfixes and introduces no new user-visible command or
  setting of its own; ticket constraints forbid redescribing/redesigning the
  hotfix here.
- No RETIRED/deprecated behaviour involved — Article 3.6 deprecator gate not
  implicated.

## Verdict

D1 (documentation gap, self-fixed, not a send-back): how-to omitted
scenario 06's timeout-vs-missing-verdict distinction. FIXED in this pass —
not blamed on an earlier role, since scenario 06 itself only landed in this
re-walk.

No other defect found. Forwarding to QA.
