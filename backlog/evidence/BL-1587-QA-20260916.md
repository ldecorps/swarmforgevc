# BL-1587 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Ancestry: merged documenter `af9334ac78` into QA at `da7b675340`.
  `git merge-base --is-ancestor <sha> HEAD` OK for `af9334ac78`
  (documenter), `13589e9072` (hardender merge), `dddb08329b` (architect's
  post-bounce NONE) — this ticket's own stage merges are ancestors of the
  commit being approved, not merely of HEAD (BL-336 trap).
- Sibling status: `qa-sibling-check.js status --ticket BL-1587` -> `VERIFY
  BL-1587`, no open deferral.
- Straggler check: `pgrep -fl 'node --test|stryker|vitest'` clean before
  and after every run in this pass.
- Pinned-file scope: `git diff main...HEAD --name-only` names exactly the
  ticket YAML, 8 evidence files, the 10 migrated property test files,
  and the step handler — `bl1305FixtureAgentBinary.property.test.js`
  correctly absent (architect-confirmed "not a floor after all").
- Each of the 11 pinned files, `npx vitest run --config
  vitest.properties.config.mjs test/<file>` x3 standalone: all green,
  zero FAIL lines (bl1305 included, unchanged and still green).
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1587-sampled-reach-floors-sweep-3-of-6.feature` —
  12/12 pass.
- Floor audit: `git diff main...HEAD -- extension/test | grep -E
  '(numRuns|assertReachFloor|>= *[0-9]|> *0|runsPerCell)'` — 132 lines,
  matching both the architect's and hardener's independent counts;
  read line by line, every removed floor/budget literal reappears as
  the corresponding `assertReachFloor`/`runsPerCell` argument at the
  same value — no numeric value went down.
- Break-then-restore (own run, smallest-budget pinned file, bl1343
  9/cell): dropped `'none-sibling'` from `SHAPES` — failed with `reach
  floor: subtraction-shape nothingSubtracted drawn 0 < 1`; `git checkout
  --` restored the file; re-ran green.
- Standing red register: `bb swarmforge/scripts/standing_red_register_cli.bb .`
  -> `"unowned":[]`. Three of the pinned files (bl1308, bl1315, bl1343)
  carry an *owned* BL-1588 row (full-lane timeout under load, unrelated
  to this parcel's reach-floor fix) — owned, not blocking.
- Full unit suite (`npm test`): 620 files / 10527 tests, all green.
  Pre-existing per-file budget-overrun warnings on 7 files, none of
  them touched by this parcel — informational, not a failure.
- Full property suite (`npm run test:properties`): 408 files / 1201
  tests, all green; 3 unhandled errors, all the BL-871 allowlisted
  `[vitest-worker]: Timeout calling "onTaskUpdate"` — no other error.
- Wiring: acceptance's 12/12 pass is itself proof the step handler
  (`specs/pipeline/steps/bl1587SampledReachFloorsSweep3Steps.js::registerSteps`)
  is loaded and invoked by the real registry, not merely present.
- Prior bounce (D1, cleaner evidence path under `extension/backlog/`)
  already reverted by architect at `15566ca40d`; confirmed not present
  in the current tree (`git ls-tree -r HEAD --name-only --
  extension/backlog/` shows only the 6 pre-existing strays from other
  tickets, tracked under BL-1552 — none from this parcel).

By QA.
