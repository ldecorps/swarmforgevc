# BL-1327 — hardener pass (2026-09-03)

Real feature (descent-ladder proposal-only review), already reviewed by
cleaner and architect with no defect found. No `src/*.ts` files touched —
CRAP not applicable.

## Re-verification (independent, this pass)

- `bb swarmforge/scripts/test/descent_ladder_lib_test_runner.bb` — ALL PASS.
- `node specs/pipeline/cli.js specs/features/BL-1327-…feature` — 4/4 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1327DescentLadderInvariants` — 8 consecutive clean runs (3/3 tests each),
  on top of the architect's own 10.
- required_wiring: `install_descent_review_cron.sh` confirmed wired into
  `install_swarmforge_crons.sh` behind `SWARMFORGE_SKIP_DESCENT_REVIEW_CRON`,
  same pattern as the freshness/schedule siblings (checked the guard is at
  the DISPATCHER level, not inside the installer itself — matches).

## Reach-floor design check (the invariant-1 property test)

The architect flagged invariant 1's per-draw reach as probabilistic
(`fc.boolean()` at p=0.5 across 24 draws) rather than deterministic-by-
construction like invariants 2 and 3. Read the test directly rather than
trusting "safe margin": the property test asserts `reach.short > 0`,
`reach.met > 0`, and `reach.tripped > 0` as hard postconditions AFTER the
`fc.assert` calls complete — so an unlucky run that missed a category would
FAIL LOUDLY, not silently pass. This is not merely probabilistically safe,
it is structurally safe: the test cannot pass without having exercised all
three corners in that specific run. No gap.

## Hand-authored mutation sweep on `descent_ladder_lib.bb` (BL-638 fallback, Babashka has no wired mutation tool)

**Mutant 1 — streak boundary** (`(< streak needed)` → `(<= streak needed)`,
an off-by-one that would let a streak exactly at the threshold hold instead
of propose): **killed** — 11 failures, cascading through every proposal
scenario. Restored; empty diff.

**Mutant 2 — guard-trip fallback order** (`(or (:effort last-known-good)
effort)` → `(or effort (:effort last-known-good))`, and the model
equivalent — this always prefers the CURRENT notch over the known-good one
when both are present, defeating invariant 2's whole point): **killed** — 1
failure, exactly the "returns to the last known-good notch" scenario.
Restored; empty diff.

**Mutant 3 — smaller-model-start-effort constant** (`"high"` → `"low"`,
reintroducing the exact defect invariant 3 exists to prevent — confounding
a model change with an effort drop in the same notch): **killed** — 1
failure. Restored; empty diff.

All three hand-authored mutants targeting the ticket's three declared
invariants are killed cleanly by the existing test suite. No test gap found.

## Fixture hygiene (own-domain check)

`bl1327DescentLadderProposalSteps.js` uses the age-guarded module-load
sweep (BL-971 class, same pattern as BL-1306/1320/1323/1332/1343 this
session) and all four `rmSync` cleanup calls are wrapped in `try/finally`.
No `bl1327-*` fixture directories left in `/tmp` after this pass's runs; no
orphaned test processes.

## Conclusion

No test gap found. Forwarding to documenter.

By hardender.
