# BL-1012 QA approval, 2026-08-21

**Reviewer**: QA. **Reviewed at**: documenter tip `5ab9fe87f2`, merged into
QA. Ancestry confirmed: `git diff` against both merge parents shows only
additive content on each side, no overlap, nothing reverted.

## Judgement call, flagged up front

`qa_e2e_procedure` step 7 asks to watch
`ls .swarmforge/daemon/handoffd.log.* | wc -l` on the live Mac across a real
hour under normal load and confirm the rotation rate drops. This is an
hour-long live-host observation, not something to run synchronously inside
a QA pass. Not treating it as a blocker: the mechanism it would observe
emergently (fewer restarts -> fewer rotations) is already proven directly
and exhaustively at the unit/property/acceptance level below - every
invariant the ticket declares is independently gated, not merely inferred
from a log-count trend. **Recommend**: the operator watches this over the
next hour of normal load per the ticket's own step 7, as a confirming
observation, not a re-verification.

## Verification order (Article 4.4, qa_e2e_procedure)

1. **Read the diff.** `contention_factor()` = load average / core count,
   integer-floored at 1, falling back to 1 on any unreadable/unparseable
   signal (never a longer window on bad data). `effective_threshold()` =
   base x factor, capped at `MAX_THRESHOLD` (600s default) - invariant 1.
   `process_daemon()`'s restart branch is scoped to the SENTINEL only
   (`age -eq 999999999`, i.e. absent/heartbeat-less log) AND
   `since < RESTART_GRACE`, never to a real measured age - invariant 2,
   confirmed by reading the code, not just the comment: a genuinely stale
   log inside the grace window still falls through to escalate/restart.
   Every incident record line includes `threshold=`, `effective_threshold=`,
   and `contention_factor=` - invariant 3.

2. **`required_wiring` verified by grep at the parcel commit**:
   `FRESHNESS_LOAD` (3 occurrences) and `FRESHNESS_RESTART_GRACE` (2
   occurrences) are real env seams in `daemon_log_freshness_check.sh`
   (documented in the script's own header, read via `${FRESHNESS_LOAD:-}` /
   `${FRESHNESS_RESTART_GRACE:-300}`); `bl1012` step handler required from
   `specs/pipeline/steps/index.js:558`.

3. **Independent test runs from my own worktree**, after merging the
   documenter's commit (clean merge, `git status` clean before/after):
   - `bash swarmforge/scripts/test/test_daemon_log_freshness.sh`: **ALL
     CHECKS PASSED** - full suite including every BL-1012-named check (factor
     1/4/20 threshold behavior, ceiling cap, grace-window suppression, the
     discriminating "a REAL stale age inside the grace window is still a
     violation" check that proves grace is not a blanket mute, and the
     attributable-record checks). Fixture `sleep 120` helper processes were
     all reaped by the harness itself - none left running after the run.
   - `npx vitest run --config vitest.properties.config.mjs
     bl1012FreshnessSelfInflictedIncidents`: **3/3 pass**, one property per
     declared invariant, generatively exercised.
   - `specs/pipeline/scripts/run_acceptance.sh
     specs/features/BL-1012-the-freshness-watchdog-stops-manufacturing-its-own-incidents.feature`:
     **9/9 scenarios pass** (covers qa_e2e_procedure steps 1-6 directly: the
     factor-1/4/ceiling threshold scenarios, the grace-window scenario, and
     the attributable-record scenario).

4. **No mutation/CRAP/DRY applies to this diff** - hardener's note confirms
   no JS/TS production files were touched (shell script + shell tests + one
   property test file), so Stryker/CRAP are genuinely out of scope, not
   deferred. Hardener's hand-mutation non-vacuity spot-check (widening the
   ceiling comparison, confirming the invariant-1 assertions then fail) is
   the documented degraded fallback for this shell surface.

5. **Documentation**: `docs/how-to/BL-675-daemon-log-freshness-watchdog.md`
   updated with the contention-relative threshold and grace-window
   behavior. `docs/reference/Specification.MD` updated.

6. **The known BL-373 hot-sync-orphan fixture**
   (`swarmforge/scripts/test/fixtures/`, untracked in every worktree,
   present before this ticket per its own notes) stays untracked and
   unswept, as the ticket's own notes explicitly direct ("do not delete it,
   it is not yours (or mine)").

7. **BL-532 sibling check**: `qa-sibling-check.js status --ticket BL-1012`
   returned `VERIFY BL-1012` (exit 0) - no open deferral.

8. **No orphaned test/mutation processes** before or after this pass.

## Verdict

PASS. Landing on `main` alongside BL-586 and BL-1017.
