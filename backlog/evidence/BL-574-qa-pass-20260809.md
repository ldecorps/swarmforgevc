# BL-574 QA pass — approved

## Re-entry context (BL-861)

BL-574 reached QA as a re-entry: it was previously stranded active with a
BL-532 sibling deferral BLOCKED_BY BL-681 and BL-762. Both blockers passed QA
and closed to `backlog/done/` on 2026-08-08, but the deferral's recorded
`--check` commands read `backlog/active/BL-681-...yaml` / a `backlog/active`
relative path — paths closing the blocker itself moves — so the literal
recorded checks were unrunnable (BL-861, specced 2026-08-09).

Verified independently before clearing:
- `git log --oneline main -- 'backlog/evidence/BL-681*' 'backlog/evidence/BL-762*'`
  shows both were QA-approved and closed (`a99a1a09`, `42531d66`, and the
  shared `BL-681-BL-762-qa-pass-20260808.md`).
- `backlog/done/BL-681-consolidation-never-drops-a-human-sentence.yaml` and
  `backlog/done/BL-762-finish-shift-bedtime-leaves-bubble-reachable.yaml`
  both exist with their acceptance feature files present on disk.
- BL-574's own code (`495d5df7`) and documentation (`c785a890f7`, "Document
  BL-574, BL-681, BL-762") are ancestors of `main`/HEAD.
- Specifier already corrected `assigned_to: specifier -> QA` on `main`
  (`34811b83`) explicitly for this re-entry.

Cleared both blockers via
`qa-sibling-check.js clear --ticket BL-574 --blocked-by BL-681 --commit c785a890f7`
and `--blocked-by BL-762 --commit c785a890f7`. `status` now reports
`VERIFY BL-574`.

## Commit verified

`c785a890f7` (documenter: "Document BL-574, BL-681, BL-762") — confirmed this
ticket's own coder/cleaner/architect/hardener merges are ancestors of it:
- `495d5df7` (coder, BL-574 implementation) is an ancestor.
- `4bf486f3` (cleaner), `1c10e03b` (architect), `b2cd357f` (hardener) — each
  names BL-574 explicitly in its evidence and is an ancestor of `c785a890f7`.

No prior BL-574 bounce evidence exists
(`git log --oneline main -- 'backlog/evidence/BL-574*'` shows only the four
clean-pass commits above, no bounce).

## Gates run

1. **Unit — pure lib**: `bash swarmforge/scripts/test/test_prompt_engine_lib.sh`
   → `ALL PASS` (10/10, including the pre-existing BL-546 Slice 1 checks).
2. **Property test**: `bb swarmforge/scripts/test/prompt_engine_fragment_cache_property_runner.bb`
   → `ALL PROPERTIES HOLD` (200 runs) — cache cold/warm/invalidated byte-identity.
3. **Acceptance**: `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-574-prompt-engine-slice2-fragments-adapters.feature`
   → 9/9 scenarios pass (fragment-assembly x4, adapter-selection x2,
   adapter-registration, cache-hit, cache-invalidation).
4. **Extension unit suite**: `npm test` → 7358/7368 pass, 413/418 files pass.
   The 10 failures are in 5 files, none touching prompt-engine/fragment/
   adapter/cache code:
   - `dependencyGateCliCleanAndViolations/ReportsAndScope/StorageGlobals`
     (6 failures): deterministic pre-existing environment gap — the pinned
     `depcruise` binary requires Node `^22||^24||>=26`; this worktree runs
     Node 20.20.2. Unrelated to BL-574, reproduces identically on any ticket.
   - `renderBriefingDiagramsCli`, `startBridgeHeadlessCli` (4 failures):
     timing-sensitive subprocess tests, flaked under severe host load
     (`uptime` load averages 33–46 during this run, consistent with the
     known Stryker-dry-run-under-load pattern). Different subsystems
     (diagram rendering CLI, bridge headless CLI), no code path shared with
     `prompt_engine_lib.bb`.
   No orphaned `node --test`/`stryker` processes before or after the run.

## Manual verification against ticket constraints

- Module boundary: primary implementation is `swarmforge/scripts/prompt_engine_lib.bb`
  (+ `prompt_engine_cli.bb`); no TypeScript mirror exists
  (`extension/src/swarm/promptEngine.ts` not present) — nothing to duplicate.
- Adapter lookup is registry-driven: `select-adapter` reads
  `adapter-registry-atom` (populated by `register-adapter!`); `compose`'s own
  `case style` dispatches only on the fixed 3-value `:bootstrap-text-style`
  capability enum, never on provider name. Confirmed by reading
  `prompt_engine_lib.bb:226-379` and by acceptance scenario
  `adapter-registration-03` (a provider registered after startup is selected
  with no compose-path edit).
- Constitution fragment unchanged across adapters: acceptance scenario
  `model-adapter-selection-02` pins constitution content identical for
  `claude`/`aider`.
- Cache key is content-hash (`sha256-hex`), not mtime/path — `invalidate-fragment`
  is explicit-only; confirmed in code and by the property test.
- `BL-546-prompt-engine-slices-2-3.feature.draft` correctly trimmed to Slice 3
  only (validation/inspect scenarios); its own header now documents the
  BL-574 handoff and points at BL-574's feature file.
- Wiring: `prompt_engine_lib.bb`'s `compose` is required and called from
  `agent_runtime_lib.bb` (the real launch-bootstrap caller), not merely
  unit-tested in isolation.

## Verdict

APPROVE. Merging up, landing on `main`, notifying coordinator for bookkeeping.
