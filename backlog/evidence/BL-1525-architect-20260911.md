# BL-1525 — architect review pass, 2026-09-11

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: node specs/pipeline/cli.js specs/features/BL-1103-one-shared-bounded-runner.feature
- **Commit hash**: afb4d930d3
- **First error excerpt**: Scenario "a command that outlives its bound is killed together with its children" failed at step "Given a bounded runner with a wall-clock bound and a command to run under it": lib must carry the load-bearing group-kill (0/3 scenarios pass; same failure on all three)
- **Failure class**: behavior
- **Expected vs observed**: 3/3 scenarios of BL-1103's shipped acceptance feature still pass after BL-1525 lands
- **Blamed role**: coder
- **Remediation pointer**: specs/pipeline/steps/bl1103OneSharedBoundedRunnerSteps.js line 85 asserts /"kill" "-KILL" "--"/.test(readFileSync(bounded_run_lib.bb)) - stale since BL-1525 (ruling A) correctly moved the group-kill into daemon_cycle_guard_lib.bb's :kill-mode :group, exactly as bounded_run_lib_test_runner.bb's own updated wiring check already expects. Point this step's group-kill assertion at daemon_cycle_guard_lib.bb (mirror the check already added to bounded_run_lib_test_runner.bb) instead of bounded_run_lib.bb's source text. Same file, line 140: assert.equal(r.exit, null, ...) on the timeout scenario is also stale - sh!'s bound-hit exit is now 124 (chokepoint convention), not null/nil (see bounded_run_lib.bb's own updated docstring and run-bounded!'s {:exit (:exit result) ...} in the coder's diff). Update the expectation to 124. Re-run node specs/pipeline/cli.js specs/features/BL-1103-one-shared-bounded-runner.feature and confirm 3/3 pass before re-forwarding.

By architect.
