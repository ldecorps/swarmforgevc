# BL-782 QA bounce — 20260826

**Commit checked:** `3d03df65c5` (Merge documenter `186ba40f13` for BL-782 QA gate)
**Task:** `BL-782-liveness-probes-scan-whole-process-table`
**Sibling check:** `VERIFY BL-782` (exit 0)
**Routing:** `coder` (earliest blamed — acceptance step handlers)

## Gates run (Article 4.4 — complete inventory)

| Gate | Result |
|------|--------|
| `npm run compile` | PASS |
| `node --test` `bl782LivenessProbesScopedToRoot.property.test.js` | 2/2 PASS |
| `test_expedite_cli.sh` | ALL PASS |
| `test_lifecycle_script_scope.sh` | PASS=15 FAIL=0 |
| `required_wiring` (`pids-matching` root needles; `bl782ExpediteLivenessScopeSteps` in `index.js`) | PASS |
| Stage evidence (coder/cleaner/architect/hardender/documenter) | present |
| APS BL-782 feature | **FAIL** — see D1 |
| Tip content vs `origin/main` | **FAIL** — see D2 |

## Defects

### D1 — acceptance hang after green (class: `acceptance`) — **blame: coder**

1. **Failing command:**
   ```bash
   specs/pipeline/scripts/run_acceptance.sh \
     specs/features/BL-782-liveness-probes-scan-whole-process-table.feature
   ```
   Also reproduces as:
   ```bash
   node --test --test-concurrency=1 \
     specs/pipeline/generated/expedite-cli-liveness-probes-match-only-processes-belonging-to-the-audited-root.generated.test.js
   ```
2. **Commit hash:** `3d03df65c5`
3. **First error excerpt:** All eight scenarios print ✔ (neighbour decoys, genuine survivors, `test_expedite_cli` ALL PASS, lifecycle regression, operator scope). The Node process then never exits. `pstree` shows unreaped children:
   ```
   sleep,429460 600
   sleep,429475 600
   … (nine `sleep 600` decoys still parented by the test worker)
   ```
   Default-concurrency first run stayed alive >7 minutes with the same decoy pattern and never printed the TAP summary footer. Serial re-run still hung after the last ✔.
4. **Failure class:** `acceptance`
5. **Expected vs observed:** Expected `run_acceptance.sh` / `node --test` to exit 0 after 8/8 green. Observed scenarios green then hang until decoy `sleep 600` processes are killed externally — Node waits on unreaped children from `spawnDecoy`.

**Remediation:** `specs/pipeline/steps/bl782ExpediteLivenessScopeSteps.js` — `spawnDecoy` / `reapDecoys`. Decoy children must be reaped in an `after`/`finally` for every scenario (including outlines), and/or `child.unref()` so the runner can exit. `stdio: 'ignore'` alone is not enough while the handles stay referenced.

### D2 — mutation caches hitchhiking on tip (class: `behavior`) — **blame: hardender**

1. **Failing command:** `git diff --name-only origin/main...HEAD | grep '^mutations/'` (and `base/`, `build/acceptance-mutation-bl669/`)
2. **Commit hash:** `3d03df65c5` (entered lineage at hardener merge `44d554110`)
3. **First error excerpt:** `origin/main` has **0** paths under `mutations/`; this tip tracks **92** `mutations/**` files plus `base/feature.json` / `build/acceptance-mutation-bl669/` — generated Gherkin-mutation artifacts (engineering guardrail: never commit mutation caches).
4. **Failure class:** `behavior`
5. **Expected vs observed:** Expected BL-782 land tip to carry only ticket deliverables (BL-506). Observed out-of-scope mutation-cache tree that would land on `main` if the tip were pushed as-is.

**Remediation:** Drop `mutations/`, `base/feature.json`, and `build/acceptance-mutation-bl669/` from the parcel tip before re-forwarding; keep manifests in the feature file only.

## Inventory

D1 (coder), D2 (hardender). Bounce to **coder** (earliest). D2 travels with the inventory.

## Orphans

Reaped hung acceptance process groups and leftover `sleep 600` decoys after both runs.

By QA.
