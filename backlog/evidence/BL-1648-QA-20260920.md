# BL-1648 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

### Gates run

- `npm run compile`: clean.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1648-the-register-cli-takes-an-injected-date.feature`: 4/4 scenarios green.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1428-every-standing-red-names-an-open-owner.feature`: 7/7 scenarios green
  (scenario 01, the wall-clock time bomb this ticket exists to fix, now passes deterministically).
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1648 <commit>`: OK (required_wiring anchor present).
- `npm test` (extension unit lane): 632/632 files, 10802/10802 tests green.
- `npm run test:properties`: 427/427 files green, 1246/1246 tests green (no FATAL ERROR / heap crash — BL-1651's
  fix still holding); one `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled error, the BL-871
  allowlisted benign error, not a defect.

### Lineage / attribution

- `git merge-base --is-ancestor 2911b4f30d <landed commit>` (documenter merge) → ancestor: OK.
- Full BL-1648 commit chain (mint → coder → cleaner → architect → hardener → documenter) verified present
  by `git log --grep`.
- Standing-red register: `backlog/standing-reds.tsv` named BL-1648 as owner of the `acceptance` lane row for
  `specs/features/BL-1428-every-standing-red-names-an-open-owner.feature` — confirmed fixed (scenario 01
  green above) before approving; `land_step_cli.bb` reports `REGISTER_ROW_RETIRED` for that row on land.

### BL-1241 entangled tip

- The QA-branch tip (a977077aaf, after syncing `origin/main` in first — the branch had carried a stale
  `backlog/active/BL-1651-*.yaml` from before BL-1651's land-repoint was skipped mid-in_process-parcel)
  named 10 `ENTANGLED_SIBLING`s (BL-1634, BL-1636, BL-1637, BL-1639, BL-1642, BL-1645, BL-1647, BL-1649,
  BL-831, BL-875) via `land_step_cli.bb`. All 10 verified closed on `origin/main` (`backlog/done/` or
  `backlog/done/M8/`) — the known BL-1650 first-parent-walk artifact, not a real hold (2026-09-19 BL-1636
  adjudication recipe). The tip-pure replay's diff against `origin/main` was inspected directly and carries
  only BL-1648's own paths (evidence files, the register row retirement, the feature/handler/CLI changes).
  Landed via `land_main_publish.sh --land` (LOCK_ACQUIRED → push → LAND_PUBLISHED
  f5b285e24a97ecd483a8e40d032706b8c4d2c450 → LAND_REPOINT_SKIPPED, a parcel in_process, no action needed).
  `abandoned_commits: [a977077aaf]` recorded on the ticket (commit d9d3e1b346).
  A stale `land-main.publish.lock` directory (dead pid 24816, left from the prior BL-1651 land session) was
  cleared before locking — confirmed dead via `ps -p` before removal.

### Prior-stage evidence reviewed

architect NONE, cleaner NONE, hardener NONE (detail: confirmed the fix actually resolves BL-1428 scenario 01,
reviewed the property-test invariant coverage, manually verified two CLI refusal paths outside the ticket's
scoped test matrix), documenter NONE. Coder's own grep-audit evidence
(`BL-1648-coder-clock-bound-assertion-grep-20260919.md`) spot-checked 5 of ~135 `Date.now()`-matching files
for the broad, over-inclusive first grep, but ran the SECOND, precisely-shaped grep
(`assert.equal(..., <number>)` on an age/days field) exhaustively over all files and found no other instance
of BL-1428's pattern — judged sufficient, not a gap: the exhaustive grep covers the actual failure shape.
