# BL-1393 — CLEANER PASS, 2026-09-04

One defect found and fixed (D1, below); everything else checked clean.
Forwarding to architect.

## D1 — duplication in the new "done, no second briefing" exit paths (fixed)

`extension/src/quality/nightClosingCeremonyLive.ts`'s `startFrozen` grew two
BL-1393 exit branches (`workedAShift === false` and `briefingAlreadySent`)
that built the identical `done`-phase `LiveAdvance` return shape
(`{...idleState(obs.nightKey), phase: 'done', sequence, startedAtMs:
obs.nowMs, drainDeadlineMs: obs.nowMs, hardDeadlineMs: obs.hardDeadlineMs}`,
`actions`), differing only in what `sequence`/`actions` held. `jscpd`
flagged it (1 clone, 14 lines / 64 tokens, 1.9% of the file). Factored out
`doneAdvance(obs, sequence, actions)`; both call sites now build their own
`sequence`/`actions` and hand them to the helper. Re-ran `jscpd` after: 0
clones. Re-ran `nightClosingCeremonyLive.test.js` (11/11),
`nightClosingCeremonyRun.test.js` (5/5),
`bl1393OneCeremonyEverySleep.property.test.js` (3/3), and
`test_bl1393_one_ceremony_every_sleep.sh` (11/11) after the change — all
still green, confirming pure refactor, no behavior change.

## What was checked

- `required_wiring` anchors, read live at their call sites (not by grep
  alone):
  1. `swarmforge/scripts/finish_shift_lib.sh:74` —
     `finish_shift_run_closing_ceremony` now invokes
     `night-closing-ceremony-run.js --sleep-path finish-shift`; the old
     direct `closing-ceremony-run.js` call is gone (grepped: 0 hits), not
     re-shipped beside the new one.
  2. `extension/src/tools/night-closing-ceremony-run.ts:27,233,238` —
     imports and calls `runClosingCeremony` from
     `metrics/closingCeremonyRun.ts` as a step between drain and briefing.
  3. `specs/pipeline/steps/bl1393CeremonyOnEverySleepSteps.js` — matches
     the `*Steps.js` directory-walk discovery suffix (BL-1371).
- `swarmforge/scripts/test/test_bl1393_one_ceremony_every_sleep.sh`: 11/11
  PASS both before and after D1's fix, covering every declared invariant:
  freeze-before-lean-packet-before-briefing ordering, the daemon's own
  closure-window trigger still gates independently of `--sleep-path`, a
  no-work sleep records an explicit empty outcome and instructs no
  briefing, no restart path (`remote_bounce.sh`, `kill_all_swarm.sh`,
  expedite park) invokes the ceremony, the direct lean-CLI call is gone,
  and no crontab/schedule file changed.
- `bl1393OneCeremonyEverySleep.property.test.js`: 3/3 PASS (includes the
  invariant-3 regression the coder's commit message describes: a ceremony
  starting with the day already briefed used to short-circuit to a stop
  with NO packet and NO outcome — indistinguishable from a ceremony that
  never ran; now records its outcome).
- `nightClosingCeremonyLive.test.js` (11/11) and `nightClosingCeremonyRun.test.js`
  (5/5) — unit coverage on both modified TS files, unaffected by D1's
  refactor.
- BL-658's and BL-820's feature files: read both diffs directly — only the
  `Feature:` header and a `retires:`-scope explanatory comment changed
  (present tense corrected, "the night" → "the shift"/one-sequence
  language); no `Scenario` block touched, no step removed. BL-658's
  mutation-manifest comment block is byte-identical apart from context
  lines around the header — not hand-edited, left for the toolchain per
  the ticket's own instruction.
- `tsc -p ./` compiles clean both before and after D1's fix.
- `jscpd` over the four touched/new non-generated files: 0 clones (after
  D1's fix; 1 clone, self-contained within `nightClosingCeremonyLive.ts`,
  before it).
- `mutation-site-count.js` on the two compiled TS files:
  `nightClosingCeremonyLive.ts` 237 sites, `night-closing-ceremony-run.ts`
  359 sites — both `over` the 100-site soft threshold. Reviewed and
  declined to split: this ticket's entire point is that the ceremony is
  now ONE ordered sequence (freeze → drain → lean-packet → briefing →
  stop) rather than two mechanisms: splitting the sequence file back apart
  would work directly against the design intent BL-1393 exists to
  establish. Same reasoning as every prior "cohesive single-feature
  module" call this session (e.g. BL-1366).
- No prior bounce history for BL-1393 (`git log --all --grep="BL-1393"`
  shows mint → approve → queue-jump (priority reorder, not a bounce) →
  promote → topic → coder → cleaner, no adjudicate-again entries).
  `main`/`origin/main` in sync (`git rev-list --left-right --count` → `0
  0`).
- Noted, not acted on: a later coder commit
  (`2ecafa89a3`, "record the one sleep path this repository cannot
  reach") exists on the coder's branch past the commit
  (`49c9a3298b`) this handoff named — an evidence-only addendum
  explaining that `night-stop.sh` lives outside this checkout so the
  parcel could not re-point it. Outside the commit this handoff carried;
  left for the coder's own next forward rather than reached for.

No architecture concerns: the sequence lives in one place
(`night-closing-ceremony-run.ts`), the pure decision logic stays separate
from the impure runner (`nightClosingCeremonyLive.ts`'s existing
separation, carried), and `finish_shift_lib.sh` drives it through the one
CLI entry point rather than duplicating any of the sequence's logic in
shell.

By cleaner.
