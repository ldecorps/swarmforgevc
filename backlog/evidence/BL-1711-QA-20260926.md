# BL-1711 — QA review pass, 2026-09-26

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: ps -eo args= | grep -E "ollama.*serve"   (the exact pipeline of ollama_ancillary_any_ollama_serve_alive, swarmforge/scripts/ollama_ancillary_lib.sh:188)
- **Commit hash**: 6240aef031
- **First error excerpt**: Output includes the pipeline's own grep process: "ugrep -G ... -E ollama.*serve" (captured 3 of 3 runs). The regex matches its own argv, so the function returns true on any host whether or not an ollama serve exists. For an external record (BL-1703 writes "pid": null) ollama_ancillary_restart_if_crashed takes the elif branch, sees alive, removes the marker and returns 0 every tick: a crashed external server is never restarted. Also matches any unrelated ollama serve on the host (e.g. a second swarm). Scenario 01 external row writes the record WITH ctx.serverPid, so the real pid-less crash path is never exercised; scenario 02b covers only alive-but-silent.
- **Failure class**: behavior
- **Expected vs observed**: approval_context FIRM: a crashed external server is restarted too / observed: with a pid-less external record the any-alive check is always true (self-match), so no restart ever happens
- **Blamed role**: coder
- **Remediation pointer**: ollama_ancillary_lib.sh ollama_ancillary_any_ollama_serve_alive: exclude self-match (e.g. pgrep -f with a [o]llama bracket pattern, or match argv[0] basename == ollama and argv[1] == serve) and scope to the recorded endpoint port if possible; add a scenario-01 external row with NO recorded pid (writeRecord("external", null)) whose process has exited, asserting exactly one restart

By QA.

## Detail

Second pass, on the rework for the 2026-09-25 bounce (D1-D3). HEAD before
this evidence was f90edb0cc3's line; on every BL-1711 path it matches the
rework tip 5ea381ed5e byte for byte.

- D2 (fixed): the new `RESTART_FAILED` token and `handoffd.bb`'s rendered
  alert text now tell "one restart failed" apart from "restarts exhausted".
- D3 (fixed): the never-serves stand-in traps TERM and kills its `sleep`
  child. After the full run the only `sleep infinity` processes on the host
  are two tmux-spy helpers, 6h41m old; none were leaked.
- D1 fix: defective, as item D1 above. Only this item is blamed.

Gates (qa-gather, report at tmp/BL-1711-gather.json): no stragglers before
or after. Sibling check returned `RELEASABLE` (BL-1704 is closed). Register
has 6 rows, `unowned: []`. Wiring OK. `npm test` exit 0.
`npm run test:properties` exit 0 (only the allowlisted BL-871 onTaskUpdate
timeout). The BL-1711 acceptance feature passed 8 of 8, green because no
scenario runs the pid-less external crash.

Revert: D1 is in code the parcel added, so BL-1711's own paths are restored
to origin/main in the next commit (every hunk on the shared files
handoffd.bb, ollama_ancillary_lib.sh, BL-1052 how-to and architecture.mmd
checked as BL-1711-only; in suite-manifest.tsv only BL-1711's two rows are
removed, and BL-1700's row stays). When the rebuilt parcel returns, it
re-adds these paths whole.
