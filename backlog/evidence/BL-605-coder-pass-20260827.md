# BL-605 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Delivered

Pure `globalTokenConsumption.ts` rollup over per-role `TranscriptUsageRecord[]`:
cumulative window totals, per-bucket sums, incomplete marking when expected roles
lack data, and `trend.ts` re-export for the shared TrendedNumber framework.
Transcript usage is authoritative; ledger `tokens: null` records are ignored.

## Invariants (BL-654)

| invariant | encoding |
|---|---|
| Global totals sum transcriptUsage across roles | `globalTokenConsumption.property.test.js` P1 |
| Pure aggregator — no fs | acceptance scenario 02 + module has no read* calls |
| Missing role data → incomplete, not silent zero | P2 + acceptance scenario 03 |

Non-vacuous: P2 breaks if incomplete buckets report `totalTokens: 0`.

## required_wiring

All four anchors present and exercised by acceptance.

## Verification

| check | result |
|---|---|
| `globalTokenConsumption.test.js` | 7/7 |
| `globalTokenConsumption.property.test.js` | 3/3 |
| `run_acceptance.sh BL-605…feature` | **4/4** |

By coder.
