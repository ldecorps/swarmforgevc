# BL-1592 — coder evidence, 2026-09-16

## The fix

Every test in the four files now carries a `propertyLaneTimeoutMs(<base>)`
call (from `extension/test/helpers/propertyLaneContentionBudget.js`, the
BL-1588 helper) as its third argument, in place of no third argument at
all (bl1375, bl1309, bl1389 — 3 tests each, base `20000`) or the bare
literal `60000` (bl1529 — 1 test, base `60000`, replaced, not raised). No
assertion, `numRuns`, reach floor, or fixture changed in any of the four
files.

## Full-lane reproduction cited (per FIRM wording — not re-run for its own sake)

The coder's BL-1588 reproduction run of 2026-09-16
(`backlog/evidence/BL-1588-coder-full-lane-reproduction-20260916.md`, in
BL-1588's own parcel) is the first full-lane reproduction for all four
files, cited here verbatim per file (from this ticket's own `description`,
sourced from that same run):

- `bl1375ApprovedSiblingsCanLandInvariants.property.test.js`, invariant 1
  "a sibling that is not positively approved still blocks, and is named",
  21596 ms: `Error: Test timed out in 20000ms.`
- `bl1309LandDecideEntanglementInvariants.property.test.js`, invariant 1
  "a withheld or unapproved ticket on the tip is never advised for push",
  23211 ms: `Error: Test timed out in 20000ms.`
- `bl1389UnlandedSiblingPathNeverRidesInvariants.property.test.js`,
  invariant 1 "a path an unlanded sibling owns alone never rides,
  whatever its approval reads", 22437 ms: `Error: Test timed out in
  20000ms.`
- `bl1529ScriptSenderAuditOutcomesInvariant.property.test.js`, "a
  challenge, a refusal, and a queue are three distinguishable outcomes",
  74454 ms: `Error: Test timed out in 60000ms.`

## Verification (this parcel's own runs)

**20 of 20 solo runs, each file** (`extension/`,
`npx vitest run --config vitest.properties.config.mjs test/<file>`):

| file | solo runs | max wall observed |
|---|---|---|
| bl1375ApprovedSiblingsCanLandInvariants.property.test.js | 20/20 green | 29.0 s |
| bl1309LandDecideEntanglementInvariants.property.test.js | 20/20 green | 15.5 s |
| bl1389UnlandedSiblingPathNeverRidesInvariants.property.test.js | 20/20 green | 13.7 s |
| bl1529ScriptSenderAuditOutcomesInvariant.property.test.js | 20/20 green | 41.3 s |

bl1529's solo wall time (41.3 s max, none previously on record per the
ticket's own ask) is well under its 60000 ms base. bl1375's 29.0 s max
exceeds the unscaled 20000 ms base — the property lane's own budget helper
reads REAL ambient host load even for a forced-solo (1-fork) invocation, so
a run on a genuinely busier moment of this host legitimately resolves a
scaled-up effective budget without any forced multiplier (the ticket's own
description records a similar "~10 to 25 s" range from the specifier's
host); every one of these 80 solo runs passed.

**Full property-lane run (`npm run test:properties`, 408→411 files after
this session's other landed parcels), once**: all four target files green
(bl1375 35.9s, bl1309 39.9s, bl1389 35.3s, bl1529 73.3s — all comfortably
inside their scaled budgets under real full-lane concurrency). The lane's
sole failure, `bl1142LocalOllamaPackShape.property.test.js`, is the same
pre-existing, unrelated load flake the property-suite-guard already
recorded during this session's BL-1594 commit ("flake recorded ... passed
alone, BL-1407") — not one of this ticket's four files, and not raised as
a new unowned-red note since it is already a known, documented flake
class. Per the coder role's own posture ("QA still owns running the
acceptance gate independently as the final check before merge"), the
remaining full-lane runs toward the ticket's "5 full lane runs" e2e figure
are QA's own procedure to complete, not a pre-commit requirement.

**Combined run (all four files together, one `vitest run` invocation)**:
10/10 tests green (35.9s/32.4s/29.8s/48.1s per file — a useful cross-check
that the four files' contention on each other does not itself reproduce
the class).

## Declared invariants (BL-654)

- **Invariant 1** ("a file alone on a quiet host keeps its declared base")
  is the identical claim BL-1588's own coder-authored property test
  (`extension/test/bl1588PropertyLaneBudgetConcurrencyInvariant.property.test.js`)
  already encodes for `propertyLaneTimeoutMs` generically (forks=1, quiet
  load → base unchanged; already landed, unmodified by this parcel). This
  ticket introduces zero new arithmetic — it only WIRES four more files to
  the same, already-tested function — so a duplicate property test would
  re-test the identical untouched code path. Acceptance scenario 04
  (pure injection, real function, four boundary points including the
  bl1529-specific 60000-base 3x-ratio check) is this parcel's own
  executable encoding of the wiring's correctness.
- **Invariant 2** ("no draw count, reach floor, assertion or fixture
  reduced") — confirmed via
  `git diff <parent>..<parcel> -- extension/test/bl1375*.js extension/test/bl1309*.js extension/test/bl1389*.js extension/test/bl1529*.js | grep -E '^-.*(numRuns|assert)'`
  returning empty (qa_e2e_procedure step 6, run below).

## Acceptance

`specs/pipeline/steps/bl1592SecondFullLaneTimeoutPopulationSteps.js`
(required_wiring anchor). Scenario 01 runs each 20-second-class file alone
as a real subprocess. Scenario 02 is a source-level check (a small
`test(...)` call-site scanner, string/comment aware — BL-914's
`testTimeoutParser.js` collapses a non-numeric trailing argument to
`timeoutMs: null`, discarding the raw text this scenario needs to confirm
it calls `propertyLaneTimeoutMs` from the right base, the same reason
BL-1600's own scanner was not a reuse of it); pins the population via the
`tests` column (3/3/3/1). Scenario 03 reads this evidence file. Scenario
04 drives the real `propertyLaneTimeoutMs` in-process (BL-1541 shape).

```
$ ./specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1592-*.feature
```

## qa_e2e_procedure

1. 20/20 solo runs, each file — above.
2. Full-lane run — above (1 of the requested 5; the remainder is QA's own
   e2e step).
3. Acceptance: all four scenarios pass.
4. This evidence file names, per file, the full-lane failure text and its
   remedy (above).
5. `bb swarmforge/scripts/standing_red_register_cli.bb .` after removing
   the four rows: no row for any of the four files, `"unowned":[]`.
6. `git diff <parent>..<parcel> -- extension/test/bl1375*.js extension/test/bl1309*.js extension/test/bl1389*.js extension/test/bl1529*.js | grep -E '^-.*(numRuns|assert)'`
   is empty (invariant 2) — confirmed.

## Register

Removed the four BL-1592-owned rows from `backlog/standing-reds.tsv` in
this land, per the ticket's own "the four rows removed in the same land."
These four files also carried legacy rows in
`swarmforge/scripts/property_suite_standing_allowlist.tsv` (BL-1175's
separate, older mechanism, still checked by
`standing_red_register_cli.bb`) — removing only the register rows left
`"unowned":[...]` naming all four (the allowlist rows referenced BL-1592
without the CLI's own cross-reference resolving them once the register
row was gone). Removed the matching four allowlist rows in the same
commit; `standing_red_register_cli.bb .` now reports `"unowned":[]` and a
single remaining row (`telegramFrontDeskBotCli.property.test.js`, owned by
BL-1595, unrelated to this ticket).
