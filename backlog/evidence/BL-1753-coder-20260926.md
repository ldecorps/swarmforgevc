# BL-1753 — coder pass — 20260926

## Invariants (stated reason, no `*.property.test.js`)

`rotateDocumenter`'s new retry logic is a bounded, 2-branch state machine
(plain call succeeds; plain call refused, forced call succeeds or fails) -
not a generative-fuzzing target with a meaningful input space for
fast-check to explore. The three declared invariants are exhaustively
covered by example-based tests exercising the REAL exported function
against real fixtures, not mocks:

- Invariant 1 ("never creates a tmux session and never runs
  consult_spawn_cli.bb, on any pack"): `spawnConsultDocumenter` is deleted
  outright (census: `git grep -e spawnConsultDocumenter -- extension/src`
  finds nothing); `extension/test/nightClosingCeremonyRotateDocumenterFallback.test.js`'s
  three cases and the BL-1753 acceptance feature's four scenarios all
  assert no consult marker / no new tmux session across every reachable
  branch (success, refused-then-forced-success, refused-even-under-force,
  standing pack).
- Invariant 2 ("forces a rotate only after the plain rotate was refused,
  and a forced rotate never moves/completes/deletes the held parcel"):
  the unit test's exact call-log assertions
  (`['role=documenter force=', 'role=documenter force=1']` only on a
  refusal, `['role=documenter force=']` alone on success) prove the
  ordering; the acceptance feature's "the coder parcel is still in
  coder's in_process" step asserts the fixture's own parcel file survives
  byte-for-byte (rotate_to_role.sh/tmux never touch mailbox files by
  construction - there is no code path that could).
- Invariant 3 ("recorded closing sequence names rotate-documenter at most
  once, BL-658's sequence scenarios stay green"): BL-658's own feature
  (unchanged, unaffected by this parcel) still passes 10/10; the BL-1753
  acceptance feature's own sequence step additionally drives
  `advanceNightClosingCeremony` (the pure machine, untouched by this
  ticket) through the same two-tick "parked, resident holds a parcel"
  shape `nightClosingCeremonyLive.test.js`'s own "drain overrun parks and
  rotates" case already uses, and counts exactly one `rotate-documenter`
  entry.

## Non-vacuity

Ran the acceptance feature and the unit test against the pre-fix
`rotateDocumenter` (spawnConsultDocumenter fallback, no force retry):
scenario 2 ("a refused rotate is forced...") and the unit test's first
case both failed - the rotate-call log held only ONE entry (the refused
plain call), never a second, forced one, since old code fell through to
the consult spawn instead of retrying. Confirmed red, then green after
the rewrite.

## BL-1593 regression caught and fixed

BL-1593's own feature freezes `nightClosingCeremonyRotateDocumenterFallback.test.js`
at exactly one `mkTmpDir` call site and exactly 3 passing tests. My first
rewrite used 2 call sites and 4 tests (an extra "no rotate_to_role.sh at
all" case) - BL-1593 scenarios 02 and 04 went red. Folded the missing-
script case out (reusing the shared `makeFixture` helper for the
remaining 3 tests keeps the one-call-site, three-test shape); all 5
BL-1593 scenarios pass again.

## QA e2e procedure (all five checks run)

1. `node specs/pipeline/cli.js` on the BL-1753 feature: 4/4.
2. BL-658 (10/10), BL-1593 (5/5), BL-1641 (4/4) features: all green.
3. `npx vitest run nightClosingCeremony` under `extension/`: 60/60 across
   the five live/run/gate/fallback files; the three ceremony property
   files (bl1528, bl1640, bl1641) green alone under the property config.
4. `git grep -n spawnConsultDocumenter -- extension/src`: no hit.
5. Committed diff: Scope files (night-closing-ceremony-run.ts, the
   rewritten fallback test), the new step handler, and evidence only.

By coder.
