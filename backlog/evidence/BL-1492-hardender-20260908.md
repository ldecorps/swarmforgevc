# BL-1492 — hardender review pass, 2026-09-08

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: npx vitest run test/*Guard*.test.js (excl .property.)
- **Commit hash**: 26dd0b4ef0
- **First error excerpt**: tempDirTrapGuard: 2 bb runners flagged (create-temp-dir with no shutdown hook/try-finally)
- **Failure class**: test-coverage
- **Expected vs observed**: zero violations
- **Blamed role**: hardener
- **Remediation pointer**: added addShutdownHook to both runners, mirroring bl1492RestartInPlaceCli.bb

## D2

- **Failing command**: bash swarmforge/scripts/test/test_handoffd_supervisor.sh case 06 (standalone verify)
- **Commit hash**: 26dd0b4ef0
- **First error excerpt**: no test exercised check! itself reaching :restart with real dead-pid + real SUPERVISOR_START_DAEMON_CMD seam - all restart coverage called respond-to-verdict! directly or pinned budget to 0
- **Failure class**: test-coverage
- **Expected vs observed**: check! wiring to restart-daemon! proven, not just decide-response/respond-to-verdict! in isolation
- **Blamed role**: hardener
- **Remediation pointer**: added case 06; confirmed it kills a hand-planted mutant (check!.:else rewired to call alarm-and-halt! unconditionally) before restoring the original file

By hardender.

## Detail

Received `Merge architect b6e7098705 into hardender` (architect's second
pass, `b6e7098705`, evidence `BL-1492-architect-20260908-2.md`, NONE — D1
already resolved by coder's `a21f9b2233`, closed by architect's own P4).

Gates run:
- `bb swarmforge/scripts/test/handoffd_supervisor_restart_budget_test_runner.bb`
  — ALL TESTS PASS (unit, decide-response).
- `bb swarmforge/scripts/test/bl1492_restart_in_place_property_runner.bb`
  — ALL PROPERTIES HOLD, 150 runs/property, generator coverage confirmed
  (p1/p2 both verdicts+outcomes, p3 both base verdicts, p4 all 3 at-kinds).
- `node specs/pipeline/cli.js specs/features/BL-1492-*.feature` — 5/5 green,
  both before and after the two fixes below.
- `npx vitest run test/*Guard*.test.js` (excl `.property.`, 18 files) —
  found D1 (below), clean after fix (183/183).
- `bash swarmforge/scripts/test/test_handoffd_supervisor.sh` — cases
  01/03/04/recovered PASS; case 02 fails identically to unmodified `main`
  (already registered BL-1498, standing red — not this parcel's defect,
  confirmed via `grep -rl BL-1498 backlog/`); case 06 (new, D2 below)
  verified standalone since case 02's failure aborts the script under
  `set -e` before reaching it.
- No Gherkin mutation (BL-113): feature has no `Scenario Outline:` (grep
  confirmed), so this is `inapplicable` per BL-638, not a gate to run.
- CRAP/DRY: N/A — Babashka only (`mutation_cost: low`, BL-472, no tooling
  wired); this pass's mutation coverage is the hand-authored sweep already
  recorded in the property runner's own non-vacuity comments (coder/
  architect passes) plus the D2 mutant below (mine).
- `dependency-gate.js`/`co-change-report.js`: N/A this pass — no JS file
  touched (only `.bb`/`.sh`); architect's prior pass already covered the
  one JS step file clean.

D1 (temp-dir-trap): both new bb runners create `fs/create-temp-dir` with
only a trailing (happy-path-only) `delete-tree` — an exception between
creation and that line leaks the fixture root. Fixed by adding the same
`addShutdownHook` pattern `bl1492RestartInPlaceCli.bb` already carries.
Re-ran the guard clean (18/18 files, 183/183 tests) and re-ran both bb
runners green after the fix.

D2 (wiring gap, not a bounce — found and closed in this pass): every
existing test of the `:restart` leg calls `respond-to-verdict!` directly
(property runner P1/P2, the acceptance CLI) or drives `check!` with the
restart budget pinned to 0 (`test_handoffd_supervisor.sh` line 53, cases
01/02/04/05 — can only ever reach `:halt`). Nothing proved `check!`'s own
`:else` dispatch, with a real dead-pid observation via `evaluate-health`
and the real `SUPERVISOR_START_DAEMON_CMD` subprocess seam, actually
reaches `restart-daemon!`. Added case 06 (fresh fixture, dead pid, budget
overridden to 2 for that one invocation, a fake start-owner script). Killed
a hand-planted mutant to prove it is real: `check!`'s `:else` branch
temporarily rewired from `(respond-to-verdict! verdict status)` to
`(alarm-and-halt! verdict status)` — case 06 went red
("did not invoke the real start-daemon-owner seam"); restored the original
file (confirmed byte-identical via `git diff`), re-ran green. This obeys
"Never hand-mutate a source file a detached suite is still reading" — no
detached job was outstanding; the mutate/verify/restore cycle ran entirely
in the foreground.

Leftover `/tmp/bl1492-repro-verify.bb` and
`/tmp/bl1492-verify-boot-5987811931337849883` predate this pass (mtime
14:25, before this pass started at 14:32) — not created by me, left alone
per "never delete what you did not create" (likely the architect's own D1
hand-repro, per their evidence's "Hand repro confirmed" note).

No orphaned test/mutation processes: confirmed via `pgrep -fl 'node
--test|stryker'` before handoff — remaining `bb`/`bash` processes are other
concurrent role sessions on this host, not spawned by this pass.

Correctness: no further defect found. Forwarding to documenter.
