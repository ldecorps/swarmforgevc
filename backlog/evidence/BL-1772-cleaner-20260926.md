# BL-1772 — cleaner review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- This is a review-only stamp-off parcel for hotfix db4da5c573 (Article
  1.10-adjacent BL-848 posture): confirm/refute an already-landed fix, no
  in-parcel rewrite. FIRM constraint verified: `git diff main...HEAD
  --name-only` names only the promoted feature (+ deleted `.draft`), the
  ticket YAML's `acceptance:` pointer, the new step handler, and evidence.
  `land_main_publish.sh`, `QA.prompt`, `land_step_cli.bb`,
  `land_step_lib.bb`, and `test_bl1772_push_repoints.sh` are untouched —
  confirmed absent from the diff.
- Re-ran independently:
  - `node specs/pipeline/cli.js specs/features/BL-1772-...feature`: 4/4 ok
    (~5.4s) — scenario 01 (clean push re-points), 02's two Outline rows
    (dirty worktree / in_process parcel, both skip but still succeed), 03
    (LAND_STOPPED never re-points).
  - `bash swarmforge/scripts/test/test_bl1772_push_repoints.sh`: ALL PASS
    (14/14), including the live-repository safety checks (origin URL
    byte-identical after the suite; no live remote points into the fixture
    dir).
  - `npm run compile` (extension/): clean.
  - `npx vitest run test/stepHandlerTmpRootGuard.test.js
    test/bl1277UnscopedStepCollisionGuard.test.js
    test/bl968StepRegistryMaterializedTreeGuard.test.js`: 13/13 — the new
    handler trips none of the standing acceptance guards (materialized
    tree, unscoped-pattern collision, tmp-root discipline).
- Reviewed the new step handler: fixture construction mirrors the shell
  suite exactly (bare origin under one `trackedTmpRoot`, never a raw
  `fs.mkdtempSync`, per BL-1636/BL-1390); scenario 03's refusal case builds
  a synthetic 2-parent commit via `commit-tree` directly (never checked
  out), matching the shell suite's own construction. Drives the REAL
  `land_main_publish.sh --push`, no reimplementation of the re-point
  decision logic.
- Invariants: all three are correctly not encoded as properties (script-
  level control-flow/diff-shape properties, not a pure module a generator
  can drive) — verified instead by the real runs above, same disposition
  class as BL-1698/BL-1767's own non-encoded halves. Re-implementing any
  of the three as a parallel JS decision function to property-test it
  would itself have been the in-parcel rewrite the ticket's FIRM
  constraint forbids — agreed with coder's reasoning.
- Disposition: **Confirmed**, matches coder's finding. No new finding
  surfaced in this review beyond what the ticket itself already named
  (the dirty-worktree-skip probe result), so no follow-up note needed.
- `mutation-site-count.js`: 314 sites, `over` the 100-site advisory
  (BL-485). Not split: a single new feature's step handler (247 lines),
  consistent with this codebase's established step-handler size for a
  cohesive multi-scenario acceptance file (bl547 at 446, bl1079 at 270,
  bl877 at 219, all already accepted this session); `required_wiring`
  pins one `registerSteps` entry landing with the feature (BL-1371) — a
  split would fragment that single pinned entry point for no
  separation-of-concerns gain. Left whole as the correct call.

By cleaner.
