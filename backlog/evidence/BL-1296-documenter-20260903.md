# BL-1296 documenter review evidence — 2026-09-03

## Received
`e7cec9a261` — hardener pass for BL-1296 (Bubble answers from its own seat).

## Doc-domain review — NONE
Checked: how-to page for the new user-visible behavior (new — none existed),
`docs/index.md` link, `docs/reference/Specification.MD` Last-Updated entry,
README/changelog currency, and the diagram registry (`docs/diagrams/`) — no
diagram depicts cursor-bridge/seat-dispatch internals, so none of the four
tracked diagrams (architecture, swarm-flow, handoff-flow, front-desk-flow)
change-trigger on this ticket. No defect found in documenter's own domain.

## Added this pass
- `docs/how-to/BL-1296-bubble-answers-from-its-own-seat.md` (Divio how-to,
  sibling of `docs/how-to/BL-1235-local-qwen-seat-behind-its-own-topic.md`).
- Linked from `docs/index.md`.
- `docs/reference/Specification.MD` Last-Updated entry stacked on top,
  dated today.

## Send-time gate fix (lineage, not a doc defect)
`swarm_handoff.sh`'s PRE_QA_GATE refused the first send attempt: commit
`fefcb7c13d` (BL-1296, a descendant of the architect-bounce-revert base
`6077a6bfbc`) was reachable on `side`/`swarmforge-architect`/
`swarmforge-cleaner`/`swarmforge-hardender` but absent from my merged tree
and not recorded as abandoned. This is real content: the coder's own
bookkeeping commit `1d9c96fe6b` had already recorded
`abandoned_commits: [6077a6bfbc]` on `backlog/active/BL-1296-...yaml`
(BL-1192's documented rebuild-off-main escape — `68da935d5d` is the
verified tip-pure rebuild that replaces the abandoned base, and it IS
merged forward into my tree) but that bookkeeping commit itself was never
merged forward past the coder branch, so the field was silently absent from
`backlog/hold/BL-1296-...yaml` (moved there by the unrelated BL-1375
expedite park). Restored the same `abandoned_commits: [6077a6bfbc]` entry
(with provenance note) to `backlog/hold/BL-1296-bubble-answers-from-its-own-seat.yaml`
in this commit — no code or doc content changed by this fix, only the
lineage record the gate itself already knows how to read.

## Verdict
NONE (doc domain) — forwarding to QA.
