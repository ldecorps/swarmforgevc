# BL-1545 — hardender review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Test-only parcel; no `src/*.ts`, `land_step_lib.bb`, `land_step_cli.bb` or
`pipeline_stage_lib.bb` touched (`mutation_cost: low`, no CRAP/DRY
applicable). No `Scenario Outline:` in the BL-1545 feature (BL-638:
Gherkin mutation is inapplicable, plain `Scenario:` only).

Gates run:
- `bash swarmforge/scripts/test/test_bl1374_sync_merge_passengers.sh` — ALL PASS.
- `run_acceptance.sh` on both the BL-1545 feature (4/4) and the BL-1374
  feature it backs (4/4).
- `bb .../bl1374_sync_merge_passengers_property_runner.bb` — ALL PROPERTIES HOLD.
- `bb swarmforge/scripts/standing_red_register_cli.bb .` — both rows still
  present (expected; a row leaves at the land that turns the file green,
  per the ticket's own notes), `"unowned":[]`.

Independently re-ran (not just read) the coder's two negative controls,
plus a third of my own, each via a real hand-mutation on a scratch copy
outside git tracking (cleaned up, `git status --short` clean throughout):

1. **Oracle vacuous-pass control** (qa_e2e item 3): reverted case 05's
   `PINNED_BASE` to the live `origin/main` SHA in a scratch copy — the new
   range premise correctly FAILS ("EMPTY range") and the BL-1296 ownership
   check fails with it (2 FAILURE(S)), confirming the vacuous-pass shape is
   closed.
2. **Root-escape control** (qa_e2e item 4): built a real scratch clone,
   ran the fixed script with `GIT_DIR` exported to it — clone's
   `for-each-ref`/`status --porcelain` byte-identical before/after, no
   `tA` branch anywhere, exit 0 ALL PASS.
3. **Guard-isolation control (new, not in qa_e2e)**: per this file's own
   "overlapping self-exclusion guards" rule — checked whether `prove_root`
   is doing real work independent of the top-level `unset GIT_DIR
   GIT_WORK_TREE`, since scenario 04 only exercises both together. Removed
   *only* the `unset` line (kept `prove_root` intact) and re-ran the
   GIT_DIR-redirection fixture: `prove_root` alone aborted the run
   ("ABORT: fixture root ... resolves to a git-common-dir outside
   $TMPROOT") before any fixture command could touch the escaped repo —
   clone still byte-identical after. Confirms `prove_root` is a real,
   independently-effective second line of defense, not vacuous alongside
   the `unset`.

No orphaned test/mutation processes; no leaked fixture dirs
(`/tmp/bl1374-sync-merge.*` empty after every run).
