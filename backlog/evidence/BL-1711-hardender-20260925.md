# BL-1711 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: all three gaps found (below) were in this stage's own
domain (mutation hardening / test-suite integrity) and were fixed in
this pass, not bounced.

BL-113 Gherkin acceptance mutation (soft, first run on this feature):
4/4 mutants killed across the two Scenario Outlines, 0 survivors.
Manifest embedded.

Three real gaps found and closed (full reasoning and non-vacuity
verification for each is in the commit message, `d929e9eebb`):
1. A restart-bound off-by-one blind spot: the property test's own
   count=0/count=3 categories cannot distinguish `count -ge MAX` from
   an off-by-one `-ge (MAX - 1)` — both readings agree at those two
   points. Added a count=2 ("just-below-bound") category; verified it
   fails under the hand-mutated off-by-one and passes on real code.
2. `ollama_ancillary_restart_if_crashed`'s own "replacement never
   answers" failure branch had zero coverage anywhere. Added a fixture
   binary variant and a new property-test category; the first attempt
   used `exec sleep infinity` which silently made the check vacuous
   (argv/cmdline gets replaced), caught and fixed before trusting it.
3. The whole handoffd.bb daemon-wiring path (`ollama-crash-restart-
   sweep!`, `send-ollama-restart-alert!`, and the `run-sweep!` line
   that reaches them) had no test anywhere — the acceptance feature and
   property test both drive the restart CLI directly, never through
   handoffd itself. Exactly BL-1698's own D7 QA-bounce class. Added
   `test_handoffd_ollama_crash_restart_sweep_wiring.sh` (real daemon
   boot, following `test_handoffd_dead_letter_notify_wiring.sh`'s
   established pattern); verified non-vacuous by removing the wiring
   line and confirming the test fails.

No TS/JS production file is in this ticket's scope — CRAP/DRY are not
wired for Babashka/shell (engineering.prompt) and were not run.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1711-….feature`: 7/7.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=soft (first
  run): 4/4 killed, 0 survivors.
- `node specs/pipeline/cli.js specs/features/BL-1703-….feature`: 5/5
  (regression, depends_on).
- `node specs/pipeline/cli.js specs/features/BL-1705-….feature`: 8/8
  (regression, depends_on).
- `node specs/pipeline/cli.js specs/features/BL-1704-….feature`: 4/4
  clean runs, 1/4 hit the ALREADY-KNOWN intermittent scenario-03
  runner-liveness flake dispositioned as environmental by the
  architect (`BL-1704-architect-20260925.md`) — re-confirmed
  unrelated to this ticket's own diff (BL-1711 only ADDS new
  functions to `ollama_ancillary_lib.sh`, touches nothing BL-1704's
  stop path reads).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1711OllamaCrashRestartInvariant.property.test.js`: 1/1
  (targeted — this file WAS changed by this pass, so also covered by
  the pre-commit `check_property_suite_drift.sh` guard's full
  `npm run test:properties` run at commit time, which passed).
- `bash swarmforge/scripts/test/test_handoffd_ollama_crash_restart_sweep_wiring.sh`
  (new this pass): ALL PASS.
- `npm test` (extension/): 638 files / 10874 tests, all green. Same
  pre-existing per-file budget warnings on a handful of unrelated
  files.
- No real process/fixture leaks: every `pgrep -f "fake-ollama..."`
  hit investigated was the interactive shell harness's own command-
  line self-match (its wrapper script embeds the literal pgrep
  command text), confirmed via `ps -eo … | grep "serve"` finding
  nothing real each time. `git status --short` clean before each
  commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner from a prior session; host load 3-19/20 cores throughout, no
  fallback needed.

By hardender.
