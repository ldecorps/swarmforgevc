# BL-1368 land note — merge-deletion guard blocked a plain merge, 2026-09-05

Same BL-1403 class as BL-1370/BL-1401. A plain `git merge 907f33b823` into
the QA worktree was refused, this time by two guards at once:
`check_ticket_deletion.sh` over a stale `backlog/paused/BL-1370-...yaml`
copy this worktree still carried (attributable — named `BL-1370` in the
message would have cleared it), and `check_merge_deletion.sh`
unconditionally over the two unattributed intake-archive files (cannot be
cleared by naming anything). This ticket's own history independently
confirms the intake-archive class: `3817524437` "Re-drop the two drained
root intakes the BL-1368 merge resurrected".

Replayed this ticket's own attributed paths (coder/cleaner/architect/
hardener/documenter) as a plain, non-merge commit (`01c3d162c4`) instead,
re-verified `npm run compile` clean, unit 456/456, property 3/3,
acceptance 4/4, then built and landed the tip-pure commit (`4b99f394e9`).

Land-approval recorded citing `01c3d162c4` (the plain replay commit) as
source: `{"at":"2026-09-05T02:37:18Z","ticket":"BL-1368","commit":"4b99f394e9","source":"01c3d162c4"}`.
Verified `is_qa_ancestor.sh 4b99f394e9` prints `approved: ... - BL-1334`.
`abandoned_commits: [907f33b823]` recorded on the ticket.

By QA.
