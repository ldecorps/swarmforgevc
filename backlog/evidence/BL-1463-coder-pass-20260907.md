# BL-1463 — coder pass

Commit: this parcel · 2026-09-07 · worktree `swarmforge-coder`

## The fix

`land_step_lib.bb`'s `land-plan`, the `(nil? paths)` escalate branch
(reached when `entangled-siblings` already succeeded and found evidence,
but `own-paths`'s own per-path attribution read then failed): now merges
`{:entangled :landed :unlanded}` from the sets already computed a few
lines above into the escalate map, exactly matching what the `:replay`
branch below it already carries. The two OTHER escalate points (a bare
`warning` from `entangled-siblings` itself failing to walk at all; "task
name names no ticket id") have no sibling sets to offer and stay bare, per
the ticket's own "How".

`land_step_cli.bb`: extracted `print-entangled-siblings!` (the
`ENTANGLED_SIBLING <id>` printing loop, previously inline only in the
`:replay` case) and called it from BOTH `:replay` and `:escalate` — one
function, not a second copy, per the ticket's own direction. The
`:escalate` case already printed `entanglement-note` when `:unlanded` was
present (BL-1447's own earlier work); it now ALSO prints the
`ENTANGLED_SIBLING` lines first, matching the replay path's ordering.

## Verification

| check | result |
|---|---|
| `land_step_lib_test_runner.bb` | ALL PASS — new BL-1463 test reproduces BL-1272's own real tree-object-deletion fixture (not a fake injected `commits-fn`, since `land-plan` doesn't accept one) directly through `land-plan` |
| Reverting `land_step_lib.bb` to pre-fix | the new test fails exactly as expected (`:unlanded` `nil` instead of `#{"BL-9002"}`) — non-vacuous |
| `run_acceptance.sh` **BL-1272** feature | **6/6** — row 4 (the standing red since 2026-09-02) now green |
| Reverting both `land_step_lib.bb`/`land_step_cli.bb` to pre-fix, same BL-1272 run | 5/6 — row 4 fails identically to the reported defect — non-vacuous |
| `run_acceptance.sh` **BL-1463** feature (new) | **5/5** |
| Same revert, BL-1463 feature | 4/5 (scenario 01 fails) — non-vacuous |
| Direct CLI check (`bl1272LandStepFixtureCli.sh ... unreadable`), qa_e2e_procedure step 3 | `LAND_ESCALATE` / `ENTANGLED_SIBLING BL-9002` / note naming it / `could not read own.txt's attribution` / exit 1 — exact match |
| Direct CLI check, qa_e2e_procedure step 4 (`no-ticket-task`) | `LAND_ESCALATE`, no `ENTANGLED_SIBLING` line, exit 1 |
| `standing_red_register_cli.bb .` | `"unowned":[]`, no row for BL-1272's feature |
| Sibling features (BL-1446, BL-1447, BL-1461, BL-1375, BL-1432, BL-1466) | all green, unaffected |
| `bl1375ApprovedSiblingsCanLandInvariants.property.test.js` | 3/3, unaffected |

## `required_wiring`

`swarmforge/scripts/land_step_cli.bb::ENTANGLED_SIBLING` — the literal
survives (now printed from the shared `print-entangled-siblings!`, called
from both outcomes).

## Standing-red register

Removed the BL-1272-feature row (owner BL-1463) from
`backlog/standing-reds.tsv` in this same commit, per Article 3.2 ("a row
is removed in the same land that turns the test green") and the ticket's
own scenario 04.

## Out of scope, left for the specifier/documenter (per this ticket's own "How")

- `docs/reference/BL-1241-...md`'s outcome table (LAND_ESCALATE lists
  siblings when it has them).
- The QA prompt's land-step item — the ticket's own text says "prose is
  specifier-owned: the specifier lands that line at activation and
  records the commit here", not the coder.
- BL-1297's fixture red — BL-1464, minted alongside, not this ticket.
