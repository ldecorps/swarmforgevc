# BL-1744 — coder pass, 2026-09-25

## The fix

Lands the operator's staged diff
(`backlog/evidence/BL-1744-staged-operator-hotfix-think-false-20260925.md`)
verbatim: `completeWithLocalModel` (`extension/src/tools/localQwenSeatLive.ts`)
now sends `think: false` on every request body, with or without a system
prompt, plus the docstring explaining why (ollama's native toggle,
measured live: 132s cut off with thinking on vs 51s clean with it off).
The two request-body assertions in `extension/test/bl1235LocalQwenSeatLive.test.js`
updated to expect the field. `git diff` of both files matches the
reference diff exactly (confirmed via direct comparison - identical
content and line positions).

## The pinned handler

`specs/pipeline/steps/bl1744LocalSeatThinkOffSteps.js` (new). Drives the
REAL compiled `completeWithLocalModel` against a stand-in fetch that
records the request it received, across both Examples rows (with/without
a system prompt) - never a restatement of the request-body construction.

Non-vacuity, checked directly (not left in the commit): reverted the
source fix (`git checkout --`, recompiled), confirmed both scenario rows
failed; restored the fix (re-applied the saved diff, recompiled),
confirmed both pass again.

## Verification run before forwarding

- `node specs/pipeline/cli.js specs/features/BL-1744-...feature`: 2 of 2.
- `npx vitest run --config vitest.config.mjs test/bl1235LocalQwenSeatLive.test.js`:
  20 of 20.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1682LocalSeatBriefingSystemPrompt.property.test.js`: 2 of 2 -
  BL-1682's invariant B ("no key other than `system` ever changes") still
  holds, since `think` is present in every body regardless of `system`.
- `npm test` (extension/, compile + unit lane): 637 files / 10868 tests,
  exit 0.
- `git diff -- extension/src/tools/localQwenSeatLive.ts
  extension/test/bl1235LocalQwenSeatLive.test.js` against the evidence
  file's reference diff: identical.

## Not this parcel's to do

qa_e2e step 4 (master's UNSTAGED working-tree copy of the two files is
superseded by this land; tell the coordinator to `git restore --` both
paths on the master checkout once this lands) is a post-land coordinator
action per the ticket's own notes - not something to act on from this
worktree now.

## Scope

Touched: `extension/src/tools/localQwenSeatLive.ts`
(`completeWithLocalModel` + docstring),
`extension/test/bl1235LocalQwenSeatLive.test.js` (two assertions),
`specs/pipeline/steps/bl1744LocalSeatThinkOffSteps.js` (new, same parcel
as the feature per BL-233/BL-1371, auto-discovered per BL-1371, no
index.js edit needed), this evidence file. `backlog/standing-reds.tsv`
NOT touched. `backlog/evidence/BL-1744-staged-operator-hotfix-think-false-20260925.md`
(the reference) NOT touched - it is the specifier's record, not mine to
edit. Nothing else in this worktree staged - the unrelated untracked
leftovers (BL-1666/BL-1652) are not this ticket's and are left as found.

## Commit blocked by an unrelated owned red (2026-09-25, ~15:23Z)

`git commit` on the above (staged: the two source/test files, the new
step handler, this evidence file) is refused by the local
`property-suite-guard` pre-commit hook: `test/bl781LiveGrepOffender.property.test.js`
fails with `isAllowedBabysitterMatch must exist` — pre-existing on
`main`, not touched by this parcel. Owner: **BL-1739**
(`backlog/active/BL-1739-bl611s-babysitterd-feature-runs-green-again.yaml`),
whose own description says it "deletes that step's definition,
`isAllowedBabysitterMatch`"; `git log` shows BL-1739 commits already on
`main` that did exactly that, ahead of `bl781`'s test being updated or
allowlisted. Not allowlisted in
`swarmforge/scripts/property_suite_standing_allowlist.tsv` (empty but
for its header) — repo-wide, this blocks every commit, not just this
one. Re-run alone (the guard's own BL-1407 retry) still fails, so this
is not a load flake.

Registering a row in that allowlist is gate-config, not this parcel's
to touch (same ownership principle as `backlog/standing-reds.tsv`), so
instead of self-serving an allowlist edit or bypassing the guard, sent
a priority-00 `note` to `specifier,coordinator` naming the red and its
owner. This parcel's own work (fix + tests + acceptance + step handler)
is complete and verified above; only the commit itself is blocked
pending that resolution.

By coder.
