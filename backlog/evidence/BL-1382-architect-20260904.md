# BL-1382 architect review — clean sweep, forwarding

**Severity note**: this is a live-data-loss defect fix (the human's
hand-installed crontab schedule was erased overnight), so this review was
run with the same extra care cleaner applied — every claim independently
re-executed, not trusted from evidence text.

## Reviewed
Merged cleaner's `7a3e51d313` — one purely-additive conflict in
`suite-manifest.tsv` (two new rows, both files confirmed present on disk).
Read the ticket's human ruling (`human_ruling:`, option 1, marker-only
ownership everywhere, provenance SUP-17 14:13:07Z), both invariants, coder
and cleaner evidence, and the full diffs of `swarmforge_cron_lib.sh` and
`reconcile_shift_schedule_crontab.bb`.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1382UnmarkedCronLineSurvivesSteps.js
```
No forbidden edges.

## Logical coupling (BL-255)
`co-change-report.js` against the five changed files: only frequency-1
hits, below the default threshold (3). Nothing flagged.

## Invariants — verified directly in the diff
1. **Marker-only ownership, both readers.** Read `swarmforge_cron_lib.sh`'s
   `swarmforge_cron_line_belongs_to_root` end to end: the three path clauses
   that caused the incident (`$root/.swarmforge/operator/`,
   `$root/start-swarm.sh`, `$root/stop-swarm.sh`) are gone. What remains —
   freshness marker, operator marker, shift-schedule begin/end, the
   freshness line's own `FRESHNESS_ROOT=$root ` signature — is exactly what
   the human ruled. `reconcile_shift_schedule_crontab.bb`'s
   `strip-schedule-lines` carries the identical set, and its own comment
   names the shell predicate as the rule it mirrors.
   - Caught the second, subtler correctness point myself: the begin/end
     bounding uses `str/ends-with?`/`(str begin " ")` in the bb side and
     `*"...begin $root"` / `*"...begin $root "*` in the shell side — both
     correctly require the root to end the marker or be followed by
     whitespace, closing the bare-substring sibling-claim bug the coder's
     own property test found (root `/a` claiming `/a-sibling`'s block).
2. **Bash/bb agreement, tested not asserted.** `test_bl1382_cron_ownership_agreement.sh`
   drives both real readers against one shared corpus and includes a
   non-vacuity case (editing one reader's marker list, not the other, and
   requiring the suite to then FAIL) — this is exactly what BL-897 asks for
   and it isn't tautological.

## Independent re-verification
- `test_bl1382_unmarked_cron_lines_survive.sh` run 2× standalone: **11/11
  PASS** both times — unmarked operator-script and scripts-dir lines survive
  both uninstall and a recognized-mode install byte-identical, a sibling
  root's marked line is untouched, an operator-schedule-marked line IS still
  removed, both paths report the unmarked line as "left in place."
- `test_bl1382_cron_ownership_agreement.sh` run 2× standalone: **6/6 PASS**
  both times, including the deliberate-disagreement non-vacuity case.
- Property test (`bl1382CronOwnershipMarkerOnly.property.test.js`, after
  `npm run compile`): **3/3 pass** (both invariants, plus the sibling-root
  case named separately).
- Acceptance (`BL-1382-...feature`): **5/5 pass**.
- `bl1162_swarmforge_cron_property_runner.sh` (BL-1162's own pre-existing
  regression suite, since this ticket changes a function BL-1162 built):
  **14/14 PASS** — multi-root isolation intact, and the re-tensed assertions
  ("orphan operator path does NOT belong to root A" / "is reported as left
  in place") read correctly as the ruling requires.
- Grepped both new test files for `crontab -l`/`crontab -` outside the
  fixture shim comment: none found; confirmed the shim-resolution guard
  (`command -v crontab` == the fixture shim) is present and would fail the
  suite rather than silently touch the live schedule.
- Grepped both wiring call sites: `swarmforge_cron_report_unmarked` is
  called from both `uninstall_swarmforge_crons.sh:26` and
  `install_shift_schedule_cron.sh:104` — an unmarked line is reported on
  both cron-writing paths, per the ruling.
- Re-ran `test_bl1162_start_stop_swarm_cron_lifecycle.sh` independently and
  captured its real exit code (not the pipeline's): **exit 1**, reason
  `CRON_DAEMON_DOWN` — confirmed pre-existing and unrelated (cron has been
  dead on this host since 2026-08-30, tracked separately as BL-1392), not
  this parcel's regression.

## required_wiring
Deliberately absent, and correctly so: both predicates sit on live call
paths already (`uninstall_swarmforge_crons.sh` sources the lib;
`install_shift_schedule_cron.sh` runs the reconcile on every start) —
confirmed by the grep above. An anchor on the parcel's own edit would be
satisfied by construction (BL-1235), matching the ticket's own `notes:`.

## Ruling and scope check
`human_ruling:` (option 1) is honored exactly: no line is swept silently —
every unmarked line naming the root is reported, never removed, on both the
stop and the install path. Option 2/3's voided clauses do not appear
anywhere in the shipped code. The legacy-preset sweep is not reintroduced
behind a flag (grepped: no new CLI flag on `uninstall_swarmforge_crons.sh`),
matching "the ticket offers that as direction only... the ruling asks for
nothing that removes an unmarked line."

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted — every claim in the evidence chain reproduced independently.
Forwarding to hardener.
