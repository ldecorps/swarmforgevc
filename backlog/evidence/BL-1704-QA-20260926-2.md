# BL-1704 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Commit tested: 741f2b017d (documenter b84a0bcfc6 merged). The D1 fix 2af031ac4e
arrived with the f0779774a2 merge. Both merges were checked against both
parents; BL-1700's paths are kept out after its bounce and revert.

Prior bounces (bounce_count 4), read from this branch's evidence: the
2026-09-26 D1 (scenario 03 example 1's orphaned `ollama runner` reaped by the
live BL-1705/BL-1726 ghost janitor) is FIXED. `killServerAndRunner()` kills
both, and example 1 asserts "no process was signalled" on a fresh untitled
process; example 2 still checks the untouched runner. The earlier code D1 and
docs D2 were fixed in previous passes.

Gates, one run each on 741f2b017d, run by hand with full logs under `tmp/`
(`BL-1704-*.log`):
- `qa-sibling-check.js status`: `VERIFY BL-1704`. `pre_qa_gate.sh`: OK.
- standing-red register: 8 rows, `unowned: []`.
- `npm test`: 638 files, 10875 of 10875 passed.
- `npm run test:properties`: 466 files, 1345 of 1345 passed; 3 allowlisted
  BL-871 `onTaskUpdate` unhandled errors.
- `run_acceptance.sh` on the BL-1704 feature: 5 of 5, with the live ghost
  janitor running.
- Stragglers: the only live test processes were a Stryker run in
  `.worktrees/hardender` (not QA's), before and after.

qa_e2e:
1. Feature 5 of 5 (above).
2. Stop-path shell tests, one run each, all green:
   test_kill_all_swarm_ollama_failure_not_fatal.sh (3 PASS),
   test_stop_ancillary_services_ollama_failure_not_fatal.sh (2),
   test_ollama_ancillary_stop_pid.sh (4), test_ollama_ancillary_launch_gate.sh
   (2), test_babysitterd_census_lib.sh (8), test_stop_ancillary_services_onboarder_dual_clear.sh,
   test_stop_ancillary_services_tunnel_orphan_reap.sh. The three
   `kill_all_swarm` tests the manifest marks live-only were not run.
3. Diff read: `ollama_ancillary_stop_swarm_owned` returns before any signal
   unless the record's owner is `swarm-owned` AND
   `ollama_ancillary_pid_is_ollama_serve` holds for the recorded pid. It
   signals only that pid and its `ollama_ancillary_runner_children`, and a
   stale record logs "is gone" or "now belongs to <cmdline>" and signals
   nothing. That matches the declared invariant.
4. Own paths: the Scope scripts (ollama_ancillary_lib.sh, stop_ancillary_services.sh,
   kill_all_swarm.sh), the two shell tests and their manifest lines, the
   property test, the fixture helper, the step handler, the feature, the
   BL-1052 how-to section, and evidence. The how-to, `ollama_ancillary_lib.sh`
   and `suite-manifest.tsv` also carry unlanded BL-1711 lines (BL-1711 depends
   on BL-1704 and is deferred at QA). The land carries BL-1704's hunks only.

Article 4.5: the changed production scripts are covered by the shell tests
above and by `bl1704OllamaStopSwarmOwnedInvariant.property.test.js` (green in
the property lane).
