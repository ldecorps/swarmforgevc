# BL-1719 - QA bounce, 2026-09-24

Parcel commit tested: 4ced232386 ("Merge documenter b5d8d8ed76 into QA.")

## Inventory (Article 4.4 - full checklist run)

Run and green: unit, property, acceptance (the BL-1719 feature), pre_qa_gate, sibling-check
VERIFY, register join empty; qa_e2e step 2: handoff_wake_session_test_runner.bb,
mono_router_lib_test_runner.bb, test_handoffd_notify_verified.sh and
test_handoffd_wake_no_session_standing_pack.sh all pass. Step 3 is post-land and was not reached.
Step 4 (scope) is clean.

### D1 - wake-session now returns nil, and most callers pass it to tmux unguarded

1. Failing command (reproduction of the tmux behaviour, on a private throwaway socket):
   `env -u TMUX tmux -S ./tmp/qa-niltmux.sock new-session -d -s victim cat;`
   `env -u TMUX tmux -S ./tmp/qa-niltmux.sock send-keys -t "" -l LANDED_IN_VICTIM;`
   `tmux ... capture-pane -p -t victim | grep -c LANDED_IN_VICTIM`
2. Commit: 4ced232386
3. Excerpt: `send_rc=0` and `1`: an empty `-t` target is accepted and lands in tmux's default
   session. In babashka, a nil in the argv reaches the process as an empty string (checked:
   `(p/sh "echo" "-t" nil "x")` prints `-t  x`).
4. Failure class: behavior
5. Expected vs observed: expected every caller of handoff-lib/wake-session to send nothing when
   it returns nil (the ticket: "send no wake anywhere"). Observed: only
   handoffd.bb notify!, maybe-notify! and handoff_inject_lib notify-delivered-recipient! check for
   nil. These pass the nil straight to agent-runtime-inject/notify-agent! (capture-pane,
   send-keys, paste-buffer with `-t ""`):
   - handoff_inject_lib.bb `notify!` (line ~77)
   - handoffd.bb `notify-in-process-resume!` (~443), the in-process chase notify (~2353),
     `context-clear-injectors` (~5378: injects `/clear` and the startup re-read),
     `role-context-fullness-percent` (~5399: reads another pane's scrollback as this role's
     fullness), `recipient-pane-busy?` (~464), `wake-target-epoch` (~1215)
   - babysitter_nudge_lib.bb `resolve-nudge-target` (~36) and its nudge
   In a standing pack, a roles.tsv row with no session (the dropped coder@2 case this ticket
   counted) now sends those injections to an ARBITRARY live pane chosen by tmux. The old code
   at least picked the resident. The context-clear path can `/clear` the wrong seat.

- Blamed role: coder
- Remediation: make every caller skip (and log, naming the configured session) on a nil
  wake-session, or give notify-agent!/capture-pane-text a nil/blank-session guard that
  refuses rather than targets `""`. Add a test per injection path (resume, chase, context-clear,
  babysitter nudge) proving a session-less standing-pack recipient reaches no pane. The existing
  no-session standing-pack shell test covers only the delivery wake.

By QA.
