# BL-1704 — QA review pass, 2026-09-26

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1704-the-swarm-stop-paths-stop-the-ollama-server-the-swarm-started.feature
- **Commit hash**: 9cabb31bac
- **First error excerpt**: not ok 4 - a record that no longer matches a live server is cleared without killing anything [1]: failed at step 'Then no process was signalled': expected the untouched runner child to still be alive. .swarmforge/daemon/orphan-janitor-audit.log: 2026-09-25T23:14:33.785444272Z reaped-ollama-ghost pid=15580 cmd=ollama runner age_ms=5164 (during this run; same shape 20:45-20:56Z)
- **Failure class**: acceptance
- **Expected vs observed**: scenario 3 example 1 is deterministic on a host running the live swarm / observed: the handler kills the stand-in server, orphaning a process titled 'ollama runner' that the live BL-1705/BL-1726 ghost janitor reaps within seconds, so the scenario reds by collision with the real swarm, not by any stop-path signal
- **Blamed role**: coder
- **Remediation pointer**: specs/pipeline/steps/bl1704OllamaStopPathsSteps.js scenario 03 'is no longer running' + extension/test/helpers/ollamaStopPathsFixture.js: never leave an orphaned 'ollama runner'-titled stand-in (e.g. record the dead pid of a separate runner-less stand-in server and keep the runner's server alive, or kill the runner with its server and assert on a stand-in the janitor cannot match). The fixture comment blaming a 'WSL2 reparenting quirk' is this janitor; correct it

By QA.

## Detail

Commit tested: 9cabb31bac (documenter bc1e887e43 merged). Prior D2 (docs):
FIXED - external names endpoint only; BL-1704/BL-1705 now point at their
documented sections. Prior D1 (code): fixed at the last pass.
Since the full qa-gather run on d4fd242514 (unit red owned by BL-1764,
everything else green) this lineage changed no BL-1704 code path; only the
acceptance run was repeated, and it went 4/5 (D1 above).
No revert: the defect is the scenario's collision with the live ghost janitor;
the stop-path code under test is correct (the stale branch returns before any
signal - lib ~218-227).
