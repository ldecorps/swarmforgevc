# BL-1729 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1729-a-property-lane-repository-walk-never-reads-the-checkouts-runtime-state.feature`:
  3/3 ok.
- `npx vitest run --config vitest.properties.config.mjs --logHeapUsage
  test/bl874PortableTimeInvariants.property.test.js
  test/tempDirTrapGuard.property.test.js`: 61/59 MB, far under the 100 MB
  bar (was 894/1778 MB on the shared main checkout).
- Census completeness (qa_e2e step 2): `comm -3 <(find test -name
  '*.property.test.js' | sort) <(grep -v '^#' test/property-lane-heap-census.txt
  | cut -f1 | sort)` — empty.
- `npx vitest run --config vitest.properties.config.mjs test/bl1443TolerantTreeWalkInvariants.property.test.js`
  (the shared walk's OWN existing invariant test, not in this ticket's
  Scope but touching the same helper): 3/3, no regression.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4.
- `node out/tools/dependency-gate.js` / `co-change-report.js` on the
  touched helper: PASSED, no forbidden edges; coupling only with BL-1443's
  own family and this parcel's own files.
- Scope check: `git diff main...HEAD --name-only` resolves to exactly the
  three Scope files + evidence (one unrelated file also appears,
  `bl611BabysitterdLifecycleSteps.js` — confirmed via `git log` to be
  BL-1739's own in-flight content sitting in this worktree, not touched by
  any BL-1729 commit).

Independently re-verified non-vacuity myself (not just read the coder's
claim): reverted the `atRoot && rootOnlyExcludeDirs.has(...)` clause to
the pre-fix `excludeDirs.has(entry.name)` alone, re-ran the feature — it
failed exactly as expected (`.../tmp/decoy.js` opened when it should not
have been). Restored the original file (`git diff` empty afterward,
confirmed clean).

Correctness read: `atRoot = dir === root` is computed once per directory
being recursed INTO (not per top-level listing call), so the root-only
exclusion applies precisely at the walk's first level and nowhere deeper —
matches the ticket's FIRM constraint ("a fixture root named `tmp` must
still be walkable") and the invariant's literal wording ("the root's
runtime directories"). The scenario 01 handler proves "never opened" via
a spying `fsImpl.readFileSync`, not a post-hoc result filter — the coder's
own evidence describes constructing a stronger mutant (read-then-filter)
that this spy still catches; I did not need to re-derive that stronger
mutant since my own simpler revert already demonstrates the same
mechanism (the spy records the open before any exclusion-of-result could
mask it).

By architect.
