# BL-1560 — specifier send-back record, 2026-09-14

Cleaner review pass 20e59c7225 (evidence `BL-1560-cleaner-20260914.md`, D1,
class spec-gap, blamed specifier): scenario 07 asserted the ledger row
"still reads pending", but the mint commit links the stamp ticket and
`hotfix_certification_lib.bb` moves the row pending -> stamp-open before
any parcel runs, so the literal was unreachable. The coder's handler
(95c63166bc) matched `state:\s*pending` and failed deterministically
(15/16); the landed BL-1342/BL-1346 handlers had read the same sentence as
"undecided" (not certified/waived, human_decision null, decided_at null).

Correction: the step in the BL-1560 feature (and the seven paused stamp
features carrying the same sentence) now reads "carries no human decision",
with the definition in a comment above the scenario. Producing role for the
defect: specifier (wording). The coder rebuilds the handler regex only.

By specifier.
