# BL-1684 — QA review pass, 2026-09-22

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel commit verified: `7c1a4e8508` (documenter's evidence commit),
merged into `swarmforge-QA` at `73e01510de`..HEAD (merge commit built on
top). Lineage confirmed: `88600ff6bc` (hardener's fix) and `d143144cdd`
(coder's bounce fix) are both ancestors of `7c1a4e8508`.

- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1684`:
  `VERIFY BL-1684` (no open deferral).
- `npm run compile` (extension/): clean, no TS errors.
- Required wiring: `specs/pipeline/steps/bl1684CaptionCapBudgetsTheEscapedLengthSteps.js`
  is auto-discovered by `specs/pipeline/steps/index.js` (BL-1371 discovery,
  no hand-maintained require list); confirmed `require()`s cleanly and
  exports a `registerSteps` function.
- `npx vitest run --config vitest.properties.config.mjs test/bl956PipelineBoardCaptionCapInvariants.property.test.js`:
  5/5 pass, including the pinned seed-10 regression example.
- `node specs/pipeline/cli.js specs/features/BL-1684-the-pipeline-boards-caption-cap-is-applied-before-html-escaping.feature`:
  2/2 ok (matches ticket's `qa_e2e_procedure` steps 1-2).
- `npx vitest run test/pipelineBoard*.test.js`: 217/217 pass (matches
  hardener's reported count).
- `npm test` (full unit suite, extension/): 634/635 files, 10850/10851
  tests pass. The one failure (`test/stepHandlerModuleLoadBudget.test.js`,
  `bl1412SpecTreeTextFilterSteps.js` over its 400ms load budget) is
  unrelated to this parcel's diff and is an already-owned standing red
  (`backlog/standing-reds.tsv`, owner BL-1687, active) — not this
  ticket's defect, not bounced.
- `npm run test:properties` (full property lane, run ONCE): 444/444 files,
  1294/1294 tests pass. One `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled error — the allowlisted BL-871 benign warning,
  not a failure.
- `swarmforge/scripts/pre_qa_gate.sh BL-1684 7c1a4e8508`: `OK`.
- Standing-red register: `backlog/standing-reds.tsv` carries a row owned
  by BL-1684 itself (`bl956PipelineBoardCaptionCapInvariants.property.test.js`)
  — expected pre-land per BL-1631 (the land step retires the landing
  ticket's own row at land time via `REGISTER_ROW_RETIRED`; the parcel
  itself never edits the register). Will verify `REGISTER_ROW_RETIRED`
  fires for this row at land time and that the row is gone from
  `origin/main` post-land.
- Bounce history: one prior bounce (hardener → coder, `invariant-unencoded`,
  `ab1a4cc6e0`, `backlog/evidence/BL-1684-hardener-bounce-20260921.md`) —
  the early-return in `composePipelineBoardHtml` bypassed the maxLength
  check whenever `repoBaseUrl` was falsy or `data.links` was empty,
  making the ticket's own last-resort fallback unreachable for the exact
  production shape the incident describes. Reviewed the fix
  (`d143144cdd`, coder) and the hardener's follow-up refactor
  (`88600ff6bc`, extracts `composePipelineBoardWithoutLinks`, closes a
  vacuous mutation-test fixture, pins the exact-fit boundary): the
  early-exit is now unconditionally `full.length <= maxLength`, both
  no-links shapes correctly fall through to the withoutLinks/fallback
  path. Confirmed correct by reading the diff and by the passing property
  and acceptance runs above, which exercise exactly this shape.
- Docs: `docs/reference/Specification.MD` updated to say the caption cap
  budgets the rendered/escaped length, not the raw text — matches ticket
  note. No diagram trigger (ticket confirms).
- No orphaned test/mutation processes before or after this pass (checked
  by cwd; a live `vitest` process seen during the run belonged to a
  different worktree, `.worktrees/coder`).

Verdict: PASS. Approving.
