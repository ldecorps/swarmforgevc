# BL-1416 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed the full diff on `handoffd.bb` and `provider_auth_observe_lib.bb`
  (the only two touched non-test files): the busy predicate now gates
  classification itself (`observe-pane-auth!` passes `busy-signal` from the
  same `chase-sweep-lib/actively-processing?` predicate `do-respawn!`/
  `do-auth-respawn!` already use); `resolve-committed-state` correctly
  commits the prior state unchanged on a proposed-but-unperformed respawn,
  and commits the decision's own state exactly for `:alert`/`:none`/a
  performed `:respawn` — traced through `observe-pane-auth!`'s call site
  and confirms the persisted `:attempts` count used in `send-auth-persist-
  alert!` is the real performed count, not the proposed one.
- No duplication introduced: the busy-branch short-circuit in
  `decide-auth-observation`, `resolve-committed-state`, and `matched-auth-
  line` are each a distinct, single-purpose function; `jscpd` over both
  changed files reports 0 clones (informational only — jscpd does not
  parse `.bb`, so this is a manual read, not a tool result).
- No architecture concerns: the pure decision lib
  (`provider_auth_observe_lib.bb`) stays free of tmux/process IO; the one
  side-effecting caller (`handoffd.bb`) is the only place that captures
  panes or calls `do-auth-respawn!`, matching the existing pure-lib /
  effectful-caller split BL-536 established.
- Per Article constraints, Babashka has no mutation/CRAP/DRY tooling wired
  (BL-472 deferred) — this pass is gated only by the unit/property/
  acceptance suites below, recording the degraded fallback as directed.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/provider_auth_observe_lib_test_runner.bb` | PASS (all assertions, old and new) |
| `bb swarmforge/scripts/test/bl1416_busy_pane_never_auth_dead_property_runner.bb` | ALL PROPERTIES HOLD, 500/500 each of P1/P2/P3 |
| `node specs/pipeline/cli.js specs/features/BL-1416-a-busy-pane-is-never-auth-dead.feature` | 7/7 scenario runs pass |
| `node specs/pipeline/cli.js specs/features/BL-536-provider-auth-error-auto-respawn.feature` (regression) | 3/3 pass, unaffected |
| `bash swarmforge/scripts/test/test_handoffd_auth_observe_wiring.sh` (real daemon + fake tmux, regression) | PASS 01-03, unaffected |
| `grep -n 'actively-processing? pane) :healthy' swarmforge/scripts/handoffd.bb` | matches (required_wiring #1) |

No fix required. Forwarding unchanged to architect.
