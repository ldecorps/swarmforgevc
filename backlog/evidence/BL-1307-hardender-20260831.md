# BL-1307 — hardender pass, 2026-08-31

Reviewed architect commit `a7d02db4e0` (coder commit `6f8227894e`).

## What this pass checked

No product/mutation tooling applies beyond what the coder already ran:
`.bb` code has no wired mutation/CRAP/DRY (engineering.prompt, BL-472
deferred) and no `extension/` files were touched (architect confirmed via
the dependency gate), so jscpd (scoped to `extension/`) and CRAP (scoped to
`extension/src/*.ts`) have nothing to measure here. Gated by the file's own
unit + property + acceptance suites, re-run directly, plus a hand-authored
mutation probe (BL-638 fallback) on top of what the coder already
non-vacuity-proved.

## Verification — RE-RUN, not trusted from the coder/architect reports

| Check | Command | Result |
|---|---|---|
| Gate unit suite | `bb .../review_forward_evidence_gate_lib_test_runner.bb` | ALL PASS |
| Gate property suite | `bb .../review_forward_evidence_gate_lib_property_runner.bb` | ALL PASS |
| BL-1307 acceptance | `run_acceptance.sh specs/features/BL-1307-....feature` | 6/6 |
| BL-1293 acceptance (sibling) | same runner | 6/6 |
| BL-806 acceptance (sibling) | same runner | 9/9 |
| Send-path regression | `test_swarm_handoff_sync_deliver.sh` | ALL PASS |
| Send-path regression | `test_swarm_handoff_daemon_backup.sh` | ALL PASS |
| Step registration | `grep bl1307 specs/pipeline/steps/index.js` | registered, line 551 |
| Orphaned processes | `pgrep -fl 'node --test\|stryker'` before/after | none |

## Hand-authored mutation probe (beyond the coder's non-vacuity proof)

The coder already proved non-vacuity by deleting the `blocked?` disjunct
(45 property failures, 2 unit failures). I additionally mutated the one
other piece of decision logic not exercised by that deletion — the
`(some #(evidence-path-for-ticket? % ticket-id) paths)` check in
`forward-carries-own-evidence?` — flipping `some` to `every?` (requiring
every changed path to be evidence, not just one). Re-ran the unit suite:
3 failures (the "evidence added among other changes" rows), confirming the
`some` semantics are load-bearing and covered. Reverted; `git diff` on the
file confirmed clean before commit.

## Fixture/lifecycle checks

- `specs/pipeline/steps/bl1307ReviewForwardOwnEvidenceSteps.js` uses the
  `SHAPES`/`VERDICTS` KNOWN_VALUES pattern correctly (engineering.prompt) —
  no passthrough of Scenario Outline values.
- Uses `fs.mkdtempSync(os.tmpdir(), ...)` with no explicit per-scenario
  cleanup, matching the sibling `bl1293ReviewForwardOwnWorkSteps.js` and the
  other 487 acceptance step files sharing this convention repo-wide — not a
  regression introduced by this parcel, and no tmux/daemon process is
  involved here (no cross-step leak hazard per the BL-788/BL-921 rules,
  which are about live servers, not plain fixture directories).
- No tmux server is started by this ticket's fixtures — the
  tmuxReaperGuard / fixtureReaper hazards do not apply.

## Verdict

**D: NONE.** No test gaps found; the coder's non-vacuity proof plus this
pass's independent `some`→`every?` probe both confirm the decision logic is
fully covered. No mutation/CRAP/DRY tooling applies (Babashka, no
`extension/` files touched).

Forwarding to `documenter`. Per the source note, `docs/reference/Specification.MD`
carries changelog entries for BL-806 and BL-950's changes to this gate; the
documenter owns adding BL-1307's.
