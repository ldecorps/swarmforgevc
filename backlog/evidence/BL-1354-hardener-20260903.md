# BL-1354 — hardener pass (2026-09-03)

## Context

Merged architect `fde2410c33` (invariants both verified non-vacuous, no
defect found, forwarding). This pass hardens the coder's rework of
`land_step_lib.bb`'s `landed-siblings`/`sibling-landed?` chain (per-sibling
own-line-changes instead of whole-blob equality) plus the cleaner's own
merge-commit-exclusion coverage gap fix.

## BL-149 file-change cooldown gate

- `mutation_cooldown_gate.bb . swarmforge/scripts/land_step_lib.bb` →
  **`skip-cooldown`** (file last committed-touched 0.19 days ago, inside the
  3-day window). Per the gate's own rule this file is NOT mutation-tested
  this pass regardless of host load — it is still actively churning
  (coder → cleaner's own self-audit fix → architect all landed commits on it
  today).
- `mutation_cooldown_gate.bb . specs/pipeline/steps/lib/bl1354SharedPathClassifyCli.bb`
  → `run` (quiet host, load 6.7-6.9/20 cores throughout). That file is a
  fixture-building acceptance DRIVER (builds a real throwaway git repo, calls
  the real `land_step_lib.bb`), not product logic in the mutation-worthy
  sense — every scenario's own assertions already exercise its one piece of
  real logic (`report`'s landed/unlanded partition), so no additional
  hand-mutation sweep was run against it.
- Given the cooldown skip, this pass leans on the non-vacuous verification
  already done and independently repeated by coder, cleaner and architect
  (each reverted the fix and confirmed the specific rows fail, both
  invariants and the real-repo regression) rather than duplicating a
  hand-mutation sweep on a file the gate says not to touch this pass. A full
  hand-mutation sweep belongs on the next pass that touches this file past
  its cooldown window.

## BL-113 Gherkin acceptance mutation (Scenario Outline present)

`run_gherkin_mutation.sh specs/features/BL-1354-….feature <mktemp-dir>
specs/pipeline/steps/index.js soft` — **3/3 killed, 0 survived, 0 errors**
(manifest embedded in the feature file). The Outline's lookup
(`bl1354SharedPathLandedSiblingSteps.js`'s `ATTRIBUTIONS` map) is keyed on
the cell value itself (KNOWN_VALUES pattern), not scenario shape.

## Verification

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — ALL PASS
  (includes the cleaner's added merge-commit regression test).
- `run_acceptance.sh specs/features/BL-1354-….feature` — 5/5 pass.
- Property `bl1354SharedPathLandedSiblingInvariants` — ran 5 consecutive
  times, 2/2 green every run, no flake.
- Whole-tree guard sweep (parcel touches `specs/pipeline/steps/` and
  `extension/test/`): all 17 `extension/test/*Guard*.test.js` (excluding
  `.property.` siblings) run. 3 failed — `tempDirTrapGuard`,
  `liveRepoDerivationGuard`, `socketFixtureShortRootGuard` — none flags a
  BL-1354 file (grepped each guard's violation output for
  `bl1354`/`BL-1354`, zero hits). Same standing reds observed on the
  BL-1337 pass earlier today; `socketFixtureShortRootGuard` is a documented
  standing red owned by BL-1290, the other two are pre-existing files last
  touched 2026-09-02 and 2026-08-25.
- Fixture cleanup: `bl1354SharedPathClassifyCli.bb`'s `classify`/`attribution`
  build and tear down their own throwaway repo inside a single process
  invocation (`try`/`finally` around `fs/delete-tree work`, plus a
  prefix-sweep on entry per BL-971) — no cross-step split, so the
  fixture-leaks-on-throw hazard does not apply here. Confirmed no stray
  `bl1354-acceptance-*` directories under `/tmp` after the full run.
- CRAP/DRY (`crapReport.js`/`jscpd`) scope to `extension/src/*.ts`; this
  parcel touches no `.ts` file, so neither applies.
- No orphaned test/mutation processes at handoff (checked `pgrep -fl
  'node --test|stryker|vitest|gherkin-mutator|mutationWorker'`, scoped to
  this worktree — clean; a live `vitest` seen mid-pass belonged to the
  coder worktree, confirmed via `/proc/<pid>/cwd`, not touched).

## Verdict

No mutation gap surfaced within this pass's scope (production file under
cooldown, gate correctly skipped it). BL-113 clean, all suites green, no
regression, no new guard violation. Forwarding to documenter.
