# BL-1650 — QA bounce (rework pass), 2026-09-20

Full checklist run (Article 4.4): compile clean; `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`
ALL PASS; BL-1650's own acceptance feature 5/5 green (including the new scenario 05 for the D1
already-applied-cherry-pick fix); `pre_qa_gate.sh` OK; full unit suite 634/634 files, 10811/10811 tests
green; property suite 428/428 files, 1251/1251 tests green (one BL-871 allowlisted benign
`onTaskUpdate` timeout, not a defect). D1's own fix (the bug I bounced last pass) is correct and
well-tested — confirmed by scenario 05 and the coder's own hand-mutation verification recorded in
`backlog/evidence/BL-1650-coder-20260920.md`.

One regression found running BL-1546's own acceptance feature (re-verified per the standard checklist
since BL-1650 touches shared land-step machinery).

## D1

- **Failing command**: `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1546-a-path-owned-only-by-a-closed-ticket-is-never-silently-excluded.feature`
- **Commit hash**: f23e0014ea2dff1e347c264d45114f7a5869c2e2
- **First error excerpt**:
  ```
  not ok 2 - a closed-owner path no commit of the landing ticket's touched refuses the land by name instead of excluding the path
  Then it exits LAND_ESCALATE and the reason names that commit, the closed sibling's id, and the path
  expected LAND_ESCALATE (exit 1), got: {"status":0,"stdout":"LAND_REPLAY land-replay/BL-9646-779ff40a7b ...
  LAND_STRAY_EVIDENCE_LANDED 54de5b01fb8033415e4972b0809f0348d9841436 -> 4eaf4d018af85e6557c7abd6a1042dd9aaf7691b docs/shared.md
  LANDED_SIBLING BL-9647
  EXCLUDED_SIBLING_PATH docs/shared.md BL-9647\n","stderr":""}
  ```
- **Failure class**: regression
- **Expected vs observed**: Expected scenario 2 to keep testing the still-refusing case (a closed-owner
  path OUTSIDE the pure-evidence-or-docs carve-out). Observed: `specs/pipeline/steps/bl1546ClosedOwnerNeverSilentlyExcludesSteps.js`'s
  `SHARED_PATH` constant is `'docs/shared.md'` again — the EXACT fixture regression the cleaner already
  found and fixed in the ORIGINAL (bounced) pass (`332efb5ee6`, moved to `swarmforge/scripts/shared.sh`,
  the cleaner's own evidence: "BL-1650: fix BL-1546's own fixture regression - use a non-doc/evidence
  shared path"). The rework's coder commit (`12c8050b5d`, "BL-1650: fix - an already-applied stray
  cherry-pick never escalates the whole replay") reverted `SHARED_PATH` back to `'docs/shared.md'`.
  `git log -p --follow` on the step handler confirms the change: `-const SHARED_PATH =
  'swarmforge/scripts/shared.sh'; +const SHARED_PATH = 'docs/shared.md';`.
- **Root cause**: `12c8050b5d`'s parents (`git log --oneline 12c8050b5d`) are "Merge QA 309a50e3ae into
  coder2." and "Merge architect 1c9677b6b2 into coder2." — the D1 fix was rebuilt on `swarmforge-coder@2`
  (a SEPARATE seat from the original `swarmforge-coder` that built and cleaned the first pass), which
  never had the original cleaner's fixture-path fix in its own ancestry. Same class as the
  `coder2-stage-queue-dir-mismatch-root-cause-0917` watch entry and the BL-1652 duplicate-seat rework
  found in this same shift (`backlog/evidence/BL-1652-stranded-coder2-duplicate-20260920.md`,
  `backlog/evidence/BL-1652-coder2-duplicate-build-reconciliation-20260920.md`) — a third instance.
- **Blamed role**: coder (the rework commit itself reverted the fixture path; not caught by cleaner,
  architect, or hardener's re-review either, all of whom recorded NONE this pass).
- **Remediation pointer**: `specs/pipeline/steps/bl1546ClosedOwnerNeverSilentlyExcludesSteps.js:26`
  — restore `const SHARED_PATH = 'swarmforge/scripts/shared.sh';` (the cleaner's own original fix,
  `332efb5ee6`), re-verify all 4 BL-1546 scenarios green.

## Separate observation, not part of this bounce

`swarmforge/scripts/test/test_handoffd_bl1652_chase_respawn_busy_lane_guard.sh` (registered `standing`
in `suite-manifest.tsv`, currently passing) traces only to the ABANDONED coder@2 commit `4eacde9068`
(already recorded under `abandoned_commits:` on BL-1652's own ticket YAML). Per
`backlog/evidence/BL-1652-coder2-duplicate-build-reconciliation-20260920.md`, coder@2 deliberately kept
this one file from its own abandoned build ("genuinely additive") despite the specifier's explicit
ruling to "drop your own extra artifacts... never forward BL-1652." The file works and is registered
correctly, so not blocking this bounce, but it is a process deviation from an explicit ruling — noted
to the specifier separately, not as a BL-1650 defect.

By QA.
