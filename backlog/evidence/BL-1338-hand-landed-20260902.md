# BL-1338 hand-landed — 2026-09-02

QA-approved commit `bc1a587622dfa6906c1c2a8c2f222bca83b8be89` (full
verification: `backlog/evidence/BL-1338-land-escalate-20260902.md`) could
not land through `land_step_cli.bb` — it reported "own-paths identical to
origin/main", a real defect in the tool (minted by the coordinator as
BL-1343, critical: the tool's attribution walk only follows a merge's
first-parent edge, and BL-1338's tagged commits entered QA's branch on the
merge's second-parent edge, so nothing was attributed to it).

Per the coordinator's own recipe
(`backlog/evidence/coordinator-bl1338-land-recipe-for-qa-20260902.md`, read
from `origin/main`), hand-landed BL-1338's own paths — computed and
verified independently by me, matching the coordinator's set exactly:
`deprecate-check.ts`'s fingerprint change, its two test files, the new
step-handler file, ONE `index.js` require line (deliberately not the two
sibling requires for still-unlanded BL-1317/BL-1271 also present in the
approved tip — registering their handlers with no backing file is the
BL-1324 failure shape), the BL-1338 evidence files, and the topic record.
Re-verified compile + both test files + the full acceptance feature fresh
against origin/main before each push, rebasing through two further origin
races (an actively busy `main` this hour) per BL-1144's bounded-rematch
discipline.

**Landed on `main`**: `95e96ef217` (content), then `a1450efaa3` (adds
`abandoned_commits: [bc1a587622]` to the ticket YAML per
`swarmforge/backlog-schema.md`, since the QA-approved commit never became
an ancestor of what actually landed).

**Also fixed in passing**: `.worktrees/QA/.swarmforge/land-main.publish.lock`
was a stray 0-byte plain FILE (not the mkdir-based lock directory the
script expects), permanently blocking `--acquire-lock` in this worktree.
Moved aside to `.worktrees/QA/tmp/stray-land-main-publish-lock-file-
20260902.bak` (not deleted) rather than left blocking every future land in
this worktree.

Requesting the coordinator do the usual bookkeeping: move BL-1338 to
`backlog/done/`.
