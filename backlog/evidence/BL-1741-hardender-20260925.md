# BL-1741 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `extension/test/emitLifecycleSnapshotCli.test.js` (test-speed
fix), `extension/test/liveRepoDerivationGuard.test.js` (consequential
census edit) — test-infrastructure only, no production code (matches
`mutation_cost: low`'s "no production code").

Checks run (host load ~15-24 on 20 cores):
- `npx vitest run --config vitest.config.mjs
  test/emitLifecycleSnapshotCli.test.js` (from `extension/`): 4/4
  passed, 38ms — far under the 7.0s budget (was 16.1s in-suite/9.6s
  alone).
- `npx vitest run test/liveRepoDerivationGuard.test.js`: 24/24.
- `node specs/pipeline/cli.js specs/features/BL-897-briefing-gathers-lifecycles-once.feature`:
  7/7 ok.
- `git diff --stat main...HEAD -- <both Scope files>`: matches exactly.

Independently re-verified the "never touches the live checkout" claim by
direct measurement, not just reading the code: captured the live
`.swarmforge/briefing/lifecycle-snapshot.json`'s mtime before and after a
real run of the real (fixed) test — byte-identical mtime, confirming
nothing was written.

Hand-mutation (revert test 4 back to `path.join(__dirname, '..', '..')`
as its root, keeping everything else): the test itself still reported
4/4 PASS — its own `walked === true` assertion is satisfied either way
in this worktree's current state, so the runtime assertion alone is not
what proves the fix. Two independent, stronger signals were: (1) the
one test's own duration jumped from 38ms to 5.66s (a live, uncached full
backlog walk) — under different host load this reproduces BL-1717's
original 16.1s-in-suite budget breach exactly; (2) the mutant's CLI run
WROTE the live `.swarmforge/briefing/lifecycle-snapshot.json` in this
worktree (mtime and size both changed) — the exact defect class BL-1741
exists to eliminate, caught by direct observation since the coder's own
"restore in finally" logic no longer exists to mask it. The live file
now legitimately holds today's dayKey (it was stale, dated yesterday,
and any real daemon run today would have regenerated it anyway) — low
risk, gitignored cache, not treated as damage requiring repair.
Restored the test file byte-for-byte (`git status --short` clean)
before continuing.

**A genuinely automated regression guard exists for this exact mutant
class, confirmed independently:** re-applied the same `__dirname` mutant
and ran `liveRepoDerivationGuard.test.js` (BL-1038's standing, repo-wide
scan) alone — it correctly went red, naming
`emitLifecycleSnapshotCli.test.js` and the exact reason ("hands the live
repository root to production code"). This is the guard the coder's own
census edit made room for (removing the file from BL-1038's
`BL-1038-EXEMPT` allowlist, since the pattern the exemption covered no
longer exists) — confirms the census edit is correct in BOTH directions:
the file no longer needs the exemption (no `__dirname` derivation
remains today), AND if one were reintroduced, the guard's general scan
would catch it without needing a new exemption entry. Restored again,
re-ran both files green together (28/28) before finalizing.

Every standing whole-tree guard (this parcel touches `extension/test/`):
`npx vitest run $(ls test/*Guard*.test.js | grep -v '\.property\.')` — 22
files, 211 tests, all green.

Fixture hygiene: `git status --short` clean of anything this pass's runs
could leak; the fixture checkout `mkFixtureCheckout` builds is swept by
`mkTmp`'s standard per-test `afterEach` (no manual cleanup needed, per
the coder's own note).

Not run: `npm run test:properties` (2026-09-17 rule — this parcel
touches no `*.property.test.js`) and Stryker/CRAP (no `src`/`out` file
in Scope).

By hardender.
