# Evidence — BL-1321 — coder pass (2026-09-02)

Role: coder. Ticket: BL-1321-swarm-stamp-seated-preferred-yield-3d70c0f4ec.
Routed by coordinator note `20260902T135251Z_003426`.

Review-only stamp-off of Cursor hotfix `3d70c0f4ec` ("Yield chase rotate when
preferred role is already seated"). This parcel CONFIRMS the land. It
reimplements nothing, re-line-ends nothing, removes no landed assert, and
writes nothing to the ledger.

## Invariants held, verified not asserted

- **inv 1 — never reimplements.** `git status` over `mono_router_lib.bb`,
  `mono_router_lib_test_runner.bb`, `handoffd.bb` and
  `backlog/hotfix-ledger.yaml` is EMPTY at handoff. The whole diff is the
  acceptance handler, its bb decision driver, the registration line, and this
  file.
- **inv 2 — green never certifies.** Scenario 05 asserts the ledger row is
  still awaiting a human AND that the ledger file is unmodified in the working
  tree. Row for `3d70c0f4ec` reads `state: stamp-open`,
  `human_decision: null`, `stamp_ticket: BL-1321`.
- **inv 3 — the yield NARROWS the redirect, never replaces it.** Executed, not
  argued: scenario 03's step drives the real gate a second time with the
  marker naming `coder` while preferred is `QA`, and asserts
  `{redirect, QA}` still comes back. BL-795 fires wherever preferred is not
  the seated role.

## qa_e2e procedure — all six items walked

1. **`chase-rotate-decision` exported; redirect requires preferred to differ
   from BOTH.** Confirmed at `mono_router_lib.bb:508-534`: the redirect arm is
   `(and preferred (not= preferred poked-role) (not= preferred active-role))`.
   EXECUTED for every Examples row, not read.
2. **`handoffd.bb` dispatches on `:action`, logs the yield on that path only.**
   Confirmed at `handoffd.bb:1504-1536`: `chase-rotate-to!` builds `decision`
   from `mono-router-lib/chase-rotate-decision` and `case`s on `(:action
   decision)`; `chase-rotate-redirect` is logged on the `:redirect` arm only,
   and `chase-rotate-seated-preferred-yield` inside the `:rotate` arm guarded
   by `(and preferred active (= preferred active) (not= preferred role))`.
   This satisfies the ticket's first `required_wiring` anchor — the live
   daemon chase path really does call the reviewed gate.
3. **`bb mono_router_lib_test_runner.bb` → `ok`.** The six seated-yield
   asserts are present at lines 902-930. **Two are byte-identical**, as the
   specifier flagged: assert 1 ("seated preferred yields: poke specifier →
   rotate specifier") and assert 6 ("marker-active preferred yields even when
   live identity would be unreadable") pass the SAME map
   (`preferred "QA"`, `poked-role "specifier"`, `active-role "QA"`,
   `poked-actionable? true`) and assert the same `{:action :rotate :target
   "specifier"}`. Six asserts, **five distinct cases**. Harmless but the
   operator note's "6 asserts" overstates coverage by one. Reported; the
   ticket forbids deleting a landed assert and none was deleted.
4. **`bash test_handoffd_rule_proposal_rotate_wiring.sh` → ALL PASS.** The
   BL-795 redirect regression is still green.
5. **Line-ending normalisation — the exact list, confirmed byte-identical.**
   `swarmforge/scripts/mono_router_lib.bb` and
   `swarmforge/scripts/test/mono_router_lib_test_runner.bb`, and only those
   two. `git diff 3d70c0f4ec HEAD` over each is EMPTY, so they are exactly
   what the commit landed; neither has regained CRLF. Independently confirmed
   the specifier's framing: those two were the ONLY CRLF files under
   `swarmforge/scripts` at the parent commit (scanned every `.bb`/`.sh` there
   at `3d70c0f4ec^`), and the tree now has zero. The apparent 1344/1839
   changed lines are the conversion; `git diff --ignore-cr-at-eol --shortstat
   3d70c0f4ec^ 3d70c0f4ec` reports the real change: **96 insertions, 34
   deletions**. Scenario 04 asserts that figure, so a later drift in what this
   commit is understood to contain fails the review rather than passing quietly.
6. **`Hotfix-Certification: pending` left standing.** No ledger write.

## Human ruling applied

`human_ruling` selects option 1: "Accept the line-ending normalisation as
landed and open no follow-up". So this parcel reports the normalisation
(scenario 04) and mints nothing. Recording for the record that the standing
hazard the specifier named is real and unaddressed by that choice: the repo
has no `.gitattributes`, so nothing prevents CRLF recurring on these or any
other file. That is the human's accepted trade, not an oversight of this
review, and no follow-up was opened.

## Scenario 03 — the stale-marker direction, stated rather than assumed

The gate reads the `.swarmforge/mono-router-active-role` MARKER and
deliberately never the live resident identity — its own docstring says
"Live-role is intentionally NOT consulted here (BL-921 already-active gate
stays separate)". The handler asserts that sentence is present in the landed
source, so a silent change of that design fails the review.

The recorded consequence, executed: a marker that WRONGLY names the preferred
role as seated turns a BL-795 redirect into a rotate onto the poked role.
**That is the safe direction.** The poked actionable role still gets the
resident, so dependency mail moves; the failure costs a missed redirect, not
a stall. The unsafe direction — a marker wrongly naming someone else as
seated — merely restores the pre-hotfix redirect behaviour, which is
already-active-guarded downstream. Neither direction reintroduces the
deadlock this hotfix fixed, which is what makes the marker safe to depend on
despite its named staleness hazard (`relaunch_resume_cli.bb`'s "BL-1020
STALE").

## An honest limit on scenario 02

Scenario 02 EXECUTES the gate (confirming `:rotate` really is chosen for that
input) and asserts the landed dispatch logs the yield with `(preferred, role)`
under exactly the guard above. It does NOT observe the log line emitted by a
live daemon: driving `chase-rotate-to!` for real needs a live socket and tmux,
which is the environmentally unsuitable boundary this project keeps out of the
unit/acceptance suites. Stating the limit rather than letting "the daemon logs
a seated-preferred yield" imply more than was executed.

## Checks run this pass

- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1321-...feature` — **9/9 pass** (was 9 failing: no step
  handler existed). New handler
  `specs/pipeline/steps/bl1321SeatedPreferredYieldStampSteps.js` + driver
  `specs/pipeline/steps/lib/bl1321ChaseRotateDecisionCli.bb`, registered in
  `specs/pipeline/steps/index.js` — the ticket's second `required_wiring`
  anchor, now satisfied.
- `bb swarmforge/scripts/test/mono_router_lib_test_runner.bb` — ok.
- `bash swarmforge/scripts/test/test_handoffd_rule_proposal_rotate_wiring.sh`
  — ALL PASS.

Scenario 01 drives the REAL `chase-rotate-decision` out of the REAL
`mono_router_lib.bb` for all five Examples rows, through a bb driver that
`load-file`s the library rather than restating its logic. A source-text
assertion cannot tell a wired gate from a dead one, and the deadlock under
review was a redirect that read correctly and sent the resident nowhere.

The Outline validates `<preferred>`/`<poked>`/`<seated>`/`<actionable>`/
`<action>`/`<target>` against explicit KNOWN_VALUES tables and throws on an
unknown value — no passthrough. `"none"` is the sanctioned absent marker.

## No property tests, and why that is not a gap

Three declared invariants. Invariants 1 and 2 quantify over this parcel's own
conduct (process, not a pure function) and are encoded as executable
assertions over repository state — git status of the reviewed files, the
ledger row. Invariant 3 IS a property of a pure function, and is encoded as a
direct executable assertion in scenario 03 rather than a generated property:
the gate's input space here is four small enumerated fields, so the Examples
table plus the explicit redirect re-check covers it exhaustively rather than
sampling it. Recording the reason rather than leaving a declared invariant
silently unencoded.

## Out of scope, untouched

- The hotfix, the active-role marker, `preferred-mono-rotate-role`,
  `attempt-resident-rotate!`'s already-active gate — all forbidden by
  `constraints`. No defect found warranting the narrow follow-up allowed for.
- The duplicated sixth assert — reported above, deliberately not removed.
- The line endings — reported, deliberately not reverted.
- `swarmforge/scripts/open_swarm_spy_router.sh` and
  `spy_router_pane_label.sh` — untracked, not created by this parcel.
