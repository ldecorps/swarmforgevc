# BL-1287 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## Cleaner-stage question the ticket posed directly

The ticket's own "How" section named this as "a cleaner-stage question, not
a coder mandate": whether `bl857TunnelOwnershipInvariants.property.test.js`'s
local `sweepLeakedFixtureTunnels()` wrapper (iterate the pids
`leakedFixtureTunnelPids` returns, SIGKILL each, catch an already-gone
race) belongs in the shared helper now that selection carries a real
decision.

**Judgment: leave it where it is.** `grep -rl leakedFixtureTunnelPids
extension/test/` shows bl857 is the ONLY caller that sweeps-and-kills; the
other three callers (BL-1287's own property test, BL-1061's property
test, the new unit test) call `leakedFixtureTunnelPids` directly to
inspect its selection, never to act on it. There is no duplication to
eliminate — a single 12-line function with one caller is not a DRY
violation — and moving it would not change what is tested or how: the
"real decision" this ticket added (creator-liveness) already lives inside
`leakedFixtureTunnelPids` itself, in the shared helper, fully covered by
`fixtureTunnelName.test.js` and the new property test. The wrapper's own
job (act on an already-correct list) is unrelated to that decision and is
exactly the kind of thin, cohesive, single-use glue that stays local per
this role's own architecture guidance — moving it would relocate code
without improving structure or separation of concerns.

## What else was checked

- Confirmed the diff is additive and layered correctly: the new
  creator-liveness filter (`creatingPidFor`/`isProcessAlive`) is chained
  AFTER the pre-existing temp-path/cloudflared/`run` filter, never
  replacing it — invariant 3 (temp-path-only reachability) is structurally
  preserved, not merely tested.
- Confirmed the fallback posture (`creatorPid === null` → still selected)
  correctly preserves invariant 2 (a fixture whose name doesn't carry the
  known shape is swept regardless, matching the pre-BL-1287 behavior) —
  read directly in the diff.
- Verified the real concurrent-overlap regression proof directly: ran
  `bl857TunnelOwnershipInvariants`, `bl787NamedTunnelInvariants`, and
  `bl1061TunnelFixtureIsolation` together, full files (no `-t` filter):
  12/12 pass, with genuine fork-pool overlap (bl787's own ~30s invariant 3
  ran concurrently with bl857's ~15s run) — the exact scenario this ticket
  exists to fix.
- Confirmed the coder's own found-and-fixed regression in the pre-existing
  `bl1061TunnelFixtureIsolation.property.test.js` (its synthetic "leaked"
  fixtures previously embedded the live test process's own pid as creator,
  which the new liveness check would have wrongly read as a live sibling)
  is correctly repaired via a dedicated dead-pid helper — re-ran directly,
  5/5 pass.
- Confirmed the coder's own found-and-fixed `check_property_suite_drift.sh`
  violation (the new property test's own fixture temp-dir allocation not
  routed through the shared `mkTmpDir()`) is correctly fixed — ran
  `bl1280MkdtempMigrationInvariants.property.test.js` directly: 6/6 pass.
- `npm run compile` clean.
- No duplication: `jscpd` reports 0 clones across the three touched/new
  `.js` files.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `npx vitest run test/helpers/fixtureTunnelName.test.js` | 6/6 pass |
| `npx vitest run --config vitest.properties.config.mjs test/bl1287FixtureSweepScopingInvariants.property.test.js` | 3/3 pass |
| `npx vitest run --config vitest.properties.config.mjs test/bl857TunnelOwnershipInvariants.property.test.js test/bl787NamedTunnelInvariants.property.test.js test/bl1061TunnelFixtureIsolation.property.test.js` (concurrent overlap proof) | 12/12 pass |
| `node specs/pipeline/cli.js specs/features/BL-1287-...feature` | 4/4 |
| `npx vitest run --config vitest.properties.config.mjs test/bl1280MkdtempMigrationInvariants.property.test.js` (regression) | 6/6 pass |
| `npm run compile` | clean |
| `bl1287FixtureSweepSparesLiveRunSteps.js::registerSteps` exists | yes (required_wiring) |
| `npx jscpd` on the touched/new files | 0 clones |

No fix required. Forwarding unchanged to architect.
