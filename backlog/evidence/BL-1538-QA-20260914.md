# BL-1538 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1538` → `VERIFY BL-1538` (exit 0),
  no open deferral.
- Straggler check before/after: `pgrep -fl 'node --test|stryker'` matched
  only unrelated shell processes both times; no leftover fixture/mutation
  process.
- `pre_qa_gate.sh BL-1538-the-bl1028-property-runner-fixture-carries-its-bb-closure HEAD`
  → `OK` (both `required_wiring` entries present: the closure guard's
  `FIXTURES` map carries the runner under `kind: 'bb-copy'`; the step
  handler is auto-discovered by `specs/pipeline/steps/index.js`'s
  `*Steps.js` suffix scan, BL-1371 — no hand-registration needed or found).
- Full unit suite (`npm test`, extension/): first run 615/616 files passed,
  1 failure in `test/telegramCursorOperatorExec.test.js` (`ENOTEMPTY` on
  `sweepPendingTmpDirs`/`/tmp/bl1204-*`) — this parcel's diff never touches
  that file or its fixtures. Isolated re-run of that file: 24/24 clean.
  Same signature already recorded as a full-suite-concurrency flake in
  `BL-1501-QA-20260911.md`, `BL-1526-QA-20260911.md`,
  `BL-1444-QA-20260906.md`, `BL-1498-QA-20260913.md`. Full-suite re-run:
  616/616 files, 10479/10479 tests clean.
- `npm run test:properties`: 403/403 files, 1189/1189 tests passed. One
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled error — the
  sole allowlisted `test:properties` error (BL-871).
- `bb swarmforge/scripts/standing_red_register_cli.bb .`: this ticket's own
  row (`bb`/`bl1028_promotion_refusal_property_runner.bb`, owned=true) is
  the only entry touching this parcel's files. One unrelated unowned row,
  `extension/test/bl1313BatchGuardVisibilityInvariants.property.test.js`,
  is a stale `property_suite_standing_allowlist.tsv` entry left over from
  BL-1503 (closed 2026-09-13, comment says "this row leaves with its
  land" — it didn't). Confirmed the file is actually green: isolated run
  3/3, and clean in the full property-suite run above. Not a red this
  parcel's evidence names; not this ticket's file; out of scope to fix
  here.
- Acceptance (`run_acceptance.sh` on the ticket's feature): 9/9 scenarios
  green (Scenario Outline's 5 Examples included).
- `bb swarmforge/scripts/test/bl1028_promotion_refusal_property_runner.bb`
  run twice directly: both `ALL PROPERTIES HOLD`, coverage shows
  `:control-commit 1` both times — the second run does not depend on state
  the first left behind.
- Enrollment confirmed by grep:
  `specs/pipeline/steps/lib/bbFixtureClosureGate.js` carries
  `swarmforge/scripts/test/bl1028_promotion_refusal_property_runner.bb`
  under `kind: 'bb-copy'`, whose `bbCopyList()` runs the fixture with
  `--copy-into <dir>` — a behavioural read, never a source-text parse
  (invariant 2).
- Read the coder's diff to the runner: `promotion-gate-deps` is now
  `(bb-load-closure-lib/compute-closure scripts-dir "promotion_gates_cli.bb")`
  — no hand list — used both by `make-fixture!`'s copy and the new
  `--copy-into` flag (invariant 1). No production line of
  `promote_and_route_next.sh`, `promotion_gates_lib.bb` or
  `promotion_gates_cli.bb` touched (constraint honored).
- Ancestry: `git merge-base --is-ancestor <hardener-merge d9f238036f> HEAD`
  and `... <coder-commit 9774c2126e> HEAD` both confirmed — the commit
  being approved actually contains this ticket's own stage merges, not
  merely a sibling's commit that happens to be an ancestor of HEAD.
- `git rev-list --left-right --count main...origin/main` → `0  0`; no
  divergence to read around. No `bounce_history` on the ticket's YAML, no
  matching record in the master checkout's `.swarmforge/bounces/` store —
  never bounced before.
- Register post-condition (per the ticket's own `qa_e2e_procedure` item 5)
  will be re-checked after landing: this ticket's row is removed from
  `backlog/standing-reds.tsv` as part of the close-out, same as every prior
  standing-red ticket in this series.
