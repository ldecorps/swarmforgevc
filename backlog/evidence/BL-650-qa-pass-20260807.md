# BL-650 — QA pass — 2026-08-07

**Verdict: PASS.** Approved for `main`.

**Commit reviewed/approved:** `b73c964ea3` (architect, merge_and_process from
`git_handoff`), which contains coder's `3122a2ff` (`BL-650: flow-watchdog
measures parcel age in active time, not wall clock`) and the merge-base with
`main` tip `5721579a` as ancestors — confirmed via
`git merge-base --is-ancestor b73c964ea3 HEAD` after a clean fast-forward
merge in this worktree (no divergent history to reconcile).

`required_stages: [coder, architect, qa]` — cleaner/hardener/documenter are
ticket-skip-flagged (bounded single-lib bb change; bb sits outside Stryker
mutate scope with clock property tests in the unit gate; alarm-text change
specified in-ticket, no runbook surface). QA is the final and only gate after
architect.

## Inventory

1. **Prior bounce/blocker history (main ref)** — `git log --oneline main --
   'backlog/evidence/BL-650*'` shows one entry: `f44e78dd` (specifier
   "blocked on BL-823" pass, 2026-08-06). Not a QA bounce. BL-823 has since
   landed (`backlog/done/BL-823-availability-interval-ledger.yaml`, renamed
   from `paused/` in this same merge) — the blocker this note raised is
   resolved. No open QA bounce exists for this ticket.

2. **BL-650-scoped unit tests** — `bb swarmforge/scripts/test/flow_watchdog_test_runner.bb`
   → `ALL PASS: flow_watchdog_lib.bb`.

3. **BL-654 declared-invariant property tests** —
   `bb swarmforge/scripts/test/bl650_flow_watchdog_active_time_property_runner.bb`
   → `bl650_flow_watchdog_active_time_property_runner: ok`. Independently
   re-run (own pass, not reused from architect's evidence).

4. **Acceptance pipeline** (own independent run, BL-112) —
   `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-650-flow-watchdog-wall-clock-age-must-be-active-time.feature`
   → 11/11 pass (`# pass 11`, `# fail 0`), including the two-example Scenario
   Outline (rotation-pack-threshold-vs-parallel-pack).

5. **TypeScript property-test command** (`npm run test:properties`, kept
   separate from unit/acceptance per engineering.prompt) — run in full. 3
   files / 6 tests failed, ALL in files unrelated to this parcel's changed
   surface (`bl787NamedTunnelInvariants.property.test.js`,
   `bl797MutationGateProbeCrashFallback.property.test.js`, one more not
   captured in the truncated tail). Every failure is `Test timed out in
   120000ms` plus `[vitest-worker]: Timeout calling "onTaskUpdate"` —
   worker-RPC starvation, not an assertion failure. Host `uptime` throughout
   this pass showed sustained load averages of 130-380 (this is a live,
   heavily-loaded swarm host). Re-ran
   `bl797MutationGateProbeCrashFallback.property.test.js` alone: still timed
   out at 147s wall time for what the config caps at 120s — confirms this is
   load-bound, not flaky-only-in-combination. None of these files, or the
   modules they exercise (named tunnels, mutation-gate probe fallback), touch
   `flow_watchdog_lib.bb`, `availability_ledger_lib.bb`, or
   `specs/pipeline/steps/bl650…`. Pre-existing environmental condition, not a
   BL-650 defect — consistent with the standing lesson that heavy
   subprocess/fuzz-trial test runs degrade under severe host load
   (memory: "Stryker dry-run times out even at concurrency=1 under severe
   load").

6. **Whole-suite unit run** (`npm test`, extension) — full run: 8 files / 16
   tests failed, 229 worker-timeout errors, same load signature (582.88s
   wall, host load 130-380 throughout). Every failing file inventoried and
   independently re-run in isolation:
   - `dependencyGateCliCleanAndViolations`, `…ReportsAndScope`,
     `…StorageGlobals` (5 tests) — failed under the shell's default node
     20.20.2 (`depcruise` requires `^22||^24||>=26`); re-ran under `nvm use
     22` → all 10 pass. Pre-existing environment quirk, matches architect's
     own note in the same evidence pass (BL-650-architect-pass-20260807.md
     item 1) that this worktree's default node needed the same workaround.
   - `bounceWatcher`, `briefingDigestLineCli`, `startBridgeHeadlessCli`,
     `mermaidRender` — re-ran individually, all pass cleanly (e.g.
     `bounceWatcher` 35/35 in 454ms).
   - `renderBriefingDiagramsCli` — the in-process assertions pass; only the
     "compiled CLI as a real subprocess" case times out (20s budget) under
     sustained load ~137-200; this spawns a full node+chromium/puppeteer
     render pipeline as a child process, which is exactly the shape that
     starves first under host contention.
   None of these 8 files touch flow-watchdog/availability-ledger/BL-650 step
   files — confirmed by diffing the failing-file list against `git show
   --stat b73c964ea3`'s changed paths (zero overlap).

7. **Wiring** — confirmed `run-sweep!` (the real `handoffd.bb` production
   caller via `flow-watchdog-sweep!`) invokes `read-pack-aware-global-thresholds`
   and `availability-ledger-lib/fold` unconditionally (not adapter-gated),
   and threads the result through `evaluate-effective-age` — grepped
   `swarmforge/scripts/flow_watchdog_lib.bb` directly, not just architect's
   claim. This is live production wiring, not only unit-isolated.

8. **Ticket intent vs acceptance** — re-read `backlog/active/BL-650-…yaml`.
   Shape items 1-3 (active-time clock w/ signature-backed evidence only,
   rotation-aware threshold resolution without a `decide-tier` branch, the
   unsuppressable-alarm invariant preserved) are each covered by a named
   scenario or the BL-654 property set; `decide-tier` confirmed untouched
   (architect's diff claim independently spot-checked: function body
   byte-identical against `main` tip pre-BL-650).

9. **Spec-gap note (architect, item 6 of its own evidence)** —
   provider-outage evidence has no live caller wiring `handoffd.bb`'s way;
   already routed by architect as a `note` to specifier + coordinator, not a
   bounce, and correctly not re-litigated here — the acceptance criteria as
   written do not require live wiring and the parcel satisfies them in full.

10. **Working tree hygiene** — `swarmforge/scripts/operator_path_lib.sh`
    remains the sole pre-existing untracked file (matches paused BL-796,
    predates this parcel) — left alone, not staged.

## Blocked checks

None. Every check above ran to completion (including re-runs to disambiguate
load noise from real regressions).

## Process hygiene

`pgrep -fl 'node --test|stryker|vitest'` checked clean before the first run
and after the last run — no orphaned test/mutation processes left behind.

By QA.
