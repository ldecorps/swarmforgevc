# BL-1494 — QA review pass, 2026-09-10

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: cd extension && npx vitest run test/tempDirTrapGuard.test.js
- **Commit hash**: f69a244bdc
- **First error excerpt**: tempDirTrapGuard real-tree scan: swarmforge/scripts/test/test_bl1494_deferred_note_no_wake.sh: creates a temp root (mktemp -d) but has no EXIT trap and does not source lib/tmp_cleanup.sh
- **Failure class**: unit
- **Expected vs observed**: expected zero temp-dir-trap violations under swarmforge/scripts; observed scenario_05 (line 125) leaks its mktemp -d ROOT under errexit if any command before the unconditional rm -rf at line 154 fails
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/test/test_bl1494_deferred_note_no_wake.sh scenario_05 (~line 125): add trap 'rm -rf "${ROOT:-}"' EXIT immediately after ROOT=$(mktemp -d), matching test_chase_sweep.sh line 44 idiom

## D2

- **Failing command**: git diff main...HEAD -- swarmforge/scripts/test/suite-manifest.tsv; grep -rn bl1494_deferred_note_no_wake_property_runner --include=*.tsv --include=*.js --include=*.bb --include=*.sh .
- **Commit hash**: f69a244bdc
- **First error excerpt**: suite-manifest.tsv gains rows for test_bl1494_deferred_note_no_wake.sh and bl1494_post_qa_sweep_wake_field_test_runner.bb but not for bl1494_deferred_note_no_wake_property_runner.bb; grep for that filename across .tsv/.js/.bb/.sh returns nothing
- **Failure class**: unit
- **Expected vs observed**: expected the coder-authored property runner proving the ticket's own two declared invariants to be wired into the standing suite like its sibling runner; observed it is unreferenced anywhere and never runs, though it passes when invoked directly (ALL PROPERTIES HOLD, confirmed by hand)
- **Blamed role**: coder
- **Remediation pointer**: add a bl1494_deferred_note_no_wake_property_runner.bb / standing row to swarmforge/scripts/test/suite-manifest.tsv, same shape as the existing bl1479_parked_active_sweep_test_runner.bb row

By QA.

## Detail

Full checklist run before D1/D2 were isolated (Article 4.4 — no first-failure
stop):

- `qa-sibling-check.js status --ticket BL-1494`: `VERIFY BL-1494`, no open
  deferral.
- Bounce history: `main`==`origin/main` (0/0 rev-list), no
  `bounce_history` on the ticket YAML — clean prior record (BL-340/BL-891).
- Merged documenter's `ffff5e13c2` into this worktree clean (no conflicts).
- No orphaned `node --test|stryker` processes before or after.
- Full unit suite (`npm test`, extension/): 612/614 files green. Two files
  red:
  - `test/operatorRuntimeBbFixtureClosure.test.js` — `respawn_bootstrap_lib.bb`
    missing from `OPERATOR_RUNTIME_BB_FILES`. Confirmed via
    `git diff main...HEAD --name-only`: not touched by this parcel.
    Pre-existing (hotfix 32fb1ff7e1, 2026-09-09, same root cause as
    BL-1503's fixture). No `standing-reds.tsv` row names this specific
    unit-lane file, but it is the exact drift pattern BL-1449 (open, todo)
    exists to eliminate by construction ("the export is computed... so a
    new load-file is in the fixture the moment it exists and the list can
    no longer drift") — treated as owned by BL-1449, not blocking. Worth
    the specifier adding an explicit register row for traceability.
  - `test/tempDirTrapGuard.test.js` — 3 violations: `landed_ticket_autoclose_test_runner.bb`
    and `wake_dedup_lib_test_runner.bb` are BL-1501 (registered,
    `standing-reds.tsv`, pre-existing, confirmed outside this diff) — not
    blocking. `test_bl1494_deferred_note_no_wake.sh` is D1 above — this
    parcel's own coder commit `2d6b91b046`, confirmed via
    `git log --all -- <path>`.
- Article 4.5 changed-path gate: `git diff main...HEAD --name-only` touches
  no `extension/src|test` path (Babashka/docs/feature-only ticket,
  `mutation_cost: medium` per the ticket's own Babashka framing, BL-472) —
  nothing additional to run.
- `npm run test:properties`: full-suite run 1 showed 6 files red; isolating
  each individually (same host-contention pattern BL-1497 already
  established on this same commit range) separated real from transient:
  - `bl1089FrontDeskLivenessFixture` — deterministic, now owned by BL-1502
    (registered, `standing-reds.tsv`) — not blocking.
  - `bl1313BatchGuardVisibilityInvariants` (both invariants) —
    deterministic, owned by BL-1503 (registered) — not blocking.
  - `bl1272LandedSiblingInvariants`, `bl1373PathSetCacheInvariants`,
    `bl1429StandingRedThrottleFoldInvariants`, `bl604TrendAnalysisInvariants`
    — each isolated individually (`npx vitest run --config
    vitest.properties.config.mjs test/<file>`), all green in isolation —
    confirmed transient/contention-flaky, not real defects, no ticket owed.
- Acceptance: `run_acceptance.sh specs/features/BL-1494-*.feature` — 7/7
  scenarios green.
- `test_chase_sweep.sh` (touched by this parcel) — 17/17 PASS including
  the new scenario 17 (deferred note held, not chased).
- `bl1494_post_qa_sweep_wake_field_test_runner.bb` — ALL PASS (4/4).
- `bl1494_deferred_note_no_wake_property_runner.bb` — ALL PROPERTIES HOLD
  when invoked directly (see D2 — never runs standing).
- `required_wiring`: handler registration is auto-discovered since BL-1371
  (`specs/pipeline/steps/index.js`), proven by construction via the
  acceptance run above. `deliver-notify-skip-deferred` tag present at
  `handoffd.bb:491`.
- Diagram currency: `docs/diagrams/handoff-flow.mmd` gains the
  `DEFERCHECK`/`DEFERRED` nodes matching the new delivery-hop decision —
  the change-trigger (wake decision, BL-1494's own subject) correctly
  fired. `docs/how-to/BL-668-*.md` updated for the sweep's new tell
  behaviour. `swarmforge/handoff-protocol.md` `wake` field prose already
  landed by the specifier per the ticket's own `approval_context`.

**Disposition**: bounce D1+D2 to coder (both trace to commit `2d6b91b046`,
ordinary implementation/hygiene defects in the coder's own new files — not
docs, not architecture, not a coverage/CRAP gap the hardener owns).

By QA.
