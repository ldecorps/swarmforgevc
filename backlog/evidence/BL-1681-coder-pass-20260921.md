# BL-1681 — coder pass, 2026-09-21

## What changed

1. `specs/features/BL-696-miniapp-lets-talk-cursor-audio.feature`:
   retired scenario 06 ("a transient speech-to-text failure is
   recoverable and does not wedge the session") whole, its `# BL-696
   lets-talk-06` tag included - never reworded (BL-1006). Feature now 7
   scenarios.
2. `specs/features/BL-696-telegram-cursor-bridge-operator-commands.feature`:
   retired scenario tg-op-04 ("/redeploy compiles and restarts the
   supervised bridge") whole, its `# BL-696 tg-op-04` tag included.
   Re-tensed the narrative comment listing /redeploy among operator
   skills to name BL-702's soft confirm. Feature now 19 scenarios.
3. `specs/pipeline/steps/bl696LetsTalkSteps.js`: removed the
   now-orphaned "speech-to-text fails transiently once then succeeds",
   "the page shows conversation state \"error\" only while retrying",
   and "the turn eventually completes with a spoken reply" step
   definitions - confirmed via `grep -rn` over every `.feature` file
   that none has any remaining scenario using them.
   `submitTurnWithClientRetry` is NOT removed - two live scenarios
   (lets-talk-02, lets-talk-03/04) still call it.
4. `specs/pipeline/steps/bl696TelegramCursorBridgeOperatorSteps.js`:
   removed the now-orphaned "the bridge decision is to redeploy" and
   "the bridge posts a redeploy started confirmation" step definitions
   - same grep confirmation.
5. New `specs/features/BL-1681-bl696s-two-successor-left-scenarios-retire.feature`
   (already minted by the specifier at ticket time - read and built
   against verbatim, never overwritten) and its handler
   `specs/pipeline/steps/bl1681Bl696SuccessorLeftRedsSteps.js`
   (required_wiring: same parcel, BL-233/BL-1371). Scenario 01 drives
   the REAL bridge server (startBridge) through the real /lets-talk/turn
   route twice, with a stub STT that fails once then succeeds - the
   surviving half of lets-talk-06's claim, restated over the route
   response rather than page-script text. Scenario 02 spawns
   `node specs/pipeline/cli.js` against each of BL-696's own two
   feature files in a child process and reads the real TAP output: all
   scenarios pass, the retired scenario name is absent, the exact
   remaining count (7, 19) is pinned.

## No production change

`git diff --stat origin/main -- extension/src` is empty - neither
feature's underlying production behaviour is touched. The page's
inline-error UX (c0f7d4b57a) and the soft-confirm /redeploy (BL-702)
remain exactly as they landed 2026-07-29; only the two stale-asserting
scenarios and their now-orphaned step definitions are removed.

## Invariants (BL-654)

`invariants: []` - none declared. No property test obligation.

## Verification

- `node specs/pipeline/cli.js` on `BL-696-miniapp-lets-talk-cursor-audio.feature`:
  7/7 ok, no scenario named the retired one.
- `node specs/pipeline/cli.js` on `BL-696-telegram-cursor-bridge-operator-commands.feature`:
  19/19 ok, no scenario named the retired one.
- `node specs/pipeline/cli.js` on this ticket's own feature: 3/3 ok.
- `grep -rn "decision is to redeploy\|redeploy started confirmation\|only while retrying" specs/features/ specs/pipeline/steps/`:
  empty.
- `git diff --stat origin/main -- extension/src`: empty.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4 pass (the
  new handler's `mkFixture()` calls `sweepStaleTmpDirs(...)` inside the
  function, never at module load - satisfies both the tmp-root
  registration guard and the BL-968 module-load-purity guard).
- `npx vitest run test/stepHandlerModuleLoadBudget.test.js`: green (the
  one violator named across today's runs,
  `bl1412SpecTreeTextFilterSteps.js`, is untouched by this parcel,
  explicitly out-of-scope per BL-1658's own ticket text, and reported
  separately - `unowned-red-bl1412-borderline-load-flake-coder-20260921.md`).
- `npx vitest run test/bl968StepRegistryMaterializedTreeGuard.test.js`:
  3/3 pass.
- `npm test` (extension/, full unit lane): 635/635 files, 10848/10848
  tests pass (clean run after the bl1412 host-load flake was confirmed
  transient in isolation and reported separately).
- `npm run test:properties`: green.

By coder.
