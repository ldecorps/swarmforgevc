# BL-1671 — QA review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `bash swarmforge/scripts/test/test_merge_deletion_guard.sh`: 18/18 PASS,
  including the new `unstaged-worktree-deletion-is-not-a-finding` case
  (BL-1671's own scenario) alongside the pre-existing BL-1242/BL-1341/BL-1662
  cases, unchanged.
- `npx vitest run --config vitest.properties.config.mjs test/bl1671MergeDeletionGuardInvariants.property.test.js`:
  2/2 pass — the invariant that the guard's verdict depends only on the
  indexed change, never an unrelated unstaged working-tree deletion.
- `node specs/pipeline/cli.js specs/features/BL-1671-the-merge-deletion-guard-reads-the-index-not-the-working-tree.feature`:
  4/4 ok.
- Wiring: `specs/pipeline/steps/bl1671MergeDeletionGuardReadsTheIndexSteps.js`
  exports `registerSteps`, landed in the same parcel as the feature
  (BL-233/BL-1371 pin), confirmed present.
- Diff review: the fix is the two-line change the ticket describes — add
  `--cached` to both `collect_deletions` diff invocations in
  `check_merge_deletion.sh`, plus the corrected header comment. No scope
  creep; nothing under `swarmforge/runtime/` touched.
- `npm test` (extension/, after `npm run compile`): 635/635 files,
  10836/10836 tests passed — no failures (the previously-registered
  `stepHandlerModuleLoadBudget.test.js`/BL-1658 red from BL-1666's pass is
  clear now).
- `npm run test:properties` (extension/): 435/435 files, 1272/1272 tests
  passed; two known-benign BL-871 `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled errors, no failures.
- `pre_qa_gate.sh BL-1671 be5f72ed67`: OK (subject-only warnings on other
  role branches, no path overlap with this parcel).
- No straggler test/mutation processes before or after.
- `qa-sibling-check.js status --ticket BL-1671`: VERIFY (no open deferral).
- No standing-red register/allowlist row names BL-1671; no prior bounce
  history for this ticket.
