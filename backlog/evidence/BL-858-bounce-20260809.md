# BL-858-boot-prefix-over-cap-regression — QA bounce — 2026-08-09

## Commit checked

`407d017d8b` (coder, "wire step handlers for the boot-prefix-cap acceptance
feature"), merged into QA's branch. This ticket declares `required_stages:
[coder, qa]` (BL-606 dynamic routing, `required_stages_routing_enabled
true` confirmed in `swarmforge.conf`) with `stage_skip_reasons` for
cleaner/architect/hardener/documenter matching the handoff envelope's
`routing_skipped` header exactly — a legitimate skip, not a misroute.
Because documenter is skipped by design, QA is the only remaining check on
anything a documenter pass would normally catch.

## Checks run (Article 4.4 complete inventory)

- `bb swarmforge/scripts/test/prompt_engine_test_runner.bb`: `stable-prefix
  chars: 43757`, `ALL PASS` — under the 44000 headroom-budget target and
  well under the 51200 cap.
- Acceptance, BL-858's own feature (`specs/features/BL-858-boot-prefix-back-
  under-cap.feature`, the LIVE file — see D1 below on why the ticket's own
  `acceptance:` pointer could not be trusted for this): 6/6 scenarios PASS.
- Acceptance, BL-618's existing feature (`specs/features/BL-618-stable-
  prefix-cap-regression.feature`): 4/4 PASS — confirmed un-red as a side
  effect of this trim, per the ticket's own notes.
- Invariant 1 (no normative rule text lost): spot-checked manually, not
  taken on the acceptance scenario's word alone. Diffed
  `articles/02_handoffs.md` at the pre-BL-858 base (`1fae3831`) against
  current, took a substantial removed paragraph (the per-role draft-file-
  location rule), and confirmed it appears verbatim in
  `articles/reference/02-handoffs-detailed.md`. Matches.
- Invariant 2 (cap unchanged): `prompt_engine_test_runner.bb` still asserts
  `(< stable-len 51200)` and separately asserts the literal 51200 itself is
  unchanged (not raised/removed) — both present, unedited assertions.
- Step-handler wiring: `bl858BootPrefixCapSteps.js` registered in
  `specs/pipeline/steps/index.js:404`. `BASE_COMMIT`/`FIX_COMMIT` constants
  (`1fae38318`/`b69856736`) both resolve to real commits; confirmed
  `407d017d8b` (the wiring commit itself) touches only the two step-registry
  files, not constitution content, so `FIX_COMMIT` correctly captures the
  full content diff as a range. `bl618StablePrefixCapSteps.js` confirmed
  byte-identical to the pre-BL-858 base — the ticket's explicit instruction
  not to silently rewrite BL-618's own landed acceptance contract was
  honored.
- `prompt-engine-lib/compose` (the actual production boot-prompt-assembly
  function) is what the runner measures against, not a parallel
  reimplementation — confirmed by reading the runner's own calls.
- No orphaned test/mutation processes before or after this pass
  (`pgrep -fl 'node --test|stryker'` clean).

## Findings

**D1 — stale `acceptance:` pointer, still names the deleted draft file**
- **Class:** `acceptance`. Not a specifier-owned `spec-gap` (Article 4.4's
  routing table) — this is a literal unfinished item from the ticket's OWN
  description, which explicitly instructs: "Materialize it to a live
  `.feature` in the SAME parcel that lands the handlers, **and point this
  ticket's `acceptance:` at the live path** — exactly as BL-618 did
  (cbfec354)." The materialize half happened (`1b7878a0` renamed
  `.feature.draft` -> `.feature`, confirmed via `git log -- backlog/active/
  BL-858...yaml` showing only the promotion commit ever touched the ticket
  YAML — the acceptance pointer update never landed). It is `acceptance`
  because the defect is in the ticket's acceptance-contract wiring itself
  (which file the contract points at), not in the behavior the contract
  verifies.
- **Blamed role:** `coder` — the only implementing role in this ticket's
  `required_stages`. Not `documenter`: documenter is deliberately skipped
  for this ticket by its own `stage_skip_reasons`, and this omission is not
  "missing documentation for a docs-worthy change" (BL-575/BL-576's
  routing class) — it is coder's own explicitly-stated task item, left
  undone in the same parcel that was supposed to land it.
  `backlog/active/BL-858-boot-prefix-over-cap-regression.yaml:142` currently
  reads `acceptance: specs/features/BL-858-boot-prefix-back-under-cap.feature.draft`
  — that path does not exist on disk (`ls specs/features/BL-858-boot-
  prefix-back-under-cap*` shows only the `.feature`, no `.draft`).
- **Remediation pointer:** `backlog/active/BL-858-boot-prefix-over-cap-
  regression.yaml` line 142, change `.feature.draft` -> `.feature`.
- **Why this matters even though I could still verify the ticket:** QA
  located and ran the correct live file this pass, but that required
  knowing to distrust the ticket's own pointer — any downstream tooling
  that resolves `acceptance:` mechanically (a sibling-check, a hotfix-
  ledger script, a future automated QA run) would 404 on a deleted file.
  This is the exact defect class BL-851's documenter caught and fixed one
  parcel ago (`backlog/evidence/BL-851-documenter-pass-20260809.md`'s
  sibling commit, `728c0b0cd3`) — BL-858 has no documenter stage to catch
  it, so it reached QA instead.

No other findings. Everything else in this pass — measured prefix size,
both feature files' full acceptance runs, both declared invariants, the
skip-routing's own stated reasons, step-handler wiring, and BL-618's file
being left untouched — checked out clean on independent verification, not
taken on the commit message's word.

## Bounce

`git_handoff` to `coder`, priority `00`, task
`BL-858-boot-prefix-over-cap-regression [acceptance: pointer still names
the deleted .feature.draft, not the live .feature]`.

Per this ticket's own `required_stages: [coder, qa]`, the fixed parcel
forwards directly back to QA (coder is the only stage between).

By QA.
