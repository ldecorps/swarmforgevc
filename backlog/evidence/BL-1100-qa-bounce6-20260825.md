# BL-1100 QA bounce 6 — 20260825

**Reviewed tip:** documenter `0923676ec3` (not merged — tip impure)
**Task:** `BL-1100-promotion-candidacy-is-decided-by-structured-fields-never-prose`
**Sibling:** `VERIFY BL-1100`
**Prior:** bounce5 `063f372735`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 0923676ec3  # fails
git rev-list --count 0923676ec3..origin/main  # → 1
# dels_on_origin=0
```

**Commit hash checked:** `0923676ec3`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = a08160279 Close BL-999: move to done)
paths=13 dels=0
(+ tip still carries BL-999 in active vs closed-on-main bookkeeping)
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto **current** `origin/main` (post BL-999
close) immediately before handoff. Observed rematch6 already stale.

**Remediation:** `git fetch && git reset --hard origin/main`; restore BL-1100
only; do not resurrect closed BL-999 into active; dels=0; verify ancestry in
the same session as `swarm_handoff.sh`.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
