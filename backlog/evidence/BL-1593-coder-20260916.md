# BL-1593 — coder evidence, 2026-09-16

## The fix

`extension/test/nightClosingCeremonyRotateDocumenterFallback.test.js`'s
`makeFixture()` had two defects, both from hotfix `a27d082c2d` landing
outside the pipeline:

1. `fs.mkdtempSync(path.join(os.tmpdir(), 'ncc-rotate-fallback-'))` (line
   29) — a raw call site, never removed. Now `mkTmpDir('ncc-rotate-fallback-')`
   from `./helpers/tmpDir` (the shape every other test in the directory
   uses), swept by the unit lane's `tmpDirSetup.js` afterEach.
2. A `fs.readdirSync(scriptsDir)` loop symlinking every `.bb` in the live
   `swarmforge/scripts/` (200+ files today). Now
   `copyLiveScriptClosureInto(path.join(root, 'swarmforge', 'scripts'),
   ['consult_spawn_cli.bb'])` from `./helpers/pinnedRepoFixture` — the
   derived closure is 12 files (verified below), grows only with
   `consult_spawn_cli.bb`'s own dependencies, never with the repo's size.

No assertion in the file changed. The stub `rotate_to_role.sh` is still
written after the closure copy, never overwritten.

## Failure text before the change (verbatim, from the ticket's own reproduction)

1. `bl1280MkdtempMigrationInvariants.property.test.js` invariant 2:
   ```
   Expected values to be strictly deep-equal:
   + [ { file: '.../nightClosingCeremonyRotateDocumenterFallback.test.js', line: 29 } ]
   - []
   ```
2. `tmpDirMigrationGuard.test.js` (BL-420):
   ```
   AssertionError: expected zero raw mkdtemp call sites, found:
   .../nightClosingCeremonyRotateDocumenterFallback.test.js:29
   ```
3. `liveRepoDerivationGuard.test.js` (BL-1038):
   ```
   AssertionError: every live-repository derivation must read a pinned fixture or record why it cannot:
     nightClosingCeremonyRotateDocumenterFallback.test.js: enumerates a live repository directory (cost grows with repo size)
   ```

## Green after the change

- `npx vitest run --config vitest.properties.config.mjs
  test/bl1280MkdtempMigrationInvariants.property.test.js` — 6/6 green.
- `npx vitest run test/tmpDirMigrationGuard.test.js
  test/liveRepoDerivationGuard.test.js` — 36/36 green (both files).
- `npx vitest run test/nightClosingCeremonyRotateDocumenterFallback.test.js`
  — 3/3 green.
- No new leak: `ls /tmp | grep -c ncc-rotate-fallback-` unchanged (21) across
  a fresh run of the fallback test before and after (the 21 are pre-existing
  leaks from before this fix landed - not this parcel's to clean up
  retroactively; the fix stops the leak going forward).
- The derived closure for `consult_spawn_cli.bb`: 12 files
  (`ambulance_lib.bb`, `consult_spawn_cli.bb`, `daemon_cycle_guard_lib.bb`,
  `handoff_lib.bb`, `mono_router_lib.bb`, `prompt_engine_lib.bb`,
  `respawn_bootstrap_lib.bb`, `rotation_telemetry_lib.bb`,
  `seat_difficulty_lib.bb`, `self_heal_telemetry_lib.bb`,
  `shell_quote_lib.bb`, `single_role_repair_lib.bb`) — not 200+.

## Acceptance

`swarmforge/scripts/run_acceptance.sh
specs/features/BL-1593-hotfix-landed-ceremony-fallback-test-uses-a-raw-mkdtemp.feature`
— 5/5 scenarios pass (scenario 01 x2 for its two Examples rows).

## Register rows

The three rows (`bl1280MkdtempMigrationInvariants.property.test.js`,
`tmpDirMigrationGuard.test.js`, `liveRepoDerivationGuard.test.js`, all
owned BL-1593) are removed from `backlog/standing-reds.tsv` in this
commit. The now-stale `bl1280...` row in
`swarmforge/scripts/property_suite_standing_allowlist.tsv` (added during
BL-1588's work as a temporary pre-commit-gate bypass while this ticket
was still unlanded) is removed in the same commit — leaving it would
have surfaced as a permanently unowned register row once the
standing-reds.tsv row left. `bb
swarmforge/scripts/standing_red_register_cli.bb .` reports 5 rows, all
owned, `"unowned":[]`.
