# BL-1401 land note — merge-deletion guard blocked a plain merge, 2026-09-05

Same BL-1403 class as BL-1370 (see `backlog/evidence/BL-1370-land-note-20260905.md`
for the full analysis): a plain `git merge af05fbda54` into the QA
worktree was refused unconditionally by `check_merge_deletion.sh` over
the same two unattributed intake-archive files. This ticket's own
history independently confirms the class: a prior pipeline role had to
"Re-drop the two drained root intakes the BL-1401 merge resurrected"
(`8573ad2f7f`) for the identical reason.

Replayed this ticket's own attributed paths (coder/cleaner/architect/
hardener/documenter) as a plain, non-merge commit (`8af39316fb`) into the
QA worktree instead, re-verified e2e/property/BL-632-acceptance green,
then built and landed the tip-pure commit (`990742a43c`).

Land-approval recorded citing `8af39316fb` (the plain replay commit,
an ancestor of `swarmforge-QA`) as source, not `af05fbda54` (never
merged): `{"at":"2026-09-05T02:23:13Z","ticket":"BL-1401","commit":"990742a43c","source":"8af39316fb"}`.
Verified `is_qa_ancestor.sh 990742a43c` prints `approved: ... - BL-1334`.
`abandoned_commits: [af05fbda54]` recorded on the ticket.

By QA.
