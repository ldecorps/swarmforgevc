# BL-1332 — QA approval (20260903)

Received: documenter commit `ad3a0b9c37` (doc-only, D1 fix), forwarding
hardener (assertion-strength gap closed in scenario 04, mutant probes),
architect, cleaner, coder's option-1 implementation (per human ruling) of
the shared-path refusal in `land_step_lib.bb::own-paths`. Merged into QA.

This is the fix for the exact contamination class I hand-worked around all
session (BL-1317/BL-1340/BL-1341/BL-1343/BL-1056/BL-1271/BL-1323's own
lands each required manually verifying `land_step_cli.bb`'s replay because
of this defect).

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- Acceptance (`specs/features/BL-1332-a-shared-path-carries-the-siblings-lines.feature`):
  6/6, including scenario 04 which runs the REAL
  `check_feature_handler_registration.sh` against the produced tip (the
  actual guard that froze `main` on 2026-09-02).
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1332SharedPathLineLeakSteps` (line 21) — confirmed.
- Declared invariants
  (`extension/test/bl1332SharedPathRefusesInvariants.property.test.js`):
  2/2.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-disabled the new shared-path refusal branch
  (`land_step_lib.bb:350`, prefixed the `and` with `false`) — both the bb
  suite (6 assertions) and the property test (both invariants) failed
  exactly as claimed. Restored (`git diff --stat` empty), re-ran clean.
- **Hardener's own mutant-kill re-verified independently**: hand-mutated
  `(some unlanded-siblings ...)` to `(every? unlanded-siblings ...)` — 6
  bb-suite failures, matching the hardener's claimed count. Restored,
  re-ran ALL PASS.
- **Scenario 04's strengthened assertion independently re-confirmed**:
  built the same fixture by hand (landing ticket's commit + a shared
  `index.js` carrying an unlanded sibling's bare `require()` with no
  backing handler file) and ran
  `check_feature_handler_registration.sh` against it directly — exit 1,
  "missing or unreadable registry module", matching the architect's and
  hardener's independent reproductions.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical to the
  standing-debt population from every prior pass this session. None touch
  `land_step_lib`, `own-paths`, or `bl1332SharedPathLineLeakSteps`.
- Property suite (`npm run test:properties`): 24 files / 14 tests red —
  the same known-flaky standing-debt population
  (bl1012FreshnessSelfInflictedIncidents,
  bl1113CursorHotfixStampOff/bl1115MainSyncStatusCliStampOff/
  bl1136BabysitterdCursorForgeStampOff stamp-off families,
  multiBranchParserCoverageCheck/perHatRolePromptEvidenceCheck/
  unreachableStepHandlerCheck landPilotedTicket family) — one more red
  test than earlier passes today within this same known-flaky set, which
  is consistent with host-load variance already documented for these
  specific files, not a regression: none reference `land_step_lib`,
  `own-paths`, or this parcel's files. BL-1332's own property test — 2/2
  green.
- Documentation currency: `docs/how-to/BL-1241-entangled-tip-at-the-land-
  step-has-a-reachable-remedy.md`'s new section and
  `docs/reference/Specification.MD`'s new top entry confirmed present and
  accurate. No diagram change-trigger fired (confirmed — none of the four
  registered diagrams depict land-step internals).
- Orphaned test processes: none after the property-suite run.

## Constraints honoured (verified, not just claimed)

- A path owned only by the landing ticket still replays whole (unshared
  case unaffected — confirmed by the bb suite's own-only cases).
- A path owned only by an unlanded sibling is still excluded, BL-1315's
  decision untouched.
- A path shared with a sibling that is NOT unlanded still replays.
- Per-hunk replay (ruling option 2) is explicitly out of scope — this
  parcel implements option 1 only, per the human's recorded ruling.

## Note on this parcel's own land

Per the coder's own flagged routing caveat: this parcel changes
`land_step_lib.bb` and touches `specs/pipeline/steps/index.js` — exactly
the shared path the new refusal fires on. Its own land is therefore a live
test of the fix, and I expect `land_step_cli.bb`'s own replay to behave
correctly (or at minimum refuse loudly rather than silently contaminating)
for the first time this session — will use it as the PRIMARY land path
rather than hand-building, and verify its output before trusting it.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
