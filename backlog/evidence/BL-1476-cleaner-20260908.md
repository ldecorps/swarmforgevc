# BL-1476 — cleaner review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Unit suite: `npm test` (extension/) - 612 files, 10380 tests, all green;
  no regression vs pre-merge baseline. The 6 per-file suite-budget offenders
  flagged in the summary are pre-existing and untouched by this ticket.
- Property suite: `npm run test:properties` scope -
  `bl1476TurnProfileSweepReadsOnlyWhatChangedInvariants.property.test.js`
  green (invariant 1, real compiled producer vs full walk).
- Acceptance: `run_acceptance.sh` on both
  `BL-1476-the-turn-profile-sweep-reads-only-what-changed.feature` (5/5) and
  `BL-1364-the-mechanical-share-of-a-turn-is-readable.feature` (8/8) - both
  named in the ticket's qa_e2e_procedure step 1.
- DRY: `jscpd` over the four touched/added files - 0 clones.
- Mutation-site count (BL-485, `mutation-site-count.js`, threshold 100):
  `turnProfileProducer.ts` 205 (over), `transcriptWalker.ts` 325 (over,
  unchanged by this pass), `run-turn-profile-producer.ts` 37 (within).
  Checked against the pre-BL-1476 baseline (old sources compiled standalone):
  113 and 291 respectively - both files were ALREADY over threshold before
  this ticket; BL-1476 added ~92 sites to the producer and ~34 to the walker.
  Extracted the summary-store persistence/freshness-check concern
  (`TranscriptSummary`/`TranscriptSummaryStore`, path/read/write,
  `statOrNull`, `summaryIsCurrent`, `computeTranscriptSummary` - a cohesive
  unit with no dependency on window/series assembly) into a new
  `transcriptSummaryStore.ts` (41 sites, within), re-exported from
  `turnProfileProducer.ts` for the existing test/step-handler imports so no
  caller changed. Result: producer 205 -> 180 (still over). Left
  `transcriptWalker.ts` whole - it was already over threshold pre-ticket and
  a further split is a separate, pre-existing concern outside this ticket's
  scope; a mechanical chop there would not improve separation of concerns.
  Both remaining over-threshold files are a SOFT advisory (BL-485), not a
  block - flagged here for the hardener's downstream mutation pass.
- Architecture: `transcriptSummaryStore.ts` depends only on
  `transcriptWalker.ts`'s per-file primitives (`classifyTranscriptText`,
  `walkTranscriptText`) - no dependency direction violation, no new
  IO/framework coupling.
- Verified the coder's claim that moving `attributeTrail` inside
  `walkTranscriptText` (applied per-file, concatenated) is behavior-
  identical to the prior single call over the concatenation of every file's
  intervals: `attributeTrail` is a pure per-interval `.map` with no
  cross-interval state, so order of application (per-file vs. once-over-all)
  cannot change its output.
- No new behavior introduced; no acceptance/Gherkin/property test authored
  or modified by this pass.
