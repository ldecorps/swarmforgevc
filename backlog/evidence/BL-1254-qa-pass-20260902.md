# BL-1254 QA pass — 2026-09-02

Merged documenter `21226fc477` into QA worktree (`6590b8bbb3`). Lineage
verified: `git merge-base --is-ancestor 21226fc477 HEAD` — OK.

Review-only BL-848 stamp-off parcel: one ticket, three ledger rows for
`3f4f69ec1b`, `70c5e0e5b0`, `5de352ed1d` reviewed as one resulting state
(consolidation authorized at mint, BL-680). Never reimplements the chain
and never writes certified/waived into the ledger.

## Process hygiene

`pgrep -fl 'node --test|stryker'`: no stray test/mutation processes before
or after this pass.

## Independent re-run

- `bb swarmforge/scripts/test/expedite_lib_test_runner.bb` — ALL PASS.
- `bash swarmforge/scripts/test/test_expedite_cli.sh` — ALL PASS
  (no-verdict-recovery and bounce-without-reason cases included; BL-782's
  known-failing cases did not fire, per qa_e2e_procedure item 4 — not
  this parcel's to fix).
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1254-swarm-stamp-expedite-no-verdict-chain.feature` —
  10/10 scenarios pass (BL-112 executable e2e gate).

## Full unit suite (`npm test`, extension/)

Same 15 pre-existing failed test files as this session's prior BL-1324 QA
pass, already confirmed as standing tracked reds (BL-1221/1229/1263/1265/
1289/1290/1291) via the BL-1318 QA-pass evidence — re-confirmed here: this
parcel touches zero `extension/` files (`git diff --stat
21226fc477~1..21226fc477 -- extension/` is empty). No new/untracked red.

## required_wiring — re-verified live

- `bl1254ExpediteNoVerdictChainStampSteps` registered at
  `specs/pipeline/steps/index.js:906`.
- `should-recover-missing-verdict?` called at `expedite_cli.bb:685`.
- `bounce-payload-valid?` called at `expedite_cli.bb:790`.
- `finalize-stage-result` called at `expedite_cli.bb:695`.

## Ledger — untouched, invariant 2 held

All three rows (`3f4f69ec1b`, `70c5e0e5b0`, `5de352ed1d`) in
`backlog/hotfix-ledger.yaml`: `state: stamp-open`, `human_decision: null`,
`stamp_ticket: BL-1254` — unchanged by this parcel.

## Invariant 3 — no scenario asserts superseded behaviour

Grepped the feature file for same-stage bounce language: only the
background/context lines describing what 70c5e0e5b0 DID (superseded) and
one scenario asserting the ticket is explicitly NOT re-entered at the same
stage on that bounce — the correct, current (5de352ed1d) behaviour.

## Constraints honoured

No hotfix source file touched (`git status --porcelain` over the four
files named in the ticket — clean, matches hardener's own independent
check). Doc "Related" entry present
(`docs/how-to/BL-848-certify-an-operator-hotfix.md:196-199`).

## Verdict

PASS. Landing on `main`; `Hotfix-Certification` stays `pending` on all
three rows until the human records the ledger decision separately.
