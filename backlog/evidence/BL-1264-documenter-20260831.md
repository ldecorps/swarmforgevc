# BL-1264 — documenter pass, 2026-08-31

Reviewed hardener's forwarded commit (via coordinator re-dispatch
`0a3d6125a7`, resuming at documenter — architect/hardener passes were
already complete before this ticket was parked to `backlog/hold/` by an
orphaned dead expedite run, then released back to `active/` unchanged).
Fix: `extension/src/metrics/backlogDashboard.ts`'s `computeNeedsApproval`
now conditionally spreads `approvalContext` instead of always writing the
key, so a ticket with no `approval_context:` produces an entry whose own
keys are exactly `id`/`title` (commit `15684ee3bf`).

## Doc-impact check

Grepped `docs/`, `swarmforge/handoff-protocol.md`, and
`docs/reference/Specification.MD` for `computeNeedsApproval`,
`NeedsApprovalEntry`, `approvalContext`, and `needsApproval` — zero hits.
No doc anywhere describes this function's per-entry shape. The ticket's own
constraints are explicit that nothing user-visible or consumer-facing
changes: `backlog.json` (the artefact the static PWA renders) stays
byte-identical — `JSON.stringify` already dropped the `undefined` key, so
the defect never reached any surface a doc would describe — and every
existing reader (`pendingApprovalsAnnouncement.ts`,
`conciergeTick.ts`, `topicRouter.ts`) already treated a missing context as
nothing to render, unchanged by this fix.

## Docs updated

None. Nothing existing was wrong, and the fix introduces no new
user/operator-visible behaviour, command, or setting to document.

No diagram depicts internal dashboard-summary producer shape; none of the
four registered diagrams' change-triggers fired.

## Send-time gate note

The first attempt to forward hit two send-time refusals: `pre_qa_gate_lib.bb`
(ancestry, BL-490) named two earlier BL-1264-tagged commits stranded on
`master` (`5f5190eaa9`, `1a7dfc3654`), superseded by the coder's tip-pure
rebuild (`0a3d6125a7`) but never recorded — fixed by adding
`abandoned_commits` to the ticket YAML. `task_scope_gate_lib.bb` (BL-1192)
then flagged `backlog/topics/BL-1308.json` and
`backlog/done/M8/BL-1308-...yaml` as foreign scope, because my own first
merge commit's subject named BL-1264 while its conflict resolution on
`backlog/topics/BL-1308.json` touched a path belonging to BL-1308 — fixed
by rebuilding the merge with a subject naming no ticket id (established
remedy: [[documenter-cross-ticket-doc-edit-needs-untagged-commit-subject]]),
since the resolution is legitimate bookkeeping sync, not BL-1264's own
authored scope.

## Verdict

Clean, no-op doc pass. Forwarding to QA.

By documenter.
