# BL-1515 — QA review pass, 2026-09-11

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Reviewed commit: 5eb658c93e (documenter's forward, checklist NONE), merged
into this worktree as `<merge-sha>` (below).

- Re-read `backlog/active/BL-1515-...yaml` and its invariants/required_wiring.
  Confirmed `swarmforge/scripts/ready_for_next.bb` sources
  `branch_identity_guard_lib.bb` (line 13) and calls
  `(enforce-branch-identity-guard!)` at line 295, ahead of inbox reads —
  invariant 2.
- Reviewed the parcel's history: coder → cleaner → architect (bounce D1:
  live-repair deliverable asserted only in commit-message narrative, no
  evidence artifact) → coder fix (added
  `backlog/evidence/BL-1515-coder-20260911.md` with fresh before/after `git
  worktree list` output and a real `decide` invocation against the live
  coder worktree, verdict `:ok`, byte-identical before/after) → cleaner →
  architect → hardender (bounce D1: no master-resident exemption, unlike the
  BL-1195 sibling guard — could rename `main` out from under
  specifier/coordinator) → coder fix (added the exemption + acceptance
  scenario 06) → cleaner → architect → hardender → documenter (NONE; added
  `docs/how-to/BL-1515-branch-identity-guard.md`, linked from
  `docs/index.md`; correctly found no diagram trigger fired).
- `pgrep -fl 'node --test|stryker|vitest'` before and after: clean both times.
- `bb swarmforge/scripts/test/branch_identity_guard_lib_test_runner.bb`:
  ALL PASS.
- `bb swarmforge/scripts/test/branch_identity_guard_lib_property_runner.bb`:
  ALL PROPERTIES HOLD (300 runs).
- `cd extension && npm run compile`: clean.
- `cd extension && npm test`: 615/615 files, 10439/10439 tests green (one
  pre-existing suite-duration/per-file-budget warning, unrelated to this
  parcel — same shape as prior passes).
- `cd extension && npm run test:properties` (full run): 3 files failed —
  `bl1313BatchGuardVisibilityInvariants` (registered `BL-1503` in
  `backlog/standing-reds.tsv`, reproduced deterministically in isolation),
  `bl1358MutantTimeCeilingInvariants` (isolated re-run: 2/2 PASS — confirmed
  transient/contention-flaky under full-suite load, same conclusion as
  `backlog/evidence/BL-1494-QA-20260910-2.md`'s prior sighting of the same
  file), and the vitest-worker `onTaskUpdate` timeout (BL-871 allowlisted).
  Neither file is touched by this parcel's diff
  (`git diff HEAD~1 HEAD --stat` — no match). No unowned red found; nothing
  blocks approval under Article 4.2.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1515-a-role-checkout-on-the-wrong-branch-is-caught-before-the-turn.feature`:
  6/6 scenarios pass, including the coder's added scenario 06
  (master-resident exemption).
- `required_wiring` confirmed: `ready_for_next.bb` sources the lib and calls
  it (above); `bl1515BranchIdentityGuardSteps.js::registerSteps` present and
  auto-discovered per BL-1371 (all 6 scenarios ran and passed).
- Invariant 1 (safe-shape-only repair) and invariant 2 (pre-inbox-read,
  fail-closed on every other mismatch) both directly exercised by the
  acceptance scenarios and the bb property suite (300 runs).
- Live-repair deliverable (item 3, FIRM in `human_approval.approval_context`):
  confirmed satisfied by the coder's D1 remediation
  (`backlog/evidence/BL-1515-coder-20260911.md`) — genuine before/after
  `git worktree list` output showing the live `.worktrees/coder` checkout
  already on `swarmforge-coder` (verdict `:ok`, idempotent), plus reflog
  evidence the original hand-rename preserved history.
- Bounce history on the ticket reviewed via `main`/`origin/main` refs
  (BL-340/BL-891 discipline): both prior bounces (architect D1, hardener D1)
  were fixed by the coder in the commits carried into this merge; no
  unresolved bounce remains.

By QA.
