# BL-1449 — hardender review pass, 2026-09-10

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Gates run:
- `npx vitest run test/operatorRuntimeBbFixtureClosure.test.js` — 6/6 (was
  2 failed / 4 passed on `main`; this parcel's merge of the architect's
  commit is what turns it green).
- New `extension/test/bl1449OperatorRuntimeFixtureFilesDeclaredExtras.test.js`
  — 2/2.
- `run_acceptance.sh` BL-1449 feature — 3/3.
- `run_acceptance.sh` BL-944 feature — 6/6 (untouched, guard unchanged).
- `run_acceptance.sh` BL-359/647/368/369 (the four other
  `OPERATOR_RUNTIME_BB_FILES` consumers) — 6/7, 2/7, 3/4, 6/6 respectively;
  the three non-6/6 counts match the already-registered BL-1514 row
  exactly (AGENT_EXITED producer BL-653 retired) and are unchanged by this
  parcel.
- `npx vitest run --config vitest.properties.config.mjs test/bl1449OperatorRuntimeClosureIsPure.property.test.js`
  — 1/1 (200 runs, independent BFS oracle).
- Standing whole-tree guards (`ls test/*Guard*.test.js | grep -v '\.property\.'`)
  — 17/18; the one failure (`tempDirTrapGuard.test.js`, naming
  `landed_ticket_autoclose_test_runner.bb` and
  `wake_dedup_lib_test_runner.bb`) is grepped-confirmed as BL-1501,
  pre-existing, unrelated to any file this parcel touches.
- CRAP/DRY: N/A — both npm scripts scope to `extension/src/*.ts`; this
  parcel's production file lives under `specs/pipeline/steps/lib/`,
  outside that scope, and outside Stryker's `out/**/*.js` mutate glob.
  Mutation coverage taken by hand (BL-638 fallback).

Hand-mutation finding, fixed in this pass: hand-removing
`.concat(OPERATOR_RUNTIME_BB_DECLARED_EXTRAS)` from the module-level
`OPERATOR_RUNTIME_BB_FILES` wiring left the unit suite, both acceptance
features, and the coder's own property test all green — the concat step
had zero coverage because `OPERATOR_RUNTIME_BB_DECLARED_EXTRAS` is empty
today. Extracted the append into an exported pure function
`buildOperatorRuntimeBbFiles(closure, declaredExtras)` (behavior-preserving
split) and added a direct unit test driving it with a non-empty extras
list. Re-mutated after the fix: the one remaining wiring line
(`buildOperatorRuntimeBbFiles(deriveOperatorRuntimeClosure(SCRIPTS_DIR), OPERATOR_RUNTIME_BB_DECLARED_EXTRAS)`)
is still indistinguishable from a bare `deriveOperatorRuntimeClosure(SCRIPTS_DIR)`
while extras stays `[]` — recorded as equivalent-while-empty (BL-234
shape: demonstrable from the code, holds only as long as
`OPERATOR_RUNTIME_BB_DECLARED_EXTRAS` is `[]`), not as a gap, since the
mechanism itself is now proven independent of that constant's current
value. Commit: fdc08c53c2.

No orphaned `node --test`/`stryker` processes before or after this pass
(`pgrep -fl 'node --test|stryker'` scoped to this worktree). `uptime` load
average 1.17–1.23 on 20 cores throughout — no load-driven degraded
fallback taken.
