# BL-1698 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Third same-day hardener pass, after QA's second bounce (D1/D2: same-
process hold-release and same-process seat relaunch both skipped
`chat-set-up!` — the exact gap my OWN prior pass reviewed and missed,
see `BL-1698-hardender-20260925-2.md`). The coder's fix
(`dc5900eb0e`) removes the `seats-chat-ready` per-process marker
entirely — `chat-set-up!` now runs unconditionally before every fix
request, in both `resume-from-hold!` and `run-gate!`'s retry branch —
and re-passed clean through cleaner and architect
(`BL-1698-cleaner-20260925-4.md`, `BL-1698-architect-20260925-3.md`).

Independently re-verified (not merely trusted) the coder's own new
regression test's non-vacuity: reintroduced a once-per-process marker
around `chat-set-up!` (renamed the real body to `chat-set-up-real!`,
gated it behind a fresh atom, exactly the shape pass 2's bounce
targeted), ran `bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js`
— the new "pass 2 D1/D2" test failed exactly as predicted (1 CHAT-CLEAR
observed vs 5 expected). Restored the file (confirmed clean via `git
status`), reran green (7/7).

**Documentation regression found, NOT fixed here (documenter's own
content, out of hardener's domain) — flagging explicitly so it is not
missed a second time:** `docs/how-to/BL-1697-local-parcel-driver.md`'s
entire pass-1 doc fix (`bfddb4aad7`, "correct hold-release docs for
D4/D5's fixed disposition") is missing from the current tree, even
though that commit IS an ancestor of HEAD (`git merge-base
--is-ancestor bfddb4aad7 HEAD` → true) — this is the SAME collateral-
damage class as BL-1704's dropped test file this session (a QA-bounce-
revert's restoration sweep scoped to production code, never docs).
Concretely, `diff <(git show bfddb4aad7:<path>) <path>` shows THREE
whole hunks reverted to their PRE-fix, now-INCORRECT wording:
1. "Releasing an escalated hold" still says "re-arms the gate for
   exactly one more try" — the pre-D4 semantic. The correct wording
   (already written once, in `bfddb4aad7`) distinguishes a ticket
   hold (chat set-up always redone, `fixTurnsUsed = fixTurnsLimit`,
   the very next failed gate escalates immediately, no further fix
   request) from a mail hold (answer left unconsumed, `release` only).
2. "Mail that needs no model turn" still says a mail hold "is visible
   and releasable via the same `release-hold!` CLI as any other" with
   no mention that an answer never resolves it.
3. The babysitterd nudge-skip bullet still says "unchanged by this
   ticket" — stale since D2 added an explicit `driver-seat?` skip.
`docs/diagrams/handoff-flow.mmd` has the SAME reversion: DHOLDCHECK/
DRESUME/DRELEASE node text reverted, and DRESUME now reads
"fixTurnsUsed = limit-1" — actively WRONG against the current code
(`= fixTurnsLimit`). The missing DMERGECONFLICT/DMAILCONFLICT →
DRELEASE-only edges are gone too. `architecture.mmd` is unaffected
(confirmed via diff, no BL-1704-era changes there to lose).

Sent a `rule_proposal` earlier this session (scope `role:hardender`,
accepted) already covering the general lesson this instance confirms:
after a QA-bounce revert, diff the WHOLE tree against the pre-revert
tip, not just the changed production path — docs are exactly as
vulnerable as test files to this collateral damage.

Checks run (commit `d8264e11ce`, shared tip with BL-1704 this batch):
- `node specs/pipeline/cli.js specs/features/BL-1698-….feature`: 9/9.
- `npx vitest run test/bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js`:
  7/7.
- `npm test` (extension/): 638 files / 10875 tests, all green.
- No TS/JS production file beyond the already-covered test helper is
  in scope — CRAP/DRY not run (Babashka/shell only).
- `git status --short` clean before each commit; no process/fixture
  leaks from this pass's own runs.

By hardender.
