# BL-957 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-957 — promotion gate never reads `depends_on` (`type: defect`, `severity: high`, milestone M8)
- **Parcel**: `00_20260819T235505Z_000215_from_documenter_to_QA_for_QA` (from documenter, commit `d166b82fd0`)
- **Verified at**: `b7a6e580d7` (QA merge of the documenter tip)
- **Reviewed by**: QA, 2026-08-20
- **Prior bounces**: none.

## Verdict

**PASS — inventory items: NONE.**

**Landing DEFERRED — see "Entangled riders" below. This is not a defect in BL-957.**

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — coder `d34a85b82`, cleaner `1521d68c3`, architect `36f3ef076`, hardener `a81c0ab72`/`bcfb3aa56`/`c8faaa6ad`, documenter `d166b82fd` | PASS — all ancestors of the cited commit |
| G2 | Prior-bounce check vs `main` and `origin/main` (BL-891) | PASS — none |
| G3 | `pre_qa_gate.sh` | PASS — `OK` |
| G4 | `qa_e2e` step 3 — block-style `depends_on` (the fail-open trap) | PASS — `REFUSE\|depends_on\|... BL-556`, exit 2. See "Stale procedure" |
| G5 | `qa_e2e` step 4 — empty `depends_on` + approved | PASS — `ALLOW` (BL-961, with the depth gate lifted so this gate is the decider) |
| G6 | Invariant 2a — dangling id fails CLOSED | PASS — `depends_on: [BL-99999-does-not-exist]` → `REFUSE\|depends_on\|... BL-99999` |
| G7 | Invariant 2b — malformed `depends_on` fails CLOSED | PASS — `REFUSE\|depends_on\|depends_on is present but names no parseable BL-/GH- ticket id - failing closed` |
| G8 | Invariant 1 — every promotion route decides through the one chain | PASS — both `promote_and_route_next.sh` and `route_backlog_to_coder.sh` reference `promotion_gates`; no third route exists |
| G9 | `promotion_gates_lib_test_runner.bb` | PASS — `ALL PASS` |
| G10 | `promotion_gates_lib_property_runner.bb` | PASS — `ALL PROPERTIES HOLD` |
| G11 | `promotion_gates_cli_test_runner.bb` | PASS — `ALL PASS` |
| G12 | Acceptance — `BL-957-promotion-gate-refuses-unsatisfied-depends-on.feature` | PASS — 15/15, 0 fail, exit 0 |
| G13 | Full unit suite | PASS — 443/444 files, 7894/7895 tests. The one failure is `renderBriefingBurndownCli.test.js` `Test timed out in 20000ms`, which passes **5/5 in isolation** — the standing ambient-load condition, not this ticket |
| G14 | Acceptance-contract gate (documenter reported it failed OPEN) | PASS by direct execution — see below |
| G15 | Docs currency | PASS — `docs/reference/Specification.MD` carries the BL-957 entry |
| G16 | Functional change present | PASS — `promotion_gates_lib.bb`/`_cli.bb`, three test lanes, step handler, feature, docs |

## The acceptance-contract gate that "failed OPEN" (G14)

The documenter's `note` reported `BL-957 send: acceptance-contract gate failed OPEN (cwd not a git repo)`. A gate that fails open is an UNRUN gate, never a pass, so it was not accepted as one.

- The fail-open posture is **by design and already adjudicated**: `backlog/evidence/BL-761-acceptance-gate-fail-open-20260820.md` records the coordinator's verification that infrastructure failures warn rather than block, and that the specific trigger was not reproduced. `BL-968` is already minted for the underlying step-registry loadability.
- `acceptance_contract_gate_lib.bb`'s own `evaluate` docstring confirms it: `registry-loadable? false -> no finding, one warning ... - fails OPEN`.
- What that gate would have checked — that **every** scenario, every Scenario Outline row and every step resolves against the real step registry — was executed directly instead: `run_acceptance.sh` on the ticket's feature file resolved and passed **15/15** with the registry loaded. `pre_qa_gate.sh` separately returned `OK`.

So the check this gate exists to perform was run to completion; only the gate's own wrapper was degraded. Recorded rather than assumed.

## Stale `qa_e2e_procedure` (not a defect — recorded so the next reader is not misled)

Two steps no longer match the live backlog. Neither indicates a fault in the gate; both are the procedure aging behind the backlog it references.

- **Steps 1–2** pin `backlog/paused/BL-955-...yaml`. BL-955 was QA-approved and closed into `backlog/done/M8/` on 2026-08-19, so it is no longer a paused, dependency-blocked candidate. Live equivalents were substituted (G4–G7).
- **Step 3** expects the refusal to name **BL-547 and BL-556**. `BL-547` has since landed in `backlog/done/`, so naming **BL-556 alone is correct**. This run is in fact stronger evidence than the step intended: both block-list entries were parsed and evaluated independently — one satisfied, one not — which is exactly the block-style fail-open trap the step targets.
- **Step 5** (auto-pick in a scratch clone) was not run as written; G8 establishes the same property statically (both promotion routes decide through the one chain) and G9–G11 exercise the chain directly. Recorded as substituted, not as passed-as-written.

## Entangled riders — why landing is deferred

The documenter's branch carries **BL-958** and **BL-960** work alongside BL-957, and both have their own parcels queued immediately behind this one. Pushing on BL-957 alone would land two tickets that have passed every stage EXCEPT the QA gate — the exact leak class BL-506 and the "approval lands entangled sibling content" lesson exist to stop.

So BL-957 is APPROVED here, and the push is held until BL-958 and BL-960 are verified from their own parcels. All satisfied ids will then be named in one landing, one merge-up broadcast, and one coordinator note (Article 2.6 — an id that never reaches the note stays active forever).

## BL-571 kept reverted through this merge

This parcel's lineage includes BL-571, which QA bounced earlier today (`backlog/evidence/BL-571-qa-bounce-20260820.md`). The merge conflicted in three files where BL-571's content was interleaved with BL-958's; each was resolved keeping BL-958 and excluding BL-571:

| File | Resolution |
|---|---|
| `specs/pipeline/steps/index.js` | registers `bl957`/`bl958`/`bl960`; `bl571` excluded |
| `swarmforge/scripts/test/test_swarm_ensure.sh` | BL-571's 52-line block dropped, BL-958's 107 lines kept |
| `docs/reference/Specification.MD` | BL-957's entry kept, BL-571's entry dropped |

`mono_router_lib.bb` and `swarm_ensure.bb` auto-merged with NO conflict marker — the silent-reintroduction shape (BL-954) — so they were checked by content afterwards. Verified at the merge commit: `single-resident-rotation` = 0, `BL-571` = 0 in ensure/test-runner/index.js/Specification.MD, and both BL-571 files still absent.

By QA.
