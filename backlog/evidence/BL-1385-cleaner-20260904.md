# BL-1385 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked

- **Correctness / manual verification** — ran
  `check_handler_module_graph.sh` directly against `HEAD`: exit 0 (the
  repo's own handler tree is healthy). Ran the acceptance fixture's
  `good`, `missing-ext-out`, and `land-replay` shapes directly (not only
  via the step handler): `good` passes clean; `missing-ext-out` correctly
  refuses with `HANDLER_LOAD_BLOCK`, naming both the handler
  (`zzBadSteps.js`) and the missing module; `land-replay` confirms the
  guard is actually reached through `land_step_lib.bb`'s own
  `run-replayed-tree-guards` list, not merely present as a standalone
  script (the exact defect class this ticket is about — registered but
  not wired).
- **Wiring (`required_wiring`)** — read `land_step_lib.bb`'s
  `replayed-tree-guards` and `run_commit_guards.sh` diffs directly:
  `check_handler_module_graph.sh` is in both lists, matching the two
  required anchors.
- **Guard-chain discipline** — `run_commit_guards.sh` still runs under
  `set -uo pipefail` with no `-e`, one status captured per guard
  (Guardrails/BL-1242/BL-1252); confirmed `run_guard
  check_handler_module_graph.sh` was appended without disturbing that
  shape. Ran `run_commit_guards.sh` directly: exit 0.
- **CRAP / mutation** — Babashka/shell have no mutation/CRAP tool wired
  (Startup Tools, BL-472 deferred); gated by the fixture-driven acceptance
  scenarios instead (re-verified above), per the documented degraded
  fallback.
- **DRY** — `jscpd` over the new JS handler and the new shell guard: 0
  clones.
- **Mutation-site size (BL-485)** — `bl1385HandlerModuleGraphGuardSteps.js`
  reports 140 sites (`over` 100). Reviewed and declined to split, same
  reasoning as this area's other tickets: one cohesive single-feature step
  handler, ~15 tightly-scoped Given/When/Then defs, site count driven by
  assertion density rather than mixed responsibilities.
- **Architecture** — the guard script is self-contained (materialises the
  tree via `git archive`, requires each handler in a child `node`
  invocation with a hooked `Module._resolveFilename`); no coupling
  introduced into `land_step_lib.bb`/`run_commit_guards.sh` beyond the one
  list entry / `run_guard` call each required anchor calls for. The
  design rationale (why not static parsing, why not `tsc`) is documented
  inline and matches what the script actually does.
- TypeScript compiles clean; the new handler discovers via BL-1371's
  registry.

No bounce history on this ticket (first pass, `main`/`origin/main` agree).
Forwarding unchanged to architect.

By cleaner.
