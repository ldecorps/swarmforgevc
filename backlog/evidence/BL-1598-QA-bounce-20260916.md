# BL-1598 — QA bounce, 2026-09-16

## D1

- **Failing command**: `npm test` (from `extension/`), 3 consecutive runs on parcel commit
- **Commit hash**: d5c7b27221 (QA branch; the received documenter commit was 1315022d09, superseded on this branch by the merge chain)
- **First error excerpt**:
  ```
  1 new-pole offender(s):
  extension/test/emitLifecycleSnapshotCli.test.js: 9.0s exceeds the 7.0s per-file budget
  3 stale register row(s) (file now under 80% of budget - remove the row):
  extension/test/epicReorderBridge.test.js: 5.0s exceeds the 7.0s per-file budget
  extension/test/pilotAcceptanceGateCli.test.js: 2.7s exceeds the 7.0s per-file budget
  extension/test/recordBounceCli.test.js: 2.3s exceeds the 7.0s per-file budget
  ```
- **Failure class**: behavior
- **Expected vs observed**: expected `budget_verdict: ok` per the ticket's
  original qa_e2e_procedure step 1; observed exit 1 from a stale-row
  refusal and a new-pole refusal, both against the specifier's own
  13:16Z measurement drifting within 2.5 hours with no code change.
- **Blamed role**: specifier (spec defect, per the specifier's own
  adjudication `backlog/evidence/BL-1598-specifier-hold-adjudication-20260916.md`);
  routed to coder per Article 4.3 (the guard is the coder's implementation
  domain) — the coder built the original spec correctly and is not at
  fault; a bounce correction is recorded against this bounce per BL-990.
- **Remediation pointer**: `extension/src/tools/check-suite-file-budget.ts`
  (`checkFileDurationBudget`, `formatGuardReport`), `extension/scripts/testDurationRecorderLib.js`
  (`buildRecord` gains `watch_files`), the feature file's amended
  scenarios (02/03), per the specifier's amendment on the ticket
  (`approval_context`, `invariants`, `description`, `qa_e2e_procedure`,
  all updated 2026-09-16 15:10Z):
  - a stale row is REPORTED, never refused (exit code untouched);
  - an unregistered file refuses as new-pole only at or above
    `PER_FILE_DURATION_BUDGET_MS * NEW_POLE_REFUSAL_FRACTION` (1.5x =
    10500 ms); between the budget and that line it is reported as
    `watch`, exit code untouched;
  - verdict kinds: `ok | watch | stale-row | unowned-row | new-pole`;
    only `new-pole` and `unowned-row` fail the exit code;
  - the nine register rows are unchanged; no row is added for
    `emitLifecycleSnapshotCli` (it is a watch file; BL-791 slice D
    inventories it separately).

Full review otherwise clean: unit (`checkSuiteFileBudgetCli.test.js`
43/43, `testDurationRecorderLib.test.js` 8/8), property
(`bl1598SuitePoleRegister.property.test.js` 2/2), and acceptance (9/9)
all green; wiring OK; scope matches the ticket's constraints;
`extension/src` changes are confined to this ticket's own files.

By QA.
