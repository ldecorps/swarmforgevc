# BL-1605 QA pass — unowned red found in the full property lane, 2026-09-17

While verifying BL-1605 (resuming an in_process parcel: documenter merge
`e7c6962b0f`, this branch's tip `9583f2177c`), ran the full
`npm run test:properties` lane via `qa-gather.js`. One file failed:

```
FAIL  test/bl1297MergeOwnPathsInvariants.property.test.js > property
(invariant 3): the land step reads delivered, the two send-time gates
read authored
Error: Test timed out in 20000ms.
```

`git diff main...9583f2177c --name-only` shows this batch branch carries
several pipelined sibling tickets alongside BL-1605 (BL-1599, BL-1604,
BL-1607, BL-1608, BL-1610 — ordinary multi-ticket batch pipelining, not
a BL-1605 diff by itself); it does include `land_step_lib.bb` (a
sibling's change, not BL-1605's — BL-1605's own `required_wiring` is
`reverse_hop_lib.bb` and `bl1605NoReverseCopyToForwardRecipientSteps.js`
only). `test/bl1297MergeOwnPathsInvariants.property.test.js` itself is
not touched by any commit on this branch.
`grep -rl bl1297MergeOwnPathsInvariants backlog/active backlog/paused
backlog/hold` and `backlog/standing-reds.tsv` (read directly) show no
owning ticket: the register currently holds only two rows (BL-1601 for
`telegramCursorOperatorExec.test.js`, BL-1613 for
`test_branch_claim_guard.sh`) — this file is absent. BL-1564 (done,
2026-09-14/15) already fixed this exact file's one-bb-process-per-case
cost; this is a fresh recurrence of the same symptom class under today's
host load, not a reopening of BL-1564's own scope.

Re-ran the file alone under `vitest.properties.config.mjs`, against this
exact tree (same `land_step_lib.bb` and every other file on the branch):
green, 15.24s total, invariant 3 alone 9.36s — well under the 20s
budget. That rules out a logic regression from any change on this
branch (BL-1605's own or a sibling's): the file passes deterministically
against this tree's code and only times out under the full 404-file
lane's contention — the same class as the BL-1556-owned bl1272 timeout
and the BL-1564 repro on 2026-09-14.

## Other checks this pass (all green/clean)

- `qa-sibling-check.js status --ticket BL-1605`: `VERIFY BL-1605`, no
  open deferral.
- Orphaned `node --test|stryker|vitest` process check: none before or
  after.
- `pre_qa_gate.sh BL-1605 9583f2177c`: `OK`.
- Full unit suite (`npm test`, extension/): exit 0, green.
- Acceptance (`run_acceptance.sh
  specs/features/BL-1605-no-reverse-copy-to-a-role-the-forward-already-names.feature`):
  5/5 scenarios green.
- Standing-red register join: `extension/test/telegramCursorOperatorExec.test.js`
  (BL-1601) and `swarmforge/scripts/test/test_branch_claim_guard.sh`
  (BL-1613) both owned, neither in this parcel's paths.

Disposition: unowned red, no open ticket found. Per Article 4.2 this
withholds approval. BL-1605 is otherwise fully verified (this is its
second pass; the first bounced D1 to coder for the socket-fixture-root
guard violation, since fixed per the specifier's 2026-09-17 06:30Z
adjudication) and waits on this ticket/adjudication before I approve and
land commit `9583f2177c`.

By QA.
