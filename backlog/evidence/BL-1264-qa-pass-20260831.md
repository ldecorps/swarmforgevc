# BL-1264 — QA pass, 2026-08-31

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1264` → `VERIFY BL-1264` (exit 0).

## Merge

Merged documenter's cited commit `26a74c07c4` into QA at `d13916ffbd`
(`git merge --no-ff`, clean — no guard trip). Diffed against both parents:
additions only, nothing dropped either direction.

**Ancestry (BL-336 discipline), including the abandoned-commit trap:** the
ticket YAML declares `abandoned_commits: [5f5190eaa9, 1a7dfc3654]` —
two earlier BL-1264-tagged commits stranded on `master` after a coder
tip-pure rebuild superseded them post park/hold. Verified directly, not
taken on the documenter's word: both abandoned SHAs are confirmed **NOT**
ancestors of my merge commit (correctly superseded), while every real
producing commit — coder's rebuild `0a3d6125a7`, the fix itself
`15684ee3bf`, cleaner `b585faa623`, architect `b52090e9f6`, hardener
`8480b9780d`, documenter `26a74c07c4` — **is** an ancestor of `d13916ffbd`.

**The functional fix predates this session.** `git merge-base --is-ancestor
15684ee3bf <this session's starting QA tip>` confirms `15684ee3bf` was
already on my branch before this session began — this final round is the
ticket's paperwork catching up (park/hold → release → re-dispatch →
documenter resumption) after the fix itself had already landed and been
running clean. Corroborating evidence: `backlogDashboard.test.js` never
appeared in either of this session's two full-suite runs (BL-1308's and
BL-1240's tips) — it was already green both times, which is independent,
pre-existing proof the fix holds.

## Ticket shape

Low-severity, cosmetic-but-real defect: `computeNeedsApproval` wrote an own
`approvalContext` key valued `undefined` for a ticket with no approval
context, contradicting the `approvalContext?: string` optional field its
own interface declares. Never reached any product surface
(`JSON.stringify` already drops `undefined`-valued own keys, so
`backlog.json` was always unaffected) — the defect lived entirely in-memory
and surfaced only as a strict-`deepEqual` unit-test failure.

## Stage corroboration

Read all evidence, including the two rounds split by the park/hold
incident. Coder (round 1, 20260830): pre-fix baseline captured, root cause
confirmed by reading the interface against the producer together, fix is a
conditional spread (`=== undefined` check, not truthiness — preserves an
empty-string context deliberately). Coder (re-dispatch, 20260831):
confirmed the fix was already on `main`, three downstream gates already
cleared, declined to re-implement per the No-Op Rule (Article 1.9/2.3.2) —
correct call, no functional change available to forward from that stage.
Architect: COMPLIANT, non-vacuity re-verified BY HAND (reverted the fix,
watched 2 unit + 2 property tests fail as predicted, restored, re-ran
clean). Hardener: 5 hand-authored mutants (per BL-638, Stryker blocked by
already-ticketed BL-1291), all 5 killed including the exact sentinel
substitution the ticket's constraints forbid. Documenter: doc-impact grep
returned zero hits anywhere in `docs/` for the touched symbols — correctly
a no-op doc pass, and recorded the two send-time gate refusals it hit and
fixed while forwarding (an ancestry gap needing `abandoned_commits`, and a
cross-ticket-scope false positive from a merge-conflict resolution on a
foreign path — both matched already-known remedies).

## Own independent verification (BL-112, separate from every prior stage's)

| Command | Result |
|---|---|
| `npm run compile` | clean |
| Direct read of `backlogDashboard.ts`'s fixed `computeNeedsApproval` | conditional spread confirmed exactly as described, `=== undefined` (not falsy), no sentinel |
| `npx vitest run test/backlogDashboard.test.js` | 51/51 pass |
| `npx vitest --config vitest.properties.config.mjs` BL-1264 property file | 3/3 pass |
| `run_acceptance.sh` BL-1264 feature | 4/4 pass |
| Consumer sweep: `conciergeTick.test.js`, `pendingApprovalsAnnouncement.test.js`, `conciergeTopicRouting.test.js` (topicRouter's real test file — not literally named `topicRouter.test.js`) | 202/202 pass, none of the readers modified by this parcel |
| `grep -n bl1264 specs/pipeline/steps/index.js` | registered, line 656 |
| Original strict assertion at `backlogDashboard.test.js:130-136` | unchanged — still `assert.deepEqual` (strict, `node:assert/strict`) against the ORIGINAL shape, not relaxed or weakened; new BL-1264 cases added below it use `hasOwnProperty`, not truthiness |

Not re-run: the full `npm test`/`npm run test:properties` lanes — this
fix has already been running clean inside both of this session's full-suite
runs (BL-1308, BL-1240 tips), and the parcel's own diff against my prior
tip is empty for all production/test code (confirmed: only backlog/docs
bookkeeping files changed in this merge). A third full run would be
redundant.

## Constraint compliance — verified directly

- Test not relaxed: confirmed above.
- No sentinel/empty-string/null substituted for a missing context: the fix
  is `{}` or `{ approvalContext: item.approvalContext }`, nothing else; an
  empty-string context is deliberately preserved (own property test
  covers `emptyContext`).
- `approvalContext` not made required: interface unchanged, still `?:`.
- Top-level `needsApproval`/`notDoneCount` "always present" posture:
  untouched — diff scoped to the per-entry map body only.
- `backlog.json` byte-identical: asserted three ways (unit test, property
  test 3, acceptance scenario "the serialised dashboard artefact is
  unchanged by the fix") — all green.
- No sweep of other producers for the same pattern: confirmed none
  attempted, matching the ticket's explicit scope line.

## Orphan check

`pgrep -fl 'node --test|stryker|vitest'` clean before and after.

## Verdict: APPROVE

By QA.
