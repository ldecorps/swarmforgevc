# BL-1335 — architect pass (2026-09-03)

## Merge

Merged cleaner `6d6d43513f` (coder `17516fe27e`) into the architect worktree.
One trivial conflict in `specs/pipeline/steps/index.js` — both branches added
a different `require(...)` line to the same list; resolved by keeping both.
No content lost (verified: both new requires present after resolution).

## Architecture review

- Two-layer boundary / extension-host-owns-IO / webview-storage / secrets:
  N/A — this parcel touches only `swarmforge/scripts/*.bb` (handoffd tick),
  a Gherkin step-handler JS file, a property-test JS file, and the step
  registry. No webview or extension-host code touched.
- Dependency-rule gate (BL-259), scoped to changed files
  (`bl1335ExhaustionOpensFailoverRecordSteps.js`,
  `bl1335ExhaustionPromotionInvariants.property.test.js`): **PASSED, no
  forbidden edges**. Also ran a full-repo scan: **PASSED, no forbidden
  edges** — no standing debt to report.
- Co-change report (BL-255) over the changed files: no pair at/above the
  default threshold (frequency 3) — all reported co-changes are frequency 1
  (this commit itself). No suspected coupling.
- `required_wiring` (`handoffd.bb::promote-exhaustion-to-failover!`):
  confirmed CALLED from `observe-pane-provider-outage!`, which is the live
  per-tick pane-observation path (same anchor shape as BL-840's producer).
  Not merely defined — verified by reading the call site, not by grep for
  the symbol name alone (BL-1235's lesson).
- Record shape: `promotion-decision`'s `:record` matches the field set
  `provider_outage_record_lib/normalize-record` already reads
  (`:id :provider :model :affected-seats :started-at-ms :ended-at-utc`,
  plus extra fields the normalizer simply ignores) — confirmed by reading
  both namespaces side by side. No third store, no second assignment path
  (invariant 2 satisfied structurally, not just asserted).

## Invariants review (BL-633/654)

All three declared invariants have a property test in
`extension/test/bl1335ExhaustionPromotionInvariants.property.test.js`,
re-run here and confirmed **3/3 pass**. None are vacuous:
- Invariant 1's reach check is satisfied by construction (`fc.constantFrom`
  over three fixed, mutually-exclusive corpora, asserted every draw) —
  distinct from BL-1352's `fc.uniqueArray` empty-case gap; no equivalent
  under-sampling risk here since every draw is checked, not just a final
  reach tally.
- Invariant 2 round-trips the promoted record through the CONSUMER's own
  `normalize-record`/`outage-open?`, not a local shape assumption.
- Invariant 3 exercises both the suppressing case (matching open record)
  and four non-suppressing shapes (closed, other model, other seat, other
  provider, empty) — confirms the idempotence check isn't over-broad.

No invariant violation found; no missing or vacuous property test.

## Independently re-run (not just trusted from evidence)

- `bb swarmforge/scripts/test/bl1335_exhaustion_promotion_test_runner.bb` —
  ALL PASS.
- `bb -e '(load-file ".../handoffd.bb")'` — loads clean (prints usage, the
  expected behavior with no project-root arg — confirms no syntax error
  introduced by the edit).
- `run_acceptance.sh specs/features/BL-1335-token-exhaustion-opens-an-outage-record.feature`
  — 6/6 pass, independently confirmed (not just the evidence file's claim).
  Scenario 04 drives the real `outage_failover_cli.bb evaluate` against the
  record the real promoter wrote — the two previously-disconnected files are
  genuinely joined, not stubbed.
- `npx vitest run --config vitest.properties.config.mjs bl1335ExhaustionPromotionInvariants`
  — 3/3 pass.

## Correctness read

No defect spotted. The classifier's fail-closed default (`:else nil`), the
announce-only handling of ambiguous text, and the idempotence check on the
exact (seat, provider, model) triple all match the ticket's constraints and
human ruling. The property-suite-guard override noted in the coder's
evidence (unrelated flaky files) is consistent with the standing,
already-surfaced BL-1234 issue — not re-flagged here.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
