# QA pass — BL-1434 — 2026-09-05

Commit under review: `effbcae548` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`ca0dabe1a7` BL-1434
coder, confirmed ancestor via `git merge-base --is-ancestor`).

This closes the exact `hostActivityFeed.property.test.js` observation I
flagged as "out of scope" during my own BL-1206 QA pass earlier this
session — confirmed here to be correctly diagnosed by the architect as a
misattributed register row, not the node:test-import class BL-1206 fixed.

## Verification

- `npx vitest run --config vitest.properties.config.mjs
  test/hostActivityFeed.property.test.js`: 1/1 pass — the file is now
  genuinely collected (was "No test suite found").
- Read the file directly: all 40 mulberry32-seeded trials run inside the
  one registered `test(...)` call — the conversion did not reduce
  coverage to a single assertion; every original check (bound holds, no
  invented line, quiet session reads quiet) survives.
- **Independently re-ran the non-vacuity proof myself**: set
  `BL1434_INJECT_INVENTED_LINE=1` and re-ran the file directly — it
  correctly FAILS, naming `trial 0: invented line INVENTED-L0-45-429`.
  Confirms the registered test is a real gate, not a vacuous pass.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1434-the-host-activity-feed-property-registers-its-trials.feature`
  — 4/4 scenarios pass, including "the register and allowlist rows for
  the file are gone at the parcel commit".
- `git diff -- extension/src/`: empty — `hostActivityFeed.ts` untouched,
  per invariant 3.
- Register cleanup confirmed: `grep -c hostActivityFeed
  backlog/standing-reds.tsv property_suite_standing_allowlist.tsv` both 0.
- `required_wiring`: `specs/pipeline/steps/bl1434HostActivityFeedPropertyRegistersSteps.js`
  exports `registerSteps`.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
