# BL-1634 coder evidence

## What was built

- `specs/pipeline/steps/bl829BubbleRemotePagePagerSteps.js`: two-hunk fix
  per the ticket's own direction.
  - Fixture step ("the served manifest carries a malformed page list")
    now writes id `malformed-operator-page` (an id no built-in page will
    ever carry) instead of `live` (the id BL-775's new built-in now owns),
    and records it on `ctx.malformedManifestPageIds`.
  - "no page from it is offered to the shell" now asserts none of the
    recorded malformed ids appear among the offered pages' ids — not that
    the offered list is empty. The bridge's built-in pages (health, host,
    operator-docs, live) are legitimately offered even when the operator
    manifest is malformed (BL-1166's own design); the handler over-
    asserted from the day built-ins existed.
- `specs/pipeline/steps/bl1634RejectedManifestOffersNoPageFromItSteps.js`
  (new): drives the real bridge the same way BL-829's own scenarios 1-2
  do (its own scoped Given/When/Then steps, distinct wording per BL-1277's
  unscoped-collision guard — never an unscoped duplicate). Reads the
  built-in page registry from the real `letsTalkRoutes.ts` module at run
  time (`operatorDocs`, `bubbleHealth`, `bubblePipelinePage`,
  `bubbleHostPage`, `bubbleLivePage`) — never a hand-typed list.
  - Scenario 01: a malformed entry whose id is not a built-in's → rejected
    whole, that id never appears among offered pages, every built-in page
    is still offered.
  - Scenario 02: a malformed entry whose id equals `bubbleLivePage.id`
    (`live`, BL-775's own new built-in — the collision the ticket pins
    before BL-775 lands) → rejected whole, and the offered page with that
    id is the bridge's own built-in entry (`entryPath`/`title` match),
    never the manifest's smuggled `entryPath`.

## Declared invariant

"A page id from a rejected operator manifest never appears among the
pages offered to the shell, and the bridge's built-in pages are offered
regardless of the operator manifest's state." — covered by the two
acceptance scenarios above (both scenarios drive the real bridge over a
real HTTP round trip; the invariant is inherently about the bridge's own
merge behavior under a live HTTP server, not a pure function this ticket
introduces, so no additional standalone property test is a better fit
than the two real-bridge scenarios — same posture as BL-829's own
scenarios 1-2, which carry no separate property test either).

## Verification

- `npx tsc -p .` (extension/): clean — no TypeScript touched by this
  ticket, only `.js` step handlers.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1634-*.feature`
  — 2/2 scenarios green. No leaked `/tmp/bl1634-*` fixture dirs after the
  run (BL-831's own bounce this session — the mkdtemp root is created and
  removed in a `finally` on each scenario's terminal step, `fixtureReaper`
  posture kept even though scenarios here are few enough that
  `onAbnormalExit` tracking would be belt-and-suspenders; confirmed no
  leaks via `ls -d /tmp/bl1634-*` after the run).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-829-bubble-remote-page-pager.feature`
  — scenario 02 (`a malformed page list rejects the whole manifest`) now
  `ok`, matching the ticket's constraint. Scenarios 03[1-4]/04/05 (the
  Android JVM suite) remain red — BL-1635's own root (a build-charset
  defect unrelated to this ticket), out of scope per this ticket's own
  `out_of_scope:`. Ran the acceptance command ONCE, per the ticket's own
  `qa_e2e_procedure:` instruction not to re-run.
- `npm test` (extension/): 630/630 test files passed, 10777/10777 tests
  passed (unaffected — this ticket touches no vitest-reachable
  production file).
- `npm run test:properties` (extension/): green (see run log).

## Files touched

- `specs/pipeline/steps/bl829BubbleRemotePagePagerSteps.js`
- `specs/pipeline/steps/bl1634RejectedManifestOffersNoPageFromItSteps.js` (new)
- this evidence file
