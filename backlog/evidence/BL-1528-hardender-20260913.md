# BL-1528 — hardender review pass, 2026-09-13

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: stryker run --mutate out/metrics/closingCeremonyRun.js
- **Commit hash**: pending
- **First error excerpt**: [Survived] ConditionalExpression closingCeremonyRun.js:100 if (failure) -> if (true): finalizeStaleCeremonyRuns never asserted a SUCCESSFUL stale-run failure-note send is excluded from finalizedFailedUndeliverable
- **Failure class**: mutation-gap
- **Expected vs observed**: a delivered failure note must never be reported undeliverable
- **Blamed role**: hardener
- **Remediation pointer**: added assertion result.finalizedFailedUndeliverable === [] to the existing delivered-note stale-run test; re-ran mutation, killed (9->8 survivors)

## D2

- **Failing command**: stryker run --mutate out/tools/night-closing-ceremony-run.js
- **Commit hash**: pending
- **First error excerpt**: [Survived] ArrayDeclaration night-closing-ceremony-run.js:187 night-stop case return [] -> return ["Stryker was here"]: no test asserted loudSurfaces stays clean through a full night-stop sequence
- **Failure class**: mutation-gap
- **Expected vs observed**: a non-lean-packet action must never pollute loudSurfaces
- **Blamed role**: hardener
- **Remediation pointer**: added assert.deepEqual(state.current.loudSurfaces, []) to the existing night-stop test; re-ran mutation, killed (41->40 survivors)

## Detail

Both D-items above were found and fixed in this same pass (not bounced
elsewhere) - recorded per Article 4.4 as the pass's own complete inventory.

Gates run:
- `npm run compile` - clean.
- Unit suite scoped to closing-ceremony files (7 files, 106 tests) - green
  before and after fixes.
- Full unit suite (`npm run coverage`, 616 files / 10479 tests) - green,
  no unrelated reds.
- Acceptance: `specs/features/BL-1528-an-undeliverable-closing-ceremony-note-is-a-failed-run.feature`
  via `run_acceptance.sh` - 5/5 scenarios pass, before and after the CRAP
  refactor.
- Property (BL-1528's own, `test/bl1528UndeliverableClosingCeremonySend.property.test.js`,
  `test:properties` lane, `npx vitest run --config vitest.properties.config.mjs`)
  - both invariant properties pass (50/30 runs).
- Standing whole-tree guards (18 files, 183 tests, `test/*Guard*.test.js`
  minus `.property.`) - all pass (files touched under `extension/test/`).
- DRY (`jscpd` on the 3 touched src files) - 0 clones.
- CRAP (`crapReport.js` on the 4 touched src files, against `src/*.ts` per
  the CRAP-vs-Stryker scoping rule): differential complexity gate applied
  against `main` baselines (workflow-detailed rule) -
  `runClosingCeremony` was 7 (baseline 5) after the coder/cleaner/architect
  pass; extracted `finalizeStaleCeremonyRuns`/`trySendNote` to restore it to
  5. `runNightClosingCeremony` was 12 (baseline 11); extracted
  `withRuntimeLoudCodes` to restore it to 11. `applyAction` unchanged at 10
  (baseline 10, pre-existing debt, not a regression). All new helpers
  (`finalizeStaleCeremonyRuns`=3, `trySendNote`=2, `surfaceLoudCodes`=2,
  `withRuntimeLoudCodes`=2) are well under 6.
- Mutation (Stryker, one file at a time, `--force`, load avg ~4-5 on 20
  cores throughout - cooldown gate said `run` for all three files):
  scoped the dry run via a ticket-local `vitest.bl1528.stryker.config.mjs`
  (temporary `stryker.config.json` pointer, reverted before forward) to
  dodge the standing Stryker-sandbox-only red in
  `test/activePoolFreshnessAudit.test.js` (BL-1066/2026-09-05 rule) without
  touching the shared `vitest.config.mjs`.
  - `closingCeremonyRun.js`: 90.00% -> 91.11% after D1's fix. 8 remaining
    survivors are all in `readPackConfText` (try/catch, `persisted` guard)
    and two `adjustments: []` literals - confirmed pre-existing by diffing
    against `main`'s copy of the file (unchanged lines), out of this
    low-slice ticket's scope.
  - `closingCeremonyStore.js`: 69.03%, 35 survivors, all in
    `recordCeremonyOutcome`'s `no_change` ternary and
    `recordCeremonyAdjustment`'s three throw-message literals - confirmed
    pre-existing by diffing against `main` (the ONLY functional change in
    this file is `finalizeCeremonyRunAsFailed`'s new `deliveryFailure`
    parameter, at line 111, which has zero survivors).
  - `night-closing-ceremony-run.js`: 15.49% -> 15.75% after D2's fix (281
    NoCoverage mutants are pre-existing `parseArgs`/`main`/date-helper/CLI
    wiring debt with 0% coverage on `main` too - not this ticket's diff).
    40 remaining survivors: two at `withRuntimeLoudCodes` (line 157,
    `length > 0` -> `true`/`>= 0`) confirmed EQUIVALENT by hand-patching
    `out/` to the always-true branch and re-running the full
    `nightClosingCeremonyRun.test.js` suite (8/8 still green) - spreading
    an empty array changes nothing observable, and nothing downstream
    compares object identity; every other survivor is in
    `localDayKey`/`parseHmToMs`/`gateBypassed`/`ceremonyIsDue`/the
    `prev.nightKey` continuation guard/the final `advanced` ternary, all
    confirmed unchanged against `main`'s copy of the file (only the local
    variable `state`->`finalState` rename touches the last one).
- No orphaned test/mutation processes left running (`pgrep -fl 'node
  --test|stryker'` clean before and after); `stryker.config.json` and
  `stryker-incremental.json` restored/removed so the worktree carries only
  the intended diff.

By hardender.
