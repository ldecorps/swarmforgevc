# BL-1617 — hardender review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

### Shell guard: `check_closed_ticket_subject.sh` — no mutation tool wired (BL-472)

Added two shell test cases to `swarmforge/scripts/test/test_check_closed_ticket_subject.sh`
covering gaps the existing suite didn't reach:

- **11: a single named id not positioned at the start still resolves**
  (subject-attribution's `:else` branch) and is refused when closed.
- **12: a ticket id filed under two folders on origin/main, only one `done`**
  — every folder listed by `git ls-tree` is checked, not only the first one
  the loop happens to see (seeded `done` then `paused` deliberately, since
  `backlog/done/` sorts before `backlog/paused/`).

Hand-authored mutation sweep (BL-567/BL-638 fallback — no mutation tool for
shell), applied directly to the guard script (verified no live readers via
`pgrep -fl check_closed_ticket_subject` first), suite re-run after each,
restored and diffed clean afterward:

| mutant | change | result |
|---|---|---|
| A | ambiguous check `-gt 1` → `-ge 1` | KILLED (test 11) |
| B | `all_done` folder comparison `==` → `!=` | KILLED (test 01) |
| C | `BRANCH == main` check `==` → `!=` | KILLED (test 01) |
| D | folders-empty boundary `-eq 0` → `-eq 1` | KILLED (test 02) |
| E | `active` folder classification dropped from the case arm | KILLED (test 02) |

All 12 shell test cases pass; 5/5 hand-mutants killed.

### Acceptance / Gherkin mutation

`run_acceptance.sh` on the feature: 8/8 pass (fresh `npm run compile` first).

`run_gherkin_mutation.sh` (hard, full re-test): 12 mutants, 7 killed, 5
survived — **all 5 are equivalent mutants, accepted with code-level reason
per BL-234/BL-1081's discipline**:

Every survivor mutates a character inside a `subject` Examples cell, and in
every case the mutated character sits in free-text prose the guard never
reads. `check_closed_ticket_subject.sh`'s only two reads of `$SUBJECT` are:
`grep -oiE "\b(BL|GH)-?[0-9]+\b"` (case-insensitive id extraction) and the
leading-id regex, which matches only an optional `close|promote|approve`
verb (case-insensitive) followed immediately by a ticket id. Neither read
touches any other word in the subject string.

- ex[0].subject `gate`→`Gate`: outside both regexes.
- ex[1].subject `Revert`→`REvert`: `Revert` is not one of the three leading
  verbs the regex matches, so it never enters the leading-verb group either
  way; the example already resolves via the single-named-id `:else` branch
  (test 11's shape), unaffected by this word's casing.
- ex[2].subject `see`→`See`: outside both regexes.
- ex[3].subject `subject`→`subjectx`: this Example is fully untagged (no
  `BL-`/`GH-` anywhere), so the guard exits 0 before either regex result
  matters — confirmed via `grep -oiE` on the mutated text, still zero hits.
- ex[5].subject `subject`→`suBject`: same as ex[2]/[0], outside both regexes;
  the id `BL-0009` (unaffected) still drives the outcome.

Verified live: grepped both regex literals from the guard script and
confirmed none of the five mutated substrings falls inside a ticket-id or
leading-verb match. Per BL-1081/BL-234, this is equivalence demonstrable
from the code, not a curiosity — recorded here, not force-killed with an
assertion on prose wording the guard was never meant to check.

### Verdict detail

12/12 shell unit, 8/8 acceptance, 5/5 hand-mutants killed, 7/12 Gherkin
mutants killed with the remaining 5 accepted-equivalent (reasoned above).
CRAP/DRY: shell has no tool wired (BL-472); the JS step handler
(`specs/pipeline/steps/bl1617RoleBranchCommitNeverLeadsWithClosedTicketSteps.js`)
already uses the `KNOWN_VALUES`-style `OUTCOME_KINDS` map per the Scenario
Outline handler rule, no changes needed there.
