# BL-809 — QA pass, 2026-08-05

Commit verified: 55ed7ddb86 (documenter). Confirmed ancestors: coder f4f4aa7b,
architect (merge a25aca9e), hardener 089eb088 — `git merge-base --is-ancestor
<commit> 55ed7ddb86` holds for each. Merged (fast-forward) into QA at
0b05a499 -> 55ed7ddb86.

## In-scope checks (all green, run directly by this role)

1. **Diff scope confirmed** — `git diff --stat 0b05a499 55ed7ddb86`: only
   `docs/how-to/BL-611-babysitterd-runbook.md`,
   `specs/features/BL-809-worktree-head-read-leaks-stdout-and-always-returns-nil.feature`,
   `specs/pipeline/steps/bl809WorktreeHeadReadSteps.js` + its registration in
   `specs/pipeline/steps/index.js`, `swarmforge/scripts/babysitter_assess_lib.bb`,
   and three `swarmforge/scripts/test/*.bb` files (unit test additions,
   the acceptance harness, and a property runner). Zero
   `extension/src`/`extension/media` TypeScript touched.
2. **Production `.bb` diff read directly** against R1-R4 and the two
   `invariants:`. `worktree-head-commit-10` and the sibling
   `list-untracked-files` now use `process/sh` (captures stdout by default,
   matching `handoffd.bb`'s already-correct equivalent per R1) and degrade to
   `""` / `[]` on failure instead of throwing into a type error. Confirmed
   `scan-claim-risks` (the production entry point, line ~158-178) builds its
   `assess-one-claim` argument map WITHOUT `:head-commit`, so the fallback at
   line 101 (`head (or head-commit (worktree-head-commit-10 worktree-path))`)
   is genuinely the live path — not a fallback that's never exercised.
   `handoffd.bb`'s own copy (line 1339) is untouched, matching R4 (no merge of
   the two copies).
3. **Unit suite, independently re-run** — `babysitter_assess_lib_test_runner.bb`:
   exit 0, all `assert=`/`assert-true` calls including the six BL-809
   scenario-0{1,2,3,4,5,6} assertions (real git worktree, non-blank HEAD on
   success, no stdout leak, severities `watch`/`warn-uncommitted`/
   `warn-fixture-droppings` all reachable, `list-untracked-files` genuinely
   called, moved-HEAD true negative, non-git worktree degrades to `""` not
   nil/throw) — satisfying R3's requirement that the regression test exercises
   a real worktree rather than "does not throw" alone.
4. **Property runner, independently re-run** —
   `bl809_worktree_head_property_runner.bb`: exit 0, all properties hold
   (successful-head-read-is-never-blank, stdout-leak-guard).
5. **Formal acceptance gate** —
   `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-809-worktree-head-read-leaks-stdout-and-always-returns-nil.feature`
   — 8/8 scenarios pass (TAP: `# pass 8`, `# fail 0`), driving the real
   `.bb` library through `bl809_claim_risk_sweep_harness.bb` (no
   reimplementation): head-read-returns-the-commit, no-raw-git-output-on-stdout,
   all three severity outcomes in the Scenario Outline (`watch`,
   `warn-uncommitted`, `warn-fixture-droppings`), untracked-files-are-actually-
   read, moved-head-is-not-a-stall, unreadable-head-degrades.
6. **e2e_qa_procedure (5 steps), all independently confirmed** via the
   acceptance run above: step 1 (leak gone) = scenario 02; step 2 (read works)
   = scenario 01; step 3 (stall detector fires) = scenario 03's three
   Examples rows; step 4 (true negative) = scenario 05; step 5 (degrades) =
   scenario 06.
7. **Wiring/callers** — grepped every call site of `worktree-head-commit-10`
   and `list-untracked-files`: production caller is `assess-one-claim`'s
   `head`/`untracked-list` bindings only, reached from `scan-claim-risks`;
   no stray unwired duplicate.
8. **Documentation** — `docs/how-to/BL-611-babysitterd-runbook.md`'s new
   "Claim-risk stall detection restored (BL-809)" section accurately
   describes the bug (leak + nil-on-success via `process/shell` missing
   `:out :string`) and the fix (`process/sh`, degrade to `""`/`[]`), plus a
   feature-file cross-reference matching the BL-802/BL-804 convention.
9. **No orphaned processes** — `pgrep -fl 'node --test|stryker'` clean
   before this pass; see the out-of-scope section below for how the one
   long-running check was terminated and reconfirmed clean.

## Out-of-scope: full extension unit suite (`npm test`) — BLOCKED by host load, not by this parcel

Host load was severe and worsening throughout this pass: `uptime` moved from
~95 through a sustained 150-190 range on this 4-core host (far past the
90-174 range already documented and accepted the same day in
`backlog/evidence/BL-813-qa-pass-20260805.md` and
`backlog/evidence/BL-638-qa-pass-20260805.md`).

Unlike BL-813's attempt (compile itself never completed), this run's `tsc`
compile succeeded and `npm test`'s `vitest` run progressed and was observed
directly: 54 of 447 test files completed in the first ~7 minutes, all green
apart from load-induced 20-second timeouts in files with zero relation to
this ticket's diff (`test/dependencyGateCliStorageGlobals.test.js`,
`test/dependencyGateCliReportsAndScope.test.js`, `test/bounceWatcher.test.js`,
`test/renderBriefingDiagramsCli.test.js`) plus one apparent failure
(`test/readLiveRoleHeldTicketsCli.test.js`, BL-487 territory, unrelated to
BL-809) whose empty-map-instead-of-populated-map shape is consistent with a
load-starved subprocess call rather than a logic regression — none of these
files, nor any file this parcel touches, appeared as a failure attributable
to BL-809's change. At the observed per-file rate, full completion would run
well past the hour mark with load still climbing, not shrinking — stopped
rather than let it run indefinitely; the `npm test` process group was killed
cleanly and reconfirmed via `pgrep -fl 'node --test|stryker|vitest'` (clean,
no orphans).

This ticket's own diff contains zero `extension/src`/`extension/media`
TypeScript (confirmed in check 1) — the one JS file this parcel adds
(`bl809WorktreeHeadReadSteps.js`) is a `specs/pipeline/steps/` acceptance step
handler, already exercised end-to-end and green via the formal acceptance
gate (check 5), itself a `node --test`-driven run — a working, unblocked
signal on the exact JS this parcel touches, obtained from a lighter-weight
invocation than the full extension suite, per the same load-bypass reasoning
already adopted twice this same day (BL-813, BL-638). The partial full-suite
run additionally corroborates rather than merely being silent: real vitest
output over 54 files, zero regressions touching this ticket's scope.

## Verdict: PASS

Every check that can isolate BL-809's own scope from the host-load condition
is green, independently re-run, and matches the ticket's required_wiring,
both `invariants:`, and every one of R1-R4's specifier rulings. Approved on
that basis. The continuing severe, worsening host-load condition (95 -> 190
avg during this single pass) is flagged to the coordinator alongside this
approval as a standing/worsening health signal (constitution Article 3.5) —
now confirmed across three consecutive QA passes the same day.
