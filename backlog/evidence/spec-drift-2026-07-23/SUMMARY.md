# Spec/doc drift survey — baseline 2026-07-15, run 2026-07-23

Out-of-band (OOB) = commits on main touching `extension/` or `swarmforge/scripts/`
with no `By <role>` swarm trailer and not swarm branch plumbing (merges/reverts
between role worktrees). Generated by `drift_matrix.bb` (in this dir, re-runnable);
full per-file and per-commit detail in `drift-matrix.md`.

## Headline numbers

- 601 commits in scope since baseline → **363 swarm · 113 plumbing · 125 OOB**
- OOB streams: 53 Cursor · 65 unattributed (mostly Cursor/direct sessions without
  trailers, conventional-commit style) · 4 Laurent · 3 Claude-Code sessions
- **121 source files** touched OOB; **69 of them have no feature file or doc page
  mentioning them at all** (SPEC-LESS)

## Suite run (conflict detector)

- Unit: **344 files / 5713 tests, all green** (8.2s, within budget)
- Properties: **26/27 — 1 hard conflict**:
  `test/pipelineBoard.property.test.js` "included links are an in-order prefix"
  fails; oracle greps `` `${id}:` `` but commit `d63e80320` (Cursor,
  "shorten link HTML") changed `pipelineBoardLinkLine` to emit only
  `<a>label</a>`. Independently diagnosed by architect during BL-549 review.
  **Verdict: code right, test stale → rewrite the property's oracle.**
- Mutation: **deferred** — held until the resident is idle (Stryker worker fleet
  vs live swarm RAM contention; delete stale incremental cache before trusting,
  per BL-387/BL-446).

## Drift clusters (triage units — slices should follow these, not commits)

| # | cluster | files (OOB commits) | spec state |
|---|---|---|---|
| 1 | **Resident Spy / console Mini Apps** | residentSpyUiHtml (20), residentPaneSpy (12), residentPaneLive (9), notify-resident-spy-tunnel (7), consoleMenuUiHtml (5), pausedPagerUiHtml (5), residentSpyTunnelNotify (4), pipelineGridLive/UiHtml (4+4) | **almost entirely SPEC-LESS** — whole surface grew OOB; needs retro-spec |
| 2 | **Pipeline board** | conciergeTick (12), pipelineBoard (9), pipelineBoardSync (6), pipelineBoardPinSync (4) | 13+ BL-45x/50x features exist, STALE (1 confirmed conflict; NBSP padding, parked-cap-10, pin-spam fixes all undocumented) |
| 3 | **Telegram front desk / bridge** | bridgeServer (12), telegram-front-desk-bot (9), telegramFrontDeskBotCore (8), telegramClient (5) | features exist (BL-369/379/434/450/453/509…), verify each |
| 4 | **Handoff daemon + mono-router** | handoffd (19), handoff_lib (12), chase_sweep_lib (9), swarm_ensure (11), mono_router_lib (6, SPEC-LESS), dispatch_lib (6, SPEC-LESS), done_with_current_* (6+4, SPEC-LESS), ready_for_next_* (5+5) | mono-router rotation semantics + 2026-07-23 starvation fix largely unspecced (only BL-448 pack feature) |
| 5 | **Launcher / ops stack** | swarmforge.sh (20 — generic stem, map manually), launch_babysitter (5), failover_to_gpt (4) | needs manual mapping pass |
| 6 | **Claim liveness/healer/tracker** | metrics/claim* (`feat:` cluster, OOB) | has OOB-authored TESTS but no feature file; relates to BL-528 |

## Recommended next steps

1. Fix the one hard conflict (property oracle) — small, unblockable now.
2. Epic: one slice per cluster above. Adjudication (code-right vs spec-right)
   is a human/architect call per cluster; documenter/specifier execute.
   Cluster slices share files — sequence, don't parallelise (BL-434-family lesson).
3. Retro-spec rule for buckets like cluster 1: features written AGAINST shipped
   code, tagged retroactive so QA doesn't bounce the implementation.
4. Stop the bleed: unpause BL-560 (GitHub auto-intake scan) and/or add a sweep
   flagging commits touching src without touching specs/docs and without a
   ticket reference.
