# BL-1075 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-1075 — window-size mitigation the panel overrides
- **Parcel**: `00_20260822T223309Z_000480_from_documenter_to_QA_for_QA` (from
  documenter, commit `8fcb8b57e5`)
- **Reviewed by**: QA, 2026-08-22
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-1075`
  → exit 0, `VERIFY BL-1075`. No open deferral.

## Verdict

**PASS — inventory items: NONE.**

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry | PASS — all stages confirmed ancestors |
| G2 | Prior-bounce check | PASS — none for BL-1075 |
| G3 | Compile | PASS (N/A functionally — `paneTailer.ts` was NOT modified by this parcel per the coder's own explicit note; invariant 3 is proven against a real server instead, matching the ticket's own "How" guidance that only the two hardening functions change) |
| G4 | `bb swarmforge/scripts/test/bl1075_window_size_ownership_property_runner.bb` | PASS — ALL 12 SERVERS PASSED (`{:mechanism-measured 1 :mixed-heights 11 :default-rows 8 :path-shell 6 :path-bb 6 :doc-read 1}`), matches coder's/architect's recorded runs |
| G5 | Acceptance (`run_acceptance.sh specs/features/BL-1075-window-size-mitigation-the-panel-overrides.feature`) | PASS — 7/7 |
| G6 | BL-113 Gherkin mutation | PASS — 4/4 killed, 0 survived (hardener's batch evidence) |
| G7 | required_wiring (`bl1075WindowSizeOwnershipSteps` registered) | PASS — confirmed by direct grep of `specs/pipeline/steps/index.js` |
| G8 | Code change is real, not just claimed | PASS — `grep -n "window-size" swarmforge/scripts/swarmforge.sh swarmforge/scripts/control_plane_lib.bb` shows only explanatory comments, no live `set-option ... window-size` line in either file (both direct reads, not grep-for-comment) |
| G9 | `focus-events off` (the live knob) still applied | PASS — confirmed present in both `harden_tmux_server` (`swarmforge.sh`) and `harden-server!` (`control_plane_lib.bb`) by direct read; architect independently confirmed `focus-events` defaults `on`/unset on a fresh server so the property runner's "off afterwards" assertion is non-vacuous |
| G10 | Docs currency | PASS — `docs/how-to/BL-tmux-wsl-segfault-upgrade.md` names no window-size mitigation; explicitly says what does carry the load (the tmux >= 3.7 preference) — confirmed by direct read |
| G11 | Fixture hygiene (this ticket leaked 6 tmux servers at authoring time — coder's own evidence) | PASS — architect independently re-confirmed post-fix: 0 surviving servers, 0 surviving directories, live swarm's 8 sessions untouched after both the property runner and acceptance suite; not re-repeated a third time here given two independent confirmations already exist and my own runs above produced no new `/tmp/bl1075-*` residue (checked) |
| G12 | Orphaned processes | PASS — clean before/after; the hardener's live `lineDiff.js` Stryker run (BL-1015 remediation, another worktree) noted again, not orphaned |
| G13 | TypeScript side not excused by the shell/Babashka tooling fallback | PASS — the ticket's own qa_e2e step 6 requires this distinction be honored; confirmed `paneTailer.ts` is genuinely untouched (not merely undertested) — `git diff --stat` against this parcel's own commit range shows zero `extension/` files, so there is no TS surface this parcel owns that the shell-lane exemption could be wrongly used to excuse |

## Live verification (read-only, on this real host)

- `tmux -S <live socket> show-options -gv focus-events` → `off` — the live
  knob remains applied on the actual running control plane.
- `tmux -S <live socket> show-options -gv window-size` → `largest` (stale) —
  this is the CURRENTLY-RUNNING server's leftover state from before this fix
  landed in code; the fix stops the option being SET going forward, it does
  not retroactively unset an already-hardened live server, and an inert
  leftover on an existing server violates nothing this ticket's invariants
  claim (the invariant is "never applies or documents" the mitigation, not
  "cleans existing servers"). The property runner's `:mechanism-measured 1`
  and `:path-shell 6`/`:path-bb 6` already exercise the NEW code against
  freshly-spawned real servers, which is the correct place to prove the
  fix — not this session's own long-running production control plane, which
  I deliberately did not bounce/restart (same reasoning as the BL-1069
  review: restarting the live control plane this QA session itself runs
  under is a real risk with no additional verification value beyond what
  the fixture-based real-server property runner already proves).

## Sibling content riding in this commit

Minimal — only `docs/reference/Specification.MD` beyond BL-1075's own files.
This is the last of the four-ticket hardener batch (BL-1069 bounced,
BL-991/BL-1029/BL-1075 approved) to reach QA this session.

Per "An Approval Authorizes Only Its Ticket's Work" (BL-506) and Article
2.6, **this approval names BL-1075 only.**

By QA.
