# BL-1280 QA pass — 2026-08-30

Commit verified: merge of documenter `a6a3243b76` into QA. BL-1280's own
coder/cleaner/architect/hardener/documenter commits (8e1c58d318, 6259b0b9ee,
3cab95d2b2, 79b18b89d6, a6a3243b76) are all confirmed ancestors of the merge
HEAD (`git merge-base --is-ancestor`).

## required_wiring — confirmed
`specs/pipeline/steps/index.js` registers `bl1280MkdtempMigrationCompleteSteps`.

## qa_e2e_procedure
1. `npx vitest run test/tmpDirMigrationGuard.test.js`: 12/12 pass, 0 failed
   (ticket expected 11; the hardener added one more real-error-vs-ENOENT test,
   which is a legitimate addition, not a discrepancy).
2. The guard's own "the real extension/test/ tree has zero raw mkdtemp call
   sites outside the shared helper" assertion passes — `findRawMkdtempCallSites`
   returns empty against the real tree.
3. Guard-not-blunted: "still flags a raw call in a DIFFERENT helper file" and
   "flags a raw call planted in a fixture test dir" both pass.
4. `SELF_EXEMPT_RELATIVE_PATHS` = exactly `['helpers/tmpDir.js',
   'tmpDirMigrationGuard.test.js', 'tmpDirMigrationGuard.property.test.js']` —
   still three, does not name `pilotMkdtempConventionCheck.test.js`.
5. `npx vitest run test/pilotMkdtempConventionCheck.test.js`: 9/9 pass — the
   planted fixture string still exercises the checker.
6. Full unit lane: see below — no test regressed on temp-dir lifetime;
   `helpers/tmpDir.js` itself (the allocator) is untouched by this ticket.

## Acceptance and property tests
- `specs/features/BL-1280-mkdtemp-migration-completed-for-the-real-test-tree.feature`:
  4/4 pass.
- `test/bl1280MkdtempMigrationInvariants.property.test.js`: 6/6 pass, both
  invariants (no premature-sweep placement; exempt list stays at three and
  the fixture-data path stays intact).

## Spot-checked two migrated files
`rotationDynamics.test.js`, `thinMainGateCli.test.js`: mechanical
`fs.mkdtempSync(path.join(os.tmpdir(), 'PREFIX-'))` → `mkTmpDir('PREFIX-')`,
prefix strings byte-identical, matching the ticket's constraint.

## Full unit lane (`npx vitest run`, extension/) — pre-existing noise, none of it BL-1280's
218 failed / 9655 tests across 26 files. Investigated before accepting as
pre-existing, per the "grep before calling a red untracked" rule:
- **366** occurrences of `CURSOR_API_KEY is not set` — this QA worktree has
  no `.swarmforge/swarm.env` (confirmed: file absent) and `CURSOR_API_KEY` is
  unset in the shell from the start (not a mid-run leak). Matches the known
  BL-720 leak-cascade failure SIGNATURE but the specific trigger here is
  session environment, not the BL-720 delete-instead-of-restore bug (ran with
  `--exclude test/cursorBridgeAgentSession.test.js` — same 218/26, confirming
  it isn't that file's leak in this instance).
- **32** occurrences of `deps.checkOrphanedAuthoredDocs is not a function` —
  confirmed = already-ticketed BL-1229 (paused), same signature I verified
  independently while QA-passing BL-1279 earlier today.
- A handful of other pre-existing reds (`tempDirTrapGuard`,
  `socketFixtureShortRootGuard`, `constitutionDocCitations`, an epic-icon
  collision, two bridge-log regex mismatches) in files BL-1280 never touched.
- **Zero overlap** between BL-1280's actual diff (`git diff --name-only
  8e1c58d318~1 a6a3243b76 -- extension/test/`, 33 files) and the 26 failing
  files, except `pilotScopedCrapCheck.test.js` — and that file's own failure
  is the BL-1229 signature above (a `landPilotedTicket` deps-stub gap
  unrelated to its temp-dir line), not anything BL-1280 changed in it.
  `helpers/tmpDir.js` (the actual `mkTmpDir` allocator) is untouched by this
  ticket; only `helpers/rawMkdtempGuard.js` (the scanner) changed, and only
  the guard's own test file exercises it, at 12/12 green.

## Verdict: APPROVE
The migration is mechanical, correct, complete (guard scan is empty against
the real tree), and the fixture-string exemption is preserved. The full-suite
noise is pre-existing, already attributed (BL-720-shaped session env gap,
BL-1229), and provably disjoint from this ticket's own changes.

By QA.
