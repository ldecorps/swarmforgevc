# Evidence — BL-1437 — coder pass (2026-09-06)

Role: coder, wearing the Model Steward hat for this discrete task (per
`model-steward.prompt` Owns/Does Not Own - the steward never assigns, the
factory does, from the row this parcel adds).

## What was implemented

1. **The real review sample.** Used the Art Director's own already-recorded,
   real BL-1419 review (`backlog/evidence/BL-1419-art-director-20260906.md`,
   By art-director, 2026-09-06 - Approved, with one non-blocking follow-up
   that landed same-day as BL-1442) as the certification evidence, per the
   ticket's own `approval_context`. Transcribed its six findings, faithfully,
   into a scorecard - never re-adjudicated or upgraded by the coder.
2. **A minimal schema extension** (ticket's own "How" permission, since the
   evaluate scorecard shape had no concept for a reviewer role's own duty):
   `model_steward_evaluate_lib.bb` gains a sixth capability dimension,
   `look_and_feel`, and a `competency->dimension` branch matching
   "look"/"feel" keywords. Additive only - an existing scorecard whose
   competency names never match those keywords still populates exactly the
   original five dimensions, unchanged (confirmed: `bl556_evaluate_ingest_test_runner.bb`
   still ALL PASS). `swarmforge/model-steward/schema/registry.schema.json`
   documents both this dimension and `art-director` in the role list (was
   stale since BL-1418 added the specifier's own 8th role too).
3. **The steward CLI run as a discrete steward task** (invariant 3):
   `bb swarmforge/scripts/model_steward_cli.bb evaluate anthropic/claude-sonnet-5
   --role art-director --scorecard <scorecard path>` against an isolated
   temp state dir. The resulting role_matrix row, capability entry and
   certification report are the CLI's OWN computed output - never hand-
   invented (invariant 1) - transcribed verbatim into the committed seed
   and the two committed artifacts below.
4. **Committed artifacts** (new `swarmforge/model-steward/` subdirectories):
   - `scorecards/art-director-claude-sonnet-5-bl1419-20260906.json` - the
     scorecard itself, `scorecard_id`
     `art-director-scorecard:bl1419-briefing-email-claude-sonnet-5-20260906`.
   - `certification-reports/anthropic__claude-sonnet-5__art-director__20260906.json`
     - the certification report the CLI produced, carrying the same
       `scorecard_id` and (see "A real gap found" below) its own `role`.
5. **`swarmforge/model-steward/seed/models.seed.json`** gains exactly the
   `art-director` role_matrix row the CLI computed:
   `{"provider":"anthropic","model":"claude-sonnet-5","score":1.0,"evidence":"art-director-scorecard:bl1419-briefing-email-claude-sonnet-5-20260906"}`.
   Deliberately **not** folded in: the live `evaluate` run's capability-
   registry overwrite (every one of claude-sonnet-5's five ORIGINAL
   dimensions flattens to 1.0, since all six scorecard entries passed and
   `set-capability-entry` REPLACES the entry wholesale, not merges). That
   would silently corrupt the model's existing, unrelated coding-competency
   capability profile (0.95/0.98/0.93/0.9/0.7) with numbers derived from an
   EMAIL LOOK-AND-FEEL REVIEW - capability-registry data invariant 2 does
   not name as protected, but corrupting it is still real, avoidable
   collateral this parcel does not need to cause (confirmed: nothing in
   `ranked-on-role-matrix?`, `assignment-eligible?`, or ModelFactory's own
   role resolution ever reads the capability registry - only role_matrix and
   `status` matter for eligibility; capability entries feed only `capability`/
   `show`/`compat-docs` display). The seed's `capabilities."anthropic/claude-
   sonnet-5"` section is therefore untouched by this parcel.

## A real gap found and fixed (BL-654-adjacent, not a declared invariant)

`build-evaluate-report` never recorded WHICH ROLE an evaluate run was FOR -
only provider/model/gates/scorecard_id. Writing the acceptance feature
against the specifier's own scenario 04 ("the certification report ...
names the role, the provider and model, the scorecard it rests on, and the
date") surfaced this: the report the CLI actually produces had no `role`
field at all. Fixed by threading `role` through `apply-evaluate` into
`build-evaluate-report` (nil is omitted, never a fabricated default - no
existing caller passes a report through without a role, so this is
additive with no regression risk; confirmed no JS/TS consumer reads this
shape at all). The committed certification report was regenerated through
the CLI a second time, now correctly carrying `"role":"art-director"`.

## A process correction, recorded rather than hidden

My first pass wrote its OWN feature file and step handler from scratch
without first checking whether the specifier had already minted one. They
had - `specs/features/BL-1437-the-art-director-seat-has-a-certified-model.feature`,
authored in the same commit that minted this ticket (`184089b44b`), four
scenarios / five runs, INVEST-compliant. Caught before commit: restored the
specifier's original file byte-for-byte (`git checkout HEAD --
<path>`) and rewrote the step handler to match ITS exact Given/When/Then
text instead of my own invented wording. The specifier's own scenario 04 is
what surfaced the missing `:role` field above - a fixable had I not
restored their file and kept mine, which asserted something weaker.

## Non-vacuity (BL-654), all three declared invariants

`swarmforge/scripts/test/bl1437_art_director_seat_certification_property_runner.bb`
- 24 generated cases, driving the REAL `model_steward_cli.bb evaluate`
against isolated MODEL_STEWARD_STATE_DIR/MODEL_FACTORY_STATE_DIR temp dirs
seeded from THIS repo's own committed seed. Each break applied by hand,
confirmed via `diff` against a pre-edit backup empty after restore,
property runner green again immediately after each restore.

- **Invariant 1** (a role_matrix row's evidence resolves to a committed
  scorecard; every certification has a report artifact): `evidence-pointer`
  changed to return a fixed, fabricated string instead of the scorecard's
  own id. 24/24 cases failed - every generated row's evidence differed from
  its own scorecard's id, and no certification report carried it.
- **Invariant 2** (the factory/gate's shared eligibility notions are
  untouched for other roles): `add-role-ranking` changed to write the new
  row into EVERY existing role_matrix key, not just the requested role.
  168/168 assertions failed (7 other roles × 24 cases) - `role-matrix
  <role>` for architect/coder/cleaner/QA/hardender/documenter/specifier all
  changed after evaluating art-director alone.
- **Invariant 3** (the steward never assigns - ModelFactory's own state is
  untouched): `run-evaluate` changed to write a spurious file under
  `MODEL_FACTORY_STATE_DIR` after a clean evaluate. 24/24 cases failed -
  every generated case found a leaked file under the factory's own state
  dir from a steward-only command.

All three restored; `diff` against pre-edit backups empty; every check
below confirmed green again immediately after each restore.

## Checks run this pass

- `bb swarmforge/scripts/test/bl1437_art_director_seat_certification_property_runner.bb`
  - ALL PASS, 24 cases.
- `bb swarmforge/scripts/test/bl556_evaluate_ingest_test_runner.bb` (regression,
  the shared evaluate-ingest TDD runner, now covering the `look_and_feel`
  dimension and the `:role` field) - ALL PASS.
- Acceptance `specs/features/BL-1437-...-has-a-certified-model.feature`
  (the specifier's own, restored) - 5/5.
- Acceptance `specs/features/BL-1418-the-art-director-seat-is-addressable.feature`
  (regression, qa_e2e #1's own requirement) - 7/7.
- `bb swarmforge/scripts/test/bl1318_pack_staffing_gate_property_runner.bb`
  (regression, 400 draws) - ALL PROPERTIES HOLD.
- `bb swarmforge/scripts/test/bob_starting_cast_test_runner.bb`,
  `model_factory_test_runner.bb`, `model_steward_test_runner.bb`,
  `model_steward_trial_lib_test_runner.bb`,
  `pack_staffing_gate_lib_test_runner.bb` (regression) - ALL PASS.
- `bash swarmforge/scripts/test/test_model_factory_cli.sh`,
  `test_model_factory_runtime_wiring.sh`, `test_model_steward_cli.sh`,
  `test_model_steward_trial_cli.sh`, `test_pack_staffing_gate.sh`
  (regression) - ALL PASS.
- Live qa_e2e probes, against the real committed seed (isolated state
  dirs, never the live `.swarmforge/model-steward/`):
  - `role-matrix art-director` → `anthropic/claude-sonnet-5 1.0
    art-director-scorecard:bl1419-briefing-email-claude-sonnet-5-20260906`.
  - `show anthropic/claude-sonnet-5` → `status":"certified`.
  - `assign --mode quality` → JSON names `"art-director"`.
  - `pack_staffing_gate_cli.bb` against a 2-line windows file built from
    the REAL `swarmforge/packs/full-forge.conf` art-director line (with a
    synthetic art-director-gate scorecard, the SEPARATE, out-of-scope
    BL-1079 gate) and the specifier's own claude-fable-5-1 line: art-director
    decides `pass`; specifier decides `refuse` naming `seat-model-unresolved`
    - exactly qa_e2e #4's own claim.
  - `compat-docs` smoke-tested against the isolated seed - no crash, renders
    the `art-director` role-matrix section alongside the other seven.

## A standing red found, unrelated - not this parcel's

`swarmforge/scripts/test/test_pack_staffing_gate_wiring.sh` case 01 fails
on a plain `git clone` of `main`'s own HEAD (commit `59337764a2` - before
ANY of this parcel's own edits), driving a self-contained fixture registry
with no dependency on the committed seed or this ticket's own changes.
Per the standing-red rule (2026-09-05): grepped `backlog/standing-reds.tsv`
and `backlog/` for its basename - no existing owner found. A priority-00
`unowned-red` note naming the file and the failing assertion (`"01: an
uncleared seat (cursor/nightly, no steward mapping) should have refused
parse_config"`) is sent to the specifier and coordinator alongside this
parcel's own forward, per role prompt.

## Out of scope, confirmed untouched

- The specifier seat's claude-fable-5-1 gap (confirmed still refuses,
  naming that seat, in the live qa_e2e probe above).
- The compliance-battery gate's own art-director-gate scorecard for the
  REAL runtime state (BL-1079's own territory) - the acceptance/property
  fixtures seed a synthetic one for isolated testing only; nothing here
  writes to the live `.swarmforge/model-steward/scorecards/`.
- What the steward certifies for the other seven roles - untouched (their
  role_matrix rows, capability entries, and adapters are all byte-identical
  in the diff).
- Documenter's own stage (compat-docs regeneration on the committed docs
  path, the launch how-to) - untouched here.
- `extension/docs/briefings/2026-09-04.json` remains untracked and was not
  created by this parcel.
