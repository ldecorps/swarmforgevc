# BL-1216 — QA bounce, 20260828

## D1 — entangled tip: cited commit carries BL-1192/1207/1211's foreign paths (BL-506 tip-purity violation, fourth occurrence this pass)

1. **Failing command:**
   ```
   env -u GIT_DIR -u GIT_WORK_TREE git diff --name-only origin/main...a984721f18
   ```

2. **Commit hash:** `a984721f18` (documenter's `docs(BL-1216): document
   duplicate-id finding pool/content verdict`), received via `git_handoff`
   payload `merge_and_process documenter a984721f18`.

3. **First error excerpt** — same cluster as the three prior bounces this
   pass (`backlog/evidence/BL-1227-qa-bounce-20260828.md`,
   `BL-1192-qa-bounce-20260828.md`, `BL-1201-qa-bounce-round2-20260828.md`):
   the diff carries complete BL-1192 (`task_scope_gate_lib.bb` + test
   runner, `backlog_hygiene_lib.bb` + test runner, `swarm_handoff.bb`,
   feature/steps, `handoff-protocol.md`), BL-1207
   (`bl1207AbandonedLockLivenessSteps.js`), and BL-1211
   (`bounceResurrectionGitAdapter.ts`, `quarantineLiftCliArgs.ts`,
   `recovery-filter-check.ts`, `recoveryFilterCliArgs.ts`, its tests and
   feature/steps) paths, beyond BL-1216's own scope
   (`specs/pipeline/steps/bl1216DuplicateIdLiveCopyContentVerdictSteps.js`,
   `backlog/evidence/BL-1216-cleaner-pass-20260828.md`, and whatever this
   documenter commit itself authors).

   Fourth parcel this QA pass from `swarmforge-coder` carrying the same
   entangled cluster — see the branch-wide `note` already sent to specifier
   + coordinator this pass (`swarmforge-coder entangled 3x: BL-1227/1192/
   1201 bounced, tip-purity broken`) for the systemic finding; this is now
   the fourth confirmation, not a new escalation.

4. **Failure class:** `behavior` (BL-506 entangled tip, as prior three).

5. **Expected vs observed:** Expected — a `git_handoff` naming `BL-1216`
   cites a commit touching only BL-1216's own paths. Observed — same
   foreign cluster as the three preceding bounces this pass.

## Other checks this pass

- BL-1216's own content not independently re-verified — **BLOCKED BY D1**,
  same reasoning as the three prior bounces this pass: not QA's call to
  resolve BL-1192/1207/1211's disputed/incomplete content on BL-1216's
  behalf. Separately, BL-1216 itself was still awaiting a specifier
  disposition over an architect-raised duplicate-id finding as of
  BL-1227's bounce this pass — worth the coder/specifier confirming that
  disposition landed before rebuilding tip-pure.

## Remediation

Coder: rebuild a tip-pure `BL-1216`-only commit on `origin/main` and
re-send, once its own specifier disposition (if still open) is resolved.
