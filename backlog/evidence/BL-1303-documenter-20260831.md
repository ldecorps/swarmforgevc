# BL-1303 documenter pass — 2026-08-31

Ticket: BL-1303 — a feature file can reach `main` with no registered step
handler; new `check_feature_handler_registration.sh` guard, wired into
both `pre-commit` (via `run_commit_guards.sh`) and `pre-merge-commit`
(previously a single-guard delegate), aggregated through a new shared
`commit_guard_chain_lib.sh`.

Received: hardener's `0a66341002` ("BL-1303: bring
assessFeatureHandlerRegistration and walkRegistry under CRAP<=6..."),
merged here as `c5572e8a94`.

Landing precondition (specifier note): BL-709's registration line must be
present on `main` before this parcel lands, since the guard refuses on
`main`'s STATE once reached on the merge path. Verified on `origin/main`
after fetch: `specs/pipeline/steps/index.js` line 398 registers
`bl709BubbleItsOwnTelegramTopicSteps` — precondition met, nothing blocking
this forward.

## Deliverables checked

- **README / guides / command lists** — no new extension command, setting,
  or developer-facing CLI invocation (the guard scripts are shelled by the
  git hooks, not run by hand as part of any documented workflow). Nothing
  to update there.
- **`docs/reference/Specification.MD`** — this project's standing
  changelog-of-record for landed tickets had no BL-1303 entry. **Fixed**:
  added a new top-of-file entry describing the guard, the two live
  incidents motivating it (both by merge, not a fresh commit), and why
  `pre-merge-commit` runs two guards rather than the whole chain. Same date
  as today; content and date land in the same commit.
- **`docs/reference/BL-632-commit-time-guard-refuses-pipeline-code-on-main.md`**
  — read in full. Its "How It Works" section asserted "two hooks delegate
  to the same standalone script" (`check_pipeline_code_on_main.sh`), which
  BL-1303 makes factually wrong: `pre-merge-commit` now runs two guards, and
  `pre-commit` runs five. **Fixed**: corrected that section to describe both
  hooks' actual chains, added a new "Guard-Chain Aggregation Is Shared, Not
  the Whole Chain (BL-1303)" section covering `commit_guard_chain_lib.sh`
  and the deliberate choice not to widen `pre-merge-commit` to the full
  chain, added a Testing entry for the new
  `test_pre_merge_commit_hook.sh`/widened `test_run_commit_guards.sh`
  coverage, and a Related Tickets entry for BL-1303 (and BL-1240, its
  same-shape sibling on a different registry, which was missing from that
  list independently of this ticket — added while in there).
- **Diagrams** — `docs/diagrams/architecture.mmd` was grepped for
  `pre-commit|pre-merge-commit|commit.guard|check_pipeline_code_on_main|check_feature_handler`.
  The two hits (BL-1096, BL-570) are historical changelog comments in the
  file, not graph nodes/edges depicting the commit-guard-chain mechanism —
  no diagram in the registry (architecture/swarm-flow/handoff-flow/
  front-desk-flow) depicts commit-hook internals, so no change-trigger
  fired. No diagram update needed.
- No user-visible extension behavior changed — this is entirely git-hook
  and CI-shape machinery, invisible to the extension's own UI/commands.

## Items

NONE — two content fixes (Specification.MD entry, BL-632 doc correction +
new section), no defects found in existing documentation.

By documenter.
