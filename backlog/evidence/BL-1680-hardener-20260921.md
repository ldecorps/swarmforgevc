# BL-1680 — hardener review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

Checked the fix's object-spread ordering explicitly: `runLocalSeatTurnFn`
builds `{...input, modelId: DEFAULT_LOCAL_SEAT_MODEL_ID, readEndpoint:
..., complete: ...}` - `modelId` is declared textually AFTER the `...input`
spread, so any `modelId` key `input` might carry can never win over the
pin (JS object-literal semantics: later keys always override earlier
ones from a spread). Confirmed `resolveLocalSeatModelId`'s own contract
(`localQwenSeat.ts:44-56`): a non-empty `configured` argument short-
circuits before `process.env[SWARMFORGE_LOCAL_SEAT_MODEL]` is ever read,
so the pin genuinely eliminates the env dependency rather than merely
outracing it.

- `echo $SWARMFORGE_LOCAL_SEAT_MODEL`: `qwen2.5-coder:latest` (the live
  incident precondition, confirmed still present on this host).
- `node specs/pipeline/cli.js` on BL-1384's own feature: 4/4 (the exact
  scenario that was 3/4 red before this fix, re-run against the live env
  value that caused it).
- `node specs/pipeline/cli.js` on BL-1680's own new feature: 3/3.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1680LocalSeatFixturePinsItsModelInvariants.property.test.js`:
  1/1.
- All 21 standing whole-tree guard files (210 tests) green.
- `git diff --stat origin/main -- extension/src/tools/localQwenSeat.ts
  extension/src/tools/localQwenSeatLive.ts`: empty - production
  untouched, per the ticket's own FIRM claim.
- `standing_red_register_cli.bb .`: the BL-1384 feature row is present
  and owned by BL-1680; `"unowned":[]`.
- `npm run compile`: clean.
- No leftover processes or fixtures.
