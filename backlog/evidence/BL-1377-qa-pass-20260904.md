# BL-1377 — QA pass, 2026-09-04

## Received

Documenter's commit `50af836208` (task
`BL-1377-a-suites-failure-set-is-recorded-once-per-base-commit`), merged
into this worktree (`b2112dd26b`).

## Verification

- `npm run compile` — clean.
- `bb swarmforge/scripts/test/suite_baseline_lib_test_runner.bb` — ALL PASS.
- `bash swarmforge/scripts/test/test_suite_baseline_cli.sh` — ALL PASS
  (37 checks, matching coder evidence V2).
- `bb swarmforge/scripts/test/bl1377_suite_baseline_property_runner.bb` —
  ALL PROPERTIES HOLD (500 runs), both declared invariants (BL-654).
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1377-a-suites-failure-set-is-recorded-once-per-base-commit.feature`
  — 10/10 pass, matching coder evidence V4.
- Process hygiene: no orphaned `node --test`/`stryker`/`vitest` before or
  after (`pgrep -fl` self-match on the check command itself excluded).
- Wiring: `suite_baseline.sh`/CLI is not yet called from any role prompt —
  confirmed **deliberate**, not a BL-149-class gap. The ticket's own
  `notes:` states the evidence-sentence wiring into role prompts is a
  separate specifier deliverable (BL-798); `required_wiring:` is
  intentionally absent because the only real consumer is prose in a role
  prompt, not a code call site. Coder's own evidence names the same scope
  boundary independently. Architect's pass traced `decide`'s control flow
  directly in source (not just the property's pass) and confirmed both
  invariants hold structurally.
- `human_approval: approved`, no `ruling_options` posed (approval_context
  is a straight efficiency ticket with one named judgement call, not a
  choice) — no `human_ruling:` expected. `bounce_history`: none.
- Hardener batch evidence (`BL-1360-...BL-1378-hardener-batch-20260903.md`)
  independently confirms: acceptance 10/10, BL-113 soft Gherkin mutation
  8/8 killed, `.bb` files gated by their own unit/property suites (no
  mutation/CRAP/DRY wired for Babashka, per engineering.prompt), DRY
  unchanged/improved, standing whole-tree guard reds pre-existing and
  unrelated.

## Verdict

PASS. Approving BL-1377's own work.

By QA.
