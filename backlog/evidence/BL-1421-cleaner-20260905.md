# BL-1421 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed the full diff on `post_qa_branch_sweep_lib.bb` and `handoffd.bb`:
  `decide-role`'s precedence swap (in-process? before dirty?),
  `normalize-state-for-landed` now preserves `:surfaced` across a landed-sha
  change and resets only `:settled`, `record-surface!`'s upsert (at most
  one entry per role+reason), and the new shared `surface-or-suppress`
  helper that unifies the `:surface` and settle-fails-to-`:divergent-branch`
  paths — this is itself a DRY improvement over the pre-existing code,
  which is exactly what caught the coder's own found-and-fixed bug (the
  settle-fails path used to surface unconditionally, never suppressing a
  repeat divergent-branch tell).
- `caught-up-to-told?` in `handoffd.bb` reuses the same `git-is-ancestor?`
  primitive `:can-ff?` already uses — no new git-invocation pattern
  introduced.
- Confirmed the diff seen when comparing against an older base includes
  unrelated BL-1416 content only because the coder's branch diverged
  before that ticket's cleaner-side DRY fix landed on this branch; the
  actual merge (3-way, common ancestor a95f87849d) did not reintroduce or
  overwrite it — verified `report_canary_once`/`run_bounded_kill_group` in
  `check_property_suite_drift.sh` (an unrelated file) is untouched and
  intact post-merge.
- No duplication introduced: `jscpd` doesn't parse `.bb` (0 files
  analyzed, informational only), but a manual read confirms the new
  `surface-or-suppress`/`caught-up-to-told-fact` functions are each
  single-purpose and eliminate a pre-existing duplication rather than add
  one.
- Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred) — this
  pass is gated by the unit/property/acceptance suites only, per
  constitution.
- Confirmed the two pre-existing `check_bb_scripts_load.sh --all` failures
  (`check_swarm_detached.bb`, `clear_identical_untracked_and_merge.bb`) and
  the separately-flagged `post_qa_branch_sweep.bb` unbalanced-paren load
  failure are all untouched by this parcel's diff (`git diff <base>
  <head> --stat` names none of them) — pre-existing, unrelated, correctly
  left to their own tickets.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/post_qa_branch_sweep_lib_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/bl1421_one_standing_surfacing_property_runner.bb` | ALL PROPERTIES HOLD, 500/500 each of P1/P1b/P2/P3 |
| `node specs/pipeline/cli.js specs/features/BL-1421-one-standing-surfacing-per-role.feature` | 6/6 scenario runs |
| `node specs/pipeline/cli.js specs/features/BL-1361-...feature` (regression) | 6/6, unaffected |
| `node specs/pipeline/cli.js specs/features/BL-668-...feature` (regression) | 5/5, unaffected |
| `grep -n caught-up-to-told? swarmforge/scripts/handoffd.bb` | matches (required_wiring #1) |
| `bl1421OneStandingSurfacingSteps.js::registerSteps` exists | yes (required_wiring #2) |

No fix required. Forwarding unchanged to architect.
