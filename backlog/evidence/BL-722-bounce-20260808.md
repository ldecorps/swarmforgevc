# BL-722 documenter bounce — 20260808

## Complete review inventory

Documentation pass for BL-722 is done (new how-to
`docs/how-to/BL-722-pilot-safe-defects.md`, cross-linked from
`docs/how-to/BL-698-telegram-cursor-operator-commands.md` and
`docs/index.md`, committed at `2542ffb3dd`). Attempting to forward to QA
tripped the pre-QA wiring gate (`swarm_handoff.sh`'s PRE_QA_GATE), which
blocks the send outright — this is the sole defect this pass found.

- D1: **class** wiring, **blamed role** coder, **remediation pointer**
  `extension/src/tools/telegramCursorBridgeLive.ts` near the
  `'pilot-safe-list'` / `'pilot-safe-start'` handler entries (~line 855-863).
  The ticket's own `required_wiring` names
  `extension/src/tools/telegramCursorBridgeLive.ts::pilot safe list/start handlers`,
  but that literal string is absent from the file — verified with
  `grep -n "pilot safe list/start handlers" extension/src/tools/telegramCursorBridgeLive.ts`
  (no match) and `grep -n "BL-722" extension/src/tools/telegramCursorBridgeLive.ts`
  (no match either). The handlers themselves are wired and functionally
  correct (`'pilot-safe-list'` and `'pilot-safe-start'` keys exist and call
  `listSafePilotDefects`/`pickSafePilotDefect` correctly); only the required
  literal wiring marker comment the ticket declares is missing, so
  `swarm_handoff.sh`'s pre-QA gate refuses every forward to QA with:
  `PRE_QA_GATE_FAIL wiring BL-722 extension/src/tools/telegramCursorBridgeLive.ts does not contain "pilot safe list/start handlers"`.

No other checks blocked by this — cleaner/architect/hardener evidence for
this batch (`backlog/evidence/BL-722-BL-852-BL-847-BL-853-*-pass-20260808.md`)
all record no defects, so this gap slipped through every review before
reaching the send-time gate that exists specifically to catch it (BL-419's
origin case).

Outside documenter's domain (Article 1.7 — documenter does not change
production code beyond factual doc-comment fixes); routed to coder, the role
that implements a ticket's `required_wiring` markers.

## Fix needed

Add a short comment containing the literal text `pilot safe list/start
handlers` at or near
`extension/src/tools/telegramCursorBridgeLive.ts`'s `'pilot-safe-list'` /
`'pilot-safe-start'` handler entries, so the gate's substring check passes.
No behavior change required — the handlers already work correctly.
