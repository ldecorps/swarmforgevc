# BL-1429 documenter review pass — 2026-09-05

Reviewed the hardened commit (`966419e5fc`) and the coder/cleaner/architect/
hardener trail for BL-1429 (standing reds throttle intake).

Checklist:
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1429StandingRedThrottleSteps.js`; the
  `emit-throttle-recommendation.ts::standing-red` anchor required by the
  first wiring line is present (imports and folds
  `computeStandingRedRecommendation` from `standingRedSignal.ts`).
- Read `extension/src/metrics/standingRedSignal.ts` and
  `emit-throttle-recommendation.ts`: confirmed invariant 1 (the emitted
  recommendation is `Math.min` of the rework cap and the standing-red
  cap, null only when both are null — cap 1 only, never 0, per the human
  ruling), invariant 2 (the change log names the signal — `count`,
  `age`, or `unowned` — that caused or cleared a change; an unchanged
  recommendation logs nothing), and invariant 3 (thresholds read via
  `readConfigValue`, defaults 10/7, an unowned red throttles regardless
  of count or age).
- Confirmed `swarmforge/swarmforge.conf` gained `standing_red_max_count 10`
  and `standing_red_max_age_days 7` with a comment naming this ticket.
- Confirmed the coordinator prompt's effective-cap paragraph was already
  amended at mint (per the ticket's own note) — `swarmforge/roles/coordinator.prompt`
  line ~41 already names the standing-red fold; no documenter edit needed
  there (role prompts are the specifier's domain).
- Per the ticket's own explicit direction ("the documenter updates the
  Article 3.5 how-to and the briefing's burn section text that lists the
  throttle causes"):
  - Extended `docs/reference/Specification.MD`'s existing "Auto-tuned
    intake throttle (BL-432...)" entry — the doc's own home for this
    mechanism, since no separate how-to page exists (grepped
    `docs/how-to/` for `BL-432`/`emit-throttle-recommendation`/
    `effective_backlog_depth_cli`, no dedicated page) — with the
    standing-red fold, and added a new dated top-of-file entry in the
    same changelog convention as the BL-1287 entry from earlier this
    session.
  - Updated `extension/src/metrics/burnSectionText.ts`'s token-burn
    warning prose to name the standing-red register alongside the
    existing throttle levers (control pause, nightly cooldown,
    `active_backlog_max_depth`) — a factual text fix inside a warning
    string literal, not a logic change. Verified
    `extension/test/burnSectionText.test.js` still passes (4/4): its
    assertions are substring `assert.match` checks against the existing
    phrases, all of which remain present after the addition.
- No "Last Updated" freshness field elsewhere touched beyond the
  Specification.MD top-of-file bump made in the same commit as the
  content change.
- No registered diagram depicts the throttle-recommendation fold — no
  diagram change needed.
- Production code touched (`burnSectionText.ts`) is limited to a warning
  string literal per the ticket's own explicit assignment of that file to
  the documenter's pass; no logic, contract, or test-asserted shape
  changed.

Result: NONE — no defect found; documented and the burn-section text
fixed per the ticket's own explicit direction.
