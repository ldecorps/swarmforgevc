# BL-1398 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made beyond the merge-conflict
resolution below.

## Merge conflict resolution

`extension/test/bl632CommitTimeGuardInvariants.property.test.js` conflicted
against my own BL-1395 cleaner commit (`e1f2d5eb25`), which had hand-added
`check_handler_module_graph.sh` to the fixture's hand-written
`EXEC_FIXTURE_FILES` list to fix the exact same standing red this ticket
properly fixes at the root. BL-1398's `deriveCommitGuardFixtureSet()`
supersedes that hand-fix entirely (it derives the whole set, including
`check_handler_module_graph.sh` and `check_bb_scripts_load.sh`, from
`run_commit_guards.sh` and the two hooks at test time), so resolved by
taking the coder's derived-set version wholesale across all three conflict
hunks and discarding my hand-written list. Verified: no leftover conflict
markers, `test_bl1398_guard_fixture_derives_set.sh`'s own "including
check_handler_module_graph.sh, the guard whose absence caused the red"
case passes against the resolved fixture.
`swarmforge/scripts/test/suite-manifest.tsv` conflict was purely additive
(the new `test_bl1398_guard_fixture_derives_set.sh` row); restored the
file's trailing-double-tab convention.

## What was checked

- `swarmforge/scripts/test/test_bl1398_guard_fixture_derives_set.sh`: 8/8
  PASS, including:
  - a guard added to a seam copy of the runner appears in the derived set
    and the fixture's chain runs it
  - a guard removed from the runner is no longer copied (the set follows
    the runner both ways, per the ticket's qa_e2e_procedure)
  - a guard the runner names but the tree lacks REFUSES naming it
    (invariant 2 - never silently skipped)
  - every guard the REAL `run_commit_guards.sh` names is in the derived
    set, explicitly including `check_handler_module_graph.sh`
  - `bl632CommitTimeGuardInvariants` is green against the real runner
  - the fixture lists no guard by hand
- `extension/test/helpers/commitGuardFixtureSet.js` read end to end:
  parses `run_guard <script> [args]` lines from the runner and both hooks
  (comments skipped), walks one hop of `source`/`.` lines bounded to
  `swarmforge/scripts`, throws naming the guard when a `run_guard`-named
  script is absent from the tree (never silently narrows the chain), and
  reports (rather than throws) a missing sourced lib, since the guard that
  wanted it still runs and reports the absence itself.
- Property suites: `bl632CommitTimeGuardInvariants.property.test.js` (1/1)
  and `bl1398GuardFixtureDerivedSet.property.test.js` (2/2) — both green
  against the resolved merge.
- `required_wiring` anchor 1 (`run_guard` literal now present in the test
  file via the derivation) — confirmed: the string appears only inside
  `helpers/commitGuardFixtureSet.js`'s own parser regex and the test's own
  independent re-read of the runner (line 313), never as a hand-written
  guard name. Anchor 2 (discovery registration) — confirmed:
  `specs/pipeline/steps/bl1398GuardFixtureDerivesItsSetSteps.js` matches
  the `*Steps.js` directory-walk discovery suffix (BL-1371).
- `jscpd` over the four new/changed non-generated files: 0 clones.
- No prior bounce history for BL-1398 (mint → approve → promote → coder,
  no adjudicate-again entries). `main`/`origin/main` in sync (`0 0`).
- All four new/changed files here (a JS helper, a JS property test, a JS
  step handler, a shell test) are hand-authored, none compiled from
  TypeScript — `mutation-site-count.js` (which counts against
  `extension/out/**/*.js`) does not apply.

No architecture concerns: `deriveCommitGuardFixtureSet` is a single-purpose
pure function (reads files, returns a plan), cleanly separated from the
test file that consumes it and from the shell fixture that mirrors the
same derivation for the bb-suite side.

By cleaner.
