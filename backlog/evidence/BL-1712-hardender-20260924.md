# BL-1712 — hardender review pass, 2026-09-24

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Verified: `npx vitest run test/pricingTable.test.js test/modelDisplayName.test.js`
(40/40); the full unit suite with coverage (636 files, 10856 tests, all
green - the BL-1687 module-load standing red is already resolved on this
tree); CRAP scoped to the two changed `src/*.ts` files
(`node scripts/crapReport.js src/metrics/pricingTable.ts
src/swarm/modelDisplayName.ts`) - every function CRAP <= 5.00, 100%
coverage, no complexity regression possible since the diff is a pure
data-row addition (no function body touched); the ticket's own acceptance
feature (6/6); BL-1436's feature reads 5/6 on this commit exactly as the
coder's evidence documents (scenario 04's register row, owned by
BL-1712, retires only at land) - not a defect, the ticket's own
documented pre-land state.

Transient flake, not a regression: a re-run of the standing whole-tree
guards (since this batch also merged BL-1687/BL-1696/BL-1695 step-file
changes) showed `stepHandlerModuleLoadBudget.test.js` failing once on
`bl709BubbleItsOwnTelegramTopicSteps.js` at 521-523ms (over the 400ms
budget) while host load was 21-25 on 20 cores from a concurrent,
unrelated session's own `test:properties` run (confirmed via `ps
--forest`, a different process tree than mine). Re-ran 3x once load
settled to 9-15: clean every time, and the full 23-guard sweep (219
tests) is green. `bl709`'s own require is already lazy (BL-1687's fix,
a `bridgeServer()` getter, not a module-scope require) - the timing
variance came from the two other module-scope cursor-bridge requires it
carries, not from this parcel's diff.

`backlog/standing-reds.tsv` untouched by this parcel (confirmed via
`git diff main...HEAD -- backlog/standing-reds.tsv`, empty), matching
the ticket's own scope note (BL-1663: a parcel never edits the
register, only the land retires a row).

By hardender.
