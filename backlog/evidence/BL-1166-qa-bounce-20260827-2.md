# BL-1166 QA bounce — 2026-08-27 (pass 2)

## Inventory

| Gate | Result | Notes |
|------|--------|-------|
| compile | PASS | `npm run compile` in extension/ |
| unit (mapped) | PASS | `npx vitest run test/operatorDocsCore.test.js` — 7/7 |
| property (mapped) | PASS | `test/operatorDocsReadOnly.property.test.js` (via vitest run) |
| acceptance | FAIL | 2/7 scenarios red (see D1) |
| required_wiring | PASS | `bl1166OperatorDocsSteps` registered in `specs/pipeline/steps/index.js` |
| whole-suite unit | BLOCKED BY | env: CURSOR_API_KEY absent in QA worktree without sourcing master `.swarmforge/swarm.env` |

## D1 — acceptance: viewport legibility step uses unset `ctx.bl1166Html`

- **Failing command:** `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1166-bubble-authored-docs-index-and-first-pages.feature` (with `source /home/carillon/swarmforgevc/.swarmforge/swarm.env` for bridge)
- **Commit tested:** `f58bb7c16` (tip-pure rematch + BL-428 hitchhiker strip)
- **First error excerpt:**
  ```
  ✖ a how-to markdown page renders readable on a phone viewport
  Error: ... failed at step "And headings and paragraphs are legible at a phone viewport width":
  The "string" argument must be of type string. Received type undefined (undefined)
  ```
- **Failure class:** acceptance
- **Expected vs observed:** Step handler should assert phone viewport CSS on the Operator docs shell for every scenario that checks page legibility; observed `ctx.bl1166Html` is undefined in how-to/reference scenarios because only scenario 01 runs "the Operator docs remote page is opened from the Bubble pager".
- **Blamed role:** coder
- **Remediation:** `specs/pipeline/steps/bl1166OperatorDocsSteps.js` — in `headings and paragraphs are legible at a phone viewport width`, load shell HTML via `operatorDocsHtml.getOperatorDocsUiHtml()` (or set `ctx.bl1166Html` in Background) instead of relying on a prior scenario-only step.

## NONE beyond D1
