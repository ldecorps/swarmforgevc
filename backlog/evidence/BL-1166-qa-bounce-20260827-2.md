# BL-1166 — QA bounce — 20260827 (rematch)

## Failing command
`specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1166-bubble-authored-docs-index-and-first-pages.feature`

## Commit tested
`5432f988c` (documenter `72bf22b38a` merged)

## First error excerpt
```
✖ the Operator docs index shows the four Divio sections from docs index.md
Then it lists tutorials how-to reference and explanation sections:
404 !== 200
```
Same 404 on how-to and reference page scenarios (02–04).

## Failure class
behavior

## Expected vs observed
**Expected:** `/operator-docs-index` and `/operator-docs-page` return 200 with token when bridge serves Operator docs (required_wiring in ticket).

**Observed:** Routes return **404**. `operatorDocsCore.ts` / `operatorDocsHtml.ts` / `letsTalkRoutes.operatorDocs` exist, but **`bridgeServer.ts` has zero `operator-docs` route registration** — same class as hardender bounce `c9fa57404`.

## Remediation (coder)
Wire operator-docs HTTP handlers into `bridgeServer.ts` route table (index, page, shell) so acceptance scenarios 02–04 pass. Auth/unavailable scenarios (05–07) already green.

By QA.
