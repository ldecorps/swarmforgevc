# BL-1320 — hardener pass (2026-09-03)

Docs-only chore (built to human ruling: docs now, optimizer-emits-
recommendation minted as a separate child). No production `.bb` or `.ts`
source touched — CRAP not applicable, and there is no library logic here to
mutation-test with the BL-638 hand-authored fallback. The parcel's own
testing discipline (every window/command line in the how-to executed
against the real `parse_config` and the real `model_steward_cli.bb`, per a
BL-654 property invariant enumerating the page's own printed steps) is
already the mutation-equivalent gate for a docs ticket: the page itself is
the thing under test.

## Re-verification (independent, this pass)

- `node specs/pipeline/cli.js specs/features/BL-1320-…feature` — 4/4 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1320DocumentedStepsAreExecutedInvariants` — 5 consecutive clean runs
  (2/2 tests each), on top of the architect's own 5.
- required_wiring: `bl1320SeatOperatorStepSteps` confirmed registered in
  `specs/pipeline/steps/index.js`.
- Spot-checked the quoted parser refusal against the live source myself:
  `grep -n "must exist because parcels address the stage"
  swarmforge/scripts/swarmforge.sh` — matches the how-to's quoted text.

## Independent non-vacuity probe (own check, not relying on prior claims)

Mutated the doc's own tier-guidance window line
(`--seat-tier hard` → `--seat-teir hard`, a plausible typo an editor could
introduce) and re-ran the acceptance feature: **1 of 4 scenarios failed**
(the window line the page prints no longer parses as the flag it claims to
be). This independently confirms the page-to-parser coupling the ticket's
whole invariant exists to guarantee — a drifted page goes red, not silently
wrong. Restored; `git diff` on the doc was empty afterward, and 4/4 passed
again.

## Fixture hygiene

Cleaner's try/finally fix (scenarios 01/02's terminal steps) already
verified by the architect; re-confirmed present in the merged file.
Age-guarded module-load sweep (BL-971 class, same pattern as
BL-1306/1323/1332/1343 this session) present. No `bl1320-*` fixture
directories left in `/tmp` after this pass's runs; no orphaned test
processes.

## Conclusion

No gap found. Forwarding to documenter.

By hardender.
