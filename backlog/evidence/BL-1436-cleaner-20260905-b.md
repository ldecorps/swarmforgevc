# BL-1436 — cleaner pass (redo after bounce), 2026-09-05

NONE — no defect found. The re-fix correctly closes the architect's
bounce (the honest-null branch had no automated coverage) with real,
targeted test coverage.

## What was checked

- Read the new tests directly (`extension/test/pricingTable.test.js`):
  the first reproduces the architect's own mutation exactly (a nonzero
  cache-creation usage on `claude-fable-5-1`, the ONE row in
  `PRICING_TABLE` that can actually reach `costFrom`'s undefined-rate
  branch, must cost `null`, never `0`); the second is the positive
  complement (the model's other three categories still price normally).
  The first test also pins its own fixture assumption
  (`cacheCreatePerMTok === undefined`) with a comment naming what to do
  if that ever changes — a well-built, self-documenting regression guard.
- **Independently reproduced the architect's original finding**: mutated
  `costFrom`'s `return null` (unknown-rate branch) to `continue` (silently
  cost at 0) directly in this worktree, recompiled, and confirmed the new
  test fails immediately (expected `null`, got `0`) — exactly the gap the
  bounce named. Restored; recompiled; reconfirmed 32/32 green.
- Resolved the `backlog/standing-reds.tsv` merge conflict between my own
  bounce-side correction (owner re-pointed to the now-active BL-1436,
  first_seen 2026-09-04) and the coder's re-fix (row removed entirely):
  took the coder's side, since `extension/src/metrics/pricingTable.ts`
  genuinely carries the `claude-fable-5-1` entry again at this commit
  (confirmed by reading the merged tree directly) — the test is green, so
  no row should survive (standing-red rule), matching invariant 3.
- Re-ran the full suite after the merge: `npx vitest run
  test/pricingTable.test.js` 32/32 (was 30, +2 new); acceptance
  `BL-1436-...feature` 6/6; acceptance `BL-627-...feature` (regression)
  6/6, unaffected.
- `npm run compile` clean.
- `grep -c pricingTable backlog/standing-reds.tsv` is 0 at this commit.

## Verification (all re-run in this worktree, all green)

| check | result |
|---|---|
| `npx vitest run test/pricingTable.test.js` | 32/32 pass (was 30, +2 new) |
| Mutation reproduction of the architect's bounce (`return null` → `continue`), reproduced independently | new test fails immediately (expected null, got 0); restored, 32/32 green |
| acceptance `BL-1436-...feature` | 6/6 |
| acceptance `BL-627-...feature` (regression) | 6/6, unaffected |
| `grep -c pricingTable backlog/standing-reds.tsv` | 0 |
| `npm run compile` | clean |

No fix required. Forwarding the re-fixed candidate to architect.
