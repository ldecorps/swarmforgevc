# BL-1724 — cleaner review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Checks run (from `extension/`, after `npm run compile`):
- `npx vitest run test/bl1652HandoffdRespawnReadingsWiring.test.js`: 7/7
  passed, 964ms — well under the 7.0s per-file budget.
- `grep -c "execFileSync('bb'" test/bl1652HandoffdRespawnReadingsWiring.test.js`
  → 1, matching the ticket's declared invariant.
- Acceptance pointer (from repo root):
  `node specs/pipeline/cli.js specs/features/BL-1652-the-chase-sweep-never-respawns-a-busy-role-and-respawns-at-most-once-per-sweep.feature`
  → 4/4 ok.

Mutation-site count (BL-485, `node extension/out/tools/mutation-site-count.js
extension/test/bl1652HandoffdRespawnReadingsWiring.test.js`): 122 sites,
`over` the 100 threshold — soft advisory, weighed and left whole. The file's
seven cases share one `beforeAll` fixture setup and one combined `bb`
process run (`runAllCases`), which is the entire point of BL-1724: splitting
the file into smaller modules would either reintroduce multiple `bb`
process spawns per run (regressing the ticket it exists to fix) or require
a shared cross-file fixture module purely to duck the count — more
indirection, no clearer structure. This is the "legitimately-cohesive
large module a split would only harm" case; stays whole.

No cleanup, DRY, or CRAP action needed: test-only change, no production
code touched, structure already follows the established BL-1663/BL-1673
patterns (single bb process, form-in-file probe, mkSharedTmpDir for
beforeAll-scoped fixtures).

By cleaner.
