# BL-1340 — cleaner pass (2026-09-02)

One trivial fix; otherwise NONE. Forwarded to architect.

## Reviewed

Diff scope (5992cbca3d..9d298f070d, the coder's actual BL-1340 work):

- `swarmforge/scripts/promotion_gates_lib.bb::pins-draft-conversion?` — a
  hand-rolled line-walk over `required_wiring:`'s block, needed because
  `read-field` documentedly returns nil for a block. The cond is a touch
  dense but each branch is commented with the WHY and covered by 4 new
  RED-first test cases (admitted-when-pinned, refused-when-parked, an
  unrelated wiring entry is not a pin, pinned-but-absent still refuses). Left
  as-is: it is a single well-tested function, not duplicated anywhere else in
  the file.
- `swarmforge/scripts/acceptance_contract_gate_lib.bb` — new
  `declaration-draft` branch added ahead of `wait-bound-hit?` in the cond,
  matching the ticket's stated ordering rationale (checked before step
  resolution). Clean.
- `swarmforge/scripts/acceptance_pointer_gate_lib.bb` — comment-only
  re-tensing to point at the new refusal seat. No code change.
- `swarmforge/scripts/pre_qa_gate_gather_lib.bb` — one new fact
  (`declaration-draft`), gathered where the function already does its git/fs
  work; the pure gate stays pure. Clean.
- `swarmforge/scripts/promote_and_route_next.sh` — `is_buildable()` and the
  buildable[]/other[] pre-partition deleted entirely, replaced by one
  `candidates[]` array and one `select` call. Left a double blank line where
  the deleted function used to sit — fixed (one line removed, no functional
  change).
- `extension/test/bl1340SelfConvertingDraftInvariants.property.test.js` — two
  declared invariants plus a static structural check on the router script.
  Ran the property suite 5x in a row to check for the flakiness class the
  architect just flagged on this ticket's sibling (BL-1343's property test,
  reverted this same day) — 5/5 clean, consistent with this generator's much
  higher reach probability (each ticket independently draws 2-3 booleans
  rather than requiring every element of an array to land the same way).

Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred); gated by
its own unit suite only, which is green.

## Verification run (not assumed)

- `bb swarmforge/scripts/test/promotion_gates_lib_test_runner.bb` → ALL PASS.
- `bb swarmforge/scripts/test/acceptance_contract_gate_lib_test_runner.bb` → ALL PASS.
- `bb swarmforge/scripts/test/promotion_gates_cli_test_runner.bb` → ALL PASS.
- `bb swarmforge/scripts/test/pre_qa_gate_gather_lib_acceptance_contract_test_runner.bb` → ALL PASS.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-626-promotion-gate-rejects-unmaterialized-feature-draft.feature` → 10/10 pass (reran after the whitespace fix, still 10/10).
- `npx vitest run --config vitest.properties.config.mjs bl1340SelfConvertingDraftInvariants` → 3/3 pass, 5 consecutive runs, no flakes.
- `bash -n swarmforge/scripts/promote_and_route_next.sh` → syntax OK after the fix.

## Confirmed, not re-litigated

- The coder's flagged pre-existing red,
  `swarmforge/scripts/test/test_promote_and_route_next_priority.sh`, still
  exits 1 on this tree (reproduced independently) — same failure the coder
  reported checking against unmodified `HEAD` before and after their change.
  Not this ticket's, not repaired here.
- The mutation-manifest stamp on `specs/features/BL-626-...feature` is left
  stale by the coder deliberately (out of coder-role scope, flagged for the
  hardener) — confirmed this is correctly not a cleaner-role fix either;
  hand-editing a mutation manifest is forbidden by the Guardrails article.
