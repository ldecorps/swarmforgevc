# BL-1764 — coder pass, 2026-09-26

## What changed

`extension/test/nightClosingCeremonyRun.test.js`: added a single
`FIXED_NOW_MS` constant (`new Date(2026, 8, 25, 12, 0, 0).getTime()` -
local noon, never the wall clock) and replaced every one of the file's 19
`Date.now()` reads with it. Every test, assertion and
`runNightClosingCeremony(` call is byte-identical otherwise - only the
source of each start instant changed, per the ticket's own FIRM
constraint.

New `specs/pipeline/steps/bl1764NightCeremonyTestsWallClockSteps.js`: a
source-text census only (never a reimplementation of the runner or a real
ceremony run) - counts literal occurrences of `Date.now()` / `new Date()`
(scenario 01) and of `test(` declarations / `runNightClosingCeremony(`
calls (scenario 02).

## Checks run

- `node specs/pipeline/cli.js specs/features/BL-1764-night-ceremony-run-unit-tests-read-no-wall-clock.feature`:
  3/3.
- `cd extension && npx vitest run test/nightClosingCeremonyRun.test.js`:
  20/20.
- Per the ticket's own qa_e2e step 2:
  `TZ23=$(node -e '...')`, then `TZ=$TZ23` and `TZ=UTC` runs of the same
  file: 20/20 under both (run near local midnight this pass, at the hour
  the original defect actually reproduced).
- `cd extension && npm test`: 638 files / 10875 tests pass.
- `git status --short`: only the test file touched (+ the new step
  handler + this evidence).
- `backlog/standing-reds.tsv` not edited (BL-1663 constraint) - the row
  already names BL-1764 as owner (added at mint) and retires at land per
  BL-1631.

By coder.
