# BL-847-resource-sampler-measures-pane-shell-not-agent-process — QA final pass (2026-08-08)

## Commit checked

QA worktree merge of documenter's `14d9122f79` (merge commit `dfe8bca1a3` ->
`14d9122f79` -> `53ce8bfa62`, all merged into QA in one step). Ancestry
confirmed: coder, cleaner `3ee40328`, architect `e68631f0`, hardener
`f9503a50` (hardener pass evidence names BL-722/BL-852/BL-847/BL-853
together) are all ancestors. Verified independently per Article 2.6 despite
the shared batch lineage.

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`, node v22.23.2): clean.
- Full unit suite (`npm test`, node v22.23.2): 401/406 files, 7228/7237
  tests green — see `backlog/evidence/BL-852-qa-pass-20260808.md` for the
  full analysis of the 5 remaining failures (host-load timeouts in files
  unrelated to this ticket: briefing/diagram rendering, bridge headless
  startup — none touch `resourceSamplerActivation.ts` or
  `pilotSafeDefects.ts`). This ticket's own files
  (`resourceSamplerActivation.test.js`, `sampleResourcesCli.test.js`) were
  green in the full run.
- Property tests (`npx vitest run --config vitest.properties.config.mjs`,
  node v22.23.2): `resourceSamplerActivation.property.test.js` green.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-847-resource-sampler-measures-pane-shell-not-agent-process.feature`):
  4/4 scenarios PASS — agent-not-shell measured, unidentifiable-agent records
  no sample, root shell never used as fallback, pane-respawn detection by
  pane pid unaffected.
- Invariants: both declared invariants have direct coverage — invariant 1 by
  scenario 1 (recorded sample describes the agent process, not the shell),
  invariant 2 by scenario 2 (no sample at all when the agent can't be
  identified, `resourceSamplesObserved` keeps its narrower meaning) plus the
  property test's random-tree coverage.
- No `required_wiring` field on this ticket (none declared); confirmed the
  fix is exercised through the real call path
  (`extension/src/swarm/resourceSamplerActivation.ts`,
  `extension/src/tools/pilotSafeDefects.ts` per the diff) via the acceptance
  steps in `bl847ResourceSamplerMeasuresAgentSteps.js`, not a standalone unit
  never invoked by production code.
- No orphaned test/mutation processes before or after this pass.

## Verdict

Clean pass. No defects. Approved as part of the BL-852/BL-847/BL-853 batch —
see `backlog/evidence/BL-853-qa-pass-20260808.md` for the shared merge-up/
landing details.

By QA.
