# BL-1040 QA pass — 2026-09-02

Merged documenter `3781fb6e3d` into QA worktree. Real production change:
seat-identity fold on the observation path across
`swarmforge/scripts/pipeline_stage_cli.bb`,
`extension/src/swarm/swarmState.ts`, `extension/src/concierge/pipelineBoard.ts`.

## Process hygiene

`pgrep -fl 'node --test|stryker'` showed a live Stryker run — traced via
`ps`/`/proc/<pid>/cwd` to `.worktrees/hardender/extension`, the hardener's
own legitimate mutation run in its own worktree, not an orphan in QA's
domain. Left untouched. No stray process in my own worktree/session before
or after this pass.

## Independent re-run

- `npm run compile` — clean.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1040SeatIdentityObservationPath.property.test.js` — 4/4 pass.
- `npx vitest run test/pipelineBoard.test.js test/state.test.js` —
  172/172 pass (32 + 140).
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1040-seat-identity-never-escapes-on-the-observation-path.feature`
  — 6/6 scenarios pass, matching qa_e2e_procedure items 1-6 (including
  item 5, the stale-file/older-producer read).
- `bash swarmforge/scripts/test/test_pipeline_stage_cli.sh` — ALL CHECKS
  PASSED, including all 5 BL-1040 shell cases (bare seat, second seat
  folds to stage not seat id, no `@` anywhere in the emitted map, an idle
  second seat does not widen the map, and the `:sent`-header seat fold the
  hardener's own hand-mutation sweep found and closed).

## Full unit suite (`npm test`, extension/)

Same 15 pre-existing standing-red test files as this session's prior QA
passes (BL-1221/1229/1263/1265/1289/1290/1291), zero overlap with this
parcel's changed paths. 9831 passed of 9856 (up from 9823/9848 before this
parcel — the 8 new BL-1040 tests, consistent).

## required_wiring / invariants — re-verified live

- Source normalization: `pipeline_stage_cli.bb:175` and `:210` both call
  `handoff-lib/seat-stage` — the shared chokepoint, not a fourth hand-rolled
  `@` parse. `role-order` (line 210) is `distinct`-folded, so a multi-seat
  stage occupies exactly one position in precedence order (invariant 2).
- Defensive read-side fold present too (invariant 1's "stale file" half):
  `swarmState.ts:220`, `stageOfSeat` folds any `@`-bearing key even from an
  older, unfixed producer's on-disk map — confirmed live by
  qa_e2e_procedure scenario 5 passing.
- `heldRoleByTicketId` (`pipelineBoard.ts:490`) delegates to the extracted
  `idsByStageFromRoleHeldTickets` (line 480) — the hardener's CRAP-fix
  extraction is real and wired, not dead code (both called from the render
  path at line 536).
- `bl1040SeatIdentityObservationPathSteps` registered at
  `specs/pipeline/steps/index.js:906`.

## Constraints / scope honoured

Diff confined to the observation path (three source files named in the
ticket) plus tests/docs — no touch to supervisor/chase/ensure/stage-dwell
(explicitly out of scope, left for BL-981's remaining slice) or to
BL-732's console pane-title regex (deliberately separate surface).

## Verdict

PASS. Landing on `main`.
