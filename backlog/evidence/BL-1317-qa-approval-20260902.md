# BL-1317 — QA approval (20260902, post-bounce round 2)

Received: documenter commit `f19645a486` ("BL-1317: documenter refix
evidence, Article 4.4 - D1 verified closed"), merged into QA at `55d3853a1a`.

Ancestry confirmed: coder (`00cc6872eb`, `e1f58e1dbb`), cleaner
(`420a30bd31`), architect (`84606ec162`), hardener (`093322324e`),
documenter (`f19645a486`) are all ancestors of `55d3853a1a`
(`git log --oneline --grep="BL-1317"` on this branch).

## D1 (prior bounce) — verified closed

D1 (`backlog/evidence/BL-1317-qa-bounce-20260902.md`): `required_wiring`
item 1 named an unaddable TypeScript-side caller for the Adapt decision.

Verified independently, not merely re-read:
- `extension/src/tools/effortDialAdapt.ts` and its test are deleted (`ls` —
  gone).
- `grep -rl "adaptRoleEffort\|effortDialAdapt" --include="*.ts"
  --include="*.js" --include="*.bb" --include="*.sh" .` (excluding
  node_modules) now returns only
  `extension/test/bl1317AdaptSingleApplierPerLanguage.test.js` (a guard
  test) and `swarmforge/scripts/seat_difficulty_lib.bb` (a comment
  cross-reference, not a call) — the dead module and its dangling test are
  gone.
- `required_wiring` on the ticket YAML now names only
  `seat_difficulty_lib.bb::adapt-effort-decision`,
  `handoff_lib.bb::record-effort-adapt!`, and
  `done_with_current_task.bb::record-effort-adapt!`.
- `grep -n "record-effort-adapt!" swarmforge/scripts/done_with_current_task.bb`
  confirms the pre-existing live caller (line 80) actually invokes it — the
  BL-1235 consumer anchor is real, not merely declared.

D1 is closed. No new item found this round.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile` in `extension/`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red, identical file set
  to the prior bounce's own inventory (`BL-1317-qa-bounce-20260902.md`) —
  BL-1291, BL-1263, BL-1221/BL-1229 (landPilotedTicket family),
  BL-1265, BL-1290/BL-1226/BL-1312, and the long-standing
  `constitutionDocCitations` failure. None names or touches
  `effortDialAdapt`, `seat_difficulty_lib`, `handoff_lib`, or
  `done_with_current_task`. Pre-existing, not this parcel's defect.
- Property suite (`npm run test:properties`): 24 files / 13 tests red — same
  standing-debt population (pilot/land-gate family, hotfix-ledger stamp-off
  family) as the prior pass. Grepped the full failure log for
  `effortDialAdapt|seat_difficulty_lib|handoff_lib|done_with_current_task|1317`
  — no hits. Plus the allowlisted BL-871 vitest-worker timeout. Pre-existing
  per BL-1063, not this parcel's defect.
- Effort-ladder cross-language parity
  (`swarmforge/scripts/test/test_bl1317_effort_ladder_parity.sh`): PASS,
  5/5 checks.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1317-adapt-tier-effort-from-outcome-signals.feature`):
  PASS, 3/3 scenarios, against the real
  swarm_handoff/ready_for_next_task/done_with_current_task pipeline.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1317AdaptEffortSteps` — registered.
- Wiring: all three `required_wiring` entries confirmed with grep, the
  consumer anchor is a real pre-existing caller (not the parcel's own diff
  gating itself, BL-1235).
- Documentation currency: already verified correct in the documenter's
  round-2 pass (`BL-1317-documenter-refix-20260902.md`) — how-to and
  Specification.MD both describe the bb-only shape accurately, no dangling
  `effortDialAdapt` reference in docs. No diagram change-trigger fired.

## Verdict

APPROVE. No open item. Landing `55d3853a1a` (this QA merge commit) on
`main`.

By QA.
