# BL-1518 — QA review pass, 2026-09-11

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- **Re-check pass for the 2026-09-11 bounce** (`backlog/evidence/BL-1518-a-QA-20260911.md`):
  D1 (hardener: re-run BL-1441/BL-620 mutation config with a live census)
  and D2 (documenter: handoff-flow.mmd diagram currency for the new
  draft-root guard) both addressed and independently verified here — not
  merely trusted from the received evidence.
- **Lineage**: `git merge-base --is-ancestor d8704979b4 2e0ea210f2` (the
  bounced commit is an ancestor of the documenter's fix commit) confirmed
  OK before merging.
- **Merge**: `Merge documenter 2e0ea210f2 into QA.` → `6c91123e94`.
- **D1 verified independently**: `grep -n draft-root-guard
  swarmforge/scripts/swarm_handoff.bb` shows the call wired at line 1148;
  `handoff_draft_root_guard_lib.bb` present and loaded. Hardener's census
  evidence (`backlog/evidence/BL-1518-a-hardender-20260911-2.md`) shows a
  full BL-1441/BL-620 mutation re-run with the specifier inbox/new and
  coder sent-dir counts unchanged before/after (0 and 1894 respectively).
- **D2 verified independently**: `grep -n "ROOTGUARD\|OUTSIDEROOT"
  docs/diagrams/handoff-flow.mmd` shows the new gate/bad nodes wired
  between DRAFT and VALIDATE, matching `swarm_handoff.bb`'s actual guard
  order.
- **Compile**: `npm run compile` clean.
- **Unit suite**: `node scripts/recordTestDuration.js` — 615 test files /
  10440 tests, all passed, including
  `BL-1518: swarm_handoff.bb refuses a draft that lies outside the
  project root a wrong cwd/env resolves, and delivers normally once
  cwd/env point at the draft's own root` (ticket item 2's exact escape
  shape). Suite-budget-overage warnings printed are a pre-existing perf
  metric, unrelated to this ticket, not a test failure.
- **Property suite**: `npx vitest run --config vitest.properties.config.mjs`
  — 389/391 test files, 1153/1156 tests passed. The 2 failing files
  (`bl1313BatchGuardVisibilityInvariants.property.test.js`,
  `bl1367ApprovalCarriesItsRuling.property.test.js`) are both pre-existing
  standing reds with open owning tickets (`backlog/standing-reds.tsv`:
  BL-1503, BL-1527) — confirmed by grep before reporting (BL-1063), not
  caused by this parcel. The two `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled errors are the BL-871 allowlisted benign case.
- **Acceptance**: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1518-a-handoff-cli-never-writes-outside-the-root-its-draft-lives-in.feature`
  — all 3 scenarios pass (draft under root sends normally; draft outside
  root refused; sibling-dir text-prefix collision still refused).
  `required_wiring` handler (`bl1518HandoffDraftRootGuardSteps.js::registerSteps`)
  confirmed present in the same parcel.
- **Item 3 verified**: `use staging please` no longer appears in
  `telegramFrontDeskBotCli.test.js` (grep clean); it remains only in
  `agentNotesCore.test.js`, which the ticket names out of scope (pure
  functions, never reaches a real CLI).
- **Wiring**: `enqueueRoleAnswerNote` (telegram-front-desk-bot.ts:1812)
  still shells the real CLI with explicit `cwd`/`env`, but the CLI-level
  `draft-root-guard!` (item 1) now makes a mutant that empties/drops that
  options object fail closed — refused before any mailbox write — rather
  than silently delivering to the wrong live mailbox, which is the actual
  escape this ticket exists to close.
- **No orphaned test/mutation processes** before or after (`pgrep -fl`
  clean both times; matched PIDs were transient pgrep subshells, verified
  via `/proc/<pid>/cmdline` already gone).
- **qa-sibling-check**: `status --ticket BL-1518` → `VERIFY BL-1518`, no
  open deferral.
