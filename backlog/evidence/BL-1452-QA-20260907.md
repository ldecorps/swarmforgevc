# BL-1452 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect. One new, distinct,
out-of-scope finding recorded separately (see below).

By QA.

## Verification (qa_e2e_procedure)

- `npm run compile`: clean.
- `test/backlogTicketId.test.js` (10), `test/qaSiblingCheckCli.test.js`
  (49), `test/recordBounceCli.test.js` (60) — 119/119 pass.
- `specs/features/BL-1452-...feature`: 7/7 pass.
- Sibling features: BL-635 15/15, BL-608 6/6, BL-990 8/8 — all green.
- Full compiled unit suite: **611/611 files, 10322/10322 tests, fully
  green** (no `npm test` exit-code noise beyond the pre-existing BL-378
  wall-clock budget quirk; the BL-1460 `bridgeServer.test.js` red from
  earlier today is gone — fixed independently, unrelated to this ticket).
- **Live proof (qa_e2e step 2), in a scratch clone under `/tmp` (mkdtemp),
  never the live checkout**: `node extension/out/tools/record-bounce.js
  --ticket GH-24 --role documenter --type feature --class behavior
  --commit 718bd15071 --by QA --evidence backlog/evidence/GH-24-QA-20260906.md`
  → `"recorded": true` (JSONL store written; the ticket-YAML merge itself
  reported `not-found` because GH-24's ticket lives under `backlog/done/`,
  best-effort and non-blocking per this tool's own documented posture).
  `bash swarmforge/scripts/is_qa_ancestor.sh 718bd15071` in that same
  clone → exit 1, `bounced: 718bd15071 appears in a ticket's
  bounce_history ... (BL-952)` (the veto held already via the 2026-09-06
  interim hand-written `bounce_history` entry on GH-24's YAML; this
  ticket's fix additionally fills in the previously-missing JSONL half).
  Confirmed the live QA worktree was never touched (`git status`
  clean, no `.swarmforge/bounces/` JSONL file exists there at all).
- qa_e2e step 3: `node extension/out/tools/qa-sibling-check.js status
  --ticket GH-24` in the real QA worktree → `VERIFY GH-24`, exit 0.
- qa_e2e step 4: `grep -rn 'BL-\d+\$' extension/src/tools/bounceArgsCore.ts
  extension/src/tools/qa-sibling-check.ts` → empty.
- The coder's own unowned-red note (`bl1375ApprovedSiblingsCanLandInvariants`,
  refused by the property-suite-guard on every `extension/src` commit until
  registered) is confirmed already owned: `BL-1465`, registered in
  `backlog/standing-reds.tsv`, minted and landed by the specifier earlier
  today.
- The hardener's deferred mutation gate (`backlog/hardening-debt-ledger.yaml`,
  Stryker dry-run timeout under host load 9.65-10.81/20 cores) is a
  legitimate, ledger-tracked deferral, not a shortcut — confirmed the row
  exists with the required fields (parcel, gate, file_set, reason, load,
  detected_at). Not something QA runs or forces; the owed-gates mechanism
  (BL-1439/1441-shaped) catches this up later.
- No orphaned `node --test`/vitest processes remained (checked by PID/cwd,
  not bare `pgrep` self-match).

## New finding, NOT part of this ticket's scope — flagged separately

Merging this parcel required investigating why BL-1450's already-bounced,
already-reverted content (`backlog/evidence/BL-1450-bounce-20260907.md`)
reappeared in this worktree's tree despite my own earlier revert
(BL-490/BL-495). Root cause: BL-1438's post-land repoint (`git reset
--hard origin/main`, run after landing BL-1447) discarded my revert and
every other QA-local-only commit that was never pushed to `origin/main` —
a bounce is never pushed, so it is exactly the kind of work this wipes.
Confirmed BL-1452's own commits never touch `bl968`'s file or any BL-1450
path, so this parcel is not itself affected; full detail and
recommendation:
`backlog/evidence/BL-1452-QA-followup-repoint-discards-unlanded-bounce-work-20260907.md`,
sent to the specifier as a `note`.

## Disposition

Approved. No rework needed. Landing next — will independently re-verify
BL-1450's stale content is correctly excluded from (or, if riding as a
passenger, matches a safe/unchanged state relative to `origin/main` in)
the tip-pure replay before pushing, given the finding above.

By QA.
