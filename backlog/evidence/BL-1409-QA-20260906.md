# BL-1409 — QA review pass, 2026-09-06

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1409-bl570-wiring-assertion-follows-the-delegation.feature
- **Commit hash**: f51abbff77
- **First error excerpt**: FAIL: 11: all-allowlisted reds must allow, got 1: property-suite-guard: run test/bl632CommitTimeGuardInvariants.property.test.js still fails when run alone; Commit rejected: property suite failed with non-allowlisted files: test/bl632CommitTimeGuardInvariants.property.test.js
- **Failure class**: spec-gap
- **Expected vs observed**: Scenario 04 ("every case passes") is unconditional, but case 11 is a separate, already-ticketed red (BL-1448, depends_on: [BL-1409], hidden behind case 07 since 08-30 per this ticket's own notes) that cannot be already-fixed when this ticket lands - the assertion is unsatisfiable within this ticket's own scope, independent of any fix quality
- **Blamed role**: specifier
- **Remediation pointer**: amend specs/features/BL-1409-bl570-wiring-assertion-follows-the-delegation.feature scenario 04 (and qa_e2e_procedure item 2) to scope the assertion to cases 01-10, or to accept case 11's specific FAIL as expected until BL-1448 lands

By QA.

## Detail

This ticket's own actual deliverable is verified CORRECT and COMPLETE; the
single finding above is a spec-gap in this ticket's own acceptance
feature, not an implementation defect, and is why this pass is not a
plain approval.

- `qa-sibling-check.js status --ticket BL-1409`: `VERIFY BL-1409` (no open
  deferral).
- Orphan process check before/after: empty both times (other worktrees'
  own legitimate concurrent runs excluded, as usual).
- **The ticket's own headline claim verified directly**:
  `run_acceptance.sh specs/features/BL-570-property-suite-drift-guard.feature`
  — 7/7 (was 0/7 on main since 2026-08-30). `bash
  swarmforge/scripts/test/test_property_suite_drift_guard.sh` — case 07
  PASS (was the six-day standing red this ticket owns); cases 01-10 all
  PASS; case 11 FAIL (see D1).
- BL-1409's own acceptance feature: 4/5 scenario runs pass (scenario 03's
  two outline examples, scenario 01/02 implied by the earlier subtests);
  only scenario 04 ("every case passes") fails, and only because of D1.
- Wiring anchors, all three confirmed:
  1. `bl570PropertySuiteDriftGuardSteps.js` — `grep -c run_guard` → 0,
     `grep -c propertyGuardIsWired` → 2 (imports and calls it).
  2. `test_property_suite_drift_guard.sh` — `grep -c run_guard` → 0,
     `grep -c propertyGuardIsWired` → 4 (qa_e2e item 4, invariant 2 —
     matches exactly).
  3. `bl1409Bl570WiringFollowsTheDelegationSteps.js` exports
     `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Invariants confirmed by direct read of
  `specs/pipeline/steps/lib/bl1409PropertyGuardWiring.js`: two-hop check
  (hook names the runner in a non-comment line; the runner's OWN derived
  guard set, via BL-1398's `deriveCommitGuardFixtureSet` with
  `hookRels: []`, names the guard) — never a one-file literal grep
  (invariant 1); the runner hop reuses BL-1398's helper, never a second
  `run_guard`-line parser (invariant 2); every broken-hop branch returns
  `{wired:false, missing, reason}` naming the missing hop specifically —
  confirmed structurally and by the acceptance feature's own scenario 03
  (outline, 2/2 runs: hook-doesn't-exec and guard-not-in-runner-set, both
  correctly named) (invariant 3).
- Seam checks (qa_e2e item 3) confirmed via scenario 03's own two outline
  examples (already counted above) — a real fixture chain with each hop
  broken in turn, each correctly named.
- Fixture hygiene (qa_e2e item 5): hardener's evidence and the coder's own
  evidence both confirm `mkTmpDir` usage (BL-1280's raw-mkdtemp guard
  caught and the coder fixed a first-draft violation); no prefix sweep
  introduced.
- Full unit lane (`npm test`, extension/): 610/610 files, 10297/10297
  tests green. Same chronic suite-duration/per-file budget overage as
  other recent parcels, unrelated.
- BL-1409's own property invariants file
  (`bl1409PropertyGuardWiringInvariants.property.test.js`) run standalone:
  3/3.
- Full property lane (`npm run test:properties`): 363/366 files green on
  the full run; see "Not bounced" below for the three that failed under
  host load — confirmed 366/366 clean via isolated re-runs.
- Standing-red register: confirmed BOTH rows present before this pass
  (`BL-1409` for case 07, `BL-1448` for case 11) exactly as the ticket's
  own notes describe. Per the register's own rule ("a row is removed in
  the same land that turns the test green") the BL-1409 row would be
  removed on an actual land — NOT done here, since this pass does not
  land (D1 holds it). Left untouched, matching current `main`.
- Hardener evidence reviewed: a real mislabeling bug found and fixed
  (`missing` field named the wrong hop for a missing-hook-file case) plus
  a real coverage gap (the file-literally-absent branch had zero exercise
  anywhere) — both with hand-mutation confirmation and clean reverts.
- Docs and other stages: architect/cleaner/documenter all recorded clean
  NONE passes; documenter's evidence confirms no doc changes were needed
  (this is an internal test-wiring fix with no documented behavior
  change).

## Not bounced (observed, not this ticket's defect, not reproducible — host load)

Full property run failed 3 files under elevated host load (uptime ~9-14
during the run): `bl1343ReplayNeverDropsOwnPathInvariants` (fixed 20s
timeout), `bl1352EscalationVisibilityInvariants` (a distribution/timing
flake, not a timeout — the exact file BL-1440's original "Not bounced"
section already documented as host-load-sensitive), and
`bl968MaterializedGuardSensitivity` (its own 300s timeout, after printing
valid coverage stats). None touch BL-1409's domain (property-guard
wiring vs. land-step replay, escalation-visibility timing, and a
load-time-binding guard). No ticket found for any (`grep -rl <name>
backlog/paused/ backlog/active/` empty; none in
`backlog/standing-reds.tsv`). Re-ran all three in isolation once load
eased: all three passed cleanly, `bl968` in its normal ~164-193s range
with only the BL-871 allowlisted benign error. Same disposition as this
shift's prior "Not bounced" sections (BL-1442, BL-1424, BL-1412).

## Disposition

D1 is the ONLY finding, and it is a spec-gap, not an implementation
defect (Article 4.4's routing table: "Acceptance scenario not testable,
ambiguous, or with no step handler" → specifier, as a `note`, not a
parcel forward). Per the same rule, when every item in the inventory is a
spec gap there is no parcel to bounce: sending a `note` to the specifier
(priority 00) and completing the inbound task without forwarding, rather
than recording this via `record-bounce.js` (reserved for a defect the
code caused) or approving/landing a parcel whose own acceptance criteria
cannot be satisfied as written.
