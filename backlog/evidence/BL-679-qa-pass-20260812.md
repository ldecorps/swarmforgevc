# BL-679 — QA pass, 2026-08-12

Commit verified: 154060aa75 (documenter). Confirmed ancestors: coder 6e82d1082f,
cleaner 9b9cbe0351, architect (via cleaner merge c775b596f), hardener
fa94342e4f — all `git merge-base --is-ancestor <commit> 154060aa75` hold.
Merged (no-ff) into QA at 545a9ca64.

## Diff scope confirmed

`git diff --stat 4bc4e2c47 154060aa75` (specifier pre-flight -> documenter):
15 files — `ambulance_lib.bb`, `chase_sweep_lib.bb`, `flow_watchdog_lib.bb`,
`handoffd.bb`, `coordinator.prompt`, the promoted `.feature` file + its new
step-handler file + `steps/index.js` registration, `Specification.MD`, the
BL-655 how-to doc, the ticket's own YAML, and four new `swarmforge/scripts/test/`
runners. No `extension/src` TypeScript touched.

## In-scope checks (all green, run directly by this role)

1. **required_wiring** — both entries' literal pattern (`"ambulance"`, the
   middle `::`-delimited field per `pre_qa_gate_lib.bb`'s parser) confirmed
   present in `flow_watchdog_lib.bb` and `handoffd.bb`.
2. **Production code read directly against the ticket's 2 invariants and 2
   required_wiring entries** — all satisfied: `decide-tier`'s five-key input
   map is untouched; `evaluate-parcel-tier` (the caller) ORs
   `parcel-ambulance-held?` in as `:snoozed?`; the ambulance ticket's own
   parcels are never held (BL-655's positive-attribution rule); the daemon's
   open-slot nudge (`decide-open-slot-nudge?`) gains an `ambulance-active?`
   gate; `coordinator.prompt` gained the matching manual-promotion rule with
   the Article-3.2.4-outranking carve-out; `ambulance-auto-exit-sweep!` runs
   unconditionally on the daemon's existing cadence and only ever calls
   `release!`, never `engage!`.
3. **Wiring confirmed live, not just unit-tested in isolation** —
   `run-sweep!` in `flow_watchdog_lib.bb` (the real per-parcel sweep) calls
   `parcel-ambulance-held?` and threads it into `evaluate-parcel-tier` at the
   real call site (`flow_watchdog_lib.bb:806-807`); `handoffd.bb` calls
   `ambulance-auto-exit-sweep!` unconditionally in both the live daemon loop
   and the new `--sweep-once` test-harness path.
4. **Unit suite** — `npm test`: two full runs, 7440/7440 then 7440/7440
   tests, with a shifting handful of unrelated failures each run
   (`renderBriefingDiagramsCli`, `dependencyGateCliReportsAndScope`,
   `dependencyGateCliStorageGlobals`, `startBridgeHeadlessCli`) — none touch
   this ticket's diff. Host was under extreme contention throughout (load
   average peaked at 183 on a 4-core machine; `uptime`/`ps aux` confirmed).
   Every flagged file independently re-run in isolation and passed cleanly
   (e.g. `renderBriefingDiagramsCli.test.js`: 4/4 green, 25.7s). Compile
   (`npm run compile`) clean.
5. **Property suite** (`npm run test:properties`) — 236/237 then, on a
   third full run, 236/237 again, both times the SAME single flake
   (`bl796NvmNodePathFollowUpAdoptInvariants.property.test.js` invariant 1,
   a real-subprocess daemon-PATH-resolution test, unrelated to this diff),
   re-run in isolation and passed cleanly (3/3, 36.3s). No orphaned
   `node --test`/`stryker`/vitest processes before or after (`pgrep`
   confirmed clean both times).
6. **BL-679-specific bb runners, independently re-run, all green:**
   - `ambulance_lib_test_runner.bb` — ALL PASS
   - `flow_watchdog_test_runner.bb` — ALL PASS
   - `dispatch_gap_test_runner.bb` — ALL PASS
   - `bl679_ambulance_perimeter_property_runner.bb` — 300 runs/property,
     ALL PROPERTIES HOLD (both declared invariants non-vacuous)
   - `ambulance_lib_property_runner.bb` — 500 runs/property, ALL HOLD
   - `ambulance_wiring_property_runner.bb` — 300 runs/property, ALL HOLD
   - `test_chase_sweep.sh` — ALL PASS (19 cases)
   - `test_handoffd_chase_sweep_wiring.sh` — ALL PASS (5 cases)
7. **Gherkin lint gate** — `gherkin_lint_gate.sh` on
   `specs/features/BL-679-ambulance-mode-perimeter.feature`: clean parse.
8. **Formal acceptance gate** —
   `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-679-ambulance-mode-perimeter.feature`
   — 11/11 scenarios pass (8 singular + 3-example Scenario Outline),
   driving the real `handoffd.bb --sweep-once` subprocess and real
   `ambulance_cli.bb`, never a reimplementation.
9. **Docs currency** — `docs/how-to/BL-655-ambulance-mode-the-hold.md` gained
   a full "The perimeter (BL-679)" section (all 3 pieces, matches code) and
   updated verification commands; `Specification.MD`'s changelog entry
   accurately describes all 3 pieces.

## Verdict

PASS. All 3 pieces (watchdog quiet, promotion freeze, auto-exit) match the
ticket's spec exactly; both invariants hold; required_wiring satisfied;
wiring confirmed live at the real call sites; acceptance 11/11; unit +
property suites green modulo confirmed host-load flakes in files this
ticket never touched, each independently re-verified clean in isolation.

By QA.
