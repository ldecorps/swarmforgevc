# BL-1513 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Shell/bb lanes — no mutation/CRAP/DRY tool wired (BL-472); hand-authored
mutation sweep per the BL-567 fallback (the ticket's own `mutation_cost`
note names this explicitly).

- `bb swarmforge/scripts/test/bl1513_router_note_fits_property_runner.bb`
  → `ALL PROPERTIES HOLD` (16 real-route length-sweep trials + 10 parser
  checks x2 shapes).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1513-*.feature`
  → 4/4 pass.
- Named qa_e2e shell tests: `test_route_backlog_role_label_bash32.sh`,
  `test_bl1097_router_refuses_dispatched_ticket.sh`,
  `test_done_with_current_work_note_evidence.sh` → all `ALL PASS`.
- Parser-compatibility bb runners re-verified green:
  `bl1097_router_dispatch_trail_test_runner.bb`,
  `bl1422_work_note_not_completed_without_work_property_runner.bb`
  (500 runs x2), `bl1097_router_no_op_origination_property_runner.bb`
  (200 runs), `dispatch_gap_test_runner.bb`.
- Sibling acceptance regressions: BL-1415 6/6, BL-1422 6/6, BL-1185 4/4 —
  all green.
- Hand-mutation (BL-567 fallback):
  - Changed the refusal threshold `> 80` to `> 81` — **provably
    equivalent, not a gap**: the composed message length is
    `34 + 2*len(id)`, always even, so a length of exactly 81 can never
    occur for any ticket id; the mutant is unobservable by construction.
    Cross-checked against `swarm_handoff.bb:615`'s own real gate
    (`(> (count note-message) 80)`, the actual sender-side validator) —
    the router's `> 80` correctly mirrors it. Recorded here per the
    equivalent-mutant discipline rather than silently discarded.
  - Restored the actual pre-fix regression (silent `MSG="${MSG:0:80}"`
    truncate-and-continue, dropping the `exit 1` refusal): caught
    decisively, 14/26 property checks failed immediately, including a
    real fixture send that queued a truncated note instead of refusing.
    Confirms the fix and its test are both load-bearing. Reverted before
    committing anything; `git status --short` clean after restore.
- No `.ts`/`.js` under `extension/src`/`extension/test` touched by this
  ticket; `npm test`/`npm run test:properties` not re-run per the
  2026-09-17 lane-set directive (already run fresh this session).
