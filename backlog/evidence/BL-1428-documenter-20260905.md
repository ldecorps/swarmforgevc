# BL-1428 documenter review pass — 2026-09-05

Reviewed the hardened commit (`e957ec9e23`) and the coder/cleaner/architect/
hardener trail for BL-1428 (every standing red names an open owner).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1428-every-standing-red-names-an-open-owner.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1428StandingRedRegisterSteps.js`; the
  `run_commit_guards.sh::check_standing_red_register.sh` wiring required
  by the first line is present (`swarmforge/scripts/run_commit_guards.sh`
  line 84, cheap tier, last in the chain).
- Read `standing_red_register_lib.bb`, `standing_red_register_cli.bb`, and
  `check_standing_red_register.sh`: confirmed invariant 1 (one reader of
  the three sources — property allowlist, hardening-debt ledger, the
  register itself), invariant 2 (the guard's `git diff --cached -U0`
  judges only added/changed lines, never pre-existing rows), and
  invariant 3 (the allowlist gate's own reader/refusal predicate is
  untouched — this parcel edits only the allowlist's rows).
- Confirmed the allowlist clean-up: the five green rows
  (`bl1012`/`bl593`/`bl632`/`bl687`/`selfHealTelemetry`) are removed from
  `property_suite_standing_allowlist.tsv`; the twenty red rows keep their
  `allowlist` disposition with an updated rationale.
- Per the ticket's own direction ("QA's daily briefing section for
  standing reds is the documenter's call: at minimum the how-to under
  docs/how-to/ explains the register, the guard, and the rule that a land
  turning a test green removes its row in the same commit"), authored a
  new how-to: `docs/how-to/BL-1428-standing-red-register.md`, covering the
  three-source join, the CLI's JSON shape, the cheap-tier commit guard's
  mechanics and fail-open posture, the allowlist clean-up, Verify
  commands, and cross-links to BL-942, BL-1175, BL-1252, BL-816, BL-1429,
  and BL-1430.
- New doc linked from `docs/index.md` in the same commit (Documentation
  Standard requirement), placed beside the BL-1175 entry it directly
  builds on.
- Checked for any existing BL-816 doc to move to `docs/deprecated/`: none
  exists (BL-816 was approved 2026-08-05 and never promoted — nothing was
  ever built, so there is no shipped-behavior doc to retire).
- No "Last Updated" freshness field in either touched doc — no date bump
  owed.
- No registered diagram depicts commit-guard membership or the register's
  internal join — no diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond the new how-to.

Result: NONE — no defect found; new how-to authored per the ticket's own
documentation direction.
