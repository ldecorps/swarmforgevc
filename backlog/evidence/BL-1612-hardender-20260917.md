# BL-1612 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Babashka/Clojure — no mutation/CRAP/DRY tool wired (BL-472); gated only by
its own unit-test suite per the shared Design And Testability rule.

- `bb swarmforge/scripts/test/review_forward_evidence_gate_lib_test_runner.bb`
  → `ALL PASS`.
- `bb swarmforge/scripts/test/parcel_rollback_guard_lib_test_runner.bb` →
  `ALL PASS`.
- `bb swarmforge/scripts/test/bl1612_send_gates_batch_property_runner.bb`
  → `ALL PROPERTIES HOLD` (300 layout trials x 2 receive modes x 2 readers
  + 20 nil-otherwise trials, fixed seed).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1612-*.feature`
  → 6/6 pass.
- Sibling regressions re-verified: BL-806 9/9, BL-1213 8/8, BL-1313 9/9,
  BL-1576 11/11 — all green.
- Hand-mutation (BL-567 fallback, no Stryker on `.bb`): reverted BOTH
  readers (`received-parcel-for-task` in `review_forward_evidence_gate_lib.bb`
  and `received-parcel-commit-for-task` in `parcel_rollback_guard_lib.bb`)
  to the pre-fix flat reader (`handoff-lib/handoff-files`) simultaneously
  — caught immediately and decisively: 1 unit failure + 3 unit failures
  (the two runners' own new batch cases) and 532/620 property failures.
  Confirms the fix is load-bearing and non-vacuous well beyond the
  coder's own reported 580/620. Reverted back before committing anything
  (working tree clean, `git status --short` verified after restore).
- **Live probe (qa_e2e_procedure step 3, run on this hardening turn):**
  this session was itself holding BL-1612's own git_handoff inside a real
  batch directory (`in_process/batch_20260917T115121Z_000001/`) received
  from the architect at commit `26da7c8623`. Called both readers directly
  against this session's own real repo root and role
  (`review-forward-evidence-gate-lib/received-commit-for-task` and the
  private `parcel-rollback-guard-lib/received-parcel-commit-for-task` via
  var-deref, same technique the coder's own acceptance step handler
  uses) — both correctly returned `26da7c8623`, not nil. This is the
  exact live scenario the ticket exists to fix, reproduced against this
  session's own real mailbox rather than only a fixture.
- No `.ts`/`.js` under `extension/src`/`extension/test` touched;
  `npm test`/`npm run test:properties` already run fresh by the coder
  this session per the 2026-09-17 lane-set directive, not re-run.
