# BL-1433 documenter review pass — 2026-09-05

Reviewed the hardened commit (`dd495242ad`) and the coder/cleaner/architect/
hardener trail for BL-1433 (a branch that holds the landed commit is not
behind).

Checklist:
- Step handler exists:
  `specs/pipeline/steps/bl1433BranchHoldsLandedCommitSteps.js`.
- `required_wiring` `handoffd.bb::contains-landed?` anchor present: the
  daemon's fact supplier answers whether the landed sha is an ancestor of
  the role HEAD, wired as the `post-qa-branch-sweep-lib/sweep!`
  `:contains-landed?` fact.
- Read `post_qa_branch_sweep_lib.bb`'s `decide-role`: confirmed invariant
  1 (`contains-landed?` is checked right after `:already-settled` and
  before `in-process?`/`dirty?`, so `:holds-landed` fires whatever else
  the worktree holds), invariant 2 (`:divergent-branch` is now the
  `:else` branch, reached only once `contains-landed?` is false — a HEAD
  that genuinely lacks the landed commit and can't fast-forward), and
  invariant 3 (`(nil? contains-landed?)` produces `:skip
  :unknown-containment`, distinct from `:missing-ref`, never a tell).
- This is the direct fix for the merge-up-note flood I flagged and
  absorbed as no-ops for roughly 45 minutes this session (60+ identical
  "branch cannot fast-forward" notes on commits `abb209f4f1` /
  `9c6e81e224` / `25d7d6f50f` / `a3d574c48f`) — confirmed the ticket's own
  measured numbers (61 notes, four roles, fifteen minutes) match that
  observed pattern.
- Operator/agent-visible behavior change (a role branch that has merged
  `main` and started its own new work no longer receives a repeating
  "branch behind ... cannot fast-forward" tell — it is now logged as
  `:holds-landed` and left alone) is documented in
  `docs/how-to/BL-668-post-qa-deterministic-branch-sweep.md` — the
  existing sweep how-to already covering BL-1361/BL-1421 — with a
  rewritten "What gets fast-forwarded" precedence table and explanation,
  an updated "Wake vs. defer" paragraph, and updated Verify/Siblings
  sections.
- `docs/index.md`'s one-line summary for that how-to extended with a
  BL-1433 clause in the same running-list style the entry already uses.
- No "Last Updated" freshness field in the edited doc — no date bump
  owed.
- No registered diagram depicts this mechanism's internal decision logic
  (a bugfix inside an existing sweep's decision precedence, not a new
  component, boundary, or pipeline-topology change) — no diagram change
  needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
