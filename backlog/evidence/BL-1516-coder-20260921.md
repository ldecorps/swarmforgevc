# BL-1516: a test fixture never reaches the live checkout — coder

## What changed

1. `swarmforge/scripts/test/test_bl1378_expedite_close_guard.sh` — added
   `prove_root` (test_bl1390_post_commit_push.sh's own `in_fixture` idiom:
   the STRING check first — `$root` itself must sit under `$TMPROOT` — then
   `git -C "$root" rev-parse --git-common-dir` must also resolve under
   `$TMPROOT`, since a relative `.git` answer proves nothing on its own).
   Called from `mk_fixture` (right after `git init`, before any further
   mutating command) and from `unlanded_commit` (before `checkout -b
   side`, the exact line that landed a stray branch on the live coder
   worktree on 2026-09-03). A bad root aborts the WHOLE test, one line
   naming the path. New section 07 regression-tests the abort itself
   (a real repo whose path sits outside `$TMPROOT`): refused, the path
   named, no new branch appears in that outside repo.
2. `swarmforge/scripts/handoffd.bb` — the BL-1395 probe placeholder
   (`project-root`'s fallback when neither an explicit arg nor
   `invoked-directly?` applies) is now an ABSOLUTE path under
   `java.io.tmpdir` (`<tmpdir>/bl1395-load-probe-no-root`), never the bare
   relative string that resolved against whatever the caller's cwd
   happened to be.
3. `swarmforge/scripts/test/bl1494_post_qa_sweep_wake_field_test_runner.bb`
   — the one live caller that drives a REAL writer under the probe path
   (`post-qa-branch-sweep-tell!` writes `handoffd.log` and
   `dispatch-gap-drafts/draft-*.txt`) now supplies its OWN mkdtemp root
   explicitly (`binding [*command-line-args* [probe-root]]` around the
   `load-file`, bl1458's own established idiom, including the
   `SWARMFORGE_ALLOW_TMP_DAEMON=1` self-re-exec BL-406 needs for any
   tmp-rooted `project-root`), instead of relying on the placeholder
   default at all — this is the runner that leaked
   `./bl1395-load-probe-no-root` at the repo root (2026-09-10 09:42:57).
   `probe-root`'s cleanup moved into a `try/finally` (was a bare trailing
   `delete-tree`, which `tempDirTrapGuard.test.js` correctly flagged as
   unprotected against a thrown assertion or exception — fixed, same
   idiom `bl1458_instruct_documenter_briefing_test_runner.bb` already
   uses).
4. `swarmforge/scripts/test/run_bb_suite.sh` — takes a census (`ls -A` of
   `git rev-parse --show-toplevel`) once before the suite loop and again
   after every test; a NEW top-level entry prints
   `ROOT_POLLUTION_DETECTED test=<file> entry=<entry>` and fails the run
   (exit 1) even when every test itself passed. The census baseline
   advances past a reported entry so a LATER test is never blamed for an
   earlier one's leak. A clean run (or `--list`/`--inventory` mode, never
   touched) prints no such line and behaves exactly as before.
5. Cleanup, in this coder worktree, per Article 1.9's approval authority
   (the ticket's own `approval_context`) — every precondition re-verified
   FRESH in this pass (not trusted from the ticket's own text), immediately
   before each deletion:
   - `git worktree remove --force` the six nested worktrees under
     `.worktrees/coder/.worktrees/{QA,architect,cleaner,coder,documenter,
     hardender}` and `git branch -D` their six `primary/<role>` branches —
     precondition re-verified: every one's tip is exactly `2e519c4a8f`,
     and `git merge-base --is-ancestor 2e519c4a8f main` is true.
     `primary/coder-cursor2` (a real, live worktree) and `primary/art-
     director` (which does not exist as a branch in this checkout at all)
     were never touched.
   - Removed `--check-once/`, `--help/`, `--tick-once/`, `undefined/`,
     `unpark/`, `bl1395-load-probe-no-root/`, and the directory literally
     named `On branch main\nnothing to commit, working tree clean\n` (git
     status output used as a path, 2026-09-03 12:46:29 — the BL-1378
     incident's own root, now fixed by item 1 above) — precondition
     re-verified for each: `git ls-files -- <entry>` empty, and every file
     found inside each (down to the leaf) is `git check-ignore`'d at
     `.gitignore:4:.swarmforge/`.
   - `git status --short`, `ls -A .`, `git worktree list` and
     `git branch --list 'primary/*'` all confirmed clean/expected after
     every deletion (see Verification).

## Verification

- `bash swarmforge/scripts/test/test_bl1378_expedite_close_guard.sh`:
  ALL PASS (26 checks including new section 07).
- `bb swarmforge/scripts/test/bl1494_post_qa_sweep_wake_field_test_runner.bb`:
  ALL PASS; confirmed no `bl1395-load-probe-no-root` entry appears at the
  repo root after the run (the pre-existing stray entry's mtime, 2026-09-10
  09:42, was unchanged by this run — and is now removed by item 5 anyway).
- `node specs/pipeline/cli.js` / `run_acceptance.sh` on BL-1516's own
  feature: 6/6, run 3x for stability.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4 (the new
  step handler uses `mkSocketFixtureRoot`).
- `npx vitest run test/tempDirTrapGuard.test.js`: 4/4 — this run's OWN
  first pass caught a real gap (bl1494's `probe-root` had no try/finally),
  fixed before this forward (see item 3).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1516FixtureNeverReachesLiveCheckoutInvariants.property.test.js`:
  3/3, plus three non-vacuity breaks below.
- `npm test` (compile + unit lane): 10847/10848 pass. The one failure
  (`test/stepHandlerModuleLoadBudget.test.js`,
  `bl1146HostQueueEnqueueNextHoldOnHostQuestionSteps.js` over its 400ms
  confirm-alone budget) is the same pre-existing, already-owned standing
  red seen during this session's other parcels — `backlog/standing-reds.tsv`
  names it under **BL-1658**; nothing in this parcel touches any
  cursor-bridge/step-handler require path.
- Did NOT run `run_bb_suite.sh` for real against the live tree (its own
  header: real tmux, agent-pane runs have killed live swarm sessions
  before) — the census logic is instead verified end to end against an
  isolated fixture checkout under mkdtemp, via
  `specs/pipeline/steps/lib/bl1516RunBbSuiteCensusCli.sh`, which copies the
  real, edited `run_bb_suite.sh` and `lib/git_env_guard.sh` there and
  stubs only the (separate, pre-existing, BL-973) inventory gate.
- Manual re-verification of every cleanup precondition, immediately before
  each deletion (see "What changed" item 5); final state confirmed via
  `ls -A .`, `git worktree list`, `git branch --list 'primary/*'` and
  `git status --short` — no stray entries, no nested worktrees, no
  `primary/*` branches besides `coder-cursor2`, no tracked-file changes
  from the deletions themselves.

## Invariants (BL-654)

Both of BL-1516's declared invariants have coder-authored, non-vacuous
property tests in
`extension/test/bl1516FixtureNeverReachesLiveCheckoutInvariants.property.test.js`
(three tests total — invariant 1 split into 1a/1b since it names two
distinct mechanisms, the fixture-root proof and the suite census):

- invariant 1a (fixture-root proof): drives the real `prove_root` over
  three shapes (not a git repository at all, a real repository whose path
  string sits outside `$TMPROOT`, and a genuinely proven root) and asserts
  the enclosing repository is untouched in every case. Non-vacuity: removed
  the string-check clause and confirmed the property fails on the
  outside-repo shape (the exact class of bug the string check exists to
  catch, since a bare `.git` answer alone proves nothing); restored,
  re-verified green.
- invariant 1b (suite census): drives the real `run_bb_suite.sh` over
  three entry shapes (a plain name, a flag-shaped name, a dotted name) plus
  a clean run, asserting the exact `ROOT_POLLUTION_DETECTED` line and a
  non-zero exit for each polluting shape, and a clean exit with no line for
  the clean one. Non-vacuity: short-circuited the census block and
  confirmed the property fails (no refusal, exit 0, for a test that
  demonstrably created an entry); restored, re-verified green.
- invariant 2 (probe placeholder never writes under cwd): drives the real
  handoffd.bb probe over three different isolated cwd shapes (including one
  with spaces and one nested), asserting no entry at cwd, an absolute log
  path outside cwd, and the cwd directory left completely empty. Non-
  vacuity: reverted the placeholder to its old relative string and
  confirmed the property fails, naming the exact leaked relative log path;
  restored, re-verified green.

All three restores verified against a clean load (`bb -e '(load-file
...)'`  for the .bb files, `bash -n` for the shell files) and a full
re-run of the property test file (green).

By coder.
