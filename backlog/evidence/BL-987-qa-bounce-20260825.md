# BL-987 QA bounce — 20260825

**Reviewed tip:** documenter `7c9ab0d29e` (not landed — unit red)
**Task:** `BL-987-headless-emitter-audit-canary-fired-unanswered`
**Sibling:** `VERIFY BL-987`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | PASS — paths=16, dels=0 |
| Sibling | VERIFY |
| `npm run compile` (extension) | PASS |
| APS BL-336 feature | 8/8 |
| `node --test …/liveChaserTelemetry.property.test.js` | 3/3 PASS |
| `node --test …/monthlyTelemetryFile.property.test.js` | **FAIL → D1** |

## Inventory

### D1 — `unit` (blame: hardender)

**Failing command:**
```
node --test extension/test/monthlyTelemetryFile.property.test.js
```

**Commit hash checked:** `7c9ab0d29e`

**First error excerpt:**
```
ReferenceError: describe is not defined
    at Object.<anonymous> (…/monthlyTelemetryFile.property.test.js:11:1)
```

**Failure class:** `unit`

**Expected vs observed:** Hardener pass claims
`monthlyTelemetryFile.property.test.js: require node:test` so the property
gate runs under `node --test` (vitest excludes `*.property.test.js`).
Observed file uses `describe`/`it` with **no** `require('node:test')` —
`node --test` fails; Vitest excludes the file too.

**Remediation:** Add `const { describe, it } = require('node:test');` to
`extension/test/monthlyTelemetryFile.property.test.js` and re-run
`node --test` green before forwarding. Rematch tip-pure onto current
`origin/main` when re-forwarding.

## Verdict

BOUNCE → hardender. Do not land.

By QA.
