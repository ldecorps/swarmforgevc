# BL-1203 — QA pass — 20260828

**Received:** `merge_and_process documenter 9739fc2f77` (handoff
`00_20260828T024726Z_000882_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `9739fc2f77...`
**QA merge commit:** `a317244581548115c413d05f18ea257e2869af91`

Second pass. First pass bounced (`BL-1203-bounce-20260828.md`) for the
documenter's pass being entirely missing — no how-to, no `docs/index.md`
link, no `Specification.MD` entry. Documenter's re-fix (`733724b78`) added
all three; nothing else in the parcel changed (the underlying dedup fix was
never in question — see the bounce evidence's own inventory).

## Merge

Clean, no conflicts (`git merge --no-ff 9739fc2f77`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor fa96e9013 HEAD` (coder, original fix) — CONFIRMED
- `git merge-base --is-ancestor 065b85d7a HEAD` (coder, D1 fixture-leak re-fix) — CONFIRMED
- `git merge-base --is-ancestor 027d2accd HEAD` (cleaner, round 2) — CONFIRMED
- `git merge-base --is-ancestor 3357f9de5 HEAD` (architect, round 2) — CONFIRMED
- `git merge-base --is-ancestor 1c3b3a0ae HEAD` (hardener) — CONFIRMED
- `git merge-base --is-ancestor 733724b78 HEAD` (documenter, doc re-fix) — CONFIRMED

## Documentation re-check (the bounced item)

- `docs/how-to/BL-1203-role-answer-notes-delivered-once.md` present:
  explains the pre-fix replay symptom, the three guarantees the fix now
  provides (identity-keyed dedup, unconditional pointer-file write, legacy
  calls never dedupe), the real wiring chain
  (`processSteeringUpdate`/`deliverAskAnswer`/`captureRoleAnswer`/
  `enqueueRoleAnswerNote`), and a `Verify` section with runnable commands.
- `docs/index.md:122` links it.
- `docs/reference/Specification.MD` top entry (dated 2026-08-28) describes
  the fix accurately and cites both the feature file and the how-to page.
- Confirmed the re-fix touched ONLY documentation:
  `git diff --name-only 1c3b3a0ae 733724b78` shows zero
  `extension/src`/`extension/test` paths.

## Verification inventory (re-run)

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1203` | `VERIFY BL-1203` — no open deferral |
| Compile | `npm run compile` | PASS, clean |
| Targeted unit | `npx vitest run telegramFrontDeskBotCli telegramFrontDeskBotCore` | 715/715 PASS |
| Targeted property | `npx vitest run --config vitest.properties.config.mjs telegramFrontDeskBotCli` | 3/3 PASS; 0 `/tmp/bl1203-property-*` stragglers (D1 fixture-leak fix still holding) |
| Acceptance | `run_acceptance.sh specs/features/BL-1203-...feature` | 2/2 PASS |
| Invariant 1 (at most one note per inbound identity) | property test (non-vacuous, keyed on `updateId` history) | Holds |
| Invariant 2 (pointer file matches announced answer) | property test (non-vacuous) | Holds |
| Wiring | grep + read | Real Telegram `update.update_id` threads through `processSteeringUpdate` → `deliverAskAnswer` → `captureRoleAnswer` → `enqueueRoleAnswerNote` end-to-end (re-confirmed unchanged from first pass) |
| Full unit/property suite | not re-run this pass | Re-run not needed — the doc-only diff cannot regress `extension/src`/`extension/test`; both suites already confirmed at the session's standing-red baseline (206/9193 unit, 18/690 property) on the immediately preceding BL-1205 pass, on top of which this merge sits |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker'` | none, before and after |

## Inventory (Article 4.4)

NONE — the bounced defect (missing documentation) is fixed; no other
defect attributable to BL-1203's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1203 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1203 active → done).

By QA.
