# BL-1529 — hardender review pass, 2026-09-12

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: specs/pipeline/scripts/run_gherkin_mutation.sh (soft, BL-1529 Scenario Outline)
- **Commit hash**: bde3893af6
- **First error excerpt**: cleanup(ctx) only called from terminal steps (some without try/finally); an earlier assertion throwing (e.g. exactly-one-handoff-file) skipped it and leaked the mkdtemp fixture root
- **Failure class**: test-infrastructure (fixture-root leak, 2026-08-18/08-19 cross-step-throw class)
- **Expected vs observed**: every scenario outcome removes its fixture root, matching passing pass
- **Blamed role**: hardender (own domain: test-infra repair)
- **Remediation pointer**: adopted BL-971/BL-1228 process-exit fixture-root registration pattern

By hardender.

## Detail

D1 was fixed in-pass (test-infrastructure repair in the hardener's own
domain per Article 1 "Owns"), not sent back — the parcel forwards normally
to documenter. The "bounce" framing above is this evidence tool's standard
template language; no send-back occurred.

Full verification this pass:
- `bash swarmforge/scripts/test/test_redo_from.sh` and `test_reroute.sh`:
  ALL PASS (the two shell tests the ticket exists to turn green).
- `extension/test/bl1529ScriptSenderAuditOutcomesInvariant.property.test.js`
  (queued/failed x 5 stages, non-vacuity re-verified) and the modified
  BL-1094 sibling property test: green.
- Acceptance (fresh `npm run compile` first, BL-497): 7/7 scenarios pass.
- BL-113 Gherkin mutation (soft) on Scenario Outline
  script-sender-speaks-the-audit-01: 4/4 mutants killed, 0 survived;
  manifest embedded in the feature file at `bde3893af6`.
- All 18 `extension/test/*Guard*.test.js` whole-tree guards (parcel
  touches `specs/pipeline/steps/`): 183/183 pass.
- Hand-verified `handoff_lib.bb`'s `queued-line-pattern` regex against all
  four real `swarm_handoff.bb` success-message shapes (DELIVERED, HELD
  (ambulance), QUEUED mailbox-only, QUEUED daemon-backup) — all four match
  and capture the outbox path.
- No CRAP/DRY: `mutation_cost: low`, no `.ts` source touched
  (Babashka-only per BL-472).
- No orphaned test/mutation processes and no leaked fixture dirs after the
  pass (`pgrep` + tmp-dir checks clean).

By hardender.
