# QA pass — BL-1439 — 2026-09-06

Commit under review: documenter's `0457ba8f0b`, fast-forwarded into
`swarmforge-QA`, carrying the full chain (`ed6fb8892b` BL-1439 coder,
confirmed ancestor via `git merge-base --is-ancestor`). Spans two days and
one mid-flight amendment (a third ledger verb, `--attempt`, minted by the
specifier alongside two successor tickets, BL-1440 and BL-1441) that
landed cleanly with no bounce.

## Verification

- `bb swarmforge/scripts/test/hardening_debt_ledger_lib_test_runner.bb`: ok.
- `bash swarmforge/scripts/test/test_hardening_debt_ledger_cli.sh`: ALL
  CHECKS PASSED, including the amended `--attempt` verb's own invariant-3
  check ("attempt: the row is NOT discharged").
- `bb swarmforge/scripts/test/bl942_hardening_debt_ledger_property_runner.bb`: ok.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1439-the-deferred-hardening-gates-of-0819-are-run-and-discharged.feature`
  — 4/4 (the amended count), including "every 2026-08-19 row ... is
  discharged or carries an attempt record, and each outstanding one is
  owned by the successor".
- **Read the live ledger directly** (`hardening_debt_ledger_read.bb .`):
  1 row genuinely discharged (BL-956/gherkin-mutation,
  `discharged_evidence: backlog/evidence/BL-1439-bl956-gherkin-mutation.md`)
  and 4 rows carry `attempted_at`/`attempted_blocker` with `discharged_at:
  null` — none falsely marked discharged. Read the discharge evidence
  file: a genuinely COMPLETE gherkin-mutation run (`total=6 completed=6
  killed=6 survived=0`, no stall), not an assertion of success.
- Read each of the 4 attempt blockers: all name real, specific, checkable
  causes (a `mutation_cooldown_gate.bb` skip on files BL-1425 touched
  hours earlier, and a Stryker dry-run blocked by the `constitutionDocCitations`
  red now owned by BL-1440) — honest attempts, not hand-waved refusals,
  each explicitly naming BL-1441 as the successor.
- Register live check: `grep BL-620\|BL-955\|BL-954\|BL-956
  backlog/standing-reds.tsv` — all 4 outstanding rows re-pointed to
  BL-1441 (never unowned); the discharged row correctly carries no
  register row at all. `constitutionDocCitations`'s row confirmed
  re-pointed to BL-1440.
- Confirmed BL-1440 and BL-1441 both exist (`backlog/paused/`).
- `required_wiring`: all four anchors confirmed (`--discharge`,
  `outstanding-debt` excludes discharged, `--attempt`,
  `bl1439DeferredHardeningGatesDischargedSteps.js::registerSteps`).
- Docs: `docs/how-to/BL-942-hardening-debt-ledger.md` updated with the
  `--attempt` verb and its rule ("a run the host refused is an attempt,
  not a discharge").
- Full unit lane (`npm test`): only 1 file failing now
  (`constitutionDocCitations`, owned by BL-1440) — down from 3 at the
  start of this shift; `operatorRuntimeBbFixtureClosure` and
  `pricingTable` (BL-1436, landed earlier this session) both confirmed
  green. No regression from this ticket.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
