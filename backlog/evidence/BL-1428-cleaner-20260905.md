# BL-1428 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed `check_standing_red_register.sh`, now in `run_commit_guards.sh`'s
  Tier 1 (a live path every future commit, including this one, goes
  through): judges only `git diff --cached -U0` added/changed lines on the
  three source paths (invariant 2 — a pre-existing stale row never
  refuses); the TAB-collapsing `read` pitfall around empty ticket fields
  is sidestepped correctly via the `\x01` re-join (`tsv_fields_01`); the
  allowlist branch reuses the SAME register join `standing_red_register_lib.bb`
  uses rather than inventing a second ownership rule (matching invariant 1
  and the guard's own header comment); fail-open is scoped only to an
  unresolvable repo root/cd, not to the guard's own refusal logic — a
  deliberate, explained choice ("this guard's OWN refusal requires being
  SURE, not merely suspicious").
- Confirmed `ticket_open` correctly checks only `backlog/paused` and
  `backlog/active` (not `done` or `hold`), matching "open" as the ticket
  defines it.
- Ran the live register CLI directly: `count: 32`, 5 unowned rows, all
  five confirmed to be stale `hardening-debt-ledger.yaml` entries whose
  owning tickets (BL-620/BL-955/BL-954/BL-956×2) have since landed —
  exactly the coder's own self-flagged, correctly-out-of-scope finding
  (BL-942's own drain mechanism, not this ticket's). Not a defect in the
  reader; the ticket's own qa_e2e literal expectation was written without
  checking the ledger's current content, as the coder's evidence states.
- Confirmed invariant 3 (allowlist gate reader/predicate untouched):
  neither `property_suite_standing_allowlist_lib.sh` nor
  `check_property_suite_drift.sh` appears in this parcel's diff; the
  allowlist's remaining 20 rows and their count (`grep -c allowlist` → 20)
  match exactly.
- No duplication: `jscpd` (which parses `.sh`, unlike the `.bb` siblings)
  reports 0 clones on the shell guard.
- Babashka/shell have no mutation/CRAP/DRY tooling wired (BL-472 deferred)
  — this pass is gated by the unit/property/acceptance suites only, per
  constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/standing_red_register_lib_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/bl1428_every_standing_red_names_an_open_owner_property_runner.bb` | ALL PROPERTIES HOLD, 200/200 each of P1/P2 |
| `node specs/pipeline/cli.js specs/features/BL-1428-...feature` | 7/7 scenario runs |
| `bash swarmforge/scripts/test/test_run_commit_guards.sh` (regression, the guard-chain itself) | 12/12 PASS |
| `(cd extension && npx vitest run --config vitest.properties.config.mjs test/bl1252CommitGuardAggregationInvariants.property.test.js)` | 5/5 PASS |
| `bash swarmforge/scripts/test/test_property_suite_drift_guard.sh` | 6/7 — scenario 07 confirmed the same pre-existing, unrelated red owned by BL-1409 |
| `bb standing_red_register_cli.bb .` | count 32, 5 unowned (all confirmed stale ledger entries with landed owners) |
| `grep -n check_standing_red_register.sh run_commit_guards.sh` | Tier 1 (required_wiring) |
| `bl1428StandingRedRegisterSteps.js::registerSteps` exists | yes (required_wiring) |
| `grep -c allowlist property_suite_standing_allowlist.tsv` | 20 (5 green rows removed, invariant 3) |
| `bb suite_inventory_cli.bb swarmforge/scripts/test` | 0 problems |

No fix required. Forwarding unchanged to architect.
