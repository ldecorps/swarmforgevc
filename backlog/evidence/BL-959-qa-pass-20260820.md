# BL-959 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-959 — APS candidate-toolchain equivalence run (`type: chore`, `severity: low`, milestone M8)
- **Parcel**: `00_20260820T015543Z_000223_from_documenter_to_QA_for_QA` (from documenter, commit `0435b85b1a`)
- **Verified at**: `8d5d34676` (QA merge of the documenter tip)
- **Reviewed by**: QA, 2026-08-20
- **Prior bounces**: 1 — architect→coder, `behavior`, `1498580a17` (D1: a false factual claim in §1). **Closed and independently re-verified** — see G10.

## Verdict

**PASS — inventory items: NONE.**

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — cleaner/architect `1498580a17`, architect pass 2 `4b591053e`, hardener `dfe32c93d`, documenter `0435b85b1a` | PASS — all ancestors of the cited commit |
| G2 | `aps_equivalence_lib_test_runner.bb` | PASS — `ALL PASS` |
| G3 | `aps_equivalence_cli_test_runner.bb` | PASS — `ALL PASS` |
| G4 | `aps_equivalence_lib_property_runner.bb` | PASS — `ALL PROPERTIES HOLD` |
| G5 | Acceptance — `BL-959-aps-candidate-toolchain-equivalence.feature` | PASS — 3/3, 0 fail, exit 0 |
| G6 | `qa_e2e` step 1 — harness end-to-end, real network fetch | PASS — cloned the candidate, rev-parse verified, dual-ran 8 corpus entries, wrote `matrix.txt`/`matrix.md`. Verdicts: **13 EQUIVALENT, 4 DIVERGENT** |
| G7 | **`qa_e2e` step 3 / INVARIANT 1 — pinned surfaces read-only** | PASS — see below |
| G8 | **`qa_e2e` step 2 / INVARIANT 2 — fail closed on a missing outcome** | PASS — see below |
| G9 | `qa_e2e` step 4 — spot-check a DIVERGENT row by hand | PASS — the matrix told the truth; see below |
| G10 | Prior bounce D1 closed, re-verified against ground truth | PASS — see below |
| G11 | Evidence report has all four required sections | PASS — §1 per-commit classification, §2 verdict matrix, §3 entry-point compatibility, §4 recommendation |
| G12 | Step-registry change covered | PASS — `bl800StepRegistryScopingConsistency.property.test.js` 2/2; the registry loads with the new `bl959ApsEquivalenceSteps` handler registered |
| G13 | Full unit suite | PASS (inherited) — 440/444 at `aa980e00b6` with all failures isolated-clean. `git diff` shows **no** `extension/` change since; only `specs/pipeline/steps/` moved, covered by G12 |
| G14 | `pre_qa_gate.sh` | **FAIL — both flagged commits adjudicated as false positives**, see below |
| G15 | Docs currency | PASS — `Specification.MD` entry plus `docs/how-to/BL-959-aps-candidate-toolchain-equivalence-run.md` |
| G16 | Functional change present | PASS — lib, CLI, runner, shell harness, step handler, feature, how-to, evidence report |

## G7 — invariant 1 measured, not assumed

This ticket fetches and RUNS a **non-pinned** toolchain, so the read-only claim was measured across a full run rather than read off the code. Hashes taken before, compared after:

| Surface | Before | After |
|---|---|---|
| `swarmforge.lock.json` | `a83077bb3a…` | `a83077bb3a…` — identical |
| `upstream-watch.json` | `250537a238…` | `250537a238…` — identical |
| `swarmforge/vendor/aps/` | 12 files | 12 files, `git status` clean |

The harness writes only into `mktemp` work dirs and clones the candidate into a separate throwaway dir under a `trap … EXIT` cleanup. It READS the lock file (for the repo URL) and the vendored pin (as the pinned side), which is exactly what invariant 1 permits.

Note on scope: engineering.prompt's pin rule ("bumping a pin is a human commit, never an agent action") is NOT breached here — nothing is vendored, installed, or bumped. The specifier flagged this reading explicitly in `approval_context` and it carries `human_approval: approved`.

## G8 — fail-closed proven by removing a result, not by reading the code

The comparator exits 1 on the complete work dir already, because 4 rows are DIVERGENT — so **exit code alone cannot distinguish INCOMPLETE from DIVERGENT**, and the verdict text had to be checked.

Deleting exactly one candidate-side result (`results/candidate/ir-dry/specs__features__BL-101-….feature.json`) and re-running the comparator alone:

```
INCOMPLETE|specs/features/BL-101-headless-secondary-swarm-pi-vps.feature|ir-dry|candidate outcome missing
```

Verdict counts moved `13 EQUIVALENT / 4 DIVERGENT` → `12 EQUIVALENT / 4 DIVERGENT / 1 INCOMPLETE`, exit non-zero. The row that lost its outcome had previously been EQUIVALENT, so **absence was demonstrably not read as equivalence**. File restored afterwards.

## G9 — the matrix told the truth

Spot-checked the most consequential DIVERGENT row, where the candidate FAILS and the pin passes:

```
DIVERGENT|specs/features/BL-106-swarm-name-branch-namespace.feature|lint-parse|
  pinned={"exit":0} candidate={"exit":1,"error":"missing example columns for explicit placeholders: role"}
```

By hand: that file uses `<role>` at line 7 (`Then every role worktree is on branch alpha/<role>`) and contains **no `Examples:` block at all**. The candidate is right and the pinned toolchain silently accepts a malformed Scenario Outline. The divergence is the candidate being STRICTER — precisely the kind of finding this ticket exists to surface for a human pin-bump decision, and the matrix reported it accurately.

## G10 — the prior bounce's factual claim, re-checked against the pin itself

Architect pass 1 bounced D1 because §1 claimed the candidate "adds `strip-empty-keys`". Rather than trust the re-fix, this was checked against the vendored pin directly: `swarmforge/vendor/aps/bb/src/aps/json.clj:23` defines `strip-empty-keys`. It exists at the pin. The report now reads "`strip-empty-keys` exists at both", names both locations, and carries an explicit correction note. D1 is genuinely closed on the facts.

## G14 — both pre-QA ancestry failures are false positives

```
PRE_QA_GATE_FAIL ancestry BL-959 169d7120d5 stranded on swarmforge-{architect,cleaner,documenter,hardender}
PRE_QA_GATE_FAIL ancestry BL-959 02454d39c5 stranded on swarmforge-documenter
```

- **`169d7120d5`** — the architect's scoped revert of BL-967, whose subject lists the tickets it deliberately did NOT revert ("…would also revert the entangled BL-571/BL-910/BL-957/BL-958/**BL-959**/BL-960 work…"). It touches no BL-959 file. This is the **fourth** ticket today hit by this same subject-line match (BL-935, BL-958, BL-910, now BL-959).
- **`02454d39c5`** — a documenter commit from 05:07, AFTER this parcel was sent, touching only `docs/reference/Specification.MD`. Its four BL-959 lines are the SAME entry repositioned: here BL-959 is the current `**Last Updated:**` entry; there BL-571's restored entry moved above it and BL-959 became `Previously August 20, 2026 (BL-959: …)`. Same content, different position — nothing of BL-959 is missing.

Neither warrants an `abandoned_commits` declaration: one is live BL-967 work, the other is a later documenter reordering that arrives naturally on the next parcel.

## Standing gate defect worth its own ticket (flagged, not a BL-959 defect)

`pre_qa_gate`'s ancestry check matches ticket ids in commit **subject lines**. Four separate QA passes today were each blocked by a commit that merely NAMED a ticket — in three cases to say that ticket's content was deliberately excluded. Each cost a full content investigation to clear. The check needs to match on touched paths / actual content rather than subject text; recommended to the coordinator as its own ticket.

By QA.
