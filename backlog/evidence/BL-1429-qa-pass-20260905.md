# QA pass — BL-1429 — 2026-09-05

Commit under review: `701bff5521` (merge of documenter's `2dcb3c5e68` into
`swarmforge-QA`), carrying the full chain (`d584d435c5` BL-1429 coder,
confirmed ancestor via `git merge-base --is-ancestor`). Went through one
architect bounce (a real misattribution bug in the clearing-reason text,
independently re-verified non-vacuous by the hardener) before reaching me.

## Verification

- `npx vitest run test/standingRedSignal.test.js
  test/emitThrottleRecommendationStandingRed.test.js`: initially 7/9 and
  2/9 failed respectively with `ENOENT`/stale-import errors — traced to my
  own worktree's `out/` predating this commit's new `standingRedSignal.ts`
  module (same benign cause the hardener's own evidence independently
  documented hitting). Ran `npm run compile`; re-ran: 20/20 pass.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1429StandingRedThrottleFoldInvariants.property.test.js`: 4/4
  pass, including "a cap change is logged exactly once naming the correct
  signal" — directly exercises the architect-bounced misattribution class.
- Read `standingRedSignal.ts` and the `emit-throttle-recommendation.ts`
  diff directly: `minRecommendedCap` implements the null-safe minimum
  (invariant 1, never a raise); thresholds compared with strict `>`
  matching "more than 10" / "older than 7 days"; `unowned.length > 0`
  matches "any unowned red at any threshold"; `readStandingRedReport`
  degrades to `null` on any CLI failure rather than throwing.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1429-standing-reds-throttle-intake.feature` — 8/8
  scenarios pass, including "the effective depth CLI prints the throttled
  cap" (real end-to-end integration with `effective_backlog_depth_cli.bb`,
  not a unit-level stub) and "the rework diagnosis and the register never
  raise each other".
- `swarmforge.conf`: both `standing_red_max_count 10` and
  `standing_red_max_age_days 7` present with the ticket's own comment.
- `required_wiring`: `specs/pipeline/steps/bl1429StandingRedThrottleSteps.js`
  exports `registerSteps`; `effective_backlog_depth_cli.bb`'s live shell-out
  chain confirmed reaching the new fold via the acceptance scenario above.
- Coordinator prompt already amended at mint (per the ticket's own note)
  — confirmed present (`swarmforge/roles/coordinator.prompt` line 41).
- Read the hardener's BL-113 mutation evidence in full: 15/20 killed, the
  5 survivors independently checked against the actual comparison logic
  and confirmed genuinely equivalent (boundary values on the same side of
  each strict `>`/`.length > 0` check) — not hand-waved.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Operational consequence, already human-approved (informational)

Ran `bb standing_red_register_cli.bb .` live: `count: 7, unowned: 7` — the
register still names 7 unowned rows (the closed-ticket hardening-ledger
entries I independently confirmed earlier this session, BL-620/955/954/956
and related). Since `unowned.length > 0`, landing this ticket immediately
recommends cap 1 for backlog intake — exactly the consequence the ticket's
own `approval_context` names and the human already approved ("that is the
'stop everything and fix failing tests' you described, made proportional").
Not a defect; flagging for visibility only.

## Verdict

APPROVED — merging up and landing.
