# Cleaner pass — BL-574, BL-681, BL-762 (2026-08-08)

## Context
Received three coder `git_handoff`s in one batch (Article 2.6):
- BL-681 (`merge_and_process coder 15b9ff9dda`) — Article 5.3 acceptance wiring
- BL-574 (`merge_and_process coder 495d5df71d`) — PromptEngine Slice 2
- BL-762 (`merge_and_process coder 628e6e8a36`) — finish-shift bedtime verb

15b9ff9dda is already an ancestor of 495d5df71d, so merging BL-574's commit
brought BL-681 in too; BL-762's commit merged cleanly on top. Ancestry
confirmed for all three (`git merge-base --is-ancestor`).

## Review scope

**BL-681** (`specs/pipeline/steps/bl681ConsolidationNeverDropsHumanSentenceSteps.js`,
feature-draft promotion): pure acceptance step-handler wiring, no production
logic. Nothing for cleaner to change.

**BL-574** (`swarmforge/scripts/prompt_engine_lib.bb`): fragment registry,
content-hash cache (`read-fragment`/`read-fragment!`/`invalidate-fragment`),
and a registry-driven adapter table (`select-adapter`/`register-adapter!`).
Decision logic (`read-fragment`) is pure with an injectable `content-fn` IO
seam, matching the project's decision/IO separation rule already used by the
sibling `.bb` libs. Adapter lookup is genuinely registry-driven (no
`case`/`cond` over provider names in the compose path) — Observable 3 is
satisfied for real, not routed around. No CRAP/DRY issues; no changes
needed.

**BL-762** (`swarmforge/scripts/finish_shift_lib.sh`,
`swarmforge/scripts/lifecycle_matrix.sh`,
`swarmforge/scripts/stop_ancillary_services.sh`): the keep/stop matrix is
factored into one data-driven source of truth
(`LIFECYCLE_MATRIX_ENTRIES` + `lifecycle_matrix_validate`) that both
`finish_shift_lib.sh` and `stop_ancillary_services.sh` read, exactly the
single-answer-in-one-place structure the ticket calls for. Coder already
refactored `stop_ancillary_services.sh`'s five inline stop blocks into named
`stop_<component>` functions plus a `stop_ancillary_component` dispatcher,
and fixed the sourced-vs-executed `set -euo pipefail` leak (guarded behind
the `BASH_SOURCE == 0` run-directly check) before I received it — no further
extraction or dedup needed.

No CRAP/DRY/structural issues found across the three tickets; no duplication
introduced.

## Verification run

- `bash swarmforge/scripts/test/test_prompt_engine_lib.sh` — 1 pre-existing
  failure, unrelated to this batch: `stable prefix under 50KB (< 51200
  chars)` (measured 65138). Confirmed pre-existing by running the identical
  suite against the pre-batch commit (303f6636) in a scratch worktree:
  64225 chars, same failure, already over the 50KB threshold there. BL-574's
  own diff touches no constitution/article/prompt content (verified via
  `git show 495d5df71d --stat`) — the ~900-char growth between the two
  measurements is unrelated drift, not this batch's doing. Out of scope for
  a no-new-behavior cleanup pass; flagging for whoever owns constitution
  size budget.
- `bb swarmforge/scripts/test/prompt_engine_fragment_cache_property_runner.bb`
  — 200/200 runs, ALL PROPERTIES HOLD.
- `bash swarmforge/scripts/test/test_finish_shift_lib.sh` — PASS=11 FAIL=0.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-681-consolidation-never-drops-a-human-sentence.feature`
  — 3/3 pass.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-574-prompt-engine-slice2-fragments-adapters.feature`
  — 9/9 pass.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-762-finish-shift-leaves-the-phone-path-up.feature`
  — 14/14 pass.

Mutation-site count tool (`mutation-site-count.js`) not run: all three
tickets' production changes are `.bb`/`.sh` (Babashka/shell), which the
project's tool table records as not wired for mutation/CRAP/DRY — TypeScript
compile/count is a no-op here (no `.ts` production files touched).

By cleaner.
