# BL-1398 — documenter pass (2026-09-04)

## Received
`aced76a5ef` (hardener; per `backlog/evidence/BL-1398-hardener-pass-20260904.md`).

## What changed, operator-visible
`extension/test/bl632CommitTimeGuardInvariants.property.test.js` built its
fixture from a hand-written `EXEC_FIXTURE_FILES` list of the real commit
guards. When `check_handler_module_graph.sh` (BL-1385) joined
`run_commit_guards.sh`'s Tier-1 chain, the hand list did not grow with it
— the fixture's copied runner could not find the new guard, the chain
failed inside the fixture, and the property test went red on `main` for a
reason unrelated to any guard actually being broken (the same class as
BL-973/BL-1279's hand-enumerated-membership rot). The fixture now derives
its guard set at test time by parsing `run_guard <script> ...` lines out
of `run_commit_guards.sh` and `swarmforge/git-hooks/pre-merge-commit`,
following each named guard's own `source`/`.` lines one level for its
libs — a guard the runner names but the tree lacks throws loud, naming
it, rather than silently running a narrower chain than production.

## Doc-domain review
`docs/how-to/BL-973-bb-fixture-closure-guards-and-suite-inventory.md` is
the established home for this exact class of fix (hand-enumerated fixture
membership rotting when the thing it mirrors grows).
- Added a new "Related — the commit-guard property fixture (BL-1398)"
  section. This fixture sits outside the page's own nine-fixture table
  (which is specifically about `.bb` `load-file` closures) since it
  derives from shell `run_guard` lines instead — noted as the same
  derive-don't-hand-list discipline applied to a different mechanism, not
  folded into the table.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-973 summary line.
- Diagram registry checked — no diagram depicts commit-guard fixture
  internals at this granularity.
- Ran `npx vitest run --config vitest.properties.config.mjs
  bl632CommitTimeGuardInvariants` from `extension/` — 2/2 PASS (previously
  red on `main`, per the ticket's own reproduction).
- Ran `npx vitest run --config vitest.properties.config.mjs
  bl1398GuardFixtureDerivedSet` from `extension/` — 2/2 PASS.

## Verdict
Doc content extended in the owning how-to page plus the standard
Specification.MD/index.md entries, not NONE. Forwarding to QA.
