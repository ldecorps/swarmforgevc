# BL-1582 — QA review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Sibling check: `qa-sibling-check.js status --ticket BL-1582` -> `VERIFY
BL-1582` (no open deferral). Register grep (standing-reds.tsv, allowlist,
suite-poles.tsv) for BL-1582: no row — expected for a docs ticket.

- Ancestry/wiring gate: first `pre_qa_gate.sh` run failed
  `PRE_QA_GATE_FAIL ancestry ... stranded on swarmforge-documenter` and
  `...swarmforge-hardender` for commit 2a187694b3 ("BL-1582: refresh
  mutation manifest timestamp (identical verdict)") — a hardener commit
  made after the documenter's handoff to QA, present on both branch tips
  but not an ancestor of what was forwarded. Its own message: a second,
  redundant mutation run overwrote the feature's `tested_at` timestamp
  with an identical verdict (7/7, 8/8, 26/26 killed, 0 survived). Merged
  it in (`Merge hardender 2a187694b3 into QA.`); re-run: OK.
- Acceptance: `run_acceptance.sh specs/features/BL-1582-*.feature` — 26/26
  scenarios pass (source-verified backticked paths and quoted literals
  against the six named source files, scenarios 02/03).
- `ONBOARDER_LAUNCH_DRYRUN=1 swarmforge/scripts/launch_onboarder.sh .`
  prints the DRYRUN supervisor/reconcile lines the guide describes.
- Cross-links: `grep -n BL-1582 docs/index.md
  docs/explanation/BL-643-non-pipeline-agents-as-a-class.md
  docs/tutorials/Onboarding-New-Project.md` — all three present.
- Read the page as an operator: task-oriented, six sections as specced,
  honesty rule intact ("The human runs that command themselves ... the
  Onboarder never launches or observes the target swarm").
- Unit suite: `git diff origin/main..HEAD --name-only -- extension/` is
  EMPTY — this parcel touches nothing under `extension/`. `npm test`
  nonetheless run three times against a host under sustained heavy
  concurrent load from other worktrees (14 concurrent vitest/stryker
  processes observed directly, load average 15–22 across the runs,
  worsening rather than transient). Each run flagged a DIFFERENT set of
  "new-pole offender" files (none touched by this parcel:
  emitLifecycleSnapshotCli.test.js, config.test.js,
  meanTicketTimeWalk.test.js) — the load-induced-noise signature already
  seen twice today on this same host (BL-1623, BL-1621 evidence). Solo
  re-run of all three flagged files: 50/50 tests pass
  (meanTicketTimeWalk.test.js 1.97s, config.test.js 2.51s,
  emitLifecycleSnapshotCli.test.js 12.3s — the last one still
  load-sensitive even solo, since the host itself remains saturated by
  concurrent swarm activity, not by anything under this parcel's control).
  Treated as host-load noise, not a regression: zero path overlap with
  this parcel's diff, and every file that tripped the gate differs
  between runs.

By QA.
