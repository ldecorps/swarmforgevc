# BL-1514 — hardender review pass, 2026-09-10

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Re-ran gherkin mutation and re-stamped both surviving features per the
ticket's own Hardener direction (their bodies changed, prior stamps
invalid):

- BL-359 (`specs/features/BL-359-always-on-operator-presence.feature`):
  soft re-run soft-skipped on the unchanged-text stamp (BL-460 shape,
  not a failure); `--level full` forced a real re-mutation of the one
  Scenario Outline (scenario 04, mishap examples) — 2/2 mutants killed,
  0 survived, fresh `tested_at`.
- BL-368 (`specs/features/BL-368-control-loss-is-not-agent-death.feature`):
  has no `Scenario Outline:` anywhere in the file (both remaining
  scenarios are plain `Scenario:`), so the run is `outcome: inapplicable`
  (BL-638) by construction, not a real pass — recorded as such, with a
  fresh `tested_at` on the inapplicable stamp. No hand-authored fallback
  sweep: the parcel's change here is a pure scenario deletion with no new
  production logic (mutation_cost: low, confirmed at mint).

Full qa_e2e_procedure re-verified independently of the coder's own run:
- `run_acceptance.sh` on both features: BL-368 3/3, BL-359 6/6, both
  `# fail 0`.
- `ls specs/features/BL-647-*.feature specs/pipeline/steps/bl647*Steps.js`
  confirms both absent; `grep -rln "dead-agent liveness sweep runs"
  specs/features` empty.
- `grep -rn dead-agent-events swarmforge/scripts specs/pipeline` hits
  only dated comments in the two shell test files that now assert
  BL-653's zero-event behaviour.
- `grep -n "rotation-mode\|rotation-opts" swarmforge/scripts/operator_runtime.bb`
  shows only the unrelated `prev-conf` site at line 1381 — the two dead
  bindings are gone.
- `test_operator_runtime_control_lost.sh`,
  `test_operator_runtime_bl647_rotation_liveness.sh`,
  `operator_lib_test_runner.bb`: ALL CHECKS PASSED / ALL TESTS PASSED.
- `bl648_relaunch_resume_property_runner.bb` (comment-only diff dropping
  the reference to the deleted bl647 property runner): ALL PROPERTIES
  HOLD, unchanged.
- `extension`: `npm run compile` clean (no TS production change in this
  parcel — the coder's only extension-tree addition is the new property
  test file); the new
  `bl1514DeadAgentEventsRetiredInvariants.property.test.js` passes 4/4
  under `vitest.properties.config.mjs`.
- Whole-tree guard sweep (parcel touches `specs/pipeline/steps/` —
  deletion — and `extension/test/` — new property file): all 17
  `*Guard*.test.js` files green except `tempDirTrapGuard.test.js`, whose
  two violations (`landed_ticket_autoclose_test_runner.bb`,
  `wake_dedup_lib_test_runner.bb`) are unrelated pre-existing debt from
  the 2026-09-09 hotfixes, already ticketed as BL-1501 (confirmed by
  grep before treating as a defect, per the standing-red presumption
  rule).
- No CRAP/DRY tooling applies: the only production files touched
  (`operator_lib.bb`, `operator_runtime.bb`) are Babashka, which has no
  mutation/CRAP/DRY wired (BL-472); `jscpd` over both confirms it finds
  nothing to analyse (not a `.js`/`.ts` target). No `src/*.ts` production
  file changed.
- Host quiet throughout (`uptime` 1.29-1.34 on 20 cores); mutation
  cooldown gate returned `run` for both `.bb` files; no orphaned
  node/stryker processes before or after; my own gherkin-mutation
  workdirs under `./tmp/bl1514-gherkin-mutation.*` removed after use.

By hardender.
