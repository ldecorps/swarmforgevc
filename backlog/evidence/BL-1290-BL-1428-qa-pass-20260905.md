# QA pass — BL-1290 and BL-1428 (batch commit) — 2026-09-05

Commit under review: HEAD after merging architect's `fe50d79aa2` into
`swarmforge-QA`, carrying both tickets' full chains
(`90d463f4da` BL-1290 coder, `7a34298555` BL-1428 coder — both confirmed
ancestors via `git merge-base --is-ancestor`). Routed architect→QA per
`stage_skip_reasons` on BL-1290 (cleaner/hardener/documenter skipped with
stated, checked-out reasons); BL-1428 ran the full chain.

## BL-1290 — a socket fixture is rooted short enough to bind

- `npx vitest run test/socketFixtureShortRootGuard.test.js`: 16/16 pass
  (was the standing red naming both violating step files).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1290-a-socket-fixture-is-rooted-short-enough-to-bind.feature`
  — 4/4 scenarios pass, including scenario 04 ("a converted fixture's
  socket path fits the length limit") — the ticket's own point that a
  green Linux run proves nothing; this scenario measures the path length
  directly against the limit rather than relying on a platform-dependent
  bind.
- Full unit lane (`npm test`): `socketFixtureShortRootGuard.test.js`
  confirmed **absent** from the failing set (14 files now, down from 15
  before this ticket).
- `required_wiring`: `specs/pipeline/steps/bl1290SocketFixtureShortRootSteps.js`
  exports `registerSteps`.

## BL-1428 — every standing red names an open owner

- `bb swarmforge/scripts/test/standing_red_register_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/bl1428_every_standing_red_names_an_open_owner_property_runner.bb`:
  ALL PROPERTIES HOLD (200 runs/property).
- `bash swarmforge/scripts/test/test_run_commit_guards.sh`: ALL PASS
  (12/12), confirming the new guard is wired into the cheap tier without
  disturbing the existing chain's behavior, ordering, or refusal
  reporting.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1428-every-standing-red-names-an-open-owner.feature`
  — 7/7 scenarios pass, including scenario "every row in the live
  register names a ticket that is open" (checks `backlog/standing-reds.tsv`
  specifically, per its own Gherkin text — not the hardening-debt ledger).
- Ran `bb swarmforge/scripts/standing_red_register_cli.bb .` directly:
  confirmed it correctly flags 5 hardening-ledger rows as `owned: false`
  (BL-620, BL-955, BL-954, BL-956 — all four verified independently by me
  to be in `backlog/done/M8/`) — this is the tool doing exactly what it's
  for, not a defect; the ticket's own invariant 2 makes clear pre-existing
  stale rows never refuse a commit, they only get named (the throttle's
  BL-1429 concern, out of this ticket's scope).
- Allowlist cleanup confirmed: `grep -c BL-1175
  property_suite_standing_allowlist.tsv` = 0 (was 25 rows citing the
  closed ticket); file is now 21 lines (was ~26), consistent with 5 green
  rows removed.
- Wiring confirmed: `grep -n check_standing_red_register.sh
  run_commit_guards.sh` — present in the cheap-tier guard chain (line 84).
  `specs/pipeline/steps/bl1428StandingRedRegisterSteps.js` exports
  `registerSteps`.
- No merge conflicts (a `<<<<<<<` grep hit was literal test-fixture text
  in an unrelated file, `test_handoffd_push_sweep_wiring.sh`, confirmed
  pre-existing and unrelated).

## Whole-suite gates

- Orphan check before/after: `pgrep -fl 'node --test|stryker'` empty both
  times.
- `npm test`: 14 files / remaining failures all pre-existing, already
  registered in the standing-red register itself (BL-1229, BL-1221,
  BL-1212, BL-1265, BL-1263, BL-1289) or its unowned-hardening-ledger set
  — ironic but correct: this very ticket's register is what now formally
  tracks them.

## Verdict

Both tickets satisfy their acceptance criteria and required_wiring.
APPROVED — merging up and landing.
