# BL-1704 — documenter review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

Third documenter pass, QA bounce D1 (own domain). Fixed:
1. The `external` bullet claimed the stop log names the server "(pid,
   endpoint)" — an external record carries no pid at all
   (`ollama_ancillary_write_record` writes `"pid": null` for that owner),
   and the actual log line (`ollama_ancillary_lib.sh` line 213) names only
   the endpoint. Reworded to "endpoint only".
2. The launch section's closing paragraph still called BL-1704 and BL-1705
   "not-yet-documented" — stale since pass 1 documented BL-1704 in this
   very file. Rewrote it to point at that section and at
   `docs/reference/Specification.MD`'s BL-1705/BL-1726 entries (the
   ghost-reaper classification, narrowed by BL-1726).

Checks: BL-1704 feature 5/5 (`specs/pipeline/cli.js`); `npm run compile`
clean.

By documenter.
