# BL-988 QA bounce 7 — 20260825

**Reviewed tip:** documenter `0c41f334a2` (not merged — tip impure)
**Task:** `BL-988-orphaned-wsl-acceptance-contract-has-no-step-handlers`
**Sibling:** `VERIFY BL-988`
**Prior:** bounce6 `b49c41b9d`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 0c41f334a2  # fails
comm -12 … | wc -l  # → 8
```

**Commit hash checked:** `0c41f334a2`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = f860b6fc8 BL-1124 QA pass)
paths=33 dels=8
deleted: BL-1124 active ticket + rematch/hardener evidence paths
(hardener pass present on tip — purity only)
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto current `origin/main` keeping landed
BL-1124. Observed tip that deletes BL-1124 paths.

**Remediation:** `git fetch && git reset --hard origin/main`; restore BL-988
only; keep all BL-1124 paths; dels=0; verify ancestry before handoff.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
