# BL-1442 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1442`: `VERIFY BL-1442` (no open
  deferral).
- Orphan process check before/after: `ps aux | awk '/[n]ode --test|[s]tryker/'`
  empty both times.
- "Landing the brief" step: `git merge-base --is-ancestor 98e0a7e817 HEAD`
  true — the art director's tip was already landed earlier this shift (the
  `land art-director tip eb7b5cafe5` note), so this parcel's own in-parcel
  merge step is the documented no-op (ticket notes, 2026-09-06 addendum).
- `bb swarmforge/scripts/test/briefing_email_test_runner.bb`: ALL PASS.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1442-briefing-list-item-leading-ticket-id-is-bold.feature`
  — 11/11 scenario runs pass. Sibling features stay green: BL-393 (5/5),
  BL-1419 (8/8).
- `bb swarmforge/scripts/test/bl1442_briefing_bold_leading_ids_property_runner.bb`:
  ALL PASS (80/80 cases).
- Fixture-leak fix (hardener): ran the acceptance feature twice in a row —
  zero leftover `aps-bl1442-briefing-bold-*` dirs under `/tmp` after either
  run (hardener's fix confirmed).
- Full unit lane (`npm test`, extension/): 610/610 files, 10283/10283
  tests green. Same chronic suite-duration/per-file budget overage as
  other recent parcels — none of the four flagged slow files touched by
  this diff.
- Full property lane (`npm run test:properties`): see "Not bounced" below
  for the one file that failed on the first pass; 363/364 files green on
  that run (1086/1087 tests), and 364/364 (1087/1087) confirmed via the
  isolated re-runs below — not a BL-1442 defect.
- Wiring anchor 1: `bold-leading-ticket-ids` defined at
  `briefing_email_lib.bb:490`, called at line 548 inside
  `render-briefing-html` as `(style-inline-elements (bold-leading-ticket-ids
  body-html))` — before the existing `"strong" "font-weight:600"` entry
  runs, per the ticket's "How".
- Wiring anchor 2: `bl1442BriefingListItemLeadingIdBoldSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`
  (BL-1371 posture).
- Invariants confirmed by direct code read of `leading-label-bold-spans` /
  `wrap-bold-spans` / `bold-leading-ticket-ids-in-li`: ENOENT — not
  applicable here; confirmed instead that only `<li>...</li>` content is
  ever touched (regex anchors on `<li>`/`</li>`), the label walk consumes
  ticket ids (`^(?:BL|GH)-\d+`), whitespace, `and`, `/,+` and `&amp;`, and
  one parenthesised aside — producing bold SPANS only for the ids
  themselves, never the aside content or trailing prose — traced by hand
  against all four worked examples in the ticket text (`BL-1386 and
  BL-1387:`, `BL-1413 (active) is`, the `BL-1398 (...) and BL-1401 (BL-632
  ...)` aside case, `BL-1374: ... credited BL-1385's replay`); the
  `"strong" "font-weight:600"` style-map entry (pre-existing, BL-1419)
  applies weight-only, no colour.
- Live render (qa_e2e_procedure items 2-4): loaded `markdown_to_html_lib.bb`
  + `briefing_email_lib.bb` directly and called `render-briefing-html`
  against `specs/pipeline/steps/fixtures/BL-1419-2026-09-05-briefing.md`
  (same recipe BL-1419's QA pass and the art director's own BL-1419 review
  used) to `tmp/briefing.html`:
  - `grep -o 'font-weight:600">BL-' tmp/briefing.html | wc -l` → 23 (note:
    `grep -c` undercounts on this single-line HTML blob — counts matching
    LINES not occurrences; corrected with `-o | wc -l`).
  - `grep -c '<style'` → 0.
  - BL-1385's leading mention: `<strong style="font-weight:600">BL-1385</strong>:`;
    its mid-sentence second mention ("as BL-1385's own new guard") stays
    plain text — confirmed both in the same rendered output.
  - "BL-1386 and BL-1387" both individually wrapped in `<strong>`, "and"
    between them left plain.
  - "Smaller fixes:" paragraph (a `<p>`, not `<li>`) unchanged — its
    pre-existing `<strong>` (from the source markdown's own `**...**`) is
    untouched, and the ticket ids that follow it in the SAME paragraph
    (BL-1358, BL-1377, BL-1353, ...) stay regular, confirming paragraphs
    are never touched.
  - Plain-text part: rendered markdown written to `tmp/briefing.txt`,
    `cmp` against the fixture — byte-identical. `git status` on
    `docs/briefings/` and `specs/pipeline/steps/fixtures/` — clean, no
    briefing file modified.
- **Sign-off (Article 1.10, qa_e2e_procedure item 5)**: sent art-director a
  `note` (priority 50): "BL-1442 sign-off: render e3217ba5cb vs BL-1419
  fixture, LGTM?". Reply received: **"BL-1442 LGTM - leading id bold,
  asides untouched, no color"** — independently confirms the same three
  points verified above. Recorded here per Article 1.10 ("your verdict is
  evidence QA records").
- Lineage: this ticket's hardener merge (`11ef5be5de`) is an ancestor of
  the citing commit (`e3217ba5cb`) — `git merge-base --is-ancestor`
  confirmed.
- Docs: `docs/reference/Specification.MD` documents `bold-leading-ticket-ids`
  and its call site. No diagram trigger fires (ticket says so; confirmed).
- Out of scope respected: no colour/size/element added (weight only, by
  code read); no paragraph/heading/blockquote touched; no other artifact
  finding folded in.

## Not bounced (observed, not this ticket's defect, not reproducible)

`npm run test:properties`'s first full run failed one file on a domain
this ticket does not touch:

- `test/bl1218RemoteControlConfigInvariants.property.test.js` — "property
  (invariant 2): a persisted launch script never disagrees with the config
  that wrote it" failed with an `edamame`/EDN parse error ("EOF while
  reading, expected \" to match \"") inside a `bb`-generated source file —
  a fast-check-generated input apparently produced a string that broke a
  generated Clojure/edn literal. Passed cleanly in SIX subsequent isolated
  re-runs (`npx vitest run --config vitest.properties.config.mjs
  test/bl1218RemoteControlConfigInvariants.property.test.js`, run once then
  five more times), 4/4 tests every time. No ticket found (`grep -rl
  bl1218RemoteControlConfigInvariants backlog/` finds only BL-1218's own
  original build/QA evidence, nothing describing this failure; not in
  `backlog/standing-reds.tsv`). Flaky by direct evidence (fail-once,
  pass-six), unrelated domain (remote-control launch-script/config
  invariant vs. briefing-email rendering) — same disposition as BL-1440's
  "Not bounced" section, which is how BL-1443 itself was surfaced.
