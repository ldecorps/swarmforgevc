# BL-1761 — cleaner review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- Merged coder 3eeabaf104 into cleaner (real fix commit 9b4d33a27e; the
  handoff sha itself was a main-sync merge on the coder branch).
- Diff scope confirmed: `extension/scripts/recordTestDuration.js` (extracted
  pure `resolveConfirmedFile(file, rootDir, repoRootDir)`, exported it),
  `extension/test/bl1761ConfirmPoleAloneSandboxInvariant.property.test.js`
  (new), `specs/pipeline/steps/bl1761ConfirmPoleAloneSandboxSteps.js` (new),
  and the coder's evidence file — matches the ticket's Scope exactly. No
  sibling-link lib touched, no other `path.join(ROOT_DIR, '..')` site
  touched (both correctly out of scope).
- Fix reviewed: a leading `extension/` segment now resolves against
  `rootDir` itself instead of `repoRootDir`, which is sandbox-neutral per
  BL-1066 (the sandbox already IS the extension root); every other
  relative/absolute path is unchanged. Matches the declared invariant.
- `node specs/pipeline/cli.js specs/features/BL-1761-...feature`: 2 of 2 ok.
- `npm run compile` (extension/): clean.
- `npx vitest run test/confirmPoleAloneOutcomeShapes.test.js`: 3 passed
  (mapped unit test for the changed production path, Article 4.5).
- `npm run test:properties` lane, scoped to just the one new
  `*.property.test.js` this parcel added
  (`vitest run --config vitest.properties.config.mjs
  test/bl1761ConfirmPoleAloneSandboxInvariant.property.test.js`): 1 passed
  — the one-exception case (a property test this parcel itself added), run
  once, not the full property lane.
- `mutation-site-count.js` on the changed file: 112 sites, `over` the
  100-site BL-485 advisory threshold (by 12). Not split: this is a small
  (252-line) single-purpose CLI script; the fix already extracted the new
  logic into its own pure, directly-testable function
  (`resolveConfirmedFile`), which is the structural improvement BL-485
  wants — a further mechanical split of the rest of the script for a
  targeted 2-function bug fix would fragment a cohesive module for no
  separation-of-concerns gain. Left whole as the correct call.
- Architecture: `resolveConfirmedFile` is pure (no IO), takes its roots as
  parameters rather than closing over module-level constants, so it is
  directly testable against both shapes without a second copy of the file
  — sound separation, nothing to flag.

By cleaner.
