# BL-1385 — documenter pass (2026-09-04)

## Received
`87275f1666` (hardener, degraded Babashka fallback for `.bb`/shell pieces,
Stryker-gated for the TypeScript resolver if the coder chose shape (b);
9 killed/0 survived mutation sweep per
`backlog/evidence/BL-1385-hardener-pass-20260904.md`).

## What changed, operator/repo-visible
A new pre-land / pre-commit guard, `check_handler_module_graph.sh`, closes
a real gap in BL-1371's discovery mechanism: discovery fails loud at
RUNTIME on an unloadable handler, but nothing previously stopped such a
handler from reaching `main` in the first place — exactly what happened
2026-09-04 (a hand-landed handler whose require resolved only in the
checking worktree took every acceptance run on `origin/main` to zero
runnable features).

## Doc-domain review
- `docs/how-to/BL-1371-step-handlers-register-by-discovery.md`: added a
  bullet under "What this means for a new ticket" describing the new
  proactive guard and both wiring points, distinguishing it from
  discovery's own runtime-only guarantee.
- `docs/how-to/BL-1252-commit-guard-chain-reports-every-violation.md`:
  the "four guards" enumeration was already stale before this ticket
  (BL-1303's `check_feature_handler_registration.sh` was never added to
  the prose list even though it's in the script) and my own ticket adds a
  fifth — rewrote the Tier 1 list to name all five current guards
  accurately rather than leaving a doubly-wrong count.
- `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`:
  that doc's own "adding a second tree guard later is one list entry, not
  a second call site" line predicted exactly this — added one sentence
  confirming `check_handler_module_graph.sh` is that second guard.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: refreshed the BL-1371 and BL-1252 summary lines.
- No new dedicated how-to page: this ticket's natural home is the two
  existing mechanism pages it directly extends (discovery, the commit
  guard chain), matching this session's established pattern for
  extension/refinement tickets over a shared mechanism.
- Diagram registry checked — no diagram depicts step-handler-registration
  or commit-guard internals at this granularity.

## Verdict
Doc content corrected/extended in three existing how-to pages (one beyond
this ticket's own scope — a preexisting staleness in BL-1252's guard count
that this ticket's own addition made doubly wrong, fixed in the same
pass) plus the standard Specification.MD/index.md entries. Forwarding to
QA.
