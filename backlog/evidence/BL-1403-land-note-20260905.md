# BL-1403 land note — the fix for this session's own recurring block, 2026-09-05

## Significance

This is the ticket for the exact `check_merge_deletion.sh` defect this
session's own QA passes worked around repeatedly (BL-1364, BL-1365,
BL-1383, BL-1402, and every routine "merge up" reconcile in between) via
hand-built replays and cherry-picks. Verified with that direct
operational experience in mind, same as the coder/cleaner/architect/
hardener's own evidence records.

## Merge into QA worktree

A plain `git merge c08fcfbe81` succeeded cleanly — no entanglement, no
conflicts. The documenter's own doc-only commit for
`docs/how-to/BL-1242-merge-deletion-guard.md` already used an untagged
subject (learned from this exact session's own experience with the
defect, per the documenter's own evidence), so no cross-ticket-doc-file
issue on the merge side.

## Verification (before landing)

Full independent QA pass: `bash -n check_merge_deletion.sh` clean.
`test_merge_deletion_guard.sh` 17/17 (16 scenarios + the hardener's own
strengthened scenario 16, which found a real gap — the diagnostic commit
shown for a doubly-unattributed deletion was never asserted on).
Property runner 60/60, ALL PROPERTIES HOLD, coverage across both move
variants and all four head/incoming ticket-naming combinations.
Acceptance `BL-1403-...feature` 6/6, `BL-1242-...feature` (regression,
original guard) 12/12 unchanged. Read the actual fix directly in the
source rather than trusting the evidence: confirmed `collect_deletions`
now runs `git diff --name-status -M` (rename detection at git's default
similarity, so a move reports `R<NNN>` and the existing `status == "D"`
filter already excludes it); confirmed `attribution_for_path` (the
merged `ticket_id_for_path`/`introducing_commit_for_path`) falls back
from HEAD to `MERGE_HEAD` whenever HEAD's subject yields NO ticket id —
not only when the subject is empty — and prints `commit<TAB>id` (never
`id<TAB>commit`, per the documented bash `read` leading-empty-field
gotcha). BL-1242's original protection (an inherited, unattributable
deletion still refuses) is unchanged — confirmed both by the regression
feature and by reading that the neither-side-names-a-ticket branch still
returns an empty id.

## qa_e2e step 5 (live) — satisfied by the shell test suite's real git operations, not separately re-derived

The ticket's own step 5 ("in any worktree behind main after this
ticket's mint commit, `git merge origin/main` with the default subject
is allowed") is exactly what this session's own QA worktree has been
doing successfully since this ticket's fix landed in my own tree via the
merge above — every "merge up" reconcile since has gone through cleanly
with a plain, unticketed message (confirmed: no cherry-pick workaround
needed for any merge in this session after this point). Did not
hand-construct a separate live fixture beyond that: the shell test suite
(scenarios 07/08/14/15/15b/16) already exercises the exact mechanism
through REAL `git merge --no-ff` operations with the commit-msg hook
installed, not mocks — which is the qa_e2e fixture-based half (steps
1-4) already thoroughly proven, and this session's own subsequent
merge-ups are the live confirmation of step 5 in practice.

## Land

`task_scope_gate_cli.bb BL-1403-... a38d878b71` returned `OK`. This land
also touches a cross-ticket doc file
(`docs/how-to/BL-1242-merge-deletion-guard.md`, named for BL-1242) —
applying the BL-1413 lesson proactively again: split into `706e8ad5bc`
(tagged, everything else) + `6118c821d2` (untagged `docs: ...`, the
BL-1242 how-to only). Built off origin/main in a throwaway
`git worktree add --detach <tmp> origin/main` — zero conflicts on all 12
files. Confirmed every file byte-identical to the QA-worktree's reviewed
content. Symlinked `extension/node_modules`/`extension/out` (this
ticket touches no TypeScript, but the acceptance runner loads every
other registered step handler too). Re-verified shell suite, property
runner, and acceptance green against that tree; removed both symlinks
before committing. `git diff --stat origin/main HEAD` (both commits
together) showed exactly the 12 intended paths.
`land_main_publish.sh --decide-only` returned `:next :push`,
`:origin-advanced-since-gate false`. Pushed `097dfe2248..6118c821d2`.
Lock acquired/released around the push per BL-1144.

## Land-approval records (BL-1334) — both recorded from the start

```
{"at":"2026-09-05T07:46:06Z","ticket":"BL-1403","commit":"706e8ad5bc","source":"a38d878b71"}
{"at":"2026-09-05T07:46:06Z","ticket":"BL-1403","commit":"6118c821d2","source":"a38d878b71"}
```
`source` for both is `a38d878b71` — the QA-worktree's own clean merge
commit this pass reviewed and approved (an ancestor of `swarmforge-QA`).
Verified `is_qa_ancestor.sh` prints `approved: ... - BL-1334` for both.

`abandoned_commits: [a38d878b71]` recorded on
`backlog/active/BL-1403-the-merge-deletion-guard-never-reports-a-move-and-never-refuses-unexemptably.yaml`.

BL-1403 is not `GH-`-seeded; no `issue_done.sh` step applies.

By QA.
