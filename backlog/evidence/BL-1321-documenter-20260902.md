# BL-1321 — documenter pass, 2026-09-02

Reviewed commit `8eb14780a3` (hardener clean sweep), merged into documenter.
Review-only BL-848 stamp-off parcel: no production wiring touched — the
reviewed hotfix `3d70c0f4ec` already landed on `main` independently, outside
this parcel.

## Doc sweep

- Followed the established stamp-off pattern (BL-1113, BL-1117, BL-1116,
  BL-1324, BL-1254, BL-1283): added a BL-1321 entry to
  `docs/how-to/BL-848-certify-an-operator-hotfix.md`'s "Related" list
  (commit `e1edd72577`), naming the reviewed commit, what it does, and the
  two reported-not-acted-on findings (the CRLF→LF re-line-ending and the
  byte-identical duplicate assert) so a reader of the ledger doc knows they
  exist without digging into the evidence chain.
- Checked whether the chase-rotate seated-preferred-yield behavior is
  described elsewhere in `docs/`: no existing how-to covers
  `mono_router_lib.bb`'s chase-rotate decision specifically, and this parcel
  introduces no new user-visible command or setting of its own — ticket
  constraints forbid redescribing/redesigning the hotfix here, consistent
  with the prior stamp-off passes today (BL-1324/BL-1254/BL-1283).
- No diagram registered under `docs/diagrams/` depicts the chase-rotate
  decision internals; none of the four registered diagrams' change-triggers
  fired. No diagram update owed.
- `docs/reference/Specification.MD`: not touched — same reasoning as the
  other stamp-offs today (review-only parcel, no new user-visible behavior
  of its own).
- No RETIRED/deprecated behaviour involved — Article 3.6 not implicated.
- Confirmed doc-only diff and ledger unchanged: `git diff 8eb14780a3..HEAD
  --stat` touches only `docs/` and `backlog/evidence/`; the `3d70c0f4ec`
  ledger row (created fresh by the coder's `--new`/`--link` at mint) still
  reads `state: stamp-open`, `human_decision: null`.

## Verdict

NONE — no documentation defect found. Forwarding to QA.
