# Evidence — BL-1441 — coder pass (2026-09-06)

Role: coder. Ticket: BL-1441-the-four-remaining-0819-hardening-gates-are-run.
Routed by coordinator note (`Work BL-1441-the-four-remaining-0819-hardening-gates-are-run`).

## Eligibility checked per row before doing anything (the ticket's own "How")

`mutation_cooldown_gate.bb` run against every file in all four ledger rows,
individually:

| row | files | decision |
|---|---|---|
| BL-620 | telegram-front-desk-bot.ts, telegramFrontDeskBotCore.ts, telegramTopicDecisions.ts | **skip-cooldown** (first two, 0.76 days) |
| BL-955 | negotiationTelegramRelay.ts, negotiationTelegramRouting.ts, telegramFrontDeskBotCore.ts | **skip-cooldown** (shares telegramFrontDeskBotCore.ts, 0.76 days) |
| BL-954-a-bounce-verifies-its-own-revert | bounceRevertGitAdapter.ts, bounceRevertVerdict.ts, record-bounce.ts | **run** (9.44/9.44/17.93 days — none touched by BL-1425) |
| BL-956-pipeline-board-caption-and-cap-hotfix | pipelineBoard.ts | **skip-cooldown** (0.74 days) |

Host load quiet throughout (2.05-2.62 vs a 40 busy threshold, 20 cores).
`extension/test/constitutionDocCitations.test.js`: 6/6 (BL-1440 landed) —
BL-954's OTHER blocker (Stryker's dry-run precondition) is also clear.

**Only BL-954's row is eligible today.** The ticket's own "How" section says
"Do not start before 2026-09-08" — written when all four rows shared at
least one active blocker (BL-1440 had not landed yet at mint time either).
That section is explicitly "direction, not mandate"; the actual gate is
`mutation_cooldown_gate.bb`'s live decision, checked per row as directed,
and BL-954's decision is `run`, independent of the cooldown that still
holds the other three until 2026-09-08. Ran it; declined the other three
(already correctly attempted under BL-1441 by a prior pass — see below).

## What ran: BL-954's mutation gate, discharged

Full run detail, every survivor named and reasoned:
`backlog/evidence/BL-1441-BL-954-a-bounce-verifies-its-own-revert-mutation.md`.
Summary: 191 mutants, 166 killed, 25 survived, 0 timeout, 0 errors
(86.91% mutation score). Discharged via
`hardening_debt_ledger_update.bb --discharge BL-954-a-bounce-verifies-its-own-revert mutation --evidence <path>`.
Its `backlog/standing-reds.tsv` hardening row removed in this same commit
(the register is a separately-maintained file, never auto-synced to the
ledger's discharge status — confirmed by re-reading the register after the
discharge: it still named BL-954 until the row was removed by hand).

Two real prerequisite defects found and fixed en route (detailed in the
BL-954 evidence file above, not repeated here): `ensureStrykerSandboxSiblings.js`
had no `backlog/` sibling link (masked until now - every prior full-suite
Stryker dry run was blocked earlier by cooldown or the citation red before
reaching the test that needed it), and `test/activePoolFreshnessAudit.test.js`'s
documented "exclude this file when running Stryker" remedy was never
actually wired for a whole-repo dry run - fixed the same way
`vitest.bl1081/bl1365/bl1383/bl1402.stryker.config.mjs` already do for
their own scopes (`vitest.bl1441.stryker.config.mjs`,
`stryker.bl1439.config.json` repointed at it).

## What did not run: BL-620, BL-955, BL-956, still genuinely blocked

Cooldown does not clear until 2026-09-08. Already recorded as attempts,
naming BL-1441, by a PRIOR pass on this ticket (visible in the live ledger
before this session touched anything - `attempted_at: "2026-09-06"` on all
three, each `attempted_blocker` naming the exact cooldown decision). Nothing
to add; re-attempting today would produce the identical, unchanged decision.
Their `backlog/standing-reds.tsv` rows are untouched - genuinely still
outstanding.

## Ticket completion status: NOT complete, correctly

`specs/features/BL-1441-....feature` scenario 01 requires EVERY 2026-08-19
row discharged and the register's hardening lane empty; scenario 02's
Examples name all four parcels. Ran live against the current repo state:

- Scenario 01: **fails**, naming exactly the three genuinely outstanding
  rows (`BL-620/mutation`, `BL-955/mutation`,
  `BL-956-pipeline-board-caption-and-cap-hotfix/mutation`) - not BL-954,
  correctly excluded now that it is discharged.
- Scenario 02: **BL-954's Example passes** (real, complete evidence);
  the other three **correctly fail**, each error naming the live
  `attempted_blocker` text - not a defect in the handler, the ledger
  genuinely has no discharge evidence for them yet.

This is the SAME shape BL-1439's own coder evidence recorded for its own
partial acceptance run: forcing this feature green today would mean either
discharging a gate that did not actually run, or writing an assertion that
lies about the ledger's real state. Neither was done. **This parcel is
committed but NOT forwarded** - the ticket's own acceptance criteria
cannot be met until the cooldown clears on 2026-09-08, and sending an
admittedly-incomplete parcel through cleaner/architect/hardener/documenter/QA
now would review work that is not finished. Real, valid progress (one of
four rows genuinely discharged, two masked infrastructure defects fixed)
sits on this branch; the remaining three rows are this same ticket's to
finish once eligible.

## Invariants (BL-654) — both admit no new encoding in this parcel

1. *"A ledger row leaves the outstanding debt only through a discharge
   that names the gate, the parcel and a committed result; a run that
   cannot complete is recorded as an attempt and stays outstanding, never
   discharged by assertion."* This is the ledger MECHANISM's own
   contract, built and property-tested by BL-1439/BL-942
   (`bl942_hardening_debt_ledger_property_runner.bb`, re-run this pass:
   **ok**, unmodified). This parcel is a CONSUMER of that mechanism
   (`--discharge`/`--attempt` via the CLI), not a modifier of it - no
   ledger code changed here. Writing a second property test asserting the
   same mechanism BL-1439's own suite already covers would be a duplicate
   test of code this parcel does not touch, not a new encoding of
   anything this parcel adds.
2. *"The register row of a gate leaves in the same commit that discharges
   it, never earlier; the hardening lane is empty only when the ledger
   owes nothing from 2026-08-19."* This quantifies over a GIT WORKFLOW
   discipline (which lines a commit's diff contains, together, in one
   commit) - process, not a pure module a property test can exercise
   (BL-654's own carve-out: "record a stated reason... when a declared
   invariant admits no executable encoding"). Honoured directly instead:
   BL-954's `standing-reds.tsv` line removal is staged in the SAME commit
   as its ledger discharge and evidence file (verified below).

## Verification

| check | result |
|---|---|
| `mutation_cooldown_gate.bb` × 4 rows (all files) | matches table above |
| `constitutionDocCitations.test.js` | 6/6 |
| `bl942_hardening_debt_ledger_property_runner.bb` | ok, unaffected |
| `test_hardening_debt_ledger_cli.sh` | ALL CHECKS PASSED, unaffected |
| `test/strykerSandboxSiblingsLib.test.js` | 21/21 |
| `test/hardenerTooling.test.js` | 5/5 |
| `test/bl643NonPipelineAgentsStepsGuards.test.js` | 7/7 |
| `test/bounceRevertCheck.test.js`, `bounceRevertRestoration.test.js`, `recordBounceCli.test.js` (unit lane, unscoped) | 82/82 |
| `npx stryker run stryker.bl1439.config.json` | 191 mutants, 166 killed, 25 survived — see the parcel-evidence file |
| `bb hardening_debt_ledger_read.bb .` (live) | BL-954's row shows `discharged_at`/`discharged_evidence`; the other three still show `attempted_at`/`attempted_blocker`, `discharged_at: null` |
| `bb standing_red_register_cli.bb .` (live) | hardening lane: 3 rows (BL-620, BL-955, BL-956) - BL-954 correctly absent |
| Acceptance `BL-1441-....feature` | 1/5 (scenario 02's BL-954 example) - the rest correctly fail, naming the genuinely-outstanding rows; see "Ticket completion status" above |

## Scope

Nothing in `out_of_scope` was touched: the cooldown gate and its window are
untouched; BL-1440's own fix is untouched (only relied upon, already
landed). No attempt to force BL-620/955/956 to run early or to
force-discharge anything. `extension/docs/briefings/2026-09-04.json`
remains untracked and was not created by this parcel.
