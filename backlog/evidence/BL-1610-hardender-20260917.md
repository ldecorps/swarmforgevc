# BL-1610 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Reverted a stray uncommitted edit found in the worktree at pass start:
  `excused-by-blob-identity?`'s `and` had been changed to `or`, contradicting
  the function's own docstring. Not part of any commit; restored via
  `git checkout -- swarmforge/scripts/merge_drop_guard_lib.bb` before any
  other work.
- `bb swarmforge/scripts/test/merge_drop_guard_lib_test_runner.bb`: green
  before and after adding new tests.
- `bb swarmforge/scripts/test/bl1576_merge_drop_guard_property_runner.bb`:
  2000 runs, ALL PROPERTIES HOLD.
- `bb swarmforge/scripts/test/review_forward_evidence_gate_lib_test_runner.bb`:
  green before and after adding new tests.
- `bb swarmforge/scripts/test/review_forward_evidence_gate_lib_property_runner.bb`:
  green.
- `bb swarmforge/scripts/test/ready_for_next_claim_task_name_runner.bb`:
  green before and after adding new tests.
- Acceptance: `specs/features/BL-1610-*.feature` 5/5 green;
  `specs/features/BL-1576-*.feature` 11/11 green (unchanged, per constraint).
- Gap found and closed: the coder's own bb runners were never extended for
  BL-1610's new logic (only the acceptance suite exercised the head-bound
  scan, `excused-by-blob-identity?`, `blocking-findings`, and
  `received-at-head-for-task` — all through a slow real-git-subprocess
  path). Added hand-authored unit tests directly against the pure/public
  API in the same three runners, each verified via hand-mutation to
  actually kill the mutant it targets (commit
  `648e7bc93e`): the head-bound-scan test, the excused-by-blob-identity AND
  vs OR truth-table (cases A/B/C), `received-at-head-for-task`'s
  present/absent/blank/wrong-task shapes, and `current-head-commit-10`'s
  exit-check.
- CRAP/DRY: not applicable — Babashka, no mutation/CRAP/DRY tool wired
  (BL-472); mutation_cost: low, hand-authored sweep per BL-638/BL-567.
- Unrelated finding, not this ticket's scope: running the standing
  whole-tree guards (`specs/pipeline/steps/` was touched by this parcel's
  own new step-handler file) surfaced a pre-existing violation in a
  DIFFERENT ticket's file, `bl1605NoReverseCopyToForwardRecipientSteps.js`
  (BL-1605, still active, not on main). Reported via a priority-00 note to
  the specifier with evidence
  (`backlog/evidence/BL-1605-hardender-socket-fixture-root-followup-20260917.md`),
  not folded into this commit (ticket-scope discipline).
- No orphaned test/mutation processes: `pgrep -fl 'node --test|stryker|vitest'`
  clean before and after.
