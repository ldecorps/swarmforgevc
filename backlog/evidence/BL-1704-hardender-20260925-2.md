# BL-1704 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second same-day hardener pass, after QA's bounce (D1: the stale-record
stop-log line folded "pid gone" and "pid recycled by another process"
into one message; D2: documenter's stale doc wording). Coder's fix
(`ff00492f27` / D1) splits the log line on `ollama_ancillary_pid_alive`
(confirmed: lines 223/225 of `ollama_ancillary_lib.sh` now read
"... now belongs to <cmdline> ..." vs "... is gone ..." respectively)
and re-passed clean through cleaner and architect
(`BL-1704-cleaner-20260925-3.md`, `BL-1704-architect-20260925-3.md`).

**Restored a real regression before this pass could forward it (not a
bounce — my own domain, fixed directly, see the separate commit
`d8264e11ce`):** the coder's fix commit explicitly restored
`ollama_ancillary_stop_swarm_owned` and its call sites from ORIG_HEAD
after QA's bounce-revert (`c5898b541c`) silently deleted this branch's
own BL-1704 content — but that restoration sweep was scoped to
production call sites, not test files, so my own earlier hardening
test (`test_kill_all_swarm_ollama_failure_not_fatal.sh`, from
`acf853e621`) fell through, absent from the lineage that reached me
with neither cleaner nor architect (both "NONE" this round) flagging
it. Verified before restoring: not a duplicate of D1/D2's own scope;
still passes unmodified against the current D1-fixed code; the rest of
my original commit's diff (the feature file's mutation-manifest header
aside) is otherwise intact. Restored the file and its suite-manifest.tsv
entry verbatim. Sent a `rule_proposal` (accepted) generalizing this and
the sibling BL-1698 doc-regression finding below.

**Documentation regression found, NOT fixed here (documenter's own
content) — flagging explicitly:** the SAME collateral-damage class hit
`docs/how-to/BL-1052-local-model-seat-launch.md` even harder than
BL-1698's how-to did. The documenter's own pre-bounce commit
(`642de5dca1`, "document the ollama stop rules") added a whole
"### Ollama is stopped by the swarm (BL-1704)" section (28 lines: the
four owner/pid-state dispositions, the `\|\| true` failure-is-never-
fatal note) — that entire section is GONE from the current tree
(confirmed: `git merge-base --is-ancestor 642de5dca1 HEAD` → true,
`grep -n "Ollama is stopped" docs/how-to/BL-1052-...md` → no match).
The "Repair" section's closing line also reverted to its PRE-BL-1704
wording, now self-contradictory: it calls BL-1704 itself "a separate,
not-yet-documented ticket" from within a doc file that is BL-1704's own
documentation. The documenter's CURRENT pass on this lineage
(`BL-1704-documenter-20260925.md`) recorded "NONE" without catching
this — the section was already gone by the time that pass ran, so
there was nothing left in the diff for it to notice; only a diff
against the pre-revert tip (`642de5dca1`) surfaces it.

Checks run (commit `d8264e11ce`, shared tip with BL-1698 this batch):
- `node specs/pipeline/cli.js specs/features/BL-1704-….feature`: 5/5.
- `bash swarmforge/scripts/test/test_kill_all_swarm_ollama_failure_not_fatal.sh`
  (restored): ALL PASS, unmodified against the D1-fixed code.
- `npm test` (extension/): 638 files / 10875 tests, all green.
- No TS/JS production file in scope — CRAP/DRY not run (shell only).
- `git status --short` clean before each commit.

By hardender.
