# BL-1509 — QA review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Resume of a hold opened earlier today for five unowned property-lane reds
(`backlog/evidence/BL-1509-QA-unowned-red-20260915.md`); the specifier's
note (parcel 001556) reports those five owned by BL-1578/BL-1579 at
`ee5032f5ba`, confirmed against the standing-red register CLI
(`standing_red_register_cli.bb list`): all five rows now carry
`"owned":true`, `"unowned":[]`.

- Straggler check: `pgrep -fl 'node --test|stryker'` before and after — no
  orphaned processes either time.
- Unit suite (`npm test`, extension/): 620/620 files, 10513/10513 tests
  green. Suite-budget warnings are perf-only, not failures.
- `required_wiring` gate: `pre_qa_gate.sh . HEAD` → `OK`.
- Property suite (`npm run test:properties`), run twice:
  - Run 1: 407/408 files green; `test/bl1308SiblingDetectorCoversReplay.property.test.js`
    invariant 1 failed with `Error: Test timed out in 20000ms.` Grepped
    `bl1308SiblingDetectorCoversReplay`/`BL-1308` against
    `backlog/paused`, `backlog/active`, `backlog/hold` — no open ticket
    (BL-1308 itself is in `backlog/done/M8/`); not in
    `backlog/standing-reds.tsv` or the register CLI's output — genuinely
    unregistered at that point.
  - Re-ran that file alone: 2/2 tests passed in 10.04s, well under the
    20s timeout — same shape as this ticket's own previously-recorded
    reach-floor/timeout flakes under concurrent swarm load.
  - Run 2 (full suite again): 408/408 files, 1201/1201 tests green — no
    recurrence of the bl1308 timeout, and none of the five previously-held
    reds resurfaced either. Only the allowlisted benign
    `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors
    (BL-871) appeared, both runs.
  - Verdict: the bl1308 timeout was a one-off environmental flake
    (isolation run + a second full run both green), not a deterministic
    defect and not this parcel's own — `bl1308SiblingDetectorCoversReplay`
    exercises sibling-detector/replay logic, nothing this parcel's diff
    touches. Not opening a new hold: the gate's actual requirement (a
    green `test:properties` run) is satisfied by run 2, and the flake did
    not recur to earn its own ticket.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1509-a-file-can-be-posted-to-a-telegram-topic-as-a-document.feature`):
  5/5 tests pass.
- Invariant ("bot token never appears in any error string"): covered by
  `extension/test/bl1509SendDocumentTokenRedactionInvariant.property.test.js`,
  which asserts `/\[redacted\]/` against the actual `redactToken` split/join
  rather than re-implementing the redaction logic.
- Ancestry: `git merge-base --is-ancestor 9e4366ce1c HEAD` (this ticket's
  own hardener merge into the commit being approved) → OK. HEAD
  `3fd48291df7eed89b43bd35c23cb18c630149d6f` is a linear descendant of every
  BL-1509 stage commit (coder → cleaner → architect → hardener →
  documenter), confirmed via `git log --oneline --all --grep="BL-1509"`.
- `required_wiring` step handler `bl1509FilePostedAsTelegramDocumentSteps.js::registerSteps`
  present and lands in this same parcel.

Approving. Landing `3fd48291df7eed89b43bd35c23cb18c630149d6f`.
