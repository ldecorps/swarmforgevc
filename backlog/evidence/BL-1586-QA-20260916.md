# BL-1586 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1586`: `VERIFY BL-1586` (no open deferral).
- No orphaned test/mutation processes before or after (`pgrep -fl 'node --test|stryker'`).
- Merged documenter's tip `4d99cf281e` into QA (`Merge documenter 4d99cf281e into QA.`); `main`/`origin/main` in sync (0/0 divergence); BL-1585 confirmed already landed and closed on `origin/main` prior to this pass.
- Reviewed all five stage evidence files (coder/cleaner/architect/hardender/documenter) for BL-1586: coder's per-file table (16 rows: branch, cells, budget, floors kept), architect NONE, hardender NONE, documenter NONE.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1586-sampled-reach-floors-sweep-2-of-6.feature` — 17/17 pass.
- Step handler (`specs/pipeline/steps/bl1586SampledReachFloorsSweep2Steps.js`) read directly: `PINNED_FILES` matches the ticket's 16-file list and the feature's Examples table exactly; `NOT_A_FLOOR_FILES` correctly names only bl1362, matching `required_wiring`.
- Per-file 5x run (own independent run, `for i in 1..5; do npx vitest run --config vitest.properties.config.mjs test/<file>; done` for each of the 16 pinned files, sequential, one file at a time): 80/80 runs green, zero FAIL lines.
- Floor audit (`git diff main...HEAD -- extension/test | grep -E '(numRuns|assertReachFloor|>= *[0-9]|> *0|runsPerCell)'`) read line by line: every removed `{ numRuns: N }` literal reappears as `runsPerCell(N * cells, cells)` with the identical total budget; every removed `assert.ok(x > 0, ...)` / `assert.ok(x >= LENGTH - 1, ...)` reappears as an `assertReachFloor(...)` call at the same floor value. Spot-verified against the pre-image on `main` for bl1342 (invariant 1: `IO_CLASSES.length - 1` / `NON_IO_CLASSES.length - 1` floors unchanged) and bl1538 (`DEPTH_FLOOR`/`CLOSURE_CELL_RUNS` unchanged at 3 and 1). No value moved.
- bl1362 spot-check: confirmed by reading the file directly that all four reach counters (`none`/`items`/`refused`/`collision` and `reached`) are incremented unconditionally on every property call in a fixed sequence, never gated on the drawn value — genuinely not a coverage floor. `git diff main...HEAD -- extension/test/bl1362ReviewEvidenceByToolInvariants.property.test.js` is empty (file untouched).
- Break-then-restore (own independent run, smallest-budget file `bl1346RcRepairStampInvariants`, budget 1/cell): temporarily changed `for (const degraded of ROLES)` to `ROLES.slice(1)`; the run failed with `reach floor: degraded role specifier drawn 0 < 1`, naming the floor exactly. Restored from a saved copy; `git status --short` clean; re-ran green (3/3 tests).
- `git diff main...HEAD --name-only -- extension/test`: exactly the 15 pinned files (bl1362 absent, as required).
- `git diff main...HEAD --name-only` outside `extension/test/`, `specs/features/BL-1586-*`, `specs/pipeline/steps/bl1586*`, `backlog/`: empty — no production source or helper file touched.
- `pre_qa_gate.sh BL-1586-sampled-reach-floors-sweep-2-of-6 HEAD .`: `OK`.
- Full unit suite (`npm test`): 620 test files / 10527 tests passed, 0 failed.
- Full property suite (`npm run test:properties`): 408 test files / 1201 tests passed, 0 failed. Two `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors — the BL-871 allowlisted benign noise, not a test failure.
- Standing-red register (`standing_red_register_cli.bb`, run before and after the parcel's own verification): `"unowned":[]` both times; the 5 owned rows (BL-1588 x4, BL-1589 x1) are full-lane-timeout class, explicitly out of scope for this ticket, and one of them (`bl1354SharedPathLandedSiblingInvariants`, a file this ticket itself swept) passed cleanly in both the isolated 5x run and the full property-lane run — no regression.
- No orphaned test/mutation processes after either run (`pgrep -fl 'node --test|stryker|vitest'` clean).
- No prior bounce history for BL-1586 on `main`/`origin/main`.
