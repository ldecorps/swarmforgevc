# BL-1263 documenter review pass — 2026-09-05

Reviewed the hardened commit (`86d9f9bac4`) and the coder/cleaner/architect/
hardener trail for BL-1263 (three standing assertions contradict deliberate
source behaviour).

Checklist:
- Step handler exists:
  `specs/pipeline/steps/bl1263StaleAssertionsRetiredToShippedBehaviourSteps.js`.
- Confirmed the fix at all three named sites:
  - `extension/test/backendSwitch.test.js` — assertion now expects
    `cursor/auto` (the backend-qualified id `readLaunchScriptModel`
    actually returns), not the stale bare `auto`.
  - `extension/test/telegramClient.test.js` — the `deepEqual` body
    assertion now includes `allows_multiple_answers`, still a full
    `deepEqual` (never loosened to a partial/key-subset check, per the
    ticket's firm constraint).
  - `extension/test/telegramCursorOperatorExec.test.js` — the fixture
    ticket now seeds `backlog/active/` (matching the BL-691 D3
    active-only ambulance guard), and the expected text matches
    "Ambulance engaged", not "refused".
- Confirmed no production source file was touched (only the three named
  test files), per the ticket's firm "do NOT change any of the three
  source behaviours" constraint. Confirmed the standing-reds register row
  was removed in the same commit, per the standing-red rule's "removed in
  the same land that turns the test green."
- This is a pure test-assertion realignment to already-shipped,
  already-correct production behavior — no new mechanism, no changed
  contract, no operator-visible behavior beyond three fewer standing
  reds. Grepped `docs/how-to/` for the touched symbols
  (`readRoleModelId`, `allows_multiple_answers`, `engageOperatorAmbulance`,
  `cursor/auto`): the hits found (BL-1318, BL-713, BL-1079, BL-1119) are
  about unrelated cursor-seat/pack-staffing/identity-steward mechanisms
  that happen to share the string "cursor" — none describes these three
  specific test assertions or their prior stale behavior, so none goes
  stale. No documentation update is in scope for this ticket.
- No "Last Updated" freshness field anywhere touched — no date bump owed.
- No registered diagram depicts unit-test assertion content — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond what the hardened commit already carries.

Result: NONE — no defect found, no documentation change in scope.
