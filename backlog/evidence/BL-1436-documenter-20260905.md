# BL-1436 documenter review pass — 2026-09-05

Reviewed the hardened commit (`110a16a51d`) and the coder/cleaner/architect/
hardener trail for BL-1436 (`PRICING_TABLE` now prices `claude-fable-5-1`,
the display-name map names it, and the mis-attributed standing-red
register row is gone).

Checklist:
- `required_wiring` anchors present, confirmed by grep:
  `extension/src/metrics/pricingTable.ts::claude-fable-5-1`,
  `extension/src/swarm/modelDisplayName.ts::claude-fable-5-1`,
  `specs/pipeline/steps/bl1436PricingTableCoversFable51Steps.js::registerSteps`.
- The ticket's own description explicitly directs: "The documenter
  refreshes the pricing section of the briefing/cost docs if they list the
  priced models." Searched and found the one living reference page that
  enumerates `PRICING_TABLE`'s rows and rulings:
  `docs/reference/specs/BL-627-pricing-table-correctness-and-coverage-invariant.md`
  (the table's own origin ticket, extended in place per this repo's
  per-ticket-section convention — same pattern as the BL-973 and BL-1241
  how-tos). Added a `## A missing row is a red on main, not a silent zero
  (BL-1436)` section naming the rates, the deliberately-unset
  cache-creation field, and the register-row cleanup. Checked
  `docs/how-to/BL-1056-a-price-with-an-expiry-date.md` (the other pricing
  doc, for windowed rates) — it only illustrates `claude-sonnet-5`'s
  window mechanism, does not enumerate the full roster, so it needed no
  change. This reference doc has no "Last Updated" field, so none was
  added.
- Added a dated top entry to `docs/reference/Specification.MD`,
  cross-linking to the BL-627 reference page.
- No `docs/index.md` change needed — no new page was authored.
- No registered diagram trigger fires — this prices one model and
  corrects a standing-red register row; it changes neither the extension
  host/webview boundary, the tmux substrate, the `.swarmforge/` state
  layout, the pipeline topology/backlog flow, the handoff file lifecycle,
  nor front-desk routing.
- No production code, test, or requirements change made by this pass —
  documentation only. Confirmed `npx vitest run test/pricingTable.test.js`
  still 30/30 after the doc-only edit.
- `docs/reference/specs/BL-627-...md` is named for a different
  (already-shipped) ticket, so its edit lands in its own UNTAGGED commit
  per the cross-ticket-doc-edit convention (BL-1192 task-scope gate); the
  Specification.MD entry and this evidence file, both this ticket's own
  paths, land in a separate tagged commit.

Result: NONE — no defect found; the BL-627 reference page gained a
section for this ticket's fix, one changelog entry added.
