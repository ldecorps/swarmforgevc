# BL-1458 — hardener review pass, 2026-09-21

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: hand-mutation: (if (zero? (:exit result)) ...) -> (if true ...) in instruct-documenter-briefing!
- **Commit hash**: 4a8e8ed7cc
- **First error excerpt**: ALL PASS survived against every existing test - none exercises a failing send
- **Failure class**: test-coverage
- **Expected vs observed**: a failed send must not create the once-per-day marker, so the next tick retries rather than silently dropping the day
- **Blamed role**: coder
- **Remediation pointer**: fixed in this pass: new bl1458_instruct_documenter_briefing_test_runner.bb added and confirmed to kill the mutant; production code needed no change

By hardener.
