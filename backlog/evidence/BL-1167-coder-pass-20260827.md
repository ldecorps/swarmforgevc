# BL-1167 — coder pass — 20260827

Same-model stage seats bypass BL-1001 tier filtering.

- `parse-seat-models` / `parse-window-seats` / `stage-models-uniform?`
- `difficulty-claim-decision` returns `:claim` when models uniform
- `ready_for_next_task.bb` passes models + window-seats into the gate
- Unit + property + acceptance (ready_for_next fixture)

By coder.
