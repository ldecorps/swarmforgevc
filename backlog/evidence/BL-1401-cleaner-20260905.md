# BL-1401 — CLEANER PASS, 2026-09-05

NONE. No defect found; no cleanup change made.

## What was checked

- The real acceptance run: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-632-commit-time-guard-refuses-pipeline-code-on-main.feature`
  — **11/11 pass**, was 4 pass / 7 fail on `main` before this ticket
  (BL-1385's `check_handler_module_graph.sh` missing from the hand-written
  fixture list, same root cause as BL-1398's property-test twin).
- `specs/pipeline/steps/bl632CommitTimeGuardSteps.js`: the hand-written
  twelve-entry copy list is gone; `mkFixtureRepo` now calls
  `deriveCommitGuardFixtureSet({ repoRoot: REPO_ROOT }).files` — imported
  from `extension/test/helpers/commitGuardFixtureSet.js`, the same helper
  BL-1398 shipped. Grepped for `run_guard` in this file: 0 hits (invariant
  3 — no second parser of the runner's guard lines).
- `swarmforge/scripts/test/test_bl1401_acceptance_fixture_derives_set.sh`:
  6/6 PASS, including the ticket's own qa_e2e steps 1-4 near-verbatim: the
  real BL-632 feature passes every scenario, a guard added to a seam
  runner copy appears in the derived set and the chain runs it, a guard
  the seam names but the tree lacks fails the build naming it, and the
  handler consumes the helper with no second parse.
- `bl1401AcceptanceFixtureDerivedSet.property.test.js`: 2/2 PASS.
- `specs/pipeline/steps/bl1401AcceptanceFixtureDerivesItsSetSteps.js::registerSteps`
  present, matches BL-1371's `*Steps.js` discovery suffix
  (`required_wiring` anchor 2). Anchor 1
  (`bl632CommitTimeGuardSteps.js::deriveCommitGuardFixtureSet`) confirmed
  live above.
- `tsc -p ./` compiles clean.
- `jscpd` over the three touched/new JS files: 0 clones.
- No prior bounce history for BL-1401 (mint → IR-DRY review → approve →
  promote → topic → coder, no adjudicate-again entries).
  `main`/`origin/main` in sync (`0 0`).
- The BL-632 feature file itself is untouched (grep confirms no diff to
  it in this commit) — exactly as the ticket specifies: its scenarios
  were already correct, only the fixture that ran them was stale.
- New/changed files here are hand-authored JS test/step-handler files, not
  compiled from TypeScript production sources — `mutation-site-count.js`
  does not apply.

No architecture concerns: this is the acceptance-side application of an
already-reviewed helper (BL-1398), applied identically to its one
remaining consumer.

By cleaner.
