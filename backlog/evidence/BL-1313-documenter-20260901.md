# BL-1313 — documenter pass, 2026-09-01

## Review inventory

- Received `git_handoff` from hardener, task
  `BL-1313-a-batch-held-parcel-is-visible-to-the-send-time-guards`,
  commit `00d71dd9d9`.
- Re-read the ticket (`backlog/paused/BL-1313-...yaml`) and the merged diff
  (`5008be4f0c`: `handoff_lib.bb` gains `handoff-files-with-batches` /
  `my-handoff-files-with-batches`; `duplicate_chain_guard_lib.bb` and
  `swarm_handoff.bb`'s `inbound-non-forwarding?` now call the batch-aware
  reader for `:in_process` walks).
- Checked whether the change is user-visible: no extension command,
  setting, or UI surface changed — this is an internal handoff-transport
  guard fix (Article 2.4 self-check, BL-760 duplicate-chain guard).
- Checked `docs/reference/Specification.MD` for prior coverage of these two
  guards: found none describing the batch-blindness this ticket fixes, so
  added a dated entry (this commit) explaining the defect, the fix, and the
  scope boundary (four other blind `in_process` reads and six pre-existing
  hand-rolled batch walkers left out, per the ticket's own notes).
- Checked `docs/diagrams/handoff-flow.mmd` (the diagram whose change-trigger
  is "the handoff file lifecycle, its gates, or the inbox/in_process/
  completed state machine"): its `FORWARD` node depicts sending the next
  `git_handoff` at a level of abstraction that does not name either guard
  individually, and no lifecycle state, gate, or transition it draws
  changed — the fix corrects what an existing, undepicted helper reads,
  not the state machine itself. No diagram edit made.
- Checked other how-to/reference docs mentioning `batch_*`/`handoff-files`
  (`BL-1261-hold-divergence-audit.md`, `BL-611-babysitterd-runbook.md`):
  both cover unrelated mechanisms (hold-divergence audit, babysitter
  runbook), not the two guards this ticket touches. No change needed.

## Findings

NONE.

## Forwarded

`git_handoff` to QA, task
`BL-1313-a-batch-held-parcel-is-visible-to-the-send-time-guards`, commit
`c8cee2e2c3` (Specification.MD entry, this evidence file).

By documenter.
