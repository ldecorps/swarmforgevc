# BL-967 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-967 — handoffd restart-storm recurrence: bound every in-cycle wait, make sweep boundaries self-localizing (`type: defect`, `severity: high`, milestone M8)
- **Parcel**: `00_…_from_documenter_to_QA_for_QA` (from documenter, commit `6161ad192b`)
- **Verified at**: the QA merge of `6161ad192b`
- **Reviewed by**: QA, 2026-08-20

## Verdict

**PASS — inventory items: NONE.**

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | `daemon_cycle_guard_lib_test_runner.bb` | PASS — `ALL PASS` |
| G2 | `daemon_cycle_guard_lib_property_runner.bb` | PASS — `ALL PROPERTIES HOLD` |
| G3 | Acceptance — `BL-967-…-bounded-waits-and-sweep-boundaries.feature` | PASS — 4/4, 0 fail, exit 0 (covers `qa_e2e` steps 1–2: scenario 01 bounded-timeout survival, 02 boundary lines, 03 no lines on idle ticks) |
| G4 | **Invariant 1 — routing half, wired not claimed** | PASS — 48 `daemon-cycle-guard-lib/` call sites in `handoffd.bb`; the chokepoint is `load-file`d at `:16` and the timeout hook installed at `:215`. Zero bare `process/sh`/`shell` calls remain (the one grep hit is a comment at `:70`) |
| G5 | **Invariant 1 — closure gate proved NON-VACUOUS** | PASS — see below |
| G6 | **Invariant 2 — boundary line per sweep, bounded volume** | PASS — `run-sweep!` ALWAYS emits `sweep-boundary sweep=<name> ms=<duration>` (action or none, error or clean, per its docstring), and the local wrapper is called at **22** sites, all inside the heavy-bundle branch (`handoffd.bb:3272-3327+`) — never on the 1s idle ticks, which is the invariant's bounded-volume clause |
| G7 | Full unit suite | PASS after attribution — 442/444 files, 7907/7909 tests. Both failures (`mermaidRender`, `bounceWatcher`) pass in isolation: **44/44, zero failures** |
| G8 | `pre_qa_gate.sh` | **FAIL — adjudicated, see below** |
| G9 | Docs currency | PASS — `docs/how-to/BL-967-handoffd-cycle-stall-diagnosis.md` added; `Specification.MD` carries the entry |
| G10 | Functional change present | PASS — new shared lib, 48 call-site conversions in the live daemon, closure gate, property runner, step handler, feature, how-to |

## G5 — the closure gate is executable AND non-vacuous

Invariant 1 has a routing half ("the daemon reaches no subprocess path outside this chokepoint") that the runner's own comment records as *"previously a stated claim, and a false one: D1 arrived through two in-cycle libs the claim missed."* It is now executable: over the **transitive `load-file` closure** from `handoffd.bb` — computed by `master_checkout_drift_lib`'s BFS, explicitly *"never a hand-maintained file list, which gets patched one name at a time and re-drifts"* — no file except `daemon_cycle_guard_lib.bb` may name `babashka.process`, `clojure.java.shell`, `process/sh` or `process/process`. Comments and string contents are stripped first so prose cannot trip it.

QA did not take that on trust. A real violation was injected into a file *inside* the closure (`handoff_lib.bb`, not the guard lib) and the gate failed loudly, naming the exact site:

```
FAIL invariant 1 structural half: no subprocess path outside the chokepoint anywhere in handoffd.bb's load-file closure
  expected: []
  actual:   ["handoff_lib.bb:1259: (def qa-nonvacuity-probe (process/sh \"\"))"]
```

File restored; `git diff` clean afterwards. The gate catches what it claims to catch.

## G8 — the pre-QA ancestry block, adjudicated on evidence

Two candidates were raised across this parcel's life. Both were checked by CONTENT, per the constitution amendment accepted today (`cdb0a1d36`: *a gate blocks on dropped-work evidence, never on a ticket id named in a commit subject*; implementation ticket BL-972).

**1. `5c8b0835f8` — pure subject-line false positive.** The documenter pre-filed an adjudication (`BL-967-pre-qa-gate-false-positive-20260820.md`); QA re-verified it independently rather than accepting it. It is a **BL-966** commit ("the depth resolution answers identically from every checkout"), touching only BL-966 files. The single BL-967 line it holds that this branch lacks is a *comment*: `;; BL-967: subprocess waits are bounded at the shared chokepoint - this lib`. No BL-967 deliverable.

**2. `4101e4bfbb` — a genuine BL-967 rider, but nothing functional is missing.** This one is real BL-967 work ("rider, discovered by the standing suite after the re-fix restore"), committed 06:23, four minutes AFTER the 06:19 parcel. It touches three files that all differ here, so it is NOT a subject-line match and deserved a real look.

Reading the diff settles it: **this branch already declares `daemon_cycle_guard_lib.bb` in BOTH fixture lists** — `operatorRuntimeBbFixtureFiles.js:57` and `readLiveRoleHeldTicketsCli.test.js:26`. The rider only (a) moves that entry earlier in the array and (b) rewrites the surrounding comment. `REQUIRED_SCRIPT_FILES` is a copy list of files to materialise into a fixture, so position is immaterial — and the test that would catch a missing entry, `readLiveRoleHeldTicketsCli.test.js`, **passes on this branch's version** in G7.

An initial reading of this rider as "missing functional work" was wrong and is corrected here: no `abandoned_commits` declaration is owed and no bounce is warranted. The rider is live coder work that will arrive down the chain on its own, exactly as BL-571's re-fix did.

## `qa_e2e` step 3 — live soak is owed AFTER landing, and is not claimed here

Step 3 asks for a read-only 30-minute observation *"after the parcel lands and the daemon restarts on it"*: zero new `handoffd` entries in `.swarmforge/daemon/freshness-incidents.log`, and `handoffd.log` showing completed cycles with boundary lines. That is not runnable before landing, by its own wording.

Recorded as **owed, not passed**. Whoever observes the daemon after this lands should check those two files; if a bounded timeout IS hit live, its log line now names the sweep and the call (that is what G6 buys), and it should be recorded as the identified cause with a follow-on minted. Step 4 (BL-789's slow-but-healthy 120–232s posture preserved) is likewise a live-observation item; nothing in this parcel changes the kill threshold, and the acceptance suite's scenario 01 covers the survive-and-continue path in fixture form.

By QA.
