# BL-638 — QA pass, 2026-08-05

Commit verified: 9d0fdfb33afc7fc4c1afffe4c45f3a0c2c8c02b0 (merge of documenter
2112fc73c7 into QA). Confirmed ancestors: coder 3f6578d0, cleaner d92ebaf4,
architect 957f76da, hardener 9347b2d4, documenter 2112fc73c7 — all `is-ancestor`
of the commit being approved.

## In-scope checks (all green, run directly, isolated from host load)

1. **Unit tests, BL-638's own files** — `node --test`:
   - `specs/pipeline/test/gherkinMutationOutcome.test.js` — 27/27 pass.
   - `specs/pipeline/test/finalizeGherkinMutation.test.js` — 10/10 pass
     (includes 3 spawned-end-to-end wiring tests).
2. **Property test, BL-638's own invariant** —
   `extension/test/bl638ZeroMutantNeverReadsAsPass.property.test.js` run in
   isolation via `npx vitest run --config vitest.properties.config.mjs` —
   3/3 pass (both invariant halves + the manifest-correction property).
3. **Formal acceptance gate** —
   `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-638-gherkin-mutation-zero-mutants-reads-as-a-pass.feature`
   — 7/7 scenarios pass (all of 01–07).
4. **Manual E2E procedure** (specifier's notes, run against the real vendored
   mutator, `./tmp/` work dirs, default `soft` level, copies deleted
   afterward, tracked feature files `git checkout --`-reverted after each
   probe):
   - Step 1 (scenarios 01/04/06): ran against this ticket's own committed,
     outline-free feature file — exit 2, `outcome: "inapplicable"`, manifest
     written with `"outcome":"inapplicable"` beside the empty scenario list
     and `implementation_hash: "unknown"` (never reads as a completed pass).
   - Step 2 (scenario 02): re-ran unchanged — same outcome, same exit code,
     same summary — stamp-driven skip does not report a silent pass.
   - Step 3 (scenario 03): ran against `BL-116-launch-path-probe-and-log.feature`
     (real `Scenario Outline`, selected by structural grep) — 2 mutants
     generated, `outcome: "pass"`, exit 0 — normal path unaffected.
   - Step 5 (scenario 05): `swarmforge/roles/hardender.prompt` BL-638 bullet
     confirmed present, naming the surgical-mutation-sweep fallback instead of
     "skip it, nothing to run".
5. **required_wiring** — both entries confirmed by direct grep:
   - `specs/pipeline/scripts/run_gherkin_mutation.sh` contains `inapplicable`.
   - `swarmforge/roles/hardender.prompt` contains the BL-638 fallback bullet
     naming `outcome: "inapplicable"` and the surgical-sweep action.
6. **Wiring/callers** — every caller of `gherkinMutationOutcome.js` /
   `gherkinMutationClassify.js` / `gherkinMutationManifest.js` is inside
   BL-638's own scope (the wrapper, its finalize CLI, its steps file, its own
   tests, its own property test) — no stray unwired module.
7. Scenario 07 (re-arming) additionally spot-checked in a `./tmp/` copy:
   added a `Scenario Outline:`+`Examples:` block to an already-stamped
   outline-free copy, re-ran — 4 mutants generated, `outcome: "pass"` —
   confirms the fix does not break the existing self-invalidating stamp.

## Out-of-scope: full extension suite and full property suite — BLOCKED by host load, not by this parcel

Four separate full/broad-suite attempts over ~50 minutes
(`npm test` x2, `npm run test:properties` x1, `npx vitest run` x1) each
failed a small, DIFFERENT subset of tests (6–14 tests across 3–6 files per
run), never the same set twice. Every failure signature was one of:
`Error: [vitest-worker]: Timeout calling "onTaskUpdate"`,
`Error: Test timed out in 20000ms`, or a subprocess exiting `143` (SIGTERM) —
infrastructure/IPC timeouts, not logic assertion mismatches — and every
failing file was outside BL-638's diff (`git diff --stat 3f6578d0^ HEAD`
confirms BL-638 touches only `specs/pipeline/*`, its steps/test files, and
`swarmforge/roles/hardender.prompt`). Examples: `dependencyGateCliReportsAndScope.test.js`,
`readLiveRoleHeldTicketsCli.test.js`, `renderBriefingDiagramsCli.test.js`,
`bl797MutationGateProbeCrashFallback.property.test.js`.

`uptime` throughout this window read load averages of 132–212 on a 4-core
host (35–50x normal) — multiple concurrent `git log --follow --name-status`
processes were independently observed consuming CPU, unrelated to any command
QA ran. Re-running the specific previously-failing files in isolation (no
other heavy process running) reproduced the same timeout signatures for two of
them (`readLiveRoleHeldTicketsCli.test.js`, `renderBriefingDiagramsCli.test.js`)
and passed for the third (`dependencyGateCliReportsAndScope.test.js`) —
consistent with a host-wide CPU ceiling starving real subprocess calls
(`bb`, diagram-rendering) past their timeouts, not with test-runner worker
contention or a code regression.

Per the same reasoning already adopted in this repo for Stryker under severe
load (skip rather than treat a starved run as signal), this is recorded as
BLOCKED by host infrastructure, not as a BL-638 defect: forcing more retries
against a host stuck 35–50x over normal load was not converging, and none of
the failures ever touched a file BL-638's diff includes.

## Verdict: PASS

BL-638's own scope is independently, repeatedly, and cleanly green by every
check that can isolate it from the host-load condition above. Approved on
that basis. The host-load condition itself is flagged to the coordinator
alongside this approval as a standing health signal (constitution Article
3.5), not fixed or worked around here.
