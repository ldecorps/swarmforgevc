# BL-1479 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Hardening pass on the coder's fix for the architect's earlier bounce (D1,
`backlog/evidence/BL-1479-bounce-20260907.md`: `park-ticket!`'s
commit-refusal rollback used `git checkout -- old new`, which reads the
INDEX post-`git mv` and leaves the rename staged indefinitely).

- Coder's fix (`e5ab7a4d94`): `rollback-park-mv!` does `git reset --
  old new` (unstage back to HEAD) then `git checkout -- old` (restore
  working-tree content) then deletes `new` (untracked again post-reset)
  - exactly the remediation direction the bounce evidence proposed.
- New unit test forces a REAL lock-timeout (pre-occupying
  `commit_integrity_lib.bb`'s own lock directory) after a real `git mv`,
  asserting `git status --short` reads completely clean afterward, the
  file is back byte-identical in `active/`, and nothing is left in
  `paused/` - the exact trigger the function's own docstring names
  ("a concurrent writer is live"), never a stubbed CLI.
- Independently verified non-vacuity, not merely trusted the claim:
  patched `rollback-park-mv!` back to the pre-fix `git checkout -- old
  new` form and re-ran the test - it crashed outright (`FileNotFoundException`,
  the file gone from BOTH `active/` and `paused/`), matching the coder's
  own description exactly. Restored, re-verified green, clean diff.
- Re-ran: `bl1479_parked_active_sweep_test_runner.bb` (ALL PASS),
  `bl1479_parked_active_sweep_property_runner.bb` (40 runs, all three
  invariants, five coverage buckets all above floor), the acceptance
  feature (5/5), and BL-1469 (7/7) / BL-1301 (8/8) siblings - all green,
  no regression.
- Reuse discipline (BL-897) confirmed by reading the code, not just the
  commit message: `parked-active-condition` calls `parked-ticket?` and
  `promotion-gates-lib/not-before-refusal` verbatim, never a second
  regex or date parse.
- Noted, not bounced (same class as BL-1469's `open-slot-nudge-sweep!`,
  accepted there for the same reason): `parked-active-sweep!` and its
  refusal-dedup helpers in `handoffd.bb` read top-level `def`'d globals
  rather than injectable parameters, so no test exercises the daemon
  wiring directly - only the pure decision functions
  (`parked-active-condition`/`parked-active-items`) and the real-git
  `park-ticket!` are unit/property-tested. Pre-existing codebase-wide
  handoffd.bb testability debt, not this ticket's to fix.
- Also noted, not bounced: for a ticket that is BOTH not_before-blocked
  AND has a live parcel (refused), the dedup key includes
  `not-before-refusal`'s own reason text, which embeds a day-count that
  decrements daily - so the "logged once" refusal log line is
  practically "once per day" rather than "once ever" for that narrow
  combination. The invariant's own wording ("not noted... every cycle")
  is about cycle-frequency spam, which this still prevents; the
  day-count text is inherited verbatim from reused code, not new logic.
- BL-149 cooldown gate: `chase_sweep_lib.bb` and `handoffd.bb` both read
  `skip-cooldown` (2/0.05 days old) - no fresh Stryker pass (no wired
  tool for `.bb` regardless). Host quiet-to-moderate throughout (load
  avg ~3.3-6.9 on 20 cores, comfortably under the 2x-cores threshold).
- No stray test/mutation processes or fixture directories from this
  pass's own runs.
