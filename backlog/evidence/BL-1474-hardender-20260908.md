# BL-1474 — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Received `927c1fb12e` (architect merge-up), merged clean as `6315640e1e`. No
mutation tool wired for Babashka (BL-472) — `replay-commit-refusal-reason`
is `mutation_cost: low`. Ran the checklist:

- `mutation_cooldown_gate.bb` on `land_step_lib.bb`: `skip-cooldown` (file
  age 0.05 days — the whole feature landed today). Correctly skipped a
  mutation-testing investment on code still this fresh, per the gate.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS,
  including the three BL-1474 integration cases (a real refusing
  `core.hooksPath` fixture hook with a message, an empty index, a silently
  refusing hook) and five pure-function cases on
  `replay-commit-refusal-reason` (empty index, non-empty stderr verbatim,
  blank stderr, nil stderr, truncation-with-naming).
- `test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
  (`npm run test:properties` lane): green — drives the real
  `replay-commit-refusal-reason` via `bb -e`, exhaustively covers every
  branch shape with a reachability floor, and pins the exact truncation
  boundary (`at-limit` not truncated, `one-over-limit` truncated).
- Hand-verified two mutants directly against `land_step_lib.bb`: (1)
  forcing `index-empty?` to `false` — killed by two existing assertions
  in `land_step_lib_test_runner.bb`, now reporting a fabricated refusal
  message where "nothing to commit" was expected. (2) the truncation
  guard's `>` weakened to `>=` — **UPDATE, post-amendment**: the specifier's
  accepted amendment to this pass's own rule_proposal (below) correctly
  called out that my first record here inferred a kill from a bare `bb -e`
  observation rather than running the pinning test. Re-verified per the
  amended rule: ran `test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
  alone against the mutant (single file, foreground, no ad hoc timeout —
  used `run_in_background` + poll instead). It genuinely FAILED after
  405s (fast-check's shrink search took 1929 attempts, each a real `bb`
  spawn — not a hang, just slow): `Property failed after 13 tests`,
  counterexample `{"kind":"at-limit","stderr":"!"×2000}`, "stderr at or
  under the bound must be quoted verbatim, untruncated" — expected the
  raw 2000-'!' string, received it truncated with "... (truncated)"
  appended. Mutant 2 is killed by that test's `at-limit` case, confirmed
  by an actual red run, not an inference. Restored the file and re-ran
  the same file alone: green again, 7.4s.
- Acceptance: `BL-1474-the-replay-reports-why-its-commit-was-refused.feature`
  5/5 green. `BL-1447-a-replay-missing-a-parcel-path-is-refused-before-publish.feature`
  (qa_e2e_procedure item 4, invariant 2's "nothing else about replay!
  changes") 3/3 green.
- No `extension/src` file touched by this ticket — CRAP/DRY not applicable.
- No orphaned bb/vitest processes belonging to this pass at handoff.

Sent a `rule_proposal` (priority 00) to the specifier: verifying a
hand-mutated `.bb` function should go through one short, directly-scoped
`bb -e` call rather than the full vitest property harness — a shared
multi-role host's OTHER concurrent test activity can make a mutant look
hung against an ad hoc timeout, and a reflexive `pkill -f "vitest run"`
cleanup then risks sweeping up another role's unrelated live processes.
Hit exactly that during this pass (a 60s timeout at load 8-9, traced back
to a process tree under the live front-desk/handoffd supervisor rather
than anything this pass started); no live-swarm daemon was harmed, and the
mutant was independently reconfirmed via the safer direct `bb -e` route.

Forwarding to documenter with the same task name.

By hardender.
