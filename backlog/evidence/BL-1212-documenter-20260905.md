# BL-1212 documenter review pass — 2026-09-05

Reviewed the hardened commit (`eb32d85d70`) and the coder/cleaner/architect/
hardener trail for BL-1212 (real-tree docs gate never recorded its live-read
exemption).

Checklist:
- Step handler exists:
  `specs/pipeline/steps/bl1212RealTreeDocsGateRecordsItsLiveReadExemptionSteps.js`.
- Confirmed the fix: `extension/test/docsStructureRealTree.test.js` gains
  a `BL-1038-EXEMPT: <reason>` marker with a real, judgeable reason (the
  live read against the real repo tree IS this test's own assertion,
  per BL-757's purpose), per the ticket's own direction and invariant
  ("never silently tolerated and never rubber-stamped"). Confirmed no
  other file was opportunistically exempted (the firm constraint against
  exempting `pilotMkdtempConventionCheck.test.js`, whose fix is BL-1209's
  separate scope, held — no change to that file in this diff).
- Confirmed the guard itself (`liveRepoDerivationGuard.js`) was not
  weakened, widened, or relaxed — this ticket adds one exemption, it does
  not renegotiate the boundary, per the ticket's firm constraints.
- Noted the mid-flight amendment recorded in the ticket (scenario 02
  retired, never reworded, after BL-1317 changed the file's own
  `REPO_ROOT` derivation to an idiom the guard's regex doesn't recognize
  — BL-1435's separate scope) — this documenter pass has nothing further
  to do about that; it's the coder's already-completed retirement, not a
  new finding.
- This is a one-comment fix (an exemption marker with its justification)
  with no production code, new mechanism, or documented contract change.
  No dedicated how-to exists for `liveRepoDerivationGuard.js`/BL-1038 in
  `docs/how-to/` to update (grepped for `BL-1038-EXEMPT` and
  `liveRepoDerivationGuard`, no hits) — no documentation update is in
  scope for this ticket.
- No "Last Updated" freshness field anywhere touched — no date bump owed.
- No registered diagram depicts unit-test exemption markers — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond what the hardened commit already carries.

Result: NONE — no defect found, no documentation change in scope.
