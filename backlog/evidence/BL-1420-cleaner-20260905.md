# BL-1420 — cleaner pass, 2026-09-05

NONE — no defect found. The three fixtures now derive their supervisor
rows from the guard's own glob, and the property runner correctly treats
an unexpected checker exit as a failed run rather than a vacuous pass.

## What was checked

- Read `extension/test/helpers/freshnessFixture.js` directly:
  `supervisorNames(scriptsDir)` is the same `*_supervisor.bb` glob,
  injectable so a test can point it at a scratch directory rather than the
  live tree; `writeGuardSatisfyingRows` writes one conf row + a fresh
  heartbeat per discovered name plus the `FRESHNESS_REQUIRED` registry —
  matches BL-1398/BL-1399's own established remedy (derive from the same
  closure the guard walks, never a hand-written count).
- Read the bb runner's diff directly: `run-checker!` now branches on
  `(:exit result)` explicitly, returning `{:ok? false :stderr ...}` on a
  non-zero exit; the main loop reports that as a failed run naming the
  stderr and evaluates NONE of P1-P4 or the coverage counters over it —
  confirmed this replaces the prior bare `:continue true` that swallowed
  the refusal into an empty, vacuously-passing observation.
- Reproduced the coder's own non-vacuity proof independently: ran the
  property runner at both the default run count and `PROPERTY_RUNS=40` —
  both report real, non-trivial coverage across all eight generator
  classes (log-absent, no-heartbeat-line, unparseable-timestamp,
  stale-heartbeat, creds-set/unset, identity-present/absent), confirming
  the properties now evaluate over genuine violations, not zero.
- Confirmed invariant 3 precisely: `git diff f18fccd9e2^..f18fccd9e2
  --name-only | grep daemon_log_freshness` is empty — this parcel's own
  diff touches none of the guard, the checker, or any conf file.
- Considered whether BL-1399's own pre-existing vitest fixture
  (`bl1012FreshnessSelfInflictedIncidents.property.test.js`, which the new
  shared helper's own header comment explicitly cites as "first wrote for
  one fixture") should also be refactored onto the new shared helper,
  since the ticket's own "How" section names "the vitest test and both
  step handlers" as consumers. Ran `jscpd` between the new helper and that
  file directly: 0 clones — the two implementations, while conceptually
  the same idea, are not literal text duplicates, so this is not a
  mechanical DRY violation. `required_wiring` anchors only the two step
  handlers and the bb runner, never BL-1399's own file, and it already
  passes. The coder's own explicit, transparent scope call (leave a
  working, already-fixed fixture alone rather than risk a regression for
  a cosmetic consolidation outside this ticket's three named call sites)
  is reasonable and matches this session's own established precedent
  (BL-1287/BL-1419: leave pre-existing, unrelated, working duplication
  untouched). Not a defect.
- `npm run compile` clean.
- No `backlog/standing-reds.tsv` or allowlist row is owned by this ticket.

## Verification (all re-run in this worktree, all green, matching every claimed number)

| check | result |
|---|---|
| `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1011-...feature` | 8/8 (was 0/8) |
| `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1012-...feature` | 9/9 (was 0/9) |
| `bb swarmforge/scripts/test/bl1011_freshness_attribution_property_runner.bb` (default) | ALL PROPERTIES HOLD, real coverage across all 8 classes |
| `PROPERTY_RUNS=40 bb ...` | ALL PROPERTIES HOLD |
| acceptance `BL-1420-...feature` | 6/6 |
| acceptance `BL-1399`/`BL-784` (regression) | 3/3, 3/3 — both unaffected |
| `grep -n FRESHNESS_REQUIRED` in all three fixed files | present (required_wiring) |
| `git diff f18fccd9e2^..f18fccd9e2 --name-only \| grep daemon_log_freshness` | empty (invariant 3) |
| `npm run compile` | clean |
| `npx jscpd` (new helper vs BL-1399's own pre-existing fixture) | 0 clones — not a DRY defect |

No fix required. Forwarding unchanged to architect.
