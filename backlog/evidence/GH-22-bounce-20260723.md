## Bounce evidence — GH-22

1. **Failing command:**
   ```
   git diff main HEAD --summary
   ```
   (run in the QA worktree after `merge_and_process documenter 5e004e63af`)

2. **Commit hash:** `5e004e63af` (documenter's handed-off tip; parent chain
   includes hardener merge `3ce374c47`, architect commit `42c556667`, and
   coder commit `46ce26046a`).

3. **First error excerpt:**
   ```
   delete mode 100644 For example, you could provide
   rename backlog/{done => active}/GH-22-telemetry-track-claude-conversation-compaction-and.yaml (100%)
   rename backlog/{active => paused}/GH-23-console-mini-app-context-budget-conversation-telem.yaml (99%)
   ```
   Plus, in `backlog/topics/GH-22.json` and `backlog/topics/GH-23.json`, the
   most recent status messages (`GH-22 ✅ done …`, `GH-23 🎵 in progress …`)
   are removed by this diff (i.e. reverted).

4. **Failure class:** `behavior`

5. **Expected vs observed:**
   - Expected: this parcel's only functional change is documenter's doc
     update to `docs/how-to/GH-22-context-telemetry-recorder-and-query-cli.md`
     (adding the NaN/Infinity example) plus removal of the stray junk file
     `For example, you could provide`. GH-22 should stay in `backlog/done/`
     and GH-23 should stay in `backlog/active/` with `assigned_to: coder`,
     matching `main` (verified equal to `origin/main` at `1d28b10ef`), since
     those transitions already landed on `main` via a prior QA cycle
     (`177a7bb1b` close GH-22, `d24f8fe18` promote GH-23) before this NaN/
     Infinity fix branch's later merge-up.
   - Observed: merging this parcel's commit `5e004e63af` on top of `main`
     REGRESSES shared backlog state: `GH-22` moves back from
     `backlog/done/` to `backlog/active/`, and `GH-23` moves back from
     `backlog/active/` (assigned to coder) to `backlog/paused/` (unassigned).
     `backlog/topics/GH-22.json` and `backlog/topics/GH-23.json` also lose
     their most recent status entries. This is almost certainly fallout from
     the architect's merge-up cleanup commit `42c556667` ("Drop stale
     cleaner-branch files reintroduced by GH-22 merge") resolving a merge
     conflict by keeping the wrong (older) side of `backlog/active|paused|done`
     and `backlog/topics/*.json`, rather than main's newer state carried in
     QA's approved commit `e3f76dded` (which the architect/documenter chain
     merged up from, per the Merge-Up Protocol, but did not fully preserve).
   - Root fix: whoever reworks this must merge `main` (already at `1d28b10ef`,
     equal to `origin/main`) into the affected branch and take **main's** side
     for `backlog/active/GH-22-*.yaml` (absent — file lives in `done/`),
     `backlog/done/GH-22-*.yaml` (present), `backlog/active/GH-23-*.yaml`
     (present, `assigned_to: coder`), `backlog/paused/GH-23-*.yaml` (absent),
     and both `backlog/topics/GH-22.json` / `GH-23.json`, then re-verify with
     `git diff main <new-tip> --summary` showing NO backlog rename/regression
     before re-forwarding to QA. The documentation addition and the stray-file
     deletion are fine and should be kept.
