# BL-1374 — QA pass, 2026-09-04

## Received

Documenter's re-run commit `e681b7a55e` (task
`BL-1374-a-sync-merge-is-not-credited-with-its-passengers`), merged into
this worktree (`4fff6bbe40`; merge-deletion guard fired on stale
`backlog/hold/BL-1296/BL-1309/BL-1356/BL-1359/BL-1360` copies un-parked on
the incoming side, resolved the same way as this session's prior lands.
Also manually de-duplicated `docs/reference/Specification.MD`: this
worktree's own local history and the incoming documenter branch each
independently carried an identical copy of BL-1371's own entry at a
different stack position — kept one, confirmed by grep no duplicate
`Last Updated` block remains for any ticket).

`required_wiring` anchor repoint (documenter's 2026-09-04 re-run,
`BL-1374-documenter-rerun-20260904.md`) confirmed: now points at
`land_step_lib.bb::merge-authored-paths` (the function that actually
narrows, per the coder's own self-audit and the architect's independent
read) and the handler file's own `registerSteps`, not the retired
`specs/pipeline/steps/index.js` DOMAINS anchor. Resolves the spec-gap the
architect flagged in its own pass.

## Verification

- `npm run compile` — clean.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — **2 failures**,
  BOTH in a `BL-1375`-owned fixture unrelated to BL-1374's own code (see
  below) — not this ticket's own suite.
- `bash swarmforge/scripts/test/test_bl1374_sync_merge_passengers.sh` — ALL
  PASS (18 checks, including the live `5d4486eb08` regression).
- `bb swarmforge/scripts/test/bl1374_sync_merge_passengers_property_runner.bb`
  — ALL PROPERTIES HOLD (24 fixture runs), both declared invariants.
- Land-step neighbors, unchanged: `bl1131_ticket_land_property_runner.bb`,
  `bl1334_land_replay_approval_property_runner.bb`,
  `land_main_publish_test_runner.sh`, `test_land_step_records_approval.sh`
  — all green.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1374-a-sync-merge-is-not-credited-with-its-passengers.feature`
  — 4/4 pass, matching coder/cleaner/architect/hardener evidence.
- Process hygiene: no orphaned `node --test`/`stryker`/`vitest` before or
  after.
- Wiring: `merge-authored-paths` (defined `land_step_lib.bb:575`) called
  from `path-owner-tickets`'s reduce at line 646 — the real narrowing site,
  confirmed by direct grep, matching the repointed anchor.
- Hardener evidence (explicit NONE, BL-1374 has no `Scenario Outline` —
  BL-113 inapplicable) independently confirms the same suites green in the
  batch pass.
- `human_approval: approved`, `human_ruling:` — option 3 ("both": code fix
  here, discipline rule the specifier's own to write) — substantive,
  non-templated. `bounce_history`: none for BL-1374 itself.

## Untracked standing red found: `land_step_lib_test_runner.bb`'s BL-1375
fixture is stale after BL-1371's discovery landing — NOT a BL-1374 defect

Two assertions in a BL-1375-authored fixture (`"BL-1375: the real tree
guard refuses an unregistered handler on a non-main tree"` and its feature-
naming companion) fail deterministically (reproduced twice, byte-identical
both times) — nothing in BL-1374's own diff touches this fixture or the
function under test.

Root cause, read directly rather than guessed: the fixture hand-writes an
OLD-style `index.js` (`const DOMAINS = [\n];\n`, i.e. an empty
hand-maintained array) alongside an unlisted `bl9009FixtureSteps.js`, and
expects `check_feature_handler_registration.sh` to refuse the feature as
unregistered. `extension/src/tools/featureHandlerRegistrationCheck.ts`'s
own top comment (line 27) now reads *"Registration is now discovery: a
handler..."* — BL-1371 (directory discovery, landed 2026-09-03) changed the
guard so ANY top-level `*Steps.js` file present in `specs/pipeline/steps/`
is reachable regardless of any `DOMAINS` array content, so the fixture's
premise (an array-absent handler = unreachable) no longer holds; the guard
correctly finds `bl9009FixtureSteps.js` reachable by discovery and refuses
nothing, and the two assertions built on "it must refuse" fail.

`grep -rl "land_step_lib_test_runner\|BL-9009-fixture\|unregistered handler" backlog/`
(evidence and paused/active/hold tickets) finds no existing ticket for this
specific staleness — genuinely untracked, not a duplicate of BL-1385 (that
ticket is about a DIFFERENT failure mode: a handler present on the tree
whose OWN module graph fails to `require`, not a fixture's stale
registration-mechanism assumption) or of BL-1371's own landed work (which
this fixture predates and was never updated for). Flagged to the specifier
by `note` below rather than silently swept or bounced to BL-1374's coder,
who owns none of this.

## Verdict

PASS. Approving BL-1374's own work. The `land_step_lib_test_runner.bb`
finding is reported separately and does not gate this ticket.

By QA.
