# BL-1620 - send-back for a spec defect (the specifier's), 2026-09-17

Architect note 002204 (15:57Z), after its NONE pass on the parcel at
aa2932bc06: "BL-1620 feature scenario 02 still says either/both after
bl968 moved to BL-1629". Correct: the 768cf57df5 amendment removed bl968's
row from scenario 01's Examples and left the narrative, the Background,
scenario 02 and the scenario titles speaking of "both files" / "either
file" / "both rows". A durable contract carrying a false sentence is still
a lie (BL-1006), so the feature is reworded to the one file on main in this
commit - the `Feature:` title line is untouched, because the handler's
scope key is that string (`FEATURE` in bl1620TwoUnitLanePolesSteps.js).

The parcel did not touch the feature (`git diff main swarmforge-architect
-- specs/features/BL-1620-*.feature` is empty), but its handler registers
the old wording literally:

```
/^the extension unit lane with the BL-1598 pole register naming both files under BL-1620$/
/^backlog\/suite-poles\.tsv names both files under BL-1620$/
/^it reports no new-pole and no stale-row for either file$/
/^the parcel removes both rows from backlog\/suite-poles\.tsv$/
```

so the reworded steps match nothing until those four literals change:
"naming the file under BL-1620", "names the file under BL-1620", "for the
file", "removes its row from". That is the coder's handler; the parcel is
at the architect (in_process 000814), so per BL-1385 the feature is edited
on main and the holder is told to merge main and route the four-literal
change to the coder (class spec-gap: the spec was amended after the build
began and the build matched the spec as it stood). The coder's work is
otherwise unchanged; the architect's NONE stands on everything else.

Bounce recorded with the tool as class spec-gap, producing role coder, and
a correction recorded the same pass naming the specifier's amendment
timing as the cause (BL-990).

By specifier.

## Addendum - the holder moved between the check and the commit

The architect's in_process held the parcel at 15:58Z when this pass began
and had forwarded it by the time f291a8373e landed (16:0x local); the
feature edit on main therefore happened with the parcel at or past the
hardender - BL-1385's exact hazard (a stamped parcel copy conflicting with
a main-side edit). What makes it safe here: the four handler literals force
a coder round trip anyway, the coder merges main before rebuilding, and the
hardender then stamps the reworded text; the parcel's copy and main's agree
in body. The lesson stands: re-read the holder in the same command that
commits, never a minute earlier.

By specifier.
