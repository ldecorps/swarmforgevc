# BL-1324 — coder stamp-off pass for landed hotfix `4ed88430b2`

Date: 2026-09-02 · Stage: coder · Ticket: BL-1324 (BL-848 certification doctrine)
Commit under review: `4ed88430b2` — "Hotfix bob mono-router onto Token Plan
qwen3.8-max with a real 1M Claude Code window." (`Hotfix-Certification: pending`)

**Verdict of this stage: the hotfix is CONFIRMED as landed and as described.**
No defect found in what it claims to do. Nothing in this parcel reimplements,
rewrites or reverts it, and nothing here writes the ledger.

## What this parcel added (review apparatus only)

| Path | What it is |
|---|---|
| `specs/pipeline/steps/bl1324ClaudeSeatQwenCloudContextWindowSteps.js` | acceptance step handlers for all 8 scenarios; drives the REAL `swarmforge.sh` under `zsh -f` |
| `specs/pipeline/steps/index.js` | registration (`required_wiring` anchor 4) |
| `extension/test/bl1324ClaudeSeatQwenCloudContextWindowInvariants.property.test.js` | one executable property per declared invariant |
| `backlog/evidence/BL-1324-coder-stampoff-4ed88430b2-20260902.md` | this file |

No production path is touched. `git diff --name-only` against the received tip
contains no `swarmforge/` script, pack conf or ledger change.

## qa_e2e_procedure, step by step

**(1) `extra_cli_targets_qwen_cloud` matching shape — CONFIRMED.**
Acceptance scenario 01 (Scenario Outline, 4 rows) calls the real function:
`--model qwen3.8-max --effort high` → true; `--model claude-sonnet-5 …` → false;
`--effort high` (no `--model` at all) → false; `--model=qwen3.8-max …` → false.
The matcher reads a space-separated `--model` token pair only.

**(2) billing-guard branch — CONFIRMED.**
Scenario 02 generates the real `.swarmforge/launch/coder.sh` with
`SWARMFORGE_USE_QWEN` unset and a `--model qwen3.8-max` seat: the script carries
`qwen_guard_map_anthropic_compat || exit 1` and
`export CLAUDE_CODE_MAX_CONTEXT_TOKENS="${CLAUDE_CODE_MAX_CONTEXT_TOKENS:-1000000}"`,
and does NOT carry the `unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN` first-party
branch. Scenario 03 shows a sibling `--model claude-sonnet-5` seat in the same
shape keeps that unset line and names neither the mapping call nor the Token Plan
host. Scenario 04 evaluates the guard's own export line with
`CLAUDE_CODE_MAX_CONTEXT_TOKENS=500000` pre-set and reads back `500000` — the
`:-` default does not overwrite an operator value. No credential VALUE reaches
the generated file (BL-130 re-checked in scenario 02).

**(3) `launch_role` pane-env branch — CONFIRMED.**
Scenario 05 runs the real `launch_role` with `tmux` faked to a logger and reads
the single `respawn-pane` command line, which is the observable form of
`provider_env_flags`. With `QWEN_API_KEY` present and the global flag unset it
carries `-e QWEN_API_KEY=<value>` and `-e CLAUDE_CODE_MAX_CONTEXT_TOKENS=1000000`.

**(4) pack conf — CONFIRMED AT THE REVIEWED COMMIT, and SUPERSEDED SINCE.**
Scenario 06 reads `swarmforge/packs/bob-multi-provider-mono-router.conf` via
`git show 4ed88430b2:…`. At that commit all seven pipeline window lines are
`claude … --model qwen3.8-max` and `config coordinator_model claude-sonnet-5`
(the coordinator is a `config` line, never a window seat).

> **Reported, not a defect:** a LATER operator commit, `441fd35112` ("Restaff bob
> BoB starting cast: Anthropic seats, coder on qwen3.8-max."), moved six of the
> seven seats back to `claude-sonnet-5`; only `coder` still requests
> `qwen3.8-max` in the working tree. That is a staffing decision made after the
> hotfix, not a refutation of it. Scenario 06 is therefore pinned to
> `4ed88430b2` on purpose — pointing it at the working tree would report a
> failure for a change the hotfix never made.

**(5) The two dormant gaps from `approval_context` — both CONFIRMED DORMANT.**

- **D-A. `--model=<value>` single-token form is not matched.** Confirmed live
  (scenario 01, row 4). Consequence if it ever fired: the seat keeps first-party
  Anthropic auth while targeting a Qwen model id. Dormant: every window line in
  every pack in `swarmforge/packs/` uses the space form.
- **D-B. OpenRouter branch precedence.** Confirmed by reading the landed
  `launch_role`: the `elif [[ "$agent" == "claude" ]] && extra_cli_targets_qwen_cloud …`
  branch is reached only after `role_uses_openrouter "$role"` has been ruled out,
  so an OpenRouter-marked role whose `--model` is `qwen*` would take the
  OpenRouter branch and receive neither the Qwen key nor the window declaration.
  Dormant: no pack combines OpenRouter routing with a `qwen*` `--model`.

Per the human ruling (option 2) these are **not fixed here** — the ticket
constraints forbid redesigning the matching shape or the branch precedence in
this parcel, and the follow-up ticket is the specifier's to mint. This stage's
recommendation to that follow-up: **match the `--model=<value>` form too, and
document the OpenRouter precedence in a comment at the `elif`** — the first is a
one-line matcher change with an existing test hook (scenario 01 row 4 flips from
`false` to `true`); the second needs no code change.

**(6) `Hotfix-Certification` — left `pending`.** `backlog/hotfix-ledger.yaml`'s
row for `4ed88430b2` is untouched by this parcel: `state: stamp-open`,
`stamp_ticket: BL-1324`, `human_decision: null`, `decided_at: null`. Acceptance
scenario 08 and invariant 2's property both assert this, and the property was
demonstrated to go red when the row is flipped to `certified` (probe below).

**(7) Global-flag cross-pack side effect — CONFIRMED as reported behaviour.**
Scenario 07 builds the guard with `SWARMFORGE_USE_QWEN=1` and finds the same
`CLAUDE_CODE_MAX_CONTEXT_TOKENS` declaration: the declaration sits on the SHARED
Token Plan compat branch, so every global-flag pack's Claude seats now declare
it, not only bob's. The commit's own comment says that is intended. **Left as
landed.** The 1M figure rests on the platform's claim that Token Plan `qwen3.*`
is a 1M window and is operator-overridable via the `:-` default; this review
verifies what the launch declares, not what the gateway serves.

**(8) Live-pack check — the bob pack is NOT live; no tmux probe was made.**
This session's pane reports `SWARMFORGE_PACK=full-forge`, so the bob pack's live
pane env is not observable and item (8)'s `tmux show-environment` path does not
apply. **No `tmux` command of any kind was run against the live server** (BL-657:
an operator client probe has killed a freshly restored server). Recorded instead,
per the ticket's own fallback: the simulated `-e` flags from a generated launch
script (item 3 above), and one read-only on-disk observation —
`.swarmforge/launch/coder.sh` for the LIVE full-forge coder seat
(`--model claude-opus-5`) carries `unset ANTHROPIC_API_KEY ANTHROPIC_AUTH_TOKEN`
and neither the Token Plan mapping nor the context declaration. That is invariant
3 holding on a real, running, non-qwen seat.

## Declared invariants — encoding and non-vacuity

All three are encoded as executable properties in
`extension/test/bl1324ClaudeSeatQwenCloudContextWindowInvariants.property.test.js`
(`npm run test:properties`). None is left unencoded, so no stated-reason clause
is needed.

| Invariant | Encoding | Generator reach |
|---|---|---|
| 1 — never rewrites/reverts the hotfix | quantifies over (hotfix artifact × revision), asserting the anchored `swarmforge.sh` hotfix regions are byte-identical to `4ed88430b2` at the **working tree**, at `HEAD`, and at every revision that has touched the file since; plus an attribution face asserting no revision touching a hotfix artifact is attributed to BL-1324 | every revision touching either artifact since the hotfix, not `HEAD` alone; the working-tree case is included because a rewrite there would otherwise pass every committed check |
| 2 — green tests never write the ledger | quantifies over the two verdict words × the reviewed commit's ledger row and every parcel-authored artifact naming that commit | scoped to the `4ed88430b2` row — the ledger legitimately holds decided rows for other commits, and a whole-file scan would fail on those |
| 3 — only a `qwen*`-targeted seat is remapped | drives the real matcher (240 generated cases, batched into one `zsh`) and the real generated launch script (8 + 8 runs) | **collision candidates by construction**: every sibling case is built by taking a real qwen model id and placing it everywhere in the CLI *except* immediately after `--model` (another flag's value, the `--model=` single-token form, a bare token, embedded mid-value, after a flag whose name merely starts with `--model`). Drawing model and decoy independently would almost never produce a string containing `qwen` at all, and the property would pass against a substring scanner. A generator assertion fails the test if any sibling case lacks a qwen id. |

**Non-vacuity probes** (each mutation applied, run, then reverted; working tree
verified clean afterwards):

| Probe | Deliberate break | Result |
|---|---|---|
| A | matcher degraded to a substring scan (`"$extra_cli" == *qwen*`) | invariant 1 **RED** (working-tree region drift) and invariant 3 **RED** (siblings detected) — the collision generator earns its keep |
| B | ledger row for `4ed88430b2` flipped to `state: certified` | invariant 2 **RED**, 1 and 3 green — correctly isolated |
| C | matcher's `qwen*` prefix changed to `zzzz*` | acceptance 0/11 (the Background's landed-commit pin) |
| D | billing-guard `\|\| extra_cli_targets_qwen_cloud "$extra_cli"` removed | acceptance 0/11 (same pin, which extracts by anchor and so cannot find the region at all) |
| E | ledger row flipped to `certified` | acceptance 10/11 — scenario 08 alone red, correctly isolated |

## Commands run

```
specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1324-claude-seat-qwen-cloud-context-window.feature
  → 11/11 pass (4 outline rows + 7 scenarios)
cd extension && npx vitest run --config vitest.properties.config.mjs \
  test/bl1324ClaudeSeatQwenCloudContextWindowInvariants.property.test.js
  → 3/3 pass
cd extension && npm run compile   → clean
```

## Pre-existing suite red (NOT introduced by this parcel)

`npm test` on the received tip is **25 failed / 9823 passed across 15 files**.
Every one of those 15 files was re-run with this parcel's three files removed and
`specs/pipeline/steps/index.js` reverted: **the same 25 failures, same files,
same counts**. This parcel adds no red. The failing files (for the record) are
`backendSwitch`, `constitutionDocCitations`, `crossFileDuplicationCheck`,
`liveRepoDerivationGuard`, `multiBranchParserCoverageCheck`,
`operatorRuntimeBbFixtureClosure`, `perHatRolePromptEvidenceCheck`,
`pilotAcceptanceGate`, `pilotScopedCrapCheck`, `shellEntryPointDriveCheck`,
`socketFixtureShortRootGuard`, `telegramClient`, `telegramCursorOperatorExec`,
`tempDirTrapGuard`, `unreachableStepHandlerCheck`.

## Ticket-less changes surfaced, not swept

Two untracked files sit in this worktree that this parcel did not create and does
not stage: `swarmforge/scripts/open_swarm_spy_router.sh` and
`swarmforge/scripts/spy_router_pane_label.sh`. Surfaced here per the
clean-up rule; neither is committed by this parcel.

Changed-path unit gate (Article 4.5): this parcel changes no production path —
the three added/edited files are an acceptance step handler, its registration,
and a property test. Their own execution is the gate, recorded above.
