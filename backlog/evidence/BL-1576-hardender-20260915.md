# BL-1576 — hardender review pass, 2026-09-15

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb swarmforge/scripts/test/merge_drop_guard_lib_test_runner.bb (hand-mutated: removed --reverse from merge-commits rev-list call)
- **Commit hash**: 654168ec7bab17a6da8ff2e7a497b5447f71a0fc
- **First error excerpt**: result: (merge2-sha merge1-sha) instead of (merge1-sha merge2-sha) - order untested
- **Failure class**: test-coverage
- **Expected vs observed**: oldest-first merge ordering per the lib docstring promise, discriminated by a test
- **Blamed role**: hardener
- **Remediation pointer**: added two-merge fixture asserting oldest-first order in merge_drop_guard_lib_test_runner.bb, commit 91479dc42c

## Detail

Merged QA's BL-1574 merge-up note (19e744c142) and architect's BL-1576
forward (654168ec7b) clean, no conflicts.

Gates run (all green, re-run directly):
- `bb swarmforge/scripts/test/merge_drop_guard_lib_test_runner.bb` — ALL PASS (with the new ordering test added).
- `bb swarmforge/scripts/test/bl1576_merge_drop_guard_property_runner.bb` — 2000 runs, ALL PROPERTIES HOLD.
- `specs/pipeline/scripts/run_acceptance.sh` on BL-1576's feature — 11/11.
- `specs/pipeline/scripts/run_acceptance.sh` on BL-1213's, BL-1205's, BL-760's features (sibling send-time gates) — 8/8, 9/9, 9/9, all still green, none edited.
- `extension/test/*Guard*.test.js` (18 whole-tree guards, this parcel touches `specs/pipeline/steps/`) — 183/183.

No orphaned test processes before or after; no leaked `/tmp/bl1576-*` fixture dirs after any run (`pgrep -fl 'node --test|stryker'`, `ls /tmp | grep -c bl1576`).

Babashka has no wired mutation/CRAP/DRY tooling (Engineering Rules,
Startup Tools) — hand-authored mutation sweep per the BL-638 fallback.
The coder's own pass already proved non-vacuity on three mutants
(`contested?`→false, `revert-excuses?`→false,
`merge-commits`→`--all`). This pass probed a fourth: dropping
`--reverse` from `merge-commits`' `git rev-list --merges` call, which
survived the entire existing suite (unit, property 2000 runs,
acceptance) because no fixture built two merges inside one
`received..forwarded` range. Confirmed real (not equivalent) by
constructing such a fixture and observing the order flip; fixed by
adding that fixture as a permanent regression test, confirmed red
against the mutant and green against the restored original.

CRAP/DRY: not applicable — all three changed/added files are `.bb` or
a `specs/pipeline/steps/*.js` step handler, outside `extension/src/*.ts`
CRAP scope and outside `jscpd`'s `extension/` DRY scope.

By hardender.
