# BL-1365 — cleaner pass, 2026-09-05

## Review

New production code splits cleanly along invariant 1's own line: the pure
fold (`ritualLedger.ts` — classification, stats, candidate selection), the
cadence (`ritualLedgerProducer.ts` + CLI + `handoffd.bb` sweep), and the read
(`closingCeremonyRun.ts`, `closingCeremony.ts`'s new field). This matches the
ticket's own described architecture; no restructuring needed.

**Mutation-site advisory (BL-485).** `mutation-site-count.js` flags two
files:

- `ritualLedger.ts` — 140 sites, 40 over. Entirely new this parcel, but a
  single cohesive concern (classify → count → score dominance → select
  candidates), all sharing the same domain types (`RitualClass`,
  `RitualClassStats`) with no independent reuse elsewhere. Splitting
  classification from scoring would separate tightly-coupled steps of one
  pipeline for no structural gain — the legitimately-cohesive-module
  exemption BL-485 itself names. Left whole.
- `closingCeremony.ts` — 494 sites, but this parcel's touch is five small
  additive hunks (one interface, one field, one default param, one
  `isEmptyCeremonyPacket` clause) to a large pre-existing file. Out of scope
  to split here.

**DRY**: the coder's own consumer sweep already found and fixed the two
hand-written `skipReasons` copies (unit test, acceptance step) before this
reached me — nothing left to flag.

**Architecture**: `RITUAL_CLASSES` restricted to the four bookkeeping path
areas (not all of `extension/src/`) is the right line — the live-repo run
documented in the coder's evidence shows the wider list produces 9
candidates (the alert-fatigue failure the ticket names first), while the
narrowed list reproduces the ticket's own measured dominance figures almost
exactly (0.945 vs 0.97, 0.009 vs 0.01). Suppression by `ritual_class:` field
match (exact) rather than title or path substring is also the right call —
documented against measured false-positive/false-negative rates (23/104
tickets over-suppressed by prefix matching).

## Verification (re-run, all green)

- `extension/test/ritualLedger.test.js` — 27/27.
- `extension/test/ritualLedgerProducer.test.js` — 18/18.
- `extension/test/closingCeremony.test.js` (existing, owns the changed type)
  — 38/38.
- `extension/test/bl1365RitualLedgerInvariants.property.test.js`
  (`npm run test:properties` config) — 4/4.
- `specs/features/BL-1365-the-ceremony-packet-names-hand-made-rituals.feature`
  via `node specs/pipeline/cli.js` — 5/5.
- `specs/features/BL-820-closing-ceremony-lean-pass.feature` (regression,
  owns the packet) — 12/12.
- `specs/features/BL-923-dwell-counts-occupied-time-not-parcel-sum.feature`
  (regression) — 5/5.
- `specs/features/BL-1119-closing-ceremony-role-quality-dial.feature`
  (regression) — 6/6.
- `swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh` (daemon boots
  with the new sweep) — ALL PASS.
- `closingCeremonyRun.ts::determinismCandidates` and
  `handoffd.bb::ritual-ledger-producer-sweep` — confirmed real call sites,
  not comments.

## Relayed, not this parcel's

Sent a priority-50 note to hardender: the coder's own observation that
BL-1364's step handler (`bl1364MechanicalShareReadableSteps.js:56`) uses
`mkTmpDir` rather than `mkProcessTmpDir`, so its fixture root leaks under
`run_acceptance.sh` (no `afterEach` hook there, unlike Vitest). BL-1364 is
already past architect; not this parcel's defect to fix or bounce, just
relaying so it isn't lost between worktrees.

## Forwarding

No code changes made. Forwarding to architect.
