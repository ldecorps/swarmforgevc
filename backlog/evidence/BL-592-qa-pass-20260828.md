# BL-592 — QA pass — 20260828

**Received:** `merge_and_process documenter 82e422a910` (handoff
`00_20260828T004012Z_000867_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `82e422a910cfd2d3eb9202b74cfb3019225bcd88`
**QA merge commit:** `7950aa666de487f84613b30987a5877709a4e358`

## Ancestry check (BL-336 guard)

- `git merge-base --is-ancestor e2f33eaec 82e422a910` (coder) — CONFIRMED
- `git merge-base --is-ancestor 81e7c6567 82e422a910` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 9920ff282 82e422a910` (architect) — CONFIRMED
- `git merge-base --is-ancestor feabd9825 82e422a910` (hardener) — CONFIRMED

## Followed the specifier's own pre-flight hazard analysis verbatim

The specifier pre-emptively investigated this exact land
(`backlog/evidence/BL-592-specifier-land-hazard-silent-drop-20260828.md`,
merged into this worktree earlier this session) and found: `main` and this
parcel's tip share **two** merge bases, so `git merge`'s virtual-base
resolution takes the delete side for every BL-592 path that descends
unchanged from that virtual base — **silently, no conflict, no red gate**.
The specifier's table named 8 paths that would disappear and the exact
consequence (the acceptance handler's absence throws on land, per BL-233,
blocking the next parcel through the gate too).

Ran the systematic `comm -23` gap check this session's BL-1188 parcel
established (compare the sender's tip's tagged paths against the current
tree by pattern, not the diff stat) — it reproduced the specifier's table
exactly: 8 files missing (`specTreeUiHtml.ts` 254 lines,
`bl592SpecTreeOnLiveConsoleWithEpicTierSteps.js`, its property test, 5
evidence files). Restored all 8 via targeted `git checkout <their-tip> --
<path>`. Registered the missing
`require('./bl592SpecTreeOnLiveConsoleWithEpicTierSteps')` in
`specs/pipeline/steps/index.js` — confirmed working by the acceptance run
below (8/8 PASS proves the handler resolved, not just that the file
exists). Re-ran the gap check after restoring — empty.

## Deliberately declined BL-1189's bundled content — an active hold, not an oversight

This tip also carries BL-1189's own evidence trail and
`residentPaneLive.ts`/`residentPaneSpy.ts` changes via ordinary branch
ancestry (documenter's branch is linear; BL-1189 precedes BL-592 in it).
**BL-1189 is QA's own prior parcel this session, actively ON HOLD**
(`backlog/evidence/BL-1189-qa-holds-20260828.md` — documenter itself
decided not to forward it, pending the specifier's disposition of BL-1211,
an unresolved provenance question on a resurfaced previously-bounced-and-
reverted file). Landing BL-592 must not silently ALSO land BL-1189's held
content as a side effect of sharing a branch. Declined
`residentPaneLive.ts`/`residentPaneSpy.ts`/their tests and BL-1189's
evidence files via `checkout HEAD --` / unstage-remove. Verified
post-resolution: `dedupePrimaryWorkingTicket` absent from
`residentPaneSpy.ts` (0 grep hits) — correctly still held, not
accidentally reintroduced.

Also kept `backlog/evidence/documenter-duplicate-ticket-pools-20260828.md`
(documenter's own informational finding about this session's duplicate-pool
incident — same disposition as when it first appeared during BL-1190's
parcel: no functional risk, useful reference).

Left the six pool-move paths the specifier's note enumerated (BL-1184,
BL-428, BL-644, BL-751, BL-1200 → `done/`; BL-1186 `paused/` → `active/`)
at the merged tree's already-correct location, untouched by this commit.

## What BL-592 delivers

Milestone → Epic → BL item → Gherkin navigation tree on the live console
(`/spec-tree`, `/spec-tree-state`), read-only, reusing `computeDocsTree`
(schema bumped to v2 for epic grouping); PWA keeps its existing
milestone→ticket drill via epic flattening (the static PWA artifact
deliberately does NOT serve the epic-tier view — confirmed by its own
acceptance scenario, consistent with the constitution's two-surface
boundary rule for phone-viewable surfaces).

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-592` | `VERIFY BL-592` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean |
| Targeted unit | `npx vitest run` on 5 changed/new test files | 4 files/110 tests PASS clean. `specTreeBridge.test.js`: 4/8 tests fail, all `CURSOR_API_KEY is not set for the headless bridge` — the same standing BL-720/728 host-env debt confirmed twice already this session (BL-644, BL-751 passes), reproduced again here via the same `startBridge` test helper other files in that debt already use. Not a BL-592 defect. |
| Targeted property | `npx vitest run --config vitest.properties.config.mjs test/bl592SpecTreeEpicTierInvariants.property.test.js` | 2/2 PASS |
| Acceptance | `run_acceptance.sh specs/features/BL-592-*.feature` | 8/8 PASS — also proves the step-handler registration fix actually resolves (not merely present on disk) |
| required_wiring (all 4 rows) | grep each anchor | `DOCS_TREE_SCHEMA_VERSION = 2` present; `/spec-tree`/`/spec-tree-state` routes present in bridgeServer.ts; console menu link present in consoleMenuUiHtml.ts; step registration present and proven-working by the acceptance run |
| Invariants (all 3) | acceptance scenarios above | "read-only" — scenario 6. "every ticket under exactly one epic bucket, including (no epic)" — scenarios 2/3. "cross-milestone epic duplicates per milestone, filtered to that milestone's members" — scenario 5. |
| Regression: previously-landed features intact | grep spot-check | `shift-velocity` (6 hits, briefing_email_lib.bb), `git_env_guard.sh` (on disk), BL-1188's `readLiveRoleHeldTickets` (3 hits) — all still present after this merge |
| Full unit/property suite | not re-run a sixth time this session | Same reasoning as this session's other hold/-family passes: standing debt already reconfirmed above at the targeted level; live branch-corruption risk already sighted once this session. |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-592 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping — ticket already
correctly pooled in `active/` per the specifier's own disposition, no pool
flag needed. BL-1189 remains ON HOLD, unaffected by this landing.

By QA.
