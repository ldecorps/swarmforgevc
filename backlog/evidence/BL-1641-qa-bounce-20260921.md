# BL-1641 — QA bounce, 2026-09-21

1 defect found (pre_qa_gate ancestry check). Batch parcel with BL-1640
(Article 2.6): BL-1640 has no failing check of its own and is forwarded
separately as its own clean git_handoff; this bounce covers BL-1641 only.

## D1

- **Failing command**: `bash swarmforge/scripts/pre_qa_gate.sh BL-1641 bca21ee5d3`
- **Commit hash**: bca21ee5d3 (QA's merge tip: documenter's forwarded
  8fbd62bff5 merged into QA)
- **First error excerpt**:
  ```
  PRE_QA_GATE_FAIL ancestry BL-1641 ee58ac07b3 stranded on swarmforge-architect
  PRE_QA_GATE_FAIL ancestry BL-1641 ee58ac07b3 stranded on swarmforge-cleaner
  PRE_QA_GATE_FAIL ancestry BL-1641 ee58ac07b3 stranded on swarmforge-coder
  PRE_QA_GATE_FAIL ancestry BL-1641 ee58ac07b3 stranded on swarmforge-documenter
  PRE_QA_GATE_FAIL ancestry BL-1641 ee58ac07b3 stranded on swarmforge-hardender
  ```
- **Failure class**: behavior (a real, live bug remains present in the
  parcel QA received - not a coverage/wiring gap)
- **Expected vs observed**: expected the parcel QA received for BL-1641 to
  be the tip of the review chain that actually fixed the architect's own
  bounce (`backlog/evidence/BL-1641-bounce-20260921.md`, "mainHasBriefing
  fails OPEN on a git read error"); observed that the documenter's
  git_handoff to QA (commit 8fbd62bff5) is on a lineage that DIVERGED from
  the fix at `d9596a138e` (architect's own bounce-review merge) - the
  coder's actual fix commit `ee58ac07b3` ("BL-1641: bounce fix (D1) -
  mainHasBriefing now actually fails closed on a git read error") is
  present on swarmforge-coder/cleaner/architect/hardender/documenter but
  is NOT an ancestor of what reached QA
  (`git merge-base --is-ancestor ee58ac07b3 HEAD` -> not an ancestor
  against QA's own tip built from 8fbd62bff5). Concretely: the version of
  `night-closing-ceremony-run.ts`'s `mainHasBriefing` that QA received
  still fails OPEN (`catch { return false; }`) on any git-level read
  error, contradicting the ticket's own FIRM invariant ("a briefing main
  already has is never replaced or rewritten") and the function's own
  documented contract - exactly the defect the architect already bounced
  once and the coder already fixed, on a branch that never made it here.
- **Blamed role**: coder (the earliest role that can rebuild the correct
  chain: `ee58ac07b3` and the two review passes on top of it - architect,
  cleaner, hardener, documenter - already exist on the respective role
  branches; what is missing is a fresh, correctly-lineaged git_handoff
  chain from that commit through to QA, since the one that reached QA
  skipped it)
- **Remediation pointer**: `extension/src/tools/night-closing-ceremony-run.ts`
  `mainHasBriefing` (coder's own fix at commit `ee58ac07b3`, already on
  `swarmforge-coder`). Rebuild/forward the parcel for BL-1641 from that
  commit (or a fresh merge of it) through the normal chain - do not
  resend the `8fbd62bff5`-based lineage; it is missing this fix entirely.

## Everything else checked this pass for BL-1641 (before the ancestry
   check ran) — no other defect found

- `bash swarmforge/scripts/test/test_bl1641_ceremony_deadline_produces_a_briefing.sh`:
  11/11 PASS (after a stale `out/` recompile - see note below).
- `node specs/pipeline/cli.js specs/features/BL-1641-a-ceremony-at-its-briefing-deadline-produces-a-briefing-before-the-stop.feature`:
  4/4 ok.
- `npx vitest run --config vitest.properties.config.mjs test/bl1641EnsureBriefingInvariants.property.test.js`:
  2/2 pass.
- `bash swarmforge/scripts/test/test_compose_banked_briefing_cli.sh`: 4/4 PASS.
- Wiring: `night-closing-ceremony-run.ts` shells `compose_banked_briefing_cli.bb`
  (confirmed present); `bl1641CeremonyDeadlineProducesABriefingSteps.js`
  exports `registerSteps` (confirmed present).

Note: an initial run of the above (before a `npm run compile`) showed the
same `mainHasBriefing`-shaped failure surfacing as unit-test failures in
`test/nightClosingCeremonyRun.test.js` - that was a STALE compiled `out/`
from an earlier verification pass in this same session, not evidence of a
second defect; after `npm run compile` all of the above went green. The
ancestry gate (which reads git history, not compiled output) is the real,
reproducible finding and is what this bounce is about.

By QA.
