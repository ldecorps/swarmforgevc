# BL-1358 — architect review, 2026-09-04

Reviewed coder commit merged in via cleaner `14426b2429`.

## Checks run

- Dependency gate on `specs/pipeline/steps/bl1358MutantTimeoutKilledSteps.js`:
  PASSED, no forbidden edges.
- Co-change: expected acceptance-pipeline hub coupling; no new smell.

## Re-verified independently, not trusted from evidence

- `node --test specs/pipeline/test/bl1358MutantTimeCeiling.test.js` — 6/6,
  including the process-group-reclaimed assertion.
- `npx vitest run --config vitest.properties.config.mjs
  bl1358MutantTimeCeiling` — 2/2 (the combined P1+P2 pass and the P1
  no-hang control).
- `run_acceptance.sh` on the BL-1358 feature — 4/4.
- `node --test` on `runnerAdapter.test.js`, `gherkinMutation.test.js`,
  `endToEnd.test.js`, `cli.test.js` — 17/17, matching the evidence's
  regression count exactly.

## Correctness read

- `DEFAULT_MUTANT_TIMEOUT_MS = 300000` matches the human ruling exactly
  (option 1, 300s). `resolveMutantTimeoutMs` correctly falls back to the
  default on any unparseable/non-positive override — an env-var typo
  cannot silently disable the ceiling, which is the state the ticket
  exists to end.
- The kill mechanism is correctly reasoned: `detached: true` puts the
  spawned child in its own process group, so `spawnSync`'s built-in
  timeout (which only reaches the direct child) is backstopped by a
  manual `process.kill(-result.pid, 'SIGKILL')` on `ETIMEDOUT` — the
  negative pid targets the whole group, matching the ticket's explicit
  direction and this repo's own prior `kill -KILL -- -PGID` scar. The
  `catch` around it correctly treats ESRCH (group already gone) as the
  wanted outcome rather than an error.
- `success: !timedOut && result.status === 0` correctly means a timed-out
  mutant is neither `test_success` nor a normal failure — it maps to
  `infrastructure_error` in the worker, which `classifyOutcome`
  (`survived > 0 || errors > 0`) already fails the gate on. Verified by
  reading `gherkinMutationClassify.js` directly: this correctly satisfies
  the human ruling (option 1: timeout fails the gate like a survivor)
  without touching the pinned vendored mutator's outcome vocabulary or
  the classifier itself — exactly the "no second copy of the rules"
  discipline this session's other reviews have been enforcing.
- The `detached: true` trade-off (Ctrl-C to the harness no longer
  propagates to the mutant by group signal) is correctly named as a
  behavior change rather than left implicit, and is a net improvement
  since the mutant is now bounded regardless.

## Verdict

COMPLIANT. Forwarding to hardener.
