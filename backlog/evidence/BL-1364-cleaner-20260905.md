# BL-1364 — cleaner pass, 2026-09-05

## Review

New production code: `extension/src/metrics/turnProfileProducer.ts` (the
missing consumer), `extension/src/tools/run-turn-profile-producer.ts` (thin
CLI wrapper), plus small additive changes to `transcriptWalker.ts`
(`INTERVAL_CATEGORIES` exported as a runtime tuple, replacing a type-only
enumeration) and `turnProfile.ts` (`categoryShares`, keyed from that tuple).
`swarmforge/scripts/handoffd.bb` gets one new sweep beside the BL-665
sibling, deliberately ordered after it per the sci var-resolution note in the
diff.

**Mutation-site advisory (BL-485).** `mutation-site-count.js` flags two
files:

- `turnProfileProducer.ts` — 113 sites, 13 over. Its sibling producer,
  `contextTelemetryProducer.ts` — the exact shape this ticket's notes direct
  the coder to follow — is itself 117 sites and has never been split.
  Splitting this file would break consistency with the established pattern
  for a difference of 13 sites and no structural gain: a legitimately-cohesive
  module a split would only harm (BL-485's own exemption). Left whole.
- `transcriptWalker.ts` — 291 sites, but this parcel only lightly touches it
  (exports `INTERVAL_CATEGORIES` as a value; no classification logic moved
  or added). It predates this ticket (BL-664) and splitting a walker this
  parcel didn't substantially grow is out of scope creep. Left whole.

**Self-audit already folded two builders into one** (`assembleWindowRecord`,
in the coder's own pass) — the DRY concern I'd otherwise raise on
`buildTurnProfileWindowRecord` / `buildTurnProfileWindowForGroups` diverging
was already caught and fixed before this reached me.

**Architecture**: the producer separates readability assessment
(`assessTranscriptReadability`), window assembly (`assembleWindowRecord`),
persistence (`turnProfileStorePath`, `readPersistedTurnProfileWindows`,
`filterNewTurnProfileWindows`, `upsertWindowRecord`) and orchestration
(`runTurnProfileProducer`) into distinct functions within the one file —
clean separation of concerns even without a file split. The CLI
(`run-turn-profile-producer.ts`) stays a thin wrapper per engineering.prompt.

No duplication found beyond what the coder already resolved.

## Verification (re-run, all green)

- `extension/test/turnProfileProducer.test.js` — 15/15.
- `extension/test/transcriptWalker.test.js` (regression) — 3/3.
- `extension/test/bl1364TurnProfileSeriesInvariants.property.test.js`
  (`npm run test:properties` config) — 4/4.
- `specs/features/BL-1364-the-mechanical-share-of-a-turn-is-readable.feature`
  via `node specs/pipeline/cli.js` — 8/8 (post scenario-05 amendment).
- `specs/features/BL-664-*.feature` (regression, owns the changed modules)
  via `node specs/pipeline/cli.js` — 11/11.
- `swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh` (daemon boots
  with the new sweep) — ALL PASS.
- `swarmforge/scripts/handoffd.bb::turn-profile-producer-sweep` — confirmed
  a real call site, ordered after `context-telemetry-producer-sweep!`.
- `extension/src/metrics/turnProfileProducer.ts::buildTurnProfileSeries` —
  confirmed a real call (required_wiring anchor), not a comment.

## Forwarding

No code changes made. Forwarding to architect.
