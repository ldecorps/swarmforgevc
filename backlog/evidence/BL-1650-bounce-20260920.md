# BL-1650 — QA bounce, 2026-09-20

Full checklist run (Article 4.4): compile clean; BL-1650's own acceptance
feature 4/4 green; BL-1546's acceptance feature 4/4 green (re-verifying the
cleaner's own-pass fixture fix); `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`
ALL PASS; `pre_qa_gate.sh` OK; full unit suite 632/632 files, 10802/10802
tests green; property suite 427/427 files, 1246/1246 tests green (one
`bl1142LocalOllamaPackShape.property.test.js` failure judged host-load
flake — exit 141 (SIGPIPE) from a spawned subprocess under this host's
concurrent multi-worktree contention, reproduced clean 3/3 in isolation,
unrelated to this parcel's diff). One defect found while attempting to
land this parcel through the very mechanism it ships.

## D1

- **Failing command**: `bb swarmforge/scripts/land_step_cli.bb BL-1650 a525f508c62d35ba15bf65fa505dd8c26fe94cde`
  (landing BL-1650's own approved commit, synced onto current `origin/main`,
  through its own new stray-cherry-pick logic)
- **Commit hash**: a525f508c62d35ba15bf65fa505dd8c26fe94cde
- **First error excerpt**:
  ```
  LAND_ESCALATE
  ENTANGLED_SIBLING BL-1648
  BL-1650: entangled tip - sibling ticket(s) BL-1648 unlanded as ancestors, tip-pure replay could not complete cleanly; specifier adjudication needed.
  land-step replay: could not cherry-pick stray evidence commit 6d63104e70b204de338443a7c860c453ae31172b
  ```
  Reproduced the underlying git call directly in an isolated scratch clone
  of `origin/main`:
  ```
  git cherry-pick -x 6d63104e70b204de338443a7c860c453ae31172b
  The previous cherry-pick is now empty, possibly due to conflict resolution.
  If you wish to commit it anyway, use:
      git commit --allow-empty
  ...
  nothing to commit, working tree clean
  ```
  Confirmed the stray's own file
  (`backlog/evidence/BL-831-coder-forward-gate-false-refusal-20260918.md`)
  is already byte-identical on `origin/main` (landed via the specifier's
  manual `cherry-pick -x` during the BL-1636 adjudication, commit
  710b07570b, the day before BL-1650 shipped this same cherry-pick as
  automation).
- **Failure class**: behavior
- **Expected vs observed**: Expected (per the ticket's own FIRM constraint,
  written for the sibling-scoring side of this same fix: "an ancestor whose
  every path is byte-identical to main ... is reported as landed, never as
  entangled") — a stray commit whose content is already present on
  `origin/main` should be recognized as already-landed and the replay
  should proceed. Observed: `land_step_lib.bb`'s stray-cherry-pick loop
  (`replay!`, the `BL-1650 items 1-2` block, around line 2139) treats ANY
  nonzero `git cherry-pick -x` exit — including the ordinary
  "now-empty, nothing to commit" case git produces for an already-applied
  patch — identically to a real conflict, aborts the cherry-pick, and
  fails the whole replay closed (`LAND_ESCALATE`). This blocks every
  future parcel whose ancestry carries an already-landed stray of this
  exact shape, which is now most of the pipeline's branches (the ticket's
  own description: "Every branch that merged the coder branch since
  carries it").

By QA.
