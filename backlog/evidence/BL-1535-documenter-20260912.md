# BL-1535 — documenter review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

Reviewed commit: 814cb7f15a (hardender's forward), merged into this
worktree at 6c9a0b3... via `git merge 2d84596e04` (hardender's evidence
commit, no conflicts).

Change scope: `mono_router_lib.bb::should-rotate-resident?` gains a
`:departing-mid-parcel` refusal branch; `handoffd.bb` gains
`departing-working-signal`/`attempt-resident-rotate!` wiring; no
extension TS/JS production code, no new command/setting/UI.

Doc updates made this pass (commit 66cf86d0c2):
- `swarmforge/handoff-protocol.md`: new "Chase never departs a working
  mid-parcel resident (BL-1535)" section, same pattern as the adjacent
  BL-921/BL-795/BL-926 mono-router chase sections it sits beside.
- `docs/reference/Specification.MD`: new changelog entry at the top,
  "Last Updated" bumped to 2026-09-12, prior entry preserved under
  "Prior entry —".

Diagrams: checked `docs/diagrams/architecture.mmd` and
`docs/diagrams/handoff-flow.mmd` against the constitution's per-diagram
change-triggers. Neither fires — this ticket adds a refusal predicate
inside the existing "chase sweep wakes it / rotate resident" decision,
not a change to the extension-host/webview/tmux boundary, the pipeline
topology, or the inbox/in_process/completed handoff state machine. No
diagram edit made.

Considered `docs/how-to/BL-576-aged-note-actionability-mono-router.md`
(references `note_actionable_after_ms`, which this ticket's
`fresh-audit-challenge?` reuses per the ticket's own FIRM constraint).
Its "busy gate ... is unchanged" line stays accurate at that doc's level
of detail (it documents the note-aging threshold, not the full rotation
decision tree); no edit needed.
