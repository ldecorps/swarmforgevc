# BL-1195 — QA pass — 20260828

**Received:** `merge_and_process documenter 87daee81be` (handoff
`00_20260828T004651Z_000870_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `87daee81be1a9ffe2b3ed004f13437cf9e2106a5`
**QA merge commit:** `474dab57bbdce86a41f7f6881cbfe81ec7c904e3`

## Ancestry check (BL-336 guard) — against the FINAL re-fix chain

This ticket went through two full bounce-and-refix cycles before reaching
QA (D1: false-flagged a master-resident sibling's own legitimate WIP; the
first re-fix only widened the exemption rather than removing the guard's
wrong premise for the shared checkout, bounced again; the final shape
exempts `master`-resident worktrees from the guard entirely). Checked
ancestry against the LAST attempt's commits, not an earlier superseded one:

- `git merge-base --is-ancestor f0365f155 87daee81be` (coder, final re-fix) — CONFIRMED
- `git merge-base --is-ancestor fa62fc86c 87daee81be` (cleaner, final) — CONFIRMED
- `git merge-base --is-ancestor c4fb41408 87daee81be` (architect, pass round 2) — CONFIRMED
- `git merge-base --is-ancestor 311fb5521 87daee81be` (hardener, harden round 2) — CONFIRMED

## Restored 9 files QA's own earlier BL-1199 decline had removed

Same class of self-correction as this session's BL-1188 parcel: BL-1195
was correctly declined out of BL-1199's merge earlier this session (its
own evidence showed a `hardener-bounce`, meaning it was still in-flight at
that time — not an oversight, the right call given the information then).
Now that it has completed its own full pipeline, a plain merge would not
auto-restore what that decline removed (same-at-merge-base / deleted-by-us
/ untouched-by-them resolves silently to the deletion). Ran the systematic
`comm -23` gap check against documenter's tip filtered to
`1195`/`worktree-drift` paths; found and restored 8 files
(`worktree_drift_lib.bb`, its unit/property/wiring tests, step handler, 3
evidence files) plus re-added the step registration in
`specs/pipeline/steps/index.js` (also removed by that same earlier
decline). Re-ran the gap check after restoring — empty.

Two shared files had genuine three-way conflicts against HEAD's side
(`ready_for_next.bb`, `suite-manifest.tsv`) — both single-hunk, HEAD
completely empty at that hunk (nothing to lose), took theirs wholesale for
both.

## What BL-1195 delivers

`worktree_drift_lib.bb`: a pre-turn guard in `ready_for_next.bb` (same
posture as the existing BL-640 freshness guard and BL-1084 supersede
guard) that refuses when a role's tracked working-tree content differs
from its own worktree's HEAD for a reason its current in-progress task
does not explain — degrading to a silent pass on any git hiccup, only ever
refusing when it can positively name real drift. **Master-resident
worktrees (`coordinator`, `specifier`) are exempted entirely**: the shared
checkout is a genuinely concurrent, multi-writer surface by design
(`commit_integrity_lib.bb`'s own header names five distinct writers with
no isolation), so no parcel-shaped signal can distinguish a legitimate
writer's WIP from real drift there — same structural exception already
applied by `check_branch_namespace.bb`, `post_qa_branch_sweep_lib.bb`, and
`pre_qa_gate_gather_lib.bb`.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1195` | `VERIFY BL-1195` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean (this ticket is pure babashka + acceptance, no TS changes) |
| Unit | `bb .../worktree_drift_lib_test_runner.bb` | ALL PASS |
| Property | `bb .../worktree_drift_lib_property_runner.bb` | ALL PROPERTIES HOLD, 100 runs |
| Shell wiring (base guard) | `test_worktree_drift_guard.sh` | 3/3 PASS |
| Shell wiring (master-resident exemption) | `test_worktree_drift_guard_master_resident_exempt.sh` | 3/3 PASS, including "only master exempted, other roles keep full detection" |
| Acceptance | `run_acceptance.sh specs/features/BL-1195-*.feature` | 3/3 PASS |
| Invariant | shell wiring evidence above | "tracked content never silently forwarded when it differs from HEAD for an unexplained reason" — directly exercised by scenario 01/02/03 |
| Sanity: `ready_for_next.bb` still runs after this session's own edit to it | invoked directly | Loaded and ran without error, correctly reported this session's own current in_process parcel state — no crash, no unintended state mutation |
| Regression: previously-landed/held features intact | grep spot-check | `shift-velocity` (present), `git_env_guard.sh` (present), `named_tunnel_liveness_lib.bb` (present, BL-1199 just landed) |
| Full unit/property/vitest suite | not re-run again this session | Zero TS/JS production paths touched; Article 4.5 scopes to changed paths, all covered above at the targeted level. |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1195 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping — ticket already
correctly pooled in `active/`, no pool flag needed.

By QA.
