# BL-1663 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Reviewed the sole coder commit (0a06a32707): `fc.sample` draws the 40
  inputs once, a single `spawnSync('bb', ...)` maps
  `land-step-lib/replay-commit-refusal-reason` over all of them, assertions
  per draw unchanged, reachability floor unchanged.
  `grep -c "spawnSync('bb'" extension/test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
  = 1, outside the property callback.
- `npx vitest run --config vitest.properties.config.mjs test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
  alone: 1/1 pass, ~0.2-0.5s (well under 1s, matches qa_e2e_procedure).
- `node specs/pipeline/cli.js specs/features/BL-1474-the-replay-reports-why-its-commit-was-refused.feature`:
  5/5 ok, unchanged (constraints: no feature change).
- `npm run test:properties` (extension/): first run showed 1 failed file
  (`bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`, a
  narrow-margin require-cost timing budget, 214ms vs 200ms) — unrelated
  to this ticket's diff, already documented as a contention flake in
  `backlog/evidence/unowned-red-bl1630-module-load-budget-timing-flake-20260920.md`,
  not registered/ticketed (BL-1630's own test, non-deterministic).
  Re-ran once: 432/432 clean, confirming the flake (one allowlisted
  `[vitest-worker]: Timeout calling "onTaskUpdate"` benign error, BL-871,
  in both runs).
- `npm test` (extension/): 635 files / 10819 tests passed.
- `pre_qa_gate.sh BL-1663 3e6afb9112`: OK (one WARNING, subject-only
  ancestry with no path overlap - benign).
- No straggler test/mutation processes in this worktree before or after
  (concurrent vitest activity observed belonged to the coder worktree,
  a different worktree, not this parcel).
- `qa-sibling-check.js status --ticket BL-1663`: VERIFY (no open
  deferral).
- Standing-red register row for this file names BL-1663 as owner; this
  land retires it (BL-1631).
