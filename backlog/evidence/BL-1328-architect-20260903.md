# BL-1328 — architect pass (2026-09-03)

## Merge

Fast-forward merge of cleaner `00d2944818` (coder `376228c909` →
`fbf4c31003` [bounced+reverted] → `db083992a6` retirement →
`d702581188` rework). No conflicts.

## Process note (not a defect, recording for context)

This ticket went through a full, correctly-executed amendment cycle: the
coder hit BL-1324's own byte-exact property pin blocking this ticket's
required change, raised a priority-00 spec-gap note rather than guessing,
the specifier amended BL-1328 with a scoped `retires:` for the two
BL-1324-owned artifacts (commit `e8f6f1bd67`, authored "By specifier"),
the coder implemented the retirement, the cleaner correctly bounced once
for an interim version that flipped a value rather than retiring it
(D1 in `BL-1328-cleaner-bounce-20260903.md`), and the coder reworked to a
proper retirement (comment + successor-coverage pointer, per BL-1006:
"retire, never reword"). Verified this whole chain by reading the actual
commits (`e8f6f1bd67`, `db083992a6`), not just trusting the evidence
narrative.

## Production fix review

Gap 1 (`swarmforge.sh::extra_cli_targets_qwen_cloud`): added
`if [[ "${parts[i]}" == --model=qwen* ]]; then return 0; fi` alongside the
existing space-separated check. Confirmed only the qwen* VALUE decides in
either form — `claude-qwen-lookalike` (a non-qwen-prefixed model containing
"qwen") correctly stays undetected in both the shipped code and the
property test's negative pool.

Gap 2: both call sites (`billing_guard` construction ~line 1975,
`launch_role` pane-env construction ~line 2259) now carry a "BL-1328
PRECEDENCE ASYMMETRY" comment naming the other site's opposite order and
the resulting credential/endpoint mismatch if ever combined. Confirmed
neither precedence actually moved — `billing_guard` still checks
`extra_cli_targets_qwen_cloud` before `role_uses_openrouter`; `launch_role`
still checks `role_uses_openrouter` (the `elif` above the pane-env branch)
before `extra_cli_targets_qwen_cloud`. Matches invariant 3's "documented,
not reconciled" requirement exactly.

No `required_wiring` declared — confirmed absent from the ticket, matching
its own stated reasoning (no new call site; an anchor would only name a
symbol this parcel itself introduces).

## Retirement review (BL-1324's assertions)

Read the actual diff, not just the narrative: BL-1324's feature retired
exactly ONE Examples row (`--model=qwen3.8-max --effort high | false`),
with a comment explaining why and pointing to BL-1328's own Outline as
successor coverage — never flipped to `true` (which would have made the
retirement invisible in history). The other three rows are untouched.
BL-1324's property-test invariant 1 had its `content`/`worktree` faces
(byte-exact freeze) retired in favor of the `attribution` face it already
had (no BL-1324-attributed commit touches a hotfix path) — this is the
same content-vs-attribution pattern already established for BL-1323 this
session, applied consistently rather than invented ad hoc. Confirmed
non-weakening: BL-1324's invariants 2 and 3, and every billing-guard/
context-window behavioral scenario, are untouched and still assert real
behavior (`qwen_guard_map_anthropic_compat`, `CLAUDE_CODE_MAX_CONTEXT_TOKENS`),
not merely "the file still exists."

## Collateral fix reviewed (BL-1330 step file)

A 12-line documentation comment (gap 2's precedence comment) pushed a
regex proximity window in `bl1330SwarmStampBobAnthropicStartingCastSteps.js`
past its `{0,700}` character cap. Widened to `{0,1600}` with a comment
explaining the claim is about wiring, not distance. Legitimate — re-ran
BL-1330's acceptance independently (12/12) to confirm.

## Independently re-run

- `./swarmforge/scripts/test/test_bl1328_qwen_model_token_forms.sh` (run
  via its own zsh shebang, not bash) — 11/11 pass.
- `run_acceptance.sh specs/features/BL-1328-…feature` — 4/4 pass.
- `run_acceptance.sh specs/features/BL-1324-…feature` — 10/10 pass
  (post-retirement).
- `run_acceptance.sh specs/features/BL-1330*` — 12/12 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1328QwenModelTokenFormsInvariants bl1324ClaudeSeatQwenCloudContextWindowInvariants`
  — 3 runs, 6/6 each, no flake.
- Dependency-rule gate (BL-259), scoped and full-repo: PASSED, no forbidden
  edges.
- Co-change report (BL-255): all reported co-changes at/below threshold.

## Hardener note carried forward

The coder's own evidence flags that retiring the BL-1324 Examples row
invalidates that feature's `acceptance-mutation-manifest` `scenario_hash`.
The manifest is never hand-edited; the Gherkin mutation run needs
regenerating for that file. This is the hardener's pass, not the coder's
or mine — noted here so it isn't missed.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
