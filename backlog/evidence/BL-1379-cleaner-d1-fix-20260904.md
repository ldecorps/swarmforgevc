# BL-1379 — CLEANER PASS (D1 fix), 2026-09-04

NONE. No defect found; no cleanup change made.

## What changed

Coder's D1 fix wires the park reversal to a real production trigger:
`expedite-park-reversal-sweep!` in `handoffd.bb`, registered in the daemon's
poll loop beside `master-main-reconcile-sweep!`, walks
`.swarmforge/expedite/*/park-record.json` and shells `expedite_cli.bb unpark
<root> <run-dir>` for each run still carrying an unsettled record. A follow-up
self-audit commit (`b17b0869b5`) fixed a self-caught performance/log-noise gap
in that same wiring: the sweep now settles a run once (`unpark-done.json`)
instead of re-shelling and re-logging every tick forever for old runs.

## What was checked

- **Ran the real end-to-end wiring test** (not only the fixture),
  `test_handoffd_expedite_park_reversal_wiring.sh`: re-ran — ALL PASS,
  spawning the actual daemon against a real fixture root and confirming a
  real ticket leaves `backlog/hold/`, comes back `blocked` with a mark
  naming its expedition, a settled run drops out of the sweep, and a
  human-held ticket is untouched.
- Confirmed the acceptance fixture no longer calls the pure lib function
  directly — `specs/pipeline/steps/lib/bl1379ParkReversalCli.sh` now runs
  `bb expedite_cli.bb unpark <root> <run-dir>`, the actual production
  subcommand — this is exactly the fixture-vs-production gap the D1 bounce
  was about, now closed. Ran it directly (`landed` shape): output still
  matches every prior scenario assertion.
- `expedite_lib_test_runner.bb` — re-ran: ALL PASS.
- `test_expedite_cli.sh` — re-ran: ALL PASS (BL-1023 rows unaffected).
- `test_handoffd_expedite_park_reversal_wiring.sh` correctly registered in
  `swarmforge/scripts/test/suite-manifest.tsv`'s standing lane.
- `jscpd` over `expedite_cli.bb`/`handoffd.bb`: 0 clones.
- TypeScript compiles clean; the acceptance handler still discovers via
  BL-1371's registry (unaffected — no JS handler changed in this rework).

This is the D1 rework, not a new bounce; `bounce_count` stays 1.
Forwarding unchanged to architect.

By cleaner.
