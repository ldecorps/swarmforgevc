# BL-759 — coder — 20260825

Broke the telegram-front-desk-bot ↔ Cursor-operator import cycle.

## Structure

- `controlDrainTimeoutMs` → `telegramControlCore.ts` (pure)
- `isPipelineEmpty` + `resolveLiveRoles` → new `telegramPipelineDrain.ts`
- Bot re-exports the same bindings (invariant 2)
- Exec / Liveness / notify-dead-letters import the leaf modules — no back-edge into the bot

## Proof

- `node extension/out/tools/dependency-gate.js` → PASSED (full repo + scoped)
- APS BL-759 → 10/10
- `telegramFrontDeskBotCli` drain/timeout unit filters → 6/6
- property suite → 2/2

No drain-semantics change.

By coder.
