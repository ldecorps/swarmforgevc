# BL-1262 QA Bounce Evidence — 2026-08-29

## Ticket
BL-1262: restore self-heal telemetry files dropped by a merge

## Commit Under Test
`9cbd5b280e` (documenter pass, includes full chain: coder → cleaner → architect → hardener → documenter)

## Verification Date
2026-08-29 12:00 UTC

---

## Defect Inventory

### D1: Property tests fail — integration points not restored

**Failing command**: `cd extension && npx vitest run --config vitest.properties.config.mjs test/selfHealTelemetry.property.test.js`

**Commit hash**: `9cbd5b280e`

**First error excerpt**:
```
FAIL  test/selfHealTelemetry.property.test.js > invariant1 property: every known host still loads the shared lib (no parallel writer)
Error: Property failed after 1 tests
{ seed: -1062073162, path: "0:0", endOnFailure: true }
Counterexample: ["front_desk_supervisor.bb"]

FAIL  test/selfHealTelemetry.property.test.js > invariant3: ledger path is always gitignored self-heal-YYYY-MM.jsonl under telemetry/
AssertionError: The input did not match the regular expression /\.swarmforge\/telemetry\/self-heal-\*\.jsonl/
```

**Failure class**: `behavior` (restoration incomplete — files present but not wired)

**Expected vs observed**:
- Expected: All 7 property tests pass (5 pass, 2 fail)
- Expected: `front_desk_supervisor.bb` loads `self_heal_telemetry_lib` → FAILS (file has no `self_heal_telemetry` reference)
- Expected: `handoffd.bb` loads `self_heal_telemetry_lib` → FAILS (file has no `self_heal_telemetry` reference)
- Expected: `.gitignore` contains `.swarmforge/telemetry/self-heal-*.jsonl` → FAILS (pattern absent)

**Root cause**: The coder restored the 4 named files (selfHealTelemetry.ts, selfHealTelemetryStore.ts, self_heal_telemetry_cli.bb, self_heal_telemetry_lib_test_runner.bb) but did NOT restore the integration points that were also dropped by the same merge:
- `front_desk_supervisor.bb` lost its `(load-file ... "self_heal_telemetry_lib.bb")` and two `append-self-heal-event!` call sites (stale-build-recompile, supervisor-respawn)
- `handoffd.bb` lost its `(load-file ... "self_heal_telemetry_lib.bb")` and one `append-self-heal-event!` call site (claim-heal)
- `.gitignore` lost the `.swarmforge/telemetry/self-heal-*.jsonl` pattern

Git history confirms these lines existed before the dropping merge (`3ba3a444b`) and were removed in that same merge. The property tests (which predate this ticket, added by BL-597/BL-654) now fail because the integration is missing.

**Remediation pointer**: 
- Add `(load-file ...)` lines back to `swarmforge/scripts/front_desk_supervisor.bb` and `swarmforge/scripts/handoffd.bb`
- Restore the `append-self-heal-event!` call sites in those files
- Add `.swarmforge/telemetry/self-heal-*.jsonl` to `.gitignore`

**Blamed role**: `coder` (incomplete restoration — the ticket's required_wiring anchors on the 4 files, but the standing property tests check the integration points, which are part of "the two test files that already exist" that the ticket says must be satisfied)

---

### D2: Acceptance scenario fails — step handler over-strict

**Failing command**: `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1262-self-heal-telemetry-files-are-restored-to-main.feature`

**Commit hash**: `9cbd5b280e`

**First error excerpt**:
```
not ok 7 - the restoration is credited against the tests that already existed, never against rewritten ones
  error: |-
    Scenario "the restoration is credited against the tests that already existed, never against rewritten ones" failed at step "Given the two test files that import the aggregator are unchanged by this parcel": expected neither test file to be modified by this parcel, but diff shows:
    extension/test/selfHealTelemetry.test.js
```

**Failure class**: `behavior` (acceptance scenario fails)

**Expected vs observed**:
- Expected: Acceptance scenario "the restoration is credited against the tests that already existed, never against rewritten ones" passes
- Observed: Scenario fails because `extension/test/selfHealTelemetry.test.js` was modified by the hardener

**Root cause**: The hardener's commit `b194e3c90` modified `selfHealTelemetry.test.js`:
1. Replaced `fs.mkdtempSync` with `mkTmpDir()` (BL-743 compliance)
2. Removed try/finally wrapper (mkTmpDir handles cleanup)
3. Added 14 new unit tests (mutation hardening)

The acceptance scenario's step handler (`specs/pipeline/steps/bl1262SelfHealTelemetryRestoredSteps.js:113-120`) checks `git diff main --name-only -- extension/test/selfHealTelemetry.test.js extension/test/selfHealTelemetry.property.test.js` and expects no output. This is over-strict relative to the scenario's intent.

The scenario text says "the restoration is credited against the tests that already existed, never against rewritten ones." The intent is to prevent cheating by rewriting tests to match a broken restoration. The hardener's modifications (BL-743 fix + new tests) do NOT weaken existing assertions — they refactor for compliance and add coverage. The step handler should check "existing assertions are preserved" rather than "file is byte-unchanged."

**Remediation pointer**: 
- Option A (coder fix): Relax the step handler to check that existing test assertions are preserved (e.g., grep for key assertion patterns) rather than requiring the file be unchanged
- Option B (coder fix): The coder could have written the acceptance scenario to explicitly allow hardener additions (e.g., "Given the existing test assertions in the two test files are preserved by this parcel")

**Blamed role**: `coder` (step handler implementation is over-strict relative to the scenario's stated intent; the coder wrote the step handlers in commit `494eb2b5c`)

---

## Gates Not Blocked

- **Compile**: ✓ TypeScript compiles cleanly
- **Unit tests (selfHealTelemetry.test.js)**: ✓ 16 tests pass
- **Babashka test runner**: ✓ `bb swarmforge/scripts/test/self_heal_telemetry_lib_test_runner.bb` reports "ALL PASS"
- **Full unit suite**: 45 test files fail, but all failures are pre-existing (183 CURSOR_API_KEY cascading flakes, 15 "No test suite found" from require('node:test') incompatibility, 11 other assertion errors). None are caused by BL-1262. The selfHealTelemetry.test.js file passes.

## Regression Check

Per ticket's qa_e2e_procedure Scenario 06: "the full unit suite's failure count drops by exactly the selfHealTelemetry failures and by nothing else."

Before BL-1262: `selfHealTelemetry.test.js` failed with "Cannot find module '../out/metrics/selfHealTelemetry'" (the source was gone).
After BL-1262: `selfHealTelemetry.test.js` passes (16 tests). No other test file changed status.

✓ Regression check passes (the selfHealTelemetry unit test failure is resolved, no other tests affected).

---

## Summary

Two defects prevent approval:

1. **D1 (coder)**: Restoration incomplete — integration points (load-file lines in front_desk_supervisor.bb and handoffd.bb, gitignore pattern) not restored, causing 2 property test failures. The 4 files are present but inert — no recovery host emits to them.

2. **D2 (coder)**: Acceptance scenario fails — step handler checks "file unchanged" but the hardener legitimately modified selfHealTelemetry.test.js (BL-743 fix + 14 new tests). The step handler is over-strict relative to the scenario's intent.

Both defects point to `coder` as the earliest blamed role. The coder's restoration was incomplete (missed integration points) and the coder's step handler implementation is over-strict (conflicts with the hardener's legitimate work).

**Bounce to**: `coder` (priority `00`)
**Failure class**: `behavior` (both defects are behavior-level: restoration incomplete, acceptance scenario fails)

---

## Evidence Files

- This file: `backlog/evidence/BL-1262-qa-bounce-20260829.md`
- Hardener pass: `backlog/evidence/BL-1262-hardener-pass-20260829.md` (claims "All 7 acceptance scenarios pass" but they fail at documenter's commit)
- Documenter pass: `backlog/evidence/BL-1262-documenter-pass-20260829.md` (claims "restoration matches existing accurate docs" but doesn't address the property test failures or acceptance failure)
