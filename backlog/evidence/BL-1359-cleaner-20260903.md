# BL-1359 — cleaner pass (2026-09-03)

## Scope

Diff (`3cc596a523^..80c602e78d`, excluding evidence files) touches exactly
what the ticket declares: `swarmforge/scripts/babysitter_check.bb` (new
`commit-first-parent`; `commit-touched-paths`' merge branch now diffs
against the resolved first parent instead of `-m --first-parent`; the
`(nil? touched) -> ::adjudication-failed` fail-closed row in `offender-row`),
the matching comment correction in `babysitterd_sweep_lib.bb`, the new
`.bb` unit-test runner (registered in `suite-manifest.tsv`), the acceptance
step handler, and the property test. Includes the coder's own self-audit
fix (fixture prefix-sweep before the run, BL-971 shape).

## Invariants checked against the diff

- **Invariant 1** (a merge is charged with exactly its first-parent delta):
  `commit-touched-paths` now runs `diff-tree --no-commit-id --name-only -r
  <first-parent> <sha>` for a merge — a genuine two-tree diff, not `-m
  --first-parent` (confirmed by measurement that flag is a no-op on a single
  named commit: `-m` alone decided the old output, the union of every
  parent's diff).
- **Invariant 2** (fail-closed unchanged): `commit-first-parent` returns nil
  on git failure rather than falling back to a different comparison;
  `commit-touched-paths` now returns nil (not `[]`) when the first-parent
  resolution fails, and `offender-row`'s new `(nil? touched) ->
  ::adjudication-failed` row means `assemble-offending-commits` withholds the
  WHOLE sweep as ancestry-unavailable rather than reading a failed call as
  clean — verified this is a case that was NOT reachable before (the old
  `(or touched [])` would have silently converted a git failure into an
  empty, clean charge set on exactly the path this ticket touches).
- **Invariant 3** (non-merge commits unchanged): the non-merge branch of
  `commit-touched-paths` is untouched; `bl1359MergeChargedInvariants`
  invariant 3 stays green whether the old or new command is in place, which
  is the correct signature for "this path did not change."

## Verification (run, not assumed)

- `bb swarmforge/scripts/test/bl1359_merge_charged_test_runner.bb` — `ALL
  PASS`.
- `npx vitest run --config vitest.properties.config.mjs
  bl1359MergeChargedInvariants` — 3/3.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1359-a-merge-is-charged-only-with-what-it-introduced.feature`
  — 7/7.
- `bash swarmforge/scripts/test/test_babysitter_check.sh` — `ALL PASS`
  (regression, unaffected).
- `bb swarmforge/scripts/test/bl962_merge_adjudication_test_runner.bb` —
  one pre-existing FAIL ("no second ancestry predicate"), confirmed via
  `git log -S` this `merge-base` call was introduced by BL-1187
  (`ec2416a9d6`), well before this parcel — matches the coder's evidence,
  not a defect of this ticket, not actionable here.
- `specs/pipeline/steps/index.js` — `bl1359MergeChargedOnlyWithIntroducedSteps`
  registered (required_wiring satisfied).

## Tooling note (Babashka)

Per Startup Tools, no mutation/CRAP/DRY tooling is wired for Babashka —
gated by the `.bb` unit suite only, green. No `extension/src/**` production
file is touched, so the mutation-site-count gate (BL-485) does not apply.

## Structure

Both comments that stated the false `--first-parent` claim are corrected in
this same parcel, as the ticket required (`babysitter_check.bb` and
`babysitterd_sweep_lib.bb`). `merge-parent-facts`/`adjudicate-merge-paths`
are unchanged, matching the ticket's prediction. No unrelated files in
scope.

## Verdict

No defect found. Forwarding.
