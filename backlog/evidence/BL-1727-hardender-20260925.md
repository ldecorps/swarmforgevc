# BL-1727 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `swarmforge/scripts/ollama_ancillary_lib.sh`
(`ollama_ancillary_stop_pid`, new `ollama_ancillary_wait_gone`) plus its
shell test, property test and acceptance feature/handler — no `src/*.ts`
or `out/**/*.js` production code touched (CRAP/Stryker not applicable);
no Babashka file touched either (`.bb` mutation fallback not applicable).

Checks run (host load ~2-3 on 20 cores, quiet):
- `bash swarmforge/scripts/test/test_ollama_ancillary_stop_pid.sh`: 4/4
  PASS — real short-lived processes for exits-after-TERM,
  ignores-TERM (KILL escalation), already-exited, and
  cannot-be-signalled (EPERM simulated via a `kill` shell-function
  shadow, `-0` passed through).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1727OllamaStopWaitsUntilGone.property.test.js` (from
  `extension/`): 2/2 passed, 5.36s — reach-floor invariant over all
  three behaviour categories plus the unsignallable-failure-path test.
- `node specs/pipeline/cli.js specs/features/BL-1727-stopping-a-started-ollama-server-waits-until-it-is-gone.feature`:
  4/4 ok.
- `node specs/pipeline/cli.js specs/features/BL-1703-a-pack-with-a-local-model-endpoint-starts-and-probes-ollama-before-it-launches.feature`:
  5/5 ok.
- `bash swarmforge/scripts/test/test_ollama_ancillary_launch_gate.sh`:
  2/2 PASS.
- `node specs/pipeline/cli.js specs/features/BL-1704-the-swarm-stop-paths-stop-the-ollama-server-the-swarm-started.feature`:
  5/5 `not ok` — every scenario "no step handler matched". Traced: BL-1704
  itself is `status: todo` in `backlog/paused/`, its step handler
  (`bl1704OllamaStopPathsSteps.js`, named in BL-1704's own
  `required_wiring`) does not exist, and neither
  `stop_ancillary_services.sh` nor `kill_all_swarm.sh` references
  `ollama` yet — a pre-existing spec-gap (BL-1727's own
  `qa_e2e_procedure` step 3 wrongly names this feature "green"), not a
  regression from this parcel. Sent as a priority-`00` note to the
  specifier (full evidence:
  `backlog/evidence/BL-1727-hardender-spec-gap-qa-e2e-bl1704-20260925.md`),
  per this pass's `spec-gap` duty — not routed as a parcel or bounce.

Hand-mutation (no wired mutation tool for `.sh`, mutation_cost: low —
BL-638 fallback): reverted `ollama_ancillary_stop_pid` in place to its
pre-BL-1727 shape (`kill "$pid" || true; return`, no wait/escalation),
confirming the coder's own non-vacuity claim independently:
- `test_ollama_ancillary_stop_pid.sh` case 01 → FAIL ("pid ... is still
  alive after a successful stop").
- The property test's both cases → FAIL (ignores-term case: pid still
  alive after claimed success; unsignallable case: exit 0 instead of
  nonzero).
Restored the original file (`git status --short` clean afterward, byte
diff confirmed empty) before re-running the real suite green again.

Fixture hygiene: no `stand-in-ollama`/SIGTERM-listener child processes
left alive after any run (`pgrep -fl` checked); `git status --short
extension/test/ swarmforge/scripts/test/` clean of anything this pass's
runs could have leaked.

Not run: `npm run test:properties` in full (2026-09-17 rule — only the
one relevant property file run, no other `*.property.test.js` touched
this parcel) and Stryker/CRAP (no `src`/`out` file in scope).

By hardender.
