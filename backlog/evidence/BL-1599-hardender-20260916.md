# BL-1599 — hardender review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Mutation (Stryker, scoped run via `vitest.bl1599.stryker.config.mjs`,
  `--mutate out/tools/check-suite-duration-budget.js`, `--force`):
  started at 80.52% (14 survived, 1 no-coverage of 77 mutants) - added the
  singular/plural-fork exact-text pins and the over-tolerance verdict pin
  to `checkSuiteDurationBudgetCli.test.js`; re-run 100.00% (77 killed, 0
  survived, 0 no-cov).
- `extension/scripts/testDurationRecorderLib.js` (`computeFinalExitCode`,
  `buildRecord`, `appendRecord`, `listTestFiles`) sits outside Stryker's
  `out/**/*.js` mutate scope by design (plain script, not compiled TS);
  adding it to `mutate` for a scoped run trips
  `test/hardenerTooling.test.js`'s "test files must not be mutated" guard
  on a substring collision (`testDurationRecorderLib` contains `test`) -
  reverted, and hand-authored 8 mutants instead (BL-638 fallback):
  `!==`->`===` on both the test and guard exit-code checks, the
  guard/work-ratchet return-priority swap, the default `workExitCode`
  value, the pass/fail result swap in `buildRecord`, `appendRecord`'s
  catch-fallback removed, and `listTestFiles`' `.endsWith` filter negated.
  All 8 killed by the existing suite (added
  `computeFinalExitCode prefers the per-file guard exit code over the
  work ratchet when both are non-zero` to discriminate the return-swap
  mutant, which every prior guard/work pairing used equal values for and
  so could not tell apart).
- BL-113 Gherkin mutation (soft) on the Scenario Outline: 27 mutants
  (Examples-table values), 0 survived, 0 errors - real pass per
  `classifyOutcome` (exit 0). The embedded manifest wrote `scenarios: []`
  despite the clean run (BL-502 shape: the vendored tool's own
  `new-manifest` criteria, not this run's outcome); trusted the run's own
  exit code over the manifest text per that rule.
- Acceptance pre-check: 5/5 green from the correct worktree root (an
  initial run from the shared master checkout at
  `/home/carillon/swarmforgevc` false-failed all 5 on a missing step
  handler - the handler file only exists in this worktree; not a real
  gap, own mistake, no bounce).
- CRAP (`src/tools/check-suite-duration-budget.ts`, full-suite coverage:
  625 files / 10668 tests green): max CRAP 4.00
  (`formatSuiteWorkVerdict`, complexity 4, 100% covered), every other
  touched function <= 3.00.
- DRY (jscpd) on the three touched files: 0 clones.
- Invariants verified against `git merge-base HEAD main`
  (`1fce68ec4acd`): `SUITE_DURATION_BUDGET_MS` (10000) and
  `PER_FILE_DURATION_BUDGET_MS` (7000) both unchanged in the diff; no test
  file removed.
- `stryker.config.json` restored byte-identical to the committed baseline
  before forwarding (`git checkout --`); `vitest.bl1599.stryker.config.mjs`
  kept as the scoped-run artifact, same convention as the other
  `vitest.bl*.stryker.config.mjs` files already in the tree.

## Worktree-drift guard defect found (not this ticket's own scope)

`ready_for_next.sh`'s BL-1195 drift guard (`enforce-worktree-drift-guard!`
in `swarmforge/scripts/ready_for_next.bb`) calls
`(handoff-lib/my-handoff-files (handoff-lib/my-mailbox-dir :in_process))`
- the non-batch-aware `handoff-files`, which does not descend into
`batch_*` subdirectories (only `handoff-files-with-batches` does, per
BL-1313). A batch-mode role (hardender, cleaner) whose in_process parcel
sits in a `batch_*` subdirectory - the normal shape for a batch role -
therefore reads as `has-in-progress-task? = false`, and any of that
parcel's own legitimate uncommitted WIP is refused as
`WORKTREE_DRIFT_DETECTED`. Hit at session start on this exact ticket
(BL-1599's own in_process parcel, `batch_20260916T202413Z_000001/...`).
Reported via `note` to the specifier separately; not fixed here as it is
swarm-machinery code, outside this ticket's scope.
