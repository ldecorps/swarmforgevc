# Two unowned reds found while batch-verifying BL-1475's touched features

Both confirmed pre-existing on `main` (reproduced identically with a clean
stash of this parcel's own diff, before any BL-1475 edit existed) and
unrelated to BL-1475's own change (git index-lock retry/verify).

## 1. `specs/features/BL-582-approval-tap-never-records-or-repaints.feature`

Scenario "an approval record write commits itself instead of leaving
uncommitted state" fails at step "Then the write is committed through the
same commit-on-decision path used by expedite writes":

```
The input did not match the regular expression
/import \{ commitApprovalWrites \} from '\.\.\/util\/commitIntegrityRunner'/
```

Cause: `specs/pipeline/steps/bl582ApprovalTapObservableSteps.js` line 389
asserts `telegram-front-desk-bot.ts`'s import line is EXACTLY
`import { commitApprovalWrites } from '../util/commitIntegrityRunner'` (no
other names). The real import has always been (confirmed against `git show
HEAD` before this parcel touched the file) `import { commitApprovalWrites,
humanDecisionCommitMessage } from '../util/commitIntegrityRunner';` - two
names, never matching that regex. Reproduced byte-for-byte on the
pre-BL-1475 file.

## 2. `specs/features/BL-418-standing-topic-icons.feature`

Scenario "each standing topic resolves to its orchestra icon", Example row
2, fails at step `Then the icon is "🏛"`:

```
expected the "operator" standing topic's icon to be "🏛", got "🛎"
```

An icon-mapping mismatch, unrelated to any file this ticket touches
(blTopicStore.ts/gitCommitScopedFile.ts/commitIntegrityRunner.ts).
Reproduced identically on the pre-BL-1475 tree.

## Not fixed here

Grepped first: `backlog/standing-reds.tsv` and every active/paused ticket -
neither failure is named anywhere. Both are unowned; sent as notes to the
specifier and coordinator (priority 00) rather than investigated or fixed
as part of BL-1475, which touches neither the front desk's import list nor
any standing-topic icon mapping.
