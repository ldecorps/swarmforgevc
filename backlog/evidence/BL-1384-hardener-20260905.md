# BL-1384 — hardener pass, 2026-09-05

Clean sweep. No test additions needed; forwarding the received commit
unchanged.

## BL-149 cooldown gate — both touched files skipped this pass

```
extension/src/tools/telegram-front-desk-bot.ts   -> skip-cooldown (age 0.01d)
extension/src/tools/telegramFrontDeskBotCore.ts  -> skip-cooldown (age 0.01d)
```

Both committed within the last 3 days (actively churning across BL-1383/
BL-1402/BL-1384 all landing on 2026-09-05) — not mutation-tested per the
gate. Reviewed the diff and its tests by hand instead.

## Hand review of the actual diff (21 lines across 2 files)

The change adds one new OR-term (`qwenLocalSeatTopicId`) to two existing
gates that already fold `cursorBridgeTopicId`/`bubbleTopicId` the same way:

- `hasCursorBridgeTopicAdapter`: `Boolean(cursorBridgeTopicId) ||
  Boolean(bubbleTopicId) || Boolean(qwenLocalSeatTopicId)`
- `attemptCursorBridgeTopicExclusion` -> `decideCursorBridgeExclusion(update,
  [cursorTopicId, bubbleTopicId, qwenLocalSeatTopicId])`

This is exactly the OR-fold shape the constitution's own "isolating fixture"
rule warns about (a fixture where all three terms happen to agree proves
nothing about any one of them) — checked the coder's 5 new tests against
that risk directly, not just the pass/fail count:

- **local-seat-only deployment** (`cursorBridgeTopicId: undefined`, only
  `qwenLocalSeatTopicId` resolves): forwarded. Isolates the new term alone -
  proves the OR actually reaches it, not just that SOME term was true.
- **adapter resolves undefined** (map absent): falls through to
  `openSubjectAndRecord` exactly as before. The negative case for the same
  term - proves it is a real gate, not always-true.
- **coexistence** (Cursor Remote topic hit, local-seat adapter also wired):
  still forwarded via the pre-existing term, confirming the new term didn't
  shadow or interfere with the others.
- **pre-existing fixture, adapter never wired at all** (no
  `qwenLocalSeatTopicId` key in the adapters object): unchanged behavior -
  regression guard for every fixture written before this ticket.
- Plus the ticket's own 4-scenario acceptance feature, re-run here (4/4),
  and the bridge-side end-to-end test the architect flagged as new coverage
  (`telegramCursorBridgeLive.test.js:3504`, draining a genuinely forwarded
  update through the same queue).

No gap found: the isolating and negative cases the OR-fold rule calls for
are both present, not merely a fixture where all terms happen to agree.

## Verification re-run

`test/telegramFrontDeskBotCore.test.js` + `telegramCursorBridgeCore.test.js`
+ `telegramCursorBridgeLive.test.js`: 725/725. Property
(`bl1384LocalSeatTopicForwardedInvariants.property.test.js`): 2/2.
Acceptance `BL-1384-...feature`: 4/4. Regression `BL-1235-...feature`: 5/5,
`BL-1296-...feature`: 6/6, both unchanged.

## CRAP / DRY

`crapReport.js` over both files flags 19 functions >6 CRAP, ALL pre-existing
debt in this large, actively-churning file (same file BL-1402 touched hours
ago) - none of them are functions this ticket's diff touched. Checked each
function this diff actually changed:

- `hasCursorBridgeTopicAdapter` (new signature) and
  `attemptCursorBridgeTopicExclusion` (new local variable +
  array element): neither appears in the >6 list.
- `buildPollAdapters`: appears in the list (CRAP=27.80) but driven entirely
  by its pre-existing 3% coverage on an unrelated giant object-literal body,
  not by this ticket's 2-line addition - the added
  `qwenLocalSeatTopicId: () => Promise.resolve(...)` line is byte-for-byte
  the same ternary shape as its `bubbleTopicId`/`cursorBridgeTopicId`
  siblings immediately above it (confirmed via `git show`), so it adds no
  new branch shape versus what was already there.

jscpd found the same one pre-existing clone already recorded in
`BL-1402-hardener-20260905.md` (barrel import/re-export list, predates both
tickets, `git blame add5449236d` 2026-08-26) - unchanged, out of scope.

## No violations found

By hardener.
