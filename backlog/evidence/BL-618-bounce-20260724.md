# BL-618 QA bounce evidence — 2026-07-24

## 1. Failing command
```
specs/pipeline/scripts/run_acceptance.sh specs/features/BL-618-stable-prefix-cap-regression.feature
```

## 2. Commit hash
`624ad2bc2e` (coder's BL-618 fix commit, merged into QA at `7a267570f6`).

## 3. First error excerpt
```
TAP version 13
# Subtest: the prompt engine test runner passes on the fix commit
not ok 1 - the prompt engine test runner passes on the fix commit
  ---
  error: 'Scenario "the prompt engine test runner passes on the fix commit": no step handler matched "Given the repository at the fix commit"'
  code: 'ERR_TEST_FAILURE'
# Subtest: every passage moved out of a boot article survives verbatim in a reference file
not ok 2 - every passage moved out of a boot article survives verbatim in a reference file
  error: 'Scenario "every passage moved out of a boot article survives verbatim in a reference file": no step handler matched "Given the set of passages this fix removed from boot-inlined articles"'
# Subtest: a slimmed boot article still points at the reference file that absorbed its prose
not ok 3 - a slimmed boot article still points at the reference file that absorbed its prose
  error: 'Scenario "a slimmed boot article still points at the reference file that absorbed its prose": no step handler matched "Given a boot article that lost a passage to a reference file in this fix"'
# Subtest: no normative rule is deleted outright by the slimming
not ok 4 - no normative rule is deleted outright by the slimming
  error: 'Scenario "no normative rule is deleted outright by the slimming": no step handler matched "Given the diff of this fix across the constitution tree"'
1..4
# pass 0
# fail 4
```

## 4. Failure class
`acceptance` — all 4/4 scenarios in the ticket's own acceptance feature file
fail with "no step handler matched" for every Given/Then in the file. No step
handler module for BL-618 exists anywhere under `specs/pipeline/steps/`
(confirmed via `grep -rl "prompt engine test runner\|stable prefix
length\|passages this fix removed\|lost a passage to a reference file\|diff
of this fix across the constitution" specs/pipeline/steps/` — zero matches).

Note: the underlying fix itself is sound — I independently verified it by
hand: `bb swarmforge/scripts/test/prompt_engine_test_runner.bb` prints
`stable-prefix chars: 50993` and `ALL PASS`, and manual inspection of the
diff confirms every passage moved out of workflow.prompt, 02_handoffs.md,
engineering.prompt, and local-engineering.prompt is preserved verbatim under
`swarmforge/constitution/articles/reference/` with a pointer left in the
slimmed article. The defect is specifically that this verification is not
wired as an executable acceptance check, which is the gate BL-112 requires
QA to run.

## 5. Expected vs observed
Expected: per engineering.prompt's Acceptance Pipeline rule ("A spec
amendment that adds SCENARIOS also adds work: wire step handlers in the same
parcel or the acceptance runner hard-fails on unhandled scenarios") and the
QA role's BL-112 instruction to run `run_acceptance.sh` as the final gate,
BL-618's coder parcel should have added a step-handler module (e.g.
`specs/pipeline/steps/bl618StablePrefixCapRegressionSteps.js`) implementing
the four Given/When/Then steps this feature file introduces, registered in
`specs/pipeline/steps/index.js`, so the acceptance run is green.

Observed: `backlog/active/BL-618-stable-prefix-over-cap.yaml`'s
`acceptance:` points at a feature file materialized by the specifier
(commit `cbfec354b`) whose four scenarios have zero step handlers. Running
the acceptance pipeline hard-fails 4/4 with "no step handler matched" for
every step in the file, even though the underlying fix (the boot-prefix
trim itself) is correct.
