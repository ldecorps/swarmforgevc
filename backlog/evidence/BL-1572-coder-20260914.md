# BL-1572 coder evidence — 2026-09-14

## What changed

- `extension/test/bl1429StandingRedThrottleFoldInvariants.property.test.js`:
  invariant 1's uniform `fc.constantFrom` draw over both axes (60 runs,
  sampled) is replaced with an outer loop over the 12
  `REWORK_CATEGORIES x STANDING_CATEGORIES` cells, one `fc.assert` per cell
  at `numRuns: runsPerCell(60, 12)` (5 draws each,
  `test/helpers/reachFloors.js`) — the same shape BL-1555 used to split
  bl956's overflow cell. Inside each cell, `writeReworkCategory`/
  `writeStandingCategory` now take a `magnitude` drawn from the category's
  own band (degraded strictly between the module's own
  `ABOVE_BASELINE_MULTIPLIER`/`SEVERE_BASELINE_MULTIPLIER` lines — 0.21–0.39
  of a 0.1 baseline; severe 0.41–1.00; standing-red `count`/`oldestAgeDays`
  drawn above `DEFAULT_STANDING_RED_MAX_COUNT`/`DEFAULT_STANDING_RED_MAX_AGE_DAYS`;
  `unownedCount` 1–3) rather than the old fixed constant, so the property
  keeps drawing something real while the category is chosen by
  construction. A combination that stops being produced now fails the file
  naming that combination via `assertReachFloor(counts, ALL_COMBINATIONS,
  INVARIANT_1_CELL_RUNS, 'combination')`, kept beside the original
  `seen.size === 12` assertion. The run prints
  `BL-1572 reach map (invariant 1): {"combinations":12,"minDrawsPerCombination":5}`,
  computed from the counts the run actually reached.
  `writeStandingCategory` keeps a default-parameter fallback
  (`STANDING_MAGNITUDE_DEFAULTS`) so invariant 2's own call site
  (`writeStandingCategory(root, state)`, unchanged) keeps its original
  fixed values (15/12/1) — invariants 2 and 3 are byte-identical to main
  (`git diff main -- <file>` confirms the diff stops at invariant 1's
  closing brace).
- `specs/pipeline/steps/bl1572FoldFloorConstructedSteps.js` (new): step
  handlers for the specifier-authored feature
  (`specs/features/BL-1572-*.feature`, not touched by this parcel — first
  attempt accidentally overwrote it with a rephrased version; reverted via
  `git checkout --` and the handler rewritten to match the specifier's own
  step text instead). Scenarios 01/02 drive the real property file under
  the real properties config (`spawnSync` from `extension/`, memoized per
  scenario), reading its own printed reach map. Scenario 03 reads the real
  test source and confirms the two floor assertions are still present.
  Discovered automatically by `specs/pipeline/steps/index.js` (BL-1371), no
  manual registration needed.

`extension/src` is unchanged (confirmed via `git diff main --name-only --
src`, empty).

## Verification

- Property file alone (`npx vitest run --config vitest.properties.config.mjs
  test/bl1429StandingRedThrottleFoldInvariants.property.test.js`): 4/4 pass,
  8 standalone repeat runs all green, each printing
  `BL-1572 reach map (invariant 1): {"combinations":12,"minDrawsPerCombination":5}`.
  A further 15 standalone runs (QA e2e procedure step 1): 15/15 green, 0
  FAIL, reach map printed every time.
- Break-then-restore: skipped `severe` in the outer loop's rework-category
  iteration — first run failed
  `expected all 12 combinations reached, got degraded:age,degraded:count,degraded:none,degraded:unowned,none:age,none:count,none:none,none:unowned`
  (the `seen.size` assertion, the earlier of the two checks); restored from
  a clean copy, re-ran, green again.
- Full acceptance run (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1572-*.feature`): 5/5 scenarios pass (01 green run, 02
  both Examples rows — 12 combinations, 5 draws of the rarest combination —
  03 both Examples rows).
- `swarmforge/scripts/check_feature_handler_registration.sh`: clean, exit 0.
- Full extension unit suite (`npx vitest run`): 616/616 files, 10479/10479
  tests passed.

## Register row

`backlog/standing-reds.tsv` line naming this ticket is left in place — per
the ticket's own notes, QA removes it in the land that turns the file
green, not the coder.

By coder.
