# BL-1367 — coder pass, 2026-09-03

An approval from any surface carries its ruling.

## What landed

| Path | Change |
|------|--------|
| `extension/src/concierge/pendingApprovalReply.ts` | `classifyApprovalRulingRequirement` (the pure decision); `recordApprovalReply` widened to take an optional ruling, so both entry points reach ONE writer |
| `extension/src/bridge/bridgeServer.ts` | `computePausedPagerApproveOutcome` consults the requirement before recording anything; the Approve body accepts an optional `ruling` |
| `extension/test/pendingApprovalReply.test.js` | 7 cases over the classifier and the widened writer |
| `extension/test/pausedPagerBridge.test.js` | 5 cases over the REAL HTTP route |
| `extension/test/bl1367ApprovalCarriesItsRuling.property.test.js` | the three declared invariants |
| `specs/pipeline/steps/bl1367ApprovalCarriesItsRulingSteps.js` + `index.js` | acceptance handlers, driving the real route |

The pager's Approve now behaves this way:

| ticket | tap | outcome |
|---|---|---|
| declares options | carries a declared option | approved **with** `human_ruling:` |
| declares options | carries nothing | **409**, nothing written, refusal names the options and points at the bot's ruling keyboard |
| declares options | carries an option it never offered | **409**, nothing written |
| declares none | plain | approved, no ruling — byte-for-byte as before |

409 rather than 500: the request was well formed and the system is healthy, a
rule said no — the same posture the promotion gate takes one route up, and the
refusal carries the gate's own words so the operator learns which choice is
outstanding (BL-572/BL-662).

## Declared invariants (BL-654)

`extension/test/bl1367ApprovalCarriesItsRuling.property.test.js` — the
TypeScript lane's standard home (`npm run test:properties`), which is the right
one here because this ticket's code is TypeScript.

| # | Invariant | Property | Non-vacuity |
|---|-----------|----------|-------------|
| 1 | never approved with `ruling_options` and no `human_ruling` | **P1** — for every declared/undeclared option set and every ruling, either the approval is recorded with a ruling or the file is byte-identical afterwards | let a bare approval through for an option-bearing ticket → **1 of 2 property tests failed** |
| 2 | an existing ruling is never overwritten or cleared | **P2** — a recorded ruling changes only when a **declared** option was just chosen, and is never cleared outright | cleared the ruling on a plain approval → **both property tests failed** |
| 3 | a ticket declaring no options approves exactly as today | **P3** — a bare approval always succeeds and writes no ruling; a ruling nobody offered is refused | (same breaks) |

**Generator reach is asserted.** A ruling drawn independently of the options
would match one essentially never, so the accepted case — the only one that
writes a ruling at all — is **constructed**: the chosen label is drawn *from*
the generated option list. The run then fails unless all five outcomes
(`options:ok`, `options:ruling-required`, `options:unknown-option`,
`no-options:ok`, `no-options:unknown-option`) were actually generated.

## Decisions worth the reviewer's eye

- **Validation is the caller's job, not the writer's.** `recordApprovalReply`
  is told what to write; a caller that has not asked
  `classifyApprovalRulingRequirement` is not silently rescued inside the
  writer. Rescuing it there would hide exactly the class of surface this
  ticket exists to catch — one that records without asking.
- **A ruling offered for a ticket that poses no choice is refused**, not
  silently written. Nothing declared it, so nothing can validate it, and a
  `human_ruling` on an optionless ticket is the same unanswerable state in the
  other direction.
- **Blank is not an answer.** A surface sending an empty `ruling` field trips
  the same refusal a missing one does.
- **A non-string `ruling` is rejected rather than coerced** at the request
  shape: a ruling is written into the ticket, and a coerced one would be a
  fabricated answer.
- The acceptance handler drives the **real HTTP route**, not the classifier.
  A scenario that called the classifier alone would report green for a
  decision that is right and not reached — which is this defect's own shape,
  one surface having the writer and the other not.

## Verification

| # | Command | Result |
|---|---------|--------|
| V1 | `npx vitest run test/pendingApprovalReply.test.js` | 88/88 pass |
| V2 | `npx vitest run test/pausedPagerBridge.test.js` | 21/21 pass |
| V3 | `npx vitest run --config vitest.properties.config.mjs test/bl1367ApprovalCarriesItsRuling.property.test.js` | 2/2 pass |
| V4 | `npx vitest run test/bridgeServer.test.js test/pendingApprovalFor.test.js test/pausedPagerUiHtml.test.js test/telegramFrontDeskBotCore.test.js` | 669 pass — every other `recordApprovalReply` consumer, unchanged |
| V5 | `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1367-*.feature` | 4/4 pass, 0 fail |
| V6 | full `npm test` | see below |

### V6, the pre-existing reds, SHOWN rather than asserted (BL-1063)

`npm test` reports **25 failing tests across 15 files**. Run again from a
scratch worktree at this parcel's base commit, with the parcel absent:

```
BASE reds: 25   WITH-PARCEL reds: 25
NEW with this parcel: 0
VANISHED with this parcel: 0
```

The failure sets are identical member for member — not merely equal in count.
(This is exactly the two-run cost BL-1377, delivered earlier today, exists to
remove; it is not adopted yet, so this pass paid it in full.)

### qa_e2e step 5, the live sweep

Every ticket in `backlog/active/` and `backlog/paused/` that declares
`ruling_options` was checked for an approval with no ruling. **The set is
empty** — 11 option-declaring tickets, all approved, all carrying a
`human_ruling:` block. BL-1309's own gap is not visible here because it is
re-pended and its ticket is held, which the ticket predicted.

## Surfaced, not swept: a second surface with the same shape

`/paused-pager/expedite` (`handlePausedPagerExpediteRoute`,
`bridgeServer.ts:783`) also calls `recordApprovalReply` with no ruling, so an
Expedite tap on an option-bearing ticket still leaves it approved with the
choice unanswered — invariant 1's shape, on a route this ticket does not name.

**I did not change it, deliberately.** BL-1083 records that this route must
record the approval *before* the gates are consulted, precisely so Expedite
satisfies the `human_approval` gate rather than being blocked by it, and warns
in the code that a fix letting approval refuse an expedite "would leave the
verb dead — the over-correction this ticket explicitly warns against". Copying
the refusal there would reverse a recorded decision, which is not a coder call.
The ticket's `required_wiring` names `computePausedPagerApproveOutcome` and its
description scopes the fix to the Approve route.

**For the specifier**: closing invariant 1 across every surface needs a ruling
on which wins — BL-1083's "expedite is never blocked" or BL-1367's "never
approved with the choice unanswered". A middle path exists (expedite records
consent *and* leaves the ticket pending its ruling) but it is a new state, not
an adjustment.

## Not in scope, per the ticket

Option buttons in the pager UI (offered on approval, not requested — the route
now *accepts* a ruling, so the UI slice has something to call). BL-1309's own
ruling, which is re-pended and waiting. The `By coder.` byline on approval
commits, which is BL-1368.

## Self-audit (Article 2.3)

**Two finders on one path, checked rather than assumed.** The pager route's
existence check matches a ticket by FILENAME (`findBacklogFilePath`), while
`readRulingOptions` and `recordApprovalReply` both match by the `id:` field
inside the file (`findTicketFilePath`). If those could disagree, an
option-bearing ticket could be read as declaring none and approved bare —
invariant 1's exact failure mode, reintroduced through the back door.

They cannot, and both directions fail closed: the reader and the writer use the
*same* finder, so they always see the same file; and a ticket the filename
finder locates but the id finder does not is refused by the writer as "not
pending approval" rather than written. Pinned by a test using a ticket whose
filename deliberately does not carry its id.

**`qa_e2e_procedure` step 2** (approve from the bot's ruling keyboard) is the
pre-existing `recordRulingReply` path, unchanged by this parcel and covered by
BL-589's own cases in the same file; V4 re-ran every consumer of it green. The
widened `recordApprovalReply` writes through the same
`rulingHumanApprovalText`, so there is one writer, not two.

**`qa_e2e_procedure` step 6** (fixture cleanup) is met by this repo's own
convention rather than a hand-rolled sweep: every fixture goes through
`mkTmpDir`, whose `afterEach` sweep BL-420 centralised, and each case also
`rmSync`s in a `finally`. A hand-rolled prefix sweep would trip
`rawMkdtempGuard`, which exists to keep that policy in one place.

**Commit note.** The pre-commit property-suite guard ran the full property
suite for this parcel (199s, a new `.property.test.js` is its trigger) and
returned `allowlisted-standing-reds; unrelated green commits not refused` — my
new property file is not among the reds.
