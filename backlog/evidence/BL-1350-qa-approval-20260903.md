# BL-1350 — QA approval (20260903)

Received: documenter commit `02c122dea9` (doc-only, Specification.MD
entry, one path correction found via self-audit), forwarding hardener
(coverage gap closed, mutation on the sibling file, standing-guard sweep),
architect, cleaner, coder's SSE keepalive fix on the live bridge server.
Merged into QA (one additive conflict in `specs/pipeline/steps/index.js`,
already resolved by the documenter's own merge and re-confirmed clean
here).

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `npx vitest run test/bridgeServer.test.js`: 101/101 (99 pre-existing +
  2 new, real in-process HTTP tests).
- Acceptance (`specs/features/BL-1350-idle-event-stream-keepalive.feature`):
  4/4.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1350IdleEventStreamKeepaliveSteps` (line 25) — confirmed.
- `required_wiring`: `writeSseKeepalive` confirmed defined in
  `bridgeServer.ts` beside `sseClients`/the poll timer, and called from a
  live `setInterval` in the same module (lines 2300, 2320-2324).
- Declared invariants
  (`extension/test/bl1350KeepaliveInvariants.property.test.js`): 3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  first attempt (commenting out the call) revealed the acceptance step's
  own check is a regex text-match, not a real wiring probe, so a
  commented-out call still matched — worth noting as a fragility in the
  acceptance driver itself, not a defect in the fix. Second attempt
  (actually deleting the `writeSseKeepalive(sseClients);` line) reproduced
  the coder's claim exactly: all 4 acceptance scenarios failed
  ("writeSseKeepalive is defined but no timer in the live server calls
  it"). Restored (`git diff --stat` empty), re-ran 4/4 clean.
- **`drainBufferedRecords` export independently confirmed live**: it is
  both exported and called by the real relay loop
  (`telegramFrontDeskBotCore.ts:3853`) — invariant 2's property test
  exercises the shipped consumer, not a re-implementation.
- **Hardener's standing-guard sweep independently re-run**:
  `tempDirTrapGuard`/`socketFixtureShortRootGuard`/`liveRepoDerivationGuard`
  — 3 failures, matching the claimed pre-existing/already-ticketed set
  exactly (`BL-1289`/`BL-1290`/`BL-1291`).
- Full unit suite (`npm test`): 15 files / 25 tests red (9901 total, up
  from 9899 baseline by this parcel's own 2 new bridgeServer tests) —
  identical standing-debt population plus this parcel's own passing
  additions. None of the 25 failures touch this parcel's files.
- Property suite (`npm run test:properties`): 24 files / 13 tests red,
  the same standing-debt population as every prior pass this session.
  BL-1350's own property test — 3/3 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry confirmed present and accurate (root cause, hold-test evidence,
  placement, inertness, dead-client handling, BL-1111 alert untouched).
  No new how-to warranted (internal fix, no user-facing surface) — matches
  the documenter's own reasoning, independently plausible. No diagram
  change-trigger fired.
- Orphaned test processes: reaped a leftover vitest process group after
  the property-suite run before proceeding.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
