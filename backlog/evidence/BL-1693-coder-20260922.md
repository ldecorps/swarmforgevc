# BL-1693: the paused-pager feature runs green again (Approve handler and gated Expedite fixture) — coder

## What changed

`specs/pipeline/steps/bl538ConsolePausedTicketPagerSteps.js` only:

1. `mkFixture()` now installs the real `promotion_gates_cli.bb` closure
   (via the shared `./lib/promotionGatesFixture.js` helper,
   `bl1425QueueJumpPastCapSteps.js`'s own dependency) so scenario 06's
   Expedite consult (BL-1083, `promoteToActive` fails closed without it)
   stops answering 409; and the real `commit_integrity_cli.bb` closure
   (derived via `./lib/operatorRuntimeBbClosure.js`'s `computeClosure`,
   the same shape `bl1368ApprovalCommitNamesDeciderSteps.js`'s own
   `installCommitIntegrity`/`makeRoot` already uses) plus a real
   initialized git repo, so the Approve route's own durable commit
   (BL-892) can genuinely succeed rather than always failing closed.
2. `writeTicket` gains an optional `extra` object (`{key: value}` lines
   appended after `priority`) so a fixture ticket can carry
   `human_approval: pending` without a bespoke second writer.
3. Six new step handlers for scenarios 07/08 (Approve): the shared
   `Given a paused ticket with human_approval pending is shown on the
   pager` (writes the pending ticket plus a companion with no
   `human_approval` field at all, so the negative half of scenario 07 has
   something real to check against), the two `Then`s scenario 07 needs
   (`canApprove` true/false per item), `When I confirm Approve` (POSTs
   `/paused-pager/approve` with the console token, mirroring the existing
   Expedite confirm step), and the three `Then`s scenario 08 needs
   (`human_approval: approved` on disk AND at `HEAD` - the same
   working-tree-vs-HEAD distinction `pausedPagerBridge.test.js`'s own
   Approve test asserts - the ticket still under `backlog/paused/`, and
   the pager's own re-fetched state showing the new YAML).

No scenario text changed (feature file untouched); no `extension/src`
change.

## TDD / break-then-restore

Ran the feature against the ORIGINAL (pre-fix) handler first to confirm
the exact mint-time red (5 pass, scenarios 06/07/08 failing - 07/08 with
"no step handler matched", 06 with `409 !== 200`). Built the fix, ran
green (8/8). Break-then-restore: removed the fixture's own
`installPromotionGates`/`installCommitIntegrity`/git-init calls (leaving
the new Approve *steps* themselves in place, unlike at mint) and re-ran -
scenarios 06 and 08 failed again (07 stayed green, since `canApprove` is
computed from the YAML field alone and needs no gate infrastructure);
restored, green again (8/8).

## Invariant (BL-654)

Declared: "Every step of the eight BL-538 scenarios resolves to a
handler and passes against the real bridge routes over a fixture root
that carries what those routes consult... no scenario text changes."
The acceptance feature run itself, against the real compiled
`bridgeServer.ts` routes and the real `promotion_gates_cli.bb`/
`commit_integrity_cli.bb` over a real git fixture, is this invariant's
own coder-authored executable test (same disposition as BL-1658's own
precedent for a small, already-enumerated scenario set) - no separate
property test adds anything a second, independent property draw over
fast-check inputs would not already have covered less faithfully than
driving the real routes eight times.

## Out of scope, confirmed untouched

`extension/src` (the Approve route, the gates, the pager UI) - all
pre-existing and correct; this parcel only teaches the ACCEPTANCE
FIXTURE to carry what those routes have consulted since BL-1083/BL-892
landed. The feature file itself - no scenario reworded, retired, or
added.

## Verified

- `node specs/pipeline/cli.js specs/features/BL-538-console-paused-ticket-pager.feature`:
  `# pass 8`, `# fail 0` (at mint: 5 pass, 3 fail); plus break-then-restore
  above.
- `grep -c 'human_approval pending is shown on the pager'
  specs/pipeline/steps/bl538ConsolePausedTicketPagerSteps.js` = 1;
  `grep -c 'promotion_gates_cli.bb'` on the same file = 1 (both
  required_wiring pins).
- `git status --porcelain`: only the handler file touched.
- Full unit lane (`npm test`, extension): 10853/10853 green (no reds this
  run - the BL-1687 module-load-budget row and the earlier BL-1692
  register rows are elsewhere in the pipeline now).
- Full property lane once (`npm run test:properties`): 446/447 files,
  1300/1301 tests. The one failure,
  `bl1687BridgeServerLoaderProbeInvariants.property.test.js` ("reached
  16 of 17 named handlers in 34 draws"), is BL-1687's own file (that
  ticket is active, held by a sibling coder seat, merged into this
  worktree via reverse-hops) - unrelated to this parcel (I never touch
  it) and already owned by an open, in-progress ticket, not a new
  unowned red.

Not run (hardener's/QA's): the standing-red register retirement check,
which only applies after landing.

By coder.
