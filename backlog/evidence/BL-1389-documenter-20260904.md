# BL-1389 — documenter pass (2026-09-04)

## Received
`2ddf294b49` (hardener, degraded Babashka fallback recorded, gated on
`land_step_lib_test_runner.bb` + the BL-1131/BL-1334 property runners per
`backlog/evidence/BL-1389-hardener-pass-20260904.md`).

## What changed, operator-visible
The structural fix for today's live BL-1386 land escalation (QA caught by
hand): the land step's per-ticket landed/unlanded verdict could read a
sibling as landed on the strength of ONE landed attributed path while its
actual source files stayed absent from `origin/main`, letting those files
ride into another ticket's replay under the wrong ticket's approval. The
CLI's own report never printed a path, only ticket ids, so nothing in the
tool's output could have shown this without a hand diff.

## Doc-domain review
- `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`:
  this ticket is the next entry in the same land-step remedy chain every
  prior related ticket this session extended (BL-1308, BL-1315, BL-1343,
  BL-1332, BL-1354, BL-1374, BL-1375) — added a new section describing the
  per-path-complete verdict, the self-sufficient per-path exclusion, and
  the new `EXCLUDED_SIBLING_PATH`/extended `LANDED_SIBLING` report lines.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-1241 summary line (already a
  running list of that page's own remedy-chain entries) with this
  ticket's fix.
- Diagram registry checked — no diagram depicts land-step internals (same
  finding as every other land-step ticket this session).

## Verdict
Doc content extended in the owning how-to page (not NONE) plus the
standard Specification.MD/index.md entries. Forwarding to QA.
