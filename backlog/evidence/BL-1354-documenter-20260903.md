# BL-1354 — documenter pass (2026-09-03)

## Context

Merged hardener `12aebe9650` (no mutation gap surfaced this pass; production
file under cooldown, gate correctly skipped; BL-113 clean, all suites
green, no regression). Reviewed the coder/cleaner/architect/hardener
evidence chain plus the ticket YAML, the feature file, and the shipped
`land_step_lib.bb` diff to identify the user/operator-visible surface this
defect fix changes.

## Review checklist (complete pass, Article 4.4)

- **Existing how-to currency**
  (`docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`):
  this doc already documents `sibling-landed?`, `landed-siblings`, and the
  `LANDED_SIBLING`/`ENTANGLED_SIBLING` reporting (BL-1272) that this defect
  sits directly beneath. Added a dated section describing the whole-blob →
  per-sibling-line-scoped fix, the merge-commit attribution-inflation cause,
  the `:vacuous` three-valued verdict, and the real-repo regression
  (BL-1332's six siblings), extending the existing doc in place rather than
  forking a new page — same detector, same functions, same output shape.
- **`docs/index.md` orphan/exhaustiveness check**: the how-to was already
  linked; extended its one-line hook to name the BL-1354 fix.
- **`Specification.MD` "Last Updated" freshness**: added a new dated entry
  at the top of the changelog, cross-referencing the same how-to. Bumped in
  the same commit as the content.
- **Diagram currency** (constitution Article 1.7 / local-engineering
  "Diagrams"): checked the four registered diagrams. None of their
  change-triggers fired — `land_step_lib.bb`'s internal sibling-landed
  scoring is not the extension host/webview boundary, the tmux substrate
  relationship, the `.swarmforge/` state layout, pipeline topology, backlog
  flow, the handoff file lifecycle, or front-desk routing. No diagram
  update required.
- **`docs/deprecated/` check** (Article 3.6): nothing retired — the ticket
  is explicit that `land_step_cli.bb`'s output shape and the
  `LAND_ESCALATE`/`LAND_REPLAY` protocol (BL-1332) are unchanged, only what
  feeds the landed/unlanded split. No page moved.
- **Production-code scope**: none touched by this pass; doc/spec files only.

## Findings

NONE.

## Verdict

Forward to QA.
