# BL-1415 documenter review pass — 2026-09-05

Reviewed the hardened commit (`7b13e9fb9f`) and the coder/cleaner/architect/
hardener trail for BL-1415 (the dropped-parcel sweep's stall clock now
reads the recipient's own dequeue/completion, and a third verdict,
DROPPED, lets the router repair a genuine drop without `--force`).

Checklist:
- `required_wiring` anchors present, confirmed by grep:
  `swarmforge/scripts/route_backlog_to_coder.sh:116` (`DROPPED` branch),
  `swarmforge/scripts/dispatch_trail_cli.bb:79` (`DROPPED` print),
  `swarmforge/scripts/chase_sweep_lib.bb:1544/1555` (`completed_at`
  folded into the clock), and
  `specs/pipeline/steps/bl1415LostDispatchSteps.js::registerSteps`.
- Searched for an existing living doc describing the dispatch-trail /
  dropped-parcel-sweep mechanism (`chase_sweep_lib.bb`,
  `dispatch_trail_cli.bb`, `route_backlog_to_coder.sh`): none exists as a
  dedicated how-to or reference page — BL-1097 and BL-1301, the two prior
  tickets in this same family this ticket's own `depends_on` names, were
  each recorded only as a dated `docs/reference/Specification.MD` entry
  (no how-to), confirmed by grepping `BL-1097`/`BL-1301` there. Followed
  the same established convention: added a matching dated top entry
  describing the widened clock, the new `DROPPED` verdict, and the
  router's three-way branch, cross-linking to the BL-1097/BL-1301 entries
  below it for the mechanism this extends. Did not invent a new how-to
  page for a mechanism whose own precedent tickets never got one.
- No `docs/index.md` change needed — no new page was authored.
- No registered diagram trigger fires: checked `docs/diagrams/handoff-flow.mmd`
  (the file/gate/inbox-in_process-completed state-machine diagram) directly
  — it does not depict the dispatch-trail verdict states (DISPATCHED/
  DROPPED/UNDISPATCHED) at all, consistent with BL-1097/BL-1301 also
  landing with no diagram change; `architecture.mmd`, `swarm-flow.mmd`,
  and `front-desk-flow.mmd` triggers (extension host/webview boundary,
  tmux substrate, `.swarmforge/` state layout, pipeline topology/backlog
  flow, front-desk routing) are likewise untouched by this ticket.
- No production code, test, or requirements change made by this pass —
  documentation only.

Result: NONE — no defect found; one changelog entry added, following the
family's own established no-how-to convention.
