# BL-1609 — cleaner review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

Babashka/Clojure — no mutation/CRAP/DRY tool wired (BL-472); gated only by
its own unit-test suite per the shared Design And Testability rule.

- Coverage/DRY review: `done_with_current_task.bb` and
  `done_with_current_batch.bb` each defined identical private
  `forwarding-inbound?`/`master-resident?` predicates. Moved both into
  `forward_evidence_lib.bb` (already loaded by both files, alongside the
  decision core and reader they feed) and updated both call sites — commit
  1ff8bc27e3.
- Architecture: the pure-decision-core / impure-orchestration split BL-654
  and BL-1422 established is preserved; the two moved predicates are
  read-only (file header / role-info reads), consistent with the lib's
  existing `sent-handoff-names-ticket-since?`.
- Mutation-site-size gate (BL-485): not applicable — TypeScript/`out/**`
  only; no `.ts` files changed by this parcel.
- Re-ran after the cleanup edit, all green:
  - `bb swarmforge/scripts/test/forward_evidence_lib_test_runner.bb` → ALL PASS
  - `bb swarmforge/scripts/test/bl1609_forward_gate_property_runner.bb` → ALL PROPERTIES HOLD
  - `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1609-*.feature` → 8/8
  - `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1422-*.feature` → 6/6
  - `test_idle_clear_respawn.sh`, `test_sidecar_tolerant_completion.sh`,
    `test_dispatch_lib_receive_mode.sh`, `test_handoff_state_dir_worktree_root.sh`,
    `test_lean_ledger_bb_wiring.sh`, `test_done_with_current_work_note_evidence.sh`
    → ALL PASS, exit 0 each.
- No `.ts`/`.js` in `extension/src`/`extension/test` touched by this
  parcel's commits since the received commit — `npm test`/property lane
  not run per the 2026-09-17 lane-set directive.
- `grep -rn "defn.*forwarding-inbound?\|defn.*master-resident?" swarmforge/scripts/*.bb`
  → each defined exactly once, in `forward_evidence_lib.bb`.
