# BL-1487 — architect review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

Reviewed commit 6a45723373 (cleaner, task BL-1487), on top of the received
782d55128b merge-up (BL-1489).

- **Two-layer/host-IO/webview/secrets/integrate-not-fork boundaries**: N/A —
  parcel touches no TypeScript/JS, no webview code, no host-IO code. Changed
  files: eight restored `swarmforge/profiles/*.{conf,prompt,yaml}` files and
  one shell test fixture edit (`test_smoke_check_stabilize_two_pack.sh`).
- **Dependency-gate hard check (BL-259)**: attempted
  `node extension/out/tools/dependency-gate.js <changed files>` — errored
  (`Can't open ... for reading`) because none of the changed files live
  under `extension/`'s scanned scope (`src`, `media`); the tool scans
  TypeScript/JS module graphs only. No TS/JS module was touched, so this
  gate has nothing to check for this parcel. Not run in full-repo mode
  (no broad refactor here to justify it).
- **Co-change coupling (BL-255)**: ran
  `node extension/out/tools/co-change-report.js` against the changed
  profile/test files. All SUSPECTED COUPLING hits (frequency >= 3) are
  among the eight profile files themselves (their own conf/prompt/yaml
  trios, which have always been created/deleted/edited as a set) plus
  `swarmforge.sh` (which syncs the directory) — pre-existing structural
  coupling, not new coupling introduced by this parcel. No action needed.
- **Restore-vs-retire ruling fidelity**: `git diff 4695d31402^ 692d2166bd --
  swarmforge/profiles/` is empty — the eight restored files are
  byte-identical to the pre-deletion state, matching
  `human_ruling: Restore the eight profile files from before 4695d31402`
  exactly (no hand edits).
- **Smoke-test fixture fix**: `test_smoke_check_stabilize_two_pack.sh`'s
  `make_good_fixture()` dropped its stale `window coordinator ...` line
  (BL-243 made the coordinator reserved infrastructure, never a declarable
  window) — correct, independent fix for the pre-existing red.
- **Standing-red register**: `bb swarmforge/scripts/standing_red_register_cli.bb .`
  — no row names BL-1487; all three rows this ticket owned were removed.
- **Re-ran verification independently** (not just trusting the coder's
  evidence):
  - `test_smoke_check_stabilize_two_pack.sh` — ALL PASS (9/9).
  - `test_shipped_confs_no_coordinator_window.sh` — ALL PASS (12 confs).
  - `run_acceptance.sh specs/features/BL-939-...feature` — 4/4 pass, 0 fail.
  - Ticket's own reference grep (`grep -rln 'swarmforge/profiles' ...`)
    returns 15 files (coder's evidence said 14 — recount, not a defect);
    every hit either names a path that now exists (launch.json, sync
    scripts, feature/step files, the how-to) or is a non-structural
    mention (a `fc.constantFrom` example string in
    `bl1233AmbientGitEnvNeverBlindsTrackedPathGuard.property.test.js`, and
    historical prose in `docs/briefings/2026-07-14.md`) that needs no
    target to exist under either ruling branch.
- **Invariants Review (BL-633/654)**: one declared invariant. Coder's
  evidence states a non-encodability reason (a whole-repo structural
  grep-fact, not a property over a pure module's input domain) rather than
  adding a vacuous fast-check property — reason is sound; agreed.
- **Property Testing pass**: no pure/testable module was touched by this
  parcel; nothing to add or improve. `npm run test:properties` not run —
  no property-shaped change to verify.
- **Correctness read**: no defect spotted beyond the checks above.

Verdict: architecturally compliant, ruling faithfully executed, all named
tests independently reproduced green. Forwarding to hardener.
