# BL-1297 — architect pass, 2026-08-30

## Verdict: PASS, forwarded to hardender.

## Checks run

- **Dependency gate (hard, BL-259):** `node out/tools/dependency-gate.js
  test/bl1295RevertAttributionInvariants.property.test.js
  test/bl1297MergeOwnPathsInvariants.property.test.js` (run from `extension/`)
  — PASSED, no forbidden edges. These are the only `extension/` files the
  parcel touched (test-only; no `src/` change).
- **Co-change tool (informational, BL-255):** run against
  `task_scope_gate_lib.bb`, `land_step_lib.bb`, and the new property test.
  Suspected-coupling hits are all files this commit or its natural siblings
  already touch (`task_scope_gate_lib_test_runner.bb`,
  `specs/pipeline/steps/index.js`, `swarm_handoff.bb`, prior BL-1192/BL-1276
  gate work) — no unexplained coupling.
- **Required wiring (BL-1297's own entry):** verified
  `bl1297MergeCommitOwnPathsSteps` is `require`d in
  `specs/pipeline/steps/index.js` (not merely present as a file).
- **Unit suites, re-run**: `task_scope_gate_lib_test_runner.bb`,
  `land_step_lib_test_runner.bb`, `unregistered_test_gate_lib_test_runner.bb`
  — ALL PASS, matches coder evidence.
- **Property suite, re-run**: `npx vitest run --config
  vitest.properties.config.mjs` on both
  `bl1297MergeOwnPathsInvariants.property.test.js` and
  `bl1295RevertAttributionInvariants.property.test.js` — 5/5 pass. Read the
  generator: reach floors are asserted per shape (merge/octopus-merge/
  empty-merge/root), the oracle is an independently constructed expected-path
  set (never a second `diff-tree` invocation), and invariant 2 directly
  cross-checks all three callers agree. Non-vacuous.
- **Acceptance, re-run**: `node specs/pipeline/cli.js
  specs/features/BL-1297-a-merge-commits-own-paths-are-not-empty.feature` —
  4/4 scenarios pass, matching the feature file's own four scenarios and the
  ticket's `qa_e2e`.

## Architecture / boundary review

- Change is confined to two `swarmforge/scripts/*.bb` libraries (extension
  host-adjacent tooling, not the VS Code extension's own src/webview
  surface) plus their tests and the pipeline step registration. No
  view/webview code touched, no I/O boundary crossed that wasn't already
  crossed, no browser storage, no secrets. N/A for most of the checklist.
- `land_step_lib.bb`'s `diff-readable?` probe now asks through the same
  `own-commit-changed-paths` walk it is vouching for, rather than a second
  independent `git` invocation — closes exactly the kind of two-invocations-
  can-disagree gap this ticket exists to fix. Correct call.
- Fix matches the ticket's directed choice (first-parent diff, not `-m`),
  handles the root-commit and unreadable-commit edges the ticket named, and
  keeps `nil`-vs-`[]` semantics as the fail-open/fail-safe signal each caller
  branches on (`mapcat` over `nil` is empty, matching the old contract; the
  `remove nil?` after is now redundant but harmless).
- BL-1295's step/property files were adjusted (moving the walk's base past
  the fixture merge) so BL-1297's fix doesn't retroactively break BL-1295's
  own scenario; verified both suites still fail against BL-1295's own
  defect (re-ran BL-1295 property tests above, still green and non-vacuous).

## Invariants review

Both declared invariants have live, non-vacuous property tests (see above).
No missing-property-test send-back.

## Routing note (not an architecture defect, recorded for visibility)

The ticket's own notes (specifier re-triage) directed this ticket through
`expedite.sh` ahead of the normal pipeline, since the defect is in the
delivery machinery. The commits actually reaching me show it went through
the normal live-swarm pipeline instead (promote → coder → cleaner →
architect, ordinary commit subjects, no expedite markers). This is a
routing/process question for the coordinator/specifier, not an architecture
defect in the parcel itself — not bounced on this basis.

No defects found. No send-back.
