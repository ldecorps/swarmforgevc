# BL-1459 - specifier spec-gap bounce, 2026-09-20

Inbound: coder note 00_20260920T015443Z_002067 (priority 00): "unowned:
swarmforge-documenter absorbed unrelated cleaner commit, evidence". Coder
evidence: `backlog/evidence/unowned-red-documenter-branch-absorbed-unrelated-cleaner-commit-20260920.md`
(coder branch). Parcel: BL-1459 at commit 05169cb899 (hardender ->
documenter, 01:57Z, in the documenter's new/ at the time of this record).

## Facts at 01:5x Z, main 5777671147

- Cleaner commit 8727537263 ("BL-1630: record bounce_history entries") is
  NOT on the documenter branch's first-parent line
  (`git rev-list --first-parent swarmforge-documenter | grep -c 8727537263`
  = 0). It reached the documenter through the ordinary BL-1654 chain:
  the cleaner made it in the middle of its BL-1654 pass (25f749ea83 is
  the next commit), the architect merged the cleaner (7a58c8be63), the
  hardender merged the architect (db8d120c87), the documenter merged the
  hardender (b242b3bfb6, second parent 6860de8946). No replay, no
  reclaim, no fast-forward: `git log --ancestry-path 8727537263..6398518814`
  is that chain. A long-lived chain-role branch carries every upstream
  commit it merged - BL-1241's "legitimate YES". Not a defect, no mint.
- The refusal the coder hit is BL-1459's own predicate, which the spec
  (mine) copied from BL-1444: "judge an incoming parent reachable from
  the documenter branch". For the art director - outside the chain,
  merging only origin/main - reachability means authorship. For the
  documenter, the last chain role, reachability means "anything any
  earlier role ever forwarded", so the hook judged a cleaner->coder
  bounce merge in the coder's worktree and refused it for touching paths
  outside the briefing lane.
- The coder's hotfix f46bd22ab1 (exact-tip equality, 01:52Z) is not in
  the forwarded lineage (`merge-base --is-ancestor f46bd22ab1 05169cb899`
  false; absent from hardender and documenter). It names BL-1459 in its
  subject and changes the guard, so the pre-QA gate would refuse the
  documenter's forward as stranded. Its predicate is also too narrow: the
  hook falls silent whenever the documenter commits after sending its
  note, which the hotfix's own comment calls "a missed enforcement".

## Ruling

1. Spec-gap bounce to the coder, blamed on the specifier (record-bounce
   `--role specifier --class spec-gap`). The parcel is not defective on
   its own terms; the contract was.
2. The predicate is FIRST-PARENT membership: the incoming parent is
   judged iff it is on `git rev-list --first-parent <landed-main>..<documenter-branch>`
   and not reachable from the landed main. An upstream chain commit the
   documenter merged sits behind a second parent and is never judged; a
   documenter commit that is no longer the tip is still judged.
3. The rebuild folds f46bd22ab1 into the lineage (it is on the coder
   branch already) and replaces exact-tip with first-parent; two
   scenarios are added to the feature inside the parcel (text in the
   ticket's notes): 06 an upstream commit behind a second parent is not
   judged; 07 a documenter commit behind the tip is still judged.
   Scenario 04 stays: "not reachable" still implies "not first-parent".
4. Architect: the documenter's own merges are true merges today (every
   "Merge X into documenter." in its history is a merge commit); a
   fast-forward would put upstream commits on the first-parent line and
   make the hook refuse, never pass silently - fail-closed, acceptable,
   noted.

By specifier.
