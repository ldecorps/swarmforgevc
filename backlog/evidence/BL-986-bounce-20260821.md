# BL-986 QA bounce, 2026-08-21

**Reviewer**: QA.
**Reviewed at**: documenter tip `30998e3dfe` (Document BL-986: standing-rule scanner now reads reference/ elaborations).

## BL-986's own work: fully verified, clean (Article 4.4 complete inventory)

- **required_wiring**: none declared for this ticket — n/a.
- **qa_e2e_procedure step 1** (`bb swarmforge/scripts/test/standing_rule_violations_lib_test_runner.bb`):
  `standing_rule_violations_lib: ALL TESTS PASSED`, including the specific
  KNOWN VIOLATION assertion (BL-252 = 1, BL-250 = 0) and the BL-986
  relocation/dedup/origin-exemption fixture assertions
  (lines 111–150 of the test runner).
- **qa_e2e_procedure step 2** (`node specs/pipeline/cli.js
  specs/features/BL-337-standing-rule-violation-observable.feature`, exit
  code read directly, never through `tail`): exit 0, 6/6 scenarios pass.
- **qa_e2e_procedure steps 3+4** (relocation neutrality, dedup): covered by
  the coder's own fixture-based unit assertions in the same test runner
  (`inlined-only` vs `relocated` vs `both` placements of the same rule
  citation) — a permanent, repeatable generative-safe encoding, stronger
  than a one-off manual file mutation. All pass.
- Additional hardener coverage independently re-run:
  `bb swarmforge/scripts/test/bl986_relocation_neutral_property_runner.bb`
  → `ALL PASS (300 runs)`;
  `bb swarmforge/scripts/test/standing_rule_violations_files_test_runner.bb`
  → `ALL TESTS PASSED`.
- **Scope check**: `git show --stat c921a4032` (BL-986's own coder commit)
  touches only `swarmforge/scripts/standing_rule_violations_*.bb`,
  `swarmforge/scripts/test/*.bb`, and its own evidence file. Zero overlap
  with `extension/src/**` or `extension/test/**`.

**BL-986 itself has NO defect.** Every gate it owns is green.

## D1 (behavior, blamed role: architect) — parcel's tree fails `conciergeTick.test.js` (2/111), traced to BL-979's un-reverted bounce riding this branch

`npx vitest run test/conciergeTick.test.js --config vitest.config.mjs` at
this parcel's commit (`30998e3dfe`, after `npm run compile`):

```
Test Files  1 failed (1)
     Tests  2 failed | 109 passed (111)
```

Both failures are the exact two the architect already found and bounced
for a DIFFERENT ticket, BL-979 (`backlog/evidence/BL-979-architect-bounce-20260821.md`):

- `test/conciergeTick.test.js:2199` — "BL-455: role-held tickets are joined
  to their backlog item epic/title..." — asserts the pre-pivot "matrix
  header carries the active ticket id(s)" shape.
- `test/conciergeTick.test.js:2436` — "BL-473: a ticket physically in
  backlog/active/ that no role holds still renders, in the not-started
  state" — same stale header-carries-id assertion.

BL-979 pivoted `pipelineBoard.ts` so tickets are rows and the header
carries stage glyphs; `conciergeTick.test.js` was never re-expressed for
that pivot and now fails against BL-979's own production code. The
architect found this on 2026-08-21, at cleaner tip `f9377ad27a`
(merged into architect at `701176a8d`), and **bounced BL-979 to coder**
(`c183c0b7a`, `bounce_history` recorded at `b6ec8c892`). BL-979 is still
`backlog/active/`, still un-refixed, still assigned to coder.

**Root cause of why this reaches BL-986**: per the constitution
("A Bounce Must Be Reverted Out Of The Bouncing Branch", BL-490/BL-495),
bouncing BL-979 does not itself remove the bounced commit — a revert is
owed in the same step. No such revert commit exists in this repo's history
for BL-979's `pipelineBoard.ts`/test changes (checked:
`git log --oneline --all | grep -i "979\|revert"`, and the code is
confirmed still broken at HEAD). The architect continued reviewing and
passing BL-986 — an entirely unrelated ticket — forward on the SAME
worktree branch that still carries BL-979's bounced-but-unreverted commits,
so hardener and documenter each merged that same contaminated tip forward
without noticing, and it now sits in the parcel handed to QA.

**Verified not a BL-986 defect**: `pipelineBoard.ts` and
`conciergeTick.test.js` do not appear in any of BL-986's own commits
(`c921a4032`/`30998e3dfe`) — see scope check above. Reverting BL-979's
content out of the tree (or landing BL-979's own coder fix first) would
clear this without touching a single line BL-986 wrote.

**Remediation**: either (a) revert BL-979's bounced commit(s) out of the
architect/hardener/documenter branch lineage per the constitutional rule,
so BL-986 can be re-forwarded on a clean tree, or (b) land BL-979's own
coder fix (re-express the two `conciergeTick.test.js` assertions for the
row-per-ticket shape, exactly as prescribed in
`backlog/evidence/BL-979-architect-bounce-20260821.md`'s own remediation
section) ahead of re-forwarding BL-986. Either way, BL-986's own ticket
needs no code change — do not route fix work to BL-986's coder.

## Outcome

Bounced to **architect** (earliest role able to clear the branch
contamination; also the role the constitutional revert obligation binds).
Class: `behavior` (a real, reproducible regression present in the parcel's
tree, distinct from BL-986's own domain). Task name carries BL-986's stable
name plus this defect summary.
