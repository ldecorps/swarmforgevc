# BL-1019 — QA bounce — 20260824

## Review inventory (Article 4.4)

Received documenter tip `852afd5025` for BL-1019. Sibling: `VERIFY BL-1019`.
HOTFIX pack/board MATCH `27273f2b0a`; BL-1113 acceptance **9/9**.

BL-1019's own surface (status/session + agent-child liveness) verifies green.
The tip also carries unfinished **BL-1101** (expedite mutation skip-fail) that
architect already bounced for bash 3.2 `set -u` empty-array expand — that D1
is still live in `expedite_mutation_sweep.sh` on this tip. Cannot land.

| Gate | Result |
|---|---|
| Sibling BL-1019 | PASS — `VERIFY` |
| Scope BL-1019 | PASS — swarm_status + marker/babysitter agree on UP/DOWN/unknown |
| Unit BL-1019 | PASS — `swarm_status_lib_test_runner.bb` ok |
| Acceptance BL-1019 | PASS — **5/5** |
| Hotfix stamp-off | PASS — MATCH `27273f2b0a`; BL-1113 **9/9** |
| Hitchhiker BL-1101 | **FAIL** — D1 below |
| BL-1101 acceptance (on tip) | PASS on Linux bash 5.2 — does not prove bash 3.2 contract |
| Orphans | NONE |

### D1 — behavior (hitchhiker / unfinished bounce) — blamed: coder

- **Failing command / contract:** tip tree includes
  `swarmforge/scripts/test/expedite_mutation_sweep.sh` lines that always
  expand `"${SURVIVORS[@]}"` / `"${SKIPPED[@]}"` into `emit_labeled_list`
  under `set -u` (architect bounce
  `backlog/evidence/BL-1101-architect-bounce-20260824.md` D1). On stock
  macOS bash 3.2 the green path (both arrays empty) unbound-errors before
  `ALL MUTANTS KILLED`.
- **Commit hash checked:** `852afd5025`
- **First error excerpt (from architect bounce, still true on tip):**
  ```
  emit_labeled_list "SURVIVORS …:" "${SURVIVORS[@]}" && fail=1
  emit_labeled_list "SKIPPED …:" "${SKIPPED[@]}" && fail=1
  ```
  No `"${#arr[@]}" -gt 0` guard before expand.
- **Failure class:** `behavior`
- **Expected vs observed:** Land tip for BL-1019 must not carry unfinished
  BL-1101 (architect-bounced, no hardener/documenter for 1101). Observed:
  BL-1101 yaml/feature/steps/script + bounce evidence ride the BL-1019
  documenter tip.
- **Remediation:** Strip BL-1101 from the BL-1019 land tip (or wait until
  BL-1101 bounce-refix clears D1 and re-tips cleanly). Re-forward BL-1019
  alone through the remaining chain. Owner: **coder**.

## Verdict

BOUNCE to **coder**. Do not land on `main`.

By QA.
