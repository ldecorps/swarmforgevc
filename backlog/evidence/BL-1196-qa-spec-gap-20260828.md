# BL-1196 — QA spec-gap hold (not approved, not forwarded)

**Failure class:** spec-gap (Article 4.4 table — "ticket instruction/spec is
stale relative to a proven, but lost, amendment"). No implementation defect
found; this is a HOLD on the ticket's own written scope, not a bounce to any
pipeline role.

## Parcel under review

Commit `2949f66695` (documenter), chain:
`4faabe579` (coder) → `71f23eff2` (cleaner) → `b484b7cf1` (architect) →
`d97c1cda7` (hardener) → `2949f6669` (documenter). Ancestry verified with
`git merge-base --is-ancestor` at each stage — genuine, unbroken chain.

## What the implementation does

`extension/test/helpers/gitEnvGuard.js`:
```js
function stripAmbientGitDirRedirect() {
  delete process.env.GIT_DIR;
  delete process.env.GIT_WORK_TREE;
}
```
Registered via `gitEnvGuardSetup.js` in both `vitest.config.mjs` and
`vitest.properties.config.mjs` setupFiles (confirmed by grep). Scoped unit
test green (4/4). Acceptance (`run_acceptance.sh` on the ticket's feature
file) green (2/2 scenarios). Full unit suite: 46 pre-existing failing files,
none touching this parcel's changed paths — confirmed already-tracked
(BL-720 CURSOR_API_KEY env-leak cascade + other pre-existing red files, see
[[cursor-api-key-test-leak-cascading-flake-20260730]] in QA's own memory) —
not a regression from this parcel.

**The pipeline built exactly, and only, what the ticket currently asks.**
Every stage (coder through documenter) explicitly and correctly scoped
`GIT_INDEX_FILE` OUT, citing: "those are the only two vars any existing
guard in this codebase has ever needed to clear; widen only if a future
incident actually implicates one." That out-of-scope text is present,
word-for-word, in the ticket YAML at every stage of this chain, including
the documenter's tip.

## Why this is a hold, not an approval

A commit `1c2ce143c` ("BL-1196: resurrect a falsely-closed prevention
ticket; root cause proven", authored 2026-08-28 02:43:43+01:00 — BEFORE
this chain's coder commit at 04:34:44) exists as a real object in this
repo (`git cat-file -t 1c2ce143c` → `commit`) but is **not an ancestor of
local `main`, `origin/main`, or any branch** (`git branch -a --contains
1c2ce143c` → empty). Per that commit's own message and QA's records of it,
it amended BL-1196 to bring `GIT_INDEX_FILE` INTO scope, based on a
root-cause proof (measured directly, not inferred) that a `git commit`
hook running from a linked worktree sets absolute `GIT_DIR` AND
`GIT_INDEX_FILE` (with `GIT_WORK_TREE` unset) in its child processes —
so a fixture that strips only `GIT_DIR`/`GIT_WORK_TREE` is not proven
sufficient against the actual hook-triggered corruption vector this ticket
exists to prevent. That amendment also reset `human_approval` to pending
pending specifier re-adjudication.

This amendment commit's absence from every current ref is consistent with
the ongoing BL-1214 main-reset-destroys-local-commits incident, which was
visibly still mid-recovery while I was checking (local `main` HEAD moved
under me during this review; reflog shows repeated "Restore N commits
destroyed by the BL-1214 main-reset bug" entries, and a live, separate
"Restore BL-1196: mis-pooled done->paused" bookkeeping commit landed on
`main` at 05:04:12 while I was writing this evidence file — a different,
earlier incident (BL-1216 duplicate-id pooling), not this one, and it
carries the SAME stale (pre-`GIT_INDEX_FILE`) ticket text). I am not
asserting a final, settled state of `main` — it is moving fast under
concurrent recovery work — only that as of this review, no ref anywhere in
this repo carries the `GIT_INDEX_FILE`-amended ticket text or scope.

**If closed now, BL-1196 would read as "prevented" a second time while the
actual proven corruption vector (`GIT_INDEX_FILE` via git hook env) remains
live** — repeating the exact failure shape already recorded against this
same ticket (falsely closed once already via the `f8a41c1e2` byte-identical
pooling bug, discovered unbuilt, resurrected, and now at risk of losing its
own resurrection's key finding to a second, unrelated incident).

## Disposition requested (specifier)

Not a coder/cleaner/architect/hardener/documenter defect — every stage
correctly implemented the ticket text it was handed. Please:
1. Confirm whether `1c2ce143c`'s `GIT_INDEX_FILE` finding still stands
   (re-run its scratch-repo proof if the commit itself can't be recovered
   from reflog/dangling objects).
2. If it stands: amend BL-1196 (or split a fast-follow ticket depends_on
   this one) to require stripping `GIT_INDEX_FILE` too, and route back
   through coder for the addendum before this closes.
3. If it does not stand (proof was wrong / superseded): confirm-promote
   this parcel as-is with recorded rationale, and I will re-verify and
   approve on the next pass.

Coordinator named for awareness given the intersection with the live
BL-1214 recovery and BL-1216 pooling bookkeeping — no bookkeeping action
requested from coordinator on BL-1196 itself until specifier adjudicates.

## What I did NOT do

- Did not approve, did not land anything on `main`, did not send a
  merge-up broadcast, did not close the ticket.
- Did not run the full, unscoped `npm run test:properties` (known hazard —
  can hijack a role branch's ref via inherited git-hook env, which is the
  same class of bug this ticket concerns). Only the scoped single-file
  vitest invocation and the acceptance runner were used.
- Did not touch `main` or `origin/main`.
- My merge of `2949f66695` into `swarmforge-QA` (this worktree only) is for
  review purposes; nothing has been pushed.

By QA.
