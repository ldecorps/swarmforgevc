# BL-613 QA bounce evidence — 2026-07-24

## 1. Failing command
No test command fails; this is a documentation-vs-code cross-check. Verification
commands run:

```
grep -rn "stuckInProcessTimeoutSeconds\|stuck_escalation\|chase-sweep-config" \
  swarmforge/scripts/*.bb swarmforge/*.conf swarmforge/packs/*.conf
grep -rn "stuck_escalation_threshold_seconds" . --include="*.bb" --include="*.conf" --include="*.sh"
```

## 2. Commit hash
`6f42409c94` (documenter's BL-613 commit, merged into QA at `9f58b3a68`).

## 3. First error excerpt

`docs/how-to/BL-349-stuck-role-escalation-email.md` states:

```
If a role is idle **past an escalation threshold** (currently 90 seconds):
...
The threshold is conservative (90 seconds for the default pack) ...
...
## How to Tune the Threshold

The escalation threshold is configured per pack in `swarmforge.conf`:

    config stuck_escalation_threshold_seconds 90
```

The actual production wiring (`swarmforge/scripts/handoffd.bb:42-54`):

```clojure
(def chase-sweep-config
  {:chaseIntervalSeconds 5
   :chaseTimeoutSeconds 30
   :maxChases 3
   :stuckInProcessTimeoutSeconds 60
   ...})
```

`stuckInProcessTimeoutSeconds` is a **hardcoded literal in handoffd.bb**, not read
from any conf file. A repo-wide grep for the doc's own config key,
`stuck_escalation_threshold_seconds`, returns zero matches anywhere in
`swarmforge/scripts/*.bb`, `swarmforge/*.conf`, or `swarmforge/packs/*.conf` — the
key does not exist. This is also internally consistent with BL-613's own coder
fix commit (`b86248ab7`), which repeatedly cites "the real 60s/3-nudge config"
and "60s threshold" as the actual production behavior it verified against.

## 4. Failure class
`behavior` — a documentation/behavior mismatch, not a compile/unit/acceptance
test failure. All automated gates (unit, acceptance, the wiring test itself)
are green; the defect is in the documenter's narrative content.

## 5. Expected vs observed
Expected: the runbook's stated threshold value and tuning instructions match
what the shipped code actually does, so a human following it during a live
incident gets the result the doc promises.
Observed: the doc claims a 90-second threshold tunable via
`config stuck_escalation_threshold_seconds <n>` in `swarmforge.conf`; the real
threshold is a hardcoded 60 seconds in `handoffd.bb`'s `chase-sweep-config`, and
`swarmforge.conf` has no code path that reads any `stuck_escalation_threshold_seconds`
key. A human editing `swarmforge.conf` per this doc's "How to Tune the Threshold"
section to change the alert timing would have zero effect and would not know why.
