# BL-1430 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Confirmed the root cause and fix are minimal and correct: `.worktrees`
  added to `EXCLUDED_DIR_NAMES` in both `bl874PortableTimeInvariants.property.test.js`
  and its sibling `tempDirTrapGuard.property.test.js` (the ticket's own
  "check them for the same hole in the same parcel" instruction) — neither
  file's assertion itself was weakened (invariant 1 holds); `git grep
  "function findPortableTimeViolation" -- extension/src specs/pipeline`
  confirms exactly one real definition.
- Verified the self-referential grep bug the coder caught and fixed (the
  acceptance step handler's own grep pattern, if written as a contiguous
  string literal, would match its own source once committed and tracked
  — fixed via string concatenation, the same trick `bl948SocketFixtureShortRootSteps.js`
  already uses): re-ran `git grep` directly, confirmed exactly one match,
  and the step handler's own text never contains the contiguous phrase.
- Ran the full properties suite: 18 files failed, and the 18 failing file
  names are byte-identical, sorted, to the 18 remaining
  `property_suite_standing_allowlist.tsv` rows — confirms invariant 3 (no
  new refusals, no red fixed by weakening) holds exactly.
- Confirmed both now-green rows (`bl874PortableTimeInvariants` and
  `tempDirTrapGuard.property.test.js`) are gone from both
  `backlog/standing-reds.tsv` and the allowlist.
- Reviewed the `bl1175PropertySuiteStandingRedsInvariants.property.test.js`
  floor-loosening (`>= 20` → `> 0`): correctly scoped — a hardcoded count
  is the wrong shape for a file whose purpose is to shrink as reds get
  fixed (BL-1428 pruned 25→20, this ticket 20→18); the real structural
  assertions (per-row shape, disposition, rationale) are unchanged.
- `jscpd` found cross-file duplication between the bl874/tempDirTrapGuard
  siblings — confirmed pre-existing and intentional (a file-level comment
  predating this ticket already states "mirrors tempDirTrapGuard.property.test.js's
  invariant-2 treatment exactly"); this parcel's own addition follows the
  same established mirroring convention, not new debt.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `npx vitest run --config vitest.properties.config.mjs test/bl874PortableTimeInvariants.property.test.js test/tempDirTrapGuard.property.test.js test/bl1175PropertySuiteStandingRedsInvariants.property.test.js` | 9/9, 6/6, 3/3 — all pass |
| `node specs/pipeline/cli.js specs/features/BL-1430-...feature` (run twice) | 2/2, 2/2 |
| Full properties suite | 18 files failed / 339 passed — the 18 failures exactly match the 18 remaining allowlist rows, byte-for-byte |
| `git grep "function findPortableTimeViolation" -- extension/src specs/pipeline` | exactly one file |
| `grep -i bl874\|tempDirTrapGuard.property backlog/standing-reds.tsv property_suite_standing_allowlist.tsv` | no matches (both rows removed from both files) |
| `bl1430PortableTimeGuardSingleDefinitionSteps.js::registerSteps` exists | yes (required_wiring) |

No fix required. Forwarding unchanged to architect.
