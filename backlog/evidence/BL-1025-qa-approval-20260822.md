# BL-1025 QA approval, 2026-08-22

**Reviewer**: QA. **Reviewed at**: documenter tip `832ae2b2a7`, merged into
QA. Ancestry confirmed: `git diff` against both merge parents shows only
additive content on each side.

## Prior bounce history checked (Article "A Prior QA Bounce Is Not In Your
Worktree")

One architect bounce on this ticket (`bounce_count: 1` in the ticket YAML,
`backlog/evidence/BL-1025-architect-bounce-20260822.md`, blamed coder):
`is_qa_ancestor.sh` re-derived `expedite_lib.bb`'s `advance-verdicts` set as
a hand-copied bash regex across a Babashka/bash boundary no import can
bridge, with a "kept in sync" comment and zero test enforcing agreement -
the exact BL-897 Guardrail hazard, word-for-word the comment text that
guardrail names as not a gate. Concrete failure mode named: a future fourth
advance-verdict token (the vocabulary was already extended once for
`forward`) would silently desync the two sides.

**Re-fix verified, not merely re-read**: commit `71ee902a2` removes the
boundary rather than policing it. `expedite_lib.bb`'s `qa-hat-verdict-record`
now writes the ALREADY-COMPUTED boolean (`:approval (= :advance class)`)
into the JSONL record; `is_qa_ancestor.sh` greps the literal
`"approval":true` and never re-derives any verdict token. `verdict` stays in
the record for human reading only, explicitly not load-bearing. This closes
the drift hazard structurally (one spelling of the vocabulary, in
`expedite_lib.bb`, full stop) rather than by adding more test coverage of
the same hazard.

## Verification order (Article 4.4, qa_e2e_procedure)

1. **Fault reproduction at the parent commit** was the architect's own job
   (pre-fix `is_qa_ancestor.sh` already existed and the original ticket's
   own investigation independently reproduced the false-CRIT against real
   BL-1021 expedite-run commits `44ef693d9`/`40e35d929`/`ed8a915f7` before
   any code was written - not re-run by me, already established fact).

2. **Independent test runs from my own worktree**, after merging the
   documenter's commit (clean merge, `git status` clean):
   - `bb swarmforge/scripts/test/expedite_lib_test_runner.bb`: **ALL PASS**.
   - `bb swarmforge/scripts/test/bl1025_expedite_approval_property_runner.bb`:
     **32 cases, exhaustive - ALL PROPERTIES HOLD**.
   - `bash swarmforge/scripts/test/test_is_qa_ancestor_expedite_store.sh`:
     **ALL CHECKS PASSED (14/14)**, including the two D1 gates specifically
     added for this bounce - "the predicate re-derives NO verdict token"
     (structural, non-vacuous) and the REAL writer's `forward`/`approved`/
     `pass` verdicts each round-tripping through the REAL predicate end to
     end (qa_e2e_procedure step 3/4) - plus every pre-existing fail-closed
     row (unreadable/corrupt/obstructed store, BL-972 self-report guard,
     BL-952 bounce veto surviving the new alternate approval path -
     qa_e2e_procedure step 5) still green.
   - `bash swarmforge/scripts/test/test_expedite_qa_verdict_store.sh`:
     **ALL CHECKS PASSED (11/11)**, including "the record is written by the
     run, not by hand" (a `--dry-run` writes no record; a real run writes
     exactly one, keyed on the QA stage, naming the run-branch tip the QA
     hat actually looked at, never `main`) - qa_e2e_procedure step 6.
   - Acceptance: `specs/features/BL-1025-expedite-approval-satisfies-the-
     pipeline-code-on-main-gate.feature` **6/6**; regression sweep on the
     two related suites the fix could plausibly disturb -
     `specs/features/BL-631-babysitter-detects-pipeline-work-on-main.feature`
     **17/17** and
     `specs/features/BL-952-a-bounced-parcel-never-reads-as-qa-approved.feature`
     **10/10** - both fully green, confirming the vocabulary-removal refactor
     did not regress the live-pipeline check or the BL-952 bounce veto.
   - No orphaned test/mutation processes before or after any run (the live
     swarm's own production `bb` daemons visible in `pgrep` are not test
     fixtures, left untouched).

3. **No JS/TS production files touched** - Babashka/shell only, matching
   the established posture for this class of surface this session.

4. **Documentation**: `docs/reference/BL-567-expeditor-manual.md` documents
   the `approval:true|false` field with a real example record, the
   bouncing-verdict case, and the three new test-suite rows in its
   verification table.

5. **BL-532 sibling check**: `qa-sibling-check.js status --ticket BL-1025`
   returned `VERIFY BL-1025` (exit 0) - no open deferral.

## Verdict

PASS. Landing on `main`.
