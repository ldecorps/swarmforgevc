# BL-1509 — hardender review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

(This file was first written to `extension/backlog/evidence/` by mistake
- `record-review-evidence.js` resolves its output path relative to the
caller's cwd, and this pass invoked it from `extension/`. Moved to the
correct worktree-root `backlog/evidence/` in this same commit.)

- Merged architect 6ba18e39d3 (BL-1509 merge_and_process) and, separately
  (non-forwarding, merge-only), QA's BL-1558 approval note - both already
  ancestors of this worktree's tip before this pass began.
- Stryker over the compiled CLI, `--mutate out/tools/send-telegram-document.js`:
  first run 58/65 killed, 7 survived (extractCaptionFlag's captionIndex
  boundary and its two slice bounds - every prior fixture put `--caption`
  at the argv tail; `sendAnnouncement`'s `if (forced)` short-circuit -
  every prior test set the force-result env var; the token/chat-id `||`
  guard - the prior test only covered both-missing; the USAGE string -
  never asserted). Added 7 targeted assertions/tests (2 new `parseArgs`
  fixtures covering `--caption` at index 0 and mid-argv; a stubbed
  `global.fetch` test for the real, non-forced send path; a
  one-var-missing config test; a stderr/USAGE assertion via an extended
  `runCli` helper that now also captures stderr). Every new assertion was
  hand-verified red against the corresponding hand-mutated `out/` line,
  then the file was restored (`diff` confirmed byte-identical) before
  committing. Re-run with `--force` (the file had picked up a stale
  `stryker-incremental.json` from an intermediate run, which reused the
  old survivor verdict for the USAGE mutant despite the new test -
  BL-1509's own stale-cache trap): 65/65 killed, 100%.
- Stryker over `--mutate out/notify/telegramClient.js` (`--force
  --dryRunTimeoutMinutes 15`; the plain run twice hit "Initial test run
  timed out!" - this 3138-line, 653-mutant file's perTest dry run needs
  longer than Stryker's default): 2 survivors fell inside `sendDocument`
  itself, both fixed and hand-verified red/restored the same way (a
  `new Blob([fileBytes])` -> `new Blob([])` mutant, since only the
  document part's filename was ever asserted, never its content; a
  `formatNetworkError('Telegram request failed', ...)` -> `''` mutant,
  since only the message text and token-absence were asserted, never the
  prefix). The remaining 107 survived / 19 no-coverage / 7 timeout sit in
  ~40 OTHER, pre-existing functions this parcel's diff never touches (no
  `stryker-incremental.json` existed anywhere in this worktree before this
  pass, so this is effectively telegramClient.ts's first full mutation run
  here) - sent as a priority-00 `unowned-survivors` note to the specifier
  per the accepted first-run-discharge amendment (2026-09-10), rather than
  chased in-pass (well over the ~50 threshold) or silently dropped.
- Found and fixed a genuine mutation-run hazard, not a test gap: a mutant
  skipping the force-result short-circuit made
  `sendTelegramDocumentCli.test.js`'s subprocess smoke test fall through to
  a real, un-timed-out `fetch` - `execFileSync` blocks the event loop
  synchronously, so vitest's own per-test timeout can never fire against
  it. Stalled a `--mutate out/tools/send-telegram-document.js` run for
  12+ minutes at 0% before being reaped by hand (own registered
  `detach_job.sh` job, killed by its own pgid). Added a bounded
  `timeout: 5000` to the subprocess call.
- BL-113 soft Gherkin mutation, both Scenario Outlines: 8/8 killed, 0
  survived/errors (manifest embedded in the feature file).
- CRAP: `sendDocument` 5.00, `parseArgs` 5.00, `extractCaptionFlag` 3.00 -
  all unchanged from the cleaner's own figures, confirmed against a fresh
  `vitest run --coverage` (the checked-in `coverage/coverage-final.json`
  was two days stale and briefly produced a false 0%/CRAP-flagged reading
  for both new-file functions - the documented "coverage report predates
  this parcel's tests" trap, not a real regression).
- `npx jscpd src --min-lines 8`: 48 pre-existing clones, none touching
  either changed file (unchanged from the cleaner's count).
- `vitest run --coverage` (full suite): 620 files / 10513 tests green.
  `vitest run --config vitest.properties.config.mjs` on
  `bl1509SendDocumentTokenRedactionInvariant.property.test.js`: 4/4 green.
- Acceptance pre-check (`run_acceptance.sh` on the parcel's own feature):
  5/5 green, `out/` freshly compiled before the run.
- `extension/stryker.config.json`'s `vitest.configFile` pointer (redirected
  mid-pass to the new include-scoped `vitest.bl1509.stryker.config.mjs`,
  which excludes the two known Stryker-sandbox-broken files
  `activePoolFreshnessAudit.test.js` and
  `bl1418RoleEnumerationClassification.test.js` per the standing
  2026-09-05 rule) was reverted to `vitest.config.mjs` before this commit -
  byte-identical to `main`. The scoped config file itself is kept
  (tracked) for any future scoped run on this ticket.
- No orphaned test/mutation processes at handoff (`pgrep -fl 'node
  --test|stryker'` clean).
