# BL-619 QA final pass — 2026-08-08

Reviewed commit `5cce7f60` (merge of documenter's `556aa5c493` into this
worktree; documenter's chain migrates `acceptance:` to a single-line path
form for the BL-761 acceptance-contract gate, on top of hardener's
`8b4a68c2`, architect's `340e238b`, cleaner's evidence, coder's slice).

## Disposition

`node extension/out/tools/qa-sibling-check.js status --ticket BL-619` ->
`VERIFY BL-619` (exit 0). No open deferral.

## Checks run

- `bash swarmforge/scripts/pre_qa_gate.sh BL-619-briefing-burn-rate-exhaustion-warning <HEAD>`
  -> `OK`.
- Acceptance (final gate, this ticket's own):
  `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-619-briefing-burn-rate-exhaustion-warning.feature`
  -> **14/14 scenarios pass** (all 9 named scenarios incl. the 2 Scenario
  Outlines' expanded examples).
- Unit suite: covered by the same full-suite run recorded in
  `BL-855-QA-pass-20260808.md` (411/411 files, 7292/7292 tests, Node 22) —
  includes `burnProjection.test.js`, `burnSectionText.test.js`,
  `usageAnchorStore.test.js`, `tokenBurnSectionCli.test.js`,
  `usageAnchorCli.test.js`, all green.
- CRAP: independently re-ran `node scripts/crapReport.js` against the 5
  touched files after a scoped `vitest run --coverage` on their own test
  files (49/49 pass) — **0 functions exceed CRAP<=6**, matching the
  hardener's own re-run. (First attempt without a fresh coverage run showed
  stale 0%-coverage numbers from an unrelated/absent report — re-ran coverage
  scoped to this ticket's 5 test files to get a live report before trusting
  the CRAP numbers.)
- Mutation: hardener deferred Stryker on the compiled JS via
  `mutation_cooldown_gate.bb`'s own busy-load check (load avg 9.31 vs the
  8.0 busy threshold at hardening time) — an existing, sanctioned gate
  mechanism in this codebase, not an ad hoc skip. In its place, hardener ran
  BL-113 Gherkin acceptance mutation on the feature's 2 Scenario Outlines
  three times, rewriting the decision-table's 4 example rows to sit exactly
  at their warn/ok boundary (survivors 14 -> 11), and traced every remaining
  survivor to either the mathematically-inevitable safe-direction case for a
  monotonic strict-inequality comparator, or a genuine BL-234 equivalent
  mutant (`isValidAnchorPct`'s flat range check). Read the trace personally;
  it holds. Accepted as the load-gated posture this project already uses
  elsewhere (matches the documented Stryker-under-severe-load practice) —
  not bounced for the deferred compiled-JS mutation run.
- Wiring: confirmed `token-burn-section` is a real adapter consumed by
  `briefing_email_lib.bb` (prepends `:leading-text` above the
  coordinator-authored body, marks the subject via `:subject-marker?`) and
  supplied by `handoffd.bb`'s adapters map
  (`:token-burn-section token-burn-briefing-section`, shelling to the
  compiled `extension/out/tools/token-burn-section.js` CLI) — not merely
  unit-tested in isolation.
- Correctness read: `burnProjection.ts`'s pure functions take no wall-clock
  reads (pinned `nowMs` threaded throughout, matches the ticket's own
  fixture-flake-trap requirement); anchor rate derivation, reset-window math,
  and the warn/ok decision match the ticket's `description:` §2 exactly.
- Orphan sweep before and after: `pgrep -fl 'node --test|stryker'` /
  `pgrep -fl vitest` -> none both times.

## Verdict

NONE — no defects found. APPROVE.
