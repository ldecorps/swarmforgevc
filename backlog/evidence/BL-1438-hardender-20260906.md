# BL-1438 — hardender review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Ran the full checklist: BL-1438's acceptance feature (4/4 scenario runs),
the coder's own property runner (32 generated cases), sibling features
BL-1431 and BL-1432 (green), all 18 whole-tree standing guards (183
tests, parcel touches specs/pipeline/steps/), and confirmed fixture
hygiene (the step handler already registers process-exit cleanup for
both its root and origin fixture dirs - no leak found there).

Considered but declined: invariant 1's "never on a rejected push" clause
has no dedicated fixture (push-rejection races are not deterministically
constructible without a new seam this ticket's infrastructure doesn't
have). Not pursued: the repoint call sits ONCE, structurally after every
early-`return` in land_main_publish.sh's run_land - a straight-line bash
function, not a branch a mutation could silently reroute around - and the
already-tested escalation scenario (03) demonstrates the same "early
return skips the repoint" shape a push-rejection test would. Building a
race fixture for this is BL-1144's domain (the push mechanics predate
this ticket and are explicitly out of scope), not a gap in BL-1438's own
new code.

Real coverage gap found and closed via hand-mutation (Babashka has no
wired mutation tool - BL-638 fallback): land_step_cli.bb's repoint verb
proves its root with `git rev-parse --git-common-dir` (the BL-1390
discipline the ticket's own How section names) before calling
post-land-repoint!, refusing exit 2 on a bad root - but nothing tested
this. Confirmed by deleting the guard: every existing test stayed green,
because post-land-repoint! itself silently folds a bad root into an
ordinary "skipped" outcome (exit 0) rather than crashing - the exact
silent-degradation shape that hides from a green suite. Closed with a
hardener addition asserting exit 2, the offending root named, and no
repoint verdict line printed at all. Confirmed the new assertion fails
against the hand-mutant and passes against the real guard; the mutant was
reverted with no diff before committing the fix (188c7b7493). Caught and
fixed my own first draft's fixture leak in the same pass (a real
`fs/create-temp-dir` for a path that never needed to exist) before it
reached this evidence.

By hardender.
