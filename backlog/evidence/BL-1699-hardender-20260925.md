# BL-1699 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: both gaps found (below) were in this stage's own domain
(mutation hardening / regression-suite integrity) and were fixed in
this pass, not bounced.

BL-113 Gherkin acceptance mutation (soft) over the feature's three
Scenario Outlines:
- Index 2 ("only a coder aider seat runs aider's test loop through
  the seat test verb") and index 3 ("the pack's aider timeout reaches
  the launch line only when it is declared"): 8/8 mutants killed, 0
  survivors. Manifest embedded in the feature file.
- Index 0 ("an aider seat's bootstrap text names no repo path, no
  pipeline script and no bang command", Examples column `role`:
  coder/QA/coordinator): all 3 mutants survived on the first pass
  (case-flip on each role literal). Investigated: mutating `role`
  either leaves the code in the same generic aider-bootstrap-text
  branch (coder/QA — case has no functional effect) or, for
  `coordinator`, diverts it into that SAME generic branch instead of
  the coordinator-specific orchestrator branch (`(= role
  "coordinator")` no longer matches "Coordinator"). Accepted as
  equivalent FOR THIS SCENARIO's own invariant only (no leaked repo
  path / scripts basename / bang-instruction): both branches of
  aider-bootstrap-text are clean of that invariant, so the mutation
  is unobservable by this scenario's Then step. The coordinator-vs-
  generic branch distinction itself is NOT untested — it is pinned
  independently by `prompt_engine_test_runner.bb`'s "aider coordinator
  text forbids coding" and by the fixed `agent_runtime_test_runner.bb`
  assertion below. Per BL-502 the manifest correctly omits this
  non-clean scenario from its `scenarios:` list.

Chasing the architect's own review through the pre-existing regression
suite (not just this ticket's new files) found two files still
asserting the PRE-BL-1699 aider bootstrap shape this ticket
deliberately removed, failing on this worktree tip before my pass:
- `agent_runtime_test_runner.bb`: 3 failures — "aider bootstrap
  includes /add", "aider bootstrap includes two-pack prompt in /add"
  (both asserted the old :add-files-then-paste shape aider no longer
  uses), and "aider coordinator mentions runtime draft path" (asserted
  the exact text invariant 1 exists to remove). Fixed by pinning the
  new :paste-prompt-file shape (`[:sleep :paste-file :submit]`, no
  `:send-literal` op at all) and asserting the draft path's ABSENCE.
- `test_alternate_runtime_launch.sh` case 07: same draft-path
  assertion, same fix (asserts absence now).
- `test_local_model_seat.sh` case 02: cosmetic only — its `pass`
  message still named "add-files-then-paste"; the assertion itself
  (bootstrap-style inequality) was unaffected. Relabeled to
  "paste-prompt-file" for accuracy.
- Grepped `swarmforge/scripts/test/` and `specs/pipeline/steps/` for
  every other `add-files-then-paste` / `handoff-draft.txt` reference:
  the remaining hits (`test_agent_runtime_lib.sh`,
  `test_swarm_handoff_sync_deliver.sh`, `connected/lib.sh`,
  `bl1518_handoff_draft_root_guard_property_runner.bb`) test the
  unrelated `handoff-draft-path` CLI/draft-file mechanism, which this
  ticket does not touch — confirmed still green, left unchanged.

Requirement 4's own negative case had NO regression coverage anywhere:
`seat test` must run UNSCOPED (SEAT_TICKET/SEAT_ACCEPTANCE both unset)
when aider's own `--auto-test` loop calls it directly and there is no
BL-1697 driver record for the seat — the acceptance feature's scenario
05 only exercises the "has a record" path, and the "no cmd configured"
early-exit in BL-1696's own feature returns before this code ever
runs. Added case 13 to `test_aider_seat_launch_config.sh`: a real
throwaway git checkout carrying the real `seat` +
`local_parcel_driver_cli.bb`/`lib.bb` closure (no driver record ever
written), confirming the configured `seat_test_command` runs with both
vars empty. Verified non-vacuity by hand-mutating the fallback to
unconditionally export `SEAT_TICKET=MUTATED`/`SEAT_ACCEPTANCE=MUTATED`
in `swarmforge/scripts/seat`: the new case caught it (`FAIL: 13:
expected ... unset ..., got: TICKET=[MUTATED] ACCEPTANCE=[MUTATED]`),
then the file was restored and confirmed clean via `git status`.

No TS/JS production file is in this ticket's scope (`git diff
b083762da6...HEAD --name-only`: two `.bb` files, one shell script, one
step handler, one property test, four `.bb`/`.sh` test files, the
golden-fixture check lives inline in the step handler) — CRAP/DRY are
not wired for Babashka/shell (engineering.prompt) and were not run.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1699-….feature`:
  10/10.
- `node specs/pipeline/cli.js specs/features/BL-1696-….feature`:
  16/16 (regression).
- `node specs/pipeline/cli.js specs/features/BL-1697-….feature`:
  12/12 (regression).
- `node specs/pipeline/cli.js specs/features/BL-1698-….feature`:
  9/9 (regression).
- `bash swarmforge/scripts/test/test_aider_seat_launch_config.sh`
  (13 cases, extended): ALL PASS.
- `bash swarmforge/scripts/test/test_alternate_runtime_launch.sh`
  (11 cases): ALL PASS.
- `bash swarmforge/scripts/test/test_local_model_seat.sh`: ALL PASS.
- `bash swarmforge/scripts/test/test_agent_runtime_lib.sh`: ALL PASS
  (unaffected sibling file, confirmed still green).
- `bb swarmforge/scripts/test/agent_runtime_test_runner.bb`: ALL PASS
  (post-fix).
- `bb swarmforge/scripts/test/prompt_engine_test_runner.bb`: ALL PASS.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1699AiderBootstrapNoRepoPaths.property.test.js`: 2/2
  (coder-authored; verified green, not authored by this pass so the
  full properties lane was not run per the 2026-09-17 rule).
- `npm test` (extension/): 638 files / 10872 tests, all green.
  Pre-existing per-file suite-budget warnings on 6 unrelated files
  (none touched by this ticket) are a standing condition.
- `pgrep -f '[t]mux -S' | wc -l`: 7 before, 6 after (this ticket's
  fixtures use fake `tmux` binaries, not real servers — no leak;
  fluctuation is other live swarm activity on the shared host).
  `git status --short` clean before commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner from a prior session; host load 3-12/20 cores throughout,
  well under the 2x-cores ceiling, no fallback needed.

By hardender.
