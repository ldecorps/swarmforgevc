
## Detail

Hardening pass completed 2026-09-09.

**Mutation testing**: Stryker full run hit a known sandbox-specific failure
(`checkFreshnessViaCli` fails in Stryker's dry run but passes in plain vitest,
documented in BL-1319). Fell back to hand-authored mutation sweep on the 5
critical logic paths in `withResolvedProblemStatement`, `buildSummaryBody`,
`taskStartedText`, and `approvalRequestedText`. All 5 mutants KILLED:
- Mutant 1: `withResolvedProblemStatement` always returns notes (ignores description)
- Mutant 2: `withResolvedProblemStatement` always returns description (no fallback)
- Mutant 3: `buildSummaryBody` does not apply firstParagraph to description
- Mutant 4: `taskStartedText` uses notes directly instead of resolved problem statement
- Mutant 5: `approvalRequestedText` uses notes directly instead of resolved problem statement

**CRAP analysis**: All functions changed by BL-1278 have CRAP <= 6:
- `withResolvedProblemStatement`: CRAP=3.00 (complexity=3, coverage=100%)
- `buildSummaryBody`: CRAP=5.00 (complexity=5, coverage=100%)
- `taskStartedText`: CRAP=2.00 (complexity=2, coverage=100%)
- `approvalRequestedText`: CRAP=3.00 (complexity=3, coverage=100%)
- `messageTextForEvent`: CRAP=6.00 (complexity=6, coverage=100%)

**DRY analysis**: jscpd reports 0 duplicates in changed files.

**Acceptance tests**: All 3 scenarios pass (approval card shows description,
fallback to notes when no description, TaskStarted card follows same rule).

**Unit tests**: All 82 tests in conciergeTopicRouting.test.js pass.

**Coverage**: Full suite (10433 tests) passes with coverage.

By hardener.
