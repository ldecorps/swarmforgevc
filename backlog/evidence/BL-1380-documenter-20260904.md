# BL-1380 — documenter pass (2026-09-04)

## Received
`b034a3aae6` ("tip-pure replay onto origin/main" — hardener rebuilt for a
clean send since the hardener branch's own history was entangled with
unrelated BL-1381 promotion bookkeeping; carries coder+cleaner+architect's
fix plus hardener's own BL-113 Gherkin mutation pass and CRAP/DRY
verification, property-suite guard bypassed for a pre-existing, unrelated
standing red).

## What changed, operator-visible
The exact BL-1367 gap (an approval route recording `human_approval:
approved` with no ruling, silently discarding a posed choice) existed on a
THIRD surface: the paused-pager Expedite route, not just the Approve
route BL-1367 already fixed. Live exposure: BL-1379, minted with three
`ruling_options`.

## Doc-domain review
`docs/how-to/BL-1367-approval-from-any-surface-carries-its-ruling.md`
already documents this exact class of fix in detail (two surfaces: bot
callback, pager approve) — BL-1380 is the natural extension, not a new
page. Added a section describing the third surface, and specifically the
DIFFERENT refusal condition Expedite uses (`ruling-required` only, never
`unknown-option`, since Expedite offers no label to validate — it weighs
only the ruling already on record) and why that difference is correct
(re-judging an already-answered ticket would leave the verb dead exactly
where BL-1083 built it not to be).
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-1367 summary line.
- Diagram registry checked — no diagram depicts the approval/ruling write
  path.

## Verdict
Doc content extended in the owning how-to page (not NONE) plus the
standard Specification.MD/index.md entries. Forwarding to QA.
