# BL-696 QA bounce — 2026-07-28

**Commit tested:** `57ac0b5c4` (HEAD) + uncommitted cursor-bridge / Let's Talk hardening parcel in worktree
**Route to:** `hardender` (earliest role owning coverage, CRAP, and mutation gates — Article 4.1 / QA.prompt)
**Prior pass attempt:** `backlog/evidence/BL-696-qa-pass-20260728.md` withdrawn — hardener gates not cleared

## Inventory (Article 4.4 — one bounce, complete)

| Gate | Result | Notes |
|------|--------|-------|
| `qa-sibling-check.js status --ticket BL-696` | PASS | `VERIFY BL-696` |
| Orphan `node --test` / `stryker` | PASS | none |
| Compile | PASS | |
| Full unit suite (`npm test`) | PASS | 373 files / 6370 tests (after coder fixes in worktree) |
| Property suite (`npm run test:properties`) | PASS | 86 tests |
| Acceptance (`run_acceptance.sh BL-696 feature`) | PASS | 8/8 |
| `pre_qa_gate.sh BL-696` | PASS | |
| Wiring (`bridgeServer.ts` `/lets-talk/turn`; console menu) | PASS | |
| Helper scripts (BB supervisors + start/stop smoke) | PASS | |
| **Hardener: CRAP ≤ 6** | **FAIL** | D1 |
| **Hardener: constitutional coverage (100%)** | **FAIL** | D2 |
| **Hardener: no surviving mutants** | **FAIL** | D3 |
| Live phone e2e (`e2e_qa_procedure`) | BLOCKED | operator-owned; not executable in CI gate |

## D1 — `unit` — CRAP > 6 on Let's Talk + cursor-bridge scope

**Failing command:** `cd extension && npm run crap:lets-talk-cursor-bridge`

**Commit hash:** `57ac0b5c4` (+ uncommitted sources above)

**First error excerpt:**
```
src/tools/telegram-cursor-bridge.ts	main	complexity=5	coverage=27%	CRAP=14.91  *** CRAP > 6 ***
src/bridge/letsTalkAudio.ts	extensionForMime	complexity=12	coverage=95%	CRAP=12.01  *** CRAP > 6 ***
src/tools/telegramCursorBridgeLive.ts	handleInboundDecision	complexity=11	coverage=82%	CRAP=11.75  *** CRAP > 6 ***
...
13 function(s) exceed the CRAP <= 6 threshold.
```

**Failure class:** `unit`

**Expected vs observed:** Article 4.1 / hardender.prompt require CRAP ≤ 6 on changed production code; observed **13 functions** over threshold in the scoped Let's Talk + cursor-bridge files.

**Blamed role:** `hardender`

**Remediation:** Refactor or split high-complexity functions — priority: `telegram-cursor-bridge.ts:main` (extract poll loop like `telegramCursorBridgeLive.ts`), `letsTalkAudio.ts:extensionForMime`, `telegramCursorBridgeLive.ts:handleInboundDecision` / `runCursorBridgePollOnce`. Re-run `npm run crap:lets-talk-cursor-bridge` until clean.

## D2 — `unit` — coverage below constitutional 100% on changed scope

**Failing command:** `cd extension && npm run coverage:lets-talk-cursor-bridge && node scripts/letsTalkCursorBridgeCoverage.js` (custom gate passes at 90%; constitutional bar is 100% per Article 4.1)

**Commit hash:** `57ac0b5c4` (+ uncommitted sources)

**First error excerpt:**
```
Overall: 90.5% (1335/1475 statements)
Coverage gate passed (>= 90%).
     27.9% (19/68)  src/tools/telegram-cursor-bridge.ts
     89.6% (43/48)  src/tools/start-bridge-headless.ts
     89.7% (105/117)  src/bridge/letsTalkLocalAudio.ts
```

**Failure class:** `unit`

**Expected vs observed:** Hardener gate requires **100%** statement coverage on hardened production files; observed **90.5%** scoped overall with `telegram-cursor-bridge.ts` CLI at **27.9%** (thin-wrapper / poll-loop blind spot per engineering-detailed.prompt).

**Blamed role:** `hardender`

**Remediation:** Extract `telegram-cursor-bridge.ts` `main()` loop behind injectable `shouldContinue` / one-shot `runCursorBridgePollOnce` tests; cover `start-bridge-headless.ts` and `letsTalkLocalAudio.ts` gaps. Target 100% on all files in the scoped mutate list.

## D3 — `unit` — surviving Stryker mutants on scoped cursor-bridge run

**Failing command:** `cd extension && npm run mutation:lets-talk-cursor-bridge` (hardener-owned; cited from last completed run — QA did not re-run per QA.prompt)

**Commit hash:** `57ac0b5c4` (+ uncommitted sources)

**First error excerpt:**
```
All files                     |  65.03 |   72.15 |     1054 |       244 |        501 |      197 |        0 |
 letsTalkCore.js             |  58.50 |   60.08 |      280 |         9 |        192 |       13 |        0 |
 telegram-cursor-bridge.js   |  56.86 |   87.88 |        8 |        50 |          8 |       36 |        0 |
 telegramCursorBridgeLive.js |  67.89 |   76.61 |      126 |        41 |         51 |       28 |        0 |
```

**Failure class:** `unit`

**Expected vs observed:** Hardener gate requires **no surviving mutants**; observed **501 survived** (65.0% mutation score) on scoped Let's Talk + cursor-bridge production files.

**Blamed role:** `hardender`

**Remediation:** Kill survivors with targeted tests; re-run `npm run mutation:lets-talk-cursor-bridge` until `survived=0` on scoped files. Report: `extension/reports/mutation/mutation.html`.

## Verdict: BOUNCE → hardender

QA-owned gates (unit, properties, acceptance, wiring) are green on the current worktree. Constitutional hardener gates are not. Forward through documenter after hardening; QA will re-run the full inventory.
