# BL-1386 + BL-1387 — LAND SUCCESS, 2026-09-04

Route 1 of the specifier's adjudication
(`backlog/evidence/BL-1386-land-escalate-adjudication-20260904.md`), extended
to cover BL-1387 once it independently reached QA and hit the identical
residual (see `backlog/evidence/BL-1386-land-escalate-20260904.md` and the
memory-recorded class: a "landed" sibling whose exclusive files are absent
from `origin/main`).

## Hand-built tip-pure commit

Built in scratch worktree `/tmp/land-bl1386-1387`, off `origin/main` at
`94be289136`. Own-paths (43 files) derived from the coder/cleaner/architect/
hardener/documenter evidence trail for both tickets, cross-checked against
`git diff --name-only origin/main <QA-tip 1226a6494b>`:

- Both tickets' own evidence, feature, step-handler, CLI, and `.bb` files —
  whole checkouts.
- `docs/index.md`, `docs/reference/Specification.MD` — line-level splice
  (both are append-only shared files carrying other tickets' entries too;
  `Specification.MD` used its own "Prior entry —" stack markers as splice
  boundaries, dropping BL-1385's block in between BL-1387's and BL-1386's).
- `extension/docs/briefings/2026-09-03.json`, `backlog/INTAKE-human-expedite-park-must-unhold-on-finish-1788436320924.md`,
  `backlog/done/BL-1337-....yaml`, `backlog/done/M8/BL-1328-....yaml` — untagged
  touches, kept per BL-1315 (same disposition as BL-1376's land).
- **Dropped**: BL-1367's six exclusive files (`bridgeServer.ts`,
  `pendingApprovalReply.ts`, three test files, one step handler) and BL-1385's
  three how-to edits (bounced back to cleaner this session,
  `backlog/evidence/BL-1385-bounce-20260904.md`).
- **Corrected**: `specs/features/BL-1387-...feature` — took the QA-tip's
  version, not `origin/main`'s. `origin/main`'s copy was stale (missing the
  specifier's scenario-06 retirement amendment: Row "an ownership record
  naming the MERGE_HEAD sha" was still present as an Outline example instead
  of retired to its own scenario). Confirmed by diff before deciding; using
  `origin/main`'s copy as the specifier's adjudication literally said would
  have shipped the stale contract and dropped scenario 06 outright — caught
  by re-running acceptance on the assembled tree (7/8, one Outline example
  failing) before push, not assumed clean.

## Re-verified on the tip-pure tree (not merely content-equal)

- `npm run compile` — clean.
- `bb swarmforge/scripts/test/master_main_reconcile_lib_test_runner.bb` —
  ALL TESTS PASS.
- `bb swarmforge/scripts/test/post_hotfix_merge_origin_lib_test_runner.bb` —
  ALL TESTS PASSED.
- `bb swarmforge/scripts/test/master_main_reconcile_lib_property_runner.bb`
  — 500 runs, ALL PROPERTIES HOLD (BL-1386 and BL-1387 generator-reach
  non-vacuity both confirmed).
- `specs/pipeline/scripts/run_acceptance.sh` on both tickets' feature files
  — BL-1386 7/7, BL-1387 8/8 (after the feature-file correction above; first
  attempt with `origin/main`'s stale copy was 7/8).
- `bash swarmforge/scripts/test/test_handoffd_master_main_reconcile_wiring.sh`
  — ALL SCENARIOS PASS.
- `bash swarmforge/scripts/check_feature_handler_registration.sh` — rc 0.
- `git diff --diff-filter=D origin/main HEAD` — empty both before and after
  the abandoned_commits follow-up push (nothing origin/main had went
  missing).

## Landed

- Tip-pure commit `04543cb639` off `origin/main` at `94be289136`. Both
  `land_main_publish.sh --decide-only` calls (before acquiring the lock, and
  again immediately before push with the lock held) read `:next :push`,
  `origin-advanced-since-gate: false`. Pushed `94be289136..04543cb639`.
- Follow-up commit `ef78aa9116`: `abandoned_commits` recorded on both ticket
  YAMLs — `[67cd1d8dd7]` on BL-1386 (the refused automated replay) and
  `[..., d4e1491cbd]` appended on BL-1387 (its own refused automated
  replay). Pushed `04543cb639..ef78aa9116`.
- Neither push carried any BL-1367 or BL-1385 content — confirmed by the
  43-file own-paths list above, not by trusting the tool.
- Both pushes went through `land_main_publish.sh --acquire-lock` /
  `--decide-only` / `--release-lock` (BL-1144 discipline), run from the
  scratch worktree holding the candidate commit (not the master checkout —
  see memory `land-main-publish-root-must-be-candidates-worktree`).
- Scratch worktree `/tmp/land-bl1386-1387` removed after the second push.

## Not a GH-seeded ticket

Neither `BL-1386` nor `BL-1387`'s `id` is `GH-<n>`; no `issue_done.sh` step
applies.

## Merge-up

Broadcasting to `coder,cleaner,architect,hardender,documenter`: merge up to
`ef78aa9116`, not `origin/main`'s prior tip — the local `main` ref in the
master checkout is unaffected by this push (BL-891 residual, still
unowned).

By QA.
