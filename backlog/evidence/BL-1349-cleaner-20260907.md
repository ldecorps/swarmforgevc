# BL-1349 — cleaner review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

The received parcel (merge of coder's `6b6427d0dd`, architect-bounce fix
commit) had, on inspection, LOST the ticket's actual numRuns tuning: the
architect's bounce revert (`7d5a9e5c96`) correctly reverted the whole merge
it landed via, but the reverse-hop merge-only inbound carried that revert
into coder's branch too, so coder's bounce-fix commit was built on a tree
where the three property files' tuning was already gone — only the
step-handler git-ref fix (the D1 defect itself) survived. Cleaner's own
branch independently inherited the same reverted state, and a routine
3-way merge kept it since only cleaner's side had touched the files since
the shared merge-base. Caught by re-running the ticket's own acceptance
after merging: `spawn-heavy-file-fits-budget-01` failed all three examples
at their pre-tuning durations (~74s/~83s/~33s vs the 15s budget), while
`no-property-is-dropped-02` passed — proving the no-deletion check is now
genuinely non-vacuous (it just had nothing to catch, since no property was
literally deleted, only the numRuns edit was lost).

Fixed by restoring the three files' content from `6b6427d0dd` (commit
`832429ff79`, "BL-1349: restore spawn-heavy numRuns tuning dropped by the
bounce-revert merge") and re-running the acceptance: `spawn-heavy-file-fits-
budget-01` examples 1 and 3 pass in isolation (7.1s, 13.3s); example 2
(`bl1252CommitGuardAggregationInvariants`) intermittently exceeds budget
under this host's current contention (re-ran isolated twice, ~40-47s both
times) — the same class of host-contention degradation the coder's own
evidence already documented and the ticket's `out_of_scope` explicitly
excludes (fork count / per-worker contention is BL-1348, currently
re-pended for the same "breaks the property suite on full-forge Linux"
reason per this session's own memory). Not treated as a BL-1349 defect: no
code in this ticket's scope caused it, and the tuning itself (numRuns
120→60) is unchanged from the coder's own reach-tradeoff analysis.

This was NOT a defect owned by any pipeline role's normal domain review
(dependency gate, invariant encoding, structure) — it was a merge-mechanics
casualty of the reverse-hop protocol interacting with a bounce-revert, so
it is recorded here as a self-caught-and-fixed finding rather than a bounce
(Never Blind-Forward a Bounce You Cannot Fix does not apply: this was
fixable in-domain, by restoring dropped content, not a defect requiring
another role's ownership).
