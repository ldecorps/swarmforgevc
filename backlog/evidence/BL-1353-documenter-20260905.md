# BL-1353 — documenter pass (2026-09-05)

## Scope reviewed

Re-read the ticket (human ruling: retire `TASK_ARRIVED` entirely), the
acceptance feature
(`specs/features/BL-1353-task-arrived-is-not-an-escalation.feature`), and
the full chain of prior evidence (coder `BL-1353-coder-20260905.md`, cleaner
`BL-1353-cleaner-20260905.md`, architect `BL-1353-architect-20260905.md`,
hardener `BL-1353-hardener-pass-20260905.md`).

## Documentation surfaces checked

- `docs/how-to/BL-653-operator-escalation-driven-wake-model.md` — the coder
  already extended this in place (commit `54a1d8df9a`, carried through the
  pipeline): `TASK_ARRIVED` added to the quiet-night regression checklist
  (§"If launches appear on a quiet night") and a new "What's retired" bullet
  explaining the removal, the measured 2026-09-02 counts, and that the
  freshness probe itself is untouched because `closing-pass-sweep!` still
  reads it for the BL-307/BL-310 relaunch up-trigger. This satisfies the
  ticket's `required_wiring` anchor (the doc must name `TASK_ARRIVED` in
  either its legitimate-sources table or its What-retired list — it does, in
  the latter). Read the diff line by line against the ticket's invariants:
  both are preserved in the prose (no wake source removed before a live
  escalation producer covers it; the freshness probe's second consumer is
  called out explicitly as unaffected). No further edit needed to this file.
- `docs/reference/Specification.MD`: no existing entry mentioned
  `TASK_ARRIVED`'s retirement. Added a new dated entry (this commit) per the
  file's standing one-entry-per-landed-ticket convention.
- Grepped `docs/` for `TASK_ARRIVED`: only the BL-653 how-to references it,
  and it is already current.
- Registered diagrams (`architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`): none depict operator
  wake-source selection at this granularity; none of their change-triggers
  fired.

## Verdict

Specification.MD entry added (this commit). No other documentation defect
found — the required how-to edit had already landed correctly upstream in
the pipeline.

By documenter.
