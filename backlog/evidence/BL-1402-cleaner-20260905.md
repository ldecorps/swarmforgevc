# BL-1402 — cleaner pass, 2026-09-05

Clean sweep. No changes needed; forwarding as-is.

## Checks run

| check | result |
|---|---|
| `test/telegramFrontDeskBotCore.test.js` | 457/457 |
| `test/bl1402FrontDeskPhotoPassthrough.test.js` | 9/9 |
| `test/atomicWrite.test.js` | 7/7 |
| property `bl1402FrontDeskPhotoPassthroughInvariants.property.test.js` | 3/3 |
| acceptance `BL-1402-...feature` | 6/6 |
| acceptance `BL-620-...feature` (regression) | 13/13 |
| acceptance `BL-955-...feature` (regression) | 8/8 |
| `jscpd` over the three touched files | 9 pre-existing clones, none in this parcel's changed line ranges (confirmed by diffing the changed line ranges against jscpd's reported clone spans) |

## Module structure review

`persistRoutedPhoto` and its helpers (`existingMediaFile`, `pruneMediaStore`,
`extensionForPhotoMimeType`, `mediaStoreDir`) form one cohesive, well-scoped
unit mirroring the existing `resolveVoiceAudio` pattern and reusing
`getFile`/`downloadTelegramFile` rather than re-implementing them. No
duplication with `cursorBridgeTelegramMedia.ts`'s bridge-side photo path —
that file is untouched (confirmed empty diff) and this ticket's own store,
by design, is a separate on-disk location for a separate consumer (the
operator, not the bridge).

`annotateSavedPhotoPath` / `formatPhotoPersistFailureAuditLine` /
`persistPhotoIfRouted` in `telegramFrontDeskBotCore.ts` are pure,
single-purpose, and deliberately kept separate from
`annotateRoutedMediaText`/`formatDropAuditLine` (BL-620) per the ticket's
own invariant 2 — reviewed and correct as landed, no rework needed.

The three call-site wirings in `processMessageUpdate` are one-line
compositions (`annotateSavedPhotoPath(annotateRoutedMediaText(...), photoOutcome)`)
at each of the three posting boundaries — no copy-pasted logic, the shared
behavior lives once in the two pure functions being composed.

## Mutation-site count (BL-485)

```
node extension/out/tools/mutation-site-count.js extension/src/tools/telegram-front-desk-bot.ts extension/src/tools/telegramFrontDeskBotCore.ts extension/src/util/atomicWrite.ts
```

| file | sites | verdict |
|---|---|---|
| `telegram-front-desk-bot.ts` | 1943 | over (pre-existing file, same as BL-1383's cleaner pass) |
| `telegramFrontDeskBotCore.ts` | 2018 | over (pre-existing file, same as BL-1383's cleaner pass) |
| `atomicWrite.ts` | 19 | within |

Both over-threshold files are the same pre-existing legacy monoliths flagged
in BL-1383's cleaner pass two parcels ago — this parcel adds ~75 lines total
across the two of them for a self-contained feature. Splitting either file
is out of scope for a `slice_size_envelope: low` wiring addition; no new
file this parcel introduces is itself over the cap.

By cleaner.
