# BL-1761 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 8b1078e562 (already an ancestor of the QA tip). qa-gather
on the QA tip (report kept at `tmp/BL-1761-gather.json`): straggler check
clean before and after, `VERIFY BL-1761`, register 6 rows `unowned: []`,
`pre_qa_gate.sh` OK, `npm test` exit 0, `npm run test:properties` exit 0,
and the BL-1761 acceptance feature passed 2 of 2.

Change read: `resolveConfirmedFile` resolves a leading `extension/` against
the extension root itself, so it works both in a real checkout and inside a
sandbox. Absolute paths pass through unchanged, and any other relative path
still resolves against the repo root. The hardener's `8bd7b1f566` repairs the
test's own `../..` walk-up without weakening an assertion. The hardener's
Stryker dry run no longer names `confirmPoleAloneOutcomeShapes.test.js`. That
dry run still fails on other sandbox-fragile files, which the hardener
censused and which are out of this ticket's scope (see BL-1771).
`required_wiring` handler `bl1761ConfirmPoleAloneSandboxSteps.js` is present.
