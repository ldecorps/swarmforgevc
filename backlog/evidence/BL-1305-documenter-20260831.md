# BL-1305 documenter pass — 2026-08-31

## Inbound

Merged hardener commit `0738994b12` into `swarmforge-documenter` (merge
commit `f836ee7...` — see this branch's merge log for the exact SHA).

## Scope check

Re-read the ticket (`backlog/active/BL-1305-fixture-agent-binary-is-the-stub.yaml`)
and the coder/cleaner/architect/hardener evidence trail. The change is
entirely acceptance-fixture/test infrastructure
(`specs/pipeline/steps/roleLifecycleParkUnneededSteps.js`,
`specs/pipeline/steps/bl1305FixtureAgentBinarySteps.js`, their unit/property
tests) — no CLI command, setting, flow, or other user-visible behavior
changed. No production `extension/src/` or `extension/out/` file touched.

## Review inventory (Article 4.4) — one pass, complete

- **D1?** README / command docs: N/A — no user-visible command or setting
  introduced or changed. No defect.
- **D2?** `docs/reference/Specification.MD`: checked for any RETIRED/
  superseded reference to this mechanism (Article 3.6) — none found. No
  content change needed there, so no "Last Updated" bump (bumping without
  a content change is itself against the documenter's own rule). No
  defect.
- **D3?** Constitution-registered diagrams
  (`architecture.mmd`, `swarm-flow.mmd`, `handoff-flow.mmd`,
  `front-desk-flow.mmd`): none of their change-triggers fired — this
  parcel changes neither the extension host/webview/tmux-substrate
  boundary, the pipeline topology/backlog flow, the handoff file
  lifecycle, nor front-desk routing. It is an internal APS fixture fix.
  No defect.
- **D4?** Whether this incident is worth a durable writeup, per this
  project's established pattern (BL-817, BL-1038, BL-1039, BL-1230, all
  fixture/test-hygiene fixes with their own `docs/reference/*.md` page):
  yes — this closes a real-billable-agent-boot defect the same way BL-817
  closed a tmux-server leak, and a future reader auditing "why does
  `roleLifecycleParkUnneededSteps.js` use `ZDOTDIR`" needs the same kind of
  pointer BL-817 provides for the reaper idiom. **Wrote**
  `docs/reference/BL-1305-fixture-agent-binary-defeats-path-shim.md` and
  linked it from `docs/index.md`, committed as `1174e64bb3`.

## Verdict

NONE — no doc defect found in the inbound work; one durable reference page
authored per the project's established documentation pattern, not owed as
a remediation. Forwarding `1174e64bb3` to QA.

By documenter.
