# BL-1431 documenter review pass — 2026-09-05

Reviewed the hardened commit (`bcce206d68`) and the coder/cleaner/architect/
hardener trail for BL-1431 (one land plan reads one tip).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1431-one-land-plan-reads-one-tip.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1431OneLandPlanOneTipSteps.js`; the
  `land_step_cli.bb::origin-main-sha` entry-point resolution required by
  the first wiring line is present (`swarmforge/scripts/land_step_cli.bb`
  line ~80 resolves it once and threads `:origin-main` at lines ~83/97).
- Read `swarmforge/scripts/land_step_lib.bb`: confirmed `land-plan`'s
  `:origin-main` is an OPTIONAL key (invariant 1) — when present, that
  exact value threads to `entangled-siblings`, `own-paths`,
  `main-ticket-sources`, and the per-path attribution map without any of
  them re-resolving `origin-main-sha` by name; when absent, `land-plan`
  itself resolves once at its own entry (the pre-existing direct/test
  contract, unchanged).
- Operator/QA-visible behavior change (a mint or approval landing on
  `origin/main` mid-walk can no longer produce a false
  `could-not-read-attribution` `LAND_ESCALATE` — the two BL-1416 and one
  BL-1407 false escalations on 2026-09-05 this ticket fixes) is
  documented in
  `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`
  — the existing land-step how-to, same doc already carrying the BL-1405
  hand-built-land section — as a new "One land plan reads one tip, immune
  to a moving `origin/main` (BL-1431)" section, describing the mechanism,
  the unchanged publish/fail-open invariants, and the BL-1432 sibling
  (walk cost, out of scope here).
- `docs/index.md`'s existing one-line summary for the BL-1241 how-to
  (a running list of parenthetical per-fix clauses) extended with a
  BL-1431 clause in the same style.
- No "Last Updated" freshness field in the edited doc — no date bump
  owed.
- No registered diagram depicts this mechanism's internal read-ordering
  (a bugfix inside the land step's own sha-resolution plumbing, not a
  new component, boundary, or pipeline-topology change) — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
