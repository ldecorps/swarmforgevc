# BL-822-resource-anomalies-miss-host-load-spikes — QA final pass (2026-08-08)

## Commit checked

`a7e8536c` (QA worktree merge of documenter's `8bb7f48af5`, batched with
BL-839/BL-819/BL-773). Ancestry confirmed: coder `8a65d548`, cleaner
`25c24fd1`, architect `de0d8701`, hardener `cd67e036`, documenter `8bb7f48a`
are all ancestors of `a7e8536c`.

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`, node v22.23.2): clean.
- Full unit suite (`npm test`): 406/406 files, 7215/7215 tests green. See
  `backlog/evidence/BL-773-qa-pass-20260808.md` for the node-version
  environment note (this ticket's own files —
  `resourceTelemetry.test.js`/`costHealthSidecar.test.js`/
  `sampleResourcesCli.test.js` — were green under both node versions,
  unaffected by the dependency-cruiser/node-22 issue).
- Property tests (`npx vitest run --config vitest.properties.config.mjs
  test/bl822HostLoadAnomaly.property.test.js`): 6/6 PASS, independently
  re-run.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-822-resource-anomalies-miss-host-load-spikes.feature`):
  9/9 scenarios PASS.
- `required_wiring`, independently re-verified by reading the code (not
  taken on the architect's word):
  1. `extension/src/notify/costHealthSidecar.ts::hostLoad` — field exists
     (`hostLoad?: HostLoadVerdict`) and is actually set
     (`sidecar.hostLoad = hostLoadVerdict`).
  2. `extension/src/notify/costHealthSidecar.ts::renderAnomalyLines` — the
     "none found" guard is `if (anomalies.length === 0 && !severe)`, where
     `severe = hostLoad?.severe === true` — confirmed the rendered verdict
     genuinely consults host load, not just the JSON field.
  3. `extension/src/tools/sample-resources.ts` — the headless sampler's own
     doc comment confirms it records host load on the same tick,
     independently of whether any role pid resolved (BL-350 headless-gap
     posture preserved for the new signal).
- Invariants: all three declared invariants have real property coverage
  (severe-day-never-quiet with anti-vacuity, per-role detection unchanged/
  additive-only, `resourceSamplesObserved` keeps its narrower meaning) —
  confirmed via the independently re-run property file above.
- No orphaned test/mutation processes before or after this pass.

## Verdict

Clean pass. No defects. Approved as part of the BL-839/BL-819/BL-822/BL-773
batch — see `backlog/evidence/BL-773-qa-pass-20260808.md` for the shared
merge-up/landing details.

By QA.
