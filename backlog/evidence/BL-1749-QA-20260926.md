# BL-1749 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 39c8a1a182, merged into QA as 2df172d67c (checked against
both parents; BL-1700's paths are kept out after its bounce and revert). The
step change (3c37458ca7) arrived with the 741f2b017d merge.

Gates, one run each on 2df172d67c, run in a detached git worktree at that
commit (full history) with full logs:
- `qa-sibling-check.js status`: `VERIFY BL-1749`. `pre_qa_gate.sh BL-1749
  2df172d67c` (from the QA worktree): OK (the ticket declares no
  required_wiring).
- standing-red register: 8 rows, `unowned: []`. Row 36 (BL-877's acceptance
  feature) names BL-1749 and retires at the land.
- `npm test`: 638 files, 10875 of 10875 passed.
- `npm run test:properties`: 466 files, 1345 of 1345 passed.
- qa_e2e step 1: `node specs/pipeline/cli.js
  specs/features/BL-877-portable-process-liveness-without-proc.feature` passed
  7 of 7 in 16 s, including scenario 7 ("a host with no working liveness
  facility says so") on this Linux host.
- Stragglers: none of QA's own; the only other live process was a BL-1766 land
  step.

Change read: the step "a host on which no liveness facility can be reached"
now sets `SWARMFORGE_PROC_DIR` to a nonexistent path as well as
`SWARMFORGE_LSOF_BIN`. `rootLiveProcessIn` rewrites `SWARMFORGE_PROC_DIR` only
when a procfs userland is forced, and scenario 7 forces none, so no later
Given points it back at a real or fake proc.

qa_e2e step 2 (own paths): `specs/pipeline/steps/bl877PortableProcessLivenessSteps.js`
plus evidence and the ticket's `abandoned_commits` (the coder's reverted first
attempt d993128030/f620ebc293). The land is tip-pure.
Article 4.5: no production path changed (step handler only).
