# BL-1760 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: the full checklist was run and found no defect.

BL-113 Gherkin mutation: both of the feature's scenarios are plain
`Scenario:` — no `Scenario Outline:`/Examples table — so the tool
correctly reports `inapplicable` (BL-638), recorded in the feature
file's own manifest rather than treated as a silent skip.

Per BL-638's fallback, verified the fix's actual protection by hand:
removed `copyClose: true` from `COPY_CLOSE_STATES` (exactly the shape
of regression the reach-floor exists to catch — a generator/cell list
that silently stops covering a value), confirmed
`assertReachFloor` genuinely fires ("reach floor: post-close-case
copyClose drawn 0 < 2"), restored the file (confirmed clean via `git
status`), reran green.

Confirmed invariant 2 (the git-subprocess-count property) is
byte-for-byte untouched by the diff, matching the ticket's own
constraint. `PER_CELL_RUNS = runsPerCell(8, 8) = 1`: each of the 8
(refileCount × copyClose) cells runs exactly once via
`fc.constant(...)`, genuinely deterministic — the "constructed, not
sampled" property holds by inspection, not just by the passing test.

No TS/JS production file is in scope — only the property test itself
(explicitly out of CRAP/DRY/mutation scope, kept separate from those
tools per engineering.prompt) and its new step handler.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1760-….feature`: 2/2.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=soft: exit 2,
  `outcome: "inapplicable"` (no Scenario Outline) — expected, recorded.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1074PostCloseRefileDuration.property.test.js`: 3/3 (targeted;
  full properties lane not run, this file was the only property test
  touched by this pass, per the 2026-09-17 rule — though `npm test`'s
  own guard already re-confirmed it green as part of the full suite).
- `npm test` (extension/): 638 files / 10875 tests, all green.
- `git status --short` clean before each commit; no process/fixture
  leaks (this ticket's own test creates only throwaway git repos via
  the existing `backlogCorpusFixture` helper, already cleaned up by
  that helper's own convention).
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load 12-27/20 cores during this pass (briefly close to
  the 2x-cores ceiling under shared-host contention, never over it).

By hardender.
