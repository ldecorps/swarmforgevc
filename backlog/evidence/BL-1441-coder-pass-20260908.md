# Evidence — BL-1441 — coder pass (2026-09-08)

Role: coder. Ticket: BL-1441-the-four-remaining-0819-hardening-gates-are-run.
Routed by coordinator note (`Work BL-1441-the-four-remaining-0819-hardening-gates-are-run`),
today being `not_before: 2026-09-08`.

## Eligibility re-checked per row, fresh, today (the ticket's own "How")

`mutation_cooldown_gate.bb` re-run against every file in the three still-
outstanding rows (BL-954's row was already discharged by an earlier pass
on this same ticket - `backlog/evidence/BL-1441-coder-pass-20260906.md` -
confirmed live: `discharged_at: "2026-09-06"`,
`discharged_evidence: backlog/evidence/BL-1441-BL-954-a-bounce-verifies-its-own-revert-mutation.md`;
untouched here):

| row | files | decision today |
|---|---|---|
| BL-620 | telegram-front-desk-bot.ts, telegramFrontDeskBotCore.ts, telegramTopicDecisions.ts | **skip-cooldown** (0.13 / 2.22 / 20704 days) |
| BL-955 | negotiationTelegramRelay.ts, negotiationTelegramRouting.ts, telegramFrontDeskBotCore.ts | **skip-cooldown** (19.40 / 19.40 / 2.22 days, shares telegramFrontDeskBotCore.ts) |
| BL-956-pipeline-board-caption-and-cap-hotfix | pipelineBoard.ts | **skip-cooldown** (0.13 days) |

Host load quiet (13.68/11.85/10.53 on 20 cores, well under the 2x-cores
busy threshold).

**Not the same blocker as the 2026-09-06 attempt, and not stale to
re-check.** BL-1425's original 2026-09-05 touch to
`telegramFrontDeskBotCore.ts`/`pipelineBoard.ts` would itself have
cleared the 3-day cooldown by today - but BOTH file sets were touched
again since, by unrelated, later work:

- `telegram-front-desk-bot.ts` and `telegramFrontDeskBotCore.ts`:
  re-touched by **this same coder's own BL-1475** parcel
  (`2b402e0b7e`, 2026-09-08 01:55, ~25 minutes before this check) -
  landed earlier in this session, unrelated to BL-1441's own scope.
- `pipelineBoard.ts`: re-touched by **BL-1451** (`a101045f63`,
  2026-09-07 22:47), a different ticket entirely.

So BL-620 and BL-955 (which share `telegramFrontDeskBotCore.ts`) and
BL-956 are all still genuinely blocked today, each for a NEW reason
supervening the original one - the cooldown gate is working exactly as
designed both times: any sufficiently fresh touch to a shared file
resets its own mutation surface's stability clock, regardless of which
ticket made it. Recorded as fresh attempts (not left at the stale
2026-09-06 blocker text, which named a cause that would itself have
expired by now): `hardening_debt_ledger_update.bb --attempt <parcel>
mutation "<today's real reason>"` for all three rows.
`attempted_at` moved from `2026-09-06` to `2026-09-08` on each; nothing
discharged (invariant 3: an attempt is never a discharge).

## What did not change

BL-954's row: untouched, still discharged. No new mutation run attempted
for any of the three still-blocked rows (attempting one that the
cooldown gate itself reports as ineligible would be exactly the "force
it and hope" the gate exists to prevent).

## Ticket completion status: NOT complete, correctly, again

Acceptance re-run live against the current repo state:

- Scenario 01: **fails**, naming the same three genuinely-outstanding
  rows as before (BL-620/mutation, BL-955/mutation,
  BL-956-pipeline-board-caption-and-cap-hotfix/mutation) - unchanged,
  correctly.
- Scenario 02: **BL-954's Example still passes**; the other three
  **still fail**, each error now naming TODAY's live `attempted_blocker`
  text (confirms the ledger update actually took and the acceptance
  handler reads the live file, not a cache).

This is the same shape as the 2026-09-06 pass and as BL-1439's own
partial acceptance before it: forcing this feature green today would
mean discharging gates that did not actually run. Neither was done.
**This parcel is committed but NOT forwarded** - three of four rows
remain genuinely outstanding, now for reasons that supervened the
originally-anticipated 2026-09-08 clearance. The earliest any of the
three could plausibly clear is whenever `telegramFrontDeskBotCore.ts` /
`telegram-front-desk-bot.ts` / `pipelineBoard.ts` next go 3 full days
without a touch from ANY ticket - not schedulable from here.

## Invariants (BL-654) — both admit no new encoding in this parcel

Same reasoning as the 2026-09-06 pass, restated because it still holds
and this parcel touches no ledger code:

1. *"A ledger row leaves the outstanding debt only through a discharge
   that names the gate, the parcel and a committed result..."* -
   ledger MECHANISM contract, already built and property-tested
   (BL-1439/BL-942); this parcel is a CONSUMER (`--attempt` via the CLI)
   only. `bl942_hardening_debt_ledger_property_runner.bb`: **ok**,
   unmodified.
2. *"The register row of a gate leaves in the same commit that
   discharges it..."* - not triggered this pass: nothing discharged,
   so no register row leaves. `backlog/standing-reds.tsv` untouched
   (still correctly names BL-1441 for all three hardening rows).

## Verification

| check | result |
|---|---|
| `mutation_cooldown_gate.bb` × 3 remaining rows (all files) | matches table above |
| `bb hardening_debt_ledger_read.bb .` (live) | BL-954 still `discharged_at`/`discharged_evidence`; BL-620/BL-955/BL-956-pipeline-board's `attempted_at` now `2026-09-08` with today's blocker text, `discharged_at: null` |
| `hardening_debt_ledger_lib_test_runner.bb` | ok, unaffected |
| `bl942_hardening_debt_ledger_property_runner.bb` | ok, unaffected |
| `test_hardening_debt_ledger_cli.sh` | ALL CHECKS PASSED, unaffected |
| `bb standing_red_register_cli.bb .` (live) | hardening lane: 3 rows (BL-620, BL-955, BL-956), all `owned: true`; `unowned: []` |
| Acceptance `BL-1441-....feature` | 1/5 (scenario 02's BL-954 example) - the rest correctly fail, naming today's live `attempted_blocker` text |

## Scope

Nothing in `out_of_scope` touched: the cooldown gate, its window, and
BL-1440's fix are all untouched (only relied upon). No attempt to force
BL-620/955/956 to run early, and no ledger code changed - only the
`--attempt` verb's own data was invoked, exactly as its contract allows.
`extension/docs/briefings/2026-09-04.json` remains untracked and was not
created by this parcel.
