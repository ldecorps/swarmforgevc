# BL-428 — QA spec-gap (acceptance feature has no step handlers) — 20260827

## Inbound

Documenter handoff `a03f7bca28` ("SG1 spec-gap resolved, forward to QA").
Merged into QA at `f58507d39`. Coder `742f2d290`, cleaner `279bf406e`,
architect `a2759319f`, hardener `28a973dfa` all confirmed ancestors of the
cited commit.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling | `qa-sibling-check.js status --ticket BL-428` | VERIFY (no open deferral) |
| Ancestry | coder/cleaner/architect/hardener merges ancestors of `a03f7bca28` | CONFIRMED |
| Compile | `npm run compile` (extension/) | PASS |
| Mapped unit (BL-1164) | `vitest run test/paneHistory.test.js test/footerDetectionParity.test.js test/footerAwareScroll.test.js` | **43/43 PASS** |
| CRAP (scoped) | `crapReport.js src/panel/paneHistory.ts` after scoped coverage run | all functions **≤6** (`detectFooterLineCount`=3.00) |
| Full unit | `npm test` | 202/9091 failed, 46 files — **BLOCKED BY host env**: CURSOR_API_KEY unset across bridge suites (BL-720/BL-728 family) + 24 files pre-existing `node:test`/vitest migration mismatch ("No test suite found"), all outside `paneHistory.ts`'s call graph. Not caused by this parcel. |
| Full property | `npm run test:properties` | 20/670 failed, 28 files — all on `property_suite_standing_allowlist.tsv` (BL-1175) **except one**: `test/tempDirTrapGuard.property.test.js` (violations in `local_coder_battery.sh`, `bl1121_reconcile_import_property_runner.bb`, `bl1122_mid_commit_mute_property_runner.bb`, `bl1134_post_add_mute_property_runner.bb`, `bl1137_cwd_scoped_mute_property_runner.bb` — none touched by BL-428). Grepped `backlog/` for these filenames and for `tempDirTrapGuard`: no active/paused/hold ticket found — genuinely untracked (BL-1063 posture), reported separately below. |
| **Acceptance (BL-112 final gate)** | `run_acceptance.sh specs/features/BL-428-decrap-paneHistory-slice.feature` | **5/5 FAIL** — every scenario: `no step handler matched "Given the paneHistory decrap slice targets extension/src/panel/paneHistory.ts"`. Grepped `specs/pipeline/steps/` for `paneHistory`, `BL-428`, `detectFooterLineCount`: zero hits — no handler was ever written for this feature. |
| Wiring | pure internal refactor, same exports/call sites as before (architect-confirmed) | N/A — no new caller to wire |

## Disposition

The specifier's SG1 fix (`14cfec1c1`, materializing
`specs/features/BL-428-decrap-paneHistory-slice.feature`) cleared the
*block-scalar-unreadable* problem but the resulting feature carries no
step-handler implementation at all — every scenario is unexecutable. Per
Article 4.4's spec-gap table ("acceptance scenario not testable ... no step
handler"), this is a spec-gap, not an implementation defect: coder/cleaner/
architect/hardener's work (unit tests, CRAP, mutation) all independently
verify the same intent and are sound. This is not bounced to any pipeline
role.

The ticket's own `qa_e2e:` field already describes an executable-by-hand
procedure (regenerate CRAP list, confirm targeted functions ≤6, confirm
existing behavior tests pass) that this pass satisfied. The specifier should
decide: wire step handlers for the 5 scenarios against that same procedure,
or retire the Gherkin scenarios as unexecutable-by-design (parked as
`.feature.draft`, `qa_e2e` remains the operative contract) — precedent:
`verification-only-chore-carries-no-executable-gate`, though BL-428 differs
in that it has real code, not a verification-only chore.

## Secondary finding (untracked, unrelated to BL-428)

`test/tempDirTrapGuard.property.test.js` is red on `main`-derived state with
no active/paused/hold ticket and not on `property_suite_standing_allowlist.tsv`.
Surfacing to specifier for disposition (ticket or allowlist entry) —
not BL-428's to fix.

## Inventory (Article 4.4)

Complete. NONE beyond the spec-gap and the secondary untracked finding above.

## Forward

Not forwarded — spec-gap only, per Article 4.4 ("when EVERY item is a spec
gap ... send the note, complete the inbound task, and do not forward").
`note` priority `00` to specifier + coordinator.

By QA.
