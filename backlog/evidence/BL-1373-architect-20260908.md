# BL-1373 — architect review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

**Parcel lineage**: This is the second architect pass. The first pass (5328cdb9e6) bounced for missing property tests (invariant-unencoded). The coder added property tests (cc7fcf8fda), cleaner passed (a9ba8b2573).

**Gates run**:
1. **Dependency gate** (REQUIRED HARD GATE BL-259): `node extension/out/tools/dependency-gate.js extension/test/bl1373PathSetCacheInvariants.property.test.js` — PASSED, no forbidden edges.
2. **Co-change coupling**: `node extension/out/tools/co-change-report.js extension/test/bl1373PathSetCacheInvariants.property.test.js` — no co-changers found.
3. **Property tests**: `npm run test:properties` — 387 test files, 1144 tests, all passed. The new BL-1373 property tests (2 tests, 20s total) encode both declared invariants and pass.
   - Invariant 1: "The classified path set follows the single source at runtime" — 15.9s, PASSED
   - Invariant 2: "A path not in the reported set produces no finding" — 4.5s, PASSED
   - The two `[vitest-worker]: Timeout calling "onTaskUpdate"` errors are the known-benign BL-871 allowlist.

**Invariants review**:
- Both declared invariants now have executable property tests encoding them.
- Tests are non-vacuous: invariant 1 would fail if the cache key didn't include qa-paths (both sweeps would return the same result); invariant 2 would fail if the sweep classified everything (commits touching unreported paths would be found).
- Tests drive the real `babysitter_check.bb` via `BABYSITTER_QA_EXCLUSIVE_PATHS_SCRIPT` seam, not a mock.

**Architectural compliance**:
- The cache key fix (d7fdd19d6d) is in the right place (`babysitter_check.bb:665-689`), minimal, backward-compatible (old caches safely miss), and respects the two-layer boundary.
- No webview, VS Code API, or process-spawn violations.
- No secrets in target working directory.
- Integrate-not-fork: SwarmForge is driven via its scripts, not modified.

**Correctness read**: The fix is correct. The root cause diagnosis (cache keyed only on tips, not qa-paths) is accurate. The property tests verify the fix holds for arbitrary path sets, not just the acceptance scenario examples.

By architect.
