# BL-1240 — architect pass, 2026-08-31

Task: `BL-1240-unregistered-test-fails-the-ticket-that-adds-it`
Received: cleaner's `c7bfa6f186` (verdict NONE, forwarding coder's
`31ffc314a0` unmodified — a re-land of `8bf2963bf2` after two earlier
bounces, restoring 21 lines a BL-490 revert had silently dropped).

## Merge note

`git merge --no-ff c7bfa6f186` initially refused
(`check_merge_deletion.sh`) on two `backlog/hold/*.yaml` -> `active/*.yaml`
promotions (BL-1240, BL-1253) whose HEAD-side introducing commit was a
generic "Merge main into architect…" subject naming no ticket id — the
known unattributed-path trap where no commit message can satisfy the
guard. Fixed per the documented remedy: cherry-picked the actual deleting
commits (`b17bde89f3` BL-1240, `797aadac0f` BL-1253) first, re-ran the
merge, resolved the resulting rename/delete conflicts to the incoming
`active/` content (verified byte-identical via diff against `c7bfa6f186`).
Both are re-promotions, not retirements.

## Verdict: CLEAN — forward to hardener

### What changed
- `swarmforge/scripts/swarm_handoff.bb`: restores the load-file and the
  21-line gate block (candidate result, warning print, block check, refusal
  arm) wiring `unregistered-test-gate-lib/findings-for-git-handoff` into
  the send path — same shape and fail-open posture as every sibling gate
  already in this file (`task-scope-block`, `tree-collapse-block`).
  Verbatim restoration from `9a77c98bac`'s own reverse diff, not new design.
- `specs/pipeline/steps/lib/bl1240UnregisteredTestGateCli.sh`: the fixture
  driver now answers the self-audit challenge (retries once on
  `AUDIT_REQUIRED`) instead of asserting against the pre-audit response —
  a fixture-only fix, matches how a real agent behaves per Article 2.3.

### Architecture rules
No extension/webview/TS code touched — confined to a `.bb` production file
and a `.sh` fixture driver. Two-layer boundary, extension-host-owns-I/O,
no-webview-storage, integrate-not-fork: all N/A.

### Hard gate — dependency-rule checker (BL-259)
No file under `extension/` in this diff; the cruiser has nothing in scope.
N/A, not skipped.

### Co-change tool (BL-255)
`co-change-report.js` against `swarm_handoff.bb` (the hub file): every
result shows exactly 1 co-change — below the frequency-3 threshold. No
coupling flagged.

### required_wiring (both entries verified live, not cited)
- `unregistered_test_gate_lib.bb::suite-inventory-lib/manifest-name` —
  confirmed: `unregistered_test_gate_lib.bb` reaches
  `suite-inventory-lib/manifest-name` directly (lines 105/107/114/135).
- `specs/pipeline/steps/index.js::bl1240UnregisteredTestFailsAuthorSteps` —
  confirmed registered at `index.js:897`.

### Checks re-run (not taken on trust)
| Command | Result |
|---|---|
| `grep -n unregistered swarm_handoff.bb` | 10 hits — call site present |
| `run_acceptance.sh specs/features/BL-1240-….feature` | 4/4 pass |
| `bb .../unregistered_test_gate_lib_test_runner.bb` | ALL PASS |
| `bb .../bl1240_unregistered_test_gate_property_runner.bb` | ALL PASS, 400 runs/invariant, all 7 shapes covered |
| `bash .../test_swarm_handoff_sync_deliver.sh` | ALL PASS |

### Invariants review (BL-633/654)
Both declared invariants already carry coder-authored property tests
(re-run above, 400 runs each including `row-absent-file` /
`row-not-a-test-name` coverage) — no new obligation, nothing weakened by
this restore.

### Correctness read
The regression this parcel fixes (invariant true on paper, false in the
live send path) was caused by a revert dropping hunks a deliberately-absent
`required_wiring` entry couldn't catch — coder's own root-cause trace
checks out against `9a77c98bac`'s reverse diff. No further defect spotted.
Untracked `swarmforge/scripts/wait_pipeline_drain.sh` predates this
session and this parcel — correctly left unswept, not mine to touch.

### For the documenter
Coder flagged (not fixed, correctly — Article 4.3) that the same revert
also dropped BL-1240's documentation; `handoff-protocol.md` and
`docs/reference/Specification.MD` still carry BL-1240 sections but should
be checked against the restored wiring rather than assumed current.
Carrying this note forward unchanged.

No spec gaps, no send-back. Forwarding to hardener with the same task
name, commit `adef3bca92` (the merge I received).

By architect.
