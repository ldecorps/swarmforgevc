# BL-1571 coder evidence — 2026-09-14

## What changed

- `swarmforge/scripts/operator_runtime.bb`: reapplied 43da3844ec's two-line
  hunk exactly — `(def recover-miniapp-bridge-script (fs/path script-dir
  "recover_miniapp_bridge.sh"))` beside the existing
  `bounce-bridge-headless-script` def, and `miniapp-bounce-bridge!` now
  invokes `recover-miniapp-bridge-script` instead of
  `bounce-bridge-headless-script`. `bounce-bridge-headless-script` itself
  stays defined (still used elsewhere — `recover_miniapp_bridge.sh` falls
  back to `bounce_bridge_headless.sh` internally when the front desk isn't
  live). No other line in operator_runtime.bb touched.
- `specs/pipeline/steps/bl1571MiniappWatchdogRecoverSteps.js` (new): step
  handlers for the specifier-authored feature
  (`specs/features/BL-1571-*.feature`, not touched by this parcel).
  Scenario 01 greps the `miniapp-bounce-bridge!` def body (not the whole
  file, so an unrelated fifth mention of either script name elsewhere
  wouldn't false-pass/fail it) for the routing. Scenario 02 runs the real
  standing shell test as a subprocess. Scenario 03 diffs each of the four
  named bridge scripts' current content against `git show main:<path>`.
  Discovered automatically by `specs/pipeline/steps/index.js` (BL-1371), no
  manual registration needed.

`recover_miniapp_bridge.sh`, `rearm_front_desk_bridge.sh`,
`bounce_bridge_headless.sh`, `stop_bridge_headless.sh`, BL-1159's own
feature file/handler, and BL-653's escalation sweep are all unchanged
(confirmed by scenario 03 and by `git diff main...HEAD --name-only`).

## Invariant (BL-654)

Declared invariant: "When front-desk-supervisor.pid names a live process, a
miniapp watchdog recovery never invokes bounce_bridge_headless.sh or
stop_bridge_headless.sh - it re-arms the front desk."

No coder-authored `*.property.test.js` for this one — stated reason: the
invariant quantifies over `recover_miniapp_bridge.sh`'s own re-arm-vs-bounce
decision logic, a bash script this ticket is explicitly barred from
touching (Not in scope / the ticket's FIRM constraint) and does not modify.
`extension/src` is untouched by this parcel, so there is no pure TS module
to attach a generative property test to, and no mutation/CRAP/DRY/property
tooling is wired for Babashka or shell (`mutation_cost: low` note, BL-472
deferred). The invariant IS covered by an executable check today: the
standing shell test's own fixture (case `bl-1159-03`) sets a live
`front-desk-supervisor.pid`, downs the bridge, and asserts the bounce-kill
stub marker never appears — exactly this invariant, run three times in
verification below and asserted green in scenario 02 of the acceptance
feature.

## Verification

- Standing shell test alone, 3x:
  `bash swarmforge/scripts/test/test_bl1159_bridge_child_survives_without_crash_giveup_loop.sh`
  → `ALL CHECKS PASSED`, exit 0, all three runs.
- Break-then-restore: pointed `miniapp-bounce-bridge!` back at
  `bounce-bridge-headless-script`; case `bl-1159-03` failed
  (`FAIL - bl-1159-03: down bridge uses recover (rearm) not bounce kill
  stub`); restored, re-ran, green.
- Full acceptance run (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1571-the-miniapp-watchdog-recovers-through-recover-miniapp-bridge.feature`):
  6/6 scenarios pass (01, 02, 03's all four Examples rows).
- `swarmforge/scripts/check_feature_handler_registration.sh`: clean, exit 0.
- `git diff main...HEAD --name-only`: `swarmforge/scripts/operator_runtime.bb`,
  the new step handler, and this evidence file only — the four bridge
  scripts, BL-1159's feature/handler, and the register row are untouched by
  this commit (the register row removal is QA's, per the ticket's notes).

By coder.
