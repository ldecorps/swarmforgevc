# BL-1341 — cleaner pass (2026-09-02)

Two trivial fixes; otherwise NONE. Forwarded to architect.

## Reviewed

Diff scope (c90be7587e..963dec3774, the coder's actual BL-1341 work):

- `swarmforge/scripts/check_merge_deletion.sh` — `collect_deletions` extracted
  as a helper called twice (HEAD, MERGE_HEAD) into one `deleted_paths` list
  with a `side_of` associative array for dedup/attribution. Clean, well
  commented at the non-obvious points (why one script and not a sibling; why
  attribution must fall back to `MERGE_HEAD`). Left as-is.
- `specs/pipeline/steps/lib/bl1242MergeBranchWorkDeletionCli.sh` — new
  `incoming-matrix`/`incoming-detail`/`incoming-kept`/`incoming-both-sides`
  modes, each building its own git fixture via the driver, matching this
  file's existing shape (same `bb -e cheshire` stderr-JSON-escape pattern
  already used by the other modes, not a new pattern). One stray blank line
  left before the closing `esac` from the diff insertion — removed.
- `specs/pipeline/steps/bl1242MergeBranchWorkDeletionSteps.js` — new BL-1341
  Then/Given/When steps, clearly sectioned. One dead-state assignment,
  `ctx.bl1341 = true` in the incoming-fixture Given step, set but never read
  anywhere in the file (the real fixture-building happens inside the CLI
  driver for this scenario family, not in JS state) — replaced with a comment
  explaining why the Given has nothing to record, no functional change.
- `extension/test/bl1341MergeDropsEitherSideInvariants.property.test.js` —
  declared invariant, side drawn explicitly per property run (not by
  independent per-element chance), so all three reach counters are
  guaranteed by construction rather than probabilistic — avoids the flakiness
  class BL-1343's first property test hit. Ran 3x, no flakes.

## Verification run (not assumed)

- `bash swarmforge/scripts/test/test_merge_deletion_guard.sh` → ALL PASS, 13/13.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1242-merge-never-silently-drops-branch-work.feature` → 12/12 pass (reran twice, after each fix, still 12/12).
- `npx vitest run --config vitest.properties.config.mjs bl1341MergeDropsEitherSideInvariants`, 3 consecutive runs → 3/3 clean.
- `bash -n specs/pipeline/steps/lib/bl1242MergeBranchWorkDeletionCli.sh` → syntax OK after the fix.

## Confirmed, not re-litigated

- The mutation-manifest stamp on `specs/features/BL-1242-...feature` is left
  stale by the coder deliberately (out of coder-role scope, flagged for the
  hardener) — same disposition already confirmed correct on BL-1340; hand-
  editing a mutation manifest is forbidden by the Guardrails article.
