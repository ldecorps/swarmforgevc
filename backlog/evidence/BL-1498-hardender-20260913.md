# BL-1498 — hardender review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No production code touched (`handoffd_supervisor.bb`, `swarm-cleanup.sh`,
`swarmforge.sh` all empty diffs vs main — confirmed independently below);
mutation_cost: low, shell has no mutation tool wired. Hardening pass was a
re-verification of the coder/cleaner/architect work plus the QA e2e
procedure's own checks, run before forwarding so QA finds nothing new.

Gates run:
- Orphan check before starting: `pgrep -fl 'node --test|stryker|vitest'` —
  two stale bash pids, already exited by the time of `ps`; no live orphan.
  `uptime`: load 1.12/1.40/1.62 on 20 cores — quiet, no cooldown/office-hours
  deferral needed (no production file changed anyway, so BL-149's gate is
  N/A).
- `bash swarmforge/scripts/test/test_handoffd_supervisor.sh` (real
  `~/.zshenv`, `RESEND_API_KEY` unset): `ALL PASS`, exit 0, 18.3s.
- Hostile-ZDOTDIR probe (QA e2e step 2, run directly rather than trusting
  the recorded procedure): `mktemp -d` root with its own `.zshenv`
  prepending a fake `tmux` that `exit 0`s; ran the file with that as the
  INHERITED `ZDOTDIR` — the fixture's own `ZDOTDIR="$(mktemp -d)"` export
  (line ~55) wins over it since it runs before any zsh invocation reads the
  variable: `ALL PASS`, exit 0, 18.2s. Confirms invariant 1 against a
  concrete hostile input, not just the operator's own (relatively benign)
  `~/.zshenv`.
- `bb swarmforge/scripts/test/handoffd_supervisor_startup_grace_test_runner.bb`:
  `ALL TESTS PASS` (7 assertions) — BL-1342's grace contract untouched.
- `git diff --stat main -- swarmforge/scripts/handoffd_supervisor.bb
  swarmforge/scripts/swarm-cleanup.sh swarmforge/swarmforge.sh`: empty.
- `grep -c ZDOTDIR test_handoffd_supervisor.sh` = 10 (>=1);
  `grep -n 'touch -t 202601010000 "\$DAEMON_DIR/handoffd.pid"'` = 3 lines
  (198/302/330-region) — case 02 and both BL-081 scenarios (01, 03) all
  age the pid file per invariant 2.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1498-….feature`: `# tests 4 / # pass 4 / # fail 0`,
  `duration_ms 46671`, no hang — confirms cleaner's `afterEach` reap fix for
  scenario 02's deliberately-live placeholder pid holds up on a fresh run.
- Standing whole-tree guards (parcel adds a file under
  `specs/pipeline/steps/`): ran the FULL current set of 18 files under
  `extension/test/*Guard*.test.js` (excluding `.property.` siblings), not
  just the two an old boot-prefix note names — `bl1277UnscopedStepCollisionGuard`,
  `bl968StepRegistryMaterializedTreeGuard`, `tmuxReaperGuard`,
  `tmpDirMigrationGuard`, `retiredEnsureEnvVarGuard`, `liveRepoDerivationGuard`,
  `portableTimeGuard`, and eleven others — all green, 183/183 tests, 17.2s.
  Relevant to this parcel's new step file: `tmuxReaperGuard` clean (no tmux
  server started by the new handler) and `tmpDirMigrationGuard` clean (uses
  `mkTmpDir`, not raw `mkdtempSync`).
- Post-run process check: no orphaned `sleep 300`/`handoffd`/tmux-socket
  process traceable to this pass's own runs; one unrelated `sleep 300`
  (pid 2611, ppid 25544, live-swarm activity, not spawned by any command
  here) aged out naturally to completion during the check — left alone
  per "never delete/kill what you did not create."
- `git status --short`: clean throughout; no stray artifacts committed.

No further hardening found or needed — cleaner's `afterEach` fix
(6d54f1708a) and the coder/architect's ZDOTDIR + pid-aging fixture changes
already close both invariants; this pass adds a hostile-input probe beyond
the operator's own environment as additional confirmation, finds nothing
new, and forwards unchanged.
