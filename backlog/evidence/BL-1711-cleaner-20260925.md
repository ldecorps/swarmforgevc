# BL-1711 — cleaner review pass, 2026-09-25

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: zsh -c "set -euo pipefail; source swarmforge/scripts/ollama_ancillary_lib.sh"
- **Commit hash**: 37d1f86317
- **First error excerpt**: before fix: swarmforge/scripts/ollama_ancillary_lib.sh:26: BASH_SOURCE[0]: parameter not set on every swarm launch (swarmforge.sh sources this file under zsh unconditionally); OLLAMA_ANCILLARY_LIB_DIR also resolved to the wrong directory there
- **Failure class**: correctness
- **Expected vs observed**: the file's own documented constraint: every function uses only constructs identical under POSIX sh, bash 3.2, and zsh - no arrays
- **Blamed role**: coder - the new top-level OLLAMA_ANCILLARY_LIB_DIR init used BASH_SOURCE (a bash array) unconditionally at source time, reached by swarmforge.sh's zsh sourcing on every launch
- **Remediation pointer**: fixed: moved the computation into ollama_ancillary_restart_if_crashed as a local, the only reader, reached exclusively through the bash-only ollama_ancillary_restart_cli.sh - confirmed clean under a real zsh -c source and unchanged (7/7) under the acceptance feature scenario 5 which exercises this exact call path

## D2

- **Failing command**: git diff --stat 4461e7d1cf -- . (full tree, post-merge)
- **Commit hash**: ed78531166
- **First error excerpt**: my own first merge resolution (later reset) silently dropped BL-1698 content beyond the 5 files the merge-drop guard flagged: local_parcel_driver_lib.bb (release-hold!/resume-writable-sweep!/driver-test-scope gone entirely), handoffd.bb (the resume-writable-sweep! startup call), babysitter_nudge_lib.bb (the D2 driver-seat? architect-bounce fix), docs/index.md, and the BL-1698 feature's own mutation-stamp manifest - none flagged by the guard since these files existed on both merge sides
- **Failure class**: correctness
- **Expected vs observed**: a merge keeps every uncontested hunk from both sides (BL-1576); this branch's own BL-1698 in-flight work must survive a QA merge-up for an unrelated ticket (BL-1699)
- **Blamed role**: cleaner (me) - my earlier acknowledgment merge (1f41308d17) favored QA's tip-pure BL-1699 land wholesale for every shared file instead of keeping this branch's superset
- **Remediation pointer**: reset the bad merge and redid it, checking out coder's (4461e7d1cf) current content file-by-file for every path with any diff, verified byte-identical after - full BL-1698 (9/9), BL-1697 (12/12), BL-1711 (7/7) acceptance suites and all touched property/unit tests pass

By cleaner.
