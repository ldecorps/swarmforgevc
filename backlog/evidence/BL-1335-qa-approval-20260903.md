# BL-1335 — QA approval (20260903)

Received: documenter commit `574b202ca0` (Specification.MD entry, BL-669
how-to section), forwarding hardener (recovery note re: a mis-archived
parcel, re-verified in the same turn), architect, cleaner, coder's
exhaustion-promotion feature built to the human's three-way autonomy
ruling. Merged into QA.

Note: the merge diff also carried `BL-1352-cleaner-bounce-20260903.md` and
a one-line restoration to `specs/pipeline/steps/bl1332SharedPathLineLeakSteps.js`
— both belong to BL-1352 (a separate, still-in-flight ticket sharing the
cleaner worktree), riding along in shared branch history. Confirmed
harmless: the `bl1332...` file is now byte-identical to `origin/main`
again (`git diff origin/main swarmforge-QA` on that path is empty). Not
this ticket's scope, not acted on.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/bl1335_exhaustion_promotion_test_runner.bb`:
  ALL PASS.
- Acceptance (`specs/features/BL-1335-token-exhaustion-opens-an-outage-record.feature`):
  6/6, including scenario 04 (runs the real `outage_failover_cli.bb
  evaluate` against a record the real promoter wrote).
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1335ExhaustionOpensFailoverRecordSteps` (line 26) — confirmed.
- `required_wiring`: `promote-exhaustion-to-failover!` confirmed called
  from `observe-pane-provider-outage!` in `handoffd.bb` (the live per-tick
  pane observation path, wrapped in try/catch) — not merely defined.
- Declared invariants
  (`extension/test/bl1335ExhaustionPromotionInvariants.property.test.js`):
  3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated the `:suspected` classification branch guard in
  `exhaustion_failover_promotion_lib.bb` (comparison target changed so it
  never matches) — 6 of the bb suite's assertions failed, reproducing the
  ambiguous-text-promotes-instead-of-announcing defect the ruling exists
  to prevent. Restored (`git diff --stat` empty), re-ran ALL PASS.
- Standing whole-tree guards re-run
  (`tempDirTrapGuard`/`socketFixtureShortRootGuard`/`liveRepoDerivationGuard`):
  3 failures, matching the same pre-existing/already-ticketed set as
  BL-1350's pass minutes earlier (BL-1289/1290/1291).
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population. None touch this parcel's files.
- Property suite (`npm run test:properties`): 26 files / 16 tests red.
  14 of the 16 test-level failures match the exact standing-debt
  population from every prior pass this session. **Two new**, both
  investigated:
  - `test/bl787NamedTunnelInvariants.property.test.js` invariant 3 — ran
    in isolation, passed (1/1). Load flake under full concurrent suite,
    same pattern already documented 4+ times this session for other
    files.
  - `test/bl1323StampOffInvariants.property.test.js` both invariants —
    ran in isolation, still failed. Root cause found: the property
    asserts `backlog/hotfix-ledger.yaml`'s row for hotfix `9c94735f03`
    stays `state: stamp-open`, but that row legitimately advanced to
    `state: awaiting-human` as BL-1323 (my own earlier land this session)
    moved through the pipeline — the property test asserts a frozen
    snapshot of external state that has since moved. **Already ticketed**
    as `BL-1356-stamp-off-invariant-watches-the-run-not-the-row.yaml`
    (confirmed by grep before reporting) — pre-existing debt surfaced by
    time passing, not by this parcel's diff, and not re-reported here.
  - BL-1335's own property test — 3/3 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry and the "Automated promotion (BL-1335)" section added to
  `docs/how-to/BL-669-outage-driven-seat-failover-via-steward.md`
  confirmed present and accurate. No diagram change-trigger fired.
- Orphaned test processes: none after the property-suite run.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
