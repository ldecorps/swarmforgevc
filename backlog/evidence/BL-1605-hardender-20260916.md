# BL-1605 — hardender review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Mutation (BL-638 fallback, no Stryker/CRAP/DRY wired for Babashka):
  hand-authored sweep on `reverse_hop_lib.bb`'s `remove-forward-
  recipients`. Swapping the `(remove (set forward-recipients)
  candidates)` argument order to `(remove (set candidates) forward-
  recipients)`: REAL bug, confirmed via `bb -e` and killed by the
  existing unit runner (`reverse_hop_lib_remove_forward_recipients_runner.bb`,
  3 of 5 assertions fail under the mutant) - no new test needed.
- BL-113 Gherkin mutation (soft) on scenario 01's Scenario Outline: 12
  mutants, 11 killed. 1 survived (m11, row 4's `reverse-roles` "nobody"
  -> "nobodY") - equivalent by design: the step handler's
  `parseReverseRoles` (`specs/pipeline/steps/bl1605NoReverseCopyToForwardRecipientSteps.js`)
  does `/^nobody$/i.test(text.trim())`, a case-insensitive match, so the
  mutated spelling still resolves to the same empty reverse-roles set.
  Manifest wrote `scenarios: []` per BL-502; the run's own per-mutant
  status lines (11 killed, 1 survived, outcome fail because of the one
  unexplained survivor at generation time) are the source of truth per
  BL-460, and the equivalence is recorded here rather than chased with
  an artificial case-sensitive assertion.
- Acceptance pre-check (`run_acceptance.sh`): BL-1605 5/5 green - all 4
  Outline rows (architect->cleaner, architect->hardender,
  cleaner->architect, cleaner->coder) plus the send-gate refusal
  scenario for a merge-only inbound of another task.
- `bb swarmforge/scripts/test/bl1299_reverse_hop_property_runner.bb`:
  ALL PASS (500 runs) - no regression on the pre-existing reverse-hop
  invariants from wiring `remove-forward-recipients` into `reverse-roles`.
- `bb swarmforge/scripts/test/reverse_hop_lib_remove_forward_recipients_runner.bb`:
  green, 5 cases (back-one full overlap, forward-to-later-role no-op,
  back-all middle-role subtraction, multi-recipient subtraction, no
  overlap passthrough).
- The call-site wiring itself (`swarm_handoff.bb`'s `reverse-roles` now
  taking `forward-recipients` and threading it through
  `remove-forward-recipients`) is exercised end-to-end by the acceptance
  suite: the defect this ticket fixes was exactly this wiring being
  absent, so scenario 01 rows 1-3 (which were the reported failure mode)
  going green is direct behavioral evidence of the wiring - no separate
  hand-mutant needed at that call site.
- CRAP: `reverse_hop_lib.bb` and `swarm_handoff.bb` are Babashka, no
  CRAP/DRY tooling wired (engineering.prompt). The JS step handler is
  outside the TS `extension/src` CRAP scope.
- DRY (jscpd) on the touched JS step handler and bb files: 0 clones.
- No orphaned test/mutation processes left behind
  (`pgrep -afl 'node --test|stryker|gherkin-mutator|mutationWorker'`
  clean). The step handler's `fs.mkdtempSync(os.tmpdir(), 'bl1605-')`
  fixture roots (real `git init` repos, one per scenario per run) leak
  with no cleanup - same prevailing pattern as BL-1608's fixture leak,
  not unique to this ticket. ~90 leaked dirs from repeated acceptance
  and BL-113 mutation runs (`/tmp/bl1605-*`) removed by hand; not fixed
  in the step handler itself as out of this ticket's narrow scope
  (`reverse_hop_lib.bb`, its runner, the `swarm_handoff.bb` call site,
  the feature, the handler, evidence and docs only).
