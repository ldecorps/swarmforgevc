# BL-1232 QA pass — 2026-08-30

## Entangled tip, rebuilt tip-pure (BL-1241) — second occurrence today

The parcel handed to QA (documenter commit `e373a940fa`) again carries
unlanded BL-1182 work as an ancestor (same coder branch, same still-`todo`,
`assigned_to: coder` ticket as BL-1284 earlier today — BL-1182 was bounced
by the architect and has not landed). Not a landable sibling, so per BL-1241
this is rebuilt tip-pure rather than approved or bounced as received.

- Isolated each stage's own atomic contribution by diffing it against its
  own direct parent (`git diff <sha>~1 <sha>`), not against a shared
  ancestor point on the entangled branch (a wide-span diff there picks up
  unrelated origin/main sync noise from BL-1277/1281/1284 landing in
  between):
  - coder `9c6200d726` → 7 files (evidence, `briefingChartSvgCommon.ts`,
    `shiftVelocityChart.ts`, both new test files, the new step-handler file,
    `specs/pipeline/steps/index.js`).
  - architect `339e53b69f` → its own evidence file only.
  - hardener `a0bc9aee19` → its own evidence file only.
  - documenter `e373a940fa` → `docs/how-to/BL-1184-briefing-shift-velocity-chart.md`
    and `docs/reference/Specification.MD` only.
  - Confirmed each isolated diff touches nothing named BL-1182 or
    `model_steward_*`/`trial-boundary-memory`.
- Applied all four onto a fresh branch from current `origin/main`. Three
  applied cleanly; the coder diff's `specs/pipeline/steps/index.js` hunk did
  not (origin/main had since gained BL-1277/1279/1280/1281's own require
  lines, landed in between) — applied everything else from that diff, then
  added the one `require('./bl1232ShiftVelocityChartReadableSteps')` line by
  hand, in the same position the hunk intended (after
  `bl1281ReachFloorsByConstructionSteps`, before the closing bracket).

## required_wiring — confirmed on the rebuilt tree
- `shiftVelocityChart.ts` imports and calls the shared
  `pickLabelIndicesByPixelGap` from `briefingChartSvgCommon.ts` (not a local
  re-pick).
- `briefingChartSvgCommon.ts` exports `pickLabelIndicesByPixelGap`.
- `specs/pipeline/steps/index.js` registers
  `bl1232ShiftVelocityChartReadableSteps` (the hand-applied line above).

## Tests, run on the rebuilt tree
- `extension/test/bl1232ShiftVelocityChartReadable.test.js`: 15/15.
- `extension/test/bl1232ShiftVelocityChartInvariants.property.test.js`: 6/6
  — all three declared invariants (clip-not-drop, label pixel-gap, and
  presentation-only/non-linear-axis-preserved) each have a passing case.
- `specs/features/BL-1232-shift-velocity-chart-readable.feature`: 6/6.
- `specs/features/BL-1184-briefing-shift-velocity.feature` (re-run per
  qa_e2e_procedure step 7, to confirm no regression to the ticket this one
  depends on): 6/6, including scenario 4 ("the briefing chart uses a
  non-linear time axis with recent precision") — still green.

## Visual verification (qa_e2e_procedure steps 1-5)
Rendered `node out/tools/render-briefing-shift-velocity.js <repo-root>`
against the live repo and inspected the PNG directly:
- Y axis fits the ordinary-day body (0-50 range); daily variation is
  clearly visible, not flattened against the floor.
- The 415 peak is still findable: named in the subtitle ("Peak 415 tickets /
  8h stretch") AND drawn as a distinct clipped marker on the cap line
  carrying its true value ("415") as text.
- No two date labels overlap; labels read left-to-right oldest-to-newest
  (2026-07-09 → 2026-08-30) with even, non-overlapping pixel spacing.
- No single day-to-day gap swallows an outsized share of the plot width.

Did not separately dry-run the full briefing send (step 6) — this ticket's
diff does not touch briefing dispatch/composition, only the chart module's
internals (a pure rendering function BL-1184 already wires in), so there is
no new call site for a dry-run to exercise beyond what the acceptance
feature and the direct render above already cover.

## Verdict: APPROVE (as the rebuilt tip-pure commit, not the entangled parcel)

By QA.
