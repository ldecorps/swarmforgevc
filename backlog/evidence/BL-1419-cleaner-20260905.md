# BL-1419 — cleaner pass, 2026-09-05

DRY fix applied (no behavior change) — real duplication found and
eliminated; the coder's renderer/layout work itself is correct and
complete.

## Defect found and fixed

`jscpd` found the `ensureBriefingsDir`/`writeBriefing`/`runHarness`
briefing-harness-driving boilerplate written out identically in the new
`bl1419BriefingEmailReflowSteps.js` and the pre-existing (BL-393)
`briefingBodyHtmlSteps.js`. Extracted the actual logic (mkdtemp, file
write, shell-and-parse) into
`specs/pipeline/steps/lib/briefingEmailHarnessFixture.js`
(`ensureBriefingsDir(ctx, prefix)`/`writeBriefing(dir, fileName,
content)`/`runHarness(harnessPath, dir, mode)`); both files now keep a
thin one-line local wrapper binding their own prefix/filename/harness-path
constant and delegate the actual work to the shared module. No behavior
change: same mkdtemp prefix per file, same target filename per file, same
harness invocation.

Left `bl896BriefingOpenTicketChartSteps.js`'s own copy alone — its
signature differs (multi-file writes, variadic harness args) and it
predates this ticket entirely (only 3 regex assertions in it were touched,
for an unrelated reason); out of scope for a bounce-free NONE pass on this
ticket's own diff.

`jscpd` re-run on the touched/new files: down from 2 clones (33 duplicated
lines) to 1 residual clone (9 lines) that is the per-file wrapper
declaration itself — inherent parameter binding, not duplicated logic
(the alternative, inlining the shared calls with each file's literal
prefix/filename at every one of briefingBodyHtmlSteps.js's 5 call sites,
would repeat the literal MORE, not less). Left as-is; recorded here as a
considered stopping point, not an oversight.

## What was checked on the coder's own work

- Read `markdown_to_html_lib.bb`'s block-aware renderer diff directly:
  the new `list-item-line?`/`blockquote-line?`/`list-continuation-line?`/
  `paragraph-continuation-line?` predicates correctly partition a run of
  consecutive lines by block type before joining and rendering; traced by
  hand that `render-inline-markdown`'s bold/code regexes run AFTER lines
  are joined (so a `**bold**` span wrapped across a line break closes as
  one `<strong>`), and that escaping still runs before inline rendering
  (unchanged order, no injection regression).
- Read `briefing_email_lib.bb`'s `render-briefing-html`/
  `style-inline-elements`/`tag-inline-styles` diff: confirmed no `<style>`
  block is ever emitted (styles are inlined via a literal, non-regex
  `str/replace` per bare opening tag — exact and total per the renderer's
  own bare-tag-only contract) and that `merge-diagram-html` is correctly
  retired (its job absorbed into the new function, not left dead
  alongside it — confirmed removed, not merely unused).
- Confirmed the plain-text part (`content`) is untouched by this diff:
  `compose-and-send-one!` still passes it to `:send-email!` exactly as
  before — the code path invariant 3 depends on was not touched.
- Confirmed the fixture
  (`specs/pipeline/steps/fixtures/BL-1419-2026-09-05-briefing.md`) is a
  byte-identical copy of the live `docs/briefings/2026-09-05.md` (`diff`,
  empty).
- Rendered the REAL live briefing through the real (compiled/loaded)
  `render-markdown-to-html` + `render-briefing-html` pipeline directly (not
  relying on the coder's own claimed numbers): 24 `<li>`, 1 `<blockquote>`,
  3 `<h2>`, 0 `<style>` blocks, 0 ragged dash-led `<p>` fragments — exact
  match to the coder's evidence and the ticket's own mint-time counts.
- Reviewed the three pre-existing-test regression fixes
  (`briefingBodyHtmlSteps.js`, `bl896BriefingOpenTicketChartSteps.js`):
  each changed only `<tag>` → `<tag[^>]*>` to tolerate the new inline
  `style` attribute, without weakening the actual assertion (still
  anchored to the specific tag name and content, `[^>]*` cannot cross a
  `>` boundary) — correct, minimal, and matches the ticket's own framing
  that style VALUES are this ticket's direction, not a fixed contract.

## Verification (all re-run in this worktree, all green, no regressions, after the DRY fix)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/markdown_to_html_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/briefing_email_test_runner.bb` | ALL PASS |
| `node specs/pipeline/cli.js specs/features/BL-1419-...feature` | 8/8 |
| `node specs/pipeline/cli.js specs/features/BL-393-briefing-body-html.feature` (regression) | 5/5, unaffected |
| `node specs/pipeline/cli.js specs/features/BL-896-briefing-not-done-burndown-stamp.feature` (regression) | 7/7, unaffected |
| `node specs/pipeline/cli.js specs/features/BL-260/BL-579/BL-580-...feature` (diagram regressions) | 5/5, 2/2, 3/3 — all unaffected |
| Real `docs/briefings/2026-09-05.md` rendered directly through the pipeline | 24 `<li>`, 1 `<blockquote>`, 3 `<h2>`, 0 `<style>`, 0 ragged `<p>` fragments |
| `npx jscpd` on touched/new files | 2 clones (33 lines) → 1 residual (9 lines, inherent per-file binding, not logic duplication) |
| `diff docs/briefings/2026-09-05.md specs/pipeline/steps/fixtures/BL-1419-...md` | identical |

Forwarding the DRY-fixed candidate to architect.
