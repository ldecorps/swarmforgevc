# BL-1311 — QA pass — 2026-09-02

## Commit verified
`07a21d45f1` (merge of documenter's `16faea2316` into QA's worktree).
Ancestry check: `git merge-base --is-ancestor 07020c93a4 07a21d45f1` — OK.
This ticket's own hardener merge (`07020c93a4`) is an ancestor of the commit
being approved, not merely of HEAD.

No prior bounce found for BL-1311 on `main`, `origin/main`, or in
`backlog/evidence/` — this is its first QA pass.

## Merge note
Merging the inbound documenter commit into this worktree tripped
`check_merge_deletion.sh` on two paths already moved further along on this
branch than on documenter's (independent of this ticket):
- `backlog/debt/BL-1056-...yaml` → `backlog/paused/...yaml` (BL-1034 park sweep)
- `specs/features/BL-1310-...feature.draft` → `.feature` (BL-1310 shipped)

Both confirmed as legitimate pre-existing path moves already landed on this
branch (checked introducing commits `87c3ede599` and `843d3f833e`), named in
the merge commit message per the guard's requirement.

## Verification

| check | result |
|---|---|
| `npm run compile` (extension/) | clean |
| `node specs/pipeline/cli.js specs/features/BL-709-bubble-its-own-telegram-topic.feature` | 8/8, incl. scenario 07 (was 7/8 on main) |
| `npx vitest run test/letsTalkBridge.test.js test/bl744TopicMergeHelpers.test.js` | 56/56 |
| `npx vitest run --config vitest.properties.config.mjs test/bl709BubbleOwnTelegramTopic.property.test.js` | 3/3 |
| `npx vitest run` (full suite) | 570 files / 9822 tests passed; 15 files / 26 tests pre-existing-failed |

The 15 failing files (`pilotAcceptanceGate.test.js`, `tempDirTrapGuard.test.js`,
`telegramClient.test.js`, etc.) do not touch `bridgeServer.ts`,
`bubbleMirrorTopic.ts`, or any Let's Talk/Bubble path. Grepped `backlog/` and
confirmed these are pre-existing, already-tracked standing debt (e.g.
BL-741, BL-1032, BL-640, BL-1215 evidence files reference the same files),
unrelated to this ticket's diff — same set the hardener already documented.

## Wiring check
`grep -n effectiveLetsTalkMirrorTopicId extension/src/bridge/bridgeServer.ts`
shows a non-definition caller: `letsTalkMirrorTopicForPath` (new, line ~170),
itself called from both `choicePollMirrorTarget` and
`mirrorLetsTalkTurnToBubble` — the two real Let's Talk mirror call sites.
Previously zero non-definition callers; qa_e2e_procedure step 2 satisfied.

## Invariants
1. "A completed Let's Talk turn reaches exactly one topic or fails loudly" —
   satisfied: scenario 07 (unbound → Cursor Remote fallback) and scenario 03
   (bound → Bubble only) both pass.
2. "Ordinary Bubble mirroring keeps its never-fall-back contract" —
   `effectiveBubbleMirrorTopicId` / `bubbleMirrorTopicForPath` untouched by
   this diff (`bubbleMirrorTopic.ts:71`), still exercised by
   `test/bl744TopicMergeHelpers.test.js` (BL-744, unrelated to this ticket).

## Documentation
`docs/reference/Specification.MD` changelog entry (commit `051c80e103`)
accurately describes the defect and the fix; matches the actual diff.
`docs/how-to/BL-718-...md` already described the correct post-fix contract
per documenter's pass — no change needed, verified against current file.

## Findings
NONE. Clean sweep — full inventory run, no defects found.

## Verdict
APPROVED. Landing on `main` (BL-247); broadcasting merge-up note to
pipeline worktree roles; notifying coordinator for bookkeeping.

Leftover process check: `pgrep -fl 'node --test|stryker'` clean before and
after (matches were self-referential pgrep-of-its-own-command-line
artifacts, confirmed dead via `/proc` check both times, not real orphans).

By QA.
