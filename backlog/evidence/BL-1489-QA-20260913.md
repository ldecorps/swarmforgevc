# BL-1489 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Sibling disposition: `qa-sibling-check.js status --ticket BL-1489` →
  `VERIFY BL-1489` (no open deferral).
- Wiring gate: `pre_qa_gate.sh BL-1489-... HEAD` → `OK`.
- Unit suite (`cd extension && npm test`): 616 files / 10479 tests, all
  green. No orphaned test/mutation processes before or after (the
  `pgrep -fl 'node --test|stryker'` hits were the probe self-matching its
  own wrapper command line, verified via `ps -eo pid,ppid,args`).
- Property suite (`npm run test:properties`): 4 files red on the first
  full-lane pass, all four already register-owned and matching their
  documented flake signature (bl1089FrontDeskLivenessFixture → BL-1502,
  bl1272LandedSiblingInvariants → BL-1556, bl1313BatchGuardVisibilityInvariants
  → BL-1503, bl1367ApprovalCarriesItsRuling → BL-1527). A second full-lane
  pass (JSON reporter) additionally red 7 more files with no register row
  (bl1297MergeOwnPathsInvariants, bl1308SiblingDetectorCoversReplay,
  bl1315OwnPathsFullRangeInvariants, bl1323StampOffInvariants,
  bl1343ReplayNeverDropsOwnPathInvariants, bl1354SharedPathLandedSiblingInvariants,
  bl1529ScriptSenderAuditOutcomesInvariant), all as generic
  `STACK_TRACE_ERROR`/timeout with no assertion failure — the signature of
  worker/subprocess contention from running two heavy property passes
  back-to-back, not this project's own test:properties concurrency (each
  spawns one bb subprocess per fast-check draw, documented load-sensitive
  in BL-1556's own register row). Re-ran all 7 together in isolation: 6
  passed clean, 1 (bl1343) timed out once at exactly the 20s boundary; ran
  bl1343 alone a third time: passed in 18.9s. None of the 11 files touch
  anything BL-1489's parcel changed (`bl982SecondSeatSteps.js`, the new
  BL-1489 steps file, `standing-reds.tsv`). Treated as transient host-load
  noise, not a new standing red — no ticket minted.
- TypeScript: `npx tsc --noEmit -p .` from `extension/` — clean.
- Acceptance, BL-982's own feature (`run_acceptance.sh
  specs/features/BL-982-second-seat-of-a-stage-boots-with-its-own-model.feature`):
  `# pass 6 / # fail 0` (was `# fail 1` row 04 before this fix).
  `git diff main -- specs/features/BL-982-*.feature` empty (feature text
  and mutation stamp unedited).
- Acceptance, BL-1489's own feature
  (`specs/features/BL-1489-the-bl982-single-seat-handler-compares-the-fields-it-names.feature`):
  `# pass 4 / # fail 0`.
- Register: `standing_red_register_cli.bb <root>` — no row for BL-982's
  feature (removed in this parcel); `unowned: []`.
- Wiring: the new step file
  (`specs/pipeline/steps/bl1489Bl982HandlerComparesNamedFieldsSteps.js`)
  is proven wired by the acceptance run itself actually invoking
  `registerSteps` and executing its 4 scenarios above — not merely
  present and unit-tested in isolation.
- Bounce history: none on `main`/`origin/main` for BL-1489 — first pass.
- Commit citing: verified `git merge-base --is-ancestor
  ad3684fe74 HEAD` (BL-1489's own hardener merge) — YES. HEAD at
  verification time: `29ada9a5f8` ("Merge documenter e387fa030e into QA.").
- BL-1241 land-step check: `land_step_cli.bb BL-1489-... HEAD` →
  `LAND_REPLAY` naming entangled siblings BL-1498, BL-1499, BL-1518 (all
  closed/done tickets whose content the walk could not fully confirm
  landed) and landed-and-confirmed BL-1485, BL-1500, BL-1528. Reviewed a
  first hand-built replay tip (`d007600a535ebc642e5e3122b66d0ee193753301`,
  since abandoned/dangling — never pushed): parent `origin/main` exactly,
  diff contains only BL-1489's own 9 paths (feature file, new steps file,
  `bl982SecondSeatSteps.js`, `standing-reds.tsv` row removal, 5 evidence
  files) — matches this ticket's own coder/cleaner/architect/hardender/
  documenter evidence exactly. Recorded `abandoned_commits: [29ada9a5f8]`
  on the ticket (634675a230). Landed via
  `land_main_publish.sh --land BL-1489-... 29ada9a5f8` (first attempt hit
  a transient `could not create worktree` LAND_ESCALATE, retried clean):
  it rebuilt an equivalent tip-pure replay with the same diff and pushed
  **`6671ee88652b5f6eb510f6e7fd5ff35af1118764`** to `origin/main`
  (`45833c6083..6671ee8865`). That is the landed commit, not `29ada9a5f8`
  and not the earlier hand-built `d007600a53`. Re-point skipped
  (`LAND_REPOINT_SKIPPED a parcel in its in_process`) — expected, this
  parcel is still `in_process` until `done_with_current.sh`; no action
  needed.
