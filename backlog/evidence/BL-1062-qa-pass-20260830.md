# BL-1062 QA pass — verification and completed BL-1241 rebuild (2026-08-30)

## Disposition: PASS

## BL-1241 entangled-tip rebuild, completed

The documenter's handoff commit (`b345346ac`) carried the coder's long-lived
branch ancestry entangled with dozens of unrelated sibling tickets' merges
(BL-1209, BL-1246, BL-1276, BL-1193, BL-1273, BL-1267/1268/1220/603
step-scoping) — the same tangle shape other tickets on this branch were
already being rebuilt around.

Rebuilt tip-pure onto `origin/main` (which already carried the prior BL-1246
rebuild at its tip): cherry-picked BL-1062's own three cleanly-scoped
commits — coder `85fa5d74f` (satisfiable-by-construction fix), hardener
`120edac96` (direct unit coverage for the shared `reachFloors` helper),
documenter `b345346ac` (Specification.MD entry) — each verified via
`git show --stat` to touch only BL-1062's own files.

**Found and fixed mid-verification**: a prior rebuild attempt on this same
branch (`f476e976a`, earlier this session) replayed only the coder's diff
and silently dropped the hardener's `extension/test/reachFloors.test.js`
and the documenter's `Specification.MD` entry. Caught by diffing the
rebuilt commit's file list against each original stage commit's own
`git show --stat` rather than trusting the rebuild as complete. Completed
by cherry-picking the missing two commits on top (`816604df5`, `276dcd561`).

`git diff origin/main HEAD --stat` (final, before the ticket-record commit)
confirmed exactly BL-1062's own eight files — evidence md, both property
test files, `reachFloors.js` helper, `reachFloors.test.js`, the step
handler, `specs/pipeline/steps/index.js`, `Specification.MD`.

`abandoned_commits` recorded on the ticket (65 hashes — the full superseded
chain from the coder's original commit through the documenter's tip,
including every sibling-ticket merge carried along).

## Verification performed

1. **Compile** — `npm run compile` (extension/): clean.
2. **required_wiring** — `specs/pipeline/steps/index.js` line 11 registers
   `bl1062ReachFloorSatisfiableSteps`. Confirmed present.
3. **Unit suite** — `npm run test`: 27 files / 219 tests failing, ZERO
   overlap with BL-1062's files. `extension/test/reachFloors.test.js`
   (hardener's own file) passes 7/7. All 27 failing files cross-checked
   against already-tracked standing debt: BL-1220 (node:test import files),
   BL-1221 (`checkOrphanedAuthoredDocs` stub gap —
   `unreachableStepHandlerCheck.test.js`, `pilotAcceptanceGate.test.js`),
   ambient `CURSOR_API_KEY` gap (BL-720 fixed the leak; this shell's ambient
   env lacked the key — infra, not code — `bridgeServer`, `topicMakeTopBridge`,
   `epicMakeTopBridge`, `epicReorderBridge`, `pausedPagerBridge`,
   `specTreeBridge`, `startBridgeHeadlessCli`, `telegramCursorBridgeCli`,
   `telegramCursorOperatorExec`, `telegramClient`), repo-hygiene guard reds
   (`constitutionDocCitations`, `liveRepoDerivationGuard`,
   `socketFixtureShortRootGuard`, `tempDirTrapGuard`, `tmpDirMigrationGuard`),
   and the previously-reported-untracked trio (`backendSwitch`,
   `backlogDashboard`, `operatorRuntimeBbFixtureClosure`) per
   `BL-1244-qa-pass-unrelated-reds-20260829.md` and
   `specifier-disposition-qa-standing-red-note-20260828.md`. Matches the
   already-dispositioned list exactly — nothing new, not blocking (BL-1063
   doctrine).
4. **Property tests** — `test/bl948SocketFixtureInvariants.property.test.js`
   and `test/bl968MaterializedGuardSensitivity.property.test.js`: 20/20
   consecutive fresh-seed runs green (both files together each run).
   bl968's coverage was deterministic every run (8/8/8 classes, 12/12
   depths), confirming construction, not luck.
5. **Non-vacuity (invariant 2)** — temporarily dropped `benign-subprocess`
   from bl968's driving `CLASSES` array; the floor went red naming exactly
   that class (`reach floor: class benign-subprocess drawn 0 < 5`). Reverted;
   `git status` confirmed a clean revert.
6. **Floors unweakened** — `CLASS_FLOOR = 5`, `DEPTH_FLOOR = 6` in bl968,
   unchanged from the ticket's own recorded arithmetic; `numRuns` in bl948
   unchanged.
7. **Acceptance** — `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-1062-generator-reach-floors-are-a-per-run-lottery.feature`:
   4/4 scenarios pass, including "a floor still fails when the generator
   stops reaching a class" and "no floor was removed to reach green".
8. **Process hygiene** — checked for orphaned `node --test`/`stryker`
   processes before and after; none of mine. A concurrent `vitest` run
   observed belongs to a separate coder-worktree process tree (different
   PPID chain, `.worktrees/coder`) — not touched.

No `src/*.ts` component in this ticket's own diff; CRAP/DRY/Stryker do not
apply (hardener's own note, re-confirmed).

By QA.
