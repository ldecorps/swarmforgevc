# BL-1317 — QA bounce (20260902)

Received: documenter commit `1f6876b099`, merged into QA at `ca66e21f5a`
("Merge documenter 1f6876b099 for QA review (BL-1317). By QA.").

## Review inventory (complete pass, Article 4.4)

- Full unit suite (`npm test` in `extension/`): 15 files / 25 tests red.
  Every one grepped against the backlog and confirmed pre-existing, already
  ticketed, and independently corroborated by the hardener's own pass
  (`backlog/evidence/BL-1317-hardener-pass-20260902.md`, "Attempts before
  the clean run" section): `test/liveRepoDerivationGuard.test.js`
  (**BL-1291**), `test/telegramCursorOperatorExec.test.js` +
  `test/telegramClient.test.js` + `test/backendSwitch.test.js`
  (**BL-1263**), `test/pilotAcceptanceGate.test.js` and 6 sibling
  `landPilotedTicket`-family files (`crossFileDuplicationCheck`,
  `multiBranchParserCoverageCheck`, `perHatRolePromptEvidenceCheck`,
  `pilotScopedCrapCheck`, `shellEntryPointDriveCheck`,
  `unreachableStepHandlerCheck`) — `deps.checkOrphanedAuthoredDocs is not a
  function` (**BL-1221**/**BL-1229**),
  `test/operatorRuntimeBbFixtureClosure.test.js` (**BL-1265**),
  `test/socketFixtureShortRootGuard.test.js` /
  `test/tempDirTrapGuard.test.js` (**BL-1290**/**BL-1226**/**BL-1312**),
  `test/constitutionDocCitations.test.js` (long-standing, prior QA
  corroboration on file). PASS — none is this parcel's own defect; not
  reproduced from any file this parcel touched.
- Property suite (`npm run test:properties`): 24 files / 13 named tests red,
  plus the allowlisted BL-871 `[vitest-worker]: Timeout calling
  "onTaskUpdate"` benign unhandled error. None of the 24 files name
  `effortDialAdapt`, `seat_difficulty_lib`, `handoff_lib`, or
  `done_with_current_task` — none overlaps this parcel's changed paths.
  Consistent with the same standing-debt population the hardener's pass
  already inventoried (pilot/land-gate family, hotfix-ledger stamp-off
  family, host-queue/liveness-probe family). Treated as pre-existing per
  BL-1063 (not reproduced from this parcel's diff); not this parcel's own
  defect.
- Effort-ladder cross-language parity
  (`swarmforge/scripts/test/test_bl1317_effort_ladder_parity.sh`): PASS,
  6/6 checks (BL-897 discipline).
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1317-adapt-tier-effort-from-outcome-signals.feature`):
  PASS, 3/3 scenarios, driving the real `swarm_handoff.bb` /
  `ready_for_next_task.bb` / `done_with_current_task.bb` pipeline over a
  seeded fixture (not prose, not a diff assertion).
- Behavior against ticket intent: the bb-side Adapt policy
  (`seat_difficulty_lib.bb::adapt-effort-decision`,
  `handoff_lib.bb::record-effort-adapt!`, wired at
  `done_with_current_task.bb::record-effort-adapt-for!`) is real and live —
  confirmed by the acceptance run actually moving
  `.swarmforge/launch/<role>.claude-settings.json` end to end. PASS.
- Documentation currency: `docs/how-to/BL-1317-...md` and
  `docs/reference/Specification.MD` both read the shipped code before
  writing (confirmed against `git log`), and the how-to's own "Wiring"
  section (§3) correctly describes ONLY the bb-side consumer chain — it
  never claims the TypeScript side has a live caller. Diagram registry: no
  change-trigger fired (confirmed against the four `DIAGRAM_FILES`
  triggers) — correctly no diagram touched. PASS, no documenter-domain
  defect.

## D1 — required_wiring item 1 unmet: `decideAdaptEffort` has zero live callers

**Failing check**: `grep -rln "adaptRoleEffort\|effortDialAdapt" --include="*.ts" --include="*.js" --include="*.bb" --include="*.sh" .` (repo root, excluding `node_modules`), then excluding `extension/src/tools/effortDialAdapt.ts` itself.

**Commit checked out**: `ca66e21f5ab2537d4a7b824bd16696fbe79f3f0b` (QA merge of documenter `1f6876b099`).

**Result**: the only hits are `extension/test/bl1317AdaptEffortInvariants.property.test.js`, `extension/test/effortDialAdapt.test.js`, `swarmforge/scripts/seat_difficulty_lib.bb` (a comment cross-referencing the file, not a call), and `swarmforge/scripts/test/test_bl1317_effort_ladder_parity.sh` (checks the exported ladder *constant*, never calls `decideAdaptEffort`/`adaptRoleEffort`). No file under `extension/src/` other than `effortDialAdapt.ts` itself imports the module (`grep -rln "from '.*effortDialAdapt'" extension/src` — empty).

**Failure class**: `behavior` (a required_wiring item is unmet — the ownership-drives-routing rule, not the class, decides where this goes; see below).

**Expected vs observed**: `backlog/active/BL-1317-adapt-tier-effort-from-outcome-signals.yaml`'s `required_wiring` names `extension/src/tools/effortDialAdapt.ts::decideAdaptEffort` and states "UI and launch paths call it, never embed a second policy" — i.e. a real TypeScript-side caller is a condition of this ticket being done. Observed: `decideAdaptEffort` and its apply-edge wrapper `adaptRoleEffort` are exercised only by their own unit/property tests; no UI surface, launch path, or any other production module calls either. This is exactly the BL-149 pattern this role's own prompt names: correct, tested, unit-green code that nothing in the live swarm invokes has zero effect there.

The feature itself is NOT broken — the bb-side implementation (`seat_difficulty_lib.bb` / `handoff_lib.bb::record-effort-adapt!`, wired at `done_with_current_task.bb`) is real, live, and independently verified above by acceptance. But the ticket's own required_wiring made an explicit, separate claim about the TypeScript side that the shipped commit does not satisfy — a self-contradiction on file: the module's own header comment claims "SINGLE POLICY... Any UI or launch path that wants Adapt calls this," which is not true of the commit as it stands. Either the required_wiring item should never have named a TS-side caller (if bb-only was always the intended shipped shape, in which case the file/comment overclaims and the wiring line is stale), or a real caller is missing. Recognizing which is coder's call, not QA's — QA does not add behavior (Does Not Own) and does not silently narrow a ticket's own required_wiring by declaring it moot.

**Remediation pointer**: `extension/src/tools/effortDialAdapt.ts` (the module and its header comment), plus whichever coder-owned decision resolves the contradiction — either wire `adaptRoleEffort` into a real caller (if a TS-side UI/launch path is meant to exist now or is close enough that stubbing it in is the intended shape), or narrow `required_wiring` on `backlog/active/BL-1317-adapt-tier-effort-from-outcome-signals.yaml` and the file's own header comment to state plainly that only the bb side is live and the TS module is exploratory/unused. Owning role: **coder** (required_wiring / implementation-wiring is a coder deliverable; this is not a docs-only, hardening-only, or architecture-layering defect).

## Verdict

BOUNCE — one item, D1. Routed to **coder** (the role whose deliverable
`required_wiring` names). No other gate failed; no spec-gap found — this is
an implementation/wiring gap against the ticket's own stated requirement,
not an ambiguity in the requirement itself.

By QA.
