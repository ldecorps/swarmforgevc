# BL-1585 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Orphan check before/after: `pgrep -fl 'node --test|stryker'` empty both times.
- `qa-sibling-check.js status --ticket BL-1585` → `VERIFY BL-1585` (no open deferral).
- Standing-red register (`backlog/standing-reds.tsv`): 5 rows, all owned by
  BL-1588/BL-1589; none name any of the 16 pinned BL-1585 files.
- Unit suite (`npm test` in extension/): 620 files, 10527 tests, all green.
  Per-file budget warnings are non-blocking perf notices, no failures.
- Property suite (`npm run test:properties`): 408 files, 1201 tests, all
  green (exit 0). Two `[vitest-worker]: Timeout calling "onTaskUpdate"`
  unhandled errors are the allowlisted BL-871 benign noise, not a swept-file
  failure.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1585-sampled-reach-floors-sweep-1-of-6.feature`): 17/17
  pass.
- Per-file floor check (qa_e2e_procedure step 1): each of the 16 pinned
  files run 5x back to back under `vitest.properties.config.mjs` (80 runs
  total) — no FAIL.
- Floor audit (step 3): `git diff main...HEAD -- extension/test | grep -E
  '(numRuns|assertReachFloor|>= *[0-9]|> *0|runsPerCell)'` read against the
  coder's table — every removed literal `numRuns`/bare-floor assertion
  reappears as a `runsPerCell`-derived `CELL_RUNS` constant of the same
  total budget and an `assertReachFloor` call at the same floor value; the
  one new floor (bl1297, value 1) is additive alongside the pre-existing
  exhaustive assertion, not a replacement.
- Break-then-restore spot check (step 4, independent of the coder's own):
  read bl1279's structure directly — confirmed an outer `for (const
  drawnDepth of DEPTHS)` with one `fc.assert` per depth and
  `DEPTH_FLOOR = runsPerCell(DEPTHS.length * 3, DEPTHS.length)`, matching
  the claimed construction shape.
- Diff scope (step 5): `git diff main...HEAD --name-only` names only the 16
  pinned test files, the feature's step handler
  (`specs/pipeline/steps/bl1585SampledReachFloorsSweep1Steps.js`), and
  evidence files — no `extension/src`, no `extension/test/helpers/reachFloors.js`.
- Ancestry: commit lineage is a direct coder→cleaner→architect→hardender→
  documenter chain for BL-1585 itself (3e35a17ca2 … d43c18b24c), merged into
  QA at 71783957dd — no sibling-ticket entanglement.
- `required_wiring`: no production anchor by design (BL-1235, ticket's own
  note) — the acceptance run (17/17) is the wiring proof for the step
  handler.
