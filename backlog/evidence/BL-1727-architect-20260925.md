# BL-1727 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Scope confirmed via the coder's two commits directly (not the merge-inflated
range): `4b529f3254` (ollama_ancillary_stop_pid waits, bounded, for the pid
to be gone) and `50bc982198` (guards the one internal caller against the
new failure return) touch only `swarmforge/scripts/ollama_ancillary_lib.sh`,
its shell test, the property test + fixture helper, the feature's step
handler, and `suite-manifest.tsv` — matches the ticket's Scope exactly.

Checks run:
- `node out/tools/dependency-gate.js test/bl1727OllamaStopWaitsUntilGone.property.test.js test/helpers/ollamaStopPidFixture.js`
  → PASSED, no forbidden edges.
- `node out/tools/co-change-report.js` on the same files → coupling only
  with this parcel's own sibling files (step handler, the lib, the shell
  test, the manifest) — no cross-ticket suspects.
- `npx vitest run --config vitest.properties.config.mjs test/bl1727OllamaStopWaitsUntilGone.property.test.js`:
  2/2 passed.
- `node specs/pipeline/cli.js specs/features/BL-1727-stopping-a-started-ollama-server-waits-until-it-is-gone.feature`:
  4/4 ok.
- `bash swarmforge/scripts/test/test_ollama_ancillary_stop_pid.sh`: 4/4 PASS.
- `bash swarmforge/scripts/test/test_ollama_ancillary_launch_gate.sh`: 2/2
  PASS (regression check named in the ticket's qa_e2e_procedure step 3).
- `npx vitest run --config vitest.properties.config.mjs test/bl1703OllamaLaunchProbe.property.test.js`:
  2/2 passed — confirms the original race (BL-1720's unowned red) is fixed.
- `required_wiring` pin resolved: `bl1727OllamaStopWaitsUntilGoneSteps.js`
  exports `registerSteps` in this same parcel.

Invariants review: the one declared invariant ("When
ollama_ancillary_stop_pid returns success, the pid it was given is not
alive") has a non-vacuous property test — the test file states, and I
verified by reading `ollama_ancillary_wait_gone`/`ollama_ancillary_stop_pid`,
that reverting to the pre-BL-1727 `kill "$pid" || true; return` would fail
the "ignores-term" case, exactly the BL-1720 race. Coverage is exhaustive
over the small discrete behaviour space (exits-after-term, ignores-term,
already-exited) with a reach-floor assertion, plus a dedicated failure-path
test (unsignallable pid never reported stopped).

Correctness read: traced `ollama_ancillary_wait_gone`'s poll loop and
`ollama_ancillary_stop_pid`'s TERM→wait→KILL→wait→fail sequence against the
ticket's FIRM constraints — bounded, never an unbounded spin; success means
not-alive at return; failure names the pid. The coder's own self-audit
finding (guarding the internal caller's now-fallible call under the
caller's `set -e`, commit `50bc982198`) is correct and verified: grepped
the whole tree for other callers of `ollama_ancillary_stop_pid` —
`ollama_ancillary_ensure_ready_for_launch` (now guarded with `|| true`) is
the only one.

Property testing pass: this IS the declared invariant's own test; no
further undeclared-property gap found in the touched pure shell/test
surface (shell scripts sit outside the fast-check pure-module scope, and
the fixture-behaviour space is already exhaustively covered above).

By architect.
