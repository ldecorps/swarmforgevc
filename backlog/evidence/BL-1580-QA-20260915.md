# BL-1580 — QA review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

This pass completes an Article 4.2 hold opened during BL-1580's own
review (`backlog/evidence/BL-1580-QA-unowned-red-20260915.md`): four
property-lane files timed out on a full-lane run, unrelated to this
parcel's diff. The specifier adjudicated and minted BL-1588 to own them
(`backlog/evidence/BL-1580-QA-unowned-red-specifier-adjudication-20260915.md`,
mint `b696762f2a`), added four `backlog/standing-reds.tsv` rows naming it,
and sent the resume note (BL-1566 shape). Resumed via
`.swarmforge/qa-holds/BL-1580-bl1295-revert-attribution-boolean-arm-is-constructed.json`
(commit `325fe1d4ea`, reds `bl1308`/`bl1315`/`bl1343`/`bl1354`).

- Synced this branch to `origin/main` (`766f5a9f74` incl. the BL-1588 mint)
  before re-verifying; `grep -E "bl1308|bl1315|bl1343|bl1354"
  backlog/standing-reds.tsv` now shows all four rows naming BL-1588 (open,
  `backlog/paused/`) — no longer unowned.
- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1580`
  → `VERIFY BL-1580` (no open deferral).
- `bash swarmforge/scripts/pre_qa_gate.sh
  BL-1580-bl1295-revert-attribution-boolean-arm-is-constructed HEAD .` →
  `OK` (two benign subject-only-ancestry warnings for the architect/cleaner
  no-op passes, no path overlap with the parcel).
- `extension/`: `npx vitest run --config vitest.properties.config.mjs
  test/bl1295RevertAttributionInvariants.property.test.js` re-run 5/5 green
  standalone post-sync (7.7–8.3s each), on top of the 15/15 the parcel's
  own `qa_e2e_procedure` step 1 already recorded before the hold.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1580-bl1295-revert-attribution-boolean-arm-is-constructed.feature`
  → 5/5 scenarios pass (the step handler
  `specs/pipeline/steps/bl1580RevertAttributionArmsConstructedSteps.js` is
  auto-discovered by `specs/pipeline/steps/index.js`'s `loadHandlers()`, no
  explicit registration line needed — confirmed functionally by the green
  run).
- `npm test` (extension/): 620/620 files, 10513/10513 tests pass. Exit 1 is
  the pre-existing `SUITE_DURATION_BUDGET_MS`/per-file budget gate
  (`backlog/paused/BL-791-epic-unit-suite-speed.yaml`, "BL-378's guard has
  19 live offenders and therefore fails every `npm test`... already a
  permanently-red gate"); the six named offenders
  (`bl1277UnscopedStepCollisionGuard`, `bl968StepRegistryMaterializedTreeGuard`,
  `briefingDigestLineCli`, `renderBriefingBurndownCli`,
  `renderBriefingDiagramsCli`, `telegramFrontDeskBotCli`) are none of this
  parcel's two changed files (BL-1063: already ticketed, grepped and
  confirmed before reporting).
- `npm run test:properties` (extension/): full 408-file lane, 1201/1201
  tests pass clean this run — including the four now-BL-1588-owned files
  and BL-1581's `bl1358...` file, all green under today's host load. Two
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors are the
  BL-871-allowlisted benign noise. Consistent with the specifier's
  load-contention finding: the reds are real but intermittent, not this
  parcel's defect.
- `git diff main...HEAD --name-only`: test file, step handler, evidence
  files only; `extension/src` unchanged, matching the ticket's `constraints:`.
- No stragglers before or after (`pgrep -fl 'node --test|stryker'` — the
  only match was the property-lane run's own process).

Citing commit `269d0f2354` ("Merge main 766f5a9f74 into QA."): the
documenter's merge `edbb16133c` and the BL-1588 mint `b696762f2a` are both
ancestors of it (`git merge-base --is-ancestor edbb16133c HEAD`, `git
branch --contains b696762f2a` includes `main`, merged into this branch by
this commit).

By QA.
