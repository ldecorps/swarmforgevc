# BL-1700 — QA review pass, 2026-09-26

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: sed -n 448,475p swarmforge/scripts/model_steward_coder_probe_lib.bb; sed -n 544,575p swarmforge/scripts/model_steward_cli.bb
- **Commit hash**: 33b0562c1a
- **First error excerpt**: probe!: (when evidence-dir (fs/create-dirs evidence-dir) ... (spit ... "local-coder-probe-" safe-model "-" stamp ".md")). evidence-dir comes only from the CLI flag --evidence-dir (model_steward_cli.bb run-probe, opt-value flags "--evidence-dir"), with no default. So `model_steward_cli.bb probe <model>` as the how-to runs it writes no summary at all; the JSON on stdout is the only trace. Remaining part of the 2026-09-26 D3 (its remediation: "probe! summary default dir backlog/evidence"); the rework fixed the resolved id, turns and llm-history parts only.
- **Failure class**: behavior
- **Expected vs observed**: ticket item 4: a Summary per invocation ... written under backlog/evidence/ as local-coder-probe-<model>-<UTC>.md / observed: a summary is written only when --evidence-dir is passed; the default invocation writes none
- **Blamed role**: coder
- **Remediation pointer**: model_steward_coder_probe_lib.bb probe!: default evidence-dir to <repo root>/backlog/evidence (keep the flag as an override; a stand-in/acceptance run may still pass a scratch dir); add a check that runs probe with no --evidence-dir and finds the summary under backlog/evidence; docs/how-to/BL-1700-model-steward-coder-probe.md stops calling it optional

By QA.

## Detail

Second pass, on the rework (documenter 34940e18b0, merged into QA as
33b0562c1a; every BL-1700 path on the QA branch matches 34940e18b0).

Against the 2026-09-26 bounce:
- D1 fixed: `run-fixture!` defaults nil caps with `or`, in one place.
- D2 fixed: `drive-until-capped!` stops on a real `fixTurnsUsed` turn cap,
  and the wall clock bounds elapsed time.
- D3 partly fixed: `resolve-model-id` reads `<endpoint>/models`,
  scorecards carry `:turns` and an absolute `:llmHistoryPath` outside the
  run root, and the re-taken evidence names `qwen3-14b:latest`. The summary
  default location is still open, as item D1 above.
- The earlier unowned red, `test_model_steward_cli.sh`, is now owned by
  BL-1767 (register row).
- The `model_steward_cli.bb` probe verb is wired at HEAD (lines 37, 544 and
  606). The 99f85f6ae4 revert did not strand it.

Gates (qa-gather, tmp/BL-1700-gather.json): no stragglers before or after,
`VERIFY BL-1700`, register 6 rows `unowned: []`, wiring OK, `npm test` exit
0, `npm run test:properties` exit 0 (only the allowlisted BL-871 timeout),
and the BL-1700 acceptance feature passed 7 of 7.

Revert: the item is in code the parcel added, so BL-1700's own paths are
restored to origin/main in the next commit. The bounce handoff names this
evidence commit, which comes before that revert.
