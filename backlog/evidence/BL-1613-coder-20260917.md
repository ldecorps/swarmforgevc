# BL-1613 — coder evidence, 2026-09-17

## What changed

`swarmforge/scripts/test/test_branch_claim_guard.sh`'s fixture wrote
`.swarmforge/swarm-identity` with only `swarm_name`/`swarm_mode` — no
`active_backlog_max_depth_conf_path`. `backlog_depth_lib.bb`'s
`conf-file-path` (BL-966) reads that key and, when absent, prints a loud
by-design fallback to stderr; `ready_for_next_task.bb` reads the pack conf
through it on every claim (BL-1004), so this test's own "a passing guard
emits no warning" assertion (`[[ -z "$ERR" ]]`, line 108) has failed since
2026-08-21. Fix, exactly the ticket's "How": two added lines create a
tracked `swarmforge/swarmforge.conf` under the fixture root and the
identity gains a third row naming it (relative form, matching
`test_backlog_depth_conf.sh`'s own convention). No assertion touched, no
case removed — confirmed:
`git diff -- swarmforge/scripts/test/test_branch_claim_guard.sh | grep -E '^-' | grep -vE '^---'`
shows exactly one removed line, the old two-row printf.

## Required wiring (ticket YAML)

- `test_branch_claim_guard.sh` carries `active_backlog_max_depth_conf_path`
  — `grep -n active_backlog_max_depth_conf_path swarmforge/scripts/test/test_branch_claim_guard.sh`
  → present in the fixture's swarm-identity printf.
- `specs/pipeline/steps/bl1613BranchClaimGuardFixtureSteps.js` exports
  `registerSteps`, lands in this commit alongside the (specifier-authored)
  feature file, auto-discovered by `specs/pipeline/steps/index.js`.

## Declared invariant (BL-654)

> "The fixture is a swarm root the launcher could have made: every key
> backlog_depth_lib.bb reads from a swarm-identity is present and names a
> file that exists under the fixture root before the first claim runs."

`swarmforge/scripts/test/bl1613_launcher_shaped_identity_property_runner.bb`
derives the required key set from `backlog_depth_lib.bb`'s own real source
(a regex over its text — never a hand-maintained duplicate that could
drift if that library ever reads a second key), then checks a candidate
root's `.swarmforge/swarm-identity` against it (`launcher-shaped?`). Four
generated shapes over real mkdtemp roots: every key present with its file
existing (true), a key missing entirely (false), a key present but its
file absent (false), and a complete identity plus unrelated noise rows
(still true). Generator-reach for all four asserted. Non-vacuousness tied
to this ticket's own concrete before/after: the byte-identical *pre-fix*
fixture identity text is shown NOT launcher-shaped (the bug), the
byte-identical *post-fix* text IS (the fix) — both replayed verbatim, not
re-derived by hand.

Deliberately does not re-test `backlog_depth_lib.bb`'s own resolution
contract (identity-present → silent, identity-absent → loud stderr) —
that is already BL-966's own declared invariant 2, covered by
`bl966_depth_identity_root_property_runner.bb`; duplicating it here would
just be two copies of the same claim under two ticket names.

`bb swarmforge/scripts/test/bl1613_launcher_shaped_identity_property_runner.bb`
→ `ALL PROPERTIES HOLD: launcher-shaped-identity (BL-1613), keys=["active_backlog_max_depth_conf_path"]`.
`extension/test/bl1613LauncherShapedIdentityInvariant.property.test.js` is
the thin Vitest wrapper (BL-654's "runs only via `npm run test:properties`"
placement).

## Acceptance (specifier's own feature file, unmodified by the coder)

`bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1613-the-branch-claim-guard-test-fixture-is-a-complete-swarm-root.feature`
→ 3/3 pass. Non-vacuous: stashing the fixture fix alone reproduces
`not ok 1` and `not ok 2` (scenario 3, the census, is unaffected either
way — it counts files, not this file's own content); restoring it returns
3/3.

## `test_branch_claim_guard.sh` itself

`env -u TMUX bash swarmforge/scripts/test/test_branch_claim_guard.sh` →
`ALL PASS`, 15/15 cases, including `guard-01: legacy role branch
swarmforge-coder passes for claim BL-529` (the case the ticket's own
reproduction named). Non-vacuous: reverting the fixture fix alone
reproduces the ticket's own exact failure line verbatim:
`FAIL: 01(swarmforge-coder): a passing guard emits no warning, got:
backlog_depth_lib: no swarm-identity for <tmp> - falling back to the
tracked default conf <tmp>/swarmforge/swarmforge.conf`.

## Full verification lane (per role prompt)

- `npm test` (compile + unit lane): 625 files / 10669 tests passed (0
  failed this run).
- `npm run test:properties` (full lane): 414 files / 1218 tests passed (0
  failed), including both `bl1613LauncherShapedIdentityInvariant` and this
  session's earlier `bl1611DriftGuardInProcessInvariant`. (An earlier
  attempt at this run overlapped with a second, accidentally-started
  concurrent full-suite run and produced four unrelated, non-reproducing
  failures from resource contention between the two - none in this
  ticket's own files; a clean single run afterward was fully green.)

By coder.
