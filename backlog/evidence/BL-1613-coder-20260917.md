# BL-1613 — coder pass, 2026-09-17

## The fix

`swarmforge/scripts/test/test_branch_claim_guard.sh`: the fixture's
`.swarmforge/swarm-identity` gains a third row,
`active_backlog_max_depth_conf_path\tswarmforge/swarmforge.conf`, and two
lines create that tracked conf (`mkdir -p "$ROOT/swarmforge"; : >
"$ROOT/swarmforge/swarmforge.conf"`) before the first `run_ready` call —
the exact shape `swarmforge.sh` itself persists (BL-966). Only the one
identity `printf` line is removed; every assertion is untouched (qa_e2e
step 3, verified below).

## Acceptance

`specs/features/BL-1613-the-branch-claim-guard-test-fixture-is-a-complete-swarm-root.feature`
was already authored by the specifier at mint (`d6da5aa625`) — this pass
adds only `bl1613BranchClaimGuardFixtureSteps.js` (BL-233/BL-1371, lands
with the feature, discovered by file name). Scenario 01 reads the real
script's own source (the defect is a gap between two real files — a
missing fixture key — that a reimplementation of either side could not
usefully stand in for); scenario 02 runs the real shell test from the
repository root; scenario 03 scans the real `swarmforge/scripts/test/`
tree for every file that drives the claim path (`ready_for_next_task` or
`ready_for_next.sh`) AND asserts an empty stderr, expecting exactly one:
this file.

## BL-654: the one declared invariant

"The fixture is a swarm root the launcher could have made: every key
`backlog_depth_lib.bb` reads from a swarm-identity is present and names a
file that exists under the fixture root before the first claim runs."

`swarmforge/scripts/test/bl1613_fixture_identity_completeness_property_runner.bb`:
the key set is DERIVED from `backlog_depth_lib.bb`'s own source (a
`(get (swarm-identity-lib/read-swarm-identity ...) "KEY")` regex scan),
never hardcoded to the one key that exists today — the property then holds
for whatever the reader actually reads, matching the invariant's own
"every key" wording rather than "the one key currently named". For each
key found, asserts the fixture's swarm-identity carries it and (since the
one key today is a conf path) that the fixture creates the file it names
before the first bare `run_ready` invocation — the same ordering scenario
01 checks, encoded independently as a property over the real source files
rather than the acceptance runner's own read.

Proven non-vacuous by hand before committing: reverting the fixture to its
pre-fix single-line swarm-identity (`git show HEAD:<path>`, no conf-path
key) fails the property naming the missing key; restored before landing.

## Register row

The standing-red register row (`backlog/standing-reds.tsv`, lane `shell`,
owner BL-1613) is left as-is — per the ticket's own "the register row
leaves in the same land" and the BL-1607 precedent, its removal is a QA
land-time action, not a coder-pass one.

## Why the npm unit/property lanes were not re-run for this parcel

This parcel touches zero files under `extension/` (a shell test fixture, a
Gherkin feature already on `main`, one new JS step handler under
`specs/pipeline/steps/`, one new Babashka property runner, and this
evidence file) — nothing `npm test`'s vitest unit lane or
`npm run test:properties` collects, and neither could possibly be affected
by a change entirely outside their scope. Both were already run fresh
earlier this session (for BL-1609, same coder turn): `npm test` 625/625
files green; `npm run test:properties` 411/412 files green, the one
failure being the pre-existing, already-owned standing red BL-1621
(`bl1297MergeOwnPathsInvariants.property.test.js`), unrelated to and
unaffected by anything in this parcel. Re-running either would burn
several more minutes to reconfirm a result nothing here can change — the
exact waste the 2026-09-17 "test lanes run for hours" directive targets.
This parcel's own verification is its qa_e2e_procedure's own scope: the
shell test, the acceptance feature, the diff check, and (added by BL-654)
the property test.

## Runs

- `bash swarmforge/scripts/test/test_branch_claim_guard.sh` → exit 0, 15
  PASS lines, no FAIL (qa_e2e step 1).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1613-*.feature`
  → 3/3 pass (qa_e2e step 2).
- `git diff b2e8f205d9..HEAD -- swarmforge/scripts/test/test_branch_claim_guard.sh | grep -E '^-' | grep -vE '^---'`
  (b2e8f205d9 is the parent commit, BL-1609's own last commit) → exactly
  one removed line, the identity printf (qa_e2e step 3).
- `bb swarmforge/scripts/test/bl1613_fixture_identity_completeness_property_runner.bb`
  → `ALL PROPERTIES HOLD`.

By coder.
