# BL-1498 — cleaner review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

Gates run:
- `bash swarmforge/scripts/test/test_handoffd_supervisor.sh` with the
  operator's real `~/.zshenv` in place, `RESEND_API_KEY` unset: `ALL PASS`,
  exit 0.
- `bb swarmforge/scripts/test/handoffd_supervisor_startup_grace_test_runner.bb`
  — `ALL TESTS PASS` (7 assertions), unchanged.
- `git diff --stat main -- swarmforge/scripts/handoffd_supervisor.bb` —
  empty.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1498-…feature` — see defect found and fixed below.
- `node extension/out/tools/mutation-site-count.js
  specs/pipeline/steps/bl1498SupervisorTestIsolatesOperatorShellSteps.js`
  — 247 sites, `over` the 100 advisory threshold. Left whole: it is one
  self-contained step handler for BL-1498's single feature file (staging,
  fixture setup, pid-aging, shell-invocation logic all serve the same three
  scenarios), and the sibling `bl977SupervisorProgressSteps.js` — the shape
  this file was explicitly modeled on — sits at 286 sites under the same
  one-file-per-feature convention used throughout `specs/pipeline/steps/`.
  A split here would fragment closely-related fixture logic, not improve
  structure (BL-485's "legitimately-cohesive large module... stays whole").

Defect found and fixed in this pass (own-stage fix, not a bounce):
- `runGeneratedTests` (`specs/pipeline/runnerAdapter.js`) reported the
  BL-1498 acceptance run as a FAILURE (`timedOut: true`, `success: false`)
  even though every one of its 4 subtests printed `ok`. Cause: scenario
  02's outline example 1 ("younger") deliberately leaves its placeholder
  daemon pid alive, and the step handler's `spawn('sleep', ['300'], {
  detached: false, ... })` for that pid was never `unref()`'d and was only
  ever reaped from a `process.on('exit', cleanup)` handler — an un-awaited
  live child keeps Node's event loop non-empty, so `'exit'` never fires
  naturally and the whole `node --test` process hangs until
  `runGeneratedTests`'s own 300s outer ceiling `SIGKILL`s it. Every
  scenario "passes" but the acceptance gate as a whole still reports
  failure and burns 5 minutes doing it.
  - Fix: reap tracked pids from a `node:test` `afterEach(cleanup)` instead
    of `process.on('exit', ...)` — the exact shape the sibling
    `bl977SupervisorProgressSteps.js` (this file's own stated model) already
    uses for the identical live-placeholder-pid fixture, and which does not
    hang.
  - Verified: reran `run_acceptance.sh` on the BL-1498 feature after the
    fix — full TAP summary now prints (`# tests 4`, `# pass 4`, `# fail 0`),
    `SCRIPT_EXIT:0`, total run ~47s (was: hanging to the 300s ceiling and
    reporting `timedOut: true`). Confirmed twice.
  - Committed separately: 6d54f1708a ("BL-1498: reap the scenario 02
    placeholder pid via afterEach, not process exit").

No other defect found. Architecture/DRY/CRAP: no production code touched
(`handoffd_supervisor.bb` untouched, per constraint); the shell fixture and
step-handler changes are test-only, matching the ticket's stated scope.
