# BL-1356 — cleaner pass (2026-09-03)

## Scope

Diff covers the six stamp-off files (bl1113, bl1115, bl1116, bl1117, bl1136;
bl1323 already fixed out-of-band per the ticket's notes), the new shared
helper `extension/test/helpers/stampOff.js`, its own test/property coverage,
the acceptance step handler, and the allowlist row removals. Also includes
the coder's own self-audit fix (a stale header comment claiming bare-`node`
runnability that wrapping the bl1116 assertions in `test()` made false).

## Ruling followed

`human_ruling` on the ticket: option 1 (keep invariants on the live ledger,
assert non-mutation across the run rather than a state literal). Confirmed
`ledgerPath` is only injected in the helper's own tests, proving the gate
still bites without writing to the live ledger; every real caller uses the
default `LIVE_LEDGER`.

## Invariants checked against the diff

- **Invariant 1** (fails when, and only when, the run changes the watched
  row): `assertRunWritesNoDecision` captures the row before `work()` runs and
  diffs it after — no starting state is asserted as a literal, only equality
  before/after.
- **Invariant 2** (non-weakening: a real write still fails from any starting
  state): `decisionMarkers` treats "decided" as "present and not null" per
  field rather than a fixed list of verdict words, so a new verdict word
  can't slip past unlisted. `bl1356StampOffHelper.test.js` (17/17) drives the
  helper directly against generated ledgers from both an undecided AND an
  already-decided starting row, and confirms a write of any decision field
  still fails from either. `bl1356StampOffInvariants.property.test.js` (2/2)
  crosses every starting state with every write kind by construction.

## Verification (run, not assumed)

- `npx vitest run test/bl1356StampOffHelper.test.js` — 17/17.
- `npx vitest run --config vitest.properties.config.mjs
  bl1356StampOffInvariants` — 2/2.
- `npx vitest run --config vitest.properties.config.mjs
  bl1113CursorHotfixStampOff bl1115MainSyncStatusCliStampOff
  bl1116ExtensionWipHotfixStampOff bl1117PipelineBoardNumericNbspStampOff
  bl1136BabysitterdCursorForgeStampOff bl1323StampOffInvariants` — all 6
  files green (12 tests), confirming the whole family, not just the new
  helper's own tests.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1356-...feature` — 6/6.
- `property_suite_standing_allowlist.tsv` — confirmed bl1113/1115/1116/1117/
  1136 rows are gone (bl1323 was never in it, per the ticket's own note).
- `git status --short backlog/hotfix-ledger.yaml` — clean; the runs left it
  byte-identical (qa_e2e step 5).
- `specs/pipeline/steps/index.js` — `bl1356StampOffWatchesTheRunSteps`
  registered (required_wiring satisfied).

## Structure / DRY

`extension/test/helpers/stampOff.js` is the single shared place all six
files now ask the same three questions (`assertRunWritesNoDecision`,
`assertParcelDoesNotEditReviewedSources`, `findTicketYaml`) — exactly the
"one shared helper, not six parallel edits" the ticket's own "How" section
asked for. Spot-checked bl1116 (module-`test()` wrap) and bl1136 (blob-scope
fix + ENOENT-safe ticket lookup + fast-check reach-by-construction) against
the helper and the ticket's reasoning — both consistent, no drift between
the docstrings' claims and what the code does.

No production `extension/src/**` file is touched by this diff, so the
mutation-site-count gate (BL-485) does not apply — this is entirely test/
step-handler surface.

## Verdict

No defect found. Forwarding.
