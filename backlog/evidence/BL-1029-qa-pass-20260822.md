# BL-1029 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-1029 — respawn launch argument survives a quoted path
- **Parcel**: `00_20260822T223211Z_000479_from_documenter_to_QA_for_QA` (from
  documenter, commit `f7a7806da7`)
- **Reviewed by**: QA, 2026-08-22
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-1029`
  → exit 0, `VERIFY BL-1029`. No open deferral.

## Verdict

**PASS — inventory items: NONE.**

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry | PASS — coder/cleaner/architect/hardener/documenter all confirmed ancestors |
| G2 | Prior-bounce check against `main`/`origin/main` | PASS — none for BL-1029 |
| G3 | Compile | N/A — pure Babashka (`shell_quote_lib.bb` + call-site wiring), zero `.ts` files touched |
| G4 | `bb swarmforge/scripts/test/shell_quote_lib_test_runner.bb` | PASS — ALL TESTS PASSED |
| G5 | `bb swarmforge/scripts/test/bl1029_respawn_quoting_property_runner.bb` | PASS — ALL 200 PATHS PASSED (`{:plain 39 :apostrophe 56 :enumerated-sites 1 :routed-file 5}`), matches coder's/architect's recorded runs |
| G6 | Acceptance (`run_acceptance.sh specs/features/BL-1029-respawn-launch-argument-survives-a-quoted-path.feature`) | PASS — 5/5 |
| G7 | BL-113 Gherkin mutation | PASS — 3/3 survived, each individually and correctly recorded EQUIVALENT (BL-234 shape): all three are case/character mutations of an illustrative launch-path Example value against a symmetric round-trip property (`unquote(quote(X)) == X` holds for any X) — genuinely undetectable by construction, not a deferred gap. Contrast with the D1 I bounced on BL-1069 earlier this session, where survivors were left unresolved with NO equivalence argument — this is the correct way to close a survivor without a code change. |
| G8 | required_wiring | N/A by design — ticket deliberately declares none (BL-531/BL-874 reasoning in its own notes); scenario 02's tree-enumeration is the wiring gate instead, independently confirmed below |
| G9 | Seven-site sweep is real, not merely claimed | PASS — `grep -rn "zsh '" swarmforge/scripts/ --include="*.bb" | grep -v /test/` → only one prose comment line in `shell_quote_lib.bb`'s own header; independently traced all 7 originally-named sites: `handoffd.bb` (2), `handoff_lib.bb` (1), `remote_control_health_lib.bb` (1) and `single_role_repair_lib.bb`'s own BL-1018 site (1) call `shell-quote-lib/launch-command` directly; `swarm_ensure.bb`'s and `babysitter_check.bb`'s sites were already consolidated onto the shared `single_role_repair_lib.bb` helper by BL-1018 landing on `main` between this ticket's mint and this parcel (matches the documenter's own changelog entry exactly, independently confirmed by reading both files directly, not merely the docs) |
| G10 | Docs currency | PASS — `docs/reference/Specification.MD`'s BL-1029 entry accurately explains the 7-named-sites-but-5-actually-converted-here discrepancy |
| G11 | Functional change present | PASS — new `shell_quote_lib.bb`, five call sites converted |
| G12 | Orphaned processes | PASS — `pgrep -fl 'node --test\|stryker'` shows a live Stryker run in `.worktrees/hardender` (`lineDiff.js`, matching my own BL-1015 bounce's D2 remediation, correctly parented under `detach_job`) — not orphaned, another role's legitimate live work |
| G13 | Babashka tooling-fallback correctly recorded | PASS |

## `qa_e2e_procedure` — every step addressed

1. Scenario 01 constructs the argument through the real code and evaluates
   it in a real shell — confirmed by reading the step handler, not merely
   trusted; re-run green (5/5 above).
2-3. Non-vacuity (pre-fix apostrophe row goes red; renamed-helper enumeration
   catches unrouted sites) — independently re-proven by the architect
   (restored pre-fix construction, reran the property runner live: 108
   failures, all apostrophe-bearing/exit-2 as expected; restored, clean
   200/200). Not repeated a third time here given it was already proven at
   authoring time and independently re-verified once.
4. Tree grep for `zsh '` — done above, clean.
5. Live sanity check on a real (apostrophe-free) host — this host's install
   path (`/home/carillon/swarmforgevc`) carries no apostrophe either, so the
   common case is unaffected by construction (invariant 2 does not change
   behavior for a path with nothing to escape); did not additionally kill a
   live role pane to force a real repair, since doing so on this session's
   own active swarm would disrupt other roles' live panes for no additional
   verification value beyond what G4/G5/G9 above already prove against the
   real code.

## Sibling content riding in this commit

Minimal — this commit's own diff against my prior tip is only
`docs/reference/Specification.MD`. BL-1075 (the remaining hardener-batch
sibling) has not yet reached QA this session.

Per "An Approval Authorizes Only Its Ticket's Work" (BL-506) and Article
2.6, **this approval names BL-1029 only.**

By QA.
