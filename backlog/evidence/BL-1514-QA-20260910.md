# BL-1514 — QA review pass, 2026-09-10

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Independent re-verification of the full qa_e2e_procedure, from `.worktrees/QA`:

1. `run_acceptance.sh specs/features/BL-368-control-loss-is-not-agent-death.feature`
   → `ok 1..3`, `# fail 0`.
2. `run_acceptance.sh specs/features/BL-359-always-on-operator-presence.feature`
   → `ok 1..6`, `# fail 0`.
3. `specs/features/BL-647-rotation-router-liveness.feature` and
   `specs/pipeline/steps/bl647RotationRouterLivenessSteps.js`: both absent.
   `grep -rln 'dead-agent liveness sweep runs' specs/features`: empty.
4. `grep -rn dead-agent-events swarmforge/scripts specs/pipeline`: only dated
   comments in `test_operator_runtime_control_lost.sh` and
   `test_operator_runtime_bl647_rotation_liveness.sh`.
5. `test_operator_runtime_control_lost.sh`,
   `test_operator_runtime_bl647_rotation_liveness.sh`,
   `operator_lib_test_runner.bb`: ALL CHECKS PASSED / ALL TESTS PASSED.
6. `grep -n dead-agent-events swarmforge/scripts/operator_lib.bb
   swarmforge/scripts/operator_runtime.bb`: no hits (function and both call
   sites gone). `grep -n "rotation-mode|rotation-opts"
   swarmforge/scripts/operator_runtime.bb`: only the unrelated `prev-conf`
   site at line 1381.
7. Both surviving features' mutation-stamp headers show `tested_at`
   2026-09-10 (after mint), confirming the hardener re-stamp.
8. `backlog/standing-reds.tsv` carries no row for BL-368, BL-359, or
   BL-647's feature paths.
9. `cd extension && npm run compile`: clean. `npx vitest run --config
   vitest.properties.config.mjs test/bl1514DeadAgentEventsRetiredInvariants.property.test.js`:
   4/4 passed.
10. Full unit suite (`npm test`, extension/): 614/615 files, 10434/10435
    tests pass. The one failure, `tempDirTrapGuard.test.js` (flags
    `landed_ticket_autoclose_test_runner.bb` and
    `wake_dedup_lib_test_runner.bb` for a missing temp-dir cleanup hook),
    is pre-existing, unrelated debt already owned by
    `backlog/paused/BL-1501-the-two-hotfix-runners-sweep-the-temp-roots-they-create.yaml`
    and tracked in `backlog/standing-reds.tsv` — confirmed by grep before
    treating as a defect (BL-1063 presumption rule).
11. Property suite (`npm run test:properties`, extension/): run three times
    in isolation (no concurrent heavy suite), with non-deterministic
    failure sets each time — none touching `operator_lib.bb`,
    `dead-agent-events`, or the three retired feature files. Two of the
    observed failures are pre-existing, ticketed standing reds unrelated
    to this parcel: `bl1089FrontDeskLivenessFixture.property.test.js`
    (`backlog/paused/BL-1502-...yaml`, in `standing-reds.tsv`) and
    `bl1313BatchGuardVisibilityInvariants.property.test.js`
    (`backlog/paused/BL-1503-...yaml`, in `standing-reds.tsv`). A third,
    `bl1373PathSetCacheInvariants.property.test.js`, appeared intermittently
    across runs (sometimes red, sometimes green) but passed cleanly 3/3
    when re-run in isolation, single-file, with no concurrent suite
    (`24.62s`, all invariants green) — consistent with resource-contention
    flakiness under concurrent heavy `bb`/fast-check load in this session,
    not a deterministic contract violation, and unrelated to BL-1514's
    changes. No new ticket minted for it: not reproducible in isolation, no
    evidence of a real defect.
12. No orphaned `node --test`/`stryker`/`vitest` processes before or after
    verification (`pgrep -fl`).
13. Wiring: `dead-agent-events` has zero callers outside `test/` per grep
    above; nothing else was expected to call it (`required_wiring: none`
    per ticket).

By QA.
