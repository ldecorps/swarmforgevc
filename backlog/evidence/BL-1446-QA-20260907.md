# BL-1446 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect. One new, distinct,
out-of-scope finding recorded separately (see below).

By QA.

## Verification (this ticket's own deliverable)

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/task_scope_gate_lib_test_runner.bb`: ALL PASS
  (untouched file, no regression).
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1446-the-land-walk-never-counts-landed-history-and-a-replay-carries-every-hop.feature`:
  5/5 pass.
- Sibling features named by qa_e2e_procedure step 1, confirmed independently
  green: BL-1432 (5/5), BL-1431 (4/4), BL-1389 (5/5).
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass.
- No file under `extension/src` or `extension/test` changed since this
  session's last full `npm test` pass (`git diff --name-only aa80d696ba
  HEAD -- extension/src extension/test` empty) — that result stands
  unchanged (609/610, one independently-owned red, BL-1460).

## Live proof, against this session's own real incidents rather than only the synthetic fixture (qa_e2e step 2's intent)

This session hit the exact bug BL-1446 fixes twice today, landing BL-1448
(evidence: `backlog/evidence/BL-1448-QA-20260907.md`). Re-ran the identical
real inputs after merging BL-1446 into this worktree:

- `bb land_step_cli.bb BL-1448 6c8caf27fb` — before BL-1446: `LAND_REPLAY`
  with a replay MISSING the actual step handler and four evidence files
  (BL-1424/BL-1446 shape). After BL-1446 (this pass): `LAND_CLEAN
  6c8caf27fb...` — the landed siblings BL-099/BL-1454/BL-1460 (all
  reachable from `origin-main`) are now correctly excluded from candidacy
  outright, matching invariant 1.
- `bb land_step_cli.bb BL-1448 d854af2126` (the second incident, a
  passenger-sibling leak on an evidence-only commit): also now
  `LAND_CLEAN`.

Both confirm invariant 1 (landed history never manufactures a candidate)
directly against real, previously-broken inputs, not only the fixture's
synthetic five-stage-commit rebuild.

## Architect/hardener's own recorded (non-blocking) finding — independently re-checked

`land-plan`'s own `candidates`/`lines-of` binding still calls the 3-arg
`ancestry-commits` (no explicit `origin-main` exclusion argument), unlike
`entangled-siblings`' internal walk which takes the 4-arg form. Both
reviewers traced this as harmless by construction (any extra line from an
origin-main-reachable commit can only help satisfy the `:landed`
`every?` check, never manufacture a false one) and confirmed it empirically
against acceptance scenario 03's three sync-count Examples. Re-ran that
scenario myself (3/3 pass, above) — agree: not a defect, a textual
consistency nit worth a coder follow-up, not its own ticket.

## New finding, NOT part of this ticket's scope — flagged separately

Verifying BL-1446 surfaced a distinct, unticketed gap: `land-plan`'s own
CANDIDATE walk (bounded by `parcel-own-base`, which BL-1446 deliberately
leaves unchanged) can miss a genuinely unlanded sibling whose entangling
commits were absorbed into the parcel's shared batch-role branch BEFORE
the parcel's own last hop — `LAND_CLEAN` is then reported for a tip that
is not actually clean. Reproduced directly against this session's own
BL-1448 land inputs (both now `LAND_CLEAN` per the section above, while
independently confirmed to carry BL-1349's genuinely unlanded
`bounce_history` as an ancestor `entangled-siblings` finds when called
with the wide, unbounded walk). Full detail, root cause and recommendation:
`backlog/evidence/BL-1446-QA-followup-landclean-can-miss-preexisting-entanglement-20260907.md`,
sent to the specifier as a `note` (not a bounce — outside BL-1446's own
stated scope, and BL-1446 fully delivers what it claims).

## Disposition

Approved and landed (`cb1999183e`, `e5bff442b3` its content commit) via a
verified-complete tip-pure replay — every one of this ticket's 12 own
paths (derived from the full `diff --name-only origin/main <cited>`, not
commit-subject grep alone, after the BL-1448 near-miss taught that lesson)
confirmed byte-identical between the cited commit and the replay before
landing; BL-1349/BL-940's paths confirmed correctly excluded, matching
`origin/main` exactly. No rework needed on this ticket's own work.

By QA.
