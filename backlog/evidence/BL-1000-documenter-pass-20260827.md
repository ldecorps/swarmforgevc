# BL-1000 — documenter pass — 20260827

## Inbound

Hardender tip `952ad6c8a0`. Merge on `swarmforge-documenter` (untracked active
yaml blocking merge removed; ticket promoted paused→active upstream).

## Living docs

How-to already landed (Aug 24); added worktree-safe fresh-checkout note for
acceptance scenario 03. Specification/index/architecture entries unchanged.

Acceptance + step handler landed upstream; ticket `acceptance:` and
`required_wiring` already set.

## Pre-QA

`specs/features/BL-1000-freshness-tests-read-a-pinned-fixture.feature`
(4/4 at hardener).

By documenter.
