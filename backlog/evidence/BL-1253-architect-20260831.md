# BL-1253 — architect pass, 2026-08-31

Task: `BL-1253-swarm-stamp-dead-feeder-owns-getupdates-2ec06b6ef1`
Received: cleaner's `a007baf9e5` (verdict NONE, forwarding coder's
`94301a6c72` — the QA-bounce rework, scenario 06 handler now registered).

Merge was clean this round (no unattributed-path trap).

## Verdict: CLEAN — forward to hardener

### Review-only ticket — scope check
Diff of `94301a6c72` touches exactly one file:
`backlog/evidence/BL-1253-coder-restamp-20260831.md`. No hotfix source
changed, no ledger row written — matches invariants 1 and 2 and the
ticket's own `constraints`/`out_of_scope`. Confirmed by reading the commit,
not by trusting the evidence's own claim.

### Architecture rules
N/A — no production code in this diff to review for the two-layer
boundary, I/O ownership, or webview storage.

### Hard gate — dependency-rule checker / co-change tool
N/A — no file under `extension/src` or elsewhere changed; nothing to run
either tool against.

### required_wiring
`specs/pipeline/steps/index.js::bl1253DeadFeederOwnsGetUpdatesStampSteps`
— confirmed registered at `index.js:899`.

### Checks re-run independently (not taken on trust)
| Command | Result |
|---|---|
| `run_acceptance.sh specs/features/BL-1253-….feature` | 8/8 pass (QA-bounced scenario 06 now handled) |
| `npx vitest run --config vitest.properties.config.mjs test/bl1253TokenOwnershipInvariants.property.test.js` | 4/4 pass |
| `grep -A5 2ec06b6ef1 backlog/hotfix-ledger.yaml` | `state: stamp-open`, `human_decision: null` — untouched |

### Spot-check of the stamp itself
Independently re-read `cursorBridgeInboundQueue.ts` at commit `2ec06b6ef1`
(not a later tip): `frontDeskPollHeartbeatPath` / `readFrontDeskPollHeartbeatMs`
are both present and exported, and the reader returns `null` on a missing
file, a JSON parse failure, or a non-finite `lastHeartbeatMs` (three
distinct routes to the same `null`) — matches the coder's qa_e2e_procedure
step (1)/(1b) verdict exactly. Did not re-walk every remaining step by
hand; the coder's table cites concrete line numbers and passing test
counts I could re-run (and did, above), which is sufficient corroboration
for a review-only stamp.

### Invariants review (BL-633/654)
- Invariant 1 (never reimplements): not independently executable — correctly
  stated as a diff-scope property, verified above by reading the commit.
- Invariant 2 (green tests never certify): carried by acceptance scenario
  "The ledger row stays pending until a human decides" — reran, passes.
- Invariant 3 (token ownership, carried from retired BL-1260): carried by
  `bl1253TokenOwnershipInvariants.property.test.js` — reran, 4/4 pass.

### Outstanding human decision — carried forward, not mine to resolve
The 90-second stall-window question (`DEFAULT_FRONT_DESK_FEEDER_STALL_MS`,
`telegramCursorBridgeCore.ts:371`) remains unanswered per the ticket's own
`approval_context`. This parcel does not and cannot close it — flagging
forward exactly as the coder's evidence does, so whoever takes the ledger
decision does not miss it.

No defect found, no send-back. Forwarding to hardener with the same task
name, commit (the merge I received).

By architect.
