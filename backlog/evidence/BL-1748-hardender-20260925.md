# BL-1748 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

A single stray `require('./lib/socketFixtureRoot')` line inside
`OPERATOR_DECIDE_STUB`'s source-text template (never a real top-of-file
require — the fixture writes this text to a standalone
`<root>/extension/out/tools/operator-decide.js` with no `./lib/`
directory beside it). Removed; the stub now loads only `fs` and `path`,
matching what it actually uses.

Checked for the same copy-paste error recurring elsewhere: searched
every `specs/pipeline/steps/*.js` for this exact 3-line stray sequence
(`const fs = require('fs');` / `const path = require('path');` /
`const { mkSocketFixtureRoot } = require('./lib/socketFixtureRoot');`)
— no other match. The many other `require('./lib/socketFixtureRoot')`
hits across the tree are all legitimate top-of-file requires for real
step-handler files that genuinely use the helper (BL-948's own
population), not stray copies inside an embedded stub string.

No new regression test needed or added: the pre-existing BL-325
acceptance scenarios 2 and 6 already exercise this exact stub (they
were red specifically because of this bug, per the ticket's own
description) and now pass — the acceptance feature itself is the
regression guard, and the ticket's own FIRM constraint says no
scenario/Examples/narrative line changes.

No TS/JS production file in scope — a step-handler fixture only.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-325-human-in-the-loop-closed.feature`:
  7/7 (was 5/7 before this fix, per the coder's own evidence).
- `npm test` (extension/): 638 files / 10875 tests — 3 failures, all
  in the already-ticketed, unrelated `nightClosingCeremonyRun.test.js`
  (BL-1764); every other file green.
- `git status --short` clean — no hardening changes were needed.

By hardender.
