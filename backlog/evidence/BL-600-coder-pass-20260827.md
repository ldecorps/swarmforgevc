# BL-600 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Delivered

Pure `humanDecisionLatency.ts` aggregator pairing approval ask and verdict
timestamps per gate: decided latency, pending open age, daily window median +
outlier split via `stageDwell.splitOutliers`, and `trend.ts` re-export for
the shared TrendedNumber framework.

## Invariants (BL-654)

| invariant | encoding |
|---|---|
| Pending asks → open age only, never fabricated completion | property P1 + acceptance scenario 03 |
| Pure aggregation over paired events | property P2/P3 + acceptance scenario 02 (no fs in module) |
| Measurement only — no nudge/block side effects | module has no Telegram or approval side calls |

## required_wiring

| anchor | status |
|---|---|
| `staleApprovalEscalation.ts::approvalAskPostedAtMs` | existing ask timestamp source (not re-wired; pure layer accepts paired ms) |
| `telegram-front-desk-bot.ts::readApprovalAskMessages` | existing store (acceptance uses fixture pairs) |
| `stageDwell.ts::splitOutliers` | imported in aggregator |
| `trend.ts::decision-latency series` | `trendForDecisionLatencyMedian` re-export |
| `bl600TrendHumanDecisionLatencySteps` | registered in `index.js` |

## Verification

| check | result |
|---|---|
| `humanDecisionLatency.test.js` | 5/5 |
| `humanDecisionLatency.property.test.js` | 3/3 |
| `run_acceptance.sh BL-600…feature` | **7/7** |

By coder.
