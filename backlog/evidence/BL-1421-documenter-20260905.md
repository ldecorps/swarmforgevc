# BL-1421 documenter review pass — 2026-09-05

Reviewed the hardened commit (`d15499b762`) and the coder/cleaner/architect/
hardener trail for BL-1421 (one standing surfacing per role).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1421-one-standing-surfacing-per-role.feature`.
- `required_wiring` step handler exists:
  `specs/pipeline/steps/bl1421OneStandingSurfacingSteps.js`; the
  `handoffd.bb::caught-up-to-told?` fact supplier required by the first
  wiring line is present (`swarmforge/scripts/handoffd.bb`, wired as the
  `post-qa-branch-sweep-lib/sweep!` `:caught-up-to-told?` adapter).
- Operator/agent-visible behavior change (a role's standing surfacing now
  persists across landed shas until its own HEAD catches up to the sha it
  was told about, instead of resetting — and silently re-telling — on
  every newly-landed commit; `decide-role` now checks in-process work
  before dirtiness, so a mid-parcel role's own dirty tree is never
  misread as the one reason that wakes it) is documented in
  `docs/how-to/BL-668-post-qa-deterministic-branch-sweep.md` — the
  existing how-to for this sweep (BL-668/BL-1361) — updating the
  precedence table, the state-persistence section, the "What triggers
  it" and "Wake vs. defer" bullets, the Verify commands, and Siblings.
- `docs/index.md`'s one-line summary for that how-to updated to mention
  BL-1421.
- No "Last Updated" freshness field in either edited doc — no date bump
  owed.
- Checked `docs/diagrams/swarm-flow.mmd` for staleness: its comment
  annotations describe the sweep only at the level of "dirty, in_process,
  or divergent roles are surfaced" — general enough that BL-1421's
  precedence/persistence fix doesn't contradict it; no topology, boundary,
  or pipeline-flow change here (a bugfix inside an existing sweep's
  decision logic) — no diagram change needed per the documenter's own
  change-trigger.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
