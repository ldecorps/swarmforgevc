# BL-1386 — documenter pass (2026-09-04)

## Received
`9224693e01` (hardener, degraded Babashka fallback recorded — no
mutation/CRAP/DRY wired for `.bb`/shell, gated on
`master_main_reconcile_lib_test_runner.bb` + its property runner +
`test_handoffd_master_main_reconcile_wiring.sh`, all green per
`backlog/evidence/BL-1386-hardener-pass-20260904.md`).

## What changed, user/operator-visible
An internal daemon reliability fix, not an extension command or UI
surface, but it changes an operator-facing OUTCOME two existing how-to
pages already document in detail: `master-main-reconcile-sweep` (BL-891)
and its BL-1120 "never abort a foreign merge" rule. Both pages described
"abort only when THIS tick started the merge" and a fixed `conflict` log
label — both now inaccurate: the daemon can recognize (and abort) its own
merge across ticks via a durable ownership record, a failed abort is
retried and logged with git's real error text, and the `conflict` label is
reserved for an actual git-reported conflict.

## Doc-domain review
- `docs/how-to/BL-1120-handoffd-must-not-abort-foreign-merge.md`: added an
  "Ownership across ticks (BL-1386)" section describing the ownership
  record, the retry/backoff, and the unchanged (never widened) protection
  for a genuinely foreign `MERGE_HEAD`.
- `docs/how-to/BL-891-master-main-reconcile-sweep.md`: rewrote step 5's
  BL-1120 clause in place, added two verdict-table rows (daemon's own
  merge aborted by ownership; abort failed), and corrected the
  `merge conflict / refuse-rematch` row's log-label claim.
- `docs/reference/Specification.MD`: new top-of-stack entry (today's
  newest).
- `docs/index.md`: refreshed the BL-891 and BL-1120 summary lines.
- Diagram registry checked: `architecture.mmd`'s `daemon/` node is already
  generic ("handoffd pid, heartbeat, log") and its master-main-reconcile
  edge label is already a high-level summary (BL-891/BL-1113) that names
  no per-file mechanism this deep — no diagram depicts reconcile-sweep
  internals at the ownership-record granularity, matching the same finding
  every other land/reconcile-internals ticket this session recorded.

## Verdict
Doc content corrected in both owning how-to pages (not NONE), plus the
standard Specification.MD/index.md entries. Forwarding to QA.
