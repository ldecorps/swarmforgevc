# BL-1306 — QA approval (20260903)

Received: documenter commit `6e1154b2b9` (doc-only, Specification.MD entry),
forwarding hardener (equivalent-mutant investigation, sibling regression
check), architect, cleaner, coder's key-basis-unification fix in
`swarm_handoff.bb`. Merged into QA.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/bl1306_audit_reroute_test_runner.bb`: ALL
  PASS (7 assertions).
- Acceptance (`specs/features/BL-1306-handoff-audit-reroute.feature`):
  4/4, including scenario 03 (an edited draft still re-challenges).
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1306HandoffAuditRerouteSteps` (line 22) — confirmed.
- Declared invariant
  (`extension/test/bl1306AuditKeyBasisInvariants.property.test.js`): 2/2.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated the call site (`swarm_handoff.bb` ~line 1120) back to the
  pre-fix basis (raw `:recipients`/`commit:` header instead of the routed
  values) — 2 of 7 bb-suite assertions failed, including the exact
  rerouted-second-invocation case the ticket describes. Restored (`git
  diff --stat` empty), re-ran ALL PASS.
- **Hardener's equivalent-mutant reasoning independently re-checked**:
  confirmed `:version 1` is a hardcoded literal at its only construction
  site (`audit-candidate`), so no fixture in this ticket's scope can make
  it disagree between the stored and looked-up sides — the survivor is a
  genuine BL-234-class equivalent, not a coverage gap.
- **Sibling regression check re-run independently** (the coder's
  concurrency-safe fixture-sweep fix touched three OTHER acceptance step
  handlers from earlier today): BL-1323 7/7, BL-1332 6/6, BL-1343 6/6 —
  all still green.
- Workaround retirement confirmed: `grep -n BL-1306
  swarmforge/handoff-protocol.md swarmforge/roles/hardender.prompt` — no
  hits, the stale-premise text is gone.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical to the
  standing-debt population from every prior pass this session. None touch
  `swarm_handoff.bb`, the audit key basis, or this parcel's files.
- Property suite (`npm run test:properties`): 25 files / 14 tests red —
  the same standing-debt population plus
  `test/bl1295RevertAttributionInvariants.property.test.js` invariant 2,
  which the coder's own evidence explicitly disclosed as a load flake
  encountered while committing this same parcel (green in isolation,
  reds only under full concurrent suite load) — consistent with the
  pattern already documented multiple times this session for different
  files under the same load. BL-1306's own property test — 2/2 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry confirmed present and accurate. No diagram change-trigger fired.

## Note: a suite-manifest.tsv gap surfaced from my own earlier BL-1317 land

This parcel's diff to `swarmforge/scripts/test/suite-manifest.tsv` (adding
`bl1306_audit_reroute_test_runner.bb`) also carries a line
(`test_bl1317_effort_ladder_parity.sh`) that belongs to BL-1317's own
work, landed by me earlier today — I omitted this file from BL-1317's
hand-built own-paths list at the time. Both lines are legitimate and will
land together as part of this parcel's tip-pure replay; not a BL-1306
defect, and no action needed on BL-1317 (already closed) beyond carrying
the line forward here.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
