# BL-1480 — QA review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Bounce history checked against `main`/`origin/main` (identical, 0/0
  divergence): no prior bounce for BL-1480. `qa-sibling-check.js status`:
  `VERIFY BL-1480` (no open deferral).
- Merged documenter commit `b34dba24fc` into QA (fast-forward, clean).
- Ancestry (BL-336 discipline): `git merge-base --is-ancestor` confirmed
  both the hardener merge (`b5a195575d`) and the coder fix (`63c9d39179`)
  are ancestors of the citied commit `b34dba24fc`; full BL-1480 commit
  chain (mint → approve → promote → coder → cleaner → architect →
  hardener → documenter) present in `git log b34dba24fc`.
- `required_wiring` confirmed live, not just present: both fixture
  basenames are `bbFixtureClosureGate.js` `FIXTURES` entries with
  `kind: 'shell-copy'`; `bl1480PromoteRouteFixtureClosureSteps.js`'s
  `registerSteps` is required directly by the BL-1480 generated test
  (`specs/pipeline/generated/bl-1480-...generated.test.js`), proven live
  by the acceptance run itself (8/8 passing).
- Standing fixtures: `test_promote_and_route_next_priority.sh` and
  `test_promote_and_route_next_no_limit_depth.sh` exit 0 from the QA
  worktree with `PACK_STAFFING_SKIP_GATE` both set and unset (4/4 runs).
  Also run from the master checkout: fails there (exit 1/1/2/2) because
  `main` has not received this commit yet — the known BL-891 propagation
  gap (local `main` in the master checkout is not auto-synced by a QA
  land), not a defect of this parcel. `grep -c 'cp "\$SCRIPTS/.*\.bb"'` is
  0 on both fixed fixtures.
- Full unit suite (`npm test`, extension/): 10400 passed, 1 failed —
  `bridgeServer.test.js` "an idle /events connection receives a periodic
  keepalive comment frame" — already a tracked, owned standing red
  (`backlog/standing-reds.tsv`: owner BL-1460, paused), unrelated to
  BL-1480's diff. Not a new red, not bounced.
- Property suite (`npm run test:properties`): 1114/1120 passed on the
  full concurrent run; 6 failures across 5 files, all "Test timed out in
  20000ms" (`bl1272LandedSiblingInvariants`, `bl1297MergeOwnPathsInvariants`,
  `bl1308SiblingDetectorCoversReplay`, `bl1309LandDecideEntanglementInvariants`,
  `bl1343ReplayNeverDropsOwnPathInvariants`) — none touch any path in
  BL-1480's diff. Re-ran all 5 files in isolation: 13/13 passed cleanly
  (23s vs the full run's 212s), confirming resource-contention flakiness
  under the live swarm's concurrent load, not a regression. The one
  allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` error
  (BL-871) also appeared (4x), consistent with the same contention.
- Acceptance (`run_acceptance.sh`): BL-1480's own feature 8/8 (all five
  scenarios, including scenario 05 added during hardening — the ticket's
  own qa_e2e_procedure predates that addition and says "four", not a
  defect); BL-973's feature 13/13 (no regression); BL-1279's feature
  10/10 (no regression).
- Constraint check: `promote_and_route_next.sh`, `promotion_gates_cli.bb`
  and the libs are untouched (`git diff --stat` on those three paths
  empty); `suite-manifest.tsv` rows for both fixtures remain `standing`;
  the new `deprecate_check_allow_stub.sh` is test-infrastructure only,
  documented, and within the coder's judged scope (reviewed and agreed
  by architect).
- Documenter: `docs/how-to/BL-973-bb-fixture-closure-guards-and-suite-inventory.md`
  gains both fixtures in its enrolled list. No registered diagram
  depicts this mechanism.
- `backlog/standing-reds.tsv` carries both BL-1480 rows (lines 35-36),
  to be removed at land per the ticket's own instruction.
- No orphaned test/mutation processes before or after (`pgrep -fl
  'node --test|stryker|vitest'` clean).
- Verdict: NONE outstanding. Approved for landing on documenter commit
  `b34dba24fc`.
