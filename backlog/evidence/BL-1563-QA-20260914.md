# BL-1563 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

1. `git diff main...HEAD --name-only` — only evidence, feature, step handler
   (+ lib/), all under specs/pipeline/steps/lib/. Confirmed the 8 hotfix
   production/doc files (mono_router_lib.bb, mono_router_rows_lib.bb,
   mono_router_rows_cli.bb, ready_for_next_task.bb, ready_for_next_batch.bb,
   ready_for_next.sh, swarmforge.conf, handoff-protocol.md) show zero diff
   vs the received commit (invariant 1 holds).
2. `run_acceptance.sh specs/features/BL-1563-*.feature` — 16/16 subtests
   (6 scenarios: scenario 01 = 9 rows including recipient-is-home; scenario
   05 = 3 rows) pass, commit cd8abdf899.
3. `bash swarmforge/scripts/test/test_ready_for_next_rotate_home.sh` — ALL
   CHECKS PASSED, cases 16-20 present by name.
4. `bash swarmforge/scripts/test/test_handoffd_priority_rotate_wiring.sh` —
   ALL PASS; source carries all 4 "mono_router_rows_cli.bb drifted from
   handoffd" fail-message literals (A-D).
5. `bb swarmforge/scripts/test/mono_router_lib_test_runner.bb` — exit 0.
6. `bash swarmforge/scripts/pre_qa_gate.sh BL-1563-swarm-stamp-router-target-0b727b286c cd8abdf899` — OK (required_wiring present).
7. `bb swarmforge/scripts/mono_router_rows_cli.bb .` — prints `QA`, exit 0.
8. `backlog/hotfix-ledger.yaml` row 0b727b286c: `human_decision: null`,
   `state: pending` — untouched by this parcel, confirming invariant/goal-6
   posture (the parcel does not decide certify/waive).
9. Full unit suite (`npm test`, extension/): 615/616 files, 10478/10479
   tests pass. Sole failure `test/telegramCursorOperatorExec.test.js`
   (`ENOTEMPTY` on `sweepPendingTmpDirs`/tmp/bl1204-*) — this parcel's diff
   never touches that file or its fixtures. Isolated re-run: 24/24 clean.
   Same signature already recorded as a full-suite-concurrency flake in
   BL-1444/BL-1498/BL-1501/BL-1526/BL-1538 QA evidence — not a new red, no
   action here.
10. `npm run test:properties`: 403/404 files, 1189/1190 tests pass. Sole
    failure `test/bl1272LandedSiblingInvariants.property.test.js` (timeout
    spawning one bb process per draw) — already registered in
    `backlog/standing-reds.tsv` as owner BL-1556 (timeout class, latent
    since BL-1272), pre-existing, unrelated to this parcel's diff (which
    touches no land_step_lib.bb / land-sibling code). No unowned-red note
    needed. Two `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled
    errors are the BL-871 allowlisted benign noise.

Review goals (BL-1563's own scope): goals 1-3 (resolver, dispatchers, drift
guard) confirmed via the real acceptance scenarios and shell/bb test
runners above — no refutation found. Goals 4-7 (recipient-is-home probe,
rotate-forward label, worktree coverage, timing) are recorded in the
coder's own evidence file (`backlog/evidence/BL-1563-coder-20260914.md`)
per the ticket's scenario notes — reviewed, nothing here contradicts those
findings.
