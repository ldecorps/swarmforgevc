# BL-1684 — hardender review pass, 2026-09-22

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: npx stryker run --mutate out/concierge/pipelineBoard.js:692-708,1186-1224 (dry-run timed out under host contention; deferred per office-hours bypass, hand-mutation used instead)
- **Commit hash**: 88600ff6bc
- **First error excerpt**: omittedLinkCount 1 !== 0 (||->&& mutant on D1 guard, caught after fixing vacuous fixture); html !== full (<= -> < boundary mutant, caught by new pinned unit test); truncateCaptionDescription off-by-one (<= -> < mutant, caught by existing exact-length unit test)
- **Failure class**: invariant-untested-boundary
- **Expected vs observed**: every hand-authored mutant on the touched functions killed by an existing or newly added test
- **Blamed role**: hardener (own pass, fixed in-pass)
- **Remediation pointer**: extracted composePipelineBoardWithoutLinks (CRAP/complexity regression fix); fixed vacuous D1 property fixture; added exact-boundary and fallthrough-sub-branch unit tests

By hardender.

## Detail

Gates run:
- `npx vitest run test/pipelineBoard*.test.js`: 217/217 pass (144 in
  pipelineBoard.test.js, +3 new).
- `npx vitest run --config vitest.properties.config.mjs test/bl956PipelineBoardCaptionCapInvariants.property.test.js`:
  5/5 pass, seed-10 pinned example included.
- `node specs/pipeline/cli.js specs/features/BL-1684-...feature`: 2/2 ok.
- Differential complexity (workflow.prompt): `composePipelineBoardHtml`
  8 (main) -> 10 (post-D1-fix, CRAP-flagged) -> 8 (post-extraction,
  matches baseline). `truncateCaptionDescription` 2 -> 4, CRAP 4.00
  (under threshold, not flagged). No other touched function regressed.
- CRAP (`node scripts/crapReport.js src/concierge/pipelineBoard.ts`,
  via `--coverage.reportOnFailure=true` per the unrelated-standing-red
  rule): `composePipelineBoardHtml` 100% coverage, CRAP 8.00 (matches
  baseline complexity, grandfathered, not a regression);
  `composePipelineBoardWithoutLinks` 100% coverage, CRAP 2.00;
  `truncateCaptionDescription` 100% coverage, CRAP 4.00. 9 functions
  file-wide exceed CRAP 6, all pre-existing (none newly introduced or
  regressed by this parcel).
- DRY (`npx jscpd --config .jscpd.json src/concierge/pipelineBoard.ts`):
  3 pre-existing clones (lines 671-679/1423-1431,
  1228-1236/1439-1452, 1461-1472/1484-1495), none overlapping this
  parcel's changed functions (967-984, 1586-1650ish). No new
  duplication introduced.

Blocked: full-file Stryker mutation could not complete. Three
successive dry runs each hit a DIFFERENT already-ticketed
Stryker-sandbox-only standing red in the full unit suite
(`bl1652HandoffdRespawnReadingsWiring.test.js` / BL-1673,
`laneProcessLib.test.js` / BL-1673's own scope,
`activePoolFreshnessAudit.test.js` / this role's own documented
standing exclusion) - each excluded in turn via a scoped
`vitest.bl1684.stryker.config.mjs` + temporary `stryker.config.json`
pointer (reverted before commit, per the 2026-09-05 rule: never edit
the shared `vitest.config.mjs`). The fourth attempt (all three
excluded) then hit "Initial test run timed out!" at ~5 min under
confirmed host contention (uptime load avg 7-10 on 20 cores, plus a
concurrent worktree's own 12-worker `vitest run --config
vitest.properties.config.mjs`) - the documented Stryker-under-load
crash signature. Deferred per the office-hours mutation bypass;
substituted a hand-authored mutation sweep against the two touched
functions' compiled `out/` code (7 hand mutants: D1 guard `||`->`&&`,
main `<=`->`<` boundary x2 shapes, binary-search `<=`->`<`,
`omittedLinkCount` accounting via the same D1 mutant), each verified
to kill against the post-fix test suite and each restored before the
next. One (D1 `||`->`&&`) initially survived due to a vacuous property
fixture, fixed in-pass (see D1 above) and reverified caught.

No orphaned test/mutation processes left running (`pgrep -fl 'node
--test|stryker'` clean before handoff, confirmed after the last
background Stryker attempt was killed and reaped by pgid).

By hardender.
