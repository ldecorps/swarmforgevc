# BL-672 / BL-673 / BL-674 — architect PASS

- Tickets: `BL-672-epic-make-top-priority-button`,
  `BL-673-topic-make-top-priority-within-epic`,
  `BL-674-epic-drilldown-topic-reprioritize-ui`
- Reviewed commit: `4d5da139d2` (architect merge of cleaner `41e589b39f`,
  BL-672 rework `ac75f67ed`, onto the restored pre-bounce tree)
- Prior round: architect send-back #1 (`3bbe59652`) — BL-672's epic-level
  "Make top" button was client-side disabled on `disableUp` (index 0 over the
  epics-only screen list), but the route's own domination set is
  `readLiveBacklogItems` (paused+hold, epics AND topics) — a live topic or
  hold item outranking the leading epic made the route perform a real change
  while the button stayed greyed out. BL-673 and BL-674 were unreviewed at
  that point (parcels pointed at the same now-superseded tip, per Article 2.6).
- Verdict: **PASS, all three** — forwarded to the hardener.

## Branch-state note — revert-the-revert done before merging (BL-490/495 trap)

Per my own bounce evidence, the four commits (`0ada47494` cleanup,
`c54eeeaa2` BL-674, `98b501664` BL-673, `2af1e046b` BL-672) were reverted out
of this branch as one indivisible unit (BL-673 modifies the file BL-672
creates). On this pass I nearly re-merged the rework directly on top of the
post-revert tip — a clean, non-conflicting merge that would have silently
kept the reverts' removals for every hunk the coder's narrow rework didn't
itself touch (the classic revert-of-a-merge trap: git resolves a hunk by
whichever side changed it since the merge-base, and "unchanged since base"
loses to "reverted" with no conflict raised). Caught before committing:
aborted, ran `git revert --no-edit` on the four revert commits (LIFO order)
to restore BL-672/673/674/cleanup content, confirmed by file existence
(`makeTopPrioritySafety.ts`, the three property/bridge test files, the three
`.feature` files all present) *before* merging `41e589b39f`, then merged.
Post-merge diff against the pre-merge tip (`e5677771c`) is exactly the coder's
8-line rework and nothing else — verified, not assumed.

## Bounce #1 fix — verified against the original repro

`epicReorderUiHtml.ts:171`: the epic-level "Make top" button no longer carries
`disableUp`-derived `disabled` — matches BL-673's topic button pattern, which
already had no disable. The route's own `changed:false` + reason (displayed by
the shared `handleActionResponse`) is now the only refusal signal, for both
levels. New coverage added and passing: the leading epic's button stays
enabled and its tap reaches the route even when a live topic could outrank it.
Confirmed no other UI precondition site in the file has the same class of
mismatch (my bounce #1's full-parcel sweep table still holds; this rework
touched only the one flagged line).

## BL-673 — topic-level make-top, reviewed fresh this pass

- **No forked algorithm.** `computeMakeTopPriority` (shared with BL-672) is
  parameterized by `dominationSet`/`dominationLabel`; BL-673's route passes
  the target's own epic's live topics as the narrower peer set while
  dependency resolution stays against the full global live set
  (`sortedLiveItems`), matching approval_context #1 (cross-epic deps still
  bound/refuse). `walkToIndex` still walks over the full global sorted list,
  so the proven BL-572 adjacent-swap order-preservation applies unchanged —
  a within-epic move cannot silently reorder other epics' pairs.
- **Route** (`handleEpicReorderTopicMakeTopRoute`, `bridgeServer.ts:886`):
  control-auth gated, body-shape validated, target-not-found-or-wrong-epic is
  a 404 refusal (scenario 07: "a topic outside the named epic is refused
  without writes" — 9/9 acceptance passing), registered in `writeRoutes`.
  Same `applyMakeTopPriorityResult`/commit-integrity tail as BL-672 — no raw
  git from the bridge.
- **Declared invariants (BL-633/BL-654), reviewed as their own pass, not
  hand-verified without a test:** all three are encoded in
  `topicMakeTopPrioritySafety.property.test.js` (invariant 1 twice — same-epic
  and cross-epic dependency framing; invariant 2 explicitly asserts pairs in
  OTHER epics too; invariant 3 no-op/changed:false). Proven non-vacuous
  myself, not just trusted: commented out the shared core's
  `worseDeps.length > 0` refusal (same line BL-672's evidence already broke)
  and reran `topicMakeTopPrioritySafety.property.test.js` — invariant 1's
  property failed immediately (`expected A2 to rank AFTER its live dependency
  A4`), confirming the test bites. Restored, reran clean (5/5).
- Acceptance: 9/9 (`BL-673-topic-make-top-priority.feature`). `.feature.draft`
  materialized, step handlers wired in `specs/pipeline/steps/index.js` in the
  same parcel (BL-441/BL-233).

## BL-674 — drill-down UI, reviewed fresh this pass

- **Untestable-edge boundary respected.** The one bit of decision logic the
  drill-down needs (`hasLiveDependency`) lives in `bridgeServer.ts`
  (testable), not inline in the webview script — matches the ticket's own
  "keep new logic thin" instruction. `epicReorderUiHtml.ts`'s drill-down is
  presentation plus forwarded input only: renders `lastData.topics` (already
  server-sorted priority-asc/id-asc, filtered client-side by epic — pure
  presentation filtering, not new ordering logic), wires the topic-make-top
  button to BL-673's live route, displays refusal/bound reasons via the
  existing shared `handleActionResponse`/`reasonOrFallback` path (no forked
  reason-rendering), and back-navigation returns to tiles.
- **No declared invariants** (`invariants: []` in the ticket YAML) — no
  property-test obligation; this is a no-op per the Invariants Review rule.
- **Mini App pane header** (standing operator rule): `<header>` is outside
  `#content` and never touched by either `renderTiles` or `renderDrilldown` —
  only `#content` swaps. Explicitly covered by its own acceptance scenario
  ("the Mini App pane header is present on the drill-down screen").
- **Webview boundary:** no localStorage/sessionStorage; all state (`view`,
  `drilldownEpicId`, `lastData`) is in-memory in the page script, refetched
  fresh on drill-in (`refresh()`, not a load-time snapshot) per
  approval_context.
- Acceptance: 5/5 (`BL-674-epic-drilldown-ui.feature`), including the
  topic-make-top tap reaching the route and a changed:false reason rendering
  verbatim.

## What passed across all three (checked, not assumed)

- **Dependency-rule gate (REQUIRED, BL-259):** full-repo scan (paths straddle
  `extension/` and `specs/`) — **PASSED, no forbidden edges.**
- **Co-change tool (BL-255):** `bridgeServer.ts`, `epicReorderUiHtml.ts`,
  `makeTopPrioritySafety.ts`, `specs/pipeline/steps/index.js` — no co-changers
  reported at the default threshold; no logical coupling flagged.
- **`required_wiring` entries, all have live callers:** BL-672's and BL-673's
  routes are registered in `writeRoutes`; BL-674's drill-down button calls
  `fetch('/epic-reorder/topic-make-top' + q, ...)`, not a stub.
- **Full recompile + touched-file suites green:** `npm run compile` clean;
  165/165 unit tests across the 9 touched test files (`epicReorderUiHtml`,
  `epicDrilldownUiHtml`, `epicMakeTopBridge`, `topicMakeTopBridge`,
  `makeTopPrioritySafety`, `topicMakeTopPrioritySafety`, `epicReorderBridge`,
  `bridgeServer`, `backlogFolders`); 9/9 property tests
  (`makeTopPrioritySafety.property.test.js` +
  `topicMakeTopPrioritySafety.property.test.js`); acceptance 9+9+5 = 23/23
  across the three features.
- **backlog/hold/ plumbing** (`backlogReader.ts`/`backlogWriter.ts`, BL-672's
  own commit): `hold` added as a first-class `BacklogFolders` read bucket and
  a legal `findBacklogFilePath` write-resolution target, consistent with
  constitution Article 3.1 (hold is human-held, never auto-promoted, but
  still live for ordering purposes) — reviewed, no issue.

## Noted, not blocking (belongs to the hardener, not architecture)

`npm run crap` (full-repo) flags several NEW functions in this parcel above
the CRAP<=6 threshold: `computeMakeTopPriority` (10.00), `visit` (10.41, the
dependency-cycle DFS), `walkToIndex` (7.04), `applyMakeTopPriorityResult`
(7.08). CRAP/mutation reduction is the hardener's gate (Article 4.1), not
architecture review — surfacing here only so it isn't missed as "pre-existing
baseline" the way `bl259-dependency-gate-crap-miss-after-bounce-chain` almost
was on an earlier ticket.

By architect.
