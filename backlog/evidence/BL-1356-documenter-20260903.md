# BL-1356 — documenter pass (2026-09-03)

## Context

Merged hardener `c4f338baf8`. Reviewed the coder/cleaner/architect/hardener
evidence chain, the ticket YAML (human ruling: option 1, keep invariants on
the live ledger, assert non-mutation across the run), the feature file, and
`extension/test/helpers/stampOff.js` to identify the operator/QA-visible
surface this defect fix changes.

## Review checklist (complete pass, Article 4.4)

- **Existing how-to currency**
  (`docs/how-to/BL-1175-property-suite-standing-reds-block-unrelated-commits.md`):
  this doc already documents the allowlist/guard mechanism this defect
  repeatedly consumed (5 of 6 stamp-off files carried identical waivers).
  Added a section describing the shared `assertRunWritesNoDecision` helper,
  the before/after non-mutation assertion replacing the moving state
  literal, the independently-checked non-weakening half, and the five
  allowlist rows removed in the same parcel — extended the existing doc in
  place (same allowlist mechanism, this shape of standing red resolved
  rather than parked) rather than forking a new page. Added cross-links to
  the four per-hotfix stamp docs this ticket touches
  (BL-1115/1116/1117/1136; no separate how-to exists for bl1113 or bl1323).
- **`docs/index.md` orphan/exhaustiveness check**: the how-to was already
  linked; extended its one-line hook to name the BL-1356 fix.
- **`Specification.MD` "Last Updated" freshness**: added a new dated entry
  at the top of the changelog, cross-referencing the same how-to. Bumped in
  the same commit as the content.
- **Diagram currency** (constitution Article 1.7 / local-engineering
  "Diagrams"): checked the four registered diagrams. None of their
  change-triggers fired — this is a property-test assertion fix and a
  shared test helper; it does not change the extension host/webview
  boundary, the tmux substrate relationship, the `.swarmforge/` state
  layout, pipeline topology, backlog flow, the handoff file lifecycle, or
  front-desk routing. No diagram update required.
- **`docs/deprecated/` check** (Article 3.6): nothing retired — the
  stamp-off invariant itself (BL-654) and the allowlist mechanism (BL-1175)
  are both unchanged in kind; only the assertion and the TSV rows it no
  longer needs changed. No page moved.
- **Production-code scope**: none touched by this pass; doc/spec/test-helper
  documentation only (the shared helper itself is test code, already
  reviewed by architect/hardener).

## Findings

NONE.

## Verdict

Forward to QA.
