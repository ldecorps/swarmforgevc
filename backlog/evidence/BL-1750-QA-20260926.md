# BL-1750 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 40395cec9e, merged into QA as 08046a72b0. The only
conflict was BL-1700's done YAML (bookkeeping union). The code change is
one file, specs/pipeline/steps/lib/fixtureReaperAbnormalExitHarness.js:
SWARMFORGE_FLEET_HOME now points into the fixture's own root.

Gates (qa-gather, one run each on 08046a72b0): `VERIFY BL-1750`, register
`unowned: []`, wiring OK, `npm test` exit 0, `npm run test:properties`
exit 0. The straggler checks before and after exited 1 with no output,
which for pgrep means no match.

qa_e2e:
1. BL-458 feature: 6 of 6, scenario 05 included. Host load before the run
   was 4.20 / 6.57 / 10.61 and after it 6.19 / 10.93 / 11.70 (the lane ran
   at 12:32:14-12:32:20Z, at the end of the gather).
2. The coder evidence names the cause with a quoted log line (`free-port
   bridge pid= 24261 cmdline= node .../start-bridge-headless.js
   /home/carillon/swarmforgevc 8765  port 8765`). The fixture resolved the
   host's real fleet home and freed the live bridge port. The production
   half (the supervisor defaulting SWARMFORGE_FLEET_HOME to the real
   $HOME) is already ticketed as BL-1779; this parcel fixes only the
   harness, as scoped.
3. `pgrep -af 'aps-fixture-reaper-abnormal-exit-'` after the run: empty.
4. At the land: QA's hand-built replay removes BL-1750's row from
   backlog/standing-reds.tsv (the retirement REGISTER_ROW_RETIRED would
   report).

Observed and not attributed: the live 8765 bridge restarted during the
gather. front-desk-supervisor.log shows a supervisor restart at 12:25:04Z
that flagged a stale build (6b309e702f, the BL-1753 land), a bridge crash at
12:28:51Z, then a recompile and crash loop until attempt 5 held at
12:29:31Z. That was during the property lane, before this parcel's
scenario ran at 12:32Z, and no free-port line appears in the window.
