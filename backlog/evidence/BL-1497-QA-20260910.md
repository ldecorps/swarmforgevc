# BL-1497 — QA review pass, 2026-09-10

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

BL-1497's OWN work carries zero defects. Gates run:

- `swarmforge/scripts/test/test_commit_integrity_lib.sh` — PASS
- `swarmforge/scripts/test/test_commit_integrity_cli.sh` — 7/7 PASS
- `swarmforge/scripts/test/test_commit_integrity_1497_lock_reap.sh` — PASS
  (84-case property runner over `reap-decision`, invariants 1/2 held;
  scenarios 01-03 via the real CLI against a planted lock)
- Acceptance: `run_acceptance.sh` on
  `specs/features/BL-1497-...feature` — 5/5 scenarios green
- `required_wiring` (BL-1497DeadHolderCommitLockReapedSteps.js::registerSteps):
  proven by construction — the acceptance run above drove the real
  `commit_integrity_cli.bb` through the registered handler, not a stub.
- `git diff main...HEAD --name-only`: no `extension/src|test` path touched
  (Babashka/docs/feature only) — Article 4.5 changed-path unit gate has
  nothing additional to run.
- `docs/reference/Specification.MD` entry read against the implementation:
  accurate. No diagram change due (no registered diagram depicts the
  commit-lock internals).
- No orphaned test/mutation processes before or after (`pgrep -fl
  'node --test|stryker'` clean both times).

**BLOCKED from approval by Article 4.2 (unowned reds) — not a BL-1497
defect, the parcel WAITS.** Running the required `npm run test:properties`
gate surfaced instability; isolating each failing file individually
(reduced host contention) separated real defects from transient
resource-contention flakes:

- Full-suite run 1 (07:56:59Z): failed `bl1367ApprovalCarriesItsRuling`,
  `bl1373PathSetCacheInvariants`.
- Full-suite run 2 (08:01:26Z): failed `bl1089FrontDeskLivenessFixture`,
  `bl1272LandedSiblingInvariants`, `bl1313BatchGuardVisibilityInvariants`
  (x2 invariants), `bl1358MutantTimeCeilingInvariants`,
  `bl1373PathSetCacheInvariants`.
- Isolated re-run (own vitest invocation, no sibling workers): `bl1272`,
  `bl1358`, `bl1373` all PASS — confirmed transient/contention-flaky, not
  real defects, no ticket owed.
- Isolated re-run: `bl1089FrontDeskLivenessFixture` — DETERMINISTIC red.
  `assert.match(src, /stamp_own_heartbeat_then_age_past_stall/)` fails
  because the live fixture (`front_desk_liveness_fixture.sh`) defines
  `stamp_own_heartbeat_immediately_stale` instead — the test's source
  regex was never updated for a helper rename.
- Isolated re-run: `bl1313BatchGuardVisibilityInvariants` — DETERMINISTIC
  red, both invariants. `FileNotFoundException:
  .../respawn_bootstrap_lib.bb (No such file or directory)` inside the
  test's own bb-closure-copy fixture — `handoff_lib.bb` now
  `load-file`s `respawn_bootstrap_lib.bb` (line 65) but this test's copy
  set was hand-listed and never updated for that transitive dependency.
- Neither `bl1089FrontDeskLivenessFixture.property.test.js` nor
  `bl1313BatchGuardVisibilityInvariants.property.test.js` is touched by
  this parcel's diff (`git diff main...HEAD --name-only` — no match) and
  both are pre-existing on `origin/main` (`main` == `origin/main` at
  60623ebf0223).
- `grep -rl` for both filenames across `backlog/paused`, `backlog/active`,
  `backlog/hold`, and `backlog/standing-reds.tsv`: no match — neither red
  carries an open ticket. Per Article 4.2 / the 2026-09-05 standing-red
  amendment, QA approves no parcel whose evidence names a red with no open
  ticket. Sent `unowned-red` note (priority 00) to specifier + coordinator
  naming both; BL-1497 stays in_process on QA pending the ticket(s).

By QA.
