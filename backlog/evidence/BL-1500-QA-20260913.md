# BL-1500 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1500`: `VERIFY BL-1500` (no open deferral).
- Ancestry: this ticket's own coder (`e42d7ed62f`), cleaner (`e2b4d0c415`),
  architect (`a63a9eb4d5`), hardener stamp (`b0066c707c`) and documenter
  (`7dbbc20738`) merges are all ancestors of the approved commit (verified
  via `git merge-base --is-ancestor`). `main`/`origin/main` in sync
  (`git rev-list --left-right --count`: 0/0). No prior bounce on this
  ticket (`bounce_history` absent from its YAML; `git log main --grep
  BL-1500` shows only mint/approve/promote/topic commits).
- `git diff main -- swarmforge/scripts/handoffd_supervisor.bb
  specs/features/BL-977-supervisor-never-halts-a-progressing-daemon.feature`:
  empty (constraint respected — neither file touched).
- `node specs/pipeline/cli.js specs/features/BL-977-supervisor-never-halts-a-progressing-daemon.feature`:
  pass 10 / fail 0 (was pass 9 / fail 1 on main pre-fix). Scenario 05 log
  no longer reads "recovered daemon healthy".
- `node specs/pipeline/cli.js specs/features/BL-1500-the-bl977-wedge-check-presents-a-daemon-older-than-the-startup-grace.feature`:
  pass 3 / fail 0.
- `bb swarmforge/scripts/test/handoffd_supervisor_startup_grace_test_runner.bb`:
  ALL TESTS PASS (7 assertions; grace contract unchanged).
- `bb swarmforge/scripts/test/bl977_supervisor_progress_property_runner.bb`:
  ALL PROPERTIES HOLD (200 pure draws + real wedged run-sweep!).
- `grep -c 'handoffd.pid' specs/pipeline/steps/bl977SupervisorProgressSteps.js`:
  3 (write + ageing call + a second unrelated reference); `utimesSync` on
  that path present inside `mkSupervisorFixture` itself, not only in a
  scenario step.
- `pre_qa_gate.sh BL-1500-the-bl977-wedge-check-presents-a-daemon-older-than-the-startup-grace HEAD .`:
  `OK` (required_wiring satisfied — the new handler file is auto-discovered
  by `specs/pipeline/steps/index.js`'s `*Steps.js` suffix scan, BL-1371).
- Full unit suite (`npm test` under `extension/`): 616 files / 10469 tests,
  all green. The run's own suite-duration/per-file budget guard
  (BL-378/BL-445) separately exits 1 on wall-clock overage in files this
  ticket never touches — same pre-existing, non-gating condition recorded
  today on BL-1464's evidence; no standing-reds.tsv row exists for it.
- Full property lane (`npm run test:properties`), host-load class
  (BL-1348-documented; same "Not bounced" disposition as BL-676/BL-1442/
  BL-1424/BL-1412/BL-1409×2/BL-1226/BL-1426 this shift): first full run, 5
  files red (`bl1313BatchGuardVisibilityInvariants`,
  `bl1089FrontDeskLivenessFixture`, `bl1297MergeOwnPathsInvariants`,
  `bl1272LandedSiblingInvariants`, `bl1327DescentLadderInvariants`); second
  full run (load eased, `uptime` 2.69 by the end), 4 files red — different
  membership (`bl1297`/`bl1327` now clean, `bl604TrendAnalysisInvariants`
  newly red), confirming host-contention flake rather than a deterministic
  regression. Of the files seen red across both runs:
  - `bl1313BatchGuardVisibilityInvariants` and `bl1089FrontDeskLivenessFixture`
    carry open register rows (BL-1503, BL-1502) — approved over per
    Article 4.2's register exception.
  - `bl1297MergeOwnPathsInvariants`, `bl1272LandedSiblingInvariants`,
    `bl1327DescentLadderInvariants`, `bl604TrendAnalysisInvariants`: grepped
    `backlog/paused|active` and `standing-reds.tsv` — no ticket, no row, all
    four belong to CLOSED tickets. Re-ran each in isolation once load eased
    (`uptime` load average 2.69/6.26/5.75): all pass cleanly, including
    `bl1272LandedSiblingInvariants` (the one that failed on `Test timed out
    in 20000ms` in both full runs, 10.9s alone). None touch a file this
    parcel changed. Not bounced, not registered — transient host-load class,
    same disposition this shift has already applied repeatedly.
- No orphaned `node --test`/`stryker`/`vitest` processes before or after
  (`pgrep`/`ps` clean both times).
- Register: `backlog/standing-reds.tsv` line 48 (acceptance / BL-977
  feature / BL-1500) removed in this land, per the ticket's own `notes:`.

## Land (BL-1241 remedy — entangled tip, replay)

`land_main_publish.sh . --land BL-1500-... 4d48cbda6f` reported
`LAND_REPLAY land-replay/BL-1500-4d48cbda6f 7c5e5ff062ff9387ce07f41a4df7ec3957ce9613`
with `ENTANGLED_SIBLING BL-1498`, `BL-1499`, `BL-1518` (all still unlanded).
Landed `7c5e5ff062` (never the cited `4d48cbda6f`) per the recipe;
`is_qa_ancestor.sh` confirmed approved. Recorded `abandoned_commits:
[4d48cbda6f]` on the ticket in a follow-up commit (`14ca14d309`), which
itself required its own land-replay (same three entangled siblings) —
landed as `d1d0a8cc00ccdea81cca21d1e7295485d0ea45ef`, `is_qa_ancestor.sh`
confirmed. `origin/main` tip is `d1d0a8cc00`; both replays' land-approval
mappings are auto-recorded (BL-1334, verified via `is_qa_ancestor.sh`).
Both `LAND_REPOINT_SKIPPED` (a parcel — this one — still `in_process`);
expected, no action needed. Coordinator notified with all three entangled
sibling ids named, per the constitution.
