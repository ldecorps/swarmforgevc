# BL-1284 QA pass — 2026-08-30

## Entangled tip, rebuilt tip-pure (BL-1241)

The parcel handed to QA (coder commit `fa7d305367`) carries unlanded work
from BL-1182 and BL-1232 as ancestors — both still `status: todo, assigned_to:
coder` (BL-1182 was bounced by the architect earlier today), so neither is a
landable sibling. Per the constitution's BL-1241 procedure, this is rebuilt
tip-pure rather than bounced or approved as-is:

- The coder's own atomic commit is isolated cleanly: `git diff 66dd6ad1d
  fa7d305367 --stat` touches exactly two files —
  `swarmforge/scripts/test/suite-manifest.tsv` (one line deleted) and
  `backlog/evidence/BL-1284-property-runner-manifest-row-20260830.md` (new).
  Matches the ticket's own declared scope exactly ("the deliverable is the
  deletion of one data row from a TSV").
- Extracted that diff, applied it cleanly onto a fresh branch from current
  `origin/main` (`bl1284-tip-pure-rebuild`), and verified there before
  landing:
  - `bb swarmforge/scripts/test/suite_inventory_cli.bb swarmforge/scripts/test`:
    exit 0, "ok - 434 test file(s), 430 standing, 4 excluded with a dated
    reason" — matches the ticket's own expected count (434, measured against
    `main`; the coder's branch reported 436 only because BL-1182's unlanded
    files were also ancestors there).
  - `run_bb_suite.sh --list`: 431 lines, non-empty.
  - `specs/features/BL-1239-suite-manifest-accounts-for-every-test-file.feature`:
    4/4 (was 2/4 per the ticket).
  - `specs/features/BL-973-copy-lists-closure-derived-and-suite-completeness.feature`:
    **13/13** — fully green (this was the one red I measured during today's
    BL-1279 pass, `12 pass / 1 fail`, failing on exactly this manifest row;
    now resolved).
- Landed the rebuilt commit directly (parent = `origin/main`, not the
  entangled branch). `abandoned_commits: [fa7d305367]` recorded on the ticket
  so `task_scope_gate_lib.bb`/`pre_qa_gate_lib.bb` re-base their walk on
  `origin/main` rather than reading the severed descent as a refusal.

No cleaner/architect/hardener/documenter stages required — the ticket's own
`stage_skip_reasons` (single TSV-row deletion; no code, design, or doc
surface touched) are accurate and match what the isolated diff actually
contains.

## Verdict: APPROVE (as the rebuilt tip-pure commit, not the entangled parcel)

By QA.
