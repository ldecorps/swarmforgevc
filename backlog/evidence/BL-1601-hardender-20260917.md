# BL-1601 — hardender review pass, 2026-09-17

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: npx vitest run test/helpers/tmpDir.test.js (hand-mutated tmpDir.js: dropped EBUSY from the retryable disjunction)
- **Commit hash**: ddbb799f35
- **First error excerpt**: 2 failed | 15 passed (17) - assert.throws(...) failed, expected EBUSY to be retried/rethrown
- **Failure class**: test-coverage-gap
- **Expected vs observed**: both disjuncts of the retryable check (ENOTEMPTY, EBUSY) are independently proven load-bearing
- **Blamed role**: hardener
- **Remediation pointer**: added EBUSY mirror of the existing ENOTEMPTY retry/exhaust tests to tmpDir.test.js; confirmed red on the mutant, green on real code

By hardender.

## Detail

This is a self-remediated finding in the hardener's own domain (mutation
hardening/test coverage), fixed in this same pass — not a bounce to
another role.

- `npm run compile` (extension/) → clean.
- `npm test` (extension/) → 625/625 files green, no new failures (only
  pre-existing per-file budget warnings for already-registered poles,
  unrelated to this ticket — BL-1618/BL-1620/BL-791 territory).
- `npx vitest run test/telegramCursorOperatorExec.test.js test/helpers/tmpDir.test.js`
  → 41/41 pass (39 before my added EBUSY tests, +2, run after restoring
  the mutant).
- `npx vitest run --config vitest.properties.config.mjs test/tmpDirRemoveRetry.property.test.js`
  → 1/1 pass (50 fast-check trials each).
- qa_e2e step 1 reproduced directly: 20 standalone runs of
  `npx vitest run test/telegramCursorOperatorExec.test.js` → 20/20 green.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1601-*.feature`
  → 5/5 pass.
- Confirmed `backlog/evidence/unowned-red-bl1388-land-step-guard-fixture-cleaner-20260917.md`
  (merged in alongside this parcel) is genuinely pre-existing and
  unrelated — reproduced on a fresh clone of `origin/main` per the
  cleaner's own evidence; no action needed from this pass.
- No `.ts` under `extension/src` touched; `tmpDir.js` is explicitly
  outside Stryker's `out/**/*.js` mutate scope (a test helper, not
  production code) — hand-mutation (BL-567 fallback) is the correct and
  only gate here, which is what surfaced D1.
