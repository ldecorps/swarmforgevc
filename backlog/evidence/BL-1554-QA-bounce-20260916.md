# BL-1554 QA bounce — 2026-09-16

## D1 — register_join never matches a real unit/property failure (path prefix mismatch)

1. **Failing command**: `node extension/out/tools/qa-gather.js --ticket BL-1554 --task BL-1554 --root /home/carillon/swarmforgevc/.worktrees/QA` (real run, real `npm test` / `npm run test:properties` subprocesses, no fake runner)
2. **Commit**: `2f9edcc13c` (merge of documenter cbc5b258f2 into QA)
3. **First error excerpt** (from the tool's own JSON report):
   ```
   "register_join": [
     { "file": "test/bl1277UnscopedStepCollisionGuard.test.js", "join": "absent" },
     { "file": "test/bl968MaterializedGuardSensitivity.property.test.js", "join": "absent" }
   ]
   ```
   while the SAME run's `register` check (`standing_red_register_cli.bb`) reports both files as owned:
   `{"lane":"unit","file":"extension/test/bl1277UnscopedStepCollisionGuard.test.js","ticket":"BL-1607",...,"owned":true}`
   `{"lane":"property","file":"extension/test/bl968MaterializedGuardSensitivity.property.test.js","ticket":"BL-1606",...,"owned":true}`
4. **Failure class**: `behavior`
5. **Expected vs observed**: expected the register join to report `owned` for a failing file the register register genuinely owns (BL-1554's own invariant: "the register join reads the register CLI's JSON"); observed `absent` for every real unit/properties failure, always, because `parseFailingFilesFromVitestOutput` (extension/src/quality/qaGather.ts) captures the bare path vitest itself prints (e.g. `test/bl1277....test.js`, since the unit/properties checks run with `cwd: path.join(ctx.root, 'extension')`), while `buildRegisterJoin` looks that bare path up directly in the register rows, which are always repo-root-relative (`extension/test/...`). The map lookup can never hit for the unit or properties lanes — the join silently reports every real red as `absent`, defeating the ticket's whole purpose (distinguishing an owned pre-existing red from a genuinely new one). The one feature scenario covering this join (`BL-1554 qa-gathers-checklist-05`) passes only because its fixture data supplies the FAIL-line file already prefixed `extension/test/...`, which is not the shape real vitest output has under this tool's own `cwd` — the fixture doesn't reproduce the real path shape, so it never exercised the actual bug.

**Blamed role**: coder (the join/parse logic in `extension/src/quality/qaGather.ts`, `parseFailingFilesFromVitestOutput` / `buildRegisterJoin`).

**Remediation pointer**: `extension/src/quality/qaGather.ts` — normalize the vitest-parsed file path to the repo-root-relative shape (prepend `extension/`) before the register lookup in `buildRegisterJoin`, OR normalize the register's `file` key the same way at lookup time; add a feature/property scenario that supplies a FAIL line in vitest's own bare (`test/...`) shape (not pre-prefixed) to prove the join actually matches a real register row.

## Full inventory
- stragglers_before: ran, clean (only other worktrees' own processes present).
- sibling: ran, `VERIFY BL-1554` (no open deferral).
- register: ran, clean; register itself lists 15 owned rows, 0 unowned.
- wiring (`pre_qa_gate.sh`): ran, `OK`.
- unit (`npm test`): ran, 1 failing file (`bl1277UnscopedStepCollisionGuard.test.js`), owned by BL-1607 (register) — not a new offender, pre-existing standing red.
- properties (`npm run test:properties`): ran, 1 failing file (`bl968MaterializedGuardSensitivity.property.test.js`), owned by BL-1606 (register) — not a new offender, pre-existing standing red.
- acceptance (`run_acceptance.sh` for BL-1554's own feature): ran, 9/9 pass.
- stragglers_after: ran, clean (same other-worktree processes, no leak from this parcel).
- **register_join**: D1 above — the only real defect found this pass.

No other item found. Only D1 routes.
