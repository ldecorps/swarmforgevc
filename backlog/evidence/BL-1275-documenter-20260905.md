# BL-1275 — documenter pass (2026-09-05)

## Scope reviewed

Re-read the ticket, the acceptance feature
(`specs/features/BL-1275-property-guard-deletes-the-evidence-for-its-own-refusal.feature`),
the coder's full diff (`f782498bed`, `check_property_suite_drift.sh`), and the
chain of prior evidence (coder `BL-1275-coder-20260905.md`, cleaner
`BL-1275-cleaner-20260905.md`, architect `BL-1275-architect-20260905.md`,
hardener `BL-1275-hardener-pass-20260905.md`).

## Documentation surfaces checked

- `docs/how-to/BL-570-property-suite-drift-guard.md` — this is the guard's
  primary how-to and its "Red (any non-allowlisted failure)" table row was
  now stale: it described the block but not that a durable log is retained.
  Updated the row and added a new section ("A non-allowlisted refusal
  retains its full suite output") describing the retention path, naming
  pattern, bound, override env var, and the two invariants (evidence
  survives; nothing lands in the tracked tree) in the same prose style as
  the rest of the doc.
- `docs/reference/Specification.MD`: added a new dated entry per the file's
  standing convention.
- Grepped `docs/` for other guard how-tos that describe
  `check_property_suite_drift.sh`'s refusal path specifically:
  `BL-1121`, `BL-1124`, `BL-1175`, `BL-1196`, `BL-1252`, `BL-632` each
  describe a different, unrelated facet of the guard (reconcile-import
  skip, canary, allowlist, fixture git-dir leak, violation-chain reporting,
  commit-time guard composition) — none claim anything about what survives
  a refusal, so none needed a change.
- `docs/diagrams/architecture.mmd` mentions `check_property_suite_drift.sh`
  twice, both at the level of "this guard exists / fail-open + override" —
  neither depicts refusal-output retention; no edit needed, change-trigger
  did not fire.

## Verdict

Two doc edits made (BL-570 how-to table row + new section;
Specification.MD entry). No other documentation defect found.

By documenter.
