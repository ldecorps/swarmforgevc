# BL-1189 — QA holds, does not merge — 20260828

**Received:** `merge_and_process documenter a35c41941d` (handoff
`00_20260828T003806Z_000866_from_documenter_to_QA`, priority `00`)

## Why this parcel is NOT being processed

The named commit (`a35c41941d`) is documenter's own
`evidence(BL-1189): documenter holds, not forwarding to QA pending
BL-1211` — its commit message and the evidence file it adds
(`backlog/evidence/BL-1189-documenter-pass-hold-20260828.md`) explicitly
state documenter decided **not to forward this ticket**, sending a `note`
instead of a `git_handoff`. Read the full chain before deciding:
documenter's hold evidence, hardener's original investigation
(`e0d5ca5adf`), and the specifier's own mint-time notes on `BL-1211`.

**The underlying concern, already twice-investigated (hardener,
independently re-verified; documenter, independently re-verified a second
time) and NOT re-litigated here:** the earlier 13-file tree-recovery
(`0bf05774a`) accidentally resurrected
`extension/test/bl1189LiveScreenOnePrimaryWorkingTicketInvariants.property.test.js`
byte-identical to content a legitimate bounce-revert (`1fcd4c167`,
BL-490/BL-495 class) had deliberately removed. It is confirmed
**functionally correct** — 4/4 passes against the current, correctly
re-fixed implementation, and coder's own genuine re-fix (`739ca994e`)
never needed to touch this file (it was never the subject of either
bounce's D-items). But its reappearance is a genuine **process/provenance
violation**: reverted content silently re-entering without going back
through the pipeline. Both prior roles correctly declined to unilaterally
dismiss or fix it themselves — it spans architect/coder/hardener branch
history and is squarely `BL-1211`'s general defect class, already minted
and `human_approval: approved`, but with **no BL-1189-specific ruling**
found yet (grepped `backlog/evidence/*.md` for `BL-1211` — only the mint
note and this file; grepped for a BL-1189+BL-1211 joint disposition —
none).

## New fact this parcel adds

**A `git_handoff` for this exact commit reached QA's `inbox/new` anyway**,
despite documenter's explicit, logged decision to send a `note` instead.
Neither hardener's nor documenter's own evidence describes generating this
handoff deliberately. Not diagnosing the dispatch mechanism itself here
(out of QA's remit and this session already has several live, tracked
swarm-machinery incidents — BL-1124, BL-1214, BL-1202/1200) — reporting the
observation for whoever owns handoff dispatch to correlate against
`swarmforge-documenter`'s own outbox/sent history around
`2026-08-28T00:38:06Z`.

## QA's disposition

**Holding, matching documenter's own posture — no merge attempted, no
commit landed for this parcel.** Completing the inbound task without
forwarding (same shape as a spec-gap disposition: the defect, if any, is
not in documenter's or an earlier stage's own work, it's an open
cross-branch provenance question upstream of QA's gate).

Sent priority-`00` note to specifier+coordinator: (1) confirming QA is
honoring the hold rather than independently re-deciding it, (2) flagging
the stray-handoff-reached-QA-despite-hold fact as new information, (3)
asking for the BL-1189-specific ruling BL-1211's disposition still owes,
since QA cannot manufacture that ruling itself.

By QA.
