# BL-602 QA bounce — 20260827

**Routing:** coder (tip-pure handoff / branch pollution — BL-506)

## Failing command

```
git merge --no-ff ce2ee20358
# CONFLICT architecture.mmd, docs/index.md, Specification.MD, steps/index.js,
#   handoff_lib.bb
# CONFLICT (modify/delete): selfHealTelemetry.test.js,
#   bl597TrendSelfHealEventsSteps.js, bl599TrendIntakeBalanceSteps.js
#   (deleted in tip; kept in HEAD)
```

## Commit tested

`ce2ee20358` (documenter handoff; tip-pure rematch claim). Merge onto
`origin/main` (`7c15ad7c8c`+) conflicts; tip deletes living sibling product.

## First error excerpt

Tip deletes or regresses already-landed work:

- `extension/test/selfHealTelemetry.test.js` (BL-597)
- `specs/pipeline/steps/bl597TrendSelfHealEventsSteps.js`
- `specs/pipeline/steps/bl599TrendIntakeBalanceSteps.js`

Plus conflicts on Spec/index/arch/steps/`handoff_lib.bb`, and post-merge
delta includes self-heal telemetry / front_desk / kill_pipeline churn outside
BL-602.

## Failure class

behavior

## Expected vs observed

**Expected:** BL-602-only tip (handoffLatency + stamps + acceptance + how-to)
merging cleanly atop current `origin/main` without deleting BL-597/599.

**Observed:** Documenter tip not tip-pure; would wipe living acceptance
wiring (BL-506).

## Defects

**D1 — entangled tip (blame: coder rematch):** Rebuild tip-pure BL-602 atop
current `origin/main` with only handoff-latency product + evidence; do not
delete BL-597/599 steps or selfHeal tests.

By QA.
