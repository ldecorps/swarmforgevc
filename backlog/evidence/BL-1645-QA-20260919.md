# BL-1645 — QA review pass, 2026-09-19

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Received `90e635d1bb` (documenter merge-up, `NONE` pass). Full pipeline
lineage confirmed as ancestors of the received commit: coder `3ef54e7dc5`,
cleaner `7bcc832056` (with `0fb6c2ab8d` temp-dir-trap fix folded in),
architect `03253fb3e6`, hardener `aae048582d`, documenter `46e16af7c5` +
`90e635d1bb`.

Gates run, all green:
- `qa-sibling-check.js status --ticket BL-1645`: `VERIFY BL-1645`.
- `bb swarmforge/scripts/test/forward_evidence_lib_test_runner.bb`:
  `ALL PASS`.
- `pre_qa_gate.sh BL-1645 90e635d1bb`: `OK` (required_wiring —
  `bl1645EvidenceWindowOpensAtCreationSteps.js` present and registered).
- Acceptance,
  `specs/features/BL-1645-completion-evidence-window-opens-at-the-inbounds-creation.feature`:
  6/6 scenarios pass.
- Full unit suite (`extension`): 631 files / 10791 tests green.
- Property suite (`npm run test:properties`): 423/424 files, 1236/1237
  tests green. One failure,
  `test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
  invariant 1, timed out at the 20s per-test ceiling under full-suite
  contention (429s wall, 424 files). Re-ran that file in isolation:
  green in 7.1s. `BL-1474` is closed (`backlog/done/M8/`); its file has
  no row in `backlog/standing-reds.tsv`. Not a standing red (isolated
  rerun is green, deterministic — this is contention/timeout, not a code
  defect) and unrelated to BL-1645's changed paths (Babashka gate libs,
  not the replay-commit JS path), so it does not block this approval;
  no unowned-red note filed.
- No orphaned `node --test`/`stryker` processes before or after the run.

Wiring: the three gate call sites (`done_with_current_task.bb`,
`done_with_current_batch.bb`, `forward_evidence_lib.bb`) all read the
window start through the one shared `inbound-window-start` reader, per
the acceptance feature's scenario 05 and the Babashka unit runner.

By QA.
