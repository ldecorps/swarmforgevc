# BL-1307 — documenter pass, 2026-08-31

Reviewed hardender commit `7016ab9849` (architect commit `a7d02db4e0`,
coder commit `6f8227894e`).

## What changed (user/operator-visible)

`swarm_handoff.bb`'s review-forward evidence gate gains a third refusal
fact, `forward-carries-own-evidence?`: a review-role forward that carries
incidental content (most commonly a merge conflict resolved mechanically
while merging a predecessor's work) but adds no Article 4.4 evidence file
for the task is now refused, closing the gap BL-806 (same-commit forward)
and BL-1293 (bare merge introducing nothing) leave open.

## Docs updated

- `swarmforge/handoff-protocol.md`: widened the "Review-Forward Evidence
  Gate" section heading to `(BL-806, widened by BL-950, BL-1307)` and added
  a new subsection, "A forward must carry its OWN review evidence, not just
  any content (BL-1307)", describing the new fact, its fail-open shape, the
  refusal wording, and the implementation split (pure `blocked?` vs. the
  new fs-touching `forward-carries-own-evidence?`).
- `docs/reference/Specification.MD`: new top changelog entry (Last Updated
  bumped to August 31, 2026) summarizing the fix, the `b7d22b9ee3` proof
  commit, the range/task semantics, and a pointer to the widened
  handoff-protocol.md section.

No other doc references this gate's decision rule (checked: `grep -rl
review.forward.evidence docs/` returns only Specification.MD). No
registered diagram (`docs/diagrams/{architecture,swarm-flow,handoff-flow,
front-desk-flow}.mmd`) depicts this gate's internal decision logic — it is
a refusal predicate inside the send path, not the inbox/in_process/
completed state machine or the pipeline topology, same reasoning BL-1293's
own `stage_skip_reasons.documenter` gave for skipping this stage entirely.
No diagram change needed.

## Findings

NONE. Clean documentation pass — no defects in the handed-off work fall in
the documenter's domain.
