# BL-1301 — QA pass

Date: 2026-09-02 · Verdict: **PASS — approved**

## Received

Merged documenter commit `1a9594bd24` (doc updates, forwarding) onto QA's tip.
Ancestry confirmed ancestors of the merge: `393634823a` (coder, no cleaner —
`stage_skip_reasons` names it) → `e62ca5c3c6` (architect merge) →
`7d1e6f8391` (architect) → `d26e487cee` (hardener merge) → `35b59b07c6`
(hardener) → `4c6c4b0b14` (documenter merge) → `1a9594bd24` (documenter).

No prior bounce history for BL-1301 on `origin/main` — this is the ticket's
first QA pass.

## qa_e2e_procedure — read against the real code, not re-simulated

The ticket's four manual steps are exactly what
`bl1301ParkedTicketSteps.js` drives against the real harness
(`dropped_parcel_sweep_harness.bb` / `dispatch_gap_sweep_harness.bb`, the
same thin wrapper `handoffd.bb`'s `dropped-parcel-sweep!` calls) rather than
a JS re-statement — confirmed by reading the step handlers, not just their
pass/fail. Acceptance run below covers all four 1:1 (scenario 1 outline =
steps 1+2, scenario 6 = step 3's "absence buys no silence", scenario 8 =
step 4's dispatch-gap-untouched check).

## Independent re-verification (not taken on prior passes' word)

- `bb swarmforge/scripts/test/dropped_parcel_test_runner.bb` → **ALL PASS**.
- `bb swarmforge/scripts/test/bl1301_parked_ticket_invariants_property_runner.bb`
  → **ALL PASS** (P1 opt-in/fail-closed, P2 one-decision blast radius, P3
  never invisible — all three declared `invariants:`).
- `bb swarmforge/scripts/test/dispatch_gap_test_runner.bb` → 2 failures
  (`top-expedited-paused-candidate-08`, `...priority breaks ties...`).
  Cross-checked myself: `grep -rl "top-expedited-paused-candidate" backlog/`
  → `backlog/paused/BL-1271-dispatch-gap-suite-stale-bug-fixtures.yaml`.
  Pre-existing, already ticketed, unrelated to this parcel's own diff.
- `bash swarmforge/scripts/test/test_chase_sweep.sh` → **ALL PASS**, 17 cases.
- `node specs/pipeline/cli.js
  specs/features/BL-1301-a-parked-ticket-is-not-a-dropped-parcel.feature`
  → **8/8 pass**, no "no step handler matched".
- Article 4.5 (changed-path unit test gate) — both production paths this
  ticket changes have dedicated, independently-run passing tests:
  `chase_sweep_lib.bb` → `dropped_parcel_test_runner.bb` (unit cases) +
  `bl1301_..._property_runner.bb` (properties) + `test_chase_sweep.sh`;
  `handoffd.bb` → `test_handoffd_one_shot_flags_parse.sh` (**ALL PASS**, 6
  cases, incl. `--chase-sweep-once`) and
  `test_handoffd_cooldown_sweep_wiring.sh` (**ALL PASS**). Ran both by name
  given the BL-1164 origin incident (BL-668 landed a broken `handoffd.bb`
  and QA passed without running the one-shot-flags test despite `handoffd.bb`
  being in the land diff) — this parcel touches `handoffd.bb` too, so the
  gate applies squarely, not just by habit.
- Registration: `specs/pipeline/steps/index.js:921` registers
  `bl1301ParkedTicketSteps`. `suite-manifest.tsv` — deliberately NOT touched
  by this parcel, and correctly so: `bl1301_parked_ticket_invariants_property_runner.bb`
  is a property runner, not a `_test_runner.bb`/`test_*.sh` shape, and
  `unregistered_test_gate_lib.bb`'s own header states property runners are
  "their own lane, never a suite member". Verified against precedent: none
  of the repo's other `*_invariants_property_runner.bb` siblings (`bl678`,
  `bl719`, `bl887`) are in the manifest either — this is the established
  convention, not a gap. (Investigated because it looked at first read like
  a BL-1240 quiet-drop; it is not.)
- `required_wiring`: none declared, deliberately (specifier's own ticket
  note explains why — the change sits inside a call path the live sweep
  already exercises, and a required_wiring anchor here would bank a green
  that gates nothing). No anchor to check.
- Doc currency: `swarmforge/backlog-schema.md`'s `status` row rewritten to
  describe both behaviours `blocked` now drives (promotion refusal +
  dropped-parcel suppression), matching the shipped code
  (`parked-ticket?`'s exact-match-after-trim on `"blocked"`, fail-closed on
  anything else including absence). `docs/reference/Specification.MD`
  changelog entry checked against the diff — accurate. No diagram
  change-trigger fired (a sweep's internal decision predicate, not a
  topology or `.swarmforge/` state-layout change).
- Orphaned process check: `pgrep -fl 'node --test|stryker'` clean before and
  after (matches were the checking command's own shell, confirmed by PID
  lookup — already exited).

## BL-1241 — entangled sibling again, same disposition as BL-1314

`BL-1324` is still mid-pipeline (its coder responded to a cleaner bounce in
`backlog/evidence/BL-1324-coder-bounce-response-20260902.md`, present in
this merge but not on `origin/main`) and rides this parcel's chain the same
way it rode BL-1314's — expected, since BL-1301 is now the ticket ahead of
it in the pack. Handled via the same `land_step_cli.bb` replay procedure;
see the landing note below for the resulting commits.

## Verdict

APPROVED, pending the standard BL-1241 tip-pure replay before landing (see
below).

By QA.
