# BL-1535 — hardender review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Reviewed commit: 814cb7f15a (architect's forward; no defect found, no
production code changed since round 2 — new regression case 07 only).
mutation_cost: medium per the ticket (Babashka only, no Stryker target).

### Gates run

- No orphaned test/mutation processes before starting
  (`pgrep -fl 'node --test|stryker'` scoped clean); `uptime` load 2.12
  (well under 2x nproc=20) — no office-hours bypass needed.
- `bb swarmforge/scripts/test/mono_router_lib_test_runner.bb` — PASS (all 6
  new should-rotate-resident? branch cases: refuse, idle-yield, same-role,
  no-parcel, ambulance-override, :busy-precedence).
- `bash swarmforge/scripts/test/test_chase_departing_mid_parcel_gate.sh` —
  PASS, all 8 cases (drives the real `attempt-resident-rotate!` through a
  fixture git repo + fake tmux + real child processes).
- `bash swarmforge/scripts/test/test_rotate_to_role_stuck_parcel_gate.sh` —
  PASS, all 12 cases (regression; case 04 confirms `rotate-resident-to!`
  stays ungated per the ticket's FIRM constraint).
- `bb swarmforge/scripts/test/daemon_cycle_guard_lib_test_runner.bb` — PASS.
- Acceptance: BL-1535 7/7, BL-805 5/5, BL-926 6/6, BL-938 4/4 — all green.
- `git diff main...HEAD -- swarmforge/scripts/handoff_lib.bb` — empty,
  confirming the FIRM constraint (rotate-resident-to!/respawn-as! untouched).
- BL-113 Gherkin mutation (soft) on the ticket's own feature (it carries a
  `Scenario Outline` + `Examples:` on the `<signal>` column):
  `bash specs/pipeline/scripts/run_gherkin_mutation.sh <feature> <fresh
  mktemp under ./tmp/> specs/pipeline/steps/index.js soft` — 2/2 mutants
  KILLED (~17s wall time, genuine re-test per-mutant, not a crash-fake per
  BL-715). Manifest embedded in the feature file
  (`# acceptance-mutation-manifest-begin/end`), workdir removed after.
- suite-manifest.tsv: `test_chase_departing_mid_parcel_gate.sh` correctly
  registered as `standing`.

### Hand-mutation verification (BL-638/BL-567 fallback — no Stryker target)

Hand-mutated `departing-working-signal`'s first `cond` clause
(`handoffd.bb` ~line 1706, `footer-busy? "busy-footer"` → `false
"busy-footer"`) on the live worktree file (no detached job outstanding),
re-ran both `mono_router_lib_test_runner.bb` and
`test_chase_departing_mid_parcel_gate.sh` — both stayed fully green, then
restored the file (empty diff confirmed).

This is a genuine EQUIVALENT branch, demonstrable from the code (BL-234
exception): `footer-busy?` is the same value fed to both
`departing-working-signal`'s first clause and `:resident-busy?` in the
`should-rotate-resident?` call. In `should-rotate-resident?`, the `:busy`
clause (`(and resident-busy? (not ignore-busy?))`) is evaluated BEFORE
`:departing-mid-parcel`, and both clauses share the identical guard `(not
ignore-busy?)`. So whenever `footer-busy?` is true: if `ignore-busy?` is
false, `:busy` fires first and `:departing-mid-parcel` is never evaluated;
if `ignore-busy?` is true, both clauses are disabled by the same guard.
Either way, the specific value `departing-working-signal` returns when
`footer-busy?` is true can never reach the `:departing-mid-parcel`
telemetry log (`(when (= gate :departing-mid-parcel) ...)`), because gate
never equals `:departing-mid-parcel` in that case. No test could ever
observe this branch's removal. Not chased further; not equivalent to a
gap — no artificial test written for it.

Other new functions (`resident-pane-pid`, `resident-pane-live-descendant?`,
`sha256-hex`, `fresh-audit-challenge?`) are already exercised end-to-end by
the 8 shell-test cases via distinct fixture combinations (working/idle pane
PID with/without a child process, fresh/stale/absent audit challenges) —
no additional hand-mutation needed there.

### Not applicable

- CRAP/DRY: no TypeScript touched (all changes are `.bb`/`.sh`/`.md`/step
  handler `.js` in `specs/pipeline/steps/`, not `extension/src`); jscpd is
  scoped to `extension/src` only.
- `sha256-hex` duplicates the same ~4-line pattern already vendored
  separately in `cache_warm_lib.bb`, `reference_freshness_lib.bb`,
  `prompt_engine_lib.bb`, `swarm_handoff.bb`, `suite_baseline_cli.bb`,
  `wake_dedup_lib.bb` — pre-existing repo-wide convention (each `.bb` script
  vendors its own), not new debt introduced by this ticket; cleaner's own
  pass already found no defect.

No orphaned test/mutation processes left running after this pass
(`pgrep -fl 'node --test|stryker'` clean; the one live `sleep 300` on the
host is `babysitterd.sh`'s own unrelated poll, not a test fixture).

By hardender.
