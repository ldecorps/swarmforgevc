# BL-725 — QA review pass, 2026-09-06

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: grep -n "^# " docs/how-to/BL-698-telegram-cursor-operator-commands.md docs/how-to/BL-702-operator-confirm-env-reload.md; grep -n "^## Shared Session\\|^### Cursor Remote" docs/how-to/BL-696-miniapp-lets-talk-cursor-audio.md
- **Commit hash**: 3654a3ae2d
- **First error excerpt**: BL-698 title (line 1): "# BL-698 - Telegram Cursor Remote operator commands"; BL-702 title (line 1): "# How to use Cursor Remote confirm + env-reload (BL-702)"; BL-696 headers: "## Shared Session with Cursor Remote" (line 140), "### Cursor Remote /pilot (operator)" (line 193) - all four still say Cursor Remote, even though the documenters own same commits rewrote the BODY text right below each to say Host.
- **Failure class**: behavior
- **Expected vs observed**: Expected every doc heading naming the topic to say Host, consistent with the ticket own instruction and with the body prose the documenter just finished rewriting in these same files. Observed the H1 titles of BL-698 and BL-702, and two section headers within BL-696, still say Cursor Remote - each now internally inconsistent with its own updated body. Two derivative cross-references also still echo the stale title (docs/how-to/BL-833 and BL-759 link labels quote BL-698 own unrenamed title; docs/index.md BL-698 link label likewise).
- **Blamed role**: documenter
- **Remediation pointer**: docs/how-to/BL-698-telegram-cursor-operator-commands.md line 1 and docs/how-to/BL-702-operator-confirm-env-reload.md line 1: rename the H1 title to say Host. docs/how-to/BL-696-miniapp-lets-talk-cursor-audio.md lines 140 and 193: rename both section headers to say Host. Then sweep the two derivative cross-references (docs/how-to/BL-833-host-agent-activity-feed.md:41, docs/how-to/BL-759-cursor-operator-front-desk-bot-import-cycle.md:38) and docs/index.md own BL-698 link label to match the corrected title.

By QA.

## Detail

This ticket's own core deliverable (the topic-name constant, the four
verified operator-facing strings, the diagram label, the invariant test,
and the acceptance suite) is verified CORRECT and COMPLETE. The single
finding above is a docs-only defect (an incomplete doc-heading sweep),
not a code defect.

- `qa-sibling-check.js status --ticket BL-725`: `VERIFY BL-725` (no open
  deferral).
- Orphan process check before/after: empty both times.
- Lineage: documenter's final commit `12335e192d` merged clean onto my
  prior HEAD `9b2edfa137` (BL-676's own just-landed commit); no conflicts.
- `CURSOR_BRIDGE_TOPIC_NAME` confirmed `'Host'`
  (`telegramCursorBridgeCore.ts:24`); its test assertion moved with it.
- `CURSOR_BRIDGE_SUBJECT_ID`/topic-map value `'CURSOR_REMOTE'` confirmed
  UNCHANGED (invariant: the binding key must never move) — grepped
  directly, only two occurrences repo-wide, both the binding key itself.
- All four named operator-facing strings confirmed switched to "Host":
  `telegramCursorBridgePilot.ts` (4 sites), `telegramCursorOperatorExec.ts`
  (unknown-verb reply), `telegram-front-desk-bot.ts` (ownership error).
  Code COMMENTS naming "the Cursor Remote bridge" as module/incarnation
  shorthand correctly left untouched throughout every touched file (per
  the ticket's own explicit carve-out) — spot-checked across all touched
  `.ts` files.
- Acceptance: `run_acceptance.sh specs/features/BL-725-rename-cursor-remote-topic-to-host.feature`
  — 9/9 (first run showed 3/9 failing with the OLD value; root-caused to a
  stale `extension/out/` compile cache from before this merge's `.ts`
  changes — `npm run compile`, then clean 9/9; not a real defect, the
  chronic stale-cache trap this shift already knows, BL-1412/BL-1226).
- Wiring anchor: `bl725RenameCursorRemoteTopicToHostSteps.js` exports
  `registerSteps`; ticket's own `acceptance:` field points at the
  materialized `.feature` (not `.draft`).
- Property invariant (`bl725HostTopicBindingInvariants.property.test.js`):
  1/1 standalone; also passed within the full property run.
- `docs/diagrams/cursor-remote-flow.mmd`: topic-check node labeled "Host";
  file itself correctly NOT renamed (content relabel only, per the
  ticket's own instruction).
- Reviewed the coder's own scope decision (initially touched BL-698's
  how-to, reverted it after the task-scope gate correctly refused a
  `BL-725:`-tagged commit editing a file named for a different ticket) —
  correct call; the documenter later legitimately completed this same
  edit via two UNTAGGED cross-ticket doc commits (the established
  convention for exactly this situation), which is where D1's gap was
  found — the documenter's own sweep covered eight how-tos plus
  `docs/index.md` but left the doc HEADINGS (as opposed to body prose)
  unfixed in three of them.
- Repo-wide grep for "Cursor Remote" beyond the documenter's own touched
  set reviewed for false negatives: the remaining matches
  (`telegramCursorOperatorCore.ts`, `telegramCursorBridgeLiveness.ts`,
  `telegramCursorBridgeLive.ts`, `telegramOperatorAmbulance.ts`,
  `telegramFrontDeskBotCore.ts`, and how-tos BL-1079/BL-810/BL-514/BL-723/
  BL-701/`architecture.mmd`) are either code comments naming the
  module/incarnation, parenthetical dual-naming ("Host / Cursor Remote"),
  or historical/changelog references to the rename itself or to past,
  unrelated tickets — none are a live operator instruction that should
  have changed. `docs/how-to/BL-810-...md:120` ("Renaming Cursor Remote to
  Host (BL-725)") is correctly a historical record of this very ticket,
  not a defect. None of these files were touched by this ticket's own
  commits (confirmed via `git log` per file against the ticket's own
  commit range), so they are pre-existing text, out of this bounce's
  scope regardless.
- Full unit lane (`npm test`, extension/): 610/610 files, 10309/10309
  green.
- Full property lane (`npm run test:properties`): first run failed 20
  files (32 tests) under host contention (uptime 15-min average ~19
  during the run). Re-ran the 18 non-bl968 failures together in isolation
  (load eased to ~5-7): 17 clean immediately, one
  (`bl1142LocalOllamaPackShape.property.test.js`) failed once more with a
  genuine (non-timeout) property-assertion mismatch rather than a bare
  timeout — reran 3 more times isolated, all 3 clean, confirming it is a
  flake under contention rather than a deterministic break (its own
  domain, local-Ollama pack-depth capping, is unrelated to BL-725).
  `bl968MaterializedGuardSensitivity` reran alone separately (its own long
  ~160-350s runtime): clean in 158s with only the BL-871 allowlisted
  benign error. All 370/370 files confirmed clean.
- Coder's own "second, unrelated defect" note (a GH-24-scoped
  `tempDirTrapGuard` fix, fixed and committed by coder but explicitly
  flagged as GH-24's scope, not this ticket's, and not bundled into this
  ticket's own commit): confirmed genuinely absent from this ticket's own
  commit (`e4633b1bd3`) — correctly kept separate, matches the No-Op /
  scope discipline.
- GH-24 riding along in this branch's ancestry as an unlanded, already-
  bounced sibling (my own bounce, `0c08dc0608`, is NOT yet an ancestor of
  this parcel — this branch forked before that bounce was sent): not this
  ticket's scope, will resolve independently when GH-24's own re-forward
  reaches QA again.

## Disposition

D1 is a documenter-owned defect (Article 4.3). Bouncing to documenter;
the fix then re-forwards down the remaining chain (cleaner → architect →
hardender → documenter → QA) so every gate runs again.
