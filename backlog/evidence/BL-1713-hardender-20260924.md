# BL-1713 — hardender review pass, 2026-09-24

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Verified: the two BL-654 property tests (`npx vitest run --config
vitest.properties.config.mjs test/bl1713LandNeverBlessesAnEmptyRangeInvariants.property.test.js`);
the ticket's own acceptance feature (4/4); the two named regression
features from `qa_e2e_procedure` step 2 (BL-1678, 4/4; BL-1241, 4/4);
`land_step_lib_test_runner.bb` (ALL PASS); the shell test runners
`test_land_step_records_approval.sh` and
`test_bl1388_land_step_guard_fixture.sh` (both ALL PASS). No
`extension/src/*.ts` change in this parcel - Babashka only
(`land_step_cli.bb`/`land_step_lib.bb`), no wired mutation/CRAP/DRY tool
(engineering.prompt "Startup Tools", BL-472).

`test_bl1366_land_is_one_command.sh`'s two failures (an origin/main
push-verification mismatch and an entangled-tip worktree-creation
failure) are pre-existing/environmental, confirmed by hand-reverting
both changed files to their pre-BL-1713 content (`git show
05c3bed060:...`) and re-running: byte-identical two failures reproduce
against the unmodified files. Matches the coder's own evidence
(unowned-red note, 09:11Z). Restored both files to the committed state
afterward (`git diff --stat` empty).

Found and fixed in this pass (commit 5ce458dc49), rather than bouncing -
own-domain strengthening: `caller-cwd-commit`'s nil-guard branch ("a cwd
outside any checkout... keeps today's repo-root resolution", the
ticket's own documented direction) had NO test coverage in either the
acceptance feature or the property test. Hand-mutated
`(and cwd-sha (not= cwd-sha canonical))` -> `(not= cwd-sha canonical)`
(BL-567 fallback) and confirmed the mutant makes EVERY land run from a
non-checkout cwd wrongly LAND_ESCALATE, via a real fixture (`bb
land_step_cli.bb` from a plain `/tmp` directory with no git init, citing
a full sha). Added a fourth `CITATION_SHAPES` cell exercising exactly
this branch; confirmed non-vacuous (fails against the mutant, passes
restored), then restored the mutated file.

Also hand-mutated (BL-567 fallback) `range-credits-ticket?`'s
`some`->`every?` and the range-check's `not`-inversion in `land-plan`:
both already caught by the EXISTING property test (the `ticket-plus-noise`
shape's two candidates disagree on credit by construction, per the
`some`/`every?`-disagreement rule) - no further gap found, both restored
after confirming the kill.

By hardender.
