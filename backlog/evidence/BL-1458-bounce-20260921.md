# BL-1458 — QA bounce, 2026-09-21

1 defect found. Full checklist run; one bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: `npx vitest run test/tempDirTrapGuard.test.js` (also
  reproduces inside the full `npm test` run)
- **Commit hash**: be5f72ed67 (QA's merge tip at the time of this pass;
  `swarmforge/scripts/test/bl1458_instruct_documenter_briefing_test_runner.bb`
  is unchanged since the hardener's commit 4a8e8ed7cc)
- **First error excerpt**:
  ```
  expected zero temp-dir-trap violations under swarmforge/scripts, found:
  /home/.../swarmforge/scripts/test/bl1458_instruct_documenter_briefing_test_runner.bb:
  creates a temp root (fs/create-temp-dir) but has no shutdown hook and no
  try/finally delete-tree
  ```
- **Failure class**: test-coverage
- **Expected vs observed**: expected the new hardener-authored test runner
  to clean up its `fs/create-temp-dir` root via a shutdown hook or
  try/finally, so a leaked temp dir survives even when an assertion inside
  the script throws or `System/exit`s early; observed a bare top-level
  `(fs/delete-tree root {:force true})` at the end of the script (line 98),
  with no `try/finally` wrapping it and no shutdown hook registered - the
  `tempDirTrapGuard` guard (its own repo-wide invariant) correctly flags
  this as unprotected.
- **Blamed role**: hardener (this file is new in the hardener's own commit
  4a8e8ed7cc, "BL-1458 hardening: instruct-documenter-briefing!'s own
  comment states..." per its evidence file
  `backlog/evidence/BL-1458-hardener-20260921.md`)
- **Remediation pointer**:
  `swarmforge/scripts/test/bl1458_instruct_documenter_briefing_test_runner.bb`
  line 44 (`(def root ...)`) through line 98 (`(fs/delete-tree root ...)`)
  - wrap the body in a `try/finally` (or register a shutdown hook, matching
    whatever pattern the guard already accepts for other Babashka runners
    in `swarmforge/scripts/test/`) so the temp root is removed even if a
    `fail!`/assertion/`System/exit` path is taken before reaching line 98.

## Everything else checked this pass — no other defect

- `bash swarmforge/scripts/test/briefing_generation_schedule_test_runner.bb`:
  ALL PASS.
- `bash swarmforge/scripts/test/test_handoffd_briefing_generation_wiring.sh`:
  4/4 PASS.
- `node specs/pipeline/cli.js specs/features/BL-1458-every-briefing-trigger-instructs-the-documenter.feature`:
  6/6 ok, including scenario 06 (the once-per-day marker amendment).
- `grep -rn "compose today's briefing per your role" extension/src swarmforge/scripts`:
  empty, as required.
- `npx vitest run --config vitest.properties.config.mjs test/bl1458BriefingTriggerInvariants.property.test.js`:
  4/4 pass.
- `npx vitest run test/briefingScheduler.test.js`: 11/11 pass.
- `npm run compile`: clean.
- `npm test` (extension/): 633/635 files, 10834/10836 tests passed — the
  ONE new failure is D1 above; the other reported failure,
  `test/stepHandlerModuleLoadBudget.test.js`, is the pre-existing
  registered/owned red (`backlog/standing-reds.tsv`, BL-1658,
  suite-contention flake) - confirmed passing standalone in isolation, not
  this parcel's defect, not re-reported here.
- `npm run test:properties` (extension/): 436/436 files, 1276/1276 tests
  passed; one known-benign BL-871 `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled error, no failures.
- No standing-red register/allowlist row names BL-1458.
- No straggler test/mutation processes before or after.

By QA.
