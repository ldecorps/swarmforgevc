# BL-729 QA pass — 2026-08-01

**Commit verified:** `0c3533431` (merge of documenter `ff42f9e48f` into QA;
BL-729's own hardener merge `7472517f3` confirmed an ancestor via
`git merge-base --is-ancestor 7472517f3 HEAD`).

## Gates run

1. `npm run compile` (extension/) — clean.
2. `npm test` (extension/) — 7029/7030 passed. The one failure
   (`test/tmpDirMigrationGuard.test.js` flagging
   `extension/test/pricingTable.test.js:92`'s raw `mkdtemp` call) is
   pre-existing and unrelated to this ticket's scope: confirmed present
   unchanged at `fa4188273` (pre-merge QA tip), introduced by BL-627, and
   already ticketed (`backlog/paused/BL-742-*.yaml`,
   `backlog/paused/BL-771-*.yaml`). Not a regression from this parcel.
3. `npm run test:properties` (extension/) — 108/108 passed, including
   `commitClaimCheck.property.test.js` (6/6).
4. `bash specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-729-commit-claims-match-their-own-diff.feature` — 9/9
   scenarios green.
5. `required_wiring` confirmed by reading the source, not assumed:
   - `pilotAcceptanceGate.ts`'s `checkClaims` step calls
     `deps.checkCommitClaims()` inside `landPilotedTicket`'s own flow
     (between contract-pass and yaml move) — not merely unit-tested in
     isolation.
   - `pilot-acceptance-gate.ts` wires the real git-backed
     `checkCommitClaims(repoRoot)` from `commitClaimGitReader.ts` into
     `PilotAcceptanceGateDeps`.
6. Manual regression against the real defect commit (ticket's own e2e
   procedure step 6), run against the compiled module directly with real
   `git show`/`git diff-tree` output (matching `commitClaimGitReader.ts`'s
   own invocation shape):
   - `findUnsupportedCommitClaims` on `6a2e4aaf6d` (BL-636's landing commit)
     returns exactly one unsupported claim: `deliver!`, sentence "Also
     restore a deliver! close paren" — matches the ticket's control
     measurement exactly.
   - `role-mail-row` (10 occurrences in the diff body, an 11th in the
     changed-path/message elsewhere per the ticket's own count) is not
     flagged — the checker does not over-fire.
7. Documentation pass (`docs/how-to/BL-727-pilot-acceptance-contract-gate.md`)
   reviewed: accurately describes the new refusal reason, the grammar, the
   fail-open posture, and file map. Complete.
8. Hardener's coverage/CRAP/DRY pass reviewed: 100% coverage, CRAP <= 6, zero
   duplication for the four changed files. Stryker language mutation ran for
   the two genuinely new files (`commitClaimCheck.ts`,
   `commitClaimGitReader.ts`); the other two are within the 3-day mutation
   cooldown (BL-727 hardened them `2026-07-31`, one day prior — confirmed via
   `git log`). Repo-wide Stryker dry run recorded as BLOCKED BY two
   pre-existing, already-ticketed defects (BL-720, BL-771) — not silently
   skipped, per Article 4.4. Gherkin mutation on the Scenario Outline: 9/9
   example-value mutants killed.
9. No orphaned `node --test`/`stryker` processes before or after this pass.
10. No prior bounce evidence for BL-729 on `main`
    (`git log --oneline main -- 'backlog/evidence/BL-729*'` — empty).

## Note (non-blocking)

`human_approval: pending` on the ticket. Per the ticket's own
`approval_context`, the specifier forwarded this to the coder deliberately
(human's 2026-08-01 steering to restore a safe `/pilot` resume path sooner),
same posture already established for BL-764. Flagged here, not treated as a
gate failure.

## Verdict

PASS. Approving.
