# BL-1485 — cleaner review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Scope: `specs/pipeline/steps/bl1318PackStaffingGateSteps.js` (20-line diff,
  two spawn sites now `delete env.PACK_STAFFING_SKIP_GATE` before applying
  any explicit override) and the new
  `specs/pipeline/steps/bl1485Bl1318HandlerDecidesOverrideSteps.js`. No other
  file changed by this ticket's own commits.
- `node specs/pipeline/cli.js specs/features/BL-1485-the-bl1318-acceptance-handler-decides-the-override-itself.feature`:
  pass 4 / fail 0.
- `node specs/pipeline/cli.js specs/features/BL-1318-pack-launch-steward-staffing-gate.feature`:
  pass 7 / fail 0 (was pass 3 / fail 4 on main under the pane's own
  `PACK_STAFFING_SKIP_GATE=1`).
- `./swarmforge/scripts/pre_qa_gate.sh BL-1485-the-bl1318-acceptance-handler-decides-the-override-itself HEAD .`:
  `OK` — required_wiring anchor satisfied by auto-discovery (BL-1371).
- `git diff main -- specs/features/BL-1318-pack-launch-steward-staffing-gate.feature`:
  empty — feature text and mutation stamp untouched, per constraint.
- Mutation-site count (BL-485), against compiled `out/`-equivalent (plain
  JS, no compile step needed for these files):
  `node extension/out/tools/mutation-site-count.js specs/pipeline/steps/bl1318PackStaffingGateSteps.js specs/pipeline/steps/bl1485Bl1318HandlerDecidesOverrideSteps.js`
  — `bl1318PackStaffingGateSteps.js` 371 sites (over the 100 threshold),
  `bl1485Bl1318HandlerDecidesOverrideSteps.js` 96 (within). Not split: the
  file is a pre-existing, single-feature step-handler module (BL-1318's own
  fixture/spawn helpers are shared across all its scenarios) whose file this
  ticket only touched for 20 lines; the ticket's own constraints forbid
  touching BL-1318's feature or its handler's required pairing (BL-233/
  BL-1371 — the handler lands with its feature, one file). A mechanical
  split here would only fragment a cohesive single-feature module for no
  structural gain — the correct call under BL-485's "soft advisory,
  cohesive module stays whole" guidance, not a gate failure.
- Architecture: high-level fixture/assertion logic stays separate from the
  one integration point that shells to `swarmforge.sh`/`pack_staffing_gate_cli.bb`;
  both spawn-env decisions are narrow, local, and match the existing file's
  established pattern (no new abstraction introduced).
- DRY/CRAP/coverage/mutation tooling (jscpd, Stryker) is scoped to
  `extension/` only (`.jscpd.json`, `stryker.config.json` live there); these
  are acceptance-lane step handlers under `specs/pipeline/steps`, outside
  that scope — consistent with Design And Testability (acceptance step
  handlers drive testable modules, are not themselves gated by the
  extension's coverage/mutation/DRY tools).
- No cleanup or refactor needed: the coder's diff is already minimal,
  narrow, and non-duplicative; no new behavior introduced beyond what the
  ticket specifies.
