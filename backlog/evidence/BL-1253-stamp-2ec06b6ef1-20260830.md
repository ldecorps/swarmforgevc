# BL-1253 — stamp-off of Cursor hotfix 2ec06b6ef1

Coder, 2026-08-30.

## Verdict: CONFIRMED

The landed behaviour is what the ticket describes, and it works. Nothing was
reimplemented, no hotfix source line was touched, and the ledger row is
untouched and still `stamp-open` with `human_decision: null`.

## What the review actually ran

Prior stamp-offs (BL-1136, BL-1113) read the source at the commit and assert
on its text. That cannot tell a wired hotfix from an unwired one — and the
fault under review was a decision that looked right and was never
re-consulted. So these scenarios EXECUTE the landed decision:

- Scenarios 01/03 drive the real `runCursorBridgePollOnce` over a real
  `front-desk-poll-heartbeat.json`, through the real exported
  `readFrontDeskPollHeartbeatMs` → `isFrontDeskInboundFeederLive` →
  `shouldUseCursorBridgeInboundQueue`. What is observed is what the bridge did
  with a queued update: drained it, or left it and called `getUpdates`.
- Scenario 02 polls twice with ONE deps object in ONE process, going stale in
  between. Nothing is restarted, which is the claim.
- Scenario 04 runs the real `start_cursor_bridge.sh`. Its own dry-run exits
  BEFORE the feeder block, so the launch is stubbed instead — a fake `bb` on
  PATH records the exported `CURSOR_BRIDGE_INBOUND_QUEUE` and claims the pid
  file — and the real script runs its real feeder decision to the real launch
  line with nothing started.

The Background verifies the tree under review IS the landed hotfix: commit
2ec06b6ef1 reachable, trailer `pending`, every hotfix symbol still present in
all three files, and the wiring reading as the composition the review drives.

**The private helper problem, stated rather than worked around.**
`resolveInboundQueueFromFeeder` is not exported, so the review composes the
same three exported functions itself. Exporting it would have edited a hotfix
source line, which invariant 1 forbids this parcel from doing. The Background
therefore pins the wiring with two regex assertions — the helper IS that
composition, and the poll loop IS wired to it — so the composition the review
drives cannot silently diverge from the one that ships. That is the one place
this review reads source text, and it is there to close exactly the gap.

**Non-vacuity, by mutating the landed code and re-running:**

| break | result |
|---|---|
| drop the `feederLive === false` gate | scenarios 01[2], 01[3], 02, 03 FAIL |
| take `useInboundQueue` once instead of per poll | scenarios 01[1], 02 FAIL |

Restored; 7/7. The second break is the original defect exactly, and scenario
02 catches it — which is the answer to "would this review have caught the
fault it exists for": yes.

## qa_e2e, point by point

1. `frontDeskPollHeartbeatPath` and `readFrontDeskPollHeartbeatMs` are defined
   in `cursorBridgeInboundQueue.ts`, and the latter returns `null` for missing
   or unparseable — confirmed, and scenario 03 exercises the unparseable path
   end to end rather than reading the `catch`.
2. The decision is consulted on every poll: `runCursorBridgePollOnce` calls
   `deps.resolveUseInboundQueue()` at the top of each poll, and
   `runCursorBridgeApp` supplies `() => resolveInboundQueueFromFeeder(opDir)`.
   Confirmed behaviourally by scenario 02.
3. `start_cursor_bridge.sh` defaults `CURSOR_BRIDGE_INBOUND_QUEUE=0` when the
   feeder is not live — confirmed for both `absent` and `stale`, and it says
   why on stderr rather than turning the mode off silently. Positive control:
   a fresh heartbeat exports `1`.
4. Extension unit tests covering the touched files — run, below.
5. `Hotfix-Certification` left `pending`. Nothing here writes the ledger.

## The invariants (BL-654)

**Invariant 2** — "green tests alone never write certified or waived" —
`swarmforge/scripts/test/bl1253_stamp_ledger_human_decision_property_runner.bb`,
seeded LCG, 400 runs, over `hotfix_certification_lib/decide-entry-state`: the
one function that decides a ledger row's state.

Generator reach is CONSTRUCTED. Drawing the four fields independently puts the
all-green shape (ticket done, `human_approval: approved`, no human decision)
at 1-in-48, and its floor becomes a lottery — the failure this repo has now
hit three times, twice by me this week. The shape is drawn first and its
fields built to match; the wide independent draw remains underneath it.
Measured: 146 green-suite-no-decision, 197 human-decided, 203 undecided, 111
blank-decision, 80 no-stamp-ticket.

The property runs both directions, so it is not satisfiable by a function that
never finalises anything, and it checks the open/closed flag too — a decided
row left open would resurface a hotfix the human already answered.

| break | result |
|---|---|
| a done stamp ticket with an approved flag certifies on its own | FAILS, 146 draws |
| an approved human decision no longer finalises | FAILS, 94 draws |

**Invariant 1** — "this stamp-off never reimplements the hotfix" — carries a
**stated reason** instead of a property test, per the coder's invariant
contract. It quantifies over this PARCEL's diff, not over a pure module's
state space; there is nothing to generate, and generating arbitrary path sets
would test a fixture rather than the parcel. It is checked EXECUTABLY and
deterministically instead, in two places that fail if it is ever violated:
the Background asserts `git status --porcelain` is empty for all three hotfix
source paths, and scenario 05 asserts the same for `backlog/hotfix-ledger.yaml`.

## A duplicate stamp ticket, surfaced not resolved

`backlog/hotfix-ledger.yaml`'s row for 2ec06b6ef1 has
`stamp_ticket: BL-1260`, not BL-1253. **BL-1260 is a second stamp-off for the
same hotfix** — `backlog/paused/BL-1260-swarm-stamp-bridge-owns-getupdates-when-feeder-dead.yaml`,
titled "the Host bridge takes over getUpdates when the front-desk feeder goes
stale — certify hotfix 2ec06b6ef1", with its own feature file.

Which one survives is not the coder's call (a ticket-id collision retires the
loser, never renumbers it), so this is a `note` to the specifier at priority
`00` and nothing here touches either ticket or the ledger link.

Worth carrying into that decision: BL-1260's `approval_context` asks the human
a question BL-1253's does not — **confirm the 90-second stall window**, on the
grounds that when the bridge judges the front desk dead it starts polling the
SAME token, and a wrong judgement is the Telegram 409 class. That question is
real and this review does not answer it: `DEFAULT_FRONT_DESK_FEEDER_STALL_MS`
is 90_000 and the start script's `FRONT_DESK_STALL_MS` defaults to the same
90000, which at least agree. If BL-1260 is the one retired, that question
should not be retired with it.

## Runs

| what | result |
|---|---|
| BL-1253 acceptance | **7/7** |
| `bl1253_stamp_ledger_human_decision_property_runner.bb` | ALL PASS, 400 runs |
| `cursorBridgeInboundQueue.test.js` + `telegramCursorBridgeLive.test.js` | 128 passed |
| `telegramCursorBridgeCore.property.test.js` | 3 passed |
| suite inventory | ok — 439 files |

## What this parcel did NOT do

No hotfix file was edited, reverted or redesigned. The feeder/queue split, the
heartbeat format and the front-desk bot are untouched. Nothing was written to
the ledger, and no ledger link was made — the ticket records that as the
coordinator/operator step. `main` was not pushed. The standing property reds
behind the hotfix's own `SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1` override are
out of scope and were not investigated, as the approval_context says.
