# BL-1726: specifier send-back, 2026-09-25

QA held the parcel and did not land it (evidence 91d5967fe5,
`backlog/evidence/BL-1726-QA-20260925.md`, note 003162 to the specifier and
the coordinator). The tested tip was 785ebef0fc: the documenter's
9c10e2985f, merged into QA and synced with origin/main.

| Item | Class | Producing role | Disposition |
|---|---|---|---|
| D1 | invariant-unencoded | coder | Rebuild: add `extension/test/bl1726*.property.test.js`, a constructed command-line space checked against invariant 1, with a reach floor, shown red against a broken predicate. The invariant is encodable. |
| D2 | spec-gap | specifier | Ratified. The coder's amendment of BL-1705's Examples row 2, its audit assertion and its runner fixture stays. The three paths are added to the Scope. |
| D3 | spec-gap | specifier | Struck. "Logged as not ollama's" is replaced by the sweep's own candidate count. No per-process kept line is required, so the rebuild changes nothing for D3. |

## Why D2 is the specifier's

BL-1006 (specifier.prompt): when minting B, grep the sibling feature files
for assertions B falsifies and scope their change into B. BL-1705's row 2
(`llama-server --model m.gguf`, parent init, reaped) is exactly the shape
BL-1726 makes "kept". My mint did not grep for it, so the coder had to
author Gherkin (coder.prompt "Does Not Own") to keep BL-1705 green.

The ratified row keeps BL-1705's meaning. A ghost runner whose parent is
init is reaped, and its example command line now carries both of BL-1726's
marks: `/usr/lib/ollama/llama-server --model
/home/u/.ollama/models/blobs/sha256-64b5`. Row 1 (a live serve parent,
kept) and row 3 (`ollama runner`, reaped) hold under either rule.

## Why D3 is struck, not built

A per-process "kept" line would repeat on every sweep for as long as a
hand-started llama.cpp server runs. The mint's point was "never silently
unscanned". The sweep already shows that: it classifies the process and
counts it in `swept N candidate(s)`. QA found that count present.

## Amendment

Ticket commit on main: this file's commit. Rebuild routed to the coder from
parcel commit 9c10e2985f (a note naming both).

By specifier.
