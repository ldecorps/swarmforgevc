# BL-738 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Context

Coordinator note referenced BL-739 (pilot-process); coder deliverable for the
vacuous-property defect is BL-738 (chunking property reaches split boundary).

## Delivered

Replaced vacuous `splitTelegramChunks` property (200-char generator vs 4096
default) with `runChunkingProperty` helper using explicit `maxLen: 50` so
generator bounds stay independent of `TELEGRAM_MESSAGE_MAX_LENGTH`. Property
requires `sawMultiChunk` on every run (minLength 51 inputs).

Shared probe in `test/helpers/chunkingPropertyProbe.js` for acceptance scenario 02
falsifiability check via deliberate broken split.

## Verification

| check | result |
|---|---|
| chunking property (vitest) | pass + multi-chunk exercised |
| `run_acceptance.sh BL-738…feature` | **2/2** |
| broken-split probe | fails with counterexample |

By coder.
