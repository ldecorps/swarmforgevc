# BL-1566 — hardender review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Merged architect's `38e2b5e4f1` (BL-1566) into the hardender worktree;
  ancestry check clean.
- `mutation_cost: low` per the ticket — Babashka only, no mutation/CRAP/DRY
  tooling wired (BL-472 deferred). Degraded fallback: coverage-gap review
  plus a hand-authored mutant kill (this prompt's BL-567 pattern), not a
  wired tool run.
- `qa_hold_lib_test_runner.bb` (pure decisions, no filesystem) was solid
  for `release?`, `hold-status-lines`, `status-lines`, `released-holds`,
  `blocks-completion?` — every branch the coder/cleaner/architect passes
  already exercised. One real gap: `red-owner` (a `some`-fold over
  register rows) had only 0- and 1-row fixtures, so a position-dependent
  mutant (first-row-wins, reversed order) had nothing to catch it. Added
  a 2-row disagreeing fixture (match not first) and a wrong-red fixture.
  All pass.
- `qa_hold_cli.bb` had NO dedicated test file at all — its three
  subcommands were exercised only through the acceptance feature's happy
  paths (open→status→close, always closing a hold it just opened) and the
  pure lib runner (no filesystem, can't reach the CLI's I/O layer).
  Uncovered: `cmd-close!`'s own refusal when no open hold exists for the
  task (the one place `refuse!` fires in this ticket), open's `--red`
  repeated-flag accumulation path, and every subcommand's missing-required-
  flag usage exit. Added `qa_hold_cli_test_runner.bb` (subprocess-CLI-
  contract convention, matching `promotion_gates_cli_test_runner.bb`),
  registered standing in `suite-manifest.tsv` (trailing-tab convention
  preserved, per cleaner's BL-1566 note on that file's 4-column shape).
- Verified the close-refusal test has real killing power, not just
  coverage: copied the tree, hand-deleted `cmd-close!`'s
  `(when-not (fs/exists? f) (refuse! ...))` guard. First assertion draft
  (`str/includes?` on the task id alone) SURVIVED — the resulting
  `FileNotFoundException`'s message happens to embed the task id inside
  the file path, so the loose substring matched the exception text too.
  Tightened to require `cmd-close!`'s own literal message
  ("no open hold for task <id>"); re-ran against the mutant (FAILED, as
  required) and against the real code (PASSED); restored, deleted the
  scratch copy.
- No orphaned processes: `pgrep -fl 'node --test|stryker|bb .*qa_hold'`
  clean before and after.
- Re-ran full verification after the additions, all green: the two
  `.bb` unit runners (lib, new cli), `suite_inventory_cli.bb` (518 files,
  514 standing, 4 excluded — unchanged besides the one new row), the 8/8
  acceptance feature, and the four standing shell suites touching the two
  live callers (`test_done_with_current_arg_rejection.sh`,
  `test_ready_for_next_no_promotion.sh`,
  `test_done_with_current_work_note_evidence.sh`,
  `test_ready_for_next_rotate_home.sh`).
- Property Testing (undeclared): no pure JS/TS module touched, nothing to
  add — same conclusion as architect and cleaner.
- Invariant re-check: no new site touches `.swarmforge/qa-holds/` outside
  `qa_hold_cli.bb`; the new CLI test's own `close`-without-open-hold case
  confirms no delete/write happens on the refusal path (the refusal fires
  before `write-hold!`/`fs/delete` in `cmd-close!`), consistent with
  coder's stated-reason verification of "a released hold is never
  silently dropped."
