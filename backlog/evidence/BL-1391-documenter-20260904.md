# BL-1391 — documenter pass (2026-09-04)

## Received
`2fbbe9effb` (hardener; per `backlog/evidence/BL-1391-hardener-pass-20260904.md`).
Re-verified `swarmforge/scripts/handoffd.bb` still loads clean after this
merge (`bb -e '(load-file ...)'`, one definition of each cron-heartbeat
function, no `read-json` call) — this ticket also touches `handoffd.bb`
and independently rediscovered the same BL-1392 crash bug during its own
bounce cycle (`backlog/evidence/BL-1391-cleaner-bounce-rework-20260904.md`),
consistent with my own earlier finding this session
(`backlog/evidence/BL-1392-documenter-critical-main-broken-20260904.md`).

## What changed, operator-visible
The reconcile sweep's `:conflict` verdict always refused, correct for
code but costly for the append-only bookkeeping collisions that actually
happen daily on the shared checkout (QA's `abandoned_commits:` write vs
the specifier's `notes:` append, etc.) — four hand-merges on 2026-09-04
alone, the same class of daily deadlock BL-1390 closes from the push
side.

## Doc-domain review
`docs/how-to/BL-891-master-main-reconcile-sweep.md` is the established,
directly-relevant home — the same page BL-1386/BL-1387 extended this
session for sibling reconcile-sweep refinements.
- Added a new section ("Bookkeeping-only conflicts resolve instead of
  refusing") describing the bookkeeping set, the append-only test, and
  the never-silent absorb.
- Updated the "merge conflict / refuse-rematch" verdict row to note the
  narrowing, and added a new "bookkeeping conflict, resolved" row.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-891 summary line.
- Diagram registry checked — no diagram depicts reconcile-sweep internals
  at this granularity (same finding as every other reconcile-sweep ticket
  this session).

## Verdict
Doc content extended in the owning how-to page (not NONE) plus the
standard Specification.MD/index.md entries. Forwarding to QA.
