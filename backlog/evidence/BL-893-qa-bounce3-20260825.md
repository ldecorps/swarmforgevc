# BL-893 QA bounce 3 — 20260825

**Reviewed tip:** documenter `db1c8dde93` (not merged — tip impure)
**Task:** `BL-893-approvals-ambulance-choice`
**Sibling:** `VERIFY BL-893`
**Prior:** bounce `e941a4f25f`, bounce2 `b6b306aa5f`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main db1c8dde93  # fails
comm -12 … → backlog/evidence/BL-893-qa-bounce2-20260825.md
```

**Commit hash checked:** `db1c8dde93`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = b6b306aa5f bounce2)
paths=21 dels=1
deleted: backlog/evidence/BL-893-qa-bounce2-20260825.md
(tip merged bounce1 e941a4f25f only — raced before bounce2 land)
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto current `origin/main` (`b6b306aa5f`)
keeping **both** bounce evidence files. Observed tip that drops bounce2.

**Remediation:** `git reset --hard origin/main` (must include `b6b306aa5f`);
restore BL-893 product only; keep `BL-893-qa-bounce*.md`; dels=0.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
