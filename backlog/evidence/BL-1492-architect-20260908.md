# BL-1492 — architect review pass, 2026-09-08

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb /tmp/bl1492-repro.bb (decide-response called directly with a 2-entry budget-exhausting restart_history, once with in-window :at values, once with the same ages but :at shifted to read as future/negative-age)
- **Commit hash**: 4acf60b3a6
- **First error excerpt**: handoffd_supervisor.bb decide-response docstring: "A malformed or future :at (age negative, or not a number) never counts as recent - fails toward :halt, the safer BL-144 posture, never silently toward more restarts." Actual filter: (and (number? at) (<= 0 age) (< age budget-window-ms)) - an excluded entry is dropped from the recent COUNT, which LOWERS it, which makes (< count budget-count) MORE likely true, i.e. biases toward :restart, the opposite of the stated intent.
- **Failure class**: behavior
- **Expected vs observed**: With restart-history already at budget-count entries, decide-response must return :halt regardless of whether those entries individual :at values are malformed or read as future (e.g. after a clock correction/NTP step). Repro: {:at (- NOW 1000)} x2 -> :halt (correct); the SAME two restarts with :at (+ NOW 1000)/(+ NOW 2000) (future-looking, e.g. post clock-skew) -> :restart (wrong) - a genuinely exhausted budget silently re-arms itself under a clock anomaly, letting the daemon restart forever instead of ever reaching BL-144's halt, which is exactly the failure mode Invariant 2 ("never...while the daemon keeps dying") forbids.
- **Blamed role**: coder
- **Remediation pointer**: Treat an entry whose :at is malformed/non-numeric/future as counting toward the budget (i.e. as recent), not as excluded from it, so uncertain accounting fails closed toward :halt as the docstring already claims. Then fix the test/property assertions and prose in handoffd_supervisor_restart_budget_test_runner.bb (the two cases currently asserting :restart for malformed/future :at) and the decide-response docstring to match the corrected, actually-safe behavior.

By architect.

## Detail

Reviewed cleaner's `41d0c7fe6c` (coder commits `4acf60b3a6`, `18e9db818c`).

- **dependency-gate.js (REQUIRED HARD GATE)**: ran against the one changed
  JS file, `specs/pipeline/steps/bl1492RestartInPlaceSteps.js` — PASSED, no
  forbidden edges. The `.bb` files are outside the dependency-cruiser
  ruleset's scope (matches BL-361/BL-1491 precedent).
- **co-change-report.js**: ran against all changed files. Long co-change
  tail on `daemon_alarm_lib.bb`/`handoffd_supervisor.bb` is the expected
  whole-suite-commit noise across shared briefing/test-runner files (same
  shape noted in BL-1491's own pass); nothing flags a new suspicious
  coupling introduced by this parcel.
- **Two-layer boundary / extension host I/O / secrets / integrate-not-fork**:
  n/a — this parcel touches only `swarmforge/` (bb) supervisor internals,
  `specs/pipeline/steps/`, and test files; no `extension/src` or webview
  code.
- **required_wiring**: `restart_history` is written by `restart-daemon!`
  and rendered by `format-failure-log`'s existing
  `"restart_history: " (pr-str restart-history)` line (previously always
  nil per the ticket's own consumer-anchor note) — confirmed. The steps
  file is a top-level `*Steps.js` file in `specs/pipeline/steps/`, picked
  up by `index.js`'s non-recursive `HANDLER_SUFFIX` discovery — confirmed,
  no manual registration needed.
- **Invariants (BL-654)**: both declared invariants have real property-test
  coverage (`bl1492_restart_in_place_property_runner.bb`, P1/P2/P3, 150
  runs each) — ran green (`ALL PROPERTIES HOLD`). Non-vacuity narrated by
  hand at the top of the file (four deliberately-broken variants, each
  caught by a specific property, restored before commit) — accepted.
  P3 in particular formally proves time-translation-invariance, but none
  of P1/P2/P3 exercise a malformed/future `:at` inside an otherwise
  budget-exhausting history — that gap is exactly where D1 lives.
- **Invariant 2, "fails toward the safer posture" clause — VIOLATED (D1)**:
  see above. `decide-response`'s own docstring and
  `handoffd_supervisor_restart_budget_test_runner.bb`'s two malformed/
  future-`:at` cases both assert `:restart` while narrating that this
  "fails toward :halt" — the assertions and the prose contradict each
  other, and the actual behavior is the unsafe direction (repro above).
- **Tests run**: `handoffd_supervisor_restart_budget_test_runner.bb` ALL
  TESTS PASS (asserts the very behavior D1 flags as wrong — the test
  itself needs correcting, not just the code). Acceptance
  `specs/features/BL-1492-*.feature` 5/5 green via
  `node specs/pipeline/cli.js`.
- **test_handoffd_supervisor.sh**: case 01 fails on this sandbox exactly as
  on unmodified `main` (`git worktree` at `/home/carillon/swarmforgevc`,
  same command, same failure) — confirmed pre-existing and already
  registered as **BL-1498** (`backlog/standing-reds.tsv`,
  `backlog/evidence/test-handoffd-supervisor-unowned-red-adjudication-specifier-20260908.md`).
  Under `ZDOTDIR=<empty dir>` isolation (BL-1498's own recipe) cases
  01/03/04 (the BL-144 halt path, budget pinned to 0 for this suite) all
  PASS; case 02 is BL-1498's separately-diagnosed cause 2 (startup-grace
  vs. a fixture that predates it), also pre-existing. Not this parcel's
  defect — not re-ticketed.
- **Correctness**: one defect (D1). No other site violates the same
  property — it is one function (`decide-response`), the malformed/future
  branch of its own filter.
