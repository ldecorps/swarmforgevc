# BL-650 — specifier pass, 2026-08-06: NOT workable yet, and `main` is dirty with ticket-less code

Parcel received: `note` from coordinator, priority 10,
`Work BL-650-flow-watchdog-wall-clock-age-must-be-active-time`
(coordinator promoted it paused → active at `070c9ac0`).

Two findings. The first blocks BL-650; the second is unrelated to BL-650 but was
found in the same checkout and must not be swept.

---

## D1 — BL-650 is dependency-blocked: `depends_on: [BL-823]`, and BL-823 has not landed

`backlog/active/BL-650-…yaml` carries `depends_on: [BL-823]`.
`BL-823-availability-interval-ledger.yaml` is still in **`backlog/paused/`** —
it is not in `backlog/done/`, and nothing it specifies has been built.

This dependency is not decorative. BL-650's own description records the
2026-08-06 measurement that killed its original premise:

> The ticket asserted "the sources are already durable". Two of its three
> interval classes have NO durable interval record to read.

and the human's ruling that resolved it:

> "Prerequisite ticket: a small append-only interval ledger written at each
> pause/stop transition ships first, BL-650 depends_on it."

BL-650 is the subtraction arithmetic; BL-823 is the only thing that will write
the intervals it subtracts. A coder handed BL-650 today has no ledger to read,
so scenarios 01, 02 and 08 cannot be made to hold against anything real —
they would be built over fakes and go dark end-to-end the moment they ship
(the exact failure shape the specifier prompt's "live DATA dependency" rule
exists to prevent).

### There is no specifier work left on BL-650

The routing to specifier came from the ticket's stale `assigned_to: specifier`,
set when I wrote the 2026-08-06 premise correction. The spec itself is finished:

| check | state |
| --- | --- |
| prose `description:` | complete, incl. the 2026-08-06 correction + human ruling |
| `acceptance:` feature file | `specs/features/BL-650-….feature`, **git-tracked**, 10 scenarios |
| gherkin lint gate | **OK — parses cleanly** |
| `human_approval:` | `approved` |
| `invariants:` | 3 declared (BL-633) |
| `epic:` / `milestone:` | `swarm-reliability` / `M8` |

Nothing here needs a specifier. Re-routing it to specifier again would loop.

### Asked of the coordinator

1. Return **BL-650** to `backlog/paused/` (it is not workable, and it is
   occupying the single active slot).
2. Promote **BL-823** instead — `priority: 53`, `human_approval: approved`,
   `depends_on: []`, feature file tracked and lint-clean — and route it to
   **coder**, not specifier. Its `assigned_to: specifier` is stale for the same
   reason BL-650's was; the spec is done.
3. When BL-823 lands, BL-650 becomes workable and should be routed to **coder**.

I have deliberately not edited either ticket's `assigned_to`: routing is the
coordinator's call (specifier.prompt, "Does Not Own"), and BL-650 is currently
an active in-flight ticket whose YAML I should not be rewriting under the
BL-317/BL-325 worktree-staleness rule.

---

## D2 — ~380 lines of ticket-less functional code are sitting uncommitted on `main`

Surfaced, not swept, per BL-506 ("An Approval Authorizes Only Its Ticket's
Work") and the workflow rule "Ticket-less changes you did not make are
SURFACED, not swept". I did not write it, I have not staged it, and I have not
reverted it.

`git status` on the master checkout, all written in a single pass at
**2026-08-06T12:18:59Z** (~34 minutes before BL-650 was promoted at 12:52):

```
 M swarmforge/scripts/flow_watchdog_lib.bb            | 249 ++++++++++++++++-
 M swarmforge/scripts/test/flow_watchdog_test_runner.bb | 114 ++++++++++
 M swarmforge/scripts/handoffd.bb                     |   4 +-
 M swarmforge/swarmforge.conf                         |  16 +-
 M docs/reference/Specification.MD                    |  15 +-
```

**What it is:** a complete, unit-tested implementation of *spec-dependent
percentile thresholds* for the flow watchdog — `warn ≈ p67` / `escalate ≈ p97`
of historical mailbox residence, keyed by the alarm's own `(from→to, type)`
identity, with a fallback chain `from->to|type → *->to|type → global conf pair`
at `min-samples-for-calibration 8`, a calibrated table at
`.swarmforge/daemon/flow-watchdog-thresholds.json` refreshed every 6h, and
`handoffd.bb` extended to hand the sweep each role's `completed/` and
`abandoned/` dirs to calibrate from. Code, tests, config comments and
`Specification.MD` all move together.

**Why it needs a decision, not a shrug:**

- **It matches no ticket.** I grepped `backlog/{root,active,paused,done}` for
  `percentile` / `p67` / `p97` / `flow-watchdog-thresholds` / `spec-dependent`:
  the only hit is BL-659, an unrelated PWA ticket. No feature file exists for
  it (`git status` shows no untracked files).
- **It is not BL-650, though it is adjacent enough to be mistaken for it.**
  BL-650 shape item 2 asks for *pack-aware* threshold resolution (scenario 06,
  `rotation-pack-threshold-vs-parallel-pack-06`); this is *percentile-calibrated*
  resolution. Both keep resolution outside `decide-tier` — the in-code comments
  even cite "acceptance-05" — but they are different mechanisms, and this
  implements **none** of BL-650's actual subject: the active-time clock.
- **It is live code, not a scratch file.** `flow_watchdog_lib.bb` and
  `handoffd.bb` are loaded by the running daemon, and `swarmforge/scripts/` is
  hot-synced into every worktree on launch (BL-373). Uncommitted changes here
  are already executing, and will be overwritten without trace by the next
  hot-sync — so "leave it and see" is not a stable state.
- **It is pipeline code written directly on `main`**, which is precisely what
  BL-631 (babysitter detects pipeline work on main) and BL-632 (commit-time
  guard) are paused to prevent.

**Asked of the coordinator:** get a ruling on whether this work is kept
(retroactively ticketed — I will spec it, and it is a genuinely good idea that
would subsume BL-650's scenario 06) or dropped (`git restore` those five paths).
Either way it should not stay in this half-state across another hot-sync.

---

## Also observed, no action requested

Four human approval flips are uncommitted in the same checkout —
`BL-819`, `BL-820`, `BL-821`, `BL-823` each show exactly
`-human_approval: pending` / `+human_approval: approved`, plus a
`docs/briefings/.sent.json` bump. These are the concierge's writes, not mine.
Flagging only so nobody reads BL-823's approval as absent: the human **has**
approved it, the flip merely has not been committed.

By specifier.
