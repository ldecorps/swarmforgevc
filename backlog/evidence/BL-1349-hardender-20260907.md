# BL-1349 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Per-file budget (qa_e2e_procedure step 1): timed each of the 7 named
  files alone via `npx vitest run --config vitest.properties.config.mjs`.
  All pass, all under the 15s budget: bl1252Expensive 9.4s,
  bl1252IndexGuards 8.8s, bl1252Refusal 8.4s, bl1252UnexpectedFailure
  8.5s, bl1252Violating 9.1s, bl787NamedTunnel 11.7s, onboarderLauncherPidGuard
  13.3s.
- No-deletion check (invariant 1): counted `fc.property(`/`fc.assert(` at
  the pre-tuning parent (`7f0e5766c9^`) vs current — onboarder 2/2, bl787
  6/6, bl1252 union-of-five-splits 10/10. Exact match, confirming
  architect's own count independently.
- Spawn-only sample reduction (invariant 2): diffed each tuned file
  against its parent — only `numRuns` and explanatory comments changed
  (onboarder 15→2, bl787 6→2, each bl1252 split 120→60); no assertion
  narrowed or removed. Confirmed the bl1252 split files that don't
  directly call `spawnSync` still route through the shared
  `helpers/bl1252CommitGuardFixture.js`'s `runRunner`, which does — the
  reduction is properly scoped to spawn-heavy properties, not applied to a
  pure one.
- Acceptance: BL-1349's own feature 8/8 pass.
- Whole-lane run (`npm run test:properties`, detached, ~342s wall under
  concurrent live-swarm host load): 12 files/15 tests failed, none of them
  a file this ticket touched. Re-ran all 12 individually (including the
  already-owned bl968 standing red) immediately after: all 12 pass alone.
  Confirmed host-contention flakes from the full-lane parallel run, not a
  BL-1349 regression — none are in `backlog/standing-reds.tsv` as
  deterministic reds and none reproduce in isolation, so no unowned-red
  note is warranted (Article 4.2 concerns a red on `main`, not a
  contention-induced full-lane flake that clears on retry). The absolute
  341s wall figure is not a clean before/after comparison against the
  ticket's 143s baseline — that baseline was measured on 2026-09-02 under
  different (unstated) host conditions, and BL-1348 (fork count) is a
  separate, unlanded ticket — so I did not treat the wall-clock delta as
  evidence either way; the per-file budget checks above are the ticket's
  actual gate.
- qa_e2e_procedure's "17 pre-existing failures on the BL-1175 standing
  allowlist" step is stale: BL-1428 (2026-09-05) drained that allowlist
  file to zero rows and made `backlog/standing-reds.tsv` authoritative.
  Flagging for the documenter/QA rather than treating as blocking — the
  ticket's own invariants and scenario 02 are the actual gate on
  "no suppression."
- Retired the stale `backlog/standing-reds.tsv` row naming
  `bl1252CommitGuardAggregationInvariants.property.test.js` (added by the
  BL-1457 mint commit after this ticket's coder/architect split already
  renamed/removed that file) — verified the file no longer exists and its
  five successors all pass comfortably under budget per above.
- Observed but out of scope: `/tmp` carries ~444 `bl787-*-prop-*` /
  `sfvc-bl1252-*-prop-*` fixture directories accumulated over time from
  killed runs (both files already use the sanctioned `mkTmpDir` helper
  with `finally` cleanup on a normal exit — this is pre-existing kill-path
  debris, not something this ticket's diff introduced or worsened).
  Not swept: a prefix sweep here risks a concurrently-running worktree's
  live fixture (confirmed a live coder-worktree vitest process during this
  pass) — exactly the concurrency-bomb shape the standing rule warns
  against. Noted for awareness, not filed as a ticket on my own judgment.
- BL-113 soft gherkin mutation (the one `Scenario Outline:`, the `file`
  column with 7 rows): 7/7 mutants killed, 0 survived, 0 errors,
  `"outcome": "pass"`. Manifest written and committed (`15835ee223`).
- No orphaned `node --test`/stryker processes of mine remained; the live
  vitest tree found belongs to the coder worktree (confirmed via cwd),
  not this pass.
