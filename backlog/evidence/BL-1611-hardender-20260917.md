# BL-1611 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Babashka/Clojure — no mutation/CRAP/DRY tool wired (BL-472); gated only by
its own unit-test suite per the shared Design And Testability rule.

- `bb swarmforge/scripts/test/bl1611_drift_guard_in_process_property_runner.bb`
  → `ALL PROPERTIES HOLD: handoff-files-with-batches in-process existence
  (BL-1611, 150 runs)`.
- `env -u TMUX bash swarmforge/scripts/test/test_worktree_drift_guard.sh`
  → `ALL PASS` (6/6, including the new batch-shape scenarios 04-06).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1611-*.feature`
  → 6/6 pass.
- Regression: `specs/features/BL-1195-*.feature` → 3/3,
  `specs/features/BL-1515-*.feature` → 6/6, both unaffected.
- Hand-mutation (BL-567 fallback, no Stryker on `.bb`): reverted
  `has-in-process-parcel?` to the pre-fix flat reader
  (`handoff-lib/my-handoff-files`) — `test_worktree_drift_guard.sh`
  scenario 04 immediately failed with `WORKTREE_DRIFT_DETECTED` on a
  batch-held parcel, confirming the fix is load-bearing and the test
  non-vacuous. Reverted back before committing anything.
- **Live probe (qa_e2e_procedure step 3, run on this very hardening
  turn):** this session was itself holding this ticket inside a batch
  (`in_process/batch_20260917T113140Z_000001/`, alongside BL-1613).
  Deliberately created an untracked file under `swarmforge/scripts/`,
  then ran `./swarmforge/scripts/ready_for_next.sh` — it resumed the
  batch (printed the current batch's tasks) instead of refusing with
  `WORKTREE_DRIFT_DETECTED`. Removed the probe file immediately after
  (`git status --short` clean). This is the exact live scenario the
  ticket exists to fix, reproduced against this session's own worktree
  rather than only a fixture.
- No `.ts`/`.js` under `extension/src`/`extension/test` touched by my own
  pass; `npm test`/`npm run test:properties` already run fresh by the
  coder this session per the 2026-09-17 lane-set directive, not re-run.
