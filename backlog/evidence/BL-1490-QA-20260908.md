# BL-1490 — QA review pass, 2026-09-08

HELD — not approved, not bounced. Every required gate below is green; the
hold is Article 4.2 (standing-red register, ratified 2026-09-05): QA
approves no parcel whose evidence names a red with no open ticket in the
standing-red register, and this pass's own regression check reproduces
one.

By QA.

## Required gates (qa_e2e_procedure) — all green

- `bb swarmforge/scripts/test/bl1490_supervisor_tick_progress_property_runner.bb`
  → `ALL PROPERTIES HOLD` (200 draws, coverage floors met, real
  `mark-tick-phase!`/`mark-tick-idle!` marker exercised).
- Acceptance, this ticket's own feature:
  `node specs/pipeline/cli.js specs/features/BL-1490-a-poll-cycle-phase-is-never-invisible-to-the-supervisor.feature`
  → 5/5 scenarios pass.
- Adjacent regression sanity (my own initiative, not in the ticket's
  `qa_e2e_procedure`): `bb swarmforge/scripts/test/bl977_supervisor_progress_property_runner.bb`
  → `ALL PROPERTIES HOLD`;
  `bb swarmforge/scripts/test/handoffd_supervisor_startup_grace_test_runner.bb`
  → `ALL TESTS PASS`. Both directly exercise `evaluate-health`'s changed
  signature (`in-sweep-budget-ms` now via `effective-in-sweep-budget-ms`)
  and confirm it stays backward-compatible.
- Lineage: hardener merge, architect merge, cleaner merge and documenter
  commit `02fe9bcf90` all confirmed ancestors of the reviewed commit.
  `qa-sibling-check.js status --ticket BL-1490` → `VERIFY BL-1490` (no
  open deferral). No prior bounce for BL-1490 found in the master
  checkout's `.swarmforge/bounces/`.
- Diff read against `constraints`/`invariants`: `evaluate-health` stays a
  pure function over its observations map (only gains
  `effective-in-sweep-budget-ms`, computed from the same map, no new
  I/O); no raise of `SUPERVISOR_STALL_MS`/the BL-675 300s threshold; no
  free-running pulser thread introduced (`run-tick-phase!` runs inline,
  synchronously, inside the existing per-tick call sites); the `:dead`
  path is untouched (no diff near it). `mark-tick-phase!`/`mark-tick-idle!`
  reuse the existing `sweep-marker!` hook (BL-977), no new state-file
  format.
- `required_wiring`: `bl1490PollPhaseNeverInvisibleSteps.js` matches
  `specs/pipeline/steps/index.js`'s `*Steps.js` discovery predicate — no
  explicit registration line needed (confirmed by grep, same as the
  architect's and hardener's own checks).
- Diagram currency: no registered diagram (`docs/diagrams/*.mmd`)
  depicts individual daemon-supervisor internals at this granularity —
  confirmed by reading `architecture.mmd`'s own per-ticket comment log,
  which the documenter correctly notes stopped being updated after
  2026-08-27 (last dated entries are all `2026-08-27`, grepped
  independently). No diagram change-trigger fired.
- No orphaned `node --test`/`stryker` processes before or after
  verification (`pgrep -fl 'node --test|stryker'` clean, scoped to this
  worktree).

## The hold

Running BL-977's own acceptance feature
(`specs/features/BL-977-supervisor-never-halts-a-progressing-daemon.feature`)
as an adjacent regression check — not required by this ticket's own
`qa_e2e_procedure`, which names only the bb property runner above —
reproduces the same red the architect already found and reported
(`backlog/evidence/BL-1490-architect-20260908.md`, "Out-of-parcel
finding"): scenario "a wedged poll loop is still caught within the
in-sweep budget" fails identically before and after this parcel
(architect's own bisection against the pre-BL-1490 versions of the three
touched `.bb` files), root-caused to `bl977SupervisorProgressSteps.js`'s
fixture never backdating `handoffd.pid` so the 2026-09-02 startup-grace
hotfix forces `:healthy` before the wedged-marker branch is reached.
BL-977's own invariant 2 is thus currently unverified by its acceptance
suite.

Grepped `backlog/{active,paused,hold}`, `backlog/standing-reds.tsv`, and
ran `bb swarmforge/scripts/standing_red_register_cli.bb .`
(`"unowned": []`, count 26 — this red is not among the 26 registered
rows, all of which are currently owned) — no ticket owns this red as of
this pass, hours after the architect's note.

This is an `acceptance`-lane red on `main` per the register's own lane
taxonomy (`backlog/standing-reds.tsv` header) — squarely the shape
Article 4.2 (`standing-red-register-amendment-2026-09-05.md`) binds QA,
specifically, not to approve past. It is out of BL-1490's own scope (its
`out_of_scope` names BL-977 "done" and untouched) and this parcel's
changes make no difference to the failure either way — so it is not a
defect of BL-1490's own work and is not bounced (Article 4.2/QA-role:
"never a bounce, the parcel did not cause it"). It is also not
approved: the merge-up broadcast, the land, and the coordinator
bookkeeping note all wait for the ticket.

No second `note` sent for the same red: the architect's priority-`00`
note to specifier + coordinator already names this exact scenario, file
and root cause; re-sending would duplicate rather than inform (Article
4.4's one-report discipline applied to standing-red escalation). This
evidence file is the corroboration and the formal record of the QA-side
hold. `BL-1490` (severity `critical`, expedited per Article 3.2.4) stays
`in_process` with QA pending the specifier's ticket; re-verify (not a
fresh full pass — reconfirm the register only) once one lands.
