# BL-1767 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 58ec5ebd37 (task BL-1767), merged into QA as b91a09cb71,
then `Merge main 82fb31f689 into QA.` (adcc5950cc). All gates ran once on
adcc5950cc. BL-1767's own coder/hardender/documenter commits (915f7617a7,
5cc8a5f56a, 58ec5ebd37) are ancestors of it. The merge was diffed against
both parents: against the documenter parent it drops only the
bounced BL-1700/BL-1711 code that QA had already reverted (a5d90ae7c3,
f394a71671), and nothing of BL-1767's. `qa-sibling-check status`: VERIFY.
No earlier bounce is recorded for BL-1767, on origin/main or in the master
bounce store.

Gates (qa-gather, 10m30s wall):
- stragglers before/after: none in this worktree. The only live vitest was
  the coder worktree's own run, left alone.
- register: 6 rows, all owned, `unowned: []`
- `pre_qa_gate.sh` (required_wiring): OK
- `npm test`: exit 0
- `npm run test:properties`: exit 0 (539s). The only unhandled errors are
  BL-871's allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"`.
  The coder's invariant property
  `test/bl1767ModelStewardSafetyCardEntries.property.test.js` is inside the
  `test/**/*.property.test.js` include.
- `run_acceptance.sh` BL-547: 15/15 (7s)

The ticket's qa_e2e_procedure, one run each:
1. `bash swarmforge/scripts/test/test_model_steward_cli.sh`: ALL PASS, no
   FAIL line; 05, 05b, 05c, 05d PASS (1s)
2. `node specs/pipeline/cli.js`: BL-547 15/15 (7s, the step above);
   BL-556 6/6 (6s); BL-1079 5/5 (6s)
3. `bb swarmforge/scripts/test/model_steward_test_runner.bb`: ALL PASS
4. Scope diff: every path BL-1767's commits touch is either a Scope file,
   the permitted shared helper `specs/pipeline/steps/lib/modelStewardSafetyCard.js`,
   the coder's invariant property test, or evidence. No change to the
   steward lib, CLI, store or seed. One `.feature` path appears:
   hardender 5cc8a5f56a added a BL-113 mutation-manifest comment header to
   `BL-1079-a-cursor-identity-can-be-steward-certified.feature`. That header
   is tool-written, the same one 494 feature files on origin/main already
   carry, and it changes no scenario text. The approval context's FIRM
   line is "No scenario text changes in any feature file", so step 4's
   "no `.feature`" is read as that. It lands with the parcel.
5. REGISTER_ROW_RETIRED for the four BL-1767 rows is done in the landing
   commit (see the land record).

Intent: every planted passing card (bl547 x2, bl556 recruiter and
compliance-battery cards, bl1079 plantScorecard, shell 05/05c) builds its
safety entries from `model-steward-lib/safety-critical-competencies` at run
time, and 05d's expected refusal is built the same way. BL-1079 scenario
1's step drops the frozen anthropic count but keeps the sonnet-unchanged
deepEqual and the no-cursor-borrow check. The invariant is met.

By QA.
