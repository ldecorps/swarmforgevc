# BL-1542 — QA review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Sibling check: `qa-sibling-check.js status --ticket BL-1542` -> `VERIFY
BL-1542`. Register: no row for BL-1542. Ruling: A (bridge-preference-
store persistence, `human_ruling` recorded, `ruling_provenance: tapped`).

- Wiring gate: `pre_qa_gate.sh` -> OK.
- Acceptance: BL-1542 6/6 (control presence, collapse/expand, refresh
  stability, bridge-not-browser-storage persistence, no-working-ticket
  case). Regression BL-609 7/7, unchanged.
- Docs: `docs/how-to/BL-1542-live-screen-ticket-strip-collapse-control.md`
  present, linked from `docs/index.md`.
- `git diff main...HEAD` for the bridge/preference surface: `bridgeServer.ts`,
  `residentSpyUiHtml.ts`, `webUiFontSizePreference.ts` (BL-609's, touched
  incidentally by the shared preference-store refactor),
  `webUiTicketStripCollapseRoutes.ts` and their tests — matches ruling A's
  scope.
- qa_e2e steps 2-5 (phone-width viewport via Bubble, Telegram Mini App
  cross-surface check, live 10s-refresh wait against a real rotation
  pack) are LIVE/manual verification against a real target swarm with a
  real Telegram client — outside this QA pass's reach (no live target
  swarm, Bubble device, or Telegram bot session available in this
  verification context; same testability-boundary limitation as
  BL-1582's Onboarder pass). The acceptance suite's 6 scenarios are the
  automated substitute the project's testability rules intend for this
  boundary (webview/live-client surfaces) and cover the same behavioral
  claims (collapse state, refresh stability, bridge-not-storage
  persistence) programmatically against the real bridge server routes.
- Unit suite: `npm test` — 628/628 files, 10757/10757 tests, exit 1 from
  the unhandled-error count alone (all 628 are the identical known-benign
  `[vitest-worker]: Timeout calling "onTaskUpdate"` class, BL-871 —
  verified via `grep -c` and a uniqueness check, no other error type
  present). "suite file budget OK: 628 files, all within 7.0s" — the
  budget gate itself is clean. Run under the most extreme host
  contention observed all session (load average 27.76, only 1 vitest
  fork available, 532s wall clock vs. the usual ~60-100s) — not a
  regression from this parcel.

By QA.
