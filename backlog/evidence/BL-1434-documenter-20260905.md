# BL-1434 documenter review pass — 2026-09-05

Reviewed the hardened commit (`0c406c5f3a`) and the coder/cleaner/architect/
hardener trail for BL-1434 (the host-activity-feed property registers its
trials).

Checklist:
- `required_wiring` `extension/test/hostActivityFeed.property.test.js::vitest`
  anchor present: the file now registers its forty trials with vitest
  (`test(...)`, not a bare top-level script ending in `process.exit(1)`).
  Step handler exists:
  `specs/pipeline/steps/bl1434HostActivityFeedPropertyRegistersSteps.js`.
- Confirmed invariant 1 (every original check — the feed never invents a
  line, the bound holds, a quiet session reads quiet — is a registered
  assertion whose failure is a red vitest run, never an unseen process
  exit), invariant 2 (the register row and the allowlist row for the
  file are both removed in this same commit — confirmed
  `backlog/standing-reds.tsv` and
  `swarmforge/scripts/property_suite_standing_allowlist.tsv` diffs each
  show exactly one row removed), and invariant 3 (no
  `extension/src/bridge/hostActivityFeed.ts` change — only the test file,
  register, and allowlist).
- Grepped `docs/how-to/` for `hostActivityFeed` and found a genuinely
  STALE reference: `docs/how-to/BL-1206-drain-the-node-test-import-entries-from-the-property-allowlist.md`
  (authored by me earlier this session) stated the allowlist "holds
  exactly one row: `test/hostActivityFeed.property.test.js` ... deliberately
  left alone" as the parcel's END state — true when written, but this
  ticket's own land clears that exact row and corrects the
  mis-attribution the architect caught (the row's own note wrongly said
  "node:test import; fails collection" when the file actually had no
  registration at all). Updated that paragraph to describe what the row
  was and that BL-1434 cleared it and fixed the mis-attribution, rather
  than leaving it read as still-standing present tense.
- The `docs/how-to/BL-833-host-agent-activity-feed.md` and
  `docs/how-to/BL-1220-unit-lane-node-test-import-guard.md` hits for
  `hostActivityFeed` are about the unrelated `.test.js` unit file (BL-1220)
  and the feed's own behavior (BL-833), neither of which this ticket
  touches or contradicts — no edit needed there.
- No "Last Updated" freshness field in the edited doc — no date bump owed.
- No registered diagram depicts property-test registration status — no
  diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond the doc fix above.

Result: NONE — no defect found in the parcel itself; one stale reference
in a sibling ticket's doc (my own earlier pass) found and fixed.
