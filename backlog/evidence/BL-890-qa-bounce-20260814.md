# BL-890 — QA bounce — 2026-08-14

## D1 — documenter pass missing entirely; no doc/changelog update anywhere in history

1. **Failing command**: not a test command — checked whether any
   documenter-authored commit or doc/changelog change exists anywhere in
   the parcel's history:
   `git log --oneline --ancestry-path 16d67151e..7c8218d373` and
   `grep -n "BL-890" docs/reference/Specification.MD docs/index.md`.

2. **Commit hash checked out and tested**: `7c8218d37` (documenter's
   forward to QA, `merge_and_process documenter 7c8218d373`), merged into
   this worktree as `8d573bfa2`.

3. **First error excerpt**:

   ```
   $ git log --oneline --ancestry-path 16d67151e..7c8218d373
   7c8218d37 Merge commit '16d67151e4' into swarmforge-documenter

   $ grep -n "BL-890" docs/reference/Specification.MD docs/index.md
   (no output)
   ```

   The only commit on top of the hardener's own pass (`16d67151e`) is a
   plain merge into the documenter's own worktree — no "By documenter."
   commit exists anywhere in this ticket's history, and `BL-890` appears
   nowhere in `docs/reference/Specification.MD` or `docs/index.md`. This
   is the same session's sibling ticket **BL-746** — also a defect fix to
   test/acceptance infrastructure with no production-code change — which
   *did* receive a documenter changelog entry ("BL-746: BL-637's stop-path
   shell scenarios ... No production behavior changed; see the new
   test-infrastructure-hygiene entry below."). BL-890 is the same shape
   (defect fix to an acceptance step-handler, `required_stages` lists
   `documenter`, no `stage_skip_reasons.documenter` recorded anywhere in
   `backlog/active/BL-890-...yaml`) and got no equivalent entry.

4. **Failure class**: `behavior` — per QA's own bounce-routing rule: "a
   stage's pass is MISSING ENTIRELY ... (e.g. no documenter doc/changelog
   update for the ticket anywhere in history...) → THAT stage. The absence
   of a stage's output is that stage's own defect." (Article 4.3 / BL-575
   precedent.)

5. **Expected vs observed**: Expected — a documenter-authored commit
   updating the changelog (or an explicit, recorded reason why BL-890
   needs none, parallel to BL-890's own legitimate
   `stage_skip_reasons.architect` entry). Observed — no documenter commit,
   no changelog entry, and no recorded skip reason for the documenter
   stage anywhere in the ticket's YAML or commit history.

## Everything else checked — no other defects found

- `node --test specs/pipeline/test/steps/dispatchGapSteps.test.js` —
  18/18 PASS, including all 4 BL-890-specific scenarios (re-run
  independently by QA).
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-890-cadence-wiring-check-is-position-independent.feature`
  — 3/3 PASS (re-run independently by QA).
- `npx vitest run --config vitest.properties.config.mjs bl890` — 2/2 PASS
  (re-run independently by QA).
- `required_wiring` literal `chase-sweep-every-cycles` confirmed present
  verbatim in `specs/pipeline/steps/dispatchGapSteps.js:24` (the check
  still anchors on the real cadence conditional, not weakened to a no-op).
- Architect skip is legitimate and properly documented:
  `stage_skip_reasons.architect` in the ticket's own YAML, and the
  cleaner→hardener chain confirms a real cleaner pass ran ("By cleaner."
  on `2e997dca3`) before hardening — this is the BL-890-specific pattern,
  unlike BL-891/BL-746's undocumented architect skip bounced separately
  this same session.
- Hardener's mutation-sweep claim (4/5 mutants killed outright, 1 survivor
  closed with two new regression tests) independently spot-checked against
  the diff and the passing 18/18 unit count — consistent.
- No orphaned `node --test`/`stryker` processes before or after QA's runs.
- No prior bounce history for BL-890 on the `main` ref.

The implementation itself is sound and independently re-verified by QA.
The sole blocker is procedural: the documenter stage produced no output
of its own — it merely merged and forwarded the hardener's commit.

## Remediation pointer

Documenter: add a changelog entry for BL-890 to
`docs/reference/Specification.MD` (parallel to the BL-746 entry already
present in this same batch's history: "BL-890: BL-222's cadence-wiring
check asserted `dispatch-gap-sweep!` within a stale 600-character window
... replaced with a position-independent structural check ... no
production behavior changed"), or, if genuinely no doc update applies,
record that reasoning explicitly (e.g. a `stage_skip_reasons.documenter`
entry or an equivalent note) before forwarding to QA again — no code
changes are requested by this bounce.

By QA.
