# BL-1474 — QA review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Received `73fe9ddfd9` (documenter merge-up). Note: the hardender made a
later, unforwarded evidence-only addendum on their own branch
(`c68330e0a2`, re-verifying mutant 2 via the actual pinning property test
per their own newly-accepted rule_proposal, per `f257b24c96` — reviewed;
paperwork-only, no code or verdict change, not required for this pass).

Gates run, all green:
- `qa-sibling-check.js status --ticket BL-1474`: `VERIFY BL-1474`.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: `ALL PASS`.
- Property test in isolation
  (`bl1474ReplayCommitRefusalReasonInvariants.property.test.js`): green.
- Full unit suite: 612 files / 10358 tests green (no production TS touched
  by this ticket; confirms no regression).
- Acceptance, `BL-1474-the-replay-reports-why-its-commit-was-refused.feature`:
  5/5 pass.
- Regression check, `BL-1447-a-replay-missing-a-parcel-path-is-refused-before-publish.feature`
  (qa_e2e_procedure item 4): 3/3 pass, unaffected.
- Wiring: `bl1474ReplayReportsWhyRefusedSteps.js` auto-discovered by the
  `*Steps.js` top-level convention (same mechanism verified for BL-1477).
- Docs: `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`
  gained the "carries the refusing guard's message" row, per direction.
- No orphaned test/mutation processes.

Land (BL-1241 remedy, same as BL-1477 moments earlier — this branch is the
same long-lived pipeline lineage): synced `origin/main` into the branch
fresh (twice, as origin/main advanced twice more from concurrent role/
coordinator activity — once landing BL-1477's own replay, once the
coordinator's bookkeeping promoting BL-1441) before running
`land_step_cli.bb BL-1474-the-replay-reports-why-its-commit-was-refused
8af7688f51`. Returned `LAND_REPLAY land-replay/BL-1474-8af7688f51
2d2138ec2c` with a long entangled-sibling list (same shared-branch shape as
BL-1477's), including `LANDED_SIBLING` lines for six already-landed
siblings and one `PASSENGER_SIBLING BL-1463` (an APPROVED, already-`done`
sibling whose shared-path content in `land_step_lib.bb` rode along per the
2026-09-03 ruling — the tool ran its passenger-guard re-check internally
and still returned a clean replay, not an escalate).

Reviewed the replay tip's diff vs `origin/main`: BL-1474's own 8 files as
expected, PLUS one incidental line —
`backlog/done/M8/BL-1477-the-context-telemetry-producer-records-again.yaml`
gained `abandoned_commits: [ccd92e1751]`. This is my own prior BL-1477
QA bookkeeping commit (made after I had already pushed BL-1477's own
replay directly, so it never got its own landing) riding along as an
untagged-merge passenger. Confirmed by diff and by byte-identical
`land_step_lib.bb`/`land_step_lib_test_runner.bb` content against my
already-fully-tested worktree tip: benign, factual, self-authored, and
already covered by my own BL-1477 evidence — accepted rather than
hand-stripped.

Landed via the BL-1144 discipline from a detached temp worktree at the
replay tip (land_main_publish.sh's `--decide-only`/push run from the
worktree HOLDING the candidate, not the master checkout) — lock acquired,
`:next :push` (`:origin-advanced-since-gate false`), pushed `2d2138ec2c` to
`origin/main` (`150091890a..2d2138ec2c`), lock released, temp worktree
removed. `is_qa_ancestor.sh 2d2138ec2c` confirms `approved` (auto-recorded,
source `8af7688f51`, BL-1334).

`abandoned_commits: [8af7688f51]` recorded on the ticket (the pre-replay
QA merge commit cited to `land_step_cli.bb`; the landed commit is the
replay `2d2138ec2c`).

By QA.
