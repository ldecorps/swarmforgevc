# BL-1559 — hardender review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Reviewed against merged commit `d6880f0332` (architect's tip, merged
`--no-ff` into hardener at `1e2f...` see this parcel's merge commit).
Mutation cost: low (bb test scripts + one step-handler file, no
production/Stryker target — Babashka has no mutation/CRAP/DRY wired,
BL-472, this is the documented fallback).

Checks run:
- `bb swarmforge/scripts/test/draw_schedule_lib_property_runner.bb`
  (coder-authored invariant property, 300 seeded draws, runs 9..48) —
  `ALL PROPERTIES HOLD`.
- Standing whole-tree guards (parcel touches `specs/pipeline/steps/`):
  `npx vitest run $(ls test/*Guard*.test.js | grep -v '\.property\.')`
  from `extension/` — 18 files, 183 tests, all pass.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1559-the-bl983-runner-constructs-its-seat-schedule.feature`
  — 14/14 scenarios pass (all three Scenario Outlines).
- `swarmforge/scripts/check_feature_handler_registration.sh` — exit 0.
- QA e2e step 1 (real runner, by name, 15 of 15): detached via
  `swarmforge/scripts/detach_job.sh` (host load 2.7-3.6 on 20 cores,
  each run ~85-115s). Result: 15/15 print `ALL PROPERTIES HOLD`, 0 FAIL
  lines. Every `generator coverage:` line met the floor: two-seat
  8-11 (floor 6), three-seat 5-8 (floor 3), all-busy 7-8 (floor 4),
  redeliver 8 (floor 4), forward 8 (floor 4), across all 15 runs — never
  merely a pass count (BL-1559's own qa_e2e_procedure warns the unfixed
  runner's 15/15 happens ~1 run in 5; here every individual coverage
  count clears its floor, not just the aggregate pass).
- QA e2e step 2 (`PROPERTY_RUNS=9`, exact-fill): one run —
  `{:two-seat 6, :three-seat 3, :all-busy 6, :redeliver 8, :forward 8}`,
  `ALL PROPERTIES HOLD` — the nine quota cells exactly fill all nine
  draws as the ticket predicts. A second same-day attempt hit the tool
  timeout under concurrent load from the still-running 15-run job
  (exit 124, no output) — re-run separately once the 15-run job's own
  process was confirmed to be the contention (its child correctly
  belonged to the detached job's pgid, not an orphan); not re-attempted
  a second time since step 1 already demonstrates the exact-fill
  behavior at 9 draws is real (RUN 5's PROPERTY_RUNS=9 pass above).
- QA e2e step 4 (break-then-restore, manual mutant, no detached job
  outstanding at the time): `seat-schedule` hand-mutated to
  `(vec (repeat runs {:n-seats 3 :n-parcels 1}))` → runner printed
  `generator coverage: two-seat reached only 0 of 16 (floor 6)` and
  `all-busy reached only 0 of 16 (floor 4)`, exit 1 — exactly the
  failure text the ticket's non-vacuity note (break 4) predicts.
  Restored verbatim from a pre-edit backup; `git status --short` clean;
  re-ran the property runner green after restore.
- `git diff main...HEAD --name-only`: the runner, the new lib, the new
  property runner, the new step handler, evidence files only —
  `swarm_handoff.bb`, `ready_for_next_task.bb`, `send_through_audit.bb`
  and every other production script untouched, matching QA e2e step 5.
- No orphaned test/mutation processes at any point (`pgrep -fl 'node
  --test|stryker'` clean before start); the detach job's own process
  group exited cleanly (`[detach_job] EXIT=0`) with no leaked fixture
  dirs or child processes afterward.

QA e2e step 6 (standing-red register row removal) is left for the land
that turns the runner green on `main` (this parcel is not yet landed);
the register currently still carries this file's row under BL-1559
(`owned: true`, `first_seen: 2026-09-14`), as expected pre-land.

No defect found. Coder's declared invariant, cleaner's and architect's
NONE passes, and this hardening pass agree: every floor is met by
construction, never by a sampled draw, matching BL-1062's rule and the
FIRM constraints in the ticket's `approval_context`.

By hardender.
