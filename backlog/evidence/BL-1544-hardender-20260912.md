# BL-1544 — hardender review pass, 2026-09-12

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb swarmforge/scripts/test/land_step_lib_test_runner.bb (hand-mutated :any-ambiguous? some->every?)
- **Commit hash**: 40184c8e02
- **First error excerpt**: FAIL BL-1544 (03b): a genuinely ambiguous touch is never silenced by a second, non-ambiguous co-touch on the same path
- **Failure class**: test-gap
- **Expected vs observed**: a second commit whose subject leads with an unrelated ticket must never silence a genuinely ambiguous co-touch on the same path (some-fold, not every?-fold)
- **Blamed role**: hardener
- **Remediation pointer**: added scenario 03b fixture in land_step_lib_test_runner.bb; verified kill by hand-mutation, restored source, re-verified green

## D2

- **Failing command**: bb swarmforge/scripts/test/land_step_lib_test_runner.bb (hand-mutated subject-attribution count guard > -> >=)
- **Commit hash**: 40184c8e02
- **First error excerpt**: FAIL BL-1544 (hardening): a single, non-leading ticket mention is that id's alone, never ambiguous
- **Failure class**: test-gap
- **Expected vs observed**: a subject naming exactly one ticket id without leading it must read {:ambiguous? false}, never widened to ambiguous
- **Blamed role**: hardener
- **Remediation pointer**: added a direct worked-example assertion in land_step_lib_test_runner.bb; verified kill by hand-mutation, restored source, re-verified green

## Detail

Both D1 and D2 are hand-authored mutation sweep survivors found and fixed
IN THIS SAME PASS (commit `40184c8e02`) — not a bounce to another role.
Rationale for the hand-authored sweep (BL-638): this ticket's own feature
file has no `Scenario Outline:`/`Examples:`, so BL-113 Gherkin mutation is
inapplicable; `land_step_lib.bb` is Babashka, which has no Stryker/CRAP/DRY
wired (engineering.prompt). Each candidate mutant was applied to
`land_step_lib.bb`, both the unit and property suites re-run to confirm
survival, a discriminating test written, the mutant re-applied to confirm
the new test goes red, then the source restored and the new test
re-confirmed green.

Gates run this pass, all green after the fix:
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`
- `bb swarmforge/scripts/test/bl1544_ambiguous_subject_property_runner.bb`
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1544-....feature` (4/4 scenarios)
- Regression: `task_scope_gate_lib_test_runner.bb`,
  `pipeline_stage_lib_test_runner.bb`,
  `bl1472_revert_reapply_transparent_property_runner.bb`,
  `bl1473_replay_no_untouched_path_property_runner.bb`,
  `suite_inventory_cli.bb` (513/509/4, unchanged)
- Whole-tree guards (this parcel adds a file under `specs/pipeline/steps/`):
  `npx vitest run extension/test/*Guard*.test.js` minus `.property.` —
  18 files, 183 tests, all pass.

CRAP/DRY: out of scope — no `src/*.ts` touched by this ticket; Babashka
has neither wired.

By hardender.
