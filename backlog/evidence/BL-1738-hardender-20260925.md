# BL-1738 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `specs/pipeline/steps/lib/operatorRuntimeFixtureGitRoot.js` (new
helper), `bl1738OperatorRuntimeFixtureGitRootSteps.js` (new, pinned
handler), fourteen converted handlers, `humanInTheLoopClosedSteps.js`
(BL-325's named reference fix), `extension/test/step-handler-tmp-root-census.txt`
— all plain Node acceptance step handlers / a JS fixture helper; no
`src`/`out` production TS or `.bb` production code touched (CRAP/Stryker
not applicable). No feature file's Scenario/Examples/narrative changed
anywhere (confirmed: `git diff --stat f93ebdf4af..HEAD -- specs/features/<all fifteen>`
empty).

Checks run (host load 2.9-5.3 on 20 cores, quiet):
- `node specs/pipeline/cli.js specs/features/BL-1738-every-operator-runtime-acceptance-fixture-roots-itself-in-a-git-checkout.feature`:
  15/15 ok.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js` (from `extension/`):
  4/4 passed — the standing BL-1636 census guard the coder's edit was
  required to satisfy.
- Spot-ran a broad sample of the fourteen converted features, chosen to
  cover files NEITHER the cleaner nor the architect had already run
  (their passes covered BL-325, BL-307, BL-413, BL-877, BL-1224 — this
  pass covered the rest): GH-26 5/5, BL-310 4/4, BL-354 5/5, BL-318 4/4,
  BL-282 5/5, BL-466 2/2, BL-369 6/6, BL-359 6/6, BL-368 3/3, BL-460
  18/18 — all green, matching the ticket's expected counts exactly.
  BL-458: 5/6 — confirmed the ONE failure is scenario 05 ("an
  interrupted step file still tears down its detached process tree"),
  exactly the ticket's own predicted pre-existing failure (owned by
  BL-1750), not a regression. Between this pass and the cleaner's sample,
  every one of the fourteen converted features plus BL-1738's own has now
  been run at least once by a reviewing role.
- `git diff --stat f93ebdf4af..HEAD -- <every Scope file>`: matches
  exactly — nothing extra, nothing missing.

Hand-mutation (no wired mutation tool for `specs/pipeline/steps/*.js`,
mutation_cost: medium — BL-638-class fallback applied to the JS fixture
lane): dropped the `git init` call from `gitifyFixtureRoot` in
`operatorRuntimeFixtureGitRoot.js` in place (no detached job outstanding).
- BL-1738's own feature (scenario 01, the live helper+CLI accept/refuse
  check): 14/15, the mutant caught immediately — killed by its own
  declared-invariant scenario, independent of any downstream handler.
- A downstream converted feature (GH-26, chosen as a representative
  single-root conversion): 0/5 — every scenario failed with the refusal
  reintroduced, confirming the helper's fix is genuinely load-bearing for
  every handler routed through it, not just the one scenario that tests
  it directly.
- Restored the file byte-for-byte (`git status --short` clean); re-ran
  both BL-1738 (15/15) and GH-26 (5/5) green before finalizing.

Guardrail re-read: concur with the architect's read on
`proveFixtureIsolated`'s `startsWith`-without-separator-boundary shape
(same class reviewed for BL-1728's `bl906` handler and BL-1738's own
`humanInTheLoopClosedSteps.js` conversion) — `gitifyFixtureRoot` always
runs `git init` on `root` immediately before the proof in every call site
this parcel introduces, so `commonDir` is deterministically `<root>/.git`
at check time; the "nested inside a live enclosing checkout" false-pass
class cannot arise here by construction.

Fixture hygiene: `find /tmp -maxdepth 1 <prefix patterns> -newermt
'-10 minutes'` returned 0 during and after this pass's own runs — no
fixture root leaked from any run in this pass. The two long-running
`operator_runtime.bb` processes found on the host (`ps -o etimes`:
3073s/6539s elapsed) predate this pass by 50+ minutes and are unrelated
live-swarm daemons (one under this repo, one under a different project),
not this ticket's fixtures.

Not run: `npm run test:properties` (2026-09-17 rule — this parcel touches
no `*.property.test.js`) and Stryker/CRAP (no `src`/`out` file in Scope);
BL-877's own feature (~300s alone) was not re-run here — already sampled
green (6/7, the ticket's own predicted BL-1749 exception) by the
architect's pass, and its unique failure mode is unrelated to the class
this hand-mutation exercises.

By hardender.
