# BL-811 — QA pass

Reviewed commit (this parcel): `147ac0d2` (merge of documenter's `57f1f29998`
into QA — documenter received unchanged from
`6d86cba7`/hardener's `b1391182`, which received unchanged from
architect's `671b3692`, which received unchanged from cleaner's `a84ea970`).
Ancestry check: `git merge-base --is-ancestor a84ea970 147ac0d2` — true.
Coder/cleaner/architect/hardener/documenter work is confirmed inside the
commit this evidence approves.

## Verification Order — sibling check

`node extension/out/tools/qa-sibling-check.js status --ticket BL-811` ->
`VERIFY BL-811` (exit 0). No open deferral; full verification pass run below.

## Process hygiene

`pgrep -fl 'node --test|stryker'` clean before this pass and reconfirmed
clean after every check below. No orphaned test/mutation processes.

## Compile

`npm run compile` (extension/) — clean.

## Unit / regression suites

```
$ npx vitest run test/telegramCursorBridgeLive.test.js test/telegramCursorBridgeCore.test.js \
    test/telegramFrontDeskBotCore.test.js test/cursorBridgeInboundQueue.test.js \
    test/telegramCursorBridgeLiveness.test.js
 Test Files  5 passed (5)
      Tests  609 passed (609)
```

Matches hardener's own reported count (609/609, +1 regression test over the
coder's original 608).

## Declared-invariant property tests (BL-654)

```
$ npm run test:properties -- test/bl811HostQueueInvariants.property.test.js
 Test Files  1 passed (1)
      Tests  3 passed (3)
```

All three ticket-declared invariants carry a property test or a stated,
verified non-encodability reason (invariant 3 — see architect's evidence,
independently spot-checked below).

## Acceptance (BL-112) — the ticket's own `acceptance:` field

```
$ bash specs/pipeline/scripts/run_acceptance.sh \
    specs/features/BL-810-host-queue-selection-poll-clear-all-and-ttl.feature
# tests 12
# pass 12
# fail 0
```

12/12 (10 scenarios, two Scenario Outlines each expanding to 2 rows),
including scenario 10 ("the vote reaches the bridge over the front desk's
existing fan-out") and scenario 12 ("an expired question is not offered for
selection") — this is my own independent run, not a re-read of the coder's
or architect's.

## required_wiring — all six re-checked directly against source (not re-read from evidence)

1. `specs/pipeline/steps/index.js::bl810HostQueuePollClearAllTtlSteps` —
   `require('./bl810HostQueuePollClearAllTtlSteps')` at line 381.
2. `telegramCursorBridgeLive.ts::sweepExpiredQueuedPrompts` — defined line
   373, called from the poll-loop tail at line 1677
   (`await sweepExpiredQueuedPrompts(deps, holder, handlerCtx.post);`).
3. `telegramCursorBridgeLive.ts::clearAllOptionIndex` — real poll option,
   computed at line 1410 (`options.length - 1`) and passed into the posted
   poll at line 1421.
4. `telegramCursorBridgeCore.ts::clearAllOptionIndex` — parsed/persisted at
   lines 664-665 (`typeof value.clearAllOptionIndex === 'number'` guard
   before assignment), field declared line 40.
5. `telegramCursorBridgeCore.ts::originTopicId` — parsed/persisted at lines
   648-649, field declared line 33.
6. `telegramCursorBridgeLive.ts::poll_answer` — still the dispatch key at
   line 1605 (`if (update.poll_answer) { ... }`), driving both
   `processQueuedPollAnswer` and `processChoicePollAnswer`.

## D1 — vote-retraction queue wipe (confirmed fixed, read directly)

`telegramCursorBridgeCore.ts:62`:

```ts
if (typeof pendingPoll.clearAllOptionIndex === 'number' && selectedIndex === pendingPoll.clearAllOptionIndex) {
  return { kind: 'clear-all' };
}
```

The `typeof ... === 'number'` guard closes the `undefined === undefined`
hole directly — read myself, not taken on the coder's/architect's word.
Property test 2 in `bl811HostQueueInvariants.property.test.js` covers this
generatively (1000 runs including all three "missing" shapes); the
hardener's added integration-level regression test drives the same fix
through the real `runCursorBridgePollOnce` path with a persisted
legacy-shaped poll. Both green above.

## D2 — help-text and liveness-line changes (confirmed correctly scoped, independently re-checked)

`git show 2b8d19d1 --stat` (the original hotfix commit) confirmed directly:
`extension/src/tools/telegramCursorOperatorCore.ts` — the file that owns
`SOFT_VERBS`/`HARD_VERBS` dispatch — is **not** in its file list. The hotfix
touched only `telegramCursorBridgeCore.ts` (help text), `telegramCursorBridgeLive.ts`,
and `telegramCursorBridgeLiveness.ts` (4 lines). This independently confirms
the coder's and architect's claim: `/pause`/`/resume`/`/stop`/`/start` were
already-dispatchable, already-shipped commands (`f9b38f53`) — the hotfix only
corrected drifted help text to match them, no new unreviewed surface. Both
D2 items confirmed intended; no follow-up ticket needed.

## CRAP / DRY — hardener's findings reviewed, not re-run (QA does not run mutation, Article "Does Not Own")

`processQueuedPollAnswer` at CRAP 17.03 (over the 6 threshold) is
pre-existing debt, not introduced by this parcel — hardener's evidence shows
this parcel's own extraction of `decideQueuedPollAnswerAction` *reduced*
complexity from 19 to 17, and the ticket's own `out_of_scope` excludes
broader queue-architecture refactors. Not a bounce item: no new debt, fix
would require exactly the out-of-scope refactor. The two `jscpd` clones are
confirmed pre-existing (present byte-for-byte before this parcel per
hardener's diff check) and are cleaner-stage territory, not QA's or this
parcel's to fix.

## Host load

`uptime` during this pass: load averages 56-80 on a 4-core host — elevated,
consistent with the load already documented and accepted across this week's
QA passes (BL-807, BL-809, BL-813, BL-638). Every check above completed
without stalling; no gate was skipped or degraded because of it.

## Untracked file surfaced, not swept

`swarmforge/scripts/operator_path_lib.sh` (untracked, pre-existing, unrelated
to this ticket) is present in this worktree. Confirmed not part of this
parcel's diff; left unstaged and unswept per BL-506 discipline (same file
already surfaced in BL-807's QA evidence).

## Out of scope for this pass: live-deploy manual E2E

The ticket's own `notes` E2E procedure (D1 retraction survival against a
real poll, starvation removal in real flow, TTL receipt topic routing over a
live 72h clock, and phone-to-bridge fan-out) requires posting to and reading
from the real Telegram operator chat and, for the fan-out step, a live
Cursor session. This worktree's environment carries a live
`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` (the real operator bot/chat, not a
test double) — but driving automated traffic through the human's live
operator channel is a side-effecting action against a shared, external
system, so it was raised with the human before proceeding. Per the human's
explicit choice (2026-08-06), this pass does NOT drive the live E2E and
approves on the automated coverage above instead. Every one of the four
procedure items is otherwise covered at the unit/property/acceptance layer
(D1: property + pinned example + hardener's integration regression test;
starvation: acceptance scenarios 01-03 + 09; TTL receipt: acceptance
scenarios 07-08 + unit test; fan-out: acceptance scenario 10, which exercises
the real `pollAndForward` -> file-queue -> `runCursorBridgePollOnce` path).

**Recommendation carried forward to the human/coordinator**: run the ticket's
own manual E2E procedure (four items, `notes` section) against the real
Telegram deploy before treating this as fully closed in production, since no
role in this pipeline has driven it end-to-end over live network I/O.

## Verdict: PASS

Every automated gate this pipeline can run is green, independently re-run at
this stage (not a re-read of upstream evidence), and every `required_wiring`
entry, D1's fix, and D2's dispositions were confirmed by reading the actual
source, not merely trusted from prior evidence files. The one item not
verified here — live-deploy manual E2E — was explicitly deferred to the
human by mutual agreement rather than silently skipped, and is called out
above as a standing follow-up.

By QA.
