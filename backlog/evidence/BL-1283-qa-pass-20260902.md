# BL-1283 QA pass — 2026-09-02

Merged documenter `ab314ebb1b` into QA worktree. Review-only BL-848
stamp-off for already-landed hotfix `2b67f4b1a2` (freeze the Telegram
pipeline board while the pack is asleep). No production TS/bb source
touched by this parcel — confirmed (`git diff origin/main HEAD --stat --
extension/src/` empty before this merge); the hotfix itself already
landed on `origin/main` separately.

## Process hygiene

`pgrep -fl 'node --test|stryker'`: clean in my own worktree before/after.

## Independent re-run

- `npm run compile` — clean.
- `npx vitest run test/conciergeTick.test.js` — 121/121 pass (the
  simulated `syncBoardIfWired`/`readRoleHeldTickets` failure lines in
  stdout are expected fixture output for the transient/liveness-probe
  cases, not real errors).
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1283-swarm-stamp-pipeline-board-sleep-freeze-2b67f4b1a2.feature`
  — 8/8 scenarios pass, matching qa_e2e_procedure items 1-6 plus the
  ledger-untouched invariant.

## Full unit suite (`npm test`, extension/)

Same 15 pre-existing standing-red test files as this session's prior QA
passes, zero overlap (BL-1283 adds no new `extension/test/` file — only
the acceptance step handler and evidence/docs). Count unchanged at
9844/9869, consistent with BL-1319 landing just before this parcel.

## required_wiring / qa_e2e_procedure — re-verified live at the reviewed commit

- `bl1283PipelineBoardSleepFreezeSwarmStampSteps` registered at
  `specs/pipeline/steps/index.js:908`.
- `pipelineBoardShouldRefresh` exported at `conciergeTick.ts:637`; called
  at `:1377` (`pipelineBoardShouldRefresh(adapters.isPipelineBoardAwake?.())`)
  — `awake ?? true` semantics confirmed by reading the function.
- `telegram-front-desk-bot.ts:3364` wires
  `isPipelineBoardAwake: () => isSwarmLive(probeSwarmLiveness(targetPath))`.
- Ledger row for `2b67f4b1a2`: `state: stamp-open`, `human_decision: null`
  — untouched by this parcel (invariant 2 held).

## Item 5 — allowlist attribution, reported not acted on

Confirmed both rows (`bl874PortableTimeInvariants`,
`tempDirTrapGuard.property.test.js`) in
`swarmforge/scripts/property_suite_standing_allowlist.tsv` cite "tracked
under BL-1175". Independently checked `backlog/done/BL-1175-*.yaml`: it is
a real closed ticket about the standing-reds-block-commits MECHANISM, and
its text does not name either specific file — the attribution is
imprecise, not fabricated. Matches the coder/architect/hardener chain's
own finding (unchanged since architect, per hardener's evidence). Per the
ticket's constraints, not acted on here — reported only, as scenario 06
requires; the human's call per ruling_options in approval_context.

## Constraints honoured

Hotfix `2b67f4b1a2` not reimplemented, rewritten, or redesigned. Both
allowlist rows left untouched (not removed or re-attributed).

## Verdict

PASS. Landing on `main`; `Hotfix-Certification` stays `pending` until the
human records the ledger decision separately.
