# QA pass — BL-1419 — 2026-09-05

Commit under review: `48fe23495e` (merge of documenter's `5abe24a9be` into
`swarmforge-QA`), carrying the full chain (`a5123374bc` BL-1419 coder,
confirmed ancestor via `git merge-base --is-ancestor`). The Art Director
seat (BL-1418) has not yet stood up; per this ticket's own
`approval_context` ("QA gates it against the scenarios, and the art
director reviews the sent result on a phone as its first act once
seated"), I gate against the stated invariants/scenarios only.

## Verification

- `bb swarmforge/scripts/test/markdown_to_html_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/briefing_email_test_runner.bb`: ALL PASS.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1419-the-briefing-email-reflows-and-reads-well-on-a-phone.feature`
  — 8/8 scenarios pass, including "the real 2026-09-05 briefing renders
  with no wrapped fragments" (a real production briefing file, not a
  synthetic fixture) and "the plain-text part still carries the markdown
  unchanged" (invariant 3).
- Regression: BL-393's own feature (`BL-393-briefing-body-html.feature`)
  still 5/5 — scenario 02 (headings/tables/bold render as elements)
  confirmed unaffected. BL-896's feature (the chart step file also
  touched) still 7/7.
- **Rendered the real fixture myself** to see the actual output rather
  than trusting the assertions alone: loaded `markdown_to_html_lib.bb` +
  `briefing_email_lib.bb` directly and called `render-briefing-html`
  against `specs/pipeline/steps/fixtures/BL-1419-2026-09-05-briefing.md`.
  Confirmed by eye: zero `<style>` blocks (mail-client-safe, all styling
  inline per element); `max-width:640px` on the outer wrapper; a system
  font stack; a blockquote spanning 8 originally-wrapped lines rendered
  as ONE `<blockquote>` element with inline `<code>` spans, not 8
  paragraphs; list items with wrapped continuation lines joined into
  single `<li>` elements — directly disproving the "eight one-line
  paragraphs for one blockquote" defect the ticket describes.
- `required_wiring`: confirmed `compose-and-send-one!` calls
  `render-briefing-html` (line 509 of `briefing_email_lib.bb`), not the
  bare converter. `specs/pipeline/steps/bl1419BriefingEmailReflowSteps.js`
  exports `registerSteps`.
- Docs: `docs/reference/Specification.MD` updated.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
