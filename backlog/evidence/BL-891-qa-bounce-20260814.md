# BL-891 — QA bounce — 2026-08-14

## D1 — architect design-review gate never ran; no documented skip

1. **Failing command**: not a test command — an ancestry check for an
   architect-authored contribution between the cleaner and hardender
   stages:
   `git log --oneline --ancestry-path 3853956d6..53d75a8d3`
   (`3853956d6` = coder's BL-891 commit; `53d75a8d3` = the hardener's own
   merge commit that starts BL-891's hardening pass).

2. **Commit hash checked out and tested**: `22be9b47c` (documenter's
   forward to QA, `merge_and_process documenter 22be9b47cc`), merged into
   this worktree as `6d2099099`.

3. **First error excerpt** — full ancestry-path output, oldest to newest,
   between the coder's commit and the hardener's merge:

   ```
   53d75a8d3 Merge commit 'f912278236' into swarmforge-hardender
   f91227823 Merge commit 'e0408b8628' into swarmforge-cleaner
   b43ceb8fb Merge commit '7f30ede4d9' into swarmforge-cleaner
   2f7e79656 Merge commit '3853956d6b' into swarmforge-cleaner
   e0408b862 BL-892: durably commit every automated human_approval verdict
   7f30ede4d BL-746: rewrite stop-path tests to drive the real stop-swarm.sh
   ```

   Every merge commit in this range names `swarmforge-cleaner` or
   `swarmforge-hardender` as its target branch — none names
   `swarmforge-architect`, and no architect-authored commit (the pattern
   seen elsewhere today, e.g. `6c9b3cdc3 BL-788: architect pass - D1
   property test verified...` or `4d02fa92f BL-866: architect
   certification pass - CERTIFY`) appears anywhere in the range. The
   cleaner's own merge (`f912278236`, "Merge commit 'e0408b8628' into
   swarmforge-cleaner") is merged directly into the hardener's branch —
   architect is skipped entirely.

   Compounding this: the hardener's own evidence file
   (`backlog/evidence/BL-891-hardener-pass-20260814.md`, line 5) states
   *"Batch handoff from architect (`f912278236`, merge_and_process)"* —
   but `f912278236` is provably a **cleaner** merge commit (see above), not
   an architect one. The hardener misattributed the parcel's origin and
   proceeded without ever receiving or producing an independent
   architecture review.

4. **Failure class**: `behavior` — this is an absence of required stage
   output, not a code defect; per Article 4.3/workflow-detailed precedent
   (BL-575), a missing stage's pass is classed `behavior` and routed to
   the owning (here, absent) stage, never `coder`.

5. **Expected vs observed**: Expected — either an architect-authored
   commit/merge sits between the cleaner and hardener stages (Article 4.1
   gate 2: "Architect – Design review passed"), or the ticket's YAML
   carries a `stage_skip_reasons.architect` entry justifying a legitimate
   skip (the pattern BL-892's own YAML uses in this same batch: `{
   architect: "reuses the existing commit_integrity_cli / runCommitIntegrity
   seam already proven by Expedite (BL-490); no new module boundary or
   data-flow design" }`). Observed — `required_stages:` in
   `backlog/active/BL-891-local-main-ref-never-advances-after-qa-lands.yaml`
   explicitly lists `architect`, the YAML has no `stage_skip_reasons` field
   at all, and no architect evidence file exists anywhere in
   `backlog/evidence/` for this ticket.

## Everything else checked — no other defects found

- `bb swarmforge/scripts/test/master_main_reconcile_lib_test_runner.bb` —
  ALL TESTS PASS (re-run independently by QA).
- `bb swarmforge/scripts/test/master_main_reconcile_lib_property_runner.bb`
  — 500 runs, ALL PROPERTIES HOLD, both non-vacuity assertions confirmed
  (re-run independently by QA).
- `bash swarmforge/scripts/test/test_handoffd_master_main_reconcile_wiring.sh`
  — 9/9 scenarios PASS, covering the ticket's full QA end-to-end procedure
  (a)-(d) (re-run independently by QA).
- Wiring confirmed: `swarmforge/scripts/handoffd.bb` loads
  `master_main_reconcile_lib.bb` (line 32) and calls
  `master-main-reconcile-sweep!` from its cadence loop (lines 3073-3086),
  sharing one try/catch per the hardener-accepted rule_proposal.
- Acceptance is declared as `.feature.draft` in the ticket's own `acceptance:`
  field (unbuilt-feature convention, BL-233) — not a gap, matches the
  ticket's own declared scope; `specs/pipeline/scripts/run_acceptance.sh`
  is not applicable here.
- No orphaned `node --test`/`stryker` processes before or after QA's runs.

The implementation itself is sound and independently re-verified by QA.
The sole blocker is procedural: the mandatory architect gate was never
run and never documented as skipped.

## Remediation pointer

Architect: review coder's `3853956d6` (BL-891: reconcile the master
checkout's local main ref after QA lands) for design/scalability/security
concerns per Article 1.5 — the pure/impure split
(`master_main_reconcile_lib.bb` vs `handoffd.bb`'s git-specific wiring),
the merge-forward-only invariant enforcement, and the dirty-tree
surface-once/self-heal state machine. Either record a normal architect
pass (evidence file, forward to hardener) or, if design review is
genuinely unnecessary, record `stage_skip_reasons.architect` in
`backlog/active/BL-891-local-main-ref-never-advances-after-qa-lands.yaml`
with a stated reason and forward to hardener — the hardener's existing
pass can stand once the gate is legitimately satisfied one way or the
other; no code changes are requested by this bounce.

By QA.
