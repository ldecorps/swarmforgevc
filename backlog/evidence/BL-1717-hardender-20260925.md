# BL-1717 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- `swarmforge/scripts/land_step_lib.bb` is Babashka-only (BL-472: no
  mutation/CRAP/DRY wired). The acceptance feature carries 2 plain
  `Scenario:`s, no `Scenario Outline:`/`Examples:` — BL-113 Gherkin
  mutation is inapplicable (BL-638), so a hand-authored surgical mutation
  sweep over the fix stood in for it.
- Sweep (3 mutants, all killed):
  1. Delete the whole BL-1717 clause (revert to pre-fix behavior) —
     killed by the new `land_step_lib_test_runner.bb` unit case
     ("excludes the shared path even though a landed co-owner also
     touched it"): `shared.md` rode when it should not have.
  2. Invert `remove` → `filter` (test landed owners against
     `unlanded-siblings` instead of not-yet-landed owners) — killed by
     the same unit case, same failure shape.
  3. Drop the `(seq not-yet-landed)` guard (vacuous-`every?`-on-empty
     trap: an all-landed owner set would wrongly exclude the path) —
     killed by the pre-existing BL-1389 unit case ("a sibling path whose
     own lines are already on origin/main is kept").
  Each mutant applied to `swarmforge/scripts/land_step_lib.bb` in place
  (no detached job outstanding at the time), one at a time, restored to
  the original byte-for-byte before the next.
- Verification, all green at HEAD:
  - `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — ALL PASS.
  - `node specs/pipeline/cli.js specs/features/BL-1717-...feature` — 2/2.
  - Neighbouring exclusion/passenger suites unchanged: BL-1389 (5/5),
    BL-1375 (7/7), BL-1481 (4/4), BL-1678 (4/4).
- `git diff main...HEAD --name-only` (post-merge-up): BL-1717's own
  changes are `swarmforge/scripts/land_step_lib.bb`,
  `specs/features/BL-1717-...feature`,
  `specs/pipeline/steps/bl1717LandedCoOwnerNeverShieldsSteps.js`,
  `specs/pipeline/steps/lib/bl1717LandedCoOwnerCli.bb`,
  `swarmforge/scripts/test/land_step_lib_test_runner.bb`, and this
  ticket's evidence/YAML — the rest of that diff is unrelated already-landed
  tickets pulled in by this session's own `main` merge-up.
- No orphaned test processes or leaked fixtures after the runs
  (`bl1717LandedCoOwnerCli.bb`'s `build!`/`run` wraps its temp dir in
  `try/finally` and sweeps stale `bl1717-acceptance-*` dirs by prefix on
  each start, BL-971 style).
- Docs (land-step reference exclusion rules, per ticket Scope) are
  documenter's stage, not touched here.
