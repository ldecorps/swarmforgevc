# BL-1224 QA pass — 2026-08-30 (post-bounce)

## Entangled tip, rebuilt tip-pure (BL-1241) — second time for this ticket

The re-submitted parcel (documenter `c7f2a3b667`) again carries unlanded
BL-1235 work as an ancestor (still `status: todo, assigned_to: coder`, not
landable). Isolated BL-1224's own five commits — coder `0264c3c995`,
hardener's mutation-stamp `353923aff`, documenter's original doc pass
`0de070faad`, architect's design-review evidence `a5e966c97`, documenter's
re-pass evidence `c7f2a3b667` — each diffed against its own direct parent,
applied cleanly onto a fresh branch from current `origin/main`, with the
`index.js` require line added by hand (same pattern as the BL-670/BL-1183
rebuilds today).

## The bounce is resolved

My prior bounce (`1ddc283639`) was process-only: the architect's design
gate had never produced any record. `backlog/evidence/BL-1224-architect-review-20260830.md`
now exists — a genuine, substantive review (constraints checked directly
against the diff, all three invariants walked through, every headline test
reproduced), explicitly acknowledging the bounce and stating the review
"was done in-session but not recorded" the first time. No code changed —
confirmed: the coder commit (`0264c3c995`) is the same commit I originally
verified, byte-identical.

## A second merge-hazard, caught by the documenter before it reached me

The documenter's re-pass evidence (`BL-1224-documenter-repass-20260830.md`)
records catching an even larger instance of the exact hazard I hit myself
today on the BL-1183 revert: merging a commit whose ancestry includes my
own QA-bounce revert silently dropped 11 files with **no conflict
markers at all** (unlike my BL-1183 case, which at least showed 2
conflicts). They diffed the merge result against the pre-merge tip file by
file, restored everything byte-identical, and documented the root cause
(a QA revert commit is not an ancestor of the fixed-and-rebuilt pipeline
branches, so a later merge that pulls in a commit descending from both the
revert and the rebuild can resolve "silently agree with the revert's
deletion"). Spot-checked their restoration independently before trusting
it: `adoptable-pid`/`adopt-entry`/`pidfile-pid` present in
`operator_runtime_watch_lib.bb`, `bl1224...Steps` registered in `index.js`.

## Re-verified on the clean tip-pure base (not just trusted from the entangled branch)
- `npm run compile`: clean.
- `swarmforge/scripts/test/operator_runtime_watch_lib_test_runner.bb`:
  ALL PASS.
- `swarmforge/scripts/test/bl1224_watch_adoption_property_runner.bb`: ALL
  PASS, 400 runs/invariant, non-vacuous coverage across all seven branches
  — identical coverage numbers to my pre-bounce run, confirming byte-
  identical logic.
- `swarmforge/scripts/test/test_operator_runtime_watch_adoption.sh`: 15/15,
  driving the real `operator_runtime_supervisor.bb --check-once`.
- `specs/features/BL-1224-watch-adopts-a-deliberately-restarted-operator-runtime.feature`:
  7/7.

## Verdict: APPROVE (as the rebuilt tip-pure commit, not the entangled parcel)

By QA.
