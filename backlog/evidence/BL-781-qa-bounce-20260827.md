# BL-781 QA bounce — 2026-08-27

**Routing:** coder (acceptance step filter — owns the step handler)

## Failing command

```
specs/pipeline/scripts/run_acceptance.sh \
  specs/features/BL-781-retire-dead-babysitter-files-keep-list-preserved.feature
```

## Commit tested

`429a9957b0` (architect handoff) merged to QA as `a7bb4cff1c`.

## First error excerpt

```
✖ repo-wide grep finds no live caller of each deleted filename [1]
  Error: ... live references to babysitter_lib.bb outside history/docs:
  specs/features/BL-781-retire-dead-babysitter-files-keep-list-preserved.feature

✖ ... babysitter_enqueue_wake.sh → same feature file
✖ ... babysitter_assess.bb → same feature file
```

10/13 scenarios pass. Scenario Outline 07 fails all 3 examples.

## Failure class

acceptance

## Expected vs observed

**Expected:** scenario 07 treats the ticket's own feature file (and other
acceptance/spec prose that *names* the retired basenames to assert their
absence) as non-live, same as `specs/pipeline/steps/` and
`swarmforge/scripts/test/` are already filtered in
`bl781RetireDeadBabysitterFilesKeepListPreservedSteps.js`.

**Observed:** the live-offender filter excludes `backlog/`, `docs/`,
`specs/pipeline/steps/`, and `swarmforge/scripts/test/`, but **not**
`specs/features/`. The feature file that documents the deleted basenames
in its Examples tables is reported as a live caller of itself.

## Defects

**D1 — acceptance (blame: coder):** Incomplete exclusion list in
`bl781RetireDeadBabysitterFilesKeepListPreservedSteps.js` (~lines 214–219).
Add `specs/features/` (or at least this feature path) to the non-live
filter so scenario 07 is not red-when-correct against its own Examples.

## Other gates (PASS — not bounce items)

| check | result |
|---|---|
| Ancestry: `be2c162fb`, `429a9957b0` | OK |
| Dead paths absent (lib / enqueue / assess / lib_test_runner) | OK |
| Salvaged libs present (assess_lib / nudge_lib / nudge_resident) | OK |
| `babysitter_assess_lib_test_runner.bb` | ok |
| `babysitter_nudge_lib_test_runner.bb` | ok |
| `test_babysitter_nudge_resident.sh` | ALL PASS |
| BL-781 scenarios 01–06 | 10/10 pass |
| Embedded BL-611 scenario 15 | pass |
| Salvaged allowlist no longer names deleted wake-runtime | pass (scenario 03) |
| Tip purity vs land tip | 10 files, BL-781-only |

## Standing debt (report only)

Full unit suite `CURSOR_API_KEY` reds — **BL-720**.

By QA.
