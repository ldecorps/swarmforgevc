# BL-1656 — QA bounce, 2026-09-20

Per the specifier's ruling
(`backlog/evidence/BL-1459-specifier-ruling-hook-chain-blocks-every-forward-to-qa-20260920.md`,
commit `10100de975`), confirming QA's own critical-note evidence
(`backlog/evidence/check-documenter-briefing-tip-blocks-ordinary-forwards-20260920.md`).

Nothing in BL-1656's own content is defective. This bounce exists solely
to unstick the forward: the documenter's branch bounced BL-1459 at
02:01Z (spec-gap) but never reverted the bounced parcel's paths off its
own branch (BL-490/495), so the bounced, over-eager
`check_documenter_briefing_tip.sh` guard rides every commit reachable
from the documenter branch — including BL-1656's tip — and refuses (via
`swarmforge/git-hooks/pre-merge-commit`) the very merge that would
deliver it, since `core.hooksPath` runs the chain from the INCOMING
(merged) tree.

## D1

- **Failing command**: `git merge 5681027848a9486c4f89d38ded51a937e72467c9 --no-ff -m "Merge documenter ... into QA."`
- **Commit hash**: 5681027848a9486c4f89d38ded51a937e72467c9 (documenter's BL-1656 tip)
- **First error excerpt**:
  ```
  Merge refused: the documenter briefing tip 5681027848... carries content outside its lane: [~70 unrelated pipeline paths]
  pre-merge-commit: COMMIT REFUSED. Guards reporting a violation: check_documenter_briefing_tip.sh
  ```
- **Failure class**: integration
- **Expected vs observed**: Expected the documenter's tip to merge cleanly (an ordinary ticket forward).
  Observed: the un-reverted, bounced BL-1459 guard on the documenter branch fires on every merge whose
  incoming side is the documenter's own tip, refusing this ordinary forward as if it were a
  briefing-tip land.
- **Blamed role**: documenter (the un-reverted bounced tool on its own branch, per BL-490/495 — the
  documenter's own responsibility to revert what a bounce wrongly leaves behind, same discipline QA
  itself missed once already today on BL-1650/BL-1653, corrected by this same specifier ruling class).
- **Remediation pointer**: restore the paths BL-1459's non-evidence commits touched to `origin/main`
  content, per the specifier's ruling's own list (`docs/how-to/BL-658-...`, the BL-1459 property test,
  step handler, `swarmforge/git-hooks/pre-merge-commit`, `check_documenter_briefing_tip.sh`,
  `suite-manifest.tsv`, `test_check_documenter_briefing_tip.sh`), in ONE commit with an untagged
  subject, then re-forward BL-1656 from the clean tip.

By QA.
