# BL-1566 — cleaner review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Merged QA note f8d1fd2b61 (BL-1570 hold resumed) and coder's e18f7e00ec
  (BL-1566) into the cleaner worktree; both ancestry checks clean.
- `bb swarmforge/scripts/test/qa_hold_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/suite_inventory_cli.bb` — ok (517 files, 513
  standing, 4 excluded).
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1566-an-article-42-hold-is-a-record-qa-resumes-from.feature`
  — 8/8 scenarios pass (scenario outline's 3 examples included).
- Ran the four standing shell suites touching the two live callers this
  ticket wires (`test_done_with_current_arg_rejection.sh`,
  `test_ready_for_next_no_promotion.sh`,
  `test_done_with_current_work_note_evidence.sh`,
  `test_ready_for_next_rotate_home.sh`) — all pass, no regressions from the
  BL-1566 hold gate/status wiring.
- `qa_hold_lib.bb` already separates pure decisions (`release?`,
  `status-lines`, `blocks-completion?`) from the shared I/O reading layer,
  and reuses `pipeline_stage_lib/extract-ticket-id` and
  `standing_red_register_lib/parse-register-rows` rather than
  reimplementing ticket-id or register parsing — no DRY action needed.
  `qa_hold_cli.bb` is a thin wrapper with no second decision layer, per
  the ticket's own convention (matches `standing_red_register_cli.bb`).
- `node extension/out/tools/mutation-site-count.js` on the new step
  handler file reports 358 sites (over the 100 advisory threshold, BL-485)
  — same class and similar magnitude as the existing
  `bl983StageQueueSteps.js` (271, also over), which stays whole by the
  same reasoning: one cohesive fixture+scenario driver for one feature's
  acceptance scenarios; a split would fragment shared fixture state
  (`ensureState`/`makeFixture`) across files for no structural gain. Not
  split. The two `.bb` production files have no compiled `out/` output and
  no mutation tooling wired for Babashka (BL-472 deferred) — degraded
  fallback per `mutation_cost: low`, gated by their own unit-test suite
  only, as this ticket records.
- One defect found and fixed directly (not a bounce — cosmetic,
  self-contained): the coder's insertion into
  `swarmforge/scripts/test/suite-manifest.tsv` dropped the file's
  documented 4-column trailing-tab convention on the three rows around the
  new entry. Functionally inert (the parser reads with `-1` split limit)
  but inconsistent with the file's own header comment; restored in
  2d5c2a6589.
