# BL-1495 — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

mutation_cost: low (Babashka/shell, no mutation/CRAP/DRY wired, BL-472) —
hand-authored mutation sweep run instead, per BL-567's fallback shape.
Host load 8.71/20 cores at start, no gate triggered.

Gates run:
- `pack_staffing_gate_lib_test_runner.bb` — green.
- `provider_compat_lib_test_runner.bb` — green (27 assertions).
- `bl1049_provider_env_scrub_test_runner.bb` — green.
- `harness_env_scrub_lib_test_runner.bb` — green.
- `swarmforge/scripts/test/test_alternate_runtime_launch.sh` — green (10/10,
  scenario 10 is this parcel's).
- Acceptance (`run_acceptance.sh` on the ticket's feature file) — 7/7 green.
- `extension/test/bl1495BaiGatewayInvariants.property.test.js` (coder-
  authored invariants, run alone via `vitest.properties.config.mjs`, never
  the whole `test:properties` lane) — 5/5 green.
- `extension/test/bl1328QwenModelTokenFormsInvariants.property.test.js`
  (the specifier-ruled retirement of its whole-branch net-diff tail) — 3/3
  green, confirmed the retained three assertions are intact.
- Whole-tree standing guards (parcel touches `specs/pipeline/steps/` and
  `extension/test/`): all 18 `test/*Guard*.test.js` files (183 tests) —
  green, including `tmuxReaperGuard`, `tmpDirMigrationGuard`,
  `bl968StepRegistryMaterializedTreeGuard`,
  `bl1277UnscopedStepCollisionGuard`.
- CRAP/DRY: not run — no `extension/src/*.ts` production file is touched
  (only `extension/test/` property files); CRAP is scoped there and has
  nothing to measure.

Hand-authored mutation sweep (12 mutants, hand-applied to the live file,
verified with `git status --short` clean between each, restored via
`git checkout --`), one per `required_wiring` site — all KILLED:
1. `pack_staffing_gate_lib.bb`: `api.b.ai` → wrong provider — killed by
   `pack_staffing_gate_lib_test_runner.bb`.
2. `pack_staffing_gate_lib.bb`: drop `claude-fable-5-1` mapping — killed
   (same runner).
3. `provider_compat_lib.bb`: `bai-host-re` no longer matches `api.b.ai` —
   killed by `provider_compat_lib_test_runner.bb` (5 assertions).
4. `provider_compat_lib.bb`: `must-remap-to-bai?` `or`→`and` — killed (7
   assertions).
5. `provider_compat_lib.bb`: `bai-key-missing` branch leaks
   `openai-api-key` instead of `nil` — killed (the one assertion built
   exactly for this).
6. `provider_respawn_env_lib.bb`: drop `B_AI_API_KEY` `-e` forward — killed
   by the property test's both-mappings comparison.
7. `handoff_lib.bb`: drop `B_AI_API_KEY` `-e` forward (the second hand-
   rolled copy) — killed, same property test.
8. `harness_env_scrub_lib.bb`: drop `B_AI_API_KEY` from the aider keep set
   — killed by `bl1049_provider_env_scrub_test_runner.bb`, including its
   own bb/shell-twin agreement check.
9. `harness_env_scrub.sh`: shell twin drops `B_AI_API_KEY` from the aider
   case — killed (grep probe on the shell function's own output).
10. `swarmforge.sh`: drop `use_bai` from the `OPENAI_API_KEY` exclusion
    condition (would leak the real key) — killed by
    `test_alternate_runtime_launch.sh` scenario 10.
11. `swarmforge.sh`: drop the pack-window `--openai-api-base api.b.ai`
    host-sniff — killed (same scenario 10).
12. `glm-mono-router.conf`: wrong base URL on the `coder` window line —
    killed by acceptance scenario 05.

No survivors, no equivalents claimed, no skips.

Observation (not a defect, no bounce): `bai_guard`'s in-pane heredoc body
(the launch-script re-apply guard) is never executed by any test — only
grepped for leaked secrets in the generated script file. This is not a
regression: `cerebras_guard` and `perplexity_guard`, the two siblings this
guard is explicitly modeled on (ticket description: "a Perplexity-style
host sniff"), carry the identical gap — neither has runtime-execution
coverage anywhere in the suite either. Standing debt shared by all three
inline heredoc guards, out of this parcel's scope (constraints: additive
only, no refactor of the Cerebras/Perplexity/Qwen cases).

Also observed, not a defect: `failover_to_gpt.sh`'s one-line
`SWARMFORGE_USE_BAI` addition to its `unset` list has no dedicated test —
same as the pre-existing `SWARMFORGE_USE_QWEN`/`SWARMFORGE_USE_CEREBRAS`
entries in that same line, which also carry none. Not a regression.

`git grep -n B_AI_API_KEY` across every file this parcel touches: names
only, never a hardcoded value (constraint held).

Working tree left clean; no orphaned test/mutation processes (all bb runs
are synchronous and short-lived; nothing backgrounded).
