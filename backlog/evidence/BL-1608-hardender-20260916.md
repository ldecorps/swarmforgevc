# BL-1608 — hardender review pass, 2026-09-16

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb -e probe on claim-task-name with a present-but-blank task: header
- **Commit hash**: ddcfb67115
- **First error excerpt**: original resolves the Work BL-... message slug (BL-2222-something); mutant (not-empty guard removed) resolves "" instead - now pinned as unit runner shape 4 and killed
- **Failure class**: coverage-gap
- **Expected vs observed**: a blank task: header falls through to the message attribution, per BL-1185 contract
- **Blamed role**: hardender
- **Remediation pointer**: added shape 4 to ready_for_next_claim_task_name_runner.bb; verified red against the mutant, green against restored source

By hardender.

## Detail

- Mutation (BL-638 fallback, no Stryker/CRAP/DRY wired for Babashka):
  hand-authored sweep on `claim-task-name` and its two claim-time call
  sites in `swarmforge/scripts/ready_for_next_task.bb`.
  - Removing the `not-empty` guard around the `task:` header read: REAL
    gap, confirmed by `bb -e` probe (a present-but-blank `task:` header
    resolved `""` under the mutant instead of falling through to the
    `Work BL-...` message). Fixed by adding a fourth shape to
    `swarmforge/scripts/test/ready_for_next_claim_task_name_runner.bb`;
    verified red against the mutant, green against the restored source
    (commit `ddcfb67115`).
  - Swapping either claim-time call site (`difficulty-allows-claim?` at
    line 116, `apply-effort-for-task!` at line 84) back to a bare
    `handoff-lib/header-field` read: killed by BL-1608's own scenario 01
    source census (both function bodies must contain `claim-task-name`;
    no bare `mutation-cost-for-task (handoff-lib/header-field` pattern) -
    confirmed live by hand-applying the swap at the `difficulty-allows-
    claim?` site and re-running the BL-1608 acceptance feature (1 of 4
    fails), then restoring.
  - The `catch Exception _ nil` branch: not reachable from any caller in
    this codebase (claim-task-name is only ever called with a real,
    just-listed handoff file path), and only fires if `slurp` throws -
    treated as equivalent, not chased.
- BL-113 Gherkin mutation (soft) on the Scenario Outline (scenario 02):
  9 mutants, 5 killed (m1, m3, m5, m6, m9 - the task-slug and resolved-
  value literals), 4 survived (m2, m4, m7, m8). All 4 survivors are
  equivalent by design, demonstrable from
  `specs/pipeline/steps/bl1608ClaimPathAttributesWorkNoteSteps.js`'s step
  handler: the `headers` column ("carry no task") only ever feeds
  `headersText.match(/task\s+(\S+)/)` to decide presence/absence of a
  `task:` header line - a case-flip inside "carry no task" never creates
  or destroys that match (m4, m7). The `message` column is irrelevant on
  row 1 because `claim-task-name`'s `or` short-circuits on the present
  header before ever parsing the message (m2), and on row 3 the message
  has no `Work BL-...` substring either way so a word-casing flip changes
  nothing (m8). Embedded manifest wrote `scenarios: []` despite 5/9 real
  kills - expected per BL-502 (any survivor drops the scenario from the
  manifest); trusted the run's own per-mutant status lines over the
  manifest text per BL-460.
- Acceptance pre-check (`run_acceptance.sh`), all green: BL-1608 (4/4),
  BL-1185 (4/4 - the fault this ticket restores, was 3/4 red on main),
  BL-1004 (5/5), BL-1001 (6/6), BL-1167 (3/3).
- `grep -n 'header-field handoff-file "task"' swarmforge/scripts/ready_for_next_task.bb`:
  only the two rework-claim sites (BL-1004, `claim-task`/`:task`,
  unrelated to cost attribution) remain - no site inside
  `difficulty-allows-claim?` or `apply-effort-for-task!`.
- `bb swarmforge/scripts/test/ready_for_next_claim_task_name_runner.bb`:
  green, 4 shapes (task header wins; headerless Work note falls back to
  message; neither resolves nil; a present-but-blank header falls
  through - the hardener's own addition).
- CRAP: `swarmforge/scripts/ready_for_next_task.bb` is Babashka, no
  CRAP/DRY tooling wired (engineering.prompt). The JS step handler
  (`bl1608ClaimPathAttributesWorkNoteSteps.js`) is outside the TS
  `extension/src` CRAP scope.
- DRY (jscpd) on the touched JS step handler: 0 clones.
- No orphaned test/mutation processes left behind
  (`pgrep -afl 'node --test|stryker'` clean); the leaked
  `/tmp/bl1608-claim-task-name-*` fixture dirs from repeated acceptance
  runs of the step handler's `fs.mkdtempSync` (no cleanup in that step,
  consistent with the prevailing pattern across ~285 files in
  `specs/pipeline/steps/`, not unique to this ticket) were removed by
  hand; not changed in the step handler itself as out of this ticket's
  narrow scope.
