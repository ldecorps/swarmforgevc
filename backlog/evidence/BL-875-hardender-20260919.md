# BL-875 — hardender review pass, 2026-09-19

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: npx vitest run test/stepHandlerTmpRootGuard.test.js (BL-1636 standing guard, run as part of every parcel touching specs/pipeline/steps)
- **Commit hash**: de1dba1d51
- **First error excerpt**: new offender(s) not in the census: [bl875StrayRootPackageLockRemovedSteps.js] - the handler created its fixture root via bare fs.mkdtempSync + a hand-rolled process.on(exit) cleanup covering normal exit only, never SIGINT/SIGTERM
- **Failure class**: resource-leak
- **Expected vs observed**: a step handlers mkdtemp root is registered with the shared fixtureReaper primitives (BL-1636 standing guard)
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: switched to fixtureReaper trackedTmpRoot() (track()/reap() rejected: reap() no-ops for a plain directory with no operator/tmux state); commit de1dba1d51; guard now green, no /tmp leak, acceptance 4/4

## D2

- **Failing command**: build_freshness_lib_test_runner.bb (baseline, then hand-mutated on-deployed-surface? = to str/ends-with? on the extension/package-lock.json clause, then restored)
- **Commit hash**: de1dba1d51
- **First error excerpt**: mutant survived BOTH BL-875s own acceptance suite and the full pre-existing build_freshness_lib_test_runner.bb: every case only tested the positive (extension/package-lock.json -> true), nothing tested the bare root path or an unrelated dirs package-lock.json stays off the surface
- **Failure class**: test-coverage-gap
- **Expected vs observed**: the exact-path invariant BL-875 itself declares (keys its deployed-surface check on that exact path) is verified in both directions
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added two negative cases to build_freshness_lib_test_runner.bb; commit de1dba1d51; confirmed it now kills the hand-applied mutant

By hardender.
