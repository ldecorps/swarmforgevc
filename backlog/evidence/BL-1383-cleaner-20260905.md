# BL-1383 — cleaner pass, 2026-09-05

Clean sweep. No changes needed; forwarding as-is.

## Checks run

| check | result |
|---|---|
| `test/providerChatSeat.test.js` | 31/31 |
| `test/telegramFrontDeskBotCore.test.js` | 449/449 |
| property `bl1383ProviderChatSeatInvariants.property.test.js` | 4/4 |
| property `telegramFrontDeskBotCore.property.test.js` | 9/9 |
| acceptance `BL-1383-a-topic-bound-to-a-chat-provider-answers-there.feature` | 5/5 |
| `jscpd` on the two new files | 0 clones |
| credential grep (`B_AI_API_KEY\|api[_-]key`) | one hit, a doc comment only |

## Module structure review

`providerChatSeat.ts` — pure decision module, no IO, single responsibility
(`decideProviderChatTurn`). No changes needed.

`telegramFrontDeskBotCore.ts` / `telegram-front-desk-bot.ts` diffs — both
additions mirror the existing `forwardCursorBridgeUpdate` /
`attemptCursorBridgeTopicExclusion` pattern exactly (core decides + stops,
live side performs). Consistent with surrounding code, no rework needed.

## Mutation-site count (BL-485)

```
node extension/out/tools/mutation-site-count.js extension/src/tools/providerChatSeat.ts extension/src/tools/providerChatSeatLive.ts extension/src/tools/telegramFrontDeskBotCore.ts extension/src/tools/telegram-front-desk-bot.ts
```

| file | sites | verdict |
|---|---|---|
| `providerChatSeat.ts` | 63 | within |
| `providerChatSeatLive.ts` | 181 | over |
| `telegramFrontDeskBotCore.ts` | 1984 | over (pre-existing file, this parcel adds ~14 lines) |
| `telegram-front-desk-bot.ts` | 1846 | over (pre-existing file, this parcel adds ~15 lines) |

The two pre-existing files are far over threshold independent of this parcel
— out of scope for a small wiring addition, not touched here beyond the
adapter hooks reviewed above.

`providerChatSeatLive.ts` (new, 215 lines) is the one file this parcel
introduces that reads over (181 vs. the 100-site cap). Considered a split
along its four groupings — map read (`readProviderChatTopicSeats`/`parseSeat`),
swarm-context composition (`readSwarmIdentity`/`countBacklogYaml`/
`composeSwarmContextBlock`), the HTTP client (`chatCompletionsUrl`/
`completeWithProviderChat`), and the turn orchestrator
(`runProviderChatSeatTurn`) — and declined it: the ticket's own description
assigns exactly this shape to this file ("`providerChatSeatLive.ts` (new,
side effects): `runProviderChatSeatTurn` posts the acknowledgement, calls the
provider, posts the reply..., and appends a small swarm-context block..."),
none of the four groupings has a second consumer today, and a mechanical
split would only fragment one cohesive "live I/O for the provider chat seat"
module without improving separation of concerns. Left whole per the
soft-advisory rule (BL-485) — a legitimately-cohesive module a split would
only harm.

By cleaner.
