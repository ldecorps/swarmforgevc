# BL-693 — QA review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Sibling check: `qa-sibling-check.js status --ticket BL-693` -> `VERIFY
BL-693`. Register: no row for BL-693. `depends_on: [BL-692]` — confirmed
satisfied (`backlog/done/M8/BL-692-...yaml`).

- Wiring gate: `pre_qa_gate.sh` -> OK.
- Guard test collected and green solo: `docsDuplicateParagraphGuard.test.js`
  21/21.
- **Break-then-fix proof (qa_e2e step 2), against the REAL docs tree, not
  a fixture**: appended a duplicate of `docs/index.md`'s own line 29 (a
  real 256-char line) back into the same file, re-ran the same guard
  test — went red naming the file, the truncated text, occurrence count
  (2), and both line numbers (29, 380). Restored the file; re-ran —
  21/21 green again. (First attempt used a real 3-line wrapped paragraph
  from a how-to doc — stayed green, since the guard's unit is a single
  trimmed line over 200 chars, not a multi-line block; corrected to a
  genuine long single line per the helper's own `DEFAULT_THRESHOLD`.)
- Step 3 (short repeated lines like `---`/headings never trip it) and
  step 4 (the same long line in two DIFFERENT files never trips it):
  both already covered by dedicated fixture-based unit tests in the same
  passing suite (`scanDocsTree never reports the same line shared across
  two different files`, the at-or-under-threshold test) — not
  independently re-demonstrated by hand given the equivalent coverage.
- Step 5: `grep -i allowlist\|exempt` over the helper — only a comment
  explaining the absence of one; no actual exemption mechanism.
- Acceptance: BL-693 13/13, including "the real docs tree repeats no
  substantial paragraph" (BL-692's dependency now satisfied) and "the
  guard reaches green without an exemption list".
- Docs: `docs/how-to/BL-693-docs-duplicate-paragraph-guard.md` present,
  linked from `docs/index.md`.
- Unit suite: `npm test` — 628/628 files, 10741/10741 tests, exit 0,
  "suite file budget OK: 628 files, all within 7.0s".

## Addendum — post-land bounce_history check (BL-340)

A `bounce_history` entry (cleaner, `63f72de252`, class integration: a real
fixture-leak defect in `bl693DocsDuplicateParagraphGuardSteps.js`'s
`mkFixtureRoot()`, same class as BL-1624/1626/1632) only appeared in this
worktree via the origin/main merge made AFTER landing — the bookkeeping
commit (`7d92515755`) took a separate path and was absent from my tree at
verification time. Checked immediately on discovery: the fix commit
(`6b1be574c1`, `onAbnormalExit`-tracked cleanup) IS an ancestor of the
commit I approved and landed (`git merge-base --is-ancestor 6b1be574c1
e36b922875` -> yes); `grep onAbnormalExit` confirms it's present in the
landed handler. Re-ran the acceptance suite once more:
`ls -d /tmp/bl693-acc-*` — zero before, zero after, 13/13 pass. No defect
shipped; the bounce was genuinely already resolved before reaching QA,
its record just arrived late.

By QA.
