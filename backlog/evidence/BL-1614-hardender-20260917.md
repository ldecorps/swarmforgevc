# BL-1614 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Babashka/Clojure — no mutation/CRAP/DRY tool wired (BL-472); property
runner + shell test + acceptance are the gates.

- `bb swarmforge/scripts/test/bl1614_work_note_active_on_main_property_runner.bb`
  → `ALL PROPERTIES HOLD` (500 runs each).
- `bb swarmforge/scripts/test/bl1422_work_note_not_completed_without_work_property_runner.bb`
  (re-verified unchanged in scope) → `ALL PROPERTIES HOLD`.
- `bash swarmforge/scripts/test/test_done_with_current_work_note_evidence.sh`
  → `ALL PASS`.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1614-*.feature`
  → 6/6 pass.
- Regression: `specs/features/BL-1513-*.feature` (route message template
  changed) → 4/4 pass.
- Hand-mutation (BL-567 fallback):
  - Dropped the `(and (some? reason) active-on-main?) :refuse-active-on-main`
    clause from `work-note-completion-decision`: the property runner
    caught it decisively (multiple counterexamples, e.g. a stated reason
    silently accepted while `active-on-main?` was true). Restored.
  - Dropped the `(not (worktree-ticket-active? ...))` guard from
    `print-merge-main-first-hint!` (would print the hint even when the
    worktree already has the ticket active/merged): caught by the BL-1614
    acceptance feature's own scenario 01 row 2 (5/6 instead of 6/6).
    Restored.
- `npm run compile` clean; `npm test` 626/626 files, 10677/10677 tests
  (a pre-existing, unrelated file-duration budget gate — untouched by
  this ticket, already tracked under BL-1620/BL-791 — is the only
  nonzero exit-code contributor).
