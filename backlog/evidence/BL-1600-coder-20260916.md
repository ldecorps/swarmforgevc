# BL-1600 — coder evidence, 2026-09-16

## The fix

`extension/test/bl1277UnscopedStepCollisionGuard.test.js`'s shipped-scan
`it(...)` now carries a third argument,
`resolveUnitLaneTimeout(20000).effectiveMs`
(`specs/pipeline/steps/lib/contentionBudget.js`, BL-1007's already-shipped
unit-lane contention helper), in place of no explicit timeout (the suite's
flat 20000 ms default). No other test in the file, and no assertion inside
the shipped-scan test, changed.

## Declared invariant — no new property test, reasoned per BL-654

The declared invariant ("a quiet host, contention factor at or under 1,
receives exactly the 20000 ms base") is about `resolveUnitLaneTimeout` /
`effectiveBudgetMs`, which this parcel does not modify. That function
already carries a coder-authored, non-vacuous property test from BL-1007
(`extension/test/bl1007ContentionBudget.property.test.js`):
"invariant 3: unusable factor leaves the base unchanged" exercises factor
`0.25` (< 1) against base `20000` and asserts the base is returned
unchanged, and "invariant 1: resolveUnitLaneTimeout records factor and
effective" exercises factor `2` growing the budget — together the exact
"grows only with contention, never a bare raise" claim BL-1600 restates.
BL-1600 introduces zero new arithmetic to this function; writing a second,
byte-for-byte-equivalent property test here would duplicate BL-1007's own
without adding failure-detection value. Scenario 02 below (Gherkin,
executable, calling the real function at four boundary points: 0.5→base,
1→base, 2.5→2.5×, 9→ceiling) is the coder-authored executable encoding
that actually belongs to this parcel: it pins the wiring's arithmetic
contract at the acceptance layer, on top of BL-1007's already-standing
property coverage of the same function.

## Acceptance

New step handler `specs/pipeline/steps/bl1600ShippedStepScanBudgetSteps.js`
(required_wiring anchor). Scenario 01 parses the real test file's source
with a small, file-scoped `it(...)` call-site scanner (string/comment
aware, balanced-paren; not a reuse of BL-914's `testTimeoutParser.js`,
which only recognizes `test(...)` call sites and collapses any non-numeric
trailing argument to `timeoutMs: null` — this scenario needs the raw
expression text to confirm it references `resolveUnitLaneTimeout` and
`20000`, never a bare literal). Scenario 02 (Outline) drives the real
`resolveUnitLaneTimeout` with the four `Examples:` factors.

```
$ ./specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1600-*.feature
# tests 5
# pass 5
# fail 0
```

## qa_e2e_procedure

1. `for i in $(seq 1 20); do npx vitest run test/bl1277UnscopedStepCollisionGuard.test.js || echo FAIL; done` — 20 of 20 green, no FAIL line.
2. `npm test` × 3 — all three runs: `Test Files 622 passed (622)`, `Tests 10566 passed (10566)`; `bl1277UnscopedStepCollisionGuard.test.js` green in every run (13.6–16.2 s wall, well inside the load-scaled budget). Each run's overall exit code is 1 from the PRE-EXISTING BL-378 per-file-duration-budget guard (a permanently-red gate since 2026-08, BL-1598's own subject — bl1277 itself is one of BL-1598's nine named poles) — unrelated to this ticket's 20 s testTimeout fix and out of this ticket's scope per its own `out_of_scope` ("Cutting the scan's cost … slice D work, not this ticket's").
3. Acceptance: both scenarios pass (above).
4. `bb swarmforge/scripts/standing_red_register_cli.bb .` after removing the row: `"unowned":[]`, no row for `bl1277UnscopedStepCollisionGuard.test.js`.

## Register

Removed the BL-1600-owned row from `backlog/standing-reds.tsv` in this
land, per the ticket's own "the register row leaves in the same land."
