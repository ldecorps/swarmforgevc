# BL-1345 — cleaner pass (2026-09-03)

## Scope

Merge `7ed58805ff` touches only files it declares: the two production fixes
(`babysitter_check.bb`, `remote_control_health_lib.bb`/`swarm_ensure.bb`),
its own step/property/bb test files, and `suite-manifest.tsv` registration.
No unrelated files touched — none of the recurring `bl1332` regression seen
in earlier BL-1352 passes this session.

## Invariants and constraints checked against the diff

- **Invariant 1** (marker read only where it applies): `babysitter_check.bb`
  now routes through `mono-router-lib/resolve-resident-role` — the same
  shared BL-1020 decision the RC-repair hotfix uses — rather than reading
  `active-role-marker` unconditionally. `resident-active-role` is further
  gated against `ordered-roles` (the pack's own roles, never a hardcoded
  list — BL-804 invariant 1 honored).
- **Invariant 2** (a misstaffed pane is never healthy): `swarm_ensure.bb`
  now compares the observed process against the role the PACK assigns the
  pane (`assigned-role-mismatch`), not against whichever role the check was
  pointed at — the exact 2026-09-02 outage shape. Placed *after*
  `actionable?` deliberately, so an ordinary degraded/session-dead repair
  isn't pre-empted into a failure report (coder's own evidence records
  catching this placement bug via `test_swarm_ensure.sh` RC-2 before
  forwarding).
- **Invariant 3** (absent/unreadable/unknown marker = no marker): verified
  by acceptance scenario 5 (3 sub-cases) and the property test.
- Constraints: hotfix `195de28861` untouched, router-pack marker semantics
  untouched (acceptance scenario 2 is the explicit regression guard),
  `required_wiring` (`bl1345StaleRouterMarkerStaffingSteps` registered in
  `index.js`) satisfied.

## Verification (run, not assumed)

- `bb swarmforge/scripts/test/bl1345_stale_marker_test_runner.bb` — ALL PASS.
- `run_acceptance.sh specs/features/BL-1345-…feature` — 7/7 pass.
- `bash swarmforge/scripts/test/test_swarm_ensure.sh` — **ALL PASS** (runs
  well past 2 minutes real-time due to real tmux/sleep fixtures; backgrounded
  to let it finish rather than judged on a truncated run). Confirmed RC-7b
  ("a stale mono-router-active-role marker is not topology for the RC check
  on a standing pack") specifically present and passing — this ticket's own
  regression guard against the hotfix it builds on.
- `npx vitest run --config vitest.properties.config.mjs
  bl1345StaleMarkerInvariants` — 15 consecutive green runs, no flake.
- Mutation-site advisory (BL-485): step-handler and property-test files at
  131/130 sites, consistent with sibling files elsewhere in this session
  (124–241 range); no split warranted.

## Verdict

No defect. Forwarding.
