# BL-1263 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, minimal, and clean.

## What was checked

- Confirmed the diff against the coder's own true parent commit
  (`eb0b8f7c0c^`) touches exactly 4 files: the 3 named test files, the new
  step handler, and the evidence doc — no source file
  (`backendSwitch.ts`/`telegramClient.ts`/`telegramOperatorAmbulance.ts`)
  touched, matching the ticket's firm constraint.
- Verified each root cause directly against the real source:
  `backendSwitch.ts:71/73` genuinely maps bare `auto` to
  `cursor/auto`/`copilot/auto`; `telegramClient.ts:321` genuinely sends
  `allows_multiple_answers` unconditionally; `telegramOperatorAmbulance.ts:49`
  genuinely carries the "BL-691 D3" active-only guard. All three
  confirmed correct and deliberate, as the ticket's own mint-time finding
  claimed.
- Confirmed site 2's assertion is still a genuine whole-body
  `assert.deepEqual` (not narrowed to a partial/substring check) — the
  constraint most at risk of being weakened to "get green." Ran the
  acceptance feature's scenario 04 (the real mutation-inject-run-restore
  proof) directly: passes, confirming the whole-body comparison still
  catches an unannounced field.
- Ran all 3 touched files directly: 123/123 pass (12+87+24), matching the
  coder's claim exactly.
- Ran the acceptance feature: 5/5 pass.
- Ran the full unit lane: 3 failed / 603 passed — `backendSwitch`,
  `telegramClient`, `telegramCursorOperatorExec` no longer among the
  failures; only `constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`
  (×2), and `pricingTable` remain, all pre-existing and separately owned
  (BL-1212/BL-1265/genuinely-unowned-and-flagged) — the failure count
  dropped from 6 to 3, exactly matching the ticket's own qa_e2e
  expectation ("drops by exactly three").
- `npm run compile` clean.
- No duplication/architecture concern applies: this is a 3-line, test-only
  diff with no new abstraction.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `git diff --name-only eb0b8f7c0c^ eb0b8f7c0c` | exactly 4 files, no source touched |
| `npx vitest run test/backendSwitch.test.js test/telegramClient.test.js test/telegramCursorOperatorExec.test.js` | 123/123 pass |
| `node specs/pipeline/cli.js specs/features/BL-1263-...feature` | 5/5 scenario runs |
| Full unit lane (`npx vitest run --config vitest.config.mjs`) | 3 failed / 603 passed — none of the 3 fixed files remain, failure count dropped by exactly 3 |
| `npm run compile` | clean |

No fix required. Forwarding unchanged to architect.
