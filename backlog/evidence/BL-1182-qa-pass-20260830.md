# BL-1182 QA pass — 2026-08-30

Commit verified: merge of documenter `181925481` into QA (`c31f7204d`).
BL-1182's own final coder/cleaner/architect/hardener/documenter commits
(f64ad32809 the D1/D2 refix, 77d06df867, a80815dfe the self-caught
merge-drop restore, 604a245de3, ac8bdfaa42, 181925481) are all confirmed
ancestors of the merge HEAD.

Bounce history checked against `main`: the architect bounced once
(D1: `permanent-for-role`'s primary clause dead code from a JSON
keywordization round-trip bug; D2 not fully detailed but fixed alongside).
Confirmed fixed — re-read the architect's re-pass evidence, which re-ran
every headline claim after the fix and reproduced clean coverage numbers
directly rather than trusting the coder's report.

Merge conflict on `docs/reference/Specification.MD` and
`specs/pipeline/steps/index.js` (both files also touched by my own already-
landed BL-1232 rebuild) — resolved by keeping both sides' entries/requires;
diffed the merge commit against BOTH parents afterward and confirmed nothing
from either side was dropped.

## Dependencies — confirmed landed
`depends_on: [BL-1181, BL-1178, BL-556]` — all three present in
`backlog/done/`.

## required_wiring — both confirmed
- `bb swarmforge/scripts/model_steward_cli.bb trial` — lists `trial
  nominate|status|assess` subcommands (verified absent before this ticket
  per the anchor's own non-vacuity note; present now).
- `specs/pipeline/steps/index.js` registers
  `bl1182DayLongBobTrialLifecycleSteps`.

## qa_e2e_procedure — all five covered, each with its own passing check
`bash swarmforge/scripts/test/test_model_steward_trial_cli.sh`: 21/21 real
CLI checks, covering all five items directly:
1. Nominate → armed trial seat (checks 01).
2. Outrank → permanent updates (checks 02).
3. Tie → cheaper wins (checks 03).
4. Lose → revert + evidence, including refusal of a same-evidence re-trial
   (checks 04).
5. Memory transfer on both the start and end boundary, and a failed
   transfer correctly refusing the nomination rather than half-moving the
   seat (checks 01/04/05).

## Additional tests run
- `extension/test/trialBoundaryMemory.test.js`: 12/12.
- `swarmforge/scripts/test/model_steward_trial_lib_test_runner.bb`: ALL PASS.
- `swarmforge/scripts/test/bl1182_trial_lifecycle_property_runner.bb`: ALL
  PASS, 500 runs per invariant, non-vacuous coverage across every branch
  (tie-cheaper-permanent, tie-cheaper-trial, tie-equal-cost, outrank,
  outranked, loss, loss-without-evidence, start-change, end-change,
  no-change all populated).
- `specs/features/BL-1182-day-long-bob-trial-lifecycle.feature`: 5/5.

## Cross-checked against architect's and hardener's own independent passes
Both re-ran the coder's headline numbers directly and got identical counts
(26 assertions / 21 checks / 500 runs/invariant / 9 or 12 TS tests — the
count grew from 9 to 12 between hardener and my own run, consistent with
the hardener's own "closed uncovered captured passthrough gap" fix adding
tests). Both independently confirm the standing 26-failed-file/218-failed-
test full-suite baseline is unchanged and unrelated (same baseline I have
independently measured three times today across BL-1277/1280/1281).
Hardener's hand-authored mutation sweep (Stryker blocked by that same
pre-existing baseline, per BL-638 fallback) found and closed one real gap
(a hardcoded `captured: true` surviving mutant) before this reached QA.

## Verdict: APPROVE

By QA.
