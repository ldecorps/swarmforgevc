# BL-1384 land note — clean merge, two-commit split applied proactively, 2026-09-05

## Merge into QA worktree

A plain `git merge c638c66de7` succeeded cleanly this time — no BL-1403
intake-file class, no entanglement with any other ticket's unlanded work
(unlike BL-1364/1365/1383/1402). The documenter's own commits already used
an untagged subject for the two cross-ticket doc edits
(`docs/how-to/BL-1235-*.md`, `docs/how-to/BL-1383-*.md`), correctly
following the established convention on their own.

## Verification (before landing)

Full independent QA pass: unit 725/725
(`telegramFrontDeskBotCore.test.js` 464/464,
`telegramCursorBridgeCore.test.js` 131/131,
`telegramCursorBridgeLive.test.js` 130/130). Property
`bl1384LocalSeatTopicForwardedInvariants` 2/2. Acceptance
`BL-1384-...feature` 4/4, `BL-1235-...feature` (regression, bridge side)
5/5, `BL-1296-...feature` (regression, Bubble seat) 6/6. Both
`required_wiring` anchors confirmed live by direct grep
(`readQwenLocalTopicId` at 3 real call sites: import, topic-map scrub,
`buildPollAdapters` wiring). Read the actual OR-gate and array-threading
directly in the source: `hasCursorBridgeTopicAdapter` now includes
`Boolean(adapters.qwenLocalSeatTopicId)`, and
`attemptCursorBridgeTopicExclusion` passes it into the array given to
`decideCursorBridgeExclusion` — matches the invariant and the hardener's
own OR-fold isolation check (local-seat-only, adapter-absent,
coexistence, and pre-existing-fixture cases all present).

## qa_e2e steps 2-3 deliberately NOT performed

Same posture as BL-1383/BL-1402: a real `local-seat-topic-map.json`
binding, a real local endpoint up/down, and live topic-icon confirmation
require the operator's own action against the live channel. Not treated
as blocking — the one declared invariant is already independently proven
by the coder's property test with both isolating and negative cases.
Recommend the operator exercise this live at their convenience.

## Land — applying the BL-1413 lesson proactively

This land ALSO touches the two cross-ticket doc files
(`docs/how-to/BL-1235-*.md`, `docs/how-to/BL-1383-*.md`), so — learning
from BL-1413's scope-gate trip earlier this session — split the replay
into two commits from the start rather than discovering the trap after
the fact: `fb3957ede2` (tagged `BL-1384: ...`, everything else including
`docs/index.md` and `docs/reference/Specification.MD`, whose basenames
name no ticket) and `8129c8c0f9` (untagged `docs: ...`, `By documenter.`,
carrying only the two cross-ticket how-to pages).
`task_scope_gate_cli.bb BL-1384-... fb3957ede2` confirmed `OK` before
pushing. Built off origin/main in a throwaway `git worktree add
--detach <tmp> origin/main` — zero conflicts on all 16 files. Confirmed
every file byte-identical to the QA-worktree's reviewed content (the
clean merge, `5aa31d3113`). Re-verified compile/unit/property/acceptance
green against that tree before committing. `git diff --stat origin/main
HEAD` (both commits together) showed exactly the 16 intended paths.
`land_main_publish.sh --decide-only` returned `:next :push`,
`:origin-advanced-since-gate false`. Pushed `acd1187721..8129c8c0f9`.
Lock acquired/released around the push per BL-1144.

## Land-approval records (BL-1334) — both commits recorded from the start

Learning applied: recorded TWO rows immediately (not discovered after a
coordinator flag, as with BL-1413), one per pushed commit:
```
{"at":"2026-09-05T07:23:07Z","ticket":"BL-1384","commit":"fb3957ede2","source":"5aa31d3113"}
{"at":"2026-09-05T07:23:07Z","ticket":"BL-1384","commit":"8129c8c0f9","source":"5aa31d3113"}
```
`source` for both is `5aa31d3113` — the QA-worktree's own clean merge
commit this pass actually reviewed and approved (an ancestor of
`swarmforge-QA`). Verified `bash swarmforge/scripts/is_qa_ancestor.sh
fb3957ede2` and `... 8129c8c0f9` both print `approved: ... - BL-1334`.

`abandoned_commits: [5aa31d3113]` recorded on
`backlog/active/BL-1384-the-local-seat-topic-reaches-the-bridge-through-the-front-desk.yaml`.

BL-1384 is not `GH-`-seeded; no `issue_done.sh` step applies.

By QA.
