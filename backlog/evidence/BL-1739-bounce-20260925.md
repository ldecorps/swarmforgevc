# BL-1739 bounce, 2026-09-25: spec-gap, charged to the specifier

- Bouncing input: QA note 003191, "BL-1739 c4c81e8f57 drops
  isAllowedBabysitterMatch; bl781 property red"
- Parcel held by QA: documenter 276c48aa92 (it carries coder c4c81e8f57)
- Producing role: **specifier**. The coder's rebuild did what the ruling on
  coder note 002139 (5f68af5510) said: delete `isAllowedBabysitterMatch` and
  the scan's `offenders` list. The ruling never counted who else reads them.

## Consumer census (the count the ruling should have made)

`git grep -n -e isAllowedBabysitterMatch -e "explicitly KEEPs" -e scanRepoForBabysitter -e "the only matches are the deterministic daemon" 276c48aa92 -- . ':!backlog'`,
leaving out the BL-611 handler itself, finds three consumers. All three are
BL-781's:

| # | Consumer | State on 276c48aa92 | Fix |
|---|---|---|---|
| D1 | `extension/test/bl781LiveGrepOffender.property.test.js` test 3, "BL-611 allowlist does not exempt deleted wake-runtime paths" | red: `isAllowedBabysitterMatch must exist` (QA) | coder rebuilds it to state BL-781's invariant 1 in the new shape |
| D2 | `bl781RetireDeadBabysitterFilesKeepListPreservedSteps.js` `loadBl611Scan` (BL-781 scenario 04) | red, by reading: it slices from `indexOf('function isAllowedBabysitterMatch')` (now -1) and reads `scan.offenders` (now gone) | coder rebuilds it on `forbidden` + `liveCodeMatches` |
| D3 | same handler, "the allowlist paths are read" (BL-781 scenario 03) | red, by reading: the `explicitly KEEPs` anchor is gone | specifier RETIRED scenario 03 on main; coder deletes its three unused step definitions |

D2 and D3 were found from the source, not from a run: the handler's anchors
are missing at 276c48aa92. QA's single run covered D1 only.

## Specifier action

- BL-781 feature: scenario 03 retired on main (never reworded), following
  the BL-1379 comment idiom.
- BL-1739 ticket: What is wanted, Scope, qa_e2e_procedure and notes amended
  on main.
- Notes (priority 00) to QA (holder) and coder (rebuild): merge main,
  re-read the ticket.
