# BL-1443 — hardender review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Ran the full checklist: BL-1443's acceptance feature (4/4 scenario runs),
all four touched property test files (bl1443TolerantTreeWalkInvariants,
bl874PortableTimeInvariants, tempDirTrapGuard, bl1060TelegramButtonUrlScheme
- 22/22 tests), the qa_e2e_procedure's race reproduction (bl868 + bl874 run
together 5 consecutive times, no ENOENT), and all 18 whole-tree standing
guards (183 tests, per the "parcel touches specs/pipeline/steps/" rule).

Coverage-gap hand-mutation (extension/test/helpers/ has no wired mutation
tool - a test helper, not src/*.ts or out/**/*.js, so neither CRAP nor
Stryker scope it): neither declared invariant names `excludeDirs`, and no
test built a tree containing one of its own excluded directory names
(fixtures use generic "sub0"/"sub2" names) - only the real production call
sites (bl874/tempDirTrapGuard walking the whole repo, which genuinely
contains node_modules/.git/out/...) exercised it, incidentally and non-
deterministically. Confirmed by hand-mutating the helper to drop the
`excludeDirs` guard entirely (walk() always recurses): every existing test
stayed green. Closed by adding a property test to
bl1443TolerantTreeWalkInvariants.property.test.js that plants a marker file
inside a directory named for each DEFAULT_EXCLUDED_DIR_NAMES entry
alongside a sibling non-excluded directory, asserting the excluded one is
never reported while the sibling is. Confirmed the new test fails against
the hand-mutant and passes against the real helper; the mutant was reverted
with no diff before committing the fix (8fa03f6ea5).

By hardender.
