# BL-1077 — QA bounce — 20260823

Received parcel was **BL-1082-invariant-2-property-vacuous** (documenter
`9fc28cb57e`, QA merge `816d76937`). Tip is a BL-1082/BL-1077 batch.
Article 4.4 inventory below; one bounce for the tip (BL-532).

## Review inventory (Article 4.4)

### D1 — `swarmforge.sh` zsh quoting for Qwen guard is unparseable (unit / property lane)

**Blamed role:** coder  
**Failure class:** unit  
**Remediation pointer:** `swarmforge/scripts/swarmforge.sh` ~L1717–1721
(`qwen_guard=$'source \''"$SCRIPT_DIR"'/qwen_launch_guard_lib.sh\'...` —
the second segment is a POSIX single-quoted string where `\'` does **not**
escape a quote, so the `if`/`elif` chain that follows fails to parse).

**Failing command (exact):**
```
cd extension && set -a && . /home/carillon/swarmforgevc/.swarmforge/swarm.env && set +a && npm run test:properties
```
Minimal repro (same parse error):
```
zsh -c "source swarmforge/scripts/swarmforge.sh /tmp/x; parse_config"
```

**Commit hash tested:** `816d769374` (full `816d769374269eb5eeec27b24cfec5ee0f6651ea`)

**First error excerpt:**
```
/home/carillon/swarmforgevc/.worktrees/QA/swarmforge/scripts/swarmforge.sh:1732: parse error near `elif'
FAIL  test/bl1078CursorSeatUsesSharedChannels.property.test.js > BL-1078/BL-654 invariant 1: a cursor seat touches exactly the channels every other seat touches
AssertionError: provisioning documenter/cursor failed:
.../swarmforge.sh:1732: parse error near `elif'
126 !== 0
```

**Expected vs observed:** Expected `swarmforge.sh` to source under zsh so
property provisioning can write launch scripts. Observed a parse error at the
`elif` after the BL-1077 `qwen_guard` assignment; `origin/main`'s prior
inline `qwen_guard=$'...'` form sources cleanly.

**Remediation:** Embed the source line without breaking ANSI-C / single-quote
nesting (e.g. keep the whole assignment inside one `$'...'` with `\'` only,
or use a plain double-quoted concatenation that zsh parses). Re-run
`npm run test:properties` and confirm
`test/bl1078CursorSeatUsesSharedChannels.property.test.js` provisions.

### Deferred sibling (not bounced)

- **BL-1082** — no failing check of its own on this tip (see Gates). Deferred
  pending BL-1077 via `qa-sibling-check.js defer` (BL-532). Clear-check is
  the same `npm run test:properties` command above.

### Gates run (complete)

| Gate | Result |
|------|--------|
| Sibling status BL-1082 | VERIFY (exit 0) |
| Compile (`npm run compile`) | PASS |
| Unit + swarm.env | Parcel green; **7 standing reds** outside tip (`sampleResourcesCli`×3, `strykerSandboxSiblingsLib`×4) — already filed `INTAKE-unit-suite-sampleResources-strykerSandbox-standing-red.md` (BL-1063) |
| Properties | **FAIL** — D1 (1 file / 1 test); BL-1082 property file alone 6/6 PASS |
| Acceptance BL-1082 feature | PASS 8/8 |
| required_wiring `specs/pipeline/steps/index.js` → `bl1082NamedModelServingSteps` | PASS; exercised by acceptance |
| Live CLI wiring `extension/out/tools/named-model.js` | PASS (help / pull compose / inside-store refuse) |
| BL-1082 prior architect bounce (vacuous inv2) | CLEARED — property quantifies inside vs outside store + broken-planner non-vacuity |
| Ticket ancestry on tip | coder/cleaner/architect/hardener/documenter commits are ancestors of HEAD |
| Orphans (`node --test` / stryker / vitest) | none left by this pass |

### BLOCKED

None beyond D1 blocking the green property-suite gate for the tip.

### What is NOT bounced

- BL-1082 named-model pull/serve behavior, acceptance, docs, inv2 encoding.
- Standing unit reds outside this tip (intake already on disk).
