# BL-1077 — QA bounce — 20260823

Received parcel was **BL-1082-invariant-2-property-vacuous** (documenter
`9fc28cb57e`, QA merge `816d76937`). Tip is a BL-1082/BL-1077 batch.
Article 4.4 inventory below; one bounce for the tip (BL-532).

## Review inventory (Article 4.4)

### D1 — `swarmforge.sh` zsh quoting for Qwen launch-guard source is unparseable

**Blamed role:** coder  
**Failure class:** unit  
**Remediation pointer:** `swarmforge/scripts/swarmforge.sh` lines 1717–1721 —
the BL-1077 `qwen_guard=$'source \''"$SCRIPT_DIR"'/qwen_launch_guard_lib.sh\'...'`
assignment. After the first `$''` closes, the next segment is a POSIX
single-quoted string where `\'` does **not** escape a quote, so the
following `if`/`elif` chain fails to parse under zsh.

**Failing command (exact):**
```
cd /home/carillon/swarmforgevc/.worktrees/QA/extension && set -a && . /home/carillon/swarmforgevc/.swarmforge/swarm.env && set +a && npm run test:properties
```
Minimal repro of the same parse error:
```
cd /home/carillon/swarmforgevc/.worktrees/QA && zsh -c 'source ./swarmforge/scripts/swarmforge.sh'
```

**Commit hash tested:** `c81b5df9e4` (evidence commit on tip; merge under
test was `816d76937` / documenter `9fc28cb57e`).

**First error excerpt:**
```
/home/carillon/swarmforgevc/.worktrees/QA/swarmforge/scripts/swarmforge.sh:1732: parse error near `elif'
FAIL  test/bl1078CursorSeatUsesSharedChannels.property.test.js > BL-1078/BL-654 invariant 1: a cursor seat touches exactly the channels every other seat touches
AssertionError: provisioning documenter/cursor failed:
.../swarmforge.sh:1732: parse error near `elif'
126 !== 0
```

**Expected vs observed:** Expected `swarmforge.sh` to source under zsh so
property provisioning can write launch scripts. Observed a parse error at
the `elif` after the BL-1077 `qwen_guard` assignment. Pre-change form on
`origin/main` (inline `qwen_guard=$'...'` without the broken
`source \''"$SCRIPT_DIR"'/...\` concatenation) sources cleanly.

**Remediation:** Embed the `source …/qwen_launch_guard_lib.sh` line without
breaking ANSI-C / single-quote nesting (one continuous `$''` with `\'`, or
plain double-quoted concatenation). Re-run `npm run test:properties` and
confirm `test/bl1078CursorSeatUsesSharedChannels.property.test.js` provisions.

### Deferred sibling (not bounced)

- **BL-1082** — no failing check of its own on this tip (see Gates). Deferred
  pending BL-1077 via `qa-sibling-check.js defer` (BL-532). Clear-check is
  the same `npm run test:properties` command above.

### Gates run (complete)

| Gate | Result |
|------|--------|
| Sibling status BL-1082 | VERIFY (exit 0) |
| Compile (`npm run compile`) | PASS |
| Unit + swarm.env | Parcel green; **7 standing reds** outside tip (`sampleResourcesCli.test.js` ×3, `strykerSandboxSiblingsLib.test.js` ×4) — already filed at `.swarmforge/operator/INTAKE-unit-suite-sampleResources-strykerSandbox-standing-red.md` (BL-1063) |
| Properties | **FAIL** — D1 (1 file / 1 test); `test/bl1082NamedModelServingInvariants.property.test.js` alone 6/6 PASS |
| Acceptance BL-1082 feature | PASS 8/8 (`bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1082-the-swarm-can-pull-and-serve-a-named-model-on-this-host.feature`) |
| required_wiring `specs/pipeline/steps/index.js` → `bl1082NamedModelServingSteps` | PASS; exercised by acceptance |
| Live CLI `extension/out/tools/named-model.js` | PASS (help / pull compose outside store / refuse store inside repo) |
| BL-1082 prior architect bounce (vacuous inv2) | CLEARED — property quantifies inside vs outside store + `brokenPullIgnoringRepoBoundary` non-vacuity |
| Ticket ancestry on tip | coder `81b457eed`, cleaner inv2 `1d34d7ab6`, architect `13a779111`, hardener `fdeb74ef5`, documenter `9fc28cb57e` are ancestors of HEAD |
| Orphans (`node --test` / stryker / vitest) | none left by this pass |

### BLOCKED

None beyond D1 blocking the green property-suite gate for the tip.

### What is NOT bounced

- BL-1082 named-model pull/serve behavior, acceptance, docs, inv2 encoding.
- Standing unit reds outside this tip (intake already on disk).
