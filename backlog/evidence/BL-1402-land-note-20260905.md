# BL-1402 land note — bounced-and-fixed ticket, small entanglement, live-photo step deferred, 2026-09-05

## Merge into QA worktree

A plain `git merge 058eb404c4` succeeded past `check_merge_deletion.sh`
(no intake-file class this time) but pulled in a smaller set of unrelated
content than BL-1413 — BL-1420 (an unrelated new ticket), a `BL-539` epic
file touch, and a coordinator note — via a shared ancestor on the
documenter's branch. Aborted and replayed this ticket's own attributed
paths instead: grepped `git log HEAD..058eb404c4` for `^[a-f0-9]* BL-1402:`
subjects (12 commits, covering the full bounce cycle — coder main pass,
architect bounce 1, coder's fix, cleaner passes on both, architect
re-review, hardener, documenter, plus the ticket YAML's own
`bounce_count`/`bounce_history` commit), diffed the union of 20 touched
paths (base `f64066c867`, the commit before the coder's first BL-1402
commit). Two conflicts this time, in the shared `docs/index.md` and
`docs/reference/Specification.MD` append points (same shape as every
other ticket today); resolved keeping both entries in each, this one
first. Every other file confirmed byte-identical to the documenter's
reviewed content.

**Separately, cleaned up a stale duplicate ticket YAML in my own
worktree.** The documenter's evidence noted a coordinator-confirmed,
already-applied cleanup on the coder's side branch (`b15c7fd7b0`,
untagged subject, removes `backlog/paused/BL-1402-*.yaml` — "main never
had this paused copy, only the active one, a leftover from a stale QA
worktree snapshot"). My own QA worktree still carried that exact stale
duplicate (confirmed: missing the active copy's later `bounce_history`,
and confirmed absent from `origin/main`). Removed it in a separate,
untagged commit (`ab2c5a220b`) matching the same reasoning — not part of
BL-1402's own scoped diff since `origin/main` never had the file to begin
with, so nothing needed to ride into the land replay for it.

## Verification (before landing)

Full independent QA pass: unit 475/475 (`telegramFrontDeskBotCore.test.js`
459/459 including the bounce's own two regression tests, plus
`bl1402FrontDeskPhotoPassthrough.test.js` 9/9, `atomicWrite.test.js` 7/7).
Property `bl1402FrontDeskPhotoPassthroughInvariants` 3/3. Acceptance
`BL-1402-...feature` 6/6, `BL-620-...feature` (regression, owns
`annotateRoutedMediaText`) 13/13, `BL-955-...feature` (regression) 8/8.
Both `required_wiring` anchors confirmed live by direct grep
(`persistRoutedPhoto` real call at `telegram-front-desk-bot.ts:2567`
inside `buildPollAdapters`; `registerSteps` export). Read the actual
bounce-1 fix directly in the source rather than trusting the evidence:
confirmed `persistPhotoIfRouted`'s gate is the allowlist
(`decision.action === 'post-existing' || decision.action ===
'operator-context' || isOpenDecision(decision)`) that architect's
remediation asked for — not the original leaky `!== 'drop'` exclusion.
Confirmed `annotateSavedPhotoPath` only appends the saved-path line for
`saved`/`already-saved` outcomes (returns `text` unchanged for
`failed`/`not-applicable`), matching invariants 1 and 2 exactly, and
confirmed it is applied AFTER `annotateRoutedMediaText` at all three
consuming call sites (`post-existing`, `operator-context`, the open-
decision path).

## qa_e2e steps 2-3 deliberately NOT performed

Same posture as BL-1383's land note: sending a real captioned photo
through the LIVE running front desk and confirming it lands on disk plus
the topic-icon behavior requires the operator's own action against the
real, human-facing Telegram channel — not something this pass takes
unilaterally. Not treated as blocking: all three invariants are already
independently and thoroughly proven by the coder's property tests
(reachability floors over all three failure modes for invariant 1, all
four outcome kinds for invariant 2, both the prune-bound and the
idempotency-redelivery breaks for invariant 3), each shown non-vacuous
against a deliberately broken implementation. Approved on the strength of
that fixture-based verification; recommending the operator send a test
photo to a bound topic at their own convenience.

## Land

`task_scope_gate_cli.bb BL-1402-... e04a077fb5 .` (run against the built
replay commit directly, from inside the throwaway worktree) returned
`OK`. Diffed the QA-worktree merge commit (`961ef6c7fa`) against its own
parent, applied in a throwaway `git worktree add --detach <tmp>
origin/main` — zero conflicts. Confirmed every file byte-identical to the
QA worktree's reviewed content. Symlinked `extension/node_modules` AND
`extension/out` (same reason as BL-1413: this ticket's own files compile
fine, but the acceptance runner loads every other registered step
handler too). Re-verified compile/unit/property/acceptance green against
that tree; removed both symlinks before committing. `git diff --stat
origin/main HEAD` showed exactly the 20 intended paths.
`land_main_publish.sh --decide-only` returned `:next :push`,
`:origin-advanced-since-gate false` (no rematch needed — origin had not
moved since the throwaway worktree forked). Pushed
`720a7743e0..e04a077fb5`. Lock acquired/released around the push per
BL-1144.

## Land-approval record (BL-1334)

Appended to the shared store (master checkout's `.swarmforge/land-
approvals/2026-09.jsonl`):
`{"at":"2026-09-05T07:03:32Z","ticket":"BL-1402","commit":"e04a077fb5","source":"ab2c5a220b"}`.
`source` is `ab2c5a220b` — the QA-worktree's own final commit this pass
reviewed and approved (an ancestor of `swarmforge-QA`; its parent,
`961ef6c7fa`, is the actual content-bearing merge, with `ab2c5a220b`
itself only the stale-duplicate-yaml cleanup layered on top).
Verified `bash swarmforge/scripts/is_qa_ancestor.sh e04a077fb5` prints
`approved: ... - BL-1334`.

`abandoned_commits: [ab2c5a220b]` recorded on
`backlog/active/BL-1402-the-front-desk-keeps-a-routed-photo-so-the-operator-can-see-it.yaml`.

BL-1402 is not `GH-`-seeded; no `issue_done.sh` step applies.

By QA.
