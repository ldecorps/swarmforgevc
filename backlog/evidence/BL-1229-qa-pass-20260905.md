# QA pass — BL-1229 — 2026-09-05

Commit under review: `5745ebf85f` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`77bcd451a5` BL-1229
coder, confirmed ancestor via `git merge-base --is-ancestor`).

This is the structural guard BL-1221 (landed earlier this session)
deliberately left out of scope, ruled on 2026-08-28: "a missing deps
member must not be filled in with a silent default." Reviewed with
particular attention to that firm constraint.

## Verification

- Read `extension/test/helpers/pilotAcceptanceGateDeps.js` directly:
  `baseAcceptanceGateDeps()` is a plain explicit object literal — no
  Proxy, no auto-defaulter, matching the ruling exactly.
- **Independently verified non-vacuity myself** (not just trusting the
  hardener's evidence): temporarily deleted the
  `checkOrphanedAuthoredDocs` line from `baseAcceptanceGateDeps()`, ran
  `test/pilotAcceptanceGateDepsCompleteness.test.js` — it failed,
  correctly naming `checkOrphanedAuthoredDocs` as the missing member.
  Restored the file; `git diff` confirms byte-identical afterward.
- `test/pilotAcceptanceGateDepsCompleteness.test.js`: 3/3 pass, including
  the synthetic-interface non-vacuity checks (a fabricated
  `newRequiredMember` is reported; a fabricated `optionalMember?` is not).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1229PilotGateDepsContractInvariants.property.test.js`: 2/2 pass.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1229-pilot-gate-deps-contract-cannot-silently-outgrow-its-test-stubs.feature`
  — 6/6 scenarios pass, including "A missing deps member is never
  silently defaulted" and "Widening the contract costs one failure, not
  one per test file" — the ruling's own two demands, both directly
  asserted.
- Ran all 9 unit-lane files migrated to the shared factory directly:
  `pilotAcceptanceGate.test.js`, `crossFileDuplicationCheck.test.js`,
  `pilotScopedCrapCheck.test.js`, `perHatRolePromptEvidenceCheck.test.js`,
  `multiBranchParserCoverageCheck.test.js`,
  `shellEntryPointDriveCheck.test.js`,
  `unreachableStepHandlerCheck.test.js`,
  `pilotMkdtempConventionCheck.test.js`, `propertyGeneratorReachCheck.test.js`
  — 95/95 tests pass across all 9 files.
- `npm run compile`: clean.
- Full unit lane (`npm test`): 6 files failing, identical set to my
  BL-1221 evidence (`backendSwitch`, `constitutionDocCitations`,
  `operatorRuntimeBbFixtureClosure`, `pricingTable`, `telegramClient`,
  `telegramCursorOperatorExec`) — no new failures, no regression.
- Full property suite (`npm run test:properties`): exit 0, clean.
- Registry cleanup: `grep -c BL-1229 backlog/standing-reds.tsv
  property_suite_standing_allowlist.tsv` both 0 — rows removed in the
  same commit per BL-1428's rule.
- `required_wiring`: `specs/pipeline/steps/bl1229PilotGateDepsContractStubsSteps.js`
  exports `registerSteps`.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — the human ruling's constraint (loud failure, never silent
defaulting) is genuinely enforced, verified by breaking it myself.
Merging up and landing.
