# BL-1566 — architect review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- Merged QA note f8d1fd2b61 (BL-1570 hold resumed) and cleaner's a119e9e430
  (BL-1566) into the architect worktree; both ancestry checks clean.
- `node extension/out/tools/dependency-gate.js` (full-repo, no args) —
  PASSED: no forbidden edges. Parcel touches no `extension/src` file, no
  webview/host boundary at stake.
- `node extension/out/tools/co-change-report.js` on the four changed
  production files — every co-change at frequency 1, below the default
  threshold of 3; nothing flagged.
- Two-layer / IO / secrets / integrate-not-fork checks: not applicable —
  parcel is Babashka scripts + a step handler only, no VS Code API, no
  webview, no secrets, no upstream-fork touch.
- Invariants Review (BL-633/654): one declared invariant ("a released hold
  is never silently dropped"). Coder's evidence
  (`BL-1566-coder-invariant-encoding-20260914.md`) takes the stated-reason
  path: the invariant quantifies over the whole call graph including
  future callers, not one pure module, so no executable property test can
  encode it; verified instead by reading the source (grep for
  `fs/delete`/`fs/move` across `qa_hold_lib.bb`/`qa_hold_cli.bb` — exactly
  one hit, inside `cmd-close!`, after the record is already written to
  `closed-dir`). Accepted: this is a genuine non-encodability, not an
  evasion — confirmed independently with my own
  `grep -rn "qa-holds" swarmforge/ specs/` sweep of the whole parcel: no
  site outside `qa_hold_cli.bb` touches the store at all, so there is no
  second site the property could be violated at.
- Property Testing (undeclared): no pure JS/TS module touched — the new
  production code is Babashka (no fast-check tooling wired for bb, BL-472
  deferred) plus a step handler that drives real CLIs over a fixture; no
  action needed.
- Required wiring (three anchors) verified by reading the diff directly,
  not eyeballed from prose: `ready_for_next_task.bb`'s
  `print-qa-hold-status-if-any!` calls `qa-hold-lib/` (ns-qualified) from
  the live receive path; `done_with_current_task.bb`'s `qa-hold-gate!`
  calls `qa-hold-lib/` from the live completion path, ahead of the BL-1422
  gate; `specs/pipeline/steps/bl1566ArticleFortyTwoHoldRecordSteps.js` is
  picked up by `specs/pipeline/steps/index.js`'s file-discovery (BL-1371,
  `*Steps.js` suffix) — no hand-maintained registration list to edit.
- DRY: `red-owner`/`open-ticket-ids-for` reuse
  `standing_red_register_lib/parse-register-rows` and
  `pipeline_stage_lib/extract-ticket-id` rather than reimplementing
  register or ticket-id parsing — matches cleaner's note, confirmed by
  reading both call sites.
- Ran directly (not just re-trusted upstream evidence):
  `bb swarmforge/scripts/test/qa_hold_lib_test_runner.bb` — ALL PASS;
  `node specs/pipeline/cli.js
  specs/features/BL-1566-an-article-42-hold-is-a-record-qa-resumes-from.feature`
  — 8/8; `bb swarmforge/scripts/test/suite_inventory_cli.bb` — ok, 517
  files, 513 standing, 4 excluded; the four standing shell suites cleaner
  named (`test_done_with_current_arg_rejection.sh`,
  `test_ready_for_next_no_promotion.sh`,
  `test_done_with_current_work_note_evidence.sh`,
  `test_ready_for_next_rotate_home.sh`) — all pass.
- `.swarmforge/qa-holds/` sits under the repo-wide `.swarmforge/` gitignore
  entry — the runtime store cannot land a stray commit.
