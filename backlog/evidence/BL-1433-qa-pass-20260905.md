# QA pass — BL-1433 — 2026-09-05

Commit under review: `f14c43acee` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`189434b39d` BL-1433
coder, confirmed ancestor via `git merge-base --is-ancestor`).

This is the exact defect behind the stream of stale "branch behind X: ...
- merge up" notes I drained repeatedly throughout this session (always
already-ancestor, never actionable) — confirmed now fixed at the root.
Reviewed with corresponding care since it changes the daemon's live sweep
logic.

## Verification

- `bb swarmforge/scripts/test/post_qa_branch_sweep_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/bl1433_branch_holds_landed_commit_property_runner.bb`:
  ALL PROPERTIES HOLD (500 runs/property).
- `bb swarmforge/scripts/test/bl1433_supplier_git_facts_runner.bb`: ran
  directly — output `{"can-ff?":false,"contains-landed?":true,...}` against
  a real git fixture (a role branch = origin/main plus one commit of its
  own, the exact shape that broke on 2026-09-05) — proves the daemon's new
  fact composition against real git, not a mock.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1433-a-branch-that-holds-the-landed-commit-is-not-behind.feature`
  — 6/6 scenarios pass, including "twenty cycles against a branch that is
  merely ahead produce no note" — directly disproving the original 61-notes-
  in-15-minutes symptom.
- Regression: BL-1421's own property runner (500/500 runs hold) and
  acceptance feature (6/6) both still green — the containment check this
  ticket adds runs before, not instead of, BL-1421's suppression.
  `test_bl1361_sweep_tells_roles.sh` (9/9) — BL-1361's every-reason-told
  contract holds unchanged.
- `required_wiring`: `handoffd.bb` defines `:contains-landed?` (line 4081)
  in the daemon's fact supplier; `specs/pipeline/steps/bl1433BranchHoldsLandedCommitSteps.js`
  exports `registerSteps`.
- Docs: `docs/how-to/BL-668-post-qa-deterministic-branch-sweep.md` updated
  with the new decision-order table row and rationale (read the table's
  double-negative phrasing carefully — "HEAD does NOT already contain..."
  as the row's *check*, failing into `:holds-landed`, is logically correct
  once parsed as a gate-for-settled-status table, not a mislabel).
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Out-of-scope note (not this ticket's defect)

The merge also carries `backlog/evidence/BL-1263-bounce-20260905.md` (an
architect bounce for a DIFFERENT, still-in-flight ticket) and its
underlying commit `e682bfa163` as ordinary ancestry from shared-branch
pipelining (BL-1241's shape) — not part of this `git_handoff`'s named
task. Confirmed `telegramClient.test.js`/`telegramCursorOperatorExec.test.js`
are still partially red (4/111 failing) — BL-1263 is genuinely incomplete,
not silently entangled. Not this parcel's scope; no action taken on it
here.

## Verdict

APPROVED — merging up and landing.
