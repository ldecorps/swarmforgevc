# BL-1773 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter eb919fd684, merged into QA as 2895775873. Checked against
both parents: the merge base is origin/main 43787cccb4, and apart from
QA-only evidence and backlog files the result equals the documenter's tip. The
merge also brought in-flight BL-1700 rework, BL-1761, BL-1767 and BL-1772
content, none of it this parcel's.

Gates, one run each on 2895775873, full logs under `tmp/BL-1773-*`:
- `qa-sibling-check.js status`: `VERIFY BL-1773`. `pre_qa_gate.sh`: OK (the
  step handler exports `registerSteps`).
- standing-red register: 6 rows, `unowned: []`.
- `npm test`: 10878 of 10879 passed. The one failure is
  `extension/test/briefingDigestLineCli.test.js` "the compiled CLI runs
  against the real repo and prints both lines": `Error: Test timed out in
  20000ms.` The register owns that file (BL-1770, row 41), and this parcel
  does not touch it.
- `npm run test:properties`: 469 files, 1349 of 1349 passed, including
  `bl1773RepointLandedParcelInProcessInvariant.property.test.js`.
- qa_e2e 1: the BL-1773 feature passed 4 of 4 in 4 s (1 scenario plus 3 rows).
- qa_e2e 2: the BL-1438 feature passed 4 of 4 in 6 s; its in_process row still
  skips.
- qa_e2e 3: `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` ALL PASS.
- Article 4.5 for `land_step_lib.bb`: also passing were
  test_land_step_records_approval.sh, test_bl1772_push_repoints.sh and
  test_is_qa_ancestor_land_replay_store.sh.

Change read (`land_step_lib.bb`, 59 added and 2 removed lines, all BL-1773):
`post-land-repoint!`'s in_process guard now lets one parcel through, and only
when `only-landed-tickets-own-pending?` holds: exactly ONE pending file, a
`git_handoff` whose `task:` extracts, via `pipeline-stage-lib/extract-ticket-id`,
to the landed ticket. An unreadable file returns nil headers and blocks, fail
closed. With no landed ticket in view, everything blocks. The dirty-tree guard,
the origin/main guard, the keep/drop classification and the
skip-never-fails-the-land promise are untouched, which matches the FIRM line
and the declared invariant.

qa_e2e 5 (own paths): `land_step_lib.bb`, `land_step_lib_test_runner.bb`, the
feature, `bl1773RepointLandedParcelInProcessSteps.js`, the property test, the
BL-1241 how-to section and evidence. The land is tip-pure. qa_e2e 4 is recorded
at the land.

## Verdict revised: NOT approved, spec gap S1 (found preparing the land)

The NONE above covers the gates. Preparing this parcel's own land (qa_e2e
step 4), the real QA worktree's `.swarmforge/handoffs/inbox/in_process` held
TWO files:
- `00_20260926T041649Z_001514_from_documenter_to_QA_for_QA.handoff` (the
  BL-1773 `git_handoff`);
- `00_20260926T041649Z_001514_from_documenter_to_QA_for_QA.handoff.claim-progress.json`,
  handoffd's claim-progress sidecar (`claim_progress_lib.bb`), content
  `{"claimCommit":"edbbab1530","claimAtMs":1790396722328,"reclaims":0}`. It is
  rewritten whenever the worktree HEAD moves during a parcel, and QA always
  commits evidence before a land, so it is present at every QA land.

`post-land-repoint!` counts every non-dot file as pending, so
`only-landed-tickets-own-pending?` sees 2 files and returns false. A read-only
probe on that directory (loading this parcel's `land_step_lib.bb` and calling
the predicate with "BL-1773") printed:
- on the real in_process (with the sidecar): `false`;
- on the handoff alone: `true`.
So from QA's worktree the re-point still skips with "a parcel in its
in_process" on every land, which is what qa_e2e step 4 says must never be
printed and what the ticket exists to end.

Class: spec gap. The ticket's own blocking list names "a second file beside
the landed one", the coder implemented that literally, and neither the
description nor the feature (whose row says "a second parcel") considers the
landed parcel's own claim-progress sidecar. The specifier must decide whether
a sidecar of the landed parcel counts (for example `<landed parcel
file>.claim-progress.json` treated as part of that parcel; any other file
still blocking). Sent as a `note` to the specifier and coordinator; no
`git_handoff`, nothing reverted (an omission, BL-1348). The parcel is not
landed. While this change sits on the QA branch, other lands run origin/main's
copy of the land tools.
