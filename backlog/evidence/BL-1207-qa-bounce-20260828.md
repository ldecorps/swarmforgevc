# BL-1207 — QA bounce — 20260828

## D1: documenter pass missing entirely

1. **Failing command, exactly as run:**
   ```
   find docs/how-to -iname "*1207*"
   grep -n "1207" docs/index.md
   grep -n "BL-1207" docs/reference/Specification.MD
   git log --oneline --all --grep="BL-1207"
   ```
2. **Commit hash checked out and tested:** `90aa76d44a7b66fb226dd2f85941c0bdb4ac2f1d`
   (QA's own merge of documenter's received tip `e45c84c6ec`, this parcel's
   `merge_and_process` commit).
3. **First error excerpt:** the first three commands returned **empty** — no
   `docs/how-to/BL-1207-*.md` file exists anywhere in the repo, no
   `docs/index.md` line mentions BL-1207, and `docs/reference/Specification.MD`
   has no BL-1207 entry. The fourth command's output contains coder ×1,
   cleaner ×2 (handoff + evidence), architect ×1, hardener ×2 (fix +
   evidence) — never a documenter-titled commit.
4. **Failure class:** `behavior` — identical shape to Article 4.3's own
   worked example (BL-1204/BL-1203 this same project, both bounced for a
   documenter pass missing entirely).
5. **Expected vs observed:** expected a how-to page, a `docs/index.md` link,
   and (if warranted) a `Specification.MD` entry, or — failing that — at
   minimum a recorded documenter-authored commit judging the ticket
   doc-exempt, matching the precedent this same ticket's own **cleaner**
   stage set one hop earlier (`206ed31ca`: "evidence(BL-1207): cleaner pass —
   no cleanup needed, forwarding unchanged" — a deliberate, recorded no-op).
   Observed: zero documenter output of any kind.

## Why this is a real gap, not a QA merge-resolution artifact

`git show --stat e45c84c6ec` is a merge of hardener's branch (`f61cbabf7`)
into documenter's own worktree line (which was mid-processing BL-1228 at the
time, tip `6ce6c9920b`), resolving two conflicts
(`extension/test/bl1211OperatorCli.test.js`, `specs/pipeline/steps/index.js`)
with **zero commits of documenter's own content** on top. Every file BL-1207
actually touches (`extension/test/cursorBridgeAgentSession.test.js`,
`specs/pipeline/steps/bl1207AbandonedLockLivenessSteps.js`,
`specs/pipeline/steps/index.js`) was already present at hardener's tip
`f61cbabf7`, unchanged by the merge — confirmed by
`git diff --name-status f61cbabf7 e45c84c6ec`, which lists only the BL-1228
files documenter was independently carrying (`6ce6c9920b`'s own scope), none
of BL-1207's.

## Is documentation actually warranted here, or is this ticket doc-exempt?

Considered and rejected treating this as a legitimate silent no-op:
production code is untouched (correct, per the ticket's own firm
constraint) and there is no new CLI/command/config surface — but the ticket
is not merely a private fixture tweak. Its own description explicitly frames
it as a **testing-discipline lesson with two named sibling precedents**
(`test/bl984FixtureSweep.property.test.js`'s `DEAD_PID_BASE` reasoning and
`test/telegramCursorBridgeRedeploy.test.js`'s pid-1 EPERM stand-in) that "the
repo already knows... and it simply was not applied here." That is exactly
the shape this session's `docs/how-to/` pattern already covers for
internal-machinery/guard tickets with no user-facing behavior
(`BL-1205-tree-collapse-guard.md`, `BL-1213-parcel-rollback-guard.md`,
`BL-1228-active-pool-freshness-hold-divergence-audit.md` — none of those
ship a new command either). A short how-to capturing "pick a dead-pid
stand-in from a declared, on-host-verified constant, never a small literal"
is the durable artifact that stops this class recurring elsewhere, which is
the same reasoning QA used to bounce BL-1204/BL-1203 for the identical gap
shape. Not asserting a `Specification.MD` entry is necessarily required
(no user-facing behavior changed) — that call is the documenter's, along
with whatever page content is warranted; what is not optional is that the
stage leave a recorded pass judging it, one way or another.

## Everything else checked — genuinely clean, not just unexamined

Completing the full inventory before this one-item bounce (Article 4.4 —
never first-failure-stop):

| Gate | Result |
|---|---|
| Ancestry (coder, cleaner, architect, hardener) | CONFIRMED all ancestors of received tip `e45c84c6ec` |
| Compile (`npm run compile`) | Clean |
| Targeted file (`vitest run test/cursorBridgeAgentSession.test.js`) | 63/63 PASS |
| Acceptance (`run_acceptance.sh BL-1207-abandoned-lock-verdict-is-host-independent.feature`) | 8/8 PASS |
| Full unit suite vs. this session's already-verified pre-existing-debt baseline (BL-1228 pass, same day) | Identical failing-test set, zero new failures — diff is empty |
| Production code touched? | No — `extension/src/bridge/cursorBridgeAgentSession.ts` untouched, confirmed by `git diff --name-status` against hardener's tip |
| Scenario 03 (declared dead-pid unreachable on this host) | Re-confirmed directly: `ps -p 42` returns nothing on this host (a *different* host fact than the ticket's mint-time journald pid, underscoring why the fix is host-independence, not a host-specific patch); `DEAD_PID=99000001` verified unreachable by the test itself (ESRCH/ERANGE, never EPERM) |
| Scenario 04 non-vacuity (guard fails on a re-added `"  42  \n"`-shaped case) | Present as its own test, keyed on the structured `MALFORMED_LOCK_CASES` list, never a prose grep — matches the ticket's explicit anti-trap constraint |
| `specs/pipeline/steps/index.js` wiring | `bl1207AbandonedLockLivenessSteps` registered cleanly alongside `bl1228ActivePoolFreshnessHoldAuditSteps` (same parcel batch) — no collision |
| Collateral-revert repair (hardener restored BL-1207's fix after my own BL-1227 batch-merge revert collaterally dropped it) | Verified: `f61cbabf7` is a clean, self-contained restore, confirmed present in the final tree |

**The underlying test fix is solid, host-independent, and precisely matches
every constraint and scenario in the ticket** — this bounce is narrowly
about the missing documentation layer, not a doubt about the fix's
correctness.

## Routing

Per Article 4.3 (a stage's pass missing entirely routes to THAT stage
regardless of failure class) and this project's own worked precedent
(BL-1204, BL-1203): **documenter**. No pipeline stage before or after
documenter needs to re-run; re-send directly to QA once a how-to page (or a
recorded, reasoned doc-exempt judgment) exists.

By QA.
