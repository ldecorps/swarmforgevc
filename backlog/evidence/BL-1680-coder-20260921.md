# BL-1680: the local seat fixture pins the model it fakes — coder

## What changed

`specs/pipeline/steps/bl1384LocalSeatTopicForwardedSteps.js` — the
`runLocalSeatTurnFn` wrapper (scenario 02's own fixture) now imports
`DEFAULT_LOCAL_SEAT_MODEL_ID` from `extension/out/tools/localQwenSeat` and
passes `modelId: DEFAULT_LOCAL_SEAT_MODEL_ID` beside the faked catalogue
(`catalogue: [DEFAULT_LOCAL_SEAT_MODEL_ID]`, was the hardcoded literal
`'qwen3:14b'` in both places) — the two can never drift apart again.
Per `resolveLocalSeatModelId`'s own contract
(`extension/src/tools/localQwenSeat.ts:44-56`), a non-empty `configured`
argument (the pinned `modelId`) always wins over
`process.env.SWARMFORGE_LOCAL_SEAT_MODEL`, so the fixture no longer reads
the live host's environment at all.

No production code changes: `extension/src/tools/localQwenSeat.ts` and
`localQwenSeatLive.ts` are byte-identical to `origin/main`
(`git diff --stat origin/main -- <both paths>` is empty).

## Verification

- `echo $SWARMFORGE_LOCAL_SEAT_MODEL` on this host: `qwen2.5-coder:latest`
  (confirmed the standing-red incident's own precondition).
- `node specs/pipeline/cli.js` on BL-1384's own feature: 4/4 (was 3/4,
  scenario 02 red, before this fix — reproduced the red first, then
  confirmed green after).
- `node specs/pipeline/cli.js` / `run_acceptance.sh` on BL-1680's own new
  feature: 3/3, run 3x for stability.
- `grep -c 'modelId' specs/pipeline/steps/bl1384LocalSeatTopicForwardedSteps.js`:
  ≥1.
- `git diff --stat origin/main -- extension/src/tools/localQwenSeat.ts extension/src/tools/localQwenSeatLive.ts`:
  empty (production files untouched, per the ticket's own FIRM claim).
- `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4 (the new step
  handler creates no raw temp roots — it spawns child processes and calls
  the real `runLocalSeatTurn` directly).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1680LocalSeatFixturePinsItsModelInvariants.property.test.js`:
  1/1, plus one non-vacuity break below.
- `npm test` (compile + unit lane): 10847/10848 pass. The one failure
  (`test/stepHandlerModuleLoadBudget.test.js`,
  `bl1146HostQueueEnqueueNextHoldOnHostQuestionSteps.js` over its 400ms
  confirm-alone budget) is the same pre-existing, already-owned standing
  red seen during this session's other parcels — `backlog/standing-reds.tsv`
  names it under **BL-1658**; nothing in this parcel touches any
  cursor-bridge/step-handler require path.

## Invariant (BL-654)

BL-1680's one declared invariant has a coder-authored, non-vacuous
property test in
`extension/test/bl1680LocalSeatFixturePinsItsModelInvariants.property.test.js`:
drives the real `runLocalSeatTurn` with the SAME injection shape the fixed
handler now uses (pinned `modelId` beside a matching faked catalogue)
across a generated matrix of `SWARMFORGE_LOCAL_SEAT_MODEL` values (unset,
and four foreign models the fixture's catalogue does not hold), asserting
the completion is ALWAYS requested for the pinned model and the turn is
ALWAYS an answer, never a does-not-hold refusal. Non-vacuity: removed the
`modelId` pin from the test's own call (simulating the pre-fix leak) and
confirmed the property fails, reproducing the EXACT original error message
("Local seat cannot answer: the endpoint is up but does not hold
\"qwen2.5-coder:latest\" (it holds: qwen3:14b)..."); restored, re-verified
green.

## Notes

- The census in the ticket's own invariant text (six callers of the real
  `runLocalSeatTurn`) is unaffected elsewhere — `bl1235LocalQwenSeatSteps.js`
  and the four `.test.js` files it names already pinned/stubbed before this
  ticket. This parcel's new `bl1680LocalSeatFixturePinsItsModelSteps.js`
  is a NEW, seventh caller (scenario 02's direct-call test) and pins
  `modelId` too, so the census stays "every caller pins or scrubs."
- The standing-red register row for BL-1384's feature (`acceptance
  <BL-1384 feature> BL-1680 2026-09-21`, added in the mint commit) is
  retired by the land per coder.prompt's standing rule — not touched in
  this parcel.

By coder.
