# BL-1319 — documenter pass, 2026-09-02

Reviewed commit `b0262530c0` (hardener clean sweep). Real production
behavior change: the coordinator's dwell/bottleneck optimizer instrument
(`extension/src/metrics/stageDwell.ts`, BL-102) now reads a non-bare seat's
mailbox and folds its records onto its bare stage BEFORE the
`PIPELINE_ORDER` membership test that used to drop the entry entirely.
Sibling of BL-1040 (landed earlier the same day), same epic
(multi-seat-stages), reuses the same `stageOfSeat` chokepoint.

## Doc sweep

- Checked `docs/` for existing coverage of the stage-dwell/bottleneck
  instrument: no dedicated how-to exists (BL-102, its original ticket,
  never got one); coverage lives entirely in `docs/reference/Specification.MD`
  and briefing-integration how-tos (`BL-600`, `BL-602`), neither of which
  describes seat handling. Consistent with this project's pattern: most
  fixes get a `Specification.MD` entry only, a standalone how-to is reserved
  for cases with enough operational surface to warrant one (as BL-1040's
  was).
- Added a dated `Specification.MD` entry (commit `2d9c1c6653`), bumping
  `Last Updated` in the same commit as the content change: the drop-not-leak
  distinction (a seat entry fails `PIPELINE_ORDER` membership and its
  mailbox is never opened, vs. BL-1040's leak-into-the-report defect), the
  fix (`stageOfSeat` deciding membership and grouping before the drop
  point), what's preserved (per-seat records via `deriveDwellRecords` and
  the new `computeSeatDwellDetail` export, single-seat output unchanged),
  latency at ship time, and the checked-and-correct-as-is surfaces
  (supervisor/chase/ensure/babysitter).
- Cross-linked BL-1319 from the BL-1040 how-to's Related section (commit
  `7885587965`, subject deliberately untagged — that file's basename names
  BL-1040, a different already-shipped ticket, per the standing cross-ticket
  doc-edit rule) rather than authoring a second standalone how-to: the two
  tickets are close enough in scope and timing that a cross-link serves a
  reader better than a near-duplicate page, and BL-1319 itself named no
  request for a how-to sibling (unlike the BL-848 stamp-off intakes, which
  explicitly ask for one).
- No diagram registered under `docs/diagrams/` depicts the dwell instrument
  or its ranking logic; none of the four registered diagrams' change-
  triggers fired. No diagram update owed.
- No RETIRED/deprecated behaviour involved — Article 3.6 not implicated.
- Confirmed doc-only diff: `git diff b0262530c0..HEAD --stat` touches only
  `docs/` and `backlog/evidence/`.

## Verdict

NONE — no documentation defect found; coverage added for the new fold.
Forwarding to QA.
