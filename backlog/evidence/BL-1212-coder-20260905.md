# BL-1212 — coder pass, 2026-09-05

## What landed

One `BL-1038-EXEMPT:` comment added to
`extension/test/docsStructureRealTree.test.js`, immediately above the
`REPO_ROOT` binding, recording why the live read is justified: BL-757's
whole purpose is running the docs-orphan checker against the real `docs/`
tree (a fixture-only gate catches nothing, which was the defect BL-757
was filed for), and its non-vacuity case already uses its own fixture
tree rather than depending on this live read. No other file touched, no
guard code touched, no other exemption added.

## Pre-fix baseline (qa_e2e item 1)

`npx vitest run test/liveRepoDerivationGuard.test.js` — **19 passed, 0
failed, ZERO violations reported**, not the ticket's expected "1 failed...
BOTH violations named". BL-1209 (this ticket's dependency) is already in
`backlog/done/`, so per the ticket's own item 1 escape clause ("if only
one appears, BL-1209 has landed... say so rather than treating the step
as failed") that half is expected. What is NOT anticipated by the ticket:
`docsStructureRealTree.test.js`'s own violation is ALSO already gone,
independent of BL-1209, confirmed and explained below.

## Confirmed finding: scenario 02 cannot be honestly satisfied as written

`docsStructureRealTree.test.js`'s `REPO_ROOT` binding changed from the
classic `path.join(__dirname, '..', '..')` idiom to
`execFileSync('git', ['-C', __dirname, 'rev-parse', '--show-toplevel'],
...)` in commit `533da24a41` ("BL-1317: hardener pass... fix two
BL-1066-class sandbox path bugs", **2026-09-02**) — eleven days AFTER
this ticket was minted (`backlog/topics/BL-1212.json`, first message
timestamp resolves to **2026-08-27**). BL-1212's own description and
qa_e2e were written against the file's ORIGINAL derivation shape, which
BL-1317 changed for an unrelated, correct reason (Stryker sandbox
path-depth safety).

`extension/test/helpers/liveRepoDerivationGuard.js`'s two detection paths
(`LIVE_ROOT_BINDING_RE`, line 39, and `LIVE_ROOT_INLINE_SRC`, line 85)
BOTH require the literal `path.join(__dirname, '..', '..')` text. Neither
recognizes an `execFileSync('git', ..., 'rev-parse', '--show-toplevel')`
binding as "the live root" at all. Verified directly against the real,
current file content (three independent checks, `node -e`):

```
liveRepoDerivation(text)                     -> null   (no derivation detected)
violationFor(file, text)                     -> null   (no violation - already true before this fix)
violationFor(file, <reason stripped>)        -> null   (STILL no violation - the marker check is never reached)
```

The exemption this ticket adds is real, correct, and asked for — it
matches BL-1038's own stated policy ("a file that genuinely must observe
the live repository stays, behind an exemption that records why") and
scenario 01 is honestly satisfied by it. But it is inert against the
guard's own mechanical behavior: the guard was ALREADY not flagging this
file (for the unrelated BL-1317 reason above) before this ticket touched
anything, and stripping the exemption's reason does not make it start
flagging it now. Scenario 02's own literal claim ("a bare marker is still
refused") is therefore FALSE against the real file as it exists today -
not because the fix is wrong, but because the file's derivation shape no
longer matches what the guard can see, for a reason entirely unrelated to
this ticket or its dependency.

**Not fixed here**: extending the guard to also recognize
`execFileSync('git', ..., 'rev-parse', '--show-toplevel')`-style root
derivation is a change to the guard's own detection boundary — explicitly
out of scope per this ticket's own constraints ("Do NOT weaken the guard,
widen its boundary... this ticket adds an exemption, it does not
renegotiate what counts as a violation").

A `note` (priority 00) was sent to the specifier alongside the original
pass, flagging the confirmed drift and asking for adjudication.

**Resolved 2026-09-05** (specifier note
`00_20260905T163957Z_001352_from_specifier`): scenario 02 RETIRED on
main (RETIRE-WITH: BL-1435, "a root derived through git rev-parse is a
live read the guard can see" — mints the guard-widening fix and carries
the bare-marker scenario forward), retired per BL-1006 (never reworded).
This coder pass merges that main state and drops the now-dead scenario-02
step handlers accordingly — the acceptance feature is 2/2 green.

## Declared invariant (BL-654) — stated reason, no new property test

The declared invariant ("A live-repository read in the unit lane is
either removed or JUSTIFIED IN WRITING... the guard keeps refusing a bare
marker...") describes the GUARD'S OWN pre-existing, general mechanism —
already covered exhaustively by `liveRepoDerivationGuard.test.js`'s own
existing tests (`'an exemption with a recorded reason is honoured'`,
`'a BARE exemption marker with no reason is NOT honoured'`, and eight
more in the same file), none of which this ticket authors or modifies.
This ticket's own change is a single data edit (one comment, no new
production code, no new mechanism) to one file already governed by that
pre-existing, already-property-adjacent-tested mechanism. There is no new
executable surface for a coder-authored property test to encode -
BL-654's "no meaningful randomizable input domain" shape, the same as
BL-1290/BL-1221/BL-1263's own stated-reason invariants this session.

## Verification

| check | result |
|---|---|
| `npx vitest run test/liveRepoDerivationGuard.test.js` | 19/19 pass, 0 violations (unchanged by this fix - already true before it, for the BL-1317 reason above) |
| `npx vitest run test/docsStructureRealTree.test.js` | 5/5 pass, unchanged (qa_e2e item 5: adding a comment changes nothing about what it gates) |
| acceptance `BL-1212-real-tree-docs-gate-records-its-live-read-exemption.feature` | **2/2** after scenario 02's retirement (main-merged), stable across two runs |
| `npm run compile` | clean |

### qa_e2e_procedure

1. Baseline recorded above: 19/19 pass, 0 violations (not the expected "1
   failed, BOTH violations" - BL-1209 landed AND the second violation is
   independently gone, both explained above).
2. Scenario 01: exemption present, reason recorded, judged as a reviewer
   would - it says the live read IS the assertion, in the file's own
   terms (mirroring `pricingTable.test.js`'s own phrasing per the
   ticket's ask).
3. Scenario 02 non-vacuity: **run for real, genuinely fails** - stripping
   the reason does not reproduce a violation, because the guard's own
   pattern-matcher does not detect this file's current derivation shape
   at all. This is the confirmed finding above, not a gap in this
   verification.
4. Scenario 03: the whole guard test passes, 19 passed, 0 failed, no
   violations - true today, independent of this ticket's own fix (see
   finding above).
5. Regression: `docsStructureRealTree.test.js`'s own suite is unchanged,
   5/5, including its real-tree assertions and its fixture-based
   non-vacuity case.

## Constraints respected

- Did NOT exempt `pilotMkdtempConventionCheck.test.js` - untouched;
  BL-1209 already fixed its own defect.
- Did NOT weaken the guard, widen its boundary, or touch
  `liveRepoDerivationGuard.js` - confirmed via `git diff --name-only`.
- Did NOT rewrite `docsStructureRealTree.test.js`'s real-tree assertions
  or its fixture-based non-vacuity case - only the one comment added.
- Did NOT add any other exemption.
