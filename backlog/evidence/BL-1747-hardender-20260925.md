# BL-1747 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `extension/test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`
only. mutation_cost: low; no production code touched. Both coder and
architect already independently reverted the fix and confirmed the
floor is genuinely flaky without it (coder: 3/3 revert runs failed;
architect: reverted `draws` only, keeping the new stricter both-sides
floor, and got 4/8 failures — consistent with the higher per-cell miss
rate the stricter floor implies: `(13/14)^40 ≈ 5.7%` per (kind, side)
cell x 14 cells ≈ 56% chance at least one is missing, matching the
observed ~50%).

Independent hand-mutation, checked directly (a third, more targeted
check than either prior stage's revert-the-whole-fix approach — proves
the floor catches a SINGLE missing cell, not just gross absence of the
constructed set):
- Backed up the file. Mutated `constructedDraws()` to skip exactly one
  cell (`kind === 'whitespace-only' && indexEmpty === false`) and
  replaced `draws = constructedDraws().concat(fc.sample(inputArb, 40))`
  with `draws = constructedDraws()` (dropping the sampled draws too, so
  the run is fully deterministic — no chance the sampler could
  coincidentally cover the dropped cell).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1474...property.test.js`: **failed as expected**, on exactly
  `false:whitespace-only` (`generator never produced non-empty-index
  stderr kind "whitespace-only"`) — confirms the floor checks each of
  the 14 cells independently, not an aggregate count.
- Restored the file (`git diff --stat` clean), reran: green.

Checks re-run:
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1474ReplayCommitRefusalReasonInvariants.property.test.js`:
  1/1 green.
- `node specs/pipeline/cli.js
  specs/features/BL-1474-the-replay-reports-why-its-commit-was-refused.feature`:
  5/5.
- `git diff main...HEAD --name-only -- extension/test` (excluding files
  from other in-flight tickets still on this branch tip) names exactly
  the one Scope file.
- Every standing whole-tree guard (this parcel touches
  `extension/test/`): `npx vitest run $(ls test/*Guard*.test.js | grep
  -v '\.property\.')` — 22 files, 211 tests, all green.

Not run: CRAP/DRY/Stryker (no `src`/`out` file in Scope), `npm run
test:properties` in full (2026-09-17 rule — ran the one touched
property file directly).

Fixture hygiene: `git status --short` clean of anything this pass's
runs could leak (pre-existing untracked BL-1652 scratch files predate
this session, not mine to remove — surfaced, not swept).

By hardender.
