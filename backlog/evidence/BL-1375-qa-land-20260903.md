# BL-1375 — QA land pass (stranded expedite branch), 2026-09-03

## Task

Coordinator note (priority 00): "Land stranded expedite/BL-1375 (tip
`c370d1e28a`) onto main; evidence 20260903." Per
`backlog/evidence/BL-1375-expedite-branch-unlanded-20260903.md` (specifier),
the expedite run for BL-1375 passed all seven internal stages (including its
own QA hat, recorded in `.swarmforge/expedite-approvals/2026-09.jsonl`) and
moved the ticket to `backlog/done/`, but the driver never pushes to `main` by
design — the branch `expedite/BL-1375` was left unlanded and unnamed in the
run's own `outstanding` block. Landing pipeline code on `main` is QA's own
action (Article 1.8/4.2, BL-247), never the specifier's.

## Legitimacy check on `human_approval`

The ticket's `human_ruling:` is substantive (a specific rider about
`check_feature_handler_registration.sh` and the BL-1324 shape, not an echoed
`(recommended)` label), was recorded (`cfde1aa954`) BEFORE any of the three
coding commits, and `human_approval: approved` is present identically on both
`main` and `origin/main` (level, `0`/`0` ahead/behind at gate time). Treated
as genuine per the four-point verification recipe; not fabricated.

## Verification performed (commit `c370d1e28a`, pre-rematch)

- `bb swarmforge/scripts/land_step_cli.bb BL-1375 c370d1e28a` → `LAND_CLEAN`
  (no entangled siblings; the active→hold park files are this run's own
  attributed paths, not another ticket's unlanded content).
- `npm run compile` — clean.
- Unit suite (`vitest run`): 25 failures across 15 files — **identical set**
  to a same-config run against unmodified `main` (ran both, diffed the FAIL
  list byte-for-byte). Standing debt, already ticketed: BL-1212, BL-1221,
  BL-1229, BL-1263, BL-1265, BL-1289, BL-1290, BL-1291 (all in
  `backlog/paused/`). Not a BL-1375 regression.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1375-approved-siblings-sharing-a-path-can-land.feature`
  — 7/7 pass, including "nothing unapproved reaches main", the
  withheld/unreadable-sibling-still-blocks cases, and both passenger-guard
  scenarios.
- `vitest run --config vitest.properties.config.mjs
  bl1375ApprovedSiblingsCanLandInvariants` — 3/3 invariants pass.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — ALL PASS.
- `bash swarmforge/scripts/test/test_check_feature_handler_registration.sh`
  — ALL PASS (9/9).
- `required_wiring` both confirmed live, not merely defined:
  `entangled-siblings` is called from two sites in `land_step_lib.bb`
  (own-paths gate, batch/replay path); `check_feature_handler_registration.sh`
  is invoked from the passenger-guard code path (`land_step_lib.bb:928`);
  `bl1375ApprovedSiblingsCanLandSteps` is `require`d in
  `specs/pipeline/steps/index.js:952`.
- `pgrep -fl 'node --test|stryker|vitest'` before and after: only another
  role's own stryker-wait shell loop (hardener worktree, unrelated) — no
  orphaned processes of this pass.

## Publish-time rematch (BL-1144)

`origin/main` had advanced past the branch's base with an unrelated commit
chain (BL-1378 mint, unrelated backlog bookkeeping — no path overlap with
BL-1375's own diff). `git merge origin/main` onto `c370d1e28a` hit one
modify/delete conflict:
`backlog/INTAKE-operator-bl1375-passenger-must-pass-registration-guard-1788427676244.md`
(deleted by BL-1375's own drain commit `22e28654fb`, appended-to by the
specifier's status note on `origin/main`). Resolved in favour of the delete —
the specifier's own appended text in that file explicitly instructs exactly
that resolution. Result commit `76206f8255...` (parents `c370d1e28a` and
`origin/main` at push time `177681d768`).

The rematch also re-tripped `check_ticket_deletion.sh` (BL-901) on the
active→hold park of BL-1296/1309/1356/1359/1360 (the guard doesn't recognise
hold/ as a surviving path for an active/ deletion) — satisfied per its own
documented exemption by naming all five ticket ids in the commit message; not
a retirement, the park stands from the original expedite run and those five
remain held pending human unblock.

Re-verified after rematch: `land_step_cli.bb BL-1375 <rematched-sha>` →
`LAND_CLEAN` again; `npm run compile` clean again;
`land_main_publish.sh --decide-only` → `:next :push`.

## Landed

- Pushed `76206f82556c726f0c411c77d2412884524c7dfd` to `origin/main`
  (`177681d768..76206f8255  HEAD -> main`), via the BL-1144 lock/decide/push
  discipline (acquire → decide-only → push → release).
- `BL-1375`'s ticket record was already in `backlog/done/` (moved
  prematurely by the expedite run itself); no further bookkeeping move is
  needed there. `human_approval`/`human_ruling` reflect the real ruling on
  both `main` and `origin/main` post-land.
- Not `GH-`-seeded (`source:` is a QA note, not a GitHub issue) — no
  `issue_done.sh` call applies.
- BL-1376 (the general reporting-gap defect this incident motivated) is
  independently active in `backlog/active/`, assigned to coder — untouched
  by this pass, no action needed here.

By QA.
