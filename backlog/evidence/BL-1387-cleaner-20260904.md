# BL-1387 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made beyond the merge resolution
itself (recorded on the merge commit, `5491e0d6fc`).

## Merge entanglement with the bounced BL-1386

Coder built this commit (`a51b4d68fe`) directly on top of BL-1386's original
`7836b7364a`, before architect's bounce of BL-1386 (`9dbd2a11c3`, D1: the
daemon's real dispatch never wires `owns-merge-head?`/`may-abort-failed-merge?`)
reached this branch. BL-1387's classifier calls BL-1386's lib primitives
(`owns-merge-head?`, `read-merge-owner`) directly, so it cannot function
without them — resolved by taking coder's incoming version of the four
conflicted files (`handoffd.bb`, `master_main_reconcile_lib.bb`, the two
`master_main_reconcile_lib` test/property runners) wholesale, which
necessarily re-carries BL-1386's lib primitives.

This is recorded, not swept: BL-1386's D1 defect is still unfixed after this
merge. BL-1387's `:own -> :skip-human-merge-in-progress` mapping is
deliberate ("BL-1386 finishes it; this ticket never acts") — it reads
ownership for classification but does not act on it. BL-1386 stays bounced,
`assigned_to: coder`, `bounce_count: 1`, unchanged by this merge; BL-1386's
own (reverted) acceptance step-handler/CLI files stay absent from the tree.

## What was checked (BL-1387's own scope)

- `master_main_reconcile_lib_test_runner.bb` — re-ran: ALL TESTS PASS.
- `master_main_reconcile_lib_property_runner.bb` — re-ran: ALL PROPERTIES
  HOLD, 500 runs, including BL-1387's declared 8-cell owner-signal generator
  reach and its own non-vacuity probe.
- `test_handoffd_master_main_reconcile_wiring.sh` — re-ran: ALL SCENARIOS
  PASS; correctly carries no BL-1386 assertions (consistent with the
  standing bounce/revert — coder's BL-1387 work never touched this file).
- `jscpd` over the two new files
  (`bl1387OrphanedMergeSurfacedSteps.js`, `lib/bl1387OrphanedMergeCli.sh`)
  plus the modified `handoffd.bb`/`master_main_reconcile_lib.bb`: 0 clones.
- Mutation-site size (BL-485): `bl1387OrphanedMergeSurfacedSteps.js` reports
  125 sites (`over` the 100 threshold). Reviewed and declined to split, same
  reasoning as BL-1386's sibling handler: one cohesive step-handler module
  for a single feature, ~13 tightly-scoped Given/When/Then defs, site count
  driven by assertion density rather than mixed responsibilities or
  complexity.
- Architecture: the new daemon adapters (`master-main-live-git-process?`,
  `master-main-index-lock-state`, `master-main-git-paths`,
  `master-main-index-carries-incoming?`, `master-main-open-merge-class`,
  `master-main-orphaned-merge-escalation`) are all side-effecting reads
  confined to `handoffd.bb`; the classification itself
  (`classify-open-merge`, `open-merge-branch`, `index-carries-incoming?`,
  `orphaned-merge-message`/`orphaned-merge-escalation`) is pure in the lib.
  No IO leaked into the pure lib.
- TypeScript compiles clean (`npx tsc --noEmit`); the new handler discovers
  correctly via BL-1371's registry (`loadHandlers()` finds it).

No bounce history on BL-1387 itself (first pass). Forwarding unchanged to
architect.

By cleaner.
