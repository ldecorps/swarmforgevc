# BL-1405 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed `land_approval_cli_lib.bb` (`short`, `already-recorded?`) and
  `record_land_approval.bb` (the thin CLI): `already-recorded?` is a pure
  READ of the store using the exact same 10-hex truncation the real writer
  applies (computed once via `short`, never re-derived), satisfying
  invariant 1 (one writer, no second serializer) — the CLI calls
  `land-step-lib/record-land-approval!` directly for the actual write, and
  the exact-field JSON substring match (`"commit":"<c>"` /
  `"source":"<src>"`) cannot false-match across different shas since both
  sides are fixed-length hex after truncation.
- Confirmed `land_step_lib.bb` and `is_qa_ancestor.sh` are untouched by
  this diff (`git diff <base> <head> --stat` names neither) — matches the
  ticket's explicit "no predicate/store-shape change" scope, and the BL-1334
  regression suite (`test_land_step_records_approval.sh`,
  `land_step_lib_test_runner.bb`) confirms nothing shifted underneath it.
- `-main`'s missing-sha refusal (invariant 2) writes nothing before
  exiting — verified by reading the control flow: `short` returns nil for
  a too-short/absent sha, and the nil check exits before any write path is
  reached.
- No duplication: `jscpd` doesn't parse `.bb` (0 files analyzed,
  informational only), but a manual read confirms `short` is computed once
  and reused correctly, and `print-verdict!` reuses the real
  `is_qa_ancestor.sh` rather than reimplementing "approved" — exactly the
  ticket's own "never a reimplementation" direction.
- Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred) — this
  pass is gated by the unit/property/acceptance suites only, per
  constitution.
- Noted with appreciation: the coder's own evidence documents catching and
  fixing a vacuity gap in the FIRST draft of the property test itself (a
  non-git fixture silently made every write refuse, so
  `already-recorded?` always answered false vacuously) before shipping —
  a good practice worth nothing further from me here.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bash swarmforge/scripts/test/test_record_land_approval_cli.sh` (new) | 5/5 |
| `bb swarmforge/scripts/test/bl1405_hand_built_land_records_approval_property_runner.bb` | ALL PROPERTIES HOLD, 300/300 each of P1/P2 |
| `node specs/pipeline/cli.js specs/features/BL-1405-...feature` | 5/5 scenario runs |
| `bash swarmforge/scripts/test/test_land_step_records_approval.sh` (BL-1334 regression) | ALL CHECKS PASSED |
| `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` (regression) | ALL PASS, unaffected |
| `bl1405HandBuiltLandRecordsApprovalSteps.js::registerSteps` exists | yes (required_wiring) |

No fix required. Forwarding unchanged to architect.
