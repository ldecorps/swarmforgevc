# BL-1243 QA pass — 2026-08-30 (post-bounce)

## Entangled tip, rebuilt tip-pure (BL-1241) — again with BL-670

The re-submitted parcel (documenter `b64714f946`) carries unlanded BL-670
work as an ancestor (still `status: todo, assigned_to: coder`, itself
mid-bounce-cycle — not landable). Isolated BL-1243's own contribution by
diffing the full merge against my own prior clean base (`e179209d0`, my QA
branch tip from before this merge) with the two BL-670-named paths excluded
(`backlog/active/BL-670-...yaml`, `backlog/evidence/BL-670-...bounce-...md`)
— 13 files remained, applied cleanly onto a fresh branch from current
`origin/main`, and re-verified there (below) before landing.

## All four bounced defects (BL-1243-bounce-20260830.md) confirmed fixed

- **D1** (invariant 3 unimplemented): `resolvePaneStatusKind` now checks
  `if (aggregateKind === 'err') return aggregateKind;` FIRST, before the
  `pane.activitySignal` early return — read directly in the diff, scoped
  correctly to `err` only (a merely `stale` aggregate still yields to the
  pane's own signal, matching the ticket's own constraint).
- **D2** (scenario 06 had no step handler): the new `When` handler
  (`the poll is failing and a role pane's own last signal was ok`) now
  exists, builds a busy pane (`activitySignal: 'ok'`) and sets
  `ctx.bl1243.aggregate = 'err'`.
- **D3** (reused `Then` step hardcoded `'ok'`): now reads
  `const aggregate = ctx.bl1243.aggregate ?? 'ok';` dynamically, and adds a
  stronger assertion for the err case (`assert.equal(kind, 'err', ...)`,
  not merely not-ok) — exceeds what I bounced on.
- **D4** (docs claimed a fix that didn't exist): `Specification.MD`'s
  BL-1243 entry now accurately describes the shipped fix; cross-checked
  against the real diff, no remaining overclaim.

## Re-verified on the clean tip-pure base (not just trusted from the entangled branch)
- `npm run compile`: clean.
- `specs/features/BL-1243-live-screen-per-pane-activity-signal.feature`:
  **7/7** — including scenario 06, which now genuinely passes (was the one
  failure I bounced on).
- `extension/test/bl1243PaneActivitySignal.test.js`: 8/8.
- `extension/test/bl1243PaneActivityInvariants.property.test.js`: 4/4
  (invariants 1 and 2; invariant 3 has no dedicated property test — not
  treated as a residual defect, since scenario 06 exercises it through the
  REAL extracted production function, not a restatement, and the
  hardener's hand-authored mutation sweep against the exact D1 fix killed
  all 4 mutants, including negating the `=== 'err'` check itself).
- required_wiring re-confirmed: `residentPaneLive.ts::activitySignal` set
  on the snapshot; `specs/pipeline/steps/index.js` registers
  `bl1243LiveScreenPerPaneActivitySteps`.

## Verdict: APPROVE (as the rebuilt tip-pure commit, not the entangled parcel)

By QA.
