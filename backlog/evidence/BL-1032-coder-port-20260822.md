# BL-1032 — coder pass, 2026-08-22 (ported onto the live line)

## Same cutover stranding as BL-1038, but further along

BL-1032's delivered work is absent from `main`, `origin/main` and the live
`swarmforge-coder` line, and present only on the `origin/cutover/wsl-2026-08-22-*`
snapshot. Unlike BL-1038, it had already run nearly the whole pipeline there —
the chain is linear and ends at QA:

    89e04323a  coder       the guard scopes by hazard, not by a quoted token   <- PORTED HERE
    cc7cb4cf29 cleaner     merge coder BL-1032 into cleaner
    4d4a718a4  architect   guard correctly scopes by hazard, both invariants verified
    ac1e31cba  hardener    closed 3 real hand-mutation gaps in tmuxReaperGuard.js
    2dedd5ac7  documenter  document the hazard-scoped tmuxReaperGuard
                           (tip, on origin/cutover/wsl-2026-08-22-QA)

Each is an ancestor of `2dedd5ac7`, and `89e04323a` is an ancestor of all of them.

**Downstream roles: port your own stage's commit above rather than redoing it.**
The review, hardening and documentation work exists and is sound; only its branch
is wrong. This parcel deliberately ports the CODER stage only — delivering the
later stages as coder work would bypass this line's own gates, which is not the
coder's call. Nothing else from the cutover branch rides along (BL-506).

Conflict on port: `specs/pipeline/steps/index.js`, against BL-1048's and
BL-1038's registrations landed on this line minutes earlier. All three kept.

## Verification

- `extension/test/tmuxReaperGuard.test.js` — **12 tests, all pass**, and the
  real-tree scan is now GREEN. Before the port this file was RED on this line
  (7 tests, 1 failing) flagging `bl1018SingleRoleRepairNeverKillsServerSteps.js`.
  That red is this ticket's own reported symptom, and the port clears it.
- Acceptance `BL-1032-tmux-reaper-guard-scopes-by-hazard-not-by-token.feature`
  — **4 pass / 0 fail**.
- `bl1032TmuxReaperScope.property.test.js` — **2 pass** (both declared invariants).

## The firm constraint held

`approval_context` is firm that BL-1018's step file must go green **without**
gaining a reaper call — "adding one to satisfy a gate is a lie the next reader
has to disbelieve". Confirmed: `specs/pipeline/steps/bl1018SingleRoleRepairNeverKillsServerSteps.js`
has no commit in this parcel's range. It went green because the guard now scopes
by hazard, which is exactly the recommended resolution.

## Still red on this line, pre-existing, owned elsewhere

`tempDirTrapGuard.test.js` flags
`swarmforge/scripts/test/bl1025_expedite_approval_property_runner.bb`
("creates a temp root but has no shutdown hook and no try/finally delete-tree").
Last touched by `71ee902a2` (BL-1025 D1 re-fix), which is an ancestor of both
`main` and `origin/main` — red on `main` itself, unrelated to this parcel, and
not folded in.
