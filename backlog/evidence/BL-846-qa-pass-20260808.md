# BL-846-role-answer-cannot-reach-a-rotating-role-live-pane — QA final pass (2026-08-08)

## Commit checked

`a3b5a01e58` (documenter, tip of the BL-846 chain), merged into QA. Ancestry
confirmed via `git merge-base --is-ancestor`: coder `e2ef8824a3`, architect
`5349835a`, and hardener `f9efff77` are all ancestors of `a3b5a01e58`.

BL-722 rode the same architect/hardener batch pass (Article 2.6 splitting)
but was independently bounced by the documenter (`backlog/evidence/BL-722-bounce-20260808.md`,
blamed coder, `required_wiring` gate fail) and did not reach this parcel —
this pass covers BL-846 only.

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`, node v20.20.2): clean.
- Scoped suite (`npx vitest run --coverage telegramFrontDeskBotCli
  pilotSafeDefects telegramCursorBridgeCore telegramCursorBridgeLive bl846`):
  512/512 passed, matching the hardener's own evidence exactly.
- Property tests (`npx vitest run --config vitest.properties.config.mjs
  test/bl846ResidentPaneResolutionFollowsIdentity.property.test.js
  test/bl846RoleAnswerDeliveryNeverLessDeliverable.property.test.js`): 2/2
  passed.
- Acceptance (`node specs/pipeline/cli.js
  specs/features/BL-846-role-answer-reaches-the-active-resident-pane.feature`):
  9/9 scenarios PASS, none skipped — matches hardener's evidence.
- Full unit suite (`npm run test`, node v20.20.2): ran twice under a
  severely loaded host (`uptime` load averages 100–136 across both runs,
  4-core host). Failures differed between the two runs (9 failures the
  first run, 7 the second, no stable overlapping set) — the signature of
  load-induced flakiness, not a deterministic regression:
  - `dependencyGateCli*` failures (5 tests, 3 files): root-caused to an
    environment/toolchain mismatch, not this parcel's diff —
    `depcruise` (dependency-cruiser 18.0.0, installed in
    `node_modules`) requires node `^22||^24||>=26`; this host runs node
    `20.20.2`. Confirmed by reading the actual subprocess stderr
    (`ERROR: Your node version (20.20.2) is not supported`). Pre-existing
    on `main` regardless of this ticket — `dependency-gate.ts` is untouched
    by BL-846's diff (only `telegram-front-desk-bot.ts`,
    `telegramCursorBridgeLive.ts` changed).
  - `renderBriefingDiagramsCli` / `startBridgeHeadlessCli` subprocess
    timeouts: re-ran both files in isolation (no concurrent suite
    contention) and both passed cleanly (`renderBriefingDiagramsCli`: 4/4,
    `startBridgeHeadlessCli`: 13/13) — confirms load-induced timeout, not a
    code defect. Neither file is touched by this parcel's diff.
  - None of the observed failures, in either run, touch
    `telegram-front-desk-bot.ts`, `telegramCursorBridgeLive.ts`, or
    `residentPaneSpy.ts` — the only files this parcel's diff changes.
- No orphaned test/mutation/vitest processes before or after
  (`pgrep -fl 'node --test|stryker|vitest'` clean both times).

## Required wiring re-verified by reading the code

`extension/src/tools/telegram-front-desk-bot.ts::readMonoRouterActiveRole`:
confirmed imported from `../concierge/residentPaneSpy` (line 174, the
already-exported reader — no second reader added) and consulted inside the
new `resolveMonoRouterAwareRoleEntry` helper (line 1317), which
`resolveRolePaneTarget` now calls (line 1339) in place of the old direct
`roles.find`. `resolveRolePaneTarget` itself has a real caller,
`redirectToRole` (line 1359) — the live-pane delivery leg used by both the
BL-425 steering-redirect path and the role-answer delivery path named in
the ticket's "wider than answers" section.

## Invariants checked against the diff

- Invariant 1 (no other role/marker-state redirected): `role !== 'coordinator'`
  guard plus exact `=== role` marker match in `resolveMonoRouterAwareRoleEntry`;
  hardener's hand-walked mutation analysis (4/4 mutation points killed) and
  the property test `bl846ResidentPaneResolutionFollowsIdentity` cover this
  directly.
- Invariant 2 (queued-note leg still runs when live-pane doesn't deliver):
  both `redirectToRole` (line 2098) and `enqueueRoleAnswerNote` (line 2108)
  remain wired at the same call site as before this parcel — confirmed by
  reading the surrounding dispatch code; unchanged per the ticket's
  out-of-scope section.

## Scope check (out_of_scope honoured)

- `ready_for_next.sh`, the in-process guard, and all seven role prompts:
  untouched (diff stat confirms no `swarmforge/roles/*.prompt` or
  `ready_for_next*` file in this parcel).
- Answer capture / pending-marker lifecycle (`captureRoleAnswer`,
  `clearRoleAwaitingAnswer`): not present in this diff at all.

## Verdict

PASS. Approving BL-846-role-answer-cannot-reach-a-rotating-role-live-pane.
