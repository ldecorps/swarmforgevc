# BL-1309 — QA APPROVAL, 20260903

Follows two prior QA passes that both withheld approval solely on
legitimacy grounds, not correctness:
`BL-1309-qa-spec-gap-unverified-ruling-20260903.md` ("If this ticket had a
genuine ruling behind it, I would be recording APPROVE here") and
`BL-1309-qa-still-pending-20260903.md`.

## What changed

`human_approval` is now `approved` and `human_ruling:` is present
(`refuse every entangled tip, so the tip-pure replay becomes the normal
land`) — matching `ruling_options` option 1 and the shipped code exactly.

Cross-checked legitimacy per the standing four-point method, given this
exact ticket's history of two prior false approvals (a self-flip by the
coder, then a pager route that discarded the ruling text):

1. `bb42148fbf` ("Approve BL-1309: record human_approval") — records the
   operator relay's confirmation of the human's in-session answer, not a
   role's own say-so; matches the specifier's own filed provenance
   (`backlog/answers-archive/ANSWER-2026-09-03-bl1309-refusal-width-
   refuse-every-entangled-tip-relayed.md`), which explicitly named "one
   tap on BL-1309 restores it" as the outstanding condition.
2. `7465ccaaec` (coordinator) independently cross-checked `bb42148fbf`
   against that same answers-archive text before flipping `status:
   blocked` → `todo`.
3. `human_ruling:` text is present, non-empty, and matches
   `ruling_options` option 1 verbatim — the exact field the two prior
   failure shapes (BL-1367 pager-drop, BL-1368 bot byline) always left
   absent.
4. The shipped code was independently confirmed (prior QA pass) to
   implement option 1 exactly, with no withheld/pending/hold predicate —
   so this is not a case of the ruling arriving after the fact and
   needing a rebuild; the two are already consistent.

## Re-verification (code unchanged since the prior full technical pass;
re-run rather than assumed)

- `npx vitest run --config vitest.properties.config.mjs
  bl1309LandDecideEntanglementInvariants` — 2/2 (both declared
  invariants).
- `bash swarmforge/scripts/test/land_main_publish_test_runner.sh` — 8/8
  PASS (including the entangled-sibling refusal and every fail-open row).
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1309-
  the-mandatory-land-decide-step-is-blind-to-entanglement.feature` — 6/6.
- `required_wiring` confirmed both entries: `ENTANGLED_SIBLING_BLOCK`
  literal present in `land_main_publish.sh`; `bl1309LandDecideStepEntanglementSteps`
  registered in `specs/pipeline/steps/index.js:953`.
- No orphaned test/mutation processes before or after.
- `git status --short backlog/hotfix-ledger.yaml` — clean, untouched.

## Verdict

APPROVE. Merge-up broadcast sent; landing next.

By QA.
