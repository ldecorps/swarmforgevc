# BL-1653 — hardender review pass, 2026-09-19

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb swarmforge/scripts/test/commit_integrity_lib_test_runner.bb (baseline, then hand-mutated fail-restoring calls staged? flag for :verify-mismatch, then restored)
- **Commit hash**: 4e0a929168
- **First error excerpt**: flipping :verify-mismatch own staged? argument (true to false) left every existing assertion green; only :commit-failed own index-restored value was ever asserted
- **Failure class**: test-coverage-gap
- **Expected vs observed**: index-restored is reported for every failure branch that really staged something (:commit-failed and :verify-mismatch alike), never only one of them
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added an assertion to the existing verify-mismatch scenario 3 case; commit 4e0a929168; confirmed it now kills the hand-applied mutant

## D2

- **Failing command**: bb swarmforge/scripts/test/briefing_email_test_runner.bb (baseline, then hand-mutated commit-sent-marker! hardcoding restored? to false, then restored)
- **Commit hash**: 4e0a929168
- **First error excerpt**: the tickets own new index-restored/index-left-dirty reporting in commit-sent-marker! had zero coverage - the one existing commit-failure test sh-fn never distinguished the restore call and nothing asserted the returned map beyond :ok
- **Failure class**: test-coverage-gap
- **Expected vs observed**: a commit failure reports index-restored when the restore succeeds, index-left-dirty when it does not, and neither when a failed git add staged nothing
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added three cases (restore succeeds/fails/nothing-to-restore); commit 4e0a929168; confirmed 2 of them fail against the hand-applied mutant

By hardender.
