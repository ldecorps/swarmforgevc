# Evidence — BL-1326 — coder pass (2026-09-02)

Role: coder. Ticket: BL-1326-bob-restaff-anthropic-coder-qwen-db7e3f2bda.
Routed by coordinator note `20260902T142106Z_003429`.

Review-only stamp-off of Cursor hotfix `db7e3f2bda` ("Restaff bob BoB
starting cast: Anthropic seats, coder on qwen3.8-max"). This parcel CONFIRMS
the land. It reimplements nothing, re-staffs nothing, and writes nothing to
the ledger.

## Invariants held, verified not asserted

- **inv 1 — never reimplements.** `git status` over the pack conf,
  `swarmforge.sh` and the ledger is EMPTY at handoff. The whole diff is the
  acceptance handler, its zsh predicate driver, the registration, and this
  file.
- **inv 2 — green never certifies.** Scenario 05 asserts the ledger row is
  still awaiting a human AND that the ledger file is unmodified in the
  working tree. Row for `db7e3f2bda`: `state: stamp-open`,
  `stamp_ticket: BL-1326`, `human_decision: null`.
- **inv 3 — BL-1322's bundled files are not re-reviewed.** The Background
  filters them out of the changed-path assertion by name, and neither is
  touched. Their presence in this commit's diff is recorded below as the
  specifier's own observation, not re-adjudicated here.

## qa_e2e procedure — all five items walked

1. **Window staffing.** Read from the commit, not the working tree. At
   `db7e3f2bda:swarmforge/packs/bob-multi-provider-mono-router.conf`:
   `coder` is `claude coder --model qwen3.8-max ... --effort high`;
   `specifier`/`architect`/`QA` are `--model claude-sonnet-5 --effort high`;
   `cleaner`/`hardender`/`documenter` are `--model claude-sonnet-5 --effort
   medium` (each with its `batch` arg); no window line names `coordinator`.
   All seven rows asserted by the Outline against explicit KNOWN_VALUES.
2. **Coordinator config.** `config coordinator_agent claude` and
   `config coordinator_model claude-sonnet-5` both still set (lines 42-43).
3. **The remap predicate — EXECUTED, not read.** Only the coder window's CLI
   tokens match `extra_cli_targets_qwen_cloud`; every other window answers
   `none`. See "how" below.
4. **Header and PREREQ prose.** The header states the Best-of-Breed
   *starting point* framing ("Anthropic on every seat except the resident
   HOME (coder)"), carries the explicit warning ("do not treat this file as
   the old multi-vendor mix until those seats are deliberately restaffed"),
   and the PREREQ block lists "Anthropic subscription (claude) for every
   non-coder seat" plus "BAILIAN_TOKEN_PLAN_API_KEY=sk-sp-… (or QWEN_API_KEY)
   for the coder seat".
5. **`Hotfix-Certification: pending` left standing.** No ledger write.

Also confirmed, though the procedure does not ask: the pack conf is
**byte-identical between `db7e3f2bda` and the reviewed tip** — the staffing
under review is still the staffing in force, not a historical state that has
since drifted.

## How scenario 02 executes the real predicate

`extra_cli_targets_qwen_cloud` lives in `swarmforge/scripts/swarmforge.sh`,
which is **zsh** and is **the swarm launcher**. Both facts shaped the driver
(`specs/pipeline/steps/lib/bl1326QwenRemapPredicateCli.zsh`):

- It EXTRACTS the function's own text from the live file with awk and evals
  it, rather than copying the body into the driver. A copy would drift from
  the shipped predicate silently, and this scenario's entire claim is about
  what the SHIPPED code does with the SHIPPED conf.
- It does NOT source `swarmforge.sh`. Sourcing the launcher has side effects
  up to and including tearing down a live swarm.
- It runs under `zsh`, not `bash`: the predicate uses zsh word-splitting
  (`${=extra_cli}`) and 1-indexed arrays, so under bash it would silently
  answer the wrong thing rather than fail loudly.

Confirmed independently that `swarmforge.sh` is untouched by `db7e3f2bda`
(`git diff --quiet db7e3f2bda^ db7e3f2bda -- swarmforge/scripts/swarmforge.sh`),
which is what keeps the predicate itself out of scope: only which seats it
now catches is under review, and that is a property of the conf.

### The extracted predicate IS the reviewed one — checked, not assumed

Extracting from the LIVE `swarmforge.sh` is only faithful if that function
still matches the reviewed commit, and `swarmforge.sh` HAS changed since
`db7e3f2bda` — at `e5ab1c4465` (BL-1318, pack launch refuses a
steward-uncleared model), nothing to do with this parcel. So the check was
made rather than assumed: `extra_cli_targets_qwen_cloud`'s own text is
**byte-identical** between `db7e3f2bda:swarmforge/scripts/swarmforge.sh` and
the reviewed tip. Scenario 02 therefore executes exactly the function that
was live when the restaff landed. Had it differed, the driver would have had
to extract from the commit rather than the working tree, and this evidence
would say so.

## SPEC GAP raised by note (priority 00), not encoded quietly

The ticket's `required_wiring` anchor reads
`specs/pipeline/steps/index.js::bl1323BobRestaffAnthropicStartingCastSteps`.
**BL-1323 is a different, live ticket** — "main-sync deadlock hints name
overlaps and teach swarm heal" — with its own feature file already in
`specs/features/`. The anchor is stale from the BL-1323 → BL-1326 remap the
recovery wave performed.

Naming this handler `bl1323*` would have satisfied the anchor's literal text
while pointing at the wrong ticket and colliding with the real BL-1323's own
handler if one is ever written. It is named
`bl1326BobRestaffAnthropicStartingCastSteps.js` instead — matching its own
feature file — and the discrepancy is raised to the specifier by `note`
(`00_20260902T143812Z_001560`). The wiring the anchor exists to prove is
satisfied either way: the handler IS registered in
`specs/pipeline/steps/index.js` and the runner executes every scenario, which
is the only thing `required_wiring` can prove.

## The specifier's bundled-files observation, confirmed

`db7e3f2bda`'s diff does carry `backlog/paused/BL-1322-*.yaml` and
`specs/features/BL-1322-*.feature` alongside the conf — verified by diffing
against its parent, which has neither. Per invariant 3 this parcel does not
re-review their content. Recording only that the observation is factually
correct: a Cursor-attributed hotfix commit did swallow a specifier-authored
ticket mint, which is the shape the "never `git add -A` on a shared checkout"
guidance exists to catch. Whether that warrants any action is the human's
call; nothing here is factually wrong and no follow-up was opened.

## Checks run this pass

- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1326-...feature` — **11/11 pass** (was 11 failing: no step
  handler existed). New handler
  `specs/pipeline/steps/bl1326BobRestaffAnthropicStartingCastSteps.js` +
  driver `specs/pipeline/steps/lib/bl1326QwenRemapPredicateCli.zsh`,
  registered in `specs/pipeline/steps/index.js`.

One correction made during authoring, recorded because it is the kind of
assertion that passes for the wrong reason: the header-prose steps first
matched against the raw comment block and failed, because the sentence WRAPS
across comment lines ("... do not treat\n# this file as ..."). The steps now
normalise the header to prose (strip `# `, collapse whitespace) before
matching. Matching raw text would have made these assertions depend on where
the author happened to wrap, which is not what any of them is about.

## No property tests, and why that is not a gap

Three declared invariants. Invariants 1 and 3 quantify over this parcel's own
conduct (process, not a pure function) and are encoded as executable
assertions over repository state — `git status` of the reviewed files, and
the Background's changed-path assertion. Invariant 2 is likewise a fact about
the ledger file, asserted in scenario 05. There is no pure module here whose
input space a generator could explore: the subject is one config file at one
commit. Recording the reason rather than leaving a declared invariant
silently unencoded.

## Out of scope, untouched

- The hotfix, `extra_cli_targets_qwen_cloud`, the billing_guard branch,
  `launch_role`'s provider elif chain — all forbidden by `constraints`, and
  none changed in this commit.
- Re-staffing toward the all-qwen cast, or diversifying other seats — the
  header is explicit that diversification is later, deliberate work.
- The `1000000` default and `CLAUDE_CODE_MAX_CONTEXT_TOKENS` override
  semantics.
- `swarmforge/scripts/open_swarm_spy_router.sh` and
  `spy_router_pane_label.sh` — untracked, not created by this parcel.
