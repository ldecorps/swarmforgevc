# BL-1296 — cleaner pass (2026-09-03)

## Scope note — entangled commit, resolved without harm to this review

The merged commit (`500e6826c4`) accidentally bundled in a rework of
`extension/test/bl1337ProfileCastInvariants.property.test.js` — the D1
fix (flaky invariant-1 reach floor) for BL-1337, which architect had
bounced and reverted out of this branch. Correct-looking work, but the
wrong ticket's commit (BL-1192's "commit each ticket separately" concern).
Kept BL-1337's file deleted in the merge (matching the legitimate bounce
state); BL-1337's own rework should arrive as its own commit against the
still-pending bounce. Does not affect BL-1296's own correctness — none of
BL-1296's files touch `bob_starting_cast_lib.bb` or its tests. Recorded
here for the coder/architect's awareness, not bounced on its own (no
functional defect in what BL-1296 itself changed).

## Scope (BL-1296 itself)

Diff otherwise touches only files it declares: the new `bubbleSeat.ts`
module, 38 lines in `telegramCursorBridgeLive.ts` (a seam interface plus
one gated block ahead of cursor's own decision), and the ticket's own
step/property/unit test files.

## Invariants checked against the diff

- **Invariant 1** (never diverges, always a mirror): structural at the
  type level — `BubbleSeatTurn`'s `answer` variant has exactly one shape,
  `via: 'front-desk-mirror'`; there is no code path that could produce an
  answer from a Bubble-only context without changing the type itself.
- **Invariant 2** (a seat answers only its own topic): the topic-match
  check is the FIRST clause in `decideBubbleSeatTurn`, before anything
  else is consulted; wired in `telegramCursorBridgeLive.ts` ahead of
  cursor's own decision, so cursor is never asked about the Bubble topic
  and vice versa. `cursorBusy` is present in the input type but
  deliberately unread — commented explicitly as "a future edit that
  starts consulting it reintroduces the defect."
- **Invariant 3** (one getUpdates owner): the Bubble seat's turn runs
  inside the SAME poll loop (`processInboundUpdates`), the identical
  posture BL-1235's qwen seat already uses — no second poller opened.
- **required_wiring**: `bl1296BubbleSeatSteps` registered in
  `specs/pipeline/steps/index.js`, confirmed.
- Watchdog (`decideSeatWatch`) treats "liveness unknown" as needing
  attention, never as "assumed alive" — matches the qa_e2e's watchdog
  requirement without conflating unknown with running.

## Verification (run, not assumed)

- `npx vitest run test/bubbleSeat.test.js` — 11/11 pass.
- `run_acceptance.sh specs/features/BL-1296-…feature` — 6/6 pass (the
  ticket's five acceptance criteria plus supervision, all present).
- `npx vitest run --config vitest.properties.config.mjs
  bl1296BubbleSeatInvariants` — 15 consecutive green runs, no flake.
- `npx vitest run test/telegramCursorBridgeLive.test.js` (the file this
  ticket edits) — 121/121 pass, no regression.

## Mutation-site advisory (BL-485)

`bubbleSeat.ts` (new production file) is 75 sites, within threshold.
`telegramCursorBridgeLive.ts` shows 1572 sites but this ticket's diff to
it is 38 lines; the count reflects the whole pre-existing file, not
something this parcel introduced — not actionable here. Step/property/
unit test files at 81–174 sites, consistent with session range.

## Verdict

No defect in BL-1296 itself. Forwarding.
