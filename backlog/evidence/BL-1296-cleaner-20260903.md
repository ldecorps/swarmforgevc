# BL-1296 — cleaner pass (2026-09-03)

## Scope: a tip-pure rebuild, merged over an older in-branch state

This commit (`68da935d5d`, "tip-pure rebuild") is the coder's full BL-1296
diff rebuilt directly off `origin/main` after the send-time task-scope gate
refused an earlier tip whose ancestry entangled BL-1309's ticket file (see
the commit body). My branch already carried an OLDER, partial BL-1296 state
from the architect's earlier non-forwarding merges (topic-wiring half only,
still D1-bounced on the turn function), so the merge conflicted in three
places. Resolved by taking the incoming side throughout, verified by diff
before resolving, not assumed:

- `extension/src/tools/telegramCursorBridgeLive.ts` (3 hunks): HEAD's
  `runBubbleSeatTurnFn` seam had no default, so the dispatch guard
  (`deps.runBubbleSeatTurnFn &&`) was unconditionally false in production
  — the exact defect the architect's D1 bounce named. Incoming's
  `const runTurn = deps.runBubbleSeatTurnFn ?? runBubbleSeatTurn;` matches
  the sibling BL-1235 seat's own shape and is what actually fixes it.
- `extension/test/bl1296BubbleSeatLive.test.js` and
  `bl1296BubbleSeatInvariants.property.test.js` (add/add): diffed both
  files before resolving — incoming is a pure superset (adds the D1
  half-two coverage and the byte-for-byte relay property), nothing on HEAD's
  side was unique or lost.
- Also confirmed and disclosed in the merge commit: this merge drops
  `specs/features/BL-1309-...feature.draft`, which is not data loss — that
  path was already renamed to its non-draft `.feature` form earlier on this
  branch (the BL-1309 architect-review merge); the incoming coder branch
  still carried the pre-rename path. Confirmed the non-draft file is present
  and intact.

## Invariants checked against the diff

- **Invariant 1** (never diverges, structural): `runBubbleSeatTurn` in the
  new `bubbleSeatLive.ts` drives `processLetsTalkTurn` (the same route the
  Let's Talk surface uses) and posts its `replyText` unedited — no code path
  composes a reply. The only text the module authors is a refusal, and only
  when the front desk could not answer.
- **Invariant 2** (own topic only): the topic gate (`decideBubbleSeatTurn`)
  runs FIRST, before the front desk is asked anything, so a foreign-topic
  message never even causes a turn.
- **Invariant 3** (one getUpdates owner): the turn runs inside the bridge's
  existing poll, same posture as BL-1235's seat; no second poller opened.
- **D1 (the architect's bounce)**, both halves now present and tested: the
  seat reads its live topic (half one, from `74266e2f36`) and the turn
  function has a real default rather than requiring injection (half two,
  this commit) — `bl1296BubbleSeatLive.test.js`'s new test asserts the
  default AND that the old injection-required guard shape is gone.

## Verification (run, not assumed)

- `npx tsc --noEmit` — clean.
- `npm run compile` — clean.
- `npx vitest run bl1296BubbleSeatTurn bl1296BubbleSeatLive bubbleSeat
  telegramCursorBridgeLive bridgeServer` — 268/268 (recompiled `out/` first;
  the coder's test files import from `../out/tools/...`).
- `npx vitest run --config vitest.properties.config.mjs
  bl1296BubbleSeatInvariants` — 5/5.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1296-bubble-answers-from-its-own-seat.feature` — 6/6.
- Mutation-site count (BL-485):
  `bubbleSeatLive.ts` (new) — 42 sites, within.
  `bubbleSeat.ts` — 75 sites, within, unchanged.
  `telegramCursorBridgeLive.ts` — 1571 sites, over, but this parcel's diff
  to it is ~59 lines against a large pre-existing file — not something this
  parcel introduced, not actionable here (same conclusion as the earlier
  BL-1296 pass).
- `specs/pipeline/steps/index.js` — `bl1296BubbleSeatSteps` registered
  (required_wiring satisfied).

## Structure

`bubbleSeat.ts` (decide, pure) / `bubbleSeatLive.ts` (perform, I/O) keeps
policy independent of the live agent session and Telegram posting, matching
the architecture rules — high-level decision logic stays testable without a
live agent, and the seam (`frontDeskTurnFn`) is injected exactly where a
real dependency would otherwise be needed. `askFrontDesk` converts a thrown
edge into a named refusal, consistent with "never fails silently."

## Still QA's

The ticket's `qa_e2e` (a live phone demonstration with the Cursor seat mid-
turn) is explicitly not claimed here or by the coder — correct, that cannot
be stood in for by a fake.

## Verdict

No defect found. Forwarding.
