# BL-764 — QA pass, 2026-08-01

## Commit reviewed

`62c22d069` (merge of documenter `98376e7d6a` into QA).

Ancestry confirmed: architect's rework-forward merge (`cc84667218`), the
coder's D1-D3 rework commit (`7c0cd3a17`), and the pre-rework cleaner tip
(`b44f62ca5`) are all ancestors of the reviewed commit — this ticket's own
lineage, not a sibling's.

## Approval-status flag — raised on stale data, corrected after merging `main`

Before merging `main` into this QA branch, my worktree's only copy of the
ticket was `backlog/paused/BL-764-front-desk-eats-host-bridge-updates.yaml`
carrying `human_approval: pending`, `status: todo`, un-promoted. That reads
alarming on its own — full pipeline work landing on a ticket whose own
`approval_context` names an explicit open sign-off question ("adopt, not
rebuild-from-scratch", bundling the liveness cue with the queue). Raised to
the operator before spending the verification pass; operator directive
(interactive, 2026-08-01): proceed with verification, flag rather than bounce.

**After merging `main`** (bringing in unpushed coordinator/specifier commits,
see below), the true current state is `backlog/active/...yaml`:
`human_approval: approved`, `assigned_to: coder` — set by the coordinator's
own promotion commit `a6a202644` ("Promote BL-764: paused → active for
coder"), which was sitting on local `main` unpushed and simply hadn't reached
this worktree yet. The approval gap was a worktree-staleness artifact, not a
live process breach: the human_approval field WAS flipped, by the coordinator,
before dispatch — this QA branch just hadn't merged that commit until now.
No corrective action needed; noting the correction for the record since the
alarm was raised (and answered) before the full picture was visible.

## Verification performed

- `npm run compile` (extension/): clean.
- Targeted run of all 11 BL-764-touched test files (`telegramCursorBridgeCore`
  [+property], `telegramCursorBridgeLive`, `telegramCursorBridgeCli`,
  `telegramCursorBridgeLiveness`, `telegramCursorBridgeRedeploy`,
  `telegramFrontDeskBotCore` [+property], `telegramFrontDeskBotCli`,
  `cursorBridgeInboundQueue` [+property]): 870/870 pass.
- `npm run test:properties`: 34/34 files, 101/101 tests green (the
  previously-flagged `bounceNaturalKey.property.test.js` flake did not
  reproduce this run).
- Full unit suite (`npx vitest run --exclude '**/*.property.test.js'`):
  394 files / 6999 of 7001 tests green. Both failures pre-existing and
  untouched by this diff (confirmed via `git log` on each file):
  - `test/startBridgeHeadlessCli.test.js` — port-binding timing flake under
    full-suite parallel load; re-run in isolation: 13/13 green.
  - `test/tmpDirMigrationGuard.test.js` — raw `mkdtemp` bypass at
    `test/pricingTable.test.js:92`, introduced by BL-627 (`9b3e1f74a`,
    2026-07-30), same known gap the prior QA pass (`BL-716-qa-pass-20260801.md`)
    already recorded. Not re-filed here.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-764-front-desk-eats-host-bridge-updates.feature`: 7/7
  scenarios green (own independent run).
- Manual e2e per ticket notes: `node out/tools/telegram-cursor-bridge.js
  --help` with no Telegram env set exits 0 immediately, prints usage, opens
  no poll (confirms the `--help` fix independent of the CLI test).
- No orphaned `node --test`/`stryker` processes before or after (a live
  `telegram-cursor-bridge.js` process was observed post-run, but it targets
  `/home/carillon/swarmforgevc` — the main checkout, not this worktree — and
  predates this QA pass; not created by my test run, left untouched).

## required_wiring confirmed (all 4, read at source)

1. `telegramFrontDeskBotCore.ts:2213-2228` — `forwardCursorBridgeUpdate`
   invoked from the update-forwarding path (not left dark).
2. `telegramCursorBridgeLive.ts:1556` — `drainCursorBridgeInboundUpdates`
   called from the live poll loop.
3. `telegramCursorBridgeLive.ts:1734` — `shouldUseCursorBridgeInboundQueue`
   drives `useInboundQueue` in `runCursorBridgeApp`.
4. `swarmforge/scripts/start_cursor_bridge.sh:60` —
   `CURSOR_BRIDGE_INBOUND_QUEUE` exported with shared-token default.

## Prior bounce (architect, D1-D3) — confirmed resolved

`BL-764-front-desk-eats-host-bridge-updates-bounce-20260801.md` flagged three
invariant-unencoded gaps (missing property tests for invariants #1/#2, no
callback_query coverage for the topic-exclusion fix). The rework is present
in the reviewed commit's lineage; re-read against source per the architect's
own re-verification in `...architect-pass-20260801.md`, and the added test
files pass in my own run above. Not re-verified line-by-line a second time —
architect's non-vacuity checks (deliberately breaking each fix and confirming
the new test catches it) are sound and I have no reason to doubt them.

## Main merge

Merged local `main` (`a70db8744`, 20 unpushed commits ahead of `origin/main`
at merge time — coordinator bookkeeping + specifier ticket-drafting work,
including `BL-771` which specs the exact `pricingTable.test.js:92` mkdtemp
gap noted above) into this branch, clean, no conflicts. Diff touched only
`backlog/`, `docs/`, `specs/features/` — no `extension/` or `swarmforge/scripts/`
files — so the test/acceptance results above stand unchanged after the merge.

## Verdict

PASS. No new defects. Approval-status apparent gap investigated and resolved
(worktree staleness, not a real gate breach — see above). Landing on `main`.
