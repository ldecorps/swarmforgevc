# QA pass — BL-1291 — 2026-09-05

Commit under review: `9a2adb5af9` (merge of architect's `1bf9a204b0` into
`swarmforge-QA`), carrying the full chain (`f4294ee16a` BL-1291 coder,
confirmed ancestor via `git merge-base --is-ancestor`). Routed
architect→QA per `stage_skip_reasons` (cleaner/hardener/documenter all
skipped with stated, checked-out reasons: test-only diff with no shared
duplication, no production module change, no living doc affected).

## Verification

- `npx vitest run test/liveRepoDerivationGuard.test.js`: 19/19 pass (was
  the standing red naming 2 violators).
- `npx vitest run test/bl1243PaneActivitySignal.test.js
  test/deprecateRetiredReferents.test.js test/docsStructureRealTree.test.js`:
  22/22 pass — the two edited files' own suites, plus the third file the
  ticket named at mint (confirmed by the coder, and independently by me,
  to already be outside the guard's current pattern match — an unrelated
  2026-09-02 `git rev-parse --show-toplevel` rewrite moved it off the
  guard's `LIVE_ROOT_BINDING_RE`, correctly left untouched here).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1291-a-live-repo-read-is-pinned-or-justified.feature`
  — 5/5 scenarios pass.
- Full unit lane (`npm test`): `liveRepoDerivationGuard.test.js` confirmed
  **absent** from the failing set (15 files / now, down from 16 before
  this ticket — this guard is the one that dropped out). The remaining 15
  failures are the same pre-existing, already-ticketed standing-red
  cluster documented in my own BL-1411/BL-1405 and BL-1414 evidence today
  (`pilotAcceptanceGate.ts` deps-contract family — BL-1229/BL-1221;
  `pricingTable.test.js` — BL-1212; `socketFixtureShortRootGuard.test.js`
  — BL-1290; `telegramClient.test.js`/`telegramCursorOperatorExec.test.js`
  — BL-1263; `tempDirTrapGuard.test.js` — BL-1289;
  `operatorRuntimeBbFixtureClosure.test.js` — BL-1265;
  `constitutionDocCitations.test.js` — BL-1212/BL-1221 family); none
  touches this ticket's two changed test files or the guard itself.
- `required_wiring` confirmed: `specs/pipeline/steps/bl1291LiveRepoReadPinnedSteps.js`
  exports `registerSteps` and every scenario in the feature resolved to a
  handler (a missing one would THROW per BL-1371, not silently pass).
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — merging up and landing.
