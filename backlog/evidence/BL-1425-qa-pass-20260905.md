# QA pass — BL-1425 — 2026-09-05

Commit under review: `b46b475c9b` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`2cd4072055` BL-1425
coder, confirmed ancestor via `git merge-base --is-ancestor`).

Human-directed reversal of one specific row of BL-1083's own approved
gate ordering. Reviewed with attention to the invariants that pin exactly
what may and may not cross.

## Verification

- Read `promotion_gates_lib.bb`'s `evaluate` directly: `queue-jump?`
  short-circuits ONLY `depth-refusal`, evaluated last of all the refusal
  checks — hold, epic-type, blocked-status, human-approval,
  acceptance-executable, slice-size-envelope, and depends-on all remain
  unconditional `or`-chain members ahead of it, in the same order as
  before. Confirms invariant 1 exactly.
- Confirmed `:crossed {:gate ... :reason ...}` is attached whenever
  `queue-jump?` actually bypassed a would-be refusal — never silent
  (invariant 3).
- `grep -n "queue-jump\|queue_jump"` on `promote_and_route_next.sh`,
  `route_backlog_to_coder.sh`, `handoffd.bb`, `chase_sweep_lib.bb`: empty
  — confirms scenario 03's structural pin directly (no coordinator/daemon
  script ever declares the mode).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1425-a-queue-jump-places-the-ticket-past-the-depth-cap.feature`
  — 7/7 scenarios pass (initial run showed 2 failures from my own stale
  `out/` build, predating this commit's new/changed TS files — same
  benign cause hit repeatedly this session; `npm run compile` then 7/7).
- Regression: `run_acceptance.sh
  specs/features/BL-1083-every-promotion-path-goes-through-the-gate.feature`
  — 4/4 pass (the falsified depth-cap Examples row correctly removed per
  BL-1006, the narrative sentence about depends_on/hold remaining true
  kept). `npx vitest run --config vitest.properties.config.mjs
  test/bl1083PromotionGateInvariants.property.test.js` — 6/6 pass (the
  matching depth-cap case removed there too).
- `npx vitest run test/backlogWriter.test.js
  test/telegramFrontDeskBotCore.test.js test/bridgeServer.test.js`:
  604/604 pass. `telegramFrontDeskBotCore.property.test.js`: 9/9 pass.
- `expediteSafety.ts`'s new `crossedCapToastText` matches the ticket's own
  specified wording exactly ("Q jumped past the active_backlog_max_depth
  cap: ...").
- `required_wiring`: confirmed `--queue-jump` flag parsed in
  `promotion_gates_cli.bb` (line 149) and threaded to `:queue-jump?`;
  `specs/pipeline/steps/bl1425QueueJumpPastCapSteps.js` exports
  `registerSteps`.
- Docs: `docs/diagrams/swarm-flow.mmd` gained the queue-jump edge into
  ACTIVE, naming which gate it crosses and which stay hard;
  `Specification.MD`, the BL-900 explanation doc, and the BL-538 how-to
  all updated.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
