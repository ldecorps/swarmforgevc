# BL-1404 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, minimal, and clean.

## What was checked

- Reviewed the fix: a single call-site change in `babysitter_check.bb`
  (`decide-escalations` now reads `nudgeable`, the post-`partition-findings`
  set, instead of raw `findings`) — exactly what the ticket's own
  description directs, with a clear comment explaining why the
  unusable-store-escalates-everything bound holds automatically (nudgeable
  already equals findings in that branch).
- `grep -n decide-escalations swarmforge/scripts/*.bb` confirms this is the
  only call site — no sibling call left unfixed.
- No duplication, no architecture concern: a one-line change with no new
  abstraction needed.
- Babysitter's own findings/waive machinery (`partition-findings`,
  `decide-nudges`) is untouched, matching the ticket's explicit
  out-of-scope note (no change to per-key waive semantics).
- Babysitter/babysitterd has no mutation/CRAP/DRY tooling wired (BL-472
  deferred) — this pass is gated by the unit/property/acceptance suites
  only, per constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bash swarmforge/scripts/test/test_babysitter_check.sh` (regression + 4 new) | 20/20 |
| `bb swarmforge/scripts/test/bl1404_waive_silences_escalation_property_runner.bb` | ALL PROPERTIES HOLD, 500/500 each of P1/P2/P3 |
| `node specs/pipeline/cli.js specs/features/BL-1404-...feature` | 4/4 |
| `node specs/pipeline/cli.js specs/features/BL-1344-...feature` (regression) | 7/7, unaffected |
| `bl1404WaiveSilencesEscalationSteps.js::registerSteps` exists | yes (required_wiring) |
| `grep -n decide-escalations swarmforge/scripts/*.bb` | one call site, fixed |

No fix required. Forwarding unchanged to architect.
