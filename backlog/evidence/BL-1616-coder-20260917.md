# BL-1616 coder pass

## Fix

`handoff_lib.bb`'s `worked-task-names-in` (the one chokepoint both
`sibling-worked-task-names` - the claim path - and
`stage-seat-worked-task-sets` - the stall sweeps - read) now attributes a
completed `note` through the SAME shared reader BL-1185/BL-1608 use
(`supersede-lib/task-name-from-content`, loaded as a leaf dependency
beside `ambulance_lib.bb`/`shell_quote_lib.bb`), unless it carries the
`no_work_reason` header `done_with_current.sh --no-work` stamps - a
declined ticket contributes nothing (invariant 2). `git_handoff`
attribution via its `task` header is unchanged.

No cycle: `supersede_lib.bb` load-files nothing itself, so `handoff_lib.bb`
loading it directly is a plain leaf dependency, same posture as
`ambulance_lib.bb`.

## Live probe (qa_e2e_procedure step 3)

```
$ bb -e '(load-file "swarmforge/scripts/handoff_lib.bb")
         (let [s (handoff-lib/worked-task-names-in ".swarmforge/handoffs/inbox/completed")]
           (println (contains? s "BL-1610"))
           (println (some #(clojure.string/starts-with? % "BL-1610") s)))'
true
true
```

Run against THIS worktree's own real `coder` seat completed/ mailbox -
BL-1610 (built from Work notes 008954/008992, per the ticket's own
description) is now named.

## Acceptance

`bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1616-*.feature`:
5 of 5 scenarios pass, driving the real `ready_for_next_task.bb` over a
fixture roster with a completed Work note (never a git_handoff) as the
prior worker's durable record - the shape BL-1004's own fixture did not
cover. New handler `bl1616WorkNoteCountsAsWorkedSteps.js` mirrors
`bl1004ReworkClaimSteps.js`'s own fixture shape (a fresh file, per the
ticket's own `required_wiring` - `bl1004ReworkClaimSteps.js` itself is
outside this ticket's constraints and untouched).

Sibling features re-verified green, unchanged in scope: BL-1004 (5/5),
BL-1185 (4/4), BL-1608 (4/4), BL-983 (5/5).

## Runner (qa_e2e_procedure step 2)

`swarmforge/scripts/test/handoff_lib_test_runner.bb` gains six unit cases
directly exercising `worked-task-names-in` (no fixture roster or subprocess
needed - the function takes an explicit `dir` argument, no `current-role`
env dependency): a git_handoff's task-header attribution (pre-existing
behaviour, re-asserted), a completed Work note's message attribution, a
`--no-work` completion contributing nothing, a note naming no ticket
contributing nothing, attribution inside a `batch_*` subdirectory, and the
union of both shapes in one directory. `bb
swarmforge/scripts/test/handoff_lib_test_runner.bb`: ALL TESTS PASSED
(already a `standing` row in `suite-manifest.tsv` - no manifest change
needed).

## Guardrails

- `check_bb_scripts_load.sh --all`: 311 scripts, all clean.
- `git status --short`: `handoff_lib.bb`, its test runner, the feature
  (pre-authored by the specifier), and the new handler only - matches the
  ticket's constraints exactly. `rework-claim-decision`, `deferral-hold?`'s
  polarity, the claim loop, and the Work-note message format are
  untouched.

## Deferred (documenter's own pass, noted here per the ticket's `notes:`)

The ticket's own notes flag coder@2's orphaned `7b78e9d58a` commit (a
duplicate BL-1610 rebuild, 2026-09-17) for revert-hold remediation on that
branch before its next merge-up - out of this ticket's own diff (a
seat-mailbox/branch-state remedy, not a `worked-task-names-in` change) and
recorded here so QA or the coordinator can route it once BL-1615 lands and
a note to coder@2 stops being a dead letter.
