# BL-1524 — hardender review pass, 2026-09-11

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Two gaps found and fixed in this pass (own commit `ea6f70f5c0`, before
forward — not a bounce, so the sweep above records clean):

1. `bl1524ChokepointLoadClosureStraysSteps.js`: `setup()` creates
   `st.dir`/`st.pathDir` under `os.tmpdir()`; cleanup lived only in the
   terminal step's `finally`. A throw in the intervening assertion step
   ("the call returns within 5 seconds...") skipped it and leaked the
   fixture dirs — the documented cross-step temp-dir hazard. Cleanup now
   also fires on that step's own throw (`cleanupFixtureDirs`, catch +
   rethrow).
2. `handoff_lib.bb`'s `run-respawn-bootstrap!` → `spawn-detached!` wiring
   had zero runtime coverage — the existing
   `respawn_bootstrap_test_runner.bb` only greps source TEXT. Added two
   `with-redefs` tests to `handoff_lib_test_runner.bb`: the built argv is
   routed through `spawn-detached!`, and a nil argv (embedded/no-op
   provider) never calls it. Confirmed both by hand-reverting the call
   site (with its require restored) and watching the new test fail, then
   restoring.

Gates run: `daemon_cycle_guard_lib_test_runner.bb` (own assertion green;
other two FAIL lines are BL-1525/BL-1526 scope, unchanged, per the
coder's evidence), `process_table_lib_test_runner.bb`,
`outage_failover_test_runner.bb`, `model_factory_test_runner.bb`,
`handoff_lib_test_runner.bb` (all green). Acceptance
`specs/features/BL-1524-...feature`: 8/8. All 18
`extension/test/*Guard*.test.js` files (183 tests) green — this parcel
touches `specs/pipeline/steps/`. No orphaned temp dirs or processes after
the runs (`pgrep -fl 'node --test|stryker'` clean, no `/tmp/bl1524-*`
leftovers).

DEGRADED GATE: Babashka + Gherkin + one JS step-handler file, no
mutation/CRAP/DRY wired for the `.bb` layer (engineering.prompt Startup
Tools). CRAP/jscpd scope to `extension/src/*.ts` only; no TypeScript
touched by this parcel.
