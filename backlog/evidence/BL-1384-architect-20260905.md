# BL-1384 — architect pass, 2026-09-05

Clean sweep. No changes needed; forwarding to hardener.

## Checks run

| check | result |
|---|---|
| `dependency-gate.js` on the 2 changed TS files | PASSED: no forbidden edges |
| `co-change-report.js` on the 2 changed TS files | no pair at or above threshold (all counts = 1) |
| `test/telegramFrontDeskBotCore.test.js` | 464/464 |
| `test/telegramCursorBridgeCore.test.js` | 131/131 |
| `test/telegramCursorBridgeLive.test.js` | 130/130 |
| property `bl1384LocalSeatTopicForwardedInvariants.property.test.js` | 2/2 |
| acceptance `BL-1384-...feature` | 4/4 |
| acceptance `BL-1235-...feature` (regression, bridge side unchanged) | 5/5 unchanged |
| acceptance `BL-1296-...feature` (regression, Bubble seat) | 6/6 unchanged |
| `required_wiring` anchor `readQwenLocalTopicId` | resolves at 3 real call sites: import, `readFrontDeskTopicMap`'s scrub, `buildPollAdapters`'s adapter |
| `required_wiring` anchor `registerSteps` | resolves, handler file registers |

## Architecture review

- Minimal, mirror-of-existing-pattern wiring: `qwenLocalSeatTopicId` is
  added to `PollAdapters` and threaded through `hasCursorBridgeTopicAdapter`
  and `attemptCursorBridgeTopicExclusion` in exactly the same shape as the
  pre-existing `cursorBridgeTopicId`/`bubbleTopicId` fields —
  `decideCursorBridgeExclusion` itself needed no change (already took an
  arbitrary owned-topic array), confirming the ticket's own claim.
- The ticket's own instruction to VERIFY rather than assume the bridge side
  needs no change was actually honored: before this parcel,
  `telegramCursorBridgeLive.test.js` had zero tests exercising
  `runCursorBridgePollOnce` with `qwenLocalTopicId`/`runLocalSeatTurnFn` set.
  Read both new tests (`telegramCursorBridgeLive.test.js:3449` and `:3504`) —
  the second specifically drains a **forwarded** update via
  `appendCursorBridgeInboundUpdate` (the same queue the front-desk half of
  this ticket feeds) and confirms it reaches the local seat identically to a
  live-polled one. This is the real end-to-end seam proof the ticket asked
  for, not an assumption.
- Topic-map scrub extended consistently
  (`frontDeskTopicMapWithoutCursorBridge(raw, cursorTopicId, [bubbleTopicId,
  qwenLocalSeatTopicId])`) — matches the ticket's "keep the scrub consistent"
  direction.
- Scope: diff touches exactly the two files and their tests/fixtures the
  ticket names; BL-1383 (provider-chat seats) and the qwen-local seat's own
  bridge-side logic are both confirmed untouched (regression suites green).
- Extension-host/webview boundary, secrets, and integrate-not-fork rules:
  not implicated — pure Telegram-poller wiring inside the extension host,
  no webview, no secrets touched.

## Invariant Review (BL-654)

The one declared invariant ("a topic the bridge process owns... is
forwarded whole... and never opens a support subject...") has a
coder-authored, non-vacuous property test, independently confirmed to fail
on two separate breaks (disabling the adapter check; dropping the topic id
from the array passed to `decideCursorBridgeExclusion`) and to hold once
restored — re-ran here, 2/2. Reachability floor and the companion
"unowned topic still opens its subject" property both present. No
violation found on code review: the wiring gates each site correctly.

## No violations found

No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener with the same task name and the received
commit.

By architect.
