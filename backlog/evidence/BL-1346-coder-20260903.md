# BL-1346 — coder pass (2026-09-03)

BL-848 stamp-off review of landed hotfix `195de28861`. Review-only: the
harness drives what landed and changes none of it. `backlog/hotfix-ledger.yaml`
is untouched — the row for `195de28861` still carries `human_decision: null`
and `decided_at: null`, and no green run here writes a decision.

## What the parcel adds

- `specs/pipeline/steps/lib/bl1346RcRepairStampFixture.js` — a real
  `swarm_ensure.bb` run against a throwaway root shaped like the hotfix's own
  RC-7b fixture (real roles.tsv with `specifier` first, real launch scripts, a
  fake tmux that RECORDS `respawn-pane` instead of performing it), plus a
  direct caller for the shared BL-1020 decision in `mono_router_lib.bb`.
- `specs/pipeline/steps/bl1346SwarmStampRcLaunchRoleSteps.js` — all five
  scenarios, registered in `specs/pipeline/steps/index.js`.
- `extension/test/bl1346RcRepairStampInvariants.property.test.js` — the three
  declared invariants (BL-654).

## qa_e2e_procedure walked

1. **Anchors + no local rule.** `rc-launch-role` resolves through
   `mono-router-lib/resolve-resident-role`, is told which pack it is on
   (`:rotation-router? (rotation-router-mode?)`), and reads the marker exactly
   ONCE — as that decision's input, never as a rule of its own. Asserted in
   scenario 04, which fails if a second marker read reappears.
2. **The hotfix's own suite.** `bash swarmforge/scripts/test/test_swarm_ensure.sh`
   — **ALL PASS** (includes RC-7 router-pack unchanged and RC-7b).
3. **The negative case that matters most.** Scenario 02 and invariant 1: with
   the stale `coordinator` marker present, a genuinely degraded pane is still
   respawned — with ITS OWN role's launch script — and no other pane is
   disturbed. Invariant 1 covers every pane in the pack by construction,
   including the first roles.tsv row the defect could reach.
4. **Router pack.** Scenario 03 and invariant 2: on `rotation router` the same
   marker still names the resident's role. The authority removed is exactly
   the non-router case.
5. **Post-repair report (recorded, not fixed here).** The hotfix did not touch
   ensure's report line; on `195de28861` a pane respawned with another role's
   script would still have read `rc:<role>: HEALTHY`. That gap is BL-1345's,
   and BL-1345's fix (`remote_control_health/assigned-role-mismatch`, compared
   against the role the PACK assigns) is in flight on this same branch — so
   the two must not be read as one change: this stamp certifies only what
   `195de28861` itself did.
6. **Certification left to the human.** Scenario 05 and invariant 3.

## Non-vacuity

Reverting the landed `rc-launch-role` line (back to honouring the marker
unconditionally) reproduces the incident exactly — `coder.sh` respawned into
`swarmforge-specifier` — and invariants 1 and 2 both go red on it. Restored
immediately; the reviewed sources are byte-identical to what landed, which
invariant 3 measures rather than asserts in prose.

## Note on fixture shape (side finding 1, not taken on)

The rc cmdline fake here is invoked as `<script> "$1" "$2"`, so it actually
receives the session. The operator's filing records that the older RC
fixtures pass a bare script path, whose fake therefore sees no arguments and
answers "no process" for every pane. That is not this parcel's to change
(ticket constraints) and it did not widen while this review was in the file —
it is unchanged, and still worth its own chore.
