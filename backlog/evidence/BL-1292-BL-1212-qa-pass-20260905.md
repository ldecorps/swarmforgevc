# QA pass — BL-1292 and BL-1212 (batch commit) — 2026-09-05

Commit under review: `3eb8e56cc5` (architect's own NONE pass, fast-forwarded
into `swarmforge-QA`), carrying both tickets' full chains
(`be778d22cd` BL-1292 coder, `809ae0df41` BL-1212 coder — both confirmed
ancestors via `git merge-base --is-ancestor`).

## BL-1292 — fixture liveness is decided by identity

- `npx vitest run --config vitest.properties.config.mjs
  test/bl857TunnelOwnershipInvariants.property.test.js`: 4/4 pass,
  including the new "BL-1292 invariant: liveness is true only while THAT
  fixture is running — a zombie and a reused pid both answer false" test,
  driven by a REAL Linux zombie process (confirmed via `/proc/<pid>/status`
  `State: Z`), not a simulated one.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1292-fixture-liveness-is-decided-by-identity.feature`
  — 6/6 scenarios pass, including "an unregistered target is reaped, and
  the test can tell" — QA's own original counterexample
  (`[prefixed, false]`, seed -436553632) replayed and passing under the
  new identity-checked helper.
- Read `extension/test/helpers/fixtureLiveness.js` directly: `isAlive`
  confirms both `process.kill(pid, 0)` AND a `ps -o args=` identity check
  against the fixture's own tunnel name — a zombie (`<defunct>`/empty args)
  and a reused pid (different command) both correctly fail.
- No production module touched (per `stage_skip_reasons`: hardener skipped
  correctly, no TS production diff).
- `required_wiring`: `specs/pipeline/steps/bl1292FixtureLivenessByIdentitySteps.js`
  exports `registerSteps`.
- Honest framing verified: the ticket never claims to have reproduced the
  original flake and says so in `approval_context` — this is fixing a test
  that asks the wrong question on its own terms, not a confirmed root-cause
  fix. Recorded here as read, not disputed.

## BL-1212 — real-tree docs gate records its live-read exemption

- `npx vitest run test/liveRepoDerivationGuard.test.js`: 19/19 pass (was
  red naming `docsStructureRealTree.test.js`).
- `docsStructureRealTree.test.js` confirmed carrying
  `// BL-1038-EXEMPT: the live read is the assertion...` and its own suite
  still passes (5/5).
- Sequencing confirmed: `depends_on: [BL-1209]`, and BL-1209 is already
  closed (`backlog/done/BL-1209-...yaml`) — landing this ticket now
  produces a true end state (guard fully green), not a still-red guard.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1212-real-tree-docs-gate-records-its-live-read-exemption.feature`
  — 2/2 scenarios pass.
- `required_wiring`: `specs/pipeline/steps/bl1212RealTreeDocsGateRecordsItsLiveReadExemptionSteps.js`
  exports `registerSteps`.
- Noted, mid-flight amendment: scenario 02 was retired by the specifier
  (`985b0df0b6`, "falsified by BL-1317's rev-parse root, guard blind")
  with a successor minted (BL-1435) and a step-handler replay
  (`bd9ca1968e`) — architect and documenter both re-reviewed post-amendment
  and passed NONE; not this QA pass's concern to re-litigate, verified the
  final state (2 scenarios, both green) is self-consistent.

### Observation — pre-existing register misattribution, not this parcel's defect

`backlog/standing-reds.tsv` carries `unit
extension/test/pricingTable.test.js BL-1212 2026-08-28 PRICING_TABLE
missing claude-fable-5-1` — but BL-1212's actual scope (verified above) is
entirely the live-repo-derivation guard exemption; it never touches
pricing-table content, and its own description only borrows
`pricingTable.test.js`'s phrasing as an EXAMPLE of how to word an
exemption reason, never claiming to fix it. `pricingTable.test.js` is
confirmed still failing in the full unit lane. This looks like a
pre-existing, unrelated row mislabeled with this ticket's number (same
"unrelated cause, out of scope" class as BL-1206's `hostActivityFeed.property.test.js`
finding) — not something BL-1212 was asked to or should fix. Once BL-1212
closes, BL-1428's own standing-red register will correctly re-flag this
row as unowned (closed-ticket owner) on its next scan — the safety net I
verified earlier this session doing exactly its job. No action taken here;
flagging for visibility only.

## Whole-suite gates

- Full unit lane (`npm test`): 3 files failing
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — `liveRepoDerivationGuard.test.js` and
  `bl857TunnelOwnershipInvariants` confirmed absent; no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process/zombie check: `pgrep -fl 'node --test|stryker'`
  empty; no leaked zombie processes from any test run.

## Verdict

Both tickets satisfy their acceptance criteria and required_wiring.
APPROVED — merging up and landing.
