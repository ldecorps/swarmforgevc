# BL-1667 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect blocking approval; one minor process note below.

By QA.

## Detail

- First acceptance run showed 1/7 failing (`An arm whose marker sits inside another arm's text is
  still reported untested`) because MY worktree's `extension/out/` was stale (compiled before this
  parcel's merge) — the step handler requires the compiled `out/tools/pilotAcceptanceGate.js`, which
  still had the old substring-containment predicate. Ran `npm run compile`; re-ran: 7/7 ok. Confirmed
  by timestamp (`src/tools/multiBranchParserCoverageCheck.ts` newer than the pre-compile `out/` copy)
  and by grepping the old `out/` for `includes(arm.marker)` before compiling. Not a parcel defect —
  `extension/out/` is gitignored, and QA must compile after syncing before running acceptance.
- `npx vitest run test/multiBranchParserCoverageCheck.test.js`: 11/11 pass (includes the new
  counterexample unit case).
- `npx vitest run --config vitest.properties.config.mjs test/multiBranchParserCoverageCheck.property.test.js`:
  5/5 pass.
- Direct call against the compiled tool with arms `c--a`, `a00`, `c--` and test texts `[c--a, a00]`:
  `c--` reports `false` (untested) — matches qa_e2e_procedure step 3.
- `npm run test:properties` (extension/): 431/432 files, 1263/1264 tests passed; the one failure
  (`bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`, non-vacuity timing budget) is
  BL-1630's own known, pre-existing, unrelated flake — owned by BL-1630, not an unowned red.
- `npm test` (extension/): 635/635 files, 10821/10821 tests passed (2 more than before this parcel,
  matching the new unit case plus one BL-1630-owned addition already in the branch).
- `pre_qa_gate.sh BL-1667 d4f49c38b4`: OK.
- No straggler test/mutation processes before or after.
- `qa-sibling-check.js status --ticket BL-1667`: VERIFY (no open deferral).
- `required_wiring`: the new scenario 07's step lands in the existing handler file
  (`bl755PilotMultiBranchParserNeedsPerArmTestsSteps.js`) in the same commit as the scenario — confirmed
  by the 7/7 acceptance pass above (the step ran).

## Process note (not a bounce)

The coder's own commit (`7630980c04`) removed the `backlog/standing-reds.tsv` register row for this
ticket itself, citing "per its own mint note." The register's own header states the invariant plainly:
"the land step itself retires every row whose owner is the landing ticket... so a row surviving to
main is a defect in the land, not a parcel author's forgotten cleanup" — i.e. removal is the LAND
step's job (BL-1631/BL-1604), not the parcel author's. In this instance it makes no material
difference (the land step will retire it anyway on landing, and the row was never on `origin/main`),
but it is out of process: had this ticket bounced before landing, the row's premature absence would
have let the still-real red masquerade as already-fixed in the register between the bounce and a
resubmission. Not blocking this approval; flagged for the coder's/specifier's awareness.
