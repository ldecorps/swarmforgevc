# BL-1108 / BL-1052 — hardener pass (batch)

Merged architect tip `2c3f5b07c2` (includes BL-1108 evidence `66dba22d2e`
and cleaner shared `agent_process_marker_lib` lineage). Combined hardening
pass over the union; both tickets forward separately (Article 2.6).

## Surface

Shell + Babashka (+ acceptance/property). No TypeScript product surface —
Stryker / CRAP / DRY **not wired** for this slice. Recorded as degraded
fallback (ticket `qa_e2e_procedure` / engineering.prompt Startup Tools).

## Mutation cooldown gate

| file | DECISION |
|------|----------|
| `agent_process_marker_lib.bb` | **run** |
| `babysitter_check.bb` | skip-cooldown |
| `babysitterd_sweep_lib.bb` | skip-cooldown |
| `harness_env_scrub_lib.bb` | skip-cooldown |
| `prompt_engine_lib.bb` | skip-cooldown |
| `remote_control_health_lib.bb` | skip-cooldown |
| `swarm_ensure.bb` | skip-cooldown |
| `swarmforge.sh` | skip-cooldown |

Host load ~6–8 on 20 cores (under 2× cores) throughout.

## Hand-authored mutation (`agent_process_marker_lib.bb`)

Four single-edit mutants, all KILLED by `agent_process_marker_lib_test_runner.bb`:

1. cursor needle → `claude ` — FAIL
2. local-model needle → `claude ` — FAIL
3. unknown-token fallback drops trailing space — FAIL
4. nil agent defaults to `""` instead of `claude` — FAIL

Restored byte-identical to pre-sweep.

## BL-113 Gherkin mutation (soft, all 4 positionals)

- BL-1108: **8/8 killed**, 0 survived, 0 errors (`outcome: pass`). Manifest stamped in feature.
- BL-1052: **2/2 killed**, 0 survived, 0 errors (`outcome: pass`). Manifest stamped in feature.

BL-788 pre-check: neither step file starts a bridge.

## Suites (green)

- `test_local_model_seat.sh` — ALL PASS
- `agent_process_marker_lib_test_runner.bb` — OK
- `babysitterd_sweep_lib_test_runner.bb` — ok
- Acceptance BL-1108 4/4; BL-1052 9/9
- Property lane: both ticket property files green
- Whole-tree guards (`test/*Guard*.test.js` minus `.property.`): 13 files / 125 tests green after harness fixes below

## Harness fixes in this pass (test infrastructure, hardener domain)

1. `bl1108_cursor_seat_readiness_acceptance_runner.bb` — temp root now has Runtime shutdown-hook `delete-tree` (tempDirTrapGuard).
2. `bl1108CursorSeatReadinessSteps.js` scenario-03 — switched from `os.tmpdir()` mkdtemp to `mkSocketFixtureRoot` + terminal-step cleanup (socketFixtureShortRootGuard + BL-921 sibling).

## Notes

- `write_role_launch_script` prints `terminal_backend_can_open_sessions: command not found` during local-model compose; suites still assert the launch body correctly. Not a mutation survivor; left as observation (not product behavior change).
- Cooldown-skipped production files were still exercised via unit/acceptance/property; full language-mutation deferred to a quiet post-cooldown pass per gate.

By hardender.
