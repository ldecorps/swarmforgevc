# BL-696 QA bounce — 2026-07-27

**Commit tested:** `da2baa31e` (documenter tip)
**Hardener tip:** `9811b738f` (ancestor check: PASS)

## Inventory

| Gate | Result | Notes |
|------|--------|-------|
| `qa-sibling-check.js status --ticket BL-696` | PASS | `VERIFY BL-696` |
| Compile (`npm run compile`) | PASS | |
| Full unit suite (`npm test`) | **FAIL** | D1 below |
| Property suite (`npm run test:properties`) | PASS | 4/4 BL-696 invariants + full suite green |
| Acceptance (`run_acceptance.sh BL-696 feature`) | PASS | 8/8 scenarios |
| `pre_qa_gate.sh BL-696 da2baa31e` | **FAIL** | D2 below |
| Ancestry (`9811b738f` ancestor of `da2baa31e`) | PASS | |
| Wiring grep (`createLetsTalkWriteRoutes` merged at `bridgeServer.ts:1292`) | PASS (behavior) | Mechanical gate still fails — D2 |
| Console menu `lets-talk` button (`consoleMenuUiHtml.ts`) | PASS | `data-testid="lets-talk"` → `/lets-talk` |
| Live phone e2e (`e2e_qa_procedure`) | BLOCKED | Requires operator on device; not executable in this gate |

## D1 — `unit` — raw `mkdtemp` in BL-696 test files

**Failing command:** `cd extension && npm test`

**First error excerpt:**
```
test/tmpDirMigrationGuard.test.js:104:10
expected zero raw mkdtemp call sites, found:
/home/carillon/swarmforgevc/extension/test/bl696LetsTalkInvariants.property.test.js:16
/home/carillon/swarmforgevc/extension/test/cursorBridgeAgentSession.test.js:11
```

**Failure class:** `unit`

**Expected vs observed:** BL-420 migration-complete gate expects every test to allocate temp dirs via `mkTmpDir` from `test/helpers/tmpDir.js`; observed two BL-696 test files still call `fs.mkdtempSync(path.join(os.tmpdir(), ...))` directly.

**Blamed role:** `hardender` (both files landed in architect/hardener commits; test-hygiene gate is hardener domain)

**Remediation:**
- `extension/test/bl696LetsTalkInvariants.property.test.js:16` — replace with `mkTmpDir('sfvc-bl696-inv-')`
- `extension/test/cursorBridgeAgentSession.test.js:10-13` — replace `mkRoot()` body with `mkTmpDir('sfvc-cursor-session-')`

## D2 — `wiring` — `pre_qa_gate` required_wiring anchor missing in `bridgeServer.ts`

**Failing command:** `./swarmforge/scripts/pre_qa_gate.sh BL-696 da2baa31e`

**First error excerpt:**
```
PRE_QA_GATE_FAIL wiring BL-696 extension/src/bridge/bridgeServer.ts does not contain "/lets-talk/turn" (the audio turn route must be registered on the live bridge host, not merely exported from a module. An unregistered handler is a 404 behind a green unit suite.)
```

**Failure class:** `wiring`

**Expected vs observed:** Backlog `required_wiring` pins `bridgeServer.ts::/lets-talk/turn`; the route **is** registered live via `createLetsTalkWriteRoutes` merged into the write-route dispatch at line 1292, but the cited file lacks the substring the gate searches for (unlike BL-538/BL-572 routes which keep path literals in `bridgeServer.ts`).

**Blamed role:** `coder`

**Remediation:** Add a path anchor in `bridgeServer.ts` beside the Let's Talk registration (e.g. comment `// BL-696: POST /lets-talk/turn, POST /lets-talk/new-session` above `createLetsTalkWriteRoutes`) **or** relocate route matchers into `bridgeServer.ts` following the BL-538 pattern. Do not leave the gate red while the modular split remains.

## Verdict: BOUNCE → hardender

Route to **hardender** (earliest role owning the blocking unit gate). Forward through documenter after fixes; QA will re-run the full inventory including `pre_qa_gate.sh`.

## Operator follow-up (non-blocking)

Live phone e2e per backlog `e2e_qa_procedure` steps 1–7 remains operator-owned after automated gates pass.
