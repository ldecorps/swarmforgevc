# BL-1229 documenter review pass — 2026-09-05

Reviewed the hardened commit (`23259f893b`) and the coder/cleaner/architect/
hardener trail for BL-1229 (pilot gate deps contract cannot silently
outgrow its test stubs).

Checklist:
- Step handler exists:
  `specs/pipeline/steps/bl1229PilotGateDepsContractStubsSteps.js`.
- Read `extension/test/helpers/pilotAcceptanceGateDeps.js` and
  `extension/test/pilotAcceptanceGateDepsCompleteness.test.js`: confirmed
  invariant 1 (`baseAcceptanceGateDeps()` is a hand-maintained, explicit
  object — never a Proxy/auto-defaulter that would silently manufacture a
  missing member) and invariant 2 (`extractRequiredMembers` reads the
  REAL `PilotAcceptanceGateDeps` interface's own source text from
  `pilotAcceptanceGate.ts`, never a second hand-copied list, so the
  completeness test fails exactly once, by name, on any future widening).
  Confirmed the human ruling's firm constraint held: no production
  contract or live-wiring change in the diff (`pilot-acceptance-gate.ts`
  line 197 untouched).
- Confirmed all sixteen `landPilotedTicket`-calling test files (the ten
  crashing plus the six that weren't) were migrated to build their `deps`
  from the shared `makeAcceptanceGateDeps`/`baseAcceptanceGateDeps`
  factory rather than their own hand-rolled object literal.
- Operator/reviewer-visible behavior change (a future `PilotAcceptanceGateDeps`
  widening now fails once, in one completeness test, naming the missing
  member — instead of crashing an unpredictable subset of the sixteen
  calling test files with no indication why) is documented in
  `docs/how-to/BL-727-pilot-acceptance-contract-gate.md` — the existing
  home doc for `landPilotedTicket`'s full contract and every check it
  gates — as a new "Test-side deps stubs cannot silently outgrow the
  contract (BL-1229)" section, an updated title BL-list, new "Where it
  lives" entries for the shared stub/completeness test/handler/acceptance
  files, and a new Siblings bullet cross-linking BL-1220/BL-1221.
- `docs/index.md`'s one-line summary for that how-to extended with a
  BL-1229 clause in the same running-list style the entry already uses.
- No "Last Updated" freshness field in either edited doc — no date bump
  owed.
- No registered diagram depicts test-stub/contract wiring — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
