# BL-1425 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's queue-jump bypass is correct,
minimal, and matches the human directive and every declared invariant.

## What was checked

- Read `promotion_gates_lib.bb`'s `evaluate` diff directly: the `or` chain
  is unchanged in order for every gate except `depth-refusal`, which is
  now wrapped in `(when-not queue-jump? ...)` — when `queue-jump?` is true
  this branch evaluates to `nil` and the `or` falls through to the final
  `{:ok true ...}` branch, correctly. `:crossed` is only added when
  `queue-jump?` AND `depth-exceeded?` both hold — a queue-jump on a ticket
  UNDER the cap correctly reports no crossing (never widens the cap or
  overstates what happened).
- Ran the full bb suite directly: `promotion_gates_lib_test_runner.bb`
  (ALL PASS), `promotion_gates_cli_test_runner.bb` (ALL PASS),
  `promotion_gates_lib_property_runner.bb` (500 runs each, ALL PROPERTIES
  HOLD).
- Built a real fixture root (cap 1, one active ticket, one paused
  approved ticket with no depends_on) and ran the REAL
  `promotion_gates_cli.bb gate-promotion` directly, both without and with
  `--queue-jump`: without → `REFUSE|active_backlog_max_depth|...`, exit 2;
  with → `ADVISORY|active_backlog_max_depth|queue-jump past cap:...` on
  stderr, `ALLOW|<file>` on stdout, exit 0 — reproduced the ticket's own
  contract independently, not merely trusting the coder's claimed output.
- Read `backlogWriter.ts`'s `runGateCli`/`parseGateVerdict` diff: the
  switch from `execFileSync` to `spawnSync` is necessary and correctly
  reasoned (stderr must be captured on the exit-0/ALLOW path, which
  `execFileSync`'s return value cannot carry); the crash-detection guard
  (`result.error || typeof result.status !== 'number' || ![0,1,2]...`)
  correctly fails closed on a signal-terminated process too
  (`result.status` is `null` when killed by signal, `typeof null !==
  'number'` is true). The crossed-line parser is gate-name-agnostic by
  construction (picks the non-`orthogonality` ADVISORY line), so
  `active_backlog_max_depth` is never restated as live TypeScript outside
  the chokepoint — confirmed BL-1083's own property test (still present,
  still passing) is what actually proves this, not merely a comment.
- Read `telegramFrontDeskBotCore.ts`'s diff: `recordExpediteDecisionAndClose`
  (the one routine both the Q jump tap and `/qjump` share, per BL-721)
  always requests `{queueJump: true}` and notifies the Approvals topic
  exactly once on a crossing — centralized correctly, not duplicated per
  caller. `normalizePromotionOutcome` passes `crossed` through unchanged
  (verified directly — it returns the object as-is or `{moved:false}`).
- Read `bridgeServer.ts`'s diff: the paused-pager Expedite route passes
  `{queueJump: true}` and the 200 body conditionally carries `crossed` —
  matches the Telegram toast's own signal, same invariant 3 discipline.
- Confirmed the BL-1083 retirement is minimal and correct per BL-1006
  (retire, never reword): exactly one Examples row dropped from the
  feature file, the matching `else` branch in the step handler replaced
  with an explicit `throw` (keeps the handler honest about what it
  supports, rather than silently falling through), and the matching
  entry removed from the property test's `REFUSING_GATES` array — no
  other line in any of the three files reworded.
- Confirmed `grep -c -- --queue-jump` on
  `promote_and_route_next.sh`/`route_backlog_to_coder.sh`/`handoffd.bb`/
  `chase_sweep_lib.bb` is 0 for each (invariant 1) — the coordinator's own
  path never declares the mode.
- `npx jscpd` on the new step handler against its closest sibling
  (`bl1083PromotionGateSteps.js`, the file it was modeled on): 0 clones.
- `npm run compile` clean.
- Mutation-site count on the touched TS files:
  `backlogWriter.ts` 222, `telegramFrontDeskBotCore.ts` 2036,
  `bridgeServer.ts` 1889 — all "over" the 100 advisory threshold, but all
  THREE are pre-existing, already-massive hub files (250/3961/2501 lines
  respectively BEFORE this ticket's diff, confirmed via `git show
  <parent>:<path> | wc -l`) that this ticket's diff only lightly touches
  (a handful of lines each). This ticket did not create the oversized
  condition, and splitting an established 3961-line front-desk-bot-core
  hub is a separate architectural undertaking well outside this small
  feature's scope — noted, not a blocker (`expediteSafety.ts`, the one
  genuinely new/small module, is within threshold at 28).

## Verification (all re-run in this worktree, all green, matching every claimed number)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/promotion_gates_lib_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/promotion_gates_cli_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/promotion_gates_lib_property_runner.bb` | 500 runs each, ALL PROPERTIES HOLD |
| Real CLI, fixture root at cap, without `--queue-jump` | `REFUSE\|active_backlog_max_depth\|...`, exit 2 |
| Real CLI, same fixture, with `--queue-jump` | `ADVISORY\|...` on stderr + `ALLOW\|<file>` on stdout, exit 0 |
| `npm run compile` | clean |
| `npx vitest run test/{telegramFrontDeskBotCore,backlogWriter,pausedPagerBridge,pausedPagerUiHtml}.test.js` | 532/532 pass |
| `npx vitest run --config vitest.properties.config.mjs test/{bl1083PromotionGateInvariants,bl721QjumpQueueJumpInvariants,bl1091ExpeditePromotionCommit,bl1380ExpediteNeverAnswersUnshownQuestion}.property.test.js` | 16/16 pass |
| acceptance `BL-1425-...feature` | 7/7 |
| acceptance `BL-1083-...feature` (depth-cap row retired) | 4/4 (was 5) |
| acceptance `BL-721-...feature`, `BL-490-...feature` | 4/4, 8/8, unaffected |
| `grep -c -- --queue-jump` on the four coordinator/daemon scripts | 0 for each |
| `npx jscpd` (new step handler vs its sibling) | 0 clones |

No fix required. Forwarding unchanged to architect.
