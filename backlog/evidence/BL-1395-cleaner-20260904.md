# BL-1395 — CLEANER PASS, 2026-09-04

One defect found and fixed (D1, below); everything else checked clean. Forwarding to architect.

## D1 — pre-existing fixture gap surfaced while verifying this ticket (fixed)

`extension/test/bl632CommitTimeGuardInvariants.property.test.js` was
already missing `check_handler_module_graph.sh` (BL-1385's guard, wired
into `run_commit_guards.sh` well before this ticket) from its fixture's
copied-script list. Confirmed by reverting the file to `origin/main`'s
version and re-running: the exact same "No such file or directory" /
exit-127 unexpected-failure shape reproduced with `check_handler_module_graph.sh`
alone, independent of anything BL-1395 added. Not this ticket's coder's
defect — pre-existing, just never tripped because no property case had
previously forced a `merge` action down this exact path.

Fixed alongside the coder's own (correct) addition of
`check_bb_scripts_load.sh` to the same fixture list, since it's the
identical one-line pattern in the same three places (const declaration,
`EXEC_FIXTURE_FILES`, and the template's copy loop). Re-ran the reverted-vs-
current comparison both ways to confirm: reverted+unfixed → red (same
failure), current+fixed → green. This is squarely test-fixture completeness
(cleaner scope), not a change to what any guard decides.

## What was checked

- `bb -e '(load-file "swarmforge/scripts/handoffd.bb")'` on the merged
  tree: exits 0, no daemon process started (confirms the `(when (= *file*
  (System/getProperty "babashka.file")) (-main))` guard at the foot of the
  file is intact and correctly positioned after the merge).
- Exactly two definitions touching cron-heartbeat state
  (`cron-heartbeat-state` and `write-cron-heartbeat-state!`, distinct
  functions, not a duplicate) — the merge-conflict resolution in
  `handoffd.bb` was a duplicated *comment block* from my own prior BL-1361
  merge colliding with this branch's comment restating the same rationale;
  resolved by keeping one copy, verified no code duplication remains.
- `swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh`: 16/16 PASS,
  including every declared invariant:
  - forward-reference / undefined-symbol / runtime-require refusals, each
    naming file, line, symbol
  - `handoffd.bb` specifically BOOTED (not merely analysed) against a
    fixture root, one heartbeat, bounded
  - a boot marker echoed inside babashka's own error Context block is not
    mistaken for a real boot (regression the coder's commit message flags)
  - verdict follows the TREE under test, not the checking worktree,
    including under a leaked `GIT_DIR` (BL-1385 invariant 2 carried,
    BL-1390-class hazard)
  - wired into both `run_commit_guards.sh`'s chain and
    `land_step_lib.bb`'s tree-guard list, and every other guard in the
    chain still reports (BL-1242/BL-1252 capture-each-status discipline,
    not `set -e`)
  - no daemon process starts on a plain `load-file`
- `swarmforge/scripts/test/test_bl1391_bookkeeping_conflict.sh` (also
  touched by this merge's conflict resolution in `suite-manifest.tsv`):
  14/14 PASS, unaffected.
- Property suites: `bl1395BbScriptsLoadGuard.property.test.js` (3/3),
  `bl1252CommitGuardAggregationInvariants.property.test.js` (5/5),
  `bl632CommitTimeGuardInvariants.property.test.js` (1/1, after D1's fix)
  — 9/9 total, all green. The one `[vitest-worker]: Timeout calling
  "onTaskUpdate"` warning is BL-871's known-benign allowlisted noise, not a
  test failure.
- `required_wiring` anchors, checked live (not by proof-of-grep alone —
  each one read at its call site):
  1. `run_commit_guards.sh:81` — `run_guard check_bb_scripts_load.sh`,
     sits in the same capture-each-status chain as every other guard.
  2. `land_step_lib.bb:1169` — `{:script "check_bb_scripts_load.sh" ...}`
     in the replay tree-guard list.
  3. `handoffd.bb:4905` — the `babashka.file` guard around `(-main)`,
     confirmed both present and functionally correct (load-file above).
  4. `specs/pipeline/steps/bl1395DaemonBootedBeforePublishSteps.js` —
     matches `index.js`'s directory-walk `*Steps.js` discovery suffix
     (BL-1371); no hand-maintained registry entry needed.
- `jscpd` over the new/changed non-generated files (`check_bb_scripts_load.sh`,
  `handoffd.bb`, `land_step_lib.bb`, `run_commit_guards.sh`, the new step
  handler): 0 clones.
- `swarmforge/scripts/test/suite-manifest.tsv` conflict: purely additive
  (`test_bl1395_bb_scripts_load.sh` row from the coder's side), restored
  the file's trailing-double-tab convention.
- No prior bounce history for BL-1395 (`git log --all --grep="BL-1395"`
  shows only mint → approve → promote → coder, no bounce/adjudicate-again
  entries). `main` and `origin/main` in sync
  (`git rev-list --left-right --count` → `0 0`).
- New/changed files here are hand-authored shell (`check_bb_scripts_load.sh`)
  and Babashka (`handoffd.bb`, `land_step_lib.bb`) plus a hand-authored JS
  step handler — none compiled from TypeScript, so
  `mutation-site-count.js` (which counts against `extension/out/**/*.js`)
  does not apply to any of them.

No architecture concerns: the new guard is a narrow, single-purpose script
invoked identically from both call sites, and the `handoffd.bb` change is
a one-line idiom already used elsewhere in the same file.

By cleaner.
