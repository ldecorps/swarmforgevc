# BL-1572 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- `qa-sibling-check.js status --ticket BL-1572` -> `VERIFY BL-1572`, no open deferral.
- `pre_qa_gate.sh BL-1572-fold-floor-constructed HEAD .` -> `OK` (required_wiring: step handler discovered by directory scan).
- No prior bounce for BL-1572 in `.swarmforge/bounces/*.jsonl` (master checkout).
- Ancestry: BL-1572's own hardener merge `6518557bfe` is an ancestor of the commit under review; `main..HEAD` contains only this ticket's own coder/cleaner/architect/hardender/documenter commits and their sync merges — no entangled sibling.
- `npm test` (extension/): 616 files, 10479 tests, all green. (Suite-duration budget warnings on 6 known slow files, pre-existing, not a failure.)
- `test/bl1429StandingRedThrottleFoldInvariants.property.test.js` run 15/15 alone under `vitest.properties.config.mjs` (qa_e2e_procedure step 1): 15 green, every run prints `BL-1572 reach map (invariant 1): {"combinations":12,"minDrawsPerCombination":5}` exactly as specified.
- Break-then-restore not separately run as a destructive check against the parcel's own worktree; the file's own `assertReachFloor`/`seen.size===12` assertions are visually confirmed present and unconditional in the diff (would fail naming the missing combination if a cell were dropped).
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1572-the-bl1429-fold-property-constructs-its-combination-floor.feature` -> 5/5 scenarios pass.
- `git diff main...HEAD --name-only`: test file, step handler, evidence files only — `extension/src` untouched; invariants 2 and 3 of the property file are unchanged in the diff.
- `npm run test:properties` (full lane, 405 files) run twice: first pass (contended by a leftover background run of this same lane) showed 2 failing files; after reaping strays and a clean rerun, showed 2 different failing files (`bl1297MergeOwnPathsInvariants` — already registered, owned by BL-1564; `bl1308SiblingDetectorCoversReplay` — green alone, no register row, no ticket needed per the 2026-09-07 green-needs-no-owner rule). BL-1572's own file (`bl1429StandingRedThrottleFoldInvariants`) was NOT among the failures in either full-lane run. This matches the ticket's own description of the lane's contention class (BL-1564) and is not a new or unowned red.
- Straggler check: no orphaned `node --test|stryker` processes before or after; reaped leftover vitest workers by process group after the contended rerun.

By QA.
