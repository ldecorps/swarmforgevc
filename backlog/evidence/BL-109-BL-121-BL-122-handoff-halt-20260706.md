# Forensics — handoff halt (2026-07-06 ~00:02 BST / 2026-07-05 23:02Z)

Evidence for the handoff-reliability trio (raw operator ticket "BL-118",
renumbered/split by the specifier):
- **BL-121** — delivery-level breakage DETECTION (1 of 2)
- **BL-122** — automatic agent-driven RECOVERY (2 of 2)
- **BL-109** — nudges/handoffs to an IDLE recipient dead-letter

Captured read-only while the swarm was frozen, before it was manually kicked
back into motion.

## Verdict: swarm halted while every dashboard reads green

- **All 8 agents idle** at an empty `❯` prompt (past-tense spinner footers, no
  task in flight): coordinator "Churned 1m22s", specifier "Cogitated 2m53s",
  coder "Cooked 17m39s", cleaner "Cooked 3m51s", architect "Crunched 5m7s",
  hardender "Cogitated 39s", QA bare prompt, documenter idle after `/clear`.
- **Daemon reports healthy** — `{"state":"healthy"}`, heartbeat 1s old, cycling
  (cycle=4020). Note `status.json` `updated_at` is 21:54:20Z — stale by ~2h —
  while the heartbeat file is current: "healthy" tracks the process, not
  delivery, and not even freshly. (→ BL-121: liveness must be delivery-level.)
- **`inbox/failed/` is empty** — the dead-letters are NOT in failed/; they sit
  in `inbox/new/` renamed `*.handoff.dead` with sibling `*.dead.chase.json`.

## Dead-letter pile: 13 dead + 13 chase-retries + 1 undelivered

31 entries in `inbox/new/` (excluding `tmp/`):
- `.dead` parcels: **13**  •  `.dead.chase.json`: **13**  •  plain `.handoff`: **1**
- Recipients: **13 for_coordinator, 17 for_specifier** — i.e. the two idle roles.
- Span: **000302 @ 11:04:56Z → 000321 @ 22:25:39Z** — ~11 hours of dead-letters
  accumulating today with zero recovery. (→ BL-121 detection, BL-122 recovery.)

### The retry gives up silently (BL-122 core gap)
`50_..._000321_from_coordinator_to_specifier_for_specifier.handoff.dead.chase.json`:
```json
{"chaseCount":3}
```
The chaser retried 3× and abandoned the parcel — no re-route to a live consumer,
no needs-human escalation. The work is simply lost.

### The lost messages are the swarm coordinating about itself
Dead parcel `..._000321_from_coordinator_to_specifier`:
```
from: coordinator   to: specifier   type: note   priority: 50
message: Raw intake BL-128 at backlog root: coordinator/specifier mailbox split.
created_at: 2026-07-05T22:25:39Z
```
Newest, still-undelivered parcel `..._000323_from_specifier_to_coordinator`
(will likely die too — coordinator is idle):
```
from: specifier   to: coordinator   type: note   priority: 01
message: BL-128 (prio 01) + BL-127 specced into backlog/paused/ at ae05f04; root drained.
```
The specifier and coordinator are trying to hand off tickets **about the mailbox
/ handoff problem itself**, and those very handoffs dead-letter.

## Why (ties the three tickets together)
Deliveries target roles sitting at an idle `❯`. Per **BL-109**, injecting into an
idle Claude Code pane misreads the idle footer as pending input, so the nudge
fails; the parcel is marked `.dead`, the chaser spins (`chaseCount` up to 3) and
then gives up. Per **BL-121**, nothing detects this at the delivery level (daemon
stays "healthy"); per **BL-122**, nothing re-delivers or escalates — so 11 hours
of coordination messages rot in `inbox/new/` while the swarm sits at a standstill.

Raw captures: `halt-forensics.txt`, `halt-forensics-detail.txt` (session scratchpad).
