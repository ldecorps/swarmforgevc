# BL-893 QA bounce 2 — 20260825

**Reviewed tip:** documenter `304ad8844d` (not merged — tip impure)
**Task:** `BL-893-approvals-ambulance-choice`
**Sibling:** `VERIFY BL-893`
**Prior bounce:** `e941a4f25f` (D1: tip not based on then-current main)

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** — tip not landable; not merged |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 304ad8844d  # fails
comm -12 <(git ls-tree -r --name-only origin/main | sort) \
  <(git diff --diff-filter=D --name-only origin/main 304ad8844d | sort)
# → backlog/evidence/BL-893-qa-bounce-20260825.md
```

**Commit hash checked:** `304ad8844d`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = e941a4f25f bounce tip)
paths=21 dels=1
deleted on tip vs present on origin/main:
backlog/evidence/BL-893-qa-bounce-20260825.md
(tip message claimed "Merge origin/main for ancestry" but pre-dates bounce land)
```

**Failure class:** `behavior`

**Expected vs observed:** Expected rematch onto `e941a4f25f` with bounce
evidence retained and `dels_on_origin=0`. Observed merge tip that drops the
bounce evidence file and still lacks current main as ancestor.

**Remediation:** `git reset --hard origin/main` (`e941a4f25f` or newer),
restore BL-893 product/docs only, keep
`backlog/evidence/BL-893-qa-bounce*.md`. Confirm ancestry + dels=0 before
forward.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
