# BL-1426 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- `qa-sibling-check.js status --ticket BL-1426`: `VERIFY BL-1426` (no open
  deferral).
- Orphan process check before/after: empty both times.
- Merge-deletion guard: my own sync merge correctly flagged the deletion
  of `post_qa_branch_sweep.bb`/`.sh` (this ticket's whole deliverable, per
  its own `retires:` list) — satisfied by naming BL-668 (the guard's own
  id-extraction from the introducing commit) in the merge commit body,
  same resolution the hardener independently used.
- Retirement confirmed on disk: `swarmforge/scripts/post_qa_branch_sweep.bb`
  and `.sh` both absent.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1426-the-never-parsed-sweep-wrapper-is-retired.feature`
  — 3/3. Sibling feature `BL-668-post-qa-deterministic-branch-sweep.feature`
  stays green: 5/5, unchanged.
- Wiring anchor: `bl1426PostQaSweepWrapperRetiredSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Invariant 1 confirmed: `git diff --name-only` across this ticket's full
  range against `swarmforge/scripts/handoffd.bb`,
  `post_qa_branch_sweep_lib.bb`, and its test runner — empty (none
  touched).
- qa_e2e item 2: `git ls-files swarmforge/scripts | grep post_qa_branch_sweep`
  lists exactly 3 survivors (`post_qa_branch_sweep_lib.bb`, and under
  `scripts/test/`, `post_qa_branch_sweep_cli.bb` and
  `post_qa_branch_sweep_lib_test_runner.bb`) — matching the ticket's own
  "concretely" list verbatim and the hardener's independent count. Note:
  the ticket's own qa_e2e text says the grep count "is 2" immediately
  before naming 3 survivors — an arithmetic slip in the ticket's own
  wording, not a substantive defect; the actual state matches the
  explicit list exactly, so not treated as a spec-gap.
- qa_e2e item 4: `bb .../post_qa_branch_sweep_lib_test_runner.bb` — ALL
  PASS. How-to's "Verify" section fenced block lists only test-runner/
  acceptance commands, never a direct invocation of the retired
  wrapper/shim; the one prose mention (`docs/how-to/BL-668-...md`) is the
  retirement sentence itself, matching scenario 03's own carve-out.
- qa_e2e item 5: scenario 02 IS the live reader-pass check (reads the
  parcel's own tracked tree directly, never a fixture, per the handler's
  own header comment) — already exercised against my live worktree by
  the acceptance run above ("every Babashka script under swarmforge/scripts
  reads to its end", ok) — no separate post-land step needed.
- Cleaner's own bounce-and-fix (D1, a real gap: coder's evidence claimed
  doc edits that the diff didn't contain) reviewed — confirmed resolved:
  the how-to and Specification.MD both carry the expected edits in the
  tree I inspected.
- Full unit lane (`npm test`, extension/): 610/610 files, 10309/10309
  tests green. Same chronic suite-duration/per-file budget overage as
  other recent parcels, unrelated.
- Full property lane (`npm run test:properties`): see "Not bounced"
  below — a large but load-explained failure set under severe host
  contention (uptime 42.25 during the run); confirmed 368/368 clean via
  isolated re-run once load eased (~8.1).

## Not bounced (observed, not this ticket's defect, not reproducible — severe host load)

Full property run failed 15 files (22 individual test failures) under
severe host contention: `uptime` read load average **42.25** during the
run (vs. a normal ~1-3), the highest observed this shift. The failure set
spans a wide, unrelated mix of domains (land-step entanglement/replay,
freshness/heartbeat timers, dry-run spawning, commit-guard aggregation,
stamp-off invariants, approval rulings, a dependency gate, a load-time-
binding guard) — the signature of host-wide resource exhaustion, not a
shared code path any of them share with BL-1426 (a static file-retirement
and reader-pass check). No ticket found for any of the 15
(`grep -rl <name> backlog/paused/ backlog/active/` empty for each; none in
`backlog/standing-reds.tsv`). Re-ran all 15 in isolation once load eased
(load average ~8.1, later ~6-8 for the long test): all 15 passed cleanly,
`bl968MaterializedGuardSensitivity` in its normal ~165s with only the
BL-871 allowlisted benign error. Same disposition as this shift's prior
"Not bounced" sections (BL-1442, BL-1424, BL-1412, BL-1409 ×2, BL-1226),
scaled to this run's much heavier contention.
