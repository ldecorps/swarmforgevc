# BL-1293 — QA pass, 2026-08-30

**Verdict: APPROVE.**

## Reviewed

Merged coder → architect → hardener chain
(`1b3200264c`, `224a08cab1`, `e0a009f945`) into `swarmforge-QA`, after first
merging `origin/main` (per coder's note that BL-1293's `qa_e2e_procedure`
step 4 was amended on `main` at `58e69ac650`). `required_stages: [coder,
architect, hardender, qa]` — cleaner and documenter legitimately skipped per
the ticket's own `stage_skip_reasons`; not re-litigated.

## Independent re-verification (not taken on the written evidence alone)

| Check | Result |
|---|---|
| `review_forward_evidence_gate_lib_test_runner.bb` | ALL PASS |
| `review_forward_evidence_gate_lib_property_runner.bb` | ALL PASS |
| `pre_qa_gate_lib_test_runner.bb` (sibling consumer) | ALL PASS |
| `pre_qa_gate_gather_lib_acceptance_contract_test_runner.bb` | ALL PASS |
| `pre_qa_gate_gather_lib_acceptance_pointer_test_runner.bb` | ALL PASS |
| BL-1293 acceptance (own feature) | 6/6 |
| BL-806 acceptance (regression, sibling gate) | 9/9 |
| BL-950 acceptance (regression, sibling gate) | 6/6 |
| Bounding negative, re-run directly against the real repo: `forward-introduces-nothing-own?("b7d22b9ee")` | `false` (matches coder/architect's claim — the real BL-1224 commit resolved a genuine conflict, so it correctly is NOT refused by this remedy; closing that shape is BL-1307, already minted) |

## Wiring

- `swarm_handoff.bb:370` calls `review-forward-evidence-gate-lib/forward-introduces-nothing-own?`,
  threading the fact into both `blocked?` (:381) and `refusal-message` (:455)
  — a real send-path caller, not merely unit-tested in isolation.
- `merge-introduces-nothing-unique?`/`commit-parents` are `defn` (public) in
  `pre_qa_gate_gather_lib.bb`, reused (not copied) by the review-forward gate
  — satisfies `approval_context`'s one hard constraint.
- Both `required_wiring` anchors resolve: `merge-introduces-nothing-unique`
  reused at `review_forward_evidence_gate_lib.bb:107`; `bl1293` registered at
  `specs/pipeline/steps/index.js:550`.
- Merge-parent-count guard (`>= (count commit-parents) 2`) present at
  `review_forward_evidence_gate_lib.bb:103`, matching sibling
  `no-dropped-work?`'s own guard — the regression coder caught in their first
  cut stays fixed.

## qa_e2e_procedure — all six items covered

1–4 and 6: covered by the BL-1293 acceptance scenarios (outline rows +
scenarios 02/03) and the successful acceptance run (a missing step handler
throws, per `required_wiring`'s own note — the clean 6/6 run is itself proof
of item 6). Item 5 (the bounding negative on `b7d22b9ee3`): independently
re-run above, matches. The ticket's own 2026-08-30 amendment already
corrected steps 4–6 to match what this remedy actually closes; nothing
further to reconcile.

## Scope note

The residual gap this ticket deliberately does NOT close (a forward that
resolves a real conflict while carrying no role-authored evidence) is
already split out and minted as **BL-1307** (found already paused in the
backlog on merging `origin/main`) — not a gap in this pass.

## Whole-suite sanity

`npm test` / `npm run test:properties` were run in full during BL-1300's QA
pass earlier today against the same set of 26 pre-existing unrelated
failures (see `backlog/evidence/BL-1300-qa-hold-pending-human-ruling-20260830.md`).
BL-1293 touches no `extension/test` or `extension/src` path, so those runs
are not re-executed here; the changed-path Babashka/acceptance runs above
are what Article 4.5 requires for this parcel's own paths.

By QA.
