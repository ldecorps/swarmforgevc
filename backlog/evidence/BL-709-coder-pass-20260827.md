# BL-709 — coder pass (option A adopt) — 20260827

## Choice

Option A from `approval_context`: adopt the Bubble-topic implementation
already on `origin/main` (core/live/front-desk/bridgeServer) as parcel
content, add the missing acceptance contract + invariant properties, and
close the unbound-fallback gap named by feature scenario 07.

## Product delta

- `effectiveLetsTalkMirrorTopicId`: dedicated Bubble when bound; else Cursor
  Remote (previous behaviour). `mirrorLetsTalkTurnToBubble` uses it.
- Unit tests for unbound fallback.
- Property encodings P1–P3 for the three declared invariants.
- APS step handlers for all seven scenarios.

## Checks

- `letsTalkBridge` BL-709 unit tests
- `bl709BubbleOwnTelegramTopic.property.test.js` 3/3
- tip paths BL-709-scoped

By coder.
