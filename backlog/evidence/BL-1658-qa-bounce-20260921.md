# BL-1658 — QA bounce, 2026-09-21

1 defect found (one root cause, 17 failing scenario outcomes). Full
checklist run to completion before this bounce (Article 4.4).

## D1

- **Failing command**: `node specs/pipeline/cli.js specs/features/BL-1658-the-seven-remaining-eager-jsdom-handlers-load-it-lazily.feature`
- **Commit hash**: (QA's merge tip, documenter forward 5e5e262df8)
- **First error excerpt**:
  ```
  not ok 9 ... failed at step "When bl1146HostQueueEnqueueNextHoldOnHostQuestionSteps.js is required alone...": unknown <handler> example value: bl1146HostQueueEnqueueNextHoldOnHostQuestionSteps.js
  not ok 10 ... "the module-load budget guard over the real tree names none of the twenty-two with an empty allowlist": no step handler matched "Then it names none of the twenty-two as a violation"
  not ok 11..25 ... "every eager cursor-bridge requirer loads that graph inside its step, and the census is complete": no step handler matched "Then extension/out/bridge/cursorBridgeAgentSession is not loaded by that require"
  ```
  9/25 scenario outcomes pass, 17/25 fail - one root cause.
- **Failure class**: acceptance
- **Expected vs observed**: expected `bl1658SevenJsdomHandlersLoadLazilySteps.js` to recognize the ticket's current Examples tables (nine handlers in scenario 01/09, "the twenty-two" in scenario 02, and scenario 03's fifteen-handler cursor-bridge population); observed the step handler still built against the EARLIER contract (eight handlers, no scenario-03 fifteen-handler population, no "cursorBridgeAgentSession is not loaded" step, no "the twenty-two" wording) - the ticket's own amendment (3), landed on `main` at 13:52 local, expanded the contract AFTER this parcel (documenter -> QA at 13:39 local) was already built and forwarded.
- **Blamed role**: coder (mechanically: only the coder can add the handler
  recognition and new step definitions the amended contract now requires)
- **Remediation pointer**: `specs/pipeline/steps/bl1658SevenJsdomHandlersLoadLazilySteps.js`
  - add the fifteen `bl...Steps.js` names from scenario 03's Examples table
    to whatever lookup backs "is required alone in a fresh child process
    with the loader intercept" and "the census grep ... names exactly
    nineteen handlers of which fifteen were eager at mint"; add
    `bl1146HostQueueEnqueueNextHoldOnHostQuestionSteps.js` to scenario
    01/09's handler set; add the "Then extension/out/bridge/cursorBridgeAgentSession
    is not loaded by that require" and "Then it names none of the
    twenty-two as a violation" step definitions.

**Attribution note (BL-990, per the specifier's own advance timing
record):** `backlog/active/BL-1658-...yaml`'s notes section already
records this exact collision - amendment (3) landed at 13:52 local while
this parcel (5e5e262df8, documenter -> QA at 13:39) was already in
QA's inbox, built against the eight-handler contract: "QA merging main
will find scenario 03 with no handler ... and bounce to the coder for
the fifteen - that bounce is the specifier's amendment timing, not the
coder's workmanship." This bounce is recorded as blamed: coder (the
mechanical fix owner) AND immediately corrected via
`record-bounce-correction.js` citing this note, so the coder's own
bounce-rate metric is not charged for a mid-flight spec amendment it had
no way to see.

By QA.
