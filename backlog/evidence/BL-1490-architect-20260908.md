# BL-1490 — architect review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- **Two-layer/host-I/O/webview/secrets boundary**: N/A. No `extension/src/**`
  or webview file touched; the whole diff is `swarmforge/scripts/*.bb`
  (daemon) plus `specs/pipeline/steps/**` acceptance wiring.
- **Dependency-rule gate**: `node extension/out/tools/dependency-gate.js`
  scopes only `extension/src/**` (`extension/.dependency-cruiser.cjs`'s own
  layer mapping) — not applicable, no file in that tree changed.
- **Co-change coupling**: ran
  `node extension/out/tools/co-change-report.js swarmforge/scripts/daemon_cycle_guard_lib.bb swarmforge/scripts/handoffd.bb swarmforge/scripts/handoffd_supervisor.bb`.
  Top hits are `handoffd.bb`/`daemon_cycle_guard_lib.bb`/
  `handoffd_supervisor.bb`'s own existing test runners and sibling daemon
  libs — expected same-subsystem coupling, nothing suspicious.
- **Invariants review**: both declared invariants have non-vacuous
  coder-authored property tests (`bl1490_supervisor_tick_progress_property_runner.bb`,
  200 draws + a real marker-file half; break-then-restore documented in the
  file's own header comment). Ran green:
  `bb swarmforge/scripts/test/bl1490_supervisor_tick_progress_property_runner.bb`
  → `ALL PROPERTIES HOLD`. Site sweep: grepped every `poll-once!`/
  `canary-sweep!`/`startup-notify-pending!`/`run-sweep!` call site in
  `handoffd.bb` — the three per-tick phases the ticket names are the only
  ones outside `run-sweep!`, and cleaner's `run-tick-phase!` extraction
  covers all three call sites (no missed site).
- **Property testing pass (undeclared)**: the touched pure decision fn
  (`evaluate-health`/`effective-in-sweep-budget-ms`) is already covered by
  the declared-invariant properties above; no further undercovered
  property-shaped module in the diff.
- **Regression check**: ran the two directly-adjacent pure-function suites
  unaffected by design —
  `bl977_supervisor_progress_property_runner.bb` (`ALL PROPERTIES HOLD`) and
  `handoffd_supervisor_startup_grace_test_runner.bb` (`ALL TESTS PASS`) —
  both green post-merge, confirming `evaluate-health`'s signature change
  (`in-sweep-budget-ms` now computed via `effective-in-sweep-budget-ms`
  instead of destructured directly) stays backward-compatible for every
  pre-BL-1490 caller.
- **Acceptance**: `node specs/pipeline/cli.js specs/features/BL-1490-a-poll-cycle-phase-is-never-invisible-to-the-supervisor.feature`
  → 5/5 scenarios green.
- **Correctness read**: traced `run-tick-phase!`'s bracket (mark before
  batch, re-mark after each unit, idle after) against all three call sites
  post-refactor and the real `check!` wiring (`in-flight-sweep-name`/
  `tick-budget-ms` observations feeding `evaluate-health`) — sound, no
  defect spotted.
- **Out-of-parcel finding (not a bounce — unrelated, predates this
  parcel)**: `specs/features/BL-977-supervisor-never-halts-a-progressing-daemon.feature`
  scenario "a wedged poll loop is still caught within the in-sweep budget"
  fails on `main`/this parcel alike — reproduced identically after
  checking out the pre-BL-1490 versions of the three changed `.bb` files
  (ancestry-clean revert-and-restore, no working-tree residue left).
  Root-caused: `bl977SupervisorProgressSteps.js`'s `mkSupervisorFixture`
  writes `handoffd.pid` fresh (no `fs.utimesSync` backdate), so
  `daemon-age-ms` is near-zero when `runCheckOnce` fires — the 2026-09-02
  startup-grace hotfix (`within-startup-grace?`, `daemon-age-ms <=
  stall-ms`) then forces `:healthy` before the wedged-marker/stale-heartbeat
  branch is ever reached, regardless of how stale the marker or heartbeat
  is. The fixture predates the startup-grace hotfix and was never updated
  to backdate the pid file, so BL-977's own invariant 2 (a wedged loop is
  still caught) is currently UNVERIFIED by its acceptance suite. Grepped
  `backlog/` for `wedged poll loop`, `in-sweep budget`, `BL-977` in
  `active/paused/hold` and `backlog/standing-reds.tsv` — no open ticket.
  Sent as a `note` to specifier + coordinator (priority `00`), not a
  bounce — BL-1490's own `out_of_scope` names BL-977 done and untouched,
  and this parcel's changes make no difference to the failure either way.
