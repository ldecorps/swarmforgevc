# BL-1566 — QA review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Merged documenter's `d510f163bd` (BL-1566) into the QA worktree at
  `8ca8a8a4fa`; ancestry checks clean —
  `git merge-base --is-ancestor <hardener 43cf7260ea|architect 38e2b5e4f1|cleaner a119e9e430> HEAD`
  all OK, so this commit carries the whole pack's own work, not a sibling's.
- `qa-sibling-check.js status --ticket BL-1566` — `VERIFY BL-1566`, no open
  deferral.
- No orphaned `node --test`/`stryker`/vitest processes before or after this
  pass.
- `pre_qa_gate.sh BL-1566-an-article-42-hold-is-a-record-qa-resumes-from HEAD`
  — `OK` (all three `required_wiring` anchors present in the live receive
  and completion paths, matching the diff read below).
- `npm test` (extension unit suite) — 616/616 files, 10479/10479 tests
  green.
- `bb swarmforge/scripts/test/qa_hold_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/qa_hold_cli_test_runner.bb` — ALL PASS.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1566-an-article-42-hold-is-a-record-qa-resumes-from.feature`
  — 8/8 scenarios pass (background, HOLD/RELEASED status lines, the
  note-completion refusal, the parking exception, and the close-with-outcome
  release all exercised against the real `ready_for_next_task.bb` /
  `done_with_current_task.bb` binaries).
- Read the diff of `ready_for_next_task.bb` / `done_with_current_task.bb`
  directly: `print-qa-hold-status-if-any!` runs first in `-main`, before
  in_process resume and before `NO_TASK`; `qa-hold-gate!` runs before the
  BL-1422 work-note-evidence gate in `done_with_current_task.bb`'s
  completion path and only refuses a `note` inbound with a released hold,
  never a `git_handoff` (the parking exception is preserved) — matches the
  ticket's `required_wiring` anchors and the acceptance scenarios' own
  assertions.
- `npm run test:properties` — 402/405 files green, 3 failed
  (`bl1297MergeOwnPathsInvariants.property.test.js`,
  `bl1343ReplayNeverDropsOwnPathInvariants.property.test.js`,
  `multiBranchParserCoverageCheck.property.test.js`); the 2
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors are the
  BL-871 allowlisted noise. None of the three touches a BL-1566 file (bb
  scripts, a step handler, docs only) and none is this parcel's defect:
  - `bl1297...` — registered (`backlog/standing-reds.tsv`, property lane,
    owner BL-1564, first_seen 2026-09-14); already an open, owned red, not
    a new one.
  - `bl1343...` — timed out at 20s in the full lane; run alone,
    16.48s/2 tests, both green (`bl1343ReplayNeverDropsOwnPathInvariants.property.test.js`).
    Same lane-timeout-under-contention class BL-1572 already named for this
    exact file (`backlog/paused/BL-1572-...yaml`, "Not in scope": green
    alone in QA's own isolation, no sampled floor — "the lane-timeout class
    BL-1564 owns for bl1297. A green test needs no owner (2026-09-07
    rule)."). No new ticket needed.
  - `multiBranchParserCoverageCheck...` — a `fc.assert` counterexample in
    the full lane; run alone 15/15 times (single run 16ms, 15-run sweep all
    green) — not registered anywhere in `backlog/standing-reds.tsv`, not
    named in BL-1572's list, but the same "green alone" shape the
    2026-09-07 rule covers. No new ticket needed; flagged here for the
    record in case it recurs.
- Documentation: `docs/index.md` and `docs/reference/Specification.MD`
  both carry the BL-1566 entry, linked to
  `docs/how-to/BL-1566-article-42-hold-is-a-durable-record.md`; no diagram
  depicts qa-hold internals (documenter's own note, confirmed correct — the
  registered diagrams are architecture/swarm-flow/handoff-flow/front-desk-
  flow, none of which depicts this internal store).
- Ticket's one declared invariant ("a released hold is never silently
  dropped") — coder's stated-reason encoding (grep for `fs/delete`/`fs/move`
  across `qa_hold_lib.bb`/`qa_hold_cli.bb`: exactly one hit, inside
  `cmd-close!`, after the record is already written to `closed-dir`) is
  sound; independently re-ran the same grep and confirm.
