# BL-1352 — cleaner pass, second review (2026-09-03)

## Context

First pass bounced D1 (property test invariant 1's generator reach was drawn
by luck rather than enumerated, ~33% failure rate) — see
`BL-1352-cleaner-bounce-20260903.md`. This is the re-review of the coder's
rework at `08435cee05`.

## D1 verified fixed

- `escalation-transport-state` invariant 1 now enumerates all four
  transport×waiting corners explicitly rather than drawing the empty-array
  arm from `fc.uniqueArray`. Re-ran `npx vitest run --config
  vitest.properties.config.mjs bl1352EscalationVisibilityInvariants` **15
  consecutive times — 0/15 failures** (was ~5/15 before the fix).
- `bb swarmforge/scripts/test/bl1352_escalation_transport_test_runner.bb` —
  ALL PASS.
- `run_acceptance.sh specs/features/BL-1352-escalation-transport-fault-is-visible.feature`
  — 7/7 pass.

## Two incidental property-test fixes (coder's own earlier work, BL-1323 and
## BL-1306) — checked, both legitimate and green

- `bl1323StampOffInvariants` invariant 1: rescoped from
  `origin/main...HEAD` (whole branch) to the stamp-off's own commits
  (`git log --grep BL-1323`), because BL-1335's legitimate `handoffd.bb` edit
  on this same branch had started tripping it. Re-ran: 3/3 pass.
- `bl1306AuditKeyBasisInvariants`: given an explicit 180s timeout budget
  (each property case spawns real `bb`/git-fixture work) and `numRuns`
  trimmed since the arms are already enumerated by the surrounding loop, not
  by repetition. Re-ran: 2/2 pass, well under budget.

## D2 — same undocumented regression recurred a second time (found and fixed
## again, not bounced)

`bl1332SharedPathLineLeakSteps.js` — a file outside this ticket's scope —
had its "the guard passed against a fixture that should reproduce the shape
that froze main" assertion silently dropped a **second** time by this same
coder commit, exactly as it was in the first pass (bounced then too, restored
at `434b759d39`). Not mentioned anywhere in `08435cee05`'s commit message or
evidence. Restored again in this merge (`e3024e1529`), verified byte-identical
to `origin/main`, and `run_acceptance.sh specs/features/BL-1332*` re-run:
6/6 pass including the restored scenario.

**Not bounced a second time** because: (a) it is the identical one-assertion
regression already diagnosed once, restoring it is mechanical and safe
(verified byte-identical to origin/main, not a judgment call about test
design), and (b) the BL-1352 rework itself (the ticket actually in front of
me) is correct and shouldn't be held hostage to a recurring but
now-well-understood side effect. Recording it here rather than silently
fixing it a second time without a trace, per Article 4.4's spirit — if this
recurs a THIRD time on a future parcel it should go to the coder (or
specifier, as a process concern) rather than be silently patched again.

## Scope / mutation-site / property-suite checks (unchanged from first pass)

- Coder's own diff-stat (`08435cee05`) touches only files it declares:
  BL-1352 production/test files plus the two named BL-1323/BL-1306
  incidental fixes. No other scope creep beyond the recurring bl1332 line.
- `SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD` override the coder used is recorded
  in their own evidence as a load-flake on unrelated files, consistent with
  this session's other overrides; not re-verified here (outside cleaner's
  property-suite ownership).

## Verdict

No defect blocking BL-1352. Forwarding.
