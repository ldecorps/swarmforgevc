# BL-1515 — coder evidence, 2026-09-11 (architect bounce D1 remediation)

Architect bounce (887f9a9080) found the live-repair deliverable (item 3 of
the ticket, FIRM in `human_approval.approval_context`) asserted only in a
commit-message narrative, with no `backlog/evidence/` artifact and no actual
`git worktree list` output. This file is that artifact, taking the
architect's "simpler" remediation path: run the now-idempotent guard and
record genuinely fresh before/after output, rather than reconstructing the
one-time rename from forensic history alone.

## `git worktree list` — BEFORE

```
/home/carillon/swarmforgevc/.worktrees/coder                                                                              c5c7ee3b49 [swarmforge-coder]
/home/carillon/swarmforgevc/.worktrees/coder/.worktrees/coder                                                             2e519c4a8f [primary/coder]
```

## Guard run — production decision path, not a paraphrase

Invoked the real `branch_identity_guard_lib.bb` `decide` fn against facts
gathered with the exact same helper functions `ready_for_next.bb`'s
`enforce-branch-identity-guard!` uses (`branch-identity-facts`, same git
reads), for role `coder`, declared branch read via
`handoff-lib/load-role-info` (the same `:session` accessor
`tree_collapse_guard_lib.bb` uses — BL-897):

```
role= coder
declared= swarmforge-coder
facts= {:declared "swarmforge-coder", :actual "swarmforge-coder", :actual-tip "c5c7ee3b49fcff557bf5e5654d0c3d6c31c2141b", :declared-ref-exists? true, :declared-tip "c5c7ee3b49fcff557bf5e5654d0c3d6c31c2141b", :origin-ref-exists? true, :origin-tip-ancestor-of-actual? true, :git-read-error? false}
verdict= {:status :ok}
```

`:ok` matches invariant 1: the declared branch is present and already equals
the actual branch, so the guard changes nothing (an `:ok` verdict is the
only status that prints no line in `ready_for_next.bb`, which is why this
turn's own dispatch — task `BL-1515-missing-live-repair-evidence` — produced
no `BRANCH_DRIFT_REPAIRED`/`BRANCH_DRIFT_DETECTED` line: the guard already
ran clean, silently, at the top of this very turn).

## `git worktree list` — AFTER

```
/home/carillon/swarmforgevc/.worktrees/coder                                                                              c5c7ee3b49 [swarmforge-coder]
/home/carillon/swarmforgevc/.worktrees/coder/.worktrees/coder                                                             2e519c4a8f [primary/coder]
```

Byte-identical to the before state — no ref moved, HEAD unchanged, confirming
the repair is idempotent (ticket's "How" section: "a second run after the
rename is `:ok`").

## Supporting history — the original rename was the safe shape

`git reflog show swarmforge-coder | tail -5`:

```
3c7c9c27ec swarmforge-coder@{932}: commit (merge): Merge cleaner 7b32e9f5f6 (reverse hop, merge-only) into coder.
4a818693fb swarmforge-coder@{933}: commit: BL-1378: self-audit — the fixture asserted the wrong sha.
0dae447d74 swarmforge-coder@{934}: commit: BL-1378: an expedite-closed ticket can satisfy the close guard.
19ce7b76f8 swarmforge-coder@{935}: commit: work that never reached main
0f88e62b85 swarmforge-coder@{936}: branch: Created from HEAD
```

The reflog for `swarmforge-coder` runs continuously back through the
`side`-era commits with a single `branch: Created from HEAD` entry at the
bottom (`0f88e62b85`, the branch's original creation) — no second creation
event at the point of the rename, confirming the rename (`git branch -m side
swarmforge-coder`, performed by hand before this parcel per the coder's
6ef1850adb commit message) preserved the branch's full commit history and
tip exactly as invariant 1 requires: a rename never moves HEAD's commit.

By coder.
