# BL-839-master-checkout-drift-from-main-on-daemon-executed-scripts — QA final pass (2026-08-08)

## Commit checked

`a7e8536c` (QA worktree merge of documenter's `8bb7f48af5`, batched with
BL-819/BL-822/BL-773). Ancestry confirmed: coder `8a65d548`, cleaner
`25c24fd1`, architect `de0d8701`, hardener `cd67e036`, documenter `8bb7f48a`
are all ancestors of `a7e8536c`
(`git merge-base --is-ancestor <each> a7e8536c` — all exit 0).

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile` under node v22.23.2 — see BL-773 note on the
  node-version environment issue): clean.
- Full unit suite (`npm test`): 406/406 files, 7215/7215 tests green (see
  `backlog/evidence/BL-773-qa-pass-20260808.md` for the node-version note;
  this ticket's own files are pure `.bb`, not part of the vitest suite).
- Independently re-run, not taken on the hardener's/architect's word:
  - `bb swarmforge/scripts/test/bl839_master_checkout_drift_property_runner.bb`
    → `ok` (both declared invariants: never-writes, fail-closed).
  - `bb swarmforge/scripts/test/master_checkout_drift_lib_test_runner.bb`
    → `ALL TESTS PASSED`.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-839-master-checkout-drift-from-main-on-daemon-executed-scripts.feature`):
  9/9 scenarios PASS.
- Invariants: both declared invariants ("never writes", "a check that
  cannot run reports that it could not run") have real, non-vacuous
  property coverage per the architect's pass — confirmed by independently
  re-running the property runner above rather than re-deriving from scratch.
- Wiring: `handoffd.bb` calls the new sweep unconditionally in its cadence
  block, wrapped in its own `try/catch` — confirmed present via the
  architect's evidence and spot-checked with `grep -n
  master-checkout-drift-sweep swarmforge/scripts/handoffd.bb`.
- No orphaned test/mutation processes before or after this pass
  (`pgrep -fl 'node --test|stryker|vitest'` — none, checked both times).

## Note on the QA-destroyed-repro-evidence incident

`backlog/evidence/BL-839-qa-destroyed-repro-evidence-20260807.md` travels
with this parcel and documents a separate QA *operational* incident against
the original 2026-08-06 dirty-checkout repro — not a defect in this
ticket's own detector code, and explicitly out of scope for this ticket
per its own `out_of_scope` field ("Cleaning up the specific 2026-08-06
BL-835 reversion ... is a one-off operator action, reported separately").
Nothing to act on here; the detector itself is what this pass reviews, and
it is sound.

## Verdict

Clean pass. No defects. Approved as part of the BL-839/BL-819/BL-822/BL-773
batch — see `backlog/evidence/BL-773-qa-pass-20260808.md` for the shared
merge-up/landing details.

By QA.
