# BL-1263 — architect bounce, 2026-09-05

## Violated rule

`backlog/standing-reds.tsv`'s own header: "A row is removed in the same
land that turns the test green." This session's own precedent (BL-1229,
BL-1290, BL-1430) has the coder remove its own ticket's register row in
the same commit that fixes the underlying test.

## Commit reviewed

`e682bfa163` (cleaner NONE pass — the underlying test fixes are correct
and complete; only the register bookkeeping is missing).

## What is wrong

`backlog/standing-reds.tsv` carries two rows naming BL-1263 as owner:

```
unit	extension/test/telegramClient.test.js	BL-1263	2026-08-29	asserts behaviour a deliberate source change replaced
unit	extension/test/telegramCursorOperatorExec.test.js	BL-1263	2026-08-29	ambulance engage/release message shape changed
```

Both rationales describe exactly the two defects this parcel fixes. I
independently ran both files after the fix: `telegramClient.test.js`
(87/87 pass) and `telegramCursorOperatorExec.test.js` (24/24 pass) — both
fully green. Neither row was touched by this parcel's own commit
(confirmed via `git diff <coder's-own-parent>..<coder's-commit> --
backlog/standing-reds.tsv`: empty).

`backendSwitch.test.js` (the third site) was never in the register, so no
row exists to remove there — nothing else to do for site 1.

## Why this matters

Once this ticket closes, these two rows will name a CLOSED ticket for
tests that are demonstrably passing — the exact "stale/orphaned register
row" class Article 3.6/BL-1428 exist to prevent, except here the fix
already happened and the row is simply not cleared. Left as-is, a future
reader (or QA's Article 4.2 gate) reads `backlog/standing-reds.tsv` and
believes these two files are still red owned by an open ticket, when
neither is true.

## Remedy

Remove both rows from `backlog/standing-reds.tsv` in the same commit (or
a follow-up commit within this same parcel) that lands the three
assertion fixes — exactly the bookkeeping this session's BL-1229/BL-1290/
BL-1430 parcels each performed for their own rows.

## Scope note

This is NOT the same class as BL-1206's `hostActivityFeed.property.test.js`
finding (recorded separately, routed as a note to specifier+coordinator):
that row's defect was never touched by BL-1206 and was explicitly out of
its scope. Here, BL-1263 fixed the exact defects these two rows describe
and simply needs to record that fact.
