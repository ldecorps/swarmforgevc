# BL-1476 — architect review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- `node extension/out/tools/dependency-gate.js` over the four touched files
  (`transcriptSummaryStore.ts`, `transcriptWalker.ts`,
  `turnProfileProducer.ts`, `run-turn-profile-producer.ts`) - PASSED, no
  forbidden edges.
- `node extension/out/tools/co-change-report.js` over the same four files -
  reported coupling is exactly the mechanism this ticket touches
  (`turnProfileProducer.ts` <-> `transcriptWalker.ts` <->
  `run-turn-profile-producer.ts` <-> `handoffd.bb`), not a hidden dependency;
  no action needed.
- Invariant 1 (identical row): `runTick` walks groups/paths in the same
  order `buildTurnProfileWindowForGroups` always used, so stage
  first-appearance order into `buildTurnProfileSeries`'s `byStage` matches a
  full walk. Verified `attributeTrail` (transcriptWalker.ts:230) is a pure
  per-interval `.map` with no cross-interval state, so applying it per-file
  inside `walkTranscriptText` and concatenating (the cleaner's claim) is
  behavior-identical to the prior single call over the full concatenation.
  Confirmed non-vacuous property test
  (`bl1476TurnProfileSweepReadsOnlyWhatChangedInvariants.property.test.js`)
  drives the real compiled producer against generated tick/mutate sequences
  with a generator reach argued to cover the "some files changed, some
  didn't, across multiple ticks" corner the defect actually lives in.
- Invariant 2 (bounded read): `statOrNull` + `summaryIsCurrent` gate every
  read in `runTick`; an unchanged file costs one stat, no read. A path no
  longer listed has its summary dropped before the tick's stat loop runs
  (`priorStore` filtered by `listedPaths`).
- Invariant 3 (deadline): checked only immediately before a needed read
  (`opts.nowFn() >= opts.deadlineAtMs`), returns `partial: true` with the
  store as accumulated so far (summaries completed persist, no window row
  written). CLI's `resolveTurnProfileTickDeadlineMs` clamps to
  `min(30000, SUPERVISOR_IN_SWEEP_BUDGET_MS / 4)` matching the ticket's
  direction; `main()` stays a thin wrapper over the exported, testable
  helpers.
- Coder's own commit (`49a716ed2a`) verified in isolation via `git show
  --stat`: the handoffd.bb diff is exactly the required comment fix
  ("firing every cycle is safe" now describes input cost too); the
  parked-active-sweep code visible in a wider range-diff belongs to
  unrelated main merges already on this branch, not this ticket's scope.
  Step handler (`bl1476TurnProfileSweepReadsOnlyWhatChangedSteps.js`) lands
  in the same commit as required (BL-233).
- `npx vitest run test/turnProfileProducer.test.js
  test/runTurnProfileProducer.test.js` - 43/43 green post-merge.
- No I/O/webview/secret boundary violations; no browser storage; no direct
  process-spawn bypassing tmux (module is pure fs/JSON, no subprocess).
