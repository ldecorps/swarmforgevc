# BL-571 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-571 — `rotation sequential` packs are invisible to ensure's declared signal (`type: defect`, `severity: medium`, milestone M8)
- **Parcel**: `00_20260819T233016Z_000214_from_documenter_to_QA_for_QA` (from documenter, commit `87cfd2cbf8`)
- **Verified at**: `affac4f82` (QA merge of the documenter tip, on top of `3dbf083ac` which carries `origin/main`)
- **Reviewed by**: QA, 2026-08-20
- **Prior bounces**: 1 — architect→coder, `behavior`, `bbb14382d8` (D1, parity gate). That D1 is CLOSED; see G7.

## Verdict

**BOUNCE — inventory items: D1 (one item).**

Every other gate this pass owns was run and passed. The delivered CODE and TESTS are correct and complete; the single defect is a missing ticket-record declaration that leaves a standing gate red.

## Inventory

### D1 — `pre_qa_gate.sh` fails ancestry: BL-571 commit `17ae8a4822` is stranded and undeclared

- **Failure class**: `integration`
- **Blamed role**: `coder`
- **Failing command**: `swarmforge/scripts/pre_qa_gate.sh BL-571-sequential-rotation-dormant-parity affac4f82ac`
- **Commit tested**: `affac4f82`
- **First error excerpt**:
  ```
  PRE_QA_GATE_FAIL ancestry BL-571 17ae8a4822 stranded on swarm/coder
  PRE_QA_GATE_FAIL ancestry BL-571 17ae8a4822 stranded on swarmforge-architect
  PRE_QA_GATE_FAIL ancestry BL-571 17ae8a4822 stranded on swarmforge-cleaner
  PRE_QA_GATE_FAIL ancestry BL-571 17ae8a4822 stranded on swarmforge-documenter
  PRE_QA_GATE_FAIL ancestry BL-571 17ae8a4822 stranded on swarmforge-hardender
  ```
- **Expected vs observed**: expected the pre-QA gate to pass (`OK`) for a parcel carrying all of BL-571's work; observed five ancestry failures naming one stranded BL-571 commit that the ticket does not declare as deliberately dropped.

**Remediation pointer**: `backlog/active/BL-571-sequential-rotation-dormant-parity.yaml` — add
`abandoned_commits: ["17ae8a4822"]` with the rationale below in `notes:`.

**Write it in FLOW style exactly as above.** A block list whose items carry trailing comments does not parse and reads to the gate as ABSENT — that is BL-935's own two-commit fix (`db914d4dd` then `66ec83cb6`); put the rationale in `notes:`, never inline in the list.

#### Why the commit must be ABANDONED and not merged

`17ae8a4822` ("BL-571: correct fake-injection seam names in the ensure tests and steps file") touches exactly two files, and **all of its BL-571 content is already present in this parcel** — verified by content, not by ancestry:

| File | This parcel vs `17ae8a4822` |
|---|---|
| `specs/pipeline/steps/bl571SequentialRotationDormantParitySteps.js` | **Superset** — same corrected `SWARM_ENSURE_*_CMD` names, PLUS the hardener's `fake_daemon_start.sh` stub and the `SWARMFORGE_SKIP_CURSOR_BRIDGE`/`SKIP_BABYSITTERD` fences |
| `swarmforge/scripts/test/test_swarm_ensure.sh` | Same corrected names (49 new-prefix uses, 0 live old-prefix uses). Differs only by **105 lines of BL-958's tests** |

`git diff 17ae8a482 HEAD` deletes **zero** lines mentioning BL-571.

Merging it would fold in BL-958's still-**bounced** control-plane work (`BL-958`/`BL-958 D1` blocks in `test_swarm_ensure.sh`), which an approval on BL-571 does not authorise — constitution "An Approval Authorizes Only Its Ticket's Work" (BL-506). This is the same disposition BL-935 recorded for `e63d364863`.

**The coder's `note` (`BL-571 seam fix ... on coder adc39cc458; merge before gate`) therefore cannot be honoured as written** — `adc39cc458` is a merge of `main` carrying human hotfix `596098dc3`, which this branch ALREADY has (`596098dc3` is an ancestor of `affac4f82`), and reaching `17ae8a4822` through it drags BL-958 along.

#### Why no earlier stage could have caught this

The stranded commit **postdates the parcel**:

| Time (2026-08-20) | Commit | |
|---|---|---|
| 00:24:36 | `ea28ce602` | hardener pass |
| 00:26:01 | `596098dc3` | human hotfix lands on `main` (the seam names) |
| 00:28:07 | `87cfd2cbf8` | **documenter forwards this parcel** |
| 00:34:02 | `17ae8a4822` | coder commits the duplicate seam fix — 6 min AFTER the parcel left |

No stage in the chain was in a position to declare it. The documenter's own pass was complete and correct at the time it ran; this is routing to the role that owns the fix, not a finding against the documenter's work.

## Gate inventory — everything else

| # | Gate | Result |
|---|---|---|
| G1 | Merge integrity — all 9 BL-571 paths vs sender tip `87cfd2cbf8` | PASS — every one UNCHANGED; the merge dropped nothing (BL-954 class) |
| G2 | Stage ancestry — coder `af2dd1b249`, cleaner `87d4c64013`, architect `71f250cb26`, hardener `89ee72a39`/`ea28ce602`, documenter `87cfd2cbf8` | PASS — all ancestors of the cited commit. **Except** `17ae8a4822` → D1 |
| G3 | Prior-bounce check vs `main` AND `origin/main` (BL-891) | PASS — the one prior bounce (architect D1) is closed at `71f250cb26` |
| G4 | `qa_e2e` step 1 — `test_swarm_ensure.sh` | PASS — **45/45, ALL PASS**, exit 0, incl. the new sequential-dormant case and the classic half-launch guard |
| G5 | `qa_e2e` step 2 — `mono_router_lib_test_runner.bb` | PASS — `ok` |
| G6 | `qa_e2e` step 3 — `bl571_single_resident_rotation_property_runner.bb` | PASS — `ok (500 runs, 246 positive, 120 sequential, 254 negative)`; a real distribution, not vacuous |
| G7 | Prior-bounce D1 obligation — the bash↔Babashka parity gate must be reachable from a step the procedure names | PASS — it lives in `mono_router_lib_test_runner.bb:774-799`, which IS step 2. It DERIVES the launcher's set by parsing `swarmforge.sh`'s `is_sequential_dormant` body and asserts set EQUALITY (BL-897) |
| G8 | `qa_e2e` step 4 — non-vacuity, predicate NARROWED to router-only | PASS — suite goes red exactly at `FAIL: BL-571: expected specifier DORMANT under rotation sequential`. Predicate restored, `git diff` clean |
| G9 | `qa_e2e` step 5 — non-vacuity, predicate OVER-WIDE | PASS with a caveat — an always-true predicate fails the suite (exit 1). It trips at `02b` rather than the classic case the step names, because `fail()` exits immediately (fail-fast runner), so the classic assertion is never reached. Over-widening IS caught; the step's wording assumes a run-to-completion runner. Restored, `git diff` clean |
| G10 | `qa_e2e` step 6 — ROTATE_HOME fence | PASS — `ready_for_next_task.bb`/`ready_for_next_batch.bb` byte-identical to `origin/main`; both still call the router-ONLY `conf-rotation-router?` |
| G11 | Fence integrity in the library | PASS — `conf-rotation-router?` refactored to delegate with `["router"]` (behaviour-identical); `resolve-rotation-router-mode?` untouched, per the ticket's BL-931 warning |
| G12 | Acceptance — `run_acceptance.sh specs/features/BL-571-sequential-rotation-dormant-parity.feature` | PASS — 4/4, 0 fail, exit 0, incl. scenario 03 (classic regression guard) and scenario 04 (rotate-home keeps router-only meaning) |
| G13 | Step-handler registration | PASS — `specs/pipeline/steps/index.js:520` |
| G14 | Full unit suite (`npm test`) | PASS — **444/444 files, 7895/7895 tests**, 460.79s, zero failures |
| G15 | Docs currency | PASS — `docs/reference/Specification.MD` documents the topology disagreement, the fence, and the BL-897 parity gate accurately |
| G16 | Dead-seam-name sweep (the incident this ticket's hardening addresses) | PASS — `grep -rn 'SWARMFORGE_ENSURE_[A-Z_]*='` over `swarmforge/` and `specs/` returns **nothing**; the single surviving mention is a comment documenting the incident |
| G17 | Fixture process leaks | PASS — no fixture daemons left behind. The three PPID-1 `handoffd`/`supervisor`/`babysitterd` processes are the REAL swarm's, rooted at the project root, not `$TMPDIR` |
| G18 | Functional change present | PASS — library predicates, ensure consumer, three test lanes, step handler, feature, docs |

## Note for the coordinator (not a BL-571 defect)

G14 ran fully green (444/444, 461s) on this same host where, ~90 minutes earlier during BL-935's pass, the identical suite reported 14 failures in 1462s — every one a wall-clock timeout under load, none an assertion. That is direct confirmation that the standing whole-suite red is ambient host load, not any ticket's code, and that it clears when the box quietens.

By QA.
