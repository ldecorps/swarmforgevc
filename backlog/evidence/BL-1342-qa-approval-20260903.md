# BL-1342 — QA approval (20260903)

Received: documenter commit `3fa2dd6c8f` (review-pass evidence, no doc
defect found), forwarding architect (clean sweep) and hardener/cleaner
review passes. This is a BL-848 review-only stamp-off for already-landed
commit `27d6ab8630` — confirms or refutes, never reimplements or
re-litigates the supervisor's alarm-and-halt policy.

## Review inventory (complete pass, Article 4.4)

- Wiring anchors on the merged tree, all present:
  - `swarmforge/scripts/handoff_lib.bb::read-envelope-if-present` (1 occ).
  - `swarmforge/scripts/handoffd.bb::outbox-parcel-unreadable` (1 occ, live
    poll loop).
  - `swarmforge/scripts/handoffd_supervisor.bb::daemon-age-ms` fed from
    `(file-age-ms pid-file)` into `evaluate-health` via `check!` (1 occ).
  - `specs/pipeline/steps/index.js` requires
    `bl1342SwarmStampHandoffdCrashloopHotfixSteps` (1 occ).
- Invariant 1 (I/O-only swallow): `read-envelope-if-present` catches
  `java.io.IOException` only — read by hand, no `Exception`/`Throwable`
  catch anywhere in the guard or its call site.
- Invariant 2 (vanished parcel is never touched): `poll-once!`'s
  `:vanished` branch only logs `outbox-parcel-unreadable` and skips —
  no delivery, archive, or file mutation on that branch. Read by hand.
- Invariant 3 (bounded, keyed grace): `within-startup-grace?` requires
  `(number? daemon-age-ms)` (nil grants nothing) and
  `(<= daemon-age-ms stall-ms)` (bounded to one stall window); `(not
  alive?) :dead` is checked before `within-startup-grace?` in the `cond`,
  so `:dead` is unreachable by the grace clause. Read by hand.
- `bash swarmforge/scripts/test/test_handoffd_outbox_vanished_parcel_wiring.sh`:
  4/4 PASS (unreadable parcel logged not thrown; daemon survives and keeps
  polling; readable parcel behind it still delivers; skipped parcel stays
  byte-untouched for the next poll).
- `bb swarmforge/scripts/test/handoffd_supervisor_startup_grace_test_runner.bb`:
  ALL PASS (7 assertions).
- `bb swarmforge/scripts/test/bl977_supervisor_progress_property_runner.bb`:
  ALL PROPERTIES HOLD (200 draws + real wedged run-sweep! marker).
- `test_handoffd_supervisor.sh` scenario 01 fails identically on unmodified
  `main` (tmux shim shadowing, per the memory note on zsh-shebang shell
  tests) — pre-existing per the ticket's own qa_e2e_procedure step 7, not
  bounced.
- Acceptance
  (`specs/features/BL-1342-swarm-stamp-handoffd-crashloop-hotfix-27d6ab8630.feature`):
  9/9, including the three age-case scenarios, the not-alive/fresh-pid-file
  negative case, and the ledger-decision-stays-human scenario.
- Constraint check: `backlog/hotfix-ledger.yaml`'s BL-1342 row still reads
  `state: stamp-open` — no `certified`/`waived` written, matching the
  parcel's own constraint.
- No hand-port into worker worktrees: the three production files
  (`handoff_lib.bb`, `handoffd.bb`, `handoffd_supervisor.bb`) run only from
  the master checkout; confirmed no commit in this ticket's history touches
  a worker worktree copy.
- Compile (`extension`, `npm run compile`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population (includes the three known standing guards:
  `tempDirTrapGuard`, `socketFixtureShortRootGuard`, `liveRepoDerivationGuard`).
  None touch this parcel's files.
- Property suite (`npm run test:properties`), isolated rerun after the
  first pass showed load-inflated extras from running concurrently with
  the unit suite: 25 files / 14 tests red, exactly matching the
  standing-debt population (including the three standing guards). No
  BL-1342 file present in the failing set. BL-1342's own property/acceptance
  coverage is fully green.
- Orphaned test processes: none of mine after the runs. One `vitest`
  process was alive mid-check but confirmed (via `ps -o pid,ppid,args`) to
  belong to the cleaner worktree's own concurrent BL-1346 flake-check run —
  left untouched.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`. Ledger row
stays `state: stamp-open` — the certify/waive decision remains human-only.

By QA.
