# BL-1766 — hardender review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Ran `run_acceptance.sh` on the feature: 2/2 pass.
- Ran BL-113 Gherkin acceptance-mutation (soft, then full after the fix) on
  the "the property passes on either side of the UTC date line" Scenario
  Outline's two Examples zones (`Etc/GMT-14`, `Etc/GMT+12`).
- Found: a case/character mutation of either zone string is not rejected by
  Node — it silently resolves to UTC offset 0 (verified live: `TZ=etc/GMT-14
  node -e 'console.log(new Date().getTimezoneOffset())'` prints 0, same for
  the mutated `Etc/GMTx12`). Since the pre-fix bug can never manifest under
  UTC (the local day and UTC day never differ there), a mutated-but-real-
  looking TZ would make even the pre-fix property pass — exactly the
  regression this scenario exists to catch, so the mutant survived on the
  original step file.
- Fixed in commit `e7b2d60ed9`: the step now asserts the applied TZ's UTC
  offset is nonzero before spawning the property, failing loudly on any
  zone string that silently falls back to UTC. Re-ran full mutation after
  the fix: 2/2 killed. Manifest and mutation-stamp refreshed to match.
- Ran the repo-wide standing whole-tree guards (this pass touches
  `specs/pipeline/steps/`): all 22 `test/*Guard*.test.js` files, 211 tests,
  all green.
- No property-lane files added or changed by this pass (only the step
  handler that drives the existing property file); `test:properties` not
  run per this project's role-scoped test-lane list.
- CRAP/DRY: N/A — no `src/*.ts` production file touched by this parcel.
- No orphaned test/mutation processes at handoff (`pgrep` clean).
