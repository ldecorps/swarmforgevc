# QA pass — BL-1312 — 2026-09-05

Commit under review: `ab05840be5` (merge of documenter's `e6ae4e36ad` into
`swarmforge-QA`), carrying the full chain (`b99eaac73b` BL-1312 coder,
confirmed ancestor via `git merge-base --is-ancestor`).

## Verification

- **Independently reproduced the raw probe myself** (not just trusting
  the coder/hardener): ran `bl1312FixtureRootSignalProbeCli.js` directly
  against the fixed `socketFixtureRoot.js` in `idle` mode, sent it a real
  `SIGTERM` after the root existed — the directory was cleaned (`ls`
  reported no match). Confirms the fix works against the actual defect,
  not only the acceptance harness's own simulation of it.
- Read the fix directly: `installExitHook` now calls
  `onAbnormalExit(removeStragglers)` instead of a bare `process.on('exit',
  ...)` — routes through `fixtureReaper`'s existing, already-global
  SIGINT/SIGTERM coverage rather than inventing a second listener, exactly
  the direction the ticket asked for and explicitly warned against
  deviating from.
- `grep -c onAbnormalExit socketFixtureRoot.js` = 4;
  `grep -n "process.on('SIGTERM'\|process.on('SIGINT'"` = none — confirms
  no private signal listener was added (invariant 2).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1312-fixture-root-signal-cleanup.feature` — 4/4 (3
  Examples rows + the listener-count scenario), including the
  `SIGTERM | already` regression-guard row (the pre-existing accidental
  path that must keep passing).
- Secondary caller-side fix in `roleLifecycleParkUnneededSteps.js`: read
  the diff directly — `liveFakeBinDirs.add(dir)` moved 6 lines earlier,
  immediately after `mkSocketFixtureRoot` returns, closing the caller-side
  window the ticket names as secondary (the shared lib already covers
  these roots from birth via its own `trackedRoots`).
- Independently re-ran the two highest-risk named regression suites (out
  of ~104 real callers of the shared helper): `run_acceptance.sh
  specs/features/BL-324-per-role-lifecycle-park-unneeded-roles.feature`
  (11/11, the edited caller file) and `specs/features/BL-1305-fixture-agent-binary-is-the-stub.feature`
  (3/3, the other named offender from the original report) — both green.
- Read the hardener's evidence in full: independently reverted the fix to
  the pre-fix bare `process.on('exit', ...)` form and re-ran the
  acceptance feature themselves (1 pass / 3 fail, matching the coder's own
  claimed non-vacuity), then restored byte-identical — genuine non-vacuity
  proof, not merely asserted.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- `required_wiring`: `specs/pipeline/steps/bl1312FixtureRootSignalCleanupSteps.js`
  exports `registerSteps`.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty;
  no leaked `/tmp` fixture roots after my own manual probe or the test
  runs.

## Verdict

APPROVED — merging up and landing.
