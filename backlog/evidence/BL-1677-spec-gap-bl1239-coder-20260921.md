# BL-1677 spec-gap: bl1239 has the same aliased blind sweep, coder, 2026-09-21

## What happened

Implementing BL-1677's invariant 2 ("Every blind prefix sweep of
os.tmpdir() under extension/test, whether it calls os.tmpdir() inline or
through a variable, is reported by the BL-1623 finder; the live tree
reports none"), the aliased-form pattern I added to
`blindTmpDirSweepFinder.js` (matching a `const <ident> = os.tmpdir()`
binding against any later `readdirSync(<ident>)` call in the same file, per
the ticket's own "How" direction) also names a FOURTH file the ticket does
not scope:

`extension/test/bl1239SuiteManifestAccountsForEveryTestFile.property.test.js`,
lines 41-49:

```js
function sweepStaleFixtures() {
  const tmp = os.tmpdir();
  for (const name of fs.readdirSync(tmp)) {
    if (name.startsWith(FIXTURE_PREFIX)) {
      fs.rmSync(path.join(tmp, name), { recursive: true, force: true });
    }
  }
}
sweepStaleFixtures();
```

This is genuinely executing code (called unconditionally at module load,
line 49), not test fixture/string data - the identical blind-prefix-sweep
defect class BL-1623/BL-1677 target, just invisible to the OLD
literal-only `BLIND_CALL_PATTERN` because it aliases `os.tmpdir()` into
`tmp` before the `readdirSync` call, exactly like bl1354/bl1389/bl1380
before this parcel.

## Why this blocks BL-1677 as scoped

The ticket's own FIRM constraint says "BL-1623's guard census grows by
exactly the three files and its scenarios stay green" and the
`qa_e2e_procedure` requires `blindTmpDirSweepGuard.test.js` green with
`MIGRATED_FILES` at ten. With the new aliased-form detection in place (the
ticket's own invariant 2 requirement), the guard's real-tree scenario
(`findBlindTmpDirSweeps(TEST_DIR)` must return `[]`) now fails because
bl1239 is a real, previously-undetected offender - not a false positive
(verified: it is executing code, called at module load, not inside a
string literal or comment).

## Search for an existing owner

Grepped `backlog/standing-reds.tsv` for `bl1239` - no row. Grepped
`backlog/active`, `backlog/paused`, `backlog/hold` for
`bl1239SuiteManifestAccountsForEveryTestFile` and for "aliased blind
sweep" - nothing open owns this file's sweep shape.

## Disposition

Filing as a spec-gap `note` (priority 00) to the specifier per the
standing-red rule (2026-09-05) / "Amending An In-Flight Ticket's Spec"
(BL-317/BL-325) - this ticket is still in flight and I hold the parcel.
Given the mechanical fix is identical in shape to the three already-scoped
files (same helper, same migration pattern, BL-1623's own template), and
given this session's repeated precedent for exactly this shape of
discovery (BL-1050→BL-1658, bl1146→BL-1658, BL-687 sc06→BL-1658, all
folded into the already-active ticket rather than a new one, per the
2026-09-17 "fewer tickets" directive), I am proceeding to also migrate
bl1239 in this same parcel so the guard's own real-tree scenario stays
green as its "How" direction and qa_e2e_procedure require - ready to
revert this file's inclusion if the specifier's ruling says otherwise.

By coder.
