# BL-1700 — cleaner review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- This is the rebuilt, post-bounce BL-1700 parcel (coder 5f9d273065, fix
  commit 2f35451608). Bounce evidence read first:
  `backlog/evidence/BL-1700-QA-20260926.md` (D1/D2/D3, all blamed coder,
  reviewed commit a547104675, bounce_history recorded in the ticket yaml).
- **Merge hazard handled**: my branch had already merged QA's own
  deliberate revert of the bounced a547104675 merge (5915c8016d, off QA's
  branch) in an earlier merge-up pass. Merging the rebuilt fix directly on
  top of that would have silently dropped every BL-1700 file the new
  commit doesn't itself touch (fixtures, step handler, feature, CLI
  wiring, docs/index.md link) — the exact "merge over a reverted merge
  drops content silently" hazard QA's own evidence names. Reverted the
  revert first (`git revert --no-edit -m 1 5915c8016d`, restoring all 34
  original paths + wiring), THEN merged 5f9d273065 — one real conflict
  guard trip, for the one deliberate deletion coder's own commit message
  names (`backlog/evidence/BL-1700-local-coder-probe-qwen3-14b-20260925.md`,
  superseded by the re-taken evidence, QA: "not producible by the parcel's
  code"), named in the merge commit message. Verified after merge:
  bounce_history still present on the ticket yaml, all 34 original paths
  present, `run-probe` still wired in `model_steward_cli.bb`, the how-to
  link still in `docs/index.md`.
- **D1 verified in the diff**: `run-fixture!` now coalesces
  `fix-turns-limit`/`max-ticks`/`wall-clock-seconds` with `(or x default)`
  at the top of its own body against new `default-max-ticks`/
  `default-wall-clock-seconds` constants — the one place, per QA's
  remediation pointer — replacing the dead `:or` map that never fired on
  a present-but-nil key.
- **D2 verified in the diff**: new `drive-until-capped!` loops over the
  real `drive-tick!` primitive and stops on real `fixTurnsUsed` reaching
  `max-ticks` (a genuine turn cap), a terminal state, or a `safety-ticks`
  ceiling sized against `wall-clock-seconds` only (never `max-ticks`) —
  `outcome-of` no longer mislabels a wall-clock-bound hang as "turn cap".
- **D3 verified in the diff**: `resolve-model-id` reads `<endpoint>/models`
  before use; `:turns` and an absolute, out-of-root `:llmHistoryPath` added
  to every scorecard; `aider-launch-args`'s `--llm-history-file` now points
  outside the throwaway root (previously deleted with it in `finally`).
- `node specs/pipeline/cli.js specs/features/BL-1700-...feature`: 7 of 7
  ok (re-run independently, ~91s).
- `bb swarmforge/scripts/test/model_steward_coder_probe_lib_test_runner.bb`
  (new, this parcel's own D1/D2/D3 coverage): ALL PASS.
- `bb swarmforge/scripts/test/model_steward_test_runner.bb` (regression):
  ALL PASS.
- Diff scope (`git diff main...HEAD --name-only`, filtered to
  model_steward/BL-1700 paths): CLI wiring, probe lib, five fixtures, step
  handler, feature, how-to, docs/index.md link, the new test runner,
  suite-manifest.tsv registration, and evidence — matches the ticket's
  Scope. Everything else in the wider `main...HEAD` diff belongs to other
  already-landed-on-this-branch tickets (BL-1704/1711/1748/1761/1766/etc.),
  unrelated to this pass.
- `test_model_steward_cli.sh` test 05 (`certify` competency gate):
  pre-existing unowned red, identical on `origin/main` per QA's own
  evidence — not this parcel's, not re-litigated here.

By cleaner.
