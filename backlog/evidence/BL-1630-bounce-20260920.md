# BL-1630 — cleaner bounce, 2026-09-20

While cleaning up the parcel, `swarm_handoff.sh` refused the forward with
`CONTRACT_AMENDED_SINCE_BASE for BL-1630`: the specifier's 2026-09-20
ruling (commit f981c5a208, landed on origin/main after the coder's own
handoff) amended the ticket's FIRM constraint and its acceptance feature
while this parcel was already in flight at cleaner.

Merged main (de3fde5c75) and re-read the ruling
(`backlog/active/BL-1630-no-step-handler-does-work-at-module-load.yaml`
"Ruling 2026-09-20" note) and the amended feature
(`specs/features/BL-1630-no-step-handler-does-work-at-module-load.feature`,
scenario 02's `Then` line). Implementing it requires new guard behavior,
not a cleanup pass:

## D1

- **Failing command**: `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1630-no-step-handler-does-work-at-module-load.feature`
- **Commit hash**: 8c9a114237 (cleaner tip after merging main and fixing
  the unrelated D1-of-this-pass E2BIG defect, see the cleaner-pass
  evidence file dated the same day)
- **First error excerpt**:
  ```
  not ok 13 - the guard over the real tree passes and names an offender when one exists
  error: 'Scenario "the guard over the real tree passes and names an offender when one exists":
   no step handler matched "Then it reports every handler under the per-handler budget,
   or on its allowlist with an owning ticket, and the index load under 5 seconds"'
  ```
- **Failure class**: acceptance
- **Expected vs observed**: Expected the step handler's regex to bind the
  amended `Then` sentence (scenario 02, "or on its allowlist with an
  owning ticket"). Observed: `bl1630NoStepHandlerWorksAtModuleLoadSteps.js`
  still matches the PRE-amendment wording — it was written against the
  ticket's original acceptance text, before the ruling landed.

  The ruling requires three implementation changes the current commit
  does not have, per the ticket's own "Ruling 2026-09-20" note:
  1. The guard's `ALLOWLIST` reason strings must each name an owning
     ticket id (bl592 -> BL-1658); a reason with no ticket id is no
     longer an accepted allowlist entry.
  2. `bl1050CursorRunFailureLogSteps.js`'s entry is REMOVED outright —
     "timing variance" is not an accepted reason. Instead, a handler the
     sequential census reads over budget must be re-measured ALONE in a
     fresh child (BL-1633's confirm-a-pole-alone rule) before the guard
     names it as a violation; only a handler still over budget when
     confirmed alone is a real violation.
  3. The step handler's own `Then` regex (and its `ALLOWLIST` mirror,
     per the handler's own comment: "must apply the SAME allowlist to
     make the same true claim") follow the amended feature text.

  Item 2 is new decision logic in the guard (or its `checkHandlerBudgets`
  helper) — a genuinely new mechanism, not a behavior-preserving cleanup
  move. This is coder-domain work (implementing an acceptance criterion),
  outside what a cleaning pass owns per this role's "Does Not Own: do not
  introduce new behavior."

By cleaner.
