# BL-1657 — QA bounce, 2026-09-20

## D1

1. **Failing command**: `npm run test:properties` (from `extension/`)
2. **Commit**: 1dec7c3bc3 (QA worktree, merge of documenter 9d428ec0b4)
3. **First error excerpt**:
   ```
   FAIL  test/bl1444ArtDirectorTipGuardInvariants.property.test.js > property (invariant): hook mode judges only an unmerged art-director-side commit; a merge whose incoming parent is not on primary/art-director, or is already reachable from the landed main, is never judged, whatever it carries
   Error: Property failed after 1 tests
   { seed: -731360202, path: "0:0:0:0", endOnFailure: true }
   Counterexample: ["not-on-art-director",["extension/src/a.ts"]]

   Caused by: AssertionError: expected hook mode to skip (exit 0) for shape=not-on-art-director
   regardless of the paths it carries (extension/src/a.ts), got rc=1: Merge refused: check_art_director_tip.sh's
   resolved art-director branch swarmforge-art-director (from the swarmforge-<seat> convention (no roles.tsv row
   for art-director)) does not exist.
   ```
4. **Failure class**: behavior
5. **Expected vs observed**: expected the "not-on-art-director" shape to make the hook SKIP (exit 0), unjudged,
   per BL-1444 invariant 2 (unchanged by this ticket); observed a REFUSAL (exit 1) because
   `mkRepo()` in `extension/test/bl1444ArtDirectorTipGuardInvariants.property.test.js` still builds the
   pre-BL-1657 fixture shape (creates `primary/art-director` only, no `roles.tsv` row and no
   `swarmforge-art-director` branch), so the guard's new roster/convention resolution falls through to a
   branch that does not exist in this fixture and refuses instead of skipping.

**Blamed role**: coder. **Remediation pointer**: `extension/test/bl1444ArtDirectorTipGuardInvariants.property.test.js`,
`mkRepo()` (and any other fixture builder in that file) — migrate to the roster shape (a `roles.tsv` row for
`art-director` checked out on `swarmforge-art-director`, matching `bl1444ArtDirectorTipLandsSteps.js`'s own
migration already done in this parcel), per the ticket's own requirement: "every BL-1444 scenario stays green."
This file was missed; only the acceptance step handler (`bl1444ArtDirectorTipLandsSteps.js`) and the shell test
were migrated.

## Also checked, no defect found (Article 4.4 full inventory)

- `node specs/pipeline/cli.js specs/features/BL-1657-...feature`: 4/4 ok.
- `node specs/pipeline/cli.js specs/features/BL-1444-...feature`: 13/13 ok.
- `bash swarmforge/scripts/test/test_bl1444_art_director_tip_guard.sh`: ALL PASS.
- `swarmforge/scripts/check_art_director_tip.sh --tip d40a6fb21f`: `ART_DIRECTOR_TIP_OK` (exit 0), matching
  `qa_e2e_procedure`.
- `swarmforge/scripts/check_art_director_tip.sh --tip d40a6fb21f --branch no/such-branch`: refuses naming
  `no/such-branch (resolved from --branch)`, matching `qa_e2e_procedure`.
- `required_wiring`: `bl1657ArtDirectorGuardRosterBranchSteps.js`'s handler is discovered and ran (4/4 ok above)
  in the same commit as the renamed feature — satisfied.
- `pre_qa_gate.sh BL-1657 1dec7c3bc3`: OK (subject-only ancestry warnings on every worktree, no path overlap —
  benign, same shape QA has recorded before).
- `npm test` (extension/, run sequentially, not concurrently with the property lane): 635/635 files,
  10819/10819 tests passed.
- `npm run test:properties` (sequential re-run to rule out a concurrency artifact after an earlier concurrent
  run showed the same two failures plus a spurious unit-suite budget failure caused by running both suites at
  once — never repeated): same two failures below, confirming they are real, not concurrency noise.
- Second property-lane failure, **not attributable to this parcel**:
  `test/bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js` ("non-vacuity: a broken listedDir check
  would silently pass an offender... incremental require cost 210.6ms exceeds the 200ms budget") — this file
  belongs to BL-1630 (`extension/test/bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`, coder-authored
  per its own header), a separate active ticket currently mid-pipeline (already bounced twice by cleaner and once
  by QA on 2026-09-20 for an unrelated defect). It is not on `origin/main` and rode into this QA worktree only as
  BL-1630's own in-flight work, unrelated to BL-1657's own paths (`check_art_director_tip.sh`,
  `bl1657ArtDirectorGuardRosterBranchSteps.js`, the renamed feature). Owned by BL-1630, not an unowned red —
  no action taken here.
- No straggler test/mutation processes before or after.
- `qa-sibling-check.js status --ticket BL-1657`: VERIFY (no open deferral).

By QA.
