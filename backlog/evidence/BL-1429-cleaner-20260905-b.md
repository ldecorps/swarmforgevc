# BL-1429 — cleaner pass (redo after bounce), 2026-09-05

DRY fix applied (no behavior change) — real duplication found and
eliminated; the coder's re-fix (clearing-branch "which was actually
binding" comparison against the prior tick) is correct and complete.

## Defect found and fixed

`jscpd` found a self-clone within
`emitThrottleRecommendationStandingRed.test.js`: the "read the change
log's last JSONL entry" block was written out in full both in the
pre-existing recovery test and in the coder's new bounce-regression test
(both live in this file, which this ticket's history owns start to
finish). Extracted a local `readLastChangeLogEntry(root)` helper within
the same file; both tests now call it. No behavior change (identical
read/parse/last-element logic). Left the same pattern in the sibling,
pre-existing `emitThrottleRecommendationCli.test.js` (BL-432) and
`bl1429StandingRedThrottleFoldInvariants.property.test.js` untouched —
out of scope for a bounce-fix pass (an established, pre-existing
convention in files this ticket does not otherwise touch, not new
duplication this ticket introduced).

`jscpd` re-run on the touched/new files after the fix: 0 clones (was 1
clone, 9 duplicated lines / 65 duplicated tokens).

## Verified the bounce's own remedy directly

Traced `describeChangeReason`'s new clearing branch by hand against the
bounce's own counterexample and the boundary cases:
- prior severe rework (cap 0) + prior standing-red count (cap 1, present
  but never binding): `priorStandingCap(1) <= priorReworkCap(0)` is false
  → falls through to the generic "rework diagnosis cleared" wording —
  exactly the bounce's own expected fix, confirmed by the new test.
- prior degraded rework (cap 1) + prior standing-red (cap 1, tie): names
  standing-red — matches the ACTIVE branch's own documented tie
  convention, kept symmetric.
- prior standing-red only (no rework signal, `priorReworkCap === null`):
  still names standing-red — the pre-BL-1429-bounce behavior for the
  isolated case is unchanged.
- prior severe rework only (no standing-red block at all,
  `priorStandingCap === null`): falls through to the generic wording —
  the original pre-BL-1429 shape, unchanged.

## What else was checked

- Confirmed every other file (`standingRedSignal.ts`, its unit tests, the
  property test, the step handler, `swarmforge.conf`, and the DRY
  extraction `standingRedRegisterFixture.js`) is restored byte-for-byte
  from the prior (already-reviewed) commits, not reauthored — this pass
  is scoped to the one clearing-branch fix plus the new regression test.
- Confirmed neither BL-1287's nor any other ticket's files were
  resurrected or lost by this merge.
- Mutation-site count on both changed TS files: `standingRedSignal.ts` 94
  (unchanged), `emit-throttle-recommendation.ts` 96 (was 82, still well
  within the 100 threshold) — no split needed.
- `npm run compile` clean.
- Confirmed neither `backlog/standing-reds.tsv` nor
  `swarmforge/scripts/property_suite_standing_allowlist.tsv` has a row
  owned by this ticket.

## Verification (all re-run in this worktree, all green, after the DRY fix)

| check | result |
|---|---|
| `npx vitest run test/standingRedSignal.test.js test/emitThrottleRecommendationStandingRed.test.js test/emitThrottleRecommendationCli.test.js` | 36/36 pass (11 + 9 + 16) |
| `npx vitest run --config vitest.properties.config.mjs test/bl1429StandingRedThrottleFoldInvariants.property.test.js` | 4/4 pass |
| `node specs/pipeline/cli.js specs/features/BL-1429-standing-reds-throttle-intake.feature` | 8/8 |
| `node specs/pipeline/cli.js specs/features/BL-432-auto-tune-intake-throttle.feature` (regression) | 5/5 pass, unaffected |
| `npm run compile` | clean |
| `node extension/out/tools/mutation-site-count.js <both changed ts files>` | 94, 96 — both within |
| `npx jscpd` on touched/new files | 0 clones (was 1, now fixed) |

Forwarding the DRY-fixed candidate to architect.
