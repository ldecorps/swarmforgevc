# BL-1686 — hardener review pass, 2026-09-21

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

Confirmed my own BL-1516 section 08 (the git-common-dir isolation test)
survives the reordering and the new `prove_root` early-return
unaffected - it drives `prove_root` directly against an already-real
`git worktree add` fixture, never through `mk_fixture`'s new call
position, so it never touches the new `return 0` branch.

Hand-mutated the safety-critical half of the new
`sweep_stale_prefix_roots` helper: forced `kill -0` to always report
"not alive" (`if false; then ... fi`, `swarmforge/scripts/test/lib/tmp_cleanup.sh`) -
the exact failure mode that would delete a genuinely LIVE sibling's
TMPROOT, the incident class this ticket exists to prevent. The clause-2
property test caught it immediately and correctly: "expected the
live-owned root ... to survive (liveCount=1 deadCount=0
useZombie=false)". Restored, re-verified green (2/2).

- All five census shell tests green:
  `test_bl1378_expedite_close_guard.sh` (including my own section 08 and
  the new section 09), `test_bl1374_sync_merge_passengers.sh`,
  `test_suite_baseline_cli.sh`, `test_bl1376_expedite_branch_handover.sh`,
  `bl1363_close_ticket_property_runner.sh`.
- Re-ran the concurrency proof myself (qa_e2e_procedure step 4): two
  instances of `test_bl1378_expedite_close_guard.sh` launched
  concurrently in the background - both exited 0, both fully green, no
  interference.
- `node specs/pipeline/cli.js` on BL-1686's own feature: 7/7.
- `grep -ln 'rm -rf "${TMPDIR:-/tmp}/${PREFIX}"' swarmforge/scripts/test/*.sh`:
  empty.
- All 21 standing whole-tree guard files (210 tests) green.
- `npm run compile`: clean.
- No leftover processes or fixtures.
