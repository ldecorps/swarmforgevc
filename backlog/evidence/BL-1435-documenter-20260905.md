# BL-1435 documenter review pass — 2026-09-05

Reviewed the hardened commit (`a71ea04695`) and the coder/cleaner/architect/
hardener trail for BL-1435 (the guard now recognizes the `rev-parse
--show-toplevel` idiom as a live-root binding, alongside `path.join`).

Checklist:
- `required_wiring` anchors present: `liveRepoDerivationGuard.js::show-toplevel`
  (`REV_PARSE_TOPLEVEL_SRC`, folded into `LIVE_ROOT_BINDING_RE` and
  `LIVE_ROOT_INLINE_SRC`) and
  `specs/pipeline/steps/bl1435RevParseRootIsALiveReadSteps.js::registerSteps`
  — both confirmed by grep and by the acceptance run below.
- This is a documented mechanism (`docs/reference/BL-1038-pinned-repo-fixture-and-live-derivation-guard.md`)
  whose "How It Works" / "The guard" section described only the
  `path.join` idiom — a real gap now that a second idiom is recognized by
  the same detection. Updated that section to describe both idioms, the
  `__dirname` tell shared by each, and the before/after vacuity this
  ticket fixes; bumped the doc's own "Last Updated" field in the same
  commit as the content change. The file's exemption table needed no
  change — BL-1435 added zero new exemption markers (confirmed via the
  coder's own file-by-file disposition table and by `git show a71ea04695`
  touching no `.test.js` file besides the guard's own).
- Added a dated top entry to `docs/reference/Specification.MD` per this
  project's changelog convention, cross-linking back to the BL-1038 entry
  for the full mechanism.
- No `docs/index.md` change needed — no new page was authored, only an
  existing reference page and the changelog were updated.
- No registered diagram trigger fires (`architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`) — this changes a unit-test
  guard's detection regex, not the extension host/webview boundary, the
  tmux substrate, `.swarmforge/` state layout, the pipeline topology, the
  handoff file lifecycle, or front-desk routing.
- No production code, test, or requirements change made by this pass —
  documentation only. Re-ran `npx vitest run test/liveRepoDerivationGuard.test.js`
  (24/24 pass) after the doc edit to confirm the worktree is otherwise
  untouched.

Result: NONE — no defect found; one reference doc updated for a genuine
mechanism change, one changelog entry added.
