# QA pass — BL-681, BL-762 (2026-08-08, re-entry after prior bounce)

## Context

Re-entry after the prior QA bounce (`backlog/evidence/BL-681-BL-762-qa-bounce-20260808.md`,
commit `b37451751c`) for two defects:

- **D1** (BL-762, blamed coder): `finish_shift_lib.sh` referenced the retired
  `onboarding-facilitator-supervisor.pid` name without being added to
  `extension/test/onboarderResidualAllowlist.js`'s `ALLOWED_EXACT_PATHS`.
- **D2** (BL-681, blamed coder): the ticket's own `acceptance:` YAML field
  still pointed at the pre-promotion `.feature.draft` path; the existing fix
  (`a60906ea`) had not been merged into the lineage that reached QA.

Received via documenter's `git_handoff` for BL-681 (task BL-681, commit
`9a6bb10bab`, `merge_and_process documenter 9a6bb10bab`), merged clean into
this worktree (merge commit on top of prior QA tip `f8d22732`). A companion
`git_handoff` for BL-762 (task BL-762, commit `f29d54bd5b`, one more
documenter commit — its own re-pass evidence note, no functional delta)
sits in this pane's inbox and is processed as its own parcel per Article
2.6; this pass's verification run covers its content as well since
`f29d54bd5b` is an ancestor-superset of `9a6bb10bab` with no code change.

`qa-sibling-check.js status --ticket BL-681` → `VERIFY BL-681` (exit 0, no
open deferral) before this pass began.

## Fix verification (D1, D2)

- D1: `extension/test/onboarderResidualAllowlist.js:28` lists
  `'swarmforge/scripts/finish_shift_lib.sh'`. `git merge-base --is-ancestor
  64ed486874 HEAD` → true.
- D2: `backlog/active/BL-681-consolidation-never-drops-a-human-sentence.yaml`
  `acceptance:` now reads
  `specs/features/BL-681-consolidation-never-drops-a-human-sentence.feature`,
  which exists on disk. `git merge-base --is-ancestor a60906ea8a HEAD` → true.

Both defect-fix commits confirmed ancestors of the commit under review, not
merely "ancestor of HEAD" (the BL-336 trap this prompt warns against) — this
ticket's own bounce-fix commits were checked directly.

## Full verification pass

- **Orphan process check (before)**: `pgrep -fl 'node --test|stryker'` — clean.
- **Unit suite**: `cd extension && npm test` — 408/411 files, 7286/7292 tests
  pass. 6 failures, all in `dependencyGateCli{CleanAndViolations,
  ReportsAndScope,StorageGlobals}.test.js`, all failing identically with
  `Your node version (20.20.2) is not supported. dependency-cruiser requires
  ^22||^24||>=26` — pre-existing host Node-version mismatch, unrelated to and
  pre-dating this batch (same finding as the prior QA bounce and hardener's
  re-pass). Confirmed via `git diff --name-only b2cd357fcb 9a6bb10bab --
  extension/ swarmforge/` → only `extension/test/onboarderResidualAllowlist.js`
  changed in this batch's delta; no dependency-cruiser file touched.
- **Property suite**: `cd extension && npm run test:properties` — 56/59
  files, 179/185 tests pass. 6 failures across 3 files:
  `bl760DuplicateChainGuard`, `bl787NamedTunnelInvariants`,
  `bl797MutationGateProbeCrashFallback` — all timeouts/assertion flakes under
  severe host load (`uptime`: load averages 38.92 58.97 41.90 at time of
  run) or (bl787) live `cloudflared`/Cloudflare-edge dependency, matching the
  exact three files already noted as environmental in the prior QA bounce
  evidence and the hardener's re-pass. Confirmed none of the three files
  reference `finish_shift`, `prompt_engine`, `consolidation`, `BL-681`, or
  `BL-762` (`grep -l` — no match), and this batch's only changed file
  (`onboarderResidualAllowlist.js`) is untouched by any of them.
- **Acceptance — BL-681**:
  `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-681-consolidation-never-drops-a-human-sentence.feature`
  — 3/3 pass.
- **Acceptance — BL-762**:
  `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-762-finish-shift-leaves-the-phone-path-up.feature`
  — 14/14 pass.
- **Orphan process check (after)**: `pgrep -fl 'node --test|stryker'` —
  clean. `pgrep -afl tmux` — only the live swarm's own repo-socket
  `swarmforge-coder` session, no leaked fixture servers.

## Wiring verification (not merely unit-tested in isolation)

- **BL-681**: the clause is live in
  `swarmforge/constitution/articles/05_amendments.md` ("A consolidation
  never drops a human sentence..."), and `swarmforge/roles/specifier.prompt`
  (lines ~307-312) cites back to it as its binding source ("Article 5.3...
  ratified 2026-07-26, BL-681") rather than restating it standalone —
  reachable from the role that exercises the authority, matching the
  ticket's own acceptance scenario 3.
- **BL-762**: `./finish-shift` exists at repo root (matching the ticket's
  scope constraint), sources `swarmforge/scripts/finish_shift_lib.sh`, which
  sources `swarmforge/scripts/lifecycle_matrix.sh` (the single keep/kill
  table both verbs read) and `stop_ancillary_services.sh` (composing
  existing per-component stop functions — confirmed no forked second
  teardown implementation). `lifecycle_matrix.sh`'s
  `LIFECYCLE_MATRIX_ENTRIES` matches the ticket's table exactly
  (babysitterd/operator-runtime/onboarder: stop for both verbs; front-desk/
  tunnels: keep for finish-shift, stop for stop-swarm), and
  `LIFECYCLE_SEAT_REVIVING_COMPONENTS=(babysitterd)` encodes the second
  invariant. `stop_ancillary_services.sh`'s header comment no longer claims
  the front desk bridge is "always-on" — rewritten to describe the actual
  compose relationship with `finish_shift_lib.sh` (BL-762's requested
  correction). No scheduled/timer caller exists anywhere (`crontab -l`
  clean; no root-level script references `finish-shift`) — confirms scenario
  06 stayed parked as `.feature.draft` and was not shipped, consistent with
  the standing veto on scheduled/timed swarm stop
  (`.swarmforge/operator/continuous-shifts.json` `scheduledStop: false`).

## Disposition

Both defects verified fixed, all gates green, both tickets' deliverables
confirmed wired into their real callers. Approved.

By QA.
