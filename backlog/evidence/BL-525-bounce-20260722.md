# BL-525 QA bounce — 20260722

## Failing command
```
git show 7c5c9ecb21:extension/src/bridge/bridgeServer.ts | md5sum
git show origin/main:extension/src/bridge/bridgeServer.ts | md5sum
```
(and the same comparison for pausedPagerUiHtml.ts, pipelineGridLive.ts,
swarmStopper.ts, pipelineBoard.ts, conciergeTick.ts, llmCostLedger.ts)

## Commit hash
Bounced commit: `7c5c9ecb21b0c38b99d3f66b2da27ce58177a712`
(documenter's forwarded handoff for BL-525, "Document ModelFactory: add
runbook for assign and apply workflows")

## First error excerpt
Merging 7c5c9ecb21 into swarmforge-QA (merge commit 50a7b7d1f, since
reverted at 78fabf968) silently reverted 7 files to a STALE pre-existing
state that predates already-shipped work on `origin/main`:

- extension/src/bridge/bridgeServer.ts — removes the `/paused-pager/approve`
  route (`isPausedPagerApproveRoute`, `recordApprovalReply` import), removes
  `cache-control`/`pragma` no-cache headers on Mini App HTML responses.
- extension/src/bridge/pausedPagerUiHtml.ts — reverted (approve UI removed).
- extension/src/bridge/pipelineGridLive.ts — reverted.
- extension/src/swarm/swarmStopper.ts — reverted.
- extension/src/concierge/pipelineBoard.ts — removes the entire BL-559
  "collapsed epics" feature (PipelineBoardCollapsedEpicEntry,
  isEpicTrackerPausedItem, buildCollapsedEpicEntries,
  formatCollapsedEpicLine(Html), linksFromCollapsedEpics, renderParkedSection
  epic branch) — ~192 lines removed.
- extension/src/concierge/conciergeTick.ts — reverted.
- extension/src/metrics/llmCostLedger.ts — reverted (BL-551 in-flight work).

Associated tests were deleted/shrunk alongside the reverted code, so the
unit suite stays green despite the regression:
- extension/test/pausedPagerBridge.test.js — deleted entirely (104 lines).
- extension/test/bridgeServer.test.js — shrunk by 36 lines (approve-route
  coverage gone).
- extension/test/pipelineBoard.test.js — shrunk by ~120 lines (collapsed-
  epics coverage gone).
- extension/test/stop.test.js — shrunk by 18 lines.
- specs/features/BL-559-pipeline-board-collapsed-epics.feature — DELETED.

Verified each reverted file's content at documenter's commit does NOT match
`origin/main`, while my own QA branch tip (pre-merge, `b86a1a978`) DOES
match `origin/main` exactly for all 7 files (byte-identical, md5 compared).
So the regression is specific to the BL-525 coder/cleaner/architect/hardener/
documenter branch, not a QA-side staleness issue.

This matches the known "stale revert-resurrection" contamination pattern:
the hardener stage already caught and partially fixed two instances of it
on this same branch (commits `5d774982d` "Restore Swarm Live Screen rename
lost to stale revert-resurrection" and `dcfea4418` "Restore live-screen
Telegram helpers lost to stale revert-resurrection") but missed these 7
files / their tests / the BL-559 feature file.

## Failure class
behavior (regression — unrelated shipped functionality reverted; ticket
scope violation per "An Approval Authorizes Only Its Ticket's Work")

## Expected vs observed
Expected: BL-525's parcel touches only ModelFactory Slice-1 files (coder
implementation, docs runbook) plus whatever the specifier explicitly pinned;
all previously-shipped functionality (paused-pager approve route, BL-559
collapsed epics, BL-551 cost-ledger in-flight work) stays intact.
Observed: merging the parcel reverts 7 unrelated production files (and
deletes/shrinks their tests, plus the BL-559 feature file) back to a stale
pre-shipment state — a silent regression that unit tests do not catch
because the tests for the reverted behavior were removed along with it.

## Action taken
Bounced merge commit `50a7b7d1f` reverted out of swarmforge-QA via
`git revert -m 1 50a7b7d1f` (commit `78fabf968`) before this bounce, per
bounce-hygiene rules — swarmforge-QA's tree is back to matching
`origin/main` for all 7 affected files.

## Unrelated, NOT part of this bounce
The pre-existing property-test failure in
extension/test/pipelineBoard.property.test.js ("the included links are
always an in-order PREFIX...") is NOT caused by this parcel — it predates
BL-525 entirely (introduced by commit `d63e80320`, "Cap pipeline board
parked list at 10 by priority and shorten link HTML", which changed
`pipelineBoardLinkLine`'s output format from `"${id}: <a...>"` to a bare
`<a...>${label}</a>` anchor without updating the property test's
`${id}:` substring assertions). This bug is already present on QA's own
branch tip independent of this merge and needs its own ticket — it does not
block or explain this bounce.
