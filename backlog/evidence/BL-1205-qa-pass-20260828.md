# BL-1205 — QA pass — 20260828

**Received:** `merge_and_process documenter aeaa486230` (handoff
`00_20260828T021150Z_000880_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `aeaa48623...`
**QA merge commit:** `a49937a5d0cf2da60e9f75aeef0b5631241bdc4a`

Severity `critical` — containment gate for the live 2026-08-27 tree-collapse
incident (BL-1213's sibling in the same thread: BL-1213 catches a rolled-
back path per accepted ticket, this ticket catches a mass deletion on ANY
hop, ticket or no ticket). One bounce in flight (architect D1, fixture
leak), re-fixed and re-verified before reaching QA.

## Merge

Clean, no conflicts (`git merge --no-ff aeaa486230`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor e7f0880e9 HEAD` (coder, original) — CONFIRMED
- `git merge-base --is-ancestor 0a43319fd HEAD` (coder, D1 re-fix) — CONFIRMED
- `git merge-base --is-ancestor 2815d0788 HEAD` (cleaner, round 2) — CONFIRMED
- `git merge-base --is-ancestor 3d4ce65ae HEAD` (architect, round 2) — CONFIRMED
- `git merge-base --is-ancestor df2e274ba HEAD` (hardener) — CONFIRMED
- `git merge-base --is-ancestor aeaa48623 HEAD` (documenter, received tip) — CONFIRMED

## Wiring verification (required_wiring, all three rows)

1. `swarm_handoff.bb:21` loads `tree_collapse_guard_lib.bb`.
2. `tree-collapse-guard-lib/refusal-message` reaches the refusal chain
   (line 428) alongside the other four send-time gates.
3. `specs/pipeline/steps/index.js:820` registers
   `bl1205HandoffRefusesAMassDeletionForwardSteps` — confirmed present.

## Design review (independent)

- Gated only on `(= "git_handoff" type)` and `(seq recipients)` — **no**
  ticket-id/task-name requirement, matching invariant 1 ("no hop is
  exempt, no ticket id required") exactly; this is the deliberate
  difference from BL-1213/pre-QA gate, which are ticket-scoped.
- Checks EVERY named recipient independently (`findings-for-git-handoff`
  reduces over `recipients`) — a finding for any one recipient blocks the
  whole send.
- Real `git merge-tree --write-tree` simulation, never a diff-based guess
  — answers "what will the receiver's tree look like" directly, per the
  ticket's own "How" section.
- Report-only: `tree_collapse_guard_lib.bb` contains no git write of any
  kind (grepped: only `ls-tree`, `merge-tree --write-tree`, both read-only
  simulations) — matches invariant 2.
- Fail-open on unreadable facts (no roles.tsv entry, unreadable branch,
  failed merge-tree simulation) — each produces a `:warning`, never a
  `:finding`, matching invariant 3 and the human-approved posture in
  `approval_context`.
- Threshold matches the ticket's suggested default exactly: smaller of 5%
  of recipient's own path count and a flat 100-path cap.

## Verification inventory

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1205` | `VERIFY BL-1205` — no open deferral |
| Compile | `npm run compile` | PASS, clean (re-run after this merge) |
| `tree_collapse_guard_lib_test_runner.bb` | `bb ...` | ALL PASS |
| `bl1205_tree_collapse_guard_property_runner.bb` | `bb ...` | 2000 runs, ALL PROPERTIES HOLD |
| Acceptance | `run_acceptance.sh specs/features/BL-1205-...feature` | 9/9 PASS — mass-deletion refused+named-count, small-deletion allowed, 5-way Scenario Outline proving every recipient role is checked (covers "QA and hardener in turn" from qa_e2e_procedure), unreadable-branch warns-and-sends, note-type exemption |
| BL-113 Gherkin mutation | manifest in feature file | 4/5 killed on the recipient-parameterized outline; the 1 survivor (`"QA"->"qA"` case mutation) independently re-derived as a genuine BL-234 code-level equivalent — the guard's dispatch logic never branches on recipient-string case, confirmed by reading `tree_collapse_guard_lib.bb`'s actual code, not just its comments |
| Fixture hygiene | hardener's independent re-verification | `rm -f /tmp/bl1205-tree-collapse-*` then re-ran acceptance; 0 stragglers after |
| Full unit suite (before/after this merge) | `npm run test` (extension/) | Standing red, explained in full: BL-1204's bundled content (unrelated sibling ticket riding the same architect-worktree history, not BL-1205's own diff — BL-1205 touches zero `extension/src`/`extension/test` files) adds 4 new passing tests plus one already-known flake (`telegramCursorOperatorExec.test.js > BL-698: ambulance engage and release`) that toggles pass/fail across my two runs depending on ambient backlog fixture state (a ticket needing to sit in `active/` vs `paused/`) — independently diagnosed with byte-identical-content proof by BOTH architect (`BL-1204-architect-bounce-20260828.md`) and hardener (`BL-1205-hardener-pass-20260828.md`) before this reached me. Grepped backlog for an existing ticket on this specific flake: none found — genuinely untracked, but it is BL-1204's territory (not touched by BL-1205's own diff) and already has two independent evidence trails on record for whoever verifies BL-1204 next. Not a BL-1205 regression. |
| Property suite | `npm run test:properties` | 17 failed / 673 passed (690) — same standing-red family as this session's BL-1198/1213 passes (one fewer than the 18/690 baseline; no new failures, consistent with ambient flakiness, not a regression) |
| Docs | grep | `docs/how-to/BL-1205-tree-collapse-guard.md` present, linked from `docs/index.md`; `docs/reference/Specification.MD` top entry updated; `swarmforge/handoff-protocol.md` gains a section |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker'` | none, before and after |

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1205's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1205 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1205 active → done).

By QA.
