# BL-1308 — cleaner pass, 2026-08-31

Task: `BL-1308-an-unlanded-sibling-on-a-second-parent-is-invisible`
Received: coder's `f3166a3fd5`.

## Verdict: NONE — no defect found, no cleanup needed

Reviewed the full diff against Cleanup Order and Architecture Rules:

- `swarmforge/scripts/land_step_lib.bb`: the fix is a single flag removal
  (`ancestry-commits` drops `--first-parent`) plus a docstring explaining why.
  `own-commit-changed-paths` / `task-tagged-changed-paths` are untouched, as
  the ticket requires. No duplication, no structure change warranted — this
  is a one-line behavioral fix in an already-well-factored function.
- `swarmforge/scripts/test/land_step_lib_test_runner.bb`: three new
  `with-fixture` cases. Their setup (mark origin, branch, commit sibling,
  commit own work, forward-merge) repeats the pattern already used
  throughout this test file — consistent with existing style, not new
  duplication worth extracting.
- `specs/pipeline/steps/bl1308SiblingDetectorCoversReplaySteps.js`: step
  handler registered in `specs/pipeline/steps/index.js` in the same commit
  (verified: `grep -n bl1308 specs/pipeline/steps/index.js` — line 905).
  Scenario Outline validates `<position>` against explicit `KNOWN_VALUES`
  (`POSITIONS`), per house rule.
- Babashka/`.bb` files: no mutation/CRAP/DRY tooling wired for this
  language (Article, Startup Tools) — gated by unit-test suite only.
- No TypeScript/`out/**/*.js` changed by this parcel, so BL-485's
  mutation-site-count check does not apply (nothing to count).

## Checks run

| Command | Result |
|---|---|
| `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` | ALL PASS |
| `run_acceptance.sh specs/features/BL-1308-…feature` | 5/5 pass |
| `grep -n bl1308 specs/pipeline/steps/index.js` | registered, line 905 |

No functional change made by cleaner; forwarding coder's work unmodified
to architect.

By cleaner.
