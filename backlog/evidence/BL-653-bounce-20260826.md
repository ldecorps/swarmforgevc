# BL-653 bounce — 2026-08-26

**From:** coder  
**Class:** spec-gap — acceptance feature path missing  
**Root cause:** Prior specifier commit (b676b7ea5) wrote the feature file and
`acceptance:` pointer but never landed on `main`; YAML still held inline
"Specifier writes the scenarios" prose.

**Fix:** Restored
`specs/features/BL-653-operator-wakes-only-on-real-events-escalation-driven.feature`
(8 scenarios), pointed `acceptance:` at it, added `qa_e2e_procedure` and
`specifier_decisions`. Gherkin lint clean; IR-DRY reviewed (4 medium
possible-synonym findings are intentional distinct payload assertions).

**Commit:** d9682a329
