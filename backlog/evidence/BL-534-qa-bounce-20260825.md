# BL-534 — QA complete review inventory (Article 4.4) — 20260825

- **Ticket**: BL-534 — thin-main CRAP-visible CLI gate (`type: feature`)
- **Parcel**: `00_20260825T100739Z_000584_from_documenter_to_QA_for_QA`
  (documenter tip `7714fb1298`; also merged descendant `1236b59dae` which
  extended `abandoned_commits` but still leaves five stranded SHAs)
- **Verified at**: `a987af3049` (QA merge of `1236b59dae`)
- **Reviewed by**: QA, 2026-08-25

## Verdict

**BOUNCE — inventory items: D1 (one item).**

Every other gate this pass owns was run and passed. The thin-main gate
implementation, tests, docs, wiring, and acceptance are complete; the single
defect is an incomplete `abandoned_commits` list that leaves `pre_qa_gate`
red.

## Inventory

### D1 — `pre_qa_gate.sh` fails ancestry: five BL-534 commits stranded undeclared

- **Failure class**: `integration`
- **Blamed role**: `documenter`
- **Failing command**:
  `./swarmforge/scripts/pre_qa_gate.sh BL-534-thin-main-crap-visible-cli-gate a987af3049`
  (also fails citing tip `1236b59dae` / `7714fb1298`)
- **Commit tested**: `a987af3049`
- **First error excerpt**:
  ```
  PRE_QA_GATE_FAIL ancestry BL-534 43679d4523 stranded on swarmforge-documenter
  PRE_QA_GATE_FAIL ancestry BL-534 fc8029268f stranded on swarmforge-documenter
  PRE_QA_GATE_FAIL ancestry BL-534 f7677bfb39 stranded on swarmforge-documenter
  PRE_QA_GATE_FAIL ancestry BL-534 99946d4c24 stranded on swarmforge-documenter
  PRE_QA_GATE_FAIL ancestry BL-534 face5a1513 stranded on swarmforge-documenter
  ```
- **Expected vs observed**: expected `OK`; observed five ancestry FAILs.
  Subject-only warnings for `dd1bf2cc69` / `144c08a150` are non-blocking.

**Remediation pointer**:
`backlog/paused/BL-534-thin-main-crap-visible-cli-gate.yaml` →
`abandoned_commits:` — append (FLOW list, no inline comments; rationale in
`notes:`):

- `43679d4523` — "BL-534: tip armed; re-route on next open slot"
- `fc8029268f` — "BL-534: record coordinator ACK — paused until parcel merges arm."
- `f7677bfb39` — "BL-534: arm on main; re-route after tip merges 918d6d6f3+"
- `99946d4c24` — "BL-534: confirm acceptance feature already on main for architect tip."
- `face5a1513` — "BL-534: record architect tip missing acceptance feature"

Keep the five already listed (`29dc3d83f8`, `918d6d6f3f`, `04b4b01b97`,
`84d4f9f041`, `12d1eff3e4`). Re-run
`./swarmforge/scripts/pre_qa_gate.sh BL-534-thin-main-crap-visible-cli-gate <tip>`
until `OK`, then re-forward to QA.

These commits only touch the paused ticket YAML (specifier/coordinator
notes); product content is already on the rematch tip — abandon, do not
merge (BL-506 / BL-972).

## Gates run this pass (non-D1)

| Gate | Result |
|---|---|
| Sibling | `VERIFY BL-534` |
| Hitchhike | PASS (empty vs `origin/main`) |
| Compile | PASS |
| Unit | PASS — thinMainGate + CLI **49/49** |
| Properties | PASS — thinMainGate **2/2** |
| Acceptance | PASS — BL-534 feature **4/4** |
| `npm run thin-main-gate` | PASS (exit 0) |
| Dogfood parcel on `thin-main-gate.ts` | PASS (exit 0) |
| qa_e2e fat / thin / out-of-scope fixtures | PASS (exit 1 / 0 / 0) |
| Wiring (`package.json`, steps index, docs) | PASS |
| Hotfix stamp-off | PASS — pack MATCH; board same; BL-1113 **9/9** |
| Orphans | NONE |
| pre_qa_gate | **FAIL — D1** |

By QA.
