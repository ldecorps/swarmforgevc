# BL-1364 land note — land_step_cli.bb LAND_ESCALATE, hand-built replay, 2026-09-05

## Verification (before landing)

Full independent QA pass on the merged parcel (`f97bce44d4`, documenter
`bdfdd4ccec` merged into the QA worktree): `npm run compile` clean; unit
suite 584/600 files green (16 pre-existing standing-red files confirmed
unrelated by grep — no BL-1364 file imported by any of them — and cross-
referenced against already-tracked tickets, one of which, the mkTmpDir/
mkProcessTmpDir migration the hardener flagged, is already minted as
**BL-1410** on `main`); `turnProfileProducer.test.js`/`transcriptWalker.test.js`/
`runTurnProfileProducer.test.js` 33/33; property suite flaky under load
(two consecutive full runs produced two different failure sets — 26/12/5
then 18/4/3 — classic contention signature, host load average 13-14 from
concurrent hardener Stryker + coder Vitest runs in sibling worktrees); this
ticket's own `bl1364TurnProfileSeriesInvariants.property.test.js` clean
4/4 both times, isolated and in the full run. Acceptance
`BL-1364-...feature` 8/8, `BL-664-...feature` regression 11/11. All three
`required_wiring` anchors confirmed live by direct grep (not just cited).
Live-repo run (qa_e2e steps 2-5): RECORDED then UPDATED with one row
(idempotent), category set matches `transcriptWalker`'s
`INTERVAL_CATEGORIES` exactly, no leaked `aps-bl1364-*` fixture (hardener's
fixture-leak fix confirmed). Documenter's "no diagram edit" verdict
(architecture.mmd's per-ticket comment log stopped being maintained after
2026-08-27) matches the identical, already-QA-approved reasoning on three
other tickets landed this same session (BL-1400, BL-1353, BL-1275) — not
a fresh omission, established precedent. Cleaned up my own scratch
telemetry-store artifact after the live-run check (gitignored,
`.swarmforge/telemetry/`).

## Land

`land_step_cli.bb BL-1364-the-mechanical-share-of-a-turn-is-readable
f97bce44d4` returned **LAND_ESCALATE**: "could not read
`backlog/active/BL-1403-the-merge-deletion-guard-never-reports-a-move-and-
never-refuses-unexemptably.yaml`'s attribution". Same structural class
already adjudicated and repeatedly worked around this session
(`BL-1370-land-note-20260905.md`, `BL-1400-land-note-20260905.md`,
`BL-1401-land-note-20260905.md`, `qa-worktree-reconcile-blocked-bl1403-
20260904.md`): BL-1403 is itself the ticket for "the merge-deletion guard
never refuses unexemptably" — its own git history is exactly the kind of
unattributed-path tangle that guard mishandles, so an attribution walk
that has to read BL-1403's own yaml across the entangled range chokes on
the thing BL-1403 exists to fix. Appending this instance here rather than
sending a fresh priority-`00` escalation to the specifier — BL-1403 is
minted, paused, and already the recorded remedy for this exact class.

Per this session's established recipe, hand-built the tip-pure replay
instead of retrying the tool:
- `task_scope_gate_cli.bb BL-1364-... f97bce44d4` (the narrower, single-
  commit scope check, not the full entangled walk) returned `OK` before
  I started, confirming each of this ticket's own pipeline commits was
  already scope-gated at commit time (BL-1192).
- Diffed `c7a5bda385..bdfdd4ccec` (the coder's pre-work base through the
  documenter's final commit) restricted to the 19 paths every stage's own
  evidence file names as this ticket's own (verified `167d7d5ab5`, the
  specifier's invariant-2 amendment, was already on `origin/main`
  separately — not part of the replay).
- Applied that patch with `git apply --3way` in a throwaway
  `git worktree add --detach <tmp> origin/main`. One conflict, in the
  shared `docs/reference/Specification.MD` append-only changelog (both
  sides wanted the top slot); resolved by keeping both entries, BL-1364's
  new one first, "Prior entry —" then BL-1275's (unchanged).
- Confirmed every other file in the replay tree byte-identical to the
  already-reviewed QA worktree content before committing (diff -q, all
  MATCH).
- Symlinked `extension/node_modules` from the QA worktree (identical
  `package-lock.json`, confirmed) rather than a fresh `npm install`, and
  re-ran compile, this ticket's unit/property tests, and both acceptance
  features against the replay tree itself (not just the QA worktree) —
  all green, same numbers as above. Removed the symlink before committing.
- Committed (`c645686400`) directly onto `origin/main`'s tip
  (`2c67608557`), `git diff --stat origin/main HEAD` confirmed exactly the
  19 intended paths and nothing else (BL-1192/BL-1296 near-miss
  discipline). `task_scope_gate_cli.bb BL-1364-... c645686400` re-confirmed
  `OK` on the built commit itself.

## Land-approval record (BL-1334)

`record_land_approval.bb` does not exist yet (BL-1405 approved but not
landed as of this pass) — appended the line by hand to the SHARED store
(`git rev-parse --git-common-dir`'s parent, i.e. the master checkout, not
this worktree):
`{"at":"2026-09-05T05:48:35Z","ticket":"BL-1364","commit":"c645686400","source":"f97bce44d4"}`.
`source` is `f97bce44d4` — the QA-worktree merge commit this entire
verification pass actually ran against and approved (an ancestor of
`swarmforge-QA`), not the never-reviewed replay-only lineage.
Verified `bash swarmforge/scripts/is_qa_ancestor.sh c645686400` now prints
`approved: c645686400 is a land-step replay of approved source f97bce44d4
... - BL-1334`.

`abandoned_commits: [f97bce44d4]` recorded on
`backlog/active/BL-1364-the-mechanical-share-of-a-turn-is-readable.yaml`
to mark the QA-worktree merge lineage as superseded by the replay (the
merge itself was never pushed to `main`; `swarmforge-QA` keeps it as
ordinary pipeline history).

## Push

Lock acquired via `land_main_publish.sh --acquire-lock`; `--decide-only`
from the replay worktree returned `:next :push` with
`:origin-advanced-since-gate false` (re-fetched immediately before, no
drift). `git push origin HEAD:main` — `2c67608557..c645686400`. Lock
released.

BL-1364 is not `GH-`-seeded; no `issue_done.sh` step applies.

By QA.
