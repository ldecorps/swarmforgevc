# BL-1352 — architect pass (2026-09-03)

## Merge

Fast-forward merge of cleaner `ba14ea4221` (coder rework `08435cee05`, cleaner
re-review). No conflicts.

## Context

First pass was bounced by cleaner for D1 (invariant 1's generator drew the
empty-array/idle arm from `fc.uniqueArray` at low `numRuns`, ~33% failure
rate on the reach floor). This pass is the coder's rework plus the cleaner's
re-review confirming the fix.

## D1 fix verified independently

`bl1352EscalationVisibilityInvariants.property.test.js` invariant 1 now
CHOOSES both `transport` and `idle` as enclosing-loop cases (`fc.constant([])`
for idle, `fc.uniqueArray(..., {minLength: 1})` for waiting) rather than
drawing the empty-array arm by chance — the reach floor is met by
construction, not by luck. Re-ran independently **5/5 consecutive green**
(cleaner measured 15/15). Not the same failure shape as BL-1352's own first
pass, and does not share BL-1352's D1 bug pattern going forward.

## required_wiring confirmed live

- `swarm_status.bb::ask_escalation` — present (`swarm_status.bb:324`
  `ask-escalation` function reads `.swarmforge/operator/status.json`'s
  `:ask_escalation` key and is called into the rendered status map at
  line 432).
- `specs/pipeline/steps/index.js::bl1352EscalationTransportFaultSteps` —
  registered (line 26).

## Architecture review

- No webview/extension-host boundary touched — pure `swarmforge/scripts/*.bb`
  (daemon-side observability/logging) plus Gherkin step handlers and property
  tests. No secrets, no browser storage, no SwarmForge-source modification.
- Dependency-rule gate (BL-259), scoped to changed JS/TS-relevant files and
  full-repo: **PASSED, no forbidden edges** both times.
- Co-change report (BL-255): the flagged "SUSPECTED COUPLING" entries are all
  BL-1352's own files co-changing across its own three commits (coder bounce,
  rework, cleaner review) — expected self-coupling of one ticket's slice, not
  a cross-module design smell.

## Invariants review (BL-633/654)

Both declared invariants have property tests, re-run and non-vacuous:
- Invariant 1 (fault never reads as silence): reach now guaranteed by
  construction (see D1 fix above); also drives the REAL `./swarm status`
  renderer, not just the decision function.
- Invariant 2 (log on change only, never once per tick): re-run, reach
  floors (`held`/`changed`) both exercised, 10/10 pass.

## Incidental fixes (BL-1323, BL-1306 property tests) — reviewed, legitimate

Two of the coder's own earlier property tests were genuinely broken by
unrelated, legitimate work on this branch (BL-1335's `handoffd.bb` edit
tripped BL-1323's whole-branch-diff scope check; BL-1306's git-fixture cases
exceeded Vitest's default timeout). Both fixes are scoped correctly — BL-1323
now measures only the stamp-off's own commits (`git log --grep BL-1323`,
`--first-parent` diff) rather than the whole branch, and no-ops correctly once
BL-1323 has landed; BL-1306 gets an explicit timeout budget and reduced
`numRuns` since its arms are already enumerated by the surrounding loop, not
by repetition. Re-ran both independently: 5/5 tests pass, in reasonable time.
Not BL-1352's invariants to own, but reviewed since they rode in on this
commit — no correctness or vacuity issue found in either.

## Recurring bl1332 line (documented, not re-bounced) — confirmed present

Cleaner's evidence records a second occurrence of a coder commit silently
dropping the "fixture reproduces the shape that froze main" assertion in
`bl1332SharedPathLineLeakSteps.js`, restored by the cleaner (byte-identical to
`origin/main`) and not re-bounced (mechanical restore, ticket-worthy only if
it recurs a third time). Confirmed the assertion is present in the merged
tree. Not re-flagging — this is process debt for the specifier, already
surfaced by the cleaner.

## Independently re-run

- `bb swarmforge/scripts/test/bl1352_escalation_transport_test_runner.bb` —
  ALL PASS.
- `run_acceptance.sh specs/features/BL-1352-escalation-transport-fault-is-visible.feature`
  — 7/7 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1352EscalationVisibilityInvariants` — 5 consecutive runs, 2/2 tests each.
- `npx vitest run --config vitest.properties.config.mjs
  bl1323StampOffInvariants bl1306AuditKeyBasisInvariants` — 5/5 pass.

## Correctness read

No defect spotted. The visibility half (status surface + log-on-change) and
the transport-unconfigured ruling (leave GitHub unconfigured, no transport
change in this ticket per the human ruling) both match the ticket.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
