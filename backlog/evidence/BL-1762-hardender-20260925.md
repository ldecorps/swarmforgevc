# BL-1762 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: the full checklist was run and found no defect.

BL-113 Gherkin mutation (first run, Scenario Outline over the 5 file
Examples): 5/5 killed, 0 survivors. Manifest embedded.

Read all 5 fixed files (`bl1252ExpensiveGuardTieringInvariant`,
`bl1252IndexGuardsAllRunInvariant`, `bl1252RefusalPredicateUnchangedInvariant`,
`bl1252UnexpectedFailureNeverPassesInvariant`,
`bl1252ViolatingGuardsAllNamedInvariant`) and the shared fixture in
full — the same shape applied consistently across every file (`KINDS`,
`PER_CELL_RUNS = runsPerCell(60, KINDS.length)`, one `fc.assert` loop
per kind drawing from `KIND_PLANS[kind]()`, `assertReachFloor` after
the loop), no outlier left on the old sampled `PLAN()`/local
`assertReach` path.

Independently re-verified (not merely trusted) non-vacuity on the
exact defect class QA found (a suiteOnly plan never drawn in 60 runs):
hand-mutated `helpers/bl1252CommitGuardFixture.js`'s `SUITE_ONLY_PLAN`
to produce a clean-shaped plan instead (simulating a kind-specific
arbitrary silently drifting off its own kind — the general failure
mode this ticket's fix must prevent). `bl1252ExpensiveGuardTieringInvariant`
failed exactly as expected: "reach floor: plan-kind suiteOnly drawn 0
< 1". Restored the file (confirmed clean via `git status`), reran
green (5/5 files, `npx vitest run --config
vitest.properties.config.mjs test/bl1252`).

Also confirmed `bl1252UnexpectedFailureNeverPassesInvariant`'s
`fc.pre(reached.length > 0)` guard is dead code under the new
construction (UNEXPECTED_PLAN/MISSING_PLAN's single-broken-guard shape
always satisfies it) — not a residual "hoping" pattern; if that
invariant ever regressed, fast-check would fail loudly ("failed to
find values satisfying the predicate") rather than silently mask
anything.

Per the ticket's own `constraints`, `backlog/standing-reds.tsv` was
not touched — the existing row for
`bl1252ExpensiveGuardTieringInvariant` already names BL-1762 and its
own land retires the row (BL-1631/BL-1663).

No TS/JS production file is in scope — only the five property test
files and their shared fixture (out of CRAP/DRY/mutation-count scope,
kept separate from those tools per engineering.prompt).

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1762-….feature`: 6/6.
- `specs/pipeline/scripts/run_gherkin_mutation.sh` level=soft (first
  run): 5/5 killed, 0 survivors.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1252`: 5/5 (targeted; full properties lane not run, per the
  2026-09-17 rule — `npm test`'s own guard is scoped to files this
  pass changed, none of which triggered it here).
- `npm test` (extension/): 638 files / 10875 tests, all green. Same
  pre-existing per-file budget warnings on a handful of unrelated
  files.
- `git status --short` clean before each commit; no process/fixture
  leaks (each test's own `withRoot`/`mkTmpDir` helper cleans up after
  itself).
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load 2-11/20 cores during this pass, no fallback
  needed.

By hardender.
