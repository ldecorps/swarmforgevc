# BL-1348 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- `npm run compile`: clean.
- `vitestWorkerMemoryBudget.test.js` 37/37, `bl1348VitestWorkerPoolHostSizingInvariants.property.test.js`
  4/4, `bl871PropertyLaneWorkerPoolCapInvariants.property.test.js` 2/2 —
  and re-ran the latter two together three MORE times (5 total) to
  independently check cleaner/architect's flake note: all clean. Agree
  with their root-cause read (a genuine host-load-timing sensitivity
  inherent to ruling B's own design, not a logic defect) — this is the
  same class of contention flake documented in this session's BL-1349/
  BL-1450 hardening passes.
- Acceptance: 3/3 pass.
- CRAP, scoped to the touched source file: every function ≤5, 100%
  coverage, exit 0.
- **Stryker mutation, scoped via a new `vitest.bl1348.stryker.config.mjs`**
  (same precedent as bl1081/bl1365/bl1383/bl1402/bl1441 — avoids the
  standing Stryker-sandbox-only red in `activePoolFreshnessAudit.test.js`,
  which crashes the WHOLE-SUITE dry run Stryker always runs regardless of
  `--mutate` scope; `stryker.config.json` reverted to its shared
  `vitest.config.mjs` pointer before this commit, per the standing rule
  against repo-wide exclude/pointer drift). First pass (scoped to just
  `vitestWorkerMemoryBudget.test.js`) read 43/46, 3 survived; two of the
  three were the pre-existing router-rotation branch and its
  `ROUTER_ROTATION` constant (BL-1336's code, sharing this compiled file,
  explicitly out of this ticket's scope) — covered by a SIBLING test file
  (`bl1336RouterForkCeiling.test.js`) I had not yet included. Widened the
  scoped config's `include` to add that file and re-ran: 45/46, both
  router-rotation mutants killed.
- **The one remaining survivor is a confirmed BL-234-class equivalent
  mutant**, not a gap: `parsePositiveIntOverride`'s
  `if (raw === undefined) return undefined;` early guard, mutated to
  `if (false)`. Traced the fallthrough: `Number(undefined)` is `NaN`,
  `Number.isInteger(NaN)` is `false`, so the general-case line
  (`Number.isInteger(n) && n > 0 ? n : undefined`) already evaluates to
  `undefined` for the exact same input the guard exists to short-circuit
  — removing the guard is unobservable at every call site, demonstrated
  from the code, not merely inferred. The function's own comment confirms
  the guard's INTENT was to avoid reaching `Number(undefined)` at all,
  not to change the result if it were reached. Recorded here as the
  code-level reason per BL-234; this equivalence is commit-local and
  would need re-checking if `parsePositiveIntOverride`'s fallthrough
  logic ever changes.
- BL-113 soft gherkin mutation (the one `Scenario Outline:`, `override`/
  `pack`/`platform`/`pool`/`ram` columns across 2 examples): 7/10 killed,
  3 survived, `"outcome": "fail"` — all 3 independently confirmed
  equivalent by direct arithmetic: (1) `ram: 19904→19901` in the
  override=12 example — `safeCount = floor(RAM*0.5/640)` is 15 at both
  values, comfortably above the override ceiling of 12, so a 3 MB RAM
  change crosses no `floor()` boundary; (2) `override: "unset"→"unsEt"`
  in the full-forge/darwin example — the step handler's `=== 'unset'`
  string check fails on the mutant, but the resulting string is passed to
  `parsePositiveIntOverride`, which returns `undefined` for ANY
  non-numeric string (verified above) — "unset" and "unsEt" are
  indistinguishable downstream; (3) `ram: 4096→4102` in the same
  example — full-forge+darwin short-circuits the ceiling to 1 regardless
  of RAM, and `safeCount` is 3 at both 4096 and 4102 MB (no boundary
  crossed), so the pool is `min(1,3)=1` either way. Manifest committed
  (`362c9b23f0`) with `scenarios: []` per BL-502 (a scenario with any
  survivor is omitted, whatever the reason).
- **Observation, not a defect**: the ticket's own notes promised "the
  specifier adds the full-forge/linux Examples row to the feature the
  same pass" once ruled — that row is still absent (only mono-router/
  linux and full-forge/darwin rows exist; `git log` shows the feature
  file has one commit, the original mint, and was never touched again).
  Checked whether this is a real coverage gap: `resolveVitestForkCeiling`
  has no branch keyed on the full-forge+linux combination specifically —
  it falls through to the same `defaultCeiling` path as any other
  non-full-forge-darwin, non-router pack, already exercised by the
  mono-router/linux row and the extensive unit/property suite. So the
  missing row is a documentation/completeness gap against the ticket's
  own stated plan, not a functional test gap — flagging for the
  documenter/specifier rather than blocking on it myself.
- No orphaned `node --test`/stryker/gherkin-mutator processes of mine
  remained; `.stryker-tmp` and `stryker-incremental.json` are gitignored,
  confirmed via `git check-ignore`; `git status` clean before each
  commit.
