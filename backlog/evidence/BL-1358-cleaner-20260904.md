# BL-1358 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## What was checked

- `bl1358MutantTimeCeiling.test.js` — re-ran: 6/6 PASS, including the
  process-group-kill test (a hung mutant that shells out to a
  descendant process is reclaimed whole, not just the direct child —
  `process.kill(-pid, 'SIGKILL')` on a `detached: true` child) and the
  configurability test (`GHERKIN_MUTATION_TIMEOUT_MS`, defaults to the
  ruled 300000ms).
- `bl1358MutantTimeCeilingInvariants.property.test.js` — re-ran (after a
  fresh `npm run compile`): 2/2 properties hold — a timeout is its own
  verdict and leaves every other mutant its ordinary outcome (P1+P2), and
  with no hang anywhere no mutant spuriously acquires a timeout verdict
  (P1 control).
- Wired into the live path: confirmed `mutationWorker.js` calls
  `runGeneratedTests` (the function that owns the ceiling), and a
  `timedOut` result maps to `infrastructure_error` — the report's
  existing third bucket, which `gherkinMutationClassify` already fails
  the gate on. No new outcome string was invented, matching the human's
  ruling (option 1: a timed-out mutant fails the gate the same as a
  survivor) without touching the classifier.
- `required_wiring`'s own honesty note (verified true, not just claimed):
  the anchor "satisfied by this parcel's own diff" is correct — this is a
  worker-internal change with no external caller to anchor against; kept
  per BL-233's same-commit convention, not asserting reachability it
  cannot prove.
- `mutation-site-count.js` on the new step handler: 110 sites (`over`
  100, closest to the threshold of any ticket reviewed this session).
  Reviewed and declined to split — same reasoning as every other ticket
  in this batch: one cohesive single-feature handler.
- `jscpd` over the new handler and the two touched adapter files: 0
  clones.
- TypeScript compiles clean; the handler discovers via BL-1371's
  registry.

No bounce history on BL-1358 (first pass, main/origin/main agree except
for 7 commits ahead, unrelated to this ticket). Forwarding unchanged to
architect.

By cleaner.
