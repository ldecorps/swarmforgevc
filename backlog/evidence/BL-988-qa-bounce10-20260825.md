# BL-988 QA bounce 10 — 20260825

**Reviewed tip:** documenter `d61b3f7f03` (not merged — tip impure)
**Task:** `BL-988-orphaned-wsl-acceptance-contract-has-no-step-handlers`
**Sibling:** `VERIFY BL-988`
**Prior:** bounce9 `dd03c420f`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main d61b3f7f03  # fails
git rev-list --count d61b3f7f03..origin/main  # → 1
# dels_on_origin=0
```

**Commit hash checked:** `d61b3f7f03`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = a385565ed Close BL-1100: move to done)
paths=22 dels=0
(+ tip resurrects BL-1100 into active vs closed-on-main)
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto **current** `origin/main` (post BL-1100
close) immediately before handoff. Observed rematch10 already stale.

**Remediation:** `git fetch && git reset --hard origin/main`; restore BL-988
only; do not resurrect closed BL-1100 into active; dels=0; verify ancestry in
the same session as `swarm_handoff.sh`.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
