# BL-1535 — architect review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

Reviewed commit: 814cb7f15a (cleaner's forward; coder's own fix commit
8f1e151430, addressing round-2 D1 from
`backlog/evidence/BL-1535-architect-20260911.md`).

### Checklist run

- Dependency-gate: inapplicable — every changed file since the round-2
  bounce (`swarmforge/scripts/test/test_chase_departing_mid_parcel_gate.sh`,
  `backlog/evidence/BL-1535-cleaner-20260912.md`) is `.sh`/`.md`, no
  TypeScript touched.
- Co-change report against `handoffd.bb`/the test file: only the expected
  same-parcel siblings (handoffd.bb, mono_router_lib.bb, the bb runner, the
  step handler, suite-manifest.tsv) flagged — all pre-existing baseline
  coupling for this ticket family, nothing new.
- `bb swarmforge/scripts/test/mono_router_lib_test_runner.bb` — PASS.
- `bash swarmforge/scripts/test/test_chase_departing_mid_parcel_gate.sh` —
  PASS, all 7 cases (new case 07 included).
- `bash swarmforge/scripts/test/test_rotate_to_role_stuck_parcel_gate.sh` —
  PASS, all 12 cases (case 04 unchanged — `rotate-resident-to!` stays
  ungated).
- Acceptance: BL-1535 (7/7), BL-805 (5/5), BL-926 (6/6), BL-938 (4/4) — all
  green.
- `bb swarmforge/scripts/test/daemon_cycle_guard_lib_test_runner.bb` — PASS.
- `handoff_lib.bb` diff vs `main`: empty — unchanged, per the ticket's FIRM
  constraint.
- Code read: `attempt-resident-rotate!` (handoffd.bb ~line 1710) is
  byte-identical to round 2's remediation (`git diff a7b6ab41e4..814cb7f15a
  -- swarmforge/scripts/handoffd.bb` shows only the docstring/logic already
  reviewed and confirmed correct in round 2 — no further code change this
  round).

### Round-2 D1 closure verified

Round 2's D1 (`backlog/evidence/BL-1535-architect-20260911.md`) asked for a
regression case proving `attempt-resident-rotate!` resolves
`departing-working-signal` and the telemetry `:role` field from
`departing-role-blocking-handoff`'s BL-927-aware resolved role rather than
the raw `mono-router-active-role` marker. New case 07 sets the marker to
`specifier` while the pane's live identity is `hardender` (holding the real
`in_process` parcel and a live descendant process), rotates onto a third
role (`coder`, named by neither the marker nor the live identity), and
asserts: refusal (`:departing-mid-parcel`), no pane respawn, and the
telemetry row naming `hardender` — explicitly asserting it does NOT name
`specifier`. This is exactly the divergence scenario D1's failure
narrative described, driven through the real `attempt-resident-rotate!`
call path (not the sibling `rotate-resident-to!` fixture), closing the gap:
the test would have failed against the round-1 code
(`departing-role (handoff-lib/read-mono-router-active-role)`, discarding
the resolved role) and passes against the round-2 fix. Confirmed by
inspection: `git diff 63ec4a6582..814cb7f15a --stat` touches only the test
file plus cleaner's evidence note — no production code changed this round,
consistent with an OMISSION bounce (missing test) being closed by adding
exactly that test.

### Invariants review

- Invariant 1 (never displace a working holder for a different target):
  encoded and passing (cases 01a/01b, and now 07 for the divergence case).
- Invariant 2 (idle holder still yields): encoded and passing (case 02).
- Invariant 3 (every refusal is observable, naming the departing role): now
  fully encoded — case 07 closes the gap round 2 left, asserting the
  telemetry row names the LIVE role and explicitly not the stale marker.

No defect found. Forward to hardener.

By architect.
