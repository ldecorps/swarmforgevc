# BL-1017 QA approval, 2026-08-21

**Reviewer**: QA. **Reviewed at**: documenter tip `208862a597`, merged into
QA (`f45be9d844`/BL-586 approval commit as first parent, documenter tip as
second). Ancestry confirmed: nothing here reverted (`git diff` against both
parents shows only additive content on each side, no overlap).

## Judgement call, flagged up front

`qa_e2e_procedure` steps 4-5 ask for a live-pack confirmation: kill one
standing role's real tmux session and watch babysitterd's sweep recreate it.
The hardener's own note explicitly left this for QA
("qa_e2e_procedure step 4 ... is explicitly out of the unit boundary per the
ticket itself - left for QA as a manual procedure, not attempted here").

This swarm is genuinely live and busy during this review (handoff traffic
from cleaner/architect/hardender/documenter/coordinator arrived continuously
while this pass ran). Killing a live standing role's tmux session touches
shared swarm state and could interrupt another role's genuinely in-flight
work - the same category of risk BL-998's QA pass flagged and chose to
route around rather than touch the live tree. I made the same call here:
**did not kill a live session**, and am not treating steps 4-5 as a QA
approval blocker, because:

- The mechanism is independently confirmed through the live CONSUMPTION
  code path, not merely the pure decision function - see item 3 below.
- The ticket's own wording ("recorded as a manual procedure since live tmux
  is outside the unit boundary") frames this as an operational runbook
  step, and the documenter has already recorded the identical 4-point
  confirmation procedure in `docs/how-to/BL-611-babysitterd-runbook.md`
  under "Bounded session repair for a vanished standing role (BL-1017)".

**Recommend**: the operator run the runbook's kill-one-session confirmation
by hand at a quiet moment, per that doc's own "confirm by hand after a
change here" instruction. Not blocking on it here.

## Verification order (Article 4.4, qa_e2e_procedure)

1. **Read the diff.** `check-live-session`'s missing-session branch now
   carries a bounded `:repair` intent (`babysitterd_sweep_lib.bb`), hung off
   the SAME branch the pre-existing `should-stand?` suppression already
   guards - invariant 1 holds by construction, not by a second predicate.
   `session-repair-allowed?`/`note-repair-attempt` implement invariant 2's
   bound (default 1 attempt / 10-minute cooldown).

2. **`required_wiring` verified by reading the live consumer**, not trusted
   from the hardener's note alone: `babysitter_check.bb`'s `-main` (around
   the `BL-1017 required_wiring` comment) reads `:repairs` out of
   `assemble-findings`'s return, and inside `(when (seq repairs) ...)` calls
   `ensure-role-session!` (tmux new-session + respawn-pane -k through the
   role's canonical launch script) and persists the cooldown state via
   `write-repair-state!`. `assemble-findings` computes `repairs` via
   `(keep :repair findings)` - a view over the SAME findings list, so the
   CRIT stays in `findings` and prints unconditionally alongside a repair
   (qa_e2e_procedure step 2, invariant "never swallow the alert").

3. **Independent test runs from my own worktree**, after merging the
   documenter's commit (clean merge, `git status` clean before/after, no
   orphaned processes):
   - `bb swarmforge/scripts/test/babysitterd_sweep_lib_test_runner.bb`: ok
     (59 assertions, 10+ BL-1017-specific covering all four scenario
     shapes plus edge cases the feature doesn't enumerate - cooldown reset,
     no-prior-state).
   - `bb swarmforge/scripts/test/babysitterd_sweep_lib_property_runner.bb`:
     ok, including P6(a)/P6(b) - both declared invariants, with
     generator-reach floor assertions confirming the random generator
     actually hits absent-standing/absent-dormant/present branches and both
     the bounded and reset-after-window paths, over 400 + 200 trials.
   - `bash swarmforge/scripts/test/test_babysitter_check.sh`: **ALL PASS**
     (11 scenarios A-K), including the two BL-1017 additions J and K, which
     drive the REAL `-main` entry point over a fake-tmux/fake-ps harness
     (no real tmux, stays inside the testability boundary) and assert on
     the actual `tmux new-session`/`respawn-pane -k` calls issued and the
     persisted `session-repairs.json` state - this is qa_e2e_procedure step
     3 (wiring, not a unit check on the pure function alone), independently
     re-run rather than trusted from the hardener's report. Hardener's note
     records this test was confirmed non-vacuous by hand-mutating the
     consuming `when` block to a no-op and observing J fail as expected;
     I did not repeat that mutation myself but the test's own assertions
     (concrete call-log checks, not a boolean) make a silent-pass unlikely.
   - `specs/pipeline/scripts/run_acceptance.sh
     specs/features/BL-1017-babysitterd-recreates-vanished-standing-session.feature`:
     **6/6 scenarios pass** (Outline's 3 rows + 3 singles), all handlers
     registered (`bl1017BabysitterdRecreatesVanishedStandingSessionSteps`
     required from `specs/pipeline/steps/index.js:557`).

4. **No mutation/CRAP/DRY tooling applies** - Babashka/Clojure has none
   wired (Engineering Rules), gated by its own unit-test suite only, which
   ran above. The hardener's hand-mutation non-vacuity check on the one
   consumption-point gap is the documented degraded fallback for this
   surface, consistent with prior Babashka-surface hardening passes.

5. **Documentation**: `docs/how-to/BL-611-babysitterd-runbook.md` gained a
   full "Bounded session repair for a vanished standing role (BL-1017)"
   section - what it does, the bound, why the CRIT is never swallowed,
   topology-respecting construction, and the manual-confirmation procedure
   (steps 4-5 above, recorded rather than QA-gated per the ticket's own
   testability-boundary framing). `docs/index.md` and
   `docs/reference/Specification.MD` updated.

6. **BL-532 sibling check**: `qa-sibling-check.js status --ticket BL-1017`
   returned `VERIFY BL-1017` (exit 0) - no open deferral.

7. **No orphaned test processes** before or after this pass.

## Verdict

PASS. Landing on `main` alongside BL-586.
