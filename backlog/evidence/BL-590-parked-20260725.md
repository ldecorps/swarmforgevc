# BL-590 parked to `backlog/hold/` — 2026-07-25 13:05 BST

Operator decision: park the business ticket and concentrate on the workflow
defects it exposed, running them offline. BL-567 (Expeditor) is being built by
hand as its own first subject — see that ticket.

Parked to **`hold/`, not `paused/`**, deliberately: `paused/` is the promotion
queue (`promote-next-paused-item-if-needed`), and BL-590 carries
`human_approval: approved` with `priority: 02`, so it would be auto-promoted
straight back on the next boot. `hold/` is a recognised live state
(`backlog_epic_milestone_audit.bb:20` treats `active`/`paused`/`hold` as live)
that promotion does not read.

## Where the work actually stands

Six architect send-backs, all on the resume-identity family. **The last coder
rework is committed and durable but NOT yet reviewed.**

```
swarmforge-coder      01562217be  BL-590 coder rework: shared target-identity
                                  function ends the resume-identity family
swarmforge-cleaner    674f2538bf  Merge coder BL-590 for cleaner review
swarmforge-architect  55ada48bc2  Revert "Reapply ..." (bounce #6 revert state)
swarmforge-QA         a7d328805f  (BL-575 — BL-590 has never reached QA)
```

`01562217be` is the fix the architect prescribed in SEND BACK #6 and
**pre-verified before writing the bounce**: one identity function exported from
the pure policy module, `findStateForTarget` comparing by it, the store deriving
its filename from it. The architect recorded that with it applied, P6 flips to
PASS and all 5967 unit tests plus 50 property tests including P5/P5b stay green.
So the probability this passes review is high — but **it has not been reviewed.**

Interrupted mid-round: the cleaner had merged the coder's work and was starting
its pass when the swarm was stopped.

## The bounce family, for whoever resumes

| # | defect | class |
|---|---|---|
| 1 | un-guarded durable writes in facilitator turn | missing branch guard |
| 2 | idempotency guard misses redelivered batches | missing branch guard |
| 3 | unguarded no-active branch re-applies stale text | missing branch guard |
| 4 | re-paste at prerequisites-ready wipes the checklist | **state-file key** |
| 5 | distinct target repos share one state file (slug not injective) | **state-file key** |
| 6 | handler and store disagree on target identity (`http://` vs `https://`) | **two layers, two definitions** |

#1, #4, #5, #6 are one root cause: target identity. Each round the architect's
diagnosis climbed a level — "guard one more branch" → "the key is wrong" → "two
layers hold two definitions". `01562217be` addresses the last of these.

Property tests P1–P6 are landed and must not be touched (architect: "Do not touch
any of it"). Note P2's stability clause was deliberately deleted at #5 — a
digest-suffixed slug cannot be idempotent under re-slugging.

## Mailbox state at park time (nothing swept, all resumable)

```
specifier    new=2   two architect rule_proposals, scope: constitution,
                     awaiting an Article 5.1 human ruling (see below)
hardender    new=1   QA merge-up broadcast note from 07:39 (BL-575), never
documenter   new=1   drained — the BL-550 stranding residue, harmless
coder/cleaner/architect/QA/coordinator   empty
```

Stopped with `./stop-swarm.sh` and **no flags** — deliberately not
`--sweep-inbox` (would archive these) and not `--reset-worktrees`.

## Open items this park does not resolve

**Two `rule_proposal` parcels need a human Article 5.1 ruling.** Both came out of
BL-590's bounces and both concern the revert-of-merge hazard:

- `50_20260725T095008Z_000540_from_architect` — if a bounced commit is already an
  ancestor of `main`, do NOT revert it out of the bouncing branch; record why in
  the bounce evidence and report the breach to the coordinator.
- `50_20260725T112532Z_000544_from_architect` — after `git revert -m 1` of a
  review merge, verify the bounced CONTENT is gone from the tree, not that the
  commit is no longer an ancestor. Ancestry is topological; revert only removes
  content.

These are well-founded. During BL-590's own bounce #6 revert, the architect had
to explicitly exclude the operator's BL-629..635 ticket filings from being
swept — commit `90d0d79f2 "Keep main's BL-629..635 filings out of the BL-590
bounce revert"`. That held on the architect's vigilance, not on a mechanism.

## To resume

1. Rule on the two `rule_proposal`s, or leave them queued.
2. Move the yaml `hold/` → `active/` (do NOT rely on auto-promotion; going via
   `paused/` invites a promotion race with whatever else is eligible).
3. The cleaner resumes from `674f2538bf`; the architect reviews `01562217be`
   against SEND BACK #6's D3/D4.
4. Before trusting any BL-590 commit as reviewed, check
   `git merge-base --is-ancestor <sha> swarmforge-QA` — this ticket's own history
   is why BL-629/630/631/632 exist.
