# BL-1537 — QA review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Checks run this pass:

- `qa-sibling-check.js status --ticket BL-1537`: `VERIFY BL-1537`, no open
  deferral.
- Merged documenter's tip `ed9bcc56ce` into QA (`2cb50df412`, `--no-ff`);
  diffed the merge against BOTH parents — nothing lost relative to either,
  the full change is exactly the incoming BL-1537 pack.
- `main`/`origin/main`: `git rev-list --left-right --count` → `0 0`, in sync.
- Read the ticket's `bounce_history`: one prior architect→coder bounce
  (`BL-1537-architect-20260912-2.md`, D1: `notify_specifier_freshness_hold`
  missing a `trap ... EXIT` on its draft) — confirmed fixed at
  `716f4b77c6` (`trap 'rm -f "$draft"' EXIT` now present, line 282) and
  covered by the shell fixture's case 05.
- `npm run compile`: clean.
- `npm test` (full unit suite): 616 files / 10445 tests PASSED. Script exit
  code 1 is the pre-existing, always-red `check-suite-duration-budget`/
  per-file-budget meta-gate (BL-378/BL-445), tracked open under epic
  BL-791 (`backlog/paused/BL-791-epic-unit-suite-speed.yaml`, "already a
  permanently-red gate" per its own text) — not a BL-1537 regression; none
  of the six offending files touch this ticket's diff.
- `npm run test:properties`: 393/395 files, 1166/1169 tests PASSED. The 2
  failing files are both pre-existing registered standing reds with open
  tickets: `bl1313BatchGuardVisibilityInvariants.property.test.js` (BL-1503,
  `backlog/paused/`) and `bl1089FrontDeskLivenessFixture.property.test.js`
  (BL-1502, `backlog/paused/`) — both root causes are unrelated bb-loading/
  fixture-contract drift, not this ticket's TS/shell diff.
- Acceptance: `run_acceptance.sh specs/features/BL-1537-*.feature` — 9/9
  scenarios PASS (all seven senders + the recorder + the census scenario).
- Shell fixture `test_production_sender_drafts_under_root.sh` run directly:
  5/5 PASS, including case 05 (the signalled-exit trap fix).
- Targeted re-run: `draftPathUnder.test.js`, `draftPathUnder.property.test.js`
  (under `vitest.properties.config.mjs`), `closingCeremonyRunArgs.test.js`,
  `tracerBulletLauncher.test.js` — all green in isolation.
- Wiring: `draftPathUnder` is imported and called by all three TS senders
  (`closing-ceremony-run.ts:34`, `night-closing-ceremony-run.ts:118`,
  `tracer-bullet-launcher.ts:174`) — not merely unit-tested in isolation.
- Re-ran the ticket's own census grep: unchanged shape, seven sender files
  clean under the post-fix negative grep except the one already-reviewed
  unrelated `SED_TMP="$(mktemp)"` scratch file at
  `promote_and_route_next.sh:387` (confirmed out of scope, matches
  architect's prior finding).
- Guard NOT relaxed: no `TMPDIR`/`os.tmpdir()`/allowlist addition in
  `handoff_draft_root_guard_lib.bb`.
- Documenter's two corrected narratives verified accurate against the
  actual seven-sender fix (`handoff_draft_root_guard_lib.bb` header,
  `BL-1518-a-*.feature` comment block) — both now correctly state the
  premise held only for role-authored and Babashka-built drafts.
- Ancestry: `merge-base --is-ancestor` confirms this ticket's own coder
  (`e300226fae`), hardener-merge (`3f1476288c`), and documenter tip
  (`ed9bcc56ce`) are all ancestors of the commit being approved
  (`2cb50df412`) — not merely of `HEAD` on a busy `main`.
- `pgrep -fl 'node --test|stryker'` clean before and after every run; no
  orphaned test/mutation processes.
- `git status` clean after the full run — no draft or build artifact
  became a tracked file.

Blamed role: none. Class: none. Remediation: none — clean sweep.

By QA.
