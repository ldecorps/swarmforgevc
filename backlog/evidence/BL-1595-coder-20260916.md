# BL-1595 — coder evidence, 2026-09-16

## The fix

Both budgeted tests in `extension/test/telegramFrontDeskBotCli.property.test.js`
now carry `propertyLaneTimeoutMs(60000)` (from
`extension/test/helpers/propertyLaneContentionBudget.js`, the BL-1588
helper) as their third argument, in place of the bare literal `60000`
(previously at lines 178 and 212). The file's first test (pure,
`composeRoleAnswerNoteMessage`, no third argument) is untouched; no
assertion, `numRuns`, or fixture changed.

## Full-lane reproduction cited (per FIRM wording — not re-run for its own sake)

The coder's BL-1588 post-fix re-verification run 1 of 5 (2026-09-16,
`backlog/evidence/BL-1588-coder-unowned-red-telegramFrontDeskBotCli-20260916.md`,
in BL-1588's own parcel) is the reproduction, cited verbatim:

```
Error: Test timed out in 60000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ test/telegramFrontDeskBotCli.property.test.js:181:1
```

Green in runs 2 to 5 of that same reproduction and alone: a load-dependent
timeout against the bare 60000ms constant, not a deterministic red — the
same class BL-1592 owns for `bl1529ScriptSenderAuditOutcomesInvariant.property.test.js`.

## Verification

**Solo run** (`npx vitest run --config vitest.properties.config.mjs test/telegramFrontDeskBotCli.property.test.js`):
3/3 tests green — the two budgeted tests took 46.3s and 53.8s, both well
under the unscaled 60000ms base (forks=1 forced for a genuinely solo
single-file invocation, per `resolveLaneForks`).

## Declared invariant (BL-654)

Invariant 1 ("a file alone on a quiet host keeps its declared base,
60000ms for both budgeted tests") is the identical generic claim BL-1588's
own coder-authored property test
(`extension/test/bl1588PropertyLaneBudgetConcurrencyInvariant.property.test.js`)
already encodes for `propertyLaneTimeoutMs`, unmodified by this parcel.
This ticket introduces zero new arithmetic — it only wires two more
per-test arguments in one file to the same, already-tested function — so
a duplicate property test would re-test the identical untouched code
path. Acceptance scenario 02 (pure injection, the real function, four
boundary points including the 60000-base 3x-ratio check against an
8-fork 20000-base budget) is this parcel's own executable encoding of
the wiring's correctness.

## Acceptance

`specs/pipeline/steps/bl1595FrontDeskBotCliPropertyTimeoutSteps.js`
(required_wiring anchor). Scenario 01 is a source-level check (a small
`test(...)` call-site scanner, string/comment aware — BL-914's
`testTimeoutParser.js` collapses a non-numeric trailing argument to
`timeoutMs: null`, discarding the raw text this scenario needs; the same
reason BL-1592's/BL-1600's own scanners were not a reuse of it), pinning
the file's population at exactly 3 tests / 2 per-test timeouts (BL-1445).
Scenario 02 drives the real `propertyLaneTimeoutMs` in-process (BL-1541
shape). Scenario 03 reads this evidence file.

```
$ ./specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1595-*.feature
```

## qa_e2e_procedure

1. 20/20 solo runs — QA's own e2e step (this parcel's solo run above
   confirms the mechanism; 20x repetition is QA's own procedure).
2. `npm run test:properties` 5 times — QA's own e2e step.
3. Acceptance: all three scenarios pass.
4. `git diff <parent>..<parcel> -- extension/test/telegramFrontDeskBotCli.property.test.js`
   touches one require line and two argument lines only; no `assert` or
   `numRuns` line changes — confirmed by inspection of the diff (one
   import line added, two `60000` literals replaced with
   `propertyLaneTimeoutMs(60000)`).
5. `bb swarmforge/scripts/standing_red_register_cli.bb .` after removing
   the row: no row for the file, `"unowned":[]`. The matching allowlist
   row in `swarmforge/scripts/property_suite_standing_allowlist.tsv`
   removed in the same land.

## Register

Removed the BL-1595-owned row from `backlog/standing-reds.tsv` and the
matching row from `swarmforge/scripts/property_suite_standing_allowlist.tsv`
in this land, per the ticket's own "the register row leaves in the same
land" and BL-1592's own established practice of removing both together.
