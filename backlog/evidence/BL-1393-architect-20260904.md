# BL-1393 architect review — clean sweep, forwarding

## Reviewed
Merged cleaner's `a7bc64e5b1` (clean, no conflicts, brought in the batch of
already-approved work my branch was behind on too). Read the ticket's three
invariants, `approval_context`'s scope reading, coder and cleaner evidence,
and the full diffs of `nightClosingCeremonyLive.ts`,
`night-closing-ceremony-run.ts`, and `finish_shift_lib.sh`.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../extension/src/quality/nightClosingCeremonyLive.ts ../extension/src/tools/night-closing-ceremony-run.ts
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1393CeremonyOnEverySleepSteps.js
```
No forbidden edges, both runs.

## Logical coupling (BL-255)
`co-change-report.js` against the four core changed files: only frequency-1
hits, below the default threshold (3). Nothing flagged.

## Invariants — verified directly in the diff
1. **One sequence, lean pass as a step.** `enterBriefing()` pushes the
   `lean-packet` action (line ~171) strictly after the drain/park branch
   resolves and strictly before `instruct-briefing` is pushed (line ~181) —
   read in order in the actual function body, not inferred from a comment.
2. **Fires only after a shift, never on a restart.** `startFrozen()`'s new
   `obs.workedAShift === false` branch is checked before the
   `briefingAlreadySent` branch, so a no-work sleep short-circuits to the
   empty-outcome path regardless of briefing state. `shiftWorkedSinceLastCeremony()`
   fails OPEN (returns `true`) when the stat probe can't tell — correctly
   conservative, since silencing a real ceremony is the worse failure mode.
   Grepped `remote_bounce.sh`/`kill_all_swarm.sh`/`expedite.sh`: none
   reference the ceremony CLI — confirmed no restart path can invoke it.
   `runNightClosingCeremony`'s gate check (`gate.mode !== 'ceremony' &&
   sleepPath === null`) only bypasses the window for an explicit
   `--sleep-path` caller; the daemon's own call (no `--sleep-path`) is
   unchanged and still fully gated — the schedule is untouched, as the
   ticket requires.
3. **Every ceremony ends in a recorded outcome.** The `briefingAlreadySent`
   short-circuit in `startFrozen()` now pushes `lean-packet` before
   `night-stop`, closing exactly the "indistinguishable from a ceremony that
   never ran" gap the coder's own property test caught on its first run.

## Independent re-verification
- **Caught and corrected a stale build**: my first e2e run (2×) showed 4
  genuine-looking failures (freeze/lean-packet/ordering/empty-outcome all
  reporting `"actions": []`) that directly contradicted the evidence's
  claimed 11/11. `ls -la` showed `out/night-closing-ceremony-run.js` older
  than the `.ts` source — a stale compile, not a regression. After
  `npm run compile`, both runs were clean:
  `test_bl1393_one_ceremony_every_sleep.sh` **11/11 PASS**, twice.
- Property test (`bl1393OneCeremonyEverySleep.property.test.js`): **3/3**.
- Acceptance: **BL-1393 9/9**, **BL-658 11/11**, **BL-820 12/12** — both
  retensed predecessors still fully green, confirming the `retires:` scope
  exemption did not touch any scenario.
- Unit suites (`nightClosingCeremonyLive`, `nightClosingCeremonyRun`,
  `nightClosingCeremony`, `closingCeremonyRun`, `closingCeremonyRunCli`):
  **35/35**.
- `test_handoffd_closing_ceremony_gate_wiring.sh`: **ALL CHECKS PASSED**
  (gate suppresses the fixed-morning trigger under `closure_stop_local`,
  and the daemon still invokes the sequence under ceremony mode with no
  `--sleep-path`).
- `jscpd` on the two touched TS files after cleaner's D1 refactor: **0
  clones**, confirming the duplication cleaner found and fixed stayed fixed.
- Grepped `finish_shift_lib.sh`: the old direct `closing-ceremony-run.js`
  call is gone (0 hits), replaced by
  `night-closing-ceremony-run.js --sleep-path finish-shift` — matches
  required_wiring anchor 1 exactly, dead logic removed not re-shipped.
- Grepped for `.swarmforge/shift-started` writers: none (confirms the
  surfaced BL-1328 finding — the explicit stamp is read but nothing writes
  it yet, correctly left unfixed here per the coder's evidence, since the
  mtime-of-`swarm-identity` fallback already satisfies invariant 2).

## required_wiring
All three anchors read live: `finish_shift_lib.sh` drives the one sequence
(anchor 1), `night-closing-ceremony-run.ts` imports and calls
`runClosingCeremony` as the lean-packet step (anchor 2), and the step
handler file matches the discovery-suffix convention (anchor 3, BL-1371).

## Scope check
`approval_context`'s reading (change WHEN the ceremony fires, never WHICH
shifts exist) is respected: `finish_shift_lib.sh`'s diff touches only the
CLI it invokes and the flag it passes; no crontab, `continuous-shifts.json`,
or pack conf appears in either commit's diff, and e2e check 5 asserts this
byte-identically. `retires:` correctly names a narrative re-tense, not a
scenario retirement — verified by both predecessors' full acceptance suites
staying green.

## Surfaced items (correctly left out of scope, not swept)
- The `.swarmforge/shift-started` stamp (BL-1328's property test vetoes any
  new executable line in `swarmforge.sh` outside its own detection helper —
  same recurring "one-parcel premise binds all later ones" class as
  BL-973/BL-1279/BL-1398, correctly not fought here, not minted by me either
  since it's cosmetic — the mtime fallback already satisfies the invariant).
- `night-stop.sh` named in the ticket but living outside this checkout
  (operator directory) — coder's addendum commit `2ecafa89a3` (evidence-only,
  not part of the commit this handoff carried) correctly records this rather
  than leaving the reader to infer it.
- `qa_e2e_procedure` item 8 (a live weekday bedtime) cannot run — cron has
  been dead on this host since 2026-08-30 (BL-1392) — correctly left for QA
  once cron is restored, not faked here.

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted (the one apparent failure was my own stale build, resolved by
recompiling — not a code defect). Forwarding to hardener.
