# Human request: Spec tree milestones filter (relayed by coordinator, 2026-09-05)

Human asked for this ~08-30 (not 08-26 as I first said — corrected by human
2026-09-05). Believed to have been minted as a ticket at the time but lost
before reaching backlog.

Human's own words, verbatim:

> tip to only show entries for which the tree contain that text (classic
> filter found in every IDE tree structure menu).

> august 30.

Context: this concerns the "Spec tree" screen (shipped BL-592,
`extension/src/bridge/specTreeUiHtml.ts`) — a milestones/epic-tier tree view
on the live console. The requested feature is a text-filter input that
narrows the tree to only entries whose text matches, standard IDE
tree-filter behavior (type text, non-matching nodes hide, matching nodes'
ancestor path stays visible).
