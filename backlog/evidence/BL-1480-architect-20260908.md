# BL-1480 — architect review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- Merged cleaner commit `2ee3a3a1f4` into `architect` (no conflicts).
- `node extension/out/tools/dependency-gate.js` (run from `extension/`,
  scoped to the two new/changed JS files) — PASSED, no forbidden edges.
  No `extension/src` files touched by this parcel.
- `node extension/out/tools/co-change-report.js` on all five changed
  files — the only non-noise cluster is `bbFixtureClosureGate.js`'s
  existing co-change with the rest of the BL-973/BL-1279 closure-guard
  family (step handlers, `bb_load_closure_*`, `suite-manifest.tsv`,
  front-desk fixtures) — expected, pre-existing shared-infra coupling,
  nothing new introduced by this parcel. The larger 4-co-change cluster
  (`.github/workflows`, `android/**`, `.vscode/**`) is bulk-commit noise,
  not a real coupling signal.
  - Ran both standing fixtures directly and with `PACK_STAFFING_SKIP_GATE`
    set and unset: `test_promote_and_route_next_priority.sh` and
    `test_promote_and_route_next_no_limit_depth.sh` — exit 0 in all four
    runs.
  - `run_acceptance.sh` on BL-1480's own feature — 7/7. Regression:
    BL-973's feature — 13/13; BL-1279's feature — 10/10. No regression
    from the `FIXTURES.entry`-as-list generalization in
    `bbFixtureClosureGate.js`.
  - `required_wiring` confirmed present: both fixture basenames are
    entries in `bbFixtureClosureGate.js`'s `FIXTURES` map;
    `bl1480PromoteRouteFixtureClosureSteps.js` exports `registerSteps` and
    lands in this same parcel (BL-233).
  - Two-layer boundary / IO ownership / webview storage / secrets rules:
    not implicated — no extension host, webview, or VS Code API surface
    touched; this is shell-fixture and JS test-infrastructure only, per
    the ticket's own FIRM constraint (`promote_and_route_next.sh`,
    `promotion_gates_cli.bb` and the libs untouched — confirmed:
    `git diff --stat` on those three paths is empty).
  - Invariants (BL-654): both declared invariants are properly
    stated-not-encoded per the coder's exemption (shell-fixture/
    subprocess behaviour, no pure module) — encoded behaviourally instead
    via acceptance scenarios 01/02 (invariant 1: full derived closure,
    grep confirms `cp "\$SCRIPTS/.*\.bb"` count is 0 on both fixtures) and
    scenario 04 (invariant 2: a load failure reports itself, non-vacuous —
    removes a real file from a real closure). Reviewed both scenarios by
    hand: they exercise the real fixtures and the real
    `bbFixtureClosureGate.js`, not a restatement.
  - Property Testing pass: no pure, testable module was touched by this
    parcel (`computeClosure` is pre-existing BL-973/BL-1279
    infrastructure, already covered by
    `bb_load_closure_agreement_test_runner.bb`) — no property test added,
    correctly a no-op per this pass's own scope boundary.
  - The coder's two out-of-scope discoveries (unowned `test_bl1028_...`
    red, already sent as a `note` by the coder this pass; the deprecator
    freshness-gate stub) were reviewed: the stub is test infrastructure
    only (no production file touched), scoped narrowly to the documented
    pure contract (`interpretFreshnessCliOutput`), and follows the same
    stand-in posture already used in these fixtures for
    `route_backlog_to_coder.sh` — no architectural objection to the
    coder's judgment that this was within scope to add without holding
    for adjudication.
  - Mutation-Site Size advisory (cleaner's note, BL-485): both files over
    the 100-site advisory threshold, checked against the analogous
    `bl1279FrontDeskFixtureClosureSteps.js` (154, also over, never split)
    — agree with cleaner's judgment: single-responsibility, splitting
    would fragment rather than clarify.

By architect.
