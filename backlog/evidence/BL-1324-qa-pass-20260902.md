# BL-1324 QA pass — 2026-09-02

Merged documenter `8b1dd75cae` into QA worktree (`20f3351569`). Lineage
verified: `git merge-base --is-ancestor 8b1dd75cae HEAD` — OK.

Review-only BL-848 stamp-off parcel: confirms or refutes landed hotfix
commit `4ed88430b2`; never reimplements/rewrites/reverts it and never
writes certified/waived into the ledger.

## Process hygiene

`pgrep -fl 'node --test|stryker'`: only my own two shell PIDs from the
prompt's job control, no stray test/mutation processes before or after
this pass.

## Independent re-run

- `npx vitest run --config vitest.properties.config.mjs
  test/bl1324ClaudeSeatQwenCloudContextWindowInvariants.property.test.js`
  — 3/3 pass (all three invariants, including invariant 3: a sibling
  Anthropic seat is untouched).
- `npm run compile` — clean.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1324-claude-seat-qwen-cloud-context-window.feature`
  — 11/11 scenarios pass (BL-112 executable e2e gate; matches
  qa_e2e_procedure items 1-4, 7-8, plus the retired-BL-1325 scenarios).

## Full unit suite (`npm test`, extension/)

15 failed test files / 25 failed tests (570 passed of 585 files, 9823
passed of 9848 tests). **Zero overlap with this parcel's changed paths**
(`specs/pipeline/steps/bl1324ClaudeSeatQwenCloudContextWindowSteps.js`,
`specs/pipeline/steps/index.js`,
`extension/test/bl1324ClaudeSeatQwenCloudContextWindowInvariants.property.test.js`,
`docs/how-to/BL-848-certify-an-operator-hotfix.md`,
`specs/features/BL-1324-claude-seat-qwen-cloud-context-window.feature`).
Per BL-1063, grepped every failing file against `backlog/` before
reporting — all 15 are pre-existing, already-tracked standing reds,
independently confirmed by the prior BL-1318 QA pass evidence
(`backlog/evidence/BL-1318-pack-launch-refuses-a-steward-uncleared-seat-qa-pass-20260901.md`):
- tempDirTrapGuard, socketFixtureShortRootGuard, liveRepoDerivationGuard
  — BL-1289 / BL-1290 / BL-1291.
- backendSwitch, telegramClient, telegramCursorOperatorExec — BL-1263.
- constitutionDocCitations, pilotAcceptanceGate, unreachableStepHandlerCheck
  — BL-1221.
- crossFileDuplicationCheck, multiBranchParserCoverageCheck,
  perHatRolePromptEvidenceCheck, pilotScopedCrapCheck,
  shellEntryPointDriveCheck — BL-1229.
- operatorRuntimeBbFixtureClosure — BL-1265.

No new/untracked red found. None re-reported.

## required_wiring — re-verified live, both anchors

- `extra_cli_targets_qwen_cloud` defined `swarmforge/scripts/swarmforge.sh:1479`;
  billing-guard call site at `:1958`
  (`[[ "${SWARMFORGE_USE_QWEN:-}" == "1" ]] || extra_cli_targets_qwen_cloud "$extra_cli"`);
  `launch_role` elif at `:2219`
  (`elif [[ "$agent" == "claude" ]] && extra_cli_targets_qwen_cloud "${EXTRA_CLI_ARGS[$index]:-}"`).
- `swarmforge/packs/bob-multi-provider-mono-router.conf` line 60: coder
  window stages `claude coder --model qwen3.8-max ...` — matches
  qa_e2e_procedure item 4 at the reviewed commit.
- `bl1324ClaudeSeatQwenCloudContextWindowSteps` registered at
  `specs/pipeline/steps/index.js:919`.

## Ledger — untouched, invariant 2 held

`backlog/hotfix-ledger.yaml` row for `4ed88430b2`: `state: stamp-open`,
`human_decision: null` — this parcel wrote nothing to it, as required.

## qa_e2e_procedure item 5 — dormant gaps

Confirmed both dormant gaps named in `approval_context` (single-token
`--model=qwen*` form not matched; OpenRouter-routed seat with a qwen*
`--model` falls through without the Qwen key/context declaration) are
accurately reported, not silently patched, per the ticket's constraints.
The human ruling (option 2) already authorizes a narrow follow-up ticket
for these, routed to the specifier to mint separately — not this parcel's
job to open it.

## Constraints honoured

- No production wiring touched by this parcel (confirmed above: only
  step-handler JS, a property test, a doc "Related" entry, and the
  feature file's mutation-manifest addition, all pre-dating this QA
  session's own diff — `git diff --stat` from the prior QA-approved
  commit onward shows only the doc/feature/evidence file additions).
- Hotfix `4ed88430b2` itself not reimplemented, rewritten, or reverted.

## Verdict

PASS. Landing on `main`; `Hotfix-Certification` stays `pending` until the
human records the ledger decision separately (this ticket does not flip
it).
