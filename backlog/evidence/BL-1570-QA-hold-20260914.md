# BL-1570 QA hold — unowned red found during property-suite run

- **Parcel commit**: f016c869a0 (Merge documenter b7e6902759 into QA.)
- **Task**: BL-1570-the-liveness-undetermined-tests-build-the-no-proc-case-on-every-host

## What was run

- Both shell tests (`test_operator_runtime_fixture_reaper_sweep_liveness_undetermined.sh`,
  `test_operator_runtime_sandbox_sweep_liveness_undetermined.sh`): ALL CHECKS PASSED.
- `pre_qa_gate.sh` (required_wiring): OK.
- `npm test` (unit suite, extension/): 616 files / 10479 tests, all green.
- `run_acceptance.sh specs/features/BL-1570-...feature`: 5/5 passed.
- `npm run test:properties`: two full runs over the whole property lane produced a
  DIFFERENT set of failing files each time (`bl1429StandingRedThrottleFoldInvariants`,
  `bl1474ReplayCommitRefusalReasonInvariants` on run 1; `bl1297MergeOwnPathsInvariants`,
  `bl1315OwnPathsFullRangeInvariants`, `bl1343ReplayNeverDropsOwnPathInvariants` on run 2).
  None of these files are touched by, or related to, BL-1570's diff (two shell test
  files + one step-handler file, all liveness/proc-dir specific).
  `bl1297MergeOwnPathsInvariants` is already registered (`BL-1564`, lane-timeout class).
  Isolating the four other suspects and re-running them alone:
  `bl1474`, `bl1315`, `bl1343` all pass in isolation (confirms lane-timeout flake,
  same root cause as BL-1564).
  `bl1429StandingRedThrottleFoldInvariants` FAILS even in isolation:
  `property: the recommended cap is always exactly the fold of the rework and
  standing-red caps... 11 !== 12` — a generator-coverage sampling gap (missing
  `severe:count` out of 12 rework×standing combinations), same class as the
  BL-1559 (bl983) sampled-coverage-floor flake, NOT a lane-timeout artifact.

## Disposition

- `test/bl1429StandingRedThrottleFoldInvariants.property.test.js` is an UNOWNED
  red: no row in `backlog/standing-reds.tsv`, no open ticket found by grep.
  Per Article 4.2 / the 2026-09-05 standing-red-register amendment, QA withholds
  approval until it is registered. This is not a BL-1570 defect (unrelated file,
  unrelated domain) and is not bounced to the coder.
- Filed an `unowned-red` note (priority 00) to specifier + coordinator naming this
  file and this evidence file. Parcel is completed (merge-only, no forward) and
  waits for a resume once the owning ticket is minted.

## Resume (2026-09-14, same day)

- Specifier note `BL-1570: bl1429StandingRedThrottleFold owned by BL-1572
  679cbd96ab - resume land` received. `backlog/standing-reds.tsv` now carries
  a `BL-1572` row for `extension/test/bl1429StandingRedThrottleFoldInvariants.property.test.js`
  (merged in via `Merge main cf56d011b6 into QA.`) — the red is owned.
- Merged up to `origin/main` (coordinator note: branch could not fast-forward
  to `7727867508`) before re-checking.
- Re-ran the previously-blocking test in isolation
  (`npx vitest run --config vitest.properties.config.mjs
  test/bl1429StandingRedThrottleFoldInvariants.property.test.js`): 4/4 passed
  this run — consistent with the register's ~6.4% sampled-miss description,
  not a new signal.
- Re-verified this ticket's own hardener merge (`d61ba51a8b`) is an ancestor
  of the commit being landed. Nothing else in BL-1570's own gates changed.
- **Approved.** Landing at `HEAD` (`Merge main cf56d011b6 into QA.` plus this
  resume note).
