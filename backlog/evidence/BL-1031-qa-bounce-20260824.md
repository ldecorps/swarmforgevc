# BL-1031 — QA bounce — 20260824

## Review inventory (Article 4.4)

Received documenter tip `1da60aa34c` (lineage: coder `b03e1263bb`, cleaner
`8ad9e36125`, architect `323685e6ec`, hardener `65f851e7d9`). Fast-forwarded
into `swarmforge-QA`. Sibling: `VERIFY BL-1031` (exit 0). HOTFIX pack/board
MATCH `27273f2b0a`; BL-1113 acceptance **9/9**.

Tip correctly routes spawn-reachable call sites through `sh!` and fail-closes
wait-bound hits in `acceptance_contract_gate_lib`. The chokepoint those sites
now share does **not** reliably fire exit **124** on depth≥2 pipe-holders —
the exact defect class BL-1021/BL-1031 exist to close. Hardener recorded a
single green `guard ALL PASS` and did not run the property lane; under
repeat the unit undrainable case and P1/P3 flake hard.

| Gate | Result |
|---|---|
| Sibling | PASS — `VERIFY BL-1031` |
| Scope / wiring | PASS — three libs via `sh!`; steps in `index.js` |
| Diff intent (routing + fail-CLOSED) | PASS on green runs; undermined by D1–D3 |
| Unit (`daemon_cycle_guard_lib_test_runner.bb`) | **FAIL intermittent** — D1 |
| Properties (`daemon_cycle_guard_lib_property_runner.bb`) | **FAIL intermittent** — D2 |
| Acceptance (BL-1031 feature) | **FAIL intermittent** — D3 (also green on lucky runs) |
| Acceptance-contract lib + gather runners | PASS (stable across runs) |
| Hotfix stamp-off | PASS — MATCH `27273f2b0a`; BL-1113 **9/9** |
| Extension unit (shared lane) | Standing reds **BL-1112** (out of tip) |
| Mutation / CRAP / DRY | DEGRADED — `.bb` untooled (hardener Gherkin/surgical recorded upstream) |
| Orphans | NONE in QA worktree after verification |

### D1 — unit — blamed: coder

- **Failing command:**
  `bb swarmforge/scripts/test/daemon_cycle_guard_lib_test_runner.bb`
- **Commit hash checked:** `1da60aa34c` (and post-tip QA HEAD containing it)
- **First error excerpt:**
  ```
  FAIL the undrainable call is reported as a bounded-wait timeout
    expected: 124
    actual:   0
  FAIL the timeout error names the bound and the command
  FAIL the timeout is ANNOUNCED, attributed to the sweep that owns the call - never silent
    expected: "dispatch-gap-sweep"
    actual:   nil
  FAIL the announcement carries the command
  ```
- **Failure class:** `unit`
- **Expected vs observed:** Depth-2 undrainable fixture
  (`bash -c 'echo child-output; sleep 5 & exit 0'`) under a 200ms bound must
  return `{:exit 124}` and fire `on-timeout!`. Intermittently returns
  `{:exit 0}` with **no** announcement (silent success). Stress: **1/15** red.
- **Remediation:** In `daemon_cycle_guard_lib.bb` `await-bounded-process` /
  undrainable fixture path, make depth≥2 pipe-hold timeouts deterministic on
  this host (WSL). A single green run is not enough — the unit case must hold
  under repeat. Owner: **coder**.

### D2 — unit (properties) — blamed: coder

- **Failing command:**
  `bb swarmforge/scripts/test/daemon_cycle_guard_lib_property_runner.bb`
- **Commit hash checked:** `1da60aa34c`
- **First error excerpt:**
  ```
  FAIL P1 … depth-2 hang exit 0, not 124
  FAIL P1 … depth-3 hang exit 0, not 124
  FAIL P3 … a depth-2 hang ended the sweep with exit 0 and no announcement
  FAIL P3 … a depth-3 hang ended the sweep with exit 0 and no announcement
  ```
- **Failure class:** `unit`
- **Expected vs observed:** Stratified hang draws at depth 2/3 with
  `p1-bound-ms 150` must hit 124 + full announcement. Same silent `exit 0`
  path as D1. Stress: **3/5** property-lane reds.
- **Remediation:** Same as D1 — durable bound at any process depth; re-run
  property lane to green under repeat before forwarding. Owner: **coder**.

### D3 — acceptance — blamed: coder

- **Failing command:**
  `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1031-bounded-chokepoint-covers-the-spawn-reachable-subtree.feature`
- **Commit hash checked:** `1da60aa34c`
- **First error excerpt:**
  ```
  ✖ no file the daemon can reach carries an unbounded subprocess call
    FAIL the undrainable call is reported as a bounded-wait timeout
      expected: 124
      actual:   0
    FAIL the timeout is ANNOUNCED … actual: nil
  ```
- **Failure class:** `acceptance`
- **Expected vs observed:** Scenario 01's gate embeds the undrainable
  chokepoint check; same flake as D1. Later re-runs can show **7/7** — do not
  treat a lucky APS green as done-done.
- **Remediation:** Same as D1. Owner: **coder**.

## Verdict

BOUNCE to **coder**. Do not land on `main`. Fix the depth≥2 bounded-wait
reliability (or its fixture) so unit + property + APS hold under repeat;
then remaining chain → QA.

By QA.
