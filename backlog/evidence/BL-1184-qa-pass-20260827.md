# BL-1184 — QA pass — 20260827

**Received:** `merge_and_process documenter 59039688f4` (handoff
`00_20260827T185419Z_000858_from_documenter_to_QA`)
**Cited commit (this ticket's own work, verified below):** `57a635292`
**Landed commit (after publish-time rematch onto origin/main):** `20eac7683`

## Ancestry check (BL-336 guard)

- `git merge-base --is-ancestor 6ba3f0fd6 57a635292` (coder) — CONFIRMED
- `git merge-base --is-ancestor 80c0537c2 57a635292` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 803b4f038 57a635292` (hardener) — CONFIRMED

`57a635292` is the QA-received merge that actually contains BL-1184's own
stage commits, not a sibling ticket's tip.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1184` | `VERIFY BL-1184` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean |
| Unit suite | `npm test` | 46 files / 202 tests fail, ALL `CURSOR_API_KEY is not set for the headless bridge` — standing BL-720/BL-728 host-env debt, identical disposition to BL-1175's own QA pass one commit prior. None touch metrics/briefing/shiftVelocity. `test/shiftVelocity.test.js` (17 tests) green. |
| Property suite | `npm run test:properties` | 27 files fail; 26 of 27 match `property_suite_standing_allowlist.tsv` (BL-1175 standing reds) verbatim. The 27th, `bl586PipelineBoardTopicIdentity.property.test.js`, is a generator-reach assertion unrelated to this ticket's domain (pipeline-board topic identity, not touched by BL-1184) — reran in isolation 5/5 green, so it is a full-suite/parallel-load flake, not a regression from this change. `test/bl1184BriefingShiftVelocityInvariants.property.test.js` (3 tests) green. The 2 `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors are the known-benign allowlisted artifact. |
| Acceptance | `run_acceptance.sh specs/features/BL-1184-briefing-shift-velocity.feature` | 6/6 scenarios PASS |
| Wiring (required_wiring, all 5 rows, hand-verified against HEAD) | grep each anchor | `maxRollingEightHourLandedForDay` (shiftVelocity.ts) present and used; `appendShiftVelocityRecord` (shiftVelocityTelemetryStore.ts) present, called from `render-briefing-shift-velocity.ts`; `briefing-shift-velocity-json` (handoffd.bb) present and wired into `briefing-diagram-section` as the third source; `shift-velocity-source-fn` (briefing_email_lib.bb) present, `diagram-section-from-sources` 3-arity accepts and renders it; `bl1184BriefingShiftVelocitySteps` required in `specs/pipeline/steps/index.js` and is a real 189-line handler (not a stub), exercised by the acceptance run above. |
| Invariants | manual read | "done/ closes only" — `closedTimesMsFromLifecycles` reads `closeDateIso` off the shared `deriveTicketLifecycles` adapter only. "No second history reader" — `shiftVelocity.ts` imports `runGitLog`/`deriveTicketLifecycles` from the existing `gitHistoryAdapter`, no new reader. "Non-linear time axis" — `shiftVelocityChart.ts` uses a logarithmic age warp (`Math.log(1+age)/Math.log(1+maxAge)`), confirmed non-linear. |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline: acquired `.swarmforge/land-main.publish.lock`,
`land_main_publish.sh --decide-only` reported `tip-contains-origin: false`
(origin/main had advanced by one unrelated specifier evidence commit,
`2ff836a56`, spec(BL-1200)), rematched via plain `git merge --no-ff
origin/main` (`20eac7683`), confirmed diff against both parents dropped
nothing, re-ran `--decide-only` → `purity-action: push`, `git push origin
HEAD:main` (`2ff836a56..20eac7683`), released the lock.

BL-1184 carries no `GH-` id — no `issue_done.sh` step required.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter;
coordinator notified for backlog bookkeeping (BL-1184 → `backlog/done/`).

By QA.
