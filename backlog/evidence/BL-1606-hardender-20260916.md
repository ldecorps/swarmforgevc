# BL-1606 — hardender review pass, 2026-09-16

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: hand-mutation sweep of extension/test/helpers/propertyLaneContentionBudget.js (ceiling fix)
- **Commit hash**: c5a5afbd7e
- **First error excerpt**: mutating PROPERTY_LANE_MAX_GROWTH from 3 to 1 survived every existing assertion - the never-below-base property only checks >=, satisfied by equality when growth collapses to 1; the sibling strictly-grows test only ever drives a 20000 base, which never approaches the shared 120000 ceiling regardless of growth
- **Failure class**: test-coverage-gap
- **Expected vs observed**: a test proving strict growth under load specifically for a base at/above the shared ceiling (bl968s own 240000)
- **Blamed role**: hardener
- **Remediation pointer**: added a dedicated test with a fork-count band chosen to stay clear of the ceiling clamp point; confirmed it passes on the real fix (7/7) and fails on the mutant (6/7) (commit c5a5afbd7e)

By hardender.
