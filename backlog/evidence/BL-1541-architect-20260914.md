# BL-1541 — architect review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- Merged cleaner 032308f492 into architect (`779e1cf1eb`).
- Two-layer / webview / secrets / integrate-not-fork checks: N/A — no
  extension host, webview, or SwarmForge-source-modifying code in this
  parcel; test-only bb scripts, a shared test lib, and one acceptance step
  handler.
- Dependency-gate (`extension/out/tools/dependency-gate.js`): N/A, no
  changed file is under `extension/`'s dependency-cruiser scope.
- Co-change report over all 8 changed files: no coupling beyond the
  parcel's own files (expected — they're one sitting per the ticket's
  INVEST-S note).
- Read every diff to the five runners plus the new
  `send_through_audit.bb`/`bl1541_send_through_audit_contract_harness.bb`:
  each `send!` site now threads the byte-identical draft/thunk through
  `send-through-audit-lib/send-through-audit!` and takes only the second
  call's result; no site reads the first call's exit code or output.
  bl982's invariant-2 rewrite (`project-first-8-cols`) matches the
  2026-09-14 amendment's projection direction exactly: an appended 9th+
  column no longer diverges the oracle, a mutated value inside the first
  8 still does (row-length guard + projected-vs-pre-blob comparison).
- Invariants Review (ticket's declared invariant: "queue assertion reads
  the mailbox, not the CLI's exit code"): confirmed by code read — every
  runner's send-result handling slurps/reads the actual queued/mailbox
  file after checking exit, never treats `(zero? exit)` alone as proof of
  delivery; already true pattern, unchanged by this parcel except for now
  actually reaching a queued state. Scenario 03 covers the shared helper's
  own two branches (queued/refused) — its only two reachable branches, so
  no property-test gap.
- Property Testing (BL-654 scope: undeclared properties on touched pure
  modules): `send-through-audit!` is a 2-line, branchless function whose
  only behavior is invoking a thunk twice — no additional property beyond
  what scenario 03's two example rows already exercise.
- Ran `run_acceptance.sh` on the feature: 7/7 pass.
- Ran each of the five runners individually, with enough wall-clock room
  (up to 290s) rather than a short `timeout` — `bb`-spawn volume is now
  ~2x per send, which pushed bl991/bl992 past 110-120s and past an
  initial short timeout I used, producing spurious spawn-time errors
  (`Cannot run program "bb"`, empty-output `send failed:`) that were
  timeout-kill artifacts of my own harness, not the parcel: bl991 and
  bl992 both later completed cleanly (`ALL 80 SENDS PASSED`,
  `ALL PROPERTIES HOLD`) with a longer window. bl951 and bl982 passed on
  the first run each (`ALL PROPERTIES HOLD`, no
  "diverged from pre-change script" line — invariant 2 green against the
  real pre-blob, matching qa_e2e_procedure step 2).
- `git diff main..HEAD -- swarmforge/scripts/swarm_handoff.bb
  swarmforge/scripts/handoff_lib.bb` empty — constraint held.
- **Non-blocking observation, routed as a note (not a bounce) to
  specifier + coordinator, same pass**: bl983's `:two-seat` coverage floor
  (>= 6 of 16 draws, `n-seats = 2 + rand-int*(2)`, i.e. a fair 50/50 draw
  per iteration) has a Binomial(16, 0.5) chance of ~10.5% of legitimately
  landing below 6 on any given run — pre-existing on `main`, unrelated to
  this fix, and newly OBSERVABLE only because the two-call fix lets the
  runner reach this check at all (previously it always died earlier on
  the audit-challenge miscount). Reproduced: 2 of 4 full local runs hit
  exactly `:two-seat 5`; the other 2 passed. Out of scope here — the
  ticket's own constraints forbid loosening any runner's existing
  coverage floors — but worth the specifier/QA knowing this specific
  runner can legitimately flake on an unrelated axis when read against
  the standing-red register or a QA re-run.

By architect.
