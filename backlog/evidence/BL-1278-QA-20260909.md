# BL-1278 — QA review pass, 2026-09-09

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Verified commit 6747e6ef3f (documenter) on swarmforge-QA. Merged
documenter 6747e6ef3f into QA worktree as merge commit.

**Verification order:**

1. **Orphaned process check**: no orphaned test/mutation processes before
   or after verification.

2. **Compilation**: `cd extension && npm run compile` — clean, no errors.

3. **Unit tests**: `cd extension && npm test` — 10,433/10,433 pass (614
   test files). Suite duration 41.52s (over budget due to pre-existing
   slow files, not BL-1278).

4. **Acceptance pipeline**: `bash specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-1278-approval-card-shows-the-description-not-the-mint-notes.feature`
   — 3/3 scenarios pass:
   - Scenario 01: approval card shows description (not notes)
   - Scenario 02: ticket without description falls back to notes
   - Scenario 03: task-started card follows same rule

5. **Property tests**: `cd extension && npm run test:properties` — 383/388
   files pass, 5 files fail with timeouts (20000ms limit). Analysis:
   - bl1089FrontDeskLivenessFixture.property.test.js: 1 timeout
   - bl1272LandedSiblingInvariants.property.test.js: 1 timeout
   - bl1297MergeOwnPathsInvariants.property.test.js: 1 timeout
   - bl1343ReplayNeverDropsOwnPathInvariants.property.test.js: 1 timeout
   - bl1373PathSetCacheInvariants.property.test.js: 2 timeouts
   - bl1474ReplayCommitRefusalReasonInvariants.property.test.js: 1 timeout

   **These are environmental timeouts, not BL-1278 regressions.** Evidence:
   - None of these test files were touched by BL-1278 (coder changed
     conciergeTick.ts, topicRouter.ts, swarmEventStream.ts,
     telegram-front-desk-bot.ts; BL-1278 is about approval card field
     wiring, unrelated to land-step/merge/replay/path-set-cache logic).
   - BL-1474 hardener evidence (2026-09-08) documents that these property
     tests timeout under concurrent system load and pass when run in
     isolation (bl1474 test was green at 7.4s when run alone).
   - The property test suite took 283.67s to run with tests totaling
     1660.64s of execution time, indicating heavy system load.
   - Per Article 4.2: "A red OUTSIDE your parcel is already ticketed
     until you have proved otherwise." These have tickets (BL-1089,
     BL-1272, BL-1297, BL-1343, BL-1373, BL-1474) and are documented as
     load-sensitive.

6. **Code review**: wiring is correct and complete:
   - backlogReader.ts already parses description from YAML (BL-117)
   - telegram-front-desk-bot.ts toFoldersSnapshot now includes description
   - conciergeTick.ts BacklogFolderItem now has description field
   - conciergeTick.ts ticketSummariesFor builds TicketSummary with description
   - swarmEventStream.ts TicketSummary interface now has description
   - topicRouter.ts taskStartedText and approvalRequestedText use
     withResolvedProblemStatement helper (prefers description, falls back
     to notes)
   - buildSummaryBody applies firstParagraph to both description and notes
   - Message-length invariant maintained (truncate calls still in place)

7. **Scope check**: coder touched only BL-1278 files (conciergeTick.ts,
   topicRouter.ts, swarmEventStream.ts, telegram-front-desk-bot.ts,
   conciergeTopicRouting.test.js, bl1278ApprovalCardShowsDescriptionSteps.js).
   Other files in the diff (docs/briefings, bl1410 steps) came from main
   merges, not BL-1278's own work.

**Conclusion**: BL-1278 satisfies its acceptance criteria, tests pass,
wiring is correct, no regressions. Property test timeouts are
environmental (system load), not BL-1278 regressions, and are ticketed
per Article 4.2. Approval stands.
