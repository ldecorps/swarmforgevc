# BL-677 hardener — hand-authored mutation sweep, 2026-09-18

No Scenario Outline in `specs/features/BL-677-epic-backfill-apply.feature`
(all 8 scenarios are plain `Scenario:`), so BL-113 Gherkin mutation is
inapplicable (BL-638). Babashka has no wired mutation/CRAP/DRY tool
(engineering.prompt's degraded-fallback rule), so this is a hand-authored
surgical mutation sweep over `epic_backfill_apply_lib.bb`'s pure functions,
the same shape `expedite_mutation_sweep.sh` established for `.bb` code
(BL-567).

## Mutants tried

1. **`refusal`'s cond order** (stale-mapping vs unknown-epic) — swapped
   the two clauses. SURVIVED every existing test: each test triggers only
   one of the two failure conditions at a time, so which one is checked
   first was invisible. A row that is both missing from `done/` and
   carries an unknown value discriminates the order — the function's own
   doc comment names the intended order ("staleness... then value
   validity"). Closed by a new unit assertion pinning `:stale-mapping` on
   such a row.

2. **`classify-row`'s cond order** (already-tagged vs blank-proposal) —
   swapped the two clauses. SURVIVED every existing test: the
   blank-proposal tests use `current-epic nil`, and the already-tagged
   test uses a non-blank proposal, so neither exercises a row that is
   BOTH already-tagged and carries a blank proposal. Closed by a new unit
   assertion pinning `:skip-already-tagged` on such a row — matches the
   function's own doc comment ("idempotency... falls out of this alone").

3. **`with-epic-line`'s inserted value** — appended an extra character to
   the written epic value. KILLED immediately by the existing
   byte-for-byte assertion. No action needed.

Both real gaps (1, 2) are closed in
`swarmforge/scripts/test/epic_backfill_apply_lib_test_runner.bb`; both
mutants re-verified to fail against the updated test file, then the
mutated scratch copies were deleted (never committed).

## Verification

- `bb swarmforge/scripts/test/epic_backfill_apply_lib_test_runner.bb`:
  ALL PASS (now 29 assertions, was 27).
- `run_acceptance.sh` on
  `specs/features/BL-677-epic-backfill-apply.feature`: 8/8 scenarios pass.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl677EpicBackfillApplyInvariants.property.test.js`: 3/3 pass (the
  coder's own non-vacuity checks for all three declared invariants are
  recorded inline in that file).
- Standing whole-tree guards (this parcel adds a file under
  `specs/pipeline/steps/`): all 20 `test/*Guard*.test.js` files (excluding
  `.property.` siblings) pass, 206 tests.
- CRAP/DRY: not wired for `.bb` (Babashka has no mutation/CRAP/DRY per
  engineering.prompt); gated by the unit-test suite above only, per the
  documented degraded fallback.

By hardender.
