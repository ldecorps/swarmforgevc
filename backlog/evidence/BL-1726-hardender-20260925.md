# BL-1726 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Rebuild pass, after QA held the parcel (evidence `91d5967fe5`) and the
specifier's send-back (`backlog/evidence/BL-1726-bounce-20260925.md`): D1
(invariant-unencoded, coder) rebuilt with a new property test; D2
(spec-gap) ratified and D3 (spec-gap) struck by the specifier, both
already reflected on this branch — nothing left for me on D2/D3 beyond
confirming they hold. Superseded the earlier same-epic hardener pass
(`backlog/evidence/BL-1726-hardender-20260924.md`, the pre-send-back
build) — re-hardened the rebuilt code rather than trusting it.

Scope: `swarmforge/scripts/orphan_janitor_lib.bb`
(`ollama-own-llama-server-cmdline?`/`reapable-ollama-ghost?`),
`orphan_janitor_lib_test_runner.bb`, the new property test, and the three
D2-ratified BL-1705 paths (feature Examples row 2, step handler audit
assertion, acceptance runner fixture) — Babashka-only (BL-472: no
mutation/CRAP/DRY wired); no `src`/`out` production code touched.

Checks run (host load moderate, ~15-24 on 20 cores):
- `bb swarmforge/scripts/test/orphan_janitor_lib_test_runner.bb`: ALL
  CHECKS PASSED.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1726OllamaLlamaServerOwnershipMarks.property.test.js` (from
  `extension/`): 1/1 passed.
- `node specs/pipeline/cli.js specs/features/BL-1726-the-ghost-reaper-touches-only-ollamas-own-llama-server.feature`:
  5/5 ok.
- `node specs/pipeline/cli.js specs/features/BL-1705-the-orphan-janitor-reaps-ghost-ollama-runners-and-detached-run-clients.feature`:
  8/8 ok — the D2-ratified Examples row 2 (both ownership marks present,
  still reaped) confirmed live, not just read.
- `git diff --stat f93ebdf4af..HEAD -- <Scope files>`: exactly the
  ratified Scope (the janitor lib + its test runner, the new property
  test, the handler + its CLI probe fixture, and the three D2 BL-1705
  paths) — nothing else.

Third independent non-vacuity check (coder mutated the `--model` blob
regex; architect independently re-derived the same mutation and confirmed
it; this pass mutated a DIFFERENT site — the call-site boolean logic in
`reapable-ollama-ghost?` itself, not the ownership predicate's internals —
to cover a mutant class neither prior pass touched):
- Hand-mutated `(llama-server-cmdline? cmdline) (and
  (ollama-own-llama-server-cmdline? cmdline) (not
  parent-live-ollama-serve?))` → `(or ...)` in place (no detached job
  outstanding).
- `orphan_janitor_lib_test_runner.bb` against the mutant: FAIL — "a
  hand-started llama.cpp server with a dead parent is never reaped",
  expected false got true. Killed by the pre-existing bb unit case.
- The property test against the same mutant: FAIL on its first
  constructed cell (`ollama-lib`/`ollama-blob`/`parentLiveOllamaServe:
  true` — an ollama-owned runner with a LIVE ollama-serve parent, which
  must NOT be reaped; the `or` mutant reaps it anyway since
  `ollama-own-llama-server-cmdline?` alone is true). Killed.
- Restored the file byte-for-byte (`git status --short` clean); re-ran
  both suites green before finalizing (bb: ALL CHECKS PASSED; property:
  1/1 passed).

Fixture hygiene: no leaked processes or files — this ticket's checks are
pure predicate reads over constructed command-line strings, no real
process spawning or tmux involved; `git status --short` shows only the
three untracked files that predate this session.

Not run: `npm run test:properties` in full (2026-09-17 rule — only the
one relevant property file, no other `*.property.test.js` in this
parcel's Scope) and Stryker/CRAP (no `src`/`out` file in Scope).

By hardender.
