# BL-989 QA bounce — 20260825

**Reviewed tip:** documenter `6c1a9dc25e` (not landed)
**Task:** `BL-989-grep-dash-p-is-gnu-only-breaks-on-macos-bsd-grep`
**Sibling:** `VERIFY BL-989`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | PASS — paths=13, dels=0 (at claim) |
| Sibling | VERIFY |
| Property `bl989PortableGrepTabAnchor.property.test.js` | 3/3 |
| `test_role_lifecycle_cli.sh` | ALL CHECKS PASSED |
| APS BL-343 feature | 7/7 |
| Hardener stage deliverable | **FAIL → D1** |

## Inventory

### D1 — `behavior` (blame: hardender)

**Failing command:**
```
git ls-tree -r --name-only 6c1a9dc25e | rg 'BL-989-hardener'
# (no matches)
```

**Commit hash checked:** `6c1a9dc25e`

**First error excerpt:**
```
required_stages: [coder, cleaner, architect, hardender, documenter, qa]
tip evidence: architect, cleaner, documenter (+ rematch)
tip evidence missing: BL-989-hardener-pass-*.md
no stage_skip_reasons for hardender
architect pass: "Pass — forward to hardender"
```

**Failure class:** `behavior`

**Expected vs observed:** Hardender stage required. Observed documenter tip
with no hardener pass and no skip reason — missing stage entirely.

**Remediation:** Hardender rematch tip-pure from current `origin/main`,
record hardener pass (or honest skip), then remaining chain to QA.

## Verdict

BOUNCE → hardender. Do not land.

By QA.
