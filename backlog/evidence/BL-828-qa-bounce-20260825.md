# BL-828 QA bounce — 20260825

**Reviewed tip:** documenter `814aec567f` (not merged — tip impure)
**Task:** `BL-828-bubble-collapsed-gesture-model`
**Sibling:** `VERIFY BL-828`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** — tip not landable; not merged |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 814aec567f  # fails
comm -12 <(git ls-tree -r --name-only origin/main | sort) \
  <(git diff --diff-filter=D --name-only origin/main 814aec567f | sort) | wc -l
# → 3
```

**Commit hash checked:** `814aec567f`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = 104b25876 briefing due refresh)
paths=22 dels=3
deleted on tip vs present on origin/main:
backlog/INTAKE-prioritize-local-ollama-remaining-20260825.md
backlog/paused/BL-1125-epic-local-ollama-swarm-readiness.yaml
backlog/paused/BL-1142-local-ollama-mono-vs-forge-cpu.yaml
```

**Failure class:** `behavior`

**Expected vs observed:** Expected tip-pure rematch from current `origin/main`
authorizing **BL-828 paths only**. Observed stale tip that would delete
landed intake + paused local-ollama tickets.

**Remediation:** Rematch onto current `origin/main`; keep those three paths;
`dels_on_origin=0`; `origin/main` must be ancestor.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
