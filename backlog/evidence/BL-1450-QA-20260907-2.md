# BL-1450 — QA re-verification pass (post-bounce rework), 2026-09-07

NONE. The full checklist was run and found no defect. The bounced D1 is
confirmed fixed.

By QA.

## The rework

Coder raised the file's internal `testTimeout` 120000ms → 240000ms
(`backlog/evidence/BL-1450-coder-rework-20260907.md`), comfortable margin
over my own worst pooled-run observation (149518ms) while staying well
under the original 300000ms this ticket lowered from. No other line
touched — confirmed: `RUNS_PER_CELL = 1`, floors still derived
(`CLASS_FLOOR`/`DEPTH_FLOOR`), no property deleted or weakened.

The coder's own evidence also documents independently catching a
merge-deletion-guard trap when merging my bounce back into their branch
(git's 3-way merge would have silently reverted the WHOLE BL-1450 fix,
not just the timeout, and reintroduced a stale standing-reds row) —
confirmed no such regression: `RUNS_PER_CELL`/floors/spawn-count are all
still correct, and `backlog/standing-reds.tsv` carries no BL-1450 row.

## Verification — the exact scenario that failed twice, now clean twice

- Per-file budget (3 solo runs): 45.2s / 47.9s / 46.0s, all well under 60s
  and the new 240s timeout, each printing the coverage line with every
  class/depth present.
- **The actual failing scenario, re-run twice**: full, unscoped `npm run
  test:properties` (the pooled lane) — bl968 completed at **133528ms**
  and **133979ms** respectively, both comfortably under 240000ms and
  consistent with each other (unlike the pre-fix 126046ms/149518ms pair,
  which both exceeded the old 120000ms). Neither run shows bl968 among
  the failed files. This is the literal qa_e2e step 3 criterion my bounce
  was about, now satisfied twice in a row.
- Acceptance: BL-1450's own feature 5/5 pass.
- Sibling features unaffected: BL-968 3/4 (same known BL-1462-owned
  disposition, unchanged), BL-1062 4/4.
- `backlog/standing-reds.tsv`: no BL-1450 row (correctly not
  reintroduced, matching the coder's own claim).
- No file under `extension/src` changed by this rework (only the
  `.property.test.js` file, excluded from `npm test`'s scope) — the full
  compiled unit suite's last-established green state (611/611, from
  verifying BL-1452) stands unaffected.
- No orphaned `node --test`/vitest processes remained after the two
  full-lane runs (checked by PID/cwd, not bare `pgrep` self-match).

## Disposition

Approved. The bounced defect is fixed, verified against the real failure
mode (not just the solo-run measurement that missed it the first time).
Landing next.

By QA.
