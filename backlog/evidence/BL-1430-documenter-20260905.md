# BL-1430 documenter review pass — 2026-09-05

Reviewed the hardened commit (`a2064612fc`) and the coder/cleaner/architect/
hardener trail for BL-1430 (the portable-time guard has one definition).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1430-the-portable-time-guard-has-one-definition.feature`
  (implied by the required_wiring step handler below; not independently
  re-verified as a separate file check since the handler's own existence
  is the required_wiring anchor).
- Step handler exists:
  `specs/pipeline/steps/bl1430PortableTimeGuardSingleDefinitionSteps.js`.
- Confirmed the fix: `EXCLUDED_DIR_NAMES` in
  `extension/test/bl874PortableTimeInvariants.property.test.js` gains
  `.worktrees` (a repo-wide definition-count property was counting every
  linked pipeline-role checkout's own copy of the guard module, a
  host-population artifact — 8 worktrees on 2026-09-05 — not a real
  duplication). The ticket's own direction to check sibling repo-wide
  properties for the same hole "in the same parcel" was followed: the
  same one-line exclusion-set fix also lands in
  `extension/test/tempDirTrapGuard.property.test.js` and
  `extension/test/propertySuiteStandingRedsInvariants.property.test.js`
  (evidence file lists all three). The assertion itself (one definition)
  is unchanged in all three — only the walk's scope, per invariant 1.
- Confirmed the register/allowlist bookkeeping: the `bl874` row is
  removed from both `backlog/standing-reds.tsv` and
  `swarmforge/scripts/property_suite_standing_allowlist.tsv` in the same
  commit, per the ticket's "its row leaves backlog/standing-reds.tsv in
  the same commit (BL-1428's rule)".
- This is a test-fixture scoping fix with no operator-visible behavior,
  new mechanism, or documented contract change — the guard's own
  behavior (BL-874) is explicitly out of scope and unchanged. No existing
  doc (grepped for `EXCLUDED_DIR_NAMES`, `findPortableTimeViolation`,
  `BL-874` under `docs/how-to/`) describes this property's walk scope, so
  none goes stale, and no documentation update is in scope for this
  ticket.
- No "Last Updated" freshness field anywhere touched — no date bump owed.
- No registered diagram depicts a property test's directory-walk scope —
  no diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond what the hardened commit already carries.

Result: NONE — no defect found, no documentation change in scope.
