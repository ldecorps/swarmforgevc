# BL-1362 — architect review, 2026-09-04

Reviewed coder commit merged in via cleaner `1109c758af`.

## Checks run

- Dependency gate on the three new tool files plus the step handler:
  PASSED, no forbidden edges.
- Co-change: brand-new files, no history yet; nothing to flag.
- `npm run compile`: clean (needed to run the vitest suites, `out/` is
  gitignored and absent from a fresh checkout).

## Re-verified independently, not trusted from evidence

- `reviewEvidenceRecord.test.js`, `recordReviewEvidenceCli.test.js`,
  `recordReviewEvidenceGate.test.js` — 18/18, including the two-sided
  gate check (recorded commit passes `review_forward_evidence_gate_lib.bb`,
  the bare received hash is still refused — confirms the gate itself was
  not weakened).
- `npx vitest run --config vitest.properties.config.mjs
  bl1362ReviewEvidenceByToolInvariants` — 2/2.
- `run_acceptance.sh` on the BL-1362 feature — 9/9.
- `check_feature_handler_registration.sh` — exit 0.

## Correctness read

- `commitPath` correctly stages and commits exactly `git add -- <relPath>`
  / `git commit -q -m ... -- <relPath>`, never `-A` — read directly, this
  is the invariant that lets the tool run against a deliberately dirty
  tree without folding unrelated work into a parcel (BL-506), and it is
  the same discipline this session's own reviews have been enforcing
  throughout.
- The verdict is validated (`parseVerdict`) BEFORE any file write or git
  operation — a refusal genuinely leaves nothing behind, matching
  invariant 3 and confirmed by the CLI test's refusal case.
- `ARTICLE_44_ITEM_FIELDS` is asserted against `QA.prompt`'s own five
  numbered fields by a parsing test rather than restated by hand — the
  BL-897 cross-boundary-constant discipline correctly applied, avoiding a
  second copy of the constitution that would silently drift.
- `main()` is a genuinely thin wrapper (`makeArgsGuardedMain`/
  `runCliMain` seams, `commitFn` injected rather than an env-var bypass)
  over the exported, independently-testable `recordReviewEvidence`.
- Commit messages the tool generates end `By <role>.`, matching the
  constitution's byline rule.
- No production-wiring anchor was declared, and the ticket's own reason
  (the only call site an anchor could name is this parcel's own — green
  by construction; the prompt changes that make this the standard route
  are the specifier's, per BL-798) is correct and consistent with how
  this session's other unanchored tickets have been scoped.

## Verdict

COMPLIANT. Forwarding to hardener.
