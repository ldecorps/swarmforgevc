# BL-1739 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Third architect pass on this ticket. D1 (2nd bounce: `isLiveCodePath`'s
root catch-all `!rel.includes('/')` admitted plain docs/config alongside
launch scripts) is fixed: `!rel.startsWith('.') && (rel.endsWith('.sh') ||
!rel.includes('.'))` — a structural, extension-shaped check rather than a
curated name list, matching the ruling's own anti-curation intent one
level down.

Independently re-verified both directions myself (not just read the
coder's claim):
- Re-ran my own exact repro from the 2nd bounce (`echo "See history:
  launch_babysitter.sh was retired." >> README.md`, then the feature):
  now `ok 26` — the false positive is gone. Reverted `README.md`.
- Re-planted a retired-name mention in a scratch file under
  `swarmforge/scripts/` (a live-code surface): `not ok 26`, correctly
  still caught. Removed the scratch file.
- Hand-checked the fix's boolean logic against the FULL current root file
  list (`git ls-files | grep -v /`): every real launch script
  (`attach-swarm.sh`, `finish-shift`, `start-swarm*.sh` ×9,
  `stop-swarm.sh`, `swarm`, `swarm-kill`) classifies true; every doc/
  config/data file (`.gitignore`, `CONTRACT.md`, `HANDOFF.md`, `README.md`,
  `USE-CASES.md`, `bb.edn`, `swarmforge.lock.json`, `upstream-watch.json`,
  `vercel.json`) classifies false — matches by hand-tracing each case
  through the boolean expression, not just trusting the coder's own table.
- `node specs/pipeline/cli.js specs/features/BL-611-...feature`: 27/27.
- Scope (commit `34b7a475a7`): exactly the one Scope file
  (`bl611BabysitterdLifecycleSteps.js`) + evidence.

No further defect found. Both of this ticket's architect bounces are now
resolved.

By architect.
