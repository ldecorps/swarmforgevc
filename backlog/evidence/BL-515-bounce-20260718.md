# BL-515 QA Bounce Evidence

1. **Failing command**: `git merge-base --is-ancestor 36c76187 c8543c07ec` (lineage check per constitution/workflow.prompt "Forwarded Commits Carry Their Lineage")
2. **Commit hash tested**: `c8543c07ec9d128890d060d1d523ffc65472c4e9` (documenter's forwarded parcel, cited task BL-515-gherkin-lint-rejects-wrapped-step)
3. **First error excerpt**:
   ```
   $ git merge-base --is-ancestor 36c76187 c8543c07ec ; echo $?
   1
   ```
   `swarmforge-hardender`'s branch tip is `36c76187` ("BL-515: harden gherkin lint gate
   test coverage gaps" — adds a phantom-column test for a SPACE-containing Examples
   column name, plus two CLI-level arg-count-guard failure-path tests). `swarmforge-
   documenter`'s branch tip `c8543c07ec` ("BL-515: document gherkin lint gate rules...")
   has `8e540eb9a6` (the hardener's PRE-hardening merge-in-progress commit) as its direct
   parent, NOT `36c76187`. The documenter's parcel never merged the hardener's actual
   hardening commit before doing docs work and forwarding to QA — the two are sibling
   commits off the same parent, confirmed via `git branch --all --contains 36c76187`
   (only `swarmforge-hardender`) vs `git branch --all --contains c8543c07ec` (only
   `swarmforge-documenter`, `swarmforge-QA`).
4. **Failure class**: `behavior` (intent mismatch — the parcel QA was asked to approve
   does not contain the hardener's real, verified output; this is not a compile/unit/
   acceptance red, the existing suite is green — `test_gherkin_lint_gate.sh` passes
   9/9 in the documenter's as-forwarded state).
5. **Expected vs observed**: Expected — documenter's commit has the hardener's forwarded
   commit (`36c76187`) as an ancestor, per "Forwarded Commits Carry Their Lineage."
   Observed — documenter's commit and the hardener's hardening commit are siblings; the
   two new coverage-gap tests (space-containing phantom-column case, CLI arg-count-guard
   fail-fast case) are ABSENT from the tree QA received. Confirmed by grep:
   `grep -n "arg-count\|Usage\|malformed" swarmforge/scripts/test/test_gherkin_lint_gate.sh`
   shows only the pre-existing malformed-feature-file case, not the two new CLI guard
   cases the hardener's commit message claims to add.

Note: this is the same defect class as the constitution's cited BL-090/BL-103 precedent
("the documenter forwarded its docs commit without merging the hardener's verified
work"). Re-entry per QA.prompt goes to `coder`; producing-role attribution is
`documenter` (the stage whose forward dropped the lineage).
