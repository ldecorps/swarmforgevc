# BL-1480 — hardender review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- Received the architect-reviewed commit (`ab4437e2d6`, merge of `37057bc06b`
  architect evidence into hardender) with a WIP scenario 05 already staged
  (uncommitted) exercising `missingFromList`'s union over the no-limit
  fixture's four declared entry points.
- Ran the WIP scenario: passed, all 8 scenarios green
  (`run_acceptance.sh` on the BL-1480 feature).
- Before trusting it, hand-mutated `bbFixtureClosureGate.js`'s union loop in
  `missingFromList` to collapse to `entryList(entry)[0]` and re-ran: the
  scenario still passed — the check was vacuous. Root cause: the real
  no-limit fixture's copy step (`copy_bb_closure`, unaffected by the
  mutation) already copies the full four-entry closure, so any subset
  closure request reads as fully satisfied regardless of correctness.
- Rebuilt the scenario's When/Then to run `missingFromList` against a
  separate `closureDir` scratch tree (same technique as scenario 02)
  carrying one load-file edge reachable only through the second entry
  point (`effective_backlog_depth_cli.bb`). Re-ran with the same
  hand-mutation: scenario now correctly failed, reporting the probe file as
  reachable-but-uncopied. Restored the mutation and confirmed clean
  (`git diff --stat` empty) before re-running green.
- Found and fixed a temp-dir leak on the new scenario's own failure path
  (BL-921/BL-922/BL-931 class): `discard(ctx)` ran only after the
  terminal assertion, so a failing assertion — the scenario's own passing
  shape under a mutant — leaked the scratch dir every time. Wrapped the
  scratch-owning steps in try/catch-discard and try/finally; confirmed
  `/tmp` carries no `bl1480-union-probe-*` residue after both the passing
  and the induced-failure run.
- Ran the two standing shell fixtures directly
  (`test_promote_and_route_next_no_limit_depth.sh`,
  `test_promote_and_route_next_priority.sh` via the acceptance suite):
  both ALL PASS.
- Ran the whole-tree standing guards over `extension/test/*Guard*.test.js`
  (18 files, 183 tests): all green — no regression from the step-file edit.
- CRAP/DRY: not applicable — only `.js` test/step files touched, no
  `src/*.ts` production code (CRAP scope) and no meaningful duplication
  introduced.
- No orphaned test/mutation processes at handoff
  (`pgrep -fl 'node --test|stryker|vitest'` clean); host load nominal
  (~2.5 on 20 cores) throughout.
- Verdict: NONE outstanding. Hardening commit `a2adfb4b95` forwarded.
