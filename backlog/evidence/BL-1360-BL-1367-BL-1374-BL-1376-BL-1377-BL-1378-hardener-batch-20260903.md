# Hardener batch pass — 2026-09-03

Batch of 6 tickets received in one `ready_for_next.sh` batch (architect
worktree chained commits `1d4e5fa8f7 -> a34eca0203 -> f7545e4284 ->
8480eba89c -> 31f43a2841 -> 5c95e5ca56`), plus a QA merge-up note for BL-1375
(`fd506c0b09`, already an ancestor of `a34eca0203` onward). Merged the tip
`5c95e5ca56` in one commit, satisfying all 6 architect handoffs and the QA
merge-up simultaneously (`16fd7b8854`). Ran a single combined hardening pass
over the union of changed files per batch-mode rules.

Tickets: BL-1360 (ceremony handoff composed not retyped), BL-1367 (an
approval from any surface carries its ruling), BL-1374 (a sync merge is not
credited with its passengers), BL-1376 (the expedite handover never names the
unlanded branch), BL-1377 (a suite's failure set is recorded once per base
commit), BL-1378 (an expedite-closed ticket can never satisfy the close
guard). BL-1375 is QA-landed already and required only the merge-up, not
independent hardening here.

## Merge-deletion guard
The merge parked BL-1296/BL-1309/BL-1356/BL-1359 from `backlog/active/` to
`backlog/hold/` (human-held, not retired) on the incoming side. Confirmed
each file's content survived at its new path (rename, not loss) before
naming all four ticket ids in the merge commit message per the guard's own
instruction.

## Compile / unit / property suites
- `npm run compile`: clean.
- `npx vitest run` on every touched *.test.js (pausedPagerBridge,
  pendingApprovalReply) plus the two new *.property.test.js files (run under
  `vitest.properties.config.mjs`, per the property-lane separation rule):
  all green (109 unit + 5 property).
- Every `.bb`/`.sh` unit and property runner for the 6 tickets: ALL PASS —
  `ceremony_handoff_lib_test_runner.bb`, `test_ceremony_handoff_cli.sh`,
  `expedite_lib_test_runner.bb`, `land_step_lib_test_runner.bb`,
  `suite_baseline_lib_test_runner.bb`, `test_suite_baseline_cli.sh`,
  `ticket_close_guard_lib_test_runner.bb`,
  `test_check_feature_handler_registration.sh`,
  `test_bl1374_sync_merge_passengers.sh`,
  `test_bl1376_expedite_branch_handover.sh`,
  `test_bl1378_expedite_close_guard.sh`,
  `bl1360_ceremony_handoff_property_runner.bb` (500 runs),
  `bl1374_sync_merge_passengers_property_runner.bb` (24 fixture runs),
  `bl1376_expedite_branch_handover_property_runner.bb` (500 runs),
  `bl1377_suite_baseline_property_runner.bb` (500 runs),
  `bl1378_expedite_close_guard_property_runner.bb` (500 runs).
- `.bb`/`.sh` have no wired mutation/CRAP/DRY tool (engineering.prompt
  Startup Tools) — gated by their own unit/property suites only, all green
  above; this is the documented degraded fallback, not a skipped gate.

## Acceptance (all 6 feature files, `run_acceptance.sh`)
BL-1360 6/6, BL-1367 4/4, BL-1374 4/4, BL-1376 7/7, BL-1377 10/10, BL-1378
12/12 — all pass.

## BL-113 soft Gherkin mutation (Scenario Outline features only)
BL-1374 and BL-1367 carry no `Scenario Outline` (plain `Scenario:` only) —
inapplicable, nothing to mutate. The other 4:
- BL-1360: 2/2 killed.
- BL-1376: 4/4 killed.
- BL-1377: 8/8 killed.
- BL-1378: 11/11 killed (foreground run hit the ~120s ceiling with none
  completed at total=11; detached via `detach_job.sh` per the standing rule,
  completed clean in ~246s, `EXIT=0`).
Total: 25/25 killed, 0 survivors, 0 errors, across the batch.

## CRAP (`src/*.ts`, forced coverage write per the standing-red rule below)
Changed/new functions in `bridgeServer.ts` and `pendingApprovalReply.ts`
(BL-1367's own diff — the only TS touched by this batch):
- `classifyApprovalRulingRequirement` (new): initially CRAP 7.00 (complexity
  7, 100% coverage) — over threshold. Reduced complexity to 5 by (a)
  extracting `matchesRulingOption` for the label-match rule, isolating the
  ticket-shape branch count from the matching rule, and (b) converting the
  two `?? `-defaulted optional params to plain default parameters (parameter
  initializers are not decision points under this repo's complexity counter,
  unlike `??`/`||`/`&&` inline). Final: CRAP 5.00.
- `computePausedPagerApproveOutcome` (modified): initially CRAP 7.00
  (complexity 7, 100% coverage) — over threshold. Reduced by extracting
  `buildRulingRefusalBody` (the two wording ternaries for which refusal
  reason to report), leaving the caller's own control flow
  (found/satisfies-ruling/changed/committed). Final: CRAP 5.00.
- `recordApprovalReply` (modified, added `ruling` param): CRAP 5.20, within
  bound, no change needed.
- New helpers `matchesRulingOption` (CRAP 1.00), `buildRulingRefusalBody`
  (CRAP 3.00), `isPausedPagerApproveRequestShape` (CRAP 3.00): all well
  within bound.
- All other CRAP>6 functions reported in these two files
  (`readApprovalCloseVerdict`, `readEpicEtaCompletionEvents`,
  `buildCostRankState`, `requireControlAuth`, `buildContextBudgetState`,
  `readHumanRulingFromText`, `extractLetsTalkChoicePoll`, two anonymous)
  are pre-existing debt untouched by this batch's diff (confirmed via
  `git diff e7cec9a261 HEAD -- <file>` — neither file's diff touches any of
  them) — not this ticket's defect, unchanged before and after.
- `npx vitest run --coverage` (no override) writes no report while 15
  unrelated files are red (pilot-gate cluster, `telegramClient.test.js`,
  `operatorRuntimeBbFixtureClosure.test.js`, the whole-tree guards below,
  etc. — same standing-red set as the prior BL-1296 pass, none touching
  `bridgeServer.ts`/`pendingApprovalReply.ts`). Forced the write with
  `--coverage.reportOnFailure=true` per the standing rule; the resulting
  numbers are a floor for files touched by those reds, not for this batch's
  files.

## DRY (`jscpd`)
0.81%/1.19% duplicated lines/tokens — improved from the prior BL-1296 pass's
1.18%/1.50% baseline, not a regression. No clone involves
`bridgeServer.ts`/`pendingApprovalReply.ts`.

## Standing whole-tree guards (touched `specs/pipeline/steps/` and
`extension/test/`, so ran every non-property `*Guard*.test.js`)
17 guard files run; 3 red — `liveRepoDerivationGuard` (BL-1291, paused),
`socketFixtureShortRootGuard` (BL-1290, known standing red), and
`tempDirTrapGuard` (also already surfaced repeatedly per grep of
`backlog/evidence/` — BL-1063-class standing debt). Confirmed none of the
three guards' violation lists name any file this batch added or touched
(grepped each guard's output for `bl1360|bl1367|bl1374|bl1376|bl1377|bl1378`
— zero hits). Pre-existing, unrelated, unchanged before and after this pass.

## Process hygiene
No orphaned `node --test`/`stryker`/`vitest`/`gherkin-mutator` processes at
handoff (`pgrep -fl` scoped to this worktree — clean before and after the
detached BL-1378 run). Detached job's own tmp dir and the three foreground
Gherkin work-dirs (`tmp/gm-bl1360`, `tmp/gm-bl1376`, `tmp/gm-bl1377`) removed
after their runs completed.

## Verdict
All 6 tickets' own test/property/acceptance suites green. BL-113 Gherkin
mutation clean where applicable (25/25 killed). CRAP within bound on every
changed/new TS function after two extractions. DRY unchanged/improved.
Standing whole-tree-guard reds confirmed pre-existing and unrelated.
Forwarding to documenter.
