# BL-1298 documenter pass — 2026-08-31

Ticket: BL-1298 — the land step's replay cannot run from a linked worktree
(`.git` is a FILE there, not a directory), and a failed create leaked its
scratch branch.

Received: hardener's `f22afd345a` ("BL-1298: hardener pass - no gaps found,
coder's non-vacuous property tests already cover every branch."), merged
here as `8fae08b11a`.

## Deliverables checked

- **README / guides / command lists** — no user-visible command, setting,
  or extension flow changed. This is an internal fix to
  `swarmforge/scripts/land_step_lib.bb`'s `replay!`, invoked only by
  `land_step_cli.bb` (QA's own land-step tool). No extension surface reads
  or depends on the scratch-checkout path. Nothing to update.
- **`docs/reference/Specification.MD`** — this section (the BL-1241 land-step
  remedy's changelog) is actively maintained with one entry per landed
  ticket in that machinery (BL-1241, BL-1272, BL-1295, BL-1297, BL-1308).
  BL-1298 was missing its own entry. **Fixed**: added a new top-of-file
  entry describing both halves of the fix (the `git-common-dir` resolution
  and the create-failure branch cleanup), matching the existing entries'
  style and level of detail. "Last Updated" date is already today
  (2026-08-31) and needed no separate bump — the content edit and the date
  are in the same commit per role rule.
- **`docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`**
  — read in full. It already describes the replay as running in "a
  dedicated linked worktree, never in the caller's own possibly-dirty
  worktree" without asserting it previously worked from every checkout
  kind — that statement remains true and is not contradicted by the fix.
  No incorrect claim to correct, and no new user-facing contract (the tool
  invocation, its CLI usage line, its exit codes and output lines) changed.
  Left unchanged, matching how BL-1295/BL-1297 (narrower fixes to the same
  machinery) were handled — a Specification.MD entry only, no how-to
  subsection — versus BL-1308, which got its own subsection because it
  changed a documented detection *scope* (full ancestry vs. first-parent),
  not an internal path-resolution detail.
- **Diagrams** — `docs/diagrams/architecture.mmd` was grepped for
  `land.replay|land_step|replay`; its one match (`replay complete only; no
  tip-only` at line 33, and a push-sweep `replay` comment at line 269) is
  about a different mechanism (push-sweep's ahead-range cache), not the
  land step's scratch-worktree replay. The land step's internal worktree
  mechanics are not depicted in this or any other registered diagram
  (swarm-flow, handoff-flow, front-desk-flow) — none of their
  change-triggers fired. No diagram update needed.

## Items

NONE — one content fix (Specification.MD changelog entry), no defects
found in existing documentation.

By documenter.
