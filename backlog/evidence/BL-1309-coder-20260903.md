# BL-1309 — CODER PASS, 20260903

## The ruling this was built to

The ticket carried `human_approval: approved` (bd7d675e74) with the binary
`ruling_options` question UNANSWERED — no selection was recorded anywhere in the
ticket, its topic file, or `backlog/answers-archive/`. The two options are, in
the specifier's own words, materially different builds, so it was put back to
the human rather than guessed.

**Answered 2026-09-03: option 1 — refuse EVERY entangled tip.** No
withheld-vs-ordinary predicate is implemented, and none should be added later
without a new ruling: the whole point of option 1 is that it has no judgment in
it to get wrong, which is how this class of defect kept recurring.

## What changed

`swarmforge/scripts/land_main_publish.sh` — the only landing step QA.prompt
makes mandatory — now consults the finished detector before it advises
anything:

- The `--decide-only` decision is computed into `DECISION` and printed LAST, so
  a refusal never has push advice already on stdout ahead of it.
- `ENTANGLED_SIBLING_BLOCK` plus one `entangled-sibling: <id> …` line per
  unlanded ticket, then a line naming the remedy (BL-1241's tip-pure replay).
  A caller keys off the literal marker, never prose.
- Documented exit status **3** on refusal, `0` otherwise (`2` stays the usage
  error), recorded in the script's own header.
- The guard cannot abort the script. It runs under `set -euo pipefail`, so the
  detector is invoked inside a command substitution with `|| true`
  (Guardrails, BL-1242/BL-1252) — a refusal is an ordinary exit and never
  leaves the land lock held. Asserted, not assumed: unit row 04 checks the lock
  directory is absent after a refusal.

Fail-open (invariant 2) covers every input the step cannot read: no tip sha, no
`land_step_lib.bb` on disk, a tip whose subject names no ticket, an unreadable
range against origin/main (`:warning` set), or a detector that errors. Only a
POSITIVE finding refuses.

## Verification

- `bash swarmforge/scripts/test/land_main_publish_test_runner.sh` — ALL PASS.
  Four new rows: refusal names the sibling and exits 3 with no advice and no
  lock held (04); a landed sibling gives the ordinary decision with no marker
  (05); a missing detector fails open (06); a tip naming no ticket fails open
  (07). Row 06 asserts its own NON-VACUITY first — with the detector present
  the same state must refuse, otherwise the row would prove only that a clean
  tip stays clean.
- **Mutation**: `bash swarmforge/scripts/test/bl1144_main_land_publish_mutation_sweep.sh`
  — 9 killed, 0 survived. This required a fix I caused and found here rather
  than shipping: adding a second `bb -e` to the script made the sweep's
  "decide-only uses bb REPL not bb -e" mutant SURVIVE, because row 01's
  `grep -q 'bb -e'` was satisfied by my new invocation once the decide call's
  `-e` was dropped. Baseline measured (9/0 before my change, 8/1 after), then
  row 01 was strengthened to assert the ABSENT form — no bb here passes its
  program as a bare argument — which pins both invocations and restores the
  kill.
- Acceptance `specs/features/BL-1309-…feature` — 6/6. The draft was promoted and
  the ticket's `acceptance:` pointer flipped to the live path in this same
  commit (BL-233).
- Property (BL-654, both declared invariants):
  `extension/test/bl1309LandDecideEntanglementInvariants.property.test.js` — 2/2.
  Generator reach by CONSTRUCTION: invariant 1 names three ROUTES, so all three
  are built and reach-asserted (plain commit, second-parent merge — the route
  BL-1308 had to widen the detector for — and a rematch onto an advanced
  origin/main). Invariant 2's three blind shapes each assert the fixture
  actually refuses BEFORE the input is broken, so no row can pass vacuously.
  NON-VACUOUS: with the refusal branch disabled, both invariants fail
  ("a tip carrying BL-9002 was advised for push").
- Regression: `specs/features/BL-1144-frequent-qa-push-races-on-main-land.feature`
  — 3/3.
- **qa_e2e step 4, the real path**: ran `land_main_publish.sh <this worktree>
  --decide-only` against the live tip. It printed `ENTANGLED_SIBLING_BLOCK` and
  named BL-1296, BL-1342, BL-1344, BL-1346. Cross-checked independently against
  `entangled-siblings` for the same tip: identical set, no warning. The two
  paths agree, which is what step 4 exists to confirm.

## required_wiring

- `swarmforge/scripts/land_main_publish.sh::ENTANGLED_SIBLING_BLOCK` — the
  consumer anchor. Present as a literal in the live script; `grep` returned 0
  at mint.
- `specs/pipeline/steps/index.js::bl1309LandDecideStepEntanglementSteps` —
  registered, so all six scenarios actually run.

## Notes for the next role

- `swarmforge/scripts/test/land_main_publish_test_runner.sh` is NOT in
  `swarmforge/scripts/test/suite-manifest.tsv`: the manifest's column-1 rule
  admits `test_*.sh` or `*_test_runner.bb`, and this file is `*_test_runner.sh`,
  which matches neither. Pre-existing, not introduced here, and not fixed here
  (renaming a runner every gate greps for is its own slice). Flagging it because
  it means these rows do not run in the standing suite — run them directly.
- Option 1 will make many ordinary lands refuse. That is the ruling, not a
  defect: QA.prompt already describes an entangled tip as the normal state of a
  long-lived role branch, and the remedy it forces is the tip-pure replay QA
  already performs by hand.

## Self-audit findings, recorded rather than left for review to discover

**A1 — the mandatory step got ~12x slower, measured.** On this worktree's real
tip: `--decide-only` takes **13.7s** with the guard and **1.1s** without it
(same tip, same run, the detector removed from beside the script). The cost is
inherent to `entangled-siblings`, which runs `commit-ticket-id` — a `git log -1`
— once per candidate commit per sibling, in both `attribution-complete?` and
(since BL-1354) `sibling-own-line-changes`. A per-run memo on that extractor
would remove most of it.

Deliberately NOT done here: the ticket says in its own `depends_on` note that
"this slice does not touch the detector — it wires the finished detector into
the step QA cannot skip". Optimising `land_step_lib.bb` is a different slice on
a file that landed hours ago. Recorded so the architect and hardener judge the
cost with the number in front of them rather than discovering it in the field.
A land happens a few times an hour, so 12s is a real but modest price for the
ruling that was chosen.

**A2 — the ticket's `status:` is stale, and it is not mine to change.** The YAML
still reads `status: blocked` from the coordinator's BL-1354 ordering hold
(a014439583), while the coordinator's own priority-`00` note ("BL-1309
unblocked (BL-1354 landed) — clear to start now") cleared it in fact. I flipped
the `acceptance:` pointer because the coder prompt requires that with a draft
promotion (BL-233); ticket status is backlog bookkeeping and belongs to the
coordinator, so it is surfaced here instead of silently edited.

**A3 — unrelated working-tree files, surfaced not swept.**
`swarmforge/scripts/open_swarm_spy_router.sh` and
`swarmforge/scripts/spy_router_pane_label.sh` are untracked in this worktree and
are not mine (recorded operator/Cursor-authored drift). Left untouched and
unstaged.
