# BL-782 QA pass inventory — 20260826 (rematch)

**Documenter tip:** `b12db1a272`
**Merge tip:** `b55bed3e1e`
**Prior bounce:** `backlog/evidence/BL-782-qa-bounce-20260826.md` (D1 coder, D2 hardener)
**Sibling check:** `VERIFY BL-782` (exit 0)

## Prior bounce clearance

| Item | Status |
|------|--------|
| D1 APS hang on unreaped `sleep 600` decoys | **CLEARED** — `child.unref()` + module `liveDecoys` + `afterEach` reap; `timeout 90 run_acceptance.sh` → 8/8 exit 0 in ~13s, no leftover sleep decoys |
| D2 mutation-cache hitchhikers | **CLEARED** — 0 paths under `mutations/` / `base/` / `build/acceptance*` vs `origin/main` |

## Tip purity

`origin/main` ancestor; `land_main_publish --decide-only` → `:push`.
~13 BL-782 paths only (expedite_cli, steps, docs, evidence).

## Ticket gates

| Gate | Result |
|------|--------|
| `node --test` `bl782LivenessProbesScopedToRoot.property.test.js` | 2/2 |
| `test_expedite_cli.sh` | ALL PASS |
| `test_lifecycle_script_scope.sh` | PASS=15 FAIL=0 |
| APS BL-782 (`timeout 90 run_acceptance.sh`) | 8/8 exit 0 |
| `required_wiring` (root-scoped needles + `bl782` steps) | PASS |
| Docs (expeditor manual + Spec) | PASS |
| Stage rematch evidence | present |
| Orphan test procs | none |

## Intent

Neighbour-root processes never count as expedite liveness survivors; genuine
same-root survivors still detected; operator scoped via `operator.prompt`.

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS** — land via BL-1144 publish discipline.

By QA.
