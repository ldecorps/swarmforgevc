# BL-1210 — QA pass, 2026-08-31

## Disposition check (BL-532)

`qa-sibling-check.js status --ticket BL-1210` → `VERIFY BL-1210` (exit 0).

## Merge

Merged documenter's cited commit `63d952a935` into QA at `28e2c75c52`
(`git merge --no-ff`, clean — no guard trip). Diff against my own prior
parent (`e429c9a2be`) adds only evidence and doc files; diff against the
documenter parent is empty (my branch already contained everything
documenter's did, via an earlier merge-up this session). Nothing dropped
either direction.

## Unusual shape — the fix already reached `origin/main` without a QA gate

Verified independently, not taken on the coder's word: every producing
commit (coder `f376eba7fb`, cleaner `8e539579b5`, architect `76b4942431`,
hardener `f62010ef77`/`4124c185b0`) is **already an ancestor of
`origin/main`** — `git merge-base --is-ancestor <each> origin/main` all
confirm YES. This happened because the coordinator's 2026-08-30 parking
(`66be60ea40`, orphaned dead BL-1295 expedite run) stranded this ticket
between hardener and documenter, and its already-hardened content later
rode onto `main` as a passenger of other tickets' entangled merges rather
than through this ticket's own QA approval. This QA pass is the first time
the ticket's work has actually been reviewed by the gate that is supposed
to see it before landing — a retroactive close of that gap, not a normal
forward flow. `backlog/hold/BL-1210-...yaml` is still the ticket's current
path in both my branch and documenter's (a separate coordinator release
commit, `400f3a7952`, promoting it to `active/`, has not yet reached either
branch) — bookkeeping for the coordinator, not blocking this approval.

Ancestry re-confirmed for the full chain against my own merge commit
(`28e2c75c52`), independent of the origin/main check above: all five
producing commits plus `63d952a935` are ancestors.

## Ticket shape

BL-695 (2026-08-25) correctly restricted git-tracked `backlog/topics/*.json`
to ticket ids, but `recordSwarmIconId` had no durable store at all for
epic/standing/role ids (classify `'unbound'`) — a marker write silently
wrote nothing while `syncTopicIcon` still reported `'updated'`. Regression,
not an original gap (confirmed against `35daa5054^`, which wrote
unconditionally). No live icon behaviour was ever broken — every affected
call site passes `isNewTopic=true`, bypassing the ownership check the
marker feeds — but 5 tests were red for two days and the mechanism was a
silent trap for the first future caller passing `isNewTopic=false`.

## Stage corroboration

Coder: pre-fix baseline captured (5 failures exactly as predicted), root
cause confirmed against the pre-BL-695 file, non-vacuity proven both
directions (2 deliberate breaks, each fails the properties that should
catch it). Architect: COMPLIANT, caught and corrected a stale-`out/` false
alarm before trusting any test result, read all three touched source files
directly, swept every `syncTopicIcon` consumer for exhaustive-switch/silent
mishandling gaps. Hardener: **real Stryker mutation run** (not a hand
sweep) on both touched files — `topicThreadKind.js` 93/97 (1 equivalent
mutant, demonstrated live via `node -e`), `blTopicStore.js` 112/126 (all 14
survivors on functions this ticket's diff never touched, confirmed by
line number) — zero real in-scope survivors; CRAP and DRY both checked,
one pre-existing near-threshold clone correctly attributed to a prior
commit, not this one.

## Own independent verification (BL-112, separate from every prior stage's)

| Command | Result |
|---|---|
| `npm run compile` | clean |
| Named regression set (`backfillEpicTopicIconsCli`, `backfillStandingTopicIconsCli`, `backfillTopicIconsCli`, `blTopicStore`, `conciergeTick`, `topicIconSync`, `topicThreadKind`) | 243/243 pass |
| `npx vitest --config vitest.properties.config.mjs` BL-1210 property file + `topicThreadKind.property.test.js` | 7/7 pass |
| `run_acceptance.sh` BL-1210 feature | 5/5 pass |
| `git status --porcelain backlog/topics/` | clean — no tracked-record leak |
| `grep -n bl1210 specs/pipeline/steps/index.js` | registered, line 652 |
| `classifyTopicThread`'s function body, diffed against `35daa5054` (pre-BL-695) | byte-identical — constraint against widening `TICKET_ID` holds |
| `conciergeTick.ts` diffed for `isNewTopic` since `35daa5054` | zero occurrences — no call site touched |

## Doc verification

`docs/reference/Specification.MD`, `docs/index.md`, and the BL-695 how-to's
new BL-1210 section all read accurately against the landed code — checked
directly, not cited.

## Orphan check

`pgrep -fl 'node --test|stryker|vitest'` clean before and after.

## Verdict: APPROVE

By QA.
