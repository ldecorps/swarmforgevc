# BL-1370 — CLEANER PASS (post-bounce), 2026-09-05

Bounce cleared. NONE further; no cleanup change made. Forwarding to
architect.

## The bounced defect (D1, `BL-1370-cleaner-bounce-20260905.md`) is fixed

`swarmforge/scripts/process_table_lib.bb`'s `project-scoped-process?` now
delegates to a new `path-boundary-after?` helper: a path match is accepted
only when the following character is a real path boundary (`/`,
whitespace, a quote, or end of string) on the command-line arm, and only
when equal or followed by `/` on the cwd arm — exactly the amendment's
wording. Confirmed by reading the function, not by grep alone.

- `test_bl887_scope_predicate_invariants_property_runner.bb`: 300 runs
  each of P1/P2, ALL PROPERTIES HOLD.
- `process_table_lib_test_runner.bb`: ALL CHECKS PASSED.
- `test_bl1370_worktree_strays.sh`: 17/17 checks, including the two new
  scenarios this bounce was about — "a prefix-sibling root's suite is
  never reported as this worktree's stray" and "a reap leaves the
  prefix-sibling root's process running" — both PASS. (One SKIP is
  environment-conditional, "this host did not put the stray in the
  caller's own group," not a failure.)
- `bl1370WorktreeStrayCheck.property.test.js`: 4/4 PASS. The test
  previously titled "known boundary: a prefix-shaped sibling root IS
  claimed by the shared classifier" (which pinned the bug as accepted
  behaviour) is gone — confirmed by grep, 0 hits for that title or
  "prefix-shaped" anywhere in the file.
- `tsc -p ./` compiles clean.
- `jscpd` over the changed `.bb`/JS files: 0 clones.

## What was otherwise checked

- `required_wiring` anchor (`bl1370WorktreeStrayCheckSteps.js::registerSteps`)
  still present and matches BL-1371's discovery suffix.
- `main`/`origin/main` in sync.
- Bounce history now on the ticket YAML (`bounce_count: 1`, one entry,
  blamed coder, class spec-gap) — recorded by `record-bounce.js` during
  the bounce, committed here alongside this pass since the tool writes it
  uncommitted.

No architecture concerns: the fix is a single new predicate helper reused
by both matching arms in the one shared classifier, exactly as the
amendment specified (no second notion of scope introduced).

By cleaner.
