# BL-1381 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## This parcel also carries the BL-1385 concurrency fix

The received commit range includes coder's fix (`839873ab82`) for the
`check_handler_module_graph.sh` concurrency race I reported against BL-1385
two turns ago (`backlog/evidence/BL-1385-cleaner-followup-concurrency-race-20260904.md`).
Specifier amended BL-1385 with invariant 3 and scenario 07; coder replaced
the fixture-style sweep-by-prefix with per-run ownership (a `.owner-pid`
file, reaping only roots whose owner is dead) plus a bounded retry around
`git write-tree` for transient index-lock contention. Independently
re-verified: nine concurrent invocations of `check_handler_module_graph.sh`
(three batches of three) all exit 0, where before the fix two concurrent
runs reliably raced. Not itself the subject of this evidence file (that is
BL-1385's own review, already forwarded past cleaner) — recorded here only
because it rode in with this commit range.

## What was checked for BL-1381 itself

- `swarm_shift_lib_test_runner.bb` — re-ran: ALL TESTS PASSED.
- `test_shift_schedule_applier.sh` — re-ran: ALL CHECKS PASSED, including
  both invariants (crontab byte-identical on any non-zero exit; the
  wrapper names its cause on every failure path, not only the empty-output
  one coder's first pass caught).
- Root cause: `babashka.process` moved from a runtime `require` inside a
  function body (SCI resolves aliases at analysis time, so this crashed the
  whole file at load, not at the call site) to the `ns` form. Read the diff
  directly — matches the ticket's own diagnosis exactly.
- Coder's own send-time self-audit caught two real gaps before forwarding
  (a non-zero reconcile exit still unnamed under `set -e`; a Python
  traceback still leaking on non-JSON input) and fixed both, verified in
  both directions (fails pre-fix, passes post-fix) — read the evidence and
  spot-checked against the diff rather than trusted.
- `SHIFT_SCHEDULE_RECONCILE_BB` is a path-injection seam (overrides WHICH
  program runs), not a `*_FORCE_RESULT` result-bypass — matches the
  Startup Tools convention; confirmed by reading the call site.
- `mutation-site-count.js` on `bl1381ShiftScheduleApplierSteps.js`: 139
  sites (`over` 100). Reviewed and declined to split, same reasoning as
  this area's other tickets: one cohesive single-feature step handler.
- `jscpd` over the new JS handler and the touched shell/bb files: 0 clones.
- TypeScript compiles clean; `bl1381ShiftScheduleApplierSteps.js` and
  `bl1385HandlerModuleGraphGuardSteps.js` both discover via BL-1371's
  registry.
- Babashka/shell have no mutation/CRAP/DRY wired (Startup Tools, BL-472
  deferred); gated by the two runners above instead, per the documented
  degraded fallback.

No bounce history on BL-1381 (first pass). Forwarding unchanged to
architect.

By cleaner.
