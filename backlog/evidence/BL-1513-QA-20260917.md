# BL-1513 — QA review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Citing commit: 18c3e26599 (`main` tip in this worktree). Coder/hardener
  commit `a43648d6ac` (BL-1513: the router note names the ticket id and its
  path, never cut) confirmed an ancestor of the citing commit
  (`git merge-base --is-ancestor a43648d6ac 18c3e26599`).
- `qa-sibling-check.js status --ticket BL-1513` → `VERIFY BL-1513` (no open
  deferral).
- `grep -n 'MSG:0:80' swarmforge/scripts/route_backlog_to_coder.sh` → no
  match (the cut branch is gone).
- `bb swarmforge/scripts/test/bl1513_router_note_fits_property_runner.bb`
  → `ALL PROPERTIES HOLD` (16 real-route length-sweep trials + 10 parser
  checks x2 shapes).
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1513-the-router-note-is-never-cut.feature` → 4/4 pass
  (id/path composition at two lengths, dispatch-trail refusal on a second
  route, refusal-not-cut over the 80-char limit).
- `bash swarmforge/scripts/test/test_route_backlog_role_label_bash32.sh` →
  ALL PASS.
- `bash swarmforge/scripts/test/test_bl1097_router_refuses_dispatched_ticket.sh`
  → 10/10 PASS (BL-1097 dispatch-trail refusal behavior unchanged).
- `check_feature_handler_registration.sh` → exit 0, no diagnostics
  (`bl1513RouterNoteFitsSteps.js::registerSteps` wired; also proven live by
  the acceptance run above).
- Full unit suite (`npm test` in `extension/`): 625 files / 10669 tests,
  all pass. Suite-budget warnings printed are pre-existing telemetry noise
  unrelated to this ticket (BL-968, front-desk, briefing CLIs, etc.), not
  failures.
- Property suite (`npm run test:properties` in `extension/`): exited 0, all
  files green (including `bl582ApprovalTapObservable.property.test.js`,
  whose "failed to close the approval ask" lines are the test's own
  simulated-failure fixture output, not a real failure).
- No prior bounce history for BL-1513 on `main`/`origin/main`
  (`git rev-list --left-right --count main...origin/main` → `0 0`, both
  refs equal; no bounce record found).
- No unowned/standing red touched by this parcel's diff.
- Straggler check: reaped one orphaned `vitest` worker left in this
  worktree's own process group after `npm run test:properties` exited
  (PGID 8571, PID 14141); left untouched a concurrent `coder2` worktree's
  own vitest workers (not mine to reap).
