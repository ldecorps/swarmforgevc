# BL-716 — QA pass, 2026-08-01

## Commit reviewed

`9513ad62d0` (documenter, "Merge branch 'main' into swarmforge-documenter" —
carries the required_wiring spec-amendment merge plus the ticket's own dns-05
work), fast-forwarded into QA.

Ancestry confirmed: hardener's mutation-hardening merge (`ed3f327f2`),
architect's pass merge (`44a56505f`), and the coder's dns-05 discovery merge
(`0d06e1cb5`) are all ancestors of the reviewed commit — this is the ticket's
own lineage, not a sibling's.

## Verification performed

- `npm run compile` (extension/): clean.
- `npx vitest run` (extension/): 391/394 files, 6881/6918 tests green. The 3
  failing files (`test/bridgeServer.test.js`, `test/pausedPagerBridge.test.js`
  — CURSOR_API_KEY leak, BL-720; `test/tmpDirMigrationGuard.test.js` — raw
  mkdtemp bypass in `pricingTable.test.js:92`, BL-714) touch none of this
  ticket's changed files (confirmed via `git diff --name-only` between the
  prior QA tip and this commit) and are pre-existing, already-ticketed
  failures — not re-filed, not blocking this parcel.
- Ticket's own new/changed unit + property tests, run directly:
  `test/residentSpyTunnelNotify.test.js` (28/28),
  `test/notifyResidentSpyTunnelCli.test.js` (26/26),
  `test/residentSpyTunnelNotify.property.test.js` (1/1, non-vacuous per
  architect's evidence).
- No orphaned `node --test`/`stryker` processes before or after.

## required_wiring confirmed (all 6, read at source, not inferred from tests)

1. `BridgeClient.kt:108,135` — `connectionFailure = true` set on DNS/connect
   failure paths.
2. `TalkEngine.kt:295,369,388` — both `sendTextTurn` and `submitAudio` route
   failures through `applyTurnFailure`, which lands connection failures on
   the distinct `Phase.ERROR` (not the fake-healthy `READY`/recording cycle).
3. `MainActivity.kt:30,94,108` — `applyPairingDeepLinkIfPresent` consumed on
   both `onCreate` and `onNewIntent`.
4. `AndroidManifest.xml:44` — `swarmforge-bubble://pair` scheme registered.
5. `notify-resident-spy-tunnel.ts:193` — DM notify carries
   `buildBubblePairingDeepLink(fullUrl)`.
6. `residentSpyTunnelNotify.ts:49,71,80,132` — notify buttons wired to
   `pairingDeepLink`, not left unreferenced.

## Acceptance gate — confirmed known, tracked, human-approved gap; not a bounce

Ran `specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-716-bubble-lets-talk-tunnel-hostname-dns.feature` myself
(independent of any run the coder did): 0/5 scenarios, all fail "no step
handler matched" — confirmed by grep that no step file matches any of this
feature's Given/When/Then text. All 5 scenarios describe Android runtime
behavior (recording phase, host resolution) with no reachable test seam from
the Node-based Gherkin runner — same non-encodability class the coder cited
for the declared invariants (BL-761).

This is not a fresh discovery: `backlog/paused/BL-761-...yaml` (human-approved,
specifier-authored) already measured this exact 0/5 on 2026-07-31 and
explicitly disposed of it as accepted debt for still-unbuilt tickets, with the
policy fix (a pre-QA gate finding) deliberately deferred pending an unresolved
question about where Android-behavior acceptance contracts live. The architect
independently re-confirmed and forwarded rather than bounce, for the same
reason: bouncing here recreates, at an earlier gate, exactly the blocking
outcome BL-761's own `approval_context` warns against.

Per Article 4.4's spec-gap routing (acceptance scenario with no step handler
→ note to specifier + coordinator, not a bounce to coder): this is that class
of item, already known and ticketed, not new implementation negligence on
BL-716. Landing this ticket makes it the 5th ticket carrying the gap BL-761
tracks (after BL-696, BL-706, BL-707, BL-718 in `done/`) — flagging this via
`note` to specifier + coordinator alongside this approval so BL-761's own
measurement table can be updated, rather than silently drifting further out of
date.

## Verdict

PASS. No new defects. Landing on `main`.
