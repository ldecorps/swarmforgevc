# BL-615 QA bounce — 20260825

**Reviewed tip:** coder `ae1f64513f` (not merged — tip impure)
**Task:** `BL-615-orphaned-claim-progress-sidecars`
**Sibling:** `VERIFY BL-615`
**Routing:** coder→QA (required_stages coder+qa; skips documented) — tip purity still applies.

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / product gates | **BLOCKED BY D1** — tip not landable; not merged |

## Inventory

### D1 — `behavior` (blame: coder)

**Failing command:**
```
git merge-base --is-ancestor origin/main ae1f64513f  # fails
comm -12 <(git ls-tree -r --name-only origin/main | sort) \
  <(git diff --diff-filter=D --name-only origin/main ae1f64513f | sort) | wc -l
# → 7
```

**Commit hash checked:** `ae1f64513f`

**First error excerpt:**
```
main_NOT_ancestor (merge-base with origin/main = e52618f50, before BL-578 land)
paths=22 dels=7
deleted on tip vs present on origin/main:
backlog/evidence/BL-578-architect-pass-20260825.md
backlog/evidence/BL-578-cleaner-pass-20260825.md
backlog/evidence/BL-578-documenter-pass-20260825.md
backlog/evidence/BL-578-hardener-pass-20260825.md
backlog/evidence/BL-578-qa-pass-20260825.md
docs/how-to/BL-578-devhost-bounce-wsl-window-leak.md
specs/pipeline/steps/bl578DevhostBounceWslWindowLeakSteps.js
(+ tip moves BL-578 ticket to paused and drops other BL-578 product paths)
```

**Failure class:** `behavior`

**Expected vs observed:** Expected tip-pure rematch from current `origin/main`
(`2177e4c7e9`) authorizing **BL-615 paths only**. Observed stale tip based on
pre-BL-578 main that would delete landed BL-578 evidence/docs/APS wiring.

**Remediation:** `git reset --hard origin/main` then re-apply only BL-615
product/test paths (sidecar remove-on-abandon + orphan reap). Must not delete
any BL-578 paths. `dels_on_origin` must be 0; `origin/main` must be ancestor.

## Verdict

BOUNCE → coder. Do not land.

By QA.
