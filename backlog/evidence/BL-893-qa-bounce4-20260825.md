# BL-893 QA bounce 4 — 20260825

**Reviewed tip:** documenter `62f26aa8fc` (not merged — tip impure)
**Task:** `BL-893-approvals-ambulance-choice`
**Sibling:** `VERIFY BL-893`
**Prior:** bounce3 `103590ea0a` (tip kept bounce*.md but main advanced)

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 62f26aa8fc  # fails
comm -12 … → docs/briefings/2026-08-25.md
```

**Commit hash checked:** `62f26aa8fc`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = 197b7a7a4 briefing: record sent marker)
paths=24 dels=1
deleted on tip vs present on origin/main:
docs/briefings/2026-08-25.md
(tip based on bounce3; main gained briefing send + .sent.json)
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto **current** `origin/main` with
`dels_on_origin=0` (keep briefing markdown + bounce evidence). Observed tip
cut from bounce3 that would delete the landed briefing body.

**Remediation:** `git fetch && git reset --hard origin/main` immediately
before forward; restore BL-893 product only; keep bounce*.md and
`docs/briefings/2026-08-25.md`.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
