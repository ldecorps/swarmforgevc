# BL-1189 — QA pass — 20260828

**Received:** `merge_and_process documenter 7211aff59e` (handoff
`00_20260828T012351Z_000875_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `7211aff59e94f0c6a2fc31d1464f296b66080841`
**QA merge commit:** `95bbc0ce0`

## Merge conflict resolved

`specs/pipeline/steps/index.js` conflicted (both sides added different
`DOMAINS` entries at the same array tail). Resolved as the union
(`bl592SpecTreeOnLiveConsoleWithEpicTierSteps`, `bl1199PackSwitchBubbleTunnelSteps`,
`bl1195WorktreeTrackedContentDriftSteps`, `bl1186DeprecatorIdentifyUnusedNotifySteps`),
dropping two duplicate `require()`s my own naive resolution would otherwise
have introduced (`bl1188...` already present above the conflict;
`bl592...` already relocated earlier in the file by documenter's branch —
git auto-merged that insertion cleanly, so re-adding it in the conflict
block would have registered the same domain twice). Verified post-resolve:
zero duplicate `require()` targets introduced by this merge (`bl718`/`bl726`
duplicates present are pre-existing on both merge parents, untouched, out
of scope). `node -c` clean.

An automatic `merge origin/main: Fast-forward` occurred between my initial
`git status` check and the manual merge (HEAD moved `d15541088` →
`6bf8f16b2`, picking up "Promote BL-1213"). Confirmed `d15541088` is an
ancestor of `6bf8f16b2` — clean fast-forward, no history lost.

## BL-336 ancestry guard — commit actually contains this ticket's own work

- `git merge-base --is-ancestor 0e810b458 7211aff59e` (specifier's ruling
  lifting the hold) — CONFIRMED
- `git merge-base --is-ancestor 739ca994e 95bbc0ce0` (coder's genuine
  re-fix: `residentPaneLive.ts`, `residentPaneSpy.ts`, both test siblings,
  step handler) — CONFIRMED, real ancestor of the commit I'm approving, not
  merely "on main by another path"
- `git merge-base --is-ancestor 8a4e160867 7211aff59e` (coder's stale,
  uninformed re-revert, declared `abandoned_commits`) — NOT an ancestor, good
- `git merge-base --is-ancestor 90b6864346 7211aff59e` (cleaner's stale
  re-cleanup, declared `abandoned_commits`) — NOT an ancestor, good

## Correction-content integrity (BL-1213/main-reset fallout check)

Per operator memory on the `main` reset saga: measured the same reference
values the specifier used to confirm QA's tip is unpoisoned.
`extension/src/bridge/specTreeUiHtml.ts` present; BL-1189 `bounce_history`
2 entries (matches); BL-1188 `bounce_history` 4 entries (matches). Content
intact, not silently reverted by this merge.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1189` | `VERIFY BL-1189` — no open deferral |
| Compile | `npm run compile` | PASS, clean |
| Targeted unit | `npx vitest run test/residentPaneLive.test.js test/residentPaneSpy.test.js` | 43/43 PASS |
| Targeted property | `npx vitest run --config vitest.properties.config.mjs test/bl1189LiveScreenOnePrimaryWorkingTicketInvariants.property.test.js` | 4/4 PASS, both declared invariants exercised |
| Acceptance | `run_acceptance.sh specs/features/BL-1189-*.feature` | 5/5 PASS (all 5 scenarios, incl. the BL-600 cascade this ticket exists to fix) |
| Regression | `npx vitest run test/pipelineBoard.test.js test/pipelineGridLive.test.js` | 142/142 PASS |
| required_wiring (all 3 rows) | grep + confirm real caller | `resolveResidentHeldTicketMeta` called by `resolveResidentHeldTicketMetaForRoles`; `tryCaptureRolePane` called 3x from `residentPaneLive.ts`'s own capture entry points; step registration present in `index.js:817`, single occurrence |
| Invariants (both) | acceptance evidence above | invariant 1 (post-close ghost) — scenario `post-close-ghost-02`; invariant 2 (at-most-one-seat) — scenarios `unique-primary-seat-01`/`stale-residual-not-equal-03` |
| Full unit suite | `npm test` | 206/9193 failed across 47 files — pre-existing, unrelated (see below), consistent with today's independently-confirmed "5 directions" standing red (BL-1188/1190/1199/1200 evidence) |
| Full property suite | `npm run test:properties` | 18/690 failed across 28 files (+2 benign `[vitest-worker]: Timeout calling "onTaskUpdate"` allowlisted per engineering.prompt) — pre-existing, unrelated |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker\|[v]itest'` | none, before and after |

## Standing red — confirmed unrelated to BL-1189, one NEW untracked defect found

None of the 47 (unit) / 28 (property) failing files touch BL-1189's changed
paths (`residentPaneLive.ts`, `residentPaneSpy.ts`, the bl1189 property/step
files). Diagnosed root causes rather than assuming:

1. **`CURSOR_API_KEY` not ambient in this shell** (bridgeServer, epicMakeTopBridge,
   epicReorderBridge, pausedPagerBridge, specTreeBridge, topicMakeTopBridge,
   ~5 more) — this environment doesn't inject it via `tmux -e` the way a live
   swarm pane would (`swarmforge.sh:1753`). Infra-environment gap, not a code
   defect.
2. **Main-lane `require('node:test')` files fail Vitest collection**
   ("No test suite found in file ..."): ~23 plain `.test.js` files
   (`unreachableStepHandlerCheck`, `crossFileDuplicationCheck`,
   `pilotAcceptanceGate`, `nightClosingCeremony*`, `multiBranchParserCoverageCheck`,
   etc.) import `test` from `node:test` instead of relying on
   `vitest.config.mjs`'s `globals: true`. Confirmed pre-existing and
   unrelated: identical on `origin/main` before this merge
   (`git show 6bf8f16b2:extension/test/<file>.test.js` — same import line).
   **Grepped `backlog/{active,paused,hold}` for this — genuinely untracked.**
   The architect's existing note/ticket (`architect-vitest-node-test-no-suite-found-20260827.md`
   → BL-1206) scoped its own grep to `test/*.property.test.js` only (13
   files) — this main-lane instance (a larger, structurally identical but
   distinct set) was never surfaced. Sent as a `note` to specifier+coordinator
   (priority `00`) rather than minting myself.
3. **`deps.checkOrphanedAuthoredDocs is not a function`**
   (`unreachableStepHandlerCheck.test.js`/`.property.test.js`,
   `pilotAcceptanceGateCli.test.js` region): BL-757 (2026-08-27, already on
   `main`) added a required `checkOrphanedAuthoredDocs` dep to
   `landPilotedTicket`'s deps contract; these test files' own `mkDeps()`
   stub was never updated to supply it. Pre-existing, confirmed on
   `origin/main`, included in the same note above.
4. Assorted independent single-file failures (`constitutionDocCitations.test.js`:
   `docs/deprecated/` cited but directory doesn't exist yet;
   `tmpDirMigrationGuard.test.js`, `tempDirTrapGuard.test.js`,
   `socketFixtureShortRootGuard.test.js`, `liveRepoDerivationGuard.test.js`:
   pre-existing repo-hygiene guard reds unrelated to Live Screen/pipeline-grid
   code) — not investigated file-by-file beyond confirming none touch
   BL-1189's paths; folded into the same note for specifier triage rather
   than blocking this ticket.

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1189's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1189 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1189 active → done).
Separate `note` (priority `00`) sent to specifier+coordinator for the newly
surfaced main-lane `node:test`/Vitest collection gap and the
`checkOrphanedAuthoredDocs` deps-stub gap — both untracked, both unrelated
to this ticket.

By QA.
