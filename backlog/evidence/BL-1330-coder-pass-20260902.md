# Evidence — BL-1330 — coder pass (2026-09-02)

Role: coder. Ticket: BL-1330-swarm-stamp-bob-anthropic-starting-cast-441fd35112.
Routed by coordinator note `20260902T142106Z_003429`.

Review-only stamp-off of Cursor hotfix `441fd35112`. This parcel CONFIRMS the
land. It reimplements nothing, re-staffs nothing, and writes nothing to the
ledger.

## FINDING 1 — a DUPLICATE LANDING, and two tickets certifying one change

`441fd35112` and `db7e3f2bda` (the commit BL-1326 reviews, which this coder
reviewed earlier today) are **two distinct commits with byte-identical
diffs**:

- `diff <(git show db7e3f2bda --format=) <(git show 441fd35112 --format=)` is
  EMPTY — same 3 files, same 250 insertions / 26 deletions, same content.
- Different parents (`b1e160e438` vs `acf577615e`), different trees, and
  **neither is an ancestor of the other**.
- **`441fd35112` IS reachable from `main`. `db7e3f2bda` is NOT.**
- Both carry their own `backlog/hotfix-ledger.yaml` row (lines 240 and 247),
  and both have an ACTIVE stamp-off ticket (BL-1326 and BL-1330).

So the human is currently being asked to certify the same change twice, and
one of the two rows names a commit that never reached `main`. Neither
ticket's text mentions the other's commit, so neither specifier pass knew.

Raised to the specifier by `note` priority `00`
(`00_20260902T144416Z_001562`). NOT adjudicated here: which row to certify,
waive, or retire is a human/specifier call, and this parcel reviews the
commit its own ticket names. The Background and scenario 06 ASSERT both
halves of the finding (this commit on main, the twin's row still present and
the twin still not on main) so it fails loudly rather than rotting if either
fact changes.

This is also, structurally, the exact shape BL-1334 addresses from the other
side: a replay mints a NEW commit for content that is already approved, and
nothing downstream knows the two are the same work.

## FINDING 2 — scenario 02 is unsatisfiable as written

Scenario 02's second step reads "the diff of commit 441fd35112 touches no
coordinator-related line". Read literally that is false: the commit rewrites
the pack header, and SIX comment lines mentioning the coordinator move,
including

    -#   coordinator  → claude → claude-sonnet-5  (Anthropic Max; auto-provisioned)
    +#   coordinator  → claude → claude-sonnet-5  (Anthropic; auto-provisioned)

— the word "Max" is dropped from the documented posture.

What the scenario protects, and what the ticket's own description claims, is
that the coordinator's STAFFING is untouched. That IS true and is what the
step now asserts:

- no NON-COMMENT line mentioning the coordinator changed;
- all three `config coordinator_*` values are byte-identical across the
  commit (`coordinator_agent claude`, `coordinator_model claude-sonnet-5`,
  `coordinator_effort medium`).

The prose delta is surfaced deliberately (the step asserts the comment lines
DID change) rather than matched away with a looser regex, and the wording gap
is raised to the specifier by `note` priority `00`
(`00_20260902T144626Z_001563`). Same disposition as BL-1319's scenario 03:
encode what is true, report what the scenario asked for and cannot have.

Worth a human eye on the "Max" wording specifically: the coordinator's model
is unchanged, but the documented CLAIM about how it is billed changed, and
nothing in this commit changes billing.

## Invariants held

- **inv 1 — one coder Qwen seat, six Anthropic seats.** Asserted per role by
  the Outline against explicit KNOWN_VALUES, read from the commit.
- **inv 2 — the 1M declaration and QWEN credential are coder-only.** See the
  limit below; the determinant is executed.
- **inv 3 — never reimplements.** `git status` over the pack conf,
  `swarmforge.sh` and the ledger is EMPTY at handoff.

## An honest limit on scenario 04

Scenario 04 says "the bob pack launch scripts generated from the landed
conf". **No launch script was generated and no launcher was run** —
generating them means executing `swarmforge.sh`, which spawns tmux sessions
and live agents.

What DETERMINES the answer is executed instead: the real
`extra_cli_targets_qwen_cloud`, run per seat, answers `qwen-cloud` for coder
and `none` for all six others. Both gates are then shown to key off that same
predicate in the landed launcher — the launch-script `billing_guard` that
writes `CLAUDE_CODE_MAX_CONTEXT_TOKENS`
(`swarmforge.sh:1958-1967`) and the pane-env branch that adds `QWEN_API_KEY`
and the same 1M flag (`swarmforge.sh:2219-2228`). Since no non-coder window
matches the predicate, neither branch is reached for them.

Stating this rather than letting "only the coder seat's launch script
declares…" imply a generated artefact was inspected.

## qa_e2e / scenarios walked

1. Staffing table — 7 roles, model and effort each asserted from the commit.
2. Coordinator — no window line names it; config untouched (see Finding 2).
3. Remap predicate — EXECUTED; only coder matches.
4. 1M + QWEN_API_KEY scoping — determinant executed, gates verified.
5. Changed paths — the only file under `swarmforge/` is the bob pack conf;
   nothing under `swarmforge/scripts/` or `extension/`. (The commit also
   carries BL-1322's ticket + feature files, which are outside `swarmforge/`
   and are BL-1322's own record, per this ticket's own framing.)
6. Ledger — `state: stamp-open`, unmodified in the working tree.

## Driver reuse, deliberately

Scenario 03 uses `specs/pipeline/steps/lib/bl1326QwenRemapPredicateCli.zsh` —
the SAME driver BL-1326's handler uses, not a second copy. It is the same
shipped predicate, and two extractors could drift against each other while
both looking correct. The driver extracts the function from the live
`swarmforge.sh` and evals it under zsh; it never sources the launcher.

Confirmed for this review too: `extra_cli_targets_qwen_cloud`'s text is
byte-identical between `441fd35112` and the reviewed tip, so the executed
predicate is the one that was live when this restaff landed.

## Checks run this pass

- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1330-...feature` — **12/12 pass** (was 12 failing: no step
  handler existed). New handler
  `specs/pipeline/steps/bl1330SwarmStampBobAnthropicStartingCastSteps.js`,
  registered in `specs/pipeline/steps/index.js` — the ticket's third
  `required_wiring` anchor. The first two anchors (the conf's
  `claude-sonnet-5` and `qwen3.8-max` staffing) are asserted by scenarios
  01 and 03.

## No property tests, and why that is not a gap

Invariants 1 and 2 are facts about ONE config file at ONE commit plus a
predicate over seven fixed CLI strings — the whole input space is the seven
window lines, and the Outline plus the executed predicate covers it
exhaustively rather than sampling it. Invariant 3 quantifies over this
parcel's own conduct (process, not a pure function) and is encoded as
executable assertions over repository state. Recording the reason rather than
leaving a declared invariant silently unencoded.

## Out of scope, untouched

- The hotfix, the staffing call itself, `extra_cli_targets_qwen_cloud`, and
  BL-1322's bundled ticket files.
- `swarmforge/scripts/open_swarm_spy_router.sh` and
  `spy_router_pane_label.sh` — untracked, not created by this parcel.
