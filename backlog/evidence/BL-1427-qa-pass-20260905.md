# QA pass — BL-1427 — 2026-09-05

Commit under review: `bd0716f972` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`b78defe7c2` BL-1427
coder, confirmed ancestor via `git merge-base --is-ancestor`).

Safety-critical fix (a probe that could run a script's real entry point,
e.g. `post_qa_branch_sweep.bb` fetching origin and sweeping live worktrees
from inside a guard) — reviewed with corresponding care.

## Verification

- `bash swarmforge/scripts/test/test_bl1395_bb_scripts_load.sh`: ALL PASS
  (16/16) — every BL-1395 recall scenario (forward reference, undefined
  symbol, runtime-require alias, unboot-able daemon, boot-marker spoofing,
  tree-scoped refusal, leaked GIT_DIR immunity, guard-chain membership,
  land-replay tree-guard membership, **no daemon process starts when
  handoffd.bb is loaded**) still refuses exactly as before. Recall is not
  lowered.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1427LoadGuardCoversEveryScriptInvariants.property.test.js`: 3/3
  properties hold (P1: analysed count equals listed count, a defect after
  a stdin-draining script is never hidden; P2: no entry-call shape or
  `-main` arity is ever executed by the probe; P3: a defect behind any
  entry-call shape still refuses, naming file and symbol).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1427-the-load-guard-covers-every-script-and-runs-none.feature`
  — 6/6 scenarios pass.
- **Directly reproduced the ticket's own two named symptoms and confirmed
  both are fixed:**
  - `bash check_bb_scripts_load.sh --all` now reaches past script 107 (the
    stdin-draining point) and correctly analyses the full tree, finding
    exactly the one remaining known-bad file (`post_qa_branch_sweep.bb`,
    explicitly out of scope — BL-1426) — confirming the drained-loop
    defect is fixed and coverage claims are now true.
  - `check_swarm_detached.bb` and `clear_identical_untracked_and_merge.bb`
    (previously falsely refused for an ArityException their real callers
    never hit) now both analyse clean via `bb_load_analyse_driver.bb`
    directly.
- **Side-effect check**: confirmed no marker files or unexpected daemon
  processes appeared from any analysis run in this session (`pgrep -fl
  'node --test|stryker'` and a live `ps` check before/after) — the probe
  genuinely never executes an entry point.
- The coder's own commit fixed 4 previously-unreached defects the restored
  coverage surfaced (`model_factory_lib.bb`, `model_steward_evaluate_lib.bb`,
  `pack_staffing_gate_lib.bb` — each gained an idempotent self-load of
  `model_steward_lib.bb`; `post_commit_push.bb` — gained a lib-path
  fallback for a probe's empty `*command-line-args*`). Re-ran every named
  real caller's own test suite independently: `model_factory_test_runner.bb`,
  `pack_staffing_gate_lib_test_runner.bb`, `model_steward_test_runner.bb`,
  `test_bl1390_post_commit_push.sh` (19/19), `test_model_factory_cli.sh`
  (9/9), `test_pack_staffing_gate.sh` (7/7) — all pass. The idempotent
  self-load and the `or`-fallback are both no-ops for every real,
  already-working caller (confirmed by reading each diff: the fallback
  only fires when the first positional arg is absent, which a real
  invocation never omits).
- Guard-chain regression: `bash test_run_commit_guards.sh` (12/12) and
  `npx vitest run test/bl1252CommitGuardAggregationInvariants.property.test.js`
  (5/5) both green — this guard's place and name in the chain are
  unchanged, as the ticket requires.
- `required_wiring`: `check_bb_scripts_load.sh` confirmed calling the
  probe and boot step both with `</dev/null` (grep, lines 108/225).
  `specs/pipeline/steps/bl1427LoadGuardCoversEveryScriptSteps.js` exports
  `registerSteps`.
- Docs: `docs/how-to/BL-1252-...md` updated with a new "The probe covers
  every listed script and runs none of them (BL-1427)" section, cross-
  linked.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — merging up and landing.
