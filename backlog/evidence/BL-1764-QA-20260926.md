# BL-1764 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter a411d743d4, merged into QA as f0779774a2 (checked against
both parents; the only divergence from the incoming side is BL-1700's paths,
kept out on purpose after that parcel's bounce and revert at 5915c8016d).

Gates, one run each on f0779774a2 (qa-gather):
- straggler check: the opening check matched one `node` (pid 10093, not a QA
  run); the closing check found none.
- `qa-sibling-check.js status`: `VERIFY BL-1764`.
- standing-red register: 5 rows, all owned, `unowned: []`.
- `pre_qa_gate.sh`: OK (required_wiring: the step handler exports
  `registerSteps`).
- `npm test`: exit 0.
- `npm run test:properties`: exit 0; only the allowlisted BL-871
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors.

Ticket qa_e2e:
1. `run_acceptance.sh` on the BL-1764 feature: 3 of 3 (01 x2, 02).
2. `TZ23=Etc/GMT+2` (local 23:02 at run time) and `UTC`:
   `npx vitest run test/nightClosingCeremonyRun.test.js` 20 of 20 under both.
3. `backlog/standing-reds.tsv` row 38 names BL-1764 for
   `extension/test/nightClosingCeremonyRun.test.js` on the parcel commit. It
   retires at the land.
4. Own paths: `extension/test/nightClosingCeremonyRun.test.js`,
   `specs/pipeline/steps/bl1764NightCeremonyTestsWallClockSteps.js`, the
   feature (hardener's mutation manifest) and `backlog/evidence/BL-1764-*.md`.
   The QA branch also carries in-flight BL-1704/BL-1766 content; the land is
   tip-pure.

Invariant (same tests, same assertions): the test file has 0 `Date.now()`,
20 `test(` and 32 `runNightClosingCeremony(` calls against main's 20 and 32,
and `grep assert` output is byte-identical to origin/main's. Only the start
instant changes (`FIXED_NOW_MS`, local 2026-09-25 12:00).

Article 4.5: no production path changed (test file and step handler only).
