# BL-631 QA bounce — 2026-08-19

## Reviewed commit
`ee674c700` (merge of documenter's `1fb1995177` into QA). Lineage verified:
`fb32c325a3` (coder bounce fix), `3493f994ca` (architect merge),
`af3a5c77a` (hardener merge), `1fb1995177` (documenter) are all ancestors
of this commit via `git merge-base --is-ancestor`.

## Complete inventory (Article 4.4 — full pass, not first-failure-stop)

1. **bb unit test runners** (4) — PASS: `babysitterd_sweep_lib_test_runner.bb`,
   `babysitter_lib_test_runner.bb`, `babysitter_assess_lib_test_runner.bb`,
   `babysitterd_freshness_lib_test_runner.bb`.
2. **`test_babysitterd_lifecycle.sh`** — 8/8 PASS.
3. **`test_babysitter_check.sh`** — 9/9 PASS (A–I), including scenario A
   (the cleaner-bounced scenario), independently reconfirmed.
4. **Acceptance** — `run_acceptance.sh
   specs/features/BL-631-babysitter-detects-pipeline-work-on-main.feature`
   — 17/17 PASS, including scenario 17 (2026-07-25 regression set
   reproduces exactly) and scenario 8 (merge-commit case via
   `git diff-tree -m --first-parent`).
5. **Required wiring** — independently grepped, present: `pipeline-code-on-main`
   in both `babysitter_check.bb` and `babysitterd_sweep_lib.bb`;
   `check-pipeline-code-on-main` reached from `assemble-findings`;
   `check_pipeline_code_on_main.sh` shelled from `babysitter_check.bb` as
   the single-source path definition (BL-632's `--list-paths`).
6. **Full extension unit suite** (`npm test`, run detached in a private
   tmux socket — host load peaked at load-average 67.86 on 4 cores during
   this pass, forcing several tests over their timeouts):
   - **D1 — REAL, BL-631-caused**: `test/tmuxReaperGuard.test.js > the real
     specs/pipeline/steps tree has zero tmux-reaper violations` FAILS,
     reproduced identically in an isolated low-load re-run (32.98s, no
     other load). `specs/pipeline/steps/bl631BabysitterDetectsPipelineCodeOnMainSteps.js`
     starts a tmux server (`new-session`, `addFakeCoordinatorPane`, line
     104) but hand-rolls its own `trackedSockets`/`trackedRoots` +
     `afterEach` teardown instead of requiring
     `specs/pipeline/steps/lib/fixtureReaper.js` and calling its `track()`.
     This is not a style nit: `fixtureReaper.js`'s own header (BL-458)
     documents exactly why the hand-rolled pattern is insufficient — an
     `afterEach` only runs on normal completion; it installs no
     `exit`/`SIGINT`/`SIGTERM` handlers, so a runner killed by timeout,
     Ctrl-C, or OOM leaks the fixture's detached tmux server permanently,
     the identical failure mode BL-458 was filed to close project-wide
     (four such leaked trees measured surviving ~18h in that incident).
     Introduced in `cf2c2ad96` ("BL-631: babysitter sweep detects pipeline
     code landing on main outside QA"), by coder.
   - 3 other failures in the same full-suite run
     (`test/conciergeTick.test.js` × 2, `test/renderBriefingBurndownCli.test.js`
     × 1) — investigated and **not attributed to BL-631**: zero diff
     touches these files or their `extension/src/` dependencies between
     the pre-merge QA HEAD (`73d247545`) and this commit
     (`git diff --stat 73d247545 ee674c700 -- extension/src/` is empty).
     Re-run in isolation at low load:
     `renderBriefingBurndownCli.test.js` PASSED (confirms its 20000ms
     timeout in the full run was load-induced), while both
     `conciergeTick.test.js` failures reproduced deterministically
     (pivoted-grid epic grouping / whitespace assertion) — a real,
     pre-existing defect on the QA branch, but not this ticket's and not
     part of this parcel. Surfaced separately to the coordinator by
     `note`, not bounced here (BL-425/BL-576: don't misroute a defect this
     ticket did not introduce).
7. **Property tests** (`npm run test:properties`, same detached run) — 2
   failures, both plain test timeouts
   (`bl632CommitTimeGuardInvariants.property.test.js`,
   `bl760DuplicateChainGuard.property.test.js`), both on files with zero
   diff in this merge, both consistent with the measured 67.86 load spike.
   9 "Unhandled Error" entries, all the exact allowlisted benign
   `[vitest-worker]: Timeout calling "onTaskUpdate"` artifact
   (engineering.prompt / BL-871) — none block the lane.
8. **Leak/process check**: `pgrep -fl 'node --test|stryker'` empty before
   and after this pass; private test tmux socket
   (`/tmp/qa-bl631-testrun.sock`) killed after use; `git status --short`
   clean in the QA worktree pre-bounce.

## Failing command (D1)
```
cd extension && npx vitest run test/tmuxReaperGuard.test.js
```

## Commit hash
`ee674c700b0a3ae5933f1dbb91097c3e0a1a92ba`

## First error excerpt
```
FAIL  test/tmuxReaperGuard.test.js > the real specs/pipeline/steps tree has zero tmux-reaper violations
AssertionError: expected zero tmux-reaper violations under specs/pipeline/steps, found:
/Users/ldecorps/projects/swarmforgevc/.worktrees/QA/specs/pipeline/steps/bl631BabysitterDetectsPipelineCodeOnMainSteps.js: starts a tmux server ('new-session') but does not require ./lib/fixtureReaper and call track()
```

## Failure class
`unit` (a standing regression guard fails against BL-631's own new file).

## Expected vs observed
Expected: zero tmux-reaper violations under `specs/pipeline/steps/` (the
standing gate `tmuxReaperGuard.test.js` asserts). Observed: one violation,
BL-631's own `bl631BabysitterDetectsPipelineCodeOnMainSteps.js`, which
starts a tmux server without wiring the shared `fixtureReaper` module's
`track()`, leaving an abnormal-exit (timeout/SIGTERM/SIGINT/OOM) leak path
that only the shared module closes.

## Remediation pointer
`specs/pipeline/steps/bl631BabysitterDetectsPipelineCodeOnMainSteps.js`:
replace the hand-rolled `trackedSockets`/`trackedRoots` + `afterEach`
teardown (lines 33–49, and the `trackedSockets.push(sock)` call at line
108) with `require('./lib/fixtureReaper')` and its `track()`/`reap()`
API, matching every other step-handler file that starts a fixture tmux
server. Owning role: **coder** (introduced in `cf2c2ad96`).
