# Documenter rematch3 — BL-828

## Ticket
BL-828-bubble-collapsed-gesture-model

## Why
Duplicate bounce2 `git_handoff` arrived after rematch2; `origin/main`
advanced again before the next gate. Rematch onto live main immediately
before handoff.

## Rematch posture
`git fetch && git reset --hard origin/main` → restore BL-828 from
`a81af77cfe`. `dels=0`; ancestry rechecked before handoff.

## Abandoned
Prior impure tips including `a81af77cfe`.

## Inventory
NONE.

By documenter.
