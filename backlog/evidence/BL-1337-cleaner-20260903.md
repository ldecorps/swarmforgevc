# BL-1337 — cleaner pass (2026-09-03)

## Scope

Diff (`d702581188`..`922078d70f`) touches only files it declares:
`bob_starting_cast_lib.bb` (extended, not forked), `bob_starting_cast_cli.bb`
(`--profile` flag added to existing verbs), and the ticket's own step/
property/bb test files. No unrelated files touched.

## Human ruling and invariants checked against the diff

- **Ruling honored**: cast + ModelFactory overlay only. No pack-conf renderer,
  no second generator, no second apply path — `apply-via-modelfactory-overlay`
  gained a second arity (gated), the original one-arg BL-1181 call site is
  byte-unchanged.
- **Invariant 1** (runnable only if every seat handshakes): `handshake-role`
  walks ranked candidates in order, `generate-cast-from-profile` produces NO
  cast entry for a seat nothing could staff; `runnable?` is
  `(empty? unstaffable)`.
- **Invariant 2** (fail loud, name the seats): `generation-failure-text`
  names every unstaffable seat plus each rejected candidate's verdict.
- **Invariant 3** (no credential material in any written file): host
  reachability is answered in the CLI adapter only, as a boolean
  (`host-reachable?` checks env-var presence, never reads or returns a
  value); the lib takes an injected predicate and reads no keys itself.
  `evidence-note-text` prints provider/model names and verdicts only.
- **required_wiring**: `apply-via-modelfactory-overlay` is the gate site,
  confirmed — 2 occurrences (definition + the new 2-arg refusal path), the
  same live door BL-1181's cast is installed through.
- **Constraints**: no cost/price table touched (grepped, zero hits); "propose,
  never silently install" — acceptance scenario 6 confirms the live pack
  conf is byte-unchanged across generation.

## Verification (run, not assumed)

- `bb swarmforge/scripts/test/bl1337_profile_cast_test_runner.bb` — ALL PASS.
- `run_acceptance.sh specs/features/BL-1337-…feature` — 7/7 pass, matching
  the ticket's own `qa_e2e_procedure` scenario-for-scenario (eligible+
  reachable, not-certified fallthrough, certified-but-unreachable
  fallthrough, floor failure, live-pack-untouched, no-secret-material).
- `npx vitest run --config vitest.properties.config.mjs
  bl1337ProfileCastInvariants` — 15 consecutive green runs, no flake.
- Named-neighbor regression suites, re-run directly:
  `bob_starting_cast_test_runner.bb` — ALL PASS; `model_steward_test_runner.bb`
  — ALL PASS; `run_acceptance.sh specs/features/BL-1181*` — 3/3 pass.

## Mutation-site advisory (BL-485)

Step/fixture/property files at 151–251 sites, consistent with this
session's range; no split warranted.

## Verdict

No defect. Forwarding.
