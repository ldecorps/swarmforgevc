# BL-1368 — documenter pass (2026-09-05)

## Scope reviewed

Re-read the ticket, the acceptance feature
(`specs/features/BL-1368-an-approval-commit-names-the-decider.feature`), and
the full chain of prior evidence (coder `BL-1368-coder-20260905.md`, cleaner
`BL-1368-cleaner-20260905.md`, architect `BL-1368-architect-20260905.md`,
hardener `BL-1368-hardener-pass-20260905.md`).

Change under review: both `human_approval` writers
(`extension/src/bridge/bridgeServer.ts`,
`extension/src/tools/telegramFrontDeskBotCore.ts`) plus the Expedite site now
compose their commit message via a shared `HUMAN_DECISION_BYLINE` +
`humanDecisionCommitMessage()` in `extension/src/util/commitIntegrityRunner.ts`,
instead of hardcoding `By coder.` on a commit that records a human decision.
A pipeline role's own commits are untouched.

## Documentation surfaces checked

- `docs/reference/Specification.MD` — grepped for `byline`; the only hit
  (Operator→Concierge rebrand, BL-453) is about chat message signatures, not
  commit bylines, and explicitly scopes that rename out. No entry describes
  the `By coder.` literal or any commit-byline behavior for approvals, so
  there is nothing stale to correct and nothing new to add — this ticket
  changes an internal attribution detail, not a documented user-facing
  behavior.
- `swarmforge/handoff-protocol.md`, `swarmforge/backlog-schema.md` — grepped
  for `By coder.` / `HUMAN_DECISION_BYLINE` / `human_approval writers`: no
  hits. Commit byline conventions for approval commits were never documented
  there.
- `docs/how-to/BL-1367-approval-from-any-surface-carries-its-ruling.md` line
  106 mentions `By coder.` only in its "Out of scope" section, correctly
  pointing at this ticket (BL-1368) as the place that fixes it — historically
  accurate as written, needs no edit now that BL-1368 has landed; it does not
  claim the byline is still `By coder.` as current behavior.
- Registered diagrams (`docs/diagrams/architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`): none depict commit-message
  composition or byline attribution; none of their change-triggers fired.
- No README, guide, or command-list references the approval-commit byline.

## Verdict

NONE — no living documentation describes the fixed behavior, so none is
stale. The change is fully self-documenting in code
(`commitIntegrityRunner.ts` carries a comment explaining the incident and the
invariant it now protects). No doc edit made.

By documenter.
