# BL-1098 architect bounce — 2026-08-23

Reviewed commit: `0608d11617` (`BL-1098/BL-1081: DRY push-sweep gates and
split oversized ACP host modules.`).

## Review inventory

### D1 — behavior / cleaner

The incoming `git_handoff` names only
`BL-1098-silent-revert-of-landed-content`, but commit `0608d11617` also adds
and refactors the ACP host implementation for BL-1081:

- `extension/src/swarm/acpHostPaneArgs.ts`
- `extension/src/swarm/acpHostPanePlan.ts`
- `extension/src/swarm/acpHostRuntime.ts`
- `extension/src/swarm/acpSeatState.ts`
- `extension/src/swarm/acpSessionEvents.ts`
- `extension/src/swarm/acpSessionMethods.ts`
- `extension/src/swarm/acpSessionTypes.ts`
- `extension/src/tools/acp-host-pane.ts`
- `extension/test/acpHostPaneArgs.test.js`

This violates the one-ticket parcel and scoped-review rule: approving
BL-1098 cannot also approve unrelated BL-1081 behavior. Split the commit so
the BL-1098 parcel contains only the silent-revert gate changes, then forward
BL-1081 as its own parcel under its own stable task name.

## Completed checks

- Dependency gate passed for every changed TypeScript file.
- BL-1098's three declared invariants are covered by the existing
  `push_sweep_lib_property_runner.bb` property runner; its non-vacuity checks
  passed.
- Co-change review found no ticket-specific coupling requiring a change.
- The full extension suite is blocked outside this parcel by the known
  `CURSOR_API_KEY` environment failure in `topicMakeTopBridge.test.js`
  (BL-720).
