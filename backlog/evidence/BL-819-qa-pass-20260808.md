# BL-819-ticket-lifecycle-ledger — QA final pass (2026-08-08)

## Commit checked

`a7e8536c` (QA worktree merge of documenter's `8bb7f48af5`, batched with
BL-839/BL-822/BL-773). Full ancestry, environment findings, and the shared
batch details are in `backlog/evidence/BL-773-qa-pass-20260808.md` — this
file covers what is specific to BL-819.

## D1/D2/D3 from the prior QA bounce (`backlog/evidence/BL-819-qa-bounce-20260807.md`)

- **D1 (coder — dropped lineage):** `6a6e69b4` (the wiring-test fix) is now
  confirmed an ancestor of `a7e8536c`
  (`git merge-base --is-ancestor 6a6e69b4 a7e8536c` — exit 0). Re-ran
  `swarmforge/scripts/test/test_lean_ledger_bb_wiring.sh` directly: all six
  cases (A1–A3, B1–B3) PASS, covering both `.bb` call sites'
  happy/non-zero-exit/missing-CLI branches. **FIXED, confirmed in-lineage.**
- **D2 (hardener — pass never ran against this ticket's code):** confirmed
  cleared. `cd67e036` is a real hardener pass against
  `extension/src/metrics/leanLedgerCompose*.ts`,
  `extension/src/metrics/leanLedgerStore.ts`,
  `extension/src/quality/leanLedger.ts` — 5 CRAP fixes, 1 DRY fix
  (`parseFlagPairs` extraction shared with `bounceArgsCore.ts`), 6 real
  coverage gaps closed, 10-mutation hand-authored sweep (Stryker blocked by
  host load, per documented policy) with 1 genuine gap found and closed
  (`role` missing from `leanLedgerEventNaturalKey`). **FIXED.**
- **D3 (documenter — required documentation missing):** partially cleared;
  see `backlog/evidence/BL-773-qa-pass-20260808.md` for the full analysis.
  The reference doc (`docs/reference/BL-819-ticket-lifecycle-ledger.md`)
  is delivered and cross-linked. The `coordinator.prompt` section is not,
  but that half is constitutionally the **specifier's** to write (Article
  1.2; the ticket's own notes say so explicitly), not gating, and routed
  onward via `note` rather than a bounce.

## Independent re-verification (this pass)

- Unit (this ticket's own files, part of the 406/406 green full-suite run):
  `leanLedger.test.js`, `leanLedgerCompose.test.js`, `leanLedgerStore.test.js`,
  `leanLedgerRecordCli.test.js`.
- Property (`test/leanLedgerInvariants.property.test.js`): 4/4 PASS,
  independently re-run.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-819-ticket-lifecycle-ledger.feature`): 12/12 scenarios
  PASS.
- Wiring: `done_with_current_task.bb` and `commit_integrity_cli.bb` both
  genuinely shell out to `lean-ledger-record.js` (confirmed by the D1
  wiring test re-run above, which exercises the real call sites, not a
  stand-in).
- Invariants: both declared invariants (idempotent double-append /
  snapshot-is-a-pure-fold; every field traceable to a known instrument)
  covered by the property test above.

## Verdict

D1 and D2 fully cleared and confirmed in-lineage. D3 substantially cleared;
the remainder is a specifier deliverable, not a blocking defect — see the
accompanying `note` to specifier + coordinator. **APPROVED** as part of the
BL-839/BL-819/BL-822/BL-773 batch.

By QA.
