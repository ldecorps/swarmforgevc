# BL-1757 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 16a8e4e055, merged cleanly into QA as a7dcf0e201.
Safety was checked before anything ran: the step handler dry-runs both
launchers, and scenario 04's real run refuses before the supervisor spawns.
test_launch_front_desk.sh pins SWARMFORGE_FLEET_HOME to a mkdtemp (BL-1779
hazard), so nothing touched the live bridge.

Gates (qa-gather, one run each on a7dcf0e201): `VERIFY BL-1757`, register
`unowned: []`, wiring OK (both launchers' SWARMFORGE_TOOLING_ROOT and the
handler), `npm test` exit 0, `npm run test:properties` exit 0, acceptance
6 of 6 (4.2 s).

qa_e2e:
1. Feature: 01, 02 x3, 03, 04 all ok.
2. test_launch_front_desk.sh, test_start_stop_cursor_bridge.sh and
   test_tooling_root_config.sh are all green and registered `standing` in
   suite-manifest.tsv.
3. `FRONT_DESK_LAUNCH_DRYRUN=1 bash swarmforge/scripts/launch_front_desk.sh
   "$PWD"` resolves the bridge and bot under this checkout's own
   extension/out/tools and serves this checkout's root (invariant 1).
4. Changed paths: swarmforge.sh, launch_front_desk.sh,
   start_cursor_bridge.sh, the new tooling_root_lib.sh (shared helper),
   their three shell tests plus suite-manifest.tsv, the step handler, the
   BL-1582 onboarding how-to, and evidence. All fall under Scope.

Invariants: these are Bash launch scripts, which carry no property-test
wiring (engineering "Startup Tools"). The feature scenarios (03 is
invariant 1; 02's rows assert the served root is the target) and the shell
tests cover both.
