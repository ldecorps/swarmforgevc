# QA pass — BL-1432 — 2026-09-05

Commit under review: `5f549116d2` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`dbdbbdfdc4` BL-1432
coder, confirmed ancestor via `git merge-base --is-ancestor`).

This is the fix for the multi-minute land-walk I personally hit repeatedly
throughout this session (the mint-vs-walk race, BL-1431's own cause).
Reviewed with corresponding care since it touches the live land chokepoint.

## Verification

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` and
  `task_scope_gate_lib_test_runner.bb`: ALL PASS.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1432-the-land-walk-ranges-over-the-parcel.feature` —
  5/5 scenarios pass, including both re-point scenarios and the
  never-re-point-with-work-in-it safety scenario.
- Regression: ran all 6 land-step sibling features directly
  (BL-1389, BL-1375, BL-1354, BL-1343, BL-1315, BL-1431) — 34/34 pass, no
  change to what counts as landed/unlanded/entangled (invariant 2).
- **Read the live-default wiring directly**: `land-plan`'s `:base` key,
  when omitted (which `land_step_cli.bb` does — confirmed by grep, no
  changes needed there), automatically computes
  `(or (parcel-own-base root task-ticket-id) origin-main)` — so option 2
  (the bounded walk) is live for every real land today, with zero call-site
  changes required. Confirmed this is not merely claimed but structurally
  true by reading `land-plan`'s own default-resolution code.
- **Live-timed a real land decide-only run**: ~2 seconds (`time
  land_main_publish.sh --decide-only`), consistent with the fix — this
  session's earlier lands (before BL-1431/BL-1432) took 3.5-7+ minutes on
  the same swarm-QA branch.
- `required_wiring`: `specs/pipeline/steps/bl1432LandWalkRangesOverTheParcelSteps.js`
  exports `registerSteps`.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures — no TS
  production files touched by this ticket (bb-only), no regression.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Spec-gap note (not a bounce — recorded and routed separately)

The cleaner's own evidence honestly flags a real inconsistency BETWEEN two
sections of this ticket's own text: `qa_e2e_procedure` step 2 describes a
LIVE, end-to-end check (a real `land_step_cli.bb` run followed by `git
rev-list --count main..swarmforge-QA` shrinking after a re-point) that
**cannot be satisfied by this parcel alone** — I independently confirmed
`grep post-land-repoint! swarmforge/scripts/*.bb` outside
`land_step_lib.bb` and its own tests finds no live call site (not
`land_step_cli.bb`, not `post_qa_branch_sweep_lib.bb`, not `handoffd.bb`).
This matches the ticket's own `required_wiring` section, which names only
the step-handler file, and its "How" section's explicit "may be the
place" (direction, not mandate) framing for option 1's wiring — so this
is a genuine spec-authoring inconsistency (the human's ruling on WHICH
mechanisms to build vs. the qa_e2e text's assumption they'd be live-wired
here), not a coder shortcut or a missing deliverable. The actual automated
gate — the acceptance feature — tests `post-land-repoint!` directly and
correctly, and fully passes.

Per Article 4.4's spec-gap class: this does not bounce the parcel (the
feature, the real gate, is satisfied); I am completing this task normally
and sending a `note` to the specifier (and coordinator) naming the gap and
recommending a follow-up ticket for wiring `post-land-repoint!` into a
real call site (BL-668's sweep or `land_step_cli.bb` itself), per the
cleaner's own suggested next step.

## Verdict

APPROVED — merging up and landing. Spec-gap for qa_e2e step 2's live-wiring
assumption routed to specifier separately, not blocking this approval.
