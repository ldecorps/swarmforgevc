# BL-1724 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Checks run (from `extension/`, after `npm run compile`):
- `node out/tools/dependency-gate.js test/bl1652HandoffdRespawnReadingsWiring.test.js`
  → PASSED, no forbidden edges.
- `node out/tools/co-change-report.js test/bl1652HandoffdRespawnReadingsWiring.test.js`
  → suspected coupling with `laneProcessLib.test.js` /
  `lane_process_lib.bb` (3 co-changes each), informational only — this
  ticket's scope is this one file, no import link, no production code
  touched.
- `npx vitest run test/bl1652HandoffdRespawnReadingsWiring.test.js`: 7/7
  passed, 912ms — well under the 7.0s per-file budget.
- `npx vitest run --config vitest.properties.config.mjs test/bl1280MkdtempMigrationInvariants.property.test.js`:
  6/6 passed, confirming the coder's `mkTmpDir` → `mkSharedTmpDir` fix is
  correctly scoped: `tmpDirSetup.js` registers `sweepSharedTmpDirs` in a
  per-FILE `afterAll`, not per-test, matching the coder's claim.
- Acceptance pointer (from repo root):
  `node specs/pipeline/cli.js specs/features/BL-1652-the-chase-sweep-never-respawns-a-busy-role-and-respawns-at-most-once-per-sweep.feature`
  → 4/4 ok.

Invariants review: the ticket's one declared invariant ("One run of the
file starts exactly one bb process...") has no property test, with a
stated non-encodability reason (BL-1724-coder-20260925.md) — it quantifies
over the file's own process-spawn structure, not a pure module's
input/output space; no generator input exists to vary. Accepted: verified
by construction (single `execFileSync('bb'` call site, `grep -c` = 1) and
by the timing evidence itself (912ms–1.6s vs. the pre-fix 8–9.6s alone),
which would regress visibly if a case reintroduced its own `bb` call.

Correctness read: traced `runAllCases`' tab-delimited `id\tvalue` line
parsing and the `beforeAll`/`afterAll` fixture lifecycle (heartbeat fixtures,
`laneChild` spawn/kill ordering, `mkSharedTmpDir` roots) against the
pre-BL-1724 per-case version — case ordering, spawn-then-sleep-then-run
sequencing, and the `role-lane-running?` process lifecycle are unchanged;
no defect found.

Property testing pass: test-only change, no pure production module
touched — no new property-test obligation.

No production code touched; test-only change satisfies the changed-path
unit test gate by construction (the file itself is the test).

By architect.
