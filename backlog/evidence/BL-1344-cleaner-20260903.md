# BL-1344 — cleaner pass (2026-09-03)

## Scope

Diff (`b754754e3d`..`379f4ad9be`) touches only files it declares: the new
`babysitter_waive_lib.bb` (pure core), `babysitter_waive.bb` (CLI),
`babysitter_check.bb` (consumer wiring), and the ticket's own step/property/
bb test files. No unrelated files touched.

## Invariants checked against the diff

- **Invariant 1** (one key, one waive): `partition-findings` matches on
  exact `:key` equality only (`contains? (:waives read-result) key`); no
  prefix/pattern matching anywhere in the lib. Acceptance scenario 3 and
  property invariant 1 both construct a same-class-different-commit pair
  by enumeration (not drawn), so the corner that matters is reached every
  run.
- **Invariant 2** (sweep never waives): `babysitter_check.bb` only calls
  `read-waive-store`/`partition-findings` (both read-only); `record-waive`
  is called only from the CLI, never from the sweep. Scenario 4 measures a
  byte-for-byte store diff across repeated sweeps.
- **Invariant 3** (unreadable = nudge): `partition-findings` suppresses
  only on `{:ok? true}` from `read-waive-store`; every other outcome
  (missing store is `{:ok? true :waives {}}` — correctly NOT an error;
  unreadable, malformed, incomplete entry are all `{:ok? false}`) leaves
  every finding in `:to-nudge`. Confirmed by acceptance scenarios 6/7 and
  property invariant 3.

## required_wiring anchors — verified

- `babysitter-waive-lib` used (not merely loaded) at the live nudge
  decision in `babysitter_check.bb` — `read-waive-store` and
  `partition-findings` both called, output threaded into `decide-nudges`.
- `bl1344BabysitterFindingWaiveSteps` registered in
  `specs/pipeline/steps/index.js`.

## Scope reading worth flagging (coder recorded it; I confirm it, not a defect)

The waive silences only the coordinator **nudge**; the same finding's
`BABYSITTER_ESCALATION` operator escalation is untouched — `to-escalate`
in `babysitter_check.bb` is still computed from the unfiltered `findings`,
not `nudgeable`. This matches the ticket's own wording exactly
(`invariants` and the `HOW` section both say "before nudging" /
"suppresses a nudge", never "escalation"), so it is in scope as written,
not a gap against what was asked. Worth the architect's eyes as a possible
follow-up, per the coder's own note — not a bounce-worthy defect here.

## Verification (run, not assumed)

- `bb swarmforge/scripts/test/bl1344_waive_lib_test_runner.bb` — ALL PASS.
- `run_acceptance.sh specs/features/BL-1344-…feature` — 7/7 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1344WaiveInvariants` — 15 consecutive green runs, no flake.
- Named-neighbour regression suites, re-run directly:
  `bash swarmforge/scripts/test/test_babysitter_check.sh` — ALL PASS;
  `bb swarmforge/scripts/test/babysitterd_sweep_lib_test_runner.bb` — ok.

## Mutation-site advisory (BL-485)

Production file(s) not present here beyond the lib/CLI (not JS, not
counted by this tool); step/fixture/property files at 94–204 sites,
consistent with this session's range; no split warranted.

## Verdict

No defect. Forwarding.
