# BL-1353 land note — merge-deletion guard blocked a plain merge, 2026-09-05

Same BL-1403 class as this session's other lands. A plain `git merge
a3c7148136` into the QA worktree would have been refused by
`check_merge_deletion.sh` over the unattributed intake-archive
deletions. Replayed this ticket's own attributed paths (across a
superseded/reconciled pipeline history — two architect passes, two
hardener passes, three documenter touches) as a plain, non-merge commit
(`5e28b04883`) instead.

Two real gaps found and fixed during the replay that weren't obvious
from a naive per-commit path list:
- The QA worktree's copy of `specs/features/BL-1353-task-arrived-is-not-an-escalation.feature`
  still had the PRE-amendment wording (scenario 03's re-tense from
  "hibernate" to "relaunch" per the specifier's 2026-09-04 amendment,
  `98454ae8c4`) — broke acceptance scenario 3 with "no step handler
  matched" until replaced with the documenter tip's version (which
  matched `origin/main`'s already-landed version byte-for-byte).
- `docs/how-to/BL-653-operator-escalation-driven-wake-model.md` was
  missing the `TASK_ARRIVED` retirement note that `required_wiring`
  anchor 1 needs — present on the documenter's tip, absent on
  `origin/main`, so it needed replaying as part of this ticket's own
  diff.

Re-verified `operator_lib_test_runner.bb` ALL TESTS PASSED,
`operator_lib_bl653_property_runner.bb` ALL PASSED, property 3/3,
acceptance 4/4, then built and landed the tip-pure commit (`09ae74acd8`).

Land-approval recorded citing `5e28b04883` (the plain replay commit) as
source: `{"at":"2026-09-05T03:20:02Z","ticket":"BL-1353","commit":"09ae74acd8","source":"5e28b04883"}`.
Verified `is_qa_ancestor.sh 09ae74acd8` prints `approved: ... - BL-1334`.
`abandoned_commits: [a3c7148136]` recorded on the ticket.

By QA.
