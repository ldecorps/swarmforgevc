# BL-975 — soak verdict PASS (amended procedure, confirmed post-fix daemon)

QA, 2026-08-22, re-run under the specifier's amended `qa_e2e_procedure`
(amendment `c01c779d7`, disposing my earlier BLOCKED verdict, evidence
`f8e61157b`). This supersedes that file's inconclusive result — see notes.

## Step 1 — daemon identity and post-fix confirmation

    .swarmforge/daemon/handoffd-build.json:
      build_sha  = fb1acf132a71596b4706c0b2c5530e4b0185943f
      started_at = 2026-08-22T19:18:07Z

    git merge-base --is-ancestor a5ecfc281b fb1acf132a71596b4706c0b2c5530e4b0185943f
      -> CONFIRMED (exit 0): this daemon's loaded code contains BL-967's fix.

    Running process: pid 1858730, `bb .../handoffd.bb .`, still the same pid
    throughout this observation (no restart) - confirmed via `ps` and via
    handoffd.log's own unbroken heartbeat-cycle sequence (below).

## Step 2 — swarm activity in the window

Window: **2026-08-22T19:42:07Z → 2026-08-22T20:12:07Z** (30 minutes, fully
inside this daemon's uninterrupted uptime, which began 19:18:07Z and was
still running unbroken at observation time, 20:13Z+).

handoffd.log shows continuous sweep activity throughout the window
(push-sweep, master-main-reconcile, dispatch-gap-sweep, chase-sweep,
flow-watchdog-sweep, etc., each completing every few seconds) and active
parcel handoffs recorded in the mailbox directories inside the window
(e.g. `.../coordinator/sent/10_20260822T191821Z_001017_from_coordinator_to_coder.handoff`
just before the window opened, plus ongoing chase/dispatch sweep output
throughout it). The swarm was actively working, not idle.

## Step 3 — freshness-incidents.log, handoffd entries only

    awk over epoch range [1787427727, 1787429527] (the window above):
      ZERO entries of any kind (any daemon) in this range.

Broader context (not required by step 3, recorded for completeness): the
file's last entry overall is `epoch=1787403841` (2026-08-22T13:04:01Z),
all `daemon=babysitterd`. There has been no `daemon=handoffd` entry at all
since well before this daemon started - matching the specifier's amendment
note. **PASS half 1: zero new handoffd freshness entries in the window.**

## Step 4 — completed heartbeat cycles

    First cycle in window:  2026-08-22T19:42:07.207534173Z heartbeat cycle=874-start
    Last cycle in window:   2026-08-22T20:12:03.388241024Z heartbeat cycle=1940-start

Cycles progressed continuously (874 → ~1940, roughly one cycle per ~1.7s)
with no gap, no restart marker, and no missing `-start`/completion pair
anywhere in the window. This is the FAST cadence, not BL-789's 120-232s
slow-but-healthy shape - that shape simply did not arise in this window,
so step 5 (no kill of a legitimately-slow cycle) is vacuously satisfied:
there was no slow cycle to kill, and grep for
`handoffd.*restart|restart.*handoffd|SIGKILL|process killed` across the
window returned nothing.

## Step 5 — no kill of a slow-but-healthy cycle

No kill/restart signature of any kind in the window (see above). N/A cycle
shape was fast throughout; nothing to misjudge.

## Observed, NOT this ticket's scope (recorded so a future reviewer doesn't
## mistake it for a stall, matching the ticket's own precedent for the
## chase-wake-skip-busy burst)

Five `flow-watchdog-alarm` WARN lines appear in the window
(19:44:47, 19:51:46, 19:55:55, 20:01:35, 20:05:54Z). These are a DIFFERENT
subsystem: per-parcel flow-stall monitoring on OTHER roles' dwell times
(cleaner->architect, coordinator->coder, architect->hardender), not
handoffd's own freshness/heartbeat health. None names a `handoffd` restart,
none is a `kill`, and the ticket's own constraints say "do not re-litigate
BL-967's gates" - handoffd's own behavior, which is this ticket's actual
subject, was clean throughout. Also present: the same
`chase-wake-skip-busy coder` burst pattern the ticket already flagged at
mint as BL-970's surface, not a stall signal by itself.

## Verdict: PASS

Zero handoffd freshness-incidents entries in a 30-minute window on a
confirmed post-fix daemon (build_sha ancestry verified), with continuous
completed heartbeat cycles and no restart/kill signature. BL-967's owed
observation is discharged.

## Relationship to the earlier BLOCKED evidence file

`backlog/evidence/BL-975-soak-20260822.md` (this session, earlier) recorded
BLOCKED against the ORIGINAL 2026-08-20-window procedure, which depended on
log data that had since rotated away. The specifier amended the procedure
(commit `c01c779d7`) to source post-fix confirmation from
`handoffd-build.json`'s `build_sha` instead of the unrecoverable audit log,
and re-scoped the window to any qualifying post-fix daemon rather than
specifically 2026-08-20's. That file's findings stand as a correct,
honest BLOCKED verdict against the procedure as it existed at the time; this
file is the successful re-run against the amended one, not a correction of
an error in the first.
