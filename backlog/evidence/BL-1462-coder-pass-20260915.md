# BL-1462 — coder pass (2026-09-15)

## What changed

- New `specs/pipeline/steps/lib/ticketYamlLookup.js`: `resolveTicketYamlPath(projectRoot, ticketId)`
  mirrors `pre_qa_gate_gather_lib.bb`'s `find-ticket-yaml-content` search
  order (backlog/active, then backlog/paused, then backlog/done -
  milestone subdirectories included, by the YAML's own `id:` field) -
  reuse of the gate library's own idea of where a ticket lives, not a
  second one. Returns `null` (never throws) when nothing matches, so a
  caller asserting on it fails naming the missing precondition.
- New `specs/pipeline/steps/lib/roleWorktrees.js`: `masterCheckoutPath` /
  `firstLinkedWorktreePath`, both derived from a live `git worktree list
  --porcelain` (the first entry is always the main checkout, by git's own
  contract) - never inferred from `.git` being a file/directory at the
  process's own cwd.
- `specs/pipeline/steps/bl968StepRegistryMaterializedTreeSteps.js`:
  - Scenario 03's Given now resolves BL-968's ticket YAML via
    `resolveTicketYamlPath` instead of a literal
    `backlog/active/BL-968-....yaml` path (stale since BL-968 closed and
    moved to `backlog/done` on 2026-08-20).
  - Scenario 04's Given now LOCATES a linked role worktree via
    `firstLinkedWorktreePath` instead of asserting the checkout the test
    process happens to run in is one; the fixed step file
    (`routingBreakEvenSteps.js`) is `require`d from THAT worktree's own
    path, so its lazy `resolveMainCheckout(__dirname)` resolution is
    genuinely exercised from a linked worktree, not from wherever the
    runner itself sits. No linked worktree found fails naming it (a real
    swarm install is the scenario's own premise), never a false red about
    the master checkout.
  - `TICKET_YAML_REL` constant removed (dead once the resolver replaced its
    one use).
- New `specs/pipeline/steps/bl1462Bl968HandlerReadsItsFixtureSteps.js`
  (required_wiring, lands in the same parcel as the feature): drives
  BL-968's real feature through `run_acceptance.sh` from the master
  checkout / a linked worktree (parsing the Node-test-runner TAP output for
  `ok 1..4` with no `not ok` line) for scenarios 01/03, the exported
  resolver directly against an mkdtemp fixture for scenario 02 (all four
  `<dir>` examples), and a `backlog/standing-reds.tsv` grep for scenario 04.
- New `swarmforge/scripts/test/bl1462_find_ticket_yaml_runner.bb`: thin CLI
  over the REAL `pre_qa_gate_gather_lib.bb/find-ticket-yaml-content`, for
  the Node-side mirror-agreement test (BL-897's rule) to compare against.
- New `extension/test/bl1462TicketYamlLookupAgreement.test.js`: bb and the
  JS resolver agree by CONTENT on each of the four known locations, on
  precedence when the ticket exists in more than one location at once
  (active over paused over done), and on "found nowhere" (both resolve to
  nothing when the ticket doesn't exist in the fixture at all).
- New `extension/test/bl1462TicketYamlLookupInvariants.property.test.js`
  (BL-654 invariant 2, coder-authored): every draw plants the target ticket
  under ALL FOUR known locations (an asserted reachability floor, not a
  sampled location choice), with a generated number of decoy tickets
  scattered across the other locations and generated non-`id:` padding
  lines around the target's own YAML content, proving the resolver finds
  it by its `id:` field regardless of clutter, not merely "the only yaml
  file present". Both new test files allocate their fixture root through
  `extension/test/helpers/tmpDir.js`'s `mkTmpDir` rather than a raw
  `fs.mkdtempSync(path.join(os.tmpdir(), ...))` - BL-1280's migration guard
  (triggered because a `.property.test.js` file is a staged path) caught
  the raw call site on first commit attempt; switched to `mkTmpDir` and
  re-verified green (see Verification).

## Verification

- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-968-step-registry-loadable-from-materialized-tree.feature`
  from this worktree (which has the fix): `# pass 4`, `# fail 0` (was
  `not ok 3` / `not ok 4` before this parcel, reproduced below).
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1462-the-bl968-handler-reads-its-fixture-not-the-live-checkout.feature`
  from this worktree: `# pass 4`, `# fail 3` right now - **expected**, not
  a defect in this parcel:
  - Scenario 01 (master checkout) and scenario 03 (linked role worktree,
    resolves to `.worktrees/QA`) both fail reproducing BL-968's ORIGINAL
    two defects, because neither the real master checkout
    (`/home/carillon/swarmforgevc`) nor `.worktrees/QA` has this fix yet -
    it exists only on this branch until it lands through the pipeline.
    This is the qa_e2e_procedure's own step 1/2 shape: those two checks are
    QA's post-land gate, not something the coder's own run can turn green
    (same shape as scenario 04 below).
  - Scenario 02 (the resolver over an mkdtemp fixture, all four `<dir>`
    examples): `ok` - passes now, as it must (no pipeline-position
    dependency).
  - Scenario 04 (`backlog/standing-reds.tsv` carries no row for BL-968's
    feature file): fails - the register row is removed by the documenter
    "in the same land" per this ticket's own `description:` "How"
    section, not by the coder.
- Non-vacuity, staged break (`SEARCH_ORDER` in `lib/ticketYamlLookup.js`
  temporarily dropped `'done'`): the property test failed immediately
  (`expected to resolve BL-1 under backlog/done, found nothing`); restored,
  `git diff` against the committed file empty afterward.
- `npx vitest run test/bl1462TicketYamlLookupAgreement.test.js --config vitest.config.mjs`
  - 6 passed.
- `npx vitest run test/bl1462TicketYamlLookupInvariants.property.test.js --config vitest.properties.config.mjs`
  - 1 passed (numRuns: 30, each covering all four known locations).
- `npx vitest run test/bl1371StepDiscovery.test.js test/bl1277UnscopedStepCollisionGuard.test.js --config vitest.config.mjs`
  - 17 passed (regression: the new handler file is discovered cleanly and
    registers no colliding unscoped pattern).
- `npx vitest run test/bl1280MkdtempMigrationInvariants.property.test.js --config vitest.properties.config.mjs`
  - 6 passed (regression: the two new test files' fixture roots are no
    longer a raw mkdtemp call site, after switching to `mkTmpDir`).
- Full `npm run test:properties` (the pre-commit guard's own run, triggered
  because a `.property.test.js` file is staged): first attempt refused the
  commit citing this exact `bl1280` violation at the raw-call-site commit;
  `bl1343ReplayNeverDropsOwnPathInvariants.property.test.js` failed in that
  same run but passed alone (recorded as a load flake by the guard's own
  BL-1407 re-run, not attributable to this parcel - unchanged by this
  parcel, no standing-red row exists for it, not touched here). Second
  attempt (after the `mkTmpDir` switch) committed clean as `44dc523284`.

## Invariants (BL-654)

1. *"No step of BL-968's handler asserts or depends on the live checkout's
   backlog bookkeeping state or its .git shape..."* — **stated reason, no
   property test**. This invariant quantifies over what the HANDLER'S OWN
   SOURCE does (which literal paths it hardcodes, which assumptions its
   assertions make) - a fact about step-handler source text and its
   authoring discipline, not a pure module's input domain a `fast-check`
   generator sweeps. It is discharged structurally instead: the literal
   `backlog/active/BL-968-....yaml` path and the `.git` isFile/isDirectory
   assumption are both gone from the handler (this parcel's whole point),
   and BL-1462's own scenarios 01/03 exercise the replacement resolver and
   worktree-locator against the REAL master checkout and a REAL linked
   worktree respectively - proving the fix, once it lands everywhere, holds
   under real (not simulated) checkout states.
2. *"Wherever backlog bookkeeping moves a ticket (active, paused, done,
   done under a milestone) the handler's lookup still finds it..."* —
   **property test**,
   `extension/test/bl1462TicketYamlLookupInvariants.property.test.js` (see
   above). Non-vacuity proven live (staged break/restore, above).
