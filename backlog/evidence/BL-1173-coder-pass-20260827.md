# BL-1173 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Delivered

- `extension/src/tools/deprecate-check.ts` — pure freshness evaluator + CLI
  printing `{"decision":"allow"|"hold",...}` (fail-closed on missing inputs).
- `promote_and_route_next.sh` consults the CLI after pick / before `git mv`;
  on hold or malformed/CLI failure: refuse, leave paused, note specifier (00).
- Acceptance steps registered; BL-1017-style fail-closed CLI path covered.

## Hold signals

Supersede marker; claims superseded/retired/obsolete without done closure;
depends_on all done + RETIRED / retired-surface references; ≥2 spec-gap bounces.

## Verification

| check | result |
|---|---|
| `deprecateCheck.test.js` | 7/7 |
| `run_acceptance.sh BL-1173…feature` | **5/5** |

By coder.
