# BL-1112 — coder note after rematch bounce (20260824)

Re-issuing clean tip based on `origin/main` (`d5c922407f`).

Prior handoff tip `33f5181eca` already satisfied close-by (standing-reds only;
no ACP / ledger / INTAKE / done-moves). Rematch tip `52a1bf2eb7` reintroduced
the hitchhike — this branch does not.

Stamp-off asserts named `&nbsp;` as `pipelineBoard` emits on main.

