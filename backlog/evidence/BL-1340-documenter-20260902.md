# BL-1340 — documenter pass (20260902)

Received: hardener commit `e7b64b98a5` (architect `de23548030`, cleaner
`e5dc8849ea`, forwarding coder's three-part fix per human ruling A),
unchanged.

## Review inventory

Checked every doc surface a promotion-gate behavior change touches:

- `docs/reference/Specification.MD` — carries a "Last Updated" changelog.
  BL-626's own entry described the pre-BL-1340 behavior
  ("drafts are never executable") with no mention of the new admission path.
  **D1**: added a new dated entry (today) summarizing the three-part fix,
  the origin deadlock, and pointers to acceptance/how-to/diagram — same
  commit as the content change, per this role's own "Last Updated" rule.
- `docs/how-to/BL-626-promotion-gate-rejects-unmaterialized-feature-draft.md`
  — existing how-to for the gate this ticket amends. **D2**: refusal shape 2
  stated "drafts are never executable" unconditionally, now false for a
  self-converting draft; updated to scope it to the parked case and
  cross-link the new doc. Committed separately with an untagged subject
  per the established cross-ticket-doc-edit convention (BL-1192's
  `task_scope_gate_lib.bb` flags a BL-1340-tagged commit touching a
  BL-626-named path as foreign scope).
- New user-facing behavior (self-converting draft admission, the
  `required_wiring:` pin convention, the QA-edge backstop, operator CLI
  usage) had no doc surface at all. **D3**: authored
  `docs/how-to/BL-1340-promotion-admits-a-self-converting-acceptance-draft.md`
  (Diátaxis how-to mode), linked from `docs/index.md` in the same commit.
- `docs/diagrams/swarm-flow.mmd` — registered diagram, change-trigger
  "when the pipeline topology or backlog flow changes"; the promotion edge's
  own comment block asserted "drafts are never executable", now stale.
  **D4**: added a BL-1340 comment block and corrected the BL-626 block's
  claim, in the same pass.
- `swarmforge/backlog-schema.md` — `required_wiring:` row described only its
  original (pre-QA gate) reader. BL-1340 gives the same field a second,
  promotion-time reader (`pins-draft-conversion?`) with a narrower matching
  rule (must name `specs/pipeline/steps`). **D5**: appended a note on the
  second consumer so the schema doc stays the authoritative description of
  what the field does everywhere it's read.
- `specs/features/BL-626-...feature` — already amended and re-stamped by
  coder/hardener (verified: example row 2 gone, scenarios 01-04 present,
  fresh mutation stamp). Not a documenter deliverable this pass; confirmed
  current, no action needed.
- Retirement (Article 3.6 / `docs/deprecated/`): not applicable — this
  ticket narrows a gate's refusal condition, it does not retire user-visible
  behavior wholesale (BL-626's gate still exists and still refuses parked
  drafts).
- README / top-level project docs: grepped for `feature.draft`,
  `is_buildable`, `promote_and_route_next` — no hits outside the files
  already covered above.

All five items (D1-D5) are documentation-only; no code, test, or feature
file was touched this pass. No item left BLOCKED.

## Verdict

Five doc-currency gaps found and closed (D1-D5). No production code, test,
or acceptance-contract change made. Forwarding to QA.

By documenter.
