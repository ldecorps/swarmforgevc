# BL-1527 coder evidence — 2026-09-13

## What changed

- `extension/test/helpers/noChoiceBlankRulingOracle.js` (new): the shared
  one-line oracle, `expectedNoChoiceRulingKind(ruling)` — `'ok'` when the
  trimmed string is empty, `'unknown-option'` otherwise. Invariant 1's
  single definition, used by both the property test's P3 branch and the
  BL-1527 step handler's scenario 02.
- `extension/test/bl1367ApprovalCarriesItsRuling.property.test.js`:
  - P3 branch now asks the shared helper instead of `if (!ruling)`.
  - Generator gained a constructed whitespace-only arm
    (`fc.constantFrom(' ', '\t', '  ')` + a `substitutesWhitespace` boolean),
    tracked under a distinct `no-options:ok-blank` reach key, added to the
    reach floor.
  - Prints `BL-1527 reach: [...]` before the floor assertions so the
    acceptance run can read what was actually generated.
- `specs/pipeline/steps/bl1527BlankRulingOracleSteps.js` (new): drives the
  real property file for scenarios 01/04 (reading its own printed reach
  line and its own source's floor array), and the real shared oracle +
  compiled classifier/writer directly for scenarios 02/03.

`extension/src/`, `property_suite_standing_allowlist.tsv`, and BL-1367's own
feature/handler are untouched (confirmed via `git status --short`).

## Verification

- Repro before fix: `classifyApprovalRulingRequirement(undefined, ' ')` →
  `{"kind":"ok"}`; old oracle (`if (!ruling)`) expected `unknown-option`.
- Property file: 8 standalone runs green after the fix, each printing
  `no-options:ok-blank` in its reach line.
- Break-then-restore (oracle): made the helper answer `'unknown-option'` for
  any truthy string (undoing the trim) — 5 runs, fails deterministically
  every time; restored, verified byte-identical to the pre-break file.
- Full acceptance run
  (`specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1527-*.feature`):
  9/9 scenarios pass.
- Break-then-restore (acceptance, oracle): same helper break → 5 of 9
  scenarios fail (scenario 01, both whitespace rows' failure surfaces via
  the shared property run); restored, byte-identical, 9/9 green again.
- Break-then-restore (acceptance, reach floor): removed the constructing
  arm and the `no-options:ok-blank` floor entry → exactly scenario 9 ("the
  blank draw is constructed and its reach is asserted") fails, naming the
  missing outcome; all other 8 scenarios still pass; restored,
  byte-identical, 9/9 green again.
- Full extension unit suite (`npx vitest run`): 616 files / 10479 tests
  passed, no regressions.

## Register row

`backlog/standing-reds.tsv` line 39 (this ticket's row) is left in place —
per the ticket's own notes, QA removes it in the land that turns the file
green, not the coder.

By coder.
