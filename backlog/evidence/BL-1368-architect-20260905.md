# BL-1368 architect review — clean sweep, forwarding

## Reviewed
Fast-forward merged cleaner's `070d870d5d`. Same recurring merge-deletion
guard limitation as BL-1370/BL-1401 (both intake files' introducing commits
have no ticket id HEAD can attribute, so no commit message can satisfy the
guard); resurrected root duplicates removed again in a follow-up commit,
archived record kept. Read the ticket's two invariants, coder and cleaner
evidence, and the actual composer/call-site diff.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1368ApprovalCommitNamesDeciderSteps.js
cd extension && node out/tools/dependency-gate.js ../extension/src/util/commitIntegrityRunner.ts ../extension/src/bridge/bridgeServer.ts ../extension/src/tools/telegramFrontDeskBotCore.ts ../extension/src/tools/telegram-front-desk-bot.ts
```
No forbidden edges, both runs.

## Logical coupling (BL-255)
`co-change-report.js` against the five changed files: only frequency-1
hits, below the default threshold (3). Nothing flagged.

## Invariants — verified directly in the code
1. **A human-decision commit never carries a pipeline role byline.**
   `commitIntegrityRunner.ts:49` — `HUMAN_DECISION_BYLINE = 'By the human,
   recorded by the front desk.'` — names the decider, no pipeline role.
   Grepped all four human-decision call sites directly: `bridgeServer.ts:953`
   (pager Approve), `telegramFrontDeskBotCore.ts:1323` (bot
   Approve/Reject/Amend), `telegram-front-desk-bot.ts:2376` (Expedite) all
   call `humanDecisionCommitMessage(...)` — no stray `By coder.` literal
   remains on any of them.
2. **Pipeline role commits are untouched.** Grepped the same files: three
   other `bridgeServer.ts` sites (epic reorder :1199, epic make-top-priority
   :1385, topic make-top-priority :1498) still literally read `By coder.` —
   correctly left alone, since those are role commits, not human-decision
   commits.

## Independent re-verification
- Compiled (`npm run compile`), then:
  `bl1368ApprovalCommitByline.test.js` **7/7**,
  `telegramFrontDeskBotCore.test.js` **449/449**,
  `commitIntegrityRunner.test.js` **10/10** — all pass (the three stderr
  lines during the run are expected fixture output for negative cases, not
  failures).
- Property test (`bl1368ApprovalCommitByline.property.test.js`): **3/3
  pass**, including the property asserting a role's own commit still
  passes the byline check while a human-decision commit correctly does not.
- Acceptance (`BL-1368-...feature`): **4/4 pass**.
- `check-commit-byline` (`compliance_battery_lib.bb:118`) is the only
  parser of a commit byline anywhere in `swarmforge/scripts/*.sh|*.bb`
  (grepped directly) — it takes an explicit `role` argument and is never
  invoked over an approval commit, and is not part of `run_commit_guards.sh`'s
  chain (grepped, 0 hits) — confirms qa_e2e step 5: no gate goes red.
- `jscpd` over the four changed TS files found 2 pre-existing clones (lines
  34-155 and 1284-1405 in one of the large files) — traced both to code far
  from any of this ticket's actual edit sites (the import line and one
  composer call per site); confirmed unrelated to this parcel's diff, not
  something it introduced.

## required_wiring
All three anchors confirmed live: `bridgeServer.ts`'s pager route and
`telegramFrontDeskBotCore.ts`'s bot path both compose from
`humanDecisionCommitMessage`, and the step handler matches BL-1371's
discovery-suffix convention.

## Scope check
Cleaner's finding (a third legitimate site, Expedite in
`telegram-front-desk-bot.ts`, correctly fixed though named only in passing
in the ticket's description) is confirmed correct on inspection — leaving
it on the old literal would have been exactly the "surviving half" the
ticket warns against, and it's a genuine human-decision commit
(`commitApprovalWrites`'s own comment already documented it as shared by
every automated `human_approval` writer).

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener.
