# BL-1516 — hardener review pass, 2026-09-21

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: hand-mutated prove_root case "/*" absolute git-common-dir comparison to always-true and ran the shell guard suite + BL-1516 property test
- **Commit hash**: 1428af956b
- **First error excerpt**: every existing bad-root fixture is either a non-worktree repo (relative .git answer) or fails the earlier string check first; a root under TMPROOT whose git-common-dir resolves to a linked worktrees outside repo (the exact shape of the ticket own item 3) was never exercised and the mutant wrongly accepts it
- **Failure class**: mutation-coverage-gap
- **Expected vs observed**: prove_root refuses any root whose git-common-dir resolves outside TMPROOT, including a linked worktree pointing at a live repo elsewhere, with a dedicated isolating test proving it
- **Blamed role**: hardener
- **Remediation pointer**: added section 08 to test_bl1378_expedite_close_guard.sh building a real git worktree add fixture; verified fails against the mutant (2 assertions), passes against real code

By hardener.

## Detail

Observation, not a defect: `bl1395-load-probe-no-root/` also exists as a
top-level directory in THIS worktree (`.worktrees/hardender`), same
pre-existing shape the architect noted in its own worktree. The ticket's
cleanup authority scopes its deletion explicitly to "the coder worktree"
only, so this instance is left untouched, not authorized by this
ticket's scope to delete elsewhere. Inert and gitignored; this parcel's
fix (item 2, the absolute probe-placeholder path) prevents recurrence.
