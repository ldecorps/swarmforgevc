# BL-1327 — documenter review pass, 2026-09-03

## Scope checked
- Ticket claim vs. handed-off commit `5031ede8e5` (hardender, merged into
  `swarmforge-documenter` at `ff72a1b057`).
- Every mechanism the ticket's `description` and its three `invariants` name:
  proposal-only (no apply path), asymmetric hysteresis (climb=1 signal,
  descent=full streak), effort-before-model ordering, cheaper-model-starts-
  at-high-effort, guard-trip-discards-progress, and the BL-1056 price-window
  re-walk — all present in `descent_ladder_lib.bb`/`descent_review_cli.bb`
  and reflected accurately in the new how-to.
- `required_wiring`: `install_swarmforge_crons.sh` wires
  `install_descent_review_cron.sh` in the same parcel that builds the
  review (grepped, present).
- Acceptance: `specs/features/BL-1327-scheduled-descent-ladder-proposes-cheaper-notch.feature`
  runs green, 4/4
  (`node specs/pipeline/cli.js specs/features/BL-1327-scheduled-descent-ladder-proposes-cheaper-notch.feature`).
- `docs/index.md` linkage for the new how-to (Divio rule: new authored docs
  linked from the index in the same commit).
- `docs/reference/Specification.MD` freshness — added an entry following the
  established convention for swarm-internal machinery/tooling additions
  (matching the BL-1332/BL-1338/BL-1343/BL-1306 entries).
- Diagram currency: no diagram depicts per-seat cost/effort-tuning state
  (`architecture.mmd`, `swarm-flow.mmd`, `handoff-flow.mmd`,
  `front-desk-flow.mmd` reviewed against their change-triggers) — this
  ticket adds a new cron-scheduled review over existing seat/guard state, no
  pipeline-topology or component-boundary change, so no diagram update is
  triggered.
- Cross-link check: the how-to points to BL-1317 (reused notch state
  machine), BL-1056 (price window), and BL-1320 (how a proposal, once a
  human decides to act on it, is actually applied via a pack conf edit)
  rather than restating any of the three.

## Findings

NONE.

- D1 (checked, no defect): the "Not this slice" exclusions (BL-548's
  calibration loop, auto-apply) are correctly reflected as out of scope in
  both the how-to and the Specification.MD entry — nothing in the shipped
  code or docs claims either.
- D2 (checked, no defect): `proposals.json`'s own `applied: false` and note
  field match the how-to's description exactly; no drift between what the
  CLI prints and what the page says it prints.
- D3 (action taken, not a defect): no how-to or Specification.MD entry
  existed for this ticket yet — both written in `documenter` commit
  `0eebb5fdff`, and the how-to linked from `docs/index.md` in the same
  commit.

## Conclusion

No documentation defect found. Forwarding to QA.
