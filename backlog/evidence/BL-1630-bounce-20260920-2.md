# BL-1630 — QA bounce, 2026-09-20 (2)

## D1

1. **Failing command**: `cd extension && npx vitest run --config vitest.properties.config.mjs test/bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js` (fails only under full `npm run test:properties` lane contention, not alone).
2. **Commit hash**: 4b7615bc14 (forwarded tip, dequeued by QA a second time per the specifier's re-forward routing).
3. **First error excerpt**:
   ```
   assert.deepEqual(
     violations,
     [],
     'expected the broken (listedDir-check-removed) guard to silently pass the directory-listing offender, proving the real check is load-bearing'
   );
   AssertionError: violations deepEqual []
   Actual: [{ file: 'zzzNonVacuityOffenderSteps.js', reason: 'incremental require cost 214.0ms exceeds the 200ms budget (confirmed alone)' }]
   ```
4. **Failure class**: behavior (the test's own fixture, per the specifier's amendment note on this ticket dated 2026-09-20 04:2x Z).
5. **Expected vs observed**: the non-vacuity test's offender fixture is meant to exercise ONLY the `listedDir` violation reason (via `require('node:os').tmpdir()` readdirSync), with the `listedDir`-check removed from a scratch copy of the guard so the offender should slip through silently (`violations === []`). On this host `os.tmpdir()` holds ~114k entries (82ms idle, 214ms under this lane's fork contention), so the listing itself trips the SEPARATE `budgetMs` check before the `listedDir` check is even reached — the assertion fails on a reason ("incremental require cost ... exceeds the 200ms budget") the test never meant to exercise, not on the thing being tested. The specifier's own ruling on this ticket (notes, 2026-09-20 04:2x Z) already prescribes the fix: the offender should list its OWN fixture directory (a handful of entries) instead of the real OS temp root, and the assertion should check that no violation carries the "lists a directory at module load" reason specifically — never that the violations array is empty outright.

**Blamed role**: coder (class behavior, the test's own fixture — per the specifier's ruling on this ticket: "QA on arrival bounces it to the coder for (1) (class behavior, the test's own fixture) unless the coder has already folded the fix").

**Remediation pointer**: `extension/test/bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`, the non-vacuity test (~line 106-129) and its `listsDir` fixture behavior (~line 35, `buildFixtureSource`): make the offender's `listsDir` behavior list the fixture's own directory rather than `os.tmpdir()`, and narrow the assertion to check that no violation names the "lists a directory at module load" reason.

Verified against `origin/main` before bouncing: this fix is NOT present in the forwarded tip 4b7615bc14 — confirmed the coder has not already folded it (per the specifier's exception clause).

No other defects found in this pass (acceptance `specs/features/BL-1630-no-step-handler-does-work-at-module-load.feature`: all scenarios pass; `pre_qa_gate.sh` not separately re-run since this is the same behavior class already identified).

By QA.
