# BL-1341 — documenter pass (20260902)

Received: hardener commit `de7e7edf01` (merged into `swarmforge-documenter`
at `74cf679704`; the merge itself deleted
`specs/features/BL-1341-merge-never-silently-drops-incoming-work.feature.draft`,
verified deliberate — its scenarios were folded into the existing BL-1242
feature file, same self-converting-draft pattern as BL-1340; confirmed by
grepping `BL-1341` markers inside
`specs/features/BL-1242-merge-never-silently-drops-branch-work.feature`
before naming the ticket in the merge commit message).

## Scope reviewed

- Ticket description, invariant, and every evidence file in the chain
  (coder, cleaner, architect, hardener) to confirm what changed:
  `check_merge_deletion.sh` (BL-1242) now diffs against BOTH `HEAD` and
  `MERGE_HEAD`, folds a both-sides drop into one finding naming both
  sides, and falls back ticket attribution to `MERGE_HEAD`'s history for
  an incoming-only path.
- Read the current script (`swarmforge/scripts/check_merge_deletion.sh`)
  directly rather than trusting the evidence chain's prose — confirmed
  the error message shape (`on this branch` / `on the incoming branch` /
  `on this branch and the incoming branch`) and the fallback order in
  `ticket_id_for_path` match what I documented.
- Existing documentation of this guard:
  `docs/how-to/BL-1242-merge-deletion-guard.md` (rewritten this pass —
  was one-directional, now documents both directions, the both-sides
  dedup, and the hardener's own diagnostic-loss fix) and
  `docs/reference/Specification.MD`'s BL-1242 entry (left as the
  historical record of the original one-directional guard; BL-1341 adds
  its own new top entry rather than rewriting history).
- `docs/index.md` — the existing BL-1242 how-to link line updated to
  mention the new direction; no new doc file created, so no new link
  needed.
- Diagram registry: none of the four registered diagrams
  (architecture/swarm-flow/handoff-flow/front-desk-flow) depict
  commit-msg git-hook guard internals — no change-trigger fired.

## Findings

NONE. Doc work: one `Specification.MD` top entry plus an `index.md` line
update (commit `0bb5c6192b`), and a rewrite of the BL-1242 how-to
(commit `edb35b24c5`, untagged subject per `task_scope_gate_lib.bb`'s
cross-ticket-filename rule — the how-to's own basename names BL-1242,
not BL-1341).

By documenter.
