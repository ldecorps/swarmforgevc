# BL-1454 — QA re-run, 2026-09-07 (second pass, after standing-red adjudication)

## Verdict: HOLD — not approved. Blocked by Article 4.2 (a newly-surfaced
unowned red), not by any defect in this ticket's own work.

## What changed since the first hold

The specifier's adjudication (`backlog/evidence/BL-1454-unowned-reds-adjudication-specifier-20260907.md`)
registered all three reds named in the first hold
(`backlog/evidence/BL-1454-QA-20260907.md`, on `expedite/BL-1454`, commit
`074910206e`) with open owners: bl1218/bl1320 -> BL-1457, bl1252 -> BL-1349.
Confirmed against the current `backlog/standing-reds.tsv` (all three rows
present, each names an open ticket in `backlog/paused/` or `backlog/active/`).

Merged `expedite/BL-1454` (074910206e) into this QA worktree branch
(clean merge, diffed against both parents, nothing dropped) and re-ran
BL-1454's own gates independently:

- `bb swarmforge/scripts/test/coordinator_activity_feed_lib_test_runner.bb`
  — ALL PASS.
- `bb -e '(load-file "swarmforge/scripts/handoffd.bb")'` — loads cleanly.
- `npx vitest run --config vitest.properties.config.mjs test/bl1454ActivityFeedSweepBudget.property.test.js`
  — 3/3 pass (this ticket's own three declared invariants).
- `run_acceptance.sh specs/features/BL-1454-the-activity-feed-sweep-fits-the-supervisor-budget.feature`
  — 4/4 pass.
- `run_acceptance.sh specs/features/GH-24-coordinator-activity-telegram-feed.feature`
  — 5/5 pass (no regression on GH-24's own scenarios from this ticket's
  Background change).
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass.
- Wiring: `coordinator-activity-feed-sweep!` still defined
  (`swarmforge/scripts/handoffd.bb:4235`) and called from the poll loop
  (line 5057).
- The three previously-red files, reconfirmed same signatures: `bl1218`
  (invariant 2) and `bl1320` still red only under `env -u
  PACK_STAFFING_SKIP_GATE` (green under this pane's normal exported env,
  6/6 with `bl1320`) — identical counterexample/message as the first hold.
  `bl1252` passed 5/5 this run (borderline, 14-19s per sub-case against a
  20s timeout — consistent with BL-1349's own characterization of this
  file as spawn-heavy and contention-sensitive, not a regression).

**BL-1454's own deliverables remain fully green.** No rework needed on this
ticket's own diff.

## The new blocking finding: an unowned red surfaced by the full unit suite

Per this role's mandatory gate ("Run the full unit test suite; it must be
green"), ran `npm test` (`npm run compile` + the full compiled suite,
`extension/`): **609/610 files, 10308/10309 tests pass; 1 file, 1 test
fails**:

`extension/test/bridgeServer.test.js` > `an idle /events connection
receives a periodic keepalive comment frame, with no snapshot re-sent`:
`AssertionError: a keepalive tick must never carry a second snapshot
frame` — the captured SSE buffer contains a `data: {...}` snapshot frame
immediately followed by `: keepalive`, violating the test's own asserted
invariant that a keepalive tick carries no second snapshot frame.

Grepped `backlog/` (including `backlog/standing-reds.tsv`,
`backlog/evidence/`, and every ticket state directory) for `bridgeServer`,
`keepalive`, and the assertion text — **no open ticket, no standing-red
row, and no evidence file adjudicates this failure.** Not part of this
ticket's diff: `bridgeServer.ts`/`bridgeServer.test.js` are untouched by
`git diff --name-only df95dd4de8 HEAD` on this branch; the file's last
substantive history is unrelated, long-closed tickets (BL-1367, BL-1380,
BL-1425, BL-1412).

**Confirmed genuinely flaky, not a fluke** (per this role's "deterministic
vs flaky only from evidence" rule): re-ran the single test 6 times total in
isolation (`vitest run test/bridgeServer.test.js -t "an idle /events
connection receives a periodic keepalive comment frame"`) — 3 pass, 3 fail,
no code changes between runs. The test's own comment documents why: it
deliberately drives the REAL server-side keepalive timer and poll timer
over a REAL HTTP connection with `pollIntervalMs: 20, keepaliveIntervalMs:
15` (BL-1350's coder evidence: "No real timer and no real sleep anywhere in
these tests" describes the OTHER BL-1350 tests, which use an injectable
fake clock — this one scenario is the deliberate exception, real timers by
design, to exercise the actual wiring end to end). Two real intervals 5ms
apart racing on the real event loop is exactly the shape that produces an
intermittent, genuine ordering violation, not a suite-contention artifact:
it reproduces at the same ~50% rate whether run alone or under the full
610-file suite.

Per the 2026-09-05 standing-red rule (`standing-red-register-amendment-2026-09-05.md`):
a red on `main` with no open ticket is `type: defect`, `severity: high` at
first sighting. Per Article 4.2: QA approves no parcel whose evidence names
a red with no open ticket in the standing-red register — this is exactly
that shape again, this time a NEW red rather than the three already
adjudicated.

## Disposition

Not approved. This is not a bounce (BL-1454 did not cause this red; its own
diff never touches the file). Sent as an unowned-red `note` (priority `00`)
to the specifier and the coordinator, per the same disposition path this
role prompt names for exactly this situation. BL-1454 WAITS again, pending
a ticket for the `bridgeServer.test.js` keepalive/snapshot race. Once that
red carries an open owner in `backlog/standing-reds.tsv`, QA re-runs: this
ticket's own gates above are already green and need no rework, so approval
should follow immediately barring a further regression in the meantime.

By QA.
