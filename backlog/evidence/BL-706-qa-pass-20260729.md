# BL-706 QA close-out — 2026-07-29

## Scope
Let's Talk minimize → compact floating bubble (draggable; record + pause +
expand; remember minimized + position after reload). float-01..04.

## Landed (initial)
- `letsTalkUiHtml.ts` — Minimize / Expand, `lets-talk-minimized` compact bubble,
  drag handle, localStorage for minimized + float position
- `letsTalkCore.ts` — `LETS_TALK_MINIMIZED_STORAGE_KEY`,
  `LETS_TALK_FLOAT_POS_STORAGE_KEY`
- How-to: `docs/how-to/BL-706-lets-talk-floating-minimized-chat.md` (+ BL-696 link)

## Bounce-back (human UX, same day)
Human rejected the first minimize chrome (tall floating control panel with
Ready / Record / Pause all / Expand). Expected a compact **chat-head** bubble
per the earlier poll vote.

### Reshape
- Round ~64px mic-face chat-head; page chrome hidden; transparent Mini App bg
- Mic / stop icons on the face; tiny pause + expand dots under the head
- Whole head draggable; position + minimized mode still persisted
- Redeployed via `bounce_bridge_headless.sh` — live `/lets-talk` serves
  `chat-head` + `width: 64px`

### Confirm
Cursor Remote poll: **Yes — ship it** (1 vote).

## Tests (2026-07-29 focused + bounce-back)
- `npx tsc -p .` (extension compile) — ok
- letsTalkCore.test.js — 23/23
- letsTalkBridge.test.js — 21/21 (asserts chat-head / 64px)
- Combined: 44/44 pass

## Out of this pass
Native OS / Android overlay PiP (see separate Android intake) — not this Mini App slice.
Telegram Mini App title chrome still frames the WebView; cannot float over
other Telegram topics from web alone.
