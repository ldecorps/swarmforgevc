# BL-1271 QA bounce — 2026-09-02

## Verification performed
- `bb swarmforge/scripts/test/dispatch_gap_test_runner.bb` — PASS (77 assertions, ALL PASS).
- `specs/pipeline/steps/index.js` registers `bl1271DispatchGapDefectOnlySteps` (line 408) — acceptance handler wired, not merely written.
- `top-expedited-paused-candidate-09 (BL-1271)` assertion present in the suite — retired-type guard lands in the stale suite, per required_wiring.
- `promotion_gates_lib.bb` `expedited-types` still `#{"defect"}` — invariant 1 (predicate stays defect-only) holds.

## D1 — invariant 2 violated by the cleaner's dedup consolidation

**Failing check**: grep for the ticket's own pre-existing assertion names in
`swarmforge/scripts/test/dispatch_gap_test_runner.bb` at the QA merge commit
`1056f9e666e28725a15c3c4f27a1e153da8715e5`.

**Commit hash tested**: `1056f9e666e28725a15c3c4f27a1e153da8715e5` (QA merge of documenter `fc7959ae96`).

**First error excerpt** (both greps return nothing):
```
$ grep -n '"top-expedited-paused-candidate-08 (BL-900): called with no epic-index (1-arity) still ranks by own priority, unchanged"' swarmforge/scripts/test/dispatch_gap_test_runner.bb
$ grep -n '"top-expedited-paused-candidate: priority breaks ties among multiple expedited candidates"' swarmforge/scripts/test/dispatch_gap_test_runner.bb
```
(no output — neither name exists in the file any more)

**Failure class**: `behavior` (an intent/invariant mismatch, not a compile or test failure — the suite is green; the ticket's own declared invariant is what's broken).

**Expected vs observed**: The ticket's own `invariants[1]` states verbatim:
"The suite goes green by correcting stale fixture data, never by deleting,
renaming or weakening an assertion: every assertion name present before this
ticket is still present after it." Observed: the cleaner pass (`f9d4cba01d`,
"BL-1271: cleaner pass — consolidate the now-duplicate expedite assertions")
deleted the assertion named `"top-expedited-paused-candidate: priority breaks
ties among multiple expedited candidates"` outright and renamed
`"top-expedited-paused-candidate-08 (BL-900): called with no epic-index
(1-arity) still ranks by own priority, unchanged"` into a combined name
(`".../unchanged / priority breaks ties among multiple expedited
candidates"`). Both original name strings are gone; the invariant requires
both to survive verbatim. The two source assertions were byte-identical
duplicates that pre-dated this ticket, so the consolidation is not wrong on
the merits — the coverage is unchanged — but it does exactly what invariant 2
names as forbidden, and the ticket text gives no exception for pre-existing
duplication. This must go back to the specifier/cleaner: either the
consolidation is reverted (both original assert= forms restored, even though
they duplicate each other), or the ticket's invariant is explicitly amended
to permit pre-existing-duplicate consolidation before this can be approved
as satisfying its own stated contract.

**Remediation pointer**: `swarmforge/scripts/test/dispatch_gap_test_runner.bb`
lines ~547-560 (current); the removed/renamed assertion originated in
cleaner commit `f9d4cba01d`. Owning role: **cleaner** — the consolidation
was introduced at the cleaner stage and forwarded unchanged by architect,
hardener, and documenter without correcting it against the ticket's own
declared invariant.

## Inventory close
No other defects found. This is the only item (D1). Routing to `cleaner`
(earliest role whose own stage introduced the defect).
