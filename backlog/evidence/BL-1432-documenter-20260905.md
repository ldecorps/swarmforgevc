# BL-1432 documenter review pass — 2026-09-05

Reviewed the hardened commit (`864a63849f`) and the coder/cleaner/architect/
hardener trail for BL-1432 (bounded land walk + QA-branch re-point,
human ruling option 3, both mechanisms).

Checklist:
- `required_wiring` anchor present:
  `specs/pipeline/steps/bl1432LandWalkRangesOverTheParcelSteps.js::registerSteps`
  — confirmed by grep and the acceptance run (5/5) below.
- This is a documented mechanism: `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`
  is the living reference for `land_step_lib.bb`'s land-step mechanics,
  already carrying its own per-ticket sections for BL-1405 and BL-1431.
  Added a matching `## ... (BL-1432)` section describing both halves of
  the ruling: the bounded walk (`parcel-own-base`, live today via
  `land-plan`'s default `:base`, no caller change needed) and
  `post-land-repoint!` (built and unit/acceptance-tested, but — per the
  architect's and cleaner's own flagged gap, independently confirmed via
  `grep -rn post-land-repoint swarmforge/scripts/*.bb specs/pipeline/steps/*.js`
  returning only the definition and its own unit/acceptance test call
  sites — not yet wired to any production caller; that live-wiring
  question is explicitly routed to QA's own `qa_e2e_procedure` step 2,
  not silently glossed over as "done"). This doc has no "Last Updated"
  field (its convention is per-section BL cross-links, not a single
  freshness stamp), so none was added.
- Added a dated top entry to `docs/reference/Specification.MD`,
  cross-linking to the BL-1241 how-to for the full mechanism.
- No `docs/index.md` change needed — the BL-1241 how-to is already linked
  there; no new page was authored.
- No registered diagram trigger fires (`architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`) — this narrows a land-time
  git-history walk and adds an unwired branch-hygiene helper; it changes
  neither the extension host/webview boundary, the tmux substrate, the
  `.swarmforge/` state layout, the pipeline topology/backlog flow, the
  handoff file lifecycle, nor front-desk routing.
- No production code, test, or requirements change made by this pass —
  documentation only.

Result: NONE — no defect found; the land-step how-to gained a section for
this ticket's two mechanisms, one changelog entry added.
