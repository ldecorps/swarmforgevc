# BL-1263 — coder pass (redo after architect bounce), 2026-09-05

## The bounce

The architect bounced the first pass (`backlog/evidence/BL-1263-bounce-20260905.md`):
the three test fixes were correct, but `backlog/standing-reds.tsv`'s own
two BL-1263-owned rows (`telegramClient.test.js`,
`telegramCursorOperatorExec.test.js`) were never removed, even though both
files this parcel fixes are exactly what those rows describe. The rule
(the register's own header: "A row is removed in the same land that turns
the test green") is the same one I applied all session for the property
allowlist (BL-1229, BL-1290, BL-1430) — missed here for the unit-lane
register specifically. The bounce commit (`5420e3bd5e`) reverted the whole
parcel out of the architect's branch; merged into this worktree per the
handback protocol ("the inbound tree is the structure").

## What's different this time

Identical to the original pass (`backendSwitch.test.js` line fix,
`telegramClient.test.js` whole-body deepEqual key,
`telegramCursorOperatorExec.test.js` fixture directory), PLUS:

- **`backlog/standing-reds.tsv`**: both BL-1263 rows removed (the
  `pricingTable.test.js`/BL-1212 row, unrelated, is untouched).

Root-cause excerpts, verification table, qa_e2e_procedure walkthrough, and
the non-vacuity proof for scenario 02's whole-body-deepEqual check are
unchanged from the original pass — see
`backlog/evidence/BL-1263-coder-20260905.md` (git history; the file no
longer exists in the working tree after the bounce revert deleted it, but
its content is unchanged and this pass re-confirms every number in it:
12/12, 87/87, 24/24, full unit lane 4 failures down from 7, 5/5 acceptance
runs stable across two).

## Verification (re-run after the redo)

| check | result |
|---|---|
| `test/backendSwitch.test.js` alone | 12/12 pass |
| `test/telegramClient.test.js` alone | 87/87 pass |
| `test/telegramCursorOperatorExec.test.js` alone | 24/24 pass |
| `npm run compile` | clean |
| acceptance `BL-1263-stale-assertions-are-retired-to-the-behaviour-that-shipped.feature` | **5/5**, stable across two runs |
| Full unit lane | 603 passed / 606 files; 4 failing tests across 3 files — `pricingTable`→BL-1212, `constitutionDocCitations` (unowned, flagged in BL-1206's evidence), `operatorRuntimeBbFixtureClosure`(2 tests)→BL-1265; same 3 unrelated files as before the bounce, none of them BL-1263's |
| `backlog/standing-reds.tsv` | both BL-1263 rows gone; `pricingTable.test.js`/BL-1212 row untouched |
| `swarmforge/scripts/check_standing_red_register.sh` | exit 0 |
| `swarmforge/scripts/check_property_suite_drift.sh` | exit 0 |

## Constraints respected (unchanged from the original pass)

- No source behaviour changed - `git diff --name-only HEAD` (against this
  parcel's own parent, once committed) touches only the 3 test files, the
  register, and the step-handler file.
- Site 2 remains a full `assert.deepEqual` over the entire parsed body.
- No other red folded in.
