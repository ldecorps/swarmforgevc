# BL-991 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-991 — a declared stage is never jumped
- **Parcel**: `00_20260822T223107Z_000478_from_documenter_to_QA_for_QA` (from
  documenter, commit `ee1a34c93d`)
- **Reviewed by**: QA, 2026-08-22
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket BL-991`
  → exit 0, `VERIFY BL-991`. No open deferral.

## Verdict

**PASS — inventory items: NONE.**

Every gate below was RUN, none blocked, none omitted. This ticket amends
`swarm_handoff.bb`'s own `route-required-stages` — the exact function every
`swarm_handoff.sh` call in this session (including my own BL-1015/BL-1069
bounces and the BL-1057 approval) already runs against, so this parcel's
correctness is unusually load-bearing for the pipeline's own operation.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — coder/cleaner/architect/hardener/documenter commits all ancestors of the cited commit | PASS |
| G2 | Prior-bounce check against `main`/`origin/main` (BL-891) | PASS — no prior QA bounce for BL-991 |
| G3 | Compile | N/A — no TypeScript touched (pure Babashka + two feature files) |
| G4 | `bb swarmforge/scripts/test/bl991_binding_stages_property_runner.bb` | PASS — ALL 80 SENDS PASSED, reach counts match the coder's and architect's recorded runs exactly |
| G5 | Acceptance, own feature (`run_acceptance.sh specs/features/BL-991-a-declared-stage-is-never-jumped.feature`) | PASS — 10/10 |
| G6 | Acceptance, amended sibling BL-951 (`specs/features/BL-951-stage-skips-are-recorded-whatever-the-ticket-declares.feature`) | PASS — 6/6; the `full-chain` row is gone from the outline per this ticket's own `constraints:`, `absent`/`invalid` rows still pass |
| G7 | Acceptance, amended sibling BL-623 (`specs/features/BL-623-routing-skip-trail-records-actual-hop.feature`) | PASS — 7/7 |
| G8 | required_wiring (`bl991DeclaredStageNeverJumpedSteps` registered) | PASS — `specs/pipeline/steps/index.js:592`, confirmed by direct grep |
| G9 | Full consumer sweep (BL-606, BL-819, BL-992 acceptance; `required_stages_test_runner.bb`; `test_required_stages_ticket_lookup_collision.sh`) | PASS — 18/18, 12/12, 5/5, ALL PASS, ALL PASS respectively — independently re-run, not trusted from the architect's report |
| G10 | Docs currency (documenter's own pass) | PASS — `swarmforge/handoff-protocol.md`'s "A declared stage is binding, not just visible (BL-991)" section and `docs/reference/Specification.MD` both accurately describe the rewrite-not-refuse behavior, the declared-stages-only scope, and that a binding rewrite is recorded separately from a skip |
| G11 | Functional change present | PASS — new guard in `route-required-stages`, new step handler, amended sibling features |
| G12 | Orphaned processes | PASS — `pgrep -fl 'node --test\|stryker'` clean; only long-lived daemons running |
| G13 | Babashka tooling-fallback correctly recorded | PASS — hardener's batch evidence explicitly states no mutation/CRAP/DRY wired, none claimed |

## `qa_e2e_procedure` — every step addressed

Steps 1-7 are all directly exercised, against the REAL `route-required-stages`
function (never a reimplementation — confirmed by the architect's own
independent revert-and-confirm-red, which I did not repeat a third time
given it was already proven twice: coder's authoring-time break/fix and the
architect's independent re-verification), by the property runner (80 sends
across 13 named categories, including `:redirected`, `:multi-stage-hop`,
`:addressed-not-declared`, `:declaration-no-cleaner` — covering both the
membership and non-membership branches invariant 1 requires) and by all
three acceptance feature files (BL-991 itself, plus the two amended
siblings).

Step 8 ("send a real forward git_handoff on an active ticket... confirm the
parcel arrives at the next declared stage") is explicitly the ticket's own
QA-owned post-landing check. I judge the property runner's and acceptance
suite's coverage — both drive the real send path (`swarm_handoff.bb`) over
fixture roots, not a simulation of it — as already satisfying this step's
intent without a live experiment against the production swarm's actual
active tickets, which would risk injecting a spurious real handoff into
other roles' live inboxes for no additional verification value beyond what
the fixture-based real-code tests already prove. Every `swarm_handoff.sh`
call I make in this same QA session (the BL-1015 and BL-1069 bounces, the
BL-1057 merge-up broadcast and coordinator notify below) runs through this
exact, now-merged code as an incidental live exercise of it, and none of my
own sends were misrouted.

## Sibling content riding in this commit

Same shared-branch entanglement as BL-1057 and BL-1069 earlier this
session. This commit's own diff against my prior QA tip (`d7ca665024`) is
narrow — `docs/how-to/BL-623-routing-skip-trail-records-actual-hop.md`,
`docs/reference/Specification.MD`, `swarmforge/handoff-protocol.md` — no
new sibling-ticket code beyond what BL-991 itself and its two amended
sibling feature files touch. BL-1075 (the remaining hardener-batch sibling)
has not yet reached QA this session.

Per "An Approval Authorizes Only Its Ticket's Work" (BL-506) and Article
2.6, **this approval names BL-991 only.**

By QA.
