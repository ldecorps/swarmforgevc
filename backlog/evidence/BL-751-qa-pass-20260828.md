# BL-751 — QA pass — 20260828

**Received:** `merge_and_process documenter ba5e2cfb70` (handoff
`00_20260828T002114Z_000860_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `ba5e2cfb70ab33b4fb315cb62368077fa24cfc44`
**QA merge commit:** `d1daef3e4077776915e1f1819789b073db922129`

## Ancestry check (BL-336 guard)

- `git merge-base --is-ancestor a920c717d ba5e2cfb70` (coder) — CONFIRMED
- `git merge-base --is-ancestor 20edfe53a ba5e2cfb70` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 3eaf6ebc7 ba5e2cfb70` (hardener) — CONFIRMED

## Branch-divergence and out-of-scope content found and declined (BL-506)

Same root cause as today's BL-644 (documenter's branch predates `f8a41c1e2`,
resolved by rename detection onto `backlog/hold/BL-751-*.yaml` cleanly — see
`backlog/evidence/BL-644-qa-pass-20260828.md`). **Additionally**, a full
`git merge --no-ff --no-commit` of documenter's tip pulled in two entirely
unrelated bodies of work simply by ordinary branch ancestry (not injected by
anyone maliciously — documenter's branch legitimately contains these commits
upstream of BL-751's own):

1. **BL-592's full coder-level implementation** (25 files touched if
   accepted: `extension/src/bridge/bridgeServer.ts`,
   `consoleMenuUiHtml.ts`, new `specTreeUiHtml.ts` (254 lines),
   `docsTree.ts`, `pwa/app.js`, a full acceptance feature (70 lines) +
   410-line step handler, tests). Traced to commit `e5cf2a3af` ("BL-592:
   live spec tree on Mini App console with epic tier"). **Not an ancestor
   of current `main`** and not an ancestor of `feabd9825` ("BL-592
   hardening: close 4 zero-coverage gaps", also not on `main`) — three
   divergent lineages for BL-592's real work, none landed. `fbfa44148`
   only restored BL-592's ticket YAML *metadata*, not this implementation.
2. **BL-1191's wake-dedup work** (`swarmforge/scripts/handoff_inject_lib.bb`
   + `swarmforge/scripts/test/test_swarm_handoff_sync_deliver.sh`).

Per BL-506 ("A parcel carries ONE ticket's task name; QA approval authorizes
ONLY that work"), declined both via selective `git checkout HEAD --
<path>` (modified files) / unstage-and-remove (new files) after the
`--no-ff --no-commit` merge, verified each declined path carried no BL-751
reference first. **Self-correction during review:** initially
mis-classified `swarmforge/roles/hardender.prompt` as unrelated infra and
declined it too; caught against the hardener's own evidence file (which
explicitly describes "Mirrored change to swarmforge/roles/hardender.prompt"
as part of BL-751's own deliverable) before committing, re-added it, and
re-verified compile/tests included it. Final kept set: 8 files, all
BL-751's own (`git diff --cached --stat` recorded in the merge commit
message).

**BL-592's apparent absence from `main` flagged separately** to
specifier+coordinator (priority 00, same note as BL-644's hold/ flag) — real,
substantial, hardened-in-part work that risks being lost if the branches
carrying it (documenter's, and whatever the `feabd9825` lineage is) ever get
reset. Not landed here; not this ticket's call.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-751` | `VERIFY BL-751` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean |
| Targeted unit test | `node --test test/telegramCursorBridgePilot.test.js` | 21/21 PASS (first run against stale pre-recompile `out/` failed on the new BL-751 assertion — recompiled, reran, green; not a real defect) |
| Full unit suite | `npm test` (extension/) | 46 files / 202 tests fail, same standing BL-720/BL-728 host-env debt as BL-644's pass minutes earlier (identical failure count). None touch `telegramCursorBridgePilot*`. 3 files also flagged over the per-file duration budget (`recordBounceCli`, `renderBriefingBurndownCli`, `renderBriefingDiagramsCli` test files) — unrelated to BL-751, consistent with host load from back-to-back full-suite runs this session, not a regression. |
| Property suite | `npm run test:properties` (extension/) | 26 files / 17 tests fail; all 26 match the 27-entry standing allowlist verbatim (same file set as BL-644's run, `bl593MutationRunTelemetry` passed again). 2 known-benign `[vitest-worker]` unhandled-error artifacts (BL-871). No new/unallowlisted failures. |
| Doc content | manual read, `docs/how-to/BL-751-pilot-new-dispatch-branch-diffed-against-siblings.md` | Accurately describes the rule, the BL-646/BL-750 motivating example, and both surfaces it lives on (hardener.prompt + composePilotExpeditorPrompt) |
| Acceptance | N/A — deliberately parked, same `verification-only-chore-carries-no-executable-gate` pattern as BL-644 | `specs/features/BL-751-*.feature.draft`, zero-scenario, outside the `*.feature` glob |
| Wiring / invariants | ticket YAML | Neither `required_wiring:` nor `invariants:` declared |

## Live sighting: property-suite-full-run-hijacks-role-branch-refs (BL-1202/1200/1124)

Between this ticket's two full-suite gate runs, `swarmforge-coder`'s branch
tip read `87a88a027 seed` for a window (reflog: burst of `init`/`seed`
commits 01:41:53–01:42:31, self-recovered to a real commit
`9145000af Merge main into coder to pick up BL-1186 promotion`, file count
9881, by 01:45:41) — the exact corruption signature already tracked as
BL-1202/BL-1200 (see
[[property-suite-full-run-hijacks-role-branch-refs]]). Window overlaps this
session's own `npm run test:properties` invocation (started 01:42:57) and/or
an automatic drift-check reacting to this session's own commits landing on
`main` touching `extension/src/*` — did not chase the exact trigger further
(BL-1202/1200 own that root-cause work, both `human_approval: pending`, per
existing memory: re-reporting the symptom achieves nothing). Branch was
healthy at both of QA's own checks (before and after); no QA action taken on
another role's branch. Recorded here as fresh, timestamped confirming
evidence only. **Given this is a second live sighting within one QA session,
no further full, unscoped property-suite runs planned unless a future gate
strictly requires it.**

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline: acquired
`.swarmforge/land-main.publish.lock`, `land_main_publish.sh --decide-only`,
`git push origin HEAD:main`, released the lock (see push log below).

BL-751 carries no `GH-` id — no `issue_done.sh` step required.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping with the same hold/
pool flag as BL-644 (both need human/specifier adjudication on final
disposition), plus the separate BL-592-missing-from-main finding above.

By QA.
