# BL-941 QA bounce — 20260827

**Reviewed tip:** documenter `76620fb6cd` merged at `adfaf4adc` (not landed)
**Task:** `BL-941-bl915-owed-mutation-and-crap-gates`
**Sibling:** `VERIFY BL-941`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Merge ancestry `76620fb6cd` | PASS |
| `npm run compile` (extension) | PASS |
| APS BL-941 feature | **5/5 PASS** |
| APS BL-915 sibling feature | **10/10 PASS** |
| `mutation_cooldown_gate.bb` (QA time) | **run** (file_age 20692.53d, load quiet) |
| Hardener Stryker deliverable (qa_e2e §2) | **FAIL → D1** |
| CRAP evidence (qa_e2e §3) | PASS (1.00 / 4.00 in hardener evidence) |
| `index.js` wiring `bl941…Steps` | PASS (line 587) |

## Inventory

### D1 — `behavior` (blame: hardender)

**Failing command:**
```
rg 'Stryker|mutation score|survivor' backlog/evidence/BL-941-hardener-pass-20260827.md
bb swarmforge/scripts/mutation_cooldown_gate.bb <repo> extension/src/tools/telegramCursorBridgeCore.ts
# DECISION: run  (at QA time — gate no longer skip-cooldown)
```

**Commit hash checked:** `adfaf4adc` (documenter `76620fb6cd` merged)

**First error excerpt:**
```
| Stryker (`mutation_cooldown_gate.bb`) | **skip-cooldown** (file_age 0.03d < 3d window)
  — per BL-149, no override; surgical sweep + property tests cover classifier boundaries |
(no mutation score, no survivor list, no Stryker report)
```

**Failure class:** `behavior`

**Expected vs observed:** Ticket `qa_e2e_procedure` §1–2 requires
`mutation_cooldown_gate.bb` to answer `run` at QA time **and** hardener
evidence to record a real Stryker run (mutation score + survivor list, zero
survivors on `isCursorAgentGone` / `shouldResetCursorAgentSession`, scoped to
compiled `out/`). Cooldown now clears (`DECISION: run`), but hardener evidence
still documents only `skip-cooldown` plus surgical/gherkin sweeps — the owed
BL-915 Stryker gate was not executed or recorded.

**Remediation:** Hardender rematch: run Stryker via
`extension/stryker.bl941.config.json` (or equivalent scoped config), record
mutation score and survivor list in `backlog/evidence/BL-941-hardener-pass-*.md`,
confirm zero survivors on both classifier functions, then forward documenter→QA.
Keep acceptance/property/surgical artifacts; do not waive Stryker because
surgical sweep passed.

## Verdict

BOUNCE → hardender. Do not land.

By QA.
