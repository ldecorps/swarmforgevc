# BL-988 QA bounce 9 — 20260825

**Reviewed tip:** documenter `5dd5d54892` (not merged — tip impure)
**Task:** `BL-988-orphaned-wsl-acceptance-contract-has-no-step-handlers`
**Sibling:** `VERIFY BL-988`
**Prior:** bounce8 `93c60bf74`

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main 5dd5d54892  # fails
comm -12 … | wc -l  # → 12
```

**Commit hash checked:** `5dd5d54892`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = ac0477aaf BL-1100 QA pass)
paths=34 dels=12
deleted: BL-1100 evidence + qa-pass (rematch/hardener/architect/cleaner)
(hardener pass present on tip — purity only)
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto current `origin/main` keeping landed
BL-1100. Observed tip that deletes BL-1100 evidence.

**Remediation:** `git fetch && git reset --hard origin/main`; restore BL-988
only; keep all BL-1100 paths; dels=0; verify ancestry immediately before handoff.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
