# BL-1121 coder pass — 2026-08-25

## Tip
Standing recipe: when MERGE_HEAD (or lone GITHEAD_*) is present and every
suite-triggering staged path is byte-identical to the incoming parent,
`check_property_suite_drift.sh` prints `skip-reconcile-import` and exits 0
without running the suite.

`SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1` stays recovery-only (distinct marker).

## Verification
- `test_property_suite_drift_guard.sh` → ALL PASS (incl. 08–10)
- `bl1121_reconcile_import_property_runner.bb` → ALL PROPERTIES HOLD
- APS BL-1121 → 3/3
