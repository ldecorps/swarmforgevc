# BL-1415 — cleaner pass, 2026-09-05

NONE — no defect found. The widened stall clock and the new three-way
verdict are correct, complete, and consistent across the sweep, the CLI,
and the router.

## What was checked

- Read `handoff-event-ms-from-headers` directly: takes the max of all four
  timestamps (`enqueued_at`/`created_at`/`dequeued_at`/`completed_at`),
  parsed defensively (`nil` when none parse — never a false freshness
  signal). Confirmed both consumers (`handoff-event-ms`'s single-file read
  and `build-dropped-parcel-trail-index`'s single-pass loop) now call this
  ONE function rather than keeping two derivations in sync by hand — BL-978
  invariant 2 preserved by construction, matching the claim.
- Read `ticket-dispatch-verdict`/`ticket-dispatch-verdict-in` directly:
  builds on `decide-dropped-parcel?` UNCHANGED (confirmed by reading the
  unmodified function itself) — the `:dropped` reason is
  `dropped-parcel-note-message`, the identical text the sweep's own nudge
  produces, so all three surfaces agree without a duplicated string.
  Traced `ticket-dispatch-verdict-in`'s deliberate `status: nil` (the
  router/CLI path doesn't read a ticket's `status:` field) against
  `parked-ticket?`'s own fail-closed contract (`= status "blocked"`
  exactly, `nil` is NOT a park) — confirmed this only ever widens which
  tickets a targeted, single-ticket route/CLI call reports as dropped
  relative to the sweep's own multi-ticket suppression, never narrows;
  the coder's own reasoning about this is correct, not merely asserted.
- Read the CLI and router diffs directly: `dispatch_trail_cli.bb`'s
  three-way print and `route_backlog_to_coder.sh`'s new `DROPPED` branch
  (routing without `--force`, printing the CLI's own reason) match the
  ticket's own required_wiring and invariant 3 exactly.
- Read the coder's own two caught-and-fixed test-infrastructure bugs
  (fixture `cwd` not pinned to the fixture root, causing a real handoff
  write into this session's own live mailbox; fixture timestamps computed
  against a fixed historical instant instead of the real wall clock the
  subprocess reads) — both are genuine, well-diagnosed fixes matching
  established hazard classes in this codebase (BL-1390's cwd-scoping
  lesson), not hand-waved.
- `bb swarmforge/scripts/test/dropped_parcel_test_runner.bb`,
  `bl719_dropped_parcel_invariants_property_runner.bb`,
  `bl1097_router_dispatch_trail_test_runner.bb`: ALL PASS / ok, all three.
- `bash swarmforge/scripts/test/test_chase_sweep.sh`,
  `test_bl1097_router_refuses_dispatched_ticket.sh`: ALL PASS, both
  (including the 3 new scenarios 08-10).
- acceptance `BL-1415-...feature`: 6/6.
- acceptance `BL-1097`/`BL-1223`/`BL-1301` (dependencies): 4/4, 6/6, 8/8 —
  all unaffected.
- `grep -n DROPPED` on the router/CLI and `grep -n completed_at` on
  `chase_sweep_lib.bb`: all present (required_wiring).
- `npx jscpd` on the new step handler: 0 clones.
- No `backlog/standing-reds.tsv` or allowlist row is owned by this ticket.
- `npm run compile`: N/A, no TypeScript touched (all `.bb`/shell files).

## Verification (all re-run in this worktree, all green, matching every claimed number)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/dropped_parcel_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/bl719_dropped_parcel_invariants_property_runner.bb` | ok |
| `bb swarmforge/scripts/test/bl1097_router_dispatch_trail_test_runner.bb` | ALL PASS |
| `bash swarmforge/scripts/test/test_chase_sweep.sh` | ALL PASS |
| `bash swarmforge/scripts/test/test_bl1097_router_refuses_dispatched_ticket.sh` | ALL PASS (10/10, including 3 new) |
| acceptance `BL-1415-...feature` | 6/6 |
| acceptance `BL-1097`/`BL-1223`/`BL-1301` (regression) | 4/4, 6/6, 8/8 — all unaffected |
| `grep -n DROPPED` / `grep -n completed_at` (required_wiring) | present |
| `npx jscpd` on the new step handler | 0 clones |

No fix required. Forwarding unchanged to architect.
