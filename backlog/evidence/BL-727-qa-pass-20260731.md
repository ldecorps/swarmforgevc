# BL-727 — QA pass, 2026-07-31

## Commit reviewed

`0654188d24` (documenter, "Merge hardener 0c573f68c3 for
BL-727-bl718-pilot-missed-unwired-acceptance — Gherkin mutation manifest
refresh after D1 fix"), merged into QA at this evidence commit's parent.

Ancestry confirmed: architect's D1-fix merge (`b256c31bc`), the D1 fix itself
(`f06e69dd9`), and the architect bounce evidence (`68b329a58`) are all
ancestors of the reviewed commit — this is the ticket's own lineage, not a
sibling's.

## Verification performed

- `npm run compile` (extension/): clean.
- `npm test` (extension/): 392/393 files, 6872/6873 tests green. The one
  failure (`test/tmpDirMigrationGuard.test.js` flags a raw `mkdtempSync` in
  `test/pricingTable.test.js:92`) is pre-existing on `origin/main`
  (`9b3e1f74a`, BL-627, confirmed via `merge-base --is-ancestor`), untouched
  by this ticket's diff, and already ticketed as **BL-742** (paused,
  `bl627-test-raw-mkdtemp-breaks-migration-gate`) — not re-filed, not
  blocking this parcel.
- `npm run test:properties`: 31/31 files, 96/96 tests green, including this
  ticket's own `pilotAcceptanceGate.property.test.js` (4/4).
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-727-pilot-acceptance-contract-gate.feature`:
  7/7 scenarios green.
- No orphaned `node --test`/`stryker`/`vitest` processes before or after.

## Architect's D1 bounce — confirmed genuinely fixed

D1 (Background step grepped `telegramCursorBridgePilot.ts` source text
instead of calling `composePilotExpeditorPrompt` and asserting on its real
output — the exact prompt-text-assertion anti-pattern the ticket forbade).
Read the fixed step handler directly
(`specs/pipeline/steps/bl727PilotAcceptanceGateSteps.js`): it now
`require()`s the compiled `out/tools/telegramCursorBridgePilot`, calls
`composePilotExpeditorPrompt('BL-727-FIXTURE')` for real, and asserts on the
returned string. No source-file `readFileSync`/`includes()` remains in that
step. Confirmed behavioral, not textual.

## required_wiring confirmed

`telegramCursorBridgePilot.ts` composes a prompt instructing
`node extension/out/tools/pilot-acceptance-gate.js <TICKET-ID>` as the only
landing path, and explicitly says never `git mv` — read directly at the
source, not inferred from a passing test.

## Verdict

PASS. No new defects. Landing on `main`.
