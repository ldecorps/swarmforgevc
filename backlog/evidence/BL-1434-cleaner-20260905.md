# BL-1434 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's conversion is correct, complete, and
the collateral fix to `bl1175PropertySuiteStandingRedsInvariants` is
correctly scoped and reasoned.

## What was checked

- Read the converted `hostActivityFeed.property.test.js` directly: all
  forty trials and every original check (active status, bound respected,
  no invented line, suffix matches emitted, quiet after end) are real
  `assert` calls inside one `test(...)` body — a failure is a red vitest
  run, not a swallowed `process.exit`. Confirmed `vitest.properties.config.mjs`
  has `globals: true`, so the bare `test(...)` call matches the sibling
  property files' own convention without an explicit import.
- Ran the ordinary pass directly: 1/1 pass.
- Ran with `BL1434_INJECT_INVENTED_LINE=1` directly (not trusting the
  coder's claimed output): the test FAILS, naming `invented line
  INVENTED-...` — confirms the conversion is a real check, not a log line,
  reproducing the non-vacuity proof independently.
- Confirmed `grep -c hostActivityFeed backlog/standing-reds.tsv
  swarmforge/scripts/property_suite_standing_allowlist.tsv` is 0/0 at this
  commit (invariant 2) — the allowlist TSV is now down to its header row
  only.
- Confirmed `git diff --stat -- extension/src` for this commit is empty
  (invariant 3) — only the test file, the two registers, and the new step
  handler changed.
- Reviewed the collateral fix to
  `bl1175PropertySuiteStandingRedsInvariants.property.test.js` (a
  DIFFERENT file's own guard test, not `extension/src`, so invariant 3 is
  unaffected): invariant 1's non-empty floor is correctly dropped (per its
  own prior BL-1430 comment, the structural per-row checks were always the
  real invariant; zero rows is the legitimate best case, not a signal of
  breakage) and invariant 2's `mkIsolatedGuard()` correctly avoids
  borrowing a real filename from the now-possibly-empty live allowlist by
  running an isolated copy of the guard script + its sourced libs against
  a synthetic allowlist row in a temp directory — never touching the live
  project's own TSV (matches this repo's own "never mutate the live
  checkout" convention, applied to a config file here rather than a git
  operation). Ran this file directly: 3/3 pass.
- Ran the acceptance feature directly: 4/4 pass.
- `npx jscpd` on the touched/new files: one self-clone within
  `bl1175PropertySuiteStandingRedsInvariants.property.test.js`, confirmed
  PRE-EXISTING (present in the file before this commit, unrelated to the
  two areas this ticket's diff touched) — out of scope for this pass, left
  untouched.
- `npm run compile` clean.

## Full property suite (`npm run test:properties`) — one unrelated, non-reproducible flake

The full suite run showed 1 failed / 1080 passed: `bl1367ApprovalCarriesItsRuling.property.test.js`.
This file is NOT in this commit's diff, NOT related to the standing-red
register/allowlist/`hostActivityFeed` anything, and re-running it ALONE
passes cleanly (2/2) — the known class of fork-pool-concurrency load flake
this repo has hit before (a different unrelated red each full-suite run,
clean alone). Not a regression this ticket introduced and not blocking
this NONE pass; noted here rather than silently ignored.

## Verification (all re-run in this worktree)

| check | result |
|---|---|
| `npx vitest run --config vitest.properties.config.mjs test/hostActivityFeed.property.test.js` | 1/1 pass |
| same, with `BL1434_INJECT_INVENTED_LINE=1` | fails, naming the invented line (reproduced independently) |
| `npx vitest run --config vitest.properties.config.mjs test/bl1175PropertySuiteStandingRedsInvariants.property.test.js` | 3/3 pass |
| `node specs/pipeline/cli.js specs/features/BL-1434-...feature` | 4/4 |
| `grep -c hostActivityFeed` on both TSVs | 0/0 |
| `git diff --stat -- extension/src` | empty |
| `npm run compile` | clean |
| `npx jscpd` on touched/new files | 1 clone, confirmed pre-existing/out-of-scope |
| `npm run test:properties` (full suite) | 1080/1081 pass; the 1 failure (`bl1367...`) is unrelated to this diff and passes alone — known flake class, not a regression |

No fix required. Forwarding unchanged to architect.
