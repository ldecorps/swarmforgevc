# BL-684 QA bounce — 2026-07-27

## Failing command
```
specs/pipeline/scripts/run_acceptance.sh specs/features/BL-684-rename-onboarding-facilitator-to-onboarder.feature.draft
```
(Run against the draft because no live `.feature` file exists — see below.)

## Commit hash
`662bb35956a696c8a277bf045808eab263440f4b` (documenter tip, forwarded to QA for BL-684)

## First error excerpt
```
TAP version 13
# Subtest: no live surface still says facilitator
not ok 1 - no live surface still says facilitator
  ---
  duration_ms: 29.879306
  type: 'test'
  location: '.../specs/pipeline/generated/the-onboarding-facilitator-is-renamed-to-the-onboarder-without-breaking-a-live-agent.generated.test.js:10:1'
  failureType: 'testCodeFailure'
  error: 'Scenario "no live surface still says facilitator": no step handler matched "Given the rename from facilitator to onboarder has landed"'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: every caller of a renamed path still resolves [1]
not ok 2 - every caller of a renamed path still resolves [1]
  error: 'Scenario "every caller of a renamed path still resolves": no step handler matched "Given the rename from facilitator to onboarder has landed"'
  ...
1..21
# tests 21
# pass 0
# fail 21
```
Every one of the 21 generated scenarios fails identically: no step handler exists
for the Background step (or any scenario step) at all — because no step handler
file was ever written for this feature.

## Failure class
`acceptance`

## Expected vs observed
- **Expected**: `specs/features/BL-684-rename-onboarding-facilitator-to-onboarder.feature`
  exists as a live, generated, passing feature (BL-112's final acceptance gate for
  this ticket — ticket's own Scenario 08 requires "the acceptance suite still
  passes with step text and handlers renamed together"), backed by real step
  handlers under `specs/pipeline/steps/`.
- **Observed**: only the non-executable `specs/features/BL-684-....feature.draft`
  companion exists — it was never materialized into a live `.feature`, and no
  `specs/pipeline/steps/bl684*.js` (or equivalent) handler file was ever created.
  `run_acceptance.sh` against the draft fails all 21 scenarios with "no step
  handler matched" — 0/21 pass. This is the same class of gap tracked in memory
  as BL-441 (a ticket's `.feature.draft` companion is never promoted to a live
  `.feature` + wired handlers at any pipeline stage, so QA hits a hard
  acceptance-gate blocker at the very end instead of earlier).

## Independent verification already run and GREEN (not in question)
- `npm run compile` — clean.
- `npm test` — 365 files / 6236 tests, all passing.
- `npm run test:properties` — 23 files / 72 tests, all passing, including the
  three BL-684-specific property tests
  (`onboarderEvidenceByteIdentical`, `onboarderRenamedPathsResolve`,
  `onboarderLauncherPidGuard`).
- Ticket's own definition-of-done command
  (`git grep -lI -i facilitator -- . ':!backlog/evidence' ':!backlog/done'`)
  returns only expected residue: this ticket's/other tickets' own YAML+topic
  record content (boundary 2 exemption / genuinely out-of-scope paused
  siblings BL-590/624/625/643 per the ticket's own `notes:`), the new
  regression/property test files whose job is to assert the old word is
  gone, and the renamed launcher/stop-script dual-name compat-shim code the
  ticket's own description requires.
- So the rename itself (filenames, identifiers, runtime paths, dual-name
  compat handling) looks correct. The **sole** blocker is the missing
  acceptance materialization + step wiring described above.

## Remediation routing
Bounced to **coder** (BL-441 precedent): the coder cannot materialize a
`.feature` from a `.draft` unilaterally (that is specifier's call per
established precedent) — the expected recovery is coder sends a priority-00
`note` to the specifier asking it to materialize
`specs/features/BL-684-rename-onboarding-facilitator-to-onboarder.feature`
from the `.draft` (removing the `.draft`), commit to `main`, notify coder;
coder then merges `main` and writes the step handler file
(`specs/pipeline/steps/bl684OnboarderRenameSteps.js` or similar) wiring all
11 scenarios' steps against the already-built modules (`launch_onboarder.sh`,
`onboarder_supervisor.bb`, `stop_ancillary_services.sh`,
`onboarder-reconcile.ts`, `onboarderState.ts`/`onboarderStateStore.ts`), then
forwards down the full chain (cleaner → architect → hardener → documenter →
QA) so every gate re-runs on the completed acceptance wiring.
