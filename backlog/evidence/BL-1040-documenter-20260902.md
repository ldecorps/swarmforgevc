# BL-1040 — documenter pass, 2026-09-02

Reviewed commit `c5fd35b65c` (hardener clean sweep: one Babashka
test-coverage gap and one CRAP regression found and fixed), merged into
documenter. Real production behavior change (not a stamp-off): the Pipeline
Board and stage map now fold a seat-qualified key (`coder@sonnet2`) onto its
bare stage on the observation path, closing BL-983's invariant 3 the other
half of the way.

## Doc sweep

- Checked `docs/` for existing coverage of the board's held-role/not-started
  derivation: `docs/how-to/BL-670-pipeline-board-last-known-stage-and-health.md`
  is the primary doc for that mechanism, but describes only the
  `claimed`/`in-transit-to`/`last-known` status precedence (BL-1048's
  not-started fix) — it says nothing about seat identity, since that leak
  didn't exist as a described defect before this ticket.
- Authored a new how-to,
  `docs/how-to/BL-1040-seat-identity-never-escapes-on-the-observation-path.md`
  (commit `251b6991c3`), covering: the three leaking layers, the fix at each
  chokepoint (source fold + dedup, reader fold covering the stale-file case,
  renderer fold), what did not change (no column widening, BL-670's
  precedence untouched, BL-981's other surfaces and BL-732 out of scope),
  why it was latent at ship time, and the qa_e2e_procedure's six
  verification steps distilled for an operator.
- Linked it from `docs/index.md` (same commit) and cross-linked it from
  `BL-670-pipeline-board-last-known-stage-and-health.md`'s Related section
  (commit `dbf50326ce`, subject deliberately untagged — that file's own
  basename names BL-670, a different already-shipped ticket, so a
  BL-1040-tagged subject touching it would trip `task_scope_gate_lib.bb`'s
  foreign-scope check per the standing cross-ticket doc-edit rule).
- Added a dated entry to `docs/reference/Specification.MD`'s running log,
  bumping `Last Updated` to today in the same commit as the content change
  (commit `251b6991c3`), summarizing the defect, the fix, what stayed
  unchanged, and the latent/sequencing note, with a link to the new how-to.
- `docs/diagrams/`: checked `architecture.mmd` — it names `swarmState.ts`
  as a box but does not depict the internal seat-fold/precedence logic, and
  none of the four registered diagrams' change-triggers (architecture
  boundary, pipeline topology, handoff state machine, front-desk routing)
  fired — the `.swarmforge/board/ticket-stage-map.json` schema shape is
  unchanged (still `{ticket -> stage-qualified-entry}`; only the key's seat
  suffix now folds away). No diagram update owed.
- No RETIRED/deprecated behaviour involved — Article 3.6 deprecator gate not
  implicated.
- Confirmed production code and tests are untouched by this pass (doc-only
  diff): `git diff c5fd35b65c..HEAD --stat` shows only `docs/` and
  `backlog/evidence/` paths.

## Verdict

NONE — no documentation defect found beyond the missing coverage, which
this pass supplies. Forwarding to QA.
