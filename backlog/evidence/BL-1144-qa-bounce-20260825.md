# BL-1144 QA bounce — 20260825

**Reviewed tip:** documenter `21cb524715`
**Task:** `BL-1144-frequent-qa-push-races-on-main-land`
**Sibling:** `VERIFY BL-1144`

## Verdict

**Bounce → documenter** (tip purity). Product gates green; rematch onto current `origin/main` only.

## Inventory

| Gate | Result |
|------|--------|
| Tip purity (`origin/main` ancestor of tip) | **FAIL → D1** |
| `dels_on_origin` | PASS (0) |
| `land_main_publish_test_runner.sh` | ALL PASS (blocked on land only) |
| APS BL-1144 feature | 3/3 pass |
| `required_wiring` (`index.js` bl1144 steps) | PASS |
| Stage evidence (architect/cleaner/hardender/documenter) | present |

### D1 — `behavior` (blame: documenter)

**Failing command:**
`git merge-base --is-ancestor origin/main 21cb524715`

**Commit hash:** `21cb524715`

**First error excerpt:**
```
main_NOT_ancestor
git rev-list --left-right --count origin/main...21cb524715 → 2 2
origin/main..tip: (empty on origin side of check)
tip..origin/main:
  a3e508c04 test(BL-1145): QA pass inventory
  25ae6a3c1 Merge documenter a1f7b16c58 for BL-1145 QA review
merge-base: a1f7b16c5 (documenter BL-1145 tip)
dels_on_origin=0
```

**Failure class:** `behavior`

**Expected vs observed:** `origin/main` must be an ancestor of the handoff tip; observed 2 commits on `origin/main` not in tip because BL-1145 landed during this parcel's queue wait.

**Remediation (documenter):**
1. `git fetch && git reset --hard origin/main`
2. Restore **BL-1144 product paths only** (land_main_publish.sh, master_main_reconcile_lib.bb, APS, docs, ticket evidence).
3. Keep all landed BL-1145 paths; do not resurrect closed tickets.
4. Re-verify `git merge-base --is-ancestor origin/main HEAD` and `dels_on_origin=0` **immediately before** `swarm_handoff.sh` in the same session.

By QA.
