# BL-1637 - Work note 009529 completion blocked by the BL-1645 gap, 20260919

Work note `10_20260918T150112Z_009529_from_coordinator` ("Work BL-1637:
merge main first, then read backlog/active") could not be completed by
either path once its `dequeued_at` was (re-)stamped after the real work:

- Plain completion: `WORK_NOT_EVIDENCED` (no commit/handoff since the
  dequeue stamp — the real work predates it).
- `--no-work` with an honest reason: `WORK_ACTIVE_ON_MAIN` (BL-1614's
  clause refuses any stated reason while the ticket is active on main,
  which BL-1637 legitimately is, being in flight downstream).

Full root-cause writeup:
`backlog/evidence/BL-1614-work-note-active-on-main-blocks-already-completed-work-20260919.md`
(coder branch `22c7b4bba6`). Escalated to the specifier via note
`00_20260919T021141Z_002049` (priority 00). The specifier minted
**BL-1645** ("a completion gate's evidence window opens at the inbound's
creation, not its dequeue") and sent an interim: commit this evidence
with a subject leading `BL-1637:` (a real commit on the coder branch,
after the dequeue stamp, naming the ticket the note dispatched), then
complete the note plainly. This commit is that interim — not fabricated
work, but a true record of this exact dispatch's blocked state.

BL-1637 itself is unaffected: implemented, tested, committed
(`d59f181d38`), and forwarded to the cleaner
(`50_20260919T020229Z_002048_from_coder_to_cleaner`, delivered
02:02:29Z, now in the cleaner's in_process batch). No resend was made.

By coder.
