# BL-1581 coder evidence — 2026-09-15

## What changed

- `extension/test/bl1358MutantTimeCeilingInvariants.property.test.js`: the
  hang's position is reached BY CONSTRUCTION — an outer loop over three
  cells `['first', 'middle', 'last']`, one `fc.assert` per cell at
  `numRuns: runsPerCell(3, 3)` (1 draw each, `test/helpers/reachFloors.js`),
  the same shape BL-1578/BL-1580 used. The `middle` cell draws its ordinary
  array at exactly length 2 (so position 1 is a genuine middle slot); `first`
  and `last` keep the original length-1-to-2 array. The position is derived
  from the cell (`0`, `1`, `ordinary.length`) rather than drawn via
  `fc.nat({max:2}) % (ordinary.length + 1)`. The per-draw body (`kinds`,
  `runMutant`, the P1/P2 assertions) is unchanged. A cell that stopped being
  exercised now fails the file naming that position via
  `assertReachFloor(counts, POSITION_CELLS, CELL_RUNS, 'position')`, kept
  beside the original `hangFollowed > 0` / `runs > 0` assertions (scenario
  03, byte-identical text). The run prints `BL-1581 reach map (bl1358):
  {"positions":3,"minDrawsPerPosition":1}`. The P1 control test in the same
  file is untouched. The draw budget of 3 and the 1000 ms ceiling are
  unchanged; only how the budget is spent changes.
- `specs/pipeline/steps/bl1581HangPositionConstructedSteps.js` (new): step
  handlers for the specifier-authored feature
  (`specs/features/BL-1581-*.feature`, not touched by this parcel).
  Scenarios 01/02 drive the real property file under the real properties
  config (`spawnSync` from `extension/`, memoized per scenario), reading its
  own printed reach map. Scenario 03 reads the real test source and confirms
  the two floor assertions are still present. The Background step's regex
  is anchored to the file's exact literal text (the BL-1580 shape) so it
  cannot shadow the "runs alone under the properties config" step.
  Discovered automatically by `specs/pipeline/steps/index.js` (BL-1371), no
  manual registration needed.

`extension/src` and `specs/pipeline/mutationWorker.js` are unchanged
(confirmed via `git diff main --name-only`).

## Verification

- Property file alone (`npx vitest run --config vitest.properties.config.mjs
  test/bl1358MutantTimeCeilingInvariants.property.test.js`): 2/2 pass,
  printing `BL-1581 reach map (bl1358): {"positions":3,"minDrawsPerPosition":1}`.
  A further 15 standalone runs (QA e2e procedure step 1): 15/15 green, 0
  FAIL, reach map printed every time.
- Full acceptance run (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1581-*.feature`): 5/5 scenarios pass (01 green run, 02
  both Examples rows — 3 positions, 1 draw of the rarest position — 03 both
  Examples rows).
- Break-then-restore:
  - Dropped `middle` from the iterated cells (loop only, the
    `POSITION_CELLS` list passed to `assertReachFloor` left unchanged) —
    first run failed `reach floor: position middle drawn 0 < 1`.
  - Dropped `first` and `middle` both, iterating `['last']` only — first
    run failed `reach floor: position first drawn 0 < 1` (the reach-floor
    check ran before `hangFollowed > 0`, matching the ticket's "whichever
    runs first").
  - Restored from a clean copy (`diff` against the pre-edit backup:
    identical), re-ran: green again.
- Full extension unit suite (`npx vitest run`): 619/620 files, 10512/10513
  tests passed. The one failure,
  `test/telegramCursorOperatorExec.test.js > BL-1204: executeOperatorVerb(/redeploy, "all")...`,
  threw `ENOTEMPTY: directory not empty` from the shared
  `sweepPendingTmpDirs()` afterEach hook (`test/helpers/tmpDir.js:76`) — a
  concurrent-worker fixture-sweep race, not this parcel's file. It passed
  24/24 standalone (`npx vitest run test/telegramCursorOperatorExec.test.js`)
  and this parcel's diff never touches it or `test/helpers/tmpDir.js`. Not
  added to `backlog/standing-reds.tsv`: it did not reproduce standalone, so
  it is not a deterministic red to register, only a noted full-suite flake.

## Register row

`backlog/standing-reds.tsv` line naming this ticket is left in place — per
BL-1580's precedent (and BL-1572's before it), QA removes it in the land
that turns the file green, not the coder.

By coder.
