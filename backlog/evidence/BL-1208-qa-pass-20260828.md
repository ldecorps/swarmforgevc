# BL-1208 — QA pass — 20260828

**Received:** `merge_and_process documenter baf844d501` (handoff
`00_20260828T034931Z_000888_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `baf844d501...`
**QA merge commit:** `6e17435b2c7b64b29795b01b7d90d93056143b85`

Single pass, no bounce. Fixes a safety-mechanism defect: `record-bounce.js`
was answering any commit whose content is live with a destructive `git
revert` remedy, based on liveness alone — it fired for real on `0bf05774a`,
a recovery commit that had just restored 13 silently-lost files; following
the remedy would have re-deleted every one of them.

## Merge

Clean, no conflicts (`git merge --no-ff baf844d501`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor e558d68c9 HEAD` (coder) — CONFIRMED
- `git merge-base --is-ancestor 0ffb7bd05 HEAD` (cleaner) — CONFIRMED
- `git merge-base --is-ancestor 860fff455 HEAD` (architect) — CONFIRMED
- `git merge-base --is-ancestor ce8549b55 HEAD` (hardener) — CONFIRMED
- `git merge-base --is-ancestor baf844d50 HEAD` (documenter, received tip) — CONFIRMED

## Design review (independent)

`decideBounceRevertVerdict` (`src/quality/bounceRevertVerdict.ts`): when
live files exist, `anyAuthored = liveFiles.some((f) =>
!f.restoredFromEarlierHistory)` gates the remedy only — `verdict` stays
`'violation'` and every live path is still returned in `liveFiles`
regardless (invariant 2: withholding the remedy never becomes a clean
verdict). `restoredFromEarlierHistory`
(`src/metrics/bounceRevertGitAdapter.ts`) is computed ONLY when the path
was absent at the commit's own parent (`parent === null` — an edit of an
existing path is never a restoration candidate) and a prior commit on
**this branch's own ancestry** (`${commit}^`, never a sibling ref) held
byte-identical content. This is exactly the invariant-1 discriminator the
ticket asks for, and exactly the guard invariant-2/scenario-03 needs
(coincidental content on a sibling branch cannot clear a bounce). No I/O
added to `src/quality/` — the new fact is gathered entirely in
`src/metrics/`, respecting the BL-259 no-io-from-policy gate.

## Verification inventory (independently re-run)

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1208` | `VERIFY BL-1208` — no open deferral |
| Compile | `npm run compile` | PASS, clean |
| Targeted unit | `npx vitest run test/bounceRevertRestoration.test.js test/bounceRevertCheck.test.js` | 24/24 PASS |
| Regression constraint (BL-954 test files unedited) | `git diff origin/main -- test/bounceRevertCheck.test.js test/bl954BounceRevertCheckInvariants.property.test.js` | Empty — confirmed byte-identical, not touched |
| BL-954 property invariants (direct, not via guard) | `npx vitest run --config vitest.properties.config.mjs bl954BounceRevertCheckInvariants` | 3/3 PASS, unedited |
| Acceptance | `run_acceptance.sh` on the BL-1208 feature | 4/4 PASS — scenario 01 (restoration withholds remedy), 02 (genuine bounce still gets it), 03 (sibling-branch coincidence guard), 04 (breach-report/published-history unchanged) |
| qa_e2e_procedure step 1 (read-only historical repro) | `node -e "...bounceRevertCheck({commit:'0bf05774a', by:'architect'})..."` — **no remedy executed** | Returns `verdict: 'breach-report', remedy: null` — matches architect's own re-run exactly; expected, not a gap, since `0bf05774a` has since landed on `main` and the pre-existing `ancestorOfMain` check now short-circuits before reaching BL-1208's new code path at all. The acceptance feature's synthetic fixtures are the operative proof of the fix, per architect's own note. |
| qa_e2e_procedure step 6 (13-file recovery still intact) | `git diff -M --diff-filter=D --name-only main swarmforge-architect` | **Empty** with rename detection (`-M`) — confirms zero real content loss. A non-rename-aware diff had shown 13/1 "deletions" against main/hardender respectively; traced every one: the main-side entries are three tickets (BL-1184, BL-428, BL-1190) that simply progressed `active/paused → done/` through normal lifecycle since 2026-08-27 (content verified present at their new path, e.g. `backlog/done/BL-1184-briefing-shift-velocity.yaml`); the hardener-side single entry is hardener's own BL-1208 evidence file, not yet merged onto architect's branch (normal pipeline ordering, architect precedes hardener). `extension/src/bridge/specTreeUiHtml.ts` (the file whose loss started the whole incident thread) confirmed present on `swarmforge-architect`. |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker'` | none, before and after |
| Docs | `docs/how-to/BL-1208-bounce-revert-remedy-requires-authorship-not-liveness.md` present, accurate; linked from `docs/index.md:121`; `Specification.MD` top entry accurate | Present and accurate |

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1208's own work.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1208 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1208 active → done).

By QA.
