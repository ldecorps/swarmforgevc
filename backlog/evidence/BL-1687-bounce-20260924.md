# BL-1687 — architect review pass, 2026-09-24 (send-back)

Reviewed commit: 96139653f84636f3ace7d1fb016cdb2636104581 (coder, "fix flaky
reachability floor in the loader-probe invariant property test"), merged via
cleaner's 6953d4464a (NONE) into architect.

## D1 — property two's reach floor stays a hoped-for one; the same class of
flake this very commit set out to fix

`extension/test/bl1687BridgeServerLoaderProbeInvariants.property.test.js`
has two properties. This commit's diff touches only the FIRST ("none of the
seventeen named handlers loads the bridge graph..."), converting it from a
random `fc.constantFrom` draw (numRuns: 80, no coverage guarantee) to a
deterministic pre-loop over all seventeen handlers plus the existing
`fc.property` draws on top — a real fix, correctly reasoned, citing BL-654.

The SECOND property ("...non-vacuity: the loader probe tracks whether
bridgeServer actually loads, independent of which require SYNTAX reaches
it") is untouched and still uses the pre-existing shape: two independent
`fc.constantFrom` picks (3 syntaxes x 2 placements = 6 cells) at
`numRuns: 30`, with coverage checked only AFTER the fact
(`assert.equal(seenSyntaxes.size, 3, ...)` /
`assert.equal(seenPlacements.size, 2, ...)`). This is the identical
reach-floor shape the commit's own message diagnoses as flaky for property
one — a hoped-for floor, not a guaranteed one (BL-654) — just over a smaller
6-cell space, so it fails less often but is not fixed.

This is not a hypothetical: a sibling coder session (branch off the same
tip, commits 9211477d30 / d13b5ab813, never forwarded — see its own commit
message) independently diagnosed the SAME property two as flaky
("~2.5% miss chance per run") and applied the by-construction remedy
(`fc.shuffledSubarray` full permutation, `assertReachFloor`/`runsPerCell`
helpers, matching BL-1062's established pattern elsewhere in this suite) to
BOTH properties. That fix was not forwarded, and the commit actually
forwarded here (96139653f8) independently re-applied a different, partial
fix that leaves property two exactly as flaky as it always was.

Remediation: apply the same by-construction reach-floor guarantee to
property two — either the sibling branch's `fc.shuffledSubarray` +
`assertReachFloor`/`runsPerCell` approach (already written and verified on
commit 9211477d30, reuse rather than re-derive), or the deterministic
pre-loop shape this commit already used for property one, adapted to the
6-cell syntax x placement population. Either remedy must guarantee
coverage of all 6 cells every run, not merely assert it after a random
draw.

By architect.
