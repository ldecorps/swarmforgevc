# BL-1400 architect review — clean sweep, forwarding

## Reviewed
Fast-forward merged cleaner's `c601884f8b`. Same recurring merge-deletion
guard limitation as BL-1370/BL-1401/BL-1368 (both intake files'
introducing commits carry no ticket id HEAD can attribute); resurrected
root duplicates removed again in a follow-up commit, archived record kept.
Read the ticket's two invariants, coder and cleaner evidence, and the full
diff of `check-feature-handler-registration.ts`.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1400NestedHandlerIsSeenSteps.js
cd extension && node out/tools/dependency-gate.js ../extension/src/tools/check-feature-handler-registration.ts
```
No forbidden edges, both runs.

## Logical coupling (BL-255)
`co-change-report.js` flags the file's own three test files at
"SUSPECTED COUPLING" (3 co-changes each) — expected, legitimate
source/test coupling, not a concern. Everything else is frequency-1/2,
below threshold.

## Invariants — verified directly in the diff
1. **A handler at any depth is in the tree, refused by name.** Read the
   diff line by line: the new `CheckIo.listTree()` seam
   (`readdirSync(..., { withFileTypes: true, recursive: true })`) replaces
   the flat `listDir(STEPS_DIR)` call feeding `stepFiles` — the existing,
   unchanged `isDiscovered` predicate now actually receives nested paths to
   classify, closing exactly the "dead code" gap the ticket describes (the
   subdirectory-rejection branch existed and was correct, just unreachable).
2. **`lib/` helpers are never false offenders.** `stepFiles` explicitly
   filters `!name.startsWith('lib/')` (a safe prefix check here — `name` is
   a path relative to `STEPS_DIR` from a controlled recursive walk, not an
   arbitrary string, so this isn't the substring-collision class BL-1370
   had to guard against). `libFiles` is now `listTree(LIB_DIR)` too
   (previously flat), so a helper nested inside `lib/` itself is still
   correctly classified as a lib file, never promoted into `stepFiles`.

## Independent re-verification
- Compiled (`npm run compile`), then:
  `checkFeatureHandlerRegistrationCli.test.js` **12/12**,
  `featureHandlerRegistrationCheck.test.js` **29/29** — both pass,
  confirming the recursive change didn't regress the suffix-based or
  missing-registry cases.
- Property test (`bl1400NestedHandlerIsSeen.property.test.js`): **2/2
  pass**.
- `test_check_feature_handler_registration.sh`: **11/11 PASS**, including
  scenario 08 ("a nested handler is refused by name, and a lib helper is
  not an offender" — both invariants in one case).
- Acceptance (`BL-1400-...feature`): **5/5 pass**.
- Ran the live guard myself with `--assume-main` (the exact flag the
  coder's evidence flags as necessary to avoid a vacuous green off-branch):
  `check_feature_handler_registration.sh $(pwd) --assume-main` → **exit 0,
  no output** — confirms qa_e2e item 4, no new offender on the real tree
  now that the walk is recursive.
- `jscpd` over the changed TS file: **0 clones**.

## required_wiring
Both anchors confirmed live: the `recursive: true` literal is present in
`readTree`'s `listTree` implementation, and the step handler matches
BL-1371's discovery-suffix convention.

## Scope check
No change to `featureHandlerRegistrationCheck.ts`, its predicate, or the
shell guard — confirmed by the diff itself touching only
`check-feature-handler-registration.ts`'s tree-building step, exactly as
the ticket's own direction anticipated ("the subdirectory branch was
correct and simply unreachable").

## Verdict
No architecture violation, no invariant violation, no correctness defect
spotted. Forwarding to hardener.
