# BL-1160 QA bounce — 20260826

**Commit checked:** `6bd2f1c5d` (Merge documenter `559daed838`)
**Task:** `BL-1160-live-screen-activity-dot-per-tile`
**Routing:** `coder`

## Gates FAIL

| Gate | Result |
|------|--------|
| Acceptance `BL-1160-live-screen-activity-dot-per-tile.feature` | **7/8 FAIL** (1/8 pass) |
| Unit `residentSpyUiHtml.test.js` (BL-1160) | **3/5 FAIL** (2/5 pass; BL-1153 pass) |
| Compile | PASS |
| Mutation sweep `bl1160_resident_spy_ui_html_mutation_sweep.sh` | 4/4 killed |
| Tip purity vs `origin/main` | **FAIL** — BL-588/653/660/1159 hitchhikers (cleaner merged on `BL-588+1159` tip `3a00ff6154`) |
| BL-1153 `index.js` registration | **FAIL** — `bl1153StickyWebFontSizeChoiceSteps` absent on HEAD |

## Defects

**D1 — acceptance (blame: coder):** Per-tile activity dots not visible in acceptance harness.

- **Failing command:** `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1160-live-screen-activity-dot-per-tile.feature`
- **First error:** `expected 8 visible tile dots, got 0` (scenario 01); palette scenarios `got hidden`; markup `expected 1 status indicator in coordinator, got 0`; fullscreen `#fs-dot` not visible
- **Expected:** N visible `.dot` elements inside grid tiles per scenario
- **Observed:** zero visible tile dots; aggregate/fs cues also missing in APS path

**Remediation:** `extension/src/bridge/residentSpyUiHtml.ts` — `updatePaneStatusDot` / `updateAllPaneStatusDots` / `setStatus` must render visible per-tile dots on grid refresh and honour `activitySignal`; ensure APS step handler loads compiled shell consistently.

**D2 — unit (blame: coder):** Three BL-1160 unit tests red after compile.

- **Failing command:** `cd extension && npx vitest run test/residentSpyUiHtml.test.js`
- **First error:** `BL-1160: grid renders one activity dot per tile` — `true !== false` (dot visibility); `tile dots honour activitySignal` — `'hidden' !== 'green'`; `unavailable pane dot hidden` — palette mismatch
- **Expected:** each `.pane-col` shows visible dot with correct ok/stale/err/hidden semantics
- **Observed:** dots hidden or wrong colour

**Remediation:** same file — per-pane dot visibility and `resolvePaneStatusKind` wiring into DOM updates.

**D3 — behavior (blame: cleaner):** Parcel merged on polluted base (`3a00ff6154` on BL-588+1159 stack). Land diff includes sibling tickets.

- **Failing command:** `git diff origin/main...HEAD --name-only | grep -E '588|653|660|1159|batchRecovery'`
- **Expected:** BL-1160-only tip from `origin/main`
- **Observed:** entangled multi-ticket land diff

**Remediation:** re-cut BL-1160 from `origin/main`; preserve BL-1153 index + tests; forward clean tree.

**D4 — behavior (blame: cleaner):** `bl1153StickyWebFontSizeChoiceSteps` dropped from `index.js` on polluted tip.

- **Failing command:** `git show HEAD:specs/pipeline/steps/index.js | grep bl1153` (empty)
- **Expected:** BL-1153 handler present alongside BL-1160
- **Observed:** absent

## Inventory

D1, D2 (coder); D3, D4 (cleaner). Earliest blamed role for routing: **coder** (D1 acceptance).

By QA.
