# BL-1705 — QA review pass, 2026-09-24

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Verified commit: 9084d618e5 ("Merge documenter 5df4f9cb77 into QA."), gathered by qa-gather.js.

- Unit, property and acceptance (the BL-1705 feature): all green. sibling-check VERIFY, pre_qa_gate
  OK, register join: nothing failing. QA signalled no real ollama process.
- qa_e2e step 2: orphan_janitor_lib_test_runner.bb (the manifest's janitor suite) passes.
  Regression on the shared sweep lib: the BL-849 darwin-janitor feature reads 7 pass / 1 fail
  (scenario 03 row 2). That red is the pre-existing one the hardener recorded
  (BL-1705-unowned-red-BL849-sweep-20260924.md), now owned by BL-1723 (standing-reds.tsv row
  on origin/main), so approval is not withheld.
- qa_e2e step 3: reapable-ollama-ghost? branches only on the live-window exemption, the
  serve/runner/run-client cmdline shape, whether the parent is a live ollama serve, orphan
  status, and age against the grace period. No branch keys on memory size or model name.
- qa_e2e step 4: scope is the two janitor libs, the acceptance runner, the property test,
  the feature and handler, the Specification.MD entry (BL-1705's hunk only; its other tags are
  landed tickets) and evidence. backlog/topics/BL-1697.json (an untagged topic record) is not
  part of the land.
- Observation for the specifier, not a defect against this ticket's spec: any llama-server
  whose parent isn't a live ollama serve is reaped at any age. A hand-started llama.cpp
  server outside tmux would therefore be killed. The ticket asks for exactly this rule.

Verdict: PASS.
