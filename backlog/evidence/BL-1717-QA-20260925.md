# BL-1717 — QA review pass, 2026-09-25

3 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb scratchpad/bl1717_edge.bb swarmforge/scripts/land_step_lib.bb (real own-paths, fixture repo, only :path-landed-fn injected - land-plan own seam)
- **Commit hash**: a3896888e9
- **First error excerpt**: A: U alone (BL-1389 control) -> {:paths ["BL-9003-own.txt"]} | B: landed L + U -> {:paths ["BL-9003-own.txt" "shared.md"]} | C: landed L + U, U in unlanded-siblings -> {:paths ["BL-9003-own.txt"]}. U: lines on shared.md not on origin/main (path-landed? false), not in ticket-level unlanded-siblings (the BL-1389 two-walks shape).
- **Failure class**: behavior
- **Expected vs observed**: B == A: FIRM says a path-landed owner does not count, so adding landed L must not un-exclude a path U alone would lose; observed B keeps shared.md and U lines ride with no report line
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/land_step_lib.bb own-paths BL-1717 disjunct (~line 1894): it drops path-landed owners, then tests the remainder with the ticket-level `every? unlanded-siblings` only. Apply BL-1389 per-path test to the remainder too (exclude whenever a not-yet-landed owner remains, i.e. (seq not-yet-landed)); add a land_step_lib_test_runner case with an injected :path-landed-fn for owners {L landed, U path-unlanded, U not in unlanded-siblings}

## D2

- **Failing command**: ls extension/test | grep -i 1717 ; git log --format=%B 952843dd03
- **Commit hash**: a3896888e9
- **First error excerpt**: (empty) - no *.property.test.js encodes BL-1717 declared invariant and no stated non-encodability reason in any BL-1717 commit or evidence file; architect NONE 2925f7bab9 passed it
- **Failure class**: invariant-unencoded
- **Expected vs observed**: a coder-authored non-vacuous property test for the invariant (a delivered path whose not-yet-on-origin/main lines are only unlanded siblings never rides, whichever landed tickets also touched it; an unlanded sibling rides only on a path the landing ticket changed, named as a passenger), per coder.prompt BL-654
- **Blamed role**: coder
- **Remediation pointer**: extension/test/bl1717*.property.test.js over CONSTRUCTED owner sets (landed/unlanded, path-landed/not, in/out of ticket-level unlanded-siblings, landing ticket owner or not) via own-paths with :path-landed-fn injected; reach floor asserted; shown red against the pre-fix disjunct and against the D1 shape (B must exclude)

## D3

- **Failing command**: git show 4fdc61595e -- docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md
- **Commit hash**: a3896888e9
- **First error excerpt**: own-paths excludes a delivered path only when every owner attributed to it is an unlanded sibling - but a path with THREE or more owners could have a landed one among them
- **Failure class**: behavior
- **Expected vs observed**: the doc states the mechanism accurately: two owners suffice (the incident itself was BL-1685 landed + BL-1693 unlanded on one path)
- **Blamed role**: documenter
- **Remediation pointer**: docs/how-to/BL-1241-...md BL-1717 section, first paragraph: drop THREE or more; once D1 is fixed, bullet 3 holds for any not-yet-landed co-owner, not only a ticket-level unlanded sibling - say so

By QA.

## Detail

Parcel: documenter f80990318f, merged into QA as a3896888e9 (origin/main
already an ancestor). Disposition: bounce to the coder, the earliest blamed
role (D1 behavior, D2 invariant-unencoded; D3 travels for the documenter's
pass). The same pass sends a spec-side note for D2 to the specifier and
coordinator, and a separate unowned-red note (below).

Gates (run or blocked, Article 4.4):
- qa-sibling-check status: VERIFY BL-1717.
- pre_qa_gate.sh BL-1717 a3896888e9 (required_wiring): OK.
- Standing-red register: 6 rows, all owned (BL-1724, BL-1725, BL-1727,
  BL-1728, BL-1729 x2).
- Unit (npm test, full log tmp/BL-1717-unit.log): Test Files 637 passed
  (637), Tests 10867 passed (10867). Exit 1 came from the per-file duration
  budget (load average 9.5-14, coder2's suites running at the same time):
    extension/test/bl1652HandoffdRespawnReadingsWiring.test.js: 15.3s exceeds the 7.0s per-file budget (confirmed alone: 8.6s, still over budget)   [owned BL-1724]
    extension/test/emitLifecycleSnapshotCli.test.js: 16.1s exceeds the 7.0s per-file budget (confirmed alone: 9.6s, still over budget)   [UNOWNED]
    extension/test/telegramFrontDeskBotCli.test.js: 19.2s exceeds the 7.0s per-file budget (confirmed alone: 8.1s, still over budget)   [UNOWNED: BL-1620 is done, no suite-poles.tsv row]
- Properties (npm run test:properties, ONE run, full log tmp/BL-1717-prop.log):
  Test Files 2 failed | 456 passed (458); Tests 4 failed | 1329 passed; 2
  errors, both `Error: [vitest-worker]: Timeout calling "onTaskUpdate"`
  (BL-871 allowlisted).
    extension/test/bl1703OllamaLaunchProbe.property.test.js [owned BL-1727]:
      AssertionError: expected pid 15234 to be stopped
    extension/test/bl1505DedupSuppressedChaseCountInvariants.property.test.js [UNOWNED], 3 tests:
      Error: Property failed after 1 tests
      { seed: -1760100479, path: "0:0", endOnFailure: true }
      Counterexample: [[false]]
      Caused by: AssertionError: expected chaseCount 1 after a mixed landed/suppressed sequence [false], got 0
      AssertionError: expected a legacy true return to still count as attempted/landed
      AssertionError: expected the broken (raw-truthy) gate to misclassify an {:attempted false} map as a chase (proving the fix must read :attempted, not just any truthy return)
    swarmforge/scripts/chase_sweep_lib.bb and the test file are byte-identical
    to origin/main, so the red is main's, not this parcel's. The last change
    to chase_sweep_lib.bb on origin/main is e9f91d6746 (2026-09-21, no
    ticket id, "an operator pause holds a role's own queued new/ items").
    That is a lead only, not a verified cause. Not re-run.
  The parcel touches no *.property.test.js.
- Acceptance (run_acceptance.sh, run one after another): BL-1717 2/2, BL-1389 5/5,
  BL-1375 7/7, BL-1481 4/4, BL-1678 4/4.
- bb: land_step_lib_test_runner.bb ALL PASS.
- qa_e2e step 3 (scope): BL-1717's own paths are land_step_lib.bb, the
  handler, specs/pipeline/steps/lib/bl1717LandedCoOwnerCli.bb (the handler's
  fixture), land_step_lib_test_runner.bb, the docs and evidence. The BL-1726
  paths on this branch belong to a sibling that was bounced for an omission
  (not reverted, per BL-1348), and those paths are not this ticket's.
- Wiring: land-plan -> own-paths (consumer pre-exists, BL-1235); the
  handler is pinned and present.
- Docs: Specification.MD entry accurate; how-to wrong per D3.
- Stragglers: none of mine. Live test processes belong to .worktrees/coder2
  and to the master checkout (pid 9457, an acceptance run).

Observation (not a D-item): the CLI prints `EXCLUDED_SIBLING_PATH <path>
<owner>` once per RAW owner, so an excluded P is also credited to the landed
L. Scenario 01 only asks for U. The coder's own test comment says the
landed owner is named on purpose. It is recorded here because the line
credits an exclusion to a ticket whose lines are already on main.

Unowned reds: no QA hold was opened, because the parcel is bounced (D1)
rather than waiting on the reds. The register is re-read when the rebuild
returns.

Bounce-step tool restore (QA.prompt, BL-1650/BL-1653 rule): this branch runs
the land step from its own checkout, so land_step_lib.bb and
land_step_lib_test_runner.bb are restored to origin/main in an untagged
commit after this evidence. When the rebuild merges back, take both files
from the PARCEL side and diff the merge against both parents: the restore
would otherwise silently revert the rebuild's non-overlapping BL-1717 hunks.

D1 probe (scratchpad/bl1717_edge.bb, run as
`bb bl1717_edge.bb swarmforge/scripts/land_step_lib.bb`):

```clojure
(require '[babashka.fs :as fs] '[babashka.process :as process] '[clojure.string :as str])
(def lib (first *command-line-args*))
(load-file lib)
(defn sh! [dir & args]
  (let [{:keys [exit out err]} (apply process/sh {:dir (str dir) :continue true} args)]
    {:exit exit :out (str/trim (or out "")) :err (str/trim (or err ""))}))
(defn commit! [root path content message]
  (fs/create-dirs (fs/parent (fs/path root path)))
  (spit (str (fs/path root path)) content)
  (sh! root "git" "add" "-A")
  (sh! root "git" "commit" "-q" "-m" message))
(defn fixture [owners-commits]
  (let [root (str (fs/create-temp-dir {:prefix "bl1717-qa-edge-"}))]
    (sh! root "git" "init" "-q" "-b" "main" ".")
    (sh! root "git" "config" "user.email" "t@t")
    (sh! root "git" "config" "user.name" "t")
    (sh! root "git" "config" "commit.gpgsign" "false")
    (sh! root "git" "commit" "-q" "--allow-empty" "-m" "seed")
    (sh! root "git" "update-ref" "refs/remotes/origin/main" (:out (sh! root "git" "rev-parse" "HEAD")))
    (doseq [[p c m] owners-commits] (commit! root p c m))
    (commit! root "BL-9003-own.txt" "own\n" "BL-9003: the landing ticket's own file")
    [root (:out (sh! root "git" "rev-parse" "HEAD"))]))
(defn run [label owners-commits landed & [unlanded]]
  (let [[root tip] (fixture owners-commits)]
    (try
      (let [r (land-step-lib/own-paths root tip "BL-9003" (or unlanded #{}) nil nil
                                       {:path-landed-fn (fn [sib _path] (contains? landed sib))})]
        (println label "->" (pr-str (select-keys r [:paths :warning]))))
      (finally (fs/delete-tree root)))))
;; U = BL-9002: its lines on shared.md are NOT on origin/main (path-landed false),
;; but it is not in the ticket-level unlanded-siblings set (#{}), the BL-1389 shape.
(run "A: U alone (BL-1389 control)"
     [["shared.md" "U line\n" "BL-9002: touch the shared path"]] #{})
(run "B: landed L + U"
     [["shared.md" "L line\n" "BL-9001: touch the shared path"]
      ["shared.md" "L line\nU line\n" "BL-9002: touch the shared path"]] #{"BL-9001"})
(run "C: landed L + U, U in unlanded-siblings (ticket scenario 01 shape)"
     [["shared.md" "L line\n" "BL-9001: touch the shared path"]
      ["shared.md" "L line\nU line\n" "BL-9002: touch the shared path"]] #{"BL-9001"} #{"BL-9002"})

```
