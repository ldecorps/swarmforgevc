# BL-1615 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Babashka/Clojure — no mutation/CRAP/DRY tool wired (BL-472); the
acceptance feature (with the architect's own D1 remediation scenario 04)
is the gate.

- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1615-*.feature`
  → 8/8 pass (the original 7 plus the rework's new scenario 04).
- Sibling regressions re-verified: BL-983 5/5, BL-1004 5/5, BL-1001 6/6,
  BL-1167 3/3 — all green.
- Hand-mutation (BL-567 fallback):
  - Reverted `origin-new-dir-for` to always return `stage-queue-dir`
    (the exact pre-rework D1 bug the architect found): caught
    immediately by the rework's own scenario 04 (7/8, the new coverage
    failing exactly where D1 predicted).
  - Removed `distinct` from `claim-queue-dirs`' directory list: survived
    (8/8 still green). **Equivalent mutant, not a gap** — demonstrable
    from the code: `claim-stage-handoff-files`' own `seen` atom already
    dedups by filename downstream (`if (contains? @seen name) false ...`
    else record and keep), so mapping `stage-handoff-files` over the same
    directory twice yields the identical final list either way. The
    directory-level `distinct` is redundant given the file-level dedup
    that already exists one layer down — invariant 1's "each file
    offered once" is enforced there regardless. Not chased further per
    BL-234's discipline.
- No `.ts`/`.js` under `extension/src`/`extension/test` touched;
  `npm test`/`npm run test:properties` not re-run per the lane-set
  directive (Babashka-only parcel, already-green session).
