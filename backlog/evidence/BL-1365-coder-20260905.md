# BL-1365 — coder pass, 2026-09-05

The closing-ceremony packet now names hand-made rituals. Written test-first:
`extension/test/ritualLedger.test.js` existed and was red before
`ritualLedger.ts` did.

## What was built

Three pieces, split along the line invariant 1 draws:

| piece | file | role |
|---|---|---|
| the fold | `extension/src/metrics/ritualLedger.ts` | pure: classify by bookkeeping path area, count volume, compute subject dominance, pick candidates |
| the cadence | `extension/src/metrics/ritualLedgerProducer.ts` + `extension/src/tools/run-ritual-ledger-producer.ts` + a `handoffd.bb` sweep | MEASUREMENT accrues on the daemon's own tick, into `.swarmforge/telemetry/ritual-ledger.json` |
| the read | `extension/src/metrics/closingCeremonyRun.ts` | the ceremony READS the store and applies selection + suppression at assembly |

**The detector is the ticket's own, unchanged**: dominance — the share of a
class's commits carrying its single most common *normalized* subject.
Normalization collapses what a generator varies (ticket ids, dates, hashes,
bare numbers), so a scripted ritual's 484 commits read as one subject and an
agent's read as a long tail. The class list is the four BOOKKEEPING areas
only, for the reason the live-repo section below records.

**Thresholds are two exported constants at the top of the file**, not buried in
a predicate, because the ticket says they will need tuning and that tuning
should be cheap: `RITUAL_VOLUME_FLOOR = 50`, `RITUAL_DOMINANCE_CEILING = 0.5`.
0.5 is a wide-margin choice against the ticket's own measured spread — the
scripted classes sit at 0.97 and 1.00, the hand-made one at 0.01, so nothing
real is anywhere near the line.

**Where the split actually falls, and why it is not arbitrary.** The ledger
stores raw per-class statistics only. Selection (thresholds) and suppression
(open tickets) happen at packet assembly, against the tickets open *now*. That
is what makes invariant 2 immediate — a ticket minted since the last sweep
silences its class on the very next packet, with no re-measurement — while
invariant 1 stays true, because nothing a ceremony does can alter what was
measured.

## Verification

| check | result |
|---|---|
| `test/ritualLedger.test.js` (new, test-first) | **27/27** |
| `test/ritualLedgerProducer.test.js` (new: parser, store, ceremony end-to-end) | **18/18** |
| `test/closingCeremony.test.js` (existing, owns the changed type) | **38/38** |
| property `bl1365RitualLedgerInvariants.property.test.js` | **4/4** |
| acceptance `BL-1365-...feature` | **5/5** |
| acceptance `BL-820-closing-ceremony-lean-pass.feature` (owns the packet) | **12/12** |
| acceptance `BL-923-dwell-counts-occupied-time...feature` | **5/5** |
| acceptance `BL-1119-closing-ceremony-role-quality-dial.feature` | **6/6** |
| `test_bl1395_bb_scripts_load.sh` (the daemon boots with the new sweep) | **ALL PASS** |

## Declared invariants (BL-654)

All three have a coder-authored property test in this parcel, each shown
non-vacuous against a deliberately broken implementation, each independent —
breaking one failed **only** its own property, with the other three still
green:

- **Invariant 1** (computed outside, merely read) — broke it by having the
  ceremony rewrite the ledger it had just read. `skipped ceremonies lose no
  measurement` failed; 2, 3 and the detector property passed. The property
  generates a random pattern of ran/skipped ceremonies and asserts a *later*
  ceremony still offers exactly what the store supports, and that no ceremony
  mutated the store. Floors asserted: at least one run that skipped a
  ceremony, at least one that reached a candidate, at least one below the
  volume floor.
- **Invariant 2** (a ticketed class is never offered again) — broke it by
  dropping the suppression term from the candidate filter. `a class an open
  ticket names is never offered` failed; the others passed. The suppressing
  text is **derived from the class under test** rather than drawn
  independently, so every generated pair is a suppression candidate by
  construction — drawing both sides freely would almost never collide and the
  property would pass while testing nothing. Floor asserted: at least one run
  reached a class that *would* have been offered without the ticket, so
  suppression demonstrably did work rather than riding on a below-floor class.
- **Invariant 3** (proposes, never mints) — broke it by writing a
  `backlog/active/AUTO-<class>.yaml` for each candidate. `a candidate is
  evidence, never an auto-minted ticket` failed; the others passed. It counts
  every file under `backlog/` before and after and asserts the ceremony
  recorded no outcome of its own when candidates exist.

A fourth, non-declared property pins the detector itself: a scripted class is
never offered and a hand-made class above the floor always is, with floors
asserting both shapes are generated.

Both broken files were restored and confirmed byte-identical by `diff`, and no
`BREAK` marker survives anywhere under `extension/src/`.

## required_wiring

Two anchors at mint, both confirmed to resolve to real code rather than a
comment:

- `closingCeremonyRun.ts::determinismCandidates` — the ASSEMBLY populates it
  (the ticket's notes are explicit that anchoring the declaring file would be
  satisfied by adding a field to an interface, which proves nothing).
- `bl1365CeremonyNamesHandMadeRitualsSteps.js::registerSteps` — the handler
  file IS the registration.

**A third was added this pass, and the addition is a judgement worth
reviewing.** `handoffd.bb::ritual-ledger-producer-sweep` is the independent
cadence invariant 1 rests on. Without it the producer exists and nothing runs
it, so the packet field would populate only from a ledger nobody writes —
exactly the shipped-inert shape this ticket was minted against (BL-1235,
BL-1361). Unlike BL-1364, this ticket's notes did not direct the coder to add
an anchor, so this is an addition rather than an instruction followed. Flagged
here rather than made quietly; a reviewer who disagrees should say so.

## Consumer sweep — exhaustive, not sampled

The packet gained a required field, so every consumer that enumerates its
shape had to be found. `grep -rn "'skipReasons'"` across `extension/`,
`specs/` and `swarmforge/` returns **two live hand-written copies of the
closed-key list**:

1. `extension/test/closingCeremony.test.js:166` — the unit assertion.
2. `specs/pipeline/steps/bl820ClosingCeremonyLeanPassSteps.js` — the "it
   contains no raw log transcript" acceptance step.

Both updated. This is the BL-973/BL-1279/BL-1398 shape — a production closure
mirrored by hand in more than one place — and only the first one failed on my
initial run, so the second would have shipped red had the sweep stopped at the
unit suite. A comment now says so at the second site. The third `skipReasons`
hit, `HypothesisParts`, is a deliberate `Pick` subset and correctly excludes
the new field: a hypothesis is derived from shift activity, not from a
candidate.

`isEmptyCeremonyPacket` also had to learn the field. A shift whose ONLY
finding is a determinism candidate is not an empty shift — without that line
the auto-`no_change` path would have swallowed the candidate and the packet
would have been computed, stored, and delivered to nobody. That is scenario
04's distinction from scenario 02, and there is a unit test for it.

## Pre-existing reds, run not assumed

The full unit suite is **26 failed / 10087 passed across 16 files**. None are
this parcel's:

- Fourteen of the sixteen fail with the same `TypeError:
  deps.checkOrphanedAuthoredDocs is not a function` in
  `pilotAcceptanceGate.ts` — a test-deps/production mismatch whose last
  touching commit is `e52261521e` (2026-08-27), long before this parcel.
- `tempDirTrapGuard` names violations in `check_bb_scripts_load.sh`,
  `local_coder_battery.sh` and three `*_property_runner.bb` files — none
  touched here.
- `liveRepoDerivationGuard` names `bl1243PaneActivitySignal.test.js` and
  `deprecateRetiredReferents.test.js` — neither touched here.

No violation list names a file this parcel created or modified.

**One consequence worth stating rather than leaving implied:**
`crossFileDuplicationCheck` is one of the fourteen dying on the
`checkOrphanedAuthoredDocs` TypeError, so it never reaches its duplication
assertion. Cross-file DRY is therefore **not** verified for the two modules
this parcel adds — the gate is out, not green. That is the hardener's dial to
turn, but it should not be read off a passing line that never ran.

## qa_e2e step 6 (live repo) — the step that found two real defects

Run against the live repo, 13786 non-merge commits over 45 days. Doing this
rather than reasoning about it changed the design twice.

**Defect 1: the class list was too broad, and the packet offered nine
candidates.** The first version classified every path area, including
`extension/src/`, `extension/test/`, `specs/features/`,
`specs/pipeline/steps/`, `swarmforge/scripts/` and `docs/`. Every one cleared
both thresholds, because writing source or a feature spec is of course
hand-made and varied. Nine candidates is the alert nobody reads — the failure
mode the ticket names first.

The distinction that matters turned out not to be "hand-made" but "hand-made
AND scriptable". A ritual is a repeated act with a fixed shape a script could
perform; authoring extension source is not one, however varied its subjects.
The list is now the four bookkeeping areas, which is also exactly what the
ticket's own measured table covers. The narrowing reproduces the ticket's
figures: topic records 0.945 against its measured 0.97, pass/bounce evidence
0.009 against 0.01.

**Defect 2: text-based suppression cannot work, and the "offers nothing"
result it produced was FALSE.** After narrowing, the live packet offered
nothing — which is what step 6 asks for, and I nearly recorded it as a pass.
It was not one. Checking which tickets did the suppressing showed prefix
matching firing on **23 of 104 open tickets** for `pass-bounce-evidence`
alone, nearly all merely mentioning `backlog/evidence/` in passing (BL-1275 is
about a property guard; BL-1383 is about a Telegram chat provider). A detector
suppressed that broadly never fires at all — the same silence as not having
built it.

Tightening to the title inverted the failure rather than fixing it. Measured
across the same 104 tickets:

| rule | promotion | closure | evidence | topics |
|---|---|---|---|---|
| prefix anywhere | 2 | 13 | **23** | 4 |
| prefix in `title:` | 0 | 2 | **0** | 0 |
| class id anywhere | 0 | 0 | 0 | 1 |

And the decisive check: the 2026-09-03 sweep's OWN findings, BL-1362
(evidence) and BL-1363 (closure), name neither path in their titles. So title
matching would fail the one case invariant 2 exists to handle, while prefix
matching suppresses everything. There is no prose rule between them.

**Resolution: the ticket declares.** Suppression now matches a
`ritual_class:` field carrying the class id (scalar or list). Exact, no false
positives, no false negatives, and it is the moment the knowledge exists — the
specifier minting a ticket from a candidate knows which class it addresses.
It fails toward FIRING: an undeclared ticket costs one judgement to dismiss,
which invariant 3 already prices in, whereas over-suppression costs the whole
mechanism silently. The 23-ticket case is pinned as a unit test so the loose
rule cannot come back.

**Honest live result under the corrected rule** (nothing declares the field
yet, so nothing is suppressed):

| class | commits | dominance | distinct subjects | offered |
|---|---|---|---|---|
| pass-bounce-evidence | 3986 | 0.009 | 3133 | yes |
| backlog-promotion | 2977 | 0.178 | 1402 | yes |
| topic-records | 2487 | **0.945** | 117 | no — scripted |
| backlog-closure | 851 | 0.455 | 232 | yes |

Three offers, not zero. **Step 6 as written cannot pass, and the reason is
that its premise expired**: it says to confirm the packet offers nothing with
BL-1360, BL-1361, BL-1362 and BL-1363 open, and all four are now in
`backlog/done/`. The two that correspond to a ritual class here are BL-1362
(evidence) and BL-1363 (closure), and their work is finished — so the honest
packet offers those classes until the 45-day window rolls past the hand-made
commits, at which point dominance rises and they drop out on their own.

The three offers are true positives, not noise, and `topic-records` being
excluded is the detector working. Recorded as a deviation from the step rather
than presented as a pass.

## Spec gap raised, not implemented around

Two things belong to the specifier, and went out as a priority-00 note rather
than being decided here:

1. `ritual_class:` is a new ticket field. Nothing declares it yet, so
   invariant 2 is inert until tickets minted from candidates carry it. The
   specifier owns whether the field is adopted and where it is documented.
2. qa_e2e step 6's premise is stale, as above.

## An observation on BL-1364, already forwarded

BL-1364's step handler (`bl1364MechanicalShareReadableSteps.js:56`) allocates
its fixture root with `mkTmpDir`. Under Vitest that root is swept by a
registered `afterEach`; the acceptance runner has no such hook, so under
`run_acceptance.sh` the root simply leaks. This handler uses
`mkProcessTmpDir` instead — removal registered on process exit, which needs no
hook and does not reach for the prefix-glob sweep that would delete a
concurrent run's fixtures (BL-1390).

Minor, and not this parcel's to fix: BL-1364 is with the cleaner. Recorded so
whoever holds it can decide, rather than discovered twice.
