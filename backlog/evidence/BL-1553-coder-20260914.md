# BL-1553 coder evidence — 2026-09-14

## What changed

- `extension/test/bl1364TurnProfileSeriesInvariants.property.test.js`:
  invariant 2's `fc.integer({ min: 0, max: 2 })` sampled damage-kind arm is
  replaced with an explicit loop over `DAMAGE_KINDS = ['interior', 'missing',
  'unreadablePath']`, one `fc.assert` per kind at `numRuns: runsPerCell(15,
  3)` (5 runs each, `test/helpers/reachFloors.js`) — the same shape BL-1533
  used for the delta sign. Each of the 5 draws per kind is guaranteed by
  construction to increment `seen.<kind>`, so the total draw count stays 15
  and every kind is exercised on every invocation rather than merely usually
  covered. The three `assert.ok(seen.<kind> >= 1, ...)` floor lines are kept
  exactly as they stood. Added a `console.log('BL-1553 invariant 2 reach
  over N draws: {...}')` line (BL-1371's precedent shape) before the floor
  assertions so the acceptance run reads the counts from the run itself.
  Invariant 1's own floors, the tolerated-face property, and invariant 3 are
  untouched.
- `specs/pipeline/steps/bl1553DamageKindReachSteps.js` (new): step handlers
  for the specifier-authored feature. Scenarios 01/02 drive the real
  property file under the real properties config (`spawnSync` from
  `extension/`, reading its own printed reach line). Scenario 03 reads the
  real test source and confirms the three per-kind `seen.<kind> >= 1`
  assertions are still present, never a restatement of what they assert.
  Discovered automatically by `specs/pipeline/steps/index.js` (BL-1371),
  no manual registration needed.
- The feature file itself (`specs/features/BL-1553-*.feature`) was already
  authored by the specifier at mint time; not touched by this parcel other
  than an accidental overwrite that was caught and reverted (`git checkout
  --`) before committing.

`extension/src/metrics/turnProfileProducer.ts` and `transcriptWalker.ts`,
`property_suite_standing_allowlist.tsv`, and the BL-1364 feature/handler are
untouched (confirmed via `git status --short`).

## Verification

- Property file alone (`npx vitest run --config vitest.properties.config.mjs
  test/bl1364TurnProfileSeriesInvariants.property.test.js`): 4/4 pass, 15
  standalone repeat runs all green (0 FAIL), each printing `BL-1553
  invariant 2 reach over 15 draws: {"interior":5,"missing":5,"unreadablePath":5}`.
- Break-then-restore: changed the unreadable-path arm to write a plain
  readable file instead of a directory — the property failed on the FIRST
  run (`record.complete` expected `false`, got `true`), not one in ten;
  restored from a backup copy, re-ran, green again (4/4, same reach map).
- Full acceptance run (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1553-*.feature`): 5/5 scenarios pass (01 green run, 02
  reach map + sum >= 15, 03 all three Examples rows).
- `swarmforge/scripts/check_feature_handler_registration.sh`: clean, exit 0.
- Full extension unit suite (`npx vitest run`): 615/616 files, 10477/10479
  tests passed. The 2 failures (`test/telegramCursorOperatorExec.test.js`,
  `ENOTEMPTY` on `sweepPendingTmpDirs`/`/tmp/bl1204-*`) are the documented
  full-suite-concurrency flake already recorded as session debris across
  BL-1263, BL-1444, BL-1498, and BL-1501's QA evidence — not this parcel's
  file, not a new defect. Isolated re-run: 24/24 pass.

## Register row

`backlog/standing-reds.tsv` line naming this ticket is left in place — per
the ticket's own notes, QA removes it in the land that turns the file
green, not the coder.

By coder.
