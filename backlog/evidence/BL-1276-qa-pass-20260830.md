# BL-1276 QA pass — completed BL-1241 rebuild, stale filename fixed (2026-08-30)

## Disposition: PASS

## BL-1241 entangled-tip rebuild

The documenter's handoff commit (`2d158b436d`) carried the coder's long-lived
branch ancestry entangled with dozens of unrelated sibling tickets' merges —
the same tangle shape BL-1062 (this session's prior parcel) was rebuilt
around minutes earlier.

Rebuilt tip-pure onto `origin/main`: cherry-picked BL-1276's own six
commits in order — coder `25e7922f0` (initial "acceptance contract"
scope), specifier amendment `d510e2602` (widened to "declared paths",
naming `retires:` too — cherry-picked **empty**, verified deliberately:
its `backlog-schema.md`/`handoff-protocol.md` doc additions were already
present on `origin/main` via a separate landing path, confirmed by grep
before skipping, not assumed), coder amendment `d6d65df53`, hardener
`e416ea6f4`, architect `907839f3d`, documenter `2d158b436d` — each verified
via `git show --stat` to touch only BL-1276's own files.

One merge conflict: `specs/pipeline/steps/index.js`, against BL-1062's own
rebuilt registration line landed moments earlier in this same session.
Resolved by keeping both `require` lines.

`git diff origin/main HEAD --stat` confirmed exactly BL-1276's own fifteen
files (including the ticket YAML's own `abandoned_commits` record).
`abandoned_commits` recorded on the ticket (131 hashes — the full
superseded chain from promotion through the documenter's tip).

## Found and fixed mid-verification: stale pre-amendment filename

The rebuilt tree carried the step handler under its PRE-AMENDMENT filename,
`bl1276AcceptanceContractNotForeignSteps.js`, even though the file's own
content and every later stage (hardener, architect, documenter) already
referred to the ticket's final widened scope ("declared paths", not
"acceptance contract"). Root cause: the original entangled chain only had
the correct final name (`bl1276DeclaredPathsNotForeignSteps.js`) because a
`Merge main for BL-1276's amendment` commit folded the rename in as a side
effect of merging unrelated branch state — none of the individual
BL-1276-attributed commits renamed the file themselves, so cherry-picking
only those commits reproduced the stale name.

Caught by comparing the rebuilt tree's file list (`git ls-tree`) against the
original documenter tip's file list before treating the rebuild as
complete — same discipline BL-1062's rebuild earlier this session required,
from the missing-file side rather than the stale-name side. Fixed via
`git mv` to the correct name plus updating the `index.js` registration;
repo-wide grep confirmed no other reference to the old filename remained.

## Verification performed (after the rename)

1. **Compile** — clean.
2. **required_wiring** — both anchors confirmed: `specs/pipeline/steps/index.js`
   registers the handler; `declared-exempt-paths` exists in
   `swarmforge/scripts/task_scope_gate_lib.bb`.
3. **Unit/property gates** — no `src/*.ts` component (this ticket is entirely
   Babashka/shell); gated by its own suites per the hardener's note:
   `swarmforge/scripts/test/task_scope_gate_lib_test_runner.bb` → ALL PASS;
   `swarmforge/scripts/test/task_scope_gate_acceptance_exemption_property_runner.bb`
   → ALL PASS.
4. **Regression on the file `swarm_handoff.bb` refactored into
   `landed_ticket_lib.bb`** — `test_swarm_handoff_sync_deliver.sh` and
   `test_swarm_handoff_daemon_backup.sh`: both ALL PASS.
5. **Acceptance, this ticket** —
   `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-1276-a-tickets-own-declared-paths-are-not-foreign.feature`:
   8/8 scenarios pass.
6. **Acceptance, regression per qa_e2e_procedure step 6** —
   `specs/pipeline/scripts/run_acceptance.sh
   specs/features/BL-1192-pre-handoff-task-scope-gate.feature`: 9/9
   scenarios pass, no regression on the sibling gate contract this ticket
   shares a file with.
7. **Live confirmation** (qa_e2e_procedure step 7, already independently
   corroborated) — the hardener's own commit records that this fix is what
   let its own earlier BL-1246 `git_handoff` send succeed without the
   task-scope gate refusing it for touching BL-1230's feature file.
8. **Process hygiene** — no orphaned `node --test`/`stryker` processes of
   mine before or after. A live `node --test` process observed belongs to a
   concurrent `.worktrees/coder` process tree — not touched.

By QA.
