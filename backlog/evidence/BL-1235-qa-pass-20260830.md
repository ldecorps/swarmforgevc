# BL-1235 QA pass — 2026-08-30

Commit verified: merge of documenter `0d56503392` into QA. BL-1235's own
coder(refix)/architect(re-pass)/hardener/documenter commits (eee0e843f6,
709a87865, 383a3c311, 6a2f0d2e9, 0d56503392) are all confirmed ancestors of
the merge HEAD. Merge conflict on `docs/reference/Specification.MD` and
`specs/pipeline/steps/index.js` (both also touched by BL-1224's just-landed
tip-pure rebuild) — resolved by keeping both sides; diffed the merge against
both parents afterward, nothing dropped from either side.

Bounce history checked against `main`: one prior architect bounce, two
items — D1 (the seat's decision logic was built with zero live callers:
every acceptance scenario drove `decideLocalSeatTurn` directly, so a
fully-tested decision wired to nothing still read green — the exact BL-419
shape) and D2 (a flaky property reach floor). Confirmed both fixed
independently, not just trusted from the bounce record.

## Dependency — confirmed landed
`depends_on: [BL-1082]` — present in `backlog/done/M8/`.

## required_wiring — both confirmed
- `extension/src/tools/telegramCursorBridgeCore.ts` exports
  `QWEN_LOCAL_SUBJECT_ID = 'QWEN_LOCAL'` alongside the existing
  `CURSOR_REMOTE`/`BUBBLE` subjects.
- `specs/pipeline/steps/index.js` registers `bl1235LocalQwenSeatSteps`.

## D1 verified fixed by reading the live wiring directly, not just trusting tests
`extension/src/tools/telegramCursorBridgeLive.ts:2004-2009`: the seat's
topic is checked and `runLocalSeatTurn` is called BEFORE any cursor-routing
decision is consulted — structurally, not by a filter applied afterward,
matching the ticket's own architecture requirement. `runLocalSeatTurnFn`
is a proper dependency-injection seam (`deps.runLocalSeatTurnFn ??
runLocalSeatTurn`), matching the CLI thin-wrapper convention.

## D2 verified fixed — re-ran the property test 3 times back to back
`bl1235LocalQwenSeatInvariants.property.test.js`: 6/6 clean on every one of
3 consecutive runs (no flake reproduced).

## Tests run
- `extension/test/bl1235LocalQwenSeat.test.js` +
  `bl1235LocalQwenSeatLive.test.js`: 35/35 (the decision logic and the live
  turn loop — endpoint reading, completion call, truncation boundaries the
  hardener's mutation sweep added).
- `extension/test/bl1235LocalQwenSeatInvariants.property.test.js`: 6/6,
  both invariants (cursor's surfaces never served; a refusal always names a
  reason and hands the turn to nobody).
- `specs/features/BL-1235-local-qwen-seat-behind-its-own-topic.feature`:
  5/5, matching all five qa_e2e_procedure items exactly.
- Full `npx vitest run`: 26 failed files / 218 failed tests — the same
  standing baseline I have independently measured across every ticket
  today (BL-1277/1280/1182/604/670). None of BL-1235's own test files
  appear in the failing set.

## Verdict: APPROVE

By QA.
