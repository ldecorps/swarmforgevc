# QA pass — BL-1430 — 2026-09-05

Commit under review: `ad7bd7a542` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`16fe14e27a` BL-1430
coder, confirmed ancestor via `git merge-base --is-ancestor`).

## Verification

- `npx vitest run --config vitest.properties.config.mjs
  test/bl874PortableTimeInvariants.property.test.js`: 6/6 pass (was the
  unowned standing red the ticket exists to fix).
- Checked the two other files the coder found had the same `.worktrees`
  walk-scope hole: `test/tempDirTrapGuard.property.test.js` (9/9 pass) and
  `test/bl1175PropertySuiteStandingRedsInvariants.property.test.js` (3/3
  pass) — both confirmed fixed in the same commit.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1430-the-portable-time-guard-has-one-definition.feature`
  — 2/2 scenarios pass.
- Full property suite regression (`npm run test:properties`): exit 0,
  clean — the `.worktrees` exclusion and the loosened BL-1175 sanity-floor
  change together introduce no regression elsewhere.
- Read the BL-1175 floor change directly (`inventory.length >= 20` →
  `> 0`): confirmed this is a stale magic-number correction, not a
  weakened assertion — the structural per-row checks (file pattern,
  disposition enum, rationale) immediately below are untouched and remain
  the real invariant; the floor only ever guarded against the inventory
  going silently empty, and a hardcoded count necessarily breaks on every
  legitimate reduction (this ticket alone drops it 20→18).
- Registry cleanup confirmed: both now-green rows removed from
  `backlog/standing-reds.tsv` and `property_suite_standing_allowlist.tsv`
  in the same commit, per BL-1428's own rule — this closes the exact
  follow-up debt I noted in my own BL-1289 evidence file earlier this
  session (the `tempDirTrapGuard.property.test.js` row going stale-green).
- `required_wiring`: `specs/pipeline/steps/bl1430PortableTimeGuardSingleDefinitionSteps.js`
  exports `registerSteps`.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — merging up and landing.
