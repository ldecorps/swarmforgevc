# BL-1203 — QA bounce — 20260828

## D1: documenter pass missing entirely

1. **Failing command:**
   ```
   find docs/how-to -iname "*1203*"
   grep -n "1203" docs/index.md
   grep -n "BL-1203" docs/reference/Specification.MD
   ```
2. **Commit hash:** `8ca4d03740c0f850889d51f2dd9394079f4ca0b1` (QA's own merge
   of documenter's received tip `79b7d18230`, this session's
   `merge_and_process` commit for this parcel).
3. **First error excerpt:** all three commands above returned **empty** —
   no `docs/how-to/BL-1203-*.md` file exists anywhere in the repo, no
   `docs/index.md` line mentions BL-1203, and `docs/reference/Specification.MD`
   has no BL-1203 entry (its top entries, in order, are BL-1205, BL-1213,
   BL-1198 — the three other tickets QA landed this session; BL-1203 is
   absent from the whole file).
4. **Failure class:** `behavior` (Article 4.3's own precedent for this
   exact shape — BL-575's missing documenter pass — is classed `behavior`,
   not `acceptance` or any other label; the class is a metric tag, ownership
   is what routes).
5. **Expected vs observed:** expected a how-to page, a `docs/index.md`
   link, and a `Specification.MD` changelog entry, matching every other
   ticket QA processed this session (BL-1198, BL-1213, BL-1205, each with
   its own `docs/how-to/BL-*.md` + index link + Specification.MD top entry)
   and matching Article 1.7's documenter deliverable ("Updates documentation
   (READMEs, comments, changelogs)"). Observed: zero documentation output
   of any kind for a `high`-severity, production-TypeScript-touching defect
   fix with two declared invariants and a five-step `qa_e2e_procedure`.

## Why this is a real gap, not a QA merge-resolution artifact

Confirmed the missing docs are not something QA's own selective-merge
declined by mistake: `git diff --name-status 1c3b3a0ae1 79b7d18230` (the
exact diff between hardener's tip and documenter's tip) shows exactly ONE
file changed — an unrelated BL-1205 evidence file that rode along via
shared branch history — and nothing else. Documenter's own received parcel
(`1c3b3a0ae1`, hardener's tip) already carried zero doc files for BL-1203
before documenter ever touched it, and documenter's tip carries zero after.
There is no documenter-authored commit anywhere in this ticket's full
history (`git log --oneline --all --grep="BL-1203"` shows coder ×2,
cleaner ×3, architect ×3, hardener ×1 — never a documenter-titled commit).
The `merge_and_process documenter 79b7d18230` handoff that reached QA is a
bare forward of hardener's own content, not a documenter deliverable.

## Everything else checked — genuinely clean, not just unexamined

Completing the full inventory before this one-item bounce (Article 4.4 —
never first-failure-stop):

| Gate | Result |
|---|---|
| Ancestry (coder → cleaner → architect → hardener) | CONFIRMED all four ancestors of received tip |
| Compile (`npm run compile`) | Clean |
| Targeted unit (`telegramFrontDeskBotCli`/`Core`) | 715/715 PASS |
| Targeted property (`telegramFrontDeskBotCli.property.test.js`) | 3/3 PASS, fixture-leak D1 fix confirmed holding (0 `/tmp/bl1203-property-*` stragglers before and after) |
| Acceptance (`run_acceptance.sh` on the BL-1203 feature) | 2/2 PASS |
| Invariant 1 (at most one note per inbound identity) | Non-vacuously property-tested, keyed on `updateId` history not a single scalar |
| Invariant 2 (pointer file always matches the announced answer) | Non-vacuously property-tested; fix makes `writeRoleAnswerFile` unconditional |
| Wiring (real caller path) | Confirmed end-to-end: real Telegram `update.update_id` threads through `processSteeringUpdate`/`deliverAskAnswer` → `captureRoleAnswer` → `enqueueRoleAnswerNote`, not merely unit-tested in isolation |
| Fixture-leak D1 bounce re-fix (architect's earlier bounce) | Confirmed present and correctly wrapped in `try`/`finally` at both property-test call sites |
| Hardener's own coverage-gap find (interleaved legacy call) | Confirmed present, hand-verified as a real (non-vacuous) mutant per hardener's own evidence |

**The underlying dedup fix itself is solid and well-verified** by unit,
property (with explicit mutation/non-vacuity proof), acceptance, and real
wiring — this bounce is narrowly about the missing documentation layer, not
a doubt about the fix's correctness.

## Routing

Per Article 4.3's own worked example, a stage's pass missing entirely
routes to THAT stage regardless of failure class: **documenter**. The fix
is mechanical — a how-to page (the pattern is already established by
`docs/how-to/BL-1205-tree-collapse-guard.md` and
`docs/how-to/BL-1213-parcel-rollback-guard.md` from this same session),
linked from `docs/index.md`, and a `Specification.MD` top-entry changelog
line — no new production logic needed, no known-good code is in question.

By QA.
