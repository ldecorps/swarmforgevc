# BL-1420 documenter review pass — 2026-09-05

Reviewed the hardened commit (`4dd344bbfd`) and the coder/cleaner/architect/
hardener trail for BL-1420 (three freshness-watchdog fixtures now pass
BL-784's registry guard, deriving their supervisor rows from the same
glob the guard walks).

Checklist:
- `required_wiring` anchors present, confirmed by grep:
  `bl1011FreshnessAlarmNamesSwarmAndReasonSteps.js` and
  `bl1012FreshnessSelfInflictedIncidentsSteps.js` both set
  `FRESHNESS_REQUIRED`; `bl1011_freshness_attribution_property_runner.bb`
  likewise; `bl1420FreshnessFixturesPassTheGuardSteps.js::registerSteps`
  present.
- This is the same fixture-rot class `docs/how-to/BL-973-bb-fixture-closure-guards-and-suite-inventory.md`
  already tracks (BL-1398/BL-1401's commit-guard section). Added a
  matching `## Related — the freshness-watchdog fixtures (BL-1399/BL-1420)`
  section: BL-1399's prior fix (one fixture, not previously documented in
  this how-to — added the missing cross-reference here as this ticket's
  own context, not a separate retroactive pass) plus BL-1420's three
  remaining call sites, the shared `freshnessFixture.js` helper, and the
  bb runner's independent same-glob Clojure derivation. This doc has no
  "Last Updated" field, so none was added.
- Added a dated top entry to `docs/reference/Specification.MD`,
  cross-linking to the BL-973 how-to.
- No `docs/index.md` change needed — the BL-973 how-to is already linked
  there; no new page was authored.
- No registered diagram trigger fires — this fixes test fixtures for an
  existing daemon-freshness checker; it changes neither the extension
  host/webview boundary, the tmux substrate, the `.swarmforge/` state
  layout, the pipeline topology/backlog flow, the handoff file lifecycle,
  nor front-desk routing.
- No production code, test, or requirements change made by this pass —
  documentation only.
- The BL-973 how-to file is named for a different (already-shipped)
  ticket, so its edit lands in its own UNTAGGED commit per the
  cross-ticket-doc-edit convention (BL-1192 task-scope gate) — the
  Specification.MD entry and this evidence file, both BL-1420's own
  paths, land in a separate tagged commit.

Result: NONE — no defect found; the BL-973 how-to gained a section for
this ticket's fixture fix, one changelog entry added.
