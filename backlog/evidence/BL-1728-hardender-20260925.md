# BL-1728 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `swarmforge/scripts/test/test_operator_runtime_babysitterd_watchdog.sh`
(fixture builder only) and
`specs/pipeline/steps/bl906OperatorBabysitterdFreshnessWatchdogSteps.js`
(fixture roots only) — no production `.bb`/`.ts` behavior touched, both
test-infrastructure fixes; CRAP/Stryker not applicable.

Checks run (host load ~2-3 on 20 cores, quiet):
- `bash swarmforge/scripts/test/test_operator_runtime_babysitterd_watchdog.sh`:
  ALL CHECKS PASSED, no REFUSED line — matches the ticket's original
  defect and its declared invariant.
- `node specs/pipeline/cli.js specs/features/BL-906-operator-babysitterd-freshness-watchdog.feature`:
  10/10 ok (was 4/10 on main per the ticket's 2026-09-25 amendment).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1728GitFixtureRootIsOwnCheckout.property.test.js` (from
  `extension/`): 1/1 passed — exercises the real
  `prove_git_fixture_root`/`init_git_fixture_root` shell functions
  (extracted by source position via `bl1728GitFixtureRootProofCli.sh`,
  never retyped) across good/not-a-repo/outside-a-live-checkout, with a
  before/after snapshot proving a refused proof never mutates the
  enclosing repository (Guardrails, BL-1390).
- `git diff --stat f93ebdf4af..HEAD -- <Scope files>`: only the shell
  test, the bl906 step handler, the property test + CLI shim — matches
  the amended Scope.
- `backlog/standing-reds.tsv`: both rows this ticket owns (the shell test
  and the BL-906 acceptance feature) present, both naming BL-1728.

Hand-mutation (no wired mutation tool for `.sh`, mutation_cost: low —
BL-638 fallback): independently re-verified the property test's own
non-vacuity claim by reverting `prove_git_fixture_root`'s
`--path-format=absolute` back to a plain `--git-common-dir` in the live
shell test file. The property test failed immediately (on the "good"
shape: a freshly `git init`'d fixture's own relative `.git` no longer
string-matched its absolute root, refused as if it were outside a live
enclosing checkout) — confirms the fix is load-bearing, not merely
present. Restored the original file (`git status --short` clean
afterward) before re-running the real suite green again (1/1 passed).

Guardrail re-read: concur with the architect's noted-not-bounced
observation that the JS step handler's `proveGitFixtureIsolated`
(`path.resolve(root, commonDir).startsWith(root)`, no explicit path-
separator boundary check, unlike the shell version's
`case "$common_dir" in "$d_abs"/*|"$d_abs")`) is theoretically weaker in
form but not reachable as a live defect on this call path:
`initGitFixtureRoot` always runs `git init` on `root` immediately before
calling the proof, so `commonDir` is deterministically `<root>/.git` at
check time — the "prefix without separator" false-pass class (a sibling
directory sharing a string prefix) cannot arise here because `commonDir`
is derived FROM `root` itself via `git -C root rev-parse`, never from an
independent path. No test added for this — there is no reachable failure
scenario to test.

Fixture hygiene: `pgrep -af "babysitterd.sh"` after all runs shows only
the two live-swarm daemons (repo-root paths), no leaked fixture-rooted
process under a mktemp path; the watchdog shell test's own `trap
cleanup_live_pids EXIT` fired correctly (exit 0). `git status --short`
clean of anything this pass's runs could have leaked (the three untracked
files present — `swarmforge/scripts/__pycache__/` and two
`bl1652-non-vacuity-*` scratch files — predate this session, not
produced by this pass).

Not run: `npm run test:properties` in full (2026-09-17 rule — only the
one relevant property file run) and Stryker/CRAP (no `src`/`out` file in
scope).

By hardender.
