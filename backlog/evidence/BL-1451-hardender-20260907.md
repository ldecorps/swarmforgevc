# BL-1451 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second hardening pass, after the coder's fix for the first pass's bounce
(D1, `backlog/evidence/BL-1451-bounce-20260907.md`: an unrecognized
`healthDot` value rendered the literal text `"undefined "` in the
caption line).

- Coder's fix (`3af8c4248a`): guards the lookup with
  `Object.prototype.hasOwnProperty.call(HEALTH_DOT_GLYPHS, dot)` rather
  than a bare truthy check - correctly goes further than my own
  remediation direction, since a bare `dot ? HEALTH_DOT_GLYPHS[dot] :
  undefined` still resolves a prototype-chain key
  (`"constructor"`/`"toString"`/etc.) to a truthy `Function`, which is
  its own live bug class.
- Independently verified the deeper claim, not just trusted it: patched
  the fix back to the bare-lookup form and re-ran the new property test
  - it failed exactly as claimed (`'function Object() { [native code] }
  9000 ...'` in the render), confirming the `hasOwnProperty` guard, not
  merely a truthy check, is load-bearing. Restored, re-verified green,
  clean diff.
- New coverage: a unit test pinning `"blue"` -> no dot, and a property
  addendum (150 runs) drawing both random strings and the five
  prototype-chain names by construction, asserting byte-identical
  rendering to the no-entry baseline and the literal absence of
  `"undefined"` anywhere in the output.
- Re-ran: full `pipelineBoard.test.js` (141 tests), the property suite
  (3 tests, malformed-dot addendum included), BL-1451's acceptance
  feature (6/6), and BL-670/BL-585/BL-1045's acceptance features (9/9,
  8/8, 5/5) - all green, no regression.
- `npm run compile`: clean.
- No stray test/mutation processes or fixture directories from this
  pass's own runs.
