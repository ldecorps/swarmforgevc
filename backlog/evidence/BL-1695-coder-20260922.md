# BL-1695 coder evidence — 2026-09-22

## Fixture cost before/after

Manual scratch-root build (the fixture's own `copyScriptsTree`, run directly,
not inside a property iteration):

```
entries at dest: 11 [
  'acceptance_pointer_gate_lib.bb',
  'backlog_depth_lib.bb',
  'bb_load_closure_cli.bb',
  'bb_load_closure_lib.bb',
  'daemon_cycle_guard_lib.bb',
  'headroom_cap_raise_lib.bb',
  'promotion_gates_cli.bb',
  'promotion_gates_lib.bb',
  'slice_size_envelope_gate_lib.bb',
  'swarm_identity_lib.bb',
  'test'
]
```

11 top-level entries (10 `.bb` files + `test/`) versus the pre-parcel 497
(496 files directly under `swarmforge/scripts` + `test/`) — a ~45x
reduction in both file count and the ~16MB/~130MB copy volume the
ticket's own measurements named.

**Deviation from the ticket's own "8 plus test/" count, found during
authoring and corrected here:** the CLI's own closure
(`closureOf(SCRIPTS, ENTRY)`) is 8 members (unchanged from mint:
`acceptance_pointer_gate_lib.bb`, `backlog_depth_lib.bb`,
`daemon_cycle_guard_lib.bb`, `headroom_cap_raise_lib.bb`,
`promotion_gates_cli.bb`, `promotion_gates_lib.bb`,
`slice_size_envelope_gate_lib.bb`, `swarm_identity_lib.bb`). Copying only
those 8 plus the runner is NOT sufficient: the runner itself
`load-file`s `bb_load_closure_lib.bb` directly (line ~56, to recompute
`promotion-gate-deps` from whatever is on disk when it runs — the
mechanism invariant 1's injected load-file edges depend on), and
invariant 1's own `closureOf(root, ENTRY)` re-derivation spawns
`bb_load_closure_cli.bb` against the scratch root. Omitting either
produces `File does not exist` from the runner's own `load-file` or from
the closure CLI itself — verified empirically before landing on the
11-file set above. Both are TOOLING the test code drives, never
something `promotion_gates_cli.bb` itself `load-file`s, so they are not
literally "in the closure" by `closureOf`'s own definition, but they are
part of the minimal set this fixture needs to function. This corrects
the ticket's "8 plus test/" figure to "8 plus the two closure-computation
files plus test/" — the underlying goal (closure-derived, not the
496-file directory) is unaffected.

## Fixture speed before/after

Whole-file run, isolated (`npx vitest run --config
vitest.properties.config.mjs test/bl1538Bl1028RunnerFixtureClosureInvariants.property.test.js`):
before ~863 ms (specifier's own mint-time measurement, twice); after
~268–301 ms across several runs today, both invariants green each time.

## Reproduction under load (qa_e2e_procedure item 3)

The ticket's own instruction: reproduce once under comparable load (the
full property lane started alongside, load average above 6), capture the
outcome verbatim; if it does not fire, say so and proceed on the code
alone.

The full property lane (`npm run test:properties`, 448 files) was run
this session with `bl1538Bl1028RunnerFixtureClosureInvariants.property.test.js`
and the new `bl1695AssertionNamesCauseInvariants.property.test.js` inside
it. Result: **448 passed (448), 1303 tests passed, 0 failures** — both
invariant files green, no assertion fired. Host load during that run
(`uptime` immediately after): `load average: 5.98, 9.70, 8.63` on a
20-core box — the 1-minute figure settling under 6 right as the run
ended, the 5- and 15-minute figures (which cover the run itself) both
above 6, so the run genuinely executed under comparable load to QA's own
sighting (load 8–11). The fixture did not race in this one run. Per the
ticket's own instruction, proceeding on the code fix alone: the 45x
reduction in copy volume and spawn count (497 files / ~16MB before vs.
11 entries after, per iteration) directly addresses the load-sensitivity
QA's evidence described, and the code fix stands on its own regardless
of whether this one reproduction attempt happened to fire.

## qa_e2e_procedure checklist

1. `npx vitest run --config vitest.properties.config.mjs test/bl1538Bl1028RunnerFixtureClosureInvariants.property.test.js`:
   2/2 pass (verified repeatedly, ~270–300ms). Scratch-root `ls` shown
   above: 11 entries, not 497.
2. `node specs/pipeline/cli.js specs/features/BL-1695-the-bl1538-closure-fixture-copies-the-closure-not-the-whole-scripts-tree.feature`:
   2/2 ok.
3. This section.
4. `git diff main...HEAD --name-only` for this parcel's own commit names
   only: `extension/test/bl1538Bl1028RunnerFixtureClosureInvariants.property.test.js`,
   `extension/test/bl1695AssertionNamesCauseInvariants.property.test.js`,
   `extension/test/helpers/bl1538ClosureFixture.js`,
   `extension/test/helpers/assertionNamesCauseCheck.js`,
   `specs/pipeline/steps/bl1695ClosureFixtureCopiesOnlyTheClosureSteps.js`.
5. Register retirement: land step's own job, not verified here.

By coder.
