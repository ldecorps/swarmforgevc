# BL-1698 — QA review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Third QA pass on BL-1698. Parcel: documenter dd5737a4d1, merged into QA as
520be5ccbb (the commit tested). Pass 1: BL-1698-QA-20260925.md (D1-D7);
pass 2: BL-1698-QA-20260925-2.md (D1-D2: chat set-up once per daemon).

Merge note: the inbound branch also carries BL-1704's and BL-1711's in-flight
code (both QA-bounced and reverted on QA's branch). QA's tree was built as
`-s ours`, plus BL-1698's own paths taken whole from the parcel, plus ONLY
BL-1698's hunk in handoffd.bb (the startup resume-writable-sweep! call; BL-1711's
two crash-restart hunks excluded), plus ONLY BL-1698's two suite-manifest lines.
Code-path diff against origin/main: BL-1698's 15 paths only, with zero BL-1704 or
BL-1711 markers.

Pass-2 items, re-verified on 520be5ccbb with pass 2's own probes (from
BL-1698-QA-20260925-2.md):
- D1 fixed: in one long-lived process, after escalation made the spec writable,
  the answer's fix request is preceded by CHAT-CLEAR/READ-ONLY/ADD and typed with
  `spec writable=false`.
- D2 fixed: after a simulated pane relaunch in the same process, the set-up is
  redone before the fix request. chat-set-up! now runs unconditionally before
  every fix request; the once-per-process marker is gone.
- Pass 1's D4/D5 re-checked (d4d5 probe): one answer fix request then ESCALATE;
  merge-conflict holds (mail and ticket) are a no-op with the answer left
  unconsumed.
- The coder's new regression test (bl1698LocalParcelDriverMailAndReleaseEdgeCases,
  one long-lived bb process, expects 5 CHAT-CLEARs) would read 1 under the old
  marker, so it is non-vacuous.

Gates (every one RUN, none BLOCKED; one run each):
- qa-gather.js: stragglers none before; sibling VERIFY; register 6 rows, all
  owned, unowned []; pre_qa_gate.sh required_wiring OK; npm test exit 0; BL-1698
  feature 9/9.
- npm run test:properties: exit 1, one failed test file, owned:
  extension/test/bl1517ProjectRootArgInvariants.property.test.js -
  "AssertionError: expected the generator to reach every site, reached:
  [\"operator_runtime.bb\",\"expedite_cli.bb\",\"dispatch_gap_sweep_harness.bb\",
  \"main_sync_status_cli.bb\",\"dropped_parcel_sweep_harness.bb\"]". It is on the
  standing-red register, owned by BL-1763; BL-1698 does not touch it. One run, no
  re-run (2026-09-20 rule). The only other error is the allowlisted BL-871
  `[vitest-worker]: Timeout calling "onTaskUpdate"`.
- qa_e2e 2: BL-1697 feature 12/12.
- qa_e2e 3: all 10 babysitter suites exit 0, plus test_babysitter_nudge_driver_seat_skip.sh
  ALL PASS.
- qa_e2e 4: local_parcel_driver_cli.bb with no args exits 1 (usage).
- Article 4.5 handoffd.bb: test_handoffd_one_shot_flags_parse.sh,
  test_handoffd_driver_seat_injection_skip.sh and
  test_handoffd_startup_restores_driver_spec_writable.sh all ALL PASS.
- No BL-1698 row in standing-reds.tsv, property_suite_standing_allowlist.tsv or
  suite-poles.tsv.

Verdict: NONE. Approve and land (a tip-pure land from BL-1698's own paths; the
handoffd.bb and suite-manifest content is BL-1698's hunks only).
