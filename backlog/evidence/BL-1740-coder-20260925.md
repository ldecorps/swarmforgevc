# BL-1740 — coder pass, 2026-09-25

## The fix

`swarmforge/scripts/chase_sweep_lib.bb`: `sweep-role-inbox!`'s `pause-held?`
now reads `(boolean (optional-adapter-call adapters :pause-hold-active? role))`
instead of calling `handoff-lib/pause-hold-active?` directly - the same
`optional-adapter-call` pattern `:role-agent-busy?`/`:role-lane-running?`
already use. Adapters carrying no `:pause-hold-active?` key (every
pre-BL-1740 fixture/property-runner) now read `false` (never pause-held),
never resolving `target-root`/`git rev-parse --git-common-dir` from
whatever checkout the process happens to run in.

`swarmforge/scripts/handoffd.bb`: added `:pause-hold-active?` to the
adapter map passed at `chase-sweep-lib/run-sweep!` (the map built in
`chase-sweep!`), wired to `(fn [role] (handoff-lib/pause-hold-active?))` -
`role` unused since a control pause holds a role's whole inbox uniformly
(the real function itself takes no role either). A paused live swarm
still holds every queued parcel, unchanged - only where the reading comes
from changed.

## The pinned handler + CLI

`specs/pipeline/steps/bl1740ChaseSweepPauseFromCallerSteps.js` (new) +
`specs/pipeline/steps/lib/bl1740ChaseSweepPauseFromCallerCli.bb` (new).
The CLI drives the REAL `chase-sweep-lib/sweep-role-inbox!` over a REAL
scratch git checkout (git init under mkdtemp, proven isolated via
`rev-parse --git-common-dir` before any further write, BL-1390) with one
stale unheld parcel for role "coder", varying only the `:pause-hold-active?`
adapter across the feature's four named modes - the last of which
(`live`) wires the REAL `handoff-lib/pause-hold-active?`, scoped to the
scratch checkout via `handoff-lib/set-project-root!` (the same override
handoffd.bb's own startup call uses), proving the live reading resolves
the CALLER's own checkout once wired in, never a different one.

Non-vacuity, checked directly (not left in the commit): reverted
`chase_sweep_lib.bb`'s fix (`git checkout --`, the file was clean
beforehand) and re-ran the CLI across all four modes by hand - `true`
(a fake adapter explicitly saying "held") still chased (`chaseCount 1`,
expected 0), because the pre-fix code ignored every adapter and read the
live checkout directly, exactly the ticket's own bug. Restored the fix,
re-ran: `none`/`false` -> 1, `true`/`live` -> 0, matching the feature's
own Examples table exactly.

## Verification run before forwarding

- `node specs/pipeline/cli.js specs/features/BL-1740-...feature`: 4 of 4.
- `bb specs/pipeline/steps/lib/bl1740ChaseSweepPauseFromCallerCli.bb <mode>`
  for all four modes, run by hand: matches the feature's Examples table.
- `bash swarmforge/scripts/test/test_chase_sweep.sh`: ALL PASS (every
  pre-existing chase-sweep scenario, unaffected - the ticket's own
  analysis holds: adapters carrying no `:pause-hold-active?` key degrade
  to "never held", the same as the pre-fix default whenever the live
  checkout genuinely wasn't paused, which is the common case every
  existing fixture already exercised).
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1505DedupSuppressedChaseCountInvariants.property.test.js`: 3 of 3
  (the originally-red file, now provably immune to a live pause - it never
  wires `:pause-hold-active?` at all, so it always reads "not held"
  regardless of the real swarm's pause state).
- `npm test` (extension/, compile + unit lane): 637 files / 10868 tests
  pass; exit 1 was purely the suite's own duration-budget guard under
  measured host load 23.18 on 20 cores - the same environmental
  contention pattern already documented earlier this session (BL-1717/
  BL-1738's own evidence), not a correctness regression.
- `grep -n ':pause-hold-active?' swarmforge/scripts/handoffd.bb`: present,
  in the map passed at `chase-sweep-lib/run-sweep!`.
  `grep -n 'handoff-lib/pause-hold-active?' swarmforge/scripts/chase_sweep_lib.bb`:
  no match (qa_e2e step 4, both halves).

## Scope

Touched: `swarmforge/scripts/chase_sweep_lib.bb` (the pause read),
`swarmforge/scripts/handoffd.bb` (one adapter entry),
`specs/pipeline/steps/bl1740ChaseSweepPauseFromCallerSteps.js` (new, same
parcel as the feature per BL-233/BL-1371, auto-discovered per BL-1371, no
index.js edit needed), `specs/pipeline/steps/lib/bl1740ChaseSweepPauseFromCallerCli.bb`
(new), this evidence file. `backlog/standing-reds.tsv` NOT touched
(coder.prompt, BL-1663). Nothing else in this worktree staged - the
unrelated untracked leftovers (BL-1666/BL-1652) are not this ticket's and
are left as found.

By coder.
