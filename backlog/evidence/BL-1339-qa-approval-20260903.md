# BL-1339 — QA approval (20260903)

Received: documenter commit `71bb1c3e3a`, forwarding hardener, architect,
cleaner, coder's writer+reader shared-root fix per the human's ruling
(option 2). Merged into QA (two additive conflicts resolved:
`specs/pipeline/steps/index.js` own require line kept;
`docs/reference/Specification.MD`'s changelog-prepend resolved as
BL-1339's entry on top, BL-1336's as "Prior entry").

This ticket fixes the exact `record-land-approval!` mechanism I have
invoked from this worktree on every land this session — directly relevant
to my own tooling, so verified with extra care.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- All five existing shell consumer suites re-run and green:
  `test_land_step_records_approval.sh`,
  `test_is_qa_ancestor_land_replay_store.sh`,
  `test_is_qa_ancestor_expedite_store.sh`, `test_is_qa_ancestor_yaml_store.sh`,
  `test_build_freshness_land_replay_approved.sh` — confirming the three
  named daemon consumers (handoffd's push sweep, the babysitter's Article
  4.2 sweep, the deploy freshness gate) and the BL-952 bounce-veto union
  all still hold.
- Acceptance (`specs/features/BL-1339-a-land-approval-record-lands-where-the-predicate-reads.feature`):
  7/7.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1339LandApprovalSharedRootSteps` — confirmed.
- Declared invariants
  (`extension/test/bl1339LandApprovalRootInvariants.property.test.js`):
  3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated `record-land-approval!`'s call site to skip
  `shared-target-root` resolution entirely (`(if-let [shared root] ...)`,
  writing to the caller's own root instead of the shared one) — the
  single-root shell test still passed (as expected, that fixture can't
  express the defect), but the multi-root acceptance feature dropped to
  2/7, matching the coder's claimed "5 of 7 red" exactly. Restored
  (`git diff --stat` empty), re-ran 7/7 clean.
- Standing whole-tree guards re-run
  (`tempDirTrapGuard`/`socketFixtureShortRootGuard`/`liveRepoDerivationGuard`):
  3 failures, matching the same pre-existing/already-ticketed set as
  every prior pass today.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population. None touch this parcel's files.
- Property suite (`npm run test:properties`): 24 files / 13 tests red —
  exactly the standard standing-debt population from the session's
  earlier passes (notably, BL-1323's invariant no longer appears — the
  BL-1336 fix confirmed stable). BL-1339's own property test — 3/3 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry confirmed present and accurate; documenter's evidence confirms
  `docs/reference/BL-632-commit-time-guard-refuses-pipeline-code-on-main.md`
  needed no correction (operates one level beneath this fix, on which
  store rather than which root). No diagram change-trigger fired.
- Orphaned test processes: reaped my own leftover vitest process group
  after the property-suite run; left a different, concurrently-running
  role's own `bl1333StampOffInvariants` process group untouched (not mine
  to kill).

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
