# BL-1724 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: test-only change (`extension/test/bl1652HandoffdRespawnReadingsWiring.test.js`),
no production `.ts`/`.bb` touched — CRAP (`src/*.ts`) and Stryker
(`out/**/*.js`) have nothing new to score; DRY not applicable to a single
file with no duplication introduced.

Checks run (host load 2.82-2.04-3.17 on 20 cores, quiet — no cooldown/load
gate applied):
- `npx vitest run test/bl1652HandoffdRespawnReadingsWiring.test.js` (from
  `extension/`): 7/7 passed, 934ms test time (1.29s total) — well under
  the 7.0s per-file budget and consistent with the coder/cleaner/architect
  timings (0.9-2.6s), confirming the fix holds independent of run order.
- `grep -c "execFileSync('bb'" test/bl1652HandoffdRespawnReadingsWiring.test.js`
  → 1, matching the ticket's declared invariant.
- `node specs/pipeline/cli.js specs/features/BL-1652-the-chase-sweep-never-respawns-a-busy-role-and-respawns-at-most-once-per-sweep.feature`
  (repo root): 4/4 ok.
- `npx vitest run --config vitest.properties.config.mjs test/bl1280MkdtempMigrationInvariants.property.test.js`:
  6/6 passed, re-confirming the coder's `mkTmpDir` → `mkSharedTmpDir` fix.
- Fixture hygiene: `pgrep -fl 'run_acceptance.sh'` found no leaked
  `laneChild` (the `role-lane-running?` fixtures' spawned process) after
  the run — `afterAll`'s `SIGKILL` is effective; `git status --short
  extension/test/` clean — no orphaned fixture files.
- Read `runAllCases`' `id\tvalue` line-parsing and the single combined
  `bb` script construction by hand: no case's printed value can contain a
  tab or newline (numbers, `nil`, `true`/`false`, or
  `format-respawn-readings`'s single-line output), so the tab-delimited
  per-line parse cannot misattribute one case's result to another; every
  assertion is an exact-string match against the real `handoffd.bb`
  output, so this pass's own concern — a false-green harness — does not
  apply here (unchanged from the architect's correctness read).
- Deferred-gate ledger: no first-run mutation gate applicable (test-only,
  `mutation_cost: low` per the ticket) — nothing to discharge or defer.

Not run: `npm run test:properties` in full (2026-09-17 rule — only the one
changed property file re-run, per the coder/architect precedent this same
session) and Stryker/CRAP full-repo passes (no `src`/`out` file in scope).

By hardender.
