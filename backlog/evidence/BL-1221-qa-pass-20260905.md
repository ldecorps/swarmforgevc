# QA pass — BL-1221 — 2026-09-05

Commit under review: `cd17a6fc6d` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`63271a56ce` BL-1221
coder, confirmed ancestor via `git merge-base --is-ancestor`).

This is the ticket behind the largest recurring cluster of "pre-existing
standing red" failures I've grepped and traced back to BL-1229/BL-1221
throughout this session's earlier QA passes (BL-1411/BL-1405, BL-1414,
BL-1291). Verified per its own `qa_e2e_procedure`.

## Verification (qa_e2e_procedure, in order)

1. `grep -rln "checkOrphanedAuthoredDocs" extension/test/*.test.js | wc -l`
   → 15 files (matches the 15 real `landPilotedTicket` callers; the two
   other files `grep -l landPilotedTicket` matches,
   `multiworktreeAcceptanceFixture.test.js` and
   `pilotAcceptanceGateCli.test.js`, only mention the name in a comment
   and a string literal respectively — confirmed by direct read, not real
   callers, correctly left unstubbed).
2. `npx vitest run test/unreachableStepHandlerCheck.test.js
   test/pilotAcceptanceGate.test.js` — both fully pass (42/42 tests), no
   `checkOrphanedAuthoredDocs is not a function`.
3. Ran all 8 unit-lane files: fully green (8/8 files). Ran all 6
   property-lane files via the full `npm run test:properties` command
   (per-file vitest invocation on a node:test-format property file
   produces a spurious "No test suite found" harness artifact — confirmed
   by running the full suite command instead, which is how these files
   are actually invoked in practice: exit 0, clean). BL-1220 (the sibling
   node:test-collection defect the ticket warned would still leave ~10
   files uncollectable) is already landed and closed
   (`backlog/done/M8/BL-1220-...yaml`), so every file is fully green here,
   not merely error-absent as the ticket's own procedure anticipated as
   the interim state.
4. `git diff` of `pilotAcceptanceGate.ts` / `pilot-acceptance-gate.ts`
   between this ticket's parent and its own commit: empty — production
   contract and call site untouched, member still required.
5. `npm run compile`: clean.

## Additional verification

- Acceptance: `run_acceptance.sh
  specs/features/BL-1221-pilot-gate-deps-stubs-missing-required-orphan-docs-check.feature`
  — 3/3 scenarios pass, including "the production contract is not
  weakened to accommodate the stubs".
- Full unit lane (`npm test`): failing files dropped from the 14-15 in my
  earlier evidence files this session to **6** — `backendSwitch.test.js`,
  `constitutionDocCitations.test.js`, `operatorRuntimeBbFixtureClosure.test.js`,
  `pricingTable.test.js`, `telegramClient.test.js`,
  `telegramCursorOperatorExec.test.js` — every one of which I already
  traced to its own separate open ticket earlier this session (BL-1263,
  BL-1212, BL-1265). None of the `checkOrphanedAuthoredDocs`-family files
  remain.
- `required_wiring`: `specs/pipeline/steps/bl1221PilotGateDepsStubsSteps.js`
  exports `registerSteps`.
- Out-of-scope item (a structural guard closing the interface/stub-divergence
  class) correctly left unbuilt, as the ticket's own `out_of_scope`
  section directs — a decision, not an oversight.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty
  before and after.

## Verdict

APPROVED — merging up and landing.
