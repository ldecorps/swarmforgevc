# BL-1563 — cleaner review pass, 2026-09-14

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

Checks run (review-only ticket per the ticket's own description - it
reviews landed hotfix `0b727b286c`, adding no production behavior):

- Merged coder's commit `3aac4e1c5e` into the cleaner worktree
  (`git merge-base --is-ancestor` confirmed lineage before merge).
- Coverage: N/A for uncovered *behavior* - this parcel adds test/review
  code only, no production logic to cover.
- CRAP: N/A - no TypeScript/production code touched (invariant: the eight
  hotfix production/doc files stay byte-identical).
- DRY (`jscpd`): 0 clones across the two step-handler JS files.
- Mutation-site count (BL-485,
  `node extension/out/tools/mutation-site-count.js`): the step handler
  `bl1563SwarmStampRouterTargetSteps.js` was 436 sites (over the 100
  threshold). Split fixture/CLI-invocation helpers into
  `specs/pipeline/steps/lib/bl1563FixtureHelpers.js` (matches the existing
  `lib/babysitterSweepFixtureHelpers.js` convention) - a genuine
  separation-of-concerns split (fixture construction vs. Gherkin step
  bindings), not a mechanical line-count chop. Result: 239 + 198 sites,
  still over threshold on both halves; left as-is rather than fragmented
  further, since `registerSteps` is the one function the ticket's
  `required_wiring` pins to this exact file and further splitting it would
  trade traceability for a number on a soft advisory.
- Architecture: extension-host/webview boundary not touched; module now
  separates test-data construction from step-binding, improving
  encapsulation per Architecture Rules.
- Re-ran the full acceptance suite after the split
  (`specs/pipeline/scripts/run_acceptance.sh` on the BL-1563 feature):
  16/16 subtests pass, unchanged from before the split.
- Re-ran the three real underlying checks the parcel drives:
  `test_ready_for_next_rotate_home.sh` (ALL CHECKS PASSED, cases 16-20
  present), `test_handoffd_priority_rotate_wiring.sh` (ALL PASS, all four
  A-D fail-message literals present in source),
  `mono_router_lib_test_runner.bb` (ok).
- Confirmed invariant: `git diff main...HEAD --name-only` names only
  evidence, the feature (already minted), the step handler + its new
  `lib/` helper, and no line of `backlog/hotfix-ledger.yaml`.
- No new/uncovered *behavior* was introduced - review-only ticket, no
  mutation hardening pass applicable beyond the structural split above.

Commit forwarded: `f5098f1637` (this evidence file's own commit, per
BL-536, names the commit AFTER it).

By cleaner.
