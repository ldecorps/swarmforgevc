# BL-1636 — hardender review pass, 2026-09-19

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: node --test specs/pipeline/test/fixtureReaper.test.js (baseline, then hand-mutated trackedTmpRoot() removing trackedTmpRoots.add(root), then restored)
- **Commit hash**: 9434404d55
- **First error excerpt**: hand-mutant (root created but never tracked, so onAbnormalExit never reaps it - a real leak) survived every existing test in the parcel; trackedTmpRoot itself had zero direct invocation coverage, only its name was fed to the finder as text
- **Failure class**: test-coverage-gap
- **Expected vs observed**: a root trackedTmpRoot() creates is removed when its owning process exits normally or is SIGTERMd
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added two subprocess-driven tests to specs/pipeline/test/fixtureReaper.test.js (normal-exit removal, SIGTERM removal), following the files own never-require-onAbnormalExit-into-the-test-process posture; commit 9434404d55; confirmed it now kills the hand-applied mutant

By hardender.
