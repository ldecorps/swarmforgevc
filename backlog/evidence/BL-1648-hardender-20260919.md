# BL-1648 — hardender review pass, 2026-09-19

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

- BL-1648's own acceptance feature: 4/4 green. BL-1428's amended feature
  (scenario 01, the fix's target): 7/7 green, confirming the `--now`
  pass-through actually fixes the wall-clock red.
- Property test (`bl1648RegisterCliInjectedDateInvariants.property.test.js`)
  reviewed: good invariant-2 coverage (both directions of the date
  ordering, non-vacuity check, no-date-means-today) and a well-reasoned
  explanation for why invariant 1 is documented rather than
  regex-scanned (a data-flow claim a source-text scan cannot answer
  without false-positiving on the very file this ticket fixed).
- Manually verified (by hand, not added as tests - see below) the two
  CLI refusal paths the acceptance/property suites do not exercise:
  an unrecognised flag (`--bogus foo`) and `--now` with no following
  value both correctly exit 2 with a named message. Left untested
  deliberately: the ticket's own IR-DRY note scopes the test matrix to
  exactly four shapes (pinned, none, two dates, malformed), a decision
  the specifier reviewed and kept; both paths are already correct and
  low-risk
  (a fixture-only seam, never live-swarm-exposed), so adding coverage
  here would be scope creep against a documented decision rather than
  closing a real gap.
- The coder's grep-audit evidence (`BL-1648-coder-clock-bound-assertion-grep-20260919.md`)
  spot-checked 5 of ~135 `Date.now()`/`LocalDate/now`-matching files
  rather than all of them - noted here as an observation for QA's own
  completeness check (Article 4.4), not something this pass's own
  checklist (test coverage on the shipped code) covers.
- Whole-tree guards (21 files, 210 tests): all green, no fixture leak
  from the new step handler.
- No hardening commit produced this pass; forwarding the merge commit
  (a genuine duplicate-function conflict resolution, the same class
  seen three times already this session).
