# BL-1703 — hardender review pass, 2026-09-24

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Verified: the property test (`npx vitest run --config
vitest.properties.config.mjs test/bl1703OllamaLaunchProbe.property.test.js`,
2/2); the ticket's own acceptance feature (5/5); the swarmforge.sh
call-site shell test `test_ollama_ancillary_launch_gate.sh` (ALL PASS,
twice); `test_aider_seat_launch_config.sh`,
`launch_contract_test_runner.bb`, `test_swarm_launch_pack_guard.sh`,
`test_swarm_outlives_launcher.sh` (all ALL PASS, unaffected); `npm test`
(636 files, 10855/10856 - the one failure is
`stepHandlerModuleLoadBudget.test.js`'s known full-suite host-load timing
flake on a synthetic fixture case, not touched by this parcel's diff,
green alone). No `extension/src/*.ts` change - bash/Babashka only, no
wired mutation/CRAP/DRY tool (engineering.prompt "Startup Tools").

Found and fixed in this pass (commit 602c6b323f), rather than bouncing -
own-domain robustness hardening: `ollama_ancillary_lib.sh` is sourced
into `swarmforge.sh`'s own top-level launch flow (inside the
`if [[ "$ZSH_EVAL_CONTEXT" == "toplevel" ]]` block - one flat shell
namespace shared with the rest of that flow), and none of its functions
declared their own parameters/locals with `local` - every call left
variables like `url`, `pid`, `binary`, `state_dir`, `elapsed`, `owner`
etc. behind as real globals in the caller's process. Confirmed live, not
hypothetical: sourced the pre-fix file with pre-existing top-level
`url`/`pid`/`binary` set, called `ollama_ancillary_ensure_ready_for_launch`,
and all three were silently overwritten to the library's own internal
values. `swarmforge.sh`'s own top-level flow does not currently read any
of these particular names after the call site, so this was not yet an
observed failure - but it is exactly the "same-named variable a future
diff adds" hazard this class of bug produces, and the fix (`local` on
every function-scoped variable) is behavior-preserving: `local` is fully
supported by both runtimes this file actually targets (bash 3.2, zsh -
never POSIX dash/sh; the file's own stated portability goal was "no
arrays, no bash-4-only builtins", not POSIX-sh execution). Re-ran the
same demonstration after the fix: the three pre-existing variables now
survive the call untouched. `bash -n` and `zsh -n` both clean.

By hardender.
