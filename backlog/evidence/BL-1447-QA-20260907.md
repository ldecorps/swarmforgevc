# BL-1447 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Why this ticket matters to this session specifically

This is the fail-closed net for the exact failure mode I hand-caught
twice today landing BL-1448 (`backlog/evidence/BL-1448-QA-20260907.md`):
an automated replay silently dropping the parcel's own step handler and
several evidence files, and separately substituting a stale pre-fix
version of a shell suite — both times the tool printed a plausible-looking
`LAND_REPLAY` for an incomplete tip. With this ticket, that class of
mistake becomes `LAND_ESCALATE` / `replay-incomplete: <path> ...` instead
of a silent bad publish.

## Verification

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS
  (includes 5 new direct unit tests for `replay-missing-paths`'s pure
  core, per architect's evidence — re-ran myself, consistent).
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1447-...feature`:
  3/3 pass (both `replay-incomplete` escalation rows, and the
  complete-replay-still-reports-LAND_REPLAY row).
- Sibling features named by qa_e2e_procedure step 1, all independently
  re-run (not trusting architect/hardener's evidence alone): BL-1432 5/5,
  BL-1431 4/4, BL-1389 5/5, BL-1241 4/4. Also re-ran BL-1375 (7/7,
  architect/hardener's own addition to the sibling list) since it shares
  `land_step_lib.bb` and the passenger-riding path this ticket's refactor
  touches.
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass.
- The two unowned reds the coder's own e2e surfaced (BL-1272 scenario 4,
  BL-1297 scenario 3) are confirmed already owned (BL-1463, BL-1464) —
  not this ticket's to fix.
- No file under `extension/src` or `extension/test` changed by this
  ticket's own diff (bb + a test-only steps handler/fixture) — the full
  compiled unit suite's last-established state stands unchanged.
- No orphaned `node --test`/vitest processes remained (checked by
  PID/cwd).

## Merge note

Merging the documenter's tip produced a genuine conflict in
`docs/reference/Specification.MD`, because this session bounced BL-1450
(`backlog/evidence/BL-1450-bounce-20260907.md`) and reverted its content
out of this branch BEFORE this BL-1447 merge — but the documenter's
branch was built on top of BL-1450's pre-bounce state, so it still
documented BL-1450 as landed. Resolved by keeping BL-1447's changelog
entry and dropping BL-1450's (that fix is not, in fact, on this branch
right now). The merge-deletion guard correctly caught this — BL-1450's
evidence files and step handler disappearing relative to the incoming
branch — and required naming BL-1450/BL-940 explicitly in the merge
commit message to confirm it was deliberate, which it is.

## For the specifier, per this ticket's own notes

"QA.prompt's interim hand check (landed 2026-09-06 with this mint,
'Before landing any replay ... until BL-1447 lands') is removed by the
specifier when this ticket lands — say so in the parcel's evidence so the
specifier is told." Confirmed it is still present at
`swarmforge/roles/QA.prompt` (the "Before landing ANY replay" section,
BL-1424-shaped hand-check) as of this landing. The SEPARATE BL-1461
interim hand-check ("Until BL-1461 lands... LAND_CLEAN is not proof")
added the same day for a different, still-open gap is unaffected and
should stay.

## Disposition

Approved. No rework needed. Landing next.

By QA.
