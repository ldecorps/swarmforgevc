# BL-1558 — QA review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- Merge documenter 9f03765583 into QA (2630d6b3a9), then commit 07c7ac4894.
- Unit suite (extension/): `npm test` — 619 files, 10495 tests, all green.
  Per-file/suite budget overages are the pre-existing perf-budget lane,
  unrelated to this ticket.
- Property suite: `npm run test:properties` — 407 files, 1197 tests, all
  green; the only errors are the allowlisted BL-871
  `[vitest-worker]: Timeout calling "onTaskUpdate"` noise.
- Acceptance: `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1558-resident-rotation-reason-honours-wrapper-env.feature`
  — 5/5 scenarios pass.
- Wiring: `bash swarmforge/scripts/pre_qa_gate.sh
  BL-1558-resident-rotation-reason-dropped-by-respawn-as HEAD` — OK.
  Confirmed `respawn-as!` (handoff_lib.bb:1344) calls the new
  `mono-router-lib/resident-rotation-reason` helper with
  `SWARMFORGE_ROTATION_REASON`, matching the ticket's invariant exactly.
- Standing-red register (`backlog/standing-reds.tsv`): empty — no unowned
  reds to gate on.
- Sibling status: `qa-sibling-check.js status --ticket BL-1558` → `VERIFY`
  (no open deferral).
- Straggler check: `pgrep -fl 'node --test|stryker|vitest'` clean before
  and after the run.
- Noted, not a defect: the cleaner's evidence file for this ticket landed
  at `extension/backlog/evidence/BL-1558-cleaner-20260915.md` (wrong cwd
  when the tool ran) instead of `backlog/evidence/`. No functional impact;
  left as-is rather than bounced.

By QA.
