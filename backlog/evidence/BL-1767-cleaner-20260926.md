# BL-1767 — cleaner review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- Merged coder 0c38b55da0 into cleaner. Diff scope confirmed against the
  ticket's Scope: `test_model_steward_cli.sh`, `bl547ModelStewardSteps.js`,
  `bl556EvaluateIngestSteps.js`, `bl1079CursorStewardCertifySteps.js`, the
  new shared helper `specs/pipeline/steps/lib/modelStewardSafetyCard.js`,
  the new property test, and evidence. Confirmed `model_steward_lib.bb`,
  `model_steward_cli.bb`, `model_steward_store.bb`, the seed file, and
  every `.feature` file are untouched by this ticket's own commits (the
  CLI's diff against main is BL-1700's unrelated `probe` dispatch, already
  on this branch from an earlier pass).
- Reviewed `modelStewardSafetyCard.js`: `readSafetyCompetencies()` shells
  to `bb -e` against the real lib and is memoized per process;
  `passingSafetyEntries` is pure. Matches the invariant's "read from the
  lib when the test runs" requirement and the direction given (a literal
  list would have needed an equality check against the lib; this instead
  reads live).
- Re-ran independently:
  - `bash swarmforge/scripts/test/test_model_steward_cli.sh`: ALL PASS (13
    cases, 05/05c/05d green).
  - BL-547 feature: 15/15. BL-556 feature: 6/6. BL-1079 feature: 5/5.
  - `bb swarmforge/scripts/test/model_steward_test_runner.bb`: ALL PASS.
  - `npm run compile` (extension/): clean.
  - `npx vitest run --config vitest.properties.config.mjs test/bl1767ModelStewardSafetyCardEntries.property.test.js`:
    1/1 (200/200 property runs) — the one-exception case (a property test
    this parcel added), run once.
  - `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4 (new/edited
    step handlers touched; none add a raw `fs.mkdtempSync`).
- `mutation-site-count.js` on the four changed JS files:
  `modelStewardSafetyCard.js` 25 (within); `bl547ModelStewardSteps.js` 446,
  `bl556EvaluateIngestSteps.js` 311, `bl1079CursorStewardCertifySteps.js`
  270 (all `over` the 100-site advisory). Not split: all three are
  pre-existing step-handler registries for their own unrelated features,
  already well over threshold before this ticket; this ticket's edit to
  each is a few lines (swap a literal safety-entry pair for the shared
  helper, one seed-count fix). The ticket's own Scope lists these files
  for a targeted fixture fix, not a restructuring mandate, and a
  mechanical split now would fragment three unrelated feature's step
  registries for no separation-of-concerns gain tied to this ticket. Left
  whole as the correct call, consistent with the same reasoning applied on
  BL-1749/BL-1761/BL-1700 this session.
- BL-556's second, discovered-by-testing fix (planting the well-known
  battery scorecard `scorecards/<provider>__<model>.json`, since
  `evaluate`'s auto-certify never reads the ingested `--scorecard`
  artifact for its safety check) is scoped to the one scenario that needs
  the auto-certify path to fire — verified the other five scenarios
  (1,2,3,4,6) still read `caps.*` off the same recruiter card and stayed
  green, per the ticket's own "check them; do not assume" instruction.

By cleaner.
