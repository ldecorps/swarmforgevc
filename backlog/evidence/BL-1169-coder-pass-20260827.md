# BL-1169 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Delivered

Extended `babysitterd_sweep_lib.bb`:

- **Half-launch** (`proc-*`, pane up / agent gone): queues bounded
  `:ensure-session` alongside the CRIT when `should-stand?` and
  `session-repair-allowed?` (same bound as BL-1017 missing-session).
- **Swarm-starved**: at streak ≥ 3 (default), queues `:ensure-control-plane`
  alongside the CRIT so recovery is not escalation-only.
- Per-role ensure suppressed when control-plane ensure is already queued
  (including starved path).

BL-1017 scenario 03 half-launch row retired (notes / BL-1006).

## Invariants

| invariant | encoding |
|---|---|
| CRIT remains visible when repair queued | acceptance 01/03 + unit |
| Topology gates half-launch repair | property P4/P6 + unit |
| Standing-pack ensure action is `:ensure-control-plane` | acceptance 04 |

## Verification

| check | result |
|---|---|
| `babysitterd_sweep_lib_test_runner.bb` | ok |
| `babysitterd_sweep_lib_property_runner.bb` | ok |
| `run_acceptance.sh BL-1169…feature` | **4/4** |
| `run_acceptance.sh BL-1017…feature` | **5/5** |

By coder.
