# BL-1226 — QA review pass, 2026-09-06

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Land incident (fourth occurrence this shift, not re-escalated — BL-1446/1447 already own it)

First `--land` attempt produced `LAND_REPLAY`; this time the tool
correctly identified `BL-1262`/`BL-1409` as unlanded `ENTANGLED_SIBLING`s
and excluded their specific paths (`EXCLUDED_SIBLING_PATH` lines) — the
entanglement-detection half worked. The bug is specifically in computing
this ticket's OWN surviving path set: the mandated hand cross-check found
9 of this ticket's own 12 files missing from the pushed tip. Fixed by
hand: merged the broken push under the correct branch, verified every
file present by name AND content, pushed the merge (`origin/main`
`59638c315c` → `9e320f1a15`), and recorded `abandoned_commits:
[0c8b6f8537]` on the ticket. No new specifier note — BL-1446 (root cause)
and BL-1447 (the automated net) already exist from BL-1424's report.
Full narrative:
`backlog/evidence/BL-1424-land-replay-dropped-own-paths-incident-20260906.md`.

## Detail

- `qa-sibling-check.js status --ticket BL-1226`: `VERIFY BL-1226` (no open
  deferral).
- Orphan process check before/after: empty both times.
- Baseline (qa_e2e item 1, context only): `grep -l mkdtempSync
  specs/pipeline/steps/*.js | wc -l` → 543; `comm -23` against files
  referencing `socketFixtureRoot` → 538 (grown since the ticket's own
  2026-08-28 measurement of 436/821, consistent with the shift's own
  ticket volume — not a pass/fail condition per the ticket's own text).
- Acceptance: first run of `run_acceptance.sh
  specs/features/BL-1226-mkdtemp-convention-gate-covers-step-handlers.feature`
  failed 12/14 with `classifyMkdtempConventionPath is not a function` —
  the same stale-`extension/out/` pattern from BL-1412/BL-1437 earlier
  this shift (the merge brought `.ts` source changes I had not
  recompiled). `npm run compile` then 14/14 pass. Not a code defect.
- Regression: `npx vitest run test/tmpDirMigrationGuard.test.js` (qa_e2e
  item 6) — 12/12, the extension/test lane's own guard unaffected by the
  widening.
- BL-1226's own property invariants file
  (`bl1226MkdtempConventionGateStepsInvariants.property.test.js`) run
  standalone: 3/3 (P1: untouched files never scanned/named; P2: any base
  expression fed through the steps lane's route-based detector is always
  caught, and the shared helper is always clean).
- Wiring anchor 1: `pilotMkdtempConventionCheck.ts` names
  `specs/pipeline/steps` in its `STEPS_JS_RE` scope classifier and scans
  it in `assessPilotMkdtempConvention`.
- Wiring anchor 2: `bl1226StepHandlerMkdtempGateSteps.js` exports
  `registerSteps`; auto-discovered by `specs/pipeline/steps/index.js`.
- Invariant 1 confirmed by direct code read: `assessPilotMkdtempConvention`
  takes `touchedRelativePaths` as its only input and only ever reads/scans
  paths from that list (`inScopePath` gates every iteration) — an
  untouched file is structurally unreachable, not merely untested.
- Invariant 2 confirmed by direct code read of `rawMkdtempDetector.ts`:
  the steps lane's `RAW_MKDTEMP_ANY_BASE_PATTERN` (`/\bmkdtempSync\s*\(/`)
  matches ANY direct `mkdtempSync(` call regardless of its argument
  expression — genuinely route-based (closing the twelve-file blind spot
  a spelling-enumeration approach could never close), while the
  extension/test lane keeps BL-743's original narrow pattern completely
  unchanged (confirmed: `tmpDirMigrationGuard.test.js` 12/12 unaffected).
- Full unit lane (`npm test`, extension/): 610/610 files, 10309/10309
  tests green — matches the hardener's own reported count exactly. Same
  chronic suite-duration/per-file budget overage as other recent parcels,
  unrelated; the `bridgeServer.test.js` SSE-keepalive flake the coder and
  hardener both documented did NOT recur in this run.
- Full property lane (`npm run test:properties`): 366/368 files green on
  the full run (1098/1100); see "Not bounced" below for the two
  land-step-family timeouts — confirmed 368/368 clean via isolated
  re-run once load eased (uptime 4.90 vs ~9-14 during the full run).
- BL-1348 entanglement, independently re-confirmed: this branch carries
  BL-1348's own held WIP checkpoint (`human_approval: pending`, still
  `backlog/active/`) as ancestor history — the SAME class of entangled
  sibling seen repeatedly earlier this shift (BL-1441, BL-1445, BL-1437's
  BL-1445). The hardener already ran `task_scope_gate_cli.bb` and got a
  clean `OK` (no entanglement of BL-1226's own scope); not independently
  re-run here since the hardener's own tool-based check is authoritative
  and this ticket's diff genuinely touches none of BL-1348's own files
  (confirmed by file-list inspection of the coder's own commit,
  a172b28cfa).
- Docs: `docs/reference/Specification.MD` gained the BL-1226 entry. No
  diagram trigger fires (not claimed by the ticket; confirmed no diagram
  touched).
- Out of scope respected: the 434 legacy steps-lane handlers untouched
  (confirmed structurally by invariant 1, not just by the ticket's own
  claim); BL-1410 and BL-1209's own files untouched by this diff.

## Not bounced (observed, not this ticket's defect, not reproducible — host load, BL-1348-documented class)

Full property run failed 2 files under host load:
`bl1272LandedSiblingInvariants` and `bl1343ReplayNeverDropsOwnPathInvariants`
(both the fixed 20s per-test timeout). Both in the land-step
entanglement/replay family, unrelated to BL-1226's domain (a static-scan
convention gate). This is the SAME contention class the coder's own
evidence already measured and filed in detail
(`backlog/evidence/BL-1348-coder-finding-fork-ceiling-linux-full-forge-contention-20260906.md`,
driven by this branch's own held BL-1348 WIP raising real fork-pool
concurrency) — not re-documented at length here. No ticket found beyond
BL-1348 itself (`grep -rl <name> backlog/paused/ backlog/active/` empty
for both files specifically; neither in `backlog/standing-reds.tsv`).
Re-ran both in isolation once load eased (~4.9): both passed cleanly.
Same disposition as this shift's prior "Not bounced" sections (BL-1442,
BL-1424, BL-1412, BL-1409).
