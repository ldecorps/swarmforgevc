# BL-1555 QA pass — unowned red found in the full property lane, 2026-09-14

While verifying BL-1555 (constructing the bl1253/bl956 reach floors), ran
the full `npm run test:properties` lane on the merged tree
(`006c8a3eed`). 403/404 files green; one file failed:

```
FAIL  test/bl1297MergeOwnPathsInvariants.property.test.js > property
(invariant 3): the land step reads delivered, the two send-time gates
read authored
Error: Test timed out in 20000ms.
```

Not touched by this parcel's diff (bl1253/bl956 files + step handler
only). `grep -rl bl1297MergeOwnPathsInvariants backlog/` and
`backlog/standing-reds.tsv` show no owning ticket for this property file
(BL-1447's adjudication covers the BL-1297 *acceptance* feature's
scenario 03 → BL-1464, done — a different lane/file).

Re-ran the file alone under `vitest.properties.config.mjs`: green,
20.59s total, invariant 3 alone 8.67s — consistent with a host-load
timeout when running inside the full 404-file lane (same class as the
BL-1556-owned bl1272 timeout already on the register), not a defect in
the invariant itself.

Disposition: unowned red, no open ticket found. Per Article 4.2 this
withholds approval. Sent `note` (priority 00) to the specifier naming it;
BL-1555 is otherwise fully verified and waits on this ticket/adjudication
before I approve and land.

By QA.
