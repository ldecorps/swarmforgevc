# BL-1147 coder pass — 2026-08-26

## What shipped

- `extension/src/tools/probeLegacyTopicAdoption.ts` — pure read-only probe
  (`legacyPerTicketTopics`, cursor Host routing, scrub candidates).
- `extension/src/tools/probe-legacy-topic-adoption.ts` — thin CLI (exit 0 when
  readable; non-zero on missing target only).
- `extension/test/bl1147ProbeLegacyTopicAdoption.test.js` — probe shape,
  openSubjectAndRecord cursor re-adopt + bridge-owned refusal.
- `extension/test/bl1147ProbeLegacyTopicAdoption.property.test.js` — invariant
  1 (read-only) + routing classification property.
- `specs/pipeline/steps/bl1147ProbeLegacyTopicAdoptionSteps.js` — all 5
  scenarios; registered in `index.js`.
- `docs/how-to/BL-1147-probe-legacy-topic-adoption.md` + `docs/index.md` link.

## Verification

- `npm run compile` — green
- `node --test extension/test/bl1147ProbeLegacyTopicAdoption*.test.js` — 12/12
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1147-*.feature` — 8/8

## Invariants

1. Probe read-only — property test snapshots `.swarmforge/` before/after.
2. Cursor re-adopt — unit + acceptance scenarios 04/05 on real
   `openSubjectAndRecord`.
3. Scrub candidates — `computeScrubCandidates` mirrors
   `frontDeskTopicMapWithoutCursorBridge` without persisting (persist remains
   `readFrontDeskTopicMap` existing behavior).
