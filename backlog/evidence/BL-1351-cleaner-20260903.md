# BL-1351 — cleaner pass (2026-09-03)

## Scope

Diff (`a8b8b3610a`..`7164c655f6`) touches only files it declares:
`bridgeServer.ts` (11 lines — routes both snapshot producers through the
new single function), the new `streamSnapshot.ts` module, and the ticket's
own test/step/fixture files. No unrelated files touched.

## Consumer sweep (invariant 1) and design decision — verified by reading, not
## just cited

- Traced every named consumer in the coder's evidence against the actual
  source: `holisticUiHtml.ts` reads only `item.id`/`item.title` off
  `backlog.active` (plus `pipeline`/`agents`/`runLog`, none narrowed);
  `bubbleHostUiHtml.ts` filters to `event: host-activity` blocks only;
  the Telegram reply relay filters to `telegram-reply` records and gets
  its backlog prose from disk (`readBacklogFolders`), never the stream.
  Union: `{id, title}`, matching `STREAM_BACKLOG_ITEM_FIELDS`.
- The `/state` routes vs. stream projection question (explicitly flagged
  by the ticket as a design call the architect must rule on, not a side
  effect) is answered: the projection is applied only inside
  `resolveEventsSnapshot`/`broadcastSnapshotIfChanged`, never inside
  `buildBridgeState` itself — `/pipeline`, `/agents`, `/backlog`,
  `/runlog` keep full fidelity. Confirmed by reading `streamSnapshot.ts`'s
  `projectBridgeStateForStream` (takes a `BridgeState` as input, doesn't
  mutate `buildBridgeState`'s output in place) and `bridgeServer.ts`'s two
  call sites (both route through `buildStreamSnapshot`, not
  `buildBridgeState` directly for the stream).
- Invariant 2 (producers can't drift) holds by construction: exactly one
  function, `buildStreamSnapshot`, produces the frame string for both the
  connect path and the rebroadcast path.

## Verification (run, not assumed)

- `npx vitest run test/streamSnapshot.test.js` — 6/6 pass.
- `npx vitest run test/bridgeServer.test.js test/bridgeState.test.js`
  (the changed-path gate for `bridgeServer.ts`, named in the coder's
  evidence) — 108/108 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1351StreamSnapshotInvariants` — 15 consecutive green runs, no flake.
- `run_acceptance.sh specs/features/BL-1351-…feature` — 4/4 pass. First
  attempt stalled for 8+ minutes at 100%+ CPU with zero output on a
  heavily-loaded shared host (load avg 9.69, several concurrent role
  sessions' processes competing) — the fixture's `pollIntervalMs: 20`
  against a real bridge server rebuilding a 1223-item backlog snapshot
  from disk every 20ms is inherently CPU-heavy by design. Killed and
  re-ran once host load eased (down to 3.43): completed cleanly in
  ~4 seconds real work. Recorded so a future reviewer on a busy host
  doesn't mistake contention for a hang.

## Mutation-site advisory (BL-485)

`streamSnapshot.ts` (the new production file) is 11 sites — well within
threshold. `bridgeServer.ts` shows 1839 sites, but this ticket's diff to
it is 11 lines (an import and two call-site swaps); the count reflects the
whole pre-existing file, not something this parcel introduced — not
actionable here. Step/fixture/property/unit test files at 118–142 sites,
consistent with this session's range.

## Verdict

No defect. Forwarding.
