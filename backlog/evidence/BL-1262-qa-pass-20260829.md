# BL-1262 QA Pass — 20260829

## Commit under review
`115185ddc5` (merge of documenter's `89029bae77` into `swarmforge-QA`).
Ancestry confirmed: coder `494eb2b5c`, cleaner `5561827e3`, architect
`b2a071a1e`, hardener `75cfb3ffb`, documenter `89029bae77` are all ancestors
of this commit. Merge introduced no hunk loss (`git diff 89029bae77
115185ddc5 -- <touched paths>` empty for every file the merge touched).

## Prior QA bounce (this ticket, `26b28d9e3`) — re-verified fixed

- **D1** (property tests fail — integration points not restored): the
  architect subsequently bounced the coder's fix attempt as scope creep
  (adding call sites to `front_desk_supervisor.bb`/`handoffd.bb` is outside
  "restores the four files it names and nothing else"). The cleaner reverted
  those two files back to untouched (confirmed: `grep -n
  "self_heal\|append-self-heal-event"` on both returns nothing). The
  `.gitignore` half of D1 (missing `.swarmforge/telemetry/self-heal-*.jsonl`
  pattern) WAS in scope and IS fixed — `invariant3` now passes. The
  `invariant1`/`invariant1 property` reds (hosts loading the shared lib) are
  the specifier's explicit 2026-08-29 amendment: owned by BL-1273 (separate
  ticket, five emit sites dropped by a *different* merge, `2e37477ec`), not
  a defect in this parcel. **Confirmed still red, exactly as predicted, and
  nothing else**: `npx vitest run --config vitest.properties.config.mjs
  test/selfHealTelemetry.property.test.js` → 6/7 pass, only `invariant1
  property: every known host still loads the shared lib` fails.
- **D2** (acceptance scenario over-strict on "file unchanged"): step handler
  `specs/pipeline/steps/bl1262SelfHealTelemetryRestoredSteps.js` now checks
  that key assertion patterns (`aggregateSelfHealCounts`, `test(`, `assert`)
  survive in the two test files, rather than requiring byte-identical
  content — implements remediation Option A from my own bounce evidence.
  Confirmed fixed: full acceptance run is 7/7 green (was 6/7).

## This pass's verification (qa_e2e_procedure)

- Four named files present, byte-identical to pre-drop `8562094f8` (only
  mode-bit diffs, 644→755, harmless).
- `front_desk_supervisor.bb` / `handoffd.bb` unchanged — no self-heal
  references (scope constraint held).
- `required_wiring` anchors present: `aggregateSelfHealCounts` (line 57),
  `emitSelfHealEvent` (line 91), `self_heal_telemetry_cli.bb` load-files
  `self_heal_telemetry_lib.bb` (line 8).
- `cd extension && npx vitest run test/selfHealTelemetry.test.js` → 16/16
  pass (module resolves; was "Cannot find module" pre-restoration).
- `bb swarmforge/scripts/self_heal_telemetry_cli.bb` runs (usage banner,
  exit 0); `bb swarmforge/scripts/test/self_heal_telemetry_lib_test_runner.bb`
  → `ALL PASS`.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1262-self-heal-telemetry-files-are-restored-to-main.feature`
  → 7/7 pass.
- `cd extension && npx tsc --noEmit -p .` → clean, exit 0.
- Full unit suite (`npm test`): 37 files / 17 tests red, identical counts to
  the run taken moments earlier during BL-581's verification (before this
  merge) — `selfHealTelemetry.test.js` not in either failing list. Every
  failing file cross-checked against BL-1262's changed-file list, zero
  overlap; all match already-tracked classes corroborated the same day in
  `backlog/evidence/BL-1244-qa-pass-unrelated-reds-20260829.md` (BL-1220,
  BL-1221, whole-tree guard reds, and the previously-reported-untracked set).
- Full property suite: same standing-debt classes as BL-1244's
  corroboration, plus the one expected-and-named `invariant1` red above.
- Orphaned processes: none before or after (pgrep matches were self-match
  artifacts of the pgrep invocation itself, confirmed dead via `ps -p`).

## Observation — not a defect in this ticket, flagged for the record

`aggregateSelfHealCounts` and `emitSelfHealEvent` (TypeScript side) have no
caller anywhere in `extension/src` outside their own defining files —
neither is imported/invoked by production code. This predates the drop:
BL-597's own `required_wiring` (`backlog/done/M8/BL-597-*.yaml`) names the
aggregator's presence, not a TS-side caller — only the `.bb` emit site is
named as wired there. BL-1262's constraints explicitly forbid re-verifying
or widening beyond BL-597's original delivery and beyond the four named
files, so this is not this ticket's defect to fix, and no ticket currently
names it (grepped `backlog/{active,paused,hold}` for
`aggregateSelfHealCounts` — no hit besides BL-1262 itself). Leaving
disposition to the specifier rather than bouncing or minting, same posture
as BL-1244's untracked-reds report and the documenter's own spec-gap note
on this ticket.

## Verdict

**PASS.** Bounce D1/D2 both resolved (or explicitly re-scoped by the
specifier's amendment). No new defects in this ticket's own scope.

By QA.
