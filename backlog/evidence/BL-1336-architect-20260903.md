# BL-1336 — architect pass (2026-09-03)

## Merge

Merged cleaner `39064e85ba` (coder `49fca1c741`) into the architect worktree.
One trivial conflict in `specs/pipeline/steps/index.js` — both branches
added a different `require(...)` line; resolved by keeping both.

## Scope

Isolated the ticket's own commit (`49fca1c741`) from the routine main-sync
noise carried along in this merge (BL-1350 landing, BL-1327 closing,
BL-1355/1356 minting — all unrelated backlog bookkeeping from concurrent
tickets, not this parcel's work). The ticket's own diff touches exactly:
`vitest-worker-memory-budget.ts`, both vitest lane configs, `swarmforge.sh`'s
rotation export, the ticket's own step/unit/property test files, and one
documented incidental fix to `bl1323StampOffInvariants` (legitimate — see
below). No scope creep beyond what the coder and cleaner both declared.

## False alarm reproduced and cleared (not a coder defect)

My own `extension/out/` build was stale (predated the `.ts` edit that adds
`ROUTER_FORK_CEILING`), which made `bl1336RouterForkCeiling.test.js` fail
with `ROUTER_FORK_CEILING` reading `undefined`. `npm run compile` cleared it;
re-run 6/6 pass. This is the exact same false alarm the cleaner's own
evidence already documents catching — recorded here only so a later reviewer
with a stale `out/` doesn't mistake it for a regression.

## required_wiring confirmed live

- `swarmforge.sh::SWARMFORGE_ROTATION` — confirmed exported at
  `swarmforge.sh:2022` (`export SWARMFORGE_ROTATION='$(rotation_signal)'`),
  beside the existing `SWARMFORGE_PACK` export, from the same
  `rotation_signal()` helper `write_swarm_identity_file` now also calls — the
  identity file and the exported env read one normalizer, cannot disagree.
- `zsh -n swarmforge/scripts/swarmforge.sh` — clean (this is a zsh script;
  the file's own pre-existing `<->` glob syntax at line 351 is unrelated and
  not touched by this ticket).

## Architecture / constraints review

- Single composition point preserved (invariant 1): `resolveVitestForkCeiling`
  gained one `rotation` param and one branch; both `vitest.config.mjs` and
  `vitest.properties.config.mjs` read `process.env.SWARMFORGE_ROTATION` into
  the same `resolveVitestWorkerPool` call. No second route — confirmed by
  reading both lane files, not by trusting the property test's own claim.
- Keyed on rotation value, never pack name — `ROUTER_ROTATION` constant
  compared against `rotation`, no pack-name pattern matching anywhere in the
  diff.
- Full-forge-on-darwin ceiling of 1 checked first and unchanged, and the two
  branches are mutually exclusive by construction (a router pack is never
  named `full-forge`).
- RAM floor stays binding: `resolveVitestWorkerPool` still takes
  `min(ramAllows, ceiling)` via `resolveWorkerPoolSize` — raising the ceiling
  cannot widen the pool past what RAM allows (confirmed by reading the
  unchanged `resolveWorkerPoolSize` call site).
- Dependency-rule gate (BL-259), scoped and full-repo: **PASSED, no forbidden
  edges** both times.
- Co-change report (BL-255): all reported co-changes at frequency 1 — no
  suspected coupling.

## Invariants review (BL-633/654)

All three declared invariants have property tests in
`bl1336ForkCeilingInvariants.property.test.js`, re-run and non-vacuous
(rotation/pack/platform enumerated by the enclosing loops, not drawn — reach
guaranteed by construction, the same lesson BL-1352's D1 bounce taught).
Invariant 3 additionally asserts `ROUTER_FORK_CEILING > MAX_WORKERS`, so a
"raise" that changed nothing would fail it.

## Incidental fix (BL-1323 property test) — reviewed, legitimate

The row's lifecycle state legitimately moved `stamp-open` → `awaiting-human`
via an unrelated coordinator bookkeeping commit that landed on this branch
through routine main-sync (confirmed: `backlog/hotfix-ledger.yaml`'s diff for
this span is exactly that one state-field change, matching commit
`4fbbd1c03c`'s own message). The property test's fix strengthens the
assertion (checks `certified`/`waived` absent AND `human_decision`/
`decided_at` both null) rather than weakening it. Re-run: 3/3 pass.

## Independently re-run

- `npx vitest run bl1336RouterForkCeiling.test.js bridgeServer.test.js` —
  107/107 pass (after recompile).
- `npx vitest run --config vitest.properties.config.mjs
  bl1336ForkCeilingInvariants bl1323StampOffInvariants` — 6/6 pass.
- `run_acceptance.sh specs/features/BL-1336-router-rotation-raises-the-vitest-fork-ceiling.feature`
  — 6/6 pass.
- `npx vitest run vitestWorkerMemoryBudget.test.js` (31) and
  `--config vitest.properties.config.mjs vitestForkCeiling` (7) — BL-935's
  existing budget tests, both green, nothing broken by this change.

## Correctness read

No defect. Matches the human ruling (fixed ceiling of 10, memory budget
still binding, keyed on rotation not pack name). The QA e2e procedure's live
half (launch a real swarm and inspect the resident's env) is explicitly not
run here — correctly deferred, since `./swarm` kills and relaunches the swarm
and must not be run from an agent worktree; the static check (launcher emits
the export line) stands in for it and is verified above.

## Verdict

Clean sweep. No defect. Forwarding to hardener.
