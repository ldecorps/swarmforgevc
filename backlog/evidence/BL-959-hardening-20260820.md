# BL-959 — hardening pass, 2026-08-20

Parcel received from architect (`4b591053eb`, review pass 2) as the second item
of this session's batch, after BL-910. Task name
`BL-959-aps-candidate-toolchain-equivalence`.

## Degraded toolchain — recorded, not implied

BL-959 is **Babashka/Clojure**. Per engineering.prompt Startup Tools, this
project has **no mutation, CRAP, or DRY tooling wired for `.bb`** — the wired
gate is its own unit suite under `swarmforge/scripts/test/`. So: **Stryker,
`crapReport.js` and `jscpd` did NOT run against this ticket's code and no
figure from them is claimed.** In their place, per the BL-567 /
`expedite_mutation_sweep.sh` pattern, a hand-authored surgical mutation sweep
was run — each mutant a single edit a correct suite must reject.

The JS side of the parcel (one step-handler file) did go through the standing
whole-tree guards.

## Gates run

| Gate | Result |
|---|---|
| `aps_equivalence_lib_test_runner.bb` | ALL PASS, exit 0 |
| `aps_equivalence_lib_property_runner.bb` | ALL PROPERTIES HOLD, 500 runs each, exit 0 |
| Acceptance — `run_acceptance.sh` on the BL-959 feature | 3 scenarios pass / 0 fail, exit 0 |
| Gherkin mutation, surgical sweep over both `Examples:` columns | **4 killed / 0 survived** |
| Hand-authored `.bb` mutation sweep (10 mutants) | **9 killed / 1 equivalent / 0 real survivors** |
| Standing whole-tree guards (7 files) | 51 passed, exit 0 |
| Fixture-leak check (BL-921/BL-931) | **0 leaked dirs**, verified across the throwing path |
| Invariant 1 (pinned surfaces read-only) | verified — see below |

## Fixture leak: the hazard is closed, and I checked it the way that can fail

`bl959ApsEquivalenceSteps.js` creates a fixture dir with `mkdtempSync` and has
two validating `throw`s downstream of it — the BL-921 shape, and the exact
BL-931 recurrence shape (a `Given` validating a Scenario Outline cell, which
fires ONLY under a Gherkin mutant, so acceptance can never reveal it).

Rather than the per-throw `cleanup(ctx)` pattern, the parcel tracks roots and
reaps them in a `node:test` `afterEach`. That is a better mechanism **if it
actually fires** — which is precisely what a reading cannot tell you. Measured:

- `$TMPDIR/bl959-equivalence-*` before the acceptance run: **0**
- after a clean 3/3 acceptance run: **0**
- after four Gherkin mutants, two of which force the validating `throw`: **0**

So `afterEach` survives a mid-scenario throw here, and the leak is closed.

## Gherkin mutation — 4 killed, 0 survived

The BL-113 mutator was not run (host load; see BL-910's evidence for the same
condition). The surgical sweep mutated each `Examples:` cell and ran the REAL
acceptance against the mutated copy, requiring a non-zero exit:

```
killed  'a differing outcome' -> 'a differing outcomes'   (KNOWN_VALUES throw)
killed  'no recorded outcome' -> 'no recorded outcomes'   (KNOWN_VALUES throw)
killed  'DIVERGENT'           -> 'INCOMPLETE'
killed  'INCOMPLETE'          -> 'DIVERGENT'
```

## Hand-authored `.bb` mutation sweep

Run in an isolated sandbox copy; the tracked lib was never modified. Baseline
verified green in the sandbox first, so a "kill" is the mutation and not a
broken harness.

```
killed  M1  exit-code: drop the empty-matrix guard (fail OPEN on nothing compared)
killed  M2  exit-code: every? -> some
killed  M3  cell: drop the recorded-null coercion
killed  M4  verdict-matrix: name the wrong missing side
killed  M5  verdict-matrix: union -> pinned only (candidate-only entries vanish)
killed  M6  verdict-matrix: (= p c) -> (not= p c)
killed  M7  scrub-message: drop the repo-root prefix strip
killed  M8  normalize-lint-outcome: a PASS also carries its message
killed  M9  normalize-dry-findings: drop the canonical sort
EQUIV   M10 normalize-dry-findings: (get report "findings" []) -> (get report "findings")
```

### M10 is an equivalent mutant (BL-234), demonstrated from the code

`sort-by` returns `()` for nil, so `(vec (sort-by f nil))` and
`(vec (sort-by f []))` are the same value — the default is swallowed by the
very next call and no assertion at any call site could distinguish them.
Verified on the real runtime, not asserted:

```
(vec (sort-by str nil))  => []
(vec (sort-by str []))   => []
equal?                   => true
```

Recorded as equivalent with its code-level reason; no artificial test was
forced to kill it. (Note this is NOT the host-masked category of BL-927 — the
equivalence here is in the language semantics, not in this machine's
filesystem.)

## Gap found and closed: the CLI's matrix-file write was covered by NO gate

`aps_equivalence_cli.bb` ends by writing the verdict matrix to
`<work>/matrix.txt` and `<work>/matrix.md`. Nothing tested that it does:

- the acceptance step handler parses the CLI's **stdout only**
  (`matrixRows(ctx)` reads `ctx.result.stdout`);
- the lib runner tests `matrix-file-path` as a **pure string function**, never
  that anything calls it;
- the property runner never invokes the CLI at all.

Those files are the harness's own deliverable — what the evidence report for
the human's pin-bump decision is built from. **Proved, not assumed**: with the
`doseq ... spit` deleted, every pre-existing gate stayed green.

```
mutant applied (matrix-file spit removed)
PRE-EXISTING acceptance gate   rc=0   <- SURVIVED
pre-existing lib unit          rc=0   <- SURVIVED
pre-existing lib properties    rc=0   <- SURVIVED
NEW cli runner                 rc=1   <- kills it
```

(The tracked CLI was restored by an EXIT trap in the same command; `git status`
confirmed clean and the write is present.)

Closed by adding `swarmforge/scripts/test/aps_equivalence_cli_test_runner.bb`,
which covers the CLI's WIRING rather than restating the pure half:

- the usage guard exits **2** and explains itself, for three argument forms —
  no test asserted that non-zero exit before;
- an **empty work dir** exits non-zero and says INCOMPLETE — declared
  invariant 2 at the CLI boundary in its most extreme form, where nothing at
  all was compared, not merely at the lib boundary;
- both matrix files are written, and carry the **rendered matrix** — a
  truncating write would pass a bare `exists?` check, so the content is
  asserted (one `EQUIVALENT` line per entry × gate; the `.md` is markdown and
  is not byte-identical to the `.txt`);
- the files are refreshed on a **failing** run too.

Its temp dir is removed in a `finally`, never only after the last assertion.

Non-vacuity verified — the new runner kills all three mutants it exists for:
deleting the matrix-file write (N1), flipping the usage guard's `or` to `and`
(N2), and always exiting 0 (N3).

## Invariant 1 — pinned surfaces read-only

Verified two ways. Statically: `aps_equivalence_run.sh` references
`swarmforge.lock.json` and `swarmforge/vendor/aps/` **only** in existence
checks (`[[ -f ]]`, `[[ -d ]]`); its single `rm -rf` targets the throwaway
`$CANDIDATE_DIR` under an EXIT trap, and it refuses to run if the candidate
checkout is not the ticket's SHA. And the parcel already encodes the invariant
executably: the property runner's P2 quantifies write-path containment over
hostile entry names — `../../swarmforge/vendor/aps/…`,
`../../../../swarmforge.lock.json`, `specs/../../../../../upstream-watch.json`
— with its own recorded non-vacuity break. Nothing to add.

## Nothing forwarded as passing that did not run

Mutation/CRAP/DRY for `.bb`: not wired, not run, not claimed. BL-113 mutator:
not run (load), replaced by the surgical sweep above and recorded as such.
