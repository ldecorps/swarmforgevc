# BL-1381 — architect review, 2026-09-04

Reviewed coder commits (3695b7c7b1, 6f93401d03) merged in via cleaner
`822253f148`.

## Checks run

- Dependency gate on `specs/pipeline/steps/bl1381ShiftScheduleApplierSteps.js`:
  PASSED, no forbidden edges.
- Co-change on `install_shift_schedule_cron.sh`/`shift_schedule_applier_lib.bb`:
  expected hub coupling within the BL-1162 cron-lifecycle family; nothing new.

## Re-verified independently, not trusted from evidence

- `bb -e '(load-file "swarmforge/scripts/shift_schedule_applier_lib.bb")
  (println :ok)'` — prints `:ok`, exit 0 (qa_e2e step 1).
- `bb swarmforge/scripts/test/swarm_shift_lib_test_runner.bb` — ALL TESTS
  PASSED (qa_e2e step 2 — confirmed it's the load fix, not a relaxed
  assertion, by reading the new `budgetShiftGovernorVerdict` rows).
- `test_shift_schedule_applier.sh` — 9/9 including all five BL-1381-tagged
  rows (verdict-less exits non-zero, never reported as "no schedule
  configured", names its cause not a traceback, no traceback leaks,
  crontab byte-identical on failure).
- `run_acceptance.sh` on the BL-1381 feature — 6/6.
- `check_handler_module_graph.sh` — exit 0, unaffected by this parcel.

## Correctness read

Read `install_shift_schedule_cron.sh` end to end: every refusal path
(missing crontab/bb, reconcile exits non-zero, reconcile prints nothing,
reconcile prints non-JSON, no scheduling verdict) exits BEFORE the only two
crontab-writing lines (`crontab -r` / `crontab -`, near the bottom) — so
invariant 1 (byte-identical crontab on any failure) holds structurally, not
just by the fixture's own assertion. `set -euo pipefail` stays in force;
the new `if ! result="$(bb ... 2>&1)"` correctly captures the reconcile's
exit status without a bare command-substitution abort (the exact defect
being fixed). The `SHIFT_SCHEDULE_RECONCILE_BB` seam is a path override,
not a `*_FORCE_RESULT` bypass — correctly scoped per the constitution's
CLI-testability rule.

The coder's self-correction to the ticket's own item-3 description (no
input actually produces an exit-0 silent-success; the real gap was the
failure not naming its cause) is verified by re-reading the pre-fix code
path (`read` under `set -e` on a `<(python3 ...)` process substitution that
dies) — the correction is right, and the fix direction is unchanged by it,
exactly as the evidence claims.

## Riding along, not this parcel's concern

`check_handler_module_graph.sh` also carries a coder fix (`839873ab82`) for
a concurrency race cleaner found and routed by note (not reworked in
cleaner's own pass) — that's BL-1385's own follow-up, arrived via the
shared coder branch, already specifier-amended (invariant 3, scenario 07)
and coder-verified; not mine to re-review under BL-1381's name. Confirmed
it doesn't regress anything in THIS parcel's own checks above.

## Verdict

COMPLIANT. Forwarding to hardener.
