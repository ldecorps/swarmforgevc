# BL-1502 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Merge commit: `10c9c11c1e` (merged documenter's `44e11fb518` into QA branch).
- `node extension/out/tools/qa-sibling-check.js status --ticket BL-1502` -> `VERIFY BL-1502` (no open deferral).
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1502-the-bl1089-fixture-source-contract-follows-the-liveness-fixture HEAD` -> `OK` (required_wiring present).
- `npm test` (extension/) -> 616 files / 10479 tests passed. Straggler check before/after: no orphaned test/mutation processes.
- `npx vitest run --config vitest.properties.config.mjs test/bl1089FrontDeskLivenessFixture.property.test.js` -> 5/5 passed (run twice, stable).
- `npm run test:properties` (full suite, run twice sequentially):
  - Run 1: 4 files / 6 tests failed — all in `bl1343ReplayNeverDropsOwnPathInvariants.property.test.js` (20000ms timeouts) plus `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871 allowlisted noise).
  - Run 2: 2 files / 3 tests failed — `bl1297MergeOwnPathsInvariants.property.test.js` (invariant 3, timeout) and `bl1313BatchGuardVisibilityInvariants.property.test.js` invariants 1/2 (known standing red, `BL-1503`, already allowlisted in `swarmforge/scripts/property_suite_standing_allowlist.tsv`).
  - Different files failed each run under host contention (many unrelated node/vitest processes concurrently active) — re-ran `bl1297MergeOwnPathsInvariants.property.test.js` and `bl1343ReplayNeverDropsOwnPathInvariants.property.test.js` each in isolation: both green (3/3 and 2/2). Classed as environmental flakiness under load per BL-1063 (grepped `backlog/` — `bl1297`/`bl1343` name no open ticket; two runs producing two different failure sets is the "run it twice" evidence this is not deterministic), not a defect this parcel introduced or is responsible for.
  - `bl1313BatchGuardVisibilityInvariants.property.test.js` is BL-1503's already-registered, already-allowlisted standing red — unrelated to this ticket's file.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1502-the-bl1089-fixture-source-contract-follows-the-liveness-fixture.feature` -> 3/3 passed.
- `git diff main...HEAD --name-only` matches the ticket's `qa_e2e_procedure` step 2 exactly (property test, one helper under `extension/test/helpers/`, the step handler, evidence files, and the allowlist-row removal); `swarmforge/scripts/test/test_front_desk_supervisor_liveness.sh` untouched.
- Removed the now-stale `BL-1502` row from `backlog/standing-reds.tsv` (test verified green) — committed alongside this evidence.
- Wiring: `specs/pipeline/steps/index.js` auto-discovers handler files by `*Steps.js` suffix (BL-1371); no manual registration needed. Confirmed `bl1502FixtureSourceContractFollowsFixtureSteps.js` is discovered (pre_qa_gate OK above).
