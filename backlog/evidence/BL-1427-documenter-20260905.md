# BL-1427 documenter review pass — 2026-09-05

Reviewed the hardened commit (`eaa059d584`) and the coder/cleaner/architect/
hardener trail for BL-1427 (the load guard covers every script and runs
none).

Checklist:
- Ticket-named acceptance feature exists (implied by the required_wiring
  step handler's own existence per the ticket's anchor):
  `specs/pipeline/steps/bl1427LoadGuardCoversEveryScriptSteps.js`.
- `required_wiring` `</dev/null` anchor present:
  `swarmforge/scripts/check_bb_scripts_load.sh` line ~108 redirects the
  probe's stdin from `/dev/null`.
- Read `bb_load_analyse_driver.bb` and the updated
  `check_bb_scripts_load.sh`: confirmed invariant 1 (the loop's `listed`
  count is captured into an array up front and compared against
  `analysed`, refusing on any gap), invariant 2 (the driver reads every
  top-level form but skips EVALUATING an entry-call form — bare `-main`,
  `-main` with `*command-line-args*`, or `(apply -main ...)` — while every
  `def`/`defn` is still evaluated eagerly), and invariant 3 (a reader
  error or an eval failure inside any def/defn's own body still
  propagates uncaught, so babashka's own error banner is unchanged).
- Agent/operator-visible behavior change (the pre-commit and land-time
  Babashka load guard now actually analyses every listed `.bb` script
  instead of silently stopping after a script that drains the loop's
  stdin, and no longer refuses a fixed-arity CLI for an `ArityException`
  it never hits when actually invoked with arguments) is documented in
  `docs/how-to/BL-1252-commit-guard-chain-reports-every-violation.md` —
  the existing commit-guard-chain how-to, which already described the
  BL-1395 load guard this ticket fixes — with a new "The probe covers
  every listed script and runs none of them (BL-1427)" section, an
  updated probe description at the top of the BL-1395 section, updated
  Related and Verify sections.
- `docs/index.md`'s one-line summary for that how-to extended with a
  BL-1427 clause in the same style.
- No "Last Updated" freshness field in either edited doc — no date bump
  owed.
- No registered diagram depicts this guard's internal probe mechanics (a
  bugfix inside an existing guard's implementation, not a new component,
  boundary, or pipeline-topology change) — no diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
