# BL-1593 — hardender review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Processed as part of a 4-item batch (BL-1573, BL-1511, BL-1512, BL-1593).

- `npm run compile` then `npx vitest run` over
  `test/tmpDirMigrationGuard.test.js`, `test/liveRepoDerivationGuard.test.js`
  and `test/nightClosingCeremonyRotateDocumenterFallback.test.js` (plus
  the rest of the `*Guard*.test.js` set): all green, 186/186 - confirms
  the two guards this ticket targets are the ones that flipped.
- `specs/features/BL-1593-*.feature` acceptance: 5/5 scenarios pass.
- `grep BL-1038-EXEMPT` on the fallback test: no match.
- `bb swarmforge/scripts/standing_red_register_cli.bb .`: no row for
  `bl1280MkdtempMigrationInvariants.property.test.js`,
  `tmpDirMigrationGuard.test.js` or `liveRepoDerivationGuard.test.js`,
  `"unowned":[]`.
- `/tmp` carries ~21 pre-existing `ncc-rotate-fallback-*` dirs from BEFORE
  this fix landed (timestamps 07:56-12:36, my own run at 13:51 added none)
  - litter from the raw-mkdtempSync bug this ticket fixes, not created by
    this pass; left alone per "never delete what you did not create".
