# BL-1682 — coder pass, 2026-09-24

## What landed

- Applied the operator's own draft patch verbatim
  (`backlog/evidence/BL-1682-operator-draft-local-seat-briefing-20260921.patch`,
  `git apply --check` clean, applied as-is - no rewrite of the seam shape
  or the briefing text, per the ticket's own FIRM clause):
  - `extension/src/tools/localQwenSeatLive.ts`: `localSeatBriefingPath`,
    `readLocalSeatSystemPrompt` (trimmed file text or `undefined` - a
    missing/empty/whitespace-only briefing is a working state, not an
    error); `completeWithLocalModel` gains an optional fifth `system`
    parameter forwarded into the request body (`JSON.stringify` drops it
    when `undefined`); `runLocalSeatTurn` reads
    `readLocalSeatSystemPrompt(deps.targetPath)` once per turn and passes
    it to `complete` as a fourth argument, never prefixed onto `prompt`.
  - `extension/test/bl1235LocalQwenSeatLive.test.js`: the patch's own five
    unit tests (briefing-present / briefing-absent, at both the
    `readLocalSeatSystemPrompt` and the `completeWithLocalModel` /
    `runLocalSeatTurn` levels).
  - `docs/reference/local-model-briefing.md` (new): the operator's 45-line
    briefing text, landed byte-for-byte from the patch.
- **`specs/pipeline/steps/bl1682LocalSeatBriefingSystemPromptSteps.js`**
  (new): drives the REAL `runLocalSeatTurn` over a REAL mkdtemp scratch
  target (`mkProcessTmpDir`, so `readLocalSeatSystemPrompt` does a real
  `fs.readFileSync`/trim - never a reimplementation of that logic in the
  handler) with a fake `complete`/`readEndpoint` seam, the same injection
  shape BL-1235's/BL-1680's own tests already use.
  `localQwenSeatLive`/`localQwenSeat` are lightweight leaf modules (no
  `@cursor/sdk` transitive edge, unlike `telegramCursorBridgeLive`) so
  they're required directly at module scope, matching BL-1680's own
  handler rather than the lazy-require pattern BL-1658/BL-1718 use for the
  heavy bridge modules.
- **`extension/test/bl1682LocalSeatBriefingSystemPrompt.property.test.js`**
  (new, BL-654 - see Invariants below).
- **`docs/index.md`**: one link line for the new
  `docs/reference/local-model-briefing.md` page under "## Reference" -
  required by `docsStructureRealTree.test.js` (BL-757's real-tree orphan
  check), which failed on the first full unit-lane run naming exactly this
  new file as a non-allowlisted orphan. This is a structural index link,
  not a rewrite of the briefing's own text - the ticket's FIRM clause is
  about the briefing CONTENT, and the operator's 45 lines are unchanged
  from the patch.

## Discovered while wiring

1. **`out/` is stale until compiled.** The patch's own five unit tests
   `require('../out/tools/localQwenSeatLive')` (the compiled JS, not the
   `src/` TS), so they failed with `readLocalSeatSystemPrompt is not a
   function` until `npm run compile` ran. Not a defect in the patch -
   `out/` is gitignored and this worktree's copy predates the patch.
2. **A new `docs/reference/*.md` file needs an index link.** Missed on
   the first full-unit-lane run: `docsStructureRealTree.test.js` (BL-757)
   failed naming `reference/local-model-briefing.md` as a non-allowlisted
   orphan. Fixed with one link line in `docs/index.md`'s Reference
   section (see above) - a mechanical, structural requirement every other
   reference doc in that list already satisfies, not additional prose
   about the briefing's own content.
3. **A transient host-load suite-budget flake, not caused by this
   ticket.** One `npm test` run (of three) flagged
   `bl1652HandoffdRespawnReadingsWiring.test.js` as a new suite-budget
   pole (11.2s in-suite); a run immediately before and after both passed
   it clean (registered-pole-only, then not flagged at all). This ticket's
   diff touches none of that file's dependencies. Consistent with this
   session's own documented host-load-timing flake class (concurrent
   swarm pipeline activity on this machine); not reported as a fresh
   unowned red per Article 4.2 since it did not reproduce on immediate
   re-run (contrast BL-849's own red, reported separately during BL-1705,
   which reproduced identically on every attempt).

## Verification

| check | result |
|---|---|
| `git apply --check` on the operator's patch | clean |
| `npm run compile` | clean |
| `npx vitest run test/bl1235LocalQwenSeatLive.test.js` | 20/20 (15 pre-existing + 5 from the patch) |
| `specs/pipeline/scripts/run_acceptance.sh` on this ticket's feature | 3/3, twice in a row |
| `npx vitest run --config vitest.properties.config.mjs test/bl1682LocalSeatBriefingSystemPrompt.property.test.js` | 2/2 |
| Non-vacuity: made `completeWithLocalModel` default a missing `system` to `''` instead of leaving it `undefined` | property test immediately failed (`"system":""` appearing when it must be absent); reverted, diff matches the operator's patch exactly, re-verified green |
| `npx vitest run test/docsStructureRealTree.test.js` | 5/5 after the `docs/index.md` link |
| `npx vitest run test/stepHandlerTmpRootGuard.test.js test/stepHandlerModuleLoadBudget.test.js` | both green - the new handler needs neither (no direct `mkdtempSync(`, no eager heavy require) |
| `npm test` (compile + unit lane) | 636 files / 10861 tests, all green (3rd of 3 attempts exit 0; the other two failed only on the pre-existing host-load suite-budget guard, on files unrelated to this diff — see "Discovered" #3) |

## Invariants (BL-654)

1. **"A local-seat turn's ollama request differs from today's request
   only by the presence of the `system` field, and that field is present
   only when a non-empty docs/reference/local-model-briefing.md exists
   under the target path."** Two pure facts, both exhaustively constructed
   (never fc-sampled - a handful of discrete states, this session's own
   precedent for a space this small):
   - `readLocalSeatSystemPrompt` is defined iff the briefing exists and is
     non-empty after trim - 4 real mkdtemp filesystem states (absent,
     empty file, whitespace-only file, real untrimmed text).
   - `completeWithLocalModel`'s constructed JSON body carries `system` iff
     the argument is defined, and every OTHER key (`model`, `prompt`,
     `stream`) is byte-identical whether or not `system` is present - 2
     system values × 3 prompt values, diffed against the
     system-`undefined` baseline for the same prompt.
   Non-vacuity: defaulted a missing `system` to `''` in
   `completeWithLocalModel` - the property immediately failed, naming the
   exact violating body; reverted (diff empty against the operator's
   patch), re-verified green (see Verification table).

## required_wiring

- `specs/pipeline/steps/bl1682LocalSeatBriefingSystemPromptSteps.js::registerSteps`
  - present, discovered by directory scan (BL-1371, no manual
    `specs/pipeline/steps/index.js` edit needed - that file has been
    discovery-based since BL-1371, the ticket's own text predates that
    migration); confirmed by the acceptance run passing 3/3.

## Out of scope (per the ticket's own text, not acted on here)

- The local-seat how-to's own "briefing file" section - the ticket
  assigns this to the documenter explicitly.
- The resolver, the endpoint probe, the topic map, the front desk - all
  untouched; the diff is exactly the patch's own two source files plus
  the new briefing doc, its index link, the new step handler, and the new
  property test.

By coder.
