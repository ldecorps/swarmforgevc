# BL-670 QA pass — 2026-08-30

Commit verified: merge of documenter `567b11fef0` into QA (`bb1ff4931`).
BL-670's own final coder/cleaner/architect/hardener/documenter commits
(48514b052 the D1 refix, dbc716313, 06b45d1b1, 567b11fef0) are all confirmed
ancestors of the merge HEAD. Merge conflict on `docs/reference/Specification.MD`
and `specs/pipeline/steps/index.js` (both also touched by my already-landed
BL-1243 rebuild) — resolved by keeping both sides; diffed the merge against
both parents afterward, nothing dropped from either side.

Bounce history checked against `main`: one prior architect bounce (D1: a
standing `BL-464` test in `state.test.js` still asserted the pre-BL-670
bare-role return shape). Confirmed fixed — read the corrected test directly
(now asserts the qualified `{stage, status}` shape with an explanatory
comment), not merely trusted from the bounce record.

## Dependency — confirmed landed
`depends_on: [BL-585]` — present in `backlog/done/M8/`.

## required_wiring — all four confirmed
- `pipeline_stage_cli.bb`: `scanned-mailbox-states` now includes `:sent`.
- `pipeline_stage_lib.bb`: defines `in-transit-status`.
- `swarmState.ts`: defines `TICKET_STAGE_STATUS_LAST_KNOWN` / `TicketStageEntry`.
- `specs/pipeline/steps/index.js` registers `bl670PipelineBoardLastKnownStageSteps`.

## Tests run
- `extension/test/bl670TicketStageQualifier.test.js` + `state.test.js`
  (the fixed BL-464 test included): 50/50.
- `extension/test/bl670StageQualifierInvariants.property.test.js`: 3/3
  (invariants 2 and 3, TS side).
- `swarmforge/scripts/test/bl670_stage_qualifier_property_runner.bb`
  (invariant 1, bb side): ALL PASS, 500 runs/invariant, non-vacuous
  coverage across every named branch (last-known, in-transit-to, claimed,
  multi, collision, ordered, has-live).
- `swarmforge/scripts/test/pipeline_stage_qualifier_test_runner.bb`: ALL PASS.
- `swarmforge/scripts/test/test_pipeline_stage_cli.sh`: 20/20 checks,
  including every pre-existing BL-1048 regression scenario still green.
- `specs/features/BL-670-pipeline-board-last-known-stage-and-health.feature`:
  9/9.
- qa_e2e_procedure item 7 (BL-585 layout regression): `test/pipelineBoard.test.js`
  136/136 — layout untouched.
- Cross-lane sibling check (matching the architect's own): `bl670StageQualifierInvariants`
  + `bl1048DeliveredParcelIsNotNotStarted` + `bl1188PipelineGridLiveStageParityInvariants`
  property tests together: 7/7 — no disagreement between the board and the
  live grid's own derivation.

## Mirrored-constant rule (BL-897)
`bl670TicketStageQualifier.test.js`'s own "status and dot literals agree
across the language boundary" tests (6/6) read the literals out of the real
`pipeline_stage_lib.bb` source and assert them equal to the TS exports —
confirmed passing.

## Standing baseline
Hardener's own full-suite re-run reports the same pre-existing, unrelated
26-failed-file/218-failed-test baseline I have independently measured
multiple times today. Not this ticket's defect.

## Verdict: APPROVE

By QA.
