# BL-1367 — QA pass, 2026-09-03

## Received

Documenter's commit `14553d1da7` (task
`BL-1367-an-approval-from-any-surface-carries-its-ruling`), merged into
this worktree (`091da409cc`).

## Verification

- `npm run compile` — clean.
- Unit suite (`vitest run`): 25 failures across 15 files, byte-identical to
  the pre-existing standing-debt baseline — no regression.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1367-an-approval-from-any-surface-carries-its-ruling.feature`
  — 4/4 scenarios pass.
- `vitest run --config vitest.properties.config.mjs
  bl1367ApprovalCarriesItsRuling` — 2/2 pass.
- `required_wiring` both confirmed live:
  `computePausedPagerApproveOutcome` (`bridgeServer.ts:861`) now takes and
  threads a `ruling` argument (called with `value.ruling` at line 923) —
  the pre-existing route this parcel changes, not a fresh symbol.
  `bl1367ApprovalCarriesItsRulingSteps` required at
  `specs/pipeline/steps/index.js:960`.
- **Live sweep (qa_e2e_procedure step 5)**: scanned every
  `backlog/active/*.yaml` and `backlog/paused/*.yaml` for
  `ruling_options:` + `human_approval: approved` + no `human_ruling:` —
  **empty set**. Invariant 1 holds against the live backlog, not only the
  fixture.
- `bounce_history`: none.
- `human_approval: approved`, no `ruling_options` posed (ticket explicitly
  notes this is deliberate — the ticket about dropped rulings must not
  itself model the failure) — consistent, no `human_ruling:` expected.

## Verdict

PASS. Approving BL-1367's own work.

By QA.
