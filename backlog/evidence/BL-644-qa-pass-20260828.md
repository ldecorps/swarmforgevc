# BL-644 — QA pass — 20260828

**Received:** `merge_and_process documenter 4069dfee37` (handoff
`00_20260828T001810Z_000859_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `4069dfee3718e0a7568ff1290941b3e637b546ef`
**QA merge commit:** `ecb209224aa473698554aec439cc79f2f0a2a961`
**Landed commit (fast-forward push, no rematch needed):** `ecb209224aa473698554aec439cc79f2f0a2a961`

## Ancestry check (BL-336 guard)

`git merge-base --is-ancestor 4069dfee3718e0a7568ff1290941b3e637b546ef ecb209224` — CONFIRMED.
BL-644 has `required_stages: [documenter, qa]` (coder/cleaner/architect/hardener
explicitly skipped per `stage_skip_reasons` — prose-only how-to guide, no
production code), so the documenter's own tip is the whole chain here; no
earlier stage merges to check.

## Branch-divergence found and resolved during merge

The documenter's branch last synced `main` at `59039688f4` — **before** the
`f8a41c1e2` "Retire BL-1188/1189/592/644/751/1196/1200" commit (see
[[coordinator-false-identical-content-claim-backlog-retire]]) and its
correction `fbfa44148`. On the documenter's own tip, BL-644's ticket file
still lived at `backlog/active/BL-644-*.yaml`; on QA's side (current `main`
lineage) it lives at `backlog/hold/BL-644-*.yaml` — moved there by `f8a41c1e2`
and never restored (that commit's "confirmed identical content" claim was
**true** for BL-644 specifically, unlike BL-592/1188/1189, so no content was
lost — only the pool changed).

`git merge --no-ff --no-commit 4069dfee37` resolved this cleanly via rename
detection: the documenter's `acceptance:` field addition (pointing at the new
`specs/features/BL-644-*.feature.draft`) landed on the **current**
`backlog/hold/BL-644-*.yaml`, not a resurrected `backlog/active/` copy. Verified
by diff before committing: only the intended fields changed, `backlog/active/`
stayed absent, no other ticket content touched. One incidental file also
merged cleanly: `backlog/evidence/BL-1184-documenter-pass-20260827.md` (the
documenter's own already-delivered evidence for a separate, already-forwarded
ticket — inherited via normal branch-merge lineage, not stapled in).

**BL-644's ticket file is still in `backlog/hold/`, not `backlog/active/`.**
Per Article 3.1, only a human can move an item out of `hold/` — QA does not
have that authority and did not attempt it. Flagged separately to
coordinator+specifier (priority 00) rather than assumed away. See disposition
note below.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-644` | `VERIFY BL-644` — no open deferral |
| Doc content | manual read, `docs/how-to/BL-644-what-not-to-do-when-tweaking-the-swarm.md` | All 18 anti-patterns present, grouped by what they break (not chronological), each carries concrete evidence (command/measurement/ticket id) |
| Cross-links | manual read | Bidirectional link with `docs/explanation/lessons-2026-07-25-green-suites-that-proved-nothing.md` ("this page is prohibitions, that page is reasoning"); linked from `docs/index.md` under How-to |
| Quoted paths verified | spot-checked against repo at HEAD | `swarmforge/scripts/kill_all_swarm.sh`, `kill_pipeline_swarm.sh`, `swarmforge/vendor/aps/bb/src/aps/mutation.clj`, `./stop-swarm.sh` — all exist at the paths quoted |
| Acceptance | N/A — deliberately parked | `acceptance:` points at `specs/features/BL-644-*.feature.draft` (not `.feature`), permanently outside the `*.feature` glob per BL-233, with a zero-scenario placeholder and explanatory header. Matches the established `verification-only-chore-carries-no-executable-gate` convention (prose acceptance judged by the documenter, not an automated scenario). Nothing to run via `run_acceptance.sh`. |
| Wiring / invariants | ticket YAML | Neither `required_wiring:` nor `invariants:` declared — doc-only ticket, none applicable |
| Compile | `npm run compile` (extension/) | PASS, clean (implied by `npm test` below) |
| Unit suite | `npm test` (extension/) | 46 files / 202 tests fail, **all** `CURSOR_API_KEY is not set for the headless bridge` — standing BL-720/BL-728 host-env debt, identical disposition to BL-1184's QA pass one commit prior (`backlog/evidence/BL-1184-qa-pass-20260827.md`). None of the 46 failing files touch anything under `docs/` or BL-644's own paths — this ticket changed zero production code, so Article 4.5's changed-path mapping is vacuous here. |
| Property suite | `npm run test:properties` (extension/) | 26 files / 17 tests fail; all 26 match `swarmforge/scripts/property_suite_standing_allowlist.tsv` verbatim (27-entry standing allowlist tracked under BL-1175 — the 27th entry, `bl593MutationRunTelemetry.property.test.js`, happened to pass this run, consistent with known flakiness in that set). 1 `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled error — the known-benign allowlisted artifact (BL-871). No new/unallowlisted failures. |
| Orphan process check | `pgrep -fl 'node --test\|stryker'` before, `pgrep -fl 'vitest\|stryker'` after | Both hits were self-match noise (already-exited pids by the time `/proc/<pid>/cmdline` was read) — no real orphaned test processes at either checkpoint |

## Inventory (Article 4.4)

NONE.

## Caution logged for the record

Ran the full, unscoped `npm run test:properties` to satisfy the property-suite
gate. [[property-suite-full-run-hijacks-role-branch-refs]] warns this can
overwrite a live role branch's ref with fixture commits (four incidents,
two branches, 2026-08-27). Checked all role branch tips + file counts
immediately after (read-only, per that memory's own recommended check): none
read `seed`, all file counts were in the expected ~9800-9900 range. No damage
this run — but the safer path next time is scoping to the single relevant
file; noting this here rather than silently repeating it.

## Verdict

**PASS** — documentation deliverable verified complete and correct; all
gates run-or-recorded-standing per Article 4.4 (none blocked, none skipped).

Landed on `main`: acquired `.swarmforge/land-main.publish.lock`,
`land_main_publish.sh --decide-only` reported `tip-contains-origin: true`,
`purity-action: push` (origin had not advanced past the base QA's branch was
already on — no rematch needed), `git push origin HEAD:main`
(`9fcce267a..ecb209224`), released the lock.

BL-644 carries no `GH-` id (`source:` is prose, not an issue URL) — no
`issue_done.sh` step required.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping **with an explicit
flag**: BL-644's ticket file sits in `backlog/hold/`, not `backlog/active/`,
so the normal active→done bookkeeping move does not apply as-is — human/
specifier adjudication needed on final disposition (direct hold→done now
that the work is genuinely complete, vs. restore-to-active first). QA did not
move the ticket file itself.

By QA.
