# BL-1469 — hardender review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second hardening pass, after the coder's fix for the first pass's bounce
(D1, `backlog/evidence/BL-1469-bounce-20260907.md`: `handoffd.bb`'s
`open-slot-nudge-sweep!` never threaded `:today` into the evaluate-ctx
it builds for `chase-sweep-lib/nudge-eligible-candidates`, so a
`not_before`-gated candidate was silently invisible to the nudge's gate
chain).

- Coder's fix (`ccbec7cb71`): threads `:today` at the one call site
  (`handoffd.bb` ~line 2094), resolved once per sweep, matching the
  file's own `briefing-email-sweep!` `:today-str` precedent and
  `promotion_gates_cli.bb`'s `today-utc` - exactly the remediation
  direction the bounce evidence proposed. `chase_sweep_lib.bb` itself
  needed no change.
- `bl963_nudge_gate_chain_property_runner.bb` gains a dedicated
  addendum: a future-`not_before` candidate ranked BEST by construction
  (the false-escalation shape), asserted absent from
  `nudge-eligible-candidates`, never named as top candidate, never
  accruing BL-798 escalation state across three decide ticks - all three
  surfaces Invariant 1 already checks for every other gate. 40 runs,
  floor 5 reached (`bl1469-not-before=40`).
- Re-ran: `promotion_gates_lib_test_runner.bb` (ALL PASS),
  `bl1469_not_before_promotion_gate_property_runner.bb` (60 runs each,
  ALL PROPERTIES HOLD), `land_step_lib_test_runner.bb` (ALL PASS, no
  interaction), `bl963_nudge_gate_chain_property_runner.bb` (ALL
  PROPERTIES HOLD, including the new addendum), and the BL-1469
  acceptance feature (7/7).
- Coverage boundary noted, not bounced: no test exercises
  `open-slot-nudge-sweep!` itself (the function that builds the
  evaluate-ctx at the call site) - it reads several top-level `def`'d
  globals (`project-root`, `backlog-active-dir`, etc.) rather than
  taking injectable parameters, the same architecture every other sweep
  in `handoffd.bb` shares (`briefing-email-sweep!` included), so this is
  pre-existing codebase-wide testability debt, not something this
  ticket's own scope introduced or can reasonably be asked to fix.
  The new property addendum verifies the underlying gate logic
  (`nudge-eligible-candidates` correctly excludes a `not_before`
  candidate when `:today` is present); the literal wiring at the call
  site is confirmed by direct read/grep, not by an automated test. If
  that one line were later silently removed, nothing would catch it -
  worth naming for whoever next touches sweep-function testability in
  this file, not a blocker for this ticket.
- Host quiet throughout (load avg ~1.88-3.01 on 20 cores).
  `handoffd.bb` reads `skip-cooldown` (authored today) - no fresh
  mutation pass; no wired tool applies to `.bb` files regardless.
- No stray test/mutation processes or fixture directories from this
  pass's own runs.
