# BL-1436 — cleaner pass, 2026-09-05

NONE — no defect found. The new pricing entry, the honest-null widening,
and the register row removal are all correct and independently verified.

## What was checked

- Read the `pricingTable.ts` diff directly: the new `claude-fable-5-1` row
  ($10 input / $50 output / $0.25 cache-read, cache-creation deliberately
  left unset) matches the ticket's own approved rates exactly, with a
  comment naming the source and cache date. `Rates.cacheCreatePerMTok`
  correctly becomes optional; `costFrom`'s rewrite is a per-category loop
  that skips a zero-token category regardless of rate knowledge (matching
  every pre-existing row's own numeric behavior exactly — traced by hand:
  a defined rate times zero tokens is 0 either way) and returns `null` for
  the whole estimate only when a NONZERO-token category has no known rate
  — confirmed `estimateCostUsdAt`'s pre-existing `rates ? costFrom(...) :
  null` ternary already returns `number | null`, so the new `null` return
  from `costFrom` propagates through unchanged code, not a new type hole.
- Live-verified `estimateCostUsd` against the REAL COMPILED table
  directly (not trusting the coder's claimed numbers): input 10, output
  50, cache-read 0.25, cache-creation `null` — exact match to
  qa_e2e_procedure step 3 and the coder's own non-vacuity proof,
  reproduced independently from scratch.
- Confirmed `modelDisplayName.ts` gained the `'claude-fable-5-1': 'Fable
  5.1'` entry directly.
- Confirmed `grep -c pricingTable backlog/standing-reds.tsv` is 0 —
  invariant 3 holds.
- `npm run compile` clean.
- `npx vitest run test/pricingTable.test.js`: 30/30 (was 29/30).
- `npx vitest run --config vitest.properties.config.mjs
  test/pricingWindows.property.test.js`: 3/3, unaffected.
- acceptance `BL-1436-...feature`: 6/6.
- acceptance `BL-627-...feature` (regression): 6/6, unaffected.
- `npx jscpd` on the new step handler: 0 clones.
- Mutation-site count: `pricingTable.ts` 157 (over threshold), but this
  is a pre-existing 266-line cohesive data-table-plus-pure-functions
  module this ticket extended by only ~40 lines (one new data row, one
  optional-field widening) — not created oversized by this diff, and
  splitting a pricing-rate table plus its cost-estimation functions
  would not improve structure. `modelDisplayName.ts` (24) is within
  threshold.

## Verification (all re-run in this worktree, all green, matching every claimed number)

| check | result |
|---|---|
| `npm run compile` | clean |
| `npx vitest run test/pricingTable.test.js` | 30/30 (was 29/30) |
| `npx vitest run --config vitest.properties.config.mjs test/pricingWindows.property.test.js` | 3/3, unaffected |
| acceptance `BL-1436-...feature` | 6/6 |
| acceptance `BL-627-...feature` (regression) | 6/6, unaffected |
| Live `estimateCostUsd(...,'claude-fable-5-1')` per category (reproduced independently against the real compiled table) | input 10, output 50, cache-read 0.25, cache-creation null |
| `grep -c pricingTable backlog/standing-reds.tsv` | 0 |
| `grep -n claude-fable-5-1 modelDisplayName.ts` | present |
| `npx jscpd` on the new step handler | 0 clones |

No fix required. Forwarding unchanged to architect.
