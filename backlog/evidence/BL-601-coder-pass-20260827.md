# BL-601 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Delivered

Pure `compactionCadence.ts` derives compaction records from structured
context-events (`compaction:true` only — pane spinner text ignored), aggregates
compactions/hr and token-at-compaction distribution per role/window, and
exports `trendForCompactionCadencePerHour` via `trend.ts`.

Append-only `compactionTelemetryStore.ts` writes derived records to
`.swarmforge/telemetry/compaction-YYYY-MM.jsonl` (gitignored).

## Invariants (BL-654)

| invariant | encoding |
|---|---|
| Spinner strings alone never emit records | `deriveCompactionRecords` + acceptance scenario 04 + property P4 |
| Undetectable roles read NA, not fabricated zero | `queryCompactionCadenceForRole` + acceptance scenario 03 + property P3 |
| Pure aggregation over records | acceptance scenario 02 (no fs in `compactionCadence.ts`) |
| Measurement only | no threshold tuning or compaction behaviour changes |

## required_wiring

| anchor | status |
|---|---|
| `context-events.jsonl` / `context_telemetry_lib.bb` | source signal (acceptance uses fixture events) |
| `compaction-YYYY-MM.jsonl` | `compactionTelemetryStore.ts` |
| `compactionCadence.ts` | pure aggregator |
| `trend.ts` | `trendForCompactionCadencePerHour` re-export |
| `bl601TrendCompactionCadenceSteps` | registered in `index.js` |

## Verification

| check | result |
|---|---|
| `compactionCadence.test.js` | 6/6 |
| `compactionCadence.property.test.js` | 4/4 |
| `run_acceptance.sh BL-601…feature` | **8/8** |

By coder.
