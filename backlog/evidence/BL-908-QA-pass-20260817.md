# BL-908 — QA PASS: the hold is resolved, independent verification is green

**Parcel verified:** documenter's redo `85fd2bdbd` / merge `e40a462f02`
(`BL-908-bubble-knowledge-screen-backlog-docs-panels-sync-btn-unreachable`),
confirmed an ancestor of this commit
(`git merge-base --is-ancestor e40a462f02 HEAD`).

## Why the hold is now resolved

`backlog/evidence/BL-908-held-pending-BL-825-gates-qa-20260817.md` held this
parcel because BL-825's code was riding into `main` unreviewed. That is no
longer true: BL-825 has since been routed through its own full
`required_stages` as its own parcel and landed independently —
`747db5c76` (architect, clean), `0db3fe77d` (hardener, CRAP splits +
coverage), `6680effba` (documenter), `7120d9524` (QA pass, approved),
`951b2396b` (coordinator: closed to `done/M8`) — all confirmed ancestors of
this commit. `backlog/active/BL-825-*.yaml` no longer exists in this
worktree; it is at `backlog/done/M8/BL-825-bubble-remote-ui-bundle-resolution.yaml`
after the `main` merge just performed. The entanglement this ticket was held
for has been independently gated and closed on its own merits, not folded
into BL-908's approval.

## Independent verification run this pass

- **JVM unit suite** (`JAVA_HOME=$(brew --prefix openjdk@17)
  ./gradlew :app:testDebugUnitTest --rerun-tasks`): BUILD SUCCESSFUL, 187
  tests, 0 failures/errors/skipped repo-wide, including all 8
  `KnowledgeReader*Test`/`*PropertyTest` classes (25 tests) BL-908 added.
- **Extension unit suite** (`npm test`): 7729/7733 passed. 4 failures, all
  pre-existing and unrelated to BL-908
  (`activateBounceWatcher.test.js`, `bounceDrain.test.js`,
  `bounceWatcher.test.js`, `renderBriefingDiagramsCli.test.js` —
  none touched by any BL-908/825/829 commit, confirmed by
  `git log --grep="BL-908" -- <those paths>` returning nothing). Root-caused
  to this host: a standalone repro (`fs.watch` on a fresh `os.tmpdir()`
  dir, no extension code involved) never receives an event, with or
  without sandbox, at both load ~11 and load ~89 — a persistent
  environment limitation of this session, not a code regression. Isolated
  re-run confirms exactly the same 3 `fs.watch`-dependent tests fail
  every time; `renderBriefingDiagramsCli` passed once isolated from
  whole-suite contention. Not bounced: no defect in BL-908's own scope,
  same principle as the BL-825 surfacing (BL-506) — surfaced here, not
  silently waived.
- **Property tests** (`npm run test:properties`): 97/97 files, 304/304
  tests passed, exit 0. 4 `[vitest-worker]: Timeout calling "onTaskUpdate"`
  unhandled-error warnings are IPC-heartbeat timeouts from severe host
  load (uptime load average 58-89 on this 4-core host during this run,
  confirmed via repeated `uptime`), not test failures — every file and
  test is recorded `passed`.
- **Acceptance pipeline**
  (`specs/pipeline/scripts/run_acceptance.sh specs/features/BL-908-bubble-knowledge-screen-backlog-docs-panels.feature`):
  11/11 scenarios pass, 0 fail, 0 skipped.
- **Wiring check**: `KnowledgeActivity` is reached from
  `TalkPanelActivity.kt:107` (`binding.knowledgeBtn.setOnClickListener {
  startActivity(Intent(this, KnowledgeActivity::class.java)) }`) — a real
  caller, not merely unit-tested in isolation. In `activity_knowledge.xml`,
  both the new `syncBtnHeader` (line 30, permanent header action) and the
  original `syncBtn` (line 146, still inside `emptyState`) wire to `sync()`
  in `KnowledgeActivity.kt:35,42` — confirms the architect's sync-trigger
  send-back
  (`BL-908-bubble-knowledge-screen-backlog-docs-panels-architect-bounce-20260817.md`)
  is fixed: the sync action now has a permanent header home in addition to
  the first-run empty-state affordance.
- **Documentation currency**: `85fd2bdbd` adds
  `docs/how-to/BL-908-bubble-knowledge-screen-backlog-docs-panels.md` and
  updates `docs/reference/Specification.MD`'s changelog header + companion-
  package bullet list to match the shipped behavior (reachability via
  `knowledgeBtn`, the sync-trigger fix, the BL-659 retarget). Current and
  accurate against the code read above.
- **Device-surface manual procedure** (BL-769 testability boundary): not
  runnable in this environment (no Android device/emulator) — this is the
  documented, expected boundary for Bubble; the ticket's own
  `qa_e2e_procedure` is recorded as a manual procedure for a human with a
  paired device, not a QA-automatable gate. Reachability (procedure step 1)
  is confirmed structurally via the wiring check above, per BL-908's own
  `notes:` disposition for reachability.

## Disposition

**APPROVED.** No defect in BL-908's own scope. Landing on `main` and
broadcasting merge-up.

By QA.
