# BL-1535 — architect review pass, 2026-09-11

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: grep -rn attempt-resident-rotate swarmforge/scripts/test/
- **Commit hash**: e3178fd17d
- **First error excerpt**: test_chase_departing_mid_parcel_gate.sh is the only test driving attempt-resident-rotate!, and every scenario keeps LIVE_ROLE equal to mono-router-active-role
- **Failure class**: invariant-unencoded
- **Expected vs observed**: a case where the marker and the BL-927-resolved live role diverge, asserting the gate refuses on the LIVE role and telemetry :role names the LIVE role, never the marker
- **Blamed role**: coder
- **Remediation pointer**: add one case to test_chase_departing_mid_parcel_gate.sh setting mono-router-active-role and LIVE_ROLE to different roles, the LIVE role holding the in_process parcel and working signal, asserting refusal + telemetry :role naming the LIVE role

By architect.
