# BL-1589 — QA review pass, 2026-09-16

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- Merged documenter tip `c1d4d16f22` into QA clean (`Merge documenter
  c1d4d16f22 into QA.`); fast-forwarded from `origin/main` before that
  with no local changes.
- `npm run compile` (from `extension/`): clean, no errors.
- `bash swarmforge/scripts/pre_qa_gate.sh BL-1589 HEAD`: `OK` (the
  `bl1589DrawKindConstructedSteps.js::registerSteps` anchor resolves).
- Ticket's own e2e procedure, step 1: `test/bl1030StopFlagTokenBoundary.property.test.js`
  run alone 15 of 15 green under `vitest.properties.config.mjs`, every run
  printing `BL-1589 reach map (bl1030): {"kinds":3,"minDrawsPerKind":20}`
  (the printed map, not just the pass count, checked via grep on the full
  captured log — 15/15 matches, 0 FAIL lines).
- Read the source: `kind = i % KINDS.length` (`KINDS = [0, 1, 2]`) drives
  the draw by construction; `refusedCount >= 12` / `lookalikeCount >= 12`
  unchanged at their original values; `assertReachFloor(kindCoverage,
  KINDS, KIND_CELL_RUNS, 'bl1030 kind')` added; reach-map line printed
  from the actually-reached counts.
- Non-vacuity (step 3, break-then-restore, this pass, same file):
  - `kind = i % 2` (kind 2 never built) → RED, exactly
    `Error: reach floor: bl1030 kind 2 drawn 0 < 20`.
  - `kind = 1` constant → RED on the pre-existing assertion, exactly
    `AssertionError: generator coverage: only 0 of 60 draws were real
    flags (floor 12)` (the earlier-positioned assertion fires first, per
    the ticket's "whichever runs first").
  - Restored byte-for-byte from a pre-edit backup; `git diff` on the file
    empty; re-ran green (3/3, reach map printed).
- `npm test` (full unit suite, from `extension/`): 620 passed / 2 failed
  (622 files; 10564/10566 tests). Both failing files —
  `extension/test/tmpDirMigrationGuard.test.js` and
  `extension/test/liveRepoDerivationGuard.test.js` — are the pre-existing
  `nightClosingCeremonyRotateDocumenterFallback.test.js:29` defect from
  hotfix `a27d082c2d`, unrelated to BL-1589; both have open register rows
  owned by **BL-1593** (`backlog/paused/BL-1593-...yaml`, open).
- `npm run test:properties` (full property suite): 408 passed / 2 failed
  (410 files; 1204/1206 tests). `bl1280MkdtempMigrationInvariants.property.test.js`
  (owner BL-1593, same root cause) and
  `bl1529ScriptSenderAuditOutcomesInvariant.property.test.js` (owner
  BL-1592, `backlog/paused/BL-1592-...yaml`, open) — both pre-existing,
  unrelated to BL-1589's own file. The 4
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors are
  the BL-871 allowlisted benign artifact.
- Standing-red register: `bb swarmforge/scripts/standing_red_register_cli.bb .`
  → 7 rows, `"unowned":[]` — every red in both suites is one of these 7
  owned rows; none of BL-1589's own files appear.
- `qa-sibling-check.js status --ticket BL-1589`: `VERIFY BL-1589` (no open
  deferral) before starting the pass.
- Acceptance: `swarmforge/scripts/run_acceptance.sh
  specs/features/BL-1589-bl1030-draw-kind-is-constructed.feature` — 6/6
  scenarios (TAP: `# pass 6`, `# fail 0`).
- `git diff main...HEAD --name-only`: names the test file, the step
  handler, the feature/evidence/topic paths, and the register-row edit —
  but the range ALSO carries **BL-1588**'s own still-unlanded work
  (several other property test files, `propertyLaneContentionBudget.js`,
  `vitest.properties.config.mjs`, `bl1588FullLanePropertyTimeoutSteps.js`,
  `property_suite_standing_allowlist.tsv`, `backlog/active/BL-1588-*.yaml`)
  as ancestor commits on the shared batch branch — BL-1588 is currently
  architect-bounced back to the coder, not forwarded to QA, and was never
  named in this parcel's handoff. This is the BL-1241 entangled-tip shape
  (ordinary pipelining on a long-lived role branch), not a scope defect in
  BL-1589's own work: `extension/src` and
  `swarmforge/scripts/expedite_lib.bb` are untouched by either ticket's
  diff. Handled at land time per the BL-1241 procedure below, not as a
  bounce.
- No stragglers before or after (`pgrep -fl 'node --test|stryker'`
  checked around every run; the only pids it ever matched were the pgrep
  invocation's own command line — confirmed gone via `ps -p`, never real).
