# BL-1364 — documenter pass (2026-09-05)

## Scope reviewed

Re-read the ticket, the acceptance feature
(`specs/features/BL-1364-the-mechanical-share-of-a-turn-is-readable.feature`),
the coder's diff (`turnProfileProducer.ts`, `run-turn-profile-producer.ts`,
`handoffd.bb`'s `turn-profile-producer-sweep!`), and the chain of prior
evidence (coder `BL-1364-coder-20260905.md`, cleaner
`BL-1364-cleaner-20260905.md`, architect `BL-1364-architect-20260905.md`,
hardener `BL-1364-hardener-pass-20260905.md`).

## Documentation surfaces checked

- No how-to existed for this new producer. Wrote
  `docs/how-to/BL-1364-turn-profile-producer-wiring.md`, mirroring the
  sibling `docs/how-to/BL-665-context-telemetry-producer-wiring.md` exactly
  in structure (What runs where table, manual run, verify, out of scope),
  since the ticket explicitly follows that shape. Documented both invariants
  (absent-vs-zero; interior-damage-vs-torn-tail) since they are the load-
  bearing design decisions a future reader would otherwise have to
  re-derive from the source.
- Linked the new how-to from `docs/index.md` in the same commit, next to its
  BL-665 sibling.
- `docs/reference/Specification.MD`: added a new dated entry per the file's
  standing convention.
- Checked every other how-to mentioning `BL-664`/`buildTurnProfileSeries`/
  `turnProfile.ts` (`GH-22`, `BL-666`, `BL-668`): each references BL-664's
  transcript walker in general terms (burn measurement, turn-profiler
  motivation) and makes no claim about whether the series builder is wired
  — none were stale, none needed a change.
- `docs/diagrams/architecture.mmd`: its per-ticket comment log stopped
  being updated after 2026-08-27 (no ticket since has added an entry there);
  the visual graph itself does not depict individual daemon sweeps at this
  granularity. No edit made — consistent with how the rest of this pipeline
  run's tickets (BL-1400, BL-1353, BL-1275) were also handled.

## Verdict

New how-to written and linked; Specification.MD entry added. No other
documentation defect found.

By documenter.
