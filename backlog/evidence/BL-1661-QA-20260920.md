# BL-1661 — QA review pass, 2026-09-20

NONE. The full checklist was run and found no defect.

By QA.

## Detail

- `npx vitest run --config vitest.properties.config.mjs test/bl1372InvariantTwoPin.property.test.js`:
  12/12 pass (matches qa_e2e_procedure step 1, "10 or more passed").
- `grep -c 'fc.string(' extension/test/bl1372InvariantTwoPin.property.test.js`: 0 (step 2).
- Step 3 (coder's `node -e` repro against the parcel's helper): the fix's own deterministic pin
  (`stripCommentsKeepStrings quote-parity pin`, lines 308-318) is exactly this repro encoded as a
  unit case in the same file, and passed in the 12/12 run above.
- `shaArb`/`refArb` now draw `fc.stringMatching(/^[0-9a-f]{7,40}$/)` and
  `fc.stringMatching(/^[A-Za-z0-9._][A-Za-z0-9._/-]{0,19}$/)` — production domain, matching the
  ticket's "How" direction.
- `node specs/pipeline/cli.js specs/features/BL-1372-the-invariant-two-pin-asks-the-qa-question.feature`:
  3/3 ok, unchanged (constraints: no feature change).
- `npm run test:properties` (extension/): 431/432 files, 1263/1264 tests passed; the one failure
  (`bl1630NoStepHandlerModuleLoadWorkInvariants.property.test.js`, non-vacuity timing budget) is
  BL-1630's own known, pre-existing, unrelated flake (already documented in
  `backlog/evidence/unowned-red-bl1630-module-load-budget-timing-flake-20260920.md` and multiple
  prior QA passes this session) — owned by BL-1630, not an unowned red, not attributable to BL-1661.
- `npm test` (extension/): 635/635 files, 10819/10819 tests passed.
- `pre_qa_gate.sh BL-1661 52f468d180`: OK.
- No straggler test/mutation processes before or after.
- `qa-sibling-check.js status --ticket BL-1661`: VERIFY (no open deferral).
- Standing-red register row for this file names BL-1661 as owner; this land retires it (BL-1631).
