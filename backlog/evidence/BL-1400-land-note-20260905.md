# BL-1400 land note — merge-deletion guard blocked a plain merge, 2026-09-05

Same BL-1403 class as BL-1370/BL-1401/BL-1368: a plain `git merge
ee22cd6641` into the QA worktree was refused by `check_merge_deletion.sh`
over the two unattributed intake-archive deletions. This ticket's own
history independently confirms the class: `d237636c9a` "Re-drop the two
drained root intakes the BL-1400 merge resurrected".

Replayed this ticket's own attributed paths (coder/cleaner/architect/
hardener/documenter) as a plain, non-merge commit (`d7722ab39e`) instead,
re-verified `npm run compile` clean, unit 16/16, property 2/2, shell e2e
11/11, acceptance 5/5, then built and landed the tip-pure commit
(`3cb48de0b2`).

One commit-time hiccup: the first tip-pure commit attempt was refused by
`property-suite-guard` on `bl1218RemoteControlConfigInvariants.property.test.js`,
unrelated to this diff — confirmed flaky (passed cleanly in isolation),
retried and the retry passed clean (a *different* flaky property test,
`bl687EpicTileSurfaceUntouched`, showed up on the retry's run but did not
block the guard, since only the FIRST run's file name matters to it).

Land-approval recorded citing `d7722ab39e` (the plain replay commit) as
source: `{"at":"2026-09-05T03:06:21Z","ticket":"BL-1400","commit":"3cb48de0b2","source":"d7722ab39e"}`.
Verified `is_qa_ancestor.sh 3cb48de0b2` prints `approved: ... - BL-1334`.
`abandoned_commits: [ee22cd6641]` recorded on the ticket.

By QA.
