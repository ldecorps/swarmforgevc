# BL-1323 — coder pass (2026-09-02): BL-848 stamp-off review of hotfix 9c94735f03

Review-only parcel. Nothing in the hotfix was reimplemented, rewritten or
reverted, and no decision was written into `backlog/hotfix-ledger.yaml` — the
row stays `state: stamp-open`, `human_decision: null`, pending the human.

## qa_e2e_procedure, answered in order

**(1) The gather prefers the marker, and the shell-out uses `git -C`.**
Confirmed in `swarmforge/scripts/babysitter_check.bb::gather-main-sync-deadlock`:
it normalizes `(:overlapping_paths deadlock)` / `(:overlapping-paths deadlock)`
through `master-main-reconcile-lib/normalize-overlapping-paths` FIRST, and only
falls through to a recompute when that is empty AND the reason is `"dirty"`.
The recompute calls `(sh! "git" "-C" (str project-root) "status" "--porcelain")`
— the corrected shape. Not read off the source alone: acceptance scenario 01
runs the real gather against a fixture root that is **not a git repository at
all**, so a recompute would have had nothing to compute from; the marker's exact
paths coming back is the proof, behavioural rather than textual.

**(2) handoffd persists the overlap at trip time.** Confirmed: the trip path
computes `overlap` via the same `normalize-overlapping-paths` helper, puts it on
the payload as `:overlapping_paths`, and persists with
`master-main-reconcile-lib/write-deadlock!`. Scenario 03 also closes the round
trip — what the trip writes, the gather reads back unchanged.

**(3) Both suites green.**
`bb swarmforge/scripts/test/master_main_reconcile_lib_test_runner.bb` → ALL
TESTS PASS. `bb swarmforge/scripts/test/babysitterd_sweep_lib_test_runner.bb`
→ ok. The cap / "(+N more)" / "./swarm heal" assertions are present and
passing, and acceptance scenario 04 exercises 0, 3 and 12 paths against the
real formatter, checking that the 9th–12th paths are NOT named.

**(4) The generic alert text is gone.** `grep -rn 'wait for BL-891 reconcile'
swarmforge/scripts/*.bb` returns nothing, and `deadlock-alert-text` is now
`(str "⚠️ " (operator-deadlock-hint …))` — literally the same body the CRIT
shows. Scenario 05 asserts both halves.

**(5) The snapshot-not-live gap — REPORTED, not acted on.** Confirmed present
exactly as `approval_context` describes: because the gather prefers the marker's
`overlapping_paths` captured once at trip time, every recurring CRIT between the
trip and `behind=0` shows the trip-time snapshot. A path the operator has since
cleared can still be named; a path that went dirty after the trip will not be.
This is a real consequence of BL-891's "at most one alert per trip" contract, so
it is a **known limitation, not a bug in this hotfix**. The human's recorded
ruling is *"Certify as landed; mint a follow-up to recompute or clearly label
the hint as trip-time-only"* — so the follow-up mint is owed by the specifier;
this parcel deliberately does not act on it, per the ticket's own firm line
("Do NOT silently 'fix' the snapshot-not-live overlap gap").

**(6) `Hotfix-Certification` left pending.** No ledger edit in this parcel.

## required_wiring

All three entries verified present. The third is this parcel's own deliverable:
`specs/pipeline/steps/bl1323MainSyncDeadlockOverlapHintsStampSteps.js`,
registered in `specs/pipeline/steps/index.js` in this same commit (BL-233).

## Verification

- `run_acceptance.sh specs/features/BL-1323-…feature` — **7/7 pass**.
- **Non-vacuity of the review itself**, shown not claimed: disabling the
  marker-preference branch in `babysitter_check.bb` (`(and false (seq marked))`)
  turns 2 of the 7 scenarios red. Restored; `git diff` on that file is empty.
  A stamp-off whose scenarios pass against a broken implementation certifies
  nothing, so this check is the point rather than a formality.
- **All three declared invariants (BL-654)**, coder-authored, in
  `extension/test/bl1323StampOffInvariants.property.test.js` — 3 passed.
  Invariants 1 and 2 quantify over this PARCEL rather than over a pure
  function, so they are checked against the working tree directly (invariant 1
  measures `git diff origin/main...HEAD` against the reviewed sources;
  invariant 2 asserts the ledger row is untouched AND that actually running the
  review suite leaves the file byte-identical — an inert ledger only counts as
  evidence if something ran that could have written to it). Invariant 3 drives
  the real formatter over empty, ordinary, over-cap and failed-read-sentinel
  overlaps, with the reach floor asserted.
- **Non-vacuity of invariant 3**: replacing `./swarm heal` in the lib fails it.
  Restored; the lib's own runner is ALL TESTS PASS again.

## For the specifier (not this parcel's work)

The human's ruling asks for a follow-up ticket on the snapshot-not-live overlap
hint — recompute it, or label the hint as trip-time-only. Recorded here rather
than minted, since minting is the specifier's office.

## Commit-time note: property-suite guard override, and why

`check_property_suite_drift.sh` refused this commit twice, naming
`test/bl1253TokenOwnershipInvariants.property.test.js` (and once
`test/bl1295RevertAttributionInvariants.property.test.js`) as non-allowlisted
reds. Both are **load flakes under the full concurrent property suite, not
regressions from this parcel**, verified rather than assumed:

- run in isolation, together with this parcel's own new property file, all
  three are green (`Tests 10 passed`), repeatedly;
- neither file, nor any module either one exercises, is touched by this
  parcel — its whole diff is a new acceptance step handler, its registration,
  a new property test, and this evidence file;
- the same two files flaked under the identical full-suite load earlier this
  session on an unrelated commit, which is what a load flake looks like and
  not what a regression looks like.

So the commit was made with `SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1`, the
override the guard's own refusal message names. Recorded here rather than left
in shell history, because an override that nobody can see later is how a real
red gets waved through. Downstream roles should treat those two files' standing
flakiness as a separate concern (they are not on
`property_suite_standing_allowlist.tsv`, and arguably should be, or should be
fixed).
