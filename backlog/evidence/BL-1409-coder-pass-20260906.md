# Evidence — BL-1409 — coder pass (2026-09-06)

Role: coder. Ticket: BL-1409-bl570-wiring-assertion-follows-the-delegation.
Routed by coordinator note (`Work BL-1409-bl570-wiring-assertion-follows-the-delegation`).

## What was implemented

1. **`specs/pipeline/steps/lib/bl1409PropertyGuardWiring.js`** - the ONE
   `propertyGuardIsWired({repoRoot, hookRel, runnerRel, readFile, exists})`
   check: (a) the hook invokes the runner in a non-comment line, AND (b)
   `deriveCommitGuardFixtureSet`'s (BL-1398) own derived guard set for the
   runner names `check_property_suite_drift.sh`. Returns
   `{wired:true}` or `{wired:false, missing, reason}` - never nil, and
   `hookRels: []` is passed to the derivation so only the RUNNER's own
   guard-invocation lines are read, never pre-merge-commit's separate,
   unrelated chain (invariant 1's own "the runner's guard set", not the
   union of every hook's).
2. **`specs/pipeline/steps/bl570PropertySuiteDriftGuardSteps.js`**'s
   Background now calls `propertyGuardIsWired` instead of a bare grep for
   `check_property_suite_drift.sh` in the hook file (which stopped matching
   the moment BL-1252 made the hook a thin `exec run_commit_guards.sh`
   wrapper). **BL-570's feature: 0/7 → 7/7.**
3. **`swarmforge/scripts/test/test_property_suite_drift_guard.sh`** case 07
   rewritten in two halves, per the ticket's own How: (a) the check itself,
   via `node -e`, against the REAL repo - never the fixture, which only
   proves the BEHAVIOUR the delegation produces; (b) the commit-through-
   the-hook half now copies the REAL `run_commit_guards.sh` +
   `commit_guard_chain_lib.sh` + the real copied hook, and drives the real
   `git commit` with `SWARMFORGE_COMMIT_GUARD_DIR` pointed at a directory
   holding the REAL property guard (+ its unconditionally-sourced libs)
   and a passing stub for every OTHER guard the REAL runner derives -
   proving hook -> runner -> the guard's own skip-paths decision, end to
   end, without booting handoffd or reading the step registry inside a
   scratch repo. **Case 07: red since 2026-08-30 → green.**
4. `specs/pipeline/steps/bl1409Bl570WiringFollowsTheDelegationSteps.js` -
   drives the REAL BL-570 feature (via `run_acceptance.sh`), the REAL
   shell suite, and the REAL `propertyGuardIsWired` against synthetic
   chain seams. **4/5 runs pass** (see "A pre-existing, unrelated red
   found" below for the one blocked run).
5. `extension/test/bl1409PropertyGuardWiringInvariants.property.test.js` -
   coder-authored, 202 generated cases (200 generative + 2 structural) for
   the three declared invariants.

## A pre-existing, unrelated red found - not this parcel's

Case 07 has been red since 2026-08-30 (`76dd67b692`), which meant NOTHING
in `test_property_suite_drift_guard.sh` past case 07 has actually RUN in
six days (`set -euo pipefail` aborts the whole script at the first `fail`).
Fixing case 07 (this ticket's own deliverable) exposed case 11
("BL-1175 all-allowlisted standing reds allow without SKIP") failing for
an UNRELATED reason: its injected fake suite output reports
`test/bl632CommitTimeGuardInvariants.property.test.js` as failing, which
triggers BL-1407's real re-run-alone mechanism - and that re-run, executed
from case 11's own throwaway fixture root (`$ROOT`, a bare git init with
no `extension/`/`node_modules/` at all), cannot actually run vitest there
and so "still fails when run alone" for an environmental reason, never
because the real property test is broken (confirmed: run directly via
`npx vitest run --config vitest.properties.config.mjs
test/bl632CommitTimeGuardInvariants.property.test.js` - 2/2 pass, clean).

**Confirmed pre-existing and independent of this ticket**: reproduced by
restoring `test_property_suite_drift_guard.sh`'s pre-BL-1409 case 07 body
verbatim (`git show HEAD:...`), neutering ONLY its already-known grep
failure so the script could proceed past it with none of this ticket's own
case-07 rework in play, and re-running - case 11 fails identically. Not in
`property_suite_standing_allowlist.tsv` or `backlog/standing-reds.tsv`; no
owning ticket found. Per the standing-red rule, a priority-00 `unowned-red`
note naming the file and the failing assertion was sent to the specifier
and coordinator; this parcel continues and forwards regardless, per the
same rule's own instruction.

This is why BL-1409's OWN feature scenario 04 ("the drift-guard shell
suite ... every case passes") and qa_e2e #2 ("passes every case") cannot
be fully satisfied by ANY fix within this ticket's scope: case 07 (this
ticket's actual deliverable) is fixed and verified directly (both via the
acceptance handler's own targeted check, and via the full shell suite run
showing cases 01-10 all PASS, case 07 among them); case 11 is a separate,
now-escalated defect in BL-1407's re-run-alone mechanism's fixture
interaction, explicitly out of BL-1409's own scope ("Explicitly NOT in
scope" names the hand-written guard lists in sibling tests as the closest
analogous exclusion - the same narrow-scope discipline applies here).

## A real regression caught by the full property suite, fixed before commit

The first draft of `bl1409PropertyGuardWiringInvariants.property.test.js`
allocated its seam fixture roots via a raw `fs.mkdtempSync(...)` with its
own `process.on('exit', ...)` cleanup, rather than the shared `mkTmpDir`
helper (`./helpers/tmpDir`) - BL-1280's own guard
(`bl1280MkdtempMigrationInvariants.property.test.js`, `findRawMkdtempCallSites`)
caught it on the first full `npm run test:properties` run, naming this
file's own line 59. Fixed to `mkTmpDir('sfvc-bl1409-prop-')`, swept by the
already-wired per-test `tmpDirSetup.js` afterEach instead of a bespoke
process-exit hook. Re-ran the full suite after the fix.

## Non-vacuity (BL-654), all three declared invariants

`extension/test/bl1409PropertyGuardWiringInvariants.property.test.js` - 2
tests (200 generative cases for invariants 1+3, 1 structural check for
invariant 2). Each break applied by hand, confirmed via `diff` against a
pre-edit backup empty after restore, both tests green again immediately
after each restore.

- **Invariants 1 and 3** (wired iff both hops hold; a broken hop always
  names itself): the hook-invokes-runner check's body replaced with
  `if (false)` - unconditionally treating the hook hop as satisfied. The
  generative property failed on its first case with `hookShape:
  "comment-only"` (expected `wired:false`, got `wired:true`).
- **Invariant 2** (exactly one parser of the runner's own guard-invocation
  lines): a second `/run_guard\s+(\S+)/` regex literal added to the
  module's own exports. The structural check failed (found a "run_guard"
  occurrence outside a comment).

## Checks run this pass

- `npx vitest run --config vitest.properties.config.mjs
  test/bl1409PropertyGuardWiringInvariants.property.test.js` - ALL PASS.
- Acceptance `specs/features/BL-570-property-suite-drift-guard.feature` -
  **7/7** (was 0/7 on main since 2026-08-30).
- Acceptance
  `specs/features/BL-1409-bl570-wiring-assertion-follows-the-delegation.feature`
  - 4/5 (scenario 4 blocked by the escalated, unrelated case-11 red above;
    scenarios 1-3, covering this ticket's own deliverable, all pass).
- `bash swarmforge/scripts/test/test_property_suite_drift_guard.sh` -
  cases 01-10 PASS (07 among them, this ticket's own fix); case 11 fails
  for the escalated, unrelated reason above; cases 12-25 never reached
  (the script aborts at the first `fail`, same `set -e` shape that hid
  case 11 behind case 07 for six days).
- `grep -c 'run_guard' specs/pipeline/steps/bl570PropertySuiteDriftGuardSteps.js`
  → `0`; `grep -c propertyGuardIsWired` on the same file → `2`. Same two
  greps on `test_property_suite_drift_guard.sh` → `0` and `4`
  (qa_e2e #4, invariant 2). Both files' own explanatory comments were
  reworded once to avoid the literal substring "run_guard" after the first
  pass tripped this exact literal grep on prose, not code.
- `npm run test:properties` (full suite, `extension/`, ~268s) - **366/366
  test files, 1093/1093 tests pass**, including the new
  `bl1409PropertyGuardWiringInvariants.property.test.js` (first caught the
  raw-mkdtemp regression above, then confirmed clean after the fix). The 2
  "Unhandled Errors" are the documented, allowlisted `[vitest-worker]:
  Timeout calling "onTaskUpdate"` (BL-871); the `runGitLog ... fatal: not
  a git repository` and `front-desk bot: failed to close the approval ask`
  lines are pre-existing noise from unrelated fixtures (bl603, bl582), not
  this parcel's own tests.

## Out of scope, confirmed untouched

- The case-11 defect itself (escalated, not fixed here - see above).
- The hand-written guard lists in the other commit-guard tests (BL-1408,
  minted the same pass as this ticket, per the ticket's own notes).
- Whether the hook may call a guard directly, bypassing the runner
  (`test_run_commit_guards.sh` case 10's own territory).
