# BL-1346 — cleaner pass (2026-09-03)

## This is a stamp-off (BL-848 review-only ticket) — checked as one

Diff (`0ed304181d`..`ac110de8dd`) adds a fixture lib, step-handler file,
property-test file, one `index.js` registration line, and a small
maintenance fix (explicit 120s timeouts on two already-reviewed sibling
stamp-off property tests, `bl1333StampOffInvariants` and
`bl1342CrashloopStampInvariants` — both drive real git-fixture work that
runs past Vitest's default; not mentioned in the coder's evidence file but
harmless, mechanical, and doesn't touch any assertion). No production file
touched (`swarm_ensure.bb`, `mono_router_lib.bb`).

## required_wiring anchors — verified present at the counts the ticket names

- `resolve-resident-role` in `swarm_ensure.bb`: 2 occurrences (ticket: 2).
- `rc-launch-role` in `swarm_ensure.bb`: 3 occurrences (ticket: 3).
- `bl1346SwarmStampRcLaunchRoleSteps` registered in
  `specs/pipeline/steps/index.js`: 1 occurrence.

## Ledger constraint honored

Only reads `backlog/hotfix-ledger.yaml`; scenario 5 ("The stamp leaves the
certification decision to the human") asserts no `certified`/`waived` state
appears. No write.

## Verification (run, not assumed)

- `run_acceptance.sh specs/features/BL-1346-…feature` — 5/5 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1346RcRepairStampInvariants` — 5 consecutive green runs (backgrounded;
  real-fixture work, ~2min/run), 0/5 failures.
- `bash swarmforge/scripts/test/test_swarm_ensure.sh` (named in the
  ticket's own `qa_e2e_procedure` item 2) — backgrounded (takes several
  minutes of real tmux/sleep fixtures, as with BL-1345) — **ALL PASS**,
  confirmed through RC-7 (router pack unchanged) and RC-7b (the hotfix's
  own fixture) both passing.

## Mutation-site advisory (BL-485)

Step/fixture/property files at 130–178 sites, consistent with this
session's review-harness range; no split warranted, none are production
code.

## Verdict

No defect. Forwarding.
