# BL-828 QA bounce 2 — 20260825

**Reviewed tip:** documenter `e4cab1369f` (not merged — tip impure)
**Task:** `BL-828-bubble-collapsed-gesture-model`
**Sibling:** `VERIFY BL-828`
**Prior bounce:** `67b737263b` (D1: deleted intake/paused paths — rematch cleared those)

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | **FAIL → D1** |
| Unit / APS / docs gates | **BLOCKED BY D1** |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main e4cab1369f  # fails
# dels_on_origin=0 but tip lacks current main ancestry
```

**Commit hash checked:** `e4cab1369f`

**First error excerpt:**
```
main_NOT_ancestor (origin/main = 0d5ad12a9 JumpQ rematch regression briefing)
paths=19 dels=0
tip based on bounce1; main advanced with briefing refresh after rematch
```

**Failure class:** `behavior`

**Expected vs observed:** Rematch onto **current** `origin/main` immediately
before forward (`origin/main` must be ancestor). Observed tip cut from
bounce1 that is already behind newer main bookkeeping.

**Remediation:** `git fetch && git reset --hard origin/main` then restore
BL-828 product only; keep bounce evidence + intake/paused paths; confirm
ancestry + dels=0 **right before** `git_handoff`.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
