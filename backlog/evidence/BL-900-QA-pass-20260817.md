# BL-900 — QA PASS: epic-priority-before-ticket-priority promotion ranking

**Parcel verified:** documenter's commit `56e7881bb6`
(`BL-900-epic-priority-before-ticket-priority`), confirmed an ancestor of
this commit (`git merge-base --is-ancestor 56e7881bb6 HEAD`).

## Lineage

Every prior stage's merge for this ticket confirmed an ancestor of the
verified commit: coder merge `04d856276`, hardener merge `fb3a2c2f2`
(itself containing the hardener's bounce-fix commit `19f3057f4`), architect
pass-2 remediation `ed54fe3a4`. `git merge-base --is-ancestor` checked pairwise
in sequence, all pass.

## Prior bounce history re-verified fixed

Read from `main` ref (`git log --oneline main -- 'backlog/evidence/BL-900*'`):
two prior bounces recorded.
- **Architect pass 2** (`ed54fe3a4`, D1 remediated) — confirmed an ancestor
  of the verified commit.
- **Hardener bounce** (`6319570ea`, "epic-index not threaded through
  chase_sweep_lib.bb") — fix commit `19f3057f4` (threads
  `promotion-gates-lib/epic-priority-index` through both
  `chase_sweep_lib.bb` call sites AND their two live `handoffd.bb` callers,
  `open-slot-nudge-sweep!` and `ambulance-auto-exit-sweep!`) confirmed an
  ancestor of the verified commit.

## Independent verification run this pass

- **Babashka unit suites**: `promotion_gates_lib_test_runner.bb` (ALL PASS),
  `promotion_gates_cli_test_runner.bb` (ALL PASS), `dispatch_gap_test_runner.bb`
  (ALL PASS — covers the `chase_sweep_lib.bb`/`handoffd.bb` epic-index
  threading), `test/test_chase_sweep.sh` (24/24 PASS).
- **Property tests** (`promotion_gates_lib_property_runner.bb`): 500 runs
  each, including net-new P9 (expedite bucket stays strictly first
  regardless of epic priority — invariant 2) and P10 (deterministic total
  order regardless of enumeration order — invariant 3). ALL PROPERTIES HOLD.
- **Acceptance pipeline**
  (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-900-epic-priority-before-ticket-priority.feature`):
  9/9 scenarios pass, covering the ticket's own QA end-to-end procedure
  (epic-priority beats own-priority, expedite bucket still wins, no-tracker
  fallback, multi-tracker most-urgent-wins, deterministic ordering, no
  extra active slot granted).
- **Extension unit suite** (`npm run test`): 7733/7733 tests, 437/437 files
  passed. The wrapper script's own exit code was 1 due to a suite-duration
  budget check (`suite duration over budget: 187.5s exceeds the 10.0s suite
  budget`) — this host was under heavy load throughout this session
  (`uptime` showed load average ~13 during the run). Confirmed unrelated to
  BL-900: BL-900's own commits (`324ef75c0`, `6319570ea`, `19f3057f4`,
  `1c1fc727b`, `ed54fe3a4`, `56e7881bb6`) never touch any file under
  `extension/src` or `extension/test` (checked per-commit via `git show
  --stat`); the files that changed under `extension/` in the wider range
  came from routine `main` merges carrying unrelated tickets (BL-896/897),
  not from BL-900's own diff.
- **Property command** (`npm run test:properties`): 304/304 tests, 97/97
  files passed, exit 0. 4 `[vitest-worker]: Timeout calling "onTaskUpdate"`
  unhandled-error warnings are IPC-heartbeat timeouts from the same host
  load, not test failures — every file and test recorded `passed`.
- **Wiring check**: `epic-priority-index` reaches the real promotion
  chokepoint — `promotion_gates_cli.bb`'s `cmd-select` (the function
  `promote_and_route_next.sh` calls) builds and passes it to
  `rank-candidates`. The hardener's own bounce-fix additionally wires it
  through `chase_sweep_lib.bb`'s `top-open-slot-candidate` and
  `top-expedited-paused-candidate`, called from `handoffd.bb`'s
  `open-slot-nudge-sweep!` and `ambulance-auto-exit-sweep!` respectively —
  confirmed both call sites pass a real `epic-priority-index` rather than
  falling back to the 1-arity `{}` default.
- **Documentation currency**: `56e7881bb6` adds
  `docs/how-to/BL-900-epic-priority-promotion-ranking.md`, updates
  `docs/diagrams/swarm-flow.mmd`'s promotion edge and header comment to
  name epic-priority ranking, cross-links from the epic-reorder console
  how-to, and adds a `Specification.MD` changelog entry. Current and
  accurate against the code read above.
- **Orphaned process check**: `pgrep -fl 'node --test|stryker|vitest'`
  clean before and after every run in this pass.

## Ordering-decisions and invariants spot-checked against acceptance scenarios

All four ordering decisions in the ticket's `description:` (multi-tracker
min, no-tracker fallback, no-`epic:` fallback, tie-break-through) and all
three declared `invariants:` (ordering-only/no extra slot, expedite bucket
strictly first, deterministic total order) are each covered by a distinct
acceptance scenario or property — no gap between what the ticket promised
and what was tested.

## Disposition

**APPROVED.** No defect in BL-900's own scope. Landing on `main` and
broadcasting merge-up.

By QA.
