# BL-1537 — hardender review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No defect is recorded as a D-item because the one gap this pass found was
fixed within the hardener's own domain (adding a test) in the same commit
being forwarded (766ab07f20), not sent back to another role.

Checks run this pass:

- BL-149 cooldown gate on all four changed compiled-file sources
  (`mutation_cooldown_gate.bb`): `run` for all four.
- Full Stryker (`--force`, incremental cache cleared) on
  `out/swarm/draftPathUnder.js`, the ticket's declared full-Stryker file:
  first run found ONE survivor - `MethodExpression`,
  `draftPathUnder.js:48:19`, dropping `.slice(2)` from
  `Math.random().toString(36).slice(2)`. Still produces a distinct,
  root-anchored path (the two pre-existing unit tests can't see it), but
  bakes the `toString(36)` leading `"0."` into the basename - a stray `.`
  in what must be a single path segment. Killed by pinning the basename
  shape (`prefix-pid-[0-9a-z]+`, no dot) in `draftPathUnder.test.js`.
  Re-run after the fix: 100% (8/8 killed, 0 survived).
- Full Stryker on the three TS sender wrappers this ticket touches
  (`closing-ceremony-run.js`, `night-closing-ceremony-run.js`,
  `tracer-bullet-launcher.js`): heavy NoCoverage/Survived across all
  three, but confirmed by inspection this is PRE-EXISTING - none of it
  maps to code these functions didn't already have before BL-1537 in the
  same shape (no unit test anywhere in the suite mocks `execFileSync` to
  reach `sendNoteViaHandoff`/`sendHandoffNote`/`sendSeedNote`, before or
  after this diff). This matches the ticket's own declared mutation
  strategy (mutation_cost note): full Stryker on the extracted pure
  helper, the wrapper sends verified through the real acceptance suite
  instead of a mocked unit test - and that acceptance suite has real
  discriminating power here (confirmed non-vacuous): it fails on a
  TMPDIR leak, a leftover draft under `tmp/`, a `HANDOFF_DRAFT_OUTSIDE_ROOT`
  refusal, or a note that never reaches the recipient's inbox - all of
  which a broken `draftPathUnder`/`mkdirSync`/try-finally wiring would
  trip. Not treated as first-run debt owed by this ticket: the gap is
  the whole file's pre-existing lack of a unit-test seam for
  execFileSync-based senders, not something BL-1537's ~15 changed lines
  introduced, and extracting one is a design change beyond this
  defect-fix ticket's declared scope.
- Differential complexity (main vs this branch) for `sendNoteViaHandoff`,
  `sendHandoffNote`, `sendSeedNote`: unchanged (complexity 1, 2, 1
  respectively on both sides) - no regression.
- CRAP (`node scripts/crapReport.js`, scoped to `src/*.ts`) on all four
  changed source files: the three touched functions score
  `sendNoteViaHandoff` 2.00, `sendHandoffNote` 5.56, `sendSeedNote` 2.00,
  `draftPathUnder` 1.00, `removeDraftIfPresent` 2.00 - all within budget.
  14 functions across the two larger files exceed CRAP 6, all
  pre-existing and untouched by this diff (`parseArgs`, `generateReport`,
  `watchLive`, `main`, `applyAction`, `runNightClosingCeremony`, etc).
- `npx jscpd` over the four changed files: 0 clones found.
- Shell fixture test
  `test_production_sender_drafts_under_root.sh`: 5/5 PASS (includes the
  architect-bounce D1 fix's own scenario 05).
- Full acceptance suite for the ticket's feature file: 9/9 PASS.
- Related unit/property suites (`draftPathUnder`, `closingCeremonyRun`,
  `closingCeremonyRunCli`, `nightClosingCeremonyRun`,
  `tracerBulletLauncher`, `tracer`): 58 unit + 2 property tests PASS.
- Full `vitest run --coverage`: 616 test files / 10445 tests PASS.
- Unrelated standing red ruled out: Stryker's dry run failed on
  `test/activePoolFreshnessAudit.test.js` ("checkFreshnessViaCli runs the
  real built CLI and returns its stdout") - this is the documented
  BL-1066 Stryker-sandbox-only red (a `../..` walk-up from `__dirname`
  resolves outside `extension/` inside the sandbox); confirmed by running
  the same test standalone (25/25 PASS) and by `git diff main...HEAD` on
  the file (no changes). Worked around per the standing rule ("A standing
  Stryker-sandbox red never licenses editing vitest.config.mjs excludes")
  with a new tracked `extension/vitest.bl1537.stryker.config.mjs`
  (include-scoped to this ticket's test files), pointed to temporarily via
  `stryker.config.json`'s `configFile` for each run and reverted
  immediately after - `stryker.config.json` carries no diff from `main`
  in the forwarded commit.
- No orphaned test/mutation processes after any run
  (`pgrep -fl 'node --test|stryker'` scoped to this worktree, clean);
  `git status` clean throughout except the intended files.

By hardender.
