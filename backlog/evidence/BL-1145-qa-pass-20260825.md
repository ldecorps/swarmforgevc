# BL-1145 QA pass inventory — 20260825

**Documenter tip:** `a1f7b16c58` (rematch product commit `e1642f102`)
**Task:** `BL-1145-open-slot-nudge-skips-epic-trackers`
**Sibling check:** `VERIFY BL-1145` (exit 0)

## Lineage (BL-336)

Documenter rematched from `origin/main` (abandoned stranded stage tips per
evidence). Product surface lives in `e1642f102`; tip `a1f7b16c58` adds stage
evidence only. `origin/main` is ancestor; `dels_on_origin=0`.

## Ticket gates (parcel-owned)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` | PASS — 16 paths, `dels=0` |
| `required_wiring` (APS handler in `index.js`) | PASS |
| BL-149 wiring (`nudge-eligible-candidates` → `evaluate`) | PASS |
| `promotion_gates_lib_test_runner.bb` | ALL PASS |
| `promotion_gates_lib_property_runner.bb` | ALL PROPERTIES HOLD |
| APS `BL-1145-open-slot-nudge-skips-epic-trackers.feature` | 3/3 pass |
| Stage evidence (architect/cleaner/hardener/documenter) | present |
| Orphan test procs | none |

## Intent

`epic-type-refusal` + `blocked-status-refusal` on `promotion_gates_lib/evaluate`
after hold, before human_approval — open-slot nudge and promote share one
structured exclusion (BL-663). Epic trackers never named as top candidate;
real feature wins when epic ranks higher; explicit epic promote still refuses.

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS** — land QA-approved tip on `origin/main`.

By QA.
