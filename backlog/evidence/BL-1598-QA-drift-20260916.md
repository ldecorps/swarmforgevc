# BL-1598 — QA finding: the committed register has already drifted from live reality, 2026-09-16

## What was run

On the parcel commit (`extension/`, `npm test`), 3 consecutive runs under
concurrent host load (another seat's `npm run test:properties` active
throughout, 1-minute load 6.4-9.0, 20 cores):

```
6 registered pole(s) reported, not refused: bl968.../telegramFrontDeskBotCli/
renderBriefingBurndownCli/briefingDigestLineCli/bl1277.../renderBriefingDiagramsCli
suite file budget exceeded:
1 new-pole offender(s):
extension/test/emitLifecycleSnapshotCli.test.js: ~9.0s (no register row)
3 stale register row(s) (file now under 80% of budget - remove the row):
extension/test/epicReorderBridge.test.js: ~5.0s (row: 10600ms)
extension/test/pilotAcceptanceGateCli.test.js: ~2.7s (row: 8500ms)
extension/test/recordBounceCli.test.js: ~2.3s (row: 7300ms)
```

Reproduced identically in shape across all 3 runs (exit code 1 every time,
same six files reported/not-refused, same one new-pole, same three stale
rows), so this is not run-to-run flake — the register's numbers, committed
by this ticket's own coder from the specifier's 13:16 UTC measurement, no
longer match the tree at land time (~15:50 UTC), roughly 2.5 hours and
several intervening lands later (BL-1600 among them, though that only
touches `bl1277`, which is still correctly `registeredPoles` above budget).

## Why this is not a bounce

- The mechanism is correct and fully tested: all deterministic checks are
  green — `checkSuiteFileBudgetCli.test.js` (43/43), `testDurationRecorderLib.test.js`
  (8/8), `bl1598SuitePoleRegister.property.test.js` (2/2), and the
  acceptance feature (9/9 scenarios, including the fixed census assertion
  that the committed file has exactly 9 rows, each with an open owner —
  true today).
- Invariant 1 (never wave a new pole through) and invariant 2 (no test
  removed) both hold; `emitLifecycleSnapshotCli` reds correctly rather
  than being silently admitted.
- The coder's committed numbers were accurate as of the specifier's own
  13:16 UTC measurement; nothing in this parcel's diff is wrong.

## Why this blocks approval anyway

The moment this lands, `npm test` starts failing on `main` for every
future parcel until the register is refreshed — the exact perpetual-red
failure mode BL-1598 exists to end, reappearing on day one because the
data went stale between mint and land. Per Article 4.2 / the standing-red
posture, an unowned red gate condition on the landed tree waits for
adjudication rather than landing broken.

## What is needed

Either (specifier's call): amend `backlog/suite-poles.tsv` in this parcel
before land (drop the three now-stale rows, add
`extension/test/emitLifecycleSnapshotCli.test.js` owned by BL-791 with a
fresh measured_ms), or mint a follow-up ticket and hold this one under
Article 4.2 until it lands in the same pass. QA has run no fix itself
(register data is not QA's to write).

By QA.
