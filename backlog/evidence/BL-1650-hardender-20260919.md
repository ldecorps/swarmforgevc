# BL-1650 — hardender review pass, 2026-09-19

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bb swarmforge/scripts/test/land_step_lib_test_runner.bb (baseline, then hand-mutated replay! removing the (cleanup!) call before the stray cherry-pick failure return, then restored)
- **Commit hash**: 7b39e5d474
- **First error excerpt**: the tickets own FIRM constraint (a failed cherry-pick aborts the whole replay, nothing is abandoned) had zero test coverage - every existing end-to-end case cherry-picks a stray whose file exists nowhere else on origin/main, so the conflict path was never exercised
- **Failure class**: test-coverage-gap
- **Expected vs observed**: a conflicting stray cherry-pick escalates rather than landing half-done: no branch or worktree left behind, the sibling stays unlanded, the caller checkout untouched
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: verified the real behavior by hand first (standalone probe script), then added the case pinning it; commit 7b39e5d474; confirmed it now kills the hand-applied mutant (leaked branch/worktree)

By hardender.
