# BL-1640 — hardener review pass, 2026-09-21

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: timed run of test_finish_shift_lib.sh case 15
- **Commit hash**: 48476322ab
- **First error excerpt**: case 15 took ~3 real minutes (no path to done) before the fix; a separate tempDirTrapGuard violation found in a different ticket own new bb file during re-verification
- **Failure class**: efficiency
- **Expected vs observed**: a shell test should not spin real wall-clock minutes when a seam already exists to reach done quickly; a fixture creating a temp root needs a try/finally cleanup
- **Blamed role**: coder
- **Remediation pointer**: fixed in this pass: case 15 gained the same background sent.json writer case 11 uses; the BL-1458 runner wrapped in try/finally

By hardener.
