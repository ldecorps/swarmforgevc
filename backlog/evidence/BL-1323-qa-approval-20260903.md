# BL-1323 — QA approval (20260903)

Received: documenter commit `51d5e92187` (doc-only, forwarding the merge
unchanged per no-op rule — confirmed NONE finding), forwarding hardener
(no-op-hardening pass), architect repass, cleaner rework (D1 fix), coder's
BL-848 stamp-off review of hotfix `9c94735f03`. Merged into QA.

This is a review-only BL-848 stamp-off parcel: it confirms or refutes
already-landed hotfix `9c94735f03`, never reimplements it, and does not
itself write a `certified`/`waived` decision into
`backlog/hotfix-ledger.yaml` — that stays the human's action.

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `bb swarmforge/scripts/test/master_main_reconcile_lib_test_runner.bb`:
  ALL TESTS PASS.
- `bb swarmforge/scripts/test/babysitterd_sweep_lib_test_runner.bb`: ok.
- Acceptance (`specs/features/BL-1323-main-sync-deadlock-hints-name-
  overlaps-and-teach-swarm-heal.feature`): 7/7.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1323MainSyncDeadlockOverlapHintsStampSteps` (line 20) — confirmed.
- Declared invariants
  (`extension/test/bl1323StampOffInvariants.property.test.js`): 3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated `master_main_reconcile_lib.bb`'s two "./swarm heal"
  instances (lines 715/717) to a placeholder string — invariants 2 and 3
  both failed exactly as claimed (2/3 tests red). Restored (`git diff
  --stat` empty), re-ran `master_main_reconcile_lib_test_runner.bb` ALL
  TESTS PASS and the property test 3/3 clean.
- **Reviewed hotfix sources confirmed untouched by this parcel**:
  `git diff origin/main swarmforge-QA -- swarmforge/scripts/
  babysitter_check.bb swarmforge/scripts/handoffd.bb swarmforge/scripts/
  master_main_reconcile_lib.bb` — empty. The parcel's own diff is
  confined to the new acceptance step handler, its registration, a new
  property test, and evidence files, matching every stage's own claim.
- **Landed behaviour independently confirmed** (not read off the evidence
  chain alone): `babysitter_check.bb::gather-main-sync-deadlock` at line
  799 normalizes the marker's `overlapping_paths` first, and the
  recompute at line 806 uses the corrected `(sh! "git" "-C" ...)` shape —
  the exact defect this hotfix fixes (a malformed `sh!` call that got
  silently swallowed).
- Hotfix ledger row for `9c94735f03`: `state: stamp-open`,
  `human_decision` unset — untouched by this parcel, as required (invariant
  2 of the ticket).
- Documenter's NONE finding independently verified:
  `docs/how-to/BL-891-master-main-reconcile-sweep.md`'s last touching
  commit is `9c94735f03` itself and it already describes
  `overlapping_paths` persistence and the `./swarm heal` instruction —
  confirmed by reading the file, not merely the evidence's claim.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical to the
  standing-debt population from every prior pass this session. None touch
  this parcel's files.
- Property suite (`npm run test:properties`): 24 files / 13 tests red, same
  standing-debt population, plus the allowlisted BL-871 vitest-worker
  timeout. BL-1323's own property test — 3/3 green.
- Coder's property-suite-guard override (`SWARMFORGE_SKIP_PROPERTY_SUITE_GUARD=1`)
  independently plausible: `test/bl1253TokenOwnershipInvariants.property.test.js`
  and `test/bl1295RevertAttributionInvariants.property.test.js` are
  confirmed absent from `property_suite_standing_allowlist.tsv` (grep, 0
  hits) — consistent with the coder's "arguably should be" note; neither
  file nor any module either exercises appears in this parcel's diff.
- Orphaned test processes: none after the property-suite run.

## Verdict

APPROVE. No open item. This parcel does not certify or waive the hotfix
(that stays a human decision on the ledger row, separate from this QA
approval) — landing this QA merge commit on `main` closes BL-1323's own
review-and-tooling deliverable.

By QA.
