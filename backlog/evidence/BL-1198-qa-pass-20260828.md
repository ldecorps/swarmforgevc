# BL-1198 — QA pass — 20260828

**Received:** `merge_and_process documenter 7c155961b0` (handoff
`00_20260828T015120Z_000878_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `7c155961b040b7cecf5354f7ad0f6aa4d0f76c9e`
**QA merge commit:** `d4c5314b2fab208464bfd74247850d3c028cec5a`

This is the ticket's SECOND pass through the pipeline. First pass bounced
2026-08-28 (`BL-1198-bounce-20260828.md`) for a missing acceptance step
handler; coder's re-fix (`c4641ae759`) added it and the parcel re-ran the
full remaining chain (cleaner → architect → hardener round 2 → documenter).

## Merge

Clean, no conflicts (`git merge --no-ff 7c155961b0`).

## BL-336 ancestry guard

- `git merge-base --is-ancestor daefb98c6 HEAD` (coder, original fix) — CONFIRMED
- `git merge-base --is-ancestor c4641ae75 HEAD` (coder, bounce re-fix: step handler) — CONFIRMED
- `git merge-base --is-ancestor 0b095c40d HEAD` (cleaner, round 2) — CONFIRMED
- `git merge-base --is-ancestor 9a157aa81 HEAD` (hardener, round 1) — CONFIRMED
- `git merge-base --is-ancestor 7c155961b0 HEAD` (documenter, received tip) — CONFIRMED

## Wiring verification (BL-149 lesson)

All three real call sites confirmed calling the shared primitive, not a
reimplementation:
```
handoffd.bb:3153: (master-main-reconcile-lib/rematch-with-push-first! ...)
swarm_heal.bb:93: (master-main-reconcile-lib/rematch-with-push-first! ...)
post_hotfix_merge_origin.bb:71: (master-main-reconcile-lib/rematch-with-push-first! ...)
```
No stray raw `git reset --hard origin/main` call sites remain outside the
primitive (`reset!` adapters) — the two remaining greps for that string are
docstrings, not executable code. Each site's `:push!` adapter reuses an
existing push mechanism (`push-sweep-push!` in handoffd.bb, already used
elsewhere in that file; a local `push-onto-origin!` wrapping the same `git
push origin main` in the other two) — no second push path invented, per the
ticket's own constraint.

## Verification inventory

| Gate | Command | Result |
|---|---|---|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1198` | `VERIFY BL-1198` — no open deferral |
| Compile | `npm run compile` | PASS, clean |
| `master_main_reconcile_lib_test_runner.bb` | `bb ...` | ALL TESTS PASS |
| `master_main_reconcile_lib_property_runner.bb` | `bb ...` | 500 runs, ALL PROPERTIES HOLD; explicit non-vacuity: "a mutant that always resets regardless of push success is flagged" |
| `test_swarm_heal_push_before_reset.sh` (real-git wiring) | `bash ...` | 4/4 PASS (ahead-only left untouched + stays unpushed; genuine-divergence reset recovery unchanged; regression guard for real-only-local commit still discarded) |
| Acceptance | `run_acceptance.sh specs/features/BL-1198-...feature` | 2/2 PASS — scenario 01 is the incident replay (qa_e2e_procedure item 3), scenario 02 is the divergence regression guard (item 2) |
| Full unit suite | `npm run test` (extension/) | 206 failed / 8987 passed (9193) — **standing red, not a regression**: exact same count as this session's immediately-prior QA passes (`BL-1186-qa-pass-20260828.md`, `BL-1189-qa-pass-20260828.md`), same CURSOR_API_KEY-env-gap cascade (`bridgeServer.test.js`, `topicMakeTopBridge.test.js`, `epicReorderBridge.test.js`, etc. — all `extension/src/bridge/**` consumers of `createLiveCursorBridgeAgentSession`, which this sandbox has no ambient `CURSOR_API_KEY` for). None of the 206 failing files is a BL-1198-touched path (BL-1198 touches only `swarmforge/scripts/*.bb` and `specs/pipeline/steps/bl1198*` — zero overlap with `extension/src|test`). |
| Property suite | `npm run test:properties` | 18 failed / 672 passed (690) — same standing-red count as `BL-1186-qa-pass-20260828.md`; failures are `selfHealTelemetry.property.test.js`, `unreachableStepHandlerCheck.property.test.js` (a `checkOrphanedAuthoredDocs` wiring gap in `pilotAcceptanceGate.ts`, untouched by BL-1198) and siblings — none touch BL-1198's files. The two `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled-error lines are the known-benign BL-871 artifact, allowlisted verbatim. |
| Docs | grep | `docs/how-to/BL-1198-rematch-reset-must-push-before-discarding-local-ahead-commits.md` present, linked from `docs/index.md:196`; `docs/reference/Specification.MD` top entry updated |
| Orphaned processes | `pgrep -fl '[n]ode --test\|[s]tryker'` | none, before and after |

## Article 4.5 (changed-path unit test gate)

BL-1198's own changed production paths are exclusively Babashka
(`master_main_reconcile_lib.bb`, `swarm_heal.bb`, `post_hotfix_merge_origin.bb`,
`post_hotfix_merge_origin_lib.bb` docs, `handoffd.bb`) plus two new
acceptance-layer files (`specs/pipeline/steps/bl1198RematchPushFirstSteps.js`,
`specs/pipeline/steps/lib/bl1198RematchPushFirstCli.bb`). No `extension/src`
production file is touched by this ticket's own diff, so CRAP/DRY/Stryker
tooling (TypeScript-only) is correctly not applicable — matching every prior
stage's own disposition. The Babashka side has no wired
mutation/CRAP/DRY tooling (engineering.prompt); unit + property (with
mutation/non-vacuity proof) + real-git wiring + acceptance above is the full
applicable verification surface, all green.

## Inventory (Article 4.4)

NONE — no defect attributable to BL-1198's own work on this pass.

## Verdict

**PASS.**

Landing on `main` per BL-1144 discipline (lock, decide-only, push, release).
BL-1198 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator notified for backlog bookkeeping (BL-1198 active → done).

By QA.
