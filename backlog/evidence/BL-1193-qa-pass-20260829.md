# BL-1193 QA Pass — 20260829

## BL-1241 entangled-tip rebuild

The documenter's handoff named commit `bf7895a35d`. Its ancestry ran back
through the SAME stale tangle BL-1273's did (the abandoned, now-superseded
old BL-1273 documenter tip, carrying the BL-1246 and
BL-1267/1268/1220/603-step-scoping siblings BL-1273's own QA pass already
found and rebuilt around). Rather than merge that tangle a second time,
rebuilt tip-pure again: cherry-picked BL-1193's three plain, cleanly-scoped
commits (coder `6b7ad7ee7e`, hardener `6f5752d069`, documenter `bf7895a35d`
— each verified via `git show --stat` to touch only BL-1193's own files)
directly onto the already-landed post-BL-1273 HEAD. `abandoned_commits`
records the full superseded chain. `git diff --stat origin/main HEAD`
confirmed exactly BL-1193's own eight files, nothing else — no new
casualties beyond what BL-1273's own pass already accounted for.

## Commit under review

`5e5b420c4` (tip of the rebuilt chain, HEAD of `swarmforge-QA`). BL-1193's
own three commits (`1ef491f0c`/coder, `fb588fa88`/hardener, `50f8c5332`
/documenter — content-identical cherry-picks of `6b7ad7ee7e`/`6f5752d069`
/`bf7895a35d`) are ancestors of it, confirmed via
`git merge-base --is-ancestor`.

## Functional verification

- `extractRetiredReferents` is wired into the real caller chain:
  `loadRetiredTokens` calls it per RETIRED-marked line (line 495 of
  `deprecate-check.ts`), `loadRetiredTokens` feeds
  `collectRetiredSurfaceHits` at the CLI's actual decision path (line 722)
  — not dead/unit-tested-only code.
- Manual repro of the ticket's own headline case:
  `node extension/out/tools/deprecate-check.js . BL-1190` → `{"decision":
  "allow"}` (was the false HOLD naming "Mint" this ticket exists to fix).
- Manual repro of the documented yield claim: `loadRetiredTokens('.')`
  against the live docs tree → `["type: bug"]`, matching the documenter's
  claim exactly ("dropped from four tokens (none retired) to the one real
  referent, `type: bug`").
- Regression on BL-1173's true-positive coverage (the ticket's own explicit
  constraint): `run_acceptance.sh
  specs/features/BL-1173-deprecator-freshness-gate-cli.feature` → 5/5 pass,
  including "check holds when depends_on are done but the ticket names
  retired behaviour."

## This pass's verification

1. **Compile**: `npm run compile` → clean, exit 0.
2. **BL-1193's own tests**: `deprecateRetiredReferents.test.js` (9/9),
   `deprecateRetiredReferents.property.test.js` (2/2, via
   `vitest.properties.config.mjs`).
3. **Surrounding module regression**: every other `deprecate*` test file —
   `deprecate.test.js` (14), `deprecateCheck.test.js` (21),
   `deprecateIdentifyUnused.test.js` (16), `deprecateAdjudication.test.js`
   (13) — 64/64 pass; property counterparts `deprecate.property.test.js`
   (4), `deprecateCheck.property.test.js` (5),
   `deprecateCheckSelfClaimBranch.property.test.js` (2),
   `deprecateAdjudicationDischarge.property.test.js` (4),
   `deprecateIdentifyUnused.property.test.js` (5) — 20/20 pass.
4. **Acceptance**: `run_acceptance.sh
   specs/features/BL-1193-deprecator-retired-token-must-name-the-actual-retired-item.feature`
   → 3/3 pass.
5. **Article 4.5 changed-path**: the sole changed production path is
   `extension/src/tools/deprecate-check.ts`; the dedicated new test files
   (item 2 above) exercise the exact changed function directly, and item 3
   covers every other consumer/sibling of the same module. No `.bb`/`.sh`
   paths changed by this ticket.
6. **Unit suite**: `npx vitest run` → 20 failed files / 33 failed tests /
   9593 passed (572 files — +1 file, +9 tests over BL-1273's same-session
   baseline of 571/9584, exactly matching
   `deprecateRetiredReferents.test.js`'s own 9 tests; the 20/33 failing
   counts are IDENTICAL to that baseline). Zero overlap between the failing
   files and BL-1193's changed-file list.
7. **Property suite**: `npm run test:properties` → 25 failed files / 14
   failed tests / 732 passed (269 files, +1 file/+3 tests over baseline —
   `deprecateRetiredReferents.property.test.js`'s own 2 tests plus one
   fewer failing test elsewhere), plus 2 allowlisted `[vitest-worker]:
   Timeout calling "onTaskUpdate"` (BL-871) unhandled errors (was 1 in the
   same-session BL-1273 baseline — same known-benign class, just fired
   twice this run). Diffed the failing-file set against the BL-1273
   same-session baseline directly: the only difference is
   `test/bl968MaterializedGuardSensitivity.property.test.js`, which failed
   in that baseline and PASSED here — a flip toward green, not a
   regression (matches this project's known real-clock/host-contention
   flake pattern for that test class). No file newly failing.
8. **Regression**: no new failing test file or test in either suite
   relative to the same-session BL-1273 baseline; both changed-file counts
   are explained exactly by BL-1193's own two new test files.
