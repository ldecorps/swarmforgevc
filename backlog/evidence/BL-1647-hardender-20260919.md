# BL-1647 — hardender review pass, 2026-09-19

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: stubbed ps -o stat= to return a leading-whitespace Z+ reading via a shell function override, sourced finish_shift_lib.sh, called _finish_shift_pid_is_zombie directly (no fixture root, no real process - cheap and load-insensitive)
- **Commit hash**: 105561076b
- **First error excerpt**: a bare [[ $stat == Z* ]] comparison missed a leading-whitespace reading (this project own reap_stale_tmp_roots.js documents the identical fix for the identical check: isZombiePid matches /^\s*Z/ rather than a bare prefix, for exactly this stock-macOS-ps reason)
- **Failure class**: portability-gap
- **Expected vs observed**: the zombie check tolerates a leading-whitespace ps -o stat= reading on any target platform (macOS and Linux)
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added a portable bash 3.2 trim before the comparison and case 10 pinning it via the same ps-stub technique; commit 105561076b; confirmed the mutant (no trim) fails the new case

## D2

- **Failing command**: npx vitest run test/stepHandlerTmpRootGuard.test.js (BL-1636 standing guard)
- **Commit hash**: 105561076b
- **First error excerpt**: bl1647ZombiePidfileNotAliveSteps.js created its fixture root with no crash-safety net - cleanupFixture ran only from terminal Then steps, same class defect as BL-875/BL-1639/BL-1645 this session
- **Failure class**: resource-leak
- **Expected vs observed**: a step handlers mkdtemp root is registered with the shared fixtureReaper primitives (BL-1636 standing guard)
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added onAbnormalExit(() => cleanupFixture(state)) in the Background step; commit 105561076b; guard now green

By hardender.
