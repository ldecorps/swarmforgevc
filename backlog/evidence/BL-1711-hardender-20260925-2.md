# BL-1711 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second same-day hardener pass, after QA's bounce (3 defects: D1/D2
coder's, blamed and fixed; D3 mine, from my own earlier pass's
`restart-fails-to-come-up` fixture addition). Cleaner's own pass
(`BL-1711-cleaner-20260925-2.md`) additionally caught a THIRD instance
of this session's recurring collateral-damage pattern — the coder's
D1/D2 merge silently dropped my whole prior hardening pass
(just-below-bound category, restart-fails-to-come-up category, the
entire daemon-wiring test) — restored it and updated two assertions
for D2's new `RESTART_FAILED` token, confirmed non-regressed.

Fixed D3 myself (own domain — a leaked process from my own earlier
fixture code): `fake-ollama-never-serves`'s `serve` branch ran `sleep
infinity` as a plain FOREGROUND command. Since the script is not its
own last statement (`exit 0` follows `fi`), bash forks a real child
rather than tail-calling into it; `ollama_ancillary_stop_pid`'s TERM
lands on the wrapper's own pid, and bash's default response to SIGTERM
while waiting on a foreground child is to die WITHOUT signalling that
child — the sleep is reparented to a subreaper and never ends. QA found
5 orphans from my own hardening runs and 1 from QA's own property-lane
run. Fixed by backgrounding sleep and trapping TERM to kill it
explicitly. Added a `cleanup()` backstop sweeping by the fixture's own
unique mkdtemp root in any surviving process's cmdline.

Verified non-vacuity by hand, and in the process found my OWN
verification method from the earlier pass was flawed: reverted the
trap fix (kept the new backstop), confirmed a genuine orphan appeared
— but my original check (`ps ... | grep "fake-ollama-never-serves"`)
found NOTHING, because the orphan's own argv is bare `sleep infinity`
once its parent wrapper has died (the wrapper's name dies with the
wrapper). Corrected the check to look for young `sleep infinity`
processes by `etimes`, confirmed the leak, then reverted BOTH the trap
and the backstop together to reproduce the full original bug, and
restored both, reconfirming clean across two full property-lane runs.

Also discovered and worked around a genuine `pgrep -f` self-match
hazard while diagnosing the above: `pgrep -f <pattern>` invoked via
`spawnSync` from a shell-wrapped diagnostic command matched pids that
turned out to be transient/self-referential, even for a pattern
guaranteed to correspond to no real process. The fixture's own
`cleanup()` backstop uses `ps -eo pid,args` + a JS substring filter
instead, which embeds no search pattern in its own command line and
cannot self-match. (The pre-existing `anyProcessMatching` helper, used
by the property test's own assertion, was independently confirmed
NOT affected in real test execution — its `pgrep -f` invocation runs
from within the vitest worker process, whose own command line never
contains the search pattern, unlike my manual diagnostic probes typed
through the interactive shell tool's own wrapper.)

Separately: while running the full extension suite, found a
pre-existing, unrelated standing red
(`nightClosingCeremonyRun.test.js`, 3/20 failing) with no register row
and no owning ticket (its origin ticket BL-1641 is already `done`).
Confirmed unrelated to this pass's own diff by reproducing it with
this ticket's own uncommitted work stashed away, and after a fresh
`npm run compile`. Sent an `unowned-red` note to the specifier
(Article 4.2/BL-1063) with full repro detail in
`backlog/evidence/unowned-red-nightClosingCeremonyRun-20260925.md`;
not routed as a parcel, no code change needed on this ticket for it.

No TS/JS production file in this ticket's own scope is touched by my
D3 fix (test fixture helper only) — CRAP/DRY not run.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1711-….feature`: 8/8.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1711OllamaCrashRestartInvariant.property.test.js`: 1/1, run
  twice, confirmed zero `sleep infinity` orphans after each run.
- `bash swarmforge/scripts/test/test_handoffd_ollama_crash_restart_sweep_wiring.sh`:
  ALL PASS (5 cases).
- `bash swarmforge/scripts/test/test_handoffd_ollama_restart_alert_text.sh`
  (D2's new test): ALL PASS (4 cases).
- `npm test` (extension/): 638 files / 10875 tests; 3 failures, all in
  the unrelated, pre-existing `nightClosingCeremonyRun.test.js` (see
  above) — every other file green.
- `git status --short` clean before each commit.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner; host load moderate throughout, no fallback needed.

By hardender.
