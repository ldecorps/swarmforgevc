# BL-1385 — QA bounce, 2026-09-04

Merged documenter `d667ddb57e` into QA worktree. Independent full review
before sending back (Article 4.4 — complete inventory, one bounce).

## D1 — required_stages violated: cleaner, architect, and hardener never
reviewed the coder's concurrency-fix rework

**Failing check:** process/lineage audit, not a test command.
**Commit range inspected:** `origin/main..2cab5597226683d3246a26fb199757bc1b8d07fd`
(the QA-worktree tip carrying BL-1385) via `git log --oneline --all --grep=BL-1385`.
**Failure class:** behavior (a required pipeline stage's pass is missing
entirely for this ticket's own content — same shape as BL-575/BL-576, not
a code defect in what WAS reviewed).

**What happened:** BL-1385's ticket YAML declares
`required_stages: [coder, cleaner, architect, hardender, documenter, qa]`.
Documenter's own bounce (`backlog/evidence/BL-1385-documenter-bounce-20260904.md`,
`e542f740c2`) sent the parcel back to **coder** because the concurrency
fix (invariant 3 / scenario 07-10, the ticket's in-flight amendment
`cf2ea583c7`) was missing from the forwarded commit. Coder reworked it
(`839873ab82`, "reaping is scoped to roots no live run owns"), rewriting
`swarmforge/scripts/check_handler_module_graph.sh`'s reaping logic AND its
`out/`→`src/` existence-check block (now `existsOnTree` three-way,
replacing bare `fs.existsSync`). That commit was merged **directly into
documenter** (`7df559255e`, "Merge coder 839873ab82 for BL-1385 rework...
into documenter") — confirmed by `git log`, no cleaner/architect/hardener
commit for BL-1385 exists between `839873ab82` and QA's receipt.

Per this prompt's own Handoff rule ("The bounced-to role fixes in its own
worktree and forwards down the REMAINING chain to QA, so every gate after
the fix still runs") the remaining chain after coder is
cleaner → architect → hardener → documenter → QA. Three of those five
stages never ran against the reworked commit.

**Independent confirmation this isn't paperwork-only:** re-ran hardener's
own hand-authored mutation sweep,
`bash swarmforge/scripts/test/bl1385_handler_module_graph_mutation_sweep.sh`,
against the current tip:

```
mutants: killed=8 survived=0 equivalent=0 skipped=1
SKIPPED (stale anchors, unrun):
  out/->src/ candidate list emptied (invariant 2, present-on-tree direction)
```

Hardener's own evidence (`backlog/evidence/BL-1385-hardener-pass-20260904.md`)
claims `9/9 killed, 0 survived` — but that pass (commit `87275f1666`,
12:17:55) ran **before** coder's rework (`839873ab82`, 12:22:50), which
refactored exactly the block mutant 6 targets (an inline `for (const cand
of [...])` became `const cands = [...]; for (const cand of cands) {` plus
a `.map(existsOnTree)` null-check, per `check_handler_module_graph.sh`
lines 238-246 today). The sweep's anchor text no longer matches, so
invariant 2's present-on-tree mutation is **unverified against the code
that would actually ship** — not proven broken, but not proven caught
either, which is what hardener's gate exists to close out.

(The BL-1381 hardener pass, `d604b33acf`, mentions re-running "the combined
BL-1385 suite (13/13)" while resolving merge conflicts on a different
ticket — that is acceptance, not this mutation sweep, and it is incidental
to BL-1381's own pass, not a governed BL-1385 hardener gate: no BL-1385
hardener evidence file, no `bounce_history` entry, no re-stamped mutation
manifest for this ticket.)

**Not in dispute:** the fix's functional behavior. Acceptance
(`specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1385-a-handler-that-cannot-load-never-reaches-main.feature`)
is 13/13 green, including scenario 13 ("two invocations running at once
each reach their own verdict") — the concurrency fix itself works. This is
a missing-gate defect, not a behavior defect: cleaner never reviewed the
refactor for duplication/readability, architect never reviewed the
`existsOnTree` design change, and hardener never re-ran mutation coverage
against the code as it stands now.

**Expected vs observed:** Expected — every required stage's pass on the
content that reaches QA, per `required_stages` and the Handoff protocol's
"remaining chain" rule. Observed — coder's rework commit reached
documenter directly; cleaner/architect/hardener's evidence trail predates
it entirely.

## Everything else checked, clean (Article 4.4 full inventory)

- `npm run compile` (extension/) — clean.
- No `extension/` paths touched by this ticket's diff (`git diff
  --name-only 2cab5597226683d3246a26fb199757bc1b8d07fd HEAD` — 0
  `extension/` hits), so the standing unit/property-suite debt already
  catalogued for this session (BL-1221/1229/1263/1265/627, standing reds)
  is unaffected; not re-run in full for this bounce.
- `required_wiring` — all three anchors present and live:
  `land_step_lib.bb:993` (`check_handler_module_graph.sh` in the tree-guard
  list), `run_commit_guards.sh:73` (`run_guard check_handler_module_graph.sh`),
  `bl1385HandlerModuleGraphGuardSteps.js::registerSteps` (line 74/244).
- Acceptance — 13/13 pass (above).
- Diagram currency — not re-checked separately; documenter's own domain,
  no diagram-affecting change in this ticket's description.

## Disposition

Bounce to **cleaner** (earliest of the three stages whose pass is
missing) with the full remaining chain (cleaner → architect → hardener →
documenter → QA) to re-run against `839873ab82`'s content. Not coder's
fault this time — the fix itself is sound; coder is not blamed.

By QA.
