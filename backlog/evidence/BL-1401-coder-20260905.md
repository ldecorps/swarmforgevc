# BL-1401 — coder evidence, 2026-09-05

Ticket: the BL-632 acceptance handler derives its fixture's guard set from
`run_commit_guards.sh` through BL-1398's helper.

## What shipped

`specs/pipeline/steps/bl632CommitTimeGuardSteps.js` lost its hand-written copy
list — twelve `path.join` constants and the pair list that walked them — and now
calls `deriveCommitGuardFixtureSet({ repoRoot: REPO_ROOT }).files`, the helper
BL-1398 shipped for exactly this. `GUARD_SCRIPT` survives because two later
steps invoke that guard directly; every other constant is gone rather than left
unused.

Consuming the helper, rather than re-parsing the runner here, **is** invariant
3: exactly one parser of `run_guard` lines exists in the repo. The handler
mentions `run_guard` once, in the comment that explains why it does not parse
them.

## The red this closes

`run_acceptance.sh specs/features/BL-632-commit-time-guard-refuses-pipeline-code-on-main.feature`
was **4 pass / 7 fail** — the "allowed" scenarios failing because the fixture's
chain died on the missing `check_handler_module_graph.sh` with exit 127, while
every guard was behaving correctly. It is now **11 pass / 0 fail**.

That feature is the commit-time guard's only executable acceptance, so while it
was red no change to that guard could be gated.

## Runs

| What | Command | Result |
|---|---|---|
| the repaired feature | `run_acceptance.sh …BL-632….feature` | **11 pass / 0 fail** (was 4 / 7) |
| e2e | `bash swarmforge/scripts/test/test_bl1401_acceptance_fixture_derives_set.sh` | **ALL PASS, 6 checks** |
| acceptance (this ticket) | `run_acceptance.sh …BL-1401….feature` | **3 pass / 0 fail** |
| properties | `npx vitest run --config vitest.properties.config.mjs test/bl1401AcceptanceFixtureDerivedSet.property.test.js` | **2 passed / 2** |

Scenario 01's claim is answered by **running BL-632's feature**, not by reading
the handler for a literal: "the feature is green" is the whole point of the
ticket, and a grep could not tell the difference between a green feature and a
handler that merely looks right.

## Declared invariants (BL-654) — all three encoded

`extension/test/bl1401AcceptanceFixtureDerivedSet.property.test.js`.

Invariants 1 and 2 are properties of the helper the handler consumes, driven
over generated runner seams with both branches asserted reached (a complete
seam, and one whose named guard is deliberately not planted — constructed, so
every case is a genuine present/absent decision). Invariant 3 is a property of
the **live handler**: it consumes the helper, carries no regex over the
runner's guard lines, and no longer holds the old copy list. A helper that were
correct but unused would satisfy neither of the first two, which is why that
third assertion reads the handler itself.

## Out of scope

BL-1398's helper is unchanged — this ticket consumes it. `bl1252`'s hand-
enumerated `INDEX_GUARDS`, named in BL-1398's evidence as the same shape in a
third place, is still unminted; it is not this parcel's.
