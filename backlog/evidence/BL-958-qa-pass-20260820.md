# BL-958 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-958 — full-forge crash: tmux control plane disappears while daemons stay up (`type: defect`, `severity: high`, milestone M8)
- **Parcel**: `00_20260820T000715Z_000217_from_documenter_to_QA_for_QA` (from documenter, commit `bc9f276c06`)
- **Verified at**: `acbfe8cd6` (QA merge of the documenter tip)
- **Reviewed by**: QA, 2026-08-20
- **Prior bounces**: 1 — architect→coder, `behavior`, `ec033029e2` (D1: the recovery decision was inert; `:halt` reported FIXED and resolved the incident). **D1 is closed and gated** — see G4.

## Verdict

**PASS — inventory items: NONE.**

**Landing DEFERRED** pending BL-960 (the last entangled sibling); see BL-957's inventory for the rationale.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — architect bounce `1ca933f155`, coder D1 re-fix `6998d9c9c`, hardener `bcfb3aa56`, documenter `6dcee5930`/`bc9f276c06` | PASS — all ancestors of the cited commit |
| G2 | Prior-bounce check vs `main` and `origin/main` (BL-891) | PASS — the one prior bounce is closed by `6998d9c9c` |
| G3 | Ensure suite — BL-958's own control-plane cases | PASS — `BL-958: control-plane loss is classified, recovered, reported, and its incident resolved`; whole suite `ALL PASS`, exit 0 |
| G4 | **Prior-bounce D1 specifically** | PASS — `BL-958 D1: :halt is honored - no recreation churn, FAILED with escalation, incident preserved`. The exact defect the architect bounced is now a named, passing assertion |
| G5 | `control_plane_lib_test_runner.bb` | PASS — `ok` |
| G6 | `bl958_control_plane_property_runner.bb` | PASS — `ok (400 runs, 400 loss, 400 bursts, 193 resolved-then-new, 201 no-scripts)` — a real distribution across both recovery branches, not vacuous |
| G7 | `swarm_status_lib_test_runner.bb` | PASS — `ok` |
| G8 | Acceptance — `BL-958-full-forge-tmux-control-plane-crash-root-cause.feature` | PASS — 5/5, 0 fail, exit 0 |
| G9 | `required_wiring` — all three entries, checked at CALL SITES not imports | PASS — `swarm_status.bb:299,306` (`observe!`, `status-agents-view`), `handoffd.bb:1535,1543` (`record-chase-failure-incident!`, `incidents-file`), `swarm_ensure.bb:717-754` (`observe!`, `launch-scripts-present?`, `recovery-decision`, `response-policy`, `probe-server!`, `resolve-open-incidents!`) |
| G10 | Docs currency | PASS — `docs/how-to/BL-958-control-plane-loss-recovery.md` added, `docs/index.md` and `Specification.MD` updated |
| G11 | **Architecture diagram currency** (project Diagrams rule) | PASS — `docs/diagrams/architecture.mmd` updated in the SAME parcel that changed what it depicts, naming `control_plane_lib.bb` as the one classifier and all three consumers |
| G12 | Full unit suite | PASS (inherited) — 443/444 files, 7894/7895 at `8133043cd9`; this parcel changed only docs, evidence and `test_swarm_ensure.sh`, none of which the extension suite covers, and the ensure suite was re-run here at G3 |
| G13 | `pre_qa_gate.sh` | **FAIL — adjudicated as a known false positive.** See below |
| G14 | Functional change present | PASS — shared lib + three consumers + property runner + acceptance + docs + diagram |

## G13 — why the red pre-QA gate is not a BL-958 defect

```
PRE_QA_GATE_FAIL ancestry BL-958 97d1d0e92d stranded on swarm/coder
```

`97d1d0e92d` is **not a BL-958 commit**. It is the CODER's response to QA's own BL-571 bounce, committed 2026-08-20T03:22 — after this parcel — and its subject reads:

> "Merge QA bounce acf5198588 (BL-571 D1) into coder: re-deliver the QA-verified parcel content for every BL-571 path; keep index.js in-flight registrations; **BL-958** stale bounced riders stay out per the bounce ruling."

The gate matches ticket ids in the commit SUBJECT, so the words "BL-958" in that sentence — which say the opposite, that BL-958 riders were deliberately EXCLUDED — are read as a stranded BL-958 commit. This is the identical, already-documented failure mode recorded in BL-935's own ticket notes for `e63d364863` ("the pre-QA ancestry check reads as a stranded BL-935 commit only because its SUBJECT ends '...BL-935 lineage'. It is not one.").

Verified by CONTENT, not by the subject line:

- `git diff HEAD 97d1d0e92d | grep '^+' | grep -c 'BL-958'` → **0**. It adds no BL-958 line this branch lacks.
- Its five touched files are BL-571's paths plus `swarm_ensure.bb`/`test_swarm_ensure.sh`.
- `git merge-base --is-ancestor acf5198588 97d1d0e92d` → true: it descends from QA's BL-571 bounce commit.
- All BL-958 content is present here: `control_plane_lib.bb` exists, 15 BL-958 assertions in the ensure suite, `bl958` step handler registered.

No `abandoned_commits` declaration is warranted, because the commit is not abandoned: it is BL-571's live re-fix, already queued back to QA. **The gate self-clears once BL-571's re-fix lands** — no action is owed by BL-958.

## Note on `qa_e2e_procedure` steps 1–6

The procedure asks for a live full-forge run with a forced tmux control-plane disappearance. That was not performed against the live swarm — deliberately: the live swarm is running this pipeline, and forcing its control plane down to test a fix would take the whole pack out (Article: the coordinator's pane exiting tears down the entire swarm).

Every step's PROPERTY was instead verified against the fixture-driven equivalents the ticket itself ships, which is what the hardener and architect gated on:

| Step | Covered by |
|---|---|
| 2–3 classify loss, not generic DOWN | G3 ensure case + G7 `swarm_status_lib_test_runner` (ONE control-plane row, not stale per-role rows) |
| 4 recovery restores sessions | G3 (`recovered ... FIXED` after re-probe) |
| 5 react per policy, not silent degradation | G4 (`:halt` → FAILED with escalation, no churn) + G6 property `no-scripts` arm, 201 runs |
| 6 one actionable incident artifact | G3 (incident resolved after recovery), G4 (incident PRESERVED under `:halt`), G6 `resolved-then-new` arm, 193 runs |

Recorded as substituted-and-covered, not as run-as-written.

By QA.
