# BL-1246 QA Pass — 20260829

## BL-1241 entangled-tip rebuild

The documenter's handoff named commit `460a3f052b`. Its ancestry ran back
through the coder's long-lived branch, still carrying the same stale
step-scoping tangle BL-1273/BL-1193/BL-1251/BL-1209's own QA passes already
found and rebuilt around — BL-1246's own three commits
(`3c07857a5`/`26dcc3667`/`460a3f052b`) were each independently clean and
self-contained (verified via `git show --stat`); the tangle is entirely
from other commits sharing the branch. Rebuilt tip-pure onto the
already-landed post-BL-1209 HEAD.

Also rescued one genuine casualty found in the same audit: the architect's
own evidence record (`6d6f2569f`, "Record untracked tmpDirMigrationGuard
standing red found reviewing BL-1209") — a real, unlanded finding (the same
BL-1280-tracked standing red I independently confirmed during BL-1209's own
QA pass), no conflict with anything, absent from `origin/main`. Cherry-picked
alongside BL-1246's own commits.

`abandoned_commits` records the full superseded chain. `git diff --stat
origin/main HEAD` confirmed exactly BL-1246's own nine files plus the one
rescued evidence file.

## Commit under review

`a931248c8` (tip of the rebuilt chain). BL-1246's own three commits
(`2bc583a9c`/coder, `a258f79d4`/hardener, `d5a599368`/documenter —
content-identical cherry-picks of `3c07857a5`/`26dcc3667`/`460a3f052b`) are
ancestors of it.

## Functional verification

- Read `nestedGitRepoGuard.js` in full: the BL-1246 exemption
  (`gitIgnoresDirectory`) is fail-closed by construction — only a clean
  `git check-ignore --quiet` exit 0 exempts; exit 1 and any other outcome
  (missing git, non-repository root, spawn failure) still report the leak.
  Asked lazily, once per candidate leak, about the CONTAINING directory
  (never the `.git` path itself, which git never answers for).
- Hardener's addition read and confirmed sound: a new test pins the
  previously-uncovered branch (a non-repository root, where
  `check-ignore` can answer neither "ignored" nor "not ignored") still
  fails closed — this is the exact CRAP/mutation gap the hardener's own
  commit subject names.
- Manual check against the LIVE QA worktree:
  `findNestedGitRepositories(process.cwd())` → `[]`, no false positives
  on real state.
- Documenter's doc changes (`docs/reference/BL-1230-nested-git-repository-guard.md`,
  `Specification.MD`) read in full and match the code exactly: exit-code
  semantics, the four invariants (P1–P4), report-only posture.

## Regression / suite

- `nestedGitRepoGuard.test.js` → 23/23 pass.
- `nestedGitRepoGuard.property.test.js` → 5/5 pass (P1, P2 unchanged; P3
  non-vacuity — the exemption fires only for genuinely-ignored directories
  across generated trees that always carry both an ignored and tracked
  branch; P4 non-vacuity — the report follows the predicate, not a
  hardcoded `tmp` name).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1230-no-leaked-git-repository-inside-the-working-tree.feature`
  → 9/9 pass (was 8 before this ticket — the widened Outline row and the
  new "ignored-directory exemption does not silence a real leak beside it"
  scenario both run).
- Unit suite: `npx vitest run` → 19 failed files / 32 failed tests / 9610
  passed (572 files) — identical failing-file/test counts to the
  same-session BL-1209 baseline (19/32); passed count up by exactly
  BL-1246's own added assertions.
- Property suite: `npm run test:properties` → 25 failed files / 14 failed
  tests / 736 passed (270 files) — identical failing counts to the
  same-session BL-1209 baseline (25/14); passed count up by exactly 2,
  matching P3/P4. 2 allowlisted `[vitest-worker]: Timeout calling
  "onTaskUpdate"` (BL-871) errors, same as baseline.
- Article 4.5 changed-path: the sole changed production path is
  `extension/test/helpers/nestedGitRepoGuard.js` (a test-helper module);
  its own dedicated unit and property suites (above) are the direct and
  correct exercise of it.
