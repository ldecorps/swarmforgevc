# BL-888 QA pass inventory — 20260825

**Documenter tip:** `9cd08b5547`
**Task:** `BL-888-teardown-copilot-pkill-unscoped-kills-siblings`
**Sibling check:** `VERIFY BL-888` (exit 0)

## Lineage (BL-336)

| Commit | Role | Ancestor of tip? |
|--------|------|------------------|
| `fe692e7ec` rematch fix + property encode | coder | yes |
| `7726ba0f3` harden soft Gherkin + mutants | hardener | yes |
| `9cd08b5547` documenter tip | documenter | tip |

Prior architect bounce D1 (`invariant-unencoded` on `b91011afa5`) remediated
by rematch tip + `bl888_copilot_kill_scope_property_runner.sh`.

## Ticket gates (parcel-owned)

| Gate | Result |
|------|--------|
| Intent / wiring (`required_wiring` ×3) | PASS — matcher helpers in kill script; APS step registered; property runner encodes invariant |
| Shell unit `test_kill_pipeline_copilot_scope.sh` | ALL PASS (01–06) |
| Property runner (200 runs) | ALL PROPERTIES HOLD |
| APS `BL-888-teardown-copilot-kill-scope.feature` | 3/3 |
| BL-637 `test_lifecycle_script_scope.sh` | PASS=15 FAIL=0 |
| BL-730 APS regression | 7/7 |
| Docs currency (how-to + Spec + index) | PASS |
| Orphan `node --test`/`stryker` after runs | none |

## Inventory

**NONE** — no parcel-owned defect.

## Outside-parcel shared-lane reds (BL-1063 — report only, not bounce)

Full `npm test` / `npm run test:properties` are red on standing debt this
diff does not touch (hardener already recorded the same posture):

| Failure | Ticket / note |
|---------|---------------|
| `socketFixtureShortRootGuard` → `bl1112StandingUnitRedsSteps.js` | **BL-1112** (paused) |
| `tmpDirMigrationGuard` / `tempDirTrapGuard` standing scans | **BL-1112** / prior mkdtemp+trap tickets (done); new violators outside BL-888 paths |
| `backendSwitch` `cursor/auto` vs `auto` | **BL-1116** (active; seat model display hotfix) |
| `backfillEpic/StandingTopicIconsCli` | not named in active YAML; outside parcel |
| `hostActivityFeed` suite empty | **BL-833** evidence trail; outside parcel |
| property: BL-1116 / BL-1117 / BL-1113 stamp-off files | those active/done stamp tickets |
| property: `bl632CommitTimeGuardInvariants` | prior BL-632/BL-971 evidence; outside parcel |
| Unhandled `[vitest-worker]: Timeout calling "onTaskUpdate"` | allowlisted benign (engineering.prompt) |
| `bl796NvmNodePathFollowUpAdoptInvariants` | green this run (**BL-1063**) |

## Verdict

PASS — approve documenter tip lineage; land QA tip on `origin/main`.

By QA.
