# BL-1320 — documenter review pass, 2026-09-03

## Scope checked
- Ticket claim vs. handed-off commit `1569ebde9e` (hardender, merged into
  `swarmforge-documenter` at `271d4283cf`).
- The how-to at `docs/how-to/BL-1320-add-or-remove-a-seat-of-a-bottleneck-stage.md`
  against every item the ticket's `description.Wanted` list names: the add
  `window <stage>@<seat>` line and the removal edit, the bare-seat constraint
  and its refusal message, the difficulty-band → tier table plus the
  `model-steward role-matrix <role>` command, and a pointer (not a restatement)
  to BL-1001's page for how work then splits between seats.
- `docs/index.md` linkage for the new how-to page (Divio "one doc, one mode"
  rule: new authored docs get linked from the index in the same commit).
- Acceptance materialization and wiring: `specs/features/BL-1320-operator-step-for-adding-a-seat-to-a-bottleneck-stage.feature`
  present, `bl1320SeatOperatorStepSteps` registered in
  `specs/pipeline/steps/index.js`, and the four scenarios pass live
  (`node specs/pipeline/cli.js specs/features/BL-1320-operator-step-for-adding-a-seat-to-a-bottleneck-stage.feature`
  — 4/4 ok), satisfying the ticket's own invariant that no documented step is
  believed on prose alone.
- Diagram currency: change-trigger review found no diagram depicting
  per-seat pack config mechanics (BL-982/BL-1001 already shipped and
  documented elsewhere) — this ticket documents existing, already-diagrammed
  topology, not a new mechanism, so no diagram update is triggered.
- `docs/reference/Specification.MD`: this is a docs-only chore adding no new
  extension command, setting, or behavior (`ruling_options` picked "docs now,
  mint the optimizer-emits-a-recommendation slice as its own child" — the
  code-behavior slice is explicitly deferred to a future ticket) — no
  Specification.MD entry needed for this parcel.

## Findings

NONE.

- D1 (checked, no defect): all four `Wanted` items are present and correct in
  the how-to; the seat-id grammar (`<stage>@<seat>`, exactly one `@`, both
  halves non-empty), the bare-seat refusal message, and the
  `model-steward role-matrix coder` command are each exercised by a passing
  scenario, not merely asserted in prose.
- D2 (checked, no defect): the how-to correctly points to
  `BL-1001-difficulty-aware-coder-seat-routing.md` for post-seat work-splitting
  rather than restating it, matching the ticket's "Not this slice" exclusion.
- D3 (action taken, not a defect): the how-to was not yet linked from
  `docs/index.md` when this pass started — added in `documenter` commit
  `c6036e95a2`, in the how-to list near the sibling BL-1001/BL-1185/BL-1167
  seat-routing entries.

## Conclusion

No documentation defect found. Forwarding to QA.
