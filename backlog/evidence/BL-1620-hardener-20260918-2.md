# BL-1620 — hardener review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

This ticket returned to the hardener after a coder round trip (four
literal patterns + scenario 02's in-suite rewrite, commit `21e826f6c1`)
carried through cleaner (`07181f4f71`, NONE) and architect (`f4c1134db0`,
NONE). On merging the architect's commit and then `origin/main`, a
further specifier amendment landed mid-flight (`d5c495e702`, on the
cleaner's own spec-gap note): the feature's Background line became
ticket-free ("Given the extension unit lane with the BL-1598 pole
register", dropping "naming the file under BL-1620"), since the register
row's owner had moved to BL-1633 and hardcoding a ticket into that line
would go stale again the next time ownership moves. The specifier's own
commit message anticipated this exact situation ("holder merges and
changes one handler regex") - fixed directly here (commit `7e74a9e2eb`)
rather than a fourth coder round trip for a one-line regex, per the
specifier's own direction to whoever holds the parcel.

Verified after the fix: all 3 acceptance scenarios green
(`bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1620-*.feature`);
three solo runs of `telegramFrontDeskBotCli.test.js` at load 6.9-7.3
(276 tests, 0 failed, 4.2-4.8s, well under the 7000ms budget); the
register row unchanged (still owned by BL-1633, per the ticket's own
current scope). No new mutation-testing work needed - the production
diff (the `runHandoff` seam) is unchanged since my earlier hardening
pass (`b2dbead3d7`), already carried forward through this round trip.

Two bounce_history conflicts resolved across two merges in this pass
(preserving both sides' legitimate entries rather than picking one) -
recorded in the merge commit messages (`8af559a7c6`).
