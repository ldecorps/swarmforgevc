# BL-1363 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## Conflict resolution note

Merging this commit conflicted on `swarmforge/scripts/test/suite-manifest.tsv`
against BL-1391's two rows, which were still present on the incoming side
(coder built this before BL-1391's bounce/revert reached this branch).
Resolved by dropping the BL-1391 rows — confirmed their backing files
(`test_bl1391_bookkeeping_conflict.sh`,
`bl1391_bookkeeping_conflict_property_runner.bb`) do not exist on this
tree (correctly reverted by architect's bounce, merged in earlier this
turn) — and keeping only BL-1363's two new rows, whose files do exist.

## What was checked

- `test_bl1363_close_ticket.sh` — re-ran FOUR times in a row (deliberately,
  given BL-1391 was just bounced for an intermittent fixture race): 15/15
  PASS every time, including "the live repository's origin URL is
  byte-identical after the suite" — the exact class of defect the main
  merge that landed just before this ticket was about.
- `bl1363_close_ticket_property_runner.sh` — re-ran: ALL PROPERTIES HOLD
  over 12 constructed cells.
- `close_ticket.sh` read directly against all three invariants:
  - Invariant 1 (same integrity path, never overridden): the
    `commit_integrity_cli.bb` refusal path rolls back the staged
    active→done rename via a scoped `update-index`/file restore — never
    a raw `git commit` fallback — and the script says so explicitly in
    its own refusal message.
  - Invariant 2 (all-or-none): every id is resolved to a source file and
    a milestone-derived destination BEFORE anything is staged; one
    unresolvable id (missing, ambiguous, or no `milestone:` field)
    refuses with nothing moved.
  - Invariant 3 (never promotes): confirmed by reading the script end to
    end — no call to `promote_and_route_next.sh` or its logic anywhere.
- The milestone destination is read from the ticket's own `milestone:`
  field via a plain `sed` extraction, never guessed or defaulted — matches
  the human's ruling (new closes settle by that field; the 665 loose
  legacy files are deliberately left untouched).
- Rollback is scoped to this close's own paths (`update-index --force-remove`
  / re-apply the saved index lines for exactly the source/dest paths) —
  never a blanket `git reset`, which the comment correctly notes would
  discard other roles' staged work on the shared checkout.
- `jscpd` against `promote_and_route_next.sh` (the script this mirrors):
  0 clones even at a lower threshold (8 lines / 40 tokens) — the shared
  shape (snapshot → integrity CLI → rollback-on-refusal) is not
  copy-pasted duplication, the two scripts differ genuinely in their
  domain (paused→active vs. active→done-by-milestone).
- `mutation-site-count.js` on the new step handler: 115 sites (`over`
  100). Reviewed and declined to split, same reasoning as every other
  ticket this session: one cohesive single-feature handler.
- TypeScript compiles clean; the handler discovers via BL-1371's registry.

No bounce history on BL-1363 (first pass). Forwarding unchanged to
architect.

By cleaner.
