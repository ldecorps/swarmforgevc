# BL-1502 — hardener pass, 2026-09-13

## Scope

`mutation_cost: low`, no `extension/src` touched — per the ticket, no
Stryker target. Hand-authored mutant sweep (BL-638 fallback) over
`extension/test/helpers/bl1089FixtureSourceContract.js`'s three rules,
each hand-mutated to a no-op (`test: () => true`), file restored after
each check.

| rule | mutant | killed by |
|---|---|---|
| `served-stamp-age-0` | `() => true` | acceptance scenario 02, backdate row (vitest alone did not exercise it — property file only asserts the real fixture) |
| `no-5000ms-backdate` | (verified by inspection: real fixture contains no `"$F" 5000`, so the backdate row is the only fixture that can differentiate it; same row covers this rule) | acceptance scenario 02, backdate row |
| `healthy-poll-retained` | `() => true` | **SURVIVED** every existing test (no Examples row removes the healthy-poll line) — closed by adding a new unit-style test to `bl1089FrontDeskLivenessFixture.property.test.js` asserting a fixture with `write_heartbeat "$F" 10` altered is rejected naming `healthy-poll-retained`. Confirmed: red against the hand-mutated helper, green restored. |

Each mutant restored via `cp`/`sed` round-trip on a live file with no
detached job outstanding, confirmed via `git diff --stat` empty after
restore.

## Verification (all green after the fix)

- `cd extension && npx vitest run --config vitest.properties.config.mjs
  test/bl1089FrontDeskLivenessFixture.property.test.js` — 5/5 passed
  (4 pre-existing + 1 new hardening test).
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1502-the-bl1089-fixture-source-contract-follows-the-liveness-fixture.feature`
  — 3/3 scenarios pass.
- `bash swarmforge/scripts/test/test_front_desk_supervisor_liveness.sh` —
  ALL CHECKS PASSED (control: fixture untouched, per the ticket's FIRM
  constraint).
- Whole-tree standing guards (this parcel touches `extension/test/` and
  `specs/pipeline/steps/`): all 18 `test/*Guard*.test.js` files (excluding
  `.property.`) — 183/183 passed.
- Break-then-restore (`write_heartbeat "$root" 0` → `...5000`) on the
  shared predicate directly: fails naming `served-stamp-age-0`; restored,
  passes.
- CRAP/DRY: N/A, no `extension/src` changed.
- No orphaned `node --test|stryker|vitest` processes after the pass
  (`pgrep -fl` checked before and after).

## Not touched

`swarmforge/scripts/test/test_front_desk_supervisor_liveness.sh` and
`swarmforge/scripts/property_suite_standing_allowlist.tsv` unchanged from
the received commit (the allowlist row removal was the coder's, reasoned
in `backlog/evidence/BL-1502-coder-20260913.md` — the interim bypass row's
own commit says rows leave with their land).

`backlog/standing-reds.tsv`'s BL-1502 row removal is QA's, at land
(ticket's own `notes:`), not this pass's.

By hardender.
