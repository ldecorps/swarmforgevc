# BL-1251 QA Pass — 20260829

## BL-1241 entangled-tip rebuild

The documenter's handoff named commit `0c0d245869`. Its ancestry ran back
through the coder's long-lived branch, which still carries the SAME stale
BL-1246/step-scoping tangle BL-1273's and BL-1193's own QA passes already
found (my rebuilds fixed the landed tips, not the coder/cleaner/architect
/hardener/documenter worktree branches that were already built on top of
the tangle). Neither sibling is any more landable now: BL-1246 is still
active/unresolved, and the step-scoping fix (now BL-1277's own work) is
still sitting in `backlog/paused/`, not even promoted.

One addition to the pattern this time: the abandoned lineage also carried a
now-STALE BL-1277 mint (`11bb0ede8`, the pre-amendment 12/14-collision
count) that `origin/main` has already superseded with the specifier's
corrected 15/17-collision version — confirmed by diffing the two YAML
bodies directly, not assumed. Left behind deliberately, not rescued.

Rebuilt tip-pure: cherry-picked BL-1251's two plain, cleanly-scoped commits
(coder `4ba1d0c9e6`, documenter `7023c8381d` — each verified via `git show
--stat` to touch only BL-1251's own files) onto the already-landed
post-BL-1193 HEAD. `abandoned_commits` records the full superseded chain.
`git diff --stat origin/main HEAD` confirmed exactly BL-1251's own five
files.

## Commit under review

`82483a224` (tip of the rebuilt chain). BL-1251's own two commits
(`2bec8d3ce`/coder, `55802ff1b`/documenter — content-identical cherry-picks
of `4ba1d0c9e6`/`7023c8381d`) are ancestors of it.

## Functional verification (qa_e2e_procedure)

- `swarmforge/swarmforge.conf`: `config master_main_reconcile_enabled true`
  (line 345), with a conf comment recording the operator's actual 2026-08-29
  ON decision and rationale — not the expired "until BL-1236 lands"
  condition. (This conf state predates and is independent of BL-1251's own
  commit diff — set directly by the operator's `cce70d985`, per the
  ticket's own notes — but the fixed part of qa_e2e_procedure asserts it
  matches the recorded decision, which it does.)
- `specs/features/BL-1248-master-main-reconcile-kill-switch.feature`:
  scenario 04 is RETIRED (a comment block naming BL-1251 as retirer and
  quoting what it asserted), never reworded — scenarios 01, 02, 03, 05 are
  byte-for-byte unchanged. The Feature: narrative is re-tensed accurately
  (states BL-1236 landed and the sweep is re-armed, past tense).
- `specs/pipeline/steps/bl1248MasterMainReconcileKillSwitchSteps.js`: the
  `FEATURE` constant is re-tensed to match the Feature: line exactly (the
  ticket's own "matched pair" warning); scenario 04's two step
  registrations are removed with it (not left as dead handlers); the now-
  unused `fs` import is removed too. Every other registration (01, 02, 03,
  05) is untouched.
- Acceptance runner: 7/8 pass (see below for the one failure and why it
  does not block).

## Documenter-flagged, non-blocking gap

The documenter's own `Specification.MD` entry explicitly discloses that
the feature file's embedded `# acceptance-mutation-manifest` stamp (a
gherkin-mutator artifact, header of the file) now describes the
pre-retirement file, and that BL-1251 carries no hardener stage
(`required_stages: [coder, documenter, qa]`, the specifier's own scoping
decision) to regenerate it. Verified this is informational, not a live
gate: grepped the codebase for any code path that reads `background_hash`
/`scenario_hash` from a feature file's manifest and compares it against
live content — none exists; those fields appear only as literal JSON
payload inside various `.feature` files' own stamps, written and read only
by the gherkin-mutator CLI itself on its next run. Nothing currently
executes against this stamp, so a stale one causes no test, gate, or
runner failure. Not BL-1251's to fix (the specifier deliberately scoped no
hardener stage here); correctly disclosed rather than silently left.

## Acceptance run: 7/8 pass, 1 pre-existing environmental failure

`run_acceptance.sh specs/features/BL-1248-master-main-reconcile-kill-switch.feature`
→ scenarios 1–7 pass (the 5-value Outline, scenario 02, scenario 03).
Scenario 8 ("switching the sweep off silences the reconcile, not the
divergence report" — the file's own scenario 05, last remaining after
retirement) fails with repeated `Cannot find module
'.../extension/out/tools/{drain-answer-files,resume-expired-pauses,apply-cooldown-pause}.js'`
inside a fresh `/tmp` git-clone fixture the real-daemon wiring helper
(`bl1256ReconcileWiringFixture.js`, shelling
`test_handoffd_master_main_reconcile_wiring.sh`) creates.

Ruled unrelated to BL-1251, not assumed:
- Scenario 05's step handler is BYTE-IDENTICAL before and after BL-1251 —
  confirmed via `git log -p` on the step file: the diff touches only the
  `FEATURE` constant, the `fs` import, and scenario 04's registrations.
  Nothing in the code path this scenario exercises changed.
- The underlying wiring script and `bl1256ReconcileWiringFixture.js` are
  not touched by BL-1251 at all.
- `extension/out/` is gitignored (constitution Guardrails) — a fresh git
  clone of the repo, which is what this fixture's temp root is, never
  carries it unless something explicitly builds/copies it in; nothing in
  the wiring script does (grepped for `extension/out|npm run compile|npm
  install|cp -r|rsync` — no hits). This is a general, pre-existing gap in
  how this fixture provisions its temp root, not content-dependent.
- Re-ran the underlying `test_handoffd_master_main_reconcile_wiring.sh`
  directly, in isolation: it produced a DIFFERENT failure this time ("FAIL:
  expected a dirty block persisting past the escalation threshold to
  escalate to the operator (BL-920)") before I stopped it — non-determinism
  under current host load (this session has run four other full unit/
  property suites and multiple real-daemon fixture batches concurrently
  today), the same real-clock/host-contention flake class already
  documented for this project's daemon-sweep test family, not a stable
  defect tied to any particular parcel.
- Grepped `backlog/` for this test file and for the missing-module string:
  no open ticket names this specific gap. Reported to the specifier by
  note (not BL-1251's to fix; pre-existing test-infrastructure fragility
  unrelated to a pure text/retirement change).

## Regression

- Unit suite: `npx vitest run` → 20 failed files / 33 failed tests / 9593
  passed (572 files) — byte-identical to the same-session BL-1193 baseline
  (expected: BL-1251 touches zero `extension/` files).
- Property suite: `npm run test:properties` → 25 failed files / 14 failed
  tests / 732 passed (269 files), 2 allowlisted `[vitest-worker]: Timeout
  calling "onTaskUpdate"` (BL-871) errors — identical counts to the
  same-session BL-1193 baseline.
- Article 4.5 changed-path: BL-1251's only production-code change is the
  step handler file (deletions only, no new logic); the direct acceptance
  run against its own feature file is the narrowest and correct exercise
  of it, and 7/8 scenarios pass with the 1 failure ruled unrelated above.
