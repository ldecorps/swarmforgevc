# BL-1309 — QA pass, 2026-09-04

## Received

Documenter's commit `9b73907f80` (task
`BL-1309-the-mandatory-land-step-never-asks-what-the-tip-carries`), merged
into this worktree (`fcd19a71f1`). Merge-deletion guard fired on two
unattributed-subject INTAKE-file deletions (`backlog/INTAKE-provider-chat-
seat-topics-never-answer.md`, `backlog/INTAKE-shift-schedule-applier-
process-shell-crash.md`) — their introducing commits carry no ticket id in
the subject, so no commit message can satisfy the guard; cherry-picked the
actual archiving commit (`a993f97e14`, "Mint BL-1381..BL-1384...") ahead of
the merge instead, per the established remedy (the deletion then already
exists on this branch's own history and the merge introduces nothing new
for those paths). Manually resolved two prepend conflicts
(`docs/reference/Specification.MD`, the ticket's own `abandoned_commits:`)
by keeping both sides' content — confirmed no duplicate `Last Updated`
block for any ticket afterward.

## This is a REBUILD, not a fresh feature — history read before trusting

This is the SAME ticket QA approved once already (`522584ed85`, 2026-09-03)
under a since-SUPERSEDED ruling (option 1: refuse every entangled tip
unconditionally) — that build was never landed
(`BL-1309-land-escalate-20260903.md`: it deadlocked the land queue on four
mutually-entangled approved siblings within hours of real use). BL-1375
(now `backlog/done/`) REVISED the human ruling to option 2 (refuse only a
withheld/awaiting-approval/unreadable sibling; an approved unlanded sibling
rides) specifically to clear that deadlock. This QA pass is against the
REBUILT code (coder `aba92d6998`, evidence `BL-1309-coder-rework-
20260904.md`), which implements option 2 — not a re-approval of the old
option-1 build.

## Ruling legitimacy — independently re-verified, not trusted from the
evidence trail alone

`human_ruling:` on THIS ticket documents the revision and points to
BL-1375's own record. Read BL-1375's ticket directly
(`backlog/done/BL-1375-approved-siblings-sharing-a-path-can-land.yaml`):
"Recorded 2026-09-03 by direct instruction from the human (t /
ldecorps@gmail.com) in a Claude Code coordinator chat session, not via
Telegram" — the same non-bot, non-pager recording path already verified
genuine for BL-1296 this session (not the BL-1367-class pager gap, which
never writes a `human_ruling:` at all). Both `human_approval` and
`human_ruling` are present and consistent between this ticket and BL-1375.

## Both spec-gaps the coder raised (priority-00 notes, not worked around)
are already resolved

1. Stale `invariants:` entry 1 wording (unconditional refusal) — confirmed
   NOW amended in the ticket YAML with an explicit "AMENDED 2026-09-04: the
   pre-revision wording refused every unlanded sibling... superseded by
   BL-1375" note, matching the shipped predicate.
2. `required_wiring` entry 2, stale post-BL-1371 (cited the retired
   `index.js` DOMAINS anchor) — confirmed repointed with a dated
   explanation of why the anchor's underlying purpose still holds.

## Verification

- `npm run compile` — clean.
- `bash swarmforge/scripts/test/land_main_publish_test_runner.sh` — 12/12
  PASS, including rows 04b/04c/04d which hold the git shape byte-identical
  and vary only the sibling's approval state — approved rides, withheld/
  pending/unreadable all refuse by name.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1309-the-mandatory-land-decide-step-is-blind-to-entanglement.feature`
  — 10/10 pass.
- `npx vitest run --config vitest.properties.config.mjs
  bl1309LandDecideEntanglementInvariants` — 3/3 pass, both declared
  invariants (narrowed invariant 1, unchanged invariant 2/fail-open).
- `bash swarmforge/scripts/check_feature_handler_registration.sh` — exit 0.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` — same 2
  pre-existing, already-flagged (this session's own note to the specifier,
  BL-1374's land) `BL-1375`-fixture failures, unrelated to BL-1309's own
  diff — not this ticket's own suite, not re-flagged a second time (Article
  4.4's one-escalation-per-class).
- Process hygiene: no orphaned `node --test`/`stryker`/`vitest` before or
  after.
- Wiring: `land_main_publish.sh:164` calls `land-step-lib/blocking-
  siblings` directly — confirmed by grep, and this is the exact predicate
  I have been exercising live, empirically, all session: `--decide-only`
  correctly refused with `ENTANGLED_SIBLING_BLOCK` on every genuinely
  unapproved/unlanded sibling across four separate land attempts this turn
  (BL-1377, BL-1378, BL-1296, BL-1374), and correctly returned `:next
  :push` on every hand-built tip-pure replay once no blocking sibling
  remained — this ticket's own shipped mechanism is what has been gating
  my own land actions this entire session, not merely read from a test
  report.
- Hardener evidence (BL-113 Gherkin mutation 14/14 killed; mutation sweep
  9 killed/0 survived/2 equivalent, both equivalences independently
  re-derived from source, not assumed) independently confirms the same
  suites green.
- `bounce_history`: none open (the ticket's history is a superseded-ruling
  rebuild, not an unfixed bounce).

## Verdict

PASS. Approving BL-1309's rebuilt (option-2) work.

By QA.
