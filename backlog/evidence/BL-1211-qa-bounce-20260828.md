# BL-1211 — QA bounce, 20260828

## D1 — entangled tip, extreme case: cited commit is a full merge of local `main` into documenter's branch (BL-506 tip-purity violation, fifth occurrence this pass)

1. **Failing command:**
   ```
   env -u GIT_DIR -u GIT_WORK_TREE git diff --name-only origin/main...173bb2422f | wc -l
   ```
   → **90** changed paths.

2. **Commit hash:** `173bb2422f` — its own subject is literally `Merge
   branch 'main' into swarmforge-documenter`, received via `git_handoff`
   payload `merge_and_process documenter 173bb2422f`. Its second parent
   chain (`git log --oneline -3 173bb2422f`) surfaces specifier minting
   commits (`Mint BL-1237...`, `BL topic record for BL-1237`) that have no
   business in a documenter parcel for BL-1211.

3. **First error excerpt** — the 90 paths span, beyond BL-1211's own scope,
   at minimum: BL-1192, BL-1207, BL-1216 (the same cluster as the four
   prior bounces this pass), plus BL-1179, BL-1196, BL-1222, BL-1228,
   BL-1230, BL-1231, BL-1233, BL-1234, BL-1235, BL-1236, BL-1237 (backlog
   YAML/topic/feature files for tickets that are, per their own files,
   still `paused/` or `active/`, i.e. not done), an INTAKE file, and
   `swarmforge/scripts/model_factory_lib.bb` +
   `swarmforge/scripts/test/model_factory_test_runner.bb` (unrelated to
   any ticket named above). This is not a scoped multi-ticket batch
   (Article 2.6) — it is an entire branch merge.

   This is the **fifth** parcel this QA pass carrying entangled foreign
   content, and by far the largest — see the branch-wide `note` already
   sent to specifier + coordinator this pass. BL-1211's own bounce_count
   (2, last event an architect bounce, `status: todo` in its own YAML) does
   not by itself prove this parcel is unready — bounce_history records
   bounces only, not clean passes, and `backlog/evidence/
   BL-1211-architect-pass-recovery-cli-20260828.md` /
   `BL-1211-cleaner-recovery-cli-pass-20260828.md` in this commit's tree
   suggest a later, undocumented-in-the-YAML re-fix round did clear
   cleaner/architect again. That question is moot for this bounce: even if
   BL-1211's own work is now sound, this specific commit cannot be landed
   as BL-1211's approval because of D1 alone.

4. **Failure class:** `behavior` (BL-506 entangled tip).

5. **Expected vs observed:** Expected — a `git_handoff` naming `BL-1211`
   cites a commit touching only BL-1211's own paths. Observed — a literal
   merge of local `main` into the documenter branch, carrying roughly a
   dozen unrelated tickets' backlog state and two unrelated `.bb` files.

## Other checks this pass

- BL-1211's own functional correctness not independently re-verified —
  **BLOCKED BY D1**: no productive way to isolate BL-1211's own diff from
  a full-branch merge without rebuilding it tip-pure first.

## Remediation

Coder/documenter: do not merge `main` into a role worktree as a way to pick
up unrelated backlog state — rebuild a tip-pure `BL-1211`-only commit on
`origin/main` and re-send. If BL-1211's own work is in fact re-passed per
the `-recovery-cli` evidence files, that content should carry over cleanly
into the rebuild.
