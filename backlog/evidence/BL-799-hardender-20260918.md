# BL-799 — hardender review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

### Scope check against the diff

`git diff main...HEAD -- extension/src/quality/qaBounce.ts extension/src/tools/recordBounceArgs.ts`
shows exactly the ticket's own diff: `KNOWN_PRODUCING_ROLES` widened by one
member (`specifier`) and the USAGE string updated to match. Neither line is
where any mutation survivor below lives.

### Mutation

Detached job (`swarmforge/scripts/detach_job.sh`, registered, log
`/tmp/bl799_stryker3.log`): `npx stryker run --mutate
out/quality/qaBounce.js,out/tools/recordBounceArgs.js --concurrency 6
--ignoreStatic --dryRunTimeoutMinutes 15`. Finished in 10m57s, exit 0.
Result: 312 killed, 15 survived, 0 timeout, 0 no-coverage, 0 errors
(95.41% mutation score). `--ignoreStatic` ran with `coverageAnalysis:
perTest` already configured, so no static mutant was silently dropped —
the 15 survivors are the full non-static set.

The scoped `vitest.bl799.stryker.config.mjs` / `stryker.config.json`
pointer (excluding two unrelated known-red files, `activePoolFreshnessAudit.test.js`
and `bl1418RoleEnumerationClassification.test.js`, per the sanctioned
Stryker-sandbox-red workaround) was reverted after the run — `git
checkout -- extension/stryker.config.json` and the scratch config file
deleted. It never touched the shared `vitest.config.mjs`.

All 15 survivors sit in `qaBounce.ts`/`recordBounceArgs.ts` code that
predates this ticket (BL-454/BL-608/BL-688/BL-689/BL-990). Checked
`backlog/evidence/BL-990-hardener-pass-20260821.md`: its own Stryker pass
over this same file set was explicitly **deferred** ("host load sustained
20-46 throughout this pass ... deferring Stryker (owed on the next quiet
pass)"), so this is that debt's first real measurement, not a regression.
15 is under the ~50-survivor in-pass-chase threshold, so chased here.

**4 real gaps, closed:**

| location | mutant | fix |
|---|---|---|
| `qaBounce.js:106:28` (`defectCountForRecord`) | `length > 0` → `true`, and `> 0` → `>= 0` | new test: `items: []` asserts exactly `1` |
| `qaBounce.js:155:44` (`hasBounceCorrectionReasonAndEvidence`) | evidence check → `true` | new test: `evidence: ''` (present, empty) asserts `false` |
| `qaBounce.js:193:33` (`hasBounceCorrection`) | `.some(target-key-eq)` → `.some(() => true)` | new test: existing corrections target a *different* ticket+commit, asserts `false` |
| `recordBounceArgs.js:51:10` (`resolveBounceInventory`) | `.every` → `.some` | new test: mixed one-valid/one-invalid array asserts `invalid-item` |

Each was verified live: hand-patched the exact mutant into the compiled
`out/` file (no detached job was running at the time — confirmed via
`pgrep -fl 'stryker\|node --test'`), re-ran the corresponding test file
and watched it fail on the new assertion, then restored `out/` from a
saved copy (`diff` confirmed byte-identical afterward). All four caught
their mutant; all four tests otherwise pass on the unmutated build
(122/122 across the three touched test files).

**10 survivors accepted as equivalent** (BL-234/BL-1081 discipline — code-level
reasoning plus empirical corpus checks, not shape or argument):

- `qaBounce.js:85:12`/`88:12` (`isKnownFailureClassValue`/`isKnownProducingRoleValue`)
  and `158:12` (`hasKnownBounceCorrectionAttribution`): each is `typeof x
  === 'string' && knownSet.has(x)`. The closed-set check is
  `Array.prototype.includes` on an array of strings (strict equality, no
  coercion) — a non-string `x` can never match a string member, so
  `isKnownXxx(x)` is already `false` for any non-string on its own; the
  `typeof` guard's removal changes nothing. Verified over a corpus of 19
  values (numbers, booleans, `NaN`, `Symbol`, functions, `Date`, arrays,
  `Infinity`, `null`, `undefined`, plus valid/invalid strings) — zero
  differences for all three functions.
- `qaBounce.js:91:19` (`isValidBounceInventoryItem`) and `166:19`
  (`isBounceCorrection`) guards (`!value || typeof value !== 'object'`):
  removing the `typeof` half only matters for a truthy non-object
  primitive. Every downstream field read (`candidate.id`,
  `candidate.kind`, etc.) on such a primitive is a property access on a
  non-null primitive, which JS resolves to `undefined` (no custom
  properties survive on a primitive), and the surrounding
  `isNonEmptyString`/`===` checks already reject `undefined`. Verified
  over a 15-value corpus (strings, numbers, booleans, `NaN`, `Symbol`,
  functions, `Date`, `Infinity`) — the real (unmutated) function already
  returns `false` for every one via the same downstream path the mutant
  would fall through to.
- `qaBounce.js:185:9`/`185:35` (`applyBounceCorrections` guard,
  `corrections.length === 0`): removing it makes the empty-corrections
  case fall through to `new Set([]).has(...)` (always `false`) inside
  `records.filter(...)`, which returns a new array containing every
  record — `deepEqual` to the guarded path's direct `return records`, just
  not reference-equal. The one existing test
  (`applyBounceCorrections with no corrections is the identity`) uses
  `assert.deepEqual`, which cannot see the difference. Confirmed live:
  `applyBounceCorrections(records, [])` and a hand-simulated fallthrough
  produce array-equal output.
- `recordBounceArgs.js:61:9`/`61:28` (`resolveBlockedCount`'s `raw ===
  undefined` guard) and `65:40` (`>= 0` → `> 0`): `Number(undefined)` is
  `NaN` and `Number.isInteger(NaN)` is `false`, so the fallback path
  already returns `0` for `raw === undefined` with no guard at all —
  confirmed live (`Number(undefined)` → `NaN`,
  `resolveBlockedCount(undefined)` → `0`). And at `parsed === 0` the
  ternary's two branches both evaluate to `0` (`0 >= 0 ? 0 : 0` and
  `0 > 0 ? 0 : 0`), so `>=` and `>` are extensionally equal for every
  integer `parsed` — no input distinguishes them.

### CRAP / DRY

`node scripts/crapReport.js src/quality/qaBounce.ts
src/tools/recordBounceArgs.ts` against the existing (same-day)
`coverage/coverage-final.json`: every function ≤ 6.00 CRAP (max is
`resolveBounceInventory` at complexity 6, 100% coverage, CRAP 6.00), all
other functions lower, all 100% covered. `npx jscpd
src/quality/qaBounce.ts src/tools/recordBounceArgs.ts`: 0 clones.

### Acceptance / Gherkin

`run_acceptance.sh` on the feature: 2/2 pass. Both scenarios are plain
`Scenario:` (no `Scenario Outline:`/`Examples:`), so
`run_gherkin_mutation.sh` correctly reports `"outcome": "inapplicable"`
per BL-638 (stamped into the feature file's manifest, not silently
skipped) — there is no example-value mutation surface to sweep, and the
.ts logic itself already carries a full Stryker pass above.

### Suite

122/122 across `qaBounce.test.js`, `bl990BounceCorrection.test.js`,
`recordBounceCli.test.js`. All 19 non-property `*Guard*.test.js` files
green (185/185) — `extension/test/` was touched, per the standing
whole-tree-guard rule.

### Process note

Resumed mid-flight: a prior session had already merged the architect's
BL-799 handoff, set up the scoped Stryker config to route around an
unrelated known-red pair of files, and launched the mutation run as a
registered detach job before the session ended. This session found the
job still live (241s elapsed, dry-run phase), let it run to completion per
the detach protocol (never hand-mutating files it was reading), then
picked up from its result.
