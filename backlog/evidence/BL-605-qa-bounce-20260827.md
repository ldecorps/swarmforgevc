# BL-605 QA bounce — 20260827

**Routing:** coder (unit suite red under Vitest)

## Failing command

```
cd extension && npx vitest run test/globalTokenConsumption.test.js
# FAIL: No test suite found in file .../globalTokenConsumption.test.js
# exit 1
```

## Commit tested

`f2a5c9168d` (coder handoff). Tip-pure vs `origin/main` after merge (7 BL-605
paths only).

## First error excerpt

```
FAIL  test/globalTokenConsumption.test.js
Error: No test suite found in file
  .../extension/test/globalTokenConsumption.test.js
```

File opens with `const { test } = require('node:test')`. Sibling metrics tests
(e.g. `burnRate.test.js`) register with Vitest-compatible bare `test` and pass
under `npm test` / `vitest run`.

## Failure class

unit

## Expected vs observed

**Expected:** unit file green under the standing Vitest lane (`npm test`).

**Observed:** Vitest reports no suite (exit 1). Same cases pass under
`node --test`, but that is not the project unit gate.

## Other gates (same pass)

| Gate | Result |
| --- | --- |
| tip purity | pass |
| compile | pass |
| property (`globalTokenConsumption.property.test.js`) | 3/3 |
| acceptance (4 scenarios) | 4/4 |
| required_wiring | present |

## Defects

**D1 — unit harness mismatch (blame: coder):** Rewrite
`extension/test/globalTokenConsumption.test.js` to Vitest-compatible
registration (drop `node:test` import; match burnRate/deprecate pattern) so
`vitest run` finds and passes the suite.

By QA.
