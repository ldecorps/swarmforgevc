# BL-1392 — documenter pass (2026-09-04)

## Received
`7671300424` (hardener; per `backlog/evidence/BL-1392-hardener-pass-20260904.md`).

## Merge fix
Auto-merged conflict in `swarmforge/scripts/test/suite-manifest.tsv` —
purely additive rows on the incoming side (`cron_heartbeat_lib_test_runner.bb`,
`test_bl1392_dead_cron_never_silent.sh`), restored the standard trailing-tab
column format after resolving.

## What changed, operator-visible
The freshness watchdog (BL-675) restarts a dead `handoffd`, but nothing
restarted — or even noticed — a dead cron DAEMON itself, so a host with
cron stopped (this WSL2 host since 2026-08-30) silently ran every
`./swarm start` printing "Installed" while nothing scheduled ever fired
for five days. Directly extends the exact mechanism BL-675's own doc
already covers (the freshness cron log this ticket's runtime half reads
is the same log `install_freshness_cron.sh` already writes).

## Doc-domain review
- `docs/how-to/BL-675-daemon-log-freshness-watchdog.md`: added a new
  section right after "Install" — the natural home, since the runtime
  half reads that exact log and the install-time half sits in the same
  install path this page already documents in detail.
- `docs/index.md`: extended the existing BL-675 summary line.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- The ticket's own "How" section explicitly named the doc home: "A
  one-line how-to entry for the WSL boot command belongs in the
  documenter's pass (BL-611 runbook)" — checked BL-611
  (`docs/how-to/BL-611-babysitterd-runbook.md`) first; it already
  cross-references BL-675's page for exactly this class of mechanism
  (line ~159, "See Daemon log-freshness watchdog"), so BL-675 is the
  correct, more specific home rather than duplicating into BL-611 too.
- Diagram registry checked — no diagram depicts cron-heartbeat internals.

## Verdict
Doc content extended in the owning how-to page (not NONE) plus the
standard Specification.MD/index.md entries. Forwarding to QA.
