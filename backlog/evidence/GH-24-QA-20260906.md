# GH-24 — QA review pass, 2026-09-06

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: grep -n "bounded retry" docs/how-to/GH-24-coordinator-activity-telegram-feed.md
- **Commit hash**: 718bd15071
- **First error excerpt**: docs/how-to/GH-24-coordinator-activity-telegram-feed.md:36: honoring 429 `retry_after` with a bounded retry.
- **Failure class**: behavior
- **Expected vs observed**: Expected the how-to to describe the 429 retry as unbounded (matching the code, its own comment in coordinator_activity_feed_post_lib.bb, and the coders own bounce-fix evidence, all three agreeing); observed it says a bounded retry instead - the opposite behavior.
- **Blamed role**: documenter
- **Remediation pointer**: docs/how-to/GH-24-coordinator-activity-telegram-feed.md, Cadence and posting section: replace a bounded retry with unbounded-on-429/immediate-report-otherwise language matching send-with-rate-limit-retry! (coordinator_activity_feed_post_lib.bb) and BL-342s retryOnRateLimit reasoning.

By QA.

## Detail

This ticket's own actual deliverable (the deterministic Telegram surfacer,
its retry logic, and its wiring into the daemon's sweep cadence) is
verified CORRECT and COMPLETE; the single finding above is a docs-only
defect, not an implementation defect.

- `qa-sibling-check.js status --ticket GH-24`: N/A — the CLI's
  `TICKET_PATTERN` only accepts `BL-\d+`; a `GH-`-prefixed id cannot run
  status (exit 2, usage). Known, already-documented tool limitation
  (precedent: `backlog/evidence/GH-25-qa-pass-20260825.md`), not a new
  finding, not blocking.
- Orphan process check before/after: empty both times.
- Lineage: documenter's own commit `728d880562` descends cleanly from the
  shared BL-676/GH-24 fork point `2bb4a3ea82`; merged clean, no conflicts.
- Acceptance: `run_acceptance.sh specs/features/GH-24-coordinator-activity-telegram-feed.feature`
  — 5/5.
- `coordinator_activity_feed_lib_test_runner.bb`: ALL PASS.
- `coordinator_activity_feed_post_lib_test_runner.bb`: ALL PASS (the
  architect-bounce fix's own new suite — 429-then-success, three
  consecutive 429s, non-429 immediate-fail, 429-with-no-retry_after).
- `check_bb_scripts_load.sh --all`: 297 scripts, clean, handoffd boots.
- Wiring anchor: `gh24CoordinatorActivityTelegramFeedSteps.js` exports
  `registerSteps`.
- Real consumer anchor (not just tested in isolation): confirmed
  `coordinator-activity-feed-sweep!` (handoffd.bb:4198) is registered via
  `run-sweep!` (handoffd.bb:5011-5012) — genuinely wired into the daemon's
  live sweep cadence, not merely defined.
- Out-of-scope confirmed untouched: `list-sent-handoffs` reads only the
  coordinator's own `sent` mailbox (`handoff-lib/load-role-info
  "coordinator"`), never the daemon's own chase/sweep actions; no LLM
  self-reporting path added; no second topic-map invented; `index.js` not
  touched (handler file is the registration, per the ticket's own
  2026-09-04 repoint).
- `docs/index.md`'s own summary line (line 193) makes no bounded/unbounded
  claim — only the how-to's own prose carries the D1 defect.
- Full unit lane (`npm test`, extension/): 610/610 files, 10309/10309
  green — run against this same tree (only a documenter evidence-file
  commit separates it from what was tested for BL-676 immediately prior
  in this same shift).
- Full property lane (`npm run test:properties`): 369/369 files clean
  (confirmed via rerun once host load eased — see BL-676's own QA
  evidence for the load/flake detail; no GH-24-specific property test
  beyond the standing suite, and none of the flaked files touch this
  ticket's domain).
- qa_e2e_procedure item 3 (live: run the swarm, send a note, complete a
  bookkeeping close, verify the Telegram topic within one tick, verify no
  re-post after a restart) was **not independently exercised live** by
  QA: it requires posting to the real coordinator Telegram topic and/or
  restarting the live `handoffd` daemon process, both hard-to-reverse,
  externally-visible actions outside a routine verification pass's blast
  radius. The five acceptance scenarios and the lib test runner's five
  cases already exercise the identical logic deterministically via
  injected seams (posted line content, idle-tick-posts-nothing,
  restart-no-duplication, failed-send-retry-without-duplication) —
  functionally equivalent coverage of the same intent without the live
  side effect.

## Disposition

D1 is a documenter-owned defect (Article 4.3: "defect entirely in docs,
comments, docstrings, or the ticket's own `docs:` deliverables →
documenter"). Bouncing to documenter; the fix then re-forwards down the
remaining chain (cleaner → architect → hardender → documenter → QA) so
every gate runs again.

## record-bounce.js gap (best-effort skip, not blocking)

`record-bounce.js --ticket GH-24 ...` refuses (exit 1, usage) for the same
reason `qa-sibling-check.js status` does: `bounceArgsCore.js`'s shared
`TICKET_PATTERN = /^BL-\d+$/i` rejects any `GH-`-prefixed id outright —
confirmed by reading the source, not just the error text. No prior
GH-ticket bounce found in `backlog/evidence/` to check for precedent
handling. Per this prompt's own best-effort posture for a tool that
cannot run ("never let this step block or gate the bounce itself"),
skipping the structured bounce-rate metric for this one and proceeding
with the actual bounce handoff — the canonical evidence file above is the
authoritative record regardless. Flagged to the specifier via a
low-priority (not blocking) note, since this is the SECOND `GH-`-prefix
tool gap surfaced this session (the first being `qa-sibling-check.js`,
already documented as non-blocking in `GH-25-qa-pass-20260825.md`) and
record-bounce.js additionally means GH-ticket bounces get no per-role
bounce-rate telemetry at all until fixed.
