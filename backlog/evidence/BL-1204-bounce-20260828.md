# BL-1204 — QA bounce — 20260828

## D1: documenter pass missing entirely

1. **Failing command:**
   ```
   find docs/how-to -iname "*1204*"
   grep -n "1204" docs/index.md
   grep -n "BL-1204" docs/reference/Specification.MD
   ```
2. **Commit hash:** `6f8bfb10864e9a87b6e44c3c428dff5fc2858c8b` (QA's own merge
   of documenter's received tip `14399d085e`, this session's
   `merge_and_process` commit for this parcel).
3. **First error excerpt:** all three commands above returned **empty** —
   no `docs/how-to/BL-1204-*.md` file exists anywhere in the repo, no
   `docs/index.md` line mentions BL-1204, and `docs/reference/Specification.MD`
   has no BL-1204 entry.
4. **Failure class:** `behavior` (same precedent as this ticket's own
   sibling BL-1203, bounced for the identical shape earlier this session —
   Article 4.3's own worked example, BL-575).
5. **Expected vs observed:** expected a how-to page, a `docs/index.md`
   link, and a `Specification.MD` changelog entry, matching every other
   ticket QA processed this session and matching Article 1.7's documenter
   deliverable. Observed: zero documentation output for a `high`-severity
   defect fix that restored two dark Telegram commands, went through THREE
   bounce rounds (missing acceptance handler, async-dispatch race, Background
   fixture leak) plus an additional hardener-found fixture-leak fix — a
   ticket with this much history is exactly the kind future maintainers most
   need a how-to page for.

## Why this is a real gap, not a QA merge-resolution artifact

`git log --oneline 428b8326a7..14399d085e` shows exactly ONE commit between
hardener's tip and documenter's tip: `14399d085` itself, titled
"merge_and_process hardender 428b8326a7 for BL-1204. By documenter." — a
bare merge with zero content of its own. `git diff --stat 428b8326a7
14399d085e` shows only ambient coordinator-bookkeeping churn picked up via
shared branch history (BL-1186/1189/1203 closes, BL-1201/1202/1208
promotions) — nothing BL-1204-shaped, nothing under `docs/`. There is no
documenter-authored commit anywhere in this ticket's full history
(`git log --oneline --all --grep="BL-1204"` shows coder ×4, cleaner ×4,
architect ×5, hardener ×1 — never a documenter-titled commit).

## Everything else checked — genuinely clean, not just unexamined

Completing the full inventory before this one-item bounce (Article 4.4 —
never first-failure-stop):

| Gate | Result |
|---|---|
| Ancestry (coder ×2 fixes, cleaner, architect ×3, hardener) | CONFIRMED all ancestors of received tip |
| Compile (`npm run compile`) | Clean |
| Targeted unit (`telegramCursorBridgeCore`/`RedeployTargets`) | 129/129 PASS |
| Acceptance (`run_acceptance.sh`, 3 independent runs) | 4/4 PASS every run |
| Fixture-leak fixes (Background lazy-init, When-step try/catch, Then-step try/finally) | All three independently re-confirmed present in the current file; 0 `/tmp/bl1204-acceptance-*` stragglers across 3 fresh runs |
| BL-113 Gherkin mutation | 3/3 killed per hardener's evidence (manifest committed with the feature file) |
| Production wiring (real caller, not just isolated test) | Confirmed: `telegramCursorOperatorExec.ts` imports and dispatches `parseFrontDeskRedeployCommand`/`startFrontDeskRedeployRun` and `parseAllRedeployCommand`/`startAllRedeployRun` for real; `telegramCursorBridgeCore.ts:948-949` lists both in `formatHelpMessage` |
| `telegramCursorBridgeCore.test.js` help assertions | Confirmed present and unweakened (qa_e2e_procedure item 5) |
| Invariant ("every accepted target listed, every listed target accepted") | Directly encoded as two Then-step assertions, both directions, against the real parser functions |

**The underlying redeploy-wiring fix and all three rounds of fixture-leak
hardening are solid and well-verified** — this bounce is narrowly about the
missing documentation layer, not a doubt about the fix's correctness.

## Routing

Per Article 4.3's own worked example (identical shape to this session's
BL-1203 bounce), a stage's pass missing entirely routes to THAT stage
regardless of failure class: **documenter**. The fix is mechanical — a
how-to page (pattern established this session by
`docs/how-to/BL-1203-role-answer-notes-delivered-once.md`,
`docs/how-to/BL-1205-tree-collapse-guard.md`,
`docs/how-to/BL-1213-parcel-rollback-guard.md`), linked from
`docs/index.md`, and a `Specification.MD` top-entry changelog line — no new
production logic needed, no known-good code is in question.

By QA.
