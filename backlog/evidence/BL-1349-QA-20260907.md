# BL-1349 — QA review pass, 2026-09-07

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Verification (qa_e2e_procedure, amended 2026-09-07 by the specifier)

- **Per-file budget**: ran each of the seven named files alone
  (`onboarderLauncherPidGuard`, `bl787NamedTunnelInvariants`, and the five
  `bl1252*` split files) through the property lane individually. All pass;
  wall clocks 9.9s-13.3s, all comfortably under the 15s budget (worst:
  `onboarderLauncherPidGuard` at 13.26s).
- **No-deletion check (invariant 1)**: counted `fc.property(`/`fc.assert(`
  at the pre-tuning parent (`8ac3d17d14^`) vs current for each file:
  onboarder 2/2, bl787 6/6, the five bl1252 splits sum to 10/10 (matching
  the original single file's 10) — exact match, independently confirming
  the coder/architect/hardener's own counts. Spot-diffed `bl787` directly:
  only `numRuns` (6→2) and an explanatory comment changed; the assertions
  themselves are untouched, matching invariant 1's own wording.
- **Acceptance**: `specs/features/BL-1349-spawn-heavy-property-files-fit-a-budget.feature`
  — 8/8 pass (7 per-file-budget Outline rows + no-deletion scenario).
- **Whole-lane check (amended step)**: ran the full `test:properties` lane
  twice under live-swarm contention (19 and 10 files red respectively,
  varying by contention level — the same load-flake shape this session
  already characterized for BL-1454/BL-1446). Reconciled the SECOND run's
  10 failed files by running each alone: 9 of 10 pass cleanly in isolation
  (contention-induced flakes, not genuine reds) — notably including all
  seven `bl12xx`/`bl13xx` land-step-invariant property tests, confirmed
  NOT a regression from this session's own BL-1446 land (which touched
  `land_step_lib.bb`, the file these tests exercise). The tenth,
  `bl968MaterializedGuardSensitivity.property.test.js`, is the
  already-owned standing red (`BL-1450`, registered, unrelated to this
  ticket). **None of the three tuned files (or their five bl1252
  successors) appeared in either failed-file list, in either run.** The
  live allowlist (`swarmforge/scripts/property_suite_standing_allowlist.tsv`)
  is untouched (1 line, header only, `git status` clean) — no suppression.
  Per the amended step, the red set (bl968 alone, once contention noise is
  reconciled out) is exactly the property-lane rows of
  `backlog/standing-reds.tsv` (bl968→BL-1450, bl1218/bl1320→BL-1457 —
  the latter two green in this pane's env, `PACK_STAFFING_SKIP_GATE=1`
  exported, consistent with their own row's documented condition).
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass (new acceptance
  step handler registers cleanly).
- `backlog/standing-reds.tsv`: the stale row naming the now-deleted
  `bl1252CommitGuardAggregationInvariants.property.test.js` is retired
  (confirmed absent); the hardener's own retirement reasoning (added after
  this ticket's own split by an unrelated mint) independently re-verified
  by grep.
- Full compiled unit suite: unaffected — `*.property.test.js` files are
  excluded from `npm test`'s scope (`vitest.config.*`'s own `exclude`),
  and no non-property file under `extension/src`/`extension/test` changed;
  this session's last full run stands (609/610, one independently-owned
  red, BL-1460).
- No orphaned `node --test`/vitest processes remained after this pass
  (checked by PID/cwd, not bare `pgrep` self-match — one live process
  found belonged to the architect's own worktree, unrelated).

## Process note, not a defect

The cleaner's own evidence (`BL-1349-cleaner-20260907.md`) documents a
self-caught-and-fixed merge-mechanics casualty: a reverse-hop/bounce-revert
interaction silently dropped the numRuns tuning from a received parcel,
caught by re-running the ticket's own acceptance and fixed by restoring
from the coder's real commit. Independently confirmed the FINAL merged
tree (what reached QA) carries no trace of that intermediate loss — the
property/assert and numRuns counts above are all measured against the
tree QA actually received and verified.

## Disposition

Approved. No rework needed. Landing next.

By QA.
