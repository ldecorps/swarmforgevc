# BL-1631 — hardener review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

Reviewed `rows-to-retire`, `registry-specs`'s new pole-register entry,
`rebuild-registry-content`, and `restore-other-tickets-registry-rows!`'s
combined restore+retire pass by eye (Babashka, no mutation/CRAP/DRY
wired). Confirmed the docstring's disjointness claim directly from
`rows-to-restore`'s own filter (`(not= owner task-ticket-id)`): a restore
candidate's owner can never equal the landing ticket, so restore and
retire never contend for the same row. Ran
`bb swarmforge/scripts/test/land_step_lib_test_runner.bb` (ALL PASS),
both acceptance features (BL-1631 5/5, BL-1604 5/5 - no regression), and
`bb swarmforge/scripts/standing_red_register_cli.bb .` (6 rows, all
owned, `"unowned":[]`). No leaked git worktrees (`git worktree list`
clean) or orphaned processes.

Two narrow observations, neither blocking:

- `rebuild-registry-content` reconstructs a registry file as
  `header + kept-data + restored`, extracting ALL comment/blank lines via
  `non-data-line?` regardless of their original position, rather than
  preserving an interspersed or trailing comment in place. All three real
  registries today have every comment strictly before every data line
  (verified directly), so this is not exercised. This assumption is not
  new to this ticket - `registry-data-lines` already relies on the same
  "all comments up front" shape for BL-1604's own key computation - so
  it is not a regression this parcel introduces, just an existing
  assumption the new rebuild path also leans on. Not worth a redesign
  against this ticket's tight scope; worth knowing if a registry ever
  gains a trailing comment.
- The new bb runner cases (07-09) copy BL-1604's own fixture idiom
  verbatim: a `scratch` git worktree built OUTSIDE the auto-cleaned
  `with-fixture` root, removed only by a bare inline `sh! ... "worktree"
  "remove"` with no try/finally, so an assertion failure earlier in the
  same block leaves it on disk. This is the SAME pre-existing pattern
  BL-1604's own cases (3089-3207) already use, not something BL-1631
  introduces; it self-heals (`fs/delete-tree scratch {:force true}`
  precedes every `worktree add`), and `root`'s own deletion clears the
  worktree's git-side bookkeeping regardless. Confirmed no `bl1631-scratch-*`
  or `bl1604-scratch-*` entries survive after this run.
