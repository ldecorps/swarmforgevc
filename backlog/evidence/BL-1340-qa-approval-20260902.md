# BL-1340 — QA approval (20260902)

Received: documenter commit `b34779b23d` (D1-D5, doc-only), forwarding
hardener `e7b64b98a5` (architect `de23548030`, cleaner `e5dc8849ea`, coder's
three-part fix per human ruling A), unchanged. Merged into QA at `9b33daac46`.

Ancestry confirmed: coder, cleaner, architect, hardener, documenter commits
are all ancestors of `9b33daac46` (`git log --oneline --grep="BL-1340"` on
this branch).

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile` in `extension/`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical file set
  to my own prior BL-1317 pass's standing-debt inventory
  (`checkOrphanedAuthoredDocs`/landPilotedTicket family,
  `constitutionDocCitations`, several real-tree scanners). None reference
  `promotion_gates_lib`, `acceptance_contract_gate_lib`,
  `pins-draft-conversion`, `bl626PromotionGateSteps`, or
  `promote_and_route_next`. Pre-existing, not this parcel's defect.
- Property suite (`npm run test:properties`): 24 files / 13 tests red, same
  standing-debt population, plus the allowlisted BL-871 vitest-worker
  timeout. BL-1340's own
  `test/bl1340SelfConvertingDraftInvariants.property.test.js` — 3/3 green.
- `bb swarmforge/scripts/test/promotion_gates_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/acceptance_contract_gate_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/acceptance_pointer_gate_lib_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/pre_qa_gate_gather_lib_acceptance_pointer_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/pre_qa_gate_gather_lib_acceptance_contract_test_runner.bb` — ALL PASS.
- `bb swarmforge/scripts/test/promotion_gates_cli_test_runner.bb` — ALL PASS.
- `swarmforge/scripts/test/test_promote_and_route_next_priority.sh` and
  `test_promote_and_route_next_no_limit_depth.sh`: both fail, independently
  reproduced by me — fixture gap (missing `daemon_cycle_guard_lib.bb` copy
  since BL-966/BL-967), not caused by this ticket. Confirmed byte-identical
  to the copy on `main` (`diff` empty against `git show main:<path>`).
  Same finding independently made by coder, cleaner, architect, and
  hardener, each recorded in their own evidence.
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-626-promotion-gate-rejects-unmaterialized-feature-draft.feature`):
  PASS, 10/10 scenarios — including the four new BL-1340 scenarios ("a draft
  the ticket pins itself to converting is promoted", "a parked draft the
  ticket does not convert is still refused", "buildability never reorders
  the expedite lane", "a parcel still naming a draft cannot reach QA").
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl626PromotionGateSteps` — registered (line 587).
- `.feature.draft` deleted: `specs/features/BL-1340-...feature.draft` no
  longer on disk; the four scenarios landed in BL-626's `.feature` in the
  same commit as their handlers.
- Wiring: `pins-draft-conversion?` (`promotion_gates_lib.bb`) and
  `declaration-draft` (`acceptance_contract_gate_lib.bb`,
  `pre_qa_gate_gather_lib.bb`) confirmed by grep — both real, not merely
  declared.
- `is_buildable()` / `buildable[]`/`other[]` pre-partition: confirmed
  removed from `promote_and_route_next.sh` (no hits on grep) — invariant 2
  (expedite ordering decided over the full eligible set) satisfied.
- Documentation currency: Specification.MD changelog entry, BL-626 how-to
  amended, new BL-1340 how-to authored and linked from `docs/index.md`
  (confirmed), `swarmforge/backlog-schema.md`'s `required_wiring` row
  documents its second reader, `docs/diagrams/swarm-flow.mmd` updated with
  a BL-1340 comment block and the BL-626 block corrected. No diagram
  change-trigger left unaddressed.
- Orphan processes: reaped a leftover vitest process group after the
  property-suite run before proceeding.

## Verdict

APPROVE. No open item. Landing `9b33daac46` (this QA merge commit) on
`main`.

By QA.
