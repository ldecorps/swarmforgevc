# BL-1100 QA bounce 2 — 20260825

**Reviewed tip:** documenter `31560f0689` (not merged — tip impure after gate race)
**Task:** `BL-1100-promotion-candidacy-is-decided-by-structured-fields-never-prose`
**Sibling:** `VERIFY BL-1100`
**Prior bounce:** `9b1b052aef` (deleted bounce evidence)

## Parcel-owned gates (run-or-blocked)

| Gate | Result |
|------|--------|
| Tip purity vs `origin/main` (at claim) | PASS — paths=8, dels=0 |
| Tip purity vs `origin/main` (post-gate fetch) | **FAIL → D1** |
| APS BL-1100 feature | 8/8 (before ancestry loss) |
| `npm run compile` | PASS |
| Sibling | VERIFY |

## Inventory

### D1 — `behavior` (blame: documenter)

**Failing command:**
```
git merge-base --is-ancestor origin/main HEAD  # fails after fetch
git rev-list --count HEAD..origin/main  # → 1
# dels_on_origin=0
```

**Commit hash checked:** `31560f0689`

**First error excerpt:**
```
main_NOT_ancestor after APS
behind=1: 34e8c34e7 BL-988: keep assigned_to documenter after tip-purity bounce
paths=9 dels=0
```

**Failure class:** `behavior`

**Expected vs observed:** Tip must keep current `origin/main` as ancestor through
QA gate window / land. Observed rematch2 raced by BL-988 bookkeeping land.

**Remediation:** `git fetch && git reset --hard origin/main` immediately before
QA handoff; restore BL-1100 only; keep bounce evidence including this file;
re-verify ancestry in the same session as `swarm_handoff.sh`.

## Verdict

BOUNCE → documenter. Do not land.

By QA.
