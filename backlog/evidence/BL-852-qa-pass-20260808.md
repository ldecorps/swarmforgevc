# BL-852-chase-sweep-respects-ambulance-hold — QA final pass (2026-08-08)

## Commit checked

QA worktree merge of documenter's `dfe8bca1a3` (merge commit to follow this
evidence file). Ancestry confirmed: coder `932a4a1f`/`b2911ade`, cleaner
`3ee40328`, architect `e68631f0`, hardener `f9503a50`, documenter `3d74713e`/
`dfe8bca1` are all ancestors of the merge. This ticket landed as part of a
documenter batch shared with BL-722/BL-847/BL-853; each ticket is verified
and evidenced independently per Article 2.6.

## Checks run (Article 4.4 complete inventory)

- Compile (`npm run compile`, node v22.23.2): clean.
- Full unit suite (`npm test`, node v22.23.2): 401/406 files, 7228/7237 tests
  green. The 5 remaining failing files
  (`briefingDigestLineCli`, `dependencyGateCliReportsAndScope`,
  `mermaidRender`, `renderBriefingDiagramsCli`, `startBridgeHeadlessCli`) are
  none of them touched by this ticket's diff (`chase_sweep_lib.bb` only).
  Host load at time of run measured via `uptime`: load average 382 (extreme,
  far above the ~2x-cores threshold the project's own flakiness lesson names
  for Stryker). Re-ran the 5 files in isolation with `--pool=forks
  --poolOptions.forks.maxForks=1`: 4/5 passed standalone
  (`briefingDigestLineCli`, `dependencyGateCliReportsAndScope`,
  `mermaidRender`, `startBridgeHeadlessCli`); `renderBriefingDiagramsCli`
  still timed out at 20s even alone, consistent with pure host-load
  contention rather than a code defect. Confirmed these same 2 tests
  (`test_ready_for_next_no_promotion.sh`'s unrelated failure, and this
  timeout class) reproduce identically on `main` HEAD (`6c7a9d9d`), so none
  of it is a regression introduced by this batch. Matches the known,
  separately tracked BL-816/BL-817 timeout-classification tickets already
  paused in the backlog.
- `bash swarmforge/scripts/test/test_chase_sweep.sh`: 14/14 scenario groups
  PASS (existing suite named in the ticket's notes, unchanged behavior for
  every non-held case).
- `bb swarmforge/scripts/test/bl852_chase_sweep_ambulance_hold_property_runner.bb`:
  300 runs/invariant, ALL PROPERTIES HOLD, both invariant-1 and invariant-3
  non-vacuity checks confirmed (a broken predicate/decision would be caught).
- Acceptance (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-852-chase-sweep-respects-ambulance-hold.feature`):
  11/11 scenarios PASS — held-not-chased, never-respawned-or-dead-lettered
  (5 recipient-liveness variants), patient's-own-parcel-chased-unchanged,
  release-resumes-from-frozen-count, terminal-reap-outranks-hold,
  in_process-nudge-stays-live, and the no-ambulance baseline.
- `required_wiring` independently re-verified by reading the code:
  `swarmforge/scripts/chase_sweep_lib.bb::item-ambulance-held?` calls
  `handoff-lib/default-ambulance-held?` (the one shared predicate, no second
  notion of held) and is consulted in `sweep-role-inbox!` before
  `decide-item-action`, which itself checks `held?` before every
  age/liveness branch (`cond already-terminal? "reaped" held? "held" :else
  ...`). `apply-inbox-item-action!`'s `case` has no `"held"` clause, so it
  falls to the `nil` default — a genuine no-op (no wake, no chase-count
  write, no telemetry).
- Invariants: all three declared invariants have real coverage — invariant 1
  and 2 by the property runner + acceptance scenarios 1-2/7/9-11; invariant 3
  by the property runner's dedicated non-vacuity check against the real
  `item-ambulance-held?`.
- No orphaned test/mutation processes before or after this pass
  (`pgrep -fl 'node --test|stryker'` empty both times).

## Verdict

Clean pass. No defects. Approved as part of the BL-852/BL-847/BL-853 batch —
see `backlog/evidence/BL-853-qa-pass-20260808.md` for the shared merge-up/
landing details. (BL-722 is excluded: still in progress upstream, not part
of this documenter batch's forward to QA.)

By QA.
