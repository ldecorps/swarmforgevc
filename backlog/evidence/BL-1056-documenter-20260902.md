# BL-1056-a — documenter pass — 2026-09-02

## Review inventory (Article 4.4)

Full checklist run against the hardened commit `713a32dd60` (merged
`5cfad64b5d`):

- README / top-level docs: no README section covers the pricing table or
  cost estimation directly — not applicable, no change needed.
- `docs/reference/Specification.MD`: **stale.** Line documented BL-627's
  intro-pricing exclusion as current fact ("its introductory $2/$10 through
  2026-08-31 is deliberately not modeled") — now false. Fixed: replaced with
  a BL-1056 paragraph describing the `until`/`then` fields, per-instant
  resolution, and the `pricing-windows` CLI. Added a new top-of-file
  changelog entry. "Last Updated" changelog convention followed (new entry
  prepended, prior entry kept, marked "Prior entry —").
- `docs/reference/specs/BL-627-pricing-table-correctness-and-coverage-invariant.md`:
  **stale.** Its ruling table recorded the intro-pricing exclusion as a
  final specifier ruling with no pointer to the reversal. Fixed: annotated
  the ruling row and added a cross-link both ways.
- New user-visible surface (`extension/src/tools/pricing-windows.ts` CLI,
  `PRICING_TABLE`'s `until`/`then` fields, `resolveRatesAt`/
  `estimateCostUsdAt`, `listPricingWindowAlerts`): no how-to existed for
  querying or using validity windows. Fixed: authored
  `docs/how-to/BL-1056-a-price-with-an-expiry-date.md` (task-oriented,
  single mode, linked from `docs/index.md` in the same commit).
- `docs/index.md` exhaustiveness: new how-to linked under How-to guides;
  BL-627 reference entry annotated with a pointer to the new how-to. No
  orphan created.
- Diagrams (`docs/diagrams/architecture.mmd`, `swarm-flow.mmd`,
  `handoff-flow.mmd`, `front-desk-flow.mmd`): none of this ticket's changes
  touch the extension-host/webview boundary, the tmux substrate
  relationship, `.swarmforge/` state layout, the pipeline topology, the
  handoff file lifecycle, or front-desk routing. No trigger fired — no
  diagram change needed.
- Ticket YAML / deprecator: no retirement involved (BL-1056 adds behavior,
  retires nothing); `docs/deprecated/` not applicable.
- Production code / tests: not documenter's domain; none touched beyond the
  factual doc fixes above.

## Verdict

NONE — no send-back-worthy defect found outside documenter's own domain.
The two stale-doc defects found above are documenter's own to fix and were
fixed in this pass (commit `e513e7905f`), not bounced.

By documenter.
