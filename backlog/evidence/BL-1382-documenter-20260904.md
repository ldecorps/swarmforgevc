# BL-1382 — documenter pass (2026-09-04)

## Received
`1654535691` (hardener; per `backlog/evidence/BL-1382-hardener-pass-20260904.md`).

## What changed, operator-visible
Two crontab-ownership predicates (`swarmforge_cron_lib.sh`'s
`swarmforge_cron_line_belongs_to_root`, `reconcile_shift_schedule_crontab.bb`'s
`strip-schedule-lines`) both claimed any line merely NAMING a path under
the root (`R/.swarmforge/operator/`, `R/start-swarm.sh`,
`R/stop-swarm.sh`), meant to sweep legacy hand-installed presets but
indistinguishable from a human's own hand-installed schedule. Overnight
2026-09-04 a full-stack stop erased three hand-installed shift lines from
the live crontab; the 09:00 weekday start was 40 minutes from being
missed. Per the human's verbatim ruling (option 1, SUP-17
2026-09-04T14:13:07Z), ownership is now marker-only everywhere — the path
clauses are removed, not narrowed. An unmarked line naming the root is
reported ("left in place (no swarmforge marker for this root): ...") by
both install and uninstall, never swept. A shared-corpus test asserts the
shell predicate and the bb strip agree on the same marker set.

## Doc-domain review
`docs/how-to/BL-1162-start-stop-swarm-cron-lifecycle-symmetry.md` is the
established home — its own "What gets managed" table stated the (now
removed) path clauses as fact.
- Narrowed the table to markers only (dropped the Operator scripts /
  Lifecycle wrappers path-pattern rows).
- Added a new "An unmarked line naming the root is reported, never swept
  (BL-1382)" section: the incident, the fix, the report format, the
  shared-corpus agreement guard.
- Corrected "Verify state" (previously implied a full sweep on stop; now
  states marker-only removal, unmarked lines reported and surviving) and
  added a dedicated Verify block with this ticket's test scripts.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: extended the existing BL-1162 summary line.
- Diagram registry checked — no diagram depicts cron predicate internals
  at this granularity (consistent with every other cron/reconcile ticket
  this session).
- Ran `test_bl1382_unmarked_cron_lines_survive.sh` — ALL PASS (11/11).
- Ran `test_bl1382_cron_ownership_agreement.sh` — ALL PASS (6/6).
- Ran `bl1382CronOwnershipMarkerOnly.property.test.js` (from `extension/`)
  — 3/3 PASS.

## Verdict
Doc content extended in the owning how-to page plus the standard
Specification.MD/index.md entries, not NONE. Forwarding to QA.
