# BL-1336 — QA approval (20260903)

Received: documenter commit `76e5ea1a24`, forwarding hardener, architect,
cleaner, coder's rotation-signal export + router-aware fork ceiling per
the human's ruling ("fixed router ceiling above the current default, with
the memory budget still the binding cap"). Merged into QA (two additive
conflicts resolved: `specs/pipeline/steps/index.js` had a leftover
duplicate `bl1352EscalationTransportFaultSteps` line from the prior
merge, removed; `docs/reference/Specification.MD`'s changelog-prepend
resolved as BL-1336's entry on top, BL-1352's as "Prior entry").

## Review inventory (complete pass, Article 4.4)

- Compile (`npm run compile`): clean.
- `npx vitest run test/bl1336RouterForkCeiling.test.js`: 6/6.
- Acceptance
  (`specs/features/BL-1336-router-rotation-raises-the-vitest-fork-ceiling.feature`):
  6/6.
- Feature-handler registration: `specs/pipeline/steps/index.js` requires
  `./bl1336RouterForkCeilingSteps` — confirmed.
- `required_wiring`: `SWARMFORGE_ROTATION` confirmed exported in
  `swarmforge.sh` beside `SWARMFORGE_PACK`, sourced from a `rotation_signal()`
  helper `write_swarm_identity_file` also calls — one normalisation, two
  readers.
- Declared invariants
  (`extension/test/bl1336ForkCeilingInvariants.property.test.js`): 3/3.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated `resolveWorkerPoolSize`'s composition
  (`Math.min(ceiling, safeCount)` → `Math.max(ceiling, safeCount)`) — 2 of
  3 property invariants failed exactly as claimed (the RAM-budget-wins
  and darwin-ceiling-holds assertions). Restored (`git diff --stat`
  empty), re-ran 3/3 clean.
- Standing whole-tree guards re-run
  (`tempDirTrapGuard`/`socketFixtureShortRootGuard`/`liveRepoDerivationGuard`):
  3 failures, matching the same pre-existing/already-ticketed set as
  every prior pass today.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population. None touch this parcel's files.
- Property suite (`npm run test:properties`): 25 files / 14 tests red.
  13 of the 14 match the exact standing-debt population from every prior
  pass today. **One new**: `bl1218RemoteControlConfigInvariants` invariant
  2 ("a persisted launch script never disagrees with the config that
  wrote it") — plausible concern since BL-1336 touches launch-script
  generation, so re-ran in isolation: 4/4 clean, confirming a load flake
  under the full concurrent suite, not a regression from this parcel's
  diff. BL-1336's own property test — 3/3 green.
- Documentation currency: `docs/reference/Specification.MD`'s new top
  entry confirmed present and accurate; the existing BL-935 doc gained a
  bullet on the router-ceiling widening (no new how-to file needed for a
  test-tooling internal change). No diagram change-trigger fired.
- Orphaned test processes: reaped a leftover vitest process group after
  the property-suite run before proceeding.

## Live-signal check (qa_e2e_procedure's live half) — deferred, per policy

The ticket's `qa_e2e_procedure` asks QA to launch a swarm under a `config
rotation router` pack and confirm `SWARMFORGE_ROTATION` is present in the
resident pane's live environment. This worktree's own swarm is currently
running under `full-forge` (checked: `.swarmforge/swarm-identity` —
`launch_pack full-forge`, `rotation` empty), and Workflow Rules forbid
running `./swarm` from an agent worktree (it kills and relaunches the
whole swarm). The coder's own evidence flags this same constraint and
explicitly defers the live check to QA — but QA is bound by the same
worktree-discipline rule the coder is, and this worktree is not running
under the pack the check requires. Verified statically instead (the
export line's unconditional presence in `swarmforge.sh`, confirmed above)
— the same fact one layer earlier, matching the coder's and hardener's
own treatment. The live confirmation remains an operator/coordinator
action outside this pipeline's worktree constraints, not a gap in this
parcel's own correctness.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
