# BL-1275 land note — merge-deletion guard blocked a plain merge, 2026-09-05

Same BL-1403 class as BL-1370/BL-1401/BL-1368/BL-1400/BL-1353: a plain
`git merge 7f74743ce9` into the QA worktree was refused by
`check_merge_deletion.sh` over the two unattributed intake-archive
deletions.

Replayed this ticket's own attributed paths (coder/cleaner/architect/
hardener/documenter, including the documenter's separate untagged
`docs/how-to/BL-570-property-suite-drift-guard.md` commit `b087eb1804`)
as a plain, non-merge commit (`4801bdfd87`) instead, re-verified
`npm run compile` clean, shell e2e 13/13, property 2/2, acceptance 4/4,
then built and landed the tip-pure commit (`c1ba9dbaea`).

One commit-time hiccup: the tip-pure commit attempt was refused once by
`property-suite-guard` on `bl956PipelineBoardCaptionCapInvariants.property.test.js`,
unrelated to this diff — confirmed flaky (passed cleanly in isolation),
retried and the retry passed clean.

Land-approval recorded citing `4801bdfd87` (the plain replay commit) as
source: `{"ticket":"BL-1275","commit":"c1ba9dbaea","source":"4801bdfd87"}`.
Verified `is_qa_ancestor.sh c1ba9dbaea` prints `approved: ... - BL-1334`.
`abandoned_commits: [7f74743ce9]` recorded on the ticket.

By QA.
