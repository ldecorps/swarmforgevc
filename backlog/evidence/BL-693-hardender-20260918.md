# BL-693 — hardender review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

### Gherkin mutation (first pass for this feature)

`run_gherkin_mutation.sh specs/features/BL-693-repeated-paragraph-gate.feature`
against `specs/pipeline/steps/index.js`, `hard`:

- `doc-dup-gate-03` (7 examples, structural-line kinds): 7/7 killed. Every
  casing mutation (`code fence`→`code fencE`, `diagram glyph`→
  `diagrAm glyph`, etc.) is rejected by the step handler's `KNOWN_VALUES`
  lookup with `unknown structural line in Examples: ...` — exactly the
  Scenario Outline handler discipline engineering.prompt requires.
- `doc-dup-gate-02` (2 examples, repeat count 2 and 286): both mutants
  (`2`→`3`, `286`→`278`) survived. The step regex captures the mutated
  Examples value for BOTH the fixture (`Array(count).fill(LONG_LINE)`)
  and the assertion (`finding.count === count`), so the scenario checks
  "the guard reports back whatever count it was given" — a property with
  no dependence on which specific count is chosen. Verified empirically:
  ```
  node -e '... findDuplicateLines(Array(n).fill(long).join("\n")) ...'
  ```
  for n = 2, 3, 286, 278, 1000 — every one reports back exactly `n`
  occurrences; `findDuplicateLines`'s only size-sensitive comparison is
  `lineNumbers.length > 1`, unaffected by which count crosses it. Accepted
  as equivalent per BL-234/BL-1081: no assertion could ever discriminate
  between two positive integers here, because the code path treats every
  count identically by design.

### Hand-mutation sweep, `docsDuplicateParagraphGuard.js` (BL-567/BL-638 fallback)

Plain JS test helper under `extension/test/helpers/`, outside Stryker's
`out/**/*.js` mutate scope. Five hand-authored mutants, each applied
in-place and restored (`diff` confirmed byte-identical after every
restore; no live mutation job was running during the sweep):

| mutant | change | result |
|---|---|---|
| A | `trimmed.length <= threshold` → `< threshold` | KILLED |
| B | `lineNumbers.length > 1` → `>= 1` | KILLED |
| C | seen-dedup guard removed (always push to `order`) | KILLED |
| D | `text.length > 80` → `>= 80` (truncation boundary) | SURVIVED — closed |
| E | truncation ternary removed entirely (`text;`) | KILLED |

Mutant D was a real gap: the only existing truncation test used a
300-char fixture (well over 80), so it could not tell `> 80` from
`>= 80`. Added `formatFinding does not truncate text at or under 80
characters` (exactly-80 fixture, asserts the full text appears
untruncated), confirmed it kills mutant D, then confirmed all 21 tests
pass on the restored (unmutated) file.

### CRAP / DRY

`docsDuplicateParagraphGuard.js` is a test helper, not `src/*.ts` — out of
CRAP's scope by definition (crapReport.js reads `src/*.ts` paths only).
`npx jscpd` over the helper and its test file: 0 clones (jscpd's own
config excludes `test/` from its default glob, so this is a coarse
check, not evidence of anything beyond "no accidental copy-paste in the
helper module itself").

### Acceptance / suite

`run_acceptance.sh` on the feature: 13/13. `docsDuplicateParagraphGuard.test.js`:
21/21. Property lane (`npm run test:properties` scope,
`bl693DocsDuplicateParagraphInvariants.property.test.js`): 2/2, unchanged
from the coder's own non-vacuity-verified pass.
