# BL-1766 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Commit tested: 05e41332f1 (documenter 2bcf9419f8 merged; BL-1766's code arrived
with the BL-1764 merge f0779774a2; both merges checked against both parents;
BL-1700's paths are kept out after its bounce and revert).

Gates, one run each on 05e41332f1 (qa-gather):
- `qa-sibling-check.js status`: `VERIFY BL-1766`. `pre_qa_gate.sh`: OK.
- standing-red register: 8 rows, all owned, `unowned: []`. BL-1766's row
  (`bl1648RegisterCliInjectedDateInvariants.property.test.js`) names BL-1766,
  so qa_e2e step 3 holds. It retires at the land.
- `run_acceptance.sh` on the BL-1766 feature: 2 of 2 (Etc/GMT-14, Etc/GMT+12).
- qa_e2e step 2: `TZ=Europe/London npx vitest run --config
  vitest.properties.config.mjs test/bl1648RegisterCliInjectedDateInvariants.property.test.js`
  passed 3 of 3 (02:54 BST).
- Change read: the property takes "today" from Node's local getters
  (`localDay()`), the day `LocalDate/now` reads. It still runs the CLI with no
  `--now` and asserts age 0, except when a local midnight falls between its two
  day reads. The CLI is unchanged.
- Straggler checks: a live Stryker run in `.worktrees/hardender` (pids 7045,
  7050, 7436; not a QA run) overlapped both lanes.

HOLD (Article 4.2): two lanes red on files this parcel does not touch.
- `npm test`: exit 1. 1 of 10875 failed:
  `extension/test/briefingDigestLineCli.test.js` "the compiled CLI runs against
  the real repo and prints both lines": `Error: Test timed out in 20000ms.`
  (duration 24668 ms, `.vitest-report.json`). The register join is "absent".
  `backlog/suite-poles.tsv` row 20 gives this file to BL-791 only as a
  DURATION pole (36.3 s, "cut work not yet minted"). No open ticket owns the
  timeout red (precedent: bl1277's timeout red is BL-1600, separate from its
  pole). UNOWNED. The run shared the host with the hardener's Stryker run.
- `npm run test:properties`: exit 1. `dangerouslyIgnoreUnhandledErrors` is set,
  so this is a real test failure, but the failing file's FAIL line is not in
  qa-gather's 4000-char tail excerpt. The excerpt holds only 11 allowlisted
  BL-871 `onTaskUpdate` timeouts, the property lane writes no JSON report, and
  the join comes from that excerpt. So the red is UNIDENTIFIED. The touched
  file passes alone (above), so it is in a file this parcel does not touch.
  Per the one-run rule the lane was not re-run to name it. On resume the gate
  re-runs on this commit with the full lane logs kept under `tmp/`.
  This is the second time qa-gather has lost a red lane's failing file this
  way (first: BL-1726 QA, 2026-09-25).
The parcel waits for an owner; this is not a bounce.

## Resume (hold RELEASED 2026-09-26)

Released on the specifier's note: `briefingDigestLineCli.test.js` is owned by
BL-1770 (register row 42, ed6df73f16). Re-ran the gate on the parcel commit
05e41332f1 itself, in a detached git worktree with full history, with the full
lane logs kept:
- `npm test`: 638 files, 10875 of 10875 passed.
- `npm run test:properties`: 466 files, 1345 of 1345 passed; only allowlisted
  BL-871 `onTaskUpdate` lines.
The first run's unit timeout and its unnamed property failure did not
reproduce on this run. The timeout's file is owned (BL-1770). The property
failure's name was lost to qa-gather's tail excerpt (owned by BL-1769, per the
specifier's note), and nothing red remains to own. The first run shared the
host with a hardener Stryker run; this one started at load 10.9.

Earlier gates stand: acceptance 2 of 2, the touched property file 3 of 3 under
Europe/London, pre-QA gate OK, and the register row names BL-1766.

Verdict: NONE stands. Approved; the hold closes as approved at the land.
