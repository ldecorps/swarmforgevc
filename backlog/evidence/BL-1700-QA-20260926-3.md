# BL-1700 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Third pass, on the D1 rework (documenter 6347ed9a7b, merged into QA as
b84e1d9f40; checked at 0ee3630f45).

Against the second-pass bounce:
- D1 fixed (coder 0997efcb03): `probe!` falls back to
  `<repo root>/backlog/evidence` when no `--evidence-dir` is given. QA ran
  `model_steward_cli.bb probe standin-model --stand-in pass --scenario
  01-one-line-fix` with no flag, and it wrote
  `backlog/evidence/local-coder-probe-standin-model-<UTC>.md`. QA removed that
  file afterwards. The lib test runner now checks this case (ALL PASS), and
  the how-to no longer calls the flag optional.

Gates (qa-gather, tmp/BL-1700-gather3.json): no stragglers before or after,
`VERIFY BL-1700`, the register has 2 rows and `unowned: []`, `pre_qa_gate`
reports wiring OK, `npm test` exited 0, and `npm run test:properties`
exited 0.

Acceptance: the gather's run was red on scenario 03 (the leak check). The
leaked process was a tmux server that QA's own earlier hand run started at
10:52:12Z, 40 s before the gather began and 9 min before scenario 03 ran.
The parcel's run did not start it. QA reaped the leak. A re-run from a
script file (the step's ps grep also matches a shell whose command line
names the probe path) passed 7 of 7 and left no probe process behind.

Register: no BL-1700 row in standing-reds, the property allowlist or
suite-poles.
