# QA pass — BL-1415 — 2026-09-05

Commit under review: `e5416f4b3e` (merge of documenter's `e3af5a2497` into
`swarmforge-QA`), carrying the full chain (`3c8e08e3e4` BL-1415 coder,
confirmed ancestor via `git merge-base --is-ancestor`).

Root-caused from the exact BL-1384/BL-1402 false-positive incident this
session's own memory notes track; the ticket was itself RE-PENDED once
after the specifier discovered the original approval was based on an
incomplete mailbox search (master checkout only, missing worktree
mailboxes — the same class of gap flagged in this session's memory).

## Verification

- `bb swarmforge/scripts/test/dropped_parcel_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/bl1097_router_dispatch_trail_test_runner.bb`:
  ALL PASS.
- `bash swarmforge/scripts/test/test_bl1097_router_refuses_dispatched_ticket.sh`:
  ALL PASS (10/10), including scenario 08 ("a dispatch completed long ago
  with nothing after it is DROPPED and routes without --force"), scenario
  09 (CLI/router/sweep agree on the reason text), and scenario 10 ("a
  dispatch note still unread past the stall threshold is DISPATCHED,
  never DROPPED" — the exact false-positive class from the incident).
- Read `handoff-event-ms-from-headers` directly: takes the max of
  `enqueued_at`/`created_at`/`dequeued_at`/`completed_at` — confirms the
  clock now reads recipient action, not just creation, and is the single
  shared derivation both the sweep and the trail-index builder use (no
  second copy, BL-978 invariant 2 preserved).
- Read `ticket-dispatch-verdict`: reuses `decide-dropped-parcel?`
  unchanged, adds only the `:undispatched` case and a `:reason` string
  shared verbatim with the sweep's own nudge text — confirmed no second
  message copy.
- Confirmed the router's three-way branch directly in
  `route_backlog_to_coder.sh`: DISPATCHED still refuses without
  `--force`; DROPPED routes without `--force`, printing the same reason.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1415-a-dispatch-that-never-reached-its-recipient-is-lost-not-dispatched.feature`
  — 6/6 scenarios pass, including "a git_handoff for the ticket in any
  worktree mailbox state means the ticket was worked and the router
  refuses" and "a dispatch the recipient acted on less than the threshold
  ago is not a drop".
- Regression: `bl1301_parked_ticket_invariants_property_runner.bb` — ALL
  PASS (fail-closed on opt-in, blast radius, never-invisible all hold
  unchanged, per BL-1301's own contract this ticket must not weaken).
- `required_wiring`: all four anchors confirmed present
  (`route_backlog_to_coder.sh::DROPPED`, `dispatch_trail_cli.bb::DROPPED`,
  `chase_sweep_lib.bb::completed_at`,
  `specs/pipeline/steps/bl1415LostDispatchSteps.js::registerSteps`).
- Full unit lane (`npm test`): 3 pre-existing unrelated failures — no TS
  production files touched by this ticket (bb/sh-only), no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
