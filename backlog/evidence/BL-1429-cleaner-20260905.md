# BL-1429 — cleaner pass, 2026-09-05

DRY fix applied (no behavior change) — real duplication found and
eliminated; coder's fold logic itself is correct and complete.

## Defect found and fixed

`jscpd` found 3 clones: the register-row-writing loop
(`writeRegisterFixture` / `writeStandingCategory`) was independently
written three times — in `emitThrottleRecommendationStandingRed.test.js`,
`bl1429StandingRedThrottleFoldInvariants.property.test.js`, and
`specs/pipeline/steps/bl1429StandingRedThrottleSteps.js` — identical
except for a cosmetic fixture-filename prefix per file. Extracted into
`extension/test/helpers/standingRedRegisterFixture.js` exporting
`writeStandingRedRegisterFixture(root, { count, oldestAgeDays,
unownedCount, filePrefix })`; all three call sites now delegate to it
(the property test's `writeStandingCategory` keeps its own
category→fixture-spec table, a thin translation layer, and calls the
shared writer). No test behavior changed — same rows, same file layout,
same fixture-prefix values per file (passed explicitly), confirmed by
re-running every affected suite green after the extraction.

`jscpd` re-run on the touched/new files after the fix: 0 clones (was 3
clones, 21 duplicated lines / 289 duplicated tokens).

## What else was checked

- Direct diff review of `standingRedSignal.ts` and the
  `emit-throttle-recommendation.ts` fold: `minRecommendedCap`/
  `describeChangeReason` correctly implement "lower wins, null is no
  constraint" (invariant 1) and correctly attribute the active/clearing
  signal (invariant 2), including the tie-break and severe-rework-beats-
  standing-red-1 case, traced by hand against several category
  combinations.
- `swarmforge.conf` diff matches the ticket's own text exactly (both keys,
  defaults 10/7, comment naming BL-1429 and the human ruling).
- `standingRedSignal.ts`'s crossing logic (`standingRedSignal`) takes
  `thresholds` as a parameter throughout, never a hardcoded 10/7 — matches
  invariant 3's non-vacuity proof.
- Confirmed neither `backlog/standing-reds.tsv` nor
  `swarmforge/scripts/property_suite_standing_allowlist.tsv` gained or
  needed a row removed by this ticket (it adds new green tests; it does
  not fix an existing red) — the only register hit is an unrelated
  pre-existing comment line naming BL-1429 in the register's own header.
- Mutation-site count on both changed/new TS files: `standingRedSignal.ts`
  94, `emit-throttle-recommendation.ts` 82 — both within the 100
  threshold, no split needed.
- `npm run compile` clean, both before and after the DRY fix.
- Confirmed the merge that brought this commit into cleaner did not
  resurrect the architect-reverted BL-1287 files (coder's branch predates
  that revert) — verified the merge diff added only BL-1429 files, no
  `caf26a4c07`-introduced path reappeared.

## Verification (all re-run in this worktree, all green, after the DRY fix)

| check | result |
|---|---|
| `npx vitest run test/standingRedSignal.test.js test/emitThrottleRecommendationStandingRed.test.js test/emitThrottleRecommendationCli.test.js` | 35/35 pass |
| `npx vitest run --config vitest.properties.config.mjs test/bl1429StandingRedThrottleFoldInvariants.property.test.js` | 4/4 pass |
| `node specs/pipeline/cli.js specs/features/BL-1429-standing-reds-throttle-intake.feature` | 8/8 |
| `node specs/pipeline/cli.js specs/features/BL-432-auto-tune-intake-throttle.feature` (regression) | 5/5 pass, unaffected |
| `npm run compile` | clean |
| `node extension/out/tools/mutation-site-count.js <both changed ts files>` | 94, 82 — both within |
| `npx jscpd` on touched/new files | 0 clones (was 3, now fixed) |

Forwarding the DRY-fixed candidate to architect.
