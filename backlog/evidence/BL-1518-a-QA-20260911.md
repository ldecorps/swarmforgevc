# BL-1518-a — QA review pass, 2026-09-11

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: npm run mutation -- stryker.bl1441bl620.config.json (ticket item 4 / qa_e2e: hardener re-run with a live specifier-inbox and coder-sent-dir census before/after)
- **Commit hash**: 96039a63cc
- **First error excerpt**: hardener evidence: "swarm_handoff.bb was skip-cooldown'd (mutation_cooldown_gate.bb: file age 0.87d < 1d cooldown, quiet host) — not mutation-tested this pass. No extension/src file changed by this ticket... so no Stryker/CRAP run applies."
- **Failure class**: acceptance
- **Expected vs observed**: hardener re-runs the BL-1441/BL-620 mutation config once with a specifier inbox/new + coder sent-dir census before and after, recorded in hardener evidence, to confirm the mutation-escape is closed (ticket item 4, duplicated in qa_e2e)
- **Blamed role**: hardener
- **Remediation pointer**: from the coder worktree with the coder launch env sourced: count .swarmforge/handoffs/specifier/inbox/new and .worktrees/coder/.swarmforge/handoffs/sent, run npm run mutation -- stryker.bl1441bl620.config.json to completion, confirm both counts unchanged and the report green, record in a new hardender evidence file

## D2

- **Failing command**: manual diff review: docs/diagrams/handoff-flow.mmd vs the new draft-root-guard! send-time gate in swarm_handoff.bb
- **Commit hash**: 727b8a801a
- **First error excerpt**: handoff-flow.mmd's VALIDATE gate node still reads only type/to/priority/commit checks; grep for root|OUTSIDE_ROOT|draft-root in the file returns nothing
- **Failure class**: behavior
- **Expected vs observed**: per Article 1.7 diagram currency, a new send-time gate added to the handoff mechanism (matching handoff-flow.mmd's own change-trigger: "when the handoff file lifecycle, its gates... change") is reflected in the SAME parcel
- **Blamed role**: documenter
- **Remediation pointer**: add a gate node for draft-root-guard! (HANDOFF_DRAFT_OUTSIDE_ROOT refusal, before WRITE) to docs/diagrams/handoff-flow.mmd using the existing gate/bad classDefs

By QA.
