# BL-1490 — QA review pass, 2026-09-08

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

This is a second pass, resuming an earlier hold (`BL-1490-QA-20260908.md`).
That pass found every required gate green but held approval per Article 4.2:
its adjacent-regression check of BL-977's own acceptance feature reproduced
the architect's out-of-parcel finding (scenario 05, "a wedged poll loop is
still caught within the in-sweep budget" — `bl977SupervisorProgressSteps.js`'s
fixture never backdates `handoffd.pid`, so the 2026-09-02 startup-grace hotfix
answers `:healthy` before the wedge branch), which was at that time an
unowned red — not in `backlog/standing-reds.tsv`.

Since then the specifier minted BL-1500 for exactly this red (`8f1dc4d6ac`,
`BL-1500: mint - BL-977 acceptance scenario 05 red ...`) and added its
register row. Merging `origin/main` (`Merge main 7277a420a3 into QA.`) into
this branch and re-running `bb swarmforge/scripts/standing_red_register_cli.bb .`
confirms `"unowned": []` (count 29) and the row itself names BL-1500 as
owner for `specs/features/BL-977-supervisor-never-halts-a-progressing-daemon.feature`.
Article 4.2's blocking condition no longer holds.

Re-ran, on the merged tip, both required gates:
- `bb swarmforge/scripts/test/bl1490_supervisor_tick_progress_property_runner.bb`
  → `ALL PROPERTIES HOLD`.
- `node specs/pipeline/cli.js specs/features/BL-1490-a-poll-cycle-phase-is-never-invisible-to-the-supervisor.feature`
  → 5/5 scenarios pass.

No orphaned `node --test`/`stryker` processes before or after (checked PIDs
matched by `pgrep -fl` were already dead — empty `/proc/<pid>/cmdline`).

Nothing else in this pass's scope changed since the first evidence file
(lineage, wiring, diagram currency, invariants) — those checks stand.
Approving and proceeding to merge-up/land/coordinator notification.
