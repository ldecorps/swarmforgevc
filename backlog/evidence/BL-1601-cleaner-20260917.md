# BL-1601 — cleaner review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By cleaner.

## Detail

- Reviewed `extension/test/helpers/tmpDir.js`'s `removeWithRetry` (bounded
  retry on ENOTEMPTY/EBUSY, rethrow after the last attempt - matches the
  ticket's FIRM "never swallow" direction exactly) and the two BL-1204
  test edits in `extension/test/telegramCursorOperatorExec.test.js`.
- DRY attempt, reverted: the two marker-wait loops added to
  `telegramCursorOperatorExec.test.js` are identical to each other and to
  the pattern `extension/test/helpers/waitForFileSync.js` (BL-1063)
  already exists to replace. Rewrote both to use `waitForFileSync` and
  re-ran - all 24 tests in the file still passed (4 consecutive runs) -
  but `specs/features/BL-1601-*.feature` scenario "exactly the two BL-1204
  spawning tests assert their marker after the dispatch" does a literal
  source-string match on the exact inline loop text (`bl1601RedeployTestsAwaitTheMarkerSteps.js`
  `MARKER_WAIT_LOOP`/`MARKER_ASSERTION` constants), so the DRY rewrite
  failed that scenario (`run_acceptance.sh specs/features/BL-1601-*.feature`
  4/5, then 5/5 after reverting). The step handler is out of cleaner's
  domain (Does Not Own: acceptance tests/handlers) - reverted the test
  file to the coder's exact original rather than touch it. A future
  ticket could relax the step handler to also accept the
  `waitForFileSync` shape, but that is a specifier-scoped acceptance
  decision, not a cleaner call.
- Re-ran after reverting: `npx vitest run test/telegramCursorOperatorExec.test.js
  test/helpers/tmpDir.test.js` → 39/39 pass;
  `npx vitest run --config vitest.properties.config.mjs
  test/tmpDirRemoveRetry.property.test.js` → 1/1 pass;
  `run_acceptance.sh specs/features/BL-1601-*.feature` → 5/5 pass.
- `git status --short` after reverting the DRY attempt: clean, tree
  byte-identical to the received commit for this file.
