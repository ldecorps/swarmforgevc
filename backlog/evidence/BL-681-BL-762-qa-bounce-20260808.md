# QA bounce — BL-681, BL-762 (2026-08-08)

Batch commit reviewed: `c785a890f7` ("Document BL-574, BL-681, BL-762", By documenter),
received via two `git_handoff`s (task BL-574, task BL-762 — no separate BL-681
handoff ever reached QA; see note at the end).

Full review pass completed: unit suite (`npm test`), property suite
(`npm run test:properties`), and the acceptance pipeline for all three
tickets' own feature files were all run. BL-574's own acceptance suite is
green (9/9) and no defect in this pass is attributable to BL-574 — see
sibling deferral below, not a bounce, for BL-574.

## D1 — BL-762: residual "facilitator" naming in the new finish-shift lib

1. **Failing command**:
   `cd extension && npx vitest run test/onboarderRenameNoResidualFacilitator.test.js`
   (also fails as part of the full `npm test` run.)
2. **Commit hash tested**: `c785a890f7`
3. **First error excerpt**:
   ```
   FAIL  test/onboarderRenameNoResidualFacilitator.test.js > no live git-tracked
   file still says "facilitator" outside the dated record and the named
   naming-decision citations
   AssertionError: unexpected residual "facilitator" mentions (BL-684):
   ["swarmforge/scripts/finish_shift_lib.sh"]
   ```
   Confirmed directly: `swarmforge/scripts/finish_shift_lib.sh:111` reads
   `_finish_shift_pidfile_alive "$op_dir/onboarding-facilitator-supervisor.pid"`.
   This is a legitimate dual-clear of the pre-BL-684 pidfile name (the same
   pattern `stop_ancillary_services.sh` and `launch_onboarder.sh` already use
   for backward compat), but those two files are explicitly allowlisted in
   `extension/test/onboarderResidualAllowlist.js` and the new
   `finish_shift_lib.sh` is not.
4. **Failure class**: `unit`
5. **Expected vs observed**: Expected the full unit suite green, no
   unallowlisted "facilitator" residue. Observed `finish_shift_lib.sh` (new
   in this batch) referencing the retired pidfile name without being added to
   `ALLOWED_EXACT_PATHS` in `onboarderResidualAllowlist.js`.

**Remediation pointer**: add `'swarmforge/scripts/finish_shift_lib.sh'` to
`ALLOWED_EXACT_PATHS` in `extension/test/onboarderResidualAllowlist.js`
(same treatment as `stop_ancillary_services.sh` / `launch_onboarder.sh`), or
drop the dual-clear line if the old pidfile name is not actually expected to
exist for this verb. Owning role: **coder** (introduced the file).

## D2 — BL-681: ticket's own acceptance: pointer is broken, and an existing fix never reached this parcel

1. **Failing check**:
   `test -f "$(grep '^acceptance:' backlog/active/BL-681-consolidation-never-drops-a-human-sentence.yaml | sed 's/^acceptance: *//')"`
2. **Commit hash tested**: `c785a890f7`
3. **First error excerpt**:
   ```
   $ grep '^acceptance:' backlog/active/BL-681-consolidation-never-drops-a-human-sentence.yaml
   acceptance: specs/features/BL-681-consolidation-never-drops-a-human-sentence.feature.draft
   $ ls specs/features/ | grep 681
   BL-681-consolidation-never-drops-a-human-sentence.feature      # only the promoted file exists
   ```
   The `.feature.draft` the ticket still points to does not exist — it was
   renamed to `.feature` in `15b9ff9d` ("BL-681: promote acceptance draft,
   wire step handlers for Article 5.3") without updating the ticket YAML's
   `acceptance:` field.

   This exact defect was already caught once: `2f76114c` ("BL-681: bounce
   evidence for stale acceptance: pointer", By documenter) and
   `a88d7764` record a documenter bounce for it, and coder already fixed it at
   `a60906ea` ("BL-681: fix stale acceptance pointer (.feature.draft to
   .feature)"), with a cleaner re-pass at `64ed4868`. **None of those three
   commits (`2f76114c`, `a60906ea`, `64ed4868`) is an ancestor of the reviewed
   commit `c785a890f7`** (`git merge-base --is-ancestor a60906ea HEAD` →
   not an ancestor). By commit timestamp, `2f76114c` (17:35:03) landed one to
   four minutes *after* documenter's own `git_handoff`s to QA for this same
   batch (17:31:07 and 17:34:07) — the fix cycle appears to have run on
   documenter/coder/cleaner's worktrees *after* the parcel was already sent
   to QA, and was never merged back into the lineage that reached QA.
4. **Failure class**: `acceptance`
5. **Expected vs observed**: Expected the ticket's own `acceptance:` field to
   resolve to a real file, and the already-authored fix (`a60906ea`) to be an
   ancestor of the commit under review. Observed the field pointing at a
   deleted path, with the fix sitting unmerged on `swarm/coder` /
   `swarmforge-cleaner`.

**Remediation pointer**: merge `64ed4868` (which already contains `a60906ea`)
forward through cleaner → architect → hardener → documenter → QA — no
re-diagnosis needed, the fix already exists. Owning role: **coder** (earliest
role whose own commit, `15b9ff9d`, introduced the stale pointer; also already
holds the fix commit).

## Sibling deferral — BL-574

BL-574's own acceptance suite (`specs/features/BL-574-prompt-engine-slice2-fragments-adapters.feature`)
is fully green (9/9), its unit tests pass, and neither D1 nor D2 touches any
file `prompt_engine_lib.bb` or its adapters/fragments/CLI use. Per BL-532,
BL-574 is **deferred**, not bounced — recorded via
`qa-sibling-check.js defer --ticket BL-574 --blocked-by BL-681 ...` and
`--blocked-by BL-762 ...` (two open blockers on the same shared commit), no
rework `git_handoff` sent for it.

## Other unit/property failures observed this pass — assessed as environmental, not this batch's defect

`npm test` also failed 3 `dependencyGateCli*` tests (host's global Node is
20.20.2; dependency-cruiser requires `^22||^24||>=26` — a toolchain/version
mismatch, not code), and hit 20s/120s timeouts in `renderBriefingDiagramsCli`,
`bounceDrain`, and (in `test:properties`) `bl760DuplicateChainGuard` and
`bl797MutationGateProbeCrashFallback`. Host load at the time: `load averages:
24.06 49.29 41.80` (`uptime`) — consistent with the project's known
severe-host-load flakiness pattern. `bl787NamedTunnelInvariants` failures
depend on live `cloudflared`/Cloudflare-edge registration, unrelated to this
batch. None of these seven files reference `finish_shift`, `prompt_engine`,
or the Article 5.3 consolidation clause (`grep` confirmed no match) — not
included as bounce defects, but noted here per the "run-or-blocked, never
silently omitted" inventory rule.

By QA.
