# BL-1568 coder evidence — 2026-09-15

## What was wrong

`swarmforge/scripts/test/test_dispatch_gap_autoroute.sh`'s case 01 asserted
exact map equality against `[{:id "BL-217" :assigned-to "coder"}]`.
`read-active-items` has carried a `:status` key since BL-1301
(393634823a, 2026-09-02), so `dispatch-gap-items` now returns
`{:id "BL-217", :assigned-to "coder", :status "todo"}` - one more key than
the exact-equality assertion tolerated, red since that landing.

## What changed

- `swarmforge/scripts/test/test_dispatch_gap_autoroute.sh`: case 01 now
  asserts `(count gaps)` is 1, `(:id (first gaps))` is `"BL-217"`, and
  `(:assigned-to (first gaps))` is `"coder"` - the id/assignee shape the
  auto-route draft (`dispatch-gap-draft-lines`) actually consumes,
  tolerating any further key the reader carries. Cases 02, 03, and 04 are
  untouched.
- `specs/pipeline/steps/bl1568DispatchGapAssertsByIdSteps.js` (new): step
  handlers for the specifier-authored feature
  (`specs/features/BL-1568-*.feature`, not touched by this parcel).
  Scenario 01 runs the real shell test as a subprocess; 02 diffs
  `chase_sweep_lib.bb` against `main`; 03 greps the test file for the new
  id/assignee assertions; 04 (Outline) runs the two named bb runners.
  Discovered automatically by `specs/pipeline/steps/index.js` (BL-1371), no
  manual registration needed.
- `swarmforge/scripts/chase_sweep_lib.bb`: untouched (confirmed by scenario
  02 and `git diff main -- <path>`).

## Known acceptance gap — flagged to the specifier, not worked around

The file still does NOT print `ALL PASS`. Case 04 (the idempotency guard)
fails independently, for a reason unrelated to `:status` and predating
this ticket - masked until now because case 01 always failed first and
exited the script before case 04 ever ran.

Root cause: case 02 constructs its draft via
`chase-sweep-lib/dispatch-gap-draft-lines` called with **no commit**
(1-arg form), which falls back to the "legacy soft-note" path - per that
function's own docstring, production `auto-route!` always supplies a
commit and gets a `git_handoff` instead (confirmed in `handoffd.bb`'s
`auto-route!`, which always calls the 2-arg form with `head-commit-10`).
The resulting note's message ("BL-217 is active with no dispatch on
record...") does not match `spec-work-ticket-id-pattern`, so
`dispatch-trail-ticket-id` (BL-1223's narrowed message-only predicate)
returns `nil` for it - it is never counted as a dispatch trail. Case 04's
second `dispatch-gap-items` call therefore still sees the gap, and
`(empty? gaps)` fails.

Confirmed by direct repro:
```
(dispatch-trail-ticket-id {:task nil :message (dispatch-gap-note-message "BL-217")})
=> nil
```

This parcel does not attempt a fix: doing so either touches
`chase_sweep_lib.bb` (forbidden by scenario 02 / the ticket's "Not in
scope" section) or changes case 02's draft construction to supply a
commit (which would then hit the `git_handoff` two-call self-audit,
BL-1529, and break case 02/03's own exit-0/queued-note assertions - the
ticket says cases 02 and 03 are unchanged). Sent a `note` (priority `00`)
to the specifier flagging this
(`.../from_coder_to_specifier.handoff`, 2026-09-15).

The standing-red register row (line 40, `backlog/standing-reds.tsv`) is
left as-is - the file is not actually green yet, so removing it would be
premature (that step is QA's, on a land that turns the file fully green).

## Invariants (BL-654)

No `invariants:` field on this ticket - no property-test obligation.

## Verification

- `bash swarmforge/scripts/test/test_dispatch_gap_autoroute.sh` 5x: case 01
  now passes every run; case 04 still fails every run (pre-existing,
  documented above).
- Break-then-restore: reverted case 01's assertion back to the old
  exact-equality form - case 01 failed again with the original
  `unexpected gaps` message; restored, re-ran, case 01 passed again.
- `bb swarmforge/scripts/test/dispatch_gap_test_runner.bb`: exit 0.
- `bb swarmforge/scripts/test/bl1097_router_dispatch_trail_test_runner.bb`:
  exit 0.
- Full acceptance run (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1568-the-dispatch-gap-autoroute-test-asserts-the-gap-by-id-and-assignee.feature`):
  4/5 scenarios pass; scenario 01 fails for the case-04 reason above.
- `git diff main -- swarmforge/scripts/chase_sweep_lib.bb`: empty.
- `git diff main...HEAD --name-only`: the shell test, the new step
  handler, and this evidence file only.

By coder.
