# QA pass — BL-1435 — 2026-09-05

Commit under review: `d0ad9365ee` (merge of documenter's `887a4f3fa7` into
`swarmforge-QA`), carrying the full chain (`0108b45bd5` BL-1435 coder,
confirmed ancestor via `git merge-base --is-ancestor`). Successor to
BL-1212's retired scenario 02 (dependency satisfied — I landed BL-1212
twice earlier this session).

## Verification

- Read `liveRepoDerivationGuard.js`'s widened detection directly:
  `REV_PARSE_TOPLEVEL_SRC` uses lookaheads for `__dirname`, `rev-parse`,
  and `--show-toplevel` in any order across `execFileSync`/`execSync`/
  `spawnSync`, and `LIVE_ROOT_INLINE_SRC` is an alternation of the
  original `path.join` idiom and this new one — the SAME binding feeds
  growth patterns, the production-escape rule, and `EXEMPTION_RE`
  unchanged (invariant 1: no second scanner).
- `npx vitest run test/liveRepoDerivationGuard.test.js`: 24/24 pass,
  including the real-tree scan (scenario 03's equivalent).
- Checked each of the 5 files the ticket names as newly visible:
  - `activePoolFreshnessAudit.test.js`, `docsStructureRealTree.test.js`:
    both carry `BL-1038-EXEMPT:` reasons.
  - `bl1300HeadroomProofIsPinned.test.js`: binds `REPO_ROOT` via
    rev-parse but only uses it to build one fixed path
    (`GATE_SH`) and to `git archive` a PINNED commit — no directory-growth
    surface, correctly needing no exemption (invariant 3's own
    allowance, read and confirmed against the file's actual usage, not
    assumed).
  - `gitEnvGuard.test.js`: its `rev-parse --show-toplevel` calls take a
    `target` variable, never `__dirname` — correctly not a live-root
    binding at all (confirmed the detection's `__dirname` lookahead
    correctly excludes it).
  - `swarmMetricsCli.test.js`: only a comment mentions the phrase, no
    actual binding — correctly unflagged.
  - All 5 files' own suites re-run directly: 79/79 pass.
- Acceptance: `run_acceptance.sh
  specs/features/BL-1435-a-rev-parse-root-is-a-live-read.feature` — 6/6
  scenarios pass, including "the exemption rule applies to a rev-parse
  root exactly as to a path.join root" and the bare-marker refusal
  (BL-1212's carried scenario, per BL-1006 retire-never-reword).
- `required_wiring`: `liveRepoDerivationGuard.js` confirmed recognizing
  `show-toplevel`; `specs/pipeline/steps/bl1435RevParseRootIsALiveReadSteps.js`
  exports `registerSteps`.
- Full unit lane (`npm test`): 3 pre-existing unrelated failures
  (`constitutionDocCitations`, `operatorRuntimeBbFixtureClosure`,
  `pricingTable`) — no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
