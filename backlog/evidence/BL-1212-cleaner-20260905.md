# BL-1212 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, minimal, and clean.
Acceptance scenario 02's failure is a confirmed, correctly-disposed
spec-gap (a stale ticket premise), not a defect in this work.

## What was checked

- Confirmed the single comment added to
  `docsStructureRealTree.test.js` (a `BL-1038-EXEMPT:` marker with a real,
  reviewable reason: the live read IS this file's own assertion, and its
  non-vacuity case already uses a fixture) — no other file touched, no
  guard code touched, no other exemption added, matching every one of the
  ticket's firm constraints.
- Confirmed `pilotMkdtempConventionCheck.test.js` (explicitly out of
  scope, owned by BL-1209) is untouched.
- Verified the coder's confirmed drift finding directly against the real
  source: `docsStructureRealTree.test.js`'s `REPO_ROOT` binding is now
  `execFileSync('git', ..., 'rev-parse', '--show-toplevel')` (landed by
  BL-1317, 2026-09-02, eleven days after this ticket was minted), while
  `liveRepoDerivationGuard.js`'s two detection regexes
  (`LIVE_ROOT_BINDING_RE`, `LIVE_ROOT_INLINE_SRC`) both only match the
  older `path.join(__dirname, '..', '..')` shape. The guard therefore
  never flagged this file at all, independent of this ticket's own fix —
  confirmed by reading both files' current content directly.
- Ran `test/liveRepoDerivationGuard.test.js`: 19/19 pass, 0 violations —
  matches the coder's claim, and is unchanged by this ticket's own edit
  (the exemption is inert against the guard's current pattern-matching,
  exactly as the coder found and documented, not because the fix is
  wrong).
- Ran `test/docsStructureRealTree.test.js`: 5/5 pass, unchanged.
- Ran the acceptance feature: 2/3 — scenarios 01 and 03 pass; scenario 02
  fails with an explicit, named `CONFIRMED SPEC GAP` error naming the
  BL-1317 drift and pointing at the coder's own evidence file, exactly
  the honest-failure disposition Article 4.4 calls for (a spec gap leaves
  by note, never silently papered over or falsely passed).
- `npm run compile` clean.
- This is a one-line comment addition; no duplication/architecture
  concern applies.

## Disposition of the acceptance scenario 02 failure

This is NOT a defect in the coder's work: the underlying deliverable (the
exemption comment) is correct and complete per every stated invariant and
constraint. The failure is the ticket's OWN acceptance scenario asserting
against a premise (the guard's derivation-shape detection) that drifted
after the ticket was minted, for an entirely unrelated reason (BL-1317's
Stryker sandbox path-depth fix). Per constitution, a spec gap routes as a
`note` to the specifier, never as a bounced parcel — the coder's evidence
states this note was already sent alongside the pass. Widening the
guard's detection boundary or amending the stale scenario is the
specifier's call to make, correctly not attempted unilaterally here (the
ticket's own constraints explicitly forbid touching the guard's
detection boundary).

## Verification (all re-run in this worktree, all green except the confirmed spec-gap scenario)

| check | result |
|---|---|
| `npx vitest run test/liveRepoDerivationGuard.test.js` | 19/19 pass, 0 violations |
| `npx vitest run test/docsStructureRealTree.test.js` | 5/5 pass |
| `node specs/pipeline/cli.js specs/features/BL-1212-...feature` | 2/3 — scenario 02's failure is the confirmed, documented spec-gap above |
| Direct read of both files | confirms the drift: guard regexes only match the old `path.join` shape, file now uses `execFileSync('git', ...)` |
| `npm run compile` | clean |
| `bl1212RealTreeDocsGateRecordsItsLiveReadExemptionSteps.js::registerSteps` exists | yes |

No fix required for this ticket's own scope. Forwarding unchanged to architect.
