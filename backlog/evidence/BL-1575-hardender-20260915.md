# BL-1575 — hardender review pass, 2026-09-15

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No production code touched (mutation_cost: low per ticket); no `extension/src`
surface, so CRAP/DRY tooling (scoped to `extension/src/*.ts`) does not apply,
and Stryker (`--mutate out/**/*.js`) does not cover `specs/pipeline/steps/`.
The feature file has no `Scenario Outline:`/`Examples:` — a plain `Scenario:`
has no example values, so BL-113 gherkin mutation reports `inapplicable`
(BL-638) rather than running; fell back to a hand-authored surgical sweep
over the parcel's own changed behaviour.

Verification run at load average 2.13/2.05/2.68 on 20 cores (quiet):
- `run_acceptance.sh` on BL-1495's feature: 7/7 pass, 6.8s.
- `run_acceptance.sh` on BL-1575's own feature: 3/3 pass, 12.0s.

Hand-authored mutants, both killed:
1. Deleted the `require('./lib/socketFixtureRoot')` line the ticket adds
   (undoing the fix). BL-1495 feature: 0/7 pass (`mkSocketFixtureRoot is not
   defined`). BL-1575 feature: 0/3 pass. Restored; `git diff` clean before
   re-verifying.
2. Removed the `f !== SELF` exclusion from `census()`'s file filter (the
   new handler's own `CALL_TEXT` literal and comments would otherwise
   misreport itself as a non-importing caller of `mkSocketFixtureRoot(`).
   BL-1575 scenario 03 failed as expected (2/3 pass, 1 fail — the census
   assertion). Restored via `git checkout --`; `git diff` clean.

No orphaned `node --test`/stryker processes after the runs
(`pgrep -fl 'node --test|stryker'`); scratch `tmp/bl149*-out` and
`tmp/bl157*-out` dirs removed before commit.

cleaner and architect passes both recorded explicit NONE (no defect).
Nothing to forward-fix.
