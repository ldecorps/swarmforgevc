# BL-1302 — QA pass, 2026-08-31

## Sibling check (BL-532)

`node extension/out/tools/qa-sibling-check.js status --ticket BL-1302` →
`VERIFY BL-1302` (no open deferral). Full verification pass taken.

## Lineage verified, not assumed

Full chain confirmed as ancestors of the documenter commit merged into QA
(`6e20423a1a`), all present in the citing merge commit `7787cd650d`:
coder `a2c09f69b1` → architect merge `a65a24773f` → hardener merge
`ae184987d4` (hardener own commit `bbdbc09a3d`) → documenter merge
`78bcebcacb` (documenter own commits `8369e33099`, `6e20423a1a`).
`git merge-base --is-ancestor` checked at each link (BL-336 discipline —
ancestor-of-HEAD alone proves nothing on a busy `main`).

## Independent re-run (not taken on trust)

| Command | Result |
|---|---|
| `bb duplicate_chain_guard_lib_test_runner.bb` | ALL PASS |
| `bb handoff_lib_test_runner.bb` | ALL PASS |
| `bb bl1302_reverse_copy_not_duplicate_chain_property_runner.bb` | ALL PASS, 200 runs, all 6 generator-reach floors met (incl. `exempt-hides-a-real-blocker`) |
| `bash test_swarm_handoff_inbound_non_forwarding.sh` (hardener's new mutant-killer) | ALL PASS, 3/3 |
| `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1302-….feature` | 3/3 pass |
| `bb suite_inventory_cli.bb` (safe anywhere; full `run_bb_suite.sh` NOT run — unsafe in-tmux, kills live panes per known incident) | ok — 444 test file(s), 440 standing, 4 excluded |
| `npm run test:properties` (extension/) | 268/293 files pass; 25 failing files diffed byte-for-byte against `swarmforge/scripts/property_suite_standing_allowlist.tsv` — **every failing file is already allowlisted under BL-1175**, zero overlap with this ticket's changed paths. The 1 unhandled error is the known-benign `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871) signature. No new red introduced. |

## Wiring confirmed (not merely unit-tested)

`duplicate_chain_guard_lib.bb`'s fixed `git-handoff-for-ticket?` and
`swarm_handoff.bb`'s `inbound-non-forwarding?` both call the shared
`handoff-lib/non-forwarding?`. The guard itself is invoked from the real
send-time validate path (`swarm_handoff.bb:336,472`), not only from its own
test runner — grepped directly, not assumed from evidence.

## required_wiring: correctly absent

Confirmed: the predicate sits inside a function already called on every
send; no new call site exists to anchor (matches ticket's own BL-1235
vacuous-anchor reasoning).

## Pre-existing red re-verified as already ticketed

`grep -rl test_reroute.sh backlog/` → `backlog/evidence/BL-1299-coder-20260830.md:129`
(`test_reroute.sh | red | red (same message)`). Not unowned, not a new
finding — confirms architect's own re-check.

## Diagrams

`grep -il "duplicate.chain\|non-forwarding" docs/diagrams/*.mmd` → empty.
No diagram depicts guard-level predicates; documenter's "no diagram change
triggered" claim confirmed independently.

## Backlog bookkeeping note

Ticket was found in `backlog/paused/` (not `active/`) when this parcel
arrived — the documenter's merge_and_process correctly renamed it
`paused/` → `active/` as part of the merge (git rename, 99% similarity).
Flagged only for visibility; not a defect in this ticket's own work.

## Landed

`git push origin HEAD:main` via BL-1144 discipline: acquired
`.swarmforge/land-main.publish.lock`, `--decide-only` → lock held (self,
no peer) → `:lock-admission :rematch-once-at-edge` per the tested 02b case
in `land_main_publish_test_runner.sh` — but `:purity-action :push` and
`:tip-contains-origin true` already, so the rematch step was a no-op;
pushed directly, then released the lock. `origin/main`:
`f902099ce2` → `7787cd650d`. Ticket `id: BL-1302`, not `GH-`-seeded — no
`issue_done.sh` step.

Merge-up broadcast sent to `coder,cleaner,architect,hardender,documenter`
(priority `00`) naming `7787cd650d`. Coordinator notified (priority `00`)
for bookkeeping (`active/` → `done/`).

By QA.
