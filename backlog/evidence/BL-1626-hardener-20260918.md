# BL-1626 — hardener review pass, 2026-09-18

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardener.

## Detail

### Mutation gap found and fixed in-pass (not a bounce)

This ticket already carries one architect bounce (`4a24ce1747`,
evidence `BL-1626-architect-bounce-20260918.md`) for
`buildFixtureScriptsDir()` leaking an mkdtemp fixture root with no
cleanup at all; the coder's fix (`2ea5f0e90f`) closed the demonstrated
leak by removing the directory in a `finally` around the terminal
"no library is missing" Then step's assertion.

That fix leaves one narrower gap: the WHEN step in between (`its
contents are diffed against the derived load-file closure...`,
`fs.readdirSync`/`computeClosure` over `ctx.fixtureScriptsDir`) has
nothing protecting it - a throw there (before the Then step's `finally`
is ever reached) still leaks the directory. Same class the architect
already named, one step earlier; also the same class I fixed in
`bl1632Bl1071ProbeCountsOnlyItsOwnFixturesHangsSteps.js` this same pass.

Fixed in commit `797d41019f` by registering the directory with
`specs/pipeline/steps/lib/fixtureReaper.js`'s `onAbnormalExit` the moment
it is created (the Given step), keeping the inline `finally` cleanup in
the Then step for the happy path. Confirmed with a standalone probe
script: a tracked mkdtemp directory survives a throw that happens before
any inline cleanup runs - reaped via the process `'exit'` handler
regardless.

All 4 acceptance scenarios re-run green after the fix; 0 leaked
`/tmp/bl803-scripts-closure-*`/`/tmp/bl1028-scripts-closure-*` directories
before or after. BL-803 (2/2), BL-1028 (10/10) and BL-1100 (8/8) - the
three previously-red features this ticket exists to fix - all still
green. Register rows for the three files correctly left in place per the
coder's own evidence (mirrors BL-1631/BL-1632's pattern: QA's land step
retires them via BL-1631's new mechanism, not this parcel).
