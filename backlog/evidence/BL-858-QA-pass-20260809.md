# BL-858-boot-prefix-over-cap-regression — QA pass — 2026-08-09

## Commit checked

`a3329b465c` (coder, "fix stale acceptance pointer flagged by QA bounce"),
merged into QA's branch. This is the fix for the sole defect (D1) recorded
in the prior bounce (`backlog/evidence/BL-858-bounce-20260809.md`,
commit `5fa430db`): the ticket's `acceptance:` field still named the
deleted `.feature.draft` after materialization to the live `.feature`.

Per `git merge-base --is-ancestor`, the bounced hardener/prior-pass work is
an ancestor of this commit — same parcel, targeted fix, not a fresh
unrelated commit.

## D1 fix verified fresh (not taken on the coder's commit message)

- `backlog/active/BL-858-boot-prefix-over-cap-regression.yaml:142` now reads
  `acceptance: specs/features/BL-858-boot-prefix-back-under-cap.feature` —
  matches the bounce's exact remediation pointer.
- `ls specs/features/BL-858-boot-prefix-back-under-cap*`: only the live
  `.feature` file exists, no `.draft`. The pointer resolves.
- Diff is a single-line change to the ticket YAML only — no other content
  touched, so nothing the prior bounce already verified clean needed
  re-checking beyond re-running the gates below.

## Checks re-run this pass

- `bb swarmforge/scripts/test/prompt_engine_test_runner.bb`: `stable-prefix
  chars: 43757`, `ALL PASS`.
- `run_acceptance.sh specs/features/BL-858-boot-prefix-back-under-cap.feature`
  (now resolvable via the ticket's own corrected pointer): 6/6 pass.
- `run_acceptance.sh specs/features/BL-618-stable-prefix-cap-regression.feature`
  (regression canary): 4/4 pass.
- Process hygiene: `pgrep -fl 'node --test|stryker'` clean before and after.

## Verdict

Clean. The one defect from the prior bounce is fixed exactly as specified,
nothing new introduced. `required_stages: [coder, qa]` — no other pipeline
stage owed a pass on this ticket. Approve.

By QA.
