# BL-1240 — documenter pass, 2026-08-31

Reviewed hardender commit `e0895a4633`'s inbound tip (architect's `adef3bca92`,
cleaner's `c7bfa6f186`, coder's `31ffc314a0`) — a re-land of `8bf2963bf2`
restoring 21 lines a prior BL-490 revert had silently dropped from
`swarm_handoff.bb`'s BL-1240 gate wiring.

## Architect's forwarded note, checked (not assumed)

The architect flagged that the same revert had also dropped BL-1240's own
documentation and asked the doc pass verify current doc content against
the RESTORED wiring rather than assume it was still current
([[documenter-must-not-skip-pass-because-docs-already-describe-behavior]]).
Did so directly, not by re-reading prose:

- `swarmforge/handoff-protocol.md` "Unregistered-Test Send-Time Gate
  (BL-1240)" section names `unregistered_test_gate_lib.bb`,
  `findings-for-git-handoff`, `parcel-own-changed-paths` (reused from
  `task_scope_gate_lib`), and `suite-inventory-lib`'s `test-file?`/
  `parse-manifest`/`manifest-name`. Grepped all four against the restored
  code: `swarm_handoff.bb:444-495` calls
  `unregistered-test-gate-lib/findings-for-git-handoff` and
  `blocked?` exactly as described; `unregistered_test_gate_lib.bb:105-135`
  reaches `suite-inventory-lib/manifest-name` and calls
  `task-scope-gate-lib/parcel-own-changed-paths` at line 126. Match.
- `docs/reference/Specification.MD`'s BL-1240 changelog entry (line 198)
  and `docs/how-to/BL-1240-unregistered-test-send-time-gate.md` describe
  the identical mechanics (parcel-scoped not tree-scoped, reused inventory
  logic, deleted-path exemption, fail-open posture, silent-row half owned
  by BL-1239). Same check, same result: accurate against the restored
  code, nothing to edit.
- `docs/index.md:139` still links the how-to page — present, not orphaned.

No content in any of the three docs described the REVERTED (broken) state
at any point — the prior revert removed the wiring, not the documentation
of what the wiring is supposed to do, so there was never a divergence to
fix. This pass's obligation was verification, which is now done and
recorded, not a skip.

## Docs updated

None. All three (`handoff-protocol.md`, `Specification.MD`,
`docs/how-to/BL-1240-...md`) already state the restored behavior exactly;
edited nothing to avoid churn with no content change.

No diagram (`docs/diagrams/{architecture,swarm-flow,handoff-flow,
front-desk-flow}.mmd`) depicts send-time gate internals — none of the four
change-triggers fired.

## Verdict

Clean. No defects, no doc drift found. Forwarding to QA.

By documenter.
