# BL-1718 — coder pass, 2026-09-24

## What landed

- **`specs/pipeline/steps/bl709BubbleItsOwnTelegramTopicSteps.js`**: the
  module-scope `const { ensureBubbleTopic } = require('.../telegramCursorBridgeLive')`
  replaced with a lazy, cached accessor (`telegramCursorBridgeLive()`) -
  the same shape this file's own `bridgeServer()` accessor already
  established for `bridgeServer.js` (BL-1687). Both call sites
  (`ensureBubbleTopic(...)`) updated to `telegramCursorBridgeLive().ensureBubbleTopic(...)`.
- **`specs/pipeline/steps/bl725RenameCursorRemoteTopicToHostSteps.js`**: the
  module-scope `const live = require(path.join(EXT_OUT, 'telegramCursorBridgeLive.js'))`
  (the `path.join` idiom, not the relative-path one) replaced the same way;
  the one call site (`live.ensureCursorTopic`) updated to `live().ensureCursorTopic`.
- **`specs/pipeline/steps/bl1718CursorBridgeLiveLoadsInsideTheStepSteps.js`**
  (new, required_wiring): the census scenario, matching BOTH require idioms
  in one regex (module-scope `const`/`let`/`var` at column 0, containing
  `require(...)` and `telegramCursorBridgeLive`) - proven to actually reach
  bl709 and bl725 by pinning both in the "mentions" assertion (BL-1445's
  own warning: a matcher blind to the `path.join` form would miss bl725
  entirely).
- **`extension/test/stepHandlerModuleLoadBudget.test.js`**: the
  non-vacuity fixture test's own require timing is host-load-dependent (a
  trivial one-`readdirSync` fixture measured 417ms on QA's busy host) - it
  now pins the fixture's `ms` to 0 before calling `checkHandlerBudgets`
  (the census helper's existing row-shape "injection seam" the ticket's
  own "How" names) and asserts the listing violation's PRESENCE rather
  than the violations array's exact length, so the assertion tests only
  what it claims to. The REAL `censusAllHandlers` call above it is
  untouched - it still proves the fixture's `listedDir: true` came from an
  actual `fs.readdirSync` at module load, not a hand-typed flag. No
  allowlist entry added anywhere (FIRM in approval_context: "timing
  variance is not an accepted reason") and `PER_HANDLER_BUDGET_MS`
  (400ms) unchanged.

## Verification

| check | result |
|---|---|
| `node -e` requiring bl709 fresh | 21 cached modules (was 212) |
| `node -e` requiring bl725 fresh | 23 cached modules (was 212) |
| `specs/pipeline/scripts/run_acceptance.sh` on this ticket's feature | 1/1 |
| `specs/pipeline/scripts/run_acceptance.sh` on BL-709's own feature | 8/8 (unaffected) |
| `specs/pipeline/scripts/run_acceptance.sh` on BL-725's own feature | 9/9 (unaffected) |
| `npx vitest run test/stepHandlerTmpRootGuard.test.js` | 4/4 |
| `npx vitest run test/stepHandlerModuleLoadBudget.test.js` | 8/8 |
| `npm test` (compile + unit lane) | 636 files / 10856 tests, all green |

## Invariant (BL-654)

"No step handler under specs/pipeline/steps requires
extension/out/tools/telegramCursorBridgeLive at module scope, in either
the relative-path or the path.join(EXT_OUT, ...) form." No separate
`*.property.test.js` file: the population this invariant quantifies over
(every real handler file's own source text) is exactly what the
acceptance scenario's census already drives end to end, against ground
truth - the same disposition BL-1585/BL-1586/BL-1587/BL-1691 recorded for
an invariant whose exhaustive check IS the acceptance scenario. Non-vacuity
(both mutations restored, diff empty, re-verified green):
- Reverted bl709's fix (put `ensureBubbleTopic` back at module scope) -
  scenario 01 failed, naming `bl709BubbleItsOwnTelegramTopicSteps.js`.
- Reverted bl725's fix likewise - scenario 01 failed, naming
  `bl725RenameCursorRemoteTopicToHostSteps.js` (proving the census
  actually matches the `path.join` idiom, not just the relative-path one).

## required_wiring

- `specs/pipeline/steps/bl1718CursorBridgeLiveLoadsInsideTheStepSteps.js::registerSteps` -
  present, discovered by directory scan (BL-1371), confirmed by the
  acceptance run passing 1/1.

## Out of scope (per the ticket's own text, not acted on here)

- BL-1659 (paused, depends on this ticket) - its own edit to
  `stepHandlerModuleLoadBudget.test.js` is a separate parcel.
- The other eleven handlers that mention `telegramCursorBridgeLive` -
  already lazy per the census; untouched.
- Docs/evidence beyond this file - the ticket's own Scope list has no
  documenter item for this defect.

By coder.
