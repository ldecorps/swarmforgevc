# BL-1224 QA bounce — 2026-08-30

Full inventory (Article 4.4). Commit checked out and tested: merge of
documenter `0de070faad` into QA.

## D1 — the architect's design-review gate is missing entirely

1. **Failing command**: `git diff b7d22b9ee~1 b7d22b9ee --stat` shows the
   full BL-1224 diff (9 files, from cleaner's merge `3bc1e28b53`) landing on
   the architect's branch for the first time; there is no commit ANYWHERE
   between cleaner's merge (`3bc1e28b53`) and hardener's merge
   (`a48a77fa9`, "Merge architect BL-1224... into hardender worktree") other
   than the bare merge `b7d22b9ee` itself ("Merge commit '3bc1e28b53' into
   swarmforge-architect (BL-1224 cleaner handoff)") and unrelated syncs
   (`86f833d7a` = a BL-1243 QA merge-up, `ed7cdaad6`/`0edf0f215`/`bacdaa9d5`
   = the BL-1235 bounce cycle on the same shared branch).
2. **Commit hash**: `0de070faad` (documenter, = the parcel handed to QA).
3. **First error excerpt**: `git log --format="%H %an %s" b7d22b9ee~1..a48a77fa9`
   names exactly one BL-1224 commit in that range, `b7d22b9ee` itself, a bare
   merge with zero insertions/deletions of its own. No
   `backlog/evidence/BL-1224-architect-*.md` file exists anywhere in history
   (every other ticket landed this session — BL-1279, 1280, 1277, 1281, 1284,
   1232, 1182, 604, 1243, 670 — has one, whether "no defect found" or a
   bounce).
4. **Failure class**: `behavior` (a missing gate, per Article 4.3's "a
   stage's pass is missing entirely" shape — not a code defect; the
   implementation itself is correct, see below).
5. **Expected vs observed**: Expected — Article 4.1 lists "Architect –
   Design review passed" as its own pipeline gate, independent of hardener's
   coverage/mutation gate; every other ticket this session recorded either a
   review-passed evidence file or a bounce. Observed — the architect stage
   produced no output of any kind for this ticket: no evidence file, no
   code change, not even a merge-commit message naming what was reviewed.
   The parcel simply passed through.

## Everything else checked, and clean — recorded so this bounce is not mistaken for a functional problem

- Own bb unit runner (`operator_runtime_watch_lib_test_runner.bb`): ALL PASS.
- Own property runner (`bl1224_watch_adoption_property_runner.bb`): ALL
  PASS, 400 runs/invariant, non-vacuous coverage across every named branch
  (different-live-runtime / tracked-alive / same-dead-pid / budget-spent /
  absent / tracked-dead / live-unrelated).
- Shell adoption test (`test_operator_runtime_watch_adoption.sh`), driving
  the REAL `operator_runtime_supervisor.bb --check-once`: 15/15 checks,
  matching every qa_e2e_procedure item (1-5) exactly.
- Acceptance (`specs/features/BL-1224-...feature`): 7/7.
- Read the real diff directly: `check-one!` is genuinely untouched (`decide`
  still calls it unchanged on the non-adoption path); no new liveness
  predicate (`adoptable-pid` takes the caller's own `alive?`); `:attempts`
  is genuinely carried over untouched in `adopt-entry`; `:pidfile-pid` is
  optional so any OTHER caller of `decide` (the constraint says `check-one!`
  is shared by five supervisors) is unaffected when it does not supply the
  key. All three constraints and all three invariants hold by direct
  reading, not merely by the passing tests.
- `specs/pipeline/steps/index.js` registers
  `bl1224WatchAdoptsRestartedRuntimeSteps` (no `required_wiring:` declared
  on this ticket, so this is a sanity check, not a gate).

This bounce exists solely to have the architect's own review actually
performed and recorded — not to request any code change. If the architect
finds nothing to add beyond what is already verified above, the correct
response is an evidence file saying so, matching every other ticket's
pattern this session.

## Owning role

The absent deliverable belongs to the stage that owns it. Routed to
**architect**.

By QA.
