# BL-1744 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `extension/src/tools/localQwenSeatLive.ts`
(`completeWithLocalModel` adds `think: false` to every request body,
plus docstring), `extension/test/bl1235LocalQwenSeatLive.test.js` (two
assertions), `specs/pipeline/steps/bl1744LocalSeatThinkOffSteps.js`
(new, pinned handler). mutation_cost: low; both coder and architect
already independently verified non-vacuity by reverting the source fix
and confirming both scenario rows/assertions fail, then restoring.

`completeWithLocalModel` CRAP = 3.00 (well under 6); every other
function in the file is also under threshold
(`node scripts/crapReport.js src/tools/localQwenSeatLive.ts`).

Independent hand-mutation, checked directly (a third, different mutant
from coder's/architect's field-removal check — a value FLIP rather than
an omission, to confirm the exact-boolean assertion discriminates a
wrong-but-present value, not just presence):
- Compiled, backed up `out/tools/localQwenSeatLive.js`. Mutated line
  165: `think: false` → `think: true`.
- `npx vitest run --config vitest.config.mjs
  test/bl1235LocalQwenSeatLive.test.js`: **failed as expected** — both
  request-body assertions (with and without a system prompt) caught the
  flipped value (2 of 20 failed, `deepEqual` diff showing `think: true`
  vs expected `false`).
- Restored the compiled file, recompiled fresh from source (`npm run
  compile`), reran: 20/20 green;
  `node specs/pipeline/cli.js specs/features/BL-1744-...feature`: 2/2
  green.

Checks re-run:
- `npx vitest run --config vitest.config.mjs
  test/bl1235LocalQwenSeatLive.test.js`: 20/20.
- `node specs/pipeline/cli.js
  specs/features/BL-1744-the-local-seat-asks-the-model-not-to-think.feature`:
  2/2.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1682LocalSeatBriefingSystemPrompt.property.test.js`: 2/2 —
  invariant B ("no key other than `system` ever changes") still holds.
- `git diff main...HEAD --name-only -- extension/src extension/test
  specs/pipeline` (excluding files from other in-flight tickets still on
  this branch tip) names exactly the three Scope files.
- Every standing whole-tree guard (this parcel touches
  `extension/test/`): `npx vitest run $(ls test/*Guard*.test.js | grep
  -v '\.property\.')` — 22 files, 211 tests, all green.

Not run: full Stryker (no `--mutate` run attempted — the file's CRAP is
already well under threshold and both prior stages plus this pass's own
independent hand-mutation confirm the one-line change is fully covered;
BL-1761 also still has the repo-wide Stryker-sandbox dry-run blocker
open), `npm run test:properties` in full (2026-09-17 rule — ran the one
relevant property file directly).

Fixture hygiene: `git status --short` clean of anything this pass's
runs could leak (pre-existing untracked BL-1652/BL-1666 scratch files
predate this session, not mine to remove — surfaced, not swept, per the
coder's own note).

By hardender.
