# BL-660 bounce — 2026-08-26

**From:** coder  
**Class:** spec-gap — acceptance feature path missing  
**Root cause:** Prior specifier commit (f1e533f00) wrote the feature file and
`acceptance:` pointer but never landed on `main`; YAML still held inline
"Specifier writes the scenarios" prose.

**Fix:** Restored
`specs/features/BL-660-three-shift-packs-conf-selectable.feature`
(10 scenarios), pointed `acceptance:` at it, added `qa_e2e_procedure` and
`specifier_decisions`. Gherkin lint clean.

**Commit:** df8d054fd
