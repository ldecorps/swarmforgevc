# BL-1209 QA Pass — 20260829

## BL-1241 entangled-tip rebuild

The documenter's handoff named commit `62ab4d2b44`. Its ancestry ran back
through the coder's long-lived branch, still carrying the same stale
BL-1246/step-scoping tangle BL-1273/BL-1193/BL-1251's own QA passes already
found and rebuilt around, plus a now-moot coder-side obstacle record
(`4732a3a198`, a fifth "BL-1251 blocked at handoff" note — moot since
BL-1251 already shipped via my own rebuild). Rebuilt tip-pure: cherry-picked
BL-1209's three plain, cleanly-scoped commits (coder `e272c6b019`, hardener
`89b920e955`, documenter `62ab4d2b44` — each verified via `git show --stat`
to touch only BL-1209's own files) onto the already-landed post-BL-1251
HEAD. `abandoned_commits` records the full superseded chain. `git diff
--stat origin/main HEAD` confirmed exactly BL-1209's own ten files.

## Commit under review

`8befd4f60` (tip of the rebuilt chain). BL-1209's own three commits
(`331adfb32`/coder, `45ed4d296`/hardener, `426f5cf70`/documenter —
content-identical cherry-picks of `e272c6b019`/`89b920e955`/`62ab4d2b44`)
are ancestors of it.

## Functional verification (qa_e2e_procedure)

1. **Pre-fix reproduction, now fixed**: `assessPilotMkdtempConvention` called
   against a fresh scratch mkdtemp root with an out-of-scope touched path
   (`['src/foo.ts']`) — previously threw `MODULE_NOT_FOUND`, now returns
   `{"checked":true,"testFilesScanned":0,"violations":[],"scannedPaths":[]}`
   with no throw.
2. **Scenario 01/02, both rows**: called against the SAME kind of scratch
   root (no detector present) with an in-scope file containing a real raw
   `mkdtempSync(path.join(os.tmpdir(), ...))` call — returns
   `{"violations":[{"file":"extension/test/foo.test.js","line":1}]}`,
   correctly flagged with file and line, against a root that is
   provably not the live repo.
3. **Scenario 02 non-vacuity / invariant 2**: confirmed via the same manual
   call above (no throw on out-of-scope) that the detector genuinely isn't
   loaded eagerly — matches the code's own lazy `loadRawMkdtempDetector()`
   call site, gated behind the in-scope check.
4. **Scenario 03**: `extension/test/pilotMkdtempConventionCheck.test.js`
   now builds fixture roots via `mkTmpDir('bl1209-subject-')` (the shared
   scratch-dir helper), confirmed by reading the test file directly — no
   `__dirname`-anchored live-repo root, no write into the collected test
   tree. The ticket's own acceptance scenario 4 ("the check's own tests
   leave the live test tree untouched") passes.
5. **Regression — the standing red this ticket exists to fix**:
   `pilotAcceptanceGateCli.test.js` → 42/42 pass (combined with
   `pilotMkdtempConventionCheck.test.js`), INCLUDING "main(): a
   claim-refused land now succeeds once the claiming sentence is amended
   out of the message, same diff throughout" — the exact case that had
   been `MODULE_NOT_FOUND`-red in the main unit lane since BL-743 landed
   on 2026-08-26. `tmpDirMigrationGuard.property.test.js` → 4/4 pass. The
   detector's own semantics are unchanged (constraint honored): `git diff`
   confirms `RAW_MKDTEMP_PATTERN`/`findRawMkdtempLines` moved verbatim into
   the new `rawMkdtempDetector.ts`, with `rawMkdtempGuard.js` re-exporting
   rather than duplicating (constraint: no second copy of the pattern).
6. **Live gate end-to-end**: covered by item 5 (`pilotAcceptanceGateCli.test.js`
   drives the compiled CLI as normally invoked) — the receipt-writing path
   that reports `mkdtempConvention.testFilesScanned` is exercised
   unstubbed and green.

## Constraints held

- `repoRoot = resolveRepoRoot(process.cwd())` in `pilot-acceptance-gate.ts`:
  `git diff origin/main~3 HEAD -- extension/src/tools/pilot-acceptance-gate.ts
  extension/src/tools/commitClaimGitReader.ts` is EMPTY — neither file
  touched, confirmed the exact forbidden line is unchanged.
- No second copy of the detector: the pattern lives once, in
  `rawMkdtempDetector.ts`; `rawMkdtempGuard.js` re-exports it.
- Gate-level stubs (`bl733ProducerCrosscheck`, `pilotScopedCrapEvidence`,
  `shellEntryPointDrive`, `pilotAcceptanceGate` x3) are unchanged — not in
  BL-1209's diff.
- Hardener's own fix (self-referential exemption gap): `EXEMPT_REPO_PATHS`
  in `pilotMkdtempConventionCheck.ts` now includes the check's own new test
  files, so the check's own fixture-string test data is never flagged as a
  violation of itself — read directly, confirmed sound.

## Two pre-existing, already-tracked failing files (not this ticket's)

- `tmpDirMigrationGuard.test.js` ("the real extension/test/ tree has zero
  raw mkdtemp call sites outside the shared helper") — 33 violations
  reported, matching `backlog/paused/BL-1280-mkdtemp-migration-completed-for-the-real-test-tree.yaml`'s
  own recorded count ("33 real sites plus 1 fixture string") exactly. This
  is BL-1280's own already-minted, already-tracked standing red — BL-1209
  changes nothing about the live tree's own call sites, only how the
  CHECK resolves its detector. Grepped `backlog/` before concluding: found
  BL-1280 directly.
- `pilotAcceptanceGate.test.js` (8 sub-tests, `checkOrphanedAuthoredDocs is
  not a function`) — present in the identical shape in the same-session
  BL-1273/BL-1193/BL-1251 baselines, unrelated to `pilotMkdtempConventionCheck.ts`
  or the detector.

## Regression

- Unit suite: `npx vitest run` → 19 failed files / 32 failed tests / 9600
  passed (572 files) — exactly ONE FEWER failing file and ONE FEWER
  failing test than the same-session baseline (20/33), matching
  `pilotAcceptanceGateCli.test.js` going fully green. No other file
  changed status in either direction.
- Property suite: `npm run test:properties` → 25 failed files / 14 failed
  tests / 734 passed (270 files, +1 file/+2 tests over the same-session
  BL-1251 baseline of 269/732 — exactly
  `pilotMkdtempConventionCheck.property.test.js`'s own 2 tests), 2
  allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"` (BL-871)
  errors, same count as baseline. No other file changed status.
