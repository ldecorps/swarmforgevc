# BL-1405 documenter review pass — 2026-09-05

Reviewed the hardened commit (`e54165e41a`) and the coder/cleaner/architect/
hardener trail for BL-1405 (a hand-built land records its land approval).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1405-a-hand-built-land-records-its-land-approval.feature`.
- Step handler exists:
  `specs/pipeline/steps/bl1405HandBuiltLandRecordsApprovalSteps.js` (the
  ticket itself notes no consumer `required_wiring` anchor is needed
  beyond the handler-file line, since QA's own hand-built land procedure —
  prose in `QA.prompt` — is the consumer, already landed at mint).
- Read `swarmforge/scripts/record_land_approval.bb` and
  `land_approval_cli_lib.bb`: confirmed the CLI is a thin wrapper over
  `land-step-lib/record-land-approval!` (invariant 1, one writer), prints
  `LAND_APPROVAL_RECORDED`/`LAND_APPROVAL_ALREADY_RECORDED` or refuses
  with `LAND_APPROVAL_UNRECORDED <reason>` on a missing sha, and shells
  out to the real `is_qa_ancestor.sh` for its printed `VERDICT` line
  (never a reimplemented check).
- Operator/QA-visible behavior change (the hand-built tip-pure land route
  now has a CLI to write the same replay-to-approved-source record the
  ordinary `land_step_cli.bb` path writes automatically, closing the
  standing Article 4.2 false-positive class from six 2026-09-04
  hand-lands) is documented per the ticket's own direction: added
  "A hand-built tip-pure land records its own approval too (BL-1405)" to
  `docs/how-to/BL-1241-entangled-tip-at-the-land-step-has-a-reachable-remedy.md`
  (the hand-built recipe's existing home doc), describing the three-step
  QA procedure (record, read the verdict, record `abandoned_commits:`)
  and cross-linking `is_qa_ancestor.sh`'s BL-1334 section by name, per the
  ticket's own "cross-links is_qa_ancestor.sh's BL-1334 section"
  direction.
- Confirmed `swarmforge/roles/QA.prompt` already carries the durable
  procedure step (lines ~126-144), landed at mint by the specifier per
  the ticket's own note ("The QA prompt step is landed at mint (below)")
  — role prompt files are the specifier's domain (Article 1.2), not the
  documenter's; no edit made there. Its "until then" interim hand-append
  fallback text is now superseded by the CLI's existence, but editing
  prompt files is out of the documenter's Owns section — left to the
  specifier/coordinator to reconcile if they choose.
- `docs/index.md`'s existing one-line summary for the BL-1241 how-to
  (already a running list of parenthetical per-fix clauses) extended with
  a BL-1405 clause in the same style.
- No "Last Updated" freshness field in the edited doc — no date bump
  owed.
- No registered diagram depicts this mechanism (a new CLI over an
  existing writer, not a new component, boundary, or pipeline-topology
  change) — no diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
