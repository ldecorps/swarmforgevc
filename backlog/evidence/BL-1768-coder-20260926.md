# BL-1768 — coder pass — 20260926

## Ground (c)

Added to `stray-superseded-verdict` (`swarmforge/scripts/land_step_lib.bb`)
as a third alternative, checked after grounds (a) and (b), which are
otherwise untouched:

- Requirement 1 (`stray-added-lines-explained-by-conflict?`): every
  non-blank line the stray's OWN patch (`sha^..sha`, never a diff against
  origin-main - that is ground (a)'s own base) adds at a path is either
  present verbatim in origin-main's current copy of that path, or is one
  of the incoming ("theirs") lines the cherry-pick conflict actually
  rendered there (`conflict-marker-theirs-blocks`, a new mirror of the
  existing `conflict-marker-ours-blocks`). A line that would simply vanish
  unexplained - dropped outside the conflict - fails this and the ground
  does not apply.
- Requirement 2 (`stray-rewritten-after-owner-landed?` /
  `owner-landed-commit-ancestor-of?`): every HEAD-side conflicting line
  (ground (b)'s own `conflict-hunk-head-ranges`/`blame-line-shas`) was
  last written by a commit that has one of the owner's OWN landed commits,
  on that same path's history, as an ancestor - the rewrite came strictly
  after the owner's own text landed, whoever authored it. Scoped per-path
  (never a repo-wide search) since the owner's original text necessarily
  touched that same path when it landed.

Reason text names the REWRITING commit(s), never the owner's own landed
one - matches ground (b)'s existing reason-text convention.

## Invariant (stated reason, no `*.property.test.js`)

"Ground (c) never skips a line the stray adds that origin/main lacks
unless that line sits in a conflicting hunk whose origin/main side was
last written only by commits descending from a landed commit of the
stray's owner; every other shape escalates by name." Babashka carries no
mutation/CRAP/DRY or property-test wiring (engineering.prompt "Startup
Tools"); the equivalent gate here is the real, non-mocked git fixture
this parcel's own test-runner cases exercise end to end:

- Positive (`land_step_lib_test_runner.bb`, "BL-1768 ground (c)"): the
  real a225d85d8b shape - owner lands its own text, another ticket
  rewrites it after - supersedes, naming the rewriting commit.
- Row 1 negative: the stray also adds a line origin-main never gets at
  all, outside the conflict - still escalates.
- Row 2 negative: the other ticket's rewrite predates the owner's own
  land (ancestry backwards) - still escalates.
- Pre-existing negative control (BL-1670's own, unmodified): an unrelated
  ticket's rewrite with NO prior owner land at all on that path - already
  covered, still escalates (ground (c) correctly finds no owner-landed
  ancestor candidate at all).

Non-vacuity: temporarily disabled ground (c) (`when false` guard) - the
positive case's four assertions failed exactly as expected (LAND_REPLAY
became LAND_ESCALATE, etc.). Restored; full suite green again (`bb
swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS).

## Scenarios (BL-1650's feature, same commit per BL-233)

Scenario 07 gains a third Examples row (the a225d85d8b shape). New
scenario 08 (two rows: the "extra unaccounted line" and "rewrite predates
the land" shapes) - both still escalate by name. All fixture builders are
explicit `KNOWN_VALUES`-style map entries (`SUPERSEDED_SHAPE_BUILDERS`,
`UNPROVEN_OTHER_OWNER_SHAPE_BUILDERS`), never passed through. 11 of 11
scenarios pass (6 pre-existing + 3 rows of 07 + 2 rows of 08).

## QA e2e procedure

1. `node specs/pipeline/cli.js` on BL-1650's feature: 11/11.
2. `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
3. Item 3 (a live run from the QA checkout over the approved tip carrying
   a225d85d8b) is QA's own step, not reproducible from this worktree.
4. Committed diff: `land_step_lib.bb`, its test runner, BL-1650's feature
   and step handler, and this evidence file only.

By coder.
