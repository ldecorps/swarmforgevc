# BL-1384 — cleaner pass, 2026-09-05

Clean sweep. No changes needed; forwarding as-is.

## Checks run

| check | result |
|---|---|
| `test/telegramFrontDeskBotCore.test.js` | 464/464 |
| `test/telegramCursorBridgeCore.test.js` | 131/131 |
| `test/telegramCursorBridgeLive.test.js` | 130/130 |
| property `bl1384LocalSeatTopicForwardedInvariants.property.test.js` | 2/2 |
| acceptance `BL-1384-...feature` | 4/4 |
| acceptance `BL-1235-...feature` (regression) | 5/5 |
| acceptance `BL-1296-...feature` (regression) | 6/6 |
| `required_wiring` anchor `readQwenLocalTopicId` | resolves at three real call sites (import, topic-map scrub, `buildPollAdapters` adapter) |
| `required_wiring` anchor `registerSteps` | resolves, handler file registers |
| `jscpd` on the two touched files | 9 pre-existing clones, none in this parcel's changed line ranges |

## Module structure review

Both wirings (`hasCursorBridgeTopicAdapter`,
`attemptCursorBridgeTopicExclusion`'s three-adapter resolve, the
`qwenLocalSeatTopicId` `PollAdapters` field, and the front-desk topic-map
scrub extension) are minimal, one-line-per-site additions that mirror the
existing `cursorBridgeTopicId`/`bubbleTopicId` pattern exactly — no new
abstraction needed, no duplication introduced. `decideCursorBridgeExclusion`
itself required no change (already took an arbitrary owned-topic array),
confirming the ticket's own claim that only the feeder side needed wiring.

## Mutation-site count (BL-485)

`telegram-front-desk-bot.ts`/`telegramFrontDeskBotCore.ts` are the same
pre-existing legacy monoliths flagged in BL-1383's and BL-1402's cleaner
passes — out of scope for a `slice_size_envelope: low` wiring addition of
this size (~25 lines total).

```
node extension/out/tools/mutation-site-count.js specs/pipeline/steps/bl1384LocalSeatTopicForwardedSteps.js
```

| file | sites | verdict |
|---|---|---|
| `bl1384LocalSeatTopicForwardedSteps.js` | 140 | over (not a Stryker target — `mutate: ["out/**/*.js"]` excludes `specs/pipeline/steps/`, same reasoning as BL-1413's cleaner pass) |

By cleaner.
