# QA pass — BL-1420 — 2026-09-05

Commit under review: `20d759589e` (documenter's own commit, fast-forwarded
into `swarmforge-QA`), carrying the full chain (`f18fccd9e2` BL-1420
coder, confirmed ancestor via `git merge-base --is-ancestor`).

Fixes a significant blind spot: BL-1011's acceptance feature had been
0/8 and BL-1012's 0/9 on `main` since 2026-08-27 (9 days), and the
bl1011 property runner held its properties vacuously over zero
violations — all silently, since acceptance runs are per-feature and
nobody ran these two.

## Verification

- `run_acceptance.sh specs/features/BL-1011-a-freshness-alarm-names-its-swarm-and-its-reason.feature`:
  **8/8 pass** (was 0/8).
- `run_acceptance.sh specs/features/BL-1012-the-freshness-watchdog-stops-manufacturing-its-own-incidents.feature`:
  **9/9 pass** (was 0/9).
- `bb swarmforge/scripts/test/bl1011_freshness_attribution_property_runner.bb`:
  ALL PROPERTIES HOLD, with real coverage across 8 distinct states (48
  runs) — no longer vacuous.
- **Independently verified non-vacuity myself**: temporarily made
  `writeGuardSatisfyingRows` skip one live supervisor's conf row
  (`names.slice(1)`), re-ran BL-1011's feature: all 8/8 correctly FAILED,
  naming the guard's own refusal (`FRESHNESS_REGISTRY_GUARD: unclassified
  supervisor script 'bridge_headless_supervisor'`). Restored the helper;
  confirmed byte-identical via `diff` against a backup, and 8/8 passing
  again afterward.
- Read `run-checker!` in the property runner directly: a non-zero checker
  exit maps to `{:ok? false :stderr ...}`, and the caller calls `report!`
  (a failed run) rather than folding it into the coverage/property
  evaluation — confirms invariant 2 (an unexpected exit is never an empty
  observation).
- Read `freshnessFixture.js`'s `supervisorNames`: derives from the same
  `*_supervisor.bb` glob the guard itself walks — confirms invariant 1
  (no hand-written supervisor list, the BL-1398/BL-1399 lesson applied
  here too).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1420-the-freshness-fixtures-pass-the-registry-guard.feature`
  — 6/6 scenarios pass, including "a fixture's conf carries one row per
  live supervisor script without editing the fixture" (the injected-
  scriptsDir proof, not a baked-in count).
- Read the hardener's BL-113 finding: the coder's own scenario-04 test had
  a self-consuming Examples cell (setup and assertion both derived from
  the same mutable numeral) — a genuine BL-908/BL-1263-class vacuity gap,
  fixed with a `KNOWN_VALUES`-style map; re-confirmed 6/6 mutants killed
  after the fix (was 3/6 survived before).
- `required_wiring`: all four anchors confirmed
  (`bl1011FreshnessAlarmNamesSwarmAndReasonSteps.js::FRESHNESS_REQUIRED`,
  `bl1012FreshnessSelfInflictedIncidentsSteps.js::FRESHNESS_REQUIRED`,
  the property runner's own `FRESHNESS_REQUIRED` env, and
  `bl1420FreshnessFixturesPassTheGuardSteps.js::registerSteps`).
- Full unit lane (`npm test`): 3 pre-existing unrelated failures — no TS
  production files touched (test-fixture-only), no regression.
- Full property suite (`npm run test:properties`): exit 0.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty.

## Verdict

APPROVED — merging up and landing.
