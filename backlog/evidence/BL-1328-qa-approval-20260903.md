# BL-1328 — QA approval (20260903)

Received: documenter commit `23c580e834` (review-pass evidence, no doc
defect found), forwarding architect (clean sweep), hardener (BL-113
Gherkin mutation re-run, including the required BL-1324 manifest regen),
and cleaner (one real bounce, properly reworked). Real feature work: a
narrow, human-ruling-authorized follow-up to BL-1324, closing the
`--model=<value>` single-token detection gap and documenting the
billing_guard/pane-env precedence asymmetry.

## Review inventory (complete pass, Article 4.4)

- **Bounce history check (BL-340) against `main`**: `bounce_count: 1`,
  cleaner bounced D1 (a stale BL-1324 Examples row asserting pre-fix
  behavior) on `e82c939e28`. Verified the fix in the current tree, not
  assumed from the bounce record — read `git log` for the specifier
  amendment `db083992a6` that retired (not reworded) the row, and
  confirmed the cleaner's own rework pass (`BL-1328-cleaner-rework-20260903.md`)
  re-verified 10/10 on BL-1324's suite before forwarding.
- Read `extra_cli_targets_qwen_cloud` in `swarmforge.sh` by hand
  (line ~1495): the equals-form check (`--model=qwen*`) is a SEPARATE
  `if` from the space-form check, so the fix cannot alter space-form
  matching — matches invariant 1 and invariant 2 (non-qwen values
  unaffected in either form, confirmed by the shell test's own negative
  cases).
- Confirmed both precedence-asymmetry comments present
  (`grep -n BL-1328 swarmforge.sh`): one at the billing_guard site
  (~1978), one at launch_role's pane-env site (~2262) — matches
  invariant 3. No precedence code changed, only detection and comments,
  confirmed by reading the diff.
- `bash specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1328-...feature`: 4/4.
- Sibling suites re-verified clean (retirement did not regress anything
  else): BL-1324's own suite 10/10 (one row correctly retired, not
  flipped — confirmed by reading the current Examples table, which
  carries a retirement comment and successor pointer rather than an
  altered value); BL-1330's suite 12/12 (unaffected regex-proximity
  collateral fix).
- `swarmforge/scripts/test/test_bl1328_qwen_model_token_forms.sh` (run
  via `zsh`, its own shebang — `bash` would misinterpret zsh-specific
  array syntax): ALL PASS, 11/11, including the equals-form-with-empty-
  value and bare-`--model`-no-value edge cases.
- Property tests (`bl1324ClaudeSeatQwenCloudContextWindowInvariants`,
  `bl1328QwenModelTokenFormsInvariants`): 6/6. BL-1324's own invariant 1
  now checks attribution (no commit whose subject doesn't name BL-1324
  touches the hotfix path) rather than byte-identity — confirmed this
  is the correct fix for the contradiction the ticket's own notes
  describe (a byte-pin that forbids the very follow-up its own human
  ruling authorized), not a weakening: the check still fails if an
  unrelated commit silently edits the pinned region.
- **Non-vacuity, independently re-verified, not trusted from evidence**:
  hand-mutated the equals-form `if` in `swarmforge.sh` to `if false;
  then` — the shell test failed exactly as expected (equals form no
  longer detected) at the very first equals-form assertion. Restored
  (`git diff --stat` empty), re-ran ALL PASS.
- Constraint check: no change to the precedence branches themselves
  (only comments); no other change to `extra_cli_targets_qwen_cloud` or
  `role_uses_openrouter` beyond the two authorized items; BL-1324's
  certification/ledger decision untouched.
- Retirement audited against BL-1006 ("retire, never reword"): the
  Examples row was retired with a rationale comment and successor
  pointer, the Scenario Outline and its other three rows are untouched,
  the decoy generator's `--model=${qwen}` entry was correctly dropped
  (no longer a decoy) — confirmed by reading the property test's decoy
  list directly, not from the narrative.
- `retires:` field present on the ticket YAML, satisfying the pre-handoff
  task-scope gate for touching BL-1324-owned paths (BL-1276).
- Compile (`extension`, `npm run compile`): clean.
- Full unit suite (`npm test`): 15 files / 25 tests red — identical
  standing-debt population (includes the three known standing guards:
  `tempDirTrapGuard`, `socketFixtureShortRootGuard`, `liveRepoDerivationGuard`).
  None touch this parcel's files.
- Property suite (`npm run test:properties`), run sequentially after the
  unit suite: 24 files / 13 tests red, exactly matching the standing-debt
  population. No BL-1328 or BL-1324 file present in the failing set.
- Mutation manifest currency: the hardener's evidence records BL-113
  Gherkin mutation re-run on BOTH BL-1328's own feature (6/6 killed) and
  BL-1324's feature, whose stale `acceptance-mutation-manifest` hash the
  retirement invalidated — regenerated (soft mode caught the mismatch
  and re-tested for real; 6/6 killed, matching the one retired row) per
  the engineering rule that a manifest is never hand-edited.
- Orphaned test processes: none of mine after the runs.

## Verdict

APPROVE. No open item. Landing this QA merge commit on `main`.

By QA.
