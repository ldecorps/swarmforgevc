# BL-1398 architect review — clean sweep, forwarding

## Reviewed
Merged cleaner's `d17eb9e25b` (clean fast-forward, no conflicts). Read the
ticket's two invariants, coder/cleaner evidence, and the full body of
`extension/test/helpers/commitGuardFixtureSet.js`.

## Dependency gate (hard gate, BL-259)
```
cd extension && node out/tools/dependency-gate.js ../specs/pipeline/steps/bl1398GuardFixtureDerivesItsSetSteps.js
```
No forbidden edges.

## Logical coupling (BL-255)
`co-change-report.js` against the changed files: only frequency-1 hits,
below the default threshold (3). Nothing flagged.

## Invariants — verified in the actual code
1. **Guard set is a function of `run_commit_guards.sh`/the two hooks, read
   at test time.** `deriveCommitGuardFixtureSet()` parses `run_guard <script>
   [args]` lines directly from the runner and hook files at call time — no
   hand-written list survives (confirmed: `EXEC_FIXTURE_FILES` and its
   eleven `path.join` constants are gone from
   `bl632CommitTimeGuardInvariants.property.test.js`).
2. **A guard the runner names but the tree lacks fails loud, naming it.**
   `deriveCommitGuardFixtureSet` throws (never `continue`/skips) when a
   parsed guard's script file is absent from the tree — correctly
   distinguished from a *sourced lib* behind an `[[ -f ]]` test, which is
   reported via `missingLibs` rather than thrown, since the guard that
   wanted it still runs and reports the absence itself. That distinction is
   the right one: a missing guard silently narrows the chain, a missing
   optional lib does not.

One implementation note beyond the two invariants: the ticket's "How"
section describes following `source`/`.` lines "one level"; the actual
`pending` walk in the helper is a full transitive BFS (bounded to
`swarmforge/scripts`, guarded by a `seen` set against cycles/repeats) rather
than a single hop. That is strictly safer than what was asked (a
multi-level lib chain is still resolved correctly) and introduces no
correctness risk given the visited-set bound — not a defect.

## Independent re-verification
- `test_bl1398_guard_fixture_derives_set.sh` run 2× standalone: **8/8 PASS**
  both times, covering every `qa_e2e_procedure` item (guard added/removed on
  a seam runner copy, a named-but-absent guard refusing by name, the real
  runner's full guard set including `check_handler_module_graph.sh`, and no
  hand-written list surviving).
- Property tests: `bl1398GuardFixtureDerivedSet.property.test.js` (2/2) and
  `bl632CommitTimeGuardInvariants.property.test.js` (2/2, **previously 1
  failed on main** — confirmed fixed).
- Acceptance (`BL-1398-...feature`): **4/4 pass**.

## required_wiring
Both anchors read live: the helper's `run_guard` literal only exists because
the fixture now reads the runner's own line shape, and the step handler file
exists under the discovery-suffix convention (BL-1371).

## Surfaced item — routed, not dropped
Coder's evidence names a genuine sibling defect explicitly carved out of
this ticket's scope ("Explicitly NOT in scope: Other fixtures that still
enumerate closures by hand; name them in the coder's evidence if met, mint
separately"): `specs/pipeline/steps/bl632CommitTimeGuardSteps.js` — the
acceptance-side twin of the fixture fixed here — still hand-enumerates the
same stale guard list. Independently reproduced:
`run_acceptance.sh specs/features/BL-632-commit-time-guard-refuses-pipeline-code-on-main.feature`
is **4 pass / 7 fail** on this tree, the seven failures being "should be
permitted" scenarios the fixture's stale chain wrongly refuses (exit 127 on
the missing `check_handler_module_graph.sh`). Grepped
`backlog/active|paused|hold` for `bl632CommitTimeGuardSteps` and for
BL-632-stale-fixture language: genuinely untracked, not a re-mint. Sending a
`note` (priority 00) to specifier and coordinator alongside this forward so
it gets minted rather than sitting only in evidence.

## Verdict
No architecture violation, no invariant violation, no correctness defect in
this ticket's own scope. Forwarding to hardener.
