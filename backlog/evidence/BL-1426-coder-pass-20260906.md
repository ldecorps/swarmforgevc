# BL-1426 — coder pass, 2026-09-06

## What was implemented

Retired the never-parsed manual post-QA sweep wrapper, per the ticket's
`retires:` declaration:

- Deleted `swarmforge/scripts/post_qa_branch_sweep.bb` (unreadable since
  its birth commit f5b6b49f1f, one unclosed paren in `role-facts`) and its
  shell shim `swarmforge/scripts/post_qa_branch_sweep.sh`.
- `docs/how-to/BL-668-post-qa-deterministic-branch-sweep.md`: removed the
  "Manual invocation" fenced block and the Modules table's "CLI wrapper"
  row; added one sentence recording the retirement (2026-09-06, the actual
  date of this pass — the ticket's own description text said 2026-09-05,
  its mint date, but the retirement itself happens now).
- `docs/reference/Specification.MD` (~line 4325, a historical "Prior
  entry" changelog block per BL-668): dropped `post_qa_branch_sweep.bb`
  from the Modules list, with a one-clause note on the retirement — per
  the ticket's own explicit direction, even though the surrounding block
  is dated history rather than a live description.
- `docs/index.md`: confirmed no change needed (per the ticket).
- Diagram registry: confirmed `swarm-flow.mmd` depicts the daemon's own
  sweep, never the wrapper (per the ticket's own analysis) — no diagram
  trigger fires, nothing to update.
- New acceptance step handler:
  `specs/pipeline/steps/bl1426PostQaSweepWrapperRetiredSteps.js`. The
  feature file (pre-minted, not modified) drives three scenarios: the
  wrapper/shim are gone; every `.bb` directly under `swarmforge/scripts`
  reads to its end via a genuine READER pass (never a load - built a small
  inline Babashka reader script that reads every top-level form with
  `LineNumberingPushbackReader` and evaluates nothing, mirroring
  `bb_load_analyse_driver.bb`'s shebang-stripping convention but
  deliberately never calling `eval`, since BL-1427's executed-entry-call
  concern is out of this ticket's scope); the how-to has no FENCED command
  invoking the sweep by hand (prose mentions, including the retirement
  sentence itself, are fine — checked by extracting only ` ``` ` blocks).

## BL-654 invariant — stated-reason carve-out, no property test

The one declared invariant ("this parcel does not modify `handoffd.bb`,
`post_qa_branch_sweep_lib.bb` or `post_qa_branch_sweep_lib_test_runner.bb`")
quantifies over WHICH FILES THIS PARCEL'S OWN DIFF TOUCHES — a process/diff-
shape constraint, not a pure module's behavior. No executable encoding
applies (per BL-654's carve-out for invariants that admit none); verified
directly instead: `git status --short` at commit time names only
`docs/how-to/BL-668-...md`, `docs/reference/Specification.MD`, the two
deleted scripts, and the new step handler — none of the three named files.

## Checks run

- Acceptance (`specs/pipeline/scripts/run_acceptance.sh` on this ticket's
  feature): 3/3.
- BL-668's own feature, unchanged: 5/5, still green.
- `bb swarmforge/scripts/test/post_qa_branch_sweep_lib_test_runner.bb`:
  `ALL PASS: post_qa_branch_sweep_lib.bb`.
- `git ls-files swarmforge/scripts | grep post_qa_branch_sweep`: three
  survivors — `post_qa_branch_sweep_lib.bb` (scripts/),
  `post_qa_branch_sweep_cli.bb` and `post_qa_branch_sweep_lib_test_runner.bb`
  (scripts/test/). The ticket's qa_e2e_procedure step 2 states the COUNT
  as 2 while naming these same three files as "the survivors" — a minor
  inconsistency in the ticket's own text (`git ls-files swarmforge/scripts`
  recurses into `scripts/test/` too, so three real, legitimate survivors
  genuinely count as 3, not 2). Recorded here rather than silently
  "corrected" either way; the actual tree state matches the ticket's own
  named survivor list exactly.
- Reader pass (scenario 02, live tree): 0 unreadable `.bb` files directly
  under `swarmforge/scripts` — satisfies qa_e2e_procedure step 5 (the
  post-land check) already, since the wrapper that was the sole offender
  is now gone.
- No `.bb` file under `swarmforge/scripts` modified by this parcel (only
  the two deletions).

## Out of scope, confirmed untouched

- `handoffd.bb`, `post_qa_branch_sweep_lib.bb`,
  `post_qa_branch_sweep_lib_test_runner.bb` (invariant 1, BL-1421's files).
- The load guard's drained loop / executed-entry-call fixes (BL-1427,
  already landed, unrelated to this ticket).
- `docs/how-to/BL-1252-commit-guard-chain-reports-every-violation.md`:
  references `post_qa_branch_sweep.bb` three times, all as PAST-TENSE
  incident narrative (BL-1427's own drained-loop investigation story,
  "was never even reached", "would have fetched... had it parsed at all").
  Confirmed this is history, not a living description of current behavior,
  and left untouched — not named in this ticket's scope.

By coder.
