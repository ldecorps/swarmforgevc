# BL-1639 — hardender review pass, 2026-09-19

2 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bash swarmforge/scripts/test/test_babysitterd_census_lib.sh (baseline, then hand-mutated babysitterd_census_pids removing the pid != self guard, then restored)
- **Commit hash**: dfb127f3b4
- **First error excerpt**: mutant survived all 7 existing cases and all 4 BL-1639 acceptance scenarios - none plants a ps-snapshot line for the calling processs own $$
- **Failure class**: test-coverage-gap
- **Expected vs observed**: the calling processs own pid is never named by the census, even when it matches babysitterd.sh
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: added case 04b constructing a snapshot line for the test own $$; commit dfb127f3b4; confirmed it now kills the hand-applied mutant

## D2

- **Failing command**: npx vitest run test/stepHandlerTmpRootGuard.test.js (BL-1636 standing guard)
- **Commit hash**: dfb127f3b4
- **First error excerpt**: new offender(s) not in the census: [bl1639BabysitterdCensusSteps.js] - the handler created four separate mkdtemp roots cleaned up only by terminal Then steps, so a thrown assertion anywhere before those steps leaks all of them
- **Failure class**: resource-leak
- **Expected vs observed**: a step handlers mkdtemp roots are registered with the shared fixtureReaper primitives (BL-1636 standing guard)
- **Blamed role**: hardener (own stage, closed same pass)
- **Remediation pointer**: registered an onAbnormalExit(() => cleanupFixture(ctx)) callback in the Background step; commit dfb127f3b4; guard now green, no /tmp leak, acceptance 4/4

By hardender.
