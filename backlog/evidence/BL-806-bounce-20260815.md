# BL-806 QA bounce — 20260815

Commit reviewed: 6de7bb005f (Merge commit '2498e50334' into swarmforge-QA,
by QA), carrying documenter's b542cfb2e ("BL-765: document Bubble's
bridge-served capability flags and hold-music catalog"). `git merge-base
--is-ancestor 41b19d4479 6de7bb005f` confirms BL-806's own architect merge
is an ancestor of the commit cited here.

This is a multi-ticket batch parcel (Article 2.6): the same documenter
commit also carries BL-765 (fully documented, its own gates verified
clean — see this parcel's approval note) and BL-514 (re-verified via
architect pass 3 / hardener pass 3, no BL-514 files changed). BL-806 is
the only ticket in this batch with a failing check of its own.

## Review inventory (Article 4.4 — run to completion before bouncing)

- **Coder/architect/hardener work — PASS:**
  - `swarmforge/scripts/review_forward_evidence_gate_lib.bb` wired live
    into `swarmforge/scripts/swarm_handoff.bb`'s validate/send `cond->`
    chain (confirmed by grep: `review-forward-evidence-block?` at
    `swarm_handoff.bb:304-336`), matching `required_wiring`'s only entry —
    not only present in the lib.
  - `bash swarmforge/scripts/test/review_forward_evidence_gate_lib_test_runner.bb`
    (my own run): ALL PASS.
  - `bash swarmforge/scripts/test/review_forward_evidence_gate_lib_property_runner.bb`
    (my own run, both declared invariants, 1000 runs + non-vacuity
    companion per architect's description): ALL PASS.
  - Acceptance (`specs/pipeline/scripts/run_acceptance.sh
    specs/features/BL-806-review-forward-evidence-gate.feature`, my own
    run): 9/9 scenarios green (Scenario Outline's 4 role examples + 5
    singles), matching architect/hardener's own counts.
  - Regression check on the directly-modified live script:
    `test_swarm_handoff_sync_deliver.sh` and
    `test_swarm_handoff_daemon_backup.sh` (my own runs): ALL PASS, no
    collateral break from the new gate.
  - Gherkin acceptance mutation manifest embedded in the feature file
    (hardener's pass): 4/4 mutants killed on the Scenario Outline, 0
    survivors — durable verdict, not re-run by QA (mutation is the
    hardener's exclusive gate per this role's "Does Not Own").
  - `npm test` (full unit suite, my own run): 1 failure
    (`test/renderBriefingDiagramsCli.test.js`), file untouched by this
    batch's diff (confirmed: `git diff 1ba16651d1..2498e50334 --stat --
    extension/test/renderBriefingDiagramsCli.test.js` empty). Re-run of
    just that file in isolation: 4/4 PASS in 21s (vs. 61.8s inside the
    full parallel run against host load 39-68 this pass) — BLOCKED BY host
    load, not a regression from this batch. Recorded, not silently
    omitted.
  - `npm run test:properties` (my own run): 88/88 files, 272/272 tests
    PASS, 0 failures (5 `[vitest-worker]: Timeout calling "onTaskUpdate"`
    RPC-layer errors, the same host-load worker-communication noise as the
    unit run above, not test failures). No TypeScript
    `*.property.test.js` file in this batch's diff for BL-806 (its own
    coverage is the babashka property runner above), so this is a general
    regression check, not BL-806's own required gate.

**BL-806's own implementation is correct and gate-clean everywhere I could
get a verdict.** The blocking defect below is a missing deliverable, not a
code defect.

## D1 — behavior: documenter's pass for this parcel documents BL-765 only; BL-806's own required deliverable never happened

BL-806 changes live, user-facing swarm protocol behavior: `swarm_handoff.bb`
now refuses a forward-direction `git_handoff` from a review role that names
exactly the commit it received for the same task. This is exactly the class
of change `swarmforge/handoff-protocol.md` exists to document — its two
sibling gates in the same `cond->` chain, the BL-760 duplicate-chain guard
and the BL-531 QA-edge durability gate, each have their own dedicated
section there (`## Duplicate-Chain Guard (BL-760)` at line 495, `## QA-Edge
Durability Gate (BL-531, BL-761)` at line 598). BL-806's gate has no section
at all (`grep -n -i "review.forward.evidence\|BL-806\|same.commit refus"
swarmforge/handoff-protocol.md` returns nothing).

The only documenter commit in this batch (`b542cfb2e`, "BL-765: document
Bubble's bridge-served capability flags and hold-music catalog") touches
`docs/how-to/BL-765-*.md`, `docs/index.md`, and
`docs/reference/Specification.MD` — all BL-765-scoped, per its own commit
message and diff. `git log --oneline -- docs/how-to/BL-806* ` and
`git log --oneline -- swarmforge/handoff-protocol.md` in the range since
hardener's forward (`ea75bacc7`, "Forwarding to documenter") both return
empty: no documenter commit touched BL-806 in any way.

Per constitution Article 4.4's routing table: "a stage's pass is MISSING
ENTIRELY — its handoff forwarded another stage's commit with zero changes
of its own where its deliverable was due... THAT stage. The absence of a
stage's output is that stage's own defect." Documented protocol currency
for a live behavioral change to the handoff send-path is documenter's
deliverable per Article 1.7 and the constitution's own description of
`handoff-protocol.md` as "the exhaustive reference" that "must not
diverge" — silently skipping it while documenting a sibling ticket in the
same batch is the same shape as BL-425/BL-576 (which named this exact file
as the recurring miss).

**Expected:** `swarmforge/handoff-protocol.md` gains a section for the
review-forward-evidence gate (same shape as its BL-760/BL-531 siblings:
what it refuses, the fail-open conditions, the reroute_reason/bounce
exemptions), landing in the same parcel that shipped the gate, per this
project's Diagrams-adjacent doc-currency rule (documenter keeps
protocol-relevant docs current in the same parcel that changes what they
describe).
**Observed:** no documenter commit touches `handoff-protocol.md` or any
BL-806-specific doc; only BL-765 was documented in this batch's single
documenter commit.

**Remediation:** documenter adds a `## Review-Forward Evidence Gate
(BL-806)` section to `swarmforge/handoff-protocol.md` (or equivalent
placement consistent with the file's existing structure), covering: what
`review_forward_evidence_gate_lib.bb::blocked?` refuses (a review role's
forward-direction git_handoff naming exactly its received commit for the
same task), the fail-open conditions (no received parcel for the task,
unreadable box, no mailbox), and the two exemptions (`reroute_reason`
marked detours, backward-direction bounces). Then re-forward BL-806's
untouched code down to QA — no rework needed in `review_forward_evidence_gate_lib.bb`
or `swarm_handoff.bb` themselves.

Blamed role: documenter (owns the missing protocol-doc deliverable; the
code, architecture review, and hardening for BL-806 are all independently
verified correct above).
