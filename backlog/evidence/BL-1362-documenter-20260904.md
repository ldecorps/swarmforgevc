# BL-1362 — documenter pass (2026-09-04)

## Received
`80f8096981` (hardener; per `backlog/evidence/BL-1362-hardener-pass-20260904.md`).

## Doc-domain review
Checked for an existing doc home before writing anything new: this
ticket's sibling tool, `record-bounce.js` (the bounce half of Article
4.4/BL-635's evidence discipline), has no dedicated how-to or reference
page either — only Specification.MD entries and passing mentions from
other pages (`docs/how-to/BL-1208-...md`, `docs/reference/BL-819-...md`).
Followed the same precedent: a full Specification.MD entry describing the
tool's CLI shape, refusal behavior, and the two invariants, no new
how-to page manufactured for an internal role-tooling CLI with no
operator-facing workflow.
- `docs/reference/Specification.MD`: new top-of-stack entry.
- `docs/index.md`: no line to add — no how-to page, matching
  `record-bounce.js`'s own precedent (not indexed there either).
- Diagram registry checked — no diagram depicts evidence-recording
  internals.

## Note for future documenter passes
This tool exists for MY role's own convention too (every review pass'
evidence file, including this one) — but my role prompt has not yet been
amended to call it, so this pass's own evidence file is still hand-written
per the existing convention. Wiring role prompts to the new tool is a
separate, specifier-owned follow-up (same shape as BL-1360's own
`ceremony_handoff.sh` — built, not yet the standard route).

## Verdict
Doc content added (Specification.MD entry only, matching sibling-tool
precedent). Forwarding to QA.
