# BL-1418 documenter review pass — 2026-09-05

Reviewed the hardened commit (`ebc698b9a8`) and the coder/cleaner/architect/
hardener trail for BL-1418 (the Art Director seat is addressable).

Checklist:
- `required_wiring` anchors present:
  `swarmforge/packs/full-forge.conf::window art-director` (the standing
  pack window line, per the human's ruling) and
  `extension/src/concierge/topicIcon.ts::art-director` (the icon map
  entry, `🔮`). Step handler exists:
  `specs/pipeline/steps/bl1418ArtDirectorSeatSteps.js`.
- Read `roleTopicMapStore.ts`, `topicIcon.ts`, `model_factory_lib.bb`, and
  the new `extension/test/bl1418RoleEnumerationClassification.test.js`:
  confirmed invariant 1 (every SWARM-ROLES enumeration names
  `art-director`; every CHAIN list — `PIPELINE_CHAIN`, `PIPELINE_ORDER`,
  `KNOWN_PRODUCING_ROLES`, `required_stages_lib.bb`,
  `routing_manifest_lib.bb` — does not, and a standing test now pins the
  classification for the next role added). Confirmed invariant 3 (the
  `🔮` icon collides with no other role's in `ROLE_TOPIC_ICON`).
- Confirmed the human ruling (standing pane, option 1) matches the
  landed pack line exactly — no on-demand/rotation-config shape, no
  presentation-stage chain wiring added (out of scope, per the ticket's
  own direction).
- Per the ticket's own explicit direction ("the documenter adds the role
  to the roles page(s) under docs/ and the how-to for launching the
  pack"): searched `docs/` broadly for a canonical roles-enumeration page
  or pack-launch how-to (checked `docs/how-to/BL-1320-...md` — a
  different mechanism, adding a `@seat` to an EXISTING stage, not a new
  role; `docs/reference/BL-643-non-pipeline-agents-reference-table.md` —
  explicitly excludes Article 1 pipeline roles like the coordinator/
  specifier/Art Director by its own stated scope; `swarmforge/PIPELINE.md`
  and the constitution articles — specifier-owned, already amended at
  mint per the ticket's own notes). No single such page exists, so
  authored a new dedicated how-to,
  `docs/how-to/BL-1418-the-art-director-seat-is-addressable.md` (seat
  shape, pack/worktree/mailbox/topic wiring, the audited role-enumeration
  classification table, and a live-verify section), linked from
  `docs/index.md` in the same commit, plus a new dated
  `docs/reference/Specification.MD` top entry (this doc's own running
  history of every shipped ticket) with a how-to cross-link.
- No "Last Updated" freshness field elsewhere touched beyond the
  Specification.MD top-of-file bump made in the same commit as the
  content change.
- No registered diagram trigger fires for this ticket (a new role's
  wiring across enumeration lists, not a change to the extension host/
  webview boundary, the tmux substrate relationship, the `.swarmforge/`
  state layout, or the pipeline topology/backlog flow that swarm-flow.mmd
  depicts) — no diagram change needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found; new how-to authored per the ticket's own
explicit documenter-note direction, since no existing page fit.
