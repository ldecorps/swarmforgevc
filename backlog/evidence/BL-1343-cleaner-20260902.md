# BL-1343 — cleaner pass (2026-09-02)

NONE — no defect found. Forwarded to architect unchanged.

## Reviewed

Diff scope (ab608471a8..8916d5cef8, the coder's actual BL-1343 work):

- `swarmforge/scripts/land_step_lib.bb::own-paths` — the fix itself. A single
  loop gains an `excluded` accumulator threaded alongside `acc`, and the
  empty-set branch distinguishes "nothing delivered" from "everything
  subtracted" per the ticket's invariant 2. Small, cohesive, well-commented
  at the one non-obvious point (why the same empty face means two different
  things). No duplication, no structural issue — one function, one job.
- `specs/pipeline/steps/bl1343ReplayDropsTheTicketsOwnPathSteps.js` — new step
  handler. Drives the real `land_step_lib.bb` against per-scenario git
  fixtures rather than reimplementing the decision in JS; BL-971 stale-fixture
  sweep present; BL-425 scoped-pattern registration. No changes needed.
- `specs/pipeline/steps/index.js` — one `require` line added (registration).
- `extension/test/bl1343ReplayNeverDropsOwnPathInvariants.property.test.js` —
  two declared invariants, generator reach asserted by construction.
- `swarmforge/scripts/test/land_step_lib_test_runner.bb` — new bb test cases.

Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred); gated by
its own unit suite only, which is green.

## Verification run (not assumed)

- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` → ALL PASS.
- `bash specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1343-replay-drops-the-tickets-own-path.feature` → 6/6 pass.
- `npx vitest run --config vitest.properties.config.mjs bl1343ReplayNeverDropsOwnPathInvariants` → 2/2 pass.
- `grep bl1343 specs/pipeline/steps/index.js` → handler registered (required_wiring pin satisfied).

## Merge note

This batch item also merged QA's BL-1317 approval broadcast and the
architect's BL-1317 non-forwarding re-pass copy (both completed separately,
no forwarding, per protocol) before this coder handoff arrived — the merge
of 8916d5cef8 hit three trivial whitespace/list-addition conflicts
(`backlog/active/BL-1317-...yaml`, `specs/pipeline/steps/index.js`,
`swarmforge/scripts/test/handoff_lib_test_runner.bb`), all resolved by
keeping both sides' real content with no functional change.
