# BL-1293 — architect review, 2026-08-30

**Verdict: CLEAN PASS. Forwarding to hardener.**

## Reviewed

Commits `1b3200264c` (BL-1293: judge a review forward by what it contributed,
not by its commit id) and `224a08cab1` (rewrap two docstring lines from the
self-audit), received from coder.

## Two-layer / IO boundary

No extension host/webview code touched. This parcel is entirely
swarm-machinery (`.bb` gate libs, `swarm_handoff.bb` wiring, and a
`specs/pipeline/steps` acceptance step handler). No violation surface for
Article 1's webview/host-IO/no-browser-storage/no-direct-spawn rules.

## Dependency gate (BL-259, hard gate)

None of the changed files are under `extension/src` — per-parcel invocation
errors on paths outside its scope (expected: those args must exist under
`extension/`). Ran full-repo mode instead from `extension/`:

```
Dependency-rule gate PASSED: no forbidden edges.
```

## Co-change / logical coupling (BL-255)

Ran `co-change-report.js` against all 5 changed non-doc files. Top suspected
couplings are exactly the sibling files this ticket's own description says
must move together (`pre_qa_gate_gather_lib.bb`, `swarm_handoff.bb`, the two
new test runners, `specs/pipeline/steps/index.js`, and the BL-806/BL-950
sibling step files) — expected, not a new coupling. The long tail of
unrelated files (`.cursor/rules/*`, `android/**`) at flat low counts is
sweep-commit noise, not logical coupling to this change.

## required_wiring (both anchors)

- `review_forward_evidence_gate_lib.bb::merge-introduces-nothing-unique` —
  present: `forward-introduces-nothing-own?` calls
  `pre-qa-gate-gather-lib/merge-introduces-nothing-unique?` directly (line
  107), no second definition.
- `specs/pipeline/steps/index.js::bl1293` — present, registered at line 550.

## Invariants Review (BL-654/633)

Declared invariant: "A review role's forward carries content that role
itself authored... A commit that introduces nothing of its own over its
parents is never a pass." Encoded non-vacuously in
`review_forward_evidence_gate_lib_property_runner.bb` — coder's evidence
shows the property at 90 failures against the reverted (identity-only)
`blocked?`, and I re-ran it green against the fixed gate myself (see
Verification below). The by-construction generator's commit id is a
descendant (never the received hash), so the refusal can only be explained
by the contribution fact, not by BL-806's old identity check. No other site
in the diff needed a separate sweep — `blocked?` is the single decision
point and both callers (`review_forward_evidence_gate_lib.bb`,
`swarm_handoff.bb`) route through it.

## Correctness read

`blocked?` stays pure; the merge-parent-count guard
(`>= (count commit-parents) 2`) is applied before calling the primitive,
matching the sibling `no-dropped-work?` caller — the regression that broke
BL-806/BL-950's own suites in the coder's first cut is fixed and pinned by a
new regression test. Fail-open behavior preserved throughout (nil/false/
unreadable commit never blocks).

The coder's own evidence documents a genuine open gap: `qa_e2e_procedure`
step 4 (`b7d22b9ee` vs `3bc1e28b53`) cannot be satisfied because that real
commit resolved an actual conflict and so is not "introduces nothing of its
own" — correctly routed as a `note` to the specifier rather than closed by
silence or worked around with a second contribution heuristic (which
`approval_context` explicitly forbids). This is a spec-gap disposition, not
an architect send-back.

## Verification (re-run, not eyeballed)

| Check | Result |
|---|---|
| `review_forward_evidence_gate_lib_test_runner.bb` | ALL PASS |
| `review_forward_evidence_gate_lib_property_runner.bb` | ALL PASS |
| `pre_qa_gate_lib_test_runner.bb` (sibling consumer) | ALL PASS |
| `pre_qa_gate_gather_lib_acceptance_contract_test_runner.bb` | ALL PASS |
| `pre_qa_gate_gather_lib_acceptance_pointer_test_runner.bb` | ALL PASS |
| BL-1293 acceptance (`specs/pipeline/cli.js`) | 6/6 |
| `extension/out/tools/dependency-gate.js` (full-repo) | PASSED, 0 forbidden edges |

## Property Testing (undeclared-property pass)

No other touched module is pure/property-shaped beyond what the declared
invariant above already covers — `swarm_handoff.bb`'s new wiring is a thin
call-site, and the step handler drives the real CLI rather than
reimplementing decision logic. Nothing to add here.

By architect.
