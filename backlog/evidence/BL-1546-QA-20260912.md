# BL-1546 — QA review pass, 2026-09-12

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

- **Compile**: `npm run compile` clean.
- **Unit suite**: `npm test` — 616 files, 10445 tests, all pass. No orphaned
  test/mutation processes before or after.
- **Property suite**: `npm run test:properties` — 4 files / 5 tests failed,
  all pre-existing and owned, none touching BL-1546's own new test:
  - `bl1089FrontDeskLivenessFixture.property.test.js` → BL-1502 (open, paused)
  - `bl1313BatchGuardVisibilityInvariants.property.test.js` (2 tests) → BL-1503 (open, paused)
  - `bl1367ApprovalCarriesItsRuling.property.test.js` → BL-1527 (open, paused)
  - `bl1297MergeOwnPathsInvariants.property.test.js` invariant 3 → same root
    cause as BL-1464 (open, paused): the fixture authors merged content
    under a foreign, non-closed ticket id (`BL-9999-other`), which BL-1389
    (landed 2026-09-04) now excludes — unrelated to BL-1546's new
    closed-on-main clause (`BL-9999-other` is not closed on origin/main, so
    BL-1546's clause never fires for this fixture). No standing-reds.tsv row
    names this exact file yet; noted to the specifier as a fold-in candidate
    under BL-1464, not blocking (an open owning ticket already exists).
  - `bl1546ClosedOwnerNeverSilentlyExcludesInvariants.property.test.js` (this
    ticket's own new property test): 2/2 pass.
- **bb suite**: `bb swarmforge/scripts/test/land_step_lib_test_runner.bb` →
  `ALL PASS: land_step_lib.bb`.
- **Acceptance (own feature)**: `node specs/pipeline/cli.js
  specs/features/BL-1546-a-path-owned-only-by-a-closed-ticket-is-never-silently-excluded.feature`
  → 4/4 scenarios pass.
- **Regression features** (qa_e2e_procedure list) — BL-1315, BL-1343,
  BL-1374, BL-1389, BL-1472, BL-1473, BL-1481, BL-1544: all green.
- **Wiring**: `land_step_lib.bb`'s new `closed-on-main?` + `cond` clause is
  threaded into `own-paths` immediately ahead of the BL-1389 exclusion
  clause (confirmed by reading the diff, not inference); the step handler
  `specs/pipeline/steps/bl1546ClosedOwnerNeverSilentlyExcludesSteps.js`
  drives the real `land_step_cli.bb`/`land_step_lib.bb`, discovered by file
  per BL-1371 (no index.js edit).
- **Docs**: `Specification.MD` dated entry added; how-to
  `docs/how-to/BL-1241-...md` gains the "closed ticket" section beside
  BL-1544's; documenter's own note confirms no diagram change is due (below
  every registered diagram's depicted granularity) — correct, this is a
  land-step decision-logic change, not a mechanism any of the four
  registered diagrams depicts.
- **Ancestry**: verified every BL-1546 pipeline commit (coder ebe5cf69b8/
  d3abc9f92e, cleaner c7bde7171e, architect 461b7ed903, hardener
  80333fcb23, documenter 5234de9224/58b2531f45/e8437108b2) is an ancestor of
  the commit being approved (this merge), not merely of HEAD.
  `qa-sibling-check.js status --ticket BL-1546` → `VERIFY BL-1546` (no open
  deferral). No prior bounce for BL-1546 on `main`/`origin/main` (in sync,
  0/0 ahead).

By QA.
