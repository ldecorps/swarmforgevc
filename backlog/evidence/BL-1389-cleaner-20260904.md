# BL-1389 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## Pre-existing standing red surfaced, not this ticket's fault

`land_step_lib_test_runner.bb` reports 2 failures ("BL-1375: the real tree
guard refuses an unregistered handler on a non-main tree" and its
follow-on assertion). Confirmed this predates BL-1389's own commits (the
diff for `06f1babcaf`/`886edf554b` does not touch this test block; ran it
against `HEAD^`, the commit immediately before this merge, with the same
result). This is `backlog/paused/BL-1388-the-land-step-runners-tree-guard-fixture-asserts-the-retired-registry.yaml`
— an already-minted, already-ticketed defect: the fixture asserts the
pre-BL-1371 hand-maintained-registry model (an empty `DOMAINS` array
should leave a handler unregistered), but BL-1371 replaced registration
with directory discovery, so a file's mere presence now registers it and
the fixture's expected refusal never fires. Not swept, not fixed here —
recorded so the next reader does not re-diagnose it.

## What was checked for BL-1389 itself

- `land_step_lib_test_runner.bb` — re-ran: every BL-1389-specific row
  (6 assertions) passes; only the pre-existing BL-1388 rows fail.
- `bl1389UnlandedSiblingPathNeverRidesInvariants.property.test.js` —
  re-ran directly (`vitest --config vitest.properties.config.mjs`): 3/3
  properties hold, including invariant 3 (the report is enough to check
  the verdict without diffing the tip — the exact gap that let BL-1367
  ride into BL-1386's replay unnoticed).
- Two touched sibling property tests re-ran for regression:
  `bl1308SiblingDetectorCoversReplay.property.test.js` (2/2) and
  `bl1252CommitGuardAggregationInvariants.property.test.js` (5/5, the
  latter now also covering `check_handler_module_graph.sh` in its guard
  list, correctly kept in sync with BL-1385's landing).
- `bl1272LandedSiblingSteps.js`'s one-line change (widened regex for the
  new `LANDED_SIBLING <id> <path>` line shape) preserves BL-1272's own
  distinction ("the id being on a LANDED_SIBLING line at all") — read the
  diff directly, and confirmed the file still loads clean.
- `required_wiring` — `EXCLUDED_SIBLING_PATH` confirmed printed by
  `land_step_cli.bb` (line 119), matching the anchor.
- Architecture: `landed-sibling-verdicts` is the one shared computation
  behind both `landed-siblings` (kept, now a thin filter over it — no
  duplicated logic) and `entangled-siblings`'s new `:landed-paths`
  reporting; `delivered-attribution` and `sibling-path-landed-fn` are
  each computed once and injected into `own-paths`/`land-plan` via
  `opts`, avoiding a second read of the same diffs. No IO duplicated, no
  logic re-implemented at a second call site.
- `mutation-site-count.js` on `bl1389UnlandedSiblingPathNeverRidesSteps.js`:
  187 sites (`over` 100). Reviewed and declined to split, same reasoning
  as this area's other tickets: one cohesive single-feature step handler.
- `jscpd` over the new JS handler and the touched `.bb` files: 0 clones.
- TypeScript compiles clean; the handler discovers via BL-1371's registry.
- Babashka has no mutation/CRAP/DRY wired (Startup Tools, BL-472
  deferred); gated by the unit runner, property tests, and acceptance
  feature instead, per the documented degraded fallback.

No bounce history on BL-1389 (first pass, `main`/`origin/main` diverge by
32/2 for reasons unrelated to this ticket). Forwarding unchanged to
architect.

By cleaner.
