# BL-1717 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Rebuild pass, after QA's bounce (backlog/evidence/BL-1717-QA-20260925.md,
commit e81a640797, 3 defects: D1/D2 coder, D3 documenter). Reviewed the
coder's rebuild (`6ebe8176d3`, D1 own-paths fix + D2 property test) against
QA's own remediation pointers and probes.

- Re-ran QA's own D1 probe script byte-for-byte (the exact clojure pasted
  into the bounce evidence) against the fixed `land_step_lib.bb`: scenario
  B ("landed L + U") now reads `{:paths ["BL-9003-own.txt"]}`, matching
  control A and scenario C exactly — the pre-fix bug (shared.md kept, U's
  lines riding unreported) is gone.
- `bb swarmforge/scripts/test/land_step_lib_test_runner.bb`: ALL PASS.
- `npx vitest run --config vitest.properties.config.mjs test/bl1717LandedCoOwnerNeverShieldsInvariants.property.test.js`:
  1/1 passed. Read the test: drives the real `own-paths` with a real git
  fixture and `:path-landed-fn` injected (never a JS restatement), four
  named cells by construction covering exactly the dimensions QA's D2
  remediation named (path-landed/not, in/out of the ticket-level
  unlanded-siblings set, landing-ticket-owns-the-path or not) — satisfies
  the D2 invariant-unencoded finding.
- `node out/tools/dependency-gate.js` / `co-change-report.js` on the new
  property test: PASSED, no forbidden edges; coupling only with this
  parcel's own sibling files.
- Acceptance, one run each: `BL-1717` 2/2, `BL-1389` 5/5, `BL-1375` 7/7,
  `BL-1481` 4/4, `BL-1678` 4/4 — the neighbouring exclusion/passenger rules
  are unchanged, matching qa_e2e_procedure step 2.

D3 (documenter, `docs/how-to/BL-1241-...md`'s "own-paths excludes a
delivered path only when every owner attributed to it is an unlanded
sibling — but a path with THREE or more owners could have a landed one
among them" wording) is UNCHANGED in this rebuild — confirmed by reading
the live doc file, still the pre-fix wording QA flagged. Per Article 4.4's
multi-stage-blame rule this item is not mine to fix; it travels forward in
the inventory to the documenter, who owns it. Not a new architect finding;
recorded here so it is not silently dropped a second time.

By architect.
