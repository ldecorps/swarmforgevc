# BL-1288 — QA pass, 2026-08-30

## Disposition check (BL-532)

`node extension/out/tools/qa-sibling-check.js status --ticket BL-1288` →
`VERIFY BL-1288` (exit 0). No open deferral.

## Verified before this pass

- required_wiring anchor: `specs/pipeline/steps/index.js:837` registers
  `require('./bl1288PushFailureClassificationSteps')`. Confirmed by direct
  grep on the merged tree.
- Ancestry: `git merge-base --is-ancestor d3adfe6bec a71af8ba58` → true. The
  hardener's own BL-1288 merge is an ancestor of the cited commit — this is
  actually BL-1288's own work, not a sibling's (BL-336 discipline).
- Architect pass (`BL-1288-architect-review-20260830.md`) and hardener pass
  (`BL-1288-hardener-pass-20260830.md`) both re-verified independently:
  clean, both declared invariants encoded non-vacuously in
  `extension/test/bl1288OnlyRejectionDiscardsInvariants.property.test.js`,
  one real mutation survivor found and closed by the hardener
  (`push-rejection?`'s outer `and`→`or`), 6/6 Gherkin mutants killed.

## Acceptance (own gate, BL-112)

`specs/pipeline/scripts/run_acceptance.sh
specs/features/BL-1288-only-a-rejected-push-authorises-discarding-local-commits.feature`
→ **5/5 green**: the 3-example Scenario Outline (rejection→discarded,
unreachable→kept, no-credentials→kept), the error-text-survives scenario, and
the BL-1198-unchanged successful-push scenario.

## Full unit suite (`npm test` in `extension/`)

16 files / 26 tests failed, 565 files / 9776 tests passed. Cross-checked
every failing file against BL-1288's own changed paths (coder/cleaner/
architect/hardener/documenter diff, `bfbbc4eeb0..a71af8ba58`) — zero overlap,
and grepped `backlog/` for each before writing this down (BL-1063):

- `deps.checkOrphanedAuthoredDocs is not a function` (crossFileDuplicationCheck,
  multiBranchParserCoverageCheck, perHatRolePromptEvidenceCheck,
  pilotAcceptanceGate, pilotScopedCrapCheck, shellEntryPointDriveCheck,
  unreachableStepHandlerCheck) → BL-1221 (paused).
- `tempDirTrapGuard` (21 non-BL-1288 `.bb`/shell runners) → BL-1289 (paused).
- `constitutionDocCitations` (`docs/deprecated/` citations in constitution
  articles, none of them BL-1288's) → pre-existing, epic `code-quality-gates`.
- `liveRepoDerivationGuard` (bl1243PaneActivitySignal, deprecateRetiredReferents,
  docsStructureRealTree) → BL-1291 (paused).
- `socketFixtureShortRootGuard` (bl1112StandingUnitRedsSteps,
  bl691AmbulanceWorkflowGapsSteps — neither is BL-1288's step file) → BL-1290
  (paused).
- `operatorRuntimeBbFixtureClosure`, `pinnedRepoFixture` (unrelated `.bb`
  closure/fixture checks — context_telemetry_store.bb, role_ask_escalation_lib.bb,
  rotation_telemetry_lib.bb, self_heal_telemetry_lib.bb, swarm_handoff.bb) —
  pre-existing, no BL-1288 file named.
- `backendSwitch`, `telegramClient`, `telegramCursorOperatorExec` (model-id
  string, Telegram poll shape, BL-698 ambulance message) — pre-existing,
  unrelated to git reconcile logic.

Same standing-debt shape this QA seat has corroborated repeatedly
(`QA-standing-red-corroboration-20260828.md`, `BL-1272-land-escalate-20260830.md`).
Not blocking this parcel.

## Property suite (`npm run test:properties`)

266 files / 817 tests passed, 25 files / 14 tests failed. **BL-1288's own
property file: 3/3 passed**
(`test/bl1288OnlyRejectionDiscardsInvariants.property.test.js`), including
the non-vacuity check against a reconstructed pre-BL-1288 mutant (the mutant
destroys work in ≥50 of the sampled cases; the real code destroys none
outside a genuine rejection).

Grepped every other failing file/message for `master_main_reconcile`,
`push-rejection`, `rematch-with-push-first`, `push-unavailable` — zero
matches. The 25 failures are the same `checkOrphanedAuthoredDocs`/collection-
gap/repo-hygiene classes as the unit lane (BL-1221/BL-1289/BL-1290/BL-1291),
plus several already-evidenced stamp-off/outage-failover/pilot-gate reds with
no BL-1288 file among them. Not blocking.

Orphan check: `pgrep -fl 'node --test|stryker'` clean before and after both
lanes. One `npm run test:properties` invocation hit the tool's own 2-minute
timeout mid-run; reaped the leaked vitest process group by pgid
(`kill -9 -- -<pgid>`) before re-running in the background to completion —
noted here per the Verification Order orphan-reaping rule, not a defect in
BL-1288.

## Entangled tip — BL-1241 remedy

The documenter's cited commit `a71af8ba58` carries two things that are not
BL-1288's own work as ancestors/co-changes: BL-1299 (`active`, still
in-flight — its yaml, topic json, and an intake archive file), and one BL-1295
confirmation-note file (BL-1295's substantive work already landed as a
passenger of BL-1297's own landing — `git merge-base --is-ancestor
459b985776 origin/main` → true; only this one audit note, recorded on the QA
branch, hadn't reached `origin/main` yet). Also present: an unticketed
operator throughput directive (`active_backlog_max_depth` in
`backlog/STEERING.md` / `swarmforge/packs/full-forge.conf`) that had, by the
time of this pass, already landed on `origin/main` through a separate route
(`origin/main`'s own `full-forge.conf` already reads `2`) — pure drift, not
entanglement.

`bb swarmforge/scripts/land_step_cli.bb BL-1288-only-a-rejected-push-authorises-discarding-local-commits
a71af8ba58` had already been run successfully earlier this session (before
this pass resumed from `in_process`), producing:

```
LAND_REPLAY land-replay/BL-1288-a71af8ba58 16570bc52b
ENTANGLED_SIBLING BL-1299
LANDED_SIBLING BL-1295
```

(reconstructed from the resulting branch/commit and independent verification
below — the original stdout was not captured before the session was
interrupted). A same-args re-run this pass hit the known BL-1298
linked-worktree friction twice: first "could not create worktree" without an
explicit repo-root (fixed by passing one), then the SAME failure again
because `git worktree add -b land-replay/BL-1288-a71af8ba58` refuses an
already-existing branch — the exact leftover-branch shape recorded in
`BL-1272-land-escalate-20260830.md`'s "two smaller issues", not a new defect.
Per QA.prompt: do not hand-roll the replay and do not loop the CLI mid-gate.
Independently verified the EXISTING replay instead (`qa-tip-pure-rebuild-
strands-commits-on-all-worktree-branches` memory note's "verify rather than
assume"):

- `git log -1 --format='%H %P' 16570bc52b` → parent is `32957a4c86`, which is
  exactly current `origin/main` (fresh, not stale).
- `git diff a71af8ba58 16570bc52b -- <every BL-1288 file: master_main_reconcile_lib.bb,
  the feature file, the step/CLI files, the how-to doc, the two .bb test
  runners>` → **empty**. BL-1288's own content is byte-identical between the
  cited commit and the replay.
- `git show 16570bc52b:specs/pipeline/steps/index.js | grep bl1288` → present.
  required_wiring anchor survives the rebuild.
- The only files differing (`git diff --name-only a71af8ba58 16570bc52b`):
  `backlog/STEERING.md`, `backlog/active/BL-1299-*.yaml`,
  `backlog/archive/INTAKE-operator-question-*.md`,
  `backlog/evidence/BL-1295-qa-confirm-landed-passenger-20260830.md`,
  `backlog/topics/BL-1299.json`, `swarmforge/packs/full-forge.conf` — all six
  accounted for above, none of them BL-1288's.

## Landed

BL-1144 discipline, run from a scratch worktree checked out at
`land-replay/BL-1288-a71af8ba58` (so `HEAD` matched the commit being
evaluated/pushed — the normal QA-worktree `HEAD` is `a71af8ba58`, not the
tip-pure replay): acquired `.swarmforge/land-main.publish.lock`,
`land_main_publish.sh --decide-only` → `:tip-contains-origin true,
:purity-action :push, :origin-advanced-since-gate false` (the
`:rematch-once-at-edge` `:next` is the self-held-lock artifact noted in
`BL-1297-qa-pass-20260830.md`, not real contention). `git push origin
16570bc52b:main` → `32957a4c86..16570bc52b main -> main`, fast-forward.
Released the lock, removed the scratch worktree, deleted the now-fulfilled
`land-replay/BL-1288-a71af8ba58` branch.

**Landed commit: `16570bc52b`, not the originally-cited `a71af8ba58`.**
`abandoned_commits: [a71af8ba58]` recorded on the ticket YAML.

BL-1288 carries no `GH-` id — no `issue_done.sh` step required.

## Merge-up broadcast

Sent to coder, cleaner, architect, hardender, documenter: merge up to
`a71af8ba58` (the normal pipeline-chain QA-approved commit — the tip-pure
`16570bc52b` is a `main`-only landing artifact built off `origin/main`
directly and is not part of any worktree role's own branch lineage).
Coordinator notified for bookkeeping, naming both the pipeline commit and
the landed commit.

## Verdict: APPROVE

By QA.
