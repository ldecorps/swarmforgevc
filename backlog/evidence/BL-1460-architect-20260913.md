# BL-1460 — architect review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By architect.

## Detail

- Merged cleaner a442773434 (evidence-only, no code) into architect at
  9d0dbec423. Reviewed commit 732504cf64 (coder: seed `lastSnapshot` on
  `/events` connect).
- Two-layer boundary, host-owns-I/O, no webview storage, no secrets in
  target dir, no direct process spawn bypassing tmux: unaffected — the
  diff is a single-closure-variable seed inside an existing route.
- `node out/tools/dependency-gate.js src/bridge/bridgeServer.ts
  test/bl1460IdleEventsOneSnapshotInvariants.property.test.js
  test/bridgeServer.test.js` (from `extension/`): PASSED, no forbidden
  edges. Full-repo scan (no args): PASSED.
- `node out/tools/co-change-report.js` on the same files: bridgeServer.ts
  is a pre-existing hub file with broad co-change fan-out (specs step
  registry, telegram front-desk, console UIs, docs) — informational only,
  none of those files are touched by this parcel; no send-back.
- Invariants review (declared, BL-633/654): invariant 1 has a new
  non-vacuous property test
  (`bl1460IdleEventsOneSnapshotInvariants.property.test.js`) — verified it
  fails (structural + behavioural, counterexample `[[true,true]]`) when the
  `lastSnapshot = snapshot` seed line is removed, restored clean after.
  Invariant 2 is correctly left uncovered here: it restates BL-1350's
  invariant 2, already authored and property-tested in
  `bl1350KeepaliveInvariants.property.test.js`, and this parcel's diff
  touches no path that could un-author it.
  `grep -n "'/events'" src/bridge/bridgeServer.ts` confirms exactly one
  connect site and one `sseClients.add` — no second site needed the same
  seed.
- Property-testing pass (undeclared coverage, BL-654 scope): only pure
  module touched is the seed line already covered above; nothing further
  to add.
- Ran `npx vitest run test/bridgeServer.test.js`: 102/102 green. Ran the
  target scenario
  (`-t "periodic keepalive comment frame"`) 20/20 green (pre-fix baseline
  was 3-6 failures per QA/specifier evidence). `npm run test:properties --
  test/bl1460IdleEventsOneSnapshotInvariants.property.test.js`: 2/2 green.
- Acceptance: `node specs/pipeline/cli.js
  specs/features/BL-1460-an-idle-events-connection-gets-one-snapshot.feature`:
  4/4 scenarios pass (TAP), including scenario 04's register-row-removed
  check.
- `grep -n "bridgeServer" backlog/standing-reds.tsv`: no row — scenario 04's
  claim confirmed directly, not just via the acceptance step.
- BL-753 (unreachable step handler): all four step-handler patterns in
  `bl1460IdleEventsOneSnapshotSteps.js` matched a live step in the 4/4
  acceptance run above — none dead.
- No diagram change-trigger fired (architecture.mmd's trigger is the
  host/webview boundary or `.swarmforge/` state layout; this is an
  internal single-variable fix, not a mechanism change).

By architect.
