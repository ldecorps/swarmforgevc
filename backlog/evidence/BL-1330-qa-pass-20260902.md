# BL-1330 — QA pass, 2026-09-02

Reviewed commit `c67660bfb2` (documenter, clean sweep NONE), merged as
`<this merge commit>`. Lineage: coder -> cleaner -> architect -> hardener
`cd55b875ff` -> documenter `c67660bfb2`, all present as ancestors.

## Ticket nature
BL-848 hotfix stamp-off for landed commit `441fd35112` (bob pack restaffed:
Anthropic claude-sonnet-5 on six pipeline seats, coder alone on Token Plan
qwen3.8-max). Review-only — confirms or refutes the landed hotfix, never
reimplements, never writes the ledger's certify/waive decision.

## Independent verification
- `git show --stat 441fd35112`: only
  `swarmforge/packs/bob-multi-provider-mono-router.conf` plus the BL-1322
  stamp-off ticket/feature files changed — no `swarmforge/scripts/` or
  `extension/` path touched. Confirms scenario 05/11's "no script/lib code
  changed" claim directly.
- `grep -n 'window\|--model' swarmforge/packs/bob-multi-provider-mono-router.conf`:
  coder -> `--model qwen3.8-max --effort high`; specifier/architect/QA ->
  `--model claude-sonnet-5 --effort high`; cleaner/hardender/documenter ->
  `--model claude-sonnet-5 --effort medium`. Matches the ticket's invariant
  exactly — one Qwen seat, six Anthropic seats, efforts preserved.
- `backlog/hotfix-ledger.yaml`: row for `441fd35112` reads `state:
  stamp-open`, `stamp_ticket: BL-1330` — linked as required, no
  `certified`/`waived` write present (constraint honored).
- Acceptance: `node specs/pipeline/cli.js
  specs/features/BL-1330-swarm-stamp-bob-anthropic-starting-cast-441fd35112.feature`
  — 12/12 pass.
- `npm test` (extension unit lane): 570/585 files, 9844/9869 pass. Same 25
  pre-existing failures as BL-1334's pass this session (pilotAcceptanceGate
  `checkOrphanedAuthoredDocs`, tempDirTrapGuard, socketFixtureShortRootGuard,
  etc.) — none intersect this parcel's diff
  (`specs/pipeline/steps/bl1330SwarmStampBobAnthropicStartingCastSteps.js`,
  `specs/pipeline/steps/index.js`,
  `specs/pipeline/steps/lib/bl1330QwenRemapPredicateCli.zsh`). Standing red,
  not new.
- No orphaned `node --test`/`stryker`/`vitest` processes before or after.

## Specifier amendment check
The ticket's own `specifier_amendment_2026_09_02` note (scenario 02 fixed
from diff-text assertion to behavior assertion after a coder spec-gap) was
already folded into the merged commit's acceptance file — verified the
scenario now asserts `coordinator_agent`/`coordinator_model`/
`coordinator_effort` unchanged rather than "no coordinator line touched".

## Verdict
APPROVED. Confirms the hotfix as landed; certify/waive stays a human ledger
decision per BL-848, untouched here.

By QA.
