# BL-1515 — coder rework, 2026-09-11 (hardener bounce D1 fix)

Fixes hardener bounce D1 (`backlog/evidence/BL-1515-hardender-bounce-20260911.md`):
`enforce-branch-identity-guard!` had no master-resident exemption, unlike its
BL-1195 sibling guard, so it could rename the shared `main` branch out from
under `specifier`/`coordinator`.

## Fix
- `swarmforge/scripts/ready_for_next.bb`: `enforce-branch-identity-guard!`
  now skips when `(:worktree-name role-info)` is `"master"`, mirroring
  `enforce-worktree-drift-guard!`'s existing exemption exactly.
- Added acceptance scenario 06 to the feature file, constructing two
  roles.tsv rows (`specifier`, `coordinator`) with `worktree-name: master`
  at one shared path and different sessions, checked out on neither
  declared branch. Asserts no `BRANCH_DRIFT` line is printed and no ref is
  renamed.

## Checklist run
- `bb swarmforge/scripts/test/branch_identity_guard_lib_test_runner.bb`: ALL PASS.
- `bb swarmforge/scripts/test/branch_identity_guard_lib_property_runner.bb`: ALL PROPERTIES HOLD (300 runs).
- `node specs/pipeline/cli.js specs/features/BL-1515-a-role-checkout-on-the-wrong-branch-is-caught-before-the-turn.feature`: 6/6 scenarios pass (new scenario 06 included).
- `git diff --stat` scope: only the guard fix + its new acceptance coverage.

By coder.
