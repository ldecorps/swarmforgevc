# BL-1465 — coder pass

Commit: this parcel · 2026-09-07 · worktree `swarmforge-coder`

## The fix

`extension/test/bl1375ApprovedSiblingsCanLandInvariants.property.test.js`
invariant 2: dropped the property's own `replay!` call entirely (since
BL-1447, `land-plan` already builds and guards the tip-pure commit before
ever returning). Now reads the SAME decision point BL-1447 put the guard
at:

- **Dangling** corner: asserts `plan.action === 'escalate'`, the reason
  names both the passenger and `check_feature_handler_registration.sh`,
  and `plan.unlanded` names the passenger.
- **Resolved** corner: asserts `plan.action === 'replay'`, the passenger
  is in `plan.passengers`, then independently re-runs
  `check_feature_handler_registration.sh --assume-main` against the
  built tip (`plan.branch`, checked out and dropped in the fixture repo
  itself — `replay!`'s own scratch worktree is already gone by the time
  `land-plan` returns) — never trusting land-plan's own internal claim
  alone, matching the file's stated ethos ("never a JavaScript
  restatement of either decision").

No assertion weakened: both reach counters (`dangling`/`resolved`) and
both floor checks are unchanged; only the plan/build/guard SEQUENCING the
property reads from changed.

## The interim allowlist/register rows removed in this same land

Per the ticket's own "How" and invariant 2 ("the register row leaves in
the same commit that discharges the ledger row" — same shape here):
removed `test/bl1375ApprovedSiblingsCanLandInvariants.property.test.js`'s
row from both `swarmforge/scripts/property_suite_standing_allowlist.tsv`
(now header-only) and `backlog/standing-reds.tsv`.

## Verification

| check | result |
|---|---|
| `npx vitest run --config vitest.properties.config.mjs test/bl1375...` | 3/3, three consecutive runs |
| Reverting to the pre-fix property test file | genuinely fails, `Expected "replay", Received "escalate"` — confirms non-vacuous |
| `run_acceptance.sh` BL-1465 feature | **4/4** |
| Scenario 3 (file green alone), reverted to pre-fix test file | fails identically — non-vacuous |
| Scenario 4 (rows removed), rows restored | fails identically — non-vacuous |
| grep for the file in both tsv files, post-fix | no match in either |

## Out of scope, left for the documenter (per this ticket's own "How")

One sentence in BL-1375's/BL-1447's how-to, if either describes the
plan/build sequencing, noting the passenger property now reads it from
the plan. No diagram trigger fires.
