# BL-1580 coder evidence — 2026-09-15

## What changed

- `extension/test/bl1295RevertAttributionInvariants.property.test.js`:
  invariant 2's uniform `fc.boolean()` draw over 6 runs is replaced with an
  outer loop over the two arms `[false, true]` (clean, genuinely foreign),
  one `fc.assert` per arm at `numRuns: runsPerCell(6, 2)` (3 draws each,
  `test/helpers/reachFloors.js`) — the same shape BL-1578 used for its
  `bl622` collision arm. Inside each arm the per-draw body is unchanged:
  it still builds the with-revert and without-revert repositories and
  compares the two gate verdicts. An arm that stopped being produced now
  fails the file naming that arm via `assertReachFloor(counts, ['clean',
  'genuinelyForeign'], REVERT_CELL_RUNS, 'arm')`, kept beside the original
  `seen.clean > 0` / `seen.genuinelyForeign > 0` assertions (scenario 03,
  byte-identical text). The run prints
  `BL-1580 reach map (bl1295): {"arms":2,"minDrawsPerArm":3}`, matching the
  file's own fixed arm count and per-arm budget. Invariant 1 in the same
  file is untouched (`git diff main -- <file>` confirms the diff stops at
  invariant 2). The draw budget of 6 is unchanged; only how it is spent
  changes.
- `specs/pipeline/steps/bl1580RevertAttributionArmsConstructedSteps.js`
  (new): step handlers for the specifier-authored feature
  (`specs/features/BL-1580-*.feature`, not touched by this parcel).
  Scenarios 01/02 drive the real property file under the real properties
  config (`spawnSync` from `extension/`, memoized per scenario), reading
  its own printed reach map. Scenario 03 reads the real test source and
  confirms the two floor assertions are still present. The Background
  step's regex is anchored to the file's exact literal text (not a bare
  `(.+)` capture) so it cannot shadow the "runs alone under the properties
  config" step, which also starts with "the property file " — a first
  attempt without the anchor made every scenario fail with "unknown
  property file value: runs alone under the properties config". Discovered
  automatically by `specs/pipeline/steps/index.js` (BL-1371), no manual
  registration needed.

`extension/src` is unchanged (confirmed via `git diff main --name-only --
src`, empty).

## Verification

- Property file alone (`npx vitest run --config vitest.properties.config.mjs
  test/bl1295RevertAttributionInvariants.property.test.js`): 3/3 pass,
  printing `BL-1580 reach map (bl1295): {"arms":2,"minDrawsPerArm":3}`.
  A further 15 standalone runs (QA e2e procedure step 1): 15/15 green, 0
  FAIL, reach map printed every time.
- Full acceptance run (`specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1580-*.feature`): 5/5 scenarios pass (01 green run, 02
  both Examples rows — 2 arms, 3 draws of the rarest arm — 03 both
  Examples rows).
- Break-then-restore: dropped the `true` arm from `REVERT_ARMS` — first run
  failed `reach floor: arm genuinelyForeign drawn 0 < 6` (the
  `assertReachFloor` check); restored from a clean copy, re-ran, green
  again.
- `swarmforge/scripts/check_feature_handler_registration.sh`: clean, exit 0.
- Full extension unit suite (`npx vitest run`): 620/620 files, 10513/10513
  tests passed.

## Register row

`backlog/standing-reds.tsv` line naming this ticket is left in place — per
BL-1572's precedent, QA removes it in the land that turns the file green,
not the coder.

By coder.
