# BL-1700 — QA review pass, 2026-09-26

3 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: bash -c 'bb swarmforge/scripts/model_steward_cli.bb probe standin-model --stand-in hang --scenario 01-one-line-fix' (and again with only --wall-clock-seconds 8)
- **Commit hash**: a547104675
- **First error excerpt**: Type: java.lang.NullPointerException / Location: swarmforge/scripts/model_steward_coder_probe_lib.bb:327:53 (no cap flags: (* wall-clock-seconds 1000)); with only --wall-clock-seconds 8: NPE at 326:33 ((min max-ticks ...)). Both flags given: runs normally (8s, wall-clock cap).
- **Failure class**: behavior
- **Expected vs observed**: a probe with a cap flag omitted runs on the documented defaults (fix-turns 3, max-ticks 400, wall-clock 900), including the how-to's own Run command / observed: NPE before driving whenever --wall-clock-seconds or --max-ticks is omitted; run-probe assoc's only the flags given, but probe! passes :fix-turns-limit/:max-ticks/:wall-clock-seconds explicitly into run-fixture!, so the keys are present with nil and its :or defaults never apply. The coder evidence's real-model command line (--scenario 01-one-line-fix --wall-clock-seconds 150 --evidence-dir backlog/evidence) also NPEs on this code, so that scorecard was not produced by the code in the parcel
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/model_steward_coder_probe_lib.bb probe!/run-fixture! (never pass nil caps; default in one place); specs/pipeline/steps/bl1700StewardCoderProbeSteps.js runProbe always injects --wall-clock-seconds/--max-ticks, so add a check that runs with no cap flag; re-take the one-line real-model evidence run on the fixed code

## D2

- **Failing command**: bash -c 'bb swarmforge/scripts/model_steward_cli.bb probe standin-model --stand-in hang --scenario 01-one-line-fix --wall-clock-seconds 900 --max-ticks 400'
- **Commit hash**: a547104675
- **First error excerpt**: {"fixtureId":"01-one-line-fix","outcome":"turn cap","handedOff":false,"wallSeconds":22.595} - elapsed 23s with the documented defaults given explicitly
- **Failure class**: behavior
- **Expected vs observed**: a seat that is still working is stopped by the wall-clock cap (900s default) and a turn cap counts model turns / observed: --max-ticks counts driver poll ticks (drive-tick! + 50 ms sleep), effective-max-ticks = min(max-ticks, wall/50ms + 5) = 400, so a busy seat is cut off after ~20s of polling on an idle CPU and scored 'turn cap' with no turn counted; the documented 900s wall clock never binds. On the GPU host (free CPU, fast ticks) any model needing more than ~20s per parcel after the launch wait scores 'turn cap' and fails the bar
- **Blamed role**: coder
- **Remediation pointer**: model_steward_coder_probe_lib.bb run-fixture! effective-max-ticks and outcome-of's 'turn cap' label; make the turn cap count model turns (the ticket's 'turn cap') and let the wall clock bound elapsed time; docs/how-to/BL-1700-model-steward-coder-probe.md Caps section follows the code

## D3

- **Failing command**: read model_steward_coder_probe_lib.bb run-fixture! (279-347), probe! (355-386), aider-launch-args (192-205) against ticket item 4; scorecard JSON from the D2 run
- **Commit hash**: a547104675
- **First error excerpt**: scorecard keys: fixtureId, outcome, handedOff, wallSeconds, commit. aider-launch-args writes --llm-history-file .probe-llm.log inside the throwaway root; run-fixture!'s finally does (fs/delete-tree root). Summary file written only (when evidence-dir ...). Committed evidence: '# local coder probe: qwen3-14b:latest / handed off 0 of 1 - verdict fail / - 01-one-line-fix: wall-clock cap (151.876s)'
- **Failure class**: behavior
- **Expected vs observed**: item 4: a JSON scorecard per run with the model id as resolved from the endpoint, scenario, outcome, turns, wall time and the llm-history path, and a summary written under backlog/evidence/ as local-coder-probe-<model>-<UTC>.md; qa_e2e step 3 needs the evidence to name the resolved model id / observed: no resolved model id (endpoint-answers? checks only the HTTP status; the id is the CLI argument), no turns, no llm-history path, and the llm history is deleted with the throwaway repo so no run's model transcript survives; the summary is written only with --evidence-dir (the how-to documents it as optional); the committed evidence names only the CLI argument
- **Blamed role**: coder
- **Remediation pointer**: model_steward_coder_probe_lib.bb endpoint check (read the id from <endpoint>/models), run-fixture! scorecard fields and an llm-history path that outlives the run root, probe! summary default dir backlog/evidence; re-taken evidence names the resolved id

By QA.

## Detail

Parcel: documenter 1338adac75, merged into QA as a547104675 (the merge brought
BL-1700's 34 paths only; checked against both parents, nothing dropped).

Gates, one run each on a547104675 (qa-gather):
- straggler check before/after: none.
- `qa-sibling-check.js status --ticket BL-1700`: `VERIFY BL-1700`.
- standing-red register: 5 rows, all owned, `unowned: []`.
- `pre_qa_gate.sh BL-1700 a547104675`: OK (required_wiring read).
- `npm test`: exit 0.
- `npm run test:properties`: exit 0; only the allowlisted BL-871
  `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors.
- `run_acceptance.sh` on the BL-1700 feature: 7 of 7. The handler's `runProbe`
  always passes `--wall-clock-seconds 30 --max-ticks 300` unless a scenario
  overrides them, which is why D1 is invisible to the suite.

Ticket qa_e2e:
1. Feature 7 of 7 (above).
2. Harness selftest (no model): all five fixtures `:red-ok true :green-ok true`.
3. Real-model evidence: RUN, falls short (D1: not producible by the parcel's
   code; D3: no resolved model id).
4. `bb swarmforge/scripts/model_steward_cli.bb probe` with no model: usage,
   exit 1. OK.
5. Own-path scope: every BL-1700 path is inside the ticket's Scope (steward
   CLI, probe lib, fixtures, step handler, feature, how-to, docs/index.md,
   evidence). The QA branch also carries unlanded siblings; they are not
   this parcel's.

Wiring: `model_steward_cli.bb:606` dispatches `"probe" (run-probe rest-args)`;
`bl1700StewardCoderProbeSteps.js` exports `registerSteps`. Both anchors hold.

Article 4.5 changed paths (`model_steward_cli.bb`,
`model_steward_coder_probe_lib.bb`):
- `bb swarmforge/scripts/test/model_steward_test_runner.bb`: ALL PASS.
- `bash swarmforge/scripts/test/test_model_steward_cli.sh`: 5 PASS, 1 FAIL,
  `FAIL: 05: certify exited 1 with a planted scorecard: certify refused: safety
  competencies absent from scorecard: coder-stop_banner_compliance
  (scorecard=scorecards/bl547test__smoke-model.json)`. The identical failure
  reproduces on an origin/main export, so it predates this parcel (certify,
  not probe). `grep -rl test_model_steward_cli backlog/` names no open ticket
  and the register has no row: UNOWNED. The likely cause is the 2026-09-23
  hotfix that added the `coder-stop_banner_compliance` competency, which paused
  BL-1707 stamps without naming this test. Sent as an `unowned-red` note.
- The acceptance feature drives the real CLI and lib end to end (the BL-1697
  precedent for a driver lib/CLI).

Further stand-in runs (hang/never, `--scenario 01-one-line-fix`):
- no cap flags: NPE 327:53. `--wall-clock-seconds 8` only: NPE 326:33.
- `--wall-clock-seconds 8 --max-ticks 400`: wall-clock cap at 8.05s.
- `never`, `--wall-clock-seconds 60 --max-ticks 400`, no `--fix-turns-limit`:
  "no model commit" at 4.9s (the fix-turns default applies there).
- `hang`, `--wall-clock-seconds 900 --max-ticks 400`: "turn cap" at 22.6s (D2).
- Every run left no `probe.tmux.sock` or `stand-in.sh` process behind.

Observation, not a D-item (the hardener raised it too): a `--scenario`-scoped
run reports "N of <scoped count>" with the five-fixture bar, so a scoped run
that hands off every fixture still reads `verdict fail` and exits 1. The
ticket defines the verdict only for the five-fixture summary.

Revert: D1 and D2 are defects in code the parcel added, so the review merge
a547104675 is reverted (`git revert -m 1`) on the QA branch after this
evidence commit. The bounce `git_handoff` names the evidence commit, which is
before the revert, so the coder's merge does not strip its own work. When the
rebuilt parcel returns, revert that revert first, then merge and check per
file (a merge over a reverted merge drops content silently).
