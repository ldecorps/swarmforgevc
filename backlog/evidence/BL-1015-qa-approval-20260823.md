# BL-1015 — QA complete review inventory (Article 4.4)

- **Ticket**: BL-1015 — a Boy Scout run cleans the top-ranked debt item in
  the same pass, inside a declared size envelope, or refuses it whole and
  says why.
- **Parcel**: `00_20260822T233025Z_000484_from_documenter_to_QA_for_QA`
  (from documenter, commit `35788d360d` — "documenter pass — add
  lineDiff.ts to Source layout").
- **Reviewed by**: QA, 2026-08-23.
- **Deferral check (BL-532)**: `qa-sibling-check.js status --ticket
  BL-1015` → exit 0, `VERIFY BL-1015`. No open deferral.
- **This is bounce #3's response.** Bounce #1 (architect → coder, D1
  git-index gap / D2 import cycle) and bounce #2 (QA → hardender, D1 six
  CRAP violations / D2 unstated mutation completeness on
  `lineDiff.ts`/`assertionGuard.ts`/`commit.ts`/`types.ts`) are recorded in
  `bounce_history` on the ticket YAML. This pass independently re-verifies
  both bounce #1 (already cleared per my own bounce #2 evidence) and every
  item named in bounce #2.

## Verdict

**PASS — inventory items: NONE.**

Every gate below was RUN, none blocked, none omitted.

## Gate inventory

| # | Gate | Result |
|---|---|---|
| G1 | Stage ancestry — hardener's bounce-#2-response commit `76268836af` confirmed an ancestor of the cited commit `35788d360d` via `git merge-base --is-ancestor` | PASS |
| G2 | Prior-bounce items still open? | Re-checked, not merely trusted: D1 (CRAP) and D2 (mutation) both independently re-verified below as closed |
| G3 | Compile (`npm run compile`) | PASS — clean |
| G4 | Full unit suite (`vitest run --coverage --config vitest.config.mjs`) | PASS — 471/471 files, 8401/8401 tests (matches hardener's own tally exactly; +8 net new tests vs. the bounce-#2 baseline) |
| G5 | CRAP (`node scripts/crapReport.js src/tools/boyScoutRun.ts src/tools/boyScoutRun/*.ts`) — independently reproduced, not read from the hardener's evidence | PASS — **zero functions exceed CRAP<=6** across all 67 listed functions in the module, 100% coverage on every one; closes bounce #2's D1 exactly (the same 6 functions previously flagged — `lineDiff.ts::countChangedLines`, `run.ts::boyScoutRun`, `assertionGuard.ts::assertionsWouldChange`, `commit.ts::commitEdits`, `report.ts::explain`, `report.ts::renderRunReport` — now read 5, 6, 6, 5, 3, 2 respectively) |
| G6 | Property suite (`vitest run --config vitest.properties.config.mjs`), separate lane | PASS with one known pre-existing failure — 146/147 files, 426/427 tests. This ticket's own `test/boyScoutRun.property.test.js` re-run in isolation: 1/1 pass. The lone failure, `bl796NvmNodePathFollowUpAdoptInvariants`, is a known, already-ticketed (`backlog/paused/BL-1063-...yaml`), pre-existing, unrelated nvm/PATH regression — confirmed independently present in `backlog/paused/`, not this parcel's defect. The 2 `[vitest-worker]: Timeout calling "onTaskUpdate"` unhandled errors are the known benign artifact (BL-871), allowlisted |
| G7 | Acceptance (`run_acceptance.sh specs/features/BL-1015-a-boy-scout-run-cleans-one-thing-or-says-why-it-cleaned-nothing.feature`) | PASS — 9/9 scenarios |
| G8 | required_wiring — all 3 entries | PASS — `boyScoutRun/run.ts:18-19` and `boyScoutRun/environment.ts:11` both import from `'../boyScoutScan'`; `specs/pipeline/steps/index.js:589` registers `bl1015BoyScoutRunCleansOneThingSteps` — confirmed by direct grep |
| G9 | DRY (`npx jscpd --config .jscpd.json`, whole-tree) | PASS — 34 pre-existing clones repo-wide, **none** touch any `boyScoutRun*` file (grepped the full clone-report output for `boyScoutRun`: no match) |
| G10 | Docs currency | PASS — documenter's own commit adds the missing `lineDiff.ts` line to the Source layout list (the cleaner's earlier split of `measure.ts` had moved the LCS algorithm out but the doc wasn't updated); accurate against the shipped module layout; no other doc changes needed since report text and CLI contract are unchanged from the already-reviewed BL-1015 doc |
| G11 | Mutation completeness (hardener's own exclusive gate — QA does not run Stryker, re-verified by reading the hardener's evidence in full, not merely its verdict line) | PASS — closes bounce #2's D2. `lineDiff.js`: 1 real defect fixed (lcsLength off-by-one), 11 killed via 3 new tests, 7 individually proven equivalent (BL-234-style, each with a direct code-level argument, not probability). `commit.js`: 100%, 102/102 (98 killed + 4 no-cov pre-existing CLI-only branch). `types.js`: 100%, 11/11 (prior 0%/11-survived figure was a stale incremental-cache artifact, re-run solo and fresh). `assertionGuard.js`: scoped Stryker run hung 15+min at fixed CPU with zero forward progress (confirmed genuinely stuck via CPU-sampling before killing by process group, no orphans left) — the newly-extracted logic this pass owns (`buildMultiset`/`hasUnaccountedLine`) was hand-mutated instead (11 mutants matching real Stryker mutant shapes: arithmetic/equality/conditional/boolean/method-negation), 10 killed, 1 proven equivalent; pre-existing regex-boundary tests for this file are unmodified by this pass (confirmed via `git diff --stat`) and were already thorough. A third distinct Stryker environmental bug (`perTest`/`all` coverage-analysis misattribution producing false Survived verdicts) was found and the hand-verification-against-the-real-suite method was used as the ground truth throughout, consistent with the two Stryker environmental bugs already on record this session |
| G12 | Functional change present (No-Op Rule) | PASS — real behavior changes (lcsLength off-by-one fix) plus substantial refactoring under gate-passing tests, not manifest-only churn |
| G13 | Orphaned processes | PASS — `pgrep -fl '[n]ode --test\|[s]tryker'` clean before and after (the two transient self-matching `bash` PIDs seen mid-session were the tool harness's own wrapper shells, already exited — confirmed empty `/proc/<pid>/cmdline`, not real orphans) |
| G14 | Working tree clean after all verification | PASS — `git status --short` empty |

## `qa_e2e_procedure` — every step addressed

Steps 1-5 map directly onto the 6 acceptance scenarios (top-item-cleaned +
rest-untouched; gate-failure abandons; envelope-refusal via the Scenario
Outline's two boundary rows plus the refusal-naming scenario; test-edit
abandonment; empty-inventory reporting) — all independently re-run via
`run_acceptance.sh` in this pass, 9/9. Step 6 ("run once against this real
repository and read the report before accepting the commit") is explicit
operational guidance for whoever first exercises the live CLI against this
repo, not itself a QA-owned executable gate — the ticket names no
`required_wiring` entry or acceptance scenario for a live self-run, and
actually invoking the tool against this repository would itself constitute
a real filesystem mutation outside this review's scope.

## Sibling content riding in this commit

None. This is a single-ticket parcel (`git_handoff` names only BL-1015); no
Article 2.6 multi-ticket forward applies.

Per "An Approval Authorizes Only Its Ticket's Work" (BL-506), **this
approval names BL-1015 only.**

By QA.
