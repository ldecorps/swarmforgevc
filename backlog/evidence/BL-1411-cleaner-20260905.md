# BL-1411 — cleaner pass, 2026-09-05

NONE — no defect found. The coder's fix is correct, complete, and clean.

## What was checked

- Reviewed `contract_freshness_gate_lib.bb`: `decide-for-ref`'s six-way
  fail-open decision table (invariant 3), `gather-and-decide-for-ref`'s
  impure fact-gathering split cleanly from the pure decision, and
  `findings-for-git-handoff`'s single reader of the acceptance path via
  `task-scope-gate-lib/declared-acceptance-path` (invariant 2, verified
  live by grep: `read-yaml-field\|acceptance:` → 0, `declared-acceptance-path`
  → 2).
- Reviewed the `swarm_handoff.bb` wiring: the gate is inserted into the
  validation `cond->` chain in the same shape as the sibling
  `task-scope-gate-lib` block (required_wiring #1, confirmed by grep at
  the actual call site, not merely present as a comment) — this is the
  live send path every `git_handoff` goes through, the same path I have
  used repeatedly this session.
- Confirmed `decide-for-ref` reads the merge-base with `main`/`origin/main`
  against the SENDER'S commit, never the parcel tip — verified in the code
  (`merge-base root commit ref`, never a comparison against a later
  worktree state) and by property P1's own coverage
  (`:p1-edit-parcel` 168/300, `:p1-amend-main` 156/300, independently
  randomized — a parcel-side edit of its own copy never flips `blocked?`).
- No duplication: `jscpd` doesn't parse `.bb` (0 files analyzed,
  informational only), but a manual read confirms the fail-open branches
  in `decide-for-ref` are each a distinct, single-purpose clause with no
  copy-pasted logic between them.
- Babashka has no mutation/CRAP/DRY tooling wired (BL-472 deferred) — this
  pass is gated by the unit/property/acceptance suites only, per
  constitution.

## Verification (all re-run in this worktree, all green, no regressions)

| check | result |
|---|---|
| `bb swarmforge/scripts/test/contract_freshness_gate_lib_test_runner.bb` | ALL PASS |
| `bb swarmforge/scripts/test/bl1411_...property_runner.bb` | ALL PROPERTIES HOLD, 300/300 each of P1/P2/P3a/P3b |
| `node specs/pipeline/cli.js specs/features/BL-1411-...feature` | 5/5 scenario runs |
| `bash swarmforge/scripts/test/test_swarm_handoff_sync_deliver.sh` (regression) | ALL PASS |
| `bash swarmforge/scripts/test/test_swarm_handoff_daemon_backup.sh` (regression) | ALL PASS |
| `bash swarmforge/scripts/test/test_swarm_handoff_inbound_non_forwarding.sh` (regression) | ALL PASS |
| `bash swarmforge/scripts/test/test_swarm_handoff_inbound_non_forwarding_batch.sh` (regression) | ALL PASS |
| `bb swarmforge/scripts/test/task_scope_gate_lib_test_runner.bb` (sibling gate regression) | ALL PASS |
| `grep -c 'read-yaml-field\|acceptance:' contract_freshness_gate_lib.bb` | 0 (invariant 2) |
| `grep -c declared-acceptance-path contract_freshness_gate_lib.bb` | 2 (invariant 2) |
| `grep -n 'contract-freshness-gate-lib/refusal-message' swarm_handoff.bb` | inside validation chain (required_wiring) |
| `bl1411ContractFreshnessGateSteps.js::registerSteps` exists | yes (required_wiring) |

No fix required. Forwarding unchanged to architect.
