# BL-1772 — QA review pass, 2026-09-26

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By QA.

## Detail

Parcel: documenter 665fc6ccf5 (task BL-1772), merged into QA as
7911d85241. The BL-1700 and BL-1711 ticket YAMLs conflicted only in
bounce_history; QA's side, which records both bounces, was kept. The merge
brought back BL-1700's and BL-1711's bounced code, because the BL-1767 land
re-point had dropped QA's local-only reverts of it. 9aad614c39 restores
those paths to origin/main (untagged subject). QA then synced main
(`Merge main 565ef4e104 into QA.`, d5707babb1), and every gate ran once on
d5707babb1. `qa-sibling-check status`: VERIFY. No earlier bounce is
recorded for BL-1772.

Gates (qa-gather):
- stragglers before and after: none in this worktree
- register: `unowned: []`
- `pre_qa_gate.sh` (required_wiring, the bl1772 handler's registerSteps pin): OK
- `npm test`: exit 0
- `npm run test:properties`: exit 0 (356s). The only unhandled errors are
  BL-871's allowlisted `[vitest-worker]: Timeout calling "onTaskUpdate"`.
- `run_acceptance.sh` on the BL-1772 feature: 4/4 (01, 02[1], 02[2], 03),
  duration 5.6s

The ticket's qa_e2e_procedure, one run each:
1. The feature: 4 cases ok (the acceptance run above), 5.6s.
2. `bash swarmforge/scripts/test/test_bl1772_push_repoints.sh`: ALL PASS
   (14), 2s.
3. Scope: `git diff origin/main` on the QA tip, restricted to BL-1772's
   own commits, names only the converted feature (the `.draft` removed),
   `specs/pipeline/steps/bl1772PushPublishRepointsQaBranchSteps.js`, the
   ticket's one-line `acceptance:` pointer flip, and evidence.
   `land_main_publish.sh` and `QA.prompt` are untouched. The feature
   carries the hardener's BL-113 manifest header (2/2 killed).

Invariants: three are declared. None is encoded as a property test; each
has a stated reason in the coder evidence. BL-654 allows that, and the
FIRM file constraint (evidence, feature, step handler with lib/ helpers,
swarmforge/scripts/test/) leaves no place for an extension/test property
file. Invariant 1 was checked by the scope diff. Invariants 2 and 3 were
checked by scenarios 02 and 03 against the real `--push`.

Live-surface confirmation (review goal 3's probe). On 2026-09-26, the BL-1767
land's `land_main_publish.sh <QA worktree> --push 0043a19bc9` re-pointed the
live QA branch from 924a360b95 to 7225607287 and logged
`:action :repointed` in the QA worktree's
`.swarmforge/daemon/land-repoint.log`. In_process held BL-1767's own
parcel and its claim-progress sidecar, so with BL-1773 landed the landed
parcel no longer blocks. One consequence found there went to the
specifier as a note, not an edit (note 003263): after a re-point, the
approved source that QA.prompt's recipe then records with
`record_land_approval.bb` is no longer on swarmforge-QA, so the replay
record prints "not approved". The landed commit is still approved by
ancestry (is_qa_ancestor exits 0). The dirty-tree skip is unchanged, and
the scenario 02 [1] row confirms it.

Disposition: the stamp-off is confirmed. The hotfix does what its commit
claims.

By QA.
