# BL-779 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Context

Re-promoted coordinator route while implementation already landed through QA
(`eb610ab5c` documenter tip on `main`). This pass verifies the parcel on the
re-promotion path — observability-only fix for pause-blind flow-watchdog alarms.

## Shape (ticket summary)

While `pause-active?` holds:

1. Flow-watchdog alarm header names `(swarm paused)` and suffix uses
   `format-pause-until-text` instead of `- rotate`/`- nudge`.
2. Babysitter all-clear line names the control pause instead of bare
   idle-correct.

BL-617 pause suppression and BL-577 unconditional watchdog both unchanged.

## required_wiring

| anchor | location |
|---|---|
| `format-pause-until-text` | `backlog_depth_lib.bb` |
| `format-alarm-text` pause branch | `flow_watchdog_lib.bb` |
| `format-all-clear-line` pause branch | `babysitterd_sweep_lib.bb` |
| `bl779PauseBlindFlowWatchdogAlarmSteps` | `specs/pipeline/steps/index.js` |

## Verification

| check | result |
|---|---|
| `flow_watchdog_test_runner.bb` | ALL PASS |
| `backlog_depth_test_runner.bb` | ALL PASS |
| `babysitterd_sweep_lib_test_runner.bb` | ok |
| `run_acceptance.sh BL-779…feature` | **5/5** |

Babashka degraded fallback (BL-472): unit and acceptance coverage only.

By coder.
