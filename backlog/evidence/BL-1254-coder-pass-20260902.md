# BL-1254 — coder pass (re-walk), 2026-09-02

Re-entry at the coder, per the specifier's corrected adjudication of
2026-08-30 in the ticket `notes:` ("the CODER builds scenario 06's step
handler on top of the documenter's work, then the parcel re-walks cleaner ->
QA"). This pass adds no new review: the 2026-08-30 coder pass
(`BL-1254-stamp-expedite-no-verdict-chain-20260830.md`) stands unchanged and
is not restated here.

## What the re-entry was for, and its current state

The stand-down reason was a BL-233 shape: the hardener and documenter each
merged main and so pulled scenario 06's CONTRACT into the feature file, while
no one had built its step handler — so the documenter tip could not pass
acceptance on scenario 06.

**That gap is closed and already on `main`.** Scenario 06's handler landed as
`36d8fd08b4` ("BL-1254: scenario 06 - a timeout is reported as a timeout, not
an absence"), which is an ancestor of `main`. Verified at `main`'s TIP CONTENT,
not by ancestry alone:

- `specs/pipeline/steps/bl1254ExpediteNoVerdictChainStampSteps.js` implements
  scenario 06's steps (the "killed for exceeding its time budget" handler is
  present, 1 match).
- `specs/pipeline/steps/index.js` registers
  `bl1254ExpediteNoVerdictChainStampSteps` (1 match).
- The feature file, the decision CLI shim, and both property-test files are
  present at `main`.

Nothing was rebuilt. Everything the earlier chain produced for scenarios 01-05
stands untouched, as the adjudication required.

## Verification run on this branch (main merged in at 199636257d)

- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1254-swarm-stamp-expedite-no-verdict-chain.feature`
  → **10 scenarios, 10 pass** (9 previously + scenario 06, the one the
  stand-down was about).
- `bb swarmforge/scripts/test/expedite_lib_test_runner.bb` → **ALL PASS**.
- `bash swarmforge/scripts/test/test_expedite_cli.sh` → **ALL PASS**. The
  ticket warns BL-782's known-failing cases here are pre-existing and not this
  parcel's to absorb; as on 2026-08-30, **they did not fail on this run**.
  Recorded rather than assumed: nothing absorbed, nothing hidden.
- Property lane (BL-654 invariant obligations, re-run because the tree moved):
  `bl1254LedgerCertificationNeedsAHuman.property.test.js` and
  `bl1254MissingVerdictNeverBounces.property.test.js` → **6 pass**.

## required_wiring — all four anchors verified live

| anchor | result |
|---|---|
| `specs/pipeline/steps/index.js::bl1254ExpediteNoVerdictChainStampSteps` | 1 match |
| `expedite_cli.bb::expedite-lib/should-recover-missing-verdict?` | 1 match |
| `expedite_cli.bb::expedite-lib/bounce-payload-valid?` | 1 match |
| `expedite_cli.bb::expedite-lib/finalize-stage-result` | 1 match |

Each decision is still called from the driver through the shared lib, not
inlined — which is what makes the reviewed bound testable as pure logic.

## Invariants — unchanged and re-checked

1. **Never reimplements the hotfixes.** This pass edits no hotfix source. The
   acceptance Background runs `git status --porcelain` over all four files the
   chain touched and fails if the parcel modified any; green.
2. **Only a human decision certifies.** All three ledger rows read
   `state: stamp-open`, `stamp_ticket: BL-1254`, `human_decision: null`. The
   ledger file is unmodified by this parcel.
3. **No scenario asserts superseded behaviour.** Enforced mechanically by the
   Background's scan of the feature file; green.

## Verdict

CONFIRMED, re-walk ready. The 2026-08-30 finding (`bounce-payload-valid?`
lower-cases `reason` but compares `class` exactly, so an upper-cased
`NO-VERDICT-ABANDONED` class would be accepted) is unchanged, still not live,
still a narrow follow-up rather than an edit here, and still pinned by
`bl1254MissingVerdictNeverBounces.property.test.js`.

All three `Hotfix-Certification` rows stay pending. Green scenarios do not
certify; only a recorded human decision does.

Forwarding to cleaner.

By coder.
