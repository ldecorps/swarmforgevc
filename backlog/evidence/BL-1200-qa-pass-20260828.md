# BL-1200 — QA pass — 20260828

**Received:** `merge_and_process documenter d474c423e5` (handoff
`00_20260828T002424Z_000861_from_documenter_to_QA`)
**Documenter tip (this ticket's own work):** `d474c423e57ad8f2a8216a6a01e066531b10e38e`
**QA merge commit:** `60d01f92816438ed307e67581b77400c13eaeda3`

## Branch-divergence and out-of-scope content found and declined (BL-506)

Same root cause and remedy as BL-644/BL-751 today (documenter's branch
predates `f8a41c1e2`; see `backlog/evidence/BL-644-qa-pass-20260828.md`).
This one's entanglement was the heaviest of the three: a full
`git merge --no-ff --no-commit` produced one genuine conflict
(modify/delete on `specs/pipeline/steps/bl592SpecTreeOnLiveConsoleWithEpicTierSteps.js`
— already declined during BL-751's parcel, independently re-touched here)
plus a large unrelated bundle: **BL-1188's and BL-1189's own real
implementations** (`pipelineGridLive.ts`/`.test.js`, `residentPaneLive.ts`,
`residentPaneSpy.ts`, both property tests + step handlers), **BL-428 and
BL-592 evidence files**, an **architect vitest-gap note**, and an
**unrelated `active_backlog_max_depth 6→7` config bump**
(`swarmforge/packs/full-forge.conf`).

Resolved the conflict by keeping the path deleted (consistent with BL-751's
decision — not reversing it). Declined everything else via selective
`git checkout HEAD --` (modified paths) / unstage-and-remove (new paths),
and hand-edited the one shared registration file
(`specs/pipeline/steps/index.js`) to keep only the
`bl1200GitEnvGuardSteps` require, dropping the bl1188/bl1189 ones it
arrived bundled with. Verified every declined path first (no BL-1200
reference, existence-in-HEAD checked before each checkout to avoid the
BL-751 hardener.prompt misclassification repeating). Kept set: 10 files,
`git diff --cached --stat` recorded in the merge commit message.

**BL-1200's own ticket yaml diff is large (53 lines) but is legitimately
its own** — not scope creep: it's the specifier's own "expedite stage 1"
repair of a genuinely malformed `required_wiring:` block (a stray
multi-line note had been appended inside the list, making the whole field
fail `pre-qa-gate-lib/read-required-wiring` and yield `wiring-entries []` —
confirmed by the specifier's own note in the diff, not asserted by QA) plus
the `abandoned_commits:` addition this parcel's own commit message
describes. Read the full diff before accepting it wholesale.

**BL-1188/1189/592/428's apparent absence from `main`** is the same family
of finding as BL-751's BL-592 flag — not landed here, flagged in the same
priority-00 note as this ticket's hold/ pool flag (see below).

## What BL-1200 actually delivers (previously entirely missing from `main`)

`swarmforge/scripts/test/lib/git_env_guard.sh` **did not exist on `main`
before this commit** — despite the ticket's bookkeeping (required_wiring,
abandoned_commits notes) having partially round-tripped through earlier
partial syncs, the actual fix for this high-severity, live defect (ambient
`GIT_DIR`/`GIT_WORK_TREE` corrupting shell-test git writes — the exact
class that hit `swarmforge-coder`'s branch during this same QA session, see
`backlog/evidence/BL-751-qa-pass-20260828.md`) was never actually landed.
Sourced from `expedite_fixture.sh` and `run_bb_suite.sh` per the ticket's
own two-site requirement.

## Verification inventory

| Gate | Command | Result |
|------|---------|--------|
| Sibling disposition | `qa-sibling-check.js status --ticket BL-1200` | `VERIFY BL-1200` — no open deferral |
| Compile | `npm run compile` (extension/) | PASS, clean |
| Shell unit | `bash swarmforge/scripts/test/test_git_env_guard_lib.sh` | 5/5 PASS |
| Acceptance | `run_acceptance.sh specs/features/BL-1200-*.feature` | 3/3 PASS |
| Property test | `node --test test/bl1200FixtureGitWritesStayInOwnRepo.property.test.js` | 4/4 PASS. Cannot run via `npm run test:properties` — the file uses `require('node:test')` directly (pre-BL-124 migration convention), a pre-existing, repo-wide, already-flagged gap (BL-1206, freshly minted in today's BL-1214-recovery merge — not re-reported), not a BL-1200 defect. |
| Wiring (required_wiring, both rows) | grep each anchor | `expedite_fixture.sh:36` sources `lib/git_env_guard.sh`; `run_bb_suite.sh:33` sources it before the inventory gate (line 44) — both CONFIRMED |
| Invariant | manual + property/shell evidence | "fixture's git writes land in its own repo, never one named only by an inherited env var" — directly exercised by shell test #04 and all 4 property cases |
| Full unit suite (`npm test`) | **not re-run a third time this session** | BL-1200 touches zero `extension/src/**` TS/JS production files (shell scripts + one pipeline-framework step handler outside `extension/`); Article 4.5 scopes the unit gate to changed production paths, and the changed path here has its own dedicated, just-run shell suite. Already run twice today (BL-644, BL-751 passes) with identical 46-file/202-test standing BL-720/728 host-env debt both times — a third run adds no new signal for files this ticket doesn't touch, and the property suite specifically carries a live, already-sighted branch-corruption risk this session (`swarmforge-coder` read `seed` mid-session, self-recovered — see BL-751's evidence). Not skipping a required check: the required check for this diff's paths already ran and passed. |

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS.**

Landed on `main` per BL-1144 discipline (lock, decide-only, push, release —
see push log). BL-1200 carries no `GH-` id.

Merge-up broadcast sent to coder/cleaner/architect/hardender/documenter.
Coordinator+specifier notified for backlog bookkeeping with the hold/ pool
flag (same shape as BL-644/BL-751) plus the BL-1188/1189/592/428
missing-from-main finding.

By QA.
