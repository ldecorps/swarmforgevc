# BL-1486 — QA bounce, 2026-09-15

D1: documenter's own merge conflict resolution silently reverted the
cleaner's standing-red register row removal — the ticket's own
`qa_e2e_procedure` step 5 fails.

1. **Failing command**:
   `bb swarmforge/scripts/standing_red_register_cli.bb .`
   (run from the merged QA worktree at the commit below)
2. **Commit hash**: `93b4fe698bb3ec1a7d3f699ac93b960f8db9473c` (`Merge
   documenter 038ba762a9 into QA.`)
3. **First error excerpt** (register CLI output, filtered to `ticket ==
   "BL-1486"`):
   ```
   specs/features/BL-1052-a-role-seat-can-be-staffed-by-a-downloaded-local-model.feature
   specs/features/BL-1218-config-off-is-honored-over-an-explicit-window-flag.feature
   specs/features/BL-1320-operator-step-for-adding-a-seat-to-a-bottleneck-stage.feature
   specs/features/BL-1418-the-art-director-seat-is-addressable.feature
   specs/features/BL-961-launcher-exports-swarmforge-pack-into-role-shells.feature
   specs/features/BL-628-bare-host-bootstrap-for-autonomous-swarm.feature
   ```
   All six still `owned: true, ticket: "BL-1486"` — unchanged from before
   this parcel started.
4. **Failure class**: `behavior` (intent mismatch — a merge silently
   reverted already-landed work, not a red test).
5. **Expected vs observed**: qa_e2e_procedure step 5 requires "The landing
   removes this ticket's six rows from `backlog/standing-reds.tsv`;
   `bb swarmforge/scripts/standing_red_register_cli.bb .` reports none of
   the six files." Observed: the CLI still reports all six.

**Root cause traced** (Guardrails: "A merge can silently revert already-
landed work — diff every merge against BOTH parents", BL-571/958/954):

- Cleaner's commit `81a1ed3ec3` ("BL-1486: retire the six standing-red
  register rows this ticket's land turns green") correctly removed all six
  rows. Confirmed present and effective through the architect merge chain
  (`c276a49924`, `5051d8bf29`, `6c7a218d9f` — each diffed against both
  parents: register count stays 0 throughout).
- The documenter's own merge, `4566a68955` ("Merge hardender 6cf853db7f
  into documenter."), hit a REAL conflict on `backlog/standing-reds.tsv`
  (commit message: `# Conflicts: # backlog/standing-reds.tsv` — main had
  landed BL-1575's mint in the interim, touching adjacent lines of the
  same file). The conflict was resolved by keeping parent 1's (the
  documenter's own prior main-merged tip, `61a4e2e5f1`) full six rows
  instead of parent 2's (hardener, `6cf853db7f`) correct removal — diffing
  the merge result against P2 shows the six rows re-added verbatim, with
  no trace of the resolution reconciling the two divergent edits.
  Confirmed: `git diff 6cf853db7f 4566a68955 -- backlog/standing-reds.tsv`
  shows exactly the six rows reinserted.

**Blamed role**: documenter (the bad conflict resolution is in the
documenter's own merge commit `4566a68955`; the register-row-currency
deliverable is also the ticket's own documenter-adjacent "Register" item
under "What is wanted").

**Remediation pointer**: `backlog/standing-reds.tsv` — re-apply cleaner's
`81a1ed3ec3` removal of the six `BL-1486`-owned rows (the six lines
matching `acceptance\tspecs/features/BL-1052...`,
`BL-1218...`, `BL-1320...`, `BL-1418...`, `BL-961...`, `BL-628...`) on top
of the current tip, preserving BL-1575's own edits (the BL-1569 removal
and BL-1495 addition) that the same conflict correctly kept. Re-run
`bb swarmforge/scripts/standing_red_register_cli.bb .` and confirm none of
the six files are reported before forwarding.

Everything else run clean and is NOT a defect (recorded for the inventory,
Article 4.4):

- Full unit suite (`npm test`, `extension/`): 616/616 files, 10479/10479
  tests pass. The run's own suite-duration/per-file budget guard separately
  exits 1 on six pre-existing slow files (bl968StepRegistryMaterializedTreeGuard,
  bl1277UnscopedStepCollisionGuard, briefingDigestLineCli,
  renderBriefingBurndownCli, renderBriefingDiagramsCli,
  telegramFrontDeskBotCli, emitLifecycleSnapshotCli) — a wall-clock check,
  not an assertion failure (BL-378/BL-445 precedent, e.g.
  `backlog/evidence/BL-1464-QA-20260913.md`); none are touched by this
  ticket's diff.
- Property suite (`npm run test:properties`): 405/405 files, 1194/1194
  tests pass, exit 0. Two `[vitest-worker]: Timeout calling
  "onTaskUpdate"` unhandled errors — the allowlisted BL-871 noise.
- Acceptance, BL-1486's own feature, `env -u PACK_STAFFING_SKIP_GATE`:
  9/9 pass (all four scenarios).
- Acceptance, the six named features, `env -u PACK_STAFFING_SKIP_GATE`:
  BL-1052 9/9, BL-1218 7/7, BL-1320 4/4, BL-1418 7/7, BL-961 4/4,
  BL-628 16/16 — all `# fail 0`.
- `env -u PACK_STAFFING_SKIP_GATE bash
  swarmforge/scripts/test/test_shipped_confs_no_coordinator_window.sh`:
  `ALL PASS`, no "pack staffing gate refused" line.
- Offline-expedite shape (`env -i PATH="$PATH" HOME="$(mktemp -d)"`) for
  BL-1052 and BL-1418: 9/9 and 7/7 pass.
- BL-1318's and BL-1485's own features, `env -u`: 7/7 and 4/4 — unchanged
  at their pre-existing baselines, no regression to the opposite
  population.
- `grep -rn PACK_STAFFING specs/pipeline/runtime.js
  specs/pipeline/scripts/`: no output (scenario 04's invariant).
- `bash swarmforge/scripts/pre_qa_gate.sh
  BL-1486-every-launcher-driving-handler-decides-the-staffing-gate-itself
  <commit>`: `OK` (required_wiring — the new handler's registerSteps
  wiring confirmed).
- `docs/how-to/BL-1318-pack-staffing-gate.md`: correctly extends BL-1485's
  existing sentence with one new sentence for this ticket's lane, rather
  than adding a third (matches the ticket's own instruction).
- No orphaned `node --test`/`stryker` processes before or after (pgrep/ps
  cross-checked; the pgrep hits were transient shell artifacts, confirmed
  empty via `ps -ef`/`/proc`).
- Ancestry: this ticket's own coder (`e67c8c1906` topic-record commit
  region), hardener (`6cf853db7f`), documenter (`038ba762a9`) are all
  ancestors of the cited commit `93b4fe698b` (`git merge-base
  --is-ancestor` verified).

By QA.
