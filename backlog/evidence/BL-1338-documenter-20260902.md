# BL-1338 — documenter pass (20260902)

Received: hardener commit `a3be017593` (BL-1338: hardener pass - close a
real mutation gap in the sed-rewrite fingerprint path), forwarded unchanged
from architect's clean sweep `e89e836998` on top of cleaner's clean sweep
`17b4e11ef4` / coder's `69ae1c2ee3`.

## Review inventory

- Read `backlog/active/BL-1338-a-routing-stamp-does-not-invalidate-an-adjudication.yaml`
  (including `human_ruling:` — the chosen option is "exclude only the
  routing fields a promotion writes"), its acceptance feature, and the
  coder/cleaner/architect/hardener evidence files.
- Read the shipped code (`extension/src/tools/deprecate-check.ts`:
  `fingerprintableTicketText`, `computeTicketFingerprint`,
  `APPENDED_ROUTING_STAMP`, `ROUTING_STAMP_LINE`) to confirm the
  operator/specifier-visible behavior actually shipped, and confirmed it
  matches the human ruling exactly — only the `assigned_to:` routing stamp
  is normalized out, every other field still hashed.
- Grepped `docs/` for existing descriptions of the adjudication fingerprint
  before writing anything: `docs/how-to/BL-1173-deprecator-freshness-gate-cli.md`
  and `docs/reference/Specification.MD` both stated the fingerprint is a
  SHA-256 over "the exact ticket YAML text" — now inaccurate, since a
  routing-stamp promotion no longer changes it. This is exactly the "doc
  already accurate ≠ doc pass done" trap in reverse: doc WAS accurate until
  this ticket, now needs correction, not a skip.
- No diagram in the registry depicts the deprecator/adjudication
  mechanism's internals; none of the four change-triggers fired.

## Doc changes made

1. `docs/how-to/BL-1173-deprecator-freshness-gate-cli.md`, "Discharging a
   hold" section: corrected the fingerprint description to state the
   routing-stamp exclusion (both the appended-fresh and in-place-sed-
   rewritten shapes), while preserving the existing statement that any
   other edit still re-arms the gate (BL-1267's invariant, unchanged and
   still true — verified against the ticket's own two declared invariants
   and the hardener's evidence that both are property-tested).
2. `docs/reference/Specification.MD`: prepended a new top-of-file "Last
   Updated" changelog entry (2026-09-02, this repo's running-changelog
   convention, `Prior entry —` chain) describing the defect, its observed
   trigger (BL-1271's own promotion, live incident), and the fix's scope.
   Left the existing 2026-08-29 BL-1267 changelog entry as-is (a dated
   historical snapshot, not a currently-binding description — the running
   log's own convention is to prepend a correction, never rewrite a prior
   entry).
3. No `docs/index.md` entry needed — no new doc file created, only two
   existing docs corrected.
4. No retirement: BL-1267's discharge mechanism and store are unchanged;
   nothing here is deprecated behavior, so `docs/deprecated/` does not
   apply.

## Verdict

NONE — no documenter-domain defect found in the inbound parcel. This
pass's own output is the doc corrections above, committed as `05c336fa5a`.

By documenter.
