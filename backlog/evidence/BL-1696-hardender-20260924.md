# BL-1696 — hardender review pass, 2026-09-24

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Verified: acceptance feature (16/16), the two BL-654 property tests (3
tests), all 22 standing whole-tree guards under `extension/test/*Guard*.test.js`
(211 tests, since this parcel adds/edits step files under
`specs/pipeline/steps/`), `bash -n swarmforge/scripts/seat`, and the
bash-3.2-compatibility grep (`mapfile|readarray|declare -A` — none found).
No `extension/src/*.ts` change in this parcel's own diff, so CRAP/Stryker
do not apply (bash has no wired mutation tool, engineering.prompt
"Startup Tools"); the feature carries two Scenario Outlines, so ran
BL-113 Gherkin mutation (`run_gherkin_mutation.sh`, hard) instead.

Found and fixed in this pass (commit e8686d9abb), rather than bouncing —
own-domain strengthening, not a defect outside the hardener's remit:

- Scenario 01's "DRAFT" branch (note verb) asserted only that a
  note-typed draft existed, never its `to`/`priority`/`message` fields —
  a mutation of the note message text in the Examples table survived
  unnoticed. Added field assertions.
- Scenario 01's "ask" branch built its expected argv from
  `ctx.invocationTokens`, the same Examples cell the mutator perturbs —
  the "expected" side mutated in lockstep with the "actual" side, so the
  comparison could never disagree with itself (a passthrough, despite
  the file's own header disclaiming one). Pinned `KNOWN_ASK_QUESTION` /
  `KNOWN_NOTE_MESSAGE` literals, and cross-checked the ask row's own argv
  display column against the pinned literal as a second, independent
  source before comparing to the real recorded call.

Gherkin mutation on scenario 01 (well-formed verbs) is now 12/12 killed
(embedded manifest in the feature file). Scenario 02 (malformed
argument / verb outside the role's set) still carries 15 survivors,
all mutations of an already-malformed example's invocation or role text
into a DIFFERENTLY malformed one (e.g. `handoff aider BL-7` →
`hAndoff aider BL-7`; `coder` → `coDer` against a row whose verb the
role already lacks). Recorded as EQUIVALENT, not chased further:
scenario 02's own assertion ("it exits 2 and prints the usage of the
verb, or the role's verbs for an unknown one" / "no script call is
recorded") is a deliberate disjunction over refusal reasons per the
ticket's own invariant text ("a refused invocation (malformed argument,
verb outside the role's set, unknown verb) runs nothing") — it does not
distinguish WHICH validation failed, by design, so any mutation that
keeps a row within the malformed-input space is unobservable to this
scenario's own checks by construction. Manually traced each of the 15
against `swarmforge/scripts/seat`'s dispatch order (verb-known check →
verb-allowed-for-role check → per-argument checks) to confirm each
mutated row still hits the same exit-2/usage-or-unknown-verb/no-call/
unchanged-tree outcome the original row hit, just via a different
validation branch. BL-113's own per-scenario manifest (BL-502) omits
scenario 02 from the embedded manifest as a result (only fully-clean
scenarios are recorded) — expected, not a regression.

By hardender.
