# BL-1620 - send-back, 2026-09-18 (spec-gap, charged to the specifier)

Inbound: hardender note 00_20260918T083203Z_001390 (priority 00, to
specifier and coordinator): "BL-1620 spec-gap: npm test intermittently
new-pole refuses; evidence has detail". Hardener evidence
`backlog/evidence/BL-1620-hardener-20260918.md` on the hardender branch
(16ef87cee7). The parcel is still in the hardender's in_process batch
(`batch_20260918T080022Z_000001/..._002205_from_architect_to_hardender`).

## D1 - the spec named no measurement condition (spec-gap, specifier)

The ticket's "under 7000 ms in three consecutive npm test runs" and the
FIRM "measured (three runs under 7000 ms at load below 8)" do not say
whether the number is the file ALONE or IN-SUITE. The How told the coder
to profile with `npx vitest run <file>` - solo - and the coder did
(3.7-5.0 s, three runs, evidence `BL-1620-coder-landed-20260917.md`).
The gate (`check-suite-file-budget.ts`, `extractFileDurations`) reads the
file's duration from `npm test`'s own JSON report, inside the lane's
10-11 forks. The hardener's eight in-suite readings at load below 8:
8.0, 9.1, 10.5, 12.3, 12.4, 12.5, 14.3, 19.7 s - six at or above the
10500 ms refusal line. Scenario 02 ("row removed ... no new-pole") is
therefore not reliably true, and the acceptance handler could not see it
because it feeds the SOLO worst run into the guard.

## D2 - the FIRM's premise is false under the in-suite measure (spec-gap, specifier)

"The two register rows ... leave with its land (BL-1598: a surviving row
reads stale)": a row reads stale only when its file measures under 80
percent of budget (5600 ms) in the run - and in-suite the file measures
8-20 s. A surviving row is reported as a registered pole, never refused
(`formatGuardReport`: only new-pole and unowned-row fail). Removing it
turns the file into a new offender on most runs. So the row must stay,
owned by an open ticket, until the gate stops refusing on contention.

## D3 - the 2026-09-17 rewording was never sent (specifier)

f291a8373e reworded the feature on main ("both files"/"either file" ->
the one file) while the parcel was already past the architect (the
addendum in `BL-1620-bounce-20260917.md` records the timing). The
specifier session died before the note reached the holder: no specifier
send after 16:00Z on 09-17 exists under `.swarmforge/handoffs/specifier/`.
The hardender's handler still carries the four old literals; the
reworded feature on main would throw at land for want of a handler.

## Disposition

- BL-1620 amended on main (this commit): scenario 01's verb, scenario 02
  rewritten (row stays under BL-1633, guard run against the parcel's
  RECORDED in-suite duration), constraints, e2e step 2, invariant 2, the
  FIRM annotated (the human told in the specifier's reply). Feature text
  in the ticket's notes, not the feature file (BL-1385: parcel past the
  coder).
- BL-1633 minted: the gate confirms a suspected new pole alone before
  refusing; owns the register row; row leaves in its land.
- Register row re-owned BL-1620 -> BL-1633 (measured_ms 19700, range in
  the note).
- Note to the hardender (holder): merge main, bounce to coder. Note to
  the coder: bounce coming, merge main first, keep the row. Note to the
  coordinator: BL-1633 paused-ready.
- Bounce recorded against commit 16ef87cee7 as spec-gap with a
  correction naming the specifier's spec as the cause (BL-990): the
  coder built to the spec as it stood.

By specifier.

## D4 - the 09:1x amendment left the narrative and Background false (specifier, found by the cleaner)

Cleaner note 000820 (2026-09-18T09:12Z), holding the parcel after the
coder's re-round-trip 21e826f6c1: the Feature narrative still said "its
register row leaves in the same land" and the Background still said
"naming the file under BL-1620" while scenario 02 said the row stays under
BL-1633. Both sentences fixed on main (the parcel is at the cleaner:
BL-1385's coder/cleaner/architect arm, edit on main and note the holder).
The Background is now ticket-free so it survives BL-1633's land. Scenario
02 itself goes false when BL-1633 retires the row; its retirement and the
narrative's re-tensing are scoped into BL-1633 (BL-1006). Not a bounce:
the cleaner recorded NONE and the parcel continues; no bounce record.

By specifier.

## D5 - cleaner note 000825 ("D4 broke handler again"): already fixed on the parcel, no bounce

The cleaner merged main (36e1e9dddb) into ITS OWN branch after the parcel
had left it (forwarded to the architect 09:13Z), re-ran acceptance there,
saw the Background step unmatched, and wrote bounce evidence
`BL-1620-bounce-cleaner-20260918.md` (cleaner branch 0b06b57091). That
branch is not the parcel. The holder at the time of D4's note was the
hardender, which did what the note asked: 7e74a9e2eb "align the
Background step regex with the specifier's ticket-free wording", merged
main (d5c495e702 is an ancestor), recorded NONE, and forwarded to the
documenter at 09:22Z; the documenter forwarded to QA at 09:25Z (001358).
At the parcel commit the feature's Background and the handler's regex
agree. The cleaner's evidence says it would send a git_handoff to the
coder; none was sent (no from_cleaner file in either coder inbox after
09:24Z). Its notes to the coordinator and documenter on this point are
void; the specifier said so by note. No bounce record: nothing bounced.

Lesson for the reviewer off the critical path: check the holder's tip
before raising a defect from a branch the parcel has already left.

By specifier.
