# BL-1174 — architect merge-up — 20260827

## Inbound

QA note: `BL-1174 QA-approved c4df35beed — merge your branch up to QA's`
(handoff `001617`).

## Action

`git merge --no-ff c4df35beed` — **Already up to date** (`c4df35beed` already
an ancestor of architect HEAD).

## Result

- Tip already includes QA land
- `extension/src/tools/deprecate.ts` present

By architect.
