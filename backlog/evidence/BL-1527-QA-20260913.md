# BL-1527 — QA review pass, 2026-09-13

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

- Merged documenter's `46e3dceb65` into QA (`ead982a1b7`).
- Property file `10/10` standalone runs green:
  `npx vitest run --config vitest.properties.config.mjs test/bl1367ApprovalCarriesItsRuling.property.test.js`.
- Helper sanity: `expectedNoChoiceRulingKind(" ")`, `("\t")`, `(undefined)`,
  `("")` → `ok`; `("a free answer")` → `unknown-option`; each equals
  `classifyApprovalRulingRequirement(undefined, <same>).kind` from the
  compiled `pendingApprovalReply.ts` — oracle and classifier agree on
  every case.
- `git diff main...HEAD --name-only`: exactly the property test, the new
  helper, and the step handler (plus this ticket's own evidence files) —
  `extension/src/`, `property_suite_standing_allowlist.tsv`, and the
  BL-1367 feature/handler are untouched.
- Acceptance: `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1527-*.feature` → 9/9 scenarios pass.
- `swarmforge/scripts/pre_qa_gate.sh` (required_wiring) → OK.
- Full unit suite: `npx vitest run` → 616/616 files, 10479/10479 tests.
- Property suite (`npm run test:properties`): two separate full runs each
  showed a different pair of unrelated files fail
  (`bl1297MergeOwnPathsInvariants`, `bl1343ReplayNeverDropsOwnPathInvariants`,
  plus assorted `[vitest-worker]: Timeout calling "onTaskUpdate"` /
  `runGitLog` fixture-git noise on the first run) — non-reproducing,
  neither ticket is in `backlog/standing-reds.tsv`, and this matches the
  known host-contention finding in
  `backlog/evidence/BL-1348-coder-finding-fork-ceiling-linux-full-forge-contention-20260906.md`
  (this host runs many concurrent agent/node processes). Re-ran both named
  files alone: 5/5 tests pass. No orphaned `vitest`/`node --test`/`stryker`
  process before or after (`pgrep -fl` clean both times).
- Standing-red register: line 39 (`bl1367ApprovalCarriesItsRuling...`,
  BL-1527) removed below — this land turns that file green, per the
  ticket's own notes.

By QA.
