# Documenter bounce — BL-681 (2026-08-08)

## Context

Received BL-681's `git_handoff` (from hardender, commit `b2cd357fcb`,
merged into `c785a890f7` alongside my doc pass for the same batch's BL-574
and BL-762). Attempting to forward BL-681 to QA, `swarm_handoff.sh`'s
pre-QA gate (BL-761 acceptance-contract check) refused the send:

```
PRE_QA_GATE_FAIL acceptance-contract BL-681 acceptance: declaration is
unreadable at the cited commit (absent, inline Gherkin, or naming a
feature file that does not exist there)
```

## Complete review pass — one defect found

Per Article 4.4, ran the acceptance-contract check for all three items my
own doc pass covers before bouncing anything:

- BL-574: gate WARNS (registry-load error, unrelated environmental
  `git rev-parse --git-common-dir` failure in the check itself, not a
  missing/wrong declaration) but does not block — forwarded successfully.
- BL-762: same environmental WARNING, not blocking — forwarded
  successfully.
- BL-681: gate FAILS closed. **D1** below is the only defect found in this
  ticket's review.

## D1 — `acceptance:` field points at a filename this batch renamed away

- **Class:** wiring (a mechanical pointer left stale by a file rename, not
  a defect in the Gherkin content itself or the ticket's requirements).
- **Blamed role:** coder — the promotion from `.feature.draft` to
  `.feature` happened in coder's own commit `15b9ff9dda` (per the
  architect pass evidence's per-ticket commit range breakdown), which is
  exactly the action BL-681's own `notes:` assigns to the coder: "Gherkin
  parked as `.feature.draft` per BL-233; the coder promotes it and wires
  handlers in the same commit." The rename (`git mv`, confirmed 100%
  content-identical in the merge diff) landed without the matching
  `backlog/active/BL-681-consolidation-never-drops-a-human-sentence.yaml`
  `acceptance:` pointer update, so the ticket's own contract now names a
  path that does not exist at any commit in this batch.
- **Remediation pointer:** `backlog/active/BL-681-consolidation-never-drops-a-human-sentence.yaml`
  line 84 — change
  `acceptance: specs/features/BL-681-consolidation-never-drops-a-human-sentence.feature.draft`
  to
  `acceptance: specs/features/BL-681-consolidation-never-drops-a-human-sentence.feature`
  (the file that already exists, already reviewed 3/3 green by both
  architect and hardener via a direct path argument to `run_acceptance.sh`
  — this is a one-line pointer fix, not new implementation work).
- **Not a spec-gap:** the scenario content itself is testable, has a step
  handler (`bl681ConsolidationNeverDropsHumanSentenceSteps.js`), and passed
  3/3 for both architect and hardener. Nothing about the Gherkin or its
  wiring is ambiguous or missing — only the ticket YAML's own pointer to it
  is stale. Routes to coder (ordinary wiring defect), not specifier/note.

## Blocked checks

None — this ticket's doc content (none needed; BL-681 is pure
constitution/prompt prose, already landed by coder) was otherwise
reviewable and clean; the acceptance-contract gate is the sole blocker.

## Disposition

Documentation-side work for BL-681 (the Governance-section staleness fix
in `docs/reference/Specification.MD`, landed in `c785a890f7`) stands;
nothing here asks the coder to redo doc work. Bouncing only for D1. The
coder fixes the one-line pointer, forwards down the remaining chain
(cleaner → architect → hardener → documenter → QA) so every gate re-runs
against the corrected commit.

By documenter.
