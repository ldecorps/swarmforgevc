# BL-1530 — coder evidence (2026-09-11)

## What changed

`swarmforge/scripts/swarm_handoff.bb` is untouched (`git diff main --
swarmforge/scripts/swarm_handoff.bb` is empty; no new `System/getenv` read —
grep for `AUDIT|SKIP` above shows the same three reads as on `main`). Three
standing shell tests were made to speak the two-call self-audit protocol
(Article 2.3, BL-1306) instead of sending a `git_handoff` draft once and
asserting a queue:

- `test_rule_proposal.sh`: case 04's `git_handoff` row is pulled out of the
  loop, sent twice (first call tolerant of either exit convention, asserted
  to print `AUDIT_REQUIRED` / `HANDOFF_NOT_QUEUED` and queue nothing; second
  call asserted with the existing `assert_queued` mailbox-only grammar
  check). `awake`/`note` stay in the single-call loop.
  Also fixed in the same file: case 03b's drain-wait loop polled the
  *outbox count* (a proxy) instead of the audit-log line count it actually
  reads next, so a loaded host (e.g. a concurrent mutation run) could starve
  the poll past its 10s deadline while delivery had in fact completed. It
  now polls the audit file's line count directly, same 40×0.25s=10s bound.
  Verified non-vacuous: reverting the second `git_handoff` call turns case
  04 red again (`assert_queued` fails against the AUDIT_REQUIRED text);
  restored and confirmed green again.
- `test_handoff_state_dir_worktree_root.sh`: cases 01, 02 and 04 each send a
  `git_handoff` draft via `swarm_handoff.bb`; each now calls twice (audit
  challenge tolerated, outbox count asserted unchanged after the first
  call; case 01 also re-asserts the nested `extension/.swarmforge` state is
  still absent after the FIRST call — the ticket's own BL-056 property).
- `test_required_stages_ticket_lookup_collision.sh`: the `send` helper now
  calls `swarm_handoff.bb` twice per task, asserting `HANDOFF_NOT_QUEUED` on
  the first call (BL-1306's reroute-after-audit contract) before parsing
  the second call's output for the installed `.handoff` path.

All three files: `bash swarmforge/scripts/test/<file>` → `ALL PASS`, exit 0.
`test_rule_proposal.sh` run 5 consecutive times, all green (checks 03b's
fix isn't itself flaky).

## Acceptance

`specs/pipeline/steps/bl1530ShellTestsSpeakTheAuditSteps.js` registers steps
for the specifier-authored feature file (unchanged by this parcel). Ran via
`run_acceptance.sh`: 7/7 scenarios pass (`# pass 7`, `# fail 0`).

## Register

Removed the three `BL-1530` rows from `backlog/standing-reds.tsv` (test
files are green; `test_redo_from.sh`/`test_reroute.sh` rows are BL-1529's,
untouched, out of scope per the ticket).

## Invariants (BL-654)

Declared invariant: "Every shell test that sends a git_handoff through
swarm_handoff.sh asserts the queue on the second identical invocation,
never the first, and none reaches the queue by bypassing the audit."

No property test authored — stated reason: this invariant quantifies over
the *shell test files' own source and process* (do they call twice? do they
assert the first call's grammar? does any of them read an audit-bypass env
var?), not over states of a pure, testable module a generator could explore
per BL-654's guidance. It is instead enforced executably by this parcel's
own acceptance scenarios 02 and 03
(`bl1530ShellTestsSpeakTheAuditSteps.js`), which grep the real test files
for the two-call shape and diff `swarm_handoff.bb` against `main` to prove
no bypass was introduced — the same discipline the ticket's own "How"
section asked for ("grep them for the two-call shape; do not make it
re-implement the tests"), run for real via `run_acceptance.sh` above.

## required_wiring

`bl1530ShellTestsSpeakTheAuditSteps.js::registerSteps` lands in this same
parcel alongside the (unchanged, specifier-authored) feature file.
