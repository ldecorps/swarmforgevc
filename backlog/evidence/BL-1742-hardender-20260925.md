# BL-1742 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `extension/test/telegramFrontDeskBotCli.test.js` — test-only
(mutation_cost: medium — no production code touched, but the ticket asks
the hardener to re-verify the `enqueueRoleAnswerNote` options mutant the
new seam test claims to kill).

**Automated Stryker mutation was attempted and is currently BLOCKED by a
pre-existing, unrelated tool defect — not a load issue on the second
attempt, and not anything in this parcel's diff.**

- Attempt 1 (foreground, backgrounded by the tool):
  `--mutate 'out/tools/telegram-front-desk-bot.js' --concurrency 1`.
  Crashed at 5m02s: `ERROR DryRunExecutor Initial test run timed out!`
  — the documented load-crash signature (Hardening Order, "Under high
  host load, Stryker's perTest dry-run can hard-crash..."). Host load was
  ~20 on 20 cores at launch and climbed further while this attempt and a
  second one (below) briefly overlapped.
- Attempt 2, via the registered `detach_job.sh` helper (same `--mutate`
  target), run after the first had already crashed: the dry run this
  time completed the vitest pass but Stryker refused to proceed —
  `ConfigError: There were failed tests in the initial test run` — one
  failing test, unrelated to this ticket's file:
  `test/confirmPoleAloneOutcomeShapes.test.js`, "confirmPoleAlone
  measures a real, existing file and returns {ms}, never a bare number",
  failing with `{"failed":"no entry for
  extension/test/bl1007ContentionBudgetSmoke.test.js in the confirmation
  report"}`.
- Root-caused, not just observed: that test calls
  `confirmPoleAlone('extension/test/bl1007ContentionBudgetSmoke.test.js')`
  (`scripts/recordTestDuration.js`), which spawns a real `vitest` child
  rooted at `REPO_ROOT_DIR = path.join(ROOT_DIR, '..')` where
  `ROOT_DIR = path.join(__dirname, '..')`. Under Stryker's sandbox, the
  sandbox dir itself IS the extension root (this prompt's own BL-1066
  rule), so `ROOT_DIR` correctly lands on the sandbox root but
  `REPO_ROOT_DIR` (one more `..`) lands on `.stryker-tmp/`, which has no
  `extension/` child — the sibling-symlink mechanism
  (`ensureStrykerSandboxSiblings.js`) only covers named repo-root
  siblings (`swarmforge/`, `docs/`, `pwa/`, `specs/`, `backlog/`,
  `.github/`), not a generic "any repo-root-relative path" escape. The
  spawned vitest child therefore targets a directory that doesn't exist
  in the sandbox, and the confirmation "finds nothing to match the
  requested file" — exactly the BL-1066-class trap this prompt already
  documents, on a file that trap's original sweep didn't cover. Confirmed
  unrelated to BL-1742: `npx vitest run
  test/bl1007ContentionBudgetSmoke.test.js` passes standalone in the real
  (non-sandboxed) worktree, and this test file is untouched by this
  ticket's diff. Grepped `backlog/active` and `backlog/paused` for
  `confirmPoleAlone`/`recordTestDuration.js` — no ticket owns this; a
  `rule_proposal` is being sent to the specifier separately per Lessons
  Discipline. This blocks a Stryker dry run for ANY `--mutate` target on
  this host right now, not just this ticket's file — not something this
  parcel's own scope can fix.
- No process/sandbox leak from either attempt: both processes and their
  registered pgid/detached-job entry are confirmed gone
  (`pgrep -g 7173` empty after attempt 2's exit); attempt 1 exited on its
  own uncaught exception with no lingering children.

**Fallback per Hardening Order's "no tooling configured" clause (tooling
IS configured but is currently non-functional for this run) — a
hand-authored mutation check on the compiled JS, independent of the
coder's and architect's own identical `{}`-drop check:**

- Backed up `out/tools/telegram-front-desk-bot.js`. Mutated line 1616
  (`enqueueRoleAnswerNote`'s call to its `runHandoff` seam):
  `cwd: targetPath` → `cwd: process.cwd()` — a DIFFERENT mutant from the
  coder's/architect's `{}`-drop, chosen to confirm the new seam test's
  exact-value assertion (not merely presence) actually discriminates a
  wrong-but-present `cwd`.
- `npx vitest run test/telegramFrontDeskBotCli.test.js -t
  "BL-1518/BL-1742"`: **failed as expected** —
  `calls[0].opts.cwd` received the real worktree path instead of the
  fixture root, killing the mutant.
- Restored the compiled file (`git diff --stat
  out/tools/telegram-front-desk-bot.js` empty, `out/` is gitignored so
  this is a hygiene check only). Re-ran the same `-t` filter and the full
  file: green (276/276, 2.37s).
- Together with the coder's and architect's independently-run `{}`-drop
  mutant (both confirmed the same seam test fails), this covers both
  halves of the named mutant class (options object dropped entirely, and
  a wrong value substituted for one of its fields) without automated
  Stryker.

Checks re-run:
- `npx vitest run test/telegramFrontDeskBotCli.test.js`: 276/276, 2.37s
  (well under the 7000ms budget).
- `node specs/pipeline/cli.js specs/features/BL-1620-...feature`: 2/2 ok.
- `node specs/pipeline/cli.js specs/features/BL-1518-...feature`: 3/3 ok.
- Every standing whole-tree guard (this parcel touches
  `extension/test/`): `npx vitest run $(ls test/*Guard*.test.js | grep
  -v '\.property\.')` — 22 files, 211 tests, all green.

Not run: CRAP/DRY (no `src`/`out` file in Scope — test-only change),
`npm run test:properties` (2026-09-17 rule — no `*.property.test.js`
touched).

Fixture hygiene: `git status --short` clean of anything this pass's runs
could leak (pre-existing untracked BL-1652 scratch files in
`swarmforge/scripts/` predate this session and are not mine to remove —
surfaced, not swept).

By hardender.
