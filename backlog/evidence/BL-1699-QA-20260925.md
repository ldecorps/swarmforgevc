# BL-1699 — QA review pass, 2026-09-25

4 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: node tmp/bl1699-words.js  (loads swarmforge/scripts/prompt_engine_lib.bb, calls prompt-engine-lib/aider-bootstrap-text for coordinator/coder/QA with and without the two-pack coord-note, splits each text into whitespace words stripped of punctuation, and checks each word against every `git ls-files` path, every directory prefix of one, and every swarmforge/scripts basename; script in ## Detail)
- **Commit hash**: 3b4196a884
- **First error excerpt**: coordinator | file-path words: ["swarm"] | dir-path words: ["backlog","scripts"] | scripts-basename words: ["seat"]
coordinator +two-pack | file-path words: ["swarm"] | dir-path words: ["backlog/paused","backlog/active","backlog","scripts"] | scripts-basename words: ["seat"]
coder | [] | [] | []   QA | [] | [] | []   coder.note / generic.note | [] | [] | []
The text reads: '... Your job: inspect swarm and backlog state ... NEVER edit production code, tests, or infrastructure scripts ... this seat runs aider with --dry-run ...'. `swarm` is a tracked file at the repo root (the ./swarm launcher), and aider's own file-mention rule adds any reply word that exactly equals a tracked relative path. 8 packs run an aider coordinator (config coordinator_agent aider: qwen, glm, cerebras, ollama-qwen3, perplexity, ollama-qwen3-8b, ollama-qwen2.5-coder, ollama-mixed).
- **Failure class**: behavior
- **Expected vs observed**: requirement 2 / invariant 1: the bootstrap text for EVERY aider role names no path that exists in the repository and no swarmforge/scripts basename / observed: the coordinator branch names `swarm` (a tracked file), `seat` (swarmforge/scripts/seat), and the directories `backlog` and `scripts`; with the two-pack note it also names `backlog/paused` and `backlog/active`
- **Blamed role**: coder
- **Remediation pointer**: swarmforge/scripts/prompt_engine_lib.bb aider-bootstrap-text's coordinator branch: reword without repo paths or basenames (e.g. 'inspect pipeline and ticket state', 'this aider session'); keep the path-bearing two-pack sentence out of the :aider style or give aider a path-free variant. Then docs/how-to/BL-1697-local-parcel-driver.md's 'Bootstrap text names no path' holds for every role, which it currently does not

## D2

- **Failing command**: sed -n 40,80p specs/pipeline/steps/bl1699AiderSeatLaunchAndBootstrapSteps.js; sed -n 44,62p extension/test/bl1699AiderBootstrapNoRepoPaths.property.test.js
- **Commit hash**: 3b4196a884
- **First error excerpt**: Both predicates: `REPO_PATHS = git ls-files` (files only, never a directory) and `forbiddenHit` = `REPO_PATHS.find((p) => p.length >= 6 && text.includes(p))` || `CHECKED_BASENAMES.find(...)` where CHECKED_BASENAMES = scripts basenames `.filter((b) => b.length >= 6)`. `swarm` (5 chars), `seat` (4), `backlog`/`scripts`/`backlog/paused`/`backlog/active` (directories) are all invisible to it. So scenario 01's coordinator example and the invariant 1 property (which generates the coordinator role and the two-pack note) pass on the text D1 shows leaking. Non-vacuity is asserted only against a long embedded path.
- **Failure class**: unit
- **Expected vs observed**: the invariant 1 check fails on any repo path or scripts basename in the text, which is what the invariant says / observed: a length floor of 6 and a files-only path list make it pass on exactly the leaks in D1
- **Blamed role**: coder
- **Remediation pointer**: extension/test/bl1699AiderBootstrapNoRepoPaths.property.test.js and scenario 01's forbiddenHit in bl1699AiderSeatLaunchAndBootstrapSteps.js: match whole words (split on whitespace, strip punctuation, as aider's own mention rule does) against every tracked path, every directory prefix, and every swarmforge/scripts basename, with no length floor. Whole-word matching removes the substring false positives the floor was avoiding. Add non-vacuity cases for a short tracked path (`swarm`) and a short basename (`seat`)

## D3

- **Failing command**: sed -n 19,25p extension/test/bl1699AiderBootstrapNoRepoPaths.property.test.js; grep -n -i 'golden' swarmforge/scripts/test/test_aider_seat_launch_config.sh
- **Commit hash**: 3b4196a884
- **First error excerpt**: Header: 'Declared invariant 2 ... Encoded instead by test_aider_seat_launch_config.sh's golden-fixture comparison (BL-1699 evidence), which runs the real generator against real pack confs.' grep -i golden on that script: no match. It has no golden fixture or byte comparison; it only negative-greps one hand-written claude conf (item 12). The real encoding is scenario 06's regeneration against `git archive main` in the step handler (one synthetic conf, the specifier role), as the hardender's evidence says.
- **Failure class**: behavior
- **Expected vs observed**: the recorded disposition of invariant 2 names where it is actually encoded / observed: it names a golden-fixture comparison that does not exist, in a file that has none. QA checked the invariant directly (## Detail): 39 packs, 163 non-aider launch scripts and 53 agent/role bootstrap texts byte-identical to origin/main, so the invariant holds and only the pointer is wrong
- **Blamed role**: coder
- **Remediation pointer**: the invariant-2 paragraph of extension/test/bl1699AiderBootstrapNoRepoPaths.property.test.js's header: name scenario 06's handler (bl1699AiderSeatLaunchAndBootstrapSteps.js, 'both are byte-identical ...') and its actual scope

## D4

- **Failing command**: grep -n 'SEAT_TICKET\|SEAT_ACCEPTANCE\|driver record\|test-scope' docs/reference/BL-1696-seat-command-vocabulary.md; sed -n 45p docs/reference/BL-1696-seat-command-vocabulary.md
- **Commit hash**: 3b4196a884
- **First error excerpt**: No match for the fallback. Line 45: '| `test` | the live pack's declared `seat_test_command` (...); refuses if none is configured | passthrough, or 2 if unconfigured |'. BL-1699 made `seat test` (swarmforge/scripts/seat) read SEAT_TICKET/SEAT_ACCEPTANCE from the running role's driver record via `local_parcel_driver_cli.bb test-scope` whenever both are unset. The reference diff only changed its out-of-scope paragraph.
- **Failure class**: behavior
- **Expected vs observed**: the seat verb reference describes what `seat test` does, including the new scope fallback / observed: the `test` row is unchanged and only the BL-1697 how-to mentions the fallback
- **Blamed role**: documenter
- **Remediation pointer**: docs/reference/BL-1696-seat-command-vocabulary.md `test` row (or a note under it): with SEAT_TICKET and SEAT_ACCEPTANCE both unset, `seat test` scopes from this seat's driver record (`local_parcel_driver_cli.bb test-scope`), and runs unscoped when there is no record

By QA.

## Detail

Parcel: documenter e478c2e7f7, merged into QA as 3b4196a884 (the commit tested).
No prior BL-1699 bounce in the master bounce store. `qa-sibling-check.js status`: VERIFY BL-1699.
No BL-1699 row in standing-reds.tsv, property_suite_standing_allowlist.tsv or suite-poles.tsv.

Merge note: the parcel was built on BL-1698's content, which QA bounced and
reverted in 01c5595b84. Three files conflicted: local_parcel_driver_cli.bb,
docs/index.md and docs/how-to/BL-1697-local-parcel-driver.md. They were resolved
by replaying only BL-1699's own delta onto QA's tree: the `test-scope` verb, its
index clause and its how-to acceptance line. BL-1698's content stays out.
Consequence to keep in mind, not an item: `driver-test-scope`'s docstring mentions
"resume/hold" and "pre-merge/mechanical-mail phases", which are BL-1698 concepts
not on this lineage until BL-1698 lands.

Gates (every one RUN, none BLOCKED; qa-gather.js report, one run each):
- stragglers before/after: none from this repo.
- register: 6 rows, all owned, unowned [].
- pre_qa_gate.sh BL-1699 3b4196a884 (required_wiring): OK.
- npm test: exit 0. npm run test:properties: exit 0.
- qa_e2e 1: BL-1699 feature 10/10 pass. It is green but blind to D1 (see D2).
- qa_e2e 2: test_aider_seat_launch_config.sh: ALL PASS.
- qa_e2e 3: generated the ollama-qwen2.5-coder-mono-router coder seat with the
  real swarmforge.sh against that pack conf. Aider line: `... --yes-always
  --no-detect-urls --read '<ROOT>/swarmforge/roles/aider/coder.note' --test-cmd
  'swarmforge/scripts/seat test' --auto-test --model-settings-file ...`, so
  --read coder.note, --test-cmd and --auto-test are all present. Bootstrap steps
  `[{:op :sleep, :ms 5000} {:op :paste-file, :path nil} {:op :submit}]` (no /add;
  :path nil is the no-prompt-file default). The coder's bootstrap text names no
  ready_for_next, no handoff-draft and no swarmforge/. The coordinator's text is D1.
- qa_e2e 4: the parcel's own paths are the Scope files, tests, docs and evidence,
  plus swarmforge/scripts/local_parcel_driver_lib.bb (driver-test-scope, 13 lines)
  and local_parcel_driver_cli.bb (the test-scope verb). The ticket lists the
  driver as out of scope, but requirement 4 needs `seat test` to read the driver
  record, so a reader beside the record's own writer is accepted and not an item.
  There is no golden fixture: the coder regenerates against `main` in scenario 06
  instead. That is direction, not mandate, so it is accepted; the false pointer to
  it is D3.
- Article 4.5 changed paths, all RUN and green: test_aider_seat_launch_config.sh,
  test_alternate_runtime_launch.sh, test_local_model_seat.sh,
  agent_runtime_test_runner.bb, test_agent_runtime_lib.sh, prompt_engine_test_runner.bb,
  bl1052_local_model_seat_property_runner.bb, BL-1696 feature 16/16 (seat), and
  BL-1697 feature 12/12 (driver lib/cli).
- Invariant 2, checked directly: every swarmforge/packs/*.conf (39), every
  non-aider window role, launch script from HEAD's swarmforge.sh vs origin/main's
  (git archive), script dir normalized, same fixture root: 163 comparisons, 0 diffs.
  bootstrap-steps per agent/role, HEAD vs origin/main agent_runtime_lib.bb: 53
  comparisons, 0 diffs. A first run showed 15 "diffs" that were a harness artifact:
  the fixture lacked tester/art-director role prompts, so neither side wrote a
  script. Fixed and re-run: 0.
- Role notes vs standing_rule_violations_files.bb: it scans roles/ for .prompt only,
  so the .note files are outside it, as the ticket directs.
- Invariant 1: the coder/QA/generic texts and both notes are clean under whole-word
  matching. The coordinator is not (D1).

Routing: every item except D4 is the coder's, and D4 (documenter) is later in the
chain, so one bounce goes to the coder and the inventory travels with it.

### Script for D1: tmp/bl1699-words.js (run from the worktree root)

```js
const {execFileSync}=require('child_process');
const path=require('path');
const files=execFileSync('git',['ls-files'],{encoding:'utf8'}).split('\n').filter(Boolean);
const fileSet=new Set(files);
const dirs=new Set(); for (const f of files){ const parts=f.split('/'); for(let i=1;i<parts.length;i++) dirs.add(parts.slice(0,i).join('/')); }
const scriptBase=new Set(execFileSync('git',['ls-files','swarmforge/scripts'],{encoding:'utf8'}).split('\n').filter(Boolean).map(p=>path.basename(p)));
function text(role, note){ return execFileSync('bb',['-e',`(load-file "swarmforge/scripts/prompt_engine_lib.bb") (print (prompt-engine-lib/aider-bootstrap-text ${JSON.stringify(role)} ${note===null?'nil':JSON.stringify(note)}))`],{encoding:'utf8'}); }
const twoPack=' This pack has no specifier: promote items from backlog/paused into backlog/active (respect active_backlog_max_depth), then send task handoffs directly to coder.';
const cases=[['coordinator',null],['coordinator',twoPack],['coder',null],['QA',null]];
const fs=require('fs');
for (const [r,n] of cases) report(`${r}${n?' +two-pack':''}`, text(r,n));
for (const f of ['swarmforge/roles/aider/coder.note','swarmforge/roles/aider/generic.note']) report(f, fs.readFileSync(f,'utf8'));
function report(label,t){
  const words=[...new Set(t.split(/\s+/).map(w=>w.replace(/^[`'"(]+|[`'",.!;:?)]+$/g,'')).filter(Boolean))];
  const fileHits=words.filter(w=>fileSet.has(w));
  const dirHits=words.filter(w=>dirs.has(w));
  const baseHits=words.filter(w=>scriptBase.has(w));
  console.log(label,'| file-path words:',JSON.stringify(fileHits),'| dir-path words:',JSON.stringify(dirHits),'| scripts-basename words:',JSON.stringify(baseHits));
}
```

### Script for the invariant 2 sweep: tmp/bl1699-inv2.js (needs `git archive origin/main swarmforge/scripts | tar -x -C tmp/bl1699-inv2` first)

```js
const fs=require('fs'), path=require('path'), {execFileSync, spawnSync}=require('child_process');
const REPO=process.cwd();
const HEAD_SCRIPTS=path.join(REPO,'swarmforge','scripts');
const MAIN_SCRIPTS=path.join(REPO,'tmp','bl1699-inv2','swarmforge','scripts');
const INDEX_OF_ROLE=`index_of_role() { local target="$1" i; for (( i = 1; i <= \${#ROLES[@]}; i++ )); do [[ "\${ROLES[$i]}" == "$target" ]] && { echo "$i"; return; }; done; }`;
function gen(root, sh, role){
  const r=spawnSync('zsh',['-f','-c',`source '${sh}' '${root}'; parse_config; ${INDEX_OF_ROLE}; write_role_launch_script "$(index_of_role ${role})"`],{encoding:'utf8',env:{...process.env,PACK_STAFFING_SKIP_GATE:'1'}});
  const f=path.join(root,'.swarmforge','launch',`${role}.sh`);
  const out=fs.existsSync(f)?fs.readFileSync(f,'utf8'):`<<no script; exit ${r.status}; ${r.stderr.slice(0,200)}>>`;
  try{fs.rmSync(f)}catch{}
  if (out.startsWith("<<no script")) globalThis.noScript=(globalThis.noScript||0)+1;
  return out.split(HEAD_SCRIPTS).join('<S>').split(MAIN_SCRIPTS).join('<S>');
}
function boot(lib, agent, role){
  const r=spawnSync('bb',['-e',`(load-file "${lib}") (println (pr-str (agent-runtime-lib/bootstrap-steps "${agent}" "${role}")))`],{encoding:'utf8'});
  return (r.stdout||'')+(r.status?`<<exit ${r.status}>>`:'');
}
const packs=fs.readdirSync('swarmforge/packs').filter(f=>f.endsWith('.conf'));
let compared=0, diffs=[], bootCache=new Map();
for (const p of packs){
  const conf=fs.readFileSync(path.join('swarmforge/packs',p),'utf8');
  const roles=[];
  for (const line of conf.split('\n')){ const m=line.match(/^window\s+(\S+)\s+(\S+)/); if(m) roles.push([m[1],m[2]]); }
  const root=fs.mkdtempSync(path.join(REPO,'tmp','bl1699-inv2-root-'));
  fs.mkdirSync(path.join(root,'swarmforge','roles'),{recursive:true}); fs.mkdirSync(path.join(root,'.swarmforge','launch'),{recursive:true});
  fs.writeFileSync(path.join(root,'swarmforge','constitution.prompt'),'');
  for (const r of ['coder','cleaner','architect','hardender','documenter','QA','coordinator','specifier','tester','art-director']) fs.writeFileSync(path.join(root,'swarmforge','roles',`${r}.prompt`),'role prompt\n');
  fs.writeFileSync(path.join(root,'swarmforge','swarmforge.conf'),conf);
  for (const [role,agent] of roles){
    if (agent==='aider') continue;
    const b=gen(root,path.join(MAIN_SCRIPTS,"swarmforge.sh"),role), a=gen(root,path.join(HEAD_SCRIPTS,"swarmforge.sh"),role); if(a!==b && !globalThis.shown){globalThis.shown=1; fs.writeFileSync("tmp/bl1699-a.sh",a); fs.writeFileSync("tmp/bl1699-b.sh",b);}
    compared++;
    if (a!==b) diffs.push(`${p} ${role}/${agent} LAUNCH differs`);
    const key=`${agent}/${role}`;
    if(!bootCache.has(key)){ const x=boot(path.join(HEAD_SCRIPTS,'agent_runtime_lib.bb'),agent,role), y=boot(path.join(MAIN_SCRIPTS,'agent_runtime_lib.bb'),agent,role); bootCache.set(key,x===y); if(x!==y) diffs.push(`${key} BOOTSTRAP differs`); }
  }
  fs.rmSync(root,{recursive:true,force:true});
}
console.log('packs',packs.length,'non-aider launch comparisons',compared,'bootstrap (agent/role) comparisons',bootCache.size);
console.log('diffs',diffs.length,'no-script generations',globalThis.noScript||0); diffs.slice(0,30).forEach(d=>console.log(' ',d));
```
