# BL-1704 — architect review pass, 2026-09-25

1 defect(s) found. One bounce, complete inventory (Article 4.4).

## D1

- **Failing command**: empirical repro — sourced
  `swarmforge/scripts/stop_ancillary_services.sh`, stubbed
  `ollama_ancillary_stop_swarm_owned` to `return 1` (simulating its own
  documented failure case: "the server (pid $pid) or a runner child did
  not stop cleanly"), then ran `stop_ancillary_services_main` under
  `set -euo pipefail` exactly as the run-directly guard at the bottom of
  the file does.
- **Commit hash**: d274c8ff02
- **First error excerpt**:
  ```
  ...
  2026-09-25T19:06:05Z stopping vscode tunnel
  STUB: ollama stop reporting failure
  exit=1
  ```
  `log "stop_ancillary_services done"` (the line immediately after the new
  `ollama_ancillary_stop_swarm_owned` call) never runs; the script exits 1
  from inside `stop_ancillary_services_main` itself.
- **Failure class**: behavior
- **Expected vs observed**: the full-stack stop's own contract (docstring:
  "Idempotent — safe when nothing is running"; every prior component call
  in the same function is effectively best-effort) and the ticket's own
  precedent in this exact file — `ollama_ancillary_ensure_ready_for_launch`
  guards its own call to `ollama_ancillary_stop_pid` with `|| true` and
  documents why in a comment right above it: "Under a caller's `set -e` an
  unguarded call here would abort right at this line, silently skipping
  the diagnostic... and this function's own `return 1`" / observed: the
  NEW call this ticket adds at `stop_ancillary_services.sh`'s
  `stop_ancillary_services_main` (`ollama_ancillary_stop_swarm_owned
  "$ROOT/.swarmforge"`, no `|| true`) has exactly the failure mode that
  comment warns about — when a swarm-owned ollama server or runner child
  survives TERM+KILL (a real, named, documented return path of
  `ollama_ancillary_stop_pid`/`ollama_ancillary_stop_swarm_owned`), the
  full-stack stop aborts mid-function under `set -euo pipefail` (the
  run-directly guard at this file's own bottom), silently skipping its own
  `log "stop_ancillary_services done"` line and returning a fresh nonzero
  exit from a script that was unconditional before this ticket. The
  sibling call site in `kill_all_swarm.sh` DOES have `|| true` — only this
  one is missing it.
- **Blamed role**: coder
- **Remediation pointer**: `swarmforge/scripts/stop_ancillary_services.sh`,
  `stop_ancillary_services_main`: change
  `ollama_ancillary_stop_swarm_owned "$ROOT/.swarmforge"` to
  `ollama_ancillary_stop_swarm_owned "$ROOT/.swarmforge" || true` (matching
  `kill_all_swarm.sh`'s own call and the file's own BL-1727 precedent), and
  add a regression test/scenario that stubs or forces
  `ollama_ancillary_stop_swarm_owned` (or a stuck stand-in process that
  survives TERM+KILL) to fail and asserts `stop_ancillary_services_main`
  still reaches its own `log "...done"` line / still runs the rest of its
  own body — the current feature only covers the three success/leave/clear
  paths (scenarios 01-03), never the documented failure return.

By architect.

## Detail

- Dependency gate (`extension/out/tools/dependency-gate.js`) on the two new
  JS files: PASSED, no forbidden edges.
- Co-change report: no anomalous coupling beyond this ticket's own files
  and BL-1703/BL-1727's known-related history on `ollama_ancillary_lib.sh`.
- Read `ollama_ancillary_lib.sh`, `kill_all_swarm.sh`,
  `stop_ancillary_services.sh` in full against the declared invariant ("a
  stop path signals an ollama process only when...") — the signalling
  logic itself (owner check, live-cmdline check, runner-children via
  `pgrep -P` never a host-wide sweep) is correct and matches BL-1385/1390's
  own precedent.
- Ran the acceptance feature (5/5 pass, no flake this run) and the
  standing shell suites (`test_ollama_ancillary_stop_pid.sh`,
  `test_stop_ancillary_services_onboarder_dual_clear.sh`,
  `test_stop_ancillary_services_tunnel_orphan_reap.sh`) — all green,
  confirming the bug only manifests on the undocumented failure branch
  none of them exercise.
- Cleaner's own D1 (scenario 03's intermittent runner-liveness flake,
  reproduced independent of any BL-1704 code) read and accepted as
  environment, not a ticket defect — did not reproduce in this pass's own
  5/5 acceptance run; no action needed here.
- `git diff main...HEAD --name-only` (this lineage) still names only the
  ticket's Scope files, tests, docs and evidence.

Routing: coder (the wiring, not the stop-contract logic itself, which is
correct) — one item, one bounce (Article 4.4).
