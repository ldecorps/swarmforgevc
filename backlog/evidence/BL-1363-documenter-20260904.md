# BL-1363 — documenter pass (2026-09-04)

## Received
`7f483badf0` (hardener; per `backlog/evidence/BL-1363-hardener-pass-20260904.md`).

## Doc-domain review
Checked for an existing doc home: `promote_and_route_next.sh` — the
mirror this ticket's own script is explicitly built to match — has no
dedicated how-to or reference page either, only tangential mentions
inside gate-specific docs. Same precedent as `record-bounce.js` and
`record-review-evidence.js` (BL-1362): an internal coordinator-tooling
CLI documented in Specification.MD only, no dedicated page manufactured.
- `docs/reference/Specification.MD`: new top-of-stack entry covering the
  script's CLI shape, the integrity-guard reuse and rollback behavior,
  the milestone-destination rule, and the human's ruling on the 665
  existing loose files (left alone).
- `docs/index.md`: no line to add — no how-to page.
- Diagram registry checked — no diagram depicts backlog-close internals.

## Verdict
Doc content added (Specification.MD entry only, matching sibling-tool
precedent). Forwarding to QA.
