# Evidence — BL-1444 — coder pass (2026-09-06)

Role: coder. Ticket: BL-1444-the-art-directors-tip-lands-on-main-by-qa.
Routed by coordinator note `20260906T113612Z_006248`.

## What was implemented

1. `swarmforge/scripts/check_art_director_tip.sh` — hook mode (no args,
   resolves the incoming merge parent via `incoming_merge_parent_lib.sh`,
   skips without judging unless the commit is reachable from
   `primary/art-director` and NOT already reachable from the landed main),
   direct mode (`--tip <sha>`, prints `ART_DIRECTOR_TIP_OK` /
   `ART_DIRECTOR_TIP_REFUSED`, refuses a commit not on
   `primary/art-director` before judging any path), and `--print-lane`
   (the `--list-paths` shape of `check_pipeline_code_on_main.sh`). Lane:
   `docs/design/` prefix, or a `backlog/evidence/` file whose basename
   contains `art-director`. Provenance exemption: the tip's own last commit
   touching a path, checked for ancestry of the landed main
   (`origin/main` else `main`) — the BL-1096 per-path shape.
2. Wired into `swarmforge/git-hooks/pre-merge-commit`'s chain, after
   `check_feature_handler_registration.sh`. Verified NOT present in
   `run_commit_guards.sh` (out of scope, own shell-suite check).
3. `specs/pipeline/steps/bl1444ArtDirectorTipLandsSteps.js` — the
   `mkFixtureRepo` shape from `bl632CommitTimeGuardSteps.js`, guard set
   derived from `pre-merge-commit`'s own `run_guard` lines only (narrower
   than the BL-1398/BL-1401 default, since this feature never exercises
   `git commit`). All **13/13** scenario runs pass.
4. `swarmforge/scripts/test/test_bl1444_art_director_tip_guard.sh` — 10
   checks (print-lane, 3 direct-mode, 4 hook-mode via `git merge --no-ff
   --no-commit` for a real MERGE_HEAD with no hook installation needed,
   2 wiring). Registered in `suite-manifest.tsv` (`bb suite_inventory_cli.bb`
   agrees: tree and manifest in sync).
5. `extension/test/bl1444ArtDirectorTipGuardInvariants.property.test.js` —
   coder-authored, one test per declared invariant (BL-654):
   - inv 1 (verdict is a function of git objects only): varies
     `SWARMFORGE_ROLE`, the checked-out branch NAME (same commit), an
     unrelated dirty working-tree file, and git identity/`$USER`, asserting
     the verdict is byte-identical to a baseline in every case.
   - inv 2 (hook mode judges only an unmerged art-director-side commit):
     three generated shapes (not-on-art-director, reachable-from-main,
     fresh-on-art-director) with randomized lane/out-of-lane path mixes;
     the first two must always skip (exit 0) regardless of content, the
     third must agree with direct mode's own verdict for the identical tip.
   - inv 3 (reads only): snapshots refs/status/remotes/HEAD/MERGE_HEAD
     before and after (both modes), asserts byte-identical; a bogus
     unreachable `origin` remote is configured so a fetch/push attempt
     would fail loudly.
   All three confirmed **non-vacuous**: each was run against a deliberately
   broken guard (inv 1: made `--tip` short-circuit OK when
   `SWARMFORGE_ROLE=QA`; inv 2: removed the "not reachable from
   primary/art-director" skip; inv 3: added a stray file write) and failed
   before the break was reverted.

## Amendment 2026-09-06 (after my own unowned-red note, commit `b47ae7d50f`)

The specifier amended the ticket while I held the parcel (before cleaner
picked it up — confirmed via `.swarmforge/handoffs` state, no `in_process`
entry on the cleaner side): two more hand-listed merge-chain fixtures needed
`check_art_director_tip.sh` added, and my `unowned-red` note on
`test_pipeline_code_on_main_guard.sh` was answered by BL-1408's register row,
not by this parcel. Both are **hand edits, never derived here** (BL-1408
owns the derivation) — sixth and seventh hand edits of the chain list,
continuing BL-1440's fifth (`backlog/evidence/closing-ceremony-2026-09-06-specifier.md`):

- **Sixth**: `swarmforge/scripts/test/test_pre_merge_commit_hook.sh` —
  `MERGE_GUARDS` gains `check_art_director_tip.sh`, one comment line
  (`# check_art_director_tip.sh (BL-1444): hand edit; BL-1408 derives this
  set`). All **9/9** cases still pass — the fixture's `write_stub` is
  guard-agnostic, and no case asserts an exact guard count.
- **Seventh**: `swarmforge/scripts/test/test_pipeline_code_on_main_guard.sh`
  — one `cp` line beside `check_feature_handler_registration.sh`'s, same
  comment shape. Verified: the missing-guard 127 noise for
  `check_art_director_tip.sh` is GONE from the output; the suite still fails
  at case 02 exactly as on `main`, naming only the four guards BL-1408 owns
  (`check_handler_module_graph.sh`, `check_bb_scripts_load.sh`,
  `check_standing_red_register.sh`, `check_constitution_doc_citations.sh`) —
  not made worse, not made green (out of scope, per the amendment).

Neither fixture's guard set was derived here — that collision with BL-1408's
own deliverable is exactly what the amendment forbids.

## Checks run this pass

- Acceptance: `specs/features/BL-1444-...-lands-on-main-by-qa.feature` —
  13/13. Siblings re-verified green after the hook edit:
  `bl632CommitTimeGuardInvariants.property.test.js` (2/2),
  `specs/features/BL-1303-a-feature-on-main-always-has-a-registered-handler.feature`
  (7/7), `specs/features/BL-1385-a-handler-that-cannot-load-never-reaches-main.feature`
  (13/13).
- `test_bl1444_art_director_tip_guard.sh` — 10/10.
- `test_pre_merge_commit_hook.sh` — 9/9 (post-amendment).
- `test_pipeline_code_on_main_guard.sh` — red at case 02, unchanged from
  `main`, BL-1408's row (post-amendment); confirmed pre-existing and
  unrelated to this diff before minting the `unowned-red` note (stashed my
  own new files, applied, re-ran on the clean tracked-file diff — same
  failure, same guards named).
- Property suite (full `npm run test:properties`, 363 files): green except
  two confirmed load flakes unrelated to this ticket
  (`bl1253TokenOwnershipInvariants`, `meanTicketTimeCost` — both re-passed
  alone, `property-suite-guard`'s own BL-1407 flake-detection said so) and
  one genuine catch in my OWN new step handler on the first attempt
  (`bl968MaterializedGuardSensitivity`): `deriveCommitGuardFixtureSet` was
  called at module load time, which is exactly the load-time-binding hazard
  that guard exists to catch. Fixed by making it lazy (computed inside
  `mkFixtureRepo`, memoized) — matches `bl632CommitTimeGuardSteps.js`'s own
  convention, which I had drifted from. Re-ran `bl968...` alone (green,
  148s) and the full suite again (green on this file) before committing.
- `bb suite_inventory_cli.bb` — ok, 497 files, 493 standing, 4 excluded.

## Process note

The `test_pipeline_code_on_main_guard.sh` case-02 red is real, pre-existing,
and not mine to fix (BL-1408's scope explicitly excluded this file at mint
time — its author's note says so — even though the file suffers the exact
same hand-list-staleness defect BL-1408 targets elsewhere). Sent a priority
`00` `unowned-red` note to specifier+coordinator per the standing-red rule
before continuing; the amendment above is the specifier's response.

## Scope

Nothing in `out_of_scope` was touched: no `run_commit_guards.sh` entry for
`check_art_director_tip.sh`, no attempt to make
`test_pipeline_code_on_main_guard.sh` green. Documenter's diagram/how-to
work is untouched (own stage). `extension/docs/briefings/2026-09-04.json`
remains untracked and was not created by this parcel — surfaced, not swept.
