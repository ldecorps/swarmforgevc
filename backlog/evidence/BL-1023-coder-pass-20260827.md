# BL-1023 — coder pass

Commit: this parcel · 2026-08-27 · worktree `swarmforge-coder`

## Context

Re-promoted coordinator route (`b969b541d`). Implementation landed through QA
(`bf77b76082`). Verifies expeditor refuses or adopts ticket before stages spend,
and `move-ticket!` cannot silently no-op.

## Invariant (BL-654)

Expeditor never reports success with unchanged backlog state — property encoded
in acceptance + `expedite_cli.bb` initiation guard.

## Verification

| check | result |
|---|---|
| `run_acceptance.sh BL-1023…feature` | **6/6** |

By coder.
