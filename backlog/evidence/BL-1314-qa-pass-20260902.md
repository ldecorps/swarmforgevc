# BL-1314 — QA pass

Date: 2026-09-02 · Verdict: **PASS — approved**

## Received

Merged documenter commit `5ee6613e78` (clean sweep, no defect) onto QA's tip
as `6bebb929a2`. Ancestry confirmed: `5a45f95bc1` (coder) → `0f659c6ace`
(cleaner merge) → `649dc596f1` (cleaner) → `5626aa723b` (architect merge) →
`e4c06a5565` (architect) → `f5a08bae94` (hardener merge) → `e301688d42`
(hardener) → `e8ae40335f` (documenter merge) → `5ee6613e78` (documenter) — all
confirmed ancestors of `6bebb929a2` via `git merge-base --is-ancestor`.

No prior bounce history for BL-1314 on `origin/main` (`main`==`origin/main`,
0/0 divergence) — this is the ticket's first QA pass.

## qa_e2e_procedure — every step run, independently (not taken on prior passes' word)

1. `bash swarmforge/scripts/test/test_pipeline_code_on_main_guard.sh` →
   **ALL PASS**, including `PASS: BL-925 invariant2-one-shared-definition:
   both the bash guard and handoffd.bb call is_qa_ancestor.sh, not a second
   independent ancestry check`.
2–5. Covered executably by `test_invariant2_qa_definition_lib.sh` (below)
   rather than by hand-mutating the live tree a second time — the coder's own
   evidence already ran the live-tree negatives (2–5) and reverted them
   cleanly; re-running the fixture-driven equivalents is the honest
   independent check here.
6. `node specs/pipeline/cli.js
   specs/features/BL-1314-invariant-two-assertion-scoped-to-the-qa-question.feature`
   → **6/6 pass**, no "no step handler matched".

## Additional gates

- `bash swarmforge/scripts/test/test_invariant2_qa_definition_lib.sh` →
  **9/9 PASS** (all named cases, including `08 complete-inventory` and
  `03 third-question`, the regression this ticket exists for).
- Property test, scoped: `npx vitest run --config vitest.properties.config.mjs
  test/bl1314InvariantTwoQaQuestionInvariants.property.test.js` → **3/3 pass**.
- Property test, full suite: `npm run test:properties` → BL-1314's own file
  passes clean (`✓ test/bl1314InvariantTwoQaQuestionInvariants.property.test.js
  (3 tests)`); 25 other files fail, **none touched by this diff**. Cross-checked
  by basename against `backlog/evidence/specifier-disposition-qa-standing-red-note-20260828.md`
  (BL-1206/BL-1220/BL-1221/BL-1247, corroborated ~45 property-lane failures
  2026-08-28) — 14 of the 25 match the `require('node:test')` collection defect
  exactly (`alertTelemetry`, `bl735PilotAcceptanceExecution`,
  `bl1146HostQueueEnqueueNext`, `bl1147ProbeLegacyTopicAdoption`,
  `crossFileDuplicationCheck`, `bl782LivenessProbesScopedToRoot`,
  `pilotScopedCrapEvidence`, `shellEntryPointDriveCheck`, `pilotAcceptanceGate`,
  `bl1150OutageFailoverCliLoadFileSafe`, `bl669OutageFailoverSteward`,
  `bl1200FixtureGitWritesStayInOwnRepo`, `bl733ProducerCrosscheck`,
  `resolveMutationConcurrency`); the stamp-off failures
  (`bl1116ExtensionWipHotfixStampOff`, `bl1117PipelineBoardNumericNbspStampOff`,
  `bl1113CursorHotfixStampOff`, `bl1115MainSyncStatusCliStampOff`,
  `bl1136BabysitterdCursorForgeStampOff`) assert live `hotfix-ledger.yaml` rows
  against a `state:` value pinned at spec time (`pending`) that has since
  legitimately transitioned (`awaiting-human` is a real ledger state, confirmed
  present in `backlog/hotfix-ledger.yaml` and named in
  `backlog/evidence/BL-1136-hardener-pass-rematch3-20260825.md`) — live-data
  drift, not a regression this parcel caused. Per BL-1063, reported as
  already-disposed standing debt, not re-ticketed.
- `npm test` (unit lane) → **15 failed / 570 passed files, 25 failed / 9823
  passed tests** — byte-identical in count to the pre-existing baseline the
  coder independently measured and recorded in
  `backlog/evidence/BL-1324-coder-stampoff-4ed88430b2-20260902.md`
  ("`npm test` on the received tip is 25 failed / 9823 passed across 15
  files… same 25 failures, same files, same counts"). No new red. Matches the
  `TypeError: deps.checkOrphanedAuthoredDocs is not a function` signature
  dispositioned as BL-1221.
- Article 4.5 (changed-path unit test gate): the one genuinely production path
  this ticket touches, `swarmforge/scripts/invariant2_qa_definition_lib.sh`,
  has its own dedicated unit suite (`test_invariant2_qa_definition_lib.sh`,
  9/9 above) and is confirmed wired — sourced at
  `test_pipeline_code_on_main_guard.sh:393`
  (`source "$SCRIPT_DIR/../invariant2_qa_definition_lib.sh"`), not merely
  present unreferenced.
- required_wiring anchors re-confirmed present: `suite-manifest.tsv:316`
  (`test_invariant2_qa_definition_lib.sh`, `standing` lane) and
  `specs/pipeline/steps/index.js:920`
  (`require('./bl1314InvariantTwoQaQuestionSteps')`).
- Doc currency: `docs/reference/BL-632-commit-time-guard-refuses-pipeline-code-on-main.md`'s
  invariant-2 description ("both `check_pipeline_code_on_main.sh` and
  `handoffd.bb` call `is_qa_ancestor.sh` rather than a second inline `git
  merge-base --is-ancestor`") remains accurate post-fix; it never claimed the
  old over-broad matching. No diagram change-trigger fired.
- Orphaned process check: `pgrep -fl 'node --test|stryker'` clean before and
  after every run (matches were the checking command's own shell, verified by
  PID lookup).

## BL-1241 — entangled sibling detected and handled

`6bebb929a2` carries `BL-1324`'s own in-flight coder commits as ancestors
(disclosed by the coder in `BL-1314-coder-pass-20260902.md`: a real defect
found in BL-1324's invariant-2 property while holding this ticket, fixed and
committed separately as `4c7bd1331a`, attributed to BL-1324, riding this
chain because BL-1324 had already been forwarded to its own cleaner). BL-1324
is NOT itself landable (mid-pipeline, not QA-approved) — step 1 of the BL-1241
remedy does not apply.

Ran `bb swarmforge/scripts/land_step_cli.bb
BL-1314-invariant-two-assertion-scoped-to-the-qa-question 6bebb929a2`:

```
LAND_REPLAY land-replay/BL-1314-6bebb929a2 0ac9bb4399c1572384945e45bcad2f9289ef22e1
ENTANGLED_SIBLING BL-1324
```

Reviewed the replayed tip `0ac9bb4399c1572384945e45bcad2f9289ef22e1`: parent
is `origin/main`'s own tip (`6097b4040a`); its diff contains exactly the 12
paths this ticket's own coder→documenter chain touched (evidence files +
`invariant2_qa_definition_lib.sh` + its unit test + the guard test edit +
manifest entry + the property test + the step handler + its registration) —
no BL-1324, BL-1298, BL-1301, or BL-472 content. Content-diffed each of those
paths between the replay and `6bebb929a2`: **byte-identical** to what was
verified above, so no re-run was needed beyond the diff check.

**Landing `0ac9bb4399c1572384945e45bcad2f9289ef22e1`, not `6bebb929a2`.**
Recording `abandoned_commits: [6bebb929a2938dda2b132114993e3d86e44b04f2]` on
the ticket per the read-abandoned-commits contract
(`swarmforge/backlog-schema.md`), so `task_scope_gate_lib.bb` and
`pre_qa_gate_lib.bb` re-base their walk on `origin/main` for this ticket going
forward rather than treating the abandoned commit's descent as live.

## Verdict

APPROVED. Landing the tip-pure replay commit; broadcasting merge-up to
worktree roles against the ORIGINAL approved commit `6bebb929a2` (the state
every worktree role's own branch should reconcile to — the replay is a
landing-only reconstruction, not a new state for role branches to merge);
notifying coordinator for backlog bookkeeping.

By QA.
