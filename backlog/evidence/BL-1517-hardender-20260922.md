# BL-1517 — hardender review pass, 2026-09-22

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No extension/src or extension/out changes (project_root_arg_lib.bb has no
mutation/CRAP/DRY wired, BL-472). Verified:
- `bb swarmforge/scripts/test/project_root_arg_lib_test_runner.bb`: ok.
- `specs/pipeline/scripts/run_acceptance.sh` on the ticket's feature:
  11/11 ok.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1517ProjectRootArgInvariants.property.test.js`: 3/3 pass.
- `npx vitest run test/bl1517HarnessMissingFixtureRootGuard.test.js`: 1/1
  pass.
- Standing whole-tree guard suite (22 files, 211 tests, since this
  parcel touches specs/pipeline/steps/ and swarmforge/scripts/test/):
  all green.

Hand-authored mutation sweep on `project_root_arg_lib.bb` (Babashka
fallback, BL-567 pattern), each applied, run, restored:
1. `check-root`'s `(str/starts-with? arg "-")` -> `"--"`: killed by
   the lib test runner's "a bare-dash arg refuses :flag-shaped" case.
2. `check-root`'s `(= strictness :repository)` -> `(= strictness
   :directory)` in the not-a-repository branch: killed by "a plain
   existing directory passes :directory strictness".
3. `binds-project-root-from-argv?`'s `and` -> `or` (the scenario-06
   enumeration discriminator): killed by
   `bl1517HarnessMissingFixtureRootGuard.test.js` - the mutant widens
   the enumerated set to any .bb file merely mentioning
   `*command-line-args*` anywhere, so the test's real subprocess sweep
   over the widened set failed after 270s (host contention from a
   concurrent worktree's own property-lane run, visible in the process
   table throughout - `sh -c npm run compile && vitest run --config
   vitest.properties.config.mjs` alongside this run). A crash/timeout
   under a real widened-population run is still a kill, not
   inconclusive - the original file's narrow `and` is the reason the
   real run finishes in ~2s.

No orphaned processes or leaked fixture dirs after the mutant run
(confirmed against the live swarm daemon process list - all long-running
supervisors, none matching the mutation probe's pid or start time; `find
/tmp -maxdepth 1 -name 'bl1517-missing-root-check-*'` empty).

By hardender.
