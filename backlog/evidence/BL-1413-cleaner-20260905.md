# BL-1413 — cleaner pass, 2026-09-05

Clean sweep. No changes needed; forwarding as-is.

## Checks run

| check | result |
|---|---|
| `sh -n daemon_log_freshness_check.sh` | clean |
| `bash swarmforge/scripts/test/test_daemon_log_freshness.sh` | all BL-1413/BL-1011/BL-1012/BL-1110/BL-784 checks pass; four pre-existing unrelated reds (BL-796-01/02/03 nvm/node PATH resolution, `fleet-telegram: default announce invoked curl`) reproduced and confirmed environmental — nothing under `daemon_log_freshness_check.sh`'s changed function |
| `bb .../bl1413_freshness_nul_byte_property_runner.bb` | 40/40 runs, ALL PROPERTIES HOLD |
| acceptance `BL-1413-...feature` | 4/4 |
| `required_wiring` anchor `grep -aE` | resolves at `heartbeat_age_secs` line 191, the live cron function |
| `required_wiring` anchor `registerSteps` | resolves, handler file registers |

## Module structure review

`heartbeat_age_secs` stays a single POSIX `sh` function, walking newest-to-
oldest with a plain `while`/`sed -n` loop rather than a second helper —
appropriate at this size (one function, `mutation_cost: low`,
`slice_size_envelope: low`); no split warranted.

`bl1413_heartbeat_age_probe.sh` extracts the real `heartbeat_age_secs`/
`iso_to_epoch` bodies at run time via `awk` range rather than hand-copying
them — avoids a stale-duplicate trap by construction, no rework needed.

`bl1413FreshnessNulByteSteps.js` follows the same established convention
BL-1011/BL-1012's own handlers for this script use (drive the real POSIX
checker as a subprocess against a real temp checkout, stub kill/start/
announce only). No duplication introduced relative to those siblings —
this ticket's fixtures and scenarios are new content, not restated logic.

## Mutation-site count (BL-485)

The shell script has no compiled JS counterpart (`mutation-site-count.js`
only parses JS/TS) and Stryker's own `mutate` scope
(`extension/stryker.config.json`) is `out/**/*.js` only — `.sh` files are
never a Stryker target, so this check does not apply to
`daemon_log_freshness_check.sh`.

```
node extension/out/tools/mutation-site-count.js specs/pipeline/steps/bl1413FreshnessNulByteSteps.js
```

| file | sites | verdict |
|---|---|---|
| `bl1413FreshnessNulByteSteps.js` | 223 | over (not a Stryker target — see below) |

Acceptance step-handler files under `specs/pipeline/steps/` are run by the
`node --test`-based acceptance runner, never by Stryker (confirmed against
`extension/stryker.config.json`'s `mutate: ["out/**/*.js"]` — this path is
outside that glob). The "over" verdict here does not correspond to a real
downstream mutation-hardening target, so no split is warranted on that
basis. The file itself is one cohesive set of step definitions for one
ticket's four scenarios.

By cleaner.
