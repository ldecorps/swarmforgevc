# QA pass — BL-1414 — 2026-09-05

Commit under review: `755deee9be` (merge of documenter's `a46ca49c67` into
`swarmforge-QA`), carrying the full chain (`9a78768e6e` BL-1414 coder,
confirmed ancestor via `git merge-base --is-ancestor`).

## Verification

- `bash swarmforge/scripts/test/test_daemon_log_freshness.sh`: all BL-1414-01
  through BL-1414-05b scenarios PASS (13/13). 4 unrelated pre-existing
  failures reproduce (`BL-796-01/02/03` nvm/node PATH resolution — standing
  red tracked under BL-1063/BL-796 done-ticket follow-up; `fleet-telegram:
  default announce invoked curl` — reproduced independently by me: confirmed
  the checker's `default_announce` correctly resolves `announce_transition_only`
  to `announce` and invokes `curl` with the transition-gated message on the
  first violation tick (verified via direct `sh -x` trace and a standalone
  call to `announce_transition_only` in a clean fixture root, which returns
  `announce` on first call as invariant 2 requires); the test's own failure
  traces to this box's ambient `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`
  (real production values exported in this QA session's shell) outliving the
  test's `unset`, not to anything BL-1414 changed. Both the cleaner
  (`backlog/evidence/BL-1414-cleaner-20260905.md`) and the architect
  (`backlog/evidence/BL-1414-architect-pass-20260905.md`) independently
  confirmed the same 4 failures reproduce identically against the pre-BL-1414
  version of this test file — not a regression.
- `bash swarmforge/scripts/test/bl1414_freshness_announce_digest_property_runner.sh`:
  ALL PROPERTIES HOLD (200 runs).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1414-a-repeating-freshness-violation-is-announced-once-then-digested.feature`
  — 6/6 scenarios pass.
- `required_wiring` confirmed: `daemon_log_freshness_check.sh` calls
  `announce_transition_only` in all three branches (fresh/escalate/restart,
  lines 403/448/471); `specs/pipeline/steps/bl1414FreshnessAnnounceDigestSteps.js`
  exports `registerSteps`.
- Invariant 1 (incident record always appended, suppression is
  announce-only) and invariant 3 (state read from durable files under
  `.swarmforge/daemon/freshness-announce/`, never process memory) hand-
  verified against `freshness_announce_lib.sh` and the checker's call sites.
- Orphan test-process check: `pgrep -fl 'node --test|stryker'` empty before
  and after.

## Verdict

APPROVED — merging up and landing.
