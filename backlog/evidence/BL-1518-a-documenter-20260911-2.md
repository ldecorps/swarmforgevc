# BL-1518-a — documenter review pass, 2026-09-11

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By documenter.

## Detail

- **QA bounce this pass addresses**: `backlog/evidence/BL-1518-a-QA-20260911.md`
  D2 — the send-time draft-root guard (`draft-root-guard!`, refusal
  `HANDOFF_DRAFT_OUTSIDE_ROOT`) added by this ticket to
  `swarm_handoff.bb` was missing from `docs/diagrams/handoff-flow.mmd`
  (Article 1.7 diagram currency, matching the diagram's own registered
  change-trigger). D1 is blamed on hardener and is addressed in
  `backlog/evidence/BL-1518-a-hardender-20260911-2.md`, not here.
- **Fix**: added a `ROOTGUARD` gate node between `DRAFT` and `VALIDATE`,
  and an `OUTSIDEROOT` terminal (bad) node, matching
  `swarm_handoff.bb`'s `-main` order — `draft-root-guard!` runs
  immediately after confirming the draft file exists, before
  `parse-draft`/`validate` (`swarmforge/scripts/swarm_handoff.bb:1148`).
  Commit `413210bd51`.
- **Verified against the checked-out guard code**: `grep -n
  "draft-root-guard\|OUTSIDE_ROOT"
  swarmforge/scripts/swarm_handoff.bb
  swarmforge/scripts/handoff_draft_root_guard_lib.bb` confirms the call
  site and the exact refusal string the diagram's `OUTSIDEROOT` node
  labels.
- **This is a re-check pass, not a fresh full doc sweep**: the prior full
  documentation pass for this ticket is `621113d62f` ("document the
  swarm_handoff.sh draft-root guard"); nothing there is invalidated by
  this D2 remediation.
