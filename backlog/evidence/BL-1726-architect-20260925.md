# BL-1726 — architect review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

## Detail

Rebuild pass, after QA held the parcel (evidence 91d5967fe5) and the
specifier's send-back (backlog/evidence/BL-1726-bounce-20260925.md): D1
(invariant-unencoded, coder) rebuilt; D2 (spec-gap) ratified; D3 (spec-gap)
struck — nothing left for the architect to re-check on D2/D3 beyond
confirming they are already reflected on this branch.

Note on the PRIOR architect pass (180506630d, 2026-09-24): it recorded
NONE on this same declared invariant with no property test present — QA's
send-back correctly names that as a miss this role's own Invariants Review
section should have caught. This pass verifies the gap is now closed, and
independently re-derives the same conclusion QA reached rather than taking
the coder's evidence file on faith.

Checks run:
- `npx vitest run --config vitest.properties.config.mjs test/bl1726OllamaLlamaServerOwnershipMarks.property.test.js`:
  1/1 passed.
- Independently re-verified non-vacuity myself (not just read the coder's
  claim): copied `orphan_janitor_lib.bb` aside, dropped the `--model`
  ollama-blob check from `ollama-own-llama-server-cmdline?` (the same
  mutation class the coder's evidence describes), re-ran the property
  test — it failed exactly as expected (`ollama-lib`/`other-path` case:
  expected `false`, got `true` — a hand-started-shaped llama-server
  wrongly reaped once only the lib-directory mark is checked). Restored
  the original file (`git diff` empty afterward, confirmed clean).
- Read the test itself: ground truth (`marksExecutable`/`marksModel`) is
  hard-coded per case, independent of the predicate under test — not
  circular. 64-case reach floor by construction (4 executables × 2 models
  × 2 × 2 × 2), asserted directly.
- `node out/tools/dependency-gate.js` / `co-change-report.js` on the new
  property test: PASSED, no forbidden edges; coupling only with this
  parcel's own evidence file.
- `node specs/pipeline/cli.js specs/features/BL-1726-the-ghost-reaper-touches-only-ollamas-own-llama-server.feature`:
  5/5 ok.
- `node specs/pipeline/cli.js specs/features/BL-1705-the-orphan-janitor-reaps-ghost-ollama-runners-and-detached-run-clients.feature`:
  8/8 ok — D2's ratified Examples row 2 (`/usr/lib/ollama/llama-server
  --model .../blobs/sha256-64b5`, parent init, reaped) carries both
  ownership marks now, confirmed by reading the feature file directly.
- `bb swarmforge/scripts/test/orphan_janitor_lib_test_runner.bb`: ALL
  CHECKS PASSED.
- Docs: `docs/reference/Specification.MD`'s BL-1726 entry read directly —
  accurately states the two-mark rule, the `ollama runner` exemption, and
  the "kept, not silently unscanned" behavior; matches the live predicate.
  D3 struck by the specifier (no per-process "kept" line required, the
  sweep's own candidate count already shows it was seen) — nothing to
  verify beyond the doc already being correct, which it is.

By architect.
