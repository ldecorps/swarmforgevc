# BL-1729 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Scope: `extension/test/helpers/tolerantTreeWalk.js`,
`extension/test/property-lane-heap-census.txt`,
`specs/pipeline/steps/bl1729PropertyWalkSkipsRuntimeStateSteps.js` — test-
infrastructure only, no `src`/`out` production TS or `.bb` production code
touched (CRAP/Stryker not applicable to this parcel).

Checks run (host load 2.9-18.2 on 20 cores):
- `node specs/pipeline/cli.js specs/features/BL-1729-a-property-lane-repository-walk-never-reads-the-checkouts-runtime-state.feature`:
  3/3 ok.
- `npx vitest run --config vitest.properties.config.mjs --logHeapUsage
  test/bl874PortableTimeInvariants.property.test.js
  test/tempDirTrapGuard.property.test.js` (from `extension/`): 61 MB / 58
  MB — far under the 100 MB bar (was 894/1778 MB on the shared main
  checkout), consistent with the coder's and cleaner's own readings.
- Census completeness (qa_e2e step 2): `comm -3 <(find test -name
  '*.property.test.js' | sort) <(grep -v '^#' test/property-lane-heap-census.txt
  | cut -f1 | sort)` — empty; both target rows present and under 100 MB.
- `npx vitest run --config vitest.properties.config.mjs
  test/bl1443TolerantTreeWalkInvariants.property.test.js` (the shared
  walk's own general invariant test): 3/3 before my own addition, 4/4
  after — no regression.
- `npx vitest run test/stepHandlerTmpRootGuard.test.js`: 4/4.
- Every standing whole-tree guard (this parcel touches `extension/test/`):
  `npx vitest run $(ls test/*Guard*.test.js | grep -v '\.property\.')` —
  22 files, 211 tests, all green.
- `git diff --stat f93ebdf4af..HEAD -- <the three Scope files>`: matches
  exactly.

**A real mutation gap found and closed (not just verified).** Hand-mutated
`tolerantTreeWalk.js` in place — dropped the `atRoot &&` guard so
`rootOnlyExcludeDirs.has(entry.name)` applies unconditionally, matching
`DEFAULT_EXCLUDED_DIR_NAMES`'s own at-any-depth behavior (a different
mutant than the coder's revert-to-pre-fix and the cleaner's `excludeDirs.
has` reversion — both of those remove the fix entirely; mine keeps the new
mechanism but widens its scope past the root). This mutant SURVIVED both
BL-1729's own feature (3/3) and the sibling `bl1443` invariant test (3/3)
— no fixture anywhere built a directory named `.swarmforge`/`tmp` NESTED
under the walk root, only at the root itself, so the fix's own defining
property ("only skipped as a direct child of the walk root," the ticket's
own FIRM wording) had zero coverage of the case that actually
distinguishes it from a plain always-exclude rule. Confirmed this is not
theoretical: `extension/tmp` is a genuine nested `tmp` directory under the
same repository root these two property files walk (`find . -type d -name
tmp`).

Closed it in `extension/test/bl1443TolerantTreeWalkInvariants.property.test.js`,
following the file's own existing "Hardener addition" precedent (an
earlier pass closed the identical class of gap for `excludeDirs`): a new
property test asserting a `rootOnlyExcludeDirs` name is skipped only when
it is a direct child of the walk root, and read when the identical
basename sits one level deeper. Verified: passes against the real fix
(4/4), fails against the hand-mutant (re-applied, re-ran: 3 passed/1
failed, the new test the one that failed), restored the real fix
byte-for-byte (`git status --short` clean before committing), and re-ran
green a final time (4/4, 14 MB heap) plus the full guard suite (22/22)
before committing at `881f37e106`.

Placed in `bl1443`'s file rather than BL-1729's own feature/handler
because: (1) feature files are the specifier's domain (Article 1.2) — a
hardener does not add Scenario/Examples text; (2) the property under test
(`rootOnlyExcludeDirs`'s root-only scoping) is a general contract of the
shared walk helper itself, not a claim specific to the two files BL-1729
names, so it belongs beside `bl1443`'s other general-contract tests
(`excludeDirs`, invariants 1-2) rather than duplicated per caller.

Fixture hygiene: every fixture in the new test and in all mutation runs
above is a `mkTmpDir`-rooted directory (per-test `afterEach` sweep,
tmpDir.js) — no raw `mkdtempSync`, no orphaned directories (`git status
--short` clean of anything besides the pre-existing untracked strays that
predate this session).

Not run: `npm run test:properties` in full (2026-09-17 rule — only the
files this parcel actually touches or that share the modified helper) and
Stryker/CRAP (no `src`/`out` file in Scope). qa_e2e step 4 (the shared
main checkout's own post-land `.swarmforge/operator/vscode-cli/`
reproduction) is explicitly a post-land step this worktree cannot perform
— no such directory exists here, as the coder's own evidence already
noted; left to QA/the land.

By hardender.
