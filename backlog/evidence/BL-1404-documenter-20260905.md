# BL-1404 documenter review pass — 2026-09-05

Reviewed the hardened commit (`d2fc855945`) and the coder/cleaner/architect/
hardener trail for BL-1404 (a recorded waive silences the operator
escalation too).

Checklist:
- Ticket-named acceptance feature exists:
  `specs/features/BL-1404-a-recorded-waive-silences-the-operator-escalation-too.feature`.
- Step handler exists:
  `specs/pipeline/steps/bl1404WaiveSilencesEscalationSteps.js` (the ticket
  itself notes no consumer `required_wiring` anchor is needed beyond the
  handler-file line since the call site is the parcel's own diff).
- Verified the actual fix in `swarmforge/scripts/babysitter_check.bb`:
  `decide-escalations` is now called with `nudgeable` (the post-waive set
  from the single `partition-findings` call), the same set `decide-nudges`
  already received — not the raw `findings` list as before.
- Operator-visible behavior change (a recorded babysitter waive now
  silences BOTH the coordinator nudge and the operator
  `BABYSITTER_ESCALATION` channel, where before it silenced only the
  nudge and left a permanent CRIT re-waking the operator every escalation
  cooldown) is documented in
  `docs/how-to/BL-611-babysitterd-runbook.md`'s waive section — the
  ticket's own direction names this doc by name. Replaced the stale
  "Scope: the waive silences the coordinator nudge only... extending the
  waive there is an explicit follow-up" paragraph (which described
  exactly the gap this ticket closes) with the fixed behavior, and added
  the new test/feature to the Verify and Acceptance-feature lines.
- No "Last Updated" freshness field in the edited doc — no date bump
  owed.
- `docs/index.md`'s existing one-line summary for the runbook doesn't
  claim nudge-only scope anywhere — no edit needed there.
- No registered diagram depicts this mechanism's internal decision logic
  (a bugfix inside an existing sweep's decider wiring, not a new
  component, boundary, or pipeline-topology change) — no diagram change
  needed.
- No production code, test, or requirements change made or needed by this
  pass — documentation only.

Result: NONE — no defect found.
