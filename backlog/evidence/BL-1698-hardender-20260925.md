# BL-1698 — hardender review pass, 2026-09-25

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

No send-back: the one gap found (below) was in this stage's own domain
(mutation hardening / invariant coverage) and was fixed in this pass,
not bounced.

BL-113 Gherkin acceptance mutation (soft) over the feature's three
Scenario Outlines (indices 0, 2, 3 — the two plain `Scenario:`s at
index 1, 4, 5 are `inapplicable` per BL-638, expected): 10/10 mutants
killed, 0 survivors, 0 errors. Manifest embedded in the feature file.

Chasing the architect's stated disposition of invariant 2 ("every
mail item ends in exactly one of: merged+completed, or
escalated+held with a recorded reason") through `mechanical-mail!`'s
merge-conflict branch in `local_parcel_driver_lib.bb` found the
branch called `escalate!` (raised the question) but never persisted
a driver record — leaving a conflicted mail item `in_process` with
no record naming why, a live breach of invariant 2, and with no way
for an operator to release it via `release-hold!` (requirement 3) —
the exact motivating problem this ticket exists to fix, recurring one
layer down for mail instead of a ticket parcel. Fixed by threading
`project-root`/`seat-id` into `mechanical-mail!` and writing the same
`{:escalated true :ticket "mail" :reason "merge conflict"}` shape
`start-ticket-parcel!`'s own conflict branch already uses.

Added `extension/test/bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js`
(4 tests, real driver CLI / real git / real `seat` script, no pure-module
reimplementation):
- the merge-conflict-persists-then-releases case above (drives a real
  conflicting merge, asserts the persisted record, the single question
  raised naming the conflict, the item still held in_process, then
  releases it via `release-hold! complete` and confirms the record
  clears);
- `ask-or-escalate-to-coordinator!`'s already-pending fallback
  (requirement 5's explicit "if the role's question slot is already
  taken … send the same text to the coordinator instead") — already
  correct, pinned since nothing exercised it before this pass;
- `release-hold!`'s two arms the acceptance feature's `complete`/`retry`
  examples don't reach: no driver record for the seat (exits 1, prints
  `null`) and an unrecognized mode (refuses nonzero, record left
  untouched — never silently doing one of the two known things).

No TS/JS production file is in this ticket's scope (`git diff
main...HEAD --name-only`: two `.bb` files, one step handler, one
fixture, two property tests, one new unit test) — CRAP/DRY are not
wired for Babashka (engineering.prompt) and were not run.

Checks run:
- `node specs/pipeline/cli.js specs/features/BL-1698-….feature`: 9/9.
- `node specs/pipeline/cli.js specs/features/BL-1697-….feature`: 12/12
  (regression, unaffected by the fix — BL-1697 has no mail-conflict
  scenario).
- `npx vitest run test/bl1698LocalParcelDriverMailAndReleaseEdgeCases.test.js`:
  4/4.
- `npm test` (extension/): 638 files / 10872 tests, all green. Pre-existing
  per-file suite-budget warnings on 6 unrelated files (none touched by
  this ticket) are a standing condition, not this pass's regression.
- `npm run test:properties` not run: this pass added no
  `*.property.test.js` file (2026-09-17 rule); the two property tests
  in scope are the coder's and architect's, verified green above via
  the acceptance runs that exercise the same driver lib.
- `pgrep -f '[t]mux -S' | wc -l`: 7 before and after every run in this
  pass — no tmux-server leak. `git status --short` clean before commit —
  no stray fixture dirs.
- `pgrep -fl 'node --test|stryker'` (own worktree scope): no orphaned
  runner from a prior interrupted session; host load 12/20 cores,
  well under the 2x-cores ceiling, no fallback needed.

By hardender.
