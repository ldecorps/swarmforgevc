# BL-1422 QA pass — 2026-09-05

BL-1422 PASSES.

## Pipeline lineage verified

Documenter's commit `6731eff0d0` merged into the QA worktree at `a6b03704e1`
(merge required naming BL-1428's already-landed BL-816 retirement, which
deleted `specs/features/BL-816-red-suite-disposition-recorded.feature.draft`
in both parents already — not a new removal here). Confirmed via
`git merge-base --is-ancestor` that this ticket's own coder (`ae97bcc00a`),
cleaner (`099ad0e732`), architect (`948306e3bf`), and hardener (`d31a220eb1`)
commits are all ancestors of the documenter commit being cited.

## Ticket-specific verification (independent re-run)

- `bash swarmforge/scripts/test/test_done_with_current_work_note_evidence.sh`:
  ALL PASS (9/9 scenarios: no-evidence refusal, commit evidence, sent/ and
  outbox/ git_handoff evidence, `--no-work` recording + blank-reason
  refusal, non-Work-note and git_handoff passthrough, 28-chase-notes burst).
- `bb swarmforge/scripts/test/bl1422_work_note_not_completed_without_work_property_runner.bb`:
  ALL PROPERTIES HOLD, 500 runs each.
- `bash swarmforge/scripts/test/test_done_with_current_arg_rejection.sh`
  (BL-652 regression): ALL PASS, unaffected.
- `specs/pipeline/scripts/run_acceptance.sh
  specs/features/BL-1422-a-work-note-is-not-completed-without-work.feature`:
  7/7 scenario runs pass.
- `required_wiring` both anchors confirmed live:
  `swarmforge/scripts/done_with_current_task.bb:109` delegates to
  `chase-sweep-lib/dispatch-trail-ticket-id`; step handler file present and
  `registerSteps` defined.
- No orphaned test/mutation/tmux processes before or after this pass.

## Full unit suite (BL-1063 diligence)

`npm test` in `extension/`: 588/604 files passed, 10207/10233 tests passed —
same 16 already-tracked failing files as the prior BL-1416 QA pass
(BL-1221/BL-1229, BL-1263, BL-1265, BL-1289/1290/1291, `pricingTable.test.js`
gap). BL-1422's diff never touches `extension/src/**`; confined to
`swarmforge/scripts/done_with_current_task.bb`, `dispatch_lib.bb`,
`chase_sweep_lib.bb` (via required_wiring), the new
`work_note_evidence_lib.bb`, their test/property runners, and one pipeline
step handler.

## Property suite (`npm run test:properties`)

18 failed test files / 4 failed tests, plus the allowlisted `[vitest-worker]:
Timeout calling "onTaskUpdate"` (BL-871) x2. Every failure grepped against
`backlog/standing-reds.tsv` and confirmed pre-existing and already owned:
`bl733ProducerCrosscheck`, `pilotAcceptanceGate`, `pilotScopedCrapEvidence`
(BL-1206, node:test import/collection failure), `multiBranchParserCoverageCheck`,
`perHatRolePromptEvidenceCheck` (BL-1229), `unreachableStepHandlerCheck`
(BL-1221) — all `deps.checkOrphanedAuthoredDocs is not a function` /
`landPilotedTicket` deps-contract gaps in `pilotAcceptanceGate.ts`, none
touched by this diff. No new failing file appeared.

## Verdict

BL-1422 APPROVED.

By QA.
