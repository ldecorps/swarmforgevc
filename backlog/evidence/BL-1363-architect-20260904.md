# BL-1363 — architect review, 2026-09-04

Reviewed coder commit merged in via cleaner `eda40f06d4`, which also
carries a rework fix to BL-1390's test fixture (a stray `git -C ""`
that had rewritten the LIVE repository's `remote.origin.url` earlier
today, since fixed and now guarded by a structural "exactly one raw git
-C" assertion plus byte-identical origin/worktree-list checks) — spot
-checked and unaffected by my own review, not this ticket's own concern.

## Checks run

- Dependency gate on `bl1363ClosingATicketIsOneCommandSteps.js`: PASSED.
- Co-change: brand-new file, no smell.
- `check_feature_handler_registration.sh`: exit 0.

## Re-verified independently, not trusted from evidence

- `test_bl1363_close_ticket.sh` — 15/15, run 4 times total (once plus a
  3x repeat to check for the class of flakiness BL-1391 turned up
  earlier this session) — clean every time, including the byte-identical
  live-origin-URL check BL-1390's rework pattern added.
- `bl1363_close_ticket_property_runner.sh` — ALL PROPERTIES HOLD over 12
  constructed cells.
- `run_acceptance.sh` on the BL-1363 feature — 5/5.

## Correctness read

Read `close_ticket.sh` end to end against `promote_and_route_next.sh`'s
own pattern (the ticket's explicit model):
- Every id is resolved (file exists, exactly one match, `milestone:`
  field present) BEFORE any `git mv` — a partial close genuinely cannot
  happen structurally, matching invariant 2.
- The snapshot/rollback is scoped to exactly this close's own paths
  (`git ls-files --stage -- <paths>` / `update-index --index-info`),
  never a blanket reset — correctly avoids discarding other roles' staged
  work on the shared checkout, the same discipline this session's other
  reviews have required throughout.
- The integrity CLI refusal path (`CLOSE BLOCKED` vs `"reason"`/`FAILED`)
  is read identically to promotion's own two shapes, and a refusal never
  falls through to a raw commit — the exact BL-1028 failure this ticket
  exists to avoid, verified by reading the code (no commit call exists
  on the refusal branch, only `rollback_close` + `exit 1`).
- The "no `commit_integrity_cli.bb` present" degraded path exactly
  mirrors `promote_and_route_next.sh`'s own equivalent branch (confirmed
  by reading both files side by side) — not a new, differently-reasoned
  fallback.
- Invariant 3 (never promotes): confirmed by reading the script to its
  end — nothing calls `promote_and_route_next.sh` or touches
  `backlog/paused/`.

## Verdict

COMPLIANT. Forwarding to hardener.
