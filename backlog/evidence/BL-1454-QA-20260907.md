# BL-1454 — QA review pass, 2026-09-07 (offline expedite, stack stopped)

## Verdict: HOLD — not approved. Blocked by Article 4.2 (unowned reds), not by
any defect in this ticket's own work.

BL-1454's own deliverables are fully verified and clean (see "This ticket's
own gates" below, all green). QA cannot approve this parcel because its own
required final-gate run (`npm run test:properties`, mandatory per the QA role
prompt: "run it ... require it green") surfaces THREE pre-existing,
reproducible, unowned reds unrelated to any file this ticket touches. Per
Article 4.2 (2026-09-05 standing-red-register-amendment): "QA approves no
parcel whose evidence names a red with no open ticket in the standing-red
register: an unowned red is a note to the specifier and the parcel waits ...
never an approval over the top of it." "'Pre-existing' is a fact about the
red, not a disposition of it."

This is not a bounce: BL-1454's own commits did not cause any of the three
reds below, so there is no role in the forward chain to route rework to.
This is the dedicated third disposition the QA role prompt names for exactly
this situation — the parcel WAITS for the specifier/coordinator to ticket
(or otherwise adjudicate) the reds, then QA re-runs and, if BL-1454's own
gates are unchanged, approves immediately.

## This ticket's own gates — all green, independently re-run

- `npm test` (compiled unit suite, `extension/`): **610/610 files, 10309/10309
  tests pass**, both times run. (Exit code 1 both times is BL-378's
  suite/per-file wall-clock duration budget guard, NOT a test failure — see
  "Noted, not blocking" below.)
- `bb swarmforge/scripts/test/coordinator_activity_feed_lib_test_runner.bb` —
  ALL PASS.
- `bb -e '(load-file "swarmforge/scripts/handoffd.bb")'` — loads cleanly,
  exit 0.
- `specs/pipeline/scripts/run_acceptance.sh specs/features/BL-1454-the-activity-feed-sweep-fits-the-supervisor-budget.feature`
  — 4/4 pass.
- `specs/pipeline/scripts/run_acceptance.sh specs/features/GH-24-coordinator-activity-telegram-feed.feature`
  — 5/5 pass (confirms the ticket's required handler-realization change to
  GH-24's Background did not regress GH-24's own scenarios).
- `npx vitest run --config vitest.properties.config.mjs test/bl1454ActivityFeedSweepBudget.property.test.js`
  — 3/3 pass (this ticket's own three declared invariants).
- `test/bl1371StepDiscovery.test.js`, `bl968StepRegistryMaterializedTreeGuard.test.js`,
  `bl1277UnscopedStepCollisionGuard.test.js` — 20/20 pass (new step file
  discovers cleanly; no unscoped-pattern collision).
- Wiring: `coordinator-activity-feed-sweep!` defined at
  `swarmforge/scripts/handoffd.bb:4235` and called from the poll loop at
  line 5057 — confirmed reached in production, matching `required_wiring`'s
  own note that the consumer anchor is pre-satisfied and the fix changes the
  mechanism's INSIDE.
- Ancestry: `git merge-base --is-ancestor <c> HEAD` holds for every stage
  commit in this parcel's own chain (specifier `1892067692`, coder
  `48a677bcde`, architect `191d714b31`, hardener `83d18a8fe9`, documenter
  `1ed2e6d2dc`, documenter-evidence `5717703d53` = current HEAD) — HEAD is
  genuinely this ticket's own work, not a sibling's commit standing in for
  it.
- No orphaned `node --test`/`stryker`/vitest processes before or after any
  run (checked via `ps aux`, not bare `pgrep -f` self-match).

Noted, not blocking: `npm test`'s exit 1 both runs is BL-791 (open, paused
epic, `status: todo`) — "BL-378's guard has 19 live offenders and therefore
fails every `npm test` through computeFinalExitCode — it is already a
permanently-red gate." This is a known, already-ticketed, host-load-driven
wall-clock budget failure (the specific offending files differ run to run:
5 offenders one run, 7 the next), not a correctness regression, and it is
not the Article-4.2 concern this hold is about (that rule is about the
property/unit/bb/acceptance/shell lanes' standing-red register, and BL-791
already owns this exact failure mode).

## The blocking finding: three unowned reds, independently confirmed

`npm run test:properties` (the full, un-scoped suite, run twice by QA
independently — the mandatory command per this role's own gate, not merely
inherited from the coder's pre-commit hook) is genuinely red. Under full
371-file contention the offender count varies by run (17 files one run, 14
the next — consistent with BL-1407's documented load-flake shape under
15+ concurrent vitest workers). Reconciling every offender from both runs by
re-running in a much smaller, far-less-contended batch (14 files together,
then the three survivors completely alone, one file per invocation, several
times each) narrows this to three FILES that fail every single time,
independent of contention, with genuine assertion/counterexample content
(not timeouts alone):

1. **`extension/test/bl1218RemoteControlConfigInvariants.property.test.js`**
   (invariant 2) — `AssertionError: no launch script was written for config
   off`, deterministic (fails alone, repeatably). Ticket **BL-1218 is
   CLOSED** (`backlog/done/M8/BL-1218-config-off-is-honored-at-launch-over-an-explicit-window-flag.yaml`).
   Last touched 2026-08-30 (`5b534f8ea9`) — five days before this ticket's
   work began and outside its diff.

2. **`extension/test/bl1252CommitGuardAggregationInvariants.property.test.js`**
   (invariants 1, 2, 3 — 5 sub-cases) — times out at 20000ms on every
   sub-case, run completely alone with nothing else competing for a fork
   (not merely under full-suite contention). Ticket **BL-1252 is CLOSED**
   (`backlog/done/M8/BL-1252-commit-guard-chain-reports-every-violation.yaml`).
   Last touched 2026-09-05 (`92b30edccb`, BL-1424) — two days before this
   ticket's work began and outside its diff.

3. **`extension/test/bl1320DocumentedStepsAreExecutedInvariants.property.test.js`**
   — **new finding, not previously reconciled correctly.** The coder's
   addendum (`backlog/evidence/BL-1454-coder-pass-20260907.md`) listed this
   file among "3 originally-failing files ... not named in the guard's
   'still fails when run alone' output ... cleared on the alone re-run" and
   every later stage (cleaner, architect, hardener, documenter) forwarded
   that claim without independently re-running it. QA's own reconcile-alone
   run shows this is **wrong**: the file fails 100% deterministically, same
   counterexample every single invocation (verified 3 times, including
   twice completely alone with no other file in the same vitest process):
   `Counterexample: ["window coder claude coder --model claude-opus-5
   --seat-tier hard"]` — this is a fixed enumeration over the how-to page's
   own printed lines (not a randomly-sampled input), so it is not a
   probabilistic flake; it fails on the same concrete doc line every run.
   Ticket **BL-1320 is CLOSED**
   (`backlog/done/BL-1320-operator-step-for-adding-a-seat-of-a-bottleneck-stage.yaml`).
   Last touched 2026-09-03 (`dade75b8a8`) — four days before this ticket's
   work began and outside its diff.

None of the three test files, nor `docs/how-to/BL-1320-add-or-remove-a-seat-of-a-bottleneck-stage.md`
(the page BL-1320's property reads), appear anywhere in this ticket's own
diff (`git diff --name-only df95dd4de8 HEAD`). None of the three tickets
(BL-1218, BL-1252, BL-1320) is in `backlog/active/` or `backlog/paused/`,
and none of the three files is a row in `backlog/standing-reds.tsv` (checked
against the current file, last touched by BL-1450's mint — the register is
actively maintained, rows are added/removed as reds are found/fixed, so
absence here is not registry staleness).

**Correction, for the record:** `extension/test/bl968MaterializedGuardSensitivity.property.test.js`
(the fourth file the coder's addendum discussed) is ALSO currently red, but
it is NOT part of this blocking set — it IS already owned:
`backlog/standing-reds.tsv` carries a `property` row for it naming **BL-1450**
(active), first seen 2026-09-06. Named here only so the specifier does not
re-mint a ticket that already exists.

## Disposition

Per the QA role prompt's own instruction for exactly this shape ("A red with
no open owner is an unowned-red note (priority 00) to the specifier and the
coordinator, and the parcel WAITS for the ticket; it is never a bounce, the
parcel did not cause it, and never an approval over the top of it"): this
parcel is **not approved**. No `swarm_handoff.sh` note can be delivered
live — this is an offline expedite run with handoffd stopped — so this
evidence file is the durable record for the specifier and coordinator to act
on at swarm resume, per the same recovery-escape precedent the coder
recorded in `backlog/evidence/BL-1454-coder-pass-20260907.md`.

**Recommended remediation** (specifier/coordinator, on swarm resume): mint or
register open tickets for `bl1218RemoteControlConfigInvariants` invariant 2,
`bl1252CommitGuardAggregationInvariants` invariants 1-3, and
`bl1320DocumentedStepsAreExecutedInvariants` (three rows in
`backlog/standing-reds.tsv`, or three defect tickets — specifier's call).
Once each carries an open ticket, re-run this QA pass: BL-1454's own gates
above are already fully green and require no rework, so approval should
follow immediately with no further coder/cleaner/architect/hardener/
documenter involvement, unless something regresses in the meantime.

By QA.
