# BL-1147 (+ batch siblings BL-733/735/737) QA pass inventory — 20260826

**Documenter tip:** `ef6a5b3b4a` (inbound task BL-1147; hardener batch with BL-733/735/737)
**Merge tip:** `5d5a9a63dd`
**Sibling checks:** `VERIFY` for BL-1147, BL-733, BL-735, BL-737

## Tip purity

`origin/main` is ancestor; `land_main_publish --decide-only` → `:push`.
0 `mutations/` hitchhikers. BL-782 acceptance handlers remain absent (prior
QA bounce held). Conflict resolution took documenter Spec + BL-782 evidence.

## Ticket gates

| Ticket | Unit/property | APS | Wiring |
|--------|---------------|-----|--------|
| BL-1147 | 12/12 | 8/8 | probe CLI + `bl1147` steps + `openSubjectAndRecord` re-adopt |
| BL-733 | 3/3 property | 4/4 | `bl733` steps |
| BL-735 | 5/5 property | 5/5 | `bl735` steps |
| BL-737 | 7/7 unit + 3/3 property | 5/5 | `bl737` steps + `crossFileDuplicationCheck` |

Stage evidence present for all four. Orphan test procs: none after runs.
CLI smoke: `probe-legacy-topic-adoption.js` exit 0. How-to + Spec current.

## Intent (BL-1147)

Read-only probe classifies legacy BL keys, cursor Host routing
(`bridge` | `operator-re-adopt` | `unbound`), scrub candidates; non-cursor
provider re-adopts cursor Host topic into OPERATOR; cursor provider throws
bridge-owned. No map mutation / Telegram from the probe.

## Inventory (Article 4.4)

NONE.

## Verdict

**PASS** — land via BL-1144; coordinator must close **BL-1147, BL-733, BL-735,
BL-737** (Article 2.6).

By QA.
