# BL-1492 — QA review pass, 2026-09-08

## Verdict: HOLD — not approved. Blocked by Article 4.2 (a newly-surfaced
unowned red), not by any defect in this ticket's own work.

BL-1492's own deliverables are fully verified and clean (see "This ticket's
own gates" below, all green or matching an already-registered standing red).
QA cannot approve this parcel because independently re-running its required
`qa_e2e_procedure` item 1 (`test_handoffd_supervisor.sh` "updated for the
ladder and green") surfaces a THIRD, previously-undiscovered pre-existing
red in that same file — reproducible identically on unmodified `origin/main`
— with no open ticket. Per Article 4.2 (2026-09-05
standing-red-register-amendment): "QA approves no parcel whose evidence
names a red with no open ticket in the standing-red register: an unowned
red is a note to the specifier and the parcel waits ... never an approval
over the top of it." "'Pre-existing' is a fact about the red, not a
disposition of it."

This is not a bounce: BL-1492's own commits did not cause this red (it
reproduces identically on `origin/main`, which contains none of BL-1492's
diff), so there is no role in the forward chain to route rework to. Per the
QA role prompt's dedicated third disposition for exactly this shape: the
parcel WAITS for the specifier/coordinator to ticket the red, then QA
re-runs and, if BL-1492's own gates are unchanged, approves immediately.

## This ticket's own gates — all green, independently re-run

- `qa-sibling-check.js status --ticket BL-1492`: `VERIFY BL-1492` (no open
  deferral).
- Orphan process check before/after: `pgrep -fl 'node --test|stryker'`
  clean both times.
- Merged `documenter` `4b952f505b` into this QA worktree branch (clean
  merge, `21e3683233`; diffed against both parents, nothing dropped except
  a 10-line unrelated ticket YAML this branch already carried).
- Full unit lane (`npm test`, `extension/`): **613/613 files, 10401/10401
  tests pass**. Exit code 1 is the chronic, pre-existing suite-duration/
  per-file wall-clock budget guard (`recordTestDuration.js`) — same
  disposition BL-1444's QA pass (2026-09-06) recorded: none of the flagged
  slow files (`bl1277UnscopedStepCollisionGuard`,
  `bl968StepRegistryMaterializedTreeGuard`, `briefingDigestLineCli`,
  `emitLifecycleSnapshotCli`, `recordBounceCli`, `renderBriefingBurndownCli`,
  `renderBriefingDiagramsCli`, `telegramFrontDeskBotCli`) is touched by
  BL-1492's diff, not this ticket's regression.
- Full property lane (`npm run test:properties`): **384/384 files,
  1128/1128 tests pass**, exit 0. One `[vitest-worker]: Timeout calling
  "onTaskUpdate"` — the BL-871 allowlisted benign case.
- `bb swarmforge/scripts/test/handoffd_supervisor_restart_budget_test_runner.bb`
  — ALL TESTS PASS.
- `bb swarmforge/scripts/test/bl1492_restart_in_place_property_runner.bb`
  — ALL PROPERTIES HOLD, 150 runs/property, generator coverage confirmed
  (p1/p2 both verdicts+outcomes, p3 both base verdicts, p4 all 3 at-kinds).
- Acceptance: `run_acceptance.sh
  specs/features/BL-1492-a-stalled-verdict-restarts-the-daemon-in-place-not-the-swarm.feature`
  — 5/5 scenarios pass.
- `test_handoffd_supervisor.sh` (unmodified, as shipped): fails at case 02
  under plain invocation — traced (matching hardender's and architect's own
  passes) to the already-registered **BL-1498** standing red (`swarm-cleanup.sh`
  zsh/`~/.zshenv` PATH shadowing + startup-grace on a freshly-written pid
  file). Under `ZDOTDIR=<empty dir>` isolation: cases 01/03/04/recovered
  PASS, case 02 still fails identically to unmodified `main` — same
  signature BL-1498 already owns, confirmed via `grep -rl BL-1498 backlog/`
  and `backlog/standing-reds.tsv`.
- Required wiring: `restart_history` written by `restart-daemon!` and
  rendered by `format-failure-log`'s `"restart_history: " (pr-str
  restart-history)` line — confirmed (previously always nil per the
  ticket's own consumer-anchor note). `specs/pipeline/steps/bl1492RestartInPlaceSteps.js`
  is a top-level `*Steps.js` file, auto-discovered by `index.js` — confirmed.
- Ancestry: HEAD (`21e3683233`) has documenter's `4b952f505b` as its
  second parent, which has hardender's, architect's, cleaner's, and coder's
  commits as ancestors (checked via `git log --oneline main..4b952f505b`)
  — genuinely this ticket's own chain, not a sibling's commit standing in
  for it.
- `bounce_history`: one bounce recorded (architect → coder, D1, class
  `behavior`, commit `41d0c7fe6c`), fixed by coder's `a21f9b2233` and
  independently re-verified by architect's second pass
  (`BL-1492-architect-20260908-2.md`, NONE) — resolved, no rework needed.

## The blocking finding: a third, previously-masked unowned red in
`test_handoffd_supervisor.sh`

The ticket's own `qa_e2e_procedure` calls for `test_handoffd_supervisor.sh`
to be "updated for the ladder and green." As shown above, the shipped
script is not literally green — it fails at case 02, matching the
already-registered BL-1498 red, which every prior stage (hardender,
architect, documenter) accepted as the known, non-blocking pre-existing
exception. Under `set -euo pipefail`, `fail()` calls `exit 1`, so case 02's
failure aborts the whole script and no later case ever runs under a plain
invocation — this is exactly why no prior stage discovered what follows.

To independently confirm hardender's/architect's own claim that case 06
(the new BL-1492 case proving `check!`'s real `:restart` dispatch) passes
"standalone," QA built a scratch copy of the script with the case-02 block
excised (BL-1498's own known failure point) so cases 03/04/05/06 and the
later BL-081 hard-stop-confirmation group could run to completion. Ran
in-place (same directory, so `SCRIPT_DIR`-relative paths resolve), under the
same `ZDOTDIR=<empty dir>` isolation used above, twice:

    PASS: 01 (dead daemon detected, alarmed, halted)
    PASS: 03 (failure log captures last log lines)
    PASS: 04 (no silent auto-restart)
    PASS: recovered daemon marked healthy
    PASS: 05 (messy death still alarms and halts)
    PASS: 06 (default budget headroom takes the real check!->restart path) — confirms hardender's/architect's own claim
    FAIL: 01: old daemon (slow to die) still alive after halt

The failing case is `test_handoffd_supervisor.sh`'s **BL-081 scenario 01**
("hard-stop confirms the old daemon's exit before hard-stopping proceeds",
lines 271-301 of the file as shipped): a fake daemon traps SIGTERM and
exits after an 0.8s delay; the assertion is that the process is dead and at
least 700ms elapsed before `check_once` (the supervisor's halt path)
returns. Both runs: the process was still alive when `check_once` returned
(caller had to `kill -9` it), i.e. the hard-stop path did not confirm the
old daemon's exit before proceeding — a DIFFERENT failure mode than
BL-1498's two causes (zsh/PATH tmux shadowing; startup-grace on a fresh pid
file), unrelated to either.

**Reproduced identically on unmodified `origin/main` `551d1ecc4b`**, via a
temporary `git worktree add --detach` of `origin/main` with the identical
scratch case-02-excision and `ZDOTDIR` isolation applied — same failure,
same message, same case. Since `origin/main` contains none of BL-1492's
diff (only `handoffd_supervisor.bb`, `daemon_alarm_lib.bb`, and
`swarm_ensure.bb` are touched by this parcel, per `git diff --stat
main..4b952f505b`), this red cannot be caused by BL-1492's changes — it
predates this parcel and is masked, on `main`, behind the same BL-1498
abort that hides it here.

Grepped first (BL-1063 discipline):
`grep -rl "slow to die\|old daemon (slow to die)\|hard-stop confirms the
old daemon" backlog/` — no hits. `backlog/standing-reds.tsv`'s one row for
`test_handoffd_supervisor.sh` (owner **BL-1498**) names only the two causes
already described above (case 01 tmux-shadowing / case 02 startup-grace);
this is a third, distinct cause with no row and no ticket.

## Disposition

Per the QA role prompt's instruction for exactly this shape (precedent:
`backlog/evidence/BL-1454-QA-20260907.md`): this parcel is **not
approved**. Sending the specifier and coordinator a `note` (priority `00`,
type `unowned-red`) naming this finding and this evidence file.

**Recommended remediation** (specifier, on adjudication): mint or register
an open ticket for `test_handoffd_supervisor.sh`'s BL-081 scenario 01
("hard-stop confirms the old daemon's exit"), add a row to
`backlog/standing-reds.tsv`, `severity: high` per the amendment's rule 1.
Once it carries an open ticket, re-run this QA pass: BL-1492's own gates
above are already fully green and require no rework, so approval should
follow immediately unless something regresses in the meantime.

By QA.
