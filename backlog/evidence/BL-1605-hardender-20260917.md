# BL-1605 — hardender review pass, 2026-09-17

NONE. The full checklist was run and found no defect.

Recorded as an explicit NONE rather than skipped: an inventory is a pass
artifact, not only a bounce artifact (Article 4.4), and the forward names
THIS commit rather than the received hash (BL-536).

By hardender.

## Detail

Second hardener pass, on the coder's follow-up rebuild fixing the
`socketFixtureShortRootGuard` defect this same hardener found and reported
(priority-00 note, `BL-1605-hardender-socket-fixture-root-followup-20260917.md`)
while running the standing whole-tree guards for BL-1610. Specifier
adjudicated it as BL-1605's own defect (not unowned), added the register
row, and QA bounced the coder for the `mkSocketFixtureRoot` swap
(`BL-1605-QA-20260917.md`).

- Merge conflict on `backlog/standing-reds.tsv`: my side (from an earlier
  `main` merge) still carried the register row; the coder's side had
  removed it (register invariant 2: "a row leaves in the same commit that
  discharges it"). Verified the underlying test actually passes BEFORE
  resolving (`npx vitest run test/socketFixtureShortRootGuard.test.js` →
  16/16 green) — only then took the deletion. This is exactly the class
  of merge defect BL-1608 was about (a merge silently keeping stale
  content over a discharged fix); resolved the opposite way this time.
  Diffed the merge commit against BOTH parents afterward to confirm
  nothing else was lost (`git diff <merge>^1 <merge>` and `^2`, on both
  `backlog/standing-reds.tsv` and the step-handler file) — clean.
- `reverse_hop_lib_remove_forward_recipients_runner.bb`: green (unchanged
  logic, already hardened in the first BL-1605 pass).
- `bl1299_reverse_hop_property_runner.bb`: 500 runs, ALL PASS.
- Acceptance: `BL-1605-*.feature` 5/5.
- Standing whole-tree guards: **18/18 pass** — the socket-fixture-root
  violation this hardener reported and every other role adjudicated is
  now fully closed.
- No new logic to hand-mutate: the coder's fix is a pure test-fixture-root
  swap (`os.tmpdir()` → `mkSocketFixtureRoot`), not a change to
  `reverse_hop_lib.bb`'s own reverse-recipients logic.
- No orphaned test/mutation processes before or after.
