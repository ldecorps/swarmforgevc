# BL-1221 documenter review pass — 2026-09-05

Reviewed the hardened commit (`c85b83c003`) and the coder/cleaner/architect/
hardener trail for BL-1221 (pilot gate deps stubs missing
checkOrphanedAuthoredDocs).

Checklist:
- Step handler exists:
  `specs/pipeline/steps/bl1221PilotGateDepsStubsSteps.js`.
- Confirmed the fix: `checkOrphanedAuthoredDocs` (a benign "no orphans"
  stub, per the ticket's direction) is now supplied in the 16
  `extension/test/*.test.js`/`*.property.test.js` files that call
  `landPilotedTicket`, listed individually in the hardener's evidence
  file. No change to `pilotAcceptanceGate.ts`'s deps contract or its
  call site, per the ticket's own constraints — confirmed no production
  file appears in the diff.
- This is a pure test-fixture repair (16 stub additions), out of scope
  explicitly excludes any structural guard, behavioral coverage of the
  check itself, or the `docs/deprecated/` gap — no new mechanism or
  operator-visible behavior to document from this ticket's own diff.
- Grepped `docs/` for `checkOrphanedAuthoredDocs` / `landPilotedTicket`
  and found a genuinely STALE reference:
  `docs/how-to/BL-1220-unit-lane-node-test-import-guard.md` described
  ten of its 24 repaired files as carrying "BL-1221's separate ... defect"
  and collecting-but-failing as "an intermediate state ... expected, not
  a regression" — present tense, describing the state BEFORE this
  ticket's own fix landed. Updated both the inline paragraph and the
  "Out of scope / separately owned" bullet to past tense, noting BL-1221
  has since landed its own stub repair, so the intermediate state no
  longer applies. This is exactly the "doc already accurate ≠ pass
  done" trap in reverse — a doc that was accurate when written but went
  stale the moment this ticket landed.
- `docs/index.md`'s existing BL-1220 summary line doesn't reference
  BL-1221 at all — not stale, no edit needed there.
- No "Last Updated" freshness field in the edited doc — no date bump
  owed.
- No registered diagram depicts test-stub contract shape — no diagram
  change needed.
- No production code, test, or requirements change made or needed by this
  pass beyond the doc fix above.

Result: NONE — no defect found in the parcel itself; one stale cross-
reference in a sibling ticket's doc found and fixed.
