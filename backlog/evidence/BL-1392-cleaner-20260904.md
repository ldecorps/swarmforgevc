# BL-1392 — CLEANER PASS, 2026-09-04

NONE. No defect found; no cleanup change made.

## Conflict resolution note

Merging this commit conflicted on `swarmforge/scripts/test/suite-manifest.tsv`
against BL-1391's two rows — the same shape as BL-1363's merge earlier
this turn: coder built this before BL-1391's bounce/revert reached this
branch. Resolved by keeping BL-1363's row once and all three new BL-1392
rows, dropping the BL-1391 rows whose backing files still don't exist on
this tree.

## What was checked

- `cron_heartbeat_lib_test_runner.bb` — re-ran: ALL TESTS PASSED.
- `test_bl1392_dead_cron_never_silent.sh` — re-ran: 16/16 PASS, including
  the two negative-checks Article invariant 3 demands: "nothing starts,
  restarts or configures a cron daemon" and "`/etc/wsl.conf` is untouched
  by these runs" — the installer only ever REPORTS and names the fix,
  never performs it.
- `bl1392_cron_heartbeat_property_runner.bb` — re-ran: ALL PROPERTIES HOLD
  over 30 constructed cases.
- Invariant 1 (install never lies about success): `CRON_DAEMON_DOWN`
  confirmed printed to stderr by `install_swarmforge_crons.sh`, naming
  the host fix command, with the installer still exiting non-zero — and
  the crontab lines are written regardless, so they fire the instant cron
  returns (read directly, matches the invariant's "still written" clause).
- Invariant 2 (BL-920 self-healing episode state): the daemon sweep logs
  `cron-heartbeat-stale` once per episode past the freshness-log age
  bound, stays silent on a second tick within the same episode, and
  re-arms after the log refreshes — the "aging it again is a NEW
  escalation" row confirms re-arming is genuine, not merely
  once-and-never-again.
- `required_wiring` — both anchors confirmed live: `CRON_DAEMON_DOWN`
  printed in `install_swarmforge_crons.sh`, `cron-heartbeat-stale` logged
  in `handoffd.bb`'s sweep; the step handler discovers via BL-1371.
- `jscpd` over the new handler and the two touched scripts: 0 clones.
- `mutation-site-count.js` on the new step handler: 156 sites (`over`
  100). Reviewed and declined to split, same reasoning as every other
  ticket this session: one cohesive single-feature handler.
- TypeScript compiles clean.
- Babashka/shell have no mutation/CRAP/DRY wired (Startup Tools, BL-472
  deferred); gated by the two runners and the shell suite instead, per
  the documented degraded fallback.

No bounce history on BL-1392 (first pass). Forwarding unchanged to
architect.

By cleaner.
