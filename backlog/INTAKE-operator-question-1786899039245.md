# Intake: a question the Operator could not answer

Filed by the Operator (2026-08-16T16:50:39.245591Z) - a question came in via Telegram
that the Operator judged it could not answer itself. This is a RAW
ask, not a spec: the specifier drains this like any other backlog-root
item and decides what (if anything) becomes a real ticket.

## The question

Human confirmed the two BL-865 approval_context decisions for the browsable knowledge screen: (a) PROMOTE BL-865 so the coordinator can drive minting of the remaining knowledge-screen slices, and (b) CONFIRM the BL-659 retarget — the previously-approved PWA knowledge-explorer becomes the native-Kotlin knowledge-screen slice (Pages PWA goes maintain-only) rather than being retired. Foundation BL-866 (companion-manifest + versioned JSON package catalog) is already landed. Please mint the two remaining slices: (1) Bubble-side: fetch/cache the versioned packages network-first, serve reads offline, show 'as of <generation>'; (2) native Kotlin backlog/docs browse panels over the shared JSON shapes (this is the browsable knowledge screen itself, retargeted from BL-659's PWA corpus goals). Source: front-desk SUP-5, human ldecorps, 2026-08-16.
