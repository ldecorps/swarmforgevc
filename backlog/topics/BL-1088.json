{"id":"BL-1088","messages":[{"seq":0,"ts":1787447240813,"author":"swarm","type":"outbound","text":"BL-1088 — `check-one!` re-arms a given-up child the moment its recorded process is dead, so the 15-minute give-up cooldown all six supervisors declare never applies to the crash-loop case it was written for - and a second test asserts the opposite of the first, so the repo holds two contradictory contracts over one function\nWhat it solves: ORTHOGONALITY - read before promoting. BL-1043 is ACTIVE and edits the SAME\nFILE (`front_desk_supervisor_lib.bb`), though a different function\n(`poll-heartbeat-stale?`, startup grace; this ticket is `check-one!`'s\n`gave-up` branch). Per the constitution's Concurrent Work Orthogonality rule\nthese tw…\nApproval context: The decision that needs a human is WHICH CONTRACT WINS, because the repo currently asserts both and the code cannot satisfy both.\nThe ticket recommends the cooldown: `gave-up` holds the child down for its configured window regardless of the corpse's liveness, and any supervisor wanting faster recov…\nBL-1088 needs your approval before it can proceed. Reply here with \"approve BL-1088\" (or \"reject BL-1088 <reason>\") to act."},{"seq":1,"ts":1787450688115,"author":"swarm","type":"outbound","text":"BL-1088 🎵 in progress — `check-one!` re-arms a given-up child the moment its recorded process is dead, so the 15-minute give-up cooldown all six supervisors declare never applies to the crash-loop case it was written for - and a second test asserts the opposite of the first, so the repo holds two contradictory contracts over one function"},{"seq":2,"ts":1787451955613,"author":"swarm","type":"outbound","text":"BL-1088 🎵 in progress — `check-one!` re-arms a given-up child the moment its recorded process is dead, so the 15-minute give-up cooldown all six supervisors declare never applies to the crash-loop case it was written for - and a second test asserts the opposite of the first, so the repo holds two contradictory contracts over one function"}]}