{"id":"BL-809","messages":[{"seq":0,"ts":1785917617812,"author":"swarm","type":"outbound","text":"BL-809 — worktree-head-commit-10 never captures stdout, so it leaks the hash to the console AND returns nil on every call — three claim-risk severities are unreachable in production\nWhat it solves: Third health-signal defect surfaced in this family on 2026-08-05, alongside\nBL-807 (stuck-in-process WARN ignoring liveness) and BL-804 (babysitter\nrouter false CRITs, closed). All three share a shape worth naming: a signal\nthat reads as live and is not — two crying wolf, this one silent. Worth a\nl…\nApproval context: Flagging the severity, because the coder reported this as a stdout leak and I am speccing it as `high`.\nThe leak is real and is the visible half. The half nobody had seen: the same missing option means the function returns `nil` even when git SUCCEEDS, and the production entry point relies on exact…\nBL-809 needs your approval before it can proceed. Reply here with \"approve BL-809\" (or \"reject BL-809 <reason>\") to act."},{"seq":1,"ts":1785954294180,"author":"swarm","type":"outbound","text":"BL-809 🎵 in progress — worktree-head-commit-10 never captures stdout, so it leaks the hash to the console AND returns nil on every call — three claim-risk severities are unreachable in production"}]}