{"id":"BL-1092","messages":[{"seq":0,"ts":1787445567847,"author":"swarm","type":"outbound","text":"BL-1092 — BL-1039's repo-creation guard keys on the wrapper's NAME (`git(`), not on what the wrapper does, so a test whose helper is spelled anything else creates repositories invisibly\nWhat it solves: Reported by the coder as a `note` on 2026-08-23: \"BL-1039 guard matches wrapper NAME git( only - any other spelling is invisible\". The observation about the regex is correct and is what this ticket carries.\nI did not take the severity from the report. BL-1039 is already in `backlog/done/`, so this …\nApproval context: This one genuinely needs your steer on WHETHER, not just how - I can argue it either way and I would rather you decided.\nFor: BL-1039's invariant 1 (\"no unit-lane test creates a git repository of its own\") is a real speed invariant worth 144.2s, and right now it is enforced by a gate that a rename …\nBL-1092 needs your approval before it can proceed. Reply here with \"approve BL-1092\" (or \"reject BL-1092 <reason>\") to act."},{"seq":1,"ts":1787450382551,"author":"swarm","type":"outbound","text":"BL-1092 — BL-1039's repo-creation guard keys on the wrapper's NAME (`git(`), not on what the wrapper does, so a test whose helper is spelled anything else creates repositories invisibly\nWhat it solves: Reported by the coder as a `note` on 2026-08-23: \"BL-1039 guard matches wrapper NAME git( only - any other spelling is invisible\". The observation about the regex is correct and is what this ticket carries.\nI did not take the severity from the report. BL-1039 is already in `backlog/done/`, so this …\nApproval context: This one genuinely needs your steer on WHETHER, not just how - I can argue it either way and I would rather you decided.\nFor: BL-1039's invariant 1 (\"no unit-lane test creates a git repository of its own\") is a real speed invariant worth 144.2s, and right now it is enforced by a gate that a rename …\nBL-1092 needs your approval before it can proceed. Reply here with \"approve BL-1092\" (or \"reject BL-1092 <reason>\") to act."},{"seq":2,"ts":1787450907303,"author":"swarm","type":"outbound","text":"BL-1092 — BL-1039's repo-creation guard keys on the wrapper's NAME (`git(`), not on what the wrapper does, so a test whose helper is spelled anything else creates repositories invisibly\nWhat it solves: Reported by the coder as a `note` on 2026-08-23: \"BL-1039 guard matches wrapper NAME git( only - any other spelling is invisible\". The observation about the regex is correct and is what this ticket carries.\nI did not take the severity from the report. BL-1039 is already in `backlog/done/`, so this …\nApproval context: This one genuinely needs your steer on WHETHER, not just how - I can argue it either way and I would rather you decided.\nFor: BL-1039's invariant 1 (\"no unit-lane test creates a git repository of its own\") is a real speed invariant worth 144.2s, and right now it is enforced by a gate that a rename …\nBL-1092 needs your approval before it can proceed. Reply here with \"approve BL-1092\" (or \"reject BL-1092 <reason>\") to act."},{"seq":3,"ts":1788082672770,"author":"swarm","type":"outbound","text":"BL-1092 ✅ done — BL-1039's repo-creation guard keys on the wrapper's NAME (`git(`), not on what the wrapper does, so a test whose helper is spelled anything else creates repositories invisibly"}]}