{"id":"BL-1075","messages":[{"seq":0,"ts":1787430161594,"author":"swarm","type":"outbound","text":"BL-1075 — The swarm applies and documents a `window-size largest` mitigation the tiling panel overrides per window\nWhat it solves: ## Provenance\nApproval context: Two calls are worth a look before this runs. First, severity `medium` rather than `high`: the knob is inert and the how-to overstates it, but the defence that actually carries the load on a vulnerable host is the tmux >= 3.7 preference, which is landed and live. Nobody is less safe than they would …\nBL-1075 needs your approval before it can proceed. Reply here with \"approve BL-1075\" (or \"reject BL-1075 <reason>\") to act."},{"seq":1,"ts":1787430828615,"author":"swarm","type":"outbound","text":"BL-1075 🎵 in progress — The swarm applies and documents a `window-size largest` mitigation the tiling panel overrides per window"}]}