{"id":"BL-1593","messages":[{"seq":0,"ts":1789551565955,"author":"swarm","type":"outbound","text":"BL-1593 — BL-1280's whole-tree mkdtemp guard is red on main since hotfix a27d082c2d landed extension/test/nightClosingCeremonyRotateDocumenterFallback.test.js with a raw fs.mkdtempSync at line 29 (one root per test body, never removed); the one call moves to the shared tmpDir helper's per-test variant with the same lifetime, the guard and the fallback test are green, and the register row leaves in the same land\nWhat it solves: ## What is wrong\nApproval context: A new feature file is authored here, so it needs a tap. No choice is posed and there are no ruling_options. FIRM: the fix is the migration BL-1280 completed for every other test file - the fallback test's one `fs.mkdtempSync` call at line 29 becomes the shared helper's per-test variant `mkTmpDir` (…\nBL-1593 needs your approval before it can proceed. Reply here with \"approve BL-1593\" (or \"reject BL-1593 <reason>\") to act."},{"seq":1,"ts":1789552088844,"author":"swarm","type":"outbound","text":"BL-1593 — Three guard tests are red on main since hotfix a27d082c2d landed extension/test/nightClosingCeremonyRotateDocumenterFallback.test.js outside the pipeline: its fixture allocates a root with a raw fs.mkdtempSync at line 29 (one per test body, never removed; the BL-1280 property guard and the BL-420 unit guard) and symlinks every .bb under the live swarmforge/scripts via readdirSync on the checkout root (the BL-1038 live-repo derivation guard); the root moves to the shared tmpDir helper's per-test variant and the scripts dir is built from consult_spawn_cli.bb's derived dependency closure, the three guards and the fallback test are green, and the three register rows leave in the same land\nWhat it solves: ## What is wrong\nApproval context: A new feature file is authored here, so it needs a tap. No choice is posed and there are no ruling_options. AMENDED 2026-09-16 the same morning it was minted, before any tap: QA's hold on BL-1510 (ed1f739525) showed the same hotfix test trips T…\nBL-1593 needs your approval before it can proceed. Reply here with \"approve BL-1593\" (or \"reject BL-1593 <reason>\") to act."},{"seq":2,"ts":1789559307516,"author":"swarm","type":"outbound","text":"BL-1593 🎵 in progress — Three guard tests are red on main since hotfix a27d082c2d landed extension/test/nightClosingCeremonyRotateDocumenterFallback.test.js outside the pipeline: its fixture allocates a root with a raw fs.mkdtempSync at line 29 (one per test body, never removed; the BL-1280 property guard and the BL-420 unit guard) and symlinks every .bb under the live swarmforge/scripts via readdirSync on the checkout root (the BL-1038 live-repo derivation guard); the root moves to the shared tmpDir helper's per-test variant and the scripts dir is built from consult_spawn_cli.bb's derived dependency closure, the three guards and the fallback test are green, and the three register rows leave in the same land"},{"seq":3,"ts":1789566249712,"author":"swarm","type":"outbound","text":"BL-1593 ✅ done — Three guard tests are red on main since hotfix a27d082c2d landed extension/test/nightClosingCeremonyRotateDocumenterFallback.test.js outside the pipeline: its fixture allocates a root with a raw fs.mkdtempSync at line 29 (one per test body, never removed; the BL-1280 property guard and the BL-420 unit guard) and symlinks every .bb under the live swarmforge/scripts via readdirSync on the checkout root (the BL-1038 live-repo derivation guard); the root moves to the shared tmpDir helper's per-test variant and the scripts dir is built from consult_spawn_cli.bb's derived dependency closure, the three guards and the fallback test are green, and the three register rows leave in the same land"}]}