{"id":"BL-1550","messages":[{"seq":0,"ts":1789243710189,"author":"swarm","type":"outbound","text":"BL-1550 — draftPathUnder.property.test.js is flaky red on main with no owner: invariant 2's generator draws `.` as a draft name, so the joined path IS the fixture directory and removeDraftIfPresent's bare unlinkSync throws EISDIR - the helper removes only a regular file and leaves a directory in place without throwing, and the property quantifies only over names that are a strict child of the fixture directory\nWhat it solves: ## What is wrong\nApproval context: A new feature file is authored here, so it needs a tap. No choice is posed and there are no ruling_options. FIRM: removeDraftIfPresent never removes a directory - not with unlinkSync (which throws EISDIR) and not with rmSync recursive (which would delete a tree the sender did not create); a directo…\nBL-1550 needs your approval before it can proceed. Reply here with \"approve BL-1550\" (or \"reject BL-1550 <reason>\") to act."}]}