{"id":"BL-1218","messages":[{"seq":0,"ts":1787875092702,"author":"swarm","type":"outbound","text":"BL-1218 — `config remote_control off` only suppresses auto-inject, so a window line naming `--remote-control` explicitly still launches a Claude remote session the config says should be off\nWhat it solves: - \"[2026-08-28] specifier: operator directives from the source intake,\n  preserved VERBATIM per Article 5.3 (a consolidation never drops a human\n  sentence) — this ticket is one half of a 1:N split, so both directives\n  are carried here in full and again in BL-1217:\nApproval context: Needs sign-off on the collision rule, because there are two defensible answers and the intake explicitly allows either.\nThis slice takes \"config wins, silently\": with `config remote_control off`, an explicit `--remote-control` on a window line is not persisted into the launch script. The alternativ…\nBL-1218 needs your approval before it can proceed. Reply here with \"approve BL-1218\" (or \"reject BL-1218 <reason>\") to act."},{"seq":1,"ts":1788071738811,"author":"swarm","type":"outbound","text":"BL-1218 🎵 in progress — `config remote_control off` only suppresses auto-inject, so a window line naming `--remote-control` explicitly still launches a Claude remote session the config says should be off"},{"seq":2,"ts":1788138356247,"author":"swarm","type":"outbound","text":"BL-1218 ✅ done — `config remote_control off` only suppresses auto-inject, so a window line naming `--remote-control` explicitly still launches a Claude remote session the config says should be off"}]}