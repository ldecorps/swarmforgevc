{"id":"BL-1285","messages":[{"seq":0,"ts":1788055984187,"author":"swarm","type":"outbound","text":"BL-1285 — test_front_desk_supervisor_liveness.sh decides its stall checks by racing a fixed wall-clock cushion, so a loaded host turns three acceptance features intermittently red\nWhat it solves: - [2026-08-30] specifier: `invariants:` is deliberately empty, and this was a\n  close call. \"No verdict depends on elapsed wall-clock time\" is a genuine\n  property over the whole slice, not an example. But a declared invariant\n  obliges the coder to carry an executable PROPERTY test for it in the s…\nApproval context: A new feature file is authored here, so it needs a human tap. The one judgment worth a look before it goes: the gate is DELAY INJECTION (run the fixture with an extra pause before each supervisor check and require the same verdict), not a repeat-under-load run. Delay injection is deterministic, cos…\nBL-1285 needs your approval before it can proceed. Reply here with \"approve BL-1285\" (or \"reject BL-1285 <reason>\") to act."}]}