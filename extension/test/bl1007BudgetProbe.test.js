'use strict';

// BL-1007: tiny probe with an explicit timeout literal so contentionBudgetSetup
// wraps the test and records loadNormalizedDurationMs. Driven only by the
// BL-1007 acceptance step for attribution evidence.

test('BL-1007 budget probe', () => {
  /* intentional no-op */
}, 5000);
