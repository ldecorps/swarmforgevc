# BL-784: every long-running *_supervisor.bb loop must have a matching row in
# daemon_log_freshness.conf. Names only — daemon_log_freshness_registry_guard.sh
# verifies each name is registered before the checker runs.
handoffd
babysitterd
cursor_bridge_supervisor
handoffd_supervisor
front_desk_supervisor
negotiation_relay_supervisor
onboarder_supervisor
bridge_headless_supervisor
operator_runtime_supervisor
