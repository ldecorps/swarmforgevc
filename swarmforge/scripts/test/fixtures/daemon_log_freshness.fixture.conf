# Fixture thresholds for freshness unit tests - pinned so a temporary ops
# raise of the live daemon_log_freshness.conf does not invalidate scenarios
# that assume handoffd|120 (BL-1000). Rows match the shipped live conf;
# staged ages in the shell suites (e.g. 200s) are stale against THESE pins.
# Fields: name|threshold_secs|log_relpath|pid_relpath|start_script
handoffd|120|.swarmforge/daemon/handoffd.log|.swarmforge/daemon/handoffd.pid|start_handoff_daemon.sh
babysitterd|600|.swarmforge/babysitterd/babysitterd.log|.swarmforge/babysitterd/babysitterd.pid|start_babysitterd.sh
